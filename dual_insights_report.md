# Dual-System Insights — 2026-08-25

Generated: 2026-08-25T07:01:44Z
Companion section to `insights_report.md`, generated by this same skill run from the same gather.

Window: 2026-07-26 → 2026-08-25 (30 days). Both halves below read the same 223 runs, 583 session blocks, and 161 substrate entries — the split is in what each signal is taken to be *evidence about*, not in the sources.

---

## REE-side (cognitive substrate)

**Hypothesis space overall — 39 registered questions, 127 hypotheses:** 47 `alive`, 46 `eliminated`, 30 `confirmed`, 3 `split`, 1 `superseded`. **63% of the space is resolved** (80 of 127 in a terminal state). This is the substrate-facing number: it says how much of the declared question space REE's behaviour has actually settled, independent of how fast anyone adjudicated it.

**Competence-floor campaign (`competence_floor`, the one `is_hero: true` question) — DECIDED and CLOSED.**
- 20 hypotheses, **0 alive**: 14 eliminated, 4 confirmed, 2 split.
- The retention `decision_question` was **DECIDED 2026-07-25** (`decision_log.v1.jsonl@2026-07-25T23:26:31Z`, scope `retention_only`): PROCESS-family loci set the decay half-life of a BC-installed competent policy — value estimator (`H-retention-critic` / V3-EXQ-788) and update constraint (`H-retention-consolidation` / V3-EXQ-792a), **each independently sufficient**, restoring retained_fraction from ~0.51 to 0.81–1.84. CONSTITUTION-family loci were eliminated (`H-retention-auxiliary-decay` / 789; `H-consummation-binding` / 821).
- The **installability frontier is also resolved**, corrected 2026-08-08: the z_world non-install finding was VOIDED as a frozen-random-projection instrument artifact; on the corrected instrument trained z_world beats random projection (mean paired AUC delta **+0.626**, 4/6 seeds), and `H-zworld-trained-instrument` is CONFIRMED. The deferred qid split is therefore moot.
- **Re-derive brake: FIRED.** `growth_restriction` reads `CLOSED TO FURTHER FAN-OUT` (2026-08-08 repose) after 5 labelled GOV-FANOUT-1 portfolios grew the denominator 7 → 20. This qid should not receive hypothesis #21; a claim depending on MECH-457/459/460/475/476 must pre-register its own qid instead.
- **Recorded inconsistency (not adjudicated here):** the `growth_restriction` prose refers to "the one still-alive leg, H-consummation-binding", while that hypothesis's `state` field reads `eliminated` and the question tallies 0 alive. Prose and state disagree; the state field is what any tooling reads.

**Conversion-ceiling campaign — still open, and it is the live scientific frontier.**
- `conversion_ceiling_root`: 6 hypotheses, **4 alive** (`H-substrate-ceiling`, `H-f-dominance`, `H-objective-misspecification`, `H-observation-interface`), 2 eliminated, **0 confirmed**. `decidable: false`. Declared distance: **"1 discriminative experiment — the return-decomposition diagnostic (not yet queued)"**. The former live gate V3-EXQ-737 ran and did not discriminate.
- `e3_fdominance_causal_discrimination`: 6 hypotheses, **4 alive** (H1–H4 all undetermined), 2 confirmed. V3-EXQ-925 (2026-08-12) eliminated and confirmed nothing among H1/H2/H4 — the F-lesion moved the score distribution with no consistent direction, eligibility toggling almost never changed the winner, and H3 landed a near-miss (competitor influence 0.086 factual / 0.096 lesioned, both just outside the driver's own 0.05 threshold **on either side**). What it did produce is a **discovered H0: a selector-regime/scale confound** that now dominates the observed pattern. `decidable: false`.
- Substrate side: `f_dominance_conversion_ceiling` carries **26 failure records**, second-heaviest in the queue, and sits `not ready` with no unresolved dependencies — i.e. gated on evidence, not on a missing build.

**High-iteration chains, read as confidence trends on the mechanism** (not as ownership):
- **INV-050 / MECH-180 (EXQ-861, 9 iterations):** 8 FAIL then **861f PASS**. Confidence moved. The 2026-08-23 autopsy adopted H3 as a *substrate/machine delta*, explicitly **not** ledger-confirmed, and the CONTROL passes write-lock as a null. The registry's `inv050_mech180_861e_producer_v...` space still holds **2 alive** (H1 vs H3 on the 861e seed-271 collapse) — so the mechanism is narrowing, not settled.
- **MECH-476 (EXQ-836, 6 FAIL):** fully retired on its own pre-registered falsifier, together with MECH-475, per the 2026-08-08 repose. Six FAILs bought a decisive retirement — confidence *lost*, cleanly.
- **MECH-074d (EXQ-894, 4 FAIL):** `mech074d_context_addressing_c2` now reads 3 eliminated / 1 alive. Narrowing.
- **SD-017 / ARC-045 / MECH-166 (EXQ-436, 5 FAIL b–f):** `sd017_arc045_mech166_slot_diff...` holds **1 alive, 0 resolved**. Five iterations have not yet discriminated anything — this is the chain with the weakest confidence movement per run in the window.
- **MECH-357 (EXQ-603r–u, 4 FAIL)**, **ARC-032 (228b–d)**, **INV-091 (828)**, **ARC-005 (848)**, **MECH-321 (867)**, **MECH-075 (903/905)**: all FAIL-only in window, all autopsied, none yet converted into a confirmation.
- **MECH-449 / ARC-107 (926a, 937, 937a, 937b):** 1 FAIL → 3 PASS, and ARC-107's `promote_to_provisional` is recorded `applied`. The clearest positive-direction chain in the window.

**Ready-and-unbuilt cognitive machinery: 1 of 161.** `scaffolded-curriculum-hazard-rebalance` is the sole substrate entry that is `ready: true` and not implemented — and its own diagnosis of record (2026-08-08, user-confirmed) reads `NO_SUBSTRATE_CHANGE_WARRANTED`. **There is essentially no known-buildable cognitive machinery sitting unbuilt.** 40 further entries are dependency-blocked and 42 are gated on evidence or a governance decision rather than on a build.

---

## REE_assembly-side (governance throughput)

**Autopsy turnaround — the fastest-moving part of the apparatus.**
- 173 autopsy documents landed in the window; 120 have a resolvable target run.
- **Median latency: 0 days.** Mean 2.0 days. Distribution: 0d ×62, 1d ×38, 2d ×11, 3d ×4, 4d ×1, 5d ×1, 7d ×1, plus two backlog catch-ups.
- **Longest-outstanding:** `failure_autopsy_V3-EXQ-512a_2026-08-09` at **97 days** from the run, and `failure_autopsy_V3-EXQ-603e_hold_weighted_entropy_dv_2026-07-29` at 56 days. Both are historical-backlog clearance, not current-flow latency — 100 of 120 cleared within 1 day.

**Decision backlog age — small and genuinely small.**
- 194 decision rows in `promotion_demotion_recommendations.md` (generated 2026-08-23T06:11:34Z): **192 `applied`, 2 `pending_user`** (`Q-094`, `Q-095`, both `hold_pending_v3_substrate`).
- The file's rationale section shows repeated deliberate work to *stop* mechanical `pending_user` re-flagging each cycle (e.g. the MECH-316/317 joint re-affirm, the ARC-106 architectural-commitment reclassification). The backlog is short because it is actively curated, not because little is decided.

**Claim churn — high, and measurable only over ~2 days.**
- `TASK_CLAIMS.json` holds 55 entries total: 51 `done`, 4 `active`. **`prune_task_claims_done.py` drops `done` claims older than 24h**, so this file cannot report a 30-day churn rate — what it shows is a ~2-day window.
- Within that window: **44 claims opened 2026-08-23, 9 on 08-24, 2 on 08-25** — roughly 40+ claims/day at peak.
- **2 active claims are stale by the documented 6-hour rule:** `worktree-agent-a53b125a2ecbdbfa7` at **49.2h** (MECH-314b per-candidate live-path validation) and `nightly-docs-20260824-010335` at **29.8h** (nightly `/update-docs`). Per the standing rule these are *reported, never auto-reaped* — absence is not abandonment. Note the first is cross-referenced by an existing chip, `chip-20260823-taskclaims-orphaned-claim-blocks-mech314bc-validation`.

**Concurrency friction rate — the single largest process cost in the window.**
- **179 of 583 dated session blocks (30.7%) document at least one friction signature.** Roughly one session in three pays a coordination-plane tax.
- Signature counts: `wedge` ×136, `skew` ×34, `throwaway worktree` ×26, `autostash` ×18, `swept` ×17, `read-modify-write` ×16, `index.lock` ×1.
- This is the barrier-analysis metric the framing memo asked for: it counts sessions that *lost work or had to route around contention*, not sessions that merely existed.

**Chip ledger — detection coverage is close to complete; execution is the constraint.**
- 1577 chips lifetime: **1450 `done`, 94 `withdrawn`, 33 `open`.** A 92% resolution rate.
- Of the 33 open, **8 are `chip-scriptscorpus-dlaptop-*` red files** in the umbrella `scripts/` test corpus, 2 are stranded-uncommitted-work chips, 1 is `git-sync NEEDS_HUMAN: REE_Working`, and 1 is a GC sweep — i.e. **12 of 33 open chips are infrastructure debt in the apparatus itself**, not scientific work.
- Every substantive finding this analysis surfaced independently — empty queue, duplicate 861f run, the 944a ERROR, the corpus reds — was **already chipped before this report ran**. The one exception is the stale `pending_review.md` (see `insights_report.md` Recommendation 1).

**Experiment-plane throughput — the apparatus is ticking while the science plane is idle.**
- **`ree-v3/experiment_queue.json` has held `items: []` since 2026-08-23T21:04:19Z — ~34 hours.** Depth ran at only 1–5 items throughout the window.
- **Last experiment result of any kind: 2026-08-24T02:38:56Z** (V3-EXQ-861f, the duplicate). ~28 hours of no results.
- Meanwhile every `REE_assembly` commit since then is `igw-ledger: update` on an hourly cadence, and the IGW workset regenerated at 2026-08-25T01:20Z reporting **237 items, 22 ready, 0 in flight**. The governance apparatus is running at full cadence and producing no scientific content, because there is none to process.

**Literature-pull cadence.**
- 29 session blocks reference a lit-pull in the window, spread 2026-08-11 → 2026-08-24.
- Corpus: 458 entries under `evidence/literature/`; newest `targeted_review_q_095`, `targeted_review_q_094`, `targeted_review_inv_013`.
- The most recent scheduled run (2026-08-24T06:05:46Z) returned **`NONE_AVAILABLE`, correctly** — consistent with `evidence_backlog.v1.json` carrying **0 literature items** (`evidence_needed` is `experimental` on 416 of 417). Whether the channel is drained or simply not emitted by the backlog generator is *not established* and is flagged as a cheap probe, not a finding.

---

## Where the two overlap

Two nodes are slow in **both** senses. For these — and only these — part of the delay is addressable by changing process rather than by doing more science.

**1. `conversion_ceiling_root` / `e3_fdominance_causal_discrimination` — hard problem *plus* an empty queue.**
- *REE-side (hard):* 8 alive hypotheses across the two spaces, 0 confirmed on the root question, both marked `decidable: false`. V3-EXQ-925 discovered an H0 selector-regime confound that has to be controlled before H1–H4 can be discriminated at all. This part is genuinely `complex (probe-gated)` and no amount of process will shorten it.
- *REE_assembly-side (addressable):* both spaces carry an **explicitly pre-registered live gate** — the return-decomposition diagnostic, and the regime-matched re-test — and **neither is queued**, while the queue has sat empty for 34 hours. The science is waiting on an experiment that has already been specified. That gap is throughput, not difficulty.
- The distinction matters for how it gets reported: the ceiling's 26 failure records look like a substrate wall, but the immediate blocker is an unqueued, already-designed diagnostic.

**2. `INV-050` / `MECH-180` (EXQ-861) — hard problem *plus* a governance defect that cost a run.**
- *REE-side (hard):* 9 iterations to reach a PASS, and the underlying space still holds 2 alive hypotheses (H1 vs H3 on the 861e seed-271 collapse). The 2026-08-23 autopsy adopted H3 as a substrate/machine delta and explicitly declined to treat it as ledger-confirmed.
- *REE_assembly-side (addressable):* **861f ran twice** — two manifests, two DB rows, both PASS — because a stale TASK_CLAIMS entry was reaped mid-run (`chip-20260824-exq861f-duplicate-run-stale-claim-reap`). On a chain that already needed 9 iterations, the apparatus spent a tenth run reproducing the ninth. That is pure process cost on the most iteration-expensive chain in the window.

**Explicitly not an overlap case:** `SD-017 / ARC-045 / MECH-166` (EXQ-436) has the weakest confidence movement per run of any chain here — 5 FAILs, 1 alive hypothesis, 0 resolved — but it is among the *best*-served nodes on the REE_assembly side (62 autopsies naming it, a `done` task claim, 2–3 commits in the last 7 days). Its slowness is entirely REE-side. Reading it as a process problem would be the exact error this split exists to prevent.
