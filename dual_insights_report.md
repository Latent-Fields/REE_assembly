# Dual-System Insights — 2026-08-01

Generated: 2026-08-01T12:37:29Z
Companion section to `insights_report.md`, generated by this same skill run — this is the
second run this session (10:44Z, then this 12:37Z refresh), which itself is the first run of
the newly-merged `/dual-insights` skill (subsumed the former `/insights` project skill earlier
today so the `/insights` name is free for Claude Code's built-in usage-analytics command).

---

## REE-side (cognitive substrate)

Unchanged from the 10:44Z run this session — no commit in the last 90 minutes touched
`hypothesis_space_registry.v1.json`, `substrate_queue.json`, or `claims.yaml`'s MECH-457/
F-dominance entries. Restating the two live fronts rather than re-deriving them:

- **Competence-floor campaign (MECH-457 cluster):** still 2 alive hypotheses
  (`H-mech476-dose-response`, `H-mech476-novelty-tagging`), 12 eliminated, 4 confirmed, 2 split
  — of 20. The live front (EXQ-836) is *more* visibly active than at 10:44Z: both its FAIL legs
  (836a, 836d) now sit in the fresh pending_review batch below, still unadjudicated.
- **Conversion-ceiling campaign (F-dominance cluster):** still 4 alive of 6
  (`H-substrate-ceiling`, `H-f-dominance`, `H-observation-interface`, `H-objective-misspecification`).
- **Ready-and-unbuilt substrate:** still 18 entries.

---

## REE_assembly-side (governance throughput)

- **Autopsy turnaround:** unchanged headline (median 1.0 day over the last 30 days) — but the
  **live queue just got materially worse in real time**, which the governance apparatus itself
  is currently narrating: `WORKSPACE_STATE.md` 2026-08-01T12:31Z records a `/governance` cycle
  that was **paused by user instruction before Step 2** — "going to run outstanding
  failure-autopsies elsewhere first, then restart" — after the regen discovered 7 more completed
  experiments had landed underneath it (`pending_review` grew **8 → 15**: 11 FAIL, 4 PASS) and
  that the local checkout was itself stale (`[ahead 2, behind 4]` against origin, requiring a
  merge mid-run). **This is a live instance of exactly the throughput metric this report tracks**
  — the review queue is not merely un-owned right now (per `insights_report.md`'s Recommendations
  this run), it is un-owned *because* the session that would own it deliberately deferred to clear
  an autopsy backlog first. That is a reasonable process call, not a defect, but it means the
  11-FAIL queue's clock is still running with nobody currently on it.
- **Decision backlog age:** still 0 pending (169/169 applied) — the 12:26Z governance regen did
  not touch decisions (it paused before Step 2, i.e. before any decision could be re-derived or
  applied).
- **Claim churn:** `TASK_CLAIMS.json` now holds 84 entries (up from 76), window
  2026-07-29T23:37Z–2026-08-01T12:34Z (~2.5 days, file-retention-bounded as before). 82 done / 2
  active. The claim flagged stale at 10:44Z (`igw-214-proposal-for-mech-203`, then ~39.5h old) is
  now **confirmed stale by the user directly** in the 12:31Z WORKSPACE_STATE entry ("user
  confirmed stale/cleared") rather than just flagged by this report's 6-hour heuristic — a clean
  cross-validation of that rule. One new active claim, `frosty-satoshi-2e7cbc` (opened
  2026-08-01T11:18:56Z, ~1h18m old, not stale), scoped to unrelated E3 re-selection work.
- **Concurrency friction rate:** the 12:31Z WORKSPACE_STATE entry is itself a fresh, self-reported
  friction incident — stale local checkout mid-regen, a blocked `git stash` (auto-mode classifier),
  worked around with a direct merge. Re-scoring the trailing tail against the same signature list
  used at 10:44Z would need a wider window than the last 90 minutes to move the 20/42 figure
  meaningfully; noting the live incident here rather than re-deriving a new ratio from too small a
  sample.
- **Literature-pull cadence:** unchanged.

---

## Where the two overlap

**No new overlap case this run** — EXQ-604c (MECH-314b/314c/Q-044) remains the one confirmed
"hard problem, slowly adjudicated" example from the 10:44Z run; nothing in the last 90 minutes
adjudicated it further either way.

One node is worth flagging as a **near-miss for the opposite shape** — REE_assembly-side friction
with no REE-side hard-science content at all: the 12:31Z governance pause was driven entirely by
process state (a stale checkout, a queue that grew mid-run, a deliberate sequencing choice to
autopsy first) — none of the 7 newly-landed experiments' claims (Q-040, INV-091, MECH-323/324,
MECH-294, ARC-062/MECH-309, SD-036) are themselves flagged as slow or stuck science. This is
governance moving carefully under real concurrent load, not a stalled claim — worth naming so a
reader doesn't conflate "the queue grew and got paused" with "a claim is stuck."
