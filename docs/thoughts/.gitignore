# thought_intake_audit.py's default output (2026-09-23). Regenerated on
# every run from claims.yaml + both thought-intake trees -- nothing in the
# codebase reads a TRACKED copy of either file (checked: serve.py,
# explorer.html, site generators, scripts/, every skill under .claude/skills
# and .agents/skills), so committing it only produced dirt: a session that
# ran the audit per umbrella CLAUDE.md Session Startup step 6a and then
# ended left the checkout dirty for the next session to trip over. Mirrors
# scripts/steward/.gitignore's `reports/` entry -- same shape, same reason.
# `--output-json`/`--output-md` on thought_intake_audit.py still let a
# caller publish a copy elsewhere (tracked or not) on purpose.
state/
