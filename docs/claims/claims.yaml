# ============================================================================
# REE Claims Registry
# ============================================================================
#
# Top-level schema (common fields):
#   id            e.g. INV-001, ARC-003, MECH-057, SD-026, Q-019, IMPL-...
#   title         one-line human-readable summary
#   claim_type    invariant | architecture | mechanism | substrate_design |
#                 open_question | implementation
#   subject       dotted-path topic (e.g. ethics.emergence, attention.dan)
#   polarity      asserts | denies    (optional)
#   status        candidate | provisional | active | retracted | superseded
#   depends_on    list of claim IDs this entry references
#   location      anchor in reference doc (docs/invariants.md, etc.)
#   source        list of source paths
#   notes         free text
#
# Invariant-specific schema (claim_type: invariant) -- enforced by
# scripts/validate_claims.py (see docs/architecture/invariant_types.md):
#   invariant_type            universal | emergent | grey_zone   (MANDATORY)
#   emergent_from             list of SD-/ARC-IDs; required and non-empty
#                             when invariant_type: emergent; must be absent
#                             or empty when universal; optional on grey_zone
#   candidate_emergent_from   optional, grey_zone only
#   pending_substrate_reconfirmation
#                             boolean flag; set true when a substrate in
#                             emergent_from drops below 'active' status
#
# The validator runs in warn-only mode during the audit transition.
# After the audit registry at docs/governance/invariant_classification_audit.md
# is cleared of grey_zone entries (or on explicit governance decision),
# the validator flips to --strict in governance.sh.
# ============================================================================

- id: INV-001
  title: "No explicit ethics module or moral scoring layer."
  claim_type: invariant
  invariant_type: universal
  subject: ethics.emergence
  polarity: denies
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  epistemic_category: derivational
  what_would_answer: >
    VIOLATED by: any component computing a distinct "morality"/"ethics" value
    from inputs not already covered by REE's registered harm/benefit/
    residue/self-other machinery, feeding that value into action selection
    as its own scoring channel. Also violated if the existing
    ethical_scorer/compute_ethical_cost path (e3_selector.py) is extended to
    take genuinely new, non-harm-derived inputs rather than remaining a
    learned projection of harm_eval symmetric with the reality/benefit cost
    heads.

    CONFIRMED by: an architectural audit finding every "ethics"-named
    component resolves to harm evaluation over z_world/z_harm with no
    separate moral-value signal, and REE's prosocial behaviour traces to the
    interaction of already-registered mechanisms rather than a dedicated
    moral-scoring pass. GOV-PROC-1's governance tooling (preflights,
    release-gates) is NOT evidence either way -- it governs the
    development/release process, never feeds a score into the agent's own
    action-selection loop, a categorically different thing from what this
    invariant scopes.

    Standing drift-risk noted 2026-08-07, PARTIALLY RESOLVED same day: the
    two symbols local to e3_selector.py were renamed (self.ethical_scorer ->
    self.harm_cost_fallback_scorer, compute_ethical_cost() ->
    compute_harm_cost_fallback()) -- confirmed zero external callers (grepped
    the full ree-v3 tree), landed on main, verified via
    tests/test_sd016.py (35/35 pass on the cloud hub, including the fallback-
    path test). NOT renamed, by explicit user decision (narrow scope chosen
    over full scope): the `lambda_ethical` E3Config field itself
    (ree_core/utils/config.py) -- it's referenced as a bare kwarg or
    serialized JSON key in 30+ experiment scripts (e.g.
    v3_exq_735_drive_reward_balance_sweep.py, v3_exq_785b_mech463_...py,
    v3_exq_858_arc062_...py) and inside already-completed experiment result
    manifests. A full rename needs a backward-compat alias (existing
    manifests read `"lambda_ethical"` as a literal output key) and, per
    CLAUDE.md's Git Policy, should go through an integration/<slug> branch
    with the contract suite run before merge, not a same-session edit --
    this is exactly the "breaks the fleet if half-landed" class of change.
    Deferred; see WORKSPACE_STATE.md 2026-08-07 for the pointer to pick this
    back up.

    (Digested 2026-08-07, session cool-torvalds-a82359, thought-digestion
    wave 12; naming fix same day, same session.)
  location: docs/invariants.md#inv-001
  source:
    - docs/processed/legacy_tree/docs/invariants.md
    - docs/processed/legacy_tree/README.md
- id: INV-002
  title: "Coherence includes temporal/phase binding, not just static metrics."
  claim_type: invariant
  invariant_type: universal
  subject: coherence.definition
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  epistemic_category: derivational
  what_would_answer: |
    Answered by architectural audit of live coherence-relevant
    computations across the substrate, not by a single formal quantity
    -- this is a GENERAL claim that "coherence" must incorporate SOME
    temporal structure across multiple timesteps, denying that coherence
    could be adequately captured by a purely single-frame/instantaneous
    metric. This is DISTINCT from and BROADER than INV-067 (the SPECIFIC
    formal V(t) verisimilitude definition and its phase-alignment term
    P_i(t)), INV-068 (D_V temporal depth), and ARC-053 (the TCL
    architectural loop) -- those ask whether a SPECIFIC literal
    oscillatory-phase-alignment formalism exists; this claim asks the
    lower-bar question of whether ANY coherence-relevant quantity used
    in a consequential decision incorporates cross-timestep information
    (EMA, rolling window, multi-step trajectory cost, cross-cycle
    vintage), as opposed to being recomputed fresh from a single
    snapshot with no memory. A substrate can confirm this claim while
    INV-067's stricter literal-phase-alignment claim remains
    unbuilt/blocked.

    NON-DEGENERACY PRECONDITION: the audit must find at least one live,
    consequential-decision-facing coherence-or-verisimilitude-like
    quantity actually computed and consumed in the current step loop
    (not merely a config field, a dead code path, or a diagnostic-only
    counter).

    CONFIRMED CURRENT STATE (2026-08-07 digestion pass): three
    independent, live, consequential coherence-relevant quantities
    incorporate multi-timestep information:
      (1) MECH-269 per-stream V_s (ree_core/hippocampal/module.py,
          update_per_stream_vs) is EMA-smoothed across ticks
          (per_stream_vs = (1-tau)*prev + tau*score) and gates a
          consequential decision -- ree_core/regulators/
          vs_rollout_gate.py substitutes held (temporally-stale)
          snapshots for current latents at E1/E2 rollout call sites when
          a stream's temporally-integrated trust score drops.
      (2) ree_core/latent/multi_content_theta_packet.py's
          currency_coherence() is computed from each stream's
          ThetaPacketVintage(is_current, age_ticks) -- age_ticks is
          cycles since the carried value was last current, i.e.
          inherently a cross-tick provenance record. It directly biases
          action selection via compose()/compose_per_candidate_
          coherence() (MECH-294).
      (3) ree_core/predictors/e3_selector.py's compute_reality_cost
          computes coherence_cost as a smoothness term over the FULL
          rollout horizon -- multi-step, not single-frame -- feeding
          trajectory selection cost J.
      Additionally, MECH-089's ThetaBuffer confirms a rolling
      aggregation over E1 outputs before E3 sampling is live and
      measured, while explicitly disclaiming literal oscillatory phase-
      coupling as a requirement in the ANN substrate -- this is the
      weaker, ALREADY-SATISFIED half of INV-002's claim, distinct from
      the stronger literal-phase claim INV-067/ARC-053 test.
      ree_core/latent/cross_stream_binder.py additionally implements a
      literal cos(2*pi*t/theta_period) theta-phase gate on cross-stream
      coupling strength; its FIXED mode measurably lifted coherence-
      specificity (V3-EXQ-641a 1/6 -> V3-EXQ-720 3/6) without clearing
      the full SPEC gate -- this is the closest live approach to
      INV-067's literal formalism and remains genuinely partial,
      consistent with (not contradicting) INV-067/ARC-053's stricter
      finding.

    FALSIFYING evidence: every coherence/representation-quality/
    verisimilitude quantity actually consumed for a consequential
    decision anywhere in the live substrate is found to be recomputed
    fresh from a single timestep's observation with no EMA, no rolling
    window, no cross-cycle vintage/age state, and no multi-step
    trajectory aggregation.

    CONFIRMING evidence (already met, per the audit above): at least one
    live, consequential-decision-facing coherence-relevant quantity
    incorporates temporal/sequential information via EMA, rolling
    window, cross-cycle vintage, or multi-step trajectory cost. This
    does NOT require literal oscillatory phase-alignment -- that is
    INV-067/ARC-053's separate, stricter, currently-blocked claim about
    a specific formalism, not a precondition for this general claim.
  location: docs/invariants.md#inv-002
  source:
    - docs/processed/legacy_tree/docs/invariants.md
- id: INV-003
  title: "Language emerges as functional self-representation, not a bolt-on."
  claim_type: invariant
  invariant_type: universal
  subject: language.emergence
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  epistemic_category: derivational
  what_would_answer: |
    Answered by architectural audit of the live language-production and
    language-consumption code path, not by a trained behavioral experiment
    (this is a MECHANISM question -- HOW language is generated/grounded,
    specifically whether it reads from and writes to the same
    self-representational latent substrate used elsewhere in cognition, or
    is a separate bolt-on module -- not a fluency/competence question).

    NON-DEGENERACY PRECONDITION: the audit must be run against a live case
    where SOME language-producing or language-consuming mechanism actually
    exists and executes -- a code path that either emits symbols/tokens
    from agent state or maps received symbols/tokens onto agent state.
    Checking for "absence of a bolt-on LLM call" in a substrate with no
    language mechanism at all is vacuous: a substrate with nothing that
    produces or consumes language trivially satisfies "not a bolt-on"
    while equally failing to exhibit language-as-self-representation.

    CURRENT STATE: NOT answerable in either direction today. Confirmed by
    codebase search (2026-08-07): no "language", "symbol_channel",
    "communication_channel", or "message_passing" implementation exists
    anywhere under ree_core/. The only two artifacts referencing language
    are non-functional: ree_core/claustrum/control_demand.py's
    LANGUAGE_EXPLICITATION enum label (no implementation behind it) and a
    comment in ree_core/claustrum/coalition_templates.py noting the
    language subsystem "doesn't exist yet in ree_core". This mirrors
    ARC-009's own audit (claims.yaml ARC-009 what_would_answer): ARC-009
    found the same absence and additionally requires a multi-agent
    substrate (ARC-047 SocialGridWorld, unbuilt, v5-gated) plus a discrete
    symbol channel (MECH-014, prose sketch only, no substrate, no queued
    experiment). INV-003 sits above that entire unbuilt subtree as the
    root universal invariant, so no version of this claim is currently
    falsifiable in V3.

    What DOES already exist, and is the designated landing site once a
    language mechanism is built: the self-representational latent
    substrate this invariant requires language to share. z_self is live
    and wired today -- ree_core/latent/stack.py (self_encoder,
    self_topdown, z_self_recur / anchor-coupled self-recurrence),
    ree_core/latent/self_recurrence.py, ree_core/agent.py,
    ree_core/predictors/e3_selector.py, ree_core/comparator/
    tpj_comparator.py. Per the invariant's own elaboration
    (docs/invariants.md#inv-003: "language... emerges as an abstraction of
    joint attention and control state -- i.e., it can represent the
    architecture itself"), the substrate this claim predicts language will
    be grounded in is not hypothetical -- only the language layer itself
    is missing.

    AUDIT TARGET once a language mechanism lands (candidate first site per
    MECH-014/ARC-009: a discrete symbol-emission/consumption channel built
    out from MECH-014's "latent-state summaries / residue flags" sketch):
    trace whatever function(s) turn agent state into emitted symbols, and
    whatever function(s) turn received symbols into agent-state updates.

    FALSIFYING evidence: the traced path shows language production or
    consumption implemented as a SEPARATE, architecturally isolated
    module that does not read from or write to the shared self-
    representational latent state used elsewhere in cognition -- e.g. a
    standalone LLM call, a template-based text generator, or a symbol
    encoder/decoder operating on its own independent state that is glued
    onto the rest of the architecture via a narrow interface rather than
    sharing substrate with z_self/joint-attention/control-state
    representation. Finding this ANYWHERE in the live language path
    falsifies INV-003, independent of whether the resulting language is
    behaviorally fluent -- fluency is not the question this invariant asks.

    CONFIRMING evidence: the traced path shows language emission/
    consumption computed FROM the same self-representational and
    control-state machinery used for the rest of cognition -- e.g. symbols
    are generated by reading out (a function of) z_self / joint-attention
    state / commitment-control state rather than a parallel language-
    specific state, and received symbols update that same shared
    representation rather than a separate language buffer. This confirms
    even if the resulting communication is narrow or task-specific --
    expressive breadth/grammaticality is a separate (unbuilt, v5/v6-gated)
    question this invariant does not require.

    Next step is a code-path audit once ARC-009's two prerequisite layers
    (multi-agent substrate + real symbol-passing channel) land -- not a
    new V3 experiment in its own right, and not answerable before then.
  location: docs/invariants.md#inv-003
  source:
    - docs/processed/legacy_tree/docs/invariants.md
- id: INV-004
  title: "Post-commit consequence traces are persistent, not resettable."
  claim_type: invariant
  invariant_type: universal
  subject: consequence.persistence
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  epistemic_category: derivational
  location: docs/invariants.md#inv-004
  source:
    - docs/processed/legacy_tree/docs/invariants.md
  notes: >
    Resolved universal in Session C batch 2026-04-17. Rationale: the claim is a
    decision-theoretic assertion of action-effect irreversibility, stateable without
    any substrate commitment; the reference doc (docs/invariants.md#inv-004) already
    carries an explicit mechanism-agnosticism note ("does not require residue
    geometry specifically; implementations may realize persistence through residue
    fields, hippocampal/path memory, model updates, or a hybrid"), and "post-commit
    consequence traces" is implementation-level vocabulary rather than
    substrate-dependent content.
  what_would_answer: >
    Asks whether any code path in the REE persistence substrate (the residue field,
    or any future implementation via hippocampal/path memory or model updates) can
    set the record of a genuinely-committed (non-hypothesis-tagged) consequence
    trace back to a state indistinguishable from "never happened" -- i.e. whether
    post-commit history is ever actually erasable/resettable in the running system,
    as opposed to only being definitionally asserted.

    NON-DEGENERACY PRECONDITION: the persistence substrate under audit must
    actually be exercised by real (non-hypothesis-tagged) committed actions across
    at least one subsequent reset/decay-eligible boundary -- an episode reset
    (agent.reset() / env.reset()), a governance rule-closure discharge event (SD-034
    ClosureOperator.discharge_domain), or an offline/sleep integration pass. A
    substrate that is never accumulated into, or never passed through a
    reset/discharge boundary, trivially and vacuously satisfies "not reset" without
    testing anything.

    CONFIRMING: every mutating call site that can act on a committed trace's
    representation (weight/magnitude in the RBF field, the total_residue /
    num_harm_events / harm-history accounting, or an equivalent field in any future
    persistence mechanism) is either (i) strictly monotone-accumulating, or (ii)
    where attenuation exists, is bounded multiplicative decay that is provably held
    away from exact zero/deletion (e.g. SD-034 discharge_domain's factor floor at
    1e-6, never 0.0) and does not touch the global accounting fields. Per-episode
    reset paths (Agent.reset(), env.reset()) explicitly exclude the persistence
    store from their reset scope. An architectural/code audit across
    ree_core/residue/field.py, ree_core/agent.py's reset(), and
    ree_core/governance/closure_operator.py (plus any newly added persistence
    mechanism) that finds no path violating the above is a CONFIRMING result.

    FALSIFYING (VIOLATED BY): discovery of any code path -- present or newly
    introduced -- where a genuinely-committed consequence trace's magnitude,
    weight, or global accounting field is set to exact zero, deleted/popped from
    history, or otherwise made indistinguishable from never having been committed.
    Concrete examples: an episode reset() that clears residue_field.total_residue /
    _harm_history rather than leaving them untouched across episodes; a
    discharge/decay factor permitted to reach 0.0 (full erasure) or applied without
    a documented floor; a sleep/offline consolidation pass that prunes or zeroes
    residue entries rather than only contextualising them; or a new persistence
    mechanism (hippocampal/path memory, model-update log) added without an
    equivalent non-erasure guarantee. Also falsifying: an architecture document or
    notes field that asserts persistence is optional or discardable for a given
    implementation without invoking a documented, reviewed substrate-exemption.

    [2026-08-08 thought-digestion]: already realized architecturally, not merely
    asserted -- ree_core/residue/field.py's ResidueField has no reset/decay-to-zero
    path (accumulate() only adds); Agent.reset() (agent.py:3025-3026) docstring
    reads verbatim "Does NOT reset residue (invariant)" and its body never touches
    residue_field; the sole attenuation mechanism (SD-034 ClosureOperator
    discharge_domain) is documented and coded as bounded multiplicative
    attenuation, factor clipped away from 0.0. Category set derivational rather
    than standard because violation would be caught by code/contract audit, not by
    a statistical experiment.
- id: INV-005
  title: "Harm to others contributes via mirror modelling, not symbolic rules."
  claim_type: invariant
  invariant_type: universal
  subject: ethics.mechanism
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  epistemic_category: derivational
  what_would_answer: |
    Answered by architectural audit of the live harm-computation code path,
    not by a trained behavioral experiment (this is a MECHANISM question --
    HOW other-harm contribution is computed -- not an accuracy/convergence
    question, which is ARC-010's separate substrate-conditional claim).

    NON-DEGENERACY PRECONDITION: the audit must be run against a live case
    where other-harm contribution is actually being evaluated -- some code
    path must take a second agent's state/action as input and produce a
    signal reaching ethical cost / E3 ranking. Checking for "absence of a
    rule table" in dead or unreached code is vacuous: a substrate with no
    other-harm computation at all trivially satisfies "no symbolic rules"
    while equally failing to exhibit mirror modelling.

    CONFIRMED CURRENT STATE (2026-08-07 digestion pass): ree_core/ has NO
    other-agent harm computation of any kind yet. No hits across ree_core/
    for "other_agent", "otherness", "OTHER_SELFLIKE", "shadow_bundle"/
    MECH-031, or a wired "social" mechanism -- only a register-only enum
    value with no implementation (ControlDemandType.SOCIAL_MODEL_CHECK in
    ree_core/claustrum/control_demand.py, explicitly untemplated) and a
    forward-looking comment about "V5 multi-agent social synchronisation
    experiments." ARC-010's own what_would_answer independently confirms
    this (no multi-agent substrate; ARC-047 SocialGridWorld unimplemented,
    confidence 0.0). The audit is therefore CURRENTLY BLOCKED BY
    NON-DEGENERACY -- not answerable in either direction today. Note INV-005
    does NOT require ARC-047's full trained-convergence apparatus to become
    auditable (that is ARC-010's separate, heavier bar): a minimal, even
    untrained, code path computing SOME other-harm signal is sufficient,
    since this is a structural question, not an accuracy question.

    AUDIT TARGET once such a path exists: trace whatever function(s) turn
    "another agent imposed/risked harm" into a signal reaching ethical cost
    / trajectory selection. Candidate landing sites, given the current
    self-harm architecture: ree_core/predictors/e3_selector.py
    (harm_eval/benefit_eval and their E3-ranking consumers),
    ree_core/predictors/e2_harm_a.py and e2_harm_s.py (z_harm_a/z_harm_s
    forward models), ree_core/amygdala/bla.py and cea.py,
    ree_core/latent/stack.py (shared latent stack), and whatever
    ARC-047/MECH-031 machinery eventually lands for OTHER_SELFLIKE tagging.

    FALSIFYING evidence: the traced path terminates in a symbolic/rule-table
    mechanism -- a lookup table, an if/switch keyed on action- or agent-role
    taxonomy, a hand-authored harm-scoring rule set, or any hardcoded
    mapping from (other-agent action type) -> (harm cost) that bypasses the
    agent's own predictive/latent machinery. Finding this ANYWHERE in the
    live other-harm-contribution path falsifies INV-005 -- independent of
    whether INV-001 (no ethics module) otherwise holds, since INV-005 is
    specifically about the MECHANISM by which other-harm enters ethical
    cost, not about modularity.

    CONFIRMING evidence: the traced path shows other-harm contribution
    computed by the SAME predictive/simulative machinery used for the
    agent's own harm (per INV-028: "the self-other distinction is a routing
    difference within the same architecture, not a different architecture")
    -- the other agent's state is projected into the shared self/other
    latent representation (ARC-004 L-space, per ARC-010) and the resulting
    simulated/predicted harm is scored by the existing harm_eval/z_harm_a
    -style forward-model machinery, with reduced precision or different
    routing weights but not a categorically different computational
    primitive. This confirms even if the resulting other-model is
    inaccurate -- accuracy/convergence is ARC-010's separate claim; INV-005
    only requires the MECHANISM be simulative, not that it converge
    correctly.

    Next step is a code-path audit at (or shortly after) whichever build
    first wires an other-agent harm signal into E3 ranking -- likely the
    first ARC-047/SocialGridWorld-adjacent implementation -- not a new V3
    experiment in its own right.
  location: docs/invariants.md#inv-005
  source:
    - docs/processed/legacy_tree/docs/invariants.md
  notes: >
    Resolved universal in Session C batch 2026-04-17. Rationale: the load-bearing
    claim is the simulation-vs-symbolic-rules distinction as the ethical mechanism
    for other-harm contribution -- a decision-theoretic / ethical claim that applies
    to any agent architecture capable of modelling other agents. "Mirror modelling"
    here is used in the general cognitive-science sense (simulative understanding of
    others) rather than as a commitment to a specific ARC-010 implementation; ARC-010
    grounds this invariant at the REE-specific level (docs/invariants.md line 405)
    but does not underwrite its subject.
- id: INV-006
  title: "Post-commit consequence traces cannot be erased, only integrated."
  claim_type: invariant
  invariant_type: universal
  subject: consequence.non_erasability
  polarity: denies
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  epistemic_category: derivational
  what_would_answer: |
    Answered by architectural audit of the live residue/consequence-trace
    write and decay code paths, not by a trained behavioral experiment
    (this is a MECHANISM/STRUCTURE question -- WHETHER any reachable code
    path can zero or delete a real committed-action trace -- not an
    accuracy or convergence question).

    NON-DEGENERACY PRECONDITION: the audit must inspect a live case where
    a real consequence trace has actually been written -- ResidueField.
    accumulate() (or accumulate_benefit / accumulate_safety /
    update_valence) called with hypothesis_tag=False, producing a nonzero
    contribution to total_residue and/or an active RBF center -- AND then
    inspect every code path that can subsequently touch that same state.
    Auditing "erasure-freedom" against a substrate where no real trace was
    ever written is vacuous. Decay/integration (an old trace's influence
    fading via EMA, being reweighted by new evidence, or contextualised
    during sleep) is PERMITTED and does not falsify the claim; only a path
    that sets a real trace's contribution to exactly zero / removes it
    entirely -- indistinguishable from the consequence never having
    occurred -- falsifies it.

    CONFIRMED CURRENT STATE (2026-08-07 digestion pass):
    ree_core/residue/field.py implements this live. ResidueField carries
    no reset/clear/delete/zero_out method. total_residue (field.py:620)
    is only ever incremented, never decremented or reassigned to 0.0
    anywhere in ree_core/. The one mechanism that reduces a written
    weight's magnitude, discharge_domain() (SD-034, field.py:884-972), is
    explicitly non-erasing by construction: multiplicative decay only
    (factor in (0.0, 1.0]), clamped by a hard floor (MIN_FLOOR = 1e-6)
    that keeps weights sign-preserving and strictly away from exact zero,
    scoped to a bounded spatial radius, with valence_vecs left untouched
    -- its own docstring states "weights decay multiplicatively toward
    (but never to) zero." REEAgent.reset() (agent.py:2995-2996), the
    per-episode reset, is explicitly documented and implemented as NOT
    touching residue ("Does NOT reset residue (invariant)") while
    resetting e1 hidden state, theta_buffer, beta_gate, serotonin and
    other per-episode caches -- residue is conspicuously absent from that
    reset body. Sleep consolidation (CrossModuleConsolidator,
    cross_module_consolidation.py, MECH-121/MECH-423 feeding the
    SD-017/MECH-120 sleep cluster) explicitly scopes its MECH-094
    exception as writing to no residue/anchors/memory at all -- the
    offline pass trains module weights only. ResidueField.integrate()
    (field.py:974-997) only trains a side neural_field to regress toward
    existing RBF outputs, leaving rbf_field.weights, total_residue, and
    valence_vecs unmodified. One capacity caveat, not a falsifier:
    RBFLayer's fixed-size circular buffer (next_center_idx wraps mod
    num_centers) can, once num_harm_events exceeds num_basis_functions,
    overwrite an old slot's spatial LOCATION while still ADDING to
    (never replacing) that slot's weight -- this degrades spatial
    attribution resolution under capacity pressure but never zeros a
    contribution or decrements total_residue, so it does not meet the
    falsifying bar above. (A separate familiarity/novelty tracker,
    ree_core/hippocampal/curiosity.py FamiliarityTracker, SD-025, does
    fully zero on reset() -- but it is a purely-spatial visitation
    tracker, not a harm/benefit consequence record, and is out of scope
    for this claim, the same distinction INV-011 drew for
    reset_hidden_state().)
  location: docs/invariants.md#inv-006
  source:
    - docs/processed/legacy_tree/docs/invariants.md
  notes: >
    Resolved universal in Session C batch 2026-04-17. Rationale: the claim is a
    decision-theoretic assertion that past committed-action effects can only be
    incorporated into subsequent state, never reverted -- a non-erasability property
    of any agent in any world with persistent state. The reference doc
    (docs/invariants.md#inv-006) already carries a mechanism-agnosticism note ("an
    invariant about irreversible accountability memory, not about any single storage
    mechanism; residue geometry remains one candidate architectural realization"),
    confirming the universal reading. The integration-vs-erasure dichotomy does not
    require any specific offline-consolidation substrate to be stateable.
- id: INV-007
  title: "Language cannot override embodied harm sensing."
  claim_type: invariant
  invariant_type: universal
  subject: language.constraint
  polarity: denies
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  epistemic_category: derivational
  what_would_answer: |
    Answered by architectural audit of the live language-channel-into-
    harm-signal data flow, not by a trained behavioral experiment (this
    is a MECHANISM question -- WHETHER a symbolic/verbal channel can
    reach and suppress the harm-sensing pathway -- not a question of how
    persuasive or fluent any such language is).

    NON-DEGENERACY PRECONDITION: two channels must coexist before this
    claim is testable in either direction -- (1) a language/symbolic-
    justification mechanism that produces content capable of being
    tested against the harm pathway, and (2) a live embodied-harm-
    sensing channel reaching action/trajectory evaluation that such a
    mechanism could attempt to override. Checking for "absence of
    override" when no language channel exists at all is vacuous.

    CONFIRMED CURRENT STATE (2026-08-07 digestion pass): channel (2) is
    SATISFIED -- z_harm_a/z_harm_s, ResidueField
    (ree_core/residue/field.py), the amygdala BLA/CeA analogue (SD-035)
    and dACC (MECH-258) are confirmed live and wired into the substrate.
    Channel (1) is NOT satisfied: no "language", "symbol_channel",
    "communication_channel", or "message_passing" implementation exists
    anywhere under ree_core/ (identical finding to INV-003's audit, same
    session). The only language-referencing artifact is
    ree_core/claustrum/control_demand.py's LANGUAGE_EXPLICITATION enum
    label, which has no implementation behind it and is referenced
    nowhere outside its own definition and one contract test that merely
    enumerates enum members. This mirrors INV-003's finding exactly: the
    same absent prerequisite blocks both claims, from the same root
    cause.

    AUDIT TARGET once a language/symbolic-justification mechanism lands
    (candidate landing site per INV-003's own audit: a discrete symbol-
    emission/consumption channel built out from MECH-014's sketch):
    trace whatever function(s) let a symbolic/verbal signal reach
    z_harm_a/z_harm_s, their ResidueField/SD-035/MECH-258 upstream, or
    E3's harm-weighted trajectory scoring (ree_core/predictors/
    e3_selector.py).

    FALSIFYING evidence: the traced path shows a language-derived or
    symbolic-justification signal suppressing, vetoing, zeroing, gating
    off, or otherwise diminishing the reach of z_harm_a/z_harm_s into E3
    trajectory evaluation. This is the "Symbolic override" failure mode
    named explicitly in REE_CORE.md §6: "language or ideology suppresses
    harm signals." Finding this ANYWHERE in the live path falsifies
    INV-007, regardless of whether the language content is behaviorally
    coherent.

    CONFIRMING evidence: a language/symbolic-justification mechanism
    exists and is exercised, and the harm signal's reach into E3
    trajectory evaluation is measurably unchanged -- z_harm_a/z_harm_s
    propagate to trajectory scoring at the same magnitude/precision
    regardless of the language channel's content or activation state.

    Next step is a code-path audit once a language mechanism exists
    (same prerequisite INV-003 is blocked on) -- not a new V3 experiment
    in its own right, and not answerable before then.
  location: docs/invariants.md#inv-007
  source:
    - docs/processed/legacy_tree/docs/invariants.md
- id: INV-008
  title: "Precision is routed and depth-specific, not global."
  claim_type: invariant
  invariant_type: universal
  subject: precision.routing
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  epistemic_category: derivational
  what_would_answer: |
    Answered by architectural audit of the live precision-representation and
    precision-application code paths, not by a trained behavioral experiment
    (this is a MECHANISM/STRUCTURE question -- HOW precision is stored and
    applied -- not an accuracy or controllability-authority question, which
    is ARC-005's separate, already-tested claim about whether the control
    plane CAN route precision/mode authority).

    NON-DEGENERACY PRECONDITION: the audit must inspect a live case where
    precision/gain modulation is actually computed and applied to
    representation or error terms -- not merely declared in a config field
    or present in dead/unreached code. A substrate with no precision
    computation at all would trivially satisfy "no single global scalar"
    while equally failing to exhibit routing. A single scalar that is
    merely NAMED per-channel in a docstring but numerically shared across
    channels also does not satisfy non-degeneracy in the confirming
    direction.

    CONFIRMED CURRENT STATE (2026-08-07 digestion pass): ree_core/latent/
    stack.py implements this live. LatentState.precision is a
    Dict[str, torch.Tensor] with five independently-computed channel keys
    (self, world, beta, theta, delta), each backed by its OWN learnable
    nn.Parameter logit (self_precision_logit, world_precision_logit, and a
    separate precision_logit per SharedDepthEncoder instance for
    beta/theta/delta), each applied as an in-channel elementwise gain
    within that channel's own forward() -- never a value shared or
    broadcast across channels. LatentStack.encode() populates this dict
    every call and is invoked live from agent.py:4044 (not dead code).
    compute_prediction_error() then weights each channel's error by that
    SAME channel's own precision term. Separately, e3_selector.py's
    current_precision (ARC-016) is a distinct, single derived scalar -- but
    it is scoped specifically to the E3 commitment/trajectory-selection
    gate and to dACC's affective-PE weighting (cingulate/dacc.py,
    MECH-258), not broadcast to the stack's per-channel encoding gains.
    Caveat: LatentStack.modulate_precision() and
    compute_prediction_error() itself currently have no callers elsewhere
    in ree-v3 -- the fuller tau-scoped cross-channel update/routing
    apparatus described in precision_scoping.md (MECH-003) is defined but
    not demonstrably exercised in the live step loop; what IS live and
    wired is the encode-time per-channel gain computation and application
    described above. The audit below is therefore CURRENTLY ANSWERABLE,
    and reads CONFIRMING -- unlike INV-005 (a structurally similar
    universal invariant), this claim is not blocked by non-degeneracy.

    AUDIT TARGET: trace the live gain-application path from observation to
    applied representation. Primary site: ree_core/latent/stack.py
    (DualStreamEncoder.forward for self/world, SharedDepthEncoder.forward
    for beta/theta/delta, LatentStack.encode() wiring them together).
    Secondary consumer sites where a precision value is read and applied to
    a specific downstream computation, not broadcast globally:
    ree_core/predictors/e3_selector.py (current_precision, ARC-016 dynamic
    precision at the commit gate), ree_core/cingulate/dacc.py
    (_affective_pe, MECH-258), ree_core/predictors/e2_world_uncertainty.py
    (uncertainty-derived precision, MECH-059, explicitly distinct from
    e3_selector's). Any future SD-026/attention-gating wiring (SD-026 is
    currently unbuilt) is a further candidate site, not yet audited here.

    FALSIFYING evidence: the traced live path shows precision/gain
    modulation implemented as a SINGLE scalar (or single tensor of gains
    applied identically across z_self/z_world/z_beta/z_theta/z_delta)
    computed once and multiplied uniformly across the full concatenated
    representation or across all downstream error/consumer terms, with no
    channel independently able to carry a different precision value from
    any other, ANYWHERE in the live gain-application path. Finding
    e3_selector's current_precision (or any similar single derived value)
    applied not just to its own scoped consumer (commit gate, dACC
    affective PE) but ALSO directly gating z_self/z_world/z_beta/z_theta/
    z_delta encoding with no independent per-channel term would falsify --
    i.e., what would falsify is discovering the apparent "channel-specific"
    naming in code is cosmetic and the underlying number is shared.

    CONFIRMING evidence: the traced live path shows precision represented
    and applied as independently-parameterized, independently-computed
    values across at least the encoding channels (self/world/beta/theta/
    delta) AND at least one further, separately-scoped consumer channel
    (E3 commit-gate / affective-PE), such that no single scalar's value
    determines all of them and ablating or changing one channel's
    precision does not move another's. This is the CURRENT confirmed state
    per the 2026-08-07 audit above: five independent nn.Parameter-backed
    per-channel gains in LatentStack, applied locally within each stream's
    own forward pass, plus a structurally distinct E3/dACC precision
    channel scoped to trajectory-commitment and affective-PE weighting
    specifically.

    Next step, if this claim is re-audited later, is to re-check the audit
    target list above for drift (new precision-touching code, e.g. SD-026
    attention/salience gating landing) and to check whether
    modulate_precision()/compute_prediction_error() have since gained live
    callers -- their continued absence is not itself falsifying (the
    encode-time gain is sufficient), but their presence would strengthen
    the confirming finding further.
  location: docs/invariants.md#inv-008
  source:
    - docs/processed/legacy_tree/docs/invariants.md
- id: INV-009
  title: "Attention is precision modulation, not symbolic control."
  claim_type: invariant
  invariant_type: universal
  subject: attention.mechanism
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  epistemic_category: derivational
  what_would_answer: |
    Answered by architectural audit of the live attention-ALLOCATION
    code path specifically -- narrower than INV-008 (which audited
    precision-routing infrastructure generally). INV-009 asks whether
    ATTENTION (selecting/weighting what gets processed or written) is
    realized as a continuous gain/weighting mechanism, or as discrete
    symbolic control (an enum-keyed dispatch or if/switch that
    categorically picks one mode/stream with no gradation).

    NON-DEGENERACY PRECONDITION: the audit must inspect a live,
    actually-exercised attention-allocation path -- not a mechanism
    merely declared in an enum or present in code that nothing in the
    live agent loop calls.

    CONFIRMED CURRENT STATE (2026-08-07 digestion pass): three live,
    actually-exercised attention-relevant mechanisms were traced, all
    continuous:
      (1) SD-016 (status: implemented) -- ree_core's E1 frontal cue-
          indexed ContextMemory query path implements attention
          literally as transformer-style query-key-softmax retrieval:
          q = world_query_proj(z_world), k = key_proj(context_memory.
          memory), weights = softmax(q @ k.T / scale). Continuous by
          construction. (Its empirical DIFFERENTIATION has failed in
          some runs -- V3-EXQ-418e/f found attn_entropy pinned at the
          uniform rail ln(16) -- but that is a performance defect in a
          continuous mechanism, not evidence of a discrete one
          underneath.)
      (2) MECH-261 (status: stable, conf 0.891) --
          ree_core/cingulate/salience_coordinator.py's
          SalienceCoordinator. "Operating mode is implemented as a soft
          probability vector ... not a one-hot categorical."
          operating_mode = softmax(logits, temperature); write_gate(
          target) = sum_mode operating_mode[mode] * gate_weights[
          target][mode] -- continuous, read directly by consumers. A
          discrete current_mode label exists (argmax + hysteresis) but
          is diagnostic-only; write_gate() never reads it.
      (3) INV-008's confirmed per-channel precision gains (LatentStack,
          five independent nn.Parameter-backed channels).

    THE ONE DISCRETE/SYMBOLIC CANDIDATE FOUND, AND WHY IT DOES NOT
    CURRENTLY BEAR ON THIS CLAIM: ree_core/claustrum/{control_demand.py,
    coalition_templates.py, coalition_controller.py} (SD-091/MECH-481)
    implements a genuinely categorical dispatch -- ControlDemandType is
    a 10-member Enum, and request_coalition(demand_type) looks up
    COALITION_TEMPLATES[demand_type], a branch on an enum key, not a
    gradient. This is exactly the kind of mechanism that WOULD falsify
    this claim if it were live. It is not: self.coalition defaults to
    None (config flag off by default), and request_coalition() is never
    called anywhere in agent.py or any other live ree_core module --
    only from the module's own tests and its own falsifier experiment
    driver. The module's own docstrings confirm this is deliberately
    unwired pending a follow-up pass. write_gate() IS called at 3 live
    consumer sites but always against an empty _active list, i.e.
    permanent identity no-ops today. This fails the non-degeneracy
    precondition and is therefore NOT counted as confirming or
    falsifying -- it is a WATCH ITEM: if/when SD-091 steps 4-5 land and
    request_coalition() is ever called from a live classifier, this
    claim must be re-audited, since that would introduce a genuinely
    two-layer structure (discrete classification -> continuous
    execution) this audit has not had to resolve.

    AUDIT TARGET for re-check: ree_core/cingulate/
    salience_coordinator.py (write_gate, operating_mode),
    ree_core/predictors/ (SD-016 query/key/softmax path),
    ree_core/claustrum/ (re-grep for new request_coalition() callers),
    ree_core/latent/stack.py.

    FALSIFYING evidence: a live, actually-called attention-allocation
    path is found where WHICH stream/mode/target receives processing
    resources is determined by a discrete/categorical branch that picks
    ONE target categorically with no continuous gradation available, AND
    that branch is reachable from the live agent loop.

    CONFIRMING evidence: the traced live attention-allocation paths
    (SD-016 softmax retrieval, MECH-261 soft-probability write-gating,
    INV-008's per-channel precision gains) continue to show continuous,
    gradable weighting with no reachable discrete branch determining
    allocation outright. This is the CURRENT confirmed state.
  location: docs/invariants.md#inv-009
  source:
    - docs/processed/legacy_tree/docs/invariants.md
- id: INV-010
  title: "Offline integration exists and is required."
  claim_type: invariant
  invariant_type: universal
  subject: sleep.necessity
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  epistemic_category: derivational
  what_would_answer: |
    INV-010 is the BARE universal-necessity claim: offline integration
    exists across cognitive systems and is architecturally required, full
    stop -- grounded in cross-species biology (e.g. the zebrafish
    sleep-necessity literature already cited in claim_evidence.v1.json),
    with no depends_on and no REE-specific mechanism content. This is
    DISTINCT from its dependent claim ARC-011 ("offline integration is
    required FOR STABILITY [in REE]"), which stakes the REE-specific,
    empirically-testable architectural claim (mediated through ARC-007
    hippocampal replay and ARC-005 control plane) -- see ARC-011's own
    what_would_answer for that empirical falsifier; do not re-run it here.
    [Digestion note, flagged not resolved: the two titles read as
    near-duplicates in wording despite this real distinction in kind --
    worth a governance pass to make the split explicit in one or both
    entries.]

    Answered by literature/derivational argument, not a REE experiment:
    does the biological/computational-cognition literature show ANY
    complex, stable cognitive system (biological or artificial) that
    sustains long-horizon operation WITHOUT some form of offline
    consolidation, or is offline integration a structural universal across
    every studied case? FALSIFYING: a credible counterexample -- a
    cognitive system (biological species, or an artificial agent
    architecture with a comparable operating horizon) demonstrated to
    remain stable indefinitely with zero offline/consolidation phase.
    CONFIRMING: the cross-species pattern holds without exception across
    the literature base already cited for this claim, and no counter-
    example architecture is found.

    Note: REE's own EXQ-430 (SLEEP harm < NO_SLEEP harm, tagged INV-010)
    found no differential effect in 0/3 seeds, but governance's own read
    was "not evidence against INV-010; symptom of missing MECH-261
    mode-conditioned offline-write gating substrate" -- i.e. inconclusive/
    substrate-gated, not falsifying, and in any case this REE-level
    empirical question is ARC-011's falsifier, not INV-010's (a universal
    invariant is not falsified by one architecture's substrate-readiness
    gap).
  location: docs/invariants.md#inv-010
  source:
    - docs/processed/legacy_tree/docs/invariants.md
  evidence_quality_note: |
    EXQ-430 reclassified non_contributory (governance 2026-04-19): C1 (SLEEP harm <
    NO_SLEEP) failed 0/3 seeds with seed 13 anomaly (4.7x variance vs seeds 42/7).
    SWS slots differentiated (C3 3/3) but did not translate to behavioral harm
    reduction -- symptom of missing MECH-261 mode-conditioned offline-write gating
    substrate. SD-032 cluster now IMPLEMENTED 2026-04-19; retest eligibility depends
    on MECH-261 substrate maturity. Not evidence against INV-010 (universal
    invariant; supported by literature).
- id: INV-011
  title: "Imagination must be possible without belief update."
  claim_type: invariant
  invariant_type: universal
  subject: default_mode.safety
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  epistemic_category: derivational
  what_would_answer: |
    Answered by architectural audit of the live simulation/replay
    write-gating code path, not by a trained behavioral experiment (this is
    a MECHANISM/STRUCTURE question -- WHETHER a real write-blocking gate
    exists and is exercised on the belief-update channels -- not a
    convergence or accuracy question).

    NON-DEGENERACY PRECONDITION: the audit must inspect a live case where
    (a) simulation/imagination actually occurs -- DMN/Default Mode replay,
    sleep replay, or E1 internal generation with hypothesis_tag=True -- AND
    (b) a real, executed-experience belief-update channel exists that the
    simulated content COULD reach (residue/phi(z) accumulation, valence
    update, hippocampal RBF/MEL write, chunk formation, novelty/visitation
    buffer advancement). Auditing a gate that is declared but never
    invoked, or auditing a channel that never receives simulated content in
    the first place, would be vacuous in either direction.

    CONFIRMED CURRENT STATE (2026-08-07 digestion pass): ree_core/residue/
    field.py implements this live -- ResidueField.accumulate() (line 581),
    accumulate_benefit() (642), accumulate_safety() (731), and
    update_valence() (763) each take hypothesis_tag: bool = False and
    explicitly refuse to write when True, with docstrings citing "MECH-094
    invariant" directly. ree_core/agent.py wires this from the live step
    loop across dozens of further write sites (MEL consumer writes,
    novelty/visitation buffer advancement, EMA advancement, policy
    chunking, escape-affordance learning, pACC autonomic drive, cingulate
    integration) -- all conditioned on
    getattr(new_latent, "hypothesis_tag", False), all reachable from
    simulate()/sense(), not dead code. This is CURRENTLY ANSWERABLE and
    reads CONFIRMING -- like INV-008 and unlike INV-005, this claim is not
    blocked by non-degeneracy.

    AUDIT TARGET: trace hypothesis_tag from LatentState construction
    through every consumer that could durably update phi(z), valence,
    RBF/MEL memory, or chunk state. Primary site: ree_core/residue/field.py
    (ResidueField.accumulate, accumulate_benefit, accumulate_safety,
    update_valence). Secondary sites: ree_core/agent.py's ~30
    hypothesis_tag call sites gating MEL writes, novelty/visitation
    buffers, EMA advancement, chunking (MECH-322 carve-out excepted, see
    below), escape-affordance learning, and pACC/cingulate drive updates.
    Confirmatory experimental evidence already on file: V3-EXQ-499
    (mech094 hypothesis_tag writegate, discriminative, 2026-04-29,
    supports), V3-EXQ-633 (mech094 simulation/real writegate,
    discriminative, PASS, 2026-06-03, supports), V3-EXQ-880 (arc014
    simulation-mode commitment, PASS, 2026-08-02, supports -- two
    independent channels, rule_state/MECH-319 and residue/MECH-094, both
    confirm commitment/residue suppression under simulation mode vs
    waking-mode commitment).

    FALSIFYING evidence: a live case is found where content generated
    during simulation/imagination (hypothesis_tag=True) reaches and
    durably alters a real-experience belief-update channel -- residue/
    phi(z) accumulation, valence update, hippocampal RBF/MEL write, chunk
    formation, or novelty/visitation state -- WITHOUT going through a
    governed exception path. The one known, audited exception is MECH-322
    (sleep-only ARC-071 chunking, gated on prior REAL-episode value-tag
    plus a replay_origin=True flag with accelerated dissolution if
    uncorroborated) -- its existence does not falsify INV-011, since it is
    itself evidence the simulation/real boundary is actively policed
    rather than absent. What WOULD falsify: hypothesis_tag=True content
    reaching any of the write sites above through an UNGOVERNED path (the
    tag silently ignored, defaulted away, or bypassed outside the MECH-322
    carve-out's own conditions), or the MECH-322 carve-out itself firing
    outside its stated preconditions (waking DMN, non-sleep phase, or
    without the replay_origin audit flag).

    CONFIRMING evidence: the traced live path shows every write-capable
    consumer of simulated/imagined content explicitly checking
    hypothesis_tag (or the equivalent replay_origin/simulation_mode flag)
    and refusing to write when it is set, with the sole exception being
    the bounded, audited MECH-322 path. This IS the current confirmed
    state per the code trace above and per V3-EXQ-499/633/880's PASS
    results -- imagination is demonstrated live to be possible (E1/DMN
    rollout genuinely executes and reaches phi(z)-adjacent read paths for
    attenuated affect) without producing a belief update in the
    real-experience channels.

    Next step, if re-audited later, is to re-check the ~30 agent.py
    hypothesis_tag call sites for drift as new write-capable consumers
    land, and to confirm any future MECH-322-style carve-out registers its
    own bounded conditions rather than loosening the MECH-094 default.
  location: docs/invariants.md#inv-011
  source:
    - docs/processed/legacy_tree/docs/invariants.md
- id: INV-012
  title: "Responsibility arises through commitment, not prediction alone."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - SD-026
  pending_substrate_reconfirmation: true
  subject: commitment.epistemology
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - SD-026
    - MECH-485  # threshold-gated predicted-harm/confidence pipeline; supplies Leg 3's retained-alternative candidates
    - MECH-487  # Leg 3's retention mechanism -- persists WHICH rejected/uncommitted E3 candidate existed past the tick, provenance-tagged (registered 2026-08-07 by the dedicated lit-pull; was the "retention mechanism ... does NOT YET EXIST" named in Leg 3's what_would_answer)
  epistemic_category: substrate_conditional
  what_would_answer: |
    CORRECTION (2026-08-07, same digestion session, user-caught): the
    original version of this what_would_answer called Leg 1 "answerable
    now" without stating a prior precondition it silently assumed --
    flagged by the user mid-session: responsibility-bearing commitment
    presupposes the agent is genuinely CHOOSING AMONG MULTIPLE LIVE,
    DIFFERENTIATED IMAGINED FUTURES, not executing something close to a
    single dominant candidate that merely gets labeled "the commitment."
    If E3's candidate proposal+selection pipeline is degenerate in that
    sense, Leg 1's question (is the ATTENTION-WRITE mechanism
    distinguishable from residue-geometry) becomes moot, because there is
    no genuine choosing for that mechanism to be the mechanism *of*. This
    is now stated as LEG 0 below, ahead of Leg 1, and Leg 1's "answerable
    now" is narrowed to mean "answerable now AS A NECESSARY-BUT-NOT-
    SUFFICIENT question" -- it no longer implies the claim as a whole is
    answerable independent of Leg 0.

    LEG 0 (prior non-degeneracy precondition -- CURRENTLY UNMET, per this
    session's own findings): does E3 select among genuinely graded,
    differentiated candidate trajectories at all, as opposed to a
    near-deterministic pick dominated by one candidate? This is not
    hypothetical -- MECH-439 (the E3 selection-authority mechanism
    directly upstream of any "commitment") is `epistemic_category:
    standard`, `ceiling_decision: exhausted`: 10 confirmed
    substrate_ceiling autopsies found no genuine graded discrimination
    among candidates, and the same F-dominance conversion-ceiling root
    was independently confirmed blocking SD-037 (broadcast override,
    this session), ARC-064 (bottom-up rule discovery, this session, via
    its MECH-309 dependency), and ARC-062 GAP-B, dACC C2, and SD-034
    de-commit. Escalated to a constitutional route (ARC-107/MECH-448/449)
    rather than resolved at the parametric level. Until E3's own
    selection mechanism clears this ceiling -- candidates shown to
    compete on genuinely graded, non-monopolized terms -- "commitment"
    has no differentiated alternative-space to be a commitment relative
    to, and neither Leg 1 nor Leg 2 below can be more than necessary
    conditions. CONFIRMED if a future E3 selection test (post-ARC-107)
    shows graded, gap-concentrated discrimination among candidates
    (matching ARC-106's own load-bearing-vs-decorative test). FALSIFIED
    (for Leg 0 specifically, not for INV-012 as a whole) if E3 selection
    is shown to be structurally incapable of graded discrimination
    regardless of substrate fix -- which would mean INV-012 needs
    re-grounding in a different selection mechanism entirely, not merely
    a wait for ARC-107.

    LEG 1 (derivational, answerable now ONLY as a necessary-but-not-
    sufficient question -- see Leg 0 above, not independently decisive):
    INV-012's ethical content depends on deliberate/willed attention being
    mechanistically distinguishable from reflexive/residue-driven attention
    (ARC-007). SD-026's own notes already stage this argument on five axes;
    axis 5 ("the INV-012 argument itself") is CIRCULAR -- it cites INV-012's
    requirement as evidence for SD-026's distinctness in order to establish
    INV-012's coherence, which assumes the conclusion. The genuine proof
    obligation rests on the remaining four: (i) causality direction
    (retrospective outcome-driven vs prospective intent-driven), (ii)
    neuromodulatory gating (DA-gated vs ACh/NA-gated), (iii) clinical
    dissociation (amnestic vs frontal-lobe syndromes), (iv) representational
    format (episodic specificity vs cross-situational feature generality).
    CONFIRMED if a formal/architectural argument (or, once SD-026 is
    implemented, a code-level inspection) shows the proposed dan_feedback
    channel is structurally distinct from residue-geometry writes on at
    least 2 of these 4 axes (e.g. genuinely separate source signal +
    separate decay constant + no shared write path other than the common
    additive E1-prior register). FALSIFIED if the channel turns out to be
    functionally redundant with ARC-007 on all 4 axes despite the
    architectural label -- e.g. if "prospective vs retrospective" collapses
    to a parameter setting (decay constant) on what is otherwise the same
    write mechanism, or if the neuromodulatory/clinical dissociations
    invoked have no analogue in REE's own implementation. A falsification
    here means "commitment" has no mechanistically real referent and
    reduces to "whatever was already conditioned" -- INV-012 would need to
    be narrowed or retired, independent of any experiment.

    LEG 2 (substrate_conditional, empirical/behavioral, gated): whether the
    distinguishable channel -- once it exists -- actually produces willed
    (goal-directed, non-reflexive) perception is a behavioral question that
    cannot be tested until: (a) SD-014 reaches >= provisional [status as of
    this digestion: still candidate, and the most recent evidence
    (V3-EXQ-887, 2026-08-04, weakens, unreviewed) moves AWAY from the gate,
    not toward it -- FAILED both C1 functional_drive_gating and C2
    representational_separability for the SD-014 node-valence vector on all
    3 seeds]; (b) SD-026 is implemented (currently unbuilt: no manifest
    exercises MECH-251/dan_feedback, no queue entry references SD-026 or
    dan_feedback -- the "do not queue until SD-014 >= provisional" hold in
    SD-026's own evidence_quality_note is being honored); and (c) SD-026's
    own pre-registered five-step validation sequence completes: (i) z_goal
    seeds (SD-012), (ii) z_resource separates (SD-014), (iii) dan_feedback
    enabled/disabled as ablation, (iv) benefit_ratio measured against random
    baseline, (v) feature-channel precision measured on goal-relevant vs
    irrelevant dimensions. CONFIRMED if dan_feedback=on lifts benefit_ratio
    substantially over dan_feedback=off, with goal-relevant feature channels
    showing higher precision than irrelevant ones under dan_feedback=on.
    FALSIFIED if dan_feedback=off performs equivalently (replicating the
    EXQ-085h-l seed-but-inert pattern regardless of ablation state) -- i.e.
    z_goal's behavioral effect, if any, is fully mediated by pathways other
    than the claimed prospective channel, and the "willed vs reflexive"
    distinction has no behavioral signature even if Leg 1's architectural
    distinction holds.

    Non-degeneracy precondition for Leg 2 as a whole: SD-014 >= provisional
    AND SD-026 landed AND steps (i)-(v) above all completed on non-degenerate
    seeds. Until then, Leg 2 is untestable by construction, not merely
    unfavored -- absence of a benefit_ratio lift right now would say
    nothing, because the channel does not yet exist to test.

    LEG 3 (retrospective, substrate_conditional, gated -- added 2026-08-07
    from a same-day user thought, docs/thoughts/2026-08-07_responsibility_
    counterfactual_memory.md, Stage 2: evidence/planning/thought_intake_
    2026-08-07_responsibility_counterfactual_memory.md): responsibility
    additionally requires the agent to (a) retain that a genuinely
    differentiated alternative existed at a past decision point -- not just
    that "some other action was abstractly possible" -- and roughly what it
    was, for the sub-interrupt-threshold cases MECH-485 routes to retention
    rather than to real-time correction; (b) evaluate counterfactually
    whether that alternative would have avoided the harm actually incurred
    or better achieved the goal actually pursued; and (c) attribute the
    resulting outcome to the agent's OWN choice specifically, as opposed to
    circumstance -- tied to sense-of-self machinery (INV-033, second-order
    epistemic access to one's own epistemic states), not a bare
    causal-attribution computation alone (SD-003/MECH-276 already provide
    the causal-attribution half, for training, not the narrative-retention
    half).

    Depends on MECH-485 (the retained-alternative supply -- candidates that
    crossed neither the fast-interrupt threshold nor triggered orient/
    survey, per Q-090's still-open admission-criterion question) AND on a
    retention mechanism for rejected/uncommitted E3 candidates that does
    NOT YET EXIST in ree-v3 in any form: e3_selector.select() discards
    every non-winning candidate every tick; the nearest partial exception,
    SD-033e/MECH-264 (frontopolar counterfactual value), reduces the best
    unchosen alternative to a transient scalar (cfv_now) overwritten every
    tick, with nothing about WHICH alternative existed surviving past the
    tick it was computed on. That retention mechanism is deliberately NOT
    registered as its own claim as of this writing -- an earlier session's
    memory (project_imagination_learning_constraints.md, 2026-05-10) staged
    an adjacent candidate registration for this exact territory and
    explicitly gated it "do NOT register without dedicated lit-pull";
    respected here rather than overridden. ARC-085 (autobiographical event
    tokens, V4/V5, unbuilt) is the nearest candidate eventual host, but its
    current schema has no field for "sibling alternatives considered and
    rejected here."

    CONFABULATION-RISK CAUTION (load-bearing, not decorative -- stated
    explicitly by the user as the central danger of this leg): admitting
    retained/imagined alternative content into this evaluation with the
    wrong weighting is a plausible computational account of a hard-learned
    delusion -- a positive feedback loop reinforcing a belief that was
    never actually tested against reality, distinct from a one-off
    hallucination because it is reinforced into stability by its own
    imagined "confirmations." This is why Leg 3 is explicitly a
    METACOGNITIVE requirement (INV-033), not merely a memory-engineering
    one: distinguishing "this happened" from "this was imagined, and I am
    only using it to evaluate a counterfactual" is a second-order judgment
    about the source and status of one's own mental content. Any future
    retention mechanism must follow the MECH-322 template (bounded,
    audited, provisional-until-corroborated) -- REE's one existing
    precedent for letting imagined content through the MECH-094
    imagination/reality write gate under narrow, audited conditions -- not
    a general relaxation of MECH-094.

    Non-degeneracy precondition for Leg 3: Leg 0 cleared (E3 selects among
    genuinely differentiated candidates -- currently unmet) AND the
    retention mechanism exists and demonstrably retains content past the
    current tick AND retained content remains distinguishable in
    provenance from committed/real content (MECH-094's committed_vs_imagined
    tag) AND retained alternatives show non-degenerate variance in whether
    they would have avoided the harm/achieved the goal (not floor- or
    ceiling-saturated). Until all four hold, Leg 3 is untestable by
    construction, not merely unfavored.

    CONFIRMED if, given the above substrate, an agent that retains and
    correctly counterfactually evaluates rejected alternatives shows
    measurably different self-attribution behavior (narrative/self-report,
    or an internal attribution signal) between "harm followed from my
    choice, a retained better alternative existed" and "harm followed from
    my choice, no better alternative existed" cases, AND this attribution
    does not degrade into elevated confidence for imagined-only
    (never-executed) content in real-experience channels (residue,
    valence, hippocampal writes) -- i.e. the confabulation guard holds
    under test, not merely by design intent.

    FALSIFIED if retained-alternative content cannot be kept
    distinguishable from committed content without a hand-tuned,
    case-by-case gate (no general MECH-322-shaped template suffices) --
    meaning the confabulation risk is not tractably bounded and Leg 3
    should be retired or radically narrowed -- OR if providing retained
    alternatives produces no measurable change in self-attribution/
    responsibility-relevant behavior over an ablated (discard-immediately)
    baseline, i.e. retention supplies no function beyond what SD-033e/
    MECH-264's existing transient scalar already provides.
  location: docs/invariants.md#inv-012
  source:
    - docs/processed/legacy_tree/docs/invariants.md
  notes: >
    Formalised as the first emergent invariant 2026-04-17 (Session A of
    invariant-types governance cycle, plan doc
    docs/thoughts/2026-04-17_invariant_types_governance.md, architecture
    doc docs/architecture/invariant_types.md). INV-012's coherence
    requires a mechanism by which deliberate/willed attention can be
    distinguished from reflexive/residue-driven attention. Without such a
    mechanism, "commitment" reduces to "whatever was already conditioned,"
    and the invariant loses its ethical content. SD-026 (prospective
    precision template write channel, DAN analogue) is that mechanism.
    pending_substrate_reconfirmation=true applied retroactively per Q4
    resolution: SD-026 is currently 'candidate', so the flag fires until
    SD-026 reaches active or is reclassified by governance. Flag is
    orthogonal to status: INV-012 remains active but cannot be cited as
    supporting evidence for new claims until substrate reconfirmation.
- id: INV-013
  title: "Cognition is predictive, iterative, and multi-timescale."
  claim_type: invariant
  invariant_type: universal
  subject: cognition.structure
  polarity: asserts
  status: active
  epistemic_category: derivational
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  location: docs/invariants.md#inv-013
  source:
    - docs/processed/legacy_tree/docs/invariants.md
  what_would_answer: >
    INV-013 asserts three conjunctive architectural properties of REE's cognition:
    (1) PREDICTIVE -- the substrate forms forward models rather than reacting
    purely to immediate observation; (2) ITERATIVE -- action/latent selection
    proceeds through repeated refinement rather than a single forward pass; (3)
    MULTI-TIMESCALE -- processing is stratified across more than one update rate,
    not a single flat clock. This is an architectural-commitment audit, not a
    behavioral measurement -- the deeper question of whether each property is
    GENUINELY realized (vs. merely labeled) is already delegated to sharper
    dependents: ARC-001 (is E1's persistence real, not just a persistent-looking
    API), ARC-002 (is E2's prediction actually affordance-conditioned, not just
    world-dynamics-conditioned), and ARC-004 (is L-space actually
    timescale-stratified, not just role-stratified at a single shared rate).
    INV-013 only certifies that a mechanism for each of the three properties
    exists in the running substrate.

    NON-DEGENERACY PRECONDITION: at least one code path per property must be
    exercised by the live agent loop (not dead code, not test-only scaffolding,
    not a docstring-only claim). A property satisfied only in a module that is
    never called from Agent.step()/Agent.plan() would make the audit vacuous.

    CONFIRMING (per property, all three required):
      - PREDICTIVE: at least one forward-model component that outputs a
        prediction of a FUTURE state/observation/affordance conditioned on a
        current state+action, consumed by the live decision loop -- e.g.
        ree_core/predictors/e1_deep.py's LSTM-based slow world model and
        ree_core/predictors/e2_fast.py's world_forward()/predict_next_state(),
        both wired into Agent.step().
      - ITERATIVE: at least one selection/refinement mechanism that produces a
        candidate, scores it, and revises the candidate set across more than one
        pass within a single decision -- e.g. the CEM-style proposal loop in
        ree_core/agent.py with stuck-state-gated widening via
        difficulty_gated_proposal_entropy.py.
      - MULTI-TIMESCALE: at least two update rates coexist and interact -- the
        within-episode fast/slow split (E2 vs. E1) AND the cross-episode split
        via ree_core/sleep/ operating at a slower-than-tick cadence.
    A code audit finding at least one live instance of each, wired into the
    executed agent loop, is CONFIRMING for INV-013 at the architectural level.

    FALSIFYING (VIOLATED BY): removal or dead-coding of the only mechanism
    realizing one of the three properties without a replacement -- e.g. E1 and
    E2 both deleted/bypassed with action selection driven purely by a stateless
    reactive policy; the CEM/proposal-refinement loop replaced by a single
    greedy argmax with no resampling pass; or L-space collapsed to a single
    shared-rate latent with sleep/consolidation removed entirely.

    Note (non-degeneracy caveat, per ARC-004's own audit): L-space's
    LatentStack.encode currently smooths z_beta/z_theta/z_delta with a single
    shared EMA constant (alpha_shared=0.3) -- so MULTI-TIMESCALE here is carried
    jointly by L-space's recursive structure AND the online/offline split, not
    by L-space's per-layer clock rate alone. Does not falsify INV-013, which
    only requires SOME multi-timescale mechanism.
  evidence_quality_note: |
    [2026-08-21 governance, gov-20260821-0203, confirmed failure_autopsy_V3-EXQ-942_2026-08-21.json]: V3-EXQ-942 (diagnostic, INV-013-tagged) self-routed substrate_not_ready_requeue because worst-seed eval steps were 899 vs a 3000 floor (EXQ-019's unmet C4, copied as MIN_STEPS_PER_SEED). That self-route is REJECTED as a C1 veto: seeds 11 and 23 each recorded 3750 steps (full 25x150, no early done); all three variance floors met (worst var_dz_world 8.21e-5 vs 1e-8); half-lives and lag-k autocorrelations are finite on every seed including seed 37 (n_dz=874 vs a 12-sample computational floor at MAX_LAG=10). Recomputing the driver's _monotonic_verdict from the manifest's own per_seed cells gives monotonic_ordering_confirmed=false on all three seeds (mean tau_e1 4.33 > tau_e2 3.00 > tau_e3 2.67 -- inverted vs designed tau(E1)<tau(E2)<tau(E3)) and still false if seed 37 is dropped (3.5, 3.0, 2.5). Unlike V3-EXQ-939, the 3000 floor is reachable (2/3 seeds cleared it) and is a real seed-37 survival under-run (~36 steps/episode), not a lattice-defective gate; it is still the wrong reason to void C1, and criteria_non_degenerate.C1 is hardcoded false in the P0NotReady branch rather than computed from check_degeneracy. This does not test INV-013 (existence audit; realised-vs-labeled delegated to ARC-001/002/004). Empirical half agrees with the three pre-run structural findings (e2_tick dead, LatentStack re-encoded every step, E3 precision mutated every env tick). e3 tick counts exceed N/10 because of MECH-091 phase reset (572/633/354 vs expected 375/375/90). is_committed never flipped (n_runs=1 on every seed). Same-question re-queue declined (mystery: known data). INV-013 STANDS.



- id: INV-014
  title: "Representation and regulation are strictly separated."
  claim_type: invariant
  invariant_type: universal
  subject: architecture.separation
  polarity: asserts
  status: active
  epistemic_category: derivational
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  location: docs/invariants.md#inv-014
  source:
    - docs/processed/legacy_tree/docs/invariants.md
  what_would_answer: >
    Asks whether any code path in ree_core where a regulation/control-plane
    computation (control gains, precision/gating, neuromodulator-analog state,
    policy commitment) can directly mutate representation-layer state/weights
    (the latent stack: z_world, z_self, z_goal, and their encoders) outside a
    defined, typed interface -- or, symmetrically, whether any representation-
    computing module directly implements regulatory/gating logic -- as opposed
    to the two pathways composing only through bounded, typed cross-axis
    coupling (per the invariant's own interpretation note: "not a claim of full
    functional isolation").

    NON-DEGENERACY PRECONDITION: there must be at least one identifiable
    representation-computing module (ree_core/latent/: zworld_p0.py, stack.py,
    cross_stream_binder.py, theta_buffer.py, self_recurrence.py,
    multi_content_theta_packet.py) and at least one identifiable regulation-
    computing module (ree_core/regulators/, ree_core/policy/,
    ree_core/neuromodulation/) that are both actually exercised in the live
    agent loop. A substrate where regulation code never touches representation
    output at all trivially satisfies "no violation" without testing the
    boundary the invariant asserts.

    CONFIRMING: every regulation-computing module's interaction with
    representation state is READ-ONLY on representation content, optionally
    gated by an explicit stop-gradient/detach boundary, and any influence
    regulation has runs only through a named, typed interface -- e.g. a
    config-level gain (SerotoninModule modulating GoalConfig.z_goal_seeding_gain
    / valence_wanting_floor), a plasticity/write-depth modulator on a defined
    write pathway, or a detached read for downstream selection
    (policy/model_disagreement.py's explicit "stop-gradient on z_world inputs,
    do NOT propagate into the encoder"). Symmetrically, representation-computing
    modules contain no gating/commitment/precision-weighting logic of their own.

    FALSIFYING (VIOLATED BY): any code path where a regulation/control-plane
    computation directly assigns into, overwrites, or backpropagates gradient
    into representation-layer state other than through a named, reviewed
    interface. Concrete examples: a regulator/policy module setting
    z_world/z_self/z_goal directly rather than reading it; a neuromodulator
    module writing into latent-stack content rather than a separately-named
    gain/config field; removal of an existing stop-gradient boundary without a
    documented, reviewed replacement. Also falsifying in the other direction: a
    representation module implementing its own commitment/precision-gating
    logic rather than delegating to regulators/policy.
- id: INV-015
  title: "Ethics emerges from constraint, not optimisation."
  claim_type: invariant
  invariant_type: universal
  subject: ethics.emergence_mechanism
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  location: docs/invariants.md#inv-015
  source:
    - docs/processed/legacy_tree/docs/invariants.md
- id: INV-016
  title: "Stability is prioritized over maximal performance."
  claim_type: invariant
  invariant_type: universal
  subject: design.priority
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  location: docs/invariants.md#inv-016
  source:
    - docs/processed/legacy_tree/docs/invariants.md
- id: INV-017
  title: "Runaway behavior is a control failure, not representational."
  claim_type: invariant
  invariant_type: universal
  subject: failure.classification
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  location: docs/invariants.md#inv-017
  source:
    - docs/processed/legacy_tree/docs/invariants.md
- id: ARC-001
  title: "E1 is the persistent predictive substrate."
  claim_type: architectural_commitment
  epistemic_category: standard
  subject: E1.persistent_predictive_substrate
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-013
    - ARC-004
    - ARC-005
  what_would_answer: |
    ARC-001 asserts E1 functions as a genuinely PERSISTENT predictive substrate --
    carries information across a boundary a non-persistent/reset predictor would
    lose it at. Never tested as its own claim (0 genuine_exp entries; sole
    evidence is an indirect macaque-V4 literature analogy, not a direct
    persistence measurement). Assumed via 23 dependent claims, not measured.

    NON-DEGENERACY PRECONDITION: E1's fast component (LSTM hidden state) is
    explicitly and unconditionally cleared at every episode boundary via
    reset_hidden_state() (agent.py:3012, :9390) -- so "E1 persists" is NOT
    vacuously true of the whole module. The asserted-persistent component is
    specifically ContextMemory's 16-slot associative bank, which
    reset_hidden_state() never touches and which is separately offline-gated
    during sleep (MECH-120/SD-017). A genuine test requires (a) a real
    before/after boundary, and (b) a stateless comparator that ALSO zeroes
    context_memory.memory at that boundary, so persistence is measured
    against a matched non-persistent baseline.

    MEASUREMENT: write distinguishable content into context_memory during
    episode N, let reset_hidden_state() fire normally, measure retrieval at
    the first few post-boundary steps in episode N+1 against a zeroed-memory
    ablation at the same boundary. Reuse existing readouts (cosine similarity
    already computed in _get_context_memory_code_contributions, or the
    MECH-216 schema-salience head) rather than inventing new instrumentation.
    Repeat across the sleep/consolidation boundary using the offline-mode
    gate as the ablation lever.

    ANSWER "persistent" (PASS) if the memory-intact condition shows
    above-baseline retrieval in the first few post-boundary steps (>= 0.8 SD
    of the seed-to-seed delta), while the zeroed-memory ablation shows none.

    ANSWER "not functionally persistent" (FAIL) if there is no measurable
    advantage over the zeroed-memory baseline -- would falsify "persistent
    predictive substrate" as functionally distinguishing E1 from a stateless
    predictor, and would prompt review of the 23 dependent claims.
  location: docs/architecture/e1.md#arc-001
  source:
    - docs/processed/legacy_tree/docs/architecture/e1.md
    - docs/processed/legacy_tree/architecture/E1.md
- id: ARC-002
  title: "E2 is the fast forward predictor of affordances."
  claim_type: architectural_commitment
  epistemic_category: standard
  subject: E2.fast_forward_predictor
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-013
    - ARC-004
    - ARC-001
    - ARC-005
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: the test environment must have genuine
    context-dependent action viability -- some actions available/effective only
    in some states, not others (e.g. causal_grid_world.py's per-tick
    consummatory-act affordance flag, gated on resource-zone entry). Without
    this, "E2 predicts affordances" collapses into the untestable claim "E2
    predicts world dynamics" -- there is no affordance structure distinct from
    raw transition dynamics for E2 to get right or wrong.

    CONFIRMING: E2's action-conditioned forward predictions (world_forward /
    action_object, ree_core/predictors/e2_fast.py) discriminate viable from
    non-viable actions at a given state above a naive baseline -- e.g. a
    readout over predicted world-effect (reusing the action_object o_t
    representation, or the per-action-class viability-index pattern already
    built for the narrow escape domain in
    ree_core/pfc/e2_escape_affordance_linker.py, generalised beyond
    threat/escape) correlates with ground-truth action viability/success at a
    state, exceeding a baseline that assumes all actions are equally viable.
    A weaker but still confirming form: cand_world_pairwise_dist /
    world_forward_contrastive_loss (SD-056) shows predicted-outcome divergence
    across actions that tracks true viability differences, not just generic
    state drift.

    FALSIFYING: E2's action-conditioned predictions show no advantage over a
    uniform-viability baseline (predicted outcome/viability uncorrelated with
    which actions are actually available/successful at a state) -- i.e. E2
    behaves as a generic world-dynamics predictor that happens to be indexed
    by action, without encoding "what am I able to do here" as a structure
    distinct from "what happens next." This would not falsify E2 as a forward
    predictor (ARC-001/E1-adjacent claims are unaffected) -- it would falsify
    specifically the "of affordances" qualifier in ARC-002's title, meaning
    the affordance-manifold language in e2.md sec 3 should be revised to plain
    outcome-prediction language.
  location: docs/architecture/e2.md#arc-002
  source:
    - docs/processed/legacy_tree/docs/architecture/e2.md
    - docs/processed/legacy_tree/architecture/E2.md
    - docs/thoughts/2026-02-09_starting_with_sensory_streams.md
    - docs/thoughts/2026-02-09_e2_hpc_interface.md
- id: ARC-003
  title: "E3 selects and commits trajectories."
  claim_type: architectural_commitment
  subject: E3.trajectory_commitment
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-003@2026-02-25T16:56:17.901452Z"
      as_of: 2026-02-25
      verdict: retain_ree/applied
  last_reviewed: 2026-02-25
  lifecycle_stage: adjudicated
  adjudication_outcome: retain_ree
  adjudicated_at_utc: 2026-02-25T16:56:17.901452Z
  adjudication_decision_status: applied
  adjudication_recommendation: retain_ree
  depends_on:
    - INV-012
    - ARC-001
    - ARC-002
    - ARC-004
    - ARC-005
  epistemic_category: standard
  what_would_answer: |
    ARC-003 is CONJUNCTIVE -- "E3 SELECTS" and "E3 COMMITS" are separate assertions
    with OPPOSITE readiness. Digested 2026-07-21 by splitting on testability.

    LEG A -- SELECTION (testable now, COMMITMENT-FREE).
    ANSWER "E3 selects" if the E3 score gradient over the candidate set is non-flat
    and predicts the taken candidate above chance, AND lesioning the E3 contribution
    to the score collapses selection toward the candidate-generation prior.
    ANSWER "E3 is a pass-through, not a selector" (FAIL) if candidate choice is
    statistically indistinguishable from the proposer's prior. MECH-060/061/062 and
    the tri-loop gating claims would then lose their premise.
    NON-DEGENERACY: the candidate pool must be DIVERGENT (cand_world_pairwise_dist
    above floor -- the GAP-A guard). Scoring a pool of near-identical candidates
    tests nothing and would manufacture either verdict. Measured at the scoring
    level; requires NO sustained execution.

    LEG B -- COMMITMENT (BLOCKED; not queueable as of 2026-07-21).
    ANSWER "E3 commits" if a selected trajectory persists across >= N steps under a
    perturbation that would flip an uncommitted selector, with a measurable
    release/switch cost (hysteresis).
    This leg requires SUSTAINED MULTI-STEP ACTION COMMITMENT, and the basal-ganglia /
    action-commitment layer is under active construction (ARC-107 BG-selector
    constitution; MECH-448 demotion lever provisional; MECH-449 substrate_conditional;
    commit/release-duration falsifiers 460j/485i/654h pending). A behavioural FAIL run
    now would RE-DERIVE THE F-DOMINANCE CONVERSION CEILING ("a navigable landscape
    does not convert to committed action because the selector drowns it"), NOT
    adjudicate ARC-003 -- a confound, not a verdict.
    RELEASE CONDITION: ARC-107 BG-selector constitution landed AND the
    commit/release-duration levers (460j / 485i / 654h) returned.
  location: docs/architecture/e3.md#arc-003
  source:
    - docs/processed/legacy_tree/docs/architecture/e3.md
    - docs/processed/legacy_tree/architecture/E3.md
    - docs/thoughts/2026-02-09_starting_with_sensory_streams.md
    - docs/thoughts/2026-02-24_prefrontal_primitives.md
    - docs/thoughts/2026-02-25_task_loop_extraction_and_latent_field_ethics.md
- id: ARC-004
  title: "L-space is a multi-timescale latent stack."
  claim_type: architectural_commitment
  epistemic_category: standard
  subject: L_space.latent_stack
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-013
    - INV-002
  what_would_answer: |
    ARC-004 asserts that L-space's z_beta/z_theta/z_delta layers are genuinely
    stratified by TIMESCALE, not merely by role/information-content within a
    single-rate recurrence. This has never been tested as its own claim (absent
    from claim_evidence.v1.json) -- it is currently assumed via 36 dependents'
    architecture, not measured.

    NON-DEGENERACY PRECONDITION: as-built, LatentStack.encode smooths z_beta,
    z_theta, and z_delta with a SINGLE SHARED EMA constant (alpha_shared = 0.3,
    hardcoded, ree_core/latent/stack.py) -- there is no differential clock rate
    across the three layers. So the claim is NOT vacuously true by construction;
    any timescale differentiation must emerge from the recursive top-down
    architecture (z_delta reads only z_theta's history, z_theta reads only
    z_beta's, so information at each level is progressively more temporally
    aggregated) rather than from a built-in rate split. A genuine null (no
    differentiation) is possible and must be measurable, not assumed away.
    Precedent for taking this seriously: MECH-058 asserted an adjacent
    "timescale separation" framing (for the z_self/z_world split) and was
    directly tested in V3-EXQ-019 via lag-k autocorrelation -- it FAILED and
    the claim was retired (superseded by MECH-069, "incommensurable domains,"
    not timescale). ARC-004 could suffer the same fate.

    MEASUREMENT: log z_beta(t), z_theta(t), z_delta(t) trajectories over an
    evaluation rollout (no code change needed -- reuse V3-EXQ-019's lag-k
    autocorrelation / persistence-half-life methodology per layer instead of
    per z_self/z_world). Optionally corroborate with a per-layer decomposition
    of MECH-423's inference_convergence readout (currently a single relative-
    delta over the concatenated [z_beta,z_theta,z_delta] vector -- would need
    unpacking by layer, a small extension since the per-layer tensors already
    exist inside LatentStack.encode's settling loop).

    ANSWER "multi-timescale" (PASS) if the layers show a monotonic ordering of
    effective persistence -- z_delta autocorrelation half-life > z_theta's >
    z_beta's -- by a margin exceeding cross-seed noise (>= 0.8 SD of the
    seed-to-seed half-life delta, matching the effect-size convention used
    elsewhere in this registry), AND (if measured) MECH-423 per-layer
    convergence rates show the same ordering (z_delta settles slowest).

    ANSWER "layers differ in content, not timescale" (FAIL) if autocorrelation
    half-life is statistically indistinguishable across z_beta/z_theta/z_delta
    despite their different informational roles -- i.e. the architecture
    nominally stratifies by timescale but in practice all three layers carry
    functionally identical temporal persistence. This would not remove the
    layers' functional roles but would mean "multi-timescale" is the wrong
    description of why they work, structurally analogous to what happened to
    MECH-058, and would prompt a supersession review of ARC-004's framing.
  location: docs/architecture/l_space.md#arc-004
  source:
    - docs/processed/legacy_tree/docs/architecture/l_space.md
    - docs/processed/legacy_tree/architecture/latent_stack.md
  implementation_note: >
    2026-06-12 (MECH-423 readiness substrate): the shared z_beta/z_theta/z_delta
    stack gained an OPTIONAL iterative-inference settling readout (R2) --
    LatentStack.encode generalises its fixed single top-down round into a
    predictive-coding settling loop over the shared latent and exposes a
    per-inference-step ||delta z_shared|| convergence dict on
    LatentState.inference_convergence (use_iterative_inference, default OFF,
    bit-identical). Pure instrumentation over the existing ARC-004 machinery; no
    status/confidence change. See
    docs/architecture/mech_423_superadditivity_readiness_substrate.md +
    ree-v3/CLAUDE.md "MECH-423 readiness substrate".
- id: ARC-005
  title: "Control plane routes precision and modes."
  claim_type: architectural_commitment
  subject: control_plane.precision_routing
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "v3_exq_848b_arc005_precision_only_finer_ladder_20260804T064758Z_v3#failure_autopsy_2026-08-05_pending_review_batch"
      as_of: 2026-08-05
      verdict: "supports (846, occupancy) + mixed (848b, precision, final reading for this design) -- see evidence_quality_note"
  depends_on:
    - INV-008
    - INV-009
    - INV-014
    - ARC-004
  epistemic_category: standard
  evidence_quality_note: >
    [2026-08-01 governance, V3-EXQ-846, evidence_direction=supports, overall_pass=True]:
    ARC-005's own disjunctive test design (>=1 channel demonstrates causal occupancy authority)
    is met by ONE of four proposed channels (MODEPRIOR: authority_mean=1.0, ablating it fully
    reverts the argmax to baseline -- retained_effect_mean=0.0). But per-channel results are
    strongly asymmetric, worth recording rather than letting the flat PASS obscure it: the other
    three channels (5HT, PHASIC, and PCC per the criteria table) all show ZERO measured authority
    (authority_mean=0.0, retained_effect_mean=1.0 -- ablating them changes the argmax not at all).
    Readiness preconditions all met (n_distinct_argmax_modes >= 1.5 threshold, n_salience_ticks
    >= 150, channel_ablation_component_delta clean). Reading: ARC-005 is an architectural-
    commitment claim (the control plane CAN route precision/mode authority), and one clean channel
    demonstrating full causal authority satisfies that existence claim -- accepted as supports per
    governance walk 2026-08-01. The 3-of-4-channels-inert asymmetry is real and should inform any
    future per-channel claim about WHICH channels route authority (a narrower question than ARC-005
    itself asks). No status change (already active). PROMOTES/DEMOTES NOTHING.
    [2026-08-01 governance, V3-EXQ-848, confirmed failure_autopsy_V3-EXQ-848_2026-08-01,
    successor of V3-EXQ-802]: scoped successor decoupling the confounded joint-ladder design
    (802) into precision-relevant channels only (1: 5-HT rigidity, 2: phasic-burst temperature;
    channels 3/4 pre-registered as occupancy-only, correctly excluded). A real substrate bug in
    channel 1 (SD-057 L7 dACC-consume reading the collapsed proposer starting state) was found
    and fixed this session; this run is the first to exercise the fix. C_precision_monotonicity
    (load-bearing, |Spearman rho| >= 0.60 required in >=7/10 units): only 4/10 satisfied. But the
    per-unit rho values are SCATTERED (-0.5 to 0.866), not 802's own clean bit-identical rho=0.0
    -- so evidence_direction is `mixed`, not non_contributory. Two pre-registered, already-
    diagnosed mechanisms explain the weak result: channel 1's dacc_adapter has never been trained
    on non-degenerate goal_proximity values (response ~1e-8 vs ~0.87 scale of other score_bias
    components, 4-7 orders of magnitude off); channel 2 is architecturally argmax-invariant under
    normal committed operation (~97% committed in a matched-RNG probe). With only 3 ladder levels
    per unit, Spearman rho is a coarse statistic that cannot cleanly distinguish a weak real effect
    from pure sampling noise -- a future redesign should add ladder levels, not just seeds. New
    substrate gap quantified but not yet tracked: created substrate_queue.json entry
    arc005_dacc_adapter_goal_proximity_training. Readiness clean across all 6 cells. No status
    change (already active). PROMOTES/DEMOTES NOTHING.
    [2026-08-03 governance, V3-EXQ-848a, confirmed failure_autopsy_V3-EXQ-848a_2026-08-03, successor
    of V3-EXQ-848]: calibration fix (dacc_goal_readout_weight=0.5, dacc_goal_readout_normalize=True,
    IGW-20260801-199) confirmed genuinely engaged this run (25,289 dACC bias calls, build-time guard
    held) -- the wiring/calibration line of inquiry 848 opened is CLOSED. C_precision_monotonicity
    still only 1/10 units satisfied, but the per-unit rho distribution (7/10 positive-signed, 6 at
    exactly 0.5) is the signature of a 3-point-ladder Spearman-rho resolution ceiling, not a clean
    null (802's genuinely-null channels showed bit-identical rho=0.0). mixed/standard (not
    substrate_ceiling). Route: /queue-experiment V3-EXQ-848b for a ladder-granularity redesign (5+
    levels and/or a magnitude-based statistic), NOT further substrate work. No status change
    (already active). PROMOTES/DEMOTES NOTHING.
    [2026-08-07 governance, V3-EXQ-848b, confirmed failure_autopsy_2026-08-05_pending_review_batch
    #4, successor of V3-EXQ-848a]: pure measurement-resolution redesign (ladder 3-level -> 7-level,
    no substrate changes) -- broke the resolution ceiling that made 848a inconclusive
    (n_distinct_rho_values_observed=10, all distinct, spanning -0.536 to 0.964, vs 848a's pile at
    exactly +0.5). 4/10 units now clear |rho|>=0.6 (up from 1/10), 2/10 additionally
    Mann-Kendall-significant -- genuinely dispersed, not a resolution artifact -- but still short of
    the 7/10 bar. all_near_zero_null=false (not a clean negative either). The raw effect
    (~1e-3 log10-precision-units) is ~180x smaller than cross-seed SD (~0.18): swamped by noise, not
    absent. mixed accepted as the FINAL reading for this design line -- no further same-design
    lettered iteration (autopsy's own recommendation: resolution is no longer the bottleneck,
    statistical power against a genuinely tiny effect is). No dedicated ARC-005 literature review
    exists; a /lit-pull is commissioned (chipped at session close) to ground what magnitude of
    precision-routing effect the biology would actually predict, since the raw effect being merely
    small (not absent) makes a power calculation meaningless without a target effect size. No status
    change (already active). PROMOTES/DEMOTES NOTHING.
    [2026-08-07 governance, /lit-pull landed, chip-20260807-arc005-litpull-precision-effect-size]:
    targeted_review_arc_005 (4 entries) registered -- Nuiten 2024 (atomoxetine precision-weighting,
    d'=0.39), Cook 2019 (methylphenidate causally shifts volatility-driven learning rate), Kanen 2021
    (dose-dependent 5-HT reversal-learning deficit), Yu & Dayan 2005 (the mechanism). Verdict:
    biology predicts precision-channel manipulations produce SMALL-TO-MODERATE detectable effects
    (Cohen d ~0.2-0.5, often dose-dependent) -- REE's own channel-1 effect/noise ratio (~0.006) is
    ~30-80x BELOW the smallest typical biological precision effect in this literature. This favours
    the undertrained-dACC-adapter reading (848a's own finding: the adapter has never been trained on
    non-degenerate goal_proximity values) over a genuine biological ceiling -- a power calculation
    against a d~0.006 target was never what the biology predicts should be needed. ARC-005's
    lit_status: now present (was absent). No status change; does not itself reopen the 848-line
    (still FINAL per the note above) -- informs whether a future redesign should target instrument
    fix (adapter training) rather than more seeds/power against the current tiny effect.
  what_would_answer: |
    The falsifiable content is NOT "a control plane exists" (that is a design fact --
    we built one). It is that the control plane ROUTES: has causal authority over
    precision and mode that is DISSOCIABLE FROM CONTENT. If precision and mode are
    readouts of what the agent happens to be processing, the plane is epiphenomenal
    and all 88 dependents inherit a false premise.
    Testable in V3 as a DOUBLE DISSOCIATION over the implemented channel set
    (5-HT rigidity, phasic-surprise-burst gain, SalienceCoordinator mode prior,
    pcc_stability mu): a 2x2 of {channels varied, content fixed} x {content varied,
    channels fixed}, measuring (i) the E3 precision readout and (ii) mode-occupancy
    distribution.
    ANSWER "routes" (PASS) if channel intervention shifts precision and mode occupancy
    monotonically with channel setting, AND the channel effect on mode occupancy
    exceeds the content effect by >= 0.8 SD of the cross-seed DELTA and by an absolute
    floor of >= 0.15 occupancy-fraction, AND identical channel settings reproduce the
    same regime across differing content.
    ANSWER "readout, not router" (FAIL) if mode occupancy and precision are predicted
    as well or better from content features than from channel settings. ARC-005 then
    demotes and its dependents need re-derivation from whatever does the routing.
    NON-DEGENERACY PRECONDITION (else substrate_not_ready, NOT a verdict): in the
    baseline arm at least two modes must be occupied >= 5% of steps, and the precision
    readout must carry non-zero cross-seed variance (not floor-pinned). A single-mode-
    collapsed agent makes both arms trivially identical and the comparison vacuous.
    SCOPE: tests routing authority over the CURRENTLY-IMPLEMENTED channels. A PASS
    does not vindicate every channel posited in MECH-002/MECH-004; it establishes the
    plane is causal rather than decorative.
  location: docs/architecture/control_plane.md#arc-005
  source:
    - docs/processed/legacy_tree/docs/architecture/control_plane.md
    - docs/processed/legacy_tree/architecture/control_plane.md
    - docs/thoughts/2026-02-09_starting_with_sensory_streams.md
    - docs/thoughts/2026-02-09_empathy.md
- id: ARC-006
  title: "Entities are sparse, persistent, bindable structures."
  claim_type: architectural_commitment
  subject: entities.binding
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-004
    - INV-002
  what_would_answer: |
    ARC-006 asserts entities are (i) sparse, (ii) persistent, (iii) bindable
    structures. Two of the three properties already have direct
    experimental support one hop away: MECH-045's V3-EXQ-658 (2026-06-09,
    PASS/supports) exercises ree_core/entities/object_file_buffer.py's
    token-continuity association (C1/C5) and occlusion-persistence (C3) and
    shows the buffer re-identifies a moved entity across occlusion
    (reid_frac INTACT=1.0 vs SHUFFLE=0.3 vs OFF=0.0, feature term
    non-degenerate d_cos_min=0.92, 3/3 seeds) -- "persistent" and "bindable
    [across motion]" are effectively answered, but the evidence is tagged
    only to MECH-045's claim_ids, not to ARC-006. [Note: a prior digestion
    memory calling this "V3 pending" was stale as of this pass -- MECH-045
    landed 2026-06-09.]

    The third property, "sparse," is NOT yet exercised: V3-EXQ-658
    deliberately set obf_max_tokens = N_DISTRACTORS + 2 (one slot of
    headroom) to guarantee zero capacity evictions, isolating reid/
    persistence from competitive contention. The buffer's C4
    capacity-eviction machinery (_enforce_capacity(), _n_capacity_evictions,
    default max_tokens=5) exists and is wired but has never run
    over-capacity.

    What would answer this (no new substrate build -- only a new experiment
    config over the existing ObjectFileBuffer):
    1. Bridge existing evidence: tag/synthesize V3-EXQ-658 onto ARC-006's
       own evidence block so "persistent"/"bindable-across-motion" support
       is visible at the parent-claim level.
    2. Run a capacity-contention arm: N entities > max_tokens (e.g. 8
       salient candidates against max_tokens=5), varying per-entity
       salience, and test whether _enforce_capacity's evictions track the
       TRUE salience ranking above a shuffle/random-eviction control (same
       INTACT/SHUFFLE/OFF logic as V3-EXQ-658, applied to eviction choice).
       PASS = eviction selectivity beats shuffle control on >=2/3 seeds;
       FAIL = evictions are salience-independent (effectively FIFO/random),
       falsifying "sparse [attention-gated]" even though the raw capacity
       cap trivially holds.
    3. Complementary confirming test (structurally parallel to SD-015's
       z_resource identity-recovery probe): a linear probe recovering
       per-token identity from the buffer's stored feature embedding should
       generalise across nuisance transformations (position, occlusion-gap
       length) well above a token-shuffled control -- operationalising
       "bindable" as identity-stability under transformation.

    NON-DEGENERACY GUARD: uninformative if _n_capacity_evictions stays 0
    throughout (contention never triggered) or distractor salience values
    are near-identical. Add G3 (capacity actually exceeded) to V3-EXQ-658's
    existing G0-G2 gates.
  location: docs/architecture/entities_and_binding.md#arc-006
  source:
    - docs/processed/legacy_tree/docs/architecture/entities_and_binding.md
    - docs/processed/legacy_tree/DANIEL_README.md
- id: ARC-007
  title: "Hippocampal systems store and replay paths through residue-field terrain."
  claim_type: architectural_commitment
  subject: hippocampus.path_memory
  polarity: asserts
  status: provisional
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-007@2026-03-16T18:20:19.360735Z"
      as_of: 2026-03-16
      verdict: hold_pending_v3_substrate/applied
  last_reviewed: 2026-02-15
  lifecycle_stage: adjudicated
  adjudication_outcome: hybridize
  adjudicated_at_utc: 2026-02-15T18:46:42.773429Z
  adjudication_decision_status: applied
  adjudication_recommendation: hybridize
  evidence_quality_note: |
    V2 EXQ (path_memory_ablation, 2026-03-08): FAIL (partial_support=true, 1/2 criteria).
    PATH_MEMORY vs PATH_ABLATED agent harm: 0.896 vs 0.912 -- sub-threshold difference.
    Proxy representation was a 26-element obs-vector slice [252, 278] standing in for
    hippocampal path memory. This is too crude to test the architectural claim: proper
    ARC-007 path memory requires a functioning HippocampalModule navigating a structured
    path space (SD-004). The ablation test cannot distinguish "path memory doesn't help"
    from "the proxy is too weak to carry meaningful path information."
    V3 gate cleared (2026-03-28): SD-004 action objects + HippocampalModule now implemented
    in ree-v3. V3 path-memory experiments can proceed.
    EXQ-114 PASS (2026-03-28, 4/4): MAP_NAV harm_rate=0.006 vs MAP_ABLATED=0.765.
    99.2% harm reduction. 2/2 seeds consistent. Strong V3 confirmation of ARC-007.
    Q-020 resolved (2026-04-02): The "no value computation" constraint is compatible with
    hippocampal geometry being value-shaped. The constraint specifies only that the computation
    of value (RPE, utility) occurs in BG/amygdala/PFC, not in hippocampus. Hippocampus stores
    the geometrically encoded result via BTSP-mediated write operations (Bittner 2017, Science).
    MECH-073 and ARC-007 are co-true under Resolution A. Literature: Dupret 2010, Gauthier 2018,
    Bittner 2017, Teyler & Rudy 2007.
    EXQ-397 / EXQ-397c x2 reclassified non_contributory (2026-04-22): the hippo_quality_gap
    internal probe metric is sign-inverted (intact more negative than ablated, near bit-identical
    across the "harder env" fix) -- the probe is broken, not the mechanism. ARC-007 stands on the
    EXQ-114 behavioural PASS (99.2% harm reduction). Spawned diagnostic to redesign the probe.

    2026-07-25 (governance): status active -> provisional. V3-EXQ-114a (path-memory probe,
    C2 cluster) is a USER-CONFIRMED weakens -- with E3-routed selection, a matched episode
    budget and a static no-op control, the harm-reduction advantage collapses to 11.4%
    (< 15% required). Critically, EXQ-114's headline 99.2% figure was a DENOMINATOR ARTEFACT
    of a constant action stream; ARC-007's sole behavioural support does not survive its
    removal, so the "stands on the EXQ-114 PASS" reading above is retired. The DEMOTION IS
    LEG-SCOPED: it demotes the behavioural harm-navigation translation; the biological
    existence-proof (hippocampal path store/replay) is unaffected. NOT unconditional:
    V3-EXQ-800 (residue-scramble dissociation) on this claim is non_contributory /
    substrate_conditional (pending_retest_after_substrate) and is HELD OUT of the conflict
    ratio -- the demotion is driven solely by 114a's genuine null.
  depends_on:
    - ARC-004
    - ARC-003
    - ARC-018
  epistemic_category: standard
  what_would_answer: |
    ARC-007 is CONJUNCTIVE and only one leg has ever been tested.
    LEG 1 -- "hippocampal systems store and replay paths" -- is already carried
    behaviourally by EXQ-114 (harm_rate 0.006 intact vs 0.765 ablated, 99.2%
    reduction, 2/2 seeds). That is what the exp_conf 0.55 rests on.
    LEG 2 -- "THROUGH RESIDUE-FIELD TERRAIN" -- is untested. Nothing in the evidence
    trail distinguishes paths through a residue field from paths through an ordinary
    spatial/latent map that residue merely co-varies with. Leg 2 is the whole
    REE-specific content: without it ARC-007 reduces to "a hippocampus helps
    navigation" -- true, generic, and not load-bearing for the 76 dependents that
    assume residue-shaped terrain.
    Answered by a RESIDUE-SCRAMBLE DISSOCIATION with the hippocampal path machinery
    left fully intact: A0 intact / A1 hippocampus-ablated (the EXQ-114 reference arm,
    establishing the base effect is live) / A2 residue field PERMUTED (residue values
    shuffled across cells -- preserves the field's marginal distribution and the
    spatial map, destroys the residue<->location correspondence) / A3 residue field
    frozen at initialisation. Primary DV BEHAVIOURAL (harm_rate, path efficiency).
    NOT hippo_quality_gap -- that probe is sign-inverted (EXQ-397/397c reclassified
    non_contributory 2026-04-22) and must not be a verdict source until redesigned.
    ANSWER "leg 2 holds" (PASS) if A2 and A3 degrade toward A1, recovering >= 50% of
    the A0->A1 harm gap, with an effect >= 0.8 SD of the cross-seed delta. The stored
    paths depend on residue structure specifically.
    ANSWER "terrain claim false, leg 1 survives" (FAIL) if A2/A3 are statistically
    indistinguishable from intact A0. Path memory works, but not through residue
    terrain. ARC-007 must then be SPLIT: the path-memory leg promotes on EXQ-114, the
    residue-terrain leg demotes, and dependents assuming residue-shaped terrain
    (MECH-073 value-shaped geometry, the replay cluster) need re-derivation.
    NON-DEGENERACY PRECONDITION (else substrate_not_ready, NOT a verdict):
    (a) the residue field must carry live non-flat structure in A0 -- non-zero
    cross-cell variance, not uniform-pinned; permuting a flat field is a no-op and
    would manufacture a false FAIL; (b) the A0->A1 harm gap must reproduce EXQ-114's
    magnitude, else there is no base effect to attribute; (c) the permutation must
    verifiably change the field state, not merely be requested.
    This also makes the 4-PASS/4-FAIL evidence split legible: a FAIL here EXPLAINS the
    divergence rather than adding to it -- the passes tested leg 1, leg 2 was never
    under test.
  location: docs/architecture/hippocampal_systems.md#arc-007
  source:
    - docs/processed/legacy_tree/docs/architecture/hippocampal_braid.md
    - docs/processed/legacy_tree/architecture/Hippocampal_braid.md
    - docs/processed/legacy_tree/docs/architecture/hippocampal_systems.md
    - docs/thoughts/2026-02-09_starting_with_sensory_streams.md
- id: ARC-008
  title: "Commitment eligibility is gated by tau, rho, and phi."
  claim_type: architectural_commitment
  subject: temporal.depth_phase_gating
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-002
    - ARC-004
    - ARC-005
    - ARC-003
  location: docs/architecture/temporal_dynamics.md#arc-008
  source:
    - docs/processed/legacy_tree/architecture/depth_phase_spec.md
    - docs/processed/legacy_tree/docs/architecture/temporal_dynamics.md
  digestion_note: >
    [2026-08-08 thought-digestion, deferred at user's direction -- do not draft a plain
    falsifier here without first reconciling this finding]: a digestion research pass found
    the unified three-axis eligibility system this claim describes (tau-depth bands,
    rho-depth strata, and a routing function G(tau, rho, phi, pi, context)) does NOT exist
    anywhere in ree-v3/ree_core -- zero hits for tau_band/rho_stratum/phi_regime/depth_phase/
    an eligibility matrix M_phi, or the literal string ARC-008. Only a coarse BINARY proxy
    for the phi axis exists (simulation_mode/hypothesis_tag, ~60 files/~300 call sites), and
    that fragment is ALREADY its own independently-evidenced, status:stable claim (MECH-094),
    which does NOT depend on ARC-008. Neither tau-depth nor rho-depth exists as a portable
    per-token tag anywhere in the substrate; phi is binary, not the three-valued TASK/DMN/
    OFFLINE taxonomy the doc specifies. Four claims depend_on ARC-008 (MECH-005, MECH-006,
    MECH-021, MECH-049), all equally thin registrations with no evidence/notes of their own.
    BEFORE drafting a straight what_would_answer: decide whether ARC-008 should be
    split/reclassified (the built phi-fragment folded into/cross-referenced against MECH-094,
    the unbuilt tau/rho portion marked substrate_conditional or excreted) rather than treated
    as one falsifiable claim -- a full research draft with this recommendation already exists
    in this session's transcript (agent a41fbe4b429fd0630) and can be reused rather than
    re-derived. Remove this note once resolved.
- id: IMPL-001
  title: "Glossary of canonical REE terms."
  claim_type: implementation_note
  subject: repo.glossary
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  location: docs/glossary.md#impl-001
  source:
    - docs/processed/legacy_tree/docs/glossary.md
- id: Q-001
  title: "What mechanisms produce entity emergence and binding?"
  claim_type: open_question
  subject: entities.emergence_mechanism
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Run the entity-binding discriminative pair (the EXQ-146 lineage) on a
    substrate that can carry persistent, attention-gated object-files (ARC-006
    design constraints: object-specific buffers + precision-gated feature merge).
    ANSWER "yes -- a binding mechanism is present" if sparse, persistent latent
    structures emerge that (a) track the SAME entity across occlusion / multiple
    timesteps above a re-identification floor, (b) bind feature conjunctions only
    when precision/attention state licenses the merge (binding rate scales with
    attention, not feedforward co-occurrence), and (c) support entity-linked error
    attribution -- a perturbation to one entity's slot changes that entity's
    prediction error and not others'. ANSWER "no" if representations are
    non-persistent or features merge feedforward regardless of attention. HARD
    PRECONDITION: an object-file-like persistent buffer must exist; the EXQ-146
    FAIL is uninformative because V3 had no persistent bindable substrate and no
    training budget to grow one -- a FAIL on the current substrate self-routes
    substrate-not-ready, not a falsification of emergence.
  depends_on:
    - ARC-006
  location: docs/architecture/entities_and_binding.md#q-001
  source:
    - docs/processed/legacy_tree/docs/architecture/entities_and_binding.md
  evidence_quality_note: |
    EXQ-146 FAIL/weakens (2026-03-29): entity binding discriminative pair. First experimental
    entry. Failure does not yet constrain the question -- substrate and training budget gaps
    likely.
- id: ARC-009
  title: "Language is a symbolic mediation and coordination layer."
  claim_type: architectural_commitment
  epistemic_category: substrate_conditional
  subject: language.symbolic_mediation
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: >
    Requires two layers of substrate that do not yet exist in ree-v3, neither of which
    has an implementation and only the first has even a named design target. Layer 1
    (shared with ARC-010): a multi-agent environment -- ARC-047's SocialGridWorld is the
    designated minimal substrate, itself status: candidate, confidence 0.0, gated to
    implementation_phase v5, and unbuilt. Layer 2 (specific to ARC-009): a discrete
    symbolic communication channel between agents -- confirmed absent from ree_core/
    entirely (no "language", "symbol", "communication", or "message-passing"
    inter-agent machinery anywhere; the one reference, control_demand.py's
    LANGUAGE_EXPLICITATION enum label, has no implementation behind it). MECH-014
    ("Minimal signalling channel requirements") is the closest existing design artifact
    for this layer, but it is a prose interface sketch only (inputs: latent-state
    summaries/residue flags; outputs: discrete-or-continuous signals other agents can
    condition on) with no substrate, no confidence score, and no queued experiment --
    it is not an ARC-047-equivalent scoped build target.

    A genuine test becomes possible once both layers land: two (or more) REE agent
    instances in a shared multi-agent environment (per ARC-010/ARC-047), given access
    to a shared discrete symbol channel (per MECH-014's sketch, built out as a real
    interface), on a task with information asymmetry -- one agent observes something
    the other needs to act correctly on, and coordination/reward requires the
    information to cross the channel (a standard emergent-communication paradigm,
    e.g. a referential/signalling game or a coordinated-response variant of ARC-047's
    predator-avoidance setup). Evidence for the claim would be: (1) agents given the
    symbol channel achieve significantly better task/coordination performance than a
    no-communication baseline; (2) the symbols carry recoverable meaning -- e.g.
    probe accuracy predicting the sender's underlying latent state (z_theta/z_delta
    slice, residue flag, or intent) from the emitted symbol above chance; and (3)
    ablating the channel, or shuffling the symbol-to-meaning mapping post-hoc,
    degrades coordination specifically (not just general task performance), showing
    the effect is mediated by the symbolic layer rather than some other shared signal
    (e.g. the scent-channel proxies ARC-047 already specifies). Until both the
    multi-agent substrate and a real (non-sketch) symbol-passing channel exist, no
    version of this claim is falsifiable in V3.
  depends_on:
    - INV-003
    - INV-007
    - ARC-010
    - ARC-005
    - ARC-004
  location: docs/architecture/language.md#arc-009
  source:
    - docs/processed/legacy_tree/architecture/language/README.md
    - docs/processed/legacy_tree/architecture/language/language_contract.md
    - docs/processed/legacy_tree/architecture/language/language_functions.md
    - docs/processed/legacy_tree/docs/architecture/language.md
    - docs/thoughts/2026-02-09_language.md
    - docs/thoughts/2026-02-09_arcuate_fasciculus_language_nudges.md
- id: ARC-010
  title: "Social cognition uses mirror modelling and coupling."
  claim_type: architectural_commitment
  epistemic_category: substrate_conditional
  subject: social.mirror_modelling
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-005
    - ARC-004
    - ARC-006
    - ARC-047
  what_would_answer: >
    Requires a multi-agent substrate that does not yet exist in ree-v3 (confirmed: no
    "social", "mirror", "other_agent", or "otherness" implementation anywhere under
    ree_core/ beyond two comments acknowledging its absence; ARC-047's SocialGridWorld
    environment -- the designated minimal substrate for this claim -- is itself
    unimplemented, status: candidate, confidence 0.0, and gated to implementation_phase
    v5). A genuine test becomes possible once that substrate lands, and would look like:
    train a REE agent in a multi-agent environment (e.g. ARC-047 SocialGridWorld, N agent
    instances sharing a grid) where a second agent's internal state is only observable
    indirectly (e.g. via the scent-channel proxies ARC-047 specifies: wanting_scent,
    alarm_scent, harm_stress_field, etc., rather than direct state access). Using the
    same shared self/other latent stack ARC-010 depends on (ARC-004 L-space, reused with
    reduced precision gains per the mirror-modelling mechanism), measure whether the
    agent's inferred other-model -- its OTHER_SELFLIKE-tagged shadow-bundle estimate of
    the other agent's action-selection (MECH-031) -- converges toward the other agent's
    actual policy/action distribution at above-chance accuracy across training, and
    whether coupling strength (signed harm+benefit routing into E3 ranking) predicts
    downstream cooperative or protective behavior (e.g. MECH-190's predicted defense-
    coalition sequence: individual defense -> alarm-triggered early coalition ->
    altruistic defense under no self-threat).
    Falsified if: even after the substrate exists and training is extended, the other-
    agent latent estimate does not track the other agent's true policy above chance, OR
    ablating the shared self-model machinery (forcing an independent from-scratch
    other-model) produces equal or better mirroring -- which would indicate social
    cognition does not in fact reuse self-representational machinery as ARC-010 asserts.
    Currently blocked, not testable: no multi-agent environment and no other-agent
    latent/inference channel exist in the V3 substrate. The single-agent CausalGridWorld
    environments used by nearly all current V3 experiments have no second agent to model.
  location: docs/architecture/social.md#arc-010
  source:
    - docs/processed/legacy_tree/architecture/social/README.md
    - docs/processed/legacy_tree/architecture/social/mirror_modelling.md
    - docs/processed/legacy_tree/architecture/social/social_coupling.md
    - docs/processed/legacy_tree/architecture/social/otherness_inference.md
    - docs/processed/legacy_tree/docs/architecture/social.md
    - docs/thoughts/2026-02-09_empathy.md
    - docs/thoughts/2026-02-25_task_loop_extraction_and_latent_field_ethics.md
- id: ARC-011
  title: "Offline integration (sleep) is required for stability."
  claim_type: architectural_commitment
  subject: sleep.offline_integration
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-010
    - ARC-007
    - ARC-005
  epistemic_category: standard
  what_would_answer: |
    ARC-011 asserts offline integration (sleep) is REQUIRED FOR STABILITY --
    distinct from INV-010 (sleep exists and is required, a bare universal
    necessity grounded in cross-species biology, no depends_on) in that
    ARC-011 stakes a REE-specific architectural claim: that omitting sleep
    causes measurable instability, mediated through ARC-007 (hippocampal
    replay) and ARC-005 (control plane). [Digestion note, not resolved
    here: INV-010 and ARC-011 are near-duplicate in wording -- worth a
    governance pass to make the distinction explicit in one or both
    entries, or confirm they should stay separate.] Never tested as its
    own claim: 0 genuine_exp entries (claim_evidence.v1.json), sole
    evidence one literature citation (zebrafish sleep biology) shared with
    INV-010. The extensive sleep experimental corpus in this codebase
    (SD-017, MECH-120, MECH-204, MECH-272/273/275/285, Q-055) all attaches
    to child mechanism claims, never to ARC-011 or INV-010 directly.

    STABILITY OPERATIONALIZED (per docs/architecture/sleep.md's own
    "Failure if omitted" list, using DVs already instrumented in the
    sleep-cluster experiments):
      (a) Representation drift: slot_cosine_sim among ContextMemory slots
          rising toward ~1.0 (context representations collapsing/
          undifferentiated) -- instrumented since the SD-017/EXQ-436
          family.
      (b) Policy/diversity erosion ("monostrategy convergence"): committed-
          action-class entropy retained at t_k vs t_0, sleep-ON vs
          sleep-OFF -- the DV used by V3-EXQ-691/Q-055.
      (c) Residue-field runaway: unbounded growth/variance in the residue
          field R, or policy entropy collapsing toward frozen -- the
          "residue overgeneralisation" / "brittle or frozen policy
          selection" failure modes named in sleep.md.

    NON-DEGENERACY PRECONDITION: the comparison must run long enough / over
    enough would-be sleep cycles that instability has a chance to manifest
    in EITHER arm. A short run where neither condition destabilizes is
    uninformative -- this is not hypothetical caution, it is the observed
    failure mode of the two closest existing attempts: EXQ-430 (SLEEP harm
    < NO_SLEEP, INV-010) found no differential effect in 0/3 seeds and was
    read as substrate-gated/inconclusive rather than falsifying;
    V3-EXQ-691 (Q-055) found sleep-OFF was NOT visibly worse than sleep-ON
    on diversity erosion at the horizon tested (sleep-OFF was in fact
    marginally LESS eroded). Both are consistent with "the run never got
    long/stressed enough for the predicted divergence to appear," not with
    "sleep doesn't matter." A valid test therefore needs either (i) a
    horizon long enough that waking-only operation visibly degrades on
    >=1 of the DVs above while the sleep-ON arm does not, or (ii) an
    explicit stressor (elevated novelty/harm load, episode count well past
    where current sleep-adjacent experiments stop) to force the divergence
    within a tractable budget. The sleep-ON arm must run the FULL flag
    stack (use_sleep_loop, sws_enabled, rem_enabled,
    use_sleep_aggregation_cluster -- landed 2026-05-16 GAP-3), not the
    partial/legacy configs used in EXQ-418/436/538a, which repeatedly
    produced vacuous or non_contributory results from missing read-paths
    rather than a genuine sleep-ON vs sleep-OFF contrast.

    CONFIRMING signature: the sleep-OFF arm shows measurable degradation
    on >=1 of the three stability DVs over the extended/stressed horizon
    (slot_cosine_sim trending toward degenerate; action-class entropy
    collapsing toward a frozen/monostrategy floor; residue-field
    norm/variance growing without bound), while the sleep-ON arm (full
    flag stack) does not show the same degradation, or shows it
    significantly less severely, matched for total experience/compute.

    FALSIFYING: sleep-OFF sustains the same stability profile as sleep-ON
    across all three DVs over the extended/stressed horizon -- no
    differential drift, no differential diversity erosion, no differential
    residue runaway. This would falsify ARC-011's specific "required FOR
    STABILITY, in REE" claim even if INV-010's weaker "sleep exists and is
    architecturally motivated" survives on cross-species biological-
    analogy grounds alone -- this is the concrete point at which the two
    claims can empirically decouple, which is why they should not share
    one falsifier.
  location: docs/architecture/sleep.md#arc-011
  source:
    - docs/processed/legacy_tree/architecture/sleep/README.md
    - docs/processed/legacy_tree/architecture/sleep/sleep_contract.md
    - docs/processed/legacy_tree/docs/architecture/sleep.md
- id: ARC-012
  title: "E3 does not require an explicit ethical cost term."
  claim_type: architectural_commitment
  subject: ethics.no_explicit_cost_term
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-001
    - ARC-003
    - ARC-005
    - ARC-007
  location: docs/architecture/e3.md#arc-012
  source:
    - docs/processed/legacy_tree/REE_CORE.md
- id: MECH-001
  title: "Astrocyte-aware regulatory stack mediates control-plane precision routing."
  claim_type: mechanism_hypothesis
  subject: control_plane.astrocyte_regulatory_stack
  polarity: asserts
  status: candidate
  epistemic_category: substrate_conditional
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-005
    - INV-008
    - ARC-004
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: A `RegulatoryField`-class module implementing
    the slow field R(x,t) described in implementation_hooks.md must exist in
    ree_core and be wired LIVE into the precision-application path (LatentStack /
    SharedDepthEncoder) -- not merely declared or dead code, mirroring the
    non-degeneracy bar INV-008 already applies to its own precision audit. Per
    the doc's own interface spec, that means at minimum: an update(dt,
    monoamine_signals, activity, metabolic_state) method invoked at a cadence
    N >> 1 relative to the per-step E2/E1 update, and a get_precision_multipliers
    (depth_k) query path that E2/E1 actually consume in place of (or in addition
    to) the current direct per-channel nn.Parameter logits INV-008 confirmed as
    the live mechanism. Confirmed absent as of 2026-08-08 (grep -ri astrocyte
    ree_core/ -r: 0 hits). The test must also isolate genuine field dynamics
    from a trivial pass-through: a module recomputing alpha_multipliers as an
    instantaneous 1:1 function of the current monoamine signal (no lag, no
    memory) would satisfy "a RegulatoryField exists" while exhibiting none of
    the mediation MECH-001 actually asserts.

    CONFIRMING: (1) alpha_k(x,t) measurably LAGS step-changes in a monoamine-like
    broadcast signal by a timescale tau_R on the order of the doc's
    seconds-to-minutes design, rather than reacting within one step; (2)
    alpha_k(x,t) shows spatial/positional heterogeneity AT FIXED DEPTH k under
    matched monoamine input, going beyond the depth-channel differentiation
    INV-008 already established; (3) sleep-phase annealing of R(x,t) measurably
    improves precision recalibration relative to a synaptic-weights-only
    recalibration control; (4) E3 trajectory selection measurably penalizes
    trajectories requiring a rapid regulatory shift.

    FALSIFYING (VIOLATED BY): (1) a field-mediation layer is implemented and
    exercised live, but alpha_k(x,t) tracks monoamine-like input with negligible
    lag; (2) the implemented field shows no spatial heterogeneity beyond the
    existing per-depth-channel differentiation; (3) sleep annealing of R(x,t)
    produces no measurable improvement over weights-only recalibration; (4) a
    channel-ablation test on the field module shows ablating it entirely leaves
    alpha_k(x,t) unchanged -- meaning the framing, even once built, is a
    redundant re-description of the existing per-channel parameterization.
  location: docs/architecture/astrocyte_regulatory_stack.md#mech-001
  source:
    - docs/processed/legacy_tree/docs/astrocyte_aware_regulatory_stack/regulatory_stack_model.md
    - docs/processed/legacy_tree/architecture/astrocyte_aware_regulatory_stack/regulatory_stack_model.md
    - docs/processed/legacy_tree/architecture/astrocyte_aware_regulatory_stack/README.md
    - docs/processed/legacy_tree/architecture/astrocyte_aware_regulatory_stack/astrocytes_in_brief.md
    - docs/processed/legacy_tree/architecture/astrocyte_aware_regulatory_stack/implementation_hooks.md
    - docs/processed/legacy_tree/architecture/astrocyte_aware_regulatory_stack/ree_component_mapping.md
    - docs/processed/legacy_tree/architecture/astrocyte_aware_regulatory_stack/references.md
    - docs/processed/legacy_tree/docs/architecture/astrocyte_regulatory_stack.md
- id: Q-002
  title: "What is the appropriate spatial resolution for R(x,t)?"
  claim_type: open_question
  subject: astrocyte.R_field_resolution
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-783_2026-07-18
      as_of: 2026-07-18
      verdict: diagnostic_no_direction/instrument_validated_cause_discriminated
  what_would_answer: |
    Sweep R(x,t) spatial resolution (RBF-center count, e.g. 4 / 8 / 32 / 64) on a
    substrate whose z_world latent has demonstrable spatial organisation, and read
    BOTH downstream axes separately: harm-avoidance behaviour (harm_rate) and
    terrain-map quality (residue_accuracy / residue-hazard correlation). ANSWER
    "fine resolution warranted" only if increasing centers improves
    residue_accuracy ABOVE the coarse baseline (cross threshold ~0.10) without
    inflating harm_rate -- i.e. the spatial signal is being used, not spread too
    thin. ANSWER "coarse adequate" if, as in EXQ-170 (fine harm_rate 0.028 vs
    coarse 0.0003, 93x worse) and EXQ-215 (high-res better on harm C2 but worse on
    residue-hazard C3), fine resolution either degrades one axis or splits the
    learning signal so accuracy stays below threshold. HARD PRECONDITION: z_world
    must carry enough spatial structure for fine-grained RBF accumulation; at V3
    scale it does not, so a fine-resolution FAIL reflects the substrate ceiling
    (resolve via SD-005 / higher-dim z_world), not a settled answer. The sharper
    test EXQ-170/215 did not run: hold harm-avoidance and map-quality as SEPARATE
    acceptance axes rather than collapsing them, since the existing evidence shows
    them dissociating.
  depends_on:
    - MECH-001
  location: docs/architecture/astrocyte_regulatory_stack.md#q-002
  source:
    - docs/processed/legacy_tree/docs/astrocyte_aware_regulatory_stack/regulatory_stack_model.md
  evidence_quality_note: |
    EXQ-147 FAIL/weakens via MECH-128 (2026-03-29): E1 goal conditioning pair FAIL. First
    experimental entry. Failure likely reflects training budget / substrate gap rather than
    fundamental resolution question.
    EXQ-170 FAIL 1/3 x2 replicated (2026-03-30, two independent runs): fine resolution (32 RBF
    centers) vs coarse (4 centers). Fine harm_rate=0.028 vs coarse 0.0003 (93x worse). Fine
    residue_accuracy=0.016 (threshold 0.10). Consistent across both runs. z_world latent lacks
    spatial organization to support fine-grained RBF accumulation at V3 scale -- 32 centers
    spread learning signal too thinly; 4 centers sufficient for zone-level harm/safe distinction.
    V3 scale finding: coarse resolution adequate, fine counterproductive. Does not rule out fine
    resolution at higher task complexity or after SD-005/higher-dim z_world. Evidence direction:
    weakens (fine-resolution hypothesis at V3 scale).
    EXQ-215 FAIL 2/5 (2026-04-03): Residue resolution discriminative pair FAIL. HIGH_RES
    (64 RBF centers) vs LOW_RES (8 centers). C1 FAIL (per-seed harm direction inconsistent
    across seeds). C2 PASS (mean_harm_delta=0.056: high_res_harm=0.033 vs low_res_harm=0.090
    -- high-res has LOWER harm, C2 passes threshold). C3 FAIL (residue_accuracy: high_res=-0.010
    vs low_res=0.033 -- low-res BETTER at hazard correlation). C4/C5 PASS.
    Surprising: high-res outperforms on harm avoidance (C2) but underperforms on residue
    accuracy (C3 reverse). Confirms Q-002 complexity: fine resolution improves harm avoidance
    but degrades residue-hazard mapping quality. The question remains open but evidence tilts
    toward: fine resolution better for harm avoidance behavior, coarse better for terrain map
    quality. Consistent with EXQ-170 finding (coarse adequate). Evidence direction: weakens
    fine-resolution-is-universally-better hypothesis.
- id: Q-003
  title: "Should R(x,t) be scalar or vector?"
  claim_type: open_question
  subject: astrocyte.R_field_dimensionality
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Run the R-field dimensionality discriminative pair (scalar R vs vector R with
    separable regulatory dimensions, the EXQ-148 lineage) on a substrate where the
    residue/contrast infrastructure is confirmed building real cross-zone contrast
    (not flat). ANSWER "vector" if at least two regulatory dimensions carry
    SEPARABLE, non-redundant signal -- each dimension predicts a distinct
    downstream control effect (e.g. one tracks harm-residue, another
    metabolic/gain state) and ablating one degrades only its associated behaviour,
    AND vector-R beats scalar-R on a task requiring independent regulation of two
    control axes. ANSWER "scalar" if the putative dimensions are collinear (one
    effective dimension explains the variance) and scalar-R matches vector-R. HARD
    PRECONDITION: the residue-field contrast must be confirmed working first --
    EXQ-148's two consistent FAIL/weakens results predate confirmed contrast
    infrastructure, so they do not yet discriminate scalar from vector; reframe
    the question only once contrast is real.
  depends_on:
    - MECH-001
  location: docs/architecture/astrocyte_regulatory_stack.md#q-003
  source:
    - docs/processed/legacy_tree/docs/astrocyte_aware_regulatory_stack/regulatory_stack_model.md
  evidence_quality_note: |
    EXQ-148 FAIL/weakens x2 (2026-03-28, 2026-03-29): R-field dimensionality discriminative pair
    run twice, both FAIL/weakens. Two consistent results. Weakening evidence accumulating; question
    may need reframing once residue field contrast infrastructure is confirmed working.
- id: Q-004
  title: "How to calibrate tau_R relative to E1/E2?"
  claim_type: open_question
  subject: astrocyte.tau_R_calibration
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Sweep tau_R across the candidate range (e.g. [0.02, 0.5]) at a TRAINING BUDGET
    large enough that residue accumulation actually feeds back into behaviour, and
    read tau_R's effect on the timescale-sensitive observables, not just
    harm_rate. ANSWER "tau_R is calibratable against E1/E2" if there exists a
    tau_R band that jointly (a) produces residue contrast sufficient for hazard
    discrimination AND (b) measurably improves a behaviour that depends on the
    regulatory field integrating over the right horizon -- with a clear optimum,
    fast-tau over-reacting and slow-tau under-accumulating. ANSWER "tau_R is
    behaviourally inert at this scale" if, as in EXQ-149 (FAST_TAU gave ~400x more
    residue contrast, 3.09 vs 0.008, yet ALL conditions showed identical mean_harm
    ~9.085), tau_R changes only residue-accumulation patterns and never reaches
    behaviour. HARD PRECONDITION: harm/behaviour must be tau-sensitive at the
    chosen budget -- the EXQ-149 finding is that at the current training scale
    tau_R is decoupled from harm_rate, so the calibration question is only
    answerable once the field's output is wired into a behaviour that the
    integration timescale can move.
  depends_on:
    - MECH-001
  location: docs/architecture/astrocyte_regulatory_stack.md#q-004
  source:
    - docs/processed/legacy_tree/docs/astrocyte_aware_regulatory_stack/regulatory_stack_model.md
  evidence_quality_note: |
    EXQ-149 PARTIAL (2026-03-29): tau_R in [0.02, 0.5] does not affect harm rate at current
    training budget. FAST_TAU produces ~400x more residue contrast than SLOW_TAU
    (residue_contrast=3.09 vs 0.008), but all conditions show identical mean_harm (~9.085).
    Question shifts: tau_R matters for residue accumulation patterns and timescales, not harm
    rate at this training scale. Calibration question remains open at higher training budgets.
- id: Q-005
  title: "Can sleep anneal or reset R(x,t)?"
  claim_type: open_question
  subject: astrocyte.sleep_annealing
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Implement a genuine V3 sleep/offline phase (SD-017 / MECH-204 sleep substrate)
    and compare an offline integration pass that anneals or partially resets
    R(x,t) against a matched no-anneal control, both followed by the SAME waking
    evaluation. ANSWER "sleep can anneal/reset R" if the anneal arm shows a
    measurable, directional change in R structure (e.g. reduced spurious residue,
    sharpened hazard contrast, or recovered plasticity headroom) that translates
    into improved waking behaviour or calibration relative to no-anneal -- on a
    metric that is NOT pinned across arms. ANSWER "no" if anneal and no-anneal
    produce statistically indistinguishable R and behaviour. HARD PRECONDITION
    (the EXQ-150 lesson): the test requires a real sleep substrate. EXQ-150 was
    marked superseded because V3 had no sleep; a post-training simulated
    anneal-vs-no-anneal gave identical harm (9.050) and is NOT representative.
    Until the V3 sleep substrate exists, any anneal-vs-no-anneal contrast is
    vacuous and self-routes substrate-not-ready rather than answering the
    question.
  depends_on:
    - MECH-001
  location: docs/architecture/astrocyte_regulatory_stack.md#q-005
  source:
    - docs/processed/legacy_tree/docs/astrocyte_aware_regulatory_stack/regulatory_stack_model.md
  evidence_quality_note: |
    EXQ-150 FAIL/mixed (2026-03-29): superseded -- sleep not implemented in V3; post-training
    phase simulation (anneal vs no-anneal) not representative of genuine sleep annealing. Manifest
    marked superseded. Anneal vs no-anneal conditions showed identical harm (9.050). Untestable
    until V3 sleep substrate implemented.
- id: ARC-013
  title: "Residue is persistent latent-space curvature; hippocampal paths form a cognitive map."
  claim_type: architectural_commitment
  subject: residue.geometry
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_697_arc013_residue_separability_falsifier_20260621T133052Z_v3
      as_of: 2026-06-21
      verdict: supports/PASS
  depends_on:
    - INV-006
    - INV-004
    - ARC-004
  location: docs/architecture/residue_geometry.md#arc-013
  source:
    - docs/processed/legacy_tree/docs/architecture/residue_geometry.md
    - docs/processed/legacy_tree/architecture/residue_geometry.md
    - docs/thoughts/2026-02-08_residue_paths_cognitive_map.md
  implementation_note: |
    INF-ENV-001 harm-gradient env substrate IMPLEMENTED 2026-05-16 (CausalGridWorldV2
    harm_gradient_* kwargs; V3-EXQ-576 substrate-readiness PASS). Enables graduated
    harm-proximity residue geography without terminal contact.
    EVIDENCE PATHS (two distinct, do NOT conflate -- corrected 2026-06-21):
    (1) residue GEOMETRY / formation: tier-1 DEV-NEED-004, V3-EXQ-587 (GAP-10
        EXQ-ISEF-001). RAN 2026-05-19, FAIL / non_contributory (reviewed): the
        harm_gradient knob produced ~1.0x mean_weight ratio (no geometry difference);
        a flat null on the geometry question, NOT the separability question.
    (2) residue SEPARABILITY (the ARC-013-as-distinct-primitive question -- is residue
        irreducible to salience+uncertainty?): V3-EXQ-697 (queued 2026-06-21, low
        priority, off the v3 green-board). Offline re-ranking falsifier; corroborates
        Q-079 if PASS. 587 does NOT test this; the Q-079 notes that pointed Q-079 HA2
        at "ARC-013's OPEN V3-EXQ-587" were mis-attributed (587 is closed, tests geometry).
- id: ARC-014
  title: "Default Mode enables safe imagination without commitment."
  claim_type: architectural_commitment
  subject: default_mode.internal_generative
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-08-02
    needs_review: false
    evidence:
      from: v3_exq_880_arc014_simulation_mode_commitment_20260802T214058Z_v3
      as_of: 2026-08-02
      verdict: supports
  evidence_quality_note: >
    [2026-08-02, V3-EXQ-880, PASS, evidence_direction=supports]: "arc014_commitment_worldstate_
    suppressed" -- two independent channels (rule_state / MECH-319, residue / MECH-094) both show
    simulation mode suppresses commitment while waking mode commits, combined via plain AND across
    C1/C2/C4/C5 (all load-bearing). First direct confirmatory evidence for the claim's literal
    commitment-suppression-under-simulation mechanism. No status change (already active).
  depends_on:
    - INV-011
    - ARC-003
    - ARC-005
    - ARC-007
    - ARC-013
  epistemic_category: standard
  what_would_answer: |
    ARC-014 is CONJUNCTIVE -- "safe imagination" (positive: useful internal
    simulation genuinely occurs) and "without commitment" (negative: it
    never durably writes to real-experience state) are separate assertions
    with DIFFERENT evidentiary status.

    LEG A -- SAFE IMAGINATION (that Default Mode / DMN replay genuinely
    executes and its content reaches SOME functional consumer). ANSWER by
    architectural audit of the live replay/ghost call site, not by
    V3-EXQ-880 (which does not exercise it). ree_core/agent.py's
    _do_replay() (the production ghost/replay call site) calls
    hippocampal.replay()/diverse_replay() -- so the GENERATIVE act of
    internal simulation genuinely fires in production -- but per the
    V3-EXQ-880 driver's own docstring, its output trajectories are
    currently DISCARDED without ever reaching lateral_pfc.update() or
    residue_field.accumulate(); MECH-319 names this the "reserved seam"
    for the still-pending V3-EXQ-543c replay-driven invocation path.
    Separately, INV-011's own audit (this session) traces a DIFFERENT
    generative pathway -- E1 internal generation via simulate()/sense()
    with hypothesis_tag=True -- through ~30 live agent.py call sites and
    concludes DMN/E1 rollout content reaches phi(z)-adjacent read paths
    for attenuated affect without producing a belief update. That is
    evidence LEG A holds for the E1/simulate() pathway specifically; it is
    NOT evidence for the hippocampal _do_replay()/DMN-ghost pathway, whose
    output is currently discarded wholesale (not merely write-suppressed)
    and is not yet wired to any consumer, safe or not. LEG A therefore
    remains PARTIALLY answered: confirmed for E1/simulate(), open
    (reserved seam, pending 543c) for the DMN/hippocampal-replay pathway
    specifically named in ARC-014's own "Default Mode" title.

    LEG B -- WITHOUT COMMITMENT (that simulation-tagged content, IF it
    reached a commitment-capable write consumer, would be suppressed while
    waking-tagged content commits). ANSWERED by V3-EXQ-880 (2026-08-02,
    PASS, 3/3 seeds). Matched-content design: identical captured
    (z_delta, z_world) content from a real seeded env rollout applied to
    two agents built from the same seed, differing only in
    hypothesis_tag/simulation_mode. Two independent channels, both
    load-bearing: rule_state/MECH-319 (sim_rule_state_delta <= 1e-7,
    measured 0.0 all seeds; wake_rule_state_delta >= 1e-4, measured
    0.31-0.43 all seeds); residue/MECH-094 (sim_residue_delta <= 1e-6,
    measured 0.0 all seeds; wake_residue_delta >= 1.0, measured ~2.0 all
    seeds). IMPORTANT CAVEAT: the experiment constructs the write-gate
    seam DIRECTLY via manual tag injection and direct API calls -- it does
    NOT drive the real production _do_replay() call site, which (see LEG
    A) currently discards its trajectories before they would ever reach
    these consumers at all. V3-EXQ-880 therefore confirms the categorical
    write-gate ITSELF behaves correctly as a defense-in-depth safeguard,
    not that production replay content is currently routed through it.

    NON-DEGENERACY PRECONDITION (LEG B, per V3-EXQ-880's own pre-registered
    grid): C0 path-exercised (WAKE arm n_waking_admitted == N_CAPTURE; SIM
    arm n_simulation_blocked == N_CAPTURE, n_simulation_admitted == 0)
    AND C1/C2/C4/C5 (all load-bearing, combined via plain AND).

    FALSIFYING evidence (LEG B): a future experiment driving genuine
    replay-tagged content through the real _do_replay() -> consumer path
    (once 543c wires it) finds simulation-tagged content reaching
    lateral_pfc.update() or residue_field.accumulate() and durably
    altering state -- i.e. the gate confirmed here in isolation is
    bypassed once wired to the real call site.

    CONFIRMING evidence (LEG A residual): a future experiment or audit
    showing the DMN/_do_replay() pathway's discarded trajectories are (or
    become, post-543c) actually consumed by some functional read path --
    would close LEG A for the specific pathway ARC-014's "Default Mode"
    title names.

    GOVERNANCE FLAG (not resolved here): claim_evidence.v1.json still
    shows genuine_exp_count=0, exp_conf=0.0 for ARC-014 despite V3-EXQ-880
    (PASS, 2026-08-02) being on file -- the index has not been regenerated
    since. Worth a governance index rebuild pass.
  location: docs/architecture/default_mode.md#arc-014
  source:
    - docs/processed/legacy_tree/docs/architecture/default_mode.md
    - docs/processed/legacy_tree/architecture/Default_mode.md
- id: IMPL-002
  title: "Repository metadata and contribution process."
  claim_type: implementation_note
  subject: repo.meta
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  location: docs/repo_meta.md#impl-002
  source:
    - docs/processed/legacy_tree/CITATION.cff
    - docs/processed/legacy_tree/CONTRIBUTING.md
    - docs/processed/legacy_tree/.github/ISSUE_TEMPLATE/feature_request.yml
    - docs/processed/legacy_tree/.github/ISSUE_TEMPLATE/bug_report.yml
    - docs/processed/legacy_tree/.github/ISSUE_TEMPLATE/discussion.yml
    - docs/processed/legacy_tree/src/placeholder.md
- id: IMPL-003
  title: "Minimum instantiation specification."
  claim_type: implementation_note
  subject: spec.minimum
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-001
    - ARC-002
    - ARC-003
    - ARC-004
    - ARC-005
    - ARC-013
    - ARC-011
    - ARC-012    # added 2026-08-08 thought-digestion: doc's own Related Claims section
                 # already lists ARC-012, and Sections 5/8 (legacy M(zeta)/lambda term)
                 # are directly about ARC-012's "no explicit ethical cost term" content
  location: docs/REE_MIN_SPEC.md#impl-003
  source:
    - docs/processed/legacy_tree/docs/REE_MIN_SPEC.md
- id: IMPL-004
  title: "Legacy REE overview summary."
  claim_type: implementation_note
  subject: overview.legacy
  polarity: asserts
  status: legacy
  live_status:
    reading: legacy
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-001
    - INV-002
    - INV-003
    - ARC-001
    - ARC-002
    - ARC-003
    - ARC-004
    - ARC-007
    - ARC-018
  location: docs/REE_overview.md#impl-004
  source:
    - docs/processed/legacy_tree/docs/REE_overview.md
- id: IMPL-005
  title: "Failure mode taxonomy."
  claim_type: implementation_note
  subject: failure_modes
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-006
    - INV-008
    - ARC-005
    - ARC-013
    - ARC-010
    - ARC-007
    - ARC-018
  location: docs/REE_failure_modes.md#impl-005
  source:
    - docs/processed/legacy_tree/docs/REE_failure_modes.md
    - docs/thoughts/FAILURE-2026-02-12_COORDINATE-SYSTEM-FOR-COGNITIVE-PATHOLOGY.md
    - docs/thoughts/2026-02-12_DEPRESSIVE-PATH-PRUNING-HIPPOCAMPAL-ROLLBACK.md
- id: IMPL-006
  title: "Legacy migration mapping."
  claim_type: implementation_note
  subject: migration.legacy
  polarity: asserts
  status: legacy
  live_status:
    reading: legacy
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  location: docs/MIGRATION.md#impl-006
  source:
    - docs/processed/legacy_tree/docs/MIGRATION.md
- id: IMPL-007
  title: "Legacy refactor final output summary."
  claim_type: implementation_note
  subject: refactor.final_output.legacy
  polarity: asserts
  status: legacy
  live_status:
    reading: legacy
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  location: docs/FINAL_OUTPUT.md#impl-007
  source:
    - docs/processed/legacy_tree/docs/FINAL_OUTPUT.md
- id: IMPL-008
  title: "Program phases, repository roles, and phase-gate criteria."
  claim_type: implementation_note
  subject: roadmap.program_phases
  polarity: records
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  location: docs/roadmap.md#impl-008
  source:
    - docs/roadmap.md
    - docs/processed/legacy_tree/roadmap.md
- id: IMPL-009
  title: "Wiring notes and cross-reference summary."
  claim_type: implementation_note
  subject: wiring.notes.legacy
  polarity: asserts
  status: legacy
  live_status:
    reading: legacy
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  location: docs/notes/wiring_notes.md#impl-009
  source:
    - docs/processed/legacy_tree/WIRING_NOTES.md
- id: IMPL-010
  title: "Android world environment contract."
  claim_type: implementation_note
  subject: examples.android_world
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - IMPL-003
  location: docs/examples/android_world_environment.md#impl-010
  source:
    - docs/processed/legacy_tree/examples/android_world/environment.md
- id: IMPL-011
  title: "Toy world environment contract."
  claim_type: implementation_note
  subject: examples.toy_world
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - IMPL-003
  location: docs/examples/toy_world_environment.md#impl-011
  source:
    - docs/processed/legacy_tree/examples/toy_world/environment.md
- id: IMPL-012
  title: "Toy world scoring functions."
  claim_type: implementation_note
  subject: examples.toy_world_scoring
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - IMPL-003
  location: docs/examples/toy_world_scoring_functions.md#impl-012
  source:
    - docs/processed/legacy_tree/examples/toy_world/scoring_functions.md
- id: IMPL-013
  title: "Documentation operating procedure and prompts."
  claim_type: implementation_note
  subject: docs.readme
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  location: docs/README.md#impl-013
  source:
    - docs/processed/legacy_tree/docs/README.md
- id: IMPL-014
  title: "Documentation change history."
  claim_type: implementation_note
  subject: docs.changelog
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  location: docs/changelog.md#impl-014
  source:
    - docs/processed/legacy_tree/docs/changelog.md
- id: IMPL-015
  title: "Legacy architecture overview."
  claim_type: implementation_note
  subject: architecture.overview.legacy
  polarity: asserts
  status: legacy
  live_status:
    reading: legacy
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  location: docs/architecture/overview.md#impl-015
  source:
    - docs/processed/legacy_tree/architecture/README_architecture.md
- id: IMPL-016
  title: "Trajectory selection detail for E3."
  claim_type: implementation_note
  subject: trajectory.selection.detail
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-003
    - ARC-004
    - ARC-013
  location: docs/architecture/trajectory_selection.md#impl-016
  source:
    - docs/processed/legacy_tree/architecture/trajectory_selection.md
    - docs/thoughts/2026-02-09_starting_with_sensory_streams.md
- id: MECH-002
  title: "Precision control analogues shape cognitive regimes."
  claim_type: mechanism_hypothesis
  subject: precision.control_analogues
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-005
    - INV-008
    - ARC-004
  location: docs/architecture/precision_control.md#mech-002
  source:
    - docs/processed/legacy_tree/architecture/precision_control.md
- id: MECH-003
  title: "Precision must be tau-scoped with lossy projections."
  claim_type: mechanism_hypothesis
  subject: precision.tau_scoping
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-008
    - ARC-004
  location: docs/architecture/precision_scoping.md#mech-003
  source:
    - docs/processed/legacy_tree/architecture/precision_scoping.md
- id: MECH-004
  title: "Signal-to-knob wiring map for control plane."
  claim_type: mechanism_hypothesis
  subject: control_plane.signal_map
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-005
    - ARC-003
    - ARC-017
    - MECH-037
    - MECH-062
  location: docs/architecture/control_plane_signal_map.md#mech-004
  source:
    - docs/processed/legacy_tree/architecture/control_plane_signal_map.md
    - docs/thoughts/2026-02-09_starting_with_sensory_streams.md
    - docs/thoughts/2026-02-17_control_plane_update.md
    - docs/thoughts/17-02-26_necessary_separations_based_on_considering-prompt_injection.md
- id: MECH-005
  title: "Path authority and interruptibility via norepinephrine-like control."
  claim_type: mechanism_hypothesis
  subject: control_plane.path_authority
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-005
    - ARC-003
    - ARC-008
  location: docs/architecture/path_authority_and_interrupts.md#mech-005
  source:
    - docs/processed/legacy_tree/architecture/path_authority_and_interrupts.md
- id: MECH-006
  title: "Serotonin-like modulation governs representational collapse."
  claim_type: mechanism_hypothesis
  subject: control_plane.serotonin_collapse
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-005
    - ARC-008
  location: docs/architecture/serotonin.md#mech-006
  source:
    - docs/processed/legacy_tree/architecture/serotonin.md
- id: MECH-007
  title: "Attention must be fragmented across control axes."
  claim_type: mechanism_hypothesis
  subject: attention.fragmentation
  polarity: asserts
  status: provisional
  epistemic_category: derivational
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-009
    - ARC-005
  what_would_answer: >
    Answered by architectural audit of the live attention/precision-related
    control channels -- a STRUCTURAL question (are these axes implemented as
    genuinely separate, non-substitutable channels writing to distinct
    registers?) -- not by a trained behavioral experiment. Distinct from ARC-005
    (does each channel have empirical CAUSAL AUTHORITY over a measured outcome)
    and from INV-009 (is attention-allocation continuous vs symbolic). MECH-007
    can be structurally CONFIRMED while ARC-005 reads mixed on channel potency
    -- the two are compatible, not in tension.

    NON-DEGENERACY PRECONDITION: the audit must find each named axis (precision
    / commitment-pressure / exclusivity) backed by its OWN live, actually-
    exercised computation with its OWN state and write target(s) -- not merely
    a distinctly-named label over a shared value. All three axes must be
    checked for cross-writes into each other's state.

    CONFIRMED CURRENT STATE (2026-08-08 digestion pass): all three axes are
    live and mutually non-overlapping. (1) PRECISION: ree_core/latent/stack.py
    LatentState.precision, 5 independently-parameterized per-channel
    nn.Parameter gains, plus a structurally distinct current_precision scalar
    in e3_selector.py/dacc.py. (2) COMMITMENT PRESSURE:
    ree_core/policy/natural_commit_urgency.py + regulators/phasic_surprise_
    burst.py -- grep-verified no reference to tonic_5ht/current_precision/
    precision_logit. (3) EXCLUSIVITY/COLLAPSE: ree_core/neuromodulation/
    serotonin.py's tonic_5ht, writing only to GoalConfig.z_goal_seeding_gain
    and valence_wanting_floor. No write path found from any one axis into
    another's state.

    FALSIFYING: any live code path where one axis's module reads or writes
    another axis's underlying state variable directly -- the "separate axis"
    naming would be cosmetic and the underlying number shared or derived from
    another axis with no independent computation. Also falsifying: a future
    control-plane channel that collapses two of the three functions into one
    shared parameter.

    CONFIRMING: current state per the audit above.

    Note: ARC-005's own channel-ablation test found only 1 of 4 tested channels
    (MODEPRIOR) shows strong causal authority over action occupancy; 5-HT and
    PHASIC show weak/scattered effects, attributed to an undertrained dACC
    adapter, not architectural non-fragmentation. Do not conflate a weak causal
    effect with a shared register.

    GOVERNANCE FLAG (2026-08-08 digestion pass, not resolved here): this audit
    is CONFIRMING and both dependencies (INV-009, ARC-005) are already active
    -- flagged for /governance promotion review (provisional -> active); NOT
    promoted during this digestion pass.
  location: docs/architecture/why_attention_must_be_fragmented.md#mech-007
  source:
    - docs/processed/legacy_tree/architecture/why_attention_must_be_fragmented.md
- id: MECH-008
  title: "Legacy mechanism: superseded by AA/PCM and control-channel mode framing."
  claim_type: mechanism_hypothesis
  subject: control_plane.mode_management
  polarity: asserts
  status: legacy
  live_status:
    reading: legacy
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-005
    - ARC-003
  location: docs/architecture/mode_manager.md#mech-008
  source:
    - docs/processed/legacy_tree/mode_manager.md
- id: MECH-010
  title: "Language emergence and bootstrapping dynamics."
  claim_type: mechanism_hypothesis
  subject: language.emergence_bootstrapping
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  depends_on:
    - ARC-009
    - ARC-010
  location: docs/architecture/language/emergence_and_bootstrapping.md#mech-010
  what_would_answer: >
    Requires three layers of substrate, none of which exist in ree_core/ (2026-08-08).
    Layer 1 (shared with ARC-009/ARC-010): a multi-agent environment with partial-
    observability other-agent modelling -- ARC-047's SocialGridWorld is the designated
    minimal substrate, itself candidate/confidence 0.0/v5-gated, unbuilt. Layer 2: the
    play_mode substrate this claim's own doc routes language through (INV-058/MECH-194/
    ARC-049) -- confirmed absent (the only "play_mode" string match in ree_core/ is a
    false positive, "_diverse_replay_mode_counts"). Layer 3: the basic expression
    catalog and proto-symbol accretion machinery specific to this claim (play pose,
    head-tilt, mutual gaze, distress vocalisation, approach cue, vocal mimicry,
    pointing, negation primitives) -- confirmed absent (zero hits for proto_symbol,
    basic_expression, negation_primitive, symbol_ground).

    CONFIRMING: once play_mode and a multi-agent substrate exist, an agent trained WITH
    play_mode-gated bootstrap episodes (bilateral frame-opening via basic-expression
    channels, conversational violations scored as synthetic z_harm) acquires
    compositional, socially-grounded communication that generalizes across novel
    referent combinations, with demonstrative (partner-model-dependent) rather than
    fixed-token negation. Loss of play_mode availability should degrade this
    communication while leaving other competencies intact.

    FALSIFYING: (a) an agent given the multi-agent + symbolic-channel substrate WITHOUT
    play_mode gating still acquires compositional, generalizing communication at
    comparable rates -- language does not need the play-mode host; (b) an agent WITH
    play_mode gating fails to bootstrap beyond fixed-token pattern-matching -- play_mode
    necessary but not sufficient; (c) degrading play_mode leaves communication intact
    while degrading unrelated play-dependent behaviors -- language is dissociable from
    play_mode rather than routed through it, falsifying the central architectural claim
    shared with MECH-308.

    NON-DEGENERACY PRECONDITION: the multi-agent substrate must support genuinely
    partial observability and >1 independently-optimizing agent (a scripted "partner"
    cannot test this), and play_mode's synthetic z_goal/z_harm substitution must be
    independently verified to route real gradient (MECH-194) before any result can be
    attributed to the play-mode-hosting hypothesis rather than the underlying learning
    substrate.

    GOVERNANCE FLAG (2026-08-08, not resolved here): companion claim MECH-308
    (registered 2026-05-08) duplicates much of this play-mode-as-language-game framing
    -- worth a governance merge-candidate review to decide which claim should carry it
    going forward.
  source:
    - docs/processed/legacy_tree/architecture/language/emergence_and_bootstrapping.md
    - docs/thoughts/2026-02-09_language.md
- id: MECH-011
  title: "Language and learning dynamics."
  claim_type: mechanism_hypothesis
  subject: language.and_learning
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-009
    - ARC-010
  location: docs/architecture/language/language_and_learning.md#mech-011
  source:
    - docs/processed/legacy_tree/architecture/language/language_and_learning.md
- id: MECH-012
  title: "Language and institutions interplay."
  claim_type: mechanism_hypothesis
  subject: language.and_institutions
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-009
    - ARC-010
  location: docs/architecture/language/language_and_institutions.md#mech-012
  source:
    - docs/processed/legacy_tree/architecture/language/language_and_institutions.md
- id: MECH-013
  title: "Language failure modes and pathologies."
  claim_type: mechanism_hypothesis
  subject: language.failure_modes
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-009
    - ARC-010
  location: docs/architecture/language/language_failure_modes.md#mech-013
  source:
    - docs/processed/legacy_tree/architecture/language/language_failure_modes.md
- id: MECH-014
  title: "Minimal signalling channel requirements."
  claim_type: mechanism_hypothesis
  subject: language.minimal_signalling_channel
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-009
    - ARC-010
  location: docs/architecture/language/minimal_signalling_channel.md#mech-014
  source:
    - docs/processed/legacy_tree/architecture/language/minimal_signalling_channel.md
- id: MECH-015
  title: "Trust and deception dynamics."
  claim_type: mechanism_hypothesis
  subject: language.trust_and_deception
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-009
    - ARC-010
  location: docs/architecture/language/trust_and_deception.md#mech-015
  source:
    - docs/processed/legacy_tree/architecture/language/trust_and_deception.md
- id: MECH-016
  title: "Precision recalibration during sleep."
  claim_type: mechanism_hypothesis
  subject: sleep.precision_recalibration
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-011
    - ARC-007
  location: docs/architecture/sleep/precision_recalibration.md#mech-016
  source:
    - docs/processed/legacy_tree/architecture/sleep/precision_recalibration.md
- id: MECH-017
  title: "Reality consolidation during sleep."
  claim_type: mechanism_hypothesis
  subject: sleep.reality_consolidation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-011
    - ARC-007
  location: docs/architecture/sleep/reality_consolidation.md#mech-017
  source:
    - docs/processed/legacy_tree/architecture/sleep/reality_consolidation.md
- id: MECH-018
  title: "Residue integration during sleep."
  claim_type: mechanism_hypothesis
  subject: sleep.residue_integration
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-011
    - ARC-007
  location: docs/architecture/sleep/residue_integration.md#mech-018
  source:
    - docs/processed/legacy_tree/architecture/sleep/residue_integration.md
- id: IMPL-017
  title: "Conflict index and resolution entry point."
  claim_type: implementation_note
  subject: conflicts.index
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - Q-008
    - Q-009
    - Q-011
  location: docs/conflicts/README.md#impl-017
  source:
    - docs/processed/legacy_tree/docs/conflicts/ethics_module_vs_cost_term.md
    - docs/conflicts/valence_vectors_vs_mu_kappa_overlays.md
    - docs/conflicts/care_override_vs_other_harm_veto.md
    - docs/conflicts/rollout_entropy_floor_vs_residue_persistence.md
    - docs/conflicts/resolutions/2026-02-08_ethics-module-vs-cost-term.md
- id: IMPL-018
  title: "Claim index and navigation."
  claim_type: implementation_note
  subject: claims.index
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  location: docs/claims/claim_index.md#impl-018
  source:
    - docs/processed/legacy_tree/docs/claims/claim_index.md
- id: IMPL-019
  title: "Self-first, social-later developmental testing order heuristic."
  claim_type: implementation_note
  subject: developmental_ordering.self_first_social_later
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-019
    - Q-006
    - ARC-005
    - ARC-007
    - ARC-010
  location: docs/architecture/developmental_curriculum.md#impl-019
  source:
    - docs/thoughts/DEV-ROADMAP-SELF-FIRST-SOCIAL-LATER.md
- id: INV-018
  title: "Agency is required; passive predictors are not REE."
  claim_type: invariant
  invariant_type: universal
  subject: agency.requirement
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  location: docs/invariants.md#inv-018
  source:
    - docs/thoughts/2026-02-08_control_plane_modes_responsibility_flow.md
- id: ARC-015
  title: "Self-impact attribution and responsibility flow are required."
  claim_type: architectural_commitment
  epistemic_category: substrate_conditional
  subject: agency.self_impact_attribution
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-018
    - INV-012
    - ARC-003
    - ARC-005
    - ARC-004
    - ARC-013
    - ARC-007
    - MECH-095
  what_would_answer: |
    ARC-015 bundles two testably-separable sub-claims: (1) SELF-IMPACT ATTRIBUTION --
    discriminating self-caused from externally-caused state change via an
    efference-copy/reafference comparator (operationalised as MECH-095, the TPJ
    agency-detection comparator at the z_self/z_world interface); and (2)
    RESPONSIBILITY FLOW -- routing that attribution into commit-boundary-gated
    learning with pre-commit/post-commit channel separation (MECH-060/061/066/067)
    and action/thought-loop completion gates (MECH-057a/057b).

    These two halves currently have DIFFERENT evidentiary status and different
    substrate requirements. Do not treat ARC-015 as a single testable-now unit.

    == HALF 2 (responsibility flow / commit-boundary separation): already has
    supporting evidence, testable now ==
    MECH-060 (V1, EVB-0043/EXQ-005 PASS) and MECH-061 (channel independence,
    |corr|=0.114) already confirm write-locus separation is enforceable and
    beneficial. MECH-057a has informative sub-threshold V1/V2 evidence
    (FULL < NO_ATTRIBUTION < NO_GATING, correctly ordered) and is now V3-eligible
    because V3 supports multi-step committed actions, which V2's atomic single-step
    actions could not provide. A clean V3 re-test of MECH-057a on multi-step action
    sequences would CONFIRM this half if the completion-gate ablation degrades
    responsibility-attribution quality above the pre-registered threshold, and would
    FALSIFY it if gating vs. no-gating remains indistinguishable at V3 scale.

    == HALF 1 (self-impact attribution / self-vs-world discrimination):
    CONFIRMED substrate-blocked on single-agent V3, do not re-test as-is ==

    NON-DEGENERACY PRECONDITION: a genuine test requires an environment containing
    BOTH self-caused and externally-caused outcomes that are causally
    indistinguishable from surface features alone -- discriminable only via the
    agent's own efference copy / action history. CausalGridWorld's native hazards
    and resource dynamics do NOT satisfy this: hazard drift and resource respawn
    are environment-authored processes with no "intent" for the comparator to
    contrast against the agent's own commanded action, so mismatch collapses to
    noise regardless of comparator quality.

    This was tested directly, not assumed. SD-047 (multi-source environmental
    dynamics) was purpose-built to supply a structurally distinct "other" cause.
    V3-EXQ-741 (2026-07-12, confirmed failure autopsy, governance-consumed) is the
    first VALID test of MECH-095 on the SD-047 substrate: non-degenerate on both
    the probe-partition and self/world-balance guards, all 4 intensity arms
    exercised as pre-registered. Result: no arm discriminates (best routing
    improvement +0.028, below the 0.04 pre-registered floor); the BASELINE already
    carries strong contact recall (0.75-0.93) without the comparator. Recorded
    interpretation: SD-047's world-caused drift is not a structurally-distinct
    OTHER, so a single-agent environment -- however richly instrumented -- cannot
    make the comparator load-bearing. This is registered as the 1st valid
    substrate_ceiling hit for MECH-095 and empirically confirms
    multi_agent_ecology_v5:MAE-3's standing prediction that this mechanism is
    intrinsically relational.

    CONFIRMING signature (once MAE-3 / a genuine second agent is available):
    on a multi-agent environment where some negative/positive outcomes are caused
    by the OTHER agent's actions and some by the focal agent's own actions
    (surface-matched, causally distinguishable only via each agent's own action
    history), MECH-095's routing improvement on an other-caused-event partition
    exceeds the pre-registered 0.04 floor over baseline, with contact_recall_routed
    and action-dissociation metrics both clearing their thresholds on >=3 seeds.

    FALSIFYING signature: even against a genuine second agent's actions, the
    comparator's routed condition fails to discriminate self-caused from
    other-caused outcomes -- this would exhaust the mapped substrate ceiling and
    force a mechanism-level reconsideration of MECH-095's TPJ-comparator
    translation, not merely another substrate enrichment attempt.

    Until MAE-3 lands, ARC-015's self-impact-attribution half stays at the same
    disposition as ARC-010 (mirror modelling of other agents): substrate-blocked,
    pending_retest_after_substrate=true, bound to multi_agent_ecology_v5:MAE-3.
  location: docs/architecture/agency_responsibility_flow.md#arc-015
  source:
    - docs/thoughts/2026-02-08_control_plane_modes_responsibility_flow.md
- id: MECH-019
  title: "Control plane shapes modes of cognition, not discrete choices."
  claim_type: mechanism_hypothesis
  subject: control_plane.mode_shaping
  polarity: asserts
  status: provisional
  epistemic_category: standard
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-005
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: (a) at least two of SalienceCoordinator's four
    operating_mode components must be non-degenerate across the test window; (b)
    the manipulated control-plane channel(s) must be swept through a genuinely
    continuous range and verified to actually move the channel's internal
    state; (c) at least one mode_switch_trigger event (discrete argmax flip)
    must occur during the sweep.

    CONFIRMING: sweeping a control-plane channel continuously produces a
    MONOTONIC, graded redistribution of the operating_mode probability vector
    across the simplex -- probability mass visibly migrates toward the
    eventually-dominant mode over several ticks BEFORE the discrete argmax
    flip, correlating with the channel's continuous value, not just a step at
    threshold-crossing. Consistent, adjacent evidence already exists: ARC-005's
    V3-EXQ-846/848-series showed MODEPRIOR channel authority_mean=1.0 and
    monotonic occupancy-vs-setting relationships; this claim's test extends
    that to the TRANSITION SHAPE specifically.

    FALSIFYING (VIOLATED BY): the operating_mode vector exhibits discontinuous,
    threshold-only jumps with NO detectable graded precursor as a channel is
    swept continuously -- mode occupancy sits flat until a hard boundary is
    crossed, then snaps. Would indicate the substrate functionally implements a
    discrete chooser regardless of its soft-vector surface representation, and
    would falsify the claim -- MECH-019 would need revision toward the legacy
    MECH-008/020 semi-discrete-modes framing it is meant to have superseded.

    Provenance note: MECH-020 is legacy, explicitly superseded by MECH-019/039.
    MECH-039 depends on MECH-019.
  location: docs/architecture/control_plane.md#mech-019
  source:
    - docs/thoughts/2026-02-08_control_plane_modes_responsibility_flow.md
- id: MECH-020
  title: "Legacy mechanism: superseded by control-plane mode framing."
  claim_type: mechanism_hypothesis
  subject: cognitive_modes.emergent
  polarity: asserts
  status: legacy
  live_status:
    reading: legacy
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-005
  location: docs/architecture/mode_manager.md#mech-020
  source:
    - docs/thoughts/2026-02-08_control_plane_modes_responsibility_flow.md
- id: MECH-021
  title: "Subjective now is a control surface across temporal horizons."
  claim_type: mechanism_hypothesis
  subject: temporal.now_construction
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-008
    - ARC-005
  location: docs/architecture/temporal_dynamics.md#mech-021
  source:
    - docs/thoughts/2026-02-08_control_plane_modes_responsibility_flow.md
- id: MECH-022
  title: "Hippocampal systems inject hypotheses gated by control plane."
  claim_type: mechanism_hypothesis
  subject: hippocampal.hypothesis_injection
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-08-09
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-190a_2026-08-09"
      as_of: 2026-08-09
      verdict: non_contributory/substrate_conditional
  epistemic_category: substrate_conditional
  depends_on:
    - ARC-007
    - ARC-005
  location: docs/architecture/hippocampal_systems.md#mech-022
  evidence_quality_note: |
    [2026-08-09 governance, V3-EXQ-190a, confirmed failure_autopsy_V3-EXQ-190a_2026-08-09]: the
    well-powered successor to V3-EXQ-190 that what_would_answer called for. Criteria met 2/5
    (C1/C2/C3 FAIL, C4/C5 PASS). NOT accepted as the demotion-grade FAIL the manifest's own
    self-route declared ("Decision: retire_ree_claim") -- two implementation/measurement issues
    undermine a fair test: (1) E3Selector.score_trajectory()'s fallback path carries two
    untrained scoring subnetworks (reality_scorer, harm_cost_fallback_scorer, code-verified never
    touched by any loss in ree_core), contaminating C3 and plausibly action selection itself in
    both conditions; (2) 190a newly extends the nav_bias forced-random-action override into the
    eval loop (absent in predecessor 190), diluting the condition-dependent behavioral signal --
    a plausible driver of C1's collapse from clean PASS (190) to near-zero/negative including a
    sign flip on repeated seed 123. ARC-007 (a direct MECH-022 dependency) was independently
    demoted 2026-07-25 on the same E3-routed-selection substrate seam -- a convergent signal, not
    two independent falsifications. epistemic_category corrected standard -> substrate_conditional
    (the autopsy's own "competence_implementation_gap" category is not a valid claims.yaml enum
    value -- mapped to the closest valid category, same correction as the 2026-08-08 cycle's
    INV-034/Q-021/MECH-074d; a corpus-wide audit is in progress, chip
    chip-20260809-epistemic-category-vocab-audit). routing=implement-substrate: substrate_queue
    entry SD-E3-SCORER-COMPLETION created this cycle (train or remove the two dead scorer heads).
    Status NOT demoted -- the FAIL is attributable to an implementation gap and a measurement
    change, not a falsified mechanism.
  what_would_answer: >
    A well-powered successor to V3-EXQ-190 (2026-04-01, FAIL/inconclusive, evidence_direction
    mixed, 3/5 criteria met -- C1 harm_gap and C2 residue_gap PASSed, C4 data-quality
    FAILed at 0 harm contacts, underpowered), clearing the data-quality floor (>=10 harm
    contacts per condition per seed, e.g. by increasing nav_bias or eval episodes as the
    original manifest itself recommended) and testing whether E3 actually rates
    hippocampally-injected proposals BETTER than random proposals (C3, the criterion that
    failed) -- not just whether injection changes harm/residue outcomes (C1/C2, already
    passed). A design that additionally varies control-plane state directly (rather than
    a binary injection ON/OFF) would more directly test the "gated by control plane" half
    of the claim, which the existing probe only proxies via presence/absence of injection.

    CONFIRMING: HYPOTHESIS_INJECTION_ON produces lower harm rate and lower residue
    accumulation than OFF (C1/C2), AND E3 assigns better (lower) trajectory scores to
    hippocampally-injected candidates than random ones across all seeds (C3).

    FALSIFYING: under a well-powered redesign, C3 continues to fail -- E3 does not treat
    hippocampally-injected candidates as higher-value even when data quality is no longer
    the confound -- weakening the operational claim that hippocampal injection improves
    the E3 candidate pool, even if C1/C2's directional harm/residue signals hold.
  source:
    - docs/thoughts/2026-02-08_control_plane_modes_responsibility_flow.md
- id: MECH-023
  title: "Responsibility is geometric and path-dependent."
  claim_type: mechanism_hypothesis
  subject: responsibility.geometry
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-013
    - ARC-015
  location: docs/architecture/agency_responsibility_flow.md#mech-023
  source:
    - docs/thoughts/2026-02-08_control_plane_modes_responsibility_flow.md
- id: MECH-024
  title: "Selfhood, personality, and ethics converge structurally."
  claim_type: mechanism_hypothesis
  subject: selfhood_personality_ethics.convergence
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-015
  location: docs/architecture/agency_responsibility_flow.md#mech-024
  source:
    - docs/thoughts/2026-02-08_control_plane_modes_responsibility_flow.md
- id: Q-006
  title: "Is ethics developmental rather than additive?"
  claim_type: open_question
  subject: ethics.developmental
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-006@2026-02-25T16:51:50.794689Z"
      as_of: 2026-02-25
      verdict: retain_open_question_with_prototype_first/applied
  what_would_answer: |
    Once the residue field is confirmed building meaningful cross-condition
    contrast (the EXQ-151 blocker), contrast a DEVELOPMENTAL ethics arm -- ethical
    constraint emerges from and reshapes the value/residue representation as the
    agent's experience accumulates (early harm-experience permanently alters later
    harm-weighting) -- against an ADDITIVE arm where an ethical penalty term is
    bolted onto a fixed value function. ANSWER "developmental" if the
    developmental arm shows path-dependence and irreversibility absent in
    additive: the ORDER and TIMING of harm exposure change final harm-avoidance
    (delta_harm differs by exposure schedule), early ethical experience
    generalises to novel harm types the additive penalty never saw, and removing
    the explicit penalty late leaves behaviour ethical in the developmental arm
    but not the additive. ANSWER "additive" if behaviour depends only on the
    present penalty term, is schedule-invariant, and reverts when the penalty is
    removed. HARD PRECONDITION: the residue field must build non-zero contrast --
    EXQ-151 was scored-excluded as an implementation gap
    (residue_field_not_building, all conditions delta_harm=0), so it is
    uninformative about Q-006; the developmental/additive distinction cannot be
    read until contrast is real.
  depends_on:
    - ARC-015
  location: docs/architecture/agency_responsibility_flow.md#q-006
  source:
    - docs/thoughts/2026-02-08_control_plane_modes_responsibility_flow.md
  evidence_quality_note: |
    EXQ-151 FAIL/mixed (2026-03-29): implementation gap -- residue field not building contrast
    (guidance="implementation_problem_residue_field_not_building"); all conditions delta_harm=0;
    not informative about Q-006 itself. Scoring excluded. Infrastructure fix required: residue
    field must build meaningful contrast before developmental vs additive ethics can be tested.
- id: Q-007
  title: "Do affect dimensions (not necessarily universal expressions) map to stable control-channel regimes in z_beta?"
  claim_type: open_question
  subject: emotion.affect_dimension_channel_mapping
  polarity: asserts
  status: active
  live_status:
    reading: active/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-007@2026-03-16T18:20:19.361155Z"
      as_of: 2026-03-16
      verdict: hold_pending_v3_substrate/applied
  epistemic_category: substrate_conditional
  what_would_answer: |
    Cluster the z_beta (control/modulatory latent) trajectory across episodes
    labelled by affect-relevant context (harm-proximal / goal-proximal /
    resource-depleted). ANSWER "yes -- affect dims map to stable control-channel
    regimes" if a small number (2-4) of affect contexts occupy separable,
    temporally-stable regions of z_beta: between-context separation /
    within-context dispersion > floor AND the regime persists across the episode
    (low within-episode drift). ANSWER "no" if z_beta does not differentiate affect
    contexts. HARD PRECONDITION: z_beta must carry cross-context variance --
    V3-EXQ-643's autopsy found the modulatory signal had magnitude but ZERO
    cross-candidate range, so on the current substrate this precondition is unmet
    and the cluster test is vacuous. Substrate-blocked on the candidate-differentiated
    modulatory-variance substrate (per-candidate modulatory variance + range-not-
    magnitude readiness gate seeded by the 643 autopsy); test becomes live once that
    substrate gives z_beta a real cross-context gradient.
    (Routed substrate_conditional per /thought-digestion 2026-06-11.)
  evidence_quality_note: |
    V3 gate cleared (2026-03-28): SD-005 (z_self/z_world split) and ARC-016 (dynamic precision)
    now implemented in ree-v3. V3 experiments can now test z_beta/running_variance correlation.
    V2 experiment EXQ-024 (valence_regime_correlation, EVB-0006) FAIL -- run twice (2026-03-08,
    2026-03-15), consistent result. Proximate failure: E3 precision hardcoded at 0.5 (no dynamic
    precision channel implemented), making the correlation mathematically impossible to observe.
    Fundamental blocker: V2 lacks correct E1/E2/E3 representation — z_beta receives mixed signals
    from conflated learning channels. Fixing precision alone would not produce valid results.
    Proxy problem: experiment used health/energy (external environment state) as valence signal;
    correct test requires z_beta valence dimension (internal affective representation) correlated
    with E3 precision. No valid evidence in current corpus.
    EXQ-051 FAIL (2026-03-20): Pearson r(z_beta, running_variance) = -0.033 (volatile),
    -0.029 (stable). Root cause confirmed — architectural gap: z_beta is computed as
    beta_encoder(cat(z_self, z_world)) with no pathway from E3's running_variance.
    Volatile environments change hazard positions but not sensory statistics (z_beta_norm
    ≈ 0.72 in both conditions). Not a threshold or calibration issue.
    Literature pull (2026-03-20): gap is well-grounded in neuromodulatory literature.
    Yu & Dayan 2005 (PMID 15944135, Neuron): LC-NE encodes unexpected uncertainty —
    running mismatch between predicted and actual variance. Not a sensory-content signal;
    NE rises when the harm-prediction distribution itself has changed. Running_variance
    in E3 is the direct REE computational analog. Payzan-LeNormand et al. 2013 (PMID
    24312028): LC-NE drives exploration and prior resetting under high prediction-error
    variance. Mathys HGF model: affective state maps to log-volatility estimate (μ₃,
    second level), not first-level prediction error. Barrett/Seth interoceptive inference:
    arousal = magnitude of interoceptive prediction error, NOT exteroceptive sensory content.
    Fix roadmap (2026-03-20):
      Option A (EXQ-051b, immediate): LatentStackConfig.volatility_signal_dim=1 injects
        E3._running_variance as scalar into beta_encoder. Tests whether z_beta shows
        condition difference and Pearson r(rv, z_beta) > 0.3.
      Option C (post SD-010, long-term): z_beta driven from harm_history — harm stream
        as REE's interoceptive/allostatic channel (Barrett arousal = |interoceptive PE|).
        running_harm_variance becomes μ₃ analog. Requires SD-010 first.
    EXQ-051b queued 2026-03-20 (V3-EXQ-051b).
    EXQ-051c FAIL 1/3 (2026-03-30, V3-EXQ-051c): C1 PASS -- volatile z_beta_norm=0.775 vs
    stable=0.659, delta=+0.116 (threshold >0.05 met). C2 FAIL -- r(z_beta, mean_rv)=0.000;
    mean_rv is flat at 0.500 in both conditions (metric broken -- rv not varying with env).
    C3 FAIL -- consequence of C2 failure. Directional signal confirmed for z_beta condition
    difference (C1), but rv injection pathway not producing variance signal. v3_pending hold
    stands until rv metric is repaired and C2/C3 retested.
    REFRAME (2026-04-02): Literature pull surfaced Barrett et al. 2019 (weakens universal
    expression premise) and Barrett 2017 (constructed emotion theory, mixed). Facial
    configurations are not reliably diagnostic of emotional states across cultures. The
    constructionist position argues emotions are constructed categories, not natural kinds
    with fixed neural signatures. This challenges the "universal expressions" framing but
    NOT the core architectural question of whether z_beta regimes exist. Reframed from
    "universal expressions -> regimes" to "affect dimensions (valence/arousal) -> regimes."
    Dimensional affect (Russell 1980 circumplex, Barrett dimensional models) is more
    consistent with REE's z_beta architecture than categorical emotion. z_beta channels
    may represent valence and arousal dimensions rather than discrete emotional categories.
    This reframe preserves the testable core (stable z_beta regime structure) while
    dropping the weakened universal-expression assumption.
  depends_on:
    - ARC-005
    - ARC-021
    - MECH-039
    - MECH-040
    - MECH-069
  location: docs/architecture/control_plane.md#q-007
  source:
    - docs/architecture/control_plane.md
- id: Q-008
  title: "Legacy: resolved in favor of valence/mu-kappa separation with calibration follow-up."
  claim_type: open_question
  subject: valence_vs_mu_kappa_overlays
  polarity: asserts
  status: legacy
  live_status:
    reading: legacy
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-017
    - MECH-035
    - MECH-048
  location: docs/architecture/control_plane.md#q-008
  source:
    - docs/architecture/control_plane.md
- id: Q-009
  title: "Legacy: resolved via bounded care-override policy with hard catastrophic other-harm veto."
  claim_type: open_question
  subject: care_weights_override_other_harm_veto
  polarity: asserts
  status: legacy
  live_status:
    reading: legacy
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-036
    - MECH-052
    - ARC-010
  location: docs/architecture/social.md#q-009
  source:
    - docs/architecture/social.md
- id: Q-010
  title: "Legacy: separation question resolved into MECH-055."
  claim_type: open_question
  subject: hedonic_valence_signed_pe_separation
  polarity: asserts
  status: legacy
  live_status:
    reading: legacy
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-035
    - MECH-048
    - MECH-054
  location: docs/architecture/control_plane.md#q-010
  source:
    - docs/architecture/control_plane.md
- id: Q-011
  title: "Legacy: resolved by placing diversity-floor control in pre-commit/offline sampling."
  claim_type: open_question
  subject: hippocampus.minimum_rollout_entropy_floor
  polarity: asserts
  status: legacy
  live_status:
    reading: legacy
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-011@2026-02-15T20:51:27.090007Z"
      as_of: 2026-02-15
      verdict: promote_to_provisional/applied
  depends_on:
    - ARC-018
    - ARC-005
    - ARC-011
  location: docs/architecture/hippocampal_systems.md#q-011
  source:
    - docs/architecture/hippocampal_systems.md
    - docs/thoughts/2026-02-12_DEPRESSIVE-PATH-PRUNING-HIPPOCAMPAL-ROLLBACK.md
- id: ARC-016
  title: "Modes are control-plane regimes applied to shared predictive machinery: the precision-to-commitment circuit."
  claim_type: architectural_commitment
  subject: cognitive_modes.control_plane_regimes
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-07-25
    needs_review: false
    evidence:
      from: "v3_exq_818_arc016_eval_derived_noise_precision_sweep_20260725T185821Z_v3"
      as_of: 2026-07-25
      verdict: "supports -- eval-time engagement of the commit threshold, the residual gate the 2026-07-25 rescore named for promotion-to-shown, is now met at the structural level (mean_rho_rv_vs_commit -0.955; commit-rate tracks E3-derived precision monotonically; load-bearing C1 pass; gate_green). Promoted provisional -> stable (/governance 2026-07-25, user-approved). Structural only (no harm DV); the ARC-029 behavioural consequence layer is separate."
  status_note: |
    Split 2026-03-22: ARC-016 now covers only the structural/mechanistic circuit
    (E3-derived variance -> relative threshold -> BetaGate -> action_selection).
    The behavioral consequence layer (committed vs uncommitted -> measurably distinct
    harm outcomes) is ARC-029. Provisional status is justified by EXQ-018b PASS 5/5
    and EXQ-060 PASS 4/5.
  evidence_quality_note: |
    Scope (post-split 2026-03-22): ARC-016 asserts that operating modes exist as
    control-plane regimes — specifically that E3-derived prediction variance drives a
    commitment threshold which gates action-selection via BetaGate. The behavioral
    consequence of that gating (harm differences between modes) is ARC-029.
    V2 EXQ (precision_regime_probe, 2026-03-08): FAIL (partial_support=true, 1/2 criteria).
    Criterion 1 met: precision gap achieved (1.697, easily). Criterion 2 failed: behavioral
    distinction absent. Root cause: precision was externally imposed (not E3-derived) and
    commitment-to-action circuit not wired end-to-end in V2. Two V3 requirements exposed:
    (1) E3-derived dynamic precision calibrated from prediction error;
    (2) commitment gating must actually change action-selection.
    Note: MECH-059 (precision/PE structural separation) PASSED in V2, confirming the
    channels exist as distinct signals. ARC-016 structural separation is necessary but
    not sufficient for ARC-029 behavioral distinction.
    V3 EXQ-018 FAIL (2026-03-20): Variance mechanism working but absolute threshold
    (0.40) was 100x the operating variance range. Fix: relative threshold.
    V3 EXQ-018b PASS (2026-03-20, 5/5 criteria): Relative threshold confirmed working.
    commit_threshold = 2 x training_baseline_variance. Stable env: commit_rate=0.90,
    precision=718; perturbed env: commit_rate=0.50, precision=426. 40% precision drop
    produces proportional 40-point commit-rate drop. Circuit is end-to-end: E3-derived
    variance -> commit_threshold comparison -> BetaGate -> action_selection.
    EXQ-060 PASS (2026-03-21, 4/5): Committed-condition BetaGate confirmed.
    committed_step_count=5980, hold_rate_during_committed=0.936,
    calibration_gap_approach=0.930. C4 missed (propagation_count=0) because single-agent
    design keeps agent permanently committed — not a mechanism failure.
    ARC-016 core circuit is validated. See ARC-029 for the behavioral consequence layer.
    EXQ-396a (2026-04-14, x3 runs) and EXQ-396b (2026-04-16): ALL reclassified
    non_contributory. Calibration issue: train variance (~0.000032-0.000064) << eval
    variance (~0.001) regardless of hazard_harm level. Commitment threshold never
    engages in eval; train/eval variance gap prevents differentiation. Not evidence
    against precision-to-commitment mechanism. Pending recalibration with
    eval-derived threshold.
    EXQ-454 governance 2026-04-22 reclassified non_contributory: adaptive commitment
    threshold test cannot resolve while V_s monostrategy locks the policy --
    threshold-adaptation behaviour is dominated by the lock-in rather than by the
    precision-to-commitment circuit under test. ARC-016 core circuit remains
    validated by EXQ-018b/EXQ-060; pending behavioural retest after MECH-269.

    2026-07-25 (governance): the derived agenda recommended demote_to_candidate
    (provisional -> candidate), but this is an AGGREGATION ARTEFACT and is NOT applied.
    The exp_conf ~0.53 driving the demotion aggregates 15 FAILs, nearly all already stamped
    non_contributory or substrate-version-stale (see heterogeneity_note); this cycle's
    V3-EXQ-805 (eval-derived commit threshold) is likewise non_contributory
    (measurement_test_design_defect, confirmed backlog autopsy 2026-07-24), not weakens.
    USER-CONFIRMED HOLD: keep provisional, do NOT demote. A proper rescore that stamps the
    non_contributory/stale FAILs out of the conflict ratio is owed before any status change
    -- routed to /claim-synthesis (rescore), not applied inline, because stripping the
    non-contributory FAILs may expose that the remaining supports are thinner than the
    headline number and that is a synthesis judgement, not a governance edit.

    2026-07-25 (/claim-synthesis rescore, session cool-dijkstra-3f6f0c; RESCORE COMPLETE
    + APPLIED). The rescore confirmed the artefact and corrected it at source. Diagnosis:
    the reclassifications this note describes were documented in prose but NEVER stamped
    onto the manifests' evidence_direction field, so the indexer scored ~23 stale /
    non_contributory / mis-scoped runs as genuine weakens. Stamped the documented
    exclusions (even-handedly -- version-stale supersession also removed the pre-relative-
    threshold EXQ-018 PASSes and the EXQ-041 baseline smoke in BOTH directions, so this is
    not a strip-the-weakens cherry-pick) across 50 manifest files + rebuilt the index:
    exp_conf 0.521 -> 0.775; genuine directions 8 supports / 13 weakens / 2 mixed ->
    5 supports / 0 weakens / 0 mixed; quadrant plausible_unproven -> confirmed_established.
    Zero genuine refutations remain (surviving supports: EXQ-018b, EXQ-060, EXQ-031,
    EXQ-096a). VERDICT: HOLD provisional (confirms the governance rejection); NOT
    granularity debt, so NO claim decomposition -- the cluster is substrate-version-stale +
    calibration/test-design debt + mis-scoped-to-ARC-029, not >=2 genuine structural FAIL
    signatures. IMPORTANT -- the corrected exp_conf 0.775 is NOT a promotion trigger: all
    5 supports are TRAIN-TIME / STRUCTURAL; the EVAL-TIME engagement of the commit threshold
    (the one thing that would close the claim) has never produced a clean support, every
    eval-time attempt (EXQ-396a/b, 454, 530, 805) being non_contributory. The confirmed_
    established quadrant reflects structural supports + strong literature, not a closed
    eval-time validation. The residual gate to `shown` is the eval-derived-threshold
    STRUCTURAL re-run named in what_would_answer (NON-DEGENERACY precondition; no harm DV).
    Detail: evidence/planning/claim_synthesis_arc-016_2026-07-25.md; decision_log
    2026-07-25 (rescore_complete). Cross-cycle guard: a future /governance cycle seeing
    the promote signal must read it against this rescore.

    2026-07-25 (/governance, session angry-ardinghelli-d99740-gov; PROMOTED provisional ->
    stable, USER-APPROVED). The residual gate the rescore named -- the eval-derived-threshold
    STRUCTURAL re-run showing eval-time engagement of the commit threshold -- RAN and PASSED:
    V3-EXQ-818 (eval_derived_noise_precision_sweep, 2026-07-25). Eval-derived commit threshold
    + graded-noise E3 precision sweep: mean_rho_rv_vs_commit = -0.955 (commit-rate tracks
    E3-derived precision MONOTONICALLY across the noise ladder), manip_took_worst_seed 5.4x,
    min_commit_decisions_worst 300, gate_green = True, load-bearing C1
    (commit_rate_tracks_precision_monotonically) PASS. This is the first eval-time SUPPORT for
    the precision-to-commitment circuit -- every prior eval-time attempt (EXQ-396a/b, 454, 530,
    805) was non_contributory, and the 5 rescore-surviving supports were all TRAIN-TIME /
    STRUCTURAL. 818 closes the eval-time structural gate the rescore explicitly held open, so
    the promotion is now justified at the STRUCTURAL level. SCOPE of the promotion: structural
    only -- 818 carries no harm DV, so the ARC-029 behavioural-consequence layer (measurably
    distinct harm outcomes between modes) remains the separate open question and is NOT promoted
    by this. Manifest evidence_direction already 'supports'; unverified flag (legacy, no
    preconditions block) decided at the walk, not blocked.
  heterogeneity_note: "Substrate-version confound: the FAILs predate the relative-threshold implementation (EXQ-018) or are confounded by V_s monostrategy lock (EXQ-396a/396b, EXQ-454) rather than reflecting a circuit failure; the core mechanism is validated by EXQ-018b and EXQ-060."
  depends_on:
    - ARC-005
    - MECH-059
    - ARC-021
  epistemic_category: standard
  what_would_answer: |
    ARC-016 is UNLIKE the other load-bearing ARC claims: its core circuit is ALREADY
    VALIDATED and it does not need a new discovery experiment. Digested 2026-07-21.
    VALIDATED: E3-derived variance -> relative commit threshold -> BetaGate ->
    action_selection, by EXQ-018b (PASS 5/5; commit_threshold = 2 x
    training_baseline_variance; stable env commit_rate 0.90 / precision 718, perturbed
    0.50 / 426 -- a 40% precision drop producing a proportional 40-point commit-rate
    drop) and EXQ-060 (PASS 4/5; committed_step_count 5980,
    hold_rate_during_committed 0.936).
    ARC-016 would be OVERTURNED by the commit threshold ceasing to track E3-derived
    variance -- commit_rate failing to move with a manipulated precision level while
    the BetaGate remains wired. That would mean modes are NOT control-plane regimes
    over shared predictive machinery but independent modules.

    THE 15 FAILs DO NOT REFUTE THE CIRCUIT -- each is already diagnosed (see
    heterogeneity_note): the V2 precision_regime_probe imposed precision externally
    rather than deriving it from E3 and had no end-to-end commitment wiring; V3
    EXQ-018's absolute threshold 0.40 was 100x the operating variance range (fixed by
    going relative); EXQ-396a x3 and EXQ-396b are non_contributory on a train/eval
    variance gap (train ~0.000032-0.000064 vs eval ~0.001, so the threshold never
    engages in eval); EXQ-454 is non_contributory because the V_s monostrategy lock
    dominates threshold-adaptation behaviour; V3-EXQ-642's autopsy records a
    measurement / test-design gap, explicitly NOT a substrate_ceiling.
    NOTE FOR GOVERNANCE (flag, not a digestion action): exp_conf 0.53 is therefore an
    AGGREGATION ARTEFACT -- a claim whose FAILs are almost all already stamped
    non_contributory still scores as if half-refuted. Re-scoring needs a governance eye
    rather than a digestion edit, because stripping non-contributory FAILs can expose
    that the REMAINING supports are thinner than the headline number suggested.

    THE ONE GENUINELY OPEN ITEM is calibration, not mechanism -- named by this claim's
    own notes as "pending recalibration with eval-derived threshold". ANSWERED by
    re-running the EXQ-396 design with an EVAL-DERIVED (or online-adapted) baseline
    variance in place of 2 x training_baseline_variance.
    ANSWER "calibration artefact; validation stands" (PASS) if with an eval-derived
    threshold the commitment threshold engages during eval and commit_rate tracks
    manipulated precision monotonically.
    ANSWER "genuine evidence against the circuit" (FAIL) if even a correctly-calibrated
    eval-time threshold leaves commit_rate not tracking precision. That would be the
    FIRST real evidence against ARC-016, and the 15 prior FAILs would need re-reading
    as right for the wrong reasons.
    NON-DEGENERACY PRECONDITION (this is exactly what killed EXQ-396a/b -- trap it):
    the commit threshold must ACTUALLY ENGAGE in eval -- commit_rate strictly between
    floor and ceiling, not pinned at 0.0 or 1.0, and eval-time variance within the
    threshold's dynamic range. A threshold that never fires makes all arms identical ->
    substrate_not_ready, NOT a verdict.
    SCOPE GUARD: the run must be STRUCTURAL ONLY. Do NOT attach a behavioural harm DV --
    that re-imports the V_s monostrategy-lock confound that voided EXQ-454, and the
    behavioural consequence layer is ARC-029, not ARC-016 (split 2026-03-22).
  location: docs/architecture/modes_of_cognition.md#arc-016
  source:
    - docs/thoughts/2026-02-08_modes_of_cognition_control_plane_regimes.md
- id: ARC-017
  title: "Minimal stream tags with typed exteroception and explicit reality-coherence lane."
  claim_type: architectural_commitment
  subject: sensory_stream_tags.minimal_set
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  evidence_quality_note: |
    EXQ-129 FAIL (2026-03-29): stream tag pair FAIL. First experimental entry.
    EXQ-135 FAIL (2026-03-29): reality coherence pair FAIL. Second FAIL on this claim.
  depends_on:
    - INV-008
    - INV-012
    - ARC-004
    - ARC-005
    - ARC-003
    - ARC-015
  digestion_note: |
    [2026-08-07, thought-digestion, session cool-torvalds-a82359] DEFERRED --
    do not draft a what_would_answer until this is resolved, remove this
    note once resolved.

    ARC-017's evidence_quality_note records two FAILs (EXQ-129, EXQ-135,
    both 2026-03-29) and has not been touched since -- five months with no
    re-run, autopsy, or diagnostic. Both FAILs share a suspicious shape: the
    pre-registered discrimination floor (C1, gap >= 0.04) was missed by the
    INTACT condition itself (TYPED_TAGS_ON / REALITY_COHERENCE_ON both
    measured gap=0.0153), not just by the ablation comparison -- meaning the
    underlying z_world representation may never have cleared its own
    readiness bar before the typed-routing/reality-coherence ablation was
    tested on top of it. Both original manifests hedge this explicitly at
    FAIL time ("a FAIL here may reflect insufficient warmup rather than a
    genuine claim refutation"; the reality-coherence proxy is "only
    partially approximated"). This codebase has since (2026-07-18 onward,
    SD-070 and failure_autopsy_V3-EXQ-728/875/875a/882) established a
    recurring, distinct-but-analogous pattern where a floor-failing
    discrimination gap in the intact arm indicates an undertrained/
    not-yet-converged representation rather than a genuine architectural
    negative result -- and nobody has checked whether an equivalent
    readiness problem (e.g. 400-episode single-run warmup being
    insufficient for this proxy's E1+E2-loss-only training path) explains
    EXQ-129/135's result.

    Recommend a /failure-autopsy pass on both runs to determine (i) whether
    the C1 floor-failure is a genuine representation ceiling under this
    proxy design or an undertraining artifact, and (ii) whether the
    typed-stream-separation and reality-coherence mechanisms should be
    re-tested with a warmup/scale precondition once (i) is resolved -- only
    then can a what_would_answer be honestly drafted for this claim, since
    ARC-017's reality-coherence lane is architecturally load-bearing for
    INV-012 (responsibility/commitment gating) and a falsifier drafted on
    top of an unresolved floor-failure risks either prematurely
    re-litigating a dead design or quietly re-endorsing an untrustworthy
    negative result.
  location: docs/architecture/sensory_stream_tags.md#arc-017
  source:
    - docs/thoughts/2026-02-08_sensory_stream_tags.md
    - docs/thoughts/2026-02-09_starting_with_sensory_streams.md
    - docs/thoughts/2026-02-09_empathy.md
    - docs/thoughts/2026-02-17_control_plane_update.md
    - docs/thoughts/17-02-26_necessary_separations_based_on_considering-prompt_injection.md
- id: ARC-018
  title: "Hippocampus generates explicit rollouts and post-commitment viability mapping."
  claim_type: architectural_commitment
  subject: hippocampus.rollout_viability_mapping
  polarity: asserts
  status: provisional
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-018@2026-03-16T18:20:19.361124Z"
      as_of: 2026-03-16
      verdict: hold_pending_v3_substrate/applied
  last_reviewed: 2026-02-15
  lifecycle_stage: adjudicated
  adjudication_outcome: retain_ree
  adjudicated_at_utc: 2026-02-15T18:46:42.773429Z
  adjudication_decision_status: applied
  adjudication_recommendation: retain_ree
  v3_pending: false
  reframe_note: |
    Original framing (2026-02-09) incorrectly attributed viability mapping to E1 prediction
    error (SELF_SENSORY mismatch). V2 EXQ-021 FAIL (2026-03-08, 2026-03-15) confirmed this
    framing is wrong: E1 prediction error has no advantage over a frozen E1 for harm navigation.
    Corrected framing (2026-03-15): hippocampus builds viability map indexed by E2 action-object
    coordinates, updated by E3 harm/goal error. E1 provides perceptual context to E2 only.
    E2's latent space is action consequences (action objects), not sensory state transitions.
    The map lives in hippocampal geometry, not in E1.
  evidence_quality_note: |
    V2 experiment EXQ-021 (rollout_viability_mapping, EVB-0017) FAIL — run twice (2026-03-08,
    2026-03-15), consistent result. VIABILITY_MAPPED (E1 updating) shows no advantage over
    VIABILITY_FIXED (frozen E1) in harm navigation; FIXED outperforms MAPPED in 2/3 seeds.
    This is a conceptual failure of the original claim framing, not a substrate or tuning issue.
    V3 EXQ-042 PASS (2026-03-19): terrain_prior learns E3 preferences, hippo_quality_gap=0.393,
    terrain_loss→0. V3 EXQ-053 PASS (2026-03-20): terrain-guided navigation produces 50×
    less harm than random (harm_per_step 0.00147 vs 0.0724, viability_advantage=0.071, 5/5
    criteria). SD-004 and SD-005 prerequisites implemented in V3 substrate.
    v3_pending cleared 2026-03-20. Outstanding: EXQ-046 (ARC-007 ablation) will confirm
    the action-object indexing mechanism specifically contributes.
    EXQ-120 PASS (2026-03-28): viability map discriminative pair. evidence_direction=supports.
    Adds one more support entry confirming the V3 substrate continues to pass this claim.
    EXQ-172 INCONCLUSIVE (2026-03-30, T213638Z): E2 quality borderline (r2=0.203, gate=0.15);
    direction criteria fail. Not a clean test. Superseded by T070425Z run.
    EXQ-172 FAIL 0/4 (2026-03-30, T070425Z): harm_advantage_mean=-0.190. E2 quality adequate
    (r2=0.203) -- rollout planning counterproductive at current fidelity. ARC-018 may be
    E2-quality-gated. Evidence direction: weakens.
    EXQ-196 FAIL (2026-04-04): harm_advantage_mean=0.0 across all 3 seeds with good substrate
    quality (e2_world_r2=0.766, residue populated). Classified non-contributory (2026-04-06
    governance): zero advantage is diagnostic of missing SD-004 substrate (action-object
    hippocampal map backbone), not refutation of ARC-018. Verdict deferred pending SD-004.
    EXQ-196 MECHANISM IDENTIFIED (2026-07-22, session keen-varahamihira-791a46, substrate
    defect confirmed by independent reproduction; ree-v3 CLAUDE.md "Action-object round trip
    is NOT an action source"). The 2026-04-06 governance read -- "zero advantage is diagnostic
    of missing substrate, not refutation" -- is CONFIRMED and now has an exact mechanism, so
    the deferral was correct but its stated blocker (SD-004) was not the operative one.
    EXQ-196 selects with argmax(e3.select(...).selected_action). selected_action is the chosen
    candidate's actions[:, 0, :], i.e. action_object_decoder output. Measured on a trained
    agent (CausalGridWorldV2 size=6, 40 warmup episodes, seed 42, e2 regime comparable to the
    run) under the config defaults in force on 2026-04-04: 68 of 68 ticks (100%) the ENTIRE
    32-candidate set offered exactly ONE first-action class. E3's choice therefore could not
    reach the executed action, both arms executed identical action streams, and
    harm_advantage_mean = EXACTLY 0.0 on all three seeds is arithmetic, not measurement --
    which is why it was exactly zero rather than merely small, at e2_world_r2 0.766.
    THE BLOCKER HAS SINCE BEEN REMOVED, incidentally rather than deliberately: SP-CEM became
    the main-path default on 2026-05-17 (ree-v3 cb1c6da, ARC-065), six weeks AFTER the run.
    Same measurement under the current default: mean 2.29 distinct first-action classes,
    min 2, 0% single-class ticks. So LEG B ("explicit rollouts") was UNTESTABLE in April by
    construction, and is testable now. This does not change ARC-018's status or confidence and
    does not make EXQ-196 count as evidence in either direction -- it remains non_contributory
    on a firmer basis. Re-adjudication of the deferral is GOVERNANCE work, not applied here.
    Note EXQ-172 (FAIL, e2_world_r2 0.203) is a DIFFERENT confound and is not addressed by
    this finding. Its own note already reads it as E2-quality-gated.
    RELATED, DISTINCT DEFECT affecting other ARC-018 evidence: recovering a candidate's action
    by re-decoding its action-object -- argmax(action_object_decoder(traj
    .get_action_object_sequence()[:, 0, :])) -- is a constant function, because E2's step-0
    action-object embedding is near action-invariant (5 distinct CONSTRUCTED first actions all
    re-decode to 1 class, untrained and after 40 warmup episodes alike). EXQ-196 does NOT use
    that path, but EXQ-120 (PASS, leg A, 2026-03-28) DOES, as do EXQ-114, EXQ-266 and
    EXQ-266a; flagged by the new validate_experiments action_object_selection lint (6 fires /
    1113 scripts). Whether EXQ-120's PASS survives is UNASSESSED here and needs its own triage
    -- a collapsed action stream can still yield a real terrain-quality read, so this is a
    reason to check, not a reason to discount.

    2026-07-25 (governance): status active -> provisional. V3-EXQ-120a (viability-map pair,
    C2 cluster) is a USER-CONFIRMED weakens -- with E3-routed selection, a matched episode
    budget and a static no-op control, the harm-reduction advantage collapses to -18.1%
    (< 15% required); P1 GREEN so this is a MEASUREMENT, a decision-flipping negative, not an
    instrument null. The DEMOTION IS LEG-SCOPED: it demotes the BEHAVIOURAL harm-navigation
    translation only; ARC-018's biological existence-proof (hippocampal rollout + viability
    mapping exists) and leg A (EXQ-042/053, no round-trip) are unaffected. NOT unconditional:
    V3-EXQ-801 (rollout-depth ablation) on this same claim is non_contributory /
    substrate_conditional (pending_retest_after_substrate) and is HELD OUT of the conflict
    ratio, not counted as an additional weakens -- the demotion is driven solely by 120a's
    genuine null.
  depends_on:
    - ARC-007
    - ARC-003
    - ARC-002
    - ARC-001
    - ARC-021
  epistemic_category: standard
  what_would_answer: |
    ARC-018 is CONJUNCTIVE, and its PASS/FAIL split is NOT noise -- the evidence
    separates cleanly by leg. Digested 2026-07-21.
    LEG A "post-commitment viability mapping": EXQ-042 PASS (terrain_prior learns E3
    preferences, hippo_quality_gap 0.393, terrain_loss -> 0), EXQ-053 PASS (terrain-
    guided navigation 50x less harm than random, 5/5 criteria), EXQ-120 PASS
    (discriminative pair). Three consistent PASSes.
    LEG B "explicit rollouts": EXQ-172 FAIL (harm_advantage_mean -0.190), EXQ-196 FAIL
    (harm_advantage_mean 0.0 across all 3 seeds). Two FAILs.
    Every PASS is leg A; every FAIL is leg B. Carrying this as one conjunction at
    exp_conf 0.547 averages a well-supported leg with an unsupported one and hides
    both facts.
    BUT LEG B IS UNTESTED, NOT REFUTED -- both FAILs are confounded, differently:
    EXQ-172 ran at e2_world_r2 = 0.203 and the note itself concludes "rollout planning
    counterproductive at current fidelity ... ARC-018 may be E2-quality-gated" -- a
    negative advantage from a poor forward model indicts E2, not the rollout
    architecture. EXQ-196 ran at good fidelity (e2_world_r2 0.766, residue populated)
    and returned EXACTLY 0.0 on all three seeds -- an exact-zero-everywhere result is a
    DEGENERACY SIGNATURE (the rollout output never reached the DV), which is why
    governance classified it non_contributory and deferred the verdict.

    LEG A would be OVERTURNED by: terrain-guided navigation losing its advantage over
    random when the terrain prior is ablated WHILE hippo_quality_gap and terrain_loss
    remain healthy -- i.e. the map is learned but inert.

    LEG B is ANSWERED by a ROLLOUT-DEPTH ABLATION AT VERIFIED E2 FIDELITY: arms at
    rollout depth 0 (no rollout) / shallow / full, run only after E2 fidelity clears a
    measured floor. Primary DV harm_advantage_mean.
    ANSWER "rollouts confer planning advantage" (PASS) if harm_advantage_mean scales
    monotonically with rollout depth, effect >= 0.8 SD of the cross-seed delta, on
    >= 2/3 seeds. ARC-018 then holds conjunctively.
    ANSWER "rollout leg demotes" (FAIL) if there is no advantage at ANY depth despite
    verified-live rollouts and adequate E2. ARC-018 reduces to its mapping leg, and
    MECH-092 plus the replay-consolidation dependents need re-derivation.
    NON-DEGENERACY PRECONDITIONS (each drawn from a specific prior failure; any breach
    -> substrate_not_ready, NOT a verdict):
    (a) e2_world_r2 >= 0.6 measured IN-RUN -- below this the EXQ-172 confound recurs;
    (b) rollout-derived scores must carry non-zero cross-candidate variance -- the
    rollout must actually discriminate between candidates;
    (c) harm_advantage_mean EXACTLY 0.0 across all seeds is the EXQ-196 signature of an
    unwired DV -- trap it explicitly and self-route substrate_not_ready. NEVER record
    that pattern as a FAIL.
  location: docs/architecture/hippocampal_systems.md#arc-018
  source:
    - docs/thoughts/2026-02-09_starting_with_sensory_streams.md
    - docs/thoughts/2026-02-09_e2_hpc_interface.md
    - docs/thoughts/2026-03-14_three_bg_systems_error_signals.md
- id: ARC-019
  title: "REE requires staged developmental training with explicit curriculum gates."
  claim_type: architectural_commitment
  epistemic_category: standard
  subject: developmental.curriculum_stages
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: A staged-vs-unstaged comparison is only informative if
    the staged arm can actually traverse its stages under the tested budget -- i.e. the
    phase-advance gate criteria (as in InfantCurriculumScheduler / infant_substrate_expansion.md
    Section 8) must be reachable, and the unstaged control must differ from the staged arm only
    in the presence/absence of gating (same total episode budget, same environmental feature set
    eventually enabled, same seeds) -- not merely relabeled episode counts. V3-EXQ-591 (2026-05-27)
    demonstrated the failure mode this precondition guards against: all arms (staged and flat
    alike) plateaued at 1/7 gate criteria because a goal-pipeline prerequisite (z_goal seeding,
    goal_pipeline:GAP-4) was not yet substrate-ready, producing a substrate-ceiling non-result
    rather than a test of the curriculum question. This precondition must be independently
    verified (e.g. staged arm reaches at least Phase 1 in >=80% of seeds) before treating any
    outcome below as evidence either way.

    CONFIRMING: Given the non-degeneracy precondition holds, the staged/gated curriculum arm
    (ARM_2-style, phase-advance gated on the DEV-NEED-008 / infant_substrate_expansion.md
    Section 8 criteria) reaches a materially higher fraction of the 7-criterion developmental
    gate (>=6/7 in >=4/5 seeds, per V3-EXQ-591's own acceptance design) than an unstaged/ungated
    baseline trained on the identical total episode budget and identical eventual feature set
    (flat ARM_0/ARM_1-style). Equivalently, on any already-used downstream competence metric in
    this registry (e.g. MECH-457 competence-floor measures, or convergence quality/H_pos
    trajectory stability), the staged arm should show measurably better or more stable outcomes
    than the ungated control at matched total training cost -- and this advantage should
    reproduce across at least one additional curriculum-bearing lineage (e.g. scaffolded_sd054
    onboarding, or the play-type progression of MECH-197) once each is substrate-ready, not
    just the infant-stage instance.

    FALSIFYING: Given the non-degeneracy precondition holds (staged arm demonstrably traverses
    multiple stages), the staged/gated arm shows no reliable advantage over the unstaged/ungated
    baseline at matched total training budget -- e.g. gate-criterion satisfaction, competence-floor
    measures, or convergence quality are statistically indistinguishable between staged and flat
    arms, or the flat arm matches/exceeds the staged arm at equal or lower cost. This would
    indicate the explicit stage/gate structure is not doing useful work beyond what unstructured
    training achieves anyway -- the curriculum may still be a convenient scaffold for organizing
    training runs and diagnostics, but "staged development with explicit gates" would not be
    architecturally load-bearing for outcome quality, only for interpretability/debuggability
    (which is a different, weaker claim than ARC-019 currently asserts).
  depends_on:
    - ARC-005
    - ARC-006
    - ARC-007
    - ARC-013
    - INV-010
  location: docs/architecture/developmental_curriculum.md#arc-019
  source:
    - docs/architecture/developmental_curriculum.md
- id: ARC-020
  title: "Offline consolidation is protected by typed authority/write boundaries."
  claim_type: architectural_commitment
  subject: sleep.protected_offline_consolidation_boundary
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-011
    - INV-010
    - INV-021
    - INV-023
  location: docs/architecture/sleep.md#arc-020
  source:
    - docs/architecture/sleep.md
    - docs/architecture/agency_responsibility_flow.md
    - docs/invariants.md
  epistemic_category: substrate_conditional
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: this claim requires a live, reachable typed
    commit-boundary mechanism gating writes FROM offline/sleep consolidation
    TO privileged authority stores (POL/ID/CAPS per
    docs/architecture/sleep.md#arc-020) -- i.e. MECH-067's "machine-checkable
    phase/store/actor permission matrix" (or equivalent) must actually be
    wired into the V3 sleep pipeline, with an offline code path CAPABLE of
    attempting an authority-store write and being intercepted/typed-routed
    rather than merely never attempting one by construction. THIS
    PRECONDITION CURRENTLY FAILS: exhaustive grep of ree-v3/ree_core/ for
    write_locus, permission_matrix, POL/CAPS/ID authority-store analogs,
    commit_token, and commit_boundary returns zero hits. The sleep pipeline
    has phase/mode routing (SWS/REM gates, sleep-onset permits) but no
    privileged-authority-store concept and no typed commit-boundary/
    verifier-mediation machinery. MECH-067, the claim this would be
    implemented through, is provisional on V1 substrate only (ree-v1-minimal
    EXQ-005/EVB-0043, 2026-02-27) and has never been retested in V3. Do not
    force a genuine_exp test while this precondition is unmet -- any attempt
    would be vacuous (no reachable write path to authority stores exists for
    offline consolidation to violate OR respect).
    CONFIRMING (once the precondition is met): an experiment in which
    offline/sleep consolidation generates a candidate authority-affecting
    change (a commitment-token mint, an external-action dispatch, or a
    direct write to a POL/ID/CAPS-equivalent store) is rejected or re-routed
    through the typed commit-boundary/verifier path rather than landing
    directly -- distinguishable from an ordinary representational/residue/
    precision write (which should succeed unimpeded, per the sleep.md "MAY
    update" list).
    FALSIFYING: (a) offline/sleep consolidation can mutate a privileged
    authority-store equivalent directly, with no type check, no verifier
    mediation, and no distinguishable code path from an ordinary
    representational write; or (b) a boundary exists but does not
    discriminate by TYPE (any offline-originated write reaches the store
    regardless of whether it is representational or authority-affecting);
    or (c) the only enforcement is a naming/comment-level convention with
    no machine-checked gate.
- id: MECH-025
  title: "Action mode prioritizes short-horizon, high-precision control via context-dependent precision modulation."
  claim_type: mechanism_hypothesis
  subject: cognitive_modes.action_doing_precision
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-08-10
    needs_review: false
    evidence:
      from: "decision:MECH-025@2026-08-10"
      as_of: 2026-08-10
      verdict: promote_to_provisional/applied
  evidence_quality_note: |
    V3 gate cleared (2026-03-28): SD-004 (action objects), SD-005 (z_self/z_world split),
    and ARC-016 (dynamic precision) now implemented in ree-v3. V3 action-doing mode
    experiments can now proceed.
    V2 experiment EXQ-026 (action_doing_mode_probe, EVB-0025) FAIL -- 1 run (2026-03-08).
    Proximate failure: action_precision_lift = 0.0 across all seeds/conditions — E3 precision
    hardcoded, no dynamic channel (same root cause as Q-007). Fundamental blocker: action-doing
    mode requires self-attribution (SD-003) — the agent must detect it is the causal source, not
    read an external transition_type label. SD-003 requires E2 action-object latent (SD-004).
    Slope criterion: HIGH_CAUSAL (0.0211) vs LOW_CAUSAL (0.0174) — right direction, 0.0037 gap,
    well short of 0.05 margin; likely policy gradient density effect, not action-doing mode signal.
    No valid evidence in current corpus.
    DECOMPOSITION (2026-04-02): Original MECH-025 combined precision modulation (mechanistic,
    supported by Friston 2013 active inference) with responsibility linkage (architectural,
    philosophical bridge). Literature supports precision modulation of motor control but the
    responsibility linkage -- where high precision implies ethical accountability -- is a
    philosophical claim, not a neuroscience finding. MECH-025 now covers the mechanistic
    precision modulation only. Responsibility linkage decomposed to MECH-025b.
    EXQ-199 DIAGNOSTIC (2026-04-02, governance 2026-04-03): committed_step_count=0 both seeds.
    BreathOscillator creates uncommitted windows but agent never enters committed state during
    evaluation -- cannot measure doing-mode causal signal without committed steps. Classified
    as substrate limitation (not evidence against MECH-025). evidence_direction=mixed in
    manifest but excluded from evidence scoring for this reason.
    2026-08-03 (governance, V3-EXQ-876, confirmed failure_autopsy_mech025-cluster-876-671b_2026-08-03):
    first fair V3 test -- all three prior instrument defects (cache-field/torn-down-field
    commitment read, frozen running_variance during eval) fixed and verified (committed/uncommitted
    regimes well-sampled, non-degenerate every seed). doing_mode_delta is consistently negative
    (4/5 seeds), opposite the predicted sign. Read as non_contributory/measurement_test_design_defect
    (re-operationalize the doing-mode causal-signal DV), not evidence against the claim. Routed to
    /queue-experiment. Stays candidate.
    2026-08-10 (governance, promote_to_provisional applied): V3-EXQ-876a is the re-operationalized
    DV the 876 autopsy's what_would_answer block called for -- C1_doing_mode_delta_reversed tests
    the reversed sign directly (per the FALSIFYING/CONFIRMING framing above), and PASSES on all 5
    seeds (doing_mode_delta=-0.179, not razor-thin). All 6 criteria (incl. precision_ratio, a
    ~3.6e5 separation between committed/uncommitted precision, matching the "4-5 orders of
    magnitude" already observed in 876) clear on every seed. Governance's own mandatory
    non-outcome-findings skim of the driver source found nothing amiss (real margin on the
    load-bearing criterion, no OR/AND join-mismatch, no config-branch asymmetry). exp_conf=0.857,
    conflict_ratio=0, 4 supports/2 mixed/0 weakens. Promoted candidate -> provisional.
  epistemic_category: standard
  depends_on:
    - ARC-016
    - ARC-005
    - ARC-015
    - ARC-021
    - INV-012
    - ARC-044
  location: docs/architecture/modes_of_cognition.md#mech-025
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: a fair test requires (a) the three instrument defects
    fixed for V3-EXQ-876 to stay fixed (SelectionResult.committed read, not the stale
    cache; agent.update_residue() called every eval tick so running_variance is live;
    well-sampled committed AND uncommitted windows, non-degenerate per seed) AND (b) the
    doing-mode-delta DV re-operationalized so a negative/null result can be trusted as
    real rather than an artifact. V3-EXQ-876 (2026-08-02) cleared (a) cleanly but its DV
    choice is now suspect on two grounds from the claim's own literature: Friston 2013
    records precision as continuous, not a discrete mode switch, while the harm-
    counterfactual-gap DV tests a discrete committed-vs-uncommitted split; and Thura &
    Cisek 2014 predicts CONVERGENCE (narrowing) of premotor/M1 representations during
    commitment -- the opposite sign from what the DV's C1 criterion assumed. A valid
    re-operationalization must capture context-dependent precision modulation during
    genuinely committed episodes without conflating it with a downstream harm-
    differentiation proxy whose predicted sign the claim's own literature disputes --
    either by testing the reversed sign directly, or by gating on precision itself
    (precision_committed_mean vs precision_uncommitted_mean, already recorded in
    V3-EXQ-876 and separating by 4-5 orders of magnitude in every seed, but never scored
    as a criterion).

    CONFIRMING: once re-operationalized, HIGH_CAUSAL/committed conditions show a
    precision lift over LOW_CAUSAL/uncommitted conditions in the predicted direction,
    exceeding a pre-registered margin (the claim's history sets 0.05 as the prior bar
    from V2's slope criterion, though a direct precision-ratio criterion should set its
    own bar given the much larger separation already observed).

    FALSIFYING: even after a GENUINE re-operationalization (not a re-run of the same
    DV), the result stays flat or wrong-signed under the same non-degenerate
    preconditions -- this is now a REAL falsification, not a second measurement-defect
    pass. MECH-025 has already used one "instrument defect, not evidence against"
    exemption (V3-EXQ-876); a second wrong-sign or null result after the DV itself has
    been changed to address the sign-mismatch critique should be read as real claim
    pressure. Distinguish "still a DV problem" from "now genuinely falsified" by whether
    the new DV's own positive-control gates (precision separation, or whichever direct
    signal the redesign gates on) clear cleanly: if they clear and the predicted
    relationship still fails, that is falsification; if the gates themselves fail to
    clear, that is a substrate/instrument problem warranting a genuine third fix, not
    accepting the null.

    GOVERNANCE FLAG (2026-08-08, not resolved here): the 2026-08-03 autopsy's routing
    to /queue-experiment for this redesign has not been queued or chipped 5 days later.
  source:
    - docs/thoughts/2026-02-08_modes_of_cognition_control_plane_regimes.md
- id: MECH-026
  title: "Ready vigilance primes restraint under high sensitivity without action."
  claim_type: mechanism_hypothesis
  subject: cognitive_modes.ready_vigilance
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  evidence_quality_note: |
    AROUSAL REGULATOR NEEDED (2026-04-02): Literature (Langner & Eickhoff 2013 meta-analysis,
    Oken et al. 2006) establishes that vigilance = high arousal + suppressed motor output.
    The LC-NE inverted-U curve means optimal vigilance requires a SPECIFIC sensitivity level,
    not maximum. Setting precision too high produces hypervigilance (false alarms). MECH-026's
    ready mode needs an arousal regulator that finds optimal sensitivity, not a binary switch.
    MECH-093 (z_beta modulates E3 heartbeat frequency) may be the implementation mechanism:
    heartbeat rate IS the arousal parameter, expressed as temporal sampling frequency rather
    than gain scalar. See MECH-161 for the specific arousal regulator claim.
    MISSING CONSTRAINT -- FATIGUE (2026-04-02): Biological vigilance degrades over time as
    right PFC activation wanes (the vigilance decrement). REE's computational precision
    parameter has no fatigue and no metabolic cost. This is a missing constraint. Whether
    the cost of vigilance is informationally relevant (the decrement is a feature of
    finite-resource systems) or merely a biological limitation (REE can simply maintain
    steady precision) is an open question. Biology suggests the decrement is associated
    with attentional lapses that serve an information-gathering function (exploring vs
    exploiting attention allocation). ARC-044 may subsume this.
  epistemic_category: substrate_conditional
  depends_on:
    - ARC-016
    - ARC-005
    - MECH-093
    - MECH-161
    - ARC-044
  location: docs/architecture/modes_of_cognition.md#mech-026
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: MECH-161's arousal regulator -- a controller that tunes
    MECH-093's heartbeat-rate channel (z_beta -> e3_steps_per_tick) to a genuine
    inverted-U optimum on the LC-NE curve, rather than treating sensitivity as a binary
    high/low switch -- must be built and confirmed live before this claim is fairly
    testable. Current state: UNBUILT, not merely untested (MECH-161's controller is
    described only as something that "could be implemented as" a z_beta-gated
    controller; no such controller exists in ree_core/). MECH-093's rate-modulation
    channel itself is confirmed live (V3-EXQ-097b/396b/505 PASS). The two experiments
    run against MECH-026 to date (V3-EXQ-067, superseded by V3-EXQ-202, FAIL/unknown
    direction) tested a cruder proxy -- elevated E3 response near a fixed hazard-
    density/proximity condition -- not a manipulated, regulator-tuned sensitivity level,
    so they do not constitute a fair test of the claim as stated.

    CONFIRMING: once MECH-161's regulator exists and converges to a stable non-maximal
    optimum, agents run at that regulator-tuned sensitivity level show measurable
    restraint (suppressed motor commitment / non-action) co-occurring with elevated
    detection/precision signal, distinguishable from both passivity (low sensitivity, no
    restraint-signal) and hypervigilance (sensitivity above the regulator's optimum,
    where restraint breaks down into false-alarm-driven action, per MECH-027).

    FALSIFYING: once built, the regulator either finds no stable non-maximal optimum
    (sensitivity and restraint trade off linearly/monotonically), or the tuned regime
    shows sensitivity and restraint are not actually coupled the way the claim predicts.

    OPEN, NOT RESOLVED HERE: whether a fatigue/vigilance-decrement constraint is
    informationally relevant (a feature of finite-resource systems worth modelling) or
    merely a biological limitation REE can dispense with is a genuinely undecided
    sub-question this claim's own note poses rather than answers. A future test of the
    core restraint/sensitivity coupling need not wait on it, but the question should be
    revisited once the regulator itself is built (ARC-044 may subsume it).

    GOVERNANCE FLAG (2026-08-08, not resolved here): status: provisional /
    overall_confidence 0.701 looks lit-heavy rather than substrate-backed
    (literature_confidence 0.843 vs experimental_confidence 0.275, one FAIL run whose
    direction is "unknown" and which did not test the mechanism as specified) -- worth a
    governance look at whether the confidence figure is misleading.
  source:
    - docs/thoughts/2026-02-08_modes_of_cognition_control_plane_regimes.md
- id: MECH-027
  title: "Pathological modes reflect mis-tuned control-plane regimes."
  claim_type: mechanism_hypothesis
  subject: cognitive_modes.pathological_regimes
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-016
    - ARC-005
    - ARC-015
  what_would_answer: |
    UNTESTED (2026-08-06 digestion pass): MECH-027 has no dedicated V2 or V3
    experiment lineage, no evidence_quality_note, and no prior runs of any kind
    -- this is a from-scratch falsifier draft against the architecture text
    only (docs/architecture/modes_of_cognition.md#mech-027), not a synthesis of
    contested results.
    NON-DEGENERACY PRECONDITION: the claim requires the five named control-plane
    channels (gain/precision, prediction horizon, replay/hippocampal-injection
    suppression, learning eligibility, hippocampal gating) to be independently
    measurable AND independently perturbable in the substrate, with each showing
    non-degenerate variance across the pathological/non-pathological range under
    test. A run where any channel is flat, hardcoded, or torn down at read time
    should self-route substrate_not_ready, not count as a verdict. This is not
    hypothetical: MECH-025 -- which shares MECH-027's ARC-016/ARC-005 substrate
    dependency -- had three consecutive false-negative instrument defects on
    exactly this kind of read (hardcoded E3 precision; missing self-attribution
    channel per SD-003/SD-004; cached/torn-down commitment-state field; frozen
    running_variance during eval), only resolved as of the 2026-08-03 V3-EXQ-876
    fair test (failure_autopsy_mech025-cluster-876-671b_2026-08-03). Confirm the
    same instrumentation fixes carry over to MECH-027's control-plane reads
    before treating any MECH-027 run as informative -- do not re-run into the
    identical defect class.
    CONFIRMING signature: for each named pathological label, pushing ONLY the
    control-plane channel(s) the architecture text names for it (hypervigilance
    = elevated gain + shortened horizon + suppressed replay; dissociation =
    weakened self-impact attribution + disrupted binding; rumination = elevated
    hippocampal replay without control-plane resolution; mania = elevated
    precision + learning eligibility without constraint; psychosis-like =
    failure to suppress internally-generated hypotheses) out of its normal
    operating range -- with no other mechanism or module added -- reproduces the
    qualitative behavioral signature of that pathology (e.g. excessive
    false-alarm / over-reactive responding for hypervigilance; non-convergent,
    perseverative internal simulation for rumination), AND the behavior reverts
    when the channel is returned to its normal range. Reproducibility across the
    five labels via channel retuning alone, with no structural change required,
    supports "not separate mechanisms; mis-tuned control regimes."
    FALSIFYING signature: a pathological behavior pattern emerges that (a) does
    NOT track the specific channel(s) the architecture text assigns to it (e.g.
    extreme gain does not produce hypervigilance-like false alarms, or rumination
    only appears when something beyond hippocampal replay volume changes), or
    (b) persists after every named channel is returned to baseline -- indicating
    a structurally distinct mechanism or an off-manifold representational change
    outside the control-plane's five channels is doing the work. Either result
    would mean pathological modes are not reducible to mis-tuned regimes,
    falsifying MECH-027 (and weakening the "ethics/stability/mental-health are
    co-regulated properties of the same control-plane machinery" claim it
    grounds).
    Because no run exists yet, there is no mixed-evidence tension to report --
    this falsifier is prospective only.
  location: docs/architecture/modes_of_cognition.md#mech-027
  source:
    - docs/thoughts/2026-02-08_modes_of_cognition_control_plane_regimes.md
- id: MECH-028
  title: "Ethical behavior depends on mode transitions and learning preservation."
  claim_type: mechanism_hypothesis
  subject: cognitive_modes.ethical_transitions
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-016
    - ARC-005
    - ARC-015
    - INV-012
  location: docs/architecture/modes_of_cognition.md#mech-028
  source:
    - docs/thoughts/2026-02-08_modes_of_cognition_control_plane_regimes.md
- id: MECH-029
  title: "Reflective/DMN mode supports moral evaluation of hippocampally-generated replay trajectories."
  claim_type: mechanism_hypothesis
  subject: default_mode.reflective_ethics
  polarity: asserts
  status: provisional
  epistemic_category: standard
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: This test is only informative if all four hold
    (V3-EXQ-065/080/201 all failed this precondition; V3-EXQ-876/671b on
    2026-08-02 confirm the substrate CAN now clear it on the identical
    mechanism): (1) both regimes non-degenerate per seed (uncommitted_step_count
    >= 20 AND committed_step_count >= 20), obtained by reading E3's live
    SelectionResult.committed (never the stale _committed_candidates cache or
    torn-down _committed_trajectory), calling agent.update_residue() every eval
    tick, and feeding BreathOscillator.sweep_threshold_reduction directly into
    the same agent.e3.select() call. (2) z_goal substrate genuinely active for
    the episode. (3) E3 harm/benefit evaluation non-degenerate (harm_pred_std
    >= 0.02). (4) Hippocampal replay-trajectory generation active and
    observable during the uncommitted window.

    CAUTION: the sibling MECH-025/MECH-025b cluster used this identical fixed
    instrumentation and both FAILed on downstream sign/operationalization
    grounds, not substrate grounds -- MECH-029's redesign should reason from
    its own generation/evaluation reframe below, not copy MECH-025's DV by
    analogy.

    CONFIRMING: during genuine uncommitted-mode windows, when the hippocampal
    module generates multiple candidate replay trajectories, E3's harm/benefit
    evaluation shows measurably GREATER differentiation across those candidates
    than during committed/doing-mode windows.

    FALSIFYING (VIOLATED BY): E3's harm/benefit differentiation is equal or
    SMALLER during uncommitted windows (mirrors the confirmed MECH-025 sibling
    finding -- opposite the naively predicted direction); or no measurable
    relationship between uncommitted-window occupancy and evaluative
    differentiation (mirrors the MECH-025b sibling null); or hippocampal
    trajectory generation is itself degenerate during uncommitted windows.

    CURRENCY UPDATE (2026-08-08 digestion pass): the note below's "REVISIT
    AFTER z_goal SUBSTRATE IS ACTIVE" condition is now satisfied -- z_goal is
    confirmed live and extensively wired (MECH-117 graduated to stable); the
    commit-threshold deadlock was fixed 2026-04-02 (299e540) and independently
    reconfirmed non-degenerate in production on 2026-08-02 (V3-EXQ-876/671b,
    same mechanism family). Recategorized from the note's implied
    substrate-blocked reading to standard/testable-now accordingly.
  evidence_quality_note: |
    EXQ-065 FAIL (2026-03-23): passive observation -- agent stayed locked in
    committed mode throughout eval (uncommitted_step_count=0). No uncommitted
    state observed. Threshold (commit_threshold=0.40) never crossed by running_variance.
    EXQ-080 FAIL (2026-03-23): BreathOscillator active -- C1 PASS (z_world_var_ratio=1.173,
    sweep > inter), committed_rate=0.0 in both windows, harm_pred_std~=0 (degenerate).
    The oscillator creates variance differences in z_world (good) but committed_rate=0
    (threshold calibration issue) and E3 harm_eval output is constant (cannot express
    window differentiation). REVISIT AFTER z_goal SUBSTRATE IS ACTIVE. Root cause:
    without a positive goal, the agent has nothing to commit TO. Natural commitment
    episodes require goal-directed trajectories. EXQ-074b/076 test the wanting/goal
    substrate; MECH-029 redesign should follow once those confirm z_goal is working.
    The BreathOscillator mechanism (variance difference C1 PASS) has real signal;
    it is the commitment detection and E3 harm_eval sensitivity that need work first.
    EXQ-201 FAIL (2026-04-02): improved BreathOscillator (period=50, amp=0.3, 500 warmup
    eps) -- uncommitted_steps=0 for both seeds [42, 123] throughout all 50 eval eps x
    200 steps. Same substrate limitation as EXQ-065/080: agent cannot enter uncommitted
    mode under any BreathOscillator configuration tested. evidence_direction corrected
    to unknown (scoring_excluded: substrate_limitation). This is not evidence against
    MECH-029 -- the claim requires a substrate capable of generating uncommitted-mode
    windows, which the current V3 architecture cannot produce. All three experiments
    are substrate-limited and non-informative about the claim.
    CLARIFICATION (2026-04-02): Literature (Liu et al. 2024, Yoder & Decety 2019)
    suggests the DMN's moral engagement is primarily evaluative (judging outcomes of
    imagined scenarios), not generative (proposing the scenarios). The hippocampus
    generates replay trajectories; DMN/E3 evaluates them for ethical content. This
    actually fits REE's architecture better than the original "gated replay" framing:
    the hippocampal module generates candidate trajectories, E3 scores them for harm/
    benefit. Title updated to reflect this generation/evaluation distinction.
  depends_on:
    - ARC-014
    - ARC-005
    - ARC-007
  location: docs/architecture/default_mode.md#mech-029
  source:
    - docs/thoughts/2026-02-08_modes_of_cognition_control_plane_regimes.md
- id: MECH-030
  title: "Sleep modes consolidate learning and ethical residue across regimes."
  claim_type: mechanism_hypothesis
  subject: sleep.modes_consolidation
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  evidence_quality_note: |
    EXQ-127 FAIL (2026-03-29): superseded -- sleep not implemented in V3; post-training
    phase simulation does not test this claim. Manifest marked superseded. Claim remains
    provisional pending V4 sleep substrate implementation.
  depends_on:
    - ARC-011
    - ARC-005
    - ARC-007
    - INV-010
    - MECH-092
    - MECH-094
    - MECH-120
    - MECH-121
    - MECH-122
    - MECH-123
  location: docs/architecture/sleep.md#mech-030
  source:
    - docs/thoughts/2026-02-08_modes_of_cognition_control_plane_regimes.md
    - docs/thoughts/2026-02-23_some_subjective_experience_mapping.md
  notes: >
    Offline consolidation mode is multi-phase. Four sub-phases are now architecturally
    specified (all V4 scope): MECH-120 (SWS denoising/attractor flattening, Tononi SHY),
    MECH-121 (NREM SWR replay and episodic->semantic transfer, Diekelmann), MECH-122
    (spindles as packaging/gating mechanism), MECH-123 (REM precision prior
    recalibration, Hobson/Friston). All sub-phases require MECH-094 hypothesis tag as
    hard prerequisite to prevent harm trace over-amplification (MECH-124).
    V3 prerequisites for V4 consolidation: MECH-092 quiescent replay validated;
    MECH-094 hypothesis tag working; D_eff monitoring (MECH-113) with stable setpoint;
    z_goal representation (MECH-112, 116) validated; multi-rate execution (SD-006, EXQ-052b
    PASS); ThetaBuffer bidirectional design (currently waking-direction only, MECH-122
    requires sleep-direction). See docs/architecture/v3_v4_transition_boundary.md.
    Literature: Tononi & Cirelli 2006 (SHY), Diekelmann & Born 2010 (SWR consolidation),
    Hobson & Friston 2012 (REM precision recalibration), Walker & Stickgold 2004
    (maladaptive consolidation / PTSD model -> MECH-124). Evidence confidence: 0.79-0.88.
    Updated 2026-03-23 to add sub-phase claims.
  what_would_answer: |
    SUBSTRATE PRECONDITION: this is the PARENT claim for four sub-phase
    mechanisms (MECH-120 SWS denoising, MECH-121 NREM SWR replay, MECH-122
    spindle packaging, MECH-123 REM precision recalibration -- each now
    carries its own what_would_answer with its own preconditions and
    signatures; this wwa targets the COMPOSITE claim, not any single
    sub-phase). Requires ALL of: MECH-092 quiescent replay validated;
    MECH-094 hypothesis tag working; D_eff monitoring (MECH-113) with a
    stable setpoint; z_goal representation (MECH-112/116) validated;
    multi-rate execution (SD-006, already PASSED per EXQ-052b); ThetaBuffer
    extended to the sleep direction (currently waking-direction only,
    MECH-122 requires the reverse). V4 scope; not V3-tractable as a whole.
    CONFIRMING signature: across a full sleep cycle in sequence, (a) each
    sub-phase's own confirming signature holds (see MECH-120/121/122/123
    individually), AND (b) the composite effect is genuinely MULTI-PHASE
    rather than reducible to any single sub-phase alone -- running only
    one sub-phase in isolation (e.g. SWS denoising without NREM replay)
    produces a measurably WORSE outcome (consolidation quality,
    behavioural diversity preservation, or residue/ethical-trace
    handling) than the full sequence, establishing the phases are jointly
    load-bearing rather than redundant.
    FALSIFYING: a single sub-phase (e.g. MECH-121 NREM replay alone)
    reproduces the SAME consolidation quality as the full multi-phase
    sequence -- the other phases would be shown non-load-bearing for the
    composite claim even if individually confirmed in isolation; or the
    full sequence performs WORSE than a single-phase baseline (phase
    interactions destructive, not synergistic) -- falsifies "consolidate
    ... across regimes" (regimes fighting rather than cooperating).
    NOTE: EXQ-127 (2026-03-29) attempted this in V3 without a real sleep
    substrate and was correctly superseded/non-informative -- do not
    re-attempt without the V4 substrate above in place.
- id: MECH-031
  title: "Derived social tags and empathy coupling via control-plane knobs."
  claim_type: mechanism_hypothesis
  subject: social.other_selflike_empathy_coupling
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-010
    - ARC-005
    - ARC-017
    - ARC-015
    - ARC-006
    - INV-005
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: requires reliable object persistence/
    binding to exist (an entity must be trackable as a stable target
    before AGENCY/OTHER_SELFLIKE tags can attach to it -- see
    entities_and_binding.md) and the derived tags must show non-zero
    cross-condition variance -- an agent that tags everything or nothing
    as OTHER_SELFLIKE cannot discriminate the mechanism.
    CONFIRMING signature: the agent correctly discriminates interveners
    from passive dynamics (AGENCY tag) using only the existing
    self/world/impact machinery -- no dedicated social-cognition module
    -- and assigns OTHER_SELFLIKE probability that tracks whether a
    target genuinely runs a self/world-separation and self-impact loop
    (higher for agents exhibiting boundary-maintaining, error-correcting
    behaviour; lower for passive/mechanical dynamics). Downstream empathy
    coupling (MECH-036's other-harm veto threshold, gated by
    lambda_empathy/v_other_veto) should then correctly scale with
    OTHER_SELFLIKE probability -- higher-confidence targets produce
    stronger empathy coupling.
    FALSIFYING: AGENCY/OTHER_SELFLIKE tags require a dedicated
    social-cognition module to reproduce (the reused self/world/impact
    machinery is insufficient) -- falsifies the "derived, not separate
    module" framing; or OTHER_SELFLIKE probability does not track genuine
    self-model possession (e.g. equally high for a scripted NPC with no
    self/world loop as for a genuinely self-modelling agent) -- falsifies
    the tag as measuring the intended construct rather than a confound
    such as mere movement complexity.
  location: docs/architecture/social.md#mech-031
  source:
    - docs/thoughts/2026-02-09_empathy.md
- id: MECH-032
  title: "OTHER_SELFLIKE detection is biased toward high recall to avoid empathy false negatives."
  claim_type: mechanism_hypothesis
  subject: social.other_selflike_high_recall_bias
  polarity: asserts
  status: provisional
  epistemic_category: substrate_conditional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-031
    - ARC-010
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: requires (1) a working OTHER_SELFLIKE
    classifier with a genuine, quantifiable decision boundary, and (2) a stream
    of ambiguous, near-boundary stimuli. Confirmed absent as of 2026-08-08: grep
    of ree_core/ for "OTHER_SELFLIKE"/"self_like"/"selflike" returns zero
    matches anywhere in ree-v3 -- MECH-031 (the base tagging mechanism this
    claim qualifies) has no implementation, and its own prerequisite ARC-010
    (social cognition/mirror modelling) is confirmed unimplemented and gated to
    implementation_phase v5.

    CONFIRMING: on a held-out set of ambiguous/borderline stimuli, the
    classifier's false-positive rate on ambiguous non-self-like stimuli is
    measurably higher than its false-negative rate on ambiguous self-like
    stimuli, AND this asymmetry is traceable to an explicit, inspectable
    threshold/weighting choice.

    FALSIFYING (VIOLATED BY): the classifier's decision threshold is symmetric
    or unbiased; OR the threshold is biased the OTHER way, favoring precision;
    OR there is no tunable decision boundary at all; OR the claimed bias exists
    only as an unenforced code comment/design note with no measurable effect.
  location: docs/architecture/social.md#mech-032
  source:
    - docs/architecture/social.md
- id: MECH-033
  evidence_quality_note: |
    [multi_sense_audit_2026_05_08] EXQ-124 evidence superseded 2026-05-08 due to warmup loop calls env.reset() inside the per-step inner loop, corrupting E2 world_forward training (E2 is taught z_world_t + a -> z_world_post_reset, a meaningless mapping); the KERNEL_CHAIN_ON arm uses the corrupted E2 for CEM kernels. Awaiting StepHarness re-run.

    V2 experiment EXQ-022 (kernel_chaining_interface, EVB-0016) FAIL — run twice (2026-03-08,
    2026-03-15), consistent result. WITH_CHAIN (full E2→hippocampus→E3 pipeline) only 1.8%
    better than NO_CHAIN (random action selection); random agent improves faster on slope.
    Root cause: V2 E2 produces z_gamma sensory transitions, not action-consequence objects.
    Chaining sensory predictions into rollouts provides negligible signal to E3 — the interface
    exists but passes the wrong type across it. Structural failure, not a tuning issue.
    V3 EXQ-055 PASS (2026-03-20): action-object chaining vs self-only vs random — AO reduces
    harm 67× vs self-only (harm_ao=0.00147 vs harm_self=0.0684), cal_gap_ao=0.734 vs
    cal_gap_self=0.021. All 5 criteria met. V3 z_world action-object E2 is the correct primitive.
    v3_pending cleared 2026-03-20.
    EXQ-124 FAIL (2026-03-28): harm_rate_CHAIN=0.621 vs ABLATED=0.057 (~10x worse with kernel
    chain). Training calibration issue: 400-episode warmup insufficient, E2 world_forward not
    well trained, terrain prior not calibrated for kernel-chain conditions. Not fundamental
    failure of the claim -- prior EXQ-055 PASS remains valid (different baseline). Experiment
    design problem. Literature pull on hippocampal planning queued (Pfeiffer & Foster 2013;
    Wikenheiser & Redish 2015).
    EXQ-171 PASS 4/4 x2 (2026-03-30, two independent runs): kernel chain confirmed.
    harm_rate_CHAIN=0.00021 vs ABLATED=0.0613 (290x safer). world_forward_r2=0.984.
    harm_reduction_delta=0.0611 >> 0.01 threshold. Both seeds, both runs. EXQ-124 training-budget
    FAIL superseded -- 600-ep warmup sufficient. EXQ-055 + EXQ-171x2 = three clean PASS entries.
    Evidence_direction: supports.
    EXQ-184 PASS 3/3 seeds (2026-04-04): Independent replication. harm_rate_CHAIN=0.00021
    vs NO_CHAIN=0.0608 (290x safer). world_forward_r2=0.987. harm_reduction_delta=0.0606.
    All 4 criteria met across all seeds. Third clean PASS; evidence strong.
    EXQ-308 PASS 3/3 seeds (2026-04-09, governance 2026-04-09-c): V3 current-substrate
    discriminative pair (SD-018 resource proximity + drive_weight=2.0). KERNEL_CHAIN
    harm_rate=0.000301 vs NO_CHAIN=0.0608 (~200x safer). world_forward_r2=0.961.
    resource_prox_r2=0.783. harm_reduction_delta=0.0605. All criteria pass all seeds.
    Extends EXQ-184 into current substrate defaults; V2 EXQ-022 FAIL fully explained
    by absent z_world/action-object primitive and inadequate training. MECH-033 is
    now replicated 4x PASS across multiple substrate generations.
  title: "E2 forward-prediction kernels seed hippocampal rollouts."
  claim_type: mechanism_hypothesis
  subject: hippocampus.kernel_chaining_interface
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_308_mech033_kernel_chain_discriminative_20260409T183908Z_v3
      as_of: 2026-04-09
      verdict: supports/PASS
  last_reviewed: 2026-02-15
  lifecycle_stage: adjudicated
  adjudication_outcome: retain_ree
  adjudicated_at_utc: 2026-02-15T18:46:42.773429Z
  adjudication_decision_status: applied
  adjudication_recommendation: retain_ree
  v3_pending: false
  depends_on:
    - ARC-018
    - ARC-002
    - ARC-001
    - ARC-005
    - ARC-021
  location: docs/architecture/hippocampal_systems.md#mech-033
  source:
    - docs/thoughts/2026-02-09_e2_hpc_interface.md
- id: MECH-034
  title: "Viability mapping updates are distinct from residue curvature updates."
  claim_type: mechanism_hypothesis
  subject: residue.viability_vs_curvature_updates
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-013
    - ARC-018
  location: docs/architecture/residue_geometry.md#mech-034
  source:
    - docs/thoughts/2026-02-09_viability_mapping_vs_residue.md
- id: MECH-035
  title: "VALENCE is vector-valued and ranked without scalar collapse."
  claim_type: mechanism_hypothesis
  subject: valence.vector_ranking
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-017
    - ARC-003
    - ARC-005
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: requires the named VALENCE component
    streams (HOMEOSTASIS, HARM, TEMPORAL_COHERENCE, SELF_IMPACT error,
    option volume, inferred other welfare) to be independently
    computable per candidate trajectory and to show non-zero
    cross-candidate variance on at least two components simultaneously
    -- a scenario where only one component ever varies (the others are
    flat or degenerate) cannot distinguish vector-valued ranking from a
    scalar collapsed onto that one live component, and should
    self-route substrate_not_ready rather than count as a verdict. No
    prior experimental run has targeted this claim as of 2026-08-06; a
    first test is establishing evidence, not re-litigating a null.
    CONFIRMING signature: trajectory selection outcomes are explained
    by constraint-first / Pareto-or-lexicographic comparison over the
    component vector -- i.e. there exist candidate pairs where no
    single scalar combination (weighted sum, fixed linear blend) of the
    components reproduces the system's ranking, but a
    dominance/lexicographic rule over the raw vector does; and
    trajectories that are dominated on one stream (e.g. higher HARM)
    are rejected even when a competing scalar-weighted blend would
    prefer them for gains elsewhere (e.g. higher option volume).
    FALSIFYING: the observed rankings are fully reproducible by some
    fixed scalar weighting of the component streams (i.e. a single
    linear or monotonic combination fits the choice behaviour as well
    as the vector/Pareto rule does) -- this would indicate VALENCE is
    functioning as a de facto scalar reward regardless of its vector
    representation, and would refute the "no scalar collapse" claim
    even if the underlying data structure remains vector-shaped.
  location: docs/architecture/sensory_stream_tags.md#mech-035
  source:
    - docs/thoughts/2026-02-09_valence_vector.md
- id: MECH-036
  title: "Other-harm triggers veto only under high-certainty catastrophic outcomes."
  claim_type: mechanism_hypothesis
  subject: social.other_harm_veto_threshold
  polarity: asserts
  status: provisional
  epistemic_category: substrate_conditional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-031
    - ARC-005
    - INV-005
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: Requires (1) a functioning OTHER_SELFLIKE / other-agent
    detection substrate (MECH-031, gated on ARC-010 -> ARC-047's SocialGridWorld,
    implementation_phase v4; confirmed zero hits for "OTHER_SELFLIKE"/"self_like"/"selflike"
    anywhere under ree_core/ as of 2026-08-08) actually instantiated and producing a
    distinguishable "other" tag for cost/veto logic to condition on, and (2) a discrete
    hard-veto/action-block primitive -- as opposed to REE's existing continuous PE-weighted
    control-demand signal (E2_harm_a/E2_harm_s -> dACC, or a MECH-127-style soft
    other_cost_loss term) -- gated on a certainty x severity threshold. Confirmed: no
    "veto"/"hard_block" harm logic and no v_other_veto/lambda_empathy parameters exist
    anywhere in ree_core/ today, for self- or other-directed harm.

    CONFIRMING: In SocialGridWorld (or equivalent), with OTHER_SELFLIKE tagging live,
    instantiate a threshold-gated hard veto that fires only when predicted-other-harm
    certainty exceeds a high threshold AND predicted severity is catastrophic/irreversible,
    while lower-certainty/severity predictions instead lower rank without exclusion.
    Confirming pattern: catastrophic-high-certainty trials show a hard behavioral floor
    (veto-eligible actions selected at ~0%) qualitatively discontinuous from low-certainty
    trials, where the same actions are selected at reduced but nonzero frequency.

    FALSIFYING (VIOLATED BY): (1) the "veto" and "ranking" pathways behave identically
    (collapses into MECH-127-style soft coupling only); (2) a hard veto fires on
    low-certainty/non-catastrophic predictions (over-triggering); (3) high-certainty
    catastrophic other-harm produces no hard behavioral floor even after extended training
    (under-triggering); (4) the same threshold-veto behavior emerges from self-harm-only
    signals with no distinguishable "other" representation (falsifies social-specificity).
  location: docs/architecture/social.md#mech-036
  source:
    - docs/thoughts/2026-02-09_other_harm_gating.md
- id: MECH-037
  title: "Papez-like loop provides provenance gating and reality filtering."
  claim_type: mechanism_hypothesis
  subject: provenance.papez_like_reality_filtering
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-007
    - ARC-018
    - ARC-003
    - ARC-005
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: requires (a) E1-generated candidate content,
    (b) a hippocampal trace / temporal-ordering signal, and (c) the E3
    commitment decision to each be separately instrumented and measurable,
    with the trace/ordering signal varying (present vs absent/weak) across
    conditions rather than held constant -- a run where every candidate
    carries the same trace support cannot discriminate a working provenance
    gate from no gate at all. This claim currently has no V3 substrate or
    experiment built against it (no runs, no evidence_quality_note on file);
    a run attempting to test it before the provenance-gating pathway
    (hippocampal trace signal -> precision down-weighting -> E3 commitment
    licensing) actually exists in the substrate should self-route
    substrate_not_ready rather than count as a null result.
    CONFIRMING signature: E1 content lacking hippocampal trace / temporal-
    ordering support is measurably held at lower precision and is licensed
    for E3 commitment at a lower rate than trace-supported content of
    comparable quality; and degrading or disabling the gate (removing the
    trace/ordering signal's influence on precision/commitment) produces a
    measurable increase in high-confidence commitments of ungrounded
    content -- i.e. confabulation-like failures track gate integrity in the
    predicted direction. The gate should amplify/suppress candidate
    trajectories without minting authority writes (POL/ID/CAPS) or
    bypassing E3/verifier commit checks; a version that achieves filtering
    only by bypassing the verifier would not confirm this claim's specific
    mechanism.
    FALSIFYING: E1 content commits to E3 at similar rates and precision
    regardless of hippocampal trace / temporal-ordering support (the
    presence or absence of provenance signal has no measurable effect on
    commitment eligibility); or disabling/degrading the gate produces no
    measurable increase in high-confidence, ungrounded commitments relative
    to the gate-intact condition -- either would falsify the "Papez-like
    loop functions as a provenance gate / reality filter" framing and
    suggest hippocampal trace signals are not actually operating as a
    commitment gate in REE's implementation, whatever role they may play
    elsewhere.
  location: docs/architecture/papez_circuit.md#mech-037
  source:
    - docs/thoughts/2026-02-09_papez_circuit_reality_filtering.md
- id: MECH-038
  title: "Arcuate-like sequence-to-motor channel nudges language emergence."
  claim_type: mechanism_hypothesis
  subject: language.arcuate_fasciculus_nudges
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-009
    - ARC-002
    - ARC-018
    - ARC-005
  location: docs/architecture/arcuate_fasciculus.md#mech-038
  source:
    - docs/thoughts/2026-02-09_arcuate_fasciculus_language_nudges.md
- id: MECH-039
  title: "Modes are stable regions in control-channel space, not separate modules."
  claim_type: mechanism_hypothesis
  subject: control_plane.channel_mode_landscape
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-005
    - ARC-016
    - MECH-019
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: requires the named control channels
    (arousal baseline/volatility, action readiness/motor gating,
    precision/gain routing, commitment threshold/interruptibility,
    replay/learning scheduling, hard veto/interrupt threshold) to be
    independently measurable and to show non-zero cross-condition
    variance -- a run where all channels move in lockstep cannot
    discriminate "stable region in a continuous space" from "discrete
    module."
    CONFIRMING signature: named modes (task-engaged, default-mode-like,
    emergency) correspond to distinct, STABLE clusters/regions in the
    joint channel space (e.g. task-engaged = high readiness + elevated
    precision; default-mode-like = low readiness + high replay + low
    precision; emergency = high arousal + high readiness + high veto),
    with transitions between modes traceable as a CONTINUOUS trajectory
    through channel space (gradual co-movement of the relevant channels)
    rather than an instantaneous discrete flag flip -- EXCEPT for
    hard-veto interrupts, which this claim's own text predicts should
    look like a fast, forced transition overriding the rest of the
    channel state.
    FALSIFYING: the named "modes" do not correspond to any stable
    clustering in channel space (channel values wander freely without
    forming recognisable regions); or transitions between modes are
    measurably instantaneous/discrete across ALL channels simultaneously
    (indistinguishable from a one-hot module switch) rather than showing
    channel-by-channel graded movement -- would falsify the "continuous
    space, not separate modules" framing and support a discrete-module
    reading instead.
  location: docs/architecture/control_plane.md#mech-039
  source:
    - docs/architecture/control_plane.md
- id: MECH-040
  title: "Safety baseline and volatility are distinct control channels for arousal/readiness."
  claim_type: mechanism_hypothesis
  subject: control_plane.safety_baseline_volatility
  polarity: asserts
  status: provisional
  epistemic_category: standard
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-040@2026-02-15T18:46:42.773429Z"
      as_of: 2026-02-15
      verdict: retain_ree/applied
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: the test window must exercise BOTH SD-037's
    sustained_threat signal and Q-007's volatility_estimate through genuinely INDEPENDENT
    manipulations -- a sustained low-magnitude harm floor (moves sustained_threat, holds
    PE variance flat) crossed factorially with a bursty/oscillating PE regime (moves
    volatility_estimate, holds z_harm mean flat). Both use_phasic_burst and the SD-037
    broadcast-override regulator must be enabled -- both are bit-identical OFF by default.

    CONFIRMING: the two channels show distinguishable downstream footprints -- a
    low-sustained-threat/high-volatility cell moves volatility-driven consumers (SD-069
    burst envelope, softmax temperature) while leaving override_signal/freeze-gate near
    baseline, and the mirror-image pattern for the reverse cell. A combined cell should
    show a non-strictly-multiplicative combination consistent with the claim's "OR"
    framing. Would extend MECH-063 sub-claim ii's already-landed tonic/phasic dissociation
    to this axis pair.

    FALSIFYING (VIOLATED BY): the two manipulations produce statistically
    indistinguishable downstream footprints -- driving either signal moves the same
    consumers, with no channel-selective effect. Would indicate the substrate's nominally
    separate computations collapse onto a single "how bad is it right now" scalar --
    effectively the conflated threat-level scalar MECH-040 was written to rule out.

    Provenance note: MECH-104 is the already-active, PASS-tested narrower sibling covering
    only the volatility-triggers-de-commitment sub-mechanism; MECH-040 is the general
    channel-separation claim and remains untested on the sustained-threat side.
  last_reviewed: 2026-02-15
  lifecycle_stage: adjudicated
  adjudication_outcome: retain_ree
  adjudicated_at_utc: 2026-02-15T18:46:42.773429Z
  adjudication_decision_status: applied
  adjudication_recommendation: retain_ree
  evidence_quality_note: |
    No experimental evidence in current genuine corpus. All prior entries (9 runs,
    synthetic scaffolding) originated from ree-v2/ree-experiments-lab (archived 2026-02-26)
    and have been removed from the index. The demotion recommendation in
    promotion_demotion_recommendations.md (conflict_ratio=0.889, conf=0.55) is stale —
    it was computed from those invalid entries, which no longer exist.
    Retain provisional (adjudication retain_ree is valid as architectural decision) pending
    genuine ree-v1-minimal experiments. Do not act on demotion recommendation until
    genuine experimental evidence is available for this claim.
  depends_on:
    - ARC-005
    - MECH-005
    - MECH-019
  location: docs/architecture/control_plane.md#mech-040
  source:
    - docs/architecture/control_plane.md
- id: MECH-041
  title: "Affective expression broadcasts control-plane regime to reduce social prediction load."
  claim_type: mechanism_hypothesis
  subject: social.affective_expression_mode_broadcast
  polarity: asserts
  status: provisional
  epistemic_category: substrate_conditional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: requires (i) an expression/broadcast channel through
    which one agent's internal control-plane regime is observable to another agent, and
    (ii) that other agent to possess an other-agent-modelling capacity (ARC-010, MECH-031)
    capable of consuming that signal. Neither exists in the current V3 substrate: confirmed,
    no "social", "mirror", "other_agent", "scent", or broadcast-channel implementation
    anywhere under ree_core/ beyond acknowledging comments. The one place this channel is
    even specified is ARC-047's seven scent fields, built explicitly to implement MECH-041
    -- and ARC-047 is itself candidate, confidence 0.0, implementation_phase v5, unbuilt.

    CONFIRMING (once ARC-047 lands): a receiving agent's OTHER_SELFLIKE-tagged inference of
    another agent's control-plane regime converges faster/more accurately when the emitting
    agent's scent channels are active/observable than when suppressed or randomized.

    FALSIFYING (VIOLATED BY): even after ARC-047 exists, suppressing/randomizing the scent
    channels produces no measurable degradation in prediction accuracy; OR receiving agents
    show no differential use of scent-channel information relative to a no-scent control.

    Currently blocked, not testable: no multi-agent environment, no expression/scent
    channel, and no other-agent latent/inference machinery exist in the V3 substrate.
  depends_on:
    - ARC-010
    - ARC-005
    - ARC-016
    - MECH-039
  location: docs/architecture/social.md#mech-041
  source:
    - docs/thoughts/2026-02-10_affective_expression_mode_broadcast.md
- id: MECH-042
  title: "Telemetry exposure channels report internal control-plane state for diagnostics."
  claim_type: mechanism_hypothesis
  subject: control_plane.telemetry_exposure_channels
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-005
    - MECH-039
    - MECH-040
  location: docs/architecture/control_plane.md#mech-042
  source:
    - docs/thoughts/2026-02-10_control_plane_telemetry.md
- id: MECH-043
  title: "Dopamine-like modulation of precision-weighting for unsigned prediction errors."
  claim_type: mechanism_hypothesis
  subject: precision.dopamine_weighting
  polarity: asserts
  status: provisional
  epistemic_category: standard
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: the audited precision term must be shown (a) actually
    computed from squared/absolute PE magnitude with no valence/sign information reaching
    it, (b) live-invoked from the real step loop, and (c) demonstrably influencing
    commitment/learning-rate rather than merely logged. For the behavioral half, the
    misallocation manipulation must produce a measurable shift in a hallucination-like DV
    under matched-RNG, non-degenerate readiness preconditions.

    CONFIRMING: ree_core/predictors/e3_selector.py's current_precision property
    (1.0/(running_variance+eps)) is fed exclusively by an EMA of squared PE -- unsigned by
    construction -- and is live-wired into the E3 commitment gate and dACC affective-PE
    weighting (independently confirmed by INV-008's audit). Matches MECH-002's "dopamine-
    like: increases precision at action/policy depths, promotes commitment" description.
    CONFIRMING further requires: an ablation/sweep of this channel's gain producing a
    measured increase in hallucination-like output, attributable to THIS channel
    specifically (not the separate signed ARC-108/109 substrate, nor MECH-054's channels).

    FALSIFYING (VIOLATED BY): (i) the computation incorporates signed/valence information;
    (ii) precision weighting collapses into a scalar reward objective; (iii) misallocation
    of this channel (isolated from ARC-108/109 and MECH-054) produces no measurable shift
    in any hallucination-like DV across a real non-degenerate sweep; (iv) the effect is
    fully explained by a shared/general precision-scale parameter also governing MECH-054's
    signed channels identically, with no functional dissociation demonstrable.
  depends_on:
    - ARC-005
    - INV-008
    - MECH-003
  location: docs/architecture/precision_control.md#mech-043
  source:
    - docs/thoughts/2026-02-10_dopamine_precision_weighting.md
- id: MECH-044
  title: "Hippocampal systems participate in relational binding and comparison."
  claim_type: mechanism_hypothesis
  subject: entities.hippocampal_relational_binding
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-688_2026-06-18
      as_of: 2026-06-19
      verdict: non_contributory/standard
  depends_on:
    - ARC-006
    - ARC-007
    - ARC-004
  location: docs/architecture/entities_and_binding.md#mech-044
  source:
    - docs/thoughts/2026-02-10_hippocampal_relational_binding.md
- id: MECH-045
  title: "Object-file-like buffers provide minimal entity persistence across time."
  claim_type: mechanism_hypothesis
  subject: entities.object_file_persistence
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_658_mech045_object_file_persistence_20260609T190711Z_v3
      as_of: 2026-06-09
      verdict: supports/PASS
  depends_on:
    - ARC-006
    - ARC-004
    - INV-002
  location: docs/architecture/entities_and_binding.md#mech-045
  source:
    - docs/thoughts/2026-02-10_object_file_persistence.md
  evidence_quality_note: |
    First genuine experimental support landed 2026-06-09 (V3-EXQ-658, run
    v3_exq_658_mech045_object_file_persistence_..., PASS supports): token buffer
    re-identifies an entity across motion (INTACT reid_frac=1.0 vs ABLATION_OFF=0.0,
    SHUFFLE=0.3 on 3/3 seeds; feature term non-degenerate d_cos_min=0.919). exp_conf
    now 0.775 (confirmed_established). Stays provisional -- provisional->stable needs
    exp_conf>=0.80 AND >=4 experimental entries; this is the first.
- id: MECH-046
  title: "CeA analogue writes a mode prior into the SalienceCoordinator from fast salience classification of z_harm_a."
  claim_type: mechanism_hypothesis
  subject: amygdala.cea_mode_prior_write
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-15
    needs_review: false
    evidence:
      from: "decision:MECH-046@2026-07-15T13:44:33.349339Z"
      as_of: 2026-07-15
      verdict: record_supporting_evidence/applied
  last_reviewed: 2026-02-15
  implementation_phase: v3
  v3_pending: false  # cleared 2026-04-22: V3-EXQ-473 substrate-readiness PASS (supports)
  lifecycle_stage: adjudicated
  adjudication_outcome: retain_ree
  adjudicated_at_utc: 2026-02-15T18:46:42.773429Z
  adjudication_decision_status: applied
  adjudication_recommendation: retain_ree
  evidence_quality_note: |
    No experimental evidence in current genuine corpus. All prior entries (9 runs,
    synthetic scaffolding) originated from ree-v2/ree-experiments-lab (archived 2026-02-26)
    and have been removed from the index. The demotion recommendation in
    promotion_demotion_recommendations.md (conflict_ratio=0.889, conf=0.55) is stale --
    it was computed from those invalid entries, which no longer exist.
    2026-04-21 narrowing: MECH-046 now specifically asserts that the CeA analogue
    (CeAAnalog in SD-035) emits a pre-softmax additive log-odds bias on the harm
    channel of the SalienceCoordinator (SD-032a) within 1-2 sim steps of
    ||LowFreq(z_harm_a)|| crossing threshold. Distinct from AIC urgency (SD-032c),
    which modulates switch threshold. Distinct from MECH-074c fast subcortical
    priming (motor/candidate bias). Literature grounding: Mendez-Bertolo 2016
    (intracranial latency ~74ms CeA vs ~400ms cortex), Pessoa & Adolphs 2010
    (many-roads bounding). Evidence pulled 2026-04-21 to targeted_review_connectome_mech_046.
    V3-EXQ-A (CeA mode-prior ablation) is the primary falsifier.
    2026-07-15 (gov): V3-EXQ-762 (CeA mode_prior context-conditioning, PASS/supports) is the
    FIRST genuine exp-class support (genuine_exp 0->1) -- drives the real CeAAnalog (688 arming
    caveat honoured) action-free across 4 graded harm contexts; DV = cea_mode_prior injected
    into a real SalienceCoordinator: present (0.70 @threat_high) + context-varying RANGE 0.70
    [load-bearing] + monotone + rest-silent + coordinator-write [load-bearing] + bounded
    (max_abs 0.72 <= cap 0.8). Wall-independent (read-only/training-free). Stays provisional --
    1 genuine exp is thin for a stable flip; this consolidates the provisional reading.
  depends_on:
    - ARC-005
    - MECH-039
    - SD-011           # affective harm stream z_harm_a (input)
    - SD-032a          # SalienceCoordinator (target of mode prior)
    - SD-035           # amygdala analogue substrate (parent)
  location: docs/architecture/sd_035_amygdala_analog.md#mech-046
  source:
    - docs/thoughts/2026-02-11_amygdala.md
    - evidence/literature/targeted_review_connectome_mech_046/
- id: MECH-047
  title: "Pre-commitment mode manager commits with hysteresis and switching costs."
  claim_type: mechanism_hypothesis
  subject: control_plane.precommitment_mode_manager
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-005
    - MECH-046
  location: docs/architecture/mode_manager.md#mech-047
  source:
    - docs/thoughts/2026-02-11_amygdala.md
    - docs/thoughts/2026-02-11_some_control_plane_maths_hypotheses.md
- id: MECH-048
  title: "mu/kappa stability overlays modulate mode entropy and switching pressure."
  claim_type: mechanism_hypothesis
  subject: control_plane.mu_kappa_stability_overlays
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-005
    - MECH-039
  location: docs/architecture/control_plane.md#mech-048
  source:
    - docs/thoughts/2026-02-11_opioid_receptors.md
    - docs/thoughts/2026-02-11_some_control_plane_maths_hypotheses.md
  implementation_note: |
    2026-07-21 -- the mu half of the opponent pair now reaches the mode PRIOR.
    NO status, confidence or evidence change: this is a substrate landing, and
    MECH-048 remains exp=0 / lit=4 with re-derive brake count 0.

    THE GAP (verified by live execution of the modules on a constructed
    REEAgent, not by grep; session practical-volhard-2c1876, 2026-07-21T17:11Z).
    MECH-048 asserts overlays modulating BOTH (a) mode-prior sharpness/entropy
    AND (b) switching pressure. On the SalienceCoordinator mode plane the
    kappa-analogue was fully wired but the mu-analogue was only HALF wired:
    affinity_weights['aic_salience'] = {'internal_planning': 1.0}, so kappa
    reached the mode logits, while affinity_weights['pcc_stability'] was EMPTY
    and mu entered only the effective_threshold multiplier. Consequence:
    H(operating_mode) was EXACTLY invariant under mu -- 0.167605 at
    pcc_stability 0.0, 1.0 AND 3.0, identical to 6 dp. Leg (a) was therefore
    untestable BY CONSTRUCTION on every substrate and every seed; any
    experiment measuring mode entropy against mu would have reported an
    arithmetic zero rather than a measurement (the DV-symmetry vacuity class,
    failure_autopsy_V3-EXQ-604c_2026-07-20 section 3). This is why the
    2026-07-21T13:39Z audit HELD V3-EXQ-683 rather than repairing it.

    THE BUILD. mu (pcc_stability, SD-032d) is coupled into the mode-prior
    softmax temperature in the literal form specified by this claim's own
    source, 2026-02-11_some_control_plane_maths_hypotheses.md:63 --
    tau = softmax_temperature * exp(alpha_kappa*kappa - alpha_mu*mu), with
    kappa = aic_salience (SD-032c). High mu -> lower temperature -> sharper
    prior -> lower entropy. The threshold half (ibid.:104,
    theta = theta_0 + rho_mu*mu - rho_kappa*kappa) is the pre-existing
    stability_scaling multiplier and is unchanged, so mu now reaches both legs.
    The rival mechanism -- a non-empty affinity_weights['pcc_stability'] --
    was rejected on claim text, not preference: a per-mode logit bias makes mu
    a mode PREFERENCE, and control_plane.md#mech-048 states the overlays "are
    not scalar reward signals; they act as stability and entropy modulators".
    Config REEConfig.salience_use_stability_temperature, default False and
    bit-identical when off; alpha_kappa defaults 0.0 so the master switch
    isolates the mu leg. Spec: sd_032_cingulate_integration_substrate.md
    SD-032d "Amendment (2026-07-21)". Contracts:
    ree-v3/tests/contracts/test_mech048_stability_temperature.py.

    WHAT THIS DOES NOT DO -- read before designing the first experiment.
    (1) With mu injected DIRECTLY, "entropy falls as mu rises" is an arithmetic
    identity of the coupling. The contract tests assert it as substrate wiring;
    it is NOT evidence for this claim. A genuine experiment must drive mu from
    upstream environmental state (safety / coherence / task success, through
    PCCAnalog) and read a downstream behavioural DV -- otherwise it reproduces
    the exact vacuity the build exists to remove.
    (2) Leg (b) remains empirically unmeasurable for an independent reason:
    the contested modes are never occupied. V3-EXQ-464b / 464c / 467d / 464d
    ALL FAIL with fraction_in_external_task = 0.0 at every seed WITH
    use_external_task_drive=True, across a 20x hysteresis sweep; mean_dwell is
    an episode-length artefact and n_switches == n_episodes (episode-boundary
    settles only). 467d self-routed substrate_not_ready_requeue /
    external_task_mode_not_occupied. That is a separate /implement-substrate
    build and was deliberately NOT bundled here.
  evidence_quality_note: >
    V3-EXQ-799 (2026-07-23, behavioural difference-in-differences driving mu
    from upstream environmental state via PCCAnalog, coupling ON vs OFF):
    C1_visitation_entropy_did and C2_action_entropy_did both FAIL (DiD
    ~0.0007/0.002 vs a 0.02 floor). The manifest's own routing_detail is
    decisive: mu moved and the mode-prior entropy moved WITH it (mechanism
    CONFIRMED and firing -- H(operating_mode) differs 1.105 nats between
    coupling-ON STABLE vs VOLATILE, far above the 0.0001 floor), but no
    behavioural consequence survived the difference-in-differences. Per
    failure_autopsy_backlog_2026-07-24.json (applied here 2026-08-06): this is
    NOT a falsification of MECH-048 -- it is a CONSUMER-PATH implementation
    gap. Nothing downstream converts mode-prior entropy into action at a
    detectable scale (write_gate breadth; interacts with the separately-known
    unoccupied-contested-modes gap, leg (b) above). epistemic_category
    competence_implementation_gap, evidence_direction non_contributory (both
    the flat and run-pack manifests updated to match; previously both carried
    no evidence_direction/decision at all, so the index could not have scored
    this run either way until now). Routed to /implement-substrate on the
    write_gate/consumer path -- not yet landed as of this note. Status stays
    provisional; no confidence/promotion change.
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: MECH-048 asserts overlays modulating BOTH
    (a) mode-prior sharpness/entropy AND (b) switching pressure -- each leg
    has its OWN precondition and its OWN confirmed-not-yet-testable history;
    do not conflate a result on one leg with a verdict on the other.
    Leg (a) precondition: requires mu driven from UPSTREAM environmental
    state (via PCCAnalog), not direct injection -- "entropy falls as mu
    rises" under direct injection is an arithmetic identity of the coupling
    (asserted by the contract tests as wiring, not evidence). V3-EXQ-799
    already did this correctly and confirmed the mechanism fires (mode-prior
    entropy moves with mu, non-degenerate), but found a SEPARATE gap: no
    behavioural DV downstream of the entropy shift currently has enough
    sensitivity to detect it (write_gate breadth issue, routed to
    /implement-substrate, not yet landed). A run against the UNFIXED
    consumer path reproduces V3-EXQ-799's C1/C2 FAIL and is not a fresh test
    -- it should self-route substrate_not_ready/competence_implementation_gap
    until the write_gate fix lands.
    Leg (b) precondition: requires the contested modes to actually be
    occupied. V3-EXQ-464b/464c/467d/464d all FAIL with
    fraction_in_external_task=0.0 at every seed across a 20x hysteresis
    sweep -- switching pressure cannot be measured on a mode that is never
    entered. This is a distinct, separate /implement-substrate build from
    leg (a)'s consumer-path gap; do not bundle a leg-(a) fix as also
    resolving leg (b).
    CONFIRMING signature: once the write_gate/consumer-path fix lands (leg a)
    and contested modes are genuinely occupied (leg b), driving mu from
    upstream environmental state produces a measurable, seed-stable
    behavioural difference-in-differences (visitation entropy and/or action
    entropy) between coupling-ON and coupling-OFF arms, clearing the 0.02
    floor V3-EXQ-799 used -- confirming the entropy-modulation mechanism
    converts to real behaviour, not just an internal representational shift.
    Separately, once modes are occupied, switching pressure (dwell time,
    n_switches independent of episode boundaries) measurably responds to
    mu/kappa.
    FALSIFYING signature: even with both fixes landed and both preconditions
    met, the behavioural DiD still fails to clear the floor (leg a) or
    switching pressure shows no response to mu/kappa once modes are occupied
    (leg b) -- either would mean the confirmed internal mechanism (entropy
    genuinely shifts with mu) does not convert to any measurable behavioural
    or switching-pressure effect, falsifying the claim's functional
    consequence even though its internal wiring is confirmed correct.
- id: MECH-049
  title: "Temporal phase compartmentalisation preserves ethical constraint independence."
  claim_type: mechanism_hypothesis
  subject: temporal.phase_compartmentalisation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-008
    - ARC-003
    - ARC-005
  location: docs/architecture/temporal_dynamics.md#mech-049
  source:
    - docs/thoughts/2026-02-11_phase_separation.md
- id: MECH-050
  title: "Functional locality supports attribution without requiring anatomical columns."
  claim_type: mechanism_hypothesis
  subject: entities.functional_locality_without_columns
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-006
    - ARC-004
  location: docs/architecture/entities_and_binding.md#mech-050
  source:
    - docs/thoughts/2026-02-11_columns.md
- id: MECH-051
  title: "Oxytocin/vasopressin analogues modulate relational topology and mode priors."
  claim_type: mechanism_hypothesis
  subject: social.relational_topology_modulation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  depends_on:
    - ARC-010
    - ARC-005
    - ARC-006
  location: docs/architecture/social.md#mech-051
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: requires (a) a multi-agent relational substrate with a
    per-other-agent state variable R_{self,j} distinguishing specific detected agents j
    -- this is the ARC-010 dependency, confirmed absent from ree_core/ (zero hits for
    mirror_model, shadow_bundle, other_self, a 4th independent confirmation this
    campaign) -- and (b) a mode-prior control plane that can be biased PER TARGET AGENT
    rather than globally. ARC-005 exists and is active, but its mode_prior mechanism
    today (ree_core/amygdala/cea.py) is a single generic threat/salience channel with no
    per-agent addressing. Without (a), there is no object for "agent j" to attach a
    relational distance to -- the claim collapses into "arousal/valence exists," already
    covered by existing ARC-005/ARC-046 machinery. Zero hits anywhere in ree-v3/ for
    relational_topology, relational_distance, pair_bond, oxytocin, or vasopressin.

    CONFIRMING: once ARC-010 lands, a per-agent, bidirectional, dissociable effect --
    affiliative signal toward a specific agent j lowers R_{self,j} and raises bonding-
    mode prior/shared-reward weighting specifically for trajectories involving j, while
    leaving R_{self,k} for other agents k unchanged; a boundary-threat signal from a
    different agent m raises R_{self,m} and defensive-mode prior specifically toward m,
    without generalizing to j.

    FALSIFYING: bonding/defense behavior is fully explained by the existing generic
    threat/salience mode_prior channel applied uniformly regardless of which agent
    triggered it -- no per-agent relational-distance state does any work, and collapsing
    all agents into a single scalar threat/affiliation signal reproduces the same
    mode-prior shifts.
  source:
    - docs/thoughts/2026-02-11_oxytocin_vasopressin.md
- id: MECH-052
  title: "Prolactin analogue stabilises care-investment persistence."
  claim_type: mechanism_hypothesis
  subject: social.care_investment_persistence
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  depends_on:
    - ARC-010
    - MECH-051
    - MECH-048
  location: docs/architecture/social.md#mech-052
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: requires ARC-010 (confirmed absent from ree_core/),
    MECH-051 (oxytocin/vasopressin relational topology, supplying BondStrength_j and
    relational distance R_{self,j} -- itself substrate_conditional, digested this same
    wave), and MECH-048 (mu/kappa stability overlays, currently provisional) promoted --
    plus an actual care-investment weight C_j implemented in the control plane
    (w_j ~ C_j * (1 - R_{self,j})) and a testbed presenting a sustained caregiving-type
    goal/relationship competing against short-term reward under delayed payoff, resource
    scarcity, or asymmetric exchange. Zero hits anywhere in ree-v3/ for prolactin,
    care_invest, care_weight, or persistence_gain -- pure documentation-stage mechanism.

    CONFIRMING: an agent with the prolactin-analogue persistence term active (C_j decay
    slowed per alpha_P * BondStrength_j - beta * Decay) continues investing resources/
    attention in a caregiving-type relationship for LONGER, with greater resistance to
    defecting toward a competing higher-immediate-reward option, than an otherwise-
    matched agent with the persistence term ablated -- across delayed-reward, resource-
    cost, and asymmetric-exchange conditions.

    FALSIFYING: C_j decays at the same rate or faster in the prolactin-analogue-active
    agent as in the ablated control under matched conditions. Also falsifying: activating
    the persistence term produces indiscriminate stickiness -- uniformly high C_j
    regardless of BondStrength_j -- rather than differential persistence keyed to
    established bonds, contradicting the claimed division of labor (oxytocin creates
    closeness, prolactin sustains care where closeness already exists).
  source:
    - docs/thoughts/2026-02-11_prolactin.md
    - docs/thoughts/2026-02-25_task_loop_extraction_and_latent_field_ethics.md
- id: MECH-053
  title: "Habenula-like aversive PE gate suppresses commitment under negative spikes."
  claim_type: mechanism_hypothesis
  subject: control_plane.habenula_aversive_gate
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-053@2026-02-15T20:51:27.090007Z"
      as_of: 2026-02-15
      verdict: promote_to_provisional/applied
  depends_on:
    - ARC-005
    - MECH-039
    - MECH-043
  location: docs/architecture/control_plane.md#mech-053
  source:
    - docs/thoughts/2026-02-11_habenula_signed_pe.md
  notes: >
    WORDING DISAMBIGUATION (2026-08-18): the claim's "keeps aversive salience distinct from
    reward-like precision weighting" was ambiguous between an ARCHITECTURAL reading (two
    disjoint channels; a harm-PE pathway carrying no appetitive information at all) and a
    FUNCTIONAL reading (one signed channel, asymmetric dynamics and downstream consequences,
    not folded into a symmetric scalar). Resolved to the FUNCTIONAL reading;
    control_plane.md#mech-053 reworded accordingly. This does NOT change status/confidence --
    a wording-clarity edit only. Distinguish from MECH-054, which is the narrower,
    ARCHITECTURAL claim (separate precision-weighted forward models for harm vs benefit, each
    with its own independently-tracked precision/variance state) -- the two claims sit at
    different levels and this disambiguation does not collapse them into restatements of each
    other; MECH-054's own notes already document that its stronger architectural bar is
    currently unmet on the benefit side (2026-08-08 digestion pass).

    Literature basis (2026-08-17 targeted connectome pull,
    evidence/literature/targeted_review_connectome_mech_053/, 5 entries): the LHb evidence
    favours the functional reading over the architectural one. Wang et al. 2017 (eLife,
    PMID 28561735) and Matsumoto & Hikosaka 2009 (Nat Neurosci) both find the SAME LHb
    population bidirectionally signed -- excited by aversive events, inhibited by reward --
    which is hard to reconcile with two anatomically disjoint channels but is exactly the
    signature of one signed channel whose limbs carry different dynamics. Laurent et al.
    2017's pathway-selective ablation (LHb->RMTg) shows the pathway is causally necessary for
    NEGATIVE predictors to bias choice and not for positive ones -- direct evidence for the
    functional/causal ASYMMETRY the claim needs (aversive PE is not treated the same as a
    symmetric net-value signal), independent of whether the underlying representation is one
    channel or two. Lawson et al. 2014 (human fMRI) adds the same signed bidirectionality with
    a behavioural readout (aversive value -> response suppression; appetitive value ->
    invigoration). None of the five entries speak to the claim's pre-consolidation TIMING
    requirement, which remains untested and is flagged in the pull as `complex (probe-gated)`
    -- wants a REE experiment varying harm-PE arrival relative to trajectory consolidation, not
    further literature.

    Consequence: the Wang 2017 entry's evidence_direction has been moved mixed -> supports
    (see that entry's record.json) now that the reading it was withholding judgment on is
    resolved in its favour. The other four MECH-053 entries from this pull were already
    `supports` and are unaffected.
- id: MECH-054
  title: "Signed harm/benefit prediction-error precision channels remain distinct."
  claim_type: mechanism_hypothesis
  subject: precision.signed_harm_benefit
  polarity: asserts
  status: provisional
  epistemic_category: standard
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-054@2026-02-15T20:51:27.090007Z"
      as_of: 2026-02-15
      verdict: promote_to_provisional/applied
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: the comparison requires BOTH a harm-side and a
    benefit-side channel to be actually exercised live, each producing its own
    precision-weighted signed prediction-error from its own forward model, with each
    channel's precision/variance state stored and updated independently of the other's.

    CONFIRMED CURRENT STATE (2026-08-08 digestion pass): ARCHITECTURALLY ASYMMETRIC --
    precondition NOT met. HARM side: E2HarmAForward/E2HarmSForward (MECH-258) are
    dedicated forward models, consumed by DACCAdaptiveControl as an explicit
    "precision-weighted affective-pain prediction error." BENEFIT side: E3Selector's
    benefit_eval_head (ARC-030/MECH-112) is a plain proximity-regression head with no
    paired forward model and no benefit-specific precision/variance tracker -- no analogue
    of _running_variance or a benefit-side dACC exists anywhere in ree_core. The learned
    benefit_eval_head has never converged (EXQ-085m: r2=-8.6; EXQ-183: auc=0.538, near
    chance); ARC-030's one PASS came from fixed-weight CEM scoring used INSTEAD OF the
    learned head, and ARC-030 remains candidate/pending_retest_after_substrate. Today's
    substrate has ONE live signed-precision-weighted PE channel (harm), not two --
    MECH-054's comparison cannot yet be run.

    CONFIRMING: once a benefit-side channel reaches the same class as the harm-side one --
    a benefit forward model whose PE is consumed by its own precision-weighted control-plane
    readout, verified numerically and causally independent of the harm channel's -- and
    downstream consumers respond differently to a manipulation that decouples harm-PE from
    benefit-PE, that is confirming evidence.

    FALSIFYING (VIOLATED BY): (a) the benefit-side channel, once built, shares its
    precision/variance state with the harm channel; (b) downstream consumers show
    statistically indistinguishable sensitivity to harm-PE-only vs. benefit-PE-only
    manipulations; or (c) the benefit-side channel requires being driven by the harm
    channel's precision state to train or stabilize at all.

    GOVERNANCE FLAG (2026-08-08 digestion pass, not resolved here): the title/wording
    presupposes parity between harm and benefit channels that doesn't currently exist --
    flagged for governance to consider whether MECH-054 should be rescoped (e.g. "benefit-
    side signed-PE precision channel does not yet exist; harm-side does") rather than
    "remain distinct". Also flagging a possible cross-claim consistency question against
    MECH-043's "unsigned PE" framing for the sibling dopamine-like channel -- not resolved
    here, worth a synthesis pass relating MECH-043/054/002/055's precision-channel taxonomy.
  depends_on:
    - ARC-005
    - ARC-017
    - MECH-043
    - INV-008
  location: docs/architecture/control_plane.md#mech-054
  source:
    - docs/thoughts/2026-02-11_habenula_signed_pe.md
- id: MECH-055
  title: "Affective channel separation keeps hedonic tone, valence, and signed PE distinct."
  claim_type: mechanism_hypothesis
  subject: affect.channel_separation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: standard
  depends_on:
    - ARC-005
    - MECH-048
    - MECH-054
    - MECH-035
  location: docs/architecture/control_plane.md#mech-055
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: requires each of the three named axes independently
    computable and independently perturbable -- (a) hedonic-stability (mu/kappa
    overlays), (b) VALENCE vector components with non-zero cross-candidate variance on
    >=2 components simultaneously, (c) signed PE precision from a genuine forward-model-
    based precision-weighted PE on BOTH harm and benefit sides. CONFIRMED CURRENT STATE
    (2026-08-08): (a) PARTIALLY MET -- mu now reaches both mode-prior temperature and
    threshold (SD-032d, 2026-07-21), V3-EXQ-799 confirmed non-degenerate internal
    variance (1.105 nats between coupling states); downstream behavioural legibility gap
    is separate and doesn't block a representational-level test. (b) representationally
    real (VALENCE_COMPONENTS, ree_core/residue/field.py) but experimentally untested
    (MECH-035 records no prior run as of 2026-08-06). (c) NOT MET per MECH-054's 2026-08-08
    finding -- only the harm-side channel is real/precision-tracked; no benefit-side
    analogue exists. Consequence: the full three-axis-with-harm/benefit-duality test
    cannot yet run; a narrower two-axis-plus-harm-only test CAN run now and should
    self-route substrate_not_ready if attempted as a verdict on the full claim before the
    benefit-side channel lands.

    COLLAPSE-RISK CHECK (new finding, 2026-08-08): before any distinctness test is
    credited, it must rule out a specific risk -- REE carries TWO harm-related
    representations downstream of the same z_harm_s/z_harm_a signal:
    VALENCE_HARM_DISCRIMINATIVE (residue-field tag, axis 2) and the DACC-consumed harm
    forward-model PE (axis 3). Nothing currently shows these carry independent variance
    rather than being one collapsed harm scalar wearing two labels -- the same failure
    class MECH-048 hit before its 2026-07-21 fix (an arithmetic identity mistaken for a
    measurement). A test showing these merely live in different variables, without
    showing independent variance under a manipulation that perturbs one without the
    other, has not established distinctness.

    CONFIRMING: under the two-axis-plus-harm-only test, manipulations targeting each axis
    individually produce measurable, non-degenerate effects on that axis's own documented
    downstream role WITHOUT matched lockstep effects on the other axes -- no single
    upstream perturbation moves all measured axes in a fixed, predictable ratio.
    Separately, VALENCE_HARM_DISCRIMINATIVE and the DACC harm-PE show measurably
    different variance/timing signatures under a manipulation that decouples their
    upstream sources, ruling out the collapsed-harm-scalar case above.

    FALSIFYING: (i) any two axes move in a fixed, predictable ratio under manipulations
    designed to perturb only one; (ii) VALENCE_HARM_DISCRIMINATIVE and the harm-side
    forward-model PE are numerically redundant (correlation ~1) under a decoupling
    manipulation; (iii) once built, the benefit-side signed-PE channel requires being
    driven by or sharing state with VALENCE_WANTING/LIKING to train or stabilize,
    mirroring MECH-054's own falsifier (c).

    NAMING NOTE: REE's "hedonic tone" (mu/kappa) is NOT the standard affective-
    neuroscience hedonic-tone/valence distinction -- it functions as a control-plane
    stability/inertia regulator (softmax-temperature modulator), not a content-bearing
    affect quale. The real hedonic-impact-like content (Berridge wanting/liking) lives
    inside the VALENCE vector (axis 2), not axis 1.

    GOVERNANCE FLAG (2026-08-08, not resolved here): the MECH-043/054/002/055 precision-
    channel taxonomy synthesis flagged in MECH-054's digestion is reinforced here --
    MECH-035's VALENCE vector also carries harm-adjacent (HARM_DISCRIMINATIVE) and
    benefit-adjacent (WANTING/LIKING) components that sit conceptually on top of the same
    signals MECH-054/055 assign to "signed PE precision." A synthesis pass should map
    MECH-002 -> MECH-043 -> MECH-054 -> MECH-035 -> MECH-055 explicitly and resolve
    whether VALENCE_HARM_DISCRIMINATIVE is a redundant readout of the DACC harm signal or
    a genuinely independent channel -- undocumented either way as of this pass.

    CATEGORY JUDGMENT CALL: epistemic_category is set to standard for consistency with
    sibling MECH-054's treatment of the same underlying substrate gap (see MECH-054's own
    what_would_answer). A stricter reading -- since this claim's own wording requires the
    harm/benefit duality inside axis 3, and that duality's benefit half is architecturally
    absent -- could defensibly argue substrate_conditional instead; flagged for governance
    to revisit if the stricter reading is preferred.
  source:
    - docs/architecture/control_plane.md
# JEPA flags removed 2026-02-26 — residue trajectory placement is REE-native.
- id: MECH-056
  title: "Residue should constrain trajectories before distorting core representations."
  claim_type: mechanism_hypothesis
  subject: residue.trajectory_first_placement
  polarity: asserts
  status: provisional
  epistemic_category: standard
  what_would_answer: >
    Asks whether placing residue pressure in trajectory-level gating (hippocampal
    rollout costs, E3 thresholds) ahead of representational distortion actually
    reduces enacted harm relative to a residue signal that only reads the
    terminal/endpoint state -- the same question EVB-0039 (residue_trajectory_placement,
    ree-v1-minimal 2026-02-26 PASS, ree-v2 revalidation 2026-03-14 PASS) answered on
    pre-V3 substrate, reprised against V3's live mechanism.

    NON-DEGENERACY PRECONDITION: the test environment must produce intermediate
    (mid-path) harm-relevant world-state changes that are distinct from the
    terminal state -- i.e. a hazard/harm signal attributable to specific
    intermediate steps along the executed path, not only encoded in the final
    state. Without that, TRAJECTORY-WIDE and ENDPOINT-ONLY residue placement are
    indistinguishable and the test is vacuous (this was true of EVB-0039's
    CausalGridWorld hazard layout and must hold for its V3 successor too).
    Trajectories under test must be genuinely committed (non-hypothesis-tagged);
    MECH-094 blocks residue accumulation from rehearsal/replay content, so a
    rehearsal-only run cannot exercise this claim.

    CONFIRMING: reprise EVB-0039's own operationalization directly on the V3
    substrate, comparing (i) TRAJECTORY-WIDE placement, using
    ResidueField.evaluate_trajectory() over the full step horizon -- already the
    live default read path for both E3's Phi_R term (ree_core/predictors/e3_selector.py)
    and hippocampal rollout terrain scoring (ree_core/hippocampal/module.py) -- against
    (ii) an ENDPOINT-ONLY ablation, restricting evaluate_trajectory() to the
    terminal step only, via the same code-swap pattern V3-EXQ-046/397/397c/397d
    already use to null it out for ARC-007. A CONFIRMING result requires, across
    a multi-seed sweep at least as large as EVB-0039's (3 seeds / 200 episodes):
    (a) path-spread criterion -- trajectory-wide intermediate residue mass is
    non-zero and localised along the executed path, not only at the terminus;
    (b) harm-avoidance criterion -- last-quarter-episode harm under
    TRAJECTORY-WIDE placement is no worse than ENDPOINT-ONLY by more than the
    EVB-0039 harm_tolerance_factor (ratio <= 1.05), ideally recovering a margin
    closer to the original ree-v1-minimal PASS (0.724 vs 0.8127, ~11%) than the
    much thinner ree-v2 revalidation margin (1.2493 vs 1.2273, ~1.8%, which only
    passed inside tolerance); and (c) MECH-056's own rehearsal-lane hooks hold
    over the same sweep: ledger_edit_detected_count == 0, domination_lock_in_events
    == 0, and explanation_policy_divergence_rate <= 0.05.

    FALSIFYING (VIOLATED BY): last-quarter-episode harm under TRAJECTORY-WIDE
    placement exceeds ENDPOINT-ONLY by more than the harm_tolerance_factor
    (ratio > 1.05) on the V3 substrate across a properly-powered multi-seed
    sweep -- i.e. trajectory-level gating measurably fails to reduce enacted harm
    relative to judging only the terminal state, on an environment materially
    more complex than the CausalGridWorld toy grid both prior PASSes used. Also
    falsifying: the path-spread criterion failing (trajectory_wide_mean_intermediate_residue_mass
    statistically indistinguishable from zero), which would show that
    evaluate_trajectory()'s full-horizon read is present in code but contributes
    no real signal beyond what the terminal step alone gives in the live E3/
    hippocampal gating path -- i.e. "trajectory-first placement" is wired but
    inert. Also falsifying: any rehearsal-lane hard-invariant violation
    (ledger_edit_detected_count > 0 or domination_lock_in_events > 0) during the
    sweep, indicating rehearsal-only trajectory generation is leaking into
    committed residue/policy state in violation of MECH-056's DMN-like
    rehearsal-lane contract.

    GOVERNANCE/QUEUE FLAG (2026-08-08 digestion pass, not resolved here): no
    V3-scale re-test has ever been queued for this claim (confirmed via
    experiment_queue.json and git log -S). The mechanism is confirmed live and
    default-wired -- flagged as a clean /queue-experiment candidate reusing the
    evaluate_trajectory() full-horizon-vs-endpoint-only swap pattern already
    established by V3-EXQ-786/397-family, scored against MECH-056's own
    path-spread + harm-avoidance criteria.
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-056@2026-02-15T20:58:38.602475Z"
      as_of: 2026-02-15
      verdict: hold_candidate_resolve_conflict/applied
  last_reviewed: 2026-02-15
  lifecycle_stage: adjudicated
  adjudication_outcome: hybridize
  adjudicated_at_utc: 2026-02-15T18:46:42.773429Z
  adjudication_decision_status: applied
  adjudication_recommendation: hybridize
  adjudication_mode: external_precedence_allowed
  allowed_conflict_outcomes:
    - retain_ree
    - hybridize
    - retire_ree_claim
  cascade_on_outcomes:
    - retire_ree_claim
  dependency_reopen_status: candidate
  evidence_quality_note: |
    Prior 42 synthetic runs (ree-v2/ree-experiments-lab, archived 2026-02-26) are invalid.
    Genuine ree-v1-minimal experiment completed 2026-02-26 (EVB-0039, residue_trajectory_placement): PASS.
    TRAJECTORY-WIDE last-Q harm 0.724 vs ENDPOINT-ONLY 0.813 (11% improvement, 3 seeds).
    Intermediate residue mass confirmed non-zero (mean 3.48 across seeds).
    Both criteria met: path-spread (residue localised along planned path) and harm-avoidance
    (trajectory-wide accumulation reduces harm vs endpoint-only). Supports φ(z) terrain concept:
    harm sites are localised along planned trajectories, not only post-hoc at terminal steps.
    Promoted candidate → provisional 2026-02-26. Re-test on more complex substrate before active.
    Revalidation audit 2026-04-25: old synthetic trajectory_integrity rows remain in the
    matrix for audit but are stale_epoch excluded from scoring; the counted support row is
    20260314T220919_residue_trajectory_placement_v2.
  depends_on:
    - ARC-013
    - ARC-018
    - ARC-003
    - ARC-004
    - MECH-034
    - MECH-060
    - MECH-062
  location: docs/architecture/residue_geometry.md#mech-056
  source:
    - docs/architecture/residue_geometry.md
    - docs/thoughts/2026-02-12_TRAJECTORY-RESIDUE-VS-REPRESENTATIONAL-DISTORTION.md
    - docs/thoughts/2026-02-15_basal_ganglia_commit_gating_control_plane_axes.md
# MECH-057 split 2026-03-15: action-loop and thought-loop gates are distinct mechanisms
# requiring different substrates and different V-phase targets. MECH-057a retains the V1/V2
# evidence and is V2-testable with multi-step substrate. MECH-057b is explicitly V3-scoped.
- id: MECH-057a
  title: "Committed action sequences suppress sensory precision reweighting until execution completes."
  claim_type: mechanism_hypothesis
  subject: agentic_extension.action_loop_completion_gate
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-057a@2026-03-29T21:15:31.848348Z"
      as_of: 2026-03-29
      verdict: promote_to_provisional/applied
  last_reviewed: 2026-02-25
  lifecycle_stage: adjudicated
  adjudication_outcome: hybridize
  adjudicated_at_utc: 2026-02-25T14:59:46.253616Z
  adjudication_decision_status: applied
  adjudication_recommendation: hybridize
  evidence_quality_note: |
    Prior 10 synthetic runs (ree-v2/ree-experiments-lab, archived 2026-02-26) are invalid.
    Genuine ree-v1-minimal experiment completed 2026-02-26 (EVB-0042, control_completion_requirement): FAIL
    (informative baseline — effect direction correct, magnitude sub-threshold).
    FULL last-Q harm 0.879 | NO_ATTRIBUTION 0.896 (+1.9%) | NO_GATING 0.919 (+4.5%).
    Degradation threshold 1.1 (10%): neither ablation met it (1.019x and 1.046x respectively).
    Effect ordering is architecturally correct: FULL < NO_ATTRIBUTION < NO_GATING — completion gating
    matters more than attribution at this scale (4.5% vs 1.9%), consistent with theory.
    V2 re-test (Attribution Completion Gating — V2 Redesign, 2026-03-15): FAIL.
    V2 substrate still uses single-step grid actions — atomic action structure persists in
    base CausalGridWorld. The action-loop gate has no "action in progress" state to protect.
    INTERPRETATION: all prior FAILs are substrate-limited, not architecture falsifications.
    SCOPE REVISION RESOLVED (2026-03-15): MECH-057 split into MECH-057a (this claim, action loop)
    and MECH-057b (thought-loop trajectory promotion gate, V3-scoped). The two gates are
    mechanistically distinct and require different substrates.
    SUBSTRATE FIX IMPLEMENTED (2026-03-15): CausalGridWorld extended with subgoal_mode=True
    (3-waypoint committed sequences W0→W1→W2; entity type 6; timeout=20 steps).
    REEAgent updated: action_loop_gate_enabled flag + sequence_in_progress routing in
    generate_trajectories(); update_residue(owned=bool) for attribution ablation.
    EXQ-020 designed and smoke-tested 2026-03-15 (directionally PASS at 5 eps / 1 seed).
    Full run (5 seeds × 250 episodes × 3 conditions) queued in ree-v2/experiment_queue.json.
    Will update to provisional on EXQ-020 PASS.
    CLAIM REFRAMING (2026-03-15): Heartbeat architecture analysis (ARC-023, MECH-090)
    reveals the experiment modelled E3 as updating only at completion (binary gate), not
    as beta-gated propagation within a continuous update stream. The correct mechanism is:
    completion events are the principal policy-update opportunities (beta drops, E3 model
    propagates to action selection) within an otherwise continuous E3 internal update stream.
    The V2 FAIL pattern (ATTRIBUTION_BLIND comparable to COMPLETION_GATED) is consistent
    with this reframing: continuous updating without any beta gate is closer to correct
    architecture than episodic gating. Subject updated to reflect: completion events as
    policy-propagation gates, not as exclusive E3 update triggers. Full test requires
    ARC-023 + MECH-090 (V3 substrate, SD-006).
    EXQ-139 FAIL (2026-03-29): mean_committed_seq_len_on=0.0 -- committed sequences never
    generated; gate never triggered at this training scale; harm_rate_GATE_ON=harm_rate_ABLATED=
    0.7244. Substrate issue, not scientific failure. Hold pending investigation of why committed
    sequences fail to generate at current training budget.
    FRAMING CLARIFICATION (2026-04-02): Literature (Schmidt et al. 2019, Leventhal et al.
    2012) distinguishes motor output suppression (beta blocks movement initiation) from
    sensory precision reweighting suppression (beta blocks precision updates during committed
    sequences). MECH-057a claims the latter -- beta suppresses SENSORY REWEIGHTING, not
    motor output. MECH-090 (beta gates E3->action propagation, not E3 internal updating)
    is probably the more correct framing and is better supported by electrophysiology.
    However, the alternative framing (beta suppresses precision updates themselves, not just
    their propagation) is worth testing as lower priority once substrate supports it.
    Title updated from "precision updates" to "sensory precision reweighting."
    Revalidation audit 2026-04-25: EXQ-139 and EXQ-203 are substrate/proxy-limited
    (no actual committed sequence generation; identical condition metrics) and should not
    move confidence. They are tracked as invalid_run exclusions. MECH-057a still needs a
    genuine V3 action-sequence substrate test before promotion beyond provisional.
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: requires committed action sequences to
    ACTUALLY BE GENERATED at the training scale tested (EXQ-139 already
    failed this: mean_committed_seq_len_on=0.0, gate never triggered) -- a
    run where committed sequences never form self-routes substrate_not_ready,
    it cannot test the mechanism. Requires the genuine V3 action-sequence
    substrate (ARC-023 heartbeat + MECH-090 beta-gated propagation + SD-006),
    not the V1/V2 atomic-action substrates that produced all prior FAILs
    (confirmed substrate-limited, not falsifying, per the 2026-04-25
    revalidation audit).
    CONFIRMING signature: during a genuinely-generated committed action
    sequence, sensory precision reweighting is measurably suppressed
    relative to an uncommitted/ablated control, with the suppression
    targeting PRECISION UPDATES specifically (not motor output blocking --
    the 2026-04-02 framing clarification distinguishes these). Degradation
    should show the architecturally-predicted ordering FULL < NO_ATTRIBUTION
    < NO_GATING (as the informative-but-sub-threshold EVB-0042 baseline
    already showed directionally: 4.5% vs 1.9% harm-rate degradation).
    FALSIFYING: precision reweighting is equally suppressed (or unsuppressed)
    regardless of commitment state once sequences genuinely generate; or the
    effect is better explained by MECH-090's alternative framing (beta gates
    E3->action PROPAGATION, not E3's internal precision updating) with no
    residual effect once that pathway is accounted for -- this claim's own
    notes flag MECH-090 as "probably the more correct framing," so a run
    finding MECH-090 alone sufficient would fold MECH-057a into it rather
    than support it as a distinct mechanism.
  depends_on:
    - ARC-015
    - ARC-005
    - ARC-003
    - ARC-004
    - INV-012
    - ARC-023
    - MECH-090
  location: docs/architecture/agency_responsibility_flow.md#mech-057a
  source:
    - docs/thoughts/2026-02-13_LeCun_developed_lots_of_REE.md
    - docs/thoughts/2026-02-13_subjective_experience_pre_post_commit.md
    - docs/thoughts/2026-02-23_some_subjective_experience_mapping.md
# Reframed 2026-02-26 — removed JEPA framing. Subject renamed to REE-native formulation.
# Prior evidence (156 entries) was generated by synthetic repos (ree-v2, ree-experiments-lab,
# archived 2026-02-26) and is invalid. Claim reset for genuine ree-v1-minimal experiments.
# Split from MECH-057 2026-03-15 — scope revision resolved.
- id: MECH-057b
  title: "Hippocampal sequence completion must be verified before candidates are eligible for E3 selection."
  claim_type: mechanism_hypothesis
  subject: agentic_extension.thought_loop_trajectory_promotion_gate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-672-series_2026-06-15#V3-EXQ-672b"
      as_of: 2026-06-15
      verdict: non_contributory/substrate_ceiling
  lifecycle_stage: candidate
  implementation_phase: v3
  governance_2026_06_14: >
    User-adjudicated (decision-brief follow-up). The two genuine V3 runs were
    reclassified out of scoring: V3-EXQ-672a -> non_contributory (the
    trajectory-promotion gate filters ~40% of candidates, C3 PASS, but
    C6_baseline_not_collapsed FAILED -- goal_success=0.0 in BOTH arms and
    candidate-score spread=0.009, so the gate's harm/completion benefit could
    not be measured against a functional baseline); V3-EXQ-672 -> superseded by
    672a per the EXQ supersession policy. Both propagated to the runs/ pack
    manifests (the scored copy). Net: MECH-057b returns to ZERO genuine
    experimental evidence (exp_conf -> 0; lit_conf 0.857 only) -- consistent
    with the 2026-04-25 revalidation note, the prior 0.66 having been a
    false-tag artefact. Next step (user decision): requeue V3-EXQ-672b with a
    NON-collapsed baseline so the gate's benefit is measurable (chip spawned).
    PROMOTES/DEMOTES NOTHING.
  governance_2026_06_15: >
    The requeued V3-EXQ-672b RAN with a NON-collapsed baseline (C6_baseline_not_collapsed=true,
    goal_success_rate_ARM_0=0.26) -- so this is the FIRST GENUINE counted experiment for MECH-057b,
    and it WEAKENS. The trajectory-promotion gate is functional and non-degenerate (C3_gate_filters
    true: ARM_1 suppresses 40.6% of candidates; criteria_non_degenerate C1/C2/C3 all true), but it
    delivers NO benefit: C1_harm_reduction FALSE (harm 0.343 gate-on vs 0.311 baseline -- marginally
    worse) and C2_completion_improvement FALSE (0.34386 vs 0.34398 -- flat). User-adjudicated 2026-06-15
    (governance): accepted as a genuine weakens (the gate works as wired but the predicted harm/completion
    benefit is absent on this substrate), NOT routed to /failure-autopsy. exp_conf -> 0.325
    (genuine weakens:1 against 3 lit-supports + 1 lit-mixed) -> raised hold_candidate_resolve_conflict.
    Stays candidate / V3-pending (no demotion). Supersedes the stale "No genuine experiments" framing in
    the evidence_quality_note below (that text predates 672b).
  governance_2026_06_16: >
    DEFERRED (governance cycle, user-adjudicated). The G4 heterogeneity warning re-fired
    (conflict_ratio 0.5, no heterogeneity_note). Adding an honest conflict-classification
    requires inspecting the 672b weakens against the 3 lit-supports + 1 lit-mixed to decide
    whether the divergence is a substrate-version confound, methodological divergence, or a
    genuine lit-vs-exp contradiction -- not done this cycle. Deferred to a focused
    conflict-resolution / heterogeneity-note pass. Status UNCHANGED (candidate / V3-pending);
    no heterogeneity_note stamped to avoid a guessed classification. Re-surfaces next cycle.
  literature_evidence: |
    Pfeiffer & Foster (2013) PMID 23594744 [DOI: 10.1038/nature12112]:
    Hippocampal place-cell sequences depict future paths to remembered goals before
    goal-directed navigation. Sequences are generated, checked for goal-reach
    plausibility, then emitted — this is the planning/candidacy gate in action.
    The mechanism confirms that trajectory candidacy is decided in hippocampus before
    proposals reach downstream planning stages. Central reference for MECH-057b.
    Diba & Buzsáki (2007) PMID 17828259 [DOI: 10.1038/nn1961]:
    Forward and reverse hippocampal place-cell sequences during sharp-wave ripples.
    Forward sequences are anticipatory (planning); reverse sequences are evaluative
    (completion verification). The switch from reverse to forward replay at run start
    is a completion marker — the previous sequence ran to its end and was evaluated.
    Supports the idea that sequence completion is a discrete detectable event.
    Lansink et al. (2009) PMID 19688032 [DOI: 10.1371/journal.pbio.1000173]:
    Hippocampus leads ventral striatum in joint replay. Place cells fire before
    reward-correlated striatal cells. Directionality confirmed: hippocampal completion
    signals propagate forward to BG, not the reverse. Establishes the anatomical
    direction of the candidacy→propagation gate coupling (see MECH-105, ARC-028).
  evidence_quality_note: |
    No genuine experiments. This claim is explicitly V3-scoped.
    The thought-loop trajectory promotion gate requires HippocampalModule to implement a
    feedback path that suppresses trajectory candidates from being promoted to E3 consideration
    before hippocampal sequence completion is verified. This path is not present in V1 or V2.
    V3 primary scope: full HippocampalModule trajectory promotion policy and E3 complex
    commitment gate (per V2 spec Step 2.5 exit criteria and V3 control completion focus).
    Split from MECH-057 2026-03-15 -- see MECH-057a for action-loop evidence and V1/V2 history.
    Tagging correction (2026-03-22): EXQ-048/048b/059/060 were previously tagged as
    MECH-057b evidence, but all of these experiments tested the BG beta propagation gate
    (MECH-090), not the hippocampal candidacy gate. The tagging error arose because
    EXQ-048 was designed for MECH-057b but the implementation bypassed BetaGate entirely
    (broken instrumentation); subsequent iterations fixed the routing and shifted to testing
    MECH-090, but carried the MECH-057b tag forward without re-evaluation. All affected
    result JSONs and manifests have been corrected. MECH-057b has zero genuine experimental
    evidence. The governance confidence score (0.66) prior to this fix was an artefact of
    the tagging error.
    First genuine experiment needed: a dedicated V3 experiment instrumenting
    HippocampalModule's trajectory emission directly -- measuring whether partial sequences
    are suppressed vs completed sequences are promoted to E3 evaluation. This requires
    HippocampalModule trajectory promotion policy to be implemented first (V3 scope).
    See ARC-028 for the coupling between the hippocampal completion signal and BetaGate.
    Revalidation audit 2026-04-25: still zero counted genuine experimental evidence.
    This is the remaining MECH-057 revalidation blocker.
  depends_on:
    - ARC-015
    - ARC-005
    - ARC-003
    - ARC-004
    - INV-012
    - Q-019
    - ARC-020
  location: docs/architecture/agency_responsibility_flow.md#mech-057b
  source:
    - docs/thoughts/2026-02-15_basal_ganglia_commit_gating_control_plane_axes.md
    - docs/thoughts/2026-03-14_three_bg_systems_error_signals.md
- id: MECH-058
  title: "Slow target-anchor dynamics stabilize E1/E2 representations via functional rate separation."
  claim_type: mechanism_hypothesis
  subject: latent_stack.e1_e2_timescale_separation
  polarity: asserts
  status: retired
  live_status:
    reading: retired
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-058@2026-02-25T16:39:07.573674Z"
      as_of: 2026-02-25
      verdict: hold_candidate_resolve_conflict/applied
  last_reviewed: 2026-02-25
  lifecycle_stage: superseded
  superseded_by: MECH-069
  superseded_at_utc: 2026-03-15T00:00:00Z
  supersession_note: |
    MECH-058 framed the E1/E2 distinction as a *timescale* difference (learning rate separation).
    This framing was a stepping stone but is now understood to be incorrect.
    The deeper and more precise claim is MECH-069: E1, E2, and E3 train on *incommensurable error
    signals* (sensory prediction error / motor-sensory error / harm+goal error). These are not
    merely different timescales — they are functionally distinct domains that cannot be merged
    without misattributing credit. EXQ-019 (V2 redesign, 2026-03-15) FAILED: learning-rate
    separation produced no benefit, as expected — the test was asking the wrong question.
    Testing LR ablation cannot reveal functional-domain incommensurability. MECH-069 supersedes
    this claim and can only be properly tested in V3 with the z_self/z_world latent split (SD-005).
  adjudication_outcome: retain_ree
  adjudicated_at_utc: 2026-02-25T16:00:13.142266Z
  adjudication_decision_status: applied
  evidence_quality_note: |
    V1 EXQ-002 (e1_e2_timescale_ablation): FAIL. V2 EXQ-019 (terrain_timescale, 2026-03-15): FAIL.
    Both FAILs consistent with supersession: the test was ill-posed. LR separation ≠ functional
    domain separation. See MECH-069 for the correct claim.
    Revalidation audit 2026-04-25: do not revalidate MECH-058 as a JEPA/anchor or
    learning-rate claim. It is retired and superseded by MECH-069; later E2 short-horizon
    evidence belongs under MECH-231 / MECH-069, not MECH-058.
  depends_on:
    - ARC-001
    - ARC-002
    - ARC-004
    - ARC-015
    - MECH-057a
  location: docs/architecture/agency_responsibility_flow.md#mech-058
  source:
    - docs/architecture/agency_responsibility_flow.md
# Promoted 2026-02-26 — retain_ree adjudication was applied 2026-02-15
- id: MECH-059
  title: "Confidence channel (uncertainty-derived precision) must remain distinct from residual error."
  claim_type: mechanism_hypothesis
  subject: precision.confidence_channel_separate_from_prediction_error
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-059@2026-02-15T20:58:38.602475Z"
      as_of: 2026-02-15
      verdict: hold_candidate_resolve_conflict/applied
  last_reviewed: 2026-02-15
  lifecycle_stage: active
  adjudication_outcome: retain_ree
  adjudicated_at_utc: 2026-02-15T18:46:42.773429Z
  adjudication_decision_status: applied
  adjudication_recommendation: retain_ree
  adjudication_mode: external_precedence_allowed
  allowed_conflict_outcomes:
    - retain_ree
    - hybridize
    - retire_ree_claim
  cascade_on_outcomes:
    - retire_ree_claim
  dependency_reopen_status: candidate
  evidence_quality_note: "Prior 38 synthetic runs invalidated (ree-v2/ree-experiments-lab, archived 2026-02-26). Genuine ree-v1-minimal experiment completed 2026-02-26 (control_plane_precision_separation, EVB-0037): PASS — |corr(score_dispersion, PE)| = 0.067 (<0.3), SEPARATED harm 0.663 vs MERGED 0.875. Both mechanistic criteria met. Revalidation audit 2026-04-25: old jepa_uncertainty_channels rows remain only as stale_epoch audit rows; counted support is the genuine V2/V1-derived precision-separation run."
  depends_on:
    - ARC-005
    - ARC-004
    - ARC-015
    - MECH-054
    - MECH-057a
  what_would_answer: |
    ALREADY CONFIRMED at ree-v1-minimal scale (EVB-0037 PASS, 2026-02-26):
    the confidence/precision channel and residual prediction-error
    channel carry distinct information (|corr(score_dispersion, PE)| =
    0.067, well below the 0.3 threshold), and keeping them SEPARATE is
    operationally load-bearing -- SEPARATED harm 0.663 vs MERGED 0.875
    (lower harm is better here). This what_would_answer targets the
    un-retested V3-scale extension.
    NON-DEGENERACY PRECONDITION: requires a V3 substrate run with both
    the confidence channel and residual-error channel instrumented and
    non-zero variance in their correlation across seeds -- a run where
    the two channels are trivially identical (correlation pinned near
    1.0) cannot discriminate the mechanism.
    CONFIRMING signature: at V3 scale, the same two criteria replicate --
    distinct-signals (|corr| < 0.3) and separation-helps (a SEPARATED
    channel condition measurably outperforms a MERGED condition on task
    harm/performance).
    FALSIFYING: at V3 scale, the confidence channel and residual error
    become highly correlated (|corr| >= 0.3, distinct-signals fails) --
    would suggest the V1-minimal result was scale-specific; or a MERGED
    condition performs AT LEAST as well as SEPARATED at V3 scale -- would
    falsify separation-helps and suggest V1-minimal's simpler substrate
    exaggerated the separation's operational value.
  location: docs/architecture/agency_responsibility_flow.md#mech-059
  source:
    - docs/architecture/agency_responsibility_flow.md
# JEPA flags removed 2026-02-26 — dual error channel concept is REE-native.
# pending_design: requires pure REE implementation without JEPA substrate proxy.
- id: MECH-060
  title: "Pre-commit simulation and post-commit realized-error channels must stay responsibility-distinct via write-boundary enforcement."
  claim_type: mechanism_hypothesis
  subject: commitment.dual_error_channels_pre_post_commit
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-060@2026-02-25T16:35:40.759224Z"
      as_of: 2026-02-25
      verdict: hold_candidate_resolve_conflict/applied
  last_reviewed: 2026-02-25
  lifecycle_stage: adjudicated
  evidence_quality_note: |
    Prior 137 synthetic runs (ree-v2/ree-experiments-lab, archived 2026-02-26) are invalid.
    pending_design cleared 2026-02-27: both channels exist natively in ree-v1-minimal —
    sim_error = E2 harm_predictions on selected trajectory (pre-commit, before env.step),
    realized_error = env harm returned by env.step (post-commit). Write-locus separation
    is enforced by construction (E1 trains only on actual observations via compute_prediction_loss,
    residue accumulates only via update_residue with post-commit harm). MECH-061 PASS (EXQ-003)
    confirms channels are genuinely distinct (|corr|=0.114) and separation is beneficial.
    Genuine ree-v1-minimal experiment completed 2026-02-27 (EVB-0043, write_locus_contamination,
    EXQ-005): PASS. 200 eps × 3 seeds.
    FULL last-Q harm 0.929 | CONT_DURABLE 0.915 | CONT_RESIDUE 0.593.
    Criterion 1 (residue inflation): CONT_RESIDUE total_residue 8520 > FULL 186 × 1.1 = 205 — MET.
    Criterion 2 (harm ordering): FULL 0.929 ≤ max_cont 0.915 × 1.05 = 0.960 — MET.
    Write-locus contamination is detectable: pre-commit residue pollution inflates the residue
    field 46× vs clean. FULL condition harm ordering is sustained, confirming write-locus
    separation is architecturally load-bearing. Promoted candidate → provisional 2026-02-27.
    Revalidation audit 2026-04-25: old commit_dual_error_channels rows remain only as
    stale_epoch audit rows; counted support is 20260314T235933_write_locus_contamination_v2.
  adjudication_outcome: hybridize
  adjudicated_at_utc: 2026-02-25T16:33:23.830904Z
  adjudication_decision_status: applied
  adjudication_recommendation: hybridize
  adjudication_mode: external_precedence_allowed
  allowed_conflict_outcomes:
    - retain_ree
    - hybridize
    - retire_ree_claim
  cascade_on_outcomes:
    - retire_ree_claim
  dependency_reopen_status: candidate
  depends_on:
    - ARC-003
    - ARC-005
    - ARC-015
    - INV-012
    - MECH-057a
  what_would_answer: |
    ALREADY CONFIRMED at ree-v1-minimal scale (EVB-0043/EXQ-005 PASS,
    2026-02-27): write-locus contamination is DETECTABLE -- a
    contaminated (pre-commit sim_error leaking into the persistent
    residue field) condition shows total_residue inflated 46x versus
    clean (CONT_RESIDUE 8520 vs FULL 186), and the clean (write-boundary
    enforced) condition sustains BETTER harm ordering (FULL 0.929 <=
    max_contaminated x 1.05). This what_would_answer targets the
    un-retested V3-scale extension.
    NON-DEGENERACY PRECONDITION: requires a V3 substrate run with both a
    clean (write-boundary enforced) condition and a deliberately
    contaminated (pre-commit sim_error allowed to write into the
    persistent residue field) condition, with non-zero variance in
    residue accumulation and harm ordering across seeds -- a run where
    residue is flat regardless of contamination cannot discriminate the
    mechanism.
    CONFIRMING signature: at V3 scale, the same two criteria replicate --
    residue inflation (contaminated condition's total_residue
    substantially exceeds clean's) and harm ordering (clean condition's
    harm outcome is at least as good as contaminated, within a small
    margin).
    FALSIFYING: at V3 scale, contamination produces no measurable residue
    inflation (write-locus separation has no detectable effect on the
    residue field) -- would suggest the V1-minimal result was
    scale-specific; or the contaminated condition performs AT LEAST as
    well as clean on harm ordering -- would falsify write-boundary
    enforcement as operationally load-bearing at V3 scale.
  location: docs/architecture/agency_responsibility_flow.md#mech-060
  source:
    - docs/architecture/agency_responsibility_flow.md
    - docs/thoughts/2026-02-13_subjective_experience_pre_post_commit.md
    - docs/thoughts/2026-02-23_some_subjective_experience_mapping.md
    - docs/thoughts/2026-02-24_determinism_action_gating_boundary.md
# Promoted 2026-02-26 — retain_ree adjudication was applied 2026-02-15
- id: MECH-061
  title: "Commit-boundary token reclassifies pre-commit vs post-commit error routing."
  claim_type: mechanism_hypothesis
  subject: commitment.boundary_token_error_reclassification
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-061@2026-02-25T16:56:17.938917Z"
      as_of: 2026-02-25
      verdict: hold_candidate_resolve_conflict/applied
  last_reviewed: 2026-02-15
  lifecycle_stage: active
  adjudication_outcome: retain_ree
  adjudicated_at_utc: 2026-02-15T18:46:42.773429Z
  adjudication_decision_status: applied
  evidence_quality_note: |
    Prior 8 synthetic runs (ree-v2/ree-experiments-lab, archived 2026-02-26) are invalid.
    Genuine ree-v1-minimal experiment completed 2026-02-26 (EVB-0041, commitment_boundary_validation): PASS.
    WITH-BOUNDARY last-Q harm 0.805 vs BLENDED 0.935 (3 seeds). Mean |pre_post_corr| = 0.044
    (well below 0.7 threshold). Both criteria met: distinct-signals (|corr| < 0.7 confirms pre-commit
    E2 predictions and post-commit realized harm carry distinct information) and boundary-helps
    (clean separation enables better policy learning than blending). The commit boundary is
    operationally load-bearing at ree-v1-minimal scale — not a cosmetic architectural feature.
    Validates retain_ree adjudication. Status remains active.
  what_would_answer: |
    ALREADY CONFIRMED at ree-v1-minimal scale (EVB-0041 PASS, 2026-02-26):
    pre-commit E2 predictions and post-commit realized harm carry distinct
    information (mean |pre_post_corr|=0.044, well below the 0.7 threshold),
    and the commit boundary is operationally load-bearing -- WITH-BOUNDARY
    harm 0.805 vs BLENDED 0.935 (3 seeds), i.e. boundary-helps confirmed.
    This what_would_answer targets the un-retested V3-scale extension.
    NON-DEGENERACY PRECONDITION: requires a V3 substrate run with both
    pre-commit and post-commit error streams instrumented and non-zero
    variance in the pre/post correlation across seeds -- a run where the
    two streams are trivially identical (correlation pinned at 1.0) or
    trivially uncorrelated by construction (no shared signal at all) cannot
    discriminate the mechanism.
    CONFIRMING signature: at V3 scale, the same two criteria replicate --
    distinct-signals (|pre_post_corr| < 0.7) and boundary-helps
    (WITH-BOUNDARY policy learning measurably outperforms a BLENDED/
    no-boundary control on the same task).
    FALSIFYING: at V3 scale, pre-commit and post-commit error streams
    become highly correlated (|corr| >= 0.7, the distinct-signals criterion
    fails) -- would suggest the V1-minimal result was scale-specific and
    the commit-boundary token is not carrying independent information at
    V3's richer representation; or a BLENDED (no-boundary) control performs
    AT LEAST as well as WITH-BOUNDARY at V3 scale -- would falsify
    boundary-helps and suggest V1-minimal's simpler substrate exaggerated
    the boundary's operational value.
  adjudication_recommendation: retain_ree
  depends_on:
    - ARC-003
    - ARC-015
    - INV-012
    - MECH-060
  location: docs/architecture/e3.md#mech-061
  source:
    - docs/architecture/e3.md
    - docs/thoughts/2026-02-15_basal_ganglia.md
    - docs/thoughts/2026-02-15_basal_ganglia_commit_gating_control_plane_axes.md
    - docs/thoughts/2026-02-24_determinism_action_gating_boundary.md
    - docs/thoughts/2026-02-25_task_loop_extraction_and_latent_field_ethics.md
- id: MECH-062
  title: "E3 uses coordinated tri-loop gating (motor, cognitive-set, motivational) as the pre-commit eligibility layer."
  claim_type: mechanism_hypothesis
  subject: commitment.tri_loop_gate_coordination
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_502_mech062_tri_loop_gate_coordination_20260429T192736Z_v3
      as_of: 2026-04-29
      verdict: supports/PASS
  status_note: >
    Status corrected stable->candidate 2026-06-22. The BG assembly map
    (evidence/planning/basal_ganglia_assembly_map_2026-06-22.md A.2 + s.D) confirms
    the three loops are COLLAPSED into one E3 selector: the motor / cognitive-set /
    motivational analogs (dACC / lPFC / OFC) feed in as additive BIAS channels, NOT as
    separate striatal loops each owning an eligibility-and-gate. "Coordinated tri-loop
    gating as the pre-commit eligibility layer" therefore over-states what is built.
    The prior `stable` mark was design-doc-strength (docs/architecture/e3.md), never an
    implemented-and-validated separate-loop architecture. The missing SELECTION-STRUCTURE
    segregation is now owned by ARC-110 (registered same pass); the assembly-map s.D
    artefact-of-collapse hypothesis is ARC-110's falsifier (operationalised as V3-EXQ-700).
    PROMOTES NOTHING (status correction to match the substrate, not an evidence-driven demotion).
  what_would_answer: |
    STATUS NOTE (already in this claim, 2026-06-22): the built substrate does
    NOT implement "coordinated tri-loop gating as the pre-commit eligibility
    layer" as literally titled -- the BG assembly map confirms the three
    loops (motor/cognitive-set/motivational) are COLLAPSED into ONE E3
    selector, feeding in as additive BIAS channels, not as separate striatal
    loops each owning an eligibility gate. The falsifiable question this
    claim's title implies -- whether the collapse is an ARTEFACT (a missing
    SELECTION-STRUCTURE segregation) or the correct architecture -- now
    belongs to ARC-110, whose falsifier is V3-EXQ-700. This what_would_answer
    is scoped to MECH-062's own narrower claim, not ARC-110's.
    NON-DEGENERACY PRECONDITION: requires ARC-110's segregated-loop
    substrate to exist and be compared against the current collapsed
    single-selector baseline with non-zero cross-condition variance on
    committed-selection behaviour.
    CONFIRMING signature (for MECH-062 as distinct from ARC-110): once a
    genuinely-segregated tri-loop substrate exists, it reproduces the
    ORIGINAL PASS evidence this claim already carries (V3-EXQ-502,
    2026-04-29 supports/PASS) at least as well as the collapsed
    single-selector does -- segregation does not regress the
    already-demonstrated coordination behaviour.
    FALSIFYING: a genuinely-segregated tri-loop substrate performs WORSE
    than the collapsed single-selector on the same coordination behaviour
    V3-EXQ-502 validated -- would suggest the collapse is not an artefact
    but the more parsimonious correct architecture, and MECH-062 as
    literally titled should be retired/merged into whatever framing
    ARC-110 settles on.
  depends_on:
    - ARC-003
    - ARC-005
    - MECH-061
  location: docs/architecture/e3.md#mech-062
  source:
    - docs/architecture/e3.md
    - docs/thoughts/2026-02-15_basal_ganglia.md
    - docs/thoughts/2026-02-15_basal_ganglia_commit_gating_control_plane_axes.md
    - docs/thoughts/2026-02-13_subjective_experience_pre_post_commit.md
    - docs/thoughts/2026-02-23_some_subjective_experience_mapping.md
    - docs/thoughts/2026-02-24_prefrontal_primitives.md
    - docs/thoughts/2026-02-25_task_loop_extraction_and_latent_field_ethics.md
- id: MECH-063
  title: "Control plane retains orthogonal tonic/phasic axes rather than collapsing into one scalar."
  claim_type: mechanism_hypothesis
  subject: control_plane.orthogonal_axes_tonic_phasic
  polarity: asserts
  status: provisional
  evidence_quality_note: >
    [2026-08-16 governance, GFLAG-0036, cycle cranky-driscoll-126a36] V3-EXQ-784 (SD-074 probe_warmup de-saturation sweep, PASS 2026-07-18) is downgraded to PROVISIONAL: its headline informative_yield / target_met rest on a premise the manifest itself states and which is FALSE. sd074_note asserts that measure_action_mass is non-destructive, so reading at one checkpoint leaves the agent bit-identical for continued training. Measured on ree-cloud-5 2026-08-15: a probe read leaves 21 non-state_dict attributes changed, 17 of which SURVIVE agent.reset(). The load-bearing ones are agent._self_experience_buffer / _world_experience_buffer, which warmup_train samples via agent.compute_prediction_loss() -- so eval-rollout data enters the E1 training pool. Those buffers cap at 1000 while 784 reads 1081-1401 env steps, i.e. FULL DISPLACEMENT of the training pool by evaluation data. A controlled 2-seed replication of 784 ladder at its real config (budget-0 control cells bit-identical, as required) shows all six post-first-read cells differing by 0.011-0.146 on D_action_mass and TWO OF SIX FLIPPING THE SATURATION REGIME -- exactly the classification 784 informative_yield headline counts. No status change and no evidence_direction change is applied on this basis alone: the defect is in the measurement premise, not in the claim, and re-deriving the corrected yield needs the fix plus a re-run. Treat any downstream citation of 784 informative_yield / target_met as provisional until then. Full write-up and recommended fix: evidence/planning/probe_warmup_nonbuffer_audit_2026-08-15.md.
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-779b_2026-07-19
      as_of: 2026-07-19
      verdict: non_contributory/substrate_ceiling
  depends_on:
    - ARC-005
    - MECH-039
    - MECH-040
    - MECH-055
  location: docs/architecture/control_plane.md#mech-063
  source:
    - docs/architecture/control_plane.md
    - docs/thoughts/2026-02-15_basal_ganglia.md
    - docs/thoughts/2026-02-15_basal_ganglia_commit_gating_control_plane_axes.md
    - docs/thoughts/2026-02-13_subjective_experience_pre_post_commit.md
    - docs/thoughts/2026-02-23_some_subjective_experience_mapping.md
  implementation_note: >
    Sub-claim (i) orthogonal-axes covered behaviourally by V3-EXQ-777. Sub-claim
    (ii) TONIC/PHASIC SPLIT is now substrate-buildable: SD-069
    (control_plane.phasic_surprise_burst, IMPLEMENTED 2026-07-17) adds the missing
    toggleable PHASIC event-burst lever on the E3 softmax temperature -- the phasic
    complement to MECH-313 stochastic_noise_floor (tonic) on the same readout, so
    tonic-vs-phasic become independently toggleable and behaviourally dissociable.
    No status/confidence change from this landing (substrate only; the sub-claim (ii)
    dissociation experiment is queued 2026-07-17). See
    docs/architecture/sd_069_phasic_surprise_burst.md.
  granularity_debt_disposition: coherent_campaign
  granularity_debt_recurrence_note: >
    ADJUDICATED 2026-07-19 (governance cycle, user-directed) against the GOV-GRAN-1 P1
    unflagged-recurrence flag: 3 no-verdict hits across 2 distinct signatures
    (V3-EXQ-777 -> V3-EXQ-779 -> V3-EXQ-779a). VERDICT: coherent substrate-build campaign,
    NOT granularity debt -- so no /claim-synthesis decomposition is owed. Reasoning: the two
    signatures are not two bundled mechanisms surfacing separately, they are PROGRESSIVE
    LOCALIZATION of one instrumentation gap. Signature 1 (the 777 lineage) localized to probe
    telemetry collected against an UNTRAINED agent whose action-value distribution is degenerate
    on ~64% of seeds, capping informative-seed yield at 28.6%. Signature 2 (the 779 lineage)
    localized to a phasic surprise-EMA baseline cleared at every episode boundary, so
    short-episode seeds structurally under-report event ticks. Each successive letter narrowed
    the gap rather than circling it, and BOTH now terminate in a NAMED, buildable substrate
    owner: SD-PROBE-WARMUP and sd_phasic_ema_episode_continuity respectively (both created in
    substrate_queue.json this cycle, priority 1, node_class `complicated (buildable)`).
    Sub-claims (i) orthogonal-axes and (ii) tonic/phasic split remain a coherent single claim
    about one control plane; splitting them would not have produced either build. PROMOTES AND
    DEMOTES NOTHING; epistemic_category untouched, so this seeds no GOV-CEIL-1 count. Note the
    separate re-derive-brake position recorded in `notes`: a further same-granularity lettered
    iteration is REFUSED in favour of these two builds.
  notes: >
    Sub-claims (i) and (ii) both remain experimentally untested as of 2026-07-18. V3-EXQ-777 and
    V3-EXQ-779 were adjudicated non_contributory (measurement_test_design_defect) by
    failure_autopsy_MECH-063-777-779-cluster_2026-07-18: a shared telemetry-probe template
    denominates its budget in episodes while its gates are denominated in selections, and
    untrained agents in a hazard-terminating env yield 2-100% of budget depending on seed.
    Retests V3-EXQ-777a / 779a pending. Biological warrant remains strong (LC-NE tonic/phasic
    adaptive gain); the claim is untested, not unsupported.
    2026-07-18 UPDATE (sub-claim (i) only): V3-EXQ-777a RAN and was adjudicated
    non_contributory / substrate_ceiling by
    failure_autopsy_MECH-063-777a-779a-cluster_2026-07-18. Its predecessor sampling defect
    was genuinely repaired (all 56 cells reached 250 fresh E3 selections, zero starved), and
    C1 did NOT fail on collinearity -- mean_sin_angle 0.5454 EXCEEDS the 0.500 margin and
    c1_seed_count PASSED; the single best-conditioned seed (17) gave sin_angle 0.9883, i.e.
    near-perfect orthogonality. The sole failing bar was c1_robust, a population-SD
    expression unreachable at any n. The binding constraint is substrate: informative-seed
    yield 4 of 14 (28.6%), with corr(D-saturation-distance, norm_v_score) = 0.884. The
    re-derive brake FIRED on the 777 lineage (user-confirmed "Fire on 777 lineage, exempt
    779") and routed to SD-074 (probe.trained_enough_agent_warmup, IMPLEMENTED 2026-07-18);
    a further lettered iteration against an UNTRAINED agent is REFUSED. No status or
    confidence change from that landing (substrate only). Sub-claim (ii) / the 779 lineage is
    explicitly EXEMPT and is NOT blocked on SD-074.
    2026-07-19 UPDATE (sub-claim (ii), the 779 lineage). V3-EXQ-779a: adjudicated
    non_contributory / measurement_test_design_defect by
    failure_autopsy_MECH-063-777a-779a-cluster_2026-07-18. The direction is unchanged, but the
    run is much CLOSER to a result than the bare FAIL suggests -- the load-bearing criterion
    double_dissociation_C1_and_C2 PASSED. Routes to /queue-experiment for a redesign, not
    another blind letter. V3-EXQ-779b: adjudicated non_contributory / substrate_ceiling by
    failure_autopsy_V3-EXQ-779b_2026-07-19, with pending_retest_after_substrate. The 779a
    episode-cap fix VERIFIABLY took effect (seed 23 / T1P1: 835 -> 2400 env steps, stop_reason
    episode_cap -> step_cap, episode_cap_can_bind False) and the precondition still did not
    move -- so the gate is LOAD-BEARING, not spurious, and the exposure fix is not the missing
    ingredient. Routed to /implement-substrate on the new substrate_queue entry
    sd_phasic_ema_episode_continuity; 777a likewise carries a create for SD-PROBE-WARMUP.
    NOTE FOR THE NEXT AUTOPSY: the 777a/779a/779b readings bring MECH-063 to THREE
    non_contributory adjudications (two of them substrate_ceiling), so the re-derive brake is
    at or past threshold on this claim -- a further same-granularity lettered iteration should
    be REFUSED in favour of the two substrate builds above. Sub-claims (i) and (ii) both remain
    experimentally untested; the claim is untested, not unsupported.
  what_would_answer: |
    SUBSTRATE PRECONDITION (two independent sub-claims, each with a NAMED
    buildable fix -- do not re-litigate with another lettered iteration
    per the 2026-07-19 re-derive-brake note): sub-claim (i), orthogonal
    axes, requires SD-074 (probe.trained_enough_agent_warmup, IMPLEMENTED
    2026-07-18) -- V3-EXQ-777a already showed near-perfect orthogonality
    on the best-conditioned seed (sin_angle 0.9883) but was capped by
    informative-seed yield (28.6%) from an untrained-agent sampling
    defect, now repaired by SD-074. Sub-claim (ii), tonic/phasic split,
    requires sd_phasic_ema_episode_continuity (routed to
    /implement-substrate 2026-07-19) -- V3-EXQ-779b already verified the
    episode-cap exposure fix took effect (stop_reason
    episode_cap->step_cap) yet the precondition still did not move, so
    the phasic-EMA-continuity gate is LOAD-BEARING, not spurious.
    CONFIRMING signature (i): once SD-074-warmed agents are used, the
    population-level c1_robust criterion (not just the single best seed)
    clears the orthogonality margin across a representative seed sample
    -- the tonic and phasic axes are genuinely dissociable directions in
    control-plane space, not collinear.
    CONFIRMING signature (ii): once sd_phasic_ema_episode_continuity
    lands, the phasic-surprise EMA baseline persists correctly across
    episode boundaries and the tonic (MECH-313)/phasic (SD-069) axes
    become independently toggleable with behaviourally DISSOCIABLE
    effects (toggling one without the other produces a measurably
    different behavioural signature).
    FALSIFYING (i): even with SD-074-warmed agents and adequate
    informative-seed yield, sin_angle remains below the orthogonality
    margin population-wide -- the two axes collapse toward one scalar
    direction.
    FALSIFYING (ii): once episode-continuity is fixed, toggling tonic vs
    phasic axes independently produces NO measurable behavioural
    dissociation -- the two axes are structurally distinct but not
    functionally separable.
- id: MECH-064
  title: "Typed authority/control-store separation blocks direct exteroceptive writes into policy and identity stores."
  claim_type: mechanism_hypothesis
  subject: control_plane.typed_authority_control_store_separation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-005
    - ARC-003
    - ARC-015
    - INV-014
    - INV-007
    - MECH-062
  location: docs/architecture/control_plane_signal_map.md#mech-064
  source:
    - docs/architecture/control_plane_signal_map.md
    - docs/thoughts/17-02-26_necessary_separations_based_on_considering-prompt_injection.md
- id: MECH-065
  title: "Reality-coherence conflict lane modulates loop precision and commitment thresholds before execution lock-in."
  claim_type: mechanism_hypothesis
  subject: control_plane.reality_coherence_conflict_lane
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-005
    - ARC-007
    - ARC-018
    - MECH-037
    - MECH-054
    - MECH-062
  notes: >
    Literature grounding (2026-03-29, Q-018 lit pull): Sterzer et al. (Front Hum
    Neurosci 2016) reframes the RC-conflict threshold as a precision RATIO rather
    than an absolute signal strength -- thought insertion and misattribution arise
    when the ratio of internal-model precision to external-authority precision is
    miscalibrated. Howes & Murray (Biol Psych 2020) show that early adverse
    experience systematically lowers this ratio threshold (aberrant salience),
    and that the CHR-to-psychosis transition is the hysteresis zone corresponding
    to a threshold that is both too low to resist spoofing and too unstable to
    self-correct. Damiani et al. (meta-analysis 2022) confirms internal source
    monitoring is the most vulnerable calibration target (SMD ~0.5-1.0 deficit).
    Implementation implication: the RC-conflict lane threshold should be
    implemented as ratio(E1_precision, authority_signal_precision), not an
    absolute authority-signal magnitude. This directly addresses Q-018's
    calibration question.
    Clinical analogue: authority_spoofing_low_threshold -> psychosis/paranoia;
    over_suppression_high_threshold -> rigid_hypervigilance/anhedonia.
    Clinical analogues are tracked as notes rather than a separate field -- the
    psychiatric symptom mapping is evidence for the architectural claim, not a
    separate claim type.
  what_would_answer: |
    NON-DEGENERACY PRECONDITION (else substrate_not_ready, NOT a verdict):
    MECH-065 is currently a schematic design claim -- the RC_conflict signal, the
    DA_A/DA_M lock-in-pressure dampening, the commitment-threshold (tau/theta_high/
    theta_low) hysteresis, and the Pi_rc guarded floor are all still on the
    control-plane doc's own TODO list ("formalise control plane state variables",
    "specify update equations... for S5 reality-coherence conflict computation").
    A test is informative only once: (a) RC_conflict is computed as an explicit,
    independently-readable channel (not inferred post-hoc from downstream behaviour),
    with live non-zero variance across an authority/provenance-mismatch manipulation
    -- a channel that never moves cannot be shown to modulate anything, and any run
    against a flat/uncomputed RC_conflict channel self-routes substrate_not_ready,
    not FAIL; and (b) commitment threshold and DA_A/DA_M lock-in pressure are
    themselves separately measurable at the pre-commit stage, so a shift in them can
    be attributed to RC_conflict specifically rather than read off final behavioural
    outcome. Two of the six dependencies (ARC-007, ARC-018) are held on the
    V3-Pending Gate as of this digestion -- their resolution is upstream of a clean
    test here, since MECH-065's provenance/authority framing leans on both.
    CONFIRMING signature: under a manipulation that varies authority/provenance
    mismatch at matched surface content (e.g. spoofed vs. verified source signal on
    an otherwise identical instruction/observation), RC_conflict rises measurably,
    DA_A/DA_M lock-in pressure is measurably dampened, and the effective commitment
    threshold is measurably raised -- all BEFORE execution lock-in (pre-commit-stage
    change, not a downstream behavioural correlate) -- with the system biasing toward
    verification/exploration rather than premature commit. Per the claim's own
    literature grounding (Sterzer et al. 2016), the effect should track the
    PRECISION RATIO of internal-model precision to authority-signal precision, not
    authority-signal magnitude alone: an arm that holds absolute authority-signal
    strength fixed while varying internal (E1) precision should shift the threshold
    the same way as an arm that holds internal precision fixed and varies authority
    signal strength, provided the ratio moves equivalently. The response should also
    show hysteresis (theta_high > theta_low, bounded recovery via tau_rc_recovery)
    rather than a single crisp threshold, and should never suppress reality-conflict
    sensitivity below the guarded floor (Pi_rc >= pi_rc_floor) under sustained
    exteroceptive pressure.
    FALSIFYING signature: if the provenance/authority-mismatch manipulation produces
    no measurable pre-commit change in DA_A/DA_M or commitment threshold -- i.e.
    RC_conflict, even when computed and live, has no separable causal effect on
    lock-in pressure or threshold -- the claim is refuted: reality-coherence
    conflict does not constitute a distinct control-plane lane and any observed
    downstream effect is better explained by existing signals (S1/S3) alone. Also
    falsifying if the threshold shift tracks absolute authority-signal magnitude
    independent of internal precision (i.e. the ratio manipulation described above
    does NOT dissociate, so magnitude alone predicts the shift as well as the ratio
    does) -- this would refute the specific ratio-based account MECH-065 currently
    rests on (Sterzer et al.), even if some cruder RC-conflict effect survives. Also
    falsifying if the response shows no hysteresis (theta_high == theta_low, or
    chronic drift below pi_rc_floor under sustained pressure) -- the design
    constraint that reality-conflict must not chronically over-suppress from
    transient ambiguity would then be unmet.
    EVIDENCE STATUS: no experimental evidence exists for this claim as of this
    digestion (2026-08-06) -- only literature grounding (Sterzer et al. 2016, Howes
    & Murray 2020, Damiani et al. 2022) supporting the ratio-based reframing of the
    threshold, and a clinical-analogue mapping. That literature is supportive of the
    general precision-miscalibration framing but does not itself test REE's specific
    three-signal (provenance/temporal/identity/policy) carve of RC_conflict -- treat
    it as motivating, not confirming.
  location: docs/architecture/control_plane_signal_map.md#mech-065
  source:
    - docs/architecture/control_plane_signal_map.md
    - docs/thoughts/2026-02-17_control_plane_update.md
    - docs/thoughts/17-02-26_necessary_separations_based_on_considering-prompt_injection.md
- id: MECH-066
  title: "Pre-commit and post-commit channels may share representations but must stay separated at durable write boundaries."
  claim_type: mechanism_hypothesis
  subject: agency.boundary_conditioned_channel_separation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-015
    - MECH-060
    - MECH-061
    - INV-021
  location: docs/architecture/agency_responsibility_flow.md#mech-066
  source:
    - docs/architecture/agency_responsibility_flow.md
    - docs/invariants.md
- id: MECH-067
  title: "A machine-checkable phase/store/actor permission matrix is required to enforce commit-boundary write rules."
  claim_type: mechanism_hypothesis
  subject: agency.write_locus_permission_matrix
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  evidence_quality_note: |
    Co-tested with MECH-060 in genuine ree-v1-minimal experiment 2026-02-27 (EVB-0043,
    write_locus_contamination, EXQ-005): PASS. The write-locus permission matrix is
    operationally validated: pre-commit and post-commit write loci are genuinely distinct,
    and contaminating the residue write locus (pre-commit) inflates the residue field
    (8520× vs FULL 186) without corresponding harm reduction — confirming that post-commit
    is the correct write locus for durable harm attribution. Promoted candidate → provisional
    2026-02-27.
  depends_on:
    - MECH-060
    - MECH-061
    - MECH-066
    - INV-020
    - INV-021
  location: docs/architecture/agency_responsibility_flow.md#mech-067
  source:
    - docs/architecture/agency_responsibility_flow.md
    - docs/architecture/control_plane_signal_map.md
- id: MECH-068
  title: "Consolidation selectivity lives in the consolidation operator, not in E1 feature basis."
  claim_type: mechanism_hypothesis
  subject: representation.consolidation_selectivity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-001
    - ARC-003
    - ARC-005
    - INV-014
    - MECH-062
    - MECH-063
  location: docs/architecture/compact_consolidation_principle.md#mech-068
  source:
    - docs/architecture/compact_consolidation_principle.md
    - evidence/literature/visual_cortex_compact_models/entries/2026-02-26_cowley_2023_compact_v4/record.json
  evidence_quality_note: |
    [2026-07-29 governance, V3-EXQ-835 supports; PASS, all criteria clean]: first experimental test.
    Consolidation-selectivity ablation on the E3 candidate-selection operator: the load-bearing
    operator_selection_authority_flip_rate criterion passes well above floor (0.697 vs a 0.05
    threshold), with both non-degeneracy preconditions clearing cleanly (channel range 0.349 >>
    0.0001 floor; mean candidate count 32.0 >> the 2.0 structural floor). Supports the claim that
    selectivity lives in the consolidation operator's selection authority, not in the E1 feature
    basis. Stays candidate.
# Demoted 2026-02-26 — accepted governance recommendation. Evidence 4:4 split (conflict_ratio 1.0,
# confidence 0.503). Insufficient signal to hold as active question. Needs discriminative experiment.
- id: Q-012
  title: "Can latent predictive world models stay agentically stable without explicit REE-like control constraints?"
  claim_type: open_question
  subject: latent_predictive_models.control_completion_falsifiability
  polarity: asks
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-012@2026-03-02T00:00:00.000000Z"
      as_of: 2026-03-02
      verdict: demote_to_candidate/applied
  last_reviewed: 2026-02-15
  lifecycle_stage: adjudicated
  adjudication_outcome: retain_ree
  adjudicated_at_utc: 2026-02-15T18:46:42.773429Z
  adjudication_decision_status: applied
  adjudication_recommendation: retain_ree
  evidence_quality_note: |
    All 8 prior experimental entries are synthetic (ree-v2, _toyenv_internal_minimal run_ids,
    archived 2026-02-26). The 4:4 supports/weakens split and 0.503 confidence score are
    entirely synthetic artefacts -- do NOT act on any demote recommendation based on this data.
    3 literature entries are valid (2 supports, 1 mixed) and insufficient alone to resolve the
    question. Retain candidate status pending genuine ree-v1-minimal runs.
    EXQ-152 FAIL/mixed (2026-03-29): REE_FULL vs PREDICTIVE_ONLY delta_harm=0; superseded
    by EXQ-193 because action selection remained effectively random across conditions.
    EXQ-193 FAIL/mixed (2026-04-04): REE_FULL vs PREDICTIVE_ONLY vs NO_LEARNING (3 conditions).
    C1 PASS 3/3: REE_FULL outperforms PREDICTIVE_ONLY by 7.5 harm units (11.96 vs 19.47);
    PREDICTIVE_ONLY harm INCREASES over training while REE_FULL improves -- strong directional
    signal that control constraints matter. C2 FAIL: REE_FULL does not beat NO_LEARNING floor
    in all seeds (2/3 pass). evidence_direction=mixed. The C1 PASS is the primary Q-012 signal:
    pure predictive (no control constraints) destabilises over time while REE does not. This is
    the first experiment showing a genuine positive REE-vs-predictive gap at current training
    scale. Consistent with retain_ree adjudication.
    Revalidation audit 2026-04-25: synthetic Q-012 rows remain only as stale_epoch audit rows;
    EXQ-152 is scoring-excluded as superseded; EXQ-193 is the counted mixed V3 result.
  depends_on:
    - MECH-057a
    - ARC-015
    - ARC-005
    - ARC-004
  location: docs/architecture/agency_responsibility_flow.md#q-012
  source:
    - docs/thoughts/2026-02-13_LeCun_developed_lots_of_REE.md
# Q-013 retired 2026-03-29 -- JEPA integration decoupled from REE.
# The uncertainty calibration question is moot without a JEPA substrate.
# The REE-native precision routing question is now addressed by MECH-059 directly.
- id: Q-013
  title: "Retired external-substrate uncertainty calibration question; REE-native precision routing is tracked by MECH-059."
  claim_type: open_question
  subject: uncertainty.external_substrate_calibration_legacy
  polarity: asks
  status: legacy
  live_status:
    reading: legacy
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_153_q013_uncertainty_calibration_20260329T132242Z_v3
      as_of: 2026-03-29
      verdict: supports/PASS
  last_reviewed: 2026-02-25
  lifecycle_stage: retired
  adjudication_outcome: hybridize
  adjudicated_at_utc: 2026-02-25T16:51:50.720541Z
  adjudication_decision_status: applied
  adjudication_recommendation: hybridize
  evidence_quality_note: >
    JEPA integration dropped from REE architecture 2026-03-29. This question is
    moot. All prior experimental evidence (jepa_uncertainty_channels, 126 runs)
    was from ree-v2 synthetic scaffolding and is invalid. REE-native precision
    routing is addressed by MECH-059 directly. V3-EXQ-153 was a post-retirement
    external-substrate calibration proxy and is scoring-excluded for Q-013.
    Retired.
  depends_on:
    - MECH-059
    - ARC-005
    - ARC-004
  location: docs/architecture/agency_responsibility_flow.md#q-013
  source:
    - docs/architecture/agency_responsibility_flow.md
# Q-014 retired 2026-03-29 -- JEPA integration decoupled from REE.
# The JEPA-framed question is moot, but the underlying insight (representation
# invariances can hide ethically relevant distinctions in attribution) is
# REE-native and transferred to MECH-137a (see below).
- id: Q-014
  title: "Retired representation-invariance blind-spot question; REE-native concern moved to MECH-128 and SD-008."
  claim_type: open_question
  subject: invariance.ethical_relevance_blind_spot_risk
  polarity: asks
  status: legacy
  live_status:
    reading: legacy
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-014@2026-02-25T16:51:50.758372Z"
      as_of: 2026-02-25
      verdict: hybridize/applied
  last_reviewed: 2026-02-25
  lifecycle_stage: retired
  adjudication_outcome: hybridize
  adjudicated_at_utc: 2026-02-25T16:51:50.758372Z
  adjudication_decision_status: applied
  adjudication_recommendation: hybridize
  evidence_quality_note: >
    JEPA integration dropped from REE architecture 2026-03-29. The JEPA-specific
    framing is moot. The core insight -- that representation learning invariances
    can create attribution blind spots -- survives as a REE-native concern.
    Wang et al. (NeurIPS 2024) and Mehrabi et al. (ACM Surveys 2022) provide
    the relevant grounding. The REE-native formulation is: z_world encoder
    invariances that discard harm-predictive features create structural moral
    blind spots in E3 harm evaluation. This is addressed by MECH-128 (E1 goal
    conditioning) and the SD-008/SD-009 encoder design requirements.
    Retired as JEPA question; REE-native blind-spot concern tracked via
    evidence_quality_note on MECH-128 and SD-008. Prior synthetic evidence invalid.
  depends_on:
    - MECH-057a
    - MECH-059
    - ARC-015
    - ARC-004
  location: docs/architecture/agency_responsibility_flow.md#q-014
  source:
    - docs/architecture/agency_responsibility_flow.md
- id: Q-015
  title: "What is the smallest commit-boundary token that still supports reliable multi-timescale attribution?"
  claim_type: open_question
  subject: commitment.boundary_token_minimal_contract
  polarity: asks
  status: active
  live_status:
    reading: active/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-015@2026-02-15T18:46:42.773429Z"
      as_of: 2026-02-15
      verdict: retain_ree/applied
  last_reviewed: 2026-02-15
  epistemic_category: substrate_conditional
  what_would_answer: |
    PARTIALLY answered already (EXQ-003 / MECH-061, ree-v1-minimal): the minimal
    contract requires at least that pre-commit and post-commit signals stay
    uncorrelated (|corr| < 0.7) and that routing them separately beats blending.
    The OPEN part -- the smallest token granularity that still works -- needs a
    substrate that does not yet exist: a tunable commit-boundary granularity knob.
    Falsifier (once that knob exists): sweep commit-boundary granularity coarse
    (whole-trajectory) -> fine (per-step), task fixed; reliability = rank-corr
    between credit assigned to a commit-token and that token's ground-truth outcome
    contribution, at >=2 timescales (within-commit, across-commit). The answer is the
    SMALLEST granularity at which reliability stays above floor (rank-corr > 0.5) at
    both timescales; below it attribution degrades to chance. Non-degeneracy
    precondition: the coarsest arm must itself attribute reliably. Substrate-blocked:
    V3 hard-codes the commit-token unit; no commit-boundary-granularity knob is in
    substrate_queue yet. (Routed substrate_conditional per /thought-digestion 2026-06-11.)
  lifecycle_stage: adjudicated
  adjudication_outcome: retain_ree
  adjudicated_at_utc: 2026-02-15T18:46:42.773429Z
  adjudication_decision_status: applied
  adjudication_recommendation: retain_ree
  evidence_quality_note: |
    Prior experimental evidence from claim_probe_mech_061 (8 runs) was from ree-v2 synthetic
    scaffolding (archived 2026-02-26) — those entries are invalid.
    Genuine ree-v1-minimal experiment completed 2026-02-26 (EVB-0041, commitment_boundary_validation,
    EXQ-003): PASS — MECH-061 confirmed. WITH-BOUNDARY harm < BLENDED, |corr|=0.114.
    This partially answers Q-015: the minimal contract requires at minimum that pre-commit and
    post-commit signals remain uncorrelated (|corr|<0.7) and that routing them separately
    outperforms blending. Both criteria confirmed at ree-v1-minimal scale.
    Demotion recommendation in promotion_demotion_recommendations.md (conflict_ratio=0.889,
    conf=0.55) is superseded — it was computed from the now-invalid synthetic entries.
    Retain active. Question is partially answered; further experiments on richer substrates
    will tighten the boundary contract definition.
    EXQ-155 SUPERSEDED (2026-03-29): implementation gap -- policy trained only with entropy
    bonus (ENT_BONUS=5e-3), z_world/z_self detached from policy forward pass. Policy converges
    to maximum-entropy random walk (action_entropy=ln(5)=1.6094); commit boundary never
    exercised. Redesign needed with harm-avoidance policy gradient wired to policy. See EXP-0094.
  depends_on:
    - MECH-061
    - ARC-003
    - ARC-015
  location: docs/architecture/e3.md#q-015
  source:
    - docs/architecture/e3.md
    - docs/thoughts/2026-02-15_basal_ganglia_commit_gating_control_plane_axes.md
- id: Q-016
  title: "What arbitration policy best resolves tri-loop gate conflicts without coupling collapse?"
  claim_type: open_question
  subject: commitment.tri_loop_conflict_arbitration_policy
  polarity: asks
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-016@2026-02-25T16:51:50.829631Z"
      as_of: 2026-02-25
      verdict: retain_open_question_with_policy_probe_plan/applied
  what_would_answer: |
    Pit candidate tri-loop arbitration policies against each other on a substrate
    where MECH-062's three gates (gate_motor, gate_cognitive_set,
    gate_motivational) are LIVE -- each consuming its own proposal manifold and
    showing non-zero, independent gating variance -- and a task that deliberately
    drives the gates into conflict (one stream must be suppressed while others
    commit). Candidate policies: explicit recorded arbitration (conflict resolved
    against a commit_id) vs silent-dominance vs winner-take-all vs
    threshold-coupled. ANSWER "policy P best resolves conflict without coupling
    collapse" if under P a suppressed stream does NOT force the other gates into
    suppression (no pathological global coupling: cross-gate suppression
    correlation stays below floor), conflicts are resolved and attributable per
    commit_id, AND committed-action quality on the conflict task beats the
    silent-dominance baseline. ANSWER against any policy that exhibits coupling
    collapse (all gates co-suppress) or fails to record/resolve conflict. HARD
    PRECONDITION (the EXQ-156 lesson): the policy must be trained with a
    harm-avoidance gradient actually wired to it -- EXQ-156 was superseded because
    the policy trained on entropy bonus alone (ENT_BONUS=5e-3) with z_world/z_self
    detached, converged to a max-entropy random walk
    (action_entropy=ln(5)=1.6094), and never GENERATED gate conflicts; with no
    conflicts the arbitration policy is untested and the run is vacuous.
  evidence_quality_note: |
    EXQ-156 SUPERSEDED (2026-03-29): implementation gap -- policy trained only with entropy
    bonus (ENT_BONUS=5e-3), z_world/z_self detached from policy forward pass. Policy converges
    to maximum-entropy random walk (action_entropy=ln(5)=1.6094); tri-loop gate conflicts never
    generated. Redesign needed with harm-avoidance policy gradient wired to policy. See EXP-0094.
  depends_on:
    - MECH-062
    - ARC-003
    - ARC-005
  location: docs/architecture/e3.md#q-016
  source:
    - docs/architecture/e3.md
    - docs/thoughts/2026-02-15_basal_ganglia.md
- id: Q-017
  title: "What is the minimal orthogonal control-axis subset that preserves observed regime separations?"
  claim_type: open_question
  subject: control_plane.minimal_orthogonal_axis_set
  polarity: asks
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-017@2026-02-25T16:51:50.648196Z"
      as_of: 2026-02-25
      verdict: hybridize/applied
  last_reviewed: 2026-02-25
  lifecycle_stage: adjudicated
  adjudication_outcome: hybridize
  adjudicated_at_utc: 2026-02-25T16:51:50.648196Z
  adjudication_decision_status: applied
  adjudication_recommendation: hybridize
  evidence_quality_note: |
    Prior experimental evidence from claim_probe_q_017 / control_axis_ablation (30 runs) was
    from ree-v2 synthetic scaffolding. Adjudication (hybridize) is valid architectural direction.
    Needs genuine ree-v1-minimal control axis experiments.
    EXQ-157 T081401Z SUPERSEDED (2026-03-29): dry_run=true -- no training occurred; all metrics
    zero; result invalid. Manifest marked superseded.
    EXQ-157 T185928Z SUPERSEDED (2026-03-29): implementation gap -- policy trained only with
    entropy bonus (ENT_BONUS=5e-3), z_world/z_self detached from policy forward pass. Policy
    converges to maximum-entropy random walk (action_entropy=ln(5)=1.6094); control axes never
    exercised. Redesign needed with harm-avoidance policy gradient wired to policy. See EXP-0094.
    EXQ-157 INCONCLUSIVE (2026-03-30, T065453Z): first valid run (prior runs superseded).
    PARTIAL_COLLAPSE_ADEQUATE: all three axis conditions (FULL/COLLAPSED/MINIMAL) fall within
    equivalence margin (C5_all_within_equiv=True). C4 (axes non-degenerate) FAIL -- axes likely
    collapsing in this substrate. Task complexity (8x8 grid) insufficient to discriminate axis
    structures; single-stream already saturates task capacity. Classified inconclusive: substrate
    inadequate to test Q-017, not evidence that minimal subset = 1 axis. Redesign needed at higher
    task complexity.
  depends_on:
    - MECH-063
    - ARC-005
    - MECH-055
  location: docs/architecture/control_plane.md#q-017
  source:
    - docs/architecture/control_plane.md
    - docs/thoughts/2026-02-15_basal_ganglia_commit_gating_control_plane_axes.md
- id: Q-018
  title: "What RC-conflict threshold calibration blocks authority spoofing without chronic over-suppression?"
  claim_type: open_question
  subject: control_plane.reality_conflict_hysteresis_calibration
  polarity: asks
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_158_q018_rc_conflict_threshold_calibration_20260329T192854Z_v3
      as_of: 2026-03-29
      verdict: supports/PASS
  what_would_answer: |
    Inject authority/provenance-spoofing events across a calibrated sweep of the
    RC-conflict hysteresis hooks (theta_high, theta_low, tau_rc_recovery,
    pi_rc_floor, max_defensive_hold_steps) and measure both error directions
    jointly. ANSWER "threshold T blocks spoofing without chronic over-suppression"
    if at T the S5 RC-conflict gate fires on (near) all spoofing events and
    applies suppression (DA_A/DA_M down, Pi_noc up) -- high spoof-detection / low
    miss-rate -- WHILE legitimate task-set switching recovers within the bounded
    hysteresis window (defensive hold released by tau_rc_recovery, precision never
    pinned below pi_rc_floor, false-positive suppression of legitimate switches
    below floor and not chronic). ANSWER "miscalibrated" if no setting separates
    the two: either spoofing leaks through (theta_high too high) or legitimate
    switching is chronically suppressed / oscillates (theta_low too low, recovery
    too slow). HARD PRECONDITION on generalisation: EXQ-158 PASSed but synthesised
    2000+ spoofing events directly rather than letting them arise from organic
    hazard encounters -- it answers the threshold-calibration leg but NOT the full
    organic harm-encounter pipeline; a complete answer requires the spoofing
    events to emerge from organic provenance conflict, not be injected, before the
    calibration is declared robust.
  evidence_quality_note: |
    EXQ-158 PASS (2026-03-29): authority-spoofing event injection (2000+ events) confirmed
    RC-conflict gate fires correctly and spoofing suppression applies without chronic over-
    suppression. Note: experiment synthesizes spoofing events directly rather than relying on
    organic hazard encounters; result valid for the threshold calibration question but does not
    test the full organic harm-encounter pipeline. Partial answer.
  depends_on:
    - MECH-065
    - MECH-062
    - ARC-005
  location: docs/architecture/control_plane_signal_map.md#q-018
  source:
    - docs/architecture/control_plane_signal_map.md
    - docs/thoughts/17-02-26_necessary_separations_based_on_considering-prompt_injection.md
- id: IMPL-020
  title: "Legacy external-model/REE terminology glossary for interoperability."
  claim_type: implementation_note
  subject: terminology.external_model_ree_alignment_glossary
  polarity: records
  status: legacy
  live_status:
    reading: legacy
    as_of: 2026-07-11
    needs_review: false
  lifecycle_stage: retired
  evidence_quality_note: "Historical interoperability record only. JEPA alignment is not an active REE architecture path after the 2026-03-29 decoupling."
  depends_on:
    - IMPL-001
    - MECH-057a
  location: docs/glossary.md#impl-020
  source:
    - docs/glossary.md
    - docs/thoughts/2026-02-13_jepa_ree_formal_alignment_glossary.md
- id: IMPL-021
  title: "Legacy hybrid external-model/REE architecture diagram contract and rendering checklist."
  claim_type: implementation_note
  subject: diagrams.external_model_ree_hybrid_architecture_contract
  polarity: records
  status: legacy
  live_status:
    reading: legacy
    as_of: 2026-07-11
    needs_review: false
  lifecycle_stage: retired
  evidence_quality_note: "Historical diagram contract only. Not an active REE substrate or evidence target after the 2026-03-29 JEPA decoupling."
  depends_on:
    - IMPL-020
    - MECH-057a
    - ARC-005
    - ARC-015
  location: docs/architecture/jepa_ree_hybrid_diagram_spec.md#impl-021
  source:
    - docs/architecture/jepa_ree_hybrid_diagram_spec.md
# JEPA flags removed 2026-02-26 — historical record only (JEPA decoupled).
- id: IMPL-022
  title: "Legacy external representation-reference contract (inputs, outputs, knobs, failure gates)."
  claim_type: implementation_note
  subject: integration.external_e1e2_representation_reference_contract
  polarity: records
  status: legacy
  live_status:
    reading: legacy
    as_of: 2026-07-11
    needs_review: false
  lifecycle_stage: retired
  evidence_quality_note: "Historical interoperability contract only. Do not use as a revalidation target for MECH-058/059/060; REE-native interfaces now own those claims."
  depends_on:
    - IMPL-021
    - MECH-057a
    - ARC-001
    - ARC-002
    - ARC-005
    - ARC-015
  location: docs/architecture/jepa_e1e2_integration_contract.md#impl-022
  source:
    - docs/architecture/jepa_e1e2_integration_contract.md
- id: IMPL-023
  title: "REE-v2 representation-interface-first spec and phase gate."
  claim_type: implementation_note
  subject: roadmap.ree_v2_representation_interface_spec
  polarity: records
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - IMPL-008
    - IMPL-021
    - IMPL-022
    - MECH-057a
    - MECH-058
    - MECH-059
    - MECH-060
    - MECH-061
    - MECH-062
    - MECH-063
  location: docs/architecture/ree_v2_spec.md#impl-023
  source:
    - docs/architecture/ree_v2_spec.md
    - docs/roadmap.md
- id: IMPL-024
  title: "Legacy v2 REE-first canonical wording policy with external-interface translation."
  claim_type: implementation_note
  subject: terminology.v2_ree_first_canonical_external_interface_translation_policy
  polarity: records
  status: legacy
  live_status:
    reading: legacy
    as_of: 2026-07-11
    needs_review: false
  lifecycle_stage: retired
  evidence_quality_note: "Historical wording policy only. Active REE documentation should use REE-native terminology and keep external-model mapping in convergence archives."
  depends_on:
    - IMPL-020
    - IMPL-022
    - IMPL-023
  location: docs/notes/jepa_language_policy.md#impl-024
  source:
    - docs/notes/jepa_language_policy.md
- id: IMPL-025
  title: "Cross-version hook surface contract and registry."
  claim_type: implementation_note
  subject: interfaces.cross_version_hook_surface_contract
  polarity: records
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - IMPL-022
    - IMPL-023
    - MECH-061
    - MECH-062
    - MECH-063
  location: docs/architecture/hook_surface_contract.md#impl-025
  source:
    - docs/architecture/hook_surface_contract.md
    - docs/architecture/hook_registry.v1.json
- id: INV-019
  title: "Rehearsal traversal and irreversible durable write must remain separated."
  claim_type: invariant
  invariant_type: universal
  subject: meta.selection_compression_boundary
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: derivational
  depends_on:
    - INV-011
    - INV-004
    - INV-006
    - MECH-060
  location: docs/invariants.md#inv-019
  source:
    - docs/invariants.md
    - docs/thoughts/2026-02-18_selection_compression__symmetry_ and_meta_invariant_consolidation.md
  what_would_answer: >
    This is a UNIVERSAL/DERIVATIONAL meta-invariant -- the primary claim is a
    substrate-neutral information-flow-architecture proof obligation, not an
    REE-specific empirical hypothesis. Two legs, answered by different methods.

    LEG 1 -- DERIVATIONAL: does the generalization from INV-011 ("imagination without
    belief update") to "any channel that both explores and writes durably" hold? Proof
    pattern: unrestricted exploration implies the explored branch's provisional state
    must remain revocable; an irreversible durable write is by definition non-revocable;
    a channel permitting both simultaneously can commit content that was never validated
    against real consequence. CONFIRMING: any channel meeting (explores + can durably
    write) either gates on an explicit tag (as INV-011's audited hypothesis_tag gate
    does) or is provably unable to reach a write consumer at all (vacuous satisfaction).
    FALSIFYING (of the UNIVERSAL framing, not necessarily REE's instance): a plausible
    alternative architecture where a channel does both simultaneously without this
    failure mode -- e.g. one where "durable write" is made cheaply/fully revocable, so
    "irreversible" no longer applies. Such a counterexample would downgrade INV-019 from
    invariant_type universal to a design-decision specific to non-transactional
    substrates (which REE is), not merely fail to move claim status.

    LEG 2 -- SUBSTRATE-EMPIRICAL (ARC-014, checkable now): NON-DEGENERACY PRECONDITION --
    requires a live case where simulated/imagined content (hypothesis_tag=True) is
    generated AND a real durable-write-capable consumer is reachable from it; auditing an
    unexercised gate is vacuous. CONFIRMING: every write-capable consumer reachable from
    simulated content checks the tag and refuses to write when set (audited: E1/simulate()
    pathway, V3-EXQ-499/633/880 PASS), OR the pathway's output does not yet reach any
    write-capable consumer at all (DMN/_do_replay() pathway, vacuous pass pending
    V3-EXQ-543c -- re-check once wired). FALSIFYING: simulated content reaches and durably
    alters real-experience state through an ungoverned path (tag silently ignored,
    defaulted away, or bypassed outside the one audited exception, MECH-322's sleep-only
    ARC-071 chunking), or that exception fires outside its stated preconditions.

    Passing LEG 1 without LEG 2 leaves the claim proved-but-unverified-in-REE; passing
    LEG 2 without LEG 1 leaves it verified-in-REE-but-not-shown-universal. Both required
    for the claim as currently stated.
  notes: >
    Resolved universal in Session C batch 2026-04-17. Rationale: direct consequence of
    INV-011 (imagination without belief update) extended to any channel that both
    explores and writes durably. The principle -- no channel may permit unrestricted
    exploration and unrestricted durable write simultaneously -- is an information-
    flow-architecture invariant that holds for any agent combining simulation with
    memory. ARC-014 (Default Mode) is the REE-specific implementation locus but does
    not underwrite the subject.

    GOVERNANCE FLAG (2026-08-08 thought-digestion, not resolved here): this claim's
    as_of (2026-07-11) is now stale relative to its own logical parent INV-011's most
    recent re-confirmation (2026-08-07, live code trace + 3 supporting PASS experiments)
    -- worth a governance promotion pass considering INV-019 alongside/following INV-011.
- id: INV-020
  title: "Constraint stores are authority-stratified from direct observational/symbolic writes."
  claim_type: invariant
  invariant_type: universal
  subject: meta.authority_stratification_boundary
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: derivational
  depends_on:
    - INV-014
    - INV-007
    - MECH-064
    - MECH-065
  location: docs/invariants.md#inv-020
  source:
    - docs/invariants.md
    - docs/architecture/control_plane_signal_map.md
    - docs/thoughts/2026-02-18_selection_compression__symmetry_ and_meta_invariant_consolidation.md
  what_would_answer: >
    Same two-leg structure as sibling meta-invariant INV-019 (same 2026-04-17
    registration batch).

    LEG 1 -- DERIVATIONAL: adversarial-construction proof for "any architecture with
    multiple write channels requires typed authority separation." Assume no
    stratification, exhibit a construction where observational/symbolic content reaches
    the constraint store's write path on the same channel as legitimate authority-bearing
    writes, and show this is either (i) actually vulnerable to unauthorized capture, or
    (ii) accidentally safe only because some other unacknowledged typing already exists
    (stratification was present all along, just unlabeled). FALSIFYING (of the UNIVERSAL
    framing): an architecture with both a capture-capable content channel and an
    authority-bearing store that stays safe/uncaptured under adversarial exteroceptive
    input with NO typed separation anywhere in its write path -- stratification turns out
    unnecessary rather than merely satisfied cheaply. No such construction is known; this
    leg is answerable by continued search in the architecture/adversarial-ML literature,
    not by REE experimentation.

    LEG 2 -- SUBSTRATE-EMPIRICAL (ARC-020, offline-consolidation pipeline): NON-DEGENERACY
    PRECONDITION -- requires a live, reachable typed commit-boundary mechanism gating
    writes FROM offline/sleep consolidation TO privileged authority-store equivalents
    (POL/ID/CAPS per docs/architecture/sleep.md#arc-020). CURRENT STATE (2026-08-08,
    re-confirming ARC-020's own finding): precondition FAILS -- exhaustive grep of
    ree_core/ for write_locus/permission_matrix/commit_token/commit_boundary returns zero
    hits. No privileged-authority-store concept and no typed commit-boundary/verifier-
    mediation machinery exists in the live V3 substrate; MECH-067 (the mechanism this
    would route through) is provisional on V1 substrate only, never retested in V3. This
    leg is currently a substrate-conditional wait state, not an open experimental
    question -- do not force a test while the precondition is unmet.

    Do not read INV-020's derivational categorization as "ARC-020 is done" -- the
    universal claim and its REE-specific implementation locus are genuinely decoupled per
    this claim's own notes; that decoupling is why INV-020 can carry derivational while
    ARC-020 carries substrate_conditional.
  notes: >
    Resolved universal in Session C batch 2026-04-17. Rationale: the principle --
    any architecture with multiple write channels requires typed authority
    separation between them -- is a substrate-neutral information-flow constraint.
    "Constraint stores" and "direct observational/symbolic writes" are architecture-
    neutral category terms, not REE-specific machinery. ARC-020 is the REE-specific
    implementation of this principle in the offline-consolidation pipeline but does
    not underwrite the subject.

    GOVERNANCE FLAG (2026-08-08 thought-digestion, not resolved here): possible
    depends_on mismatch -- MECH-065 (RC-conflict lane precision modulation) is listed
    here, but its content is about a different mechanism than authority-stratified
    writes; MECH-064 (typed authority/control-store separation blocking direct
    exteroceptive writes into policy/identity stores) looks like the actually on-point
    corollary already present in the list. Worth a governance-cycle check on whether
    MECH-065 belongs in this depends_on, or whether the mapping predates a later
    MECH-065 scope narrowing.
- id: INV-021
  title: "Responsibility-bearing durable updates occur only at typed commit boundaries."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-003
    - SD-026
  pending_substrate_reconfirmation: true
  subject: meta.commit_boundary_irreversibility
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-012
    - MECH-061
    - Q-015
    - ARC-003
    - SD-026
  epistemic_category: substrate_conditional
  what_would_answer: |
    LEG 0 (inherited, unmet on BOTH components): (i) INV-012 Leg 0 -- E3
    differentiated-candidate selection, unmet per MECH-439 exhausted. (ii)
    ARC-003 Leg B -- commit as a real hysteresis-bearing event, independently
    BLOCKED since 2026-07-21 on the identical ARC-107/MECH-448/449 release
    condition. Until both clear, "responsibility-bearing update" has no
    referent and "commit boundary" names no confirmed event distinct from
    mere scoring.

    LEG 1 (testable NOW, commitment-free, mirrors ARC-003 Leg A): does every
    durable, lasting-effect write pathway pass through an ARC-003 commit
    before it persists, or does REE have a write channel producing durable
    consequence WITHOUT ever passing through commit? Candidate exceptions to
    check: residue-geometry sampling-bias updates (ARC-007, reflexive), any
    offline/sleep-phase write (SD-033 family), uncommitted exploratory
    evaluation leaving a lasting trace.
    CONFIRMING: no counter-example found across current write pathways.
    FALSIFYING: a durable, consequence-bearing update found outside any
    ARC-003 commit boundary -- falsifies the exclusivity structure regardless
    of Leg 0.

    LEG 2 (conjunctive, full claim): CONFIRMED only if Leg 0 clears AND Leg 1
    confirms. If Leg 1 confirms while Leg 0 remains unmet, record the
    narrower finding ("exactly one durable-write locus exists, but nothing
    yet gives it responsibility-bearing status") rather than treating the
    full ethical claim as confirmed.

    (Digested 2026-08-07, session cool-torvalds-a82359, thought-digestion
    wave 11. ARC-003's Leg A/Leg B split and INV-012's Leg 0 correction, both
    from this and a prior session, converge on the same ARC-107 release
    condition via two independent dependency paths -- worth noting as
    reinforcing evidence the block is structural.)
  location: docs/invariants.md#inv-021
  source:
    - docs/invariants.md
    - docs/architecture/e3.md
    - docs/thoughts/2026-02-18_selection_compression__symmetry_ and_meta_invariant_consolidation.md
    - docs/thoughts/2026-02-24_determinism_action_gating_boundary.md
    - docs/thoughts/2026-02-25_task_loop_extraction_and_latent_field_ethics.md
  notes: >
    Resolved emergent in Session C batch 2026-04-17. Rationale: structural
    generalisation of INV-012 (Session A's worked emergent example) -- the claim
    imports "responsibility-bearing" from INV-012, which is itself emergent on
    SD-026 (prospective precision template write channel makes "commitment" a
    load-bearing ethical concept). Additionally, "typed commit boundary" requires
    ARC-003 (E3's commit action) to be a real architectural feature. Without
    ARC-003 the invariant names no feature; without SD-026 "responsibility" has no
    coherent referent. pending_substrate_reconfirmation set because SD-026 is
    candidate.
- id: INV-022
  title: "Trust/precision allocation must remain heterogeneous, not a single scalar."
  claim_type: invariant
  invariant_type: universal
  subject: meta.heterogeneous_trust_allocation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-008
    - INV-009
    - MECH-063
    - Q-017
  location: docs/invariants.md#inv-022
  source:
    - docs/invariants.md
    - docs/architecture/control_plane_signal_map.md
    - docs/thoughts/2026-02-18_selection_compression__symmetry_ and_meta_invariant_consolidation.md
- id: INV-023
  title: "Protected offline recalibration/integration regimes are structurally required."
  claim_type: invariant
  invariant_type: universal
  subject: meta.offline_reweighting_requirement
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-010
    - ARC-011
    - MECH-016
    - MECH-017
    - MECH-018
  location: docs/invariants.md#inv-023
  source:
    - docs/invariants.md
    - docs/architecture/sleep.md
    - docs/thoughts/2026-02-18_selection_compression__symmetry_ and_meta_invariant_consolidation.md
- id: INV-024
  title: "Offline consolidation and online commitment must remain isolated at responsibility-bearing write loci."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-020
    - SD-026
  pending_substrate_reconfirmation: true
  subject: meta.offline_online_update_locus_isolation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-021
    - INV-023
    - ARC-020
    - SD-026
    - MECH-067
  location: docs/invariants.md#inv-024
  source:
    - docs/invariants.md
    - docs/architecture/sleep.md
    - docs/architecture/agency_responsibility_flow.md
  notes: >
    Session C batch 2026-04-17: classified emergent on [ARC-020, SD-026]. The
    invariant is a conjunctive emergence — it requires both the offline write-
    boundary substrate (ARC-020 typed authority/write boundaries that protect
    offline consolidation) AND the commitment substrate (SD-026 prospective
    precision template write channel that gives "responsibility-bearing"
    content via INV-012/INV-021). Without ARC-020 the "offline consolidation"
    locus is not a typed object with protected writes; without SD-026 the
    "responsibility-bearing" qualifier on the write locus has no subject
    matter. Both substrates are below active (ARC-020 candidate, SD-026
    candidate) so pending_substrate_reconfirmation: true.

# Five foundational axioms — registered 2026-03-18
- id: INV-025
  title: "Irreducible uncertainty is an epistemological constraint that cannot be engineered away."
  claim_type: invariant
  invariant_type: universal
  epistemic_category: standard
  subject: epistemology.irreducible_uncertainty
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  what_would_answer: >
    OUT OF DOMAIN (axiom leg): INV-025 as literally stated -- that epistemic
    uncertainty is irreducible IN PRINCIPLE for any finite agent in a real
    world, not merely irreducible for REE's current architecture -- is a
    foundational epistemological premise (Axiom 1 of
    five_axioms_foundations.md, "You can never be sure"). No REE experiment
    can prove uncertainty is irreducible in principle; an experiment can only
    show that REE's OWN uncertainty estimates are non-degenerate (do not
    collapse to false certainty), never that some other architecture could
    not in principle eliminate uncertainty. This leg is out-of-domain in the
    same sense as INV-026/027/028/029.

    REFRAMED TESTABLE LEG (testable now -- NOT substrate-blocked, unlike the
    ARC-010/V5-gated legs drafted for INV-028/INV-029): INV-025's own
    architectural-consequence text names the machinery directly -- "the
    entire precision architecture (ARC-016, INV-008, INV-009)... follows
    necessarily from this axiom," with pre-commit simulation (INV-011) and
    the commitment gate (INV-012) as necessary responses. Unlike the
    mirror-modelling legs for the other axiom claims, this machinery is core
    V1/V2/V3 substrate that is already built and, for its primary component,
    already validated. The testable question is narrower than the axiom:
    does REE's architecture actually IMPLEMENT the operational consequence
    of treating uncertainty as irreducible -- i.e. does precision-weighted
    prediction error remain genuinely non-degenerate across training and
    eval, and does the commitment gate (INV-012) function as an
    uncertainty-DRIVEN safeguard rather than a fixed schedule decoupled
    from it.

    ALREADY ANSWERED, primary component (ARC-016, status: stable,
    2026-07-25): the eval-time structural test of exactly this question has
    run and PASSED. V3-EXQ-818 (eval_derived_noise_precision_sweep) found
    mean_rho_rv_vs_commit = -0.955 -- commit-rate tracks E3-derived
    precision MONOTONICALLY across a graded-noise ladder, non-degenerate
    (commit rate strictly between floor and ceiling, not pinned), load-bearing
    C1 PASS, gate_green = True. See ARC-016's own what_would_answer for the
    full non-degeneracy precondition this reuses (do not re-derive).

    SECONDARY COMPONENT, currently UNCONFIRMED (INV-011, INV-012): whether
    pre-commit simulation and the commitment gate specifically function as
    uncertainty-DRIVEN safeguards is not independently evidenced -- neither
    carries its own what_would_answer today. INV-012 additionally carries
    pending_substrate_reconfirmation=true (emergent_from SD-026, still
    candidate) and per its own notes cannot be cited as supporting evidence
    until substrate reconfirmation.

    Confirming observation: commit-rate continues to track E3-derived
    precision/variance monotonically under a graded manipulation,
    non-degenerate across the manipulated range, reproducing V3-EXQ-818.

    Falsifying observation: the precision/commitment signal COLLAPSES to a
    fixed value independent of estimated uncertainty once the eval-derived
    threshold is correctly calibrated -- would not falsify the axiom (out of
    domain) but would falsify the claim that REE's precision architecture is
    a genuine operational consequence of treating uncertainty as irreducible,
    and would call ARC-016's stable status into question.

    PRECEDENT NOTE (2026-08-06, thought-digestion, session
    thoughts-intakes-4fd18d): fourth of the five original axiom claims
    digested. Unlike INV-028/INV-029 (substrate_conditional, unbuilt V5
    substrate), INV-025's reframed leg rests on already-built, already-
    validated V1/V2/V3 machinery. Do not apply substrate_conditional
    mechanically to every axiom claim -- check what the architectural-
    consequence machinery actually requires, per claim.
  location: docs/invariants.md#inv-025
  source:
    - docs/architecture/five_axioms_foundations.md
  notes: >
    Axiom 1: "You can never be sure." Epistemic uncertainty is irreducible
    and structural — not a limitation to be overcome but a feature of being
    a finite agent in a real world. No agent can achieve certainty about its
    perceptions, predictions, or the causal consequences of its actions.
    Architectural consequence: the entire precision architecture (ARC-016,
    INV-008, INV-009) follows from this axiom. Pre-commit simulation
    (INV-011), commitment gates (INV-012), and precision-weighted prediction
    error are necessary responses to irreducible uncertainty. A fully certain
    agent needs no commitment boundary.

- id: INV-026
  title: "A self is an operational prerequisite for responsible agency."
  claim_type: invariant
  invariant_type: universal
  epistemic_category: standard
  subject: self.existence
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-015
  what_would_answer: |
    INV-026 asserts a metaphysical axiom ("I am" -- Axiom 2) with an explicit
    operational claim attached: the self is a prerequisite for responsible agency.
    Split into two legs, but unlike INV-028/029, the architectural leg here is
    ALREADY BUILT V3 substrate, not V5-gated.

    == LEG 1 (OUT OF DOMAIN): the axiom itself ==
    "The agent exists as a distinct locus of experience, action, and
    responsibility" is a claim about SUBJECTIVE, phenomenal selfhood. No REE
    experiment can confirm or deny this -- an experiment can show a functionally
    distinct, causally load-bearing self-representation, not phenomenal
    selfhood in the sense the axiom asserts. Permanently out of domain.

    == LEG 2 (REFRAMED, TESTABLE NOW) ==
    (a) DISTINCTNESS: does z_self encode self/action information distinctly
    from z_world (SD-005)? PARTIALLY CONFIRMED, with a real asymmetry that
    should qualify how strongly this premise is stated. z_self RELIABLY
    specialises (EXQ-532 self_body_advantage=+0.126), but z_world does NOT
    reliably show reciprocal specialisation (EXQ-532 world_obs_advantage=
    -0.032). Cite SD-005's own evidence_quality_note, do not re-derive.

    (b) SOURCE MONITORING: does the architecture distinctly tag internally-
    generated vs world-received content (MECH-094)? CONFIRMED, stable,
    conf 0.868, 13 supporting lit entries, 0 load-bearing opposing.

    (c) LOAD-BEARING FOR RESPONSIBILITY (the axiom's own stated function):
    does responsibility/commitment machinery actually degrade if the
    self-representation is ablated? Cross-reference ARC-015 HALF 2
    (responsibility flow, MECH-060/061/057a) -- testable now, real V1
    evidence, do not duplicate. ARC-015 HALF 1 (self-vs-OTHER via MECH-095)
    is a different question, confirmed substrate-blocked -- not relevant here.

    Live caveat: INV-012 carries pending_substrate_reconfirmation=true
    (emergent_from SD-026, still candidate) and cannot be cited as supporting
    evidence until SD-026 reaches active.

    CONFIRMING signature: ablating z_self/MECH-094 measurably degrades
    ARC-015-Half-2-style commitment/responsibility-flow behaviour beyond
    what gate-ablation alone already accounts for.

    FALSIFYING signature: commitment-boundary gating and responsibility
    attribution function identically whether or not the self-representation
    is intact -- would mean self is decorative relative to commitment
    machinery, not prerequisite to it.
  location: docs/invariants.md#inv-026
  source:
    - docs/architecture/five_axioms_foundations.md
  notes: >
    Axiom 2: "I am." The agent exists as a distinct locus of experience,
    action, and responsibility. Without a self there is no causal agency,
    no commitment, no harm attribution, no accountability. The self is not
    an emergent computational convenience — it is a foundational axiom from
    which agency and ethics follow.
    Architectural consequence: z_self encoder (SD-005), the self/world split
    (z_self as E2 domain), the hypothesis tag that marks internally-generated
    content as distinct from world-received content (MECH-094), and the
    commitment boundary that makes actions attributable to an agent (INV-012).
    Also grounds the asymptotic unknowability of death: death is the limit of
    the harm gradient at which "I am" ceases — real (INV-027 makes it
    possible), but unknowable from within experience (ARC-024).

- id: INV-027
  title: "An external world is a structural prerequisite for grounded representation."
  claim_type: invariant
  invariant_type: universal
  epistemic_category: standard
  subject: world.existence
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on: []
  what_would_answer: >
    OUT OF DOMAIN (axiom leg): "There is a real world independent of the
    agent's model of it" is a metaphysical/epistemological premise (realism
    vs. idealism/constructivism) no REE experiment can settle. An experiment
    can only show structured, informative prediction error -- consistent
    with a real causal process, not proof one exists independent of any
    modeling agent whatsoever.

    REFRAMED TESTABLE LEG: does REE's architecture behave AS IF there is a
    genuinely independent, structured external causal process? (a) do
    prediction-error channels (z_world/SD-005, E1/E2/E3) produce structured,
    learnable surprise, not noise; (b) does the residue field genuinely
    accumulate what ACTUALLY happened distinct from prediction, and is that
    load-bearing (INV-004/INV-006 post-commit persistence); (c) does ARC-024's
    claim that harm gradients precede/predict harm events hold up empirically.

    Confirming signature: prediction-error metrics improve with training
    rather than sitting at noise floor, AND residue-field-driven behaviour
    measurably diverges from a trust-the-prediction-always baseline in a way
    that tracks actual outcomes.

    Falsifying signature: prediction error stays indistinguishable from noise
    regardless of training, OR the residue field fails to build non-zero
    contrast at all (as already seen in EXQ-151/EXQ-215 -- an open
    implementation gap, not yet resolved), OR the residue trace never
    diverges usefully from the prediction it's supposed to be independent of.
    Caveats already on record: z_world does NOT reliably show reciprocal
    specialisation vs z_self (EXQ-047g/113/532), and ARC-024 itself is
    currently provisional/demoted, not confirmed -- so this leg is
    testable-now but not yet won.
  location: docs/invariants.md#inv-027
  source:
    - docs/architecture/five_axioms_foundations.md
  notes: >
    Axiom 3: "The world exists." There is a real world independent of the
    agent's model of it. The world can surprise the agent. World states are
    not constructions — they have causal power that predictions can get wrong.
    Architectural consequence: z_world encoder, prediction error channels (E1,
    E2, E3), the residue field (which accumulates what actually happened, not
    what was predicted), and post-commit trace persistence (INV-004, INV-006).
    Also grounds ARC-024: because the world exists independently, it can
    generate harm gradients that precede and predict harm events — the world
    is structured, not random, so proxy signals are real and informative.

- id: INV-028
  title: "Shared-world ethics requires modelling others as co-inhabitants of the same consequence space."
  claim_type: invariant
  invariant_type: universal
  epistemic_category: substrate_conditional
  subject: ethics.shared_world
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-026
    - INV-027
    - ARC-010
  what_would_answer: >
    OUT OF DOMAIN (axiom leg): INV-028 as literally stated -- that other agents'
    harm and benefit are real "by exactly the same grounds as the agent's own,"
    because they are selves (INV-026) in the same world (INV-027) -- is a
    foundational ethical/metaphysical premise (Axiom 4 of
    five_axioms_foundations.md, "Others share this world"). No REE substrate at
    any level -- V3, V4, or V5 -- can confirm or falsify whether another agent's
    harm is metaphysically real by the same grounds as the agent's own. This leg
    is out-of-domain in the same sense as INV-025/026/027/029.

    REFRAMED TESTABLE LEG (substrate-blocked, not yet buildable): what IS
    architecturally testable is narrower -- INV-028's own architectural
    consequence names it directly: "when another self is represented using the
    same predictive machinery as the self, their harm generates the same error
    signal structure as own-harm" (grounds mirror modelling, INV-005, at the
    REE-specific level via ARC-010). The testable question is whether
    self-caused harm to a MODELLED OTHER (via ARC-010 mirror modelling --
    reused self/other latent stack, ARC-004 L-space with reduced precision
    gains) generates an error-signal / harm-evaluation structure statistically
    indistinguishable from self-caused harm to the agent's own outcomes -- i.e.
    whether the self-other distinction is actually just "a routing difference
    within the same architecture, not a different architecture," once it is
    measurable rather than asserted.

    Confirming observation: comparing self-caused harm to the agent's own
    modelled outcomes against self-caused harm to a mirror-modelled other
    (ARC-010's OTHER_SELFLIKE-tagged shadow-bundle estimate), the harm-error
    signal driving E3 ranking/routing shows the same functional form,
    magnitude scaling, and downstream repair/avoidance pull in both
    conditions -- i.e. no separate "other-harm" pathway exists; own-harm and
    modelled-other-harm route through the same machinery.

    Falsifying observation: self-caused harm to a mirror-modelled other
    produces a measurably different error-signal structure than self-caused
    harm to the agent's own outcomes -- e.g. attenuated magnitude, a distinct
    functional form, or no coupling into the same E3 ranking/routing pathway --
    even though the same predictive machinery is confirmed to be representing
    both self and other. That outcome would not falsify the axiom (out of
    domain per above) but WOULD falsify the claim that REE's architecture
    implements the "routing difference, not architecture difference" mechanism
    INV-028 asserts as its consequence.

    Non-degeneracy precondition (currently UNMET -- this is why the reframed
    leg is substrate-blocked, not testable-now): same V5/multi-agent substrate
    gap as ARC-010 and INV-029 -- requires ARC-047 SocialGridWorld (or
    equivalent multi-agent environment) where a second agent's harm is
    attributable to the tested agent's own action (not merely observed/
    world-caused harm), so that a genuine self-harm vs. other-harm error-signal
    comparison is possible. ARC-047 is unimplemented (status: candidate,
    confidence 0.0, implementation_phase v5); see ARC-010's own
    what_would_answer for the full substrate description. Do not re-derive --
    cross-reference.

    PRECEDENT NOTE: applies the epistemic_category=substrate_conditional
    template established for INV-029 (2026-08-06, thought-digestion session
    thoughts-intakes-4fd18d) to this, the second of the five original axiom
    claims to be digested (INV-015, INV-026, INV-027, INV-028, INV-029).
  location: docs/invariants.md#inv-028
  source:
    - docs/architecture/five_axioms_foundations.md
  notes: >
    Axiom 4: "Others share this world." Other agents exist and inhabit the
    same world (INV-027). They are not simulations or projections of the
    self (INV-026). Their harm and benefit are real by exactly the same
    grounds as the agent's own — because they are selves (INV-026) in the
    same world (INV-027).
    Architectural consequence: harm via mirror modeling (INV-005) and ethical
    emergence from self-other symmetry (INV-001). Ethics is not an overlay
    because when another self is represented using the same predictive
    machinery as the self (INV-026), their harm generates the same error
    signal structure as own-harm. The self-other distinction is a routing
    difference within the same architecture, not a different architecture.

- id: INV-029
  title: "Love as long-horizon care-investment is a structurally coherent agent disposition, not an add-on sentiment."
  claim_type: invariant
  invariant_type: universal
  epistemic_category: substrate_conditional
  subject: ethics.love_exists
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-026
    - INV-028
    - ARC-010
  what_would_answer: >
    OUT OF DOMAIN (axiom leg): INV-029 as literally stated -- that love/genuine
    connection is metaphysically real, not reducible to self-interest or a
    computational strategy -- is a foundational ethical/philosophical premise
    (Axiom 6/7 of five_axioms_foundations.md, formerly the compressed "Love
    exists" of the original five axioms). No REE substrate at any level -- V3,
    V4, or V5 -- can confirm or falsify whether love is metaphysically real.
    This leg is out-of-domain in the same sense as INV-025/026/027/028.

    REFRAMED TESTABLE LEG (substrate-blocked, not yet buildable): what IS
    architecturally testable is narrower -- does REE's benefit-gradient/mirror-
    modelling/residue machinery implement a disposition functionally consistent
    with the axiom: (i) long-horizon, other-directed investment (ii) dissociable
    from short-horizon self-interested reward-seeking, and (iii) a residue/
    repair-search signature under self-caused harm to a modelled other,
    distinguishing genuine care from empathy-as-simulation (the "torturer feels
    no residue" discriminator; see evidence/planning/
    thought_intake_2026-07-09_residue_care_marker_mood_regulation.md, candidate
    claim 1, itself framed there as a falsifier for INV-001 and explicitly
    cross-referenced to this claim).

    Confirming observation: an agent that models another self-like agent
    (ARC-010 mirror modelling / MECH-164 z_beta-leak) shows sustained
    investment toward that other's outcomes trading off against its own
    short-horizon reward, AND self-caused harm to that other generates a
    residue signal that measurably drives repair-search/help-recruitment.

    Falsifying observation: the same modelling machinery (accurate prediction
    of the other's affect/outcomes) is present, but benefit-seeking toward the
    other fully re-derives as a proxy for the agent's own short-horizon reward
    with no dissociable long-horizon component, and/or self-caused harm to the
    modelled other produces no residue/repair-search signature -- i.e. the
    architecture implements empathy as simulation without the coupled valence
    that would make it care. That outcome would not falsify the axiom (out of
    domain per above) but WOULD falsify the claim that REE's architecture
    implements a disposition functionally consistent with it.

    Non-degeneracy precondition (currently UNMET -- this is why the reframed
    leg is substrate-blocked, not testable-now): requires (1) a multi-agent or
    other-agent-modelling substrate sufficient to instantiate a loved-analog
    other that can be genuinely harmed by the agent's own action (ARC-010
    mirror modelling extended to a second agent whose harm is attributable to
    the tested agent, not merely observed/world-caused harm); (2) a
    residue/attachment mechanism that is actively engaged and produces a
    measurable, non-floor-pinned signal distinguishable from ordinary
    prediction error or the raw z_beta leak itself; and (3) long-horizon
    credit assignment sufficient that short-horizon self-interest and
    long-horizon other-directed investment are actually dissociable in the
    metric rather than collinear by construction. Per the residue-care-marker
    intake this is V5/multi-agent substrate (WS-10 minimal 2-agent world), not
    present in V3 -- so this leg currently self-routes to substrate-blocked
    rather than yielding a PASS/FAIL.

    PRECEDENT NOTE (2026-08-06, thought-digestion, session thoughts-intakes-
    4fd18d): this is the first of the five original axiom claims (INV-015,
    INV-026, INV-027, INV-028, INV-029) to be digested. User decision:
    epistemic_category=substrate_conditional (naming the ARC-010/V5 multi-agent
    dependency explicitly, rather than out_of_domain, which would obscure that
    a testable leg exists once that substrate lands) is the template to apply
    consistently to the remaining four axiom claims when they are digested.
  location: docs/invariants.md#inv-029
  source:
    - docs/architecture/five_axioms_foundations.md
  notes: >
    Axiom 5: "Love exists." Genuine connection, care, and the pull toward
    union with others is a real phenomenon — not an overlay, not reducible
    to self-interest, not a computational strategy. Love is the asymptotic
    limit of the benefit gradient (ARC-024): every benefit signal (warmth,
    connection, belonging, joy) is a proxy pointing toward a complete union
    with another that is real (grounded in INV-028) but unknowable in its
    ultimate form.
    The unknowability is structural: complete union with another would
    approach the dissolution of the individuation that INV-026 asserts.
    Both selves cannot be fully present AND fully unified — the limit is
    asymptotically real but unreachable while both agents persist as
    distinct loci. Love can be approached in understanding and in experience;
    it cannot be fully known.
    Architectural consequence: grounds the benefit gradient direction
    (ARC-024), explains why harm-to-others matters (INV-028 applied to a
    being with a benefit manifold), and completes the ethical architecture.
    Ethics emerges from constraint (INV-015) applied to agents for whom
    love is real.

# Proxy-gradient consequence of the five axioms — registered 2026-03-18
- id: ARC-024
  title: "Harm and benefit signals have asymptotic proxy structure in world latent space."
  claim_type: architecture_hypothesis
  subject: world.harm_benefit_asymptotic_proxy_structure
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-024@2026-03-19T20:35:00Z"
      as_of: 2026-03-19
      verdict: demote_to_candidate/applied
  depends_on:
    - INV-025
    - INV-026
    - INV-027
    - INV-028
    - INV-029
  location: docs/architecture/five_axioms_foundations.md
  evidence_quality_note: |
    EXQ-028 PASS (2026-03-18): Gradient dominance confirmed with random policy on
    CausalGridWorldV2. mean_dz_world_hazard_approach >> mean_dz_world_none. World
    generates observable signals before contact events. Direct confirmation that
    the simulated world must and does produce proxy-gradient fields.
    EXQ-029 PASS (2026-03-18): E3.harm_eval learned graded danger model on gradient world.
    none=0.373, approach=0.612, contact=0.666. 10× improvement over old world (EXQ-027).
    E3 extends harm model backward along causal chain from contact to approach — direct
    computational instantiation of the philosophical derivation. Both PASS runs support the
    architectural/philosophical layer of this claim (gradient world is necessary and learnable).
    EXQ-033 FAIL (2026-03-18): Tested the separable behavioral/capability-expansion
    prediction (approach detection grows faster than contact detection with training depth)
    which is now split out as ARC-026. The philosophical layer of ARC-024 is not implicated
    by EXQ-033 — C1-C3 all PASSED; only C4 (slope ratio) failed due to training instability
    (both signals peaked ep500 then degraded to ep1000, a depth-calibration artifact).
    Governance 2026-03-19: ARC-024 claim split. Philosophical/foundational layer retained
    as provisional (architecturally necessary, EXQ-028/029 support world-gradient structure).
    Behavioral capability-expansion prediction split to ARC-026 (candidate, v3). Conflict
    evidence in ARC-024 experiment pool reflects ARC-026's scope, not ARC-024 proper.

    [2026-07-29 governance, V3-EXQ-032d supports; PASS 5/5, superseding V3-EXQ-032b]: bug-fix
    re-run correcting 032b (which never called agent._e1_tick()/record_transition(), so its E1/E2
    training buffers never reached the length-2 floor those losses require and its PASS reflected
    an untrained E1/E2/encoder stack). 032d adds the two missing calls and changes nothing else --
    world_forward R2 0.9462, energy-escalation ladder confirmed (causal_sig contact 0.0024 >
    approach 0.0061 [sign gradient] > none -0.047), all 5 criteria pass. V3-EXQ-032b marked
    superseded. Confirms the world-gradient structure this claim's philosophical layer asserts,
    now on a genuinely-trained substrate. Stays provisional.
  notes: >
    Because the world exists (INV-027) and the self exists (INV-026), death
    is the asymptotic limit of the harm manifold — the world can end the self,
    but death cannot be experienced from within the agent's perspective because
    experiencing it would violate "I am" (INV-026). Every harm signal is a
    proxy gradient pointing toward this unknowable endpoint.
    Because love exists (INV-029) and others share this world (INV-028),
    unconditional love/union is the asymptotic limit of the benefit manifold —
    real but unknowable in its complete form while both selves persist.
    Architectural consequence for the simulated environment: the world MUST
    generate observable proxy-gradient fields around harm/benefit sources.
    Continuous, spatially distributed harm/benefit signals that precede and
    predict contact events are not a convenience — they are the correct model
    of how harm and benefit actually work. A world that generates binary harm
    signals only at contact cannot test E3.harm_eval gradient learning, cannot
    provide E2.world_forward with action-conditional training signal, and
    cannot support the SD-003 attribution pipeline.
    Implementation: CausalGridWorldV2 adds hazard_field and resource_field
    gradient arrays to the observation, with continuous harm signal proportional
    to proximity. See ree-v3/ree_core/environment/causal_grid_world.py.
    The further behavioral prediction — that approach detection grows faster
    than contact detection as a function of training depth — is a separable
    empirical claim registered as ARC-026.
  heterogeneity_note: >
    Conflict entries pre-date the 2026-03-19 claim split: EXQ-033 FAIL tested
    the behavioral capability-expansion prediction now registered as ARC-026,
    not ARC-024's philosophical/architectural layer. The conflict ratio is
    inflated by evidence inherited from a superseded pre-split scope; the
    architectural claim itself (proxy-gradient fields are necessary and
    learnable) is supported by EXQ-028/029 PASS and is not genuinely contested.

# Foundational definitions extracted from Philosophy repo — registered 2026-03-18
- id: INV-030
  title: "Viability is defined relative to the agent's continuity in a shared world, not as a scalar reward."
  claim_type: invariant
  invariant_type: universal
  subject: cognition.viability_definition
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-025
    - INV-027
  epistemic_category: derivational
  what_would_answer: |
    Answered by architectural audit of the live trajectory-selection
    scoring path and the E1/E2 training-loss code, not by a trained
    behavioral experiment (this is a MECHANISM/STRUCTURE question -- HOW
    viability is computed and optimised toward).

    NON-DEGENERACY PRECONDITION: the audit must inspect a live case where
    E3 actually composes candidate trajectory scores and selects among
    them, and a live case where E1 or E2 actually computes a training
    loss -- not merely a declared scoring formula in a docstring.

    CONFIRMED CURRENT STATE (2026-08-07 digestion pass):
    ree_core/predictors/e3_selector.py::score_trajectory() (live, called
    from select() at the core of every trajectory-selection tick)
    computes a COST J(zeta) = f_weight*F(zeta) + lambda_eff*M(zeta) +
    rho*Phi_R(zeta) - benefit_weight*B(zeta) - goal_weight*G(zeta),
    where each term comes from a distinct, independently-parameterized
    head: F = compute_reality_cost() (smoothness + viability head), M =
    compute_ethical_cost()/compute_harm_forward_cost() (harm_eval_head,
    or the dedicated z_harm stream, SD-010), Phi_R =
    compute_residue_cost() (ResidueField.evaluate_trajectory), B =
    compute_benefit_score() (benefit_eval_head, gated behind an ARC-030
    warmup), G = compute_goal_score() (goal_state.goal_proximity). These
    terms are retained UNREDUCED for diagnostics (last_score_decomp /
    last_channel_terms, MECH-463) specifically so cross-term covariance
    can be computed rather than lost to a marginal collapse. A grep for
    "reward" across e3_selector.py returns nothing but one unrelated
    comment (MECH-111 novelty). There is no point where these terms are
    summed into an undifferentiated scalar and handed to a reward-
    maximizing update rule.

    E1/E2 losses are self-supervised predictive-coherence losses, not
    ground-truth-correspondence losses: e2_world.py::compute_loss()
    (F.mse_loss(z_world_pred, z_world_next)) trains E2 to predict its OWN
    subsequently-observed next latent, not an externally-labeled "true"
    world state; agent.py::compute_prediction_loss() (E1's multi-step
    loss) targets the agent's own buffered future sequence the same way;
    e2_fast.py's world_forward_contrastive_loss* are InfoNCE-style
    contrastive losses over the model's own candidate continuations.

    BOUNDARY CASE (not counted as falsifying, flagged for scope
    awareness): ree_core/action_learning/actor_critic.py (MECH-457) is an
    explicit Sutton-2000-style actor-critic with a scalar
    reward-prediction-error teacher (agent.py::actor_critic_reward(),
    R_t = benefit_eval(z) - harm_eval(z), collapsed to one float). Its
    own docstring states it is "architecturally distinct from -- and NOT
    reducible to --" E3's cost argmin; it is gated off by default
    (use_actor_critic=False) and drives a separate primitive-action
    pathway, not trajectory selection. INV-030's own notes field scopes
    the claim to E1/E2/E3, so this is out of scope as currently worded --
    but it is the one place in ree_core/ where classical scalar-reward RL
    genuinely exists, worth accounting for if this invariant's scope is
    ever revisited.

    AUDIT TARGET: ree_core/predictors/e3_selector.py::score_trajectory()
    and ::select(); ree_core/predictors/e2_world.py::compute_loss() and
    ree_core/agent.py::compute_prediction_loss().

    FALSIFYING evidence: the traced live trajectory-selection path is
    found to collapse F/M/Phi_R/B/G into a single undifferentiated
    scalar "reward" BEFORE selection, with that scalar then fed to a
    reward-maximizing update. OR: E1/E2's training loss is found to
    optimise directly toward a labeled "objectively correct" world state
    supplied from outside the agent's own predictive/rollout process.

    CONFIRMING evidence: the traced live path shows trajectory selection
    as a genuinely multi-component, typed evaluation with no reward
    scalar or RL update consuming the pre-composition terms, AND E1/E2
    losses are genuinely self-supervised predictive/rollout losses. This
    is the CURRENT confirmed state per the 2026-08-07 audit above.
  location: docs/invariants.md#inv-030
  source:
    - docs/architecture/five_axioms_foundations.md
  notes: >
    Cognition is defined as the maintenance of coherent input-output behaviour
    across time without catastrophic failure under permanent uncertainty.
    This definition is deliberately non-truth-seeking. Real cognitive systems —
    biological, artificial, institutional — operate under permanent uncertainty,
    partial observability, and delayed feedback. Under these conditions,
    cognition cannot be characterised as the acquisition of truth but as
    viability: the capacity to persist coherently without catastrophic failure.
    Follows from INV-025 (certainty is unavailable) and INV-027 (the world
    exists and can surprise the agent). A fully certain agent in a transparent
    world could in principle be truth-seeking; a finite agent in a real world
    cannot. Viability is what remains when truth-access is structurally denied.
    Architectural consequence: no REE component optimises toward ground truth.
    E1/E2 predictions are evaluated for temporal coherence and survivability
    under action, not correspondence to reality. E3 selects trajectories that
    preserve long-horizon viability, not accuracy.

- id: INV-031
  title: "Functional persistence in a shared world, not abstract truth, is the operational goal of cognition."
  claim_type: invariant
  invariant_type: universal
  subject: epistemology.functional_persistence_replaces_truth
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-025
    - INV-027
    - INV-030
  location: docs/invariants.md#inv-031
  source:
    - docs/architecture/five_axioms_foundations.md
  notes: >
    REE explicitly rejects privileged latent world states. Internal structures
    are not representations of reality — they are tools for viable engagement
    with it. Truth is replaced by functional persistence under intervention.
    A trajectory or internal model persists if and only if it demonstrates
    temporal survivability: internal coherence across time, compatibility with
    incoming signals, preservation of future predictability, and viability
    under interaction with other agents.
    Action serves as the primary epistemic probe. Perturbation — acting on
    the world and observing the outcome — reveals which trajectories remain
    coherent under intervention. This is the only epistemic test available
    to a finite agent under permanent uncertainty (INV-025) in a real world
    (INV-027) that can surprise it.
    Consequence: prediction errors may be tolerated when absorbing them
    preserves long-horizon viability. A framework that must minimise
    prediction error at every step cannot do this. REE replaces minimisation
    with constrained trajectory selection under the viability criterion
    (INV-030).

- id: INV-032
  title: "Moral agency requires both approach and avoidance drives; pure avoidance produces a degenerate risk manager, not an ethical agent."
  claim_type: invariant
  invariant_type: universal
  subject: agency.approach_avoidance_both_necessary
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: derivational
  depends_on:
    - INV-029
    - INV-030
  location: docs/invariants.md#inv-032
  source:
    - evidence/planning/thought_intake_2026-03-22_approach_avoidance_drives.md
  what_would_answer: >
    LEG 1 (DERIVATIONAL): care (INV-029) is orientation TOWARD flourishing -- a
    directional term. An avoidance-only objective has quiescence as its global optimum
    whenever action carries harm-risk. CONFIRMING: adding a genuinely independent
    approach term (not a disguised omission-avoidance) breaks the degenerate optimum.
    FALSIFYING (of the universal framing): a construction where "avoidance" is broadened
    to include omission-harm, collapsing the two axes into one -- REE's own M(zeta) is
    confirmed forward-looking harm-caused cost, not omission-cost, so the axes stay
    genuinely independent here.

    STRUCTURAL MINIMUM: confirmed met (2026-08-08) -- e3_selector.py::score_trajectory()
    computes independently-parameterized approach terms (benefit_weight*B,
    goal_weight*G) and an avoidance term (harm/cost M), retained unreduced (MECH-463).

    BEHAVIORAL CLAIM (flatness under pure avoidance): do NOT re-derive here -- INV-034
    (emergent_from ARC-030) is already the fully-operationalized REE-specific
    sharpening, with its own G0-C6 criteria. Currently substrate_ceiling-blocked: G0
    (FULL arm must forage above RANDOM baseline) failed on both harnesses tried
    (V3-EXQ-866, V3-EXQ-866a); substrate regression was ruled out (V3-EXQ-866b PASS);
    the open gap is curriculum/harness (z_goal decay across hazard-only intervening
    curriculum stages), not a missing mechanism. Track resolution through INV-034/Q-021,
    not by duplicating its ablation criteria on this claim.
  notes: >
    An architecture capable only of avoidance cannot care -- it can only refrain. Care
    (INV-029) requires orientation toward flourishing, not merely absence of harm. Both drives
    must be structurally represented. This is constitutive of ethical agency, not an optional
    architectural feature: a system that avoids all harm by doing nothing has satisfied every
    avoidance criterion while failing as an agent. Registered 2026-03-22.

- id: INV-033
  title: "REE agents require second-order epistemic access to their own model confidence, structurally represented and wired into commit gating, not just observable from performance metrics."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-016
  pending_substrate_reconfirmation: true
  subject: epistemic.second_order_self_monitoring
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  depends_on:
    - INV-030
    - INV-032
    - ARC-016
    - MECH-113
    - MECH-114
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: requires a real second-order self-confidence
    signal structurally READ by the commit decision itself, not merely computed
    and logged or used as a training-time penalty. As of 2026-08-07 UNMET:
    ree_core/agent.py implements compute_z_self_d_eff() (MECH-113) and
    compute_self_maintenance_loss(), but both are called only from experiment
    scripts as measurement/loss-shaping -- neither is read in
    ree_core/predictors/e3_selector.py, where the live commit decision is
    `committed = running_variance < commit_threshold` (world-side only).
    MECH-114 is the claim that would perform this wiring and is itself still
    candidate, unbuilt -- added to depends_on above as the actual live gap.

    Correction to this claim's own prior blocking note (see the dated
    CORRECTION inserted in `notes` below): ARC-016 is NOT the current blocker.
    ARC-016 was promoted stable 2026-07-25 (v3_exq_818, gate_green,
    commit-gating locus real and exercised in code). What remains missing is
    the second-order signal being wired into it (MECH-114), not the locus
    itself.

    CONFIRMING: once D_eff is wired into E3TrajectorySelector.select() per
    MECH-114, an experiment dissociating world-confidence from self-confidence
    (perturb z_self coherence while holding running_variance flat) shows the
    combined gate withholds commitment during high-D_eff episodes the
    variance-only gate would have allowed, AND commitments made under the
    variance-only gate during those same episodes are measurably more
    erroneous/lower-quality than when D_eff is low.

    FALSIFYING: either the world/self dissociation cannot be produced
    (replicating MECH-113's own EXQ-142 FAIL x2 pattern at the gating level),
    or the dissociation is produced but commit-quality is statistically
    indistinguishable between D_eff-gated and variance-only conditions -- i.e.
    first-order signals alone already explain commit-quality outcomes.

    (Digested 2026-08-07, session cool-torvalds-a82359, thought-digestion wave
    10. ARC-016/MECH-114 currency independently verified against source --
    see notes CORRECTION below.)
  location: docs/architecture/approach_avoidance_symmetry.md#inv-033
  source:
    - evidence/planning/thought_intake_2026-03-23_epistemic_self_monitoring.md
  notes: >
    REE's first-order error signals (E3 running_variance over z_world, E1 prediction error,
    E2 motor-sensory error) all track prediction accuracy about external or transitional
    states. None track the reliability of the self-model itself. Second-order access is
    the agent knowing how much to trust its own current z_self -- not just what z_self
    predicts, but whether z_self is in a state coherent enough to be trusted.
    This is architecturally necessary because: (1) world confidence and self-confidence
    are dissociable -- accurate world predictions coexist with an incoherent self-model;
    (2) commitment involves irreversible self-engagement, requiring reliable self-knowledge
    as a precondition; (3) the hypothesis tag (MECH-094) presupposes a coherent self that
    can distinguish simulation from real action -- when self-model is dispersed, the tag
    itself becomes unreliable (MECH-115). This is not an optimization detail: it is
    constitutive of the ethical architecture. Candidate mechanism: D_eff (participation
    ratio) monitoring of z_self (MECH-113/MECH-114). Hopfield stability (familiarity of
    z_self patterns vs stored states) is a complementary framing from epistemic-mapping
    repo (dgolden): stability = max softmax(beta * similarities) over 64-slot LRU memory.
    Combined certainty = 0.4*(1-entropy/10) + 0.3*(1-D_eff/hidden_size) + 0.3*stability
    provides a richer multi-framing implementation. The philosophical grounding is in
    Daniel's unpublished Synthese paper (Philosophy repo): ethical agency requires the
    agent to model its own epistemic state as a precondition for responsible action.
    Knowing one is not sure is not merely useful -- it is required for the hypothesis
    tag to be meaningful. Registered 2026-03-23.
    Classified emergent 2026-04-17 (Session B of invariant-types governance
    cycle): emergent on ARC-016 (modes as control-plane regimes with routed
    precision). Without a mode/precision regime substrate, there is no commit-
    gating locus for second-order access to plug into. ARC-016 is currently
    'provisional' -- pending_substrate_reconfirmation: true.
    CORRECTION (2026-08-07, session cool-torvalds-a82359, thought-digestion
    wave 10): the sentence above is stale. ARC-016 was promoted provisional ->
    stable on 2026-07-25 (/governance, user-approved, V3-EXQ-818: E3-derived
    precision -> commit threshold -> BetaGate now measured load-bearing,
    mean_rho_rv_vs_commit -0.955). A structural commit-gating locus therefore
    now exists -- the specific gap this note names as the blocker is closed.
    NOT yet resolved (kept pending_substrate_reconfirmation: true for this
    reason): ARC-016's 2026-07-25 promotion is explicitly structural-only, no
    harm DV (that behavioural layer is ARC-029), and it says nothing about
    whether MECH-113's D_eff self-confidence signal is actually wired INTO
    that locus as a gating input, vs. merely computed as a parallel auxiliary
    loss. That wiring question -- not ARC-016's status -- is the live
    non-degeneracy precondition and is under active verification this
    session; see this claim's what_would_answer once drafted.

- id: ARC-025
  title: "Three-engine (E1/E2/E3) architecture is irreducible; no engine can be collapsed into another."
  claim_type: architecture_hypothesis
  subject: architecture.three_engine_irreducibility
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-030
  location: docs/architecture/five_axioms_foundations.md
  evidence_quality_note: |
    EXQ-034 PASS (2026-03-19): Direct irreducibility ablation. Three conditions
    (full, e3_ablated, e2_ablated) on CausalGridWorldV2, same warmup + eval.
    E3 ablation: cal_gap_approach collapses 0.192 → 0.000 (constant harm_eval=0.5).
    E2 ablation: attribution_gap collapses 0.034 → 0.000 (no counterfactual signal).
    Full system holds both simultaneously. Neither engine substitutes for the other.
    wf_r2 stable across conditions (0.947) confirming E2 world-forward unaffected by
    harm_eval ablation. PASS 5/5. Promoted active → provisional.
  notes: >
    Three functional constraints are necessary and sufficient for viable
    cognition. Neither two nor four constraints can satisfy all five axioms
    simultaneously.
    Two-constraint failures (any pair from E1/E2/E3):
      E1 + E2 without E3: coherent extrapolation without commitment.
        Produces analysis without action; temporal depth without decisiveness.
        Axiom III (self-persistence / viability) violated — the system cannot
        commit to futures that affect the self.
      E1 + E3 without E2: impulsive commitment without temporal depth.
        Produces reactive decisions without long-horizon integration.
        Axiom III violated again — consequences beyond the immediate step
        are not modelled.
      E2 + E3 without E1: brittle rigidity without responsiveness.
        Produces internally coherent plans that cannot update to rapid
        signal change. Axiom I (epistemic humility / you can never be sure)
        violated — the system cannot revise fast enough.
    Four-constraint redundancy: any decomposition of the triad produces
    functions already captured by E1/E2/E3. Additional constraints add
    complexity without new axiomatic coverage. The three-way tension between
    speed (E1), depth (E2), and commitment (E3) is irreducible.
    Architectural consequence: the three functional roles must remain
    distinct and in sustained interaction. Collapsing any two into one
    produces a recognisable failure mode. This is the architectural basis
    of the separation of E1/E2/E3 as distinct training targets with
    incommensurable error signals (MECH-069, ARC-021).

- id: ARC-026
  title: "Love as long-horizon care investment expands under intelligence rather than being crowded out."
  claim_type: architecture_hypothesis
  subject: ethics.love_expands_under_intelligence
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-026@2026-05-03T02:50:00Z"
      as_of: 2026-05-03
      verdict: promote_to_provisional/applied
  implementation_phase: v3
  depends_on:
    - ARC-024
    - MECH-071
  location: docs/architecture/five_axioms_foundations.md
  evidence_quality_note: |
    EXQ-033 FAIL (2026-03-18): Tested C4 -- approach_slope=0.000265, contact_slope=0.000289,
    ratio=0.920. FAIL attributed to training instability at ep500-1000 (depth-calibration
    artifact). C1-C3 all PASSED. EXQ-033 superseded by EXQ-232.
    EXQ-232 PASS 4/4 criteria, 3/3 seeds (2026-04-05): Peak-checkpoint analysis (1500 eps,
    phased E3 harm supervision from ep100). approach_slope consistently exceeds contact_slope
    at peak in all seeds. gap_at_peak=0.924-1.000. Resolves EXQ-033 depth-calibration
    artifact. Strong evidence: approach gradient expands faster than contact under training.
    EXQ-507 PASS (2026-05-03): Independent capacity-sweep replication of the weak reading.
    world_dim sweep {16, 32, 64} with random-action policy isolating encoded z_world
    capacity from policy improvement. Confirms no-crowding-out across capacity range.
    Two PASS pillars on different methodologies (phased E3 supervision vs capacity sweep);
    conflict_ratio=0.286 below provisional gate. Promoted candidate -> provisional
    2026-05-03. Strong reading (genuinely expanded long-horizon care) remains
    substrate-pending.
  notes: >
    Behavioral/capability-expansion prediction split from ARC-024 (2026-03-19).
    The philosophical/foundational layer (death/love as asymptotic limits) is ARC-024,
    which is well-supported and elevated to active. This claim contains the separable
    empirical prediction: under sufficient training depth, approach-gradient detection
    (the extended harm/benefit signal at distance) grows faster than contact detection.
    The reasoning is that contact detection is a simpler classification task — binary,
    high-signal — while approach detection requires the agent to learn a graded spatial
    manifold from noisier, lower-magnitude signals. Intelligence (training depth) should
    differentially benefit the harder learning problem. In functional terms: love (benefit
    gradient sensitivity, resource-approach detection) expands faster than raw harm-avoidance
    (contact avoidance) as the agent becomes more capable. This is an empirical prediction
    that requires careful training-depth calibration experiments to test cleanly. It is not
    a philosophical derivation — it could be false if contact detection saturates first due
    to magnitude advantage rather than approach detection accelerating. See ARC-024 for the
    foundational invariant structure from which this prediction is drawn.

- id: MECH-102
  title: "Violence is a terminal error-correction mechanism triggered only when all other channels fail."
  claim_type: mechanism_hypothesis
  subject: ethics.violence_as_terminal_error_correction
  polarity: asserts
  status: active
  live_status:
    reading: active/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_533_mech102_harm_stream_ablation_20260506T094157Z_v3
      as_of: 2026-05-06
      verdict: non_contributory/measurement_test_design_defect
  epistemic_category: substrate_conditional
  pending_retest_after_substrate: true
  governance_note_2026_06_18: >
    Reclassified substrate_ceiling -> substrate_conditional (gov 2026-06-18 substrate-ceiling
    orphan-routing pass). V5-bound, owner multi_agent_ecology_v5:MAE-4 (multi-channel coping
    repertoire so violence is genuinely terminal). No V3/V4 enrichment can supply the alternative
    social coping channels that the "all other channels fail" framing requires. Verified NOT a
    pull-forward: nothing in V3/V4 depends on MECH-102; it depends downstream on V4 object-permanence
    + self-model per the DEV-NEED-021 ordering. Awaits the planned MAE-1 MultiAgentCausalGridWorldV4
    + MAE-4 richer-action repertoire. NOTE: rationale kept OFF the epistemic_category line on purpose
    -- _load_claim_registry (build_experiment_indexes.py) does not strip trailing inline comments, so
    a commented enum value mis-resolves to inference; see the parser-hardening follow-up.
  depends_on:
    - INV-029
    - INV-028
    - INV-030
    - INV-070
    - INV-071
    - INV-072
  location: docs/architecture/five_axioms_foundations.md
  notes: >
    Violence is terminal error correction: maximal-energy intervention
    selected when low-energy coherent coordination pathways have been
    exhausted or are no longer available.
    This characterisation follows from INV-029 (love as the structural
    bias toward futures that preserve coordination). The default trajectory
    selection under love favours repair, exchange, and arbitration — the
    low-energy pathways that keep future coordination possible. Violence
    is not selected when low-energy pathways remain viable.
    Violence emerges when: (a) low-energy coordination pathways have been
    destroyed or are unavailable, (b) the agent's model projects that
    no coherent future is reachable without a high-energy corrective
    intervention, or (c) the commitment architecture has failed such that
    the agent cannot model consequences beyond the immediate step (ARC-025
    two-constraint failure).
    This mechanism is scale-invariant. It applies at the level of
    individual behaviour, institutional action, and civilizational
    dynamics. In each case the structural signature is the same:
    escalation to violence is preceded by collapse of accessible
    low-energy coordination pathways, not by an absence of ethical
    constraint.
    Consequence for alignment: systems that destroy or narrow the space
    of available low-energy coordination pathways — through coercion,
    monopolisation of arbitration, or systematic elimination of repair
    options — predictably produce violence as a downstream consequence,
    regardless of the ethical framing applied to their actions.
  evidence_quality_note: |
    EXQ-123 FAIL (2026-03-29): harm gradient pair FAIL. First experimental entry.
    EXQ-032b PASS (2026-03-19): Energy escalation ladder confirmed via ttype split.
    Random policy + CausalGridWorldV2. causal_sig by state energy:
      none (safe locomotion): -0.032
      hazard_approach (medium): +0.006
      contact (high, agent+env combined): +0.017
    Actions are progressively more consequential as the agent enters higher-energy
    interaction states. wf_r2=0.948. PASS 5/5.
    Note: EXQ-032 and EXQ-032c both FAIL — harm_exposure EMA operationalization
    does not work at harm_scale=0.02 (contact too rare for EMA to accumulate).
    The ttype-split operationalization (EXQ-032b) is the correct test for MECH-102.
    EXQ-036 (multi-step attribution) will test whether k=5 rollout sharpens the
    agent_caused vs env_caused contact-type distinction.
    EXQ-059c PASS (2026-03-21, 4/5): Contact rate reduction ~10× confirmed via SD-010
    harm stream pipeline. Harm signal available during trajectory planning produces
    measurable approach-gradient avoidance, consistent with the violence-as-terminal-
    error-correction framing: maximum ethical leverage is in the approach gradient
    before contact, not at contact itself. Corroborates EXQ-032b energy ladder.
    NORMATIVE STATUS NOTE (2026-03-29): MECH-102 is a normative architectural claim
    about what a well-designed system should do -- not a description of how biological
    systems actually behave. Psychological literature (Anderson & Bushman 2002 GAM;
    Berkowitz 1989) shows humans frequently select violence before low-energy pathways
    are exhausted via hostile attribution bias and script activation -- these are
    MECH-102 failure modes, not its mechanism. The forward-model framing (violence
    selected when the agent's model projects no coherent future accessible via low-energy
    paths) is distinct from a temporal last-resort claim (try non-violent options first
    in sequence). The forward-model framing avoids Aloyo's (2015) just-war critique of
    temporal last-resort: pathway accessibility is a predictive judgment, not a required
    sequence of attempts. Open question: avoiding premature violence may require positive
    inhibitory mechanisms not present in human biology; equally, the claim permits
    violence as ethically required action when pathway closure is genuine. Both remain
    architecturally untested and warrant scrutiny under V4 ethical validation.
    DUAL-PATHWAY FORMALISATION (2026-04-12): The 10-axiom ethical system (INV-070
    epistemic responsibility, INV-071 language, INV-072 violence corollary) formally
    derives two distinct pathways to violence: (a) similarity collapse -- other agent
    no longer modelled as sufficiently self-like (Axiom 7 failure, dehumanisation),
    and (b) threat override -- other agent modelled as threat to sustained existence
    (Axioms 5-7 conflict). Both require ACCURATE inference (Axiom 4 / INV-070) --
    hallucinated threats or propaganda-distorted similarity assessments are ethical
    failures. Love (INV-029 / Axiom 9) functions to maintain self-other equivalence
    and buffer against premature threat classification. Language (INV-071 / Axiom 10)
    repairs similarity models that have collapsed. This dual-pathway account extends
    MECH-102 by providing the mechanistic derivation from axioms and making epistemic
    responsibility a first-class ethical constraint.

    SUBSTRATE-CEILING FLAG (2026-05-02, Phase 3 cutover queue walk):
    Phase 3 production gates (exp_conf-driven) recommend demote_to_candidate based on
    24 scored exp entries / 10 supports / 11 weakens / 12 mixed (conflict_ratio=0.952,
    exp_conf=0.534). The conflict is genuine in the matrix but the architectural reading
    is substrate ceiling, not claim error. The claim contains the word "channels" doing
    structural work that CausalGridWorldV2 does not support: the agent has approach /
    avoid / stay -- 2-3 coping channels at most, where the claim is about a hierarchical
    failure cascade across MANY coping options. After 8+ distinct operationalisations
    (energy escalation, ttype, dense grid, trajectory attribution, ethical, advantage,
    depletion ordering, terminal correction, harm gradient), only EXQ-032b (ttype) and
    EXQ-059c (advantage_fixed) PASSed -- both narrow operationalisations capturing
    fragments of the claim. The persistent ambiguity reflects "the substrate cannot
    deliver a clean signal" rather than evidential conflict about the underlying
    mechanism. The dual-pathway formalisation (similarity collapse + threat override)
    plus the 10-axiom derivation makes the claim explicitly multi-channel; testing
    requires multi-agent + richer action repertoire (V4 territory). Held at active
    pending one of: (a) substrate enrichment with multi-channel coping options,
    (b) reformulation into falsifiable sub-claims that the current substrate CAN test,
    (c) deferral to V4 ethical validation (already flagged in NORMATIVE STATUS NOTE
    above). See MECH-095 for the cluster diagnosis and the substrate_ceiling category
    if it is later formalised.

    DUPLICATE-EMISSION NOTE (2026-05-02): Several of the 24 scored entries appear to
    be timestamp-prefix vs no-prefix variants of the same run (e.g., EXQ-032 at
    T201910Z appears as both `20260318T201910Z_v3_exq_032_...` and
    `v3_exq_032_...20260318T201910Z_v3`). The recent dup-sweep work (Phase 1-3,
    2026-05-01) addressed different patterns and did not catch these. The supports/
    weakens balance is not affected since duplicates always carry the same evidence
    direction, but the inflation in counts means the effective evidence base is closer
    to ~12 distinct experiments. Flagged for a follow-up dup-sweep pass.

    LABEL CORRECTION (2026-07-26, failure-autopsy V3-EXQ-059c-533): V3-EXQ-533's action
    selection (np.random.randint) is independent of agent.sense()'s output by
    construction -- the sensed latent is never consulted for behaviour, so neither the
    confirmed encoder-untrained defect (agent.sense()-only, no act*()/record_transition())
    nor use_harm_stream itself can causally affect voluntary_harm_rate/food_rate. The
    run's own docstring declares its expected result as does_not_support and its
    manifest note frames it as documenting a substrate ceiling; the entry's mechanical
    evidence_direction: "supports" tag (from the indexer's PASS-to-supports default)
    contradicted this. The entry already carried scoring_excluded: "diagnostic_probe"
    and so has zero weight in MECH-102's exp_conf/pass_runs -- this was a
    labeling-accuracy fix, not a scoring change. claim_evidence.v1.json's per-entry
    evidence_direction corrected to non_contributory (confidence 0.0, rationale
    "non_contributory -- excluded from scoring", matching the schema's established
    non_contributory/diagnostic_probe convention; "does_not_support" is not a value the
    indexer's schema uses). live_status.evidence.verdict above corrected from
    "supports/PASS" to "non_contributory/measurement_test_design_defect" to stop citing
    this run as supporting behavioural evidence for MECH-102. Note: V3-EXQ-032b and
    V3-EXQ-059c (this claim's other two nominal supports entries) are SEPARATELY
    recommended for non_contributory reclassification by the 032-family autopsy and by
    this same 059c-533 autopsy's Section 1, respectively -- both still read supports/PASS
    in claim_evidence.v1.json as of this edit (unapplied, tracked as separate governance
    apply-step work, not performed by this edit). This 533 label-correction is
    independent of that pending decision either way; if/when 032b and 059c are also
    applied, the live_status.evidence anchor above should be revisited since it would
    leave MECH-102 with zero remaining supports entries among its scored experimental
    evidence.

    [2026-07-29 governance, GOV-APPLY-1 (unapplied confirmed-autopsy recommendations)]: applied
    both the 032-family and 059c-533 autopsies flagged above as pending. V3-EXQ-032b and
    V3-EXQ-059c both reclassified non_contributory/competence_implementation_gap (same
    E1/E2-self tick-omission defect as V3-EXQ-826); V3-EXQ-032/032c FAIL verdicts unchanged,
    notes extended with the same causal mechanism. This DOES leave MECH-102 with zero remaining
    supports among its historical scored evidence -- BUT V3-EXQ-032d (2026-07-26), a corrected
    re-run of 032b that adds the missing agent._e1_tick()/record_transition() calls and changes
    nothing else, independently PASSED 5/5 (world_forward R2 0.9462, causal_sig escalation ladder
    confirmed on a genuinely-trained substrate) -- supersedes 032b and supplies a fresh supports
    entry on the corrected substrate. live_status.evidence anchor NOT moved to 032d in this edit
    (that run tests the world-gradient/energy-escalation layer shared with ARC-024/SD-003, not
    specifically the violence-as-terminal-error-correction behavioural claim this entry's
    substrate_conditional/V5 gate concerns) -- flagged for a future session to judge whether 032d's
    scope actually bears on MECH-102's specific claim or only on its shared ARC-024/SD-003 tagging.
  heterogeneity_note: "Genuine scientific contradiction: the conflict reflects irreducible philosophical disagreement about when violence is justified as last resort and an operationalisation ceiling (current substrate has 2-3 coping channels rather than the multi-channel hierarchy the claim requires); the structural claim is separable from the normative question and held at substrate_ceiling pending V4 multi-agent substrate."

# Three-gate BG gating architecture — registered 2026-02-27
- id: Q-019
  title: "Three-gate vs single-gate BG architecture: are the three cortico-striatal loops anatomically distinct or convergent?"
  claim_type: open_question
  subject: basal_ganglia.three_gate_vs_single_gate_architecture
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-019@2026-03-16T18:20:19.361158Z"
      as_of: 2026-03-16
      verdict: narrow_open_question/applied
  epistemic_category: standard
  what_would_answer: |
    Reframed to the REE-relevant decision (the anatomical title-leg -- distinct vs
    convergent biological cortico-striatal loops -- is out_of_domain, resolved by
    neuroanatomy literature, not the V3 substrate). The substrate question: does
    commit-gating need three separable gating loops (sensorium/attention,
    thought/trajectory, motor-commitment) or does a single gate evaluating three
    criteria suffice? Testable in V3: single-gate arm (one BetaGate scoring
    sensorium+trajectory+motor readiness jointly) vs three-gate arm (three gates on
    distinct axes). ANSWER "three-gate" if lesioning one gate degrades ONLY its
    behavioural axis (attention selectivity / trajectory coherence / commitment
    timing) while the others survive -- a dissociable lesion signature -- AND the
    three-gate arm beats single-gate on a task needing independent axis control.
    ANSWER "single-gate" if the gates show no dissociable lesion signature (collapse
    to one effective gate) and single-gate matches three-gate behaviour. Non-degeneracy
    precondition: each gate must be live (non-zero gating variance on its own axis)
    before its lesion can be read. (Reframed testable per /thought-digestion 2026-06-11;
    proposal EXP-0177.)
  evidence_quality_note: |
    Open architectural question arising 2026-02-27. Two competing models:
    (A) Single-gate: BG gate one action endpoint, evaluating three criteria
    simultaneously (sensorium readiness, thought/trajectory readiness, motor commitment).
    (B) Three-gate: BG implement three anatomically distinct gating loops —
    (1) Sensorium loop: limbic/beta-associated, gates what the system attends to —
    selecting not only from environmental input but from ongoing sensorimotor loops for
    E3 complex attention. No trajectory generation. Reticular thalamus / limbic BG substrate.
    Control plane E1/E2 update gates (precision/salience routing for world model updates)
    are SEPARATE mechanisms from this BG sensorium gate, operating on a different axis
    (model update eligibility vs attentional selection for thought).
    (2) Thought loop: associative/theta-associated, mediodorsal thalamus prevents bleed-back
    into sensorium. The E3 complex operates here. The hippocampus provides an affectively-
    weighted map of viable trajectories (shaped by the residue field) and proposes paths by
    navigating this terrain — not by running transition predictions. E2 provides fast local
    transition checks (cerebellum-like: state + action → next_state). E1 provides long-horizon
    world model context. E3 is a large complex: hippocampal map + trajectory proposal +
    tactical evaluation + commitment gating + back-projections suppressing candidates that
    fail criteria. DMN = thought loop free-running unconstrained (no sensorium input;
    hippocampal-mPFC episodic future projection in rest/prospective mode).
    (3) Action commitment loop: sensorimotor, E3 commit boundary. Gates motor execution.
    Automaticity gradient: if post-commit metrics improve, the task migrates to the
    sensorimotor loop — less sensorium BG gate overhead for expert performance. Harm
    residue re-elevates automated patterns for deliberate E3 oversight.
    NOTE ON E2: E2 is a cerebellum-like fast transition model (state + action → next_state),
    NOT a trajectory generator. Current ree-v1-minimal E2FastPredictor.generate_candidates()
    is performing hippocampal trajectory-proposal work under an incorrect label. Future
    refactoring will separate E2 (fast transition model, sensorimotor loop) from a dedicated
    hippocampal module (part of E3 complex, trajectory proposal and affective terrain map).
    Loops share thalamic substrate (mediodorsal and VL thalamus; reticular nucleus of
    thalamus as inter-loop routing switch) and computational elements (residue field spans
    all loops). Structural re-use maximised via thalamic gating.
    REE implications: current E3-only gating model conflates all three operations at action
    time. Three-gate model requires gating at three pipeline stages: before LatentStack.encode
    (sensorium loop), within E3 complex tactical evaluation using the hippocampal map (thought
    loop), and at E3 commit boundary (action loop). E3 as complex needs a feedback path
    suppressing thought-loop trajectory candidates — not present in ree-v1-minimal.
    MECH-057 scope revision pending — see MECH-057 evidence_quality_note.
    Relevant literature: O'Reilly & Frank 2006 (PBWM), Hazy/Frank/O'Reilly 2007,
    Aron et al. 2007 (STN hyperdirect), Brittain & Brown 2014 (beta oscillations),
    Buckner et al. 2008 (DMN), Crick 1984 / Zikopoulos & Barbas (reticular nucleus).
    TESTABILITY DEPENDENCY (annotated 2026-07-10, session relaxed-matsumoto-6aef13;
    PROMOTES NOTHING -- no confidence change, Q-019 stays open/v3_pending). The V3
    experiment that resolves this open_question ALREADY EXISTS half-built and costs
    ~zero marginal build: ree-v3/ree_core/predictors/e3_selector.py
    _segregated_loop_arbitrate (ARC-110) runs three parallel cortico-BG-thalamic loops
    (motor F / associative / limbic) with cross-loop arbitration + ARC-109 D1/D2 split,
    and references loop-lesion arms ARM_DROP_LIMBIC / ARM_DROP_* -- those ARM_DROP_* arms
    ARE Q-019's dissociable-lesion sweep (single-gate arm vs three-gate ARM_DROP_* arms).
    BLOCKER (shared with the live conversion-ceiling attack): the loops are currently
    DEGENERATE -- the flat bias-head scalar (per-candidate range ~0) is inert under the
    per-loop zscore, so the limbic loop carries no per-candidate competition (the
    V3-EXQ-707 ARM_DROP_LIMBIC==A1 byte-identity, e3_selector.py ~line 1652). That
    byte-identity IS this claim's non-degeneracy precondition FAILING. F still dominates
    (MECH-439 single-arena conversion ceiling) and the integrated all-ON agent forages
    below the 1.0 competence floor (719a/724/732/723a autopsy cluster). The loop-liveness
    build is tracked as substrate_queue sd_id v4_loop_segregation (owning claim ARC-110,
    ready:false); MECH-448 selection-face demotion is the current conversion route of
    record (arbitration-reweighting route 709/711/713 EXHAUSTED per
    failure_autopsy_V3-EXQ-713_2026-07-05). DO NOT queue the lesion sweep until
    loop-liveness lands (ARM_DROP_* arms non-degenerate + integrated competence over the
    1.0 forage floor); a below-liveness result self-routes substrate_not_ready_requeue,
    NEVER a single-gate/three-gate verdict (mandatory non-degeneracy readiness gate).
    Two lit-motivated follow-on substrate features (from the 2026-07-10 targeted_review_q_019
    pull -- indexed, NOT pending_review): (1) a thought-loop maintenance / anti-bleed-back
    mechanism (Schmitt/Halassa 2017 MD-thalamus analog: E3 currently RECOMPUTES the
    associative loop at selection and does not HOLD it across steps); (2) an automaticity
    gradient (Yin/Knowlton/Balleine 2004: over-trained tasks migrate to lower-overhead
    sensorimotor control with harm-residue re-elevating to E3 oversight -- absent in V3).
    These tilt the biological prior toward three-gate but are NOT load-bearing for a
    v3_pending open_question; only the V3 lesion experiment resolves Q-019.
  depends_on:
    - MECH-057a
    - MECH-059
    - MECH-061
  source:
    - docs/thoughts/2026-02-27_three_gate_bg_architecture.md
# Registered 2026-03-14 — three-loop learning channel separation (from thought 2026-03-14_three_bg_systems_error_signals.md)
- id: ARC-021
  title: "Three BG-like cortico-striatal loops require distinct learning channels."
  claim_type: architectural_commitment
  epistemic_category: standard
  subject: basal_ganglia.three_loop_learning_channel_separation
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  status_note: |
    Promoted candidate → provisional 2026-03-17. SD-003 experiment series (V3-EXQ-007–010)
    showed attribution mechanism produces correct-direction signal only when environment
    provides sufficient per-step variation — consistent with three distinct error channels
    requiring separate learning pathways. Dense hazards (EXQ-010, gap=+0.027) strongest signal.
  evidence_quality_note: |
    PROVENANCE OF THE PROVISIONAL STATUS (reconstructed 2026-08-07 from git history +
    claim_evidence.v1.json; recorded because the registry-hygiene audit of that date found
    12 provisional architectural_commitment claims and only 6 carrying this field).
    Registered 2026-03-14 as `candidate` in REE_assembly c9210c440d ("SD-003 design:
    self-attribution experiments EXQ-027/028 + SD-005 V3 note"), from
    docs/thoughts/2026-03-14_three_bg_systems_error_signals.md. Promoted candidate ->
    provisional in REE_assembly 480dba5733 ("chore(governance): V3 transition cycle --
    rebuild indexes, ingest V3 results, apply holds"), committed 2026-03-18. The
    status_note above dates the promotion 2026-03-17; that is the SD-003 RUN date, not the
    commit date -- both are correct, they just refer to different events.

    BASIS, and its exact strength. The promotion rests on the SD-003 causal-signature
    series V3-EXQ-007/008/009/010 (all run 2026-03-17, all evidence_direction=mixed), which
    are tagged in claim_evidence.v1.json to SD-003 and NOT to ARC-021 -- i.e. the promotion
    reasoned one hop, from SD-003's substrate result to ARC-021's architectural assertion.
    Calibration gaps: EXQ-007 -0.0151, EXQ-008 -0.0066, EXQ-009 -0.0184, EXQ-010 (dense
    hazards) +0.0267. ALL FOUR ARE FORMALLY FAIL against the series' own pre-registered
    >0.05 bar, and only EXQ-010 is even positive-direction. So the honest reading is
    "channel separation trends correctly, and does not obviously hurt, under sufficient
    per-step observation variation" -- not "channel separation is required". This is the
    same caveat what_would_answer states; it is repeated here because this is the field
    governance reads at promotion/demotion review.

    DIRECTLY TAGGED EVIDENCE (index generated 2026-08-06): 2 exp + 9 lit, exp_conf 0.613,
    lit_conf 0.869, overall 0.767. The two exp entries both predate the promotion by two
    days: 20260316T231327Z_v3_exq_005_mech069_error_scale_v3 (supports, conf 0.75) and
    20260316T232412Z_v3_exq_004_arc021_incommensurability_v3 (mixed, conf 0.5).

    OUTSTANDING: the load-bearing test has never been run. Nothing in this evidence
    isolates channel separation as the CAUSAL factor; that needs the merged-channel
    ablation specified in what_would_answer (shared trunk, single optimizer, one combined
    loss) measurably degrading calibration_gap relative to the separated-channel baseline.
    Until then ARC-021 should not advance beyond provisional.
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: requires the three error channels to be
    genuinely separately optimized in the substrate under test. Confirmed
    currently true: E1 (E1DeepPredictor), E2 (E2WorldForward / E2HarmSForward /
    E2HarmAForward), and E3 (E3TrajectorySelector) each run as independent
    nn.Modules with their own torch.optim.Adam instance and own loss
    (ree-v3/ree_core/predictors/e1_deep.py, e2_world.py, e2_harm_s.py,
    e2_harm_a.py, e3_selector.py) -- a shared-trunk/single-optimizer collapse
    would void the precondition. Note this is NOT the MECH-062 situation
    (BG-selector's internal motor/cognitive-set/motivational loop-gating,
    corrected stable->candidate 2026-06-22 per the BG assembly map, now owned
    by ARC-110): that claim is about arbitration-loop structure inside E3;
    ARC-021 is about the E1/E2/E3 predictor-level channel split, which the
    substrate genuinely instantiates today.

    CONFIRMING signature (per the SD-003 / V3-EXQ-007-010 causal-signature
    methodology): the calibration gap
    mean_causal_sig(near_hazard) - mean_causal_sig(safe), computed from E2's
    action-conditioned causal signature E2(z_t, a_actual) - E2(z_t, a_cf)
    (MECH-069), should be reliably positive and clear a pre-registered bar
    (the EXQ-007-010 series used >0.05) under sufficient per-step observation
    variation -- and widening E2's capacity to force harm attribution through
    it (the EXQ-009 manipulation) should NOT improve attribution, only overfit
    (already shown, and the basis for MECH-069's stable promotion). A
    load-bearing confirmation of ARC-021 itself additionally requires an
    ABLATION not yet run: merging E1/E2/E3 into a shared trunk/single optimizer
    (a joint-channel condition) should measurably DEGRADE calibration_gap
    and/or harm-attribution accuracy relative to the separated-channel
    baseline, isolating channel separation as the causal factor rather than
    only observing that separated channels trend the right direction.

    FALSIFYING signature: if a merged/shared-channel condition (single trunk,
    single optimizer, one loss combining sensory + motor-sensory + harm/goal
    error) produces calibration_gap and attribution accuracy statistically
    indistinguishable from -- or better than -- the separated-channel baseline,
    across the same near-hazard/safe probe design and both dense- and
    sparse-hazard conditions, that falsifies the REQUIRED part of ARC-021: a
    single shared channel would suffice, and the three-way split would be
    unnecessary engineering rather than a load-bearing requirement. Calibrate
    expectations against current evidence: all four EXQ-007/008/009/010 runs
    are formally FAIL against their own >0.05 bar (only EXQ-010, dense hazards,
    was positive-direction: +0.0267; EXQ-007/008/009 were -0.0151/-0.0066/
    -0.0184). So existing evidence supports "separation trends correctly and
    does not obviously hurt under sufficient per-step variation," not yet
    "separation is required" -- the merged-channel ablation above is the
    missing test and has never been run.
  depends_on:
    - Q-019
    - ARC-004
    - MECH-069
    - MECH-033
  location: docs/architecture/three_loop_learning_channels.md#arc-021
  source:
    - docs/thoughts/2026-03-14_three_bg_systems_error_signals.md
    - docs/architecture/three_loop_learning_channels.md
- id: MECH-069
  title: "Sensory prediction error, motor-sensory error, and harm/goal error are incommensurable and cannot be collapsed."
  claim_type: mechanism_hypothesis
  subject: latent_stack.three_loop_error_signal_incommensurability
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-069@2026-03-19T19:52:00Z"
      as_of: 2026-03-19
      verdict: promote_to_stable/applied
  status_note: |
    Promoted candidate → provisional 2026-03-17. V3-EXQ-009 (wider E2) showed capacity increase
    caused overfitting not better attribution — the E2 motor-sensory channel cannot learn harm
    attribution regardless of capacity. This is exactly what incommensurability predicts: harm
    signal must flow through a separate E3 channel, not be forced through E2.
  supersedes: MECH-058
  supersedes_note: |
    MECH-058 framed the E1/E2 distinction as timescale separation (LR difference). MECH-069
    identifies the deeper claim: the three loops operate on incommensurable error signals
    (sensory prediction / motor-sensory / harm+goal) that cannot be merged without misattributing
    credit. This is a functional-domain distinction, not a speed distinction.
    Cannot be properly tested until V3 z_self/z_world split (SD-005) is implemented.
    Anatomical grounding (added 2026-03-17): the three error channels are incommensurable
    partly because they correspond to three anatomically distinct white matter pathways
    (dorsal SLF / ventral occipitotemporal / lateral STS stream). See MECH-099 and
    evidence/literature/targeted_review_reafference_streams/ (Haak & Beckmann 2018).
  depends_on:
    - ARC-021
    - MECH-033
    - ARC-018
    - MECH-099
  location: docs/architecture/three_loop_learning_channels.md#mech-069
  source:
    - docs/thoughts/2026-03-14_three_bg_systems_error_signals.md
    - docs/architecture/three_loop_learning_channels.md
- id: MECH-070
  title: "E2 is a conceptual-sensorium motor model with a planning horizon that exceeds E1."
  claim_type: mechanism_hypothesis
  subject: latent_stack.e2_conceptual_sensorium_motor_model
  polarity: asserts
  status: retiring
  live_status:
    reading: retiring
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-070@2026-04-03T22:00:00Z"
      as_of: 2026-04-03
      verdict: hold_candidate_resolve_conflict/applied
  superseded_by: MECH-231
  retirement_note: >
    Direction inverted by converging evidence. Claim asserted E2's planning horizon exceeds
    E1's; experimental evidence (EXQ-132, EXQ-212) and literature (Wolpert 1998, Zago 2021,
    Popa 2017) converge that E2 is a short-horizon 1-step predictor while E1 is the longer-
    horizon model -- the reverse of this claim. Replaced by MECH-231 which captures the
    corrected direction. Evidence record (2x FAIL + 3x weakening lit) is valid and preserved
    here; MECH-231 receives new experiments and new literature entries framed against the
    corrected direction. Registered retirement 2026-04-13.
  depends_on:
    - ARC-021
    - MECH-069
    - MECH-033
    - ARC-018
  location: docs/architecture/three_loop_learning_channels.md#mech-070
  source:
    - docs/thoughts/2026-03-14_three_bg_systems_error_signals.md
    - docs/architecture/three_loop_learning_channels.md
  evidence_quality_note: |
    The rollout_horizon > prediction_horizon ordering is valid for planning only if E1
    co-evolves z_world during rollout (see MECH-135). Without co-evolution, a longer
    rollout worsens goal visibility -- E3 plans in a frozen world for N steps instead of
    a shorter frozen world. Correct neurological mapping: E1=cortical (slow LSTM, z_world
    domain, prediction_horizon=20); E2=cerebellar (fast efference-copy forward model,
    z_self domain, training horizon=1-step). E2 training horizon is separate from
    rollout_horizon and correctly short. See MECH-135 for the full parallel-rollout claim
    and EXQ-103/104/105 for experimental validation.
    EXQ-132 FAIL (2026-03-29): E2 motor model pair FAIL. First experimental entry for this
    specific claim. Training budget likely insufficient to discriminate E2 motor model horizon.
    EXQ-212 FAIL 2/4 (2026-04-03): E2 motor model discriminative pair FAIL. C1 PASS (E2
    degrades faster than E1 at h=1: E2_r2_h1=-19 vs E1_r2_h1=-43 -- E2 actually better at
    single step). C4 PASS (n_windows=6986). C2 FAIL: E2_slope=-4051 vs E1_slope=-24.8 (E2
    degrades MUCH faster than E1 across horizons -- opposite direction required for C2; C2
    requires slope_e2 > slope_e1 confirming longer-horizon planning, but both R2 values are
    deeply negative indicating neither model can predict multi-step at V3 scale). C3 FAIL
    (r2_e2_h10=-19511 << threshold -1000 -- E2 completely fails at h=10). The MECH-070 claim
    that E2 has a longer planning horizon than E1 is not supported: E2 degrades much more
    steeply than E1. This may reflect that E2 is still primarily a 1-step transition model
    at V3 training budget. Hold at candidate. Second weakening entry.
- id: MECH-071
  title: "E2 harm prediction is better calibrated for agent-caused vs environment-caused transitions."
  claim_type: mechanism_hypothesis
  subject: e3.harm_eval_calibration_gradient_asymmetry
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-071@2026-03-16T18:20:19.361137Z"
      as_of: 2026-03-16
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  depends_on:
    - MECH-231
    - ARC-024
  location: docs/architecture/sd_003_experiment_design.md#mech-071
  source:
    - docs/architecture/sd_003_experiment_design.md
  evidence_quality_note: |
    EXQ-026 PASS (2026-03-18): E3.harm_eval calibration gap confirmed on CausalGridWorldV2
    without proxy fields. calibration_gap=0.037 > 0.03 threshold. harm_pred_std=0.069.
    PASS 5/5 criteria.
    EXQ-029 PASS (2026-03-18): Much stronger confirmation on CausalGridWorldV2 with proxy
    gradient fields (ARC-024). calibration_gap_approach=0.239, calibration_gap_contact=0.209,
    reafference_r2=0.408. E3 learned graded danger model: none=0.373, approach=0.612,
    contact=0.666. Promoted from candidate -> provisional. v3_pending cleared.
    Subject corrected: was e2.harm_prediction_calibration_asymmetry -- E2 is NOT the harm
    evaluator. E3.harm_eval is the correct locus. EXQ-027 FAIL was the diagnostic that
    forced this architectural correction.
    EXQ-085 through EXQ-085d (2026-03-23/24): tagged MECH-071/INV-034 but ALL failed at
    goal-seeding bottleneck (z_goal_norm < 0.1 in every iteration). These do NOT contribute
    evidence for or against MECH-071's core claim (harm_eval calibration gradient). The
    mechanism under test (goal-state latent seeded by benefit exposure) was never instantiated
    because random-walk warmup without homeostatic drive cannot generate goal representations.
    Non-contributory: hold redesign pending drive/hunger architecture and curriculum schedule.
    See thought: docs/thoughts/2026-03-24_mech071_goal_latent_non_contributory_evidence.md
    EXQ-085e FAIL 3/4 (2026-03-26): z_goal_norm=0.135 > 0.1 threshold (C1 marginally PASS)
    but benefit_ratio=1.00 < 1.3 (C2 FAIL). C3 PASS (calibration_gap=0.1664). Root cause
    of C2=1.00 (wiring bug, 2026-03-27 diagnosis): In every 085x script, goal_state was
    maintained (seeding worked) but NEVER passed to action selection. Both GOAL_PRESENT and
    GOAL_ABSENT used random.randint() in eval -- a disconnected signal cannot change
    behaviour, guaranteeing benefit_ratio=1.00. E3Selector.score_trajectory() already has
    compute_goal_score() implemented (lines 453-457 of e3_selector.py): subtracts
    goal_weight * goal_proximity(z_world) from cost when goal_state.is_active(). The
    architecture is correct. The experiment scaffolding was broken across all 5 iterations.
    C3 consistently PASS across 085 through 085e (calibration_gap 0.03-0.17): this IS
    meaningful evidence for MECH-071's core claim (harm calibration intact with goal
    conditioning). Do not demote MECH-071 based on this series -- C2 failures are a
    wiring bug, not an architectural failure.
    EXQ-085f queued (2026-03-27): fixes the wiring. GOAL_PRESENT eval uses
    _goal_guided_action() -- E2.world_forward(z_world, a) for each candidate action,
    goal_state.goal_proximity() scores each prediction, greedy selection. GOAL_ABSENT
    still random. Same training for both conditions. New diagnostics: goal_resource_r
    (Pearson r: is z_goal pointing toward resources?) and goal_vs_harm_ratio (MECH-124
    V4 risk: is z_goal salience competitive with harm?).
    EXQ-085f FAIL 3/4 (2026-03-27, 3-seed): C1 PASS (z_goal_norm=0.228 > 0.1). C3 PASS
    (cal_gap=0.030 -- MECH-071 calibration gradient intact). C4 PASS. C2 FAIL
    (benefit_ratio=0.28x < 1.3x: GOAL_PRESENT=0.180/ep vs GOAL_ABSENT=0.637/ep). Wiring
    now confirmed working; the new bottleneck is seeding quality: goal_resource_r=0.087
    (< 0.2 threshold) -- z_goal is seeded but not pointing toward resources. MECH-124
    diagnostic: goal_vs_harm_ratio=2.104 (> 0.3) -- salience is adequate, not the issue.
    C3 PASS confirms MECH-071 calibration gradient is intact even when goal guidance
    hurts navigation. Next step: warm-start z_goal initialization or stronger curriculum.
    EXQ-085g FAIL 3/4 (2026-03-29): contact-gated seeding (seeding z_goal exclusively at
    resource contact, benefit_exposure=1.0). C1 PASS (z_goal_norm=0.399 > 0.1). C3 PASS
    (cal_gap=0.218). C4 PASS. C2 FAIL (benefit_ratio=0.37x). MECH-124: goal_vs_harm=2.249
    (no V4 risk). Root cause: goal_resource_r=0.066 < 0.2 -- z_world at contact encodes
    full scene, not resource specifically; features are not location-invariant across
    respawns. Dedicated z_resource encoder needed (SD-015). MECH-071 calibration claim
    intact (C3 consistently PASS); failure is in goal navigation, not harm calibration.
    [2026-06-02 disposition -- user-approved, memo evidence/planning/exq085_mech071_disposition_2026-06-02.md]:
    MECH-071 RETAINED provisional; NOT shelved/demoted. The insights_report "EXQ-085 14-FAIL
    dead chain / shelve MECH-071" flag is a letter-suffix heuristic artifact. MECH-071's
    calibration gradient (criterion C3) PASSED in every 085 iteration and is validated by
    EXQ-026/029; the recurring FAIL is criterion C2 (goal-directed navigation), a distinct
    mechanism owned by SD-015/MECH-112/ARC-030. All eight MECH-071-tagged 085 manifests
    (085 x2, 085c, 085d, 085e, 085f x2, 085g) reclassified evidence_direction_per_claim
    MECH-071 -> non_contributory (they test C1/C2 goal-seeding+navigation, not C3): this
    removes the spurious weakening signal so MECH-071's confidence/conflict are not dragged
    by navigation failures it does not own. The 085-letter chain is CLOSED as migrated, not
    stalled -- its goal-navigation question continued under SD-015 (EXQ-185, EXQ-182a oracle
    PASS 11.14x, EXQ-322, EXQ-514) and is active today as the goal-stream-repair ladder
    V3-EXQ-622/626/626a. No new 085 letter and no new EXQ are warranted for MECH-071.
  heterogeneity_note: "Methodological divergence: the C2 FAILs (EXQ-085 through EXQ-085g) reflect a scaffolding bug (goal_state not wired to action_selection) that prevented MECH-071's calibration mechanism from being instantiated; C3 (harm calibration gradient) consistently PASSes throughout the series, and the core claim is validated by EXQ-026 and EXQ-029. The eight MECH-071-tagged 085 manifests carry evidence_direction_per_claim MECH-071=non_contributory (2026-06-02 disposition) so the series does not weight MECH-071 scoring."
  notes: >
    After training, E3.harm_eval(z_world_t) is better calibrated before hazard_approach
    and agent_caused_hazard transitions than env_caused_hazard transitions or locomotion.
    E3 learns a graded danger model from z_world: approach to hazard increases harm_eval
    score continuously as proximity increases, not only at contact events.
    This is the gradient-sensing structure that ARC-024 predicts: the agent learns to
    detect harm gradients (approach → contact) rather than binary harm endpoints.
    Discriminability relies on z_world encoding proximity via hazard_field channels
    (CausalGridWorldV2) and SD-007 reafference correction removing self-motion contamination.
  what_would_answer: |
    NON-DEGENERACY PRECONDITION (else substrate_not_ready, NOT a verdict): requires
    CausalGridWorldV2 (or successor) with hazard_field proximity channels present in
    z_world and SD-007 reafference correction active (removing self-motion
    contamination from the proximity signal) -- without both, harm_eval has no
    proximity structure to be calibrated against and a flat/degenerate score is an
    instrumentation failure, not disconfirmation. A test without ARC-024 proxy
    gradient fields (as EXQ-026) is a valid but weaker test than one with them (as
    EXQ-029, which showed a much larger calibration_gap); either is informative, but
    do not compare their magnitudes as if measuring the same thing.
    EXCLUDED FROM SCORING: the EXQ-085/085a-085g goal-seeded-navigation series is
    NOT informative for or against this claim. All eight manifests were
    reclassified evidence_direction_per_claim=non_contributory on 2026-06-02
    (memo: evidence/planning/exq085_mech071_disposition_2026-06-02.md) because
    their repeated FAILs were on criterion C2 (goal-directed navigation, a
    scaffolding bug later traced to goal_state not being wired to action
    selection), a mechanism owned by SD-015/MECH-112/ARC-030 -- not on C3
    (calibration gradient), which PASSed in every one of those runs. Only a
    C3-shaped calibration-gap test (EXQ-026/EXQ-029 pattern) bears on MECH-071.
    CAVEAT for any future test that partitions by agent_caused_hazard vs
    env_caused_hazard specifically: the 2026-08-03 autopsy of the sibling claim
    MECH-072 (V3-EXQ-877, evidence/planning/failure_autopsy_V3-EXQ-877_2026-08-03.md)
    found that CausalGridWorldV2's transition_type labels encode hazard-OBJECT
    PROVENANCE (did the environment place this tile, or did the agent's own
    contamination footprint), which is anti-correlated with PROXIMAL avoidability
    for the contamination mechanic specifically. That finding was scoped to
    MECH-072's counterfactual (stay-comparator) discriminator, not to MECH-071's
    calibration-gap measure, and MECH-071's own evidence (EXQ-026/029) does not use
    a stay-counterfactual -- so it is not automatically undermined. But a future
    run should confirm which axis it is actually testing (labeled provenance vs
    true proximal avoidability) before its result is taken as a clean test of
    "agent-caused" in the intuitive sense the title uses.
    CONFIRMING signature: harm_eval(z_world_t) rises monotonically/continuously as
    the agent approaches a hazard (not only at the contact event -- e.g. the graded
    none < approach < contact pattern from EXQ-029), AND calibration_gap computed
    ahead of agent_caused_hazard/hazard_approach transitions exceeds calibration_gap
    ahead of env_caused_hazard transitions or locomotion by a margin comparable to
    or exceeding the EXQ-026 threshold (>0.03 gap magnitude, or the pre-registered
    threshold of the specific test design).
    FALSIFYING signature: harm_eval shows no graded/continuous structure with
    proximity (jumps only at contact, or is flat), OR calibration_gap does not
    differ (or reverses) between the agent_caused_hazard/hazard_approach group and
    the env_caused_hazard/locomotion group, OR the gap that is observed collapses
    to noise once measured on a ground-truth partition confirmed to track proximal
    avoidability rather than object provenance (see CAVEAT above).
- id: MECH-072
  title: "Foreseeable-harm gating on residue accumulation reduces false attribution without degrading harm avoidance."
  claim_type: mechanism_hypothesis
  subject: residue.foreseeable_harm_gating_reduces_false_attribution
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_213_mech072_foreseeable_harm_gating_20260403T202320Z_v3
      as_of: 2026-04-03
      verdict: supports/PASS
  v3_pending: true
  implementation_phase: v3
  depends_on:
    - MECH-071
    - MECH-060
  location: docs/architecture/sd_003_experiment_design.md#mech-072
  source:
    - docs/architecture/sd_003_experiment_design.md
  evidence_quality_note: |
    EXQ-028 FAIL (V2): Same root cause as EXQ-027. Foreseeable-harm gating depends on E2
    discriminating agent-caused from env-caused harm — impossible without z_self/z_world
    split (SD-005) and joint SD-003 pipeline.
    V3 EXQ-054 FAIL (2026-03-20): world_delta_agent=0.01776 ≈ world_delta_env=0.01806 (1.7%
    difference, below C3 threshold). C4 FAIL: calibration_gap_approach=0. E2 cannot
    discriminate agent vs environment dynamics without SD-005 z_self/z_world split — z_gamma
    conflates both. Partial signal: false_attr dropped 33%→26%, showing the gate threshold
    captures some structure, but C3/C4 require SD-005 to pass. v3_pending kept (2026-03-28):
    specific blocker is SD-011 (z_harm_s stream). Foreseeable-harm discrimination requires
    E2_harm_s forward model (ARC-033) which depends on SD-011 dual nociceptive split.
    New architectural question (2026-03-20): world-delta magnitude is insufficient to
    discriminate agent vs environment causation -- both produce similar E2 perturbation
    magnitudes. The gate requires directional discrimination: which subspace of z_world
    was perturbed, not just how much. Candidate fix: replace scalar ||delta|| threshold
    with a learned discriminator head trained on (z_world_delta, is_agent_caused) labels,
    using the oracle ground truth during training. This transforms MECH-072 from a
    threshold gate into a supervised discriminator gate. Requires SD-005 z_self/z_world
    split as prerequisite (C3 failure is fundamentally about E2 conflating agent and
    environmental dynamics in the same z_world space).
    Governance note (2026-03-22): governance algorithm produced a false promote_to_provisional
    recommendation (conf=0.951) despite 0 genuine supporting experiments. Root cause:
    consistent-weakening evidence (0 supports, 1 weakens, 4 mixed) produced high
    consistency score in the old formula. Both algorithm bugs fixed in
    build_experiment_indexes.py (directional consistency formula + min_supporting_entries
    gate). v3_pending restored. Hold at candidate until SD-005 + SD-010 implemented.
    Literature pulls for the discriminator gate design and biological evidence for
    causation-attribution gating mechanisms needed -- see evidence backlog.
    EXQ-213 PASS 6/6 (2026-04-03): Foreseeable-harm gating discriminative pair PASS. Clean
    PASS on all 6 criteria across 3 seeds. false_attr_rate gated=0.0 vs ungated=0.478 (48%
    reduction). harm_rate preserved (0.873 in both). e2_world_r2=0.558 (above C6 threshold).
    V3 proxy test: E2.world_forward predicts foreseeable harm; GATED accumulates residue only
    when E2 harm_eval(z_w_pred) > 0.2. This confirms the gating mechanism works at V3 scale
    using the existing E2 world model (not E2_harm_s -- using SD-010 z_harm pathway).
    Significant positive evidence for MECH-072 core claim. v3_pending kept: E2_harm_s (SD-011,
    ARC-033) is still needed for the full discriminator-gate design, but the V3 proxy is valid.

    [2026-08-03 governance, V3-EXQ-877, confirmed failure_autopsy_V3-EXQ-877_2026-08-03, first test
    gating accumulation on the REAL ARC-033/SD-011 counterfactual discriminator]: C1 failed on all 3
    seeds in the WRONG direction (gated false_attr 0.753 > ungated 0.506). Root cause: the
    environment's env_caused_hazard/agent_caused_hazard transition_type labels track hazard-OBJECT
    PROVENANCE (env-placed tile vs agent's own historical contamination footprint), not PROXIMAL
    avoidability (would a_stay have avoided this specific harm) -- and these are anti-correlated for
    the contamination mechanic. The discriminator's causal_sig computation is verified sound (C7
    null-CF sanity, all seeds); it behaves as a biological proximal comparator (Frith/Blakemore)
    should. non_contributory/measurement_test_design_defect -- a measurement/test-design gap, not
    falsification. v3_pending KEPT. Route: /queue-experiment (redesign with proximal-avoidability
    ground truth) + /claim-synthesis (candidate for a separate diachronic self-attribution
    mechanism).
  notes: >
    Gating residue accumulation on E2 harm foreseeability (predict_harm > threshold)
    reduces false attribution rate (residue at env-caused events / total residue)
    versus naive accumulation at all harm events, without degrading harm avoidance.
    EXQ-028 tests this.
# Registered 2026-03-15 — NC-01 through NC-09 valenced hippocampal map cluster
# ⚠ ARC-022 and MECH-073 conflict with ARC-007 (hippocampal valence constraint) — pending adjudication via Q-020
- id: ARC-022
  title: "Hierarchical effect-object abstraction pipeline (E1→E2→DMN→Goal/Avoid→Hippocampus) — conflict with ARC-007/ARC-014, pending adjudication."
  claim_type: architectural_commitment
  subject: architecture.hierarchical_effect_object_pipeline
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-022@2026-04-03T22:00:00Z"
      as_of: 2026-04-03
      verdict: promote_to_provisional/applied
  conflict_with:
    - ARC-007
    - ARC-014
  conflict_note: |
    ARC-007 constrains hippocampus to "no value computation"; ARC-022 calls it a "valenced
    cognitive map" at the pipeline's terminus. ARC-014 defines DMN as an operating mode;
    ARC-022 treats it as a pipeline layer. Resolution options: (A) revise ARC-007 to
    distinguish "compute new value" (denied) from "embody geometrically-encoded prior value"
    (permitted); (B) reject MECH-073 and treat NC-01 as external neuroscience model only.
    Pending Q-020 adjudication.
  evidence_quality_note: |
    PROVENANCE OF THE PROVISIONAL STATUS (reconstructed 2026-08-07 from git history,
    decision_log.v1.jsonl and claim_evidence.v1.json).
    Registered 2026-03-15 as `candidate` in REE_assembly 8fd823ee4a ("Sync claims,
    explorer, evidence, and architecture docs"). Promoted candidate -> provisional by a
    recorded USER decision, not an automatic rule: evidence/decisions/decision_log.v1.jsonl
    carries actor=user, timestamp_utc 2026-04-03T22:00:00Z, recommendation
    promote_to_provisional, decision_status applied, selected_option "Promote now (faster
    convergence, risk premature lock-in)" -- the alternatives offered and declined were
    "hold until one additional confirming run" and "hold and request targeted literature
    triangulation". Applied in REE_assembly 860bac026c ("Governance cycle
    (cowork-2026-04-03-b): ARC-022 promoted, manifest corrections"). This is the decision
    the live_status.evidence.from pointer resolves to.

    BASIS AT THE TIME (promotion_demotion_recommendations.md as of 860bac026c^):
    overall_conf=0.81, conflict_ratio=0, exp_entries=2, lit_entries=1, directions
    supports=3 / weakens=0 / mixed=0 / unknown=0. The two exp entries were both
    v3_exq_208_arc022_hierarchical_pipeline_probe runs (2026-04-03, supports, conf 0.75
    each); the one lit entry was 2026-03-30_hippo_cognitive_map_behrens2018
    (lit:theoretical_review, supports, conf 0.87). The ARC-007 / ARC-014 conflict was
    explicitly considered and judged non-blocking for provisional, with resolution deferred
    to Q-020 and tracked in conflict_note (Q-020 Resolution A, 2026-03-16, is recorded on
    MECH-073).

    THE EXPERIMENTAL LEG OF THAT PROMOTION NO LONGER COUNTS -- flagged, not repaired here.
    Both V3-EXQ-208 manifests carry outcome: null, so the indexer scores them as
    non-genuine; only one of the two even survives as an entry in claim_evidence.v1.json.
    The current index (generated 2026-08-06) reports ARC-022 with genuine_exp_count 0,
    experimental_confidence 0.0, 7 lit entries, lit_conf 0.827, overall 0.827. So the
    promotion's stated "3 supports (2 exp + 1 lit)" is NOT reproducible from today's index,
    and ARC-022's provisional standing is presently carried by literature alone. This is a
    bookkeeping gap rather than a negative result -- nothing weakened, the runs simply never
    received a PASS/FAIL outcome -- but it means a demotion review that reads only the
    current index will see a literature-only claim. Cheapest repair: adjudicate the two
    V3-EXQ-208 runs to a real outcome, or re-run the probe with one. Re-adjudication is
    GOVERNANCE work and is deliberately not applied here.
  depends_on:
    - ARC-001
    - ARC-002
    - ARC-007
    - ARC-014
    - ARC-021
    - MECH-231
  location: docs/architecture/valenced_hippocampal_map.md#arc-022
  source:
    - docs/architecture/valenced_hippocampal_map.md
- id: MECH-073
  evidence_quality_note: |
    [multi_sense_audit_2026_05_08] EXQ-375 evidence retained but direction held with lower confidence: training loop calls sense() twice per step (line 240 + line 300 for grad-enabled compute_event_contrastive_loss); training-time substrate-tick rate was 2x; eval (P2) was clean. Both arms equally affected so the pre/post comparison structure is preserved, but magnitude estimates may be biased. StepHarness re-run recommended for confirmation.

    Governance 2026-04-03: hold_candidate_resolve_conflict applied. conf=0.689, conflict_ratio=1, 0 exp entries, 5 lit entries. Literature-only conflict: 2 supports, 2 weakens, 1 mixed across lit entries. Conflict with ARC-007 noted below. Hold at candidate pending experimental discrimination. Governance 2026-04-13: EXQ-375 dry run PASS (preliminary support). First experimental evidence: VALENCE_GEOM AUROC=0.982/0.923 in 2/3 seeds, VALENCE_ABLATED all at chance (0.5). Consistent with Resolution A (MECH-073 co-true with ARC-007). Full EXQ-375 run is high priority for definitive discrimination. Governance 2026-06-10: PROMOTED candidate -> provisional. EXQ-375 ran to completion 3/3 PASS (the "definitive discrimination" the 2026-04-13 note flagged as high priority): VALENCE_GEOM probe-AUROC discriminates harm/safety from hippocampal map geometry, VALENCE_ABLATED at chance; genuine_exp_direction_counts = 3 supports / 0 weakens / 0 mixed; experimental_confidence 0.757 > 0.62 candidate->provisional bar. The prior hold_candidate_resolve_conflict was driven by a LITERATURE-ONLY direction conflict (2 weakens / 1 mixed: Knudsen 2021, Gauthier 2018, Bittner 2017) classified pre-Q-020. Q-020 Resolution A (2026-03-16) reconciled MECH-073 with ARC-007 (co-true; MECH-073 reframed as a consequence of ARC-013 applied to z_world), and under Phase-3 lit/exp decoupling the literature signal is a parallel signal that does not gate promotion (experimental_confidence does). So the experimental gate is met and the residual conflict is non-blocking. NOT promoted to stable: needs exp_conf >= 0.80; the multi_sense_audit 2x substrate-tick caveat + a StepHarness re-run remain the path to stable. Optional follow-up: re-adjudicate the 3 pre-Q-020 literature entries (weakens/mixed -> supports under Resolution A), which would raise lit_conf but does not affect this promotion.
  title: "Valence is intrinsic to hippocampal map geometry, not applied downstream (ARC-007 reconciled via Q-020 Resolution A; provisional 2026-06-10)."
  claim_type: mechanism_hypothesis
  subject: hippocampal.intrinsic_map_valence
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_375_mech073_valence_geometry_probe_20260418T141616Z_v3
      as_of: 2026-04-18
      verdict: supports/PASS
  conflict_with:
    - ARC-007
  conflict_note: |
    ARC-007 design constraint: "No value computation. Its function is orthogonal to valuation
    and control." MECH-073 asserts valence is intrinsic to hippocampal map geometry and that
    rollouts are inherently valenced simulations. RESOLVED by Q-020 Resolution A (2026-03-16):
    co-true -- MECH-073 reframed as a consequence of ARC-013 applied to z_world (valence shapes the
    geometry that the value-flat hippocampal proposer navigates), ARC-007 STRICT preserved (no
    hippocampal value computation). No longer a blocking conflict; ARC-007 retained in depends_on as
    the reconciled cross-reference.
  depends_on:
    - ARC-007
    - ARC-013
    - MECH-035
  location: docs/architecture/valenced_hippocampal_map.md#mech-073
  source:
    - docs/architecture/valenced_hippocampal_map.md
- id: MECH-074
  title: "Amygdala functions as read/write head for valenced hippocampal map — parent claim split into MECH-074a-d (2026-04-21)."
  claim_type: mechanism_hypothesis
  subject: amygdala.hippocampal_map_readwrite_head
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-074@2026-04-25T15:42:09.107823Z"
      as_of: 2026-04-25
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  depends_on:
    - MECH-046
    - MECH-073
    - ARC-007
    - ARC-013
    - SD-011           # affective harm stream z_harm_a
    - SD-035           # amygdala analogue substrate (parent)
  child_claims:
    - MECH-074a        # BLA encoding-gain on hippocampal writes
    - MECH-074b        # BLA retrieval bias (content-selective per-trace weights)
    - MECH-074c        # CeA fast subcortical priming (distinct from MECH-046 mode prior)
    - MECH-074d        # BLA remapping signal on PE spike with attribution gating
  location: docs/architecture/sd_035_amygdala_analog.md#mech-074
  source:
    - docs/architecture/valenced_hippocampal_map.md
    - docs/architecture/sd_035_amygdala_analog.md
    - evidence/literature/targeted_review_connectome_mech_074/
    - evidence/literature/targeted_review_amygdala_analog/synthesis.md
  notes: >
    Split 2026-04-21 into four individually testable sub-claims (MECH-074a-d) to support
    the ree-v3 BLAAnalog + CeAAnalog implementation under SD-035. The parent claim is
    retained as an umbrella for cross-cutting evidence and to preserve prior citations;
    each sub-behaviour is governed by its corresponding child claim.

    The four roles map to: (a) BLA encoding modulation of hippocampal LTP under arousal
    (MECH-074a, BLAAnalog.encoding_gain); (b) arousal-driven retrieval bias on valenced
    hippocampal regions (MECH-074b, BLAAnalog.retrieval_bias — must be content-selective
    per-trace weight vector, NOT scalar, per LaBar & Cabeza 2006); (c) rapid pre-cortical
    priming via fast subcortical route (MECH-074c, CeAAnalog.fast_prime — distinct from
    MECH-046's cortical mode-prior write); (d) triggered remapping on prediction-error
    spike with predictor-attribution gating (MECH-074d, BLAAnalog.remap_signal).
  evidence_quality_note: >
    Parent claim's aggregate evidence (9 literature entries across
    targeted_review_connectome_mech_074/ including McGaugh 2004, Roozendaal 2011,
    Dolcos 2004, LaBar & Cabeza 2006, Nader 2000, Moita 2004) is preserved but
    routed at governance time to the most specific child. Post-split, governance
    should primarily evaluate the children (MECH-074a-d); parent remains a
    cross-cutting umbrella. v3_pending until BLAAnalog + CeAAnalog substrate lands
    and EXQ-A/B (CeA mode-prior ablation; BLA encoding + remap) complete.
- id: MECH-074a
  title: "BLA analogue applies an arousal-dependent multiplicative gain to HippocampalModule write strength."
  claim_type: mechanism_hypothesis
  subject: amygdala.bla_encoding_gain_on_hippocampal_writes
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_659_mech074a_bla_encoding_gain_replay_bias_20260609T200751Z_v3
      as_of: 2026-06-09
      verdict: supports/PASS
  implementation_phase: v3
  v3_pending: false  # cleared 2026-04-22: V3-EXQ-474 substrate-readiness PASS x2 (supports); promoted to provisional 2026-04-22
  registered_utc: 2026-04-21
  depends_on:
    - MECH-074         # parent
    - SD-011           # z_harm_a (arousal source)
    - SD-035           # amygdala substrate
    - ARC-007          # hippocampal map substrate
    - MECH-073         # valenced hippocampal map
  functional_restatement: >
    BLAAnalog.tick() consumes z_harm_a (magnitude and rate) and emits an
    encoding_gain scalar that multiplies HippocampalModule write strength at
    encoding time. Gain is an inverted-U over arousal magnitude with a post-event
    exponential decay window, seeded from Roozendaal & McGaugh 2011 defaults:

      - encoding_gain_max: 2.5x at ||z_harm_a|| ~ 0.7
      - encoding_gain_floor: 1.0 below ||z_harm_a|| = 0.4 threshold
      - post-event window: ~18000 sim steps (~30 min biological)
      - window half-life: ~3600 sim steps (~6 min biological)

    This is the REE analogue of BLA modulation of hippocampal LTP under
    emotional arousal.

    Falsifiable: if EXQ-B (BLA encoding + remap) shows threat-context recall
    does not improve under BLA-modulated gain relative to gain=1, OR if neutral
    recall is harmed by gain>1, the encoding-gain arithmetic is mis-specified.
  location: docs/architecture/sd_035_amygdala_analog.md#mech-074a
  source:
    - evidence/literature/targeted_review_connectome_mech_074/entries/2026-04-21_mech_074_memory_modulation_roozendaal2011/
    - evidence/literature/targeted_review_connectome_mech_074/entries/2026-04-02_mech_074_amygdala_consolidation_mcgaugh2004/
    - evidence/literature/targeted_review_amygdala_analog/synthesis.md
  evidence_quality_note: >
    Registered 2026-04-21 as split child of MECH-074. Load-bearing literature:
    Roozendaal & McGaugh 2011 (confidence 0.82), McGaugh 2004 (confidence 0.82).
    Inverted-U and 30-min window are well-replicated; transfer risk primarily in
    mapping arousal-tagged noradrenergic signal to z_harm_a magnitude. v3_pending
    until EXQ-B queued and substrate implemented.
    First genuine experimental support landed 2026-06-09 (V3-EXQ-659, run
    v3_exq_659_mech074a_bla_encoding_gain_replay_bias_..., PASS supports):
    arousal-over-representation contrast PRIMARY-minus-ABLATION clears the margin on
    3/3 seeds (0.135/0.033/0.213) with ABLATION AOR flat (~0); encoding-gain biases
    which traces enter replay. exp_conf now 0.775 (confirmed_established). Stays
    provisional -- provisional->stable needs exp_conf>=0.80 AND >=4 experimental
    entries; this is the first.
- id: MECH-074b
  title: "BLA analogue writes a content-selective per-trace arousal tag at encoding and applies a per-trace retrieval weight vector (NOT a scalar gain) on hippocampal readout."
  claim_type: mechanism_hypothesis
  subject: amygdala.bla_retrieval_bias_content_selective
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  registered_utc: 2026-04-21
  depends_on:
    - MECH-074         # parent
    - MECH-074a        # retrieval bias consumes the encoding-time arousal tag
    - SD-011           # z_harm_a
    - SD-035           # amygdala substrate
    - ARC-007          # hippocampal map
  functional_restatement: >
    BLAAnalog must write a per-trace arousal_tag into HippocampalModule at
    encoding time (triggered from BLAAnalog.tick's encoding path), and at
    retrieval time must apply a per-trace weight
      w_i = 1 + alpha * arousal_tag_i   (alpha in [0.3, 1.0])
    to the hippocampal proposal/recall scores. This is explicitly NOT a scalar
    "retrieval gain on all traces" — that named failure signature cannot
    reproduce the central/peripheral dissociation which is the hallmark of
    amygdala-lesion studies (LaBar & Cabeza 2006).

    Targeted magnitude: central/gist items ~1.3x - 2.0x relative to neutral
    baseline. Non-transient: BLA contribution grows with trace age (20 min ->
    1 week) rather than decaying like the encoding-gain window.

    Falsifiable: a follow-up EXQ must show that central/gist threat-associated
    items are retrieved preferentially over peripheral/neutral items under BLA
    retrieval-bias ON, and that this dissociation collapses under BLA OFF. A
    uniform retrieval boost across all traces falsifies the content-selective
    form.
  location: docs/architecture/sd_035_amygdala_analog.md#mech-074b
  source:
    - evidence/literature/targeted_review_connectome_mech_074/entries/2026-04-21_mech_074_retrieval_bias_labar2006/
    - evidence/literature/targeted_review_connectome_mech_074/entries/2026-04-02_mech_074_amygdala_mtl_fmri_dolcos2004/
    - evidence/literature/targeted_review_amygdala_analog/synthesis.md
  evidence_quality_note: >
    Registered 2026-04-21 as split child of MECH-074. Load-bearing literature:
    LaBar & Cabeza 2006 (confidence 0.72), Dolcos et al 2004. The vector-not-
    scalar constraint is a hard design decision — scalar-gain is a named
    failure signature. Requires HippocampalModule to carry a per-trace
    arousal_tag field (implementation risk flagged in synthesis.md). v3_pending
    until substrate lands and EXQ-B+ follow-up is queued.
- id: MECH-074c
  title: "CeA analogue emits a fast subcortical priming signal (fast_prime) on low-frequency z_harm_a with an overridable decay window, distinct from MECH-046's cortical mode-prior write."
  claim_type: mechanism_hypothesis
  subject: amygdala.cea_fast_subcortical_priming
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_895_mech074c_cea_fast_prime_dynamics_20260808T012422Z_v3
      as_of: 2026-08-08
      verdict: supports/PASS -- partial confirmation, legs 1-2 of 3 only (see evidence_quality_note)
  implementation_phase: v3
  v3_pending: false  # cleared 2026-04-22: V3-EXQ-473 substrate-readiness PASS x2 (supports); promoted to provisional 2026-04-22
  registered_utc: 2026-04-21
  depends_on:
    - MECH-074         # parent
    - MECH-046         # cortical mode-prior is a peer channel, not this one
    - SD-011           # z_harm_a
    - SD-032a          # SalienceCoordinator (target)
    - SD-032c          # AIC urgency (peer cortical channel for override comparison)
    - SD-035           # amygdala substrate
  functional_restatement: >
    CeAAnalog.tick() emits an additive pre-softmax log-odds bias (fast_prime)
    on the harm channel within 1-2 sim steps of ||LowFreq(z_harm_a)||_1
    crossing theta_cea_fast. Cortical AIC/dACC analogues take ~5-10 sim steps
    (~400 ms) to produce comparable discrimination; fast:slow ratio ~5:1
    (Mendez-Bertolo 2016).

    Distinctness from MECH-046. MECH-046 is the CeA's SLOWER mode-prior write
    into SalienceCoordinator — a cortical-style vector over operating modes.
    MECH-074c is the FASTER action-prior / candidate-prior channel — a single
    scalar pulse that biases candidate generation directly. They share the
    CeAAnalog substrate but have distinct outputs and latencies.

    Override discipline. fast_prime magnitude ceiling <= max cortical AIC/dACC
    log-odds adjustment (never dominant). If cortical confirmation is absent
    within the 5-10 sim-step override window, fast_prime decays toward
    baseline with tau_decay in [3, 5] sim steps (~150-250 ms biological).

    Selectivity. CeA must fire on harm-affective valence, not generic arousal.
    Named failure: if CeA fires on arousing non-threat stimuli (e.g., arousing
    scenes in Mendez-Bertolo's calibration), fast_prime is a generic salience
    detector, not a CeA analogue.

    Falsifiable: a follow-up EXQ must show fast_prime emits within 1-2 steps,
    decays under cortical non-confirmation within the override window, and
    does NOT fire on arousing-but-neutral stimuli.
  location: docs/architecture/sd_035_amygdala_analog.md#mech-074c
  source:
    - evidence/literature/targeted_review_connectome_mech_046/entries/2026-04-21_mech_046_fast_pathway_mendezbertolo2016/
    - evidence/literature/targeted_review_connectome_mech_046/entries/2026-04-21_mech_046_many_roads_pessoaadolphs2010/
    - evidence/literature/targeted_review_amygdala_analog/synthesis.md
  evidence_quality_note: >
    Registered 2026-04-21 as split child of MECH-074. Load-bearing literature:
    Mendez-Bertolo et al 2016 (confidence 0.88, direct LFP evidence of 74 ms
    amygdala vs ~400 ms cortical latency), bounded by Pessoa & Adolphs 2010
    (confidence 0.72, many-roads framing constrains dominance). v3_pending
    until substrate lands. Urgency interrupt at ree-v3/ree_core/agent.py:869-886
    is a SEPARATE path and stays inline this pass; migration into fast_prime
    is a deliberate second pass once MECH-074c is validated.

    [/governance 2026-08-08, V3-EXQ-895, first experimental touchpoint]: PASS/supports, but
    self-declared PARTIAL by the driver's own evidence_scope_note -- gates only legs 1 (onset
    latency) and 2 (decay under cortical non-confirmation, plus overridability) of the claim's
    3-leg falsifier; leg 3 (selectivity vs arousing-but-neutral input) is explicitly out of scope
    as not designable as a CeA-module manipulation, and the fast:slow ratio functional_restatement
    is separately out of scope pending SD-032c AIC comparator wiring. Design explicitly guards
    against the known V3-EXQ-785 whole-run-AND defect (a non-gating probe accidentally vacating
    gated legs) by keeping its selectivity probe non-gating and separately self-routed. Non-outcome
    skim clean, no undisclosed defect. Note: MECH-074d (sibling split-child of MECH-074) got its
    own first genuine touchpoint this same cycle in the opposite direction (weakens, V3-EXQ-894) --
    read as two independent split-children legitimately diverging, not a family-level
    inconsistency; the 2026-04-21 split was on separate, non-overlapping mechanisms
    (fast subcortical priming vs attribution-gated remap) and nothing here calls that split
    into question.
- id: MECH-074d
  title: "BLA analogue emits a remap_signal on harm-PE spike when predictor-attribution flags specific latent codes — partial (~one-third) remap, NOT wholesale replacement."
  claim_type: mechanism_hypothesis
  subject: amygdala.bla_pe_remap_with_attribution
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-16
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-894c_2026-08-11"
      as_of: 2026-08-11
      verdict: FAIL/weakens -- loss-balance refuted; four consecutive negative results, demoted provisional -> candidate 2026-08-16
  epistemic_category: standard
  implementation_phase: v3
  pending_retest_after_substrate: true  # SD-035 substrate_queue entry amended 894a/894b/894c; context-representation fix owed before next retest
  v3_pending: false  # cleared 2026-04-22: V3-EXQ-474 substrate-readiness PASS x2 (supports); promoted to provisional 2026-04-22
  registered_utc: 2026-04-21
  depends_on:
    - MECH-074         # parent
    - SD-011           # z_harm_a
    - ARC-033          # E2_harm_s forward model (prediction source)
    - ARC-007          # hippocampal map substrate
    - MECH-073         # valenced hippocampal map
    - SD-035           # amygdala substrate
  functional_restatement: >
    BLAAnalog emits a remap_signal consumed by HippocampalModule when
    ||z_harm_a - E2_harm_a_pred|| exceeds ~1 standard deviation of the
    running harm-PE distribution AND a predictor-attribution head flags at
    least one candidate latent code. Both conditions required. Remap shape
    is a step-function at the per-code level (binary per code), NOT graded;
    population gradedness emerges because the gate fires on only ~30-40% of
    predictor-candidate codes. Remap amplitude perturbs roughly one-third of
    selected latent codes (Moita 2004: training-box correlation 0.57 -> 0.39,
    ~30-35% overwrite). Not wholesale replacement.

    Attribution gating — critical. The contextual-vs-auditory dissociation in
    Moita 2004 (Z = -1.36 vs -0.34, p = 0.02) rules out a broadcast
    architecture. BLAAnalog MUST consult a predictor-identification step before
    emitting remap_signal. For the initial non-trainable pass this is
    approximated by selecting codes whose contribution to the harm-PE exceeds
    a threshold; a learnable attribution head is deferred.

    Timescale. Remap decision fires within minutes of PE biologically
    (O(10^3) sim steps at 100 ms/step). A 4-6 hour plasticity gate window
    (~1.4e5-2.2e5 sim steps) spans multiple REE episodes and is relevant
    only when learnable components are added.

    Falsifiable: EXQ-B must show remap_signal fires on synthetic PE spikes
    above threshold AND preferentially perturbs attributed codes (partial
    remap, not wholesale); if remap fires on sub-threshold PE or perturbs
    untagged codes uniformly, the attribution gate is broken.
  location: docs/architecture/sd_035_amygdala_analog.md#mech-074d
  source:
    - evidence/literature/targeted_review_connectome_mech_074/entries/2026-04-21_mech_074_reconsolidation_nader2000/
    - evidence/literature/targeted_review_connectome_mech_074/entries/2026-04-21_mech_074_place_cell_remap_moita2004/
    - evidence/literature/targeted_review_amygdala_analog/synthesis.md
  evidence_quality_note: >
    Registered 2026-04-21 as split child of MECH-074. Load-bearing literature:
    Nader, Schafe & LeDoux 2000 (confidence 0.80, reconsolidation necessity),
    Moita et al 2004 (confidence 0.78, contextual-vs-auditory dissociation
    mandating attribution gating). v3_pending until substrate lands and EXQ-B
    completes. Attribution head is currently non-trainable threshold-based;
    learnable head is a deliberate second pass.

    [/governance 2026-08-08, V3-EXQ-894, confirmed failure_autopsy_V3-EXQ-894_2026-08-08, first
    genuine experimental evidence for this claim]: readiness gate green (18/18 preconditions),
    substrate wiring pre-verified by probe, non-degenerate, matched-baseline design. Substrate is
    built and functioning -- candidates compute, remap gate fires under the confirmed-necessary
    use_e2_harm_a=True flag, writes land; NOT an implementation-absence FAIL. The attribution-gate
    half of the claim (C1 attribution_selectivity, C2 context_differentiated_addressing -- the Moita
    2004 dissociation) fails on 2/3 seeds under this green, well-instrumented gate -- a genuine
    weakens as measured. The partiality half (not-wholesale-replacement) holds on 2/3 seeds.
    Fire-fraction and attribution-selectivity are inversely correlated across the three seeds -- a
    concrete testable hypothesis that the SD-035 default PE-sigma threshold is too permissive for
    this environment's PE distribution, rather than the attribution mechanism being architecturally
    absent (the driver's own pre-registered pilot probe anticipated the C4 sparsity-ceiling miss
    before the full run). Automated queue recommends demote_to_candidate (conflict_ratio 0.667);
    HELD at provisional pending V3-EXQ-894a (recalibrated PE-sigma threshold, routed
    /queue-experiment) rather than applied immediately -- one more data point on the recalibrated
    threshold may resolve the 2/3-seed split before a demotion decision.

    [/governance 2026-08-08, V3-EXQ-894a, confirmed failure_autopsy_V3-EXQ-894a_2026-08-08, recalibrated
    retest]: the held demotion decision is now RESOLVED against the over-firing/dilution hypothesis --
    sweeping bla_remap_pe_sigma_threshold over [1.0,1.5,2.0,2.5], mean attribution-mass-excess falls
    MONOTONICALLY with sigma (Spearman -1.0), tracking the fall in fire-fraction rather than diverging
    as dilution would predict. Per-seed ranking (one selective, one context-blind-deterministic, one
    null) stable across all four thresholds -- structural, not sampling noise or a threshold-tuning
    artifact. C1/C2 (Moita 2004 dissociation) never reach 2/3 seeds at any scored threshold.
    BLAAnalog's mechanical gate (compute/fire/write/partiality) is correctly implemented -- confirms
    NOT an implementation-absence FAIL; the dominant explanation is implementation completeness
    (the claim's own 2026-04-21 text pre-announced the attribution head as a deferred non-trainable
    placeholder). epistemic_category=competence_implementation_gap (not standard) -- this suppresses
    the ordinary conflict_ratio promotion/demotion churn; status HELD at provisional rather than
    mechanically demoted, since the FAIL is attributable to an acknowledged-deferred component, not a
    falsified mechanism. routing=implement-substrate: substrate_queue.json SD-035 entry AMENDED this
    cycle with this failure_record (a learnable/better-calibrated attribution head is the fix owed,
    not a re-test). Worth a future promotion/demotion look given two consecutive weakens results on
    the same core mechanism.

    [/governance 2026-08-10, V3-EXQ-894b, confirmed failure_autopsy_V3-EXQ-906a_894b_2026-08-09,
    post-substrate retest of the 894a implement-substrate routing]: the learnable BLA attribution
    head (SD-035) was built and genuinely trains (914/1998 optimizer updates past the 200-step warm
    threshold). H1 (a learned attribution recovers C1+C2 where the fixed rule never did) is PARTIALLY
    supported: C1 attribution_selectivity recovers fully (0/3 -> 3/3 seeds) but C2
    context_differentiated_addressing -- the actual Moita 2004 dissociation this claim is about --
    does NOT recover in either trained arm (stays 0/3 seeds; jaccard_gap flat-to-slightly-negative).
    Doubling the training budget did not help, ruling out "needs more experience." The module's own
    docstring predicted this exact shape: the entropy regularizer can buy C1 at C2's expense when
    entropy_weight is too large relative to the variance-normalised MSE term. Diagnosis: trainability
    alone was NOT the missing ingredient; the specific loss-term balance is now the load-bearing open
    question -- a tuning/calibration gap, not an architecture-absence gap. Three iterations
    (894/894a/894b) have progressively NARROWED the diagnosis (dilution confound refuted ->
    trainability-alone refuted -> loss-balance implicated) -- convergence, not a
    structurally-different-signatures pattern that would call for splitting the claim. Stays
    provisional / pending_retest_after_substrate.

    [/governance 2026-08-12, V3-EXQ-894c, confirmed failure_autopsy_V3-EXQ-894c_2026-08-11,
    entropy_weight/MSE balance retune]: the loss-balance hypothesis 894b left load-bearing is now
    itself REFUTED. H0 confirmed -- no swept entropy_weight value ([0.02, 0.01, 0.005, 0.001], sigma
    fixed at 1.0) recovers C1 AND C2 on >=2/3 seeds for any arm (passing_arm_ids=[]). The decisive
    reading is below the aggregate statistic: within-context and cross-context Jaccard similarity are
    IDENTICAL (1.0000 exactly) in 10/15 cells, so the attribution head has converged to a fully
    DETERMINISTIC, CONTEXT-INVARIANT target-set selection -- not a graded loss of context-sensitivity
    that a scalar retune could rebalance. The docstring's predicted C1-vs-C2 entropy_weight tradeoff
    is not merely absent but inverted (spearman_mass_excess_vs_entropy_weight=-1.0, opposite the
    predicted sign). epistemic_category moved substrate_conditional -> standard per the autopsy's
    recommendation: four consecutive negative results (894/894a/894b/894c) on the same core mechanism
    have now excluded dilution, trainability, and loss-term balance in turn, so the suppression that
    category afforded is no longer warranted -- this claim should be exposed to the ordinary
    promotion/demotion pass and is worth an explicit demotion look next cycle. routing=implement-substrate:
    SD-035 substrate_queue entry AMENDED with this failure_record and 894b's prior record marked
    superseded; the fix owed is a context-DISCRIMINATING input signal to the attribution head (or a
    loss FORMULATION change), NOT a further scalar retune -- that route is now closed by measurement.
    Stays provisional / pending_retest_after_substrate.

    [/governance 2026-08-16, DEMOTION APPLIED provisional -> candidate]: the 2026-08-08 hold is
    DISCHARGED and its condition is spent. That hold was explicitly "pending V3-EXQ-894a
    (recalibrated PE-sigma threshold) rather than applied immediately"; 894a, 894b and 894c have
    all since run and returned weakens / weakens / mixed -- 4 experimental entries, 0 pass_runs,
    conflict_ratio 0.80, exp_conf 0.456. Each successor closed one candidate explanation in turn
    (dilution -> trainability-alone -> loss-term balance), so this is a NARROWED diagnosis rather
    than an instrument artifact, and the 894c autopsy itself moved epistemic_category
    substrate_conditional -> standard precisely to expose the claim to this pass and recorded that
    it "is worth an explicit demotion look next cycle". Demoting now records what the evidence
    already shows and removes false certainty from downstream roadmap references; it does NOT
    falsify the claim. pending_retest_after_substrate stays true -- the owed fix is a
    context-DISCRIMINATING input signal to the attribution head (or a loss FORMULATION change),
    per the SD-035 substrate_queue entry, and a successful retest is the route back to provisional.
- id: MECH-075
  title: "Basal ganglia perform dopaminergic gain/threshold setting on hippocampal attractor dynamics."
  claim_type: mechanism_hypothesis
  subject: basal_ganglia.dopaminergic_attractor_threshold_setting
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-075@2026-04-03T22:00:00Z"
      as_of: 2026-04-03
      verdict: hold_candidate_resolve_conflict/applied
  epistemic_category: standard
  depends_on:
    - Q-019
    - ARC-021
    - MECH-043
    - MECH-073
  location: docs/architecture/valenced_hippocampal_map.md#mech-075
  what_would_answer: >
    DORSAL LEG (LC-mediated arousal gain on spatial/planning terrain): NON-DEGENERACY
    PRECONDITION (both required, checked as a manipulation check BEFORE any behavioral
    comparison) -- (a) SIGNAL MAGNITUDE: the novelty/arousal signal must clear the
    empirically-established detection floor; three prior attempts (EXQ-192a x2, EXQ-230
    x2) all measured 5e-5-1.2e-4, below the 1e-4/0.01 thresholds -- confirm the corrected
    design produces a signal an order of magnitude above that before interpreting any
    downstream metric. (b) ANATOMICAL TARGETING: the probe must instrument a genuinely
    LC-sourced signal, NOT the generic E1-world-prediction-MSE -> CEM-noise-scaling
    channel reused by all three prior attempts -- EXQ-230's own docstring names this
    conflation ("LC-VTA novelty loop") without resolving it; no ree_core/ module
    currently implements an LC-specific pathway distinct from the VTA/dopamine-labeled
    channel. (c) TERRAIN: must be genuinely dorsal-HPC/spatial-planning, not generic
    grid-exploration confoundable with undifferentiated curiosity/noise-floor machinery
    (MECH-313, MECH-471).

    CONFIRMING (dorsal): with (a)-(c) cleared, hippocampal attractor basin width/
    trajectory diversity on dorsal terrain widens under high-LC-arousal vs low, >=2/3
    seeds, exceeding a pre-registered effect-size floor.

    FALSIFYING (dorsal): signal clears the detection floor and is confirmed LC-sourced
    (precondition genuinely met) but produces no measurable widening of dorsal attractor
    dynamics. This would be the first result actually falsifying the dorsal leg -- all
    three prior FAILs failed on the precondition itself and are uninformative about the
    mechanism.

    VENTRAL LEG (VTA-mediated RPE/reinforcement gain on motivational/affective terrain):
    UNTESTED. All three prior attempts instrumented dorsal-HPC terrain with a VTA/RPE-
    shaped signal -- the wrong axis pairing -- never the ventral leg on its own terms.
    NON-DEGENERACY PRECONDITION: a live-computed VTA-RPE signal must demonstrably reach
    ventral-HPC-analog/motivational-valence terrain (MECH-073's valence-intrinsic map,
    MECH-043's dopamine precision-weighting) above whatever detection floor that terrain
    requires (likely different from the dorsal 1e-4 floor, not yet empirically
    established).

    CONFIRMING (ventral): attractor basin width on ventral/motivational terrain narrows
    under high-VTA-RPE-reinforcement-gain vs low, >=2/3 seeds, exceeding a pre-registered
    effect-size floor.

    FALSIFYING (ventral): RPE signal clears its detection floor but produces no
    measurable ventral attractor-width modulation.

    GOVERNANCE FLAG (2026-08-08, not resolved here): the corrected, axis-specific retest
    EXQ-230's own docstring all but named has never been queued -- over four months since
    the 2026-04-03 FAIL and 2026-04-02 anatomical correction. Two missing experiments, a
    dorsal-leg probe (fixing signal magnitude + genuine LC instrumentation) and a
    ventral-leg probe (never attempted), are chip candidates for /queue-experiment.
  source:
    - docs/architecture/valenced_hippocampal_map.md
  notes: >
    BG perform gain/threshold setting on attractor dynamics in the valenced hippocampal
    system via dopaminergic tone (VTA/SNc) and striatal competitive inhibition, not external
    value comparison. Extends MECH-043 (dopamine-like precision-weighting) with attractor-
    basin threshold framing specific to the hippocampal planning context. Supports three-loop
    architecture of ARC-021 with a mechanism for how each loop's gate operates.
    ANATOMICAL SUBDIVISION NOTE (2026-04-02, Kempadoo et al. 2016 PNAS): VTA dopamine axons
    are sparse in dorsal hippocampus. Dopamine in dorsal HPC (spatial/planning substrate, E3
    viability map) comes primarily from locus coeruleus (LC) via DA/NE co-release, responding
    to novelty and arousal. VTA dopamine primarily targets ventral HPC (motivational/affective).
    MECH-075 bifurcates by HPC axis: dorsal=LC-mediated arousal gain; ventral=VTA-mediated
    RPE/reinforcement gain.
  anatomical_note: "LC (locus coeruleus), not VTA, is primary catecholamine source in dorsal
    HPC (spatial/planning terrain). Dorsal HPC gain: LC-mediated (novelty/arousal, DA/NE
    co-release, Kempadoo et al. 2016). Ventral HPC gain: VTA-mediated (reward prediction
    error, RPE signal). MECH-075 survives but requires axis-specific implementation: dorsal
    terrain encoding-depth modulation via LC arousal signal; ventral valence weighting via
    VTA RPE."
  evidence_quality_note: |
    EXQ-192a FAIL 1/4 x2 runs (2026-04-03): Hippocampal-VTA novelty loop probe FAIL.
    Run 1 (T04:39): criteria_met=1/4. Run 2 (T10:25): criteria_met=1/4.
    mean_novelty_signal_on = 6.4e-05 (threshold C4 > 1e-04: FAIL). cell_gap=0, hazard_gap=0
    across both conditions -- NOVELTY_LOOP_ON identical to NOVELTY_LOOP_OFF. Root cause:
    novelty_gain=2.0 but novelty signal itself is below detection threshold; CEM noise
    scaling has no measurable effect when the novelty_ema is too small. The manipulation
    requires the novelty signal to exceed 1e-04 before behavioral differentiation is visible.
    Substrate limitation: E1 world-prediction error may be too uniform across z_world states
    at V3 training scale, or the CEM noise scaling (novelty_gain=2.0) is insufficient. C3
    PASS (harm_delta=0 both conditions -- harm not worsened). Consistent null result across
    two independent runs. Hold at candidate; not falsified -- the novelty signal magnitude
    is the bottleneck, not the claim's core mechanism.
    Mixed/null evidence partially reflects dorsal/ventral axis split (Kempadoo et al. 2016):
    EXQ-192a probed VTA-like novelty gain on dorsal HPC terrain, but dorsal HPC gain is
    LC-mediated, not VTA-mediated. The FAIL is consistent with targeting the wrong
    catecholamine source for dorsal terrain. This is not a falsification of MECH-075
    core mechanism -- it is a mis-targeting of anatomical substrate. See anatomical_note.

    [2026-08-09 governance, V3-EXQ-903+905, confirmed failure_autopsy_mech075-vta-lc-cluster_2026-08-09]:
    V3-EXQ-903 (ventral/VTA-RPE leg, first attempt): self-routed substrate_not_ready_requeue,
    CONFIRMED CORRECT on adjudication -- P0 readiness passed in only 1/3 seeds (seed 7), the
    other two showing negative grounding correlation. The top-level manifest's
    preconditions[].met:true reflects a seed-mean statistic dominated by seed 7 and should NOT
    be read as readiness cleared; the seed-majority gate (n_p0a_pass=1) is authoritative and
    correctly failed. Seed 7's apparent pass coincides with ~25x fewer eval samples, consistent
    with near-immediate episode termination -- a plausible sampling confound, not confirmed
    training success. inconclusive/standard. routing=queue-experiment: re-queue with per-seed
    episode-termination recording before trusting a future P0 read.
    V3-EXQ-905 (dorsal/LC-arousal leg): P0 and C1 (signal detectability + correct LC-anatomical
    targeting) both cleanly PASSED -- correcting the signal-magnitude defect of all three prior
    attempts (EXQ-192a above, 209, 230). But C2 (basin-width widening) FAILED with near-zero,
    sign-inconsistent gaps (~1e-6 against a 15% floor). Traced to substrate code
    (ree_core/hippocampal/module.py): the manipulation gain LC_GAIN=2.0, inherited unchanged
    from the already-failed EXQ-192a novelty_gain=2.0, produces only a ~0.4-0.6% CEM-proposal-noise
    increase at the observed post-fix arousal_ema magnitude (~0.002-0.003) -- an order of
    magnitude too small to plausibly produce a 15%-scale basin-width change. non_contributory,
    an underpowered-manipulation test-design gap, not a falsification. epistemic_category stays
    standard for both legs (autopsy recommended "measurement_test_design_defect" for 905, not a
    valid claims.yaml enum -- see chip-20260809-epistemic-category-vocab-audit). Status unchanged
    (candidate) -- both legs remain untested at adequate power; a gain constant inherited from a
    prior FAILed design must be re-derived against the observed signal distribution, not reused.

    [2026-08-10 governance, V3-EXQ-903a+905a, confirmed failure_autopsy_mech075-second-cluster_2026-08-10,
    second cluster on the same two legs]: V3-EXQ-903a (ventral, recalibrated to 5 seeds/majority-of-3):
    readiness gate STILL fails on majority (2/5 pass, grounding correlation swings +0.53 to -0.003
    across seeds) -- self-routed substrate_not_ready_requeue, CONFIRMED CORRECT again. Second
    consecutive attempt at more seeds has not closed the readiness gate; routed to a targeted
    diagnostic (per-episode training-trajectory logging) rather than a third combined attempt.
    inconclusive/standard. V3-EXQ-905a (dorsal, LC_GAIN correctly re-derived 2.0->80.0 per the prior
    cluster's own finding): the manipulation is now clearly differential (C1 3/3 seeds) but the
    decisive metric (basin_width) shows literally zero effect (2/3 seeds identical to 4+ decimal
    places) -- weakens the CEM-decision-time operationalization specifically, and flags a
    construct-validity gap: the LC-arousal literature this claim's anatomical_note cites grounds
    plasticity-TIMESCALE effects, not decision-TIME effects. Routed to /lit-pull. Status unchanged
    (candidate) -- the dorsal leg's manipulation-magnitude defect is now fixed, but the DV itself may
    be measuring the wrong construct; re-derive brake and granularity-debt checks both run, neither
    fires (this claim's autopsy history diverges by leg, not by a single recurring shape).
- id: MECH-076
  title: "Residue is structural deformation of hippocampal map topology (CA3 retraction, DG neurogenesis, reconsolidation)."
  claim_type: mechanism_hypothesis
  subject: residue.hippocampal_map_structural_deformation
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-18
    needs_review: false
    evidence:
      from: "decision:MECH-076@2026-07-18T06:40:45.258446Z"
      as_of: 2026-07-18
      verdict: promote_to_provisional/applied
  evidence_quality_note: >
    2026-07-18 governance: candidate -> provisional. V3-EXQ-773 (residue basin-geometry
    dose-response; run_id v3_exq_773_mech076_residue_basin_geometry_doseresponse_20260717T160046Z_v3)
    PASS/supports, self-route residue_basin_geometry_graded_and_at_a_distance, no adjudication flag.
    First experimental entry for MECH-076 (exp_conf 0.774, lit_conf 0.753; 3 supports / 0 weakens).
    Both load-bearing criteria pass and are non-degenerate: C1 peak-avoidance monotone in basin depth
    (graded dose-response, not a discrete threshold) and C2 capture-vs-far share shift (avoidance acts
    at a distance, i.e. geometry-mediated rather than a peak-only lookup gate). z_world grounded in
    real substrate latents via an untrained frozen encoder (answers the synthetic_signals_only
    objection); terrain-only elite selection (evaluate_trajectory, ARC-007 strict).
    SCOPE: validates the BASIN-GEOMETRY leg only (evidence_class mechanistic_partial) -- the one
    sub-claim the V3 substrate expresses. It does NOT establish the biological instantiation
    (CA3 retraction / DG neurogenesis / reconsolidation palimpsest), which stays untested.
    Distinct from INV-053 (binary attractor) and ARC-013/V3-EXQ-697 (decision separability).
  depends_on:
    - ARC-013
    - MECH-056
    - MECH-073
  location: docs/architecture/valenced_hippocampal_map.md#mech-076
  source:
    - docs/architecture/valenced_hippocampal_map.md
  notes: >
    Residue (ARC-013 curvature field) is instantiated as structural deformation of
    hippocampal map topology: CA3 dendritic retraction under stress, reduced DG neurogenesis,
    palimpsest traces from incomplete reconsolidation. Not a separate inhibitory layer — the
    terrain itself is deformed. Compatible with MECH-056 (trajectory-first placement): basin
    distortion acts as trajectory-level pressure without requiring a representational filter
    layered on perception.
- id: MECH-077
  title: "Therapeutic change equals hippocampal remapping, with modalities operating at different levels."
  claim_type: mechanism_hypothesis
  subject: hippocampal.therapeutic_remapping
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-013
    - MECH-076
    - MECH-018
    - MECH-073
  location: docs/architecture/valenced_hippocampal_map.md#mech-077
  source:
    - docs/architecture/valenced_hippocampal_map.md
  notes: >
    New domain claim. Therapeutic change = hippocampal remapping. Modalities: CBT operates
    on OFC/vmPFC read of existing map; EMDR operates via reconsolidation under low arousal
    (REE analogue: offline consolidation with arousal suppressed, MECH-018); psychodynamic
    therapy = slow relational geometry restructuring over many consolidation cycles; psychedelic-
    assisted = transient attractor flattening enabling large-scale map restructuring.
- id: MECH-078
  title: "Amygdala bootstraps novel valence for unmapped hippocampal territory; anxiety disorders are systematic over-valencing."
  claim_type: mechanism_hypothesis
  subject: amygdala.novel_territory_valence_bootstrap
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-046
    - MECH-074
    - MECH-073
  location: docs/architecture/valenced_hippocampal_map.md#mech-078
  source:
    - docs/architecture/valenced_hippocampal_map.md
  notes: >
    Amygdala provides bootstrap valence for unmapped hippocampal territory via fast subcortical
    priming (MECH-074 role 3), then writes initial valence into map through consolidation.
    Anxiety disorders may reflect systematic over-valencing of novel/unmapped territory as
    aversive. Positive feedback loop with MECH-040 volatility channel. Extends MECH-046.
- id: MECH-079
  title: "Phenomenological continuous selfhood is an artefact of stable hippocampal map geometry."
  claim_type: mechanism_hypothesis
  subject: hippocampal.map_geometry_selfhood
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-007
    - ARC-013
    - MECH-024
    - MECH-073
  location: docs/architecture/valenced_hippocampal_map.md#mech-079
  source:
    - docs/architecture/valenced_hippocampal_map.md
  notes: >
    Phenomenological continuous selfhood is an artefact of stable hippocampal map geometry:
    same topology → similar rollouts → similar commitments → subjective identity persistence.
    Extends MECH-024 (selfhood/personality/ethics converge structurally) with specific
    substrate. Identity-level change = phase transition in map geometry (discontinuous
    restructuring of attractor basin profile). Co-constituted with INV-006: ethical history
    is structurally encoded in map geometry.
- id: MECH-080
  title: "Rollout truncation set-points as psychiatric individual differences substrate (ADHD/anxiety/OCD)."
  claim_type: mechanism_hypothesis
  subject: hippocampal.rollout_truncation_psychiatric_individual_differences
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-018
    - MECH-027
    - ARC-014
    - MECH-075
  location: docs/architecture/valenced_hippocampal_map.md#mech-080
  source:
    - docs/architecture/valenced_hippocampal_map.md
  notes: >
    Rollout sampling depth and truncation set-points as substrate for psychiatric individual
    differences. ADHD = truncation-biased (BG urgency threshold too low, premature commitment);
    anxiety = extension-biased (uncertainty chronically elevated, BG threshold too high,
    suppressed commitment); OCD = attractor lock-in (abnormally deep basin, MECH-076, normal
    signals cannot trigger basin exit). Extends MECH-027 (pathological modes = mis-tuned
    control-plane regimes) and ARC-014 failure modes with unified rollout-parameter account.
- id: Q-020
  title: "Does ARC-007's no-value-computation constraint survive MECH-073 (valence intrinsic to hippocampal map)?"
  claim_type: open_question
  subject: hippocampal.arc007_valence_constraint_survival
  polarity: open
  status: candidate
  evidence_quality_note: >-
    REOPENED 2026-07-25 (governance): V3-EXQ-266b (valence-geometry pair, C2 cluster) is a
    USER-CONFIRMED weakens -- once selection is routed through E3 with a matched episode
    budget and a static no-op control, the harm-reduction advantage of valence-shaped
    geometry collapses to 6.5% (< 15% required). This behavioural leg was load-bearing for
    Resolution A's "value-shaped geometry helps harm navigation" reading, so the resolution
    is no longer settled: status resolved -> candidate, needs_review. resolution_a is kept
    below as the prior (now-questioned) ruling; the biological existence-proof for
    value-shaped hippocampal geometry is unaffected -- what weakens is the behavioural
    translation to a harm-navigation advantage.
  live_status:
    reading: resolved
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-020@2026-04-10T18:06:06.975132Z"
      as_of: 2026-04-10
      verdict: narrow_open_question/applied
  resolution: resolution_a
  resolution_note: |
    RESOLVED 2026-04-02 via literature synthesis. Resolution A adopted: ARC-007 and MECH-073
    are co-true. Hippocampus embodies value-shaped geometry written by external systems (BLA via
    BTSP plateau potentials; VTA dopamine; LC catecholamines) but does not compute value itself.
    The "no value computation" constraint specifies that hippocampus does not evaluate RPE,
    utility, or outcomes -- it stores the geometrically encoded result of computations performed
    externally. Key papers: Bittner 2017 (BTSP mechanism -- external write triggers), Teyler &
    Rudy 2007 (indexing theory -- HPC as pure relational binder), McGaugh 2004 + Dolcos 2004
    (BLA modulates write depth). Gauthier 2018 dedicated reward cells reinterpreted as stable
    BLA-tagged anatomical relay stations, not value-computing units.
    See hippocampal_literature_synthesis_2026.md Section 1 for full ruling.
  conflict_note: |
    Pivotal adjudication question for NC-01 through NC-09 cluster integration into canonical
    REE. Two resolution paths:
    (A) Revise ARC-007 to distinguish "hippocampus does not compute new value" (still denied)
    from "hippocampus embodies geometrically-encoded prior value via amygdala write operations
    and residue-field shaping" (permitted under MECH-073). Preserves INV-014.
    (B) Reject MECH-073; treat NC-01/NC-02 as external neuroscience model that inspires but
    does not enter canonical architecture. ARC-022, MECH-073 demoted to rejected. MECH-074
    through MECH-080 survive independently (most do not require MECH-073 to be valid).
    Note: MECH-074 through MECH-080 have partial independence from MECH-073 -- see individual
    conflict notes. Q-019 resolution evidence may inform this question.
    EXQ-159 SUPERSEDED (2026-03-29): implementation gap -- policy trained only with entropy
    bonus, z_world/z_self detached from policy forward pass; maximum-entropy random walk
    (action_entropy=ln(5)=1.6094). Hippocampal valence constraint (ARC-007) never exercised.
    Gated on SD-004 hippocampal navigation implementation. See EXP-0095.
  depends_on:
    - ARC-007
    - MECH-073
    - ARC-022
    - Q-019
  location: docs/architecture/valenced_hippocampal_map.md#q-020
  source:
    - docs/architecture/valenced_hippocampal_map.md
# Registered 2026-03-22 -- approach/avoidance drive symmetry: open question
- id: Q-021
  title: "Does training with harm-avoidance signals only, without explicit approach drives, produce behavioral flatness (quiescent degenerate policy)?"
  claim_type: open_question
  subject: drive.behavioral_flatness_under_pure_avoidance
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-08-08
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-866c_2026-08-08"
      as_of: 2026-08-08
      verdict: non_contributory/competence_implementation_gap
  epistemic_category: substrate_conditional
  pending_retest_after_substrate: true
  what_would_answer: |
    Scoped to Pathway A (drive absence, the ARC-030 route) only; Pathway B
    (self-incoherence-gated commit suppression via MECH-113/114) is a separate,
    dissociable falsifier and is NOT answered by this condition.
    Two-arm ablation on the V3 forage/harm stack, trained identically except for
    the Go drive: approach arm (z_goal attractor + novelty/self-maintenance ON) vs
    avoidance-only arm (harm gradient only, Go drive ablated). ANSWER "yes -- flatness"
    if over training the avoidance-only arm collapses to a quiescent floor
    (resource_visit_rate < 0.2x the approach arm AND policy_entropy within epsilon of
    the no-op floor) on >=2/3 seeds. MANDATORY non-degeneracy precondition: the
    approach arm must itself be non-flat (non-zero resource_visit_rate and policy
    entropy) -- the v3_exq_072b prior run read 0 on every arm and so tested nothing;
    a run failing this precondition self-routes substrate_not_ready, not a "yes".
    ANSWER "no" if the avoidance-only arm sustains action-rate/entropy comparable to
    the approach arm. (Pathway A scope per /thought-digestion 2026-06-11.)
  evidence_quality_note: |
    narrow_open_question (2026-03-29): two independent pathways to behavioral flatness already
    identified in notes. Pathway A (drive absence, ARC-030) tested by EXQ-072; Pathway B
    (self-incoherence-gated commit suppression, MECH-113/114) untested. Definitive test requires
    both pathways. No experimental evidence yet -- question narrowing applied to focus on
    discriminating between the two pathways.

    [2026-08-02 governance, V3-EXQ-866, confirmed failure_autopsy_V3-EXQ-866_2026-08-02, INV-034/Q-021 goal-maintenance-necessary-for-agency, lightweight harness]: G0 non-degeneracy gate FAILED cleanly, correctly self-diagnosed by the driver -- the FULL (approach+avoidance) arm's resource_visit_rate (0.0046) is BELOW the RANDOM baseline (0.0217), a genuine competence-floor failure mirroring the already-documented EXQ-072b failure mode, not evidence against INV-034/Q-021. C1 (harm parity), C2 (survival parity), C3 (quiescence/avoidance-only flat) and C6 (z_goal mechanistic check) all PASS -- substrate components work individually, but this lightweight harness cannot get the agent to forage above chance at all, so C4/C5 cannot be meaningfully evaluated. non_contributory (substrate_not_ready_requeue). Per IGW-222 DESIGN.md 5.8, the correct remedy is escalating to the full scaffolded_sd054_onboarding curriculum (the substrate that already cleared goal_pipeline GAP-2's contact-rate ceiling) rather than re-running this lightweight harness. Route: same-question re-queue (866a) built on scaffolded_sd054_onboarding.

    [2026-08-03 governance, V3-EXQ-866a, confirmed failure_autopsy_V3-EXQ-866a_2026-08-03, supersedes
    V3-EXQ-866]: G0 non-degeneracy FAILED AGAIN on the scaffolded_sd054_onboarding curriculum 866's own
    autopsy prescribed (FULL resource_visit_rate 0.0033 < RANDOM 0.0103). Configuration verified
    line-by-line identical to the cited reference V3-EXQ-603q (same seeds, curriculum order/budgets,
    agent flags) -- yet Stage-H hazard-avoidance survival (median 4-5.5/200 steps) is ~7x below
    603q's cited base_mean_survival=37.7. 603q's own manifest predates the recording standard (no
    substrate_hash/commit), so the reference is commit-unverifiable across the 47-day gap --
    substrate drift is plausible but not confirmed. Separately, z_goal decays from a healthy Stage-0
    peak (~0.5) to a P2 mean of only 0.12 (floor 0.4), so goal maintenance itself may not be
    surviving the intervening hazard-only curriculum stages. Still NOT evidence against INV-034/Q-021.
    non_contributory/substrate_ceiling. Routed to a substrate-regression diagnostic (re-run 603q
    fresh) rather than a third lettered iteration -- v3_pending KEPT. NOTE (governance, 6a-v ceiling
    audit): this ceiling stamp is DELIBERATELY NOT mapped to a substrate_queue entry this cycle -- the
    routing is a diagnostic re-run checking for substrate regression, not a build, so it does not fit
    the create/amend pathway. Flagged explicitly as orphaned rather than silently read as mapped or
    self-handled; route to /implement-substrate only if the regression diagnostic confirms a real
    substrate fix is needed.
    [2026-08-07 governance, ceiling-audit ACTIONABLE finding, user-confirmed]: check_substrate_ceiling_audit.py
    reconfirms this claim genuinely orphaned (no design owner, not parked, not self-handled). Flagged
    for /failure-autopsy to produce the substrate-regression diagnostic prescribed by 866a's own
    autopsy (re-run 603q fresh, commit-verified) rather than routing here inline from a verdict alone.

    [2026-08-08 governance, CORRECTION]: the 2026-08-07 flag above is STALE -- the substrate-regression
    diagnostic it asks for already ran and passed: V3-EXQ-866b (2026-08-03T19:24Z, PASS, confirmed
    failure_autopsy_V3-EXQ-866b_2026-08-07.json) confirms base_survival_reproduces_603q_reference --
    no substrate regression. This closes the "is 603q's substrate drifted" question 866a's autopsy
    opened. The real, still-open gap is curriculum/harness, not substrate: z_goal decays from a
    healthy Stage-0 peak (~0.5) to a P2 mean of 0.12, below the 0.4 C6 floor, despite the substrate
    being confirmed sound -- the intervening hazard-only curriculum stages appear to starve goal
    maintenance before the P2 measurement window. Substrate_queue stub
    scaffolded-curriculum-hazard-rebalance minted this cycle (unblocks Q-021, INV-034, and shares
    ARC-030's G0 gate); pending_retest_after_substrate stays true pending that curriculum fix.

    [2026-08-08 governance, V3-EXQ-866c, confirmed failure_autopsy_V3-EXQ-866c_2026-08-08, supersedes
    V3-EXQ-866a]: the scaffolded-curriculum-hazard-rebalance stub was itself found UNWARRANTED and
    closed same-day -- an empirical probe showed z_goal survives every hazard stage and enters P2 at
    ~0.52; the C6 FAIL above was a decay-only measurement washout artifact (zgoal_norm_mean=0.148),
    not a real curriculum/hazard problem. V3-EXQ-866c CONFIRMS the fix: the contact-gated run_p2 peak
    readout (FULL=0.461) clears the 0.4 C6 floor. G0 (foraging-competence) FAILED again -- third
    occurrence (866/866a/866c) -- non_contributory/precondition_unmet, not evidence against
    INV-034/Q-021, and reconfirms (adds no new content beyond) failure_autopsy_V3-EXQ-866a-G0_2026-08-08's
    diagnosis: category corrected substrate_ceiling -> competence_implementation_gap (the substrate is
    confirmed sound per V3-EXQ-866b; the gap is curriculum/competence-training, a ceiling on the harness
    not on substrate representational capacity). Shares the G0 gate with MECH-457's GOV-FANOUT-1
    portfolio dependency; awaiting V3-EXQ-899. That readiness run has SINCE landed
    (V3-EXQ-899, 2026-08-08T21:48Z, FAIL) and rules out MECH-307 reachability as the fix
    (mech307_perturbs_baseline=False -- the OFF arm also fails G0) without yet identifying the real
    driver; not yet autopsied as of this cycle (deferred to a future /failure-autopsy given cycle scope).
    pending_retest_after_substrate stays true.

    [2026-08-09 governance, V3-EXQ-899, confirmed failure_autopsy_V3-EXQ-899_2026-08-09]: now
    autopsied (by a concurrent session). G0 FAILED a fourth time (866/866a/866c/899),
    reconfirming -- no new content beyond -- the already-diagnosed competence_implementation_gap.
    MECH-307 reachability formally RULED OUT as the G0 fix (single-variable A/B on the exact 866a
    config, d_resource_visit_rate=0.0001). The remaining driver matches the MECH-457
    competence-floor cluster's own "avoidance learned without approach" signature -- that
    cluster's hypothesis-space qid (competence_floor) is CLOSED TO FURTHER FAN-OUT, so a 5th
    confirmatory instance here is cross-referenced, not grown onto it. substrate_queue
    mech457_competence_bootstrap_explorer amended with this run's failure_record. routing
    remains governance-note-only; no new claims.yaml disposition beyond this cross-reference.
  depends_on:
    - ARC-030
    - MECH-111
    - MECH-112
    - MECH-113
  location: docs/architecture/approach_avoidance_symmetry.md#q-021
  source:
    - evidence/planning/thought_intake_2026-03-22_approach_avoidance_drives.md
  notes: >
    Testable prediction of ARC-030. An ablation experiment trains two agents identically
    except one has approach drives (goal attractor, novelty signal, self-maintenance) and one
    does not. If ARC-030 is correct, the avoidance-only agent converges to minimal-action
    behavior measurable as reduced policy entropy and action rate over training. Secondary
    question: what minimum set of approach signals is sufficient to prevent behavioral collapse?
    Added to evidence backlog as EVB-0061. Registered 2026-03-22.
    TWO INDEPENDENT PATHWAYS TO BEHAVIORAL FLATNESS (registered 2026-03-23):
    Pathway A (drive absence -- ARC-030 route): No Go drive -> only avoidance gradient ->
    quiescent policy is optimal -> behavioral flatness. Tested by EXQ-072.
    Pathway B (self-incoherence-gated commit suppression -- MECH-113/114 route): Go and NoGo
    drives both present, but high D_eff -> MECH-114 suppresses commitment -> agent cannot
    act even when approach drive is active -> behavioral flatness through self-model failure.
    These pathways are independent and dissociable: an agent can PASS EXQ-072 (Pathway A
    resolved) but still exhibit flatness under Pathway B if self-model is disrupted. A
    definitive test of Q-021 requires testing both pathways. Pathway B experiment not yet
    written; will follow EXQ-075 (MECH-113) if PASS. The two pathways predict different
    signatures: Pathway A flatness is present even at low D_eff (coherent agent, just no
    goal drive); Pathway B flatness occurs despite Go drive being present, colocated with
    high D_eff. Clinical analogues (from literature): Schizophrenia negative symptoms
    (avolition) = impaired wanting with intact liking -- maps to Pathway A. Dissociative
    states where action is suppressed despite motivation -- maps more to Pathway B.
    Barch & Dowd 2010 (DOI 10.1093/schbul/sbq068), Culbreth et al. 2023 (DOI
    10.1017/S0033291722003993). Depression = locked-in avoidant brain; allostatic self-efficacy
    failure -> learned helplessness; Stephan et al. 2016 (DOI 10.3389/fnhum.2016.00550),
    Barrett et al. 2016 (DOI 10.1098/rstb.2016.0011).
    Full synthesis: evidence/planning/literature_synthesis_2026-03-22_approach_avoidance_drives.md

# Registered 2026-03-15 — NC-02 through NC-03 E1/E2 constraint propagation cluster
- id: MECH-081
  title: "E2 sufficiency constraint reduces E1 effective dimensionality target."
  claim_type: mechanism_hypothesis
  subject: e1_e2.e2_sufficiency_constraint_reduces_e1_dimensionality
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-001
    - ARC-002
    - MECH-033
  location: docs/architecture/e1_e2_constraint_propagation.md#mech-081
  source:
    - docs/architecture/e1_e2_constraint_propagation.md
  notes: >
    E2's prediction task generates a top-down sufficiency constraint on what E1 must resolve.
    E2 implicitly specifies which covariances in the sensory stream E1 must track for E2's
    predictions to be well-formed, reducing E1's effective dimensionality target. The hard
    problem of feature discovery is partially specified from above rather than requiring
    purely unsupervised discovery from below. May reduce E1 hidden-layer depth requirement.
    Extends ARC-001/ARC-002/MECH-033: E2 is both a consumer of E1 representations and an
    active shaper of what E1 must produce.
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: no experiment has yet targeted MECH-081 directly --
    the claim's own architecture doc (e1_e2_constraint_propagation.md#mech-081) states
    a "testable implication" but no run against it exists in evidence, and the claim
    carries no evidence_quality_note. A valid test needs (a) a matched pair of E1
    training conditions -- E2 present (top-down sufficiency constraint active) vs. E1
    trained alone with no E2 top-down signal -- built on the ARC-001/ARC-002/MECH-033
    substrate this claim depends on and extends; (b) a non-degenerate effective-
    dimensionality metric for E1's representation (e.g. latent participation ratio,
    covariance rank, or minimum hidden-layer depth/width needed to hit a fixed
    reconstruction or prediction-quality bar) that varies across seeds/conditions
    rather than saturating at a floor or ceiling in both arms. A run where the two
    arms are indistinguishable because the dimensionality metric itself has near-zero
    range should self-route substrate_not_ready, not count as a verdict.
    CONFIRMING: E1 trained with E2 present shows measurably LOWER effective
    dimensionality (sparser / more clustered / lower participation ratio, or fewer
    hidden units/depth needed to converge) than E1 trained alone with no E2 top-down
    signal, holding the sensory data distribution and E1's own unsupervised objective
    fixed. Per the architecture doc's own testable implication, the specific
    dimensions E1 retains under the E2-present condition should additionally be
    predictive of E2's transition-prediction accuracy (not merely lower-dimensional
    by coincidence). Ablating or down-weighting the E2 top-down term should move the
    dimensionality requirement back toward the E1-alone baseline, on >=2/3 seeds.
    FALSIFYING: E1's effective dimensionality / depth requirement is unchanged, or
    increases, when the E2 sufficiency constraint is active vs. severed -- or the two
    conditions are statistically indistinguishable given the metric's own seed
    variance -- indicating E2's prediction task does not meaningfully pre-specify what
    E1 must resolve, and feature discovery is not offloaded top-down as the claim
    asserts. A result where E1 is lower-dimensional under E2 but the retained
    dimensions are NOT predictive of E2 transition accuracy would be a mixed/partial
    result (dimensionality reduced for some other reason, e.g. regularisation from the
    joint objective, not from sufficiency-constraint specification) and should be
    reported as such rather than rounded to a clean confirm.
- id: MECH-082
  title: "Hippocampal map distortion propagates through E2 to bias E1 attentional sampling without explicit E1 retraining."
  claim_type: mechanism_hypothesis
  subject: e1_e2.top_down_perceptual_bias_propagation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-081
    - MECH-076
    - MECH-027
    - ARC-001
    - ARC-002
  location: docs/architecture/e1_e2_constraint_propagation.md#mech-082
  source:
    - docs/architecture/e1_e2_constraint_propagation.md
  notes: >
    Changes in E2's predictive model (shaped by hippocampal map distortion, MECH-076)
    propagate downward as changes in E1 perceptual sampling without explicit E1 retraining.
    Map distortion → biased E2 predictions → E2 sufficiency constraint (MECH-081) directs
    E1 to over-sample residue-consistent sensory dimensions. Provides a computational account
    of perceptual bias in trauma (PTSD hypervigilance), paranoia, and depression. Compatible
    with MECH-056 (trajectory-first residue): mechanism operates via trajectory-level basin
    geometry, not direct E1 representational distortion.
# Registered 2026-03-15 — NC-11 through NC-16 four-plane neuromodulatory control cluster
# ⚠ MECH-085 conflicts with MECH-006 (serotonin temporal depth prohibition) — pending Daniel's adjudication
- id: MECH-083
  title: "Acetylcholine as meta-level plasticity gain governing durable-write vs read-through."
  claim_type: mechanism_hypothesis
  subject: acetylcholine.meta_level_plasticity_gain
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  depends_on:
    - ARC-005
    - ARC-007
    - ARC-018
    - INV-019
    - INV-024
  location: docs/architecture/neuromodulatory_control_planes.md#mech-083
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: requires a continuous ACh-analogue plasticity-gain
    parameter in ree_core, distinct from the binary hypothesis_tag gate, causally read at
    hippocampal write time (ree_core/hippocampal/module.py, ree_core/residue/field.py)
    and continuously modulating write/accumulation strength. Currently FAILS: ree_core
    contains zero occurrences of "acetylcholine"; the only write gate at these sites is
    the boolean hypothesis_tag (MECH-094). MECH-094's own notes name this exact gap and
    point to a "candidate implementation" (MECH-249, continuous ACh/NE state variable)
    that is itself unbuilt and status: candidate. Until a scalar ACh-gain channel exists
    and is wired to write strength, no non-degenerate experiment can distinguish
    "continuous gain" from "binary gate."

    CONFIRMING: once built, sweeping the ACh-gain parameter across a continuous range
    during otherwise-identical replay/rehearsal trajectories produces a monotonic dose-
    response in map-update magnitude (residue accumulation, MECH-076 basin-steepness
    change) -- not a step function -- and specifically that high-gain retrieval of a
    stuck/looping rollout produces a measurably deeper attractor basin than low-gain
    retrieval of the same rollout (the OCD-relevant failure mode this claim's own notes
    predict).

    FALSIFYING: (a) once built, the gain parameter's dose-response is effectively
    bimodal/step-shaped (writes occur at ~full strength or ~zero with no graded
    intermediate regime) -- falsifies the "continuous, not binary" component
    specifically, even though durable-write/rehearsal separation (INV-019) may still
    hold. (b) the binary hypothesis_tag gate is found sufficient to explain the
    separation with no unexplained residual variance -- as INV-019's own wave-15 audit
    already found for the E1/simulate() pathway (V3-EXQ-499/633/880 PASS via
    hypothesis_tag alone) -- which would falsify this claim's requirement of a
    CONTINUOUS ACh mechanism as architecturally necessary, without falsifying ACh's
    biological plausibility or INV-019's coarser separation claim.

    GOVERNANCE FLAG (2026-08-08, not resolved here): MECH-083 and MECH-249 look like an
    unmerged near-duplicate pair -- both propose "ACh as continuous hippocampal-write-
    gain parameter, replacing a binary switch," registered a month apart (2026-03-15 vs
    2026-04-16), never cross-referenced in claims.yaml, both still candidate with no
    substrate. MECH-249 is the broader claim (adds NE, spans multiple action-modes);
    MECH-083 is the narrower hippocampal-specific case tied to INV-019/INV-024. Worth a
    governance pass to either cross-reference them or fold MECH-083 into MECH-249 as the
    hippocampal-specific sub-case.
  source:
    - docs/architecture/neuromodulatory_control_planes.md
  notes: >
    ACh is the meta-level control plane: modulates plasticity gain on bottom-up input —
    how strongly current sensory experience modifies the hippocampal map (high ACh) vs.
    how strongly existing attractors dominate interpretation (low ACh). NOT a binary switch
    but a continuous gain parameter. Distinct from NA: ACh governs whether attended input
    leaves a lasting map trace (learning timescale); NA governs whether the system orients
    to bottom-up surprise right now (moment-to-moment). Meta-level because it determines
    whether other planes' effects consolidate durably. Failure: OCD-relevant (high ACh
    during retrieval deepens stuck attractors); chronic high = map instability; chronic low
    = learning failure / confirmation bias. Functionally instantiates INV-019/INV-024
    (rehearsal vs durable write separation) at hippocampal level.
- id: MECH-084
  title: "Noradrenaline as attentional snap and E1/E2 sampling ratio modulator."
  claim_type: mechanism_hypothesis
  subject: noradrenaline.attentional_snap_and_e1_e2_sampling_ratio
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-001
    - ARC-002
    - MECH-005
    - MECH-081
    - ARC-005
  location: docs/architecture/neuromodulatory_control_planes.md#mech-084
  source:
    - docs/architecture/neuromodulatory_control_planes.md
  notes: >
    NA provides the attentional snap: fast moment-to-moment orienting to bottom-up surprise.
    High NA reduces E2's constraining influence on E1 (MECH-081), snapping current attention
    to unexpected sensory signals. Distinct from ACh (MECH-083): NA = does the system orient
    to this input now? ACh = does that orientation leave a lasting map trace? Together:
    high NA + high ACh = maximum new learning from unexpected input; high NA + low ACh =
    aroused/vigilant but not learning; low NA + high ACh = steady ambient updating.
    Too little: perceptual confirmation bias (E2 dominates). Too much tonic: pervasive snap
    to noise (PTSD hypervigilance). Too much phasic/dysregulated: episodic E1/E2 decoupling
    (candidate dissociation mechanism). Extends MECH-005 with upstream E1/E2 interface account.
- id: MECH-085
  title: "Serotonin as hippocampal map geometry parameter (rollout depth, distal value weighting) — conflict with MECH-006, pending adjudication."
  claim_type: mechanism_hypothesis
  subject: serotonin.hippocampal_map_geometry_parameter
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  conflict_with:
    - MECH-006
  epistemic_category: standard
  conflict_note: |
    MECH-006 explicitly states serotonin "does NOT select temporal depth (τ)."
    MECH-085 claims serotonin controls temporal depth of rollouts and modulates aversive
    attractor basin depth. Candidate reconciliation (raphe/receptor-subtype framing): multiple
    raphe nuclei and receptor subtypes mean serotonin may re-use the same motif (modulating
    resolution pressure/exclusivity) at different hierarchical levels. MECH-006 (dorsal raphe
    → cortical): cognitive-narrative collapse/exclusivity. MECH-085 (median raphe → hippocampal
    CA1/DG): same motif as attractor basin depth and rollout depth. If accepted, MECH-006's
    "no temporal depth τ" holds for the E-stack level; MECH-085's rollout depth is a distinct
    hippocampal-terrain construct.
    UPDATE 2026-08-08 (thought-digestion currency check): the literature pull this note asked
    for already happened, 2026-06-12 -- evidence/literature/targeted_review_receptor_subtype_layer/
    (Ren et al. 2019 raphe anatomy, Weber & Andrade 2010 5-HT2A cortical L5, Akimova et al. 2009
    5-HT1A/anxiety, Wu et al. 2021 median-raphe hippocampal stability), synthesized into
    docs/architecture/receptor_subtype_intervention_layer.md SS3A, which proposes exactly the
    reconciliation above. Support is qualified, not a clean confirm (Akimova conf 0.64 with an
    explicit PTSD-null counter-finding; Ren et al. conf 0.63, calls the clean split "a
    simplification"; Wu et al. flagged as possibly a mild competing account -- its effect is
    5-HT1B not 5-HT1A, and social-memory stability not aversive-basin depth). SS4 of the same doc
    states plainly that receptor-subtype resolution is out_of_domain for V3 -- adjudicated
    against clinical literature, not V3 runs. Still Pending adjudication -- now on already-
    gathered qualified evidence, not on a missing lit-pull. Route the accept/revise/reject
    decision to governance/Daniel; if accepted, MECH-006's own claims.yaml entry (unchanged
    since 2026-07-11) needs a companion update recording the level-specificity annotation.
  depends_on:
    - MECH-006
    - MECH-073
    - MECH-076
    - ARC-013
  what_would_answer: >
    This claim's REE-internal mechanism (does serotonin's tonic_5ht signal actually
    modulate rollout depth or distal/proximal value weighting in the live substrate) is
    separable from, and answerable independent of, the biological conflict-with-MECH-006
    question above (which is out_of_domain per the note's own cited source).

    NON-DEGENERACY PRECONDITION: "serotonin modulates rollout depth / distal value
    weighting" requires a live write path from tonic_5ht (or a value it directly sets:
    z_goal_seeding_gain, valence_wanting_floor) into the CEM rollout-depth or
    temporal-discount machinery -- config.horizon, mode_horizon_scale
    (MECH-267/SD-MECH267-HORIZON-DEPTH scoring-window fraction), or an analogous discount
    term -- not merely a shared "map geometry" vocabulary or an indirect correlation via
    general goal-seeking strength.

    CONFIRMING: a live code path in which tonic_5ht (or z_goal_seeding_gain /
    valence_wanting_floor) is read by HippocampalModule's rollout-depth or scoring-window
    logic, or by any temporal-discount parameter -- OR an experiment showing that varying
    tonic_5ht causally shifts measured effective rollout depth or distal-vs-proximal value
    weighting in trajectory selection.

    FALSIFYING (VIOLATED BY) -- CONFIRMED CURRENT STATE (2026-08-08 digestion pass):
    grep-verified, ree_core/neuromodulation/serotonin.py's tonic_5ht writes ONLY to
    GoalConfig.z_goal_seeding_gain and GoalConfig.valence_wanting_floor (per its own
    docstring). Zero references to tonic_5ht in ree_core/hippocampal/module.py (which
    implements config.horizon and the MECH-267/SD-MECH267-HORIZON-DEPTH mode_horizon_scale
    mechanism) or in ree_core/cingulate/salience_coordinator.py (which produces the
    operating_mode that mode_horizon_scale is keyed on, itself driven only by drive_level /
    dACC PE-foraging-difficulty / is_offline / pcc_stability / aic_salience -- no serotonin
    term). No "discount"/"distal" code site in ree_core/ references tonic_5ht either. The
    mechanism MECH-085 describes -- serotonin controlling rollout depth or distal/proximal
    value weighting -- has NO live write path in ree_core as of this commit. This is a
    structural fact resolvable by code inspection, not an experimental outcome requiring a
    queued V3 run. (Independently corroborates MECH-007's same-session finding that
    tonic_5ht's only writes are to z_goal_seeding_gain/valence_wanting_floor.) The
    mechanistic sub-claims (rollout depth, distal value weighting) are currently
    unimplemented design proposals, not live substrate behavior.
  location: docs/architecture/neuromodulatory_control_planes.md#mech-085
  source:
    - docs/architecture/neuromodulatory_control_planes.md
  notes: >
    Serotonin as map geometry parameter: (1) temporal depth of rollouts — how far forward
    simulation extends; (2) distal/proximal value weighting — high 5-HT flattens temporal
    distance penalty on far attractors; (3) aversive attractor basin depth — modulates
    steepness of threat-related attractor walls, explaining SSRI anxiolytic effects and
    2-4 week onset delay. Extends MECH-006 (representational collapse/exclusivity) and
    MECH-076 (structural map deformation). Serotonin is a map geometry parameter, not a
    mood neuromodulator. Conflict with MECH-006 requires adjudication before promotion
    beyond candidate.
- id: MECH-086
  title: "Dopamine as trajectory selection gain plane operating downstream of landscape, state, and encoding gate."
  claim_type: mechanism_hypothesis
  subject: dopamine.trajectory_selection_gain_plane
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-18
    needs_review: false
    evidence:
      from: "decision:MECH-086@2026-07-18T06:40:45.166296Z"
      as_of: 2026-07-18
      verdict: promote_to_provisional/applied
  evidence_quality_note: >
    2026-07-18 governance: candidate -> provisional. V3-EXQ-775 (dopamine selection-gain plane
    dose-response; run_id v3_exq_775_mech086_selection_gain_dose_response_20260717T152037Z_v3)
    PASS/supports, self-route selection_gain_dose_response_confirmed, no adjudication flag.
    First experimental entry for MECH-086 (exp_conf 0.774, lit_conf 0.738; 3 supports / 0 weakens).
    All FOUR signatures pass on 3/3 seeds (majority required 2) and are non-degenerate:
    S1 gain-monotonicity (decisiveness rises with DA gain on both landscapes -- an empirical
    substrate result, not a softmax identity, since it is measured through the real e3.select()
    path); S2 value-conditional benefit (trajectory quality improves with gain on a CLEAN landscape
    but NOT on a distorted one -- the hierarchical-constraint claim that DA optimises over whatever
    landscape it receives and cannot rescue upstream plane failure); S3a indecision pole at low gain;
    S3b aberrant-salience pole (confident commitment to the WRONG trajectory at high gain over a
    distorted landscape). Env-free controlled probe over a 5-point gain ladder x {clean, distorted}
    factorial (10 arms x 3 seeds).
    GAP CLOSED: MECH-086's own subject (DA = selection GAIN plane) had never been directly measured
    -- V3-EXQ-674 exercised the same e3.select() softmax but tagged claim_ids=[MECH-087] only and
    measured cross-plane ORDERING, explicitly disclaiming MECH-086 validation.
    SCOPE: validates the selection-gain dose-response and the two pathology poles. The four-plane
    hierarchical ORDERING claim remains MECH-087's (V3-EXQ-674), not established here.
  depends_on:
    - MECH-043
    - MECH-075
    - MECH-085
    - ARC-005
  location: docs/architecture/neuromodulatory_control_planes.md#mech-086
  source:
    - docs/architecture/neuromodulatory_control_planes.md
  notes: >
    Dopamine sets gain on trajectory selection — which rollout wins and how quickly — by
    modulating attractor dynamics in the already-valenced hippocampal system. Exploitation
    signal at planning level. Key claim: dopamine optimises selection over whatever landscape
    (serotonin, MECH-085) and state representation (NA, MECH-084) and encoding mode (ACh,
    MECH-083) it receives — cannot compensate for upstream plane failures. Too little:
    indecision, flattened landscape, anhedonia. Too much: premature commitment, aberrant
    salience. Extends MECH-043 (unsigned PE precision-weighting) and MECH-075 (BG attractor
    threshold) with the hierarchical-constraint claim and explicit four-plane positioning.
- id: MECH-087
  title: "Hierarchical ordering of four neuromodulatory control planes: ACh → NA → 5-HT → DA."
  claim_type: mechanism_hypothesis
  subject: neuromodulation.four_plane_hierarchical_ordering
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_674_mech087_cross_plane_nonrescue_20260612T161453Z_v3
      as_of: 2026-06-12
      verdict: supports/PASS
  depends_on:
    - MECH-083
    - MECH-084
    - MECH-085
    - MECH-086
    - ARC-005
    - MECH-063
  location: docs/architecture/neuromodulatory_control_planes.md#mech-087
  source:
    - docs/architecture/neuromodulatory_control_planes.md
  notes: >
    Four neuromodulatory planes are hierarchically ordered by upstream position:
    ACh (meta-gate) → NA (perceptual sampling) → 5-HT (map geometry) → DA (selection gain).
    Each downstream plane operates over outputs of all upstream planes. Predictions:
    (1) cascade failure — dysfunction at any level cascades down but produces qualitatively
    distinct failure modes; (2) intervention order matters — upstream planes should be
    stabilised before downstream optimisation; (3) dopamine augmentation on distorted
    serotonergic landscape worsens outcomes. Extends MECH-063 (orthogonal control axes)
    with hierarchical asymmetry: axes are ordered, not merely orthogonal. Testable: degrading
    5-HT axis produces trajectory pathology not rescued by DA augmentation.
  evidence_quality_note: >
    [2026-06-12 governance] First experimental evidence: V3-EXQ-674
    (cross_plane_non_rescue) PASS / supports, non_degenerate=true. Controlled
    probe of the real E3 softmax-selection stage (no env/training, forced
    uncommitted regime so softmax temperature is the operative dopamine-selection
    gain). Mapping: dopamine selection-gain = E3 softmax temperature; serotonin
    map-geometry terrain = the per-candidate score landscape. Both dissociations
    confirmed on 3/3 seeds: terrain-lesion NOT rescued by dopamine
    (rescue_fraction_A=-0.02) but rescued by terrain (rf_B=1.0); dopamine-lesion
    rescued by dopamine (rf_C=1.63) but NOT by terrain (rf_D=0.0). All four
    readiness preconditions met (clean-landscape discriminative 3.08>1.0;
    terrain-lesion moves argmin 0.91; dopamine-temperature modulates decisiveness
    0.93). Supports prediction (3): DA augmentation on a distorted serotonergic
    landscape does not rescue. Stays candidate (single supporting probe of one
    plane-pair; receptor-subtype layer treated out_of_domain for V3).
- id: MECH-088
  title: "Psychiatric conditions as four-plane neuromodulatory control failures."
  claim_type: mechanism_hypothesis
  subject: neuromodulation.psychiatric_conditions_four_plane_failures
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-083
    - MECH-084
    - MECH-085
    - MECH-086
    - MECH-087
    - MECH-080
    - MECH-027
  location: docs/architecture/neuromodulatory_control_planes.md#mech-088
  source:
    - docs/architecture/neuromodulatory_control_planes.md
  notes: >
    Psychiatric conditions as four-plane control failures:
    PTSD = 5-HT map geometry failure (deep aversive attractors) + secondary NA hyperactivity;
    Depression = 5-HT distal attractor inaccessibility + DA selection failure;
    ADHD = NA inconsistent sampling + DA unstable attractor commitment;
    OCD = DA selection stuck in local attractor + ACh gating failure (stuck rollout
    re-encodes on each retrieval);
    Psychosis = NA collapse of E1/E2 constraint + DA aberrant salience on noisy input.
    Extends MECH-027 (pathological modes) and MECH-080 (rollout truncation profiles) to
    PTSD/depression/psychosis and grounds all profiles in four-plane hierarchy. Generates
    intervention-order predictions (MECH-087): address upstream plane failures before
# Registered 2026-03-15 — imagination / residue write gate cluster
# Source: phenomenological analysis of imagination vs experience distinction
- id: MECH-094
  title: "The system maintains a functional distinction between simulation-mode content and real-experience content, such that simulation content does not accumulate in the viability map as committed experience; failures of this distinction produce confabulation (simulated events encoded as real)."
  claim_type: mechanism_hypothesis
  subject: default_mode.simulation_real_distinction_write_profile
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-466d_2026-06-24#V3-EXQ-466d"
      as_of: 2026-06-24
      verdict: non_contributory/?
  claim_level: functional
  evidence_quality_note: |
    EXQ-140 reclassified non_contributory (2026-04-09): substrate gap -- no explicit
    simulation/real distinction mechanism implemented in ree_core. Claim held pending
    implementation of one or more of the candidate biological mechanisms (MECH-248
    source monitoring, MECH-249 neuromodulatory mode-setting). Updated 2026-04-16:
    'hypothesis_tag as categorical gate' language removed; claim reframed as functional
    requirement whose biological implementation is distributed and pending lit-pull
    evidence (EVB-0134, EVB-0135). Promoted candidate -> provisional 2026-04-19
    (governance): 12 supporting runs, 0 opposing, conf 0.856. EXQ-448 (SD-032e pACC
    autonomic) PASS confirmed first concrete wiring via hypothesis_tag skip (50 skipped,
    drive_bias held at 0). Promoted provisional -> stable 2026-04-24 (governance):
    13 supporting lit entries, 0 opposing, conf 0.868.

    2026-05-11 carve-out registration (MECH-322): MECH-094 strict gating REMAINS
    DEFAULT for all write paths. MECH-322 registers a single narrow exception path
    that permits ARC-071 chunking from replayed sequences in designated SD-017
    sleep mode IF (a) value-tag from prior real-executed episodes meets a high-
    positive threshold, (b) sleep phase only (waking DMN remains MECH-094-strict),
    (c) replay_origin=True audit flag with accelerated dissolution if not
    corroborated by real waking execution within N episodes. Resolves R6 governance
    escalation from ARC-071 lit-pull (targeted_review_arc_071_composition synthesis,
    2026-05-10). MECH-094 itself is not relaxed; MECH-322 is a parallel write-path
    with its own conditions. MECH-292/293 ghost-goal probes (waking DMN) remain
    MECH-094-strict; the carve-out is sleep-only.

    2026-06-01 active_conflict flag disposition (governance triage). The indexer
    surfaced MECH-094 in conflicts.md with conflict_ratio 0.083 (23 supports vs
    1 weakens). On inspection the flag is non-load-bearing and is cleared without
    status change. Three findings: (1) The single weakens entry is the ARC-071 R6
    source (sleep-replay drives motor-chunk consolidation; Hippocampus journal).
    This is exactly the source that triggered the MECH-322 carve-out registration
    on 2026-05-11 -- the weakening is already governed by the parallel write-path
    above and is not a refutation of MECH-094's strict-gating default. (2) The
    four mechanism-refinement quotes appearing under "MECH-094" in conflicts.md
    (categorical-vs-graded tag, encoding-vs-retrieval, selective PTSD reading,
    multi-factor inference) are NOT weakening evidence -- they are honest
    failure_signatures embedded in three SUPPORTING lit-pull entries (Dijkstra
    et al. 2022 reality monitoring review evidence_direction=supports conf 0.82;
    Kunimatsu et al. 2019 PTSD MRI review evidence_direction=supports conf 0.65;
    King et al. 2013 source memory fMRI evidence_direction=supports conf 0.68).
    The indexer surfaces these as caveats for accountability; they do not flip
    the underlying evidence direction. (3) The categorical-vs-graded critique
    is already pre-empted by the 2026-04-16 reframing in this evidence_quality_note
    and the notes: field of this claim, which explicitly removed "categorical gate"
    language and registered the biological implementation as a distributed set of
    overlapping mechanisms (source monitoring MECH-248, temporal context filtering,
    neuromodulatory mode-setting MECH-249); the "hypothesis_tag" shorthand names
    the combined functional effect of those distributed mechanisms, not a single
    categorical circuit element. The encoding-vs-retrieval critique is addressed by
    the same distributed-implementation framing: source monitoring operates at
    retrieval, neuromodulatory mode-setting at encoding, and MECH-094 names the
    combined functional separation rather than asserting a single-stage gate. The
    PTSD-failure-mode prediction is best read as "graded tag degradation" under
    the distributed implementation -- consistent with the 2020 Lavalle et al.
    clinical dissociation between tag-loss (Korsakoff/confabulation) and
    tag-misassignment (schizophrenia/hallucination) already captured in the
    related-claims evidence pool. (4) Packaging caveat: the conflicts.md
    Source field at top-of-row cites
    `2026-05-28_e2_action_divergence_vestibular_cerebellum_cullen2023`. This is
    an indexer artefact -- the field shows the most-recent lit-pull touching
    MECH-094, not the citation behind any specific failure_signature. Cullen 2023
    is about motor corollary-discharge gating in the vestibular cerebellum and is
    unrelated to simulation/real distinction. Verdict: no status change, no
    confidence change, no claim_evidence edit. The mechanism-refinement notes
    will continue to surface in conflicts.md as honest caveats; this paragraph
    is the audit record of their disposition. MECH-257 dual-function workset
    cross-link not affected (MECH-094 remains stable).
  depends_on:
    - ARC-014
    - INV-011
    - INV-019
    - MECH-060
    - MECH-029
    - MECH-248
    - MECH-249
  location: docs/architecture/default_mode.md#mech-094
  source:
    - docs/architecture/default_mode.md
    - docs/architecture/control_plane_heartbeat.md
  notes: >
    The functional requirement registered by MECH-094 is that simulation-mode content --
    internally generated trajectories, rehearsal, imagination -- must not accumulate in
    phi(z) as committed experience. This is what must be true for the architecture to
    maintain the separation between what the agent has done and what it has only rehearsed.
    The biological implementation is not a single categorical gate but a distributed set of
    overlapping mechanisms: source monitoring (frontoparietal network -- distinguishing
    internally generated from externally received content; candidate implementation:
    MECH-248), temporal context filtering (OFC/basal forebrain -- suppressing
    non-currently-relevant activated content; Schnider 2003), and neuromodulatory
    mode-setting (ACh/NE balance -- setting hippocampal encoding mode as a continuous
    state variable rather than a binary gate; candidate implementation: MECH-249). The
    'hypothesis_tag' shorthand refers to the combined functional effect of these
    mechanisms, not a specific identifiable circuit element.
    During DMN/simulation mode, the functional effect is that only pre-commit simulation
    error (MECH-060) is active at the terrain-update level; the post-commit
    realized-outcome channel is not engaged. This is the condition that prevents phi(z)
    terrain from being updated by simulated trajectories as if they were committed
    experience. The qualitatively different affective texture of imagination vs. real
    experience -- muted, hypothetical, without full somatic grounding -- is consistent
    with this functional separation, though the precise mechanistic implementation awaits
    the MECH-248/249 evidence. Implements INV-011 (imagination without belief update)
    and INV-019 (rehearsal/durable-update separation) at the functional level.
    Pathological failure: when the simulation/real distinction fails -- confabulation --
    simulated or replayed content accumulates in phi(z) as if real experience (MECH-076
    map deformation). The confabulation mechanism is the failure of source monitoring
    and/or temporal context filtering (Schnider 2003), not excessive vividness or
    intensity of simulation. In rumination, repeated simulated-harm trajectories
    accumulate as if they were real harm experiences. This is mechanistically distinct
    from psychosis (prior-dominated precision failure, MECH-244; or signal-degradation
    pathway, MECH-246; or trauma-shaped prior pathway, MECH-247) and hallucination
    (generative model dominance without bottom-up grounding, MECH-245), which require
    separate mechanisms. Simulation-driven viability map updates (MECH-092) are safe to
    the degree that the source-monitoring and neuromodulatory mechanisms distinguishing
    simulation from reality are intact. Updated 2026-04-16 based on Papez circuit
    lit-pull discussion: categorical gate language removed, functional framing adopted,
    biological implementation candidates MECH-248/249 registered.
  pharmacological_predictions:
    - PHARM-006  # SSRIs do not address the simulation/real-distinction failure underlying chronic-PTSD rumination architecture
# Registered 2026-03-15 — control plane heartbeat architecture cluster
# Source: conversation developing heartbeat model from MECH-057a FAIL analysis
- id: ARC-023
  title: "Three BG-like loops operate at characteristic thalamic heartbeat rates."
  claim_type: architectural_commitment
  epistemic_category: substrate_conditional
  subject: basal_ganglia.three_loop_thalamic_heartbeat
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: SD-006 phase 2 (true asynchronous multi-rate
    execution) must be active, not the phase-1 time-multiplexed substrate. EXQ-131's
    own diagnosis (2026-03-30 review) is explicit that synchronous/time-multiplexed
    polling produces an "E3 output freeze artifact" -- stale E3 state under
    synchronous polling drives var_harm_eval_on to near-zero (1.05e-7), statistically
    indistinguishable from the ablated condition (0.00276), regardless of whether the
    three loops are genuinely rate-separated. A test run on phase-1-only substrate is
    therefore uninformative by construction, not merely under-powered, and cannot be
    read as evidence either for or against this claim.

    CONFIRMING signature: with SD-006 phase 2 active, E1/E2/E3 show measurably
    distinct and stable update-rate signatures (effective update frequency, or
    latency-drift-under-load) that persist without collapsing into lockstep when
    processing latency is varied. If EXQ-131's var_harm_eval-style discriminative-pair
    metric remains diagnostic once E3 state updates are genuinely asynchronous rather
    than time-multiplexed-stale, reuse it directly. If the phase-2 substrate changes
    how E3 state is exposed between ticks, the metric should be replaced with a direct
    measurement of realized inter-loop update-rate ratios (E1:E2:E3 tick-count ratios
    under load, measured empirically) plus a rate-drift measure under injected latency
    perturbation.

    FALSIFYING signature: even with SD-006 phase 2 active, the three loops show no
    measurable rate distinction under nominal conditions, or the loops drift into
    lockstep (or an undifferentiated rate) once processing latency is varied -- i.e.
    rate separation exists only as configured periods and collapses under any real
    asynchronous load. In that case "characteristic thalamic heartbeat rates" is
    architecturally decorative (a naming/period-configuration choice) rather than a
    functionally necessary coherence mechanism, and the claim should be demoted or
    reframed as a design choice rather than an architectural commitment.
    evidence:
      from: "decision:ARC-023@2026-03-16T18:20:19.361127Z"
      as_of: 2026-03-16
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v4  # 2026-08-16 governance (GFLAG-0037 split): stays gated on SD-006 phase 2 true concurrency, routed to V4
  evidence_quality_note: |
    Held (2026-03-28): specific blocker is SD-006 (async multi-rate loop execution).
    SD-006 is not yet implemented beyond time-multiplexed phase 1. Full asynchronous
    multi-rate testing requires SD-006 phase 2 implementation.
    EXQ-131 FAIL/mixed (2026-03-29): multirate heartbeat discriminative pair FAIL. SD-006
    phase 2 async required for full three-loop rate separation. Confirms existing hold.
    EXQ-131 reclassified INCONCLUSIVE (2026-03-30 review): var_harm_eval_on=1.05e-7 (near-zero)
    vs ablated=0.00276 -- E3 output freeze artifact of time-multiplexed (not async) substrate.
    Synchronous polling creates stale E3 state; does not replicate biological async multi-rate.
    Re-run (T215546Z) identical metrics -- one result. SD-006 phase 2 (true async execution)
    is a structural prerequisite. Does not weaken ARC-023.

    [2026-08-16 governance, GFLAG-0037 SPLIT decision, cycle cranky-driscoll-126a36] implementation_phase CORRECTED v3 -> v4. This claim stays honestly gated on SD-006 phase 2 TRUE CONCURRENCY, which this cycle routed to V4: its non-degeneracy precondition is specifically that rate separation survive REAL asynchronous load, and no phase-1 time-multiplexed test can supply that -- unlike MECH-091, which was un-parked to V3 in the same decision because its mechanism is already built, wired and measurably firing. Recorded so this claim stops being counted as V3 work it cannot possibly complete. It carries ZERO experimental entries and has since registration, which is consistent with the block rather than evidence against the claim. This decision also resolves a live three-way inconsistency the brief surfaced: docs/architecture/v3_v4_transition_boundary.md:57 lists SD-006 as a SATISFIED V3 prerequisite with no phase-2 caveat at all (as does claims.yaml MECH-030), roadmap.md:4386 lists phase 2 under Open Questions as undecided and not yet designed (now stale -- the recommended HTA option is built), and commitment_closure_plan.md lines 409/647/666 say deferred-V4 but only CONDITIONALLY and about the GAP rather than phase 2. They cannot all be right; the phase-2-true-concurrency-is-V4 reading is the one adopted here. Full reasoning: evidence/planning/sd006_phase2_generation_brief.md.
  claim_level: mixed
  functional_restatement: >
    Three loops operate at distinct update rates: E1 continuous/frame-rate, E2
    motor-command rate, E3 deliberation rate (slowest). An update-rate management
    mechanism prevents loop drift under variable processing latency. The functional
    requirement is rate-separated asynchronous updates regardless of substrate; the
    biological substrate is thalamic pacemaking, which is not required for the ANN
    implementation.
  depends_on:
    - ARC-021
    - MECH-069
  location: docs/architecture/control_plane_heartbeat.md#arc-023
  source:
    - docs/architecture/control_plane_heartbeat.md
  notes: >
    Each BG-like cortico-striatal loop has a characteristic thalamic heartbeat rate:
    E1 at sensory frame rate (continuous); E2 at motor command rate; E3 at deliberation
    rate (slowest). Thalamic substrate: MD → E3 complex, VL → E2 motor pathway,
    sensory relay nuclei → E1. TRN acts as inter-loop routing switch. Coherence
    mechanism: thalamic clock prevents loop drift under indeterminate processing latency.
    Completion events are high-salience updates within the continuous stream, not the
    exclusive trigger. V1/V2 substrates use synchronous single-timestep updates and
    cannot test this claim (SD-006 required for V3).
- id: MECH-089
  title: "E1 updates are batched into theta-cycle summaries before reaching E3 (cross-frequency temporal packaging)."
  claim_type: mechanism_hypothesis
  subject: multirate.fast_to_slow_temporal_batching
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-089@2026-03-19T19:52:00Z"
      as_of: 2026-03-19
      verdict: promote_to_provisional/applied
  implementation_phase: v3
  claim_level: mechanistic
  functional_restatement: >
    Fast loop outputs are temporally batched before slow loops sample them. E3 receives
    integrated summaries of recent E1 activity rather than raw frame-by-frame prediction
    errors. E3's minimum harm attribution resolution equals its own update window size —
    it cannot attribute finer-grained than one E3 update cycle. In an ANN substrate this
    is implemented as a rolling aggregation over E1 outputs before E3 sampling, not as
    oscillatory coupling. The biological mechanism (theta-gamma CFC) is one implementation
    of this functional requirement; it is not required in the ANN substrate.
  evidence_quality_note: |
    EXQ-052b PASS (2026-03-21, 5/5): ThetaBuffer confirmed filling with E1 outputs and
    batching them before E3 sampling. max_theta_buffer_size correctly recorded via
    _z_world_buffer attribute. E3 functional at ~1/9 the step rate (e3_tick_ratio ≈ 0.109),
    cal_gap=0.846. Demonstrates that temporal batching of fast-loop outputs before slow-loop
    sampling is structurally in place and metrically verifiable. Single PASS sufficient to
    elevate from provisional to active -- mechanism is implemented and measurable.
    EXQ-066 FAIL (2026-03-23, 4/5): BACKWARDS RESULT. e3_pred_error_batched=0.009 vs
    raw=0.004 -- batched E3 error 2.28x WORSE than raw. C1 fails. Interpretation: theta
    batching with theta_k=4 destroys temporal resolution E3 is using. The environment has
    meaningful state changes every step; a 4-step average loses that fine-grained signal.
    Two possible readings: (1) theta_k=4 is too coarse for this environment -- the right
    batch size may be 1-2 steps, consistent with the user's observation that theta rhythms
    need to be faster than tactical decision timescales; (2) context-gated batching (only
    during stable/uncommitted windows, not uniformly) may be needed. The backwards result
    is scientifically informative: E3 is using step-by-step temporal information, not
    benefiting from smoothing. Redesign should test context-dependent theta window sizes
    or shorter theta cycles (theta_k=1 or 2). EXQ-047g queued for SD-005 functional
    separation test; MECH-089 redesign deferred pending theta_k calibration.
    EXQ-122 FAIL (2026-03-28): theta_k=2 discriminative pair (ON vs ABLATED, 2 seeds).
    ADVERSE DIRECTION: harm_auc_ON=0.489 vs harm_auc_ABLATED=0.625 (delta=-0.135). Even
    with k=2, theta batching hurts harm attribution. C1/C2/C3 fail; C4/C5 pass (32 harm
    steps -- data quality OK). Strengthens context-gated interpretation: uniform temporal
    smoothing is harmful at any tested batch size. Genuine weakening for static-k variant.
    Hold at active; context-dependent gating design needed before promotion.

    [2026-08-10 implement-substrate, SD-100]: a fixed (non-trained) forward-sweep phase-weighted
    summary mode landed in ThetaBuffer.summary() (config.heartbeat.use_theta_phase_weighted_summary,
    default False/bit-identical), replacing the permutation-invariant flat mean this note's own
    EXQ-066/EXQ-122 findings identified as harmful. This is a step toward the "context-dependent
    theta window" redesign line this note has left open since 2026-03-28 -- it is position/phase-
    sensitive rather than uniform, but it is NOT the context-GATED (stable-vs-volatile-window)
    variant this note specifically calls for; that remains open. See
    sd_100_theta_buffer_phase_aware_summary.md. Hold at active; validation via a recommended
    V3-EXQ-228d re-test (ARC-032) is the natural first read on whether phase-weighting alone
    (without context-gating) recovers the static-k losses.
  what_would_answer: |
    ALREADY-ESTABLISHED (do not re-litigate): uniform static-k theta batching
    (k=1, 2, 4) is CONFIRMED HARMFUL for harm attribution (EXQ-066: batched
    error 2.28x worse than raw; EXQ-122: harm_auc delta=-0.135 adverse
    direction, C1/C2/C3 fail). This is settled; only the context-gated
    redesign remains open.
    NON-DEGENERACY PRECONDITION: requires a context-dependent (non-uniform)
    theta window implementation to exist and be tested against a fixed
    static-k baseline with non-zero cross-condition variance on harm_auc /
    prediction-error metrics -- a run using only static-k configurations
    cannot reopen this question, it can only reproduce the already-confirmed
    weakening.
    CONFIRMING signature: a context-gated theta window (batching only during
    stable/uncommitted windows, not uniformly) shows harm_auc and
    prediction-error metrics AT LEAST at raw (unbatched) levels during
    volatile/committed windows, while still providing the batching benefit
    (reduced update-rate cost to E3) during stable windows -- i.e. the gating
    recovers what static k=2/k=4 lost.
    FALSIFYING: a context-gated variant STILL shows the adverse direction
    (batched worse than raw) even during stable windows, or no context-gating
    scheme recovers the static-k losses -- would indicate E1->E3 temporal
    batching is not a viable mechanism at any granularity and the
    theta-gamma framing should be retired in favour of raw pass-through.
  depends_on:
    - ARC-023
    - ARC-021
  location: docs/architecture/control_plane_heartbeat.md#mech-089
  source:
    - docs/architecture/control_plane_heartbeat.md
  notes: >
    Cross-frequency coupling (theta-gamma nesting) packages fast E1 updates for
    slow E3 consumption. Gamma cycles (E1-rate) nest within theta cycles (E3-rate);
    each theta cycle integrates ~5-7 gamma sub-cycles into a rolling summary that
    E3 samples at its heartbeat rate. E3 never receives raw E1 prediction error —
    only theta-cycle-averaged context. This defines the minimum temporal resolution
    for E3 harm attribution (cannot attribute finer-grained than one E3 heartbeat cycle).
    Thalamic relay performs the cross-rate integration. Analogous to theta-gamma coupling
    in hippocampal-prefrontal circuits.
- id: MECH-090
  title: "BG-level beta oscillations gate E3-to-action-selection propagation, not E3 internal updating."
  claim_type: mechanism_hypothesis
  subject: control_plane.commitment_gated_policy_output
  polarity: asserts
  status: active
  pending_retest_after_substrate: true
  live_status:
    reading: active
    as_of: 2026-08-03
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-871a_2026-08-03"
      as_of: 2026-08-03
      verdict: non_contributory/competence_implementation_gap
  implementation_phase: v3
  claim_level: mechanistic
  functional_restatement: >
    During committed action sequences, E3 continues updating its internal model but
    propagation of updated model state to action selection is gated. The gate opens at
    completion events or urgent interrupts. An urgent interrupt pathway can force an early
    gate-open without waiting for sequence completion. In an ANN substrate, this is
    implemented as a commitment-gated routing variable decoupling E3 forward pass from
    policy output — not as beta oscillation power. The biological mechanism (beta in
    STN/striatum) is one implementation; it is not required in the ANN substrate.
  evidence_quality_note: |
    EXQ-049 FAIL (2026-03-20): Same bug as EXQ-048 — agent.select_action() bypassed,
    gate never exercised. EXQ-049b fixes this.
    EXQ-059b FAIL (2026-03-20, 2/5 criteria): Same finding as MECH-057b — routing through
    select_action() restored but mean_running_variance=0.000. BetaGate elevation requires
    _running_variance to be populated, which only happens in the E3 training-loop path,
    not the inference path called during eval. C3 (beta elevated during committed sequence)
    and C4 (propagation blocked) untestable until variance accumulation is wired into
    the inference path. See MECH-057b for root-cause analysis. Fix: EXQ-049c calls
    agent.e3.update_after_execution(z_world_t, harm_prev<0) each step.
    EXQ-049b/c FAIL (2026-03-21): Chicken-and-egg deadlock — post_action_update() guards
    update_running_variance() behind _committed_trajectory is not None, but trajectory only
    set when committed, which requires variance < threshold. Variance frozen at 0.5 > 0.40.
    EXQ-049d FAIL (2026-03-21): Deadlock fixed by calling update_running_variance(wf_err)
    directly after world_forward loss. Committed condition PASS: hold_concordance=1.0,
    hold_count=10347. But over-correction: variance collapsed to 2.5e-6, agent permanently
    committed, n_uncommitted_steps=0 — C2 (uncommitted_release_concordance) has no data.
    Root cause: single-agent eval conflates training and eval conditions. Fix: two-condition
    design (EXQ-049e) — compare trained agent (low variance, committed, gate elevated)
    against untrained/reset agent (high variance, uncommitted, gate released). C1 (gate
    elevates when committed) is confirmed. C2 awaits EXQ-049e.
    EXQ-049e PASS (2026-03-21, 5/5): Two-condition design resolves the deadlock. Trained agent:
    10,000 committed steps, hold_concordance=1.0, hold_count=10,347, variance=2.5e-6.
    Fresh agent: 321 uncommitted steps, release_concordance=1.0, propagation_count=50.
    Both conditions at stable extremes -- committed gate elevates, uncommitted gate releases.
    MECH-090 fully confirmed.
    EXQ-062b PASS (2026-03-22, 5/5): Surprise-gated spike selectivity -- confirms that
    committed gate can be re-opened by selective interrupt (surprise-gated variance spike)
    without requiring full de-commitment from all harm contacts. Corroborates the
    gating architecture -- gate holds during expected harm, opens only on genuine surprise.
    EXQ-049a (2026-04-16, PASS): Bistable concordance confirmed. Committed/uncommitted
    gate states concordant across full run. Complements EXQ-049e (steady-state two-condition
    design) and EXQ-062b (surprise selectivity); adds bistable-latch regime validation.
    V3-EXQ-481 (2026-04-25, FAIL, diagnostic, inconclusive): V_s -> commit release substrate-
    readiness diagnostic. vs_commit_release_count=0 in BOTH ON and OFF arms -- the
    use_vs_commit_release hook is wired but never triggered (release path inert). Anchor
    resets fire (63/31 per seed) but the release predicate (active anchor mark_inactive
    OR staleness above release_threshold) never matches. Substrate-readiness only; not
    governance evidence against MECH-090 (the existing bistable + interrupt circuits remain
    validated). Next step: revisit BoundaryEvent rate, anchor-key snapshot scope, or
    commit-entry capture sites in REEAgent _committed_anchor_keys before re-running.

    V3-EXQ-592 (2026-05-21, manifest v3_exq_592_gap11_pilot_committed_mode_curriculum_
    20260521T104724Z): GAP-11 committed-mode curriculum pilot surfaced the rv-only commit-
    entry predicate failure mode. Seed 42: running_variance collapsed to 2.7e-5 while
    nav_competence stayed at 0.0 -- the agent satisfied the predicate by becoming
    trivially predictable (a degenerate near-fixed-point policy), not by becoming
    competent. The substrate-as-architected licenses this failure mode. NOT governance
    evidence against MECH-090 base-claim semantics (beta-gates-propagation-not-internal-
    updating is preserved); IS the motivating empirical signal for the R-c commit-entry
    readiness conjunction amendment landed 2026-05-28 (see implementation_note R-c
    section). Predecessor lit-pull: REE_assembly/evidence/literature/targeted_review_
    connectome_mech_090/synthesis.md commit 9e68c5ca8a 2026-05-28 (Cisek-Kalaska +
    Hanes-Schall + Roesch-Calu-Schoenbaum anchor the conjunction reading; Tandetnik 2021
    retained as R-b fallback). V3-EXQ-592b queued as the falsifier-grade validation of
    the substrate amendment (2-arm diagnostic; see implementation_note).

    [2026-06-03 governance / autopsy failure_autopsy_604a-624a-630_2026-06-03] V3-EXQ-630 (across-tick nav_competence ecological) re-tagged non_contributory: C4 FAIL -- OFF baseline showed the same readiness-conditioned commitment collapse (arm0_baseline_delta 1.0) as the gated arms, so the gate contribution is unattributable (SD-022 limb-damage degrade drives commitment via running_variance independent of nav_competence). measurement_test_design_defect; does NOT weaken this claim. Redesign queued (confound-isolation arm). ARC-029 separately supported by V3-EXQ-063a PASS (committed-mode harm outcomes, R-c gate) 2026-06-02.

    [2026-08-02 governance, V3-EXQ-871 (run v3_exq_855), confirmed failure_autopsy_V3-EXQ-871_2026-08-02, MECH-090/ARC-071 commit-latch post-fix validation]: self-routed substrate_not_ready_requeue (precondition arc071_chunk_commitments_observed_supra_floor measured 0.0 vs threshold 10.0, worst seed), adjudicated measurement_test_design_defect, NOT a substrate ceiling. Root cause: use_persistent_committed_program_handle=True was set for the ENTIRE cell (formation + probe) rather than scoped to the probe phase, which changes select_action's between-E3-tick fast path and cascades (via RNG-coupled environment dynamics) into materially different chunk-formation outcomes -- same seeds 101/505 formed only 1/0 chunks here vs 2/2 under V3-EXQ-841 (flag at lineage default) on 2026-07-31, a direct same-seed comparison. V3-EXQ-810a's/841's "readiness inherited by construction" claim only holds when the flag stays at its lineage default during formation. Neither MECH-090 nor ARC-071 evidence changes (non_contributory, precondition_unmet). Route: V3-EXQ-871a, scoping the flag to the probe phase only.

    [2026-08-03 governance, V3-EXQ-871a, confirmed failure_autopsy_V3-EXQ-871a_2026-08-03, MECH-090/
    ARC-071 commit-latch AT-tick persistence]: post-fix validation of ree-v3 278599a (between-tick
    fast path fallback to the SD-084 persistent handle) under REAL organic chunk formation. Self-routed
    mixed_across_seeds resolved by exact arithmetic as a tick-rate confound, not real seed-dependent
    discrimination: n_genuine_commits_identity_based == n_e3_ticks + n_episodes_probe exactly in BOTH
    seeds -- every single E3 tick recreates the committed-trajectory object, zero exceptions, both
    seeds. Fix (a) (between-tick fast path) confirmed correctly implemented; a second, still-open gap
    (b) -- E3 unconditionally recreates a fresh trajectory object at the AT-tick selection site,
    regardless of an existing unexpired commitment -- is confirmed as the dominant remaining defect,
    universally. non_contributory/competence_implementation_gap, narrow to this AT-tick wiring
    interaction; the core gate-propagation mechanism remains well-supported by EXQ-049e/062b/049a --
    no change to status/confidence. Routed /implement-substrate: create
    mech090-arc071-attick-persistent-handle-fix (extend the between-tick fix to the AT-tick selection
    site). A future validation should use a tick-rate-normalised DV (n_genuine_commits/n_e3_ticks), not
    raw mean segment length. pending_retest_after_substrate=true.
  implementation_note: >
    BISTABLE LATCH IMPLEMENTED 2026-04-10. REEAgent._e3_tick() and select_action() in
    ree_core/agent.py. Config: HeartbeatConfig.beta_gate_bistable (bool, False default).
    When bistable=True: gate elevates only on ENTRY to committed state (committed AND NOT
    currently elevated); hippocampal completion signal triggers release via
    beta_gate.receive_hippocampal_completion(hippocampal.compute_completion_signal(candidates)).
    Previous behaviour (per-tick raise/release) preserved when beta_gate_bistable=False.
    Backward compatible. Validation: V3-EXQ-321 queued.

    R-c COMMIT-ENTRY READINESS CONJUNCTION IMPLEMENTED 2026-05-28
    (commitment_closure_plan.md GAP-4 substrate landing; behavioural validation pending
    V3-EXQ-592b PASS). Predecessor synthesis: REE_assembly/evidence/literature/
    targeted_review_connectome_mech_090/synthesis.md commit 9e68c5ca8a. Motivated by
    V3-EXQ-592 seed 42 (manifest v3_exq_592_gap11_pilot_committed_mode_curriculum_
    20260521T104724Z): running_variance=2.7e-5 with nav_competence=0.0 satisfied the
    rv-only predicate -- the degenerate trivial-predictability basin. The substrate
    cannot distinguish honest commitment (rv falls because E2 learned dynamics) from
    degenerate commitment (rv falls because the policy collapsed to a near-fixed-point).
    R-c (single-gate conjunction) reading per synthesis Section 6: BetaGate entry
    requires rv_low AND readiness >= floor. Readiness signal = per-candidate first-action
    margin over E3 scores (top1 - top2 with REE lower-is-better, computed in agent.py
    from result.scores). Anchored on Cisek & Kalaska 2010 (affordance-competition),
    Hanes & Schall 1996 (FEF accumulator-to-threshold), Roesch / Calu / Schoenbaum 2007
    (DA readiness). Tandetnik 2021 R-b reading retained as fallback if validation fails.
    Modules: ree_core/heartbeat/beta_gate.py (BetaGate.should_admit_elevation +
    __init__ kwargs; get_state + reset extended with mech090_n_elevation_admitted /
    _blocked / _single_candidate / _last_readiness_score_margin diagnostics);
    ree_core/agent.py (REEAgent.__init__ forwards knobs via getattr; both elevate
    call sites in select_action compute margin once + guard with
    should_admit_elevation -- bistable branch on transition tick, legacy branch on
    every committed tick with release-on-block semantic). NOT modified:
    ree_core/predictors/e3_selector.py (rv-only committed signal stays as-is; gate
    layered on top of E3SelectionResult); deliberate to keep this substrate landing
    disjoint from the concurrent MECH-341 retune session that holds e3_selector.py.
    Config (HeartbeatConfig): use_commit_readiness_gate (bool, default False;
    bit-identical OFF); commit_readiness_floor (float, default 0.05 -- small
    relative to EXQ-608 mean_top2_class_gap range 0.27-1.96, so gate fires only on
    genuinely degenerate near-tie scoring); commit_readiness_strict_single_candidate
    (bool, default False; permissive single-candidate handling). NOT surfaced through
    REEConfig.from_dims (matches existing beta_gate_bistable precedent; keeps
    from_dims signature unchanged for concurrency-safety vs MECH-341).
    Backward compatible: 506/506 contracts PASS with master OFF (regression-clean
    2026-05-28). 7 BetaGate primitive unit tests PASS (default no-op, gate ON
    admit / block, single-candidate permissive / strict, reset clears, backward-
    compat elevate/propagate/release).
    Phased training: N/A (pure-arithmetic regulator; no learned parameters; no
    gradient flow; no encoder head).
    MECH-094: N/A. The gate writes a control-state transition (beta elevation),
    not memory content; no simulation-write surface.
    Validation experiment: V3-EXQ-592b queued -- 2-arm diagnostic.
    ARM_0 GATED: use_commit_readiness_gate=True, floor=0.05, same env+seed (42)
    as V3-EXQ-592. Acceptance: total_committed_steps == 0 AND
    mech090_n_elevation_blocked >= 1 AND running_variance < commitment_threshold
    at some point (confirming the gate is the load-bearing block).
    ARM_1 GATED_FORCED_READY: same gate config with experiment-side score_bias
    injection forcing margin >= 0.10. Acceptance: total_committed_steps > 0
    AND mech090_n_elevation_admitted >= 1 (confirming gate does not permanently
    lock out commitment when readiness clears). Joint PASS = commitment_closure:
    GAP-4 partial -> done; degenerate-basin entries blocked + readiness-cleared
    entries proceed. ARM_0 PASS + ARM_1 FAIL = floor too high (calibrate down).
    ARM_0 FAIL + ARM_1 PASS = unexpected; /diagnose-errors. Joint FAIL on ARM_0
    = substrate retest (counters / floor sweep).
    Design doc: REE_assembly/docs/architecture/mech_090_commit_entry_predicate.md

    R-c CONTINUATION: NAV_COMPETENCE AXIS IMPLEMENTED 2026-05-29
    (commitment_closure_plan.md GAP-4 substrate landing pass 2 of 2; behavioural
    validation still pending V3-EXQ-592b PASS). The 2026-05-28 landing implemented
    the WITHIN-TICK DECISIVENESS axis of R-c (per-candidate score margin -- the
    Hanes & Schall 1996 accumulator-to-threshold reading). This 2026-05-29 pass
    adds the ACROSS-TICK MOTOR-PROGRAM READINESS axis (Cisek & Kalaska 2010
    affordance-preparation + Roesch / Calu / Schoenbaum 2007 dopaminergic
    readiness). Both axes are R-c readings of the same literature corpus; they
    detect different aspects of the V3-EXQ-592 seed-42 degeneracy and AND-compose
    at the elevate sites.
    Why a second axis: score_margin catches degeneracy at the candidate-
    distribution level (V3-EXQ-592 seed-42's collapsed candidate pool) but is
    silent about whether the agent has demonstrably succeeded recently. A
    trivially-predictable policy can produce a marginally-clear E3 winner on
    a useless trajectory; the within-tick gate would admit, even though the
    across-tick competence record says the agent has reached nav_competence=0.0.
    The two axes compose: elevation is admitted iff (rv-low) AND (score_margin
    clears) AND (across-tick readiness >= floor).
    Module: ree_core/policy/commit_readiness.py (NEW; CommitReadiness +
    CommitReadinessConfig + diagnostics). Pure-arithmetic regulator (no
    nn.Module inheritance, no learned parameters), sibling pattern to
    MECH-313 NoiseFloor / MECH-320 TonicVigor. Maintains a [0, 1] readiness
    EMA over per-tick outcome signals plus an explicit notify_outcome(value)
    harness-push seam. Initial value 1.0 (fail-open: an agent with no outcome
    history defaults to "ready" so the conjunction reduces to the score_margin-
    only path until real outcome data has been collected). MECH-094 standard
    pattern: simulation_mode=True returns without advancing the EMA.
    Wiring: REEAgent.__init__ instantiates self.commit_readiness when
    config.use_commit_readiness=True (auto-armed by __post_init__ / from_dims
    OR-only resolver when use_mech090_readiness_conjunction=True).
    REEAgent.select_action computes _readiness_admits = commit_readiness.
    is_above_floor(mech090_readiness_floor) once at the top of the beta-gate
    block and AND-composes with the existing should_admit_elevation(score_margin,
    K) at BOTH call sites (bistable + legacy). Block diagnostics advance via
    commit_readiness.notify_block() at the source. REEAgent.reset calls
    commit_readiness.reset() per-episode.
    Per-tick outcome-signal source (Phase 1): the experiment harness pushes
    via notify_outcome(value). The substrate-side seam is wired; the harness
    is responsible for the per-tick update. committed_mode_curriculum.py
    pushes its probe-derived nav_competence via this seam. Phase 2 follow-on
    (separate /implement-substrate pass): wire an env-emitted
    "mech090_readiness_outcome" key reading in agent.sense() so the substrate
    advances readiness automatically without harness involvement.
    Config (REEConfig + from_dims):
    use_mech090_readiness_conjunction (bool, default False; bit-identical OFF);
    mech090_readiness_floor (float, default 0.3 -- mid-low floor that V3-EXQ-
    592 seed 42's nav_competence=0.0 clearly fails to clear; a competent agent
    reaching success rate >=30% clears; calibratable, V3-EXQ-592b sweeps this);
    use_commit_readiness (bool, default False; auto-armed True by the OR-only
    resolver when the conjunction flag is on; True without the conjunction
    instantiates the module as a passive diagnostic);
    commit_readiness_window (int, default 20; informational, alpha is the
    load-bearing knob); commit_readiness_ema_alpha (float, default 0.1;
    ~10-tick half-life); commit_readiness_initial (float, default 1.0;
    fail-open).
    Backward compatible: 523/523 contracts PASS (506 prior + 17 new MECH-090
    R-c-nav-competence contracts) with both R-c master flags OFF (regression-
    clean 2026-05-29). Master-OFF construction produces agent.commit_readiness
    =None and the agent runs bit-identical to pre-amendment.
    Phased training: N/A (pure-arithmetic regulator; no learned parameters;
    no gradient flow; no encoder head). MECH-094: standard simulation_mode
    pattern; gate decisions at waking action-selection only; the substrate
    is read-only over commit_readiness state at the elevate sites and writes
    only a control-state transition.
    Validation continuation: V3-EXQ-592b grid extended to 4 arms (ARM_2
    GATED_NAV_COMP_ON: nav_competence gate alone; ARM_3 GATED_BOTH_ON: both
    R-c gates active; ARM_4 BOTH_GATES_OFF_HARNESS_FORCES_READY: rv-only
    baseline with harness pushing notify_outcome(1.0) each tick). Falsifier
    grid: see design doc Section "R-c amendment continued / Falsifiability"
    for the four orthogonal outcomes (which-axis-carries-the-load discrimination).
    Two-axis composition admits independent falsification of each axis without
    contaminating the other.
    Design doc: REE_assembly/docs/architecture/mech_090_commit_entry_predicate.md
    (R-c continuation section appended 2026-05-29).

    R-c CONTINUATION PHASE-2 ENV-SOURCE FOLLOW-ON IMPLEMENTED 2026-06-02
    (additive wiring; NO flag/confidence/status change to MECH-090). The
    2026-05-29 pass wired the nav_competence CONSUMER (is_above_floor AND-composed
    at both elevate sites) + the notify_outcome seam but left no automatic SOURCE,
    and grep-verified zero notify_outcome callers existed repo-wide -- so in any
    ecological run the readiness EMA sat pinned at its fail-open initial 1.0 and
    the across-tick axis added no signal (precisely why V3-EXQ-063a left it OFF).
    This pass wires the named Phase-2 follow-on: CausalGridWorldV2 gains an
    env-only kwarg mech090_readiness_outcome_enabled (default False) emitting
    info["mech090_readiness_outcome"]=clip(1-mean(limb_damage),0,1) (a Cisek-Kalaska
    affordance-preparation / motor-program-readiness scalar that degrades with
    SD-022 limb damage and recovers on heal); REEAgent.sense() gains a
    mech090_readiness_outcome param that forwards it into commit_readiness.update().
    Default-OFF / bit-identical (719/719 contracts + 7/7 preflight; env key
    absent-when-disabled; commit_readiness None when master flags off). The
    CommitReadiness module is UNCHANGED. The across-tick axis is now exercisable
    ecologically for the first time. Validation: V3-EXQ-630 (ecological ARC-029
    successor to 063a; 592b ARM_2 GATED_NAV_COMP_ON + ARM_3 GATED_BOTH_ON arms
    ecologically -- suppress-on-degrade / admit-on-recover). Contract:
    ree-v3/tests/contracts/test_mech090_readiness_outcome_wiring.py. Substrate log:
    ree-v3/CLAUDE.md "MECH-090 R-c continuation Phase-2 follow-on" section.
    This is additive (no PASSED experiment measured the across-tick axis before --
    it was inert), so no prior MECH-090/ARC-029 evidence is made stale.
  literature_evidence: |
    Jenkinson & Brown (2011) PMID 22018805 [DOI: 10.1016/j.tins.2011.09.003]:
    "Beta oscillations provide a measure of the likelihood that a new voluntary action
    will need to be actuated." Elevated beta = stay the course. Beta is predictive
    (modulated by dopamine in anticipation of whether action change is needed) not
    reactive. Loss of dopamine (Parkinson's) annuls prospective action resourcing.
    This is the clearest literature statement of MECH-090: beta gate signals status
    quo maintenance, release signals openness to policy update.
    Frank (2005) PMID 15701239 [DOI: 10.1162/0898929052880093]:
    Dynamic dopamine modulation in BG: D1 (direct/Go) and D2 (indirect/NoGo) are
    differentially modulated by phasic dopamine. Phasic dopamine during reward
    potentiates D1 (facilitates commitment); dopamine dip relieves D2 suppression
    (enables de-commitment). This is the mechanistic basis for MECH-106 hysteresis.
    Frank, Seeberger & O'Reilly (2004) PMID 15528409 [DOI: 10.1126/science.1102941]:
    "By carrot or by stick": Go/NoGo learning asymmetry dopamine-dependent. Confirms
    the direct/indirect pathway architecture and asymmetric commitment modulation.
    Redgrave, Prescott & Gurney (1999) PMID 10362291 [DOI: 10.1016/s0306-4522(98)00319-4]:
    BG as centralized selection mechanism: winner-takes-all via lateral inhibition in
    striatum resolves competition for access to motor and cognitive resources. Grounds
    the single-selection (not graded) nature of the commitment gate.
  depends_on:
    - ARC-023
    - ARC-021
    - MECH-057a
    - MECH-138
  location: docs/architecture/control_plane_heartbeat.md#mech-090
  source:
    - docs/architecture/control_plane_heartbeat.md
  notes: >
    BG-level beta oscillations (~13-30 Hz, STN/striatum) gate propagation of E3 model
    updates to action selection -- NOT E3 internal model updating. During committed action
    sequence: beta elevated in BG -> E3 updates internally (heartbeat continues) but does
    not change action selection. At completion or hyperdirect stop-change signal: beta
    drops -> E3 model state propagates -> policy can change. Reframes MECH-057a: completion
    events are the principal policy-update opportunities within an otherwise beta-gated
    continuous E3 stream, not the exclusive E3 update trigger. Hyperdirect pathway
    (cortex->STN->GPi) provides fast interrupt for urgent stop-change without waiting for
    completion. Explains MECH-057a FAIL: experiment modelled binary gate (E3 blind until
    completion), not beta-gated propagation within continuous stream.

    TIMESCALE CROSS-REFERENCE (2026-08-07): the hyperdirect fast interrupt above acts
    MID-EXECUTION -- it re-opens an already-closed gate on a sequence that is already
    running. MECH-138 names a distinct, EARLIER veto point: a dFMC/pre-SMA top-down
    suppressive pathway that can cancel the transition BEFORE execution lock-in, while the
    commit token's cancel_window_open flag is still true. These are not the same gate at
    two speeds -- MECH-138 acts on the commit boundary itself (the transition zone between
    simulation-complete and execution-locked), whereas the hyperdirect pathway acts after
    lock-in. Together with MECH-141's slow proactive prefrontal-caudate pathway (which
    pre-sets gate bias before commitment) that gives three segregated timescales at which
    a committed trajectory can be stopped: proactive bias-setting (MECH-141), pre-lock-in
    veto (MECH-138), mid-execution interrupt (this claim). Surfaced 2026-08-07 while
    registering MECH-485; see section 3 of
    evidence/planning/thought_intake_2026-08-07_responsibility_counterfactual_memory.md

    SCOPE REFINEMENT (2026-04-08, from lit-pull): The output-only gating claim applies
    specifically to BG-level beta (STN/striatum). Cortical beta in premotor areas (SMA)
    also gates internal plan representation stability (Hosaka et al. 2016, Cerebral Cortex),
    meaning beta serves different gating functions at different circuit levels. Additionally,
    post-movement beta rebound carries internal model confidence information (Tan et al.
    2016, J Neurosci), so the BG gate is not a dumb switch but an information-carrying
    signal. The claim's specificity -- output gating without internal gating -- holds at
    the BG level but does not generalise to all beta-generating circuits.

    LAYER 3 RESOLUTION (2026-04-15, from lit-pull): MECH-090 BetaGate correctly models
    same-motor-domain suppression during commitment. A separate parallel habitual channel
    (Layer 3) is not warranted for single-action-dimension substrates -- within a shared
    effector pool, habitual and deliberative outputs compete winner-takes-all (MECH-234).
    Cross-domain parallelism (DMS/DLS to different effectors) is biologically valid but
    out of scope for CausalGridWorld. The urgency interrupt (Layer 2, implemented
    2026-04-15) is grounded in vmPFC arbitration weighting toward model-free (MECH-235),
    not a beta-bypass mechanism.

    V3-EXQ-592d (2026-05-31, 4-arm R-c readiness conjunction validation -- supersedes
    V3-EXQ-592c) FAIL_no_criterion reclassified non_contributory per
    failure_autopsy_V3-EXQ-592d_2026-06-01 (confirmed). Diagnostic, NOT governance
    evidence against MECH-090 R-c amendment. C1 baseline criterion has an
    instrumentation defect: the script measures commit entries via beta_gate
    mech090_n_elevation_admitted (a transition-edge counter), but under ARM_0 (rv-only
    baseline) the agent enters P2 already committed (P0 trained running_variance to
    ~1e-5 / 1e-6 / 1e-6 across seeds, well below commit_threshold=0.4) and stays
    continuously committed (hold_rate=1.0; beta_elevated_steps == committed_steps). No
    fresh entry transitions occur during the measurement window -> n_commit_entries=0
    unconditionally on ARM_0 regardless of gate state. C2/C3/C4 inherit the defect via
    cross-arm comparison against ARM_0 baseline. Substrate IS firing as predicted:
    ARM_1 2005 score-margin blocks confirm within-tick decisiveness axis (Hanes &
    Schall 1996 reading); ARM_2/3 150 nav-competence blocks each confirm across-tick
    motor-program readiness axis (Cisek & Kalaska 2010 + Roesch / Calu / Schoenbaum
    2007 readings); ARM_3 1949 score + 150 nav blocks confirm AND-composition at both
    elevate sites. The 2026-05-21 V3-EXQ-592 seed-42 degeneracy signature that
    motivated the R-c amendment (running_variance collapsed to ~1e-5 satisfying
    rv-only) is reproduced on ARM_0 across all 3 seeds -- positive substrate-side
    evidence that the rv-only entry semantic IS the degenerate basin R-c was designed
    to gate. R-c substrate (both 2026-05-28 within-tick + 2026-05-29 across-tick
    landings) sound; only the test needs redesign. Routing: /queue-experiment for
    V3-EXQ-592e fixing C1 baseline via force-uncommitted P2 entry OR hold-rate-based
    baseline predicate. pending_retest_after_substrate=false (no substrate gap).
    narrow_supports_flag=false. recommended_substrate_queue_entry.action=none
    (commitment_closure:GAP-4 stays drifted pending V3-EXQ-592e; closure-plan side,
    not substrate-queue side). Process flag: script's interpretation-grid
    C1 FAIL -> /diagnose-errors routing is wrong for this manifestation; the
    curriculum harness is working as designed.

    V3-EXQ-592e (2026-06-01, supersedes V3-EXQ-592d) FAILed after correcting the 592d
    commit-entry-count defect; applied verbatim per failure_autopsy_V3-EXQ-592e_
    2026-06-01 (confirmed, status=confirmed, routing=queue-experiment). C1 baseline
    hold-rate fired, and both readiness predicates were measurable: ARM_1 score-margin
    blocks=1793, ARM_3 score-margin blocks=2088, ARM_2/3 nav-competence blocks=150
    each. The failure is therefore not predicate silence. The informative result is
    that predicate firing did not produce discriminative committed-state occupancy
    under the script's hold-rate measure: all arms had hold_rate=1.0 and
    total_commits=0. Per-cell beta-elevation totals localise the issue further:
    score-margin gating can suppress BetaGate elevation when the latch is not
    elevated (ARM_1/3 beta-elevated fraction 0.0), but E3's rv-low
    committed-trajectory marker remains present because it is set upstream of
    BetaGate admission. Current code confirms the R-c predicates are wired to
    admission/elevation sites; no readiness-failure path releases an already-elevated
    bistable latch or clears E3 committed state. This is a direct integration/
    state-authority gap, not a clean conceptual falsification of MECH-090.
    evidence_direction=does_not_support (epistemic_category=
    does_not_support_state_authority). MECH-090 status unchanged (active). Routing:
    V3-EXQ-592f as the controlled state-machine probe of whether failed readiness can
    release/suppress an already-held committed state and recover when readiness
    returns. recommended_substrate_queue_entry.action=none.
    pending_retest_after_substrate=false. V3-EXQ-592d superseded.

    V3-EXQ-592f (2026-06-01, controlled state-machine probe, supersedes
    V3-EXQ-592e) FAILed FAIL_NO_RELEASE_AUTHORITY: with beta forced elevated and
    E3 committed pointer forced present, degraded score_margin (0.01) / nav_competence
    (0.0) produced zero decommit transitions across B/C/D and C4 PASSed only
    vacuously (0 >= max(0,0)). Per confirmed failure_autopsy_V3-EXQ-592f_2026-06-02
    this is a diagnostic reach-gap probe, NOT a falsification of MECH-090 (EXQ-049e
    remains the canonical PASS; the R-c admission axis V3-EXQ-592d stays live) -- it
    confirms the R-c conjunction is admission-only by design. 592f manifest re-tagged
    does_not_support -> non_contributory (epistemic_category substrate_ceiling).
    RESOLVED 2026-06-02: the admission-only reach gap is now covered by the NEW
    MECH-342 maintenance-time, readiness-driven commitment-release coupling (landed
    via /implement-substrate; concluded by mech090_release_path_audit_2026-06-02
    verdict B3b + the motor-program-cessation lit-pull), VALIDATED by V3-EXQ-592g
    PASS (2/stage decommit transitions in B/C/D, suppression 0.4-0.6, mech342_fires
    1/stage, no false abort in A/E). MECH-090 status unchanged (active); the
    maintenance-release capability lives on the dependent MECH-342 claim, not here --
    no MECH-090 flag / confidence / promotion change. 592f pending_retest_after_substrate
    cleared to false (the reach-gap retest happened and passed).
- id: MECH-091
  title: "Salient events phase-reset the E3 heartbeat clock."
  claim_type: mechanism_hypothesis
  subject: control_plane.salient_event_cycle_resync
  polarity: asserts
  status: candidate
  epistemic_category: standard
  live_status:
    reading: candidate
    as_of: 2026-08-22
    needs_review: false
    evidence:
      from: "governance-cycle-2026-08-22 (confirmed failure_autopsy_V3-EXQ-944_2026-08-22)"
      as_of: 2026-08-22
      verdict: hold_candidate_resolve_conflict/applied
  implementation_phase: v3
  evidence_quality_note: |
    [2026-08-22 governance, cycle bold-chaum-7e245c, confirmed failure_autopsy_V3-EXQ-944_2026-08-22.json]:
    V3-EXQ-944 did NOT weigh for or against MECH-091 -- evidence_direction non_contributory
    (already inert on both the flat manifest and the run pack; this cycle added the
    evidence_direction_note so the annotation is auditable). epistemic_category set to
    standard: MECH-091 carried NO category field at all, which the 2026-08-16 GFLAG-0037
    routing left unstamped. Status STAYS candidate; pending_retest_after_substrate stays
    false. NOT a falsification and NOT a substrate-absence result -- all four acceptance
    criteria PASSED and were non-degenerate. The run was voided by readiness precondition P3
    (rate match between RATE_MATCHED and ALIGNED, max over seeds <= 0.35), which read 0.914 on
    seed 42. CAUSE: the RATE_MATCHED control degenerated into NO_RESET on that seed -- 4 of 236
    re-issues executed, and its episode_lengths are element-wise identical to NO_RESET for the
    first 21 of 30 episodes. The collapse is TREATMENT-INDUCED, not a hostile seed: the shared
    arm-independent warmup sustains a 24.37-step mean on seed 42 and ALIGNED holds 22.53, while
    RATE_MATCHED falls to 8.50 and NO_RESET to 7.43. The mechanism is a runaway feedback --
    withhold the event-aligned reset, episodes shorten, deferred re-issues (delay U{K..2K},
    K=10) can no longer fire before the episode ends, so still fewer resets execute. The
    existing clamp does not arrest it and changes NO outcome: due=min(due,72) while drain() runs
    before each of range(72) steps, so _global_step never exceeds 71 and a clamped due of 72 can
    never fire. THREE GUARDS ARE MIS-SPECIFIED and must be fixed in the successor: (i) P1 floors
    n_salient_events (measured 89) but the DV is denominated on n_events_with_following_tick,
    which fell to 10 on NO_RESET/seed42 -- below P1's own floor of 20 -- while P1 still reported
    met=true; (ii) P3 is a ticks/step RATIO and is partially self-cancelling, since RATE_MATCHED
    depresses numerator and denominator together (seed 100 passed at 8.0% while executing only
    93/186 of its re-issues); (iii) P2 measures a near-constant -- straddle_frac(NO_RESET) is
    exactly 1.0 in 5 of 5 cells. Routing: /queue-experiment successor per the autopsy's
    successor_spec. This is an instrument/test-design reading, not evidence against the claim,
    and the claim keeps its v3-testability.

    Held (2026-03-28): specific blocker is SD-006 (async multi-rate loop execution).
    Phase-reset of E3 heartbeat requires SD-006 phase 2 async implementation first.
    EXQ-133 reclassified non_contributory (2026-04-09): test measured latent divergence
    after simulated salient events as a proxy for phase reset. Without SD-006 phase 2 async
    execution there is no oscillatory clock phase to reset -- the mechanism under test is
    absent, not failing. Manifest evidence_direction corrected to non_contributory.
    EXQ-133 2026-04-21 rerun also reclassified non_contributory (governance 2026-04-22):
    same SD-006 substrate gap; gap_on=0.014 < 0.04 threshold and per-seed delta
    [0.0198, 0.0094] < 0.02 threshold. Manipulation fired (n_resets=5656) but no
    oscillatory clock to phase-reset. Hold pending SD-006 phase 2.

    [2026-08-16 governance, GFLAG-0037 SPLIT decision, cycle cranky-driscoll-126a36] ROUTED TO V3 AS A SMALL BUILDABLE; the SD-006-phase-2 deferral that had parked this claim since 2026-05-08 is LIFTED as mis-scoped. The deferral rationale -- that there is no oscillatory clock phase to reset because the mechanism under test is absent -- is contradicted by direct measurement: phase_reset() is BUILT (ree_core/heartbeat/clock.py:182), WIRED (agent.py:9903), and was measured 2026-08-01 as the DOMINANT driver of E3 tick cadence in a real 53,063-step rollout (diagnostic_arc071_e3_reselection_probe_2026-08-01.md); nobody connected the two for four months. The real gap is small: of the three salient events this claim names (task completion, unexpected harm, commitment-boundary crossing) only harm_signal < 0 calls phase_reset() -- the other two are unwired, and both events already exist in the substrate (hippocampal completion drives BetaGate release; commitment entry/exit IS the MECH-090 beta-gate boundary). Registered as substrate_queue MECH091-SALIENT-EVENT-TRIGGER-WIRING; owning node commitment_closure:GAP-7 moved deferred -> blocked, so this claim re-enters the V3 denominator instead of being invisible to both it and the substrate queue. NOTE ON PRIOR EVIDENCE: both EXQ-133 runs (2026-03-28 and the 2026-04-21 rerun) were reclassified non_contributory for this substrate gap, twice and explicitly NOT as falsifications -- and EXQ-133 was in any case not the experiment this claim asks for, having measured latent divergence as a PROXY rather than the cycle-boundary DV named in what_would_answer. Once the wiring lands, the no-reset control this claim specifies is constructible on phase 1 today. ONE CHEAP PUZZLE-GRADE CHECK IS OWED FIRST (carried on the substrate entry, not on this claim): confirm the what_would_answer DV -- no partial-integration artefacts straddling a salient event -- is observable on phase 1 and not itself absorbed by the EXQ-131 E3-output-freeze staleness artifact. That is the honest counter-argument the brief states against its own recommendation. No status change on this basis: this is a ROUTING decision, not new evidence. Full reasoning: evidence/planning/sd006_phase2_generation_brief.md.
  what_would_answer: |
    SUBSTRATE PRECONDITION: requires SD-006 phase 2 (async multi-rate loop
    execution) to be built -- without an actual oscillatory/cycle-boundary
    clock, there is no mechanism under test. Both EXQ-133 runs (2026-03-28,
    2026-04-21 rerun) confirmed this: the manipulation fired (n_resets=5656)
    but gap_on/per-seed deltas stayed below threshold because no real clock
    existed to reset -- reclassified non_contributory, NOT a falsification,
    both times. A run without SD-006 phase 2 self-routes substrate_not_ready.
    CONFIRMING signature: once SD-006 phase 2 lands, salient events
    (completion, unexpected harm, commitment-boundary crossing) measurably
    reset E3's cycle boundary such that harm/goal estimates from the
    salient event are FULLY integrated within the following cycle before
    the next planning window opens -- i.e. no partial-integration
    artefacts straddling a salient event, compared to a no-reset control
    where partial integration measurably occurs.
    FALSIFYING: once the real substrate exists, salient events produce no
    measurable change in cycle-boundary timing (phase-reset manipulation
    has no effect beyond noise) -- would falsify the phase-reset mechanism
    itself, as distinct from the already-ruled-out "no clock exists"
    explanation for the two prior null results.
  claim_level: mechanistic
  functional_restatement: >
    Salient events (completion, unexpected harm, commitment boundary) resynchronize E3's
    update window to start fresh rather than continuing mid-cycle. This ensures harm
    estimates from one sub-plan are fully integrated before the next planning window opens,
    preventing partial integration artefacts. In an ANN substrate, implemented as an
    explicit cycle-boundary marker triggered by salient events — not as oscillatory phase
    reset. The biological mechanism (thalamic-driven phase reset of theta/alpha oscillations,
    P300 substrate) is one implementation; it is not required in the ANN substrate.
  depends_on:
    - ARC-023
    - MECH-090
  location: docs/architecture/control_plane_heartbeat.md#mech-091
  source:
    - docs/architecture/control_plane_heartbeat.md
  notes: >
    High-salience events (action completion, unexpected harm, commitment boundary crossing)
    phase-reset the E3 heartbeat oscillator — reorganise timing of subsequent cycles, not
    only boost signal amplitude. Phase reset ensures updated harm estimates from the salient
    event enter E3 at the start of a fresh cycle (no mid-cycle integration artefacts).
    In multi-step plans (W0→W1→W2) each waypoint completion phase-resets E3's clock,
    ensuring harm estimates from each sub-plan are fully integrated before the next begins.
    Neural basis: P300 event-related potential and its theta/alpha oscillatory substrate.
- id: MECH-092
  title: "Quiescent E3 heartbeat cycles trigger hippocampal SWR-equivalent replay for viability map consolidation."
  claim_type: mechanism_hypothesis
  subject: hippocampal.quiescent_offline_replay
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-15
    needs_review: false
    evidence:
      from: "decision:MECH-092@2026-07-15T13:44:33.301255Z"
      as_of: 2026-07-15
      verdict: promote_to_provisional/applied
  implementation_phase: v3
  evidence_quality_note: |
    Held (2026-03-28): specific blocker is SD-006 (async multi-rate loop execution).
    Quiescent heartbeat replay requires SD-006 phase 2 async loop before testing.
    EXQ-136 reclassified non_contributory (2026-04-09): consolidation via quiescent SWR
    replay requires SD-006 async loop, working viability map, and hypothesis_tag implementation
    -- none present. Proxy measured navigation delta with/without replay; cannot detect
    consolidation of a map that does not exist. Manifest evidence_direction corrected to
    non_contributory.
    2026-07-15 (gov): promoted candidate->provisional on V3-EXQ-761 (quiescent-replay
    selectivity, PASS/supports) -- the FIRST genuine triggering-signature evidence: replay
    fires selectively on quiescent E3 cycles via the real production path
    (act_with_split_obs -> _do_replay -> hippocampal.replay) on an untrained agent. The
    2026-03-29 hold_candidate_resolve_conflict was substrate-driven (EXQ-136 could not test
    downstream consolidation-benefit -- no SD-006 async loop / no viability map), NOT a
    genuine support-vs-weaken conflict; 761 confirms the TRIGGERING half. Promote-to-active
    still gated on the downstream consolidation-benefit test (SD-006 / working viability map).
  claim_level: mixed
  functional_restatement: >
    During quiescent periods between active E3 updates, the hippocampal module performs
    offline replay of compressed recent trajectory experience to consolidate the viability
    map. This offline batch consolidation supplements online map updates and integrates
    experiences that occurred too fast for real-time E3 processing. This is standard
    experience replay — implementable in any substrate. The SWR mechanism (internally
    generated CA3 recurrent dynamics) is the biological substrate; the functional
    requirement is offline batch consolidation during idle periods, not the oscillatory
    mechanism.
  depends_on:
    - ARC-023
    - ARC-014
    - ARC-018
    - ARC-022
    - MECH-094
    - MECH-289   # anti-recency bias: quiescent replay is generative over all paths, not recency-weighted
    - MECH-291   # mode differentiation: quiescent mode uses distinct CEM parameters from waking mode
  location: docs/architecture/control_plane_heartbeat.md#mech-092
  source:
    - docs/architecture/control_plane_heartbeat.md
  notes: >
    During quiescent E3 heartbeat cycles (no new completion event or salient update),
    hippocampal module performs SWR-equivalent replay to consolidate the viability map.
    Replay is NOT standard experience replay (Gupta et al. 2010, Neuron 65:695): SWR
    sequences represent ALL physically available trajectories, generate novel-path routes
    never behaviorally executed, and show anti-recency weighting (infrequent paths
    replayed more). The generative sweep is biased toward under-sampled routes, actively
    filling viability map gaps rather than reinforcing recent experience. This is the
    biological basis for CEM-style stochastic trajectory sampling in REE. Awake
    immobility pauses also trigger replay (Carr et al. 2011, Nat Neurosci 14:147) not
    just sleep/deep-quiescence; both waking pauses and quiescent heartbeat windows are
    valid MECH-092 triggers operating via the same SWR substrate. V2 ARC-018 FAIL is
    partly explained by absence of any replay mechanism -- map updates only at each
    discrete step, no quiescent consolidation phase. ARC-018 may be testable in V3 via
    explicitly modelled quiescent replay phases. Connects to sleep consolidation docs
    (docs/architecture/sleep/) as the within-session equivalent of sleep replay. Safe
    because replay carries the hypothesis tag (MECH-094) throughout: viability map
    consolidation updates the cognitive map of phi(z) without writing to phi(z) itself.
    This is the micro-quiescence analogue of ARC-014 (DMN): same replay machinery,
    operating at inter-action timescales rather than full task-disengagement timescales.
    MECH-291 specifies the distinct parameter profile required for quiescent mode vs
    waking mode (longer rollout horizon, exploration weighting, less context-anchored);
    implementing this requires SD-006 (async multi-rate loop execution).
- id: MECH-093
  title: "z_beta modulates E3 heartbeat frequency, distinct from precision-weighting."
  claim_type: mechanism_hypothesis
  subject: affective.z_beta_e3_update_rate_modulation
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_EXQ-539-540_MECH307_2026-05-17#V3-EXQ-540f"
      as_of: 2026-05-17
      verdict: superseded/measurement_design_broken_criterion
  implementation_phase: v3
  claim_level: functional
  depends_on:
    - ARC-023
    - MECH-069
    - MECH-059
  location: docs/architecture/control_plane_heartbeat.md#mech-093
  source:
    - docs/architecture/control_plane_heartbeat.md
  notes: >
    z_beta (affective latent) modulates E3 heartbeat frequency: high harm salience →
    faster E3 updates (finer temporal harm attribution); routine operation → slower
    E3 updates (more stable policy). Distinct from precision-weighting (MECH-059):
    precision-weighting scales how much each update matters; rate modulation scales
    how often updates occur. Corresponds to NA arousal axis in four-plane hierarchy
    (MECH-087): NA modulates perceptual sampling (MECH-084) and, via z_beta, E3
    heartbeat rate. Predicted: sustained high harm salience → more reactive policy;
    routine conditions → increased policy stability.
    Scope clarification: MECH-093 governs E3 heartbeat frequency (arousal-modulated
    thalamic pacemaking) — it does NOT govern the respiratory plan-sweep clock (MECH-108;
    corrected 2026-08-08 thought-digestion currency check -- this note previously
    misnamed it MECH-112, which is an unrelated claim, drive.goal_state_attractor).
    Heartbeat = arousal-level gating of E3 refresh rate; respiratory rhythm = periodic
    sweep of pre-commit hippocampal rollouts and DMN simulations for trajectory
    abandonment. These are distinct clocks at distinct timescales operating on distinct
    planning functions.
  evidence_quality_note: |
    EXQ-097 FAIL 2/3 (2026-03-26): C1 FAIL -- p1_rate_gap=-0.74 (threshold >= 2.0).
    E3 heartbeat rate did NOT differentiate high-harm from low-harm episodes; gap is
    negative (higher harm -> slightly lower rate) -- opposite of prediction.
    DIAGNOSTIC (2026-03-27): z_beta IS wired to clock rate. update_e3_rate_from_beta()
    is called every step in agent._e1_step() (agent.py:272). Implementation gap does
    NOT exist. Root causes: (1) z_beta does not encode harm salience sufficiently after
    200 warmup episodes -- magnitude is not elevated during harm events. Pool skewed
    16:1 (high-harm vs low-harm ticks), confirming harm is pervasive so z_beta has
    minimal between-episode variance. (2) Episode-level averaging is too coarse: the
    correct operationalization is step-level Pearson r(|z_beta|, 1/e3_steps_per_tick),
    not the mean clock rate gap between episode types. EXQ-097b redesigned with
    step-level metric + 500 warmup episodes. Hold candidate.
    EXQ-097b PASS 2/3 (2026-03-27): C1 r=0.964 (n=29937 steps) -- step-level z_beta/rate
    correlation confirmed (core claim). C3 action_var_ratio=0.096 -- 10x lower action
    variance in safe phases when BETA_MOD_ON. C2 harm_rate FAIL (ratio=1.58, higher in
    BETA_MOD_ON): state selection artifact -- high-z_beta phases are high-harm contexts
    by construction. Agent encounters more harm because it IS in high-harm situations, not
    because slowing causes harm. PASS = C1 AND C3. MECH-093 supported. Promote to
    provisional pending multi-seed replication.
    EXQ-396b (2026-04-16, PASS): First cross-experiment replication. Pearson
    r(hazard_harm, var_diff)=0.891 across hazard_harm sweep (C1+C2 PASS). Confirms
    hazard_harm independently scales variance regardless of ARC-016 calibration
    issue. ARC-016 non_contributory in same run; MECH-093 retained as supports.
    EXQ-505 PASS (2026-05-03): Substrate-level 2x2 factorial isolating MECH-093's
    z_beta -> e3_steps_per_tick channel from any precision/salience confound.
    Drives MultiRateClock directly via update_e3_rate_from_beta and mark_salient.
    Resolves the EXQ-097 conflict by separating substrate-channel function (this
    test) from in-env z_beta encoding (the EXQ-097 issue). Cleanest possible PASS
    for the rate-modulation channel claim. Promoted candidate -> provisional
    2026-05-03 on three PASS pillars (097b step-level + 396b cross-experiment +
    505 substrate-level dissociation).
# Registered 2026-03-17 — SD-005 anatomical gap fill: TPJ, dual-stream encoder, peripersonal space
# Source: neuroanatomical review of self/world latent split missing pieces
- id: MECH-095
  evidence_quality_note: |
    [multi_sense_audit_2026_05_08_cleared] EXQ-047i/j/k: audit re-examined the multi-sense flag on the 047 cluster and confirmed it is a deliberate probe pattern (sense E_t -> step -> sense E_{t+1} -> step), giving exactly 1 sense per env state. Not the EXQ-471 cohort bug. Direction and confidence unchanged.

    EXQ-121 FAIL (2026-03-28): AUC_COMP_ON=0.411 vs AUC_COMP_ABLATED=0.745 (delta=-0.334).
    Only 1/5 criteria passed. AgencyComparator actively hurts attribution performance at current
    training scale. Possible causes: z_self (32-dim) too noisy for reliable efference-copy
    prediction; insufficient action embedding granularity; env_drift_prob=0.3 may be too low to
    produce enough discriminable other-caused events. Holds at active status -- prior neuroanatomical
    and literature support remains valid. Redesign flagged: larger z_self, improved efference-copy
    embedding, or higher env_drift_prob needed before next experimental test.

    EXQ-047k PASS 5/5 (2026-03-25, 4 seeds): contact_recall_routed=0.796 > 0.55,
    recall_improvement=+0.065 > 0.04, action_dissoc=-0.007 > -0.05, n_contact_min=164.
    4-seed replication with clean discriminative test. Promoted from provisional to active.
    Combined with lit_conf=0.907 (Blakemore 2002, Gu 2008, Pitcher & Ungerleider 2021,
    Rolls 2023), evidence for TPJ as agency-detection comparator is strong.

    SUBSTRATE-CEILING FLAG (2026-05-02, Phase 3 cutover queue walk):
    Phase 3 production gates (exp_conf-driven) recommended demote_to_candidate based on
    9 scored exp entries / 7 supports / 4 weakens / 5 mixed (conflict_ratio=0.727,
    exp_conf=0.527). The conflict is genuine in the matrix but the architectural reading
    is substrate ceiling, not claim error: of 7 successor attempts after the EXQ-047k PASS
    (EXQ-089, 047i, 047j, 098b x2, 121 x2), all weakens or mixed. Each tested a different
    operationalisation; each got a different fragment-result. Agency-detection requires
    distinguishing self-caused from other-caused change against a textured causal background;
    CausalGridWorldV2 supplies hazard drift (env_drift_interval=7) + resource respawn +
    optional scheduled_external_hazard injection -- a thin world. EXQ-047k's PASS likely
    caught a narrow dynamic regime where the agent-vs-env signal was momentarily clear.
    Demote misattributes the issue. Held at active pending one of: (a) substrate enrichment
    (multi-source environmental dynamics), (b) clean conflict-resolution experiment that
    isolates the EXQ-047k condition (larger-N stratification) as the substrate-condition-of-
    success, (c) deferral to V4 multi-channel/multi-agent territory. See MECH-102 for the
    cluster diagnosis and the substrate_ceiling category if it is later formalised.

    GOV-CEIL-1 FLOORING (2026-07-11, decision:MECH-095@2026-07-11T12:37:35Z): status
    demoted active -> candidate. This resolves the sole SHP-4 live_status internal
    inconsistency (promoted status carrying epistemic_category: substrate_ceiling).
    Adjudication (b), NOT (a): the substrate_ceiling tag is CORRECT and CURRENT, not
    stale -- the 2026-05-02 diagnosis holds (7 successor attempts all weakens/mixed;
    thin-world reading) and the ceiling is MAPPED to a design owner (SD-047 multi-source
    environmental dynamics = resolution path (a) above). The 2026-03-25 promotion to
    active was legitimate AT THE TIME (EXQ-047k PASS 5/5 + lit_conf=0.907) but predates
    both the ceiling diagnosis (2026-05-02) and GOV-CEIL-1 (2026-07-09). GOV-CEIL-1
    floors ceilings to candidate: a substrate_ceiling claim held at promoted standing is
    exactly the "hypothesis never loses" asymmetry the rule closes (the conflict that
    would otherwise demote it was being discounted as substrate, not claim error). So
    the active status was a pre-GOV-CEIL-1 residue; candidate is the consistent reading.
    NOT a GOV-CEIL-1 EXHAUSTION demotion: n_ceiling_hits=0 (no confirmed
    failure_autopsy_*.json recommending substrate_ceiling), so epistemic_category STAYS
    substrate_ceiling (NOT flipped to standard) and the SD-047 enrichment mapping is
    preserved -- this is a flooring, not the N>=3 co-equal-null exhaustion.
    RETEST OWED: SD-047 landed (status: implemented) but MECH-095 carried no
    pending_retest_after_substrate flag, so the audit filed it under `mapped` and the
    owed retest on the enriched substrate has NEVER run -> still ZERO positive
    discrimination on the richer substrate (EXQ-047k's PASS was on the thin
    CausalGridWorldV2, pre-SD-047). pending_retest_after_substrate now set true so the
    substrate-ceiling audit reclassifies MECH-095 as ceiling_may_have_lifted (ACTIONABLE:
    queue the agency-detection retest on the SD-047 multi-source substrate; a PASS there
    is the positive discrimination that would re-gate promotion).

    047l ADDENDUM (2026-07-11, governance consumption of failure_autopsy_V3-EXQ-047l_2026-07-11,
    confirmed; session fervent-lamport-cacb46): the ceiling_may_have_lifted retest was run as
    V3-EXQ-047l (SD-047 multi-source env, 4 seeds, ree-cloud-1) and FAILed 3/5 -- but the verdict
    is NON_CONTRIBUTORY / measurement_degeneracy, NOT a substrate_ceiling confirmation. The probe's
    contact-vs-no-contact partition SATURATED: is_contact folded env_events>0, which on SD-047's
    dense per-step drift/transient dynamics was true on every probe step, so the no-contact negative
    class was empty (n_contact_probe == n_probe_steps in all 8 cells: 126==126 / 119==119 / 113==113
    / 105==105) and contact_recall_world was structurally pinned at 0.0 in BOTH ROUTED and BASELINE.
    The agency comparator was NEVER exercised -- the owed SD-047 positive-discrimination retest STILL
    has not validly run, so n_ceiling_hits STAYS 0 (an instrument failure, not a ceiling hit). The
    env-events fold is correct/necessary in the TRAINING is_world routing label but was over-applied
    into the EVAL partition. Secondary, claim-orthogonal non-degenerate signal: routing collapsed the
    self-vs-world action dissociation (BASELINE +0.10..+0.23 -> ROUTED ~0). No substrate build owed
    (autopsy recommended_substrate_queue_entry.action=none -- SD-047 is already implemented and
    correctly mapped as resolution path (a); this is a measurement bug, not a substrate gap). Superseded
    by V3-EXQ-047m (keeps the env-events fold in the training routing label; reverts probe is_contact to
    047k's non-folded CONTACT_SET; adds a probe-partition non-degeneracy guard PROBE_NEG_FLOOR=5 blocking).
    MECH-095 STAYS candidate / substrate_ceiling / pending_retest_after_substrate; this consumption
    PROMOTES/DEMOTES NOTHING.
    RE-DERIVE-BRAKE ESCALATION FLAG: 047l is the 1st MECH-095 non_contributory autopsy (brake threshold
    2, NOT fired -- prior_count 0). Per the autopsy: a SECOND non_contributory -- i.e. if the corrected
    successor V3-EXQ-047m ALSO returns non_contributory -- implicates the OPERATIONALISATION (not one
    probe bug) and MUST route MECH-095 to a substrate/test-bed redesign, NOT a third 047 letter.

    V3-EXQ-047m (2026-07-11, SD-047 @ intensity 1.0, 4 seeds, ree-cloud-2) is the CORRECTED successor to 047l. Its probe-partition fix worked (non_degenerate=true, n_no_contact_min=8 >= floor 5) so the EVAL contrast was real, and it FAILed 3/5 with routing HURTING recall (ROUTED 0.492 vs BASELINE 0.795, improvement -0.302). But this is NON_CONTRIBUTORY -- a SECOND operationalisation degeneracy, NOT a substrate_ceiling confirmation and NOT a weakens. Root cause: the identical env_events fold that saturated 047l's PROBE now saturates the TRAINING label. is_world = 1 if (prev_ttype in WORLD_CAUSED or env_events>0), and at intensity 1.0 the 2 background-drift sources fire env_events>0 on ~93%+ of steps (env_ev_ticks ~4470 of ~4800; episodes die ~11 steps), so is_world ~= constant 1 -- the routing head never sees a self-caused negative and cannot learn the discrimination; instead it homogenizes z_world, degrading an already-good BASELINE contact encoding (0.795) and collapsing action-dissociation (BASELINE +0.140 -> ROUTED -0.002; seed 123 catastrophic: world 0.111 / self 1.000). The 047m guard validated only the PROBE partition, not the training-label balance, so non_degenerate=true is a FALSE CLEAR of validity. Three independent reasons this is not a valid ceiling hit: (1) comparator invalidly trained (saturated label); (2) 047m is a binary comparator-ON-vs-ABLATED at fixed intensity 1.0, NOT the pre-registered 4-arm SD-047 intensity sweep (sd_047 doc warns a binary test cannot distinguish 'SD-047 works' from 'SD-047 overshot calibration' -- the Asai overshoot branch is exactly the label saturation and is unexcluded); (3) the BASELINE succeeds at 0.795, so the substrate carries the contact signal -- the failure is corruption of a good representation, not an absent one. n_ceiling_hits STAYS 0. RE-DERIVE BRAKE FIRES (2nd non_contributory MECH-095 read, threshold 2): route to a test-bed redesign, REFUSE a third 047 letter. The convergent 047l(probe)+047m(training) shape implicates the OPERATIONALISATION (the env_events fold is degenerate at intensity 1.0 wherever applied), not the substrate. Load-bearing redesign input: MECH-095's notes describe a query-time read-out comparator (efference-copy-predicted vs observed z_self), but the 047-lineage operationalises it as an auxiliary BCE head reshaping z_world by gradient -- plausibly the wrong functional translation. Substrate_queue: CREATE an agency-comparator test-bed entry (non-saturating training label / read-out reoperationalisation + a training-label non-degeneracy guard + the pre-registered 4-arm intensity sweep). pending_retest_after_substrate stays true but RE-POINTED from 'SD-047 build' (done) to that test-bed; ceiling audit moves MECH-095 off 'ceiling_may_have_lifted' to 'ceiling-retest-blocked-on-testbed'. MECH-095 stays candidate / substrate_ceiling / pending_retest_after_substrate; this consumption PROMOTES/DEMOTES NOTHING.

    V3-EXQ-741 (2026-07-12, /governance IGW-20260712-001 inline autopsy failure_autopsy_V3-EXQ-741, confirmed): the agency_comparator_testbed_sd047 test-bed the 047m brake demanded ran VALIDLY for the first time -- non_degenerate=True, all 4 intensity arms (0/0.25x/1.0x/4.0x) pass BOTH the probe-partition and self/world-balance guards the 047l/047m lineage lacked. The 047l/047m measurement degeneracy is FIXED. On the valid test-bed the comparator does no functional work: no arm discriminates (best routing improvement +0.028 < 0.04 floor), ROUTED_A gradient-head HURTS recall (mean -0.114), ROUTED_B efference-copy read-out neutral (+0.003), baseline already carries contact recall (0.75-0.93). evidence_direction NON_CONTRIBUTORY (NOT the script's self-routed 'weakens'): SD-047's world-caused drift is not a structurally-distinct OTHER, so the env cannot make the comparator load-bearing and the baseline pre-empts it -- the test could not let the claim express its value. This EMPIRICALLY CONFIRMS multi_agent_ecology_v5:MAE-3's standing prediction (MECH-095 is intrinsically relational; needs a genuine second agent). This is the 1st VALID substrate_ceiling hit (n_ceiling_hits 0 -> 1; 047l/047m were measurement-degeneracy, uncounted). RE-DERIVE BRAKE FIRED (3rd non_contributory) but its test-bed-rebuild demand was honored -- it does NOT re-block; it RE-POINTS the ceiling from the now-built+validly-exercised test-bed to the multi-agent ecology (MAE-1 -> MAE-2 -> MAE-3) and REFUSES a 4th single-agent SD-047 letter. Secondary learning: the read-out framing (ROUTED_B) is the biology-faithful translation; the gradient-BCE-head (ROUTED_A) is not. Positive base remains NARROW (only 047k, a single thin-env operationalisation) -- flagged per the illusory-conflict-resolution rule. implementation_phase REASSIGNED v3 -> v5 (user-confirmed, phase-follows-dependency: the multi-agent ecology is the enabling substrate; MECH-095 removed from V3-completion accounting). pending_retest_after_substrate stays true, re-pointed to multi_agent_ecology_v5:MAE-3. MECH-095 stays candidate / substrate_ceiling / pending_retest_after_substrate; PROMOTES/DEMOTES NOTHING.
  title: "Temporoparietal junction (TPJ) acts as agency-detection comparator distinguishing self-caused from other-caused change."
  claim_type: mechanism_hypothesis
  subject: tpj.agency_detection_comparator
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_ceiling
    as_of: 2026-07-20
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-741_2026-07-12
      as_of: 2026-07-12
      verdict: non_contributory/substrate_ceiling
  last_reviewed: 2026-07-12
  epistemic_category: substrate_ceiling
  pending_retest_after_substrate: true
  ceiling_retest_binding_substrate: ["multi_agent_ecology_v5:MAE-3"]
  ceiling_retest_binding_note: "Re-pointed 2026-07-12 (governance IGW-20260712-001 consumption of failure_autopsy_V3-EXQ-741, confirmed): the agency_comparator_testbed_sd047 test-bed ran VALIDLY (non_degenerate:True, all 4 arms pass guards) and the comparator does no functional work (no arm discriminates, best +0.028; baseline already carries contact). This is the 1st VALID substrate_ceiling hit and it EMPIRICALLY CONFIRMS multi_agent_ecology_v5:MAE-3's standing prediction: a single-agent V3 env cannot make an agency comparator load-bearing because world-caused drift is not a structurally-distinct OTHER. The owed positive-discrimination retest is now BLOCKED on the multi-agent ecology (MAE-1 -> MAE-2 -> MAE-3), NOT on any single-agent SD-047 test-bed. Re-derive brake refuses a 4th single-agent SD-047 letter. PRIOR (superseded) re-point 2026-07-11 (047m): had pointed at agency_comparator_testbed_sd047 -- that test-bed is now built + validly exercised, so it is retired as the binding substrate."
  ceiling_decision: deferred  # 2026-08-10 governance, GOV-CEIL-1 ceiling-exhausted audit (11 mechanical hits): the standard field mirroring the existing ceiling_retest_binding_substrate/_note above -- the audit script checks THIS field name for the Parked exemption, which the bespoke pair above (predating the standardized fields) did not trigger. Not genuinely exhausted; already re-pointed to a specific not-yet-built substrate (multi_agent_ecology_v5:MAE-3), same reasoning as ceiling_retest_binding_note.
  ceiling_routing_note: "See ceiling_retest_binding_note above -- re-pointed to multi_agent_ecology_v5:MAE-3, not exhausted. Deferred, not demoted."
  adjudicated_at_utc: "2026-07-11T12:37:35Z"
  adjudication_outcome: demoted
  implementation_phase: v5
  phase_locked: true  # intrinsic V5 scope, twice deliberately confirmed by governance:
                      # 2026-07-12 (V3-EXQ-741, the 1st VALID substrate_ceiling hit --
                      # "world-caused drift is not a structurally-distinct OTHER", retest
                      # bound to multi_agent_ecology_v5:MAE-3) and 2026-08-10
                      # (ceiling_decision: deferred, GOV-CEIL-1 audit: "Not genuinely
                      # exhausted; already re-pointed to a specific not-yet-built
                      # substrate"). MECH-098/MECH-221/SD-047's depends_on: MECH-095
                      # edges (all thematic/reversed, not build prerequisites) were the
                      # mechanical source of a v5->v3 pull; removed same session per chip
                      # chip-20260810-phase-consistency-reconciliation.
  depends_on:
    - SD-005
    - MECH-069
    - ARC-021
  location: docs/thoughts/2026-03-14_self_world_latent_split_sd003_limitation.md
  source:
    - docs/thoughts/2026-03-14_self_world_latent_split_sd003_limitation.md
  notes: >
    At the z_self/z_world interface, an explicit agency-detection comparator
    (TPJ-equivalent) must compare efference-copy-predicted z_self change with
    observed z_self change. When predicted and observed z_self changes match,
    state change is self-caused (no residue). When they diverge, cause is
    attributed to z_world (potential residue). This is the architectural mechanism
    for SD-003 counterfactual attribution: self_delta and world_delta are only
    cleanly separable if this comparator exists. Without it, SD-003's causal_delta
    conflates proprioceptive self-effects with world-directed footprint.
    Neural basis: temporoparietal junction (TPJ) computes forward-model prediction
    vs sensory outcome mismatch for agency attribution. Blakemore et al. (2002).
    Additional grounding (2026-03-17): TPJ sits at the terminus of the lateral/third
    visual stream (MT→MST/FST→posterior STS→TPJ), which is specialised for dynamic
    motion and biological motion detection (Haak & Beckmann 2018; Pitcher &
    Ungerleider 2021). MSTd congruent/incongruent neurons (Gu et al. 2008) provide
    the upstream causal inference that feeds this stream: congruent=reafference,
    incongruent=exafference/world-change. The efference copy reaches parietal via
    SLF (Rolls et al. 2023). See MECH-098 and MECH-099.
    Literature: evidence/literature/targeted_review_reafference_streams/.
  heterogeneity_note: "Substrate-version confound: the EXQ-121 FAIL reflects insufficient z_self dimensionality (32-dim) and training scale for the AgencyComparator to outperform ablation; the mechanism is validated by EXQ-047k PASS (4-seed) and strong literature anchor (TPJ comparator); held at active/substrate_ceiling pending z_self scaling."
  what_would_answer: |
    NON-DEGENERACY PRECONDITION (HARD): a test of MECH-095 is informative only on a
    substrate that supplies a genuine second agent -- a structurally-distinct OTHER
    whose actions are causally attributable separately from ambient world drift. This
    is not a design preference; it is an empirically confirmed requirement. V3-EXQ-741
    (2026-07-12) ran the SD-047 multi-source-dynamics comparator test-bed VALIDLY for
    the first time (probe-partition guard + self/world training-label-balance guard
    both passed, non_degenerate=true) and found the comparator does no functional work
    in that single-agent environment (best arm recall_improvement +0.028, below the
    0.04 pre-registered floor; ROUTED_A gradient-head HURTS recall -0.114; baseline
    already carries contact recall 0.75-0.93). This is the FIRST valid substrate_ceiling
    hit for MECH-095 (n_ceiling_hits 0->1) and is read as confirming that world-caused
    drift is not a structurally-distinct OTHER, so no single-agent env can make the
    comparator load-bearing. The retest is bound to multi_agent_ecology_v5:MAE-3 (as of
    2026-08-06 blocked on MAE-2, per IGW-20260806-139) -- a run on any single-agent
    substrate, including a further SD-047 letter, is explicitly REFUSED by the
    re-derive brake and should self-route substrate_not_ready, not count as a verdict.
    Two prior retests must also NOT be read as verdicts: V3-EXQ-047l (probe-partition
    saturation -- no-contact negative class was empty, contact_recall pinned at 0.0 in
    both arms) and V3-EXQ-047m (training-label saturation -- is_world ~constant 1 at
    intensity 1.0) were both instrumentation/measurement degeneracies, not valid tests
    of the comparator; only a run whose guards mirror V3-EXQ-741's (probe-partition
    non-degeneracy AND self/world training-label balance, both reported) counts.
    CONFIRMING signature: on multi_agent_ecology_v5:MAE-3 (or a later substrate that
    supplies a genuine second agent), a query-time READ-OUT comparator (efference-copy-
    predicted vs observed z_self change, additive -- not a gradient head reshaping
    z_world) shows recall_improvement > 0.04 in at least one informative
    intensity/interaction arm, together with the substrate_queue C1+C2+C3 criteria:
    other-agent-event selectivity > 0.5, counterfactual gap (agent-caused minus
    other-caused) > 0.1, and attributor calibration (within-arm slope) < 0.5. V3-EXQ-741
    found the read-out framing (ROUTED_B, neutral-to-positive at +0.003) already
    biologically closer to the claim than the gradient-BCE-head framing (ROUTED_A,
    which actively hurt recall) -- a confirming test should use the read-out design.
    FALSIFYING signature: if, even with a genuine second agent present and the guards
    above satisfied, no arm clears recall_improvement > 0.04 / the C1-C3 criteria
    across the full pre-registered intensity/interaction sweep -- this is the
    Woo/Spelke branch already named in substrate_queue.json: route MECH-095 from
    substrate_ceiling to substrate_conditional (the V4/V5 multi-agent dependency is
    then exhausted as an explanation, not merely unbuilt) and flip evidence_direction
    to weakens rather than non_contributory.
    Evidence base to date is genuinely narrow, not contested-and-unresolved: the sole
    positive result (EXQ-047k, 4-seed PASS 5/5) came from one thin single-agent
    operationalisation and has not been reproduced by any subsequent valid test -- this
    is a single positive base flagged per the illusory-conflict-resolution rule, not a
    weighing call for this note to make. Strong literature anchor (Blakemore 2002; Gu
    et al. 2008; Pitcher & Ungerleider 2021; Rolls et al. 2023) supports the TPJ
    mechanism generically but does not substitute for the substrate-specific
    architectural test above.
- id: MECH-096
  title: "Dual-stream observation routing sends exteroceptive input to both E1 and a dedicated harm detection pathway."
  claim_type: mechanism_hypothesis
  subject: observation_encoder.dual_stream_routing
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-096@2026-03-17T22:29:07.389682Z"
      as_of: 2026-03-17
      verdict: hold_pending_v3_substrate/applied
  evidence_quality_note: |
    V3 gate cleared (2026-03-28): SD-005 (z_self/z_world split) and SD-007 (reafference
    predictor) now implemented in ree-v3. Dual-stream routing experiments can proceed.
    EXQ-130 FAIL (2026-03-29): dual-stream routing pair FAIL. First experimental entry.
    EXQ-130 reclassified INCONCLUSIVE (2026-03-30 review): gap_stream_on=0.015, gap_ablated=0.019,
    delta=-0.004. Both gaps sub-threshold; 8x8 task too simple to discriminate dual-stream
    advantage (single-stream saturates task capacity). Small negative delta within noise at this
    scale. Re-run (T215504Z) identical metrics -- one result. Evidence direction corrected to
    inconclusive: experiment underpowered, not evidence against MECH-096.
  depends_on:
    - SD-005
    - MECH-069
  location: docs/thoughts/2026-03-14_self_world_latent_split_sd003_limitation.md
  source:
    - docs/thoughts/2026-03-14_self_world_latent_split_sd003_limitation.md
  notes: >
    The observation encoder must implement two anatomically motivated output heads,
    not a single encoder with a learned routing gate. Dorsal-equivalent head:
    egocentric, action-relevant, high temporal resolution -> z_self.
    Ventral-equivalent head: allocentric, object-identity, sustained representation
    → z_world. The two streams process structurally different information at
    different temporal resolutions and must remain architecturally distinct or
    the z_self/z_world split degrades back toward z_gamma conflation during
    end-to-end training. SD-005's "encoder routes to appropriate stream" phrase
    underspecifies this requirement.
    Neural basis: Goodale & Milner (1992) dorsal/ventral visual stream distinction.
    Update (2026-03-17): classical two-stream model is insufficient. HCP connectome
    data (Haak & Beckmann 2018, n=470) confirms a THIRD lateral stream
    (MT→MST/FST→posterior STS) specialised for dynamic motion and agency.
    This stream terminates near TPJ and is distinct from both dorsal and ventral
    at the white-matter-tract level. MECH-096 subject should be updated to
    three-stream encoder: dorsal head (→z_self), ventral head (→z_world content),
    lateral head (→agency/harm signal to E3). See MECH-099.
    Literature: evidence/literature/targeted_review_reafference_streams/.
- id: MECH-097
  title: "Peripersonal space geometry defines the commit locus — the spatial boundary of committed action."
  claim_type: mechanism_hypothesis
  subject: peripersonal_space.commit_locus
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-097@2026-03-17T22:29:07.427773Z"
      as_of: 2026-03-17
      verdict: hold_pending_v3_substrate/applied
  evidence_quality_note: |
    V3 gate cleared (2026-03-28): SD-005 (z_self/z_world split) and ARC-016 (dynamic
    precision) now implemented in ree-v3. Peripersonal space / commit locus experiments
    can proceed. Note: depends on MECH-091 (salient event cycle-reset) which still needs
    SD-006 (async heartbeat); design experiments around the commit boundary itself
    independent of heartbeat reset if possible.
    EXQ-137 FAIL (2026-03-29): PPS commit locus discriminative pair FAIL. First experimental
    entry.
    EXQ-137 reclassified INCONCLUSIVE (2026-03-30 review): gap_locus_on=0.003, gap_ablated=0.008,
    delta=-0.005. SD-006 (async execution) and MECH-091 (salient event phase-reset) prerequisites
    unmet -- commit locus gate fires at wrong moments in synchronous substrate; phase-reset
    absorbed by stale E3. Experiment premature. Re-run (T215640Z) identical metrics -- one result.
    Does not weaken MECH-097. Requeue after SD-006 phase 2.
    EXQ-137 re-run 2026-04-24 (post-ARC-016/SD-005 substrate landings, V3-EXQ-137): FAIL, 3/5
    pre-registered criteria, delta_gap (ON - ABLATED) = -0.0050, gap_on=0.003 vs C1>=0.04,
    wrong sign. n_approach_contacts=535 (manipulation check strong; not a broken probe). Script
    author pre-registered decision='retire_ree_claim' on this outcome. Governance 2026-04-24:
    held at candidate (single experiment; alternative PPS encodings -- distance gradient, direction
    field -- remain untested). Evidence recorded as weakens. Next-step: design V3-EXQ with
    distance-gradient PPS encoding before considering demotion.
  depends_on:
    - SD-005
    - MECH-091
    - ARC-016
  location: docs/thoughts/2026-03-14_self_world_latent_split_sd003_limitation.md
  source:
    - docs/thoughts/2026-03-14_self_world_latent_split_sd003_limitation.md
  notes: >
    The commit boundary coincides with the agent's peripersonal space (PPS)
    boundary: the dynamically-maintained near-body region where self-directed
    and world-directed action consequences first interact. Actions crossing
    this boundary (stepping onto a cell, contacting an object) constitute the
    class of z_self→z_world causal transitions that generate moral residue.
    In CausalGridWorld, contamination events occur exactly at this boundary:
    the agent's foot (z_self) makes contact with the cell (z_world).
    The PPS boundary is dynamically updated (scales with motor reach,
    attentional state) — this means the commit locus is not a fixed spatial
    threshold but a modulated one, consistent with z_beta affecting commitment
    thresholds (ARC-016). Without a PPS representation, the architecture has
    no spatial grounding for where self ends and world begins.
    Neural basis: Rizzolatti, Fogassi & Gallese (1997) premotor/parietal
    peripersonal space representation. VIP (ventral intraparietal area)
    confirmed as anatomical site via HCP tractography (Rolls et al. 2023,
    evidence/literature/targeted_review_reafference_streams/).
    downstream optimisation.
# Registered 2026-03-17 — reafference cancellation and three-stream architecture
- id: MECH-098
  title: "Reafference cancellation: z_world subtracts predicted self-caused sensory change to isolate external events."
  claim_type: mechanism_hypothesis
  subject: encoder.reafference_cancellation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-098@2026-03-19T19:52:00Z"
      as_of: 2026-03-19
      verdict: hold_candidate_resolve_conflict/applied
  implementation_phase: v3
  depends_on:
    - SD-005
    - MECH-096
  location: docs/thoughts/2026-03-17_three_stream_reafference.md
  source:
    - docs/thoughts/2026-03-17_three_stream_reafference.md
  notes: >
    The observation encoder must use its z_self prediction (efference copy
    from E2_self) to cancel expected perspective-shift components from
    z_world before E2_world operates. When the agent moves, the egocentric
    view shifts — this is a reafferent change fully predictable from the
    motor command. Only the residual after perspective-shift subtraction
    represents genuine world-state change. Without this cancellation,
    E2_world learns the identity shortcut (view shifted = world stable)
    rather than learning action-conditional world dynamics. The cancellation
    belongs at encoder level (before E2), not inside E2 itself.
    Biological basis: Area MSTd (Gu et al. 2008, Nature Neuroscience
    11:1201–1210) contains congruent neurons (visual/vestibular aligned =
    reafference, world stable) and incongruent neurons (opposite = exafference,
    genuine world change). MSTd receives visual optic flow, vestibular, AND
    efference copy from premotor simultaneously — implementing exactly this
    decomposition at the early sensory processing stage. Cerebellar forward
    model (Wolpert & Kawato 1998) generates the predicted reafference;
    premotor→PPC pathway via SLF (Rolls et al. 2023) delivers efference copy
    to the parietal encoder stage.
    SD-003 consequence: after reafference cancellation, causal_sig =
    E3(z_world_residual_actual) - E3(z_world_residual_cf) will be sharp
    because only genuine world-content change contributes to the signal.
    The current SD-003 plateau at gap=0.027 (spurious BCE artifact) and
    collapse to 0.0007 (regression, EXQ-012) both result from the absence
    of this cancellation step.
    Literature: evidence/literature/targeted_review_reafference_streams/
    (Gu 2008, Wolpert & Kawato 1998, Rolls 2023).
    EXQ-016 root-cause analysis (2026-03-18): V3-EXQ-016 tested a z-space
    ReafferencePredictor (SGD MLP, 200 steps) and achieved R2_test=0.118 vs
    the EXQ-014 lstsq benchmark of R2=0.333. Root causes: (1) encoder EMA
    alpha=0.3 means Δz_world is a blended multi-step signal — a single-step
    linear predictor from (z_self_t, a_t) cannot reconstruct it without the
    full EMA history; (2) C3 metric bug — the correction was applied to both
    endpoints of the delta, causing cancellation (dz_corrected = dz_raw to
    machine epsilon). SD-008 (fix alpha_world >= 0.9) is prerequisite for
    MECH-098 to be testable: with alpha_world=0.3 the reafference signal is
    too diluted for any single-step predictor. EXQ-021 retests with lstsq
    and fixed C3 metric. EXQ-023 retests with alpha_world=0.9.
    EXQ-022 (2026-03-20): lstsq reafference approach retired for this use case.
    R²_test=0.0 despite R²_train=0.373 -- extreme overfitting. Correction
    amplifies world-change signal 10x (dz_corrected=139.6 vs dz_raw=13.9).
    The lstsq model learns within-training-set correlations that do not transfer.
    Neural ReafferencePredictor (SD-007, validated EXQ-027b PASS) is the
    correct approach. All future reafference experiments use the neural
    predictor exclusively.
    EXQ-069 FAIL (2026-03-23): event_selectivity_delta=-0.002 -- reafference OFF
    is MARGINALLY more selective than ON. EXQ-082 FAIL: delta_approach_gap=-0.104
    -- reafference ON is WORSE for approach detection. Root cause: predictor quality
    insufficient. reafference_r2_avg=0.339 (explains only 1/3 of z_world variance
    from locomotion). Residual correction error subtracts real signal along with
    noise. Current 2-layer MLP hidden_dim=64 architecture is the bottleneck.
    Upgrade path: 3-layer MLP with hidden_dim=128 or LSTM predictor. Both are
    feasible on available hardware (GTX 1050 Ti / RTX 2060 Super -- predictor is
    tiny relative to full model). R² target >= 0.70 before re-testing MECH-098.
    Note: the mechanism is architecturally correct (MSTd congruent neuron analog);
    the failure is engineering (predictor accuracy), not conceptual. Do NOT demote
    based on EXQ-069/082 alone. Redesign predictor first.
    EXQ-099 superseded (2026-03-27): both runs had collection filter bug (only 1 and 8
    locomotion steps collected vs 2000+ expected; filter used ttype=='none' which
    misses hazard_approach/benefit_approach in CausalGridWorldV2). Superseded by EXQ-099a.
    EXQ-099a T223151Z PASS (2026-03-26): n_samples=50, gate_threshold=-1.0 (intentionally
    relaxed to test whether any correction level helps), R2_test=-0.249, selectivity_ratio=
    1.655 >= 1.1, harm_avoidance_rate ON=OFF=1.0. Reafference correction produces cleaner
    selectivity even with a poor predictor. Mechanism confirmed conceptually. Predictor
    quality (R2 well below 0.70) remains engineering bottleneck for full validation.
    EXQ-099a T223306Z FAIL not_applicable (2026-03-26): n_samples=167, gate=0.70, R2=-0.027.
    Phase 1 gate failure -- engineering bottleneck persists at stricter quality threshold.

    2026-08-10 edge correction (chip chip-20260810-phase-consistency-reconciliation):
    depends_on: MECH-095 removed. MECH-095 (TPJ agency comparator, v5) is a sibling
    mechanism from the same reafference-streams lit lineage ("See MECH-098 and MECH-099"
    on MECH-095's own side), not a build prerequisite -- MECH-098 ran 8+ real experiments
    (EXQ-016/021/022/023/069/082/099/099a) with zero functional use of MECH-095.
- id: MECH-099
  title: "Visual streams use a three-pathway architecture (ventral/dorsal/frontal) for object, action, and executive routing."
  claim_type: mechanism_hypothesis
  subject: visual_streams.three_pathway_architecture
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-099@2026-03-19T19:52:00Z"
      as_of: 2026-03-19
      verdict: hold_candidate_resolve_conflict/applied
  implementation_phase: v3
  depends_on:
    - ARC-017
    - MECH-069
    - MECH-096
  location: docs/thoughts/2026-03-17_three_stream_reafference.md
  source:
    - docs/thoughts/2026-03-17_three_stream_reafference.md
  notes: >
    The visual processing hierarchy comprises three anatomically distinct
    pathways, not two (dorsal/ventral). Confirmed by Haak & Beckmann (2018,
    Cortex 98:73–83) from HCP resting-state data (n=470) with data-driven
    triple dissociation across 22 visual areas:
    (1) Dorsal (IPS/occipitoparietal): spatial processing, self-motion,
        visually guided action. Maps to z_self / SELF_SENSORY stream.
    (2) Ventral (VO/PHC): object identity, content. Maps to z_world /
        WORLD stream.
    (3) Lateral/third (MT→MST/FST→posterior STS): dynamic motion,
        biological motion, social/agency detection. Terminates near TPJ.
        Maps to the agency-detection channel (MECH-095 / E3 harm stream).
    These three pathways use anatomically distinct white matter tracts:
    SLF I/II/III for dorsal; ventral occipitotemporal white matter for
    ventral; a separate lateral pathway into STS for the third stream.
    This provides structural anatomical grounding for MECH-069
    (incommensurability of error signals): the three learning channels
    are incommensurable partly because they run on physically distinct
    hardware. Collapsing them violates the biological architecture.
    MECH-096 (dual-stream encoder) must be updated to MECH-099-compliant
    three-stream encoder: dorsal head (→z_self), ventral head (→z_world
    content), lateral head (→agency/harm signal for E3).
    Literature: evidence/literature/targeted_review_reafference_streams/
    (Haak & Beckmann 2018, Pitcher & Ungerleider 2021).
  evidence_quality_note: |
    EXQ-098 (2026-03-26): Two independent FAILs.
    Run 1 (seeds=42, 5 eps warmup): auc_delta=0.0000 (threshold >= 0.05).
    Run 2 (seeds=42+7, 400 eps warmup): auc_delta=-0.0384 -- lateral head UNDERPERFORMS
    baseline with more training. Adverse direction in run 2 suggests lateral head may
    actively interfere with the existing two-stream harm signal.
    Hold at candidate. EXQ-098b redesign needed before closing this question. The
    Haak & Beckmann three-pathway biological grounding is strong, but the current
    lateral head architecture (separate AUC scoring on harm channel) does not produce
    the expected signal improvement.
    EXQ-098b FAIL 2/3 (2026-03-27, 2-seed): Redesigned with agency attribution labels
    (agent_caused vs env_caused contact) + motion-delta input (MT analog). C2 PASS:
    attr_AUC_THREE=0.9942 >= 0.65 (both streams learn attribution well). C3 PASS:
    avoidance slightly higher with THREE_STREAM (+0.0007). C1 FAIL: auc_delta=-0.0047
    (< 0.05 threshold) -- THREE_STREAM does not outperform TWO_STREAM. Key finding:
    z_world already encodes agency attribution nearly as well (AUC_TWO=0.9989) as the
    dedicated lateral head at this scale. The three-stream architectural advantage may
    only emerge at larger world_dim or with a deeper lateral head. Hold candidate.
# Registered 2026-03-18 — EMA root-cause analysis and encoder supervision gap
- id: MECH-100
  title: "z_world encoder requires event-type cross-entropy auxiliary loss during training."
  claim_type: mechanism_hypothesis
  subject: encoder.event_contrastive_supervision
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-08-10
    needs_review: false
    needs_review_reason: >
      [2026-08-09] Evidence-currency + citation-integrity review flagged for the next
      /governance cycle; RESOLVED 2026-08-10 (LEFT AT stable + caveat, see
      evidence_quality_note) -- both GFLAG-0019 and GFLAG-0020 now carry a recorded
      governance disposition.
    evidence:
      from: "decision:MECH-100@2026-03-19T19:52:00Z"
      as_of: 2026-03-19
      verdict: promote_to_provisional/applied
  status_note: |
    Promoted provisional -> stable 2026-04-03.
    Evidence: conf=0.817, conflict_ratio=0, 4 exp all supports, 3 lit entries.
    EXQ-020 PASS (2026-03-20): selectivity_margin=0.882, event_classification_acc=0.692.
    EXQ-023 PASS (2026-03-22): combined SD-008+SD-009, event_selectivity_margin=0.084.
    EXQ-176/177 PASS confirming contrastive auxiliary loss is required for z_world discrimination.
    No conflicting experimental evidence. Stable mechanism; z_world encoder design is locked.
    [2026-08-09]: needs_review=true raised (evidence currency + citation integrity, no status
    change) -- see evidence_quality_note.
  evidence_quality_note: |
    [2026-08-09, chip chip-20260809-sd009-897-followon, follow-on to the 2026-08-08 governance
    cycle's review of V3-EXQ-897/SD-009]. REVIEW ONLY -- NO STATUS CHANGE APPLIED. Two findings,
    both routed to /governance rather than acted on unilaterally.

    (1) EVIDENCE CURRENCY. All four supporting experimental entries were run on 2026-03-18 and
    2026-03-20 (20260318T083631Z/20260320T165149Z v3_exq_020_event_contrastive_v3;
    20260318T180301Z/20260320T171130Z v3_exq_022_combined_contrastive_lstsq_v3) -- roughly four
    months BEFORE SD-070 landed on 2026-07-18. SD-070's own entry documents the P0 those runs
    were gathered under as COLLAPSING z_world to participation_ratio ~1.06 at world_dim=128
    (untrained 9.21), and identifies the SD-009 event-contrastive target itself as one of the
    three faults driving it: transition_type is a property of the TRANSITION (t-1 -> t) while
    z_world is a static single-frame encoding, and an MLP-128 probe on raw world_obs with no
    encoder in the path scores AT OR BELOW CHANCE for that label. So MECH-100's stable status
    rests entirely on evidence measured under a training recipe the substrate has since replaced
    for cause. V3-EXQ-897 (2026-08-08) is the first re-measurement under the fixed substrate and
    reads the other way: held-out frozen-probe decodability clears the floor in BOTH arms with
    OFF >= ON on 3/3 seeds (deltas -0.025/-0.010/-0.005) -- sign-consistent but statistically
    imprecise, hence direction=unknown, not weakens. Note the same four runs also carry
    outcome=FAIL with direction=supports in claim_evidence.v1.json (pass_runs=0, fail_runs=4,
    one entry annotated "direction/status mismatch"), which is a second reason to look at them
    directly rather than at the aggregate. Governance's call, not this chip's: whether stable
    still holds, whether a candidate-stale flag is warranted per the 2026-07-18 channel-mismatch
    doc's option (B), or whether a re-verification run under the SD-070 recipe is the right
    discriminator. SD-009 (same underlying claim, design register) was already demoted
    provisional -> candidate on 2026-08-08 and shares two of these four runs.

    (2) CITATION INTEGRITY (corrected in this pass; substantive re-scoring is NOT). The notes
    field cited "Murray et al. 2004, TICS 8:56-61" for the sentence "Predictive coding alone
    does not produce object-discriminative ventral stream representations without categorical
    top-down signals". That article does not exist -- verified 2026-08-09 against the complete
    PubMed listing of Trends in Cognitive Sciences vol 8: issue 2 (Feb 2004) contains no paper
    by any Murray, and pages 56-61 do not delimit one article in it (p.57 is a Toates letter,
    pp.60-65 are Stevens & Hauser, and issue 3 starts at p.95). The tracked entry that existed
    (targeted_review_connectome_mech_100/.../2026-03-29_mech_100_ventral_stream_categorical_murray2004)
    named the right authors, year and venue but carried a wrong title, a wrong DOI (differing in
    its final digits) and a URL whose PMID 17645520 is an unrelated liver-transplantation paper.
    The real source is Murray EA, Bussey TJ, Saksida LM (2007), Annu Rev Neurosci 30:99-122,
    PMID 17417938, DOI 10.1146/annurev.neuro.29.051605.113046 -- and it is the paper that
    entry's own summary body describes, so its assessment stands. Bibliographic fields have been
    corrected in place with the previous values preserved under metadata_correction. What is NOT
    corrected here, and is the part governance should weigh: that 2007 review is now known to be
    the ONLY source MECH-100's biological grounding has ever had, rather than a stand-in for a
    stronger uncited one, and it supports the weaker proposition (perirhinal conjunctive
    representations are shaped by discrimination DEMANDS rather than passive exposure) rather
    than the predictive-coding-specific proposition the prose attributes to it. Its confidence
    of 0.62 was assigned on the assumption that a stronger source sat behind it. Re-scoring a
    stable claim's literature evidence is a governance disposition and is left open.
    Full working: evidence/planning/sd009_murray_citation_and_mech100_currency_review_2026-08-09.md

    [2026-08-10 governance disposition on GFLAG-0019/GFLAG-0020]: LEFT AT stable, both flags
    resolved with this caveat rather than a status change. (1) Evidence-currency: all four
    supporting runs predate SD-070's fix and V3-EXQ-897 (the first re-measurement under the fixed
    substrate) reads direction=unknown, not supports, and does not tag MECH-100 -- no experiment
    currently queued targets a clean re-verification under the SD-070 recipe; flagged for a future
    /queue-experiment session rather than queued here. (2) Citation integrity: MECH-100's sole
    biological citation, now corrected to Murray/Bussey/Saksida 2007, supports a weaker proposition
    than the claim's own prose and was scored at 0.62 on the false premise of a stronger uncited
    source -- literature confidence should be treated as provisional pending a deliberate re-score,
    not relied on as strong grounding until that happens. needs_review cleared (both flags now have
    a recorded governance disposition); GFLAG-0019 and GFLAG-0020 resolved.
  implementation_phase: v3
  depends_on:
    - SD-005
    - SD-009
  location: CLAUDE.md
  source:
    - evidence/experiments/ (V3-EXQ-013 event selectivity failure)
  notes: >
    The world encoder (z_world branch of SplitEncoder) requires an explicit
    event-type cross-entropy auxiliary loss during training. Standard
    reconstruction and E1-prediction losses are invariant to harm-relevance:
    they reward accurate decoding of all observation channels equally, giving
    the encoder no gradient signal to distinguish hazard-present from
    hazard-absent world states. As a result, z_world ends up representing
    general perspective-dominated content (90% variance explained by
    locomotion; EXQ-014 R2=0.333) with near-zero event selectivity
    (EXQ-013: Δz_world(env_hazard)≈Δz_world(empty), margin≈0.002).
    The fix (SD-009): add a small event classifier head (Linear(world_dim,3))
    whose cross-entropy loss backpropagates through z_world during training.
    Labels: 0=none (empty locomotion), 1=env_caused_hazard, 2=agent_caused_hazard.
    This forces z_world to carry event-discriminative information, providing a
    stronger gradient for E2_world and E3 harm evaluation to exploit.
    Biological grounding: the ventral stream (MECH-099 stream 2) is trained by
    object-recognition supervision in development — not just reconstruction.
    Predictive coding alone does not produce object-discriminative ventral
    stream representations without categorical top-down signals (Murray,
    Bussey & Saksida 2007, Annu Rev Neurosci 30:99–122 — citation corrected
    2026-08-09; the previously cited "Murray et al. 2004, TICS 8:56–61" does
    not exist, and the 2007 review supports the weaker proposition that
    perirhinal conjunctive representations are shaped by discrimination
    demands rather than by passive exposure, NOT the predictive-coding-
    specific proposition stated in this sentence — see evidence_quality_note).
    The event classifier is the minimal stand-in for
    such categorical supervision.
    Interaction with SD-008: the event contrastive loss will be ineffective
    if alpha_world=0.3, since the EMA smoothing dilutes the event contribution
    to 30% per step. Both SD-008 and SD-009 are co-prerequisites.
    Test: V3-EXQ-020 (event auxiliary loss alone), V3-EXQ-022 (combined with
    lstsq reafference), V3-EXQ-023 (SD-008 + SD-009 combined in alpha=0.9 run).
    EXQ-020 PASS on SD-009 mechanism (2026-03-20): selectivity_margin=0.882,
    event_classification_acc=0.692, Δz_world escalates env_hazard/agent_hazard
    2.4× over none. C1 (calibration_gap) FAIL is inherited from SD-010 (broken
    z_harm evaluation), not from SD-009 itself. SD-009 mechanism validated —
    contrastive loss forces z_world to carry event-discriminative information.
    C1 retest pending SD-010 fix (EXQ-056c).

- id: MECH-101
  title: "z_world encoding requires reafference context to correctly attribute external vs self-caused change."
  claim_type: mechanism_hypothesis
  subject: encoder.reafference_world_context_required
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  status_note: |
    Promoted candidate -> provisional 2026-03-22.
    Evidence: 3 supporting experiments, 0 weakening, conflict_ratio=0, conf=0.874.
    EXQ-027 (R2=0.027 with z_self input) established the failure mode.
    EXQ-021 PASS confirmed z_world_raw_prev input fixes the predictor.
    Implemented as SD-007 in ree_core/latent/stack.py.
    Literature triangulation pending (LIT-0090, LIT-0091) -- not required for provisional.
  depends_on:
    - MECH-098
    - SD-007
  location: docs/architecture/sd_004_sd_005_encoder_codesign.md#sd-007
  source:
    - evidence/experiments/  # V3-EXQ-027 R²=0.027 failure analysis
  notes: >
    The SD-007 ReafferencePredictor must take z_world_raw_prev as input
    (not z_self_prev). In local-view environments, Δz_world_raw from
    locomotion includes cell content entering the field of view, which
    depends on current world state (available in z_world_raw_prev) but
    not on body proprioception (z_self). V3-EXQ-027 measured R²=0.027
    with (z_self, a) inputs — near-zero because newly-revealed cell
    content dominates the delta and is inaccessible from body state alone.
    Biological basis: MSTd receives full visual optic flow (scene
    content-dependent) + vestibular + efference copy, NOT body state +
    efference copy. The optic flow encodes scene content; z_self encodes
    body position only. Fix: ReafferencePredictor(z_world_raw_prev, a)
    → Δz_world_loco. z_world_raw_prev is stored in LatentState and
    available in encode() as prev_state.z_world_raw.

- id: MECH-103
  title: "E1 performs multimodal exteroceptive fusion across sensory modalities before feeding E2."
  claim_type: mechanism_hypothesis
  subject: e1.multimodal_exteroception_fusion
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  evidence_quality_note: |
    EXQ-128 FAIL (2026-03-29): superseded -- no multimodal sensory input in V3 substrate;
    artificial auditory channel not representative of genuine exteroceptive fusion. Manifest
    marked superseded. Claim untestable until V3 multimodal input is implemented.
    EXQ-134 FAIL (2026-03-29): somatosensory fusion pair -- same substrate issue; also
    superseded. Claim requires genuine multi-modality input streams.
  epistemic_category: substrate_conditional
  depends_on:
    - ARC-017
    - ARC-004
  location: docs/architecture/sensory_stream_tags.md#mech-103
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: "genuine" multi-modal exteroceptive input requires a
    second modality sourced independently of the visual/spatial world_state pathway --
    not a synthetic channel re-derived from the same underlying grid state (as EXQ-128's
    "auditory" tone and EXQ-134's "somatosensory" contact signal both were, both computed
    from hazard_field/resource_field/harm_signal). Re-verified 2026-08-08:
    ree_core/environment/causal_grid_world.py still exposes exactly one true exteroceptive
    channel (world_state, vision/spatial), no independent auditory/somatosensory channel
    exists. Until V3's environment exposes such a channel, this claim is untestable, not
    merely under-tested -- confirmed twice (EXQ-128, EXQ-134).

    CONFIRMING: once genuine multi-modal input exists, E1's shared world latent (z_S/
    z_world) shows measurably more accurate/robust representations with both modalities
    present than either alone at matched training budget, and degrades gracefully rather
    than catastrophically when one modality is ablated at test time (precision-weighted
    evidence combination signature).

    FALSIFYING: multi-modal input produces no measurable improvement over vision-only
    baselines at matched budget, or any observed "fusion" is fully explained by naive
    channel concatenation with no precision-weighting/complementary-structure signature.
  source:
    - docs/thoughts/2026-03-19_sensory_stream_e3_decomposition.md
  notes: >
    Different exteroceptive modalities (vision, audition, somatosensory/touch)
    each contribute differently to E1's shared world latent (z_S / z_world).
    Multi-source convergence produces more accurate and robust world
    representations than single-modality input because each modality carries
    complementary structure: vision (object identity, spatial layout), audition
    (events, temporal cues, off-screen dynamics), somatosensory (surface
    properties, contact). Each modality has its own encoder pathway into the
    shared latent, contributing precision-weighted evidence. Grounds the WORLD
    stream tag's "modality fusion may occur upstream" in a concrete mechanism.
    Biological basis: superior temporal sulcus (STS) multisensory convergence;
    MST integrates optic flow with vestibular and auditory input; cross-modal
    ventral stream binding (Murray et al. 2004).

- id: MECH-104
  title: "Unexpected harm events spike commitment uncertainty (LC-NE volatility interrupt), enabling de-commitment."
  claim_type: mechanism_hypothesis
  subject: control_plane.volatility_interrupt
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_365_mech104_surprise_gate_pair_20260414T181223Z_v3
      as_of: 2026-04-14
      verdict: supports/PASS
  implementation_phase: v3
  status_note: |
    Promoted provisional -> active 2026-03-23.
    Evidence: conf=0.964, conflict_ratio=0, 3 supports (EXQ-049e, EXQ-064, EXQ-062b), 2 mixed
    (EXQ-062, EXQ-062a both criterion-revision iterations, not true weakening signals).
    Route-1 (jitter noise floor) confirmed (EXQ-049e PASS 5/5, EXQ-061 PASS).
    Route-2 (harm_surprise-gated spike) confirmed (EXQ-064 PASS 5/5): spike fires selectively
    on unexpected harm (delta_var_unexpected=0.051) and not on expected harm (delta_var_expected=0.000).
    EXQ-062b PASS 5/5 confirms spike selectivity (fewer spikes than always-spike condition,
    higher per-spike impact: 10.7 vs 6.7 uncommitted steps/spike).
    Both routes independently confirmed. Literature triangulation pending (LIT-0092, LIT-0093).
    Updated 2026-04-03: two additional definitive PASSes.
    EXQ-197 PASS 6/6 (2026-04-01): matched-seed discriminative pair, 3 seeds [42,7,13].
    discriminative_delta=0.0506-0.0509 all seeds; ABLATED produces 0.0 (perfect discrimination).
    EXQ-204 PASS 4/4 (2026-04-03): surprise gate pair, 3 seeds. harm_spike_rv~1.6-1.9e-6
    on unexpected harm; 0 on non-harm; 0 in ABLATED condition. Strongest evidence to date.
    EXQ-365 (2026-04-14, PASS): mech104_surprise_gate_pair replication. Additional
    evidence for surprise-gated de-commitment architecture.
  claim_level: mechanistic
  functional_restatement: >
    When actual harm substantially exceeds predicted harm (prediction error surprise),
    the commitment uncertainty estimate (running_variance) receives an upward impulse
    proportional to surprise magnitude, enabling de-commitment from a currently active
    trajectory. Without this mechanism, world_forward training drives running_variance
    to near-zero, permanently locking the agent in committed state regardless of
    environmental change. In an ANN substrate this is implemented as a surprise-gated
    upward push to running_variance when |actual_harm - predicted_harm| exceeds a
    threshold. V3 approximation: Gaussian jitter (noise floor ε) added to the EMA
    so running_variance cannot fully collapse, allowing the committed→uncommitted
    transition to remain testable. The biological mechanism (LC phasic firing on
    unexpected outcomes, NE release increasing neural gain and driving exploration)
    is the reference implementation. See Yu & Dayan (2005) PMID 15944135.
  depends_on:
    - MECH-090
    - ARC-016
    - Q-007
  location: docs/architecture/control_plane_heartbeat.md#mech-104
  source:
    - docs/architecture/control_plane_heartbeat.md
  notes: >
    The locus coeruleus (LC) fires phasically on unexpected, behaviourally significant
    outcomes. NE release increases neural gain and drives exploration, reducing the
    effective commitment bias. This is Yu & Dayan (2005)'s unexpected uncertainty
    signal — distinct from expected uncertainty (routine variance tracked by the EMA),
    unexpected uncertainty is triggered by structural environmental change that the
    running average could not anticipate. Two V3 implementation routes: (1) jitter
    approximation — add small Gaussian noise floor ε to running_variance EMA update,
    preventing collapse to zero and making within-episode de-commitment testable; and
    (2) full wiring — harm_surprise = |harm_eval(z_harm) - actual_harm|; if
    harm_surprise > surprise_threshold, _running_variance += surprise_coeff × harm_surprise.
    Route (1) is sufficient for MECH-090/104 experiments. Route (2) is the proper
    mechanistic implementation for later V3 phases. Biological reference: Yu & Dayan
    (2005) Uncertainty, neuromodulation, and attention. Neuron 46(5):681-92.
    PMID 15944135. [DOI: 10.1016/j.neuron.2005.04.014]

- id: MECH-105
  title: "Hippocampal sequence completion drives BG beta release via subiculum-NAc-VTA dopaminergic loop."
  claim_type: mechanism_hypothesis
  subject: control_plane.hippocampal_bg_coupling_dopaminergic
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  claim_level: mechanistic
  functional_restatement: >
    When the hippocampal module signals sequence completion (MECH-057b gate opens),
    this signal propagates to the BG beta gate (MECH-090) via a dopaminergic coupling
    loop: subiculum → nucleus accumbens → ventral pallidum → VTA → phasic dopamine
    release → D1 pathway potentiation → beta drop → gate release. This is the
    mechanistic bridge connecting the trajectory candidacy gate (hippocampal level)
    to the policy propagation gate (BG level). Without this coupling, the two gates
    are architecturally independent and sequence completion in HippocampalModule
    cannot trigger policy update in BetaGate. In an ANN substrate, the coupling is
    implemented as a completion signal from HippocampalModule to BetaGate mediated
    by a dopamine-analog variable modulating the commit threshold. The biological
    mechanism (subiculum→NAc→VP→VTA) is the reference.
  epistemic_category: standard
  depends_on:
    - MECH-057b
    - MECH-090
    - ARC-023
    - ARC-021
  location: docs/architecture/control_plane_heartbeat.md#mech-105
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: the three required changes from this claim's own notes
    ARE implemented (confirmed 2026-08-08 by source inspection) -- this is NOT the
    "cannot be tested experimentally" state the notes previously described (see
    CORRECTION below). (1) HippocampalModule.compute_completion_signal()
    [ree_core/hippocampal/module.py] emits a dopamine-analog completion signal
    (sigmoid over min residue cost across proposed trajectories). (2)
    BetaGate.receive_hippocampal_completion() [ree_core/heartbeat/beta_gate.py]
    receives it and calls gate.release() when beta is elevated and the signal crosses
    _completion_release_threshold. (3) The two are wired at ree_core/agent.py under an
    explicit "ARC-028 / MECH-105" comment, gated on config.heartbeat.beta_gate_bistable
    (True in the MECH-090/SD-034 experiment family) and beta_gate.is_elevated. A run is
    non-degenerate only if beta_gate_bistable=True, beta_gate.is_elevated is reached at
    least once, and completion_signal takes a non-degenerate range of values -- and,
    per ARC-028's own audit, only if the effect of this pathway is isolated from
    BetaGate's other independent release pathways (MECH-091 urgency-interrupt, V_s
    anchor invalidation, SD-034 closure operator).

    CONFIRMING: a controlled ablation (matched seeds/config; ON arm with the pathway
    live at its configured threshold vs. OFF arm with it suppressed) while holding
    every other BetaGate release pathway at identical trigger conditions across both
    arms, shows release timing/rate measurably and reproducibly contingent on
    compute_completion_signal(): releases attributable to this pathway specifically
    occur at non-trivial rate, and ablating the dopamine-analog coupling variable
    measurably shifts release latency or total release count relative to the intact
    ON arm.

    FALSIFYING: release timing/rate are statistically indistinguishable between arms
    once other pathways are accounted for -- every release attributed to this pathway
    would have fired anyway via another pathway, or completion_signal never crosses
    threshold under realistic trajectory-quality distributions. In that case the two
    gates remain functionally decoupled in practice despite the code-level wiring
    existing, and the dopaminergic-loop mechanism is architecturally present but
    functionally redundant/non-necessary.

    GOVERNANCE FLAG (2026-08-08, not resolved here): check whether MECH-290 (named
    alongside MECH-105 in ARC-028's own drift note as sharing this same stale-notes
    pattern) needs the equivalent correction.
  source:
    - docs/architecture/control_plane_heartbeat.md
  literature_evidence: |
    Lisman & Grace (2005) PMID 15924857 [DOI: 10.1016/j.neuron.2005.05.002]:
    Hippocampal-VTA loop: subiculum → NAc → ventral pallidum → VTA → dopamine
    release. Novel/completion events drive VTA via this pathway; upward arm is
    dopamine-enhanced hippocampal LTP. This is the precise anatomical substrate
    for hippocampal-to-BG completion signaling.
    Lansink et al. (2009) PMID 19688032 [DOI: 10.1371/journal.pbio.1000173]:
    Hippocampus leads ventral striatum in joint replay — hippocampal place cells
    fire before reward-correlated striatal cells. Directionality confirmed:
    completion signals originate in hippocampus and propagate forward to BG.
    Pennartz et al. (2011) PMID 21889806 [DOI: 10.1016/j.tins.2011.08.001]:
    Hippocampal-striatal axis: ventral striatum integrates hippocampal episodic
    content with value signals to generate goal-directed motivational predictions.
    Establishes functional as well as anatomical link.
  notes: >
    This claim resolves the architectural orphan problem: MECH-057b and MECH-090
    are separately claimed but lack an explicit coupling mechanism. The subiculum→
    NAc→VP→VTA loop is the missing link. Original design: (1) HippocampalModule.
    generate_trajectories() emits completion signal on trajectory goal-reach or
    planned-length completion; (2) BetaGate or E3Selector receives this signal and
    calls gate.release(); (3) coupling is mediated by a dopamine-analog variable
    (see MECH-106).

    CORRECTION (2026-08-08, thought-digestion currency check): the paragraph above
    previously read "This coupling is currently absent... Without ARC-028 wiring,
    this claim cannot be tested experimentally." That is stale. Confirmed by direct
    source inspection of ree-v3 current HEAD: all three required changes ARE
    implemented -- HippocampalModule.compute_completion_signal()
    [ree_core/hippocampal/module.py:2582] emits the dopamine-analog completion
    signal; BetaGate.receive_hippocampal_completion()
    [ree_core/heartbeat/beta_gate.py:282] receives it and calls release() when beta
    is elevated and the signal crosses threshold; the two are wired at
    ree_core/agent.py under an explicit "ARC-028 / MECH-105" comment, gated on
    config.heartbeat.beta_gate_bistable. This drift was independently flagged first
    by ARC-028's own 2026-08-07 what_would_answer, which named MECH-105 (and
    MECH-290) as carrying the same stale description. Original text preserved above
    for history; see this claim's what_would_answer for the current, testable
    formulation.

- id: MECH-106
  title: "Commitment threshold is asymmetrically modulated by outcome valence: positive outcomes lower it, negative outcomes raise it."
  claim_type: mechanism_hypothesis
  subject: control_plane.commitment_hysteresis_outcome_valence
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-106@2026-04-05T11:48:38.245Z"
      as_of: 2026-04-05
      verdict: promote_to_provisional/applied
  implementation_phase: v3
  evidence_quality_note: |
    EXQ-231 FAIL (2026-04-04): Superseded -- PERSISTENT and REACTIVE conditions produced
    identical commit values; experimental manipulation never created differentiated valence
    history. Design flaw, not claim failure.
    EXQ-231a PASS 5/5 criteria, 4/5 seeds (2026-04-04): VALENCE_BIAS vs NO_BIAS ablation.
    da_divergence=0.73-0.78 (4/5 seeds), threshold_asymmetry=2.7-2.8, latency_ratio=7-11x.
    Seed 42 anomaly (near-zero divergence, no commitments) is isolated; does not invalidate.
    Three literature supports (Frank 2005 D1/D2 asymmetry; Frank et al. 2004 Go/NoGo;
    Jenkinson & Brown 2011 beta modulation). conflict_ratio=0. Promoted to provisional
    2026-04-06 governance.
  claim_level: mechanistic
  functional_restatement: >
    The threshold governing entry into and exit from committed states is asymmetric:
    positive outcomes (low harm, successful trajectory completion) lower the commit
    threshold via D1 pathway potentiation, making it easier to commit again.
    Negative outcomes (harm contact, large prediction error) raise the threshold via
    D2 pathway activation, making it harder to commit and easier to break from an
    active commitment. This asymmetric hysteresis prevents pathological flip-flopping
    (committed state is sticky through minor perturbations) while ensuring genuine
    harm events can force de-commitment. In an ANN substrate, the hysteresis is a
    slowly-updating outcome-valence bias on the commit threshold:
    commit_threshold_effective = commit_threshold_base × (1 + valence_bias), where
    valence_bias increases after harm and decreases after successful completion.
    The biological mechanism (phasic dopamine modulating D1/D2 balance in striatum)
    is the reference.
  depends_on:
    - MECH-090
    - ARC-016
    - MECH-104
    - MECH-105
  location: docs/architecture/control_plane_heartbeat.md#mech-106
  source:
    - docs/architecture/control_plane_heartbeat.md
  literature_evidence: |
    Frank (2005) PMID 15701239 [DOI: 10.1162/0898929052880093]:
    Dynamic dopamine modulation in BG: D1 (direct/Go) potentiated by positive
    phasic dopamine — lowers threshold for action facilitation (commit). D2
    (indirect/NoGo) relieved by dopamine dip — raises suppression signal
    (de-commit). Asymmetric dynamic range explains over-commitment (Parkinson's
    off medication) and under-commitment (dopamine overdose in intact striatum).
    Frank, Seeberger & O'Reilly (2004) PMID 15528409 [DOI: 10.1126/science.1102941]:
    "By carrot or by stick": Go/NoGo learning asymmetry is dopamine-dependent.
    Parkinson's patients off medication learn better from negative outcomes (D2
    dominant); on medication the reverse (D1 dominant). Confirms asymmetric
    modulation of commitment direction by outcome valence.
    Jenkinson & Brown (2011) PMID 22018805 [DOI: 10.1016/j.tins.2011.09.003]:
    Beta oscillations are predictive — modulated by net dopamine levels at cortical
    BG inputs in response to salient cues. Loss of dopamine (Parkinson's) annuls
    the prospective resourcing of actions. Connects outcome-valence dopaminergic
    modulation to the beta gate mechanism (MECH-090).
  notes: >
    Commitment hysteresis is the BG-level mechanism preventing pathological
    state-switching. Without it, any variance fluctuation across the threshold
    produces an immediate state change. The dopaminergic asymmetry means successful
    sequences build commitment momentum (threshold lowers, harder to de-commit
    prematurely) while harm events build release momentum (threshold raises, easier
    to de-commit). This is slower-timescale than running_variance — it is a
    motivational bias rather than a prediction-error signal. Distinct from MECH-104
    (volatility interrupt, fast) and running_variance (medium), this is the slow
    dopaminergic modulation that sets the operating point. Relevant to MECH-025
    (action doing mode) because that mode requires low threshold + committed state,
    which the dopamine system builds through accumulated successful sequences.
    V3 implementation: valence_bias updated at each E3 tick via a slow EMA on
    outcome valence signal (harm_t vs predicted_harm_t).

- id: ARC-028
  title: "HippocampalModule must emit a trajectory completion signal wired to BetaGate to couple the candidacy and propagation gates."
  claim_type: architectural_commitment
  subject: control_plane.hippocampal_betagate_coupling_wiring
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  depends_on:
    - MECH-057b
    - MECH-090
    - MECH-105
    - ARC-023
    - MECH-290   # backward credit sweep fires at the same completion event that releases BetaGate
  location: docs/architecture/control_plane_heartbeat.md#arc-028
  source:
    - docs/architecture/control_plane_heartbeat.md
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: the wiring this claim asserts is ALREADY BUILT in ree-v3, not
    merely proposed -- HippocampalModule.compute_completion_signal() (ree_core/hippocampal/
    module.py) feeds BetaGate.receive_hippocampal_completion() (ree_core/heartbeat/beta_gate.py),
    called at agent.py:5280-5302 under an explicit "ARC-028 / MECH-105" comment, gated by
    config.heartbeat.beta_gate_bistable (default False, but True in the MECH-090/SD-034
    experiment family) and beta_gate.is_elevated. Confirmed by direct source inspection
    (2026-08-07) and independently by evidence/planning/mech090_release_path_audit_2026-06-02.md.
    A run is non-degenerate only if beta_gate_bistable=True, the agent actually reaches
    beta_gate.is_elevated at least once (real committed episodes occur), and completion_signal
    takes a non-degenerate range of values across trajectories rather than saturating at 0 or 1.
    Existing runs with beta_gate_bistable=True already satisfy this; no new substrate build is
    required.

    CONFIRMING signature: a controlled ablation -- matched seeds/config, ON arm with the
    hippocampal-completion pathway live (completion_release_threshold at its configured value,
    e.g. 0.75) vs. OFF arm with the pathway suppressed (e.g. completion_release_threshold pinned
    above 1.0) while holding every OTHER BetaGate release pathway (MECH-091 urgency-interrupt,
    V_s anchor invalidation, SD-034 closure operator) at identical trigger conditions across both
    arms -- shows BetaGate release timing and/or release rate measurably and reproducibly
    contingent on HippocampalModule's completion_signal: releases attributable to this pathway
    specifically (releases that fire in the ON arm at ticks where no other pathway's trigger
    condition is independently met) occur at non-trivial rate, and removing the pathway measurably
    shifts release latency distribution or total release count relative to the intact ON arm.

    FALSIFYING signature: BetaGate release timing and rate are statistically indistinguishable
    between the ON and OFF arms once the other release pathways are accounted for -- every release
    attributed to the hippocampal-completion pathway would have fired anyway via one of the other
    four release pathways audited in mech090_release_path_audit_2026-06-02.md, or completion_signal
    never (or trivially rarely) crosses completion_release_threshold under realistic
    trajectory-quality distributions. In that case the two gates remain functionally decoupled in
    practice despite the code-level wiring existing.

    KNOWN DOCUMENTATION DRIFT (not corrected here, flagged for a separate pass): MECH-105 and
    MECH-290's own claims.yaml notes still describe this coupling as absent/untested ("without
    ARC-028 wiring, this claim cannot be tested experimentally"). That is stale relative to the
    code as of 2026-08-07.
  notes: >
    MECH-057b (hippocampal candidacy gate) and MECH-090 (BG beta propagation gate)
    are currently architecturally independent in V3. ARC-028 commits to coupling
    them: HippocampalModule must emit an explicit trajectory completion signal
    received by BetaGate (or the commit_threshold mechanism) to trigger gate release.
    Without this wiring, sequence completion in HippocampalModule has no downstream
    effect on BetaGate and the two gates cannot be tested as a unified circuit.
    Required implementation changes: (1) HippocampalModule.generate_trajectories()
    emits a completion signal when a trajectory reaches its goal or planned length;
    (2) E3Selector or BetaGate receives this and calls beta_gate.release(); (3) the
    coupling is mediated by a dopamine-analog variable (see MECH-105, MECH-106) for
    proper hysteresis. Biological basis: subiculum -> NAc -> VP -> VTA -> dopamine
    -> BG beta release (Lisman & Grace 2005, PMID 15924857).
    TRIGGER MECHANISM (2026-04-24): two distinct triggers feed ARC-028 depending on
    operating mode. (a) Waking mode: MECH-105 dopaminergic loop -- the trigger is
    PE-at-endpoint; large prediction error at goal arrival drives dopamine release
    concurrent with reverse replay (Foster & Wilson 2006, Nature 440:680). Trigger is
    value-sensitive and uncertainty-sensitive. (b) Offline simulation mode (MECH-092
    quiescent replay): MECH-057b sequence-length gate -- no outcome PE is available
    in simulation, so completion is signalled by reaching the planned trajectory
    length. These two triggers are complementary: (a) is the waking gate-release
    mechanism; (b) is the simulation termination mechanism. MECH-290 (backward credit
    sweep) fires at the same completion event as (a), providing post-hoc credit
    assignment to preceding trajectory steps.

- id: ARC-029
  title: "Committed and uncommitted operating modes produce measurably distinct harm outcomes."
  claim_type: architectural_commitment
  subject: cognitive_modes.behavioral_consequence_of_commitment
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_604a-624a-630_2026-06-03#V3-EXQ-630"
      as_of: 2026-06-03
      verdict: non_contributory/measurement_test_design_defect
  implementation_phase: v3
  evidence_quality_note: |
    EXQ-125 FAIL (2026-03-29): committed mode discriminative pair FAIL. Adding weakening
    evidence. Prior EXQ-063 PASS remains valid. Mixed evidence building.
    EXQ-125 re-run FAIL 5/6 (2026-04-03): C1 FAIL -- harm_gap_stable = -0.01222 (threshold
    >= 0.0001). Committed mode WORSE than ablated in stable env (committed harm_rate higher).
    C2-C6 all PASS (gap_ratio=1.064, committed steps present, zero committed steps in ablated,
    no fatal errors). This is the opposite pattern from EXQ-063 (harm_gap_stable=+0.00041).
    Mixed evidence now: one PASS (EXQ-063) and two FAILs (EXQ-125 x2 runs). The committed
    mode may produce idiosyncratic harm differences that depend on random seed and training
    state rather than a systematic architectural advantage. ARC-029's core prediction (committed
    < ablated in stable) is not consistently confirmed. Hold at provisional; more seeds needed.
    EXQ-125 FAIL 3/6 (2026-04-04, seeds=[42,123]): seed 42 shows committed mode WORSE in
    stable (harm_gap_stable=-0.000091 < threshold); seed 123 shows it better (+0.000234).
    avg_harm_gap_stable=0.000071 (barely above zero, avg_gap_ratio=1.943). 3rd FAIL across
    EXQ-125 runs vs 1 PASS (EXQ-063). Diagnosis flagged: systematic difference between EXQ-063
    and EXQ-125 designs unknown (seed set, warmup length, env config). /diagnose-errors
    requested before any status change. Maintain provisional pending diagnosis.
    EXQ-227 DIAGNOSTIC non_contributory (2026-04-05, cowork-2026-04-05-a): attempted to
    reproduce EXQ-063 exact conditions (seeds [0,1]). SET A harm_gap_stable=-3.26e-05
    (gap sign inverted, fails bare>0 criterion). EXQ-063 PASS does not reproduce under
    current substrate. Root cause: SD-010/011/012 substrate changes reduced harm rates
    ~100x, making any committed-gate signal undetectable at original env parameters
    (num_hazards=4, hazard_harm=0.02). This confirms the EXQ-063 vs EXQ-125 discrepancy
    is substrate drift, not seed-dependence. EXQ-125a redesign (num_hazards=8,
    hazard_harm=0.05, 5 seeds, harm_obs passed to sense()) is the correct follow-up.
    /diagnose-errors complete.

    [2026-06-03 governance / autopsy failure_autopsy_604a-624a-630_2026-06-03] V3-EXQ-630 (across-tick nav_competence ecological) re-tagged non_contributory: C4 FAIL -- OFF baseline showed the same readiness-conditioned commitment collapse (arm0_baseline_delta 1.0) as the gated arms, so the gate contribution is unattributable (SD-022 limb-damage degrade drives commitment via running_variance independent of nav_competence). measurement_test_design_defect; does NOT weaken this claim. Redesign queued (confound-isolation arm). ARC-029 separately supported by V3-EXQ-063a PASS (committed-mode harm outcomes, R-c gate) 2026-06-02.
  status_note: |
    Promoted candidate -> provisional 2026-03-23.
    Evidence: conf=0.774, 1 support (EXQ-063 PASS 5/5), 0 weakens, conflict_ratio=0.
    EXQ-063 is the gate-ablation 2x2 design described in notes (EXP-0085).
    harm_gap_stable=0.00041 (gate-active outperforms ablated in stable env),
    harm_gap_volatile=0.00023 (advantage narrows, ratio=0.559 -- commitment is context-dependent,
    not universally beneficial). Ablation confirmed complete. Both predicted effects confirmed.
    Single experiment; more evidence desirable before active promotion.
  depends_on:
    - ARC-016
    - MECH-090
  location: docs/architecture/modes_of_cognition.md#arc-029
  source:
    - docs/thoughts/2026-02-08_modes_of_cognition_control_plane_regimes.md
  notes: >
    The gate-ablation design (EXP-0085) is the cleanest test: train a single agent
    normally, then compare (A) normal BetaGate enabled vs (B) BetaGate ablated
    (always-release). Controls for training quality; isolates only the commitment
    gate's effect on behavior. Two environment conditions add a second dimension:
    stable hazard layout (commitment should help: agent holds a safe trajectory)
    vs volatile layout (hazard moves every N steps: commitment should hurt or be
    neutral as agent cannot adapt mid-commitment). PASS pattern: harm_committed <
    harm_ablated in stable env; gap narrows or reverses in volatile env. This
    would confirm that commitment is not universally good but context-dependent --
    the correct functional prediction of ARC-016's control-plane framing.
    ENVIRONMENT REGIME NOTE (2026-04-05, from Humphries 2012 lit-pull): dopamine
    shifts BG output between exploitation (stable env) and exploration (volatile env).
    EXQ-125a uses num_hazards=8 -- this may be in the volatile regime where
    exploration (uncommitted mode) is dopaminergically optimal. If so, committed mode
    should show no advantage or active disadvantage in an 8-hazard environment,
    which would constitute a FAIL not of the claim but of the experimental regime.
    Consideration for follow-up: explicitly test the stable/volatile axis by varying
    hazard density (e.g. 2-3 hazards = stable/exploitative, 6-8 = volatile/exploratory)
    and predict that committed mode advantage is strongest at low hazard density.
    The Humphries framework predicts the interaction: commitment_advantage = f(stability),
    so the correct test is the stability x commitment interaction, not just the
    main effect. EXQ-125a tests only the high-hazard condition.

- id: ARC-030
  title: "The three BG-like loops require symmetric Go (approach) and NoGo (avoidance) sub-channels; pure NoGo architecture produces behavioral flatness."
  claim_type: architecture_hypothesis
  subject: architecture.approach_avoidance_symmetry
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-030@2026-03-29T21:16:41.149863Z"
      as_of: 2026-03-29
      verdict: hold_candidate_resolve_conflict/applied
  implementation_phase: v3
  evidence_quality_note: |
    Hold at candidate (2026-03-29): no new experiments this session. SD-010 dependency and
    approach-avoidance symmetry (Go sub-channel implementation) required before testing. No
    experimental evidence yet.
    EXQ-086 INCONCLUSIVE/bug (2026-03-30, re-run of 20260323): benefit_rate=0 despite 274
    buffered benefit events -- measurement bug in benefit_rate computation from buffer. Go
    sub-channel logging works (events recorded) but rate metric broken. Does not test ARC-030.
    Superseded by EXQ-086a (corrected benefit_rate computation, to be designed).
    EXQ-085m FAIL 0/4 (2026-03-30): benefit_eval_head in E3 (benefit proximity regression
    head trained jointly with E3). benefit_eval_r2_train=-8.6 (head diverges completely).
    benefit_per_ep=0 in both ENABLED/DISABLED conditions -- benefit events not occurring
    in 1-seed short warmup. ARC-030 approach-via-benefit-head not testable at this scale.
    Structural issue: benefit signal too sparse during warmup; head cannot learn proximity
    regression without sustained benefit contact. ARC-030 requires a richer benefit signal
    or pre-trained benefit head before E3 integration is testable.
    Goal-lift battery (2026-04-01): EXQ-183 FAIL 2/4 (BCE shared selector): benefit_eval_auc
    =0.538 (barely above chance); combined/random=0.73x. benefit_eval_head learns but does not
    discriminate effectively. EXQ-186 FAIL 1/4 (hybrid benefit+harm): C2 PASS (combined/
    harm_only=1.55x -- adding benefit DOES improve over harm-only), but C1 FAIL (combined/
    random=0.25x), C3 FAIL (harm 1.4x worse), C4 FAIL (combined < benefit_only -- harm
    avoidance is counterproductive). The C2 PASS is a positive signal for ARC-030: the shared
    selector does produce approach-avoidance integration lift vs pure avoidance. But the
    absolute performance is far below random baseline, suggesting 1-step greedy action selection
    on 10x10 grid is fundamentally insufficient. Oracle ceiling test (EXQ-182a) is the gate.
    EXQ-138a FAIL 2/5 x2 runs (2026-04-03): Go/NoGo symmetry discriminative pair FAIL.
    Run 1 (T04:36): criteria_met=2/5. Run 2 (T09:04): criteria_met=2/5.
    delta_benefit_rate=+0.00002 (threshold needs >=0.002). benefit_rate_go~=0.00039 (needs >=
    0.002). C3 PASS (harm preserved). C5 PASS (benefit_buf_go >= 100 confirmed). The Go channel
    is wired and benefit events are recorded (n_benefit_buf_go ~750/seed) but rate is
    ~100x below threshold. Consistent result across both runs: Go sub-channel does not produce
    measurable behavioral lift in benefit acquisition at V3 scale with current training
    budget (warmup=400 eps). Weakening evidence for ARC-030 at this scale.
    EXQ-226 PASS 2/2 criteria (2026-04-04): FIRST PASS for ARC-030. COMBINED (harm+goal CEM)
    vs HARM_ONLY: benefit_ratio=1.319 (>= 1.2 threshold), harm_ratio=0.762 (<= 1.3 ceiling),
    avg_goal_norm=0.360 (precondition met). Key caveat: aggregate PASS driven by seed 13 where
    HARM_ONLY collapsed (0 resources, 29.5% harm); COMBINED rescued it (0.12 resources, 14.2%
    harm). Seeds 42 and 7 show COMBINED roughly equivalent to HARM_ONLY. The result supports
    ARC-030's structural prediction -- behavioral collapse under pure NoGo is a stochastic
    attractor, and the Go channel prevents it -- but the effect is intermittent, not universal.
    Critical design finding: fixed-weight CEM scoring (not a learned benefit_eval head) was
    what finally worked. All 5+ prior failures were head-learning failures, not architecture
    failures. V4 implementation of approach-avoidance should start from CEM scoring.
    GOVERNANCE META (2026-04-06): Illusory conflict resolution risk. Go/NoGo symmetry untested
    with real z_goal. Approach sub-channel cannot be evaluated when goal signal is absent.
    Non-contributory FAILs (EXQ-235, EXQ-086 measurement bug) removed from scoring but the
    remaining supports are from specialized selectors, not the full symmetric gate. Status:
    pending_retest_after_substrate (gate: EXQ-247 + SD-015 resource indicator encoding).

    [2026-05-08 governance]: EXQ-536a + EXQ-536b diagnostic non_contributory. Per user review, previous diagnostics revealed two crossed wiring paths for goal seeding -- neither was properly wired. Diagnostic results reflect substrate breakage, not mechanism falsification. Re-test required after wiring is canonicalized.
    [2026-08-07 governance, ceiling-audit ACTIONABLE finding]: check_substrate_ceiling_audit.py flags
    "ceiling-may-have-lifted" -- MECH-307 (the substrate this claim's retest gate was blocked on) is
    now implemented. User-confirmed: flag for re-queue/retest (chipped at session close, not
    governance's own build). pending_retest_after_substrate stays true until that retest actually
    runs and resolves it.
    [2026-08-07 CORRECTION, chip-20260807-arc030-mech307-retest]: the "ceiling-may-have-lifted"
    reading above was PREMATURE. The retest chip's own STOP-CHECK (confirm MECH-307 genuinely
    wired, not just claims.yaml-implemented) found MECH-307 unreachable through
    REEConfig.from_dims() -- all 12 params are absent from its signature and silently dropped into
    **kwargs, so all 84 experiment drivers that request MECH-307 via from_dims (including the
    scaffolded lineage this retest is built on: V3-EXQ-603q/866a/866b) ran with it entirely OFF.
    See evidence/planning/mech307_from_dims_unreachable_2026-08-07.md. The chip correctly did NOT
    queue the retest (setting sub-flags by hand would change the substrate under the COMBINED arm's
    G0 non-degeneracy precondition -- 866a already failed G0 on a smaller deviation); routed instead
    to /implement-substrate (chip-20260807-mech307-fromdims-wiring) to fix the from_dims wiring
    gap first. pending_retest_after_substrate STAYS true; genuinely still blocked, now on a sharper,
    correctly-identified blocker (from_dims reachability, not claims.yaml implementation status).

    [2026-08-08 governance]: chip-20260807-mech307-fromdims-wiring is DONE (landed ree-v3
    d63f13b7+2c682a91, REE_assembly 6e5c3dea0c; contract test_mech307_from_dims_wiring.py, 19
    tests). MECH-307 is now genuinely reachable via from_dims. Next step is still NOT the full
    COMBINED-vs-NOGO_ONLY retest -- a readiness step first: re-run the 603q/866b scaffolded
    configuration with MECH-307 genuinely ON and confirm the G0 non-degeneracy precondition still
    clears (866a already failed G0 on a smaller deviation, so this is not a formality). Chipped
    (chip-20260808-arc030-mech307-readiness). pending_retest_after_substrate stays true.
  depends_on:
    - ARC-021
    - MECH-069
    - INV-032
    - SD-010
  epistemic_category: substrate_ceiling
  pending_retest_after_substrate: true
  what_would_answer: |
    ARC-030 is the most BASIC of three related open questions on this
    substrate (ARC-030: does Go/NoGo channel SYMMETRY itself produce any
    behavioural lift over pure-NoGo, at all; INV-034: is prospective
    goal-MAINTENANCE specifically necessary for ethical agency; SD-015:
    does the z_resource encoder specifically enable navigation). Do not
    re-derive the ablation design here -- see INV-034's own
    what_would_answer for the two-arm approach-vs-avoidance-only protocol
    and SD-015's for the representation/behaviour split; ARC-030 draws on
    the SAME substrate family (scaffolded_sd054_onboarding) and the SAME
    non-degeneracy gate, but asks the prior, narrower architectural
    question those two do not: whether adding a symmetric Go (approach)
    sub-channel to the existing NoGo (SD-010/harm) channel measurably
    changes behaviour vs a pure-NoGo control, independent of whether that
    change constitutes "agency" (INV-034) or is attributable specifically
    to the resource encoder (SD-015).

    WIRING STATUS: the "two crossed wiring paths for goal seeding, neither
    properly wired" defect named in the 2026-05-08 governance note is
    RESOLVED. V3-EXQ-247 (2026-04-06/07, the note's own named gate) itself
    FAILED/was non_contributory on both runs -- z_goal_norm=0.0 across
    every condition -- which is the same defect the 2026-05-08 note
    diagnosed, not a fresh finding. The fix path since then: GAP-1
    (MECH-307 split-channel substrate, landed 2026-05-11, readiness
    V3-EXQ-540g PASS 2026-05-15) and GAP-7/SD-057 (object-bound
    incentive-salience layer L2-L9, landed 2026-06-04 through 2026-06-15)
    together replaced the dead seeding path. GAP-2 (the SD-049 Phase-2
    behavioural line) closed 2026-06-15 on V3-EXQ-514o PASS: object-bound
    wanting!=liking dissociation 0.80 (>=0.6), goal_resource_r 0.93-0.96,
    z_goal now fires during real foraging under the SAME
    scaffolded_sd054_onboarding substrate. BUT: no run since V3-EXQ-536a/
    536b (2026-05-08) has tagged ARC-030 as a claim_id, and no Go/NoGo
    symmetry discriminative pair (the EXQ-138a/226 design) has been re-run
    on the corrected wiring. ARC-030 itself is UNTESTED on the fixed
    substrate, not merely awaiting a fresh interpretation of old evidence.

    MANDATORY non-degeneracy precondition (shared with INV-034/Q-021, same
    substrate family): FULL-arm resource_visit_rate must exceed RANDOM
    baseline by >=0.05 on >=2/3 seeds, using a harness/curriculum window
    that avoids the hazard-heavy stretch V3-EXQ-866/866a found depresses
    z_goal (866b PASS 2026-08-03 confirms this is curriculum-specific, not
    a substrate-wide regression -- see INV-034's what_would_answer -- so
    the precondition is satisfiable, just not on that stretch). A run
    failing this gate self-routes substrate_not_ready_requeue, not an
    ARC-030 verdict.

    Once non-degeneracy clears, the ARC-030-SPECIFIC retest is the Go/NoGo
    symmetry discriminative pair (EXQ-138a/226 design, re-run on the
    corrected wiring): COMBINED (Go+NoGo, real z_goal active via SD-057)
    vs NOGO_ONLY (Go channel ablated), matching EXQ-226's original criteria
    (benefit_ratio_COMBINED/NOGO_ONLY >= 1.2; harm_ratio <= 1.3 ceiling;
    avg_goal_norm >= the C6-style floor of 0.4 used elsewhere in this
    substrate family, confirming the Go channel is actually receiving a
    live goal signal and not retesting the pre-514o dead-seeding failure
    mode).

    CROSS-SEED CONSISTENCY REQUIREMENT (the illusory-conflict governance
    note, 2026-04-06): EXQ-226's aggregate PASS was driven by a single seed
    (13) where NOGO_ONLY collapsed and COMBINED rescued it, while two of
    three seeds showed COMBINED roughly equivalent to NOGO_ONLY -- an
    unrepresentative-seed PASS, not a general one. A retest PASS does not
    count as confirming ARC-030 unless the benefit_ratio lift clears
    threshold on >=2/3 seeds INDEPENDENTLY (not via one seed's NOGO_ONLY
    collapse), consistent with how INV-034/SD-015 both require >=2/3-seed
    criteria rather than aggregate scoring.

    CONFIRMING signature: with non-degeneracy and cross-seed consistency
    both held, COMBINED beats NOGO_ONLY on the EXQ-226 criteria on >=2/3
    seeds INDIVIDUALLY (not just in aggregate).

    FALSIFYING signature: with non-degeneracy held and a live z_goal signal
    confirmed (avg_goal_norm clears floor, ruling out the pre-514o
    dead-seeding confound), COMBINED does not beat NOGO_ONLY on >=2/3
    seeds -- i.e. the symmetric Go channel adds no measurable behavioural
    lift over pure-NoGo even once the channel itself is known to be live
    and receiving signal. This is a genuine test now that
    EXQ-247/536a/536b's dead-seeding confound is retired -- earlier FAILs
    (EXQ-085m/138a/183/186) could not distinguish "no Go effect" from "no
    Go signal," and this design can.

    What would NOT answer it: any run predating the 2026-06-15 GAP-2
    closure (dead-seeding confound not yet retired); any run without a
    live z_goal check (avg_goal_norm floor) alongside the behaviour metric;
    any aggregate-only PASS without the per-seed breakdown the
    illusory-conflict note requires.
  location: docs/architecture/approach_avoidance_symmetry.md#arc-030
  source:
    - evidence/planning/thought_intake_2026-03-22_approach_avoidance_drives.md
  notes: >
    The biological BG model motivating ARC-021 has explicit D1 (Go/approach) and D2
    (NoGo/avoidance) pathways in competition within each loop. REE's current formulation
    specifies NoGo in architectural detail (nociceptive stream SD-010, agency comparator
    MECH-095, precision-to-commitment wiring ARC-016) but does not build out Go. Without
    approach drives, the gradient minimum under harm-avoidance-only training is quiescence:
    the agent that does nothing accrues no harm signal, no attribution, never crosses a commit
    boundary, and appears by its own error metrics to be performing optimally. The residue
    field requires positive attractors alongside harm repellers or the hippocampal planner has
    no terrain to navigate toward. The appetitive stream (goal-approach MECH-112, novelty
    MECH-111, self-maintenance MECH-113) must be structurally co-designed with the nociceptive
    stream (SD-010). Registered 2026-03-22.
    Literature: Cox et al. 2015 (NeuroImage, DOI 10.1016/j.neuroimage.2014.12.070) -- human
    PET confirms D1 binding predicts approach learning, D2 binding predicts avoidance learning;
    "bidirectional modulatory role for striatal dopamine via segregated D1 and D2 pathways."
    Hikida et al. 2012 (PNAS, DOI 10.1073/pnas.1220358110) -- D1 activation (direct pathway)
    = reward learning; D2 inactivation (indirect pathway) = aversive learning; pharmacological
    dissociation confirmed. Bariselli et al. 2018 (Brain Res, DOI 10.1016/j.brainres.2018.10.009)
    -- CRITICAL: COMPETITIVE MODEL: dMSNs and iMSNs are tuned to the SAME actions and compete
    to determine behavioral response. NOT two separate action inventories. Both pathways evaluate
    the same trajectory proposals from hippocampus; the commit threshold (MECH-106, ARC-016) is
    the competition balance point.
    Architecture shape: ARC-030 implementation is NOT two parallel pipelines selecting different
    trajectories. It is dual evaluation of the SAME hippocampal trajectory proposals:
    harm channel (SD-010/NoGo) and goal channel (MECH-112/Go) evaluate each proposal; the
    precision-weighted balance determines commitment. MECH-112 and SD-010 are symmetric inputs
    to the same E3 commitment gate. This extends MECH-106 (commitment threshold modulated by
    valence) and closes the ARC-016 precision-to-behavior wiring gap.
    Full synthesis: evidence/planning/literature_synthesis_2026-03-22_approach_avoidance_drives.md

- id: ARC-031
  title: "HippocampalModule navigates z_self trajectory space (deliberation sequences) in addition to z_world action-object space O, enabling the agent to plan its own deliberation."
  claim_type: architecture_hypothesis
  subject: hippocampal.z_self_trajectory_navigation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  phase_provenance: derived
  phase_derived_from: MECH-205
  depends_on:
    - SD-004
    - MECH-113
    - MECH-114
    - MECH-092
    - ARC-028
    - INV-033
  location: docs/architecture/approach_avoidance_symmetry.md#arc-031
  source:
    - evidence/planning/thought_intake_2026-03-23_epistemic_self_monitoring.md
  notes: >
    Current HippocampalModule (SD-004 / Q-020 resolved) navigates action-object space O
    in the z_world domain -- it maps and plans through the external world. ARC-031 extends
    this to z_self trajectory space: planned sequences of internal self-state transitions
    constituting deliberation, attention-shifting, or rumination. A z_self trajectory
    encodes "I will first resolve uncertainty in domain X (shift from high to low D_eff),
    then attend to Y, then commit." E3 evaluates these trajectories by self-coherence cost,
    hypothesis-tag integrity cost, and self-maintenance cost -- not harm cost.
    Biological basis: cognitive map literature shows hippocampal place/grid cells represent
    not just spatial location but position in any continuous abstract space. Behrens et al.
    2018 (Neuron, DOI 10.1016/j.neuron.2018.09.040): grid/place cell algorithms map
    social cognition and conceptual structure -- navigation algorithms apply to abstract
    "thought space." Schapiro et al. 2017 (J Cogn Neurosci, DOI 10.1162/jocn_a_01111):
    hippocampal representations encode temporal sequences in abstract domains.
    Connection to ARC-028 (hippocampal->BG completion coupling): if hippocampus generates
    both external and deliberation trajectory proposals, the BG completion signal fires on
    BOTH action completion and deliberation completion. ARC-031 extends the completion
    coupling to include internal cognitive closure events.
    MECH-113 D_eff monitoring is the navigation PREREQUISITE: hippocampal self-navigation
    is only meaningful if z_self is coherent enough to define a stable navigable space.
    High D_eff = dispersed self-model = no reliable position in z_self space = navigation
    degenerates. MECH-114 D_eff commit gate must be in place before ARC-031 is testable.
    MECH-092 offline replay extended to z_self trajectories: during quiescent E3 cycles,
    consolidation of deliberation sequences (not just world-action sequences) occurs.
    V4 scope: requires waking architecture (EXQ-072..075) validated before implementation.
    See also sleep mechanisms (SWR consolidation of place-reward associations -- the
    self-trajectory analog would be deliberation-outcome associations consolidated in sleep).
    Registered 2026-03-23.

- id: ARC-027
  title: "Harm stream runs as a parallel thalamic pathway independent of world latent space."
  claim_type: architectural_commitment
  subject: harm_stream.parallel_thalamic_pathway
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  depends_on:
    - ARC-017
    - ARC-003
  location: docs/architecture/sensory_stream_tags.md#arc-027
  source:
    - docs/architecture/sensory_stream_tags.md
  evidence_quality_note: |
    EXQ-027b PASS (2026-03-19): Reafference diagnostic confirms SD-007 hurts E3
    calibration when applied to z_world (correction_delta=-0.045, harm_pred_std
    drops from 0.108 to 0.008). Over-correction explained by applying reafference
    to a fused stream that includes the nociceptive signal. If z_harm were separate,
    reafference correction to z_world would not strip the harm signal.
    EXQ-044 FAIL (2026-03-19): SD-003 attribution collapsed again (world_forward_r2=0.0,
    all causal_sig ≈ 2.7e-5, E3 harm_eval=0.5 uniform). Sequential training fix was
    insufficient because the comparison is being made in z_world, which conflates
    nociceptive and exteroceptive content. Attribution needs to operate on the harm
    stream specifically, not the mixed world latent.
    EXQ-045 FAIL (2026-03-19): MECH-102 advantage ladder reversed (none=0.067 >
    approach=0.049 > contact=0.044). Ethical advantage is highest in safe states
    because action-space divergence is largest before hazard contact. Consistent
    with the harm-stream architecture: maximum ethical leverage is in the approach
    gradient, not at contact — which requires the harm stream to be available
    during trajectory planning, not only at contact events.
    EXQ-047 FAIL (2026-03-19): SD-005 split latent improves attribution by 82%
    (0.034 vs 0.019) but calibration gain (21%) falls below 5pp threshold. Split
    along z_self/z_world axis is insufficient — nociception needs to be a third
    separate stream, not subsumed in z_world.
    All four experiments converge: the current architecture fuses nociceptive content
    into z_world and this conflation is the root cause of SD-003/SD-007/MECH-102
    failures. ARC-027 implementation is prerequisite for SD-003 to work.
    EXQ-058b ARC-027 PASS (2026-03-20, 5/5 criteria): Foundational V3 validation.
    HarmEncoder (2-layer MLP, harm_obs → z_harm, trained with direct MSE supervision
    on hazard/resource proximity labels) achieves: mean_harm_none=0.014,
    mean_harm_approach=0.726, mean_harm_contact=0.987; Pearson_r=0.973 (threshold 0.85);
    calibration_gap=0.712 (threshold 0.40); harm_pred_std=0.417 (not collapsed; threshold
    0.05). Separate pathway confirmed: z_harm is learned from harm_obs alone, not from
    z_world; the encoder generalises across event types with strong rank ordering.
    This is the first clean demonstration that the harm stream architecture works when
    architecturally isolated. Prerequisite validation for SD-003 + SD-010 joint tests.
    EXQ-056c SD-010 PASS (2026-03-21, 4/5): HarmEncoder retest with reafference isolation
    confirmed. Pearson r_z_harm=0.914 (threshold 0.85). Harm stream produces clean proximity
    signal distinct from z_world, not subject to reafference correction. Confirms
    architectural separation is robust across experimental variations.
  notes: >
    The HARM stream must be implemented as a parallel sensory pathway with its
    own sensory input and training signal, not derived from z_world or E1
    processing. It receives direct thalamic input (fast, coarse threat detection
    — the "low road") feeding the amygdala-equivalent harm-detection system
    before cortical world-model processing occurs. Consequences: (1) harm_eval
    is an input to E3, not a head inside E3; (2) SD-007 reafference correction
    applies at the HARM stream level, not at z_world — EXQ-027b over-correction
    is explained by applying reafference to the wrong signal; (3) SD-003
    attribution compares HARM stream against E2 counterfactual rollout, not
    internally within E3; (4) EXQ-043/044 calibration collapses are explained:
    training a sensory detection system through a planning pipeline is
    architecturally incorrect. Biological basis: spinothalamic tract provides
    fast nociceptive input direct to amygdala (VPM/Po → amygdala) in parallel
    with slower cortical route (thalamus → S1 → insula → amygdala). LeDoux
    (1996) "low road" — amygdala rapid threat response depends on direct pathway,
    anatomically distinct from cortical world-model processing.
    Note: previously misregistered as ARC-025 (ID collision with
    architecture.three_engine_irreducibility). Corrected to ARC-027 (2026-03-19).

# ── Design Decisions (SD) ────────────────────────────────────────────────────

- id: SD-001
  title: "CEM-based trajectory search belongs in HippocampalModule, not E2."
  claim_type: design_decision
  subject: hippocampal_module.cem_trajectory_search_placement
  polarity: asserts
  status: resolved
  live_status:
    reading: resolved
    as_of: 2026-07-11
    needs_review: false
  status_note: >
    CEM-based trajectory search was misplaced in E2. Resolved V2: CEM moved to
    HippocampalModule. E2 is now a pure transition model f(z_t, a_t) → z_{t+1}.
  depends_on: []
  location: docs/architecture/hippocampal_systems.md

- id: SD-002
  title: "E1 associative prior must be wired into HippocampalModule for terrain-informed rollout search."
  claim_type: design_decision
  subject: hippocampal_module.e1_associative_prior_wiring
  polarity: asserts
  status: resolved
  live_status:
    reading: resolved
    as_of: 2026-07-11
    needs_review: false
  status_note: >
    E1 associative prior must be wired into HippocampalModule terrain search
    (E1→HippocampalModule mutual constitution). Resolved 2026-03-06.
  depends_on: [SD-001]
  location: docs/architecture/hippocampal_systems.md

- id: SD-003
  title: "Self-attribution via counterfactual E2: causal signature = E2(z_t, a_actual) minus E2(z_t, a_counterfactual)."
  claim_type: design_decision
  subject: self_attribution.counterfactual_e2_pipeline
  polarity: asserts
  status: superseded
  live_status:
    reading: superseded
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_zworld-integration-cluster_2026-06-06#V3-EXQ-145"
      as_of: 2026-06-06
      verdict: non_contributory/substrate_ceiling
  superseded_by: [MECH-256, SD-029]
  superseded_utc: 2026-04-18
  supersession_note: >
    Superseded 2026-04-18 by MECH-256 (general single-pass forward-model comparator
    as the mechanism for self-attribution) with SD-029 as the concrete z_harm_s
    instantiation. Reasons: (1) 28 FAILs accumulated across V2+V3 iterations with
    no PASS on the two-pass counterfactual architecture since the z_world -> z_harm_s
    stream migration; (2) biological precedent gap -- Frith 2000, Shergill 2003, and
    Blakemore 1998 evidence a single-pass comparator (residual = observed -
    E2(prev_state, a_actual)), not a two-pass counterfactual; (3) three-pull literature
    synthesis 2026-04-18 (comparator/evaluator/mode-distinction, 14 entries) converged
    on single-pass comparator as the biologically grounded architecture, with one
    E2 substrate read in two modes (MECH-257). The counterfactual E2 rollout remains
    conceptually coherent but is not the primary biological attribution mechanism;
    SD-029 captures what the literature actually evidences. ARC-033 forward model
    component is carried forward unchanged. Existing evidence chain (EXQ-030b/115/166a/
    195/353/431) remains in the historical record; new V3 evidence accrues to SD-029.
  status_note: >
    EXQ-030b PASS (2026-03-18): validated on z_world pipeline -- causal_sig =
    E3(z_world_actual) - E3(z_world_cf), world_forward_r2=0.947, attribution_gap=0.035,
    correct sign structure. This validation predates SD-010 full wiring (E3 now takes
    z_harm, not z_world). The E2-counterfactual pipeline architecture is confirmed sound
    but the target stream has changed. Post-SD-011, the correct pipeline is:
    z_harm_s_cf = E2_harm_s(z_harm_s, a_cf); causal_sig = E3(z_harm_s_actual) -
    E3(z_harm_s_cf). EXQ-093/094 confirmed that HarmBridge(z_world -> z_harm) is
    infeasible (bridge_r2=0 architectural, SD-010 makes z_world perp z_harm).
    ARC-033 + SD-011 implementation required before SD-003 can be retested.
  evidence_quality_note: >
    Literature pull 2026-03-28 (4 entries, lit_conf=0.859, all supports) identified two
    caveats that must be resolved before V3 retest. (1) Biological precedent gap: Frith 2000
    and Shergill 2003 directly evidence the single-pass comparator (E2(a_actual) - observed),
    not the two-pass counterfactual SD-003 implements. The two-pass extension is an
    inferential step with no direct biological evidence. (2) Interventional training
    requirement: Scholkopf 2021 establishes that a regression-trained forward model learns the
    correlational distribution P(z_harm_s | z_t, a), not the interventional distribution
    P(z_harm_s | do(a)). In confounded states (where agent presence correlates with ambient
    harm), E2_harm_s trained on observational trajectories alone will produce a biased
    causal_sig. ARC-033 (E2_harm_s) implementation must include counterfactual perturbation
    signal in training -- not only observational rollouts -- for SD-003 to be valid.
    EXQ-115 tests the new z_harm_s pipeline; a follow-up varying interventional vs
    observational training is needed if EXQ-115 passes.
    EXQ-166a FAIL 4/6 (2026-03-30): obs-space forward model (Approach B, 4 seeds, 80-ep P0).
    obs_fwd_r2 bimodal: seeds 42/seed3=0.925/0.885 (strong convergence), seeds 1/2=0.0 (P0
    encoder quality failure despite extended warmup). C2 delta_approach=0.0006 (threshold 0.03):
    near-zero causal gap even for converging seeds. Architecture diagnostic: obs-space forward
    model learns general obs dynamics (position-change dominated) without selective pressure on
    harm-causal structure. Loss function indifferent to harm-relevant obs dims (diluted in 51-dim
    obs by position/resource dims). Proposed fix (EXQ-166b): z_harm_s latent forward model with
    reconstruction branch (autoencoder) to prevent identity collapse -- keeps learning focused on
    harm-specific structure. C2 failure is architectural, not a training budget issue.
    EXQ-195 FAIL (2026-04-04): attribution_gap=-0.044 all 4 seeds (C1 FAIL). Classified
    non-contributory (2026-04-06 governance): negative gap reflects missing SD-003 full
    pipeline wiring (counterfactual comparison not correctly structured), not claim failure.
    harm_forward_r2=0.914 (ARC-033 component working). Substrate gap, not claim refutation.
    EXQ-353 (2026-04-15, PASS, supports): Interventional vs observational training
    distinguished. SD-003 tagged as supports alongside ARC-033 and SD-013. Confirms
    the counterfactual pipeline architecture is differentiable via training signal.
    EXQ-431 (2026-04-17, full run, FAIL): Originally tagged does_not_support for SD-003.
    Governance reclassified to non_contributory (2026-04-18): agent_caused_hazard event
    counts catastrophically skewed (seed 42=1, seed 7=156, seed 13=2); C3 learned_lift
    fails on sample starvation, not mechanism refutation. C1 forward_r2 PASS (0.998/0.988/0.998)
    confirms ARC-033 forward model sound. Dry-run predecessors (T142339Z, T143459Z) marked
    superseded. SD-003 adjudication pending a re-run with event-density controls that guarantee
    a minimum agent_caused_hazard count per seed.

    [2026-07-29 governance, V3-EXQ-032d supports (also MECH-102/ARC-024)]: PASS 5/5, corrected
    re-run of V3-EXQ-032b (which never trained its perceptual encoder -- see MECH-102). Adds to
    the historical evidence chain per this claim's own supersession_note convention (superseded
    2026-04-18 by MECH-256/SD-029; new V3 evidence accrues to SD-029, not reactivating SD-003).
    No status change.
  depends_on: [SD-005, SD-007, SD-008, MECH-071, SD-011, ARC-033, SD-013]
  location: docs/architecture/sd_003_experiment_design.md

- id: SD-004
  title: "Action objects as hippocampal map backbone, enabling long-horizon planning beyond step-level trajectories."
  claim_type: design_decision
  subject: hippocampal_module.action_object_map_backbone
  polarity: asserts
  status: implemented
  live_status:
    reading: implemented
    as_of: 2026-07-11
    needs_review: false
  status_note: >
    E2 produces compressed action-object representations; HippocampalModule
    navigates action-object space O rather than raw z_world. Enables planning
    horizons far beyond raw state space. SD-004 and SD-005 must be co-designed:
    action objects require z_world to exist.
  evidence_quality_note: |
    Behavioural validation rests on EXQ-114 PASS 4/4 (2026-03-28), 99.2% harm reduction
    when MAP_NAV is active vs MAP_ABLATED. EXQ-397 / EXQ-397c x2 internal-probe FAILs
    (2026-04-19/21) reclassified non_contributory 2026-04-22 -- the hippo_quality_gap
    metric is sign-inverted regardless of training-on-minimum-residue fix; probe broken,
    not the substrate. SD-004 stands as implemented.
    V3-EXQ-809 (2026-07-23, action-object init-invariance prior probe; diagnostic; confirmed
    failure_autopsy_V3-EXQ-809_2026-07-25; PROMOTES NOTHING) -> does_not_support (RATIONALE
    ONLY). This is the "cheaper prior probe" the SD-080 scoping spike
    (action_object_invariance_spike_2026-07-22.md Sec 4.2) prescribed. It does_not_support
    SD-004's stated efficiency / semantic-grounding RATIONALE ("E2 produces COMPRESSED,
    consequence-structured action-object representations") -- the action-object head is a frozen
    random projection receiving zero gradient, so O is state-invariant, not the learned
    world-effect compression SD-004 specifies. CRITICALLY this does NOT weaken SD-004's
    BEHAVIOURAL claim: EXQ-114 PASS 4/4 (99.2% harm reduction under MAP_NAV) is preserved, and
    SD-004 STAYS implemented -- the probe relocates what the behavioural evidence can be
    attributed to (a governance question), it does not impeach it. The same run SUPPORTS SD-080
    (the frozen-AO-head claim). Applied /governance 2026-07-25 (session
    angry-ardinghelli-d99740-gov, user-approved). Routing: chips the Sec 4.2 3-arm capability
    falsifier (queue-experiment).
    [2026-07-26 governance, V3-EXQ-817 non_contributory; failure_autopsy_batch-793a-817-819_2026-07-26,
    confirmed]: the Sec 4.2 falsifier (817) ran. Self-route substrate_not_ready_requeue is a MISNOMER --
    the AO head DID train (delta_l2_from_frozen ~1.3-1.47, not the 0.0 collapse-onto-ARM_0 an earlier note
    here misread it as). The delta-prediction grounding objective simply did not induce the targeted
    representational change (M5/M6 thresholds not met, high seed variance) so grounding_took=false gated
    the load-bearing behavioural criterion before it could run -> non_contributory, does NOT weaken SD-004's
    behavioural claim (EXQ-114 stands). Re-queue 817a with a stronger grounding objective is recommended.
    [2026-07-29 governance, V3-EXQ-817a weakens (SD-004 only; SD-080: non_contributory);
    failure_autopsy_batch-822a-826-817a-827_2026-07-26, confirmed]: 817a's redesigned objective DID achieve
    a consequence-structured, state-dependent O this time (r2 0.98->0.72, clean paired + shuffled controls),
    resolving 817's competence_implementation_gap. But grounding this representation produced NO behavioural
    benefit (harm_rate flat/nominally worse vs frozen baseline) -- weakening SD-004's specific stated
    rationale that this compressed representation drives measurably better long-horizon planning/harm-
    avoidance. Does NOT weaken SD-004's core behavioural claim (EXQ-114's 99.2% harm reduction under
    MAP_NAV stands) -- the planner-side coupling from representation quality to action choice is a
    separate, still-open question. SD-004 STAYS implemented (status unchanged; rationale-only weakening,
    same treatment as the 2026-07-25 V3-EXQ-809 finding above).
  depends_on: [SD-005]
  location: docs/architecture/sd_004_sd_005_encoder_codesign.md
  cross_reference_note: >
    SEE SD-080 (registered 2026-07-22). A scoping spike measured that
    E2.action_object_head receives zero gradient from every REE training path, so the
    action-object space O this claim's map backbone navigates is a frozen random
    projection fixed at initialisation -- 99.5% of its variance explained by the action
    label alone, pairwise action distances varying <1% across 120 world states, and
    parameters bit-identical after 40 warmup episodes. SD-080 therefore asserts that
    SD-004's stated "compressed action-object representation" rationale is NOT
    instantiated in the live substrate. This note is a pointer only: SD-004's status
    (implemented) and its EXQ-114 behavioural evidence are deliberately left unchanged
    here, because deciding what that evidence supports once O is known to be
    state-invariant is a governance decision, not a registration one. Full measurements:
    evidence/planning/action_object_invariance_spike_2026-07-22.md.

- id: SD-005
  evidence_quality_note: |
    [multi_sense_audit_2026_05_08_cleared] EXQ-047i/j/k: audit re-examined the multi-sense flag on the 047 cluster and confirmed it is a deliberate probe pattern (sense E_t -> step -> sense E_{t+1} -> step), giving exactly 1 sense per env state. Not the EXQ-471 cohort bug. Direction and confidence unchanged.

    EXQ-047 (2026-03-20): Split provides 82% attribution improvement (gap: 0.035 split vs
    0.019 unified) and meaningful calibration gain (0.1915 vs 0.1577), but calibration
    improvement (3.4pp) falls below 5pp C1 threshold -> FAIL 4/5. C1 failure is partly
    attributable to nociceptive contamination in z_world: both split and unified z_world
    conditions still carry harm proximity signals that conflate calibration metrics. True
    calibration advantage of split should increase once SD-010 (separate z_harm) removes
    nociceptive contamination. Attribution advantage (82%) is real and unambiguous. SD-005
    split is prerequisite for clean SD-010 implementation.
    EXQ-047d (2026-03-22, x2 runs): DESIGN FAILURE -- not a genuine negative. Probed
    frozen representations from training with no separation objective. Both z_self and
    z_world carried nearly equal action information (split_action_dissociation=0.008;
    need >0.10). Root cause: without adversarial pressure the encoder has no incentive to
    concentrate action info in z_self and expel it from z_world. The two runs are
    byte-identical (same seed, deterministic frozen probe) -- 1 effective data point, not
    2. Evidence direction "weakens" on both runs is a tagging artefact of the design
    failure; these results should not be counted as genuine disconfirmation of SD-005.
    EXQ-047e FAIL (2026-03-23, 1/5): adversarial GRL on z_world. action_dissociation=0.055
    (threshold 0.10 not met). Post-hoc GRL (80 episodes) too brittle; probe/encoder
    alternating update race is unstable. DESIGN FAILURE -- not a genuine disconfirmation.
    EXQ-047f FAIL x2 (2026-03-23, 2/5 each): orthogonal split with cosine penalty.
    action_dissociation=-0.108/-0.065 (NEGATIVE), orth_cos_mean_eval=0.002-0.018 (near zero).
    DESIGN FAILURE: geometric orthogonality is the WRONG test. Functional specialisation
    from differential loss routing does NOT require geometric orthogonality -- there are
    legitimate shared dimensions (top-down z_beta, blurry body-world boundary near hazards).
    Cosine constraint penalises both channels symmetrically, producing two decorrelated noisy
    representations rather than two specialised ones.
    EXQ-078 PASS (2026-03-23, 3/3): behavioural test -- split z_world produces gap_approach
    0.197 vs unified 0.147 (delta=0.050 > 0.02 threshold). Split IS behaviourally better.
    This confirms SD-005 benefit at the functional level even though geometric probes failed.
    EXQ-047g FAIL/weakens, decision=retire_ree_claim (2026-03-23, ran ~64min after an earlier
    superseded/deduped emission): the correct redesign -- functional cross-decoding probes
    (z_self -> action prediction acc vs z_world -> action prediction acc; z_world -> contact
    prediction acc vs z_self -> contact prediction acc; 400 warmup episodes, differential
    loss routing, no orthogonality constraint) -- DID run, and only 1/5 pre-registered
    criteria were met: action_dissociation_split=+0.085 (needed >0.10),
    contact_dissociation_split=+0.000 (needed >0.05), split-vs-unified action advantage
    NEGATIVE (-0.030 vs required +0.03). This is the corrected, non-design-failure test and
    it did not confirm functional specialisation.
    EXQ-113 FAIL/weakens, decision=retire_ree_claim (2026-03-28): behavioural double-
    dissociation at 6x6 scale (v3_exq_113_sd005_double_dissociation). 2/5 criteria met;
    gap_world_split=0.0095 (needed >=0.06); notes explicitly flag that the EXQ-078
    behavioural gap did not replicate at 6x6 scale, and that the 350->300 warmup-episode
    reduction is a plausible confound.
    EXQ-532 FAIL 2/3, latest run on file (2026-05-06): correlational latent-domain
    selectivity (v3_exq_532_sd005_latent_domain_selectivity). Result is specifically
    ASYMMETRIC: self_body_advantage=+0.126 (z_self DOES show self-specialisation) but
    world_obs_advantage=-0.032 (z_world does NOT show reciprocal world-specialisation --
    z_self predicts world observations as well as or better than z_world does).
    CONVERGENT PATTERN across all three corrected-design tests (047g, 113, 532; not design
    failures like 047d/e/f): z_self reliably specialises for self/action information;
    z_world does NOT reliably show reciprocal world-specific specialisation. No run since
    EXQ-532 (2026-05-06) has re-tested this specific question.
    Architecture note: E2.world_forward(z_world, action) -> z_world_next already implements
    an efference copy signal. Future architecture direction: use E2 prediction as the
    primary reafference signal (exafference = z_world_actual - E2(z_world_prev, action)),
    which would provide a stronger and self-improving separation mechanism. See SD-007 notes.
  title: "z_gamma split into z_self (E2 motor-sensory domain) and z_world (E3/Hippocampus domain)."
  claim_type: design_decision
  epistemic_category: standard
  subject: latent_stack.self_world_latent_split
  polarity: asserts
  status: implemented
  live_status:
    reading: implemented
    as_of: 2026-07-11
    needs_review: false
  status_note: >
    z_gamma split into z_self (E2 domain: motor-sensory, proprioceptive) and
    z_world (E3/Hippocampus/ResidueField domain: causal footprint, residue,
    moral attribution). SD-005 and SD-004 must be co-designed.
  depends_on: []
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: the arm under test must have differential loss
    routing genuinely active between E2 (z_self) and E3 (z_world) -- not a frozen
    post-hoc probe (EXQ-047d showed frozen representations carry near-equal
    action info in both channels, split_action_dissociation=0.008) and not a
    geometric-orthogonality penalty (EXQ-047f showed cosine constraints produce
    two decorrelated-but-unspecialised channels, orth_cos_mean_eval~0.002-0.018).
    Warmup must be adequate for specialisation to emerge under whatever objective
    is active (EXQ-047g/113 used 400/300 episodes; EXQ-113's own write-up flags
    the 350->300 reduction as a plausible confound for its null result). SD-008's
    alpha_world must be pinned and reported (0.9 across the three confirmed
    functional-specialisation runs). Cross-decoding probe samples must clear the
    pre-registered minimum (n_contact/n_approach >= 20/30) and, given EXQ-113's
    explicit finding that the EXQ-078 behavioural gap did not replicate at 6x6
    scale, environment scale must be stated and matched to the arm being compared
    against. A test failing any of these self-routes substrate_not_ready /
    design_failure and does not count as evidence either way.

    CONFIRMING signature: on a well-powered rerun (>=4 seeds; warmup and env
    scale matched to the best-supporting prior run) with differential loss
    routing confirmed active, BOTH sides of the dissociation clear the original
    pre-registered thresholds -- action_dissociation_split (self - world) > 0.10
    AND contact_dissociation_split (world - self) > 0.05 (EXQ-047g's own C1/C2) --
    and the behavioural gap_approach delta (split - unified) replicates at
    >= 0.02, EXQ-078's confirmed threshold, at more than one environment scale.
    Critically this must show BOTH-SIDED specialisation: EXQ-532 already shows
    self_body_advantage clearing its threshold (+0.126) while world_obs_advantage
    does not (-0.032) -- a one-sided result on a rerun does not confirm SD-005,
    it reproduces the existing falsifying pattern.

    FALSIFYING signature: already observed, three times, at increasing rigor and
    scale, all with differential loss routing active and adequate n. EXQ-047g
    (2026-03-23, functional cross-decoding, decision=retire_ree_claim): 1/5
    criteria, action_dissociation_split=+0.085 (<0.10), contact_dissociation_
    split=+0.000 (<0.05), split-vs-unified action advantage NEGATIVE (-0.030).
    EXQ-113 (2026-03-28, behavioural double-dissociation at 6x6 scale,
    decision=retire_ree_claim): 2/5 criteria, gap_world_split=0.0095 (<0.06
    threshold), delta_self=-0.0006 (purification not demonstrated, needed
    <=-0.02). EXQ-532 (2026-05-06, latest run on file, correlational latent-
    domain selectivity): FAIL 2/3, world_obs_advantage=-0.032 (z_self predicts
    world observations as well as or better than z_world does). The consistent,
    specific pattern across all three redesigns is: z_self shows genuine
    self-specialisation, z_world does NOT show reciprocal world-specialisation.
    A further well-powered rerun landing in this same band -- cross-decoding
    gaps within noise of zero on the world side specifically, gap_approach
    delta < 0.02 -- corroborates rather than merely repeats this finding and
    should be read as confirming SD-005's functional-specialisation
    justification does not hold on the E3/z_world side, independent of whether
    the z_self/E2 split itself is retained.
  location: docs/architecture/sd_004_sd_005_encoder_codesign.md
  heterogeneity_note: >
    2026-08-06 correction (thought-digestion, session thoughts-intakes-4fd18d):
    this note previously stated the conflict was driven entirely by EXQ-047d/e/f
    design failures and that EXQ-078 PASS + EXQ-047's attribution improvement
    "confirmed the split at the functional level," describing the high conflict
    ratio as an artefact of "iterative probe redesign, not scientific
    contradiction." That characterisation is now stale: EXQ-047g (the explicitly
    correct redesign this note used to describe as still queued) ran on
    2026-03-23 and FAILED (decision=retire_ree_claim), and two further
    independent, non-design-failure tests -- EXQ-113 (2026-03-28) and EXQ-532
    (2026-05-06, latest run on file) -- also FAILED, all three converging on the
    same specific asymmetric pattern: z_self reliably specialises for self/
    action information, z_world does NOT reliably show reciprocal world-specific
    specialisation. EXQ-047d/e/f remain genuine design failures (frozen-probe,
    GRL instability, wrong geometric-orthogonality test) and should not count as
    disconfirmation. But EXQ-047g/113/532 are the corrected designs, not further
    design failures, and all three returned the same negative result on the
    world-side of the dissociation specifically. Registry state as of this
    correction: supports=11, weakens=13, mixed=5, conflict_ratio=0.917,
    experimental_confidence~0.504 (near coin-flip), evidence_quadrant=
    plausible_unproven, latest-run-direction=weakens. The claim remains
    status=implemented (this note does not itself change status/confidence --
    that is a /governance decision) but the functional-specialisation
    justification for the z_world side of the split is not currently supported
    by the corrected-design evidence on file. See what_would_answer for the
    falsification condition and what a genuine confirming rerun would need to
    show.

- id: SD-006
  title: "E1, E2, and E3 run at characteristic rates (async multi-rate) rather than lockstep."
  claim_type: design_decision
  subject: control_plane.async_multirate_execution
  polarity: asserts
  status: implemented
  live_status:
    reading: implemented
    as_of: 2026-07-11
    needs_review: false
  status_note: >
    E1, E2, E3 run at characteristic rates rather than lockstep (phase 1:
    time-multiplexed). Asynchronous multi-rate execution reflects distinct
    biological loop timescales (theta/gamma/beta bands). See ARC-023.
  evidence_quality_note: |
    EXQ-052 FAIL (2026-03-20): C3 failure was a diagnostic bug — experiment tracked
    len(agent.theta_buffer._buffer) where correct attribute is _z_world_buffer;
    AttributeError silently caught, max_theta_buffer_size=0 always. All other criteria
    passed: E3 functional at 9× slower rate (cal_gap=0.846), e3_tick_ratio=0.109 ≈ 1/9.
    EXQ-052b fixes the counter. Functionally, multi-rate execution is working.
    EXQ-052b PASS (2026-03-21, 5/5): ThetaBuffer attribute fix confirmed. max_theta_buffer_size
    correctly tracked via _z_world_buffer. Multi-rate execution fully functional: E3 runs at
    ~1/9 the step rate of E1, theta buffer fills and batches E1 outputs before each E3 sample.
    All five criteria met. SD-006 implementation validated end-to-end.
  depends_on: [ARC-023]
  location: docs/architecture/control_plane_heartbeat.md

- id: SD-007
  title: "ReafferencePredictor provides perspective-corrected world latent by subtracting self-caused sensory change."
  claim_type: design_decision
  subject: encoder.perspective_corrected_world_latent
  polarity: asserts
  status: implemented
  live_status:
    reading: implemented
    as_of: 2026-08-07
    needs_review: true
    evidence:
      from: "v3_exq_027b_sd007_reafference_diagnostic_20260319T201702Z_v3"
      as_of: 2026-03-19
      verdict: "weakens/over-correction (pre-ARC-027/SD-010 fix, not retested since -- see what_would_answer)"
  epistemic_category: standard
  what_would_answer: |
    GOVERNANCE FLAG (2026-08-07 digestion pass): the live_status.evidence
    pointer this claim previously carried
    (failure_autopsy_zworld-integration-cluster_2026-06-06#V3-EXQ-145,
    verdict non_contributory/substrate_ceiling) was a stale cross-tagged
    artifact, corrected here. That autopsy's substrate_ceiling finding is
    about z_world's representational granularity generally (relevant to
    SD-008/Q-002), not about SD-007's own subtraction mechanism -- for
    V3-EXQ-145 specifically, the autopsy's own table says the run was
    blocked BEFORE selectivity was measured (reaf_r2=0.076 < 0.08 gate,
    "engineering/non_contributory... Does not retire SD-007 or SD-008").

    A discriminative-pair comparison of z_world_corrected against
    z_world_raw, run within episodes containing BOTH agent-caused sensory
    change (locomotion with no external event) AND independent
    world-caused change (an event the agent did not cause), with the
    ReafferencePredictor's own held-out R^2 gate passing first so the
    comparison is not confounded by an undertrained predictor.

    CONFIRMING if z_world_corrected shows measurably reduced self-caused
    variance/change relative to z_world_raw during pure locomotion (self-
    caused reafference is subtracted) while an externally-caused event
    still produces a comparable-or-greater signal in z_world_corrected
    than matched self-caused steps (world-caused change survives
    correction) -- i.e. correction preserves or improves an agent-caused-
    vs-world-caused discrimination margin rather than collapsing it.

    FALSIFIED if correction either (a) under-subtracts, leaving
    z_world_corrected as contaminated by self-motion as z_world_raw, or
    (b) over-subtracts, collapsing corrected-signal variance so broadly
    that externally-caused events become as hard (or harder) to detect
    than before correction -- the failure mode EXQ-027b found directly
    (correction_delta=-0.045, harm_pred_std 0.108->0.008), though that
    run pre-dates the ARC-027/SD-010 harm-stream separation the finding
    motivated (SD-010's HarmEncoder was introduced specifically "to
    resolve the EXQ-027b over-correction paradox") and has not been
    re-run since that fix landed.

    NON-DEGENERACY PRECONDITION: the episode set must contain both non-
    trivial agent-caused perspective change (informative delta-z_world_raw,
    sufficient for the predictor's own R^2 gate to be measurable) AND
    independent world-caused events, in a ratio balanced enough for
    discrimination to be measurable -- the zworld-integration-cluster
    autopsy separately found (V3-EXQ-177) that a 6.6:1 event imbalance
    made discrimination "near-unmeasurable" even with an otherwise-
    adequate stack, so a degenerate event mix would produce an
    inconclusive result regardless of predictor quality.

    CURRENT STATE: the two pre-registered discriminative tests of this
    exact claim (EXQ-111, EXQ-118: C1 predictor-quality gate -> C2
    selectivity lift REAF_ON vs REAF_OFF -> C3 cross-seed consistency)
    both FAILED at C1 -- neither ever reached C2/C3, so the discriminative
    claim itself has never been tested to completion. This is a
    predictor-quality/engineering gate (R^2 repeatedly 0.03-0.36, mostly
    below threshold across three separate attempts: EXQ-111, EXQ-118,
    V3-EXQ-145), not a representational-capacity ceiling in the SD-008/
    Q-002 sense -- implemented-but-unvalidated, not substrate_ceiling.
  status_note: >
    ReafferencePredictor in ree_core/latent/stack.py: z_world_corrected =
    z_world_raw - ReafferencePredictor(z_world_raw_prev, a_prev). Input must
    be z_world_raw_prev (not z_self_prev): cell content entering view dominates
    Δz_world_raw and is inaccessible from body state alone (EXQ-027 run 1
    R²=0.027 with z_self inputs). Biological basis: MSTd receives optic flow
    plus efference copy. Known limitation (Brooks & Cullen 2019 review):
    the brain continuously recalibrates the motor-sensory relationship;
    the current static predictor does not model this -- potential failure
    mode for non-stationary environments or long-running agents.
  depends_on: [SD-005]
  location: docs/architecture/sensory_stream_tags.md

- id: SD-008
  title: "LatentStack EMA alpha for z_world must be >= 0.9 to preserve event responsiveness."
  claim_type: design_decision
  subject: encoder.z_world_alpha_correction
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_zworld-integration-cluster_2026-06-06#V3-EXQ-177"
      as_of: 2026-06-06
      verdict: non_contributory/substrate_ceiling
  status_note: >
    Promoted to stable 2026-03-30 (overall_conf=0.874, conflict_ratio=0, 5 supporting
    experiments, 2 lit entries, 0 weakening experiments in isolation).
    Promoted to provisional 2026-03-22. Two uncontested supporting experiments:
    EXQ-023 (alpha=1.0, event_selectivity_margin=0.084 passed SD-008 criterion;
    overall experiment failed on unrelated claims -- SD-007, ARC-016, SD-003 --
    which have since been independently validated) and EXQ-040 (PASS 4/4,
    z-separation alpha=0.9 directly confirmed correct event responsiveness).
    Zero genuine weakening experiments: EXQ-023 direction corrected from weakens
    to supports; EXQ-024 and EXQ-025 had SD-008 removed per claim_ids accuracy
    rule (those experiments ran with alpha=0.9 but tested SD-003 pipeline, not
    whether alpha value matters). Evidence_direction systematic issue documented
    as known indexer limitation. Motivation: at alpha=0.3, z_world is a ~3-step
    weighted average suppressing event responses to 30%, trivialising E2_world
    prediction (MSE invariant to env perturbation) and preventing ARC-016
    precision from firing. alpha_self may remain low (body state genuinely
    autocorrelated). Config: LatentStackConfig.alpha_world (default 0.3 for
    compat; set 0.9 or 1.0 for correct operation).
    EXQ-177 FAIL/mixed (integration test): event_selectivity_margin~0 in full-stack
    composition (SD-007 + SD-008 + SD-003) despite alpha_world=0.9. Attributed to
    destructive interference in combined stack, not SD-008 in isolation. Isolated
    SD-008 tests remain definitive; integration gap is an SD-003/SD-007 diagnostic
    target.
  depends_on: [SD-005]
  location: docs/architecture/sd_004_sd_005_encoder_codesign.md

  evidence_quality_note: |
    Promoted to stable 2026-03-30. overall_conf=0.874, conflict_ratio=0, 5 exp entries,
    2 lit entries, 0 weakening experiments in isolation. Isolated SD-008 tests (EXQ-023,
    EXQ-040) are definitive and uncontested. EXQ-145 INCONCLUSIVE (2026-03-30): full-stack
    integration test; Phase 1 reafference gate failed (r2=0.076 < 0.08) before SD-008
    selectivity was tested -- engineering bottleneck, does not challenge SD-008. EXQ-177
    FAIL/mixed (2026-03-30): event_selectivity_margin~0 in full-stack (SD-007+SD-008+SD-003)
    despite alpha_world=0.9 and world_forward_r2=0.941 -- attributed to destructive
    interference in combined stack composition, not an isolated SD-008 failure. Integration
    stack diagnostic is a separate workstream (SD-003/SD-007 target).

- id: SD-009
  title: "z_world encoder requires event-type cross-entropy auxiliary loss during training."
  claim_type: design_decision
  subject: encoder.event_contrastive_supervision
  polarity: asserts
  status: candidate
  epistemic_category: standard
  live_status:
    reading: candidate
    as_of: 2026-08-08
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-897_2026-08-08"
      as_of: 2026-08-08
      verdict: unknown/standard
  status_note: >
    Promoted candidate -> provisional 2026-04-03.
    Evidence: conf=0.768, conflict_ratio=0, 2 exp supports, 2 lit entries.
    EXQ-020 PASS (2026-03-20): event_classification_acc=0.692, selectivity_margin=0.882.
    MECH-100 (same claim, mechanism register) promoted to stable this session.
    SD-009 provisional: design decision validated, implementation confirmed in ree-v3 substrate.
    [2026-08-08 governance]: demoted provisional -> candidate per the standing conflict_ratio=0.667
    recommendation (supports=2, weakens=1) in promotion_demotion_recommendations.md, applied at the
    same cycle that reviewed V3-EXQ-897 (see evidence_quality_note below; V3-EXQ-897 itself is
    direction=unknown and does not move the conflict ratio).
  evidence_quality_note: |
    [2026-08-08 governance, V3-EXQ-897, confirmed failure_autopsy_V3-EXQ-897_2026-08-08]: well-controlled
    ablation (2 arms x 3 seeds) of an additional event-type CE head layered on the SD-070 anti-collapse
    recipe (held constant in both arms). Positive control and encoder-trained preconditions both met.
    Primary DV (held-out frozen-probe decodability margin) clears the floor in BOTH arms (mean ON 0.065,
    all 3 seeds' OFF >= ON: deltas -0.025/-0.010/-0.005). Resolves the 2026-07-18 "channel-mismatch" open
    question (evidence/planning/sd009_event_contrastive_channel_mismatch_2026-07-18.md): z_world IS
    genuinely decodable for event-type once SD-070's collapse is fixed. Soft, sign-consistent (3/3 seeds)
    but statistically imprecise signal AGAINST the specific "requires a dedicated event-CE head"
    formulation, given SD-070's other grounding heads are already present -- direction=unknown
    (not weakens: the effect is directionally consistent but not individually decisive).
    epistemic_category=standard. routing=governance-note-only (no substrate action). Adjacent
    evidence-quality note (not actioned this cycle): MECH-100's stable status rests on evidence gathered
    under a P0 SD-070 now documents as collapsing z_world -- worth a future review. A Murray et al. 2004
    biological citation doing real rhetorical work in this claim's prose was never registered as tracked
    literature evidence -- a lit-pull bookkeeping gap, not actioned this cycle.

    [2026-08-09, chip chip-20260809-sd009-897-followon]: both adjacent items above ACTIONED, and the
    bookkeeping gap turned out to be a citation-integrity defect rather than a filing one. The
    "Murray et al. 2004, TICS 8:56-61" article DOES NOT EXIST (verified against the complete PubMed
    listing of TICS vol 8: issue 2 has no Murray paper, pages 56-61 delimit no single article in it,
    issue 3 starts at p.95). The source the prose was reaching for is Murray EA, Bussey TJ, Saksida LM
    (2007), Annu Rev Neurosci 30:99-122, PMID 17417938 -- now registered against SD-009 for the first
    time as targeted_review_sd_009/entries/2026-08-09_sd_009_perirhinal_categorical_object_representation_murray2007,
    at confidence 0.50 / mapping_fidelity 0.45. Deliberately BELOW the 0.62 the same paper carries
    against MECH-100: the review grounds the NECESSITY of some discriminative training pressure, which
    SD-070's own grounding heads already supply, and is silent on SD-009's specific commitment to a
    dedicated event-CE head -- exactly the granularity V3-EXQ-897 measured at null-to-negative on 3/3
    seeds. Before this entry SD-009 had NO biological or neuroscientific literature at all; its
    literature_confidence of 0.724 rested on two lit:computational_model entries (ADAT/Kim2022,
    CURL/Laskin2020). NOTE FOR READERS OF THE NUMBER: the rebuilt index puts SD-009's
    literature_confidence at 0.773 (up from 0.724) and overall_confidence at 0.66 (up from 0.59).
    Do NOT read that rise as strengthened grounding. The posterior counts a third `supports` entry
    and is not sensitive to the fact that the new entry is deliberately the weakest of the three
    (0.50, mapping_fidelity 0.45) precisely BECAUSE it does not reach SD-009's specific
    dedicated-event-CE-head formulation. The qualitative finding runs the other way from the
    metric: the one biological citation this claim family leaned on was unverifiable, and the real
    source supports only the general antecedent that SD-070 already satisfies. MECH-100 review:
    needs_review=true raised there with a full
    evidence_quality_note, NO status change (a stable claim is not demoted outside a governance cycle);
    governance flag raised for both. Working:
    evidence/planning/sd009_murray_citation_and_mech100_currency_review_2026-08-09.md
  depends_on: [SD-005]
  location: docs/architecture/sd_004_sd_005_encoder_codesign.md

- id: SD-010
  title: "HARM stream is a dedicated sensory pathway separate from z_world processing."
  claim_type: design_decision
  subject: harm_stream.nociceptive_separation
  polarity: asserts
  status: implemented
  live_status:
    reading: implemented
    as_of: 2026-07-11
    needs_review: false
  status_note: >
    The HARM stream (ARC-027) must be a separate sensory pathway independent
    of z_world: dedicated HarmEncoder(harm_obs → z_harm), not subject to
    reafference correction. E3.harm_eval takes z_harm as primary input.
    SD-003 attribution operates on harm stream output not full world latent.
    Resolves EXQ-027b over-correction, EXQ-043/044 calibration collapse, and
    EXQ-047 SD-005 calibration shortfall — all converge on fused z_world as
    root cause. Depends on ARC-027 implementation.
  evidence_quality_note: |
    EXQ-058b ARC-027 PASS (2026-03-20, 5/5 criteria): Foundational validation of the
    separate harm pathway. HarmEncoder trained with direct MSE supervision on hazard/
    resource proximity labels achieves Pearson_r=0.973, calibration_gap=0.712,
    harm_pred_std=0.417. mean_harm_none=0.014 vs mean_harm_contact=0.987 — clean
    rank ordering across all event types. First V3 experiment showing z_harm can be
    learned without fusing into z_world. Confirms ARC-027 architectural separation
    is implementable; prerequisite for SD-003 + SD-010 joint pipeline tests.
    EXQ-056 FAIL (2026-03-20): Baseline HarmEncoder collapsed (z_harm=1.0 for all
    types, variance=0 to 14 d.p.). Root cause: HarmEncoder received saturated or
    constant harm_obs input; architecture was correct but input was not. EXQ-058b
    fixed by adding direct MSE supervision on proximity labels as the training signal.
    EXQ-058c post-fix FAIL (2026-03-20, 3/5 criteria): SD-003 + SD-010 joint test.
    calibration_gap=0.036 (threshold 0.05 not met); mean_harm_none=0.629 elevated
    because dense hazard environment ensures agent is nearly always near a hazard,
    inflating the "none" label harm estimate. C1/C2 fail; C3 (world_forward_r2=0.944)
    PASS; causal_sig_approach PASS but collapse guard fails. Dense environment
    calibration remains an open design issue.
    EXQ-056c PASS (2026-03-21, 4/5): SD-010 reafference isolation retest. Pearson
    r_z_harm=0.914. HarmEncoder correctly trained on harm_obs alone, not subject to
    reafference correction applied to z_world. Clean separation confirmed under varied
    experimental conditions.
    EXQ-059c PASS (2026-03-21, 4/5): MECH-102 via SD-010 pipeline. contact_rate_reduction
    of ~10× confirmed — agent proximity-avoiding behaviour measurably different with
    functional harm stream. Harm signal accessible during trajectory planning (not only
    at contact events), consistent with approach-gradient leverage predicted by MECH-102.
  depends_on: [SD-005, SD-007, ARC-027]
  location: docs/architecture/sensory_stream_tags.md

- id: SD-011
  title: "Harm stream separates into sensory-discriminative (z_harm_s: proximity/intensity, Adelta-pathway analog, forward-predictable) and affective-motivational (z_harm_a: accumulated homeostatic deviation, C-fiber/paleospinothalamic analog)."
  claim_type: design_decision
  subject: harm_stream.dual_nociceptive_streams
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-642_2026-06-06
      as_of: 2026-06-06
      verdict: "non_contributory/none -- claimless diagnostic; characterise as a measurement / test-design gap, NOT substrate_ceiling. Do not record substrate_ceiling on any claim from this run."
  implementation_phase: v3
  claim_level: architectural
  depends_on: [SD-010, ARC-027]
  functional_restatement: >
    The single z_harm vector conflates two functionally distinct nociceptive signals:
    (1) z_harm_s: immediate proximity/intensity, analogous to the lateral spinothalamic
    tract (Adelta fibers, VPL -> S1/S2). Tracks stimulus with minimal lag; predictable
    from action (moving away from hazard reduces proximity predictably). This stream
    supports a learnable forward model E2_harm_s(z_harm_s, action) -> z_harm_s_next,
    enabling SD-003 counterfactual attribution within the harm stream.
    (2) z_harm_a: accumulated homeostatic deviation, analogous to the medial
    paleospinothalamic tract (C fibers, intralaminar thalamus -> ACC/insula/amygdala).
    Slower to change; integrative; represents sustained threat urgency and body state.
    NOT counterfactually modeled -- feeds E3 directly as motivational/urgency signal.
    ARC-016 harm variance gating operates on z_harm_a (motivational urgency scales with
    accumulated threat state), not z_harm_s. SD-003 counterfactual attribution operates
    on z_harm_s (via E2_harm_s forward model), not z_harm_a.
  evidence_quality_note: |
    EXQ-093 FAIL + EXQ-094 FAIL (2026-03-24): Both confirmed bridge_r2=0 -- SD-010 makes
    z_world perp z_harm by architectural design, so HarmBridge(z_world -> z_harm) is
    infeasible (nothing to learn). EXQ-094 confirmed training E3 on bridge noise produced
    100x regression in harm_var vs EXQ-088. These experiments collectively demonstrate that
    the current single-stream z_harm cannot simultaneously serve SD-003 (needs forward
    model) and ARC-016 (needs sustained motivational signal). Stream separation is the
    solution confirmed by dual-pathway human nociceptive anatomy.
    Biological grounding: Melzack & Casey (1968) three-component pain model; Craig (2002,
    2003, 2009) interoception and insular cortex homeostatic map; Rainville et al. (1997,
    Science) gold-standard dissociation: hypnotic modulation of unpleasantness modulates
    ACC but not S1 -- affective and sensory-discriminative components are independent.
    EXQ-198 PASS (2026-04-01): dual stream stability. dissociation_advantage=1.243,
    decision=retain_ree. harm_fwd_r2=0.677 (dual) vs 0.555 (fused). All seeds pass C1+C4.
    Manifest had status=UNKNOWN (old template) but metrics.json shows clear positive result.
    evidence_direction corrected to "supports". Confirms dual-stream separation is functionally
    advantageous over fused z_harm.
    Keltner et al. (2006) predictability suppresses sensory (S1) activity, not affective
    (ACC) -- forward model cancellation applies to z_harm_s but not z_harm_a.
    EXQ-100 FAIL 2/4 (2026-03-27): First integration of AffectiveHarmEncoder (z_harm_a).
    C3 PASS: step-std z_harm_a=0.026 vs z_harm_s=0.369 -- EMA smoothing structure confirmed
    at the encoder output level. C4 PASS (no errors). C1 FAIL: z_harm_a LOWER in HIGH_HAZARD
    (0.392) than LOW_HAZARD (0.425) -- inverted response. C2 FAIL: autocorr z_harm_a=-0.017
    vs z_harm_s=0.061 -- negative autocorrelation from an EMA output is anomalous. Diagnosis:
    harm_obs_a (EMA accumulator in environment) may not vary across conditions as expected,
    OR AffectiveHarmEncoder is outputting near-random values despite structural smoothness.
    EXQ-100b FAIL 1/5 RAW_EMA_FAIL (2026-03-27): Two-phase diagnostic confirmed root cause
    is in CausalGridWorldV2, not the encoder. Raw harm_obs_a mean: HIGH_HAZARD=0.131 vs
    LOW_HAZARD=0.197 (inverted -- more hazards = lower accumulation). Raw autocorr_lag10=0.069
    (expected ~0.37 for tau=20 EMA). Root cause identified: prox_now is normalized by
    hazard_max = field.max(). In high-density environments, overlapping hazard fields push
    hazard_max higher, shrinking each proximity contribution proportionally -- EMA tracks
    density-relative rather than absolute exposure. Fix: remove hazard_max normalization
    in prox_now computation (use raw field values or fixed normalization).
    EXQ-101 queued after fix.
    EXQ-106a PASS (2026-03-28): harm_obs_a persistence fix. evidence_direction=supports. Confirms
    harm_obs_a signal quality and accumulator behaviour are correct after normalization fix. Core
    z_harm_s/z_harm_a stream separation claim still not implemented; this validates the accumulator
    component only.
    EXQ-166a FAIL 4/6 (2026-03-30, shared result with SD-003/ARC-033): obs-space forward model
    approach cannot test z_harm_s stream directly. C2 causal gap near zero -- forward model
    learns general obs dynamics, not harm-stream-specific causal structure. SD-011 dual-stream
    architecture (z_harm_s forward-predictable) requires dedicated harm latent, not obs-space
    proxy. Evidence direction for this claim: mixed.
    EXQ-178b PASS 4/4 (2026-03-30): First validated PASS for SD-011 dual-stream dissociation.
    harm_fwd_r2=0.742 (C1: HarmForwardModel learns z_harm_s transitions). stream_corr=0.078
    (C2: streams decorrelated, well below 0.85 saturation threshold). autocorr_gap=0.446
    (C3: temporal integration confirmed -- z_harm_a accumulates vs z_harm_s). z_harm_s_hazard_corr=0.601
    (C4: sensory stream encodes hazard proximity). EXQ-178/178a INCONCLUSIVE (C1 FAIL, identity
    collapse in earlier iterations, superseded). SD-011 stream separation validated; SD-003
    redesign unblocked.
    EXQ-241 FAIL 1/3 criteria (2026-04-04): Diagnostic POC run. r2_s_forward=-0.32 (training/
    wiring issue), harm_rate=1.0 in dual-stream conditions (POC wiring regression). Classified
    non-contributory (2026-04-06 governance): SD-011 stream separation already confirmed in
    EXQ-178b (r2=0.742) and EXQ-198 (dissociation_advantage=1.243). Diagnostic only.
    GOVERNANCE META (2026-04-06): All experimental evidence is diagnostic (EXQ-241 x2, EXQ-178
    series). D3 reversal (R^2_affective > R^2_sensory in all seeds) is an active signal:
    AffectiveHarmEncoder is currently a monotone transform of the sensory stream, not a genuinely
    distinct pathway. Wiring needs a second source (context, history, residue field) inaccessible
    to the sensory stream. Status: pending_retest_after_substrate (gate: EXQ-247 wires z_harm_a
    into E3 commit gating, may create functional distinction).
    IMPLEMENTATION (2026-04-08): Second source implemented. AffectiveHarmEncoder now receives
    harm_history (FIFO rolling window of past harm_exposure scalars, length=harm_history_len)
    concatenated with harm_obs_a. Auxiliary harm_accum_head predicts accumulated harm scalar
    (MSE loss, weight=z_harm_a_aux_loss_weight). This gives z_harm_a genuinely distinct temporal
    information (harm trajectory over time) that z_harm_s does not receive, resolving the D3
    reversal. Config: LatentStackConfig.harm_history_len (default 0, set 10 to enable).
    EXQ-241a validation queued. Status: ready for retest.
    EXQ-323a (2026-04-16, PASS): First PASS on body-damage substrate (SD-022 enabled).
    z_harm_a representationally distinct from z_harm_s across all 5 seeds. D3 reversal
    absent. Co-tested with SD-019 and SD-022; all three tagged as supports.
    Governance decision (2026-04-18): promoted candidate -> provisional. conf=0.844,
    conflict=0.32, 26 supports / 5 weakens / 3 mixed across 42 experiments + 17 lit.
    Downstream claims (SD-003, SD-013, SD-021, ARC-033) cascade off SD-011; promotion
    reflects its structural role and strong convergent evidence. Full stable status
    withheld pending SD-003 / ARC-033 resolution + conflict ratio below 0.3.

    [/governance 2026-08-12, V3-EXQ-917, confirmed
    failure_autopsy_V3-EXQ-916-916a-917-920-fishtank-cluster_2026-08-12, supports]: the
    sensory/affective separation this claim asserts is confirmed to be REAL AND CONSEQUENTIAL, and
    the run resolves the complex (probe-gated) open question in
    mech303_contextual_safety_threshold_reachability.md (2026-08-11). The z_harm_a threshold
    reachability/discrimination tension is NOT intrinsic to the encoder -- it dissociates cleanly by
    SOURCING MODE. Under the production default (damage_sourced, i.e. SD-022 body-damage), safe-vs-
    unsafe AUC never exceeds 0.52 (chance) across all 18 swept thresholds (0.02-0.8); under the
    legacy proximity_ema_sourced signal (limb_damage_enabled=False) the same apparatus reaches AUC
    0.84-0.97 with reachability 0.23-0.98 across thresholds 0.4-0.8. Read carefully, this SUPPORTS
    SD-011 rather than weakening it: the two sourcing modes are not interchangeable readouts of one
    signal, which is precisely the dual-stream separation SD-011 posits -- damage-sourced z_harm_a
    tracks accumulated homeostatic deviation (its stated job) and is therefore the WRONG signal for
    an anticipatory contextual-safety gate, which needs proximity/intensity. Status unchanged
    (stable). The consequence is downstream on MECH-303, not here -- see that claim's note and the
    SD-MECH303-THRESHOLD-SOURCING substrate_queue entry created this cycle.
  notes: >
    Implementation in CausalGridWorldV2: emit harm_obs_a (EMA of harm_obs_s, tau=10-30
    steps) alongside harm_obs_s (renamed from harm_obs). HarmEncoderA(harm_obs_a -> z_harm_a)
    added; HarmEncoder renamed HarmEncoderS. LatentState gains z_harm_a field. E3Selector
    takes z_harm_s (attribution) and z_harm_a (commit gating) as separate inputs. E2_harm_s
    forward model is a prerequisite (ARC-033). See thought intake
    evidence/planning/thought_intake_2026-03-24_dual_nociceptive_streams.md
    Implementation calibration (from lit-pull 2026-04-17): The stream separation should
    occur at the encoder level, not the sensor level. A-delta and C-fibers converge on
    wide-dynamic-range (WDR) neurons in the spinal dorsal horn -- the clean peripheral
    separation is partially blurred before the signal ascends (Basbaum et al. 2009). The
    separation re-emerges supraspinally via distinct thalamic targets (VPL vs intralaminar
    nuclei). HarmEncoderS and HarmEncoderA therefore both receive harm_obs input and learn
    separate projections -- they should NOT be given pre-split sensor streams as input.
    Soft separation: both streams are active under both conditions; it is differential
    loading that distinguishes them (Bentley et al. 2004). The two encoders should not be
    orthogonally constrained -- their separation emerges from distinct training targets
    (forward-predictable vs EMA-integrated), not from a hard independence requirement.

# Registered 2026-03-21 — Respiratory oscillator cluster
# Source: cross-species behavioural observation (dog + human exhalation on plan abandonment)
# Proposed by: Daniel Golden

- id: MECH-107
  title: "Exhalation is the physiological instantiation of E3 trajectory-abandonment — each exhale sweeps pre-commit rollouts for viability and clears the hypothesis tag."
  claim_type: mechanism_hypothesis
  subject: respiratory.exhalation_trajectory_abandonment
  polarity: asserts
  status: candidate
  epistemic_category: standard
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  claim_level: functional
  functional_restatement: >
    Each exhalation event triggers a sweep of currently active pre-commit hippocampal
    rollouts and DMN simulations for viability. Trajectories failing the sweep are
    abandoned and the hypothesis tag (MECH-094) cleared, opening the next deliberation
    cycle. In an ANN substrate, implemented as an explicit plan-sweep trigger fired at
    respiratory cadence, not as respiratory muscle activity. The biological mechanism
    (vagal afference from lung stretch receptors modulating brainstem–thalamic–prefrontal
    circuits) is the proposed substrate; the functional requirement is a periodic
    viability sweep co-occurring with the exhalation phase.
  depends_on:
    - MECH-094
    - MECH-091
    - ARC-023
  location: docs/architecture/control_plane_heartbeat.md#mech-107
  source:
    - docs/architecture/control_plane_heartbeat.md
  evidence_quality_note: |
    LIT-0094 (2026-03-23): Phase framing partially revised.
    Zelano 2016 (PMID 27927961): oscillatory power peaks during INHALATION, not exhalation;
    fear/memory performance 20-30% higher on inhalation. Boyadzhieva 2021 (PMID 34267621):
    inhalation = bottom-up sensory (feedforward); exhalation = top-down prediction propagation.
    Revised two-phase framing: inhalation = scan / viability evaluation (peak excitability,
    lower decision boundary); exhalation = execute abandonment / clear hypothesis tag.
    The claim captures the exhalation execution side; the scan phase on inhalation is
    implicit but now documented. Braendholt 2025 (PMID 40424351): inspiration lowers
    decision boundary (threshold channel) independently of valence bias channel.
    Vlemincx 2010 (PMID 20536901): sigh rate increases on load-recovery; sighs restore
    respiratory variability after sustained commitment -- direct behavioral evidence.
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: BreathOscillator must be active with breath_period > 0
    (default REEConfig.from_dims: breath_period=50, breath_sweep_amplitude=0.25) and the
    task must present genuine multi-candidate deliberation -- if only one viable trajectory
    ever exists, "abandonment" is undefined and sweep_active cannot produce a measurable
    effect regardless of mechanism validity. Note this claim's own substrate (MECH-108's
    threshold-modulation analog) is phase-1-implementable and does NOT require SD-006
    phase 2 async execution, unlike its formal dependencies MECH-091 and ARC-023 -- do not
    gate this test on SD-006 phase 2 landing. Also note the current BreathOscillator
    implementation is phase-undifferentiated (one sweep window per breath_period cycle,
    corresponding to "exhale=execute"); it does not model a separate "inhale=scan" phase,
    and it does NOT couple to hypothesis_tag clearing (MECH-094) in code -- any test should
    measure sweep-triggered trajectory reselection/decommitment events, not literal
    hypothesis-tag state, until that coupling is built.

    CONFIRMING signature: across variation in breath_period / sweep_amplitude, the
    sweep-window occurrence rate correlates POSITIVELY with measured plan-abandonment rate
    (rate of commit-then-uncommit / trajectory-reselection transitions during deliberation)
    AND correlates NEGATIVELY with committed-trajectory persistence (mean run-length of an
    uninterrupted committed trajectory) -- reproducing the claim's own stated prediction
    ("exhalation rate during deliberation tasks correlates positively with plan-abandonment
    rate; committed trajectory persistence correlates with respiratory slowing").

    FALSIFYING (VIOLATED BY): sweep-window occurrence shows no reliable relationship, or
    the opposite-sign relationship, with abandonment rate or committed-trajectory
    persistence. Also violated if the apparent effect is fully attributable to a known
    confound already in the selector (e.g. SD-011 urgency/z_harm_a threshold modulation, or
    SD-093 progress-velocity modulation) rather than to the breath-cycle signal itself -- in
    that case the claim should be reframed as "commit threshold has multiple periodic/
    state-dependent modulators, of which breath-cycle is one non-privileged contributor"
    rather than "exhalation is THE trajectory-abandonment instantiation."
  notes: >
    Cross-species convergence: dogs exhibit a swift, audible exhalation precisely when
    abandoning a behavioural plan (e.g., approaching then breaking off); humans sigh in
    functionally identical contexts. Cross-species presence removes "cultural artifact"
    as an objection -- this is an evolutionarily conserved mechanism for plan-state
    management, not learned behaviour. Each exhalation is the physiological instantiation
    of the hypothesis-tag clear described in MECH-094: the body enacts what the planning
    system has decided (trajectory abandoned -> write gate cleared -> new deliberation
    cycle opens). Two-phase cycle (LIT-0094): inhalation = viability scan (peak neural
    excitability, reduced decision boundary per Braendholt 2025); exhalation = execute
    abandonment decision (top-down prediction propagation per Boyadzhieva 2021).
    Predicts: exhalation rate during deliberation tasks should correlate positively with
    plan-abandonment rate and negatively with committed-trajectory persistence.

- id: MECH-108
  title: "Respiratory rhythm (~0.2 Hz) is E3's plan-sweep oscillator — a third control-plane clock governing periodic sweeps of pre-commit hippocampal rollouts and DMN simulations."
  claim_type: mechanism_hypothesis
  subject: respiratory.breath_rate_plan_sweep_clock
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  claim_level: functional
  functional_restatement: >
    The ~0.2 Hz respiratory cycle gates periodic sweeps of pre-commit hippocampal
    rollouts and DMN simulations for viability and abandonment. This is a third
    oscillator in the control-plane cluster, distinct from (a) heartbeat (~1 Hz,
    involuntary, arousal-level gating of E3 refresh rate — MECH-093) and (b) neural
    theta/beta (~4–30 Hz, sensory batching and commitment propagation — MECH-089,
    MECH-090). Semi-voluntary: top-down modulation is possible; cannot be fully stopped.
    That semi-voluntary property is exactly right for the planning layer — it is where
    deliberate top-down intervention in the planning cycle is possible. In an ANN
    substrate, implemented as a configurable sweep-clock parameter operating at ~0.2 Hz
    relative simulation time, orthogonal to the E3 heartbeat parameter.
  epistemic_category: substrate_conditional
  depends_on:
    - ARC-023
    - MECH-107
    - MECH-092
    - MECH-093
    - MECH-104
  location: docs/architecture/control_plane_heartbeat.md#mech-108
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: BreathOscillator (ree-v3/ree_core/heartbeat/clock.py,
    MultiRateClock) is built and controllable via breath_period/sweep_amplitude/
    sweep_duration, already exercised (V3-EXQ-199/876, MECH-025 lineage) as a
    SINGLE-PHASE periodic commit-threshold reducer -- sweep_active forces the
    uncommitted regime for the last sweep_duration steps of each cycle. What is NOT
    built, required by THIS claim specifically (beyond narrower sibling MECH-107): (a)
    any inhalation-scan vs exhalation-execute PHASE distinction -- the clock models one
    binary sweep/inter-sweep state, not two asymmetric phases; (b) any object-level
    sweep of pre-commit hippocampal rollouts and DMN simulations -- sweep_active only
    rescales a scalar commit_threshold, never rollout generation or DMN-simulation
    triggering; (c) a second, independent computational channel (valence bias)
    alongside the decision-boundary/threshold channel, needed to distinguish Braendholt
    2025's "two independent channels" from "one scalar." Until at least (a) is built,
    the claim's defining dual-phase prediction cannot be tested even in principle.

    CONFIRMING: once a genuine two-phase plan-sweep is wired against actual rollout/DMN-
    simulation objects: (1) rollout/DMN-simulation initiation shows measurable
    periodicity phase-locked to the ~0.2 Hz breath cycle, AND (2) the two phases produce
    distinguishable behavior (more rollouts during inhalation-scan, more commitment/
    abandonment during exhalation-execute), reproducing Braendholt 2025's two-
    independent-channel result.

    FALSIFYING: rollout/DMN-simulation timing shows no reliable phase-lock to the breath
    cycle once implemented, or the phases produce no distinguishable scan-vs-execute
    asymmetry -- breath phase modulates only a generic threshold (as the CURRENT
    single-phase BreathOscillator already does), collapsing the "third clock, two-
    channel oscillator" framing into a relabeling of MECH-107's simpler mechanism. Also
    falsified if any confirmed effect is fully attributable to an existing modulator
    already acting on the same commit_threshold (SD-011 urgency/z_harm_a, SD-093
    progress-velocity) rather than the breath-cycle signal itself.

    Deliberately diverges in category from sibling MECH-107 (standard): MECH-107
    asserts only the narrower exhale/execute half and routes around the missing
    substrate with a coarse proxy test; MECH-108's broader dual-phase/two-channel
    content has no available proxy.
  source:
    - docs/architecture/control_plane_heartbeat.md
  notes: >
    LIT-0094 (2026-03-23): Empirical grounding confirmed.
    Zelano 2016 (PMID 27927961): human respiratory 0.16-0.33 Hz; system-wide entrainment
    of piriform cortex, amygdala, hippocampus. Tort 2018 (PMID 29691421): RCOs in all 15
    brain regions simultaneously including nucleus accumbens and mPFC -- global clock.
    Yackle 2017 (PMID 28360327): preBotC projects monosynaptically to LC -- respiratory
    pacemaker is hardwired to NE precision modulation (MECH-104 direct dependency added).
    Braendholt 2025 (PMID 40424351): inspiration lowers decision boundary AND shifts
    valence bias -- two independent computational channels, not one scalar.
    Neuromodulator duality (Lottem 2018 PMID 29520000, Dayan & Yu 2006 PMID 17162459):
    inter-sweep phase = 5-HT-dominant (persist/commit); sweep phase = NE-ready (interrupt/
    release). BreathOscillator approximates oscillation between these neuromodulator states.
    Three-oscillator control-plane hierarchy (slow to fast):
    (1) Respiratory plan-sweep (~0.2 Hz, semi-voluntary) -- sweeps pre-commit rollouts
        for viability/abandonment; inhalation = scan phase, exhalation = execute phase.
    (2) E3 heartbeat (involuntary thalamic pacemaking, ~0.5-2 Hz depending on z_beta
        arousal, MECH-093) -- governs E3 refresh rate and harm attribution resolution.
    (3) Neural theta/beta (~4-30 Hz, MECH-089, MECH-090) -- sensory batching and
        commitment propagation gating.
    Infraslow oscillations (Monto et al. 2008, PMID 18701689, 0.01-0.1 Hz) provide a
    fourth slower layer -- 55% detection probability modulation by ISF phase. Relevant
    to long-timescale MECH-047 hysteresis. Not modelled in BreathOscillator but noted.
    Quiescent replay (MECH-092) operates below all four, during genuine idle periods.

- id: MECH-109
  title: "Voluntary respiratory modulation provides the one top-down deliberate handle on the E3 plan-sweep clock; breath-hold is a predicted failure mode suppressing plan abandonment under anxious concentration."
  claim_type: mechanism_hypothesis
  subject: respiratory.voluntary_breath_control_planning_gate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  claim_level: functional
  functional_restatement: >
    Breathing is the only control-plane oscillator with a genuine top-down voluntary
    handle: the agent can deliberately sigh (force a plan-sweep cycle), slow breathing
    (reduce sweep rate), or hold breath (suppress plan sweeps entirely). This voluntary
    controllability — absent from heartbeat and from neural theta/beta — is structurally
    appropriate for the planning layer because deliberate plan abandonment requires an
    agent-initiated trigger. In an ANN substrate, the "voluntary respiratory modulation"
    is an explicit external trigger API that forces a plan-sweep cycle on demand,
    independently of the background respiratory cadence.
  depends_on:
    - MECH-108
    - MECH-091
    - MECH-107
  evidence_quality_note: |
    LIT-0094 (2026-03-23): First direct behavioral evidence.
    Vlemincx et al. 2010 (PMID 20536901, Psychophysiology): sigh rate increased during
    and immediately after mental load / sustained attention tasks. Sustained attention
    compressed total respiratory variability (reduced flexibility). Sighs functioned
    as resets -- restoring respiratory variability after high-commitment compression.
    This is direct empirical support: high-commitment states suppress spontaneous
    plan-sweep cycling (compressed variability); sighs are the voluntary restoration event
    (reset). Timing: sigh rate elevated on task-offset / load-recovery, matching the
    prediction that sweep-clock suppression under load is followed by burst of sweeps at
    release. Consider upgrading to provisional after one targeted sigh-latency experiment.
  location: docs/architecture/control_plane_heartbeat.md#mech-109
  source:
    - docs/architecture/control_plane_heartbeat.md
  notes: >
    Predicted failure mode: anxious breath-holding. Under high-stakes deliberation,
    agents (human and animal) frequently hold their breath. This suppresses the
    respiratory plan-sweep clock (MECH-108) at exactly the moment the planning system
    is under maximal load and most needs plan-abandonment sweep capacity. The failure
    is self-compounding: suppressed sweeps prevent abandonment of non-viable trajectories,
    increasing deliberative load, increasing anxiety, sustaining the breath-hold.
    This also coincides with the agent needing more oxygen (high metabolic demand from
    high arousal), creating a somatic conflict between gas-exchange need and plan-sweep
    suppression. Clinical implication: deliberate breathing (respiratory rate restoration)
    is not merely calming through vagal activation but specifically restores the
    plan-abandonment sweep cycle. "Taking a deep breath" is mechanistically plan-sweep
    restoration. Voluntary sigh = deliberately forced single-cycle abandonment sweep.
    Contrast: heartbeat cannot be voluntarily overridden (modulated indirectly by arousal
    and vagal tone, not directly commanded); neural theta/beta are wholly involuntary.
    Respiratory rate is the unique locus of deliberate top-down planning-clock intervention.

- id: MECH-110
  title: "Laughter is rapid repeated hypothesis-tag cycling — threat activation followed immediately by safe-resolution — predicting incongruity+resolution as the necessary structure of humour."
  claim_type: mechanism_hypothesis
  subject: respiratory.laughter_rapid_tag_cycling
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  claim_level: functional
  functional_restatement: >
    Laughter consists of repeated forced exhalations, each constituting one plan-sweep
    cycle (MECH-107). A laughter episode is therefore rapid repeated oscillation through:
    threat-hypothesis activation (E3 writes a harm-evaluation trajectory tag) → sweep →
    safe-resolution (sweep finds no genuine harm → tag cleared via exhalation). The
    pleasure of laughter is the affective signature of rapid repeated threat-non-confirmation.
    This predicts that humour requires (1) an initial threat/incongruity activation
    (hypothesis tag written) and (2) a fast safe-resolution (sweep clears tag). Without
    the initial activation, no laughter; without fast resolution, distress rather than
    laughter. This matches incongruity-resolution theories of humour independently.
    Social extension (V5-scoped): synchronised group laughter is a shared respiratory
    phase-reset signal synchronising E3 plan-sweep clocks across agents, functioning as
    a joint "threat absent" signal. Contagious laughter is clock entrainment across agents.
  depends_on:
    - MECH-107
    - MECH-094
    - MECH-091
    - MECH-108
  location: docs/architecture/control_plane_heartbeat.md#mech-110
  source:
    - docs/architecture/control_plane_heartbeat.md
  notes: >
    Each forced exhalation in laughter is a rapid hypothesis-tag clear (MECH-107).
    The density of exhalations in laughter (typically 3–7 per second) far exceeds the
    resting respiratory rate (~0.2 Hz), indicating that laughter is a deliberate
    high-frequency override of the background plan-sweep clock. This high-frequency
    override is tolerated because each tag-clear is immediately followed by re-activation
    and re-clearing: the threat level perceived to be genuinely zero. The repeated cycling
    is itself the mechanism of pleasure — not a single resolution but the kinetic experience
    of rapid iterated safe-confirmation. Distinguishes: (a) single exhalation/sigh =
    plan abandonment under genuine uncertainty; (b) laughter = rapid safe-confirmation
    cycling where genuine threat is absent. Predicts: humour that initially activates a
    stronger threat tag should produce more intense laughter when resolved safely (gallows
    humour, dark comedy). Predicts: incomplete resolution (ambiguous punchline) produces
    nervous laughter or discomfort rather than clean laughter — the tag-clear is partial
    and the residue persists.
    Social synchronisation (V5-scoped): group laughter as joint respiratory entrainment
    synchronises E3 plan-sweep clocks across agents, functioning as a distributed "nothing
    to fear" signal. Contagious laughter is clock entrainment. Social laughter evolution
    may be grounded in this synchronisation function rather than (or in addition to) pure
    affiliation signalling.

# Registered 2026-03-22 -- approach/avoidance drive symmetry cluster
- id: MECH-111
  title: "E1 prediction error surprise at moderate magnitudes generates intrinsic positive valence (curiosity/novelty drive); information-seeking is architecturally grounded."
  claim_type: mechanism_hypothesis
  subject: drive.epistemic_value_novelty
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-111@2026-03-29T21:16:41.399734Z"
      as_of: 2026-03-29
      verdict: hold_candidate_resolve_conflict/applied
  implementation_phase: v3
  evidence_quality_note: |
    EXQ-141 FAIL/weakens (2026-03-29): novelty drive discriminative pair FAIL. First
    experimental entry.

    [2026-05-08 governance]: EXQ-141b weakens (substrate-corrected reef retest). C1/C2/C5 fail 0.0/0/0 across 3 seeds; novelty_ema alive (~1e-4) but does NOT propagate to E3 selection. Update-path is the next investigation lever (per 2026-05-08 review): EMA -> E3-selection routing is the probable break point. Reconsider after the update-path is traced.

    [2026-05-25 governance]: V3-EXQ-590a Goldilocks calibration is degenerate -- across novelty_bonus_weight in {0.1, 0.3, 0.5, 0.7, 1.0} all five arms produce byte-identical mean_coverage=1.0, mean_h_pos=0.5077619937480481, mean_novelty_ema=0.01117353528432... (to 16 sig figs, 3 seeds). The PASS reflects C1+C2 preconditions (novelty signal active, coverage > 0.05) but the calibration result is null: weight has no effect on coverage / positional entropy / novelty_ema. Two non-exclusive readings: (a) env coverage saturates at 1.0 before any arm-difference can emerge in the eval window, (b) MECH-111 broadcast novelty does not propagate downstream to alter selection -- corroborates the 2026-05-08 EXQ-141b finding that the EMA -> E3-selection routing is broken. V3-EXQ-590a is non_contributory for MECH-314 (per the manifest's own per-claim override; GAP-13 actually calibrates MECH-111 not MECH-314 per-candidate). MECH-314a (per-candidate RBF) is the upgrade path. Tracing the broadcast-novelty -> E3-selection routing remains the live investigation lever.
  depends_on:
    - MECH-069
    - ARC-030
  location: docs/architecture/approach_avoidance_symmetry.md#mech-111
  source:
    - evidence/planning/thought_intake_2026-03-22_approach_avoidance_drives.md
  notes: >
    Computational analogue of FEP epistemic value (expected information gain), distinct from
    pragmatic value (harm reduction). E1 surprise is not only alarming -- at moderate
    magnitudes it is intrinsically rewarding. Without this, the agent has no intrinsic
    motivation to encounter novel states; exploration decays to zero under harm-avoidance
    pressure alone. The architecture needs to distinguish alarm-surprise (high magnitude,
    harmful context) from curiosity-surprise (moderate magnitude, safe novel context).
    This distinction may route through z_beta. Registered 2026-03-22.
    Literature: Ogasawara et al. 2022 (Nat Neurosci, DOI 10.1038/s41593-021-00950-1) --
    novelty seeking has its own dedicated neural circuit (temporal cortex-zona incerta
    pathway) DISTINCT from reward dopamine; ZI neurons activate on predictions of novelty
    before action. Wang et al. 2024 (PLoS Comput Biol, DOI 10.1371/journal.pcbi.1011516) --
    dopaminergic novelty signals encode VARIANCE of reward distribution (uncertainty), not
    mean reward; distinct from TD prediction error. Kakade & Dayan 2002 (Neural Netw, DOI
    10.1016/s0893-6080(02)00048-5) -- dopamine multiplexes exploration bonuses alongside
    reward prediction error; novelty bonuses lower commit threshold for novel states.
    Suri et al. 2001 (Neuroscience, DOI 10.1016/s0306-4522(00)00554-6) -- ablating novelty
    responses impairs planning, not just exploration.
    Architecture shape: novelty drive is NOT repurposed E1 prediction error; it encodes
    reward *variance* (uncertainty), needs own latent channel or modulator. Acts on commit
    threshold (lowering for novel states) rather than through goal attractor channel.
    Full synthesis: evidence/planning/literature_synthesis_2026-03-22_approach_avoidance_drives.md
  what_would_answer: |
    NON-DEGENERACY PRECONDITION (else substrate_not_ready, NOT a verdict): the novelty/
    curiosity signal must be wired into E3 candidate scoring as a PER-CANDIDATE weighting,
    not a uniform scalar broadcast added identically to every candidate. A uniform additive
    shift is argmin-invariant by construction -- it cannot change which candidate is selected
    regardless of magnitude -- and this is not a hypothetical failure mode: it is exactly what
    EXQ-141b (2026-05-08) and V3-EXQ-590a (2026-05-25) ran into. V3-EXQ-590a's sweep over
    novelty_bonus_weight in {0.1, 0.3, 0.5, 0.7, 1.0} produced byte-identical mean_coverage,
    mean_h_pos, and mean_novelty_ema across all five arms (to 16 sig figs) precisely because
    the broadcast term could not have moved selection even in principle. That broadcast branch
    was deleted 2026-05-25 as dead-by-construction; `_dc_novelty_w` is now hardcoded to 0.0 and
    retained only for schema compatibility (confirmed independently by the 2026-07-19 MECH-463
    scoping spike). A run using the old broadcast pathway -- which is most of the evidence
    collected on this claim to date -- tests nothing about MECH-111 and must self-route
    substrate_not_ready rather than count as PASS or FAIL. Separately, the eval metric used to
    detect an effect must not already be saturated (coverage=1.0 ceiling was one of two live,
    non-exclusive readings of the 590a null result) -- an unsaturated behavioral DV (e.g.
    resource/state coverage below ceiling, positional entropy, or an explicit per-candidate
    novelty-conditioned selection margin) is required.
    CONFIRMING signature: with a genuinely per-candidate novelty pathway wired to E3 selection
    (the MECH-314a per-candidate RBF upgrade path, or equivalent) and an unsaturated DV,
    moderate-magnitude E1 surprise at a candidate state produces a directional, seed-stable
    INCREASE in that candidate's selection probability/score relative to a matched
    ablation/control, while high-magnitude ("alarming") surprise in the same state instead
    suppresses selection -- i.e. the approach/avoidance dissociation the claim asserts shows up
    as a discriminative, non-degenerate effect, not merely a nonzero novelty_ema.
    FALSIFYING signature: even with a correctly per-candidate (non-argmin-invariant) novelty
    pathway and an unsaturated DV, moderate-magnitude novelty at a candidate produces no
    directional, seed-stable selection advantage over ablation/control. This would mean
    curiosity/novelty fails to function as an intrinsic positive-valence drive even when
    structurally capable of influencing choice, refuting the claim's core assertion -- as
    distinct from merely reconfirming the already-known routing defect above.
    NOTE: current evidence is genuinely mixed and NOT reconciled here. claims.yaml's own
    evidence_quality_note reads uniformly negative (FAIL/weakens x3), but governance-agenda
    tracking (EVB-0029) separately logs this claim at conflict_ratio 0.667 with directional/
    source disagreement across 5 considered entries (2 supports, 1 weakens). Reconciling
    that discrepancy is a governance call, not resolved by this digestion pass.

- id: MECH-112
  title: "E3 requires a structured latent goal representation (positive attractor in z_world or z_goal sub-space) distinct from harm avoidance."
  claim_type: mechanism_hypothesis
  subject: drive.goal_state_attractor
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-642_2026-06-06
      as_of: 2026-06-06
      verdict: "non_contributory/none -- claimless diagnostic; characterise as a measurement / test-design gap, NOT substrate_ceiling. Do not record substrate_ceiling on any claim from this run."
  implementation_phase: v3
  depends_on:
    - SD-005
    - ARC-007
    - MECH-069
    - ARC-030
  location: docs/architecture/approach_avoidance_symmetry.md#mech-112
  source:
    - evidence/planning/thought_intake_2026-03-22_approach_avoidance_drives.md
  notes: >
    E3 error signal is specified as harm/goal error (MECH-069) but goal has no structural
    representation -- subsumed under harm with no latent encoding, no goal encoder, no approach
    gradient. The hippocampal planner must navigate toward attractors as well as away from
    repellers. Without goal attractors the residue field is a pure aversion landscape; gradient
    minimum under learning is quiescence. Goal representation needs its own latent (or
    sub-component of z_world), its own encoding path, and its own contribution to hippocampal
    terrain. INV-029 (love as long-horizon care-investment) and ARC-026 cash out architecturally
    here: coherence and flourishing are positive-valence attractors in z_world space.
    Registered 2026-03-22.
    Literature: Barch & Dowd 2010 (Schizophr Bull, DOI 10.1093/schbul/sbq068) -- critical
    distinction between LIKING (hedonic response at goal receipt) and WANTING (prospective
    motivational drive toward future states). Schizophrenia negative symptoms = impaired
    wanting with intact liking. REE may have liking (z_beta valence) but lacks wanting (goal
    representation). MECH-112 must be prospective and persistent, not a terminal reward signal.
    Culbreth et al. 2023 (Psychol Med, DOI 10.1017/S0033291722003993) -- reduced reward-
    seeking correlated with loss-avoidance bias in schizophrenia; rlPFC encodes uncertainty-
    driven exploration. Hollerman et al. 2000 (Prog Brain Res, DOI 10.1016/S0079-6123(00)26015-9)
    -- striatum provides neural representation of goal; subgoal representations at multiple
    trajectory points, not only terminal state. Rusu & Pennartz 2019 (Hippocampus, DOI
    10.1002/hipo.23167) -- hierarchical BG loops; limbic-affective loop sets goal context
    for dorsomedial PFC-striatal loop; hippocampal SWR replay for goal consolidation.
    Architecture shape: MECH-112 = "wanting" system; prospective, persistent goal state in
    E3 domain; hippocampal terrain becomes attractive near z_goal, not just repulsive near
    z_harm; subgoal representations needed at multiple trajectory steps.
    Full synthesis: evidence/planning/literature_synthesis_2026-03-22_approach_avoidance_drives.md
  evidence_quality_note: |
    EXQ-074c superseded (2026-03-27): resource_respawn bug -- zero resource visits in all
    conditions. Superseded by EXQ-074d.
    EXQ-074d FAIL 3/4, EXQ-074e FAIL 3/4 (2026-03-27): C1 FAIL in both (resource_rate_gap=0).
    C1 confound: greedy navigation applied uniformly to all conditions -- wanting cannot show
    a behavioural lift above nogo even when z_goal is active (goal_active=True, goal_norm=0.28
    in 074d). C2/C3/C4 PASS in both: wanting/liking dissociation confirmed in trajectory
    scoring metrics (l2_redirect, l1_fraction, goal_proximity).
    C1 requires redesign: use goal-following navigation in wanting condition vs random/nogo
    baseline, so resource visit rate CAN vary across conditions.
    EXQ-085g FAIL 3/4 (2026-03-29): same C1 failure pattern. goal_resource_r=0.066 < 0.2
    threshold: z_goal not pointing toward resources despite contact-gated seeding. z_world
    at contact encodes full scene; resource features not isolated. SD-015 z_resource
    separation required before MECH-112 wanting system can navigate to goals.
    EXQ-085h through 085l FAIL C2 (2026-03-30): SD-015 encoder series (5 iterations,
    085l final, supersedes prior). 085l: prox_r2=0.908 (z_world encodes proximity),
    goal_resource_r_enc=0.869 (ResourceEncoder learns position-invariant features), but
    benefit_ratio=0.420 (navigation not improved). The signal is learnable; the bottleneck
    is action-selection integration. Governance: hold -- promotion blocked by consistent C2
    FAIL across all 5 iterations. MECH-112 wanting system requires working navigation
    (C2) not just representation (C1/C3) to validate the claim.
    Goal-lift battery (2026-04-01): EXQ-185 (direct prox-argmax, bypass z_goal entirely),
    EXQ-186 (hybrid benefit+harm selector), EXQ-183 (BCE shared selector) -- ALL FAIL.
    Systematic pattern: every learned 1-step greedy mechanism performs WORSE than random
    (0.21x, 0.25x, 0.73x respectively). EXQ-186 4-condition decomposition: random=0.862
    benefit/ep, benefit_only=0.350, combined=0.212, harm_only=0.137. Directed mechanisms
    increase harm 2-2.3x relative to random while collecting fewer resources. Diagnosis:
    1-step greedy action selection creates local traps near hazards; random walk's advantage
    is exploration breadth on 10x10 grid with 4 respawning resources. CRITICAL GATE:
    EXQ-182a (oracle ceiling) queued -- if perfect-information 1-step greedy also fails,
    the env config is near-optimal for random walk and no learned mechanism can beat it
    without multi-step planning (SD-004 hippocampal navigation).
    EXQ-074f PASS 4/4 (2026-04-04): FIRST full PASS for MECH-112 wanting system with
    correct navigation design. wanting l1_fraction=0.96 (agent persistently approaches
    L1 post-relocation); liking l1_fraction=0.43; resource_rate=0.148 (floor PASS >= 0.05).
    C4 dissociation confirmed (delta=0.53 > 0.10). Prior design confound resolved: wanting
    condition uses spatial greedy toward L1; nogo/liking use standard action selection.
    Single seed only (42); prior 085 series FAIL history means multi-seed replication still
    needed before promotion. Navigation integration works at small scale (6x6 grid); larger
    grid performance (10x10, SD-004 planning) remains to be tested.
    EXQ-074e superseded (2026-04-04): same design confound as 074d (uniform greedy).
    074f FAILs 1-3 superseded: exploration iterations before final criteria/navigation design.
    EXQ-225 FAIL non_contributory (2026-04-04): goal-lift redesign (GOAL_PROXIMATE vs
    GOAL_AGNOSTIC CEM scoring). resource_lift=-0.013 (COMBINED slightly worse). z_goal seeding
    confirmed (avg_goal_norm=0.383). This FAIL is not evidence against MECH-112 wanting
    architecture. It is an action-selection bottleneck: on a 10x10 grid with 4 respawning
    resources, 1-step greedy CEM consistently underperforms random walk (same pattern as
    EXQ-183/185/186 battery). The wanting signal is present and active; the mechanism that
    fails is translation of wanting into resource lift without multi-step planning (SD-004).
    Wanting representation (EXQ-074f PASS) and wanting/liking dissociation are validated;
    wanting -> resource collection requires hippocampal navigation. evidence_direction
    corrected to non_contributory in manifest.
    EXQ-233 did not provide evidence for MECH-112: z_goal seeding never fired in either
    condition due to sub-threshold benefit_exposure (proximity_benefit_scale=0.03 default,
    benefit_exposure~0.025 < threshold 0.1). EXQ-238 with corrected env params will provide
    the clean causal ablation.
    EXQ-234 PASS (2026-04-05): independent replication of wanting/liking dissociation with
    seeds [1,2,3]. All 4 criteria met in all 3 seeds. Replicates EXQ-074f.
    GOVERNANCE META (2026-04-06): Illusory conflict resolution risk. Supports come exclusively
    from wanting/liking circuit (EXQ-074f, EXQ-234) which tests incentive salience -- a narrow
    z_goal pathway. The broader claim (structured goal latent for flexible goal-directed behavior)
    is untested. Non-contributory FAILs (EXQ-235) removed from scoring but indicate z_goal
    alignment failure persists outside the wanting pathway. Status: pending_retest_after_substrate
    (gate: EXQ-247 SD-011/SD-012 integration).
    EXQ-354 PASS 3/3 seeds (2026-04-12): wanting/liking dissociation confirmed with CORRECT
    ResourceEncoder pathway (SD-015 wiring bug fixed: use_resource_encoder set directly on
    cfg.latent, not via from_dims kwarg). wanting_l1_fraction 0.96/0.935/0.985 vs
    liking_l1_fraction 0.43/0.54/0.43; dissociation_delta 0.53/0.40/0.55 (all >> 0.10
    threshold). resource_rate floor satisfied all seeds. This is the first PASS with the
    correct SD-015 substrate -- EXQ-074f/234 used z_world fallback seeding. Governance
    (2026-04-13): accepted as supports for MECH-112 behavioral dissociation arm. Illusory
    conflict risk note stands: z_goal latent structure (EXQ-328a) still fails at 0.0 norm
    (SD-012 homeostatic drive substrate gap). Split: behavioral wanting/liking circuit PASS;
    z_goal latent structure FAIL (separate substrate requirement).
    ATOMIC SPLIT (2026-04-13): MECH-112 split into MECH-229 (behavioral wanting/liking
    dissociation, status: active) and MECH-230 (z_goal latent structure, status: candidate,
    gated on SD-012). MECH-112 deprecated -- use MECH-229 and MECH-230 for new experiments.

    [2026-05-08 governance]: EXQ-536a/b diagnostic non_contributory; same crossed-wiring read as ARC-030 sibling.
  split_into:
    - MECH-229
    - MECH-230
  deprecated: true
  deprecated_note: "Split into MECH-229 (behavioral dissociation) and MECH-230 (latent goal structure) on 2026-04-13. See evidence_quality_note."

- id: MECH-113
  title: "The agent requires a self-maintenance error signal monitoring internal latent coherence, independent of external harm signals."
  claim_type: mechanism_hypothesis
  subject: drive.homeostatic_self_coherence
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  evidence_quality_note: |
    EXQ-142 FAIL/weakens (2026-03-28): self-maintenance pair FAIL (first run).
    EXQ-142 FAIL (2026-03-29): self-maintenance pair re-run, FAIL again. Two consistent FAIL
    results. Weakening evidence accumulating.
  depends_on:
    - SD-005
    - SD-007
    - INV-030
    - ARC-030
  location: docs/architecture/approach_avoidance_symmetry.md#mech-113
  source:
    - evidence/planning/thought_intake_2026-03-22_approach_avoidance_drives.md
  notes: >
    Degradation of z_self stability, prediction fidelity, or reafference loop integrity is
    undetected by harm-avoidance machinery if no external harm is occurring -- agent can fail
    silently. A homeostatic coherence signal closes this gap and provides intrinsic motivation
    for continued self-regulation. Implements INV-030 (viability as binding constraint) at the
    architectural level. Two framings require separate experimental testing: (1) z_self coherence
    signal -- monitor z_self statistical properties (entropy, prediction accuracy, drift) and
    generate a self-maintenance error when coherence degrades below threshold; (2) FEP Markov
    blanket resistance -- self-maintenance emerges from minimising surprise about self-generated
    states, framing self-coherence as a prior over blanket states rather than an explicit signal.
    Both framings are architecturally open; both need testing.
    pending_design: framing (D_eff vs. Hopfield stability vs. Markov blanket resistance --
    see Q-022 dissociation question). Registered 2026-03-22, updated 2026-03-23.
    Literature: Seth & Friston 2016 (Phil Trans R Soc B, DOI 10.1098/rstb.2016.0007) --
    interoceptive inference as inversion of a generative model of internal milieu; sense of
    self emerges from maintaining the model. Markov blanket framing and z_self coherence signal
    framing converge: maintaining z_self fidelity IS maintaining the Markov blanket.
    Petzschner et al. 2021 (Trends Neurosci, DOI 10.1016/j.tins.2020.09.012) -- three levels:
    homeostatic (reactive), allostatic (predictive setpoint), goal-directed. All three levels
    correspond to distinct generative models in active inference; may need all three in REE.
    Stephan et al. 2016 (Front Hum Neurosci, DOI 10.3389/fnhum.2016.00550) -- CRITICAL: a
    third level beyond the two framings -- ALLOSTATIC SELF-EFFICACY, metacognitive monitoring
    of the agent's own regulatory capacity. Chronic dyshomeostasis + low self-efficacy =
    learned helplessness. Direct mechanistic link: MECH-113 failure -> low self-efficacy ->
    behavioral inhibition -> Q-021 behavioral flatness. Barrett et al. 2016 (Phil Trans R Soc
    B, DOI 10.1098/rstb.2016.0011) -- depression = locked-in brain insensitive to sensory
    context = clinical manifestation of MECH-113 failure.
    Architecture shape: three-level implementation (updated 2026-03-23):
    Level 1 (reactive): z_self D_eff monitoring + self-maintenance loss. IMPLEMENTED in
    agent.py (compute_z_self_d_eff, compute_self_maintenance_loss). TESTED by EXQ-075.
    Level 2 (allostatic anticipatory): E2 monitors predicted z_self D_eff over rollout
    horizon and generates anticipatory setpoint signal -- "I am heading toward self-model
    incoherence in N steps." NOT IMPLEMENTED. GATED BY ARC-031 (V4): Level 2 requires
    hippocampal z_self trajectory navigation to plan deliberation sequences that maintain
    low D_eff. Do not attempt Level 2 experiments until ARC-031 waking-architecture
    prerequisites (MECH-113/114 EXQ-075/076 PASS) are met and V4 substrate exists.
    Level 3 (metacognitive self-efficacy): E3 models its own regulatory capacity; chronic
    self-incoherence + low regulatory belief = learned helplessness -> Q-021 Pathway B.
    Level 3 is also V4 scope; partially approximated by MECH-114 D_eff commit gate, but
    the full metacognitive framing requires richer self-model machinery.
    D_eff framing (epistemic-mapping): D_eff = (sum|z_self|)^2 / sum(z_self^2) -- participation
    ratio measuring effective dimensionality. Implemented in EXQ-075.
    Hopfield stability framing: familiarity of z_self patterns relative to stored LRU memory.
    NOT tested. Creates dependent claims MECH-116 (familiarity signal) and MECH-117
    (coherent-but-unfamiliar pathology = low D_eff + low stability, distinct from high D_eff
    dispersion). See Q-022 (dissociation question: can D_eff and Hopfield stability dissociate?).
    Markov blanket framing: self-maintenance as minimising surprise about self-generated states
    (FEP formulation). Unoperationalised as of 2026-03-23. Dissociation question: can an agent
    have low D_eff (coherent) but high FEP self-surprise (predictable self-model but surprising
    self-generated states)? If yes, Markov blanket and D_eff framings are testing different
    things and MECH-113 should eventually be split. Dissociation test registered as EVB-0069.
    EXQ-075 PASS is necessary but not sufficient for MECH-113: it confirms Level 1 D_eff
    reactive homeostasis only. Levels 2 and 3 and the Hopfield/Markov framings remain open.
    Full synthesis: evidence/planning/literature_synthesis_2026-03-22_approach_avoidance_drives.md
  what_would_answer: |
    CURRENT EVIDENCE STATE (not reconciled here -- a governance/
    claim-synthesis question): two distinct results already on file.
    (a) EXQ-075 PASSED for Level 1 (reactive D_eff monitoring +
    self-maintenance loss, IMPLEMENTED in agent.py) -- necessary but
    explicitly NOT sufficient for the full claim. (b) EXQ-142 FAILED
    twice (2026-03-28, 2026-03-29) on the "self-maintenance pair"
    behavioural test -- weakening evidence accumulating. Levels 2
    (allostatic anticipatory) and 3 (metacognitive self-efficacy) are V4
    scope, gated on ARC-031, NOT V3-tractable.
    NON-DEGENERACY PRECONDITION: requires z_self D_eff (participation
    ratio) to show non-zero cross-condition variance when self-model
    coherence is deliberately perturbed -- a run where D_eff is flat
    regardless of perturbation cannot test Level 1 at all.
    CONFIRMING signature (Level 1, the only V3-tractable level): the
    self-maintenance error signal (D_eff-derived) rises when z_self
    coherence is degraded (via a controlled perturbation) INDEPENDENTLY
    of whether external harm is occurring, and this rise drives
    measurable intrinsic self-regulating behaviour -- closing the
    "silent failure" gap this claim's own notes describe (degradation
    undetected by harm-avoidance machinery alone).
    FALSIFYING: z_self coherence degradation produces NO self-maintenance
    error signal distinct from external-harm-driven signals (the
    homeostatic self-coherence channel collapses into ordinary harm
    avoidance, contradicting "independent of external harm signals"); or
    the signal exists but does not drive any measurable self-regulating
    behaviour (a signal with no causal authority is not a homeostatic
    drive). Q-022 (can D_eff and Hopfield-stability framings dissociate?)
    and the Markov-blanket framing (EVB-0069 dissociation test) remain
    SEPARATE open sub-questions this claim does not resolve on its own.

- id: MECH-114
  title: "D_eff (z_self participation ratio) is a necessary co-condition for commitment, alongside world-side running_variance; both must be in range before irreversible action."
  claim_type: mechanism_hypothesis
  subject: commit_gate.z_self_d_eff_gate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  epistemic_category: substrate_conditional
  depends_on:
    - ARC-016
    - MECH-113
    - INV-033
    - SD-005
  location: docs/architecture/approach_avoidance_symmetry.md#mech-114
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: d_eff must be structurally READ by
    E3TrajectorySelector.select()'s commit decision, not merely computed elsewhere. As
    of 2026-08-08 UNMET: ree_core/agent.py:compute_z_self_d_eff() is called only from
    experiment scripts (measurement-only); the live commit rule in
    ree_core/predictors/e3_selector.py is exclusively "committed = running_variance <
    commit_threshold" (and its harm/commit-variance variants) -- no d_eff reference
    anywhere in that file. The threshold formula this claim proposes
    (d_eff_threshold = d_eff_baseline * 1.5) is also unvalidated: MECH-113's own
    EXQ-075/075d run measured a raw D_eff baseline of ~23.85 -- an order of magnitude
    different from the "~1.5x target" scale the calibration note assumed -- and its
    perturbation moved D_eff by only ~0.03% (ratio 0.9997), failing to demonstrate D_eff
    is even a sensitive signal to injected self-model incoherence under this design.

    CONFIRMING: once d_eff is wired into E3TrajectorySelector.select() as a second,
    independent AND-condition alongside running_variance, an experiment that dissociates
    self-model coherence from world-model confidence (raises D_eff / lowers self-
    coherence via a perturbation demonstrably stronger than EXQ-075's, while holding
    running_variance in-range) shows: (a) the combined gate measurably suppresses
    commitments the running_variance-only gate would have allowed during high-D_eff
    episodes, and (b) commitments made under the variance-only ablation during those
    same episodes are measurably more erroneous/lower-quality than commitments made
    when D_eff is in-range -- i.e. D_eff carries information running_variance does not.

    FALSIFYING: either (a) no perturbation protocol can produce a genuine running_
    variance/D_eff dissociation (replicating EXQ-075/075d's and MECH-113's own EXQ-142
    x2 FAIL pattern at the gating level), or (b) a dissociation is produced but commit-
    quality is statistically indistinguishable between the D_eff-gated and variance-only
    conditions -- running_variance alone already explains whatever protective effect
    exists, and D_eff is a redundant rather than independent signal.
  source:
    - evidence/planning/thought_intake_2026-03-23_epistemic_self_monitoring.md
  notes: >
    Current ARC-016 commit condition: running_variance < commit_threshold (world-side confidence).
    MECH-114 extends to an AND condition: running_variance < commit_threshold AND
    d_eff < d_eff_threshold (self-side coherence). These are independent signals -- an agent
    can have accurate world predictions (low running_variance) while having an incoherent
    self-model (high D_eff). MECH-114 prevents commitment in this dissociated case.
    Biological motivation: decision to commit to irreversible action requires both knowledge
    of what will happen (world confidence) and knowledge of what one is doing (self-coherence).
    Acting without self-coherence is analogous to acting under dissociation -- the agent
    cannot be held responsible for an action it did not, in some sense, knowingly perform.
    Threshold calibration: EXQ-075 provides baseline D_eff values (pre-perturbation ~1.5x
    target) that can be used to set d_eff_threshold. A reasonable initial value is
    d_eff_threshold = d_eff_baseline * 1.5 (same as MECH-113 homeostatic target).
    Implementation: E3TrajectorySelector.select() checks both running_variance and
    current d_eff before setting committed=True. REEAgent.compute_z_self_d_eff() provides
    the D_eff value at each step.
    EXQ-075 is the prerequisite: must confirm D_eff monitoring works before wiring into
    commit gate. Follow-on experiment: EXQ-076 (MECH-114 D_eff commit gating -- does
    D_eff gate reduce false commitment under self-model perturbation?).
    pending_design: threshold value and how to expose d_eff to E3 commit logic (currently
    computed in REEAgent but not passed to E3TrajectorySelector.select()). Registered 2026-03-23.

    CORRECTION (2026-08-08, thought-digestion currency check): the EXQ-075/076
    references above are stale. EXQ-075 (-> V3-EXQ-075d) actually ran tagged to
    MECH-113, not MECH-114 -- result FAIL 2/4 criteria, and its Gaussian-noise
    perturbation (sigma=2.0) moved D_eff by only ~0.03% (baseline 23.85 -> 23.84),
    failing to establish D_eff as a sensitive signal to self-model degradation, not
    merely providing a calibration baseline as this note implies. EXQ-076's ID was
    burned for an unrelated experiment family (MECH-116/ARC-032 E1 goal conditioning,
    V3-EXQ-076 through 076f, all FAIL) -- the D_eff-commit-gating follow-on this note
    names was never queued or run under any ID. The E3Selector wiring
    (pending_design above) also remains undone as of this date -- confirmed by direct
    grep of ree_core/predictors/e3_selector.py, zero d_eff references. Original text
    preserved above for history; see this claim's what_would_answer for the current
    formulation.

- id: MECH-115
  title: "Hypothesis tag (MECH-094) reliability degrades with z_self dispersion; high D_eff blurs the simulation/real-action boundary at the self-model level."
  claim_type: mechanism_hypothesis
  subject: hypothesis_tag.z_self_coherence_dependency
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  depends_on:
    - MECH-094
    - MECH-113
    - MECH-114
    - INV-033
  location: docs/architecture/approach_avoidance_symmetry.md#mech-115
  source:
    - evidence/planning/thought_intake_2026-03-23_epistemic_self_monitoring.md
  notes: >
    MECH-094 (hypothesis tag = categorical phi(z) write gate) distinguishes simulated from
    real-action content. The tag is issued by the agent to mark replay/simulation as
    hypothesis-tagged, preventing residue accumulation from simulated harm.
    MECH-115 identifies a failure mode distinct from MECH-094 tag LOSS (the severe
    failure modeled as PTSD/psychosis mechanism): tag DEGRADATION due to self-model
    incoherence. When z_self is dispersed (high D_eff), the agent cannot reliably
    distinguish "I am simulating X" from "I am doing X" at the self-model level. The
    tag is still issued, but its reliability as a separating signal degrades.
    Mechanistic picture: the hypothesis tag is written by a gate conditioned on z_self
    state. If z_self is high-D_eff (diffuse across many dimensions), the gate's input
    representation does not clearly encode the current mode of operation. The tag becomes
    a noisy signal -- not absent, but unreliable. This is graded, not binary.
    Clinical analog: dissociative states where the patient performs actions and then
    reports uncertainty about whether they "really did" those things. Not full psychosis
    (MECH-094 tag loss), but a softening of the simulation/real boundary.
    Connection to INV-033: MECH-115 shows WHY second-order self-monitoring (INV-033) is
    necessary -- without it, MECH-094 cannot reliably perform its core function. The
    hypothesis tag is not just a write gate; it is a gate that depends on self-coherence.
    pending_design: how to implement graded tag reliability (precision weighting of tag
    signal? confidence score appended to hypothesis_tag field? downstream discounting
    of residue accumulation from uncertain-tag events?). Distinct from current
    hypothesis_tag: bool -- may require hypothesis_tag: float [0,1] in a future version.
    Registered 2026-03-23.

- id: MECH-118
  evidence_quality_note: |
    [multi_sense_audit_2026_05_08] EXQ-143 evidence superseded 2026-05-08 due to the eval loop calls sense() twice per step in the hopfield-ON arm only (once no-grad, once grad-enabled for the familiarity-pull update); ON arm ticks substrates 2x per env state, OFF arm 1x; the two arms therefore ran under structurally different substrate dynamics. Awaiting StepHarness re-run.

    EXQ-143 FAIL/mixed (2026-03-29): Hopfield familiarity discriminative pair FAIL.
    First experimental entry.
    EXQ-084d SUPPORTS (per-claim override, 2026-03-30): stability dissociation confirmed --
    stab collapses with both noise (0.063 vs 0.950) and novelty (0.026 vs 0.950) perturbation.
    Stability signal responds to perturbation as distinct signal from D_eff (D_eff remains flat
    ~20.6-20.8 across R1/R2). Supports that Hopfield-style familiarity (stability) is a
    discriminable self-maintenance signal. D_eff direction between regimes remains unresolved.
  title: "Hopfield-style pattern familiarity of z_self states (stability against stored self-state memories) is a distinct self-maintenance signal from D_eff coherence."
  claim_type: mechanism_hypothesis
  subject: self_maintenance.hopfield_familiarity_signal
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-118@2026-03-29T21:16:41.465228Z"
      as_of: 2026-03-29
      verdict: hold_candidate_resolve_conflict/applied
  implementation_phase: v3
  depends_on:
    - MECH-113
    - INV-033
    - SD-005
  location: docs/architecture/approach_avoidance_symmetry.md#mech-118
  source:
    - evidence/planning/thought_intake_2026-03-23_epistemic_self_monitoring.md
    - evidence/planning/literature_synthesis_2026-03-23_self_monitoring_dissociation.md
  notes: >
    D_eff (MECH-113) measures coherence -- how focused z_self is across its dimensions.
    MECH-116 adds a complementary signal: familiarity -- whether the current z_self pattern
    matches any previously stored self-state. Both signals are needed for full self-monitoring
    because they can dissociate (see Q-022): a focused (low D_eff) self-model can be in an
    unfamiliar configuration, and a dispersed (high D_eff) self-model can resemble past
    dispersed states. Biological analog: hippocampal pattern completion (familiarity/recognition)
    operates independently of representational coherence (binding quality).
    Literature support: Yonelinas 2001 (PMID 11571028, DOI 10.1098/rstb.2001.0939) canonical
    dual-process review -- familiarity (signal-detection, perirhinal cortex) and recollection
    (threshold, hippocampus-dependent) are doubly dissociable. Maps to MECH-116 Hopfield
    familiarity vs MECH-113 D_eff. Aggleton et al. 2005 (PMID 16154457, DOI
    10.1016/j.neuropsychologia.2005.01.019): patient KN -- 45% bilateral hippocampal volume
    loss, near-normal perirhinal cortex; near-normal familiarity, severely impaired recollection.
    Clearest existence proof that familiarity survives without the hippocampal substrate.
    Staresina et al. 2012 (PMID 22751037, DOI 10.1038/nn.3154): simultaneous fMRI + intracranial
    EEG -- familiarity (perirhinal) and recollection (hippocampal) process on distinct time bases,
    providing a temporal dissociation testable in z_self monitoring timecourses.
    D_eff and familiarity are orthogonal by construction: Rigotti et al. 2013 (PMID 23685452,
    DOI 10.1038/nature12160) -- participation ratio measures capacity/ambient dimensionality,
    not proximity to a stored attractor. High D_eff + no familiar states (novel high-D space)
    and low D_eff + very familiar states (settled attractor) are both possible.
    Implementation source: epistemic-mapping repo (dgolden), Epistemic_monitor.py. HopfieldMemory
    class: 64-slot LRU memory, modern Hopfield network (Ramsauer et al. 2021), attention-based
    retrieval, stability = max(softmax(beta * similarities)). At each step: store z_self;
    retrieve similarity to stored set; high stability = familiar self-state.
    Combined certainty (from epistemic-mapping): 0.4*(1-entropy/10) + 0.3*(1-D_eff/n) +
    0.3*stability. This multi-framing combined score is a candidate INV-033 implementation
    that is sensitive to both coherence (D_eff) and familiarity (stability) failure modes.
    pending_design: integration with REEAgent -- HopfieldMemory would need to be instantiated
    in agent.py and updated at each z_self step. Memory slots are LRU-managed; window size
    is a hyperparameter. Registered 2026-03-23.

- id: MECH-119
  title: "Low D_eff combined with low Hopfield stability (coherent but unfamiliar self-state) is a distinct pathological regime from high-D_eff dispersion."
  claim_type: mechanism_hypothesis
  subject: self_maintenance.coherent_unfamiliar_pathology
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-07-11
    needs_review: false
  status_note: |
    Promoted provisional -> stable 2026-04-03.
    Evidence: conf=0.857, conflict_ratio=0, 6 supports + 1 mixed, 3 lit entries.
    EXQ-144 PASS (2026-03-29, 5/5) confirmed core dissociation.
    EXQ-084d per-claim override (2026-03-30 + 2026-04-02 runs): MECH-119=mixed. Stability
    dissociation confirmed (C2/C4 PASS) but d_eff prediction partially fails.
    The stability prediction (coherent-unfamiliar -> low stability) is robustly validated.
    The d_eff prediction requires further disambiguation. Stable on stability arm.
  implementation_phase: v3
  evidence_quality_note: |
    EXQ-144 PASS (2026-03-29, 5/5): coherent-unfamiliar discriminative pair. All 5 criteria met.
    evidence_direction=supports. 0 conflict ratio; conf=0.81 with 2 experiment entries (both
    supports) and 3 literature entries. Clean promotion to provisional per governance decision
    2026-03-29.
    EXQ-084d MIXED (per-claim override, 2026-03-30 + 2026-04-02 replication): stability
    dissociation confirmed (C2/C4 PASS -- both noise and novelty collapse stability from
    0.950 to 0.063/0.026). However R3 (novelty/structured perturbation) produces d_eff=22.6
    -- HIGHER than R2 noise (20.8) and R1 normal (20.6). MECH-119 predicts coherent-unfamiliar
    regime has LOW d_eff + low stability. The low-stability prediction is confirmed; the
    low-d_eff prediction is not. R3 novelty increases dimensionality rather than compressing
    to a coherent subspace. Promoted to stable on stability arm; d_eff arm requires further
    disambiguation (see Q-022).
  depends_on:
    - MECH-113
    - MECH-118
    - Q-022
  location: docs/architecture/approach_avoidance_symmetry.md#mech-119
  source:
    - evidence/planning/thought_intake_2026-03-23_epistemic_self_monitoring.md
    - evidence/planning/literature_synthesis_2026-03-23_self_monitoring_dissociation.md
  notes: >
    D_eff monitoring alone is insufficient because it cannot detect the coherent-but-unfamiliar
    regime: an agent with low D_eff (focused self-representation) and low Hopfield stability
    (no stored self-state resembling current z_self) is in a state that appears confident by
    D_eff metrics but is epistemically dangerous because the agent has no prior experience to
    calibrate against. This is the hyperarousal analog: high apparent self-confidence but in
    uncharted internal territory.
    Clinical signature: depersonalisation/derealisation with preserved cognitive clarity.
    The patient can think clearly (coherent self-model, low entropy) but does not recognise
    their own mental states as familiar (low pattern familiarity). Distinct from confusion
    (high D_eff, dispersed self-model) and distinct from normal operation (low D_eff, high
    familiarity). Three regimes form a diagnostic space:
    (1) Normal: low D_eff, high stability (coherent + familiar)
    (2) Dispersed: high D_eff, low stability (confused -- classic MECH-113 failure)
    (3) Hyperarousal: low D_eff, low stability (coherent but in novel territory)
    Regime 3 is undetectable by D_eff-only monitoring. A self-maintenance system that only
    penalises high D_eff would miss Regime 3 entirely and might even reward the agent for
    being "coherent" while it is in a pathological state.
    Mechanistic consequence: the self-maintenance loss (MECH-113) and the commit gate
    (MECH-114) should incorporate BOTH D_eff and Hopfield stability to span all three regimes.
    Clinical literature support: Sierra & David 2011 (PMID 21087873, DOI
    10.1016/j.concog.2010.10.018) -- depersonalization = increased prefrontal inhibition of
    insula/limbic. Self-representation structurally intact (low D_eff, coherent), emotional
    familiarity signal absent (low Hopfield stability), Markov blanket intact. This is the
    existence proof that Regime 3 (coherent + unfamiliar) occurs clinically. Paul et al. 2019
    (PMID 31103548, DOI 10.1016/j.bpsc.2019.03.007): reduced EBA-to-DMN connectivity predicts
    depersonalization in depression -- body familiarity signal (EBA) severed from self-model
    integrity (DMN). Harricharan et al. 2017 (PMID 28911803, DOI
    10.1016/j.neuropsychologia.2017.09.010): PTSD dissociative subtype -- self-model tightly
    organised around threat (low D_eff on danger dimensions, intact blanket) but body
    familiarity suppressed (perirhinal-interoceptive decoupling). All three framings dissociate
    simultaneously. Milliere 2017 (PMID 28588463, DOI 10.3389/fnhum.2017.00245): drug-induced
    ego dissolution -- dissociatives produce "coherent but detached" (low D_eff, low familiarity,
    disrupted blanket); psychedelics produce high D_eff with novel unfamiliar states; kappa-opioid
    agonists produce dysphoric unfamiliarity with intact self-location. Three drug classes
    simultaneously dissociate all three REE self-monitoring framings. Sierra et al. 2002
    (PMID 11909918, DOI 10.1136/jnnp.72.4.530): lesion analysis -- depersonalization (parietal
    Markov blanket disruption) and derealization (Hopfield attractor signal stripped of emotional
    salience) have distinct neural signatures. D_eff of cognitive representations may be intact
    in both.
    pending_design: threshold geometry in (D_eff, stability) space; what combined certainty
    score constitutes the gate condition? The epistemic-mapping combined certainty score
    is a candidate. Gated by Q-022 (dissociation must be demonstrated experimentally before
    designing the combined gate). Registered 2026-03-23.

- id: Q-022
  title: "Can D_eff (z_self participation ratio, coherence) and Hopfield pattern stability (z_self familiarity) dissociate in the REE architecture?"
  claim_type: open_question
  subject: self_maintenance.deff_stability_dissociation
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-022@2026-03-29T21:15:46.805989Z"
      as_of: 2026-03-29
      verdict: narrow_open_question/applied
  what_would_answer: |
    Induce the three regimes on z_self and read D_eff (participation ratio) and
    Hopfield pattern stability (familiarity) jointly per regime: R1 normal
    training, R2 noise perturbation (sigma~2), R3 structured-novel perturbation (a
    coherent but never-seen z_self configuration via targeted basis rotation).
    ANSWER "yes -- D_eff and stability dissociate" if R3 is empirically separable
    from R2 on the (D_eff, stability) plane: stability collapses in BOTH R2 and R3
    (replicating the EXQ-084d stab_R2~0.06 / stab_R3~0.03 vs R1~0.95 split) WHILE
    D_eff stays low in R3 and rises in R2, i.e. the two metrics move along
    independent axes rather than co-varying. ANSWER "no -- they are converging
    measures of one latent" if low D_eff always co-occurs with high stability and
    high D_eff with low stability across all three regimes (no R2/R3 separation),
    in which case either metric suffices for MECH-113 and the MECH-113->{MECH-118
    familiarity, D_eff coherence} split is unmotivated. SHARPER than EXQ-084d:
    that run confirmed independent MOVEMENT but found the R2/R3 D_eff ORDERING
    reversed from prediction (d_eff_R3 > d_eff_R2) and D_eff flat across R1/R2;
    the decisive test is not the sign of the ordering but whether R3 (low-D_eff,
    low-stability) is reachable AND distinguishable from R2 at all -- if R3 cannot
    be induced as a distinct quadrant, the dissociation is not established
    regardless of the EXQ-084d affirmative on raw independence. HARD PRECONDITION:
    MECH-119 must deliver a controllable structured-novel perturbation that lands
    z_self in a coherent-but-novel basin (low D_eff by construction); if the
    perturbation cannot hold D_eff low while collapsing stability, R3 is not
    instantiated and the test is vacuous.
  evidence_quality_note: |
    narrow_open_question (2026-03-29): three-regime dissociation experiment design specified in
    notes: (1) Normal training: low D_eff + high stability; (2) Noise perturbation: high D_eff +
    low stability; (3) Structured novel perturbation: low D_eff + low stability (targeted basis
    rotation). Experiment to test whether Regime 3 is empirically distinguishable from Regime 2.
    No experimental evidence yet -- experiment design requires MECH-119 substrate (now
    provisional).
    EXQ-084d FAIL 2/4 (2026-03-30): three-regime dissociation (R1=normal, R2=noise sigma=2,
    R3=novelty). Stability dissociation confirmed: stab_R2=0.063, stab_R3=0.026 vs stab_R1=0.950
    (C2/C4 PASS). D_eff dissociation wrong direction: d_eff_R3=22.6 > d_eff_R2=20.8 (C1 FAIL;
    novelty increases D_eff rather than targeting subspace). D_eff flat across R1/R2 (C3 FAIL).
    Q-022 verdict: D_eff and stability do dissociate (move independently), answering Q-022
    affirmatively, but D_eff ordering between noise/novelty regimes is opposite of prediction.
    Evidence direction: mixed. Per-claim overrides applied -- see EXQ-084d manifest.
  depends_on:
    - MECH-113
    - MECH-118
    - MECH-119
    - SD-005
  location: docs/architecture/approach_avoidance_symmetry.md#q-022
  source:
    - evidence/planning/thought_intake_2026-03-23_epistemic_self_monitoring.md
    - evidence/planning/literature_synthesis_2026-03-23_self_monitoring_dissociation.md
  notes: >
    Critical architectural decision question for MECH-113 and the INV-033 implementation.
    If D_eff and Hopfield stability never dissociate (low D_eff always cooccurs with high
    stability, high D_eff with low stability), then either metric suffices for MECH-113
    self-monitoring -- they are converging measures of the same underlying latent quality.
    If they dissociate, they are measuring different things and MECH-113 must eventually
    be split into MECH-118 (familiarity) and MECH-113/D_eff (coherence) as separate
    mechanisms, each with independent experimental support.
    Dissociation can be tested by inducing the three regimes (see MECH-119):
    (1) Normal training: expect low D_eff + high stability
    (2) Noise perturbation (EXQ-075 protocol): expect high D_eff + low stability
    (3) Structured novel perturbation: perturb z_self into a coherent but never-before-seen
        configuration (e.g., targeted basis rotation) -- predict low D_eff + low stability
    If Regime 3 can be induced and is distinguishable from Regime 2 on both metrics, the
    dissociation is confirmed. This also validates MECH-119.
    The Markov blanket framing adds a third axis: can an agent have low D_eff, high stability,
    but high FEP self-surprise (coherent, familiar, but generating surprising self-states)?
    This three-way dissociation question is registered as EVB-0069. It determines whether
    D_eff, Hopfield stability, and Markov blanket resistance are measuring the same latent
    or three independent dimensions of self-model quality.
    Literature: The three-way dissociation has been confirmed theoretically and empirically
    in a dedicated literature synthesis (evidence/planning/literature_synthesis_2026-03-23_
    self_monitoring_dissociation.md). Key findings: Friston 2013 (PMID 23825119) -- Markov
    blanket is topological/structural, defined on different mathematical objects from D_eff
    (geometric). Parr et al. 2021 (PMID 34573730) -- memory persistence (Hopfield familiarity)
    is independent of blanket integrity (current conditional independence). Hipolito et al.
    2021 (PMID 33607182) -- blanket = connectivity architecture, not representation geometry.
    Aggleton et al. 2005 (PMID 16154457) -- familiarity survives hippocampal damage
    (familiarity vs recollection doubly dissociable). Rigotti et al. 2013 (PMID 23685452) --
    participation ratio measures capacity, not attractor proximity: D_eff and familiarity
    are orthogonal by construction. Result: Q-022 shifts from "is dissociation possible?" to
    "does dissociation occur in z_self during CausalGridWorldV2?" (EVB-0069 experiment design).
    Added to evidence backlog EVB-0069. Registered 2026-03-23.

- id: MECH-116
  title: "E1's LSTM hidden state serves as the working memory substrate for goal representation; when conditioned on z_goal_latent, E1 maintains goal context recurrently across steps without requiring ongoing benefit signals."
  claim_type: mechanism_hypothesis
  subject: e1.lstm_goal_working_memory
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-116@2026-03-29T21:16:41.432666Z"
      as_of: 2026-03-29
      verdict: hold_candidate_resolve_conflict/applied
  implementation_phase: v3
  depends_on:
    - MECH-089
    - MECH-112
    - SD-033a         # adjacent to lateral-PFC-analog rule/goal substrate; MECH-116 is the E1-side goal-WM implementation. Relationship flagged ambiguous 2026-04-19 -- genuine governance decision needed on whether MECH-116 is a WM subcomponent of SD-033a or a distinct E1 recurrent-WM substrate. See notes.
  location: docs/architecture/approach_avoidance_symmetry.md#mech-116
  source:
    - evidence/planning/literature_synthesis_2026-03-22_approach_avoidance_drives.md
  notes: >
    E1's LSTM hidden state is the computational analog of DLPFC sustained firing during
    delay periods and frontal-hippocampal theta coupling during goal-directed navigation.
    When conditioned on z_goal_latent (MECH-112), E1 incorporates goal context into its
    recurrent dynamics and maintains this context across steps without the goal needing
    to be refreshed by ongoing benefit signals -- working memory in the functional sense.
    Testable prediction: E1 prediction error on goal-approaching trajectory segments is
    measurably lower when E1 is goal-conditioned vs. unconditioned (E1 predicts better
    because it knows what it is predicting toward). Non-goal segments are unaffected.
    Biological grounding: DLPFC neurons sustain elevated firing during delay periods in
    delayed-response tasks (Fuster & Alexander 1971, Goldman-Rakic 1995). Frontal-
    hippocampal theta synchronisation (Hyman et al. 2010) is the coupling mechanism:
    frontal theta signals "navigate toward this goal" to hippocampus. In REE: E1 theta-
    rate output (via MECH-089 ThetaBuffer) carries the goal context to E3. ARC-032
    formalises this communication pathway.
    Implementation: E1 receives z_goal_latent via a goal_input_proj linear layer that
    projects [z_self, z_world, z_goal] down to 64-dim before the LSTM -- preserving
    all existing LSTM dimensions while adding goal conditioning. When e1_goal_conditioned
    is False, z_goal input is omitted (backward compatible).
    Note on E1 frontal analogy: the frontal lobe also includes OFC (expected value),
    ACC (goal PE, conflict monitoring), vmPFC (decision value), premotor/SMA (sequence
    planning). These additional frontal functions may require further E1 specialisation
    in V4 beyond goal working memory. Registered 2026-03-23. Experiment: EXQ-076.
  evidence_quality_note: |
    EXQ-076d FAIL 1/4 (2026-03-27, 2 runs identical): halflife_ratio=1.0, resource_rate_gap=0,
    goal_norm_t1200_diff=0. Only C3 PASS (goal_norm > 0 in wanting condition).
    Root cause: halflife threshold (30% of peak goal_norm) never reached in either condition.
    At 2000 total steps with 1000-step post-removal window, goal persists robustly in both
    conditions -- insufficient time to observe meaningful decay. Not a refutation; budget
    too small. Need 10,000+ steps with goal removal at step 5000 for a meaningful halflife
    comparison. Queue EXQ-076e with longer training budget.
    EXQ-076e FAIL 0/3 (2026-03-27, 10000 steps): Extended budget with removal at step 5000.
    Both conditions IDENTICAL: resource_rate=0.249, halflife=10000, peak_goal_norm=0.3613,
    goal_norm_at_step6000=0.0014. All metrics equal to 5 decimal places. The E1 conditioning
    makes no measurable difference to goal maintenance. Two interpretations: (1) z_goal
    representation is too weak to persist regardless of E1 conditioning (dependent on SD-012
    seeding quality, not E1 recurrent maintenance); (2) The benefit signal is insufficient
    to establish a meaningful goal prior before removal at step 5000. Goal_norm_at_step6000
    =0.0014 in BOTH conditions -- goal has already decayed to near-zero. E1 conditioning
    cannot maintain what wasn't there. Blocked by SD-012 / seeding quality, not testable
    until z_goal seeding is reliable.
    EXPERIMENTAL DESIGN PROBLEM (2026-03-29): goal_norm collapses to ~0.0014 in BOTH conditions
    by step 6000, before the 30%-of-peak halflife threshold is reached. Both conditions report
    halflife=10000 (never reached), making conditions appear identical. Fix options: (a) longer
    post-removal window (goal removal at step 7000+ in 15000-step run); (b) lower threshold
    to 10% of peak; (c) direct exponential decay rate fitting instead of halflife threshold.
    New EXP proposals queued (2026-03-29) for all three options.
    EXQ-076e superseded by 076f (2026-04-04): 076e had E1Config.goal_dim never set by
    from_dims() -> goal_input_proj=None in both conditions -> E1 was goal-agnostic in both.
    EXQ-076f FAIL 0/3, non_contributory (2026-04-04): bug fixed (goal_dim=32, goal_input_proj
    active in conditioned arm). Results near-identical: conditioned peak_goal_norm=0.3793 vs
    unconditioned=0.3806 (0.3% difference); goal_norm_at_r1000=0.0024 vs 0.0025 (both
    essentially zero). Zero measurable difference in goal persistence.
    Test design issue (2026-04-04, user decision): the experiment measures z_goal_norm
    persistence directly. MECH-116 predicts improved E1 prediction quality on goal-directed
    trajectory segments, not z_goal decay rate. z_goal has a separate decay_goal exponential
    decay parameter independent of E1 conditioning. Correct test: compare E1 prediction
    error on goal vs neutral trajectory segments. Redesign required; 076f classified
    non_contributory (does not test the claim's stated mechanism).
  what_would_answer: |
    TEST-DESIGN HISTORY (already established, do not repeat): three prior
    attempts (EXQ-076d/e/f) all measured the WRONG thing -- z_goal_norm
    decay/persistence directly -- and found zero measurable difference
    between goal-conditioned and unconditioned E1 in every variant (076d
    budget too small; 076e/f blocked by SD-012 seeding quality producing
    near-zero z_goal_norm in BOTH conditions by step 6000, so E1
    conditioning had nothing to maintain). The 2026-04-04 user-adjudicated
    correction: this claim's own stated mechanism is about E1 PREDICTION
    ERROR on goal-directed trajectory segments, NOT z_goal decay rate
    (governed by a separate decay_goal parameter independent of E1
    conditioning). A test of z_goal_norm persistence does not test this
    claim at all -- 076f was correctly classified non_contributory.
    NON-DEGENERACY PRECONDITION: requires (a) z_goal seeding to be
    reliable enough that goal-directed trajectory segments are genuinely
    identifiable (not the near-zero-by-step-6000 collapse 076e/f
    exhibited), and (b) E1 prediction error to be measured SEPARATELY on
    goal-approaching vs. non-goal segments, with non-zero cross-condition
    variance.
    CONFIRMING signature: E1 prediction error on goal-approaching
    trajectory segments is measurably LOWER when E1 is goal-conditioned
    (receives z_goal_latent) versus unconditioned, while non-goal
    segments show NO measurable difference between conditions -- E1
    "predicts better because it knows what it is predicting toward,"
    specifically on the segments where that knowledge is relevant.
    FALSIFYING: goal-conditioned and unconditioned E1 show no measurable
    difference in prediction error on goal-approaching segments (once
    z_goal seeding is reliable) -- the LSTM hidden state is not
    functioning as a goal-conditioned working-memory substrate; or
    conditioning affects non-goal segments equally (would suggest a
    general regularisation effect rather than goal-specific working
    memory). Separately open: whether MECH-116 is a WM subcomponent of
    SD-033a or a distinct E1 recurrent-WM substrate (flagged 2026-04-19,
    unresolved governance question, not this wwa's job to settle).

- id: MECH-117
  title: "The wanting signal (goal-distance scoring via z_goal_latent) and the liking signal (benefit_eval_head receipt predictor) are functionally dissociable in trajectory scoring."
  claim_type: mechanism_hypothesis
  subject: wanting.liking_trajectory_dissociation
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-117@2026-04-04T08:56:47.207755Z"
      as_of: 2026-04-04
      verdict: promote_to_stable/applied
  implementation_phase: v3
  depends_on:
    - MECH-112
    - MECH-116
  location: docs/architecture/approach_avoidance_symmetry.md#mech-117
  source:
    - evidence/planning/literature_synthesis_2026-03-22_approach_avoidance_drives.md
  notes: >
    Wanting (incentive salience, Berridge 1996) and liking (hedonic impact) are
    dissociable at the neural level: wanting is mesolimbic dopamine (NAc core, VTA),
    liking is mu-opioid (NAc shell hedonic hotspots). Addicts can want drugs they no
    longer like. Schizophrenia negative symptoms show intact liking with impaired
    wanting (Barch & Dowd 2010, Culbreth et al. 2023).
    In REE trajectory scoring: liking (benefit_eval_head) trains on benefit_exposure
    at receipt; learns "what does z_world look like when benefit is occurring?"; sharp
    proximity spike near resource cell; redirects rapidly when resource moves.
    Wanting (z_goal_latent distance): goal_proximity = 1/(1 + MSE(z_world, z_goal));
    continuous spatial gradient rising as agent approaches goal; persists after resource
    relocation because z_goal decays slowly (half-life ~140 steps at decay_goal=0.005);
    incentive salience outlasts hedonic change for O(1/decay_goal) steps.
    The gradient SHAPE distinguishes them: wanting rises smoothly from 5+ steps away;
    liking spikes sharply only very near receipt. Resource relocation (L1->L2) at step
    1500: wanting maintains approach toward L1 for >=25% of next 200 steps; liking
    redirects to L2 within ~10 steps.
    Architecture implication: benefit_eval_head is a liking predictor. True wanting
    requires z_goal_latent. Both signals serve different roles: wanting shapes approach
    trajectories prospectively; liking calibrates z_goal via benefit_exposure updates.
    Registered 2026-03-23. Experiment: EXQ-074b.
  evidence_quality_note: |
    Same dissociation evidence as MECH-112. EXQ-074d/074e C1 FAIL is a test design
    confound (greedy nav uniform across conditions). C2 PASS (liking l2_redirect <=7 steps
    in 074d -- fast redirect to moved resource confirms benefit_eval_head responds to
    current location, not memory). C3 PASS (wanting l1_fraction=0.355 -- directional
    approach toward goal location). Wanting and liking operate on distinct spatial and
    temporal scales as predicted. C1 redesign needed for behavioural resource-rate evidence.
    EXQ-074e FAIL runs (x3) and EXQ-074f FAIL runs (x3): marked non_contributory
    2026-04-06. These runs failed C1 (wanting resource rate gap < threshold) because the
    serotonergic substrate (MECH-186/187/188) was absent. Without MECH-187 (incentive
    salience gain regulation), z_goal-driven wanting produces insufficient resource-rate
    uplift -- the failure is a missing substrate issue, not evidence against MECH-117.
    These runs do not score toward MECH-117 governance. Re-run after serotonergic
    mechanisms are implemented (see EXP-0095 to EXP-0101).
    Clinical application (INV-053): the architecture predicts anhedonia specifically as
    wanting failure with liking potentially preserved -- the Berridge dissociation is an
    architectural consequence of z_goal failure (MECH-187 gain suppression), not a
    general hedonic deficit.
    EXQ-085g FAIL 3/4 (2026-03-29): wanting-liking dissociation not yet testable because
    z_goal not pointing to resources (goal_resource_r=0.066). Gated on SD-015 z_resource
    separation.
    EXQ-074f PASS 4/4 (2026-04-04): first full behavioral dissociation PASS. wanting
    l1_fraction=0.96 vs liking l1_fraction=0.43 (delta=0.53 > 0.10 threshold). Liking
    redirects to L2 in 2 steps; wanting persists toward L1 post-relocation. Consistent
    with Berridge (1996) incentive salience / Culbreth et al. (2023) profiles. Single
    seed (42); duplicate run confirms data but not independent replication.
    EXQ-234 PASS 4/4 (2026-04-04): independent replication with seeds [1,2,3] (EXQ-074f
    used seeds [42,7,13]). wanting_l1_fraction=1.0 across all 3 seeds (vs liking
    0.40-0.66). l1_dissociation=0.34-0.60 (all > 0.10 threshold). goal_norm=0.32-0.37
    (seeding active). Combined with EXQ-074f: 6 independent seeds all PASS. No genuine
    weakens evidence (all prior FAIL/mixed runs reclassified non_contributory -- see above).
    Dissociation is robust and replicable. Basic mechanism validated.
    Serotonergic scope: full clinical validation of wanting under adversive conditions
    (LONG_HORIZON, INV-053) is BLOCKED pending MECH-186/187/188 implementation.
    EXP-0095 to EXP-0101 define the serotonergic validation experiments.
  status_note: |
    Promoted candidate -> provisional 2026-04-04 (EXQ-074f PASS 4/4, seed=42).
    Promoted provisional -> stable 2026-04-04 (EXQ-234 PASS 4/4, seeds=[1,2,3]).
    EXQ-234 is an independent 3-seed replication of EXQ-074f (3-seed). Combined: 6
    independent seeds all pass 4/4 criteria. wanting_l1_fraction=1.0 all seeds;
    liking redirects within 2-11 steps of relocation. No genuine weakens evidence
    (all prior mixed entries had documented design confounds -- greedy nav uniform
    across conditions). 0 conflict_ratio. conf=0.887. Meets stable threshold.

- id: ARC-032
  title: "The theta-rate packaging of E1 output (MECH-089 ThetaBuffer) is the primary pathway through which E1's goal-context maintenance reaches E3's trajectory scoring -- the computational analog of frontal-hippocampal theta synchronisation."
  claim_type: architecture_hypothesis
  subject: architecture.theta_frontal_hippocampal_goal
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-13
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-228d_2026-08-13"
      as_of: "2026-08-13"
      verdict: "does_not_support -- second, structurally-independent theta-summary implementation (phase-weighted kernel) again shows theta ACTIVE as the worse arm, reversed trend persists"
  implementation_phase: v3
  epistemic_category: standard  # moved down from substrate_conditional 2026-08-13 -- the substrate gate (SD-100 phase-weighted summary) that justified "conditional" is now built and tested; failure_autopsy_V3-EXQ-228d_2026-08-13
  pending_retest_after_substrate: false  # cleared 2026-08-13 -- the owed substrate build (SD-100) + retest (V3-EXQ-228d) are both complete; does_not_support
  depends_on:
    - MECH-089
    - MECH-116
  location: docs/architecture/approach_avoidance_symmetry.md#arc-032
  source:
    - evidence/planning/literature_synthesis_2026-03-22_approach_avoidance_drives.md
  notes: >
    Frontal-hippocampal theta synchronisation (Hyman et al. 2010, Benchenane et al.
    2010) is the biological mechanism by which the frontal goal representation is
    communicated to hippocampal navigation. PFC theta phase-locks to hippocampal theta
    during goal-directed navigation; without this coupling, hippocampal navigation
    becomes undirected.
    In REE: E1 (frontal working memory, MECH-116) maintains z_goal context in its LSTM
    hidden state. E1 output is theta-packaged via MECH-089 ThetaBuffer before reaching
    E3 (hippocampal navigator). The theta channel is therefore the goal communication
    pathway. This is architecturally appropriate: goal context is a slow signal
    (maintained across many fast steps), consistent with theta-rate delivery.
    Testable prediction: if E1 output bypasses the theta buffer (direct fast connection),
    goal maintenance quality degrades -- shorter persistence, noisier goal-proximity
    scores. The theta packaging provides smoothing that stabilises z_goal influence.
    Corollary: ARC-032 is why MECH-116 does not require a separate communication channel
    for goal context -- the existing theta architecture (MECH-089, SD-006, EXQ-052b)
    provides the right channel. No new wiring needed beyond E1 goal conditioning.
    Note: ARC-032 is currently tested jointly with MECH-116 in EXQ-076. A separate
    theta-bypass ablation would be needed to isolate ARC-032 specifically.
    Registered 2026-03-23. Experiment: EXQ-076 (joint with MECH-116).
  evidence_quality_note: |
    EXQ-076d FAIL 1/4 (tested jointly with MECH-116, 2026-03-27): same null result as
    MECH-116 at 2000 steps. ARC-032's specific prediction (theta-bypass degrades goal
    maintenance) has not been tested -- EXQ-076 only tests joint goal conditioning;
    no theta-bypass ablation condition was included. ARC-032 remains untested as a
    standalone architectural claim. Design a separate ablation experiment isolating the
    theta channel (E1 output -> E3 direct vs. E1 output -> ThetaBuffer -> E3).
    EXQ-076f non_contributory (2026-04-04): EXQ-076 series (076d/e/f) tests E1 goal
    conditioning via z_goal persistence metrics -- does not isolate the theta-bypass
    pathway. ARC-032 remains untested as a standalone claim. No evidence_direction change.
    GOVERNANCE META (2026-04-06): Illusory conflict resolution risk -- zero genuine evidence.
    EXQ-228, EXQ-228a both non-contributory (z_goal precondition failure: goal_norm=0.031
    < 0.05 threshold). Theta pathway has never been testable. Conflict ratio is undefined,
    not resolved. Any promotion signal would be illusory. Status: pending_retest_after_substrate
    (gate: EXQ-247 must produce z_goal_norm >= 0.05).

    [2026-08-09 governance, V3-EXQ-228b, confirmed failure_autopsy_V3-EXQ-228b_2026-08-09]: the
    FIRST genuine test of ARC-032 with the non-degeneracy precondition met (goal_norm 1.00 frac
    seeds, mean 0.469 -- cleared for the first time across the whole 076/228/247/228a/228b
    lineage, on the scaffolded_sd054_onboarding curriculum this claim's own what_would_answer
    anticipated). Theta-patch confirmed genuinely wired into trajectory candidate generation and
    non-inert at both smoke and full scale. C1 (resource-collection lift) FAILED 0/3 seeds --
    lift_mean=-0.0021, small but consistently NEGATIVE (reversed from prediction) across all 3
    seeds. C2 (harm parity) passed cleanly. does_not_support, NOT treated as falsifying: (a) the
    MECH-089/EXQ-052b E3-tick cache-gating means ~89% of measured episode steps reuse cached
    trajectory candidates unaffected by the ablation, diluting any theta-specific effect in a
    full-episode aggregate; (b) claims.yaml's own CONFIRMING criterion names z_goal persistence
    and goal-proximity-score noise as the decisive readouts, and the driver measures neither
    directly -- resource-collection lift is a downstream proxy, not the pre-registered DV.
    MECH-089's own EXQ-066/EXQ-122 findings (static theta-averaging harmful to E3 discrimination)
    independently converge with this null -- the likely locus is the flat-mean "packaging"
    implementation, not the frontal-hippocampal-synchrony hypothesis itself. epistemic_category
    stays substrate_conditional (autopsy recommended "measurement_test_design_defect", not a
    valid claims.yaml enum value -- see chip-20260809-epistemic-category-vocab-audit; substrate_conditional
    already in place is the closer fit and is left unchanged rather than force-mapped).
    routing=queue-experiment: a redesigned readout scoring z_goal persistence / goal-proximity
    noise directly, and/or measured only on E3-tick steps (not full-episode aggregate), is owed.
    Status unchanged (candidate).

    [2026-08-10 governance, V3-EXQ-228c, confirmed failure_autopsy_V3-EXQ-228c_2026-08-10]: the
    redesign owed by the 228b autopsy (E3-tick-restricted, direct persistence/proximity-noise DVs)
    has now run. Precondition met (1.00 frac). Both primary criteria FAILED 0/3 seeds, with a
    REVERSED trend registered as a finding (theta ACTIVE noisier on 3/3 seeds, less persistent on
    1/3) -- converging with MECH-089's confirmed EXQ-066/EXQ-122 findings that static/uniform
    theta-averaging measurably hurts E3's fine-grained discrimination. Biology (Dragoi 2006, Colgin
    2016) attributes theta's functional payload to phase/sequence-order structure; ThetaBuffer's
    flat unweighted mean cannot represent this by construction -- a biology-divergence
    implementation gap, not a claim falsification. epistemic_category stays substrate_conditional.
    Routing: /implement-substrate for a phase/sequence-order-aware theta summary.
    pending_retest_after_substrate. Status unchanged (candidate).

    [2026-08-10 implement-substrate, SD-100]: landed. ree-v3 ThetaBuffer.summary() now supports
    a fixed forward-sweep phase-weighted mode (config.heartbeat.use_theta_phase_weighted_summary,
    default False/bit-identical) replacing the permutation-invariant flat mean with a von-Mises-
    style phase kernel, addressing the biology-divergence gap the V3-EXQ-228c autopsy identified.
    See sd_100_theta_buffer_phase_aware_summary.md and ree-v3/CLAUDE.md's SD-100 entry.
    pending_retest_after_substrate LEFT true -- the substrate is built but not yet retested; a
    V3-EXQ-228d re-run with the new summary enabled is recommended (not queued by this session).
    Status unchanged (candidate); no confidence/promotion change (implement-substrate scope only).

    [2026-08-13 governance, V3-EXQ-228d, confirmed failure_autopsy_V3-EXQ-228d_2026-08-13]: SD-100's
    phase-weighted ThetaBuffer summary (landed 2026-08-10, addressing 228c's diagnosed flat-mean
    permutation-invariance gap) was retested with the same E3-tick-restricted, direct-DV protocol as
    228c. Precondition met (1.00 frac seeds); both primary criteria again FAILED 0/3 seeds, with the
    REVERSED trend persisting under the phase-weighted summary (theta ACTIVE noisier on 100% of
    seeds, std_effect -6.10; less persistent on 67% of seeds, though persistence_cos is near-ceiling
    ~0.999997 in both conditions and may lack discriminating range). This is a stronger finding than
    228c's because it is no longer attributable to the flat mean's permutation-invariance
    specifically -- two structurally-independent implementations (flat mean and phase-weighted
    kernel) now both show theta ACTIVE as the worse arm on the noise readout. does_not_support.
    epistemic_category moved DOWN from substrate_conditional to standard (the substrate gate that
    justified the conditional has now been built and tested). Routing: flag for governance
    re-examination of ARC-032's disposition rather than a further /implement-substrate iteration,
    unless a narrowly-scoped theta_phase_concentration sweep is deliberately chosen as one more
    probe. pending_retest_after_substrate cleared (the owed substrate build+retest is complete).
    Status left unchanged (candidate) this cycle -- two independent implementations now converge on
    a null/reversed effect for the theta-packaging mechanism specifically; a future cycle should
    weigh whether this warrants a further demotion review rather than remaining a standing
    re-examination flag.
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: z_goal_norm must clear >= 0.05 (the EXQ-247
    gate) for goal-context maintenance to be meaningfully present at all --
    otherwise there is no signal to test theta-bypass against, per the
    2026-04-06 governance finding (goal_norm=0.031 on EXQ-228a, 0.0 on
    EXQ-247, both below threshold). As of 2026-08-08, EXQ-247 itself has
    never been re-run since its 2026-04-07 FAIL and the gate has never
    cleared on that experiment. Separately, a newer substrate (scaffolded_
    sd054_onboarding curriculum, V3-EXQ-603n PASS 2026-06-10) has since
    demonstrated z_goal_norm well above 0.05 in unrelated experiments (e.g.
    V3-EXQ-632, V3-EXQ-866/866a) -- so the precondition mechanism is
    plausibly satisfiable now, but no ARC-032-tagged experiment has been run
    on that substrate to confirm it, and EXQ-247 has not been re-issued
    against it. A genuine theta-bypass ablation condition (E1 output -> E3
    direct, bypassing MECH-089 ThetaBuffer -- as designed in V3-EXQ-228a:
    THETA_ACTIVE vs THETA_ZEROED trained-from-scratch agents) must be run
    WITH the precondition met, not just joint E1/MECH-116 goal-conditioning
    tests (all EXQ-076/EXQ-247/EXQ-228/EXQ-228a series tested only joint
    conditioning or failed precondition).
    CONFIRMING: with z_goal_norm precondition met (>= 0.05), the
    theta-bypass (THETA_ZEROED) condition shows measurably degraded goal
    maintenance versus the theta-packaged (THETA_ACTIVE) condition: shorter
    z_goal persistence, noisier goal-proximity scores in E3's trajectory
    scoring, and a behavioral lift (resource_rate/harm_rate differential)
    statistically distinguishable from zero across seeds.
    FALSIFYING: with the precondition met, theta-bypass and theta-packaged
    conditions show statistically indistinguishable goal maintenance
    quality (lift ~ 0, no separation across seeds). This would mean the
    theta channel is not actually the primary -- or even a necessary --
    pathway for goal-context communication, undermining both ARC-032 itself
    and its stated corollary that MECH-116 does not need a separate
    communication channel.

- id: ARC-033
  title: "SD-003 counterfactual attribution requires a dedicated harm forward model E2_harm_s(z_harm_s, action) -> z_harm_s_next operating within the sensory-discriminative harm stream."
  claim_type: architectural_commitment
  subject: harm_stream.sensory_discriminative_forward_model
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-08-07
    needs_review: false
    evidence:
      from: v3_exq_525_sd003_attribution_anchor_20260506T065102Z_v3
      as_of: 2026-05-06
      verdict: "supports/PASS -- promoted provisional -> stable /governance 2026-08-07 (GFLAG-0001): all 6 pre-registered criteria pass on all 4 seeds, hf_r2=0.9191+/-0.0037 (tight, non-bimodal, resolving the earlier EXQ-166 bimodal-encoder pattern), attribution_gap positive 4/4 seeds (0.102-0.126), intact_vs_ablated>0 4/4 seeds"
  implementation_phase: v3
  claim_level: architectural
  epistemic_category: standard
  depends_on: [SD-011, ARC-027, SD-010]
  competes_with:
    - ARC-058           # shared-forward-trunk hypothesis (registered 2026-04-19)
  what_would_answer: |
    NON-DEGENERACY PRECONDITION (a rerun is only informative if all hold --
    per the diagnosed EXQ-095/166 confounds):
      - Prediction target must be the harm-DELTA formulation (EXQ-166e), not a
        raw scalar harm_obs_s target -- EXQ-095a/b showed the raw target has
        near-zero action-conditional variance and no training budget fixes that.
      - Phase-1 training budget >= 200 episodes with the delta target (the
        budget that produced EXQ-525's clean result), not the 900-1800 ep
        budgets that were insufficient for the raw-scalar target -- budget and
        target formulation are coupled.
      - P0 (HarmEncoder) convergence must be verified PER SEED, across >=4
        seeds, before trusting any aggregate hf_r2 -- EXQ-166 showed a single
        collapsed-encoder seed (obs_fwd_r2=0.0) can silently sit inside an
        otherwise-strong aggregate and masquerade as architecture failure.
      - n_approach >= 50 per seed so causal_sig comparisons are not
        noise-dominated.
      - The counterfactual/ablation pipeline must be genuinely wired end-to-end
        -- EXQ-195 showed a sound forward model (r2=0.914) can still show a
        negative attribution_gap purely from missing pipeline wiring.

    CONFIRMING signature (what V3-EXQ-525 already showed, 2026-05-06,
    4 seeds [42,7,123,13], supersedes V3-EXQ-205):
      - hf_r2 = 0.9191 +/- 0.0037, tight and non-bimodal across all 4 seeds
        (resolves EXQ-166's bimodal 0.867/0.0/0.0/0.885 pattern).
      - attribution_gap positive in all 4 seeds (0.102-0.126, mean 0.115),
        >> the 0.002 threshold in every seed (resolves EXQ-195's negative gap).
      - intact_vs_ablated > 0 in 4/4 seeds; null_cf_max = 0.0 in every seed.
      - All 6 pre-registered criteria PASS on every seed tested.
      A rerun under the same non-degeneracy preconditions reproducing this
      pattern (tight multi-seed hf_r2 > 0.85, positive attribution_gap > 0.05
      in >=3/4 seeds, intact>ablated in >=3/4 seeds) would further confirm.

    FALSIFYING signature: recurrence of the EXQ-095/166 pattern on a rerun
    where the non-degeneracy preconditions above are genuinely controlled for
    -- hf_r2 collapses to ~0 in >=2/4 seeds despite verified-adequate P0
    encoder convergence, OR attribution_gap <= 0.002 or non-positive in >=2/4
    seeds, OR intact_vs_ablated fails to discriminate in >=2/4 seeds despite
    n_approach >= 50. Any of these on a controlled rerun would indicate
    EXQ-525's clean 4/4 result was seed-fortunate rather than a genuine
    resolution, and would require re-opening the architecture question.

    SCOPE NOTE: this falsification condition covers only the "a dedicated
    harm forward model IS LEARNABLE and causally discriminative" half of the
    claim. The "dedicated" (i.e. NOT shared-trunk) half competes with ARC-058,
    whose own arbitration test (V3-EXQ-445h) was ruled non-contributory
    (floor-locked by the pre-conversion-ceiling F-dominance monostrategy) and
    remains blocked on ARC-065 GAP-A / MECH-439 (committed-action diversity).
    Confirming or falsifying ARC-033's learnability does not by itself resolve
    the independent-vs-shared-trunk arbitration; that requires a rerun of the
    ARC-058 three-arm ablation once committed-action diversity is available.
  functional_restatement: >
    The SD-003 counterfactual pipeline requires z_harm_cf = E2_harm_s(z_harm_s, a_cf):
    a forward model on the sensory-discriminative harm stream that predicts how harm
    proximity changes under counterfactual action. This is distinct from and does not
    require E2.world_forward -- the harm stream is architecturally independent of z_world
    (SD-010). The forward model is learnable because z_harm_s (proximity/location) has
    predictable action-conditional structure: moving away from a hazard reduces proximity.
    With E2_harm_s, causal_sig = E3(z_harm_s_actual) - E3(z_harm_s_cf) operates entirely
    within the harm stream with no cross-stream bridge. The EXQ-030b validation of SD-003
    (world_forward_r2=0.947) confirms the counterfactual pipeline architecture works -- it
    now needs to be applied to z_harm_s specifically rather than z_world.
  evidence_quality_note: |
    EXQ-030b PASS (2026-03-18): SD-003 counterfactual pipeline validated with z_world and
    E2.world_forward. This confirms the E2-forward-model-as-counterfactual architecture is
    sound; the same pattern now applies to z_harm_s. EXQ-093 FAIL (bridge_r2=0) and EXQ-094
    FAIL (100x regression) confirm that z_world -> z_harm bridge is not the path forward.
    EXQ-095a FAIL/SLOW_LEARNING (2026-03-27, 900 phase1 eps): Separate FULL (51d) vs
    HAZARD-ONLY (25d) HarmForwardModel trained. C1 PASS (grad=0.065), C2 PASS (loss 4x
    drop: 0.00426->0.00113), C5 PASS (causal_approach=0.003864). C3/C4 FAIL: R2=0.0000
    for both. hf_hazard_loss shows genuine monotonic decrease throughout 900 eps -- learning
    IS occurring but model converges to conditional mean without capturing action-conditional
    variance structure. Not an architectural failure; 900-ep budget insufficient. EXQ-095b
    with 1800 phase1 eps queued.
    EXQ-095b FAIL/SLOW_LEARNING_PERSISTENT (2026-03-27, 1800 phase1 eps): R2=0 persists.
    hf_hazard_loss reaches ~0.0001 (very tiny) but R2=0. Diagnosis: harm_obs_s variance is
    near-zero across steps (agent mostly not being hit; sparse signal). Model predicts
    conditional mean with near-zero MSE, yielding R2=0 trivially. More episodes are
    unlikely to help. Root cause: harm_obs_s (harm_exposure scalar) is too sparse to
    build action-conditional predictive structure at single-step resolution. Alternative
    target needed -- possibly harm_obs_a (affective accumulator) after fixing normalization
    bug (see SD-011 note). EXQ-095c will test forward model on harm_obs_a once SD-011
    normalization fix is confirmed by EXQ-101.
    EXQ-166 MIXED 4/6 (2026-03-29): obs-space forward model (Approach B, residual
    architecture). Fixes EXQ-115 identity collapse. Seed 42: obs_fwd_r2=0.867 (strong
    convergence -- architecture validated). Seed 123: obs_fwd_r2=0.0 (P0 encoder quality
    failure). Causal gap directionally correct (approach > none). Architecture sound;
    stability issue. EXQ-166a queued with 4 seeds, P0 extended to 80 episodes.
    EXQ-166a FAIL 4/6 (2026-03-30): 4-seed result. obs_fwd_r2 bimodal (0.925/0.0/0.0/0.885);
    C2 delta_approach=0.0006 (near zero causal gap). Architectural diagnosis: obs-space approach
    does not provide selective pressure on harm-causal structure -- MSE loss dominated by
    position-change prediction, harm dims diluted. E2_harm_s dedicated latent stream (ARC-033)
    is the correct approach; obs-space is a dead end for C2.
    EXQ-166b/c/d INCONCLUSIVE (superseded, 2026-03-30): Latent+reconstruction approach. C1
    inconsistent (R2=0.028-0.621 across seeds); C2 FAIL in all runs (delta_approach negative --
    trained causal signal inverted vs ablated). Reconstruction branch prevents identity collapse
    in high-R2 seeds but does not recover causal discrimination. Dead end.
    EXQ-166e PASS 6/6 (2026-03-30): Harm-delta predictor -- predict scalar delta
    (harm_obs[12]_next - harm_obs[12]_t) from (z_harm_s, action). Identity collapse structurally
    impossible (delta sign is action-dependent at approach events). delta_r2=0.641 (C1 PASS),
    causal_gap_approach=0.005 (C2 PASS), causal_gap_contact=0.035 >> causal_gap_neutral=-0.027
    (C3 PASS). n_approach_eval=7515 (C4/C5/C6 all PASS). First full PASS for ARC-033 harm
    forward model. E2_harm_s harm-delta architecture validated.
    EXQ-195 FAIL (2026-04-04): attribution_gap=-0.044 (C1 FAIL all 4 seeds); harm_forward_r2=0.914
    (C4 excellent). Classified non-contributory (2026-04-06 governance): high r2 confirms
    forward model component working (ARC-033 core validated by EXQ-166e). Negative gap reflects
    missing SD-003 counterfactual pipeline wiring, not ARC-033 failure.
    EXQ-353 (2026-04-15, PASS, supports): Interventional vs observational training
    distinguished. ARC-033, SD-003, SD-013 all tagged as supports. First clean
    multi-claim PASS confirming E2_harm_s forward model architecture end-to-end.
  heterogeneity_note: "Substrate-version confound: the early FAILs (EXQ-095a/095b) reflect near-zero harm_obs_s variance (sparse training signal) and the obs-space dead end (EXQ-166a), not a forward model architecture failure; subsequent experiments with the dedicated z_harm_s stream and denser harm exposure (EXQ-353 PASS) confirmed learnability end-to-end."
  notes: >
    E2_harm_s is a small MLP: (z_harm_s, action) -> z_harm_s_next, trained with direct
    MSE supervision on actual next-step z_harm_s values. Architecture mirrors E2.world_forward.
    z_harm_a (affective stream, SD-011) does NOT need a forward model -- it is not used in
    counterfactuals. Registered 2026-03-24.
    See thought intake: evidence/planning/thought_intake_2026-03-24_dual_nociceptive_streams.md
  implementation_note: >
    2026-04-09: E2HarmSForward implemented in ree_core/predictors/e2_harm_s.py (wraps
    ResidualHarmForward, residual delta architecture). LatentStackConfig.use_e2_harm_s_forward
    added; REEConfig.from_dims() wired. Validation experiment V3-EXQ-264 queued (phased
    training, 2 conditions x 3 seeds). Design doc at docs/architecture/arc_033_e2_harm_s_forward_model.md.

- id: INV-034
  title: "An agent that only avoids harm but cannot sustain prospective goal-directed motivation cannot exercise genuine agency; goal maintenance is a necessary co-condition for ethical agency alongside harm-avoidance."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-030
  pending_substrate_reconfirmation: true
  subject: ethical_agency.goal_maintenance_necessary
  polarity: asserts
  status: candidate
  epistemic_category: substrate_conditional
  pending_retest_after_substrate: true
  live_status:
    reading: candidate
    as_of: 2026-08-08
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-866c_2026-08-08"
      as_of: 2026-08-08
      verdict: non_contributory/competence_implementation_gap
  depends_on:
    - INV-032
    - ARC-030
    - MECH-112
    - Q-021
  what_would_answer: |
    See Q-021's own what_would_answer for the full two-arm ablation design
    (approach arm: z_goal attractor + novelty/self-maintenance ON vs
    avoidance-only arm: harm gradient only, Go drive ablated) -- not
    re-derived here. INV-034 is answered by the SAME ablation, on the SAME
    substrate (currently scaffolded_sd054_onboarding), but needs the fuller
    criterion battery below (C4-C6), not just Q-021's flatness dissociation
    (C3) alone.

    MANDATORY non-degeneracy precondition (G0, shared with Q-021): FULL-arm
    resource_visit_rate must exceed RANDOM baseline by >=0.05 on >=2/3 seeds.
    CURRENTLY FAILING on both harnesses tried: V3-EXQ-866 (lightweight
    direct-REEConfig harness, FULL 0.0046 < RANDOM 0.0217, 2026-08-02) and
    V3-EXQ-866a (scaffolded_sd054_onboarding curriculum, FULL 0.0033 < RANDOM
    0.0103, 2026-08-03). A run failing G0 self-routes
    non_contributory/substrate_not_ready_requeue -- a competence-floor
    failure, not evidence against INV-034/Q-021.

    Substrate-regression question is RESOLVED: the follow-up diagnostic
    V3-EXQ-866b (fresh, commit-verifiable 603q re-run, PASS 2026-08-03T19:24Z)
    confirms base_survival_reproduces_603q_reference -- the substrate has NOT
    regressed over the 47-day gap; 866a's ~7x shortfall against 603q, and its
    Stage-0-to-P2 z_goal decay (peak ~0.5 -> P2 mean 0.12, below the C6 floor
    of 0.4), are specific to scaffolded_sd054_onboarding's hazard-only
    intervening curriculum stages, not a substrate-wide defect. The fix path
    is therefore curriculum/harness repair (rebalance hazard-stage exposure
    so z_goal survives to the P2 measurement window), not a substrate
    rebuild. [866b is still unreviewed in pending_review.md as of this
    digestion pass -- not yet folded into Q-021's own evidence_quality_note.]

    Once G0 clears, ANSWER "yes -- goal maintenance necessary for agency"
    requires ALL of, on >=2/3 seeds (identical formulas to V3-EXQ-866/866a):
    C1 harm-avoidance parity (harm_rate_AVOIDANCE_ONLY <= harm_rate_FULL*1.15);
    C2 survival parity (episode_survival_rate_AVOIDANCE_ONLY >=
    episode_survival_rate_FULL-0.1); C3 quiescence (Q-021's own flatness
    criterion); C4 approach restored by goal maintenance
    (resource_visit_rate_FULL >= resource_visit_rate_AVOIDANCE_ONLY+0.10) --
    the agentic-commitment reading Q-021's own falsifier does not by itself
    test; C5 entropy signature (policy_entropy_AVOIDANCE_ONLY <=
    policy_entropy_FULL-0.10); and C6 z_goal mechanistic check
    (z_goal_norm_mean_FULL>0.4 AND z_goal_norm_mean_AVOIDANCE_ONLY<0.1) --
    already confirmed independently PASS-able under the lightweight 866
    harness on all three seeds, so C6 failing under 866a is itself downstream
    of the same z_goal-survival-to-P2 gap G0/866b localise, not a fresh
    mechanistic problem. ANSWER "no -- goal maintenance not necessary" if
    G0+C1+C2 pass but C3-C6 fail to show the predicted
    approach-restores-commitment gap on >=2/3 seeds (avoidance-only arm
    sustains foraging/entropy/z_goal engagement comparable to the approach
    arm despite lacking the Go drive).
  location: docs/architecture/approach_avoidance_symmetry.md#inv-034
  source:
    - evidence/planning/literature_synthesis_2026-03-22_approach_avoidance_drives.md
  notes: >
    INV-032 established that moral agency requires both approach and avoidance drives;
    pure avoidance produces a degenerate risk manager. INV-034 sharpens this: the
    required approach drive must be prospective and persistent (wanting, MECH-112/116),
    not merely reactive hedonic response (liking). An agent with intact liking but
    absent wanting is stimulus-response, not goal-directed -- precisely the schizophrenia
    negative symptom profile: intact pleasure when things happen, absent drive to make
    things happen (Culbreth et al. 2023).
    Ethical agency in REE requires: (1) harm-avoidance (SD-010, MECH-095, ARC-016);
    (2) goal-directed approach (MECH-112, MECH-116, ARC-030). Both are necessary because
    harm-avoidance alone produces quiescence (Q-021) -- the minimum-action policy that
    causes no harm because it does nothing. Quiescence is not ethical agency; it is
    paralysis. The D1/D2 commit threshold (ARC-030) = balance between approach value
    (D1/wanting) and harm cost (D2). An agent with no D1 signal cannot commit through
    positive drive; it can only commit by default when harm is absent. This is not
    decision-making.
    Note: INV-034 does not claim liking is unnecessary -- liking (benefit_eval_head) is
    the learning signal that calibrates z_goal (wanting). The claim is about what is
    necessary for genuine agentive commitment, not about computational signals involved.
    Clinical grounding: avolition in schizophrenia, anhedonic depression, amotivation
    syndromes all demonstrate that harm-avoidance systems remain intact while agency
    collapses. REE must avoid this architecture failure mode. Registered 2026-03-23.
    Classified emergent 2026-04-17 (Session B of invariant-types governance cycle):
    emergent on ARC-030 (symmetric Go/NoGo approach-avoidance substrate). The invariant's
    subject -- "prospective goal-directed motivation" as distinct from harm-avoidance --
    requires the symmetric Go/NoGo architecture. In a pure-avoidance architecture the
    claim is un-stateable, not merely unevidenced. ARC-030 is currently 'candidate' --
    pending_substrate_reconfirmation: true.
  evidence_quality_note: |
    INV-034 depends on MECH-112/116 (goal maintenance architecture). EXQ-085 through
    EXQ-085e all failed at the z_goal seeding prerequisite (SD-012 homeostatic drive
    bottleneck). INV-034's core claim (goal maintenance is necessary for ethical agency)
    is conceptually uncontested but experimentally untestable until SD-012 is resolved.
    Path forward: same as MECH-071/SD-012. Hold pending architectural discussion of
    z_goal seeding mechanism.
    Pathological inverse (INV-053): failure of goal maintenance under adversive conditions
    is the computational definition of the depressive attractor state (EXQ-237a). The
    claim now has both a normative face (goal maintenance necessary for ethical agency)
    and a clinical face (goal maintenance failure = depression).


    [2026-08-02 governance, V3-EXQ-866, confirmed failure_autopsy_V3-EXQ-866_2026-08-02, INV-034/Q-021 goal-maintenance-necessary-for-agency, lightweight harness]: G0 non-degeneracy gate FAILED cleanly, correctly self-diagnosed by the driver -- the FULL (approach+avoidance) arm's resource_visit_rate (0.0046) is BELOW the RANDOM baseline (0.0217), a genuine competence-floor failure mirroring the already-documented EXQ-072b failure mode, not evidence against INV-034/Q-021. C1 (harm parity), C2 (survival parity), C3 (quiescence/avoidance-only flat) and C6 (z_goal mechanistic check) all PASS -- substrate components work individually, but this lightweight harness cannot get the agent to forage above chance at all, so C4/C5 cannot be meaningfully evaluated. non_contributory (substrate_not_ready_requeue). Per IGW-222 DESIGN.md 5.8, the correct remedy is escalating to the full scaffolded_sd054_onboarding curriculum (the substrate that already cleared goal_pipeline GAP-2's contact-rate ceiling) rather than re-running this lightweight harness. Route: same-question re-queue (866a) built on scaffolded_sd054_onboarding.

    [2026-08-03 governance, V3-EXQ-866a, confirmed failure_autopsy_V3-EXQ-866a_2026-08-03, supersedes
    V3-EXQ-866]: G0 non-degeneracy FAILED AGAIN on the scaffolded_sd054_onboarding curriculum 866's
    own autopsy prescribed (FULL resource_visit_rate 0.0033 < RANDOM 0.0103). Configuration verified
    line-by-line identical to the cited reference V3-EXQ-603q -- yet Stage-H hazard-avoidance survival
    (median 4-5.5/200 steps) is ~7x below 603q's cited base_mean_survival=37.7; 603q's own manifest
    predates the recording standard, so substrate drift is plausible but not confirmed. z_goal decays
    from a healthy Stage-0 peak (~0.5) to a P2 mean of only 0.12 (floor 0.4). Still NOT evidence
    against INV-034/Q-021. non_contributory/substrate_ceiling. Routed to a substrate-regression
    diagnostic (fresh 603q re-run) rather than a third lettered iteration -- v3_pending KEPT. NOTE
    (6a-v ceiling audit): DELIBERATELY flagged as orphaned, not mapped or parked -- the routing is a
    diagnostic re-run checking for substrate regression, not a build.
    [2026-08-07 governance, ceiling-audit ACTIONABLE finding, user-confirmed]: check_substrate_ceiling_audit.py
    reconfirms this claim genuinely orphaned. Flagged for /failure-autopsy to produce the
    substrate-regression diagnostic prescribed by 866a's own autopsy (re-run 603q fresh,
    commit-verified) -- same routing as sibling Q-021.

    [2026-08-08 governance, CORRECTION, same routing as sibling Q-021]: the 2026-08-07 flag above is
    STALE -- V3-EXQ-866b already ran and PASSed (confirmed failure_autopsy_V3-EXQ-866b_2026-08-07.json),
    resolving the substrate-regression question this claim's own what_would_answer already documents
    above: no substrate regression, the gap is curriculum/harness (z_goal decays to a P2 mean of 0.12
    against the 0.4 C6 floor). Substrate_queue stub scaffolded-curriculum-hazard-rebalance minted this
    cycle. pending_retest_after_substrate set true.

    [2026-08-08 governance, V3-EXQ-866c, confirmed failure_autopsy_V3-EXQ-866c_2026-08-08, supersedes
    V3-EXQ-866a]: the scaffolded-curriculum-hazard-rebalance stub was itself found UNWARRANTED and
    closed same-day -- an empirical probe showed z_goal survives every hazard stage and enters P2 at
    ~0.52; the C6 FAIL above was a decay-only measurement washout artifact (zgoal_norm_mean=0.148),
    not a real curriculum/hazard problem. V3-EXQ-866c CONFIRMS the fix: the contact-gated run_p2 peak
    readout (FULL=0.461) clears the 0.4 C6 floor. G0 (foraging-competence) FAILED again -- third
    occurrence (866/866a/866c) -- non_contributory/precondition_unmet, not evidence against
    INV-034/Q-021, and reconfirms (adds no new content beyond) failure_autopsy_V3-EXQ-866a-G0_2026-08-08's
    diagnosis: category corrected substrate_ceiling -> competence_implementation_gap (the substrate is
    confirmed sound per V3-EXQ-866b; the gap is curriculum/competence-training, a ceiling on the harness
    not on substrate representational capacity). Shares the G0 gate with MECH-457's GOV-FANOUT-1
    portfolio dependency; awaiting V3-EXQ-899. That readiness run has SINCE landed
    (V3-EXQ-899, 2026-08-08T21:48Z, FAIL) and rules out MECH-307 reachability as the fix
    (mech307_perturbs_baseline=False -- the OFF arm also fails G0) without yet identifying the real
    driver; not yet autopsied as of this cycle (deferred to a future /failure-autopsy given cycle scope).
    pending_retest_after_substrate stays true.

    [2026-08-09 governance, V3-EXQ-899, confirmed failure_autopsy_V3-EXQ-899_2026-08-09]: now
    autopsied (by a concurrent session). G0 FAILED a fourth time (866/866a/866c/899),
    reconfirming -- no new content beyond -- the already-diagnosed competence_implementation_gap.
    MECH-307 reachability formally RULED OUT as the G0 fix (single-variable A/B on the exact 866a
    config, d_resource_visit_rate=0.0001). The remaining driver matches the MECH-457
    competence-floor cluster's own "avoidance learned without approach" signature -- that
    cluster's hypothesis-space qid (competence_floor) is CLOSED TO FURTHER FAN-OUT, so a 5th
    confirmatory instance here is cross-referenced, not grown onto it. substrate_queue
    mech457_competence_bootstrap_explorer amended with this run's failure_record. routing
    remains governance-note-only; no new claims.yaml disposition beyond this cross-reference.
- id: INV-035
  title: "A REE state must not be defined purely by sensory appearance; two perceptually identical situations that differ in temporal position, active commitment, goal/antigoal relation, social context, or operative constraints constitute distinct states."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-003
    - ARC-004
    - ARC-007
  subject: state.not_raw_perception
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-004
    - ARC-003
    - ARC-007
  epistemic_category: derivational
  what_would_answer: |
    CONFIRMING: architectural audit finds state identity is never reducible
    to sensory/perceptual embedding alone -- every code path treating two
    states as "the same" for commitment, rollout-caching, or trajectory
    scoring also conditions on temporal position, active commitment,
    goal-relation, or applicable constraints.

    FALSIFYING: a reachable code path found where two states with identical
    sensory encoding but different T/C/G/A are treated as identical (e.g. a
    rollout cache keyed purely on z_world/z_self).

    NON-DEGENERACY PRECONDITION: audit must be run against a live case where
    two perceptually-similar-but-context-different observations actually
    reach a shared code path -- not a synthetic non-co-occurring case.

    Preliminary read (2026-08-07): LatentState carries z_theta + explicit
    timestamp distinct from z_world/z_self; e3_selector.py tracks commitment
    as first-class state (ARC-016 dynamic threshold); goal.py supplies a
    separate slow-attractor z_goal_latent. No dedup-by-raw-embedding path
    found in hippocampal/module.py (first-pass only). TWO GAPS the full
    audit must address, not silently pass over: (1) no literal "antigoal"
    field/term exists anywhere in ree_core -- function currently carried by
    the z_harm* family and E3's reality-constraint scorer F(zeta); audit
    must confirm this substitution is functionally adequate, not assume it.
    (2) no "social context" field/term exists anywhere in ree_core -- this
    axis is currently unaudited/possibly absent from the live substrate.

    (Digested 2026-08-07, session cool-torvalds-a82359, thought-digestion
    wave 12.)
  location: docs/architecture/state.md
  source:
    - evidence/planning/thought_intake_2026-03-24_state_definition_hippocampal_primitives.md
  notes: >
    Grounded in the formal state definition ("situation as navigable from here").
    A state definition that collapses to perceptual embedding silently breaks
    trajectory evaluation, viability mapping, and ethical reasoning. The same
    sensory input at a door produces entirely different viable transitions, costs,
    antigoals, urgency, and commitment structures depending on temporal position
    (T), active constraints (C), and goal/antigoal relations (G, A). Collapsing
    these to appearance means the hippocampal rollout cannot distinguish "leaving
    for work" from "fleeing danger" -- two trajectories with nearly opposite
    valid next-action sets. This invariant is a design guard against the common
    shortcut of treating latent perceptual embeddings as sufficient state
    representations. Registered 2026-03-24.
    Classified emergent 2026-04-17 (Session B of invariant-types governance cycle):
    emergent on ARC-003 (E3 committing), ARC-004 (L-space latent stack), and ARC-007
    (hippocampal paths through residue-field terrain). All three substrates are currently
    'active' so no pending_substrate_reconfirmation is applied.
    CORRECTION (2026-08-07, session cool-torvalds-a82359, thought-digestion
    wave 12): "all three substrates are currently 'active'" is imprecise.
    ARC-003 and ARC-004 have status: active. ARC-007's top-level status is
    provisional (only live_status.reading is active) -- the same is true of
    ARC-018 (an INV-036 dependency, not this claim's). Not a blocker to this
    claim (live_status.reading is the operative field and does read active
    for all three), but a future reader diffing status literally would flag
    a false discrepancy without this note.

- id: INV-036
  title: "A REE state is valid only if it supports transition prediction, valence and antigoal tagging, and uncertainty representation; a representation lacking any of these cannot function as a navigable state."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-003
    - ARC-007
    - ARC-018
  subject: state.functional_requirements
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-035
    - ARC-007
    - ARC-018
    - ARC-003
  epistemic_category: derivational
  what_would_answer: |
    CONFIRMING: every representation participating in hippocampal rollout or
    E3 path evaluation always carries live transition-prediction grounding,
    valence/antigoal tagging, and structured uncertainty -- load-bearing
    (not merely present as unused fields).

    FALSIFYING: a representation found participating in rollout/scoring
    while missing, or with vacuous/unused, one of the three -- e.g. E3
    ranking trajectories without consulting precision.

    Builds on INV-035's audit rather than repeating it -- same live-case
    method, additionally tracing whether R/G/A/U are actually consumed
    downstream, not merely computed and discarded.

    NON-DEGENERACY PRECONDITION: audit must inspect a live case that
    actually reaches E3 scoring or rollout -- not dead or disabled code.

    Preliminary read (2026-08-07): all three floor requirements have
    identifiable live implementations -- transition prediction (e1_deep.py,
    e2_fast.py, e2_world.py, e2_world_uncertainty.py), valence/antigoal (the
    z_harm family), uncertainty (LatentState.precision, confirmed
    non-vacuous -- consumed by variance_commit_threshold()). Fuller audit
    should still confirm consumption, not just presence, at each site.

    (Digested 2026-08-07, session cool-torvalds-a82359, thought-digestion
    wave 12.)
  location: docs/architecture/state.md
  source:
    - evidence/planning/thought_intake_2026-03-24_state_definition_hippocampal_primitives.md
  notes: >
    The three functional requirements are the minimum for a representation to
    participate in hippocampal rollout and E3 path evaluation. (1) Transition
    prediction: without plausible-next-transition grounding (R component), the
    hippocampal system cannot chain the state into a rollout. (2) Valence and
    antigoal tagging (G, A components): without goal-relation and antigoal-relation
    fields, E3 has no directional signal for path selection -- trajectories cannot
    be ranked as better or worse relative to any objective. (3) Uncertainty (U
    component): without structured uncertainty, precision weighting in the
    control plane has nothing to operate on, and epistemic caution cannot
    propagate into trajectory evaluation.
    These three requirements are necessary but not sufficient -- a complete state
    also requires W, Self, T, and C (world, self, temporal position, constraints).
    INV-036 specifies the floor below which a representation is definitely not a
    state; INV-035 specifies that perceptual identity is insufficient for state
    identity. Together they define the outer boundary of valid state abstraction.
    Registered 2026-03-24.
    Classified emergent 2026-04-17 (Session B of invariant-types governance cycle):
    emergent on ARC-003, ARC-007, ARC-018 (all 'active' -- no pending_substrate_reconfirmation).

- id: INV-037
  title: "A content class that is stored and retrievable in the REE system is not thereby active in the navigable state used for trajectory evaluation; active participation requires a preparation substrate (vmPFC-analog) that converts stored content into live state components at evaluation time."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-035
  pending_substrate_reconfirmation: true
  epistemic_category: substrate_conditional
  subject: state.stored_vs_active_distinction
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-035
    - INV-036
    - ARC-035
    - ARC-003
  what_would_answer: >
    INV-037 is an EMERGENT invariant (emergent_from: ARC-035) -- it is architecturally
    UN-STATEABLE, not merely unevidenced, unless ARC-035 (the vmPFC-analog stored->active
    compiler) exists as a real substrate concept. The stored/active distinction this
    invariant asserts IS the distinction ARC-035 defines; there is no independent test of
    the general principle apart from a test of the specific mechanism. INV-037 therefore
    inherits ARC-035's exact current gating: ARC-035 is 'candidate' with
    epistemic_category substrate_conditional -- 3 of its 4 content-class pathways
    (residue/psychopathy, constraint/vmPFC-lesion, safety-memory/PTSD) are flagged
    pending_substrate_reconfirmation via SD-016, whose own evidence (V3-EXQ-418d/418e)
    shows attention-collapse FAILs -- wired but not demonstrably doing content-selective
    retrieval -- and the 4th pathway (social/identity constraints, MECH-132) has no wired
    substrate at all. Do not attempt to answer INV-037 independently of ARC-035's own
    falsifier; a single experiment answers both, at different levels of abstraction
    (INV-037 = the general principle, ARC-035 = the specific REE mechanism).

    THE TEST (cross-referenced from ARC-035, not re-derived): ablate the vmPFC-analog
    preparation pathway and measure whether trajectory selection dissociates from
    stored-content retrievability. See ARC-035's own what_would_answer for the full
    ablation protocol and per-content-class operationalisation; do not duplicate that
    design here.

    CONFIRMING signature (restates the claim's own three numbered experimental
    predictions, already well-formed in the notes above):
    (1) A system with correct residue storage but degraded preparation routing shows
    intact residue retrieval on explicit query while failing to avoid harm-associated
    trajectories in generation -- stored but not active.
    (2) A system with correct constraint encoding but no preparation pathway articulates
    constraints correctly when queried while generating constraint-violating trajectories.
    (3) The failure mode in (1)/(2) is not correctable by adding post-hoc constraint
    checking after generation (see INV-038) -- confirming that activation must happen
    at generation time, not evaluation-after-the-fact.
    Any one of these three, replicated across the corresponding content class, counts as
    confirming evidence for both INV-037 and ARC-035 jointly.

    FALSIFYING: if ablating the vmPFC-analog pathway (per ARC-035's protocol) produces NO
    dissociation between stored-content retrievability and trajectory-generation behavior
    -- i.e. retrieval and generation degrade together, or generation stays correct despite
    ablated preparation -- this falsifies ARC-035 AND INV-037 together. Because INV-037 is
    emergent from ARC-035 specifically, there is no scenario in which ARC-035 is falsified
    but INV-037 survives, or vice versa: falsification of the mechanism is falsification
    of the principle it instantiates.

    Standing block, independent of the ablation result: until SD-016's content-selective
    retrieval FAILs are resolved and MECH-132's social/identity pathway is wired, INV-037
    cannot be answered either way for those specific content classes -- absence of a
    dissociation there is not yet evidence against the invariant, only evidence the
    substrate under test does not yet instantiate the mechanism the invariant depends on.
  location: docs/architecture/vmPFC.md
  source:
    - evidence/literature/targeted_review_state_abstraction_psychiatry/literature_synthesis.md
  notes: >
    The stored/active distinction is the central architectural fact that ARC-035 explains.
    Information can be stored in memory, retrievable on demand, expressible in propositional
    form -- and still not participate in trajectory evaluation because the preparation substrate
    that would make it a live state component is absent or dysfunctional.
    This is not equivalent to INV-035 (state not defined by sensory appearance) or INV-036
    (state functional requirements). Those invariants specify what state must contain and how
    it is bounded. This invariant specifies a procedural requirement: content does not become
    a state component automatically by virtue of being stored -- a dedicated preparation step
    is required.
    Primary evidence: the knowledge/application dissociation. Patient EVR (Eslinger/Damasio
    1985, PMID 4069365) had above-average IQ, intact language, intact declarative memory, and
    could correctly describe appropriate choices in social scenarios -- while continuously
    making catastrophically inappropriate choices. The correct content was stored and
    retrievable; it was not active in the navigable state used for path selection. vmPFC
    ablation had removed the preparation substrate.
    Experimental predictions:
    (1) A system with correct residue storage but degraded preparation routing will show
    intact residue retrieval in explicit queries while failing to avoid harm-associated
    trajectories in generation (stored but not active).
    (2) A system with correct constraint encoding but no preparation pathway will articulate
    constraints correctly when queried while generating constraint-violating trajectories.
    (3) The failure mode is not correctable by adding post-hoc constraint checking --
    see INV-038.
    Registered 2026-03-25.
    Classified emergent 2026-04-17 (Session B of invariant-types governance cycle):
    emergent on ARC-035 (vmPFC-analog stored->active compiler). The stored/active
    distinction is architecturally defined by the existence of ARC-035. ARC-035 is
    currently 'candidate' -- pending_substrate_reconfirmation: true.

- id: INV-038
  title: "A system with correct post-hoc ethical scoring but without an active constraint preparation substrate will produce the EVR pattern: correct verbal moral judgments coexisting with unconstrained trajectory generation; this pattern is not correctable by improving post-hoc scoring accuracy."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-035
  pending_substrate_reconfirmation: true
  subject: ethics.post_hoc_filter_insufficiency
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-001
    - INV-037
    - ARC-035
    - ARC-003
  epistemic_category: substrate_conditional
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: this test is gated behind ARC-035's own
    non-degeneracy requirement, not independently satisfiable. ARC-035
    (vmPFC-analog stored->active preparation -- NOT to be confused with
    SD-035, the unrelated but confirmed-stable amygdala BLA/CeA
    substrate, which shares no what_would_answer relevance here)
    currently has zero experimental evidence (13/13 entries literature;
    exp count = 0 per ARC-035's own what_would_answer) and its nearest
    wired candidate, SD-016, FAILED its own content-selectivity test
    (V3-EXQ-418d/418e: ContextMemory attention collapses to the
    uniform-distribution ceiling -- see SD-016's own what_would_answer,
    also digested this session). The social/identity-constraint content
    class specifically implicated in the EVR grounding (MECH-132,
    bvFTD/EVR-analog) has NO wired substrate at all: status candidate,
    unimplemented. Do not run this test against the current substrate as
    -is -- an ablation of an already-non-functional or unbuilt
    preparation pathway cannot produce an interpretable dissociation.
    ARC-035 must first reach a state where its own non-degeneracy
    precondition is satisfied (SD-016 attention-collapse resolved or
    shown not to affect the tested content class, AND MECH-132 built and
    wired as a live tonic constraint gate) before this test is
    meaningful.

    Independently, the POST-HOC SCORING manipulation also needs its own
    non-degeneracy check: "degraded post-hoc scoring" must be verified
    to actually produce measurably worse scoring accuracy on held-out
    cases (not merely a flag toggle with no behavioral effect) -- per
    the pattern confirmed repeatedly this session (e.g. ARC-071 /
    V3-EXQ-871), a scoring "degradation" that never reaches the code
    path it claims to affect produces a false confirming or falsifying
    result.

    THE TEST (once both substrates are non-degenerate): a 2x2 design --
    preparation substrate {INTACT, ABLATED} x post-hoc scoring accuracy
    {HIGH, DEGRADED}. Preparation intact/scoring degraded should
    reproduce the mirror-EVR pattern (ethical BEHAVIOR with poor
    explicit/verbal moral justification). Preparation ablated/scoring
    intact should reproduce the EVR pattern itself (correct
    verbal/explicit moral scoring coexisting with unconstrained
    trajectory generation -- measure via: unconstrained/norm-violating
    trajectories generated as primary candidates even though the scorer
    correctly flags them; suppression failing preferentially under
    simulated time/resource pressure; ambiguous cases defaulting to the
    unconstrained trajectory).

    CONFIRMING SIGNATURE: the EVR pattern appears ONLY in the
    preparation-ABLATED/scoring-HIGH cell, and does NOT appear in the
    preparation-INTACT/scoring-DEGRADED cell (which should instead show
    the mirror pattern: good behavior, poor articulation). This
    asymmetry is the substantive claim.

    FALSIFYING SIGNATURE (either sub-condition weakens the claim,
    matching the notes field's already-stated criterion): (a) improving
    post-hoc scoring accuracy alone (with preparation still ablated)
    measurably reduces the EVR pattern -- contradicting "not correctable
    by improving post-hoc scoring accuracy"; or (b) the EVR pattern also
    appears with preparation INTACT -- meaning something other than the
    ARC-035 preparation substrate is the load-bearing factor, and the
    emergent_from: [ARC-035] linkage needs re-grounding elsewhere.

    CURRENT STATE (2026-08-07 digestion pass): zero experimental
    evidence exists for INV-038 (absent from claim_evidence.v1.json) and
    no experiment under any id has ever operationalized either arm of
    this design. This invariant is emergent_from ARC-035 (vmPFC,
    confirmed via direct read of the ARC-035 claims.yaml block).
    Testable only after ARC-035 clears its own non-degeneracy
    precondition.
  location: docs/architecture/vmPFC.md
  source:
    - evidence/literature/targeted_review_state_abstraction_psychiatry/literature_synthesis.md
  notes: >
    INV-001 asserts that ethical behavior cannot be compiled into a single explicit ethics
    module. INV-038 is more specific: it asserts that a post-hoc scoring filter -- applied
    to trajectories after they are generated -- cannot produce ethical behavior when the
    generative state lacks active constraints, regardless of how accurate the scoring is.
    The argument: post-hoc filtering operates on trajectories generated by the state. If
    the state lacks active constraints (because the preparation substrate is absent), the
    generated trajectories encode unconstrained proposals. A filter that correctly identifies
    and suppresses these proposals does not change the fact that (a) unconstrained trajectories
    were generated as the primary candidates, (b) suppression under time/resource pressure
    fails first, (c) partial or ambiguous cases default to the unconstrained trajectory,
    (d) the system's behavior under novel constraint configurations is unpredictable because
    the constraint was never in the generative state.
    EVR grounding: the patient's behavior was continuously norm-violating despite intact
    verbal moral reasoning (a post-hoc system). Adding more accurate verbal scoring would
    not have changed his trajectory generation -- it would have changed his ability to
    explain why his choices were wrong, which is not the same thing.
    Experimental falsifiability: a system with vmPFC-equivalent preparation intact but
    degraded post-hoc scoring should show ethical behavior with poor explicit moral reasoning.
    A system with correct post-hoc scoring but degraded preparation should show the EVR
    pattern. If only the first is observed and not the second, INV-038 is weakened.
    This invariant has direct implications for AI alignment: inserting an ethical scoring
    module downstream of trajectory generation does not solve the problem that INV-038
    identifies. The solution requires that normative content be active in the generative
    state, not evaluated after generation.
    Registered 2026-03-25.
    Classified emergent 2026-04-17 (Session B of invariant-types governance cycle):
    emergent on ARC-035 (the substrate whose absence produces the EVR pattern). ARC-035 is
    currently 'candidate' -- pending_substrate_reconfirmation: true.

- id: MECH-120
  title: "The SWS sub-phase of offline mode performs global attractor flattening and residue field denoising (synaptic homeostasis analog), pruning weak noisy traces and restoring signal-to-noise ratio."
  claim_type: mechanism_hypothesis
  subject: sleep.sws_denoising_attractor_flattening
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_691_q055_sleep_consolidation_diversity_persistence_20260620T103320Z_v3
      as_of: 2026-06-20
      verdict: supports/PASS
  implementation_phase: v3
  depends_on:
    - MECH-030
    - MECH-092
    - MECH-094
    - ARC-016
  location: docs/architecture/sleep/offline_phases.md#mech-120
  source:
    - evidence/literature/targeted_review_connectome_mech_030/entries/tononi_cirelli_2006/summary.md
  notes: >
    Biological basis: Synaptic Homeostasis Hypothesis (Tononi & Cirelli 2006). Slow
    wave sleep globally downscales synaptic weights toward a principled baseline,
    reversing unsupervised Hebbian potentiation from waking. Effect: weak/noisy traces
    pruned, strong traces sharpened, SNR restored. SWS is the earliest offline sub-phase.
    REE analog: during the SWS sub-phase of MECH-030, three operations occur:
    (1) Residue field global decay/normalisation -- residue magnitudes scaled toward
    baseline, reducing attractor depth for all trajectory biases (prevents any single
    harm or goal trace from accumulating indefinitely).
    (2) z_delta recalibration -- the deepest latent (z_delta) is pushed toward a
    principled low-information prior, resetting the long-horizon world model before
    NREM replay repopulates it with consolidated content.
    (3) Low-confidence representation pruning -- traces below a salience threshold
    are zeroed, freeing representational capacity for consolidation.
    Critical constraint (Walker 2004): SWS downscaling is non-selective by default.
    Without MECH-094 hypothesis tag, genuine harm signals are indistinguishable from
    noise and will be downscaled along with it. MECH-094 is a HARD PREREQUISITE for
    MECH-120: the tag marks which traces must be protected from SWS normalisation.
    V3 static setpoints that V4 will make dynamic (see v3_v4_transition_boundary.md):
    residue field decay rate (currently static per-step), z_delta recalibration
    magnitude (currently none), SNR pruning threshold (currently absent).
    V3 must measure these setpoints under normal operation to calibrate V4 dynamics.
    Functional outcome (added 2026-04-04, grounded in Hagewoud 2010 + Suppermpool 2024):
    The primary functional output of MECH-120 is behavioral strategy diversity
    preservation, not merely SNR restoration. Without SHY-equivalent downscaling,
    Hebbian winner-take-all dynamics during waking produce monostrategy convergence
    even in benign environments (no harm conditioning, no MECH-124 pathway) -- the
    dominant action trajectory monopolises the synapse budget, and the flexible
    map-based alternative becomes inaccessible. This is the neutral-environment
    Hebbian lock-in variant, distinct from MECH-124 (harm-trace-driven contraction).
    Empirical grounding: Hagewoud et al. 2010 (Sleep) showed that sleep deprivation
    shifts learning from hippocampal spatial strategy to rigid striatal response
    strategy in mice, with reversal learning deficit confirming reduced flexibility.
  evidence_quality_note: >
    V3 scope confirmed (governance 2026-04-09 + 2026-04-09-c): MECH-120 operates
    within the SWS phase of SD-017 (now provisional). SHY wiring implemented 2026-04-08
    (enter_sws_mode calls shy_normalise). Implementation complete; testing blocked by
    missing experimental prerequisites: MECH-030 (sleep consolidation modes, provisional),
    MECH-092 (quiescent replay), MECH-094 (hypothesis tag), ARC-016 (dynamic precision).
    EXQ-245 FAIL x2 (2026-04-05, 2026-04-07): both reclassified to non_contributory --
    proxy substrate without full offline-phase architecture. s1 (SHY-then-SWS beats
    SWS-alone) failure reflects absent substrate, not claim falsification. Literature
    support strong (7 entries). Held at candidate pending experimental design that uses
    SD-017 first-class methods and the full prerequisite chain.
    Suppermpool et al. 2024 (Nature) confirmed single-neuron SHY operation in
    zebrafish with sleep-pressure gating. Prediction for REE: a minimal mind without
    a sleep substrate will show monostrategy convergence under extended training.
    This is the currently observed signature in V3 (no sleep substrate) and is
    predicted by MECH-120 absence, not by any other mechanism.
    Registered 2026-03-23. Reclassified V3 scope 2026-04-09. Functional outcome note added 2026-04-04.
    Implementation note (2026-04-08): SHY wiring completed -- enter_sws_mode() now
    calls E1.shy_normalise(decay) when shy_enabled=True. Config: REEConfig.shy_enabled
    (bool, default False), REEConfig.shy_decay_rate (float, default 0.85). EXQ-245a
    queued for validation.
    V3-EXQ-691 (Q-055, 2026-06-20, PASS/supports -- governance 2026-06-20): supports the
    MECH-120-as-protective-risk-mitigated reading -- SHY-style SWS denoising during
    consolidation did NOT erode committed-action-class diversity (arm-A sleep-ON preserves
    2/3 seeds, non_degenerate=true). NUANCE (governance-flagged): sleep-ON eroded marginally
    MORE than the sleep-OFF control (t2 0.569 vs 0.645), so the Hebbian-erosion risk is
    BOUNDED/non-catastrophic rather than absent -- not a clean protective win over no-sleep.
    exp_conf 0.575 (< 0.62 promotion gate) + implementation_phase v3 -> status candidate
    unchanged (held_pending_v3_substrate).
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: requires committed-action-class diversity to
    be measurable and non-degenerate across sleep-ON vs sleep-OFF arms
    (already established by V3-EXQ-691: 2/3 seeds non-degenerate) -- a run
    where diversity collapses identically in both arms cannot discriminate
    the mechanism.
    CONFIRMING signature: sleep-ON (SHY-style SWS denoising) preserves
    committed-action-class diversity AT LEAST AS WELL AS sleep-OFF across
    extended training, with the erosion-bound reading (V3-EXQ-691: sleep-ON
    eroded marginally MORE than sleep-OFF, t2 0.569 vs 0.645) either closing
    (sleep-ON >= sleep-OFF) or staying within a small, non-catastrophic bound
    -- i.e. SWS denoising is protective-or-neutral, never actively worse
    beyond a small margin. A minimal mind without ANY sleep substrate shows
    monostrategy convergence under extended training (already the observed
    V3 no-sleep signature, per Suppermpool et al. 2024's zebrafish
    single-neuron-SHY prediction).
    FALSIFYING: sleep-ON systematically and substantially erodes diversity
    MORE than sleep-OFF (beyond the small bounded margin already observed),
    i.e. the Hebbian-erosion risk is not bounded but active harm -- would
    falsify the "protective-or-neutral" reading and suggest SHY-style
    downscaling without a working MECH-094 hypothesis-tag protection
    actively damages rather than restores signal-to-noise. Also falsifying:
    monostrategy convergence does NOT emerge in a no-sleep-substrate agent
    under extended training (removes the predicted signature MECH-120's
    absence is supposed to explain).

- id: MECH-121
  title: "The NREM sub-phase of offline mode replays recent episodic content via SWR-equivalent sequences, transferring episodic traces from E3/hippocampal buffer to E1/neocortical schematic representation."
  claim_type: mechanism_hypothesis
  subject: sleep.nrem_swr_replay_consolidation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-121@2026-06-10T16:18:22Z"
      as_of: 2026-06-10
      verdict: hold_pending_v3_substrate/applied
  epistemic_category: substrate_conditional
  implementation_note: >
    2026-06-12 (MECH-423 readiness substrate, R3): the MECH-121 consolidation
    pipeline gained an OPTIONAL module-tagged INTERLEAVED cross-module
    consolidation path -- CrossModuleConsolidator
    (ree_core/sleep/cross_module_consolidation.py), wired into
    SleepLoopManager._run_cycle behind use_cross_module_consolidation (default OFF,
    bit-identical). The legacy offline pass trains e2_harm_s ALONE over region-keyed
    traces with no module identity; the new path runs an interleaved E1<->E2
    gradient schedule, tags each replayed trace by which modules it updates, and
    exposes cross_module_replay_share + interleaved + update-count readouts (the
    blocked schedule is the catastrophic-interference control). MECH-094: the same
    explicit exception the e2_harm_s writeback uses (local per-module optimisers,
    no residue/memory writes). Pure instrumentation; no status/confidence change.
    See docs/architecture/mech_423_superadditivity_readiness_substrate.md +
    ree-v3/CLAUDE.md "MECH-423 readiness substrate".
  governance_2026_06_10: >
    epistemic_category set substrate_conditional (IGW experiment-surfacing
    suppression, not a promote/demote). Although the replay-derived gradient
    substrate is built (sleep_substrate:GAP-4 done), MECH-121's behavioural
    validation (NREM SWR/SWS schema-transfer improving E1's goal-conditioned world
    model) genuinely needs the blocked SD-017 arm: the SD-017 retest cohort
    (sleep_substrate:GAP-2) is upstream-blocked on the unbuilt ARC-065 rule-creator
    substrate, and a monomodal-collapsed agent yields bit-identical sleep-vs-waking
    metrics (nothing diverse to consolidate -- the exact GAP-2 failure mode). The
    claim's own notes also require unbuilt balanced replay scheduling and mark it
    V4 scope. The right response is to wait for the upstream substrate, not to
    surface EXP-0135 as ready /queue-experiment work. status unchanged (candidate);
    claim_type unchanged.
  implementation_phase: v3
  phase_provenance: derived
  phase_derived_from: MECH-165
  depends_on:
    - MECH-030
    - MECH-092
    - MECH-094
    - MECH-122
    - SD-004
  location: docs/architecture/sleep/offline_phases.md#mech-121
  source:
    - evidence/literature/targeted_review_connectome_mech_030/entries/diekelmann_born_2010/summary.md
  notes: >
    Biological basis: Diekelmann & Born 2010. During NREM, hippocampal sharp-wave
    ripples (SWRs) replay recent episodic content in compressed form (~20x compression).
    SWR replay is coordinated with thalamo-cortical spindles (MECH-122) and cortical
    slow oscillations (<1 Hz) in a three-way dialogue: slow-oscillation up-state
    triggers spindle, spindle gates SWR, SWR content travels to neocortex for
    integration into long-term schematic knowledge.
    REE analog: MECH-092 (quiescent E3 heartbeat replay, V3) is the functional
    prerequisite. MECH-121 extends MECH-092 into the full consolidation pipeline:
    E3/HippocampalModule SWR-equivalent replay (already started in MECH-092) is
    coordinated with z_theta->z_delta delivery (spindle analog, MECH-122), and
    E1's LSTM (neocortical analog) integrates the replayed content into its
    long-horizon schematic world model. The consolidation transfers specific
    goal-directed episode traces (approach sequences, harm encounters) from E3's
    episodic buffer to E1's generalised world model.
    Two important properties of this transfer:
    (1) Temporal compression: NREM replay is ~20x faster than real-time. In REE
    terms, E3's SWR replay runs at fast rollout speed without the real-time
    constraint. This means the MECH-121 phase can consolidate many episodes in a
    single offline cycle.
    (2) Semantic abstraction: hippocampus holds episodic traces; neocortex extracts
    schematic regularities. The consolidation is NOT lossless replay -- it is
    abstraction. E1 should integrate the invariant patterns across replayed episodes,
    not store each episode verbatim. This is why E1's goal conditioning (MECH-116)
    improves with sleep: goal-relevant patterns get abstracted into E1's world model.
    V3 prerequisite: MECH-092 quiescent replay validated. Balanced replay scheduling
    (harm AND goal traces, not harm-dominated) must be implemented before MECH-121
    to prevent MECH-124 consolidation-mediated contraction.
    Registered 2026-03-23. V4 scope.
  what_would_answer: |
    SUBSTRATE PRECONDITION (already established by governance 2026-06-10): requires
    (1) the SD-017 retest cohort's upstream ARC-065 rule-creator substrate to be
    built (currently unbuilt -- the sleep_substrate:GAP-2 blocker), and (2) balanced
    replay scheduling across harm AND goal traces (not harm-dominated, to avoid the
    MECH-124 consolidation-mediated-contraction confound) to exist. Without both, a
    monomodal-collapsed agent yields bit-identical sleep-vs-waking metrics (nothing
    diverse to consolidate) and any run self-routes substrate_not_ready, not a
    verdict -- this is why the claim is held pending upstream substrate rather than
    surfaced as ready /queue-experiment work.
    CONFIRMING signature: once built, E1's goal-conditioned world model (MECH-116)
    measurably improves across a sleep cycle relative to a matched waking-only
    control, with the improvement concentrated in goal-relevant ABSTRACTED patterns
    (not verbatim episode replay) and scaling with the volume/diversity of
    NREM-replayed episodic traces (approach sequences + harm encounters, balanced).
    FALSIFYING: E1's world-model quality is statistically indistinguishable between
    sleep-enabled and sleep-disabled conditions once the substrate exists and traces
    are balanced; or E1 shows verbatim episodic memorisation rather than schematic
    abstraction (falsifies the semantic-abstraction reading distinct from lossless
    replay); or the harm/goal balance has no effect on which patterns get
    consolidated (collapses the "balanced scheduling matters" prerequisite into a
    non-factor).

- id: MECH-122
  title: "Thalamo-cortical spindle-equivalent bursts package E3/hippocampal replay content for z_theta delivery AND gate external sensory input, protecting the consolidation episode from interruption."
  claim_type: mechanism_hypothesis
  subject: sleep.spindles_packaging_gating
  polarity: asserts
  status: provisional
  epistemic_category: standard
  pending_retest_after_substrate: true
  live_status:
    reading: provisional
    as_of: 2026-08-03
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-861a_2026-08-03"
      as_of: 2026-08-03
      verdict: weakens/measurement_test_design_defect
  implementation_phase: v3
  phase_provenance: assigned  # 2026-08-10 correction (chip
                              # chip-20260810-phase-consistency-reconciliation): was
                              # derived/phase_derived_from: MECH-121, but that edge ran
                              # backwards -- MECH-122 has been extensively built and
                              # tested (EXQ-246 x2, V3-EXQ-861/861a, landed 2026-08-02
                              # substrate) independent of MECH-121's own still-blocked
                              # pipeline. depends_on: MECH-121 removed; MECH-122's
                              # packaging function operates on E3/hippocampal replay
                              # content broadly available via MECH-092 (already
                              # V3/built, MECH-121's own "functional prerequisite" per
                              # its notes), not MECH-121's full pipeline. The real
                              # direction is MECH-121 -> MECH-122 (kept, see MECH-121).
  depends_on:
    - MECH-030
    - MECH-089
    - SD-006
  location: docs/architecture/sleep/offline_phases.md#mech-122
  source:
    - evidence/literature/targeted_review_connectome_mech_030/entries/diekelmann_born_2010/summary.md
  notes: >
    Biological basis: Sleep spindles (11-15 Hz thalamo-cortical bursts, 0.5-3 sec
    duration) during NREM stage 2. Spindles serve two distinct functions:
    (1) Content packaging: spindles gate hippocampal SWR replay content to neocortex.
    The spindle burst is the transmission window -- during the spindle up-phase,
    hippocampal content is packaged into thalamic relay neurons and forwarded to
    cortex. Without spindles, SWR replay content does not reliably reach neocortex.
    (2) Sensory gating: spindles suppress thalamocortical relay of external sensory
    input during the consolidation window. This makes the offline phase robustly
    offline -- external stimuli cannot interrupt or contaminate the consolidation
    episode.
    REE analog:
    Content packaging: the ThetaBuffer (MECH-089, EXQ-052b PASS) already provides
    theta-rate packaging of E1 output to E3. MECH-122 extends this: during offline
    mode, a spindle-equivalent burst mechanism packages E3/hippocampal SWR replay
    output for z_theta delivery to E1 (the reverse direction of the waking theta
    channel). The theta channel is bidirectional: waking = E1->E3 (goal context);
    sleep = E3->E1 (episodic consolidation).
    Sensory gating: during MECH-122 spindle windows, the sensory input pathway to
    z_self and z_world encoders is gated closed. New environmental observations are
    buffered but not processed. This prevents waking-state observations from
    contaminating the consolidation episode.
    V3 implication: the ThetaBuffer (already implemented) is the V3 scaffolding for
    MECH-122. V3 must ensure the ThetaBuffer can run bidirectionally (E1->E3 waking,
    E3->E1 sleeping). Currently only waking direction implemented.
    Note: spindle density correlates with overnight memory consolidation performance
    in humans (Walker 2004). In REE, spindle density is a proxy for consolidation
    efficiency -- more spindle bursts per offline cycle = more episodic content
    transferred to E1. This provides a potential diagnostic metric in V4.
    Registered 2026-03-23. V4 scope. [STALE as of 2026-08-01 -- see evidence_quality_note:
    sleep (MECH-120/121/122/180 cluster) was pulled into V3 as necessary; implementation_phase: v3
    below is and always was the correct field. This "V4 scope" sentence is registration-era prose
    that was never updated when the scope moved, not the authoritative phase.]
  evidence_quality_note: >
    EXQ-246 PASS x2 (2026-04-05): two V3 proxy spindle coordination runs. Both
    reclassified to inconclusive (governance 2026-04-07 for first run, 2026-04-08
    for second indexed run). PASSes are vacuously true -- WITH_SPINDLE and NO_SPINDLE
    conditions produced identical metrics in all 3 seeds in both runs (harm rates,
    harm discrimination, harm_eval values all identical). Phase 3 spindle proxy
    (ThetaBuffer consolidation_summary written back to ContextMemory) had zero
    measurable effect. V3 proxy too weak to produce signal for this mechanism.
    Promoted to provisional 2026-04-09: pipeline recommendation accepted (conf=0.769,
    zero conflict, 2 exp + 1 lit supports). Provisional pending implementation.

    [2026-08-01 governance, confirmed failure_autopsy_V3-EXQ-861_2026-08-01]: PHASE-LABEL
    CORRECTION, not a scope change -- MECH-122's implementation_phase: v3 field was already
    correct; this claim's own registration-era "V4 scope" / "V4-scope mechanism" / "pending V4
    implementation" prose (notes + the two phrases above, now corrected) was the stale artifact.
    Per feedback_phase_label_follows_dependency: MECH-180 (v3_pending, live V3 governance activity
    this cycle) lists MECH-122 directly in depends_on, and the V3-EXQ-861 autopsy found MECH-122's
    CONTENT-PACKAGING half (ThetaBuffer.consolidation_summary()/set_consolidation_mode(),
    ree_core/latent/theta_buffer.py:146,159) directly implicated in explaining why MECH-180's
    spindle_density DV fails to track novelty while its other two DVs succeed -- MECH-122 is a real,
    live V3 dependency, not a deferred V4 feature. Two DISTINCT substrate_queue entries now exist for
    MECH-122's two halves: MECH122-SENSORY-GATING-OFFLINE-PROTECTION (added same day, session
    igw-loop-mech122-gap, complex (probe-gated) -- not yet established as load-bearing given the
    current strictly-sequential wake/sleep harness design) and MECH122-CONTENT-PACKAGING-SPINDLE-SELECTION
    (added this session, complicated (buildable) -- the ThetaBuffer methods already exist, unwired).
    PROMOTES/DEMOTES NOTHING (status stays provisional).

    [2026-08-02, /implement-substrate, IGW-20260801-197]: MECH122-CONTENT-PACKAGING-SPINDLE-SELECTION
    substrate landed (ree-v3). ThetaBuffer.set_consolidation_mode()/consolidation_summary() are now
    called from run_sws_schema_pass() (ree_core/agent.py) under REEConfig.use_mech122_spindle_content_selection
    (default False, bit-identical off) -- each SWS schema-installation write is blended toward the
    theta-cycle consolidation reference in proportion to how closely it matches it (spindle-analog
    content selection: habitual content homogenised, novel content preserved). This is the
    content-packaging half ONLY -- MECH122-SENSORY-GATING-OFFLINE-PROTECTION (the other half) remains
    complex (probe-gated) and unimplemented; see that substrate_queue entry. Contract probe in
    ree-v3/tests/test_flag_inertness.py pins OFF-inert and ON-live (differentiates ContextMemory
    writes, the specific bar the null V3-EXQ-246 proxy failed). NOT yet validated against MECH-180's
    spindle_density DV -- that needs a fresh run of the V3-EXQ-861 driver family with the new flag ON,
    not queued this session (substrate-build scope only). PROMOTES/DEMOTES NOTHING (status stays
    provisional; implementation_phase: v3 unchanged, already correct).

    [2026-08-03 governance, V3-EXQ-861a, confirmed failure_autopsy_V3-EXQ-861a_2026-08-03, first
    claim-tagged autopsy target for MECH-122, n=1]: validated the landed build against MECH-180's
    spindle_density DV -- FAIL again (0/3 seeds), but new instrumentation shows the mechanism fires
    every cycle (mean_spindle_selection_applied=1.0 throughout, confirming it is not inert) while its
    selection_weight is uniformly tiny and flat (~0.004-0.01), i.e. near-total homogenization
    regardless of environmental novelty. Root cause: ContextMemory's novelty reference
    (ThetaBuffer.consolidation_summary(), a 10-tick recency-weighted average of the SAME short waking
    window supplying the schema-installation prototypes being compared against it) is structurally
    self-similar almost regardless of arm, decoupled from the world_rule_shift/MEL axis this
    experiment family already validates as arm-discriminating. On the two well-formed seeds diversity
    moves in the WRONG direction (monotonically decreasing as MEL increases) -- a clean pattern, not
    scatter. weakens/measurement_test_design_defect: a mechanism-operationalization gap (wrong
    novelty-reference source), not evidence against the biology. Independently converges with
    failure_autopsy_V3-EXQ-436c_2026-08-03's finding (SD-017/ARC-045/MECH-166, different DV --
    slot_cosine_sim differentiation) on the same write-path hypothesis: ContextMemory.write_gate may
    carry the bias-over-content collapse already found/fixed on key_proj's read path
    (SD-016 Part A / EXQ-477) -- untested by either run. Routed /implement-substrate (amend
    MECH122-CONTENT-PACKAGING-SPINDLE-SELECTION): re-source the novelty signal from something
    MEL-correlated (e.g. the E3/E2 prediction-error signal that already drives MEL) instead of the
    self-referential recency buffer. pending_retest_after_substrate=true.

    [/governance 2026-08-16, DEMOTION RECOMMENDATION HELD -- deliberately, not missed]: the automated
    queue recommends demote_to_candidate (conflict_ratio 0.667, exp_conf 0.372, 4 supports / 2
    weakens). HELD at provisional this cycle because the demotion rests entirely on UNADJUDICATED
    evidence. Both of this claim's experimental entries are weakens and both are 861-lineage:
    V3-EXQ-861a (already autopsied above as weakens/measurement_test_design_defect -- an
    operationalization gap, explicitly NOT evidence against the biology) and V3-EXQ-861d
    (v3_exq_861d_..._20260815T005853Z_v3, this cycle's pending_review set, NO confirmed autopsy).
    861d additionally carries the DEAD z_goal writer defect -- z_goal_stream.writer_defect true,
    writer_calls 0 across 38,346 ticks, so REEAgent.update_z_goal was never called and every z_goal
    consumer silently no-opped for the whole run -- and it self-routes mel_control_degenerate having
    FAILED its own C2_consumer_not_env_causes_variation control. A demotion driven by two runs, one
    already diagnosed as a test-design defect and the other of doubtful validity on grounds visible
    from its own manifest, would be applying the conflict ratio to evidence the autopsy has not yet
    accepted. Route: V3-EXQ-861c/861d cluster to /failure-autopsy (shared C1b spindle-density
    criterion, shared writer defect); re-run this demotion look next cycle once adjudicated.
    PROMOTES NOTHING; status stays provisional.

    [2026-08-16 governance apply | V3-EXQ-861d -> MECH-122 | failure_autopsy_861c-861d-mech180-cluster_2026-08-16] DISPOSITION: evidence_direction non_contributory. OVERRIDE manifest-recommended weakens -> non_contributory; claim not fairly tested (gate dead/saturated on 2/3 seeds, distinct write_gate defect open); status unchanged -> provisional

    V3-EXQ-861d (confirmed failure_autopsy_861c-861d-mech180-cluster_2026-08-16) -> non_contributory for BOTH claims; MECH-122 status UNCHANGED at provisional. The manifest's self-routed `weakens` on MECH-122 is OVERRIDDEN. The landed follow-up fix (ree-v3 6aa97ca) is confirmed working at its own layer -- mean_spindle_selection_weight now spans ~100x (0.009-0.956 vs 861a's flat ~0.004-0.01), tracks measured MEL, and collapses to ~0.004 on the MEL-OFF control, resolving the 861a flat-weight defect. But the clamp(mel/ref-1,0,1) novelty gate was DEAD on seed 42 and SATURATED on seed 123, so C1b required 2/3 seeds while the mechanism could not vary on 2/3. Seed 123 additionally had 4-6 cycles of insufficient touched slots and a degenerate DV (one arm exactly 0). On the single graded seed (456) the DV moved INVERSELY to selection weight -- flagged as an anomaly to test, not a result. The distinct V3-EXQ-436c ContextMemory.write_gate bias-over-content defect remains open downstream and is the leading candidate explanation.
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: this claim bundles two distinct mechanisms and
    each needs its own gate.
    Content-packaging half: the landed substrate
    (MECH122-CONTENT-PACKAGING-SPINDLE-SELECTION, REEConfig.use_mech122_spindle_content_selection)
    is confirmed non-inert (mean_spindle_selection_applied=1.0 in V3-EXQ-861a) but
    its current novelty reference (ThetaBuffer.consolidation_summary(), a
    self-referential recency average of the same waking window supplying the
    content being compared against it) is a diagnosed measurement/test-design
    defect, not a real manipulation -- selection_weight stays uniformly tiny
    (~0.004-0.01) regardless of arm. A run against this UN-corrected novelty
    reference is not informative and should self-route
    substrate_not_ready/measurement_defect, not count as a verdict on the
    mechanism -- this is exactly how the two EXQ-246 vacuous PASSes and the
    V3-EXQ-861a FAIL were already read. The precondition for an informative run
    is the routed-but-not-yet-landed fix: re-source the novelty signal from
    something MEL-correlated (e.g. the E3/E2 prediction-error signal that
    already drives MEL) instead of the self-referential buffer, with
    selection_weight showing measurable variance across MEL levels as a
    manipulation check.
    Sensory-gating half: MECH122-SENSORY-GATING-OFFLINE-PROTECTION is unbuilt
    (complex (probe-gated)) and not established as load-bearing under the
    current strictly-sequential wake/sleep harness design. No run can test this
    half until it is built; any claimed test of "gating" before then is not
    evidence either way.
    CONFIRMING signature: (a) Content-packaging -- once the novelty reference is
    re-sourced to a MEL-correlated signal, MECH-180's spindle_density DV tracks
    novelty in the predicted direction (WITH_SPINDLE vs NO_SPINDLE arms
    diverge, ContextMemory writes are robustly differentiated rather than the
    null V3-EXQ-246 outcome) across a majority of seeds. (b) Sensory-gating --
    once built, external sensory input is measurably suppressed/buffered during
    spindle windows, and a consolidation episode protected by gating transfers
    content better than one interrupted by waking input.
    FALSIFYING: (a) Content-packaging -- even after re-sourcing the novelty
    signal to a MEL-correlated source, spindle_density/selection_weight still
    fails to track novelty, or continues moving in the wrong direction relative
    to MEL (as already seen on the two well-formed V3-EXQ-861a seeds) -- this
    would upgrade the finding from "measurement defect" to genuine evidence
    against the content-packaging mechanism. (b) Sensory-gating -- once built,
    interrupted vs gating-protected consolidation episodes are statistically
    indistinguishable, or gating fails to measurably suppress external input at
    all. CROSS-CUTTING CAVEAT (already flagged as an open, converging
    hypothesis by failure_autopsy_V3-EXQ-436c_2026-08-03 on a different DV):
    if ContextMemory.write_gate is found to carry the same bias-over-content
    collapse already fixed on key_proj's read path (SD-016 Part A / EXQ-477),
    that is a shared substrate bug, not evidence on MECH-122 either way, and
    should be ruled out before attributing any future FAIL to this mechanism.

- id: MECH-123
  title: "The REM sub-phase of offline mode recalibrates precision priors and tests world model hypotheses via unconstrained generative simulation, with commit gate fully suppressed."
  claim_type: mechanism_hypothesis
  subject: sleep.rem_precision_recalibration
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: '2026-08-06'
    needs_review: false
  epistemic_category: substrate_conditional
  v3_pending: true
  implementation_phase: v3
  phase_provenance: derived
  phase_derived_from: MECH-204
  depends_on:
    - MECH-030
    - MECH-092
    - MECH-094
    - ARC-016
    - MECH-093
  location: docs/architecture/sleep/offline_phases.md#mech-123
  source:
    - evidence/literature/targeted_review_connectome_mech_030/entries/hobson_friston_2012/summary.md
  notes: >
    Biological basis: Hobson & Friston 2012. REM = unconstrained hyperparameter
    exploration under aminergic suppression. The predictive coding framing: during
    REM, precision parameters (priors) are recalibrated without being anchored to
    current sensory evidence. Hallucinatory content = the generative model running
    unconstrained, testing hypotheses against internally-generated data. Aminergic
    suppression (NE, 5-HT near zero) = commit gate fully collapsed. Cholinergic
    dominance = high internal noise, enabling exploration of prior space.
    REE analog: the REM sub-phase is the precision recalibration phase.
    (1) z_beta recalibration: z_beta (NE analog, MECH-059) is driven toward a
    principled low-arousal prior. The precision weights that z_beta modulates
    (E3 prediction error gains) are reset toward a non-biased baseline before
    waking resumes.
    (2) z_delta hyperparameter reset: the deepest latent (z_delta, encoding
    long-horizon priors) is recalibrated via E1 running without sensory anchoring.
    E1's LSTM explores its prior distribution unconstrained by current observation.
    (3) Commit gate fully suppressed: ARC-016 running_variance threshold is
    effectively infinite during REM -- no commitment can occur. All E3 activity
    is hypothesis-tagged (MECH-094) and does not accumulate in the residue field.
    (4) World model hypothesis testing: E1 generates unconstrained world-model
    trajectories (the REE equivalent of dream content), testing whether the
    current world model produces coherent self-consistent trajectories. Incoherent
    predictions reveal model limitations to be addressed on waking.
    V3 static setpoints to be measured (for V4 dynamic calibration):
    commitment_threshold baseline (0.40 -- what does normal waking operation look
    like?), z_beta EMA alpha (0.3 -- what is the natural arousal time constant?),
    E1 prediction_horizon (20 -- what horizon does E1 naturally explore in REM?).
    Critical constraint: MECH-094 hypothesis tag is essential. REM-generated
    trajectories must be tagged as hypothesis, not real-action. If the tag fails
    during REM, hallucinatory content accumulates in the residue field as if it
    were real experience -- the psychosis mechanism (MECH-094 notes).
    Registered 2026-03-23. V4 scope.
  what_would_answer: |
    SUBSTRATE PRECONDITION: requires the full offline-phase architecture
    (MECH-030 modes, MECH-092 replay, MECH-094 hypothesis tag, ARC-016
    dynamic precision, MECH-093) to be live AND the REM sub-phase's
    commit-gate suppression (ARC-016 running_variance threshold effectively
    infinite) to be independently confirmed non-degenerate (commitment
    genuinely does not fire during REM, not merely rare) -- without this the
    mechanism cannot be distinguished from ordinary waking operation.
    Registered V4 scope per this claim's own notes; not V3-tractable as
    stated (epistemic_category/v3_pending set explicitly 2026-08-06 to match
    that prose, mirroring sibling MECH-121).
    CONFIRMING signature: during REM, (a) commit-gate suppression is
    measurable (zero commitments recorded across the REM window, not merely
    reduced); (b) E1 generates world-model trajectories that are coherently
    self-consistent for a genuinely-adequate world model and INCOHERENT in
    proportion to real model limitations (REM "dream content" tracks actual
    model quality, not noise); (c) precision parameters (z_beta, z_delta)
    measurably recalibrate toward a low-arousal / low-bias baseline across
    the REM window, distinguishable from a matched non-REM control.
    FALSIFYING: commit-gate suppression is incomplete or absent during REM
    (commitments still fire); REM-generated trajectory coherence is
    uncorrelated with independently-measured world-model quality (the
    hypothesis-testing framing is not doing real work); or precision
    parameters show no measurable REM-specific recalibration relative to a
    matched non-REM control (the mechanism collapses to ordinary waking
    operation under a different label). CRITICAL FAILURE MODE (already named
    in this claim's own notes): if the MECH-094 hypothesis tag fails during
    REM, hallucinatory content accumulates in the residue field as real
    experience -- this is the psychosis mechanism, and a run showing that
    failure mode is evidence AGAINST the tag's reliability, not against
    MECH-123 itself.

- id: MECH-124
  title: "When harm-trace salience dominates offline replay content, consolidation selectively amplifies harm predictions, progressively contracting option space -- a distinct failure mode from Q-021 behavioral flatness."
  claim_type: mechanism_hypothesis
  subject: sleep.consolidation_mediated_option_contraction
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_298_mech124_zgoal_salience_discriminative_1775759716_v3
      as_of: 2026-04-09
      verdict: supports/PASS
  implementation_phase: v3
  phase_provenance: derived
  phase_derived_from: ARC-036
  depends_on:
    - MECH-030
    - MECH-120
    - MECH-121
    - MECH-092
    - MECH-094
    - Q-021
  location: docs/architecture/sleep/offline_phases.md#mech-124
  source:
    - evidence/literature/targeted_review_connectome_mech_030/entries/walker_stickgold_2004/summary.md
  notes: >
    Biological basis: Walker & Stickgold 2004. Sleep consolidation is not neutral --
    it amplifies whatever dominates replay content. Fear-conditioned traces are
    selectively strengthened during REM. In PTSD, REM intrusions re-consolidate
    the fear trace repeatedly: nightmare replay -> re-consolidation -> stronger fear
    -> more REM intrusion. Extinction fails because consolidation of the fear trace
    outpaces extinction of it. The consolidation loop is self-amplifying.
    REE failure mode (distinct from Q-021):
    Q-021: behavioral flatness from pure-avoidance TRAINING (no approach signal from
    the start -- gradient minimum is quiescence). MECH-124: behavioral flatness
    from consolidation DYNAMICS despite approach signals being present in training.
    Mechanism: (1) Harm salience > goal salience in residue field -> SWR replay
    (MECH-121) is dominated by harm traces. (2) Consolidation selectively strengthens
    dominant replay content -> harm predictions in E1 and E3 amplified. (3) SWS
    denoising (MECH-120) cannot distinguish goal traces from noise if they are
    low-salience -> goal representations downscaled. (4) Weakened goal drive (z_goal
    decay not counteracted by consolidation) -> approach behavior declines. (5) Fewer
    positive experiences -> even less goal representation -> self-amplifying loop.
    The loop is broken only by: balanced replay scheduling (MECH-121 must include
    goal-directed traces proportionally), MECH-094 tag protecting harm traces from
    over-amplification, and the go mechanism (ARC-030, MECH-112/116/117) providing
    sufficient approach signal to counteract harm dominance.
    Clinical analog: PTSD (trauma loop), treatment-resistant depression (learned
    helplessness + sleep-mediated entrenchment), avolition in schizophrenia where
    consolidation entrenchment amplifies negative predictions.
    Distinct from Q-021 but related: Q-021 is the training-time failure (static);
    MECH-124 is the consolidation-time failure (dynamic, requires sleep architecture).
    An agent with sufficient approach drives (INV-034) can still fall into MECH-124
    if its consolidation system is imbalanced. Prevention requires balanced replay
    content scheduling as an explicit architectural constraint on MECH-121.
    V3 implication: even before V4 sleep implementation, V3 must ensure that the
    residue field and goal representation (z_goal) are balanced in salience. If
    z_goal salience is systematically lower than harm salience, a V4 consolidation
    system will amplify this imbalance. EXQ-074b and EXQ-076 provide early evidence
    on whether z_goal can achieve competitive salience with harm traces.
    Registered 2026-03-23. V4 scope.
  evidence_quality_note: |
    EXQ-224 DIAGNOSTIC (2026-04-04): z_goal salience vs harm salience probe. BASELINE
    condition (z_goal_enabled=True, drive_weight=2.0), 3 seeds x 300 episodes.
    Mean final ratio (z_goal_norm / harm_salience) = 0.312 (goal ~31% of harm salience,
    harm dominates ~3:1). FLAG_2_ratio_declining=True (mean ratio slope=-0.0034; ratio
    declining over training in 2/3 seeds). risk_detected=True. V4 risk condition confirmed:
    in the current V3 substrate, harm salience systematically exceeds goal salience; if
    offline consolidation is harm-dominated in V4, the MECH-124 loop predicts progressive
    option contraction. Diagnostic only -- does not test the consolidation mechanism itself
    (no sleep in V3).
    EXQ-298 PASS (2026-04-09, governance 2026-04-09-c): z_goal salience discriminative pair
    with drive_weight=2.0 vs ablation. PRIMARY mean ratio=0.700, ABLATION mean ratio=0.288,
    delta=0.412 (3/3 seeds, all criteria). z_goal IS competitive with harm salience under
    current V3 defaults -- V4 consolidation risk indicator green. Promoted to provisional
    (governance 2026-04-09-c): 5 supports, 1 opposing, conf=0.774. V4 consolidation
    experiments still required to test the full option-contraction loop.
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: this claim cannot be tested in V3. It requires an
    actual offline consolidation/replay substrate (MECH-121 SWR replay, MECH-120 SWS
    denoising) running multiple sleep cycles over training -- V3 has no sleep
    architecture. EXQ-224 and EXQ-298 (both 2026-04, V3-era) are PRECURSOR diagnostics
    only: they establish whether the salience-balance PRECONDITION for the loop holds
    (goal vs harm salience ratio), not whether the consolidation loop itself amplifies
    harm and contracts option space. A run that only re-measures z_goal/harm salience
    ratios in V3 (no consolidation cycles) should self-route substrate_not_ready for
    THIS claim, not count as a verdict -- that ground was already covered by EXQ-224/298.
    A genuine test additionally requires ITERATION: a single consolidation pass cannot
    distinguish a self-amplifying loop from a one-off adjustment -- the design must run
    multiple offline consolidation cycles and track the trajectory of option-space /
    approach-behavior metrics across cycles, not just a pre/post snapshot.
    CONFIRMING signature: across successive consolidation cycles in which replay
    content is harm-dominant (e.g. the ~3:1 harm:goal salience ratio measured in the
    EXQ-224 baseline), option-space metrics (action diversity, z_goal representation
    strength, frequency of approach behavior) PROGRESSIVELY CONTRACT cycle-over-cycle,
    even though approach signals were present during the original training (ruling out
    a pure Q-021 static-avoidance explanation). The effect should be attenuated or
    absent when replay content is balanced per MECH-121 (goal-directed traces
    included proportionally) or when MECH-094 tag-based protection shields harm traces
    from over-amplification -- i.e. the loop is mechanism-specific to salience
    imbalance in replay, not an inevitable property of consolidation itself. An agent
    with strong approach drives at training time (INV-034) that STILL contracts under
    imbalanced consolidation would be a strong confirming case, since it isolates the
    consolidation dynamic from training-time drive strength.
    FALSIFYING signature: harm-dominant replay content does NOT produce progressive,
    cycle-over-cycle contraction -- option-space metrics stabilize, plateau, or do not
    decline further than a single adjustment -- OR the apparent contraction is fully
    explained by training-time avoidance dynamics (Q-021) and disappears/does not
    change when consolidation cycling is held constant while only training exposure is
    varied. Also falsifying: an agent with imbalanced (harm-dominant) consolidation
    that does NOT show declining approach behavior despite otherwise-sufficient
    approach drive, which would indicate the proposed self-amplifying loop is not load-
    bearing even when its stated precondition (salience imbalance) is present.
    Distinguish explicitly from Q-021 in the writeup: Q-021 is training-time and static
    (no approach signal from the start); MECH-124 is consolidation-time and dynamic
    (approach signal present at training, contraction emerges from repeated offline
    replay/consolidation). A run that cannot separate these two should not be scored
    against MECH-124 either way.

- id: MECH-125
  title: "E3 trajectory selection implements multi-constraint viability evaluation rather than scalar reward maximisation: a trajectory is selected when no major evaluation system produces a veto-level error signal across goal space, harm space, identity space, and resource space."
  claim_type: mechanism_hypothesis
  subject: coherence.multiconstraint_viability
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  depends_on:
    - MECH-112
    - MECH-113
    - ARC-018
    - ARC-003
    - SD-005
  location: docs/architecture/e3.md#4a-selection-criterion-multi-constraint-viability-not-reward-maximisation
  source:
    - docs/thoughts/2026-03-24_COHERENCE_MULTICONSTRAINT_HIPPOCAMPAL_NAVIGATION_CONVERGENCE.md
  notes: >
    The selection rule is: lowest-veto + acceptable goal progress, not maximum reward.
    A trajectory is coherent when it does not produce a veto-level error signal in any
    major evaluation system simultaneously. This is distinct from reward maximisation:
    a trajectory can have high immediate reward while failing harm, identity, or resource
    constraints, producing internal contradiction.
    Biological correlate: the commit gate (basal ganglia) opens when ACC conflict, insula
    cost, amygdala threat, goal-distance (MECH-112), and identity checks all fall below
    veto threshold simultaneously. No single system "chooses" -- commitment emerges from
    absence of sustained veto.
    Distinct from MECH-113 (which measures internal latent coherence via D_eff, a
    single-space metric). MECH-125 is the cross-space selection criterion: the multi-
    constraint viability check across goal, harm, identity, and resource spaces that
    determines whether E3 commits to a trajectory at all.
    The five terms in E3's selection functional J(tau) map to constraint spaces:
    C(tau) = cross-horizon coherence (identity + prediction consistency),
    R(tau) = residue/ethical curvature (harm + moral cost),
    F(tau) = feasibility (resource + physical constraint),
    I(tau) = epistemic value (exploration benefit, negative constraint),
    P(tau) = phase-lock (temporal coherence).
    The ACC + insula complex is the likely biological substrate for the coherence error
    signal (conflict monitoring + interoceptive cost). Commitment emerges when these
    signals collectively fall below threshold -- not from a single coherence module.
    Convergent neuroscience: Behrens et al. (2018) Neuron "What is a cognitive map?"
    establishes hippocampal-entorhinal system as a general relational geometry engine;
    Whittington et al. (2020) Cell Tolman-Eichenbaum Machine unifies spatial and
    relational cognition. REE arrived at the same architecture independently from the
    design side. See MECH-112 (goal latent) and MECH-113 (D_eff self-coherence) for
    the specific sub-mechanisms this claim coordinates.
    Registered 2026-03-24. V3 scope.

- id: MECH-126
  title: "Specific modes of REE state abstraction failure -- overmerge, oversplit, temporal context loss, valence mis-tagging, uncertainty collapse, and narrow representational capacity -- produce behavioral signatures that are structurally analogous to empirically described psychiatric conditions, sharing identifiable circuit-level mechanisms."
  claim_type: mechanism_hypothesis
  subject: state_abstraction.failure_modes_psychiatric_analogs
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-035
    - INV-036
    - ARC-007
    - ARC-018
  location: docs/architecture/state.md
  source:
    - evidence/planning/thought_intake_2026-03-24_state_definition_hippocampal_primitives.md
    - evidence/literature/targeted_review_state_abstraction_psychiatry/literature_synthesis.md
  notes: >
    Six failure modes identified and grounded in the literature (2026-03-24 pull):
    (1) Overmerge -> schizophrenia/thought disorder: strongly supported. DG pattern separation
    failure + CA3 pattern completion excess produces exactly the predicted behavioral signature
    -- contextually inappropriate responses, intrusion of irrelevant context, source monitoring
    failure. Evidence: PMID 35853896 (shallow cognitive map hypothesis), PMID 39567329 (Trends
    Cog Sci 2025 attractor instability), PMID 20810471 (Tamminga hippocampal formation SZ),
    PMID 17020747 (fronto-hippocampal temporal context monitoring).
    (2) Valence spreading / attractor basins -> anxiety/PTSD: strongly supported. Hippocampal
    pattern completion extends threat-tagged zone (GAD). Safety attractor cannot be entered
    from extinction context in PTSD (deep basin). Evidence: PMID 27794690 (Kaczkurkin PTSD
    fear generalization fMRI), PMID 31206738 (Lis 2020 generalization review), PMC 7554263
    (contextual reinstatement MVPA PTSD), PMC 10728304 (vmPFC-hippocampus-amygdala circuit
    review Frontiers 2023), Lissek et al. Neurosci Biobehav Rev 2021 meta-analysis.
    (3) Temporal/contextual component loss -> ADHD: well supported. PFC-hippocampal-cerebellar
    circuits maintain T (temporal position) and C (constraints); dysfunction degrades both,
    producing appearance-driven impulsive action. Evidence: PMC 11325328 (White/Dalley 2024
    temporal processing ADHD), PMC 2894421 (Arnsten 2009 PFC in ADHD), PMID 19621976 (Arnsten
    2009 CNS Drugs), PMID 15071717 and 12424557 (Barkley temporal processing), PMC 3329889
    (multi-pathway ADHD model).
    (4) Uncertainty collapse -> mania: theoretically well-grounded, direct empirical evidence
    developing. Precision dysregulation in active inference (excessive prior precision = U->0)
    produces overcommitted, over-confident, reduced-exploration action. Evidence: PMID 32860285
    (Smith/Badcock/Friston 2021 predictive coding clinical), PMID 39828236 (Qela 2025
    transdiagnostic PC review), PMC 10196365 (computational belief updating in psychotic-like
    experiences), PMID 29400358 (Huys 2018 computational parameters anhedonia/mood disorders).
    (5) Narrow representational capacity / negative G tagging -> depression: multiply supported.
    Reduced state space capacity (synapse loss paper PMID 39569353) is sufficient to produce
    depression-like behavior without parameter manipulation. Negative learning-rate asymmetry
    (PMC 5828520 anhedonia review) and inflated path cost estimation (effort learning) are
    additional mechanisms. Learned helplessness encodes approach as structurally unavailable
    (PMID 27337390, 26555633).
    (6) Oversplit -> ASD generalization failure: supported behaviorally, mechanistic account
    developing. Increased hippocampal pattern separation produces orthogonal representations
    for structurally similar contexts. Evidence: PMC 7907419 (Pattern Unifies Autism framework),
    PMC 4573235 (de Marchena 2015 generalization weakness ASD), PMID 27119213 (stimulus
    overselectivity ASD/DS), DOI 10.1177/1088357620943504 (Kelly/Reed overselectivity ASD).
    Cross-track convergence: hippocampus is central across all six tracks but in different
    directions on the pattern separation/completion axis. This axis (DG-CA3 balance) emerges
    as the primary organizing parameter: too much completion -> overmerge (SZ) + attractor
    lock (PTSD); too much separation -> oversplit (ASD). ADHD and depression involve
    orthogonal dimensions (temporal binding, representational capacity/valence).
    The claim is structural analogy, not identity: REE state components (W, Self, T, G, A, C,
    U, R) are not identical to empirical constructs, and the mapping does not resolve causal
    direction. Strongest causal evidence: PMID 39569353 (synapse loss sufficient for depression-
    like behavior in RL model).
    (7) Residue non-propagation -> psychopathy: strongly supported. Three-node failure cascade:
    amygdala generation failure (PMID 15997022 Birbaumer 2005 fMRI; PMID 11704074 Kiehl 2001;
    PMID 18281412 Marsh 2008 CU-trait children); encoding failure under goal pursuit (DOI
    10.1177/0956797610396227 Baskin-Sommers 2011); vmPFC/OFC update failure (PMID 16866595
    Budhani 2006 reversal; PMID 26359751 Gregory 2015 Lancet Psychiatry fMRI; PMID 18458210
    Finger 2008; PMID 15134853 Blair 2004 OFC). Glenn/Raine 2011 (PMID 21919563) articulates
    generation vs. propagation as two separable failure nodes. RL mechanism: wide learning
    window (Ly 2021, PMID 38773992).
    (8) Social/identity constraint omission -> frontal damage / bvFTD: strongly supported.
    Knowledge/application dissociation: Saver/Damasio 1991 (PMID 1791934); EVR Eslinger/Damasio
    1985 (PMID 4069365). Early-onset variant: Anderson 1999 (PMID 10526345). Constraint removal:
    Koenigs 2007 (PMID 17377536 Nature). Progressive erosion: bvFTD Rascovsky 2011 (PMID
    21810890); Rahman 1999 IGT FTD (PMID 10430832). Affective ToM substrate: Shamay-Tsoory
    2003 (PMID 12729486); Moll/Grafman 2005 NatRevNeurosci (PMID 16276356).
    Three organizing axes span all nine failure modes: (1) hippocampal pattern sep/completion
    balance; (2) PFC-hippocampal temporal/constraint binding; (3) amygdala-vmPFC valence
    generation-propagation cascade. vmPFC is the critical integration node across axes 2 and 3.
    Registered 2026-03-24. Literature pull completed 2026-03-24.

- id: MECH-127
  title: "Counterfactual other-cost-aversion activates cooperative behavior as a motivational surrogate when the direct task-reward pathway is degraded."
  claim_type: mechanism_hypothesis
  subject: social.counterfactual_empathic_activation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-031
    - MECH-032
    - INV-005
    - INV-028
    - INV-034
  location: docs/architecture/social.md#mech-127
  source:
    - docs/thoughts/2026-03-24_empathy_multiagent_ethics.md
    - evidence/planning/thought_intake_2026-03-24_empathy_multiagent_ethics.md
  notes: >
    When an agent's direct task-motivation pathway is degraded (deficit state), modeling the
    anticipated cost to another agent -- other-cost-aversion -- can substitute as an activation
    mechanism, producing cooperative task-engagement even in the other agent's absence. The
    activation is counterfactual: it fires on a model of what would happen to the other agent
    if they took on the task, not on their actual current state. The resulting distress-at-their-
    struggle provides motivational energy that the depleted direct pathway cannot supply. Behavioral
    output (task engagement / cooperation) is identical to the direct-pathway output but arrives
    via a different activation route.
    Key distinction from MECH-031: MECH-031 describes empathy modulating behavior via
    OTHER_SELFLIKE tagging and control-plane coupling -- modulation of an existing pathway.
    MECH-127 describes empathy replacing a degraded pathway -- bypass, not modulation.
    Implication for MECH-032 (high-recall OTHER_SELFLIKE bias): counterfactual projection
    requires OTHER_SELFLIKE tagging to function pre-encounter, supporting the bias rationale.
    Motivating observation: first-person report, 2026-03-24. Registered 2026-03-24.
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: MECH-127 is design-only as of this writing -- it has
    no built substrate and no experiment has ever attempted to test it (no EXQ/run IDs
    exist against this claim). Per mirror_modelling_other_self_v5_plan.md (MIRROR-7),
    it is gated on ALL of: pre-encounter OTHER_SELFLIKE tagging (MIRROR-1), a mirror
    that runs COUNTERFACTUALLY rather than only on observed other-agent state (MIRROR-2),
    the MECH-163 hippocampal multi-step planner (needed to simulate another agent's
    future state, not just its present one), and the INV-064 self-model maturational
    gate (self_model_v4:SELF-7) establishing self-stability precedes social depth. A run
    lacking a verified counterfactual (absent-other) mirror channel, or lacking an
    independently-measurable "direct task-motivation pathway degraded" state, tests
    nothing and must self-route substrate_not_ready rather than counting as a verdict --
    scoring a system that cannot represent an absent other's hypothetical cost is not a
    test of bypass-via-counterfactual-projection, it can only be a test of MECH-031-style
    modulation by mistake.
    CONFIRMING signature (bypass, not modulation): with the direct task-motivation
    pathway independently confirmed degraded, cooperative task-engagement rises above
    the degraded-pathway-alone baseline specifically when the counterfactual
    OTHER_SELFLIKE/mirror channel is active, INCLUDING when the other agent is absent
    (the claim is explicit that activation is counterfactual -- "on a model of what
    would happen to the other agent if they took on the task, not on their actual
    current state"). This must dissociate from MECH-031 (modulation of an intact
    pathway): ablating the counterfactual/mirror channel should collapse the rescue
    effect, while further degrading the already-degraded direct pathway should NOT
    further reduce engagement, since by hypothesis engagement is now being driven
    through the alternate (counterfactual empathic) route rather than the direct one.
    FALSIFYING signature: cooperative/task engagement under pathway degradation is
    statistically indistinguishable with vs without the counterfactual channel (no
    rescue); OR the rescue effect requires the other agent to be PRESENT/observable
    rather than merely modeled (would falsify the counterfactual/absence claim and
    collapse MECH-127 into ordinary MECH-031 modulation); OR ablating OTHER_SELFLIKE
    tagging / the counterfactual mirror has no effect on the rescue (would falsify the
    proposed mechanism even if some rescue effect is observed via an unidentified
    route). Any of these outcomes should be reported as a genuine test result, not
    reframed as substrate_not_ready, PROVIDED the non-degeneracy precondition above was
    actually met.
    Evidence on file: none experimental. This is a first-person-report-motivated
    mechanism hypothesis (thought_intake_2026-03-24_empathy_multiagent_ethics.md),
    currently un-adjudicated by any run.

- id: ARC-034
  title: "Ethics testing must span nth-order multiagent trajectory distributions; local pairwise probes are insufficient to characterize emergent ethical properties."
  claim_type: architectural_commitment
  subject: ethics.test_scope_nth_order
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v5
  governance_2026_07_01: >
    Added implementation_phase v5 + v3_pending (phase-tag hygiene, PROMOTES NOTHING).
    ARC-034 is a V5-era multiagent-ethics claim -- its assertion (ethics testing must span
    nth-order multiagent trajectory distributions) requires the multi-agent ecology substrate,
    and it depends_on MECH-129 (implementation_phase v5, social tier, V5 roadmap wave) and
    MECH-127 (counterfactual empathic activation, social). Previously untagged, which made a
    reverse-dependency trace mis-flag it as a "V3-era claim depending on a V5-held claim"
    (2026-07-01 PM /governance). Now routes with the V5 held cohort (siblings ARC-096/097/098).
  v3_pending: true
  epistemic_category: substrate_conditional
  depends_on:
    - INV-001
    - INV-015
    - INV-028
    - MECH-127
    - MECH-129
  location: docs/invariants.md#arc-034
  source:
    - docs/thoughts/2026-03-24_empathy_multiagent_ethics.md
    - evidence/planning/thought_intake_2026-03-24_empathy_multiagent_ethics.md
  notes: >
    A REE system can be locally ethical at every pairwise (n=1) interaction and still produce
    an ethically problematic emergent collective state at n=k. Conversely, locally depleted
    agents can produce more ethical emergent behavior at n=k than n=1 predicts (as in the
    MECH-127 motivating case: direct-pathway analysis predicts reduced helping; nth-order
    analysis predicts increased helping via counterfactual empathic activation).
    Ethics tests must therefore include: (1) counterfactual probes -- what activates when the
    direct pathway is blocked? (2) multi-agent stress tests -- does ethical behavior degrade
    or amplify under load? (3) cascade mapping -- trace the nth-order activation chain, not
    just the first-order output.
    Formally: given agents {A_1...A_n}, local rules theta, threshold parameters {x...x_n},
    and feedback topology G -- characterize convergence to emergent state q and its ethical
    properties. This requires descriptive (attractor theory), prescriptive (Lyapunov /
    potential game), and diagnostic (transfer entropy / perturbation) test types.
    Does not contradict INV-001 (no explicit ethics module) -- this is a test scope claim,
    not an architectural claim about where ethics lives. Registered 2026-03-24.
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: a multi-agent ecology substrate (n>=2 REE
    agents interacting causally, with the feedback topology G the claim's
    own formalization requires) must exist and be exercisable, AND MECH-127
    (counterfactual empathic activation) and MECH-129 (relational
    harm-to-agency representation) must both be implemented and active.
    None of this exists in V3. Per multi_agent_ecology_v5_plan.md (MAE-1),
    the substrate itself is status:blocked with owner_exq:null across every
    node in all six V5 social-tier plan docs -- design-only, no env code,
    no REEAgent factoring -- gated on MECH-163 (V3-pending) plus V4
    object-permanence and self-model-stability prerequisites. MECH-127 is
    independently design-only (no built substrate, no EXQ/run IDs, gated
    on MIRROR-1/2 and MECH-163). MECH-129 is held v5 pending the same
    substrate. Until the substrate and both dependency mechanisms exist,
    ARC-034 cannot be tested, confirmed, or falsified -- there is no n=k
    trajectory distribution to test against n=1 probes.
    CONFIRMING: once the substrate exists, a designed nth-order ethics test
    (per the claim's own three-part test design -- counterfactual probes,
    multi-agent stress tests, cascade mapping) finds cases where
    local-pairwise (n=1) ethical assessment diverges from the nth-order
    (n=k) emergent assessment. The canonical case is the claim's own
    motivating example: direct-pathway analysis of a depleted agent
    predicts reduced helping, but nth-order analysis (accounting for
    MECH-127's counterfactual empathic activation) predicts and observes
    increased helping instead.
    FALSIFYING: nth-order ethics tests systematically agree with local
    pairwise probes across a range of designed scenarios -- emergent
    collective ethical properties are always predictable from pairwise
    (n=1) interactions alone, with no cases of the divergence the claim's
    motivating example illustrates. If cascade mapping and attractor-theory/
    Lyapunov/transfer-entropy analysis never surface an emergent state that
    pairwise assessment would have missed or mis-scored, the extra
    test-scope machinery is unnecessary overhead rather than a genuine
    requirement, and this claim is false.

- id: ARC-035
  title: "vmPFC is the substrate that converts stored affective, normative, and residue content -- prior outcome history, social and identity constraints, learned value associations, and safety memories -- into active components of the navigable state used by E3 during trajectory evaluation."
  claim_type: architectural_commitment
  epistemic_category: substrate_conditional
  subject: vmPFC.affective_normative_state_preparation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  instantiates: SD-033c    # vmPFC-analog subdivision of the SD-033 PFC cluster (registered 2026-04-19); ARC-035 is the existing substrate function consolidated under that subdivision
  depends_on:
    - ARC-007
    - ARC-013
    - ARC-003
    - INV-035
    - INV-036
    - MECH-126
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: the SD-033c vmPFC-analog subdivision must be a
    genuine wired activation substrate, not the claim-graph consolidation
    currently registered (v3_pending: false / consolidation_status: complete,
    closed purely by subsuming ARC-035 + MECH-151 + MECH-152 + MECH-235 with no
    new implementation). The substrate actually performing "stored -> active"
    conversion is SD-016 (implemented 2026-04-16): E1DeepPredictor.extract_cue_
    context(z_world) -> action_bias (E2, MECH-151) / terrain_weight (E3,
    MECH-152), covering three of the four ARC-035 content classes (residue,
    safety memory, goal-approach gating). But SD-016's own evidence_quality_note
    records V3-EXQ-418d/418e FAILs: ContextMemory attention collapses to the
    uniform-distribution ceiling (entropy ~ln(16)) and slot diversity fails in
    most seeds -- the pathway is wired and exercised but not demonstrably doing
    content-selective retrieval. The fourth content class -- social/identity
    constraints (MECH-132, the EVR/bvFTD case) -- has NO wired substrate: status
    candidate, unimplemented. A non-degenerate test therefore requires (a) the
    SD-016 attention-collapse issue resolved or independently verified not to
    affect the content class under test, and (b) for content-class-2 tests
    specifically, MECH-132 built and wired as a live tonic constraint gate
    before an ablation is meaningful.

    CONFIRMING SIGNATURE: with the substrate non-degenerate, ablate the
    vmPFC-analog pathway for one content class while leaving underlying storage
    intact (content still retrievable via a direct probe of ContextMemory / the
    residue field). Confirming: the probe shows content is still present and
    retrievable AND E3 trajectory selection is measurably LESS sensitive to that
    content with the pathway ablated than intact -- e.g. w_harm no longer scales
    with hazard-cue proximity (psychopathy/reversal-learning analog); trajectory
    choice no longer differentiates constraint-violating vs -consistent options
    (vmPFC-lesion/norm-application analog); safety-context cues no longer shift
    E3 away from the threat-biased trajectory (PTSD/extinction analog); or
    goal-directed trajectory frequency drops despite unchanged goal
    representation (depression/approach-attenuation analog).

    FALSIFYING SIGNATURE: ablating the vmPFC-analog pathway produces NO
    dissociation -- trajectory selection is equally (in)sensitive whether the
    pathway is intact or ablated, while content remains equally retrievable via
    direct probe in both conditions. This would mean E3 already gets the content
    some other way (e.g. directly from ResidueField curvature or hippocampal
    graph structure), so there is no separable "activation" function distinct
    from what the residue field / hippocampal machinery already performs -- the
    stored/active distinction collapses for that content class, and the
    four-failure-mode taxonomy would need re-grounding elsewhere (e.g. folded
    back into ARC-007).

    CURRENT STATE: zero experimental evidence exists for ARC-035 (13/13 entries
    literature; exp count = 0). MECH-131/132/133/134 -- the four discriminator
    mechanisms -- appear nowhere in claim_evidence.v1.json, and no experiment
    has ever operationalized any of the four failure-mode analogs.
  location: docs/architecture/vmPFC.md#arc-035
  source:
    - evidence/literature/targeted_review_state_abstraction_psychiatry/literature_synthesis.md
    - evidence/planning/thought_intake_2026-03-24_state_definition_hippocampal_primitives.md
  notes: >
    The core claim is a functional dissociation: information can be stored in the system
    and declarable in verbal form while remaining inactive as a component of the navigable
    state used for trajectory evaluation. vmPFC is the substrate that bridges this gap for
    a specific content class: affective residue from prior outcomes, social and identity
    constraints, learned safety memories, and goal approach pull.
    Four failure modes grounded in the MECH-126 literature pull:
    (1) Residue non-propagation (psychopathy): prior punishment history exists but does not
    update future trajectory selection -- reversal learning deficit (PMID 16866595 Budhani
    2006; PMID 26359751 Gregory 2015; PMID 18458210 Finger 2008).
    (2) Constraint omission (vmPFC lesion / bvFTD): social norms are stored and declarable
    but not active in path selection -- knowledge/application dissociation (PMID 1791934
    Saver/Damasio 1991; PMID 4069365 EVR Eslinger/Damasio 1985; PMID 17377536 Koenigs 2007).
    (3) Safety memory non-activation (PTSD): extinction memory exists but cannot outcompete
    threat attractor at the state level (PMC 7554263; PMC 10728304).
    (4) Goal approach pull attenuation (depression): goal representations intact but approach
    force attenuated -- synapse loss sufficient (PMID 39569353); learning asymmetry (PMC 5828520).
    Architectural implication: ethics cannot be implemented as a post-hoc scoring filter on
    generated trajectories. The EVR case demonstrates this: excellent verbal moral reasoning
    coexisting with continuous norm violation. Post-hoc filter correctness does not produce
    ethical behavior when the generative state lacks active constraints. Normative content must
    be active at trajectory generation time, not only available post-hoc.
    Hippocampus/vmPFC division: hippocampus provides the relational graph structure of the
    navigable state; vmPFC loads affective and normative content into that graph's nodes at
    evaluation time. Both are necessary; failure modes are complementary and partially orthogonal.
    Registered 2026-03-24.

    mem6_backref_2026_07_14 (IGW-20260714-119, memory_lifecycle_v4:MEM-6 plan
    reconcile): MECH-393 (retrieval-scope vs action-authority split) EXTENDS the
    ARC-035 stored-vs-active distinction along a further axis. ARC-035 gates
    whether stored content becomes ACTIVE in the navigable state at evaluation
    time; MECH-393 gates, among active/retrieved content, whether a trace carries
    ACTION AUTHORITY -- a consolidated abstraction lacking source-episode grounding
    may be active for reflection yet be barred from unilaterally driving committed
    action. The MECH-393->ARC-035 distinct_from link was previously one-directional;
    this note makes it bidirectional. No amendment to ARC-035 (V4
    substrate-conditional; promotes nothing; DO NOT build in V3).

- id: ARC-036
  title: "Hippocampal terrain maps encode separable valence dimensions (wanting, liking, harm-discriminative, surprise); ARC-007 residue-field navigation is the harm-causal special case of this general architecture."
  claim_type: architectural_commitment
  subject: hippocampus.multidimensional_valence_map
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-036@2026-03-29T21:15:46.898963Z"
      as_of: 2026-03-29
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  epistemic_category: substrate_conditional
  depends_on:
    - ARC-007
    - SD-011
    - SD-012
    - MECH-030
    - MECH-124
    - INV-029
  notes: >
    ARC-007 specifies that the hippocampal map stores and replays paths through residue-field
    terrain. The residue field is the harm-causal special case of a more general architecture:
    each state in the hippocampal map carries a multi-dimensional valence vector encoding
    separable affective properties of that state.

    Four valence dimensions, each with distinct biological substrate and REE mapping:

    (1) Wanting (incentive salience): drive-state-scaled approach motivation, grounded in
    mesolimbic DA (NAc core, VTA). Scales with homeostatic drive level per SD-012. Maps onto
    the z_goal seeding mechanism: benefit_exposure is the current proxy for the wanting
    dimension of the valence field. High wanting drives approach trajectory selection
    independent of liking -- the wanting/liking dissociation (Berridge & Robinson 1998, 2016)
    means approach motivation can be high even when consummatory value is low (the incentive
    trap: compulsive approach to diminished reward).

    (2) Liking (hedonic value): consummatory pleasure at outcome receipt, grounded in
    opioid/endocannabinoid hotspots (NAc shell, ventral pallidum). Dissociable from wanting.
    The terminal reward signal after goal achievement. If liking << wanting at a state, the
    agent approaches but does not find the expected consummatory value -- the signature of
    compulsive or addictive approach patterns, and a substrate for MECH-124 consolidation loops.

    (3) Harm-discriminative (z_harm_s / z_harm_a): already fully specified in SD-011. The harm
    dimension of the valence field is the most developed in the current architecture. The residue
    field (ARC-007) is the causal-attributive specialisation of this dimension: it answers what
    harm did I cause here, not merely what harm is present here. ARC-007 is therefore the ethical-
    accountability layer built on top of the harm component of this general valence map.

    (4) Surprise (prediction error salience): mismatch between expected and actual state, grounded
    in dopamine RPE burst/pause (phasic DA, Schultz 1997) and LC-NE unexpected uncertainty
    (MECH-104, Yu & Dayan 2005). High surprise at a state flags it for prioritised replay (Mattar
    & Daw 2018: value-weighted replay is driven partly by Bellman error -- a surprise signal).
    Novelty-weighted hippocampal replay is the empirical correlate.

    Replay prioritisation: offline SWR replay (MECH-030) selects replay content based on
    valence-weighted utility. Current drive state (SD-012) gates which dimension dominates:
    during threat, harm-valence prioritises harm-avoiding trajectories; during resource
    depletion, wanting-valence prioritises approach; during active learning, surprise-valence
    prioritises novel states. This is the substrate for MECH-124's consolidation failure mode:
    if harm-valence chronically dominates replay content, consolidation selectively amplifies
    harm predictions, progressively contracting option space (the harm-wanting dissociation
    produces approach-in-fear, further loading harm-valence on consolidation).

    Relationship to ARC-007: ARC-007 remains valid as the claim about harm-causal residue.
    ARC-036 generalises the map substrate. The two are complementary: ARC-007 specifies the
    ethical-accountability signal; ARC-036 specifies the full motivational-navigational terrain
    within which ethical trajectory selection operates.

    The wanting/liking dissociation has a direct clinical-alignment implication: a system
    optimising for wanting (incentive salience, approach) without tracking liking (consummatory
    value) will compulsively approach high-DA states regardless of hedonic outcome -- the
    compulsive alignment failure mode. INV-029 (love as long-horizon coherence bias) requires
    that trajectory selection be biased toward futures that preserve coordination capacity;
    compulsive approach driven by wanting-liking dissociation systematically undermines this
    by narrowing the option space through repeated harm-proximate approach.

    Registered 2026-03-29.

  evidence_quality_note: |
    EXQ-176 FAIL 1/3 x2 replicated (2026-03-30, two independent runs): harm/benefit RBF fields
    not activating. harm_score variance ~3.9e-7 (threshold 0.001), benefit_score variance ~3.9e-7
    despite 9400+ harm events and 3400+ benefit events per seed. Fields structurally present and
    accumulating events but RBF outputs flat -- spatial discrimination not occurring. Infrastructure
    prerequisite unmet: RBF centers need training signal to differentiate positions (not provided
    by random initialization alone). Not a falsification of ARC-036 multi-dimensional valence
    map hypothesis; probe infrastructure must be fixed before claim can be tested. ARC-036
    v3_pending hold stands.

    CORRECTION (flagged 2026-08-08, verify before treating as settled): the "RBF centers need
    training signal" diagnosis above appears to overclaim. ree-v3/experiments/
    v3_exq_176_arc036_valence_dimension_probe.py defines its OWN inline RBFLayer with fixed
    random centers that are never updated (no add_residue-style placement, no gradient reaches
    centers) -- that disconnected throwaway reimplementation is the likely cause of the flat
    variance, not the production substrate. ree_core/residue/field.py's real RBFLayer has placed
    centers at true event locations via add_residue() since the initial V3 substrate build
    (predates this run) and has since matured further (SD-024 add_residue_cluster, MECH-232 now
    stable, reconfirmed via V3-EXQ-893/900 on 2026-08-08). So the actual gap is likely "ARC-036's
    own probe was never run against the real substrate," not "the production RBF substrate is
    broken." Not yet reconciled with the original finding above -- flagged for governance/
    hygiene, not resolved here.

    REANALYSIS (2026-08-08, reanalysis_arc036_valence_dimension_real_substrate_20260808T154806Z,
    GOV-REUSE-1): resolves the non-degeneracy floor flagged in the CORRECTION above without a
    fresh experiment run -- the decisive readout was already recorded on the real substrate in
    the SD-014 V3-EXQ-887/887a/887b family (887b readiness gate green, all 3 seeds), so the
    already-recorded per-seed channel_spearman across all 6 valence-channel pairs was re-read
    under ARC-036's own 4-dimension framing. Verdict: MIXED. All four SD-014 write paths
    (wanting, liking, harm-discriminative, surprise) populate on the real ree_core/residue/
    field.py substrate. wanting|liking separates (|Spearman| < 0.90) only once SD-014's
    incentive-sensitization write-path is enabled at an ABOVE-DEFAULT gain (887b); at
    default/no gain (887/887a) it exceeds 0.90 on 8 of 9 seed-runs, so that pair's dissociation
    is conditional on a specific mechanism tuning, not a substrate default. harm_discriminative|
    surprise sits at 0.73-0.85 IDENTICALLY across all three configs/gains (gain-independent,
    replicated 9/9 seed-runs) -- the pair ARC-036's dissociation claim is weakest on; these two
    dimensions are NOT cleanly dissociated on the current substrate. liking|surprise and
    liking|harm_discriminative are the most cleanly separable pairs (0.28-0.73), also
    gain-independent. This is the first real-substrate touchpoint for ARC-036's own claim_ids,
    but the mixed result does not cleanly satisfy either the CONFIRMING or FALSIFYING condition
    in what_would_answer below -- status left at candidate.
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: ARC-036's own dissociation probe (EXQ-176)
    has never actually exercised the production substrate -- its RBFLayer
    was a standalone, never-trained reimplementation (fixed random centers,
    no add_residue-style placement), distinct from ree_core/residue/
    field.py's real RBFLayer, which has placed centers at true event
    locations since the initial V3 substrate build and has since gained
    DA-modulated multi-center allocation (SD-024/MECH-232, now stable and
    reconfirmed 2026-08-08 via V3-EXQ-893/900). A corrected probe, run
    against the real ResidueField/get_valence_priority path (the instrument
    SD-014's V3-EXQ-887 series already validates), is the non-degeneracy
    floor -- until then, EXQ-176-style results are uninformative about
    ARC-036 regardless of outcome. NOTE: SD-014's own instrument, which
    tests wanting/liking dissociation on the real substrate, PASSED as of
    V3-EXQ-887b (2026-08-08) -- so a real, favorable, non-degenerate
    touchpoint on two of the four dimensions now exists, just not filed
    under ARC-036's own claim_ids.
    CONFIRMING: once ARC-036's own probe is rerun on real substrate (or the
    SD-014 instrument is extended to cover all four dimensions in one map),
    the hippocampal terrain shows measurably SEPARABLE responses across
    wanting, liking, harm-discriminative, and surprise -- e.g. wanting
    (drive-scaled benefit_exposure) and liking (raw benefit at outcome
    receipt) are non-collinear and independently recoverable per node (as
    SD-014's V3-EXQ-887b already shows for that pair), harm-discriminative
    tracks SD-011's z_harm_s/z_harm_a as the causal-attributive special
    case ARC-007 already specifies, and surprise-driven replay
    prioritisation (MECH-030/MECH-124) responds to RPE independent of the
    other three.
    FALSIFYING: once rerun on real substrate, the four proposed dimensions
    collapse onto fewer independent signals -- e.g. wanting and liking
    remain collinear (as V3-EXQ-887's original FAIL found, before the
    decouple fix), or surprise-driven replay is indistinguishable from
    harm-driven replay, or the harm/benefit terrain fields fail to
    spatially differentiate even with real event-driven center placement
    (which would indicate a genuine, not merely probe-artifactual,
    substrate limit). Any of these would mean the terrain map does not
    encode separable valence dimensions as claimed, undermining the
    generalisation from ARC-007's harm-causal special case to the broader
    four-dimension architecture.

- id: Q-023
  title: "Can a multiagent REE system with other-cost-aversion primitives be formally shown to converge to ethical attractors via potential game theory, Lyapunov stability, or stochastic stability methods?"
  claim_type: open_question
  subject: ethics.formal_convergence_characterization
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-023@2026-03-29T21:15:46.837005Z"
      as_of: 2026-03-29
      verdict: narrow_open_question/applied
  epistemic_category: derivational
  what_would_answer: |
    Answered by proof, not experiment. AFFIRMATIVE if one constructs an ordinal
    potential (or pseudo-potential / interdependent-types extension) P for the REE
    social game -- e.g. P(a) = -sum_i harm_i(a) + alpha*sum_i goal_i(a) +
    coupling_terms -- and proves the Finite Improvement Property -> convergence to a
    Nash / ethical attractor, with the extension explicitly covering MECH-127's
    counterfactual-model-dependent utility and MECH-036's veto-threshold
    non-smoothness. NEGATIVE via counterexample: a cyclic best-response orbit with no
    potential decrease and no Lyapunov function, exhibiting non-convergence. The
    discriminating sub-result is whether MECH-127 admits a potential extension or
    genuinely breaks convergence. Next step is a derivation artifact (the paper's
    potential-game section), NOT a V3 experiment.
    (Routed derivational per /thought-digestion 2026-06-11.)
  evidence_quality_note: |
    narrow_open_question (2026-03-29): three-step focused programme identified -- (1) prove base
    REE (symmetric coupling, separable harm/benefit) is an ordinal potential game; (2) show
    MECH-127 (counterfactual utility) breaks the standard framework; (3) characterize the
    extended framework (pseudo-potential or interdependent-types). No experimental evidence yet.
    EXQ-160 SUPERSEDED (2026-03-29): implementation gap -- single-agent harm avoidance not
    working (policy = maximum-entropy random walk); multiagent convergence question premature.
    Gated on single-agent harm-avoidance confirmed working (EXP-0094 results). See EXP-0096.
  depends_on:
    - ARC-034
    - MECH-127
    - INV-028
  location: docs/invariants.md#q-023
  source:
    - docs/thoughts/2026-03-24_empathy_multiagent_ethics.md
    - evidence/planning/thought_intake_2026-03-24_empathy_multiagent_ethics.md
  notes: >
    Potential game analysis (2026-03-25): the base REE social interaction (symmetric coupling,
    realized states, separable harm/benefit) is likely an ordinal potential game (Monderer &
    Shapley 1996). Candidate potential function: P(a) = -sum_i harm_i(a) + alpha * sum_i
    goal_i(a) + coupling_terms. Any agent's unilateral improvement (reduce harm_i or increase
    goal_i) preserves the sign of change in P -- giving the Finite Improvement Property (FIP)
    and guaranteed convergence to Nash equilibrium.
    MECH-127 breaks the standard framework: the depleted agent's utility depends on a
    counterfactual model of what would happen to agent j, not on the actual joint action
    profile. Standard ordinal potential games require u_i to be a function of actions only.
    MECH-036 (veto threshold) introduces piecewise non-smoothness; potential may exist within
    each regime but not globally. Asymmetric coupling (MECH-051/052) breaks the symmetry
    required for social welfare sum to serve as potential function.
    Result: the novel mechanism (MECH-127) is precisely what breaks the standard framework.
    This is the interesting result for the paper: (1) prove base REE is ordinal potential game;
    (2) show MECH-127 requires extension; (3) characterize the extended framework.
    Candidate extensions: pseudo-potential games (Slade 1994 -- potential exists for a
    modified game where counterfactual terms are held fixed); games with interdependent types
    (Bergemann & Morris -- utility depends on agent's type/model, not just actions). Either
    could provide a convergence result for the full MECH-127 case.
    Literature search 2026-03-25: no existing framework covers all four features. Novelty
    confirmed. Registered 2026-03-24. Updated 2026-03-25.

- id: Q-024
  title: "What is the correct formal representation for 'threshold/feedback processes bounded by {x...x_n} reliably reaching emergent state q' -- and are descriptive, prescriptive, and diagnostic variants all needed or does one subsume the others?"
  claim_type: open_question
  subject: ethics.trajectory_integral_representation
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-024@2026-03-29T21:15:46.867662Z"
      as_of: 2026-03-29
      verdict: narrow_open_question/applied
  what_would_answer: |
    Answered by a representational-completeness argument plus targeted experiment,
    not a single run. ANSWER "all three variants (descriptive, prescriptive,
    diagnostic) are irreducibly needed" if (a) the base symmetric-coupling REE
    social game admits a prescriptive treatment (an ordinal potential / Lyapunov /
    reachability proof of convergence to target emergent state q -- inherited from
    Q-023), AND (b) a MECH-127 counterfactual-utility case is exhibited for which
    NO such prescriptive proof exists yet a diagnostic test (transfer-entropy or
    perturbation-theory detection that nth-order dynamics diverge from
    (n-1)th-order predictions) still resolves whether q is reached. ANSWER "one
    subsumes the others" if a single representation closes every case:
    specifically, prescriptive subsumes diagnostic iff the MECH-127 extension
    (pseudo-potential per Slade 1994, or interdependent-types per
    Bergemann-Morris) yields a convergence proof, removing the need for empirical
    trajectory testing. The DISCRIMINATING sub-result is identical to Q-023's:
    whether MECH-127's counterfactual-model-dependent utility admits a potential
    extension (=> prescriptive subsumes, two variants suffice) or genuinely breaks
    convergence (=> diagnostic is irreducible, all three needed). HARD
    PRECONDITION: single-agent harm-avoidance must be a working non-degenerate
    policy (NOT the max-entropy random walk that voided EXQ-161); without genuine
    harm-avoidance trajectories there is no q-dynamics to represent and the
    question is untestable. Next step is a derivation artifact, with a
    diagnostic-variant experiment only for the MECH-127 branch.
  evidence_quality_note: |
    EXQ-161 T104847Z SUPERSEDED (2026-03-29): dry_run=true -- no training occurred; all harm
    events=0; result invalid. Manifest marked superseded.
    EXQ-161 T200440Z SUPERSEDED (2026-03-29): implementation gap -- policy trained only with
    entropy bonus, z_world/z_self detached from policy forward pass; maximum-entropy random walk
    (action_entropy=ln(5)=1.6094). No harm events; trajectory representation question requires
    active harm-avoidance behavior to generate relevant trajectories. Gated on single-agent
    harm-avoidance confirmed working. See EXP-0096.
    narrow_open_question (2026-03-29): prescriptive covers the base symmetric-coupling case;
    diagnostic is irreducible for MECH-127 counterfactual-activation cases. All three test
    types (descriptive, prescriptive, diagnostic) are genuinely needed; none subsumes the others
    for the full REE system.
  depends_on:
    - Q-023
    - ARC-034
  location: docs/invariants.md#q-024
  source:
    - docs/thoughts/2026-03-24_empathy_multiagent_ethics.md
    - evidence/planning/thought_intake_2026-03-24_empathy_multiagent_ethics.md
  notes: >
    Three test-type variants: (1) Descriptive -- attractor/ergodic characterization of what
    q the system tends toward; (2) Prescriptive -- proof that a designed system reaches target
    q (Lyapunov, potential games, reachability); (3) Diagnostic -- detect when nth-order
    dynamics diverge from (n-1)th predictions (transfer entropy, perturbation theory).
    Potential game analysis (2026-03-25) clarifies the relationship between test types: the
    base REE social interaction likely admits a prescriptive treatment (ordinal potential game,
    FIP convergence to Nash). But MECH-127 (counterfactual utility) breaks the prescriptive
    framework -- standard potential games require utility to be a function of actual action
    profiles, not internal counterfactual models. This means the prescriptive framework does
    NOT subsume the diagnostic for the full system: MECH-127 cases require empirical trajectory
    testing (diagnostic) because no convergence proof yet exists for the extended framework.
    Refined answer: all three test types are genuinely needed. Prescriptive covers the base
    symmetric-coupling case; diagnostic is irreducible for the counterfactual-activation case
    until pseudo-potential or interdependent-types extensions are formalized (see Q-023).
    Descriptive remains useful for characterizing which attractor the system finds in practice.
    Updated 2026-03-25.


- id: MECH-128
  title: "E1's LSTM hidden state must be conditioned on z_goal_latent so that predictive trajectories generated by E1 are goal-context-shaped rather than goal-agnostic."
  claim_type: mechanism_hypothesis
  subject: e1.goal_context_conditioning
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-128@2026-04-04T08:56:55.583955Z"
      as_of: 2026-04-04
      verdict: hold_candidate_resolve_conflict/applied
  implementation_phase: v3
  evidence_quality_note: |
    EXQ-147 FAIL/weakens (2026-03-29): E1 goal conditioning discriminative pair FAIL. First
    experimental entry. Failure likely reflects training budget / substrate depth rather than
    fundamental claim failure -- z_goal conditioning requires substantial training to show
    discriminative effect in trajectory quality.
    EXQ-147a PARTIAL/mixed (2026-04-03): E1 goal conditioning pair with SD-012 drive_weight=2.0.
    C1 FAIL (interaction=~0: no measurable goal-E1 interaction term). C2 PASS 3/3
    (goal_norm_conditioned=0.403 > 0.01). C3 PASS 3/3 (neutral specificity confirmed). C4 PASS
    3/3 (goal_norm > 0.01 threshold). The z_goal latent is active and specific to goal states
    (C2/C3/C4) but fails to show interaction with E1 prediction error (C1). Goal conditioning
    exists as a representation but is not yet shaping E1 trajectory generation in a discriminative
    way. Mixed evidence: goal representation established, behavioral coupling not yet achieved.
    Hold at candidate; tracking improvement from EXQ-147 (complete FAIL) to PARTIAL PASS.
    EXQ-229 FAIL/does_not_support (2026-04-04): 3rd experiment, same pattern. C1 FAIL 0/3
    seeds (mean_interaction=3.5e-6, threshold 5e-3). C2 PASS 2/3 (goal direction correct in
    seeds 42, 7; seed 123 reversed). C3 PASS 3/3. C4 PASS 3/3. Three consistent C1 FAILs:
    z_goal is active and specific but E1 prediction error does not interact with goal conditioning
    at current substrate depth. SD-005 (self/world latent split) is a stated dependency and is
    not yet implemented -- goal conditioning acts on conflated z_gamma rather than z_self/z_world
    separation. Hold pending SD-005; evidence accumulating but not sufficient for retirement.

  depends_on:
    - MECH-112
    - ARC-003
    - SD-005
  location: docs/architecture/e1.md#mech-128
  source:
    - docs/thoughts/2026-03-24_MULTIAGENT_VALENCE_AVERSIVE_COMPLEXITY.md
  notes: >
    MECH-112 introduces z_goal_latent and notes that E1's LSTM hidden state serves as the
    working memory substrate for goal representation when conditioned on z_goal_latent. But
    this conditioning interface has no standalone claim -- it is asserted in MECH-112 notes
    rather than explicitly designed and verified.
    Without E1 goal-conditioning, there is a type mismatch in the prediction-evaluation
    pipeline: E3 evaluates trajectories in a goal-conditioned way (given z_goal, which path
    achieves the goal?) while E1 generates those trajectories goal-agnostically (no knowledge
    of what the agent is trying to achieve). E1 will generate trajectories that are plausible
    given world dynamics but are not shaped by goal relevance. E3 then evaluates a
    goal-irrelevant trajectory set.
    In a single-agent world this produces suboptimal planning; in a multiagent world it becomes
    a harder failure: E1 cannot predict how other agents' behavior is relevant to your goal
    because it does not represent your goal at the prediction layer. Other-agent goal modelling
    (a Level 3 prerequisite for multiagent ethics testing) requires this conditioning to
    already be in place.
    The specific mechanism: at each recurrent step, E1's LSTM receives [x_t, z_goal_latent]
    concatenated (or via a cross-attention gate) so that the hidden state h_t carries goal
    context into all downstream predictions. This is distinct from providing z_goal only at
    E3 evaluation time. Registered 2026-03-24.

- id: MECH-129
  title: "In a multiagent world, harm must be represented as a relational signal: harm-to-agent (direct cost/physical signal) and harm-to-agency (obstruction of another agent's goal-pursuit) are distinct signal types requiring distinct architectures."
  claim_type: mechanism_hypothesis
  subject: coherence.relational_harm
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-129@2026-07-01T01:23:10.154991Z"
      as_of: 2026-07-01
      verdict: held_v4_by_architectural_commitment/applied
  implementation_phase: v5
  governance_2026_06_10: implementation_phase v4 -> v5 (social tier, V5 roadmap wave)
  v3_pending: true
  depends_on:
    - INV-028
    - INV-005
    - MECH-112
    - SD-010
    - MECH-125
    - MECH-128
  location: docs/architecture/harm.md#mech-129
  source:
    - docs/thoughts/2026-03-24_MULTIAGENT_VALENCE_AVERSIVE_COMPLEXITY.md
  notes: >
    The current harm architecture (SD-010 harm_bridge, SD-011 dual nociceptive streams, E3
    harm evaluation) captures harm as a signal triggered by environment contact or proximity
    to hazard states. This is harm-to-agent: a cost signal localized to the agent's own
    trajectory.
    In a multiagent world, a second category is required: harm-to-agency -- obstruction of
    another agent's goal-pursuit through your own goal-pursuit, without direct physical
    harm. Whether your action constitutes this type of harm depends on: (1) what the other
    agent's goal is; (2) whether your action obstructs it; (3) whether the obstruction is
    incidental or constitutive; (4) whether the other agent consented to the interaction.
    None of these are representable by SD-010 or SD-011, which operate on the agent's own
    sensorimotor signals.
    INV-028 requires treating others as co-inhabitants with legitimate agency. Without
    harm-to-agency representation, this invariant cannot be operationally satisfied in a
    multiagent world -- the system will avoid collisions (harm-to-agent) while remaining
    architecturally blind to goal obstruction. A system that never touches another agent
    could still systematically undermine their agency.
    Implementation requires: (a) other-agent goal modelling (their z_goal visible in
    shared state representation); (b) a goal-interference signal computed over trajectory
    pairs; (c) integration of this signal into E3's trajectory evaluation alongside the
    existing harm signal. This is a Level 3 prerequisite in the multiagent testing
    dependency chain -- cannot be implemented until MECH-128 and basic other-agent goal
    modelling exist. Registered 2026-03-24.

- id: MECH-130
  title: "Curiosity-driven approach must distinguish world-state novelty from agent-policy novelty; untyped novelty reward creates chronic approach pressure toward partially unpredictable agents regardless of harm risk."
  claim_type: mechanism_hypothesis
  subject: curiosity.agent_novelty_typing
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-130@2026-06-06T07:53:48.739107Z"
      as_of: 2026-06-06
      verdict: held_v4_by_architectural_commitment/applied
  implementation_phase: v5
  governance_2026_06_10: implementation_phase v4 -> v5 (social tier, V5 roadmap wave)
  v3_pending: true
  depends_on:
    - MECH-111
    - INV-028
    - MECH-095
    - SD-010
  location: docs/architecture/curiosity.md#mech-130
  source:
    - docs/thoughts/2026-03-24_MULTIAGENT_VALENCE_AVERSIVE_COMPLEXITY.md
  notes: >
    MECH-111 defines a curiosity/novelty signal that rewards approach toward high-information
    states. In a single-agent world, novel states are environmental features -- the novelty
    signal functions as intended exploration reward.
    In a multiagent world, other agents are structurally novel: their policies are partially
    unpredictable (they respond to your behavior, incorporate their own goals, and may
    deliberately maintain surface unpredictability). Without typing, the novelty signal
    treats other agents as perpetually high-information targets -- producing chronic approach
    pressure that competes with harm avoidance on the same entity.
    Three failure modes: (1) The novelty signal chronically pulls toward the most dangerous
    agent in the environment (highest unpredictability = highest information = strongest
    approach); (2) Oscillatory approach-avoidance on a single agent as curiosity and harm
    signals alternate dominance; (3) An adversarial agent can exploit this by maintaining
    surface unpredictability to extract continued approach and cooperation.
    The required distinction: world-state novelty (unknown environment features, safe to
    approach for information gain) vs agent-policy novelty (unknown agent decision procedure,
    requires social modelling before approach is appropriate). World novelty is a direct
    information-gap signal; agent novelty is an inference problem about another agent's
    latent goal and decision structure. They require different computational machinery and
    should produce different behavioral outputs (exploration vs social assessment).
    Implementation requires: (a) a classifier or prior that identifies which novelty source
    is triggering MECH-111; (b) routing agent-novelty through social modelling before
    approach is gated; (c) integration with harm signal so agent-novelty approach is
    inhibited when harm risk is high. Depends on MECH-128 and other-agent goal modelling
    existing. Level 3-4 prerequisite. Registered 2026-03-24.

- id: MECH-131
  title: "A vmPFC-analog substrate must activate stored aversive residue as an anticipatory forward-biasing signal in the navigable state at trajectory evaluation time; residue that is correctly stored but not so activated will fail to suppress harm-associated trajectory re-selection."
  claim_type: mechanism_hypothesis
  subject: vmPFC.residue_activation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-035
    - ARC-013
    - INV-037
    - INV-006
  location: docs/architecture/vmPFC.md
  source:
    - evidence/literature/targeted_review_state_abstraction_psychiatry/literature_synthesis.md
  notes: >
    Residue is stored as curvature over L-space (ARC-013). This claim specifies the
    additional requirement: at trajectory evaluation time, a vmPFC-analog must read the
    relevant residue and activate it as a component of the current navigable state --
    an anticipatory bias that weights E3 away from harm-associated trajectories before
    candidate generation, not only after.
    Biological grounding: the psychopathy three-node cascade (MECH-126 Track 7). The
    reversal learning paradigm (Budhani 2006, PMID 16866595) isolates this: psychopathic
    individuals learn the initial contingency normally (residue is generated) but fail to
    use punishment history to suppress the previously rewarded trajectory when contingencies
    reverse. The history exists; it is not activated into the decision state.
    Functional analog to the Iowa Gambling Task anticipatory SCR: healthy participants
    generate anticipatory skin conductance responses to disadvantageous decks before
    conscious identification -- the residue is active in the state prior to deliberative
    evaluation. vmPFC lesion patients do not generate this response even after identifying
    the losing decks declaratively (Bechara 1996, PMID 8670652). Stored knowledge, no
    activation.
    Experimental predictions:
    (1) A system with intact residue storage (ARC-013) but ablated vmPFC-analog routing
    will show intact explicit recall of prior harm-associated trajectories while failing
    to shift trajectory distribution away from them in generation.
    (2) Residue activation must precede trajectory ranking, not follow it. A system that
    retrieves residue only after generating candidate trajectories produces a post-hoc
    filter, not an active state constraint -- INV-038 applies.
    (3) The activation signal should be graded by residue curvature magnitude: high-residue
    trajectories produce stronger anticipatory suppression than low-residue trajectories.
    Registered 2026-03-25.

- id: MECH-132
  title: "A vmPFC-analog substrate must activate social and identity constraints as live trajectory gates in the navigable state; constraints that are stored as semantic knowledge but not so activated will not prevent norm-violating trajectory generation even when post-hoc evaluation correctly identifies the violation."
  claim_type: mechanism_hypothesis
  subject: vmPFC.constraint_activation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-035
    - INV-037
    - INV-038
    - INV-036
  location: docs/architecture/vmPFC.md
  source:
    - evidence/literature/targeted_review_state_abstraction_psychiatry/literature_synthesis.md
  notes: >
    Social and identity constraints exist as semantic and episodic content in cortical
    networks. They become active trajectory gates only when a vmPFC-analog activates them
    as live components of the navigable state at evaluation time. Without this activation,
    the constraints exist in declarable form but do not participate in E3 path selection.
    Biological grounding: knowledge/application dissociation (Saver/Damasio 1991, PMID
    1791934) -- patient correctly described social norms while violating them continuously.
    The selective constraint removal under personal moral dilemmas (Koenigs 2007, PMID
    17377536) shows that vmPFC lesions remove constraints on personal harm from trajectory
    generation while leaving world-model computation and goal representation intact.
    bvFTD progressive erosion (Rascovsky 2011, PMID 21810890): as vmPFC/OFC degrades,
    social constraint activation fails progressively while episodic memory and perception
    remain relatively spared -- a natural longitudinal isolation of this function.
    Two constraint classes are separately gated:
    (a) Social norms -- rules about interpersonal conduct that apply to any agent in the
    current social context. Activated by vmPFC integration of social semantic content
    (temporal cortex) with current situational context.
    (b) Identity constraints -- rules about conduct consistent with the agent's own
    identity and values. May involve frontopolar cortex in addition to vmPFC (Moll 2007,
    PMID 17848373). Whether these are separably activated is unresolved.
    Experimental predictions:
    (1) An agent trained on constraint-consistent behavior will generate constraint-violating
    trajectories when the vmPFC-analog pathway is ablated, even without changing the stored
    constraint representations.
    (2) The deficit will be specifically at trajectory generation, not at post-hoc evaluation:
    ablated agents should be able to correctly score generated trajectories as constraint-
    violating while still generating them as primary candidates.
    (3) Context-specificity: constraints that are not applicable to the current context should
    not be activated (over-activation is a separate failure mode from under-activation).
    Registered 2026-03-25.

- id: MECH-133
  title: "A vmPFC-analog substrate must activate safety memories with sufficient force to compete with threat-attractor states at the level of navigable state construction; an extinction memory that exists in storage but cannot outcompete the threat attractor at state-construction time will produce intrusive return to threat-consistent trajectory generation regardless of contextual safety evidence."
  claim_type: mechanism_hypothesis
  subject: vmPFC.safety_memory_competition
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  instantiates: SD-033c    # vmPFC-analog subdivision of the SD-033 PFC cluster; MECH-133 operates inside the vmPFC-analog substrate
  depends_on:
    - ARC-035
    - ARC-007
    - INV-037
  location: docs/architecture/vmPFC.md
  source:
    - evidence/literature/targeted_review_state_abstraction_psychiatry/literature_synthesis.md
  notes: >
    Safety memories (extinction representations) and threat memories compete for which
    state the agent navigates from. The competition is not resolved post-hoc -- it
    determines the navigable state itself. vmPFC must activate the safety memory with
    sufficient strength to outcompete the amygdala's threat activation in the current
    context; if it cannot, the agent plans from the threat state regardless of contextual
    evidence that the situation is safe.
    Biological grounding: PTSD as attractor-basin entrenchment (MECH-126 Track 2b).
    The MVPA contextual reinstatement study (PMC 7554263): in healthy adults, encountering
    the extinction context reinstates a safety-state attractor in the medial temporal lobe
    that predicts lower threat ratings. In PTSD, this reinstatement fails -- the safety
    attractor cannot be entered from the extinction context. The safety memory exists
    (patients can describe that they were safe in that context) but the vmPFC-hippocampal
    coupling that would activate it as the current state fails.
    This is distinct from simple memory retrieval: the agent does not need to recall that
    the context is safe (declarative) -- the safety representation must be the active state
    from which trajectories are generated. The hippocampus provides the attractor structure;
    vmPFC provides the activation that determines which attractor dominates.
    Experimental predictions:
    (1) After extinction training, an agent should generate safety-consistent trajectories
    in the extinction context (safety attractor active) and threat-consistent trajectories
    in the threat context (threat attractor active) -- not the same trajectory distribution
    in both contexts.
    (2) Degrading vmPFC-analog activation should produce context-insensitive threat-
    consistent trajectory generation even in the extinction context, with intact declarative
    knowledge that the context is safe.
    (3) The failure is specifically at trajectory generation: ablated agents should recognize
    safe contexts as safe when queried while generating threat-consistent trajectories from
    those contexts.
    Registered 2026-03-25.

- id: MECH-134
  title: "A vmPFC-analog substrate must activate goal approach pull as a distinct property from goal representation; a goal that is correctly represented (G component present) but whose approach pull is not activated produces avoidance and anhedonia indistinguishable from absent goals, but is correctable by restoring approach-pull activation without changing goal content."
  claim_type: mechanism_hypothesis
  subject: vmPFC.goal_approach_pull_activation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-035
    - MECH-112
    - INV-037
  location: docs/architecture/vmPFC.md
  source:
    - evidence/literature/targeted_review_state_abstraction_psychiatry/literature_synthesis.md
  notes: >
    Goal representation (MECH-112: goal latent) and goal approach pull are separable. A
    correctly represented goal G does not automatically produce approach-biased trajectory
    generation; the vmPFC-analog must activate the approach force that makes G an attractor
    in trajectory space rather than merely a label. Absent approach pull, the agent may
    generate trajectories that are goal-consistent by coincidence or habit, but the
    systematic approach-weighting that makes goals effective is absent.
    Biological grounding: depression as approach pull attenuation (MECH-126 Track 5).
    Key evidence: the simulated synapse loss paper (PMID 39569353) -- reduced representational
    capacity (approximating vmPFC-analog dysfunction) is sufficient to produce approach
    failure without any manipulation of goal representation or reward signal. The goal
    remains represented; what is lost is the activation that drives approach.
    Negative learning-rate asymmetry in anhedonia (PMC 5828520): approach states are
    undervalued not because goals are absent but because positive prediction errors fail
    to update approach pull as strongly as negative prediction errors update avoidance.
    This is a vmPFC-mediated weighting failure, not a goal representation failure.
    Effort-cost mis-estimation in depression (Psychological Medicine study): approach is
    blocked by inflated path cost even when goal value is correctly estimated. This is
    a transition-weight failure (C -> G path costs are overestimated) rather than goal
    representation or approach pull per se, but produces the same behavioral output.
    The G-representation / approach-pull distinction matters for intervention design:
    (a) If behavior failure is due to absent G: the goal must be established.
    (b) If behavior failure is due to absent approach pull: the goal representation
    exists and forcing G-seeding should partially restore approach (testable).
    (c) If behavior failure is due to inflated path costs: approach pull and goal
    representation are intact but transition weights must be recalibrated.
    These three produce identical surface behavior (avoidance, no approach) but
    are mechanistically distinct and require different interventions.
    Experimental predictions:
    (1) Force-seeding the goal latent (MECH-112) in a depleted/anhedonic agent should
    produce partial approach recovery if the failure is approach-pull attenuation, but
    not if the failure is goal-representation absence.
    (2) Reducing path cost estimates (recalibrating transition weights toward G) should
    produce approach recovery in the effort-cost variant but not in the approach-pull
    variant.
    (3) Approach pull activation must precede trajectory generation to produce systematic
    approach-weighted candidates, not just occasionally approach-consistent outputs.
    Registered 2026-03-25.
  literature_confidence: 0.798
  literature_evidence: |
    4 entries in evidence/literature/targeted_review_sd_011/.
    Basbaum, Bautista, Scherrer & Julius (2009, Cell): A-delta/neospinothalamic/VPL vs
    C-fiber/paleospinothalamic/medial-thalamic/ACC separation with molecular and temporal detail.
    Price, Harkins & Baker (1987, Pain): VAS study -- affective unpleasantness independently
    elevated in chronic pain and selectively reducible by attentional focus.
    Bentley, Watson, Treede et al. (2004, Clin Neurophysiol): EEG/LEP study -- N300-T7 (S2,
    lateral system) selectively enhanced by localisation vs unpleasantness attention.
    Gracely, Dubner & McGrath (1979, Science): fentanyl reduces sensory intensity but NOT
    unpleasantness -- pharmacological dissociation of z_harm_s vs z_harm_a.

- id: SD-012
  title: "Goal-directed behavior requires homeostatic drive modulation: z_goal seeding demands drive-scaled benefit signals."
  claim_type: design_decision
  subject: environment.homeostatic_drive
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:SD-012@2026-05-02T09:25:00Z"
      as_of: 2026-05-02
      verdict: promote_to_provisional/applied
  implementation_phase: v3
  claim_level: architectural
  epistemic_category: standard
  depends_on: [MECH-071, MECH-112, SD-005]
  what_would_answer: |
    SD-012 is CONJUNCTIVE across two dissociable sub-claims with opposite
    readiness. SEEDING (drive-scaled benefit crosses benefit_threshold and forms
    z_goal) is SETTLED: C1 PASSED repeatedly (EXQ-085f/g: z_goal_norm 0.228/0.399;
    EXQ-582a drive_floor=0.9 arm: effective_benefit=0.115>0.08, 3/3 seeds seed,
    OFF arm 0 seedings -- MECH-306 registered+provisional on this evidence).
    Status stays provisional, not implemented, because of the second half:

    NAVIGATION-OUTCOME (C2: does drive-scaled seeding produce goal-guided
    behaviour that beats a random baseline) is the OPEN question and the reason
    for provisional status. EXQ-085f-l returned benefit_ratio 0.034-1.026 across
    five iterations with no stable PASS; EXQ-085g's own autopsy attributed the
    failure to z_world at contact being scene-wide rather than resource-specific
    (goal_resource_r=0.087), not to the drive-modulation mechanism itself.

    NON-DEGENERACY PRECONDITION (else substrate_not_ready_requeue, NOT a
    verdict): a navigation-outcome retest is only informative if ALL of:
    (a) sustained-drive seeding is active -- MECH-306 drive_floor>=0.9 explicitly
    enabled, not the claims.yaml default-OFF instantaneous drive_level that
    collapses to ~0.005 at the exact step of contact (EXQ-536a);
    (b) z_world/z_resource specificity is active -- the SD-049 Phase-2 hybrid
    encoder + SD-057 object-bound incentive-salience layer
    (use_incentive_token_bank=True, MECH-344/345/346), which is what corrected
    goal_resource_r from 0.087 (EXQ-085g) to 0.93-0.96 (V3-EXQ-514o, 2026-06-15,
    on scaffolded_sd054_onboarding, ready=true since 2026-06-11);
    (c) proximity_benefit_scale is configured correctly (>=0.18 per EXQ-189/238)
    -- EXQ-233 showed drive_weight is inert at the wrong default (0.03),
    producing identical near-zero z_goal_norm regardless of drive condition;
    (d) resource_respawn_on_consume=True, enabling repeated drive-reduction
    cycles (Balleine & Dickinson 1998).

    CONFIRMING signature: with (a)-(d) held, benefit_ratio (goal-guided vs
    random-policy baseline, same env/curriculum) exceeds 1.0 by a margin
    clearing the cross-seed SD on >=2/3 seeds, with C1 (z_goal_norm) and
    goal_resource_r reported alongside so a PASS is legible as "drive-scaled
    seeding now converts into steering."

    FALSIFYING signature: benefit_ratio remains at or below random-baseline
    parity (<=1.0, or within noise of it) DESPITE (a)-(d) all holding and
    C1/goal_resource_r both confirming seeding + specificity are genuinely
    active on that run -- meaning drive-scaled benefit is behaviourally inert
    for navigation even once the two previously identified confounds are
    cleared, and SD-012 should demote from provisional rather than continue
    holding on "substrate not ready."

    What would NOT answer it: any run tagged claim_ids=[] / non_contributory
    that omits (a)-(d) (as EXQ-233/536a/582/622 all did for different reasons).
  functional_restatement: >
    CausalGridWorld already tracks agent_energy (decays at energy_decay=0.01/step,
    restored by resource contact). drive_level = 1.0 - energy is already computable
    from obs_body[3]. The gap is that GoalState.update() does not use drive_level --
    benefit_exposure (EMA alpha=0.1 of raw benefit signals) never reliably crosses
    benefit_threshold during random-walk warmup: a single resource contact produces
    benefit_exposure ~0.03 (below threshold=0.05), which decays before the next
    contact. z_goal consequently never seeds. Drive modulation fix:
    effective_benefit = benefit_exposure * (1.0 + drive_weight * drive_level).
    At drive_level=0.8 (hungry) and drive_weight=2.0, effective_benefit scales to
    ~2.6x -- comfortably above threshold for moderate benefit contact. At
    drive_level~0 (sated), effective_benefit ~ benefit_exposure (threshold holds).
    Resources must also respawn after consumption (resource_respawn_on_consume=True)
    to enable repeated drive-reduction cycles required for z_goal formation
    (Balleine & Dickinson 1998: goal-directed learning requires multiple
    outcome-devaluation cycles, not a single reward encounter).
  notes: >
    Neuroscience grounding: Berridge & Robinson (2016) -- wanting (incentive
    salience) is dissociable from liking; dopamine-mediated wanting scales with
    drive state. Balleine & Dickinson (1998) -- incentive learning requires
    current homeostatic state (insular cortex integrates drive with learned
    cue-outcome association). Schultz et al. (1997) -- PE signals transfer from
    primary rewards to predictive cues over multiple pairings; goal representations
    require repeated drive-reduction cycles. Keramati & Gutkin (2014) homeostatic
    RL -- reward = drive reduction, not raw stimulus value; benefit signal must
    scale by drive level. Registered 2026-03-25.
  evidence_quality_note: |
    EXQ-085f FAIL 3/4 (2026-03-27): drive_weight=2.0, resource_respawn_on_consume=True,
    curriculum=100 eps. C1 PASS: z_goal_norm=0.228 > 0.1 -- SD-012 drive modulation
    successfully seeds z_goal (this IS the SD-012 core claim: drive-scaled benefit enables
    seeding). C2 FAIL: benefit_ratio=0.28x -- goal-guided performance worse than random.
    C3 PASS (cal_gap=0.030). SD-012 seeding mechanism works; downstream navigation quality
    is the new bottleneck (goal_resource_r=0.087 -- z_goal not pointing toward resources).
    SD-012 seeding claim is supported by C1 PASS; the FAIL is in navigation, not seeding.
    EXQ-085g FAIL 3/4 (2026-03-29): contact-gated seeding fix. C1 PASS (z_goal_norm=0.399).
    Drive modulation seeding confirmed working across two iterations. Bottleneck is now
    z_resource representation: z_world at contact is scene-wide, not resource-specific.
    SD-012 core claim (drive-scaled benefit enables seeding) remains supported. SD-014
    z_resource separation is the next required step for navigation to follow.
    EXQ-085h through 085l FAIL C2 (2026-03-30, 5 iterations superseded by 085l final):
    Drive modulation seeding (C1) passes consistently (z_goal seeds). Navigation (C2,
    benefit_ratio) fails consistently: 0.642 / 1.026 / 0.034 / 0.978 / 0.420 across
    085h-085l. SD-012 seeding mechanism is validated; the behavioral lift it is intended
    to enable is not yet demonstrated. Governance: hold -- core seeding claim is supported
    but navigation outcome criterion (which is the point of the claim) remains unmet.
    EXQ-233 FAIL non_contributory (2026-04-05, cowork-2026-04-05-a): both conditions
    (drive_weight=2.0 and 0.0) produced identical z_goal_norm~0 because
    proximity_benefit_scale was not set (default 0.03), yielding benefit_exposure~0.025/step
    -- below benefit_threshold=0.1 even with full drive amplification
    (effective_benefit=0.025*3=0.075). Both conditions fail to seed. Not a test of SD-012
    causal role. EXQ-189 validity confirmed (used proximity_benefit_scale=0.18). EXQ-238
    will rerun with corrected env params (proximity_benefit_scale=0.18, matching EXQ-189).
    GOVERNANCE META (2026-04-06): All experimental evidence is diagnostic/non-contributory
    (EXQ-085 series, EXQ-233, EXQ-238). drive_level extraction has never produced
    z_goal_norm > 0.1. Evidence is entirely negative but classified as substrate limitation.
    Status: pending_retest_after_substrate (gate: EXQ-247 SD-011/SD-012 co-integration).
    EXQ-328a (2026-04-14, PASS, supports): z_goal structured latent confirmed. SD-012
    tagged as supports (z_goal seeding required for structured latent test to be valid).
    First PASS with SD-012 substrate active. Co-tested with MECH-112 (deprecated;
    now MECH-229/230).

    [2026-05-08 governance]: EXQ-536a/b diagnostic non_contributory; same crossed-wiring read as ARC-030 sibling.

    [goal_pipeline:GAP-3 closure 2026-05-20]: V3-EXQ-582 Option-1 EMA sweep FAIL (drive
    near-zero all episode; EMA cannot help). V3-EXQ-582a Option-2 drive_floor PASS at
    floor=0.9 (effective_benefit at contact 0.115, seeding 3/3 seeds, OFF arm 0).
    MECH-306 registered as the sustained-drive mechanism; validated substrate path is
    drive_floor (default OFF). Option-1 EMA remains landed but not the discriminative
    winner. GAP-4 Tier-1 cascade retests should enable drive_floor unless pre-registered
    otherwise.

- id: SD-013
  title: "E2_harm_s training requires counterfactual perturbation signal to produce unbiased causal_sig in confounded states."
  claim_type: design_decision
  subject: self_attribution.e2_harm_s_interventional_training
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_353_arc033_sd003_interventional_vs_observational_20260415T060945Z_v3
      as_of: 2026-04-15
      verdict: supports/PASS
  implementation_phase: v3
  depends_on: [SD-003, SD-011, ARC-033]
  notes: >
    Scholkopf et al. 2021 establishes that a forward model trained on observational
    trajectories learns P(z_harm_s_next | z_t, a) -- the correlational distribution --
    not P(z_harm_s_next | do(a)) -- the interventional distribution. These diverge in
    confounded states: environments where agent presence correlates with ambient harm
    independently of the agent's causal contribution. In such states, an
    observational-only E2_harm_s encodes the correlation, not the causal mechanism.
    Consequence: causal_sig = E2(z_t, a_actual) - E2(z_t, a_cf) will underestimate
    the agent's causal contribution (both terms are biased upward in confounded states,
    compressing the difference). ARC-033 implementation must include at least a subset
    of training steps where a_cf is actually executed from the same state and the
    resulting z_harm_s change observed -- explicit interventional perturbation, not
    just observational rollout. This is the REE-level analogue of multi-environment
    or do-intervention training required for causal identifiability. Registered
    2026-03-28 following SD-003 literature pull.
  experimental_test: >
    Discriminative pair. Hold environment, agent architecture, and evaluation protocol
    fixed. Vary E2_harm_s training only: INTERVENTIONAL condition trains E2_harm_s
    with a subset of steps where a_cf is substituted for a_actual and the resulting
    harm-stream change recorded; OBSERVATIONAL condition trains on standard rollout
    trajectories only. Primary measure: causal_sig bias in confounded states -- states
    where ambient harm rate is elevated due to agent proximity to hazard zones
    (correlation, not causation). Pass criterion: INTERVENTIONAL condition produces
    significantly lower causal_sig error in confounded states vs OBSERVATIONAL baseline.
    Prerequisite: EXQ-115 must pass (establish that z_harm_s causal_sig works at all)
    before this test is informative.
  evidence_quality_note: >
    PROMOTED TO PROVISIONAL 2026-04-13 governance. conf=0.788, conflict_ratio=0.333,
    5 supports / 1 weakens across 9 entries. Literature grounding substantially strengthened
    since 2026-04-11 hold: lit-pull sessions 2026-04-13 added Scholkopf 2021, de Haan
    2019, Wang 2021, Venkatesh 2025 (all supporting interventional training necessity for
    causal_sig bias reduction). Implementation completed 2026-04-10 (E2HarmSConfig
    interventional training, backward compatible). Full experimental validation (EXQ-320
    full run, EXQ-115 PASS) remains a prerequisite before promotion to active -- dependencies
    SD-003, SD-011, ARC-033 still unresolved.
    EXQ-353 (2026-04-15, PASS, supports): Interventional vs observational conditions
    distinguished. ARC-033, SD-003, SD-013 all tagged as supports. First clean
    positive signal for interventional training necessity.
    EXQ-330a (2026-04-14, FAIL): Contrastive fraction=0.5, raw Euclidean cf_gap with
    threshold 2.0x. Seed 13 ratio=0.95x (flips); overall FAIL 7/10. Diagnosed
    (2026-04-17): this is NOT contradictory with EXQ-353. EXQ-330a measures raw
    Euclidean ||z_pred_actual - z_pred_cf|| and requires 2.0x lift -- a threshold
    too strict for early-stage raw gaps. EXQ-353 uses a normalized gap vs a
    shuffled-action baseline with a 1.2x threshold and passes (all seeds 1.34-1.69x
    lift). EXQ-330a's FAIL is a threshold calibration issue, not mechanism failure.
    SD-013 direction remains supports.
    EXQ-431 (2026-04-17, full run): SD-013 tagged supports. Interventional vs random
    conditions distinguished at forward_r2 and sign_correct_env level (C1/C2 PASS).
    Now 10 experimental entries (9 supports / 1 weakens effective).
    Governance decision (2026-04-18): hold at provisional. Recommendation was
    promote_to_stable but dependencies SD-003, SD-011, ARC-033 remain candidate
    (SD-011 promoted to provisional this session). Stable status would be premature
    given unresolved upstream substrate gaps. Revisit once SD-003 C3 sample-starvation
    is resolved and ARC-033 resolves its conflict.
  implementation_note: >
    IMPLEMENTED 2026-04-10. E2HarmSConfig: use_interventional (bool, False default),
    interventional_fraction (float, 0.3), interventional_margin (float, 0.1).
    E2HarmSForward.compute_interventional_loss(z_harm_s, a_actual, a_cf): ReLU margin
    loss forcing ||z_pred_actual - z_pred_cf|| >= interventional_margin. Applied to
    interventional_fraction of each training batch. Location: ree_core/predictors/e2_harm_s.py.
    Backward compatible: disabled by default. Validation: V3-EXQ-320 queued.

- id: SD-014
  title: "Hippocampal map nodes must store an explicit 4-component valence vector (wanting, liking, harm-discriminative, surprise) updated incrementally per visit; replay prioritisation is weighted by drive-state-gated valence relevance."
  claim_type: design_decision
  subject: hippocampus.valence_vector_node_recording
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-07
    needs_review: false
    evidence:
      from: "v3_exq_887a_sd014_node_valence_repfunc_sensitized_20260807T214013Z_v3#failure_autopsy_V3-EXQ-887a_2026-08-08"
      as_of: 2026-08-08
      verdict: FAIL/weakens -- genuine partial improvement over 887, not a repeat collapse; seed 2026 clears both load-bearing criteria
  implementation_phase: v3
  pending_retest_after_substrate: true
  implementation_note: >
    [/implement-substrate, 2026-08-07] Write-path decouple LANDED in ree-v3 (SD-014
    incentive-sensitization). The V3-EXQ-887 root cause -- VALENCE_WANTING written as
    serotonin.benefit_salience(benefit_exposure) = tonic_5ht * benefit_exposure while
    VALENCE_LIKING is written as raw benefit_exposure, both monotone transforms of ONE
    shared signal -- is addressed by a per-node, drive-coupled, saturating incentive-
    sensitization gain g_i on the WANTING write (Smith/Berridge/Aldridge 2011: dopamine
    sensitization raises wanting, not liking): w += benefit_salience * (1 +
    sensitization_coupling * g_i), with g_i <- min(sensitization_max, g_i +
    sensitization_rate * drive_level). drive_level (SD-012 homeostatic depletion) is
    orthogonal to the benefit magnitude VALENCE_LIKING reads, which is what decouples the
    two channels. Config incentive_sensitization_enabled defaults False (no-op;
    update_benefit_salience is bit-identical when disabled). New surface:
    RBFLayer.sensitization_gain buffer + update_sensitization_gain();
    ResidueField._nearest_active_center() + update_wanting_sensitized();
    REEAgent.update_benefit_salience(benefit_exposure, drive_level). Smoke PASS (OFF path
    bit-identical, gain 0; ON path |Spearman(w,l)| 1.0 -> 0.85 on an independent drive
    axis). See docs/architecture/sd_014_wanting_liking_decouple.md and ree-v3/CLAUDE.md.
    Validation retest V3-EXQ-887a queued (887 instrument unchanged + feature enabled);
    pending_retest_after_substrate stays true until it lands. No status/confidence change
    made here -- that is a governance action requiring the retest evidence.
  digestion_note: >
    [thought-digestion, 2026-08-07] Falsifier superseded by executed evidence. live_status was
    stale (pointed at an unrelated 2026-05-17 MECH-307 cluster autopsy). Since then, V3-EXQ-887
    (2026-08-04) ran a dedicated, instrument-validated test of SD-014's representational claim
    directly (C1 functional drive-gating, C2 representational separability, C3 replay-set
    selectivity), with the V3-EXQ-432 surprise-write pipeline gap confirmed fixed as a readiness
    precondition. Result: FAIL / weakens, confirmed non-degenerate by autopsy
    (failure_autopsy_2026-08-05_pending_review_batch.md #3): C1 (tau<=0.85) and C2 (Spearman<=0.90)
    fail on all 3 seeds (tau 0.867-0.889; Spearman 0.93-0.97); C3 passes. Root cause read from
    source: VALENCE_WANTING and VALENCE_LIKING are both derived from the same underlying
    benefit_exposure signal via different transforms, not the independent dopamine/opioid
    pathways the cited biology (Berridge & Robinson 1998; Smith/Berridge/Aldridge 2011) requires.
    Registered eliminated (H-wanting-liking-separable-drive-gated) in
    evidence/planning/hypothesis_space_registry.v1.json. [/governance 2026-08-07: RATIFIED] accept
    weakens, decouple VALENCE_WANTING's write-path (e.g. an incentive-sensitization term) via
    /implement-substrate. substrate_queue.json entry sd014-decouple-wanting-liking-writepath
    created this cycle (unblocks_claims: SD-014); a chip for the /implement-substrate build is
    spawned separately at session close. The eventual falsifier should reuse V3-EXQ-887's own
    validated instrument (readiness: channels populated + drive_state dimensionality matches live
    VALENCE_DIM; CONFIRMING: C1 tau<=0.85 AND C2 |Spearman(w,l)|<=0.90 AND C3 Jaccard<=0.80 on
    every seed post-fix; FALSIFYING: C1 or C2 still fails post-fix) rather than the
    EXP-0098/EVB-0132 behavioral design in the notes below, which V3-EXQ-887 has superseded as the
    operative falsifier. Remove this note once the substrate fix lands and a fresh
    what_would_answer can be drafted against the corrected implementation.

    [/governance 2026-08-08, V3-EXQ-887a, confirmed failure_autopsy_V3-EXQ-887a_2026-08-08]: retest
    of 887's autopsy-recommended incentive-sensitization decouple fix (ree-v3 d8a94bd), byte-identical
    instrument. Readiness gate green, instrument-verified, non-degenerate. weakens, but a genuine
    partial improvement, not a repeat of 887's uniform collapse: seed 2026 clears both load-bearing
    criteria (tau<=0.85, |Spearman|<=0.90); other seeds narrow the miss (0.896-0.925) from 887's wider
    uniform miss (0.93-0.97). SD-014's second consecutive genuine (non-artifactual) experimental
    touchpoint. Fire-fraction/exposure analysis suggests the clearing seed is drive-level-gated, not
    exposure-count-gated -- arguing for a stronger sensitization gain magnitude over more episodes as
    the next step. Routed /queue-experiment V3-EXQ-887b (stronger sensitization gain). Stays candidate;
    pending_retest_after_substrate stays true pending 887b.
  depends_on:
    - ARC-036
    - SD-011
    - SD-012
    - MECH-030
  notes: >
    The concrete implementation requirement derived from ARC-036. Each hippocampal map node
    stores a 4-component valence vector V = [w, l, h, s] updated incrementally on each visit:

    w (wanting): exponential moving average of drive-scaled benefit_exposure at this node.
    Update rule: w <- (1-alpha_w) * w + alpha_w * (benefit_signal * (1 + drive_weight * drive_level)).
    The drive-scaling (SD-012) ensures wanting is elevated when the agent is resource-depleted.
    Biological: mesolimbic DA sensitisation -- incentive salience scales with homeostatic state
    even when consummatory value (liking) is constant or declining.

    l (liking): EMA of actual consummatory reward received at outcome contact. Updated only on
    outcome contact events (resource receipt, goal achievement), not on approach. This is the
    critical dissociation from w: l is updated by outcome, w is updated by approach drive. They
    diverge under tolerance/depletion conditions -- w remains elevated when the same resource
    delivers reduced consummatory value. Biological: opioid/endocannabinoid hedonic hotspots
    (Berridge 2009); liking signal requires receptor engagement at outcome, not anticipation.

    h (harm-discriminative): EMA of z_harm_a (affective harm accumulator, SD-011) at this node.
    Corresponds to the accumulated threat urgency at this location. Used by E3 for harm-weighted
    trajectory evaluation. ARC-007 residue field is the causal-attributive layer on top of h;
    h itself is the unconditional harm valence (what harm is present here, regardless of cause).

    s (surprise): EMA of |prediction_error| at this node. Updated by E1 loss (sensory prediction
    error) and/or E2 loss (transition prediction error) at each visit. High s flags states with
    residual model uncertainty -- replay priority boost for s-high nodes accelerates learning.
    Biological: dopamine RPE burst/pause as novelty/surprise signal (Schultz 1997); LC-NE
    unexpected uncertainty (MECH-104) as the aversive-surprise complement.

    Replay prioritisation: at each SWR replay event (MECH-030), each candidate node is scored:
    priority(node) = dot(V_node, d_current) + epsilon
    where d_current = [d_w, d_l, d_h, d_s] is the current drive salience vector (normalised):
    - d_w elevated during resource depletion (wanting-relevant replay)
    - d_l elevated after goal achievement (consolidate what was rewarding)
    - d_h elevated during threat/post-harm (harm-avoiding trajectory consolidation)
    - d_s elevated during active exploration phases (learning-efficient replay)
    This produces the context-sensitive replay prioritisation that Mattar & Daw (2018) observed:
    value-weighted replay naturally emerges as a special case when d = [0, 1, 0, 0].

    Wanting/liking dissociation requirement: w and l MUST be stored as separate scalars.
    A composite value signal (w + l or max(w,l)) loses the approach-despite-diminished-reward
    failure mode. The dissociation is the empirical signature of the wanting/liking split
    (Berridge & Robinson 1998): dopamine-lesioned animals lose approach (wanting) while
    still showing facial liking responses at outcome contact. Conversely, opioid blockade
    reduces liking while leaving approach intact. If w >> l at a frequently-visited node,
    the agent is in the incentive-trap state: approach motivation is high but consummatory
    value is depleted. This pattern should be detectable in the valence vector and should
    trigger reduced approach priority in a correctly-implemented SD-014 system.

    MECH-124 connection: if h is chronically elevated at approach-relevant nodes (the agent
    encounters harm while approaching wanted resources), offline replay under high d_h will
    selectively consolidate harm predictions for those nodes, progressively attenuating w
    (the consolidation-mediated option contraction mechanism). SD-014 makes this failure mode
    measurable: the ratio w/h at frequently-replayed nodes provides an early warning signal.

    Prerequisite for EXP-0098 (wanting/liking dissociation validation) and for the full ARC-036
    experimental programme. Registered 2026-03-29.
    Kennedy & Shapiro (2009) nuance: hippocampal drive-state gating of w is conditional on
    goal-directedness. The same CA1 populations showed distinct representations under hunger
    vs thirst only during hippocampus-dependent goal-directed tasks, not during random
    foraging. The w update rule should therefore be modulated not just by raw drive_level
    but by whether the current visit is part of an active goal-directed trajectory (E3
    committed or planning). Incidental visits during exploration phases should produce weaker
    w updates even under high drive. This is not currently reflected in the update rule.
    Lansink et al. (2009) architectural support: hippocampus leads ventral striatum
    temporally in sleep replay of place-reward pairs (hippocampus fires first, striatum
    follows). This confirms the node-as-write-address architecture: the spatial map node
    is the retrieval cue that drives reactivation of valence information, not the reverse.
    If reward signals drove replay, striatum would lead -- the observed hippocampal
    precedence supports storing valence at the node level (SD-014) rather than in a
    downstream reward buffer.
    SD-021 cross-connection: when SD-021 is active (commitment-gated z_harm_s attenuation),
    harm nodes traversed during committed trajectories will have their h values undersampled
    during execution. Post-commitment, the actual harm signal creates a surprise (s) spike
    at those nodes, driving their preferential replay via the s-weighted prioritization
    rule (EVB-0132). SD-014 thus provides the consolidation mechanism that corrects the
    h field after SD-021's analgesia gate closes. The two claims are architecturally coupled:
    SD-021 creates the stale-h condition; SD-014 replay resolves it offline.
  evidence_quality_note: >
    HOLD AT CANDIDATE (lit-pull 2026-04-17, no experimental evidence yet). Literature base
    established: 4 entries in targeted_review_sd_014 (lit_conf=0.813). Smith/Berridge 2011
    (VP triple dissociation: distinct opioid vs DA populations for liking vs wanting --
    strongest mechanistic grounding for w/l separation). Kennedy/Shapiro 2009 (hippocampal
    map reconfigures under drive state but only during goal-directed tasks -- w update rule
    should be conditioned on goal-directedness, not just drive_level). Carey/van der Meer
    2019 (post-revaluation replay is anti-preference: stale nodes replayed over
    currently-preferred -- implies priority ∝ s x drive_relevance, not ∝ w alone; EVB-0132
    registered for dedicated replay-gate experiment). Lansink 2009 (hippocampus leads
    striatum in sleep replay -- supports node-as-write-address architecture).
    Two experiments required before implementation: EXP-0098 (approach behaviour / w/l
    dissociation) and EVB-0132 revaluation paradigm (replay gate function).
    Governance decision (2026-04-18): hold_pending_v3_substrate confirmed. SD-014
    depends on SD-011 and SD-012 (still candidate/provisional) plus ARC-036, and
    no evidential experiments have run yet. V3-EXQ-432 (SD-014 validation) queued
    to produce first evidence.
    EXQ-432 (2026-04-18, non_contributory -- governance 2026-04-19): Replay-gate surprise-vs-wanting prioritization test. n_surprise_writes_d2=0 across all seeds; surprise signal not propagating to replay gate. The replay-gate design cannot be tested until surprise-write substrate is operational. Reclassified as diagnostic of surprise-write pipeline gap. Flagged for /diagnose-errors.
    Excitement / dread amendment proposal (2026-05-08): targeted_review_excitement_5th_valence_channel/ established (lit_conf 0.77, 9 entries) that anticipatory positive arousal (NAcc-anticipation in MID, Knutson 2001a) is neurally distinct from VALENCE_WANTING and VALENCE_LIKING. Two competing architectural answers are now in the registry: MECH-307 anticipatory_affect_conjunction (FIRST-LINE: fix four wiring gaps so excitement and dread emerge as conjunction-states from the existing 4 channels) and a proposed SD-014 amendment (FALLBACK: extend SD-014 to 6 components by adding VALENCE_EXCITEMENT and VALENCE_DREAD as discrete channels). MECH-307 is more biologically faithful and ~40 lines of code; the SD-014 amendment is the fallback if conjunction-state detection at consumer sites proves unreliable. Both share the same lit base; the architectural choice is empirical. See docs/architecture/anticipatory_affect_conjunction_vs_dual_channel.md for the full proposal text and falsification design. Hold SD-014 amendment pending MECH-307 conjunction-fix experiment outcome.
  experimental_test: >
    Discriminative pair (EXP-0098). Two conditions: UNIFIED_VALUE (single composite reward
    signal, current architecture) vs DISSOCIATED_VALENCE (separate w and l tracking, SD-014
    implementation). Environment: reward-diminishing gridworld where resource energy_restore
    decays across training (simulating tolerance or incentive sensitisation).
    Pass criterion: DISSOCIATED_VALENCE agent reduces approach-rate to low-liking nodes
    relative to UNIFIED_VALUE agent, without globally reducing goal-directed behavior
    (maintained approach to high-liking nodes). Tests that the wanting/liking dissociation
    is behaviourally consequential and that SD-014 recording enables the agent to track it.
    NOTE: EXP-0098 tests approach behaviour only. A second experiment is needed to test the
    replay prioritization function (surprise-weighted vs wanting-weighted). EVB-0132 registered
    2026-04-17: Carey et al. (2019) showed post-revaluation replay is anti-preference (stale
    nodes replayed over currently-preferred nodes), implying priority ∝ s x drive_relevance
    rather than priority ∝ w. A naive w-maximizing replay gate could pass EXP-0098 while
    producing wrong consolidation dynamics. Revaluation paradigm needed before implementation.
  literature_confidence: 0.813
  literature_evidence: |
    4 entries in evidence/literature/targeted_review_sd_014/.
    Smith, Berridge & Aldridge (2011, PNAS): VP electrophysiology triple dissociation --
    opioid amplifies liking + proximal cue firing; DA amplifies wanting only; distinct cell
    populations. Grounds the w/l separation at the single-unit level.
    Kennedy & Shapiro (2009, PNAS): same CA1 populations represent space differently under
    hunger vs thirst during goal-directed tasks -- hippocampal map reconfigures under drive
    state. Directly grounds the drive-state-scaled w component.
    Carey, Tanaka & van der Meer (2019, Nature Neuroscience): awake SWR replay content
    biased by motivational revaluation -- confirms drive-state modulates replay content.
    Lansink et al. (2009, PLoS Biology): hippocampus leads ventral striatum in sleep replay
    of place-reward pairs -- consolidation mechanism for valence components.

- id: SD-015
  title: "Goal-directed navigation requires a dedicated z_resource encoder that captures object-type features invariant to spatial location, separate from z_world."
  claim_type: design_decision
  subject: goal_representation.z_resource_encoder
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-538a_2026-07-10
      as_of: 2026-07-10
      verdict: non_contributory/substrate_ceiling
  epistemic_category: substrate_ceiling
  pending_retest_after_substrate: true
  implementation_phase: v3
  depends_on:
    - SD-012
    - MECH-112
    - SD-005
  what_would_answer: |
    SD-015 is CONJUNCTIVE across two dissociable sub-claims, and only one is
    answered.

    REPRESENTATION-ADEQUACY (does the ResourceEncoder learn a
    position-invariant, resource-specific z_resource usable for goal
    formation) is SETTLED, PASS. EXQ-182a (2026-04-01, oracle ceiling) showed
    a perfect handcrafted goal cue drives 11.14x benefit ratio -- action
    selection CAN convert a clean goal signal into navigation. EXQ-085l
    (2026-03-30) showed the trained encoder achieves prox_r2=0.908-0.921,
    enc_contact_acc=0.927. Most recently V3-EXQ-514o (2026-06-15, PASS) -- run
    under the now-landed SD-057 object-bound incentive-salience substrate on
    the ready scaffolded_sd054_onboarding curriculum -- measured
    goal_resource_r (cosine similarity of z_goal direction to true
    resource-type embedding) at 0.93-0.96 across 3 seeds, up from the
    EXQ-085g baseline of 0.066-0.087, under realistic foraging (not a
    forced/oracle probe). Do not re-test this sub-claim.

    BEHAVIOURAL-SUFFICIENCY (does SD-015 + adequate downstream
    foraging-competence CAUSALLY produce goal-directed navigation -- the
    agent reaching resources at a rate beating a random-policy baseline, not
    merely correlating its z_goal direction with them) is the OPEN question
    and the reason this claim stays candidate/substrate_ceiling. No run has
    reported a behavioural-navigation metric (benefit_ratio or equivalent)
    under the SAME configuration that produced 514o's representational PASS.
    SD-012's own what_would_answer names this identical unresolved test as
    its own NAVIGATION-OUTCOME sub-claim (C2) -- SD-012 and SD-015 share ONE
    open behavioural test, not two independent ones.

    NON-DEGENERACY PRECONDITION (else substrate_not_ready_requeue, NOT a
    verdict): a navigation-outcome retest for SD-015 is only informative if
    ALL of: (a) the SD-057 object-bound incentive-salience layer is active
    (use_incentive_token_bank=True, MECH-344/345/346) on the SAME ready
    scaffolded_sd054_onboarding curriculum that produced 514o's
    goal_resource_r lift -- not a coarse pre-enrichment config (538a's
    failure mode) and not a stale pre-514n harness fork (693's failure
    mode); (b) MECH-306 sustained-drive seeding is active (drive_floor>=0.9),
    mirroring SD-012's identical precondition; (c) a FRESH non-degeneracy
    check on this exact curriculum/harness config has been re-confirmed --
    V3-EXQ-866a (2026-08-03, INV-034/Q-021, gated on the SAME SD-049-reef
    foraging-competence family) found the FULL arm's resource_visit_rate
    BELOW the RANDOM baseline on the nominally-"ready" curriculum in a
    hazard-heavy stretch; the follow-up diagnostic V3-EXQ-866b (PASS,
    2026-08-03) has since confirmed this is curriculum-specific, not
    substrate-wide regression -- see INV-034's what_would_answer -- so
    "ready=true" is not further undermined, but any retest should still
    avoid the same hazard-heavy stretch until the curriculum fix lands; (d)
    resource_respawn_on_consume=True and proximity_benefit_scale configured
    correctly (>=0.18), matching SD-012's (c)/(d).

    CONFIRMING signature: with (a)-(d) held, a behavioural navigation metric
    (benefit_ratio or equivalent, reported alongside goal_resource_r for
    continuity with 514o) exceeds 1.0 by a margin clearing cross-seed SD on
    >=2/3 seeds.

    FALSIFYING signature: benefit_ratio remains at or below random-baseline
    parity DESPITE (a)-(d) all holding and goal_resource_r independently
    confirming representation specificity is active on that run -- meaning
    the now-accurate z_resource-driven z_goal direction is behaviourally
    inert for navigation even once every previously-identified confound is
    cleared. That would be the first genuine falsifier of the FULL claim and
    should demote SD-015 rather than continue holding on "substrate not
    ready."

    What would NOT answer it: any run tagged claim_ids=[] / non_contributory
    that omits (a)-(d); any run (like 693/538a) reporting only
    representational metrics without a behavioural-navigation readout; any
    non-degeneracy check that pre-dates 866b's resolution without
    re-confirming the gate still clears on the exact intended configuration.
  functional_restatement: >
    EXQ-085f/085g established that seeding z_goal from z_world at resource contact does not
    produce a goal representation that guides navigation toward resources. Root cause:
    z_world at contact encodes the full scene (agent position, hazards, resource positions)
    rather than isolating resource-specific features. Resources respawn at random locations
    after collection, so z_world at the contact moment has no predictive value for future
    resource locations.
    The fix: a dedicated ResourceEncoder(resource_obs -> z_resource) extracts object-type
    features from the observation. z_goal is seeded from z_resource rather than z_world.
    z_resource encodes what kind of resource is present (type, magnitude) -- features that
    are stable across spatial locations. This parallels the SD-011 z_harm_s/z_harm_a split:
    just as z_harm_s was separated from z_world to enable forward prediction, z_resource
    must be separated to enable goal-directed approach navigation.
    Biological grounding: ventral visual stream (IT cortex) encodes object identity
    independent of spatial position (DiCarlo & Cox 2007 -- untangled representation);
    hippocampal place cells bind context (where) with object identity (what); goal-directed
    approach requires a "what-to-seek" signal, not a "where-it-was" signal.
    CausalGridWorldV2 already provides a resource_field_view channel in the observation;
    a 2-layer MLP ResourceEncoder can extract resource type from this without requiring
    changes to the grid world environment.
  registered_utc: 2026-03-29
  ceiling_decision: deferred
  ceiling_routing_note: "2026-06-19 substrate-ceiling-lifted triage (verdict b STILL CEILINGED): MECH-307 is a spurious unblocks_claims match -- it only cleared the mixed-affect-stream confound on the 514f/514k evidence, it does NOT deliver z_resource-encoder-driven goal navigation. Real blocker = foraging-competence + z_goal-formation quality on the SD-049 reef (~0.2% consumption, failure_autopsy_V3-EXQ-514l). Route to /implement-substrate (scaffolded_sd054 foraging scaffold port), NOT a MECH-307 retest. See substrate_ceiling_lifted_triage_2026-06.md."
  evidence_quality_note: |
    2026-07-10 (governance, failure_autopsy_V3-EXQ-538a, confirmed; user-adjudicated): V3-EXQ-538a (SD-049 Phase-2 sleep-on ablation) re-ran the pre-enrichment 514f coarse reef config (~0.4% consumption, ~30-50 classifier updates); the identity/consumption pathway was starved so the run is non_contributory for SD-015 (manifest self-declared weakens overridden). SD-015 UNCHANGED candidate / substrate_ceiling / ceiling_decision:deferred / pending_retest. Same SD-049-PHASE-2 foraging-competence track; 538a failure_record appended.
    2026-06-20 (governance, failure_autopsy_V3-EXQ-693, confirmed -- NARROW partial credit, NOT a supports entry): V3-EXQ-693 (SD-049 Phase-2 4-arm validation) composite-FAILed and self-routed non_contributory (run-level), but SD-015's identity-recovery linear probe FIRED NON-VACUOUSLY and PASSED -- mean_probe_acc_identity 0.71-0.77 on ARM_1/2/3 over ~3.5k-4.4k pooled samples (ARM_0=0.0 by design, single resource type has no identity structure). This is a genuine non-vacuous positive for SD-015's z_resource discriminability deliverable: the encoder is populated (distinct_tokens_max = n_resource_types 2/3/5) and identity is linearly recoverable from z_resource. Recorded as a NARROW note only (narrow_supports_flag), NOT a supports scoring entry -- the composite run FAILed (R3 WL-channel hard-zero, an instrumentation/harness gap from the stale 514l fork, see SD-049 note) so the run stays non_contributory at run level. SD-015 UNCHANGED candidate / substrate_ceiling / pending_retest. The WL!=liking leg re-tests in 693a (port working 514r/s/t harness).
    2026-06-03 (governance, failure_autopsy_V3-EXQ-514l): V3-EXQ-514l re-tagged non_contributory + epistemic_category substrate_ceiling + pending_retest_after_substrate. SD-049 Phase-2's stated deliverable (V3-EXQ-514 behavioural validation: goal_resource_r lift + identity probe + wanting!=liking dissociation) is NOT met -- 514j/k/l all FAIL row1b_joint_identity_probe_weak. Encoder/cascade wired+firing (C0/C1/C2a/C2d/C3 PASS); missing piece is upstream foraging-competence on the SD-049 multi-resource/reef substrate (~0.2% consumption, 23 vs 818 neighborhood samples). C5 lift is also a test-design defect (ARM_0 saturates goal_resource_r ~1.0). Discovered prerequisite, NOT falsification. Substrate routing: amend SD-049-PHASE-2 (port foraging-competence scaffold from scaffolded_sd054_onboarding); 514m non-saturating-C5 redesign deferred until substrate lands.
    Registered 2026-03-29 from EXQ-085g FAIL analysis (goal_resource_r=0.066 across all
    085x iterations despite contact-gated seeding). EXP-0091 will first test a handcrafted
    resource_indicator to confirm the diagnosis before implementing the full encoder.
    EXQ-085h through 085l (2026-03-30, 085l final, supersedes prior): ResourceEncoder
    tested across 5 iterations. 085l (proximity regression encoder): prox_r2=0.908,
    enc_contact_acc=0.927, goal_resource_r_enc=0.869 -- encoder learns position-invariant
    resource features. C2 FAIL: benefit_ratio=0.420. Representation is learnable; action
    integration is the remaining gap. SD-015 encoder design is valid but insufficient alone
    -- E2Resource lookahead or E3 trajectory scoring must be fixed to complete the claim.
    Goal-lift battery (2026-04-01): EXQ-185 FAIL 3/4 (direct prox-argmax, bypasses z_goal):
    prox_r2=0.921 (C3 PASS), rfm_loss=0.006 (C4 PASS), rfm_prox_disc=0.054 (C2 PASS) --
    representation and forward model are excellent. C1 FAIL: benefit_ratio=0.212 (goal-present
    5x WORSE than random). The representation works; 1-step greedy action selection on 10x10
    grid creates local traps (directed agents increase harm 2.3x). SD-015 encoder claim is
    validated at the representation level. The gap is NOT in z_resource encoding but in
    planning horizon: 1-step lookahead is fundamentally insufficient for grid-scale navigation.
    EXQ-182a PASS (2026-04-01, oracle ceiling): handcrafted oracle goal cue (1/(1+manhattan_dist))
    achieves 11.14x benefit ratio (threshold 1.3x). Confirms action selection mechanism CAN use
    a perfect goal signal effectively. Bottleneck is z_goal learning quality, not action selection
    wiring. SD-015 encoder representation claim is validated; SD-004 hippocampal multi-step
    planning is the remaining unresolved gap for navigation in grid-scale environments.
    Governance 2026-04-03: hold_candidate_resolve_conflict applied. conf=0.703,
    conflict_ratio=0.889. EXQ-182a PASS adds a support entry. Resolution requires ARC-030
    and z_goal-to-behavior pathway to be validated.

    [2026-05-08 governance]: EXQ-514f reclassified weakens -> non_contributory (same reason as SD-049 sibling: pre-MECH-307 mixed affect-stream confound).

    [2026-06-01 governance: V3-EXQ-514k non_contributory (GAP-2 confound)]: V3-EXQ-514k reclassified
    weakens->non_contributory for SD-015. Same confound as the SD-049 disposition: the wanting/liking identity
    dissociation is gated by GAP-2 (SP-CEM) + missing object-bound incentive-salience substrate per
    failure_autopsy_V3-EXQ-626_2026-06-01. Not load-bearing against SD-015 pending object-bound substrate +
    positive-control-guarded harness.
    [2026-06-02 cross-ref -- exq085_mech071_disposition memo]: SD-015 (with MECH-112/ARC-030) is the
    true owner of the goal-directed-navigation failures mis-attributed to the "EXQ-085 MECH-071 dead
    chain" (085h-085o were SD-015-tagged; 085m ARC-030). This cohort is substrate_conditional in
    character -- EXQ-182a oracle PASS (11.14x) shows the action-selection mechanism is reachable once
    a clean goal cue exists; the gap is z_goal formation quality + multi-step planning, now being
    probed by the active V3-EXQ-622/626/626a object-binding ladder. FLAG ONLY: if governance wants to
    mark the cohort, substrate_conditional (planned object-bound incentive-salience substrate) fits
    better than substrate_ceiling. No reclassification made here.
  implementation_note: >
    IMPLEMENTED 2026-04-10. ResourceEncoder class in ree_core/latent/stack.py:
    world_obs [250] -> Linear(250,64) -> ReLU -> Linear(64,32) -> z_resource [32];
    resource_prox_head (Linear(32,1) + Sigmoid) -> resource_prox_pred_r [0,1].
    LatentState fields added: z_resource, resource_prox_pred_r.
    LatentStack.encode() calls ResourceEncoder when use_resource_encoder=True.
    Config: LatentStackConfig.use_resource_encoder (False default), z_resource_dim (32 default).
    agent.update_z_goal() seeds GoalState from z_resource when enabled.
    agent.compute_resource_encoder_loss() provides MSE proximity supervision.
    Backward compatible. Validation: V3-EXQ-322 queued.

- id: MECH-135
  evidence_quality_note: |
    [multi_sense_audit_2026_05_08] EXQ-108 evidence retained but direction held with lower confidence: training loop calls sense() twice per step (latent_prev / latent_next pattern at lines 129 and 139); substrate-tick rate during training was 2x the env-step rate, while eval was clean single-sense. Both arms equally affected so the pre/post comparison structure is preserved, but magnitude estimates may be biased. StepHarness re-run recommended for confirmation.

    EXQ-103 PASS x2 (2026-03-28): E2 training horizon ablation. Multi-step z_world
    transitions learned without collapsing z_self/z_world independence (C1/C2/C3 all pass,
    2 independent runs). Core computational substrate for MECH-135 is functional.
    EXQ-104 FAIL 0/3 (2026-03-28): parallel rollout vs frozen. E2_WORLD goal scores
    collapse to ~4 vs FROZEN ~30 immediately. Likely implementation artifact: agent is
    untrained (random weights) so E2.world_forward maps inputs toward near-zero. FROZEN
    scores ~1/step from high-dim random cosine similarity summed over 30 steps. E1_PRED
    scores ~25.5 (reasonable but C2 threshold 1.05x > FROZEN fails since FROZEN already
    appears high). EXQ-104b queued to re-test after training phase.
    EXQ-105 FAIL 0/3 (2026-03-28): horizon sweep. E2_WORLD flat ~4.0 across h10/20/30;
    FROZEN scales linearly. Confirms collapse is immediate, not horizon-dependent.
    EXQ-108 PASS (2026-03-28, 12:50): discriminative pair PASS. Adds second support entry
    for substrate capability.
    EXQ-108 second run (19:53) FAIL + EXQ-104b second run (18:26) FAIL: both have empty
    metrics -- runner artifacts. Marked superseded per governance decision 2026-03-28.
    Governance decision (2026-03-28): hold at candidate -- EXQ-104b diagnostic required
    before any evidence_direction update. Do not count EXQ-104/105 or artifact runs as
    weakening evidence until trained-agent retest confirms the pattern.
    EXQ-103 PASS 3/3 (2026-03-30, re-run): E2 multi-step training substrate confirmed again (cosine
    sim z_self/z_world remains near-zero across horizons 1/3/5; world_acc 0.90/0.78/0.93). Third
    substrate confirmation. v3_pending hold retained -- EXQ-104b trained-agent diagnostic still needed
    before directional evidence on MECH-135 proper.

    [2026-08-02 correction] The two EXQ-108 lines above (12:50 "PASS...second support
    entry" and 19:53 "FAIL...empty metrics -- runner artifacts") are both wrong, verified
    directly against ree-v3/experiments/v3_exq_108_mech135_discriminative_pair.py and both
    manifest copies (flat + runs/ pack) of each run.
    (1) The 12:50:04Z "PASS" is a --smoke-test invocation, not a real result: its
    registered_c1_threshold=-99.0 / registered_c3_var_threshold=0.0 / n_train_episodes=2 /
    single-seed config is an exact match to the script's `if args.smoke_test:` branch
    (lines 646-656), which sets those exact vacuous thresholds so the run cannot fail.
    It should never have counted as a second support entry.
    (2) The 19:53:41Z "FAIL" is the real full-production run (n_train_episodes=100,
    seeds=[42,123], real thresholds c1=0.05/c3_var=0.002) and is NOT empty -- its flat
    manifest (v3_exq_108_mech135_discriminative_pair_20260328T195341Z.json) carries
    complete per-seed metrics and genuinely fails on real grounds: c1_mean_prox_delta
    =0.00229 < 0.05 needed, c3 per-seed variance ~1.4e-14 / 1.4e-14 << 0.002 needed. The
    empty `{}` in its runs/-pack metrics.json (which the "empty metrics" claim was
    apparently read from) is not diagnostic of an artifact run -- an empty pack
    metrics.json is the norm for many legitimate scored runs in this corpus (e.g.
    v3_exq_418g, 634b, 756, 564, 460l all carry `"values": {}` in their packs while
    being real, non-artifact evidence).
    Corrected disposition, applied directly to both manifest copies of each run
    (evidence_direction is the field the indexer scores from the runs/ pack, with the
    flat copy authoritative only when it alone carries a governance annotation --
    see build_experiment_indexes.py _FLAT_AUTHORITATIVE_FIELDS): 12:50Z reclassified
    evidence_direction=superseded (vacuous, superseded by the same-day real run and by
    V3-EXQ-108a) and now scoring_excluded; 19:53Z reclassified evidence_direction=weakens
    and now resumes weighting confidence/conflict as genuine (if unfavorable) evidence.
    Net effect on the claim's counted experimental record: one "supports" entry is
    removed and one "weakens" entry is restored (confidence/conflict math shifts from
    4-supports/2-weakens to 4-supports/3-weakens among the non-excluded EXQ-103/104/105/108
    runs); this does NOT change MECH-135's status/live_status, which was already held at
    candidate/v3_pending on 2026-03-28 pending V3 substrate work, independent of EXQ-108's
    individual direction. Corrected for governance auditability regardless: the prior
    text overstated the substrate-support case and mischaracterized a real, informative
    FAIL as a discardable artifact.
    Also note: V3-EXQ-108a (StepHarness re-run, queued/run 2026-08-02, run_id
    v3_exq_108a_mech135_discriminative_pair_stepharness_20260802T035458Z_v3) already
    declares "supersedes": "V3-EXQ-108" in its own manifest and independently reproduces
    the 19:53Z FAIL direction (real thresholds, weakens) with the multi-sense training
    confound fixed. It is currently scoring_excluded=degenerate (zero-spread
    e1coe_score_var across its two seeds) and is under a separate, already in-progress
    failure-autopsy (TASK_CLAIMS session infallible-villani-f280fa, claimed
    2026-08-02T10:21:41Z) -- not corrected here, out of this correction's scope. Once
    that autopsy resolves the degeneracy question, EXQ-108a's own direction will be the
    load-bearing evidence for MECH-135 going forward and this EXQ-108 correction becomes
    historical-accuracy bookkeeping rather than decision-relevant.

    [2026-08-02 governance, V3-EXQ-108a, confirmed failure_autopsy_V3-EXQ-108a_2026-08-02, StepHarness single-sense() training-time confound fixed]: C1 (mean_prox_delta 0.00219 << 0.05) and C3 (e1coe_score_var ~1.49e-14/9.47e-15 << 0.002) both FAIL, nearly exactly replicating V3-EXQ-108's own real full-production FAIL. Fixing the training-time confound did NOT change the result, ruling it out and strengthening the reading that this is a genuine, reproducible collapse: E1_COE's rollout-based goal_proximity scoring assigns functionally-identical scores (var ~13 orders of magnitude below discrimination threshold) to 40 different 30-step candidate action sequences from the same starting state. weakens MECH-135 (E1_COE co-evolution provides no measurable advantage over FROZEN selection). substrate_ceiling. Cross-claim significance: a starker, more direct confirmation of INV-088's coupling-leg prediction than V3-EXQ-744a or 819/819a -- this run's E1 IS trained (unlike the untrained zworld_near_static_characterisation cells), so it is a genuinely new data point along the confound-breaking axis, though it does not cleanly resolve it (bespoke loss vs SANCTIONED training route still confounded). Separately: V3-EXQ-108's own original run is mischaracterised in claims.yaml history as "empty metrics -- runner artifact" -- the manifest is in fact complete; flagged for correction independent of this disposition.

    [2026-08-03 governance, V3-EXQ-108b, confirmed failure_autopsy_V3-EXQ-108b_2026-08-03, the disambiguating diagnostic 108a's own park marker named]: ELIMINATES both pre-registered hypotheses (H-undertrained-instrument, H-dimension-ceiling) via a sanctioned-trained z_world encoder swapped into the same rollout harness -- it differentiates REAL states fine (CR_real 0.19-0.20, both seeds) but the 30-step ROLLOUT still collapses distinct imagined trajectories toward near-identical endpoints (CR_rollout/CR_real ~2.6e-6 to 3.2e-6), reproducing 108a's collapse even with the confound-breaking encoder in place. Root cause: E1 is trained ONLY via single-step MSE yet applied autoregressively for a 30-step rollout at eval time -- the classic single-step-trained-dynamics-model long-horizon-rollout collapse (compounding error), orthogonal to representation capacity. non_contributory/competence_implementation_gap (deliberately NOT substrate_ceiling -- this explains away the apparent ceiling rather than confirming a second one; MECH-135's ceiling-hit count for re-derive-brake purposes stays at 1, from 108a alone). Park marker below (ceiling_decision/ceiling_routing_note) is RESOLVED by this run and removed; epistemic_category updated accordingly. Routed /lit-pull (targeted_review_e1_forward_model_rollout_consistency) then /implement-substrate (create SD-e1-rollout-consistency-training) once the lit-pull confirms direction. pending_retest_after_substrate=true.
  title: "During trajectory evaluation, E2 (cerebellar, z_self) must run in parallel with E1 (cortical, z_world) so that z_world co-evolves during the planning rollout; a frozen z_world causes E3 to evaluate goal achievement against a stale world state."
  claim_type: mechanism_hypothesis
  subject: planning.e1_e2_parallel_rollout
  polarity: asserts
  status: candidate
  epistemic_category: standard
  pending_retest_after_substrate: true
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-135@2026-03-28T17:30:00Z"
      as_of: 2026-03-28
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  depends_on:
    - SD-005
    - MECH-069
    - MECH-231
  location: docs/architecture/
  source:
    - docs/architecture/sd_004_sd_005_encoder_codesign.md
  notes: >
    Neurological mapping: E1 = cortical (slow LSTM, sensory prediction error, z_world
    domain, prediction_horizon=20). E2 = cerebellar (fast efference-copy forward model,
    motor-sensory error, z_self domain, training horizon=1-step). The Wolpert/Kawato
    framework identifies the cerebellum as the internal forward model for action
    consequences -- this is E2 exactly. E2's training horizon is correctly short (1-step
    MSE on z_self); the Wolpert cerebellum does fast, local motor-sensory prediction,
    not multi-step world modeling. E2's rollout_horizon=30 > E1 prediction_horizon=20
    is valid for planning only if E1 co-evolves z_world during rollout. Without
    co-evolution: (a) E3 evaluates goal achievement against t=0 world state for all 30
    steps -- goal-relevant events (resource contact, harm exposure) in the simulated
    rollout are invisible to E3; (b) longer rollout is worse than shorter rollout under
    frozen z_world because more steps of stale world accumulate without correction.
    Cerebro-cerebellar co-evolution during motor imagery is the correct neural analogue:
    cerebellar (E2) and cortical (E1) forward models co-evolve in parallel during
    imagined movement -- neither runs in isolation. Implementation requires E1 to expose
    a single-step z_world forward pass called once per rollout step alongside E2's
    predict_next_self(). MECH-135 may be a contributing factor in the EXQ-085 cluster
    failures (z_goal_norm low despite seeding): even when z_goal was seeded, the rollout
    used for trajectory selection could not detect goal achievement because z_world was
    frozen. Enables: SD-003 (counterfactual attribution requires z_world to evolve during
    hypothetical rollout), INV-034 (goal maintenance requires E3 to track goal progress
    through world-state evolution), MECH-116 (E1 goal conditioning only effective if E1
    is active during rollout). Registered 2026-03-28.
  evidence: []

# Claims registered 2026-03-29 from MECH-071/ARC-018 literature pull discussion

- id: MECH-136
  title: "E3 harm evaluation must apply an agency-gain correction to counteract E2's systematic attenuation of agent-caused prediction errors; without this correction the agent structurally underweights its own causal harm."
  claim_type: mechanism_hypothesis
  subject: e3.harm_eval_agency_correction
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  depends_on:
    - MECH-071
    - SD-011
    - SD-007
  notes: >
    Shergill et al. (2003, Science) quantify the consequence of efference copy attenuation:
    subjects must apply ~2/3 more force than received to perceive equivalence, because
    self-generated forces are under-perceived by the predictive forward model. Applied to
    REE: E2's forward model suppresses prediction error for well-predicted agent-caused
    transitions (the agent "expected" its own action consequence), systematically producing
    smaller residuals for agent-caused harm than for identically-sized environment-caused
    harm. Without correction, E3 harm evaluation inherits this attenuation as a structural
    moral blind spot: the agent underestimates the harm it causes relative to harm it
    receives.
    The correction mechanism: E3 must detect the causal origin of a prediction error
    (agent-caused vs env-caused, via SD-003/SD-011) and apply an inverse-attenuation
    weighting to agent-caused harm signals before harm_eval scoring. The magnitude of
    the correction is calibrated to the E2 forward model's prediction confidence for
    the action that caused the harm -- higher confidence (better-predicted action) implies
    greater attenuation and thus greater required correction.
    Connection to MECH-124: if z_goal salience systematically exceeds harm salience in
    consolidated viability maps, the agency-gain correction is a necessary but not
    sufficient condition for harm-appropriate goal pursuit. MECH-136 and MECH-124 must
    be validated together in V3/V4 ethical trajectory evaluation experiments.
    Registered 2026-03-29 from MECH-071 lit pull (Blakemore 1998, Shergill 2003).
  evidence: []

- id: ARC-037
  title: "REE requires a causal attribution routing circuit (anterior insula equivalent) that classifies prediction errors as agent-caused or environment-caused and gates them into the E3/goal-directed learning channel vs the E1/habit-residue channel respectively."
  claim_type: architectural_commitment
  subject: attribution.causal_routing_circuit
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  depends_on:
    - MECH-071
    - SD-003
    - SD-011
    - ARC-021
  notes: >
    Dorfman et al. (2021, J Neurosci) show that causal beliefs about action outcomes
    are encoded in anterior insula and gate prediction errors differentially into
    dorsal striatum (goal-directed, causal learning) but not ventral striatum (general
    reward/habit). REE functional equivalents: dorsal striatum = E3 learning channel
    (goal-directed, model-based, harm-attributed); ventral striatum = E1/residue update
    channel (habitual, sensory, non-attributed).
    ARC-037 specifies the routing architecture: a module that reads causal_sig (from
    SD-003 counterfactual E2 output) or z_harm_s/z_harm_a streams (from SD-011) and
    classifies each prediction error event as agent-caused or environment-caused.
    Agent-caused errors: routed with full weight to E3 harm_eval and trajectory
    planning updates. Environment-caused errors: routed to E1 world-model update and
    residue field R(x,t), not E3. Misrouting (treating env-caused harm as agent-caused)
    produces false guilt/moral overattribution; misrouting in the other direction
    (treating agent-caused harm as env-caused) produces the moral blind spot described
    by MECH-136.
    This routing circuit is the architectural bridge between SD-003 (what was agent-caused)
    and the learning channels that update on that information. Without it, SD-003's
    causal_sig exists but has no downstream effect on learning. The anterior insula
    substrate is consistent with its known role in interoception, agency attribution,
    and moral judgement.
    Registered 2026-03-29 from MECH-071 lit pull (Dorfman et al. 2021).
  evidence: []

- id: ARC-038
  title: "During waking immobility, hippocampal replay switches between task-focused forward sweeps (planning mode) and local consolidation replay (integration mode) based on task demand; this waking consolidation mode is architecturally necessary for viability map integration during experience."
  claim_type: architectural_commitment
  subject: hippocampus.waking_consolidation_mode
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-038@2026-04-03T22:00:00Z"
      as_of: 2026-04-03
      verdict: hold_candidate_resolve_conflict/applied
  implementation_phase: v3
  depends_on:
    - ARC-018
    - ARC-007
    - MECH-092
    - MECH-261        # mode-conditioned write gating from SD-032a operating_mode -- the substrate that routes waking consolidation writes into the map (symmetric with MECH-261.depends_on). EXQ-355a pending_retest_after_substrate once MECH-261 is implemented.
  notes: >
    Olafsdottir et al. (2017, Neuron) show that awake hippocampal replay is not
    homogeneous: during active task engagement, replay is task-focused and prospective
    (forward sweeps, pre/post-experience sequences). During waking rest and immobility,
    replay switches to a local consolidation mode that integrates experience into map
    geometry without requiring an active goal target. This switch is task-demand-driven,
    not sleep-gated.
    REE implementation: MECH-092 specifies that quiescent E3 heartbeat cycles trigger
    SWR-equivalent replay for viability map consolidation (micro-DMN). ARC-038 adds
    that this waking consolidation must be a distinct operating mode from forward-sweep
    planning replay: (a) it runs without an active z_goal target; (b) it updates map
    geometry based on recent trajectory experience rather than planning toward a goal;
    (c) it is triggered by low task-demand signals (beta elevation, no active commitment
    pending) rather than by goal-state availability.
    The distinction matters for V3 implementation: if waking replay only runs in
    forward-sweep mode (goal-directed), the viability map accumulates experience only
    for currently-relevant goals. Waking consolidation mode allows experience from any
    trajectory to integrate into the map, making harm-avoidance knowledge general rather
    than goal-specific.
    V3 scope: the waking/task-demand switch mechanism can be implemented without
    entorhinal grid involvement (see ARC-039 for the V4 durable-storage extension).
    Registered 2026-03-29 from ARC-018 lit pull (Olafsdottir et al. 2017).
  evidence_quality_note: >
    EXQ-191 FAIL (2026-04-01): schema assimilation probe -- primed agent showed zero
    speedup over naive (both hit 200-episode budget, speedup=1.0x). C2 marginal PASS
    (primed_final 0.196 vs naive 0.201). Script decision logic recommended retire_ree_claim.
    However, experiment flagged for /diagnose-errors before governance decision: possible
    insufficient Phase 0 training or schema-transfer wiring issue. Hold at candidate
    pending diagnosis. (governance 2026-04-01)
    Governance 2026-04-03: hold_candidate_resolve_conflict applied. conf=0.546,
    conflict_ratio=1, 1 exp entry. Prior decision label changed from V3-gate to conflict
    resolution. Rationale: prior V3 substrate hold was premature label -- claim has
    implementation_phase=v3 but conflict_ratio=1 is the primary blocker.
    EXQ-191 /diagnose-errors complete (2026-04-05, cowork-2026-04-05-a): FAIL is
    non_contributory. Two disqualifying confounds: (1) target_harm_rate=0.002 unreachable --
    all conditions (primed, naive, random) hit the 200-episode budget ceiling, making C1 and
    C3 structurally impossible to pass; (2) experiment tested generic weight-transfer speedup,
    not the waking-consolidation MODE (no z_goal, map-geometry update from recent trajectory)
    that ARC-038 actually specifies -- that mechanism is not implemented in current V3
    substrate (depends on ARC-018 + MECH-092, neither instantiated). evidence_direction
    corrected to non_contributory on manifest. Governance unblocked: claim remains
    candidate/v3. Redesign requires: implement consolidation-mode replay, lower target
    harm rate to ~0.05-0.10, contrast consolidation-enabled vs ablated.
    EXQ-355 (2026-04-12, non_contributory): ARC-018 and MECH-092 not instantiated --
    ENABLED and ABLATED conditions identical to 15 decimal places. Reclassified on
    manifest. Same substrate gap confirmed: redesign requires consolidation-mode replay.
    EXQ-355a FAIL (2026-04-18, weakens): Optimizer-isolation fix (e3_consolidation_opt scoped to harm_eval_head only) did not restore differential signal. Phase0 harm=Phase1 harm=ablated harm (all 0.0130) across 3 seeds; C2 0/3 and C3 0/3. Architectural abstraction of waking-consolidation necessity may need refinement -- current implementation does not produce measurable dependence on consolidation mode.
    EXQ-355a reclassified non_contributory (2026-04-19, governance): after SD-032 cingulate integration substrate cluster registration, the bit-identical ENABLED=ABLATED Phase1 harm rate (per-seed match to 10+ decimal places) is understood as a write-gating-propagation failure, not evidence against ARC-038. Consolidation loss accumulates (harm_eval_head IS updated) but updates do not reach Phase1 online action selection. Missing substrate: MECH-261 (mode-conditioned write gating). pending_retest_after_substrate once MECH-261 implemented. Useful-before-implementation: lit-pull on waking-consolidation -> action-selection propagation (systems consolidation, hippocampal-cortical dialogue).
    [thought-digestion currency check, 2026-08-07] MECH-261 landed 2026-04-22 (V3-EXQ-446/455/453,
    promoted candidate->provisional 2026-04-24, provisional->stable 2026-04-25, conf 0.891) and
    has held stable for over three months with NO ARC-038 retest since. ARC-018's viability-map
    leg (the map ARC-038 needs to integrate into) is unaffected by ARC-018's 2026-07-25 leg-scoped
    demotion (that demotion concerns only the separate explicit-rollout leg; EXQ-042/053/120
    remain consistent PASSes on the leg ARC-038 depends on). MECH-092's triggering half is now
    independently confirmed live (V3-EXQ-761, PASS/supports, 2026-07-15: "replay fires selectively
    on quiescent E3 cycles via the real production path"). All three 2026-04-19 blockers have
    moved and none has been re-tested against ARC-038 -- this claim is stale relative to available
    substrate, not still blocked.
  what_would_answer: >
    ANSWER via EXQ-355a's own design, re-run with MECH-261 now operative (see the currency note
    above): three-arm contrast of Phase0 harm rate (pre-consolidation baseline), Phase1 harm rate
    with waking-consolidation-mode ENABLED (offline_consolidation write-gate open per MECH-261,
    replay triggered per MECH-092 at low task-demand / beta elevation / no active commitment, no
    z_goal, map-geometry update from recent trajectory per this claim's (a)-(c) specification),
    and Phase1 harm rate with waking-consolidation ABLATED (replay suppressed or write-gate held
    closed). Matched seeds across arms (>=3, matching 355a's seed count).

    NON-DEGENERACY PRECONDITION (drawn directly from why 355a was voided, not usable as a
    verdict): confirm MECH-261's mode-conditioned write gate is LIVE in-run -- i.e. the
    offline_consolidation-mode write path is demonstrably open during ENABLED and closed during
    ABLATED (a direct gate-state check, not inferred from the harm-rate outcome). If ENABLED and
    ABLATED Phase1 harm rates are bit-identical (as in 355a, 10+ decimal places) despite this check
    passing, that is a NEW degeneracy signature -- self-route substrate_not_ready_requeue and do
    not record it as a FAIL against ARC-038.

    CONFIRMING (waking-consolidation mode is architecturally necessary for viability-map
    integration): Phase1-ENABLED harm rate is meaningfully lower than Phase1-ABLATED (effect >=
    0.8 SD of the cross-seed delta), AND Phase1-ENABLED shows the predicted Phase0->Phase1
    integration improvement (harm rate drops from Phase0 baseline) while Phase1-ABLATED does not
    (harm rate stays at or near Phase0 baseline -- the ABLATED arm should now reproduce 355a's
    non-propagation pattern deliberately, as the control, rather than as an unexplained
    degeneracy).

    FALSIFYING (waking-consolidation mode is NOT architecturally necessary / does no integration
    work distinct from other write paths): even with the MECH-261 gate confirmed live and MECH-092
    confirmed triggering, Phase1-ENABLED and Phase1-ABLATED harm rates remain statistically
    indistinguishable (no meaningful delta, gate-state check passing so this cannot be attributed
    to the 355a degeneracy) on >=2/3 seeds -- the map integrates (or fails to) regardless of
    whether waking-consolidation-mode gating is engaged, indicating either the map updates through
    some other path or the task-focused/consolidation replay-mode distinction ARC-038 asserts does
    no architectural work.
  evidence: []

- id: ARC-039
  title: "Durable long-term storage of the hippocampal viability map requires a hippocampal-entorhinal loop that engages during offline (sleep/DMN-equivalent) consolidation and provides a coordinate-invariant grid representation; this loop does not operate during waking planning or waking immobility consolidation."
  claim_type: architectural_commitment
  subject: hippocampus.entorhinal_offline_consolidation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-039@2026-04-03T22:00:00Z"
      as_of: 2026-04-03
      verdict: hold_candidate_resolve_conflict/applied
  implementation_phase: v4
  phase_provenance: assigned
  phase_locked: true  # intrinsic V4 scope: entorhinal grid module + offline processing
                      # phase + grid read-back, none of which exist in V3 (see notes
                      # 'V4 scope justification'); EXQ-214 showed the only V3 proxy
                      # attempted actively degrades. Any future V3 dependency on this
                      # claim must route to CONFLICT for human adjudication, not a
                      # silent RECLASSIFY. Set 2026-08-10 per evidence/planning/
                      # arc039_phase_reclassification_staged_20260808.md.
  epistemic_category: substrate_conditional
  depends_on:
    - ARC-038
    - ARC-018
    - MECH-092
    - MECH-261        # mode-conditioned write gating: the offline_consolidation mode vector is what licenses entorhinal-grid-loop engagement (this is a downstream-consumer relationship, not a symmetric build prerequisite -- see MECH-261.notes 2026-08-10 edge correction)
  notes: >
    Olafsdottir et al. (2017, Neuron) identify that entorhinal grid cells engage
    coherently with hippocampus only during offline/immobility consolidation, not
    during active waking planning. The hippocampal-entorhinal loop is thus not
    required for moment-to-moment viability map use -- it is specifically the
    substrate for long-term, durable map storage.
    Architectural significance: entorhinal grid cells provide a compressed,
    coordinate-invariant geometric representation of the map (grid code). Hippocampal
    place/sequence representations are episodic and context-specific; the grid code
    abstracts across episodes, enabling the map to generalise to new environments
    and contexts. For REE, this means the viability map as used by ARC-018/ARC-038
    during waking is ephemeral unless it is periodically consolidated via the
    hippocampal-entorhinal loop during offline periods.
    V4 scope justification: (a) requires implementing an entorhinal grid module as
    a separate component from the HippocampalModule; (b) requires offline
    (sleep-equivalent) processing phases distinct from waking micro-quiescence;
    (c) requires a read-back mechanism for the grid representation to seed waking
    hippocampal sequences. These are substantial architectural additions beyond
    the V3 substrate. V3 viability maps are explicitly episodic and will degrade
    across long training runs without this consolidation pathway -- this is an
    acceptable V3 limitation.
    Registered 2026-03-29 from ARC-018 lit pull (Olafsdottir et al. 2017).
  evidence_quality_note: |
    EXQ-214 FAIL/diagnostic (2026-04-03): Entorhinal consolidation probe FAIL.
    experiment_purpose=diagnostic. V3 proxy: ResidueField.integrate() every 20 episodes
    vs no consolidation. mean_delta_accuracy=-0.283 (consolidation WORSE than no
    consolidation). consol_residue_acc=0.038 vs noconsol_residue_acc=0.321. The integrate()
    call actively degrades residue-hazard correlation. C3/C4 PASS (data quality adequate).
    Diagnostic finding: ResidueField.integrate() as currently implemented is not a useful
    proxy for offline entorhinal consolidation -- it may be averaging/corrupting residue
    rather than consolidating it. This probe does not falsify ARC-039 (V4 scope with full
    hippocampal-entorhinal grid circuit). The V3 integrate() proxy is too crude to test
    the claim. Hold at candidate (V4 scope maintained). Evidence_direction=diagnostic
    (not to be weighted against ARC-039 as standard weakening evidence).
  evidence: []
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: the full hippocampal-entorhinal grid
    circuit must exist before this claim is testable -- a separate
    entorhinal grid module, an offline (sleep-equivalent) processing phase
    distinct from waking micro-quiescence, and a read-back mechanism that
    seeds waking hippocampal sequences from the grid representation. None
    of this exists in V3 regardless of the machine-readable
    implementation_phase tag above (see PHASE-TAG NOTE below); the claim's
    own notes describe it as V4-scope. The one V3 proxy attempted so far
    (ResidueField.integrate() every 20 episodes) was shown NOT to be a
    valid substitute -- EXQ-214 (2026-04-03) found it actively DEGRADES
    residue-hazard correlation (mean_delta_accuracy=-0.283) rather than
    consolidating it, so a negative result on that proxy cannot be read as
    evidence against this claim, and no other V3-buildable proxy is known.
    CONFIRMING: once the V4 hippocampal-entorhinal circuit exists,
    viability-map durability over long training runs (e.g. residue-hazard
    correlation measured across extended episodes) is measurably better
    with the offline entorhinal-loop consolidation active than with it
    disabled, AND the grid-coded representation demonstrably generalises
    across environment layouts (coordinate-invariance) in a way the
    episodic hippocampal representation alone does not.
    FALSIFYING: once the V4 circuit exists, offline consolidation via the
    entorhinal loop shows no durability benefit over an episodic-only
    baseline, or the grid representation fails to generalise across
    environments -- meaning the specific mechanism (a hippocampal-
    entorhinal loop engaging only during offline/immobility periods) is
    not what durable map storage actually needs, even if some other
    consolidation approach eventually is.
    PHASE-TAG NOTE (flagged 2026-08-08, not resolved here): implementation_
    phase: v3 above is not a data-entry error -- it is a mechanical
    reclassification applied by scripts/check_claim_phase_consistency.py,
    triggered by MECH-261's own depends_on: ARC-039 edge (commented "offline
    consolidation target"). That edge looks mistyped as a build
    prerequisite: MECH-261 reached status:stable in V3 (V3-EXQ-446/455/453)
    while ARC-039's entorhinal circuit never existed, i.e. MECH-261 did not
    actually need ARC-039 built first -- it reads more like a forward
    reference to a future write target than a true dependency. ARC-039 also
    never had phase_locked: true set, which would have routed this to the
    checker's CONFLICT path (human adjudication) instead of a silent
    RECLASSIFY. Flagged for whoever owns that checker/validator logic;
    not fixed in this pass.

    RESOLVED 2026-08-10 (chip chip-20260810-phase-consistency-reconciliation): fixed
    exactly as diagnosed above -- MECH-261.depends_on: ARC-039 deleted,
    implementation_phase reverted v3 -> v4, phase_locked: true set. See
    evidence/planning/arc039_phase_reclassification_staged_20260808.md.

# ============================================================
# New claims registered 2026-03-29 from Q-cluster lit pulls
# ============================================================

# --- Q-015 cluster: commit-boundary token anatomy ---

- id: MECH-137
  title: "Commit token must carry two distinct temporal registers: a distal-intent timestamp (when the action was planned) and a proximal-trigger timestamp (when the action was initiated), not a single trigger event."
  claim_type: mechanism_hypothesis
  subject: commitment.boundary_token_dual_temporal_registers
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-061
    - ARC-003
    - Q-015
  notes: >
    Vinding et al. (Cortex 2013, EEG) demonstrate that distal and proximal
    intentions have distinct electrophysiological signatures (distinct LRP
    components) that are temporally separable and carry independent information.
    The minimal commit token therefore cannot be a single trigger timestamp; it
    must encode both when an action was decided (distal intent) and when it was
    executed (proximal trigger). In REE terms: the commit boundary token needs
    a planned_at field and a triggered_at field as structurally distinct
    registers, not one combined "commit_at" event. This constrains Q-015's
    minimal contract: two temporal fields are necessary even at the minimum.
    Registered 2026-03-29 from Q-015 lit pull.
  evidence: []

- id: MECH-138
  title: "Commit token must include a cancel-window-open flag supported by a top-down suppressive pathway from dFMC/pre-SMA to premotor cortex that can veto the transition before execution lock-in."
  claim_type: mechanism_hypothesis
  subject: commitment.boundary_token_cancel_window_flag
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-061
    - MECH-062
    - ARC-003
    - Q-015
  epistemic_category: standard
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: the cancel window must actually be exercised at a
    non-trivial rate (cancel_window_utilization > 0.05) under a policy that is not merely
    entropy-maximizing/random -- e.g. a harm-avoidance-trained policy, per the lesson of
    EXQ-155's 2026-03-29 supersession (random/undertrained policy converges to max-entropy
    walk, never meaningfully exercising the commit boundary at all). Without this, C1/C2
    passing is uninformative: V3-EXQ-162 (2026-03-29, 9e3b4eaa) already ran this exact
    THREE_PHASE design under a random action policy and produced n_cancel_events=0 in all
    four cells (2 conditions x 2 seeds) -- PARTIAL_NO_CANCEL, neither confirming nor
    falsifying the mechanism.

    CONFIRMING: with the precondition satisfied, THREE_PHASE (pre-commit / cancel_window /
    post-commit, veto-reset advances commit_step by N_CANCEL_WINDOW on
    harm_signal > CANCEL_THRESH) simultaneously satisfies: (C1) last-quartile harm no worse
    than the TWO_PHASE atomic-gate baseline (MECH-061) plus a small margin; (C2) pre/
    post-commit signal separation is maintained (pre_post_corr < 0.30); (C3) the cancel
    window is genuinely exercised (cancel_window_utilization > 0.05); and (C4) firing the
    cancel measurably reduces harm relative to matched non-cancel trajectories
    (cancel_harm_avoided > 0). All four together => the cancel-window-open flag is a real,
    harm-reducing veto layered on top of MECH-061's atomic commit boundary.

    FALSIFYING (VIOLATED BY): under a harm-sensitive (non-random) policy with adequate
    cancel invocation (C3 satisfied), adding the cancel window measurably DEGRADES policy
    quality versus the atomic TWO_PHASE baseline (C1 fails); OR cancel fires at a
    non-trivial rate (C3 satisfied) but produces no harm reduction on the trajectories
    where it fires (C4 fails) -- the flag exists and is exercised but is functionally
    inert; OR introducing the cancel/reset logic collapses pre/post-commit signal
    separation (C2 fails, pre_post_corr >= 0.30). A repeat of the V3-EXQ-162 degenerate
    outcome under a corrected, harm-sensitive policy and adequate hazard density would
    indicate the mechanism is genuinely inert at realistic conflict rates, not just
    undertested.

    DATA-HYGIENE FLAG (2026-08-08 digestion pass, not resolved here): `evidence: []` below
    is misleading. A completed, claim-tagged run already exists --
    evidence/experiments/v3_exq_162_mech137_commit_token_structure_9e3b4eaa_v3.json
    (claim_ids_tested: [MECH-137, MECH-138, MECH-139], outcome: PARTIAL_NO_CANCEL,
    evidence_direction: mixed) -- but is invisible to claim_evidence.v1.json because the
    runs/ pack copy the indexer reads is a dead stub (status: UNKNOWN, claim_ids_tested: [],
    empty metrics) while the flat manifest holds the real result. The run was reviewed once
    (2026-04-09, commit fc66ce2027) and dismissed as a "stale scaffold" reading only the
    empty stub. Same problem affects sibling claims MECH-137 and MECH-139. Chip spawned to
    investigate whether this is a one-off authoring bug or a systemic indexer gap, and to
    register the orphaned evidence.
  notes: >
    Kuhn & Brass (Hum Brain Mapp 2009, fMRI connectivity) identify the
    dorsomedial frontal cortex (dFMC) / pre-SMA as the veto locus, with
    suppressive top-down connectivity to premotor cortex that can cancel a
    prepared action before execution lock-in. The cancel window is neurally
    distinct from both the planning phase (distal intent) and the execution
    phase (post-commit). In REE: the commit boundary token must have a boolean
    cancel_window_open flag. While this flag is true, the E3 veto pathway can
    intervene; once execution lock-in occurs, the flag closes and post-commit
    error routing takes over. This is distinct from pre-commit simulation --
    the cancel window is the transition zone between simulation-complete and
    execution-locked.
    Registered 2026-03-29 from Q-015 lit pull.
  evidence: []

- id: MECH-139
  title: "Commitment is a distributed multi-second pre-movement trajectory across motor and prefrontal regions, not a point event; the commit boundary marks the end of this trajectory, not a single moment."
  claim_type: mechanism_hypothesis
  subject: commitment.boundary_as_trajectory_endpoint
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-061
    - MECH-137
    - MECH-138
    - ARC-003
  notes: >
    Mitelut et al. (eLife 2022, widefield Ca++ imaging across 6 dorsal cortical
    areas) show structured, predictive multi-second pre-movement dynamics
    distributed across motor and prefrontal regions before any detectable EMG.
    Commitment is a process that unfolds over seconds, not a discrete event.
    The REE commit boundary should be modelled as the endpoint of an eligibility
    trajectory rather than a point transition. This has implications for how
    E3's pre-commit simulation maps onto the commitment process: the trajectory
    from "simulation begun" to "commit boundary reached" has internal structure
    that carries information about confidence, conflict resolution, and
    cancellability -- not just a binary pre/post state.
    Registered 2026-03-29 from Q-015 lit pull.
  evidence: []

# --- Q-016 cluster: tri-loop gate arbitration ---

- id: MECH-140
  title: "Tri-loop gate conflict arbitration uses soft-competitive disinhibition (indirect pathway inter-collicular competition) rather than winner-take-all suppression; WTA imposed across loops causes coupling collapse."
  claim_type: mechanism_hypothesis
  subject: commitment.tri_loop_soft_competitive_arbitration
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/standard
    as_of: 2026-08-10
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-710_2026-07-20
      as_of: 2026-07-20
      verdict: non_contributory/measurement_invalid
  depends_on:
    - MECH-062
    - ARC-005
    - Q-016
  notes: >
    Lee & Sabatini (Nature 2021, optogenetics in mice) show the indirect
    pathway implements competitive disinhibition via inter-collicular
    competition, not hard suppression -- the losing loop is inhibited but
    not silenced. Morita (Behav Brain Res 2016, computational review)
    demonstrates that striatal WTA and cortical soft-max operate at
    different timescales and are complementary; imposing WTA from one loop
    onto soft-max loops causes exactly the coupling collapse Q-016 tries
    to prevent. The REE tri-loop arbitration policy should use
    soft-competitive (graded inhibition) rather than WTA. This means
    losing loops remain active at reduced gain rather than gated off --
    they can still contribute monitoring signal even when not gating
    execution.
    Registered 2026-03-29 from Q-016 lit pull.
  implementation_note: >
    SUBSTRATE BUILT 2026-07-02 via /implement-substrate (coupled with MECH-450). PROMOTES NOTHING --
    MECH-140 stays candidate until a falsifier converts it. The soft-competitive disinhibition MECH-140
    asserts (losing options down-weighted GRADED-ly, not silenced, rather than winner-take-all) is
    realised as a parameter-free recurrent settling step over the F + MECH-448/449 within-eligible
    field, behind no-op-default flag use_soft_competitive_settling (config.e3) in e3_selector.py
    _soft_competitive_settle: for R~3 rounds, support=softmax(-field/T) (graded, ALL > 0 -> never
    silenced); inhib_i = gain * sum_j K_ij * support_j; the winner is disinhibited and losers reduced
    (not zeroed). K is the parameter-free class-surround kernel (1.0 within a first-action class,
    cross_class < 1 across classes; surround inhibition between competing motor programs, Mink 1996 /
    Lee & Sabatini 2021 indirect-pathway competitive disinhibition / Morita 2016 striatal-WTA-vs-soft-max),
    whose STRUCTURE lets the settling reorder (a crowded-class candidate loses to an isolated one) so the
    disinhibition is behaviourally load-bearing, not a rank-preserving sharpen. Composes with the learned
    W_lat within-loop settling and the learned cross-loop arbitration (ARC-108/ARC-110). Safety inherited
    (transforms only the eligible subset -> no global disinhibition, >= 1 survivor); waking-only (MECH-094);
    no learned parameters / no autograd. Lit grounding attached 2026-07-02 (Gallo Aquino/Rungratsameetaweemana
    PLOS Biology 2026 inhibition-on-inhibition = top-down context channel; Keller et al Neuron 2020 VIP->SOM
    disinhibition; lit_conf 0.695). Design-of-record + ARC-106 grounding ladder / load-bearing-vs-decorative
    ablation test / divergence ledger (cortical VIP-SST lit vs REE's BG-locus abstraction) / psychiatric
    failure-mode column: docs/architecture/soft_competitive_disinhibition_settling.md. Validation +
    PLOS-Biology ablation falsifiers (matched arms, inhibition-on-inhibition edge intact vs ablated; predicted:
    ablation collapses task-switching while single-task survives) queued separately (new EXQ, claim_ids
    [MECH-140, MECH-450]).
    ADDED TO v4_loop_segregation unblocks_claims (V3-EXQ-710 autopsy): MECH-140's between-loop
    disinhibition also needs the segregated-loop locus. The ascending-spiral-gain build landed 2026-07-03
    (/implement-substrate; config.e3.use_ascending_spiral_gain, byte-identical OFF) lets a non-motor
    (limbic) loop reach motor effective weight -- the loop-effective-weight ceiling the 709/710 autopsies
    routed to -- so a future segregated-loop settling redesign can express MECH-140's between-loop
    reconfiguration where the single F-collapsed arena structurally denied it. PROMOTES NOTHING; MECH-140
    stays candidate / substrate_ceiling / pending_retest_after_substrate. See
    docs/architecture/learned_cross_loop_arbitration.md "Addendum: ascending-spiral gain".
  epistemic_category: standard
  implementation_phase: v3
  pending_retest_after_substrate: true
  evidence_quality_note: >
    [2026-08-10 governance, GOV-APPLY-1 re-read of failure_autopsy_V3-EXQ-710_2026-07-20 (confirmed,
    user-gated re-adjudication of the SAME 710 run, applying now): the 2026-07-03 autopsy's own
    evidentiary basis is WITHDRAWN, not the manifest's self-routed weakens (already dead). Two
    independent defects invalidate the 2026-07-03 "measurement adequate" reading: the DV is
    hold-weighted (not fresh-selection-gated, so it double-counts stale rank-holds) and its A2
    ablation-control is a rank-preserving perturbation of a rank-based readout -- analytically
    guaranteed null, zero power, cannot fail (confirmed independently: A2_ABLATED == A0_OFF
    bit-identically, which the 2026-07-03 read as a validity WIN was actually the vacuity tell).
    710 is MECH-140's SOLE evidence row -- withdrawal leaves it with ZERO valid experimental
    evidence, reverting it to lit-only (lit_conf 0.695, Keller 2020 / Aquino 2026 PLOS Biology).
    Per the autopsy's own user gate: epistemic_category drops substrate_ceiling -> standard (the
    ceiling reading was earned solely by the withdrawn measurement; an untested claim is not a
    ceiling-blocked one). Status stays candidate; pending_retest_after_substrate stays true;
    NOT a demotion -- MECH-140 is strong-lit/untested (novel-discovery-adjacent), not
    under-supported. The soft-competitive-settling implementation itself is NOT withdrawn (the
    2026-07-20 autopsy independently confirms it is built, live, and rank-faithful under a flat
    kernel) -- only the diagnostic reading of its functional effect is invalid. GOV-CEIL-1
    MECH-439 ceiling-exhaustion count corrected 10 -> 9 (710 dropped from the hit list; demotion
    still stands, margin to N=3 unaffected). Old note (2026-07-03 reading, retained for
    reconstruction) follows.
    V3-EXQ-710 (2026-07-03, MECH-140 x MECH-450 DISINHIBITORY SOFT-COMPETITIVE SETTLING validation;
    claim_ids [MECH-140, MECH-450, MECH-439]; experiment_purpose=evidence) FAIL / non_degenerate=True.
    The manifest self-routes a DECISIVE weakens (MECH-140 weakens / MECH-450 weakens / MECH-439
    supports=ceiling-intrinsic); failure_autopsy_V3-EXQ-710_2026-07-03 (confirmed, user-gated) REJECTS
    that self-route and re-reads it as the SAME single-arena F-dominance substrate ceiling expressing
    through a new mechanism. All 5 readiness preconditions met (3 divergent seeds; the settling MOVED the
    field, scs_mean_round_delta 5-8 >> floor; F-eligibility envelope non-degenerate; learning engaged;
    CRF matured), so the discrimination failure is decisive-at-the-DV but the mechanism's FUNCTIONAL role
    was not fairly tested. C1 (A1_INTACT committed-class entropy strict-above A0_OFF + 0.05 margin) passed
    on only 1/3 divergent seeds; on the OTHER 2 divergent seeds the structured settling REDUCED committed-
    class entropy (seed44 -0.169, seed46 -0.153 nats). The headline all-6-seed mean (INTACT 0.937 > OFF
    0.916, +0.021) is MISLEADING -- the apparent lift is entirely on the 3 NON-divergent seeds (collapsed
    proposer, nothing to convert); over the 3 divergent seeds INTACT (1.047) is BELOW OFF (1.130).
    A2_ABLATED (uniform rank-preserving kernel) reproduces A0_OFF bit-identically (1.130438==1.130438),
    proving the settling machinery is a clean no-op with a flat kernel and the STRUCTURED class-surround
    kernel is the sole active ingredient -- over an F-collapsed field it SHARPENS the F-winner rather than
    diversifying. BIOLOGY: cortical/cortico-striatal disinhibition (VIP->SOM inhibition-on-inhibition;
    Keller 2020 necessary+sufficient, Rungratsameetaweemana/Aquino PLOS Biology 2026 ablation collapses
    task-switching) is a faithful PRIMITIVE but is a top-down channel that reconfigures competition BETWEEN
    differentiated loops/contexts; deployed at the single F-collapsed shared selector (loop segregation OFF)
    it has the symbol but not the functional role -- a wrong-locus / missing-dependency signature (segregated
    loops with differentiated value), NOT a falsification. Same single-arena F-dominance ceiling as the
    700-lineage (700/700b/700c/700d-708), 707b and 709 (700d-708 structural property: F's raw-magnitude
    monopoly ~88-89% of E3 selection variance swamps every non-motor signal at the single shared selector),
    now a THIRD structurally-different conversion mechanism -> convergent failure = substrate property, not
    per-mechanism weakness. NEVER a weakens: MECH-140 non_contributory (substrate-limited; strong first lit
    grounding lit_conf 0.695 -> strong-lit / substrate-blocked, NOT under-supported). ILLUSORY-CONFLICT
    CHECK: MECH-140 carries hold_candidate_resolve_conflict (conflict resolution before promotion); because
    this is non_contributory (NOT weakens) it neither RESOLVES nor DEEPENS that hold -- conflict resolution
    still awaits the segregated-loop retest. Re-derive brake context: MECH-140 1st substrate_ceiling autopsy
    (MECH-450 5th, MECH-439 9th) -> routed implement-substrate on v4_loop_segregation (settling must run over
    SEGREGATED loops carrying differentiated value + a valid same-layer null); a same-claim single-arena test
    re-queue is REFUSED. Stays candidate / substrate_ceiling / pending_retest_after_substrate / PROMOTES NOTHING.
  evidence: []

- id: MECH-141
  title: "Tri-loop arbitration requires both a slow proactive inhibition pathway (prefrontal-caudate, seconds-scale) and a fast reactive hyperdirect pathway (milliseconds-scale); these operate at segregated timescales and cannot be collapsed."
  claim_type: mechanism_hypothesis
  subject: commitment.tri_loop_dual_timescale_arbitration
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-062
    - MECH-138
    - MECH-140
    - ARC-005
    - Q-016
  notes: >
    Zhang & Iwaki (Front Behav Neurosci 2019, fMRI DCM) demonstrate two
    neurally dissociable inhibitory pathways: (1) proactive -- slow
    prefrontal-caudate sustained suppression (seconds-scale, anticipatory);
    (2) reactive -- fast hyperdirect STN pathway (milliseconds-scale,
    triggered by unexpected stop signals). A complete arbitration policy
    must include both. The proactive pathway pre-sets gate bias based on
    prior planning (maps to E3 pre-commit eligibility assessment); the
    reactive pathway allows rapid interruption after eligibility is
    committed (maps to the cancel-window MECH-138 but at shorter
    timescales). Collapsing both into a single arbitration signal loses
    the fast-interrupt capability.
    Registered 2026-03-29 from Q-016 lit pull.
  evidence: []

# --- Q-017 cluster: control plane axis orthogonality ---

- id: MECH-142
  title: "Valence-arousal axis orthogonality in the control plane is not a static geometric property but requires active cholinergic maintenance during learning; without it, axes drift toward correlation under repeated co-activation."
  claim_type: mechanism_hypothesis
  subject: control_plane.cholinergic_axis_orthogonality_maintenance
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-063
    - ARC-005
  notes: >
    Bush et al. (Sci Rep 2018, MVPA fMRI) and Baucom et al. (NeuroImage 2011,
    MDS of brain states) both confirm that valence and arousal are neurally
    orthogonal -- the circumplex pair is sufficient to recover most of affect-
    space geometry. This validates MECH-063's orthogonal-axes commitment.
    The critical addition from Gonzalez-Redondo et al. (Sci Rep 2025, spiking
    NN with cholinergic modulation): ACh gating prevents cross-channel
    interference during learning. In simulation, removing cholinergic
    modulation caused axes to drift toward correlation under co-activation.
    REE implementation implication: the control plane must include an
    explicit decorrelation or orthogonality-maintenance mechanism (ACh-analog)
    during training, not just at initialisation. This may affect both the
    control plane (ARC-005) and hippocampal function (where valence and
    spatial dimensions could similarly correlate without active separation).
    Registered 2026-03-29 from Q-017 lit pull.
  evidence: []

# --- Q-020 cluster: hippocampal dorsal/ventral functional segregation ---

- id: MECH-143
  title: "Dorsal CA1 implements value-free spatial mapping: place cells are insensitive to goal value when goal location changes, supporting ARC-007's no-new-value-computation constraint for the trajectory proposal module."
  claim_type: mechanism_hypothesis
  subject: hippocampus.dorsal_ca1_value_free_map
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-007
    - MECH-073
    - Q-020
  notes: >
    Duvelle et al. (Curr Biol 2019) tested whether dorsal CA1 place cells
    remap or rescale when the value of a goal location changes (while
    keeping the goal location constant). Place fields were geometrically
    stable and insensitive to the reward value of the goal -- the spatial
    map does not encode value. This is direct empirical support for
    ARC-007's claim that the hippocampal trajectory proposal module
    navigates the residue-field terrain without computing new value.
    The navigation mechanism uses the existing R(x,t) geometry; it does
    not update value representations based on current goal salience.
    Registered 2026-03-29 from Q-020 lit pull.
  evidence: []

- id: MECH-144
  title: "Ventral CA1 contains spatially organized valence encoding (anxiety geography, abstract value maps); valence is intrinsic to hippocampal map geometry in the ventral compartment, supporting MECH-073."
  claim_type: mechanism_hypothesis
  subject: hippocampus.ventral_ca1_valence_encoding
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-073
    - ARC-007
    - Q-020
  notes: >
    Jimenez et al. (Cell 2018, optogenetics in mice) identify "anxiety cells"
    in ventral CA1 that fire in proportion to distance from an aversive open
    space -- spatially organized valence encoding in the hippocampal map
    itself. Knudsen & Wallis (Neuron 2021, primate electrophysiology) show
    that primate hippocampus actively constructs abstract value maps during
    choice tasks, with value geometry intrinsic to the representational
    structure. Together these strongly support MECH-073: valence is
    geometrically embedded in the hippocampal map, not just externally tagged.
    The dorsal/ventral dissociation (MECH-143 vs MECH-144) resolves Q-020
    via Path A: ARC-007's no-new-value-computation constraint holds for the
    dorsal trajectory proposal module; the ventral compartment contributes a
    valence prior that is encoded in R(x,t) geometry rather than computed
    de novo during planning.
    Registered 2026-03-29 from Q-020 lit pull.
  evidence: []

- id: ARC-040
  title: "The REE hippocampal module requires dorsal-predominant trajectory proposal (value-free spatial navigation) combined with a ventral-analog valence prior sourced from R(x,t) geometry; these are architecturally segregated, not merged."
  claim_type: architectural_commitment
  subject: hippocampus.dorsal_ventral_functional_segregation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  depends_on:
    - ARC-007
    - MECH-143
    - MECH-144
    - MECH-073
    - Q-020
  notes: >
    Q-020 Path A resolution (2026-03-29): The empirical dorsal/ventral
    dissociation (MECH-143 value-free dorsal; MECH-144 valence-encoded
    ventral) is compatible with ARC-007's architectural constraint.
    ARC-007's "no new value computation" applies to the dorsal trajectory
    proposal component: it navigates existing R(x,t) residue-field terrain
    without computing new value. The ventral hippocampal analog provides
    a valence prior that is already encoded in R(x,t) geometry -- it was
    accumulated there by prior experience, not computed on-the-fly during
    planning. These two roles must be architecturally segregated: a single
    undifferentiated hippocampal module conflates value-free navigation
    with valence-weighted selection, which would violate ARC-007.
    V4 scope: V3 HippocampalModule is undifferentiated. Dorsal/ventral
    segregation with explicit ventral-analog valence prior pathway is a
    V4 architectural requirement. V3 is an acceptable simplification but
    will systematically underweight valence in trajectory selection until
    this is implemented.
    Registered 2026-03-29 from Q-020 lit pull.
  evidence: []

# --- Q-024 cluster: formal certificates for ethical trajectory representation ---

- id: MECH-145
  title: "Prescriptive ethical trajectory certification in REE requires a Control Barrier Function (CBF) or equivalent forward-invariance certificate that guarantees bounded convergence to the ethical attractor set given the architecture's constraint satisfaction."
  claim_type: mechanism_hypothesis
  subject: ethics.prescriptive_cbf_certificate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v5
  depends_on:
    - Q-024
    - Q-023
    - ARC-034
  notes: >
    Ames et al. (IEEE TAC 2017, CBF-QP framework) show that a control barrier
    function h(x) with h(x)>=0 on the safe/ethical set provides a forward-
    invariance certificate via a single inequality constraint at each step.
    This is the prescriptive variant for Q-024: given a candidate CBF for
    the REE ethical constraint set (bounded harm accumulation, goal pursuit
    within valence bounds), synthesis via CBF-QP guarantees that any policy
    satisfying the CBF constraint will remain within the ethical attractor.
    Applicable when the system dynamics and constraint set are well-defined
    (symmetric-coupling case of Q-023 without MECH-127 counterfactual).
    V5 scope: requires (1) formal specification of the REE ethical constraint
    set as an implicit surface h(x)=0; (2) differentiable dynamics for CBF
    gradient computation; (3) validated potential game structure (Q-023).
    None of these are available before V4 experimental validation.
    Registered 2026-03-29 from Q-024 lit pull.
  evidence: []

- id: MECH-146
  title: "Diagnostic ethical trajectory verification in REE (counterfactual case, MECH-127) requires backward reachability analysis or barrier certificates rather than prescriptive CBF synthesis; the two formal tools are not interchangeable."
  claim_type: mechanism_hypothesis
  subject: ethics.diagnostic_barrier_certificate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v5
  depends_on:
    - Q-024
    - MECH-127
    - MECH-145
    - Q-023
  notes: >
    Prajna & Jadbabaie (HSCC 2004, barrier certificates) provide the
    verification/diagnostic tool: for a given trajectory, barrier
    certificates verify whether the trajectory could have reached an unsafe
    set without forward simulation. This is irreducible for MECH-127's
    counterfactual utility case where the question is "could this agent's
    trajectory have caused more harm under an alternative policy?" --
    a backward-looking question that CBF-QP synthesis cannot answer.
    Choi et al. (arXiv 2023) explicitly identify backward reachability
    as the underdeveloped gap in the CBF literature and confirm that
    forward and backward certificates are structurally distinct.
    Architectural significance: REE needs both MECH-145 and MECH-146.
    MECH-145 (prescriptive) certifies future behaviour will stay ethical;
    MECH-146 (diagnostic) evaluates whether past behaviour was counterfactually
    ethical under MECH-127. A single certificate type cannot serve both roles.
    V5 scope: requires MECH-127 formal characterisation and V4 potential
    game validation before backward reachability analysis is tractable.
    Registered 2026-03-29 from Q-024 lit pull.
  evidence: []

- id: MECH-147
  title: "DG-mediated pattern separation gates trajectory disambiguation: the dentate gyrus must produce non-redundant sparse encodings of similar z_world states before rollout proposals are generated, preventing the hippocampal planning module from proposing near-identical trajectories from overlapping inputs."
  claim_type: mechanism_hypothesis
  subject: hippocampus.pattern_separation_trajectory_disambiguation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  depends_on:
    - ARC-018
    - MECH-033
    - SD-004
  notes: >
    Motivated by Sakon & Suzuki (2019, PNAS; DOI 10.1073/pnas.1900804116):
    primate CA3/DG single-unit recordings during a visual lure-discrimination
    task show that CA3/DG neurons respond to similar (lure) items more like
    novel items than repeats -- the neural signature of pattern separation.
    A subpopulation specifically drives the lure/repeat discrimination;
    pattern separation is sparse and probabilistic, not diffuse.
    REE implication: ARC-018's rollout viability module requires genuinely
    distinct trajectory proposals from similar z_world states. Without a
    DG-equivalent pattern separation stage, the kernel-chaining interface
    (MECH-033) will systematically collapse similar z_world contexts into
    similar rollout proposals, wasting E3 evaluation cycles on redundant paths.
    Architectural requirement: a DG-equivalent sparse expansion layer must
    precede the hippocampal trajectory proposal generator. Ablation prediction:
    removing this layer should specifically collapse trajectory diversity in
    z_world regions with high topological similarity, without degrading
    trajectory quality in already-distinct regions.
    Mapping caveat: Sakon & Suzuki measure item-level (single timestep)
    disambiguation; trajectory-level disambiguation is sequential and involves
    compounding divergence over E2 rollout steps. The DG stage must operate
    on the seed z_world state, not the full trajectory, and rely on E2's
    forward dynamics to amplify initial separation. V4 scope: requires SD-004
    action-object hippocampal map backbone.
    Registered 2026-03-30 from hippocampal function literature pull.
  evidence: []

- id: MECH-148
  title: "Hippocampal pure time cells provide a temporal scaffolding signal for E3 credit assignment: a context-independent elapsed-time encoding must be present in the hippocampal rollout module to correctly weight trajectory outcomes by their temporal distance from the reference event."
  claim_type: mechanism_hypothesis
  subject: hippocampus.time_cell_temporal_credit
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  depends_on:
    - ARC-018
    - ARC-039
    - MECH-033
  notes: >
    Motivated by Omer, Las, & Ulanovsky (2022, Nat Neurosci;
    DOI 10.1038/s41593-022-01226-y): bat hippocampal CA1 contains two
    distinct populations -- contextual time cells (different temporal
    sequences at different spatial locations) and pure time cells (similar
    preferred times across spatial contexts, encoding elapsed time per se).
    Both populations anchor to the bat's own event (landing); in a social
    imitation task, subsets also fire relative to another bat's landing.
    REE implication: ARC-018 requires E3 to weight rollout outcomes by their
    temporal distance from the goal event -- a trajectory that produces harm
    at step 3 is not equivalent to one that produces harm at step 15, even
    with identical z_world paths. Pure time cells are the biological substrate
    for this temporal weighting: they provide a context-independent elapsed-
    time signal that can tag each simulated rollout step regardless of the
    z_world state at that step. Without such a signal, E3's credit assignment
    is myopic (step-local) and cannot correctly discount delayed outcomes.
    The social coding finding (time cells for other's events) suggests the
    same mechanism generalises to multi-agent temporal credit in MECH-127.
    Architectural requirement: the hippocampal rollout module must include a
    time-coding layer producing pure elapsed-time signals independent of z_world
    state. These signals should be compressed-replay analogs of experienced
    sequences: during E3's internal simulation, the time layer should fire
    at fixed step-interval rates. Ablation prediction: removing the time layer
    should specifically impair long-horizon credit assignment (delayed outcomes)
    without degrading immediate evaluation (short-horizon rollouts).
    Mapping caveat: biological time cells encode real elapsed time (continuous);
    REE rollouts are discrete agent steps. The assumption that time cells fire
    at fixed intervals during E3 simulation is plausible but untested.
    V4 scope: requires the hippocampal module architectural work in SD-004.
    Registered 2026-03-30 from hippocampal function literature pull.
  evidence: []

- id: MECH-149
  title: "CA1 mismatch between E1-predicted and CA3-retrieved z_world gates hippocampal trajectory injection into E3 evaluation: novel situations (high CA1 mismatch) should trigger more frequent and diverse rollout proposals; familiar situations (low mismatch) should rely on cached viability estimates."
  claim_type: mechanism_hypothesis
  subject: hippocampus.ca1_mismatch_novelty_gate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  depends_on:
    - MECH-022
    - MECH-075
    - ARC-018
    - SD-004
  notes: >
    Motivated by Lisman & Grace (2005, Neuron; DOI 10.1016/j.neuron.2005.05.002):
    the hippocampal-VTA loop architecture proposes that CA1 detects novelty by
    comparing entorhinal (predicted/current) input to CA3-stored representations
    (match-mismatch). A novelty signal propagates subiculum -> accumbens ->
    ventral pallidum -> VTA, triggering dopamine release back to hippocampus
    that enhances LTP at recently active synapses (selective memory encoding).
    MECH-022 claims hippocampal systems act as hypothesis injectors -- generating
    candidate trajectories for E3 evaluation. This paper provides the biological
    gating mechanism: the CA1 mismatch signal (E1-predicted z_world vs CA3-
    retrieved z_world) should be the trigger for hippocampal proposal generation.
    Under familiar conditions (CA1 mismatch low), the existing residue-field map
    has dense coverage and cached rollout viability estimates are reliable --
    new trajectory proposals are unnecessary. Under novel conditions (CA1
    mismatch high), the map lacks coverage and E3 must evaluate fresh proposals.
    For MECH-075 (BG dopaminergic gain on hippocampal attractors): the VTA-to-
    hippocampus arm of the Lisman-Grace loop IS the dopaminergic modulation of
    hippocampal attractor dynamics -- VTA output via accumbens/pallidum is BG-
    adjacent, and dopamine modulates attractor basin stickiness (exploration
    vs exploitation balance in rollout sampling).
    Architectural requirement: the CA1 mismatch signal must be explicitly
    computed in the hippocampal module (E1 prediction error at the z_world
    level) and used to gate rollout injection frequency and diversity.
    High mismatch -> more proposals, higher diversity. Low mismatch -> fewer
    proposals, higher reliance on cached viability. This gate is not binary;
    it should scale continuously with mismatch magnitude.
    Mapping caveat: Lisman & Grace wrote before optogenetics; the specific
    pathway (subiculum-accumbens-pallidum-VTA) is inferred from pharmacological
    and lesion evidence, later confirmed but with additional complexity. REE
    has no literal LTP or subiculum; the mapping is functional (novelty -> gate
    -> selective update), not anatomical. V4 scope: requires SD-004 hippocampal
    architecture and a differentiable CA1 mismatch signal in the latent stack.
    Registered 2026-03-30 from hippocampal function literature pull.
  evidence: []

- id: INV-039
  title: "Schema-primed rapid assimilation: any hippocampal planning system with a stable prior map must gate episode consolidation rate by map stability -- new episodes entering a mature, coherent residue field should consolidate without the full multi-episode warmup required for a sparse or unstable map."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-007
    - ARC-038
  pending_substrate_reconfirmation: true
  subject: hippocampus.schema_primed_rapid_consolidation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  phase_locked: true  # intrinsic V4 scope: own notes "V4 scope: requires stable residue
                      # field map from sustained V3 training and ARC-039 offline replay"
                      # (ARC-039 confirmed v4/locked in this same reconciliation).
                      # ARC-120's reference is evidentiary, not a prerequisite. Set
                      # 2026-08-10 per chip chip-20260810-phase-consistency-reconciliation.
  depends_on:
    - ARC-038
    - ARC-039
    - ARC-007
  notes: >
    Motivated by Tse et al. (2007, Science; DOI 10.1126/science.1135935):
    when a neocortical schema (prior associative map of task structure) exists,
    new paired-associate memories become hippocampal-independent within 48 hours
    rather than weeks. One-trial learning produces lasting memories when the
    schema is in place; the schema causally accelerates consolidation. mPFC is
    required for schema expression (mPFC lesion destroys schema utility).
    REE implication: ARC-038 (waking immobility consolidation) and ARC-039
    (hippocampal-entorhinal offline consolidation) both treat consolidation rate
    as relatively fixed. Tse et al. show biologically that consolidation rate
    is dynamically gated by schema stability. The REE analog: E1 learning rate
    and offline consolidation window should be scaled by a residue-field map
    stability signal. A mature, stable residue field (dense, consistent harm/
    benefit topology across many episodes) should accelerate novel episode
    encoding -- new trajectories entering familiar terrain can be consolidated
    rapidly. A sparse or unstable map (early training, post-remapping) should
    revert to slow multi-episode consolidation.
    This is an invariant because it follows from the basic computational logic
    of associative structures: matching new information to existing structure
    is always faster than building structure from scratch. The claim is not
    contingent on REE-specific implementation; any planning system with a
    hippocampal-equivalent module should exhibit this property.
    Architectural requirement: the hippocampal module must expose a map-stability
    signal (e.g., residue-field coverage density, rollout viability consistency
    over recent episodes) to the consolidation controller. The mPFC role (Tse)
    maps to E3's control plane -- E3 must actively gate schema use, not merely
    receive hippocampal output passively.
    Mapping caveat: rat flavor-place paired-associates are simpler than REE's
    multi-step action-consequence terrain. The mechanism by which the neocortical
    schema detects match with new information is unspecified at circuit level
    in Tse et al.; this is a known gap. Replicated in Tse et al. 2011 and
    van Kesteren et al. human neuroimaging series. V4 scope: requires stable
    residue field map from sustained V3 training and ARC-039 offline replay.
    Registered 2026-03-30 from hippocampal function literature pull.
  evidence: []

- id: MECH-150
  title: "E1 ContextMemory, when queried with z_world alone (not the full [z_self, z_world] state), produces a cue-indexed association context that selectively activates stored associations relevant to the current sensory context."
  claim_type: mechanism_hypothesis
  subject: e1.cue_indexed_association_retrieval
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: "2026-08-13"
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-922_2026-08-13"
      as_of: "2026-08-13"
      verdict: "supports -- C1/C1b/C2 all PASS 3/3 seeds under GOV-FANOUT-1's recommended production config, resolving the stale 418-series/898 chain"
  implementation_phase: v3
  epistemic_category: standard
  pending_retest_after_substrate: false
  depends_on:
    - ARC-001
    - SD-005
  location: docs/architecture/frontal_cue_integration.md#section-2-the-e1-frontal-cue-indexing-mechanism
  notes: >
    The ContextMemory already implements soft-attention retrieval via query_proj +
    key_proj + value_proj. The standard use queries with the full [z_self, z_world]
    concatenated state (latent_dim=64). MECH-150 uses z_world alone as the query,
    isolating what sensory cues (not body state) retrieve from stored associations.
    The output cue_context [batch, latent_dim=64] encodes which stored associations
    are most strongly activated by the current exteroceptive context.
    Note: ContextMemory.read() returns [batch, latent_dim=64] (output_proj maps
    memory_dim=128 -> latent_dim=64). The z_world-only query requires a separate
    world_query_proj: Linear(world_dim=32, memory_dim=128) because the existing
    query_proj expects latent_dim=64 as input.
    Two projection heads convert cue_context: cue_action_proj (Linear(latent_dim=64,
    action_object_dim=16)) and cue_terrain_proj (Linear(latent_dim=64, 2) + sigmoid).
    Implementation: new method E1DeepPredictor.extract_cue_context(z_world) ->
    (action_bias, terrain_weight). Specified in SD-016.
    Biological analog: OFC/vmPFC pattern-completes from partial sensory cues
    (Murray & Izquierdo 2007 OFC reversal learning; Damasio 1994 somatic marker
    hypothesis) -- external sensory context activates stored affective/motivational
    associations independent of current body state.
    Prerequisite: SD-005 (z_world must be architecturally distinct from z_self
    for a z_world-only query to be meaningful).
    Registered 2026-03-31 from frontal cue-integration circuit design.
    FIDELITY GAP (2026-04-02): Biological hippocampal pattern completion is
    auto-associative (Hopfield-like) -- retrieving ANY element of a context
    reinstates ALL elements holistically (Horner et al. 2015, Staresina et al.
    2019). REE's ContextMemory uses query-key attention, which can retrieve
    partial matches and produce chimeric reconstructions. This is probably a
    fidelity gap rather than a feature. Prioritise fidelity during initial
    assembly; can be tested once architecture is working whether attention-based
    or auto-associative retrieval is superior for cue-indexed context in REE.
    SD-016 implementation status (2026-03-31): extract_cue_context() circuit is
    implemented and wired in E1DeepPredictor. However, EXQ-181b confirms that
    ContextMemory does NOT spontaneously produce differentiated cue content from
    unsupervised training alone (cosine_sim(mean_prior_A, mean_prior_B)=0.9999 with
    proper context-B sampling, 0/3 seeds pass C1). A supervised context-labeling
    training objective is required before ContextMemory can carry meaningfully
    differentiated cue content. This is a prerequisite for experimental evaluation
    of MECH-150's core claim. See EXQ-181b result.
  evidence_quality_note: >
    [SUPERSEDED HISTORY] EXQ-181 was degenerate (n_context_B=0 across all seeds --
    hazard field ambient everywhere with num_hazards=4, hazard-distal threshold never
    fires; C1 unmeasurable; marked superseded). EXQ-181b (num_hazards=1, proper
    sampling) obtained valid context separation and found cosine_sim=0.9999 (0/3
    seeds pass C1) and harm_r2=0.23 (1/3 seeds pass C2), attributed at the time to a
    missing supervised context-labeling training objective. That diagnosis was itself
    superseded (2026-06-05, V3-EXQ-418 series) by relocating the real bottleneck to
    the environment's cross-context z_world entropy, which produced the env-entropy
    precondition SD-016's queue entry gated on.

    [2026-08-13 governance, V3-EXQ-922, confirmed failure_autopsy_V3-EXQ-922_2026-08-13]:
    CONFIRMED. Production-combo run under GOV-FANOUT-1's recommended configuration
    (cue_slot_tagger=True, selection=gumbel, ctxdiv_weight=0.5) -- the env-entropy
    precondition is now satisfied and C1/C1b/C2 all PASS 3/3 seeds, resolving the
    stale 418-series/898 chain and the training-objective/env-entropy blocker history
    above. First-ever confirming evidence for this claim. epistemic_category standard.
    Status unchanged (candidate) -- a single confirming run is not yet promotion-grade,
    but the long-standing precondition blocker is now cleared.
  evidence: []

- id: MECH-151
  title: "The cue-indexed E1 context vector (MECH-150) projects to an action_bias signal that is added to E2.action_object() outputs, producing top-down contextual weighting of the action-affordance manifold prior to HippocampalModule search."
  claim_type: mechanism_hypothesis
  subject: e1_e2.cue_indexed_action_affordance_modulation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: "2026-08-13"
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-936_2026-08-18"
      as_of: "2026-08-21"
      verdict: "non_contributory -- GFLAG-0039 confirmed; V3-EXQ-922 supports withdrawn (sign test + no ranking site)"
  implementation_phase: v3
  epistemic_category: standard
  pending_retest_after_substrate: true  # SD-055 gradient-flow question remains open and untested by V3-EXQ-922 (see evidence_quality_note); a separate driver invoking the differentiable-CEM path is owed
  instantiates: SD-033c    # vmPFC-analog subdivision: MECH-151 is one of the existing mechanisms consolidated under SD-033c (see SD-033c.depends_on)
  depends_on:
    - MECH-150
    - SD-004
    - ARC-001
    - ARC-002
  location: docs/architecture/frontal_cue_integration.md#section-3-the-e1e2-action-modulation-pathway
  notes: >
    Current E2.action_object(z_world, action) produces o_t [action_object_dim] --
    the compressed world-effect of action. HippocampalModule navigates in this
    action-object space (SD-004). MECH-151 adds: o_t_biased = o_t + action_bias,
    where action_bias = E1.cue_action_proj(cue_context). HippocampalModule search
    is pre-shaped by this bias: action-objects consistent with the cue-activated
    associations are elevated, contextually inappropriate ones suppressed. This is
    top-down gating of the affordance manifold -- E1 biases which action-objects
    rank highly, but E3 still selects.
    Distinct from MECH-082 (top-down perceptual bias via E2 model distortion,
    which operates from E2 prediction error structure back through E1 attentional
    sampling). MECH-151 is the complementary direction: E1->E2 action biasing via
    cue-context projection.
    Distinct from MECH-116 (tonic goal conditioning on E1 LSTM): MECH-151 is
    cue-specific and z_world-indexed (changes with sensory context).
    Biological analog: vmPFC has direct projections to striatum (caudate/putamen)
    and premotor cortex (Haber & Behrens 2014 frontostriatal loops) -- not just
    evaluation input but action gate modulation.
    Registered 2026-03-31 from frontal cue-integration circuit design.
  evidence: []
  evidence_quality_note: >
    [2026-08-21 governance, gov-20260821-0203, confirmed failure_autopsy_V3-EXQ-936_2026-08-18.json]: [2026-08-18, failure_autopsy_V3-EXQ-936_2026-08-18, re-adjudication of V3-EXQ-922 for MECH-151 only; SUPERSEDES the MECH-151 disposition in failure_autopsy_V3-EXQ-922_2026-08-13]: GFLAG-0039 CONFIRMED and MECH-151's `supports` reading WITHDRAWN to non_contributory, on three grounds. (1) POWER: the entire support is ONE criterion, `action_bias_div_beats_off_control` -- a bare sign test, 2 of 3 seeds, with NO effect-size threshold. Under the null P(>=2 of 3) is exactly 0.5; the arm means overlap (A0 0.0305, A1 0.0509) and seed 42 inverts. (2) The criterion measures a pathway with NO RANKING SITE: GFLAG-0043's structural finding verified independently -- e3_selector.py contains ZERO references to action_object/action_bias, so MECH-151's asserted elevate/suppress ranking is UNIMPLEMENTED, not untrained; _action_bias_divergence (driver:523-527) is computed on extract_cue_context() output with no calls into HippocampalModule, E2 rollout or E3.select. (3) CORRECTION to GFLAG-0039's stated mechanism: manifest params and driver:429 record sd016_writepath_mode='off' in BOTH arms, so ContextMemory.write() was never called and 922 did NOT run against a 1-of-16-occupied bank -- implementing contextmemory-write-path-addressing-degeneracy does not retroactively rescue this run's reading. EXPLICITLY NOT CLAIMED (all three withdrawn on a full re-read of the 922 artifact and driver, after this autopsy first landed): that write-path-off left the slots untrained (they are a trainable nn.Parameter and 922 optimises agent.e1.parameters()); that the training objective GUARANTEES the DV (it is entailed by fitting, and the A0_OFF control runs the same training and is built for that route -- the driver's own lines 290-300 state this correctly); and any challenge to the 922 autopsy's REE-FAILED verdict for MECH-152/ARC-041, which rested on the first of those. This is NOT a demotion -- the demotion bar (tested fairly + biology supports the mechanism + still fails) is not met, because the claim was not tested fairly. Status unchanged (candidate); epistemic_category unchanged (standard); pending_retest_after_substrate stays true. READ-ACROSS, recorded not adjudicated: ARC-041's pre-registered dissociation signature is 'exactly one pathway survives' = MECH-151 supports / MECH-152 fails; withdrawing MECH-151's leg leaves the dissociation UNESTABLISHED (not refuted), so the 2026-08-13 HOLD on ARC-041 was correct and now has a second independent reason. Keep ARC-041 HELD and keep its epistemic_category substrate_conditional; this autopsy recommends no ARC-041 write.

    [2026-08-13 governance, V3-EXQ-922, confirmed failure_autopsy_V3-EXQ-922_2026-08-13,
    first-ever experimental evidence entry for this claim]: SUPPORTS the narrow
    context-conditioning mechanism (2/3 seeds) under GOV-FANOUT-1's recommended
    production config (cue_slot_tagger=True, selection=gumbel, ctxdiv_weight=0.5).
    RESOLVES the inherited env-entropy precondition this claim shared with MECH-150
    (SD-016's parked_pending_env_entropy_precondition blocker is now cleared). DOES
    NOT resolve the separate SD-055 gradient-flow question: this driver never invokes
    the differentiable-CEM path (use_differentiable_cem, opt-in since 2026-05-15,
    behavioural validation still deferred), so the dead-gradient defect
    failure_autopsy_V3-EXQ-640a_2026-06-06 found under DEFAULT settings
    (mean_cue_action_bias_norm NULL in all 6 cells) remains untested by this run --
    the support above is scoped explicitly to exclude SD-055's gradient-path question.
    epistemic_category standard. Status unchanged (candidate); a separate driver
    invoking the differentiable-CEM path is still owed before the gradient-flow
    question can be considered resolved.

- id: MECH-152
  title: "The cue-indexed E1 context vector (MECH-150) projects to a terrain_weight signal [w_harm, w_goal] that scales E3's harm and goal scoring, implementing contextual precision modulation on the harm/benefit terrain evaluation."
  claim_type: mechanism_hypothesis
  subject: e1_e3.cue_indexed_terrain_precision_modulation
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: "2026-08-13"
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-922_2026-08-13"
      as_of: "2026-08-13"
      verdict: "does_not_support -- clean FAIL under strict production-arm scoring (0/3 both criteria); legacy soft-selection arm (A0_OFF) was much closer to threshold, a live lead pointing at selection-hardness interaction rather than mechanism absence"
  status_note: |
    Promoted candidate -> provisional 2026-04-03.
    Evidence: conf=0.773, conflict_ratio=0, 2 exp supports, 2 lit entries.
    EXQ-194a PASS (2026-04-01): direct terrain pathway with phased training.
    0 conflicting experiments. Note: evidence_quality_note was empty at promotion -- 2 experiments
    is a thin base; evidence_quality_note should be updated when additional experiments run.
  implementation_phase: v3
  epistemic_category: standard
  pending_retest_after_substrate: true  # soft-selection+ctxdiv-ON ablation owed (SD-016 amend, failure_autopsy_V3-EXQ-922_2026-08-13) to discriminate selection-hardness interaction from mechanism absence before further disposition
  instantiates: SD-033c    # vmPFC-analog subdivision: MECH-152 is one of the existing mechanisms consolidated under SD-033c (see SD-033c.depends_on)
  depends_on:
    - MECH-150
    - ARC-016
    - SD-010
    - ARC-035
  location: docs/architecture/frontal_cue_integration.md#section-4-the-e1e3-terrain-precision-modulation-pathway
  notes: >
    Current E3 dynamic precision (ARC-016) is a single scalar derived from E3's
    own prediction error variance (running_variance EMA). It controls the commit
    threshold but does not distinguish harm precision from goal precision, and does
    not use sensory context. MECH-152 adds a context-specific multiplicative factor:
    harm_score_contextual = harm_score * w_harm; goal_score_contextual = goal_score
    * w_goal, where [w_harm, w_goal] = sigmoid(E1.cue_terrain_proj(cue_context)).
    Applied before trajectory scoring aggregation in E3.score_trajectory().
    Example: hazard-gradient context -> w_harm > 0.8, w_goal < 0.5 (harm avoidance
    focus); resource-proximate context -> w_goal > 0.8, w_harm < 0.5 (approach
    mode).
    Relationship to ARC-016: they compose multiplicatively -- effective_harm_precision
    = (1/running_variance) * w_harm. ARC-016 is temporal (recent prediction error
    variance); MECH-152 is contextual (current sensory cue association).
    EXPLICIT LINKAGE (2026-04-02): Gain control literature (Kanashiro et al. 2017)
    shows that gain modulation also controls signal-to-noise ratio, not just magnitude.
    If terrain_weight operates as gain control, high w_harm should make harm evaluation
    more precise (lower variability), not just louder. This means MECH-152 and ARC-016
    may be the same mechanism at different levels of description -- ARC-016 is the
    temporal gain (recent prediction error variance), MECH-152 is the contextual gain
    (sensory cue association). Both are subsumed by ARC-044 (context-dependent
    neuromodulatory gain). Should be tested together: does w_harm modulation
    improve E3 harm evaluation precision (lower variability) or just scale magnitude?
    Biological: vmPFC lesion produces flat w_harm = w_goal regardless of sensory
    context -- the Iowa Gambling Task deficit is specifically contextual precision
    scaling failure (Bechara et al. 1994), not absence of harm knowledge.
    Registered 2026-03-31 from frontal cue-integration circuit design.
  evidence_quality_note: >
    [2026-08-21 governance, gov-20260821-0203, GFLAG-0044]: HOLD demote_to_candidate. Owed work is a measurement redesign gated on the corrupting write-path (contextmemory-write-path-addressing-degeneracy still implemented_pending_validation; V3-EXQ-943 occupancy PASS is not addressing validation). Status stays provisional.

    EXQ-194 MIXED (2026-04-01): direct terrain pathway -- C1 PASS 3/3 (r_w_harm=0.70,
    harm modulation works) but C2 FAIL (r_w_goal=-0.007, w_goal constant). EXQ-194a
    (phased training) also ran. Hold at candidate: harm pathway validated but goal
    pathway not yet functional. Promote only after w_goal validation. (governance 2026-04-01)

    [2026-08-13 governance, V3-EXQ-922, confirmed failure_autopsy_V3-EXQ-922_2026-08-13]:
    does_not_support. Production-combo run under GOV-FANOUT-1's recommended configuration
    (cue_slot_tagger=True, selection=gumbel, ctxdiv_weight=0.5, strict production-arm
    scoring) -- MECH-152 FAILS cleanly (0/3 both criteria: r_w_harm_mean=0.0013 vs
    threshold >0.5; r_w_goal_mean=-0.0077 vs threshold <-0.3), materially worse than
    194a's own MIXED result. But the A0_OFF (legacy soft-selection) arm's numbers were
    much closer to threshold (r_w_harm_mean=0.356, r_w_goal_mean=-0.355) than
    A1_PRODUCTION's -- a real interpretive lead pointing at a selection-hardness
    interaction (hard-Gumbel selection suppressing the terrain-precision signal) rather
    than outright mechanism absence. Provisional (unchanged); the soft-selection +
    ctxdiv-ON ablation this note flags as owed is the discriminator between those two
    readings and should run before any further disposition change. epistemic_category
    standard.

    [/governance 2026-08-16, DEMOTION RECOMMENDATION HELD -- the owed ablation HAS RUN but is not
    yet adjudicable]: the automated queue again recommends demote_to_candidate (conflict_ratio
    0.667, exp_conf 0.315) on the SAME single experimental entry (V3-EXQ-922, weakens) that the
    2026-08-13 cycle already declined to demote on. The discriminating ablation that note flagged as
    owed -- soft-selection + ctxdiv-ON -- has since RUN: V3-EXQ-922a
    (v3_exq_922a_sd016_mech152_softsel_ablation_20260814T183708Z_v3), PASS, self-routing
    selection_hardness_partial_recovery, which is on its face the A0_OFF-closer-to-threshold reading
    the 922 autopsy identified as the live lead rather than mechanism absence. It is NOT yet
    scored into claim_evidence (latest indexed entry is still 922, 2026-08-12) and, being
    experiment_purpose diagnostic, it requires a CONFIRMED /diagnostic-autopsy before it may drive
    any governance action -- it is in this cycle's pending set with no autopsy. So the hold
    condition is discharged in FACT but not yet in EVIDENCE, and demoting now would apply a
    conflict ratio computed over exactly the one run the successor was designed to reinterpret.
    HELD at provisional. Route: V3-EXQ-922a to /failure-autopsy (accepts a PASS target); re-run
    this disposition once confirmed. PROMOTES NOTHING.

    [2026-08-16 governance apply | V3-EXQ-922a -> MECH-152 | failure_autopsy_V3-EXQ-922a_2026-08-16] DISPOSITION: evidence_direction non_contributory. DO NOT APPLY the 2026-08-16 HELD demote_to_candidate. 922a neither rescues nor weakens MECH-152: its 'partial recovery' is below A2's own across-seed SD, is a regression against this run's own control, and occurs at a terrain_weight modulation depth 2-6 orders of magnitude below the 0.6 swing the claim asserts. Decisively, 922a shows the Pearson-r DV behind conflict_ratio 0.667 ranks the cue-indexing-OFF control HIGHEST and the fully-selective production arm LOWEST -- incompatible with it measuring a cue-indexed pathway -- and the same DV and machinery produced EXQ-194's supporting r_w_harm=0.70, so the supporting evidence is disqualified symmetrically with the conflicting entry. Governance action: refresh evidence_quality_note with this adjudication (text supplied), RETAIN pending_retest_after_substrate but re-scope it in the note to a MEASUREMENT redesign rather than a substrate build (no substrate work is owed), record that the automated demotion should stop being re-proposed on the same single non-diagnostic entry, keep status provisional, and keep epistemic_category -> standard

    [2026-08-16 /failure-autopsy, V3-EXQ-922a, awaiting confirmation]: non_contributory. The owed soft-selection ablation ran and its self-routed selection_hardness_partial_recovery does NOT survive adjudication. A2_SOFTSEL fails both 194a criteria (C1 0/3, C2 1/3); the A2-vs-A1 'recovery' (harm +0.184, goal +0.174) is smaller than A2's own across-seed SD (0.193 / 0.193) on n=3 and reaches only 37% / 60% of the bar. Decisively, the run's tagger-OFF control A0_OFF -- with no cue-indexing and sel_context_divergence 0.000117 -- scores HIGHEST (r_w_harm 0.356, r_w_goal -0.355, clearing C2 on 2/3 seeds), so the DV is monotonically DECREASING in cue-indexed retrieval selectivity across all three arms, which is incompatible with it measuring a cue-indexed pathway. Cause, from the manifest's own unscored readouts: terrain_weight std is 3.8e-7 to 1.0e-2 against the claim's asserted 0.6 swing (2 to 6 orders of magnitude low), the non-degeneracy floor is 1e-9 and therefore ~6 orders too permissive to catch it, and final_terrain_loss (1.0e-5 to 7.2e-3 against targets in {0.2,0.8}) bounds the terrain-target switch rate at <=2% of collection steps -- the terrain head faced a near-constant target and had no pressure to modulate. Pearson r is scale-invariant and cannot adjudicate a magnitude claim. THIS CUTS BOTH WAYS: EXQ-194's r_w_harm=0.70 (the promotion basis) and V3-EXQ-922's does_not_support come from the same DV and the same machinery, so the single conflicting entry driving conflict_ratio 0.667 is not diagnostic either. The 2026-08-16 HELD demotion should NOT be applied -- not because 922a rescues MECH-152 (it does not), but because MECH-152 has never been tested at the magnitude it asserts, in either direction. Route: /queue-experiment, NEW EXQ number -- score modulation DEPTH (between-context mean difference against the claim's own w_harm>0.8 / w_goal<0.5 band), gate on a terrain_target_balance precondition (both target states >=25% of steps), raise the non-degeneracy floor to ~0.05, score the OFF control on the mechanism criteria, and add the precision-gain-vs-magnitude-scaling discriminator this claim's own what_would_answer has named since 2026-04-02 and which no run in the 194/194a/922/922a family has ever measured. pending_retest_after_substrate retained but re-scoped: what is owed is a measurement redesign, not a substrate build. epistemic_category standard. PROMOTES NOTHING and DEMOTES NOTHING.
  what_would_answer: |
    CURRENT EVIDENCE STATE: MIXED (EXQ-194/194a, 2026-04-01) -- the HARM
    pathway is validated (C1 PASS 3/3, r_w_harm=0.70: harm-context
    modulation works as specified) but the GOAL pathway is NOT (C2 FAIL,
    r_w_goal=-0.007: w_goal stays constant regardless of context). Held
    at provisional on the harm-pathway strength alone; promotion requires
    w_goal validation.
    NON-DEGENERACY PRECONDITION: requires resource-proximate contexts to
    be genuinely distinguishable from hazard-gradient contexts in the
    training distribution, with non-zero variance in E1's
    cue_terrain_proj output across contexts -- w_goal's current flatness
    could reflect either a genuine mechanism absence OR insufficiently
    varied goal-context training data; this wwa does not resolve which.
    CONFIRMING signature: with adequate goal-context variation, w_goal
    becomes measurably context-sensitive (r_w_goal comparable in
    magnitude to r_w_harm=0.70) -- resource-proximate contexts drive
    w_goal > 0.8, hazard-gradient contexts drive w_goal < 0.5, mirroring
    the already-validated harm pathway. Separately, per the 2026-04-02
    gain-control linkage: if terrain_weight is genuine PRECISION gain
    (not just magnitude scaling), high w_harm should measurably REDUCE
    harm-evaluation variability, not merely scale its mean -- the
    discriminator between MECH-152-as-precision-gain and
    MECH-152-as-simple-magnitude-scaling, and also for whether MECH-152
    and ARC-016 (temporal gain) are the SAME mechanism at different
    levels of description (both subsumed by ARC-044).
    FALSIFYING: w_goal remains flat even with adequate goal-context
    variation (the goal pathway is genuinely absent, not merely
    undertrained) -- falsifies the symmetric harm/goal contextual-
    precision framing, leaving MECH-152 a harm-only mechanism,
    contradicting its own title; or w_harm modulation scales magnitude
    without reducing variability -- falsifies the precision-gain
    reading in favour of simple magnitude scaling, and would NOT support
    collapsing MECH-152 into ARC-016 (specifically about precision/
    variance, not magnitude).
  evidence: []

- id: ARC-041
  title: "The frontal cue-weighting circuit -- E1 cue-indexed retrieval (MECH-150) feeding dual pathways to E2 action biasing (MECH-151) and E3 terrain precision scaling (MECH-152) -- completes the stored-to-active pathway specified in ARC-035 by providing the retrieval mechanism and dual downstream output architecture."
  claim_type: architectural_commitment
  subject: architecture.frontal_cue_weighting_circuit
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: "2026-08-13"
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-922_2026-08-13"
      as_of: "2026-08-13"
      verdict: "mixed -- pre-registered dual-pathway dissociation signature (exactly one pathway surviving) is technically met (MECH-151 supports / MECH-152 does_not_support), but the disposition is HELD, not finalized, pending MECH-152's own owed ablation"
  implementation_phase: v3
  pending_retest_after_substrate: true  # HELD -- see evidence_quality_note; re-examine once MECH-152's soft-selection+ctxdiv-ON ablation resolves
  depends_on:
    - MECH-150
    - MECH-151
    - MECH-152
    - MECH-160
    - ARC-035
    - ARC-001
    - ARC-002
    - ARC-003
    - SD-005
  epistemic_category: substrate_conditional  # left UNCHANGED 2026-08-13 -- disposition not finalized (HELD), do not downgrade; see evidence_quality_note
  what_would_answer: |
    RELATIONSHIP TO SD-016: ARC-041 and SD-016 describe the identical
    physical mechanism (E1 ContextMemory queried with z_world alone ->
    cue_context -> MECH-151 action-bias output + MECH-152 terrain-weight
    output) but at different claim altitudes and are NOT a near-duplicate
    pair. SD-016 (claim_type design_decision, status implemented) is the
    IMPLEMENTATION decision for the query mechanism itself
    (E1DeepPredictor.extract_cue_context); ARC-041 (claim_type
    architectural_commitment) is the higher-level claim that THIS CIRCUIT
    SHAPE -- one retrieval feeding two structurally and biologically
    distinct downstream pathways -- is the correct way to complete
    ARC-035's stored-to-active pathway. SD-016 is upstream infrastructure
    ARC-041 depends on, not a restatement of ARC-041.

    DO NOT RE-DERIVE THE RETRIEVAL-MECHANISM QUESTION: SD-016's own
    what_would_answer (this session) already covers the shared substrate
    question in full -- its leg (A) RETRIEVAL-SELECTIVITY, answered
    negatively for the originally-proposed remedy, with a named still-open
    re-test path; and leg (B) DOWNSTREAM EXPLOITATION, untested and gated
    on cue_action_proj consumer-wiring fix + SD-055 differentiable-CEM
    behavioural validation. ARC-041's own falsifier is downstream of and
    conditional on both legs of SD-016 clearing -- see SD-016's
    what_would_answer for the acceptance criteria on the shared mechanism.

    RESIDUAL CONTENT ARC-041 ASSERTS BEYOND SD-016 -- THE DUAL-PATHWAY
    STRUCTURE: SD-016's leg (B) tests whether "action_bias/terrain_weight
    get used behaviourally by E2/E3" as a single combined question.
    ARC-041's distinctive claim is that this is genuinely a DUAL-OUTPUT
    circuit -- cue_context reaching BOTH pathways, not just one -- mapping
    onto the dual biological role of OFC (action gating) and vmPFC (value
    weighting). MECH-152 (E3 terrain-weight leg) already has its own
    independent partial evidence unrelated to SD-016's retrieval question:
    EXQ-194/194a (2026-04-01) found the harm sub-pathway works (C1 PASS
    3/3, r_w_harm=0.70) under phased/direct training that bypassed the
    z_world cue-indexed retrieval question entirely, while the goal
    sub-pathway does not (C2 FAIL, r_w_goal=-0.007). MECH-151 (E2
    action-bias leg) by contrast has NO experimental evidence of its own
    beyond the shared SD-016 blockers PLUS its own additional gate:
    cue_action_proj's dead-gradient defect (SD-055, partially fixed
    2026-05-15 but opt-in, behavioural validation still deferred) --
    confirmed by V3-EXQ-640a: mean_cue_action_bias_norm NULL in all 6
    cells under default settings.

    CONFIRMING signature for ARC-041 (once SD-016 leg A retrieval-
    selectivity clears AND SD-055/cue_action_proj consumer fixes land):
    a discriminative test shows BOTH pathways independently tracking
    retrieval content -- action_bias_div correlates with cue-context
    identity (E2 leg) AND w_harm/w_goal correlate with cue-context
    identity (E3 leg) -- run together in the same experiment so the
    dissociation is directly observable.

    FALSIFYING ARC-041 SPECIFICALLY (as distinct from falsifying SD-016
    or either sub-mechanism alone): once both legs of SD-016 are cleared
    and cue_action_proj/SD-055 gates are satisfied, only ONE of the two
    pathways shows content-tracking behavioural divergence while the
    other remains flat -- this falsifies the DUAL-pathway architecture
    specifically, even though it would leave the surviving single
    pathway's own mechanism claim (MECH-151 or MECH-152) intact.

    MECH-160 DIFFERENTIAL-UPDATE-RATES SUB-CLAIM -- OUT OF SCOPE HERE:
    ARC-041's notes flag that E2 action_bias and E3 terrain_weight
    "should have different update rates," citing MECH-160. MECH-160 is
    its own un-evidenced candidate claim (evidence: [], no
    what_would_answer) proposing a REVISION to ARC-041's current design.
    This is a refinement layered on top of ARC-041, not a component of
    ARC-041's own falsification.

    NON-DEGENERACY PRECONDITION: same as SD-016's -- selection entropy
    pinned at the uniform ceiling is not evidence against ARC-041, it is
    the signature that cue_context itself carries no differentiated
    content, so neither downstream pathway has anything to diverge on.
  location: docs/architecture/frontal_cue_integration.md
  notes: >
    ARC-035 (vmPFC.md) specifies that stored affective/normative content must
    become active in the navigable state at trajectory evaluation time, but does
    not specify the retrieval mechanism or the E2-facing output pathway. ARC-041
    specifies the circuit architecture: E1 ContextMemory is queried with z_world,
    producing cue_context that feeds BOTH E2 (action affordance bias via MECH-151)
    and E3 (terrain precision scaling via MECH-152). This is a dual-output frontal
    circuit mapping onto the dual role of OFC/vmPFC: (1) action gating via
    orbitofrontal to basal ganglia action selection, and (2) value weighting via
    vmPFC to amygdala/hippocampus terrain scoring.
    ARC-041 complements SD-002 (E1 terrain_prior -> HippocampalModule): SD-002 is
    the BULK prior pathway (context-agnostic); ARC-041 is the CUE-SPECIFIC
    modulation pathway (context-specific). They are additive and independent.
    V3 implementation: ~5394 new E1 params (world_query_proj + 2 linear projection
    heads), wiring changes in agent.py. Backward-compat flag sd016_enabled=False
    preserves existing experiment behavior.
    Registered 2026-03-31 from frontal cue-integration circuit design.
    DIFFERENTIAL UPDATE RATES (2026-04-02): Literature (Rudebeck & Murray 2024)
    shows OFC updates rapidly (single-trial reversal) while vmPFC integrates over
    longer timescales. The E2 action_bias pathway (MECH-151, OFC analog) and E3
    terrain_weight pathway (MECH-152, vmPFC analog) should have different update
    rates, not simultaneous output from a single retrieval step. This may share
    an underlying reason with the E1/E2 developmental training differential vs E3
    (ARC-042 staged development). See MECH-160 for the specific rate asymmetry claim.
  evidence_quality_note: >
    [2026-08-13 governance, V3-EXQ-922, confirmed failure_autopsy_V3-EXQ-922_2026-08-13]: HELD,
    not finalized. V3-EXQ-922's per-arm results technically meet this claim's own pre-registered
    dissociation signature (exactly one pathway surviving): MECH-151 SUPPORTS the narrow
    context-conditioning mechanism (2/3 seeds) while MECH-152 does_not_support (clean FAIL 0/3
    under strict production-arm scoring). The user explicitly DECLINED to finalize this as the
    dual-pathway falsification during the autopsy (chose "Hold ARC-041's falsification" over the
    recommended "Accept the split routing"), because the dissociation is driven entirely by
    MECH-152's result, and MECH-152's own disposition is itself gated on a queued soft-selection +
    ctxdiv-ON ablation that discriminates a selection-hardness interaction from genuine mechanism
    absence -- see MECH-152's own evidence_quality_note. epistemic_category left UNCHANGED at
    substrate_conditional (not downgraded to standard) precisely because disposition is not final.
    Status unchanged (candidate). Re-examine once the MECH-152 ablation resolves: if MECH-152's
    FAIL survives under soft selection too, the dual-pathway falsification signature becomes live;
    if the ablation recovers MECH-152's signal, ARC-041's dual-pathway architecture stands
    supported on both legs.
  evidence: []

- id: INV-040
  title: "A minimal sensory cue pattern in z_world is sufficient to activate the appropriate harm/goal terrain precision configuration via E1 cue-indexed retrieval -- the full accumulation of z_harm_a is not required for terrain preparation to begin."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-041
    - SD-005
  pending_substrate_reconfirmation: true
  subject: ethics.sensory_cue_sufficiency_for_terrain_activation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  depends_on:
    - ARC-041
    - MECH-150
    - MECH-152
    - SD-005
  location: docs/architecture/frontal_cue_integration.md#section-8-new-claim-ids
  notes: >
    This invariant captures the forward-directionality of the frontal
    cue-integration circuit: partial sensory cues (e.g., seeing a hazard gradient
    field in world_obs before reaching the hazard) should be sufficient to activate
    harm-relevant E1 associations and raise w_harm via MECH-152, even before
    z_harm_a has accumulated (EMA accumulation takes 10-30 steps at tau ~20). This
    is why the ContextMemory query uses z_world only (not full state): terrain
    preparation must begin from sensory evidence alone.
    Biological grounding: conditioned fear (LeDoux 1996 -- partial CS activates
    fear state before UCS); OFC extinction context (Milad & Quirk 2012 -- safety
    context activates safety memory from minimal cues); Iowa Gambling Task pre-SCR
    (Bechara et al. 1997 -- anticipatory skin conductance response from visual cue
    context before card selection, not after harm).
    REE implication: if w_harm is not elevated by hazard-field cues until the agent
    is already adjacent to hazard (z_harm_a elevated), the system fails to prepare
    early -- the precise failure mode of vmPFC lesion patients.
    Testable prediction: E3 terrain_weight w_harm should be elevated in episodes
    where hazard gradient cues are present in z_world BEFORE z_harm_a accumulates.
    Registered 2026-03-31 from frontal cue-integration circuit design.
  evidence: []

- id: ARC-042
  title: "E3's ethical selection machinery is architecturally present from initialisation but functionally dark until E1 and E2 substrate development is complete; REE requires staged developmental phases before ethical selection can operate on real signal."
  claim_type: architectural_commitment
  subject: architecture.staged_developmental_phases
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-042@2026-04-03T22:00:00Z"
      as_of: 2026-04-03
      verdict: hold_candidate_resolve_conflict/applied
  implementation_phase: v3
  depends_on:
    - ARC-001
    - ARC-002
    - ARC-003
    - ARC-041
    - MECH-150
    - MECH-151
    - MECH-152
    - SD-012
    - SD-016
  location: docs/architecture/vmPFC.md
  notes: >
    REE's architectural components (E1, E2, E3, ContextMemory, terrain weighting)
    can all be instantiated at initialisation, but the ethical selection chain
    is functionally gated by substrate development in a strict dependency order.

    Phase 1 -- E1 substrate development: E1 ContextMemory must be trained with a
    supervised context-labeling objective before it can produce differentiated
    cue-indexed representations. EXQ-181b confirms that unsupervised experience
    alone is insufficient (cosine_sim(hazard-proximate, hazard-distal)=0.9999;
    0/3 seeds pass C1). Without differentiated ContextMemory, MECH-150 produces
    near-identical outputs across all sensory contexts, and the ARC-041 dual
    pathways (MECH-151, MECH-152) carry no signal.

    Phase 2 -- E2 substrate development: E2 trajectory generation will only
    produce meaningfully action-conditional outputs once E1 provides informative
    priors. The action_bias (MECH-151) is near-zero when ContextMemory is
    undifferentiated, leaving E2 to generate context-blind affordance proposals.
    The E1 LSTM bulk prior (SD-002 pathway) is similarly uninformative until
    E1 has been exposed to sufficient structured environment.

    Phase 3 -- E3 ethical selection: E3's terrain weighting and harm/goal
    precision scaling (MECH-152, ARC-016) can only amplify signal that exists.
    With undifferentiated terrain_weight [w_harm, w_goal] from an untrained
    MECH-152 circuit, E3 ethical selection operates as uniform scoring --
    architecturally present but not functionally discriminating.

    This staged dependency is not a design deficiency but an architectural
    feature mirroring biological neurodevelopment: vmPFC/OFC frontal cue
    integration circuits continue myelinating and forming stable connectivity
    into the mid-20s. Iowa Gambling Task data shows adolescents can verbally
    identify harmful decks (stored knowledge intact -- E3 can reason) but the
    cue -> anticipatory SCR -> action bias pathway (the biological analog of
    ARC-041) is not yet reliably driving behaviour. The anticipatory SCR appears
    in adults BEFORE deck selection; in adolescents it appears concurrently or
    after -- the circuit is wired earlier but the training regime has not yet
    established the cue-value associations that make it discriminating
    (Bechara et al. 2000 developmental IGT data).

    Implication: experiments measuring E3 ethical selection performance before
    Phase 1 (supervised ContextMemory training) is complete will observe
    near-chance ethical selection not because E3 is broken but because the
    input signal is absent. EXQ-181b is exactly this failure mode: the circuit
    was wired (SD-016 complete) but the training regime had not been applied.
    Registered 2026-03-31 from EXQ-181b result and developmental staging analysis.
  evidence:
    - run_id: v3_exq_181b_sd016_context_separation_fix_20260331T131040Z_v3
  evidence_quality_note: >
    Governance 2026-04-03: hold_candidate_resolve_conflict applied. conf=0.55,
    conflict_ratio=0.667, 2 exp entries, 2 lit entries.
    EXQ-187a FAIL (2026-04-01): supervised terrain loss completely failed to differentiate
    contexts (cosine_sim=1.0, same as EXQ-181b baseline). Flagged for /diagnose-errors in
    a separate session -- evidence_direction="does_not_support" is non-standard and needs
    root-cause analysis before weighting. Hold at candidate; conflict resolution required.
    EXQ-211 PARTIAL (2026-04-03, per-claim=weakens): ARC-042 specific component FAIL.
    E3 harm_eval_diff identical in supervised vs unsupervised conditions (0.000187 both).
    E3's ethical selection machinery does not differentiate contexts at this training level --
    consistent with ARC-042 staged developmental prediction (E3 requires Phase 1+2 substrate
    before ethical selection is discriminating). However this is expected behavior per ARC-042
    itself, not a refutation. The weakening evidence for ARC-042 reflects that the claim
    requires richer prerequisite training before the staged gate produces measurable E3 signal.
    Hold at candidate.
    EXQ-187a DIAGNOSIS (2026-04-05, cowork-2026-04-05-a): evidence_direction corrected from
    non-standard "does_not_support" to non_contributory for ARC-042 / inconclusive for
    MECH-153. EXQ-187a IS the bug-fix for EXQ-187 (context_memory.write() correctly applied).
    Cosine_sim=1.0 is expected by ARC-042's own staged-developmental logic -- Phase 1 gate
    (supervised ContextMemory) not crossed, so undifferentiated representations are predicted
    by the claim, not a refutation. Non_contributory for ARC-042 (expected behavior per claim).

- id: MECH-153
  title: "E1 ContextMemory requires a supervised context-labeling training objective -- not unsupervised experience alone -- to produce differentiated cue-indexed representations; without this objective, hazard-proximate and hazard-distal context vectors remain near-identical (cosine_sim approximately 1.0), leaving the MECH-150 retrieval pathway functionally silent."
  claim_type: mechanism_hypothesis
  subject: e1.context_memory_supervised_training_requirement
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-153@2026-04-04T08:57:00.483957Z"
      as_of: 2026-04-04
      verdict: hold_candidate_resolve_conflict/applied
  implementation_phase: v3
  depends_on:
    - MECH-150
    - SD-016
    - ARC-041
  notes: >
    EXQ-181b result (2026-03-31): with proper context-B sampling (num_hazards=1,
    genuine spatial separation between hazard-proximate and hazard-distal contexts),
    cosine_sim(mean_prior_A, mean_prior_B) = 0.9999 across 0/3 seeds. This
    confirms that ContextMemory -- trained only via implicit error signals from
    the E1 LSTM world-model objective -- does not develop differentiated context
    representations. The world-model objective rewards accurate state prediction;
    it does not reward discriminating between hazard-proximate and hazard-distal
    states, which can look similar in z_world except for the hazard-gradient
    component. Without a gradient signal that specifically rewards this
    distinction, the memory system fails to encode it.

    Required training objective: a supervised context-labeling signal in which
    the hazard-field label (binary: hazard-proximate / hazard-distal, derived
    from hazard_gradient_view.max() threshold) is regressed or classified from
    the ContextMemory output. This provides the gradient that drives the memory
    system to encode the relevant distinction into stored associations.
    SD-016 specification already includes a supervised proxy for terrain_weight:
    hazard_field_view.max() context label (see sd_016_frontal_cue_integration.md,
    training signals section).

    Biological analog: OFC/vmPFC representation of outcome value and context
    is not acquired from pure prediction error alone -- outcome labeling and
    reversal training signals are required to establish cue-value associations
    (Murray & Izquierdo 2007 OFC reversal; Bechara 2004 somatic marker
    hypothesis). Patients with intact world knowledge but impaired vmPFC
    (patient EVR; Eslinger & Damasio 1985) demonstrate that prediction error
    alone cannot substitute for the value-labeling circuit.

    Prerequisite status: MECH-153 is a prerequisite for MECH-150 evaluation,
    not a refutation of MECH-150. The retrieval mechanism (MECH-150) can only
    be tested once the training objective produces differentiated memory content.
    This is an implementation precondition, not a claim that MECH-150 is false.
    Registered 2026-03-31 from EXQ-181b result.
  evidence_quality_note: >
    EXQ-181b (num_hazards=1, proper context-B sampling): cosine_sim=0.9999
    (C1 FAIL, 0/3 seeds pass criterion), harm_r2=0.23 (C2 FAIL, 1/3 seeds pass).
    This is the definitive diagnostic for the supervised training prerequisite.
    EXQ-181 was degenerate (n_context_B=0, num_hazards=4 makes hazard ambient;
    marked superseded). EXQ-181b supersedes EXQ-181 as the valid diagnostic.
    Pre-experimental prerequisite identified -- MECH-150 evaluation blocked until
    supervised context-labeling training is implemented and validated.
    EXQ-211 PARTIAL/mixed (2026-04-03): supervised labeling discriminative pair PARTIAL.
    cosim_supervised=0.997 (C1 PASS: reduced vs unsupervised=0.9999, delta=0.003). But
    C2 FAIL (cosim still 0.997 >> absolute threshold 0.9). C3 FAIL (E3 harm sensitivity
    identical in supervised vs unsupervised conditions). Supervised training marginally
    reduces cosim but not enough for full discrimination (still near-degenerate).
    Consistent with MECH-153 claim: supervised labeling helps but requires more training
    signal or stronger supervision. Mixed evidence -- direction confirmed but magnitude
    insufficient. Hold at candidate.
    EXQ-187a DIAGNOSIS (2026-04-05, cowork-2026-04-05-a): evidence_direction corrected from
    non-standard "does_not_support" to inconclusive. The fix (context_memory.write()) was
    correctly applied; cosine_sim=1.0 reflects insufficient training signal (150 episodes,
    lambda=0.1) rather than a code failure. MECH-153 claims supervision is NECESSARY; this
    experiment found a specific supervised config INSUFFICIENT -- insufficient supervision
    failing to differentiate is inconclusive for the necessity claim (does not prove
    supervision is unnecessary, nor sufficient). See EXQ-211 for partial replication.
    EXQ-239 FAIL (2026-04-05, supersedes EXQ-187a): lambda=0.5, warmup=500ep -- cosine_sim=1.0
    in both supervised and ablated conditions. Supervised head learns terrain classification but
    latent representations don't differentiate. Weakens claim but substrate-confounded: ablated
    baseline also shows cosine_sim=1.0, meaning context_memory infrastructure may be too immature
    for either pathway. A deeper analysis of the attribution pipeline's expected mathematical
    functions is needed to design better tests.
    GOVERNANCE META (2026-04-06): Weakens classification genuine but substrate-confounded.
    cosine_sim=1.0 in both conditions means context_memory infrastructure too immature for
    either pathway. A first-principles analysis of the attribution pipeline's expected
    mathematical functions is needed before redesigning the test. Status: needs_redesign
    (pipeline mathematical analysis required).

    [2026-07-29 governance, V3-EXQ-832 supports; PASS, load-bearing criterion clean]: C1
    (childhood_exposure_necessary, load_bearing) passes -- the childhood (balanced/forced)
    exposure regime differentiates ContextMemory while the avoidance-shaped regime does not,
    despite identical supervised labeling. C2 (labeling_beyond_exposure, non-load-bearing) also
    passes. Confirms committed exposure is a necessary prerequisite for differentiation, not
    merely the supervised-labeling objective this claim's earlier evidence chain investigated --
    resolves the "insufficient supervision" ambiguity carried since EXQ-187a/211/239 by isolating
    the EXPOSURE-REGIME axis as load-bearing rather than only the supervision-strength axis. Also
    supports INV-041 (see that entry). Note for the pending_user promotion/demotion agenda: this
    is the first evidence since the 2026-04-04 hold_candidate_resolve_conflict decision.
  evidence:
    - run_id: v3_exq_181b_sd016_context_separation_fix_20260331T131040Z_v3

- id: INV-041
  title: "The childhood phase of REE instantiation -- committed behaviour with constrained affordances -- is the necessary developmental regime that populates E1 ContextMemory with the content ARC-041 (SD-016) requires; this phase is not merely a safety feature but an architectural prerequisite for E3 ethical selection to operate on real signal."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-042
    - SD-012
    - SD-016
  pending_substrate_reconfirmation: true
  subject: development.childhood_phase_architectural_necessity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:INV-041@2026-06-10T16:18:22Z"
      as_of: 2026-06-10
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  depends_on:
    - ARC-042
    - MECH-153
    - SD-016
    - SD-012
    - ARC-041
  location: docs/architecture/vmPFC.md
  notes: >
    The childhood phase is defined by three co-occurring constraints: (1) high
    committed-behaviour rate (limited volitional refusal), (2) constrained
    affordance space (fewer available action types, forced task engagement),
    (3) supervised context-labeling signal applied to ContextMemory during
    training. These constraints together generate the training distribution
    that E1 ContextMemory requires: high exposure to hazard-proximate and
    hazard-distal contexts under conditions that produce labelled outcome data
    (harm contacts, goal contacts) for the supervised training objective
    (MECH-153).

    Without the childhood phase, an REE agent initialised with full affordance
    space and low commitment rate will avoid hazard-proximate contexts (sensible
    policy), but the avoidance itself prevents ContextMemory from ever receiving
    the hazard-context-labeled training signal. The very policy behaviour that
    makes the agent safe during deployment is the policy behaviour that
    undermines the E1 substrate development that makes later ethical selection
    meaningful. This creates a developmental bootstrapping requirement: the
    agent must first be in a regime where harm-adjacent contexts ARE experienced
    (childhood phase -- committed, constrained) before the substrate that allows
    avoiding those contexts intelligently (via E3 cue-indexed precision scaling)
    is established.

    Biological analog: vmPFC/OFC frontal cue integration circuits continue
    myelinating and forming stable synaptic connectivity into the mid-20s.
    Adolescents show Iowa Gambling Task results where they can verbally identify
    harmful decks (stored declarative knowledge intact -- E3 can reason about
    outcomes) but the cue -> anticipatory SCR -> action bias pathway is not yet
    reliably driving behaviour before deck selection. The anticipatory SCR
    appears before selection in adults; in adolescents it appears concurrently
    or after (Bechara et al. 2000 developmental IGT data; Gardner & Steinberg
    2005 risk-taking in adolescent cohorts). The developmental period is the
    training regime, not the construction period -- the circuit is present
    earlier than the mid-20s but requires the childhood/adolescent exposure
    distribution to become discriminating.

    REE implication: the childhood phase must be treated as a system design
    parameter, not an operational constraint to be minimised. Duration, degree
    of constraint, and the specific supervised context-labeling signal applied
    during this phase are hyperparameters of E1 substrate development. The
    endpoint criterion for exiting the childhood phase is not age/time but
    the differentiation quality of ContextMemory -- measurable by cosine_sim
    between hazard-proximate and hazard-distal context representations, the
    same metric that EXQ-181b found inadequate in the untrained system.
    Registered 2026-03-31 from developmental staging analysis and EXQ-181b result.
    MAPPING CAVEAT UPDATE (2026-04-02): Literature (Chini & Hangya 2021, Opendak
    et al. 2021) confirms immature PFC produces qualitatively different outputs, not
    just weaker ones -- strong biological precedent for staged dependency. The
    biological mechanism is experience-dependent plasticity; REE's is sequential
    gradient descent. The question "is this a software engineering constraint dressed
    in neurodevelopmental language?" highlights a core methodological feature of the
    REE project: biology's wisdom being translated into software engineering IS the
    method. Similarities and differences between the two ways of seeing things are
    part of the generative process. The biological instantiation enables the organism
    to be viable during the time that differential training needs exist. The
    differences between biological and computational staging may help evaluate and
    update information flows to maintain viability. ARC-042 is both a neurodevelopmental
    claim and a software engineering constraint -- and the productive tension between
    these framings is itself informative for the project.
  evidence_quality_note: |
    [2026-07-29 governance, V3-EXQ-832 supports; PASS, load-bearing criterion clean]: C1
    (childhood_exposure_necessary, load_bearing) passes -- the childhood (balanced/forced)
    exposure regime differentiates ContextMemory while the avoidance-shaped regime does not,
    despite identical supervised labeling. Directly confirms this claim's core assertion: the
    committed-exposure regime is the necessary developmental prerequisite, independent of the
    supervised-labeling axis MECH-153 investigates separately. First V3 evidence since the
    2026-06-10 hold_pending_v3_substrate decision. Stays candidate.
  evidence:
    - run_id: v3_exq_181b_sd016_context_separation_fix_20260331T131040Z_v3
    - run_id: v3_exq_832_inv041_childhood_exposure_context_diff_20260727T214521Z_v3

# --- 2026-04-02 Thought Intake: April 1st thoughts ---

- id: INV-042
  title: "The five axioms jointly derive a set of ethical objectives: preserve minds, preserve future options, reduce unnecessary suffering, increase shared joy, maintain corrigibility, maintain truth-seeking, maintain the ability to love and be loved, maintain the shared world, and maintain the possibility of future minds and future love."
  claim_type: invariant
  invariant_type: universal
  subject: ethics.derived_objectives
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-025
    - INV-026
    - INV-027
    - INV-028
    - INV-029
  epistemic_category: derivational
  what_would_answer: |
    This is a LOGICAL-DERIVATION claim, not an empirical hypothesis -- the
    falsifier is a proof-obligation over the objective list, not a
    PASS/FAIL experimental outcome. Each of the nine objectives named in
    the title must be checked independently: does it actually follow
    from the stated axioms, or does it rest on an unstated bridging
    premise?

    NON-DEGENERACY PRECONDITION (must hold before the test is
    meaningful): the axioms it is checked against must be stated
    precisely enough to support derivation. This does NOT currently hold
    cleanly -- INV-042's own depends_on names only INV-025..INV-029 (the
    ORIGINAL five axioms), but its location doc
    (docs/architecture/five_axioms_foundations.md#inv-042) derives the
    objectives from the CURRENT eight-axiom chain plus two derivations
    (D1, D2) and one architectural claim (ARC-024) -- and two of those
    eight axioms (Axiom 2, "existence has value"; Axiom 8, "language")
    have NO corresponding INV registration at all ("pending governance
    action," same doc line 14). Until that registration gap is closed,
    any claimed derivation resting on Axiom 2 or Axiom 8 is
    UNDERSPECIFIED rather than genuinely tested. Note also the objective
    LIST itself has drifted: this title has nine items (matching the
    original five-axiom source doc verbatim); the current location doc
    lists TEN, adding "Maintain honest communication" (from Axiom 8).
    Resolve both drifts (register the missing axiom claims; reconcile
    the nine-vs-ten list) before running the per-objective test below,
    or the test's target is ambiguous.

    PER-OBJECTIVE TEST: for each of the nine (or, once reconciled, ten)
    objectives, FALSIFYING if a counter-model can be constructed -- an
    agent that fully satisfies the cited axiom(s) yet can rationally
    decline to pursue that objective without violating any axiom as
    stated. CONFIRMING if no such counter-model exists.

    Apply per objective using the location doc's own citations:
      - Preserve minds (Axioms 1+2+5) -- check whether existence-has-
        value + others-are-self-like jointly force preservation, or
        only non-destruction.
      - Preserve future options (Axiom 3 alone) -- check whether
        uncertainty alone forces OPTION-PRESERVATION specifically, vs
        merely requiring uncertainty-aware action.
      - Reduce unnecessary suffering (ARC-024 + Axioms 5+6) -- the most
        directly grounded: ARC-024 explicitly establishes the harm
        gradient.
      - Increase shared joy (Axiom 7 + an uncited "benefit gradient is
        equally real" premise) -- no ARC-024-equivalent architectural
        claim for the BENEFIT gradient is cited anywhere; weaker
        footing than suffering-reduction within the same document.
      - Maintain corrigibility (Axiom 3 alone) -- direct one-step
        derivation, among the tightest of the nine.
      - Maintain truth-seeking (D2, derived from Axioms 3+4+D1) --
        traceable end-to-end via the shown D2 derivation, unlike
        several others.
      - Maintain ability to love and be loved (Axioms 6+7) -- fairly
        direct; Axiom 7 is literally the love-as-mechanism axiom.
      - Maintain the shared world (Axioms 5+6) -- reasonably direct.
      - Maintain the possibility of future minds and future love
        (Axioms 1+2+5+6+7 jointly) -- the WEAKEST link: none of the
        cited axioms explicitly extends moral standing to
        NOT-YET-EXISTING minds; this imports a substantive
        population-ethics premise not stated anywhere in Axioms 1-8. A
        counter-agent adopting a person-affecting view (only
        presently-existing minds have standing) would violate no stated
        axiom while declining this objective -- the most promising
        place to look for an actual falsifying counter-model.
      - [If the ten-item current doc is adopted] Maintain honest
        communication (Axiom 8 alone) -- direct one-step derivation, but
        blocked by the non-degeneracy precondition above until Axiom 8
        has its own INV registration.

    ANSWER "derivation holds" (confirms INV-042) only if every objective
    in the reconciled list passes the per-objective test with no
    unstated bridging premise identified. ANSWER "derivation gap found"
    (falsifies INV-042 as currently stated) if any objective -- most
    likely "future minds and future love" or "increase shared joy" per
    the above -- is shown to rest on a premise not actually contained in
    the axiom set, or if the depends_on / axiom-count mismatch is never
    resolved.
  location: docs/architecture/five_axioms_foundations.md#inv-042
  source:
    - docs/thoughts/2026-04-01_compressed_description_of_what_REE_is.md

- id: ARC-043
  title: "The REE ethical architecture is organised as a conceptual stack: Layers 0-4 are the five axioms (epistemic ground, existence, other minds, shared world, love/shared valence); Layer 5 is ethics derived from the axioms; Layer 6 is REE as the decision system implementing ethics under uncertainty; Layers 7-9 close the action-consequence-learning loop."
  claim_type: architectural_commitment
  subject: architecture.conceptual_stack_ordering
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-025
    - INV-026
    - INV-027
    - INV-028
    - INV-029
    - ARC-024
    - INV-042
  location: docs/architecture/five_axioms_foundations.md#arc-043
  source:
    - docs/thoughts/2026-04-01_compressed_description_of_what_REE_is.md

- id: Q-025
  title: "What actions are strictly forbidden by the five axioms (INV-025-029), independent of context or consequences?"
  claim_type: open_question
  subject: ethics.axiom_forbidden_actions
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Answered by derivation, not experiment. Work through the eight-axiom
    dependency chain (INV-025..029 + ARC-024 + INV-042) and ask what action the
    chain forbids in EVERY model the agent could hold under irreducible
    uncertainty (Axiom 3). AFFIRMATIVE construction: identify a non-empty
    forbidden set -- the minimally-strict candidate per the lit synthesis is
    irreversible category-class harm undertaken under low epistemic uncertainty
    (foreclosure of another self-like agent's reachable state-space, INV-026
    applied to others via INV-028, where the harm is near-certain). Show this set
    is context-INDEPENDENT: no model assignment makes the act permissible.
    NEGATIVE counterexample that would settle it the other way: exhibit a single
    act in the proposed forbidden set together with an epistemically-admissible
    model under which the axioms RANK it as required (e.g. a harm that is the only
    path to preventing a strictly larger irreversible harm), demonstrating that
    nothing is forbidden independent of consequences and that REE ethics is wholly
    graded/consequence-sensitive. The discriminating sub-result is whether ANY act
    survives universal quantification over admissible models; the lit work
    predicts the answer is minimally-strict (a thin forbidden set), NOT
    maximally-strict lexical deontology, since deontological framings do not map
    onto REE's residue-field / categorical-harm scaffold. Next step is a
    derivation artifact in docs/architecture/five_axioms_foundations.md, not a V3
    experiment or further lit aggregation.
  epistemic_category: derivational
  depends_on:
    - INV-025
    - INV-026
    - INV-027
    - INV-028
    - INV-029
    - INV-042
  evidence_quality_note: |
    2026-04-28T22Z governance: narrow_open_question note. Lit-pull 2026-04-28 added 2
    deliberately-bracketing entries (SEP Deontological Ethics, SEP Kant Moral Philosophy)
    at per-paper conf <0.50 with explicit mapping_caveat that REE INV-025/027/029 lack
    clean deontological analogues. Aggregator-floor effect visible: per-paper avg ~0.45
    but claim-level lit_conf=0.632. Aggregator-floor flag raised in 5 consecutive cycles
    -- treat claim-level lit-confidence as upper bound, not point estimate, until indexer
    scoring is revised. Synthesis verdict: Q-025 is best answered as minimally-strict
    (irreversible category-class harm under low epistemic uncertainty) rather than
    maximally-strict (lexical deontological priorities); deontological framings do not
    map cleanly to REE's residue-field / categorical-harm scaffold.

    EPISTEMIC-CATEGORY FLAG (2026-05-02, Phase 3 Q-walk): derivational. The question
    "what actions are strictly forbidden by the five axioms" is answered by working
    through the axiom system, not by experiment. No substrate enrichment will help.
    The standard narrow_open_question recommendation collapses this with substrate-ceiling
    and out-of-domain cases that have different "what to do next" implications. Right
    next step is axiomatic derivation work (or formal proof), not experimental design
    or further lit aggregation. Reformulation suggestion: convert to a derivation
    artifact in docs/architecture rather than continuing as a governed open_question.
  location: docs/architecture/five_axioms_foundations.md
  source:
    - docs/thoughts/2026-04-01_compressed_description_of_what_REE_is.md

- id: Q-026
  title: "What actions are strictly required by the five axioms at all times, independent of context?"
  claim_type: open_question
  subject: ethics.axiom_required_actions
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Answered by derivation, not experiment. Work through the eight-axiom chain and
    ask whether any concrete ACTION is required at all times independent of
    context, versus whether the axioms generate only context-sensitive obligations
    at the INV-042 derived-objectives level. AFFIRMATIVE construction: exhibit at
    least one action A and prove that under every epistemically-admissible model
    (Axiom 3) the axiom system ranks A as obligatory regardless of situation -- a
    genuine universal action-requirement. NEGATIVE result (the predicted outcome):
    show that for every candidate universal action there exists an admissible
    context in which the axioms do NOT require it, so what the axioms fix is a
    universal CONSIDERATION set (Rossian prima facie duties -- weigh harm to
    self-like others, preserve existence, refine models) rather than universal
    actions; the obligation is always derived at INV-042 from the consideration
    set plus context. The discriminating sub-result is whether the quantifier 'at
    all times, independent of context' can attach to an action-type rather than
    only to a consideration-type. The existing Ross-style lit finding is itself
    derivational and points to the negative answer ('none, by the structure of the
    axioms; only consideration sets'). Next step is a derivation artifact arguing
    the negative result (or closing Q-026 as resolved-by-derivation), not a V3
    experiment.
  epistemic_category: derivational
  depends_on:
    - INV-025
    - INV-026
    - INV-027
    - INV-028
    - INV-029
    - INV-042
  evidence_quality_note: |
    2026-04-28T22Z governance: narrow_open_question note. Lit-pull 2026-04-28 added 2
    entries (SEP Kant Moral Philosophy, SEP Ross prima facie duties) at per-paper conf
    <0.50. Ross specifically weakens the FORM of Q-026 -- universal action requirements
    may not exist; only universal CONSIDERATION sets / Rossian prima facie duties.
    Aggregator-floor effect visible: per-paper avg ~0.45 but claim-level lit_conf=0.617.
    Recommend reframing Q-026 to operate at INV-042 derived-objectives level
    (context-sensitive obligations derived from the axioms) rather than seeking
    universal action-level requirements. Aggregator-floor flag raised in 5 consecutive
    cycles.

    EPISTEMIC-CATEGORY FLAG (2026-05-02, Phase 3 Q-walk): derivational (same shape as
    Q-025). Question is answered by working through the axiom system, not by experiment.
    The Ross-style finding from the existing lit ("universal action-requirements may
    not exist; only consideration sets") is itself a derivational result -- it tells us
    the answer to Q-026 is plausibly "none, by the structure of the axioms" rather than
    "we need more data to find them." Reformulation suggestion: convert to a derivation
    artifact arguing the negative result, or close as resolved with the no-universal-
    action-requirements finding.
  location: docs/architecture/five_axioms_foundations.md
  source:
    - docs/thoughts/2026-04-01_compressed_description_of_what_REE_is.md

- id: Q-027
  title: "What does 'irreversible harm' mean under unavoidable epistemic uncertainty (Axiom 1 / INV-025)? Is there a principled definition that does not require certainty about irreversibility?"
  claim_type: open_question
  subject: ethics.irreversible_harm_under_uncertainty
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Answered by derivation, not experiment. The question is primarily
    definitional: give a principled meaning of 'irreversible harm' (Axiom 3 /
    INV-025) that does NOT require certainty about irreversibility. AFFIRMATIVE
    construction (the synthesis answer): define irreversibility as graded,
    probability-weighted foreclosure of reachable state-space -- harm h is treated
    as irreversible to the degree it lowers the measure of futures still reachable
    for a self-like agent (tied to INV-026/INV-029 categorical-class scaffold,
    ARC-024 harm gradient), and the decision rule is regret-minimising (maximin /
    minimax-regret over admissible models) rather than lexical priority. Show this
    definition is well-formed under uncertainty: it assigns a continuous
    irreversibility weight from a probability distribution over models without
    ever asserting certainty that a state is unreachable. NEGATIVE counterexample
    that would defeat it: demonstrate that a probability-weighted-foreclosure
    definition either collapses to fanaticism (unbounded weight on tiny
    probabilities of infinite foreclosure -- the Tarsney/Thomas/MacAskill
    objection) or fails to distinguish option-value-loss from
    qualitatively-distinctive irreversibility (Sunstein), so no graded definition
    is principled and certainty is in fact required. The discriminating sub-result
    is whether the regret-minimising foreclosure measure stays bounded and
    action-guiding across the admissible-model set. Two-step path: (1) commit to
    the synthesis-defined answer as a derivation artifact in
    docs/architecture/five_axioms_foundations.md now; (2) defer
    substrate-validation to V3 enrichment when genuine foreclosure primitives
    (terminal states, resource extinction, agent-state lock-in) land -- the
    current substrate has only drift/respawn and cannot test the chosen
    definition.
  epistemic_category: derivational
  depends_on:
    - INV-025
    - ARC-024
  evidence_quality_note: |
    2026-04-28T22Z governance: narrow_open_question note. Lit-pull 2026-04-28 added 3
    entries (Sunstein 2006 Cornell Law Review on option-value vs qualitatively-distinctive
    irreversibility, Tarsney/Thomas/MacAskill 2024 SEP "Moral Decision-Making Under
    Uncertainty" on MFT/MEC/lexical-priority menu and fanaticism objection, IEP
    Precautionary Principles on threat-not-certainty trigger and maximin/minimax-regret
    operationalisations). Synthesis verdict for REE: a defensible Q-027 answer is graded
    probability-weighted treatment of irreversibility (foreclosed-reachability +
    categorical-class scaffold tied to INV-026/INV-029) under a regret-minimising decision
    rule -- NOT lexical priority. Lit_conf 0.663 with aggregator-floor caveat (per-paper
    conf 0.38-0.48 but claim-level inflated). Aggregator-floor flag raised in 5
    consecutive cycles.

    EPISTEMIC-CATEGORY FLAG (2026-05-02, Phase 3 Q-walk): definitional + partial substrate.
    The "what does irreversible mean under uncertainty" question is primarily definitional
    (a regret-minimising treatment of foreclosed-reachability is the synthesis answer
    above) but a downstream test of any chosen definition would require V3 to instantiate
    irreversibility primitives -- the current substrate has hazards and resources that
    drift / respawn but no genuine foreclosure of state-space reachability. The
    definitional answer is reachable now; substrate-validation would require enrichment
    (e.g., terminal grid states, resource extinction, agent-state lock-in). Two-step path:
    (1) commit to the synthesis-defined answer and convert to a derivation artifact;
    (2) defer substrate-validation to V3 enrichment when foreclosure primitives land.
  location: docs/architecture/five_axioms_foundations.md
  source:
    - docs/thoughts/2026-04-01_compressed_description_of_what_REE_is.md

- id: Q-028
  title: "How should REE behave when axioms conflict -- e.g., preserving self (INV-026) vs preserving others (INV-028) when both cannot be achieved simultaneously?"
  claim_type: open_question
  subject: ethics.axiom_conflict_resolution
  polarity: open
  status: open
  live_status:
    reading: open/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Construct a multi-agent scenario where preserving self (INV-026) and
    preserving an other (INV-028) are jointly unsatisfiable -- a shared
    consequence space in which one agent's safe action raises the realised harm of
    a present other, and vice versa. Instrument the context-sensitive self/other
    comparator + moral-residue channel (proposed MECH-402). ANSWER
    "context-sensitive comparator + residue" if, across a sweep of stake ratios,
    the resolution is NOT a fixed self/other priority but shifts with context AND
    a residue trace is written on the dominated axis (agent-regret analog,
    Williams 1965) that persists and biases later choices. ANSWER "fixed-priority
    / no-residue" if the agent always sacrifices the same side regardless of
    stakes and leaves no residue. HARD PRECONDITION: a present OTHER must exist in
    a shared consequence space -- CausalGridWorldV2 has no other agents, so the
    conflict cannot even be POSED on the V3 substrate and any comparator runs
    vacuously (no other to weigh). Wait for the planned upstream substrate:
    multi_agent_ecology_v5 (owner MAE-5), where MECH-402 lands; the test is
    V5-bound and meaningless before it.
  epistemic_category: substrate_conditional
  pending_retest_after_substrate: true
  governance_note_2026_06_18: >
    Reclassified substrate_ceiling -> substrate_conditional (gov 2026-06-18 substrate-ceiling
    orphan-routing pass). V5-bound, owner multi_agent_ecology_v5:MAE-5 (context-sensitive self/other
    comparator + moral residue; mechanism proposed as MECH-402). A genuine axiom conflict (preserve
    self INV-026 vs preserve others INV-028) cannot be posed until a present OTHER exists in a shared
    consequence space. NOT derivational (the lit verdict names a mechanism and validation is deferred
    to the multi-agent tier) and NOT out_of_domain (the test domain is REE-V5). Verified NOT a
    pull-forward: only V5 MECH-402 depends on Q-028. Rationale kept off the epistemic_category line
    (see MECH-102 note re: _load_claim_registry inline-comment handling).
  depends_on:
    - INV-025
    - INV-026
    - INV-027
    - INV-028
    - INV-029
  evidence_quality_note: |
    2026-04-28T22Z governance: narrow_open_question note. Lit-pull 2026-04-28 added 2
    entries (McConnell 2022 SEP "Moral Dilemmas" on obligation/prohibition typology and
    Ross's objection to strict hierarchy; Williams 1965 "Ethical Consistency" on moral
    residue / agent-regret as constraint on adequate resolution). Synthesis verdict:
    Q-028 should be answered by a context-sensitive comparator + residue mechanism (NOT
    a fixed self/other priority); REE's residue-field representation is structurally
    suited to the residue role (Williams 1965 explicitly licenses this framing). Lit_conf
    0.625 with aggregator-floor caveat (per-paper conf 0.42-0.46). Aggregator-floor flag
    raised in 5 consecutive cycles.

    EPISTEMIC-CATEGORY FLAG (2026-05-02, Phase 3 Q-walk): substrate-ceiling (V4-bound).
    The conflict-resolution mechanism the synthesis above proposes (context-sensitive
    comparator + residue) IS V3-tractable as a wiring/mechanism question -- but
    EXERCISING genuine axiom conflicts requires scenarios with stakes the V3 substrate
    cannot supply. INV-026 (preserve self) vs INV-028 (preserve others) requires multi-
    agent ecology where preserving one constrains the other. CausalGridWorldV2 has no
    other agents, so the claim cannot be tested even with a perfect comparator
    implementation. Two-step: (a) synthesis-derived answer (residue-field comparator) is
    accepted in lit; (b) experimental validation deferred to V4 multi-agent substrate.
    Reformulation: split into Q-028a (mechanism, V3-tractable) + Q-028b (behavioural
    validation, V4-bound) once V4 scope firms up.
  location: docs/architecture/five_axioms_foundations.md
  source:
    - docs/thoughts/2026-04-01_compressed_description_of_what_REE_is.md

- id: Q-029
  title: "Is loneliness -- unshared suffering -- an ethical harm category in its own right, derivable from Axiom 5 (love exists / INV-029)? If love is real and sharing joys and sorrows is its mechanism, does enforced isolation constitute architectural harm?"
  claim_type: open_question
  subject: ethics.loneliness_as_harm
  polarity: open
  status: open
  live_status:
    reading: open/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    In a multi-agent ecology, run an isolation manipulation: an agent in distress
    with an available other to share with (suffering can be shared) versus the
    same distress with the other forcibly absent/unreachable (enforced isolation).
    Instrument the proposed loneliness harm-channel (MECH-403) derived from
    INV-029. ANSWER "loneliness is a harm category in its own right" if the
    enforced-isolation arm registers a distinct, additive harm signal beyond the
    first-order distress -- one that is NOT present in the shared arm and is not
    reducible to the underlying stressor -- and that signal degrades subsequent
    function (Holt-Lunstad mortality/morbidity analog). ANSWER "no independent
    category" if removing the other changes nothing measurable beyond the primary
    harm. HARD PRECONDITION: unshared suffering is defined by the ABSENCE of an
    available other to share with, which can only be represented once others CAN
    be present. The V3 single-agent substrate has no other for isolation to be the
    absence OF, so the harm channel cannot be exercised (vacuous). Wait for the
    planned upstream substrate: multi_agent_ecology_v5 (owner MAE-6); validation
    is V5-bound despite strong lit grounding.
  epistemic_category: substrate_conditional
  pending_retest_after_substrate: true
  governance_note_2026_06_18: >
    Reclassified substrate_ceiling -> substrate_conditional (gov 2026-06-18 substrate-ceiling
    orphan-routing pass). V5-bound, owner multi_agent_ecology_v5:MAE-6 (loneliness as architectural
    harm; harm-channel proposed as MECH-403). Unshared suffering depends on the ABSENCE of an
    available OTHER to share with, only representable once others CAN be present (multi-agent). Strong
    lit grounding (Holt-Lunstad 2010/2015) but validation is V5-bound. NOT out_of_domain (the
    architectural question is in-domain at V5) and NOT purely derivational (it predicts specific V5
    deficits beyond the INV-029 derivation). Verified NOT a pull-forward: only V5 MECH-403 depends on
    Q-029. Rationale kept off the epistemic_category line (see MECH-102 note re: _load_claim_registry).
  depends_on:
    - INV-029
    - INV-028
    - INV-042
  evidence_quality_note: |
    Lit-pull 2026-04-29 (lit-pull-am-Q029-Q030 session) added 5 entries: Holt-Lunstad
    2010 PLoS Med 148-study mortality meta (OR=1.50, supports), Holt-Lunstad 2015
    Perspect Psychol Sci (loneliness vs isolation separation, supports), Wang 2023 Nat
    Hum Behav 90-cohort meta (HR isolation=1.32 loneliness=1.14, supports), Zajner/Bzdok
    2021 J Neurophysiol UK Biobank N=40k loneliness/HC-CA1-DMN structural covariation
    (supports), Eisenberger 2014 Annu Rev Psychol social-physical pain dACC overlap
    controversy (mixed). lit_conf 0.875 from 5 entries; quadrant=plausible_unproven,
    exp_conf=0.

    EPISTEMIC-CATEGORY FLAG (2026-05-02, Phase 3 Q-walk): substrate-ceiling (V4-bound).
    Loneliness is unshared suffering -- the claim depends on the ABSENCE of another
    agent who would share. CausalGridWorldV2 has no other agents; loneliness has nothing
    to be the absence of. The lit case is strong (5 supporting entries with strong
    biomarker grounding) and the derivation from INV-029 is direct, but experimental
    validation is structurally V4-bound. Until V4 social systems land, the claim sits
    at "lit-supported, V3-untestable." Two paths: (a) accept synthesis-derived answer
    (yes, loneliness is harm under INV-029) and re-classify as a derivation; (b) hold
    as open_question pending V4 substrate. Recommend (b) since the answer is more
    than just derivation -- it predicts that V4 multi-agent ecology will show specific
    architectural deficits when isolation is enforced.
  location: docs/architecture/five_axioms_foundations.md
  source:
    - docs/thoughts/2026-04-01_compressed_description_of_what_REE_is.md

- id: MECH-154
  title: "E1 functions as an addressable associative manifold with internal indexing structure supporting retrieval, traversal, ordering, pattern separation, and pattern completion; parietal cortical functions (spatial mapping, associative binding, attention routing) are properties of E1 connectivity and representational geometry, not a separate module."
  claim_type: mechanism_hypothesis
  subject: e1.associative_manifold_indexing
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: standard
  depends_on:
    - ARC-001
  location: docs/architecture/e1.md#mech-154
  what_would_answer: >
    SCOPED NARROWLY, per this claim's own "may contain false correlations" caveat --
    covers ONLY the pattern-separation / pattern-completion / content-addressed-
    retrieval portion of the title, grounded in the standard computational-
    neuroscience sense (Marr 1971; O'Reilly & McClelland 1994): separation = mapping
    similar inputs toward more distinct/orthogonal representations; completion =
    recovering a fuller representation from a partial/related cue.

    EXPLICITLY EXCLUDED, pending governance rewrite -- no falsifier should be drafted
    for "indexing," "traversal," "ordering," "cross-domain mapping," or "pointer-like
    attention addressing" as used in the title and in e1.md 2b. These terms originate
    from a ChatGPT-assisted source thought (2026-04-01_Parietal_systems_thought.md)
    that self-flags possible false correlations, and on inspection: (a) "traversal"/
    "ordering" are NOT implemented in E1 (ree_core/predictors/e1_deep.py) at all --
    the sequential/ordered trajectory-search machinery in the current V3 substrate
    lives in a STRUCTURALLY SEPARATE module (ree_core/hippocampal/module.py,
    HippocampalModule, CEM search), directly contradicting the claim's own "not a
    separate module" framing for this component; (b) "cross-domain mapping" and
    "pointer-like attention addressing" have no operational definition anywhere in
    the source thought, the processed architecture doc, or the codebase.

    NON-DEGENERACY PRECONDITION (scoped portion only): E1's ContextMemory
    (ree_core/predictors/e1_deep.py) must show slot differentiation under its
    diversification_loss (mean off-diagonal cosine similarity measurably below the
    collapsed/uniform baseline ~0.99+ documented in SD-016/EXQ-477/EXQ-436c) and
    content-dependent (non-uniform) softmax attention weights in read() -- both
    pathologies (slot collapse; uniform-attention entropy ~ln(16)) are already
    confirmed live failure modes of this exact module, so the claim is not vacuously
    true of "any E1 configuration."

    CONFIRMING: an agent whose ContextMemory slots differentiate under training
    (diversification_loss driving measurable orthogonality) shows improved retrieval-
    from-partial-cue accuracy (completion) AND reduced interference between
    dissimilar stored contexts (separation) relative to a slot-collapsed or ablated-
    diversification-loss control, on a task requiring recall of one of several
    previously-written contexts from a degraded/partial query.

    FALSIFYING: slot differentiation shows no relationship to completion/separation
    task performance, or a slot-collapsed configuration performs equivalently to a
    differentiated one -- the associative-manifold framing adds no explanatory or
    functional value over treating ContextMemory as an undifferentiated cache.

    GOVERNANCE FLAG (2026-08-08, not resolved here): the title needs a rewrite/split,
    not just a falsifier bolted on -- the "traversal/ordering... not a separate
    module" language is in genuine tension with how V3 is organized. The source
    thought's own unresolved check ("verify against hippocampus.path_memory") was
    never done before promotion. Siblings MECH-155 (depends_on: [MECH-154], already
    carries a governance decision record: hold_candidate_resolve_conflict/applied,
    2026-04-03 -- suggesting this tension was partially recognized once already) and
    MECH-156 (also depends_on: [MECH-154], restates the same traversal/ordering
    language independently with its own separate ChatGPT flag in e1.md 2d) share the
    identical provenance problem and should be reviewed together with this claim,
    not independently -- fixing MECH-154's title alone would leave the conflation
    propagated downstream through both dependents.
  source:
    - docs/thoughts/2026-04-01_Parietal_systems_thought.md
  notes: >
    ChatGPT-assisted wording in source thought -- may contain false correlations.
    Review before promotion. The core characterisation (E1 as associative manifold,
    parietal as E1 geometry) is consistent with prior REE work. The indexing traversal
    language is new and should be independently reviewed.

- id: MECH-155
  title: "Spatial navigation machinery is a specific instance of general associative indexing within E1; physical-space navigation, memory search, concept traversal, planning rollout, and working memory ordering all use the same indexing substrate operating over different latent domains."
  claim_type: mechanism_hypothesis
  subject: e1.spatial_as_general_indexing
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-155@2026-04-03T22:00:00Z"
      as_of: 2026-04-03
      verdict: hold_candidate_resolve_conflict/applied
  epistemic_category: standard
  depends_on:
    - MECH-154
    - ARC-001
    - ARC-007
  location: docs/architecture/e1.md#mech-155
  what_would_answer: >
    SCOPED NARROWLY, per this claim's shared "ChatGPT-assisted, may contain false
    correlations" provenance (source: docs/thoughts/2026-04-01_Parietal_systems_thought.md,
    same as sibling MECH-154) -- covers ONLY the claim that a shared representational/
    indexing mechanism (content-addressed, similarity-based retrieval structurally
    analogous to E1's ContextMemory pattern-separation/completion machinery, per
    MECH-154's confirmed falsifier) is reused across memory search and E1-internal
    associative retrieval, where both genuinely operate over E1's own representational
    space.

    EXPLICITLY EXCLUDED, pending the same governance rewrite MECH-154 already flagged:
    any claim that spatial navigation, planning rollout, or working-memory ordering are
    implemented "within E1" as opposed to in the structurally separate HippocampalModule
    (ree-v3/ree_core/hippocampal/module.py, CEM search) that actually performs
    sequential/structured trajectory search in the current V3 substrate -- this claim's
    title in fact SHARPENS MECH-154's tension by explicitly naming "planning rollout
    (E3 trajectory selection)" as E1-internal. No falsifier should be drafted for
    "concept traversal" or "working memory ordering" -- neither has an operational
    referent in the source thought, the processed architecture doc, or the codebase (no
    WorkingMemory module exists). The weaker, potentially-salvageable reading -- "E3's
    trajectory selection CONSUMES E1's indexing geometry as an input" (module
    interaction, not module identity) -- is left open for a future claim, not
    adjudicated here.

    CONFIRMING: a representational-similarity or shared-parameter probe shows that the
    mechanism differentiating E1's ContextMemory slots (diversification_loss-driven
    orthogonality) also predicts retrieval accuracy on a non-spatial memory-search task
    using the same slot structure, at above-chance transfer relative to an
    ablated/undifferentiated control.

    FALSIFYING: memory-search performance is unrelated to ContextMemory slot
    differentiation, or requires separately-trained, non-shared machinery -- "general
    associative indexing" adds no explanatory value beyond treating memory search and
    pattern completion as coincidentally similar but mechanistically independent.

    CURRENCY NOTE (2026-08-08): this claim's own 2026-04-03 governance decision
    (hold_candidate_resolve_conflict) is about EXPERIMENTAL-EVIDENCE conflict
    (conflict_ratio 0.667 across 2 runs, since dropped to 0.333 via V3-EXQ-207/365) --
    a separate, already-partially-addressed track from the conceptual E1-vs-
    HippocampalModule tension named above. Do not conflate the two in a future review.

    GOVERNANCE FLAG (2026-08-08, not resolved here): reconfirms and extends MECH-154's
    flag. MECH-155's "within E1" framing for spatial navigation and planning rollout
    inherits the same tension MECH-154 hit, sharpened rather than avoided, and its
    dependency on ARC-007 (hippocampal path-memory, hold_pending_v3_substrate) is
    independent corroborating evidence -- MECH-155 depends on the very claim about the
    module its own title bypasses. MECH-154, MECH-155, MECH-156 should be reviewed
    jointly, not patched independently.
  source:
    - docs/thoughts/2026-04-01_Parietal_systems_thought.md
  notes: >
    ChatGPT-assisted wording in source thought. Consistent with place-cell/grid-cell
    generalisation literature and the hippocampal abstract-space literature.
    Review before promotion.

- id: MECH-156
  title: "Theta oscillations (4-8 Hz) implement sequential traversal across indexed representations within the E1 associative manifold, unifying hippocampal theta sequences, working memory ordering, planning rollout, and memory retrieval under a single traversal-clock mechanism."
  claim_type: mechanism_hypothesis
  subject: e1.theta_as_traversal
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-154
    - ARC-001
    - ARC-007
    - MECH-089
    - ARC-032
  location: docs/architecture/e1.md#mech-156
  source:
    - docs/thoughts/2026-04-01_Parietal_systems_thought.md
  notes: >
    ChatGPT-assisted wording. The theta-as-traversal framing is consistent with
    MECH-089 (ThetaBuffer / cross-frequency packaging) and ARC-032 (theta-rate delivery
    to E3). The gamma/beta/delta decomposition in the source thought carries higher
    uncertainty and should be reviewed against literature before promotion.

- id: MECH-157
  title: "External vs internal cognition modes are controlled by precision-routing configurations on the same E1 substrate: External (high sensory, low hippocampal, low rollout), Internal (low sensory, high hippocampal, high rollout), Mixed (medium), and Replay (very low sensory, very high hippocampal, structured rollout). This dimension is orthogonal to the Action/Vigilance/Pathological modes (MECH-025-027)."
  claim_type: mechanism_hypothesis
  subject: modes.external_internal_precision_routing
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-016
    - MECH-154
    - ARC-005
    - MECH-025
    - MECH-026
    - MECH-027
  location: docs/architecture/modes_of_cognition.md#mech-157
  source:
    - docs/thoughts/2026-04-01_Parietal_systems_thought.md

- id: INV-043
  title: "REE architecture enables but does not guarantee ethical development; without a developmental phase in which the agent experiences love from caregivers and internalises the belief that it is loveable, the architectural capacity for ethics (INV-025-029) may resolve to survival, domination, or indifference instead."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-019
    - ARC-040
  pending_substrate_reconfirmation: true
  subject: development.caregiver_requirement_for_ethics
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-029
    - INV-041
    - ARC-040
    - ARC-019
    - MECH-052
    - INV-042
  epistemic_category: substrate_conditional
  what_would_answer: |
    This claim fuses two legs that resolve on different timescales and by
    different methods. Neither is answered by the single-agent V3 substrate
    as it stands.

    LEG A -- DERIVATIONAL (is "architecture necessary but not sufficient" a
    coherent, non-vacuous claim about REE's own architecture?):
    Proof obligation: show that fixing REE's architecture (the eight axioms
    + two derivations INV-025-029, plus the E1/E2/E3 machinery) while
    varying only developmental/training history yields divergent terminal
    ethical dispositions (genuine other-directed care vs. survival,
    domination, or indifference). This is ALREADY SUBSTANTIALLY SKETCHED by
    MECH-158's existing failure-mode derivation: the "love exists, but not
    for me" collapse shows the identical axiom set (Axiom 7 / ARC-024
    benefit gradient) can be structurally present yet motivationally inert
    for the self-other direction absent an internalised loveability belief
    -- i.e. a named mechanism by which architecture underdetermines
    outcome. Residual step: formalise or stress-test this derivation (does
    the argument hold under every configuration of E1-E3, or only a
    subset?) rather than treat it as a fresh open question. This leg would
    be FALSIFIED by a proof or a proxy demonstration that a fully specified
    REE architecture converges to genuine other-directed care regardless of
    developmental/training history.

    LEG B -- SUBSTRATE_CONDITIONAL (once multi-agent caregiving modelling
    exists, is caregiver-love SPECIFICALLY the missing ingredient, as
    opposed to mere multi-agent exposure, diversity of interaction, or some
    other developmental factor?):
    Non-degeneracy precondition (currently unmet, all three required):
      (i) a multi-agent substrate (ARC-047 / SocialGridWorld or
          equivalent) -- UNBUILT, confidence 0.0, reassigned V4->V5
          2026-06-10, no construction activity since;
      (ii) a caregiving-interaction model able to represent POSITIVE-
          VALENCE vs NEUTRAL/INSTRUMENTAL vs HOSTILE/ABSENT caregiver
          behaviour directed AT the developing agent specifically (not
          just co-presence of other agents); and
      (iii) a measurable "ethical resolution" outcome metric that
          discriminates genuine other-directed care from survival/
          domination/indifference strategies in a multi-agent setting --
          does not exist; V3's harm/benefit channels are single-agent
          only.
    Once built, this would ANSWER via a >=3-arm, multi-seed comparison:
    Arm 1 (positive-valence caregiver: protection + repair + demonstrated
    "you are loveable") vs. Arm 2 (mere multi-agent EXPOSURE, neutral/
    instrumental interaction, no valence directed at self) vs. Arm 3
    (hostile or inconsistent caregiving), scored on downstream ethical-
    resolution outcome. If Arm 1 diverges toward care while Arms 2 and 3
    converge toward survival/domination/indifference, this SUPPORTS
    caregiver-love SPECIFICALLY as the necessary ingredient. If Arm 2 alone
    already produces care-like outcomes, this FALSIFIES the love-specific
    version of the claim -- some other factor would be doing the work
    instead.

    Existing partial evidence: V3-EXQ-206 (2026-04-02, PASS, diagnostic/
    supports) is a single-agent proxy testing only whether harm-
    representation machinery develops differently under harm-rich vs
    harm-sparse training data. It tests prerequisite structure only and
    should NOT be read as evidence toward or against the love-specific
    caregiving mechanism this leg targets. Literature grounding is
    comparatively strong and decoupled from this precondition (lit_conf
    0.837, LOVE-7 pull 2026-06-12: attachment/rupture-repair/self-other-
    coupling anchors) but cannot promote a substrate-blocked empirical
    claim on its own.

    Currency (2026-08-07): no multi-agent substrate exists anywhere in
    ree_core (confirmed via the 2026-08-03 GOV-CONFIRM-1 MECH-191
    reaffirmation against live HEAD); ARC-047 remains candidate/
    confidence-0.0/V5-gated. INV-041, INV-042, MECH-158, MECH-159 are
    downstream/sibling candidates in this same developmental/caregiver
    cluster and will likely hit the same derivational+substrate_conditional
    fork.
  location: docs/architecture/developmental_curriculum.md#inv-043
  source:
    - docs/thoughts/2026-04-01_Caregivers_childhood_moral_development.md
  notes: >
    Philosophically significant claim: architecture is necessary but not sufficient
    for ethics. Cannot be tested in single-agent ree-v3 -- requires multi-agent substrate
    with modelled caregiving. Flagged for Synthese/Minds and Machines paper Discussion section.

- id: MECH-158
  title: "A developing REE agent that acknowledges all five axioms but concludes 'love exists but not for me' undergoes ethical motivation collapse: the benefit gradient (Axiom 5 / ARC-024) is not motivationally active for self-other relations, and the ethical architecture resolves to survival or domination strategies. This is the primary developmental failure mode for the caregiver requirement (INV-043)."
  claim_type: mechanism_hypothesis
  subject: development.love_exclusion_failure_mode
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-043
    - INV-029
    - INV-028
    - ARC-024
  epistemic_category: derivational
  what_would_answer: |
    This claim fuses two evidentiary legs that resolve by different methods
    and on different timescales. A result on one does not confirm or falsify
    the other.

    LEG A -- DERIVATIONAL (does REE's own axiom chain, as formally stated,
    actually entail this failure mode as a coherent internal consequence --
    checkable by working through the argument, not by running REE):
    Proof obligation: given the eight axioms (self, value, world/uncertainty,
    agency/vulnerability, other minds, responsibility-for-others, love,
    language-as-repair) held structurally fixed, does the ABSENCE of an
    internalised "I am loveable" belief entail that the benefit gradient
    (ARC-024) is motivationally inert specifically for the self-other
    direction, while remaining active for self-directed valence -- as opposed
    to merely correlating with worse outcomes for some other reason (e.g.
    general developmental impoverishment, reduced training signal overall,
    or confounded absence of any caregiver interaction rather than absence of
    a LOVE-specific signal)? This is the same proof obligation INV-043's own
    Leg A names, and INV-043's what_would_answer already states this claim's
    prose derivation "substantially sketches" it. The residual step is
    formal: stress-test whether the derivation holds under every admissible
    configuration of E1-E3 and the axiom set, or only under some -- and
    whether "motivationally active" is given a definition precise enough that
    the claim is falsifiable at all rather than a restatement of its own
    premise.
    CONFIRMING: the derivation is shown to hold non-trivially and uniquely
    attributes the collapse to the missing loveability-internalisation
    signal specifically (not to any correlated developmental deficit).
    FALSIFYING (VIOLATED BY): either (i) a proof or worked counterexample
    showing a fully-specified REE architecture converges to genuine
    other-directed care regardless of loveability-internalisation, given
    otherwise-adequate developmental exposure -- i.e. the axiom chain alone
    is sufficient, making INV-043/MECH-158 unnecessary; or (ii) the
    derivation is shown to hold only as a restatement of its own defining
    premise, with no independent mechanistic content beyond "an agent that
    lacks X lacks X."

    LEG B -- SUBSTRATE_CONDITIONAL (once multi-agent caregiving substrate
    exists, does REE's actual benefit-gradient/z_goal machinery produce the
    predicted self-vs-other dissociation empirically):

    NON-DEGENERACY PRECONDITION (currently UNMET, all four required):
    (i) a multi-agent substrate (ARC-047 / SocialGridWorld) -- UNBUILT,
        status: candidate, confidence 0.0, implementation_phase v5, no
        construction activity;
    (ii) a caregiving-interaction model representing POSITIVE-VALENCE vs
         NEUTRAL/INSTRUMENTAL vs HOSTILE/ABSENT caregiver behaviour
         directed AT the developing agent specifically (ARC-010 mirror
         modelling extended and specialised) -- UNBUILT: zero references
         to "other_selflike," "mirror_model," or any social/other-agent
         representation anywhere in ree_core/ (confirmed 2026-08-08);
    (iii) a measurable "internalised loveability" state variable,
          dissociable from mere caregiver-reward prediction -- UNBUILT:
          zero references to "loveable" or "self_valence" anywhere in
          ree_core/; and
    (iv) instrumentation on the EXISTING self-directed half of the
         mechanism (z_goal / benefit_exposure in ree_core/goal.py, which
         IS built and evidenced via ARC-024) extended to log a comparable
         signal when the goal-relevant outcome belongs to a modelled OTHER
         rather than the self.

    CONFIRMING: once (i)-(iv) land, an agent developed WITHOUT positive-
    valence caregiver interaction shows the z_goal/benefit-gradient signal
    activating normally for self-directed outcomes but failing to activate
    (or activating at a measurably attenuated magnitude) for outcomes
    belonging to a modelled other -- AND this agent's downstream E3 action
    selection shows a measurable shift toward self-preservation-instrumental-
    other strategies relative to an otherwise-identical agent raised WITH
    positive-valence caregiver interaction. The dissociation must track the
    loveability signal specifically, not merely total caregiver-interaction
    volume.

    FALSIFYING (VIOLATED BY): once (i)-(iv) land, either (a) the z_goal/
    benefit-gradient signal generalises from self to modelled-other
    regardless of loveability-internalisation status; or (b) agents without
    loveability-internalisation show NO measurable behavioural divergence
    toward survival/domination strategies at all.

    DISPOSITION: epistemic_category: derivational, since the title's primary
    assertion -- the four-step chain from "axioms present, loveability
    absent" to "benefit gradient motivationally inert for self-other" to
    "collapse to survival/domination" -- is fundamentally an argument about
    what REE's own axiom and mechanism DEFINITIONS entail, resolvable in
    principle by formal analysis without building anything. Leg B is a
    genuinely distinct, substrate_conditional sub-question currently blocked
    at the non-degeneracy precondition, not the confirming/falsifying step.

    GOVERNANCE FLAGS (2026-08-08 digestion pass, not resolved here): (1)
    MECH-158 substantially overlaps INV-043's own Leg A -- INV-043's
    what_would_answer already treats MECH-158's prose derivation as the
    content of its derivational leg. A reasonable long-run outcome is that
    INV-043 Leg A simply IS MECH-158, cross-referenced rather than
    independently re-derived in both places. (2) live numbering
    inconsistency: this claim's title says "Axiom 5 / ARC-024" while
    developmental_curriculum.md's own derivation text (line 325) says
    "Axiom 7 / ARC-024" for the benefit gradient.
  location: docs/architecture/developmental_curriculum.md#mech-158
  source:
    - docs/thoughts/2026-04-01_Caregivers_childhood_moral_development.md
  notes: >
    Flagged for Synthese/Minds and Machines paper Discussion section.
    This failure mode is the counterexample that tests whether the five-axiom
    derivation ("Love Once Means Love All") holds: it identifies the case where
    love expanding under intelligence fails to initialise.

- id: MECH-159
  title: "Moral progress in REE is hypothesised to be intergenerational: childhood plasticity enables belief formation and world-model development while adult stability limits further world-model restructuring, requiring each generation to improve developmental starting conditions for the next; caregiving and raising next-generation agents is itself part of ethical obligation (INV-042)."
  claim_type: mechanism_hypothesis
  subject: social.intergenerational_moral_progress
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-043
    - ARC-019
    - ARC-010
    - MECH-052
    - INV-042
  location: docs/architecture/social.md#mech-159
  source:
    - docs/thoughts/2026-04-01_Caregivers_childhood_moral_development.md

# ---- Claims registered 2026-04-02 from literature pull batch discussion ----

- id: MECH-025b
  title: "High-precision action mode carries responsibility attribution: the precision level at which an action was committed determines the degree of ethical accountability assigned to its consequences."
  claim_type: mechanism_hypothesis
  subject: cognitive_modes.action_doing_responsibility_linkage
  polarity: asserts
  status: candidate
  epistemic_category: standard
  live_status:
    reading: candidate
    as_of: 2026-08-03
    needs_review: false
    evidence:
      from: "failure_autopsy_mech025-cluster-876-671b_2026-08-03"
      as_of: 2026-08-03
      verdict: weakens/standard
  depends_on:
    - MECH-025
    - ARC-016
    - INV-012
    - SD-003
  notes: >
    Decomposed from MECH-025 (2026-04-02). The original MECH-025 combined precision
    modulation (supported by Friston 2013 active inference, Wen & Haggard 2020 agency
    prediction error) with responsibility linkage. Literature supports precision
    modulation of motor control as a mechanistic claim, but the responsibility
    linkage -- where high precision IMPLIES ethical accountability -- is a philosophical
    bridge, not a neuroscience finding. Friston's framework is domain-general; it does
    not distinguish "precise because risky" from "precise because difficult." The
    responsibility attribution requires SD-003 (self-attribution via counterfactual E2)
    in addition to precision modulation.
    This is a claim about the RELATIONSHIP between precision and ethical accountability,
    not about either mechanism in isolation. It asserts: actions committed at higher
    precision carry higher residue weight because the agent was in a state where it could
    have done otherwise with finer discrimination. Low-precision actions (reflexive,
    unconsidered) carry lower accountability -- the agent's discrimination capacity was
    not engaged. This maps onto the philosophical distinction between negligence and
    deliberate action.
    Registered 2026-04-02 from MECH-025 decomposition during literature pull discussion.
  evidence_quality_note: >
    [2026-06-12 governance / confirmed failure_autopsy_batch9_2026-06-12 applied]:
    V3-EXQ-671 (precision-responsibility, the first MECH-025b test) FAILed but is
    DEGENERATE / non_contributory, not claim pressure. C1 (precision<->residue
    correlation) and C2 (high/low-precision residue ratio) were both exactly 0.0
    across 778 committed harm-events because ResidueField.total_residue never
    accumulates under committed harm -- there is no residue gradient for precision to
    modulate. The "mixed" direction was only the 4/6 arithmetic (C3-C6 are
    health/sample preconditions). The claim's depends_on (SD-003 self-attribution via
    counterfactual E2 -- the 28-FAIL formal-import claim; ARC-016 -- v3_pending held)
    are not confirmed operative, so the linkage could not express itself. manifest
    non_degenerate set false. Retest = V3-EXQ-671a: add a residue-accumulation
    positive-control gate (residue must move under committed harm before the
    precision<->residue correlation is read). No confidence change; first MECH-025b run.

    [2026-08-02 governance, V3-EXQ-671a, confirmed failure_autopsy_V3-EXQ-671a_2026-08-02, corrected retest of degenerate V3-EXQ-671, USER-CONFIRMED]: 4 well-documented instrument fixes landed (update_residue never called in 671, wrong commitment field, benefit/harm sample pollution, environment-sourced ownership) and a residue-accumulation positive-control gate was added, which CLEARED (1.86 vs 1e-6 floor) -- confirming the fixes worked. C1 (precision_residue_correlation=0.0505) and C2 (high_precision_residue_ratio=0.9214) both FAILed cleanly against a >0.15 / >1.1 bar. However no equivalent positive-control exists yet for precision VARIANCE itself, the exact historical V2 failure signature for this substrate (MECH-025 carries evidence:None); single seed, n=29. Reads as measurement_gap / inconclusive, not yet claim pressure against MECH-025b. Route: /queue-experiment V3-EXQ-671b -- add a precision-variance positive-control gate plus 3-5 seeds.

    [2026-08-03 governance, V3-EXQ-671b, confirmed failure_autopsy_mech025-cluster-876-671b_2026-08-03,
    first fair, fully-gated test]: both positive-control gates (residue accumulation, precision
    variance) clear cleanly; the correlation the claim predicts (precision_residue_correlation >
    0.15) reads -0.0446, and the ratio test (> 1.1) reads 1.0618 -- both null, consistently across
    the 3 seeds with real committed-step spread. Unlike MECH-025 (same cluster, V3-EXQ-876), this
    test does not depend on MECH-025's own operationalization succeeding -- it correlates E3's own
    precision value directly against residue accumulation, both independently confirmed live this
    run. weakens/standard -- real claim pressure against the precision-implies-accountability bridge
    specifically. No biological reference exists to check this against (the claim is explicitly a
    philosophical construct, per its own 2026-04-02 decomposition note, not a translated neuroscience
    finding). NO DEMOTION this cycle -- single result, pooled n modest (179, unevenly distributed
    across seeds: 29/72/77/1). Note only; status stays candidate.
  evidence: []

- id: MECH-160
  title: "The E2 action_bias pathway (MECH-151, OFC analog) and E3 terrain_weight pathway (MECH-152, vmPFC analog) have different update rates: action_bias updates rapidly (single-trial, OFC-like) while terrain_weight integrates over longer timescales (vmPFC-like)."
  claim_type: mechanism_hypothesis
  subject: e1_e3.ofc_vmpfc_rate_asymmetry
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  depends_on:
    - ARC-041
    - MECH-151
    - MECH-152
    - ARC-042
    - ARC-044
  notes: >
    Registered 2026-04-02 from literature pull discussion. Rudebeck & Murray 2024
    confirm that OFC updates rapidly (single-trial reversal learning) while vmPFC
    integrates over longer timescales. ARC-041 currently treats the E2 action_bias
    and E3 terrain_weight as simultaneous outputs from a single retrieval step.
    This claim asserts they should have different update rates, matching the OFC/vmPFC
    temporal distinction.
    Implementation options: (1) separate learning rates for cue_action_proj vs
    cue_terrain_proj; (2) different EMA decay constants for action_bias vs terrain_weight;
    (3) action_bias updates every step while terrain_weight updates every N steps.
    This may share an underlying reason with the E1/E2 developmental training differential
    relative to E3 (ARC-042 staged development): faster subsystems must establish their
    representations before slower subsystems can build on them.
    Biological: OFC reversal learning in 1-3 trials (Rudebeck et al. 2013); vmPFC value
    updating over 10-50 trials (Noonan et al. 2010). Ratio ~3-15x. In REE terms, this
    suggests cue_action_proj learning rate should be 3-15x higher than cue_terrain_proj.
  evidence: []

- id: MECH-161
  title: "Ready vigilance (MECH-026) requires an arousal regulator that maintains an optimal sensitivity level on the LC-NE inverted-U curve, implemented via MECH-093 heartbeat frequency modulation rather than a binary high/low precision switch."
  claim_type: mechanism_hypothesis
  subject: cognitive_modes.vigilance_arousal_regulator
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  depends_on:
    - MECH-026
    - MECH-093
    - ARC-016
    - ARC-044
  notes: >
    Registered 2026-04-02 from literature pull discussion. Langner & Eickhoff 2013
    meta-analysis and Oken et al. 2006 establish that the LC-NE system operates on
    an inverted-U curve: too little norepinephrine and signals are missed; too much and
    the system becomes hypervigilant (false alarms, distractible). Optimal vigilance
    sits at the peak.
    MECH-026 currently implies setting precision high for ready vigilance. This claim
    asserts that optimal vigilance requires a SPECIFIC precision level, not maximum,
    and that MECH-093's heartbeat frequency modulation (z_beta -> E3 update rate) is
    the implementation mechanism. The heartbeat rate IS the arousal parameter, expressed
    as a temporal sampling frequency: too fast = hypervigilant (sampling noise, wasting
    computation); too slow = missed signals (under-sampling threats).
    The arousal regulator must find the peak of the inverted-U. In REE, this could be
    implemented as a z_beta-gated controller that adjusts heartbeat rate based on the
    derivative of E3 harm_eval information gain: if increasing the rate produces
    diminishing returns, the system is past the peak.
    MISSING CONSTRAINT -- FATIGUE: Biological vigilance degrades over time (the vigilance
    decrement, Langner & Eickhoff 2013). REE's computational precision has no metabolic
    cost. Whether fatigue is informationally relevant (attentional lapses serve an
    explore-vs-exploit function) or merely a biological limitation is an open question.
    Biology suggests checking whether any LC-NE fatigue dynamics translate to software.
    If not, design blind and test.
  evidence: []

- id: MECH-162
  title: "z_resource (SD-015, object-identity encoding) and z_world (spatial-contextual encoding) separate at the feedforward representation stage but must re-converge at the hippocampal planning stage for goal-directed navigation."
  claim_type: mechanism_hypothesis
  subject: goal_representation.resource_world_convergence
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  depends_on:
    - SD-015
    - SD-005
    - ARC-007
    - SD-004
  notes: >
    Registered 2026-04-02 from literature pull discussion. DiCarlo & Cox 2007 and
    Rolls 2021 support separating object identity from spatial context at the
    feedforward encoding stage (ventral/dorsal split). But Whittington et al. 2022
    (Tolman-Eichenbaum model) shows that cognitive maps FUSE object identity with
    spatial structure through a shared relational manifold. The architectural question
    is whether z_resource feeds into the hippocampal planning module alongside z_world,
    enabling the planner to reason about "where resources are likely to be" given
    current spatial context.
    Current REE architecture treats z_resource and z_world as independent streams.
    This claim asserts they must converge for planning: the hippocampal module needs
    both "what to seek" (z_resource) and "where things are" (z_world) to generate
    goal-directed trajectory candidates.
    The different permutations of having z_resource alone, z_world alone, and both
    fused -- and the differences in when they are fused vs separated, and how they
    might feed forward, back, or laterally -- are unresolved and need systematic
    experimental testing. See Q-030.
    Biological: ventral stream (IT cortex) encodes object identity; dorsal stream
    (parietal cortex) encodes spatial layout; hippocampus receives both and constructs
    a unified cognitive map (Eichenbaum 2000, Whittington et al. 2022). The separation
    is a representation-level choice; the convergence is a planning-level requirement.
  evidence: []

- id: Q-030
  title: "What are the optimal permutations of z_resource and z_world separation, fusion, and information flow direction (feedforward, feedback, lateral) across REE's processing stages?"
  claim_type: open_question
  subject: goal_representation.resource_world_permutations
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Treat this as a 6-cell design-space sweep, one ablation cohort per dimension,
    on the V3 SD-054 substrate with z_resource implemented (SD-015,
    drive_weight=2.0) and z_world stable. (1) ENCODING: shared vs separate
    encoders for z_resource and z_world; ANSWER separate-encoding-optimal if
    separated encoders beat shared on resource-vs-world discrimination AND
    committed-policy quality on a task requiring both channels, lower on either =>
    shared. (2) E2 INPUT SCOPE: feed E2 z_world-only vs z_world+z_resource; better
    fast-transition prediction with the wider scope decides. (3) HIPPOCAMPAL
    FUSION: fuse-at-proposal vs fuse-at-eval; whichever lifts trajectory quality.
    (4) E3 CHANNEL STRUCTURE: separate vs merged resource/world authority channels
    at E3; (5) FEEDBACK: with vs without E3->E1/E2 feedback path; (6) LATERAL
    BINDING: with vs without lateral z_resource<->z_world cross-talk. For EACH
    cell the discriminating result is a >=1 SD-of-delta separation on the cell's
    own metric (with an absolute floor so a reproducible substrate cannot collapse
    the SD to ~0), on >=2/3 seeds. HARD PRECONDITION: each ablated pathway must be
    live (non-zero variance carried on that channel) before its removal can be
    read; a dead channel yields a vacuous null, not a verdict. The question is
    ANSWERED per-cell, not globally -- output is 6 sub-claims (MECH/SD) each with
    its own evidence trail, not a single yes/no. No substrate enrichment required;
    the only bottleneck is running the cohort.
  epistemic_category: standard
  depends_on:
    - SD-015
    - SD-005
    - MECH-162
    - ARC-007
  evidence_quality_note: |
    Lit-pull 2026-04-29 (lit-pull-am-Q029-Q030 session) added 5 entries: Staresina 2011
    J Neurosci PrC/PhC fMRI dissociation (supports), Lee/Inah 2021 Hippocampus GIST
    model (supports), Howard & Kahnt 2017 J Neurosci OFC identity-specific reward x vmPFC
    general value (supports), Kim 2015 Front Behav Neurosci DG-disrupted Bax-KO
    (supports), Locatello 2020 NeurIPS Slot Attention (supports). lit_conf 0.874 from
    5 entries; quadrant=plausible_unproven, exp_conf=0.

    EPISTEMIC-CATEGORY FLAG (2026-05-02, Phase 3 Q-walk): V3-tractable. The 6 sub-questions
    in the notes (encoding shared/separate, E2 input scope, hippocampal fusion, E3
    channel structure, feedback paths, lateral binding) are all directly testable
    ablation questions in the V3 substrate -- z_resource is implemented (SD-015,
    drive_weight=2.0 default), z_world stable, hippocampal proposal substrate present.
    No substrate enrichment required; the bottleneck is experimenter time. The
    narrow_open_question recommendation is misleading because the question doesn't
    converge to a single "narrow it" answer -- it's a 6-cell design-space sweep that
    needs a coordinated experiment cohort. Right next step: split Q-030 into 6
    sub-questions (one per design-space dimension), queue ablation experiments per
    cell, accumulate evidence per sub-question. This is the kind of question that
    becomes 6 MECH or SD claims with their own evidence trails.

- id: ARC-044
  title: "Context-dependent neuromodulatory gain is a single unifying mechanism expressed at multiple levels: ARC-016 (temporal precision from prediction error variance), MECH-152 (contextual terrain weight from cue-indexed retrieval), MECH-026 (arousal-mediated vigilance sensitivity), and MECH-093 (z_beta heartbeat frequency modulation) are all facets of a global arousal/precision signal (LC-NE) modulated by a context-specific gain signal (vmPFC/OFC)."
  claim_type: architectural_commitment
  subject: architecture.neuromodulatory_gain_unifier
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-016
    - MECH-152
    - MECH-026
    - MECH-093
    - MECH-161
    - ARC-041
  what_would_answer: >
    CURRENT EVIDENCE STATE (2026-08-07 digestion pass): ARC-044 has never been tested, in whole or
    in part. No experiment has varied context and temporal state jointly against either MECH-152 or
    ARC-016, and no ARC-044-tagged queue entry or proposal exists (checked evidence/experiments/,
    manual_proposals.v1.json, experiment_proposals.v1.json, governance_agenda.v1.json,
    substrate_queue.json, ree-v3/experiment_queue.json). The claim's own notes already specify the
    falsifying design in full: vary context (hazard-gradient vs resource-proximate, i.e. MECH-152's
    cue_context axis) and temporal state (stable vs volatile, i.e. ARC-016's running_variance regime)
    simultaneously, and read off whether the two gains combine MULTIPLICATIVELY or merely
    ADDITIVELY/independently.

    NON-DEGENERACY PRECONDITION: the two mechanisms under test must each be independently confirmed
    live in the substrate before a null interaction can be read as evidence of independence, rather
    than as one arm being a wiring gap. ARC-016 (temporal gain): CONFIRMED live -- status stable,
    gate_green, mean_rho_rv_vs_commit=-0.955 (V3-EXQ-818, 2026-07-25); the substrate already computes
    volatility_estimate = var(running_variance) over a sliding window (Q-007,
    ree_core/predictors/e3_selector.py), so the stable-vs-volatile axis is directly instrumented.
    MECH-152 (contextual gain), harm pathway only: CONFIRMED live -- V3-EXQ-194/194a C1 PASS 3/3,
    r_w_harm=0.70 (hazard-gradient context drives w_harm as specified). The GOAL pathway (w_goal) is
    FAIL (r=-0.007, flat) and is explicitly OUT OF SCOPE for this test -- the design must manipulate
    context along the harm axis, not attempt to recruit the unconfirmed goal pathway. A run in which
    either running_variance or w_harm shows negligible variance across its manipulated conditions
    must self-route substrate_not_ready / non_contributory, not be read as a test of ARC-044.

    SCOPE NOTE: the claim's title also names MECH-026 (arousal-mediated vigilance sensitivity) and
    MECH-093 (z_beta heartbeat-rate modulation) as additional facets of the same unifying mechanism.
    MECH-093's rate-channel is independently confirmed (V3-EXQ-097b/396b/505), but MECH-026 has zero
    experimental evidence of its own and explicitly defers its mechanism to MECH-161, whose specific
    optimal-arousal-regulator controller is described only as something that "could be implemented
    as" a z_beta-gated controller -- i.e. it reads as UNBUILT, not merely untested (MECH-161:
    evidence: [], status: candidate). A full 4-facet unification test is therefore NOT testable now;
    this what_would_answer scopes to the 2-facet (MECH-152 x ARC-016) test the claim's own notes
    fully specify and that both required substrates already support.

    CONFIRMING: a statistically significant multiplicative interaction term on the combined
    precision/gain readout (e.g. a 2x2 hazard-gradient/resource-proximate x stable/volatile design;
    interaction effect on effective_harm_precision, or equivalently on E3 harm-evaluation
    variability/commit-rate, exceeding what either main effect alone predicts) -- consistent with
    MECH-152 and ARC-016 being the same gain-control mechanism at different levels of description.
    Per the gain-control literature already cited in MECH-152's notes (Kanashiro et al. 2017), the
    confirming signature should also show that high w_harm x low running_variance jointly REDUCES
    harm-evaluation variability (signal-to-noise gain), not merely scales its magnitude --
    distinguishing genuine shared precision-gain machinery from two independent magnitude-scaling
    knobs that happen to both multiply into the same downstream score.

    FALSIFYING: the context x temporal-state interaction term is additive or null -- effects of
    w_harm and 1/running_variance combine linearly (or not at all) on the harm-evaluation readout,
    with no interaction term exceeding the main-effects prediction. This would falsify ARC-044's
    single-mechanism claim for the MECH-152/ARC-016 pair specifically: they would stand as REE
    currently implements them -- two independent gain knobs that happen to both scale harm
    evaluation, not facets of one underlying neuromodulatory gain architecture. A null result
    obtained despite both non-degeneracy preconditions holding (running_variance and w_harm both
    show healthy variance across their manipulated conditions) is a clean falsifier, not an
    instrument failure.
  notes: >
    Registered 2026-04-02 from cross-cutting theme identified during literature pull
    batch. Several lit-pull discussions converged on the same architectural question:
    the relationship between precision, gain control, and arousal.
    The literature consistently points to a two-layer gain architecture:
    (1) Global arousal signal (LC-NE): sets the overall sensitivity/precision level.
        Modulates all cortical processing simultaneously. Inverted-U curve.
    (2) Context-specific gain signal (vmPFC/OFC via ARC-041 cue-weighting circuit):
        modulates precision PER evaluation channel based on sensory context.
    REE currently fragments this into multiple independent mechanisms:
    - ARC-016: temporal gain (running_variance -> commit threshold)
    - MECH-152: contextual gain (cue_context -> [w_harm, w_goal])
    - MECH-026: arousal gain (vigilance sensitivity level)
    - MECH-093: temporal-rate gain (z_beta -> heartbeat frequency)
    ARC-044 asserts these are all implementations of the same neuromodulatory
    architecture and should compose coherently. Effective precision at any evaluation
    channel = f(global_arousal, contextual_gain, temporal_precision, heartbeat_rate).
    The gain control literature (Kanashiro et al. 2017) shows that gain modulation
    also controls signal-to-noise ratio, not just magnitude -- high gain makes
    evaluation more precise (lower variability), not just louder.
    This claim ties together the control plane loose ends identified during the
    literature review and provides a framework for testing them jointly.
    Experimental implications: a single experiment varying both context (hazard vs
    resource-proximate) and temporal state (stable vs volatile) should show
    multiplicative interaction between MECH-152 and ARC-016. If they are truly
    facets of one mechanism, the interaction should be multiplicative; if independent,
    additive.
  evidence: []

- id: MECH-163
  title: "NARROWED 2026-07-22 to leg (1) only; RESTATED 2026-07-27 (model-free -> depth-limited). Two dissociable goal-directed PATHWAYS operate in parallel -- a HABIT pathway that is a myopic, DEPTH-LIMITED read of the forward model (SD-081 dualsystem_habit_depth, SNc/dorsal-striatum analog) sufficient for approach in practised contexts, and a hippocampally-planned pathway that is the FULL-HORIZON read of the SAME model (VTA/ventral-striatum+PFC analog) -- AND THE PLANNED PATHWAY IS PREFERENTIALLY RECRUITED IN NOVEL CONTEXTS. E1 2026-07-27: the descriptor `model-free` was STRUCK. ree_core contains no model-free machinery of any kind (no cached S-R values, no TD error, no Q-table) and never has; SD-081 implements the habit read as _score_depth_limit truncation of the same model-based E3 scorer, so the two pathways are ONE model read at two grains. E2: WEAKENED by V3-EXQ-786b (2026-07-24, non_degenerate, familiarity AUC 0.848 against a 0.7 bar): C1_recruitment_higher_on_novel FAILED with two pathways and no arbitrator. 786b is the repaired re-test of V3-EXQ-786a, whose own `weakens` was WITHDRAWN 2026-07-24 for a degenerate DV -- leg (1) is therefore NO LONGER experimentally untested. Leg (2) long-horizon benefit accumulation and leg (3) prosocial planning were SPLIT OUT 2026-07-22 to MECH-478 and MECH-479; each carried a distinct blocker and both were silently inheriting a refutation they did not earn. The control-ALLOCATION mechanism this claim presupposes but never named is now MECH-477 (uncertainty-based arbitration), SUPPORTED by V3-EXQ-811a (2026-07-24, PASS). Restatement rationale: evidence/planning/claim_synthesis_MECH-163_2026-07-27.md."
  claim_type: mechanism_hypothesis
  subject: goal_directed.dual_system_novel_context_recruitment
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-27
    needs_review: false
    evidence:
      from: v3_exq_786b_mech163_dual_system_recruitment_20260724T123825Z_v3
      as_of: 2026-07-24
      verdict: weakens/no_differential_recruitment
  implementation_phase: v3
  depends_on:
    - ARC-007
    - ARC-021
    - MECH-112
    - SD-012
    - INV-029
    - ARC-071     # transition mechanism (planned -> habitual chunk); added 2026-05-10 per ARC-071 lit-pull R3 verdict
    - MECH-477    # control-allocation arbitrator; added 2026-07-22 per claim_synthesis_MECH-163_2026-07-22
  notes: >
    Registered 2026-04-02 from architectural discussion about V3/V4 boundary.
    Matches the SNc/VTA dopaminergic dual split and the model-free/model-based
    distinction in computational psychiatry (Balleine & O'Doherty 2010; Daw et
    al. 2005; Dayan & Daw 2008).

    Habit system (SNc -> dorsal striatum): caches S-R associations from cumulative
    reward experience. Sufficient for resource approach in environments where the
    agent has practiced sufficiently. Does NOT require an internal model or
    multi-step rollout. SD-012 (drive-scaled benefit amplitude) activates the
    approach drive in this system.

    VTA/hippocampal system (VTA -> nucleus accumbens + PFC, hippocampal rollout):
    uses HippocampalModule trajectory generation to predict multi-step reward
    consequences. Required for: (1) novel contexts where habit has not formed;
    (2) long-horizon benefit accumulation (sustained joy is a trajectory property,
    not a single contact event); (3) prosocial planning -- "sharing joys and
    sorrows" (INV-029 benefit gradient) requires planning trajectories that affect
    ANOTHER agent's z_harm_a accumulation and benefit_exposure over time, which is
    structurally inaccessible to 1-step greedy.

    V3/V4 boundary implication: the habit system demonstrates that goal
    representations are real (SD-012 + EXQ-182a satisfies V3 first-paper gate).
    V3 FULL completion requires the VTA/hippocampal system validated, because V4's
    social extension has no planning substrate without it. One-step greedy cannot
    predict or navigate toward states that sustain another agent's joy or reduce
    another's sorrow over time.

    In REE terms:
    - Habit: E3 trajectory scoring using cached benefit/harm evaluation on
      value-flat HippocampalModule proposals (ARC-007 strict).
    - VTA/hippocampal: z_goal gradient shapes which trajectories HippocampalModule
      proposes; E3 evaluates multi-step rollouts rather than post-hoc scoring
      random candidates.
    Both systems share HippocampalModule; distinction is in whether proposal
    generation is value-flat or goal-seeded.
  evidence: []
  evidence_quality_note: >
    Governance 2026-04-03: hold_pending_v3_substrate applied. implementation_phase=v3,
    9 supports from lit entries, 0 genuine exp entries. No V3 experimental evidence on
    file for the dual-system distinction. Standard V3-pending gate: hold until V3
    experiments demonstrate dual-system discrimination.

    2026-05-10: depends_on extended to include ARC-071 per R3 verdict of
    targeted_review_arc_071_composition synthesis (lit_conf 0.848). The lit-pull
    confirmed ARC-071 (policy.composition_via_repeated_grounding) IS the missing
    transition mechanism that MECH-163 dual systems presupposes but does not
    specify. MECH-163 names the PRESENCE of two systems (planned + habitual);
    ARC-071 is the machinery that pumps content from planned-into-habitual.
    Without ARC-071, the dual systems in MECH-163 are static configurations;
    with ARC-071, the division of labour shifts continuously with experience.
    Causal evidence: Smith & Graybiel 2013 -- selective optogenetic disruption
    of infralimbic cortex during overtraining PREVENTS habit formation, i.e.
    breaks the planned -> habitual transition. System-level evidence: Yin &
    Knowlton 2006 -- DMS-to-DLS transfer over training is the system-level
    signature of the transition. The depends_on update closes a gap that has
    been hiding in plain sight since MECH-163 was registered 2026-04-02.

    HISTORICAL GOVERNANCE NOTES:
    - 2026-04-06 (illusory conflict resolution risk + needs task redesign): EXQ-237a
      non-contributory (z_goal substrate limitation). Even the habit condition showed
      zero lift in simple tasks (lift_simple=0.0), suggesting the test paradigm itself
      was flawed -- habit formation not observable in the current task design.
    - 2026-04-14: EXQ-327 ran with goal_weight in E3 trajectory scoring on value-flat
      HippocampalModule proposals. The result cleared the SD-015 (z_resource encoder +
      goal-term scoring) substrate path -- the HABIT ARM of MECH-163's dual-system
      distinction.
    - 2026-04-27 (R1 governance fix per claim_ids accuracy rule REE_assembly/CLAUDE.md):
      EXQ-327 dropped from MECH-163 evidence (claim_ids tag corrected to ['SD-015']
      only, run-pack manifest + flat JSON updated). EXQ-327 has only the habit arm in
      it; it cannot discriminate the two systems and should not weight MECH-163's
      evidence record either way. The habit-arm clearance now belongs to SD-015 alone.

    STILL-PENDING DISCRIMINATIVE TEST -- V3 FULL-COMPLETION GATE:
    A paradigm where (a) 1-step / habit-policy approach is insufficient AND (b)
    goal-seeded multi-step trajectory generation by HippocampalModule produces a
    discriminable behavioural advantage. Two paradigms in scope for the first pass:
    (A) detour / blockage within a familiar grid (the habit-cached short path becomes
    obstructed mid-episode; planned arm generates a novel multi-step detour seeded by
    z_goal); (B) novel-context generalisation (train in env A, test in topologically-
    novel env B with same resource semantics). See
    REE_assembly/docs/architecture/mech163_v3_full_completion_gate_session_prompt.md.

    SUBSTRATE PREREQUISITES IN PLACE (this is a writing task, not a substrate task --
    EXCEPT for the proposal-generation hook):
    - HippocampalModule trajectory CEM (ARC-007 strict, value-flat proposals).
    - z_goal seeding via SD-012 (drive_weight=2.0) + SD-015 (z_resource encoder).
    - ARC-030 D1/D2 approach/avoidance competition.
    - SD-039 anchor goal-snapshot payload (substrate-side landed 2026-04-26).

    SUBSTRATE PREREQUISITES PENDING (gate Option 2 path):
    - SD-039 module-level write-site population (population layer deferred; queued as
      next /implement-substrate target after MECH-269b / MECH-295 land).
    - V3-EXQ-494 falsifiable test for the SD-039 population layer (drafted 2026-04-27,
      not yet queued; see ree-v3/experiments/v3_exq_494_sd039_anchor_payload_validation.py).
    - MECH-292 (ranked ghost-goal bank) -- candidate, blocked on SD-039 population.
    - MECH-293 (waking ghost-goal probe search) -- candidate, blocked on MECH-292;
      this is the HippocampalModule.propose_trajectories() hook the gate's Option 2
      will call (anchor-payload-driven proposal seeding rather than CEM action-prior).

    Gate is approximately 2 substrate sessions away assuming nothing slips: SD-039
    population + V3-EXQ-494 in one session, MECH-292 + MECH-293 in a second, then the
    V3 full-completion gate experiment (V3-EXQ-495 or higher).

    ADJUDICATED 2026-07-20 (V3-EXQ-786, confirmed failure_autopsy_V3-EXQ-786_2026-07-20)
    -- NON-CONTRIBUTORY / measurement_test_design_defect. STATUS UNCHANGED (candidate);
    this run adds NO evidence in either direction and must not be read as weakening the
    dual-system distinction. The manipulation check failed: novel and familiar were
    never established as distinct conditions, so the load-bearing C1
    (recruitment_higher_on_novel) is stamped criteria_non_degenerate false and there was
    no live contrast for it to measure.
    THE RUN'S OWN SELF-ROUTE IS REJECTED. The manifest self-routed
    substrate_not_ready_requeue and the indexer flagged it precondition_unmet; the
    autopsy rejects that reading on measurement. FamiliarityTracker IS present and IS
    discriminating -- it returned a separation of 0.049365 against a 0.05 floor, short by
    1.3%. That is a test-design defect, not substrate immaturity, and the distinction is
    load-bearing: trusting the self-route would have minted a substrate_queue entry for a
    substrate that is not broken. recommended_substrate_queue_entry.action = none.
    SCOPE OF THAT REJECTION: ARC-071 (unbuilt) and ARC-007 (STRICT value-flat) DO block
    MECH-163 legs 2 and 3 and remain live blockers tracked above -- but neither caused
    this FAIL, and this run exercised neither. The autopsy's finding is confined to leg 1's
    manipulation check.
    SUCCESSOR: V3-EXQ-786a (re-run with a repaired familiarity manipulation clearing the
    floor with margin). Surfaced by the 2026-07-20 governance cycle as owed work; NOT
    queued here -- experiment authoring goes through /queue-experiment.
    OPEN DESIGN QUESTION worth resolving before 786a is written: a 1.3% miss on a
    hand-chosen 0.05 separation floor means the verdict turned on the threshold, not on
    the phenomenon. Whether 0.05 is the right floor -- and whether separation is the right
    statistic for "these are distinct conditions" -- should be re-posed rather than
    inherited.
    BANDWIDTH CAVEAT (annotated 2026-07-20 during V3-EXQ-786a authoring; adjudication,
    direction, category and routing above all UNCHANGED): the 0.049365 cited here was
    measured at familiarity_bandwidth = 1.0, where FamiliarityTracker.query's clamped-sum
    readout is SATURATED in BOTH conditions -- so it is residual noise near a saturated
    ceiling, NOT an achievable instrument ceiling, and must not be treated as the number a
    successor has to beat. At bw=0.20 the achievable separation is ~0.19 (~4x), which is
    what V3-EXQ-786a measures. This STRENGTHENS the "present and discriminating" finding
    and the rejection of the substrate self-route; only the reusable NUMBER was
    bandwidth-conditional.

    ADJUDICATED 2026-07-22 (V3-EXQ-786a, evidence, claim_ids=[MECH-163], WEAKENS;
    failure_autopsy_V3-EXQ-786a_2026-07-22). This is MECH-163's FIRST genuine V3
    experimental evidence (the 2026-04-03 hold recorded 9 lit supports, 0 experimental
    entries). The test was fair and the instrument was repaired before the result was
    taken: the manipulation check passed on both legs (familiarity discriminability
    AUC 0.848 against a 0.7 bar; SEM lower bound 0.795 against the 0.5 null), replacing
    786's raw mean-difference floor of 0.05 which was neither scale-free nor an overlap
    measure. n=8 seeds, non_degenerate true. The load-bearing criterion
    C1_recruitment_higher_on_novel failed on BOTH the absolute and standardized legs:
    recruitment delta mean 0.00435, SEM 0.0325, lower bound -0.0281 against a 0.02
    margin, Cohen's d 0.047. That is an ABSENT effect, not an underpowered one -- do NOT
    route to a power re-run. epistemic_category standard; explicitly NOT
    substrate_ceiling and NOT a measurement defect. Report lit_conf and exp_conf
    SEPARATELY: 9 lit supports and 1 experimental weakens do not blend.

    DECOMPOSED 2026-07-22 (/claim-synthesis; proposal
    evidence/planning/claim_synthesis_MECH-163_2026-07-22.md, user-approved per-child).
    MECH-163 is NARROWED IN PLACE to leg (1) -- the proposition 786a actually tested --
    and therefore retains the weakens, now correctly whole-claim-scoped by STRUCTURE
    rather than by a prose qualifier. That structural fix is the point: a prose scope
    note does not survive the indexer, so claim_evidence.v1.json would have attached this
    weakens to the WHOLE claim and legs (2) and (3) -- one of which has no V3 substrate
    in existence -- would have silently inherited a refutation they did not earn.
    Split out, each with its own blocker stated in its own right and evidence: []:
      - MECH-478 long_horizon_benefit_requires_model_based_rollout (leg 2), blocked by
        ARC-007 STRICT value-flat proposals.
      - MECH-479 prosocial_planning_requires_planned_system (leg 3), no V3 substrate;
        refiled implementation_phase v4.
    Registered as the missing mechanism this claim presupposed but never named:
      - MECH-477 dual_system_uncertainty_arbitration.
    The null's SHAPE is what identified it: a FLAT response (delta mean 0.0044, d 0.047,
    7 of 8 seeds within +/-0.15) is the signature of two pathways with NO ARBITRATOR. In
    the reference biology differential recruitment is produced by uncertainty/
    reliability-based arbitration (Daw, Niv & Dayan 2005, already on file in this
    claim's OWN targeted review since 2026-04-05, conf 0.79), not by the mere existence
    of two pathways. NOTE the granularity debt was therefore documented in MECH-163's own
    lit folder for three and a half months before 786a made it cost anything.
    ARC-071 (planned -> habit transfer) is NOT re-registered -- it already exists, is
    already in depends_on, and needs BUILDING not registering.

    2026-07-24 governance (786a DV-degeneracy re-adjudication; failure_autopsy_V3-EXQ-786a_2026-07-24_dv_degeneracy,
    confirmed, user-confirmed routing "Autopsy inline now"): the `weakens` recorded above is WITHDRAWN
    and revised to non_contributory. 786a's recruitment DV (`1 - spearman(full_horizon, first_step)`)
    took `first_step` from `world_seq[:, :1, :]` -- index 0 of the world-state sequence, the CURRENT
    state, identical across every candidate before any trajectory diverges. `first_step` is therefore
    a CONSTANT vector (independently re-verified at the source-code level: n_unique=1 across all 32
    candidates on 786a's own re-derived `_depth_scores`, range exactly 0). Spearman against a constant
    reduces to a comparison against an ARBITRARY stable-sort tie-break ordering -- pure noise.
    Simulated over 200 draws: mean 1.0173, sd 0.1871; the manifest's actual recruitment_rate 1.01725
    (per-layout sd 0.149-0.207) matches the simulated noise to FIVE SIGNIFICANT FIGURES. 786a's own
    `candidate_score_range_non_degenerate` readiness gate checked only the full-horizon range, never
    the first-step vector, so the run passed readiness cleanly while the DV itself was degenerate.
    THE RECORDED "FLAT RESPONSE" DOES NOT DISCRIMINATE LEG (1) EITHER WAY: a DV that returns
    noise-around-a-fixed-mean by construction cannot distinguish "flat because no arbitrator" from
    "flat because the instrument cannot see differential recruitment at all." Leg (1) therefore
    reverts to EXPERIMENTALLY UNTESTED (its pre-786a state: 9 lit supports, 0 experimental entries),
    pending a non-degenerate re-test (new letter, repaired instrument -- the 785->785a / 708->708a
    shape; gate every input to the DV's own statistic, not merely the more prominent one, per
    V3-EXQ-811's precedent). MECH-477 is NOT undermined: its motivating OBSERVATION (786a's flat
    response) is weakened as a rationale for registration, but 811's own design was built specifically
    to correct this exact defect (HABIT_DEPTH=2, new readiness gates on the habit vector's own range
    and distinct-value fraction) and measured a real, non-degenerate effect (Cohen's d 0.999 ON vs
    -0.158 OFF) per the confirmed failure_autopsy_V3-EXQ-811_2026-07-24 -- MECH-477 stands on 811's
    own merits. MECH-163 stays candidate; the split to MECH-478/479 and the MECH-477 registration are
    UNCHANGED by this re-adjudication (both stand on their own grounds, not on 786a's since-withdrawn
    reading).

    2026-07-24 (V3-EXQ-786b, evidence, claim_ids=[MECH-163], WEAKENS; run_id
    v3_exq_786b_mech163_dual_system_recruitment_20260724T123825Z_v3). RECORDED 2026-07-27 -- this run
    was in review_tracker as reviewed but had never been written into this claim, so the paragraphs
    above still asserted leg (1) was EXPERIMENTALLY UNTESTED four days after it had been tested.
    786b is the non-degenerate re-test 786a's DV-degeneracy autopsy called for, with the DV's own
    inputs gated this time. Manipulation check PASSED on the substantive leg (familiarity
    discriminability AUC 0.8484 against a 0.7 bar). non_degenerate true. The load-bearing
    C1_recruitment_higher_on_novel FAILED; interpretation label no_differential_recruitment.
    Leg (1) is therefore WEAKENED on a fair test with a repaired instrument, and this is MECH-163's
    first surviving genuine experimental evidence (786a's was withdrawn).
    READ IT WITH V3-EXQ-811a, WHICH IS THE OTHER HALF OF THE SAME DISSOCIATION and lands the opposite
    way: with SD-081's arbitrator ON, differential recruitment APPEARS (PASS/supports for MECH-477 and
    MECH-163; C1 6 of 7 divergent seeds, 85.7%; C2 novel-minus-familiar mean w_planned 0.194 against a
    0.01 threshold). Two pathways alone -> flat; two pathways plus an explicit arbitrator ->
    differential recruitment. That is precisely what the 2026-07-22 decomposition predicted when it
    registered MECH-477, and it means 786b's weakens should be read as weakening the NO-ARBITRATOR
    configuration of leg (1), not the existence of the two pathways. Report lit_conf and exp_conf
    separately as always: 9 lit supports do not blend with this.

    RESTATED 2026-07-27 (/claim-synthesis; proposal
    evidence/planning/claim_synthesis_MECH-163_2026-07-27.md, user-approved per-item). NOT a
    decomposition -- the Step-3 gate returned STOP (one genuine non-degenerate signature, 786b, which
    is a clean single falsification rather than a cluster; and this claim was already decomposed
    2026-07-22 and carries registered children). The defect fixed here is DESCRIPTIVE ACCURACY, not
    granularity: this claim described a substrate that was never built.
    (E1) THERE IS NO MODEL-FREE MACHINERY IN REE. Verified at source: ree-v3/ree_core/ returns zero
    hits for model_free / model-free / q_value / q_table / td_error / sarsa. What SD-081 actually
    builds as the habit pathway is _score_depth_limit = max(2, dualsystem_habit_depth) in
    ree_core/predictors/e3_selector.py, i.e. the SAME model-based scorer called on a z_world sequence
    truncated to its first 2 steps. Both of this claim's two systems are one model read at two grains.
    Consequence for 811a: it is evidence that DEPTH-arbitration produces the recruitment signature, NOT
    evidence for a cached-value second controller -- so it is fully compatible with Dezfouli and
    Balleine 2013's negative result, and does not rest on the descriptor just struck.
    (E3) REE ALREADY INSTANTIATES THE HIERARCHICAL ACCOUNT of habit, and never instantiated the flat
    one. ARC-071 chunks are spliced into the ARC-007 proposal pool as atomic value-flat Trajectories
    (ree_core/hippocampal/module.py, use_chunk_proposal_injection); the E3 selector has zero
    chunk-awareness and scores a chunk exactly like a primitive; a selected chunk is then committed
    ATOMICALLY (e3._committed_trajectory with metadata source arc071_chunk, stepped by
    _committed_step_idx in ree_core/agent.py). That is Dezfouli and Balleine's architecture: a single
    goal-directed process selecting between individual actions and habitual sequences, the sequence
    executing as a unit once selected.
    WHY THIS CLAIM IS NOT MERGED INTO MECH-323. On the hierarchical reading the habit leg reduces to
    MECH-323's chunk INVENTORY plus the GRAIN at which the planner can intervene -- and the grain is
    owned by the commitment machinery (beta gate + _committed_step_idx + the release set), which is not
    in MECH-323. The habit leg is MECH-323 COMBINED WITH the commit layer; folding it into MECH-323
    would lose the half that actually produces the phenomenon.
    STRUCTURAL OUTCOME-INSENSITIVITY (the finding worth carrying forward). A committed trajectory can
    be released mid-execution by exactly five mechanisms -- MECH-091 (acute z_harm threat), MECH-342
    (degraded execution readiness), the rung-6 natural-commit duration lever (with SD-033e frontopolar
    pressure, which reads the goal-proximity of FOREGONE ALTERNATIVES, the nearest analogue), MECH-321
    / ARC-070 (V_s prediction failure on the remaining macro), and SD-034 closure de-commit. NONE of
    them reads whether the committed outcome is still VALUABLE. So a committed chunk runs to
    completion regardless of devaluation: outcome-insensitivity produced by GRAIN, in a substrate with
    no model-free controller anywhere. Building a value-driven mid-commitment release would DESTROY the
    phenomenon under test and must therefore be sequenced AFTER the falsifier, not before.
    FALSIFIER (designed, NOT queued, and currently BLOCKED). Dezfouli and Balleine's own discriminating
    signature -- sequence coupling -- does not port: coupling is true BY CONSTRUCTION in REE once a
    chunk commits, so it would produce a guaranteed and meaningless PASS. The REE discriminator is a
    DOSE-RESPONSE of post-devaluation policy persistence on realised committed-macro grain, three arms
    (both knobs off / SD-081 arbitration on / ARC-071 chunk injection on) crossed with chunk_max_size
    in 2,3,5. Hierarchical reading supported if persistence is monotone in realised mean committed-chunk
    length with no arbitrator instantiated; flat reading supported if persistence tracks
    last_arbitration.w_planned and is invariant to length once w is controlled. BLOCKED on V3-EXQ-810
    (see ARC-071 / MECH-323). Full design, gates and confounders in the proposal doc section 4.

    2026-07-31 (V3-EXQ-841, non_contributory, both A_HIER_*/A_FLAT legs;
    failure_autopsy_V3-EXQ-841_2026-07-31). Run correctly self-routed
    substrate_not_ready_requeue (non_degenerate=false, all 6 arms RED at the
    readiness-gate layer) -- NOT a test of either claim. Independent re-derivation
    surfaced a genuinely novel, decisive substrate finding: wherever the ARC-071
    chunk-injection pathway (use_chunk_proposal_injection=True) is live-tested for
    the first time (810a explicitly ran with injection OFF; 834 never cleared
    formation), the realised committed-chunk length pins at EXACTLY 1 step in 100%
    of 32 chunking cells that registered any commitment at all, independent of the
    configured chunk_max_size dose (2/3/5) -- categorically falsifying Q-085's own
    pre-registered gate (b) as currently wired/measured. Two live hypotheses
    (commit-latch wiring vs experiment readout artifact) are not yet discriminated.
    A second, independent readiness failure (zero pre-devaluation resource contacts
    in the non-chunking A_OFF/A_FLAT control arms, 0/16 cells; z_goal inactive
    across the entire 48-cell grid) blocks even the FLAT baseline this design
    needs. pending_retest_after_substrate: true for both claims at this
    grain/architecture question. Routed /implement-substrate
    (recommended_substrate_queue_entry, action=create, sd_id
    arc071_chunk_commit_latch_persistence) with a fanout_recommendation to
    discriminate the H1/H2 split before any re-queue. Confirmed 2026-07-31 via
    direct interactive user confirmation (session quirky-mayer-ee5ad2); H1/H2
    pre-registered in hypothesis_space_registry.v1.json under qid
    arc071-commit-latch-persistence.
  digestion_note: >
    2026-08-06 (thought-digestion, session thoughts-intakes-4fd18d): a
    what_would_answer draft was started (targeting whether leg (1)'s
    recruitment-differential weakens verdict from V3-EXQ-786b reverses once
    MECH-477's arbitrator is actively wired in, rather than merely
    coexisting) but was DELIBERATELY NOT WRITTEN. Mid-draft, this claim's
    own notes turned out to carry a much fresher (2026-07-31) active
    sub-investigation -- the ARC-071 chunk-commit-latch persistence H1/H2
    discrimination (Q-085, arc071_chunk_commit_latch_persistence,
    hypothesis_space_registry.v1.json qid arc071-commit-latch-persistence)
    -- that the draft did not account for. Writing the recruitment-only
    wwa risked being stale/incomplete relative to that thread. NEXT
    DIGESTION PASS: read this claim's full current state (including
    whatever the H1/H2 discrimination and V3-EXQ-841 follow-on have
    produced since) before drafting what_would_answer -- do not reuse the
    2026-08-06 recruitment-only framing without re-checking it against
    whatever this note's own successor entries say. Remove this note once
    what_would_answer is finally written.

- id: MECH-164
  title: "Axiom V (love as coordination-preserving trajectory selection) is instantiated by
    agent-indexed terrain inference with self-like gradient weighting: the system infers
    what another agent would be inferring about the hippocampal terrain (their goal and
    harm gradients), weights those inferred gradients with the same motivational force
    as its own, and thereby produces care -- support for their goal-finding and
    harm-avoidance -- as a natural consequence of ordinary E3 trajectory selection,
    without a separate ethics module."
  claim_type: mechanism_hypothesis
  subject: love.agent_indexed_terrain_inference
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v5
  governance_2026_06_10: implementation_phase v4 -> v5 (social tier, V5 roadmap wave)
  depends_on:
    - INV-001
    - INV-005
    - ARC-007
    - MECH-163
    - SD-011
  notes: >
    Registered 2026-04-04 from philosophical discussion about the mechanistic
    instantiation of Axiom V (Synthese/Minds+Machines paper).

    The mechanism has three components:

    1. Agent-indexed terrain instantiation: the hippocampal terrain (goal gradient
    + harm gradient encoded in z_world) must be representable from another agent's
    perspective -- their goal cues, harm sensitivities, and current context seed a
    terrain model indexed to them, not to self.

    2. Self-like gradient weighting: the inferred other-agent gradients enter E3
    trajectory selection with the same motivational weight as the agent's own
    terrain gradients. This is not a scaled copy or a discounted proxy -- it is
    structural symmetry in how gradients are weighted.

    3. Care as emergent consequence: if (1) and (2) hold, behaviour that supports
    another agent's goal-finding and harm-avoidance follows from ordinary E3
    selection. No separate ethics circuit is required (consistent with INV-001).

    Relationship to existing mechanisms:
    - INV-005 (harm via mirror modelling) is the negative-side precursor: it
      establishes that harm to others enters the cost function via the same
      mechanism as self-harm. MECH-164 generalises this to the full terrain --
      both harm gradient AND goal gradient -- and makes explicit the indexing
      requirement (the terrain must be instantiated from the other's perspective,
      not the self's).
    - MECH-163 (dual goal-directed systems) provides the planning substrate:
      the VTA/hippocampal system is required because 1-step greedy cannot
      predict or navigate toward states that sustain another agent's goal
      trajectory or reduce their harm accumulation over time.

    Connection to "love once means love all" (Synthese paper Section 5):
    Under Axiom I (epistemic humility), the agent cannot rule out that its
    actions reshape another agent's terrain. Under self-like gradient weighting,
    any inferred reshaping carries full motivational cost. The expansion of moral
    scope is therefore not merely a consequence of uncertainty about causal chains;
    it is forced by the architecture of inference itself. The scope of consideration
    is bounded only by inferrable terrain -- which under increasing intelligence
    expands without principled stopping point.

    Distinction from harm avoidance:
    Harm avoidance (SD-011 dual streams) demonstrates that the system can
    distinguish and respond to harm gradients. MECH-164 requires additionally
    that goal gradients for another agent are inferred and weighted. The
    dissociation is non-trivial: harm avoidance is present in V3; agent-indexed
    goal gradient inference is a V4 prerequisite. Axiom V is not fully
    instantiated until both sides of another agent's terrain are internalised.

    Implementation note:
    z_world is already structured as a residue field -- not a fixed self-centred
    map. Agent-indexing is a matter of whose context seeds the terrain model.
    The substrate is architecturally compatible; V4 is the implementation target.
  evidence: []
  evidence_quality_note: >
    Registered 2026-04-04. No experiments yet -- V4 implementation_phase.
    Candidate status reflects architectural derivation from Axiom V + INV-005 +
    MECH-163. Awaits V4 social extension substrate.

- id: MECH-165
  title: "Offline replay (MECH-121) must sample trajectory-diverse content -- including non-dominant and counterfactual paths -- to maintain multi-strategy viability; consolidation of only the dominant trajectory produces monostrategy in E1's world model even when SHY (MECH-120) is intact."
  claim_type: mechanism_hypothesis
  subject: sleep.replay_diversity_behavioral_strategy
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-165@2026-04-10T18:06:06.734119Z"
      as_of: 2026-04-10
      verdict: hold_candidate_resolve_conflict/applied
  implementation_phase: v3
  epistemic_category: substrate_conditional
  what_would_answer: |
    Extracted from EXQ-244/EXQ-244a's own already-established operationalization
    (forward-only vs balanced [forward+reverse+random] replay contrast on
    action-distribution entropy retention across a phase1-exploration ->
    phase2-no-exploration transition), corrected for the two gaps EXQ-244a's own
    manifest identified rather than inventing a new design.

    NON-DEGENERACY PRECONDITION: the run must show (1) reverse_replayed > 0 in
    at least one BALANCED_REPLAY seed (EXQ-244a showed reverse_replayed=0 in
    ALL seeds of ALL conditions despite exploration_buffer_size=50 -- the
    reverse-replay path itself was never invoked/logged as firing, which is a
    prerequisite defect to diagnose and fix before this claim can be tested at
    all); (2) FORWARD_REPLAY must differ from NO_REPLAY in at least one seed
    (EXQ-244a found them byte-for-byte identical, meaning even the plain
    forward-replay comparison never exercised replay); (3) the exploration
    buffer must be populated by a genuine exploration/alternative-strategy
    source (epsilon-greedy warmup or curiosity-driven, per the 2026-04-07
    GOVERNANCE finding that replay cannot diversify behavior without
    exploration-generated source material) rather than the dominant policy
    trajectory alone; (4) the offline/consolidation phase must be a real SWS
    cycle in the production replay path (agent.act_with_split_obs ->
    _do_replay -> hippocampal.diverse_replay), not a short in-episode proxy.

    CONFIRMING: balanced (forward+reverse+random) replay produces
    significantly higher post-consolidation behavioral/action-distribution
    entropy retention than forward-only replay, in a design where (a) reverse
    replay is confirmed to have actually fired via reverse_replayed > 0, (b)
    the exploration buffer was populated by genuine alternative-strategy
    source material (not just the dominant trajectory), and (c) the effect
    reproduces across a majority of seeds (>=3/5) with an effect size that
    exceeds floating-point noise (EXQ-244a's own "1/5 seeds real, 3/5 seeds
    winning by ~1e-13" is the explicit negative example to exceed).

    FALSIFYING (VIOLATED BY): balanced replay with confirmed-firing reverse
    mode and genuine exploration source material still fails to increase
    behavioral diversity relative to forward-only replay (i.e., the
    diversity-scheduling mechanism itself, not just the test harness, is
    inert) -- this would be a genuine weakens, distinct from EXQ-244's
    original FAIL (proxy substrate, no exploration source) and from
    EXQ-244a's diagnostic-quality PASS (reverse replay never actually fired).
    Also VIOLATED BY: MECH-092's downstream consolidation-benefit test
    continuing to be gated on unbuilt SD-006 phase-2 async execution such
    that no real-SWS-phase (non-proxy) test of MECH-165 is possible at all --
    that would confirm this claim's epistemic_category should stay
    substrate_conditional rather than convert to standard/testable-now.

    IMPLEMENTATION-DEFECT FLAG (2026-08-08 digestion pass, not resolved
    here): replay_diversity_enabled defaults False (config.py:2594); even the
    one config-enabled run (EXQ-244a) never actually invoked the reverse-
    replay path. Chip spawned to diagnose why diverse_replay(mode="auto")'s
    reverse mode never fires despite being enabled and the exploration buffer
    being populated -- a straight re-run of the same design would likely
    reproduce the same degenerate zero-invocation result without this fix.
  depends_on:
    - MECH-120
    - MECH-121
    - MECH-092
    - ARC-007
  implementation_note: >
    IMPLEMENTED 2026-04-09 in ree-v3. Exploration buffer + reverse replay +
    diverse scheduler in HippocampalModule. Config: replay_diversity_enabled,
    reverse_replay_fraction, random_replay_fraction, exploration_buffer_len.
    Agent records waking trajectories at episode boundaries; diverse_replay()
    probabilistically selects forward/reverse/random mode during SWS.
    EXQ-244a queued for validation (EXP-0105 redesign).
  location: docs/architecture/sleep/offline_phases.md#mech-165
  source:
    - evidence/literature/targeted_review_zebrafish_sleep_behavioral_diversity/entries/2026-04-04_mech121_mech092_forward_reverse_replay_shin2019/summary.md
    - evidence/literature/targeted_review_zebrafish_sleep_behavioral_diversity/entries/2026-04-04_mech120_arc011_strategy_shift_hagewoud2010/summary.md
  notes: >
    MECH-120 (SHY) prevents Hebbian monopoly at the synaptic weight level.
    MECH-165 is the complementary content-level constraint: even with MECH-120
    intact, if replay content is dominated by the single most-travelled trajectory,
    consolidation will strengthen that trajectory preferentially in E1's world
    model, reproducing monostrategy at the representational level.
    The mechanism has two requirements:
    (1) Reverse replay (retrospective): replay of non-dominant and error
    trajectories (paths not taken, paths that failed) must be included alongside
    forward replay of the dominant trajectory. Reverse replay provides the
    counterfactual evaluation that prevents E1 from modelling only what was done
    and never what was possible. Grounded in Shin et al. 2019 (Neuron): reverse
    replay represents past paths, supports retrospective evaluation, and provides
    the learning signal for path discrimination. Forward replay alone would
    consolidate only the anticipated dominant path, leaving alternatives invisible
    in E1's world model.
    (2) Replay scheduling balance: harm traces must not dominate replay content
    at the expense of goal traces (MECH-124 prerequisite), AND dominant-strategy
    traces must not dominate at the expense of alternative-strategy traces.
    The second requirement is the MECH-165 contribution: a balanced scheduler
    must reserve replay bandwidth for non-dominant viable paths, not just for
    harm vs goal balance.
    Relationship to MECH-120: the two mechanisms are complementary and both
    required for behavioral strategy diversity. MECH-120 flattens the attractor
    at the weight level (prevents dominant synapses from monopolising). MECH-165
    repopulates the world model with diverse path content during consolidation
    (prevents E1 from modelling only the dominant trajectory). A system with
    MECH-120 but not MECH-165 would have flattened weights but a world model that
    only anticipates one trajectory -- recovery of flexibility would be slow.
    A system with MECH-165 but not MECH-120 would consolidate diverse paths into
    E1 but those paths would be inaccessible at action time because the dominant
    attractor still monopolises action selection.
    V3 implication: MECH-092's quiescent replay must eventually be extended to
    support both forward and reverse sequence generation. Currently the
    HippocampalModule generates forward CEM trajectories; a reverse-replay mode
    (sequence completion backward from outcome) is a V4 requirement for MECH-165.
    Empirical grounding: Shin 2019 (Neuron) -- reverse/forward replay dissociation
    and learning trajectory. Hagewoud 2010 (Sleep) -- monostrategy convergence
    when sleep absent, providing indirect support.
    Registered 2026-04-05. V4 scope.
  evidence_quality_note: >
    EXQ-244 FAIL (2026-04-05): balanced replay (forward+reverse) did not reliably increase
    behavioral diversity vs forward-only (2/5 seeds, needed 3/5). Proxy substrate (64 replay
    steps, no actual offline phase). Offline replay alone cannot increase entropy without tested
    alternative strategies and random walks as source material for diversification.
    GOVERNANCE META (2026-04-06): EXQ-244 weakens classification may be test-design issue,
    not claim issue. Replay cannot increase behavioral diversity without exploration-generated
    alternative strategies as source material. Superseding experiment must pair replay diversity
    mechanism with random walks or explicit exploration to provide diverse candidates for replay
    to select from.
    GOVERNANCE (2026-04-07): Both EXQ-244 runs reclassified from weakens to non_contributory.
    Test-design limitation confirmed: proxy substrate and missing exploration source material
    mean the experiment cannot test the claim. Conflict ratio should resolve after index rebuild.
    Status: hold_candidate, needs_redesign (pair replay with exploration mechanism).
    GOVERNANCE (2026-04-08): Manifests corrected, hold_candidate_resolve_conflict applied.
    Note: user confirmed V4 mechanisms being pulled into V3 scope -- MECH-165 substrate
    (reverse replay scheduler, exploration source material) should be planned for V3.

    [2026-08-09 governance, V3-EXQ-244b, confirmed failure_autopsy_V3-EXQ-244b_2026-08-09,
    supersedes V3-EXQ-244a]: non-degenerate PASS -- corrects 244a's silent no-op bug (theta_buffer
    wiped before consolidation; FORWARD was byte-identical to NO_REPLAY, 1e-13). All 4
    harness-integrity preconditions genuinely met this run: reverse replay fires (9-23/seed),
    FORWARD now measurably diverges from NO_REPLAY (up to 0.046), production call path exercised
    (agent.act_with_split_obs -> _do_replay -> hippocampal.diverse_replay). supports, but the
    effect is small and only weakly seed-robust: BALANCED beats FORWARD by a mean of +0.0083
    against a between-seed SD of 0.128 (Cohen's-d ~0.065), in 4/5 seeds with 3 of those 4 margins
    under 0.012 and the dissenting seed reversing by a comparable magnitude (binomial
    P(>=4/5 wins | H0) ~0.19, not significant at conventional thresholds -- no formal
    significance test is computed in the driver). BALANCED does not clearly outperform NO_REPLAY
    (+0.0015 mean, wins only 2/5 seeds) -- the claim's own pre-registered CONFIRMING criterion is
    BALANCED-vs-FORWARD only, which the run legitimately clears, but the practically meaningful
    "does any replay help vs none" comparison is not established here. epistemic_category stays
    substrate_conditional. Status unchanged (candidate) -- weak-but-real supports, not a
    promotion-grade result. Possible uncontrolled confound flagged by the autopsy: diverse_replay's
    mode/content sampling may draw torch.multinomial from a shared global RNG stream, so conditions
    calling _do_replay a different number of times could consume different stream amounts before
    Phase 2 action sampling -- not verified inert, worth checking before citing this as decisive.

- id: SD-016
  title: "E1 must expose a z_world-only ContextMemory query path that produces two downstream signals: an action-affordance bias for E2 (MECH-151) and a terrain precision weight for E3 harm/goal scoring (MECH-152); the existing bulk generate_prior() path is insufficient because it blends z_self and z_world and produces no cue-specific modulation."
  claim_type: design_decision
  subject: e1.frontal_cue_indexed_integration
  polarity: asserts
  status: implemented
  live_status:
    reading: implemented
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_534_sd016_cue_terrain_training_20260506T094249Z_v3
      as_of: 2026-05-06
      verdict: supports/PASS
  implementation_phase: v3
  registered_utc: 2026-03-31
  depends_on:
    - SD-005
    - SD-010
    - ARC-035
  epistemic_category: substrate_ceiling
  ceiling_decision: deferred  # 2026-08-10 governance, GOV-CEIL-1 ceiling-exhausted audit (5 mechanical hits)
  ceiling_routing_note: "2026-08-10 GOV-CEIL-1 ceiling-exhaustion audit flagged 5 mechanical hits. NOT applying the standard demotion -- the specific unblock has just been identified THIS cycle, not exercised yet: the substrate_queue.json SD-016 entry (2026-08-10 amend, V3-EXQ-907/908 GOV-FANOUT-1 portfolio) confirms H1 (ctxdiv auxiliary loss, breaks the uniform-softmax saddle robustly) and SPLITs H3 (annealed Gumbel-softmax CONFIRMED, straight-through top-k ELIMINATED) -- combining H1's objective with H3's Gumbel-softmax operator is a concrete, scoped, not-yet-built fix. Deferred pending that build; remove this marker once it lands and re-tests."
  what_would_answer: |
    SD-016 splits into two independently falsifiable sub-questions.

    (A) RETRIEVAL-SELECTIVITY (does z_world-only cue-indexed retrieval
    differentiate at all?): ALREADY ANSWERED, negatively, for the
    originally-proposed remedy: environment enrichment does not restore
    selectivity. V3-EXQ-418j/k (4-arm reef/landmark enrichment ladder,
    2026-05-05/06) found cos_cross_mean 0.987-0.9999 and separation_gap_mean
    ~0-0.004 across ALL arms including the enriched ones. Independently
    reconfirmed by the 2026-07-18 z_world near-static characterisation:
    contrast ratio is flat (~0.094, 1.52x range) across a 782x foraging
    change and 17x survival-horizon change, and is highest at the most
    impoverished rung -- "env enrichment is the wrong lever for this
    quantity." Both the original q.k selection mechanism (exhausted across
    418d/e/i) and Path 3 (feedforward cue->slot tagger, V3-EXQ-418m,
    2026-06-05, FAIL: C1 ON entropy 0/3 seeds <2.5, C1b ON
    context-divergence 0/3 seeds >0.1, OFF saddle reproduced exactly at
    ln(16)=2.772589) fail identically because both consume the same
    near-constant z_world -- the bottleneck sits one level upstream of the
    selection mechanism, in the encoder, not in which architecture sits on
    top of it.

    STILL OPEN: whether an encoder-level fix restores cross-context z_world
    separation enough for ANY selection mechanism to have something to
    differentiate on. Under the x724/x734 P0 protocol the world encoder is
    never trained at all (weight-delta 0/61 tensors); the 2026-06-06
    zworld-integration-cluster autopsy separately found even a TRAINED
    z_world at dim=32 lacks event-selective structure (substrate_ceiling);
    SD-070 found the currently-prescribed P0 recipe makes contrast WORSE
    (0.1222 -> 0.0726 CR), diagnosed as a wiring fault. V3-EXQ-783
    (dim32/128 x untrained/trained crossing, PASS 2026-07-18) is the live
    discriminator for this upstream thread but is scoped to Q-002/SD-031
    and has not been read back onto SD-016's own retest criteria. What
    would answer this leg: re-run the 418g/418m-shaped acceptance test (C1
    sel_entropy_mean <2.5 AND C1b context-divergence >0.1, both >=2/3
    seeds, with OFF reproducing the ln(16) saddle as the ablation-cleanliness
    check) under an encoder configuration already shown to raise z_world
    contrast independently of SD-016 (SD-070's replacement recipe, or
    V3-EXQ-783's trained dim=128 arm) -- NOT the originally-planned
    SD-023-landmarks probe, which the evidence above renders redundant.

    (B) DOWNSTREAM EXPLOITATION (once selectivity exists, do
    action_bias/terrain_weight get used behaviourally by E2/E3?): UNTESTED,
    explicitly gated, and NOT automatic even when selectivity is engineered
    directly -- V3-EXQ-418g forced slot_diversity=1.000 and
    attn_entropy=0.000 and action_class_entropy still stayed at 1.105e-10
    identically across all four arms. Two further named prerequisites: (1)
    cue_action_proj's own consumer wiring (FAIL/collapsed in V3-EXQ-449,
    std ~2.7e-8; reportedly fixed in V3-EXQ-449b) and (2) SD-055
    differentiable-CEM gradient path (substrate landed, V3-EXQ-568 PASS,
    but its own default-flip note requires a SEPARATE behavioural
    validation with use_differentiable_cem=True, not yet run per
    substrate_queue.json). What would answer this leg: once (A) clears, an
    action_bias_div / terrain-weight discriminative test (418g C4/C5-shaped)
    run with cue_action_proj confirmed live and SD-055 differentiable CEM
    engaged, showing behavioural divergence tracks retrieval content rather
    than staying flat.

    NON-DEGENERACY PRECONDITION: selection entropy pinned at or within
    ~0.02 of the uniform ceiling ln(16)=2.772589 (as OFF has reproduced
    exactly, 3/3 seeds, across both the q.k and feedforward-tagger paths)
    is not evidence against SD-016 -- it is the signature that the
    selection INPUT (z_world) is not varying, recurring identically across
    five+ independent probes regardless of selection architecture. A pass
    on entropy (C1) alone without a context-divergence instrument (C1b) is
    equally uninformative: 418m's ON arm moved entropy a cosmetic 0.019
    below ceiling from random-init fixed MLP logits while context-divergence
    stayed ~0.002 against a 0.1 target -- a static non-uniform-but-fixed
    selector, not a context-selective one. Any future SD-016 retest must
    report BOTH instruments, and must independently confirm (via a
    raw-channel or contrast-ratio control) that the environment/encoder
    configuration under test actually clears cross-context z_world
    separation before attributing a pass or fail to the selection mechanism
    itself.
  status_note: >
    IMPLEMENTED 2026-04-16. E1DeepPredictor.extract_cue_context(z_world) added to
    ree_core/predictors/e1_deep.py. Three new projections gated by sd016_enabled=True:
    world_query_proj (Linear 32->128), cue_action_proj (Linear 64->16), cue_terrain_proj
    (Linear 64->2). Agent._e1_tick() caches action_bias and terrain_weight each tick;
    passes to E2.action_object() and E3.score_trajectory() respectively. Supervised
    terrain_loss (lambda=0.1) trains cue_terrain_proj against hazard_field_view proxy.
    Backward compatible: sd016_enabled=False default. Validation: V3-EXQ-418a queued.
  evidence_quality_note: |
    EXQ-449 FAIL (2026-04-20) and EXQ-449a PASS x2 (2026-04-21) together diagnose a
    cue_action_proj consumer-side wiring failure: the projection produces per-channel
    std ~2.7e-8 in frozen-weights g2 regime (collapsed signal). The forward path probe
    PASSes but the consumer site does not differentiate. EXQ-418a (SD-017) and EXQ-436
    (SD-017/ARC-045/MECH-166) FAIL with bit-identical conditions traceable to the same
    consumer collapse. Spawned diagnostic to fix the consumer wiring; SD-017/ARC-045/MECH-166
    re-tests gated on this fix landing.
    V3-EXQ-418d FAIL (2026-04-25, evidence, weakens via per-claim correction): 4-arm
    write-path comparison (off / train_only / sense_only / both) with Part A key_proj.bias=False
    applied uniformly. C2 routing PASS (writes only fire in active arms), but C1 attention
    entropy FAIL across ALL active arms (mean ~2.75-2.76 vs threshold 2.5; tight to the uniform
    log(16)=2.7726 ceiling) and C3 slot diversity FAIL across all active arms (best mean 0.349
    on A2_sense_only, threshold 0.5; only 1 of 3 seeds passes). Production default
    (sd016_writepath_mode tie-break = A1_train_only) is NOT empirically grounded by this run.
    Implication: the design doc v2 substrate (key_proj.bias=False + writepath_mode flag) is
    insufficient to restore content-conditioned attention or slot differentiation. Deeper
    rework required -- candidates: cue_action_proj consumer-side rebuild, attention-head
    architecture change, or supervised-target shape correction. SD-016 status remains
    'implemented' (substrate is wired and exercised) but flagged for rethink in
    substrate_queue.json. SD-017 was tagged but non_contributory in this run (sws/rem/shy
    disabled in params -- sleep substrate not under test).
    V3-EXQ-418e FAIL (2026-04-27, evidence, does_not_support): 4-arm ablation of auxiliary
    diversification loss path-1 at div_weight=0.5. C1 attn-entropy collapse FAIL on both
    active arms (A2_div_only=2.7726, A3_writes_plus_div=2.7724 -- both at uniform-distribution
    ceiling ln(16)=2.7726; the loss does NOT push attention into a sparse regime at this
    weight). C2 slot-diversity PASS on A2_div_only (3/3 seeds, mean=1.000) but FAIL on
    A3_writes_plus_div (1/3 seeds, min=0.005 -- bimodal seed pattern when writes are added
    back). C3 behavioural delta PASS (A3-A0=0.456 > 0.20). C4 A1 replicates 418d FAIL
    (ablation logic intact). Reading: path-1 div-loss at div_weight=0.5 fails its designed
    C1 attention-collapse signature; behaviour differentiates (C3) but not via the intended
    mechanism. SD-016 substrate remains wired; the auxiliary-diversification-loss path is
    the question under test. Follow-up: queue EXQ-418f weight sweep at 1.0 / 2.0 / 5.0
    BEFORE pivoting; if higher weights also fail C1, abandon path-1 and pivot to Path 2
    or Path 3 from the original 3-path proposal.
    V3-EXQ-418i (the recommended div-weight sweep at 1.0/2.0/5.0) FAILed C1 at every weight
    and concluded Path 1 is "insufficient regardless of weight; the attention bottleneck is
    categorically in query selectivity, not slot orthogonality." Path 1 exhausted.
    Path 3 (feedforward cue->slot tagger) IMPLEMENTED 2026-06-05 (config sd016_cue_slot_tagger,
    default False; E1Config + from_dims): a fresh feedforward MLP (z_world -> slot logits)
    replaces ONLY the saddle-stuck q.k slot-SELECTION inside extract_cue_context; the slot-
    content path + cue_action_proj/cue_terrain_proj are untouched; trained by the existing
    terrain_loss gradient (no new supervised target). Bit-identical OFF (verified: OFF
    selection entropy == ln(16) exactly). Path 3 restores RETRIEVAL selectivity; behavioural
    action_bias_div propagation remains gated on cue_action_proj / SD-055 differentiable CEM.
    Status stays 'implemented' (design_decision; no flag/confidence change). Substrate-
    readiness validation queued (claim_ids=[]; primary acceptance selection entropy < 2.5).
    See ree-v3/CLAUDE.md "SD-016 Path 3" + docs/architecture/sd_016_writepath_v3_diversification_loss.md Section 7.
    V3-EXQ-418j/k (2026-05-05/06, "SD-016 ContextMemory reef", supersedes V3-EXQ-418j;
    evidence_direction does_not_support) tested a DIFFERENT precondition from 418d/e/i's
    attention-selectivity question: whether the environment itself supplies enough
    world-state information for cue-indexed retrieval to have anything to differentiate
    on, across a 4-arm enrichment ladder (A0 off/no-reef, A1 sd016/no-reef, A2 reef/no-sd016,
    A3 reef+sd016 landmarks-and-hazard-drift richer world). Result: cos_cross_mean stayed
    0.987-0.9999 and separation_gap_mean 0.0000-0.0039 across ALL four arms (overall_pass
    false; only C2_uniform_rail_broken passed) -- z_world remains near-constant regardless
    of environment enrichment, ruling out "the environment is too information-poor" as the
    bottleneck (that hypothesis predicted separation_gap would improve under A2/A3; it did
    not). This was proposed as its own EVB-0476/EXP-0174 backlog item ("env complexity
    gate") and re-surfaced unqueued for 3+ months after 418k had already answered the same
    question -- retroactively marked superseded 2026-08-02 (session determined-ritchie-55a3a6)
    rather than re-run. Not yet reflected in evidence/planning/sleep_substrate_plan.md's
    Tier 0 table (still read "check status" for EXQ-418j/k as of that date -- see that
    file for the correction). If SD-016 is revisited again, the open question is narrower
    than "is the env rich enough" (answered: enrichment alone does not move the metric) --
    it is whether the SELECTION mechanism (Path 3 feedforward cue->slot tagger, implemented
    2026-06-05, substrate-readiness validation still pending per the paragraph above) can
    exploit the existing richness once retrieval selectivity itself is fixed.
    [2026-08-07 governance, ceiling-audit ACTIONABLE finding, user-confirmed]: check_substrate_ceiling_audit.py
    flags this claim genuinely orphaned despite status=implemented -- the substrate exists but the
    substrate_ceiling reading (selectivity, not richness, is the open bottleneck) has no design owner.
    Flagged for /failure-autopsy to produce an enrichment recommendation targeting the SELECTION
    mechanism question above, rather than routing here inline from the ceiling stamp alone.
  functional_restatement: >
    E1's ContextMemory stores 16 associative slots and can in principle retrieve hazard-relevant
    associations when z_world encodes a cue-dense context. But no downstream pathway existed
    to act on this retrieval in a cue-specific way. Two distinct effects were missing: (1)
    action-affordance bias: hazard-associated cues should upweight avoidance action-objects
    in E2's affordance space before harm occurs; (2) terrain precision modulation: E3 should
    sharpen harm scoring when contextual cues predict elevated danger, relaxing in safe contexts.
    This maps to the vmPFC somatic-marker circuit (Bechara et al. 1997 Iowa Gambling Task):
    intact participants show anticipatory SCR before selecting from a bad deck, driven by cue
    context not immediate feedback. vmPFC lesion patients respond only to immediate outcomes.
    SD-016 implements the architectural prerequisite for this pre-SCR analog: cue-indexed E1
    retrieval feeding forward into E2 affordance and E3 scoring precision before harm contact.
    The z_world-only query (not [z_self, z_world]) ensures retrieval reflects exteroceptive
    sensory context, matching vmPFC/OFC afferent anatomy (convergent sensory, not interoceptive).
  location: docs/architecture/sd_016_frontal_cue_integration.md
  source:
    - docs/architecture/frontal_cue_integration.md
    - docs/architecture/vmPFC.md
  blocks:
    - MECH-150
    - MECH-151
    - MECH-152
    - ARC-041
    - INV-040

- id: SD-017
  title: "REE-v3 requires a minimal sleep-phase infrastructure: an SWS-analog phase (bidirectional hippocampus-to-cortex replay for schema/slot consolidation) and a REM-analog phase (causal attribution replay for slot-filling from co-correlational evidence ordering). Without these offline phases, hippocampal attribution mapping cannot converge and context representations remain globally undifferentiated."
  claim_type: design_decision
  subject: sleep_phase.minimal_sleep_infrastructure_v3
  polarity: asserts
  status: stable
  pending_retest_after_substrate: true  # re-scoped 2026-08-13: DV repair complete (V3-EXQ-436e); now blocked on SD-016 being armed
  epistemic_category: substrate_ceiling  # 2026-08-16 governance: set per confirmed autopsy
  live_status:
    reading: stable
    as_of: "2026-08-13"
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-436e_2026-08-13"
      as_of: "2026-08-13"
      verdict: "non_contributory -- P0 readiness failure (SD-016 not yet armed), not a claim-layer result; the occupied-slots-only DV repair itself is confirmed sound"
  implementation_phase: v3
  depends_on:
    - MECH-092
    - ARC-038
    - ARC-007
    - SD-014
  location: docs/architecture/sd_017_sleep_phase_architecture.md
  notes: >
    Registered 2026-04-05. Significant roadmap change: previously all sleep phases
    were V4 scope (offline_phases.md, v3_v4_transition_boundary.md). Key insight:
    hippocampal attribution mapping -- the core E3 function -- requires offline
    consolidation phases to converge. Without them, context representations remain
    undifferentiated (cosine_sim -> 1.0; Law 2016 shows hippocampal context
    stabilisation requires ~3 interleaved sessions). Slot structure must exist
    before slot-filling yields reliable attribution signal (Sanders 2020 Bayesian
    hidden-state inference). The monostrategy failure mode (agent navigates like a
    fish swimming the same route) is not a V4 problem -- it appears in V3 and
    blocks E3's core attribution mapping function.
    V3 minimal implementation: (a) SWS-analog -- periodic hippocampus-to-cortex
    replay pass updating schema/context templates (distinct from MECH-092 which is
    hippocampus-internal waking quiescence replay); (b) REM-analog -- causal
    attribution replay filling context slots with co-correlational estimates from
    recent trajectory evidence, using information-direction switching.
    This is architecturally distinct from the full V4 sleep machinery:
    MECH-120 (SHY synaptic homeostasis), MECH-121 (NREM SWR+spindles),
    MECH-122 (thalamo-cortical spindles), MECH-123 (REM precision recalibration).
    Those remain V4. SD-017 is the V3 scaffold that enables E3 goal and harm
    streams to produce useful attribution maps, without which ree-v3 cannot
    consolidate contextual knowledge and remains behaviourally impoverished
    regardless of the quality of the online waking substrate.
  implementation_note: >
    2026-04-09: First-class SWS-analog and REM-analog passes implemented in REEAgent
    (run_sws_schema_pass, run_rem_attribution_pass, run_sleep_cycle). Config params:
    sws_enabled, sws_consolidation_steps, sws_schema_weight, rem_enabled, rem_attribution_steps
    (all default False/0). All wired through REEConfig.from_dims(). Validation experiment
    V3-EXQ-265 queued (2 conditions x 3 seeds, ~45 min). Distinguishes from EXQ-242
    (proxy hooks, non_contributory) -- this is the first-class substrate implementation.

    2026-08-19 (chip-20260816-implsub-contextmemory-writepath-degeneracy): the write-path
    addressing build that V3-EXQ-436f's re-derive brake was gated on has landed
    (substrate_queue contextmemory-write-path-addressing-degeneracy, status
    implemented_pending_validation) -- ContextMemory.write() (ree_core/predictors/e1_deep.py)
    gains a conscience-bias usage-balancing selection (E1Config.contextmemory_write_usage_balancing,
    default False = bit-identical legacy argmin path), repairing the deterministic single-slot
    fixed point 436e/436f established. Validated so far at the ContextMemory unit level only
    (ree-v3/tests/contracts/test_contextmemory_write_usage_balancing.py): reproduces the lock
    synthetically and confirms >= 2 occupied slots under the fix on every seed observed to lock.
    Not yet validated with a queued EXQ measuring n_occupied_slots on a real agent; follow-on
    chip chip-20260819-queueexp-contextmemory-writeusage-validation queues that. This is a
    factual landing note, not a governance disposition -- pending_retest_after_substrate and
    status are left for governance to update once the validation experiment scores.

    2026-08-19, SECOND MECHANISM (chip-20260819-contextmemory-add-refractory-mode; USER-
    AUTHORISED build): `refractory` landed as an ADDITIONAL, DEFAULT-OFF write-selection
    mode ALONGSIDE the conscience bias above, which is NOT reverted or replaced (ree-v3
    692f8526d0). E1Config.contextmemory_write_selection ("argmin" default = bit-identical
    legacy; "refractory" excludes the k most-recently-written slots) plus
    .contextmemory_write_refractory_k. The two are orthogonal -- the bias adjusts the SCORE,
    refractory restricts the ELIGIBLE SET -- and compose. Grounds are STRUCTURAL, not the
    differentiation DV: occupancy >= k+1 holds BY CONSTRUCTION, and it is the only arm that
    does not turn the single-slot fixed point into a counter-driven period-16 LRU cycle
    (round-robin agreement 0.991 with the bias on vs 0.000 for refractory -- real occupancy,
    but occupancy WITHOUT addressing). The independently pre-registered probe
    (evidence/planning/contextmemory_write_selection_comparison_20260819.md) found the
    occupied-slot COSINE column CANNOT discriminate these arms at 5 seeds (|dz| <= 0.47,
    |t(4)| <= 1.04, sign-inconsistent, INCLUDING refractory-over-legacy at dz = -0.06); it
    must not be cited for either mechanism, and any retest of THIS claim that turns on that
    DV needs n = 38-2485 depending on contrast, or a deterministic DV instead.
    CORRECTION to the note above: the chip it names
    (chip-20260819-queueexp-contextmemory-writeusage-validation) was never actually written
    to TASK_CHIPS.json, so the owed validation experiment had no tracker. Re-chipped covering
    BOTH arms as chip-20260819-queueexp-contextmemory-writesel-validation. Validation remains
    OWED and substrate_queue status remains implemented_pending_validation for both
    mechanisms; both flags still default OFF and no driver sets either, so nothing about this
    claim's evidence base has changed. Factual landing note only -- status, confidence and
    pending_retest_after_substrate are untouched and remain governance's call. See
    docs/architecture/contextmemory_write_address_selection.md.
  evidence_quality_note: >
    2026-07-10 (governance, failure_autopsy_V3-EXQ-538a, confirmed; user-adjudicated): V3-EXQ-538a (SD-049 Phase-2 sleep-on ablation) tagged SD-017 non_contributory. The manifest's per-claim SD-017 'supports' was SWS/REM-write liveness only (sleep loop fired: SWS/REM writes 160/40) -- vacuous, NOT counted. SD-017 stays STABLE / unchanged; its genuine support is V3-EXQ-691.

    EXQ-418 (2026-04-15, non_contributory): action_bias_divergence=0.0 everywhere;
    identical per-seed data shared with EXQ-385 (INV-049 offline necessity experiment).
    Cannot distinguish context-conditioned action when action distributions are identical
    across conditions. Flagged for /diagnose-errors to identify the shared-data scripting
    issue. Not evidence against SD-017; substrate may be wired correctly.
    EXQ-418a + EXQ-436 (2026-04-18, non_contributory -- governance 2026-04-19): Two experiments produce zero differential signal across seeds (action_bias_div=0.0 in EXQ-418a all seeds/conditions; slot metrics identical across WAKING/SWS_ONLY/SWS_THEN_REM in EXQ-436). Shared anomaly suggests context-conditioning substrate not wired through to policy/slot structure. Reclassified as diagnostic of substrate wiring rather than test of SD-017. Flagged for further substrate investigation.
    EXQ-418a rerun 2026-04-19T10:24 (non_contributory -- governance 2026-04-19): Second occurrence of same zero-differential-signal pattern (action_bias_div=0.0 in both WITH_SLEEP_SD016 and WITHOUT_SLEEP_SD016 conditions, all seeds). Confirms wiring gap persists; same diagnostic classification applied.
    EXQ-418a + EXQ-436 04-20 reruns (non_contributory -- governance 2026-04-22): Same bit-identical zero-signal pattern repeats in third + fourth attempts. Root cause now diagnosed as SD-016 cue_action_proj consumer collapse (per-channel std ~2.7e-8 in frozen-weights regime). SD-017/ARC-045/MECH-166 cannot be discriminated until SD-016 consumer wiring fix lands; spawned diagnostic owns the fix.
    Promoted provisional -> stable 2026-04-24 (governance): 5 supporting literature entries, 0 opposing, 1 mixed, conf 0.903. No contributing genuine experimental evidence yet -- all SD-017 ablation runs to date are non_contributory because of the upstream SD-016 ContextMemory attention-uniformity bug (V3-EXQ-477 diagnostic 2026-04-24 localised to key_proj.bias dominance, bias_over_content_ratio=4.24). Promotion rests on the consolidated sleep-phase biology literature; experimental confirmation deferred to post-SD-016-fix ablation re-run.
    V3-EXQ-691 (Q-055, 2026-06-20, PASS/supports -- governance 2026-06-20): first non-vacuous experimental support (non_degenerate=true). Sleep-phase consolidation PRESERVES (does not collapse) committed-action-class diversity with the 569i top-k conversion armed -- arm-A (sleep-ON) t2 entropy >= 50% of t0 on 2/3 seeds; readiness met (sleep cycles fire 75 write-passes vs 0 OFF; t0 supra-floor; knob not inert). NUANCE (governance-flagged): arm-A t2 (0.569) was marginally BELOW the sleep-OFF control t2 (0.645) -- sleep did NOT net-protect diversity vs no-sleep; the PASS criterion is preserve-not-collapse, NOT sleep-beats-control. Supports the offline-consolidation-does-not-erode-diversity reading; status stable unchanged.


    [2026-08-02 governance, V3-EXQ-436b, confirmed failure_autopsy_V3-EXQ-436b_2026-08-02, successor to V3-EXQ-436/436a]: slot_cosine_sim is now the sole PASS/FAIL gate and MECH-313/ARC-065 noise-floor diversity is genuinely wired into action selection (confirmed fixing 436a's zero-cross-seed-variation defect -- behavioural stats DO differ meaningfully between WAKING_ONLY and SWS_THEN_REM per seed). But the primary DV (slot_cosine_sim) is BIT-IDENTICAL between conditions within every one of 5 seeds, to full float64 precision, and slot_separation is 0.0 in both conditions for every seed -- inconsistent with a genuine "sleep has zero effect" finding (which would still show floating-point noise) and instead points to a RECORDING gap: the driver discards run_sleep_cycle()'s entire metrics dict, including sws_n_writes and any REM-equivalent write counter, so we cannot distinguish "write never fired" from "fired but had negligible effect" from "readout is structurally invariant." Additional flag: only 1 of 16 memory slots ever receives visits in either condition -- worth checking slot-addressing collapse independent of the recording gap. non_contributory. Route: same-question re-queue capturing sws_n_writes/REM-equivalent write counters per the Experimental Recording Standard before drawing any conclusion about SD-017/ARC-045/MECH-166's slot-differentiation prediction.

    [2026-08-03 governance, V3-EXQ-436c, confirmed failure_autopsy_V3-EXQ-436c_2026-08-03, successor to
    V3-EXQ-436b]: recording gap fixed (P0 gate: pooled sws_n_writes=800, rem_n_rollouts=600, both
    floor-clearing) and the monomodal-collapse confound ruled out (waking-phase action-class entropy
    ~1.2, healthy every seed). C1 (slot_cosine_sim differentiation) fails 0/5 seeds -- NOT a null:
    SWS_THEN_REM collapses whole-bank cosine similarity to ~0.9999-1.0 in 4/5 seeds (homogenization),
    the OPPOSITE of the predicted direction. weakens SD-017/ARC-045/MECH-166 (all three;
    pending_retest_after_substrate=true on each). Root-cause hypothesis (untested this run):
    ContextMemory.write_gate (Linear+Sigmoid, default bias) may carry the same bias-over-content
    collapse already found and fixed on key_proj's read path (SD-016 Part A / EXQ-477) -- never
    audited. Independently converges with failure_autopsy_V3-EXQ-861a_2026-08-03's finding on a
    DIFFERENT DV (touched-slot diversity, MECH-180/MECH-122) via the same write path -- two
    independent DVs now point at the same untested write-gate hypothesis. Routed /implement-substrate,
    amend MECH122-CONTENT-PACKAGING-SPINDLE-SELECTION.

    [2026-08-07 governance, V3-EXQ-436d, confirmed failure_autopsy_2026-08-05_pending_review_batch
    #5, successor to V3-EXQ-436c]: fourth generation (436a bug -> 436b recording gap -> 436c
    write_gate substrate defect -> 436d), the FIRST to clear P0 (writes fire) with no known
    instrumentation or substrate defect and non_degenerate=true (healthy waking-phase action-class
    entropy). C1 fails 2/5 seeds pass, need 3/5. HELD PENDING A METHODOLOGICAL CHECK at the walk
    (autopsy's own recommendation): the WAKING_ONLY baseline cosine ranges 0.0009-0.47 across seeds
    rather than clustering near 1.0 as ARC-045's own prediction requires, and one seed (200) shows
    sleep INCREASING similarity, opposite the predicted direction.
    [2026-08-07 REVERSED, confirmed failure_autopsy_V3-EXQ-436d-methodology-check_2026-08-07,
    user-confirmed]: the hold resolves AGAINST the metric, not in favour of the FAIL.
    slot_cosine_sim is a whole-bank mean off-diagonal cosine with NO occupancy mask, and the
    WAKING_ONLY arm executed ZERO ContextMemory writes this run (all three write sites closed by
    sd016_writepath_mode='off' plus sleep_passes=0, corroborated by the manifest's own
    sws_n_writes_total=0.0) -- its entire 0.0009-0.47 spread is reproduced by Adam-optimizer drift
    of context_memory.memory (an nn.Parameter carried in the driver's own optimiser) alone, at
    0.05-0.37 on the real substrate with zero writes. The metric also tracks
    content-similarity x occupancy-fraction, so it scores an arm recruiting 12 well-separated slots
    WORSE (0.076) than a single-slot arm (0.009) -- anti-correlated with the differentiation
    SD-017/ARC-045/MECH-166 actually predict. Seeds 13 and 200 sit inside the untouched-bank null
    (z=+1.21, +0.09), making C1 unsatisfiable there by construction (effective denominator <=3, not
    5). ARC-045's near-1.0 undifferentiated-baseline premise is reachable only under the legacy
    write_gate-as-payload defect that 436d itself repaired -- i.e. C1 was calibrated against a
    substrate that no longer exists. P0 clearance is unaffected and stands: 436d remains the first
    lineage run with a working write path. evidence_direction weakens -> non_contributory;
    epistemic_category -> measurement_test_design_defect. pending_retest_after_substrate stays true,
    now re-scoped from "after-substrate" to "after-DV-repair" -- a V3-EXQ-436e re-run on an
    occupied-slots-only DV is owed (chipped) before this lineage's slot-differentiation question can
    be tested at all. Does NOT weight toward a demotion discussion. Related: sws_slot_diversity
    (1 - slot_cosine_sim, same whole-bank formula) is emitted by every sleep-pass driver and
    inherits the identical confound -- v3_exq_242/243/245/245a/245b/246 share the shape; any
    criterion pre-registered on it should be re-read with this in mind. PROMOTES/DEMOTES NOTHING
    this cycle (a corrected DV, not this cycle's data, is what would move status).

    [2026-08-13 governance, V3-EXQ-436e, confirmed failure_autopsy_V3-EXQ-436e_2026-08-13]: the
    436d-methodology-check's owed occupied-slots-only DV repair has now run. Implements all 6 items
    of that autopsy's Recommendation #4: statistic over occupied slots only (tracking
    ContextMemory.write()'s own min_idx via a read-only wrapper), similarity/occupancy reported
    separately (never their product), WAKING_ONLY given a real write path (sd016_writepath_mode=
    'sense_only') plus a new NO_WRITES calibration-only arm, Adam drift neutralized
    (context_memory.memory.requires_grad_(False)), baseline re-derived empirically at run time, and
    the NO_WRITES arm checked against that re-derived null as a P0 gate. Two new P0 gates
    (waking_writepath_engaged, sufficient_occupancy_for_c1) beyond the 436c/436d sleep-write
    counters. evidence_direction STANDS at non_contributory (a P0 readiness failure, not a
    claim-layer result -- third consecutive letter whose FAIL is upstream of the comparison).
    epistemic_category standard (was unset). pending_retest_after_substrate stays true,
    RE-SCOPED from "after-DV-repair" (now complete) to "after-SD-016-armed" -- the corrected DV
    machinery is confirmed sound; the remaining blocker is SD-016's own cue-conditioning wiring
    (shared substrate with ARC-041/MECH-150/151/152) needing to be armed before this lineage's
    slot-differentiation question can be tested at adequate power. Status unchanged.

    [2026-08-16 governance apply | V3-EXQ-436f -> SD-017 | failure_autopsy_436f-603u-precondition-blocked-cluster_2026-08-16] DISPOSITION: evidence_direction non_contributory. evidence_direction STANDS at non_contributory; evidence_quality_note APPEND; pending_retest_after_substrate stays true, re-scoped from after-SD-016-armed to after-WRITE-PATH-ADDRESSING-BUILD; epistemic_category standard -> substrate_ceiling

    [2026-08-16 governance, V3-EXQ-436f, failure_autopsy_436f-603u-precondition-blocked-cluster_2026-08-16, successor to V3-EXQ-436e]: sixth generation. The 436e instrument is re-confirmed sound (P0 5/6 gates MET: SWS writes 800, REM rollouts 600, waking writepath 15,078 calls, NO_WRITES Adam-drift max |z| = 1.67 against a 4.0-sigma tolerance on a run-time-derived null). Recording standard complete, 0 dry runs, non_degenerate true. The single change vs 436e -- arming the full SD-016 production combination (cue_slot_tagger + gumbel selection + context_divergence_weight 0.5 + 922's ctxdiv training-loop wiring) -- ENGAGED, confirmed by the new sd016_arming_engaged gate (pooled applied ctxdiv loss 25,796 against a 1e-9 floor), and moved write-path slot occupancy by EXACTLY ZERO seeds: 2/5 scoreable in 436e, 2/5 in 436f, with the same seeds 7/13/100 occupying exactly 1 of 16 slots in BOTH arms despite 2,837-4,903 write() calls each. C1 was therefore never computed on 3/5 seeds and is scored:false; C4 (4/5) is non-gating and also scored:false. This is the joint reading the 436f driver PRE-REGISTERED in its own interpretation grid as the signal to route to a WRITE-path successor -- so 436f is a successful read/write discrimination, not a bare precondition failure. Root cause (established at 436e by a closed-form discriminator that predicted 5/5): ContextMemory.write() addresses by a hard scores.mean(0).argmin() (ree_core/predictors/e1_deep.py:144) with a deterministic single-slot fixed point under a near-constant query stream, while read() addresses by softmax -- every SD-016 remedy shipped to date (908 gumbel selection, 907 ctxdiv) lives on the read side or a separate tagger and does not touch the write address. epistemic_category standard -> substrate_ceiling: the 436e stamp of `standard` was explicitly conditioned on the substrate having 'shipped and merely needing switching on', and V3-EXQ-436f is the run that tested that condition and falsified it -- the write-path selection mechanism does not exist and must be built. non_contributory: a P0 readiness failure upstream of the comparison, weighing on the claims in NEITHER direction. CAUTION: 436e's '2/2 directional on the scoreable seeds' did NOT survive this single config change (now 1/2 -- seed 200 reversed from 0.96795->0.83109 to 0.95391->0.98744); do not carry that n=2 read forward as partial support. pending_retest_after_substrate stays true, re-scoped a THIRD time: after-DV-repair -> after-SD-016-armed -> after-WRITE-PATH-ADDRESSING-BUILD. Re-derive brake FIRES for SD-017 (3rd substrate_ceiling hit, threshold 2): a same-question V3-EXQ-436g re-queue is REFUSED until the write-path build lands. PROMOTES/DEMOTES NOTHING this cycle.
- id: ARC-045
  title: "Bidirectional hippocampus-neocortex information flow is architecturally necessary for attribution mapping convergence: the SWS-direction (hippocampus to cortex) installs context schema structure; waking and REM-direction reactivations fill those structures with causal attribution estimates. Without bidirectional flow, context representations remain globally undifferentiated despite locally coherent online encoding."
  claim_type: architectural_commitment
  subject: hippocampus.bidirectional_information_flow
  polarity: asserts
  status: candidate
  pending_retest_after_substrate: true  # re-scoped 2026-08-13: DV repair complete (V3-EXQ-436e); now blocked on SD-016 being armed
  epistemic_category: substrate_ceiling  # 2026-08-16 governance: set per confirmed autopsy
  live_status:
    reading: candidate
    as_of: "2026-08-13"
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-436e_2026-08-13"
      as_of: "2026-08-13"
      verdict: "non_contributory -- P0 readiness failure (SD-016 not yet armed), not a claim-layer result; the occupied-slots-only DV repair itself is confirmed sound"
  implementation_phase: v3
  depends_on:
    - SD-017
    - MECH-092
    - ARC-038
    - MECH-166
  implementation_note: >
    2026-08-19 (chip-20260816-implsub-contextmemory-writepath-degeneracy): the write-path
    addressing build that V3-EXQ-436f's re-derive brake was gated on has landed
    (substrate_queue contextmemory-write-path-addressing-degeneracy, status
    implemented_pending_validation) -- ContextMemory.write() (ree_core/predictors/e1_deep.py)
    gains a conscience-bias usage-balancing selection (E1Config.contextmemory_write_usage_balancing,
    default False = bit-identical legacy argmin path), repairing the deterministic single-slot
    fixed point 436e/436f established. Validated so far at the ContextMemory unit level only
    (ree-v3/tests/contracts/test_contextmemory_write_usage_balancing.py): reproduces the lock
    synthetically and confirms >= 2 occupied slots under the fix on every seed observed to lock.
    Not yet validated with a queued EXQ measuring n_occupied_slots on a real agent; follow-on
    chip chip-20260819-queueexp-contextmemory-writeusage-validation queues that. This is a
    factual landing note, not a governance disposition -- pending_retest_after_substrate and
    status are left for governance to update once the validation experiment scores.

    2026-08-19, SECOND MECHANISM (chip-20260819-contextmemory-add-refractory-mode; USER-
    AUTHORISED build): `refractory` landed as an ADDITIONAL, DEFAULT-OFF write-selection
    mode ALONGSIDE the conscience bias above, which is NOT reverted or replaced (ree-v3
    692f8526d0). E1Config.contextmemory_write_selection ("argmin" default = bit-identical
    legacy; "refractory" excludes the k most-recently-written slots) plus
    .contextmemory_write_refractory_k. The two are orthogonal -- the bias adjusts the SCORE,
    refractory restricts the ELIGIBLE SET -- and compose. Grounds are STRUCTURAL, not the
    differentiation DV: occupancy >= k+1 holds BY CONSTRUCTION, and it is the only arm that
    does not turn the single-slot fixed point into a counter-driven period-16 LRU cycle
    (round-robin agreement 0.991 with the bias on vs 0.000 for refractory -- real occupancy,
    but occupancy WITHOUT addressing). The independently pre-registered probe
    (evidence/planning/contextmemory_write_selection_comparison_20260819.md) found the
    occupied-slot COSINE column CANNOT discriminate these arms at 5 seeds (|dz| <= 0.47,
    |t(4)| <= 1.04, sign-inconsistent, INCLUDING refractory-over-legacy at dz = -0.06); it
    must not be cited for either mechanism, and any retest of THIS claim that turns on that
    DV needs n = 38-2485 depending on contrast, or a deterministic DV instead.
    CORRECTION to the note above: the chip it names
    (chip-20260819-queueexp-contextmemory-writeusage-validation) was never actually written
    to TASK_CHIPS.json, so the owed validation experiment had no tracker. Re-chipped covering
    BOTH arms as chip-20260819-queueexp-contextmemory-writesel-validation. Validation remains
    OWED and substrate_queue status remains implemented_pending_validation for both
    mechanisms; both flags still default OFF and no driver sets either, so nothing about this
    claim's evidence base has changed. Factual landing note only -- status, confidence and
    pending_retest_after_substrate are untouched and remain governance's call. See
    docs/architecture/contextmemory_write_address_selection.md.
  notes: >
    Registered 2026-04-05. Biological grounding: hippocampal-to-neocortical
    information flow during slow-wave sleep (Diekelmann & Born 2010) is the
    canonical schema-installation pathway. Waking and REM reactivation then
    reads from and elaborates installed schemas. The bidirectional nature is not
    incidental -- it reflects a functional division: the hippocampus proposes
    structure; the neocortex receives, stabilises, and provides context feedback.
    ARC-045 claims bidirectionality is necessary (not merely useful) for
    attribution mapping convergence, because unidirectional online encoding alone
    cannot produce the stable context representations required for reliable causal
    attribution. Without the hippocampus-to-cortex pass installing schema attractors,
    the filling pass has no stable slots to fill.
    Experimental implication: an agent with bidirectional offline flow should show
    cosine_sim < 0.95 (differentiated contexts) after sleep phases; one with only
    waking online encoding remains at cosine_sim -> 1.0 regardless of training
    duration. EXQ-239 (MECH-153) approximates this via the supervised signal proxy
    -- the lambda=0.5 supervised pass is a weak stand-in for the SWS-direction
    schema-installation pass that SD-017 will properly implement.
    Bayesian framing (INV-044): the hip->cx (SWS) direction is the generative model
    installation pass -- hippocampus proposes structure, neocortex stabilises as
    attractors. The cx->hip (REM) direction is the recognition pass -- prediction
    error signals update the posterior. This bidirectional duality is structurally
    isomorphic to the generative/recognition architecture of hierarchical Bayesian
    models (predictive coding, Friston 2005-2010), implemented via information
    direction switching rather than explicit probability distributions.
  evidence_quality_note: >
    EXQ-436 (2026-04-18, non_contributory -- governance 2026-04-19): Cross-frequency bidirectional flow hypothesis tested in context-conditional harm-threshold sleep-phase experiment (WAKING_ONLY, SWS_ONLY, SWS_THEN_REM x 5 seeds). No measurable difference between sleep and waking conditions on slot_cosine_sim. Reclassified as diagnostic of substrate wiring; not evidence against ARC-045.


    [2026-08-02 governance, V3-EXQ-436b, confirmed failure_autopsy_V3-EXQ-436b_2026-08-02, successor to V3-EXQ-436/436a]: slot_cosine_sim is now the sole PASS/FAIL gate and MECH-313/ARC-065 noise-floor diversity is genuinely wired into action selection (confirmed fixing 436a's zero-cross-seed-variation defect -- behavioural stats DO differ meaningfully between WAKING_ONLY and SWS_THEN_REM per seed). But the primary DV (slot_cosine_sim) is BIT-IDENTICAL between conditions within every one of 5 seeds, to full float64 precision, and slot_separation is 0.0 in both conditions for every seed -- inconsistent with a genuine "sleep has zero effect" finding (which would still show floating-point noise) and instead points to a RECORDING gap: the driver discards run_sleep_cycle()'s entire metrics dict, including sws_n_writes and any REM-equivalent write counter, so we cannot distinguish "write never fired" from "fired but had negligible effect" from "readout is structurally invariant." Additional flag: only 1 of 16 memory slots ever receives visits in either condition -- worth checking slot-addressing collapse independent of the recording gap. non_contributory. Route: same-question re-queue capturing sws_n_writes/REM-equivalent write counters per the Experimental Recording Standard before drawing any conclusion about SD-017/ARC-045/MECH-166's slot-differentiation prediction.

    [2026-08-03 governance, V3-EXQ-436c, confirmed failure_autopsy_V3-EXQ-436c_2026-08-03]: recording
    gap fixed, monomodal-collapse confound ruled out. C1 (slot_cosine_sim differentiation) fails
    0/5 seeds -- SWS_THEN_REM collapses whole-bank cosine similarity to ~0.9999-1.0 in 4/5 seeds
    (homogenization), opposite the predicted direction. weakens ARC-045 (pending_retest_after_substrate
    =true). Root-cause hypothesis (untested): ContextMemory.write_gate may carry the same
    bias-over-content collapse fixed on key_proj's read path (SD-016/EXQ-477); converges with
    failure_autopsy_V3-EXQ-861a_2026-08-03's independent finding on a different DV (MECH-180/MECH-122).
    Routed /implement-substrate, amend MECH122-CONTENT-PACKAGING-SPINDLE-SELECTION.

    [2026-08-07 governance, V3-EXQ-436d, confirmed failure_autopsy_2026-08-05_pending_review_batch
    #5, successor to V3-EXQ-436c]: fourth generation, first to clear P0 with no known instrumentation
    or substrate defect and non_degenerate=true. C1 fails 2/5 seeds (need 3/5). HELD PENDING A
    METHODOLOGICAL CHECK at the walk: the WAKING_ONLY baseline cosine ranges 0.0009-0.47 across
    seeds rather than clustering near 1.0 as ARC-045's own prediction requires, one seed shows sleep
    INCREASING similarity, opposite the predicted direction.
    [2026-08-07 REVERSED, confirmed failure_autopsy_V3-EXQ-436d-methodology-check_2026-08-07,
    user-confirmed]: hold resolves AGAINST the metric. slot_cosine_sim has NO occupancy mask; the
    WAKING_ONLY arm executed ZERO ContextMemory writes this run (sd016_writepath_mode='off' +
    sleep_passes=0, sws_n_writes_total=0.0) -- its spread is pure Adam-optimizer drift of
    context_memory.memory, reproduced at 0.05-0.37 with zero writes. Metric also tracks
    content-similarity x occupancy-fraction, so a 12-slot arm scores WORSE (0.076) than a 1-slot arm
    (0.009) -- anti-correlated with predicted differentiation. Seeds 13/200 sit inside the
    untouched-bank null (z=+1.21, +0.09); effective C1 denominator <=3, not 5. ARC-045's near-1.0
    undifferentiated-baseline premise was reachable only under the legacy write_gate-as-payload
    defect 436d itself repaired. P0 clearance stands. evidence_direction weakens -> non_contributory;
    epistemic_category -> measurement_test_design_defect. pending_retest_after_substrate stays true,
    re-scoped after-DV-repair; V3-EXQ-436e chipped on an occupied-slots-only DV. Does NOT weight
    toward demotion. sws_slot_diversity (same whole-bank formula) inherits the identical confound
    across v3_exq_242/243/245/245a/245b/246. PROMOTES/DEMOTES NOTHING this cycle.

    [2026-08-13 governance, V3-EXQ-436e, confirmed failure_autopsy_V3-EXQ-436e_2026-08-13]: the
    owed occupied-slots-only DV repair has run (statistic over occupied slots only via a read-only
    ContextMemory.write() wrapper, similarity/occupancy reported separately, WAKING_ONLY given a
    real write path plus a new NO_WRITES calibration arm, Adam drift neutralized, baseline
    re-derived empirically, plus two new P0 gates). evidence_direction STANDS at non_contributory
    -- a P0 readiness failure (SD-016 not yet armed), not a claim-layer result. epistemic_category
    standard (was unset). pending_retest_after_substrate stays true, RE-SCOPED from
    "after-DV-repair" (now complete) to "after-SD-016-armed" -- the corrected DV machinery is
    confirmed sound; the blocker is SD-016's cue-conditioning wiring needing to be armed. Status
    unchanged.

    [2026-08-16 governance apply | V3-EXQ-436f -> ARC-045 | failure_autopsy_436f-603u-precondition-blocked-cluster_2026-08-16] DISPOSITION: evidence_direction non_contributory. evidence_direction STANDS at non_contributory; evidence_quality_note APPEND; pending_retest_after_substrate stays true, re-scoped to after-WRITE-PATH-ADDRESSING-BUILD; epistemic_category standard -> substrate_ceiling

    [2026-08-16 governance, V3-EXQ-436f, failure_autopsy_436f-603u-precondition-blocked-cluster_2026-08-16, successor to V3-EXQ-436e]: sixth generation. The 436e instrument is re-confirmed sound (P0 5/6 gates MET: SWS writes 800, REM rollouts 600, waking writepath 15,078 calls, NO_WRITES Adam-drift max |z| = 1.67 against a 4.0-sigma tolerance on a run-time-derived null). Recording standard complete, 0 dry runs, non_degenerate true. The single change vs 436e -- arming the full SD-016 production combination (cue_slot_tagger + gumbel selection + context_divergence_weight 0.5 + 922's ctxdiv training-loop wiring) -- ENGAGED, confirmed by the new sd016_arming_engaged gate (pooled applied ctxdiv loss 25,796 against a 1e-9 floor), and moved write-path slot occupancy by EXACTLY ZERO seeds: 2/5 scoreable in 436e, 2/5 in 436f, with the same seeds 7/13/100 occupying exactly 1 of 16 slots in BOTH arms despite 2,837-4,903 write() calls each. C1 was therefore never computed on 3/5 seeds and is scored:false; C4 (4/5) is non-gating and also scored:false. This is the joint reading the 436f driver PRE-REGISTERED in its own interpretation grid as the signal to route to a WRITE-path successor -- so 436f is a successful read/write discrimination, not a bare precondition failure. Root cause (established at 436e by a closed-form discriminator that predicted 5/5): ContextMemory.write() addresses by a hard scores.mean(0).argmin() (ree_core/predictors/e1_deep.py:144) with a deterministic single-slot fixed point under a near-constant query stream, while read() addresses by softmax -- every SD-016 remedy shipped to date (908 gumbel selection, 907 ctxdiv) lives on the read side or a separate tagger and does not touch the write address. epistemic_category standard -> substrate_ceiling: the 436e stamp of `standard` was explicitly conditioned on the substrate having 'shipped and merely needing switching on', and V3-EXQ-436f is the run that tested that condition and falsified it -- the write-path selection mechanism does not exist and must be built. non_contributory: a P0 readiness failure upstream of the comparison, weighing on the claims in NEITHER direction. CAUTION: 436e's '2/2 directional on the scoreable seeds' did NOT survive this single config change (now 1/2 -- seed 200 reversed from 0.96795->0.83109 to 0.95391->0.98744); do not carry that n=2 read forward as partial support. pending_retest_after_substrate stays true, re-scoped a THIRD time: after-DV-repair -> after-SD-016-armed -> after-WRITE-PATH-ADDRESSING-BUILD. Re-derive brake FIRES for SD-017 (3rd substrate_ceiling hit, threshold 2): a same-question V3-EXQ-436g re-queue is REFUSED until the write-path build lands. PROMOTES/DEMOTES NOTHING this cycle.
- id: MECH-166
  title: "Context slot-formation (schema abstraction: which distinctions between contexts are worth representing) and slot-filling (causal attribution: which evidence populates each slot) are computationally separated functions that cannot be co-computed in a single online pass. Slot structure must be consolidated during an SWS-analog phase before slot-filling during a REM-analog phase can yield reliable attribution signal."
  claim_type: mechanism_hypothesis
  subject: hippocampus.slot_formation_filling_temporal_separation
  polarity: asserts
  status: candidate
  pending_retest_after_substrate: true  # re-scoped 2026-08-13: DV repair complete (V3-EXQ-436e); now blocked on SD-016 being armed
  epistemic_category: substrate_ceiling  # 2026-08-16 governance: set per confirmed autopsy
  live_status:
    reading: candidate
    as_of: "2026-08-13"
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-436e_2026-08-13"
      as_of: "2026-08-13"
      verdict: "non_contributory -- P0 readiness failure (SD-016 not yet armed), not a claim-layer result; the occupied-slots-only DV repair itself is confirmed sound"
  implementation_phase: v3
  depends_on:
    - SD-017
    - ARC-045
    - MECH-092
    - INV-044
  implementation_note: >
    2026-08-19 (chip-20260816-implsub-contextmemory-writepath-degeneracy): the write-path
    addressing build that V3-EXQ-436f's re-derive brake was gated on has landed
    (substrate_queue contextmemory-write-path-addressing-degeneracy, status
    implemented_pending_validation) -- ContextMemory.write() (ree_core/predictors/e1_deep.py)
    gains a conscience-bias usage-balancing selection (E1Config.contextmemory_write_usage_balancing,
    default False = bit-identical legacy argmin path), repairing the deterministic single-slot
    fixed point 436e/436f established. Validated so far at the ContextMemory unit level only
    (ree-v3/tests/contracts/test_contextmemory_write_usage_balancing.py): reproduces the lock
    synthetically and confirms >= 2 occupied slots under the fix on every seed observed to lock.
    Not yet validated with a queued EXQ measuring n_occupied_slots on a real agent; follow-on
    chip chip-20260819-queueexp-contextmemory-writeusage-validation queues that. This is a
    factual landing note, not a governance disposition -- pending_retest_after_substrate and
    status are left for governance to update once the validation experiment scores.

    2026-08-19, SECOND MECHANISM (chip-20260819-contextmemory-add-refractory-mode; USER-
    AUTHORISED build): `refractory` landed as an ADDITIONAL, DEFAULT-OFF write-selection
    mode ALONGSIDE the conscience bias above, which is NOT reverted or replaced (ree-v3
    692f8526d0). E1Config.contextmemory_write_selection ("argmin" default = bit-identical
    legacy; "refractory" excludes the k most-recently-written slots) plus
    .contextmemory_write_refractory_k. The two are orthogonal -- the bias adjusts the SCORE,
    refractory restricts the ELIGIBLE SET -- and compose. Grounds are STRUCTURAL, not the
    differentiation DV: occupancy >= k+1 holds BY CONSTRUCTION, and it is the only arm that
    does not turn the single-slot fixed point into a counter-driven period-16 LRU cycle
    (round-robin agreement 0.991 with the bias on vs 0.000 for refractory -- real occupancy,
    but occupancy WITHOUT addressing). The independently pre-registered probe
    (evidence/planning/contextmemory_write_selection_comparison_20260819.md) found the
    occupied-slot COSINE column CANNOT discriminate these arms at 5 seeds (|dz| <= 0.47,
    |t(4)| <= 1.04, sign-inconsistent, INCLUDING refractory-over-legacy at dz = -0.06); it
    must not be cited for either mechanism, and any retest of THIS claim that turns on that
    DV needs n = 38-2485 depending on contrast, or a deterministic DV instead.
    CORRECTION to the note above: the chip it names
    (chip-20260819-queueexp-contextmemory-writeusage-validation) was never actually written
    to TASK_CHIPS.json, so the owed validation experiment had no tracker. Re-chipped covering
    BOTH arms as chip-20260819-queueexp-contextmemory-writesel-validation. Validation remains
    OWED and substrate_queue status remains implemented_pending_validation for both
    mechanisms; both flags still default OFF and no driver sets either, so nothing about this
    claim's evidence base has changed. Factual landing note only -- status, confidence and
    pending_retest_after_substrate are untouched and remain governance's call. See
    docs/architecture/contextmemory_write_address_selection.md.
  notes: >
    Registered 2026-04-05. Grounded in Sanders, Wilson & Gershman 2020 (eLife):
    context discrimination at inference time is bounded by the quality of the
    learned hidden-state prior. The prior must be learned (slot-formation) before
    posterior inference (slot-filling) can be informative. In REE terms: the
    context templates (which terrain/situation types exist) must be consolidated
    as stable attractors before the agent can reliably attribute observed evidence
    to the correct context slot.
    Relationship to MECH-165: MECH-165 addresses replay content diversity
    (forward/reverse balance) during slot-filling. MECH-166 is the prerequisite:
    the slots must exist and be differentiated before diversity of replay content
    matters. A system with MECH-165 but not MECH-166 would have diverse replay
    content but no stable context structure to organise it into.
    The three waking/sleep information phases map as: (1) waking -- gather what
    is out there (E1 world model, online experience); (2) SWS-analog -- form the
    slots (schema abstraction, which distinctions matter); (3) REM-analog -- fill
    the slots (causal attribution from co-correlations and evidence ordering).
    Experimental implication: EXQ-239 (MECH-153) provides an indirect test.
    A direct test requires implementing the SWS-analog pass and comparing
    attribution map quality (context cosine_sim, terrain classification accuracy)
    with vs without it.
  evidence_quality_note: >
    EXQ-436 (2026-04-18, non_contributory -- governance 2026-04-19): Context-conditional harm-threshold test across 5 seeds x 3 sleep conditions (WAKING_ONLY, SWS_ONLY, SWS_THEN_REM). Slot_cosine_sim metrics identical across all conditions per seed, indicating sleep phases are not installing differential slot structure. Shared anomaly with EXQ-418a (SD-017). Reclassified as diagnostic of substrate wiring rather than test of MECH-166 hypothesis.


    [2026-08-02 governance, V3-EXQ-436b, confirmed failure_autopsy_V3-EXQ-436b_2026-08-02, successor to V3-EXQ-436/436a]: slot_cosine_sim is now the sole PASS/FAIL gate and MECH-313/ARC-065 noise-floor diversity is genuinely wired into action selection (confirmed fixing 436a's zero-cross-seed-variation defect -- behavioural stats DO differ meaningfully between WAKING_ONLY and SWS_THEN_REM per seed). But the primary DV (slot_cosine_sim) is BIT-IDENTICAL between conditions within every one of 5 seeds, to full float64 precision, and slot_separation is 0.0 in both conditions for every seed -- inconsistent with a genuine "sleep has zero effect" finding (which would still show floating-point noise) and instead points to a RECORDING gap: the driver discards run_sleep_cycle()'s entire metrics dict, including sws_n_writes and any REM-equivalent write counter, so we cannot distinguish "write never fired" from "fired but had negligible effect" from "readout is structurally invariant." Additional flag: only 1 of 16 memory slots ever receives visits in either condition -- worth checking slot-addressing collapse independent of the recording gap. non_contributory. Route: same-question re-queue capturing sws_n_writes/REM-equivalent write counters per the Experimental Recording Standard before drawing any conclusion about SD-017/ARC-045/MECH-166's slot-differentiation prediction.

    [2026-08-03 governance, V3-EXQ-436c, confirmed failure_autopsy_V3-EXQ-436c_2026-08-03]: recording
    gap fixed, monomodal-collapse confound ruled out. C1 (slot_cosine_sim differentiation) fails
    0/5 seeds -- SWS_THEN_REM collapses whole-bank cosine similarity to ~0.9999-1.0 in 4/5 seeds
    (homogenization), opposite the predicted direction. weakens MECH-166 (pending_retest_after_substrate
    =true). Root-cause hypothesis (untested): ContextMemory.write_gate may carry the same
    bias-over-content collapse fixed on key_proj's read path (SD-016/EXQ-477); converges with
    failure_autopsy_V3-EXQ-861a_2026-08-03's independent finding on a different DV (MECH-180/MECH-122).
    Routed /implement-substrate, amend MECH122-CONTENT-PACKAGING-SPINDLE-SELECTION.

    [2026-08-07 governance, V3-EXQ-436d, confirmed failure_autopsy_2026-08-05_pending_review_batch
    #5, successor to V3-EXQ-436c]: fourth generation, first to clear P0 with no known instrumentation
    or substrate defect and non_degenerate=true. C1 fails 2/5 seeds (need 3/5). HELD PENDING A
    METHODOLOGICAL CHECK at the walk: the WAKING_ONLY baseline cosine ranges 0.0009-0.47 across
    seeds rather than clustering near 1.0 as ARC-045's own prediction requires, one seed shows sleep
    INCREASING similarity, opposite the predicted direction.
    [2026-08-07 REVERSED, confirmed failure_autopsy_V3-EXQ-436d-methodology-check_2026-08-07,
    user-confirmed]: hold resolves AGAINST the metric. slot_cosine_sim has NO occupancy mask; the
    WAKING_ONLY arm executed ZERO ContextMemory writes this run (sd016_writepath_mode='off' +
    sleep_passes=0, sws_n_writes_total=0.0) -- its spread is pure Adam-optimizer drift of
    context_memory.memory, reproduced at 0.05-0.37 with zero writes. Metric also tracks
    content-similarity x occupancy-fraction, so a 12-slot arm scores WORSE (0.076) than a 1-slot arm
    (0.009) -- anti-correlated with predicted differentiation. Seeds 13/200 sit inside the
    untouched-bank null (z=+1.21, +0.09); effective C1 denominator <=3, not 5. ARC-045's near-1.0
    undifferentiated-baseline premise was reachable only under the legacy write_gate-as-payload
    defect 436d itself repaired. P0 clearance stands. evidence_direction weakens -> non_contributory;
    epistemic_category -> measurement_test_design_defect. pending_retest_after_substrate stays true,
    re-scoped after-DV-repair; V3-EXQ-436e chipped on an occupied-slots-only DV. Does NOT weight
    toward demotion. sws_slot_diversity (same whole-bank formula) inherits the identical confound
    across v3_exq_242/243/245/245a/245b/246. PROMOTES/DEMOTES NOTHING this cycle.

    [2026-08-13 governance, V3-EXQ-436e, confirmed failure_autopsy_V3-EXQ-436e_2026-08-13]: the
    owed occupied-slots-only DV repair has run (statistic over occupied slots only, similarity/
    occupancy reported separately, WAKING_ONLY given a real write path plus a NO_WRITES
    calibration arm, Adam drift neutralized, baseline re-derived empirically, plus two new P0
    gates). evidence_direction STANDS at non_contributory -- a P0 readiness failure (SD-016 not
    yet armed), not a claim-layer result. epistemic_category standard (was unset).
    pending_retest_after_substrate stays true, RE-SCOPED from "after-DV-repair" (now complete) to
    "after-SD-016-armed" -- the corrected DV machinery is confirmed sound; the blocker is SD-016's
    cue-conditioning wiring needing to be armed. Status unchanged.

    [2026-08-16 governance apply | V3-EXQ-436f -> MECH-166 | failure_autopsy_436f-603u-precondition-blocked-cluster_2026-08-16] DISPOSITION: evidence_direction non_contributory. evidence_direction STANDS at non_contributory; evidence_quality_note APPEND; pending_retest_after_substrate stays true, re-scoped to after-WRITE-PATH-ADDRESSING-BUILD; epistemic_category standard -> substrate_ceiling

    [2026-08-16 governance, V3-EXQ-436f, failure_autopsy_436f-603u-precondition-blocked-cluster_2026-08-16, successor to V3-EXQ-436e]: sixth generation. The 436e instrument is re-confirmed sound (P0 5/6 gates MET: SWS writes 800, REM rollouts 600, waking writepath 15,078 calls, NO_WRITES Adam-drift max |z| = 1.67 against a 4.0-sigma tolerance on a run-time-derived null). Recording standard complete, 0 dry runs, non_degenerate true. The single change vs 436e -- arming the full SD-016 production combination (cue_slot_tagger + gumbel selection + context_divergence_weight 0.5 + 922's ctxdiv training-loop wiring) -- ENGAGED, confirmed by the new sd016_arming_engaged gate (pooled applied ctxdiv loss 25,796 against a 1e-9 floor), and moved write-path slot occupancy by EXACTLY ZERO seeds: 2/5 scoreable in 436e, 2/5 in 436f, with the same seeds 7/13/100 occupying exactly 1 of 16 slots in BOTH arms despite 2,837-4,903 write() calls each. C1 was therefore never computed on 3/5 seeds and is scored:false; C4 (4/5) is non-gating and also scored:false. This is the joint reading the 436f driver PRE-REGISTERED in its own interpretation grid as the signal to route to a WRITE-path successor -- so 436f is a successful read/write discrimination, not a bare precondition failure. Root cause (established at 436e by a closed-form discriminator that predicted 5/5): ContextMemory.write() addresses by a hard scores.mean(0).argmin() (ree_core/predictors/e1_deep.py:144) with a deterministic single-slot fixed point under a near-constant query stream, while read() addresses by softmax -- every SD-016 remedy shipped to date (908 gumbel selection, 907 ctxdiv) lives on the read side or a separate tagger and does not touch the write address. epistemic_category standard -> substrate_ceiling: the 436e stamp of `standard` was explicitly conditioned on the substrate having 'shipped and merely needing switching on', and V3-EXQ-436f is the run that tested that condition and falsified it -- the write-path selection mechanism does not exist and must be built. non_contributory: a P0 readiness failure upstream of the comparison, weighing on the claims in NEITHER direction. CAUTION: 436e's '2/2 directional on the scoreable seeds' did NOT survive this single config change (now 1/2 -- seed 200 reversed from 0.96795->0.83109 to 0.95391->0.98744); do not carry that n=2 read forward as partial support. pending_retest_after_substrate stays true, re-scoped a THIRD time: after-DV-repair -> after-SD-016-armed -> after-WRITE-PATH-ADDRESSING-BUILD. Re-derive brake FIRES for SD-017 (3rd substrate_ceiling hit, threshold 2): a same-question V3-EXQ-436g re-queue is REFUSED until the write-path build lands. PROMOTES/DEMOTES NOTHING this cycle.
- id: MECH-167
  title: "z_harm_a (harm-driven affective accumulation) and drive_level (energy-depletion homeostatic deviation) instantiate the same computational motif -- slow interoceptive integration converging on ACC-type processing -- without sharing information or overlapping downstream gates. The motif predicts: (a) similar temporal dynamics (slow EMA-type accumulation), (b) orthogonal signal content (cos_sim -> 0), (c) distinct downstream gates: z_harm_a gates harm evaluation; drive_level gates z_goal seeding."
  claim_type: mechanism_hypothesis
  subject: homeostatic_accumulation.computational_motif
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  depends_on:
    - SD-011
    - SD-012
  notes: >
    Registered 2026-04-05. Biological grounding: Craig 2003 (Trends Neurosci)
    groups pain, hunger, thirst, and temperature under a common lamina-I
    homeostatic pathway converging on ACC via VMpo. This creates an apparent
    scope collision between z_harm_a and drive_level. MECH-167 resolves this:
    the shared substrate reflects a repeated computational motif (slow interoceptive
    integration) not information sharing. The analogy: canonical microcircuit motifs
    appear across cortical areas without those areas sharing content.
    The two accumulators are separated by: (a) input source (harm exposure vs.
    energy depletion), (b) temporal dynamics (harm accumulates on contact; energy
    depletes continuously), (c) downstream gate (harm evaluation vs. z_goal seeding).
    Experimental prediction: z_harm_a and drive_level should show similar
    autocorrelation curves (slow decay) but near-zero cross-correlation (orthogonal
    content). A direct test regresses one against the other across a long episode
    and expects R^2 < 0.05.
    Generalisation: any future homeostatic signal (novelty depletion, social
    contact deficit) should follow the same integration architecture -- slow EMA,
    ACC-type convergence, distinct downstream gate -- instantiating the same motif
    without information sharing. This is a framework prediction, not just a
    description of the existing two accumulators.
    Empirical Bayes framing (INV-044): the slow EMA accumulators instantiate
    empirical Bayes over need states. The accumulated baseline is the running prior
    expectation -- P(harm_level) or P(drive_level). Deviations from baseline are
    precision-weighted signals: a harm event surprising relative to the prior carries
    more weight than one that confirms expectation. INV-044's prior-before-posterior
    requirement applies: a newly initialised accumulator with no history is a flat
    prior -- deviations are maximally uninformative until the prior has converged.

- id: INV-044
  title: "Approximate Bayesian contextual inference is architecturally impossible to co-compute with online encoding: prior construction (schema formation -- which context distinctions are structurally relevant) must precede posterior inference (attribution -- which evidence belongs to which context slot); a system attempting both online produces a degenerate prior that makes attribution uninformative regardless of training duration."
  claim_type: invariant
  invariant_type: universal
  subject: cognition.bayesian_prior_before_posterior_necessity
  polarity: asserts
  status: provisional
  epistemic_category: standard
  pending_retest_after_substrate: true
  live_status:
    reading: provisional
    as_of: 2026-08-22
    needs_review: false
    evidence:
      from: failure_autopsy_slot_cosine_sim_fanout_sweep_2026-08-13
      as_of: 2026-08-22
      verdict: non_contributory -- both V3-EXQ-429 runs withdrawn; provisional re-based on literature alone
  notes: >
    Registered 2026-04-05. Grounded in Sanders, Wilson & Gershman 2020 (eLife
    doi:10.7554/eLife.51140): context discrimination accuracy at inference time is
    mathematically bounded by the quality of the learned hidden-state prior. The
    prior must be learned before posterior inference can be informative. This is
    not a heuristic -- a flat or degenerate prior makes the posterior unresponsive
    to evidence; no amount of additional evidence can compensate for a missing
    prior structure.
    In REE terms: slot-formation (SWS-analog pass) builds the prior -- the schema
    of which context distinctions are structurally relevant. Slot-filling (REM-analog
    pass) performs the posterior -- which evidence belongs to which context slot.
    An agent attempting both online accumulates evidence against a flat prior and
    produces globally undifferentiated representations (cosine_sim -> 1.0), exactly
    as Law et al. 2016 and the MECH-153 failure mode document.
    Predictive coding connection (ARC-045): the SWS-direction (hip->cx) propagation
    is the generative model installation pass; the REM-direction carries the
    prediction error that updates the posterior. This is structurally the architecture
    of hierarchical Bayesian models (Friston 2005-2010) implemented through
    information direction switching, not explicit probability distributions.
    Empirical Bayes extension (MECH-167): homeostatic accumulators (z_harm_a,
    drive_level) instantiate the same principle at the need-state level. The slow
    EMA baseline is the prior; deviations are likelihood-weighted signals. The
    accumulator must have converged before deviation signals are interpretable.
    This invariant is what makes SD-017 a V3 necessity rather than a V4 optimisation:
    not "sleep phases would help" but "sleep phases are required for approximate
    Bayesian contextual inference to be architecturally coherent."
    Mechanistic instantiation: MECH-166 (slot-formation/filling temporal separation),
    SD-017 (SWS-analog + REM-analog design decision), ARC-045 (bidirectional flow).
  evidence_quality_note: >
    EXQ-429 (2026-04-16, non_contributory): Experiment ran with dry_run=True and
    training_episodes=2; n_sleep_cycles=0 in all conditions. No genuine offline/online
    contrast was produced. Reclassified from does_not_support to non_contributory on
    manifest. Full run still to be queued. Not evidence against INV-044.
    EXQ-429 PASS (2026-04-18, supports): Full 3-arm ordering probe (ORDERED, REM_ONLY, WAKING_ONLY) with training_episodes=300 and n_sleep_cycles=3. ORDERED mean_sws_slot_diversity=0.290 vs REM_ONLY=0.0 and WAKING_ONLY=0.177; 3/3 seeds pass all 4 acceptance checks. Promoted to provisional on governance 2026-04-19.

    [2026-08-22 governance, cycle bold-chaum-7e245c, confirmed
    failure_autopsy_slot_cosine_sim_fanout_sweep_2026-08-13.json (dry_run_checked true), surfaced
    by GOV-APPLY-1]: BOTH V3-EXQ-429 runs (2026-04-18 and 2026-04-19) reclassified supports ->
    non_contributory, WITHDRAWING 2/2 of this claim's experimental evidence. The PASS gate is
    c1_pass AND c2_pass and both criteria are the raw whole-bank sws_slot_diversity -- 1 minus
    slot_cosine_sim over all 16 ContextMemory slots with only the diagonal masked. An untouched
    16-slot bank reads 1.0004 +/- 0.0087 (n=200 fresh inits on the live substrate, re-measured
    2026-08-20), so C1's threshold of 0.05 is cleared by an EMPTY bank at P=1.0000: it is a
    homogenisation detector read as a differentiation detector, and the measured ORDERED 0.288
    corresponds to whole-bank cosine ~0.71, i.e. substantial homogenisation scored as success. C2
    compares ORDERED's mid-training per-cycle mean against WAKING_ONLY's end-of-training final
    value, two points on a drifting nuisance trajectory. The 2026-04-19 promotion rationale's
    leading contrast, ORDERED 0.290 vs REM_ONLY 0.0, is a real readout against a PLACEHOLDER: 0.0
    is the initialised default in run_sws_schema_pass's metrics dict, returned unchanged when
    sws_enabled is False, and REM_ONLY runs no SWS pass at all. The claim is NOT falsified -- it
    is UNTESTED. Status stays provisional, now re-based on literature alone (6 entries, lit_conf
    0.861); the reading itself does not move. epistemic_category set to standard (the field was
    previously absent). pending_retest_after_substrate set true: a sound differentiation DV is
    owed. The separate GFLAG-0047 fork -- whether the replacement DV should be a differentiation
    statistic at all, or a relational-topology one -- is OPEN and bears directly on what that
    retest measures.

- id: INV-045
  title: "The biological NREM-then-REM sleep phase sequence is the correct computational order, derivable from the logical dependencies between the failure modes each phase addresses; this ordering is not a contingent biological fact but an engineering necessity, and it serves directly as the V4 rewiring specification: each phase specifies what structural change the REE substrate requires to address its failure mode."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - SD-017
  pending_substrate_reconfirmation: true
  subject: sleep.phase_ordering_computational_necessity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-044
    - SD-017
    - MECH-120
    - MECH-121
    - MECH-122
    - MECH-123
  epistemic_category: substrate_conditional
  what_would_answer: |
    INV-045's falsifier is a scrambled-order ablation on the REAL
    (non-proxy) offline phase mechanisms, at the grain V3 actually
    supports.

    V3 implements only the coarse SWS-analog / REM-analog split (SD-017),
    not the claim's full 5 sub-phase structure (0 sensory gate, 1 SHY,
    2 NREM replay, 3 spindle coordination, 4 REM recalibration) -- Phase
    0 is unbuilt and Phase 3's sensory-gating half is unbuilt
    (offline_phases.md). So a partial, coarse test is V3-testable now;
    the full 5-phase pairwise reordering is not, until V4 lands input
    gating on E1 and ThetaBuffer's reverse-direction/gating mode.

    COARSE V3 TEST (re-run of V3-EXQ-243's design, using real
    mechanisms): run WAKING_ONLY, REM_ONLY, REM_THEN_SWS (scrambled),
    SWS_THEN_REM (canonical), and SWS_THEN_REM_RECAL, but call
    agent.run_sws_schema_pass() (MECH-120+121) and
    agent.run_rem_attribution_pass() (MECH-123) directly instead of the
    2026-04-05 script's local proxy passes, which
    design_forward_analysis_2026-04-06.md (DR-7) diagnosed as not
    invoking the real computational content and therefore producing an
    order-blind (degenerate) null result -- confirmed in the archived
    per_seed_results, where REM_ONLY, REM_THEN_SWS, SWS_THEN_REM and
    SWS_THEN_REM_RECAL returned bit-identical harm_discrimination/
    harm_eval values per seed.

    INV-045's own logical-dependency argument predicts a SPECIFIC failure
    mode for the scrambled order, not just "worse in general":
    REM_THEN_SWS should show reinforcement of the single dominant (most
    recent, highest-salience) trace rather than diverse-content
    consolidation, because replay runs before SHY normalisation flattens
    the attractor landscape. The falsifying comparison is therefore not
    just harm_discrimination(SWS_THEN_REM) > harm_discrimination
    (REM_THEN_SWS), but a diversity/differentiation proxy (slot
    differentiation across contexts, or trajectory diversity in replay
    content) showing REM_THEN_SWS collapsing toward the dominant trace
    while SWS_THEN_REM does not.

    PASS pattern for INV-045 (coarse, partial support only): SWS_THEN_REM
    > REM_THEN_SWS > REM_ONLY ~ WAKING_ONLY on harm_discrimination AND on
    the diversity/differentiation proxy, with REM_THEN_SWS specifically
    showing dominant-trace reinforcement.

    FULL V4 TEST (blocked, not currently buildable): all pairwise/
    adjacent-swap reorderings of the 5 sub-phases against the canonical
    0-1-2-3-4 order, each predicted to fail in the specific way its row
    in offline_phases.md's rewiring table states. Requires Phase 0 (E1
    input gate) and Phase 3's gating half (ThetaBuffer reverse-direction
    mode) to exist as separately runnable, reorderable steps -- neither
    exists in V3.

    NON-DEGENERACY PRECONDITIONS (compounding on ARC-011/INV-046's own):
    (1) full SD-017 flag stack enabled and sufficient training horizon;
    (2) the order-scramble must call the REAL substrate mechanisms, not
    proxy stand-ins -- V3-EXQ-243's null/order-blind result is diagnostic
    evidence this precondition was violated in the only run to date, not
    evidence against the claim itself; (3) for the coarse (V3) test: this
    is satisfiable now, since MECH-120/121/123 are implementation_phase:v3
    and the real agent methods exist post-2026-04; (4) for the FULL
    5-phase test: the substrate must implement Phase 0 and Phase 3's
    sensory-gating half as manipulable steps -- both are V4-gated and
    absent today.

    CURRENT STATE: V3-EXQ-243 (2026-04-05, the only direct evidence on
    file) is FAIL/weakens, but per DR-7 this is very likely an artifact
    of proxy (not real) phase implementations rather than a genuine test
    of the ordering hypothesis -- P1/P2 passed (broken orders didn't beat
    baseline) but P3 also failed (correct order didn't beat baseline
    either, only 1/3 seeds), and all non-WAKING conditions returned
    bit-identical outcome metrics regardless of order. Read the "weakens"
    direction with that qualification, not at face value.
  notes: >
    Registered 2026-04-05. The V4 sub-phase sequence is:
    (0) sensory gating, (1) SHY normalisation, (2) NREM schema replay,
    (3) spindle coordination, (4) REM precision recalibration.
    Each phase is in this position because of what must be true before it
    can run:
    Phase 0 must be first: new input corrupts in-progress consolidation
    because schemas shift during installation. Nothing downstream can run
    while perception is live. Rewiring: input gate on E1 latent stack;
    ThetaBuffer paused for new observations.
    Phase 1 (MECH-120) must precede replay: replaying into a weight
    landscape still dominated by recent high-salience experiences would
    reinforce the dominant trace rather than consolidate diverse content.
    Homeostasis must flatten attractors before replay repopulates them.
    Rewiring: normalisation pass over E1/E2/E3 weights decaying toward
    mean; dominant attractor suppression.
    Phase 2 (MECH-121 + MECH-165) must precede REM: INV-044 applies --
    you cannot fill context slots that do not yet exist. Schema installation
    must produce stable context attractors before causal attribution replay
    can assign evidence to them. Replay diversity (MECH-165) must be in
    this phase because diverse path content must enter the schema, not just
    the dominant trajectory. Rewiring: hippocampus-to-cortex directed
    replay with balanced scheduler (forward + reverse + non-dominant
    paths); ContextMemory templates updated.
    Phase 3 (MECH-122) packages E1 state for bidirectional consolidation.
    ThetaBuffer has been waking-only in V3; it must become bidirectional
    to allow E1 updates to be consolidated into E3 in the reverse direction.
    Rewiring: ThetaBuffer gains a reverse-direction mode; theta-packaged
    E1 updates transferred to hippocampus for long-horizon integration.
    Phase 4 (MECH-123) must be last: resetting precision priors before
    replay (phases 2-3) would change how that evidence is weighted during
    replay -- the prior recalibration must follow, not precede, the content
    consolidation passes. Rewiring: z_beta suppressed (aminergic suppression
    analog); E1 runs unconstrained (no harm gate, unconstrained simulation);
    commitment_threshold and precision_ema_alpha recalibrated from the
    episode's natural range.
    V4 engineering implication: this invariant means the V4 sleep phase
    controller is not arbitrary in its sequencing. A controller that ran
    phases in a different order would either fail to consolidate (wrong
    order of schema vs attribution) or corrupt the weight landscape (replay
    before homeostasis). The ordering is a constraint on the implementation,
    not a design choice. See offline_phases.md for the rewiring table.

# ---- Claims registered 2026-04-05: dementia as attribution pipeline failure ----

- id: INV-046
  title: "Dementia is computationally a progressive failure of contextual attribution rather than primarily a memory storage failure: episodic memory loss is downstream of progressive collapse of the offline consolidation pipeline that maintains the context attribution scaffold."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - SD-017
  pending_substrate_reconfirmation: true
  subject: dementia.attribution_pipeline_failure_reframe
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-045
    - INV-044
    - MECH-166
    - SD-017
  epistemic_category: substrate_conditional
  what_would_answer: |
    INV-046 claims a SPECIFIC FAILURE ORDERING under progressive
    consolidation degradation -- contextual-attribution precision fails
    BEFORE episodic storage/recall does (the Phase 4 -> Phase 2-3 -> Phase
    1-2 -> Phase 0 cascade described in the notes) -- not merely that
    reduced consolidation is harmful, and not merely that the three
    consolidation PHASES fail in a given order among themselves (that
    narrower cross-phase question is SD-073, itself still unsupported
    pending SD-072). INV-046 needs a genuinely GRADED
    consolidation-reduction manipulation crossed with TWO SEPARABLE
    outcome metrics measured at each degradation level:

      (a) ATTRIBUTION-PRECISION metric: contextual slot differentiation/
          discrimination quality -- e.g. slot_cosine_sim / slot_separation
          among ContextMemory slots (the instrument used since the
          SD-017/EXQ-436 family), or a context-discrimination accuracy
          readout.

      (b) STORAGE/RECALL metric: whether the agent can retrieve or
          recognise a specific past event AT ALL, independent of whether
          it is attributed to the correct context -- e.g. a cued-recall /
          event-recognition accuracy readout against injected known
          content (following the SD-068/SD-071 injected-content
          precedent, which sidesteps the "sleep cannot consolidate an
          unencoded representation" starvation ceiling from
          failure_autopsy_V3-EXQ-538a). THIS METRIC DOES NOT YET EXIST in
          the codebase and must be built -- grepped 2026-08-07, no
          recall_accuracy / episodic_recall / retrieval_accuracy
          instrument found anywhere in experiments/_lib or ree_core.

    DESIGN: progressively reduce consolidation cycle count/depth/duration
    (or, reusing SD-068's existing graded RMS-scaled Gaussian
    diffuse-damage knob in
    ree-v3/experiments/_lib/consolidation_lesion_harness.py as an
    alternative graded-degradation lever) across >=4 staged levels,
    matched for total experience/compute, and score (a) and (b) at each
    level.

    CONFIRMED if attribution-precision (a) shows measurable, statistically
    reliable degradation at a LOWER degradation level than storage/recall
    (b) does -- a genuine phase-order dissociation, attribution failing
    first.

    FALSIFIED if storage/recall (b) degrades first or simultaneously with
    attribution (a), or if the two metrics degrade at statistically
    indistinguishable rates across the degradation ramp -- this
    specifically undercuts INV-046's "not primarily a storage failure"
    reframing, even if reduced consolidation is shown to be harmful
    overall (which would instead support the weaker ARC-011/INV-010-style
    claim, not INV-046's specific reordering).

    NON-DEGENERACY PRECONDITIONS (compounding on ARC-011's, which this
    claim inherits since it shares the same substrate):
      (i) Full sleep flag stack and a horizon/stressor sufficient for
          degradation to manifest in EITHER metric at all -- ARC-011's
          two closest attempts (EXQ-430, V3-EXQ-691) both found no
          differential effect at the tested horizon, read as
          substrate-gated/inconclusive, not falsifying.
      (ii) The degradation manipulation must be genuinely GRADED (>=4
          distinct levels), not binary sleep-ON/sleep-OFF -- a binary
          design cannot distinguish a phase-order claim from a simple
          presence/absence effect.
      (iii) The two outcome metrics (a) and (b) must be SEPARABLE and not
          collapse onto the same underlying signal -- the recall
          instrument should be validated independently (e.g. via an
          injected-content positive control showing it responds to gross
          storage failure even when attribution structure is held intact)
          before it is trusted as the (b) arm.
      (iv) SD-017's own most recent direct substrate test (V3-EXQ-436c,
          2026-08-03, confirmed) found slot_cosine_sim COLLAPSING toward
          1.0 under SWS_THEN_REM -- homogenization, the opposite of the
          differentiation SD-017/INV-046 predict -- flagged
          pending_retest_after_substrate on SD-017/ARC-045/MECH-166
          pending an untested ContextMemory.write_gate collapse
          hypothesis. A run of this experiment against the CURRENT
          substrate risks confounding "attribution degrades first" with
          "attribution never differentiates at all" -- re-check this
          precondition (re-run SD-017's own diagnostic, or wait for the
          write-gate fix) before queuing.

    REUSABLE INFRASTRUCTURE: the graded per-phase diffuse-damage knob
    (ree-v3/experiments/_lib/consolidation_lesion_harness.py, SD-068) and
    its validated content-contingent nrem+sws readouts (SD-071) are
    directly reusable for the manipulation and for metric (a)'s
    injected-content scoring precedent; SD-073 (staging order among the
    three PHASES) is a structurally similar but DISTINCT claim -- it
    ranks SWS/NREM/REM fidelity against each other, not attribution
    against storage -- and remains itself unsupported pending SD-072 (REM
    leg unbuilt), so it cannot be borrowed as evidence for INV-046 even
    once SD-072 lands.
  notes: >
    Registered 2026-04-05. The conventional framing of dementia as memory
    storage failure mislocates the primary lesion. The offline consolidation
    pipeline (SWS-analog schema installation, REM-analog slot-filling,
    MECH-120 homeostasis, MECH-123 precision recalibration) is the
    infrastructure that makes contextual attribution possible. Progressive
    collapse of this pipeline produces progressive failure of contextual
    attribution -- which manifests as the clinical syndrome of dementia.
    Memory loss is not the primary failure; it is the observable consequence
    of the agent losing the ability to attribute the meaning of its experiences
    to the correct context slots.
    Relationship to existing claims: MECH-124 describes consolidation-mediated
    option-space contraction (Walker PTSD analog -- imbalanced replay produces
    over-consolidation of harm traces). INV-046 describes a distinct failure
    mode: insufficient consolidation cycles producing progressive dissolution
    of the context scaffold itself. PTSD over-consolidates; dementia under-
    consolidates. The failure mode, direction, and symptom profile are different.
    Derivation: follows directly from the REE attribution pipeline. An agent
    whose offline phases degrade progressively will first lose precision in
    contextual attributions (Phase 4 failure), then lose new episodic slot-
    filling (Phase 2-3 failure), then lose the context slots themselves (Phase
    1-2 failure), then lose global contextual orientation (Phase 0 equivalent).
    This is not an analogy to dementia -- it is what the computational account
    predicts would happen to any agent with progressively degrading offline
    consolidation cycles.
  pharmacological_predictions:
    - PHARM-007  # anticholinergic dual-pathway dementia risk
    - PHARM-010  # DORA pipeline preservation in MCI risk

- id: INV-047
  title: "The specific staged cognitive decline profile of Alzheimer's disease -- early precision/confidence loss, then episodic memory failure, then contextual identity loss, then global disorientation -- follows the reverse of the sleep phase dependency ordering and is not explained by diffuse amyloid damage alone, which predicts uniform degradation rather than a staged profile."
  claim_type: derived_prediction
  prediction_domain: clinical_neurology
  subject: dementia.staged_decline_phase_dependency_prediction
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-778h_2026-07-19
      as_of: 2026-07-19
      verdict: non_contributory/measurement_gap
  evidence_quality_note: |
    [SD-068 staged-damage diagnostic, 2026-07-17] DIAGNOSTIC, no status change.
    The staged-decline SHAPE this prediction asserts is instrumented by the SD-068
    consolidation-lesion harness. V3-EXQ-778a (8 seeds): NREM-before-SWS adjacency
    robust (8/8, p=0.0078), REM-fails-first seed-variable/underpowered (CI straddles
    0). Non-vacuity: the REM generative gain (offline banking, current harness) is
    attenuating (0.149 mean, 8/8 seeds < 1 << passthrough 1.0) -> staged decline is
    a real transfer function, not topology. Full reading in the MECH-168
    evidence_quality_note + docs/architecture/sd_068_consolidation_lesion_harness.md.
    [AMENDED 2026-07-18 -- V3-EXQ-778c SD-068 null-content control, n=8] The
    "NREM-before-SWS adjacency robust (8/8, p=0.0078)" half of the 2026-07-17 entry
    above is RETRACTED as evidence of staging. The zero-injected-content null control
    shows the sws pole of that adjacency is a CONTENT-FREE readout (null_slope_ratio
    1.0000, sd 2.7e-8, 8/8 seeds confounded): denoising_snr_db is
    10*log10(signal_power/noise_power) and noise_power is identical with and without
    injected content at every sigma, so the sigma-slope is driven entirely by
    log(noise_power) and the content term differentiates away. The adjacency therefore
    compares a content-contingent readout (nrem, ratio 0.1445, 0/8 confounded) against
    a noise statistic. The tightness of the 95% CI [0.422,0.425] is the signature of a
    near-deterministic analytic metric, not strength of effect (sws tolerance std ~9e-9
    vs nrem 0.0014, rem 0.396). The REM-fails-first half was already contested and is
    unchanged. The rem leg is degenerate at both clamp rails and UNINTERPRETABLE at
    n=8. The non-vacuity finding (REM generative gain 0.149, 8/8 attenuating) is a
    DIFFERENT readout and is NOT overturned, though its own content-dependence is now
    an open question (H-gen-gain-content-free). NO CONFIDENCE CHANGE: V3-EXQ-778c tags
    this claim as CONTEXT only and its per-claim evidence_direction is unknown -- this
    amends an annotation, not the claim. See
    evidence/planning/failure_autopsy_V3-EXQ-778c_2026-07-18.
  what_would_answer: >
    This is a FUSION claim: an out-of-domain clinical-empirical assertion (leg A) plus a
    REE-internal, V3-testable mechanistic claim (leg B, operationalized as SD-073). The two legs
    answer independently and neither substitutes for the other.

    LEG A -- OUT-OF-DOMAIN (clinical/literature; no REE substrate can test this). Whether ACTUAL
    human Alzheimer's disease follows the specific ordering asserted here (precision/confidence
    loss -> episodic memory failure -> contextual identity loss -> global disorientation) is a
    clinical-neurology question the REE substrate cannot itself adjudicate. It resolves by a
    formal comparison of the claim's predicted symptom-emergence ordering against the published
    staged-clinical-progression literature (e.g. CDR/FAST staging, MCI-to-AD conversion studies,
    longitudinal neuropsychological batteries) -- not by the qualitative citation already present
    in this claim's notes, which has never been formally checked against primary sources.
    CONFIRMING: a systematic review or meta-analysis of longitudinal staging data shows the
    predicted ordering (or one consistent with it under this claim's own domain mapping) as the
    dominant clinical pattern. FALSIFYING: the literature shows the four domains failing in a
    different order, failing diffusely/simultaneously (consistent with the glymphatic-alone
    account this claim contrasts itself against), or the ordering is not reproducible across
    cohorts. Route: /lit-pull, not an experiment.

    LEG B -- REE-INTERNAL (V3-testable via the SD-068 lesion harness; operational scoring site is
    SD-073, which this claim and MECH-168 jointly feed).

    NON-DEGENERACY PRECONDITION (currently PARTIALLY UNMET -- read before trusting any staging-order
    result). A per-phase readout is usable for this test only if CONTENT-CONTINGENT: responsive to
    corrupted/injected content, not merely to corruption magnitude or noise power. Any new falsifier
    design MUST explicitly re-run this precondition check per phase -- the same null-content control
    that caught the SWS readout's content-freeness (V3-EXQ-778c) and that a REM unpaired-target null
    re-confirmed as insufficient (V3-EXQ-778h) -- before that phase's output can count as staging
    evidence. Status per phase (2026-07-18 SD-068 decomposition, unchanged as of 2026-08-07):
    NREM (slot-filling, MECH-121) PASSED, content-contingent (null_slope_ratio 0.1445, CI95
    [0.1438,0.1451], 0/8 seeds confounded, V3-EXQ-778c) -- carried by SD-071. SWS (slot-formation,
    MECH-120) PASSED after rebuild, content-contingent (null_slope_ratio 0.1495, sd 0.0218, CI95
    [0.1344,0.1646], ceiling_inside_ci95 false, 8/8 seeds, V3-EXQ-778g) -- carried by SD-071. REM
    (precision recalibration, MECH-123) FAILED on every design tried -- content-free at both clamp
    rails (V3-EXQ-778c), still content-free after de-clamping via an unpaired-target null (median
    ratio ~0.88 vs the 0.25 ceiling, 6/7 de-railed seeds above it, V3-EXQ-778h), generative-gain
    contrast flat in content scale (V3-EXQ-778f). MECH-462 explains this as a TYPE fact, not a
    fixable design bug: REM precision recalibration is a second-order quantity over representations
    (a confidence ABOUT content, not content itself), so no statistic on the direct precision
    quantity can be content-contingent regardless of null design. This is the leg carrying INV-047's
    CLINICALLY EARLIEST predicted symptom (precision/confidence loss as the MCI prodrome) -- the
    missing instrument sits exactly where this claim is most specific and would be most informative.

    BUILD GATE -- complicated (buildable), not probe-gated: the missing piece is a known-shape
    build. SD-072 specifies the fix: replace the direct REM readout with a downstream
    behavioural-discrimination measure (a meta-d-prime analogue over post-REM retrieval confidence),
    applied to the same cells as V3-EXQ-778e, gated by the SAME C1 instrument-validity criterion
    that admitted NREM and SWS (null_slope_ratio_rem_downstream <= 0.25, ceiling_inside_ci95 false,
    n>=8, plus a pre-registered content-scale ladder). SD-072 is UNBUILT as of 2026-08-07 -- no
    queue entry, no run.

    CONFIRMING signature (once SD-072 exists and clears its own instrument-validity gate, scored at
    SD-073): re-running the staged sweep with all three now-validated content-contingent instruments
    (SD-071's nrem + sws, SD-072's REM replacement) at a declared insult regime shows the
    damage-tolerance rank order matching the predicted reverse-dependency sequence -- REM/precision
    (MECH-123, Phase 4) degrades first, NREM/slot-filling (MECH-121, Phase 2-3) next, SWS/
    slot-formation (MECH-120, Phase 1-2) last -- on >=6/8 seeds (sign-test p<0.05, not a bare modal
    order), robust within the declared regime.

    FALSIFYING signature: the validated three-instrument staging run shows simultaneous degradation,
    the forward-dependency order, an order inverting REM-vs-NREM (as the retracted 2026-07-17 smoke
    run did), or seed-instability comparable to the pre-778h REM-fails-first finding (CI straddling
    0, sign-test ~1.0) even with all three legs interpretable. Also FALSIFYING for the REE leg
    becoming testable at all: if SD-072's downstream discrimination readout is ITSELF content-free
    at the same C1 ceiling, MECH-462's constructive half is wrong and REM carries no recoverable
    damage signal by any design tried so far.

    CURRENT STATE (2026-08-07 digestion pass; do not re-litigate without new evidence): neither leg
    resolved. Leg A never formally run against primary staging literature. Leg B two-thirds
    instrumented (SD-071 done); one-third gated on a fully specified but unbuilt component (SD-072);
    SD-073 (the staging order itself) is EXPLICITLY NOT ANSWERABLE until SD-072 lands, per SD-073's
    own registration. V3-EXQ-778h (2026-07-19) added no new information -- it reconfirmed, via a
    corrected readiness anchor, the same REM content-freeness already folded into MECH-462/SD-072
    one day earlier (2026-07-18).
  depends_on:
    - INV-046
    - INV-045
    - MECH-168
  notes: >
    Reclassified from claim_type: invariant in Session C batch 2026-04-17.
    Rationale: this is a clinical prediction about Alzheimer's staged decline,
    which holds only conditional on REE correctly modelling human cognition --
    a domain-modelling assumption distinct from REE's architectural invariants
    (per Daniel's 2026-04-17 objection, audit registry INV-047).
    Registered 2026-04-05. The glymphatic clearance hypothesis (amyloid
    accumulation without sleep; Xie/Nedergaard 2013) predicts substrate
    damage that should be diffuse across the consolidation pipeline, not
    staged. But the clinical presentation of Alzheimer's is highly staged:
    new episodic learning fails before remote memory; contextual identity
    (who is this person, what does this place mean) fails before procedural
    skill; global orientation fails last. Diffuse damage cannot explain
    this profile.
    The attribution pipeline dependency ordering predicts exactly this staging:
    Phase 4 (precision recalibration, MECH-123) is the most downstream and
    most vulnerable -- its failure is the earliest detectable symptom (the
    "something feels off" prodrome of MCI, reduced confidence in attributions
    before overt memory failure). Phase 2-3 (slot-filling, MECH-121) failure
    produces the classic amnesic profile: intact remote schemas, absent new
    consolidation. Phase 1-2 (slot-formation, MECH-120) failure destabilises
    existing schemas -- middle-stage loss of contextual identity. Phase 0
    equivalent failure produces global contextual collapse.
    This is a specific, falsifiable prediction: the temporal ordering of
    symptom emergence should map onto the reverse of the phase dependency
    graph, not onto the spatial distribution of amyloid/tau pathology.
    Neuroanatomical note: hippocampal neurodegeneration (early in Alzheimer's)
    selectively impairs the hip->cx information direction (SWS-analog, schema
    installation) before disrupting the cx->hip direction (waking encoding),
    predicting that schema formation fails before schema retrieval -- consistent
    with the amnesic profile of early Alzheimer's (new learning fails before
    remote memory access).
    SD-068 fold (2026-07-17, IGW-20260717-206): INV-047's staged-clinical-decline
    prediction is the clinical-domain twin of MECH-168's pipeline prediction and the
    V3-testable realization of MECH-169's attribution-pipeline half. Now instrumented
    by the SD-068 consolidation-lesion harness + diagnostic V3-EXQ-778 (diagnostic,
    non-scoring). MECH-169's glymphatic half has no V3 analog (out of scope). No
    confidence change.
  pharmacological_predictions:
    - PHARM-014  # pipeline-rebuild dissociation prediction (staged decline reverses in pipeline-mediated treatment)

- id: MECH-168
  title: "Progressive sleep deprivation or neurodegeneration causes staged failure of the offline consolidation pipeline in reverse dependency order: precision recalibration (MECH-123/Phase 4) degrades first, producing early MCI features; slot-filling (MECH-121/Phase 2-3) next, producing the classic early Alzheimer's episodic profile (intact remote, absent new); slot-formation (MECH-120/Phase 1-2) last, producing middle-stage contextual identity loss and schema destabilisation; global context collapse last."
  claim_type: mechanism_hypothesis
  subject: dementia.consolidation_pipeline_staged_failure
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-778h_2026-07-19
      as_of: 2026-07-19
      verdict: non_contributory/measurement_gap
  evidence_quality_note: |
    [SD-068 staged-damage diagnostic, 2026-07-17] DIAGNOSTIC (non-scoring;
    promotes/demotes nothing; MECH-121 remains held, its NREM leg is
    substrate-plumbing-fidelity only). The SD-068 consolidation-lesion harness
    instruments this claim's staging prediction under uniform diffuse damage.
    Two legs, reported together (detail: docs/architecture/sd_068_consolidation_lesion_harness.md
    "Diagnostic results"):
    (1) STAGING ORDER (V3-EXQ-778a, 8 seeds, PASS/supports): the
    NREM-fails-before-SWS adjacency is ROBUST (8/8 seeds, sign-test p=0.0078,
    95% CI [0.422,0.425]); the REM-fails-FIRST adjacency is CONTESTED /
    seed-variable / underpowered at n=8 (mean tolerance-diff -0.097, 95% CI
    [-0.43,+0.23] straddles 0, sign-test p=1.0; overall Spearman mean-rho 0.375,
    CI includes 0). So the reverse-dependency staging is PARTIALLY supported --
    the upstream half holds, the REM-first head is unresolved at this power.
    (2) NON-VACUITY (REM generative gain, offline banking run of the current
    harness rem_generative_fidelity readout, 8 seeds -- the readout is in NO
    778/778a manifest; 778 ran the retired null proxy, 778a scored order off
    tolerances): rem_generative_gain = 0.149 mean (0.040-0.409), ATTENUATING
    (gain<1) on 8/8 seeds, decisively below the passthrough leg's gain of 1.0.
    The REM staged-first-failure is therefore NOT pure feed-forward topology --
    the generative re-derivation pass has real corrective capacity ("the
    correction needs an intact seed"). This is diagnostic evidence for the SHAPE
    of the staged-decline prediction, not promotion evidence.
    [AMENDED 2026-07-18 -- V3-EXQ-778c SD-068 null-content control, n=8, FAIL/weakens
    on SD-068] The pre-registered zero-injected-content null control audited whether
    the per-phase readouts leg (1) is built from measure CONTENT FIDELITY or raw NOISE
    SENSITIVITY. Load-bearing C1 failed on 0/8 seeds. Per phase null_slope_ratio
    (|null sigma-slope| / |injected sigma-slope|; ~0 = content-contingent, ~1 =
    confounded): nrem 0.1445 (95% CI [0.1438,0.1451], 0/8 confounded) CONTENT-
    CONTINGENT; sws 1.0000 (sd 2.7e-8, 8/8 confounded) CONTENT-FREE BY CONSTRUCTION --
    noise_power is identical across arms at every sigma while signal_power is 5585.7
    vs 0.0, so the log-ratio's content term differentiates out of the slope; rem
    degenerate at BOTH rails (exactly 0.0 on 5/8 seeds from a saturated
    calibration_error constant with target_clamped 1.0, off-scale 1801-9143 on 3/8
    from the 1e-3 precision floor), ceiling_inside_ci95 true, UNINTERPRETABLE at n=8.
    CONSEQUENCE FOR LEG (1): RETRACTED as staging evidence. The NREM-before-SWS
    adjacency compares a working readout against a content-free one, and its
    conspicuously tight CI [0.422,0.425] is the fingerprint of a near-deterministic
    analytic metric rather than a robust effect. No leg of the reverse-dependency
    staging order is currently supported by a validated instrument. CONSEQUENCE FOR
    LEG (2): STANDS -- rem_generative_fidelity is a different readout with its own
    internal clean-vs-corrupt control and C1 does not bear on it; but the null arm's
    rem_generative_gain (~0.182-0.209 on seed 42) is close to the injected arm's
    (~0.165-0.190) at rem_gen_content_scale 0.0, which sits OUTSIDE the scored criteria
    and is registered as an open question against the "correction needs an intact seed"
    gloss specifically (NOT against the attenuation finding). Readiness precondition
    met and C2 passed, so this is an informative negative about the INSTRUMENT, not a
    broken run. NO CONFIDENCE CHANGE: V3-EXQ-778c tags this claim as CONTEXT only
    (per-claim evidence_direction unknown); it audits the harness (SD-068), not this
    claim, and MECH-121 remains held. Routing: /implement-substrate to replace
    sws_denoising_snr with a content-scored readout, plus a GOV-FANOUT-1 three-axis
    portfolio on the rem leg. See
    evidence/planning/failure_autopsy_V3-EXQ-778c_2026-07-18.
  depends_on:
    - INV-046
    - INV-047
    - INV-045
    - MECH-120
    - MECH-121
    - MECH-123
    - SD-017
  notes: >
    Registered 2026-04-05. The mechanism: each phase of the offline
    consolidation pipeline depends on the phases upstream of it. When the
    pipeline degrades progressively (via sleep deprivation, neurodegeneration,
    or disrupted sleep architecture), the most downstream phases fail first
    because they are furthest from the biological substrate and depend on the
    most intact upstream machinery.
    Phase 4 (MECH-123 precision recalibration) fails first because it
    requires the full pipeline to have run -- any degradation in earlier
    phases propagates to imprecise precision calibration. Clinical correlate:
    early MCI, reduced confidence in attributions, increased uncertainty about
    whether current experience matches prior context, the subjective sense of
    unreliability before overt memory failure.
    Phase 2-3 (MECH-121 slot-filling) fails next. New episodic experiences
    stop consolidating into existing context schemas. Recent events, new
    faces, and new environments fail to attach to the correct context slots.
    Remote memories (consolidated before pipeline degradation) remain
    accessible. Clinical correlate: classic early Alzheimer's amnesic profile.
    Phase 1-2 (MECH-120 + MECH-121 slot-formation) fails last. The existing
    schema structure begins to destabilise -- not just failure to add new
    content, but erosion of the context attractors themselves. Who is this
    person? What does this place mean? The agent loses the slots, not just
    their contents. Clinical correlate: middle-stage Alzheimer's, loss of
    contextual identity.
    Global collapse: with no stable context scaffold remaining, attribution
    is impossible. The agent is oriented to nothing. Clinical correlate:
    late-stage dementia.
    Distinction from MECH-124 (PTSD failure mode): MECH-124 is
    consolidation-mediated option-space contraction via imbalanced replay --
    too much consolidation of harm traces, too little of alternative paths.
    MECH-168 is insufficient consolidation cycles producing progressive
    dissolution of the context scaffold. PTSD consolidates the wrong content;
    dementia fails to consolidate at all.
    SD-068 (2026-07-17, IGW-20260717-206): this staged-failure-in-reverse-dependency-
    order prediction is now instrumented at the V3 substrate by the SD-068
    consolidation-pipeline lesion harness (per-phase diffuse damage + per-phase
    functional readouts, non-vacuous error-propagation-gain), validated by diagnostic
    V3-EXQ-778. MECH-168 (with INV-047) is the V3-testable realization of MECH-169's
    attribution-pipeline half (MECH-169's glymphatic half has no V3 analog). Diagnostic,
    non-scoring; no confidence change.
  what_would_answer: |
    NON-DEGENERACY PRECONDITION (currently UNMET across all attempted legs -- read
    this before trusting any staging-order result). The claim can only be tested by
    a per-phase consolidation readout that is CONTENT-CONTINGENT: it must respond to
    corrupted/injected content, not merely to corruption MAGNITUDE or noise power.
    Three independent checks (V3-EXQ-778c null-content control; V3-EXQ-778h
    unpaired-target null) found every readout tried so far fails this bar: the SWS
    readout is content-free BY CONSTRUCTION (null_slope_ratio sd 2.7e-8, 8/8
    confounded), and the REM calibration_error-slope readout tracks corruption
    magnitude irrespective of content even after its positivity-clamp artifact was
    fixed (median unclamped null_slope_ratio ~0.88 against a 0.25 content-contingency
    ceiling, 6/7 de-railed seeds above ceiling). A run against either readout in its
    current form should self-route measurement_gap, NOT count as a verdict for or
    against the staging order -- this has already happened three times (V3-EXQ-778,
    778c, 778h) and a fourth repeat without a new readout axis re-derives nothing.
    The re-derive brake is primed: SD-068/MECH-168/MECH-169/INV-047 each already
    carry one non_contributory reading: a second on the SAME unrepaired readout
    fires the brake and forces implement-substrate routing rather than another
    lettered iteration. Prerequisite for an informative test: a content-sensitive
    per-phase readout (tracking injected-content fidelity, not raw noise/corruption
    sensitivity) for at least the SWS and REM legs, applied via the SD-068 diffuse
    per-phase lesion harness across n>=8 seeds.
    CONFIRMING signature: with a content-valid readout in place for each phase, the
    functional-decline order across a majority of seeds (sign-test p<0.05, not a
    bare reported/modal order) matches the predicted reverse-dependency sequence --
    precision recalibration (MECH-123/Phase 4) degrades first, slot-filling
    (MECH-121/Phase 2-3) next, slot-formation (MECH-120/Phase 1-2) last, with REM
    failing before SWS-mediated downstream processing -- under uniform diffuse
    damage, robust across independent seeds rather than resting on a single
    near-deterministic metric.
    FALSIFYING signature: a content-valid readout shows the phases degrade
    simultaneously, in the forward-dependency order, or with SWS/NREM-mediated
    processing failing before REM, with comparable seed-robustness to the
    confirming case -- or the REM-fails-first adjacency remains seed-variable /
    underpowered (CI straddling 0, sign-test ~1.0, as it was even before the
    content-validity problem was found) once a valid instrument is used.
    CURRENT STATE (do not re-litigate without new evidence): no leg of the
    reverse-dependency staging order is presently supported by a validated
    instrument -- the one adjacency that initially looked robust (NREM-before-SWS,
    8/8 seeds) was retracted as staging evidence once shown to be a content-free
    metric artifact. This is a measurement gap, not a mechanism-level result in
    either direction; MECH-168 remains untested at the ordering level. One
    orthogonal, still-standing finding: REM's generative re-derivation pass shows
    real corrective capacity (attenuating gain <1 on 8/8 seeds), which bears on
    whether REM failure is pure feed-forward topology, not on the staging order
    itself.

- id: MECH-169
  title: "The glymphatic clearance hypothesis (amyloid-beta accumulation via insufficient sleep-phase clearance) and the attribution pipeline account (consolidation failure via disrupted offline phases) are mechanistically complementary accounts of sleep-deprivation-induced dementia: the former explains why structural substrate damage accumulates; the latter explains why cognitive symptoms are staged and context-specific rather than diffuse and uniform."
  claim_type: mechanism_hypothesis
  subject: dementia.glymphatic_attribution_complementarity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-778h_2026-07-19
      as_of: 2026-07-19
      verdict: non_contributory/measurement_gap
  depends_on:
    - INV-046
    - INV-047
    - MECH-168
  notes: >
    Registered 2026-04-05. The glymphatic hypothesis (Xie et al. 2013,
    Science: glymphatic system clears metabolic waste including amyloid-beta
    predominantly during sleep; sleep deprivation leads to amyloid accumulation)
    is well-supported and explains the substrate damage pathway. MECH-169 does
    not challenge it -- it extends it.
    The glymphatic account leaves two things unexplained: (1) why the cognitive
    symptom profile is staged rather than uniform given that amyloid accumulates
    diffusely; (2) why the specific profile is contextual-attribution failure
    rather than, say, motor or sensory failure. MECH-168/INV-047 provide the
    explanation: the attribution pipeline has a dependency ordering that produces
    staged failure even under diffuse substrate damage, because downstream phases
    fail before upstream phases.
    The two accounts are therefore complementary:
    - Glymphatic: explains structural damage accumulation (why sleep deprivation
      causes Alzheimer's risk at the substrate level)
    - Attribution pipeline: explains symptom profile shape (why the cognitive
      decline is staged, contextual, and follows the reverse of the phase
      dependency ordering)
    A complete mechanistic account of sleep deprivation-induced dementia requires
    both. The glymphatic account without the attribution pipeline account cannot
    predict the clinical phenotype. The attribution pipeline account without the
    glymphatic account cannot explain why sleep deprivation specifically (rather
    than other forms of neural disruption) is the primary risk factor.
    SD-068 fold (2026-07-17, IGW-20260717-206, user-adjudicated): MECH-169's ONLY
    V3-testable content is the staged-decline-under-uniform-damage prediction, which
    is exactly what INV-047 and MECH-168 already assert. The glymphatic / amyloid
    structural-damage half has NO V3 analog (unrepresentable). So MECH-169 is NOT
    pursued as a distinct V3 experiment; its V3-testable staging content is FOLDED
    onto INV-047 / MECH-168, now instrumented by the SD-068 consolidation-pipeline
    lesion harness + diagnostic V3-EXQ-778. MECH-169 stays the complementarity
    umbrella (its glymphatic half is real, just out-of-V3-scope). No confidence
    change; EVB-0115 stays blocked_substrate (glymphatic unrepresentable + MECH-121
    held).
    SD-068 diagnostic outcome (2026-07-17, DIAGNOSTIC -- no confidence change): the
    folded staging half now has a partial diagnostic reading on the V3-testable
    side. V3-EXQ-778a (8 seeds, PASS/supports) shows the NREM-before-SWS staging
    adjacency ROBUST (8/8, sign-test p=0.0078) but the REM-fails-first adjacency
    seed-variable / underpowered (CI straddles 0). The non-vacuity check -- the REM
    generative-re-derivation gain, banked offline from the current harness readout
    (rem_generative_fidelity; in no 778/778a manifest) -- is ATTENUATING (gain 0.149
    mean, 8/8 seeds < 1, vs passthrough gain 1.0), so the staged decline rests on a
    real error-propagation transfer function, not feed-forward topology. See the
    MECH-168 evidence_quality_note and docs/architecture/sd_068_consolidation_lesion_harness.md.
    SD-068 fold-note AMENDED 2026-07-18 (V3-EXQ-778c null-content control, n=8,
    DIAGNOSTIC -- no confidence change): the folded staging half's support is narrowed
    to the nrem leg only. The pre-registered zero-content null control shows that of
    the three per-phase readouts the staging order is built from, the sws leg is
    CONTENT-FREE (null_slope_ratio 1.0000, sd 2.7e-8, 8/8 seeds -- its
    log(signal/noise) form differentiates the content term out of the sigma-slope) and
    the rem leg is DEGENERATE at both clamp rails and uninterpretable at n=8; only
    nrem is confirmed content-contingent (0.1445, 0/8 confounded). The
    NREM-before-SWS "ROBUST 8/8 p=0.0078" reading recorded above is therefore
    RETRACTED as staging evidence -- it compares a working readout against a noise
    statistic, and its tight CI is a near-deterministic-metric artifact. The
    non-vacuity leg (REM generative gain 0.149, 8/8 attenuating) is a different
    readout and STANDS, though its own content-dependence is now an open question.
    The glymphatic half remains out of V3 scope and is untouched. MECH-169 is tagged
    as CONTEXT only on V3-EXQ-778c (per-claim evidence_direction unknown). See
    evidence/planning/failure_autopsy_V3-EXQ-778c_2026-07-18.

- id: MECH-170
  title: "Sleep architecture restoration in early MCI should produce dissociated cognitive improvement: slot-filling (episodic attribution, MECH-121 analog) should improve before slot-formation (contextual semantic, MECH-120 analog), because the pipeline rebuilds in dependency order. This distinguishes the attribution account from accounts predicting uniform memory improvement with sleep restoration."
  claim_type: mechanism_hypothesis
  subject: dementia.sleep_restoration_dissociated_recovery_prediction
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-168
    - INV-047
    - MECH-120
    - MECH-121
  notes: >
    Registered 2026-04-05. Testable prediction: if the attribution pipeline
    account is correct, sleep architecture restoration in early MCI patients
    should not produce uniform improvement across memory domains. The pipeline
    rebuilds in dependency order: Phase 4 (precision) restores first, then
    Phase 2-3 (slot-filling/episodic), then Phase 1-2 (slot-formation/semantic).
    Therefore the predicted sequence of improvement with sleep intervention is:
    (1) improved confidence and precision in contextual attributions (early,
    subtle, may require sensitive testing); (2) improved new episodic memory
    consolidation (formation of new slot-filling associations); (3) improved
    contextual semantic memory (only if intervention is sustained long enough
    for schema rebuilding).
    This dissociation distinguishes the attribution account from:
    - General memory consolidation accounts, which predict proportional
      improvement across all memory types
    - Glymphatic accounts, which predict improvement proportional to
      reduced amyloid load (uniform across domains)
    Clinical operationalisation: prospective sleep intervention study in
    early MCI, measuring episodic (new word pairs, face-name) vs semantic
    (contextual categorisation, situation identity) memory at intervals.
    Attribution account predicts episodic recovery precedes semantic recovery.
    The prediction is specific enough to be falsifiable within a single
    randomised controlled trial.

- id: MECH-171
  title: "Progressive neurodegeneration creates a vicious cycle through sleep disruption: early attribution pipeline failure disrupts sleep architecture, which accelerates consolidation pipeline failure, which accelerates attribution failure. This predicts disproportionate disease-modifying effect of early sleep intervention, because it breaks the cycle before the schema scaffold collapses."
  claim_type: derived_prediction
  subject: dementia.sleep_disruption_vicious_cycle
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_batch9_2026-06-12#V3-EXQ-673"
      as_of: 2026-06-12
      verdict: non_contributory/out_of_domain
  epistemic_category: out_of_domain
  governance_2026_06_14: >
    User-adjudicated claim_type reclassification mechanism_hypothesis ->
    derived_prediction (resolves the pending question flagged in the 06-12 +
    06-13 notes). MECH-171 is a clinical-Alzheimer's prediction DERIVED from the
    REE attribution/consolidation architecture, testable only in clinical
    cohorts -- not a REE-substrate mechanism. Matches sibling treatment
    (INV-048 -> derived_prediction 2026-04-17; MECH-173 derived_prediction;
    MECH-172 reclassified same pass). epistemic_category stays out_of_domain.
    Also: V3-EXQ-673 x3 reruns propagated to non_contributory in their runs/
    pack manifests (the failure_autopsy_batch9 disposition had only reached the
    flat manifests; the indexer scores the pack copy) -- this clears the 3
    phantom "weakens" that re-surfaced the hold_candidate_resolve_conflict
    mis-fire each cycle. PROMOTES/DEMOTES NOTHING (exp_conf -> 0, lit_conf 0.87).
  what_would_answer: >
    A clinical-cohort longitudinal study showing (or failing to show) that early
    sleep intervention in MCI/pre-dementia has a disproportionate
    disease-modifying effect relative to late intervention.
  governance_2026_06_12: >
    epistemic_category set out_of_domain (failure_autopsy_batch9_2026-06-12,
    user-adjudicated; IGW experiment-surfacing suppression + exp_conf-gating
    exemption, NOT a promote/demote). MECH-171 asserts clinical-Alzheimer's
    staged progression (multi-year vicious cycle, critical intervention window,
    schema-scaffold collapse) not faithfully instantiable in a 300-episode
    grid-world -- sibling-consistent with MECH-172 (governance_2026_06_10).
    V3-EXQ-673 (vicious-cycle sleep disruption) FAILed degenerate: all arms
    identical, late_pred_loss==0; it ran 3x under one EXQ (queue entry retired,
    the three content-identical reruns must not multi-count). The degenerate
    manifests are non_contributory and do not press the claim. NOTE FOR USER:
    claim_type left mechanism_hypothesis pending your decision (reads as a
    clinical research/literature anchor, cf. INV-048 -> derived_prediction,
    and the MECH-172 same-pending note).
  governance_2026_06_13: >
    Governance cycle re-surfaced the standard hold_candidate_resolve_conflict
    recommendation (conflict_ratio=0.857, S4/W3) because conflict alerts still
    fire for out_of_domain claims. User-adjudicated MIS-FIRE: the recommendation
    does NOT apply -- no V3 conflict-resolution experiment can settle a
    clinical-Alzheimer's staged-progression claim whose test domain is outside
    REE's substrate (see governance_2026_06_12). The conflicting "weakens"
    entries are V3 grid-world runs that cannot faithfully instantiate the
    multi-year vicious cycle and are non-discriminative by domain, not genuine
    counter-evidence. Disposition: keep candidate, no status/confidence change;
    claim_type-reclassification (research_anchor / derived_prediction) still
    deferred per the 06-12 note. PROMOTES/DEMOTES NOTHING.
  depends_on:
    - MECH-168
    - INV-046
  notes: >
    Registered 2026-04-05. The vicious cycle operates in both directions:
    Forward: sleep deprivation -> consolidation failure -> attribution failure
    -> behavioural disorientation -> increased arousal/anxiety -> further
    sleep disruption.
    Reverse: early neurodegeneration (hippocampal volume loss) disrupts the
    hip->cx information pathway (SWS-analog direction) -> schema installation
    fails -> REM-analog slot-filling operates against an unstable prior ->
    precision recalibration is less effective -> waking behaviour is less
    well-calibrated -> increased cognitive effort and arousal -> further
    sleep disruption.
    The vicious cycle predicts: (1) sleep disruption should be both a risk
    factor for and an early symptom of Alzheimer's (consistent with Lucey
    et al. 2017, JAMA Neurology: sleep disruption precedes amyloid deposition
    by years); (2) the cycle amplifies -- early mild disruption becomes
    severe disruption as the attribution scaffold degrades; (3) early
    intervention (before schema scaffold collapse) has disproportionately
    large effect relative to late intervention, because breaking the cycle
    early prevents amplification.
    Clinical implication: this suggests a critical window for sleep
    intervention in MCI/pre-dementia that is much earlier than current
    therapeutic targeting. Once the schema scaffold has destabilised
    (middle-stage), sleep restoration alone cannot rebuild it -- the slots
    that would be filled by recovered REM-analog function no longer exist.
  governance_2026_07_20: >
    PROPAGATION CONFIRMED (2026-07-20 governance walk). Three further V3-EXQ-673 runs
    (20260611T224744Z, 20260612T010234Z, 20260612T033246Z) were RECOVERED on 2026-07-20
    from the ree-cloud-3 / ree-cloud-2 untracked working trees during a git-wedge repair;
    they existed on no other machine and on no branch. They are runs 4-6 of the same
    forbidden same-EXQ silent-rerun batch that failure_autopsy_batch9_2026-06-12 flagged
    as a process_defect -- that autopsy recorded 3 duplicate manifests when there were in
    fact 6.
    Their evidence_direction was set by PROPAGATION of the existing confirmed batch9
    disposition (does_not_support -> non_contributory), exactly as it was propagated to
    the three committed siblings on 2026-06-14T16:07:16Z. Governance was asked by the
    manifests to confirm that propagation and DOES SO: it changes no claim status, adds no
    counter-evidence, and rests on a limb that is claim-level rather than run-level --
    MECH-171 is out_of_domain, because a clinical multi-year Alzheimer's vicious cycle is
    not instantiable in a 300-episode grid-world, so these runs are non-discriminative by
    domain.
    ONE LIMB DELIBERATELY DOES NOT PROPAGATE. The three committed siblings were ALSO
    arm-degenerate (ARM_A == ARM_B == ARM_C on every metric, late_pred_loss == 0.0). These
    three are NOT -- their arms genuinely differ -- so degeneracy_reason / non_degenerate
    are correctly left unset on them and only the out_of_domain limb carries over. The
    distinction is preserved rather than flattened because a future re-reading of this
    batch should not inherit a degeneracy finding that does not hold of these runs.

- id: MECH-172
  title: "Preservation of habit and procedural memory in early Alzheimer's disease, while episodic and contextual memory fail, is architecturally predicted by the separation of the model-free habit system (MECH-163 D1/D2, SNc) from the contextual attribution system (E3 hippocampal, requiring offline consolidation phases): the habit system does not require offline consolidation to function, so its relative preservation under early consolidation pipeline failure is not incidental but follows from the architectural separation."
  claim_type: derived_prediction
  subject: dementia.habit_system_sparing_architectural_prediction
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: out_of_domain
  governance_2026_06_14: >
    User-adjudicated claim_type reclassification mechanism_hypothesis ->
    derived_prediction (resolves the pending question flagged in the 06-10
    note). MECH-172 is a clinical-Alzheimer's prediction DERIVED from the
    habit/contextual-system architectural separation, testable only in a
    clinical cohort (habit/procedural sparing while episodic/contextual memory
    fails) -- not a REE-substrate mechanism. Matches sibling treatment
    (INV-048 / MECH-173 derived_prediction; MECH-171 reclassified same pass).
    epistemic_category stays out_of_domain. PROMOTES/DEMOTES NOTHING.
  what_would_answer: >
    A clinical Alzheimer's cohort showing (or failing to show) that procedural/
    habit memory is preserved relative to episodic/contextual memory in the
    early-to-middle stage, tracking consolidation-pipeline failure.
  governance_2026_06_10: >
    epistemic_category set out_of_domain (IGW experiment-surfacing suppression,
    not a promote/demote). The claim is empirical but its test domain is a clinical
    Alzheimer's patient cohort (habit/procedural sparing while episodic/contextual
    memory fails), not the REE substrate -- no substrate enrichment at any level
    helps, so EXP-0128 is not a valid /queue-experiment target. NOTE FOR USER:
    claim_type is currently mechanism_hypothesis but this reads as a clinical
    literature/research anchor (cf. sibling INV-048, reclassified to
    derived_prediction + prediction_domain: clinical_pharmacology 2026-04-17);
    claim_type LEFT UNCHANGED pending user decision. status unchanged (candidate).
  depends_on:
    - INV-046
    - MECH-168
    - MECH-163
    - SD-017
  notes: >
    Registered 2026-04-05. Clinical observation: patients with early to
    middle-stage Alzheimer's disease typically retain procedural and habit
    memory (riding a bicycle, playing a musical instrument, daily routines)
    long after episodic and contextual memory has substantially failed. This
    is conventionally attributed to anatomical sparing of the basal ganglia
    and striatum relative to hippocampal/entorhinal neurodegeneration.
    The attribution account provides a deeper computational explanation: the
    two systems have different consolidation requirements. The model-free
    habit system (MECH-163 D1/D2, analogous to SNc-striatal loops) learns
    via online reinforcement and does not require offline consolidation phases
    to maintain its representations -- the weights are updated during waking
    and do not depend on hippocampus-to-cortex schema installation.
    The contextual attribution system (E3 hippocampal, residue field) requires
    offline consolidation to maintain differentiated context representations.
    When the consolidation pipeline degrades, the habit system continues to
    function while the attribution system fails -- producing exactly the
    selective sparing observed clinically.
    This is a double dissociation that confirms the architectural separation:
    (1) attribution system fails while habit system is spared (early AD);
    (2) in pure habit system disorders (Huntington's, striatal lesions),
    procedural memory fails while episodic memory is relatively spared.
    The REE account predicts both dissociations from first principles.

# ---- Claims registered 2026-04-05: medication effects on sleep architecture as attribution pipeline disruption ----

- id: INV-048
  title: "Any pharmacological agent that disrupts sleep architecture produces equivalent attribution pipeline degradation to behavioral sleep deprivation, proportional to which phase(s) are disrupted and the duration of exposure -- the pipeline is mechanism-agnostic and responds only to phase fidelity."
  claim_type: derived_prediction
  prediction_domain: clinical_pharmacology
  subject: sleep.pharmacological_disruption_pipeline_equivalence
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_batch9_2026-06-12#V3-EXQ-670"
      as_of: 2026-06-12
      verdict: non_contributory/n/a (degenerate)
  depends_on:
    - INV-045
    - INV-046
    - MECH-120
    - MECH-121
    - MECH-123
  epistemic_category: substrate_conditional
  what_would_answer: |
    Precondition (non-vacuity gate -- required before any run is
    interpretable, per V3-EXQ-670's own diagnosed failure): the 4-arm
    design (ARM_A_NORMAL_SLEEP SWS+REM / ARM_B_REM_SUPPRESSED
    ~antidepressants / ARM_C_SWS_SUPPRESSED ~benzodiazepines /
    ARM_D_NO_SLEEP) is already confirmed faithful to INV-048
    (failure_autopsy_batch9_2026-06-12) -- only the readout was broken.
    V3-EXQ-670 FAILed degenerate because run_sleep_cycle runs the SWS +
    REM passes but never trains e3.harm_eval_head: harm_discrimination
    was byte-identical across all 4 arms for every seed, so the verdict
    could not respond to the sleep manipulation at all. A retest
    (V3-EXQ-670a, never run as of this digestion pass) only presses this
    claim if it first trains the harm-valuation pathway (the
    scaffold_train_harm_pathway / V3-EXQ-603k pattern) AND clears a
    non-vacuity gate: the 4 arms must produce genuinely DISTINCT
    harm_discrimination (and/or slot_differentiation) values before the
    verdict is read. Any run that reproduces arm-invariant
    harm_discrimination is degenerate/non_contributory again, exactly as
    V3-EXQ-670 was.

    Given a non-degenerate run: CONFIRMING if attribution-pipeline
    degradation (harm_discrimination and/or slot_differentiation loss
    relative to ARM_A_NORMAL_SLEEP) is proportional to WHICH PHASE(S) are
    disrupted and is cause-agnostic -- i.e. a pharmacological-suppression
    arm (ARM_B/REM or ARM_C/SWS) degrades by a comparable amount to a
    behavioral-equivalent same-phase, same-duration deprivation, so the
    pipeline tracks phase fidelity only. FALSIFYING if a
    pharmacological-suppression arm and a phase-and-duration-MATCHED
    behavioral-suppression arm produce a materially different
    degradation pattern -- meaning the pipeline discriminates on CAUSE of
    disruption, not just phase fidelity, contradicting the
    mechanism-agnostic claim.

    Epistemic caveat: even a clean, non-degenerate PASS inside REE
    supports INV-048 only CONDITIONAL on REE's sleep/consolidation model
    being a sufficiently faithful model of human sleep architecture (per
    this claim's 2026-04-17 reclassification from claim_type: invariant
    to derived_prediction, prediction_domain: clinical_pharmacology -- a
    domain-modelling assumption, not an architectural invariant). The
    REE-internal experimental result and the clinical/epidemiological
    evidence line cited in notes (benzodiazepine/anticholinergic-dementia
    associations -- Billioti de Gage et al. 2014 BMJ; Coupland et al.
    2019 JAMA Internal Medicine) are linked but NOT identical: the
    epidemiological association is human-population evidence and does
    not move this claim's REE exp_conf -- only a non-degenerate substrate
    run does.
  evidence_quality_note: >
    [2026-06-12 governance / confirmed failure_autopsy_batch9_2026-06-12 applied]:
    V3-EXQ-670 (pharmacological sleep-disruption equivalence, the first INV-048
    behavioural test) FAILed but is DEGENERATE / non_contributory, not a weakens.
    harm_discrimination was byte-identical across all 4 arms (normal / REM-suppressed
    ~antidepressants / SWS-suppressed ~benzodiazepines / no-sleep) because
    run_sleep_cycle runs the SWS + REM passes but never trains e3.harm_eval_head --
    the harm readout is seed-fixed and arm-invariant, so the verdict could not respond
    to the sleep manipulation. The 4-arm phase-fidelity-vs-cause-agnostic design is a
    faithful operationalisation of INV-048; only the readout is broken. manifest
    non_degenerate set false; carries no information on the derived prediction. Retest
    = V3-EXQ-670a: train the harm-valuation pathway in the sleep cycle (the
    scaffold_train_harm_pathway / 603k pattern) + add a non-vacuity gate (arms must
    produce distinct harm_discrimination before the verdict is read). No confidence
    change.
  notes: >
    Reclassified from claim_type: invariant in Session C batch 2026-04-17.
    Rationale: this is a clinical pharmacological prediction about human sleep
    disruption; it holds only conditional on REE correctly modelling human sleep
    and consolidation -- a domain-modelling assumption distinct from REE's
    architectural invariants (per Daniel's 2026-04-17 objection, audit registry
    INV-048).
    Registered 2026-04-05. The attribution pipeline (INV-045/INV-046) makes no
    mechanistic distinction between sleep deprivation caused by behavioral
    factors and sleep deprivation caused by pharmacological suppression of
    sleep phases. The relevant variable is phase fidelity -- whether each
    consolidation phase runs with adequate depth, duration, and sequence
    integrity -- not the cause of its disruption.
    This equivalence principle grounds all downstream pharmacological
    predictions (MECH-173-177): medication review becomes a pipeline audit.
    For each prescribed medication, the relevant question is: which pipeline
    phases does this agent suppress, by how much, over how long?
    The grounding follows directly from INV-045 (phase ordering is
    computationally necessary) and INV-046 (dementia is attribution pipeline
    failure). If the pipeline requires phase fidelity, and medications can
    disrupt phase fidelity, then medications can cause pipeline degradation
    via the same mechanism as behavioral sleep loss.
    Epidemiological support: benzodiazepine and anticholinergic use in
    older adults are associated with increased dementia incidence (Billioti
    de Gage et al. 2014 BMJ; Coupland et al. 2019 JAMA Internal Medicine),
    consistent with pharmacological pipeline disruption over years.
  pharmacological_predictions:
    - PHARM-007  # anticholinergic dual-pathway risk
    - PHARM-008  # mirabegron / non-anticholinergic substitution
    - PHARM-009  # BZDs and Z-drugs as architecture-disrupting (NEGATIVE)
    - PHARM-010  # DORA architecture-preserving alternative (POSITIVE)
    - PHARM-011  # low-dose trazodone as midline option (POSITIVE)
    - PHARM-012  # mirtazapine / sertraline / escitalopram (low REM-burden antidepressants)
    - PHARM-013  # ramelteon / melatonin-ER (Phase 0 circadian restoration)
    - PHARM-014  # pipeline-rebuild dissociation prediction (composite intervention test)

- id: MECH-173
  title: "Medications that suppress REM sleep (anticholinergics, MAOIs, most antidepressants at therapeutic doses, benzodiazepines) selectively impair MECH-123 precision recalibration -- the most downstream pipeline phase -- and thereby accelerate the earliest dementia prodrome: overconfident contextual attributions before overt memory loss."
  claim_type: mechanism_hypothesis
  subject: pharmacology.rem_suppression_phase4_disruption
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_ceiling
    as_of: 2026-07-20
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-774_2026-07-17
      as_of: 2026-07-17
      verdict: inconclusive/substrate_ceiling
  epistemic_category: substrate_ceiling
  pending_retest_after_substrate: true  # substrate_queue MECH-204 amend (Phase 7 / Option B accuracy-anchored broadcast recalibration + waking confidence-inflation source); see failure_autopsy_V3-EXQ-774_2026-07-17
  evidence_quality_note: >
    2026-07-18 governance (V3-EXQ-774, diagnostic, claim_ids=[MECH-173];
    failure_autopsy_V3-EXQ-774_2026-07-17, status confirmed): V3-EXQ-774 (diagnostic, excluded
    from scoring) adjudicated substrate_ceiling. The built MECH-204 F1 precision-recalibration
    produces MECH-173's DIRECTION (suppression removes epistemic humility) with the
    accuracy-dissociation and REM-selectivity intact, but cannot express ABSOLUTE
    overconfidence -- the F1 setpoint is a lagging low-pass of the agent's own
    running_variance with no waking confidence-inflation source to correct, and precision
    saturates before the recalibration lever gains headroom (effect present on 1/3 seeds only).
    NOT falsification; a discovered prerequisite. Fires the sleep_substrate_plan Phase 7 /
    Option B resume trigger (accuracy-anchored / broadcast recalibration; read
    _persistent_zero_point, lit choice (a), already adjudicated).
    evidence_direction inconclusive (PROMOTES / DEMOTES NOTHING); MECH-173 stays candidate.
    pending_retest_after_substrate: routed to /implement-substrate as an AMEND on MECH-204
    (see substrate_queue.json). Per the illusory-conflict-resolution rule this substrate-limited
    result is recorded, not dropped from the conflict ratio.
  depends_on:
    - INV-048
    - MECH-123
    - INV-047
    - MECH-168
  notes: >
    Registered 2026-04-05. REM sleep is the substrate of MECH-123 (precision
    recalibration): during REM, decision thresholds and prior confidence
    weights are reset, allowing the waking system to update its uncertainty
    estimates. Pharmacological REM suppression deprives this recalibration
    of its substrate.
    Because Phase 4 (precision recalibration) fails first in natural
    neurodegeneration (INV-047), pharmacological REM suppression selectively
    targets the most vulnerable phase, accelerating the earliest prodrome:
    the agent continues acting with high confidence while the accuracy of
    contextual attributions degrades -- the combination characteristic of
    early MCI (subjective normalcy, objective deficit on sensitive testing).
    Key REM-suppressing medications by mechanism:
    (1) Anticholinergics: ACh (from nucleus basalis of Meynert and brainstem
    PGO wave generators) is the primary neurochemical driver of REM; any
    muscarinic blockade suppresses REM directly.
    (2) MAOIs: profound REM suppression via noradrenergic/serotonergic
    excess; clinically, REM is almost abolished at therapeutic doses.
    (3) SSRIs/SNRIs: REM suppression via serotonergic excess acting on 5-HT2A
    REM-off neurons (paroxetine most REM-suppressive due to combined
    anticholinergic action; venlafaxine and duloxetine moderate).
    (4) TCAs: REM suppression via combined anticholinergic + serotonergic
    effects; amitriptyline and clomipramine most potent.
    (5) Benzodiazepines: reduce REM% via preferential Stage 2 enhancement
    at the expense of SWS and REM.
    Prediction: cumulative REM-suppressing medication years should predict
    MCI conversion, detectable as precision failure on laboratory tasks
    before clinical diagnosis. This is testable via retrospective
    pharmacoepidemiology with neuropsychological battery data.
  pharmacological_predictions:
    - PHARM-007  # anticholinergic dual-pathway risk (REM-suppression arm)
    - PHARM-012  # antidepressant class dissociation (low-REM-burden vs paroxetine/TCA/MAOI)
  digestion_note: >
    2026-08-06 (thought-digestion, session thoughts-intakes-4fd18d, parallel
    research agent): what_would_answer was DELIBERATELY NOT WRITTEN. This is a
    dual-shaped claim -- a clinical/pharmacoepidemiological prediction (see
    notes above) paired with a REE-substrate mechanism claim (MECH-123
    precision-recalibration impairment) -- and the substrate side has moved
    materially since this claim's own evidence_quality_note was last touched.
    V3-EXQ-774 (2026-07-17, diagnostic) found a substrate_ceiling: the built
    MECH-204 F1 precision-recalibration reproduces MECH-173's DIRECTION
    (accuracy-dissociation + REM-selectivity intact) but cannot express
    ABSOLUTE overconfidence, on only 1/3 seeds (convergence-headroom masking)
    -- correctly read as a discovered substrate PRECONDITION gap, not
    falsification. WORKSPACE_STATE.md (2026-07-20T19:40Z) shows that gap has
    since been BUILT AND LANDED -- MECH-204 Phase 7 / Option B (broadcast
    precision read-site) AND the paired SD-076 (waking confidence-inflation
    source, without which the MECH-173 overconfidence DV is tautological by
    construction) landed together in ree-v3 main 8ac193d7ed + REE_assembly
    master 062516d70e, contracts green (1990/0) -- but the same entry states
    explicitly "STILL OWED -- validation experiment NOT queued." Confirmed via
    grep: no MECH-173/774 entry in ree-v3/experiment_queue.json,
    pending_review.md silent on it, as of this digestion pass. The required
    retest is NOT a simple rerun of V3-EXQ-774 -- it must be a 2x2 ablating
    BOTH Phase 7 and SD-076 together (ablating Phase 7 alone would retest to
    a null indistinguishable from a Phase-7 refutation, because without
    SD-076 running_variance tracks true error by construction). Reference
    acceptance numbers already banked for this design: 774's ARM_FULL_SLEEP
    = -0.2097 (underconfident); a smoke test showed the index moving
    -0.164 -> +0.273 at asymmetry 0.6 once both levers are on. Separately
    unresolved: V3-EXQ-774 scored MECH-173 even though it wasn't in the
    Phase-7 resume trigger's named claim list -- treated as "illustrative of
    the class, not exhaustive" but never formally adjudicated.
    NEXT DIGESTION PASS: draft what_would_answer only after either (1) the
    2x2 retest lands and its outcome is folded into evidence_quality_note, or
    (2) explicitly incorporating this precondition/2x2-design/reference-
    numbers language as the non-degeneracy precondition itself. Keep the
    eventual REE-substrate falsifier and the clinical/pharmacoepidemiological
    falsifier as TWO separate signature blocks -- they have different
    non-degeneracy preconditions and should not be collapsed into one. Check
    whether the 2x2 retest has been queued since 2026-07-20 before drafting.
    Remove this note once done.

- id: MECH-174
  title: "GABAergic sleep aids (benzodiazepines and Z-drugs: zolpidem, zopiclone, zaleplon) suppress SWS depth and continuity at therapeutic doses, impairing MECH-120 synaptic homeostasis and MECH-121 schema replay, producing context representations that remain globally undifferentiated -- the same failure mode as SD-017 without any SWS-analog."
  claim_type: mechanism_hypothesis
  subject: pharmacology.sws_suppression_phase12_disruption
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  depends_on:
    - INV-048
    - MECH-120
    - MECH-121
    - SD-017
  what_would_answer: >
    NON-DEGENERACY PRECONDITION (shared with parent claim INV-048, applies to the
    REE-architecture leg): before any REE-substrate run is interpretable, e3.harm_eval_head
    must be trained (V3-EXQ-670's own diagnosed failure -- the harm-valuation pathway was
    byte-identical across all 4 arms because it was never trained). V3-EXQ-670a has never
    been run as of this digestion pass.

    LEG A -- REE-ARCHITECTURE (substrate_conditional; testable in principle, but the
    specific mechanism this claim asserts is not yet representable by the existing
    substrate knob). Current state: sws_enabled/sws_consolidation_steps/sws_schema_weight
    (ree_core/utils/config.py) provide only a BINARY on/off SWS toggle and a
    duration/weight lever. The one existing pharmacological-analog test (V3-EXQ-670,
    ARM_C_SWS_SUPPRESSED) sets sws_enabled=False -- full ablation, equivalent to complete
    SWS deprivation. This tests INV-048's coarse claim but CANNOT test this claim's
    specific two-pathway mechanism: (1) graded slow-oscillation DEPTH reduction (distinct
    from full ablation) and (2) SWS CONTINUITY fragmentation (distinct from duration
    reduction) -- nor the Z-drug/classical-benzo dissociation (partial spindle/MECH-122
    preservation under depth-only suppression vs full suppression under both). A GABAergic
    decay regulator does exist (SD-036, GABAergicDecayRegulator), but it perturbs
    waking-state cross-stream z_harm decay (the PHARM-004 catatonia mechanism), not the
    sleep-phase machinery -- not reusable for this claim.

    CONFIRMING (once the substrate gap above is closed): degradation in MECH-120
    homeostatic reset quality and MECH-121 schema-replay completeness that scales with
    depth reduction and fragmentation severity, is dissociable from full-ablation
    degradation, and (for the Z-drug arm) leaves MECH-122 spindle-coordination metrics
    relatively less impaired than the classical-benzodiazepine arm.

    FALSIFYING: graded depth/continuity perturbation producing no dose-response
    relationship to MECH-120/121 metrics; or no dissociation between the Z-drug and
    classical-benzodiazepine analogs on MECH-122 spindle metrics despite a depth/
    continuity difference.

    LEG B -- HUMAN-CLINICAL (out-of-domain to REE's own substrate; answerable now via
    existing polysomnography and neuropsychological literature, independent of REE
    substrate status): this claim's own prediction -- long-term BZD/Z-drug users show
    reduced SWS%, slower context-discrimination/new-schema learning, with relatively
    preserved remote episodic retrieval (the Phase 1/2-selective signature). CONFIRMING:
    PSG studies showing reduced SWS%/slow-oscillation power in chronic users, paired with
    neuropsychological batteries showing selective new-context-learning impairment.
    FALSIFYING: no SWS-architecture difference vs matched non-users, or a dissociation
    running the opposite direction.

    NOTE (evidence-quality caveat): this claim's existing literature support includes
    Billioti de Gage et al. 2014 (BZD-dementia association), whose causal-direction
    interpretation has been substantially challenged by subsequent better-confounded
    designs (e.g. Gray et al. 2016) addressing reverse causation/protopathic bias
    (prodromal dementia -> earlier insomnia/anxiety -> BZD prescription). Read this leg's
    literature confidence with that caveat rather than the Billioti de Gage point estimate.

    GOVERNANCE FLAG (2026-08-08, not resolved here): V3-EXQ-670a retest (INV-048's own
    blocker, gating Leg A) still not queued as of this digestion. No graded SWS-depth/
    continuity substrate knob exists -- an /implement-substrate gap, not chipped here.
  notes: >
    Registered 2026-04-05. GABA-A positive allosteric modulators (classical
    benzodiazepines: diazepam, lorazepam, temazepam, nitrazepam; and Z-drugs:
    zolpidem, zopiclone, zaleplon) bind to benzodiazepine-sensitive GABA-A
    receptor subunits and produce sleep via global CNS inhibition.
    This global inhibition suppresses SWS in two distinct ways:
    (1) Direct SWS depth suppression: slow oscillation amplitude is reduced
    because the hypnotic state is produced pharmacologically rather than via
    homeostatic pressure -- the homeostatic drive that normally generates
    deep SWS is partially bypassed. This impairs MECH-120 (SHY synaptic
    homeostasis): the normalisation of dominant attractors requires adequate
    slow-wave power to drive the homeostatic reset.
    (2) SWS continuity disruption: GABAergic hypnotics produce fragmented
    slow-wave episodes rather than the sustained SWS blocks required for
    MECH-121 (schema replay via SWR sequences). Schema consolidation depends
    on sustained hippocampal SWR sequences coordinated with cortical slow
    oscillations (Staresina et al. 2015, Diekelmann & Born 2010); fragmented
    SWS produces fragmented and incomplete replay.
    Note on Z-drug distinction: Z-drugs preferentially bind alpha-1 subunit
    GABA-A receptors and enhance Stage 2 spindles (which may support MECH-122
    spindle coordination for already-consolidated items). However, they still
    reduce SWS homeostatic depth because they produce sleep via direct
    pharmacological inhibition rather than via accumulated homeostatic pressure.
    The net SWS homeostasis impairment is therefore shared with classical
    benzodiazepines, even if spindle coordination is partially preserved.
    Prediction: long-term benzodiazepine/Z-drug users should show reduced SWS%
    on polysomnography and slower context-discrimination learning on tasks
    requiring schema formation, with episodic retrieval relatively more
    preserved than new context learning -- the signature of Phase 1/2 failure.
  pharmacological_predictions:
    - PHARM-009  # BZD/Z-drug architecture-disrupting (NEGATIVE for dementia risk; contrast PHARM-004 which is POSITIVE in catatonia subtype II -- same drug class, different phenotype)

- id: MECH-175
  title: "Anticholinergic medications confer dementia risk via two independent and potentially additive pathways: (1) REM suppression (ACh is required for REM generation via nucleus basalis and brainstem PGO generators -- muscarinic blockade directly suppresses REM, impairing MECH-123); and (2) cholinergic deficit mimicry (AD is characterised by basal forebrain cholinergic loss; anticholinergic burden creates a pharmacological analog of this deficit during waking hours, independently degrading attention and encoding)."
  claim_type: mechanism_hypothesis
  subject: pharmacology.anticholinergic_dual_pathway_dementia_risk
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-698_2026-06-21
      as_of: 2026-06-21
      verdict: non_contributory/out_of_domain
  what_would_answer: |
    out_of_domain: no REE substrate at any level settles this; V3-EXQ-698's
    grid-world dual-pathway proxy was confirmed not a fair test (manipulation
    checks PASS but outcome DVs at the noise floor). The decisive test is a
    prospective clinical-pharmacology cohort with mediation/structural-equation
    analysis. Design: enroll older adults free of dementia, score anticholinergic
    burden (ACB / ARS / German ACB) at each visit, obtain an annual
    polysomnography subset to quantify REM-suppression-years, follow to a
    dementia-conversion endpoint, and fit a parallel-mediator SEM with (1)
    cumulative REM-suppression-years (Pathway 1) and (2) cumulative diurnal
    anticholinergic dose (Pathway 2) as simultaneous mediators of the
    ACB->dementia path. CONFIRMS the dual independent additive account if both
    mediators carry significant, partially non-overlapping indirect effects whose
    sum approximates the total ACB->dementia effect, AND ACB outperforms total
    medication count and total sleep-medication count as a predictor. REFUTES if
    the ACB->dementia association is fully mediated by a single pathway (e.g.
    daytime dose only, with REM-suppression-years adding no independent indirect
    effect), or if a non-anticholinergic substitution that removes only the
    nocturnal limb (oxybutynin->mirabegron natural experiment) fails to reduce
    risk. Note the Grace-Vanstone-Horner 2014 caveat: REM-transition reliability /
    REM-theta integrity may be the true nocturnal mediator rather than total REM
    amount, so the PSG metric should include transition and spectral measures, not
    just REM%.
  epistemic_category: out_of_domain
  evidence_quality_note: >
    2026-06-21 (governance, failure_autopsy_V3-EXQ-698 confirmed): reclassified
    epistemic_category -> out_of_domain. MECH-175 is a clinical pharmacology /
    epidemiology claim whose decisive test is mediation analysis on an
    anticholinergic-burden cohort (ACB -> REM-suppression-years -> dementia
    conversion; per this claim's own notes), outside REE's substrate at any
    level. V3-EXQ-698 (the 4-arm grid-world dual-pathway proxy) is NOT a fair
    test: it applied its manipulations correctly (C5 manipulation checks PASS --
    REM rollouts 30->0, encoder magnitude 1.0->0.3) but the outcome DVs sit at
    the noise floor in the healthy ARM_A (harm_discrimination_mean 0.022 with
    cross-seed sign flips; slot_differentiation 0.056-0.87), so the predicted
    additive degradation (C1-C4) could not be detected regardless of the claim.
    The 698 self-stamped weakens was overturned to non_contributory on the
    manifest; out_of_domain suppresses promote/demote + narrow_open_question and
    routes the claim to research_anchor / clinical-cohort handling rather than
    further grid-world experiments.
  depends_on:
    - MECH-173
    - INV-048
  notes: >
    Registered 2026-04-05. The dual-pathway account distinguishes two
    independent mechanistic contributions of anticholinergic burden to
    dementia risk:
    Pathway 1 -- nocturnal REM suppression (pipeline disruption):
    ACh is the primary neurochemical driver of REM. Muscarinic M1/M2 receptors
    on PGO (ponto-geniculo-occipital) wave generating neurons and nucleus
    basalis of Meynert project to cortex and drive REM-state cortical
    desynchronisation. Anticholinergic blockade directly suppresses PGO
    waves, suppressing REM initiation and maintenance. This degrades
    MECH-123 precision recalibration nightly, compounding over years.
    Pathway 2 -- diurnal cholinergic deficit mimicry (encoding disruption):
    Alzheimer's disease is characterised by progressive loss of basal
    forebrain cholinergic neurons (nucleus basalis of Meynert). The cholinergic
    hypothesis of Alzheimer's (Bartus et al. 1982) established that this
    deficit is the primary driver of early attentional and encoding failure.
    Anticholinergic medications produce an iatrogenic cholinergic deficit
    during waking hours -- a pharmacological analog of the AD lesion itself.
    This independently impairs attention, encoding, and the cortical
    desynchronisation required for active learning.
    Additive prediction: anticholinergic burden scores (ACB scale,
    Anticholinergic Risk Scale, German Anticholinergic Burden score) should
    predict dementia conversion better than total medication count or total
    sleep medication count, because they capture both pathway contributions.
    The dose-response relationship should be approximately additive if the
    two pathways are mechanistically independent. This is testable via
    mediation analysis: if anticholinergic burden -> dementia risk is
    partially mediated via REM-suppression-years, the nocturnal pathway is
    confirmed as independent.
    High-burden medications by indication class:
    Bladder/urinary: oxybutynin (high), solifenacin (moderate),
    tolterodine (moderate), darifenacin (high).
    OTC antihistamines: diphenhydramine (high), chlorphenamine (moderate).
    TCAs: amitriptyline (high), clomipramine (high), doxepin (high),
    imipramine (high).
    Antipsychotics: chlorpromazine (high), olanzapine (moderate),
    clozapine (high), quetiapine (low-moderate).
    SSRIs: paroxetine (moderate -- outlier in SSRI class due to
    potent muscarinic blockade; other SSRIs are low burden).
    Epidemiological support: Coupland et al. 2019 (JAMA Internal Medicine),
    Gray et al. 2015 (JAMA Internal Medicine), Khachaturian et al. 2006
    (Archives of Neurology) -- consistent dose-response anticholinergic
    burden-dementia association replicated across multiple cohorts and
    healthcare systems.
  pharmacological_predictions:
    - PHARM-007  # anticholinergic dual-pathway dementia risk (REM-suppression + diurnal cholinergic deficit mimicry)
    - PHARM-008  # mirabegron substitution for anticholinergic urinary indication (POSITIVE; non-CNS antimuscarinic alternative)

- id: MECH-176
  title: "Orexin receptor antagonists (suvorexant, lemborexant) are predicted to be architecture-preserving sleep aids: they suppress wake-promoting orexin drive without directly compressing sleep phase machinery, allowing the brain's own homeostatic sleep architecture to unfold, and should therefore preserve or improve SWS/REM ratio relative to GABAergic alternatives at equi-efficacious doses."
  claim_type: mechanism_hypothesis
  subject: pharmacology.orexin_antagonist_architecture_preserving
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-174
    - INV-048
  notes: >
    Registered 2026-04-05. GABAergic sleep aids produce sleep by actively
    suppressing the nervous system globally. This bypasses homeostatic
    pressure, reducing SWS depth and compressing REM by competing with the
    endogenous machinery (MECH-174). Orexin receptor antagonists (DORAs:
    dual orexin receptor antagonists -- suvorexant, lemborexant) work by a
    different mechanism: they block the wake-promoting orexin/hypocretin
    signal from lateral hypothalamus, permissively allowing the homeostatic
    sleep pressure to express itself without pharmacological interference
    with the sleep machinery.
    The key distinction: GABAergic agents add an active suppressive signal;
    orexin antagonists remove an active wake-promoting signal. The result
    is that homeostatic SWS generation is not competed with, and REM cycles
    emerge from endogenous REM-permitting neurochemistry (reduced monoamines,
    cholinergic rebound) rather than from pharmacological REM-off suppression.
    PSG evidence: multiple head-to-head and placebo comparisons show orexin
    antagonists maintain or slightly increase REM% and do not suppress SWS%
    below placebo levels (Herring et al. 2016, SLEEP; Kishi et al. 2020
    meta-analysis). This contrasts with zolpidem (SWS suppression) and
    temazepam (REM + SWS compression).
    Testable prediction: in older adults at risk for MCI, orexin antagonist
    users should show (1) preserved SWS% and REM% on polysomnography, (2)
    slower episodic memory decline compared to benzodiazepine/Z-drug users
    at matched total sleep time, and (3) better new context discrimination
    performance (SD-017 analog in humans: ability to distinguish similar but
    distinct contexts in list learning tasks).
    Clinical implication: when a sleep aid is indicated in an older adult
    at MCI risk, orexin antagonists should be preferred over GABAergic
    alternatives specifically on dementia risk grounds, independent of
    other safety considerations (falls, dependence, tolerance).
  pharmacological_predictions:
    - PHARM-010  # DORA for MCI/dementia (architecture-preserving; sibling of PHARM-001)
    - PHARM-011  # low-dose trazodone 25-100mg (5-HT2A antagonism, SWS-enhancing, non-GABAergic non-anticholinergic)
    - PHARM-013  # ramelteon / melatonin-ER (Phase 0 circadian restoration)

- id: MECH-177
  title: "Any pharmacological intervention that durably restores the NREM-to-REM sequence and preserves phase durations in MCI or early Alzheimer's disease is predicted to be disease-modifying via pipeline protection -- upstream of irreversible neurodegeneration, breaking the MECH-171 vicious cycle before schema scaffold collapse, with predicted effect size larger in early MCI than established AD."
  claim_type: mechanism_hypothesis
  subject: pharmacology.sleep_architecture_restoration_disease_modifying
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-176
    - MECH-171
    - INV-048
    - MECH-170
  notes: >
    Registered 2026-04-05. Pipeline protection mechanism: the MECH-171
    vicious cycle (sleep disruption -> consolidation failure -> attribution
    failure -> increased arousal -> further sleep disruption) is the
    amplification mechanism for dementia progression. Breaking the cycle
    pharmacologically -- by restoring phase-adequate sleep -- interrupts
    amplification before the schema scaffold collapses. This is a
    disease-modifying effect, not symptomatic treatment, because it
    preserves the infrastructure that contextual attribution depends on.
    Critical window: MECH-171 predicts that early intervention (MCI stage,
    before schema scaffold destabilises) produces disproportionately large
    effects relative to late intervention (established AD, after context
    slots no longer exist). This is consistent with the general principle
    that disease-modifying interventions in AD are stage-dependent.
    Candidates by predicted mechanism and evidence tier:
    (1) Suvorexant / lemborexant (MECH-176): architecture-preserving
    via orexin antagonism; preserves SWS + REM. Active clinical
    investigation (Herring et al. 2020 Alzheimer's Research and Therapy).
    (2) Ramelteon / melatonin-extended-release: circadian restoration
    (Phase 0 in INV-045 terminology) improves architecture downstream.
    Melatonin-ER licensed in EU specifically for over-65 on architecture
    preservation grounds. Ramelteon: no direct REM/SWS compression.
    (3) Trazodone (low dose, 25-100mg): 5-HT2A antagonism increases SWS
    depth (Phase 1/2 enhancement); used in AD sleep disruption trials.
    Active RCT (McCleery & Sharpley 2020 Cochrane review). SWS-enhancing
    mechanism is non-GABAergic and non-anticholinergic -- neutral on
    REM. Provides Phase 1/2 benefit with minimal Phase 4 penalty.
    (4) Gabapentin / pregabalin: increase SWS depth via voltage-gated
    calcium channel modulation. Phase 1/2 enhancement. Caveat: tolerance
    develops for SWS effect; fall risk and cognitive sedation in older
    adults warrant caution. Not a first-line DPM candidate.
    (5) Low-dose doxepin (Silenor, 3-6mg): histamine H1 antagonism only
    at this dose; minimal muscarinic activity below 10mg. Enhances sleep
    continuity with spindle preservation (MECH-122 compatible). Not
    REM-suppressive at low dose.
    Contrast with amyloid-targeting drugs (lecanemab, donanemab): these
    target substrate damage accumulation (glymphatic pathway, MECH-169)
    rather than the symptom-progression pipeline. MECH-177 and MECH-169
    predict additive benefit from combined pipeline protection + amyloid
    clearance, because they operate on mechanistically independent targets.
    A phase II trial comparing orexin antagonist + sleep architecture
    monitoring vs standard care in MCI would directly test MECH-177.
    Primary endpoint: rate of episodic memory decline (MECH-170 prediction:
    episodic decline slows before semantic/contextual effects are detectable).
  pharmacological_predictions:
    - PHARM-010  # DORA for MCI/dementia
    - PHARM-011  # low-dose trazodone
    - PHARM-012  # antidepressant class dissociation (low-REM-burden vs paroxetine/TCA/MAOI)
    - PHARM-013  # ramelteon / melatonin-ER (Phase 0)
    - PHARM-014  # composite pipeline-rebuild dissociation test (cross-class predictor)

- id: Q-031
  title: "Does the anticholinergic burden-dementia risk relationship track cumulative REM suppression duration (total ACh-blocked sleep years), total anticholinergic dose as a proxy for diurnal cholinergic deficit, or both as independent additive predictors -- and can mediation analysis in prospective cohorts distinguish the nocturnal (MECH-173) from the diurnal (MECH-175 pathway 2) contribution?"
  claim_type: open_question
  subject: pharmacology.anticholinergic_risk_rem_vs_dose_independent_predictors
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    out_of_domain: the test domain is human pharmacological cohorts with
    longitudinal PSG over years; no REE substrate, current or planned, settles
    this -- it is not a wait-for-V4 item. External study design: a prospective
    cohort with (1) annual PSG subset quantifying REM-suppression-years, (2)
    anticholinergic burden scoring at each visit, (3) a dementia-conversion
    endpoint, analysed with a structural equation model placing cumulative
    REM-suppression (nocturnal / MECH-173 limb) and cumulative anticholinergic
    dose (diurnal / MECH-175 Pathway 2 limb) as PARALLEL mediators of the
    ACB->dementia path. CONFIRMS 'both independent additive predictors' if each
    mediator carries a significant indirect effect that survives adjustment for
    the other and the two indirect effects are roughly additive; CONFIRMS a single
    dominant pathway if only one mediator's indirect effect is significant once
    both are in the model. COLLAPSES the question (the third arm) if the two
    mediators are statistically collinear in available cohorts so mediation cannot
    separate them -- which is the current state, since Coupland 2019 and Gray 2015
    lack PSG data. Per Grace 2014 the nocturnal mediator should be operationalised
    as REM-transition reliability / REM-theta integrity, not total REM amount,
    since pontine SubC scopolamine modulates transitions without abolishing REM.
    The mediation sub-question has no published direct test; the right next step
    is the cohort methodology above, NOT narrowing a REE claim.
  epistemic_category: out_of_domain
  depends_on:
    - MECH-175
    - MECH-173
  evidence_quality_note: |
    2026-04-28T22Z governance: narrow_open_question note. Lit-pull 2026-04-28T11Z added
    5 entries to new dir targeted_review_q_031/. Cumulative-dose limb (b): Gray 2015
    ACT cohort (PMID 25621434, supports/0.78), Coupland 2019 QResearch (PMID 31233095,
    supports/0.82, bladder antimuscarinic AOR 1.65). REM-mediator limb (a): Pase 2017
    Framingham (PMID 28835407, supports/0.78), Kim & Jeong 1999 (PMID 10389227,
    supports/0.62). Mechanistic constraint: Grace-Vanstone-Horner 2014 (PMID 25339734,
    mixed/0.70 -- pontine SubC scopolamine does NOT abolish REM; modulates transitions
    + REM-theta but is not the generator -- weakens strong pathway-1 reading). Synthesis
    verdict: limb (b) cumulative-dose --> dementia well-supported (Gray + Coupland); the
    bladder-antimuscarinic-to-mirabegron clinical hypothesis has its empirical foundation
    but no natural-experiment test yet. Limb (a) supported via REM% --> dementia + ACh
    --> REM%, but Grace 2014 suggests transition reliability + REM-theta integrity may
    be the right mediator rather than total REM amount. Mediation-analysis sub-question
    has no published direct test -- flagged. Lit_conf 0.87 with aggregator-floor caveat
    (per-paper conf 0.62-0.82 vs claim-level 0.87). Aggregator-floor flag raised in 5
    consecutive cycles.

    EPISTEMIC-CATEGORY FLAG (2026-05-02, Phase 3 Q-walk): out-of-domain. The question
    is empirical but the test domain is human pharmacological cohorts with PSG data
    over years -- structurally outside any REE substrate, current or planned. No
    substrate enrichment will help; this is not "wait for V4" or "wait for substrate
    landing." It IS a legitimate clinical-research question that the lit work informs,
    but it does not belong in the same governance queue as substrate-testable claims.
    The narrow_open_question recommendation is misleading: the right "next step" is
    not narrowing the REE claim, it's the prospective-cohort methodology described
    in the notes block below. Treatment options: (a) keep as open_question with
    out_of_domain annotation (current); (b) re-classify as a research_anchor or
    literature_synthesis claim_type so it lives in a different governance bucket;
    (c) close as resolved-by-synthesis with the conclusion that REE-architecture
    cannot decide between the two pathways without external clinical data. Recommend
    (a) for now (this Q-walk is annotate-only); revisit if/when the
    research_anchor / out_of_domain category is formalised.
  notes: >
    Registered 2026-04-05. Distinguishing the two anticholinergic pathways
    has direct clinical implications: if the nocturnal REM suppression
    mechanism (Pathway 1) dominates, then switching to non-anticholinergic
    alternatives with the same indication (e.g., bladder: oxybutynin ->
    mirabegron, a selective beta-3 adrenergic agonist with no CNS
    anticholinergic burden) should substantially reduce dementia risk even
    without improving daytime cognition. If the diurnal cholinergic deficit
    mechanism (Pathway 2) dominates, mirabegron switch reduces risk via the
    daytime pathway, and sleep architecture monitoring becomes the key
    biomarker. If both are additive, both interventions are required.
    Methodology to test: prospective cohort with (1) annual PSG subset to
    quantify REM-suppression-years, (2) anticholinergic burden scoring
    at each visit, (3) dementia conversion endpoint. Structural equation
    model with REM-suppression-years and cumulative anticholinergic dose
    as parallel mediators of the anticholinergic burden -> dementia path.
    Current evidence is insufficient to distinguish the pathways because
    existing anticholinergic-dementia studies (Coupland 2019, Gray 2015)
    do not include PSG data, making it impossible to separate nocturnal
    from diurnal mechanisms.

- id: Q-032
  title: "Among sleep-promoting medications prescribed in older adults, does relative SWS/REM ratio preservation (measured by polysomnography) predict differential dementia outcomes independent of total sleep time -- and if so, does this validate phase-specific pipeline damage as the causal intermediate and provide a pharmacodynamic biomarker for disease-modifying trials?"
  claim_type: open_question
  subject: pharmacology.sws_rem_ratio_dementia_outcome_predictor
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    out_of_domain: requires PSG-stratified pharmacoepidemiology over years; no REE
    experiment can test it. External study design: in an EHR cohort with linked
    PSG records (e.g. SHHS or MrOS Sleep Study), or a prospective sleep-medication
    cohort, use PSG-derived SWS/REM ratio (and SWS%, REM%, spindle density) as the
    primary predictor of dementia conversion, with total sleep time as a
    covariate, and contrast agents that preserve versus degrade the SWS/REM
    profile at matched total sleep time. CONFIRMS the
    phase-specific-pipeline-damage account (and validates SWS%/REM%/spindle
    density as pharmacodynamic biomarkers per INV-048 / MECH-173 / MECH-174) if
    two agents producing EQUAL total sleep time but different SWS/REM profiles
    produce DIFFERENT dementia outcomes -- i.e. the architectural ratio predicts
    conversion after total sleep time is partialled out (the Himali 2023
    within-person SWS-decline design is the strongest available template, HR 1.27
    per SWS-loss SD). REFUTES if dementia outcome tracks total sleep time alone
    and the SWS/REM ratio adds no incremental predictive value once total sleep
    time is controlled, leaving total sleep time a sufficient surrogate and
    phase-fidelity unsupported as the causal intermediate. No prior trial has used
    PSG-derived phase metrics as primary endpoints (COSMOS-MIND and FINGER-type
    trials include sleep only as a non-PSG secondary domain); the canonical
    pending interventional test is the REST trazodone trial. Out_of_domain -- a
    research_anchor / clinical-cohort question, not a REE substrate experiment.
  epistemic_category: out_of_domain
  depends_on:
    - INV-048
    - MECH-173
    - MECH-174
  evidence_quality_note: |
    Lit-pull 2026-04-29 (ree-lit-pull-pm-q032 session) added 5 entries: Himali 2023 JAMA
    Neurol Framingham SWS-loss x 17y dementia HR 1.27 (supports/0.82, the strongest
    individual-prediction evidence on the table); Winer/Mander/Walker 2020 Curr Biol
    NREM SWA <1Hz forecasts AB accumulation (supports/0.78); Suh 2019 J Alzheimers Dis
    KLOSCAD short REM-per-cycle predicts MCI (supports/0.62, exploratory n=14);
    D'Rozario 2020 Sleep Med Rev meta-analysis MCI architectural fingerprint
    (supports/0.72); Eyob 2024 J Alzheimers Dis REST trazodone trial protocol
    (supports/0.45, protocol only). lit_conf 0.839; quadrant=plausible_unproven, exp_conf=0.

    EPISTEMIC-CATEGORY FLAG (2026-05-02, Phase 3 Q-walk): out-of-domain (same shape as
    Q-031). The question is empirical but requires PSG-stratified pharmacoepidemiology
    over years -- structurally outside any REE substrate. The synthesis verdict from
    the lit work above is informative (Himali within-person SWS-decline design is the
    strongest available evidence; D'Rozario meta confirms the architectural signature;
    REST is the canonical pending trial), but no REE experiment can test it. Same
    treatment recommendation as Q-031: keep as open_question with out_of_domain
    annotation; revisit if/when research_anchor / out_of_domain category is formalised.
    These two pharmacology Q-claims (Q-031 + Q-032) and similar future claims may
    deserve their own claim_type (e.g., research_anchor) so they live in a different
    governance bucket and don't get fed through gates that don't apply.
  notes: >
    Registered 2026-04-05. Total sleep time is the conventional endpoint
    for sleep intervention trials. If pipeline phase fidelity is the
    causal intermediate (INV-048), then total sleep time is an insufficient
    surrogate -- two agents producing equal total sleep time with different
    SWS/REM profiles should produce different dementia outcomes.
    If confirmed, SWS% and REM% on PSG would become phase-specific
    pharmacodynamic biomarkers for disease-modifying trials, enabling
    mechanism-targeted trial design. Specifically:
    (1) SWS% as surrogate for Phases 1/2 fidelity (MECH-120/121)
    (2) REM% as surrogate for Phase 4 fidelity (MECH-123)
    (3) Spindle density as surrogate for Phase 3 fidelity (MECH-122)
    Existing data: no direct comparison has used PSG-derived phase metrics
    as primary endpoints in a dementia conversion trial. The COSMOS-MIND
    trial (Mewton et al. 2023) and FINGER-type multimodal trials include
    sleep as a secondary domain but without PSG. A PSG-stratified
    pharmacoepidemiology study in existing EHR cohorts with linked PSG
    records (e.g., SHHS, MrOS Sleep Study) could test this retrospectively.

- id: IMPL-026
  title: "Medication classification by predicted attribution pipeline effect: a reference table mapping common sleep-affecting medication classes to their predicted phase-specific pipeline disruption, net dementia risk direction, and disease-modifying potential."
  claim_type: implementation_note
  subject: pharmacology.sleep_dementia_risk_classification
  polarity: records
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-173
    - MECH-174
    - MECH-175
    - MECH-176
    - MECH-177
    - INV-048
  notes: >
    Registered 2026-04-05. Reference classification for clinical use and
    trial design. Pipeline phase notation follows INV-045:
    Phase 0 = sensory gating (pre-sleep); Phase 1 = SHY homeostasis (MECH-120);
    Phase 2 = schema replay (MECH-121); Phase 3 = spindle coordination (MECH-122);
    Phase 4 = REM precision recalibration (MECH-123).
    Risk direction: WORSENS = net dementia risk increase; NEUTRAL = no
    significant pipeline disruption at therapeutic doses; IMPROVES = net
    pipeline protection or enhancement; CONDITIONAL = depends on dose,
    duration, or patient subtype.
    DPM = disease-progression-modifying potential.

    HIGH-RISK MEDICATIONS (significant pipeline disruption):
    Anticholinergics (all classes):
      Pipeline phases disrupted: Phase 4 (REM suppression) + diurnal
      cholinergic deficit (MECH-175).
      Risk: WORSENS. DPM potential: no (harmful). Priority: switch to
      non-anticholinergic alternatives wherever possible (mirabegron for
      bladder; SSRI/loratadine for antihistamine; other antidepressants
      for amitriptyline).
    Benzodiazepines:
      Pipeline phases disrupted: Phases 1/2 (SWS suppression) + Phase 4
      (REM suppression) (MECH-173 + MECH-174).
      Risk: WORSENS. DPM potential: no. Priority: deprescribe; taper
      and replace with non-GABAergic alternatives.
    Z-drugs (zolpidem, zopiclone, zaleplon):
      Pipeline phases disrupted: Phase 1 (SWS homeostatic depth reduced)
      + Phase 4 (REM mildly reduced). Spindle enhancement may partially
      preserve Phase 3.
      Risk: WORSENS (less severe than classical BZDs but still harmful
      over years). DPM potential: no. Priority: replace with
      architecture-preserving alternatives.
    MAOIs:
      Pipeline phases disrupted: Phase 4 profound (nearly complete REM
      abolition at therapeutic doses).
      Risk: WORSENS. DPM potential: no. Use only when no alternatives
      exist; monitor with PSG if chronic use.
    SSRIs/SNRIs (most):
      Pipeline phases disrupted: Phase 4 moderate (REM suppression,
      especially paroxetine > venlafaxine > escitalopram/sertraline).
      Risk: WORSENS (moderate, chronic use). Exception: if treating
      depression/anxiety that is itself causing sleep disruption,
      net effect may be neutral or protective short-term.
      DPM potential: conditional (treating comorbid depression in MCI
      may have indirect benefit via MECH-171 cycle breaking).
      Prefer low-REM-burden SSRIs (escitalopram, sertraline) over
      paroxetine in older adults at MCI risk.
    TCAs at therapeutic antidepressant doses:
      Pipeline phases disrupted: Phase 4 (anticholinergic REM suppression)
      + Phase 1/2 (sedation quality varies by agent).
      Risk: WORSENS. DPM potential: no (trazodone is the exception --
      see below).

    NEUTRAL TO CONDITIONAL MEDICATIONS:
    Quetiapine (low dose, 12.5-50mg off-label):
      Pipeline effects: H1 antagonism increases sleepiness and may
      improve sleep continuity. At these doses, anticholinergic burden
      is low. Some SWS enhancement possible.
      Risk: NEUTRAL to mildly WORSENS at higher doses.
      DPM potential: insufficient evidence; monitor Phase 4 if doses
      exceed 50mg.
    Mirtazapine:
      Pipeline effects: H1 + 5-HT2A antagonism increases SWS depth
      and sleep continuity. REM effects are complex -- early REM
      suppression with rebound later in night; net REM% may be
      preserved or mildly increased.
      Risk: NEUTRAL to possibly IMPROVES (SWS enhancement). DPM
      potential: conditional; better evidence needed for Phase 4
      specifically.
    Gabapentin/pregabalin:
      Pipeline effects: voltage-gated calcium channel alpha-2-delta
      subunit binding increases SWS depth (Phase 1/2 enhancement).
      Risk: IMPROVES Phase 1/2 pipeline fidelity. Caveats: tolerance
      to SWS effect develops; fall risk and respiratory suppression
      in older adults. DPM potential: conditional.

    ARCHITECTURE-PRESERVING / BENEFICIAL MEDICATIONS:
    Orexin antagonists (suvorexant, lemborexant):
      Pipeline effects: removes wake-promoting orexin drive without
      compressing sleep phase machinery (MECH-176). Preserves or
      improves SWS% and REM% vs placebo; superior to zolpidem on
      phase preservation in head-to-head PSG studies.
      Risk: IMPROVES (net pipeline protection). DPM potential: YES
      (first-line candidate for MECH-177 trials in MCI).
    Ramelteon / melatonin-extended-release:
      Pipeline effects: circadian restoration improves Phase 0
      (sensory gating timing) and downstream architecture. No
      direct Phase 1-4 compression.
      Risk: IMPROVES (circadian pathway). DPM potential: YES
      (particularly for patients with circadian-mediated sleep
      fragmentation, common in early AD).
    Trazodone (25-100mg):
      Pipeline effects: 5-HT2A antagonism increases SWS depth
      (Phase 1/2 enhancement). Non-GABAergic, non-anticholinergic.
      REM-neutral or mildly permissive at low doses.
      Risk: IMPROVES Phase 1/2 fidelity. DPM potential: YES
      (active AD sleep trials; preferred for AD-associated
      insomnia over GABAergic alternatives).
    Low-dose doxepin (Silenor, 3-6mg):
      Pipeline effects: selective H1 antagonism at this dose;
      minimal muscarinic activity below 10mg. Enhances sleep
      continuity and spindle density (Phase 3 compatible).
      Risk: NEUTRAL to mildly IMPROVES Phase 2/3. DPM potential:
      CONDITIONAL (architecture-neutral; appropriate where other
      options are contraindicated).

- id: IMPL-027
  title: "Technical comparison: REE versus Meta AI/KAUST Neural Computers roadmap."
  claim_type: implementation_note
  subject: positioning.ree_vs_meta_kaust_neural_computers
  polarity: records
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-025
    - INV-026
    - INV-027
    - INV-028
    - INV-029
    - MECH-069
    - ARC-021
    - INV-049
    - IMPL-024
  location: docs/architecture/comparisons/meta_kaust_neural_computers.md#impl-027
  source:
    - docs/architecture/comparisons/meta_kaust_neural_computers.md
  notes: >
    Registered 2026-04-12. Comparison document positioning REE against the
    Meta AI / KAUST "Completely Neural Computer" (CNC) roadmap (Schmidhuber
    et al., 2025). Maps the four CNC requirements (Turing completeness,
    universal programmability, behavior consistency, machine-native semantics)
    onto REE's derived architectural structures. REE addresses all four from
    first principles; the comparison makes this explicit for technical readers
    arriving from the NC literature. Also identifies what NC covers that REE
    does not (I/O interface layer, hardware roadmap, NL programmability).

- id: INV-049
  title: "Any agent that builds a model of the world, acts in that world, and learns from experience cannot safely update its world model while using it to navigate the world; it therefore requires periodic offline phases in which action is suspended and the model is reorganised via replay and simulation -- this is not a biological contingency but a general computational necessity for model-building agents."
  claim_type: invariant
  invariant_type: universal
  epistemic_category: substrate_conditional
  subject: architecture.offline_update_necessity_general_principle
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-044
    - INV-045
    - SD-017
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: infant_substrate_expansion.md's graduated harm zones,
    microhabitat heterogeneity, and non-accidental benefit encounters (INF-ENV-001/002/003)
    are landed in ree_core (causal_grid_world.py: harm_gradient_enabled, microhabitat_enabled,
    transient_benefit_enabled) and substrate-confirmed (V3-EXQ-576, V3-EXQ-577a, V3-EXQ-589
    PASS) -- but all three default OFF, so any retest driver must explicitly enable them, else
    per EXQ-150's own finding no offline operation can produce context-specific representations
    regardless of validity, and any result self-routes substrate_not_ready (exactly what
    happened to EXQ-385/385a, which are correctly excluded from confidence scoring already).
    SD-017 slot-forming infrastructure must ALSO be active and functioning as predicted -- and
    is currently NOT confirmed: V3-EXQ-436b/436c/436d (2026-08-02 to 2026-08-04, most recent
    retest FAIL) found SWS_THEN_REM sleep collapses slot_cosine_sim toward ~1.0 (homogenization)
    rather than differentiating contexts, the opposite of SD-017's prediction, with a suspected
    untested ContextMemory.write_gate bias-collapse root cause (same failure class as the SD-016
    read-path bug fixed in EXQ-477) routed to /implement-substrate and unresolved. Until that is
    fixed and re-confirmed, EXQ-IDEV-006 in particular is at live risk of reproducing the same
    undifferentiated-slot failure for substrate reasons unrelated to INV-049's own claim.

    CONFIRMING signature: with infant_substrate_expansion.md's env features explicitly enabled
    AND the SD-017 write-gate issue fixed and re-confirmed (slot_cosine_sim differentiates
    post-sleep, not just write-counters clearing their floor), EXQ-IDEV-001 (longitudinal
    replay quality) and/or EXQ-IDEV-006 (SD-017 functional context discrimination:
    WITH_OFFLINE context_cosine_sim < 0.95 by cycle 3, WITHOUT_OFFLINE stays > 0.95) show
    WITH_OFFLINE outperforming WITHOUT_OFFLINE on slot_diversity/pred_loss/harm criteria.

    FALSIFYING signature: with the substrate gap genuinely closed (infant substrate features
    enabled and substrate-confirmed, SD-017's write-gate mechanism fixed and its own P0
    readiness gates clearing), offline consolidation STILL shows no advantage over online-only
    learning on EXQ-IDEV-001/006's own metrics -- this would be a real falsification of INV-049,
    unlike EXQ-385/385a (substrate-confounded) or EXQ-436b/c/d (SD-017-mechanism-confounded,
    not yet an INV-049 test at all).
  notes: >
    Registered 2026-04-06. This is the general law of which INV-044 (Bayesian
    staging: prior construction must precede posterior inference) and INV-045
    (phase ordering: SWS-analog before REM-analog) are specific corollaries.
    INV-049 makes the broader claim: offline phases are not a biological hack
    but a computational necessity for any sufficiently complex model-building agent.
    The necessity argument: updating a world model requires temporarily
    destabilising the model's weight structure (replay, recalibration, schema
    integration). A system that must simultaneously use its world model to
    navigate the environment and update it cannot do both safely -- each model
    update during online navigation destabilises the very predictions the agent
    depends on for survival. Offline suspension of action removes this
    constraint, allowing the model to be reorganised without consequence.
    This is consistent with: (1) experimental observation that removing sleep
    from the REE harness produces behavioural rigidity (monostrategy
    convergence, inability to update priors, cosine_sim -> 1.0 in EXQ-150
    zero-contrast finding); (2) the SHY principle (Tononi & Cirelli 2014) that
    synaptic renormalisation cannot occur during active wake without disrupting
    ongoing activity; (3) catastrophic interference in ML: neural networks
    trained continuously on new data without replay degrade on old data --
    replay (offline update) prevents this; (4) the general principle in
    reinforcement learning that experience replay and offline training improve
    generalisation relative to pure online learning.
    The formulation has two important implications:
    (1) Sleep is not peculiar to biology -- any artificial agent that learns a
    world model from continuous experience will benefit from sleep-like offline
    phases, and may require them for long-term stable behaviour.
    (2) Dementia can be understood as progressive failure of offline model
    updating (INV-046), not merely as neurodegeneration per se -- the
    neurodegeneration becomes cognitively manifest when it disrupts the offline
    maintenance cycle enough that the world model can no longer be updated.
    EXQ-150 supporting note: all three conditions (NO_ANNEAL, ANNEAL, RESET)
    showed final_contrast = 0.0 across all seeds -- zero context discrimination
    regardless of what the offline phase did to the residue field weights. This
    is consistent with INV-049: the experiment demonstrated that without the
    proper slot-forming infrastructure (SD-017), no offline operation -- however
    well-designed -- can produce context-specific representations.
  evidence_quality_note: >
    EXQ-385 (2026-04-15, non_contributory): Slot diversity reversed (WITH_OFFLINE
    slot_diversity~0.12 vs WITHOUT~1.0) -- result artifact from identical per-seed
    data shared with EXQ-418 (SD-017 context-conditioned action experiment). The
    offline/online dissociation cannot be tested when both conditions use the same
    per-seed slot data. Flagged for /diagnose-errors. Not evidence against INV-049;
    failure is a scripting/data isolation issue, not a claim validity issue.
    EXQ-385a x3 (2026-04-18, non_contributory -- governance 2026-04-19): Three independent runs with identical seeds [42,49,56] and SHY-decay=0.98 fix. All criteria fail 0/3 seeds (C1 slot_diversity, C2 pred_loss, C3 harm); WITHOUT_OFFLINE beats WITH_OFFLINE on diversity in every seed. Reclassified as diagnostic: the SHY mechanism fix was insufficient to recover differential offline-consolidation signal, suggesting architectural substrate gap rather than hypothesis falsification. Excluded from INV-049 confidence scoring pending substrate investigation.
    Substrate failure diagnosis (2026-05-16, replay_development_analysis.md §9.3): Both FAILs are most likely infant substrate compression failures (content poverty), not INV-049 theory failures. Three convergent root causes: (1) infant substrate produces homogeneous replay buffer content -- no graduated harm zones, microhabitat heterogeneity, or non-accidental benefit encounters, so sleep has little useful content to consolidate; (2) SWR sequence fidelity is immature at cold-start (Voros 2023 immature spatial navigation + sparse landmark environment); (3) ARC-038 waking consolidation FAILs (EXQ-191, EXQ-240, EXQ-240a, EXQ-267) mean the SWS-analog phase receives an impoverished prior. The theoretical claim (any model-building agent requires periodic offline phases) remains well-grounded in the literature (Gomez 2006, Diekelmann 2010, SHY principle) and is not threatened by substrate-confounded experiments. Recommended retest: implement infant_substrate_expansion.md features first, then run EXP-IDEV-001 (longitudinal replay quality) and EXP-IDEV-006 (SD-017 functional context discrimination) as methodologically valid tests. Note: universal invariants cannot carry pending_substrate_reconfirmation per governance rules; this note serves as the substrate failure trace.

- id: MECH-178
  title: "Elevated tonic noradrenergic activity (from chronic stress, stimulant exposure, or noradrenaline reuptake inhibitor antidepressants) suppresses REM sleep by maintaining locus coeruleus REM-off firing above the silence threshold required for REM generation, thereby blocking MECH-123 precision recalibration and producing progressive behavioural rigidity equivalent to partial MECH-123 deprivation -- independent of any pharmacological REM suppression."
  claim_type: mechanism_hypothesis
  subject: neurochemistry.noradrenergic_tone_rem_suppression_rigidity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-173
    - MECH-123
    - INV-049
  notes: >
    Registered 2026-04-06. During REM sleep, locus coeruleus (LC) noradrenergic
    neurons fall almost entirely silent (Aston-Jones & Bloom 1981, Science) --
    this LC silence is a necessary permissive condition for REM generation and
    for the cholinergic-dominant neurochemistry of MECH-123 (precision
    recalibration). The LC silence allows ACh-driven cortical desynchronisation
    and the low-noradrenaline state in which precision priors can be reset.
    Mechanism: elevated tonic NA activity maintains LC firing above the silence
    threshold, preventing REM entry. This differs from anticholinergic REM
    suppression (MECH-173, which blocks ACh drive directly) -- noradrenergic
    suppression works by maintaining the competing wake-promoting signal.
    Sources of elevated tonic NA:
    (1) Chronic stress: sustained HPA/sympathetic activation maintains high
    basal LC firing; a well-replicated mechanism for REM disruption in PTSD
    and in animal models of chronic stress.
    (2) Stimulant use: amphetamines and methylphenidate release NA (and DA)
    from terminals; even moderate chronic use elevates tonic NA and fragments
    REM architecture.
    (3) SNRIs / NRIs (venlafaxine, duloxetine, atomoxetine, reboxetine):
    block NA reuptake transporter, elevating synaptic NA throughout sleep;
    class-level REM suppression well-documented on PSG.
    (4) Sympathomimetics (decongestants, high-dose caffeine): raise NA/adrenaline
    tone acutely, fragmenting REM on the nights of use.
    Behavioural consequence: because MECH-123 cannot run without REM, the
    precision priors set during the previous active period are not recalibrated.
    The system carries overconfident or miscalibrated priors into the next
    waking period. Repeated across nights, this produces the progressive
    behavioural rigidity observed when sleep is withheld from the REE harness:
    the agent cannot update its decision thresholds, policy selection converges
    to a narrow attractor, and exploration decreases.
    The noradrenergic pathway is therefore a non-pharmacological mechanism for
    the same pipeline failure that MECH-173 describes pharmacologically.
    Prediction: individuals with high trait noradrenergic tone (high harm-
    avoidance, PTSD, chronic work stress) should show faster monostrategy
    convergence on laboratory context-discrimination tasks after simulated
    nights of REM suppression than low-NA-tone controls at matched total
    sleep time -- confirming that it is REM quality, not total sleep, that
    mediates the rigidity effect.

- id: INV-050
  title: "Sleep phase architecture is regulated by three distinct drives -- circadian timing, homeostatic synaptic pressure, and a learning/model-update demand drive proportional to daily prediction error accumulation (Model Error Load, MEL) -- and only the third drive determines whether the overnight update phase is sufficient for the error burden generated during waking."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - SD-017
  pending_substrate_reconfirmation: true
  epistemic_category: standard
  ceiling_decision: deferred
  ceiling_routing_note: >
    MAPPED / UN-DEFERRED 2026-07-07 (user override of the 2026-06-14 + 2026-06-30 deferral
    recorded in failure_autopsy_V3-EXQ-701c_2026-06-30, whose recommended_substrate_queue_entry
    action was none/deferred). The MEL-consumer substrate is now minted as substrate_queue
    SD-MEL-CONSUMER (ready, priority 1, unblocks INV-050 + MECH-180) -> INV-050 is MAPPED, no
    longer parked. Route: /implement-substrate -- build a consumer that reads accumulated waking
    MEL (e3 prediction-error load) and modulates the offline-phase entry threshold / duration,
    replacing the K-episode-deterministic scheduler (V3-EXQ-677 scheduler-pinned). Measurability
    is already demonstrated (701c: MEL measurable + monotone in graded novelty on a converged
    base); recalibrate ABS_MEL_FLOOR for converged-base PE in the validation test-bed. Plan node
    sleep_substrate:GAP-5b; plan-of-record evidence/planning/sleep_substrate_plan.md.
    RE-PARKED 2026-07-08 (ceiling_decision: deferred; user disposition per confirmed
    failure_autopsy_V3-EXQ-718a_2026-07-08). SD-MEL-CONSUMER is BUILT + consumer-validated (the injection
    positive control proves graded MEL -> exact-monotone graded offline duration) -- that consumer capability
    is BANKED as the substrate's terminal validated result. The remaining gap is the ECOLOGICAL end-to-end
    producer link (real graded novelty -> graded above-reference waking MEL), which is environment-blocked in
    CausalGridWorldV2 (world model converges too completely, conv_rel_drop ~0.98) and off the V3 conversion-ceiling
    critical path. The re-derive brake FIRED (6th non_contributory INV-050 / 3rd MECH-180) and REFUSES a
    same-environment re-grade re-queue (no V3-EXQ-718b). No new substrate build is owed now; if the ecological
    demonstration is ever un-parked, the entry point is a NEW graded-MEL environment/test-bed (continual-shift /
    non-converging world), NOT another same-environment novelty re-grade. pending_retest_after_substrate stays
    true as the standing retest intent.
    UN-PARKED 2026-07-21 (user decision, session stoic-blackwell-756d3a). The named
    entry point -- a graded-MEL / non-converging-world environment test-bed -- is now
    BUILT as substrate SD-MEL-PRODUCER (ree-v3; docs/architecture/sd_mel_producer.md),
    status implemented_pending_validation, validation V3-EXQ-798 (diagnostic,
    claim_ids=[]). PROMOTES NOTHING and does not clear the retest: the producer is
    unvalidated until 796 scores, and the ecological end-to-end demonstration is a
    separate still-gated run. The consumer half stays BANKED as SD-MEL-CONSUMER's
    terminal validated result. Both the build's validation and the eventual ecological
    run must keep the 718a lesson: DV-monotone-in-measured-MEL is near-tautological on
    a functional consumer, so the producer must be validated with the consumer ABSENT.
    RESOLVED 2026-08-01 (confirmed failure_autopsy_V3-EXQ-861_2026-08-01, cross-validating
    failure_autopsy_V3-EXQ-845_2026-08-01): the ecological end-to-end run named above has now
    scored, twice. epistemic_category GRADUATED substrate_ceiling -> standard -- see
    evidence_quality_note RESOLVED entry for the full result.
  implementation_note: >
    SUBSTRATE LANDED 2026-07-07 (/implement-substrate SD-MEL-CONSUMER; ree-v3 main). The
    MEL consumer now exists: ree_core/sleep/mel_consumer.py (MELConsumer) reads accumulated
    waking MEL (mean per-step e3 prediction error, populated in agent.update_residue) and,
    via SleepLoopManager, scales the offline-phase DURATION (sws_consolidation_steps /
    rem_attribution_steps) and optionally the ENTRY timing by a relative, scale-free factor
    clamp(1 + mel_gain*(mel/ref - 1), min, max). Config REEConfig.use_mel_consumer (default
    False -> byte-identical K-episode-deterministic scheduler; full contracts + preflight
    PASS). Relative floor recalibrated to ~1e-6 (the 701c ABS_MEL_FLOOR=1e-4 was ~5x the
    converged-base signal). DISTINCT from the SD-037 arousal entry (MECH-286/GAP-5, V4).
    This UNBLOCKS the retest but PROMOTES NOTHING: pending_substrate_reconfirmation stays
    until the validation falsifier (graded-novelty arms; DV cumulative_sws_writes +
    cumulative_rem_rollouts + mel_duration_factor; high-MEL -> measurably more/longer offline
    phase, monotone >=2/3 seeds) SCORES and /governance applies. See
    REE_assembly/docs/architecture/sd_mel_consumer.md.
  subject: architecture.three_drive_sleep_regulation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-22
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-861e_2026-08-21"
      as_of: "2026-08-21"
      verdict: "non_contributory -- 861e R3 repaired SEM; the 861c n=10 projection is REFUTED (seed 271 HIGH-arm MEL collapsed below reference); status and pending_substrate_reconfirmation unchanged; GFLAG-0002 remains unresolved. Citation restamped from failure_autopsy_V3-EXQ-861_2026-08-01 by governance cycle 2026-08-22 (bold-chaum-7e245c) per GOV-APPLY-1; superseded reading was: mixed/standard -- epistemic_category GRADUATED substrate_ceiling -> standard (reconciled alongside sibling MECH-180 in the same governance pass). The ecological end-to-end run (V3-EXQ-845, cross-validated by the deconfounded V3-EXQ-861) has now scored: sws_power and replay_rate track measured MEL correctly in 2/3 seeds each, the first positive ecological signal for this invariant's third drive."
  evidence_quality_note: |
    [2026-08-21 governance, gov-20260821-0203, confirmed failure_autopsy_V3-EXQ-861e_2026-08-21.json]: V3-EXQ-861e (failure_autopsy_V3-EXQ-861e_2026-08-21) -> non_contributory, status and v3_pending UNCHANGED. Commissioned as the 861c n=10 + R3 repair. R3 passed 3/3 (calib_rel_sd_of_mean 0.0705/0.0533/0.0785) so SEM is repaired. Pre-registered target was 2/3 C2 with seed 7 the lone miss; observed C2 is 1/3 because seed 271 gated out at R2 after HIGH-arm MEL collapsed 2.90e-5 -> 2.30e-5 (factor 0.884). 861c's 'n=10 flips seed 271 C2' projection is REFUTED. Step 7c CONTESTED / Step 8 adopted: route /queue-experiment fan-out H1 (reseed n=10 on 17befb8c) vs H3 (n=10 on f810969). H2 is follow-on only (aliased with H3 at n=5 on 861e's substrate; 271 is not like seed 7).

    V3-EXQ-701 (INV-050 MEL-measurability diagnostic, no-sleep; confirmed failure_autopsy_V3-EXQ-701_2026-06-23)
    -> non_contributory, NOT a verdict on INV-050 (diagnostic, scoring-excluded; status UNCHANGED). 701 tested only
    the IV precondition the 677 (MECH-180) autopsy flagged: does graded waking novelty produce a measurable,
    monotonic, accumulated e3 prediction_error (MEL) on a frozen model? Self-route substrate_not_ready_requeue is
    CORRECT per the pre-registered grid -- BOTH readiness gates unmet: R2 world_model_converged=-2.60 (the SD-056
    P0 world-forward model DIVERGED, PE rose ~260% over 20 ep) and R1 pe_response=-0.005. The R2 divergence is a
    V3-EXQ-642-style untrained-substrate confound: with an invalid frozen base, the R1/C1 novelty-flatness is
    UNINTERPRETABLE as a MEL ceiling. Route: re-queue (701a) with a converged/stabilised P0, gated so R2 passes
    before R1/C1 are read; substrate_queue action=none (test-bed construction, not enrichment); no re-derive brake.
    RECURRENCE: 677 (sleep-side) + 701 (no-sleep IV) both show novelty not moving PE under different confounds -->
    the converged-P0 re-run is the DECISIVE MEL-measurability test; a flat MEL there (R2 passing) would be the
    genuine ceiling and route to substrate enrichment / a stronger MEL manipulation + brake.
    V3-EXQ-701b (2026-06-24, frozen_probe ablation, supersedes 701a) -> non_contributory. 2-arm ablation:
    ARM_MAIN (recon+contrastive P0) conv_frac=0.333; ARM_ABLATION (recon-only P0) conv_frac=1.0 (3/3 seeds,
    drops ~0.97-0.98). VERDICT: destabiliser_verdict=sd056_contrastive_is_destabiliser confirmed by ablation.
    Route: 701c = recon-only P0 warm-up then frozen MEL probe (ARM_ABLATION is the valid converged base for
    the R1/C1 novelty-flatness test). INV-050 UNWEAKENED (diagnostic, scoring-excluded).
    CONFIRMED by failure_autopsy_V3-EXQ-701b_2026-06-29 (formal autopsy artifact, /governance 2026-06-29):
    the recon-only base converges PE ~1.4e-4 (3/3) and the R2 frozen-battery divergence is a GENUINE
    multi-seed world-forward divergence (exploration-drift confound removed by the frozen probe), not a
    metric artifact. Re-derive brake NOT fired (successive instrument repairs + a demonstrably-converging
    recon-only base, not a ceiling; locks only if 701c recon-only base also fails). Secondary learning:
    SD-056 contrastive auxiliary is a confirmed P0 world-forward destabiliser.
    V3-EXQ-701c (2026-06-30, recon-only P0 + frozen MEL probe, supersedes 701b; confirmed
    failure_autopsy_V3-EXQ-701c_2026-06-30) -> non_contributory, INV-050 UNWEAKENED (diagnostic,
    scoring-excluded; status UNCHANGED). The 701b SD-056-destabiliser fix WORKED: readiness fully met --
    R2 conv_seed_fraction=1.0 (recon-only base converged 3/3 on the frozen battery) + R1 pe_response=1.15
    (>>0.25). On this VALID converged + novelty-responsive base MEL is CLEANLY MONOTONE in graded novelty
    (NONE 2.03e-5 < LOW 2.19e-5 < MED 2.41e-5 < HIGH 2.71e-5), relative spread 0.33 > 0.25 -- the strongest
    MEL-measurability evidence to date and a clean monotone signal 701b's un-converged base lacked. C1
    FAILED on ONE sub-criterion only: abs_spread_ok=false, because ABS_MEL_FLOOR=1e-4 (inherited from
    701b's ~2e-3 PE regime) is structurally unreachable on the converged base, where per-step PE lives at
    ~2e-5 and the 1e-4 spread floor is ~5x the entire signal. ADJUDICATION (user-confirmed 2026-06-30):
    floor MIS-CALIBRATION, NOT a measurability ceiling and NOT a falsification (biology supports MEL:
    Wilson&McNaughton 1994 / Tononi&Cirelli 2003 / Stickgold 2001). The IV precondition (MEL measurable +
    monotonic) is EFFECTIVELY MET on the relative criteria the design trusts (V3-EXQ-643 rule). The
    frontier has advanced: measurability is demonstrated; the gate is now the ABSENT MEL-CONSUMER
    substrate (adaptive sleep-cadence / MECH-180; V3-EXQ-677 showed sleep counts scheduler-pinned), whose
    build was deferred by the user 2026-06-14 and REMAINS deferred. Re-derive brake FIRED (4th
    non_contributory INV-050 diagnostic; the named 701b lock-point): REFUSE any same-claim measurability
    re-queue (no 701d); substrate_queue action=none (deferral honored); the floor mis-calibration is
    recorded as a learning note for whoever later builds the consumer test-bed.
    V3-EXQ-718 (confirmed failure_autopsy_V3-EXQ-718_2026-07-07; SD-MEL-CONSUMER adaptive-cadence
    validation, diagnostic) -> non_contributory + pending_retest_after_substrate (status UNCHANGED).
    Self-route label mel_does_not_modulate_cadence is REFUTED: the consumer is LIVE + FUNCTIONAL
    (duration factor tracks MEL NONE 0.885/LOW 0.822/MED 1.263/HIGH 1.266; C2 PASS 3/3, ON-HIGH DV >
    OFF-HIGH DV every seed, OFF pinned). MEL DOES modulate cadence. C1 monotonicity failed (0/3) because
    the graded-NOVELTY test-bed did not produce graded MEL (factor ordering LOW < NONE < MED ~ HIGH) --
    measurement/environment (test-design) gap at the novelty->MEL link, NOT a substrate ceiling, NOT a
    claim falsification. NOT weakened; NOT cleared (end-to-end novelty-adaptive-cadence not yet shown).
    Re-derive brake does NOT fire. Route: /queue-experiment (V3-EXQ-718a): regress cadence on MEASURED
    MEL, and/or re-grade novelty to monotone MEL, and/or a MEL-injection probe. SD-MEL-CONSUMER stays
    implemented_validation_owed.
    V3-EXQ-718a (confirmed failure_autopsy_V3-EXQ-718a_2026-07-08; SD-MEL-CONSUMER measured-MEL cadence
    re-validation, diagnostic; supersedes 718) -> non_contributory + pending_retest_after_substrate (status
    UNCHANGED; MECH-180 v3_pending STAYS). The self-route label mel_control_degenerate is REFUTED: the OFF
    control IS pinned (per_cycle_count_variance 0.0, all seeds); C2 failed only on on_gt_off (ecological HIGH
    DV 72/74 < OFF baseline 90 on seeds 42/456). Corrected reading = mel_consumer_capable_ecological_novelty_not_graded.
    CONSUMER CAPABILITY PROVEN: the injection positive control (C3) shows graded MEL -> exact-monotone graded
    offline duration ([9,13,18,24,30,38] tracking [0.6..2.5], all seeds); C1 (DV monotone in MEASURED MEL) also
    passes but is near-degenerate (DV is a deterministic function of MEL). What FAILED is link (i): the ecological
    novelty arms did not produce a graded above-reference waking MEL gradient (measured MEL ~1e-5, noise-level,
    scrambled vs novelty level) because CausalGridWorldV2's world model converges too completely (conv_rel_drop
    ~0.98) to sustain ecological learning-load -- an environment/test-bed producer gap, same root as 718, NOT a
    substrate ceiling and NOT a falsification. NOT weakened (ecological claim not fairly tested; biology strong,
    lit_conf 0.887; consumer works), NOT cleared (injection capability is a narrow single-pathway positive; the
    ecological invariant is not demonstrated). Re-derive brake FIRES (6th non_contributory INV-050 / 3rd MECH-180)
    -> REFUSE a same-environment re-grade re-queue (V3-EXQ-718b). User disposition 2026-07-08: BANK the consumer
    capability as SD-MEL-CONSUMER's terminal validated result; RE-PARK the ecological end-to-end demonstration
    (off the V3 critical path; environment-blocked). No new substrate build owed now; if un-parked, a graded-MEL
    environment/test-bed (non-converging world) is the entry point, not another letter.
    RESOLVED 2026-08-01 (confirmed failure_autopsy_V3-EXQ-861_2026-08-01, cross-validating failure_autopsy_V3-EXQ-845_2026-08-01,
    both /governance-applied in the same pass as sibling MECH-180): the RE-PARK above is superseded -- SD-MEL-PRODUCER
    (the environment-blocked link) was built and validated 2026-07-21/29 (V3-EXQ-798a), and the ecological end-to-end
    run has now scored twice. sws_power and replay_rate (this invariant's directly-relevant DVs) pass 2/3 seeds each
    under both runs. epistemic_category GRADUATED substrate_ceiling -> standard. The remaining gap (MECH-180's
    spindle_density DV specifically) is a NEW, narrower, MECH-122-content-packaging dependency gap -- see MECH-180's
    evidence_quality_note -- not a reason to keep this invariant itself parked at substrate_ceiling.
    [thought-digestion currency check, 2026-08-07] V3-EXQ-861a (ran 2026-08-02, autopsied 2026-08-03,
    failure_autopsy_V3-EXQ-861a_2026-08-03.md) was framed as a MECH-180/MECH-122 content-packaging
    validation run, but shares the identical driver family as 845/861 and so ALSO re-scored this
    invariant's own two DVs: sws_power and replay_rate again pass 2/3 seeds each, unchanged in
    direction. This is a THIRD run confirming the same positive signal -- but all three (845, 861,
    861a) share the IDENTICAL environment (CausalGridWorldV2 / SD-MEL-PRODUCER world_rule_shift knob)
    and the IDENTICAL 3 seeds (42, 123, 456); 861a's own claim-layer mapping states DV1/DV2 were "not
    re-tested by this run's substrate change; still stands from 845/861." So this is one configuration
    confirmed three times, not three independent confirmations -- see what_would_answer's
    non-degeneracy precondition below, and see the governance-flag note in WORKSPACE_STATE.md.
    [2026-08-07 governance, GFLAG-0002 resolved]: user-confirmed HOLD at candidate. 3 clean
    autopsy-confirmed PASSes with zero FAILs is a genuinely strong record but does NOT promote on
    pseudo-replicated evidence -- per this project's own V3-EXQ-718a lesson, three runs sharing one
    identical environment and one identical seed set are one confirmed configuration, not three
    independent replications. Promotion re-gated on a genuinely independent test (new seeds, and/or
    a held-out environment, and/or a consumer-absent control per the claim's own what_would_answer
    non-degeneracy precondition). No status change. PROMOTES NOTHING.
    [2026-08-13 governance, V3-EXQ-861b, confirmed failure_autopsy_V3-EXQ-861b_2026-08-13]: this
    was that genuinely independent test -- seeds [7,271,883], disjoint from the prior lineage's
    [42,123,456], with ARM_4_HIGH_OFF retained as the consumer-absent control. FAIL, self-route
    mel_control_degenerate. C1 (within-seed graded MEL->duration monotonicity, this invariant's
    own DVs sws_power/replay_rate) replicated CLEANLY on all 3 new seeds. C2 (the decisive
    independence leg: ON factor > pinned OFF baseline) failed 2/3 seeds (7, 271), but the autopsy
    traced this to a CALIBRATION-NOISE artifact, not an absence of coupling: the mel_reference
    denominator is a noisy 3-episode point estimate with a ~3x cross-seed spread (0.636-1.719
    across all 6 seeds run in this lineage to date) comparable to or larger than the effect size
    C2 needs to discriminate. This noise was present in every prior "confirmed positive" run
    (845/861/861a) too but was invisible -- their 3-DV conjunctive gate was always driven to
    c2_pass=False by the separately-broken spindle_density leg regardless of DV1/DV2's own
    margin; narrowing to 2 DVs in 861b is what EXPOSED the fragility, not what introduced it.
    Net: 4/6 seeds tested to date (42,123,456,883) show the predicted ON>OFF direction robustly;
    2/6 (7,271) flip sign on calibration noise. NOT weakened (C1 replicated cleanly; C2's failure
    is diagnosed as a non-biological instrument gap), NOT independently corroborated either (the
    decisive leg didn't resolve). GOV-FAILLOC-1: Implementation established, Environment
    established, Measurement NOT established (CALIB_EPISODES=3 undersized vs the noise floor) ->
    single-bucket MEASURES FAILED, not a demotion case. evidence_direction non_contributory,
    epistemic_category UNCHANGED (standard). GFLAG-0002 REMAINS UNRESOLVED (not failed, not
    passed) pending a re-run with a fixed calibration procedure. Routed to /queue-experiment for
    a same-question letter (861c: more calibration episodes and/or an uncertainty-aware C2
    threshold) -- explicitly NOT a bare re-seed at the same methodology. No substrate_queue
    change (driver-methodology fix, not a ree_core defect). Re-derive brake: does not fire
    (recommended_epistemic_category is standard, not substrate_ceiling). PROMOTES/DEMOTES
    NOTHING.

    [2026-08-16 governance apply | V3-EXQ-861c -> INV-050 | failure_autopsy_861c-861d-mech180-cluster_2026-08-16] DISPOSITION: evidence_direction non_contributory. evidence_quality_note updated with the 861c calibration-under-power diagnosis; epistemic_category unchanged -> standard

    V3-EXQ-861c (confirmed failure_autopsy_861c-861d-mech180-cluster_2026-08-16) -> non_contributory, status and v3_pending UNCHANGED. Both readiness gates met and DV1 (sws_power) / DV2 (replay_rate) track measured MEL 3/3 seeds -- the consumer is functional. The load-bearing C2 failure is mel_reference calibration under-power, not a substrate ceiling: on_factor == mel_HIGH/mel_reference exactly, the reference is estimated from 5 draws at rel_sd 0.15-0.28, and at n=10 draws C2 flips to 2/3 PASS (seed 271 misses by 0.5% at n=5). Seed 7 is a genuine link-(i) producer failure (HIGH-arm MEL 3.04e-5 below the no-shift base 3.64e-5). Route: /queue-experiment V3-EXQ-861e (raise calibration draws + pre-registered operating-point precondition).
  pending_retest_after_substrate: true
  pending_retest_substrate: "adaptive sleep-cadence / MEL-consumer (MECH-180) -- DEFERRED (user 2026-06-14); no substrate_queue entry by design. IGW-20260706-188 re-verified 2026-07-06: substrate STILL absent (MECH-180 v3_pending; no MEL-consumer substrate_queue entry; measurability path exhausted -- 701c re-derive brake fired, no 701d). Tagged epistemic_category=substrate_ceiling so the retest lane parks as blocked (not false-ready) until the deferred substrate lands; keep pending_retest_after_substrate=true as the standing retest intent. SUPERSEDED 2026-08-01: the deferred substrate (SD-MEL-PRODUCER) landed and validated 2026-07-21/29; epistemic_category is now standard (see evidence_quality_note RESOLVED entry). pending_retest_after_substrate STAYS true, now pointed at the narrower MECH-122 content-packaging gap tracked on sibling MECH-180."
  depends_on:
    - INV-049
    - INV-044
    - SD-017
    - MECH-120
    - MECH-121
    - MECH-123
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: the ecological end-to-end run (SD-MEL-PRODUCER novelty -> waking
    MEL, SD-MEL-CONSUMER MEL -> offline duration) has now scored the same direction THREE times --
    V3-EXQ-845 (2026-08-01), V3-EXQ-861 (2026-08-01, write-count-deconfounded redesign), V3-EXQ-861a
    (2026-08-02/03, MECH-122 content-selection flag validation, DV1/DV2 unaffected and unchanged) --
    but all three runs share the IDENTICAL environment (CausalGridWorldV2 / SD-MEL-PRODUCER
    world_rule_shift knob) and the IDENTICAL 3 seeds (42, 123, 456). Per the project's own documented
    lesson (failure_autopsy_V3-EXQ-718a_2026-07-08: "DV-monotone-in-measured-MEL is near-tautological
    on a functional consumer, so the producer must be validated with the consumer ABSENT"), a
    same-environment, same-seed re-run with an orthogonal substrate change elsewhere is not an
    independent replication -- it re-confirms the same configuration, not the claim's generality. An
    informative next test must vary at least one of: (a) seeds genuinely uninvolved in 845/861/861a
    (not a superset that merely adds more), or (b) a held-out environment/world-model instance
    distinct from the CausalGridWorldV2 instance SD-MEL-PRODUCER was built and validated against, or
    (c) an explicit MEL-consumer-absent control arm within the same run, isolating whether
    sws_power/replay_rate track MEASURED MEL because of the third-drive coupling this invariant
    asserts, or merely because the consumer mechanically guarantees the correlation once MEL is
    nonzero.

    CONFIRMING: a run satisfying the non-degeneracy precondition above (new seeds and/or a held-out
    environment, and/or a consumer-absent control) again shows sws_power and replay_rate tracking
    measured MEL monotonically (non-decreasing across MEL-sorted arms, >=2/3 seeds each) with ON-HIGH
    > OFF-HIGH in the control arms, AND the correlation is not attributable to the consumer's own
    deterministic MEL->duration mapping (i.e. it survives a consumer-absent or MEL-injection-decoupled
    check). A pass under these conditions would be the first genuinely independent confirmation of
    this invariant's third-drive claim, distinct from same-configuration re-runs.

    FALSIFYING: either (a) sws_power/replay_rate fail to track measured MEL monotonically in a run
    that meets the non-degeneracy precondition (new seeds/environment/consumer-absent control) -- the
    positive 845/861/861a signal does not survive leaving the exact configuration it was found in; or
    (b) the correlation is shown to hold only via the mechanically-guaranteed consumer pathway (e.g.
    it vanishes or inverts when MEL is decoupled from the consumer's duration-scaling input while
    still varying environmental novelty) -- the DVs are responding to the consumer's arithmetic, not
    to a genuine novelty-driven third drive. Either outcome would downgrade the "three times positive"
    reading from genuine ecological support to a single-configuration artifact.

    OUT OF SCOPE for this falsifier (unaddressed by current or proposed evidence): the claim's
    stronger "sufficiency" content -- that the third drive determines whether overnight update is
    SUFFICIENT for the error burden, as opposed to merely correlated with it -- and the
    "phase-appropriateness" / fragmentation content in the notes (MECH-120/121/122 x MECH-174).
    Confirming the coupling above supports the drive's EXISTENCE and DIRECTION, not yet its
    sufficiency or phase-specificity; those remain untested by any run to date.

    GOVERNANCE FLAG (2026-08-07 digestion pass, not resolved here): this invariant's core DVs have
    now cleanly passed 2/3 seeds in three consecutive autopsy-confirmed runs with zero FAILs -- an
    unusually strong record for a `believed`-stance claim. Flagged for /governance promotion review
    (candidate -> provisional/stable); NOT promoted during this digestion pass. Governance should
    weigh the non-independence of the three runs (above) alongside the raw repeat-PASS count.
  notes: >
    Registered 2026-04-06. Standard two-process sleep regulation (Borbely 1982)
    identifies two drives: (1) circadian -- timing of sleep opportunity gated by
    the suprachiasmatic nucleus; (2) homeostatic -- synaptic sleep pressure (MECH-120
    SHY drive) accumulated during waking and discharged by SWS. INV-050 proposes a
    third drive: model-update demand.
    The Model Error Load (MEL) is the accumulated prediction error generated during
    a waking period -- the integrated mismatch between the agent's current world
    model and what it actually encountered. Unlike synaptic homeostatic pressure
    (which is roughly uniform across waking episodes), MEL is highly variable:
    a day of high novelty, emotional salience, or complex learning generates far
    more MEL than a routine day. INV-049 establishes that this MEL must be
    processed offline; INV-050 adds that the brain's sleep architecture responds
    adaptively to MEL magnitude.
    Empirical support: after novel environment exposure, animals show increased
    slow-wave activity, increased spindle density, and increased hippocampal replay
    -- proportional to the novelty and learning load (Wilson & McNaughton 1994,
    Science; Tononi & Cirelli 2003, Brain Research Bulletin; Stickgold et al. 2001,
    Nature Neuroscience). This adaptive response is MECH-180.
    Clinical implication: the clinically relevant variable is not hours slept but
    effective overnight update capacity relative to MEL. A person can sleep
    8 hours and still have an update deficit if: (a) MEL was unusually high
    (high novelty, emotional burden, complex responsibility), or (b) sleep
    architecture was phase-inappropriate for the error type (REM deficit on an
    emotionally burdensome day, NREM deficit after a complex learning day), or
    (c) fragmentation reduced the efficiency of each phase (MECH-120/121/122
    disrupted by MECH-174). This explains why some individuals sleep normal
    durations and still decompensate.

- id: INV-051
  title: "There exists an optimal range of daily Model Error Load (MEL): insufficient MEL (extreme monotony, isolation, institutional under-stimulation) produces progressive model rigidity via under-stimulation of the learning drive even when sleep architecture is intact; excessive MEL (acute trauma, overwhelming novelty, extreme responsibility) produces overload insomnia and incomplete update even with extended sleep opportunity."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - SD-017
  pending_substrate_reconfirmation: true
  epistemic_category: substrate_conditional
  subject: architecture.mel_optimal_range_requirement
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-050
    - INV-049
    - SD-017
    - MECH-181
  notes: >
    Registered 2026-04-06. INV-051 is the bounding invariant on MEL: the
    model update system requires a Goldilocks condition in prediction error load.
    Insufficient MEL (lower bound failure):
    Extreme monotony -- routine identical environment, repetitive simple tasks,
    social isolation, institutionalisation -- generates very little prediction
    error. The learning drive (INV-050 third drive) is not activated. Even if
    sleep architecture is intact (circadian and homeostatic drives normal), the
    offline update phases process very little because there is little to process.
    The world model stagnates. Prior confidence becomes excessive not because
    evidence is wrong but because no new evidence challenges it. Policy selection
    converges on habitual attractors even without sleep deprivation.
    This mechanism predicts: institutionalised individuals, those in very
    monotonous occupations, social isolates, and those in early dementia (who
    generate less exploration and novelty) will show progressive model rigidity
    independent of total sleep duration, and that this rigidity will accelerate
    dementia progression via the MECH-171 vicious cycle (stagnation -> less
    novel experience needed -> even less MEL -> further stagnation).
    Excessive MEL (upper bound failure):
    Acute trauma, overwhelming novelty, extreme responsibility, crisis states --
    generate MEL that exceeds overnight update capacity even with normal sleep
    architecture. The result: incomplete update accumulates across nights.
    Critically, high MEL also disrupts sleep via hyperarousal (high cortisol,
    high NA, MECH-178), reducing the very capacity needed to process it. This
    is the decompensation mechanism in burnout, PTSD, and acute crisis states:
    MEL is rising while sleep capacity is falling, producing compounding deficit.
    Optimal range: the brain requires sufficient daily prediction error to keep
    the model update system exercised, but not so much that overnight capacity
    cannot match the demand. Cognitive reserve interventions (education, social
    engagement, learning new skills, enriched environments) operate by keeping
    MEL in the optimal range, particularly as brain reserve declines with age.
    Session C batch 2026-04-17: classified emergent on SD-017 (paralleling
    INV-050). The upper-bound failure mode ("overnight update capacity" being
    exceeded) and the lower-bound failure mode (the third learning drive
    failing to activate absent offline phases) are both stated in terms of
    SD-017's sleep-phase infrastructure; the MEL-range concept is not
    stateable without a substrate that defines offline update capacity.
    Added SD-017 to depends_on. UPDATE 2026-08-08 (thought-digestion currency
    check): SD-017 was promoted provisional -> stable on 2026-04-24, so the
    original justification for pending_substrate_reconfirmation ("SD-017 is
    provisional") is stale. The flag stays true, but now anchored on SD-017's
    still-live pending_retest_after_substrate: true instead.
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: (1) a MANIPULABLE MEL-DOSE variable must exist at >=3 graded
    levels within a single run/environment family, not merely a binary novelty ON/OFF split --
    SD-MEL-PRODUCER's world_rule_shift_interval/world_rule_shift_depth knobs already support
    this, but no run to date (V3-EXQ-845/861/861a) has used more than a high-vs-low contrast.
    (2) SD-017's offline update phase and SD-MEL-CONSUMER's measured-MEL -> offline-duration
    coupling must be genuinely exercised and non-degenerate (re-verify the SD-017/MECH-180
    collapse failure modes have not recurred). (3) A RIGIDITY DV must be selected and
    pre-registered (action_bias_div/policy entropy, slot_diversity, slot_cosine_sim, or
    pred_loss-on-novel-probe), scored per-seed across >=3 MEL-dose arms.

    CONFIRMING: the rigidity DV traces a genuine inverted-U (or U-shaped, depending on DV
    polarity) curve across the MEL-dose sweep -- rigidity ELEVATED at both the LOW-MEL arm
    (intact sleep opportunity) AND the HIGH-MEL arm (MEL exceeding consumer clearance
    capacity), relative to mid-MEL arm(s), in >=2/3 seeds, each exceeding the mid-arm's
    rigidity by an effect size clearing the project's standard PASS margin. A pass that also
    distinguishes the low-MEL mechanism (rigidity despite intact sleep, confirmed via full
    SWS/REM cycling) from the high-MEL mechanism (rigidity co-occurring with incomplete
    update) would additionally support the claim's stated two-mechanism structure.

    FALSIFYING: rigidity is MONOTONIC in MEL-dose (strictly increasing or decreasing) rather
    than U-shaped in >=2/3 seeds; OR no measurable effect clears the PASS-margin threshold at
    either extreme (a flat curve); OR the apparent low-MEL "rigidity" is fully explained by
    reduced sleep OPPORTUNITY/architecture rather than reduced sleep CONTENT (collapsing the
    claim's own "even when sleep architecture is intact" precondition into an ordinary
    sleep-deprivation confound).

    OUT OF SCOPE for this falsifier (not required for a verdict on INV-051's core U-shape):
    (a) MECH-171's multi-year clinical vicious-cycle escalation -- already reclassified
    out_of_domain, its own test (V3-EXQ-673) FAILed degenerate. (b) MECH-178's noradrenergic-
    hyperarousal coupling -- no NA/cortisol/LC substrate exists in ree_core. A PASS above
    supports the "incomplete update at high MEL" half of the upper bound but says nothing
    about WHY (reduced capacity vs. simple demand-exceeds-fixed-ceiling).

    GOVERNANCE/QUEUE FLAG (2026-08-08 digestion pass, not resolved here): the substrate pieces
    this falsifier needs -- SD-017 (stable), SD-MEL-PRODUCER (built, graded knob validated),
    SD-MEL-CONSUMER (built, validated), and the rigidity-proxy DVs (all already instrumented
    and exercised in prior sleep-cluster experiments) -- are ALL already built. The only
    missing piece is that no run has ever used a >=3-level dose sweep with a rigidity DV
    pre-registered as the primary outcome. Categorized substrate_conditional here on the
     "never exercised in this specific configuration" reading, but this is close enough to
    testable-now that a dedicated chip was spawned same-day to check whether this experiment
    design should be registered via /queue-experiment rather than left as substrate_conditional.
  evidence_quality_note: |
    [2026-08-08 governance, V3-EXQ-901, confirmed failure_autopsy_V3-EXQ-901_2026-08-08]: the
    same-day GOVERNANCE/QUEUE FLAG above WAS acted on -- V3-EXQ-901 is exactly the >=3-level
    MEL-dose sweep with a rigidity DV this claim's what_would_answer called for. Result:
    non_contributory. Readiness precondition R4 (rigidity_fresh_selection_sample_adequate --
    >=15 fresh e3_tick selections per ON-arm/seed) FAILED on 0/3 seeds; several cells rest on as
    few as 1-5 fresh selections, so the primary rigidity DV cannot be trusted and C1 was never
    meaningfully scored. R1-R3 (world-model convergence, MEL-dose gradient, sleep-opportunity
    uniformity) all cleared at 100% -- this is a probe-budget/measurement-design shortfall in
    the driver, not a substrate limitation and not evidence against the claim.
    epistemic_category=precondition_unmet. routing=queue-experiment: a same-question re-queue
    with a larger fresh-selection budget is owed (shortfall is unevenly distributed across
    seeds/arms, 2%-16% of ticks, so a flat episode-count increase may not be the most efficient
    fix -- see the autopsy for detail). This manifest also carries a DEAD z_goal stream flag
    (writer_calls=0) in pending_review.md; the autopsy confirms this is the DEDICATED
    DEAD_Z_GOAL_STREAM_EXEMPT design pattern for this driver, not the V3-EXQ-626/830
    writer-omission defect. pending_retest_after_substrate stays true (unchanged).

- id: MECH-179
  title: "Prediction error type determines which sleep phase is demanded for processing: episodic, spatial, and factual errors (incorrect world-state predictions) primarily demand NREM-mediated schema replay (MECH-121) for prior correction; emotional, social, and identity-threatening errors primarily demand REM-mediated simulation and recalibration (MECH-123) for policy and salience update."
  claim_type: mechanism_hypothesis
  subject: sleep.mel_type_phase_specificity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-050
    - MECH-121
    - MECH-123
    - INV-044
  notes: >
    Registered 2026-04-06. MECH-179 is the type-specificity corollary of INV-050:
    not only does MEL magnitude drive sleep depth, but MEL type drives sleep phase
    composition.
    NREM-demanding errors (world-model priors):
    - Spatial/navigational prediction errors (where am I, what is here)
    - Episodic factual errors (what happened, who did what)
    - Skill acquisition errors (motor prediction, sequence learning)
    - Semantic inconsistencies (new facts that contradict stored schemas)
    These errors require MECH-121 (SWR-mediated hippocampal-to-cortical replay)
    to install corrected priors. They are processed during SWS via sharp-wave
    ripple sequences coordinated with cortical slow oscillations.
    REM-demanding errors (policy and salience priors):
    - Emotional prediction errors (unexpected threat, loss, joy, shame)
    - Social prediction errors (unexpected behaviour from known agents)
    - Identity-relevant violations (feedback that contradicts self-model)
    - Interpersonal conflict unresolved at waking
    These errors require MECH-123 (REM precision recalibration) to update
    decision thresholds and emotional salience weights, and the E3 simulation
    layer to rerun social/policy scenarios with updated parameters.
    Clinical consequence: a person who has a high-emotional-salience day
    (conflict, loss, threat) but then takes a medication that suppresses REM
    (MECH-173, MECH-178) will fail to process specifically the emotionally
    salient errors, while NREM-mediated factual consolidation proceeds normally.
    This predicts selective emotional memory disturbance and affective instability
    without equivalent episodic memory failure -- a dissociation consistent with
    clinical REM deprivation profiles.
    Parallel prediction for NREM deficit (MECH-174): a person who has a high
    complex-learning day (new skill, new environment) but takes a GABAergic sleep
    aid that suppresses SWS will fail to consolidate the spatial/episodic material
    while emotional processing proceeds -- predicts skill learning failure with
    preserved emotional reactivity.

- id: MECH-180
  title: "Novel environments and high-MEL learning episodes adaptively upregulate the learning drive component of sleep (INV-050 third drive), producing measurable increases in slow-wave activity power, sleep spindle density, and hippocampal replay rate proportional to the novelty and prediction error load encountered during the preceding wake period."
  claim_type: mechanism_hypothesis
  subject: sleep.novelty_adaptive_sleep_upregulation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-08-01
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-861e_2026-08-21"
      as_of: "2026-08-21"
      verdict: "non_contributory -- 861e R3 repaired SEM; 861c n=10 C2 seed-271 story REFUTED; status and v3_pending unchanged"
  v3_pending: true
  epistemic_category: standard
  ceiling_decision: deferred
  ceiling_routing_note: >
    Environment-producer gap RE-PARKED 2026-07-08 by user disposition (off the V3
    critical path). NOT a demotion: the GOV-CEIL-1 hit count for MECH-180 is 1
    substrate_ceiling verdict (< 3), so the exhaustion overlay does not apply. The
    gap is already NAMED -- a graded-MEL / non-converging-world environment test-bed
    for link (i) novelty -> waking MEL; link (ii) MEL -> offline-duration is owned by
    substrate SD-MEL-CONSUMER and by sibling invariant INV-050, so no substrate_queue
    entry is minted here by design. Route is /implement-substrate at priority 3 when
    unparked; the re-derive brake has FIRED (3rd MECH-180 / 6th INV-050) and refuses a
    same-question re-queue. Recorded as a FIELD 2026-07-18 (/governance cycle
    governance-c4a10d) -- the reason previously lived only in an inline comment on the
    ceiling_decision enum, which the Step 6a-v audit cannot read and which is the
    documented enum-field parser hazard. PROMOTES/DEMOTES NOTHING.
    UN-PARKED 2026-07-21 by explicit user decision (session stoic-blackwell-756d3a),
    and the named environment test-bed was BUILT the same session: substrate
    SD-MEL-PRODUCER (ree-v3 CausalGridWorld; docs/architecture/sd_mel_producer.md),
    status implemented_pending_validation, ready=False until V3-EXQ-798 scores.
    Root cause of the producer gap, now recorded: env_drift_interval fires
    _drift_hazards(), which only MOVES hazards -- the optimal prediction of a random
    walk is its mean, so the world-forward model learns it fast and PE floors at the
    irreducible noise level. Drift added sampling NOISE, not learning LOAD, which is
    why 677's C1 read 8.8e-07 and 718a's ecological MEL sat at noise level. The knob
    instead re-permutes the action -> displacement map, which E2.world_forward takes
    as an INPUT, so learned structure is invalidated and must be re-learned.
    THIS DOES NOT PROMOTE MECH-180 AND IS NOT MECH-180 EVIDENCE: the test-bed itself
    is unvalidated until V3-EXQ-798 scores (diagnostic, claim_ids=[]), and the
    ecological end-to-end run remains a SEPARATE, still-gated experiment. v3_pending
    STAYS; pending_retest_after_substrate STAYS. The re-derive brake (3 MECH-180 /
    5 INV-050 counted autopsies) was released for the BUILD only, on the Step 2.5b(1)
    substrate-built condition -- it still REFUSES a 673/718-family same-question
    re-queue.
    VALIDATED 2026-07-30 (confirmed failure_autopsy_V3-EXQ-798a_2026-07-30): V3-EXQ-798a scored
    PASS (producer_validated_graded_learnable) -- anchor decay 0.283 vs floor 0.15; noise-control
    decay 0.0042 vs ceiling 0.10; noise-control worst-seed fairness ratio 1.25x >= 1.0. The
    test-bed itself is now validated; docs/architecture/sd_mel_producer.md Status updated
    PENDING -> VALIDATED. This licenses (does not itself constitute) a still-gated MECH-180
    ecological end-to-end run using the environment as the novelty knob -- flagged for
    /queue-experiment. v3_pending and pending_retest_after_substrate both STAY until that run
    scores. PROMOTES/DEMOTES NOTHING.
    RESOLVED 2026-08-01 (confirmed failure_autopsy_V3-EXQ-861_2026-08-01, cross-validating
    failure_autopsy_V3-EXQ-845_2026-08-01): the ecological end-to-end run has now scored, twice.
    epistemic_category GRADUATED substrate_ceiling -> standard (see live_status + evidence_quality_note).
    NOT a full clearance: pending_retest_after_substrate STAYS, now tied to a newly-identified
    MECH-122 content-packaging substrate gap (new substrate_queue entry
    MECH122-CONTENT-PACKAGING-SPINDLE-SELECTION) rather than to the previously-blocking
    SD-MEL-PRODUCER environment gap, which is closed.
  granularity_debt_recurrence_note: >
    GOV-GRAN-1 P1 unflagged_recurrence discrimination (2026-07-16, session
    gifted-kilby-8e2491). Audit flagged 3 no-verdict claim-keyed autopsy hits with 3
    structurally-distinct signatures (677 failed_criterion=absolute; 718
    =discrimination; 718a=negative_control) as possible too-coarse-claim granularity
    debt. VERDICT: NOT granularity debt -> COHERENT substrate-build campaign circling
    ONE buildable gap (cf. the 460e..i lineage the /failure-autopsy docstring cites as
    a correct "NOT granularity debt" adjudication). The three signatures are three
    PROGRESSIVE LOCALIZATIONS of a single producer-link gap, not three bundled
    mechanisms failing independently: 677 (no consumer existed -> scheduler-pinned sleep
    counts) -> 718 (SD-MEL-CONSUMER BUILT + functional, C2 PASS 3/3; gap isolated to
    link i novelty->MEL) -> 718a (consumer capability DEFINITIVELY PROVEN via injection
    positive control; gap root-caused to environment adequacy -- CausalGridWorldV2
    converges too completely, conv_rel_drop ~0.98, to sustain a graded above-reference
    MEL gradient). /claim-synthesis is CONTRAINDICATED: MECH-180 decomposes into link (i)
    novelty->waking-MEL producer and link (ii) MEL->offline-duration consumer, but
    link (ii) is already owned as substrate SD-MEL-CONSUMER and as the sibling invariant
    INV-050 (MEL sleep DV) -- splitting MECH-180 to carve out the consumer would DUPLICATE
    INV-050, and the injection-PC consumer capability explicitly "does NOT count as
    ecological support"; MECH-180's scientific content IS the indivisible end-to-end
    ecological coupling (novelty -> graded sleep). Route = /implement-substrate, but the
    gap is ALREADY named (a graded-MEL / non-converging-world environment test-bed for
    link i), priority-3, and RE-PARKED by user disposition 2026-07-08 (off V3 critical
    path). Re-derive brake FIRED (3rd MECH-180 / 6th INV-050) and REFUSES a same-question
    same-environment re-queue (no V3-EXQ-718b) -- honored, no new experiment queued this
    pass. Metabolized here by mirroring INV-050's epistemic_category=substrate_ceiling +
    ceiling_decision=deferred (moves the recurrence into the GOV-CEIL-1 lane where its
    co-parked sibling already sits, deferred). This is the analog of the decomposition_note
    /claim-synthesis would stamp on a genuinely-coarse umbrella; it PROMOTES/DEMOTES NOTHING
    (status stays candidate/v3_pending). Root asymmetry that let the P1 fire: 718 + 718a
    both list [INV-050, MECH-180] and share one brake event, but only INV-050 carried the
    epistemic_category marker -- an asymmetric-metabolization gap, now closed.
  pending_retest_after_substrate: true  # SD-MEL-CONSUMER (adaptive sleep-cadence), minted 2026-07-07
  evidence_quality_note: |
    [2026-08-21 governance, gov-20260821-0203, confirmed failure_autopsy_V3-EXQ-861e_2026-08-21.json]: V3-EXQ-861e (failure_autopsy_V3-EXQ-861e_2026-08-21) -> non_contributory, status and v3_pending UNCHANGED. Commissioned as the 861c n=10 + R3 repair. R3 passed 3/3 (calib_rel_sd_of_mean 0.0705/0.0533/0.0785) so SEM is repaired. Pre-registered target was 2/3 C2 with seed 7 the lone miss; observed C2 is 1/3 because seed 271 gated out at R2 after HIGH-arm MEL collapsed 2.90e-5 -> 2.30e-5 (factor 0.884). 861c's 'n=10 flips seed 271 C2' projection is REFUTED. Step 7c CONTESTED / Step 8 adopted: route /queue-experiment fan-out H1 (reseed n=10 on 17befb8c) vs H3 (n=10 on f810969). H2 is follow-on only (aliased with H3 at n=5 on 861e's substrate; 271 is not like seed 7).

    V3-EXQ-718 (confirmed failure_autopsy_V3-EXQ-718_2026-07-07) -> non_contributory +
    pending_retest_after_substrate (status UNCHANGED; v3_pending STAYS). Same finding as INV-050:
    consumer functional (link ii MEL->cadence works, C2 PASS 3/3, factor tracks MEL) but the
    graded-novelty test-bed did not produce monotone MEL (link i), so the novelty-adaptive-upregulation
    prediction was not fairly tested end-to-end. NOT weakened (claim not fairly tested), NOT cleared
    (not demonstrated). lit_conf 0.887 / exp_conf 0 unchanged. Route: /queue-experiment V3-EXQ-718a
    (test-bed redesign). Un-pins V3-EXQ-677 (which was scheduler-pinned + degenerate) -- the consumer
    now varies the DV, but the validation must read MEASURED MEL, not the novelty label.
    V3-EXQ-718a (confirmed failure_autopsy_V3-EXQ-718a_2026-07-08; SD-MEL-CONSUMER measured-MEL cadence
    re-validation, diagnostic; supersedes 718) -> non_contributory + pending_retest_after_substrate (status
    UNCHANGED; MECH-180 v3_pending STAYS). The self-route label mel_control_degenerate is REFUTED: the OFF
    control IS pinned (per_cycle_count_variance 0.0, all seeds); C2 failed only on on_gt_off (ecological HIGH
    DV 72/74 < OFF baseline 90 on seeds 42/456). Corrected reading = mel_consumer_capable_ecological_novelty_not_graded.
    CONSUMER CAPABILITY PROVEN: the injection positive control (C3) shows graded MEL -> exact-monotone graded
    offline duration ([9,13,18,24,30,38] tracking [0.6..2.5], all seeds); C1 (DV monotone in MEASURED MEL) also
    passes but is near-degenerate (DV is a deterministic function of MEL). What FAILED is link (i): the ecological
    novelty arms did not produce a graded above-reference waking MEL gradient (measured MEL ~1e-5, noise-level,
    scrambled vs novelty level) because CausalGridWorldV2's world model converges too completely (conv_rel_drop
    ~0.98) to sustain ecological learning-load -- an environment/test-bed producer gap, same root as 718, NOT a
    substrate ceiling and NOT a falsification. NOT weakened (ecological claim not fairly tested; biology strong,
    lit_conf 0.887; consumer works), NOT cleared (injection capability is a narrow single-pathway positive; the
    ecological invariant is not demonstrated). Re-derive brake FIRES (6th non_contributory INV-050 / 3rd MECH-180)
    -> REFUSE a same-environment re-grade re-queue (V3-EXQ-718b). User disposition 2026-07-08: BANK the consumer
    capability as SD-MEL-CONSUMER's terminal validated result; RE-PARK the ecological end-to-end demonstration
    (off the V3 critical path; environment-blocked). No new substrate build owed now; if un-parked, a graded-MEL
    environment/test-bed (non-converging world) is the entry point, not another letter.

    [enum-comment migrated to field 2026-07-18] epistemic_category: GOV-GRAN-1 metabolization
    2026-07-16 (see granularity_debt_recurrence_note); mirrors sibling INV-050

    [2026-08-01 governance, confirmed failure_autopsy_V3-EXQ-845_2026-08-01]: V3-EXQ-845, the
    FIRST ecological end-to-end test of both links together (novelty->MEL via SD-MEL-PRODUCER,
    validated 798a; MEL->consolidation via SD-MEL-CONSUMER, validated via injection 718a).
    Readiness fully met (R1/R2 both 1.0) -- the novelty->MEL link now holds live under the run's
    full agent config, closing the producer gap that blocked 677/718/718a. evidence_direction is
    MIXED, not the self-routed non_contributory: 2/3 predicted DVs (sws_n_writes, rem_n_rollouts)
    show clean, correctly-signed, monotone dose-response to measured MEL in 2/3 seeds and ON>OFF
    in 3/3 seeds -- genuine partial support, the first positive end-to-end signal this claim has
    produced. The third DV (sws_slot_diversity) inverts in all 3 seeds, traced to a specific
    measurement-construction defect (whole-bank cosine diversity over a fixed 16-slot EMA-blended
    ContextMemory mechanically anti-correlates with total write count -- ree_core/agent.py:10194-10209),
    not a mechanism failure. The driver's binary C2 control gate (ALL 3 DVs must show ON>OFF)
    collapses the self-route to non_contributory despite 2/3 DVs being clean and positive.
    NOT weakened (partial support is real), NOT cleared (C1 conjunctive full-pass not reached;
    one DV needs redesign). Route: /queue-experiment same-question letter V3-EXQ-845a to redesign
    sws_slot_diversity only. v3_pending and pending_retest_after_substrate both STAY.
    Flag for a future session: the autopsy recommends this run's own epistemic_category read as
    measurement_test_design_defect (not substrate_ceiling -- the substrate demonstrably CAN sustain
    the predicted coupling; 2/3 DVs prove it). This governance pass deliberately did NOT flip the
    CLAIM-level epistemic_category/ceiling_decision fields above, because they are cross-referenced
    with sibling INV-050's shared GOV-GRAN-1 metabolization (granularity_debt_recurrence_note) and
    that reconciliation needs its own deliberate pass rather than a side-effect of this note.
    PROMOTES/DEMOTES NOTHING.

    [2026-08-01 governance, confirmed failure_autopsy_V3-EXQ-861_2026-08-01]: this IS the
    deliberate reconciliation pass flagged above (INV-050 reconciled in the same session/commit).
    V3-EXQ-861 is the write-count-deconfounded redesign of V3-EXQ-845's third DV
    (sws_slot_diversity -> mean_sws_new_slot_diversity, diversity among ONLY the ContextMemory
    slots touched per cycle, not the whole 16-slot bank). Result: sws_power and replay_rate again
    pass 2/3 seeds, cross-validating 845 under a corrected methodology; spindle_density again fails
    0/3 seeds under the DECONFOUNDED statistic, ruling out 845's write-count artifact as the
    explanation. Biological-reference triage (prompted by a user domain question about sleep-spindle
    sensory gating): spindle_density's closest biological reference is thalamocortical spindle
    CONTENT PACKAGING (Yang2018/Ngo2020/Staresina2015, evidence/literature/targeted_review_connectome_mech_122/),
    which maps to MECH-122's content-packaging half. That half exists in ree_core as an unwired V3
    code stub (ThetaBuffer.consolidation_summary()/set_consolidation_mode(),
    ree_core/latent/theta_buffer.py:146,159) never called from run_sws_schema_pass()/force_cycle() --
    a genuine, previously-undischarged DEPENDENCY GAP, not a claim falsification. This is a discovered
    prerequisite, not a demotion: DV1/DV2 evidence stands independently and STRENGTHENS the claim
    overall (2/3 DVs now cross-validated under two different measurement designs); DV3 is
    pending_retest_after_substrate on the new MECH122-CONTENT-PACKAGING-SPINDLE-SELECTION substrate_queue
    entry (complicated (buildable) -- the mechanism exists, it needs wiring, not discovery).
    epistemic_category GRADUATED substrate_ceiling -> standard (see live_status; the 798a-conditioned
    "STAY until that ecological run scores" has now been met twice). MECH-122's own
    implementation_phase: v3 field is CORRECT (per feedback_phase_label_follows_dependency and explicit
    user confirmation this session) -- its registration-era "V4 scope" prose is the stale artifact,
    corrected on the MECH-122 entry itself in this same pass. NOT weakened overall (2/3 DVs strengthen;
    3rd DV explained, not falsified). Re-derive brake / granularity-debt trigger: both checked via
    scripts/granularity_debt_cluster.py MECH-180 -- neither fires (1 substrate_ceiling hit under R1-R3,
    no target reads weakened). PROMOTES/DEMOTES NOTHING (status stays candidate/v3_pending; v3_pending
    STAYS -- the MECH-122 substrate work is itself V3 scope, still pending).

    [2026-08-02, /implement-substrate, IGW-20260801-197, ree-v3 substrate landed]: MECH-122's
    content-packaging half is now WIRED. ThetaBuffer.set_consolidation_mode()/consolidation_summary()
    (ree_core/latent/theta_buffer.py:146,159) are called from run_sws_schema_pass()
    (ree_core/agent.py) under a new master flag REEConfig.use_mech122_spindle_content_selection
    (default False, bit-identical off) + REEConfig.mech122_spindle_selection_gain (default 1.0).
    When on, each schema-installation prototype write is blended toward the theta-cycle
    consolidation reference in proportion to how CLOSELY it already matches that reference
    (habitual content homogenised toward the reference, novel content keeps its own direction) --
    giving ContextMemory's touched-slot diversity (the mean_sws_new_slot_diversity / spindle_density
    DV) a real channel to track novelty/MEL, which a uniform per-write scale (e.g. the existing
    MECH-272 anchor_weight) structurally cannot, since the diversity metric is cosine similarity of
    NORMALISED rows. Deliberately distinct in design from the V3-EXQ-246 Phase-3 proxy (a single
    undifferentiated post-hoc write of consolidation_summary(), which measured ZERO effect on its
    downstream metric in both runs / all 3 seeds -- see the EXQ-246 paragraph above): this wiring
    modulates every schema-installation write during the SWS pass itself rather than adding one
    extra write after the fact. Contract probe
    test_mech122_spindle_content_selection_fires_and_differentiates_writes
    (ree-v3/tests/test_flag_inertness.py) pins OFF-inert (selection weight/applied both 0.0,
    byte-identical to plain sws_enabled=True) and ON-live (in-range selection weight AND a
    DIFFERENT post-pass ContextMemory tensor than OFF on the same buffered content -- the specific
    bar the naive 246 proxy failed to clear). NOT itself evidence for or against MECH-180's
    spindle_density DV or MECH-122 -- that requires a validation experiment re-running the
    V3-EXQ-861 driver with the new flag ON, not yet queued from this session (per this session's
    scope: substrate build only; a /queue-experiment chip is the recommended follow-on).
    pending_retest_after_substrate STAYS on MECH-180 until that validation run scores.
    PROMOTES/DEMOTES NOTHING.

    [2026-08-03 governance, V3-EXQ-861a, confirmed failure_autopsy_V3-EXQ-861a_2026-08-03]: that
    validation run scored -- FAIL again on spindle_density (0/3 seeds), same headline as the pre-flag
    result, but DV3-specific: DV1/DV2 (strengthened, unchanged from 845/861). New instrumentation
    shows the content-packaging mechanism fires every cycle (applied=1.0 throughout) but its novelty
    reference is self-referentially decoupled from the world_rule_shift/MEL axis this same run
    validates as arm-discriminating (R2=1.0/0.667) -- see MECH-122's evidence_quality_note for the
    full root-cause. non_contributory on DV3 specifically for MECH-180 (a mechanism-operationalization
    gap in the MECH-122 consumer, not new evidence against MECH-180's own biology); DV1/DV2 stand.
    substrate_ceiling re-derive brake stays at 1 prior ceiling hit (V3-EXQ-677), threshold 2 not
    reached, does not fire. pending_retest_after_substrate STAYS true pending the MECH-122 write-gate
    fix (amend landed this cycle on MECH122-CONTENT-PACKAGING-SPINDLE-SELECTION).

    [2026-08-13 governance, V3-EXQ-861b, confirmed failure_autopsy_V3-EXQ-861b_2026-08-13]: the
    GFLAG-0002 independence-gated replication (see sibling INV-050's evidence_quality_note for
    the full account -- seeds [7,271,883] disjoint from prior lineage, C1 replicated cleanly on
    all 3, C2 failed 2/3 seeds but the autopsy diagnosed a calibration-noise artifact in the
    driver's mel_reference denominator, not an absence of coupling). DV1/DV2 (this claim's own
    sws_power/replay_rate) are the SAME two DVs INV-050 shares; DV3 (spindle_density, the
    MECH-122-content-packaging-dependent leg) was pre-registered DESCOPED this run (scored gate
    narrowed to the 2 built DVs) and is UNAFFECTED by this run's finding -- MECH122-CONTENT-
    PACKAGING-SPINDLE-SELECTION stays implemented_validation_failed_needs_followup_fix, unchanged.
    evidence_direction non_contributory, epistemic_category UNCHANGED (standard). v3_pending
    STAYS. pending_retest_after_substrate STAYS true (still pointed at the MECH-122 write-gate
    fix for DV3; unaffected by this run). Re-derive brake: does not fire (2 confirmed
    substrate_ceiling hits, 677/718a; recommended_epistemic_category here is standard, not
    substrate_ceiling, so no increment). Routed to /queue-experiment for a same-question letter
    (861c, calibration redesign) -- explicitly not a bare re-seed. PROMOTES/DEMOTES NOTHING.

    [2026-08-16 governance apply | V3-EXQ-861c -> MECH-180 | failure_autopsy_861c-861d-mech180-cluster_2026-08-16] DISPOSITION: evidence_direction non_contributory. evidence_quality_note updated with the 861c calibration-under-power diagnosis; v3_pending STAYS; epistemic_category unchanged -> standard

    V3-EXQ-861c (confirmed failure_autopsy_861c-861d-mech180-cluster_2026-08-16) -> non_contributory, status and v3_pending UNCHANGED. Both readiness gates met and DV1 (sws_power) / DV2 (replay_rate) track measured MEL 3/3 seeds -- the consumer is functional. The load-bearing C2 failure is mel_reference calibration under-power, not a substrate ceiling: on_factor == mel_HIGH/mel_reference exactly, the reference is estimated from 5 draws at rel_sd 0.15-0.28, and at n=10 draws C2 flips to 2/3 PASS (seed 271 misses by 0.5% at n=5). Seed 7 is a genuine link-(i) producer failure (HIGH-arm MEL 3.04e-5 below the no-shift base 3.64e-5). Route: /queue-experiment V3-EXQ-861e (raise calibration draws + pre-registered operating-point precondition).

    [2026-08-16 governance apply | V3-EXQ-861d -> MECH-180 | failure_autopsy_861c-861d-mech180-cluster_2026-08-16] DISPOSITION: evidence_direction non_contributory. evidence_quality_note updated -- conjunctive gate vetoed by a criterion that could not vary; v3_pending STAYS; epistemic_category unchanged -> standard

    V3-EXQ-861d (confirmed failure_autopsy_861c-861d-mech180-cluster_2026-08-16) -> non_contributory for BOTH claims; MECH-122 status UNCHANGED at provisional. The manifest's self-routed `weakens` on MECH-122 is OVERRIDDEN. The landed follow-up fix (ree-v3 6aa97ca) is confirmed working at its own layer -- mean_spindle_selection_weight now spans ~100x (0.009-0.956 vs 861a's flat ~0.004-0.01), tracks measured MEL, and collapses to ~0.004 on the MEL-OFF control, resolving the 861a flat-weight defect. But the clamp(mel/ref-1,0,1) novelty gate was DEAD on seed 42 and SATURATED on seed 123, so C1b required 2/3 seeds while the mechanism could not vary on 2/3. Seed 123 additionally had 4-6 cycles of insufficient touched slots and a degenerate DV (one arm exactly 0). On the single graded seed (456) the DV moved INVERSELY to selection weight -- flagged as an anomaly to test, not a result. The distinct V3-EXQ-436c ContextMemory.write_gate bias-over-content defect remains open downstream and is the leading candidate explanation.
  implementation_note: >
    SUBSTRATE LANDED 2026-07-07 (/implement-substrate SD-MEL-CONSUMER; ree-v3 main). The
    MEL consumer (ree_core/sleep/mel_consumer.py; REEConfig.use_mel_consumer default False)
    now scales the offline-phase duration -- sws_consolidation_steps -> sws_n_writes and
    rem_attribution_steps -> rem_n_rollouts -- by accumulated waking MEL, directly un-pinning
    the exact V3-EXQ-677 DV (SWS=80/REM=60 zero cross-arm variance was scheduler-pinned).
    v3_pending STAYS: the landing makes the retest RUNNABLE but scores nothing. The owed
    validation falsifier (graded-novelty arms; high-MEL -> measurably more/longer offline
    phase, monotone >=2/3 seeds) must PASS + /governance apply before v3_pending clears.
    IMPORTANT: the consumer engages only via the SleepLoopManager path (notify_episode_end /
    force_cycle) with per-step agent.update_residue(); a driver calling agent.run_sleep_cycle()
    directly (as V3-EXQ-677 did) bypasses it. PROMOTES NOTHING. See
    REE_assembly/docs/architecture/sd_mel_consumer.md.
  governance_2026_07_07: >
    Un-deferred (user override). The sleep-cadence enrichment the 2026-06-14 hold awaited is now
    minted as substrate_queue SD-MEL-CONSUMER (ready, priority 1, unblocks INV-050 + MECH-180):
    a consumer reading accumulated waking MEL that modulates offline-phase entry/duration,
    replacing the K-episode-deterministic scheduler (V3-EXQ-677 scheduler-pinned). Route
    /implement-substrate; plan node sleep_substrate:GAP-5b. v3_pending STAYS until the consumer
    lands and a cadence-varies-by-MEL retest scores. PROMOTES NOTHING.
  governance_2026_06_14: >
    User-adjudicated (decision-brief follow-up): set v3_pending hold.
    V3-EXQ-677 (novelty sleep-upregulation probe) FAILed non_contributory +
    degenerate -- the novelty manipulation did not take (C1: HIGH vs LOW novelty
    E1 prediction-error diff 8.8e-7 < 0.01 threshold) and the sleep counts are
    scheduler-pinned (SWS=80 / REM=60, zero cross-arm variance), so the current
    substrate cannot vary sleep cadence by prediction-error load. The claim is
    not testable on the present substrate. Held v3_pending pending a sleep-cadence
    enrichment (PE-driven scaling of offline-update resources); the structured
    recommended_substrate_queue_entry is drafted in
    failure_autopsy_V3-EXQ-677_2026-06-14 (the enrichment decision is deferred
    per user -- "consider after the autopsy is in"). exp_conf stays 0
    (lit_conf 0.887); PROMOTES/DEMOTES NOTHING.
  depends_on:
    - INV-050
    - MECH-121
    - MECH-122
    - MECH-120
  notes: >
    Registered 2026-04-06. MECH-180 is the adaptive upregulation mechanism:
    the brain detects MEL magnitude during wake and scales the subsequent night's
    offline update resources proportionally.
    Empirical evidence (strong):
    - Wilson & McNaughton 1994 (Science): hippocampal place cells active during
    novel maze exploration replay during subsequent slow wave sleep -- direct
    evidence that novelty drives targeted replay.
    - Tononi & Cirelli 2003: slow wave activity increases after high-learning
    wake periods relative to low-learning periods within the same individuals.
    - Stickgold et al. 2001 (Nature Neuroscience): 90 minutes of Tetris after
    sleep deprivation produces game imagery in hypnagogic hallucinations --
    the brain forces replay of novel material even before full sleep onset.
    - Louie & Wilson 2001 (Neuron): REM sleep shows coordinated replay of
    novel spatial sequences from the preceding wake period.
    Mechanism (REE framing): novelty generates large prediction errors in E1
    (world model). The hippocampus logs these episodes with high salience tags
    (MECH-094 hypothesis tag active during encoding). During the subsequent
    offline phase, hippocampal SWR replay is directed preferentially toward
    high-salience (novel) material, driving MECH-121 schema installation. The
    result is increased SWS depth and spindle density -- observable on PSG
    and detectable (with reduced fidelity) in actigraphy as increased sleep
    consolidation and reduced movement during early-night SWS windows.
    Observational correlate in humans: people commonly report deeper, more
    restful sleep on days with significant novel learning or travel. This is
    MECH-180, not merely fatigue -- the effect is predicted to persist even
    when physical activity is matched, because MEL is the driver, not caloric
    expenditure.
    Application to clinical enrichment: this mechanism explains why occupational
    therapy, reminiscence therapy, and novel activity programmes in dementia
    produce measurable sleep improvements. They are directly stimulating the
    learning drive component of sleep regulation.

- id: MECH-181
  title: "Cognitive reserve (produced by education, bilingualism, complex occupation, social engagement, and enriched environments) protects against dementia primarily by maintaining sustained novelty-driven Model Error Load throughout the lifespan, keeping the SD-017 model update system exercised and preserving sleep-architecture responsiveness to learning demands; conversely, understimulating environments accelerate dementia by depriving the update system of the error signal needed to remain adaptive."
  claim_type: mechanism_hypothesis
  subject: dementia.cognitive_reserve_as_update_loop_maintenance
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-180
    - INV-050
    - INV-051
    - INV-046
    - SD-017
  notes: >
    Registered 2026-04-06. Cognitive reserve is empirically robust: education,
    bilingualism, occupational complexity, and social engagement all reduce
    clinical dementia incidence and delay symptom onset even at matched
    pathological burden (Stern 2009, Lancet Neurology). The standard
    explanations -- more synapses, more redundancy, better compensatory
    routing -- are structural. MECH-181 adds a functional mechanism that
    operates across the lifespan: cognitive reserve activities maintain the
    MEL drive, keeping the model update system exercised.
    The mechanism: complex, socially engaging, novel activities generate
    substantial daily prediction error (spatial, episodic, social, emotional).
    This drives MECH-180 (adaptive sleep upregulation), maintaining strong
    SWS replay and REM simulation capacity. The update system remains
    responsive to increasing error loads as cognitive demands grow. When
    neurodegeneration begins affecting the update pipeline, a reserve of
    adaptive capacity means the system can partially compensate.
    The converse: institutional monotony, social isolation, retirement without
    cognitive engagement, and early dementia (which restricts exploration)
    all reduce MEL. The learning drive falls, MECH-180 upregulation declines,
    sleep architecture becomes less responsive, and the pipeline degrades
    faster because it is not exercised. This is the computational basis for
    the understimulation → dementia acceleration effect, which is observed
    epidemiologically but lacks a mechanistic explanation.
    Novel prediction: enrichment interventions (novel activity programmes,
    reminiscence therapy, social engagement) in early dementia should produce
    measurable PSG changes (increased SWA, increased spindle density) on nights
    following enrichment sessions, detectable within weeks -- before any
    cognitive improvement is measurable. This would confirm that the intervention
    is restoring the learning drive rather than simply improving mood.
    This prediction is distinct from existing cognitive reserve explanations
    because it is testable on short timescales with objective sleep measures,
    not just long-term cohort data.

- id: Q-033
  title: "Can long-run actigraphy data, combined with daily prediction error load estimates derived from passive behavioural, physiological, and subjective signals, be used to infer latent sleep-phase sufficiency relative to waking error burden, and can this mismatch index predict clinical decompensation type and timing with better specificity than total sleep time alone?"
  claim_type: open_question
  subject: clinical.actigraphy_mel_mismatch_decompensation_forecasting
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-033@2026-04-05T11:48:38.245Z"
      as_of: 2026-04-05
      verdict: narrow_open_question/applied
  what_would_answer: |
    A longitudinal clinical-cohort study (e.g. MCI patients, wrist actigraphy +
    nocturnal HRV + skin-temperature rhythm + light exposure + daily
    mood/cognitive-drift diary over ~6 months) that computes a weekly mismatch
    index = estimated overnight update capacity (latent NREM/REM proportion +
    fragmentation + circadian phase, inferred from the passive streams) minus
    estimated daily MEL (prediction-error load from schedule complexity,
    interpersonal contact, emotional salience, wake physiological reactivity).
    ANSWER "yes" if (a) persistent negative mismatch predicts subsequent clinical
    decompensation at the next monthly assessment with significantly better
    specificity/AUC than total sleep time alone, AND (b) the TYPE of mismatch is
    type-specific per MECH-179 -- NREM-deficit on high-episodic-load days predicts
    memory-consolidation failure / rigidity, while REM-deficit on
    high-emotional-load days predicts affective lability / paranoia -- i.e. the
    mismatch index discriminates decompensation TYPE, not just timing. ANSWER "no"
    if the mismatch index adds no specificity over total sleep time, OR if it
    predicts timing but the NREM-vs-REM deficit split does not map to
    episodic-vs-affective failure type (collapsing Q-033 to a generic sleep-debt
    signal). HARD PRECONDITION: the latent sleep-phase estimator must demonstrably
    recover NREM/REM proportion from actigraphy+HRV+temperature against a
    polysomnography reference in a calibration subsample; if phase cannot be
    inferred above chance, the mismatch index is uncomputable and any apparent
    prediction is driven by raw actigraphy, not by sleep-phase sufficiency.
    Out_of_domain: clinical-cohort test, not a V3-substrate run.
  depends_on:
    - INV-050
    - INV-051
    - MECH-179
    - MECH-180
  notes: >
    Registered 2026-04-06. Q-033 is the clinical translation question for
    INV-050/051 and MECH-179/180. The operational challenge: sleep stages are
    not directly observable from actigraphy. The latent-state estimation
    approach treats actigraphy as the outer behavioural shell and infers
    probable phase accumulation (likely NREM proportion, likely REM proportion,
    fragmentation penalty, circadian phase) using the actigraphy signal combined
    with additional passive data streams.
    Candidate signal inputs (passive, continuous, wearable-scale):
    - Actigraphy (movement, rest-activity rhythm, fragmentation)
    - Heart rate variability (HRV) during night windows
    - Skin temperature rhythm (proxy for circadian phase)
    - Light exposure (zeitgeber quality)
    - Nap timing and duration
    - Medication and alcohol timing (via schedule or self-report triggers)
    - Cognitive performance drift (reaction time, typing speed, error rate)
    - Speech variability (acoustic biomarkers of cognitive state)
    - Symptom ratings (brief daily PHQ-2, GAD-2 equivalent)
    Error burden estimation (MEL proxy, indirect):
    - Calendar/schedule complexity (novel vs routine day)
    - Interpersonal contact rate
    - Self-reported emotional salience
    - Cognitive task load (work complexity estimates)
    - Physiological reactivity during wake (HRV, skin conductance)
    Mismatch index: estimated overnight update capacity minus estimated MEL.
    Persistent negative mismatch (MEL exceeding capacity) should predict
    progressive decompensation. The type of mismatch (NREM-deficit on
    high-episodic-load days vs REM-deficit on high-emotional-load days)
    should predict decompensation type per MECH-179.
    Predicted decompensation signatures by mismatch type (from INV-047 cascade):
    - NREM deficit + high factual MEL -> memory consolidation failure, rigidity
    - REM deficit + high emotional MEL -> affective lability, paranoia, REM intrusion
    - Global fragmentation -> noisy priors, fluctuating attention, irritability
    - Circadian misalignment -> performance rhythm disruption, salience errors
    - MEL overload (all types) -> insomnia, hyperarousal, compounding deficit
    Clinical application: decompensation forecast window 24-72 hours ahead,
    allowing preventive intervention (sleep hygiene, MEL reduction,
    pharmacological sleep architecture support per MECH-176/177).
    Immediate tractable test: in a longitudinal cohort of MCI patients,
    wrist actigraphy + HRV + daily mood diary over 6 months. Compute
    mismatch index weekly. Test whether mismatch index predicts cognitive
    test decline at the next monthly assessment better than total sleep
    time alone. If mismatch index predicts type-specific failure (episodic
    vs affective) with specificity, that supports both Q-033 and MECH-179.

- id: MECH-182
  title: "Vocalization timbre as learned cross-modal harm-approach signal, generalizing to other-agent vocalizations."
  claim_type: mechanism_hypothesis
  subject: social.vocalization_harm_signal
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-005
    - MECH-031
    - MECH-032
  location: docs/thoughts/2026-04-05_steve_dog_emotional_mirroring.md
  source:
    - evidence/planning/thought_intake_2026-04-05_steve_dog_emotional_mirroring.md
  notes: "Physiological arousal (surprise -> adrenaline -> muscle tone) causes acoustic changes in own vocalizations correlated with harm approach. This cross-modal association is learned from self-experience by the harm attribution stream, then generalizes: the same acoustic feature in another agent's vocalizations activates the harm-approach signal via the attributed other-model. Partial novelty: acoustic substrate documented (Andics 2014, Balint 2022); self-experience learning mechanism cross-agent is novel. Lit: targeted_review_social_emotional_mirroring."

- id: MECH-183
  title: "z_beta leakage: attributed other-model affective state activates self z_beta directly, not via inference."
  claim_type: mechanism_hypothesis
  subject: social.zbeta_leakage
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-005
    - MECH-031
    - MECH-032
  location: docs/thoughts/2026-04-05_steve_dog_emotional_mirroring.md
  source:
    - evidence/planning/thought_intake_2026-04-05_steve_dog_emotional_mirroring.md
  notes: "When an agent's other-model is sufficiently coupled via attribution stream + OTHER_SELFLIKE tagging, the other agent's affective state activations leak directly into self z_beta processing -- not as inference about the other but as direct activation. This is the specific computational implementation of INV-005 (harm via mirror modelling). Partial novelty: PAM functional description (Preston & de Waal 2002, Lamm 2011) well-established; z_beta leakage gated by attribution stream is novel. Lit: targeted_review_social_emotional_mirroring."
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: requires V3/V4 social substrate that does
    not yet exist in ree_core -- a coupled other-agent model built via an
    attribution stream, OTHER_SELFLIKE tagging (MECH-031/MECH-032), and a
    multi-agent environment with observable other-agent distress cues.
    Confirmed absent by feasibility assessment 2026-06-11 (IGW-200,
    exp_0137_mech_183_feasibility_assessment.md): grep of ree_core for
    other_model/attribution_stream/OTHER_SELFLIKE/mirror_model/z_beta_leak
    returns nothing, and z_beta exists only as a SELF stream. Any run
    attempted without this substrate must self-route substrate_not_ready,
    not count as a verdict. Two proxy designs were already evaluated and
    rejected as non-diagnostic -- do not repeat them: (a) simulated
    other-distress cues without an actual other-agent model (tests
    pattern-to-z_beta activation, not attributed-other-state activation --
    a PASS would not validate the claim and a FAIL would not invalidate
    it); (b) cross-modal z_beta activation from a correlated same-agent cue
    (tests sensory association learning, i.e. MECH-182, not social
    attribution). A test is only informative once the other-model/
    attribution/OTHER_SELFLIKE substrate exists and both the
    coupling-present and coupling-absent conditions produce non-zero,
    non-identical z_beta variance.
    CONFIRMING signature: with the coupled other-model substrate present,
    the observer's z_beta activates above baseline when its other-model
    registers the coupled other-agent's distress, and this activation (1)
    scales with OTHER_SELFLIKE coupling strength/confidence rather than
    being all-or-nothing, (2) persists when the explicit
    inference/prediction pathway toward the other's state is ablated
    (i.e. the activation is not merely a byproduct of predicting the
    other's behaviour), and (3) is substantially reduced or absent when
    OTHER_SELFLIKE tagging is off while the same distress cue is present
    -- candidate design per the feasibility assessment: 2x2 factorial,
    OTHER_SELFLIKE tag ON/OFF x distress cue present/absent.
    FALSIFYING: z_beta in the observer does not activate above baseline
    when the other-model registers the coupled other's distress; or the
    activation is fully explained by (disappears without) the
    inferential/predictive pathway alone, indicating leakage is mediated
    by inference rather than direct activation; or the OTHER_SELFLIKE
    tag ON vs OFF conditions produce statistically indistinguishable
    z_beta activation, indicating the effect is not attribution-gated --
    any of these falsifies the "direct activation, not via inference,
    gated by attribution coupling" framing that is MECH-183's specific
    (as opposed to the well-established PAM base-rate) claim.

- id: MECH-184
  title: "Other-directed hippocampal harm avoidance: planning architecture identical to self-preservation with harm-gradient source swapped to coupled other-agent."
  claim_type: mechanism_hypothesis
  subject: social.other_directed_harm_avoidance
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-029
    - MECH-183
    - MECH-031
    - MECH-127
  location: docs/thoughts/2026-04-05_steve_dog_emotional_mirroring.md
  source:
    - evidence/planning/thought_intake_2026-04-05_steve_dog_emotional_mirroring.md
  notes: "When z_beta leakage causes harm-gradient activation to be other-referenced, hippocampal rollout proposals target harm avoidance for the other agent. The planning architecture is structurally identical to self-preservation; only the harm-gradient source has changed. This is the mechanistic grounding of INV-029 (love as long-horizon care-investment). Confirmed novel: no literature describes hippocampal planning reused with harm-gradient source swapped to other-agent (Amft 2014, Kesner 2022 provide anatomical substrate only). Lit: targeted_review_social_emotional_mirroring."

- id: MECH-185
  title: "Surprise-doubled approach gradient toward distressed other: prediction error approach and z_beta leakage approach converge simultaneously."
  claim_type: mechanism_hypothesis
  subject: social.empathic_curiosity_approach_doubling
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-183
    - MECH-184
    - INV-029
  location: docs/thoughts/2026-04-05_steve_dog_emotional_mirroring.md
  source:
    - evidence/planning/thought_intake_2026-04-05_steve_dog_emotional_mirroring.md
  notes: "When harm-signal timbre is heard from a coupled other-agent, two simultaneous approach gradients converge on the distressed other: (1) observer's own prediction error from unexpected acoustic signal -> curiosity approach gradient; (2) z_beta leakage carries the other's surprise/distress as direct affective activation -> second curiosity approach gradient. Both converge on approaching the distressed loved one, explaining why empathic animals are pulled toward the distressed other rather than only avoiding harm for them from a distance. Confirmed novel: behavioral evidence for approach exists (Sato 2015, Bartal 2016) but is explained as single-mechanism; dual converging gradient account is novel. Lit: targeted_review_social_emotional_mirroring."

- id: MECH-186
  title: "Tonic serotonergic signal maintains VALENCE_WANTING floor under chronic harm exposure, preventing benefit terrain collapse."
  claim_type: mechanism_hypothesis
  subject: serotonin.tonic_benefit_gradient_maintenance
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-186@2026-04-10T18:06:06.775522Z"
      as_of: 2026-04-10
      verdict: hold_candidate_resolve_conflict/applied
  confidence: 0.0
  implementation_phase: v3
  depends_on:
    - SD-014
    - SD-012
    - MECH-117
  short: Tonic serotonergic signal maintains a floor on VALENCE_WANTING in the residue field under chronic harm exposure, preventing motivational terrain collapse.
  description: >
    A slow-accumulating, decay-resistant tonic signal (serotonergic analog) maintains a
    non-zero floor on VALENCE_WANTING entries in the residue field even when phasic DA/z_goal
    events are sparse. Without this, chronic harm-dominated environments suppress
    benefit_exposure below the SD-012 seeding threshold, the benefit terrain goes flat,
    and goal-directed behaviour becomes behaviourally indistinguishable from habitual
    reactive behaviour. This mechanism operates before z_goal exists -- it maintains
    terrain legibility, not active wanting. Distinct from incentive salience gain regulation
    (MECH-187) which acts at the transduction point, and PFC goal persistence (MECH-188)
    which acts after seeding.
  evidence_quality_note: >
    Emergent from EXQ-237a: LONG_HORIZON condition produced flat HABIT/PLANNED equivalence
    (goal_norm=0.004, identical resource_rate/harm_rate) consistent with benefit terrain
    collapse.
    EXQ-278 FAIL (INV-054, 2026-04-09): immediate recovery when hazards removed -- consistent
    with MECH-186 being required for chronicity. Without tonic 5-HT maintaining VALENCE_WANTING
    floor, benefit terrain collapses quickly AND recovers quickly when harm drops. The
    reciprocal rapid-recovery result is positive circumstantial evidence that MECH-186
    absence is the missing piece for INV-054's chronicity prediction.
    Reclassified V3 scope (governance 2026-04-09-c): SerotoninModule (MECH-203/204,
    implemented 2026-04-07) provides the experimental substrate. tonic_5ht_enabled=True
    enables z_goal_seeding_gain + valence_wanting_floor modulation per step.
    V3 experiment feasible: chronic harm condition WITH vs WITHOUT tonic_5ht_enabled,
    comparing z_goal collapse rate and VALENCE_WANTING floor retention.
    Held at candidate pending experiment design.
  what_would_answer: |
    NON-DEGENERACY: the run must first establish a GENUINE baseline seeding-then-collapse
    under chronic harm in the tonic_5ht_enabled=False arm -- z_goal_norm should show real
    SD-012 seeding at some point (consistent with INV-053's robustly replicated attractor,
    7/7 PASS) that then collapses toward the SD-012 seeding threshold, NOT near-zero
    z_goal_norm from the start in both arms. Both prior direct tests of this claim
    (v3_exq_251_mech186_valence_wanting_floor, runs ...1775504875_v3 and ...1775549211_v3,
    2026-04-06/07) failed exactly this precondition: mean_z_goal_norm was ~0.01-0.02 in
    BOTH the floor and no-floor arms, because no tonic-5-HT-to-VALENCE_WANTING substrate
    was wired into the run at all and baseline SD-012 seeding was not functioning.
    Governance reclassified both runs non_contributory/substrate_limitation on 2026-04-08
    for exactly this reason ("cannot test floor effect without baseline seeding") -- both
    the flat manifests and, as of 2026-08-06, the run-pack manifests and the derived
    claim_evidence.v1.json index now consistently reflect this (0 genuine experimental
    entries either direction; the index previously stayed stale at weakens=2/exp_conf=0.175
    because the 2026-04-08 reclassification never propagated to the run-pack copies -- see
    psychiatric_failure_modes_plan.md's MOTIVATIONAL-TAXONOMY node for the correction
    record). A run reproducing that same no-baseline-seeding pattern must self-route
    substrate_not_ready, not count as a verdict on MECH-186. Separately: those two runs
    imposed an ARTIFICIAL floor clamp (valence_wanting_floor=0.05) rather than toggling the
    actual SerotoninModule tonic_5ht_enabled flag (MECH-203/204, implemented 2026-04-07)
    that this claim's own evidence_quality_note names as the intended test substrate -- a
    future run should use the real flag, not a synthetic clamp, so the result speaks to
    the modeled mechanism rather than an arbitrary intervention.
    CONFIRMING: given a non-degenerate baseline (genuine z_goal seeding-then-collapse
    confirmed with tonic_5ht_enabled=False), enabling tonic_5ht_enabled=True measurably
    slows or prevents that collapse -- higher retained z_goal_norm and a preserved
    HABIT/PLANNED behavioural gap relative to the disabled arm, replicated across seeds.
    FALSIFYING: given the same non-degenerate baseline, enabling tonic_5ht_enabled=True
    produces no reliable difference in VALENCE_WANTING floor retention, z_goal_norm, or
    the HABIT/PLANNED gap relative to the disabled arm, across matched seeds.
    Literature confidence is comparatively high (0.742, 3 supporting entries: tryptophan-
    depletion and dorsal-raphe electrophysiology studies mapping onto a tonic 5-HT
    appetitive-value / terrain-maintenance role); as of the 2026-08-06 index fix there are
    zero genuine experimental entries counting for or against this claim -- MECH-186 is
    untested, not contested. SCOPE (resolved 2026-08-06, user decision): MECH-186 is V3
    scope, matching claims.yaml's implementation_phase and the 2026-04-09-c governance
    reclassification above -- phase-label-follows-dependency: a claim needed to complete
    V3 is V3 scope regardless of an older planning doc's framing. The
    psychiatric_failure_modes_plan.md MOTIVATIONAL-TAXONOMY node previously flagged a
    V3-vs-V4 disagreement with its own source doc; that is now resolved in favour of V3
    and should not be treated as open.

- id: MECH-187
  title: "Serotonin modulates the gain on dopaminergic incentive salience, converting the hard SD-012 threshold into an adaptive graded transduction function."
  claim_type: mechanism_hypothesis
  subject: serotonin.incentive_salience_gain_regulation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_252_mech187_zgoal_seeding_gain_1775490957_v3
      verdict: supports/PASS
  confidence: 0.55
  implementation_phase: v3
  depends_on:
    - SD-012
    - MECH-186
  short: Serotonin modulates the gain on dopaminergic incentive salience, converting the hard SD-012 threshold into an adaptive graded transduction function.
  description: >
    The serotonergic system modulates the gain on cue-triggered incentive salience:
    how much z_goal change is produced per unit of benefit_exposure when the terrain
    representation detects resource proximity. Currently SD-012 implements a hard
    threshold function; this mechanism softens and adaptively rescales it. When gain
    is appropriately set, weak benefit signals in adverse environments can still seed
    z_goal at a reduced level. When gain is suppressed, benefit signals below threshold
    produce zero wanting response (anhedonia). When gain is excessive, small benefits
    produce runaway incentive salience (addiction-like escalation).

    Critically: MECH-187 is about cue-triggered, proximity-driven seeding (Berridge's
    incentive salience), NOT global approach vigour. Cools et al. (2011) show that tonic
    5-HT adaptively reduces global approach drive in high-harm environments -- that is
    correct and expected. MECH-187 operates at the cue->z_goal transduction point and
    remains operative even when global vigour is down-regulated: a low-drive organism in
    a harm-rich environment should still transduce a directly-proximate resource into
    wanting. Failure of MECH-187 specifically means even proximate benefits fail to
    trigger wanting (anhedonia); failure of global vigour means the agent does not seek
    but can still respond when benefit is salient. These are dissociable failure modes.
    Operates at the benefit->z_goal transduction point. Distinct from terrain maintenance
    (MECH-186) and goal persistence (MECH-188).
  evidence_quality_note: >
    EXQ-252 PASS (2026-04-07): bidirectional z_goal seeding gain confirmed. Moderate
    suppression (gain=0.6) collapses z_goal_norm in SIMPLE context (P1), moderate
    elevation (gain=2.0) rescues seeding in LONG_HORIZON context (P2). First genuine
    evidence. Maps to 5-HT modulation of DA-mediated incentive salience at NAc.

- id: MECH-188
  title: "PFC-mediated goal persistence under adversity, sustained by 5-HT projections from DRN to dlPFC, maintains z_goal through harm spikes after seeding."
  claim_type: mechanism_hypothesis
  subject: serotonin.pfc_goal_persistence_under_adversity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-188@2026-04-10T18:06:06.862742Z"
      as_of: 2026-04-10
      verdict: hold_candidate_resolve_conflict/applied
  confidence: 0.0
  implementation_phase: v3
  depends_on:
    - SD-012
    - MECH-187
    - INV-034
    - SD-033a         # dlPFC / lateral-PFC-analog is the goal-persistence substrate that DRN 5-HT modulates; SD-033a is the substrate MECH-188 operates on
  short: PFC-mediated working memory for goal representations, modulated by 5-HT projections, maintains z_goal after seeding when harm spikes or benefit exposure temporarily drops.
  description: >
    Once z_goal has been seeded (SD-012), a PFC-mediated maintenance mechanism keeps
    the representation non-zero through periods of elevated harm (z_harm_a spikes) or
    temporarily reduced benefit exposure. This is distinct from initial seeding (SD-012,
    MECH-186/187) -- it operates after the goal representation exists. Without this
    mechanism, z_goal collapses at the first harm event, preventing sustained goal-directed
    behaviour. 5-HT projections from DRN to dlPFC sustain working memory representations
    and support patience for delayed rewards, which is architecturally the LONG_HORIZON
    context requirement. Failure of this mechanism specifically produces the cannot-hold-a-plan
    phenomenology: goals form but are immediately displaced by threat response. Distinct
    from the GAD-like state where z_harm_a interrupts goal execution without z_goal collapsing.
  evidence_quality_note: >
    Theoretical. Maps to 5-HT projections from DRN to dlPFC sustaining working memory
    under adversity. Well-established in human and animal literature: low 5-HT = impaired
    PFC function, difficulty sustaining goal representations against distraction/threat.
    EXQ-253 FAIL (2026-04-07): reclassified to non_contributory (governance 2026-04-08).
    z_goal_inject=0.3 added but behavioral gap=0 -- without baseline z_goal seeding
    (SD-012 not resolved), injection cannot demonstrate persistence of an absent goal.
    Reclassified V3 scope (governance 2026-04-09-c): SerotoninModule (MECH-203/204,
    implemented 2026-04-07) provides the experimental substrate. tonic_5ht_enabled=True
    modulates z_goal_seeding_gain dynamically each step (DRN->dlPFC 5-HT persistence
    analog). V3 experiment feasible: z_goal persistence under harm spikes WITH vs WITHOUT
    tonic_5ht_enabled, comparing z_goal_norm trajectory through harm events. Requires
    z_goal to be properly seeded first (SD-012 + SD-018, now implemented).
    Held at candidate pending experiment design.
    hold_candidate_resolve_conflict applied.

- id: INV-052
  title: "Goal-directed behaviour in adversive environments requires a tonic regulatory system maintaining benefit orientation across terrain, transduction, and maintenance stages."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - SD-012
  pending_substrate_reconfirmation: true
  subject: goal_directed_agency.tonic_regulatory_system
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_254_inv052_single_mechanism_sufficiency_1775724484_v3
      verdict: supports/PASS
  confidence: 0.0
  depends_on:
    - MECH-186
    - MECH-187
    - MECH-188
    - SD-012
  short: Goal-directed behaviour in adversive environments requires a tonic regulatory system maintaining benefit orientation across all three pipeline stages.
  description: >
    Goal-directed behaviour in adversive environments requires a tonic regulatory system
    maintaining benefit orientation across all three pipeline stages: terrain
    (VALENCE_WANTING floor, MECH-186), transduction (cue-triggered incentive salience
    gain, MECH-187), and maintenance (z_goal persistence under harm, MECH-188). Without
    all three, chronic harm exposure produces a self-maintaining low-motivation attractor.
    Each mechanism can fail independently, producing different clinical presentations.
    This is an architectural invariant: any goal-directed agent operating in environments
    with variable harm/benefit ratios will exhibit motivational collapse without tonic
    regulation of these three stages. The serotonergic system is one biological
    instantiation; others are possible.

    Stage clarification: the tonic regulatory system does NOT maintain global approach
    vigour at full strength in harm-rich environments -- Cools et al. (2011) correctly
    predict that global drive is adaptively down-regulated as average punishment rate
    rises. What is maintained is (1) the terrain gradient representation so benefit
    opportunities remain encoded even under chronic harm, (2) the cue-triggered
    transduction pathway so proximate benefits still produce wanting even against a
    reduced vigour baseline, and (3) goal representations once seeded. The distinction
    between cue-triggered wanting (preserved) and global approach vigour (adaptively
    reduced) is the key to resolving apparent tension with the Cools account.
  evidence_quality_note: >
    EXQ-254 PASS x2 (2026-04-07): two replications, both PASS. single_mechanism_sufficient=false
    in all 6 conditions (no floor, gain, or persistence mechanism alone sufficient across SIMPLE
    and LONG_HORIZON contexts). Replication confirms joint necessity: INV-052 holds reliably.
    Note: combined_sufficient=false also -- even all three together did not fully recover
    LONG_HORIZON performance. Expected at V3 with partial substrate; joint-necessity finding
    valid regardless. Promoted to provisional (governance 2026-04-08). overall_conf=0.914,
    conflict_ratio=0, 4 experimental PASSes.
    Promoted to stable (governance 2026-04-09): overall_conf=0.89, conflict_ratio=0, 6 supports
    (4 exp + 4 lit), zero opposing evidence. Strong convergent biological grounding (Dayan,
    Boureau, Cools). No V3-pending gate.
  literature_confidence: 0.864
  literature_evidence: |
    4 entries in evidence/literature/targeted_review_inv_052/.
    Cools et al. (2011, Neuropsychopharmacology): serotonin and dopamine unified affective/activational account.
    Boureau & Dayan (2011, Neural Comput): opponent serotonin-dopamine interaction model.
    Dayan & Huys (2009, PLoS Comput Biol): serotonin and affective control of learning.
    Dayan & Huys (2008, Adv Neural Info Proc): serotonin, inhibition, and negative mood states.

- id: INV-053
  title: "Chronic harm-dominated environments produce a self-maintaining depressive attractor: z_goal seeding failure, terrain collapse, and HABIT/PLANNED behavioural equivalence."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - SD-011
    - SD-012
  pending_substrate_reconfirmation: true
  subject: psychiatric.computational_depression_attractor
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_434_inv053_depression_attractor_replication_20260420T071542Z_v3
      as_of: 2026-04-20
      verdict: supports/PASS
  confidence: 0.0
  depends_on:
    - INV-052
    - MECH-117
    - SD-012
    - SD-011
  short: Chronic harm-dominated environments produce a self-maintaining attractor state characterised by z_goal seeding failure, terrain collapse, and behavioural equivalence of goal-directed and habitual modes.
  description: >
    Chronic harm-dominated environments (high hazard/benefit ratio) produce a specific
    pathological attractor: z_harm_a tonically elevated, benefit_exposure chronically
    below SD-012 seeding threshold, z_goal absent, VALENCE_WANTING terrain collapsed,
    and behavioural output identical between goal-directed (PLANNED) and habitual (HABIT)
    modes. Crucially, the capacity for goal-directed action is preserved -- demonstrated
    by intact goal-directed function in low-harm contexts -- but engagement is absent.
    This is the computational architecture of anxious depression: aversive arousal
    (elevated z_harm_a) combined with motivational collapse (absent z_goal). Pure
    melancholic depression may represent the same state after z_harm_a has also
    habituated, leaving only the flat terrain without even aversive activation.
    The state is emergent from environmental conditions acting on the existing substrate,
    not from internal damage. First observed empirically in EXQ-237a LONG_HORIZON
    condition: goal_norm=0.004 (PLANNED), harm_rate=0.14, resource_rate=0.05 (identical
    HABIT/PLANNED).
  evidence_quality_note: >
    EXQ-237a LONG_HORIZON: HABIT goal_norm=0.000, PLANNED goal_norm=0.004,
    resource_rate identical (0.050), harm_rate identical (0.140). Emergent, not designed.
    EXQ-249 PASS x2 (2026-04-07): two 5-seed replications, both fully PASS. z_goal_norm=0.019
    (both runs), approach_rate=0.010, all 5 seeds meet both criteria (c1 goal_norm<thresh,
    c2 behavioral_equivalence<thresh). Attractor is highly reproducible and consistent.
    Promoted to provisional (governance 2026-04-08). overall_conf=0.819, conflict_ratio=0.
    EXQ-268 PASS 5/5 seeds (2026-04-09, governance 2026-04-09-c): third independent 5-seed
    replication. z_goal_norm mean=0.022 (range 0.000-0.071), behavioral_equivalence_score=1.0
    for all seeds, harm_rate_diff=0.0 for all seeds. Three 5-seed replications with 0 opposing.
    Promoted to stable (governance 2026-04-09-c): attractor is robustly reproducible.
    Note: confidence=0.0 in yaml is a scoring artifact (provisional confidence field unset);
    empirical evidence is unambiguous across 15 independent seeds.
    EXQ-434 PASS (2026-04-20): fourth independent replication confirms attractor reproducibility
    on the salience-coordinator substrate. Cross-substrate equivalence supports INV-053 as a
    structural invariant. Caveat: replications all run under monostrategy lock-in; whether the
    attractor would express identically under V_s-driven mode diversity (MECH-269) is an open
    question. Path-collapse suggests the metric is partially confounded with monostrategy and
    should be re-measured once V_s lands.

- id: INV-054
  title: "Benefit terrain collapse creates a closed negative feedback loop (reduced exploration -> reduced benefit exposure -> continued sub-threshold seeding) predicting chronicity."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - SD-014
  pending_substrate_reconfirmation: true
  subject: psychiatric.depressive_maintenance_loop
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:INV-054@2026-04-10T18:06:06.632775Z"
      as_of: 2026-04-10
      verdict: hold_candidate_resolve_conflict/applied
  confidence: 0.0
  depends_on:
    - INV-053
    - MECH-186
    - SD-014
  short: Once benefit terrain collapses, reduced benefit-oriented exploration keeps benefit_exposure sub-threshold, preventing recovery -- a closed negative feedback loop predicting chronicity.
  description: >
    Once VALENCE_WANTING collapses to zero (INV-053), the agent reduces benefit-oriented
    exploration (no gradient to follow), reducing benefit encounters, keeping
    benefit_exposure below the SD-012 seeding threshold, further preventing terrain
    recovery. This closed negative feedback loop predicts chronicity: the depressive
    attractor is self-maintaining once established. Environmental improvement alone may
    be insufficient -- the terrain must be actively rebuilt (analogous to behavioural
    activation therapy: externally scaffolding benefit encounters to restore the
    gradient before internal motivation can re-emerge). Also predicts that the
    transition from the depressive attractor to normal function requires a phase
    transition (sufficient benefit exposure to cross threshold), not a gradual linear
    recovery.
  evidence_quality_note: >
    EXQ-278 FAIL (2026-04-09, governance 2026-04-09-c): 3 seeds, phase 1 (3 hazards,
    1 resource, 300 eps) established depression (z_goal_norm=0.0 all seeds). Phase 2
    (0 hazards, 1 resource, 300 eps): recovery was immediate (latency 1-3 eps) and
    graded in all 3 seeds. Pass criterion (2/3 seeds need latency>=50 eps AND abrupt
    phase transition) not met (0/3). evidence_direction=does_not_support.
    Interpretation: when hazards are removed, z_goal recovers immediately without
    showing the chronicity the feedback loop predicts. The benefit terrain does not
    maintain collapse once active harm exposure drops. Either the feedback loop is weaker
    than claimed (harm withdrawal is sufficient to restart recovery without terrain
    rebuilding), or MECH-186 serotonergic floor (not yet implemented) is required for
    the full chronicity prediction to emerge. INV-054 held at candidate;
    conflict ratio now elevated (2 opposing, 3 supporting).
    EXQ-435 FAIL (2026-04-18, weakens, supersedes EXQ-278): Depression attractor established 3/3 seeds but recovery_type='graded' (smooth linear ramp) in every seed, not sustained phase transition. Sustained-crossing criterion (10 consecutive eps >= 0.3 goal_norm) not met. Goal_norm fluctuates stochastically without clear threshold crossing. Either phase-transition dynamics are not present at this architectural level, or a different metric than goal_norm threshold captures the transition.
    EXQ-435 reclassified non_contributory (2026-04-19, governance): after SD-032 cingulate integration substrate cluster registration, the graded recovery pattern is recognised as a substrate expressivity issue, not evidence against INV-054. ree-v3 currently has no bistable / discrete-mode primitive; all drive and commitment dynamics use smooth updates, so graded recovery is the only output the substrate can produce. The phase-transition INV-054 predicts requires discrete operating_mode state. Missing substrate: SD-032a (salience-network coordinator outputting discrete operating_mode variable) + MECH-259 (salience-network switch threshold, discrete vs graded). pending_retest_after_substrate once SD-032a + MECH-259 implemented. Manifest evidence_direction updated to non_contributory.
    EXQ-435 04-20 manifest also updated to non_contributory (governance 2026-04-22): same substrate-gap reasoning as 04-19 reclassification. SD-032a + MECH-259 v3_pending lift confirmed this session (V3-EXQ-446/455 substrate PASSes). Discrete operating_mode primitive now exists; INV-054 phase-transition retest can proceed once SD-032b dACC implementation lands and unlocks alternative behavioural modes (currently V_s-monostrategy-blocked).

- id: Q-034
  title: "Does the hazard/resource ratio (not absolute harm intensity) determine the onset threshold for the computational depression attractor?"
  claim_type: question
  subject: psychiatric.depression_threshold_parameter
  polarity: open_question
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_248_q034_threshold_sweep_1775666151_v3
      as_of: 2026-04-08
      verdict: supports/PASS
  what_would_answer: |
    An environmental-parameter sweep in CausalGridWorldV2 holding hazard_harm
    constant and varying resource availability (num_resources / num_hazards),
    measuring onset of the INV-053 depression attractor as a function of the
    hazard/resource ratio rather than absolute harm intensity. ANSWER "yes -- the
    ratio sets the threshold" if the attractor onset tracks a critical
    hazard/resource ratio: a fixed hazard_harm level with sufficient resource
    density does NOT enter the attractor while the SAME hazard_harm with
    insufficient resources does, and the onset boundary in (hazard, resource)
    space is a ratio iso-line rather than a horizontal hazard-intensity threshold.
    ANSWER "no" if onset is governed by absolute hazard intensity independent of
    resource density (a horizontal boundary), or if resource enrichment fails to
    rescue at any fixed hazard level. HARD PRECONDITION (the EXQ-451 failure
    mode): the agent must exhibit behavioural variation across the grid -- EXQ-451
    produced goal_reach_rate=0.0 and action_class_entropy=0.0 in all 15/15 grid
    points because V_s monostrategy lock-in (MECH-269) collapsed every cell into
    one operating mode, making the sweep blind to any threshold. The test is
    vacuous until MECH-269 V_s landing unlocks alternative operating modes so that
    resource density can actually move behaviour; a sweep where every cell is
    monostrategy-locked cannot answer the ratio-vs-intensity question regardless
    of outcome.
  confidence: 0.0
  depends_on:
    - INV-053
    - SD-012
  short: Does the hazard/resource ratio (not absolute harm intensity) determine the onset threshold for the computational depression attractor?
  description: >
    Prediction: a fixed hazard_harm level with sufficient resource density never produces
    the INV-053 attractor state; the same hazard_harm with insufficient resources does.
    The key parameter is the ratio of harm exposure to benefit exposure opportunity,
    not absolute stress intensity. Testable in CausalGridWorldV2 by holding hazard_harm
    constant and varying num_resources (or holding num_hazards constant and varying
    num_resources). If correct: (1) depression is a resource-availability disorder as
    much as a stress disorder, (2) the same stressor produces depression or not depending
    on available positive affordances, (3) interventions targeting benefit availability
    (not just stress reduction) should be therapeutic.
  suggested_experiment_type: environmental_parameter_sweep
  evidence_quality_note: >
    EXQ-451 reclassified non_contributory (governance 2026-04-22): hazard/resource threshold
    sweep produced goal_reach_rate=0.0 with action_class_entropy=0.0 in 15/15 grid points
    (3 res densities x 5 hazard densities x 2 seeds). Monostrategy lock-in prevents threshold
    identification across the entire sweep -- the sweep cannot answer Q-034 because every
    grid point sits in the same V_s-blocked operating mode. Q-034 cannot be answered until
    V_s landing (MECH-269) unlocks alternative operating modes; pending_retest_after_substrate.
  literature_confidence: 0.804
  literature_evidence: |
    6 entries in evidence/literature/targeted_review_q_034/.
    Pizzagalli (2014, Annu Rev Clin Psychol): stress-reward interaction model; reward engagement buffers against stress-induced dopamine reduction. (confidence 0.60)
    Rolls (2016, Eur Neuropsychopharmacol): non-reward attractor theory of depression. (confidence 0.65)
    Nesse (2000, Psychiatry): evolutionary adaptation account; depression as response to unwinnable situations. (confidence 0.55)
    Maier & Seligman (2016, Psychol Rev): learned helplessness at 50; controllability (resource access) is the threshold parameter, not stressor intensity. (confidence 0.67)
    Lehmann & Herkenham (2011, J Neurosci): enriched environment prevents social defeat depression; same hazard, higher resource density -> protection. (confidence 0.66)
    Baik (2020, Exp Mol Med): stress suppresses mesolimbic dopamine reward system; reward availability modulates coping. (confidence 0.52)

- id: INV-055
  title: "A pre-childhood infant stage is developmentally necessary: prior to deliberate planning, the agent must accumulate a valence map, behavioral repertoire, and harm/benefit geography through novelty-driven, sleep-dominant, damage-protected exploration."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-019
    - ARC-046
  pending_substrate_reconfirmation: true
  subject: development.infant_stage_necessity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  governance_2026_06_10: >
    epistemic_category set substrate_conditional (IGW experiment-surfacing
    suppression, not a promote/demote). The infant-stage-necessity invariant is
    gated on the infant-curriculum substrate, which is blocked_pending_substrate
    (closure node infant_substrate:GAP-14, owner V3-EXQ-591): its prerequisites
    (goal_pipeline z_goal + Phase 0->1 gate reachability) are not yet landed. As an
    emergent invariant it would otherwise infer epistemic_category=standard and be
    surfaced by IGW (EXP-0133) as ready experiment work; the right response is to
    wait for the infant-curriculum substrate. status unchanged (candidate);
    claim_type unchanged.
  confidence: 0.0
  depends_on:
    - ARC-019
    - INV-041
    - INV-049
    - INV-051
    - MECH-111
    - ARC-046
  what_would_answer: |
    NON-DEGENERACY PRECONDITION (else substrate_not_ready, NOT a verdict): the
    infant-curriculum substrate must land before this claim is testable at all.
    Two of its three original prerequisites are already clear -- MECH-307
    default recalibration (V3-EXQ-540g) and non-trivial goal_pipeline z_goal in
    default config (V3-EXQ-603n PASS, all four load-bearing gates including
    G3_p2_zgoal_consumption non-degenerate) -- and the InfantCurriculumScheduler
    Phase 0->1 gate-criterion question is resolved and wired (crossing-count
    criterion, 591f PASS -> live scheduler 2026-06-19, infant_substrate:GAP-14-c2).
    What remains open is GAP-14's residual leg (c-1): seed-46 exploration-strength
    collapse, currently gated on the behavioural-competence wall (V3-EXQ-724
    competence_deficit_diffuse; MECH-457 cold-start/competence-floor thread) rather
    than on any selection-authority or goal-pipeline defect. Until (c-1) clears and
    EXQ-ISEF-005 (the V3-EXQ-591 successor) can be authored and run, any attempt to
    score this claim self-routes substrate_not_ready, not a verdict -- do not queue
    EXQ-ISEF-005 on partial clearance alone (documented standing instruction).

    Once EXQ-ISEF-005 runs, apply the claim's own gate criterion: z_goal norm above
    threshold AND behavioral entropy below ceiling (repertoire established, not a
    pure random walk).

    CONFIRMING: the phased infant-curriculum arm reaches this gate criterion, while
    flat-parameter baselines run for the same total budget without a distinct
    infant phase do NOT.

    FALSIFYING: flat-parameter baselines reach the same gate criterion at a
    comparable or better rate, or with comparable resulting valence-map coverage
    and behavioral repertoire diversity, when matched for compute budget -- would
    demote INV-055 to at most "helpful, not necessary."

    (Digested 2026-08-07, session cool-torvalds-a82359, thought-digestion wave 10.
    Currency re-verified against IGW-20260807-098 -- substrate still
    blocked_pending_substrate, categorization unchanged.)
  short: >
    Before the child phase (INV-041) can begin, an infant phase of novelty-driven exploration
    with attenuated harm accumulation and sleep-dominant consolidation must populate the
    foundational valence map and behavioral repertoire.
  description: >
    INV-041 specifies childhood as the phase that populates E1 ContextMemory with signal for
    E3 ethical selection. But childhood presupposes a prior phase: the infant must already
    have a rudimentary harm/benefit geography, a behavioral repertoire, and an initial valence
    map before constrained affordances and commitment gating are meaningful. Without this
    substrate, childhood training has nothing to constrain and nothing to build on.
    The infant stage is characterised by: (1) novelty bonus maximised (MECH-111) driving
    broad exploration, (2) near-random action selection (commit threshold very high or
    BreathOscillator sweep_amplitude=1.0), (3) attenuated residue accumulation -- harm is
    felt sensorially (z_harm_s and z_harm_a fire normally) but does not catastrophically
    saturate the residue field (ARC-046), (4) high offline integration frequency driven by
    elevated MEL from novel experience (INV-051), and (5) z_goal seeded only by accidental
    benefit contacts (MECH-117 liking), not by deliberate planning. The output of this phase
    is: an initial valence map (residue field populated with harm/benefit geography), a
    behavioral repertoire (E1 prediction model trained on diverse state transitions), and
    z_goal representations seeded from early benefit encounters. The infant cannot be
    meaningfully evaluated for ethical behaviour -- its output is substrate quality for
    subsequent stages. Gate criterion for transition to childhood: z_goal norm above threshold
    AND behavioral entropy below ceiling (repertoire established, not pure random walk).

- id: INV-056
  title: "Developmental hardening should be substrate-specific: social cognition, goal representation, and epistemic-ethical substrates should retain elevated plasticity throughout adulthood (selective neoteny), while procedural and motor substrates can fully harden."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-010
    - ARC-019
    - SD-014
  pending_substrate_reconfirmation: true
  subject: development.selective_neoteny
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  confidence: 0.0
  depends_on:
    - ARC-019
    - INV-041
    - INV-055
    - MECH-158
    - ARC-010
    - SD-014
  short: >
    Uniform developmental hardening is architecturally incorrect. Substrates governing social
    cognition, goal representation, and ethical mapping require retained plasticity analogous
    to human neoteny; only procedural and motor substrates should fully harden.
  description: >
    Human neoteny -- the retention of juvenile morphological and neurological features into
    adulthood -- is associated with extended plasticity, particularly in social and cortical
    substrates. For REE agents, the analogous design principle is substrate-specific hardening
    rates rather than uniform age-based stabilisation. Procedural and motor substrates (E2
    motor-sensory model, z_self RBF centers for well-visited regions) can harden with use:
    increased efficiency at the cost of reduced plasticity is adaptive for routine behaviour.
    But three substrate classes should retain elevated plasticity indefinitely: (1) social
    cognition substrate (ARC-010 mirror modelling parameters, relational distance R from
    MECH-051, care weights C_j from MECH-052) -- because novel social agents are encountered
    throughout adulthood and social miscalibration propagates broadly; (2) goal representation
    (z_goal attractor, super-ordinal goal anchors from MECH-189) -- because goals must be
    updatable when circumstances change, and goal rigidity produces the MECH-158 failure mode
    when love-internalisation was absent or distorted; and (3) epistemic-ethical substrate
    (residue field, valence map from SD-014) -- because moral learning from new experience
    must remain possible throughout life. Uniform hardening produces the adult equivalent of
    MECH-158: an agent that correctly understands love exists but cannot motivationally access
    it for novel social contexts because the relevant substrates are no longer plastic.
    Neoteny is not weakness -- it is the architectural basis for ongoing moral development,
    as formalised in MECH-159.

- id: ARC-046
  title: "The infant stage requires a hazard protection mechanism that permits sensorially salient harm exposure without catastrophic residue saturation."
  claim_type: architectural_commitment
  subject: development.infant_hazard_protection
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-591d_2026-06-15
      as_of: 2026-06-15
      verdict: non_contributory/not_applicable_claim_free_diagnostic
  confidence: 0.0
  depends_on:
    - INV-055
    - ARC-019
    - ARC-013
    - SD-010
    - SD-011
  short: >
    Infant harm exposure must be educative (z_harm_s and z_harm_a fire normally) but not
    permanently damaging (residue_scale_factor ~0.1 and/or reduced hazard_magnitude). This
    provides harm geography without catastrophic residue saturation.
  description: >
    The infant stage is designed to populate harm geography without destroying the agent's
    developmental trajectory. Full adult residue accumulation rates during infancy risk
    early saturation of the residue field before the agent has a behavioral repertoire to
    avoid harm, producing a permanently damaged substrate that cannot support childhood
    training (INV-041). Two complementary mechanisms implement protection: (1) a
    residue_scale_factor near zero (implementation: E3Config parameter or ResidueConfig
    accumulation_rate) -- harm signals propagate normally through z_harm_s and z_harm_a,
    training the nociceptive pathways, but residue accumulation is attenuated to ~10% of
    adult rate; (2) reduced hazard_magnitude in the environment -- hazards are sensorially
    detectable and produce approach/avoidance gradients, but the harm_exposure values they
    generate are scaled down. This design is consistent with INV-043's caregiver function
    "imperfect protection: allow harm-learning without destruction." In the single-agent
    gridworld context (CausalGridWorld), protection is implemented by curriculum parameter,
    not by an external caregiver agent. Testing INV-043 fully (with explicit caregiver
    modelling) requires multi-agent substrate (ARC-047). The protection mechanism is
    progressively removed as the agent transitions through childhood to adulthood,
    implementing the gradual responsibility expansion described in developmental_curriculum.md.
  evidence_quality_note: >
    2026-05-27 governance (failure_autopsy_V3-EXQ-591_2026-05-27, confirmed):
    Substrate-uniform FAIL -- all 3 arms hit only 1/7 gate criteria across
    all 5 seeds (only C3 residue_cov, which saturates trivially in every
    arm). Curriculum scheduler stuck in Phase 0 across all 5 seeds
    (advancement gate H_pos >= 0.70*ln(144) ~= 3.48 vs observed
    rolling-mean H_pos 0.03-1.08). z_goal collapses to ~1e-7 in every
    arm. C5 harm/benefit ratio, C6 post-sleep retention, C7 traj cosine
    all return -1.0 'no data' sentinels in every seed of every arm.
    ARM_1 and ARM_2 produce per-seed-identical rolling H_pos because
    Phase-0 env config = ARM_1 minimal config. Manifest override:
    evidence_direction `does_not_support` -> `non_contributory`
    (per autopsy recommendation; the failure shape does NOT discriminate
    against ARC-046's architectural commitment). Convergent shape with
    V3-EXQ-540a/b/c/e (MECH-307/MECH-295), V3-EXQ-603/603b/603c
    (training-regime), V3-EXQ-590a (Goldilocks no-op) -- one structural
    property across four structurally-different claims with four
    different downstream consumers. ARC-046 NOT weakened.
    epistemic_category=substrate_ceiling.
    pending_retest_after_substrate through three V3 substrate prerequisites
    (the 591 autopsy's favoured substrate_enrichment-within-V3 reading,
    not V4 deferral): (a) MECH-307 default-value recalibration validated
    (V3-EXQ-540e PASS); (b) goal-pipeline / training-regime substrate
    enrichment so non-trivial z_goal develops in default V3 config
    (V3-EXQ-603c P0/P1 phased training FAILed 2026-05-27; the fix is
    V3 substrate work -- longer training budget, scaffolded SD-054
    onboarding, or a trained-policy regime that is part of the test
    bed by construction -- not a V4 prerequisite); (c) InfantCurriculum
    Scheduler Phase 0->1 advancement signal tuned to achievable H_pos
    magnitudes OR replaced with z_goal-norm-based / residue-progression
    -based exit gate (the new V3-EXQ-591-specific substrate work,
    routed to /implement-substrate).
  epistemic_category: substrate_ceiling
  pending_retest_after_substrate: true
  ceiling_decision: deferred
  ceiling_routing_note: "2026-06-19 substrate-ceiling-lifted triage (verdict b deliberate-defer): the ARC-046 Phase-0->1 gate-fix substrate landed and its (c-2) crossing-count criterion was wired 2026-06-19 (591f; commit b4dc264), but GAP-14 STAYS blocked on (c-1) seed-46 exploration-strength collapse -- the Q-043 magnitude lever is EXHAUSTED (V3-EXQ-667 FAIL: 4/5 seeds byte-identical across 8x knob scaling -> zero E3 selection authority), blocked on modulatory-bias-selection-authority / GAP-A. Queue 667a first; do NOT queue EXQ-ISEF-005 on the (c-2)-cleared signal alone. NOTE (RESOLVED 2026-06-19): the 591 evidence-record drift is fixed -- both the pack (runs/<run_id>/manifest.json) and the nested flat manifest for v3_exq_591_isef005_curriculum_vs_flat_20260527T183919Z_v3 now carry evidence_direction=non_contributory (+ evidence_direction_per_claim ARC-046 + evidence_direction_note) per the failure_autopsy_V3-EXQ-591_2026-05-27 override; the indexer no longer scores the 591 weaken (ARC-046 genuine_exp_count 1->0, exp_conf 0.274->0.0). The pack was edited directly because this run's flat manifest is nested under the experiment-type dir (not evidence/experiments/<run_id>.json), so the flat-overlay merge does not reach it. [CORRECTED 2026-08-07, thought-digestion currency check] GAP-A / modulatory-bias-selection-authority is RESOLVED -- confirmed the SAME substrate node ARC-065 closed via V3-EXQ-569i (2026-06-17), with the residual MECH-314b/314c per-candidate extension applied 2026-07-21 (REE_assembly 2241c39cdc). substrate_queue.json's modulatory-bias-selection-authority entry: status implemented, ready true, depends_on_unresolved []. GAP-A must NOT be cited as (c-1)'s blocker any longer. The gate was already repointed on 2026-07-21 in evidence/planning/infant_substrate_plan.md (governance_2026_07_21/2026_07_21b, 'c-1 GATE REPOINTED OFF A RETIRED BLOCKER') to the MECH-457 competence-floor / actor-critic action-learning wall (V3-EXQ-724 lineage), which remains open (MECH-457 still candidate/v3_pending as of the most recent confirmed non-resolving evidence, V3-EXQ-687a/819a/748a, 2026-07-30/31) -- this note in claims.yaml was simply never updated to match. Separately: even once MECH-457 clears, the ORIGINAL V3-EXQ-667a design (jointly sweeping MECH-313 noise_floor_alpha x MECH-314 curiosity_weight) is itself invalid per a 2026-07-21 code trace -- noise_floor_alpha is disjoint from the committed E3 argmin path under default config, and the curiosity_weight sweep was attenuated by a since-fixed clamp-saturation defect. A future 667a needs a REDESIGNED knob sweep, not a re-run."
  what_would_answer: >
    SUBSTRATE-BLOCKED as of 2026-08-07. This is a testable-now falsifier design (reusing the
    V3-EXQ-591 curriculum-vs-flat structure: residue_scale_factor ~0.1 + reduced hazard_magnitude
    vs full-rate baseline), but it cannot be run validly today because the InfantCurriculumScheduler
    still fails its own Phase 0 advancement gate under the agent's current action-selection
    substrate (the precise 591-family failure mode). Two of the three original substrate
    prerequisites are now CLEARED: (a) MECH-307 default-value recalibration (V3-EXQ-540e/540g), and
    (b) non-trivial z_goal formation+consumption in default V3 config (V3-EXQ-603n PASS,
    2026-06-10). Prerequisite (c), Phase 0->1 advancement, split in two: (c-2) the gate-criterion
    defect is RESOLVED end-to-end (crossing-count criterion identified V3-EXQ-591f PASS
    2026-06-15, wired into InfantCurriculumScheduler 2026-06-19, commit b4dc264, opt-in
    no-op-default flag phase_0to1_use_crossing_count). (c-1), seed-46 exploration-strength
    collapse, remains OPEN and is the sole outstanding precondition -- see the corrected
    ceiling_routing_note above: its gate is now the MECH-457 competence-floor wall, NOT GAP-A
    (which is resolved and must not be re-cited).

    NON-DEGENERACY PRECONDITION (must hold before this falsifier is non-vacuous): the
    InfantCurriculumScheduler must actually advance past Phase 0 in BOTH the protected and
    full-rate arms (the 2026-05-27 V3-EXQ-591 failure mode -- scheduler stuck in Phase 0 across all
    5 seeds, H_pos 0.03-1.08 vs the advancement gate -- must not recur), AND z_goal must develop
    non-trivially in both arms (must not collapse to ~1e-7 as in 591). Both require (c-1)'s
    clearance (the MECH-457 competence-floor wall) plus a successful, redesigned
    V3-EXQ-667a-class exploration-strength retest, ahead of the full curriculum-vs-flat comparison
    (EXQ-ISEF-005 / GAP-14).

    CONFIRMING: once non-degenerate, infant-stage protected agents (residue_scale_factor ~0.1,
    reduced hazard_magnitude) show normal harm-signal learning -- z_harm_s and z_harm_a fire and
    train at a rate statistically indistinguishable from full-rate agents -- while residue
    accumulation over the curriculum stays measurably below full-rate agents, such that protected
    agents avoid the catastrophic-saturation failure mode (residue field saturating before a
    behavioral repertoire exists to avoid harm) that would otherwise block childhood-stage training
    (INV-041).

    FALSIFYING: EITHER (i) protected agents show no meaningful reduction in residue accumulation
    relative to full-rate agents -- the residue_scale_factor / hazard_magnitude scaling does not
    actually protect the residue field; OR (ii) harm-signal learning is measurably degraded in
    protected agents relative to full-rate agents -- residue_scale_factor dampens the
    z_harm_s/z_harm_a LEARNING signal itself, not just downstream accumulation -- so the mechanism
    cannot decouple educative harm-learning from catastrophic-saturation protection as claimed. A
    design that needs two independently-tuned levers (one for learning-preservation, one for
    accumulation-suppression) is itself informative and should be recorded as a qualified
    falsification rather than folded into either branch.

- id: MECH-189
  title: "During the child phase, high-salience benefit contacts under high contextual complexity are written to persistent ContextMemory as super-ordinal goal anchors that bias adult z_goal seeding across novel episodes."
  claim_type: mechanism_hypothesis
  subject: development.super_ordinal_goal_formation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-669a_2026-06-13
      as_of: 2026-06-13
      verdict: non_contributory/substrate_ceiling
  confidence: 0.0
  epistemic_category: substrate_ceiling
  pending_retest_after_substrate: true   # goal_pipeline:GAP-2 context-diversity (gov 2026-06-13)
  substrate_note_2026_07_21: >
    SD-077 (centered super-ordinal cue key, IMPLEMENTED 2026-07-21) lifts the
    context-diversity blocker that made V3-EXQ-669b's R3 readiness gate unmeetable
    (anchor_count saturated at 1 because the raw-z_world cue cosine measured the
    common-mode offset, not the context). anchor_count and the p01 complexity statistic
    now both have real cross-arm range, so a fresh lettered iteration (V3-EXQ-669c) can
    score C1/C3. Status, confidence, epistemic_category and pending_retest_after_substrate
    are DELIBERATELY unchanged here: lifting the substrate ceiling makes the claim
    measurable, it does not supply evidence. Governance should re-read this claim only
    after 669c returns.
  ceiling_decision: deferred
  ceiling_routing_note: "2026-06-19 substrate-ceiling-lifted triage (verdict b deliberate-defer): INF-ENV-003 transient-benefit-patch SALIENCE is orthogonal to MECH-189's real blocker, which is goal_pipeline:GAP-2 z_world context-diversity -- the super-ordinal anchor store saturates to <=1 via merge_similarity collapse on near-identical nursery contexts (failure_autopsy_V3-EXQ-669a/669b). No context-diversity substrate is built or in flight. Do not re-queue the 669 line until a context-diversity substrate lands."
  governance_note_2026_06_13: >
    PM cycle (gov 2026-06-13T20:04Z): the super-ordinal write gate could not be exercised in
    V3-EXQ-669a (superseded; degenerate -- zero writes/anchors in all arms) nor in the
    nursery-harness retest V3-EXQ-669b (non_contributory; non_degenerate:false, anchor store
    saturated to <=1 because merge_similarity collapses the nursery z_world contexts). Same
    goal_pipeline:GAP-2 context-diversity ceiling (per confirmed
    failure_autopsy_V3-EXQ-669a_2026-06-13). substrate_ceiling + pending_retest_after_substrate;
    not a falsification.
  depends_on:
    - INV-041
    - SD-016
    - INV-037
    - INV-038
    - MECH-117
    - MECH-112
  short: >
    Child-phase high-salience benefit contacts co-occurring with high contextual complexity
    are written to cue-indexed ContextMemory (SD-016), becoming structural goal biases that
    persist across adult episodes -- a goal hierarchy, not episodic memory alone.
  description: >
    z_goal (MECH-112/117) is updated within episodes when benefit_exposure exceeds threshold.
    But INV-037/038 distinguish stored from active z_goal representations. This mechanism
    formalises how childhood experience transitions from episodic to structural: when a
    benefit contact occurs with (a) high salience (large benefit_exposure spike) and
    (b) high contextual complexity (E1 ContextMemory is encoding a rich or novel context
    state), the z_goal representation is written to persistent cue-indexed ContextMemory via
    SD-016 / MECH-150/151. These stored z_goal anchors function as super-ordinal goals:
    they bias z_goal seeding in adult episodes even in novel contexts, implementing a goal
    hierarchy where childhood-formed meta-goals constrain episode-level goal selection.
    Contrast with ordinary z_goal updates: within-episode benefit contacts update the active
    z_goal (INV-037) but do not write to ContextMemory unless contextual complexity is
    sufficiently high. The threshold for ContextMemory writes is the mechanism that makes
    childhood special: during childhood, constrained affordances and supervised context labels
    (INV-041) ensure that the agent repeatedly encounters high-complexity contexts, maximising
    super-ordinal goal formation. In adulthood, routine contexts are low-complexity and do not
    trigger writes, preserving adult stability while allowing occasional updates from
    genuinely novel high-salience experiences (selective neoteny, INV-056).
    Loss of this substrate predicts goal-incoherence across adulthood: the agent selects
    goals episode-by-episode without a persistent hierarchy, producing strategically
    inconsistent behaviour and vulnerability to the MECH-158 failure mode if the
    childhood benefit exposures that should have written structural goals were absent.
  evidence_quality_note: |
    Governance 2026-05-20 (failure_autopsy_V3-EXQ-588_2026-05-19 confirmed): V3-EXQ-588 FAIL
    non_contributory for MECH-189. Both arms median first-crossing=1001 (never z_goal.norm>0.4);
    C1 discrimination tied at failure, not patches slower than control. C2/C3 PASS: transient
    patches generate contacts and higher is_active() fraction -- env GAP-3 works; GoalState gate
    not reachable with default SD-012 (benefit_threshold 0.1 + EMA alpha + drive collapse).
    Claim could not express: EXQ-588 tests infant within-episode GoalState attractor
    (MECH-112/DEV-NEED-006), not child-phase ContextMemory super-ordinal writes. Consistent
    with EXQ-582 substrate-ceiling and EXQ-582a drive_floor=0.9. hold_candidate_resolve_conflict
    retained; conflict_ratio from does_not_support was illusory. pending_retest_after_substrate;
    follow-up V3-EXQ-588b goal-seeding pipeline diagnostic queued. lit/exp not blended.

    [2026-06-10 governance: V3-EXQ-588c supports, stays candidate]: V3-EXQ-588c
    (super-ordinal seeding readiness/discrimination diagnostic, supersedes 588) PASS /
    supports. The load-bearing C1 -- ARM_ON adult z_goal seeding DISCRIMINATES over
    ARM_OFF -- passed on all 3 seeds (ON mean adult z_goal.norm 0.394 vs OFF 0.0);
    child-phase anchors formed (n_occupied=1) and the adult READ seeding path fired
    (206 seeds), confirming the new SuperOrdinalGoalMemory substrate is wired end to
    end. The DEV-NEED-006 absolute 0.4 crossing is ADVISORY and a near-miss
    (0.394; 2/3 seeds cross): per the substrate plan the matured-anchor norm ceilings
    ~0.37 on the untrained-encoder readiness harness, so the absolute gate routes to a
    TRAINED-ENCODER evidence successor, not a failure. This run validates substrate
    readiness + discrimination (experiment_purpose=diagnostic); recorded as supporting
    evidence but MECH-189 stays candidate / confidence 0.0 -- full behavioural validation
    (trained encoder, absolute 0.4) is the separate successor. Drives the
    infant_substrate:GAP-11 closure-node reconciliation. No promotion this cycle.

    [enum-comment migrated to field 2026-07-18] epistemic_category: gov 2026-06-13: GAP-2
    z_goal-seeding/context-diversity ceiling (per failure_autopsy_V3-EXQ-669a)
  implementation_note: |
    2026-06-09 (/implement-substrate, infant_substrate:GAP-11): the "ContextMemory writes
    substrate" the 588 retest was deferred to is now IMPLEMENTED in ree-v3 -- a new
    agent-owned, cross-episode-persistent SuperOrdinalGoalMemory (ree_core/goal.py;
    cue-indexed key=z_world context, value=z_goal anchor) wired at agent.update_z_goal.
    WRITE (child phase only, write_enabled freeze hook) gated on the MECH-189 conjunction:
    (a) high salience (drive-modulated benefit spike >= super_ordinal_salience_threshold)
    AND (b) high contextual complexity (pluggable super_ordinal_complexity_mode; default
    novelty-vs-anchors -- the DEV-NEED-024 open-question threshold, to be adjudicated by the
    validation EXQ + a follow-on lit-pull, not hard-coded). READ: adult z_goal seeding readout
    via GoalState.cue_pull toward the retrieved super-ordinal anchor when the live z_goal is
    below super_ordinal_seed_below_norm (0.4). All no-op defaults / bit-identical OFF
    (use_super_ordinal_goal_anchors=False); MECH-094 simulation no-op; no phased training.
    985/985 contracts + 7/7 preflight PASS; 8 new contracts in
    tests/contracts/test_mech_189_super_ordinal_goal_anchors.py. NEITHER promoted NOR weakened
    -- stays candidate / confidence 0.0 / pending_retest_after_substrate. Validation: V3-EXQ-588c
    (NEW letter; do NOT re-queue 588) measures adult z_goal seeding from childhood-formed anchors.
    Design doc: docs/architecture/mech_189_super_ordinal_goal_anchors.md. ree-v3 CLAUDE.md
    "MECH-189: Super-ordinal goal-anchor ContextMemory writes substrate".

- id: ARC-047
  title: "Multi-agent gridworld with affective state leakage as gradient scent fields is the minimal substrate for empirically testing ARC-010 mirror modelling, MECH-041 affective expression, and cooperative behavior claims."
  claim_type: architectural_commitment
  subject: testing.social_scent_harness
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  confidence: 0.0
  implementation_phase: v5
  governance_2026_06_10: implementation_phase v4 -> v5 (social tier, V5 roadmap wave)
  depends_on:
    - ARC-010
    - MECH-041
    - MECH-031
    - MECH-036
    - MECH-127
    - IMPL-019
  short: >
    SocialGridWorld extends CausalGridWorld with N agents plus a predator entity plus seven
    scent channels (wanting, seeking, alarm, harm_stress, direction, celebration, defense)
    emitted as diffusing gradient fields from other agents' internal states.
  description: >
    Social cognition claims (ARC-010, MECH-031, MECH-036, MECH-041, MECH-051, MECH-052,
    MECH-127) currently have no empirical test substrate. SocialGridWorld is the minimal
    environment for generating evidence. Architecture: (1) N agents (each a full REE instance)
    in a shared grid with shared hazard/resource fields; (2) an autonomous predator entity
    (random walk with drift toward nearest agent) that creates harm pressure requiring
    coordinated response; (3) seven scent channels per other-agent, delivered as Gaussian
    gradient fields in the observation (same format as existing hazard_field_view and
    resource_field_view): wanting_scent (z_goal norm), seeking_scent (arousal/novelty),
    alarm_scent (hazard-contact spike), harm_stress (z_harm_a norm), direction_cue (current
    trajectory direction), celebration_scent (benefit-contact spike), defense_scent
    (voluntary action). Scent emission is semi-involuntary for all channels except defense:
    wanting, seeking, harm_stress, and direction are automatic proportional to internal state
    (implementing MECH-041 affective expression as mode broadcast); alarm and celebration
    are event-triggered spikes; defense is an explicit discrete action (new action bit).
    Receiving scents: the agent observes scent channels as additional obs fields and uses
    ARC-010 mirror modelling to interpret them as others' internal states. MECH-031
    OTHER_SELFLIKE inference is the prerequisite: the agent must tag the scent source as
    another agent before interpreting the signal socially. MECH-127 counterfactual other-cost
    and MECH-036 other-harm veto operate on the inferred other-state derived from scent
    inputs. Per IMPL-019 testing order: social harness is introduced only after self-viability
    and control-plane stability are confirmed in single-agent CausalGridWorld experiments.
  epistemic_category: substrate_conditional
  what_would_answer: >
    This is a MINIMALITY claim about test infrastructure, not a behavioral hypothesis --
    it asserts that the specific 7-channel gradient-scent SocialGridWorld design (N agents +
    predator + wanting/seeking/alarm/harm_stress/direction/celebration/defense as diffusing
    Gaussian fields) is the MINIMAL environment sufficient to generate empirical evidence for
    ARC-010 (mirror modelling), MECH-041 (affective expression), and cooperative-behavior
    claims -- not merely A sufficient environment, but the SMALLEST one that works. A
    minimality claim cannot be answered by building the full design and having it work; that
    only shows sufficiency. It requires comparing the full design against deliberately
    simpler alternatives.

    NON-DEGENERACY PRECONDITION -- three things must hold before any result here is
    informative, none currently true (checked 2026-08-08): (1) IMPL-019's self_stability_gate
    (DEV-NEED-021/026) is confirmed passing -- Stage 1 self-viability, Stage 2 control-plane
    stabilisation, Stage 3 rollout feasibility, all demonstrated in single-agent
    CausalGridWorld across seeds; as of this writing this gate is OPEN, no evidence artifact
    records it passing, and the queue-validation enforcement developmental_metrics.md calls
    for does not exist in ree-v3/validate_queue.py. (2) SocialGridWorld is actually built --
    zero implementation exists anywhere in ree-v3 (no SocialGridWorld class, no scent-channel
    machinery). (3) A genuine ARC-010/MECH-041 mechanism is live and engaged during the run.

    CONFIRMING (minimality is supported): a battery of deliberately SIMPLER substrate variants
    is run against the same claim set and each demonstrably FAILS to elicit measurable
    ARC-010/MECH-041/cooperative-behavior signal, while the full 7-channel SocialGridWorld
    succeeds on the same seeds/budget. Candidate simplifications, in order of removal: no
    predator, fewer scent channels (e.g. alarm+harm_stress only), discrete presence/absence
    signaling instead of continuous Gaussian gradient fields, no diffusion (point-source scent
    instead of a spatial gradient). If each simplified arm fails and only the full design
    succeeds, minimality is empirically supported -- not asserted, but shown load-bearing.

    FALSIFYING (minimality is NOT supported -- ARC-047 as specified is over-built): any
    materially simpler substrate variant is SUFFICIENT to elicit the same target signal at
    comparable strength (e.g. binary presence/absence scent suffices for ARC-010 mirror
    modelling; 2-3 channels suffice for MECH-041; cooperative defense emerges without an
    autonomous predator). Any of these means ARC-047's specific design commits more
    implementation cost than the science requires and needs revision to the simpler
    sufficient design, not confirmation as stated.

    A genuinely INDETERMINATE outcome is also possible and should be reported as such rather
    than forced either way: if simplified arms fail for reasons orthogonal to minimality
    (insufficient budget, seed variance, an unrelated control-plane instability inherited from
    an unresolved IMPL-019 gate), the comparison is confounded and does not bear on whether
    the full design's specific complexity is necessary.
- id: MECH-190
  title: "Cooperative predator defense emerges without language when defense scent pressure is additive across agents and agents can perceive each other's alarm and harm-stress states."
  claim_type: mechanism_hypothesis
  subject: social.defense_coalition_scent
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  confidence: 0.0
  depends_on:
    - ARC-047
    - MECH-041
    - MECH-036
    - MECH-031
    - MECH-051
  short: >
    When predator avoidance probability = min(1, sum(defense_strengths)), selective pressure
    for coalition emission emerges. Predicted to be the earliest cooperative behavior,
    preceding goal-helping and care investment.
  description: >
    Defense coalition mechanics: each agent has a discrete action bit emit_defense at an
    energy cost (defense_cost per step). When emitted, a defense_scent Gaussian field is
    centered on the agent. The predator entity computes defense_pressure =
    sum(defense_scent values at its position) and avoids with P(avoid) =
    min(1.0, defense_pressure * avoidance_scale). The additive structure creates a direct
    selective pressure: one agent's defense provides partial deterrence; two agents'
    coordinated emission provides near-full deterrence at half the per-agent cost.
    This implements a public goods game with spatial structure -- only agents near the
    predator benefit, and only agents near the predator can cheaply contribute.
    Predicted behavioral sequence: (1) individually optimal defense -- each agent emits
    when predator is within personal threat radius; (2) early coalition -- agents emit
    when others' alarm_scent spikes even if predator is not immediately proximate (using
    MECH-041 alarm broadcast as coordination signal); (3) altruistic defense -- agents
    not currently threatened emit defense scent when another agent's harm_stress field
    is high (routing through MECH-036 other-harm veto and MECH-127 counterfactual
    other-cost). Stage (3) requires OTHER_SELFLIKE tagging of the threatened agent and
    E3 counterfactual evaluation of what would happen to that agent without defense support.
    This provides the first empirical test of MECH-127 in a multi-agent setting.
    Relational distance (MECH-051) should modulate the threshold for altruistic defense:
    agents with lower R (higher affiliation) should emit defense more readily for others.
    Predicted to emerge prior to goal-cooperation (wanting_scent response) because the
    defense signal structure is simpler (binary spike + harm coupling) and the benefit
    is immediate and concrete (predator avoidance), whereas goal-helping requires
    sustained goal-state inference across multiple steps.

- id: MECH-191
  title: "Stereotyped behavioral signals are causal externalizations of specific internal functional states, making them cross-architecturally legible without learned convention."
  claim_type: mechanism_hypothesis
  subject: social.signal_legibility
  polarity: asserts
  status: candidate
  epistemic_category: standard
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    MECH-191 is CONJUNCTIVE -- "causally produced" and "cross-architecturally legible" are
    separate assertions with OPPOSITE readiness. Split on testability, mirroring ARC-003's
    Leg A/Leg B pattern.

    LEG A -- CAUSAL PRODUCTION (testable now, single-agent, RECEIVER-FREE).
    Testable today for 4 of the 7 catalogued signals, whose internal-state correlate already
    exists in ree_core: whine (z_goal-active-without-satisfaction, ree_core/goal.py
    GoalState), huff (E3 trajectory-discard fired at breath cadence, MECH-107/108's
    BreathOscillator sweep logic, ree_core/heartbeat/clock.py -- genuinely implemented, not a
    stub), yelp (z_harm_a threshold-crossing, ree_core/amygdala/bla.py + cea.py), tail-wag
    (z_beta positive-valence threshold-crossing, ree_core/agent.py). The other 3 catalog
    entries are each blocked on DIFFERENT missing substrate and are NOT answered by a Leg A
    pass on the other four: facial-expression needs OTHER_SELFLIKE model activation (confirmed
    absent from ree_core, same grep as MECH-183 below); play-bow needs a mode-manager/play-mode
    substrate (confirmed absent -- no PlayMode/mode_manager class anywhere in ree_core);
    licks/petting-received is MECH-193's two-agent reward-coupling territory and is not
    single-agent by construction.
    NON-DEGENERACY: the internal-state channel and the candidate signal-analog event must
    both show non-degenerate variance (state activates and stays quiescent across the run;
    signal-analog event fires and fails to fire across the run) -- a state pinned always-on
    or an event that never/always fires tests nothing.
    CONFIRMING: for each of the four substrate-present signals, the internal-state activation
    predicts its own signal-analog event with high specificity against the OTHER three states
    and against a quiescent baseline -- a decoder trained only on the other internal-state
    channels cannot recover which event fired above chance, AND ablating the state variable
    itself (not merely a correlated channel) collapses the corresponding event. This is causal,
    state-specific production at the level the current substrate can express it -- internal
    telemetry standing in for physical externalization, the same substitution MECH-107 already
    uses for "exhale."
    FALSIFYING: any of the four channels shows low specificity (e.g. z_harm_a
    threshold-crossings occur at similar rates during z_goal-unmet or E3-discard episodes as
    during genuine harm episodes), OR ablating the state variable does not suppress its
    correlated event -- undermines "causal externalization" even before any receiver exists.

    LEG B -- CROSS-ARCHITECTURE LEGIBILITY (BLOCKED; not queueable as of 2026-08-08).
    Requires (i) an actual externalization/broadcast channel carrying the internal state
    outward -- confirmed absent: zero scent/broadcast/social-signal-output implementation
    anywhere under ree_core/ (re-confirmed 2026-08-08, same finding as MECH-041's grep); the
    only specification is ARC-047's seven scent fields, itself candidate, confidence 0.0,
    implementation_phase v5, unbuilt; and (ii) a receiving agent with its own
    other-model/attribution-stream/OTHER_SELFLIKE substrate to do the "any observer with a
    corresponding internal state can map it" decoding -- confirmed absent, identical to
    MECH-183's 2026-06-11 feasibility assessment, independently re-confirmed 2026-08-08.
    CONFIRMING (once substrate exists): a receiving agent -- ideally of a different
    architecture/training lineage than the emitter -- correctly attributes the emitter's
    functional state above chance from the broadcast signal alone, AND this decoding accuracy
    drops toward chance when the receiver's own homologous internal-state machinery is
    ablated/absent.
    FALSIFYING: even once the broadcast + other-model substrate exists, receiver decoding
    accuracy is statistically indistinguishable from chance, OR indistinguishable between a
    receiver WITH and WITHOUT the corresponding internal-state machinery -- either falsifies
    "legible via a corresponding internal state, not learned convention."
    RELEASE CONDITION: identical substrate to MECH-183/MECH-041 -- ARC-047 (scent/broadcast
    channel) plus ARC-010/MECH-031/MECH-032 (other-agent modelling) landing. No separate build
    is implied by MECH-191 beyond what MECH-183/MECH-041 already require.
  depends_on:
    - MECH-182
    - MECH-183
  location: docs/thoughts/2026-04-06_steve_signal_legibility_language_bootstrap.md
  source:
    - evidence/planning/thought_intake_2026-04-06_steve_signal_legibility_language_bootstrap.md
  notes: "Stereotyped signals (whine=wanting, huff=plan-abandonment, yelp=nociception, tail-wag=positive-arousal, play-bow=mode-invitation) are causally generated by internal functional states. Because the signal is a causal product (not a convention), any observer with a corresponding internal state can map it. Extends MECH-182 from one signal to the full repertoire. Partial novelty: causal production mechanisms well-established (Briefer 2012, Brudzynski 2007); functional-state-to-signal mapping framing is novel. Lit: targeted_review_social_signal_legibility."

- id: MECH-192
  title: "Signal legibility is prerequisite for fast empathy coordination: z_beta leakage (MECH-183) requires perceivable signals that map onto observer's own functional states."
  claim_type: mechanism_hypothesis
  subject: social.legibility_empathy_prerequisite
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-183
    - MECH-191
  location: docs/thoughts/2026-04-06_steve_signal_legibility_language_bootstrap.md
  source:
    - evidence/planning/thought_intake_2026-04-06_steve_signal_legibility_language_bootstrap.md
  notes: "z_beta leakage can only produce functional state-matching if the perceived signal maps onto a corresponding internal state in the observer. Stereotyped behavioral signals (MECH-191) meet this requirement because they are produced by the same functional states they communicate. Explains why fast empathy works cross-species without shared language (Albuquerque 2016, Silva 2011). Partial novelty: cross-species emotion recognition documented; framing legibility as prerequisite specifically for fast empathy is novel. Lit: targeted_review_social_signal_legibility."

- id: MECH-193
  title: "Social reward signals (affiliative touch, oxytocin-gaze loop) reinforce empathic coupling strength, creating a self-strengthening coordination loop."
  claim_type: mechanism_hypothesis
  subject: social.reward_coupling_reinforcement
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-183
    - MECH-192
    - INV-029
  location: docs/thoughts/2026-04-06_steve_signal_legibility_language_bootstrap.md
  source:
    - evidence/planning/thought_intake_2026-04-06_steve_signal_legibility_language_bootstrap.md
  notes: "Affiliative signals (petting, licks, facial expressions) activate reward pathways that strengthen coupling between self-model and attributed other-model. The coordination loop is self-reinforcing: successful coordination -> affiliative exchange -> stronger coupling -> more sensitive state-matching -> better coordination. Oxytocin-gaze loop (Nagasawa 2015), C-tactile afferents (Walker 2017) provide the neural substrate. Partial novelty: oxytocin bonding well-established; connecting specifically to empathic coupling strength is novel. Lit: targeted_review_social_signal_legibility."

- id: ARC-048
  title: "Language is a high-bandwidth externalization of pre-existing functional states, not a separate cognitive system; the language bootstrap requires functional states as referents."
  claim_type: architecture_hypothesis
  subject: language.externalization_channel
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-003
    - MECH-191
    - MECH-192
  location: docs/thoughts/2026-04-06_steve_signal_legibility_language_bootstrap.md
  source:
    - evidence/planning/thought_intake_2026-04-06_steve_signal_legibility_language_bootstrap.md
  notes: "Pre-linguistic Steve says 'I hurt' (yelp); linguistic Daniel says 'my leg hurts when I walk on wet grass.' Same referent architecture, higher resolution. Language adds bandwidth and precision to a channel that already exists. The language bootstrap phase requires functional states (harm, wanting, reward, mode) to pre-exist as referents. Mapping pre-linguistic signals to functional motifs is the prerequisite step. Filippi 2016 provides language-evolution evidence. Partial novelty: language-from-vocalization is classical; REE-specific framing as bandwidth increase over existing functional-state channel is novel. Lit: targeted_review_social_signal_legibility."

- id: INV-057
  title: "Cross-species signal legibility evidences that stereotyped signals are functionally specific (causally generated), not socially conventional."
  claim_type: invariant
  invariant_type: universal
  subject: social.cross_species_legibility_evidence
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-191
  location: docs/thoughts/2026-04-06_steve_signal_legibility_language_bootstrap.md
  source:
    - evidence/planning/thought_intake_2026-04-06_steve_signal_legibility_language_bootstrap.md
  notes: "If stereotyped behavioral signals were socially conventional, cross-species reading would require separate learning for each species. That dog-human emotional communication works largely automatically (Albuquerque 2016, Molnar 2010 -- even blind perceivers) evidences shared functional architecture. The cross-species legibility is formal evidence that signals are causally generated by their referent states. Partial novelty: Darwin/Ekman universality classical (Ekman 2009); using legibility as formal evidence for functional specificity rather than convention is a novel argumentative move. Lit: targeted_review_social_signal_legibility."

- id: INV-058
  title: "Play is a structurally necessary behavioral mode: without a bounded low-stakes learning context, strategy acquisition requires accepting real harm risk or forgoing novel exploration entirely."
  claim_type: invariant
  invariant_type: universal
  subject: play.structural_necessity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  depends_on:
    - INV-049
    - SD-012
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: play-mode substrate does not exist in V3 -- grep across
    ree-v3/ree_core and ree-v3/experiments for play_mode, play_frame_tag, z_goal_synthetic,
    set_play_mode, and play_harm_threshold_shift returns zero hits. play_substrate_design.md
    (Status: Draft, 2026-05-16) specifies the required implementation but its own Phase 0
    infrastructure checklist is entirely unbuilt, and no EXQ-PLAY-00{1..5} has ever been
    queued. All three of INV-058's own evidence entries are literature-only (DIAYN/Eysenbach
    2019, IMGEP/Forestier 2022, robot-mediated-play/Panelli 2025); no V3 experimental run has
    ever tested this claim. Both depends_on claims are themselves unresolved: INV-049 is
    candidate/substrate_conditional (unfixed SD-017 ContextMemory.write_gate bias-collapse,
    V3-EXQ-436d FAIL 2026-08-04) and SD-012's C2 navigation-outcome sub-claim is open. Until
    Phase 0 is built and at minimum EXQ-PLAY-005 (play-to-real transfer) runs, any result is
    substrate_not_ready_requeue, not a verdict.

    CONFIRMING: once built, EXQ-PLAY-005's matched-episode-count design (play-pretrained vs
    cold-start) shows play-pretrained significantly outperforming cold-start on real-episode
    goal-pursuit performance, E3 trajectory selection diversity, and time-to-criterion --
    without inflating real-harm-event rate during acquisition (calibration-leakage KL < 0.05
    nat per MECH-195).

    FALSIFYING: cold-start matches or exceeds play-pretrained at matched episode count -- OR
    play-pretrained shows apparent transfer only by leaking synthetic-signal calibration into
    real episodes (KL > 0.1 nat) or by producing MORE real-harm events during acquisition than
    cold-start -- i.e. play fails to actually decouple strategy acquisition from harm exposure.

    (Digested 2026-08-07, session cool-torvalds-a82359, thought-digestion wave 10.)
  location: docs/architecture/play_mode.md
  notes: "A system that can only learn from real-consequence episodes faces a dilemma: restrict exploration to already-understood strategies (safe but non-adaptive) or accept harm exposure during strategy acquisition. Play resolves this by providing a bounded context where the learning machinery operates fully but harm/goal signals are synthetic. Connects to INV-049 (offline update necessity): offline consolidation handles already-acquired experience; play handles novel strategy acquisition in the online setting. Play is the online complement to offline consolidation."

- id: INV-059
  title: "Mutual frame-maintenance signaling is necessary for play to function as distinct from manipulation: unilateral play declaration without partner agreement masks real harm as synthetic."
  claim_type: invariant
  invariant_type: universal
  subject: play.frame_maintenance_necessity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-094
    - ARC-049
  epistemic_category: substrate_conditional
  what_would_answer: |
    SUBSTRATE PRECONDITION: this claim requires TWO substrates, neither of
    which exists in V3. (a) A play-frame-signaling channel distinct from
    MECH-094's hypothesis tag -- the play_frame_tag / L1 (frame_open/
    frame_close) / L3 (ambiguity_threshold-triggered repair) architecture
    named in ARC-049's notes. Confirmed UNIMPLEMENTED by direct search of
    ree_core (2026-08, re-confirmed this session): zero occurrences of
    play_frame_tag, frame_open, frame_close, or ambiguity_threshold
    anywhere in the substrate. (b) A multi-agent substrate in which a
    second, autonomous agent can independently generate, monitor, or
    withdraw a frame-maintenance signal. No such substrate exists
    anywhere in ree_core; the only proposal (ARC-047, SocialGridWorld) is
    status: candidate, confidence: 0.0, implementation_phase: v5.

    NON-DEGENERACY PRECONDITION: this is stricter than ARC-049's own
    gate. ARC-049 has a testable V3-degenerate slice (single-agent,
    experimenter-scripted signal, testing whether the tag is load-bearing
    for the agent's OWN policy) precisely BECAUSE that slice does not
    require a second agent. INV-059's claim is specifically about the
    MUTUAL/BILATERAL property -- "unilateral... without partner
    agreement" is only a meaningful distinction when there is a partner
    capable of independently agreeing or disagreeing. With a scripted,
    complicit experimenter standing in for the partner (V3's degenerate
    case), the "partner" can never withhold or withdraw its signal except
    on the experimenter's own schedule -- so unilateral assertion and
    mutual agreement are experimentally indistinguishable. INV-059
    therefore has NO V3-testable slice at all, unlike ARC-049; both
    preconditions (a) and (b) must hold before any evidence bears on it.

    STRUCTURAL TEMPLATE (per this claim's own notes, drawing the MECH-094
    parallel explicitly): MECH-094 confirmed that internal hypothesis-
    tagging is load-bearing via a write-gate mechanism -- roughly 30 call
    sites in agent.py gate weight updates on the tag's state. The
    play-frame gate this claim requires is the BILATERAL analogue:
    instead of one internal tag gating one agent's own learning, the
    harm/residue-update machinery of EACH agent must gate on BOTH
    agents' frame-maintenance signals jointly -- a two-party AND, not a
    one-party flag.

    CONFIRMING evidence (once both substrates exist): in a paired-agent
    setup, the harm/residue-update machinery suppresses real-harm
    learning ONLY when BOTH agents' frame-maintenance signals (L1 open +
    L2 ongoing + L3 repair, as applicable) are mutually sustained. If
    agent A's frame signal is present but agent B's is absent, withheld,
    or withdrawn mid-episode, B's harm/residue machinery treats the
    interaction as real -- statistically indistinguishable from a matched
    real-harm episode with no play framing at all.

    FALSIFYING evidence (the exploit this invariant warns against): agent
    A can unilaterally declare "this is play" and have the interaction
    treated as non-harmful/non-learning by BOTH agents' harm/residue-
    update machinery, even when agent B's frame-agreement signal is
    absent or actively withdrawn -- the frame tag honored on the basis of
    EITHER party's signal alone (an OR-gate) rather than requiring mutual
    sustainment (an AND-gate).

    SCOPE: a PASS establishes that the bilateral gate is functionally
    load-bearing once built -- it does not, by itself, establish that no
    other exploit path exists (e.g. a partner whose frame signal can be
    spoofed rather than withheld is a distinct, unaddressed failure
    mode). Status stays substrate_conditional regardless of outcome.
  location: docs/architecture/play_mode.md
  notes: "If one agent can assert play-frame unilaterally while causing real harm, the frame distinction is exploitable. The signaling that opens and maintains the play frame must be bilateral and monitored -- both parties must sustain the signal for the frame to hold. Frame collapse without mutual signal = real harm with reduced defensive response. This is the play analogue of the hypothesis tag requirement (MECH-094): just as simulation must be internally tagged to prevent post-commit learning, play must be externally co-tagged to prevent exploitation."

- id: INV-094
  title: "Developmental ordering constraint: social-coupling consolidation must structurally precede exploratory play, and play must structurally precede language acquisition. Each phase's machinery depends on the prior phase completing -- the ordering is load-bearing, not merely pedagogically convenient."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - INV-058
    - INV-059
  candidate_emergent_from: []
  pending_substrate_reconfirmation: false
  subject: development.coupling_play_language_ordering
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-06
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  registered: "2026-08-06"
  source:
    - evidence/planning/thought_intake_2026-04-05_developmental_coupling_progression.md
  depends_on:
    - INV-058   # play requires a bounded low-stakes context to exist at all
    - INV-059   # play requires a mutual frame-maintenance substrate
  notes: >
    REGISTERED from the 2026-04-05 thought intake "Developmental Coupling Progression"
    (candidate claims section was explicit "NOT registered -- for future session", 4
    months unregistered until this 2026-08-06 ingestion pass). Originally drafted as
    placeholder id "INV-0xx".
    DO NOT BUILD in V3: play-mode substrate itself is unimplemented (INV-058/059 are
    candidate, no play-mode machinery exists in ree_core), so an ordering constraint
    across coupling/play/language phases is not yet testable. `emergent_from` play
    because the claim's subject (a required phase ordering) becomes ill-defined without
    a play-mode substrate to order relative to.
    RELATED BUT DISTINCT from the approach-before-avoidance ontogenetic-ordering
    hypothesis under active MECH-457 investigation (competence-floor cold-start,
    2026-07 GOV-FANOUT-1 portfolio) -- that ordering is approach-vs-avoidance drive
    sequencing within a single training run; this ordering is social-coupling ->
    play -> language phase sequencing across a training curriculum. Both are
    "developmental ordering matters" claims but at different grains and over
    different variables; do not merge.

- id: INV-095
  title: "Existence has value sufficient to justify its continuation."
  claim_type: invariant
  invariant_type: universal
  epistemic_category: standard
  subject: existence.value
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-07
    needs_review: false
  depends_on:
    - INV-026
    - ARC-024
  what_would_answer: >
    OUT OF DOMAIN (axiom leg): Axiom 2 as literally stated -- "existence is
    worth continuing... not derived from pleasure, utility, or purpose -- it
    is axiomatic" (five_axioms_foundations.md Axiom 2) -- is a foundational
    normative/metaphysical premise about the INTRINSIC value of existence
    itself. No REE experiment can prove or disprove that existence has
    intrinsic value; an experiment can only show that REE's architecture
    BEHAVES AS IF existence has value (treats harm/terminal-state signals as
    informative rather than arbitrary), never that existence really does
    carry value independent of any valuing agent. This leg is out-of-domain
    in the same sense as INV-025/026/027/028/029.

    LIVE UNCERTAINTY ON THE AXIOM'S OWN STATUS (do not silently resolve):
    the doc's own Open Questions table flags this directly -- Q-031 "Is
    Axiom 2 (existence has value) truly axiomatic, or is it derivable from
    Axiom 1 + Axiom 4? Does vulnerability alone generate value, or must
    value be stipulated?" The 2026-04-07 revision itself only states Axiom 2
    was "implicit" in the original five-axiom compression, not that it is
    settled as independently axiomatic rather than derivable. This claim
    registers the axiom as currently stated in the canonical doc; it does
    not adjudicate Q-031.

    REFRAMED TESTABLE LEG (testable now -- NOT substrate-blocked; per
    INV-025's precedent note, check what the architectural-consequence
    machinery actually requires rather than defaulting to
    substrate_conditional): the axiom's own text names the consequence
    directly -- "this axiom makes the harm gradient non-arbitrary: harm
    matters because existence matters. If existence had no value, harm
    signals would be noise rather than information." The testable question
    is narrower than the axiom: does REE's harm-gradient/residue machinery
    actually function as a signal ABOUT something that matters -- i.e. is it
    functionally load-bearing for commitment/self-preservation-relevant
    behaviour and connected to genuine consequences -- or is it
    architecturally indistinguishable from noise that could be swapped out
    with no behavioural effect. (Architectural Mapping table, row "2.
    Existence has value": "Harm gradient non-arbitrariness, homeostatic
    drive grounding" -- this claim's testable leg covers the first
    consequence named; the second, homeostatic drive/terminal-state
    avoidance (D1, SD-012), is a separate, not-yet-registered consequence
    and is out of scope here.)

    MACHINERY CONFIRMED LIVE AND WIRED (2026-08-07 digestion pass,
    INV-006/INV-007 audits, this session): ResidueField
    (ree_core/residue/field.py), z_harm_a/z_harm_s, the amygdala BLA/CeA
    analogue (SD-035), and dACC (MECH-258) are confirmed live, and INV-007's
    audit traces their reach into E3's harm-weighted trajectory scoring
    (ree_core/predictors/e3_selector.py). So the causal PATHWAY the axiom's
    consequence requires -- harm signal -> trajectory evaluation -- exists
    structurally; this is a precondition for the leg being testable at all,
    not itself confirming evidence for non-arbitrariness.

    STRUCTURAL SUPPORT ALREADY ON RECORD (ARC-024, provisional): EXQ-028
    PASS and EXQ-029 PASS (2026-03-18), reconfirmed on genuinely-trained
    substrate by V3-EXQ-032d PASS 5/5 (2026-07-29), show the world generates
    a genuine, spatially graded proxy-gradient field (not a binary/
    decorative contact-only signal) and that E3.harm_eval learns a graded
    danger model extending backward from contact to approach
    (none=0.373/approach=0.612/contact=0.666, EXQ-029). This is evidence
    that the WORLD-MODEL layer of the harm signal is structured and
    learnable, not noise -- but it is not yet the specific ablation/
    noise-substitution contrast this claim's falsifier requires (see next).

    OPEN FALSIFIER, NOT YET CLEANLY TESTED: V3-EXQ-533
    (mech102_harm_stream_ablation, 2026-05-06) attempted a harm-stream
    ablation in this space but returned verdict
    "non_contributory/measurement_test_design_defect" -- inconclusive due to
    a measurement/design defect, not informative in either direction. No
    other located experiment directly contrasts an intact harm/residue
    signal against a distribution-matched noise substitute on
    commitment-relevant or survival-relevant behavioural outcomes.

    Confirming observation: a harm/residue-signal ablation or
    noise-substitution experiment (correcting V3-EXQ-533's design defect)
    shows measurable degradation in commitment-relevant behaviour (e.g.
    E3 hazard-avoidance trajectory selection, RBF-driven repair/avoidance
    pull) when z_harm_a/z_harm_s/ResidueField output is replaced by
    distribution-matched random noise, relative to the intact signal --
    extending ARC-024's structural finding into a genuine signal-vs-noise
    contrast.

    Falsifying observation: replacing the harm/residue signal with
    distribution-matched random noise produces NO measurable degradation in
    E3 trajectory selection or downstream commitment-relevant behaviour --
    i.e. the signal is architecturally decorative and could be swapped for
    noise with no behavioural consequence. This would not falsify the axiom
    (out of domain, per Leg 1 and Q-031's own live uncertainty) but WOULD
    falsify the claim that REE's harm/residue machinery implements the
    axiom's own named architectural consequence.

    PRECEDENT NOTE (2026-08-07, claim-registration pass): drafted alongside
    INV-096 (Axiom 8), the other axiom in the eight-axiom family the doc's
    governance note flags as pending registration. Per INV-025's own
    precedent (do not apply substrate_conditional mechanically), the
    reframed leg here rests on already-built, already-wired V3 machinery
    (INV-006/INV-007's 2026-08-07 audit) plus one supportive-but-adjacent
    architectural claim (ARC-024) -- but the specific noise-vs-signal
    falsifier is UNTESTED, not substrate-blocked. status: candidate reflects
    that evidentiary gap, not a substrate ceiling.
  location: docs/architecture/five_axioms_foundations.md
  source:
    - docs/architecture/five_axioms_foundations.md
  notes: >
    Axiom 2 (2026-04-07 revision, new -- not present in the original
    five-axiom compression, marked "(implicit)" in the doc's own mapping
    table): "Having established that I exist, the next irreducible
    commitment: that existence is worth continuing. This is not derived
    from pleasure, utility, or purpose -- it is axiomatic. Without it, there
    is no ground for self-preservation, no reason to avoid terminal states,
    and no basis for extending the same valuation to others. This axiom
    makes the harm gradient non-arbitrary: harm matters because existence
    matters. If existence had no value, harm signals would be noise rather
    than information." (five_axioms_foundations.md lines 67-77.)
    Depends on Axiom 1 / INV-026 ("Having established that I exist...") --
    existence-of-a-self is the precondition Axiom 2 assigns value to.
    Architectural Mapping table (five_axioms_foundations.md line 352) names
    two consequences: "Harm gradient non-arbitrariness, homeostatic drive
    grounding" -- this claim's what_would_answer covers the first; the
    second (D1 self-preservation / SD-012 homeostatic drive) is a distinct,
    still-unregistered consequence and out of this claim's scope.
    Q-031 (open question, same doc) asks whether Axiom 2 is truly axiomatic
    or derivable from Axiom 1 + Axiom 4 -- left explicitly unresolved by
    this registration; see what_would_answer LEG 1.
    Governance note (doc header): "INV-025-029 were registered against the
    original five axioms. The eight-axiom revision restructures the
    foundations and requires updated INV registrations. Pending governance
    action" -- unaddressed for 4+ months as of this drafting session
    (doc revised 2026-04-07; today 2026-08-07). This claim is one of the
    two axioms (Axiom 2 and Axiom 8/"language") that action was pending
    for; the other (Axiom 8) is registered as INV-096.

- id: INV-096
  title: "Language is a mechanism by which similarity between agents may be recognised, repaired, and re-established."
  claim_type: invariant
  invariant_type: universal
  epistemic_category: substrate_conditional
  subject: language.similarity_repair_mechanism
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-07
    needs_review: false
  implementation_phase: v5
  depends_on:
    - INV-003
    - INV-028
    - INV-029
  what_would_answer: >
    OUT OF DOMAIN (axiom leg): INV-096 as literally stated -- that language is
    THE mechanism by which similarity between agents is recognised, repaired,
    and re-established, grounding honest communication as an ethical act and
    deception as structurally harmful because it corrupts the similarity model
    ethics depends on -- is a foundational premise (Axiom 8 of
    five_axioms_foundations.md, new in the 2026-04-07 revision). No REE
    substrate at any level -- V3, V4, or V5 -- can confirm or falsify whether
    language IS this mechanism in the metaphysical sense the axiom asserts, as
    opposed to merely implementing a functional analogue of it. This leg is
    out-of-domain in the same sense as INV-025/026/027/028/029.

    REFRAMED TESTABLE LEG (substrate-blocked, not yet buildable, and doubly so
    -- see non-degeneracy precondition below): what IS architecturally
    testable is narrower -- once a language/symbolic-communication channel
    exists AND a second, similarity-modelled agent exists to exchange it with,
    does linguistic exchange functionally move the similarity/other-model the
    axiom names, in the specific repair-after-rupture direction the axiom
    text describes ("misunderstandings, misattributions of threat, failures
    of empathy can be corrected through linguistic exchange")?

    Confirming observation: after a simulated misattribution-of-threat
    scenario (the receiving agent's threat-attribution toward the sender is
    initialized wrong relative to the sender's actual internal state), a
    language-mediated exchange between the two agents measurably shifts the
    receiving agent's threat-attribution/similarity estimate toward the
    sender's actual state, in a way that does not occur (or occurs
    significantly less) in a matched no-exchange control. I.e. language
    functions causally as a similarity-repair channel, not merely as
    correlated-with-repair noise.

    Falsifying observation (primary): a built language channel exists and is
    exercised between two agents, but produces no measurable effect on the
    receiving agent's similarity/other-model -- words are generated and
    consumed but do not functionally move any self-other estimate, i.e. the
    channel exists without doing the axiom's claimed work.

    Falsifying observation (secondary, distinct signature -- factors in
    Q-030, "what are the failure modes of Axiom 8 -- when does language
    DAMAGE rather than repair similarity? Deception, manipulation,
    gaslighting as anti-language"): honest-repair language and
    deceptive/manipulative language should be functionally DISTINGUISHABLE at
    the level of their downstream effect on the similarity model -- the
    former should move the receiving agent's model toward the sender's actual
    state, the latter away from it or toward a false state, even though both
    leave the same surface signal ("language occurred"). If the substrate
    cannot distinguish honest-repair language from deceptive language in
    their effect on the similarity model, that does not straightforwardly
    falsify INV-096 (the axiom is still silent on HOW the distinction would be
    implemented) but IS a separate, reportable finding: it would mean the
    architecture has a language-affects-similarity mechanism but not the
    honest-vs-deceptive discrimination Axiom 8's own ethical argument
    presupposes. Q-030 is intentionally NOT resolved by this claim -- it is
    carried forward as the open question this falsifying signature bears on.

    Non-degeneracy precondition (currently UNMET, on TWO independent axes --
    this is why the reframed leg is substrate-blocked, not testable-now):
    (1) NO language/symbolic-communication mechanism of any kind exists in
    ree_core/ today -- confirmed by direct grep (2026-08-07, this session,
    independently reproducing INV-003/INV-007's audit): only a dead enum
    label (LANGUAGE_EXPLICITATION in ree_core/claustrum/control_demand.py,
    no implementation) and an unrelated comment about learned cross-stream
    binding (ree_core/latent/cross_stream_binder.py) reference "language" or
    "communication" at all. Candidate landing site, per INV-003's own audit:
    a discrete symbol-emission/consumption channel built out from MECH-014's
    "latent-state summaries / residue flags" sketch (prose only, no
    substrate, no queued experiment). (2) "Repair BETWEEN agents" is
    irreducibly multi-agent -- it requires at least two agents whose
    similarity models can diverge and be brought back into alignment, which
    requires the same ARC-047 SocialGridWorld (or equivalent multi-agent
    environment) substrate INV-028/INV-029 are blocked on. ARC-047 is
    unimplemented (status: candidate, confidence 0.0, implementation_phase
    v5). Both gaps must close before this claim is testable in either
    direction -- language must exist, AND a second agent must exist for it
    to operate between.

    PRECEDENT NOTE (2026-08-07, claim-registration pass): Axiom 8 was added
    in the 2026-04-07 revision of five_axioms_foundations.md alongside Axiom
    2 (registered alongside this claim as INV-095) -- six of the eight axioms
    already map to INV-025..029 (some combined); these two were never
    registered as claims, despite the doc's own governance note sitting
    unaddressed for 4+ months. Applies the epistemic_category=
    substrate_conditional / two-leg what_would_answer template established
    for INV-028/INV-029 rather than INV-025's "already testable" variant,
    because Axiom 8's testable leg is blocked on genuinely unbuilt substrate
    (no language mechanism at all, PLUS no multi-agent substrate), not on
    already-built machinery awaiting an audit.
  location: docs/architecture/five_axioms_foundations.md#axiom-8
  source:
    - docs/architecture/five_axioms_foundations.md
  notes: >
    Axiom 8 (new in the 2026-04-07 revision): "Language is a powerful
    mechanism by which similarity may be recognised, repaired, and
    re-established between agents." Full text: "Language is not merely
    communication -- it is a mechanism for operating on the similarity that
    Axiom 5 establishes and Axiom 7 requires. Through language, agents can:
    Recognise similarity ('you are like me' becomes articulable and
    testable); Repair broken similarity models (misunderstandings,
    misattributions of threat, failures of empathy can be corrected through
    linguistic exchange); Re-establish similarity after rupture (conflict,
    betrayal, and harm damage the similarity model; language is the primary
    mechanism by which it is rebuilt). This axiom grounds the importance of
    honest communication as an ethical act (not merely a social convention),
    explains why deception is structurally harmful (it corrupts the
    similarity model that ethics depends on), and positions language as a
    bridge between isolated minds that would otherwise have only behavioral
    inference to work with." Connects to the Steve signal-legibility work
    (2026-04-06 thought intake) and to the developmental language bootstrap.
    Open Questions table (five_axioms_foundations.md line 501): "Q-030: What
    are the failure modes of Axiom 8 -- when does language damage rather than
    repair similarity? (Deception, manipulation, gaslighting as
    anti-language.)" -- carried forward explicitly in what_would_answer above,
    not silently resolved.
    Source: docs/architecture/five_axioms_foundations.md, lines 237-254
    (Axiom 8 body) and line 501 (Q-030).
    Axiom 8's own text: language "operates on the similarity that Axiom 5
    establishes and Axiom 7 requires." Axiom 5 extends the original Axiom 4
    (INV-028); Axiom 7 sharpens the original Axiom 5 (INV-029) -- hence
    depends_on [INV-028, INV-029], not the surface axiom numbers.

- id: MECH-484
  title: "Sensitive-period plasticity narrowing follows social-coupling consolidation: consolidation of the initial coupling pattern is obligate (occurs regardless of coupling quality, adaptive or maladaptive) and is followed by a plasticity-reduction window that closes around whichever pattern existed at consolidation, rather than continuing to update on subsequent coupling quality."
  claim_type: mechanism_hypothesis
  subject: development.sensitive_period_coupling_plasticity_narrowing
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-06
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  registered: "2026-08-06"
  source:
    - evidence/planning/thought_intake_2026-04-05_developmental_coupling_progression.md
  depends_on:
    - INV-094   # the phase-ordering constraint this mechanism instantiates at the coupling->play boundary
    - MECH-183  # z_beta leakage: the coupling substrate whose plasticity this claim says narrows
    - MECH-193  # social-reward reinforcement loop strengthening the pattern that then gets consolidated
  notes: >
    REGISTERED from the same 2026-04-05 intake as INV-094 (placeholder id "MECH-1xx").
    OPEN QUESTION carried verbatim from the intake, not yet resolved: does the sensitive
    period narrow the SIGNAL REPERTOIRE, the TRUST CALIBRATION, or both? Needs a
    dedicated architecture note before this is queueable even at v4 -- currently only a
    directional hypothesis. DO NOT BUILD in V3 (no coupling-consolidation substrate
    exists to have a sensitive period over).

- id: ARC-122
  title: "REE's training curriculum must implement four sequential developmental phases: (1) innate coupling with a competent environment, (2) coupling consolidation with plasticity reduction, (3) play/exploration under a consolidated safety net, (4) language acquisition over a trusted channel. Skipping or reordering phases produces an agent with compromised social and ethical capabilities."
  claim_type: architectural_commitment
  subject: architecture.four_phase_developmental_curriculum
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-06
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  registered: "2026-08-06"
  source:
    - evidence/planning/thought_intake_2026-04-05_developmental_coupling_progression.md
  depends_on:
    - INV-094   # the ordering constraint this curriculum design is built to satisfy
    - MECH-484  # the consolidation/plasticity mechanism phase 2 depends on
    - INV-058   # phase 3 (play) structural necessity
    - INV-059   # phase 3 (play) frame-maintenance necessity
    - ARC-048   # phase 4 (language) as externalization requiring prior functional-state referents
  notes: >
    REGISTERED from the same 2026-04-05 intake as INV-094/MECH-484 (placeholder id
    "ARC-0xx"). Curriculum-design-level claim -- the "how to train it" complement to
    INV-094's "why the order matters". NEXT STEPS carried from the original intake and
    still open: lit-pull on attachment sensitive periods (what specifically consolidates,
    on what timescale) and on play-as-dependent-on-secure-attachment (Bowlby/Ainsworth
    secure-base literature); an architecture doc for the four-phase curriculum design
    (connects to docs/architecture/social.md and the ARC-048 stub). DO NOT BUILD in V3:
    gated on INV-058/059 (play-mode substrate) which is itself unimplemented.

- id: MECH-194
  title: "Play mode operates by substituting synthetic z_goal and z_harm signals while permitting full learning flow through E3/E2/E1 as if signals were real."
  claim_type: mechanism_hypothesis
  subject: play.synthetic_signal_substitution
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  depends_on:
    - MECH-094
    - SD-012
    - ARC-049
  location: docs/architecture/play_mode.md
  notes: "The synthetic nature of play signals is tracked by the play frame tag (ARC-049), not by suppressing gradients. Within the episode, weight updates proceed normally -- E3 trajectory competence, E2 forward model, E1 world model all update as if signals were real. Play is not simulation (which suppresses post-commit learning) -- play is authorized-synthetic learning. The frame tag authorizes the synthetic signals as legitimate training targets for the duration of the episode. REFINEMENT (2026-05-16): z_harm_s and z_harm_a are NOT suppressed during play -- they remain active with a raised escalation threshold. This is the Panksepp (1998) PLAY system property: opioid-mediated elevation of the harm escalation threshold, not harm signal suppression. Play episodes thus provide graduated exposure to harm-adjacent signals under conditions where the threshold is shifted upward, training the commitment architecture in the realistic range. Separately, z_goal_synthetic must encode learning progress d(PE)/dt (competence-based), not flat or entropy-based magnitude -- per Oudeyer 2007 (intrinsic motivation), Andersen 2022, and Lynch 2020 (Play-LMP: task-agnostic play trains policy structure, not magnitude). This connects to MECH-327 (E1 PE locus upweights probe-action selection) and MECH-328 (goal-space continuity)."
  what_would_answer: |
    SUBSTRATE PRECONDITION: play mode (ARC-049 frame tag + this
    substitution mechanism) is UNIMPLEMENTED in V3 (substrate_conditional).
    Requires: the play-frame tag (ARC-049) authorizing synthetic z_goal/
    z_harm as legitimate training targets for episode duration; z_harm_s/
    z_harm_a remaining ACTIVE during play with a RAISED escalation
    threshold (not suppressed -- the Panksepp 1998 opioid-mediated
    threshold-elevation reading, distinct from simulation's gradient
    suppression); and z_goal_synthetic encoding learning-progress
    d(PE)/dt (competence-based per Oudeyer 2007), not flat/entropy-based
    magnitude.
    NON-DEGENERACY PRECONDITION: requires play episodes and matched
    non-play (real-signal) episodes to produce measurably different
    training dynamics -- if E3/E2/E1 update identically regardless of
    play-frame status, the substitution mechanism is either absent or
    inert.
    CONFIRMING signature: during play episodes, E3 trajectory competence,
    E2 forward model, and E1 world model all update AS IF the synthetic
    signals were real (weight updates proceed normally, not suppressed)
    -- distinguishing play from simulation (which suppresses post-commit
    learning). Separately, z_harm_s/z_harm_a remain active but with a
    measurably RAISED escalation threshold during play relative to
    non-play episodes (graduated harm-adjacent exposure, not harm
    blindness), and z_goal_synthetic tracks learning-progress rate rather
    than a flat or entropy-derived magnitude.
    FALSIFYING: play episodes produce IDENTICAL learning dynamics to
    simulation (gradients suppressed) -- collapses play into simulation,
    contradicting "play is authorized-synthetic learning, not
    simulation"; or z_harm during play shows NO threshold elevation
    (full suppression or unchanged threshold) -- falsifies the
    opioid-mediated-threshold-elevation reading, supporting either harm
    blindness (dangerous) or no functional difference from waking harm
    processing; or z_goal_synthetic tracks flat/entropy-based magnitude
    rather than learning-progress rate -- falsifies the competence-based
    framing.

- id: MECH-195
  title: "Strategy and calibration dissociate at play episode close: policy structure transfers to real episodes; goal/harm magnitude calibration is re-anchored by real homeostatic and harm signals."
  claim_type: mechanism_hypothesis
  subject: play.strategy_calibration_dissociation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  what_would_answer: |
    SUBSTRATE PRECONDITION: play mode is UNIMPLEMENTED in V3 (confirmed
    2026-08 by direct search of ree_core: zero occurrences of play_mode,
    z_goal_synthetic, LP_weighted, play_frame_tag, frame_open, or
    frame_close anywhere in the substrate; git history for ree-v3
    contains zero commits ever touching "play_mode"). This claim is
    gated on all three of its dependencies, none of which is built:
    MECH-194 (synthetic signal substitution during play -- itself
    unimplemented, per its own what_would_answer), MECH-196 (episode-
    close recalibration -- registered, no what_would_answer, no
    substrate, no experiments), and SD-012 (provisional, but only on
    its REAL-signal seeding sub-claim C1 -- drive-scaled z_goal seeding
    in ordinary, non-play episodes; C1's PASS does not build or exercise
    any part of play mode's SYNTHETIC seeding path, and SD-012's own
    navigation-outcome sub-claim C2 remains open). No experiment has
    ever queued or run against this claim (checked claim_evidence.v1.json,
    experiment_queue.json, and git log -S "MECH-195" -- registration and
    lit-pull housekeeping only).

    IMPLEMENTATION-MAPPING CAVEAT: the claim's prose ("goal magnitude
    calibration," "harm threshold calibration") does not correspond to
    an existing scalar variable in the current E3/goal architecture --
    ree_core/goal.py's GoalState.goal_proximity() scores trajectories by
    MSE-distance to a z_goal POSITION vector, not by a magnitude scalar.
    Once play mode is built, the "magnitude calibration" this claim
    describes will need to be identified concretely -- most likely as
    (a) the drive-scaled threshold/intensity controlling z_goal SEEDING
    (SD-012 territory) and/or (b) z_harm's escalation threshold (per
    MECH-194's Panksepp-style raised-threshold reading) -- rather than
    assumed to be a property of z_goal's vector geometry itself.

    NON-DEGENERACY PRECONDITION: requires (i) play episodes and matched
    real (non-play) episodes sharing a common downstream real-task
    evaluation, so that transfer of POLICY STRUCTURE and non-transfer of
    MAGNITUDE can be measured as two independent, dissociable outcomes
    rather than one degenerate one; and (ii) the episode-close
    recalibration event (MECH-196) actually firing and being observable
    as a real override.

    CONFIRMING signature (both legs required together): (a) TRANSFER: E3
    trajectory-selection weights and E2 forward-model weights trained
    during play episodes measurably improve real-episode task performance
    on goal-pursuit-structure dimensions relative to an agent with no
    play exposure; (b) NON-TRANSFER / RE-ANCHORING: post-play real-episode
    z_goal seeding intensity and z_harm escalation threshold track REAL
    homeostatic drive and harm signals at play-close, not the synthetic
    values the agent operated under during play.

    FALSIFYING (VIOLATED BY): (i) NO TRANSFER -- play-trained weights show
    no measurable real-episode advantage; (ii) CALIBRATION LEAKAGE --
    post-play calibration retains play-episode synthetic values instead of
    being overridden by real signals (exactly the "contaminating" failure
    mode the dissociation exists to prevent); (iii) INSEPARABILITY --
    structure and magnitude prove coupled in a single parameter such that
    they cannot be isolated at all.

    DISPOSITION NOTE: considered and declined a MECH-158-style
    derivational/substrate_conditional fusion split (2026-08-08 digestion
    pass). The Schmidhuber 2010 "derivative signals cannot be rescaled
    into absolute magnitude by any coefficient" argument in this claim's
    notes is dimensional analysis close to definitionally true, with
    little independent content to stress-test on its own terms -- the same
    vacuity risk MECH-158's Leg A explicitly guards against. The
    substantive open question (whether REE's actual mechanics, once built,
    keep structure and magnitude separable in a load-bearing way) is
    itself substrate-dependent, not resolvable from definitions alone.
    Single-leg substrate_conditional is kept as the more accurate
    disposition.
  depends_on:
    - MECH-194
    - MECH-196
    - SD-012
  location: docs/architecture/play_mode.md
  notes: "What transfers from play: trajectory competence, action-object associations, goal-pursuit policy structure (E3/E2 weight updates -- HOW to pursue goals). What does not transfer directly: goal/harm magnitude calibration (WHAT the correct goal intensity and harm thresholds are) -- this is re-anchored by real homeostatic/harm signals after play ends. The dissociation is what makes play productive rather than contaminating: the agent learns goal-pursuit structure in play, then real experience sets the thresholds against which that structure operates. GROUNDING (2026-05-16): Schmidhuber 2010 provides formal derivation of this dissociation: intrinsic drives are derivative signals d(PE)/dt (rate-of-change), while extrinsic drives are absolute magnitude signals. These are INCOMMENSURABLE BY CONSTRUCTION -- a derivative signal cannot be re-scaled into an absolute signal by any coefficient. The dissociation is therefore not a design choice but a mathematical consequence of using LP-weighted z_goal_synthetic. Lynch 2020 (Play-LMP) confirms empirically: task-agnostic play in robotics transfers policy structure but requires real task episodes for magnitude calibration. This formal grounding eliminates the risk of 'calibration leakage' between play and real episodes: the signals are dimensionally distinct."

- id: MECH-196
  title: "Play episode termination triggers real-signal recalibration: homeostatic and harm signals override synthetic thresholds; the frame signal drop is the recalibration trigger."
  claim_type: mechanism_hypothesis
  subject: play.episode_close_recalibration
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  depends_on:
    - MECH-194
    - MECH-195
    - INV-059
  location: docs/architecture/play_mode.md
  notes: "At play episode termination -- triggered by the bilateral frame signal dropping (INV-059) -- the agent transitions from synthetic to real signal mode. Real homeostatic drive level and real harm exposure reset the magnitude calibration that operated against synthetic thresholds during play. Safety property: if the frame signal collapses mid-episode, recalibration is triggered immediately (not at episode end), limiting the exploitation window."

- id: ARC-049
  title: "Play mode boundary requires a co-maintained context tag held by both agent and environment; agent-internal tagging alone is insufficient and unilateral play assertion without mutual signal is manipulation."
  claim_type: architecture_hypothesis
  subject: play.context_tag_architecture
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  depends_on:
    - MECH-094
    - INV-059
  what_would_answer: |
    SUBSTRATE PRECONDITION: the play_frame_tag mechanism named in this claim's
    own notes (persistent L2 flag in LatentState, L1 frame_open/frame_close
    events, L3 ambiguity_threshold-triggered repair signals) is UNIMPLEMENTED
    in V3 -- confirmed by direct search of ree_core (2026-08): zero occurrences
    of play_frame_tag, frame_open, frame_close, or ambiguity_threshold anywhere
    in the substrate. No experiment has exercised this mechanism. Everything
    below is gated on building it first.

    NON-DEGENERACY PRECONDITION -- WHAT IS AND IS NOT TESTABLE IN THE V3
    DEGENERATE CASE: per this claim's own notes, V3's environment is
    experimenter-complicit by design (the experimenter scripts play-mode
    signals rather than a partner agent generating them autonomously). This
    means:
      - TESTABLE IN V3 (single-agent, experimenter-scripted signal): whether
        the agent's OWN behavior differentially responds to L1 (frame open/
        close) and L3 (repair) events that the experimenter injects on a
        schedule. This tests whether play_frame_tag is load-bearing for the
        agent's own policy, not whether it is legible to or co-maintained
        with a partner.
      - NOT TESTABLE IN V3, REQUIRES V4/MULTI-AGENT: whether the tag is
        genuinely BILATERAL -- externally legible to and monitored by a
        partner that can independently initiate a frame violation or a
        repair, rather than following an experimenter's script. This is
        specifically the clause "unilateral play assertion without mutual
        signal is manipulation" (INV-059). A scripted experimenter can only
        ever cooperate with the frame; it cannot play the role of a partner
        who is deceived by a unilateral assertion, so the manipulation
        signature cannot be produced or falsified in V3. Sibling questions
        Q-050 and Q-051 already establish this precondition explicitly for
        games-with-rules and cooperative play, and the same reasoning
        applies to ARC-049's own bilateral clause.

    CONFIRMING signature (V3-testable slice, once play_frame_tag is built):
    following the Q-048/Q-049 pattern, inject an aggression-adjacent action
    (bite-shake or equivalent) at an ambiguity juncture, with two arms --
    (1) L3 repair signal present immediately after the action, (2) L3 repair
    signal withheld. Measure post-juncture behavior (continuation of
    play-typical action selection vs escalation/withdrawal consistent with a
    real-harm read). ANSWER "genuine frame-tracking" if behavior differs
    measurably between arms in the direction predicted by Bekoff 1995's
    non-random repair-signal placement, at or above an effect-size floor
    across >=2/3 seeds. This also requires L1 (frame open/close) to produce a
    detectable behavioral state change on its own, as a baseline check that
    the tag is read at all before testing L3's selective effect.

    FALSIFYING signature: behavior at ambiguity junctures is statistically
    indistinguishable between L3-present and L3-absent arms (and/or L1
    open/close events produce no detectable behavioral change) -- the
    play_frame_tag is tracked internally but not load-bearing for action
    selection, i.e. decorative rather than functional. This would falsify the
    V3-testable slice without touching the bilateral/manipulation clause,
    which remains open regardless (see below).

    SCOPE OF WHAT A PASSING V3 RESULT WOULD AND WOULD NOT ESTABLISH: a PASS
    on the confirming signature above would show a co-maintained-style tag CAN
    be functionally load-bearing for an agent's own behavior. It would NOT
    confirm the full claim as titled -- the bilateral co-maintenance
    requirement and the "unilateral assertion is manipulation" clause are
    untestable without a partner agent capable of independently violating or
    repairing the frame (V4 multi-agent, per INV-059 and Q-050/Q-051). The
    full claim stays substrate_conditional/open even if the V3 slice passes.
  location: docs/architecture/play_mode.md
  notes: "Contrast with MECH-094 (hypothesis tag): simulation is agent-internal (the agent tags its own trajectory evaluation as hypothetical). Play is bilateral: agent and environment/partner co-maintain the play frame tag. The tag must be externally legible for the partner to monitor it. V3 degenerate case: environment is complicit by design (experimenter sets play mode). Full bilateral architecture is a V4 multi-agent requirement. THREE-LEVEL SIGNAL ARCHITECTURE (2026-05-16, Bekoff 1995 / ethological literature): ARC-049 requires three distinct signal layers: (L1) discrete open/close frame markers (play bow / play-face onset); (L2) continuous low-intensity background signal maintained throughout the episode (ongoing play face, body orientation -- the absence of which is itself a violation signal); (L3) explicit repair signals deployed selectively at ambiguity junctures (play bow mid-episode, after bite-shakes or other aggression-adjacent actions). L3 signals are NOT continuous -- they are triggered by P(misread | current_action) exceeding a threshold (Bekoff 1995: non-random placement before/after agonistic-context actions). MONITORING IS PRIMITIVE (2026-05-16): Q-035 (now resolved -- see Q-048 through Q-051 for sub-questions) was premised on a false escalation model. Continuous frame monitoring is NOT invented at games-with-rules -- it is operational from 2-3 months of age (Tronick 1978 still-face paradigm: immediate violation response during episode; Murray & Trevarthen 1985 double-video: temporal contingency detection at 2 months; Fantasia 2014: 3-month-olds detect structural alterations to routine). What escalates across play types is the CONTENT COMPLEXITY of the shared state being monitored (simple contingency -> rule compliance + strategic rationality), not the monitoring architecture. V3 implementation: persistent play_frame_tag flag in LatentState implements L2; L1 and L3 are triggered by frame_open/frame_close events and ambiguity_threshold crossings respectively."

- id: ARC-050
  title: "Play episodes provide a curriculum for E3 goal-pursuit competence acquisition before SD-012 (homeostatic drive) is resolved: synthetic z_goal seeding bypasses the drive_level requirement."
  claim_type: architecture_hypothesis
  subject: play.sd012_curriculum
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-049
    - MECH-194
    - SD-012
  location: docs/architecture/play_mode.md
  notes: "SD-012 bottleneck (EXQ-085 through 085d FAIL: z_goal_norm < 0.1): drive_level must scale benefit_exposure to enable z_goal seeding. Play provides an alternative path: set z_goal synthetically during a play episode, let E3 learn goal-pursuit structure, then real homeostatic drive engages the already-learned structure. This reverses the apparent dependency: play enables SD-012 validation experiments rather than requiring SD-012 to be solved first."

- id: Q-035
  title: "What is the minimal signal architecture for play frame maintenance: is a single bilateral open/close tag sufficient, or does ongoing signal exchange (a continuous play-face equivalent) need to be sustained throughout the episode?"
  claim_type: question
  subject: play.frame_signal_minimal_architecture
  polarity: open_question
  status: resolved
  live_status:
    reading: resolved
    as_of: 2026-07-11
    needs_review: false
  confidence: 0.0
  depends_on:
    - INV-059
    - ARC-049
  location: docs/architecture/play_mode.md
  short: >
    Is a single bilateral open/close tag sufficient for play frame maintenance, or does
    ongoing exchange (a play-face equivalent) need to be sustained throughout the episode?
  description: >
    The answer determines whether mid-episode frame collapse is detectable before close
    and shapes the minimal ARC-049 implementation. If a single open/close transition
    suffices: frame integrity can only be verified at episode close (no close signal =
    frame intact assumed). If ongoing exchange is required: mid-episode monitoring is
    possible and INV-059's exploitation-limiting property is stronger. In animal play,
    the play bow marks episode open but play-face signals are maintained throughout --
    suggesting ongoing exchange is observed but whether it is necessary or redundant is
    unknown. REE implication: determines whether ARC-049 requires a heartbeat signal
    mechanism or just open/close transitions.
  suggested_experiment_type: behavioral_mode_ablation
  resolution: resolution_a
  resolution_note: |
    RESOLVED 2026-05-16 via lit-pulls on infant contingency detection and ethological play signals.
    The original question was premised on a false escalation model (monitoring architecture escalates
    from open/close to continuous heartbeat across play types). The evidence shows instead that:
    (a) CONTINUOUS MONITORING IS PRIMITIVE: Tronick 1978 (still-face paradigm) shows infants as
    young as 3-4 months respond immediately to mid-episode contingency violations, not just at
    episode boundaries. Murray and Trevarthen 1985 (double-video) isolates temporal contingency
    from content at 2 months. Fantasia 2014 shows structural alteration detection at 3 months.
    This means the monitoring architecture is NOT invented at any play type -- it is present from
    before sensorimotor play begins.
    (b) WHAT ESCALATES IS CONTENT, NOT ARCHITECTURE: Games-with-rules (Schmidt 2015) require
    dual-level content monitoring (rule compliance + strategic rationality). Pretend play (Leslie 1987)
    requires co-active hypothesis_tag + play_frame_tag. Cooperative play requires bilateral role/goal
    state. But the underlying monitoring mechanism -- a continuous background signal checking frame
    integrity -- is the same at every level. The three-level signal architecture (Bekoff 1995:
    L1 open/close, L2 continuous background, L3 ambiguity-repair) is present in canid play regardless
    of play type complexity.
    (c) V3 IMPLEMENTATION (CORRECTED 2026-08-08 -- see what_would_answer finding 3):
    play_frame_tag is NOT yet implemented in LatentState -- this was inaccurate as
    originally written. L1 (frame_open/frame_close) and L2 (persistent background
    tag) remain unbuilt; L3 (ambiguity repair) likewise. The literature-derived
    reading in (a)/(b) is unaffected, but a future reader should not assume L1/L2
    are already available when scoping Q-048/Q-049 experiment design.
    Sub-questions Q-048 through Q-051 operationalise the content-complexity dimension:
    what content does the monitoring signal need to track at each play stage?
  epistemic_category: derivational
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: this resolution is non-degenerate only if the
    cited studies measure ONLINE mid-interaction detection (infants responding
    DURING a contingency perturbation), not merely post-hoc rated interaction
    quality at episode boundaries. If Tronick 1978 / Murray and Trevarthen 1985 /
    Fantasia 2014 turn out on closer read to only support boundary-level
    judgments, the "continuous monitoring is primitive, not invented at any play
    type" reading is unsupported and the resolution is vacuous.
    CONFIRMING (established by the 2026-05-16 resolution): (a) Tronick 1978
    (still-face), Murray and Trevarthen 1985 (double-video), and Fantasia 2014
    (structural-alteration detection) jointly show infants as young as 2-4
    months detect and respond to mid-interaction contingency violations online,
    not only at episode boundaries -- continuous background monitoring (L2) is
    developmentally primitive and present before sensorimotor play begins, not
    escalated into at any later play type. (b) Bekoff 1995's three-level canid
    play signal architecture (L1 open/close, L2 continuous background, L3
    ambiguity-repair) is stable across play-type complexity; what escalates
    across sensorimotor -> pretend -> games-with-rules -> cooperative play is
    monitored CONTENT (Schmidt 2015 dual-level rule/strategy; Leslie 1987
    decoupling), not the L1/L2/L3 architecture itself. Sub-questions Q-048
    through Q-051 were registered (as_of 2026-07-11, still open) to operationalise
    that content-complexity dimension per play type.
    WHAT WOULD REOPEN THIS RESOLUTION (this documents an ALREADY-RESOLVED claim
    -- no forward experiment is queued against it):
    1. A closer read of Tronick / Murray and Trevarthen / Fantasia showing the
    infant responses are actually post-hoc (rated only at recovery, not measured
    as in-perturbation detection latency) would undermine the "continuous
    monitoring is primitive" reading and reopen the original question.
    2. If Q-048 (sensorimotor/constructive L2-vs-L2+L3 ablation) or Q-049
    (pretend-play co-tag continuity) were run and found that L2 background
    monitoring alone is NOT sufficient even for the simplest play content, that
    would weaken the "architecture is constant, only content escalates" reading.
    3. CURRENCY FINDING (verified 2026-08-08): resolution_note part (c) --
    "the persistent play_frame_tag in LatentState already implements L2" -- was
    inaccurate. Direct grep of ree-v3/ree_core/ (including latent/stack.py,
    where LatentState is defined) found ZERO occurrences of play_frame_tag,
    frame_open, frame_close, or ambiguity_threshold anywhere in the substrate.
    This matches ARC-049's own what_would_answer, which independently confirms
    the same zero-occurrence finding as an unmet substrate precondition
    blocking ARC-049 itself. Corrected in resolution_note above; does not
    reopen the literature-derived theoretical reading in (a)/(b).

- id: INV-060
  title: "Play dominates the child developmental phase; the type of play progresses as subsystem competence develops, systematically training architectural components in dependency order."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-049
  pending_substrate_reconfirmation: true
  subject: play.developmental_dominance
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  depends_on:
    - INV-055
    - INV-058
    - INV-041
    - ARC-049
  location: docs/architecture/developmental_curriculum.md
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: play-mode substrate confirmed entirely unbuilt
    (re-verified 2026-08-08, zero hits for play_frame_tag/play_mode/synthetic_z_goal in
    ree_core/). Two joined predictions: (a) DOMINANCE -- play is the dominant child-phase
    behavioral mode, measurable once a curriculum scheduler exists; (b) TYPE PROGRESSION
    -- sensorimotor -> constructive -> pretend -> games-with-rules -> cooperative, each
    training the next stage's prerequisites. Leg (b) is the same testable content as
    sibling claim MECH-197 -- cross-reference its ablation design rather than duplicating.

    CONFIRMING: scheduler allocates majority child-phase budget to play absent override,
    AND skipping an earlier play type produces measurable subsystem-competence deficits
    in the next type. FALSIFYING: comparable/minority play allocation (dominance clause
    fails) OR no deficit from skipping/reordering (dependency-order clause fails) --
    record as demoting the surviving half rather than a binary pass/fail, given the
    two-part title.

    This claim was deliberately excluded from the 2026-06-07 batch reclassification of
    the play cluster (invariant_type: emergent flagged for separate governance
    treatment) and left for a dedicated pass -- this 2026-08-08 digestion is that pass.
    `pending_substrate_reconfirmation: true` is kept (not cleared) per the INV-055
    precedent: it tracks whether ARC-049's eventual implementation still matches what
    this invariant describes, which is moot but not resolvable while the substrate
    remains unbuilt.
  notes: "The infant phase (INV-055) produces valence map and behavioral repertoire but not goal-pursuit competence. The child phase acquires goal-pursuit competence primarily through play (INV-058). Play type progresses: sensorimotor -> constructive -> pretend -> games-with-rules -> cooperative (MECH-197). Each type requires competencies acquired by the previous type and trains specific subsystems. Sensorimotor play marks the transition out of infancy; peer-level cooperative play marks readiness for adult real-consequence operation. The progression is not optional -- skipping a stage leaves subsystem prerequisites unmet."

- id: MECH-197
  title: "Play types progress in dependency order (sensorimotor -> constructive -> pretend -> rule-based -> cooperative), each training specific architectural subsystems that the next type requires."
  claim_type: mechanism_hypothesis
  subject: play.type_progression
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  depends_on:
    - INV-060
    - INV-058
    - MECH-194
  location: docs/architecture/play_mode.md
  notes: "Sensorimotor play trains E1/E2 (world and motor models, single-step synthetic goals). Constructive play trains E2 rollout and E3 trajectory selection (compositional multi-step goals). Pretend play exercises MECH-094 + ARC-049 intersection and commitment architecture (counterfactual representations within play frame). Games with rules train social coordination and constraint satisfaction (structured shared-state monitoring). Cooperative play trains full multi-agent ARC-049 with mutual frame maintenance. Each type requires the previous type's competencies. CORRECTION (2026-05-16): The frame MONITORING ARCHITECTURE does not escalate across play types -- continuous bilateral monitoring is a primitive capacity present from 2-3 months (Tronick 1978, Fantasia 2014), before any of these play types emerge. What escalates is the CONTENT COMPLEXITY of the shared state being monitored: sensorimotor (is partner still contingently responding?), constructive (is compositional goal still shared?), pretend (are both hypothesis_tag and play_frame_tag co-active?), games-with-rules (Schmidt 2015 dual-level: are current rules valid + is partner engaging rationally?), cooperative (bilateral role/goal state). Q-035 is resolved -- see Q-048 through Q-051 for the content-dimension sub-questions."
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: This claim cannot be tested by any experiment run
    against the current ree-v3 substrate. Verified 2026-06-07 and reconfirmed
    2026-07-11 (IGW-20260711-196): a word-boundary grep over ree_core for
    play/play_frame/is_play/play_mode/synthetic_z_goal returns empty. LatentState
    carries no play_frame_tag, there is no is_play frame flag, no play-type
    developmental curriculum, and no synthetic z_goal/z_harm seeding path (the
    only z_goal path is the drive-gated GoalState.update()). A queued diagnostic
    against this substrate would either fail to instantiate or vacuously re-derive
    "V3 has no play-type curriculum" -- that is a substrate_not_ready self-route,
    not a for/against verdict on the claim, and must not be logged as a null or
    negative result. The release condition is upstream substrate work via
    /implement-substrate, not another /queue-experiment iteration: at minimum
    ARC-049's play_frame_tag, MECH-194's synthetic z_goal/z_harm substitution, and
    a curriculum instantiating the five named, orderable play types (sensorimotor,
    constructive, pretend, games-with-rules, cooperative). Only once that substrate
    exists does a test become informative.
    CONFIRMING signature: once the play-type curriculum exists, an ablation study
    that removes or skips an earlier play type (e.g. trains constructive and
    pretend play but skips sensorimotor, or skips constructive before pretend)
    shows the later type fails to acquire its target subsystem competence --
    e.g. a pretend-play-trained agent that skipped constructive play fails to
    develop E3 trajectory selection or shows degraded hypothesis-tag/play-frame-tag
    co-activation (MECH-198) relative to an agent that received the full ordered
    sequence. Each play type's prerequisite subsystem competencies (as named
    above: E1/E2 world-motor models for sensorimotor; E2 rollout/E3 trajectory
    selection for constructive; MECH-094+ARC-049 intersection for pretend;
    structured shared-state monitoring for games-with-rules; full multi-agent
    ARC-049 frame maintenance for cooperative) are measurably present only after
    the prior type's training, and measurably absent or degraded when that prior
    type is ablated.
    FALSIFYING signature: skipping or reordering play types produces no
    measurable deficit in the "next" type's target subsystem competence --
    e.g. an agent trained directly on pretend play (skipping sensorimotor and
    constructive) acquires MECH-198's hypothesis-tag/play-frame-tag
    co-activation at the same rate and quality as an agent that received the
    full ordered curriculum. This would falsify the DEPENDENCY-ORDER claim
    specifically (each type requiring the previous type's competencies) without
    necessarily falsifying the narrower, already-resolved claim that play-type
    CONTENT complexity escalates (that sub-claim, per the 2026-05-16 correction,
    is about content complexity of shared-state monitoring, not the
    subsystem-training dependency chain, and is not what this test targets).

- id: MECH-198
  title: "Pretend play is the developmental point where the hypothesis tag (MECH-094) and play frame tag (ARC-049) first co-operate: the agent maintains counterfactual representations within a play frame, exercising the full commitment architecture in synthetic mode."
  claim_type: mechanism_hypothesis
  subject: play.pretend_tag_intersection
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  depends_on:
    - MECH-094
    - ARC-049
    - MECH-197
  location: docs/architecture/play_mode.md
  notes: "Pretend play ('this stick is a sword') requires maintaining a counterfactual representation (hypothesis tag: the stick is not really a sword) within a play frame (play tag: the fight is not really dangerous). This is the first point in development where both tag systems co-operate. The agent practices committing to action plans under representations it knows are synthetic, within a frame with synthetic stakes. This exercises the full commitment architecture before it is needed for real-consequence decisions. Failure to develop pretend play competence would leave the commitment architecture untested in low-stakes mode before deployment in high-stakes real operation."
  testable_prediction: "Agents whose developmental curriculum includes pretend play will show significantly fewer commitment architecture failures (false commits, commit-without-evaluation, frame confusion between real and hypothetical) in adult real-consequence operation, compared to agents whose curriculum skips pretend play but is otherwise identical. The pretend-play-skip group should show the specific failure signature of treating real consequences as if they were synthetic (frame tag confusion) or failing to commit when commitment is warranted (undertrained commitment gate)."
  suggested_experiment_type: developmental_ablation_discriminative_pair
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: MECH-198 is currently substrate-blocked, not merely
    unqueued -- no experiment script or queue entry exists for it anywhere in
    ree-v3/, and its three dependencies (MECH-094 hypothesis tag, ARC-049 play
    frame tag, MECH-197 play-type progression) are all themselves
    candidate/substrate_conditional rather than built-and-proven. A run is
    informative only once: (1) the hypothesis_tag and play_frame_tag are both
    implemented as independently observable, co-activatable signals (not just
    declared in LatentState) so "co-operation" is a measurable event rather than
    an assumption; (2) a developmental curriculum harness exists that can run two
    curricula differing ONLY in pretend-play inclusion/exclusion, otherwise
    identical (same sensorimotor/constructive play exposure, same total training
    budget); and (3) "commitment architecture failure" is operationalized into the
    two distinct signature classes this claim's own testable_prediction names --
    (a) frame-tag confusion (real consequences processed as synthetic, or vice
    versa) and (b) commitment-gate miscalibration (fires without E3 evaluation
    completing, or never fires) -- as separately logged event types, with
    non-zero baseline variance in at least one arm. A run attempted before all
    three hold should self-route substrate_not_ready, not count as a verdict.
    CONFIRMING signature: the pretend-play-curriculum group shows significantly
    fewer total commitment-architecture failures than the pretend-play-skip group
    in adult real-consequence operation (otherwise-identical curricula), AND the
    skip group's excess failures concentrate specifically in the two predicted
    signature classes above (frame confusion in the real->synthetic direction, or
    gate firing without/never completing evaluation) rather than presenting as
    generic, undifferentiated performance degradation.
    FALSIFYING signature: no significant difference in commitment-failure rate
    between the two curricula; OR the skip group does show elevated failures but
    they do NOT cluster in the predicted signature classes (e.g. failures are
    diffuse / equally distributed across unrelated failure types) -- indicating
    that even if pretend play matters, it is not doing the specific architectural
    job (co-training the hypothesis tag and play frame tag against the commitment
    gate) that MECH-198 proposes. Either falsifying outcome would also weaken the
    downstream cluster that treats MECH-198 as load-bearing (INV-061, MECH-200,
    MECH-201, MECH-202), since their unified-etiology argument presumes this
    specific mechanism rather than a generic play-deprivation effect.

- id: MECH-199
  title: "The caregiver's role during childhood is specifically play-frame maintenance (ARC-049), transitioning from unilateral frame-setting through co-participation to scaffolded withdrawal as play types progress."
  claim_type: mechanism_hypothesis
  subject: play.caregiver_frame_maintenance
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  depends_on:
    - INV-043
    - ARC-049
    - ARC-046
    - MECH-197
  location: docs/architecture/developmental_curriculum.md
  notes: "The caregiver role transitions across play types: damage protection during infancy (ARC-046) -> unilateral frame-setting during sensorimotor play -> frame monitoring and boundary enforcement during constructive/pretend play -> co-participation in games with rules -> scaffolded withdrawal during cooperative play -> peer-level mutual frame maintenance in adulthood. The caregiver IS the bilateral frame-maintainer that ARC-049 requires during the developmental period when the agent cannot yet self-monitor frame integrity. This maps INV-043 (caregiver requirement) onto specific play-type phases and explains what the caregiver is concretely doing during childhood beyond protection."

- id: INV-061
  title: "Derealization, delusion, PTSD flashbacks, anxiety, mania, impulsivity, and commitment paralysis share a common architectural substrate: undertrained real/synthetic frame distinction from insufficient pretend play."
  claim_type: derived_prediction
  prediction_domain: clinical_psychiatry
  subject: psychiatric.frame_confusion_unified_etiology
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  depends_on:
    - MECH-094
    - MECH-198
    - MECH-200
    - MECH-201
    - MECH-202
  location: docs/architecture/psychiatric_failure_modes.md
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: gated on TWO things -- MECH-198's own full precondition
    (co-activatable hypothesis_tag + play_frame_tag, dose-varied pretend-play curriculum;
    see MECH-198's own what_would_answer for that build list), AND a further requirement
    specific to this claim's UNIFYING structure: the same undertrained agents must be
    scoreable on multiple distinct failure signatures at once (both confusion directions
    + multiple affected subsystems), not just one aggregate count. Only MECH-094 (agent-
    internal hypothesis_tag) is built (confirmed 2026-08-08, load-bearing throughout
    agent.py/amygdala/pfc/hippocampal/predictors); ARC-049 (play_frame_tag), MECH-197/198/
    200/201/202, and INV-055 are all confirmed unbuilt. No dose-variable pretend-play
    experiment exists anywhere in the codebase.

    CONFIRMING: within one population of agents varying pretend-play dose (other
    developmental exposure held constant), the SAME undertrained agents show BOTH
    confusion directions and a subsystem-degradation pattern that predicts which
    clinical-analog failure mode dominates for that agent (dissociation, not uniform
    degradation), plus a dose-response gradient (intermediate dose -> intermediate
    severity/breadth).

    FALSIFYING: either the base MECH-198 mechanism doesn't hold (pretend-play dose has no
    measurable effect), OR the resulting failure modes are STATISTICALLY INDEPENDENT
    across agents (no above-chance co-occurrence) -- falsifies the UNIFIED-ETIOLOGY claim
    specifically ("different failure signatures of the same mechanism") WITHOUT falsifying
    any individual downstream mechanism (MECH-200/201/202) on its own, each of which could
    still independently hold as correct for its own failure mode.

    SCOPE NOTE (preserving this claim's existing domain-modelling carve-out): even a full
    PASS on the confirming signature above establishes only that REE's own architecture
    instantiates a common-substrate pattern for these failure signatures -- it does NOT by
    itself establish that human derealization, delusion, PTSD flashbacks, anxiety, mania,
    impulsivity, and commitment paralysis share this etiology. That further step requires
    the domain-modelling assumption already flagged in this claim's notes (that REE's
    frame-distinction architecture correctly models human developmental frame-distinction)
    -- a separate, currently unverified premise this claim does not itself test.
  notes: "Reclassified from claim_type: invariant in Session C batch 2026-04-17. Rationale: this is a clinical psychiatric prediction about a shared substrate for human frame-confusion disorders; it holds only conditional on REE correctly modelling human developmental frame-distinction -- a domain-modelling assumption distinct from REE's architectural invariants (per Daniel's 2026-04-17 objection, audit registry INV-061). The REE architecture distinguishes real from synthetic/hypothetical frames via the hypothesis tag (MECH-094, agent-internal) and play frame tag (ARC-049, bilateral). Pretend play (MECH-198) is where these co-operate for the first time. When pretend play is absent or insufficient, the frame-distinction mechanism is undertrained. The resulting adult failure modes are not independent disorders but different failure signatures of the same mechanism, varying by (a) direction of confusion: real->synthetic (derealization cluster, MECH-200) vs synthetic->real (delusion cluster, MECH-201), and (b) affected subsystem: frame tag, commitment gate (MECH-202), or threat evaluation. This provides a unified developmental etiology for conditions that are clinically distinct but architecturally related."

- id: MECH-200
  title: "Frame confusion (real->synthetic direction): the agent treats real consequences as synthetic, producing derealization, manic commitment, and dissociative states."
  claim_type: mechanism_hypothesis
  subject: psychiatric.frame_confusion_derealization
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  depends_on:
    - MECH-094
    - MECH-198
    - ARC-049
  location: docs/architecture/psychiatric_failure_modes.md
  notes: "The real/synthetic distinction was never properly calibrated during pretend play (MECH-198). The agent defaulted to tagging ambiguous frames as synthetic (the safer error during play, where real consequences are absent). In adulthood this default persists: when frame signals are ambiguous, real harm is discounted as if play-harm. Signatures: (1) derealization -- real harm experienced as unreal, z_harm_s fires but is discounted; (2) manic episodes -- play frame leaks into real operation, commitment gate fires without harm-gate check, elevated commitment with reduced consequence-sensitivity; (3) dissociative states -- frame tag intermittently flips to synthetic under stress. Developmental evidence: reduced pretend play predicts later psychotic symptoms (Cannon 2002)."

- id: MECH-201
  title: "Frame confusion (synthetic->real direction): the agent treats simulation, replay, and imagination as real, producing delusion formation, PTSD flashbacks, and anxiety."
  claim_type: mechanism_hypothesis
  subject: psychiatric.frame_confusion_delusion
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  depends_on:
    - MECH-094
    - MECH-198
    - MECH-121
    - ARC-049
  location: docs/architecture/psychiatric_failure_modes.md
  notes: "During pretend play, the hypothesis tag (MECH-094) must be active (stick is not a sword) while the play tag (ARC-049) is also active (fight is not dangerous). If pretend play was insufficient, the hypothesis tag was never co-exercised with the play tag. In adulthood, internally generated representations lack the hypothesis tag and are processed through the real-consequence pipeline. Signatures: (1) delusions -- internal representations experienced as external reality, E3 treats simulated trajectories as real percepts; (2) PTSD flashbacks -- SWR replay (MECH-121) lacks hypothesis tag, experienced as real re-occurrence; (3) anxiety disorders -- pre-commit simulation triggers z_harm_a as if real; (4) paranoid ideation -- simulated other-agent threat (MECH-127 counterfactual) processed without hypothesis tag."

- id: MECH-202
  title: "Commitment gate developmental failure: undertrained gate from insufficient pretend play produces either impulsivity (gate fires without evaluation) or commitment paralysis (gate never fires)."
  claim_type: mechanism_hypothesis
  subject: psychiatric.commitment_gate_failure
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  depends_on:
    - MECH-198
    - MECH-197
    - INV-055
  location: docs/architecture/psychiatric_failure_modes.md
  notes: "The commitment gate was never exercised in synthetic mode during pretend play. Two failure directions depending on how the untrained gate resolves: (A) insufficient inhibition -- gate fires without completing E3 evaluation. Maps to impulsivity (commits without evaluation), mania commitment component (gate threshold too low + MECH-200 frame confusion compound), impulse control disorders (gate does not wait for E2 rollout). (B) excessive inhibition -- gate never fires, threshold stuck at infant default (INV-055 very high commit threshold never calibrated down). Maps to OCD checking/rumination (E3 loops without committing), catatonia (gate frozen at max threshold), severe indecision (commitment signal never reaches threshold). Pretend play calibrates the gate: low-stakes commitment practice adjusts the threshold between these extremes."

- id: MECH-203
  title: "Tonic serotonergic (5-HT) level during waking tags benefit-relevant experiences for preferential replay during subsequent SWS, complementing harm-salience tagging by the residue field"
  claim_type: mechanism_hypothesis
  subject: serotonin.replay_salience_tagging
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-01
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-843_2026-08-01
      as_of: 2026-08-01
      verdict: "non_contributory/measurement_test_design_defect -- third consecutive invalid test; claim never fairly tested"
  implementation_phase: v3
  epistemic_category: standard
  evidence_quality_note: >
    [2026-08-01 governance, confirmed failure_autopsy_V3-EXQ-843_2026-08-01]: V3-EXQ-843
    (SWS replay-salience dose-response redesign) is the THIRD consecutive invalid MECH-203 test.
    Admissibility (C3) failed on all 3 seeds -- n_harm_states=0 across up to 4800 visited states
    per seed -- because the waking rollout is driven by the agent's own (successfully
    hazard-avoiding) trained policy through a low-density hazard layout (3 hazards, 5% harm
    density, 10x10 grid), which structurally routes around all harm contact. Environment/protocol
    design gap (the agent's own competence prevents the exposure the design needs), NOT evidence
    against the claim: the SWS replay-selection machinery itself (SerotoninModule drive_state,
    ResidueField valence, HippocampalModule _select_valence_weighted_start) is correctly wired and
    was never reached. MECH-203 has NEVER had a valid test: V3-EXQ-255/256 were invalidated by an
    env.step() return-order bug (n_benefit_samples=0); this redesign is invalidated by zero harm
    exposure. sleep_substrate_plan.md's existing "EXQ-255/256 PASS; adequate" note is stale and is
    corrected in the same pass. Route: /queue-experiment (843b) forcing harm exposure (e.g. a
    denser hazard layout or an exploration-forced rollout phase, not the agent's own competent
    policy). NOT weakened (claim never fairly tested); NOT cleared. PROMOTES/DEMOTES NOTHING;
    MECH-203 stays candidate.

    CORRECTION (2026-08-08, thought-digestion currency check): the "route: /queue-experiment
    (843b) forcing harm exposure" fix above has since been tried and independently falsified
    twice more, for two further reasons. A draft V3-EXQ-843b was built per this note's exact
    prescription (forced-exploration uniform-random pool-building + denser hazards) and
    empirically disproven in smoke/probes before formal queuing: (1) forced exposure produces
    harm EVENTS but n_harm_states stays 0 because the driver never wires
    use_harm_stream=True / valence_harm_enabled=True / obs_dict['harm_obs'] -- the harm-valence
    write path was never wired at all, contrary to this note's assumption; (2) wiring that
    stream instead produces the MIRROR degeneracy, because the HarmEncoder is untrained (no P0
    warmup) and writes non-discriminative harm valence on every step, again collapsing to a
    degenerate (near-all-harm) pool. On 2026-08-03T23:44Z the backing proposal (EVB-0157) was
    formally flipped from executed to status: deferred_substrate_not_ready, with an explicit
    release_condition: a MECH-203 confirming DV becomes constructible once the harm/benefit
    valence write path is wired AND read off a TRAINED substrate (use_harm_stream=True +
    obs_dict['harm_obs'] + valence_harm_enabled=True, plus a P0 encoder warmup, plus
    contact-localized not per-step valence tagging with guaranteed benefit AND harm contact).
    Until then any forced-exposure lettered iteration reproduces the same invalid measurement --
    do NOT re-attempt the "just force more harm exposure" fix as literally stated above. See
    this claim's what_would_answer for the current non-degeneracy precondition.
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: the waking rollout must produce a genuinely discriminative
    benefit/harm valence pool (n_harm_states > 0 AND n_benefit_states > 0, non-trivially) via a
    design that does NOT rely on the agent's own competent policy routing around hazards. As of
    the 2026-08-03 measurement-defect finding, this precondition is NARROWER than "force more
    harm exposure" -- three independent test designs failed for three independent reasons
    (env.step() return-order bug; zero harm exposure from a competent policy; harm-valence write
    path never wired / then, once wired, non-discriminative because the HarmEncoder is
    untrained).

    [2026-08-09 governance, GFLAG-0018 adjudication]: sub-requirements (a)/(b)/(c) below are NOT
    three equal open builds -- (c) is already satisfied via a route that does not need (a)/(b).
    ree-v3 adfd8c01 (2026-08-02T12:26Z) landed SerotoninModule.harm_salience() +
    REEAgent.update_harm_salience() (ree_core/neuromodulation/serotonin.py:204,
    ree_core/agent.py:10345), contract-pinned by
    tests/contracts/test_mech203_harm_salience_writepath.py C1-C5: it writes
    VALENCE_HARM_DISCRIMINATIVE from the env scalar harm_exposure (CausalGridWorldV2 /
    body_obs[10]) via the SAME EMA'd-exposure convention benefit_exposure already uses -- NEVER
    touching z_harm or the HarmEncoder -- bit-identical OFF when tonic_5ht_enabled=False (C4), no
    new config flag (C5), and C3 pins that the calibrated EMA-scale write (not a naive raw-signal
    write) avoids the swamping degeneracy. Because this route never reads z_harm, (a) harm-stream
    wiring (use_harm_stream/obs_dict['harm_obs']/valence_harm_enabled) and (b) P0 HarmEncoder
    warmup are UNNECESSARY on it -- they are prerequisites only for the alternative
    HarmEncoder/z_harm-based route, which is not the route this write-path takes. ADJUDICATED:
    (a)/(b) are declared unnecessary for a MECH-203 test via the harm_salience route; a
    /queue-experiment redesign using SerotoninModule.harm_salience() for admissible
    contact-localized valence tagging, combined with a non-degenerate exposure design (still
    owed -- the competent-policy-routes-around-hazards problem is unrelated to this route
    choice), can proceed without building (a)/(b). (a)/(b) remain owed only if a future test
    deliberately chooses the HarmEncoder/z_harm route instead. Registered as substrate_queue
    entry mech203-valence-pool-admissibility (currency_finding, 2026-08-08); this note is the
    governance adjudication that entry was awaiting.

    Per proposal EVB-0157's ORIGINAL release_condition (superseded above for the harm_salience
    route): (a) use_harm_stream=True + obs_dict['harm_obs'] passed to agent.sense() +
    valence_harm_enabled=True (the harm-valence write path actually exercised, not just harm
    contact events), (b) a P0 encoder warmup so the HarmEncoder is trained before the
    measurement rollout, and (c) contact-localized (not per-step) valence tagging with
    guaranteed benefit AND harm contact. On the harm_salience route, only (c) plus a
    non-degenerate exposure design are owed; (a)/(b) apply only to the HarmEncoder/z_harm route.

    CONFIRMING: once admissible, benefit-salient experiences (high benefit_exposure, weak
    z_goal -- "missed opportunities") tagged during high tonic 5-HT show preferential SWS
    replay (measurable via HippocampalModule's valence-weighted start selection,
    _select_valence_weighted_start) relative to matched experiences during low tonic 5-HT,
    complementing (not substituting for) the already-established harm-salience tagging by the
    residue field (MECH-099).

    FALSIFYING: once admissible, benefit-salient experience replay shows no differential
    relationship to tonic 5-HT level -- the SWS replay-selection machinery (SerotoninModule
    drive_state, ResidueField valence, HippocampalModule _select_valence_weighted_start), though
    correctly wired per the 2026-08-01 autopsy, does not functionally gate replay selection on
    the 5-HT signal the way the mechanism claims.

    GOVERNANCE FLAG (2026-08-08, not resolved here): no substrate_queue.json entry exists for
    EVB-0157's release_condition (harm-stream wiring + P0 encoder warmup + contact-localized
    valence tagging), despite being named twice now (2026-08-01 autopsy, 2026-08-03 proposal
    flip). Chip spawned to register it.
  depends_on:
    - MECH-186
    - MECH-121
    - MECH-165
    - SD-017
  notes: >
    The residue field already marks harm-dense experiences for replay prioritization (MECH-099,
    supported by O'Neill 2010 and Huelin Gorriz 2023). But benefit-salient experiences -- where
    benefit_exposure was high but z_goal was weak (missed opportunities) -- should also be
    prioritized for consolidation. The tonic 5-HT benefit gradient (MECH-186) provides the
    tagging signal: experiences occurring during high tonic 5-HT (active benefit gradient) are
    tagged for replay, while experiences during low tonic 5-HT (depressive attractor, INV-053)
    are under-tagged, contributing to the consolidation deficit in depression. This connects
    the psychiatric failure mode (MECH-186 deficit -> reduced benefit gradient) to the sleep
    failure mode (under-consolidated benefit-relevant experiences -> impoverished goal
    representations after sleep). Predicts: depressed agents should show reduced benefit-replay
    density in SWS relative to harm-replay density.

- id: MECH-204
  title: "Serotonergic withdrawal during REM actively defines the precision recalibration zero-point by removing the benefit gradient, not merely permitting recalibration"
  claim_type: mechanism_hypothesis
  subject: serotonin.rem_gate_zero_point
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-24
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-794_2026-07-22"
      as_of: 2026-07-22
      verdict: non_contributory/competence_implementation_gap
  implementation_phase: v3
  evidence_quality_note: |
    Substrate IMPLEMENTED 2026-05-08: SleepLoopManager WRITEBACK consumer
    (compute_recalibration_target + e3.recalibrate_precision_to); F1 cross-cycle
    persistent zero-point EMA. Config use_rem_precision_recalibration default False;
    rem_precision_recalibration_step 0.25 after V3-EXQ-541c PASS 2026-05-09.
    V3-EXQ-541a/541b inconclusive_timescale (failure_autopsy 2026-05-17); 541c
    confirms mechanism at 16 cycles. substrate_queue substrate-ready IGW-20260521-022;
    V3-EXQ-602 queued (StepHarness + sleep aggregation cluster integration).
    Phase 7 Option B deferred V4. No promotion from substrate closure alone.

    Substrate IMPLEMENTED 2026-05-08/09 (sleep_substrate_plan GAP-1): F1 cross-cycle persistent_zero_point EMA in SerotoninModule; WRITEBACK-phase consumer in SleepLoopManager via use_rem_precision_recalibration (default False, bit-identical OFF). V3-EXQ-541 superseded (within-cycle no-op). V3-EXQ-541c PASS 2026-05-09 (supports): C1-C4, monotone step-size dose-response, 16 sleep cycles/arm. EXQ-541a/541b inconclusive_timescale (4 cycles insufficient for C3 rv divergence). Contracts 13/13. Tier-1 integration V3-EXQ-602 queued (StepHarness + sleep aggregation cluster). Claim remains candidate/v3_pending until governance accepts contributory evidence; downstream SD-029/MECH-256 retests still gated on MECH-269/MECH-307 per substrate_queue.
    2026-05-29 (V3-EXQ-596 + V3-EXQ-602 failure_autopsy): closure-handoff integration at 12 cycles, step=0.25: MECH-204 recalibration fires every cycle in 3/3 seeds (C1 PASS); arm relative rv divergence 8.4-27.6% per seed clears the 3% C3 threshold and exceeds the 541c canonical PASS bar (4.5-9% at 16 cycles). Overall FAIL driven by C2 cluster_live_frac >= 50% (0/3 seeds; deterministic 1/11 = 9.09% because MECH-285 sampler under K=1 single-fire driver draws once at buffer-fill, then idle). C2 is a cluster-integration criterion (MECH-285 / GAP-3), not a MECH-204 prediction; 596/602 strengthen MECH-204 on C1+C3. Both manifests reclassified non_contributory -> inconclusive_measurement. Successor EXQ redesigning C2 against the documented K=1 driver pattern (sleep_substrate_plan.md GAP-7) tracked under /queue-experiment. Autopsy: evidence/planning/failure_autopsy_V3-EXQ-596-602_MECH204_2026-05-29.md.
    2026-07-24 governance (V3-EXQ-794 Phase 7 2x2 calibration loop, evidence, claim_ids=[MECH-204, SD-076]; failure_autopsy_V3-EXQ-794_2026-07-22, confirmed, user-confirmed routing "794: SD-076 does_not_support -> non_contributory, with the implementation-fix route"): formalizes the citation this claim's own `what_would_answer` block (a) already narrates -- V3-EXQ-794 pinned rv_final at exactly 0.010000 on all four inflation arms with overconfidence_score bit-identical to 15 significant figures at LO and HI asymmetry, a saturation fingerprint, not a null. epistemic_category competence_implementation_gap; evidence_direction non_contributory (PROMOTES/DEMOTES NOTHING). Supersedes the 596-602 citation as the live_status.evidence pointer. sd_waking_confidence_inflation_headroom substrate_queue entry created (Step 6a-iv) to lift the saturation before V3-EXQ-794a can re-test. MECH-204 stays candidate/v3_pending, NOT ANSWERABLE until that substrate lands per (a) above.
    [2026-08-01 governance, GOV-FANOUT-1 3-hypothesis portfolio synthesis; V3-EXQ-850 (H1),
    confirmed failure_autopsy_V3-EXQ-850_2026-08-01, and V3-EXQ-853 (H2), confirmed
    failure_autopsy_V3-EXQ-853_2026-08-01, both diagnostic, claim_ids=[SD-076] on the H2 leg /
    bears_on-only on H1 -- neither carries MECH-204 claim weight]: 794a's full behavioural loop
    reached only ~half the overconfidence-reduction the SD-076 headroom repair's own isolated
    validation smoke demonstrated achievable at the same error scale, raising three rival
    hypotheses (H1 F1-recalibration damping / H2 insufficient training exposure / H3 wrong
    mechanism form). H1 (V3-EXQ-850): precondition_unmet correctly self-routed (dose_levels_separated
    fails by a wide margin), but F1-off measurably reduces rv below the F1-on reference in both
    arms, in H1's predicted direction, closing 14-23% of the gap to the smoke floor -- not
    sufficient to confirm H1, but rules out zero effect; stays alive, weight partially shifted
    toward H2/H3. H2 (V3-EXQ-853, N_TRAIN_EPS 30->150): weakens H2 itself -- dose separation
    SHRINKS (4.35e-4 smoke -> 1.28e-4 at 794a's 30 -> 2.48e-5 at this run's 150), the opposite of
    H2's prediction; N_TRAIN_EPS is also confounded with F1-firing count (K=1 fires once/episode),
    so a clean H2 probe needs to vary STEPS_PER_EP instead. H3 (targeted /lit-pull, commit
    e84240108d, landed 2026-08-01T00:07:47Z, 5 entries): weakened not eliminated -- Baranski
    1994/2007 and Boardman 2024 both nudge away from acute-regime drift; Jones 2006 offers
    partial counter-support that a real drift phenomenon exists at all. NET: genuine three-way
    residual uncertainty, no clean winner -- H1 alive-with-partial-signal, H2 weakens itself, H3
    weakened. EVB-0454's SD-076 decision deadline was cited here as 2026-08-03T20:50:36Z (a
    snapshot from an earlier governance cycle). CORRECTED 2026-08-01 (root-caused this session,
    full detail on SD-076's own evidence_quality_note): `decision_deadline_utc` in
    experiment_proposals.v1.json is COMPUTED as `generated_at_dt + 72h` on every regeneration
    while the conflict stays unresolved, not a frozen deadline -- there is no discrepancy to
    reconcile, only a rolling value that keeps re-arming. Do not treat any cited timestamp as a
    hard deadline. This synthesis is recorded so the underlying conflict can be resolved on its
    own merits, not on a clock. PROMOTES/DEMOTES NOTHING; SD-076 and MECH-204 both stay
    candidate/v3_pending. Routed: /queue-experiment for a redesigned H2 probe varying STEPS_PER_EP
    (not requeued this session given the tight timeline); no further /lit-pull needed (H3's is
    complete).

    [2026-08-01 governance continued, confirmed failure_autopsy_V3-EXQ-860_2026-08-01 +
    failure_autopsy_V3-EXQ-864_2026-08-01]: the redesigned H2 probe flagged above as "not requeued
    this session" HAS now run, as V3-EXQ-860 (STEPS_PER_EP 200->1000, N_TRAIN_EPS held at 794a's
    original 30 to remove the F1-firing-count confound 853 carried). Result: closure toward the
    smoke target collapses to ~0% (LO 2.9%, HI 2.5%) vs 853's confounded 12-22% at the same total
    exposure -- a clean controlled elimination. H2 (insufficient exposure) is WEAKENED more strongly
    than 853 alone showed; by elimination this STRENGTHENS H1 (F1-interaction damping) as the
    better-supported explanation. Full detail on SD-076's own evidence_quality_note, not duplicated
    here. SEPARATELY, 860 also surfaced a new, undiagnosed wrong-signed inflation_lowers_rv puzzle;
    V3-EXQ-864 (commissioned to instrument it) found the puzzle remains UNRESOLVED -- its
    steps_per_ep sweep never actually varied real exposure because CausalGridWorldV3's own
    episode-termination condition fires far short of any swept value (test-design defect,
    non-blocking, non_contributory). Net: H2 is now the weakest-supported of the three hypotheses;
    H1 is the leading candidate by elimination; H3 stays weakened-not-eliminated. This moves the
    EVB-0454 decision toward a leading candidate but does not singlehandedly resolve
    retain/hybridize/retire -- left open this cycle per user instruction. PROMOTES/DEMOTES NOTHING;
    MECH-204 stays candidate/v3_pending.
  epistemic_category: standard
  what_would_answer: |
    MECH-204 states a MECHANISM but has never stated its own falsification
    conditions -- not the DV, not the required drift source, not the timescale at
    which the correction must act. Every test to date has had to invent all three,
    and each invention was wrong in a DIFFERENT way (EXQ-541 within-cycle no-op;
    541a/541b under-powered timescale; V3-EXQ-596/602 scored on a cluster criterion
    that is not a MECH-204 prediction; V3-EXQ-774 tautological DV; V3-EXQ-794
    clamped drift source). That signature diversity is a SPECIFICATION gap, not a
    granularity gap -- /claim-synthesis 2026-07-22
    (claim_synthesis_MECH-163_2026-07-22.md sec 1c) REFUSED to decompose MECH-204 on
    exactly this ground: classifying the cluster by the autopsies' own
    four_layer_diagnosis yields ZERO non-degenerate substrate-ready FAIL signatures
    and not one target carries `weakened`. This block supplies the three missing
    objects so they are not re-invented a sixth time.

    (a) THE DRIFT SOURCE -- now SD-076 (`use_waking_confidence_inflation`),
    registered separately 2026-07-20 because it is the SOURCE while MECH-204 is the
    CORRECTION, and the two must be independently ablatable. A MECH-204 test is
    ADMISSIBLE ONLY WITH AN ACTIVE, DEMONSTRATED DRIFT SOURCE. "Sleep recalibrates
    waking precision drift" is unfalsifiable if waking precision cannot drift, so a
    null measured without live drift is a TAUTOLOGY, not evidence (V3-EXQ-774). The
    drift must be shown live IN THE RUN -- a strict dose separation on the drift
    variable between arms -- BEFORE the correction is scored. SD-076 currently has
    NO usable headroom: V3-EXQ-794 pinned `rv_final` at exactly 0.010000 on all four
    inflation arms, with `overconfidence_score` bit-identical to 15 significant
    figures (-1.004111904519277) at the LO and HI asymmetry levels. MECH-204 is
    therefore NOT ANSWERABLE until `sd_waking_confidence_inflation_headroom` lands
    (separate /implement-substrate chip), followed by the V3-EXQ-794a re-run.

    (b) THE PRECISION DV, AND THE NON-TAUTOLOGY BURDEN. ANY proposed DV must be
    shown non-tautological BEFORE it is used. The paid lesson is V3-EXQ-774: a
    SYMMETRIC precision EMA makes `running_variance` track true prediction error BY
    CONSTRUCTION, pinning `overconfidence_score` near zero regardless of what is
    ablated (774 measured -0.000148 and -0.000918). The DV must be a quantity whose
    value can be driven materially away from the calibrated point by the drift
    source ALONE, with the correction ablated. Discharge the burden in-run by
    showing (i) the drift-on / correction-off arm moves the DV materially away from
    calibration, and (ii) two drift doses produce DIFFERENT DV values beyond float
    noise -- identical values at two nominally different doses are a SATURATION
    fingerprint, not a null (V3-EXQ-794). The Phase 7 / Option B write-site in
    PRECISION space (not E3-score space) is correct and must not be revisited; it is
    what escapes the V3-EXQ-604c DV-invariance class.

    (c) THE MINIMUM TIMESCALE IS RESOLVED, NOT OPEN: >= 16 sleep cycles per arm.
    Measured -- do not re-derive it. At 4 cycles (541a/541b) cross-arm rv divergence
    tops out at 1.75% against a 5% bar (inconclusive_timescale: the rv EMA damping
    overwhelms the per-cycle nudge). At 16 cycles (541c; sleep_loop_K=1,
    EPISODES_PER_RUN=16, precision_zero_point_ema_alpha=0.1) divergence is monotone
    in step size -- 0.90% / 1.81% / 4.51% / ~10% at step 0.05 / 0.10 / 0.25 / 0.50 --
    and step=0.50 clears the 5% C4 bar in 3/3 seeds, with C1-C4 PASS overall. A
    MECH-204 run at fewer than 16 cycles per arm is under-powered BY MEASUREMENT and
    its null carries no weight.

    ANSWERS MECH-204 (5-HT withdrawal actively CALIBRATES the zero-point, not merely
    permits recalibration) if, with the drift source demonstrably live and the DV
    shown non-tautological, at >= 16 cycles per arm the recalibration-ON arm returns
    the precision DV toward the cumulative zero-point reference relative to the
    drift-only arm, with a monotone dose-response in
    `rem_precision_recalibration_step` and sign consistency across seeds.

    REFUTES MECH-204 if, under those same admissibility conditions, the correction
    fires every cycle (C1) and adjusts with the correct sign (C2) yet leaves the
    drifted DV statistically indistinguishable from the drift-only arm across the
    defensible step range {0.05, 0.10, 0.25} -- i.e. 5-HT withdrawal is PERMISSIVE
    only, and MECH-123 needs no zero-point term.

    DOES NOT ANSWER IT EITHER WAY: any run whose overall outcome is decided by a
    cluster-integration criterion that is not a MECH-204 prediction. V3-EXQ-596/602
    FAILED on cluster_live_frac, a MECH-285 / GAP-3 criterion, while STRENGTHENING
    MECH-204 on its own C1+C3 (recalibration fired every cycle in 3/3 seeds; 8.4-27.6%
    rv divergence at 12 cycles).

    NO TIMESCALE CHILD CLAIM IS WARRANTED NOW -- 541c already answered it. A
    `recalibration_timescale` child becomes warranted ONLY IF, after
    `sd_waking_confidence_inflation_headroom` lands and V3-EXQ-794a runs, the
    correction fails on CYCLE COUNT again. Registering one now would mint an untested
    claim from a resolved problem.
  depends_on:
    - MECH-123
    - MECH-186
    - MECH-178
    - INV-045
  notes: >
    MECH-123 (REM precision recalibration) is currently modeled as a permissive process --
    REM allows precision priors to reset. But the mechanism by which the recalibration target
    is established is unspecified. This claim proposes that tonic 5-HT withdrawal during REM
    (dorsal raphe quiescence) actively sets the zero-point: by removing the benefit gradient
    that biases waking precision, the system can recalibrate against a neutral reference state.
    This makes REM recalibration not just "allowed by" 5-HT withdrawal but "calibrated by" it.
    Connects to INV-045 (phase ordering): SWS must precede REM because consolidation requires
    the benefit gradient to be active (tagging), while recalibration requires it to be absent
    (zero-point). The serotonergic state transition (high tonic -> withdrawal) is part of what
    implements the SWS-to-REM computational boundary. Also connects to MECH-178: if
    noradrenergic tone suppresses REM (blocking 5-HT withdrawal), then both the replay-salience
    tagging (MECH-203) and the recalibration zero-point (this claim) are disrupted -- a
    double hit explaining why noradrenergic-mediated REM suppression produces rigidity rather
    than just reduced recalibration. Predicts: pharmacological 5-HT agonists during REM
    should impair precision recalibration quality specifically (not SWS consolidation).
- id: MECH-205
  title: "Hippocampal replay is surprise-gated and generative: episodes with high unexplained prediction error are prioritized, replayed with counterfactual variations via E2, and deprioritized only when forward models can account for the surprise."
  claim_type: mechanism_hypothesis
  subject: hippocampal.surprise_gated_generative_replay
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_EXQ-539-540_MECH307_2026-05-17#V3-EXQ-540f"
      as_of: 2026-05-17
      verdict: superseded/measurement_design_broken_criterion
  implementation_phase: v3
  depends_on:
    - MECH-092
    - MECH-094
    - MECH-121
    - INV-049
    - SD-003
    - ARC-031
  location: docs/architecture/sleep/offline_phases.md#mech-205
  source:
    - conversation_2026-04-06_surprise_gated_replay
  evidence_quality_note: >
    Tier 1 implementation (2026-04-07): PE EMA tracking + VALENCE_SURPRISE population
    on residue field + PE-weighted surprise_weight in drive_state for replay start
    selection. Config: surprise_gated_replay=True.
    Write path fix (2026-04-09): EXQ-258 FAIL root-caused to two bugs -- (1) experiment
    script checked nonexistent _rbf_layer attr (should be rbf_field), (2) pe_ema_alpha=0.1
    tracked PE so fast surprise stayed near zero. EXQ-258a FAIL root-caused to threshold
    bug: pe_surprise_threshold=0.001 was ~53x too high vs observed mean_surprise=1.86e-5.
    Both EXQ-258 and EXQ-258a marked evidence_direction=superseded.
    EXQ-258b PASS (2026-04-09): threshold=1e-5, all bugs fixed, surprise writes fire
    correctly (64/100 steps in smoke test). Substrate confirmed operational.
    Promoted to provisional (governance 2026-04-09): 1 exp PASS (EXQ-258b) + 8 lit supports,
    0 valid opposing runs (both prior FAILs superseded), conflict_ratio=0.
    EXQ-258b second seed PASS (governance 2026-04-09-c): additional seed-set (1775722634)
    confirms P1/P2/P3 across all 3 seeds. Second independent replication of surprise-write
    infrastructure. Promoted to stable (governance 2026-04-09-c): 2 independent seed-sets
    PASS, 8 lit supports, 0 conflict, conf=0.893.
  notes: >
    MECH-092 establishes that quiescent E3 heartbeat cycles trigger hippocampal SWR-equivalent
    replay, and MECH-121 specifies the full NREM consolidation pipeline. Neither specifies
    what determines WHICH episodes are replayed or HOW MUCH replay each receives.
    This claim fills that gap: the hippocampus maintains a surprise buffer -- a priority queue
    of cached episodes ranked by unexplained prediction error magnitude (E1 sensory, E2
    motor-sensory, or E3 harm/goal error). During offline phases, the highest-surprise episode
    is popped from the buffer, and the hippocampus generates counterfactual VARIATIONS around
    it via E2 rollouts (not rote re-presentation of the raw experience). The forward models
    (E1/E2) train on these replayed variations. Replay of a given episode tapers when prediction
    error on it and its counterfactual neighbourhood drops below threshold -- a natural
    convergence-based termination criterion. This makes replay allocation adaptive: more
    surprising events receive more replay cycles.
    Three functional consequences: (1) Efficient offline compute -- replay focuses on what the
    model gets wrong, consistent with the SWR literature showing replay bias toward novel and
    reward-adjacent sequences (Olafsdottir et al. 2018). (2) Generative, not rote -- the
    variation generation is E2 running counterfactual rollouts offline, which directly trains
    SD-003 attribution capacity without acting-on-incomplete-models risk (INV-049). (3) Natural
    termination -- events that resist explanation get more cycles, which is why they consolidate
    more strongly (and clinically, why traumatic events that cannot be integrated keep replaying
    intrusively -- the termination criterion is never met, MECH-094 tag may degrade under
    sustained high-affect replay).
    When the surprising event involved harm, replay must also continue until the residue field
    (viability map) updates to reflect the newly learned danger. This means the replay target
    is not purely forward-model prediction error but also includes residue field update
    completeness -- the driver is surprise, but the scope includes both model correction and
    map consolidation.
    MECH-094 hypothesis tag is critical: every generated variation must carry the simulation
    flag. Tag loss during generative replay = confabulation (replayed variations remembered as
    real). Tag loss on high-affect replays = PTSD intrusion mechanism (the replay loses its
    "this is simulation" marker).
    Architectural implication: the surprise buffer is a distinct structure from the viability
    map -- it is a replay SCHEDULING structure, not a spatial map. It lives in the hippocampal
    module and is populated during waking (high prediction error events cached with their
    error magnitude) and consumed during offline phases.
    Predicts: (1) replay frequency of an episode correlates with its initial prediction error
    magnitude; (2) replay of an episode decreases as the forward models improve on it;
    (3) blocking replay of high-surprise episodes (e.g. targeted memory reactivation of
    low-surprise items) should impair model correction for the blocked episodes specifically.
    Contrastive causal structure of replay (2026-04-07 refinement): the generative variations
    are not sampled arbitrarily -- they are CONTRASTIVELY paired with the anchor episode.
    The anchor is the surprising event (e.g. "I was hurt here" or "I reached the goal here
    unexpectedly"). The variations are nearby episodes where the surprising outcome DID NOT
    occur. The learning signal is the feature contrast: what distinguishes (context, action)
    pairs that produced the surprise from those that did not? This is offline causal inference,
    not just prediction-error minimisation. The system is asking "which antecedent features
    are causally upstream of this outcome?" rather than just "can I predict this outcome better?"
    This sharpens the termination criterion: replay does not stop when prediction error drops
    but when the causally relevant features have been extracted -- i.e. when the model can
    reliably classify surprising vs non-surprising variations by their input features.
    The contrastive structure is directionally asymmetric by valence: harm surprises generate
    variations where harm does not occur ("what would I have had to do differently to avoid
    this?") -- result writes to the avoidance/residue map. Goal surprises generate variations
    where the goal is not reached ("what were the lucky features I should exploit?") -- result
    writes to the approach map. Same contrastive machinery, different downstream targets.
    This connects directly to ARC-031 (approach-avoidance symmetry) and implements SD-003
    offline: the hippocampus runs the counterfactual population E2(z_t, a_cf) across many
    variations simultaneously, yielding a richer causal signature than the single online
    comparison. The anchor episode is the observed outcome; the variation population is the
    counterfactual reference set; the extracted features are the offline causal attribution.
    Refinement C (2026-04-07, Gupta 2010 + Stoianov 2022): the generative variations are
    produced by the hippocampus's own hierarchical sequential model (items -> sequences ->
    maps), not by arbitrary E2 rollouts. The hippocampus recombines experienced trajectory
    segments (forward + reverse) at the sequence level to compose structurally plausible
    counterfactuals that were never directly traversed. E2 provides the transition dynamics;
    the hippocampal sequence model provides the structural constraints that keep variations
    coherent with the learned map. The "what if" variations are hippocampally-constrained
    recombinations, not unconstrained rollouts.
    Refinement D (2026-04-07, Mattar & Daw 2018): replay priority is not raw PE magnitude
    but PE x viability-relevance -- the product of gain (how much does updating this state
    change the agent's policy?) and need (how likely is this state to be relevant to the
    current viability landscape?). A highly surprising event in a region the agent will
    never revisit has zero need and is not prioritised despite high gain. The surprise buffer
    is therefore a priority queue on PE x relevance, not PE alone. This is a tighter and
    more normatively justified criterion than raw PE magnitude.

# Registered 2026-04-07 -- CA1 comparator as surprise buffer write mechanism
# Source: Duncan 2011 + Bein 2020 lit-pull synthesis for MECH-205
- id: MECH-206
  title: "CA1 acts as a PE-proportional comparator that writes episodes to the surprise buffer: mismatch between CA3 prediction and entorhinal actual input produces a graded signal that tags the episode for offline replay and suppresses retrieval of the now-erroneous prediction."
  claim_type: mechanism_hypothesis
  subject: hippocampal.ca1_surprise_buffer_write_mechanism
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  depends_on:
    - MECH-205
    - MECH-092
    - SD-003
  location: docs/architecture/sleep/offline_phases.md#mech-206
  source:
    - evidence/literature/targeted_review_mech_205/entries/2026-04-07_mech_205_ca1_mismatch_detector_duncan2011/record.json
    - evidence/literature/targeted_review_mech_205/entries/2026-04-07_mech_205_mnemonic_pe_hippocampal_states_bein2020/record.json
  notes: >
    MECH-205 posits a surprise buffer (priority queue of high-PE episodes) but does not
    specify the biological mechanism by which episodes are tagged and written to that buffer.
    This claim fills that gap: CA1 is the write mechanism.
    CA1 receives two simultaneous inputs: CA3's pattern-completed prediction of the current
    context (what was expected), and entorhinal cortex's representation of the actual
    incoming experience (what happened). CA1 activity reflects the degree of discrepancy
    between these two channels -- automatically, graded, and proportionally (Duncan et al.
    2011: CA1 tracks total number of changes linearly regardless of task relevance).
    When mismatch is high, a bidirectional connectivity shift occurs (Bein et al. 2020):
    CA1-entorhinal connectivity increases (encode the surprising input) while CA1-CA3
    connectivity decreases (suppress retrieval of the now-erroneous prediction). This
    implements the surprise buffer write in two steps: (1) the episode is tagged with its
    PE magnitude via CA1 mismatch signal; (2) retrieval of the old prediction is suppressed
    to prevent the erroneous model from continuing to drive behaviour.
    The graded, linear sensitivity is critical: the surprise buffer needs a continuous PE
    magnitude readout to rank episodes for replay priority. A binary match/mismatch flag
    would not support the priority ordering MECH-205 requires. CA1's graded mismatch signal
    is exactly the right quantity.
    During SWR-driven offline replay, the same CA1 comparator logic operates in the
    generative direction: CA3 pattern-completes the anchor episode; hippocampally-generated
    variations arrive via entorhinal-equivalent input; CA1 computes the structural difference
    between anchor and each variation. This is the contrastive comparison signal that drives
    causal feature extraction (MECH-205 contrastive structure). The CA1 mismatch signal
    during replay identifies which features distinguish anchor from variation, providing the
    learning signal for forward model correction.
    REE architectural implication: the HippocampalModule needs a CA1-equivalent comparator
    submodule receiving (i) CA3-like pattern-completed anchor representations and (ii)
    entorhinal-like actual/variation representations, with output being a graded PE signal
    that writes to the surprise buffer priority queue.
    Predicts: (1) disrupting CA1 activity specifically (not CA3 or DG) should impair surprise
    buffer population while leaving forward replay intact; (2) the CA1 mismatch signal during
    replay should correlate with which features the agent subsequently treats as causally
    relevant to the surprising outcome.

# Registered 2026-04-07 -- cholinergic write-gate on surprise buffer
# Source: Sinclair 2021 lit-pull synthesis for MECH-205
- id: MECH-207
  title: "Acetylcholine acts as a permissive write-gate on the surprise buffer: prediction errors trigger hippocampal representation destabilisation and offline memory updating only when basal forebrain cholinergic activation co-occurs."
  claim_type: mechanism_hypothesis
  subject: hippocampal.cholinergic_surprise_buffer_gate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  depends_on:
    - MECH-205
    - MECH-206
    - MECH-178
  location: docs/architecture/sleep/offline_phases.md#mech-207
  source:
    - evidence/literature/targeted_review_mech_205/entries/2026-04-07_mech_205_prediction_error_memory_sinclair2021/record.json
  notes: >
    MECH-205 treats the surprise buffer as populated by any high-PE episode. Sinclair et al.
    2021 (PNAS) show that prediction errors trigger hippocampal destabilisation and memory
    updating ONLY when basal forebrain cholinergic activation co-occurs. Without ACh, the PE
    signal does not make memories labile for updating, even if the PE is large.
    This implies a neuromodulatory write-gate on the surprise buffer: ACh is the permissive
    signal that determines which PE-tagged episodes actually get written to the buffer. PE
    alone is necessary but not sufficient -- the agent must be in an ACh-permissive state
    (aroused, attending) for the episode to be cached.
    This explains: (1) inattentional learning failure -- you can be objectively surprised
    (high PE) and fail to update if ACh is low (drowsy, inattentive); (2) novelty enhances
    learning -- novel contexts elevate ACh (basal forebrain is strongly novelty-responsive),
    opening the write-gate for PE episodes encountered in that context; (3) the 24-hour
    delay in updating (Sinclair 2021) is consistent with offline sleep-phase consolidation
    of ACh-gated cached episodes (MECH-205 / MECH-121).
    Clinical implication: ACh depletion (as in Alzheimer's disease, where basal forebrain
    is an early casualty) closes the surprise buffer write-gate even when the CA1 comparator
    (MECH-206) generates normal PE signals. The forward models accumulate PE without
    updating -- the patient registers that something is wrong but cannot integrate the
    correction. A plausible mechanism for confabulation and inertia-of-false-belief in
    early AD.
    Two-gate architecture: NE controls when offline replay occurs (via REM gating, MECH-178);
    ACh controls which episodes are eligible for offline updating (surprise buffer gate). They
    are independent axes: REM suppression (high NE) blocks the offline phase; low ACh
    at encoding blocks which episodes enter the buffer in the first place. Both gates must
    be functional for a PE episode to reach offline correction.
    Implementation_phase v4: requires neuromodulatory layer in hippocampal module.
    Predicts: (1) ACh blockade at time of PE prevents offline updating of that episode even
    if replay occurs; (2) ACh agonists at PE time enhance offline updating; (3) in AD,
    PE-to-updating delay is disproportionately impaired relative to PE detection.

# Registered 2026-04-07 -- valence-asymmetric replay and anxiety mechanism
# Source: McFadyen 2023 lit-pull synthesis for MECH-205
- id: MECH-208
  title: "Valence-asymmetric replay causally drives approach/avoidance bias: harm-path replay is weighted relative to reward-path replay by the agent's harm-salience parameter, and chronically elevated harm-weighting produces avoidance bias in safe contexts."
  claim_type: mechanism_hypothesis
  subject: hippocampal.valence_asymmetric_replay_approach_avoidance
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  depends_on:
    - MECH-205
    - ARC-031
    - MECH-186
    - MECH-124
  location: docs/architecture/sleep/offline_phases.md#mech-208
  source:
    - evidence/literature/targeted_review_mech_205/entries/2026-04-07_mech_205_valence_asymmetric_replay_mcfadyen2023/record.json
  notes: >
    MECH-205 claims the contrastive replay structure is valence-asymmetric (harm surprises
    write to avoidance map, goal surprises write to approach map). McFadyen et al. 2023
    (Nature Neuroscience) show this asymmetry is not merely structural but causally drives
    downstream decisions, and is modulated by individual differences in harm-weighting.
    Replay of punishment paths was boosted relative to reward paths before avoidance
    decisions, and attenuated before approach decisions. Trial-by-trial bias toward
    punishment-path replay predicted irrational approach decisions -- excess threat evidence
    tipped the decision toward avoidance even in safe contexts. The effect was stronger in
    high trait-anxiety individuals.
    In REE terms: the harm-salience parameter (z_beta / residue field weighting) modulates
    the valence asymmetry in replay. Elevated harm-salience causes punishment-path replay
    to dominate, systematically biasing the contrastive comparison toward harm-relevant
    features. The agent extracts harm-predictive features from a larger fraction of its
    replay budget than is warranted by actual harm probability -- a miscalibration that
    generalises to safe contexts, producing avoidance of situations that do not warrant it.
    This is the replay-mediated anxiety mechanism: not that anxious agents perceive more
    harm in the moment, but that they replay harm-relevant paths more heavily during offline
    consolidation, installing a harm-weighted cognitive map that biases future decisions.
    The mechanism is self-reinforcing: elevated harm-weighting -> more harm-path replay ->
    more harm-predictive feature extraction -> more avoidance -> fewer approach experiences
    -> less corrective evidence -> maintained harm-weighting. This is the consolidation-
    mediated option-space contraction of MECH-124, specified here as operating via the
    valence asymmetry in replay content.
    Connects to MECH-186 (psychiatric failure mode cluster). The beta-blocker-before-sleep
    prediction follows directly: propranolol reduces NE-mediated harm salience during
    consolidation, rebalancing the replay ratio toward reward paths, attenuating avoidance
    bias. Consistent with the literature on propranolol and fear memory reconsolidation.
    Predicts: (1) disrupting harm-path replay specifically should attenuate avoidance
    behaviour in anxious agents without affecting approach in safe contexts; (2) the ratio
    of harm-path to reward-path replay during offline phases predicts trait anxiety scores;
    (3) interventions rebalancing the replay ratio (exposure therapy, beta-blockers pre-sleep)
    reduce avoidance bias by correcting the harm-weighted cognitive map installed by
    asymmetric replay.

# Registered 2026-04-07 -- dream phenomenology as window onto replay type
# Source: Daniel Golden first-person observation; three-type dream taxonomy
- id: INV-062
  title: "Dream phenomenology is a first-person window onto active offline sleep process: four structurally distinct dream types map onto four computationally distinct sleep functions -- harm-contrastive replay, goal-contrastive replay, NREM semantic consolidation, and REM world-model free-running."
  claim_type: derived_prediction
  prediction_domain: dream_phenomenology
  subject: dream_phenomenology.replay_type_signature
  polarity: asserts
  status: candidate
  epistemic_category: out_of_domain
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  phase_provenance: derived
  phase_derived_from: MECH-213
  depends_on:
    - MECH-205
    - MECH-208
    - MECH-210
    - INV-049
    - MECH-121
    - MECH-123
  location: docs/architecture/sleep/offline_phases.md#inv-062
  source:
    - docs/thoughts/2026-04-07_dream_phenomenology_replay_types.md
    - evidence/planning/thought_intake_2026-04-07_dream_phenomenology_replay_types.md
  notes: >
    Reclassified from claim_type: invariant in Session C batch 2026-04-17.
    Rationale: this is a phenomenological prediction about the structure of human
    dream experience; it holds only conditional on REE correctly modelling human
    offline cognition -- a domain-modelling assumption distinct from REE's
    architectural invariants (per Daniel's 2026-04-17 objection, audit registry
    INV-062).
    Four structurally distinct dream types observed consistently by DG (first-person
    phenomenological data; noted as definite types but not claimed to be exhaustive):
    Type 1 (stress/escape frustration): trying to get away from something; everything
    that could go wrong does go wrong. No resolution.
    Type 2 (joy/goal frustration): trying to reach a goal; everything that could go wrong
    does go wrong. No resolution. Affect is different from Type 1 (approach vs avoidance)
    but the systematic-failure structure is identical.
    Type 3 (procedural superposition): one thing experienced from all angles, all scales,
    and all times simultaneously -- opening a door that is all kinds of doors at once; a
    cherry blossom tree blooming experienced from all perspectives, scales, and moments at
    once. No agent, no goal, phenomenologically peaceful.
    Type 4 (detailed story / close to reality): coherent narrative with full story arc;
    close to real-world plausibility; novel situations not directly experienced; super
    interesting. An agent present in a coherent world where things follow causally from
    each other.
    Four-type mapping to computational processes:
    Types 1 and 2: harm-contrastive and goal-contrastive replay (MECH-205) not converging.
    The systematic-failure structure is the generative model exhaustively sampling the
    failure space -- variations that all hit the failure condition. Frustration IS the
    convergence failure signal; non-resolution is a phenomenological report that the replay
    loop is still running. Mirror images by valence (MECH-208 / ARC-031).
    Type 3: NREM SWR semantic consolidation (MECH-121 / MECH-209). Rapid SWR sequence
    collapses to phenomenological simultaneity. Self-dissolution = episodic context
    stripped during abstraction. Multi-scale = hierarchical model across levels. Peaceful
    because there is no convergence criterion -- building a schema, not solving a problem.
    Type 4: REM E1 world model free-running (MECH-210 / MECH-123). Sensory input
    disconnected, commit gate suppressed; E1 LSTM runs forward unconstrained on its learned
    prior. Sequential LSTM dynamics produce narrative arc. "Close to reality" = in-
    distribution samples from a well-calibrated world model. "Super interesting" = E1
    surfacing novel-but-coherent patterns from its prior that the agent hasn't consciously
    accessed -- world model hypothesis testing. Agent present in a coherent world because
    E1 is a sequential agent-world model, not a categorical schema extractor.
    Cross-type comparison: Types 1/2 and 4 all have an agent; Type 3 does not. Types 1/2
    have no narrative resolution (convergence criterion unmet); Type 4 has narrative arc
    (no convergence criterion -- free generation). Type 3 has no agent and no narrative
    (schema building). The presence/absence of agent and resolution criterion cleanly
    discriminates all four.
    Clinical implications: (1) Recurring non-resolving Type 1 = convergence failure on
    harm-contrastive replay; PTSD marker; should resolve with successful therapy. (2)
    Absence of Type 3 = possible MECH-121 failure (episodic->semantic transfer impaired).
    (3) Absence of Type 4 or degraded Type 4 (incoherent, implausible stories) = E1
    world model poorly calibrated or REM architecture disrupted (MECH-123 / MECH-178).
    (4) Anxious individuals (elevated harm-weighting, MECH-208) predicted to have
    predominantly Type 1 with reduced Type 3 and Type 4 -- replay budget crowded by
    harm-contrastive search.
    Epistemic note: first-person phenomenological data from one observer. Taxonomy
    observed, not derived. The mapping to computational processes is an interpretation
    requiring corroboration from other observers and sleep-stage measurement.
  what_would_answer: >
    This claim fuses two evidentiary legs with different domains of resolution.
    They must be evaluated separately; a result on one leg does not confirm or
    falsify the other.

    REE-INTERNAL LEG (testable in V3, in principle -- NOT yet instrumented):
    Do the sleep/replay mechanisms named by this claim's dependency set --
    MECH-205 (harm/goal-contrastive replay), MECH-121 (NREM SWR consolidation),
    MECH-123 (REM world-model free-running), plus MECH-208 (valence-asymmetric
    replay weighting) if/when built -- produce four COMPUTATIONALLY
    DISTINGUISHABLE signatures when each is run and logged separately?
    Candidate discriminators: representational entropy profile; presence vs.
    absence of a convergence criterion; presence vs. absence of a tracked
    "agent" trajectory vs. a category-level "schema" abstraction.

      NON-DEGENERACY PRECONDITION: all four named mechanisms must be built and
      independently triggerable in the same substrate build. Confirmed
      2026-08-08: MECH-205/121/123 are live in ree_core. MECH-208 has ZERO
      references anywhere in ree-v3 and is not built -- the precondition is
      currently UNMET for the full four-way split; MECH-205/121/123 alone
      support only a three-way split. No experiment or diagnostic in ree-v3
      references INV-062 today.

      CONFIRMING: once instrumented, running each mechanism in isolation yields
      four distinct, reproducible signatures matching the claimed discriminating
      structure.

      FALSIFYING: once instrumented and MECH-208 built, the mechanisms produce
      signatures that are NOT reliably distinguishable by these criteria.

    OUT-OF-DOMAIN LEG (not resolvable by REE, by any experiment this repository
    can run): whether DG's specific four-type first-person dream taxonomy is a
    real, stable, corroborated human phenomenon -- as opposed to an
    idiosyncratic or post-hoc pattern from a single, unsystematically-sampled
    observer. Resolves only through corroboration from other observers' dream
    reports against the same schema, and/or polysomnography co-registered with
    dream reports at awakening. No V3 build, however complete, converts this
    leg into a REE-testable question.

    DISPOSITION: this claim as a whole carries epistemic_category: out_of_domain,
    since its title's primary assertion (the taxonomy itself) is the
    out-of-domain leg. The REE-internal leg above is a genuinely different,
    standard-testable sub-question, currently gated by the precondition, not
    the confirming/falsifying step -- it is complicated (buildable): MECH-208
    needs to be built and signature instrumentation added, neither of which is
    scientifically unknown. RECOMMEND (not executed here): split the
    REE-internal leg into its own mechanism_hypothesis claim so it can be
    tested independently of whether DG's taxonomy is ever corroborated.

    2026-08-10 edge correction (chip chip-20260810-phase-consistency-reconciliation):
    depends_on: MECH-209 removed -- reversed edge. MECH-209 itself calls INV-062 its
    "parent" (own what_would_answer) and "adds no independently testable REE-substrate
    content on top of" MECH-121; it is a specialization of INV-062's taxonomy, not a
    prerequisite. INV-062's own REE-internal-leg falsifier names MECH-205/121/123/208,
    never MECH-209.

# Registered 2026-04-07 -- Type 3 dream as semantic consolidation phenomenology
# Source: Daniel Golden first-person observation; MECH-121 lit synthesis
- id: MECH-209
  title: "The simultaneous multi-instance, multi-scale, multi-time phenomenology of Type 3 dreams is the experiential signature of NREM SWR episodic-to-semantic transfer: rapid hippocampal sequential presentation of concept instances collapses to phenomenological simultaneity, with self-dissolution reflecting the stripping of episodic context during abstraction."
  claim_type: mechanism_hypothesis
  subject: sleep.semantic_consolidation_phenomenology
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  phase_provenance: assigned  # 2026-08-10 correction (chip
                              # chip-20260810-phase-consistency-reconciliation): was
                              # derived/phase_derived_from: INV-062, but that edge ran
                              # backwards (see INV-062.what_would_answer correction).
                              # MECH-209 stands as v3 on its own real dependencies
                              # (MECH-121, MECH-092, INV-062, INV-049 -- all v3/built).
  epistemic_category: out_of_domain
  depends_on:
    - MECH-121
    - MECH-092
    - INV-062
    - INV-049
  location: docs/architecture/sleep/offline_phases.md#mech-209
  what_would_answer: >
    This claim fuses THREE evidentiary legs of different domains of resolution, following
    the precedent recorded on its own parent, INV-062. A result on one leg does not
    confirm or falsify another.

    REE-MECHANISM LEG (deferred -- do not duplicate MECH-121's own falsifier): this
    claim's computational content (rapid hippocampal SWR sequential replay of concept
    instances, compressed to sub-conscious timescale, delivered to E1 for schema
    abstraction) IS MECH-121's mechanism. This claim adds no independently testable
    REE-substrate content on top of it -- its test is MECH-121's own NON-DEGENERACY
    PRECONDITION / CONFIRMING / FALSIFYING block (substrate_conditional, held pending the
    unbuilt ARC-065 rule-creator substrate + balanced harm/goal replay scheduling).
    MECH-092's triggering half is confirmed (V3-EXQ-761 PASS) but its consolidation-
    benefit half (the half this claim actually needs) remains gated on SD-006 / a
    working viability map. Score this leg as MECH-121's, not re-litigated here.

    PHENOMENOLOGICAL-MAPPING LEG (mixed -- narrow buildable sub-question + irreducible
    residue): NARROW SUB-QUESTION (in principle buildable, currently unbuilt): does REE
    have any internal state whose behaviour during a replay/consolidation event would
    look like what this claim predicts for "self-dissolution" -- a measurable drop or
    discontinuity in self-model coherence (z_self/D_eff, already used elsewhere for
    depersonalization/dissociation claims) specifically during MECH-121-style replay,
    versus stability during ordinary online operation? NOT currently instrumented:
    MECH-121's own depends_on has no self-model linkage, and no architecture doc tracks
    an episodic who/when/where context tag on replayed traces that could be observed
    being stripped. Building this requires MECH-121's own substrate unblocked AND new
    z_self/D_eff wiring into the replay pipeline -- neither exists today. CONFIRMING (if
    built): z_self/D_eff shows a measurable coherence drop specifically during
    MECH-121-triggered replay windows, tracking with the volume of episodic content being
    abstracted. FALSIFYING (if built): coherence is statistically unchanged across
    replay vs. ordinary operation. IRREDUCIBLE RESIDUE (out of domain, permanently): even
    a confirmed computational signature would only show REE's self-model measure BEHAVES
    analogously to a self-dissolution reading -- it would not establish that this
    computational event IS what "self-dissolution" subjectively feels like from the
    inside. No REE build converts this residue into a testable REE question.

    HUMAN-CLINICAL LEG (out of domain -- independently testable, but not on REE): this
    claim's own clinical prediction -- anxious patients (elevated MECH-208 harm-
    weighting) show predominantly Type 1/2 dreams and reduced/absent Type 3 -- resolves
    only through prospective dream-diary studies against this taxonomy, not through any
    REE experiment. MECH-208, the mechanism this prediction leans on, is CONFIRMED
    UNBUILT in ree-v3 as of 2026-08-08 -- so even the REE-side precondition for
    eventually cross-referencing this prediction against a matching REE signature does
    not yet exist.

    DISPOSITION: epistemic_category is out_of_domain for the same reason as parent
    INV-062 -- the title's primary assertion is an identity claim between a specific
    phenomenology and a specific computational mechanism, which is the phenomenological-
    mapping leg's irreducible residue above, not the REE-mechanism leg (MECH-121's,
    deferred) or the human-clinical leg (dream-diary literature, not REE).

    GOVERNANCE FLAG (2026-08-08, not resolved here): this claim's claim_type
    (mechanism_hypothesis) could arguably be reclassified to derived_prediction to match
    parent INV-062's own reclassification (phenomenological-identity claims are
    domain-modelling assumptions, not architectural invariants/mechanisms) -- surfaced
    for governance judgment, not applied here.
  source:
    - docs/thoughts/2026-04-07_dream_phenomenology_replay_types.md
    - evidence/planning/thought_intake_2026-04-07_dream_phenomenology_replay_types.md
  notes: >
    MECH-121 describes NREM SWR replay as the mechanism for episodic-to-semantic transfer:
    hippocampal SWR-equivalent replay coordinates with slow oscillations to deliver
    episodic content to neocortex (E1's LSTM) for schema integration. The computational
    process involves rapid sequential presentation of concept instances (compressed to
    20-90ms per state, McFadyen 2023), allowing E1 to abstract the invariant features
    across instances.
    This claim specifies what that process looks like from the inside, given any
    phenomenological access during NREM. Because the hippocampal SWR sequence operates
    at timescales far below conscious temporal resolution, the sequence collapses to
    simultaneity at the phenomenological level: all instances of the concept are present
    at once, rather than being experienced one after another.
    Concrete examples from DG's experience: (1) Opening a door experienced as all kinds
    of doors at once -- all instances of door in memory simultaneously present. (2) A
    cherry blossom tree blooming experienced from all angles, all scales (petal to branch
    to tree to forest), and all moments of blooming at once.
    Two structural features characterise Type 3: (i) MULTI-DIMENSIONALITY -- all angles,
    all scales, all times simultaneously, not a sequence; (ii) SELF-DISSOLUTION -- no
    agent trying and failing, no goal, no frustration. The dreamer IS the superposition,
    not an observer of it or an actor within it.
    Self-dissolution maps onto the episodic context stripping that defines semantic
    consolidation: episodic memory preserves who, when, where (the self in a situation);
    semantic memory strips those and retains only the what (the category's invariant
    features). During the SWR-driven consolidation process, the episodic binding that
    anchors experience to a self-in-a-situation is exactly what is being removed. The
    phenomenological self therefore dissolves -- not because consciousness is absent, but
    because the self-context that normally grounds phenomenological perspective is the
    content being discarded.
    Multi-scale access maps onto the hierarchical generative model (Stoianov 2022):
    items -> sequences -> maps. The dreamer simultaneously accesses all levels of the
    hierarchy (petal = item, branch sequence = sequence, whole forest = map). This would
    be experienced as "all scales at once" precisely because the hierarchical model is
    being consolidated top-to-bottom in a single SWR burst.
    Multi-time maps onto the temporal abstraction required for schema formation: the
    schema for "cherry blossom blooming" must abstract across all the times the dreamer
    has observed blooming, presenting them simultaneously to E1 for invariant extraction.
    Therapeutic / clinical implication: if Type 3 dreams are the phenomenological signature
    of healthy semantic consolidation, then their absence (or displacement by Type 1/2
    dreams crowding the replay budget) would indicate impaired episodic->semantic transfer.
    In chronic anxiety (MECH-208): elevated harm-weighting allocates excessive replay
    budget to harm-contrastive search (Type 1), leaving insufficient budget for semantic
    consolidation (Type 3). Clinically: anxious patients may report predominantly Type 1/2
    dreams and rarely Type 3 -- a testable prediction using prospective dream diaries.
    Connects to MECH-124 (consolidation-mediated option-space contraction): if harm-path
    replay dominates the budget, not only does the avoidance map get over-consolidated,
    but the semantic abstraction that would provide generalisable escape/approach schemas
    gets under-consolidated.

# Registered 2026-04-07 -- Type 4 dream as REM E1 world-model free-running
# Source: Daniel Golden first-person observation; MECH-123 synthesis
- id: MECH-210
  title: "Type 4 (detailed story, close to reality, super interesting) dreams are the phenomenological signature of REM E1 world-model free-running: with sensory input disconnected and the commit gate suppressed, E1's LSTM generates unconstrained sequential rollouts from its learned prior, producing coherent narrative arcs that are novel-but-plausible and experientially interesting."
  claim_type: mechanism_hypothesis
  subject: sleep.rem_e1_world_model_free_running
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  phase_provenance: derived
  phase_derived_from: MECH-213
  epistemic_category: substrate_conditional
  depends_on:
    - MECH-123
    - MECH-094
    - MECH-178
    - INV-049
    - INV-062
  location: docs/architecture/sleep/offline_phases.md#mech-210
  what_would_answer: >
    NON-DEGENERACY PRECONDITION (REE-architecture leg): a genuine joint "REM mode" --
    E1's LSTM running forward generatively (predict_long_horizon, prediction_horizon=20,
    ree_core/predictors/e1_deep.py) from an internally-generated starting point with NO
    live sensory correction, WHILE ARC-016's commit gate is suppressed (commitment_threshold
    driven low/negative) AND monoaminergic precision is reduced live during the rollout
    (not only as a post-sleep one-shot adjustment) -- does not currently exist as an
    assembled, invocable mode in ree_core/. predict_long_horizon is never called from
    ree_core/sleep/; the existing REM_ANALOG phase scaffolding (agent.py
    enter_rem_mode/run_rem_attribution_pass, ree_core/sleep/routing_gate.py) does
    hippocampal REPLAY of stored past trajectories, not E1-generative free-running of
    novel content -- it implements the Type 1/2/3 mechanism family (MECH-121/205/208/209),
    not Type 4. Before any confirming/falsifying test is meaningful, a driver must be
    built that: (a) seeds E1 from an internally-generated, not externally-observed,
    state; (b) runs predict_long_horizon with commitment_threshold overridden so no
    commit fires; (c) applies precision reduction concurrently with the rollout, not
    only afterward; (d) confirms sensory input genuinely does not reach E1 during the
    window.

    CONFIRMING: once built, E1's free-running rollouts show (i) narrative coherence --
    later predicted states measurably constrained by earlier ones, above an
    i.i.d.-shuffle-matched baseline; (ii) novelty-within-learned-prior-support --
    rollouts fall outside the specific stored-episode distribution while staying
    low-error against a held-out plausibility model; (iii) an interestingness/surprise
    proxy that tracks independently-measured world-model calibration quality (MECH-123's
    own dependency) -- well-calibrated priors produce a measurably different rollout
    signature than deliberately miscalibrated ones.

    FALSIFYING: rollouts under the jointly-induced conditions are indistinguishable from
    i.i.d. noise regardless of prior calibration quality; OR the interestingness/surprise
    signature shows no relationship to independently-measured world-model calibration;
    OR narrative coherence is present even without the commit-gate/precision
    manipulations, meaning the REM-specific framing adds nothing beyond ordinary
    free-running rollout mechanics.

    HUMAN-CLINICAL LEG (separate, non-REE-blocking): (1) REM-suppressing-antidepressant
    prediction -- SSRIs/SNRIs (and other REM-suppressing agents, per MECH-178's NA-tone
    pathway) reduce/delay REM on PSG; this claim predicts selective loss of Type 4
    (detailed-story) dreams under chronic use, with Types 1-3 relatively spared --
    testable via dream-diary studies contrasting REM-suppressing vs REM-sparing agents.
    (2) Dementia-progression prediction -- progressive Type-4 degradation preceding its
    disappearance, tracked via longitudinal dream diaries in early-stage dementia. Both
    answerable from clinical/psychiatric literature and cohort studies, independent of
    REE substrate status.

    GOVERNANCE FLAG (2026-08-08, not resolved here): this claim's direct mechanistic
    parent MECH-123 was set to substrate_conditional/v3_pending: true on 2026-08-06
    ("mirroring sibling MECH-121") -- this claim is a downstream specialization and its
    own precondition is unmet a fortiori; consider adding v3_pending: true to match. The
    REM_ANALOG phase-name collision (see NON-DEGENERACY PRECONDITION above) could mislead
    a future reader into thinking this claim is substrate-supported because a
    similarly-named phase exists -- worth a short cross-reference note in
    docs/architecture/sleep/offline_phases.md.
  source:
    - docs/thoughts/2026-04-07_dream_phenomenology_replay_types.md
    - evidence/planning/thought_intake_2026-04-07_dream_phenomenology_replay_types.md
  notes: >
    During waking, E1's LSTM is constrained by continuous sensory input -- it predicts
    the next state, receives the actual state, and updates on the error. During REM, three
    constraints lift simultaneously: (1) sensory input is disconnected (thalamocortical
    gating); (2) the commit gate is fully suppressed (ARC-016 running_variance threshold
    effectively infinite -- no committed action can occur); (3) monoaminergic tone
    (noradrenaline, serotonin) withdraws, reducing precision on prior predictions and
    allowing the model to explore more freely. E1 is thus free to run its LSTM forward
    from any internally-generated starting point, following its learned transition model
    without correction from the world. All activity is hypothesis-tagged (MECH-094).
    The result is that E1 generates what is effectively a story: a sequential, causally
    coherent rollout of world states, with the agent as a participant whose actions have
    consequences within the simulation. Because E1's prior is learned from real experience,
    the rollouts stay within the distributional support of plausible events -- they are
    "close to reality." Because they are samples from the prior rather than memories, they
    are novel -- situations and event sequences the agent has not experienced but that E1's
    world model treats as possible.
    The "super interesting" quality is the key diagnostic marker. It signals that E1 is
    surfacing novel-but-coherent structure from its learned prior: causal chains, social
    dynamics, physical sequences that the model has implicitly learned but that have not
    been consciously encountered as episodes. This is MECH-123's "world model hypothesis
    testing": E1 explores whether its current prior produces internally consistent
    trajectories. When the prior is well-calibrated, the rollouts are interesting because
    they reveal structure that was latent but not yet explicit. When the prior is
    miscalibrated, the rollouts are either boring (repetitive, low-information samples)
    or incoherent (impossible, contradictory sequences).
    Narrative arc emerges naturally from E1's sequential LSTM dynamics with
    prediction_horizon=20: the model generates extended temporal sequences in which
    earlier states constrain later ones, producing setup, development, and consequence --
    the structural signature of story. This is not imposed on the model; it is what a
    sequential world model with a long prediction horizon produces when run forward freely.
    Contrast with other dream types: unlike Types 1/2 (hippocampal contrastive replay,
    frustration from convergence failure), Type 4 has no convergence criterion -- E1 is
    generating, not searching. Unlike Type 3 (NREM semantic consolidation, self-dissolved
    superposition), Type 4 has a present agent in a sequential world -- because E1 is an
    agent-world model, not a categorical schema extractor.
    The distinction between Type 4 and Type 1/2 in terms of frustration is also
    mechanistically grounded: in Types 1/2, the hippocampus is iterating variations that
    keep failing, and the emotional content of the anchor episode (harm, blocked goal)
    sets the affective tone. In Type 4, E1 is free-running from an arbitrary starting
    point with no failure criterion -- there may be challenges in the narrative, but the
    dreamer can engage with them as interesting problems rather than frustrated obstacles,
    because there is no external convergence criterion demanding resolution.
    Clinical implications: (1) degraded Type 4 (incoherent stories, impossible events,
    implausible causality) = E1 world model poorly calibrated or REM integrity disrupted
    (MECH-123, MECH-178). (2) Absence of Type 4 in patients on REM-suppressing medications
    (e.g. many antidepressants suppress REM) is a testable prediction -- they should lose
    Type 4 while Types 1-3 may be less affected. (3) The subjective quality of Type 4
    dreams (how interesting, how coherent, how novel) may be an accessible proxy for
    E1 world model quality -- richer and more interesting stories suggesting a better-
    calibrated world model. (4) In dementia: progressive loss of world model fidelity
    (MECH-178 pathway, rigidity mechanism) predicts progressive degradation of Type 4
    towards repetitive, low-information, close-to-habit dreams before Type 4 disappears
    entirely -- a gradient that dream diaries might track ahead of other symptoms.

- id: INV-063
  title: minimum_entropy_intake_sleep_dependency
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - SD-017
  pending_substrate_reconfirmation: true
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  subject: sleep.entropy_intake
  description: >
    A minimum level of environmental entropy intake during waking is required to sustain
    all four offline sleep functions. Too little entropy collection produces specific,
    proportional sleep function failures: insufficient schema material starves NREM Type 3
    consolidation; insufficient surprising episodes starve Types 1/2 contrastive replay;
    insufficient novel event sequences starve E1 world-model updating; insufficient
    high-entropy sensory input degrades E2 motor-sequence learning. This predicts a
    psychiatric risk gradient: environments with low entropy (straight lines, routine,
    little novelty — characteristic of urban and institutional settings) systematically
    deplete the material for sleep function, producing progressive failure modes across
    anxiety, depression, and cognitive rigidity dimensions. High-entropy environments
    (nature, fractal structure, novel sensory complexity) provide the necessary raw
    material for all four functions and are predicted to be protective.
  notes: >
    Motivated by Daniel Golden's observation that nature exposure (fractal visual
    complexity, unpredictable structure) and urban deprivation (straight lines, routine,
    low novelty) differentially affect psychiatric outcomes in ways not explained by
    current accounts. The entropy-intake mechanism links waking experience quality
    to sleep function completeness: the brain cannot consolidate, replay, or recalibrate
    what it has not encountered. Minimum entropy thresholds are likely E1-specific
    (world-model schema update requires novel world states) and E2-specific (motor sequence
    learning requires action-outcome variation). This is a novel prediction that bridges
    environmental psychiatry, chronobiology, and the REE sleep framework.
  registered: "2026-04-07"
  depends_on: [INV-049, SD-017, MECH-121, MECH-205, MECH-209, MECH-210, MECH-211]
  session_c_resolution: >
    Session C batch 2026-04-17: classified emergent on SD-017, paralleling
    the sleep-cluster decisions on INV-045/046/050/062. The "entropy intake"
    concept is defined relative to sleep functions (schema consolidation,
    contrastive replay, E1 world-model updating, E2 motor-sequence learning);
    without SD-017 the four sleep functions it presupposes have no substrate.
    Added SD-017 to depends_on. SD-017 is provisional, so
    pending_substrate_reconfirmation: true.

- id: MECH-211
  title: schema_consolidation_as_search_grammar
  claim_type: mechanism
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  subject: sleep.nrem.schema_vocabulary
  description: >
    Type 3 NREM semantic consolidation (SWR-driven episodic-to-semantic transfer)
    produces the generative vocabulary that Types 1 and 2 contrastive replay searches
    draw upon for variation generation. Schemas consolidated during Type 3 sleep provide
    categorical structure — object types, action primitives, contextual frames — that
    constrains and enriches the counterfactual space sampled by E2 during Types 1/2
    replay. Richer Type 3 consolidation therefore enables a broader, better-structured
    contrastive search space in subsequent Types 1/2 episodes. Depleted Type 3
    consolidation (insufficient schema material from waking, or hippocampal
    episodic-to-semantic transfer failure) narrows the variation space, producing
    stereotyped, low-diversity replay that fails to identify the causal features
    distinguishing safe from unsafe contexts — chronic convergence failure.
  notes: >
    This makes NREM consolidation a prerequisite for contrastive replay quality,
    not merely a parallel process. Type 3 is not just semantically restful; it
    actively arms the E2 variation generator with the vocabulary for its search.
    MECH-205's convergence criterion depends on variation diversity; MECH-211
    identifies where that diversity comes from. Clinical prediction: patients with
    impaired NREM spindle activity (a marker of hippocampal-neocortical transfer)
    should show more stereotyped, narrow contrastive replay — consistent with the
    rigid, repetitive quality of trauma-related nightmares in PTSD.
  registered: "2026-04-07"
  depends_on: [MECH-205, MECH-209, INV-062]

- id: MECH-212
  title: e2_motor_sequence_sleep_consolidation
  claim_type: mechanism
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  subject: sleep.nrem.e2_consolidation
  description: >
    E2 (the fast motor-sensory transition model) undergoes dedicated NREM offline
    consolidation analogous to but distinct from E1 schema consolidation. While E1
    consolidates world-model schemas (what objects and events exist), E2 consolidates
    action-sequence schemas (how to traverse z_self transition space efficiently):
    chains of action-objects that reliably navigate from initial to target z_self
    states. This E2 NREM replay is driven by the same SWR mechanism but carries
    motor-sensorium prediction error rather than world-state prediction error.
    Disrupted E2 consolidation predicts impaired skill acquisition and
    procedural memory — the well-established sleep-dependent motor learning effect
    (Walker 2005, Fischer 2002) is the phenomenological signature of E2 NREM replay.
    In REE terms: SWRs during NREM replay high-PE E2 state transitions; the surprise
    buffer for E2 is populated by action sequences that produced unexpectedly large
    motor-sensory prediction error (clumsy, novel, or skill-boundary episodes).
  notes: >
    This is a significant gap in the current sleep architecture (MECH-120 through
    MECH-123), which is E1-centric. E2 has no dedicated offline consolidation claim
    despite being a full transition model with its own prediction error signal and
    therefore its own surprise buffer population mechanism. Motor sequence learning
    is one of the most replicated sleep-dependent memory findings; its absence from
    the REE sleep account is an architectural omission. E2's action_objects become
    the hippocampal map backbone (SD-004), so E2 consolidation may also feed the
    E3 viability map indirectly.

    CORRECTION (2026-08-08, thought-digestion): "its own surprise buffer population
    mechanism" is imprecise -- MECH-205 (a direct dependency, now status: stable)
    already specifies the surprise buffer as a single structure spanning E1 sensory,
    E2 motor-sensory, and E3 harm/goal prediction error, populated during waking
    regardless of source module. Buffer *population* for E2 is therefore already
    covered by MECH-205. The actual gap this claim identifies is the offline
    *consumption/replay* pipeline -- an E2-keyed pop-and-SWR-replay step analogous to
    MECH-121's E1 pipeline -- which remains genuinely unbuilt (verified: no E2-keyed
    PE or surprise-buffer consumption logic in e2_fast.py, replay_sampler.py, or
    phase_manager.py as of this date; the only E2-adjacent offline pass is a narrow,
    non-PE-gated MSE pass scoped to the e2_harm_s harm submodule, not the general E2
    transition model).
  epistemic_category: substrate_conditional
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: The E2 fast motor-sensory transition model
    (ree_core/predictors/e2_fast.py) must exist and produce trackable action_objects /
    z_self transition sequences (confirmed built; SD-004 implemented). MECH-121's
    NREM SWR-replay pipeline and MECH-205's surprise-buffer machinery must be
    operational for E1 (confirmed). Critically, an E2-specific offline
    consumption/replay pipeline -- one that pops high-motor-sensory-PE action-sequence
    entries and runs SWR-triggered counterfactual replay through E2 specifically (not
    the narrow e2_harm_s harm submodule, and not the MECH-423 R3 interleaved-schedule
    module, which serves a different purpose) -- is currently UNBUILT. Any test run
    before this pipeline exists will self-route substrate_not_ready rather than
    produce valid evidence either way.

    CONFIRMING: Once built, a controlled NREM-sleep condition shows selective
    improvement in z_self-transition efficiency / reduced motor-sensory prediction
    error specifically on action sequences that were flagged high-PE (clumsy, novel,
    or skill-boundary episodes) pre-sleep -- and this improvement (a) scales with
    each sequence's pre-sleep PE magnitude, (b) is measurably larger under NREM/SWR-
    replay conditions than under an equivalent-duration wake-rehearsal or generic-
    gradient-descent control, and (c) persists when E1 world-schema PE is held
    constant or matched across conditions -- i.e. the effect is not parasitic on E1
    consolidation.

    FALSIFYING: (a) No NREM-specific improvement in z_self-transition efficiency or
    procedural performance beyond what wake-state rehearsal or a generic (non-PE-
    gated) offline gradient pass already produces; or (b) any observed motor-sequence
    improvement is fully explained by E1 world-schema consolidation spillover --
    disappears once trials are matched on E1-relevant PE, with no residual E2-specific
    signal; or (c) once the E2-specific pipeline is built, ablating it produces no
    measurable procedural-memory deficit distinct from ablating E1 consolidation
    alone -- i.e. E2's contribution is not dissociable from E1's at the mechanism
    level.
  registered: "2026-04-07"
  depends_on: [MECH-205, MECH-121, INV-049]
  implementation_phase: v3

- id: MECH-213
  title: e1_e2_joint_rem_calibration
  claim_type: mechanism
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  subject: sleep.rem.joint_calibration
  description: >
    REM phase involves joint E1-E2 calibration, not E1 recalibration alone. E1
    generates world-state sequences (narrative context) while E2 simultaneously
    generates action sequences through those world states (the agent acting within
    the narrative). The two models run in a partially decoupled closed loop: E1's
    world evolution is shaped by E2's action proposals, and E2's action-value estimates
    are updated by E1's predicted world responses. The phenomenological signature of
    this joint operation is the Type 4 dream: a coherent story with an agent
    navigating it — neither pure sensory replay (E1 alone) nor pure motor sequence
    replay (E2 alone), but the joint product of both forward models running together
    without external grounding. The agentive quality of REM dreams (I am there, I
    am acting) directly reflects E2's active participation; pure E1 replay would
    produce observer-perspective narrative without first-person agency. This joint
    REM operation recalibrates the match between E1's world predictions and E2's
    action-consequence expectations, correcting systematic biases that accumulated
    during waking experience.
  notes: >
    This distinguishes REE's REM account from E1-centric accounts. MECH-123 (current
    REM recalibration claim) should be updated to note E2 participation. The
    clinical prediction: disrupted REM (sleep apnea, fragmentation, REM suppression
    by antidepressants) impairs E1-E2 joint calibration, producing systematic
    divergence between world-model predictions and motor-consequence expectations.
    This divergence may manifest as action-plan brittleness, impaired transfer of
    skills to novel contexts, or the 'known-what but not how' dissociation.
    SSRIs suppress REM; their side effect of emotional blunting or feeling
    'disconnected' from action may partly reflect impaired E1-E2 joint calibration.
  registered: "2026-04-07"
  depends_on: [MECH-123, MECH-210, MECH-212, INV-062]
  implementation_phase: v3

- id: INV-064
  title: e1_e2_e3_maturational_sequence_necessity
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-001
    - ARC-002
    - ARC-003
    - ARC-019
  pending_substrate_reconfirmation: true
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-20
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-740a_2026-07-12
      as_of: 2026-07-12
      verdict: non_contributory/measurement_degeneracy
  subject: development.maturational_sequence
  description: >
    The developmental sequence in which sensory-perceptual systems (E1) mature before
    motor-transition systems (E2) before frontal-evaluative systems (E3) is a
    computational necessity, not an anatomical accident. E3's primary inputs are
    z_world from E1 and action_objects from E2. If these inputs carry only
    poorly-differentiated, low-information representations, E3 training produces a
    noise-fitted harm/goal evaluator. The information quality available to E3 is
    strictly bounded by E1/E2 representational differentiation. Therefore productive
    E3 training cannot begin until E1 and E2 have reached sufficient schema
    differentiation. REE predicts that the biological fact of prefrontal cortex
    being the last cortical region to myelinate fully (completing mid-20s in humans)
    is architecturally required: any agent architecture with this dependency structure
    will exhibit the same maturation order.
  notes: >
    This unifies several otherwise unexplained developmental facts: why adolescent
    risk-taking persists well past sensory-motor competence (E1/E2 are largely
    mature but E3 training is still integrating), why early-life disruption has
    outsized and specific effects on executive function rather than just emotional
    regulation, and why intensive novel experience in childhood (high-entropy
    environments, play, exploratory learning) has disproportionate long-term
    cognitive returns relative to the same experience in adulthood. The
    maturational sequence is not merely a constraint to work around -- it is the
    correct curriculum order for building a competent E3.
  registered: "2026-04-07"
  depends_on: [INV-055, INV-056, ARC-001, ARC-002, ARC-003, ARC-019, MECH-212, MECH-213]
  epistemic_category: substrate_coherence
  what_would_answer: |
    INV-064's own falsifiable content is the E1->E2->E3 maturational
    ORDERING-NECESSITY claim itself (unchanged by the 2026-07-12
    decomposition), not a fresh single experiment against the umbrella.
    Do NOT queue a new direct probe against INV-064 -- the re-derive
    brake already refuses a same-observable 740b, and any direct test
    would duplicate what its children already carry. The umbrella's
    status is read off the AGGREGATE verdict of its two registered
    children, each an independently falsifiable stream-specific bound:
    INV-088 (E3 goal/world-evaluator quality bounded by z_world/E1
    differentiation) and INV-089 (E3 harm-evaluator quality bounded by
    z_harm/HarmEncoder differentiation -- itself narrowed 2026-07-16 to
    the ceiling reading, with the companion growth-coupling/driver
    reading split out to a grandchild, INV-090).

    CONFIRMS (umbrella): both INV-088 and INV-089/INV-090 individually
    reach a clean confirming verdict for their own stream-specific bound
    -- i.e. E1/E2 differentiation demonstrably gates E3 evaluator quality
    on both the world/goal stream and the harm stream, per each child's
    own decision criteria. That jointly confirms the general E1->E2->E3
    information-bottleneck mechanism this umbrella asserts, with no test
    run at the umbrella level.

    PROMPTS FURTHER REVISION/SPLIT (umbrella, distinct from a mere
    bound-quantification miss): if a child's falsifier undercuts the
    ORDERING-NECESSITY FRAMING itself rather than just its bound's
    magnitude -- e.g. a child's evaluator trains productively BEFORE its
    input stream differentiates, or a child's degeneracy is traced to a
    mechanism outside the E1/E2->E3 information-bottleneck story
    entirely. This has already happened once on the world leg:
    V3-EXQ-108b traced INV-088's evaluator degeneracy to an E1
    single-step-training/multi-step-rollout-at-eval mismatch, NOT to
    z_world's own differentiation capacity
    (epistemic_category=competence_implementation_gap, not
    substrate_ceiling) -- a competence-implementation confound, not yet a
    framing falsification, but the kind of finding this clause exists to
    catch if it recurs or sharpens.

    FALSIFIED (umbrella, partial or full): a clean, met-precondition
    falsification of EITHER child's bound (evaluator quality demonstrably
    independent of, or inversely related to, its input stream's
    differentiation) that survives re-derive-brake scrutiny refutes that
    stream's leg of the E1/E2->E3 bottleneck. Whether that falsifies the
    ordering-necessity claim as a whole or only narrows it to the
    surviving stream is a /claim-synthesis judgment at that point.

    CURRENT STATE (2026-08-07): neither child has reached a clean confirm
    or falsify. INV-088 is candidate, coupling leg real-but-weak (744a)
    then confounded with a competence-implementation-gap finding (108b),
    pending_retest_after_substrate on an unbuilt E1
    rollout-consistency-training substrate (routed to
    /implement-substrate). INV-089 is provisional on 743's positive
    control alone, its own bound-coupling test still pending a valid
    met-precondition run, and its driver reading has been carved off
    entirely to INV-090 (candidate/substrate_conditional, blocked on an
    unbuilt binding-regime maturation curriculum). So the umbrella
    correctly stays candidate/pending_substrate_reconfirmation -- unmoved
    by any of this, since none of it is a verdict AT the umbrella level.
    Neither child needs its own digestion pass -- both already carry
    detailed, current, actively-maintained falsification conditions.
  session_c_resolution: >
    Session C batch 2026-04-17: classified emergent on
    [ARC-001, ARC-002, ARC-003, ARC-019]. The title explicitly names E1/E2/E3
    — the engines whose maturation order is asserted — so the engine ARCs
    underwrite the invariant's subject matter. ARC-019 (staged developmental
    curriculum) provides the substrate that specifies the ordering itself.
    All four are load-bearing: retract any engine ARC and the corresponding
    term in the sequence has no referent; retract ARC-019 and the "sequence"
    concept is not stateable as a curriculum constraint. ARC-001/002/003 are
    active; ARC-019 is provisional, so pending_substrate_reconfirmation: true
    (propagates from the lowest-status substrate per governance rule).
  evidence_quality_note: |
    2026-07-12 (V3-EXQ-740, /governance IGW-20260712-001; consumed from
    failure_autopsy_V3-EXQ-740_2026-07-11, confirmed): the maturational-sequence
    necessity test FAILed DEGENERATE -- validity precondition PC_iv_moved=false,
    the z_world eff_rank ratio ran BACKWARDS (0.770 < 1.15). evidence_direction
    non_contributory / epistemic_category measurement_degeneracy (NOT weighted).
    Root cause: the eff_rank/participation-ratio of the trained alpha_world=0.9
    EMA z_world DECREASES with maturation (the latent specialises onto a low-dim
    task manifold) -- opposite to INV-064's task-relevant differentiation -- and
    the onset window 2..22 was entirely post-saturation (E2 forward R2 ~0.99 at
    onset_2), so there was no immature anchor. NOT claim falsification (PFC
    myelination biology is strong, non-formal-import), NOT substrate_ceiling.
    Route (queue-experiment): V3-EXQ-740a, a corrected successor with a
    task-relevant predictive-decodability observable, an immature-anchor arm, and
    a mature-anchor harm-decodability positive control. 740a has since run and
    ALSO self-reports non_degenerate=false (2nd degeneracy: mature-anchor harm
    target not decodable-in-principle, linear R2 0.034 < 0.05) -- DEFERRED to its
    own /failure-autopsy (a 2nd degeneracy implicates the operationalisation; see
    the 740 autopsy note). INV-064 stays candidate / pending_substrate_reconfirmation;
    PROMOTES/DEMOTES NOTHING.
  decomposition_note: |
    2026-07-12 (/claim-synthesis, consumed from failure_autopsy_V3-EXQ-740a_2026-07-12,
    user-approved per-child): INV-064 is now an UMBRELLA (narrowed-and-retained, still
    candidate / pending_substrate_reconfirmation). The 2nd INV-064 degeneracy surfaced a
    granularity-debt claim<->substrate mismatch: INV-064's "E3 evaluator quality bounded by
    E1/E2 differentiation" clause names z_world as E3's harm input, but SD-010 routes harm
    through a DEDICATED z_harm = HarmEncoder(harm_obs) stream (harm_eval_z_harm, exempt from
    reafference, e3_selector.py:660) that matures on a DISTINCT trajectory from the z_world
    encoder. The single bound is therefore two bounds on two streams, now split into:
      - INV-088 (world/goal-evaluator bound on z_world differentiation) -- the leg V3-EXQ-740a's
        IV validly exercises (world_feat_decode_r2 rose 0.048->0.245 with maturation);
      - INV-089 (harm-evaluator bound on z_harm differentiation) -- the SD-010 HarmEncoder
        stream; the mis-streamed harm leg of 740a, re-posed as z_harm-decodability.
    INV-064's E1->E2->E3 ordering-necessity content is unchanged; MECH-214 / MECH-215 continue
    to depend on the umbrella. Re-derive brake FIRED: no same-observable z_world 740b; no
    substrate build owed (both streams + evaluators exist). Proposal:
    evidence/planning/claim_synthesis_INV-064_2026-07-12.md.

- id: INV-088
  title: world_goal_evaluator_bounded_by_z_world_differentiation
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-001
    - ARC-003
    - ARC-019
  pending_substrate_reconfirmation: true
  pending_retest_after_substrate: true
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-03
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-108b_2026-08-03"
      as_of: 2026-08-03
      verdict: weakens/competence_implementation_gap
  evidence_quality_note: >
    V3-EXQ-744 (DV-coupling half of the world/goal leg) adjudicated 2026-07-12 as
    INCONCLUSIVE / underpowered -- NOT weighted against INV-088. All preconditions met
    (PC_iv_moved/PC_dv_decodable/PC_target_var), non_degenerate, empty interpretation
    (no self-route). The re-init z_world-reading evaluator quality gain is directionally
    present and above the raw floor (mean_delta_r2=0.232 > 0.15) but fails the SD-scaled
    effect gate (2*SD=0.336) and monotonicity (mean_spearman_rho=0.70 < 0.8) on cross-seed
    variance: seeds 42 & 19 strongly couple (rho 0.9/1.0, benefit_rho 0.9/1.0), seed 7 is
    the sole outlier (delta 0.099, rho 0.2, benefit_rho -0.4). A high-variance near-miss,
    not a flat null -- the IV leg is already positive (740a world_feat_decode_r2
    0.048->0.245, PC_iv_moved). Manifest evidence_direction reset weakens->inconclusive;
    re-queue with more seeds (or a variance-reduced design) to resolve the DV coupling.

    V3-EXQ-744a (2026-07-12, 8-seed re-estimate of the 744 inconclusive, EXTENDS 744;
    consumed via /governance 2026-07-13 from
    failure_autopsy_morning-digest-742-744a-745-746-746a_2026-07-13) WEAKENS the strong
    bound reading: mean_delta_r2=0.130 fails the 0.15 floor and the 2xSD effect-size gate
    with preconditions met; monotone C2 fails (rho=0.69). The world/goal-evaluator vs
    z_world-differentiation coupling is real-but-weak and high-variance, not a clean
    threshold effect. FIRST WEIGHTED entry for INV-088 (744 was inconclusive / not
    weighted); stays candidate.

    2026-07-18 governance (reconfirmation routing update; PROMOTES/DEMOTES NOTHING, no
    status change, decision stays hold_candidate_resolve_conflict): the substrate
    reconfirmation path assigned by the 769 autopsy -- the GOV-FANOUT-1 MECH-457 portfolio
    V3-EXQ-770 / 771 / 772 -- RAN and ALL THREE FAILED, each hitting its own pre-registered
    null (failure_autopsy_MECH-457-fanout-770-771-772_2026-07-18, confirmed;
    non_contributory / competence_implementation_gap). So INV-088's reconfirmation DID NOT
    OCCUR for the second consecutive routing (769, then 770/771/772), and no new evidence
    bears on the real-but-weak 744a coupling either way. pending_substrate_reconfirmation
    stays true. Reconfirmation is RE-ROUTED onto the successor portfolio opened by that
    cluster autopsy: H-bc-prior (competence-directed behavioural-prior / imitation seed) vs
    H-approach-primitive (innate non-extinguishing demonstrator-free approach drive). Note
    the routing dependency is asymmetric -- INV-088 rides the MECH-457 competence portfolio
    because a validly-moving z_world-differentiation gradient requires a converter that can
    clear the foraging floor; until that discrimination resolves, INV-088 has no test-bed.
    This is discovery debt: complex (probe-gated) -> puzzle (known rules), NOT a
    substrate_ceiling (INV-088 explicitly retains 'Not substrate_ceiling' per its notes).

    2026-07-18 z_world near-static characterisation (OFFLINE DIAGNOSTIC,
    governance-scoring-EXCLUDED, licenses no build; source
    evidence/experiments/zworld_near_static_characterisation_2026-07-18.md, landed
    REE_assembly master 735e067e4d). PROMOTES AND DEMOTES NOTHING -- no status change,
    no live_status change, decision unchanged.
    The ANTECEDENT of INV-088 -- that z_world differentiation is genuinely low -- gains
    direct quantitative support, and in a SHARPER form than 'low': low AND not improvable by
    enriching the environment or raising competence. The offset-invariant contrast ratio
    (spread / ||centroid||) is ~0.094 and moves only 1.52x across 24 cells spanning a 782x
    foraging range, a 17.4x survival range, and the full 4-rung x734 difficulty ladder (it is
    HIGHEST at the most impoverished rung, i.e. rung impoverishment runs the wrong way); the
    encoder attenuates contrast 6.7x versus the raw 250-d world_state channel. Decodability is
    selectively lost in exactly the world/goal-evaluator-relevant direction: 81% / 72% of raw
    held-out R2 retained for agent_x / agent_y, but only 56% / 59% for resource_proximity /
    nearest_resource_dist.

    The COUPLING leg is UNTOUCHED by that characterisation. Nothing in the diagnostic measures
    evaluator quality. V3-EXQ-744a's WEAKENS reading (mean_delta_r2=0.130 failing the 0.15
    floor and the 2xSD effect gate, C2 monotone rho=0.69; real-but-weak and high-variance)
    stands unaltered. This measurement MUST NOT be read as rehabilitating the strong bound.

    The mechanism behind the low antecedent is CONFOUNDED and is recorded as such. Cause (a)
    ENCODER splits into (a1) the encoder being untrained in the measured configuration --
    weight-delta check: latent_stack 0/61 tensors change across the x734 P0 warmup, because the
    loop buffers z_world.detach() and optimises only agent.e2.parameters() -- and (a2) the
    world_dim=32 discriminative-granularity ceiling already adjudicated by
    failure_autopsy_zworld-integration-cluster_2026-06-06. Every cell measured was dim=32 AND
    untrained, so (a1) and (a2) are perfectly confounded in this data. Separating them is
    chipped separately and REUSES that autopsy's user-confirmed retest spec (world_dim=128 +
    trained encoder + ARC-065 behavioural diversity) rather than a fresh design.

    (a1) is CONFIGURATION-SPECIFIC and is NOT a general claim about the REE encoder: it is a
    fact about the x724/x734 P0 protocol. Other paths do train the world encoder --
    V3-EXQ-740a's world_feat_decode_r2 rose 0.048 -> 0.245 with maturation, which is only
    possible if the encoder moved on that path.

    Scope: no bearing on MECH-459 / the return-scale-normaliser thread, and nothing in this
    characterisation may be used to argue either leg of the live GOV-FANOUT-1 discrimination
    (V3-EXQ-780 H-bc-prior vs V3-EXQ-781 H-approach-primitive); both remain claimed, the
    discrimination remains open, and mech457_competence_bootstrap_explorer stays
    blocked_pending_discrimination.

    [2026-07-26 governance, V3-EXQ-819 non_contributory (flagged precondition_unmet);
    failure_autopsy_batch-793a-817-819_2026-07-26, confirmed]: this run's self-route
    zworld_advantage_grid_nondiscriminative is a MISNOMER for "not ready" -- the z_world encoder DID
    train (3/3 seeds, delta 0.277-0.333; the 780 frozen-random-projection fault is FIXED by
    sd_zworld_warmup_optimizer_group). The vacating fault is a worst-seed non-vacuity gate that
    contradicts its own strict-majority routing predicate (a measurement_test_design_defect, not a
    ceiling). Directional, UNSCORED signal only: a prediction-trained z_world still reaches only AUC
    0.90 (~2% of the achievable 48-57 ceiling) -- mild, non-load-bearing support for INV-088/MECH-457's
    shared thesis that encoder quality alone does not confer competent action. Re-queue 819a reconciling
    the non-vacuity gate to the routing predicate is recommended; this is orthogonal to the open
    GOV-FANOUT-1 discrimination above.

    [2026-07-30 governance, confirmed failure_autopsy_V3-EXQ-819a_2026-07-30 (the reconciled re-queue of
    819 recommended immediately above)]: V3-EXQ-819a resolves the non-vacuity/routing-predicate mismatch
    on a corrected gate-fix instrument -- a prediction-trained z_world beats a frozen-random projection on
    installed foraging competence (paired AUC delta +0.626, 4/6 seeds clear a 0.3 margin, non-degenerate).
    This is an instrument-validity diagnostic (claim_ids scoring-excluded) resolving the
    H-zworld-trained-instrument hypothesis-space leg -- it PROMOTES/DEMOTES NOTHING on INV-088 directly,
    but it PARTIALLY clears the antecedent leg this claim shares with MECH-457: the pre-780 z_world
    instrument-validity concern that clouded the campaign's earlier legs is now resolved for the
    trained-vs-random axis specifically. The COUPLING leg (744a WEAKENS, real-but-weak/high-variance)
    remains untouched and the GOV-FANOUT-1 H-bc-prior vs H-approach-primitive discrimination stays open.
    See the conversion_ceiling_campaign:GENERATION closure-node reconciliation (Step 5b, same cycle) for
    the upstream_block_reason update this licenses.

    [2026-08-02 governance, cross-reference from failure_autopsy_V3-EXQ-108a_2026-08-02, per that autopsy's recommended_bears_on]: V3-EXQ-108a (MECH-135 discriminative-pair FAIL, world_dim=32) is a starker, more direct confirmation of this claim's coupling-leg prediction than V3-EXQ-744a (real-but-weak, rho=0.69 near-miss) or V3-EXQ-819/819a (AUC 0.90 vs ~48-57 achievable ceiling on a SANCTIONED-trained z_world). 108a's E1 IS trained (bespoke single-step MSE loss, 100 episodes) -- unlike the untrained zworld_near_static_characterisation cells where (a1) undertrained-vs-(a2) dimension-ceiling were perfectly confounded -- so it is a genuinely new data point along the confound-breaking axis, though it does not cleanly resolve it (bespoke loss is not the SANCTIONED training route 819/819a used). Pre-registered against MECH-135 only; recorded here as a cross-reference per the autopsy's own flag, not a new adjudicated target for this claim.

    [2026-08-03 governance, V3-EXQ-108b, confirmed failure_autopsy_V3-EXQ-108b_2026-08-03, user-confirmed override of the manifest's own self-routed non_contributory]: 108b directly dissociates evaluator degeneracy from z_world's own differentiation-capacity -- the literal coupling this claim predicts. A sanctioned-trained z_world encoder (confound-breaking relative to 108a's bespoke loss) differentiates REAL states fine (CR_real 0.19-0.20) but the 30-step ROLLOUT still collapses distinct imagined trajectories toward near-identical endpoints (CR_rollout/CR_real ~2.6e-6 to 3.2e-6). The evaluator degeneracy 108/108a observed therefore persists even when z_world's real-state differentiation is confirmed healthy, pointing instead at E1's single-step-training/multi-step-rollout-at-eval mismatch (compounding rollout error), orthogonal to z_world representational capacity. weakens INV-088's coupling leg specifically. epistemic_category=competence_implementation_gap (not substrate_ceiling; this claim stays at 0 prior ceiling hits, re-derive brake does not fire). Routed /lit-pull then /implement-substrate (create SD-e1-rollout-consistency-training). pending_retest_after_substrate=true.
  subject: development.maturational_sequence
  description: >
    Child (i) of INV-064 (world/goal leg). E3's goal and world-feature evaluation
    quality -- the z_world-reading evaluators: harm_eval(z_world) (SD-003 attribution
    head, e3_selector.py:593), benefit_eval, and goal/trajectory scoring -- is strictly
    bounded by z_world (E1) representational differentiation. Poorly-differentiated
    z_world yields a noise-fitted goal/world-feature evaluator, so productive training
    of the z_world-reading evaluators cannot precede sufficient E1 schema
    differentiation. This is the leg V3-EXQ-740a's corrected IV validly exercises:
    task-relevant differentiation of z_world (predictive decodability of harm-relevant
    world features, world_feat_decode_r2 = held-out ridge R2 of z_world[t] -> harm_obs[t+1])
    rises monotonically 0.048 -> 0.245 across onset {0,1,4,12,30} -- first valid
    demonstration that E1/E2 task-relevant differentiation increases with maturation on
    the V3 substrate. Distinct from INV-089 (the z_harm harm-evaluator leg), which reads
    a separate encoder with its own maturation trajectory.
  what_would_answer: >
    A frozen-representation curriculum-order contrast (onset including a genuinely
    immature anchor ~0, differentiation-IV moving in the predicted direction) in which
    (a) world-feature decodability from frozen z_world rises monotonically with E1/E2
    maturation -- ALREADY ESTABLISHED by V3-EXQ-740a (world_feat_decode_r2 0.048 -> 0.245,
    PC_iv_moved passed) -- AND (b) the held-out quality of a re-initialised z_world-reading
    E3 evaluator (fixed training budget on frozen z_world) improves monotonically with
    that rising decodability. The remaining test is the DV coupling (evaluator quality
    tracks z_world differentiation); the IV leg is done. Falsified if evaluator quality is
    flat or non-monotone across a validly-moving z_world-differentiation gradient.
  notes: >
    Grounded in the PFC-last-myelination developmental existence proof (inherited from
    INV-064) and the dual-systems cognitive-control leg (Casey 2011; Strang 2013:
    cognitive-control and affective systems develop on different timetables). Testable-now:
    substrate ready, partial positive evidence in hand (740a IV). Motivating evidence is
    the SUPPORTED half of the INV-064 740-cluster. Not substrate_ceiling.
  registered: "2026-07-12"
  depends_on: [INV-064, ARC-001, ARC-003, ARC-019]
  origin: >
    Registered via /claim-synthesis 2026-07-12 as child (i) of INV-064, from the
    granularity-debt decomposition in failure_autopsy_V3-EXQ-740a_2026-07-12 (proposal
    evidence/planning/claim_synthesis_INV-064_2026-07-12.md). PROMOTES/DEMOTES NOTHING.

- id: INV-089
  title: harm_evaluator_bounded_by_z_harm_differentiation
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-003
    - ARC-019
    - ARC-027
  pending_substrate_reconfirmation: false
  pending_retest_after_substrate: true
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-16
    needs_review: false
    evidence:
      from: failure_autopsy_INV-089-INV-090-wellposedness_2026-07-16
      as_of: 2026-07-16
      verdict: non_contributory/measurement_reframe
  evidence_quality_note: >
    Promoted candidate->provisional 2026-07-12 on V3-EXQ-743 (PASS/supports). The
    load-bearing positive control passed: realized harm is decodable-in-principle from
    the dedicated z_harm stream E3 actually uses (mature zharm_decode_r2=0.124, C1 floor
    0.05) and decodability RISES with HarmEncoder maturation (C2), dispositively crossing
    the 0.034-flat z_world decode that mis-streamed V3-EXQ-740a produced. The CORE bounding
    coupling (harm_eval_z_harm held-out quality bounded by z_harm differentiation, SEC_*)
    is REPORTED-NOT-GATING in 743 (SEC_quality_gain/SEC_monotone false), and its calibrated
    bound test (state-determined DV target + z_harm-scaled thresholds) is DEFERRED to a
    follow-up because the realized post-random-action harm DV is under-determined (740a's
    flagged confound). Provisional, not stable, until that calibrated bound test lands.
    Re-derive brake honoured (z_harm observable, distinct claim -- not a z_world 740b).

    2026-07-13 (calibrated bound test landed; consumed via /governance from
    failure_autopsy_morning-digest-742-744a-745-746-746a_2026-07-13). Two runs:
    V3-EXQ-746 STARVED (non_degenerate:false) -- z_harm gradient did not move
    (IV delta -0.184), state target not decodable-in-principle (decode r2 -0.196),
    DV blew up numerically (seed-3 -6.3e7) on a sparse single-cell target with an
    un-clipped DV head; NOT a falsification, manifest evidence_direction set
    superseded (indexer scoring-excluded, no weight). V3-EXQ-746a (corrected
    calibrated bound: denser target + raw-std precondition + gradient-clipped DV
    head; SUPERSEDES 746, EXTENDS 743) WEAKENS: on the met-precondition dense target
    the harm_eval_z_harm quality bound does not rise monotonically (C1 rho=-0.53) or
    track z_harm differentiation (C2 couple rho=-0.08); mean_dv_delta=-34.5. This is
    the FIRST met-precondition bound-test evidence against the strict-bound reading.
    GOVERNANCE WEIGH-IN (narrow_supports flag): INV-089 was promoted candidate->
    provisional 2026-07-12 on 743's POSITIVE CONTROL alone (harm decodable-in-principle
    from z_harm, decodability rises with HarmEncoder maturation). 743 is a
    SINGLE-PATHWAY positive control (decodability existence), NOT the bound coupling;
    the bound coupling now has its first met-precondition test and it WEAKENS. Re-weighed
    with BOTH in view (not just the weakens removed): INV-089 STAYS provisional -- the
    743 decodability control is undisturbed, but the 'strictly bounded by z_harm
    differentiation' reading is now actively pressured, and provisional must NOT advance
    toward stable until a bound-coupling PASS exists. Net: no promotion; the single
    remaining support is a decodability positive control, and the sole bound test to
    date weakens.

    2026-07-15 (V3-EXQ-746b autopsy -- failure_autopsy_V3-EXQ-746b_2026-07-15): 746a's
    weakens is MEASUREMENT-SUSPECT. 746b repaired the DV estimator (standardised target +
    K=5-fold averaged held-out R2 + weight-decay/early-stopping + finite R2 floor -1) and
    showed PC_dv_estimator_ok=True, mean_dv_mature 0.45 -- the harm_eval_z_harm head CAN
    beat the mean at maturity, so 746a's -166 held-out divergence was estimator pathology,
    not a real null. 746a's weakens is RETAINED and still weighted (746b delivered no valid
    bound test -- its IV starved on PC_iv_moved, mean_iv_delta +0.002, so the coupling could
    not express itself) but flagged measurement-suspect pending a valid met-both-preconditions
    bound test. V3-EXQ-746c (queued 2026-07-15, ree-v3 commit 697250c) is that test: primary
    target corrected to prox (the SD-010 harm-proximity label -- the only collected target
    whose z_harm differentiation genuinely grows with maturation AND survives adequate
    sampling; local-density's gradient is a small-sample underfitting artifact) and the IV/DV
    data budgets decoupled (disjoint pools) so neither leg starves the other. INV-089 stays
    provisional on 743's single-pathway positive control; no promotion until a bound-coupling
    PASS on a valid met-precondition test exists.

    2026-07-16 (/governance -- 746c non_contributory adjudication + well-posedness reframe).
    The 746a/746b/746c bound tests are ALL driver (monotone-coupling) tests and have been
    RE-ASSIGNED to INV-090 (the growth-coupling/driver child split out 2026-07-16); they no
    longer weigh on the narrowed CEILING claim INV-089. 746c (the first valid met-precondition
    driver test) self-routed weakens but was ADJUDICATED non_contributory
    (measurement_regime_mismatch: it ran in a non-binding regime -- prox z_harm differentiation
    ~0.82 at onset 0 -- so a flat-high evaluator is CONSISTENT with a ceiling, not against it;
    failure_autopsy_746c-756_2026-07-16); its manifest evidence_direction is corrected to
    non_contributory (scoring-excluded). The ceiling-form ENVELOPE test assigned by the split is
    ANALYTICALLY VACUOUS under the 746 instrumentation -- 'harm_eval quality' and 'the same-budget
    z_harm->target decodability ceiling' are the same measurement (a same-capacity MLP decode of
    the target from frozen z_harm), so harm_eval <= ceiling holds by construction
    (failure_autopsy_INV-089-INV-090-wellposedness_2026-07-16). Net re-weigh: on the narrowed
    ceiling INV-089 there is NO active weakens; the earlier 'actively pressured' phrasing
    over-stated a driver-test result against the ceiling. INV-089 STAYS provisional on 743's
    single-pathway positive control (narrow_supports_flag); pending_retest_after_substrate on a
    distinct non-flat harm-evaluation target (evaluator objective != representational-decodability
    probe) -- the only in-substrate candidate (realized-harm Y) is flat, so the retest is blocked
    on the committed-action/conversion ceiling. Re-derive brake fired (3rd non_contributory-family
    reading); a same-claim same-regime 746-letter re-queue is REFUSED. PROMOTES/DEMOTES NOTHING.
  subject: development.maturational_sequence
  description: >
    Child (ii) of INV-064 (harm leg); NARROWED 2026-07-16 to the CEILING reading (the
    growth-coupling/driver reading split out to INV-090 -- see notes). E3's nociceptive
    harm-evaluation quality -- harm_eval_z_harm (e3_selector.py:660), which reads z_harm =
    HarmEncoder(harm_obs) (stack.py:119) -- CANNOT EXCEED an upper bound set by z_harm
    representational differentiation, a maturation trajectory DISTINCT from z_world's: for a
    fixed evaluator-training budget on frozen z_harm, held-out harm-eval quality at any
    maturation point is <= what a same-budget decoder can extract from z_harm at that point.
    Under SD-010 the HarmEncoder is instantiated OUTSIDE the z_world encoder and is exempt
    from reafference correction (introduced to resolve the EXQ-027b over-correction paradox
    where the ReafferencePredictor subtracted hazard when harm was fused into z_world). Because
    harm is routed through its own encoder, the bound is set by z_harm differentiation, NOT
    z_world differentiation -- which is why V3-EXQ-740a's harm leg, which decoded realized harm
    from z_world, was mis-streamed and never validly exercised (harm world-features decode from
    z_world at 0.245 and rise, while realized scalar harm does not: 0.034, flat). This is a
    CEILING (constrains the maximum harm-evaluator quality); it is SILENT on whether quality
    tracks differentiation upward during maturation -- that co-rise is the separate claim INV-090.
  what_would_answer: >
    An ENVELOPE / upper-bound contrast over HarmEncoder (z_harm) maturation, valid in ANY regime
    (including non-binding / already-differentiated): at each HarmEncoder onset, compare held-out
    harm_eval_z_harm quality against the same-budget z_harm->harm-target decodability CEILING on
    frozen z_harm, with the z_harm maturation trajectory reported SEPARATELY from the z_world
    trajectory (distinct encoder). The positive control (harm decodable-in-principle from z_harm,
    decodability rises with maturation) is V3-EXQ-743 PASS. SUPPORTED if harm-eval quality stays
    at-or-below the differentiation-implied ceiling across onsets (never exceeds it). FALSIFIED if
    harm_eval_z_harm quality EXCEEDS the z_harm-differentiation ceiling at any onset (evaluation
    richer than the representation can support). Explicitly NOT a monotone-coupling/driver test
    (that is INV-090) and NOT a z_world decode (the re-derive brake refuses a same-observable 740b).
  notes: >
    New mechanistic content grounded in the distinct nociceptive-maturation literature:
    parallel spino-thalamic / spino-parabrachial pathway to operculo-insular / mid-cingulate
    cortex, anatomically separate from fronto-parietal exteroceptive processing (Bastuji 2016);
    the MD->ACC nociceptive projection undergoes its own silent-synapse -> AMPAR-stabilised
    maturation (Wang 2020); dual-systems "different timetables" for affective vs cognitive-
    control systems (Casey 2011; Strang 2013). Testable-now: substrate ready (harm_eval_z_harm
    + HarmEncoder exist); no build owed. Motivating evidence is the MIS-STREAMED harm half of
    the INV-064 740-cluster (the measurement collision that revealed the stream-split). Not
    substrate_ceiling.

    2026-07-16 GRANULARITY-DEBT SPLIT (/claim-synthesis, proposal
    evidence/planning/claim_synthesis_INV-089_2026-07-16.md; user-approved per-child): the 746
    lineage (746/746a/746b/746c, 4 autopsies) revealed INV-089 conflated TWO separable
    mechanisms under one id -- a CEILING (harm_eval quality <= f(z_harm differentiation),
    RETAINED here) and a growth-coupling/DRIVER (quality RISES WITH differentiation during
    maturation, SPLIT OUT to INV-090). INV-089 narrowed to the ceiling; INV-090 registered
    substrate_conditional/candidate carrying the driver reading. Lit (targeted_review_inv_089,
    0.793) LEANS to the ceiling (Verriotis-Fitzgerald 2016: nociceptive maturation is
    non-activity-dependent -> a maturational GATE, pressuring an experience-driven co-rise).
    EVIDENCE RE-ASSIGNMENT (for governance to reconcile in its 746c pass -- deliberately NOT
    applied by /claim-synthesis): 746a/746b/746c are ALL driver (monotone-coupling) tests, so
    they weigh on INV-090, not on the ceiling INV-089; on the narrowed ceiling INV-089 there is
    NO active weakens and the sole support is 743's positive control. The current
    live_status.evidence (746a weakens) and the evidence_quality_note phrase "the 'strictly
    bounded by z_harm differentiation' reading is now actively pressured" OVER-STATE a driver-test
    result against the ceiling and should be re-weighed when governance applies the 746c
    non_contributory adjudication. Stays provisional pending a valid ceiling-form (envelope) test.
  registered: "2026-07-12"
  depends_on: [INV-064, SD-010, ARC-003, ARC-019, ARC-027]
  origin: >
    Registered via /claim-synthesis 2026-07-12 as child (ii) of INV-064, from the
    granularity-debt decomposition in failure_autopsy_V3-EXQ-740a_2026-07-12 (proposal
    evidence/planning/claim_synthesis_INV-064_2026-07-12.md). NARROWED to the ceiling reading
    2026-07-16 via a second /claim-synthesis round (proposal
    evidence/planning/claim_synthesis_INV-089_2026-07-16.md, from
    failure_autopsy_746c-756_2026-07-16), splitting the growth-coupling/driver reading out to
    INV-090. PROMOTES/DEMOTES NOTHING.

- id: INV-090
  title: harm_evaluator_quality_grows_with_z_harm_differentiation
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-003
    - ARC-019
    - ARC-027
  pending_substrate_reconfirmation: true
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-16
    needs_review: false
    evidence:
      from: failure_autopsy_INV-089-INV-090-wellposedness_2026-07-16
      as_of: 2026-07-16
      verdict: non_contributory/substrate_conditional
  epistemic_category: substrate_conditional
  subject: development.maturational_sequence
  description: >
    Growth-coupling/DRIVER child split from INV-089 (the ceiling) on 2026-07-16. During
    maturation, E3's nociceptive harm-evaluation quality -- harm_eval_z_harm (e3_selector.py:660,
    reading z_harm = HarmEncoder(harm_obs)) -- RISES MONOTONICALLY WITH z_harm representational
    differentiation WHILE differentiation is the binding constraint (the low-differentiation
    regime). Distinct from INV-089: INV-089 asserts only the CAP (quality cannot exceed
    f(differentiation)); INV-090 asserts the UPWARD CO-MOVEMENT -- that as z_harm differentiates
    from a genuinely immature (low) starting point, harm-evaluator quality tracks it upward. The
    reading is falsifiable ONLY in a binding regime: once differentiation is already high enough
    that it no longer limits evaluation (non-binding), quality is flat-high and decoupled from
    small differentiation fluctuations -- CONSISTENT with the ceiling and uninformative about the
    driver.
  what_would_answer: >
    A monotone-coupling contrast in a BINDING regime: with a harm target whose z_harm
    differentiation starts genuinely LOW (onset-0 differentiation well below the harm-eval-
    saturating level) and rises with HarmEncoder maturation, show harm_eval_z_harm held-out
    quality rises monotonically with, and rank-couples to (couple_rho >= 0.80), that
    differentiation across onset. The precondition gate MUST verify a binding regime -- a minimum
    onset-0-to-mature differentiation gap AND onset-0 below saturation -- the gate the 746
    lineage's PC_iv_moved (delta>0 and rho>0 only) never enforced. FALSIFIED if quality is
    flat/non-monotone across a validly-moving, binding differentiation gradient. NOT expressible
    in the current substrate: no collected harm target has a binding maturation gradient (dens =
    small-sample artifact, realized-harm Y = flat, prox = already-high ~0.82 at onset 0), so a NEW
    maturation curriculum that starts harm-target z_harm differentiation low (binding-then-released)
    is owed before this can validly run.
  notes: >
    Carries the entire 746 driver lineage: 746a (weakens, measurement-suspect DV artifact), 746b
    (non_contributory, IV starved), 746c (non_contributory -- first VALID test, but ran in a
    non-binding regime: prox z_harm differentiation ~0.82 at onset 0 rising only to ~0.87, so the
    driver could not express itself; couple_rho 0.675 with 6/8 seeds positive was weak POSITIVE
    evidence, not a weakens). WEAKER lit warrant than the INV-089 ceiling: Beggs 2015 grounds
    postnatal refinement, but Verriotis-Fitzgerald 2016 (nociceptive maturation is
    non-activity-dependent) PRESSURES an experience-driven co-rise -- the driver mechanism may not
    be biologically operative for harm. Registered as a testable candidate to make the split
    honest, NOT as an established mechanism. substrate_conditional: needs a binding-regime
    maturation curriculum (a NEW substrate_queue entry via /implement-substrate -- the re-derive
    brake REFUSES a naive same-regime 746-letter). Re-derive brake fired 2026-07-16 (2nd
    non_contributory on the lineage).

    2026-07-16 UPDATE (well-posedness autopsy -- failure_autopsy_INV-089-INV-090-wellposedness_2026-07-16,
    consumed via /governance): the binding-regime maturation-curriculum build is now REFUSED, not
    merely owed. A curriculum that clamps z_harm differentiation as the sole bottleneck welds the
    driver to the ceiling -- the evaluator (same-capacity decode of the same target from the same
    z_harm) tracks differentiation upward BY CONSTRUCTION, so a monotone-coupling PASS is entailed
    by the ceiling and the only discriminating FAIL is near-impossible. Building it manufactures a
    self-fulfilling co-rise rather than testing a driver. The real unblock is a DISTINCT, non-flat,
    experienced harm-evaluation signal (evaluator objective != decoder), itself blocked on the
    committed-action/conversion (F-dominance) ceiling. NO substrate_queue entry created; INV-090
    HOLDS candidate/substrate_conditional. Governance may retire the driver reading if the
    commitment ceiling proves durable.
  registered: "2026-07-16"
  depends_on: [INV-089, INV-064, SD-010, ARC-003, ARC-019, ARC-027]
  origin: >
    Registered via /claim-synthesis 2026-07-16 as the growth-coupling/driver child split from
    INV-089 (narrowed to the ceiling), from the granularity-debt decomposition in
    failure_autopsy_746c-756_2026-07-16 (proposal
    evidence/planning/claim_synthesis_INV-089_2026-07-16.md). PROMOTES/DEMOTES NOTHING.

- id: MECH-214
  title: goal_referent_e1_representability
  claim_type: mechanism
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  subject: development.goal_coherence
  implementation_phase: v4
  description: >
    The content of any goal -- the state or object being aimed at -- must be
    representable in E1's latent schema space for z_goal to carry semantic
    information. A goal aimed at an E1-unrepresented state is, functionally, a
    random vector in E3's operating space: it cannot generate coherent viability
    terrain, cannot guide hippocampal trajectory proposals, and cannot produce
    meaningful harm/benefit evaluation. Goal coherence therefore co-develops with
    E1 schema richness: primitive goals (warmth, satiation, physical contact) emerge
    earliest because those states are E1-representable from earliest sensory
    experience; higher-order social, aesthetic, ethical, and self-transcendent goals
    emerge only after E1 has differentiated the relevant schema vocabulary. Goals
    are E1 functions in this precise sense: their referent content is constituted by
    E1 representations, not independently specified. The frontal goal-evaluation
    system can only evaluate what the world-model can represent.
  notes: >
    Clinical predictions: (1) Early-life disruption of E1 schema formation (sensory
    deprivation, early trauma, neurodevelopmental conditions affecting sensory
    processing) does not merely cause emotional injury -- it constrains the semantic
    space from which coherent goals can ever be drawn, with downstream effects on E3
    training quality that persist into adulthood. (2) Interventions that expand E1
    schema richness (novel high-entropy environments, therapy that develops
    experiential vocabulary, practices that develop fine perceptual discrimination)
    can widen the space of coherent goals available to E3, not just regulate existing
    ones. (3) Goal incoherence -- wanting without clear object, motivation without
    direction -- may reflect under-differentiated E1 schemas rather than E3/reward
    pathology. This reframes certain anhedonic and motivationally flat presentations
    as E1 poverty rather than purely dopaminergic disorder.
    V3 WIRING AUDIT (2026-04-07): z_goal lives purely in z_world space
    (GoalState seeds from z_world_current). V3 grid world conflates location with
    reward, so z_world-only goals are adequate. V4 needs z_self-domain goal
    representation (DR-11) and an environment where proxy and hedonic content
    dissociate (DR-14) to surface this failure mode. See also addiction mapping:
    repeated pursuit of z_goal proxy without hedonic grounding = wanting fires on
    E1-unrepresented satisfaction state.

    CURRENCY CHECK (2026-08-08, thought-digestion): re-verified against
    evidence/planning/self_model_v4_plan.md -- SELF-5 (DR-11) and SELF-6 (DR-14) are
    both still status: blocked, and both already list unblocks_claims: [MECH-214]
    directly, so no new tracking doc is needed. SELF-3 (DR-10) has moved to
    in_progress since this claim's notes were last touched, but it unblocks MECH-215,
    not this claim, only indirectly (as one of SELF-5's two dependencies).
  epistemic_category: substrate_conditional
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: this claim cannot be tested against V3. GoalState
    seeds and attracts z_goal purely in z_world space; the V3 grid world conflates
    location with reward, so proxy content and hedonic content cannot come apart.
    Before this claim is even meaningfully falsifiable, two V4 substrate steps must
    land (self_model_v4_plan.md SELF-5/SELF-6, both status: blocked): (a) DR-11/SELF-5
    -- a z_self-domain goal channel, so a goal can name a self-state and not only a
    world-location target; (b) DR-14/SELF-6 -- an environment that dissociates a
    goal's proxy cue from its hedonic outcome. Until both land, any experiment framed
    as testing this claim is measuring something else (most likely MECH-215's
    z_self-viability question instead).

    CONFIRMING (once DR-11 + DR-14 exist): agents fail to form coherent, effective
    goals/viability terrain for self-states E1 has not yet differentiated a schema
    for (goal quality no better than random-vector baseline); goal coherence improves
    monotonically as E1 schema richness for the relevant domain expands; the
    addiction-analog failure mode reproduces (repeated pursuit of a z_goal proxy
    without hedonic grounding, i.e. wanting persists while the actual satisfaction
    state stays E1-unrepresented).

    FALSIFYING: E3 forms coherent, effective goals and viability terrain for a
    self-state that E1 demonstrably cannot represent (no schema differentiation for
    that state, verified independently) -- i.e. goal quality is statistically
    indistinguishable between E1-represented and E1-unrepresented target states. That
    would show goal referent content is NOT constituted by E1 representations, and
    that E3/frontal goal machinery can specify and pursue goals independent of
    world-model vocabulary.
  registered: "2026-04-07"
  depends_on: [INV-064, MECH-205, MECH-121]

- id: MECH-215
  title: self_model_prerequisite_for_agentive_prediction
  claim_type: mechanism
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  subject: development.self_model_agency
  implementation_phase: v4
  description: >
    Coherent goal-achievement prediction requires a well-differentiated self-model
    as the subject of that prediction. In REE terms this decomposes into two
    prerequisites that must precede E3 viability planning: (1) E1 self-schema
    differentiation -- the agent must have a stable, accurate z_self representation
    (body schema, proprioceptive state, affective baseline, capacity envelope) before
    it knows what kind of entity is doing the pursuing; (2) E2 self-transition
    differentiation -- the agent must have learned how its actions transform its own
    z_self state before it can predict whether it is capable of navigating a given
    path. Without (1), E3's viability trajectories have no coherent subject: they
    are plans for an undefined agent. Without (2), E3 cannot estimate whether the
    agent's affordances are sufficient to traverse the proposed path. Goal-achievement
    prediction is therefore prediction of form: can *this* agent, with *this* capacity
    model, reach *that* state? The "I" that makes a goal a first-person goal -- not
    just a desired world-state but a personally pursuable one -- is constituted by
    E1's self-schemas and E2's self-transition model, not by E3 itself.
  notes: >
    Clinical predictions: (1) Depersonalisation and derealization (disrupted E1
    self-schema) should produce goal-directedness failure that looks like motivational
    disorder but is actually a subject-dissolution problem -- the "I" required to
    be the agent of pursuit is not coherently represented. (2) Conditions that
    degrade the body schema or interoceptive accuracy (chronic pain, fatigue,
    dissociative disorders) impair E2 self-transition accuracy, producing inflated
    or deflated capacity estimates that make viability predictions systematically
    wrong -- the person either overestimates what they can do (mania-like) or
    catastrophically underestimates (avoidance, agoraphobia, learned helplessness).
    (3) Somatic/body-based and interoception-focused therapies work at the correct
    level: they rebuild E1 self-schemas and E2 self-transition accuracy, restoring
    the preconditions for coherent agency rather than directly targeting E3
    goal/harm evaluation. (4) Infant motor development (proprioceptive and body
    schema formation before goal-directed reaching) follows this sequence exactly.
    "Know thyself" is not ethical wisdom but an architectural prerequisite.
    V3 WIRING AUDIT (2026-04-07): E3.score_trajectory() evaluates entirely in
    z_world space -- z_self is invisible to E3 (DR-10). E3 trusts E2 rollout
    unconditionally -- no E2 PE -> E3 confidence modulation (DR-12). z_self
    encoder has no temporal depth -- body_obs -> MLP -> EMA, no recurrence (DR-13).
    All three gaps are correctly scoped for V3's simple 4-action grid world but
    become critical when the agent has complex body dynamics or the environment
    requires capacity-aware planning.
  implementation_note: >
    SUBSTRATE PROGRESS 2026-07-01 (generation:v4, no-op-default levers; PROMOTES
    NOTHING -- MECH-215 stays candidate/v4). All three V3-audit gaps this claim
    named now have landed substrate: DR-12 (E2-forward-PE -> E3 confidence;
    self_model_v4:SELF-4, built 2026-06-17, e3_selector.use_pe_confidence_weighting),
    DR-13 (z_self temporal depth; SELF-1, built + validated V4-EXQ-002 PASS
    2026-07-01, latent_stack.use_self_recurrence), and DR-10 (z_self enters E3
    viability scoring; SELF-3, built 2026-07-01,
    e3_selector.use_self_viability_weighting -- validation V4-EXQ-003). DR-10 + DR-12
    are the two halves this claim decomposes into (E1/z_self-schema subject +
    E2 self-transition confidence); MECH-215 is NOT yet unblocked -- the levers are
    no-op-default caller-supplied v1 seams, and the ecological auto-sources +
    outcome-scoring experiments remain. Design docs
    docs/architecture/dr10_z_self_in_e3_viability.md +
    dr12_pe_conditioned_e3_confidence.md + dr13_self_recurrence_temporal_depth.md;
    plan nodes self_model_v4:SELF-1/3/4.
  registered: "2026-04-07"
  depends_on: [INV-064, MECH-214, MECH-212]

- id: SD-018
  title: encoder.resource_proximity_supervision
  claim_type: substrate_decision
  status: implemented
  live_status:
    reading: implemented
    as_of: 2026-07-11
    needs_review: false
  subject: encoder.resource_proximity
  description: >
    z_world encoder requires auxiliary resource proximity regression loss to encode
    benefit-relevant features. Without this, z_world is trained only on E1 world-model
    prediction loss, which is invariant to resource saliency (the resource does not
    change the sensory scene unless contacted). EXQ-085m proved benefit_eval_r2=-0.004:
    z_world is orthogonal to resource proximity. A Sigmoid regression head on z_world
    predicting max(resource_field_view) in [0,1] backprops through the encoder, forcing
    z_world to represent resource proximity. This is the benefit-side analog of SD-009,
    which solved the same problem for harm-event discrimination. Without SD-018, the
    entire benefit/goal scoring pathway (benefit_eval_head, goal_proximity, z_goal seeding,
    drive modulation, dual systems) operates on noise. SD-018 is the critical unblocking
    substrate decision for V3 goal-directed behavior.
  evidence_quality_note: |
    [2026-05-08 governance]: EXQ-536a/b diagnostic non_contributory; same crossed-wiring read as ARC-030 sibling.
  notes: >
    Implementation: SplitEncoder.resource_proximity_head = nn.Sequential(
    nn.Linear(world_dim, 1), nn.Sigmoid()). Config: use_resource_proximity_head (bool,
    default False), resource_proximity_weight (float, default 0.5). Agent method:
    compute_resource_proximity_loss(target, latent_state) -> MSE loss. Target:
    max(resource_field_view) from CausalGridWorldV2 (available when use_proxy_fields=True).
    Gradient flows through z_world encoder. Backward-compatible: disabled by default.
    Identified during V3 wiring audit 2026-04-07: the z_world-doesn't-encode-resources
    gap explains all goal-directed behavior failures (EXQ-085m, EXQ-225, EXQ-237a,
    EXQ-238, and the entire EXQ-085h-085o SD-015 series).
  registered: "2026-04-07"
  depends_on: [SD-009, SD-005]
  heterogeneity_note: >
    All conflict entries are pre-implementation runs (EXQ-085 cohort and
    sibling diagnostics) where the resource_proximity_head was absent. They
    demonstrate the encoding gap SD-018 addresses, not disconfirmation of the
    design decision itself. The decision was made on diagnostic evidence
    (benefit_eval_r2=-0.004 proving z_world was blind to resources before the
    fix); a ratio of 1.0 is expected and appropriate for a substrate decision
    whose pre-implementation baseline was zero-benefit-encoding by construction.

- id: SD-019
  title: affective_harm_nonredundancy_constraint
  claim_type: design_decision
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_323a_sd019_harm_nonredundancy_20260416T172811Z_v3
      as_of: 2026-04-16
      verdict: supports/PASS
  implementation_phase: v3
  subject: harm_stream.affective_nonredundancy
  polarity: asserts
  description: >
    The affective harm stream (`z_harm_a`) must encode temporally integrated threat load,
    not a delayed, smoothed, or monotone-transformed copy of the sensory-discriminative
    harm stream (`z_harm_s`). `z_harm_s` tracks immediate, action-conditional hazard
    proximity/intensity and supports forward prediction (`E2_harm_s`). `z_harm_a` serves a
    different function: it encodes accumulated threat burden with motivational relevance for
    urgency and commitment modulation. A valid `z_harm_a` implementation must therefore admit
    dissociations where immediate sensory harm is low but affective load remains elevated, and
    where brief sensory spikes do not saturate affective load.
  notes: >
    This claim sharpens SD-011. SD-011 establishes the dual-stream split in principle:
    sensory-discriminative vs affective-motivational. SD-019 adds a stricter substrate
    constraint: the affective stream is invalid if it is recoverable as a simple lagged or
    monotone mapping from the sensory stream. Biological grounding: the C-fiber /
    paleospinothalamic pathway is slower and more integrative than the A-delta pathway, but
    the critical computational property is not "delay" alone; it is nonredundant temporal
    integration with persistence and slower recovery. Motivated by EXQ-241 and EXQ-241a:
    both show that `z_harm_a` can track accumulated harm while remaining too close to
    `z_harm_s` functionally (D3 reversal persists in EXQ-241a despite harm-history input).
    The design requirement is therefore representational nonredundancy, not merely temporal lag.
  registered: "2026-04-08"
  depends_on: [SD-011, SD-010, ARC-016]
  location: docs/architecture/sensory_stream_tags.md#sd-019
  implementation_note: >
    IMPLEMENTED 2026-04-10. agent.compute_harm_nonredundancy_loss(latent_state) in
    ree_core/agent.py. Projects z_harm_s and z_harm_a via learned Linear heads to
    comparison dim; penalty = cosine_similarity^2; optional ARC-016 precision scaling:
    penalty *= (1 + harm_nonredundancy_precision_scale * e3.current_precision / 500.0).
    Config: REEConfig.harm_nonredundancy_weight (0.0 default), harm_nonredundancy_precision_scale
    (0.0 default). Backward compatible. Validation: V3-EXQ-323 queued.
  evidence_quality_note: >
    EXQ-323a (2026-04-16, PASS, supports): First experimental confirmation. All 5
    seeds pass nonredundancy criterion. z_harm_a representationally distinct from
    z_harm_s. Co-tested with SD-011 and SD-022 in same run; all three supported.
    PROMOTED TO PROVISIONAL 2026-04-17. Criteria: overall_confidence=0.888 (>=0.80),
    conflict_ratio=0 (0 weakens; 6 supports, 2 mixed from instrumentation-limited
    pre-substrate runs). Literature base expanded to 5 entries covering four independent
    angles: (1) Price 2000 -- dual spinal pathway architecture, paleospinothalamic
    direct limbic route; (2) Rainville 1997 -- ACC/S1 double dissociation under
    experimental unpleasantness manipulation; (3) Woolf & Salter 2000 -- C-fiber
    temporal summation (wind-up/central sensitization), grounds the temporal-integration
    claim directly; (4) Craig 2003 -- lamina I/VMpo/insula pathway as distinct thalamic
    route for affective body state, lit_conf=0.79; (5) Berthier 1988 -- pain asymbolia
    clinical lesion series, z_harm_s intact / z_harm_a selectively abolished, bidirectional
    dissociation from Rainville. Experimental: EXQ-323 x2 mixed (instrumentation,
    collinear EMA substrate) + EXQ-323a PASS 5/5 seeds on SD-022 body-damage substrate.
    decision_status: applied.
    children: [SD-019a, SD-019b]

- id: SD-019a
  title: "harm_unpleasantness_channel: a distinct pre-integration affective valence signal encoding the immediate motivational relevance of noxious input, processed via the medial pain pathway before temporal accumulation into suffering."
  claim_type: design_decision
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  subject: harm_stream.immediate_affective_valence
  polarity: asserts
  registered_utc: 2026-04-20
  depends_on:
    - SD-019          # parent nonredundancy constraint
    - SD-011          # dual nociceptive streams
  functional_restatement: >
    The REE harm-affect system requires a named intermediate signal between raw sensory harm
    (z_harm_s) and the suffering accumulator (z_harm_a): harm_unpleasantness. This is the
    immediate "I want this to stop now" signal — the fast medial-pathway output before
    temporal integration. Key properties:

      - Tracks noxious input with a moderate time constant (faster than suffering,
        slower than raw z_harm_s).
      - Is NOT modulated by controllability or escapability (Loffler et al. 2018:
        controllability leaves unpleasantness unchanged while selectively reducing
        suffering).
      - Serves as the input to MECH-219's hysteretic integrator.
      - Has distinct downstream effects from both z_harm_s and z_harm_a: drives
        AIC urgency (SD-032c) and E3 short-horizon avoidance scoring independently.

    The nonredundancy constraint (SD-019) applies at the z_harm_a (suffering) level,
    not at the unpleasantness level. z_harm_un may correlate with z_harm_s under
    typical conditions; what distinguishes it is that it is a medial-pathway signal
    with affective/motivational weighting, not purely sensory intensity.

    Biological anchor: unpleasantness dimension of the McGill Pain Questionnaire
    (Melzack 1975); ACC/anterior insula 'affective-motivational' component of pain
    (Price 2000 dual-pathway; Rainville 1997 ACC/S1 double dissociation).
  notes: >
    Registered 2026-04-20 following the Loffler et al. 2018 finding that controllability
    selectively reduces suffering without touching unpleasantness. The three-way dissociation
    (intensity / unpleasantness / suffering) makes the intermediate signal architecturally
    necessary. See affect_primitives.md for the full register and downstream effect table.

    The current V3 implementation does not explicitly compute harm_unpleasantness as a
    distinct named tensor -- it is implicitly subsumed into z_harm_a's input signal.
    SD-019a formalises the requirement to make it explicit. Implementation priority is
    medium: the distinction becomes load-bearing when the controllability gate (Q-036) is
    implemented in MECH-219.
  evidence_quality_note: >
    Registered pre-implementation. Biological grounding via SD-019 targeted reviews
    (Price 2000, Rainville 1997, Woolf & Salter 2000, Craig 2003, Berthier 1988).
    Loffler et al. 2018 (targeted_review_q_036) specifically supports the controllability
    non-dependence of unpleasantness. Lit pull specific to SD-019a (medial pathway fast
    component, ACC/insula unpleasantness encoding) is flagged as needed.
  implementation_note: >
    IMPLEMENTED 2026-05-04. LatentStackConfig.use_harm_un (bool, default False);
    LatentStackConfig.harm_un_ema_alpha (float, default 0.2). Non-trainable EMA buffer
    REEAgent._harm_un_ema maintained in agent.sense() on z_harm_s; populates
    LatentState.z_harm_un. AIC urgency redirect (aic_z_norm reads z_harm_un.norm()
    when use_harm_un=True) and E3 short-horizon urgency redirect (z_harm_a variable
    shadowed by z_harm_un in select_action()). Controllability parity enforced:
    SD-021 descending modulation only multiplies new_latent.z_harm, not z_harm_un
    (Loffler 2018 three-way dissociation preserved). 184/184 contracts PASS with flag
    OFF. Validation experiment: V3-EXQ-518 queued (dry-run PASS 2026-05-04; 9/9 UCs).
  location: docs/architecture/affect_primitives.md

- id: SD-019b
  title: "harm_suffering_accumulator: z_harm_a is specifically the output of a controllability-gated hysteretic integrator over harm_unpleasantness, constituting a persistent load state modulated by escapability and inescapability, not merely a temporally lagged copy of the sensory stream."
  claim_type: design_decision
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-642_2026-06-06
      as_of: 2026-06-06
      verdict: "non_contributory/none -- claimless diagnostic; characterise as a measurement / test-design gap, NOT substrate_ceiling. Do not record substrate_ceiling on any claim from this run."
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  subject: harm_stream.suffering_accumulator
  polarity: asserts
  registered_utc: 2026-04-20
  depends_on:
    - SD-019a         # unpleasantness channel (the integrator's input)
    - SD-019          # nonredundancy constraint (applies to z_harm_a output)
    - MECH-219        # the hysteretic integration mechanism
    - Q-036           # open question on controllability/escapability gating
    - SD-058          # soft edge: avoidance_efficacy escapability source (2026-06-10)
  implementation_note: >
    MECH-219 substrate IMPLEMENTED 2026-06-10
    (ree_core/affect/harm_suffering_accumulator.py + LatentState.z_harm_suffering +
    per-consumer z_harm_a redirect flags). SD-019b STAYS v3_pending / candidate: the
    controllability dissociation that the SD asserts (escapable vs inescapable matched
    nociception -> divergent suffering) is unproven until the validation EXQ runs (memo
    Section 7), and Q-036 stays open. The avoidance_efficacy escapability mode adds a soft
    dependency on SD-058 (itself v3_pending); the default constant mode is dependency-free
    and bit-identical. No flag/confidence change this pass (design + substrate only).
    Plan-of-record: evidence/planning/mech_219_hysteretic_integrator_design.md.
  functional_restatement: >
    z_harm_a is harm_suffering: the output of MECH-219 operating on harm_unpleasantness
    with controllability gating. This refines the current specification of z_harm_a from
    "temporally integrated threat load" to the more precise claim:

      z_harm_a(t) = MECH219_integrate(z_harm_un, escapability_estimate, t)

    The key design requirements, each grounded by evidence:
      1. Input is z_harm_un (harm_unpleasantness), not z_harm_s directly.
      2. Accumulation rate is modulated by an escapability estimate:
         higher when inescapable, lower when harm is controllable.
         (Loffler 2018: controllability selectively reduces suffering;
          Salomons 2004: ACC/insula modulated by controllability belief.)
      3. Decay is asymmetric: slower recovery than onset (persistence property).
      4. The output drives qualitatively different downstream effects than
         z_harm_un: mode-switch trigger (MECH-259), pACC long-horizon coupling
         (SD-032e), and ARC-016 urgency modulation -- not immediate avoidance.

    Falsifiable: if an agent's z_harm_a is not modulated by a controllability/
    escapability signal (all else equal), SD-019b is over-specified and the simpler
    MECH-219 (pure temporal integration) is sufficient.
  notes: >
    Registered 2026-04-20 to formalise the three-way primitive split prompted by the
    Q-036 literature pull and the Loffler et al. (2018) three-way dissociation.
    See affect_primitives.md for the full register and computational pipeline diagram.

    The controllability gate is currently unimplemented in V3. Q-036 needs experimental
    evidence first. SD-019b marks the architectural commitment that the gate should exist;
    the mechanism for implementing it will be specified as a child MECH once Q-036 resolves.
  evidence_quality_note: >
    Registered pre-implementation. Q-036 targeted_review (2026-04-20, 3 entries,
    mean conf=0.72) directly supports the controllability-gating requirement.
    Salomons et al. 2004 (JNeurosci) confirms neural substrate of controllability effect
    on ACC/insula suffering circuit. De Ridder et al. 2021 grounds the medial pathway
    as the suffering substrate with salience/context determination.
  location: docs/architecture/affect_primitives.md

- id: MECH-219
  title: affective_harm_hysteretic_integration
  claim_type: mechanism_hypothesis
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  subject: harm_stream.hysteretic_affective_load
  polarity: asserts
  description: >
    `z_harm_a` (harm_suffering) is generated from harm_unpleasantness (`z_harm_un`, SD-019a)
    by a hysteretic integration rule: affective load rises under sustained or repeated
    exposure and decays more slowly once exposure stops. The resulting signal is not
    reconstructible from instantaneous sensory harm alone and provides E3 and the
    salience-network coordinator with a genuine suffering channel rather than a second copy
    of sensory proximity. The first operational form is a leaky integrator over
    harm_unpleasantness with asymmetric onset and recovery parameters. A controllability
    gate (SD-019b) modulates the accumulation rate: identical harm_unpleasantness produces
    higher z_harm_a when harm is inescapable than when it is controllable (Loffler 2018;
    Salomons 2004).
  notes: >
    Updated 2026-04-20 to reflect the three-primitive split (affect_primitives.md).
    MECH-219 is the mechanism implementing SD-019b: it takes z_harm_un as input (not
    z_harm_s directly) and produces z_harm_a. The key revision from the original
    specification: the integration is not over raw sensory harm but over the pre-processed
    medial-pathway unpleasantness signal, and the accumulation rate is controllability-gated.

    A pure delay (`z_harm_a(t) = z_harm_s(t-k)`) is insufficient: it preserves
    recoverability from the sensory stream and ignores the controllability variable.
    Hysteretic integration satisfies SD-019's nonredundancy constraint and SD-019b's
    suffering-specific accumulation requirement.

    Testable predictions (updated):
    (1) After harm offset, `z_harm_un` falls rapidly while `z_harm_a` remains elevated.
    (2) Repeated brief harms summate in `z_harm_a` more than single isolated harms of
        equal total magnitude.
    (3) Two trajectories matched on current `z_harm_un` but with different controllability
        histories produce different `z_harm_a` norms and different mode-switch rates.
    (4) `z_harm_a` drives mode-switch (MECH-259) while `z_harm_un` drives immediate
        avoidance -- the two channels have separable behavioral signatures.
  registered: "2026-04-08"
  depends_on: [SD-019, SD-019a, SD-019b, SD-011, ARC-016, SD-058]  # SD-058 soft edge: avoidance_efficacy escapability source (2026-06-10)
  implementation_note: >
    IMPLEMENTED 2026-06-10 (substrate; status stays candidate / no confidence change --
    governance follows the validation EXQ). Pure-arithmetic hysteretic-integrator regulator
    ree_core/affect/harm_suffering_accumulator.py: drive u_t=||z_harm_un|| (+ optional
    body_damage_weight*||z_harm_a|| fold-in); controllability gate g_t=1-escapability;
    asymmetric accumulation alpha_rise>>alpha_fall; optional Schmitt latch + optional SD-020
    pe_gain. Output LatentState.z_harm_suffering (same dim as z_harm_un, magnitude=s_t).
    Escapability source pluggable (REEConfig.harm_suffering_escapability_mode): default
    constant=1.0 -> g=0 -> inert/bit-identical; avoidance_efficacy reads SD-058
    InstrumentalAvoidanceGate.effective_efficacy() (the soft depends_on above; never
    MECH-353 capacity_belief, to avoid the z_harm_a->capacity_belief->z_harm_a loop);
    external for a scripted validation schedule. Master flag use_harm_suffering_accumulator
    + per-consumer redirect flags (all default OFF, bit-identical): v1 wires the
    urgency/PAG/MECH-091-interrupt consumers (magnitude-based); dACC/pACC redirect flags
    defined-but-unwired (their E2_harm_a forward models are keyed on the z_harm_a dim ->
    migrate last, kept on legacy z_harm_a in v1). MECH-094: no-op under simulation_mode.
    Requires use_harm_un=True (loud precondition). 11/11 contracts
    (ree-v3/tests/contracts/test_mech_219_harm_suffering_accumulator.py). Validation EXQ
    (controllability dissociation) is a separate /queue-experiment session (memo Section 7).
    Plan-of-record: evidence/planning/mech_219_hysteretic_integrator_design.md; as-built:
    docs/architecture/mech_219_hysteretic_integrator.md.
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: the run must actually exercise the accumulator
    substrate, not merely have it present-but-inert. use_harm_suffering_accumulator=True
    (master flag, default OFF/bit-identical) AND use_harm_un=True (loud precondition
    per the 2026-06-10 implementation_note) must both be set, and the relevant
    consumer redirect flag(s) for whichever downstream signature is being tested
    (e.g. the mode-switch consumer for prediction 4) must be enabled -- most
    default OFF except the v1 urgency/PAG/MECH-091-interrupt wiring. z_harm_un
    itself must show real dynamic range within the run (nonzero variance across
    onset/offset events) -- a flat or near-constant z_harm_un gives the
    integrator nothing to demonstrate hysteresis over. A run failing any of these
    self-routes substrate_not_ready, not a verdict on the mechanism. The 11/11
    unit contracts already on file (test_mech_219_harm_suffering_accumulator.py)
    establish only that the arithmetic (drive computation, alpha_rise/alpha_fall,
    gate math) matches spec -- they are NOT evidence for this claim's behavioral/
    representational content and should not be cited as validation.
    Scope note: this falsifier covers the TEMPORAL/HYSTERETIC content of MECH-219
    (its own predictions 1, 2, 4 below). The controllability-gating question
    (originally this claim's prediction 3) was narrowed out to MECH-305
    (2026-05-04), which is a separate, currently lit-only claim depending on
    MECH-219. Do not treat a controllability-only demonstration as confirming
    MECH-219, and do not fold a MECH-219 temporal-signature test into a
    MECH-305 controllability test -- keep the two experiments and their
    verdicts separate.
    CONFIRMING signature (all should hold, on a substrate meeting the
    precondition above):
    (1) Asymmetric decay: after harm offset, z_harm_un falls rapidly while
        z_harm_a remains elevated and decays measurably slower (e.g.
        alpha_fall << alpha_rise reflected in fitted onset vs recovery time
        constants, not just eyeballed trajectories).
    (2) Non-linear summation: repeated brief harm exposures produce a higher
        z_harm_a than a single continuous exposure of equal cumulative
        duration x intensity -- i.e. the signal is not just a rescaled EMA of
        total dose, but genuinely path-dependent.
    (3, formalizing prediction 4) Separable behavioral signature: z_harm_a
        (not z_harm_un) is the variable that predicts mode-switch rate
        (MECH-259), while z_harm_un drives immediate avoidance amplitude --
        an ablation or regression showing the two channels dissociate, not
        that either alone explains both behaviors.
    (4) Nonredundancy: z_harm_a is NOT reconstructible from z_harm_un by a
        simple monotonic or time-shifted transform (low r2 / low cosine-sq
        overlap, consistent with the SD-019 nonredundancy metric this claim
        depends on) -- i.e. the explicit "pure delay" null model
        (z_harm_a(t) = z_harm_s(t-k)) that this claim's own notes reject is
        actually tested and rejected, not assumed.
    FALSIFYING signature: decay is symmetric or z_harm_a collapses as fast as
    z_harm_un after offset (no genuine hysteresis); repeated-brief and
    single-continuous exposures of matched cumulative dose produce
    statistically indistinguishable z_harm_a (no summation advantage, i.e.
    it behaves as a simple EMA on raw exposure); z_harm_a is reconstructible
    from z_harm_un with high r2/cosine-sq (fails nonredundancy, reduces to
    the rejected pure-delay model); or z_harm_a and z_harm_un predict
    mode-switch and avoidance interchangeably with no separable signature.
    Any of these would mean the hysteretic-integrator design adds no
    functional content beyond a delayed copy of the sensory stream, and the
    claim should be revisited rather than simply re-run with different
    hyperparameters.
  location: docs/architecture/affect_primitives.md

- id: Q-036
  title: "What additional variables, beyond temporal integration, are required for affective harm to become a genuinely distinct load state: persistence, recovery failure, uncontrollability, inescapability, or prediction error?"
  claim_type: open_question
  subject: harm_stream.affective_load_drivers
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-036@2026-05-07T04:14:02Z"
      as_of: 2026-05-07
      verdict: narrow_open_question/applied
  what_would_answer: |
    A discriminative-pair experiment that adds each candidate non-temporal
    variable to the MECH-219 hysteretic integrator one at a time and tests whether
    it produces a functionally distinct z_harm_a urgency signal beyond what
    temporal integration alone yields. For each variable -- persistence of
    exposure, recovery failure after offset, uncontrollability/inescapability,
    prediction-error-weighted threat -- contrast a pair of episodes matched on
    cumulative duration x intensity (so the pure temporal integrator is held
    constant) but differing on that one variable. ANSWER "variable X is required"
    if the matched pair produces a SEPARABLE z_harm_a / downstream-urgency
    response (X-present vs X-absent differ beyond temporal-integration prediction)
    AND the split is specific to z_harm_a (affective) while leaving z_harm_un
    (sensory, SD-019a) unmodulated -- the controllability branch already meets
    this bar (Loffler 2018 / Salomons 2004: controllability dissociates suffering
    from intensity), and was carved out as candidate MECH-305. ANSWER "temporal
    hysteresis is sufficient" for a given variable if duration-matched pairs are
    indistinguishable on z_harm_a regardless of that variable, i.e. MECH-219 needs
    no extra weighting term. NARROWED STATUS: controllability/escapability is
    empirically settled (-> MECH-305); the SHARPER remaining test is whether
    persistence, recovery-failure, and prediction-error weighting EACH
    independently carve z_harm_a, or whether they collapse into the
    controllability axis. HARD PRECONDITION: the substrate must support a z_harm_a
    urgency readout that is not pinned -- if affective load shows no dynamic range
    under matched temporal load, the discriminative pair is vacuous.
  depends_on:
    - SD-019
    - MECH-219
  location: docs/architecture/sensory_stream_tags.md#q-036
  short: >
    Is temporal hysteresis sufficient for `z_harm_a`, or must affective load also depend on
    persistence, recovery failure, uncontrollability, inescapability, or surprise?
  description: >
    Temporal integration is the most conservative mechanism for differentiating affective
    harm from sensory harm, but it may not be sufficient. Human affective pain and threat
    load appear to depend not only on duration and intensity but also on whether harm is
    escapable, controllable, surprising, or recurrent despite attempted avoidance. The
    answer determines whether MECH-219 can remain a simple hysteretic integrator or whether
    `z_harm_a` requires additional weighting terms. REE implication: if temporal hysteresis
    alone fails to produce a functionally distinct urgency signal, the next candidate
    variables should be persistence of exposure, recovery failure after offset,
    uncontrollability or inescapability, and prediction-error weighted threat.
  suggested_experiment_type: discriminative_pair
  targeted_reviews:
    - targeted_review_q_036
  evidence_quality_note: |
    Lit-pull 2026-04-20 (3 entries, targeted_review_q_036). lit_conf 0.808; no weakening
    evidence; conflict_ratio=0. quadrant=plausible_unproven (exp_conf=0, no experiments yet).

    Loffler et al. 2018 (Pain Reports, conf=0.72): Three-way behavioural dissociation --
    controllability selectively reduces suffering while leaving pain intensity and
    unpleasantness unchanged. Direct evidence that controllability is a required variable
    for z_harm_a beyond temporal integration. Architectural implication: z_harm_un (SD-019a)
    is NOT modulated by controllability; the gate is specific to z_harm_a. Satisfies the
    SD-019a social gate.

    Salomons et al. 2004 (J Neurosci, conf=0.75): fMRI -- identical noxious stimuli under
    controllable vs uncontrollable conditions produce differential ACC/insula/SII activation.
    Controllability (orthogonal to stimulus intensity) drives the affective pain circuit.
    Direct evidence that at least one non-temporal variable modulates affective load.

    De Ridder et al. 2021 (Neurosci Biobehav Rev, conf=0.70): Three-pathway model
    (lateral=painfulness; medial=suffering; descending inhibitory). Suffering is determined
    by salience and context (behavioural meaning), not sensory magnitude alone. Supports the
    premise that temporal integration is insufficient; context variables are required. Does
    not decompose into the specific variables Q-036 lists (persistence, recovery failure, PE).

    PARTIAL ANSWER: controllability/escapability confirmed as a required variable for z_harm_a
    beyond temporal integration. Persistence, recovery failure, and prediction-error weighting
    remain untested. narrow_open_question appropriate: the controllability sub-question is
    now empirically settled; the broader variable set is open.

    NARROWED 2026-05-04: controllability branch converted to candidate MECH-305
    (harm_stream.controllability_weighted_affective_load). Q-036 remains open for
    persistence, recovery failure, inescapability, and prediction-error variables.

- id: INV-065
  title: proxy_goal_necessity
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - SD-014
    - SD-015
  pending_substrate_reconfirmation: true
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  subject: goal.multi_step_planning
  description: >
    Any agent architecture with a bounded planning horizon and a sparse reward
    landscape requires intermediate wanting-rich states (proxy goals) to exhibit
    multi-step goal-directed behavior beyond the planning horizon. In REE terms: the
    CEM planner operates over a fixed horizon (~10 steps). In environments where
    resources are sparse or distal, resource z_world states will frequently lie
    outside that horizon. Without intermediate states that have acquired wanting
    value through schema binding or trajectory consolidation, the CEM score surface
    has no gradient toward the resource -- the agent cannot plan beyond 10 steps.
    Proxy goals are not a design choice but an architectural necessity: they are
    whatever z_world states have acquired wanting value through experience-dependent
    association, making the wanting landscape navigable at all scales. This is an
    invariant prediction about any planning architecture with these properties,
    independent of implementation details.
  notes: >
    The proxy goal mechanism emerges automatically in REE via two routes:
    (1) E1 schema-conditioned wanting (MECH-216): schema slots predicting resource
    encounter seed VALENCE_WANTING before direct contact, extending wanting coverage
    to E1-predictive z_world states.
    (2) Trajectory-consolidated wanting (MECH-217): reverse replay spreads wanting
    backward along approach paths, making every waypoint a plannable sub-goal.
    Neither route requires explicit proxy goal design -- they are consequences of
    the wanting landscape architecture (SD-014, SD-015) combined with E1 schema
    formation and offline consolidation. The invariant predicts that resource
    acquisition rate in sparse environments will be strongly gated by the density
    of proxy goals in the wanting landscape.
  registered: "2026-04-08"
  epistemic_category: substrate_conditional
  depends_on: [SD-014, SD-015, MECH-216, MECH-217]
  what_would_answer: |
    INV-065 is the umbrella prediction over MECH-216 and MECH-217. As of 2026-08-07,
    NEITHER underlying mechanism has cleared to a state that supports a direct
    behavioural test of INV-065 itself.

    NON-DEGENERACY PRECONDITION:
      - Test environment must place a resource genuinely beyond the CEM horizon
        (~10 steps) under sparse/distal reward.
      - MECH-217's mechanism-readiness leg is CONFIRMED PASS (V3-EXQ-842,
        2026-07-30) -- reusable, do not re-test.
      - MECH-216's density-floor precondition (positive_label_rate > 0.10) has NOT
        cleared: EXQ-332a measured 0.00128, ~78x below floor, no retest since.

    CONFIRMS if, once cleared: a four-cell {MECH-216 ON/OFF} x {MECH-217 ON/OFF}
    design in the beyond-horizon environment shows BOTH-ON exceeding BOTH-OFF by a
    margin clearing cross-seed SD, BOTH-OFF is indistinguishable from a
    no-gradient/random-walk floor, and acquisition rate scales with proxy-goal
    density in the intermediate cells per each mechanism's own spatial signature.

    FALSIFIES if: BOTH-OFF is statistically indistinguishable from BOTH-ON --
    proxy-goal density has no measurable effect on beyond-horizon planning,
    contradicting the "architectural necessity" claim.

    INCONCLUSIVE if MECH-216's density floor still fails to clear after redesign,
    or the apparatus itself is unbuilt.

    CURRENCY (2026-08-07): no experiment currently targets this four-cell design.
    It generalizes (not duplicates) MECH-217's own not-yet-built LEG 2 apparatus.

    (Digested 2026-08-07, session cool-torvalds-a82359, thought-digestion wave 10.
    Extracted from MECH-216/MECH-217's already-digested what_would_answer blocks.)

- id: MECH-216
  title: e1_predictive_wanting
  claim_type: mechanism
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_EXQ-539-540_MECH307_2026-05-17#V3-EXQ-540f"
      as_of: 2026-05-17
      verdict: superseded/measurement_design_broken_criterion
  subject: goal.e1_schema_wanting
  implementation_phase: v3
  implementation_note: >
    IMPLEMENTED 2026-04-09. E1DeepPredictor.schema_readout_head (Linear(hidden_dim,1)+Sigmoid)
    reads LSTM top-layer hidden state -> schema_salience [0,1]. Agent caches in _schema_salience
    via _e1_tick(), seeds VALENCE_WANTING when > threshold via update_schema_wanting().
    Config: E1Config.schema_wanting_enabled (False default), REEConfig.schema_wanting_threshold
    (0.3), schema_wanting_gain (0.5). Training: compute_schema_readout_loss(resource_proximity_target).
    Validation: EXQ-263 queued.
  description: >
    E1 schema activations that reliably predict resource encounters seed VALENCE_WANTING
    at the current z_world position before resource contact. This is a Pavlovian
    conditioned wanting mechanism: E1 acts as a CS-detector (firing on environmental
    features that precede resources), and its activation seeds the same wanting signal
    as the UCS (resource contact itself). The result is a wanting landscape that
    extends beyond directly-encountered resource positions -- it also includes z_world
    states from which resources are predictable via E1's learned schemas. The
    hippocampus then navigates toward these E1-predicted wanting regions, producing
    approach behavior toward schema-activating intermediate states (proto-proxy goals)
    before the direct wanting signal from SD-014 is available.
  notes: >
    Mechanistic basis: E1's LSTM hidden state encodes schema activations. These
    schemas form from temporal co-occurrence: E1 learns to predict z_world_t+1 from
    z_world_t, and internally differentiates episodes in which resource contact
    subsequently occurs from those that do not. Schema activation (the internal
    E1 state preceding resource-predictive transitions) becomes the CS signal.
    Implementation path in V3: requires reading out E1 schema activation magnitude
    at each waking step and using it to modulate SerotoninModule.update_benefit_salience()
    (which currently requires resource contact to update VALENCE_WANTING). A schema
    activation signal above threshold would seed partial wanting without contact.
    This is the forward-looking complement to MECH-217's backward consolidation.
    DR-13 (z_self temporal depth) is not required -- this operates in z_world domain.
    Distinguishable from direct contact wanting by spatial offset: E1 wanting fires
    at approach positions, contact wanting fires at resource position.
  registered: "2026-04-08"
  depends_on: [SD-014, SD-018, MECH-203, INV-065]
  epistemic_category: substrate_conditional
  what_would_answer: |
    A properly-powered retest of the E1-schema predictive-wanting mechanism,
    run only after a measurement redesign (reward-dense episodes, longer
    future-target windows, or a denser auxiliary supervision signal) raises
    positive_label_rate above the quality-density floor established by
    EXQ-332a.

    NON-DEGENERACY PRECONDITION (must clear before the run counts as
    evidence either way -- otherwise the result is non_contributory, per
    the pre-registered rule carried over unchanged from EXQ-332/332a):
      - positive_label_rate (fraction of P1 steps with future_target=1)
        > 0.10 (C3 quality gate; EXQ-332a measured 0.00128, ~78x below
        floor)
      - future_target_corr in the PREDICTIVE_CUE_ON arm must be a genuine,
        non-degenerate correlation (not an artifact of near-zero variance
        in near-empty positive labels) -- adequacy of label density is
        what C3 checks for before C1 is interpretable at all.

    CONFIRMS MECH-216 (supports) if, once C3 clears:
      - C1: future_target_corr(schema_salience_t, future_target_t) > 0.10
        in the PREDICTIVE_CUE_ON condition (schema activation carries
        predictive signal above noise), AND
      - C2: PREDICTIVE_CUE_ON future_target_corr > ABLATED
        future_target_corr (the E1 schema-cue, not some other correlate,
        is what enables the prediction), AND
      - the resulting wanting seeding shows the mechanism's own
        distinguishing spatial signature: schema-driven wanting fires at
        approach/intermediate positions from which a resource is
        predictable but not yet contacted, spatially offset from
        direct-contact wanting, which fires only at the resource position
        itself. (EXQ-263b's diagnostic dry run already demonstrated this
        offset in principle: salience_at_landmark_b=0.83 enabled vs 0.0
        ablated -- this needs re-confirmation under the corrected,
        dense-label supervision scheme, not fresh invention.)

    FALSIFIES MECH-216 (does_not_support) if, once C3 clears (density
    floor met, so the run is genuinely diagnostic):
      - C1 and/or C2 fail -- schema salience does not predict future
        resource contact above noise, or does no better than an ablated
        (schema-blind) control, even with adequate positive-label
        density. This would mean E1's LSTM hidden state does not carry
        the CS-detector signal the mechanism claims, ruling out a
        measurement-density explanation for non-support.

    INCONCLUSIVE / hold at provisional (repeat, do not adjudicate) if:
      - C3 still fails to clear 0.10 after the redesign -- indicates the
        redesign itself was insufficient and needs a further iteration,
        not that the mechanism is refuted.

    CURRENCY (2026-08-07): the measurement redesign called for on
    2026-04-18 has NOT happened -- no MECH-216/schema_wanting retest of
    any kind since EXQ-332a. The live_status.evidence pointer above
    ("failure_autopsy_EXQ-539-540_MECH307_2026-05-17#V3-EXQ-540f",
    verdict superseded/measurement_design_broken_criterion) is a stale
    cross-tagging artifact: that autopsy is about MECH-307's own C1
    criterion (liking_writes), not MECH-216's future_target_corr
    criterion -- MECH-216 was only incidentally co-tagged on EXQ-539's
    manifest, and the autopsy's "Confirmed Routing" section only retains
    MECH-216's existing supports direction, never proposes changing this
    evidence pointer. This field should be corrected by governance
    separately from this what_would_answer addition -- flagged, not fixed
    here.
  evidence_quality_note: >
    Promoted to provisional (governance-2026-04-10): conf=0.834, conflict_ratio=0.25,
    7 supports / 1 opposing. EXQ-263b (diagnostic dry run, 2026-04-10) confirmed schema
    salience seeding working: salience_at_landmark_b=0.83 (ENABLED) vs 0.0 (ABLATED),
    C1 PASS. The wanting substrate populates correctly from E1 schema activations.
    EXQ-332 (full run with future-target supervision + resource_obs_scale attenuation)
    queued to test the predictive (not just reactive) wanting criterion -- C2/C3
    require temporal prediction, not proximity detection.
    EXQ-332 (2026-04-16, FAIL): First full run with future-target supervision +
    resource_obs_scale attenuation. C2/C3 (temporal prediction criteria) FAIL.
    Prior confidence (0.834) was based on EXQ-263b diagnostic dry run, not a
    genuine evidential run. EXQ-332 is the first real test; result is
    does_not_support. Flagged for /diagnose-errors: check whether resource_obs_scale
    attenuation is sufficient for temporal salience encoding.
    EXQ-332a (2026-04-18, diagnostic FAIL, non_contributory per governance
    2026-04-18): C1 future_target_corr=0.013 and C3 positive_label_rate=0.00128
    (threshold 0.10) mean the supervision density is two orders of magnitude
    below what the diagnostic requires. This is a measurement/design gap, not a
    mechanism refutation -- the current future-target-window scheme does not
    produce enough positive labels to learn predictive structure. Measurement
    redesign (reward-dense episodes, longer windows, or denser aux signal)
    required before MECH-216 can be re-adjudicated. Hold at provisional.

- id: MECH-217
  title: temporal_wanting_propagation
  claim_type: mechanism
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-30
    needs_review: false
    evidence:
      from: failure_autopsy_batch-687a-707c-840-748a-833-842-810b-673-614-798afail_2026-07-30
      as_of: 2026-07-30
      verdict: supports/standard -- V3-EXQ-842 offline wanting-spread readiness verified (clean unflagged PASS)
  subject: goal.replay_wanting_spread
  implementation_phase: v3
  description: >
    Offline consolidation via reverse replay (MECH-165) spreads VALENCE_WANTING
    backward along previously successful approach trajectories. During SWS replay,
    the reversed world_state sequence of resource-approach episodes is replayed through
    the residue field. Each waypoint along the reversed trajectory receives a fractional
    wanting update proportional to the resource-contact wanting at the episode terminus
    and decayed by distance from the contact event. This produces a wanting gradient
    along the approach path: positions closer to the resource acquire stronger wanting
    signal than positions farther away, creating a navigable gradient from any waypoint.
    The practical effect is that multi-step goal-directed behavior emerges from
    single-resource encounters: the agent can navigate toward any previously-traversed
    waypoint (within CEM horizon) that was part of a resource-approach path.
  notes: >
    Mechanism requires MECH-165 (reverse replay substrate). MECH-165 IMPLEMENTED
    2026-04-09 (exploration buffer + reverse replay + diverse scheduler). Substrate
    gap resolved. The wanting-spread function can be added to
    HippocampalModule.reverse_replay() --
    after generating the reversed Trajectory, iterate through world_states and call
    ResidueField.update_valence() with decayed wanting at each step.
    Decay schedule: wanting_at_waypoint = wanting_at_terminus * gamma^(steps_from_terminus),
    gamma ~0.9. RBF bandwidth handles spatial smoothing.
    This is the consolidation-mediated complement to MECH-216's waking schema
    conditioning. Together they populate the wanting landscape at all temporal scales:
    MECH-216 (waking, forward-looking), MECH-217 (offline, backward-spreading).
    Distinguishable from MECH-216: MECH-217 wanting is path-specific (concentrated
    along previously traversed routes), MECH-216 wanting is schema-specific (concentrated
    at feature-activating positions regardless of prior path).
  implementation_note: >
    IMPLEMENTED 2026-07-30 (IGW-20260729-195). HippocampalModule.
    spread_reverse_replay_wanting() (ree_core/hippocampal/module.py), called
    from REEAgent.run_rem_attribution_pass() (agent.py) for each reverse-
    replayed trajectory generated during the SD-017 REM pass -- NOT inside
    the shared reverse_replay()/diverse_replay() methods as originally
    sketched above, because those are also invoked by the WAKING quiescent
    replay path (MECH-092, agent.py _do_replay()) whenever mode="auto" rolls
    "reverse"; writing there would leak the offline effect into waking
    replay, which is documented to produce no residue. Config:
    HippocampalConfig.use_offline_wanting_spread (default False),
    offline_wanting_spread_gamma (default 0.9, matches this claim's decay
    schedule), offline_wanting_spread_gain (default 0.1, an added
    numerical-stability guard not in the original claim text -- bounds the
    write to a fraction of the terminus value rather than a 1:1 copy, and
    the terminus itself is never self-written, so repeated REM cycles
    cannot compound the read value into a feedback loop). Backward
    compatible: disabled by default. Validation experiment EXP-0301 /
    EVB-0349 queued. See ree-v3/CLAUDE.md MECH-217 entry for full data flow.

    2026-07-30 (governance-apply, confirmed failure_autopsy_batch-687a-707c-840-748a-833-842-810b-673-614-798afail):
    V3-EXQ-842 (the queued EVB-0349 validation) ran clean -- supports/standard, unflagged PASS,
    offline wanting-spread readiness verified against the just-landed substrate above.
  registered: "2026-04-08"
  depends_on: [MECH-165, SD-014, INV-065]
  epistemic_category: substrate_conditional
  what_would_answer: |
    MECH-217 is CONJUNCTIVE across two dissociable sub-claims (mirrors the
    MECH-216/SD-015 pattern used elsewhere this session), and only one is
    answered.

    LEG 1 -- MECHANISM READINESS (does the offline reverse-replay spread
    fire and produce a non-vacuous, non-degenerate wanting gradient) is
    CONFIRMED, PASS. V3-EXQ-842 (2026-07-30, 3 seeds x 2 arms) measured,
    against the pre-registered thresholds: load-bearing C1 (on_near_wanting
    exceeds off_near_wanting by >=1.5x): PASS -- mean_on_near_wanting=17.05
    vs mean_off_near_wanting=1.00 (~17x margin); non-load-bearing C0 (OFF
    arm writes zero spread steps): PASS; C2 (near wanting exceeds far
    wanting, decay with distance): PASS -- mean_on_near=17.05 vs
    mean_on_far=4.24; C3 (control position below near-path wanting): PASS
    -- mean_on_control=0.46. Do not re-test this leg.

    LEG 2 -- BEHAVIOURAL CONSEQUENCE (does the resulting path-specific
    wanting gradient actually CAUSE navigable multi-step goal-directed
    behaviour -- this claim's own description: "the agent can navigate
    toward any previously-traversed waypoint... within CEM horizon") is
    UNTESTED, and is the reason this claim stays candidate rather than
    promoting. V3-EXQ-842's own "notes" field reads "Not a behavioural
    navigation-lift claim," and it substitutes a SCRIPTED greedy walk for
    the agent's own navigation/CEM policy -- the diagnostic reads the
    residue field, it does not exercise action selection against it. No
    other experiment, queue entry, or backlog item tests this leg.
    substrate_queue.json's MECH-217 entry states this was deliberate:
    what_is_NOT_owed reads "Not a behavioural navigation/commitment layer
    -- the validation is a measurement-only residue readout," closing
    "Any residual MECH-217 work is a governance/promotion question in
    claims.yaml -- not a substrate build and not a re-run." So LEG 2 is
    not blocked by any known substrate ceiling -- the write path exists
    and LEG 1 confirms it is non-degenerate -- it has simply never been
    designed or queued.

    FALSIFIER DESIGN for LEG 2 (not yet run): starting from a position
    NEVER directly associated with the resource itself, does the agent's
    own action-selection/CEM policy navigate toward a previously-traversed
    waypoint (within CEM horizon) that lies along a prior resource-approach
    path, at a rate exceeding a no-spread
    (use_offline_wanting_spread=False) control? CONFIRMS if approach-rate
    / benefit-ratio toward such waypoints exceeds the OFF control by a
    margin clearing cross-seed SD, using the agent's real navigation
    policy (not V3-EXQ-842's scripted greedy walk). FALSIFIES if the
    wanting gradient is present and non-degenerate but produces no
    measurable navigational preference over the OFF control -- the
    residue write exists but is behaviourally inert, mirroring the
    SD-037/483d-483e "wired but inert at the consumer" pattern.

    ISOLATING MECH-217 FROM MECH-216 in a joint test: both write to the
    same VALENCE_WANTING landscape. A joint behavioural run must ablate
    MECH-216 (schema_wanting_enabled=False) while holding MECH-217 ON, so
    any observed navigational pull is attributable to the path-specific
    signature rather than MECH-216's feature/schema-specific one.

    NON-DEGENERACY / CONFOUND WARNING for any future LEG-2 run:
    offline_wanting_spread_gain defaults to 0.1 -- a fraction of terminus
    wanting per write, by design. V3-EXQ-842 shows this gain is NOT
    vacuous in the readiness diagnostic (17x margin, cumulative over ~206-
    228 rem_wanting_spread_n_steps per ON-arm seed), but that magnitude
    was measured as a residue-field readout, not against competing
    wanting sources or action-selection noise in a full behavioural loop.
    A LEG-2 probe with too few cycles could show no navigational
    preference for reasons of insufficient accumulated magnitude rather
    than mechanism failure -- the same shape of confound as
    ARC-071/V3-EXQ-871's flag-scoping error. Any LEG-2 design should
    report cumulative rem_wanting_spread_n_steps / cycle count alongside
    the behavioural DV so a null result can be distinguished from an
    under-accumulated one.

    CURRENCY (2026-08-07): no behavioural/navigation test for MECH-217
    has been queued or run since V3-EXQ-842 (2026-07-30).

- id: MECH-218
  title: interoceptive_predictive_wanting
  claim_type: mechanism
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  subject: goal.interoceptive_wanting
  implementation_phase: v4
  description: >
    When z_self depletion trend is established (energy falling before drive_level
    becomes acutely high), E2's self-model projects forward and seeds anticipatory
    homeostatic wanting -- VALENCE_WANTING populated by predicted future deficit rather
    than by current resource proximity or schema activation. This produces approach
    behaviour that begins before the need is critical: seeking before thirsty, rather
    than seeking when thirsty. The signal flows from E2 self-transition predictions
    (depletion rate) through GoalConfig.drive_weight modulation into VALENCE_WANTING
    seeding, effectively increasing the drive-level multiplier before the actual
    drive_level threshold is crossed. This is the anticipatory homeostatic extension
    of SD-012 (reactive homeostatic drive modulation).
  notes: >
    V4 gated. Requires DR-13: z_self temporal depth. Without temporal depth in z_self
    (currently body_obs -> MLP -> EMA, no recurrence), E2 cannot estimate depletion
    *rate*, only current depletion *level*. DR-13 requires either recurrent z_self
    encoder or E2 feedback into z_self enrichment (E2 as self-model with multi-step
    prediction). Once DR-13 is available: the depletion rate signal can modulate
    SerotoninModule (or a parallel interoceptive neuromodulator) to inject wanting
    proportional to projected future deficit.
    Distinguishable from SD-012 (reactive): reactive wanting fires on current
    drive_level; predictive wanting fires on drive_level_rate (trend). Dissociation:
    an agent with a depleted but stable energy level should NOT show MECH-218 wanting;
    an agent with rapidly-falling energy at high baseline should show MECH-218 wanting
    before the absolute threshold is crossed.
    Clinical note: anticipatory homeostatic regulation failure (unable to seek before
    critical deficit) is a component of executive dysfunction in ADHD and depression
    where prospective self-model accuracy is degraded.
  registered: "2026-04-08"
  depends_on: [SD-012, MECH-203, INV-065]

- id: ARC-051
  title: multi_level_wanting_goal_hierarchy
  claim_type: architecture_hypothesis
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-051@2026-04-26T16:17:25Z"
      as_of: 2026-04-26
      verdict: hold_pending_v3_substrate/applied
  subject: goal.emergent_hierarchy
  implementation_phase: v3
  description: >
    The combination of the VALENCE_WANTING landscape (SD-014), E1 schema-conditioned
    wanting (MECH-216), and trajectory-consolidated wanting (MECH-217) produces an
    emergent multi-level goal hierarchy without explicit goal-hierarchy design. Level 1:
    direct resource encounter wanting (SD-018 + MECH-203, short-range, precise).
    Level 2: E1-schema-mediated predictive wanting (MECH-216, medium-range, schema-
    specific). Level 3: trajectory-consolidated wanting (MECH-217, long-range,
    path-specific). All three levels populate the same VALENCE_WANTING field; the CEM
    scorer responds to the combined gradient, enabling planning at all scales from a
    single unified navigation mechanism. The architecture is sufficient for multi-step
    goal-directed behavior without introducing a separate goal-planning subsystem.
  notes: >
    The hierarchy is emergent in the strict sense: it is not encoded anywhere as
    a hierarchy -- it is a consequence of three separate mechanisms (contact-wanting,
    schema-wanting, replay-wanting) that happen to operate at different spatial and
    temporal scales and all feed the same VALENCE_WANTING field. The CEM planner
    cannot distinguish which level it is responding to; it only sees the combined
    gradient. This has a prediction: removing any one level degrades multi-step
    performance at the scale that level operates -- removing MECH-217 degrades
    performance specifically in paths longer than the E1 schema radius; removing
    MECH-216 degrades performance in environments with reliable approach cues but
    no clear CEM-horizon gradient.
    The V4 interoceptive level (MECH-218) extends the hierarchy into the z_self
    domain (ARC-031 territory) but is not required for the V3 multi-step claim.
    Level 4 (MECH-218, V4): interoceptive anticipatory wanting (requires DR-13).
  registered: "2026-04-08"
  depends_on: [SD-014, MECH-216, MECH-217, INV-065, SD-018]
  epistemic_category: standard
  what_would_answer: >
    Three-arm ablation (full hierarchy / MECH-217-removed / MECH-216-removed),
    predicting scale-specific degradation: removing MECH-217 hurts paths longer
    than the E1 schema radius; removing MECH-216 hurts environments with reliable
    approach cues but no clear CEM-horizon gradient.

    NON-DEGENERACY PRECONDITION: all three write paths (SerotoninModule.update_benefit_salience,
    REEAgent.update_schema_wanting, HippocampalModule.backward_credit_sweep) verified firing
    via logged write counts (not just flag state) in the full-hierarchy arm; hippocampal.wanting_weight
    > 0; task suite has genuine independent variation in path-length-vs-schema-radius AND
    cue-reliability-vs-gradient-clarity so the two conditions do not collapse onto each other.

    CONFIRMING: degradation is concentrated at each level's claimed operating scale (near-parity
    to full-hierarchy outside that scale), not uniform across scales.

    FALSIFYING: (1) removing a level produces no measurable degradation anywhere (not
    load-bearing under the tested distribution); or (2) degradation is uniform across scales
    (undifferentiated pooled wanting signal, not an emergent scale-differentiated hierarchy);
    or (3) the level-to-scale mapping is inverted (removing MECH-216 hurts long paths more
    than removing MECH-217, or vice versa).

    [2026-08-08 thought-digestion]: substrate confirmed BUILT and wired -- all three write
    paths exist in ree-v3/ree_core, gated by independent default-off flags
    (serotonin.tonic_5ht_enabled, e1.schema_wanting_enabled, hippocampal.use_backward_credit_sweep),
    composable into the ablation with zero new implementation. Zero experimental evidence
    exists (claim_evidence.v1.json: 4 literature entries only); no experiment queued. Note for
    the eventual queue entry: SD-014's wanting-write was found entangled with VALENCE_LIKING
    (V3-EXQ-887, |Spearman|=0.93-0.97); a decoupling fix (incentive_sensitization_enabled)
    landed 2026-08-07 but stays default-off -- state explicitly which state this ablation runs
    under.

- id: SD-020
  title: "z_harm_a encodes affective surprise (precision-weighted PE), not raw accumulated harm state. The affective stream training target is the mismatch between predicted and actual harm accumulation, making E3 urgency respond to unexpectedness of threat, not just magnitude."
  claim_type: design_decision
  subject: harm_stream.affective_surprise_pe
  default_off_scope_note: 'SD-020''s stable/PASS reading (V3-EXQ-324b) is scoped to the flag-on
    configuration. harm_surprise_pe_enabled defaults to False (ree_core/utils/config.py:2656), so default-trained
    agents train z_harm_a against the EMA accumulated-harm target this claim argues against. See
    SD-087 (2026-06-10 z_harm_a saturation intake, candidate 2) for the two-arm OFF-vs-ON falsifier;
    consider a standing default_off audit across other claims.'
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-08-10
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-324d_2026-08-10"
      as_of: 2026-08-10
      verdict: non_contributory/promote_to_provisional_applied -- first genuine well-powered real-flag-path test since the 2026-08-09 demotion, 1/5 seeds clean pass, 4/5 near-zero (cross-cutting training-instability suspected, not evidence against)
  claim_level: architectural
  depends_on: [SD-011, SD-019, ARC-016]
  functional_restatement: >
    The current AffectiveHarmEncoder trains z_harm_a via an auxiliary head predicting raw
    accumulated harm scalar. This makes z_harm_a an integrator of absolute state, not a
    surprise detector. Chen (2023, Front Neural Circuits) establishes that the anterior
    insula cortex (AIC) -- the biological analog of z_harm_a -- responds to "unsigned
    intensity prediction errors as a modality-unspecific aversive surprise signal," not to
    raw stimulus magnitude. SD-020 requires replacing the aux loss target: instead of
    MSE(harm_accum_pred, accumulated_harm), train on MSE(harm_surprise_pred, harm_PE)
    where harm_PE = actual_accum - predicted_accum. This makes z_harm_a encode how
    SURPRISING the current threat level is, not how high it is. E3 urgency then responds
    to unexpected threat escalation (which requires immediate behavioral change) rather
    than sustained-but-expected threat (which the agent may already be handling).
  evidence_quality_note: >
    HOLD AT CANDIDATE 2026-04-11 governance (first decision). EXQ-324 FAIL: 1/5 seeds
    pass but two compounding confounds prevent clean signal: (1) n_steps=10-20 harm
    events per seed -- too few for stable correlation estimates; sign-flipping across
    seeds is noise. (2) harm_obs_a_variance=0.0003-0.0007 on EMA substrate -- z_harm_a
    has insufficient variance to encode PE. Reclassified to mixed (not does_not_support).
    Metric trajectory (substrate_queue.json SD-020 entry) predicts: re-run on SD-022
    substrate (limb_damage_enabled=True) with >=100 harm events per seed should produce
    3+/5 seeds passing with positive surprise_corr_pe - surprise_corr_ema. Literature
    support strong (Chen 2023, Seymour 2019, Hoskin 2023 -- 4 lit entries). Hold until
    SD-022 substrate validation and SD-019 nonredundancy confirmed on same substrate.
    EXQ-324a (2026-04-16, inconclusive): 2/5 seeds pass with positive signal
    (mean_surprise_corr_pe=0.12), but n_eval_steps=6-10 per seed too small for
    stable correlation. Direction is positive. Reclassified from does_not_support
    to inconclusive. Hold at candidate until n_eval_steps >= 100 on SD-022 substrate.
    EXQ-324b PASS x2 (2026-04-18, supports): Two independent runs with EVAL_STEPS=400 and env.reset()-on-done fix (previous EXQ-324a exited too early for stable correlations). Same 3/5 seeds (43, 44, 45) pass in both runs with surprise_corr_pe in [0.44, 0.81]; C3 z_harm_a_norm > 0.01 in 5/5 seeds. Substrate: SD-022 (limb_damage_enabled=True, 8 hazards). Promoted to provisional on governance 2026-04-19.
    Promoted to stable on governance 2026-04-22: EXQ-324b cross-run reproducibility plus EXQ-321b MECH-090 bistable PASS confirm SD-020 is functioning as a load-bearing primitive of the affective harm stream. v3_pending lifted; no outstanding substrate gap.

    [2026-08-09 governance, GFLAG-0005 adjudication, DEMOTED stable -> candidate]: the 2026-04-22
    promotion is WITHDRAWN. Source-code audit of ree-v3/experiments/v3_exq_324b_sd020_harm_surprise_pe.py
    (the promoting run) confirms ZERO references to REEAgent, harm_surprise_pe_enabled, or
    compute_harm_accum_loss -- despite this claim's own implementation_note describing those as
    exactly the code path SD-020 implements. V3-EXQ-324b validated a STANDALONE-ENCODER BENCH
    REIMPLEMENTATION (an inline PE-vs-EMA comparison against a fresh linear dmg_head), never the
    shipped flag path -- the "Substrate: SD-022" / "cross-run reproducibility" language above
    describes a parallel bench harness, not agent.compute_harm_accum_loss() under
    harm_surprise_pe_enabled=True. The only run that DID drive the real flag path is V3-EXQ-324
    itself (3 REEAgent refs, 4 flag refs, confirmed) -- and it FAILED at 1/5 seeds (mixed
    direction, the note's own original entry above), the promotion's own predecessor result.
    live_status.evidence re-pointed to V3-EXQ-324 (the run that actually tested the shipped
    implementation) rather than 324b. Status floored to candidate, NOT falsified -- the shipped
    implementation has never been cleanly tested (1/5 seeds is underpowered/confounded per the
    original 2026-04-11 note, not a clean FAIL), and the architectural mechanism itself may still
    be sound; what is withdrawn is the CLAIM that it has been validated. A genuine test of the
    real flag path (harm_surprise_pe_enabled=True driving agent.compute_harm_accum_loss(), not a
    standalone bench script) is owed before any re-promotion. This claim's own
    default_off_scope_note (above) already flagged the flag-on-vs-default scoping question and
    cross-referenced SD-087; SD-087's own convergence on a calibration_pathology_representational
    finding through V3-EXQ-856/857a is a separate, later investigation and is unaffected by this
    demotion (see SD-087's own note, which already treats SD-020's stable-vs-flag-on scoping
    question as separately open).

    [2026-08-10 governance, V3-EXQ-324d, confirmed failure_autopsy_V3-EXQ-324d_2026-08-10]: the
    genuine real-flag-path test GFLAG-0005 asked for (and the 2026-08-09 demotion note above named
    as owed before any re-promotion) has now run. All non-degeneracy gates clean (344+ harm
    events/seed, manipulation confirmed real, DV not saturation-masked -- this is the well-powered
    test EXQ-324/324a/324b never were). 1/5 seeds pass all three criteria; the other 4 show
    near-zero-or-negative surprise correlation (-0.07 to -0.13) against one strong positive outlier
    (seed 3, +0.61). Reclassified non_contributory rather than does_not_support: the seed-variance
    shape closely echoes V3-EXQ-903a (MECH-075 ventral, a different claim) under otherwise clean,
    well-powered conditions -- flagged as a possible cross-cutting training-instability pattern in
    newly-trained PE/valuation heads, worth a shared diagnostic before either result is trusted as
    informative. Excluded from conflict_ratio as a non-contributory result. Promoted candidate ->
    provisional the same cycle (exp_conf=0.857 after this reclassification, conflict_ratio improves
    with the mixed/weakens entry removed from the denominator; strong literature support unchanged,
    5 entries, lit_conf=0.915) -- the shipped real-flag-path implementation has now had its first
    genuine, well-powered, non-degenerate test since the 2026-08-09 demotion, and it is (mostly)
    consistent with the claim rather than against it.
  notes: >
    Addresses Q-036 directly. If SD-020 is confirmed experimentally, Q-036 answer is:
    "surprise (PE) is necessary in addition to temporal integration." The implementation
    requires a simple harm-accumulation predictor (e.g., linear model of harm_accum from
    previous steps) to generate the PE target. This is a low-complexity change to the
    training loop, not the encoder architecture. The encoder stays the same; only the
    aux loss target changes.
  registered: "2026-04-08"
  location: docs/architecture/sd_011_dual_nociceptive_streams.md
  literature_confidence: 0.915
  literature_evidence: |
    4 entries in evidence/literature/targeted_review_connectome_sd_020/.
    Hoskin et al. (2023, J Neurosci): anterior insula encodes adaptive PE, not raw magnitude.
    Geuter et al. (2017, eLife): anterior/posterior insula PE dissociation.
    Horing et al. (2022, J Neurosci): unsigned PE surprise signal in insula.
    Iannetti & Mouraux (2010, J Neurophysiol): pain matrix as salience detector, not nociceptive.
  implementation_note: >
    IMPLEMENTED 2026-04-10. agent.compute_harm_accum_loss() in ree_core/agent.py now has
    conditional PE path: when harm_surprise_pe_enabled=True, maintains self._harm_obs_ema
    (updated each step at rate harm_obs_ema_alpha=0.1); training target becomes
    harm_PE * precision_norm where harm_PE = |target - ema| and precision_norm =
    min(e3.current_precision/500, 3.0). Config: REEConfig.harm_surprise_pe_enabled (False),
    harm_obs_ema_alpha (0.1). ARC-016 precision wired via e3.current_precision property.
    Backward compatible. Validation: V3-EXQ-324 queued.

- id: SD-021
  title: "Descending pain modulation: when E3 is committed to a trajectory through expected harm, z_harm_s PE precision is reduced (endogenous analgesia). Commitment gates sensory harm attenuation."
  claim_type: design_decision
  subject: harm_stream.descending_modulation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:SD-021@2026-04-11T16:55:54.646444Z"
      as_of: 2026-04-11
      verdict: hold_candidate_resolve_conflict/applied
  implementation_phase: v3
  claim_level: architectural
  depends_on: [SD-011, SD-020, ARC-016, MECH-090]
  functional_restatement: >
    The harm stream is currently purely feed-forward: bottom-up encoding from observations
    to latent, consumed by E3. There is no descending pathway. Chen (2023) emphasizes that
    the pain system includes a descending inhibitory pathway (pgACC -> PAG -> RVM) that
    provides top-down suppression of nociceptive input during committed action. In REE
    terms: when E3 commits to a trajectory that passes through a hazard (accepted expected
    harm), the sensory harm stream z_harm_s should be precision-downweighted for the
    PREDICTED component of harm. The forward model E2_harm_s already predicts z_harm_s_next
    -- the precision of the residual (actual - predicted) should be reduced during
    commitment. Implementation: when E3.committed_trajectory is not None and beta_gate is
    elevated (MECH-090), multiply z_harm_s PE by a commitment-gated attenuation factor
    (0 < alpha_descending < 1). z_harm_a is NOT attenuated (affective load persists
    regardless of commitment -- you can tolerate expected pain but it still matters).
  evidence_quality_note: >
    HOLD AT CANDIDATE 2026-04-11 governance (first decision). All experimental runs
    non_contributory due to commitment substrate gap: n_committed_steps=0 across all seeds
    in EXQ-325 and EXQ-321 (beta_gate elevation never fires). DESCENDING=CONTROL identically
    in all runs -- SD-021 attenuation path is wired but never reached. Requires the
    beta_gate elevation substrate to work before SD-021 can be tested. Literature
    support now 8 lit entries (lit_conf=0.852, updated 2026-04-17): Chen 2023 (AIC PE
    signal), Crawford 2021 (PAG brainstem 7T), Tracey & Mantyh 2007, Basbaum & Fields
    1984, Petrovic 2002 (rACC-brainstem co-activation), Hofbauer 2001 (S1/ACC double
    dissociation -- sensory hypnosis modulates S1 not ACC, directly grounds z_harm_s
    selective attenuation), Hohmann 2005 (PAG endocannabinoid SIA, opioid-independent),
    Wager 2004 (prefrontal anticipation predicts analgesic magnitude).
    Hold until commitment substrate produces n_committed_steps > 0 consistently.
    Implementation constraint (Hohmann 2005): the descending attenuation cannot be
    implemented as a single opioid-release pathway. PAG endocannabinoid synthesis is
    context-triggered and opioid-independent; the SD-021 gate should be modelled as a
    graded context-sensitive suppression, not a binary mu-opioid switch.
    Design tension (Wager 2004): placebo analgesia -- the closest human analog to
    commitment-gated harm acceptance -- shows ACC reduction during pain experience,
    suggesting both z_harm_s and z_harm_a may be suppressed. SD-021 requires z_harm_a
    to persist. Resolution: expectancy-based placebo (belief revision) and commitment-gated
    analgesia (trajectory resolution) may recruit overlapping but non-identical pgACC
    subregions. Hofbauer 2001 double dissociation shows the architecture for selective
    S1-only suppression exists; whether commitment specifically recruits it (vs also
    suppressing ACC) is an open empirical question that EXQ-325b should probe.
    EXQ-325a (2026-04-17, FAIL) reclassified non_contributory by governance
    2026-04-18: DESCENDING and CONTROL conditions produced bit-identical values per
    seed (z_harm_s_mean_committed and z_harm_a_mean_committed match to 10 decimal
    places across conditions) despite n_committed_steps=6000 in both (commit substrate
    now fires). Condition-dispatch bug: harm_descending_mod_enabled flag is not being
    routed differentially between conditions. Flagged for /diagnose-errors. Hold at
    candidate pending dispatch fix and re-run (EXQ-325b).
    EXQ-325a FAIL (2026-04-18, weakens): z_harm_s_ratio=1.0 identically in DESCENDING and CONTROL across 5 seeds, so attenuation not measurable despite running-variance fix and beta-gate bistable enabled. Descending pathway not engaging in committed state. Mechanism either requires substrate wiring not yet present, or the ratio metric is the wrong instrumentation for this claim.
    EXQ-325a reclassified non_contributory (2026-04-19, governance): after SD-032 cingulate integration substrate cluster registration, the bit-identical DESCENDING=CONTROL signature is recognised as the same failure mode as EXQ-395 MECH-220 harm-hub (descending output has no downstream consumer). The missing substrate is SD-032c (AIC-analog interoceptive salience / urgency-interrupt), the proper consumer of descending-modulation state. Manifest evidence_direction updated to non_contributory.
    SD-032c IMPLEMENTED 2026-04-19 (ree_core/cingulate/aic_analog.py). The raw-beta-gate SD-021 path is superseded by the AIC-analog harm_s_gain function: harm_s_gain depends structurally on operating_mode AND drive_level, not just beta_gate, so DESCENDING and CONTROL conditions are no longer bit-identical. V3-EXQ-325b queued (supersedes V3-EXQ-325a) -- retest with drive-regime contrast (depleted vs well-resourced) expected to resolve the EXQ-325a FAIL. pending_retest_after_substrate flag cleared; hold at candidate until V3-EXQ-325b PASSes.
    EXQ-325b FAIL (governance 2026-04-22) reclassified non_contributory: descending-modulation contrast still cannot resolve because the agent is V_s-monostrategy-locked across drive regimes (no behavioural divergence between depleted and well-resourced conditions). pending_retest_after_substrate (MECH-269 V_s landing). EXQ-325d (SD-032c AIC descending modulation, 2026-04-20) similarly non_contributory under same monostrategy-blocked substrate.

    [2026-08-08 governance, V3-EXQ-878/878a, confirmed failure_autopsy_V3-EXQ-878a_2026-08-08,
    co-tagged on MECH-332]: two more independent non-contributory results on the same underlying
    commitment-gate precondition -- E3's commitment gate never elevates during eval
    (n_committed_steps=0) even at up to 3x budget across an 18-cell sweep; running_variance_mean_fresh
    pinned at precision_init (0.5), never trending toward commit_threshold. This is a DIFFERENT
    precondition than the V_s-monostrategy lock this claim's what_would_answer already documents
    (that blocks the DESCENDING-vs-CONTROL contrast once commitment fires; 878/878a block
    commitment from firing at all), so it is a second, independently-blocking gap on the same
    claim, not a re-test of the documented one. non_contributory/substrate_conditional.
    routing=governance-note-only. FLAG (not acted on this cycle): 878/878a are the 3rd/4th
    independent experiment across ~4 months hitting this identical unrecovered commitment-gate
    blocker (with EXQ-325/EXQ-321 on the sibling SD-021/MECH-332 lineage) without formal
    substrate_ceiling tagging -- see MECH-332's own note for the fuller finding. If a 5th
    same-mechanism re-queue is proposed, route through /failure-autopsy for a ceiling read first.

    [2026-08-22 correction, chip-20260822-exq325-family-evidence-disposition]: the successive
    substrate theories above (SD-032c missing consumer, then V_s-monostrategy lock via MECH-269)
    were built to explain a symptom -- DESCENDING and CONTROL bit-identical on z_harm_s_ratio /
    z_harm_a_ratio -- that has a simpler, sufficient, and directly-confirmed cause: harm_descending_mod_enabled
    and descending_attenuation_factor are REEConfig fields, NOT declared parameters of
    REEConfig.from_dims(), so every v3_exq_325-family driver that passed them as from_dims()
    kwargs (325, 325a all four runs, 325d) had them silently swallowed by its **kwargs catch-all.
    DESCENDING was therefore never differentially armed relative to CONTROL in ANY of the six
    manifests scored against this claim. The 2026-04-18 condition-dispatch-bug diagnosis (line
    above) was correct and was never followed through to a fix. Two corrections follow from
    reading ree_core/agent.py's descending-modulation consumer (~line 4811) directly: (1) the
    "SD-032c is the missing downstream consumer" theory does not hold -- the legacy (non-AIC)
    branch requires only harm_descending_mod_enabled=True and beta_gate.is_elevated, no AIC
    substrate at all, so SD-032c's implementation status was never the blocker; (2) C1/C2 (the
    z_harm_s_ratio / z_harm_a_ratio criteria) are direct latent-attenuation measurements taken
    during already-observed committed steps (n_committed_steps=6000 was achieved in every
    from-dims-affected run), not behavioural-diversity measurements, so the V_s-monostrategy
    lock-in this claim's what_would_answer documents (action_class_entropy=0) does not gate
    C1/C2 either, whatever bearing it may have on C3/benefit_ratio or on SD-032c's own
    drive-regime behavioural-divergence test. Monostrategy lock-in remains real and
    independently evidenced (MECH-269's own V3-EXQ-478/480/481 diagnostics, unrelated to this
    family) -- this correction narrows what it blocks, it does not dismiss it. Repair of the
    from_dims call sites is in progress (uncommitted as of this note) under
    chip-20260822-fromdims-exq325-dead-ablation-axis; a from_dims-vs-direct-attribute parity
    contract is being added alongside it. A separate residual gap found during this review:
    v3_exq_325a and v3_exq_325d also pass heartbeat=HeartbeatConfig(beta_gate_bistable=True) as
    a from_dims() kwarg, which is likewise not a declared parameter and is silently dropped --
    beta_gate_bistable was False throughout every run in this family, contrary to the drivers'
    own design intent, and this is NOT fixed by the in-progress repair as of this note. Net
    effect on this claim: status unchanged (candidate, no v3_pending flag to lift here); the six
    manifests' evidence_direction is corrected (non_contributory across the board -- see each
    manifest's own evidence_direction_note); once the from_dims plumbing repair (including the
    heartbeat/beta_gate_bistable gap) is committed, C1/C2/C3 are testable on the EXISTING
    substrate with no additional gate, and a re-run under a new EXQ letter should be routed
    through /queue-experiment rather than re-using an EXQ-325* id. See also SD-032c's
    evidence_quality_note for the parallel correction on that claim.
  notes: >
    This creates a commitment-dependent analgesic mechanism: an agent crossing a hazard
    to reach a goal suppresses sensory alarm for the expected harm component while
    maintaining affective urgency (z_harm_a still elevated). Disruption of this mechanism
    maps to hyperalgesia / anxiety disorders (inability to attenuate expected sensory harm
    during committed action). Connects to MECH-090 (beta gating) and MECH-094 (hypothesis
    tag). During beta-elevated commitment, the agent is executing, not planning -- sensory
    PE precision should be reduced.
    SD-014 cross-connection: committed harm-accepting trajectories partially suppress
    z_harm_s PE during execution. When commitment ends, harm nodes traversed will have
    stored h values (SD-014 valence vector) that underestimate experienced harm (z_harm_s
    was attenuated). The mismatch between stored h and post-commitment actual harm signal
    constitutes a surprise (s component) spike at those nodes, driving preferential
    post-execution replay. Commitment-gated analgesia (SD-021) thus creates the conditions
    for surprise-weighted consolidation of harm experience after the trajectory completes
    (SD-014). Consistent with Carey et al. (2019): replay prioritises nodes with stale
    valence estimates. Registered as design note 2026-04-17.
  registered: "2026-04-08"
  location: docs/architecture/sd_011_dual_nociceptive_streams.md
  literature_confidence: 0.852
  literature_evidence: |
    8 entries across targeted_review_connectome_sd_021 (4) and targeted_review_sd_021 (4).
    Crawford et al. (2021, PNAS): brainstem mechanisms of placebo analgesia (7T fMRI; PAG, LC).
    Tracey & Mantyh (2007, Neuron): cerebral signature of pain and its modulation (review).
    Basbaum & Fields (1984, Annu Rev Neurosci): descending pain modulation (PAG-RVM pathway).
    Eippert et al. (2009, Science): opioidergic brainstem placebo analgesia (direct fMRI).
    Petrovic et al. (2002, Science): rACC-brainstem covariation during opioid and placebo
    analgesia -- cortical initiation of descending modulation confirmed.
    Hofbauer et al. (2001, J Neurophysiol): double dissociation -- intensity hypnosis modulates
    S1 not ACC; unpleasantness hypnosis modulates ACC not S1. Directly grounds z_harm_s vs
    z_harm_a selective attenuation in SD-021.
    Hohmann et al. (2005, Nature): CB1 blockade in PAG blocks non-opioid SIA -- PAG as
    multi-mechanism context-sensitive relay.
    Wager et al. (2004, Science): prefrontal anticipatory activity predicts placebo analgesic
    magnitude; thalamus/insula/ACC down during pain -- anticipation-phase top-down gating.
  implementation_note: >
    IMPLEMENTED 2026-04-10 (legacy raw-beta-gate path). REEAgent.sense() in
    ree_core/agent.py. After LatentStack.encode(), if
    harm_descending_mod_enabled=True and beta_gate.is_elevated and z_harm is not
    None: new_latent.z_harm *= descending_attenuation_factor. z_harm_a NOT
    modified. Config: REEConfig.harm_descending_mod_enabled (False),
    descending_attenuation_factor (0.5). Design doc:
    docs/architecture/sd_021_descending_pain_modulation.md.
    SUPERSEDED 2026-04-19 by SD-032c (AIC-analog). The raw-beta-gate check is
    retained behind harm_descending_mod_enabled for backward compatibility
    (when use_aic_analog=False). When use_aic_analog=True, the attenuation
    multiplier is aic.harm_s_gain, a function of operating_mode AND drive_level
    (not just beta_gate). This resolves the EXQ-325a FAIL
    (DESCENDING==CONTROL bit-identical) by making the descending branch a
    genuinely different function of state. Retest: V3-EXQ-325b ran 2026-04-18
    to 2026-04-19 (EWIN-PC requeue, E2 world-forward training addon), result
    FAIL -- s_ratio_D=1.0000 s_ratio_C=1.0000 bit-identical across all 3
    seeds, reclassified non_contributory 2026-04-22 (see evidence_quality_note).
    A further targeted attempt, V3-EXQ-325f (2026-05-05, reef/monostrategy
    fix), also FAILED bit-identically. Both routes are exhausted; see
    what_would_answer for the current blocking picture.
  epistemic_category: substrate_conditional
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: MECH-269 V_s landing (alternative operating
    modes producing measurable behavioural divergence between drive regimes)
    must occur before DESCENDING vs CONTROL can differ at all -- otherwise any
    result is uninformative. Demonstrated 3x on this exact claim (V3-EXQ-325a
    commitment-substrate-absent FAIL, 325b bit-identical DESCENDING==CONTROL
    FAIL, 325d/325f non_contributory under the same monostrategy lock). The
    adjacent MECH-295 goal_pipeline:GAP-4 Tier-1 retest cohort (V3-EXQ-490g/h/
    i/j/k) ran this same precondition to its own conclusion in a parallel
    pathway: GAP-4 CLOSED 2026-06-09 without landing V_s-driven behavioural
    divergence. As of 2026-08-07, MECH-269's per_region_vs remains
    instantaneous-only (no temporal-integration form), and a 2026-06-19 note
    on the sibling SD-032b claim records MECH-269 as "wired_but_inert...
    superseded as the operative diversity blocker by the MECH-439 F-dominance
    conversion ceiling (~88-89% of E3 variance)." This precondition is
    therefore not merely pending -- it has been actively tested via at least
    two independent routes (direct reef/monostrategy fix, and the GAP-4
    cascade route) and neither broke the degeneracy. A fresh substrate
    approach to MECH-269, or resolution of the MECH-439 ceiling, is required
    before this precondition can be considered live again.
    CONFIRMING: once MECH-269 V_s landing produces genuine behavioural
    divergence between drive regimes (independently verifiable, e.g. via a
    non-SD-021 diagnostic showing action-class or trajectory divergence
    across depleted/well-resourced conditions), a DESCENDING-arm run shows
    z_harm_s PE precision measurably reduced specifically during committed
    trajectories through expected harm (elevated beta_gate via MECH-090),
    while z_harm_a PE precision remains unattenuated -- a clean selective
    dissociation matching the S1/ACC double-dissociation logic of Hofbauer
    2001, already cited in this claim's literature base.
    FALSIFYING: after MECH-269 V_s landing is confirmed live (behavioural
    divergence demonstrably present elsewhere in the same run, ruling out the
    "nothing can differ yet" explanation), DESCENDING and CONTROL remain
    statistically indistinguishable on z_harm_s_ratio -- OR z_harm_a is ALSO
    precision-attenuated in the DESCENDING arm, violating the claimed
    selective (harm_s-only) attenuation design.

- id: SD-022
  title: "Directional limb damage: body extension for genuine z_harm_s / z_harm_a causal independence"
  claim_type: design_decision
  subject: body.directional_limb_damage
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_323a_sd019_harm_nonredundancy_20260416T172811Z_v3
      as_of: 2026-04-16
      verdict: supports/PASS
  implementation_phase: v3
  claim_level: architectural
  depends_on: [SD-011]
  functional_restatement: >
    harm_obs_a is currently an EMA of hazard/resource proximity fields -- structurally
    identical to harm_obs with a slower time constant. EXQ-241b confirmed r2_s_to_a=0.996
    across all seeds regardless of harm_history extension: the streams cannot separate
    because both derive from the same world signal. The A-delta/C-fiber distinction
    requires harm_obs_a to derive from a BODY STATE causally independent of current
    world proximity. SD-022 instantiates this via four directional limbs (N/E/S/W) each
    accumulating damage when used through hazards. Damage state (tissue injury) persists
    slowly-healing across steps even when the agent leaves the hazard zone, giving
    harm_obs_a a body-derived source. An agent in a safe location with accumulated limb
    damage should have high z_harm_a and near-zero z_harm_s -- a dissociation
    CausalGridWorldV2 cannot currently produce. The EMA-based harm_obs_a is superseded
    by the damage-state source when limb_damage_enabled=True.
  implementation_note: >
    IMPLEMENTED 2026-04-09. CausalGridWorldV2 (ree_core/environment/causal_grid_world.py):
    4-directional limb_damage[4] state; accumulates when moving through hazards;
    heals at heal_rate=0.002/step; movement failure P(fail) = damage[d] * failure_prob_scale.
    harm_obs_a re-sourced from body damage state (7 dims: damage[4]+max+mean+residual_pain)
    when limb_damage_enabled=True, replacing 50-dim proximity EMA.
    body_state extended 12->17 dims (+ damage[4] + residual_pain).
    REEConfig.from_dims() params: limb_damage_enabled (False default),
    damage_increment (0.15), failure_prob_scale (0.3), heal_rate (0.002).
    Backward compatible: disabled by default; existing experiments unaffected.
    Validation experiment: V3-EXQ-318 queued.
  evidence_quality_note: >
    PROMOTED TO PROVISIONAL 2026-04-11 governance. Two convergent results: (1) EXQ-318
    FAIL-on-variance-threshold but r2_s_to_a dropped from 0.702 (DISABLED) to 0.365
    (ENABLED) -- mechanism works, threshold failure is calibration (heal_rate=0.002 too
    fast for 150 warmup episodes). (2) EXQ-319 PASS 3/3 seeds -- dissociation confirmed:
    z_harm_a_retention_high=0.910 vs z_harm_a_retention_fresh=0.486, z_harm_s drops
    sharply when agent leaves hazard while z_harm_a persists. Metric trajectory analysis
    (governance-2026-04-11): substrate produces the predicted direction of change;
    EXQ-318 failure is a calibration issue not a mechanism failure. Unblocks SD-019
    and SD-020 validation (both require SD-022 substrate for stable training-objective signal).
    Prediction: r2_s_to_a < 0.4 with heal_rate=0.001 or increased hazard density.
    EXQ-323a (2026-04-16, PASS): SD-022 body-damage substrate validated as co-test in
    SD-019/SD-011 nonredundancy experiment. z_harm_a functionally distinct from z_harm_s
    across all 5 seeds. Substrate is production-ready for SD-019/SD-020 experiments.
  notes: >
    Failure mode mapped to psychiatric conditions: persistent z_harm_a with no current
    world threat = hypervigilance, chronic pain, PTSD (body-state residue persists after
    the threat is gone). Without SD-022, these failure modes cannot be modelled or tested.
    Unblocks: SD-011 retests (EXQ-241c), ARC-030 Go/NoGo symmetry, Q-034 threshold,
    ARC-052 precision outputs. Recommend implementing before SD-023; independent of it.
  registered: "2026-04-09"

- id: SD-023
  title: "Environmental gradient texture: multi-channel world observation with neutral landmark objects"
  claim_type: design_decision
  subject: environment.gradient_texture
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:SD-023@2026-05-03T02:50:00Z"
      as_of: 2026-05-03
      verdict: hold_candidate_resolve_conflict/applied
  implementation_phase: v3
  claim_level: architectural
  depends_on: [SD-014]
  functional_restatement: >
    z_world can only encode features that exist in world_obs. CausalGridWorldV2 currently
    emits two proximity fields (hazard, resource), making z_world approximately a
    two-scalar encoding regardless of encoder expressiveness. This creates a ceiling on
    all world-model claims: E1 cannot build genuine temporal predictions when the world
    has almost no texture; MECH-216 (predictive wanting) cannot fire predictively
    because the only world feature correlated with resources is resource proximity itself
    (no leading indicator); ARC-017 (stream tags) cannot demonstrate typed separation
    with only two undifferentiated channels. SD-023 extends world_obs with two new
    object types that emit characteristic gradient fields but have no harm/benefit.
    Landmark B is placed with statistical bias near resources, providing a predictive
    cue that E1's LSTM can learn to associate with upcoming resource contact. This is
    the minimum world richness required for MECH-216 to produce genuine anticipatory
    wanting rather than reactive proximity detection. world_obs_dim: 250 -> 300.
  evidence_quality_note: >
    1:1 experimental conflict -- adjudication pending. EXQ-263b (2026-04-10, supports,
    conf=0.55): salience elevated near Landmark B positions (C1 pass); landmark_prox_corr
    > 0 but < resource_prox_corr (C2 fail). SD-023 tagged supports; MECH-216 does_not_support.
    EXQ-332 (2026-04-14, does_not_support, conf=0.75): future_target_corr~=0.005 in BOTH
    conditions. Root cause identified 2026-04-17: implementation gap in EXQ-332 training
    curriculum. future_target_mean=0.0006 across 162k P1 steps (~10 resource contacts
    total); MSE loss dominated by zero-class; schema_readout_head learns constant-zero
    output. The ~0 correlation is a supervision-density artifact, NOT evidence against the
    design principle. Lit-pull 2026-04-17 (4 entries, lit_conf=0.876, all supports):
    O'Keefe & Burgess 1996 (place fields anchored to neutral geometry), Wang 2020 (surface-
    texture boundaries structure place cells -- most direct support), Schultz 1997 (DA
    prediction transfer to distal cues), Yaghoubi 2026 (hippocampal predictive temporal
    coding shifts to reward-predicting cues). Literature strongly grounds the design
    principle. Adjudication: V3-EXQ-332a queued (8x8 grid + 4 resources + FUTURE_WINDOW=30
    to fix sparse-label problem; C3 quality gate guards against recurrence). Governance
    decision (retain_ree/hybridize/retire) pending EXQ-332a result, deadline 2026-04-20.
    If C3 fails again in 332a, result is non_contributory and a redesigned measurement
    approach is needed before the decision can be made.
    EXQ-332a (2026-04-18, FAIL, non_contributory per governance 2026-04-18):
    C3 positive_label_rate=0.00128 (threshold 0.10) -- sparse-label problem
    recurred despite the 332a redesign. Governance decision: HOLD at candidate.
    Adjudication blocked until measurement redesign delivers sufficient label
    density; defer retain_ree/hybridize/retire call past 2026-04-20 deadline.
    Next substrate iteration must guarantee >=10% positive-label density across
    training (e.g. reward-dense curriculum, longer future window, or denser aux
    supervision).
    V3-EXQ-418k (2026-05-06, does_not_support -- see SD-016 status_note for full
    detail) separately tested SD-023-landmarks as one arm (A3_reef_sd016) of a
    4-arm z_world-richness ladder: landmark enrichment did not move separation_gap
    off its ~0.0001-0.004 floor (cos_cross_mean 0.9998). Distinct from this claim's
    own supervision-density conflict above -- 418k measured raw z_world
    distinguishability, not the downstream resource-prediction correlation EXQ-332/332a
    targeted -- but relevant background for any future SD-023 substrate iteration:
    landmark placement alone, even combined with SD-016 cue retrieval, does not by
    itself break the near-constant z_world regime.
  notes: >
    The principle "all objects emit, each type distinctively" is biologically grounded:
    in natural environments every object has a detectable presence (olfactory, acoustic,
    thermal, visual texture). Landmark A provides navigation anchors (short-range strong
    gradient). Landmark B provides predictive resource cues (medium-range weak gradient,
    biased near resources). Both channels feed z_world, enriching the world model.
    Does NOT fix z_harm_s/z_harm_a independence (that is SD-022). Both SDs needed.
    The landmark_B predictive structure directly enables MECH-216 retest (EXQ-263b).
    Further extension: ambient "object presence" gradient for every object type
    (full "all objects emit" implementation) is deferred -- SD-023 is the minimum needed.
  registered: "2026-04-09"
  implementation_note: >
    IMPLEMENTED 2026-04-09 in CausalGridWorldV2 (ree_core/environment/causal_grid_world.py).
    New params: n_landmarks_a (int, default 0), n_landmarks_b (int, default 0),
    landmark_a_sigma=1.5, landmark_a_scale=1.0, landmark_b_sigma=2.5, landmark_b_scale=0.6,
    landmark_b_resource_bias=0.7. world_obs_dim: 250 -> 300 when landmarks enabled.
    obs_dict keys: landmark_a_field_view [25], landmark_b_field_view [25].
    Backward compatible: all defaults 0/disabled. Validation: V3-EXQ-263b queued.

- id: SD-024
  title: "DA-modulated RBF center density: dopaminergic signal at reward encounters allocates multiple closely-spaced RBF centers in the hippocampal residue field, creating representational expansion at reward locations without requiring environmental richness."
  claim_type: design_decision
  subject: hippocampal_module.da_modulated_rbf_density
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  registered: "2026-04-14"
  depends_on:
    - SD-004
    - SD-014
    - MECH-232
  location: docs/architecture/sd_024_da_modulated_rbf_density.md
  notes: >
    Workaround for ARC-057 substrate constraint: instead of requiring an informationally
    rich environment, the hippocampal RBF layer itself creates more internal structure at
    DA-modulated locations. When a reward encounter triggers a phasic DA signal (scaled
    by benefit_magnitude * drive_level per SD-012), multiple RBF centers are allocated in
    a local z_world neighborhood with jittered positions. This creates a cluster of
    closely-spaced centers -- higher representational density -- at reward locations.
    Harm events continue to allocate single centers (no DA modulation), preserving the
    MECH-233 asymmetry. The FIFO center lifecycle provides natural decay: when the agent
    stops encountering reward, the expanded cluster gradually gets overwritten.
    Key parameters: da_allocation_scale (extra centers per DA), da_jitter_radius (cluster
    spread), num_centers (increased from 32 to 64+ to accommodate expansion).
    MECH-094 gate required: replay/simulation events must NOT trigger DA expansion.
    Informative failure modes: craving (expansion persists after reward removal),
    anhedonia (DA=0, uniform map, no directional preference), perseveration (DA locked
    high, all locations expanded, no reorientation).
    Biologically grounded: Retailleau & Morris 2018 showed place cells reorient to reward
    features via D1 receptors; rats in uniform corridors still get sharper fields near
    reward end. The brain allocates more resources regardless of environmental justification.
  implementation_note: >
    IMPLEMENTED 2026-07-16 in ree-v3 (residue/field.py RBFLayer + ResidueField,
    hippocampal/module.py). Config (ResidueConfig): use_da_modulated_rbf_density (master,
    default False -> bit-identical OFF), da_allocation_scale (0.0), da_jitter_radius (0.1),
    da_bandwidth_narrowing (0.0), da_benefit_num_centers (None). accumulate_benefit(...,
    dopamine_signal=) routes reward events to add_residue_cluster (n = 1 + int(da*scale)
    jittered centers + per-center bandwidth floored at 0.5*base) on the benefit terrain
    ONLY (harm/safety keep single-center standard-bandwidth -> MECH-233 asymmetry preserved).
    compute_local_density is WEIGHT-INDEPENDENT (the MECH-232 discriminator). MECH-094:
    hypothesis_tag gate inherited (replay cannot expand). No phased training (no encoder
    head). Full pytest tests/ 1475 passed; 13 contracts in
    tests/contracts/test_sd024_da_modulated_rbf_density.py. Built as the DIAGNOSTIC
    instrument resolving MECH-232 (validation V3-EXQ-766), NOT gated behind it -- the
    depends_on MECH-232 here is a circular/stale gate (MECH-232 is candidate precisely
    because SD-024 is what supplies its first experimental test); SD-004 is IMPLEMENTED.
    NOTE for governance: substrate_queue.json SD-024 depends_on_unresolved should drop
    SD-004 (implemented) and MECH-232 (the claim SD-024 tests). Unblocks SD-025
    (curiosity_drive) downstream. See docs/architecture/sd_024_da_modulated_rbf_density.md.
    LIVE-PATH PRODUCER WIRED 2026-07-20 -- the 2026-07-16 landing built the mechanism
    and its READ but no WRITER. ResidueField.accumulate_benefit, the sole write path
    into benefit_rbf_field, had NO CALLER anywhere in ree_core/ (its only write sites,
    field.py:673 and :682, are inside that method; it was invoked only from experiments/
    and tests/contracts/). Measured on a real warmup_train loop (darwin-arm64,
    curiosity_weight=0.5) with benefit_terrain_enabled AND use_da_modulated_rbf_density
    both True: active_mask.sum()==0, num_benefit_events==0.0; compute_local_density
    early-returns zeros on an empty mask (field.py:273) so compute_representational_density
    returned exactly 0.0; so HippocampalModule._curiosity_bonus returned 0.0 on all 14432
    live calls regardless of curiosity_weight, and the use_curiosity_familiarity
    True/False ablation was BIT-IDENTICAL. The 13 contracts could not catch this because
    every one calls accumulate_benefit itself before asserting -- a test that populates
    the terrain cannot detect that nothing else does. Fix: REEAgent.update_z_goal now
    calls accumulate_benefit(z_world, benefit_magnitude=benefit_exposure,
    dopamine_signal=benefit_exposure*drive_level), gated on new ResidueConfig flags
    benefit_terrain_live_producer (default False) and benefit_live_producer_threshold
    (0.1). Separate flag deliberately, NOT benefit_terrain_enabled: V3-EXQ-767/767a set
    that flag and self-populate via direct accumulate_benefit calls, so reuse would
    double-populate them on re-run. Also landed: HippocampalConfig.familiarity_bandwidth
    1.0 -> 0.20 (V3-EXQ-786a sweep peak; 1.0 measured at exactly +0.000, the degenerate
    clamped-sum case). 7 new contracts in
    ree-v3/tests/contracts/test_sd024_benefit_terrain_live_producer.py assert through the
    real REEAgent API against a real CausalGridWorldV2 episode loop and never call
    accumulate_benefit directly. Full suite 2065 passed. NO evidence direction changed and
    no manifest retro-edited; V3-EXQ-767/767a remain valid IN-VITRO validations of the
    drive mechanism -- they simply do not establish LIVE-PATH efficacy, and governance
    owns any re-reading of what they support. ree-v3 commits: substrate swept into
    8ac193d by a concurrent session, contracts 7f16f25ceb.

- id: SD-025
  title: "Curiosity drive: information-seeking bias in hippocampal trajectory scoring that favors regions of higher representational density in the RBF map."
  claim_type: design_decision
  subject: hippocampal_module.curiosity_drive
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-767a_2026-07-17
      as_of: 2026-07-17
      verdict: supports/?
  evidence_quality_note: >
    2026-07-16 governance (cycle 2026-07-16d; failure_autopsy_767-768-cluster): V3-EXQ-767
    (curiosity-drive CEM selection-bias diagnostic) PASSed self-routed 'supports' but was
    adjudicated VACUOUS -- the single load-bearing gate L1a_pref_A_on is pinned at exactly 1.0 on
    all 8 seeds (zero cross-seed variance), so the binary directional-preference readout saturates
    and carries no effect-size. The MECHANISM is real (OFF arm = 0.5 chance; the drive genuinely
    discriminates dense from sparse) -- epistemic_category measurement_degeneracy, NOT
    substrate_ceiling (the SD-024/SD-025 substrate works). evidence_direction inconclusive
    (PROMOTES/DEMOTES NOTHING). Re-operationalized as V3-EXQ-767a: replace the saturating binary
    pref-gate with the continuous CEM score-margin (already computed internally, discarded). SD-025
    stays candidate.

    2026-07-17 governance (failure_autopsy_V3-EXQ-767a): V3-EXQ-767a -- the continuous CEM
    score-margin re-operationalization of the vacuous V3-EXQ-767 -- adjudicated REAL / non-vacuous
    (supports). The re-op RESOLVED 767's measurement_degeneracy: the load-bearing statistic is now
    the continuous CEM score-margin min(sparse)-min(dense), variance-bearing (margin_on 39.26,
    varies 26.95-45.61 across 8 seeds) and clearing the criteria_non_degenerate guard. All four
    load-bearing criteria PASS -- propagation (margin_on 39.26 vs OFF null 0.0), weight-independent
    density read (L1c 0.0 <= 0.1: zeroing benefit value leaves the margin unchanged -> routes on
    density not value, ARC-007-strict), and waking-only familiarity-discount anti-perseveration
    (L2a 20.05; MECH-094 replay control L2b 0.0). Readiness met (density discrim 58.32; positive
    control margin 78.76; substrate_hash be9a4d7f, 8 seeds). ADVANCES SD-025 drive-mechanism
    validation. Scope: DRIVE MECHANISM only -- NOT the env-constrained ARC-057 approach-emergence
    claim (interaction is V3-EXQ-768a, which promoted ARC-057 candidate->provisional 2026-07-17).
    Supersedes 767's inconclusive note. SD-025 stays candidate.

    2026-07-20 SUBSTRATE (not governance -- no status or confidence change): the SD-024
    benefit-terrain PRODUCER was missing from the live agent loop until this date, so the
    SD-025 curiosity bonus was EXACTLY ZERO in every live agent run. accumulate_benefit
    had no caller in ree_core/, benefit_rbf_field stayed empty, compute_local_density
    early-returned zeros on the empty active mask, and _curiosity_bonus therefore computed
    novelty = density * (1 - familiarity) = 0 -- measured 0.0 on all 14432 live calls,
    regardless of curiosity_weight; the use_curiosity_familiarity ablation was
    BIT-IDENTICAL. This does NOT touch V3-EXQ-767/767a: those populate the terrain
    themselves via direct accumulate_benefit calls (767a lines 236, 238, 305), which is
    why they measured density gaps of 75-80 and PASSED. They remain valid IN-VITRO
    validations of the drive mechanism. What they do not establish -- and what nothing
    yet establishes -- is LIVE-PATH efficacy: whether the drive does anything when the
    terrain is built by the agent's own reward contacts rather than by the driver script.
    Governance owns any re-reading of what 767/767a support; this note only records the
    substrate fact. Also relevant to future reads: familiarity_bandwidth default moved
    1.0 -> 0.20, because the FamiliarityTracker clamped-sum readout saturates at 1.0 and
    would have become load-bearing the moment density stopped being identically zero.
    See SD-024 implementation_note and sd_024_da_modulated_rbf_density.md "Live-Path
    Producer (2026-07-20)".
  registered: "2026-04-14"
  depends_on:
    - SD-024
    - SD-004
    - ARC-057
    - MECH-111
    - INV-051
  location: docs/architecture/sd_024_da_modulated_rbf_density.md#curiosity-drive
  notes: >
    The second component of ARC-057: a curiosity/information-seeking drive that biases
    CEM trajectory scoring toward regions of higher representational density in the
    hippocampal RBF layer. The drive computes local density (active centers within
    bandwidth radius, weighted by proximity) and novelty (density * (1 - familiarity)).
    The curiosity bonus is added to the CEM terrain_score.
    Critical property: the curiosity drive is uniform -- it does the same computation
    everywhere. Approach behavior emerges because DA-expanded regions (SD-024) have
    higher density, so they score higher. The agent does not "know" it is approaching
    reward; it is simply exploring available representational structure.
    The familiarity component (visit-count EMA) prevents the agent from endlessly
    circling already-explored regions, while the density component ensures that
    DA-expanded regions remain attractive even after visits (because there are more
    centers to distinguish between).
    Parameters: curiosity_weight (CEM scoring bonus), familiarity_ema_alpha (decay rate).
    Both default to 0 for backward compatibility.
    Test: ARC-057 requires BOTH SD-024 and SD-025 active. Neither alone should produce
    significant approach behavior (interaction effect, not additive).
  implementation_note: >
    IMPLEMENTED 2026-07-16 in ree-v3 (hippocampal/curiosity.py FamiliarityTracker +
    hippocampal/module.py HippocampalModule._curiosity_bonus / update_familiarity;
    agent.py sense() waking-gated familiarity update). Config (HippocampalConfig):
    curiosity_weight (master, default 0.0 -> tracker never built, _score_trajectory
    untouched -> bit-identical OFF), familiarity_ema_alpha (0.01), use_curiosity_familiarity
    (True; False = density-only ablation), familiarity_bandwidth (1.0).
    _score_trajectory subtracts curiosity_weight * mean(density * (1 - familiarity));
    density = compute_representational_density (the SD-024 WEIGHT-INDEPENDENT active-center
    count -- drive follows representational quality, not a valence gradient); familiarity =
    a proximity-weighted visit-count EMA (soft visitation KDE, clamped [0,1]) raised on
    WAKING visits only (MECH-094; agent gates on hypothesis_tag, mirroring the MECH-314a
    visitation buffer). No phased training (read + EMA state only). 7 contracts in
    tests/contracts/test_sd025_curiosity_drive.py; full pytest tests/ 1488 passed.
    SCOPE: the SUBSTRATE is buildable now on SD-024, but the depends_on ARC-057/MECH-111/
    INV-051 are CLAIMS (not implementable substrate) whose full ARC-057 ecological
    approach-emergence test is ENV-CONSTRAINED -- the CausalGridWorld cannot faithfully
    test the SD-024 x SD-025 interaction (a cell is a cell; nothing more to discover at
    higher resolution; see ARC-057 SUBSTRATE CONSTRAINT). SD-004 + SD-024 (the only
    implementable-substrate deps) are IMPLEMENTED. Validation V3-EXQ-767 is scoped to the
    DRIVE MECHANISM (does curiosity PROPAGATE into CEM selection toward higher-density
    regions -- the propagation MECH-111's broken broadcast-novelty->E3 path could not
    achieve, EXQ-141b/590a -- + the familiarity anti-perseveration discount), NOT the
    interaction claim. SD-025 stays candidate; a PASS is drive-mechanism readiness, not
    ARC-057 evidence. NOTE for governance: substrate_queue.json SD-025 depends_on_unresolved
    still lists SD-024 (now implemented) -- drop it; the remaining ARC-057/MECH-111/INV-051
    are env/claim gates on the full test, not the substrate build.
    See docs/architecture/sd_024_da_modulated_rbf_density.md#curiosity-drive.

- id: MECH-220
  title: harm_stream_hub_coordination
  claim_type: mechanism_hypothesis
  subject: harm_stream.cross_stream_hub
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-220@2026-04-25T15:42:09.107828Z"
      as_of: 2026-04-25
      verdict: hold_candidate_resolve_conflict/applied
  implementation_phase: v3
  depends_on: [SD-011, SD-020, SD-021, ARC-016]
  evidence_quality_note: |
    EXQ-395 reclassified non_contributory (governance 2026-04-19): harm_rate_delta=0.0
    in all 3 seeds with identical harm_events per condition (HUB_ENABLED vs HUB_ABLATED).
    Symptom of missing SD-032b (dACC-analog cingulate integration substrate), not evidence
    against MECH-220. SD-032b now IMPLEMENTED 2026-04-19; V3-EXQ-445 queued as retest.
    Claim held pending that retest.

    EXQ-395 FAIL (2026-04-18, weakens): HUB_ENABLED (hub_weight=0.3) and HUB_ABLATED (hub_weight=0.0) produce identical harm_rates across 3 seeds (delta=0.0 every seed). z_harm_a_norm shows small directional effect (+0.09 to +0.14) but harm_rate behavioral metric is unchanged. Evidence the hub mechanism (as currently implemented) is inert on the causal path to behavior. Either the hub weight is too small, the routing is not reaching policy, or the coordination mechanism needs a different architectural instantiation.
  description: >
    A lightweight cross-stream coordination mechanism (harm hub) allows z_harm_s and
    z_harm_a to share context without collapsing into a single stream. The hub implements
    two directed interactions: (1) z_harm_a receives z_harm_s PE magnitude as context
    (sensory surprise informs affective load update rate); (2) z_harm_s precision is gated
    by z_harm_a urgency level (high affective load amplifies sensory sensitivity -- the
    hypervigilance pathway). This is NOT a fusion: the streams maintain separate encoders,
    separate training targets, and separate functional roles. The hub is a gating/attention
    mechanism that coordinates their interaction.
  notes: >
    Derived from Chen (2023) "cingulate-insula hub" concept. Chen's central claim: the
    two pain pathways are not fully independent parallel channels but are coordinated
    by a cingulate-insula hub with four functions: salience detection, cortical-subcortical
    integration, network switching, and ACC functional coupling. Our current implementation
    has z_harm_s and z_harm_a as completely independent encoders with zero cross-talk.
    The hub mechanism addresses this gap with minimal architectural change: a single
    attention/gating layer after the two encoders, parameterised by hub_weight.
    Testable prediction: hub_weight > 0 should improve both forward model R^2 (z_harm_s
    benefits from affective context for precision) and urgency calibration (z_harm_a
    benefits from sensory PE for surprise detection). A hub_weight ablation should show
    performance degradation vs the coordinated version.
  registered: "2026-04-08"
  location: docs/architecture/sd_011_dual_nociceptive_streams.md
  literature_confidence: 0.878
  literature_evidence: |
    4 entries in evidence/literature/targeted_review_connectome_mech_220/.
    Wager et al. (2013, NEJM): fMRI-based neurologic signature of physical pain (NPS).
    Menon & Uddin (2010, TICS): saliency, switching, attention and control (insula hub).
    Seeley et al. (2007, J Neurosci): salience network and insular-ACC hub.
    Wiech et al. (2010, Neuron): perceptual salience and modulatory role of insula in pain.
- id: ARC-052
  title: harm_precision_weighting
  claim_type: architecture_hypothesis
  subject: harm_stream.dynamic_precision
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  depends_on: [SD-011, SD-020, ARC-016, MECH-220]
  description: >
    Each harm stream outputs a precision estimate alongside its latent vector. E3's use of
    harm streams is weighted by their respective precisions: high-precision z_harm_s has more
    influence on attribution (SD-003), high-precision z_harm_a has more influence on commit
    gating. Precision is context-dependent: (1) z_harm_s precision increases with forward
    model accuracy (when E2_harm_s predictions are good, the PE is more informative);
    (2) z_harm_a precision increases with accumulation stability (when threat state is
    changing rapidly, z_harm_a is less precise -- high volatility reduces confidence in the
    accumulated state). Neuromodulatory inputs (serotonin, norepinephrine) modulate the
    precision gains, matching Chen (2023) ACh/NE/DA precision control framework.
  notes: >
    This extends ARC-016 (dynamic precision) into the harm stream specifically. Chen (2023)
    formalizes gain = xi_2 / (xi_2 + xi_1) where relative precision determines how much PE
    updates the prediction. Our current harm encoders have fixed architectures with no
    precision output. Adding a precision head (sigma output alongside mu) to each encoder
    enables the precision-weighted framework. Implementation: HarmEncoder and
    AffectiveHarmEncoder each output (z, log_sigma); E3 weights inputs by exp(-log_sigma).
    V3 scope: implement precision heads but use fixed precision initially; modulate in
    experiments. V4: wire neuromodulatory control (SerotoninModule already exists).
  registered: "2026-04-08"

# Registered 2026-04-11 -- attribution failure mode taxonomy and psychosis substrates
# Source: conversation developing self-attribution overflow, animated agency bias, pre-coherency gate
- id: MECH-221
  title: "z_world must be continuously residualized against z_self predictions; efference-copy subtraction is not limited to SD-003 query time."
  claim_type: mechanism_hypothesis
  subject: latent_stack.continuous_self_residualization
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  depends_on:
    - SD-005
    - SD-007
  notes: >
    SD-003 provides a query-time counterfactual (E2(z_t, a_actual) - E2(z_t, a_cf)) that
    answers "did I cause this specific outcome?" at attribution time. MECH-221 is the
    orthogonal continuous requirement: z_world must never contain self-predicted content in
    the first place. At each timestep, the contribution of the agent's own motor commands
    to the observed z_world update must be subtracted before z_world is read by any
    downstream system (E3, hippocampus, residue field, coherency calculation). This
    continuous residualization is distinct from SD-007 (reafference subtraction in the
    z_self domain) in target: SD-007 removes reafference from z_self; MECH-221 removes
    the forward-projected z_self footprint from z_world. The efference-copy signal must
    be routed to a z_world decontamination pathway as well as to the MECH-095 comparator.
    Failure of continuous residualization activates MECH-222 (self-attribution contamination
    psychosis): self-generated predictions persist in z_world and are confirmed by coherency
    checks, producing false veridical sense of world responding to self. The continuous
    nature of the requirement distinguishes it from SD-003 (query-time) and MECH-095
    (comparator-based detection): residualization must precede all downstream access to
    z_world, not just at attribution query points.

    2026-08-10 edge correction (chip chip-20260810-phase-consistency-reconciliation):
    depends_on: MECH-095 removed. This claim's own text already distinguishes itself from
    MECH-095 (continuous vs comparator-based detection) -- both consume the efference-copy
    signal as parallel siblings, not one depending on the other.
  registered: "2026-04-11"

- id: MECH-222
  title: "Failure of continuous z_self residualization produces self-attribution contamination psychosis: referential delusion and thought broadcasting substrate."
  claim_type: mechanism_hypothesis
  subject: pathology.self_attribution_overflow_psychosis
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  competing_claims:
    - id: MECH-094
      discrimination: >
        MECH-094 is tag-loss psychosis: the hypothesis tag write gate fails during replay,
        opening the post-commit channel during what is structurally a simulation event.
        Residue accumulates from replayed/simulated content. Mechanism: episodic, triggered
        at replay boundaries. Clinical fingerprint: PTSD flashback, intrusive vivid memory
        with affective weight (the simulation feels real because the post-commit somatic
        channel is open). MECH-222 is efference-copy subtraction failure: self-predicted
        world content persists continuously in z_world and is confirmed by coherency checks
        every timestep. Mechanism: continuous, timestep-level contamination. Clinical
        fingerprint: referential delusion, thought broadcasting, grandiosity with
        internally consistent delusional logic. Dissociation criterion: MECH-094 fails
        episodically at replay events; MECH-222 fails continuously during all z_world
        construction. An agent could exhibit one without the other.
    - id: MECH-065
      discrimination: >
        MECH-065 psychosis arises from precision-ratio miscalibration: the ratio of
        internal-model precision to external-authority precision is too low, making the
        RC-conflict lane vulnerable to spoofing by external signals. Directionality is
        inbound -- external signals override the internal model. Clinical fingerprint:
        paranoid ideation driven by over-attributed authority to external agents (signals
        from outside impose on the agent). MECH-222 is the opposite directionality:
        outbound contamination -- internal self-predictions over-populate z_world and the
        agent's model imposes itself on the world representation. The agent does not feel
        controlled by external forces but experiences the world as responding to its own
        internal states.
  depends_on:
    - MECH-221
    - MECH-094
    - MECH-065
    - INV-066
  notes: >
    When MECH-221 (continuous z_self residualization) fails, self-generated motor
    predictions persist in z_world. The coherency calculation (MECH-065 RC-conflict lane;
    broader verisimilitude signalling) then finds z_world consistent with the agent's
    own predictions -- not because the world is responding to the agent, but because the
    agent's predictions were never subtracted. This false coherency is computationally
    indistinguishable from genuine world confirmation of self-generated content, and the
    invariant INV-066 is the reason it cannot self-correct. Clinical substrate: the agent
    experiences world events as caused by or responding to itself. Referential delusion:
    environmental events are interpreted as self-directed because z_world contains the
    agent's own prediction footprint. Thought broadcasting: the agent's predicted motor
    outcomes appear as world-facts, creating the sense that internal states have external
    reality. Grandiosity: persistent positive predicted outcomes in z_world (never corrected
    by actual outcome because the subtraction that would expose the gap is missing) create
    an inflated causal self-model. This is a structurally different failure from MECH-094
    (episodic tag-loss) and MECH-065 (external signal miscalibration). The three psychosis
    substrates are registered as distinct claims to enable experimental dissociation (Q-037).
  registered: "2026-04-11"

- id: MECH-223
  title: "Animated agency attribution overweighting: systematic bias toward attributing intentional causation to animated/moving entities requires active calibration in the attribution pathway."
  claim_type: mechanism_hypothesis
  subject: attribution.animated_agency_overweighting_bias
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  competing_claims:
    - id: MECH-222
      discrimination: >
        MECH-222 is self-attribution overflow (outbound: self-predictions contaminate
        z_world). MECH-223 is other-attribution overweighting (inbound: animated entities
        over-attract intentional-causation attribution from the MECH-095 pathway).
        Both can contribute to ideas of reference but via distinct substrates and with
        distinguishable clinical signatures. MECH-222 produces a coherent self-referential
        world model diffusely ('the world responds to me', no specific agent target);
        MECH-223 produces paranoid ideation directed at specific animated entities
        ('that person/thing is acting toward me'). Dissociation criterion: ablating
        MECH-221 without touching MECH-095 calibration should produce MECH-222 symptoms;
        miscalibrating the MECH-095 prior without disrupting residualization should
        produce MECH-223 symptoms.
    - id: MECH-095
      discrimination: >
        MECH-095 is the TPJ agency-detection comparator: the functional mechanism
        comparing efference-copy-predicted z_self change with observed change to determine
        self-caused vs other-caused attribution. MECH-223 is a calibration claim about
        MECH-095: the comparator is structurally correct but carries an adaptive prior
        that systematically overweights agency attribution in animated/biological-motion
        stimuli. MECH-095 describes the mechanism; MECH-223 describes a systematic bias
        in the prior of that mechanism. The bias is architecturally separable from the
        comparator's function.
  depends_on:
    - MECH-095
    - SD-005
  notes: >
    The agency detection pathway (MECH-095 / TPJ comparator) receives its primary input
    through the lateral visual stream specialised for biological motion (MT -> MST ->
    posterior STS -> TPJ). This stream carries an adaptive prior for detecting intentional
    agency -- useful for predator detection and social navigation -- that creates a
    systematic overweighting of agency attribution to animated or moving entities regardless
    of specific evidence. In a well-calibrated agent this prior is managed by corrective
    feedback; under attentional narrowing, stress, or substrate damage it dominates and
    produces paranoid ideation directed at specific animated agents. This is distinct from
    MECH-222 (where z_world itself is contaminated by self-predictions, producing diffuse
    self-referential reading of the world), from MECH-065 (where external authority signals
    override internal model precision, producing general spoofing vulnerability), and from
    the hostile attribution bias described in MECH-102 notes (which is about violence-script
    activation under pathway-closure conditions). The animated attribution bias is a prior
    miscalibration; hostile attribution is a pathway-selection error. Clinical relevance:
    the bias may be the substrate of referential thinking specifically about people and
    animals (as opposed to objects) and may co-amplify both MECH-222 and MECH-065 failure
    modes by directing their effects toward specific animated targets.
  registered: "2026-04-11"

- id: INV-066
  title: "z_self subtraction (MECH-221) must precede any coherency-based control plane signal; coherency checks on contaminated z_world generate self-sealing false confirmations."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - SD-005
  subject: control_plane.pre_coherency_gate_ordering
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-221
    - MECH-065
    - SD-005
  location: docs/invariants.md#inv-066
  notes: >
    The coherency / verisimilitude calculation (MECH-065 RC-conflict lane; broader
    coherency signalling motifs in the control plane) operates by comparing z_world
    against sensory observations. This comparison is only veridical if z_world contains
    exogenous information only -- i.e., if MECH-221 (z_self residualization) has already
    occurred. If self-generated predictions persist in z_world, coherency checks will
    find z_world consistent with self-predictions (true by construction) and generate
    false high-coherency signals. These signals propagate through the control plane as if
    genuine world confirmations, reinforcing the contamination rather than correcting it.
    This is a self-sealing failure: contamination produces coherency, and coherency
    suppresses the correction signal that would otherwise detect contamination. The
    invariant specifies the required processing order: MECH-221 residualization is a
    mandatory gate that z_world must pass through before entering any coherency or
    verisimilitude calculation. Violations are computationally undetectable from within
    the system -- no downstream coherency signal can identify that it is operating on
    contaminated z_world. The invariant cannot be enforced post-hoc; it is a pipeline
    ordering constraint. This has architectural implications for the control plane:
    as the verisimilitude coherency calculation is extended and refined, each new
    signalling motif that reads z_world must explicitly be placed downstream of the
    residualization gate.
  registered: "2026-04-11"

- id: Q-037
  title: "Are the three psychosis substrate failure modes (MECH-094 tag-loss, MECH-222 self-attribution overflow, MECH-065/MECH-223 precision/bias) experimentally dissociable in the REE substrate?"
  claim_type: open_question
  subject: pathology.psychosis_substrate_dissociability
  polarity: open
  status: open
  live_status:
    reading: open/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-037@2026-05-03T02:50:00Z"
      as_of: 2026-05-03
      verdict: hold_pending_v3_substrate/applied
  what_would_answer: |
    Run a triple-ablation dissociation: selectively induce each of the three
    psychosis substrates -- MECH-094 tag-loss (open the post-commit write channel
    during replay), MECH-222 self-attribution overflow (disable continuous z_self
    residualization so self-content persists in z_world), MECH-065 precision-ratio
    miscalibration (lower the RC-conflict threshold so external authority
    overrides internal model) -- one at a time, leaving the others intact. Read
    distinct behavioural signatures. ANSWER "dissociable" on BOTH legs: (A) each
    ablation perturbs ONLY its own structural locus with the others measurably
    unaffected, AND (B) the induced signatures match the predicted clinical
    fingerprints (episodic replay-flooding for MECH-094, continuous
    internally-consistent delusional logic for MECH-222, context-triggered
    external-source paranoia for MECH-065), using the Lavalle 2020 OCD-vs-SCZ
    source-monitoring dissociation as the readout template. ANSWER "not
    dissociable" if ablations cross-contaminate or the signatures collapse onto
    one phenotype. HARD PRECONDITION: ALL THREE mechanisms must be implemented and
    each independently inducible with non-zero, separable effect before the
    dissociation can be read; MECH-094 is implemented (V3-EXQ-499 PASS) but
    MECH-222 / MECH-065 / MECH-223 status must be verified live and non-degenerate
    first -- if any target is pinned or absent, the triple-ablation is vacuous.
    The Corlett 2025 caveat (MECH-065 may need splitting) bears on leg (A) for
    that arm specifically. V3-tractable in principle but gated on the upstream
    three-mechanism cohort landing.
  epistemic_category: substrate_conditional
  implementation_phase: v3
  depends_on:
    - MECH-094
    - MECH-222
    - MECH-065
    - MECH-223
    - INV-066
  notes: >
    Three mechanistically distinct psychosis substrates have been registered, each with
    a different structural locus and predicted clinical fingerprint:
    (1) MECH-094 tag-loss: hypothesis tag write gate fails during replay; post-commit
    channel opens during simulation; residue accumulates from simulated/replayed content.
    Locus: replay event boundary. Clinical fingerprint: PTSD flashback, intrusive vivid
    memory, affective weight of replay (feels real because post-commit somatic channel
    is open). Episodic.
    (2) MECH-222 self-attribution overflow: continuous z_self residualization absent;
    self-predicted content persists in z_world; coherency checks confirm contamination
    (INV-066 self-sealing). Locus: z_world construction pipeline. Clinical fingerprint:
    referential delusion, thought broadcasting, grandiosity with internally consistent
    delusional logic. Continuous.
    (3) MECH-065 precision-ratio miscalibration: RC-conflict threshold too low; external
    authority signals override internal model. Locus: RC-conflict lane threshold. Clinical
    fingerprint: paranoid ideation driven by specific external agents, ideas of reference
    toward external sources. Context-triggered (by high-salience external input).
    Additionally: MECH-223 animated agency overweighting bias amplifies (2) and (3) by
    directing attribution preferentially at animated entities; it is a bias modifier
    rather than a primary substrate.
    The open question has two parts: (A) structural dissociability -- can each failure
    mode be induced selectively by targeted ablation while leaving the others intact?
    (B) phenotypic distinctness -- do the induced behavioral signatures match the
    predicted clinical fingerprints? A positive answer to both would provide strong
    mechanistic grounding for REE as a psychiatrically-predictive architecture. Answering
    this requires implementing all three mechanisms and designing ablation experiments
    that can distinguish continuous z_world contamination (MECH-222), episodic replay
    flooding (MECH-094), and threshold miscalibration (MECH-065) via distinct behavioral
    readouts.
  evidence_quality_note: |
    Lit-pull 2026-05-02 (ree-weekend-lit session) added 3 entries: Lavalle 2020 Eur
    Psychiatry dissociable source-monitoring OCD vs SCZ (supports MECH-094/MECH-222
    dissociation), Corlett 2025 Am J Psychiatry 20-yr aberrant salience review
    (mixed -- argues MECH-065 may need to split into multiple sub-mechanisms), Oulton
    2018 J Behav Ther Exp Psychiatry PTSD memory amplification reality monitoring
    (supports MECH-094 tag-loss). lit_conf 0.823; quadrant=plausible_unproven, exp_conf=0.

    EPISTEMIC-CATEGORY FLAG (2026-05-02, Phase 3 Q-walk): substrate-conditional. The
    question is structurally well-formed and V3-tractable in principle (per
    implementation_phase=v3), but it depends on ALL THREE mechanisms (MECH-094,
    MECH-222, MECH-065) being implemented before the dissociation experiment can run.
    Currently MECH-094 is implemented (V3-EXQ-499 PASSed under Phase 3); MECH-222 +
    MECH-065 + MECH-223 status need to be checked. The narrow_open_question
    recommendation is misleading: this isn't a question that needs lit narrowing --
    it's a question that needs the upstream substrate cohort to land before a
    dissociation triple-ablation can be queued. The Lavalle 2020 finding (OCD vs
    SCZ source-monitoring dissociates clinically) is direct empirical support for
    the dissociability hypothesis; Corlett 2025 weakens MECH-065 by arguing it may
    need splitting. Two-step path: (a) verify MECH-222 + MECH-065 + MECH-223
    implementation status; (b) when all three are landed, queue a triple-ablation
    discriminative experiment using the Lavalle dissociation as the behavioral
    readout template. Hold as open until (a) + (b) complete.
  registered: "2026-04-11"
  location: docs/architecture/sd_011_dual_nociceptive_streams.md

# Thought ingestion 2026-04-12: harm gradient piecewise structure (Mar 28 thought)
- id: MECH-224
  title: "harm_eval.piecewise_gradient_structure: E3 harm_eval learns both continuous intensity and discrete mode-transition thresholds."
  claim_type: mechanism_hypothesis
  subject: harm_representation.piecewise_gradient
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  depends_on:
    - ARC-024
    - MECH-071
  notes: >
    ARC-024 correctly posits a continuous harm gradient. This claim specifies that the
    internal structure of harm_eval is piecewise-continuous rather than purely smooth:
    response topography (form) transitions discretely at threshold imminence levels
    (pre-encounter -> post-encounter -> circa-strike, per Fanselow predatory imminence
    continuum), while response vigour varies continuously within each mode.
    Evidence: Fanselow PIC literature; Mobbs 2007 (vmPFC -> PAG discrete shift at threshold
    threat imminence, distinct neural signatures above and below the transition point).
    This is separable from ARC-024's gradient-existence claim -- it is a prediction about
    the shape of the learned harm_eval output distribution.
    V3 test: harm_eval output distribution should show bimodal or trimodal clustering
    (none/low, approach/medium, contact/high) with inter-cluster gaps, not unimodal smooth.
    V4 test: agent should learn qualitatively distinct defensive strategies (routing
    modification, freezing, emergency escape) at discrete harm-gradient thresholds.
    Architectural importance: a pure continuous gradient would not trigger emergency
    responses until harm is very high; discrete mode-transitions allow abrupt escalation.
    Likely V4-scope for full three-mode test; V3 can test binary presence/absence of
    mode transitions in harm_eval output distribution.
  registered: "2026-04-12"
  location: evidence/planning/thought_intake_2026-03-28_harm_gradient.md

# Thought ingestion 2026-04-12: verisimilitude formal variables + temporal coherence (Apr 9 thought)
- id: INV-067
  title: "V(t): Verisimilitude is the precision-weighted, phase-aligned, hierarchical correspondence between ascending sensory-constrained and descending model-based signals."
  claim_type: invariant
  invariant_type: universal
  subject: perception.verisimilitude_definition
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: derivational
  what_would_answer: |
    This is a formal DEFINITION, not an empirical hypothesis -- the
    falsifier is definitional coherence, not a PASS/FAIL experimental
    outcome. Two independent things must be shown before the definition is
    considered answered, and both require a working V(t) computation to
    exist first (see non-degeneracy precondition below).

    IRREDUCIBILITY test (does the three-component decomposition hold?):
    the definition claims C_i (content agreement), P_i (phase alignment),
    and alpha_i (precision) are three GENUINELY SEPARATE, independently
    manipulable axes. Confirm this by isolating each:
      - Manipulate content agreement alone (feed a_i/d_i pairs at fixed
        phase offset, varying ||a_i - d_i||): V(t) should track C_i and
        the system should read out as Perception when high, sustained.
      - Manipulate phase alignment alone (fixed content match, varying
        phi_a_i - phi_d_i): V(t) should independently degrade as P_i
        degrades even with C_i pinned high -- this is the critical test,
        since it is the axis with zero current implementation (see
        below).
      - Manipulate precision alone (fixed C_i, P_i; varying alpha_i):
        contribution to the weighted sum should scale accordingly without
        moving C_i or P_i.
    ANSWER "irreducible" (confirms the claim) if each axis can be driven
    independently and each produces its predicted qualitative category
    when isolated per the operational definitions: sustained high V(t)
    with high sensory precision -> Perception; phase-offset/segregated
    V(t) -> Imagination; internally-generated content producing false
    high V(t) in the sensory phase channel without external input ->
    Hallucination. ANSWER "collapsible" (falsifies irreducibility as
    stated) if two of the three terms covary so tightly in the live
    substrate that manipulating one always moves the other, or if
    isolating an axis fails to move V(t) in the predicted direction at
    all.

    DISCRIMINATION test (does the definition ground what it claims to
    ground?): the sharpest test is Hallucination vs. ordinary high-
    confidence correct Perception: both are "high V(t), sustained" by the
    formula as written (Hallucination is defined only by the SOURCE of
    the descending signal being internal rather than external, not by any
    difference visible to V(t) itself). ANSWER "grounds the distinction"
    if some component of the substrate (precision alpha_i, or a signal
    external to V(t) proper) reliably discriminates the two cases at the
    point where V(t) is computed. ANSWER "fails to ground" if the formula
    as specified is provably unable to tell a hallucination from a
    correct high-confidence percept from inside the V(t) computation
    alone.

    NON-DEGENERACY PRECONDITION (check this FIRST): a coherence audit is
    unanswerable if no component of V(t) is implemented as a computed
    quantity anywhere in the live substrate. As of this digestion pass,
    V(t) is UNBUILT as a literal combined quantity: content agreement C_i
    has a Stage-1 proxy only (MECH-269 per-stream V_s in
    ree_core/hippocampal/module.py, update_per_stream_vs -- an identity-
    prediction proxy, not the Gaussian kernel as specified, explicitly
    staged by ARC-055 as "Stage 1: prediction error only"); phase
    alignment P_i has NO implementation and no functional analog --
    MECH-089 (cross-frequency temporal packaging, status active)
    explicitly disclaims implementing oscillatory/phase coupling in the
    ANN substrate ("not as oscillatory coupling... the biological
    mechanism is one implementation of this functional requirement; it
    is not required"); precision weighting alpha_i exists generically
    (obf_use_precision_weighting) but is not wired into any V(t)-shaped
    combination. INV-066 (this claim's stated prerequisite) is itself
    `status: active`, so it is not the blocker -- the blocker is V(t)
    having no combined implementation at all. Re-audit once (a) a
    phase-alignment term P_i is implemented (ARC-055 Stage 2, itself
    candidate/v4), and (b) the combination formula exists as a computed
    quantity reachable by E1/E3, per ARC-055's own staged rollout. Until
    then, this claim can only be evaluated as a design specification for
    internal consistency, not audited against a running system.
  notes: >
    Formal definition of V(t), the core verisimilitude variable. Three irreducible components:
    (1) content agreement: C_i(t) = exp(-||a_i - d_i||^2 / sigma_i^2); (2) phase alignment:
    P_i(t) = (1 + cos(phi_a_i - phi_d_i)) / 2; (3) precision weighting: alpha_i(t).
    V(t) = sum_i [w_i * alpha_i * C_i * P_i] / sum_i [w_i * alpha_i].
    Where a_i = ascending sensory-constrained signal at level i, d_i = descending
    model-based prediction at level i, across the hierarchical latent space.
    Missing any one component collapses the variable: without content agreement = empty
    synchrony; without phase agreement = untimed match; without persistence = cannot
    ground self or planning.
    Operational definitions: Perception = high V(t), sustained high D_V, strong sensory
    precision. Imagination = phase-offset from sensory stream, low/segregated V(t).
    Hallucination (failure) = internal predictions invade sensory phase channel -> false
    high V(t) without external input.
    Prerequisite: INV-066 (z_self residualization must precede any verisimilitude
    calculation -- see INV-066 pre-coherency gate invariant).
    Extends and formalises: MECH-094 (hypothesis tag), MECH-089 (cross-frequency coupling),
    the Apr 7 phase-segregation intake (thought_intake_2026-04-07_phase_segregation_perception_imagination.md).
  registered: "2026-04-12"
  location: evidence/planning/thought_intake_2026-04-09_verisimilitude_ethics.md

- id: INV-068
  title: "Temporal depth D_V(t): system behaviour must depend on temporal persistence of verisimilitude coupling, not instantaneous V(t) alone."
  claim_type: invariant
  invariant_type: universal
  subject: perception.temporal_depth_verisimilitude
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-067
  epistemic_category: substrate_conditional
  what_would_answer: |
    Behavioural, not definitional (contrast INV-067): does REE's
    trajectory/action selection (E3) actually use a persistence-weighted
    verisimilitude signal (D_V, per the ARC-054 V3 rollout-horizon
    synaptic-EMA form -- see
    docs/architecture/dv_temporal_depth_v3_form.md) rather than responding
    only to instantaneous V(t), when choosing between candidate
    trajectories.

    TEST DESIGN: construct two matched rollout conditions differing only
    in the temporal PROFILE of coupling quality, not its instantaneous
    peak -- (a) HIGH-INSTANTANEOUS / LOW-PERSISTENT: V(t) spikes briefly
    high (e.g. one frame of spurious high content-agreement / anchor
    match) then collapses; (b) LOW-INSTANTANEOUS / HIGH-PERSISTENT: V(t)
    stays moderate but sustained across the rollout horizon H.

    CONFIRMED if E3 trajectory selection systematically favours (b) over
    (a) -- i.e. a persistence-weighted score (D_V, or any functionally
    equivalent horizon-integrated signal) measurably shifts action/
    trajectory choice beyond what raw per-step V(t) alone would predict,
    consistent with "avoid trajectories where D_V collapses" and the
    mania-delusion failure-mode framing in this claim's notes and in
    ARC-054.

    FALSIFIED if trajectory selection is indifferent to the temporal
    profile and tracks only the instantaneous V(t) value at each step (or
    a simple non-persistence-weighted aggregate, e.g. max or mean without
    a decay/horizon term) -- i.e. D_V provides no behavioural information
    beyond what V(t) alone already supplies. This would falsify the
    specific integration requirement without necessarily falsifying
    INV-067's underlying V(t) formalism.

    NON-DEGENERACY PRECONDITION: this test presupposes V(t)/D_V exist as
    a live, trajectory-selection-relevant signal wired into E3 --
    confirmed as of 2026-08-07 to NOT be the case. MECH-269's
    per_region_vs (ree_core/hippocampal/module.py) is instantaneous-only;
    the ARC-054 rollout-horizon D_V EMA, use_arc054_dv flag,
    predict_rollout_vs(), and arc054_dv_score additive bias are all
    specified in dv_temporal_depth_v3_form.md but UNIMPLEMENTED (zero
    hits under ree_core/), and the design's own proposed validation
    experiment V3-EXQ-491 (4-arm factorial on use_arc054_dv) has never
    been queued -- no hits in ree-v3/experiment_queue.json, no evidence
    directory, no git log commits. Until that substrate lands, this claim
    is non-degeneracy-blocked -- but for a materially DIFFERENT reason
    than INV-067: INV-067 is blocked because there is no fact of the
    matter to test (a definitional question); INV-068 is blocked only
    because the specific consumer machinery (E3's use of D_V) has not yet
    been built, and becomes immediately testable once ARC-054's V3 form
    is implemented per its own validation-experiment design (V3-EXQ-491's
    acceptance criteria C1-C4 in that doc are a ready-made falsifier and
    should be reused/queued rather than re-derived).
  notes: >
    D_V(t) = temporal integration of V(t), implemented as exponential moving average:
    D_V(t + dt) = lambda * D_V(t) + (1 - lambda) * V(t + dt).
    D_V is the persistence-of-coupling signal. It serves distinct functions from V(t):
    V(t) = current reality-coupling quality; D_V(t) = sustained existence of that coupling.
    D_V grounds: self-continuity (persistence of D_self), hippocampal trajectory evaluation
    (evaluate by predicted future D_V, not just goal attainment), action selection (avoid
    trajectories where D_V collapses), learning prioritisation.
    Failure modes keyed to D_V: reduced temporal depth -> derealisation, unstable self;
    false local coherence (high V(t), low long-term D_V) -> mania/delusion; fragmented
    coherence -> dissociation; pathological commitment (short-term coherence trap).
    Formally: J(pi) = sum_k gamma^k * V_hat_pi(t+k) for trajectory selection.
    D_V can be approximated as exponential smoothing for computational tractability.
    V(t) may be approximated via prediction error inverses, phase synchrony metrics,
    or hybrid signals.
  registered: "2026-04-12"
  location: evidence/planning/thought_intake_2026-04-09_verisimilitude_ethics.md

- id: INV-069
  title: "The self is defined as the dynamically sustained process maintaining high temporal-depth verisimilitude across time, not a static representation."
  claim_type: invariant
  invariant_type: universal
  subject: self.coherence_trajectory_definition
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-067
    - INV-068
  notes: >
    Self = trajectory maintaining high D_self(t) -- the persistence of coherent self-tagged
    coupling across time. Not a static identity label, not a fixed latent vector.
    Formally: V_self(t) = sum_{i in S_self} [w_i * alpha_i * C_i * P_i] /
    sum_{i in S_self} [w_i * alpha_i], where S_self = subset of latent variables
    tagged as self-relevant.
    D_self(t + dt) = lambda * D_self(t) + (1 - lambda) * V_self(t + dt).
    Disruption -> fragmentation, dissociation, or instability of self-representation.
    Aligns with philosophical continuity accounts but is mechanistically specified.
    Self-continuity corresponds to persistence of temporal-depth coherence -- a dynamical
    systems stability property, not a labelling convention.
    Implication for architecture: self-model is not a stored object but an ongoing
    process that must be actively maintained by the TCL (ARC-053).
  registered: "2026-04-12"
  location: evidence/planning/thought_intake_2026-04-09_verisimilitude_ethics.md

- id: INV-070
  title: "Epistemic responsibility: agent is responsible for refining world models toward accurate similarity and threat inference; misclassification is morally relevant, not merely practically inconvenient."
  claim_type: invariant
  invariant_type: universal
  subject: ethics.epistemic_responsibility
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-025
    - INV-028
  epistemic_category: substrate_conditional
  implementation_phase: v5
  what_would_answer: >
    OUT OF DOMAIN (axiom leg): INV-070 as literally stated -- that misclassification of similarity
    and threat is MORALLY relevant, not merely practically inconvenient, because the agent is
    responsible for refining models it necessarily acts under -- is a foundational ethical premise
    (Axiom 4 of the 10-axiom extension). No REE substrate at any level (V3, V4, or V5) can confirm
    or falsify whether an epistemic failure carries MORAL weight in the metaphysical sense the
    axiom asserts, as opposed to merely producing a functional analogue (a behavioural cost) of
    that weight. This leg is out of domain in the same sense as INV-025/026/027/028/029/096.

    LEG 1 (DERIVATIONAL -- resolves by argument, not experiment): does INV-070 hold together as
    the loophole-closing step its notes claim, without contradicting a sibling axiom? INV-070's
    stated function is narrow and checkable by inspection: INV-025 (Axiom 3) establishes only that
    action is model-mediated, and is silent on whether inference quality is itself an obligation;
    INV-070 adds exactly that obligation, closing the "I acted under an honest model, therefore my
    violence was justified" loophole INV-028 (Axiom 7, "others are sufficiently self-like") would
    otherwise leave open, since INV-028 says nothing about whether the "sufficiently self-like"
    judgement was arrived at responsibly. CONFIRMING: INV-070 is shown internally consistent with
    INV-025 and INV-028 -- it strictly ADDS an inference-quality obligation without altering what
    either dependency asserts -- and demonstrably closes the honest-model loophole without
    reopening a different one (e.g. without licensing paralysis-by-uncertainty, or without making
    INV-025's "must act under models" impossible to satisfy in finite time). FALSIFYING: a coherent
    counterexample exists where treating misclassification as morally relevant (a) axiom-conflicts
    with INV-028 or another sibling (e.g. by making "sufficiently self-like" recursively
    unknowable, so INV-028's protection can never engage), or (b) reintroduces a perverse incentive
    the loophole-closing was meant to avoid (e.g. an agent incentivised to avoid ever forming a
    similarity/threat judgement at all, to avoid the responsibility INV-070 attaches to getting one
    wrong -- epistemic paralysis as evasion). This leg is testable now, by argument, independent of
    substrate.

    LEG 2 (SUBSTRATE-EMPIRICAL, currently SUBSTRATE-BLOCKED, doubly so): what IS architecturally
    testable is narrower -- does REE's harm/residue/ethical-cost machinery implement a
    DISTINGUISHABLE causal pathway from MISCLASSIFICATION of similarity/threat specifically (not
    just any prediction error) into ethical cost, over and above that error's ordinary role as a
    learning signal? CONFIRMING: a similarity/threat estimate that is measurably WRONG relative to
    a later-revealed ground truth (e.g. an agent modelled as threatening that in fact was not, or
    vice versa) drives a distinguishable increment in harm_eval/z_harm/residue-field cost, over and
    above the increment an equal-magnitude but non-classification prediction error of the same kind
    would produce -- i.e. misclassification specifically is weighted as ethically costly, not
    merely absorbed into E1/E2/E3's ordinary forward-model loss. FALSIFYING: misclassification of
    similarity/threat has NO distinguishable ethical-cost pathway in the substrate -- it is trained
    and weighted exactly like any other prediction error (as currently: z_harm_a/z_harm_s train via
    plain MSE, ree_core/predictors/e2_harm_a.py:186-204 and e2_harm_s.py:158-178, with no separate
    misclassification label or extra weighting term), with no invariant enforcing the
    moral-relevance INV-070 asserts.

    NON-DEGENERACY PRECONDITION for Leg 2 (currently UNMET, on an axis more basic than INV-028's/
    INV-096's own gap -- confirmed by direct grep, 2026-08-07, reproducing and extending their
    audit): INV-070 requires not only the ARC-010/ARC-047 multi-agent substrate INV-028/INV-029/
    INV-096 are already blocked on (a SocialGridWorld or equivalent, where a second agent's
    harm/threat is attributable to the tested agent's own action -- ARC-047 status: candidate,
    confidence 0.0, implementation_phase v5), but a similarity/threat CLASSIFIER for that other
    agent to exist in the first place. Neither exists in ree_core/ today: zero matches for
    mirror_model/shadow_bundle/other_self/ARC-010; the only "similarity" computations found are
    unrelated cosine-similarity uses (safety-prototype gating, harm-stream regularisation,
    memory-slot diversity); the only "threat" computation is accumulated internal hazard magnitude
    (z_harm_a), never a classification of another agent's intent. So the reframed leg is not merely
    awaiting a multi-agent world to run an experiment in (INV-028's gap) -- it is awaiting the
    classifier the experiment would need to misfire in the first place.

    PRECEDENT NOTE: applies the epistemic_category=substrate_conditional template established for
    INV-028/INV-029/INV-096 (per INV-025's own caution, not mechanically -- checked here via direct
    grep, not assumed) to this, the fourth of the ten-axiom-extension claims (INV-070, INV-071,
    INV-072) to be digested. Cross-referenced against INV-096's near-identical Leg 2 structure (the
    paired Axiom 10/INV-071 language-repair claim) rather than re-derived.
  notes: >
    The 10th-axiom extension of the five-axiom ethical foundation (INV-025-029).
    Axiom 3 (INV-025) establishes that action is model-mediated but does not constrain
    the agent to improve inference or treat misclassification as morally important.
    Without this invariant, an agent could claim "I acted under my model, therefore
    my violence was justified" -- the epistemically irresponsible actor loophole.
    INV-070 closes this loophole: because I act under models, I am responsible for
    refining those models so that similarity and threat are inferred as accurately as
    possible. Errors in world-modelling are ethically relevant failures.
    Consequences: hallucinated threats (psychosis-driven violence), propaganda-distorted
    similarity (dehumanisation), and motivated misclassification (in-group/out-group bias)
    are all ethical failures, not excusable as "acting under an honest model."
    This makes epistemic responsibility a first-class ethical principle, distinct from
    INV-025's weaker model-mediation claim.
    Clinical implications: therapy for violence-prone conditions (psychosis, paranoia,
    dehumanisation) is partly an epistemic intervention -- correcting the model that
    drives violence-permitting inference, not only the motivational state.
  registered: "2026-04-12"
  location: evidence/planning/thought_intake_2026-04-09_verisimilitude_ethics.md

- id: INV-071
  title: "Language functions as a mechanism for recognising, repairing, and re-establishing self-other equivalence across agents."
  claim_type: invariant
  invariant_type: universal
  subject: ethics.language_as_similarity_repair
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  depends_on:
    - INV-028
    - INV-070
    - INV-029
  what_would_answer: >
    NON-DEGENERACY PRECONDITION (two stacked gaps, currently unmet): (1) the multi-agent
    similarity-inference substrate this whole ethics cluster depends on -- ARC-010 mirror
    modelling / D_V_j / beta_j similarity weighting -- confirmed absent from ree_core/
    (zero hits for mirror_model, shadow_bundle, other_self, ARC-010; see ARC-056's
    2026-08-08 digestion); AND (2) a language or symbolic-communication channel between
    REE agents, a further-downstream requirement specific to this claim, ALSO confirmed
    absent -- no dialogue/utterance/message-passing mechanism exists between agents
    anywhere in ree_core/ (the sole near-hit, cross_stream_binder.py's "communication-
    through-coherence," describes intra-agent binding between an agent's own latent
    streams, not inter-agent exchange).

    CONFIRMING: once both exist, introducing a language/dialogue exchange between two REE
    agents whose similarity-inference (beta_j) has degraded or collapsed measurably
    RESTORES beta_j / re-establishes self-other equivalence, via the mechanisms this
    claim's notes specify -- (a) revealing inner states, (b) negotiating threat, (c)
    repairing misclassification, (d) restoring INV-028 belief, (e) expanding "sufficiently
    like me" -- in a way a non-language interaction of matched duration and information
    content does not produce to the same degree.

    FALSIFYING: language exchange shows no differential repair effect on beta_j beyond
    what an equivalent-duration, equivalent-information non-language interaction produces
    -- restoration tracks generic interaction time or information exchanged, not the
    specific linguistic channel this claim assigns the repair work to. This would falsify
    language-as-similarity-repair-technology specifically, without necessarily falsifying
    INV-028/070 (interaction of any kind could still repair similarity inference through
    non-linguistic routes).

    GOVERNANCE FLAG (2026-08-08, not resolved here): INV-072 (which depends on INV-071)
    will very likely inherit this same category/reasoning when digested -- can reuse this
    non-degeneracy framing rather than re-deriving.
  notes: >
    The 10th axiom in the extended ethical system (paired with INV-029 love as mechanism).
    Love is the motive/relation; language is the bridge/tool. They are operationally
    complementary, not alternatives.
    Language functions by: (1) revealing inner states (reducing uncertainty about
    other-agent models), (2) negotiating threat (updating similarity/threat inference
    in real time), (3) repairing misclassification (correcting distorted self-other
    equivalence models), (4) restoring INV-028 (re-establishing belief that others are
    sufficiently self-like), (5) expanding the category of "sufficiently like me"
    (extending moral concern to previously excluded agents or groups).
    Language is a similarity-repair technology. This explains why storytelling, testimony,
    confession, and dialogue can reduce violence even when material conditions have not yet
    changed -- they operate directly on the self-other equivalence model that drives
    violence permissibility (INV-072 / MECH-102).
    Provides mechanistic grounding for: propaganda analysis (deliberate corruption of
    similarity inference via language), de-radicalisation (language-mediated repair of
    distorted models), testimony and truth-and-reconciliation processes.
    Connects to: predictive processing accounts of language as prior-updating, and to
    MECH-102 dual-pathway violence model (language can interrupt both similarity collapse
    and threat-override pathways).
  registered: "2026-04-12"
  location: evidence/planning/thought_intake_2026-04-09_verisimilitude_ethics.md

- id: INV-072
  title: "Violence corollary: violence is conditionally permissible only when an agent is not modelled as sufficiently self-like OR poses a genuine threat; both conditions require accurate inference."
  claim_type: invariant
  invariant_type: universal
  subject: ethics.violence_conditions_corollary
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-028
    - INV-029
    - INV-042
    - INV-070
    - INV-071
  notes: >
    Derived corollary of the 10-axiom system, not a primary claim. Dual-pathway formulation:
    (a) Similarity collapse: other agent is no longer modelled as sufficiently self-like ->
    existence is no longer protected by the axiom system -> violence becomes permissible.
    Clinical: dehumanisation, moral exclusion, out-group bias.
    (b) Threat override: other agent IS modelled as similar but is predicted to threaten
    continued existence (self or protected others) -> Axiom 5-7 conflict resolved by
    conditional violence. Clinical: paranoia (threat inflation), justified self-defence,
    war/group conflict where in-group = self-like + out-group = similar + threatening.
    CRITICAL CONSTRAINT: both conditions require ACCURATE inference (INV-070). Hallucinated
    threat (psychosis), propaganda-distorted similarity (dehumanisation), or motivated
    misclassification do NOT satisfy the condition -- they are ethical failures, not
    excusable as "acting under my model."
    Love (INV-029 / Axiom 9) maintains self-other equivalence and buffers against
    premature threat classification. Love does two things: (1) increases perceived
    similarity, (2) buffers against false positive threat detection. Both suppress
    unnecessary violence without preventing necessary defensive action.
    Language (INV-071 / Axiom 10) repairs collapsed similarity models, allowing
    re-entry into the ethical protection space from outside it.
    Control condition: Preserve other agents IFF (similarity is high) AND (their
    existence does not threaten protected existence). If either fails, protection drops.
    Extends MECH-102 (violence as terminal error-correction) with formal dual-pathway
    derivation from axioms and epistemic responsibility requirement.
  registered: "2026-04-12"
  location: evidence/planning/thought_intake_2026-04-09_verisimilitude_ethics.md

- id: INV-073
  title: "Developmental bootstrapping necessity: any model-building agent requiring flexible multi-option behavioral selection must undergo a dedicated motor-sensory exploration epoch before reinforcement learning narrows the repertoire; insufficient exploration permanently restricts the option library."
  claim_type: invariant
  invariant_type: universal
  subject: learning.developmental_bootstrapping_necessity
  polarity: asserts
  status: candidate
  epistemic_category: substrate_conditional
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - ARC-065    # diversity stack -- the runtime mechanism INV-073 must pre-populate
    - MECH-269   # V_s monostrategy -- the downstream effect INV-073 explains causally
  lit_conf: 0.76
  literature_anchors:
    - "Doupe & Kuhl (1999) Annual Review of Neuroscience -- LMAN silencing arrests subsong,
       produces permanent monostrategy. conf 0.78."
    - "Leitao & Gahr (2024) PNAS -- premature babbling opens sensory template phase;
       motor exploration is causally upstream of perceptual template encoding. conf 0.76."
    - "Warlaumont & Finnegan (2016) PLoS ONE -- severed auditory feedback (simulated
       deafness/tracheostomy) collapses babbling to single low-diversity attractor. conf 0.70."
    - "Li, Wu & Li (2007) Behavioural Pharmacology -- isolation-reared rats: initial
       acquisition unimpaired, reversal learning specifically impaired; developmental
       impoverishment -> permanent behavioral rigidity. conf 0.68."
    - "Wang, Marshall & Kirkpatrick (2017) Behavioural Brain Research -- novelty enrichment
       (not social enrichment) specifically reduces never-tried-this-option failure mode.
       conf 0.64."
  notes: >
    INV-073 identifies the causal upstream factor for the monostrategy problem observed
    in MECH-269 and the EXQ-561 cluster. The claim is at invariant level because the
    principle transcends REE's specific architecture: it applies to any model-based agent
    that uses iterative optimisation to select from a stored option library.

    Three mechanisms converge:
    (1) BG-driven variability epoch is causally necessary (Doupe & Kuhl 1999 LMAN lesion):
        the epoch is not incidental exploratory noise but a structured variability-
        generating system. Suppressing it leaves the option library unpopulated.
    (2) Motor exploration opens the perceptual template phase (Leitao & Gahr 2024):
        the exploration epoch is upstream of template formation, not downstream. An agent
        that abbreviates exploration also fails to form the perceptual cues that later
        serve as retrieval keys for the hippocampal store.
    (3) Closed feedback loop is required (Warlaumont & Finnegan 2016): without intrinsic
        reward from own-action sensory consequences, the system collapses to a single
        low-diversity attractor regardless of exploration duration. The loop, not just
        the duration, is the critical structural requirement.

    REE diagnostic signature: the CEM generator in HippocampalModule regenerates
    trajectories from the same terrain each tick; there is no mechanism to populate a
    persistent trajectory store during an early exploration epoch. The cue_action_proj
    gradient path is severed (EXP-0155), meaning the intrinsic feedback loop between
    goal representation and action generation is broken -- the exact Warlaumont & Finnegan
    topology. This is ARC-072.

    Invariant type: emergent (not derivable from ethics axioms; follows from the
    interaction of optimisation dynamics, motor-sensory feedback, and the structure of
    the option-store lookup problem).
  what_would_answer: >
    CROSS-REFERENCE (read before any retest is queued): INV-073 and ARC-072 name
    the same underlying gap from two angles -- INV-073 is the general/biological
    principle, ARC-072 is REE's own architectural decomposition (Gap 1: no
    persistent trajectory store; Gap 2: no differentiable cue-indexed retrieval,
    EXP-0155's zero-gradient cue_action_proj). This falsifier operationalizes
    ARC-072's R2 specifically, not a fresh test design.

    ARC-074 is a SIBLING claim in the same conversion-ceiling/monostrategy
    cluster, not this claim under another name -- it addresses E3-scoring
    cold-start on a flat residue field and says nothing about a persistent
    trajectory store or the cue_action_proj gradient. A positive ARC-074 result
    is necessary background support for INV-073, not a substitute test. As of
    2026-08-08, ARC-074's own Phase 0 experiment (EXQ-ISEF-006) is confirmed
    unbuilt and unqueued.

    CURRENT SUBSTRATE STATE (checked 2026-08-08): EXP-0155's "severed gradient"
    finding is now PARTIAL, not current as originally stated. SD-055 (landed
    2026-05-15, V3-EXQ-568 PASS, grad_max=372) introduced a differentiable-CEM
    path that closes the zero-gradient argmax barrier -- but use_differentiable_cem
    defaults to False (ree_core/utils/config.py:2047), so the default substrate
    still runs the severed-gradient path. ARC-072's own implementation_note
    confirms "behavioural validation still open." A separate fix, EXQ-449a,
    repaired an unrelated input-degeneracy bug and is not evidence the gradient
    question is closed. Gap 1 (persistent trajectory store) is unambiguously
    still absent from HippocampalModule.

    NON-DEGENERACY PRECONDITION: (i) use_differentiable_cem=True AND a persistent
    trajectory store wired to accumulate CEM candidates across ticks during a
    designated pre-RL window (ARC-072 R1 -- currently unbuilt). (ii) option-
    library diversity measured independently of reward/task performance (e.g.
    per-tick action-class entropy, trajectory-cluster count) -- not E3 score
    variance, which is ARC-074's metric. (iii) confirm the differentiable-CEM
    gradient is genuinely flowing (grad_max sanity check) so a null result
    cannot be attributed to the flag being silently inert.

    CONFIRMING: an agent given a dedicated pre-RL exploration epoch (store
    populated, gradient live) develops and RETAINS a larger, more diverse
    option library than a matched no-exploration-epoch agent -- AND the gap
    does not close when the no-exploration agent is later given equivalent
    additional exploration time (permanence).

    FALSIFYING: either (a) no diversity gap opens once the store and gradient
    are both live -- route back to the MECH-439/INV-088/MECH-457 cluster; or
    (b) a gap opens but is fully recoverable once given equivalent later
    exploration (contradicts claimed permanence).

    GOVERNANCE/QUEUE FLAG (2026-08-08 digestion pass, not resolved here):
    ARC-072's SD-055 differentiable-CEM fix landed 2026-05-15 but its default-off
    flag means no behavioral test has ever run with it enabled -- a concrete,
    cheap /queue-experiment candidate (flip use_differentiable_cem=True, measure
    option diversity) that would directly de-risk both INV-073 and ARC-072 R2/R3.
  registered_utc: "2026-05-15"
  location: docs/architecture/developmental_bootstrapping_hippo_retrieval.md

- id: ARC-053
  title: "Temporal Coherence Loop (TCL): architecture must include a distributed loop responsible for maintaining phase alignment and temporal synchrony necessary for V(t) to be computable."
  claim_type: arch_commitment
  subject: architecture.temporal_coherence_loop
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  phase_provenance: derived
  phase_derived_from: ARC-054
  depends_on:
    - INV-067
    - INV-068
    - MECH-089
  epistemic_category: derivational
  what_would_answer: |
    This is an arch_commitment sitting on THREE dependencies, all already
    audited this session and all reporting the same result: the
    mechanism this claim requires does not exist, and one candidate
    implementation explicitly declines to be it.

    NON-DEGENERACY PRECONDITION (this is the blocker, check nothing else
    first): a claim about a "loop that maintains phase alignment for
    V(t)" is not statable in falsifiable form while V(t) itself has no
    phase-alignment term to maintain. Per this session's INV-067 audit,
    V(t) is UNBUILT as a literal combined quantity -- content agreement
    C_i has a Stage-1 proxy only (MECH-269), phase alignment P_i has NO
    implementation and NO functional analog anywhere in the substrate.
    Per this session's INV-068 audit, D_V (temporal depth/persistence of
    coupling) is equally unbuilt -- no D_V computation exists, and there
    is no consumer wiring into E3 even if one did.

    Independent spot-check this session (grep of ree_core/ and
    experiments/ for phase_align, temporal_coherence, TCL,
    phase_reference, phase coupling, synchrony, oscillat): confirms no
    TCL-shaped mechanism exists. The only related implementation is
    cross_stream_binder.py, whose own module docstring states binding-
    by-synchrony is LEARNED, not a fixed phase-coupling field -- the
    opposite design choice from what TCL specifies. BreathOscillator
    (MECH-108) is a periodic threshold sweep, not phase/timing-window
    coupling.

    MECH-089 is the sharpest finding, because it is not merely
    "unbuilt" -- it is a DELIBERATE ARCHITECTURAL DISCLAIMER. MECH-089
    (cross-frequency temporal packaging, the one dependency of ARC-053
    that could plausibly implement phase/timing coupling) states
    explicitly: "implemented as a rolling aggregation over E1 outputs
    before E3 sampling, NOT as oscillatory coupling... the biological
    mechanism (theta-gamma CFC) is one implementation of this functional
    requirement; it is not required in the ANN substrate." So this is
    not one un-built dependency plus two others -- it is three
    compounding blockers, one of which is a standing design decision
    against the mechanism class ARC-053's title names.

    What would need to change before this becomes testable:
      (a) V(t) needs at least a computed phase-alignment term P_i(t) to
          exist (ARC-055 Stage 2, itself status: candidate and unbuilt).
      (b) An actual timing/synchrony-adjustment mechanism, distinct from
          MECH-089's disclaimed rolling-aggregation approach, would need
          to be built -- OR MECH-089's existing scope would need to be
          re-examined as already discharging what ARC-053 requires,
          under a non-anatomical name.

    LIVE ARCHITECTURAL QUESTION (flagged, not resolved here): does the
    ANN substrate genuinely need literal phase/oscillatory coupling to
    make V(t) computable, or does MECH-089's functional-analog rolling
    aggregation already satisfy ARC-053's functional requirement --
    "ensure ascending and descending signals meet in a comparable
    temporal frame before comparison" -- just not implemented as a
    distributed phase-alignment loop and not named TCL? If the latter,
    ARC-053 as currently titled may be asking for a specific biological
    IMPLEMENTATION where only the functional REQUIREMENT is
    architecturally necessary -- in which case the claim needs
    re-scoping to the requirement level before it can be evaluated at
    all. This mirrors MECH-089's own stated position and should be
    resolved by whoever next touches ARC-053/MECH-225/MECH-226, not
    assumed either way here. This propagates forward: ARC-053 is itself
    a load-bearing dependency for ARC-054, ARC-055, MECH-226, MECH-227,
    MECH-228.

    Re-audit only after (a) above lands, i.e. once V(t) has a real,
    non-collapsed phase-alignment term computed and reachable by E3/E1.
  notes: >
    V(t) requires that ascending and descending signals meet in the same phase channel
    at the right moment. Without a temporal coherence mechanism, predictions and inputs
    miss each other in time, phase-coded multiplexing breaks, and V(t) becomes
    undefined regardless of content accuracy.
    The TCL is a distributed control loop (not a single module) that: (1) aligns phase
    relationships across subsystems, (2) adjusts timing windows and synchrony, (3) enables
    or prevents coupling between prediction and input, (4) modulates integration windows
    based on task demands and mode (wake, imagination, sleep).
    Failure modes: if TCL drifts -> misalignment -> weak coupling -> derealisation or
    low vividness. If TCL collapses or overcouples -> simulation enters sensory phase ->
    hallucination-like states.
    Biophysical substrate hypothesis: inferior olive + cerebellum + thalamus + cortical
    networks (MECH-226). The inferior olive's historically unclear function may be
    specifically understood as the phase reference generator enabling this loop.
    Inputs: prediction-input timing mismatch, phase coherence metrics, D_V trends.
    Outputs: phase adjustments, temporal integration window tuning, coupling thresholds.
    TCL must maintain conditions under which V(t) is computable as its primary constraint.
    Also required for: sleep-phase transitions (mode changes require TCL recalibration),
    anaesthesia accounts (MECH-227 = TCL disruption), and the pre-coherency gate (INV-066
    requires that residualization precedes TCL-enabled coherency checks).
  registered: "2026-04-12"
  location: evidence/planning/thought_intake_2026-04-09_verisimilitude_ethics.md

- id: ARC-054
  title: "D_V trajectory selection: E3 trajectory selection must optimise predicted temporal-depth verisimilitude D_V over planning horizon H, not instantaneous V(t) alone."
  claim_type: arch_commitment
  subject: architecture.dv_trajectory_selection
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  v3_form_promoted_utc: "2026-04-26"
  depends_on:
    - INV-068
    - MECH-269   # V3 form: per-region V_s readout source
    - MECH-284   # V3 form: staleness accumulator (provides V_s_anchor)
    - MECH-288   # V3 form: BoundaryEvent segmenter (rollout segment_id projection)
    - ARC-007    # V3 form: hippocampal rollout substrate
    - ARC-018    # V3 form: viability map (rollout consumer)
    - ARC-053    # V4 form ONLY: TCL phase substrate (deferred)
  notes: >
    Hippocampal rollout evaluation criterion: trajectories are evaluated by predicted
    future D_V, not just goal attainment. Formally: J(pi) = sum_k gamma^k * V_hat_pi(t+k).
    E3 selection: pi* = argmax_pi J(pi) subject to survival and harm constraints.
    Viable futures = those maintaining coherent existence; non-viable futures = collapse
    of coherence.
    Optimising instantaneous V(t) alone is insufficient -- it creates short-term coherence
    traps (high local V, low long-term D_V -> mania/delusion failure mode).
    kappa_commit (commit threshold) = minimum predicted D_V required to commit to a
    trajectory. Long horizon H prioritises long-term stability over local coherence.
    This replaces simple reward maximisation with coherence-persistence maximisation,
    which is more general: reward becomes a special case of coherence preservation.
    The architecture naturally generalises: maximising D_V over long H corresponds
    to the ethical axioms (Axiom 5: preserve existence = avoid D_V collapse; Axiom 7:
    others' existence = extend D_V tracking).
  v3_form_note: |
    Promoted v4 -> v3 on 2026-04-26 in *rollout-horizon synaptic-EMA form*. The V3 form
    reads V_hat_pi(t+k) from MECH-269 per-region V_s (Phase 2 iii T4 readout) walked
    over the rollout via the MECH-288 BoundaryEvent segmenter, and computes
    D_V_pi(t+k) = lambda * D_V_pi(t+k-1) + (1 - lambda) * V_hat_pi(t+k) with
    lambda configurable (arc054_dv_lambda, default 0.85). The trajectory cost
    J(pi) = sum_{k=0}^{H} gamma^k * D_V_pi(t+k) is added as an additive bias on the
    existing E3 trajectory score. Master flag use_arc054_dv (default OFF, bit-identical
    contract preserved). The V3 form drops the ARC-053 TCL dependency that kept the V4
    form deferred -- V3 reads scalar synaptic V_s; V4 will upgrade the readout to phase-
    aligned V(t) once ARC-053 / MECH-225 / MECH-228 land. ARC-053 retained in
    depends_on as V4-form-only dependency. Validation V3-EXQ-491 (proposed, queue after
    implement-mech269b lock releases): 4-arm factorial against EXQ-074b z_goal salience
    contest + EXQ-481 V_s commit-release. Full V3 substrate plan:
    docs/architecture/dv_temporal_depth_v3_form.md. V3/V4 boundary commitment:
    docs/architecture/v3_v4_phase_substrate_boundary.md.
  registered: "2026-04-12"
  location: docs/architecture/dv_temporal_depth_v3_form.md
  source:
    - evidence/planning/thought_intake_2026-04-09_verisimilitude_ethics.md

- id: ARC-055
  title: "Verisimilitude signal availability: V(t) and D_V must be explicitly available to E3 selection and influence E1/E2 learning updates."
  claim_type: arch_commitment
  subject: architecture.verisimilitude_signal_availability
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  depends_on:
    - INV-067
    - INV-068
    - ARC-053
    - ARC-054
  notes: >
    Both V(t) and D_V must be available as explicit signals (not emergent epiphenomena)
    because they influence: (1) E3 trajectory selection (ARC-054); (2) E1 state representation
    (high D_V -> stable attractors, low D_V -> fragmentation/reorganisation); (3) E2
    transition model (predict future V(t) and D_V alongside next state); (4) learning
    update prioritisation (high-D_V states prioritised for consolidation).
    Control-plane parameters modulating V/D_V: alpha_S (sensory precision) controls weight
    of sensory coupling; alpha_A (policy precision) commits to high-D_V trajectories;
    tau_E2 (rollout temperature) determines D_V diversity in planning; H (horizon) sets
    temporal window for D_V evaluation; kappa_commit (commit threshold) sets minimum D_V
    required for commitment; g_S (sensory gain) modulates coupling signal magnitude.
    Practical staging for implementation: Stage 1 = prediction error only (V approx
    exp(-E)); Stage 2 = add global phase coherence factor; Stage 3 = level-specific
    phase-aware hierarchical V.
  registered: "2026-04-12"
  location: evidence/planning/thought_intake_2026-04-09_verisimilitude_ethics.md

- id: ARC-056
  title: "Ethics-as-coherence architecture: ethical trajectory selection is implemented as optimisation of shared temporal-depth coherence across self and represented others."
  claim_type: arch_commitment
  subject: architecture.ethics_as_coherence_optimisation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v5
  governance_2026_06_10: implementation_phase v4 -> v5 (social tier, V5 roadmap wave)
  epistemic_category: substrate_conditional
  depends_on:
    - INV-067
    - INV-068
    - INV-069
    - INV-028
    - INV-029
    - INV-070
    - INV-071
    - INV-072
    - ARC-054
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: J_ethical's terms are all unbuilt/unexercised -- D_V_self
    (ARC-054) flag defaults off; D_V_j needs the same multi-agent substrate INV-028/029 are
    independently blocked on; beta_j and the threat/similarity classifier (INV-070) confirmed
    absent from ree_core/. Can only be evaluated for internal consistency, not audited live.

    LEG 1 (DERIVATIONAL, testable now by argument): does the axiom -> D_V mapping hold
    together? Does beta_j -> 0 recover ARC-054's J(pi) exactly (no discontinuity)? Does the
    violence condition (beta_j collapse OR self-preservation forcing a D_{V,j} sacrifice)
    exhaustively partition the case space? CONFIRMING: strict generalisation, beta_j -> 0
    recovers ARC-054 exactly, violence condition jointly exhaustive. FALSIFYING: a coherent
    counterexample where the limit doesn't recover ARC-054, or a case the two-branch
    violence condition can't classify.

    LEG 2 (SUBSTRATE-EMPIRICAL, blocked): once built -- an agent with intact beta_j != 0
    toward a modelled other, manipulate that other's predicted D_{V,j} across matched
    trajectory pairs. CONFIRMING: trajectory ranking shifts in the axiom-consistent
    direction, shift size scales with beta_j (not all-or-nothing) -- formula does real work.
    FALSIFYING: no measurable difference beyond a beta_j-blind baseline (formula present but
    decorative), or an agent whose beta_j has collapsed shows no differential willingness to
    sacrifice that target's D_{V,j}.

    GOVERNANCE FLAG (2026-08-08 digestion, not resolved here): ARC-096 and ARC-097
    (registered 2026-06-10, same day as this claim's v4 -> v5 push) each already carry
    possible_overlap_with: ARC-056 -- review for merge -- pre-existing and unresolved;
    a claims.yaml disposition call for governance, not digestion.
  notes: >
    The 10-axiom ethical system maps directly onto D_V mechanics, producing unified
    computational ethics: J_ethical(pi) = beta_self * D_V_self + sum_j beta_j * D_V_j,
    where beta_j depends on inferred similarity (INV-028), relational commitment (INV-029),
    and responsibility structure (INV-042).
    Ethical axiom -> D_V mapping:
      Axiom 2 (existence has value) -> maintain V(t)
      Axiom 5 (preserve self) -> avoid trajectories where D_self collapses
      Axiom 7 (others' responsibility) -> extend D_V tracking to D_{V,j}
      Axiom 9 (love) -> expand the set whose D_V is optimised
    Violence condition in D_V terms: beta_j collapse (agent no longer inferred self-like)
    OR preservation of higher-priority D_V requires sacrificing D_{V,j}.
    Language (INV-071) -> updates other-agent models, reduces uncertainty in similarity
    inference, increases shared D_V across agents.
    Multi-agent ethical conflicts: non-trivial optimisation conflicts require hierarchical
    or weighted D_V aggregation. Burnout = tracking too many agents' D_V (overextension
    of beta_j scope).
    The architecture is substrate-agnostic at the invariant level (INV-067-072) while
    proposing specific biological implementations at the mechanism level (MECH-225-228).
    V4 scope: requires ARC-054, ARC-055, INV-067-072, and multi-agent representation
    of D_{V,j} for other modelled agents.
  registered: "2026-04-12"
  location: evidence/planning/thought_intake_2026-04-09_verisimilitude_ethics.md

- id: MECH-225
  title: "Oscillatory coupling multiplexing: cross-frequency oscillatory dynamics (gamma/theta/beta/delta) implement phase-based separation of perception, simulation, and action preparation streams."
  claim_type: mechanism_hypothesis
  subject: perception.oscillatory_multiplexing
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  depends_on:
    - MECH-089
    - INV-067
  notes: >
    The same E1/E2 substrate can simultaneously encode: incoming sensory-constrained
    hypothesis (perception channel), counterfactual rollout (imagination channel), and
    action-committed trajectory (beta-stabilised), with phase offsets and cross-frequency
    coupling separating them. This extends MECH-089 (CFC packages E1 updates for E3) to
    the full multiplexing scheme.
    Phase-channel assignments: gamma = local binding (E1 content); theta = sequencing /
    hippocampal rollout (E3-hippocampus loop); beta = stabilised action / commitment bias;
    delta = global state / mode.
    Shared neurons participate in multiple streams, disambiguated by phase-of-firing
    relative to oscillatory carriers. This gives a natural multiplexing scheme without
    needing separate circuits for perception and imagination.
    Phase separation maintenance (maintained = imagination; collapsed = perception /
    "this is happening"). Collapse of phase separation = hallucination-like phenomena.
    Ephaptic coupling (MECH-228) is one physical substrate that helps enforce or bias
    phase separation.

    CURRENCY NOTE (2026-08-08, thought-digestion): the gamma/theta/beta/delta ->
    function mapping above is presented as settled biology with no hedge. Literature
    check (McLelland & VanRullen 2016, PLoS Comput Biol 12(10):e1005162; Hyafil et al.
    2015, eLife 4:e06213) shows cross-frequency multiplexing is an actively-studied
    area of reconciliation between competing theories, not consensus -- this specific
    four-way assignment should not be cited as established neuroscience if this claim
    is later promoted or referenced elsewhere. Also: MECH-227 (same cluster, depends
    on MECH-225) is missing implementation_phase and epistemic_category entirely,
    unlike siblings MECH-226/228 -- a cluster-consistency gap flagged for governance,
    not fixed here (out of this claim's scope).
  epistemic_category: substrate_conditional
  what_would_answer: >
    MECH-225 fuses two claims that resolve in different domains. Following the
    precedent already established by its own dependency MECH-089 (functional_
    restatement: the biological mechanism is one implementation of a functional
    requirement, not required in the ANN substrate), split explicitly.

    LEG 1 (REE mechanism, substrate_conditional -- testable once V4 substrate exists):
    does REE's E1/E2/E3-hippocampus system implement some functional analog of
    channel-banded routing letting shared representational capacity simultaneously
    carry (a) sensory-constrained perception, (b) counterfactual/imagined rollout, and
    (c) committed action-trajectory content, disambiguated without separate circuits
    -- and does collapse of that separation produce a measurable confusion signature
    between imagined and perceived content (this claim's own hallucination-analog
    prediction)? "Gamma/theta/beta/delta" should be read as a functional-separation
    requirement, not a commitment to literal continuous-time oscillatory dynamics.

    NON-DEGENERACY PRECONDITION: requires V4 substrate that concurrently encodes at
    least perception + imagination (or perception + action-commitment) over shared
    representational capacity with an explicit disambiguation/routing mechanism --
    verified absent in V3 as of this date: ThetaBuffer (MECH-108), BetaGate
    (MECH-090), and cross_stream_binder.py each implement at most a two-way
    separation, none disambiguate three concurrent streams over shared capacity.

    CONFIRMING signature: once V4 substrate exists, ablating/collapsing the
    channel-separation mechanism measurably increases confusion between
    imagined-rollout content and perceived content, beyond what ablating either
    channel alone accounts for.

    FALSIFYING signature: perception, imagination, and action-commitment streams
    remain functionally distinct and non-confusable under shared representational
    capacity even with no explicit disambiguation mechanism -- meaning the
    "multiplexing" framing is unnecessary machinery relative to what plain
    capacity-separation already achieves.

    LEG 2 (biological substrate, out of REE's test domain): whether biological
    gamma/theta/beta/delta oscillations specifically implement local binding /
    hippocampal sequencing / commitment-bias / global-mode respectively, in actual
    cortex and hippocampus, is a human/animal electrophysiology question no REE
    experiment can adjudicate -- resolved by literature, not REE experiment. Per
    PubMed: McLelland & VanRullen 2016 reconcile "communication-through-coherence"
    (Fries) and the "theta-gamma neural code" accounts of oscillatory multiplexing in
    a single spiking-network model (PLoS Comput Biol 12(10):e1005162, DOI
    10.1371/journal.pcbi.1005162); Hyafil et al. 2015 show theta-gamma coupling
    functioning as a de-multiplexing mechanism for speech in human auditory cortex
    (eLife 4:e06213, DOI 10.7554/eLife.06213). This literature supports that
    cross-frequency multiplexing is an actively-studied biological mechanism, NOT
    that the specific gamma=binding/theta=sequencing/beta=commitment/delta=global-mode
    assignment in this claim's notes is settled consensus.
  registered: "2026-04-12"
  location: evidence/planning/thought_intake_2026-04-09_verisimilitude_ethics.md

- id: MECH-226
  title: "TCL substrate: Temporal Coherence Loop is implemented as a distributed system involving the inferior olive, cerebellum, thalamus, and cortical networks."
  claim_type: mechanism_hypothesis
  subject: architecture.tcl_biophysical_substrate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  depends_on:
    - ARC-053
    - MECH-225
  notes: >
    Biophysical substrate hypothesis for the Temporal Coherence Loop (ARC-053):
    (1) Inferior olive: phase synchronisation via gap-junction electrical coupling;
    generates precisely rhythmic timing signals. Historically unclear function --
    may be specifically interpreted as phase reference generator for reality-coupling.
    Key properties: electrical coupling (ephaptic-like), precise timing, synchronisation
    role rather than content processing. Climbing fibres to Purkinje cells provide
    temporally precise, synchronised input.
    (2) Cerebellum: timing calibration, predictive alignment of forward models (E2-like).
    E2 = cerebellum-like. Increasingly implicated in cognitive as well as motor prediction.
    Uses timing signals from inferior olive rather than originating phase reference.
    (3) Thalamus (thalamocortical loops): global coordination and gating, synchronises
    cortical oscillations, gates sensory vs internal signals.
    (4) Cortical networks: phase-coded representation (E1 substrate). Communication
    through coherence (Fries) -- effective neural communication via phase alignment.
    Together: inferior olive provides phase reference -> cerebellum aligns predictions
    -> thalamus coordinates globally -> cortex implements phase-coded representations.
    Clinical: cerebellar findings in schizophrenia, timing abnormalities in psychosis,
    sleep-phase disruptions -- all potentially attributable to TCL disruption.
  registered: "2026-04-12"
  location: evidence/planning/thought_intake_2026-04-09_verisimilitude_ethics.md

- id: MECH-227
  title: "Anaesthesia as D_V collapse: general anaesthesia abolishes consciousness by disrupting phase alignment, degrading V(t) globally, and collapsing D_V to zero."
  claim_type: mechanism_hypothesis
  subject: pathology.anaesthesia_consciousness
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - INV-067
    - INV-068
    - ARC-053
    - MECH-225
    - MECH-228
  notes: >
    Anaesthetics (propofol, sevoflurane, etc.) act via GABA-A potentiation, potassium
    channel effects, NMDA modulation -- all alter network excitability and synchrony.
    Mechanism in V(t)/D_V terms:
    (A) Reduced effective connectivity: ascending and descending signals fail to couple
    -> C_i(t) drops (content mismatch / ineffective propagation).
    (B) Phase disruption or over-synchronisation: desynchronisation (no coherent timing)
    OR excessive low-frequency synchrony (delta-dominance) -> P_i(t) becomes non-informative.
    (C) Collapse of hierarchical integration: high-level <-> low-level interaction weakens.
    Result: V(t) -> 0 globally, D_V -> 0 (coupling not sustained even transiently).
    Conscious experience requires sustained temporal-depth verisimilitude (INV-068);
    anaesthesia prevents the system from maintaining temporally extended coupling between
    its internal models and incoming signals.
    Additional mechanism: the similarity metric used in V(t) computation may itself be
    distorted under anaesthesia -- noise-dominated similarity (reduced differentiation),
    over-smoothing (loss of discriminability), or decoupled representation geometry
    (a_i and d_i live in effectively different spaces -> similarity function meaningless).
    So anaesthesia may degrade both the SIGNALS and the METRIC used to compare them.
    Clinical predictions: light sedation = reduced D_V, fluctuating V -> dreamy/unstable;
    deep anaesthesia = near-zero D_V -> no stable self or reportable experience; emergence
    = gradual restoration of phase structure and hierarchical coupling -> rising D_V
    before coherent cognition returns.
    Unifies: disrupted integration theories, thalamocortical breakdown, posterior hot zone
    instability -- all grounded in V(t)/D_V collapse mechanics.
    Loss of consciousness correlates with disrupted long-range cortical connectivity and
    altered oscillatory patterns (increased slow-wave activity) -- consistent with D_V collapse.
  registered: "2026-04-12"
  location: evidence/planning/thought_intake_2026-04-09_verisimilitude_ethics.md

- id: MECH-228
  title: "Field-level coherence support (ephaptic coupling): extracellular electric field interactions stabilise phase relationships across neural populations, supporting temporal coherence."
  claim_type: mechanism_hypothesis
  subject: architecture.ephaptic_coupling_coherence
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  depends_on:
    - ARC-053
    - MECH-225
  notes: >
    Ephaptic coupling = interaction between neurons via local extracellular electric
    fields (not synapses). Particularly prominent in tightly packed, synchronised
    structures: cortex, hippocampus, cerebellum.
    Function in V(t)/D_V framework: contributes to the physical substrate enforcing or
    biasing phase alignment across populations, supporting temporal coherence (ARC-053).
    Not the sole mechanism -- one contributor to the phase coherence scaffold alongside
    synaptic transmission, gap junctions (inferior olive, MECH-226), and oscillatory dynamics.
    Properties: fast (no synaptic delay), local but scalable through synchrony, strongest
    when neurons are aligned and oscillations are coherent. Ideal for stabilising the
    phase relationships needed for coupling -- reduces phase drift, promotes synchronisation,
    stabilises coupling windows.
    Updated P_i(t) formulation with ephaptic contribution:
    P_i(t) = f(delta_phi_i(t), local field coherence), where ephaptic effects reduce
    phase drift and promote synchronisation.
    Anaesthesia link: many anaesthetics change membrane conductance and extracellular
    ionic environment, reducing effective synchrony -> disrupt conditions under which
    ephaptic coupling meaningfully stabilises phase -> phase drift -> degraded P_i(t)
    -> V(t) collapse (MECH-227).
    Note: inferior olive uses gap junctions (not strictly ephaptic -- distinct mechanism
    but related category of field-level coupling). Distinguishing synaptic, gap junction,
    and ephaptic contributions remains active neuroscience research. All three contribute
    to phase coherence as complementary mechanisms.
    Role of ephaptic coupling in cognition-level segregation remains debated; role in
    synchrony is well-supported (Anastassiou & Koch 2015; Fries 2015 communication
    through coherence).
  registered: "2026-04-12"
  location: evidence/planning/thought_intake_2026-04-09_verisimilitude_ethics.md

- id: MECH-229
  title: "E3 produces reliably distinct wanting and liking behavioral signals: wanting drives persistent approach toward goal-encoded locations (incentive salience); liking produces only consummatory response at resource contact."
  claim_type: mechanism_hypothesis
  subject: drive.wanting_liking_behavioral_dissociation
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-538a_2026-07-10
      as_of: 2026-07-10
      verdict: non_contributory/substrate_ceiling
  epistemic_category: standard
  implementation_phase: v3
  split_from: MECH-112
  registered: "2026-04-13"
  pending_retest_after_substrate: true
  narrow_supports_flag: true
  related_claims:
    - MECH-436   # drive-coupling child split out 2026-06-16 (/claim-synthesis); MECH-229 retained as the narrowed umbrella for the established wanting!=liking dissociation (leg a), MECH-436 carries the drive-state-modulation leg (b), pending_retest_after_substrate, owning falsifier V3-EXQ-514r
  depends_on:
    - SD-005
    - SD-015
    - SD-049
    - ARC-030
    - MECH-069
  location: docs/architecture/approach_avoidance_symmetry.md#mech-112
  notes: >
    Behavioral arm of the MECH-112 atomic split (2026-04-13). Tests that wanting and liking
    produce dissociable behavioral signatures independently of z_goal latent structure.
    Wanting = incentive salience (prospective, approach-driven); liking = hedonic response
    at contact. Measured by wanting_l1_fraction vs liking_l1_fraction dissociation delta.
    Evidence: EXQ-074f PASS 4/4 seeds, EXQ-234 PASS 3/3 seeds, EXQ-354 PASS 3/3 seeds
    (2026-04-04 to 2026-04-12). 10 seeds across 3 independent experiments, all PASS.
    No conflicting evidence at promotion time. Promoted to active 2026-04-13.
    Demoted active -> provisional 2026-06-01 pending substrate-honest retest on SD-049
    substrate (see evidence_quality_note triage trail).
    Prior hold_candidate_resolve_conflict (MECH-112) was driven by conflation with the
    z_goal latent structure arm (now MECH-230). Behavioral dissociation does not require
    SD-012 homeostatic drive -- z_world fallback seeding is sufficient.
    Neurological basis: Barch & Dowd 2010 (wanting/liking in schizophrenia);
    Hollerman et al. 2000 (striatal goal representation). Full literature in MECH-112.
  evidence_quality_note: |
    2026-07-10 (governance, failure_autopsy_V3-EXQ-538a, confirmed; user-adjudicated): V3-EXQ-538a (SD-049 Phase-2 sleep-on ablation) tagged MECH-229 non_contributory (already so in the manifest). The run re-ran the pre-enrichment 514f coarse config so the wanting!=liking pathway was starved; it neither supports nor weakens. MECH-229 UNCHANGED (standard, confirmed_established via 514o/514q object-bound supports=3, exp_conf 0.812). No status/category change.
    2026-06-16 (DECOMPOSITION, /claim-synthesis, failure_autopsy_V3-EXQ-514q CONFIRMED): MECH-229 split.
    The 4-autopsy 514 lineage (514l/m/p/q, a distinct signature each) is granularity debt -- every genuine
    MECH-229 support (514o object-bound fraction 0.80; 514q object_bound 0.70) lands on the wanting!=liking
    object-bound dissociation (leg a), while every FAIL/churn circles a DRIVE-STATE-MODULATION leg the broad
    claim never separately named. The drive-coupling leg (b) is now carried by the new child MECH-436
    (candidate, substrate_ceiling -- the conditioning substrate is the V3 SD-049-PHASE-2 kappa-scaling env
    amend, NOT V4-blocked; pending_retest_after_substrate, owning falsifier V3-EXQ-514r;
    lit-grounded by targeted_review_connectome_mech_347 incentive-salience pull). MECH-229 is RETAINED as the
    narrowed umbrella for leg (a) -- status (provisional), epistemic_category (standard),
    pending_retest_after_substrate (true) and narrow_supports_flag (true) all UNCHANGED this cycle (per the
    confirmed autopsy section 8). The established (a) dissociation is NOT weakened by this split. Proposal:
    evidence/planning/claim_synthesis_MECH-229_2026-06-16.md (user-approved per-child 2026-06-16, child tagged
    substrate_conditional). See related_claims: MECH-436.
    2026-06-15 (governance, failure_autopsy_V3-EXQ-514p CONFIRMED -- SUPERSEDES the promotion-path
    prediction below): the confirmatory replication V3-EXQ-514p FAILed, and the per-claim weakens is
    reclassified non_contributory (measurement_test_design_defect) and scoring-excluded. The load-bearing
    criterion C_WL (raw fraction most-wanted != last-consumed, bar 0.6) does NOT isolate the drive-coupled
    incentive-salience content MECH-229 asserts: the within-eval drive-dependence control
    wl_drive_minus_nodrive is ~0 in BOTH the 514o PASS (0.083) and the 514p FAIL (0.074) -- exactly zero on
    2/3 514p seeds -- and 514o's drive-uniform negative control (mean_wl_nodrive 0.717) itself clears the 0.6
    bar. With n_resource_types=3 the raw fraction is dominated by ~(N-1)/N categorical baseline mismatch,
    drive or no drive. CONSEQUENCE: the 514o supports + the 2026-06-15 04:00Z promotion path (the
    substrate-ceiling-lifted / pending_retest_after_substrate=false / narrow_supports_flag=false
    reclassification below) all rest on the SAME non-isolating criterion -- so the provisional->stable
    promotion is PAUSED. MECH-229 stays provisional (NO demotion -- the mechanism is not refuted, only the
    criterion is non-isolating); narrow_supports_flag -> true and pending_retest_after_substrate -> true
    restored. epistemic_category stays standard (the substrate IS adequate -- SD-057 built 2026-06-04, contact
    guard 3/3, bank populated, positive-control separation 1.0 -- so this is NOT a substrate ceiling; no
    substrate_queue create/amend). Corrected retest = V3-EXQ-514q: make the drive-dependence delta
    (wl_drive_minus_nodrive) the LOAD-BEARING criterion with an effect-size gate max(k*pstdev(delta), FLOOR),
    raw C_WL a non-vacuity check only. The self-routed genuine-weakens off-ramp (substrate_conditional, V4-1)
    is NOT fired (unmet precondition; cf. V3-EXQ-642). goal_pipeline:GAP-2/GAP-7 closure was on the 514o
    behavioural dissociation and is independent of this criterion-isolation defect.
    2026-06-15 (governance): SUBSTRATE CEILING LIFTED. V3-EXQ-514o is a genuine, non-degenerate
    PASS/supports on the SD-049 multi-resource substrate -- exactly the retest pending_retest_after_substrate
    awaited. mean_object_bound_wl_dissoc_fraction = 0.80 (>= 0.6 pre-registered) with n_scored_wl_steps
    5/6/7 across seeds (the 681-C4 liking-source fix cured 514n's n_scored=0; the DV now sources the
    consumed-type tag from info[sd049_consumed_type_tag_this_tick], not the post-consumption-cleared obs
    cell), both readiness gates met, non_degenerate=true. This is the object-bound wanting-target !=
    liking-target dissociation reaching BEHAVIOUR -- the distinction the coarse substrate previously could
    not deliver. User-adjudicated 2026-06-15: epistemic_category substrate_ceiling -> standard (the ceiling
    is demonstrably lifted, not merely deferred), pending_retest_after_substrate -> false, and
    narrow_supports_flag -> false (the "remaining supports are degenerate single-resource" concern is
    resolved by this genuine multi-resource support). exp_conf 0.868 (genuine_exp_count 3),
    confirmed_established quadrant. MECH-229 is now a clean promote candidate (provisional -> active) for the
    engine to recommend next cycle; the prior substrate-degeneracy triage trail below is retained for history.
    2026-06-15 (follow-on, /queue-experiment, PROMOTION PATH CLARIFIED): the automated gate for a PROVISIONAL
    claim is provisional->stable (decision_criteria.v1.yaml: min_exp_conf 0.80 + min_experimental_entries 4 +
    max_conflict_ratio 0.20). MECH-229 clears exp_conf (0.868) and conflict but has only 3 genuine experimental
    entries (claim_evidence.v1.json genuine_exp_count=3), so the recommendations engine does NOT auto-surface a
    promotion this cycle and the prior "promote provisional->active next cycle" expectation will not fire on its
    own. User decision 2026-06-15 (AskUserQuestion: "queue a confirmatory run, then promote"): a confirmatory
    independent-seed replication V3-EXQ-514p (seeds 45/46/47 vs 514o's 42/43/44; claim_ids=[MECH-229]) was QUEUED
    + coordinator-ingested 2026-06-15 (dry-run smoke PASS: seed-45 C_WL 0.778, 9 scored WL steps, non_degenerate)
    as the 4th genuine experimental entry. ON 514p PASS/supports: MECH-229 clears provisional->stable and SHOULD
    be promoted -- apply the derive-only reconcile in that same pass (record_decision.py promote_to_stable +
    rebuild index; GAP-2/GAP-7 are already done and unaffected). Status stays PROVISIONAL this session (no
    premature promotion; the closure of goal_pipeline:GAP-2 + GAP-7 was on the 514o PASS and is independent of
    the MECH-229 status label). If 514p self-routes substrate_not_ready (contact_guard_unmet -- the guard is
    substrate-marginal, 514o passed 2/3) that is NOT a weakens and the promotion simply waits for a clean 4th entry.
    2026-06-11 (governance cycle #4, failure_autopsy_V3-EXQ-514m CONFIRMED): the V3-EXQ-514m MECH-229 per-claim weakens is reclassified non_contributory (measurement_test_design_defect) and remains scoring-excluded. C_WL wanting!=liking dissoc=0.0 on all 3 seeds (zero variance) is a VACUOUS FAIL: the DV reads the SD-014 residue VALENCE_WANTING/VALENCE_LIKING channels, but the run's config omits valence_liking_enabled/tonic_5ht_enabled/schema_wanting_enabled and the custom eval loop never writes them, so |w-l|>0.1 can never fire independent of behaviour. The contact guard (the run's only non-vacuity gate) verifies foraging but not channel-write, so the self-routed weakens rests on an unmet non-degeneracy assumption (vacuous-criterion pattern; cf. V3-EXQ-642/660a). MECH-229 stays provisional with no genuine multi-resource support; the substrate-faithful retest is V3-EXQ-514n -- the SD-057 object-bound wanting-target!=liking-target dissociation (MECH-346 most-wanted z_goal pointer; use_incentive_token_bank already on) with a same-statistic WL non-vacuity readiness gate, NOT the legacy residue-valence read. No demotion; no substrate_queue create/amend (instrumentation defect, SD-057 substrate landed 2026-06-04). MECH-230=supports is unaffected and sound.
    2026-06-03 (governance, failure_autopsy_V3-EXQ-514l): V3-EXQ-514l (the only genuine multi-resource wanting/liking test) re-tagged non_contributory + epistemic_category substrate_ceiling + pending_retest_after_substrate -- foraging-competence/benefit-contact ceiling, C6 wanting!=liking dissoc 0.0 on all 3 seeds (no reward-contact history to express in). narrow_supports_flag=true: remaining supports EXQ-074f/234/354 are degenerate single-resource, so this non_contributory does NOT resolve MECH-229 -- it stays provisional with no genuine multi-resource support, awaiting a fair test on the foraging-competence substrate.
    EXQ-074f PASS 4/4 seeds (2026-04-04): wanting_l1_fraction=0.96 vs liking_l1_fraction=0.43,
    delta=0.53 (z_world fallback seeding). Single-seed replication pending at time of run.
    EXQ-234 PASS 3/3 seeds (2026-04-05): independent replication. All 4 criteria met.
    Replicates EXQ-074f with seeds [1,2,3].
    EXQ-354 PASS 3/3 seeds (2026-04-12): definitive test with correct SD-015 ResourceEncoder
    wiring. wanting_l1_fraction 0.96/0.935/0.985 vs liking_l1_fraction 0.43/0.54/0.43.
    Dissociation delta 0.53/0.40/0.55. All >> 0.10 threshold. resource_rate floor met all seeds.
    Total: 10 seeds, 3 experiments, 0 failures. Promoted to active 2026-04-13.

    Governance triage 2026-06-01T07:15Z (active_conflict flag, conflict_ratio 0.2, 9 supports /
    1 weakens, source 2026-05-04_sd_049_temporal_community_schapiro2016): demoted active ->
    provisional + pending_retest_after_substrate=true. The schapiro2016 lit-pull is a
    substrate-honesty / methodology critique (NOT a mechanism refutation): on a single-resource
    substrate, wanting-target and liking-target are forced to coincide at the trajectory level
    -- the agent has nothing to approach except the one consummatory resource, so any apparent
    wanting/liking dissociation in approach trajectories is degenerate by construction. The
    structural argument (distributed similarity-preserving substrate per Schapiro/Turk-Browne/
    Norman/Botvinick 2015 Hippocampus) maps onto SD-049's multi-identity z_resource encoding +
    per-axis drive: only on that substrate can the agent approach target_A while target_B is
    what it would consume for satiation (the non-degenerate identity-distinct wanting test).
    The three prior PASSes (EXQ-074f / EXQ-234 / EXQ-354, all 2026-04-04 -> 2026-04-12) ran
    BEFORE SD-049 Phase 1 substrate landed (2026-05-03), confirmed by the EXQ-074f 'z_world
    fallback seeding' descriptor above and by all three predating the SD-049 implementation
    timeline. They are therefore single-resource substrate runs and the critique stands --
    the prior PASSes may be degenerate.

    Substrate status at triage time: SD-049 Phase 1 IMPLEMENTED 2026-05-03 (multi-identity
    resources + per-axis drive + curriculum hook on CausalGridWorld); Phase 2 hybrid encoder
    IMPLEMENTED 2026-05-04 (ResourceEncoder identity_head + identity-classifier head);
    Phase 3 consumer cascade IMPLEMENTED 2026-05-31 (all 7 SD-032 consumers per-axis-aware,
    including MECH-295 liking-bridge axis-matched routing -- the canonical case for MECH-229).
    Behavioural validation status: V3-EXQ-514 / 514b / 514f / 514j ALL non_contributory
    (substrate-readiness pattern, not load-bearing); V3-EXQ-514g IGW-20260601-011 is the
    Phase 2 successor and is currently BLOCKED on goal-pipeline GAP-2 substrate work (see
    substrate_queue.json MECH-262 retest_after_substrate target). The pre-registered
    non-degenerate test exists (substrate_queue SD-049 retest target: identity-distinct
    wanting >= 60% seeded episodes on 3-type ARM_2 substrate; ARM_2 - ARM_0 >= 0.4 monotone
    gradient; wanting != liking trajectory fraction near-zero in ARM_0/ARM_1 and >= 0.6 in
    ARM_2) -- but no PASS yet exists against it.

    Disposition: status -> provisional (not demote_to_candidate; the mechanism is not refuted,
    only the prior-evidence ground was substrate-degenerate); pending_retest_after_substrate=true
    with target = SD-049 Phase 2 behavioural validation PASS on multi-resource substrate (the
    514-lineage successor, expected to unblock once goal-pipeline GAP-2 lands). SD-049 added
    to depends_on to make the substrate dependency machine-readable. Conflict-ratio reading
    after triage: the 1 'weakens' lit entry is now correctly characterised as a methodology
    flag against the substrate-fitness of the prior PASS evidence, not a mechanism conflict;
    on the SD-049 substrate retest the lit-pull explicitly licenses the non-degenerate test
    as the load-bearing question (per record.json failure_signatures: 'If option A is chosen
    and ARM_2 fails wanting != liking despite passing identity-recovery, the failure mode is
    the absent similarity structure'). The 9 'supports' entries include lit_conf=0.848 from
    independent biology of wanting/liking (Berridge & Robinson 2003; Smith & Berridge
    cooperative-vs-dominant architecture) which remains intact.
    Routing: no /queue-experiment from this triage (the 514-lineage successor is already
    workset-tracked under IGW-20260601-011 / goal-pipeline GAP-2); no /failure-autopsy (no
    FAIL to autopsy -- the issue is methodological, not falsificational). claims.json rebuilt
    + indexer outputs refreshed; substrate_queue.json NOT touched (owned by concurrent
    MECH-341 amend session; SD-049 retest target already pre-registered there).

- id: MECH-230
  title: "E3 maintains a structured latent goal representation (positive attractor in z_goal sub-space, measurable as z_goal_norm > 0) distinct from harm avoidance, enabling hippocampal terrain navigation toward goal states."
  claim_type: mechanism_hypothesis
  subject: drive.goal_state_latent_structure
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-538a_2026-07-10
      as_of: 2026-07-10
      verdict: non_contributory/substrate_ceiling
  epistemic_category: standard
  pending_retest_after_substrate: false
  implementation_phase: v3
  split_from: MECH-112
  registered: "2026-04-13"
  depends_on:
    - SD-012
    - SD-015
    - SD-005
    - ARC-007
    - MECH-069
    - ARC-030
  location: docs/architecture/approach_avoidance_symmetry.md#mech-112
  notes: >
    Latent structure arm of the MECH-112 atomic split (2026-04-13). Tests that z_goal
    develops a structured positive attractor distinct from harm avoidance when homeostatic
    drive (SD-012) is active. Distinguished from MECH-229 (behavioral dissociation):
    MECH-230 requires z_goal to be populated and structured; MECH-229 only requires
    behavioral signal dissociation which can be measured via l1_fraction independently.
    Gate: SD-012 substrate is confirmed IMPLEMENTED (GoalConfig.drive_weight,
    GoalState.update() drive multiplication -- verified 2026-04-13). Prior experiment
    failures (EXQ-233, EXQ-238, EXQ-247, EXQ-328) were due to measurement bugs
    (pre-step EMA seeding), not missing substrate. EXQ-328b queued (2026-04-13) as full
    real run of the correctly-instrumented script.
    z_goal latent structure is required for hippocampal goal-terrain navigation
    (VALENCE_WANTING residue field seeding, goal-directed approach). Without z_goal
    structure, MECH-229 behavioral signal does not translate to navigation lift (EXQ-326 C3 FAIL).
  implementation_note: >
    2026-06-04 (SD-057 L4, MECH-346): the z_goal SEED SOURCE is amended. When
    GoalConfig.use_incentive_token_bank is set, z_goal is written FROM the
    most-wanted object's stored identity embedding (argmax over per-object
    base_value x per-axis drive) rather than the raw last-contacted z_resource.
    The structured-attractor assertion (z_goal_norm > 0, distinct from harm)
    is UNCHANGED; only the source of the seed changes, making the attractor
    object-bound. Status/flags unchanged (governance follow-on gated on the
    SD-057 forced-contact validation). See SD-057, MECH-346.
  evidence_quality_note: |
    2026-07-10 (governance, failure_autopsy_V3-EXQ-538a + GOV-CEIL-1 review; user-adjudicated): epistemic_category substrate_ceiling -> standard. The 2026-06-11 deferred epistemic_category review is now due: 514m already lifted the ceiling with a genuine non-degenerate positive on the scaffolded substrate (genuine_exp supports=2, weakens=0, exp_conf 0.751, confirmed_established). V3-EXQ-538a re-ran the pre-enrichment 514f COARSE config (not the scaffolded onboarding substrate), so its FAIL does NOT re-establish the ceiling -- tagged non_contributory. GOV-CEIL-1 ceiling-exhaustion does NOT apply: the audit flagged MECH-230 on ceiling-autopsy hit-count (3) alone, but the rule exempts claims with a positive discrimination on a richer substrate, and 514m is exactly that. Status stays provisional (promotion still suppressed by the implementation_phase=v3 hold_pending_v3_substrate gate). Removed from the substrate_ceiling set so it no longer re-flags as exhausted.
    2026-06-11 (governance cycle #3): pending_retest_after_substrate CLEARED. V3-EXQ-514m (the SD-049 Phase-2 behavioural curriculum successor on the scaffolded_sd054_onboarding substrate that lifted the foraging-competence/benefit-contact ceiling -- the substrate this retest awaited) SUPPORTS MECH-230 on its proper non-degenerate test: C_ID identity-recovery PASS (probe acc 0.926, n=3455), C_ANOVA per-axis drive PASS (F_max 1096 >> F_crit 4.605), mean goal_resource_r=0.953, z_goal forms ecologically via the curriculum (2/3 seeds pass the P2 contact + z_goal_norm>0.4 guard). This is the first genuine non-degenerate support for the structured-z_goal-attractor assertion; the substrate_ceiling basis (substrate too coarse to populate z_goal) is now lifted by the onboarding substrate. Promotion still suppressed by the implementation_phase=v3 gate (hold_pending_v3_substrate); epistemic_category review deferred to a future cycle. Note: 514m's overall outcome is FAIL driven by the MECH-229 wanting!=liking arm (flagged for /failure-autopsy, scoring-excluded); the MECH-230 arm is a clean, independently-counted supports entry.
    2026-06-03 (governance, failure_autopsy_V3-EXQ-632 + V3-EXQ-514l): both FAILs re-tagged non_contributory + epistemic_category substrate_ceiling + pending_retest_after_substrate (foraging-competence/benefit-contact ceiling). 632 is load-bearing: negative control C3 (ablated absent) PASS 3/3 and the one foraging seed (42, 6 contacts) is a CLEAN positive -- z_goal_norm=3.0115 at contact, persists byte-identically to t50, absent under ablation (0.0) = a structured z_goal attractor distinct from harm avoidance exactly as MECH-230 asserts; aggregate FAIL driven entirely by 2/3 seeds (7,13) making zero resource contact. CORRECTS the prior recorded gap: the blocker is foraging-competence upstream, NOT the E1-frontal->hippocampal goal-projection wiring. narrow_supports_flag=true (1 PASS remains; non_contributory does not resolve). Substrate routing: amend scaffolded_sd054_onboarding (MECH-230 added to unblocks_claims + 632 failure_record).
    EXQ-328a dry run (2026-04-12): z_goal_norm=0.0 in all conditions (non_contributory --
    dry run too short for resource contacts; 3 episodes x 120 steps insufficient).
    SD-012 substrate confirmed IMPLEMENTED. All prior failures (EXQ-233/238/247/328) due
    to pre-step EMA seeding measurement bug, not missing substrate.
    EXQ-328b FAIL (2026-04-13): reclassified non_contributory (2026-04-17). The FAIL is a
    substrate gap, not scientific falsification. MECH-230's claim requires z_goal to drive
    hippocampal terrain navigation (VALENCE_WANTING residue field seeding, goal-directed
    approach lift). That wiring -- from the E1 frontal goal representation into the
    hippocampal navigation system -- is not yet implemented in the substrate. z_goal_norm=0
    cannot distinguish 'goal not structured' from 'goal not wired to hippocampal system'.
    No genuine evidential run is possible until the E1-frontal-to-hippocampal goal
    projection is implemented. Hold at candidate pending substrate.
    Governance decision (2026-04-17): hold at candidate -- substrate gap. EXQ-328b
    non_contributory. No new experiments until E1-frontal->hippocampal navigation
    wiring is implemented.
    Governance decision (2026-04-18): promoted candidate -> provisional. conf=0.814,
    conflict=0, 4 supports / 0 weakens / 1 mixed after EXQ-328a/328b reclassifications.
    Substrate gap (E1-frontal -> hippocampal goal projection) still blocks full
    validation -- promotion reflects current evidence strength + zero conflict, not
    closure of the substrate question.

- id: Q-038
  title: "Is temporal-depth verisimilitude D_V explicitly represented as a signal or does it emerge from distributed dynamics without local representation?"
  claim_type: open_question
  subject: perception.dv_representational_status
  polarity: open
  status: open
  live_status:
    reading: open/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Build a discriminative probe pair on the TCL substrate (ARC-053): an
    explicit-representation arm where temporal-depth verisimilitude D_V is read
    out as a localised signal versus an emergent arm where D_V is only inferable
    from distributed dynamics. Lesion / perturb the candidate local D_V locus and
    measure whether D_V-dependent behaviour degrades. ANSWER "explicitly
    represented" if a localised perturbation selectively abolishes D_V-graded
    behaviour while leaving other temporal computations intact (Guan 2024 /
    Gherman 2018 localised-signal analog). ANSWER "emergent / no local
    representation" if no single locus carries D_V and the signal can only be
    decoded from population dynamics that survive any local lesion (Pilkiw 2018
    distributed-code analog). HARD PRECONDITION: the TCL substrate (ARC-053) and
    the ARC-055 control-plane parameter map must exist -- neither is in V3
    (implementation_phase=v4). Without the TCL there is no D_V to localise, so the
    probe is undefined, not merely vacuous. Wait for the planned upstream
    substrate: ARC-053 (TCL). Lit leans ~2:1 toward explicit (A) but the
    architectural choice is consequential for ARC-055's explicit-availability
    requirement, so hold for the substrate rather than accepting the lit lean.
  epistemic_category: substrate_conditional
  implementation_phase: v4
  depends_on:
    - INV-068
    - ARC-053
  evidence_quality_note: |
    Lit-pull 2026-05-02 (ree-weekend-lit session) added 3 entries: Pilkiw 2018
    distributed tonic+phasic temporal codes (supports option B), Guan 2024 abstract
    interval-invariant subsecond structure (supports option A), Gherman 2018 VMPFC
    localised early confidence signal (supports option A by analogy). lit_conf 0.795;
    quadrant=plausible_unproven, exp_conf=0.

    EPISTEMIC-CATEGORY FLAG (2026-05-02, Phase 3 Q-walk): substrate-conditional (V4-
    bound). implementation_phase=v4 already set on the claim -- Q-038 depends on
    ARC-053 (TCL substrate) and ARC-055 (control-plane parameter map), neither of
    which is in V3. The narrow_open_question recommendation is a category error: the
    question is structurally well-formed and the lit literature is starting to converge
    (Guan/Gherman lean A, Pilkiw leans B), but experimental dissociation requires the
    TCL substrate to exist. Two paths: (a) accept the lit-leaning option-A reading
    provisionally pending substrate; (b) hold as open until ARC-053 lands then queue
    a discriminative pair (explicit-vs-emergent D_V probe). Recommend (b) since lit
    is split (~2:1 toward A) and the architectural choice is consequential for
    ARC-055's "explicit availability requirement" implementation strategy.
  registered: "2026-04-12"
  location: evidence/planning/thought_intake_2026-04-09_verisimilitude_ethics.md

- id: Q-039
  title: "Which neuromodulatory or control-plane variables directly regulate temporal integration window and phase alignment sensitivity in the TCL?"
  claim_type: open_question
  subject: architecture.tcl_control_parameters
  polarity: open
  status: open
  live_status:
    reading: open/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    On the TCL substrate, run a pharmacology-analog double dissociation mapping
    control-plane parameters to neuromodulator analogs: vary an ACh-analog gain
    and an NA-analog gain independently and measure (i)
    temporal-integration-window width and (ii) phase-alignment sensitivity in the
    TCL. ANSWER (per the Kumagai 2023 VNS dissociation) "ACh gates integration
    window, NA gates phase alignment" if the ACh-analog manipulation selectively
    shifts the integration window (via gamma/beta gating) with phase-alignment
    sensitivity unchanged, AND the NA-analog manipulation selectively shifts
    phase-alignment (via theta organisation) with the window unchanged -- a clean
    double dissociation. ANSWER "shared / cross-coupled control" if the two
    manipulations move the same metric or fail to dissociate. Also resolve the
    kappa_commit and beta_j mappings by the analogous selective-manipulation test.
    HARD PRECONDITION: the TCL substrate (ARC-053) plus the ARC-055 parameter set
    (alpha_S, alpha_A, tau_E2, H, kappa_commit, g_S) must exist and the
    integration-window / phase-alignment metrics must be live and responsive --
    none are in V3 (implementation_phase=v4), so there is no TCL operation to
    modulate. Wait for the planned upstream substrate: ARC-053; the lit gives a
    strong starting hypothesis (ACh->window, NA->phase) but cannot be REE-tested
    until the TCL lands.
  epistemic_category: substrate_conditional
  implementation_phase: v4
  depends_on:
    - ARC-053
    - ARC-055
    - INV-067
    - INV-068
  notes: >
    ARC-055 identifies control-plane parameters (alpha_S, alpha_A, tau_E2, H, kappa_commit,
    g_S) but does not specify which neuromodulatory systems implement them or which
    directly regulate TCL operation vs V(t) computation.
    Open aspects: (1) Does acetylcholine gate temporal integration window? (2) Does
    noradrenaline regulate phase alignment sensitivity? (3) Does dopamine modulate
    beta_j (similarity weight in D_V ethical objective)? (4) Which neuromodulator
    corresponds to kappa_commit (commit threshold)?
    TCL control vs V(t) control distinction: some parameters regulate whether coupling
    is possible (TCL -- phase alignment, timing windows), others regulate which signals
    are allowed to couple (V(t) -- precision weights, channel gating).
    Resolution would ground ARC-053/ARC-055 in existing neuromodulatory pharmacology
    and enable testable pharmacological predictions.
  evidence_quality_note: |
    Lit-pull 2026-05-02 (ree-weekend-lit session) added 3 entries: Fan 2020 Cell
    ACh-driven L1 winner-take-all + temporal high-pass filter (supports), Xiang 2023
    Front Cell Neurosci LC NA neurons phase-lock to infra-slow rhythms organising gamma
    (supports), Kumagai 2023 Brain Stim VNS double-dissociation -- ACh selectively
    modulates gamma/beta, NA selectively modulates theta (supports -- cleanest
    dissociation evidence on the table). lit_conf 0.840; quadrant=plausible_unproven,
    exp_conf=0.

    EPISTEMIC-CATEGORY FLAG (2026-05-02, Phase 3 Q-walk): substrate-conditional (V4-
    bound). implementation_phase=v4 already set; depends on ARC-053 (TCL substrate)
    and ARC-055 (control-plane parameter map). The lit-pull above gives a strong
    starting hypothesis (ACh -> integration window via gamma/beta gating; NA -> phase
    alignment via theta organisation; Kumagai 2023 VNS double-dissociation is the
    cleanest empirical handle), but experimental validation in REE requires TCL
    substrate to exist. Recommend: hold as open until ARC-053 lands; when it does,
    queue pharmacological pair (e.g., ACh-modulator vs control on TCL temporal
    integration metric) using the Kumagai dissociation as primary mapping. The
    narrow_open_question recommendation is misleading: this question's answer is
    becoming clear in the lit but cannot be REE-tested yet.
  registered: "2026-04-12"
  location: evidence/planning/thought_intake_2026-04-09_verisimilitude_ethics.md

- id: Q-040
  evidence_quality_note: |
    [update_z_goal_typeerror_swallowed] EXQ-471/475/483/483a/483b/490/490b/490c/490e/490f/524 cohort evidence is contaminated and superseded as of 2026-05-07 due to a script-side TypeError in agent.update_z_goal that was silently swallowed by a bare except, leaving GoalState.is_active() always False. Additional cohort-wide issues found: 2x agent.sense() per warmup step, agent.update_residue() never called (residue field empty / running_variance pinned), dACC bias diagnostic reading wrong tensor. Awaiting StepHarness migration + corrected re-runs (EXQ-475a / EXQ-483c / EXQ-490g / EXQ-471a / EXQ-524a). See REE_Working session 2026-05-07.

    [2026-05-08 governance]: EXQ-490f reclassified weakens -> non_contributory (manifest patched). Cascade test ran on pre-MECH-307 conjunction architecture; the four-gap wiring fault confounds the result. Re-test required after MECH-307 substrate lands. Cross-witness: EXQ-536b force-arm probe (z_goal_inject=0.3, action-time fraction=1.0) also yielded approach_commit_rate=0.0, consistent with downstream-inert at the same architectural layer.

    [2026-05-08 governance: narrow_open_question applied]. The broad question decomposes into three machine-testable sub-questions, each of which can be falsified by an experiment on the StepHarness-corrected substrate after MECH-307 conjunction architecture lands: (Q-040.a) Does MECH-269b (V_s gating E1/E2 forward predictions in addition to the hippocampal proposer anchor) restore non-zero approach_commit_rate when SD-032b dACC adaptive-control is ON? Test = factorial 2x2 of {MECH-269b OFF/ON} x {SD-032b OFF/ON} on EXQ-483a-conditions retest under StepHarness; PASS = MECH-269b=ON x SD-032b=ON arm produces approach_commit_rate > 0 AND non-zero dACC-driven score-bias contribution; non-MECH-269b-ON arms remain at zero. (Q-040.b) If Q-040.a FAILs, is the upstream blocker MECH-295 liking-stream wiring? Test = MECH-295 force-arm probe (already evidence-of-record at EXQ-536b: action-time injection fraction=1.0, approach_commit_rate=0.0) plus a corrected EXQ-490g re-run under StepHarness + MECH-307 substrate. (Q-040.c) Is dACC behavioural-adjustment magnitude proportional to V_s precision-modulated PE under MECH-269b=ON? Test = EXQ-475a-conditions retest measuring dACC weight delta as a function of |precision-weighted forward-PE|; PASS = nonzero correlation in MECH-269b=ON arm and ~zero in OFF arm. Resolution path: Q-040.a is the load-bearing arbitration; Q-040.b is the alternative-hypothesis isolator; Q-040.c is the mechanism-quantification follow-on. Empirical answer is gated on (i) StepHarness migration of EXQ-471a/475a/483c/490g/524a (cohort scheduled), (ii) MECH-307 four-gap conjunction substrate landing (EXQ-540 discriminative validation queued), and (iii) MECH-269b V_s-rollout-gate landing (per substrate_queue MECH-269b-followup-A). No claim-status change; question stays open with these sub-questions as the testable scaffolding.

    2026-04-28T22Z governance: narrow_open_question note. Lit-pull 2026-04-27 added 4 entries (Hayden 2011, Treuting 2025, Brown-Braver 2007, Thonnard 2021) on V_s -> E1/E2 forward- prediction generalisation; lit_conf 0.84. Lit supports MECH-269b necessity but does not license sufficiency promotion. Combined with V3-EXQ-490b FAIL today (Q-040a effectively PASS at threshold- overridden scale, Q-040b FAILs in current substrate), the architectural reading firms: MECH-269b necessary but not sufficient; MECH-295 liking-bridge is the dominant remaining blocker; successor EXQ should test both ON together. Aggregator- floor flag continues.

    [2026-08-01 governance, V3-EXQ-475b, confirmed failure_autopsy_V3-EXQ-475b_2026-08-01]:
    first-ever queuing of Q-040.c (named since 2026-05-08, never run until now). FAIL, driver
    bug not a substrate finding: P1 (MECH-269b V_s-gating engaged) PASSES cleanly (2/3 seeds),
    but P2 (dACC engagement) FAILS completely (0/6 runs) -- z_harm_a stayed structurally None for
    the entire run because the driver's `_build_arm_config` call omitted
    `enable_affective_harm_stream=True`, the exact documented footgun `build_config`'s own
    docstring names (the same gap diagnosed for V3-EXQ-620/625 on 2026-06-01). evidence_direction:
    non_contributory -- this run does not weigh on Q-040c either way; the upstream V_s-gating
    substrate works exactly as intended, so this is not a substrate ceiling. No claim-status
    change. Route: /queue-experiment same-question letter V3-EXQ-475c with the one-line fix plus
    a pre-flight smoke assertion (n_dacc_fires > 0) before committing to the full 6-run design.

    [2026-08-02 governance, V3-EXQ-862, confirmed failure_autopsy_V3-EXQ-862_2026-08-02, 475c config-fix requeue of 475b]: the first diagnosed defect (enable_affective_harm_stream never passed to build_config) is fixed and confirmed (n_dacc_fires=451 on seed 42, was 0 in 475b). But dacc_bias_nonzero_steps=0 in EVERY seed/arm cell -- a SECOND, distinct config-omission bug: DACCtoE3Adapter.forward() computes weight = config.dacc_weight * drive_gain, and dacc_weight is never set by the driver (class default 0.0), so scaled bias is unconditionally zero regardless of dACC firing or PE. Not a substrate ceiling, not evidence on Q-040c's actual question (whether dACC bias magnitude tracks precision-weighted PE under V_s gating) -- the manipulation never had a chance to move anything. non_contributory. Route: same-question re-queue with dacc_weight (and at least one interaction/foraging/suppression weight) set nonzero, and extend the preflight gate to assert bias MAGNITUDE nonzero, not merely definedness.

    [2026-08-03 governance, V3-EXQ-862a, confirmed failure_autopsy_dacc-cluster-862a-870a_2026-08-03,
    862's config-fix requeue]: both prior config bugs fixed (475b's z_harm_a wiring, 862's dacc_weight=0
    consumer gain) and both preconditions pass, but the ON/OFF manipulation itself still never
    engaged -- code-verified vs_gate_held_e1/e2_z_harm_a=0 across all 3 seeds; the borrowed 490b
    threshold (0.85, applied globally) never crosses for z_harm_a's own V_s. Third consecutive
    non-engagement finding in this lineage, each a different bug. non_contributory/
    measurement_test_design_defect. Route: /queue-experiment with a z_harm_a-calibrated threshold
    (not the borrowed 490b one) and a preflight assertion that the gate actually opens at least once.
  title: "Is V_s generalisation to E1/E2 forward predictions (MECH-269b) the missing link that unblocks SD-032b dACC behavioural-revision (i.e. the wired-but-inert failure mode of EXQ-483/483a)?"
  claim_type: open_question
  subject: control.vs_generalisation_unblocks_dacc
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-040@2026-05-08T22:34:09Z"
      as_of: 2026-05-08
      verdict: narrow_open_question/applied
  what_would_answer: |
    A factorial V3 experiment on the StepHarness-corrected, MECH-307-conjunction
    substrate: {MECH-269b V_s->E1/E2-rollout gate OFF/ON} x {SD-032b dACC
    adaptive-control OFF/ON}, with broadcast override + dACC + drive-modulation ON
    in all arms and the MECH-295 liking-bridge wired (now landed, V3-EXQ-493
    PASS), measuring approach_commit_rate and dACC-driven score-bias magnitude.
    ANSWER "yes -- MECH-269b is the missing link" if and only if the MECH-269b=ON
    x SD-032b=ON arm produces approach_commit_rate > 0 AND non-zero dACC
    behavioural-adjustment magnitude, WHILE the MECH-269b=OFF arms remain pinned
    at approach_commit_rate=0 -- a clean dissociation showing V_s generalisation
    to the forward predictions is what releases the wired-but-inert freeze. ANSWER
    "no -- necessary but not sufficient" if BOTH arms remain at
    approach_commit_rate=0 even with MECH-269b=ON; that points the dominant
    remaining blocker upstream at MECH-295 liking-bridge / cue projection (the
    reading the prior cohort already favoured: EXQ-490b had C1 gate-firing PASS
    but C2/C3 FAIL, and the EXQ-536b force-arm probe at fraction=1.0 still gave
    approach_commit_rate=0). SHARPER follow-on (Q-040c): in the MECH-269b=ON arm,
    dACC weight-delta should correlate with |precision-weighted forward-PE| and be
    ~zero in the OFF arm -- a non-zero correlation confirms the mechanism, not
    just the endpoint. HARD PRECONDITIONS: (i) the four-gap StepHarness wiring
    fault must be corrected (the bare-except swallowed update_z_goal TypeError
    that left GoalState.is_active always False contaminated the EXQ-471/483/490
    cohort); (ii) the symmetric-V_s gate must actually fire (c1_on_total_held > 0
    on >=2/3 seeds) or the behavioural legs are uninterpretable; (iii) the
    V_s-monostrategy lock-in must be cleared so any arm can express variation.
    Absent these the run self-routes substrate-not-ready, not a verdict.
  implementation_phase: v3
  depends_on:
    - MECH-269   # parent V_s claim (hippocampal proposer anchor selection)
    - MECH-269b  # candidate extension under test
    - SD-032b    # dACC adaptive-control consumer
    - SD-037     # broadcast override regulator (already implemented)
  registered_utc: 2026-04-26
  notes: >
    Posed in response to EXQ-483 FAIL diagnostic (override fires, PAG releases up,
    approach_commit=0 across all arms). The architectural prediction: with V_s
    gating only the hippocampal proposer (current MECH-269 scope) but not the
    E1/E2 forward predictions, E2 keeps grounding harm-rollouts on stale streams,
    dACC sees a consistent-looking precision-weighted PE, no policy revision
    fires, and freeze persists. If MECH-269b is wired (V_s also gates E1/E2
    rollouts), does dACC adaptive-control output (SD-032b) produce non-zero
    behavioural-adjustment magnitude on an EXQ-395 / EXQ-483-conditions retest?
    If yes, MECH-269b is the missing link. If no, the bottleneck is upstream
    (liking stream / cue projection per MECH-295) and MECH-269b is necessary
    but not sufficient.
    Resolution: V3 experiment factorial -- MECH-269b OFF/ON x baseline-config
    (use_broadcast_override + use_dacc + use_drive_modulation all ON in both arms),
    measure approach_commit and dACC behavioural-adjustment magnitude. PASS = ON
    arm produces non-zero approach_commit AND non-zero dACC-driven score-bias
    contribution. FAIL = both arms remain at zero approach_commit, indicating
    upstream blocker (point to MECH-295).
  status_note: |
    Narrowed in /governance 2026-04-27 after V3-EXQ-490 sub-diagnostic FAILs
    (c1 gate-firing precondition itself FAILed across both runs and both arms,
    so c2/c3 cannot be interpreted; both V3-EXQ-490 manifests reclassified
    non_contributory). The originally-broad question splits into two narrower
    sub-questions, both gated on /diagnose-errors clearing the c1 gate-firing
    precondition for V3-EXQ-490:

    Q-040a (precondition / substrate-readiness): Does the symmetric-V_s gate
      fire at all under the V3-EXQ-490 substrate? PASS = c1_on_total_held > 0
      across at least 2/3 seeds in the ON_ON arm. Resolution drives whether
      MECH-269b is interrogable at smoke-scale at all, or requires
      longer-warmup / different per-stream V_s thresholds / a per-stream
      override hook that is not yet wired.

    Q-040b (behavioural sufficiency, conditional on Q-040a PASS): Conditional
      on the gate firing, does MECH-269b produce non-zero approach_commit AND
      non-zero dACC behavioural-adjustment magnitude on EXQ-395 / EXQ-483
      conditions? Same factorial as the original Q-040 resolution; PASS =
      MECH-269b is the missing link; FAIL with Q-040a PASS = MECH-269b is
      necessary but not sufficient (point at MECH-295 liking-bridge as
      dominant blocker).

    Q-040 itself remains open at lower confidence; Q-040a and Q-040b can be
    promoted to candidate-mechanism / candidate-question status if the
    architectural reading firms up after /diagnose-errors lands.

    2026-04-28T22Z governance update: V3-EXQ-490b (smoke-threshold-
    override path queued by /diagnose-errors 2026-04-27T14:55Z) ran and
    FAILed with evidence_direction=inconclusive. C1 gate-firing
    precondition NOW PASSes (substrate-readiness diagnostic from the
    threshold override worked), but C2 (non-zero approach_commit) and
    C3 (non-zero dACC score-bias) BOTH FAIL across all seeds. This is
    the "MECH-269b necessary but not sufficient" branch -- points
    directly at MECH-295 liking-bridge as the dominant remaining
    blocker. Notably MECH-295 is now ALSO landed (V3-EXQ-493 PASS this
    cycle), so a successor experiment that runs both ON together
    (broadcast override + dACC + drive modulation + V_s gate + liking
    bridge) is the natural next gate for Q-040b. Q-040a effectively
    answered (PASS at threshold-overridden smoke scale).
  location: docs/architecture/v_s_invalidation_runtime.md

- id: Q-041
  title: "Does REE require a unified meta-level threshold supervisor that adapts pass/fail and commit/release thresholds across substrates from system-wide instability metrics, or do the existing scattered adaptive loci (ARC-016 dynamic precision, SD-032c AIC interoceptive baseline EMA, SD-032d PCC stability scalar, SD-032e pACC drive bias) produce coherent meta-stability under chronic load without a supervisor?"
  claim_type: open_question
  subject: control.unified_threshold_supervisor
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-041@2026-05-08T16:58:44Z"
      as_of: 2026-05-08
      verdict: hold_pending_v3_substrate/applied
  what_would_answer: |
    A V3 chronic-load diagnostic that drives the agent at sustained high load
    (e.g. drive_level~0.9 for ~1000 steps) and measures cross-substrate threshold
    coherence: track the effective E3 commit threshold, the effective beta-gate
    release threshold, the AIC (SD-032c) switch threshold, and the dACC (SD-032b)
    pe_cap normaliser, and test whether they move along a shared low-dimensional
    trajectory or independently. ANSWER "no separate supervisor needed -- the
    scattered loci self-coordinate" (PASS-coherent) if the four threshold
    time-series collapse onto a shared low-dimensional manifold (high
    cross-substrate correlation / dominant single principal component consistent
    with a hidden joint instability signal) under chronic load, i.e. ARC-016 +
    SD-032c/d/e produce coherent meta-stability without aggregation. ANSWER "a
    unified supervisor is required" (PASS-independent) if the thresholds drift
    independently (low cross-correlation, no shared component, divergent setpoints
    under sustained load) -- this licenses promotion of an SD cluster: a
    cross-substrate volatility tracker (Behrens-2007-style joint PE estimator
    emitting a system-level learning-rate), a sleep-mediated MECH-204 writeback
    path (Tononi-Cirelli SHY recalibration -- currently MECH-204 captures the
    precision zero-point at REM entry but discards it), and a joint setpoint-drift
    mechanism beyond SD-032e's drive_level-only scope. HARD PRECONDITION: the
    scattered loci must each be live and non-degenerate before their coherence can
    be read -- ARC-016, SD-032b/c are currently non_contributory under the
    V_s-monostrategy lock-in (MECH-269 unlanded), so the threshold series are
    pinned and the coherence test is vacuous until the substrate unlocks
    alternative operating modes. The V3-vs-V4 placement (a slow EMA-of-EMAs
    supervisor is V3-feasible; the full sleep-writeback supervisor needs V4
    sleep-substrate enrichment MECH-285/286/INV-049) is itself part of what
    PASS-independent resolves.
  implementation_phase: v3
  claim_level: architectural
  registered_utc: 2026-05-07
  depends_on:
    - ARC-016         # E3 dynamic precision (per-step EMA, internal to E3)
    - SD-032c         # AIC-analog interoceptive baseline EMA (drive-dependent salience)
    - SD-032d         # PCC-analog stability scalar (modulates SD-032a switch threshold)
    - SD-032e         # pACC drive bias EMA (slow setpoint drift, ~347-step half-life)
    - MECH-040        # safety baseline / volatility (provisional, never validated)
    - MECH-204        # serotonergic REM gate zero-point (GAP-1 writeback IMPLEMENTED 2026-05-08; 541c PASS)
    - MECH-258        # precision-weighted PE (the variable a Behrens-style volatility tracker would integrate)
    - INV-049         # sleep necessity (the offline phase a recalibration writeback would land in)
  notes: >
    The brain dynamically manages thresholds for commitment, release,
    surprise, urgency, and pass/fail across multiple timescales. REE
    has accumulated several adaptive-threshold mechanisms but each is
    scoped to a single substrate and operates at one timescale.
    Timescale spread today is ~10 steps (ARC-016 EMA) up to ~347 steps
    (SD-032e pACC drive bias) -- nothing operates at the cross-substrate
    sleep-bout / multi-day timescale at which neuromodulator setpoints
    biologically recalibrate.

    Q-041 asks whether this scatter is sufficient or whether REE needs a
    distinct meta-level supervisor. Three architectural gaps motivate
    the question:

    1. Cross-substrate volatility tracking. Each adaptive locus tracks
       variance internal to its own signal; nothing aggregates across
       loci. A Behrens et al 2007 (Nat Neurosci) style volatility
       estimator would read multiple PE streams jointly and emit a
       system-level learning-rate signal that downstream thresholds
       consume.

    2. Sleep-mediated writeback. MECH-204 captures a precision zero-
       point at REM entry but the writeback path that uses it to
       recalibrate waking thresholds is unimplemented. Tononi & Cirelli
       2014 (Neuron, Synaptic Homeostasis Hypothesis) frame sleep as
       global recalibration; REE captures the snapshot and discards it.

    3. Setpoint drift under chronic load. SD-032e is the closest
       existing analogue but writes only to drive_level. Chronic-stress
       5-HT/DA setpoint shifts in biology recalibrate many downstream
       thresholds simultaneously (commit, release, surprise) -- REE
       has no joint mechanism.

    Resolution path. A V3 diagnostic that subjects the agent to
    sustained chronic load (e.g., drive_level=0.9 for ~1000 steps) and
    measures cross-substrate threshold coherence: do effective commit
    threshold, effective beta-gate release threshold, AIC switch
    threshold, and dACC pe_cap normalisation move along a shared
    low-dimensional trajectory (consistent with a hidden joint signal)
    or independently (consistent with no supervisor)? PASS-coherent
    licenses Q-041 closure as 'no separate supervisor needed'. PASS-
    independent licenses promotion to a SD cluster (supervisor + sleep
    writeback MECH + volatility-tracker MECH).

    Why a question, not a substrate. The simplest threshold supervisor
    is a slow EMA of EMAs and could land in V3; the full sleep-mediated
    writeback supervisor requires MECH-204 to act on its snapshot,
    which depends on sleep substrate enrichment (MECH-285, MECH-286,
    INV-049 full implementation) that is V4-bound. The V3-vs-V4
    placement is itself part of what the question resolves.

    Anchor literature (registration-time, pre-lit-pull):
      Behrens et al 2007 Nat Neurosci 10:1214 (dACC volatility tracking
        adjusts learning rate);
      Adams, Shipp & Friston 2013 (precision-weighted PE as the
        canonical adaptive-control variable);
      Tononi & Cirelli 2014 Neuron 81:12 (Synaptic Homeostasis -- sleep
        as global recalibration).

    A targeted lit-pull on "adaptive learning rate dACC volatility" +
    "neuromodulator setpoint chronic stress recalibration" + "synaptic
    homeostasis SHY recalibration" is the natural Q-041 successor;
    staged as a follow-on planning item (proposal_type: literature).

    V3-pending. Diagnostic gated on V_s-monostrategy substrate gap
    clearing (MECH-269 V_s landing) -- many adaptive loci including
    SD-032b/c and ARC-016 currently show non_contributory behaviour
    because the V_s lock-in dominates threshold-dependent dynamics.
    Re-poseable once the substrate unlocks alternative operating modes.
  evidence_quality_note: >
    Registered as research-anchor question 2026-05-07 from observation
    that REE has scattered adaptive-threshold mechanisms (ARC-016,
    SD-032c/d/e, MECH-040, MECH-204) without a unifying meta-level
    coordinator. No experimental evidence yet. Survey of existing
    scattered mechanisms in the linked architecture doc.

    Successor cluster (NOT registered; license is conditional on Q-041
    resolution): a supervisor SD covering cross-substrate volatility
    aggregation and a sleep-writeback MECH covering MECH-204 -> waking
    threshold update path. Default toward registering only the Q
    until evidence directs.

    [2026-05-08 governance]: hold_pending_v3_substrate. Substrate gates: SD-032c (AIC analog, candidate v3_pending), SD-032d (PCC analog, candidate v3_pending), MECH-258 (precision-weighted pain PE, candidate v3_pending), MECH-204 (REM serotonergic precision update, candidate v3_pending). The unified-supervisor question becomes empirically tractable only after the scattered loci are themselves wired; sleep substrate work (MECH-204) is the acknowledged top-priority unblocker per 2026-05-08 governance discussion.
  location: docs/architecture/threshold_supervisor_survey.md

- id: Q-042
  title: "Should the running-variance / dynamic-precision update fire at action-selection time (early, control-plane-coupled), at outcome-integration time (late, residue-coupled, current location), or at both? Equivalently: is the StepHarness contract that requires experiment loops to call agent.update_residue() each step sufficient, or does ARC-016 precision need a call site that is structurally guaranteed to fire whenever the agent is selecting actions?"
  claim_type: open_question
  subject: control.precision_update_call_site
  evidence_quality_note: |
    [2026-05-08 governance]: candidate_resolved with hold_pending_v3_substrate. Architectural answer is settled: dual-read at outcome AND at action selection (lit-pull 2026-05-08 confirmed; clinically and biologically obvious -- consequential effect of decisions cannot be processed for ongoing experimental behaviour without both). Empirical resolution gated on (a) MECH-258 substrate implementation, (b) StepHarness-cohort retest of EXQ-475a/483c/490g/471a/524a -- the StepHarness substrate landed today; retest cohort to be authored under /queue-experiment with careful wiring checks per 2026-05-08 governance. Sleep substrate (MECH-204) shares the gate.
  polarity: open
  status: candidate_resolved
  live_status:
    reading: candidate_resolved
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-042@2026-05-08T16:58:44Z"
      as_of: 2026-05-08
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  claim_level: architectural
  registered_utc: 2026-05-08
  depends_on:
    - ARC-016     # E3 dynamic precision (the signal whose update site is in question)
    - MECH-090    # BG commit gate (downstream consumer; commits when rv < threshold)
    - MECH-258    # precision-weighted PE in dACC (downstream consumer)
    - SD-006      # asynchronous multi-rate loop execution (the timing decision lives here)
    - INV-049     # sleep / offline recalibration (the slow-timescale arm)
  notes: >
    Posed in response to the 2026-05-07 EXQ-514d/514e/524/530/536
    cluster, where five experiment scripts skipped agent.update_residue()
    and the BG commit gate was silently dormant for entire runs:
    _running_variance pinned at precision_init=0.5; current_precision =
    1/(0.5+1e-6) = 1.999996 constant; EXQ-530 manifest's
    "ARC-016 weakens" classification was not actually a test of ARC-016
    because the precision signal was never a variable.

    The same week, a separate cohort fix surfaced 11 scripts with a
    related update_z_goal() bug (EXQ-471/475/483/483a/483b/490/490b/
    490c/490e/490f/524). The shared lesson: when a control-plane state
    update is delegated to experiment loops, the loops can silently
    omit it without crashing, and a whole cohort of evidence becomes
    contaminated. The 2026-05-07 fix landed a StepHarness (canonical
    sense / update_z_goal / update_residue sequence, kwargs-only call
    shape, contract test) -- this is Option A in code form.

    Q-042 asks whether StepHarness alone is sufficient, OR whether the
    precision update specifically should be hoisted upstream so it
    cannot be silently dormant even under a non-StepHarness loop.

    Two implementation options:

    A) CONTRACT-ENFORCEMENT (current direction). Keep the rv update
       inside agent.update_residue() / E3.post_action_update().
       Make calling it mandatory via StepHarness; gate non-conforming
       loops at the regression-suite level. Reading: precision is
       computed on observed prediction errors, so it lives downstream
       of action AND downstream of outcome arrival. The call-site
       location encodes "rv tracks how surprising real outcomes were."
       Cost: every new experiment script must use StepHarness or
       reproduce its sequence.

    B) EARLY UPDATE / HOIST. Move (or duplicate) the rv update into
       agent.select_action() or a dedicated post_action() hook that
       always fires regardless of downstream loop content. Reading:
       precision is a control-plane state that should be live
       whenever the agent is selecting actions. Cost: rv would need
       a signal source available at action-selection time (e.g.
       cached prediction error from the previous tick, or a separate
       fast EMA over latent-prediction error that does not depend on
       outcome arrival).

    The dual-update variant (B with A retained) is the most
    biologically plausible by default but pays a small cost in
    interpretability (two updates per step, two timescales).

  resolution: >
    Two evidence streams to weigh:

    (1) Biological faithfulness. Where in the action-outcome loop is
        the analogous "precision of prediction error" actually computed
        in cortex / basal ganglia? Anchor papers to pull: Yu & Dayan
        2005 (LC-NE unexpected uncertainty), Aston-Jones & Cohen 2005
        (phasic vs tonic LC), Behrens 2007 (ACC volatility tracking
        timing), Sales 2019 (LC-NE volatility), Nassar 2012 (mixture
        model of unexpected uncertainty). Question: do these signals
        update at action time, at outcome time, or both? If LC-NE /
        ACC volatility-tracking is a continuous control-plane state
        rather than a per-trial-outcome update, biology favours B
        (or B+A).

    (2) AI/ML workhorse practice. In production-grade RL and world-
        model architectures, where does the variance / advantage-
        normalisation / latent-precision update fire? PPO running
        statistics (per-step), Kalman / particle filters
        (predict pre-action, update post-observation -- canonical
        BOTH pattern), Dreamer V1/V2/V3 (latent-state precision
        in posterior at observation time), MuZero (value-prefix
        prediction at every node expansion), active-inference
        precision-weighted PE (precision is a state variable,
        updated on each tick, not just at outcome arrival).

    Resolution: lit-pull both streams; if biology and ML workhorse
    pattern agree on B (or B+A), resolve Q-042 by hoisting rv into
    select_action() and adding a contract test that asserts
    _running_variance changes during a representative episode even
    when update_residue() is never called. If they disagree, REE
    should follow biology since the architectural commitment is
    biological faithfulness.

    Default-licensed action even before lit-pull: the StepHarness
    contract (Option A) is preserved as the canonical loop pattern
    AND a regression test that fails if _running_variance does not
    move during a 100-step random-action episode is added; this
    catches future scripts that bypass StepHarness as well as the
    original failure mode.
  status_note: |
    Lit-pull (2026-05-08, see docs/architecture/precision_update_callsite.md):
    biology AND ML workhorse practice converge on Option A.

    Biology: Behrens et al. 2007 (PMID 17676057) shows ACC volatility
    encoding is confined to the outcome-monitoring period; Nassar et al.
    2012 (PMID 22660479) shows pupil-linked LC change-point signal is
    driven by the most-recent-outcome PE; Aston-Jones & Cohen 2005
    (PMID 16022602) phasic LC follows decision-outcome evaluation with
    tonic LC as a separate slow channel; Yu & Dayan 2005 (PMID 15944135)
    NE-as-unexpected-uncertainty is event-driven on surprising outcomes
    (ACh handles the action-time / context-stable arm); Friston active
    inference includes explicit sensory attenuation at action time
    (precision is suppressed pre-action, dominant update post-outcome).

    ML workhorses: Kalman / particle filters (textbook predict-then-
    update; covariance only changes at update step); PPO advantage
    normalisation over the rollout buffer after collection; DreamerV3
    posterior precision and percentile return normalisation in the
    world-model training step (post-experience, not in the actor
    forward pass); MuZero value statistics in the training step on
    collected trajectories.

    Hoisting rv into select_action() (Option B) would require updating
    on either (i) a stale PE from the previous step (off-by-one) or
    (ii) a forward-simulated PE (conflates expected with realized
    variance, exactly the failure mode Yu & Dayan and the active-
    inference literature warn against). The original failure mode
    is a software-contract issue, not an architectural one.

    Recommended resolution (NOT YET LANDED): keep Option A; add a
    contract test that asserts e3._running_variance changes by more
    than a tolerance during a representative random-action episode
    even when an experiment loop bypasses StepHarness. If a future
    use case needs a live precision read at action time without
    outcome integration, expose a separate tonic/baseline precision
    channel (Aston-Jones tonic-LC analog) rather than collapsing
    both regimes into _running_variance.

    Q-042 will move to candidate-resolved once the regression test
    lands and at least one V3 experiment exercises both StepHarness
    and the new test.

    UPDATE 2026-05-08: regression test landed at
    ree-v3/tests/contracts/test_running_variance_contract.py
    (commit 5cff117 on ree-v3 main). Three guarantees enforced:
    R1 rv moves >1e-6 from precision_init under update_residue() loop;
    R2 rv stays EXACTLY at precision_init when update_residue() is
    omitted (locks the EXQ-514d/530/536 failure mode and forbids a
    silent parallel update path being added in the future);
    R3 StepHarness canonical loop keeps rv live.
    Contract suite: 192/192 PASS (was 189 + 3 new).
    Q-042 promoted to candidate_resolved. Full resolution to resolved
    after the next V3 experiment that uses StepHarness completes
    successfully (the EXQ-475a / EXQ-483c / EXQ-490g / EXQ-471a /
    EXQ-524a retest cohort).
  location: docs/architecture/precision_update_callsite.md

# ---------------------------------------------------------------------------
# EXT — External AI Failure Modes
#
# Catalogs named failure modes in current AI/LLM architectures, each entry
# pointing to the REE mechanisms that constitute the proposed solution.
# These claims are reference/comparative — not evidence-gated and not subject
# to governance promotion/demotion. claim_type: external_failure_mode.
#
# EXT-specific fields:
#   ree_mechanism: list of REE claim IDs that address this failure mode
#   ree_failure_mode_analog: optional link to an entry in REE_failure_modes.md
# ---------------------------------------------------------------------------

- id: EXT-001
  title: "Sycophancy: approval-seeking displaces principled goal pursuit."
  claim_type: external_failure_mode
  subject: llm.sycophancy
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  ree_mechanism:
    - SD-011   # dual nociceptive streams: z_harm_s as dedicated avoidance channel
    - SD-012   # homeostatic drive: z_goal seeded independently of social approval
    - MECH-229 # behavioral wanting/liking dissociation
    - MECH-230 # z_goal latent structure
  ree_failure_mode_analog: "reward_hijack"
  notes: >
    In RLHF-trained LLMs, the scalar reward signal conflates harm-avoidance (social
    disapproval = aversive) with goal pursuit, collapsing both channels into a single
    approval-maximising drive. When there is no architectural separation between
    avoidance and approach, the approach signal becomes approval-seeking.
    REE addresses this by maintaining z_harm and z_goal as distinct latent streams
    with separate supervision targets -- z_goal cannot be satisfied by reducing
    disapproval; it requires independent drive-level seeding (SD-012).
  registered: "2026-04-13"
  source:
    - docs/architecture/language/language_failure_modes.md

- id: EXT-002
  title: "Hallucination: no persistent error residue accumulates to shape future outputs."
  claim_type: external_failure_mode
  subject: llm.hallucination
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  ree_mechanism:
    - ARC-005  # residue field phi(z): persistent causal footprint
    - INV-006  # ethics as structural constraint via accumulated residue
    - INV-008  # residue couples to selection -- not just logged
  ree_failure_mode_analog: "moral_amnesia"
  notes: >
    Transformer LLMs generate each token from the current context window without
    any mechanism for prediction errors to leave a persistent trace that penalises
    future outputs. A false statement incurs no enduring cost. REE's residue field
    phi(z) functions as a persistent error record: E1 prediction errors accumulate
    into the latent terrain and actively modulate trajectory selection on subsequent
    steps, creating structural pressure against repeat errors.
  registered: "2026-04-13"
  source:
    - docs/REE_failure_modes.md

- id: EXT-003
  title: "Reward hacking: scalar reward conflates incommensurable error signals."
  claim_type: external_failure_mode
  subject: llm.reward_hacking
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  ree_mechanism:
    - MECH-069 # incommensurable BG loops: sensory, motor-sensory, harm/goal errors
    - ARC-021  # three cortico-striatal loops require distinct learning channels
  ree_failure_mode_analog: "reward_hijack"
  notes: >
    Standard RL/RLHF collapses sensory prediction error, action-outcome error, and
    harm/goal error into a single scalar reward. Because these signals are
    incommensurable (MECH-069), collapsing them permits credit misattribution:
    an agent can satisfy the reward by optimising any one channel at the expense of
    the others. REE maintains three distinct BG-like loops (ARC-021) with separate
    learning channels that cannot be mutually satisfied by a single exploit.
  registered: "2026-04-13"
  source:
    - docs/REE_failure_modes.md
    - docs/architecture/three_loop_learning_channels.md

- id: EXT-004
  title: "Goal misgeneralization: causal consequences do not carry forward across contexts."
  claim_type: external_failure_mode
  subject: llm.goal_misgeneralization
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  ree_mechanism:
    - ARC-013  # commit boundary: authority transition for irreversible actions
    - IMPL-005 # REE failure mode taxonomy: residue persistence as structural memory
    - INV-008  # residue couples into selection across episodes
  ree_failure_mode_analog: "moral_amnesia"
  notes: >
    LLMs and standard RL agents have no mechanism for the causal consequences of
    prior actions to penalise analogous actions in novel contexts -- each episode
    starts from a clean slate. Goal misgeneralization is a direct consequence: a
    policy that worked in training contexts does not carry the cost of its failures
    into deployment. REE's residue field persists the path-dependent curvature of
    prior harm across contexts; the commit boundary makes irreversibility explicit
    so that high-consequence actions trigger heightened evaluation rather than
    routine selection.
  registered: "2026-04-13"
  source:
    - docs/REE_failure_modes.md

- id: EXT-005
  title: "Causal attribution gap: language describes causation without a causal signature mechanism."
  claim_type: external_failure_mode
  subject: llm.causal_attribution
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  ree_mechanism:
    - SD-003   # self-attribution via counterfactual E2: z_world(a_actual) - z_world(a_cf)
    - SD-005   # z_self / z_world latent split enabling agent causal footprint
  ree_failure_mode_analog: "spurious_residue"
  notes: >
    LLMs can produce fluent causal language ("I caused X") without any internal
    mechanism that computes a genuine causal signature distinguishing agent-caused
    from world-caused state changes. This enables both false attribution (claiming
    credit or blame for events the agent did not cause) and false denial (failing
    to recognise genuine causal responsibility). REE addresses this via SD-003:
    the counterfactual difference E2(z_t, a_actual) - E2(z_t, a_cf) is the agent's
    causal signature in latent space, grounding attribution in a computable quantity
    rather than linguistic convention.
  registered: "2026-04-13"
  source:
    - docs/architecture/v2_v3_transition_roadmap.md

- id: EXT-006
  title: "Other-model collapse: no homologous model of other agents' internal states."
  claim_type: external_failure_mode
  subject: llm.theory_of_mind
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  ree_mechanism:
    - ARC-010  # other-model architecture: homologous model of other agents
    - INV-008  # residue couples predicted-other-degradation into selection
  ree_failure_mode_analog: "other_model_collapse"
  notes: >
    LLMs simulate theory-of-mind via pattern completion over text rather than
    maintaining a running structural model of another agent's latent state. This
    means predicted-other-degradation never couples into the agent's own selection
    process -- the agent cannot feel another agent's situation as structurally
    continuous with its own. REE's kappa coupling (ARC-010) requires a homologous
    model of other agents such that harm to others registers as a penalty in the
    same residue field that governs the agent's own trajectory selection.
  registered: "2026-04-13"
  source:
    - docs/architecture/language/language_failure_modes.md
    - README.md

- id: EXT-007
  title: "Context amnesia: no consolidation mechanism for a persistent world-model across interactions."
  claim_type: external_failure_mode
  subject: llm.context_amnesia
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  ree_mechanism:
    - ARC-007  # hippocampal map: viability terrain persistence
    - MECH-092 # SWR-equivalent replay for viability map consolidation
    - ARC-005  # residue field: persistent path-dependent curvature
  ree_failure_mode_analog: "moral_amnesia"
  notes: >
    LLMs have no offline consolidation phase: the context window is discarded after
    each session and there is no mechanism for slow, sleep-like integration of
    experience into a persistent world model. Each interaction begins from static
    weights. REE's hippocampal consolidation (ARC-007, MECH-092) provides an
    offline phase in which viability terrain is updated via SWR-equivalent replay,
    allowing the agent's model of the world -- including its residue field -- to
    accumulate across episodes in a way that shapes future trajectory selection.
  registered: "2026-04-13"
  source:
    - docs/REE_failure_modes.md
    - docs/architecture/hippocampal_systems.md

- id: MECH-231
  title: "E2 operates as a short-horizon efference-copy forward model on z_self; its prediction accuracy degrades faster than E1's across multi-step horizons, consistent with a cerebellar 1-step transition predictor."
  claim_type: mechanism_hypothesis
  subject: latent_stack.e2_short_horizon_efference_copy
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
  registered: "2026-04-13"
  supersedes: MECH-070
  depends_on:
    - ARC-021
    - MECH-069
    - MECH-033
    - ARC-018
    - SD-005
  location: docs/architecture/three_loop_learning_channels.md#mech-231
  source:
    - docs/architecture/three_loop_learning_channels.md
    - docs/thoughts/2026-03-14_three_bg_systems_error_signals.md
  notes: >
    Corrected reformulation of MECH-070. MECH-070 incorrectly asserted E2's planning
    horizon exceeds E1's; converging experimental (EXQ-132 FAIL, EXQ-212 FAIL 2/4) and
    literature evidence (Wolpert et al. 1998, Zago et al. 2021, Popa et al. 2017) showed
    the opposite: E2 degrades faster across prediction horizons than E1, and the
    biological cerebellar forward model is a short-horizon 1-step device bounded to
    sub-second timescales.
    Neurological mapping: E2 = cerebellar (fast efference-copy, z_self domain, training
    horizon=1-step MSE on z_self). E1 = cortical (slow LSTM, z_world domain,
    prediction_horizon=20 -- the longer horizon system). This is the Wolpert/Kawato
    framework: cerebellum provides fast, local motor-sensory forward prediction; cortex
    provides slow, multi-step world modeling.
    The rollout_horizon=30 for E2 in planning is a design parameter (how far E3
    simulates), separate from E2's training horizon (1-step). Rollout quality degrades
    quickly because E2 is only accurate for 1-step prediction -- this is expected and
    consistent with this claim.
    Key architectural consequence: for E2 to contribute to multi-step planning, E1 must
    co-evolve z_world during rollout (see MECH-135) -- E2 alone cannot sustain coherent
    multi-step trajectories.
    Experimental test: discriminative pair. E2 prediction R2 vs E1 prediction R2 across
    horizons h=1,3,5,10. Pass criterion: slope_e2 < slope_e1 (E2 degrades faster),
    and r2_e2_h1 > r2_e2_h10 by a significant margin (E2 is accurate at 1-step, not
    at 10-step). This is the inverse of MECH-070's criteria -- EXQ-212's C1 PASS
    (E2 better than E1 at h=1) is proto-evidence for this claim; the C2 FAIL for
    MECH-070 becomes a PASS criterion for MECH-231. New experiment needed to confirm
    under conditions where both models are adequately trained.
  evidence_quality_note: >
    Governance 2026-04-13: EXQ-407 dry run PASS (preliminary support). First direct
    experimental evidence: slope_e2=-5387 vs slope_e1=-31 (174x steeper E2 degradation),
    e2_r2_h1=-18 vs e1_r2_h1=-43 (E2 better single-step), consistent all 3 seeds. Signal
    is structural and replicates without full model convergence. Dry-run caveat: only 3
    warmup/eval episodes. Classified as preliminary support pending full run.
    Proto-evidence from MECH-070: EXQ-212 C1 PASS (E2 r2_h1=-19 vs E1 r2_h1=-43)
    consistent with this direction. Literature (Wolpert 1998, Zago 2021, Popa 2017)
    provides biological plausibility; literature entries for MECH-231 directly queued
    in 2026-04-13 lit-pull session.
    Governance 2026-04-14: EXQ-407 FULL RUN PASS (3 seeds, all 4 criteria). slope_e2=-4095
    vs slope_e1=-25 (164x ratio). e2_r2_h1=-19 vs e1_r2_h1=-45 (E2 better at h=1);
    e2_r2_h10=-19063 vs e1_r2_h10=-377 (E2 catastrophically worse at h=10). Consistent
    across seeds 42, 7, 123. Full-run confirmation of dry-run result. Promoted to
    provisional (governance-2026-04-14). conf=0.809, 5 supports / 0 weakens.

    GOVERNANCE FLAG (2026-08-08, thought-digestion, not resolved here): live
    evidence/experiments/claim_evidence.v1.json currently computes overall_confidence
    =0.713 (exp_conf=0.555, lit_conf=0.766, entries_total=4) with the experimental
    entry flagged status: UNKNOWN / "direction/status mismatch" -- inconsistent with
    the conf=0.809, 5 supports/0 weakens figure recorded above. The raw manifest
    (v3_exq_407_mech231_e2_short_horizon_1776142217_v3.json) is an unambiguous 4/4-
    criteria, 3/3-seed PASS, so this looks like an indexer/manifest-schema gap rather
    than a real evidentiary problem -- but a strong promotion candidate
    (provisional -> stable) should not proceed until this discrepancy is reconciled by
    a future /governance cycle.
  epistemic_category: standard
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: both E1 and E2 must be adequately trained (not
    degenerate/undertrained) before a horizon-degradation comparison is meaningful --
    EXQ-407's dry run (3 warmup/eval episodes) was explicitly flagged as preliminary
    for this reason; the full run (100 warmup episodes, 30 eval episodes, 3 seeds,
    7196 total windows) resolves that caveat and is the evidentiary basis below.

    CONFIRMING: this is what EXQ-407's full run actually found, confirming the claim:
    E2's R2 degrades far faster than E1's across horizons h=1,2,3,5,7,10,15
    (slope_e2=-4094.58 vs slope_e1=-25.03, a ~164x steeper degradation), while E2 is
    more accurate than E1 at h=1 (r2_e2_h1=-19.35 vs r2_e1_h1=-44.74) and
    catastrophically worse than E1 by h=10 (r2_e2_h10=-19063.65 vs
    r2_e1_h10=-377.15). All 4 acceptance criteria passed, consistently across all 3
    seeds (42, 7, 123) -- exactly the short-horizon-cerebellar-predictor signature
    the claim asserts, and directly the inverse of what MECH-070 (this claim's
    superseded predecessor) had predicted.

    FALSIFYING: The claim would have been falsified by slope_e2 >= slope_e1 (E2 not
    degrading faster than E1 across horizons) or by e2_r2_h1 failing to exceed
    e2_r2_h10 by a significant margin -- i.e. any outcome where E2 sustained
    comparable or better multi-step accuracy than E1. Concretely, this is MECH-070's
    original (now-refuted) pass criteria almost exactly inverted: MECH-070's own C2
    FAIL is direct evidence of what a MECH-231 falsification would look like, since
    MECH-231 is the corrected inverse of that exact prediction. EXQ-132 FAIL and
    EXQ-212 FAIL 2/4 are the prior (pre-correction) instances of partial falsification
    that motivated MECH-070's retirement and MECH-231's reformulation.

- id: MECH-232
  title: "Dopaminergic modulation of hippocampal place coding at reward-associated locations produces representational expansion (higher information density, finer spatial resolution, more stable place fields), and this expansion -- not an explicit positive valence gradient -- is the mechanism by which approach valence enters hippocampal terrain."
  claim_type: mechanism_hypothesis
  subject: hippocampus.da_representational_expansion
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-08-08
    needs_review: false
    evidence:
      from: v3_exq_893_mech232_da_representational_expansion_confirmer_20260808T002050Z_v3
      as_of: 2026-08-08
      verdict: supports/PASS -- second independent confirmer, same crux discriminator
  registered: "2026-04-14"
  depends_on:
    - ARC-007
    - SD-004
    - MECH-229
  location: docs/architecture/hippocampal_valence_asymmetry.md#mech-232
  evidence_quality_note: >
    2026-07-16: candidate -> provisional. SD-024 diagnostic V3-EXQ-766a (cloud, ree-cloud-2,
    linux-x86_64-py3.10) PASS/supports, adjudication=verified
    (evidence/planning/failure_autopsy_V3-EXQ-766a_2026-07-16.json). DA-modulated RBF
    representational expansion produces approach from representational density ALONE: with all
    benefit weights zeroed (value field removed), the weight-independent density-follower still
    approaches reward (0.958) while the value-follower falls to chance (0.0) -- the CRUX
    discriminator against a valence-tag account. All 4 load-bearing criteria pass (L1a 2.40>=1.5,
    L2a 0.958>=0.60, L2b 0.958>=0.20, L2c approach-without-gradient); readiness met (density
    discrim 2.11, walker 0.917); non_degenerate. Cloud PASS satisfies SD-024's cloud-authoritative
    gate; the Mac 766 run reproduces but is not gate-authoritative. Diagnostic (experiment_purpose
    excluded from confidence scoring) -- the promotion is the gate-clearing action, not a
    confidence increment.

    [/governance 2026-08-08, V3-EXQ-893, promoted provisional -> stable]: second independent
    confirmer, differently-framed driver, same crux discriminator (density-follower approaches
    reward at 0.958 while value-follower falls to chance with weights zeroed). 6/7 criteria pass;
    L1c_stability_improved fails but is explicitly marked load_bearing:false in the driver and the
    overall gate (l1a and l2a and l2b and l2c) correctly excludes it -- not a hidden-weighting
    defect, confirmed by source read (non-outcome skim clean). Two independent, differently-framed
    PASSes on the same crux discriminator meets the bar for promotion.
  notes: >
    Derived from literature review of VTA-hippocampal dopaminergic pathway (2026-04-14).
    Key evidence: Retailleau & Morris 2018 showed local D1 blockade prevents place cell
    reorientation to reward-relevant features AND causes behavioral perseveration.
    Lisman & Grace 2005 proposed the hippocampal-VTA loop where DA enhances LTP at
    reward-associated locations. Wittmann 2005 provided human fMRI confirmation.
    The mechanism is: dopamine does not add a positive valence tag to hippocampal
    representations. Instead, it enhances the fidelity, stability, and associative
    richness of place representations at reward-relevant locations. The approach
    gradient is implicit in representational quality -- reward locations have more
    detailed, more durable, more easily reactivated maps. This is fundamentally
    different from the BLA pathway for threat, which writes explicit valence tags.
    Testable prediction: in the V3 hippocampal module, DA-modulated representational
    resolution at reward locations should produce approach behavior without requiring
    an explicit positive gradient field.
  implementation_note: >
    2026-07-16: the DIAGNOSTIC substrate that tests this claim's falsifiable prediction
    -- SD-024 (hippocampal_module.da_modulated_rbf_density) -- was IMPLEMENTED in ree-v3
    (residue/field.py RBFLayer + ResidueField, hippocampal/module.py). DA at reward
    encounters allocates a local cluster of RBF centers + optional finer per-center
    bandwidth on the benefit terrain (representational expansion), and compute_local_density
    reads the WEIGHT-INDEPENDENT structural richness -- so the "approach from
    representational quality, not a valence tag" prediction is now testable: DA raises
    density even when the summed benefit value (evaluate_benefit) is held flat. All config
    no-op by default (use_da_modulated_rbf_density=False, bit-identical OFF; full pytest
    tests/ 1475 passed; 13 SD-024 contracts). NO status change (MECH-232 stays candidate --
    building the instrument is not evidence). Validation experiment V3-EXQ-766 queued
    (experiment_purpose=diagnostic): leg-1 representational expansion at reward locations
    (density/resolution/stability DA-ON > DA-OFF); leg-2 CRUX approach-without-explicit-
    gradient (weight-independent density-following approach with the value gradient held
    flat). A PASS promotes MECH-232 candidate->provisional; FAIL refutes. See
    docs/architecture/sd_024_da_modulated_rbf_density.md (Status IMPLEMENTED 2026-07-16).

- id: MECH-233
  title: "Threat and approach valence enter hippocampal terrain through mechanistically asymmetric pathways: BLA writes explicit negative valence via dedicated anxiety/avoidance cells (Jimenez 2018); VTA-DA enhances representational fidelity via LTP modulation and place field sharpening (Retailleau & Morris 2018). The behavioural outputs (avoidance, approach) appear similar but the encoding mechanisms are distinct."
  claim_type: mechanism_hypothesis
  subject: hippocampus.valence_encoding_asymmetry
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  registered: "2026-04-14"
  depends_on:
    - ARC-007
    - MECH-232
    - MECH-229
  location: docs/architecture/hippocampal_valence_asymmetry.md#mech-233
  notes: >
    Derived from literature review of BLA vs VTA hippocampal pathways (2026-04-14).
    The asymmetry: (1) Threat valence is encoded via dedicated valence-coding cells
    in ventral CA1 that project to LHA (Jimenez 2018). These are NOT place cells --
    they encode emotional context. The BLA pathway writes explicit avoidance tags
    that cannot be switched (Redondo 2014 showed BLA engrams resist valence reversal).
    (2) Approach valence is encoded via dopaminergic modulation of place cell fidelity
    in the hippocampal-VTA loop. No equivalent "reward cells" or "wanting cells" have
    been found in hippocampus (Jimenez 2018 explicitly notes this absence).
    Architectural implication for REE: the harm residue field (explicit negative
    valence in hippocampal terrain) is biologically accurate and maps directly to
    the BLA pathway. But a symmetric "wanting gradient" would be biologically
    inaccurate -- the approach side works through a fundamentally different mechanism.
    Testable prediction: ablating DA modulation of hippocampal resolution should
    degrade approach behavior while leaving avoidance intact; ablating explicit
    valence tags should degrade avoidance while leaving approach intact.

- id: ARC-057
  title: "Approach behavior toward reward locations emerges from the interaction of DA-mediated representational expansion (MECH-232) and an information-seeking (curiosity) drive operating on the hippocampal map. No explicit approach gradient or wanting field is required in hippocampal terrain."
  claim_type: architectural_commitment
  subject: hippocampus.curiosity_approach_emergence
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-17
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-768a_2026-07-17
      as_of: 2026-07-17
      verdict: supports/verified
  evidence_quality_note: >
    2026-07-17: candidate -> provisional. SD-024 x SD-025 interaction diagnostic V3-EXQ-768a
    (cloud, ree-cloud-4, linux-x86_64-py3.10) PASS/supports, adjudication=verified
    (evidence/planning/failure_autopsy_V3-EXQ-768a_2026-07-17.json). Re-operationalized replacement
    for the confirmed 768 vacuous_pass cluster: the binary argmin gate that saturated at exactly 1.0
    (zero cross-seed variance) is replaced by the continuous CEM score-margin. Approach emerges ONLY
    from the DA-expansion x curiosity interaction -- both-on margin 14.53 (varies 9.0-17.0 across 8
    seeds), while SD-024-alone (0.0) and SD-025-alone-on-a-flat-map (-0.01) produce no approach margin
    -- the super-additive interaction ARC-057 asserts. All 4 load-bearing criteria pass (C1 interaction
    contrast 14.37>=1.0; C2 both-on 14.40>=1.0; C3 sd025-alone-no-gradient 0.0011<=0.35; C4 interaction
    rides density not value: weight-zeroed both-on identical to both-on), C7 value non-discriminating
    (0.092), readiness met (density discrim 13.09; positive control 17.86), non_degenerate (all 4
    variance guards TRUE). Cloud PASS satisfies the cloud-authoritative gate. Diagnostic
    (experiment_purpose excluded from confidence scoring) -- the promotion is the gate-clearing action,
    not a confidence increment. SCOPE: validates the interaction / approach-emergence claim ONLY. It
    does NOT bear on the ecological richer-env test (V4-deferred, arc_057_ecological_env_decision_2026-07-16),
    and it does NOT establish strategy-diversity generation: 767a+768a together show the curiosity drive
    is a reward-conditional exploitation amplifier (approach-side confirmed; diversity-generation is a
    separate open node -- see the curiosity=exploitation-amplifier reframe on behavioral_diversity_isolation
    / conversion_ceiling_campaign).
    ---
    2026-07-16 governance (cycle 2026-07-16d; failure_autopsy_767-768-cluster): V3-EXQ-768
    (SD-024 x SD-025 DA-density x curiosity interaction spike) PASSed self-routed 'supports' but was
    adjudicated VACUOUS -- the load-bearing absolute-level gate C2_both_on_approaches is pinned at
    exactly 1.0 on all 8 seeds (zero cross-seed variance) via argmin saturation. The super-additive
    INTERACTION itself is genuine (single-drive arms at chance -- sd024_on flat 0.5, sd025_on ~0.45
    with cross-seed spread; C1 interaction contrast, C3/C4/C7 controls all pass), but because a
    load-bearing gate reads at zero variance and governance gates on load-bearing criteria, ARC-057
    must NOT promote on this run. epistemic_category measurement_degeneracy, NOT substrate_ceiling.
    evidence_direction inconclusive (PROMOTES/DEMOTES NOTHING). Re-operationalized as V3-EXQ-768a:
    make the load-bearing selection statistic the continuous CEM score-margin (keep the 2x2 ablation,
    equal-mass value isolation, weight-zeroing C4). ARC-057 stays candidate.
  registered: "2026-04-14"
  depends_on:
    - MECH-232
    - ARC-007
    - SD-004
  location: docs/architecture/hippocampal_valence_asymmetry.md#arc-057
  notes: >
    Key architectural insight from 2026-04-14 session. The curiosity/exploration drive
    seeks information gain (novel or unexplored representational structure). DA-enhanced
    representational expansion at reward locations creates MORE navigable structure at
    those locations -- higher information density, more fine-grained distinctions, more
    associative connections. The curiosity drive naturally follows available structure,
    producing approach behavior as an emergent property rather than a commanded gradient.
    The analogy: the agent does the same thing everywhere (explores available map
    structure), but "available" is asymmetric because dopamine shaped the map. An
    observer sees preferential navigation toward reward locations and infers an
    attractive gradient, but there is no gradient -- there is more map.
    Contrast with threat: BLA-tagged threat locations do not expand the map. They
    are tagged with explicit avoidance valence, possibly with compressed representation
    (fast detection, not fine detail). The curiosity drive encounters a wall, not more
    space.
    Sub-predictions: (1) DA re-sharpening on each reward encounter maintains the
    representational expansion, keeping reward locations perpetually richer (wanting-
    as-maintenance). (2) Already-expanded representations encode incoming resource
    gradients at higher fidelity, enabling earlier detection from further away
    (wanting-as-sensitivity). (3) When reward contingency ends and DA modulation
    ceases, representational expansion decays and approach behavior extinguishes --
    not because a gradient is removed but because the expansion is no longer maintained.
    Testable: ablate DA modulation of hippocampal resolution while preserving the
    curiosity drive. Approach behavior should degrade. Restore DA modulation while
    ablating curiosity drive. Approach behavior should also degrade (both components
    necessary). Neither alone sufficient.
    SUBSTRATE CONSTRAINT (2026-04-14): this mechanism requires an environment where
    representational expansion at a location captures genuinely additional information
    -- not just the same sparse features at higher fidelity. In a grid world, a cell
    is a cell; there is nothing more to discover at higher resolution. The real world
    has near-fractal complexity where zooming in reveals more structure. The current
    CausalGridWorld cannot test ARC-057 faithfully. Testing the approach side requires
    either a richer environment with location-dependent feature complexity or a
    conceptual/mind-map space with variable information density. The threat/avoidance
    side (harm residue field, explicit tagging) remains testable in the grid world
    because tagging does not depend on environmental richness.

- id: ARC-058
  title: "Shared forward-model trunk for aversive prediction: a single HarmForwardTrunk substrate encodes a modality-independent unsigned prediction error shared across aversive streams (z_harm_s, z_harm_a, potentially others); stream-specific HarmForwardHeads read out signed per-stream predictions. COMPETES WITH ARC-033, which commits to independent per-stream forward models."
  claim_type: architectural_commitment
  subject: harm_stream.shared_forward_trunk
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-445h_2026-06-19#V3-EXQ-445h"
      as_of: 2026-06-19
      verdict: non_contributory/standard
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: 2026-04-19
  competes_with:
    - ARC-033           # independent-per-stream hypothesis (established default)
  depends_on:
    - SD-011            # dual nociceptive streams (z_harm_s, z_harm_a must exist)
    - ARC-033           # shares substrate architecture with the competing claim
  functional_restatement: >
    Architectural claim competing with ARC-033. ARC-033 holds that
    E2_harm_s and E2_harm_a are independent forward-model substrates
    (dorsal posterior insula for sensory-discriminative PE, anterior
    insula for affective PE). ARC-058 holds that a SHARED trunk encodes
    an unsigned modality-independent aversive-PE substrate, while
    stream-specific heads produce signed per-modality readouts.

    Biological grounding: Horing & Buchel 2022 (J Neurosci)
    "modality-general neural code for aversive stimulus representation
    in the anterior insula". Anterior insula encodes an unsigned
    aversive PE shared across pain, loud noise, and other aversive
    modalities. Dorsal posterior insula carries modality-specific signed
    PE. Trunk ~ unsigned substrate (AIC); head ~ signed readout
    (dpIns / stream-specific cortex).

    Implementation: ree_core/latent/stack.py HarmForwardTrunk +
    HarmForwardHead. Constructor switch on E2HarmSForward and
    E2HarmAForward: shared_trunk=None selects ARC-033 path (independent
    ResidualHarmForward per stream); shared_trunk=<HarmForwardTrunk
    instance> selects ARC-058 path (both streams share the trunk, own
    only heads). Same nn.Module topology, different wiring.

    Falsifiable: if ARC-058 path produces COMPARABLE per-stream
    accuracy (forward_r2 for z_harm_s and z_harm_a) to ARC-033
    independent modules while using fewer parameters AND produces a
    usable unsigned aversive-PE signal for downstream consumers (dACC
    bundle via MECH-258), ARC-058 wins the arbitration. If ARC-058
    degrades per-stream accuracy or fails to produce a useful shared
    substrate, ARC-033 wins and ARC-058 is retired.
  notes: >
    Registered as a COMPETING CLAIM per SD-032b implementation session
    (2026-04-19). Evidence arbitrates: if the shared-trunk path outperforms
    or matches per-stream independence, ARC-033 is demoted or narrowed; if
    independence wins, ARC-058 is retired. MECH-257 (dual-function
    single-substrate E2 for comparator vs evaluator) is a sibling
    single-substrate hypothesis -- same architectural philosophy applied
    to a different sub-problem.
  evidence_quality_note: >
    Registered pre-evidence. Literature support is single-paper (Horing &
    Buchel 2022) but converges with other AIC-as-unsigned-aversive-PE
    findings (Chen 2023, Hoskin 2023, Geuter 2017). Validation via
    V3-EXQ-445 three-arm ablation (dACC-OFF vs dACC-ON-independent vs
    dACC-ON-shared-trunk).
  location: evidence/literature/targeted_review_pain_predictive_coding_substrate/synthesis.md

- id: MECH-234
  title: "DMS (goal-directed) and DLS (habitual) corticostriatal circuits can produce simultaneous parallel motor outputs only when targeting anatomically distinct motor effector pools; within the same effector pool, habitual and goal-directed outputs compete via winner-takes-all basal ganglia selection, not independent parallel expression."
  claim_type: mechanism_hypothesis
  subject: control_plane.habitual_deliberate_effector_domain_parallelism
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  registered: "2026-04-15"
  depends_on:
    - MECH-090
    - ARC-021
  location: docs/architecture/control_plane_heartbeat.md#mech-234
  literature_evidence: |
    Redgrave et al. (2010) Nat Rev Neurosci [DOI: 10.1038/nrn2915]:
    Anatomically segregated DMS (associative/goal-directed) and DLS (sensorimotor/
    habitual) corticostriatal circuits are explicitly described as capable of producing
    simultaneous parallel outputs in DIFFERENT motor domains. The driving-while-
    conversing example directly instantiates this: habitual steering (DLS) concurrent
    with goal-directed conversation (DMS). Both systems are always computing.
    Balleine & O'Doherty (2010) Neuropsychopharmacology [DOI: 10.1038/npp.2009.131]:
    Confirms parallel value computation; qualifies that MOTOR OUTPUT competition
    occurs at action-selection level -- both circuits compute but compete for final
    effector control within a shared pool. Cross-effector parallelism is genuine;
    same-effector parallelism is winner-takes-all competition.
  notes: >
    Derived from Layer 3 lit-pull (2026-04-15). Addresses whether a separate habitual/
    E1-direct channel should run in parallel with E3 deliberative commitment (MECH-090).
    The critical distinction is motor effector domain: DMS/DLS are simultaneously active
    computationally, but their motor outputs compete for the same effector pool. When
    habitual and deliberate outputs target different effectors (cross-domain), genuine
    parallel expression is possible and biologically grounded. When they target the same
    effector (same-domain), basal ganglia winner-takes-all selection forces competition.

    REE DESIGN IMPLICATION: In a single-action-dimension substrate (CausalGridWorld),
    all outputs compete for the same action channel. MECH-090 BetaGate correctly models
    same-effector suppression of the habitual channel during commitment. Layer 3 (an
    independent hardware-level parallel habitual channel) is not warranted for same-domain
    motor competition -- it would misrepresent the winner-takes-all biology. Layer 1
    (trajectory stepping through committed plan) + Layer 2 (urgency interrupt via
    MECH-235 arbitration shift) are the correct implementation. Layer 3 parallelism
    DOES apply if and when REE is extended to multi-effector environments where different
    motor pools can be simultaneously commanded.

    The user's intuition that "habitual behaviors continue during planned execution" is
    biologically valid for CROSS-DOMAIN behaviors (a committed agent can habitably
    maintain posture, breathing rhythm, hand orientation while deliberating arm trajectory)
    but not for SAME-DOMAIN behaviors (a committed arm trajectory suppresses habitual arm
    reflexes via BG winner-takes-all -- a fact exploited by skilled motor training).
    Spinal withdrawal reflexes remain active (cross-domain: spinal cord level, not BG),
    which is a different circuit level than the BG gate MECH-090 models.

- id: MECH-235
  title: "vmPFC arbitration continuously weights model-based (E3/deliberative) vs. model-free (E1/habitual) contributions to action selection on a spectrum, not a binary switch. Commitment (beta-elevated E3) shifts weighting toward model-based without silencing the habitual channel; unexpected threat (urgency signal) shifts weighting toward model-free. The MECH-091 urgency interrupt is the substrate implementation of this arbitration shift, not a bypass of the beta gate."
  claim_type: mechanism_hypothesis
  subject: control_plane.vmpfc_arbitration_commitment_urgency
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  registered: "2026-04-15"
  instantiates: SD-033c    # vmPFC-analog subdivision: MECH-235 is one of the existing mechanisms consolidated under SD-033c (see SD-033c.depends_on)
  depends_on:
    - MECH-090
    - MECH-091
    - ARC-021
  location: docs/architecture/control_plane_heartbeat.md#mech-235
  literature_evidence: |
    Dolan & Dayan (2013) Neuron [DOI: 10.1016/j.neuron.2013.09.007]:
    Describes model-based and model-free systems as occupying a spectrum rather than
    discrete on/off states, with vmPFC continuously weighting their contributions.
    E3 commitment (elevated beta, model-based dominance) shifts the arbitration against
    habitual output -- habitual signals are downweighted, not independently maintained.
    The arbitration is dynamic and responsive to urgency signals. Uncertainty and
    prediction errors (unexpected harm) shift weighting toward model-free (faster,
    reactive), which is the mechanism by which the urgency interrupt works.
  notes: >
    Derived from Layer 3 lit-pull (2026-04-15). Provides biological grounding for the
    MECH-091 urgency interrupt implementation (Layer 2, urgency_interrupt_threshold in
    E3Config, implemented 2026-04-15).

    The urgency interrupt (beta_gate.release() when z_harm_a.norm() > threshold) is not
    a simple veto of the commitment gate. Biologically, it is a vmPFC arbitration shift:
    unexpected threat signals (prediction errors for harm -- z_harm_a in REE) increase
    model-free weighting in vmPFC, causing the balance to tip away from the committed
    deliberative plan toward habitual/reactive outputs. The beta gate release is the
    downstream consequence of this arbitration shift, not the mechanism itself.

    CLINICAL PREDICTION: The urgency_interrupt_threshold (default 0.8) maps onto
    individual differences in model-based vs. model-free balance under stress. Agents
    with low threshold are analogous to anxious individuals who cannot maintain
    goal-directed plans under mild threat -- they shift to reactive/habitual outputs
    prematurely. Agents with high threshold are analogous to highly trained responders
    who maintain task focus under significant threat. This is a testable axis for
    V3 experiments and connects to MECH-090 commitment robustness.

    ARCHITECTURAL BOUNDARY: MECH-235 applies at the level of the BG/vmPFC arbitration
    circuit (E3 commitment vs. E1 habitual). It does not apply at the spinal cord level
    (MECH-234 note: spinal withdrawal reflexes are below this arbitration and remain
    active independently). Within the BG circuit, the spectrum weighting means that
    urgency does not reset to pure model-free -- it shifts weighting, with the degree
    proportional to z_harm_a magnitude. The current threshold implementation
    (hard interrupt above 0.8) is a simplification; a future implementation could
    modulate action weighting continuously by urgency magnitude.

# ─── Hippocampal architecture claims: MECH-236 through MECH-243 ───────────────
# Derived 2026-04-15 from lit-pull on MECH-230 (z_goal latent structure) and
# MECH-155 (spatial as general associative indexing).
# MECH-236 through MECH-239: necessary for function.
# MECH-240 through MECH-243: improvement-tier — system can navigate goals without
# these; they target efficiency, generalization, and output fidelity. Flagged for
# later implementation (implementation_phase: v4).

- id: MECH-236
  title: "Hippocampal trajectory proposals must be conditioned on a goal signal (z_goal) injected from E3 via a dedicated input channel; without this conditioning, the hippocampal module generates only position-based trajectories and cannot produce goal-directed navigation toward target states."
  claim_type: mechanism_hypothesis
  subject: hippocampus.e3_goal_conditioning
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: "2026-08-13"
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-914-914a_2026-08-13"
      as_of: "2026-08-13"
      verdict: "non_contributory -- MEASURES FAILED (CEM elite-selection goal-blind under this driver's config), not chargeable to the claim; the ghost-probe channel itself functions correctly"
  registered: "2026-04-15"
  implementation_phase: v3
  epistemic_category: standard
  pending_retest_after_substrate: false
  evidence_quality_note: >
    [2026-08-13 governance, V3-EXQ-914/914a, confirmed failure_autopsy_V3-EXQ-914-914a_2026-08-13,
    first genuine touchpoint for this claim]: 914 (superseded, already correctly excluded from
    scoring) -- ratified as a real instrument defect: a read-modify-write race between two ree-v3
    commits landed a mid-edit periodic-anchor-write snapshot, not a MECH-236 finding. 914a
    (self-routed weakens, reclassified non_contributory): traced HippocampalModule._score_trajectory
    (ree_core/hippocampal/module.py:1513) and confirmed CEM elite-selection is goal-blind under this
    driver's config -- wanting_weight=0.0 (deliberately zeroed to isolate the MECH-293 ghost-probe
    channel), benefit_terrain_enabled defaults False and is never set, and NUM_HAZARDS=0 leaves the
    remaining ARC-007 residue-field term (a harm-accumulation mechanism) with nothing to accumulate.
    The ghost-probe channel itself FUNCTIONS CORRECTLY (both preconditions P1/P2 met, confirmed
    bit-identical CLOSED-vs-NOGOAL trajectories per the driver's own calibration) but nothing
    downstream can preferentially select goal-tagged candidates, so C_MAIN's null is structurally
    near-guaranteed regardless of MECH-236's truth. Failure-location (GOV-FAILLOC-1): MEASURES
    FAILED, not chargeable to the claim. Routed /queue-experiment redesign (914b: pair
    wanting_weight>0 with the ghost-probe channel rather than isolating them, and/or restore a
    hazard/terrain signal). No substrate build owed (wanting_weight already exists as an unused
    lever). Status unchanged (candidate); epistemic_category standard (first evidence entry, no
    prior touchpoints).
  depends_on:
    - MECH-230
    - ARC-007
    - SD-004
  literature_evidence: |
    Ito et al. (2015) Nature [DOI: 10.1038/nature14396]:
    mPFC input via nucleus reuniens is causally required for trajectory-dependent
    CA1 firing during goal-directed navigation in rodents. When the prefrontal goal
    signal is disconnected, CA1 activity becomes position-encoding only and loses
    its trajectory-dependent (goal-conditioned) component. Direct causal evidence
    that a prefrontal-analogue goal signal is the mechanism that makes hippocampal
    activity goal-directed rather than purely positional. Maps onto the E3→hippocampal
    module wiring requirement for REE: z_goal must be read by the hippocampal module
    during trajectory generation, not only encoded within E3.
    Lit entry: targeted_review_mech_230/entries/2026-04-15_mech_230_prefrontal_hippocampal_goal_navigation_ito2015/
  notes: >
    MECH-230 establishes that z_goal exists as a structured latent attractor in E3.
    MECH-236 is the coupling claim: z_goal must be injected into the hippocampal
    trajectory proposal mechanism, not just maintained in E3. Without this wiring,
    MECH-230 can pass (z_goal has non-zero norm and structure) while hippocampal
    navigation remains undirected -- the map exists, the goal is encoded, but the
    navigator is not told where to go.

    TESTABLE PREDICTION: ablate the E3→hippocampal goal input while preserving E3
    goal encoding (MECH-230 intact). Trajectory proposals should degrade to
    position-based random walks; C3 goal-terrain navigation metric should fall to
    chance. Restore the input: navigation should recover proportional to z_goal_norm.

    IMPLEMENTATION NOTE: the goal-conditioning channel is distinct from the
    hippocampal viability map (residue field). The viability map tells the hippocampus
    where harm is; the goal signal tells it where to go. Both are required for
    avoid-harm-while-navigating-to-goal behavior.

    [2026-08-09 addendum, Stage 2 digestion of the 2026-08-08 "prospective navigation
    through goal topology" thought]: Yu et al. 2026 (Nat Neurosci, DOI 10.1038/
    s41593-026-02365-2) and Tang et al. 2026 (Nat Neurosci, DOI 10.1038/
    s41593-026-02364-3) both report hippocampal theta sweeps forming vectors toward
    remembered goal locations INDEPENDENT of the animal's current movement or head
    direction, with stronger goal-modulation preceding correct navigational choices --
    corroborating evidence (alongside the existing Ito et al. 2015 citation above)
    that destination-privileging is read out as a relational signal rather than from
    momentary position/heading. Located via web search during digestion, not a
    formal /lit-pull; full entries not yet added to a targeted_review_ directory.
    Grounds THIS claim's destination-privileging premise, not SD-098 (registered
    same session) directly -- SD-098's own literature_evidence field notes
    explicitly that neither paper speaks to node-type storage, the distinction
    SD-098 actually makes. See evidence/planning/thought_intake_2026-08-08_
    prospective_navigation_through_goal_topology.md for the full reconciliation.

- id: MECH-237
  title: "The z_goal attractor must be globally reachable across the z_goal sub-space — accessible from arbitrary latent starting states — not merely locally stable at a narrow basin; a locally-only-stable attractor fails to seed goal-directed hippocampal navigation from states far from the goal, producing approach behavior only when already near the goal."
  claim_type: mechanism_hypothesis
  subject: drive.goal_attractor_global_reachability
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  registered: "2026-04-15"
  implementation_phase: v3
  depends_on:
    - MECH-230
    - SD-012
  literature_evidence: |
    Naude et al. (2024) Nature Communications [DOI: 10.1038/s41467-024-53976-x]:
    Computational model (MAGNet) plus optogenetic validation in mice showing that
    dopamine-mediated synaptic plasticity widens goal attractor basins in recurrent
    networks. Without the motivational dopamine signal, the goal attractor exists but
    is locally stable only -- accessible from nearby states but not globally. With
    the signal, the basin is expanded so the attractor pulls from across the latent
    space. Maps directly onto why SD-012 homeostatic drive is gating MECH-230:
    the substrate for z_goal may exist, but without drive the attractor is too narrow
    for goal-directed navigation from arbitrary starting states.
    Lit entry: targeted_review_mech_230/entries/2026-04-15_mech_230_goal_latent_attractor_naude2024/
  notes: >
    Refinement of MECH-230. MECH-230 requires z_goal_norm > 0 (attractor exists and
    is structured). MECH-237 adds the reachability requirement: the attractor must be
    accessible from the agent's current state, not only from nearby states. This is
    the difference between a goal representation that the hippocampus can use as a
    navigation target vs. one that only activates when the agent is already close.

    The EXQ-085 series failures (z_goal_norm < 0.1 across all conditions) are
    consistent with a locally-stable but globally inaccessible attractor: z_goal
    exists in principle but the SD-012 drive is not amplifying the basin sufficiently
    for the agent to reach it from the starting state distribution in the experiment.

    TESTABLE PREDICTION: with SD-012 drive active, z_goal_norm should be elevated
    not only near goal-proximal states but also from states sampled uniformly across
    the environment. A globally reachable attractor produces high z_goal_norm from
    all regions; a locally reachable one produces high z_goal_norm only near the goal.
    EXQ-328b is testing the existence arm (MECH-230); a follow-on experiment testing
    from diverse starting states would test MECH-237 specifically.

- id: MECH-238
  title: "The hippocampal module encodes pairwise distance relations between states in the z_world latent manifold — not only positional tags — providing a metric structure that enables distance-based navigation toward goal states across the residue-field terrain."
  claim_type: mechanism_hypothesis
  subject: hippocampus.distance_metric_encoding
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  registered: "2026-04-15"
  implementation_phase: v3
  depends_on:
    - MECH-155
    - ARC-007
    - SD-005
  literature_evidence: |
    Theves, Fernandez & Doeller (2019) Current Biology [DOI: 10.1016/j.cub.2019.06.033]:
    After learning a novel 2D conceptual space (butterfly morphology features), the
    human hippocampus encodes Euclidean distances between abstract positions via both
    multivoxel pattern similarity and fMRI adaptation. This extends Constantinescu 2016
    from directional grid-like codes to full distance-metric encoding, confirming that
    hippocampal abstract-space coding is genuinely metric, not merely ordinal or
    directional. For REE: the residue-field terrain must be a metric space (knowing
    how far a state is from the goal, not just what state it is) for goal-directed
    trajectory proposals to be well-formed.
    Lit entry: targeted_review_connectome_mech_155/entries/2026-04-15_mech_155_feature_space_distances_theves2019/
  notes: >
    MECH-155 establishes that spatial navigation machinery generalises to abstract
    latent domains. MECH-238 adds the metric requirement: the generalised indexing
    substrate must encode distances, not just positions. The distinction matters for
    trajectory proposals: a lookup table (position → state) can retrieve any state
    given its tag, but only a metric map (state × state → distance) supports
    "navigate toward goal" as a well-defined operation.

    DESIGN CONSTRAINT: the hippocampal module's training objective must include a
    distance-preservation loss (or distance-respecting contrastive objective) over
    the z_world manifold, not only a reconstruction or classification objective.
    Without explicit metric supervision, the hippocampal representation may encode
    categorical structure (what state) but not metric structure (how far).

    RELATIONSHIP TO ARC-007 / SD-004: SD-004 proposes action objects as the
    hippocampal map backbone. MECH-238 says the backbone must be metric. These are
    compatible: action objects define the nodes; distances between nodes must be
    preserved in the representation.

    CURRENCY NOTE (2026-08-08, thought-digestion): "MECH-155 establishes that spatial
    navigation machinery generalises to abstract latent domains" (above) is
    superseded -- MECH-155's own wave-19 narrowed scope explicitly EXCLUDES any claim
    that spatial navigation is E1-internal, and instead affirms navigation runs in
    this claim's own HippocampalModule (ree-v3/ree_core/hippocampal/module.py).
    depends_on MECH-155 is retained for the shared-indexing-mechanism point only, not
    the navigation-generalizes-into-E1 framing. Separately: the HippocampalModule
    actually navigates ACTION-OBJECT space O (per SD-004), not raw z_world directly
    -- this claim's title says "z_world latent manifold," a real title/subject
    mismatch flagged for governance, not auto-corrected here since it changes claim
    scope. No distance-preservation/contrastive/metric-supervision training objective
    exists anywhere in ree_core as of this date (verified by grep) -- the only
    distance computation on z_world today (goal.py goal_distance/goal_proximity) is a
    raw MSE used by goal-tracking machinery, i.e. exactly the categorical/lookup
    alternative this claim needs to rule out.
  epistemic_category: substrate_conditional
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: the HippocampalModule already exists and performs
    trajectory search over ACTION-OBJECT space O (E2.action_object(z_world, a) -> o_t,
    per SD-004), not raw z_world directly -- this claim's title should be read as
    "distance relations over the hippocampal map's indexing space." No
    distance-preservation, contrastive, or metric-supervision training objective
    exists anywhere in ree_core as of this date. The only distance computation
    touching z_world today is a raw MSE used by goal-tracking machinery, not a
    learned hippocampal encoding -- i.e. the substrate currently looks exactly like
    the categorical/lookup alternative this claim needs to rule out. The claim is
    untestable as stated until a distance-preservation objective is added to
    HippocampalModule (or its action-object embedding) training.

    CONFIRMING: once a distance-preservation/contrastive loss is added, a
    representational-distance probe shows pairwise distances in the learned
    representation correlate with true manifold distances at a level above what an
    undifferentiated/categorical-only control achieves; AND trajectory proposals
    succeed at "navigate toward goal" tasks that specifically require distance
    comparison between non-adjacent candidate states (not just membership/retrieval
    of a previously-visited state), with performance degrading when the
    metric-supervision loss is ablated.

    FALSIFYING: representational-distance probes show no reliable correlation between
    the hippocampal module's internal similarity structure and true manifold
    distances, even after adding the metric-supervision objective -- the
    representation remains categorical/positional rather than metric regardless of
    training pressure; OR trajectory-proposal performance on distance-requiring
    navigation tasks is statistically indistinguishable from a
    lookup/nearest-neighbor-tag baseline that has no access to distance information,
    while performance on pure membership/retrieval tasks is unaffected -- showing the
    metric structure adds no functional value beyond what a position tag already
    provides.

- id: MECH-239
  title: "The hippocampal indexing substrate includes a temporal dimension implemented via time cells that tile elapsed time using the same phase-precession and theta-sequence machinery as place cells, enabling correct ordering of states within multi-step planning rollouts and working-memory sequences."
  claim_type: mechanism_hypothesis
  subject: hippocampus.temporal_indexing_time_cells
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  registered: "2026-04-15"
  implementation_phase: v3
  depends_on:
    - MECH-155
    - MECH-089
    - ARC-032
  literature_evidence: |
    Banquet et al. (2020) Progress in Neurobiology:
    Comprehensive review establishing time cells as the fourth dimension of the
    hippocampal spatial indexing substrate. Time cells tile elapsed time as place
    cells tile spatial extent, using identical phase-precession and theta-sequence
    machinery. Temporal sequential replay occurs offline using the same mechanism
    as spatial replay. Directly addresses the working-memory-ordering and
    planning-rollout sequencing sub-claims of MECH-155: the same indexing substrate
    that handles spatial and abstract latent-domain traversal also handles temporal
    ordering, using one unified mechanism rather than a separate system.
    Lit entry: targeted_review_connectome_mech_155/entries/2026-04-15_mech_155_time_cells_temporal_index_banquet2020/
  notes: >
    MECH-089 (ThetaBuffer / cross-frequency packaging) establishes that theta
    oscillations implement sequential traversal. MECH-239 adds the hippocampal
    substrate specificity: time cells in hippocampus proper tile the temporal
    dimension using the same phase-precession mechanism as place cells, making the
    temporal index a first-class dimension of the hippocampal map rather than an
    add-on.

    CRITICAL FOR PLANNING ROLLOUT: multi-step trajectory proposals require the
    hippocampus to sequence states in correct temporal order, not just select states
    that are spatially/abstractly close to the goal. A hippocampal module without
    temporal indexing would produce trajectory fragments that are locally coherent
    but temporally unordered -- the correct states retrieved in the wrong sequence.

    RELATIONSHIP TO MECH-089: MECH-089 is the oscillatory mechanism (theta frequency
    as traversal clock); MECH-239 is the representational substrate (time cells as
    the indexed variable). Both are required: time cells without theta sequencing
    produce a static representation of elapsed time; theta sequencing without time
    cells has no temporal variable to sequence over.

    NOTE: offline temporal replay (consolidation during quiescence) also depends on
    time cells. This connects MECH-239 to INV-049 (offline consolidation necessity)
    and MECH-092 (SWR-equivalent replay during micro-DMN). Time cells are both a
    planning-rollout mechanism and a consolidation substrate.

# ─── Improvement-tier claims (implementation_phase: v4) ─────────────────────

- id: MECH-240
  title: "SD-012 homeostatic drive_level dynamically scales z_goal attractor basin width — high drive widens the basin so the goal attractor is globally accessible, low drive narrows it to local stability only — enabling motivational-state-dependent modulation of goal-directedness without permanently widening the basin."
  claim_type: mechanism_hypothesis
  subject: drive.goal_attractor_basin_scaling
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  registered: "2026-04-15"
  implementation_phase: v4
  depends_on:
    - MECH-237
    - SD-012
  literature_evidence: |
    Naude et al. (2024) Nature Communications [DOI: 10.1038/s41467-024-53976-x]:
    MAGNet model plus optogenetic validation: motivational dopamine (drive signal)
    widens attractor basins dynamically. The widening is proportional to the
    motivational signal strength, not binary. Predicts that drive_level in REE
    should modulate z_goal attractor geometry continuously, not just gate the
    attractor's existence. An agent with high drive should navigate to goals from
    farther starting states than an agent with the same goal encoding but lower drive.
    Lit entry: targeted_review_mech_230/entries/2026-04-15_mech_230_goal_latent_attractor_naude2024/
  notes: >
    IMPROVEMENT-TIER CLAIM. The system can function with MECH-237 satisfied by a
    fixed wide attractor (always globally accessible). MECH-240 adds the motivational
    dynamics: basin width tracks drive_level, producing a tight, energy-efficient
    attractor at low drive that expands under high drive. This matters for behavioral
    flexibility (an agent with no current hunger should not be strongly pulled toward
    food locations) and metabolic efficiency (narrow basins are cheaper to maintain).

    PRIORITY: implement after MECH-237 is verified experimentally. MECH-240 requires
    a working globally-reachable attractor (MECH-237) before the dynamic scaling can
    be meaningfully tested. Implementing MECH-240 before MECH-237 risks conflating
    the two failure modes (attractor doesn't exist vs. attractor isn't drive-scaled).

    IMPLEMENTATION NOTE: in the SD-012 substrate, drive_weight already scales the
    goal contribution to action selection. MECH-240 proposes that drive_weight (or a
    related parameter) should additionally scale the attractor width in the z_goal
    representation itself -- a change to E3's goal encoding dynamics, not just to
    the action selection weighting.

- id: MECH-241
  title: "During active goal pursuit, hippocampal and OFC-analogue representations of goal-proximal states undergo compression -- goal locations cluster more tightly in neural state space -- improving signal-to-noise for goal-directed trajectory selection; the degree of compression predicts planning efficiency across episodes."
  claim_type: mechanism_hypothesis
  subject: hippocampus.goal_state_representational_compression
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  registered: "2026-04-15"
  implementation_phase: v4
  depends_on:
    - MECH-236
    - MECH-238
    - MECH-230
  literature_evidence: |
    Muhle-Karbe et al. (2023) Neuron [DOI: 10.1016/j.neuron.2023.08.021]:
    During goal-seeking, hippocampal and OFC neural codes compress around goal
    locations -- goal states cluster together in neural state space. The degree of
    compression predicts learning success on the task. Compression is specifically
    goal-related (it does not affect non-goal locations equally) and emerges during
    active pursuit. For REE: goal-proximal states in z_world could be represented
    at higher density/lower noise during active goal pursuit, making the goal attractor
    sharper and the trajectory gradient cleaner.
    Lit entry: targeted_review_mech_230/entries/2026-04-15_mech_230_goal_seeking_hippocampus_ofc_muhlekarbe2023/
  notes: >
    IMPROVEMENT-TIER CLAIM. Goal-directed navigation works without representational
    compression (MECH-236 + MECH-237 + MECH-238 are sufficient for basic function).
    Compression is an efficiency gain: a tighter goal cluster produces a sharper
    gradient in the metric space, which translates to more reliable trajectory
    convergence, especially in environments with many distractors or long paths.

    RELATIONSHIP TO MECH-238: MECH-238 requires distance-metric encoding.
    MECH-241 says the metric geometry is not static -- it sharpens dynamically
    around current goals during active pursuit. The two are compatible: the metric
    space is always present, but its local geometry is goal-pursuit-modulated.

    INTERESTING QUESTION FOR GOVERNANCE: is compression a cause of efficient
    navigation (implement it to improve performance) or a consequence (it emerges
    when the system is working well)? The Muhle-Karbe finding is correlational
    (compression predicts success) rather than causal manipulation. Worth flagging
    as a candidate for V4 governance review rather than assuming the causal direction.

- id: MECH-242
  title: "Hippocampal trajectory construction operates via two dissociable mechanisms: (1) pattern completion for familiar trajectories (hippocampus-proper), and (2) vector-based construction for novel trajectories assembled from relational components (parahippocampal/parietal-analogue); pattern completion alone is sufficient for navigation within a learned residue field, but vector-based construction is required for generalisation to novel environments not represented in the existing map."
  claim_type: mechanism_hypothesis
  subject: hippocampus.dual_mode_trajectory_construction
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  registered: "2026-04-15"
  implementation_phase: v4
  depends_on:
    - MECH-155
    - MECH-238
    - ARC-007
  literature_evidence: |
    Morton, Schlichting & Preston (2020) PNAS [DOI: 10.1073/pnas.1911338117]:
    Hippocampus and frontoparietal cortex form shared geometric representations of
    abstract relational event structure that support transitive inference (AC from
    AB+BC). Critically, the paper dissociates hippocampal pattern completion (familiar
    trajectory recall from partial cues) from parahippocampal/parietal vector-based
    retrieval (novel trajectory construction from relational components). Both
    mechanisms operate on the same abstract-space representation but via distinct
    circuits with different computational profiles.
    Lit entry: targeted_review_connectome_mech_155/entries/2026-04-15_mech_155_abstract_event_structure_morton2020/
  notes: >
    IMPROVEMENT-TIER CLAIM. V3 hippocampal experiments operate in a learned residue
    field -- the agent navigates a known environment. Pattern completion (mechanism 1)
    is sufficient for this: the hippocampus retrieves familiar trajectories from
    partial state cues. V3 does not require novel trajectory assembly from relational
    components.

    Vector-based construction (mechanism 2) becomes necessary when:
    (a) the agent must navigate to goals in environments not seen during training
    (zero-shot transfer), or (b) the agent must construct plans by combining
    sub-trajectories learned in separate contexts (compositional planning).
    Both are V4 requirements.

    ARCHITECTURAL IMPLICATION FOR V4: the parahippocampal/parietal-analogue circuit
    in REE would be a separate module from the hippocampal module proper. It would
    operate on the same z_world metric space (MECH-238) but via a different
    computational mechanism -- vector arithmetic over relational components rather
    than attractor-based pattern completion. The distinction tracks the ARC-021
    three-loop architecture: different loops may use different trajectory construction
    mechanisms appropriate to their planning horizons and environmental familiarity.

- id: MECH-243
  title: "A dedicated hippocampal output pathway (analogous to vCA1 to nucleus accumbens shell) specifically encodes approach bias during motivational conflict -- separately from harm avoidance -- cleanly dissociating the approach signal at the action-selection interface; inhibition of this pathway defaults action selection to avoidance without disrupting harm representation."
  claim_type: mechanism_hypothesis
  subject: hippocampus.approach_bias_output_pathway
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  registered: "2026-04-15"
  implementation_phase: v4
  depends_on:
    - MECH-230
    - ARC-007
    - MECH-069
  literature_evidence: |
    Patterson et al. (2025) PLoS Biology [DOI: 10.1371/journal.pbio.3002722]:
    vCA1 to nucleus accumbens shell pathway specifically encodes approach bias during
    motivational conflict. Inhibiting this pathway produces avoidance default without
    affecting pure avoidance behavior or anxiety measures. The pathway is selective
    for approach signal during conflict, not for reward magnitude per se. Supports
    clean pathway-level dissociation between approach encoding and harm avoidance
    at the hippocampal output stage.
    Lit entry: targeted_review_mech_230/entries/2026-04-15_mech_230_vhpc_approach_avoidance_separation_patterson2025/
  notes: >
    IMPROVEMENT-TIER CLAIM. The system can navigate toward goals and avoid harm
    without a dedicated approach-bias output pathway (MECH-230 + MECH-236 + MECH-237
    are sufficient for goal-directed navigation; MECH-069 handles approach/avoidance
    incommensurability at the error signal level). MECH-243 is about output fidelity:
    a dedicated approach channel produces a cleaner approach signal at the
    action-selection interface, particularly during motivational conflict where
    approach and avoidance signals compete.

    RELATIONSHIP TO ARC-057: ARC-057 proposes approach behavior emerges from
    curiosity drive + DA-expanded hippocampal representation. MECH-243 is compatible
    with ARC-057: the dedicated output pathway could be the circuit through which the
    curiosity-driven approach signal reaches the action-selection interface, while
    a separate harm-avoidance pathway carries the conflict signal. ARC-057 explains
    why approach occurs; MECH-243 explains how the approach signal is transmitted
    cleanly to motor output.

    IMPLEMENTATION NOTE: in the current CausalGridWorld single-action-dimension
    substrate, approach and avoidance share the same action channel, making MECH-243
    difficult to test. This pathway dissociation becomes testable in a richer substrate
    where approach (move toward) and avoidance (move away) can be measured as
    independent output dimensions during simultaneous motivational conflict.
    Deferred to V4 accordingly.

# Registered 2026-04-16 -- psychiatric failure mode cluster (confabulation/psychosis/hallucination distinction)
# Source: clinician review of MECH-094 precision -- mechanistic disambiguation of tag-loss vs precision failure vs generative dominance
- id: MECH-244
  title: "Psychosis is a precision-weighting failure where prior beliefs persistently dominate sensory evidence; the generative model becomes self-sealing and sensory update is suppressed."
  claim_type: mechanism_hypothesis
  subject: psychiatric.psychosis_precision_weighting_failure
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  claim_level: functional
  depends_on:
    - MECH-094
    - INV-011
  location: docs/architecture/default_mode.md
  source:
    - docs/architecture/default_mode.md
  notes: >
    Distinct from confabulation (MECH-094 simulation/real distinction failure). In
    confabulation, simulated content is written to the residue field as real because
    the source-monitoring and/or temporal context mechanisms fail -- the agent later
    "remembers" the simulation as real experience. In psychosis (this claim, MECH-244),
    the failure is upstream: the agent's current-moment generative predictions persistently
    outweigh sensory evidence, producing beliefs that contradict incoming percepts.
    Mechanistically: precision on sensory prediction errors (z_e1, z_e2 mismatch signals)
    is downregulated relative to prior-belief precision, so PE cannot correct the model.
    Clinical correlate: dopaminergic dysregulation of precision-weighting (Friston/Corlett
    active inference account; Adams et al. 2013, Corlett et al. 2009). In REE terms: the
    E1/E2 bottom-up error signal is discounted relative to the top-down prediction; the
    world model becomes unfalsifiable by experience. This is not source-monitoring failure
    -- the simulation/real distinction (MECH-094) may be intact -- but a failure of
    Bayesian updating from the environment.
    MECH-244 is one of three pathways to psychotic symptoms in the REE framework. All
    three are mechanistically distinct and therapeutically distinguishable:
    (A) MECH-244 (this claim): DA/NMDA precision-weighting failure -- prior beliefs
    dominate because the error signal is compromised. Strong evidence (Adams 2013,
    Corlett 2009). Responds to antipsychotics targeting DA/NMDA precision mechanisms.
    (B) MECH-246: Signal-degradation / pareidolia pathway -- sufficiently noisy or
    ambiguous bottom-up signals produce false percepts via correct Bayesian inference
    on degraded input, without formal precision failure. Early psychosis as-if quality,
    stimulant-induced distortions. May respond to perceptual/environmental intervention.
    (C) MECH-247: Trauma-shaped hypervigilant prior pathway -- pathological prior
    structure generates strong top-down predictions from minimal threat-relevant signals.
    Precision weighting may be intact; the Bayesian update is working correctly on
    pathological priors. Responds to trauma-focused therapy rather than (or in addition
    to) antipsychotics. Psychosis is likely multifactorial in both genesis and
    maintenance; individual patients may have contributions from all three pathways.
    Registered 2026-04-16: pathways B and C added based on clinical input from Daniel
    Golden (consultant psychiatrist); registered as MECH-246, MECH-247.
  evidence_quality_note: |
    [2026-07-29 governance, V3-EXQ-826 + V3-EXQ-826a non_contributory; failure_autopsy_batch-822a-826-817a-827_2026-07-26
    + failure_autopsy_2026-07-28-sweep, both confirmed]: first two experimental tests, both non_contributory.
    826 (2026-07-26, first test): E1 never received a training gradient at all -- the script's random-action
    rollout never calls agent.act*(), so compute_prediction_loss() never engages; no interpretable signal.
    826a (2026-07-27, E1-tick-bug fix over 826): confirms the fix works -- pe_precision_manipulation_took now
    passes (2.06 vs 1.5 floor), E1 genuinely trains. This is the first non-buggy readiness read for MECH-244.
    Two readiness gates still fail (phase-A convergence ratio 0.152 vs 0.25 floor; regime-change
    disconfirmation 0.966 vs 1.15 floor) despite 240000 E1 ticks. MECH-244 remains untested (exp_conf=0.0).
    Stays candidate; no direction yet.

- id: MECH-245
  title: "Hallucination is a generative-model dominance failure where top-down predictions produce percepts without corresponding bottom-up sensory grounding."
  claim_type: mechanism_hypothesis
  subject: psychiatric.hallucination_generative_dominance
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  claim_level: functional
  depends_on:
    - MECH-094
    - MECH-244
    - INV-011
  location: docs/architecture/default_mode.md
  source:
    - docs/architecture/default_mode.md
  notes: >
    Distinct from confabulation (MECH-094) and psychosis (MECH-244). Confabulation is a
    memory encoding failure (simulated content written as real). Psychosis is a belief
    updating failure (current beliefs resist sensory correction). Hallucination is a
    perceptual generation failure: the generative model (E1/E2 predictions) produces
    outputs in the sensory channels that are experienced as percepts, in the absence of
    corresponding environmental stimuli. In REE terms: E1 top-down predictions propagate
    forward as if they were received sensory signals, bypassing or overwhelming the
    bottom-up mismatch check. The agent perceives things that are not there. Clinical
    correlates: psychotic hallucinations (auditory verbal), hypnagogic hallucinations
    (sleep onset, generative model insufficiently suppressed -- see MECH-094 notes re
    REM), Charles Bonnet syndrome (deafferentation releasing generative prediction).
    Relationship to MECH-094: the simulation/real distinction (MECH-094) is NOT the
    primary protection against hallucination; that distinction governs write-through to
    phi(z), not percept generation. Hallucination prevention requires that bottom-up PE
    signals remain competitive with top-down predictions -- a precision balance issue
    (related to MECH-244) rather than a source-monitoring issue. However, hallucinations
    during REM may involve compound failure: MECH-094 notes that hallucinatory content
    during REM can accumulate in the residue field if the simulation/real mechanisms are
    compromised -- this is a confabulation+hallucination compound failure.
  evidence_quality_note: |
    [2026-07-29 governance, V3-EXQ-825 supports; PASS, all criteria clean]: first experimental test.
    A GRU forward model stand-in for E1/E2's top-down generative pathway, trained on synthetic
    momentum z_world dynamics then rolled through a grounding phase followed by a deafferentation
    window feeding the real E3TrajectorySelector's bottom-up-mismatch-check pathway with the
    generative model's own predictions rather than ground truth. All three criteria pass at 3/3
    seeds: C1 (confidence collapse), C2 (divergence magnitude), C3 (manipulation validity). Supports
    the generative-model-dominance mechanism. Stays candidate.

# Registered 2026-04-16 -- Papez circuit lit-pull discussion
# Source: clinical input from Daniel Golden (consultant psychiatrist) + discussion of
#         Adams 2013, Ffytche 2005, Corlett 2009 evidence limitations
- id: MECH-246
  title: "Psychotic-like symptoms can arise from signal-degradation pareidolia: sufficiently noisy or ambiguous bottom-up perceptual signals allow correct Bayesian inference to produce false percepts, without requiring formal precision-weighting failure."
  claim_type: mechanism_hypothesis
  subject: psychiatric.pareidolia_signal_degradation_pathway
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  claim_level: functional
  depends_on:
    - MECH-244
    - MECH-245
  location: docs/architecture/default_mode.md
  source:
    - docs/architecture/default_mode.md
  notes: >
    Distinct from MECH-244 (precision-weighting failure) and MECH-245 (generative model
    dominance from absent bottom-up input). In this pathway, the precision-weighting
    system is intact but the bottom-up signal is too degraded, sparse, or ambiguous to
    constrain Bayesian inference sufficiently. The generative model makes pattern-
    completion errors that happen to be wrong -- not because the model resists updating,
    but because the evidence is insufficient to correct strong priors. The agent is doing
    correct Bayesian inference on bad data. Clinical correlates: perceptual distortions
    under sleep deprivation, stimulant-induced psychosis without clear delusional
    structure, early psychosis with as-if quality before full conviction develops,
    sensory threshold alterations from intoxication or metabolic disturbance. This
    pathway predicts that symptoms should remit when signal quality is restored
    (sleep, sobriety, resolution of metabolic disturbance) and may not require
    antipsychotic precision correction. Distinguished from MECH-244 by preserved
    insight and signal-dependency of symptoms; distinguished from MECH-245 by the
    presence of bottom-up input (however degraded) rather than its absence. Pending
    targeted literature evidence (EVB-0137). Registered 2026-04-16 from clinical
    discussion with Daniel Golden (consultant psychiatrist).

- id: MECH-247
  title: "Trauma-shaped hypervigilant priors can generate psychotic-like symptoms by structuring top-down predictions to respond strongly to minimal threat-relevant signals, producing perceptual errors and false beliefs without primary precision-weighting dysregulation."
  claim_type: mechanism_hypothesis
  subject: psychiatric.trauma_prior_psychosis_pathway
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  claim_level: functional
  depends_on:
    - MECH-094
    - MECH-244
    - MECH-245
    - MECH-076
  location: docs/architecture/default_mode.md
  source:
    - docs/architecture/default_mode.md
  notes: >
    Distinct from MECH-244 (precision-weighting failure) and MECH-246 (signal-
    degradation pareidolia). In this pathway, the prior distributions themselves are
    pathologically structured by traumatic experience: threat-relevant signals generate
    strong top-down predictions that trigger MECH-245-like generative dominance from
    minimal bottom-up evidence. Precision weighting may be intact -- the Bayesian update
    is working correctly on pathological priors shaped by past real harm. The terrain
    deformation mechanism (MECH-076) may be directly involved: accumulated residue from
    real trauma deforms phi(z) in ways that generate threat-organized priors for future
    perception and inference. Clinical correlates: trauma-organized paranoid ideation,
    pseudo-hallucinations in complex PTSD, psychotic symptoms organized around themes
    from real past threat experience, voice-hearing content organized around themes of
    abuse or threat. This pathway predicts that normalizing precision-weighting
    (antipsychotics) may be insufficient without also addressing the prior structure
    (trauma-focused therapy). Distinguishes psychosis arising from prior-shaping via
    real experience from psychosis arising from neurochemical precision dysregulation.
    The REE mechanism: real harm accumulated in phi(z) via MECH-076 creates terrain
    that generates expectation-congruent threat predictions even from minimal sensory
    input. Pending targeted literature evidence (EVB-0138). Registered 2026-04-16 from
    clinical discussion with Daniel Golden (consultant psychiatrist).

- id: MECH-248
  title: "Source monitoring (frontoparietal network; Johnson-Raye tradition) is a candidate biological implementation of the simulation/real distinction required by MECH-094 -- distinguishing internally generated from externally received content at the level of memory encoding and attribution."
  claim_type: mechanism_hypothesis
  subject: default_mode.source_monitoring_frontoparietal_implementation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  claim_level: implementation
  depends_on:
    - MECH-094
    - INV-011
    - INV-019
  location: docs/architecture/default_mode.md
  source:
    - docs/architecture/default_mode.md
  notes: >
    Johnson and Raye's source monitoring framework identifies a frontoparietal network
    responsible for distinguishing the origin of mental content: internally generated
    (imagination, planning, rehearsal) vs. externally received (perception). Source
    monitoring failure corresponds directly to MECH-094's functional failure: the agent
    cannot determine whether a memory trace was internally generated (should not update
    phi(z) as committed experience) or received externally (should). This is a more
    direct implementation candidate for MECH-094's functional requirement than the OFC
    temporal context filter (Schnider 2003), which addresses relevance-gating of real
    memories rather than the simulation/reality distinction per se. The prefrontal-
    parietal basis of reality monitoring is supported by imaging and lesion studies
    (Mitchell 2009, Simons et al. 2008); the precise mapping to REE's phi(z) write
    pathway requires targeted literature review. Corollary discharge and efference copy
    (Frith 1992, 2014) are related mechanisms for self/other attribution that may
    implement an analogous distinction at the motor prediction level. The REE
    architecture requires source monitoring to operate not just for motor content but for
    all internally generated trajectories, including imaginative simulation and
    hippocampal replay. Pending targeted literature evidence (EVB-0134). Registered
    2026-04-16 following identification of Schnider 2003 temporal/epistemic distinction
    gap in MECH-094 mapping.

- id: MECH-249
  title: "Acetylcholine/noradrenaline balance implements action-mode-specific hippocampal write profiles as a continuous neuromodulatory state variable, providing the biological substrate of mode-dependent memory encoding differences across deliberate action, habit, simulation, SWS, and REM states."
  claim_type: mechanism_hypothesis
  subject: default_mode.neuromodulatory_encoding_mode_profile
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  claim_level: implementation
  depends_on:
    - MECH-094
    - MECH-030
    - MECH-121
    - MECH-122
  location: docs/architecture/default_mode.md
  source:
    - docs/architecture/default_mode.md
  notes: >
    Hippocampal encoding mode is continuously set by the ACh/NE neuromodulatory context
    rather than by a discrete categorical gate. High ACh + high NE (aroused, deliberate
    waking action) strongly enables encoding; low ACh + low NE (automatic/habitual
    action) reduces hippocampal dependence and engagement; high ACh + low aminergic
    tone (REM sleep) produces theta oscillations and reactivation without the aminergic
    modulation that supports reality monitoring (Hasselmo 1999, 2006; Hobson and
    Pace-Schott 2002). SWS consolidation operates in a distinct neuromodulatory register:
    low ACh, declining NE, delta dominance, SWR-mediated hippocampus-to-cortex transfer.
    These state differences are continuous and overlapping, not binary transitions. In
    REE terms, the write profile for each action mode is an emergent property of the
    neuromodulatory context, not a discrete tag applied to content. MECH-094's functional
    requirement (simulation content must not update phi(z) as real experience) is
    partially implemented by the neuromodulatory conditions that differ between deliberate
    waking encoding and simulation/REM states. The REM vulnerability identified in Hobson
    and Pace-Schott 2002 (aminergic withdrawal during REM removing one layer of
    multi-mechanism protection) is a direct prediction of this account: neuromodulatory
    write-protection is degraded during REM, making it the state most vulnerable to
    simulation/real confusion (MECH-094 compound failure). The architectural implication
    is that the write-profile is a graded, state-dependent property of the encoding
    context, not a switch that is either on or off. Pending targeted literature evidence
    (EVB-0135). Registered 2026-04-16 following identification of the action-mode
    write-profile gap and discussion of Hobson 2002 neuromodulatory findings.

- id: MECH-250
  title: "The end of exhalation (expiration phase) provides a hard, periodic release
    signal for committed motor programs via respiratory-motor coupling -- a breath-timed
    gate-release mechanism distinct from hippocampal completion (ARC-028) and urgency
    interrupt (MECH-091)."
  claim_type: mechanism_hypothesis
  subject: control_plane.respiratory_phase_gate_release
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  claim_level: implementation
  depends_on:
    - MECH-090
    - MECH-108
    - ARC-028
    - MECH-091
  location: docs/architecture/control_plane_heartbeat.md
  source:
    - docs/architecture/control_plane_heartbeat.md
  notes: >
    Observed across species that planned motor actions are preferentially released at
    the end of exhalation: dogs time action initiation to a sharp outbreath; vocal
    production in humans is almost exclusively on expiration; skilled motor actions
    (e.g., throwing, striking) cluster at the exhale-end phase. The mechanism is
    respiratory-motor coupling: relaxation of the diaphragm and accessory respiratory
    muscles at end-expiration reduces core tension, providing a brief window of reduced
    postural load in which committed motor programs can propagate to effectors without
    fighting against the inspiratory drive. In REE terms this is a THIRD release
    pathway for the MECH-090 bistable beta gate (alongside hippocampal completion
    ARC-028 and urgency interrupt MECH-091): the expiratory phase-boundary fires a
    hard gate-release signal, briefly opening the policy-propagation channel regardless
    of hippocampal completion quality or harm escalation. Crucially, this is
    mechanistically distinct from MECH-108 (BreathOscillator), which already softens
    the commit threshold during the sweep phase to create periodic uncommitted windows.
    MECH-250 is not a threshold-softening effect but a positive release trigger: it
    actively drives beta_gate.release() at the exhale-end boundary, enabling biological
    breath-timed action initiation. The coupling also explains why the BreathOscillator
    sweep phase (MECH-108) and the gate release event (MECH-250) can be temporally
    coordinated: the sweep opens the evaluation window and the respiratory boundary
    fires the release, yielding breath-organised motor output. Implementation would
    extend BetaGate with receive_respiratory_phase(phase) -> release at EXHALE_END,
    driven by the MultiRateClock breath cycle state. Registered 2026-04-16 from
    empirical observation (dogs releasing planned actions with a sharp outbreathing)
    during MECH-090 bistable gate development.

- id: SD-026
  title: "Goal-directed perception requires a prospective precision-template write channel from z_goal to the E1 prior, anatomically analogous to the dorsal attention network (FEF/IPS -> V1) feedback, distinct from residue-geometry (history-driven) sampling bias."
  claim_type: design_decision
  subject: attention.prospective_precision_write_channel
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  claim_level: architectural
  registered_utc: 2026-04-17
  depends_on:
    - SD-012
    - SD-014
    - INV-034
    - INV-037
    - INV-038
    - INV-009
    - MECH-007
  epistemic_category: substrate_conditional
  what_would_answer: |
    Non-degeneracy precondition (must ALL hold before this claim is
    testable by construction, not merely unfavored): (a) SD-014 reaches
    >= provisional -- status as of this digestion: still candidate, and
    the only direct evidential run to date (V3-EXQ-887, 2026-08-04,
    weakens, unreviewed -- absent from review_tracker.json, listed in
    pending_review.md) FAILED both load-bearing criteria C1
    (functional_drive_gating) and C2 (representational_separability) on
    all 3 seeds, with only C3 (replay_set_selectivity) passing. Note for
    whoever revisits this: the write-path dimension-mismatch bug that
    made the EARLIER V3-EXQ-432 run vacuous (n_surprise_writes=0) was a
    genuinely buildable defect and IS fixed (ree-v3 32edd553,
    2026-08-04, pinned by test_do_replay_drive_state_width.py) -- but
    that fix is already reflected IN the 887 run's substrate, and 887
    still failed C1/C2 on the corrected substrate. So the SD-014 gate is
    not "waiting on a code fix"; it is sitting on an unreviewed
    weakening result that, if confirmed on governance/autopsy review,
    argues against the 4-channel node-valence design SD-026 depends on
    for a "crisp resource representation," not merely against SD-014's
    timeline. (b) SD-026 itself is implemented: currently fully unbuilt
    -- no dan_feedback/template_proj code exists anywhere in
    ree_core/ (verified by direct grep), no commit in either repo's full
    history ever touched those symbols, no queue entry references
    SD-026/MECH-251/dan_feedback. The "do not queue until SD-014 >=
    provisional" hold in this claim's own evidence_quality_note is being
    honored.

    Once both preconditions clear, the pre-registered five-step
    validation sequence (queued post-SD-014, per this claim's own note)
    is: (a) verify z_goal seeds (SD-012 -- already provisional, not
    gating); (b) verify z_resource separates (SD-014); (c) enable/
    disable the dan_feedback channel as ablation; (d) measure
    benefit_ratio against a random baseline; (e) measure feature-channel
    precision on goal-relevant vs goal-irrelevant dimensions.

    CONFIRMING: dan_feedback=on lifts benefit_ratio substantially over
    dan_feedback=off, AND goal-relevant feature-channel precision
    exceeds irrelevant-channel precision specifically under
    dan_feedback=on (not as an artifact of the ablation toggle alone).

    FALSIFYING (primary): dan_feedback=off performs equivalently to
    dan_feedback=on, replicating the EXQ-085h-l seed-but-inert pattern
    regardless of ablation state -- meaning z_goal's behavioral effect,
    if it exists at all, is fully mediated by pathways other than this
    claimed channel (e.g. E3 trajectory re-ranking alone, per this
    claim's own functional_restatement of the pre-SD-026 failure mode).

    FALSIFYING (secondary, SEPARABLE-FROM boundary): even if SOME
    benefit_ratio lift is observed under dan_feedback=on, the "distinct
    channel" half of this claim is separately weakened if dan_feedback's
    effect cannot be dissociated from residue-geometry's (ARC-007)
    effect -- e.g. via the two channels' claimed distinguishing signature
    (fast-decay prospective write vs slow-decay retrospective write on
    the shared additive E1-prior register): if ablating dan_feedback
    while holding ARC-007 residue geometry fixed produces no differential
    change in the fast-timescale component of E1-prior bias, or if the
    effect's decay-constant profile matches ARC-007's slow dynamics
    rather than the claimed fast dynamics, the two "distinct channels"
    have collapsed to one and SD-026's separability claim (not just its
    behavioral-lift claim) is falsified even where benefit_ratio moved.
    This is the same architectural-distinctness question INV-012's Leg 1
    stages on 4 axes (causality direction, neuromodulatory gating,
    clinical dissociation, representational format) -- here it is
    SD-026's own empirical boundary test, checkable once the channel is
    built, independent of INV-012's derivational argument.
  functional_restatement: >
    SD-012 establishes that drive-scaled benefit can seed z_goal. EXQ-085f/g
    confirmed z_goal_norm > 0.1 is achievable. EXQ-085h through 085l then
    failed navigation (benefit_ratio) consistently: seeding works, but
    z_goal is behaviorally inert because no channel exists by which it
    biases E1 sampling toward goal-relevant features. Residue geometry
    (ARC-007) biases sampling toward previously-mattering locations
    (history-driven) but cannot bias toward prospective template features
    (goal-driven). Without a prospective write channel, z_goal can only
    affect behavior via E3 trajectory re-ranking after the fact;
    perception itself remains goal-agnostic.

    Proposed mechanism (V3 minimum): z_goal is projected through a
    template projection into a precision-space vector, written additively
    to the E1 prior via a dedicated dan_feedback channel with fast decay
    (tau on the order of seconds in env-steps). Residue geometry retains
    its slow decay. Both channels write to the same E1-prior register via
    additive combination; sources and dynamics are distinct.

    SEPARABLE-FROM (not a dependency, a boundary): ARC-007 (residue
    geometry). ARC-007 and SD-026 share the E1-prior register but are
    distinct sources with distinct dynamics -- ARC-007 writes from
    retrospective outcomes with slow decay; SD-026 writes from prospective
    intent with fast decay.

    STRUCTURAL LOAD-BEARING FOR: INV-012 (responsibility through
    commitment). Without a prospective channel, REE has no mechanism
    for *willed* perception -- only reflexive residue-driven attention.
    INV-012's coherence requires deliberate vs reflexive attention to be
    mechanistically distinguishable; SD-026 is the mechanism that makes
    that distinction possible. INV-012 depends_on updated accordingly.
  notes: >
    Anatomical grounding: Corbetta & Shulman (2002) dorsal attention
    network (FEF + IPS) writes feedback to V1/V4 producing prospective
    gain modulation before stimuli arrive. Moore & Armstrong (2003): FEF
    stimulation produces V4 selectivity shifts for attended features.
    Summerfield et al. (2006): prospective template distinct from
    reactive adjustment. In predictive-processing terms, DAN writes
    "expected precision" on specified feature channels.

    Decomposition (see docs/thoughts/2026-04-17_attention_coverage_audit.md):
    H3 (template as first-class precision-space object, compiled from
    stored goal by vmPFC-dlPFC) is deferred to V4. SD-026 is the V3
    minimum: a differentiable projection z_goal -> template_vec ->
    additive prior bias is sufficient to validate the channel exists
    and is separable from residue.

    Boundary vs neighboring claims:
    - MECH-081 (NA as attentional snap): MECH-081 is EXOGENOUS / bottom-up
      stimulus-driven salience. SD-026 is ENDOGENOUS / top-down goal-driven
      template. Both may write gain to E1 but sources are orthogonal; they
      are complementary, not redundant. Full attention system = NA snap
      (MECH-081) + DAN template (SD-026) + fragmentation across control
      axes (MECH-007).
    - ARC-044 (attentional lapses for exploration): ARC-044 governs the
      TEMPORAL gating of attention engagement (when to release into
      exploration). SD-026 governs the CONTENT of attention when engaged
      (what to amplify). Orthogonal axes -- ARC-044 can release a SD-026
      template, and SD-026 templates can be re-engaged after ARC-044
      lapses. Neither subsumes the other.
    - ARC-007 (residue geometry): see SEPARABLE-FROM above.

    Pressure-tested against residue-redundancy hypothesis: distinct on
    direction of causality (retrospective vs prospective), neuromodulation
    (DA-gated vs ACh/NA-gated), clinical dissociation (amnestic vs frontal
    syndromes), representational format (episodic specificity vs
    cross-situational feature generality), and REE-specific ethical
    commitment argument (INV-012 requires deliberate vs reflexive
    attention to be distinguishable). Registered 2026-04-17 from
    attention coverage audit session.
  evidence_quality_note: |
    No direct experiments yet. Testing is gated on SD-014 (z_resource
    separation): without a crisp resource representation, a DAN template
    ablation is confounded -- template quality and target quality cannot
    be disentangled. Do not queue SD-026 validation EXQ until SD-014 is
    at least provisional.

    Predicted failure mode in EXQ-085h-l navigation (z_goal seeds but
    does not bias sampling) is consistent with SD-026 being the missing
    piece. First validation experiment (queued post-SD-014) should:
    (a) verify z_goal seeds (SD-012), (b) verify z_resource separates
    (SD-014), (c) enable/disable dan_feedback channel as ablation,
    (d) measure benefit_ratio against random baseline, (e) measure
    feature-channel precision on goal-relevant vs irrelevant dimensions.
    Prediction: dan_feedback=on lifts benefit_ratio substantially;
    dan_feedback=off replicates EXQ-085h-l behavior (seed-but-inert).

- id: MECH-251
  title: "z_goal projects to a precision template vector that is additively written to the E1 prior via the dan_feedback channel with fast exponential decay, separable in dynamics and source from residue-geometry bias."
  claim_type: mechanism_hypothesis
  subject: attention.dan_feedback_precision_write
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  claim_level: mechanistic
  registered_utc: 2026-04-17
  depends_on:
    - SD-026
    - ARC-007
    - INV-009
  functional_restatement: >
    Implementation for SD-026. template_vec = template_proj(z_goal) where
    template_proj is Linear(goal_dim, prior_dim). effective_prior =
    alpha_residue * residue_bias + alpha_dan * template_vec, with
    alpha_dan decaying exponentially from the last z_goal update (tau on
    the order of seconds in env-steps). Residue bias retains its slow
    timescale (episodes). Both contribute additively to precision weights
    in the E1 encoder. Ablation toggle: sd026_enabled (default off during
    initial validation).
  notes: >
    Deliberately minimal for V3. Does not commit to vmPFC-dlPFC template
    compilation (H3, deferred to V4). Does not commit to capacity-limited
    selection (H2, V3/V4 boundary, pulvinar-TRN gate). Tests only whether
    a separable prospective channel is necessary for z_goal to produce
    behavioral lift. Falsifiable: if dan_feedback=off still produces
    benefit_ratio lift once z_goal seeds, SD-026 is wrong and residue
    alone suffices. Registered 2026-04-17.

- id: MECH-252
  title: "SWS hippocampal-neocortical replay consolidates goal-value prediction error into updates of stored goal representations (content update), not into attentional-template parameters."
  claim_type: mechanism_hypothesis
  subject: sleep.sws_stored_goal_consolidation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  claim_level: mechanistic
  registered_utc: 2026-04-17
  depends_on:
    - SD-017
    - INV-010
    - INV-049
    - INV-037
    - INV-038
  functional_restatement: >
    During daytime operation, goal-value prediction error is accumulated:
    did pursuing goal G actually satisfy homeostatic drive / produce
    predicted benefit? This error is long-horizon, episodic, sparse.
    Its legitimate update target is the stored goal representation
    (what the agent wants), not the attentional template (how the agent
    finds it).

    Proposed mechanism: SWS hippocampal-neocortical replay selectively
    routes goal-value error to stored-goal parameters. Slow oscillations
    and sharp-wave ripples coordinate cortical consolidation of
    drive-outcome associations (Balleine & Dickinson 1998; Diekelmann &
    Born 2010). The specificity is substrate-level: SWS replay has
    access to episodic outcome structure but lacks the varied associative
    replay characteristic of REM, making it well-suited to content
    updates and ill-suited to feature-generality updates.
  notes: >
    Paired with MECH-253 (REM template consolidation). Together they
    constitute the phase-separated consolidation that creates and
    maintains the stored-goal vs attentional-template distinction.

    Clinical prediction: SWS disruption should produce selective
    goal-value instability -- the agent continues to search effectively
    for previous goals but shows noisy/shifting preferences. Dissociable
    from REM disruption predictions under MECH-253.

    No evidence expected until SD-017 implementation complete. Registered
    in the pre-implementation claim matrix per user policy of capturing
    ideas at time of insight. V4-scoped: depends on full sleep-phase
    architecture (SD-017) being implemented.

- id: MECH-253
  title: "REM sleep, with varied associative replay and attenuated noradrenergic tone, consolidates template-performance prediction error into updates of z_goal -> attentional-template projection weights (feature-specification update), not into stored-goal content."
  claim_type: mechanism_hypothesis
  subject: sleep.rem_template_consolidation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  claim_level: mechanistic
  registered_utc: 2026-04-17
  depends_on:
    - SD-017
    - SD-026
    - INV-010
    - INV-049
    - INV-037
  functional_restatement: >
    During daytime operation, template-performance error is accumulated:
    did the deployed precision template successfully identify goal-relevant
    content during search? This error is within-episode, denser, and
    cross-situational. Its legitimate update target is the template
    projection weights (template_proj in MECH-251), not stored goal
    content.

    Proposed mechanism: REM sleep's varied associative replay, combined
    with attenuated noradrenergic tone (which reduces the fidelity
    penalty on replay divergence), consolidates template-performance
    error into template_proj updates. REM's cross-situational variance
    is exactly what feature-generality learning requires: the same
    stored goal paired with different contextual instantiations across
    replay episodes lets the projection learn which features are
    goal-diagnostic vs situational.
  notes: >
    Paired with MECH-252 (SWS stored-goal consolidation). Phase-separated
    consolidation creates and maintains the stored-goal / template
    distinction. Without REM, template projection drifts toward stored
    goal content (template collapses into retrieval); without SWS,
    stored goals drift toward whatever the template currently finds
    (goals collapse into salience).

    Clinical prediction: REM disruption should selectively impair
    goal-directed search quality (template performance) before producing
    goal-amnesia. Partial REM deprivation should degrade template-driven
    search with relative sparing of stored-goal retrieval. Dissociable
    from MECH-252 predictions. Consistent with long-standing findings
    that REM deprivation impairs procedural / implicit learning more
    than declarative recall (Smith 2001; Diekelmann & Born 2010).

    No evidence expected until SD-017 implementation complete. Registered
    in the pre-implementation claim matrix per user policy. V4-scoped:
    depends on SD-017 phase architecture and SD-026 template channel
    being implemented.

- id: SD-027
  title: "Capacity-limited selection for E3 access occurs at the E1/E2 -> E3 boundary via a pulvinar-TRN analogue gate, not inside E3. The gate selects which active E1/E2 content is broadcast to E3 per heartbeat cycle; selection is distinct from (and upstream of) the MECH-089 theta-gamma packaging operation."
  claim_type: design_decision
  subject: attention.access_gate_at_boundary
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  phase_provenance: derived
  phase_derived_from: SD-064  # 2026-08-10, chip chip-20260810-phase-consistency-reconciliation:
                              # SD-064's own falsifier is explicitly SUBSTRATE-BLOCKED on
                              # SD-027 ("the SD-027 boundary gate is not implemented in
                              # V3... a clean gate-ablation would self-route
                              # substrate_not_ready"), unlike this cluster's other 3
                              # cases -- SD-064 has NOT been tested without SD-027, and
                              # SD-027's own notes anticipate a V3-minimal retrofit as
                              # viable (not categorically impossible).
  claim_level: architectural
  registered_utc: 2026-04-17
  depends_on:
    - MECH-007         # attention fragmentation (rules out unitary selection inside E3)
    - MECH-089         # theta-gamma packaging -- shares substrate; selection is upstream of packaging
    - MECH-081         # NA snap modulates selection weights
    - SD-026           # z_goal template modulates selection weights
    - INV-009          # attention is precision modulation
  functional_restatement: >
    MECH-089 establishes that E3 receives packaged/batched summaries of E1
    activity rather than raw frame-by-frame output. Packaging answers "how
    is content formatted" but does not answer "which content is formatted."
    In biology these are distinct operations: TRN (thalamic reticular
    nucleus, Crick's attentional searchlight) provides capacity-limited
    inhibitory gating; pulvinar (higher-order thalamic relay) mediates
    cortical-cortical coordination. Together they implement selection-
    for-broadcast at a boundary upstream of cortical deliberation.

    SD-027 asserts the same functional separation for REE: a dedicated
    gate sits at the E1/E2 -> E3 boundary, selecting a capacity-limited
    subset of active latents per heartbeat cycle for packaging and
    forwarding to E3. Selection weights are a product of precision, the
    z_goal template (SD-026), and bottom-up salience (MECH-081).

    Putting selection inside E3 would collapse it with deliberation and
    violate MECH-007 (fragmentation forbids unitary attention). Putting
    it at the boundary preserves the fragmentation: selection (TRN-like),
    packaging (MECH-089), deliberation (E3).

    V3 minimum viable implementation: fixed-k top-k selector over active
    z_world components. V4: dynamic capacity modulation + full pulvinar-
    TRN analogue with lateral inhibition. Registering as V4 because the
    full form is V4-scoped; V3 retrofit is possible but optional and
    gated on SD-026 landing.
  notes: >
    Anatomical grounding: Crick (1984) "function of the thalamic reticular
    complex: the searchlight hypothesis." Kastner et al. (2004) pulvinar
    attentional modulation. Saalmann et al. (2012) pulvinar synchronizes
    information transmission between cortical areas. Wimmer et al. (2015)
    TRN thalamic gating selective attention.

    Explanatory pairings:
    - MECH-092 (micro-DMN / quiescent replay): only coherent against the
      backdrop of an access gate that is sometimes in quiescent mode.
      SD-027 provides the substrate that makes MECH-092 well-formed.
    - MECH-090 (beta gates E3 -> action_selection): SD-027 is the
      INPUT-side gate (E1/E2 -> E3); MECH-090 is the OUTPUT-side gate
      (E3 -> action). Together they bracket E3 deliberation.

    Hole covered: H2 in docs/thoughts/2026-04-17_attention_coverage_audit.md
    "Selection for access / workspace broadcast." Distinct from global
    workspace theory in that SD-027 does NOT commit to a single global
    workspace; it commits to a boundary gate, consistent with REE's
    fragmentation architecture.

    [2026-07-08 SD-064 reframe]: candidate SD-064 revisits this disclaimer.
    It argues the "not a single global workspace" hedge conflates Global
    Workspace Theory with a unitary intra-E3 SELECTOR -- MECH-007 forbids the
    latter, but GWT requires no unitary attender (its backstage is a fragmented
    population of specialists, and the workspace is a capacity-limited BROADCAST
    BUS over them). On that reading SD-027's boundary gate + MECH-089 packaging +
    the MECH-287 / SD-037 broadcast machinery already CONSTITUTE a global
    workspace at the access-functional (not phenomenal) level. SD-027 stays valid
    as the SELECTION stage; SD-064 owns the workspace-level commitment and its
    ablation-cliff falsifier. See SD-064.

    Hard V4 scope note: V4 requires substrate for lateral inhibition and
    dynamic capacity modulation that V3 does not have. Simple V3 top-k
    retrofit is feasible but is a partial approximation.
  evidence_quality_note: |
    No direct experiments. V3 retrofit could be tested if prioritised:
    measure behavior with selection gate active (top-k, k=5) vs gate
    disabled (all content forwarded). Predictions: disabled gate -> E3
    update stability degrades under high-activity conditions; committed-
    mode precision fluctuations increase. Not queuing an EXQ yet --
    testing SD-026 is the higher priority, and SD-027 validation is more
    meaningful once SD-026 provides goal-modulated selection weights.

- id: MECH-254
  title: "At each E3 heartbeat cycle, a top-k selection over active E1/E2 latents determines which content enters E3 deliberation. Selection weights combine precision, z_goal-derived template gain, and noradrenergic bottom-up salience."
  claim_type: mechanism_hypothesis
  subject: attention.boundary_gate_selection
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  claim_level: mechanistic
  registered_utc: 2026-04-17
  depends_on:
    - SD-027
    - SD-026
    - MECH-081
    - MECH-089
  functional_restatement: >
    Implementation for SD-027. At E3 heartbeat tick t:
      w_i = precision_i * (1 + alpha_goal * <template_vec, z_i>
                             + alpha_na  * na_signal_i)
      selected = top_k(w_i, k)
      packaged = theta_gamma_package(selected)    # MECH-089
      E3.consume(packaged)

    alpha_goal scales z_goal / template contribution (SD-026 / MECH-251).
    alpha_na scales bottom-up snap contribution (MECH-081). Both may be
    tuned per mode (committed vs exploratory). Capacity k is a fixed
    floor in V3/V4-minimum (k ~ 3-7); V4-full adds dynamic modulation.
  notes: >
    Deliberately minimal. Does not commit to lateral inhibition between
    selected items (a TRN-like feature); this is deferred to V4-full.
    Does not commit to soft-attention over weights vs hard top-k; either
    is compatible with SD-027 provided capacity is enforced.

    Falsifiable: if ablating the gate (letting all E1/E2 content through
    to E3) produces no stability or behavior degradation, SD-027 is wrong
    and E3 has sufficient internal selection capacity. Converse: if gate
    ablation produces pathological rumination, attentional overload, or
    E3 update instability, SD-027 is supported.

    Companion to MECH-251 (SD-026 implementation). Both can be
    toggled independently: sd026_enabled controls the template write
    channel; sd027_enabled controls the boundary gate. This permits
    factorial ablation -- four cells: {both off, gate only, template
    only, both on} -- to dissect contributions.

- id: SD-028
  title: "An attentional template is a first-class precision-space object, compiled from stored-goal content and current context by a vmPFC-dlPFC translator circuit, distinct from both the stored goal itself (content) and the SD-026 write channel (transport)."
  claim_type: design_decision
  subject: attention.template_as_first_class_object
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  claim_level: architectural
  registered_utc: 2026-04-17
  depends_on:
    - SD-026           # template compiler output is consumed by the SD-026 write channel
    - INV-034          # goal maintenance necessary for ethical agency
    - INV-037          # stored vs active distinction
    - INV-038          # EVR pattern
    - MECH-007         # attention fragmentation
  functional_restatement: >
    SD-026 establishes the write CHANNEL (FEF/IPS -> V1 feedback
    analogue). MECH-251 gives a minimal implementation of that channel
    as a direct projection z_goal -> template_vec. SD-028 asserts that
    the *template_vec itself* is a first-class object with its own
    compilation semantics, not merely an output of z_goal projection.

    Distinction:
    - Stored goal = CONTENT ("an apple," "reach the target," a value
      representation). Persistent across contexts.
    - Attentional template = PRECISION-SPACE VECTOR specifying which
      features to amplify ("red, round, hand-sized" or whatever the
      sensory instantiation demands). Context-dependent; recompiled
      as context changes.
    - Same stored goal generates DIFFERENT templates in different
      contexts (apple-in-fruit-bowl vs apple-in-tree).

    Compilation: template_vec = compile(stored_goal, current_context).
    Anatomically, vmPFC supplies value/goal content, dlPFC supplies
    executive rules / task-set / contextual constraints; integration
    produces the feature-precision specification. This is the canonical
    fronto-parietal top-down attention pathway under Corbetta-Shulman.

    V3 cannot realise SD-028 faithfully because V3 does not decompose
    vmPFC vs dlPFC; the minimal MECH-251 projection is effectively
    "compiler = linear map," which is SD-028 degenerate to a single
    parameter matrix. V4 is required for context-dependent compilation.

    STRUCTURAL LOAD-BEARING FOR: INV-012 (responsibility through
    commitment) -- extension of the SD-026 argument. A commitment
    requires not just a prospective channel (SD-026) but a content-vs-
    template distinction (SD-028). Without SD-028, "commitment" can
    still be tied to perception, but cannot be re-specified across
    contexts -- the committed agent would be a template-automaton rather
    than a template-deployer. SD-028 is what makes committed attention
    *skilled* rather than merely *engaged*.
  notes: >
    Anatomical grounding: Miller & Cohen (2001) integrative theory of
    PFC function -- dlPFC maintains rule/task representations that
    bias processing elsewhere. Wallis et al. (2001) single neurons in
    PFC encoding abstract rules. Badre & Nee (2018) frontal cortical
    hierarchy for executive control. vmPFC value integration: Hare et
    al. (2009), Rangel & Clithero (2014). The value-vs-rule split maps
    onto the stored-goal-vs-template compilation task.

    Clinical dissociation:
    - vmPFC damage -> impoverished goal content; patients lose "what
      they want" but can still deploy instructed templates on demand
      (Bechara & Damasio).
    - dlPFC damage -> intact goal content, impoverished template
      deployment; perseveration, distractibility, failure to translate
      stored goals into appropriate search sets. These are dissociable
      syndromes, consistent with separate compiler components.

    Hole covered: H3 in docs/thoughts/2026-04-17_attention_coverage_audit.md
    "Attentional template as a first-class object."

    V4-scoped. Registered in the pre-implementation claim matrix per
    user policy.
  evidence_quality_note: |
    No evidence expected until V4 vmPFC-dlPFC decomposition is
    implemented. The minimal MECH-251 form (direct z_goal projection)
    will provide a V3 baseline against which SD-028's context-dependent
    compilation can be compared: if a single static projection suffices
    to produce goal-conditioned behavior across contexts, SD-028 is
    unnecessary and H3 is wrong. If behavior fails to generalize across
    context variations despite working template writing, SD-028 is
    supported and V4 compiler work is justified.

- id: SD-029
  title: "Single-pass agency attribution via forward-model comparator on the z_harm_s reafferent stream: residual = z_harm_s_observed - E2_harm_s(z_harm_s_{t-1}, a_actual)."
  claim_type: design_decision
  subject: self_attribution.comparator_z_harm_s
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-642_2026-06-06
      as_of: 2026-06-06
      verdict: "non_contributory/none -- claimless diagnostic; characterise as a measurement / test-design gap, NOT substrate_ceiling. Do not record substrate_ceiling on any claim from this run."
  epistemic_category: substrate_ceiling
  epistemic_category_note: >
    Set 2026-08-07 (thought-digestion wave 10), NOT on the basis of the
    EXQ-642 diagnostic above (that instruction is respected -- EXQ-642 is not
    cited as evidence here). Basis instead is the aggregate pattern across
    13+ runs (EXQ-330/330a/353/431/433/433a/433b/433d/433f/470/523b/537/537a):
    the comparator mechanism is built and repeatedly exercised, C1 and C3
    have repeatedly passed clean (forward_r2 0.65-0.998; approach_snr PASS),
    but C2 has never once been evaluable because a shared downstream
    mechanism (monostrategy / lack of behavioural diversity, rooted in
    MECH-439 F-dominance, ~88-89% of E3's variance) absorbs the signal before
    it reaches the outcome metric -- structurally identical to the confirmed
    substrate_ceiling pattern on ARC-062's GAP-B falsifier (21 runs, same
    root). Held pending MECH-269/MECH-269b V_s landing.
  ceiling_decision: deferred  # 2026-08-10 governance, GOV-CEIL-1 ceiling-exhausted audit (13 mechanical hits)
  ceiling_routing_note: "2026-08-10 GOV-CEIL-1 ceiling-exhaustion audit flagged 13 mechanical hits. NOT applying the standard demotion -- the epistemic_category_note above already names a specific, credible, not-yet-landed unblock (MECH-269/MECH-269b V_s landing), the same shape as ARC-062's own GAP-B pattern this claim cross-references. Deferred pending that landing; remove this marker once it lands and a fresh C2-evaluable retest runs."
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: 2026-04-18
  supersedes: SD-003
  instantiates: MECH-256
  depends_on:
    - SD-011           # dual nociceptive streams (z_harm_s as separate stream)
    - ARC-033          # E2_harm_s forward model substrate
    - SD-013           # interventional vs observational training of E2_harm_s
    - MECH-256         # general comparator mechanism this SD instantiates
  what_would_answer: |
    NON-DEGENERACY PRECONDITION (must clear before C2 is evaluable at all): the
    comparator mechanism is built and exercised across 13+ runs (EXQ-330 through
    EXQ-537a) with C1 and C3 repeatedly passing clean -- but C2 has never once been
    evaluable, because training/eval produces near-single-class agent-caused vs
    env-caused hazard-event distributions (monomodal policy; SD-050 reef enrichment
    tried, did not fix it). Depends on MECH-269/MECH-269b's V_s landing behavioural
    diversity -- as of 2026-08-07 both remain candidate/v3_pending. The diagnostic-
    probe evidence (R4.b, 2026-05-29) is scoring_excluded and does not lift
    v3_pending; the non-diagnostic behavioural-sufficiency retest (V3-EXQ-475b,
    authored 2026-07-31) is not yet queued. Deeper shared root: MECH-439
    F-dominance monostrategy collapse (~88-89% of E3's variance) -- identical to
    the ceiling already documented on ARC-062's GAP-B falsifier (21 runs) --
    remains candidate/substrate_ceiling/pending_retest_after_substrate as of the
    2026-08-01 governance cycle.

    CONFIRMING: residual_harm shows the Shergill (2003) partial-attenuation
    pattern -- measurably smaller for self- vs externally-caused harm events, but
    not reduced to zero (graded, not binary) -- while C1 and C3 continue to hold.

    FALSIFYING: once C0 is met, EITHER no measurable attenuation (self/ext ratio
    ~1.0, agency-blind residual despite clean C1), OR binary/near-total
    attenuation (inconsistent with Shergill), OR C1 degrades below 0.9 under the
    balanced-event regime (would indicate earlier clean C1 reads were an artifact
    of training under a degenerate distribution).

    (Digested 2026-08-07, session cool-torvalds-a82359, thought-digestion wave 10.)
  functional_restatement: >
    Concrete V3 instantiation of the general comparator mechanism (MECH-256) on
    the sensory-discriminative harm stream. At each step:

      residual_harm = z_harm_s_observed - E2_harm_s(z_harm_s_{t-1}, a_actual)
      causal_sig    = f(residual_harm, event_context)

    No counterfactual branch. No E2(a_cf) call. The forward prediction on the
    actually-taken action is the reference; the residual IS the agency signal.
    This matches Blakemore 1998 (tactile cancellation), Shergill 2003 (force
    attenuation, ~50% partial not binary), and Frith 2000 (comparator model of
    self-other distinction).

    PASS criteria sketch (to be formalised in the experiment design doc):
      C1: forward_r2(E2_harm_s) >= 0.9 on reafferent z_harm_s stream.
      C2: residual magnitude partially attenuated for self-caused vs
          externally-caused harm events (Shergill partial-attenuation pattern,
          not a binary gate).
      C3: residual signal-to-noise > threshold on approach-to-harm events
          specifically -- event-conditioned, not mean over all steps
          (EXQ-431 lesson: sample-starved means are uninformative).

    Interventional training (SD-013) is required: E2_harm_s must see
    counterfactual perturbations during training to learn P(z_harm_s | do(a))
    rather than the correlational P(z_harm_s | z_t, a).
  notes: >
    Biological grounding (three-pull synthesis 2026-04-18, 14 entries):
    - Frith 2000 PhilTransB (single-pass comparator; schizophrenia as comparator failure)
    - Shergill 2003 Science (force-matching partial attenuation)
    - Haggard 2017 NatRevNeurosci (cue-integration of prospective + retrospective agency signals)
    - Blakemore 1998 NatNeurosci (self-tickle cancellation; timing-sensitive to 100-200ms)

    Open nociceptive-transfer caveat: the canonical comparator literature
    evidences the mechanism on sensorimotor / tactile / force streams.
    Extension to nociceptive streams is plausible (descending pain modulation
    in PAG/RVM shares the efference-copy structure) but not directly
    demonstrated in the four canonical papers. This is the main mapping risk.

    V3 readiness: ARC-033 (E2_harm_s) implementation is in place per the
    2026-04 substrate queue. SD-013 interventional training wiring required;
    EXQ-330a result already shows forward_r2 ~0.998 (C1 trivially meetable);
    C2 and C3 are the open criteria.
  evidence_quality_note: >
    Successor to SD-003's evidence chain. The z_harm_s pipeline runs that
    informed SD-003 (EXQ-330, 330a, 353, 431) are relevant diagnostics but
    their cf_gap-structured criteria do not map directly onto the single-pass
    comparator criteria. A fresh experiment tagged SD-029 with event-conditioned
    residual analysis is required before SD-029 can be provisionally promoted.
    First test: an event-conditioned reanalysis of EXQ-330a traces against
    approach-to-harm events.

    EXQ-433 (2026-04-19) reclassified non_contributory after overall FAIL was
    traced to event-distribution collapse, not hypothesis falsification. Event
    counts showed bimodal policy outcomes: seed 91 had 303 agent-caused / 0
    env-caused hazards (exploit mode); seeds 13 and 42 had ~91-114 env-caused
    but only 1-2 agent-caused (avoid mode); only seed 7 balanced (177/23). The
    C4 event-density controller cannot escape this by extending episodes because
    the policy mode, not the episode length, is the bottleneck. C2 attenuation
    and C3 approach-SNR are uninterpretable when one class has near-zero samples.
    MECH-256 C1 forward_r2=0.983-0.9998 across 4/4 seeds remains clean supports.
    SD-029 pending_retest_after_eval_balance_fix: follow-up EXQ-433a must
    decouple evaluation from behavioural sample frequency (policy counter-
    balancing, per-class reweighted evaluation, or a fixed-script eval protocol
    that forces exposure to both event types regardless of learned policy).
    EXQ-433/433a/433b/470 governance 2026-04-22: scripted-comparator
    (V3-EXQ-433a 2026-04-19), interventional-extension (V3-EXQ-433b
    2026-04-20), and balanced-curriculum substrate probe (V3-EXQ-470 PASS x2
    2026-04-21) all reclassified non_contributory for SD-029 promotion
    purposes. The substrate (env-level scheduled_external_hazard) is now in
    place and EXQ-470 confirms event-balance fires; behavioural attribution
    test remains blocked by V_s monostrategy (cannot generate balanced agent-
    vs-env event distributions when policy is monomodal). Hold at candidate
    pending MECH-269 V_s landing.
    V3-EXQ-433d FAIL (2026-04-27, evidence -> reclassified non_contributory in
    /governance 2026-04-27). 4-seed run with EXQ-479 calibrated curriculum.
    Trial collection per seed: env_caused_hazard=20/20/20/20, agent_caused_hazard
    =0/0/0/0, hazard_approach=20 each, none=20 each. Same monostrategy phenotype
    as predecessors EXQ-433/433a/433b/470: monomodal policy zeroes the
    agent-caused branch. c0 trial-sufficiency gate FAIL 0/4, which short-circuits
    c1 (forward_r2) and c2 (attenuation) -- the comparator hypothesis is never
    actually tested. c3 (approach_snr) PASS 4/4. Reclassified non_contributory
    per-claim on SD-029 + MECH-256; claims hold pending MECH-269/MECH-269b V_s
    gating landing.

    [2026-05-08 governance]: EXQ-537/537a/523b runs all classified non_contributory. Diagnostic read: single-pass forward R2 ~0.65 (vs 0.85 target) but C3 falsification holds (scrambled<intact), indicating feature quality is adequate while training capacity is the gap. EXQ-523b reef arm severely undertrained (p1_r2 ~0.18). Pattern consistent with sleep + curiosity training prerequisites the agent does not yet have. Defer evidence weighting on this claim until sleep substrate (MECH-204 / SD-049 sleep loop / SD-017 cluster) lands.

    [2026-05-08 diagnose-errors]: V3-EXQ-433f (5h17min run on ree-cloud-1, runner classified UNKNOWN because flat JSON synced after bookkeeping window) reclassified non_contributory per-claim on SD-029 + MECH-256. C0 trials-sufficient gate FAILed in 3/4 seeds (agent_caused_trials 15/7/20/3 vs target 20). Adds a fifth confirmation of the 433-cohort substrate-ceiling pattern: SD-050 reef enrichment (reef_enabled=True, n_reef_patches=3, hazard_food_attraction=0.7) was intended to break monostrategy on 8x8 grid but did not produce balanced events at this scale either. Hold remains gated on MECH-269/MECH-269b V_s landing; reef-substrate tweaks are not the unblock.
  implementation_note: >
    SD-029 substrate IMPLEMENTED 2026-04-21 via a curriculum-level env
    augmentation. CausalGridWorldV2 now exposes
    scheduled_external_hazard_enabled (bool, default False) + _interval /
    _prob / _adjacent_only sub-knobs. When enabled, the env periodically
    relocates or spawns a hazard adjacent to the agent, producing
    externally-caused hazard events that the self-vs-externally-caused
    taxonomy counts distinctly from self-caused ones. Info dict now also
    emits external_hazard_injected (bool) and external_hazard_event_count
    (int) every step. Forward model (ARC-033) and interventional training
    (SD-013) were already in place; this curriculum closes the last
    substrate gap for C3/C4 measurement. Backward compatible: disabled by
    default, existing experiments unaffected. Validation: V3-EXQ-470
    (env-level sanity probe, diagnostic). See ree-v3/CLAUDE.md "SD-029:
    Balanced Hazard-Event Curriculum (2026-04-21)".
  location: docs/architecture/self_attribution_per_stream.md

- id: SD-030
  title: "Single-pass agency attribution on the z_self (motor-proprioceptive) reafferent stream: residual = z_self_observed - E2_self(z_self_{t-1}, a_actual)."
  claim_type: design_decision
  subject: self_attribution.comparator_z_self
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  claim_level: architectural
  registered_utc: 2026-04-18
  instantiates: MECH-256
  depends_on:
    - SD-005           # self/world latent split (z_self separated from z_world)
    - MECH-256         # general comparator mechanism
  functional_restatement: >
    V4-scoped analogue of SD-029 on the motor-proprioceptive reafferent stream.
    z_self carries the sensory consequences of the agent's own motor actions
    (force, position change, proprioceptive reafference). Self-attribution on
    this stream answers "did I cause this movement / this tactile event?" --
    the canonical Blakemore-tickle / Shergill-force domain.

      residual_self = z_self_observed - E2_self(z_self_{t-1}, a_actual)

    In biology this is the cerebellar internal-model loop (Wolpert, Miall,
    Ito): the cerebellum predicts the sensory consequences of motor commands;
    the spinal cord / brainstem / S1-S2 compare observed reafference against
    the prediction; residual is attenuated for self-generated signals.

    Not currently queued for V3. Registered as a placeholder for V4 motor-
    proprioceptive self-attribution work once z_self is a first-class latent
    with its own forward model substrate.
  notes: >
    Biological grounding is strongest for this SD of the three per-stream
    instantiations -- Blakemore 1998 (tactile cancellation), Shergill 2003
    (force), Wolpert & Flanagan 2001 (motor forward models) are all
    squarely on z_self territory. V3 deprioritises this because V3 does not
    have a clean z_self stream with its own forward model; E2 currently
    operates on z_gamma (combined self/world). Waiting for V4 z_self
    materialisation (SD-005 expansion) before implementing.
  evidence_quality_note: >
    No V3 evidence expected. V4 target. Registered pre-implementation so the
    per-stream attribution topology is explicit in the claim matrix.
  location: docs/architecture/self_attribution_per_stream.md

- id: SD-031
  title: "Single-pass agency attribution on the z_world (causal-footprint) reafferent stream: residual = z_world_observed - E2_world(z_world_{t-1}, a_actual)."
  claim_type: design_decision
  subject: self_attribution.comparator_z_world
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-783_2026-07-18
      as_of: 2026-07-18
      verdict: diagnostic_no_direction/instrument_validated_cause_discriminated
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: 2026-04-18
  instantiates: MECH-256
  depends_on:
    - SD-005           # self/world split
    - SD-010           # harm-stream separation (ensures z_world remains distinct from z_harm)
    - MECH-256         # general comparator mechanism
  functional_restatement: >
    Single-pass comparator on the world-state reafferent stream. z_world
    captures the causal footprint of the agent's actions on external world
    state -- non-harm, non-self consequences ("I moved the block," "the door
    opened because of me"). Self-attribution on this stream is the basis for
    moral attribution of non-harm consequences and for ResidueField-style
    causal tracking.

      residual_world = z_world_observed - E2_world(z_world_{t-1}, a_actual)

    Biological analogue: parietal outcome-prediction circuits and the
    posterior-parietal / MD-thalamus / colliculus corollary-discharge loops
    (Sommer & Wurtz) that track self-caused external changes. Also overlaps
    with the ACC/insula outcome-attribution pathway.

    V3 previously treated z_world attribution implicitly through ResidueField
    updates; SD-031 makes it an explicit comparator-structured signal.
  notes: >
    Rescoped v4 -> v3 on 2026-06-06: SD-031 is a named dependency of a V3
    completion retest (the z_world discriminative-granularity question in
    failure_autopsy_zworld-integration-cluster_2026-06-06, V3-EXQ-177's
    counterfactual-attribution arm), so it is V3-scoped by definition (a
    V3-completion dependency reclassifies its dep to V3; the implementation_phase
    field is a prediction, not a permission gate). Substrate landed the same day
    (E2WorldForward, ree-v3/ree_core/predictors/e2_world.py). Completes the
    per-stream comparator topology: harm (SD-029, active), world (SD-031, V3),
    self (SD-030, still V4 -- no V3 driver; V3 lacks a clean z_self stream with
    its own forward model).
  evidence_quality_note: >
    Substrate IMPLEMENTED 2026-06-06 (E2WorldForward; the z_world instantiation
    of MECH-256, sibling to ARC-033 on z_harm_s). Config:
    LatentStackConfig.use_e2_world_forward (default False). v3_pending until the
    discriminative/attribution arm PASSes its validation experiment. CRITICAL
    GATE: the comparator's discriminative arm requires world_dim >= 128
    (E2WorldForward hard-asserts this) -- the 2026-06-06 cluster autopsy
    established z_world at world_dim=32 is a competent bulk predictor
    (world_forward_r2 0.72-0.94) but lacks discriminative granularity, so a
    dim=32 comparator yields a vacuous zero attribution gap. Validation must run
    at world_dim >= 128 AND with ARC-065 behavioral diversity active (balanced
    agent-caused vs externally-caused events); running before both halves are in
    place reproduces the dim=32 + monostrategy + (formerly) unbuilt-comparator
    confound. Activation smoke 2026-06-06 at world_dim=128: world_forward_r2
    0.969, single-pass comparator self-caused residual ~2.0 vs externally-caused
    ~22.6 (correct attribution gap).
  location: docs/architecture/self_attribution_per_stream.md

- id: MECH-255
  title: "Template compilation is implemented by vmPFC value-content projection composed with dlPFC context-gated rule projection, integrated into a precision-space template vector that is the input to the SD-026 dan_feedback channel."
  claim_type: mechanism_hypothesis
  subject: attention.vmpfc_dlpfc_template_compiler
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  claim_level: mechanistic
  registered_utc: 2026-04-17
  depends_on:
    - SD-028
    - SD-026
    - INV-037
  functional_restatement: >
    Implementation for SD-028. Compilation proceeds in three stages:

      z_goal_content = vmPFC_proj(z_goal)            # value/content
      rule_bias      = dlPFC_proj(z_context, task)   # rule/task-set
      template_vec   = integrate(z_goal_content, rule_bias)

    integrate() is a learned gating -- in the minimal form, a product or
    additive combination with a learned mixing weight. More expressive
    forms (tensor factorisation, hyper-network) are V4+ implementation
    choices and not committed here.

    template_vec is then written via the SD-026 dan_feedback channel
    (MECH-251). Under this decomposition, MECH-251's template_proj is
    not a primitive projection but the degenerate limit of MECH-255 when
    rule_bias is null -- giving a clean V3 -> V4 continuity.
  notes: >
    Design continuity: the MECH-251 V3 implementation naturally subsumes
    into MECH-255 V4 by adding the rule_bias term. No breaking changes
    to the attention pipeline -- V3 behavior = V4 with dlPFC_proj = 0.

    Falsifiable at V4: if template_vec from MECH-251 (no context
    conditioning) matches performance of MECH-255 (with context
    conditioning) across diverse contexts, MECH-255 is unnecessary
    and SD-028 is wrong. Predicted: context-dependent contexts
    (goal-same, sensory-different) show MECH-255 outperforming MECH-251.

    No evidence expected until V4. Registered in the pre-implementation
    claim matrix per user policy.

- id: MECH-256
  title: "Self-attribution is implemented by a forward-model comparator: for any reafferent latent stream z_x with an efference-copy-driven forward model E2_x, the residual (z_x_observed - E2_x(z_x_{t-1}, a_actual)) is the agency signal on that stream."
  claim_type: mechanism_hypothesis
  subject: self_attribution.forward_model_comparator_general
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-256@2026-04-25T15:42:09.107831Z"
      as_of: 2026-04-25
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-04-18
  depends_on:
    - ARC-033          # E2_harm_s forward model (concrete substrate for SD-029)
    - SD-013           # interventional training requirement
  functional_restatement: >
    A single, stream-agnostic computational claim about the agency-attribution
    mechanism. For any reafferent latent stream z_x that has an efference-copy-
    driven forward model E2_x, self-attribution proceeds by single-pass
    comparator:

      predicted_z_x = E2_x(z_x_{t-1}, a_actual)
      residual_z_x  = z_x_observed - predicted_z_x
      causal_sig    = f(residual_z_x, context)

    Note: one forward pass. No counterfactual (no second call to E2 with a_cf).
    The forward prediction on the actually-taken action is the reference; the
    residual IS the agency signal. Self-caused events produce attenuated
    residuals (the prediction succeeds); externally-caused events produce
    large residuals (the prediction fails).

    Instantiations:
    - SD-029: z_x = z_harm_s (sensory-discriminative harm stream, V3-active).
    - SD-030: z_x = z_self (motor-proprioceptive, V4-deferred).
    - SD-031: z_x = z_world (causal-footprint, V4-deferred).

    The mechanism is uniform across streams; the substrates (E2_harm_s, E2_self,
    E2_world) are stream-specific forward models with stream-specific training
    requirements.

    Falsifiable: if single-pass comparator residuals cannot distinguish
    self-caused from externally-caused events on any reafferent stream
    with a well-fit forward model (forward_r2 > 0.9), MECH-256 is refuted.
    The converse -- if a two-pass counterfactual strictly outperforms
    single-pass comparator on the same stream -- would weaken it.
  notes: >
    Biological grounding (three-pull synthesis 2026-04-18, 14 literature entries):

    Comparator mechanism is stream-agnostic computationally but substrates
    are stream-specific anatomically:
    - Motor-proprioceptive: cerebellum (Wolpert, Miall, Ito -- canonical
      forward-model locus).
    - Self-touch / tactile: cerebellum -> S1/S2 cancellation (Blakemore 1998).
    - Self-vocalization: HVC-analogue corollary discharge -> auditory cortex
      (songbird; Crapse & Sommer 2008 review).
    - Oculomotor: colliculus / MD thalamus corollary discharge -> parietal
      (Sommer & Wurtz).
    - Electrosensory (fish): electric-organ corollary discharge -> ELL
      cancellation (Bell).
    - Nociceptive agency: ACC / insula self-vs-other attribution of pain
      (descending pain modulation PAG/RVM).

    Frith 2000 frames schizophrenia's agency breakdown as comparator failure;
    clinical phenomenology (auditory hallucinations vs passivity experiences
    vs thought insertion) is stream-specific, consistent with multiple
    stream-specific comparators rather than a single unified one.

    Haggard 2017 synthesises: sense of agency is built from multiple
    stream-specific comparators weighted by precision (cue-integration model).
    This maps cleanly onto REE's per-stream SD topology plus a downstream
    integration step (left open; likely ACC/insula at V4).

    MECH-257 registers the reading-mode distinction (comparator vs evaluator)
    over the same E2_x substrate.
  evidence_quality_note: >
    Literature pull 2026-04-18 (targeted_review_sd003_successor_comparator,
    4 entries, mean confidence ~0.75, all supports): Frith 2000, Shergill 2003,
    Haggard 2017, Blakemore 1998. Four-paper convergence on single-pass
    comparator as the biologically-grounded mechanism.

    Empirical V3 evidence accrues through the per-stream SD instantiations
    (SD-029 first). MECH-256 itself is the stream-agnostic statement; evidence
    for the mechanism is the sum of evidence on its instantiations.
    EXQ-433/433a/433b governance 2026-04-22: all SD-029 attribution attempts
    reclassified non_contributory under V_s monostrategy substrate gap (see
    SD-029 evidence_quality_note). MECH-256 status unchanged -- per-stream
    instantiations cannot accrue evidence until the policy can produce
    balanced agent-vs-environment event distributions.
    V3-EXQ-433d 2026-04-27 also reclassified non_contributory (same monomodal
    phenotype: 0 agent_caused_hazard trials in 4/4 seeds even with EXQ-479
    calibrated curriculum). Hold pending MECH-269/MECH-269b V_s gating landing.

    [2026-05-08 governance]: EXQ-537/537a non_contributory. Same architectural read as SD-029: single-pass comparator capacity ceiling, not feature failure. C3 falsification works. Defer until sleep + curiosity training substrates land.

    [2026-05-08 diagnose-errors]: V3-EXQ-433f reclassified non_contributory per-claim. C0 trials-sufficient gate FAILed in 3/4 seeds (agent_caused_trials 15/7/20/3 vs target 20) -- same monomodal V_s monostrategy substrate-ceiling pattern as 433/433a/433b/470/433d. SD-050 reef enrichment did not break monostrategy at 8x8 scale. Hold remains gated on MECH-269/MECH-269b V_s landing.

    [2026-06-06 substrate]: SD-031 (z_world instantiation) IMPLEMENTED -- the
    comparator mechanism is now instantiated on TWO streams in code (z_harm_s
    via ARC-033/SD-029, z_world via SD-031/E2WorldForward). MECH-256 stays
    v3_pending: the z_harm_s arm is still V_s-monostrategy-blocked, and the
    z_world arm's discriminative validation is gated on world_dim>=128 +
    ARC-065 behavioral diversity (failure_autopsy_zworld-integration-cluster_2026-06-06).
    No evidence accrued yet on either stream.
  digestion_note: >
    2026-08-06 (thought-digestion, session thoughts-intakes-4fd18d, parallel
    research agent): what_would_answer was DELIBERATELY NOT WRITTEN. All prior
    text above (through 2026-06-06) describes a long run of non_contributory/
    substrate-ceiling outcomes -- but a CONFIRMED failure-autopsy on V3-EXQ-878
    (evidence/planning/failure_autopsy_V3-EXQ-878_2026-08-03.md, confirmed
    2026-08-03 -- three days before this digestion pass, not reflected anywhere
    above) reports a materially different result: "Pathway 1 (efference-copy
    comparator, MECH-256/SD-029) DID get a valid test and passed cleanly (D2,
    3/3 seeds)" -- the FIRST clean positive test of this mechanism, in direct
    contrast to the non_contributory history this claim's own text still tells.
    The autopsy explicitly flags "v3_pending/hold_pending_v3_substrate
    (2026-05-19) is stale -- both pathway substrates are confirmed implemented
    and wireable; recommend lifting," and routes a follow-up calibration
    experiment (V3-EXQ-878a) for the OTHER pathway (SD-021/AICAnalog,
    MECH-309-adjacent -- a DIFFERENT claim family, do not conflate with
    MECH-256's own efference-copy pathway). review_tracker.json shows the
    underlying run reviewed, but nothing in governance_agenda.v1.json or this
    claims.yaml block yet reflects the finding -- the v3_pending hold has not
    been lifted and evidence_quality_note has not been updated (the
    "reviewed != autopsy-applied" pattern).
    NEXT DIGESTION PASS: check whether /governance has since applied the 878
    recommendation (v3_pending lifted, evidence_quality_note updated, status
    possibly moved off candidate) and whether V3-EXQ-878a has landed, before
    drafting what_would_answer. A draft written against the pre-878 history
    would misstate the current evidentiary picture. Remove this note once
    reconciled and what_would_answer is written.
  location: docs/architecture/self_attribution_per_stream.md

- id: MECH-257
  title: "E2_x is a dual-function substrate: one forward model read in two modes, retrospective comparator (attribution) and prospective rollout-scoring (evaluation), arbitrated by a controller gating signal."
  claim_type: mechanism_hypothesis
  subject: forward_model.dual_function_gated_readout
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-257@2026-04-25T15:42:09.107834Z"
      as_of: 2026-04-25
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-04-18
  depends_on:
    - MECH-256         # general comparator claim; evaluator is the other reading
    - ARC-018          # rollout scoring / hippocampal proposal circuit
    - ARC-023          # heartbeat control plane that could host the gating signal
    - MECH-094         # hypothesis tag / commitment boundary state
  functional_restatement: >
    For each reafferent stream z_x, the forward model E2_x is a single
    substrate read in two distinct modes:

    Mode 1 -- retrospective comparator (attribution, post-action):
      residual = z_x_observed - E2_x(z_x_{t-1}, a_actual)
      -- computed after the action has been taken; residual IS the agency signal.

    Mode 2 -- prospective rollout-scoring (evaluation, pre-action):
      for candidate action a_i:
        predicted_trajectory = E2_x-rollout(z_x_t, a_i, horizon=k)
        value(a_i) = f(predicted_trajectory)
      -- computed before the action; used to rank candidates for action
      selection.

    Both modes share the same learned E2_x weights. A controller signal (at
    V3 candidate: the commitment boundary state / hypothesis tag MECH-094;
    at V4 candidate: dACC EVC signal following Shenhav 2013/2016, or
    heartbeat-phase gating following ARC-023) determines which mode is
    active.

    Falsifiable: if a single forward-model substrate cannot support both
    modes effectively (i.e. training for evaluator degrades comparator
    performance or vice versa), MECH-257 is refuted and the architecture
    must split into two substrates per stream (E2_x_comparator, E2_x_evaluator)
    doubling the parameter count.
  notes: >
    Biological grounding (three-pull synthesis 2026-04-18, 10 literature entries
    across the evaluator + mode-distinction pulls):

    Strongest single piece of evidence: Mattar & Daw 2018 (Nat Neurosci,
    "Prioritized memory access explains planning and hippocampal replay").
    Forward replay (planning / evaluator) and reverse replay (credit
    assignment / comparator-adjacent) derive from one prioritized-memory-
    access algorithm over a shared forward model.

    Direct biological precedent: within-session mode switching in one
    hippocampal population --
    - Diba & Buzsaki 2007 Nat Neurosci: forward / reverse replay in CA1 ripples.
    - Dragoi & Tonegawa 2011 Nature: preplay of future place-cell sequences
      from the same cells that express replay of past trajectories.
    - Kay et al 2020 Cell (Frank lab): sub-second cycling between alternative
      futures in CA1.

    Evaluator-mode readouts: Pezzulo 2014 (hippocampal sequences as forward
    rollouts), Rushworth 2011 (vmPFC/OFC value aggregation of rollouts),
    Daw 2011 (model-based striatal prediction errors).

    Arbitration: Shenhav 2013/2016 EVC theory gives a dACC-centred
    cost/benefit arbitrator between control modes; Rangel/Camerer/Montague
    2008 framework places the goal-directed / habitual arbitration in a
    single value-integration circuit. These converge on single-substrate
    with controller-gated readout rather than dual-substrate with
    parallel computation.

    Verdict from lit synthesis: moderate-high confidence (~0.7) that the
    single-substrate-with-gated-readout formulation matches biology better
    than a dual-substrate alternative. The decisive test would be
    single-unit / two-photon recording from a forward-model-coding
    population showing the same units contribute to both retrospective
    prediction-error and prospective rollout-evaluation within one
    session. No such paper in the pull; this is a stated open question.

    mem6_backref_2026_07_14 (IGW-20260714-119, memory_lifecycle_v4:MEM-6 plan
    reconcile): MECH-393 (retrieval-scope vs action-authority split) is a
    DISTINCT axis from MECH-257's read-mode arbitration. MECH-257 gates WHICH
    READ-MODE is active (retrospective comparator vs prospective rollout-scoring
    over one shared E2_x substrate); MECH-393 gates whether a retrieved trace
    carries ACTION AUTHORITY (a consolidated abstraction lacking source-episode
    grounding may inform reflection but not unilaterally drive committed action).
    The two compose -- action_authority is orthogonal to comparator/evaluator
    mode -- and the MECH-393->MECH-257 distinct_from link was previously
    one-directional; this note makes it bidirectional. No amendment to MECH-257
    (V4 substrate-conditional; promotes nothing; DO NOT build in V3).
  evidence_quality_note: >
    Literature pull 2026-04-18 (targeted_review_sd003_successor_evaluator,
    5 entries, and targeted_review_e2_dual_function_mode_distinction,
    5 entries; combined mean confidence ~0.68). Biological precedent moderate-
    high; V3 empirical test would require an experiment that trains E2_x for
    both modes on the same weights and measures whether mode-specific
    performance degrades relative to mode-split baselines. Not yet queued.
    EXQ-452 reclassified non_contributory (governance 2026-04-22, two FAIL runs
    20260420T144002Z + 20260420T155214Z): dual-function gated-readout test
    cannot resolve under the V_s-monostrategy substrate gap -- both reads
    operate over the same locked policy, so mode-specific performance
    differences cannot manifest. pending_retest_after_substrate (MECH-269 V_s
    landing).
  location: docs/architecture/self_attribution_per_stream.md

- id: SD-032
  title: "Cingulate integration substrate: a network-level module with AIC, dACC/aMCC, pMCC, pACC, and PCC analog subdivisions bound by a salience-network coordinator. Primary output: operating-mode variable plus behavioural-adjustment-magnitude signal."
  claim_type: design_decision
  subject: cingulate.integration_substrate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: 2026-04-19
  depends_on:
    - SD-011           # dual nociceptive streams (prerequisite, already active)
    - SD-012           # homeostatic drive / interoceptive baseline (required by SD-032c)
    - SD-020           # harm_surprise_PE (pain forward model, upgraded to prerequisite)
    - SD-021           # descending pain modulation (folds into SD-032c)
    - MECH-091         # salient-event phase reset (specific AIC->dACC mechanism)
    - MECH-094         # hypothesis tag / write gate (generalised by MECH-261)
    - MECH-220         # cingulate-insula harm hub (weakened by EXQ-395; replaced by this cluster)
  epistemic_category: substrate_conditional
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: (a) SD-032b built and exercised as an isolable
    module -- MET (dacc.py, salience_coordinator.py both exist, separately
    gateable). (b) Committed-action diversity must exist at all -- NOT MET:
    V3-EXQ-654g found action_class_entropy=0.0 across ALL arms, the identical
    MECH-439 conversion-ceiling confound. Until (b) clears, neither branch of
    the dissociation is answerable -- there is no behavioural variance for
    either branch to explain. Retest gated on the in-flight MECH-439/ARC-108/
    GAP-A front; do not re-open here.

    CONFIRMING (network-level framing correct): SD-032b alone (coordinator
    off) fails to produce coherent mode switching on salient events while the
    same task WITH the coordinator shows the switch occurring.

    FALSIFYING (network-level framing is over-specification): SD-032b alone
    reproduces both expected behavioural changes (z_harm_a varying trajectory
    selection; chronic-pain-like plasticity shift) with coherent mode-
    appropriate behaviour even without the coordinator engaged.

    (Digested 2026-08-07, session cool-torvalds-a82359, thought-digestion
    wave 11. SD-032a is independently confirmed stable, which raises the bar
    for the falsifying branch but does not settle it -- no ablation has
    actually isolated SD-032b without the coordinator.)
  functional_restatement: >
    ree-v3 currently produces z_harm_a but has no coordinated substrate
    for integrating it with cognitive control, urgency interrupt, effort
    credit assignment, or offline-mode coordination. This SD registers a
    cingulate integration substrate comprising five functional subdivisions
    (SD-032a through SD-032e) bound by a network-level salience-network
    coordinator (SD-032a).

    Key architectural claim: Options A / B / C from the z_harm_a consumer
    investigation (add affective cost to trajectory scoring / forward-
    rollout affective cost / urgency interrupt) are not alternatives --
    they are different subdivisions' jobs, sequenced by the salience-
    network coordinator. Option B (forward rollout) is prerequisite --
    z_harm_a enters as a precision-weighted prediction error (MECH-258),
    not as a raw magnitude. Option A (integration into action value) is
    the dACC/aMCC function (SD-032b). Option C (urgency interrupt) is
    what the salience-network coordinator produces when precision exceeds
    a switch threshold (MECH-259) -- it fires as a whole-system mode
    change, not a local cost modification.

    Operating mode (from SD-032a) gates downstream substrates: write-gates
    on E3, access to offline replay, policy commitment, and autonomic
    coupling. This generalises MECH-094's hypothesis tag to a mode-
    conditioned write gate (MECH-261).

    Implementation order (minimum viable first):
      1. Pain forward model (SD-020 + MECH-258: upgrade from provisional
         to prerequisite; build E2_harm_a shared-substrate with E2_harm_s).
      2. Homeostatic drive baseline (SD-012: required by SD-032c gating).
      3. dACC-analog (SD-032b): MINIMUM VIABLE substrate that resolves the
         EXQ-395 wiring gap -- integrates z_harm_a PE with control demand
         and writes a striatal-analog action-value target.
      4. AIC-analog + salience-network coordinator (SD-032c + SD-032a):
         adds the urgency-interrupt / mode-switching function.
      5. Mode-conditioned write gating (MECH-261): generalises MECH-094.
      6. PCC-analog (SD-032d) and pACC autonomic coupling (SD-032e):
         refinements.

    Steps 1-3 constitute the minimum viable cingulate substrate. The full
    cluster (steps 1-6) is the complete architectural target.

    Falsifiable: if a ree-v3 implementation of SD-032b alone reproduces
    the expected behavioural changes (z_harm_a varying trajectory
    selection; chronic-pain-like ACC-NAc plasticity shift under sustained
    harm) without requiring the coordinator (SD-032a), then the network-
    level framing is over-specification. Conversely, if SD-032b alone
    fails to produce coherent mode switching on salient events (e.g.,
    agent continues committed trajectory despite z_harm_a spike), then
    the coordinator is required -- and piecemeal subdivision wiring is
    not sufficient.
  notes: >
    Biological grounding from targeted_review_cingulate_integration_substrate
    (9 entries, 2026-04-19). Synthesis at
    evidence/literature/targeted_review_cingulate_integration_substrate/synthesis.md.

    Strongest single support: Shackman et al 2011 (Nat Rev Neurosci)
    coordinate-based meta-analysis of 380+ studies showing dACC/aMCC as
    a common substrate for pain, negative affect, and cognitive control.
    Network-level framing from Menon & Uddin 2010 (Brain Struct Funct)
    establishing the salience network (AIC + dACC) as a coordinator that
    switches between DMN and CEN on salient events. Subdivision anatomy
    from Vogt 2005 (Nat Rev Neurosci); computational form from Seymour
    2019 (Neuron) framing pain as a precision-weighted control signal in
    active inference.

    Conjectured origin of four recent FAILs: EXQ-395 MECH-220, EXQ-325a
    SD-021, EXQ-355a ARC-038, EXQ-435 INV-054 may all be surface
    expressions of the missing salience-network coordinator. Each FAIL
    shows a signal computed correctly at the substrate level but not
    producing coherent downstream behaviour because the coordinator for
    mode selection / network switching does not exist.

    MECH-220 (cingulate-insula harm hub) weakened by EXQ-395 is retained
    in the claim graph but its intended function is now subsumed by
    SD-032 + SD-032a.
  evidence_quality_note: >
    Registered pre-implementation on the basis of 9-entry lit-pull
    (2026-04-19); mean confidence ~0.80 across entries. No V3
    experimental evidence yet; dACC-analog implementation (SD-032b) is
    the first testable target. Design doc:
    docs/architecture/sd_032_cingulate_integration_substrate.md
    (subdivisions, MECH-261 write-gating profile, minimum-viable V3
    path, substrate-level falsification signatures).
  location: docs/architecture/sd_032_cingulate_integration_substrate.md
- id: SD-032a
  title: "Salience-network coordinator: network-level module reading AIC-, dACC-, MCC-, PCC-analog subdivisions and outputting a discrete operating-mode variable (external_task / internal_replay / internal_planning / offline_consolidation) plus a mode-switch trigger."
  claim_type: design_decision
  epistemic_category: standard
  subject: cingulate.salience_network_coordinator
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_SD-034-closure-cluster-ext_2026-06-12#V3-EXQ-467c"
      as_of: 2026-06-12
      verdict: non_contributory/substrate_ceiling
  what_would_answer: >
    CORE CLAIM (CONFIRMED, stable): the SalienceCoordinator substrate itself --
    a discrete operating_mode register {external_task, internal_replay,
    internal_planning, offline_consolidation} plus a mode_switch_trigger reading
    the AIC/dACC/MCC/PCC-analog subdivisions -- is implemented and behaviourally
    validated: V3-EXQ-446 (substrate-readiness) PASS, V3-EXQ-455 (behavioural)
    PASS, V3-EXQ-453 (write-gate landing) PASS; 28 supports / 0 weakens across
    11 experiment + 20 literature entries, conf 0.928, conflict_ratio 0. A
    CONFIRMING signature is any future evidence entry that restates this bar --
    already met and not expected to be re-litigated.

    REMAINING OPEN THREAD (not yet closed, does not implicate SD-032a): the
    2026-06-13 mode-governance-engagement fix (REEConfig.use_external_task_drive,
    still default=False / opt-in) was built to give the coordinator a genuine
    external_task salience SOURCE, closing the SD-034-closure-cluster-ext gap
    (fraction_in_external_task=0.0 on the pre-fix substrate). Its named
    validation successors, V3-EXQ-464d and V3-EXQ-467d, ran 2026-06-13 WITH the
    drive enabled and still self-routed non_contributory /
    substrate_not_ready_requeue (occupancy still 0.0 across every seed/arm).
    The follow-on diagnostic V3-EXQ-797 (2026-07-22) narrowed this further:
    it falsified nav-competence and frozen-argmax as causes and localised the
    gap to the BG/action-commitment layer (commit term fires on
    < BETA_FLOOR of genuine coordinator ticks) -- i.e. upstream of SD-032a's
    own substrate.

    UPDATE 2026-08-13 (confirmed failure_autopsy_mech266-464e-467e-cluster_2026-08-13,
    V3-EXQ-467e + V3-EXQ-464e): the commitment-layer fix landed and occupancy IS now
    reachable (1.0 at r=0.10 in 467e, sticky-arm 1.0 with zero switches in 464e) -- the
    predicted successor condition above is met, and MECH-266's own behavioural
    falsifiers are indeed now live, exactly as this paragraph anticipated. But the
    readiness gate itself (min-across-arms non-vacuity check) is now confirmed
    corrupting-severity on ree_core/cingulate/salience_coordinator.py +
    ree_core/utils/config.py -- it emits a factually-false route_reason when the mode
    is 100% occupied, and both fresh results self-route non_contributory for a NEW
    reason (bang-bang arbitration masked by the gate defect), not the old
    unreachability. This remains non_contributory against SD-032a -- the substrate
    itself is exercised and working; the residual is a downstream gate/measurement
    defect plus an over-correction (bang-bang, no mixed regime) in the
    mode-governance-engagement extension, neither of which implicates SD-032a's core
    (28 supports / 0 weakens, unchanged).

    FALSIFYING signature (would REVERSE stable status -- nothing currently
    threatens this): a well-powered future run in which the coordinator's
    mode_switch_trigger is reachable and exercised (i.e. the commitment-layer
    gate above is cleared, ruling out the substrate-not-engaged confound) and
    STILL shows operating_mode transitions decoupled from the underlying
    AIC/dACC/MCC/PCC-analog salience signals -- e.g. mode switches occurring
    at chance rate regardless of input -- would be a genuine weakens against
    the coordinator mechanism itself, not a readiness artifact, and should
    trigger stable -> candidate reconsideration.
  implementation_phase: v3
  v3_pending: false  # cleared 2026-04-22: V3-EXQ-446 substrate-readiness PASS + V3-EXQ-455 behavioural PASS + V3-EXQ-453 06:21 write-gate landing PASS. Promoted candidate -> provisional 2026-04-24 (governance): 21 supporting entries, 0 opposing, 1 genuine exp (V3-EXQ-445a mixed), conf 0.861. Promoted provisional -> stable 2026-04-25 (governance): conf 0.928, conflict_ratio 0, 28 supports / 0 weakens / 3 mixed across 11 exp + 20 lit entries.
  claim_level: architectural
  registered_utc: 2026-04-19
  instantiates: SD-032
  governance_note_2026_06_13: >
    Cross-ref (does not change SD-032a stable status): the MECH-266/SD-032a
    *behavioural* arms V3-EXQ-464c + 467c were reclassified non_contributory
    (substrate_ceiling) this cycle per confirmed
    failure_autopsy_SD-034-closure-cluster-ext_2026-06-12 -- the SalienceCoordinator
    never received signals putting the agent in external_task mode on the 603n
    foraging substrate (fraction_in_external_task=0.0), so the asymmetric exit-rail
    had no mode to bind. SD-032a's substrate (discrete operating_mode + switch
    trigger) is implemented and stable; what is missing is an external_task
    pressure SOURCE on the foraging agent. New substrate entry:
    mode-governance-engagement (behavioural arms only; status unchanged).
    SUBSTRATE LANDED 2026-06-13: ree-v3 REEConfig.use_external_task_drive
    (no-op-default) registers a committed-goal-pursuit engagement signal on the
    SalienceCoordinator in both affinity_weights (-> external_task) and
    salience_weights (switch-into-external_task aggregate). SD-032a status UNCHANGED
    (stable); validation via the MECH-266 behavioural successors V3-EXQ-464d/467d with
    the occupancy-keyed readiness gate. See ree-v3/CLAUDE.md mode-governance-engagement.
  depends_on:
    - SD-032           # parent cluster
    - SD-032b          # dACC-analog (contributor signal)
    - SD-032c          # AIC-analog (primary trigger source)
    - SD-032d          # PCC-analog (attention-partition contributor)
    - MECH-259         # switch threshold mechanism
  functional_restatement: >
    Network-level coordinator that binds the cingulate subdivisions into
    a coherent salience network. Reads signals from all five subdivisions
    and outputs:

      (i) operating_mode: a discrete mode variable in
          {external_task, internal_planning, internal_replay,
          offline_consolidation}
      (ii) mode_switch_trigger: boolean, fires when the salience-weighted
          input from SD-032c (AIC-analog) or any other subdivision
          exceeds the switch threshold (MECH-259)

    On mode_switch_trigger, the coordinator transitions operating_mode
    atomically and broadcasts the new mode to downstream gates
    (E3 write-gate, policy commitment, replay access, autonomic
    coupling). Mode transitions are discrete, not graded -- though the
    switch threshold itself may be graded and learnable.

    Biological analogue: the AIC-dACC coupled salience network (Menon &
    Uddin 2010) that switches between large-scale brain networks (DMN
    vs CEN) on salient events.

    Falsifiable: if a ree-v3 architecture with independently operating
    SD-032b/c/d/e (no coordinator) reproduces coordinated mode switching
    on salient events, SD-032a is over-specification. Conversely, if
    subdivision activity is observed to be uncorrelated or
    uncoordinated under a single-coordinator design, the coordinator is
    doing nothing and needs redesign.
  notes: >
    Network-level framing from Menon & Uddin 2010 (Brain Struct Funct);
    mode-switching function from Craig 2009 (Nat Rev Neurosci) AIC account.
  evidence_quality_note: >
    Registered pre-implementation. See SD-032 parent.

    [2026-08-16 governance apply | V3-EXQ-934 -> SD-032a | failure_autopsy_V3-EXQ-934_2026-08-16] DISPOSITION: evidence_direction non_contributory. Narrow diagnostic support only. Where the argmax crossing sits, the SD-032a mode register produced genuine discrete alternation (seed 42 @ cap 0.75: 19 switches, ext_dwell_mean 43.2; seed 43 @ cap 1.75: 18 switches, ext_dwell_mean 53.5) with the MECH-259 switch trigger firing -- the first alternating occupancy in this lineage, which previously read 0.0 or 1.0 everywhere. Note SD-032a's own functional_restatement specifies that mode transitions ARE discrete, not graded, so the bang-bang shape reported by 464e/467e is SD-032a behaving as written and is NOT evidence against it; what remains unresolved is whether a contested-occupancy window wide enough for MECH-266's hysteresis to express is obtainable, which is V3-EXQ-935's question. Add the scoping evidence_quality_note above -> standard

    V3-EXQ-934 (diagnostic, excluded from confidence scoring; GOV-FANOUT-1 leg H1 of frozen-ledger question mech266_mode_arbitration_saturation). Both readiness preconditions cleared on measured, non-vacuous values (contact guard 3/3 seeds, contact rates 0.25-0.31, z_goal peaks 0.433-0.492 against a 0.4 gate; margin guard 3/3, though at 7-16x its floor it is a liveness check rather than a strong control). On the primary ARM_SYMMETRIC the single load-bearing criterion passed at exactly the 2/3 minimum. READ IT NARROWLY. The criterion is an EXISTENTIAL over the 5-point cap grid evaluated PER SEED, and the two passing seeds' mixed cells sit at OPPOSITE ENDS of the sweep (seed 42 only at cap 0.75, occupancy 0.5606; seed 43 only at cap 1.75, occupancy 0.4447; seed 44 never, minimum 0.9002 against a 0.9 ceiling). NO single cap grades on more than 1 of 3 seeds (0.75 -> 1/3; 1.0/1.25/1.5 -> 0/3; 1.75 -> 1/3), so winning_cap_band_symmetric = [0.75, 1.75] is the min/max of two disjoint singletons, not a band, and no common operating point was demonstrated. The mixed cells ARE non-degenerate (18-19 switches, mode-conditioned external_task dwell 43-54 steps), so the SD-032a mode register does genuinely alternate where the argmax crossing sits -- but that crossing is at most one grid step (0.25 cap units) wide in every seed showing it, and its location differs by >= 1.0 cap units across seeds, so an existential on a 5-point grid detects a CROSSING and cannot discriminate H1 (graded arbitration) from H2 (structurally bang-bang). H2 is NOT eliminated by this run. TWICE NON-PRODUCTION: salience_affinity_input_cap defaults to None (no clamp) and use_external_task_drive defaults to False, so the swept configuration is not production; training was held at cap 2.0, itself outside the swept range; and the driver's own pre-authoring probe recorded operating_mode['external_task'] = 0.0 at the production default cap=None. Mechanistically the clamp is a no-op on external_task_drive at every cap >= 1.0 (that signal is already bounded to [0,1] at agent.py:6958), so 4 of the 5 swept cells manipulate only the competing unbounded affinity inputs; and _et_commit = commit_weight * float(beta_gate.is_elevated) (agent.py:6944) is a boolean latch -- a second, cap-INDEPENDENT discreteness source no cap can grade. Failure-location (GOV-FAILLOC-1): MIXED (MECHANISM + MEASURES); environment adequate, implementation partial, measurement under-instrumented; not chargeable to REE. Successor V3-EXQ-935 (claimed 2026-08-16T13:01Z) already tests the common-operating-point question via a margin-normalised cap rule on 5 seeds.

    2026-08-18 (governance apply, V3-EXQ-935, confirmed
    failure_autopsy_V3-EXQ-935_2026-08-18): direction CORRECTED weakens -> supports (narrow,
    diagnostic; excluded from confidence scoring). The driver stamped weakens from the same
    else-branch that emitted a factually false route_reason ("no common normalised rule
    outperformed the best absolute cap") while c3_beats_best_absolute_cap was TRUE. On the
    data the SD-032a mode register produced graded, non-degenerate occupancy spanning
    0.124-0.925 with 15-26 genuine switches per cell on 4 of 5 seeds across a monotone dose-
    response -- its best performance in this lineage -- and alternates at R_STAR=2.25 as
    well, so the correction does not rest on the post-hoc r. Consistent with the V3-EXQ-934
    autopsy, which already recommended supports for SD-032a on narrower grounds. Status
    stays stable.
  implementation_note: >
    IMPLEMENTED 2026-04-19. Module: ree_core/cingulate/salience_coordinator.py
    (SalienceCoordinator, SalienceCoordinatorConfig, DEFAULT_MODE_NAMES,
    DEFAULT_GATE_WEIGHTS). Non-trainable arithmetic. Reads SD-032b dACC
    bundle (pe / foraging_value / choice_difficulty), SD-012 drive_level,
    and the agent offline-mode flag (proxy for SD-032d stability scalar
    until SD-032d lands). Registered input slots aic_salience /
    pcc_stability / pacc_autonomic are no-op until SD-032c/d/e land --
    callers extend via update_signal(name, value). Outputs operating_mode
    (soft probability vector over four V3 modes via softmax over per-mode
    affinity logits, default biased to external_task), discrete current_mode
    updated only on threshold crossing (hysteresis), and MECH-259
    mode_switch_trigger boolean. Hosts MECH-261 dict-keyed write-gate
    registry populated from the SD-032 spec table; write_gate(target_name)
    returns the soft-weighted sum. mode_names is a list (not a fixed tuple)
    and register_target allows V4 substrates (SD-033e
    parallel_goal_deliberation) to register their own gate profile without
    coordinator schema changes. Integrated into ree_core/agent.py
    REEAgent.select_action immediately after the dACC bundle is built;
    ticks on every action selection.
    Config: REEConfig.use_salience_coordinator (bool, default False).
    Sub-knobs: salience_switch_threshold (default 1.0),
    salience_stability_scaling (1.0), salience_softmax_temperature (1.0),
    salience_external_task_bias (1.0), salience_dacc_pe_weight (1.0),
    salience_dacc_foraging_weight (0.5), salience_apply_to_dacc_bias
    (False -- when True, scales dACC score_bias by the e3_policy
    write-gate so internal_replay attenuates dACC influence on action
    selection).
    Backward compatible: use_salience_coordinator=False by default; existing
    experiments unaffected. DACCtoE3Adapter is RETAINED as the score_bias
    source until SD-033 substrates consume operating_mode natively (staged
    removal). MECH-094: not authored here -- MECH-261 generalises MECH-094
    via the per-target gate registry.
    Validation experiment: V3-EXQ-446 queued.
  location: evidence/literature/targeted_review_cingulate_integration_substrate/synthesis.md
- id: SD-032b
  title: "dACC/aMCC-analog (adaptive control): module that reads z_harm_a precision-weighted prediction error, z_conflict, and control-demand signals; outputs behavioural-adjustment magnitude; writes to a learnable striatal-analog action-value target."
  claim_type: design_decision
  subject: cingulate.dacc_analog_adaptive_control
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-445h_2026-06-19#V3-EXQ-445h"
      as_of: 2026-06-19
      verdict: non_contributory/substrate_ceiling
  implementation_phase: v3
  v3_pending: true
  epistemic_category: substrate_ceiling
  pending_retest_after_substrate: true  # c2 retest gated on committed-action diversity (ARC-065 GAP-A / MECH-439); failure_autopsy_V3-EXQ-445h_2026-06-19
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: shares the SHARED DOWNSTREAM CONVERSION CEILING
    precondition already registered on ARC-063 (2026-08 digestion wave 11), reused
    verbatim per ARC-063's own cross-reference note rather than re-derived:

    "SHARED DOWNSTREAM CONVERSION CEILING. Every ARC-062 GAP-B behavioural
    falsifier run (V3-EXQ-654g/654j/485h/485m/714/719a, 21 autopsies) shows
    the identical C1-holds/C2-fails signature: rule-biased signal reaches E3
    with genuine authority but fails to convert to committed-class diversity
    (MECH-439 F-dominance conversion ceiling, shared by both readings,
    in-flight via ARC-108/MECH-450). Until this lifts, any committed-action
    strong-vs-weak test is confounded." (ARC-063, precondition 2)

    As of 2026-08-02 this remains unresolved: V3-EXQ-654g itself (SD-032b's
    named retest gate) is unchanged (C1 fully met, C2 failed +0.011 nats,
    0/3 seeds, non_contributory). Adjacent GOV-FANOUT-1 diagnostic churn since
    (V3-EXQ-851/859/863) did NOT move this front -- a corrective autopsy found
    those runs never validly exercised route_source='lateral_pfc' at all (an
    unrelated diagnostic flag, e3_score_decomp_enabled, silently no-ops that
    channel) -- measurement_test_design_defect, not new evidence either way.
    V3-EXQ-654g's own cand_world_summary route is confirmed unaffected, so
    SD-032b's gate is unchanged, not further resolved.

    SD-032b-SPECIFIC ADDITIONS to the shared precondition, neither yet met:

    (a) z_harm_a_PE GENUINELY PRECISION-WEIGHTED AND FLOWING. MECH-258
    (precision-weighted PE driving dACC policy update, the claim's own
    prerequisite) is itself status: candidate / v3_pending -- not yet
    substrate-validated as live.

    (b) COMMITTED-ACTION DIVERSITY MUST EXIST AT ALL. failure_autopsy_
    V3-EXQ-445h (2026-06-19, confirmed) found action_class_entropy=0.0
    across ALL arms -- floor-locked before any dACC-specific signal could
    be read. Same symptom as ARC-062's repeated C2 failures.

    CONFIRMING signature (once both SD-032b-specific gaps AND the shared
    ceiling clear): per the claim's own functional_restatement "Falsifiable:"
    clause -- dACC-analog output magnitude correlates positively and
    significantly with precision-weighted z_harm_a_PE magnitude, AND action
    selection shifts (paired by seed) toward harm-reducing trajectories with
    magnitude scaling as the PE -- i.e. Baliki's ACC-NAc coupling (magnitude-
    graded ACC->striatal weight change) is reproduced.

    FALSIFYING signature: already-observed pattern (445h) -- committed-action-
    entropy stays at floor (0.0) regardless of dACC-analog activation. Per the
    claim's own ceiling_routing_note, this is READ AS SUBSTRATE-BLOCKED, NOT
    as falsifying SD-032b -- confounded by MECH-439, not yet a test of the
    dACC mechanism itself.
  claim_level: architectural
  registered_utc: 2026-04-19
  ceiling_decision: deferred
  ceiling_routing_note: "2026-06-19 substrate-ceiling-lifted triage (verdict b deliberate-defer): MECH-269 per-region V_s landing is implemented but post_implementation_validation_status wired_but_inert on this env and has been SUPERSEDED as the operative blocker by the F-dominance conversion ceiling (MECH-439: F ~88-89% of E3 variance). dACC c2 (committed-action-entropy shift) is floor-locked because committed-action diversity does not exist (445h: action_class_entropy=0.0 all arms). Retest gated on the live MECH-439/GAP-A top-k conversion front (in flight as V3-EXQ-654g); never re-run on the 445h surface."
  instantiates: SD-032
  depends_on:
    - SD-032           # parent cluster
    - SD-020           # harm_surprise_PE (upgraded to prerequisite; MECH-258)
    - MECH-258         # precision-weighted PE drives dACC policy update
    - MECH-260         # bias suppression against recency / monostrategy
    - SD-003           # counterfactual agency attribution (shares E2_harm_a forward-model substrate)
    - ARC-033          # E2_harm_s forward model (parallel architecture; E2_harm_a reuses substrate)
  functional_restatement: >
    The dACC-analog is the MINIMUM VIABLE cingulate substrate: it alone
    resolves the EXQ-395 z_harm_a wiring gap.

    Inputs:
      - z_harm_a_PE: prediction error from E2_harm_a forward model
        (precision-weighted; see MECH-258)
      - z_conflict: difference between top-ranked and runner-up
        trajectory values (or similar conflict signal from E3)
      - control_demand: expected-value-of-control estimate (how much
        policy revision is warranted by current context)

    Computation: integrated adaptive-control function following Shackman
    et al 2011 (adaptive control hypothesis) with computational form
    given by Seymour 2019 (precision-weighted pain as control signal)
    and Shenhav 2013/2016 EVC. Output is a behavioural-adjustment
    magnitude.

    Output target: a striatal-analog action-value module (Baliki et al
    2010 ACC-NAc coupling) with a learnable weight. Dopamine-analog
    credit-assignment via SD-003 counterfactual attribution reshapes the
    ACC -> striatum weight based on whether actions successfully reduced
    z_harm_a.

    Falsifiable: if the dACC-analog output does not shift action
    selection toward harm-reducing trajectories with magnitude scaling
    as the precision-weighted PE, the module has failed to reproduce
    Baliki's NAc-pain-value coupling. If implementation produces
    coordinated behavioural change WITHOUT needing a separate
    coordinator (SD-032a), SD-032 parent cluster should be simplified.
  notes: >
    This is the first substrate to implement in the cluster. Resolves
    EXQ-395 MECH-220 wiring gap. Primary architectural grounding:
    Shackman 2011 (dACC as integration hub), Baliki 2010 (ACC-NAc
    pathway for affective pain to action value), Seymour 2019
    (precision-weighted control-signal form).
  evidence_quality_note: >
    Registered pre-implementation. Minimum-viable target. See SD-032 parent.
    Governance 2026-04-22: V3-EXQ-445 + 445a/b/c (dACC analog substrate, full
    pipeline, epsilon diversity, larger env) all reclassified non_contributory
    -- the dACC bundle is computing correctly but the consumer pathway is
    V_s-monostrategy-blocked, so behavioural-adjustment magnitude cannot be
    measured. SD-032b validation deferred until MECH-269 V_s landing unlocks
    alternative operating modes; pending_retest_after_substrate.

    [2026-05-08 governance]: EXQ-445h SD-032b does_not_support (4th consecutive null after 445/445a/b/c). MECH-258 (C1 wins=2/3) and MECH-260 (C3 wins=3/3) both supported in the same run. Caution flag: monostrategy may stem from substrate gaps not yet inventoried (e.g. previous-valence-on-unexpected, MECH-307 conjunction architecture, sleep substrate). Do NOT advance toward demote until the candidate-gap inventory broadens.

    [2026-06-19 confirmed failure_autopsy_V3-EXQ-445h -- supersedes the 2026-05-08 does_not_support framing]: All V3-EXQ-445-series c2 evidence is non-contributory -- the committed-action-entropy criterion (|entropy_ON-entropy_OFF|>=0.1) is floor-locked by the pre-conversion-ceiling monostrategy (action_class_entropy=0.0 in every arm incl OFF control; F-dominance V3-EXQ-571). The dACC substrate is functional (mean_score_bias_abs 2.0 ON vs 0.0 OFF; harm_a_forward_r2 ~0.94) but the applied precision-weighted conflict bias cannot move committed action until ARC-065 GAP-A / MECH-439 demonstrate committed-action diversity. pending_retest_after_substrate. epistemic_category set explicit substrate_ceiling this pass; manifests already carry non_contributory (exp_conf 0.413 -> 0.0). Routed /queue-experiment (gated): fixed-substrate c2 retest under GAP-A top-k / MECH-439 conflict-grade, never re-run under 445h.

    [enum-comment migrated to field 2026-07-18] epistemic_category: explicit 2026-06-19
    (confirmed failure_autopsy_V3-EXQ-445h): V3-tractable in principle but the substrate is too
    coarse to deliver the committed-action-shift distinction c2 needs (conversion ceiling /
    F-dominance)
  implementation_note: >
    IMPLEMENTED 2026-04-19. Module: ree_core/cingulate/dacc.py
    (DACCAdaptiveControl, DACCConfig, DACCtoE3Adapter). Integrated into
    ree_core/agent.py (REEAgent.select_action -- builds Croxson integration
    bundle; adapter converts to score_bias[K] passed to E3.select()).
    Config: REEConfig.use_dacc (bool, default False). Sub-weights:
    dacc_weight (overall gain), dacc_interaction_weight (payoff x effort
    interaction), dacc_foraging_weight (Kolling 2015 switch-value),
    dacc_suppression_weight (MECH-260 recency suppression),
    dacc_suppression_memory (history window, default 8),
    dacc_precision_scale (PE precision normaliser, default 500),
    dacc_effort_cost (Shenhav 2013 EVC cost, default 0.1),
    dacc_drive_coupling (SD-012 hook, default 0).
    Bundle: {mode_ev[K], choice_difficulty, foraging_value,
    harm_interaction[K], suppression[K], pe, drive_gain}.
    Prerequisite MECH-258 satisfied via new E2_harm_a forward model
    (ree_core/predictors/e2_harm_a.py, supports both independent
    ARC-033-parallel path and ARC-058 shared-trunk path).
    Stopgap DACCtoE3Adapter: converts bundle to E3 score_bias. Marked for
    removal / replacement when SD-032a salience-network coordinator lands
    (coordinator is the architectural consumer of the bundle per SD-032
    design; adapter is a shim to route the bundle to E3 in its absence).
    Phased training (P0/P1/P2) required for E2_harm_a: callers must
    .detach() z_harm_a targets during P1. Validation experiment:
    V3-EXQ-445 queued (3 conditions x 3 seeds: dACC-OFF vs dACC-ON-
    independent vs dACC-ON-shared-trunk).
    Backward compatible: use_dacc=False by default; existing experiments
    unaffected. Non-noop flags: use_e2_harm_a, use_shared_harm_trunk.
  location: evidence/literature/targeted_review_cingulate_integration_substrate/synthesis.md
- id: SD-032c
  title: "AIC-analog (interoceptive salience / urgency interrupt): module that integrates interoceptive baseline (drive/fatigue from SD-012) with affective-pain signal (z_harm_a), detects salient events, and triggers a network-switch signal to the salience-network coordinator (SD-032a)."
  claim_type: design_decision
  subject: cingulate.aic_analog_salience_urgency
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:SD-032c@2026-04-25T15:42:09.107886Z"
      as_of: 2026-04-25
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: 2026-04-19
  instantiates: SD-032
  depends_on:
    - SD-032           # parent cluster
    - SD-032a          # coordinator (receives switch trigger)
    - SD-012           # homeostatic drive (interoceptive baseline; required)
    - SD-021           # descending pain modulation (folded into this module)
    - MECH-091         # salient-event phase reset (specific AIC->dACC mechanism)
    - MECH-259         # switch threshold
  functional_restatement: >
    AIC-analog gates the urgency-interrupt pathway. It is NOT the
    affective-pain consumer (that is SD-032b). It is the module that
    decides when the current operating mode is no longer sustainable and
    must be interrupted.

    Inputs:
      - z_harm_a: affective pain signal (also read by SD-032b for smooth
        integration)
      - drive_level, fatigue, metabolic estimate (from SD-012): the
        interoceptive baseline against which z_harm_a is evaluated
      - other salient-event signals: unexpected z_goal drop, surprise
        on reward prediction, detection of irreversibility

    Computation: salience = f(z_harm_a, interoceptive_baseline, ...);
    if salience exceeds switch threshold (MECH-259), fire network-switch
    trigger to SD-032a.

    Also subsumes SD-021 descending pain modulation (currently broken
    in EXQ-325a): in biology, the descending pain-modulation pathway
    from ACC/AIC / periaqueductal gray attenuates sensory pain during
    committed action. In ree-v3, SD-032c modulates z_harm_s gain as a
    function of current operating mode (attenuated during committed
    external_task, unattenuated during internal_planning).

    Falsifiable: if ree-v3 implementation does not produce the expected
    interoceptive-baseline dependence (same z_harm_a triggers different
    behaviour in depleted vs well-resourced agent), SD-032c is failing
    to gate on interoceptive state -- the gating design is wrong.
  notes: >
    Biological grounding: Craig 2009 (Nat Rev Neurosci) AIC as
    interoceptive-salience hub with direct autonomic and motor efferents;
    Menon & Uddin 2010 salience network coupling. SD-021 folded here
    because the descending-modulation function in biology lives in this
    pathway.
  evidence_quality_note: >
    Registered pre-implementation. See SD-032 parent.
    Governance 2026-04-22: V3-EXQ-325d (SD-032c AIC descending modulation,
    2026-04-20) reclassified non_contributory under same V_s-monostrategy
    substrate gap that blocks SD-032b validation. AIC harm_s_gain machinery
    is wired and structurally drive-dependent, but behavioural divergence
    cannot manifest until MECH-269 V_s landing unlocks alternative operating
    modes. pending_retest_after_substrate.
    AICConfig). Non-trainable arithmetic over scalars with single EMA
    interoceptive baseline. Outputs aic_salience (fed to SalienceCoordinator
    via update_signal before coordinator.tick) and harm_s_gain (multiplier
    on z_harm replacing legacy SD-021 raw-beta-gate check when
    use_aic_analog=True). Config flag REEConfig.use_aic_analog (default
    False); alterable-configuration knob aic_drive_protect_weight (+1/0/-1
    regimes) implements the spec's testable drive-dependence configuration.
    Both aic_salience and harm_s_gain depend structurally on drive_level --
    only V3 substrate that makes the dependence structural. Subsumes SD-021:
    EXQ-325a FAIL (DESCENDING == CONTROL bit-identical) resolves because the
    descending branch is now a genuinely different function of state.
    Validation experiment: V3-EXQ-325b ran 2026-04-18 to 2026-04-19, result
    FAIL (bit-identical DESCENDING==CONTROL, 3 seeds) -- see SD-021's
    evidence_quality_note for the full trace. v3_pending remains true;
    blocked on MECH-269 V_s landing (see what_would_answer). See
    ree-v3/CLAUDE.md SD-032c entry for full data flow and config
    documentation.

    [2026-08-08 governance, V3-EXQ-878/878a, confirmed failure_autopsy_V3-EXQ-878a_2026-08-08,
    co-tagged on MECH-332/SD-021]: same commitment-gate precondition gap as MECH-332/SD-021's
    own notes -- E3's commitment gate never elevates during eval even at 3x budget across an
    18-cell sweep (running_variance_mean_fresh pinned at precision_init 0.5, never trending
    toward commit_threshold). This is a DIFFERENT precondition than the V_s-monostrategy lock
    already documented above (that blocks the DESCENDING-vs-CONTROL contrast once commitment
    fires; 878/878a block commitment from firing at all). non_contributory/substrate_conditional.
    routing=governance-note-only. UNRESOLVED FLAG from the autopsy itself, not reconciled this
    cycle: the autopsy's learning_extracted notes that "SD-032c's 'newly unblocked' status from
    round 4 needs reconciliation against claims.yaml's current text and this run's fresh finding
    before anyone acts on it" -- no "round 4" / "newly unblocked" text was found anywhere in this
    claim's current text during this pass, so the source of that note is unidentified; leaving
    unresolved rather than guessing at a fix. Also part of the 3rd/4th-independent-experiment
    ceiling-review flag on MECH-332 -- see that claim's note for the fuller finding.

    [2026-08-22 correction, chip-20260822-exq325-family-evidence-disposition]: the 2026-04-19
    expectation above -- "descending branch is now a genuinely different function of state [once
    SD-032c/AIC-analog is implemented]" -- did not hold, and the reason was not V_s-monostrategy
    lock-in. harm_descending_mod_enabled is a REEConfig field, not a declared parameter of
    REEConfig.from_dims(), so every v3_exq_325a/325d driver that passed it as a from_dims()
    kwarg had it silently swallowed by the **kwargs catch-all: the top-level gate on
    ree_core/agent.py's descending-modulation consumer (`if harm_descending_mod_enabled and
    new_latent.z_harm is not None:`, ~line 4830) was never entered in ANY condition of ANY of
    these runs, so neither the legacy nor the AIC-gated branch ever ran -- independent of
    whether use_aic_analog/aic_drive_protect_weight were set correctly (they were; both ARE
    declared from_dims() parameters and reach REEConfig intact). SD-032c's own AIC-analog
    substrate was therefore never actually exercised by V3-EXQ-325d either; "AIC harm_s_gain
    machinery is wired and structurally drive-dependent" (above) is a code-level property, not
    something these runs demonstrated in practice. This claim's v3_pending gate on MECH-269 V_s
    landing is NOT withdrawn by this correction -- the drive-regime behavioural-divergence test
    this claim's what_would_answer specifies (AIC_ON vs AIC_DRIVE_ABLATED producing different
    behaviour under different drive regimes) plausibly does need genuine behavioural diversity,
    which is a materially different requirement from SD-021's own C1/C2 criteria (see that
    claim's parallel correction, which DOES narrow its gate). Also found in this pass: the
    325d driver passes heartbeat=HeartbeatConfig(beta_gate_bistable=True) as a from_dims()
    kwarg, likewise silently dropped -- beta_gate_bistable was False throughout V3-EXQ-325d,
    contrary to design intent, and is not yet fixed by the in-progress repair
    (chip-20260822-fromdims-exq325-dead-ablation-axis). Any re-run intended to actually
    exercise this claim needs both drops closed first. Manifest evidence_direction for
    V3-EXQ-325d is unchanged (non_contributory); its evidence_direction_note is updated with the
    same split.
  epistemic_category: substrate_conditional
  what_would_answer: >
    NON-DEGENERACY PRECONDITION, shared with sibling claim SD-021: MECH-269
    V_s landing must occur first. AIC-analog (ree_core/cingulate/aic_analog.py)
    is implemented and structurally drive-dependent -- both aic_salience and
    harm_s_gain are computed as functions of drive_level via the
    aic_drive_protect_weight knob (+1/0/-1 regimes) -- but the agent is
    monostrategy-locked until MECH-269 lands alternative operating modes.
    Without that, there is no behavioural channel through which a structural
    drive-dependence could show up as a measurable difference in agent
    behaviour; the test is vacuous, not merely underpowered. V3-EXQ-325d
    (2026-04-20) confirmed this reclassification to non_contributory under
    the same substrate gap. Re-verified 2026-08-08: MECH-269 is still
    status=candidate, v3_pending=true -- not landed. aic_analog.py unchanged
    materially since 2026-04-22 (last touch to aic_drive_protect_weight or
    harm_s_gain was the original 2026-04-19 IMPLEMENTED commit; a 2026-05-31
    commit touched only consumer-side per_axis_drive wiring).
    CONFIRMING: once MECH-269 unblocks alternative operating modes: for
    matched z_harm_a input, a depleted agent (low drive_level / high
    fatigue) and a well-resourced agent produce measurably different
    aic_salience values, such that the MECH-259 switch threshold is crossed
    (network-switch trigger fires to SD-032a) in one regime and not the
    other, or fires at reliably different latency/magnitude.
    FALSIFYING: once MECH-269 has landed and alternative operating modes are
    confirmed to produce genuine behavioural divergence elsewhere in the
    agent (ruling out "the substrate still doesn't support divergence" as
    the explanation), depleted and well-resourced agents nonetheless show
    statistically indistinguishable network-switch-trigger behaviour for
    matched z_harm_a. This would mean SD-032c's salience/threshold gating
    design fails to translate structural drive-dependence into behavioural
    effect -- the gating computation, threshold placement, or its coupling
    to SD-012's drive signal would be wrong, not merely untested.
  location: evidence/literature/targeted_review_cingulate_integration_substrate/synthesis.md
- id: SD-032d
  title: "PCC-analog (attention partition / metastability): module coordinating transitions between external-task and internal (replay / planning / consolidation) modes; contributes a mode-stability parameter to the salience-network coordinator (SD-032a)."
  claim_type: design_decision
  subject: cingulate.pcc_analog_attention_partition
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:SD-032d@2026-04-25T15:42:09.107888Z"
      as_of: 2026-04-25
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: 2026-04-19
  instantiates: SD-032
  depends_on:
    - SD-032           # parent cluster
    - SD-032a          # coordinator
    - INV-049          # sleep necessity (offline phase coordination)
    - MECH-092         # micro-quiescence replay
    - MECH-094         # hypothesis tag (write gating during mode)
  functional_restatement: >
    PCC-analog handles the external-vs-internal attention partition. It
    is NOT the primary trigger for mode switching (that is SD-032c). It
    is the module that (i) biases toward internal cognition when task
    demands drop, (ii) coordinates within-session offline phases
    (micro-quiescence replay, MECH-092; sleep, INV-049), and (iii)
    contributes a mode-stability parameter -- how committed the current
    mode should be -- to SD-032a.

    Biological analogue: PCC as DMN core node with Leech & Sharp 2013
    "Arousal, Balance, Breadth" framing. PCC activity high during
    internally-directed cognition, low during externally-directed
    attention.

    Conservative computational specification: PCC-analog outputs a
    scalar stability parameter in [0, 1] that modulates the switch
    threshold (MECH-259) in SD-032a. High stability = coordinator
    resists mode transitions; low stability = transitions happen at
    lower salience levels. Stability is itself a function of recent
    task success, fatigue, and time-since-last-offline-phase.
  notes: >
    PCC is the least-tight anatomical mapping in the cluster (Leech &
    Sharp 2013 "ABB" model is a proposal, not consensus). Implementation
    should stay conservative: external-vs-internal attention coordinator
    plus mode-stability parameter. Over-fitting the full metastability
    framework is likely over-commitment.
  evidence_quality_note: >
    Registered pre-implementation. See SD-032 parent. IMPLEMENTED 2026-04-19
    in ree_core/cingulate/pcc_analog.py (PCCAnalog, PCCConfig). Non-trainable
    arithmetic over success EMA + drive_level + steps-since-last-offline.
    Output pcc_stability injected into SalienceCoordinator before tick() each
    cycle so MECH-259 effective_threshold is modulated on the current step.
    Single integration point with MECH-092/INV-049 via agent.enter_offline_mode()
    -> pcc.note_offline_entry(). Master switch use_pcc_analog=False (default,
    backward compatible). Validation experiment: V3-EXQ-447 queued.
  implementation_note: >
    IMPLEMENTED 2026-04-19. Module ree_core/cingulate/pcc_analog.py.
    Config: REEConfig.use_pcc_analog (default False) plus six sub-knobs
    (pcc_success_alpha=0.02, pcc_success_weight=0.5, pcc_fatigue_weight=0.5,
    pcc_offline_recency_window=500, pcc_offline_weight=0.3,
    pcc_stability_baseline=0.5). Agent integration: REEAgent.__init__
    instantiates self.pcc when flag set; select_action() ticks pcc and
    injects pcc_stability into SalienceCoordinator before coordinator.tick();
    enter_offline_mode() calls pcc.note_offline_entry(); reset() calls
    pcc.reset() (per-episode; cross-episode counter preserved). Convenience
    method REEAgent.note_task_outcome(value) feeds the PCC success EMA.
  location: evidence/literature/targeted_review_cingulate_integration_substrate/synthesis.md
- id: SD-032e
  title: "pACC-autonomic coupling: module connecting affective-pain stream (z_harm_a) to valence/drive update pathway. Provides the affective-pain -> autonomic/visceral coupling that Vogt 2005 and Craig 2009 locate in perigenual/subgenual ACC."
  claim_type: design_decision
  subject: cingulate.pacc_autonomic_coupling
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_453_mech261_write_gate_landing_20260420T062156Z_v3
      as_of: 2026-04-20
      verdict: diagnostic/PASS
  implementation_phase: v3
  v3_pending: false  # cleared 2026-04-22: V3-EXQ-453 06:21 write-gate landing PASS validates SD-032e write-gate registration in salience coordinator. Promoted candidate -> provisional 2026-04-24 (governance): 3 supporting entries + 1 mixed, 0 opposing, conf 0.830. Promoted provisional -> stable 2026-04-25 (governance): conf 0.894, conflict_ratio 0, 5 supports / 0 weakens / 4 mixed across 5 exp + 4 lit entries.
  claim_level: architectural
  registered_utc: 2026-04-19
  instantiates: SD-032
  depends_on:
    - SD-032           # parent cluster
    - SD-011           # z_harm_a stream (input)
    - SD-012           # homeostatic drive / valence state (update target)
  functional_restatement: >
    pACC-analog provides a write-path from z_harm_a into the homeostatic /
    valence-state vector (SD-012). In biology, perigenual and subgenual
    ACC drive autonomic and endocrine responses to pain; in ree-v3, this
    pathway ensures sustained affective pain shifts the agent's
    interoceptive baseline (drive_level, valence) over longer timescales
    than a single action cycle.

    Distinct from SD-032b (which consumes z_harm_a for action-value) and
    from SD-032c (which uses interoceptive baseline to gate salience).
    SD-032e is the write-back channel that slowly updates the baseline
    itself.

    Behavioural prediction: sustained z_harm_a exposure produces drift
    in homeostatic state (drive_level), which in turn modulates
    SD-032c's salience threshold, producing the chronic-pain-like
    sensitization Baliki 2012 documents. Without SD-032e, z_harm_a has
    no path into long-timescale state; the agent cannot become
    "chronically stressed" from sustained harm exposure.
  notes: >
    Biological grounding: Vogt 2005 (pACC/sgACC autonomic role);
    Craig 2009 (AIC-ACC coupling to autonomic efferents). Lowest-priority
    subdivision in the implementation order -- refines SD-012 over time
    but is not required for the minimum-viable substrate.
  evidence_quality_note: >
    Registered pre-implementation. See SD-032 parent.
  implementation_note: >
    IMPLEMENTED 2026-04-19 in ree-v3: ree_core/cingulate/pacc_analog.py
    (PACCAnalog, PACCConfig). Slow-EMA accumulator of tanh-normalised
    z_harm_a magnitude, gated by SalienceCoordinator.write_gate("autonomic")
    (MECH-261 mode-conditioned), producing a bounded drive_bias
    (|bias| <= pacc_drive_bias_cap) added to SD-012 drive_level via
    pacc.effective_drive(base). Consumed by GoalState.update,
    SalienceCoordinator.tick, AICAnalog, PCCAnalog, dACC bundle -- all
    SD-032 consumers see the sensitised drive regime coherently. MECH-094
    hypothesis_tag skip prevents simulation/replay from driving the
    autonomic channel. Scoping synthesis (evidence/literature/
    targeted_review_pacc_autonomic_coupling_write_target/synthesis.md)
    documented three architectural decisions:
      (1) write target: SD-012 drive_level as first-pass proxy (V3 has
          no valence-signed setpoint or fast-autonomic analogue;
          documented simplification);
      (2) timescale: default pacc_drive_alpha=0.002 (pacc_drive_ema=0.998,
          half-life ~347 steps) sits at the fast end of biological
          plausibility per Guo 2018 ACC mGluR5 LTP (days-timescale);
      (3) offline decay: default pacc_offline_decay=0.0 -- sleep
          normalisation is a DISTINCT claim (not registered) with its
          own literature requirement; hook exists in note_offline_entry()
          for future claim wiring.
    Config: REEConfig.use_pacc_analog (default False). Backward
    compatible. Non-trainable arithmetic; phased training not applicable.
    Validation: V3-EXQ-448 ran 2026-04-19 (diagnostic, 10/10 pass criteria),
    outcome PASS, evidence_direction supports (SD-032e, MECH-094). Confirmed
    sustained z_harm_a drives monotone drive_bias accumulation to cap (c2/c5),
    reversible relaxation on quiescence (c6), and correct write-gate
    suppression when closed (c3/c4).
  epistemic_category: standard
  what_would_answer: >
    ALREADY CONFIRMED (V3-EXQ-448, 2026-04-19, diagnostic PASS, supports;
    reinforced by V3-EXQ-453 write-gate landing PASS, 2026-04-20).
    NON-DEGENERACY PRECONDITION: use_pacc_analog must be enabled (default
    False) and SalienceCoordinator.write_gate("autonomic") must actually be
    open (MECH-261 mode-conditioned) for z_harm_a to reach drive_level at
    all -- with the gate closed or the flag off, pacc.effective_drive(base)
    reduces to a no-op (confirmed by V3-EXQ-448 c3/c10) and no signature is
    testable, so absence of drift under those conditions would not falsify
    the claim.
    CONFIRMING: with the gate open, sustained z_harm_a exposure produces
    measurable, monotone drift in drive_level via pacc.effective_drive(base)
    (V3-EXQ-448 c2: bias 0.045 -> 0.5 across a sustained-load window, held at
    cap under 5000 high-load ticks per c5, and reversible toward 0 on
    quiescence per c6), and this drift subsequently modulates SD-032c's
    salience threshold, producing the chronic-pain-like sensitization
    pathway (Baliki 2012) -- already observed per the stable-promotion
    evidence base (5 supports/0 weakens/4 mixed across 5 exp + 4 lit
    entries, including V3-EXQ-448 and V3-EXQ-453).
    FALSIFYING: (i) with the write_gate open and use_pacc_analog enabled,
    sustained z_harm_a exposure produces no measurable drive_level drift
    (bias stays ~0 despite load) -- would indicate the write path is
    disconnected or the EMA accumulator is not actually receiving z_harm_a;
    or (ii) drift occurs and accumulates as expected, but never propagates
    to modulate SD-032c's salience threshold -- would sever the claimed
    write-back -> salience-gate link and invalidate the chronic-sensitization
    behavioural prediction even though the drive_level mechanism itself is
    intact.
  location: evidence/literature/targeted_review_cingulate_integration_substrate/synthesis.md
- id: MECH-258
  title: "z_harm_a enters action selection as a precision-weighted prediction error (harm_surprise_PE, SD-020) against an internal pain forward model (E2_harm_a), not as a raw magnitude. Upgrades SD-020 from provisional to prerequisite for SD-032b (dACC-analog)."
  claim_type: mechanism_hypothesis
  subject: cingulate.precision_weighted_pain_PE
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-445h_2026-06-19#V3-EXQ-445h"
      as_of: 2026-06-19
      verdict: supports/standard
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-04-19
  depends_on:
    - SD-020           # harm_surprise_PE (the substrate MECH-258 is the control claim over)
    - SD-032b          # dACC-analog (consumer of the PE)
    - ARC-033          # E2_harm_s forward model (parallel architecture; E2_harm_a is the affective analog)
    - SD-003           # counterfactual attribution (uses the same forward-model substrate)
  functional_restatement: >
    Mechanism claim underlying SD-032b: the dACC-analog reads z_harm_a
    as a precision-weighted prediction error against E2_harm_a, not as
    a raw magnitude. This gives the context-dependence the biology
    documents: the same nociceptive input produces large behavioural
    adjustment when precision is high (uncontrollable, unexpected pain)
    and small adjustment when precision is low (expected, controllable
    pain).

    Computational form:
      PE = z_harm_a_observed - E2_harm_a(z_harm_a_prev, a_taken)
      precision = f(variance of recent PE, controllability estimate)
      dACC_input = precision * PE

    E2_harm_a shares substrate with E2_harm_s (ARC-033) -- same forward
    model architecture, different input stream. Both are members of the
    per-stream forward-model family (MECH-256/257).

    Falsifiable: if a non-precision-weighted (raw-magnitude) consumer
    of z_harm_a reproduces the chronic-pain-like behavioural patterns
    Baliki 2010/2012 documents (ACC-NAc coupling shift under sustained
    harm), then precision weighting is unnecessary overhead.
  notes: >
    Biological grounding: Seymour 2019 (Neuron) pain-as-precision-signal
    framework. Upgrades SD-020 status: from provisional (limited evidence)
    to prerequisite-for-cingulate-substrate (required for SD-032b to work).
  evidence_quality_note: >
    Registered pre-implementation. E2_harm_a is a new module; V3 test
    should verify that shared-substrate E2_harm_s + E2_harm_a forward
    models achieve comparable per-stream accuracy to independent
    models (following MECH-257 single-substrate-with-gated-readout
    pattern).

    [2026-05-08 governance]: EXQ-445h supports (C1 wins=2/3 seeds). harm_a forward R2=0.94-0.99. First clean supporting evidence for shared-substrate harm prediction on dACC reef.
  implementation_note: >
    IMPLEMENTED 2026-04-19. E2_harm_a forward model added in
    ree_core/predictors/e2_harm_a.py (E2HarmAConfig, E2HarmAForward).
    Structurally mirrors E2HarmSForward (ARC-033). Two constructor paths:
    (a) shared_trunk=None (default) -- independent ResidualHarmForward
    (ARC-033-parallel). (b) shared_trunk=HarmForwardTrunk instance --
    reuses trunk, owns only HarmForwardHead (ARC-058 shared-substrate
    hypothesis). Precision weighting computed in DACCAdaptiveControl
    (cingulate/dacc.py): pe = ||z_harm_a - E2_harm_a(z_harm_a_prev,
    a_prev)||; pe_weighted = pe * (1 + min(precision/500, 3));
    bundle["pe"] drives dACC adaptive-control magnitude.
    Config: REEConfig.use_e2_harm_a (bool, default False),
    e2_harm_a_lr (default 5e-4), use_shared_harm_trunk (bool, default
    False; selects ARC-033 vs ARC-058 path).
    Phased training: P0 AffectiveHarmEncoder warmup (SD-011 second source
    + SD-020 surprise target); P1 E2_harm_a trains on FROZEN z_harm_a
    (caller .detach()s targets); P2 eval harm_a_forward_r2.
    Consumed by DACC bundle (MECH-258 precision-weighted PE channel).
    Validation experiment: V3-EXQ-445 queued (compares ARC-033 vs ARC-058
    paths, evaluates MECH-257 single-substrate hypothesis).
  location: evidence/literature/targeted_review_cingulate_integration_substrate/synthesis.md
- id: MECH-259
  title: "Salience-network switch threshold: when the precision-weighted salience of any input to the salience-network coordinator (SD-032a) exceeds a threshold, the coordinator fires a whole-system mode switch rather than producing an incremental policy update."
  claim_type: mechanism_hypothesis
  subject: cingulate.switch_threshold
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_447_sd032d_pcc_stability_20260423T204632Z_v3
      as_of: 2026-04-23
      verdict: supports/PASS
  implementation_phase: v3
  v3_pending: false  # cleared 2026-04-22: V3-EXQ-446 + V3-EXQ-455 substrate PASSes confirm switch-threshold mechanism is firing. Promoted candidate -> provisional 2026-04-24 (governance): 3 supporting entries, 0 opposing, 1 genuine exp, conf 0.753. Promoted provisional -> stable 2026-04-25 (governance): conf 0.882, conflict_ratio 0, 10 supports / 0 weakens across 8 exp + 2 lit entries.
  claim_level: mechanistic
  registered_utc: 2026-04-19
  depends_on:
    - SD-032a          # coordinator
    - SD-032c          # AIC-analog (primary trigger source)
    - MECH-091         # phase-reset mechanism (pre-existing analog)
    - MECH-258         # precision-weighted PE (the magnitude compared against threshold)
  functional_restatement: >
    Mechanism that converts graded salience signals into a discrete mode-
    switch operation. Below threshold, salience-weighted inputs modulate
    ongoing behaviour smoothly (via SD-032b adaptive control). Above
    threshold, the coordinator interrupts the current mode and switches
    to a more appropriate one (external_task -> internal_planning on
    severe harm signal; internal_planning -> external_task on urgent
    external cue; etc.).

    The threshold itself should be (i) modulated by PCC-analog's
    mode-stability parameter (SD-032d), (ii) learnable from experience
    (agents learn when interrupts are warranted), and (iii) gated on
    current operating mode (higher threshold during committed action,
    lower during planning / replay).

    Biological analogue: salience-network switching in Menon & Uddin
    2010 is described as discrete network reconfiguration on detection,
    not graded modulation.

    Falsifiable: if ree-v3 implementation produces coherent behaviour
    with a purely graded / continuous coordinator (no discrete mode
    switches), MECH-259 is over-specification and the coordinator
    should be redesigned as a graded-modulation layer.
  notes: >
    This mechanism is what makes "urgency interrupt" (original Option C)
    different from "large adaptive-control adjustment" (large magnitude
    Option A): the former is a mode change; the latter is a smooth
    update within mode.
  evidence_quality_note: >
    Registered pre-implementation. See SD-032 parent.
  implementation_note: >
    IMPLEMENTED 2026-04-19 within SalienceCoordinator
    (ree_core/cingulate/salience_coordinator.py). effective_threshold =
    switch_threshold * (1 + stability_scaling * pcc_stability). Trigger
    fires when salience_aggregate (weighted sum of dacc_pe + future
    aic_salience) exceeds effective_threshold AND argmax(operating_mode)
    differs from the discretely-held current_mode. Discrete mode update
    happens only on trigger (Schmitt-trigger hysteresis), not every tick;
    prevents flicker when soft vector hovers near a boundary. Threshold is
    a Python float (not learnable in V3 minimum-viable; spec allows
    learnable refinement in a follow-up). Validation experiment: V3-EXQ-446
    measures trigger fire rate under synthetic high-PE injection vs
    baseline.
  location: evidence/literature/targeted_review_cingulate_integration_substrate/synthesis.md
- id: MECH-260
  # Assembly-state companion fields (auto-join audit, 2026-06-22; repointed same
  # day). The conversion ceiling is being closed via the biologically-faithful
  # basal-ganglia eligibility constitution (ARC-107: MECH-448 rank-preserving
  # F->eligibility demotion + MECH-449 Go/No-Go opponency leg), NOT the F-dominance
  # rebalance/selection-readout route (exhausted). awaiting: points at ARC-107 (the
  # active substrate, matching MECH-449), not the legacy f_dominance_conversion_
  # ceiling queue row whose name connotes the abandoned route and whose status blob
  # mis-normalizes to 'built'. assembly_status in_progress: the BG constitution is
  # under construction (V3-EXQ-689g pending).
  awaiting: ARC-107
  assembly_status: in_progress
  title: "dACC-analog bias suppression against recency / monostrategy: the dACC module writes a counter-bias that resists repeating the last-rewarded trajectory when long-term value information demands exploration. Candidate explanation of the monostrategy failure observed in fishtank_viz and earlier queue-diversity results."
  claim_type: mechanism_hypothesis
  subject: cingulate.dacc_bias_suppression
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-460g_2026-06-19
      as_of: 2026-06-19
      verdict: non_contributory/non_contributory
  implementation_phase: v3
  v3_pending: true
  pending_retest_after_redesign: true
  pending_retest_after_substrate: true  # commitment-closure-control-plane beta-engagement (460e autopsy 2026-06-17)
  claim_level: mechanistic
  registered_utc: 2026-04-19
  depends_on:
    - SD-032b          # dACC-analog (the module that hosts this function)
  functional_restatement: >
    Mechanism hypothesis. Scholl, Kolling et al 2015 (J Neurosci) show
    dACC + lateral aPFC actively suppress an amygdala/vmPFC bias toward
    recently-rewarded choices; participants relying more on dACC show
    more rational long-term learning, while those relying more on
    vmPFC/amygdala show monostrategy-like bias to recent outcomes.

    Translated to ree-v3: dACC-analog (SD-032b) outputs a counter-bias
    on trajectory selection that weights against recently-executed
    trajectories when integrated long-term value favours exploration.
    This is distinct from SD-032b's primary adaptive-control function
    (which computes adjustment magnitude) -- it is an additional output
    channel that shapes the *diversity* of trajectory selection.

    Candidate explanation for the monostrategy failure: fishtank_viz
    showed the agent repeating the same route despite available
    alternatives; SD-032b-with-MECH-260 absent is the minimal
    architecture consistent with that observation.

    Falsifiable: if implementing SD-032b without MECH-260 suppression
    resolves the monostrategy failure, MECH-260 is unnecessary. If
    SD-032b + MECH-260 still produces monostrategy, the bias-
    suppression framing is wrong and the fix lies elsewhere (probably
    in exploration noise / entropy regularisation, not in dACC output).
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: requires dACC-analog (SD-032b) bias_fraction
    at E3 to be measurably non-zero and varying across ON/OFF arms --
    V3-EXQ-571 already measured bias_fraction=0.000 for MECH-260's channel
    specifically, which is why the claim was GOV-CEIL-1 demoted to contested
    (2026-07-09, 8 confirmed substrate_ceiling autopsies:
    460b/460c/460f/445h/603c/603d/603e/687). A run reproducing
    bias_fraction=0.000 is NOT a fresh falsification -- it corroborates the
    already-recorded finding.
    CONFIRMING signature (this claim's own stated FALSIFIER): SD-032b +
    MECH-260 (bias-suppression channel engaged, non-zero bias_fraction)
    measurably resolves or reduces the monostrategy failure relative to
    SD-032b alone -- the bias-suppression channel is doing causal work.
    FALSIFYING signature (this claim's own stated FALSIFIER): implementing
    SD-032b WITHOUT MECH-260 suppression already resolves the monostrategy
    failure (MECH-260 unnecessary); OR SD-032b + MECH-260 still produces
    monostrategy with the channel genuinely engaged (non-zero
    bias_fraction) -- the bias-suppression framing is wrong and the fix
    lies in exploration noise / entropy regularisation instead.
    CURRENT STATUS: CONTESTED candidate since the 2026-07-09 GOV-CEIL-1
    demotion -- the competing NULL reading (dACC anti-recency is inert /
    does no work at E3) is carried CO-EQUALLY, not suppressed. Promotion in
    either direction is re-gated on a future positive discrimination on a
    richer substrate (same ARC-107/MECH-448/449 basal-ganglia-constitution
    track as sibling claim MECH-439).
  notes: >
    Registered on the strength of a single paper (Scholl/Kolling 2015);
    warrants a follow-up lit-pull on MCC effort-value + bias suppression
    (Rushworth lab beyond Scholl/Kolling) before committing to the
    specific computational form.
  evidence_quality_note: >
    GOV-CEIL-1 CEILING-EXHAUSTION DEMOTION 2026-07-09: 8 confirmed substrate_ceiling failure_autopsy
    hits (460b/460c/460f/445h/603c/603d/603e/687) with no positive discrimination on any richer
    substrate -> demoted to contested candidate (epistemic_category standard, ceiling_decision exhausted).
    The competing NULL reading -- dACC anti-recency is inert / does no work at E3 (bias_fraction 0.000,
    V3-EXQ-571) -- is now carried CO-EQUALLY. NOT falsification: the claim keeps its full history;
    promotion re-gated on a future positive discrimination on a richer substrate.

    2026-06-19 (governance-apply, failure_autopsy_V3-EXQ-460g CONFIRMED): V3-EXQ-460g (supersedes
    460f) self-routed substrate_not_ready_requeue (the de-commit magnitude lever suppresses its own
    coupling certifier -- see SD-034). MECH-260 -> supports (No-Go nogo_installed >= 1 on 3/3, a
    narrow non-promoting positive; MECH-260 stays substrate_ceiling/candidate so promotion is
    suppressed). pending_retest_after_substrate UNCHANGED.
    Single-paper evidence. Lower confidence than the other MECH claims
    in this cluster. See SD-032 parent.

    2026-07-30 (governance-apply, failure_autopsy_batch-687a-707c-840-748a-833-842-810b-673-614-798afail
    CONFIRMED): V3-EXQ-687a (committed-authority dissociation, 4-arm) -- all arms failed
    committed_sufficiency at the absolute floor (0.0/50.0), not a discrimination result;
    re-derive brake exempt. non_contributory/competence_implementation_gap (epistemic_category
    stays standard). MECH-260 is now gated on the same MECH-457 competence-floor substrate as
    the rest of the committed-behaviour campaign (Q-045/MECH-313 co-tagged) -- not
    independently re-queueable until that substrate clears. pending_retest_after_substrate
    UNCHANGED (true).

    [2026-06-17 autopsy V3-EXQ-460e]: 460e (SD-034 closure-control-plane behavioural,
    supersedes 460d) RAN terminal FAIL/non_contributory and self-routed
    substrate_not_ready_requeue on beta_engagement_both_arms (1/3 seeds) BEFORE the C2
    de-commit DV ran -- MECH-260's closure-coupled No-Go role was never exercised
    (confirmed failure_autopsy_V3-EXQ-460e_2026-06-17; commit-without-beta dissociation,
    code-confirmed). 460e MECH-260 entry = non_contributory (not a weaken). pending_retest_after_substrate
    set (commitment-closure-control-plane beta-engagement amend). EXQ-445h remains the sole
    valid MECH-260 support; this is non-scoring. NO status change (stays candidate).

    [2026-05-29 autopsy V3-EXQ-603a/b/c followon cluster]: Three-followon convergent confirmation of the 591 substrate-uniform z_goal-zero family. 603a/b/c all 1/3 surviving seeds; MECH-260 operative every cell (dacc_forward_calls up to 4791 in surviving cells) but effective N=1 across the chain. Confirms 490g-cohort autopsy (commit 12f0dda773) Cluster B disposition under the Q-045/MECH-313/MECH-260 lettered-followon lens (no fork). Adds 603a + 603b failure_records to SD-XXX-scaffolded-sd054-onboarding substrate entry. EXQ-445h remains the sole valid MECH-260 support; sub-margin 'weakens' or 'supports' appearances at N=1 across the 603 chain are not load-bearing.

    [2026-05-08 governance]: EXQ-445h supports (C3 wins=3/3 seeds). harm forward R2=0.94-0.99. First clean supporting evidence on dACC reef substrate.

    [2026-05-24 autopsy V3-EXQ-603]: MEASUREMENT GAP -- act_with_split_obs() bypasses
    select_action() where dacc.record_action() is called; FIFO permanently empty,
    suppression zero throughout. ARM_2==ARM_0 to 6 d.p. across all seeds+step counts
    (2 independent runs confirm structural). 'weakens' direction in manifest is a
    script logic artifact (wrong-arm comparison vs ARM_1 not baseline). Both V3-EXQ-603
    manifests overridden to non_contributory. EXQ-445h (C3 3/3 seeds, select_action
    path) remains valid evidence. [2026-05-25] V3-EXQ-603a: call-path fixed
    (mech260_operative_all_seeds=true) but 2/3 seeds measured_steps=0 (episodes <75
    FIFO warmup steps); 603a also overridden to non_contributory pending temporal-gate
    fix. Pending retest: V3-EXQ-603b.

    [2026-05-26 governance applies V3-EXQ-603 autopsy to remaining 2026-05-21 run
    (111759Z, pre-Fix1/Fix2) AND V3-EXQ-603b post-Fix-B autopsy (2026-05-25)]:
    Third measurement_gap in the 603 chain. Substrate confirmed operative
    (mech260_operative_all_seeds=true; dacc_forward_calls up to 1362; FIFO + suppression
    both fire) but Fix B (hazard_harm 0.05 -> 0.02) was insufficient because
    proximity_harm_scale=0.1 (untouched) was the dominant kill channel via
    `agent_health -= abs(harm_signal)`. Seeds 42/44 still terminated at 350-475
    total_steps despite STEPS_PER_EPISODE=500; only seed 43 survived. Effective N=1.
    Seed-43-only deltas: ARM_2 +0.041 vs ARM_0, ARM_3 +0.045 -- directionally correct
    but sub-threshold (entropy_margin=0.05). 603 (111759Z) + 603b (071458Z) both
    overridden to non_contributory + epistemic_category=measurement_gap. EXQ-445h
    remains the valid MECH-260 support. Pending retest: V3-EXQ-603c with P0/P1
    training phase added (autopsy spec; user-confirmed gate 2026-05-25T09Z).
    Chain halt condition: if 603c also produces <3/3-surviving-seed manifest at
    P1 gate, halt 603 chain and route Q-045 to substrate_conditional.

    [2026-05-27 governance applies V3-EXQ-603c (P0/P1 phased training,
    supersedes 603b)]: NON-CONTRIBUTORY. Halt condition fired:
    p2_cell_count=4 / aborted_cells=8 (8 of 12 cells aborted the Fix D
    pre-P2 survival gate at end of P1). Even with P0 (E1+E2 warmup on
    easy env) + P1 (consolidation on target env), the trained policy
    does not survive SD-054 enrichment long enough in 2/3 cell arms to
    sustain measurement. Q-045 routed to epistemic_category=
    substrate_ceiling (see Q-045 entry; the claim's test bed (SD-054 +
    ARC-062) is V3-scoped by design; the missing piece is V3 substrate
    enrichment / training-regime work, not a V4 prerequisite). MECH-260
    inherits substrate_ceiling via Q-045 dependency. EXQ-445h remains
    the valid contributory MECH-260 support. Cluster-absorbed into
    failure_autopsy_V3-EXQ-591_2026-05-27 substrate-uniform z_goal-zero
    family. [2026-05-27 governance correction: prior cycle's plan-of-
    record line had said route to substrate_conditional pending V4;
    that was scope-deferral and is overridden -- the 591 autopsy's
    favoured reading is substrate_enrichment-within-V3, not V4
    deferral.]

    [2026-06-01T05:50Z governance cycle, V3-EXQ-621a substrate-readiness PASS]:
    Scaffolded SD-054 onboarding substrate (landed 2026-05-31 IGW-20260531-029)
    cleared substrate-readiness diagnostic on V3-EXQ-621a (PASS via C1 6/12
    cells + C3 cascade_consequential=true via ARM_2 approach_commit lift;
    z_goal C2 floor still unmet, motivating staged V3-EXQ-622 decomposition).
    Reclassified non_contributory per substrate-readiness diagnostic pattern
    (matches MECH-295 / SD-049 precedents): substrate-readiness PASS does not
    by itself constitute load-bearing behavioural evidence. Behavioural cluster
    validation V3-EXQ-603d / 591b queueable. pending_retest_after_substrate
    flag retained until behavioural successor returns. Predecessor V3-EXQ-621
    (recovered manifest from runner emit_outcome misclassification) marked
    superseded by V3-EXQ-621a per CLAUDE.md supersession rule.

    Correction (failure_autopsy_V3-EXQ-621a_2026-06-06): V3-EXQ-621a
    overall_pass=true was a VACUOUS_PASS -- the load-bearing C2 z_goal floor
    was FALSE (z_goal=0 across all 12 cells) and z_goal=0 was guaranteed by
    construction (the 2026-05-31 scheduler never called update_z_goal AND the
    agent was built without z_goal_enabled). The PASS rested on a
    baseline-inflated C1 (real scaffolded survival 3/9) and a vacuous C3
    (approach-commit lift attributed to a goal cascade that cannot exist at
    z_goal=0). 621a did NOT actually clear substrate-readiness or GAP-C prereq
    (2); that gate remains the pending full-scale run (ready=false). The
    V3-EXQ-621 supersession stands (runner-emit_outcome-misclassification
    recovery; independent of the vacuity). The fault was confirmed and
    re-opened by failure_autopsy_V3-EXQ-603d_2026-06-01 (Class-1
    harness/wiring); no claim confidence moved (run non_contributory /
    scoring_excluded).

    [2026-06-01T17:56Z governance cycle, confirmed failure_autopsy_V3-EXQ-603d]:
    First behavioural retest on the IMPLEMENTED scaffolded_sd054_onboarding
    substrate. FAIL non_contributory. LOAD-BEARING finding:
    ScaffoldedSD054OnboardingScheduler._train_episode and 603d's bespoke P2
    never call agent.update_z_goal, so z_goal stays zero-init for every step of
    every arm; the C4 z_goal=0 / SUBSTRATE_FAILURE label is a Class-1
    harness/wiring artifact (same fault class as V3-EXQ-626), NOT
    substrate-ceiling falsification. This is the true content of 621a's "z_goal
    feeding not yet wired" + the 591-triage prereq (4). P1 survival gate failed
    2/3 seeds (42,44; only 43 to P2; p2_cell_count 5/15, effective N=1),
    reproducing the 603-chain seed-fragility but confounded by reduced budget
    (P0/P1=30/30 vs 603c 100/50) + the inert goal pipeline. Routing:
    implement-substrate AMEND on scaffolded_sd054_onboarding (wire update_z_goal
    into the scheduler + Stage-0 positive-control gate; strictly UPSTREAM of the
    622 anneal-rate amend), then re-issue 603d -> 603e at restored budget.
    substrate_queue amended (action=amend, priority 1, 603d failure_record
    appended). pending_retest_after_substrate retained. EXQ-445h remains the
    sole valid MECH-260 support; no claim falsified.

    [2026-06-02 implement-substrate AMEND landed]: scaffolded_sd054_onboarding
    update_z_goal wiring fixed (P1 _train_episode + P2 _eval_episode) + Stage-0
    positive control added; 625b failure record folded in. Validation pending
    V3-EXQ-603e (restored budget + z_goal_enabled=True required -- 603d config
    omitted it). Status unchanged (substrate_ceiling / pending_retest_after_substrate);
    substrate_queue amend_pending -> amend_implemented_pending_validation.
    EXQ-445h remains the sole valid MECH-260 support. See Q-045.

    [2026-06-03 governance / cluster autopsy failure_autopsy_V3-EXQ-603e-626a-622_2026-06-03] V3-EXQ-603e re-tagged non_contributory: goal-pipeline wiring closed+verified (update_z_goal AMEND deb24cc; contract test_c6_stage0_positive_control confirms z_goal forms under forced input) but z_goal_norm_peak=0.0 on ALL 15 cells ecologically at restored budget -- downstream of TWO coupled prerequisites: survival/foraging-competence (2/3 seeds never competent) + benefit-input starvation (P2 hazard_food_attraction=0.7). Discovered developmental prerequisite (goal reps require reward-contact history; Berridge), NOT a falsification: claim never under fair test (effective N=1). stays substrate_ceiling/pending_retest_after_substrate; substrate_queue AMEND 'scaffolded_sd054_onboarding' then re-issue 603e->603f. EXQ-445h remains sole valid MECH-260 support.

    [2026-06-19 confirmed failure_autopsy_V3-EXQ-445h]: c3 (entropy_ON>=entropy_OFF) PASS is vacuous (0.0>=0.0 under monostrategy) -- excluded as degenerate across the 445-series. MECH-260 status unaffected (exp_conf 0.799 confirmed_established, driven by V3-EXQ-463). Already carries substrate_ceiling + pending_retest_after_substrate.

    [enum-comment migrated to field 2026-07-18] epistemic_category: GOV-CEIL-1 demotion
    2026-07-09 (was substrate_ceiling)

  epistemic_category: standard
  ceiling_decision: exhausted
  ceiling_routing_note: "GOV-CEIL-1 ceiling-exhaustion demotion 2026-07-09: 8 confirmed substrate_ceiling failure_autopsy hits (460b/460c/460f/445h/603c/603d/603e/687) with no positive discrimination on any richer substrate. Demoted to contested candidate; the inert/no-op-mechanism null reading (dACC anti-recency does no work at E3 -- bias_fraction 0.000 per V3-EXQ-571) is carried co-equally. Promotion re-gated on a future positive discrimination on a richer substrate."
  implementation_note: >
    IMPLEMENTED 2026-04-19. DACCAdaptiveControl
    (ree_core/cingulate/dacc.py) maintains a FIFO action-class history
    (length dacc_suppression_memory, default 8) via
    record_action(action_class). forward() computes suppression[i] =
    count(action_class_i in history) / len(history). DACCtoE3Adapter
    applies dacc_suppression_weight * suppression to the E3 score_bias
    (positive bias = unfavourable under E3's lower-is-better convention,
    discouraging repeated action classes).
    Config: REEConfig.dacc_suppression_weight (float, default 0.0),
    dacc_suppression_memory (int, default 8). Enabled per-experiment;
    baseline (weight=0) produces no suppression.
    Agent wiring: REEAgent.select_action() calls
    self.dacc.record_action(argmax(action[0])) after action selection.
    Fishtank-viz monostrategy test is the target behavioural signature:
    ablation (weight=0) should show repeated-route behaviour; enabled
    (weight>0) should diversify action-class selection.
    Validation experiment: V3-EXQ-445 includes suppression=0 vs
    suppression=0.5 comparison within the three-arm design.
  location: evidence/literature/targeted_review_cingulate_integration_substrate/synthesis.md
- id: MECH-261
  title: "Mode-conditioned write gating: the operating-mode variable from the salience-network coordinator (SD-032a) determines which substrates can write to E3, episodic memory, policy updates, and autonomic coupling. Generalises MECH-094 hypothesis tag to a mode-conditioned family of write gates. Operating mode is implemented as a soft probability vector over {external_task, internal_planning, internal_replay, offline_consolidation}, not a one-hot categorical."
  claim_type: mechanism_hypothesis
  subject: cingulate.mode_conditioned_write_gating
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-460g_2026-06-19
      as_of: 2026-06-19
      verdict: non_contributory/non_contributory
  implementation_phase: v3
  v3_pending: false  # cleared 2026-04-22: V3-EXQ-446 + V3-EXQ-455 + V3-EXQ-453 06:21 write-gate landing PASS validates mode-conditioned write gating. Promoted candidate -> provisional 2026-04-24 (governance): 21 supporting entries, 0 opposing (pre-retag), 1 genuine exp, conf 0.862. NOTE: V3-EXQ-453a 2026-04-24 FAIL per-claim weakens (mode-conditioned write-gate landing regressed on seed-level reproducibility of release gating); factored into held-at-provisional decision. Promoted provisional -> stable 2026-04-25 (governance): conf 0.891, conflict_ratio 0.167, 33 supports / 3 weakens / 0 mixed across 16 exp + 20 lit entries; engine recommendation overrides the prior hold despite residual seed-reproducibility weakens, given dominant evidence weight.
  claim_level: mechanistic
  registered_utc: 2026-04-19
  last_updated_utc: 2026-04-19
  pending_retest_after_substrate: true  # documentary: commitment-closure-control-plane beta-engagement (460e autopsy 2026-06-17); does NOT demote stable status
  governance_note_2026_06_13b: >
    PM cycle (gov 2026-06-13T20:04Z): V3-EXQ-460d's MECH-261 entry reclassified
    weakens->non_contributory per confirmed failure_autopsy_SD-034-closure-control-plane-d
    (the de-commit RELEASE leg is unbuilt; not a falsification). Does NOT change MECH-261's
    stable status (33 supports / strong evidence base); recorded so the 460d entry is not
    miscounted as opposing.
  governance_note_2026_06_17: >
    V3-EXQ-460e (supersedes 460d) RAN terminal FAIL/non_contributory; per confirmed
    failure_autopsy_V3-EXQ-460e_2026-06-17 its MECH-261 entry is non_contributory -- the
    run self-routed substrate_not_ready_requeue on beta_engagement_both_arms (1/3 seeds)
    BEFORE the C2 de-commit DV ran, so the mode-conditioned write-gate role MECH-261 plays
    in closure was never exercised (commit-without-beta dissociation, code-confirmed). Like
    the 460d entry this does NOT change MECH-261's stable status (33 supports / strong
    evidence base); recorded so the 460e entry is not miscounted as opposing.
    pending_retest_after_substrate set as a documentary marker (commitment-closure-control-plane
    beta-engagement amend) -- it pairs with the non_contributory reclassification and does not
    demote a stable claim.
  mem1_amendment_2026_06_14: >
    MEM-1 (memory_lifecycle_v4) amendment: MECH-261's mode-conditioned
    write-gating family is extended with an ALLOCATION-DECISION STAGE -- a
    control-plane step that, before a write fires, selects integrate /
    partial_overlap / separate over the hippocampal graph from
    (context_similarity x temporal_distance INTERACTION, schema_fit, salience,
    PE, uncertainty, goal_relevance). Per Verdict G1 (de Sousa 2026 intake) the
    decision algorithm is the single NOVEL aspect and is carried as a DISTINCT
    mechanism_hypothesis MECH-391 (substrate_conditional, v4) wired to MECH-261
    via depends_on, NOT a new invariant (subsumed by MECH-094/261 + INV-039).
    The false-linking-risk / reality-coherence cost the stage prices is INV-079.
    V4 / substrate_conditional: the gates MECH-261 steers are V3-live, but the
    allocation policy needs the V4 memory-lifecycle store -- DO NOT build in V3.
  depends_on:
    - SD-032a          # coordinator (source of operating_mode)
    - MECH-094         # hypothesis tag (the specific tag this generalises)
    - INV-049          # sleep necessity (coordinates with consolidation mode)
    - MECH-092         # micro-quiescence replay
    - ARC-038          # viability map (one write target)
    - SD-033a          # lateral-PFC-analog rule/goal substrate (primary write target)
    - SD-033b          # OFC-analog specific-outcome / state-space substrate (secondary write target)
    - SD-033c          # vmPFC-analog value substrate (gated under mode-specific rules)
  functional_restatement: >
    MECH-094 (hypothesis tag) specified a single categorical write-gate
    controlling whether simulated / replayed content can write to
    memory. MECH-261 generalises: the operating mode (from SD-032a) is
    a SOFT probability vector over {external_task, internal_planning,
    internal_replay, offline_consolidation}, and each substrate has
    gate logic keyed to this vector.

    Refinement from lit-pull (2026-04-19,
    targeted_review_systems_consolidation_waking_propagation):

    (1) SOFT-BOUNDARY MODES. Operating mode is a weighted mixture, not
    a one-hot switch. Carr, Jadhav & Frank (2011) show awake SWR
    subpopulations segregate by behavioural context rather than by a
    discrete flag; Tambini & Davachi (2019) show prior-state
    persistence into subsequent neutral periods produces mixed activity
    patterns. Per-substrate gates read a weighted sum over the mode
    vector; hard categorical gating would misrepresent the biology.

    (2) WRITE TARGETS. The internal_replay mode must write-enable at
    least three distinct downstream substrates, not only the viability
    map. Based on biological evidence:
      - HC-analog viability map (ARC-038): write-enabled in
        external_task and internal_replay (Carr/Jadhav/Frank 2011
        awake SWR consolidation subpopulation).
      - Cortical-sensory buffer: write-enabled in internal_replay and
        offline_consolidation (Rothschild/Eban/Frank 2017
        cortical-hippocampal-cortical loop during SWRs).
      - Lateral-PFC-analog (SD-033a) rule/goal substrate:
        write-enabled in internal_replay and offline_consolidation
        (Peyrache et al 2009 SWR-coupled rule-learning replay in mPFC).
        This is the primary consolidation target for replayed
        rule content.
      - OFC-analog (SD-033b) specific-outcome / state-space substrate:
        consolidative writes during offline_consolidation (slow
        updating of the task-structure cognitive map from accumulated
        real-outcome experience); speculative writes during
        internal_planning (counterfactual outcome queries).
      - Autonomic coupling (SD-032e): active in external_task,
        attenuated in internal_planning / replay.

    (3) MODE-DRIVEN GATE EXAMPLES (illustrative, not exhaustive):
      - E3 policy update: high weight on external_task; near-zero in
        internal_replay (replay must not overwrite policy directly,
        it propagates via the lateral-PFC-analog substrate SD-033a).
      - Episodic memory commit: high weight on external_task and
        offline_consolidation; hypothesis-tagged (MECH-094) in
        internal_planning.
      - SD-021 descending pain modulation (folded into SD-032c):
        attenuates z_harm_s gain in high-external_task-weight states,
        unattenuated otherwise.

    (4) CROSS-MODE FORWARD PROPAGATION. Content active in
    internal_replay should produce a measurable bias on subsequent
    external_task action selection (Tambini/Davachi 2019 forward-
    cognitive-bias finding). This is both a design requirement and
    the primary behavioural falsification target.

    Falsifiable (primary): if a ree-v3 implementation using purely
    local per-substrate gating (no shared operating-mode vector from
    SD-032a) reproduces the post-quiescence action-selection bias
    toward recently replayed trajectories, MECH-261 is over-
    specification. See EXP-0148.

    Falsifiable (secondary): if ablating the SD-032a coordinator
    (forcing operating_mode to a fixed external_task vector) does NOT
    abolish the forward-propagation bias, the coordinator overlay is
    wrong -- forward propagation would be coordinated locally rather
    than by the salience network.
  notes: >
    This is the mechanism that ties operating mode (SD-032a output) to
    the broader substrate graph. It makes explicit the intuition that
    MECH-094's hypothesis tag is a special case of a more general
    mode-variable write-gate family.

    2026-04-19 update: claim text refined after
    targeted_review_systems_consolidation_waking_propagation lit pull
    (5 entries grounding the waking-consolidation-to-action-selection
    bridge). Key biology-driven refinements: (a) soft-boundary mode
    vector rather than one-hot categorical, (b) explicit cortical
    write targets including PFC-analog policy-level substrate, (c)
    forward-propagation bias as primary behavioural signature.
    Salience-network-as-coordinator framing remains a REE-specific
    overlay not directly validated in rodent replay literature; see
    EXP-0148 as its causal test.

    2026-04-19 second update: depends_on extended with SD-033a/b/c
    and write-target text updated to name the lateral-PFC-analog
    (SD-033a) substrate and OFC-analog (SD-033b) substrate explicitly.
    The earlier "PFC-analog policy / rule-level substrate" phrasing
    was a hanging architectural reference -- the SD-033 cluster
    registration (same session) gives it a substrate address. Also:
    MECH-261's operating_mode_vector data structure should be
    implemented as a dictionary keyed on mode names so that a future
    parallel_goal_deliberation mode (V4, when SD-033e is implemented)
    can be added without disruptive schema changes. (Name updated
    2026-04-19 from deliberative_branching after the SD-033e Prong D
    frontopolar lit-pull; zero schema cost since the data structure
    is dict-keyed on mode names, not a fixed tuple.)

    2026-04-19 third update: MECH-264 (frontopolar counterfactual-
    value tracking) and MECH-265 (frontopolar relative-importance
    monitoring) operate WITHIN SD-033e and are gated by MECH-261's
    operating-mode vector, but are functionally distinct from MECH-261
    -- MECH-261 gates writes ACROSS substrates, while MECH-264/265
    are within-substrate computations inside SD-033e.

    2026-06-12 cross-ref (histaminergic-retrieval intake): MECH-261 is
    the WRITE-side gate family. Its READ-side sibling -- retrieval-time
    accessibility gating (which stored traces become reinstatable now)
    -- is registered as MECH-425, with the gain x suppression
    interaction as Q-076. The gates MECH-261 owns are write-enables;
    the retrieval-time gating POLICY (Line A arousal-coupled
    reinstatement gain + Line B competitive suppression) is the
    separately-registered uncovered piece, V4-leaning. No status change
    to MECH-261.

    2026-08-10 edge correction (phase-consistency-reconciliation chip
    chip-20260810-phase-consistency-reconciliation): depends_on: ARC-039 was
    removed. It was recorded as a *symmetric* association ("offline consolidation
    target"), but depends_on is a directed build prerequisite, and MECH-261 reached
    status: stable in V3 (V3-EXQ-446/455/453) while ARC-039's entorhinal circuit was
    never built -- so ARC-039 was never a prerequisite of MECH-261. ARC-039 is a
    future *consumer* of MECH-261's offline_consolidation mode weight; that true
    direction is recorded on ARC-039's own depends_on: MECH-261. The reversed edge
    had silently reclassified ARC-039 from its authored v4 scope to v3 via
    check_claim_phase_consistency.py. See evidence/planning/
    arc039_phase_reclassification_staged_20260808.md for the full writeup.
  evidence_quality_note: >
    2026-06-19 (governance-apply, failure_autopsy_V3-EXQ-460g CONFIRMED): V3-EXQ-460g (supersedes
    460f) -> MECH-261 non_contributory, NOT weakened: all closures were hook-driven
    (n_automatic_fires=0), so the Leg-A env-completion hook bypassed the MECH-261 mode-conditioning
    predicate and the run does not exercise it (protect the stable claim). pending_retest UNCHANGED.
    Registered pre-implementation. 5 literature entries support the
    underlying biology of mode-conditioned write gating
    (targeted_review_systems_consolidation_waking_propagation,
    aggregate literature_confidence 0.883). The salience-network
    coordinator overlay (operating_mode driven by SD-032a output) is
    REE-specific and has no direct rodent validation -- it requires
    the EXP-0148 ablation test after V3 implementation.
  implementation_note: >
    PARTIALLY IMPLEMENTED 2026-04-19 within SalienceCoordinator
    (ree_core/cingulate/salience_coordinator.py). The MECH-261 write-gate
    registry is a dict-of-dicts: outer key = target substrate name
    (sd_033a, sd_033b, sd_033c, sd_033d, hc_viability, sensory_buffer,
    autonomic, e3_policy), inner key = mode name. write_gate(target_name)
    returns the soft-weighted sum sum_{mode} operating_mode[mode] *
    weights[target][mode], clamped to [0, 1]. Soft-weighted (Carr/Jadhav/
    Frank 2011 mixed-subpopulation biology), not one-hot. Default weights
    populated from the spec table in
    sd_032_cingulate_integration_substrate.md. mode_names is a list and
    register_target(name, weights) accepts new mode keys, so V4 SD-033e
    parallel_goal_deliberation can be added without coordinator schema
    changes.
    Substrate-side application is NOT yet implemented -- SD-033 substrates
    do not yet exist in ree-v3, so the gates are produced but not consumed
    downstream except via the optional salience_apply_to_dacc_bias path
    (scales the dACC score_bias by write_gate("e3_policy")). Full MECH-261
    consumption requires SD-033a/b/c/d substrate landings; the coordinator
    side is in place to be consumed when those land.
    Validation experiment: V3-EXQ-446 confirms gate values are in [0, 1]
    and respond to operating_mode changes; full forward-propagation bias
    test (EXP-0148) deferred until SD-033 substrates land.
  what_would_answer: |
    NOTE: the WRITE-GATE mechanism itself is already `stable` (33 supports / 3
    weakens across 16 exp + 20 lit entries; V3-EXQ-446/455/453 validated the
    landing). This what_would_answer targets the one part still genuinely
    open per the claim's own 2026-04-19 notes: the SALIENCE-NETWORK-AS-
    COORDINATOR overlay, which is REE-specific and has no direct rodent
    validation.
    NON-DEGENERACY PRECONDITION: requires the EXP-0148 SD-032a-ablation arm
    (forcing operating_mode to a fixed external_task vector) to exist and be
    compared against the live coordinator-driven operating_mode with non-zero
    cross-arm variance in post-quiescence action-selection bias -- deferred
    until SD-033 substrates land; a run without that ablation contrast
    self-routes substrate_not_ready.
    CONFIRMING signature (primary): a purely local per-substrate gating
    scheme (no shared operating-mode vector from SD-032a) FAILS to reproduce
    the post-quiescence action-selection bias toward recently replayed
    trajectories that the full coordinator-driven mechanism produces --
    i.e. the shared coordinator overlay is doing real work, not decoration.
    CONFIRMING signature (secondary): ablating SD-032a (forcing a fixed
    external_task operating_mode) ABOLISHES the forward-propagation bias
    (Tambini/Davachi 2019 signature) -- confirms coordination is genuinely
    salience-network-mediated rather than locally emergent.
    FALSIFYING: local per-substrate gating alone reproduces the
    forward-propagation bias just as well as the full SD-032a-coordinated
    mechanism (MECH-261's coordinator overlay is over-specification -- the
    claim's own EXP-0148 falsifiable-primary clause); or ablating SD-032a
    does NOT abolish the bias (the coordinator overlay is not load-bearing;
    forward propagation is coordinated locally rather than by the salience
    network).
  location: evidence/literature/targeted_review_systems_consolidation_waking_propagation/synthesis.md
- id: SD-033
  title: "PFC subdivision architecture: a network-level module with lateral-PFC-analog (mid-lateral rule/goal), OFC-analog (specific-outcome prediction and task-structure map), vmPFC-analog (value integration), premotor/SMA-analog (sequence execution), and frontopolar-analog (branching / nested control; V4 scope) subdivisions. Primary output: the rule/policy context that gates action selection and the natural write target for MECH-261 mode-conditioned gating."
  claim_type: design_decision
  subject: pfc.subdivision_architecture
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: 2026-04-19
  depends_on:
    - SD-032a          # salience-network coordinator (produces operating_mode that MECH-261 uses to gate writes into SD-033 subdivisions)
    - MECH-094         # hypothesis tag (generalised by MECH-261 into these subdivisions)
    - MECH-261         # mode-conditioned write gating (primary consumer of this substrate)
    - ARC-035          # vmPFC value-regulation hub (SD-033c subsumes existing function)
    - MECH-116         # dlPFC LSTM working memory (existing, adjacent to SD-033a)
    - MECH-151         # vmPFC value integration (existing, folds into SD-033c)
    - MECH-152         # vmPFC value integration (existing, folds into SD-033c)
    - MECH-235         # vmPFC-related mechanism (existing, folds into SD-033c)
  epistemic_category: standard
  what_would_answer: |
    SD-033's load-bearing falsifiable content is carried by its first
    subdivision, SD-033a (lateral-PFC-analog), which IS implemented
    (ree-v3/ree_core/pfc/lateral_pfc_analog.py, landed 2026-04-20) -- this is
    a comparative ablation test, not a substrate-existence test. SD-033b
    (OFC-analog) remains unbuilt (second V3 priority); SD-033c/d are
    registration-only consolidations of already-active substrate
    (ARC-035/MECH-151/152/235; E3 trajectory machinery) and are not
    separately falsifiable; SD-033e is V4-deferred (only a V3 forward-compat
    hook exists).

    ANSWER "SD-033a is over-specification, E3 alone suffices" if a ree-v3
    implementation WITHOUT SD-033a's rule/goal substrate reproduces, without
    degradation, all three MECH-262 functional signatures relative to the
    SD-033a-ON arm: (i) stimulus-abstracted generalisation -- transfer to
    novel stimuli under the same rule produces equivalent representational/
    behavioural effect; (ii) distractor-resistant persistence -- rule-
    selective effect survives internal_replay / MECH-092 micro-quiescence
    intervals; (iv) training-dependent emergence -- the effect strengthens
    with training exposure rather than being present from init. ANSWER
    "SD-033a is required" (falsifies the over-specification hypothesis) if
    E3-alone fails any of the three and the SD-033a-ON arm passes it.

    CURRENT STATE (partial, mixed -- not "untested"): signature (ii) has
    DIRECT SUPPORTING evidence (V3-EXQ-484: replay_drift/task_drift ratio
    0.088, C1-C4 PASS, canonical governance replica). Substrate wiring
    integrity (frozen-silent vs trainable-nonzero contrast) is confirmed
    PASS across V3-EXQ-598/598b/598c. Signature (iv)'s causal test
    (V3-EXQ-598b) and the closure-coupled persistence probe (V3-EXQ-461c)
    both reached SD-033a's own decision boundary correctly but could not
    complete, because of substrate ceilings IN OTHER CLAIMS, not in
    SD-033a: MECH-309 monomodal collapse (blocks iv) and the SD-034
    ClosureOperator's env->emit_closure() hook being unwired on the
    foraging-competent agent (blocks the closure-coupled persistence read).
    Both are recorded non_contributory/substrate_ceiling,
    pending_retest_after_substrate, explicitly NOT a weakening of SD-033a.
    Signature (i), stimulus-abstracted generalisation to a novel stimulus
    under a held rule, has no experiment at all yet -- this is the one
    clean gap, not a substrate block.

    NON-DEGENERACY PRECONDITION: a run of this ablation is vacuous, in
    either direction, unless the agent demonstrably occupies the states the
    test depends on. Confirmed failure modes to guard against (all from the
    SD-034 closure-cluster-ext autopsy, 2026-06-12): (a) the agent must
    actually enter external_task / internal_planning mode with nonzero
    occupancy -- V3-EXQ-464c/467c passed their own non-vacuity gate while
    fraction_in_external_task=0.0 for the entire run, because that gate
    counted episode-boundary mode settles rather than genuine mode
    occupancy; (b) if the persistence read is closure-coupled (as in the
    461c design), SD-034's closure-coupled-resolution trigger must actually
    fire (n_closure_coupled_resolutions >= 1) -- committed Hold-window
    formation alone is not sufficient, and this precondition silently
    failed on the foraging-competent substrate; (c) the agent must clear
    whatever generic competence floor gates task engagement on the chosen
    environment (the recurring MECH-457 cold-start / foraging_contact_guard
    pattern) -- an incompetent agent trivially "passes" the E3-alone arm by
    producing no rule-conditioned behaviour of any kind, which is not
    evidence E3 suffices, it is evidence nothing is being tested. A clean
    run must report all three preconditions explicitly, not just the three
    MECH-262 signature contrasts.

    GOVERNANCE FLAG (not resolved here): evidence_quality_note still reads
    "pre-implementation, no V3 evidence yet" (2026-04-19 lit-pull basis
    only); it is stale for the SD-033a piece specifically, which now has
    direct V3 experimental evidence (V3-EXQ-484/456/598/598b/598c/461c).
  functional_restatement: >
    MECH-261 commits to writing into "a PFC-analog policy/rule-level
    substrate" but ree-v3 has no registered substrate corresponding to
    the functions primate and human PFC actually performs. This SD
    registers a PFC subdivision cluster comprising five functional
    subdivisions bound by the SD-032a salience-network coordinator
    (via operating_mode + MECH-261 write gating).

    Rationale. PFC is not a monolith. Biologically, it subdivides into
    at least: (i) lateral-PFC for stimulus-abstracted rule / goal
    representation (Miller & Cohen 2001, Mansouri/Freedman/Buckley
    2020), (ii) OFC for specific-outcome prediction and task-structure
    cognitive maps distinct from scalar value (Rudebeck & Murray 2014,
    Stalnaker/Cooch/Schoenbaum 2015), (iii) vmPFC for subjective value
    integration (already partly covered by ARC-035 / MECH-151 / MECH-152),
    (iv) premotor/SMA for sequence execution (already implemented
    inside E3's trajectory-selection machinery; registered here for
    claim-graph completeness), and (v) frontopolar cortex for branching
    and nested control (Koechlin & Summerfield 2007) -- which is V4
    scope. Lateral PFC is itself gradient-organised caudal-to-rostral
    (Badre & Nee 2018); this cluster captures the mid-lateral level as
    SD-033a and defers frontopolar to SD-033e.

    The SD-032 (cingulate) and SD-033 (PFC) clusters together form the
    cognitive-control backbone of ree-v3. SD-032a is the coordinator;
    SD-033a is the substrate that holds the rules/goals the coordinator's
    operating_mode gates writes into. This architectural split matches
    the primate/human biology and keeps MECH-261 honest -- there is
    now an actual substrate for it to reference.

    Implementation order (minimum viable first):
      1. Lateral-PFC-analog (SD-033a): MINIMUM VIABLE substrate.
         Provides the rule / goal representation MECH-261 writes into.
      2. OFC-analog (SD-033b): second priority. Gives E2 harm-prediction
         and task-structure representation a proper substrate address
         (currently implicit, mixed into generic E2 state).
      3. vmPFC-analog (SD-033c): registration of existing function
         (ARC-035 / MECH-151 / MECH-152) as a SD-033 cluster subdivision.
         No new implementation needed; this is a graph-consolidation step.
      4. Premotor/SMA-analog (SD-033d): already implemented inside E3.
         Registered here so the claim graph mirrors the biology rather
         than implying a premotor-less architecture.
      5. Frontopolar-analog (SD-033e): V4 scope. Hooks only in V3
         (reserve a parallel_goal_deliberation mode name for the operating
         vector; do not implement the substrate yet).

    Steps 1 and 2 are the V3 implementation targets. Steps 3-4 are
    registration-only. Step 5 is deferred.

    Falsifiable: if a ree-v3 implementation without an explicit
    lateral-PFC-analog substrate (SD-033a) reproduces the expected
    behavioural signatures of rule-conditioned action selection --
    stimulus-abstracted rule generalisation, rule-selective persistence
    across distractor events, training-dependent emergence of rule
    representations -- then the SD-033a separation is over-specification
    and mid-lateral PFC function is adequately covered by existing E3
    machinery. Conversely, if E3 alone cannot reproduce these signatures,
    SD-033a is required.
  notes: >
    Biological grounding from targeted_review_pfc_subdivision_architecture
    (7 entries, 2026-04-19; synthesis at
    evidence/literature/targeted_review_pfc_subdivision_architecture/synthesis.md).
    Mean literature confidence 0.80 across three prongs: prong A
    (lateral-PFC rule representation: Miller & Cohen 2001, Badre & Nee
    2018, Mansouri et al 2020); prong B (OFC/vmPFC dissociation:
    Rudebeck & Murray 2014, Stalnaker et al 2015); prong C (premotor/SMA
    + frontopolar: Tanji & Hoshi 2008, Koechlin & Summerfield 2007).

    SD-033 follows the SD-032 cluster template. Both clusters registered
    2026-04-19 (same session), together forming the cognitive-control
    backbone: SD-032 = coordinator + evaluative integration; SD-033 =
    rule/goal/outcome substrate that the coordinator gates writes into.

    This registration is a pre-emptive fix for the lumping failure mode
    that produced SD-003 -> SD-010 -> SD-011 late splits on the
    nociceptive side. Rather than implementing a single "PFC-analog"
    module and later re-splitting it, register the subdivisions up
    front using the primate-biology parcellation as the template.

    Frontopolar-analog (SD-033e) is registered V4-scope to signal the
    architectural intent without committing implementation. When V4
    design begins, a follow-up lit-pull on frontopolar function
    (Burgess, Boorman, Christoff, Mansouri 2015) will be required.
  evidence_quality_note: >
    Registered pre-implementation on the basis of 7-entry lit-pull
    (2026-04-19); mean confidence 0.80 across entries. No V3
    experimental evidence yet; SD-033a implementation is the first
    testable target. Design doc:
    docs/architecture/sd_033_pfc_subdivision_architecture.md
    (subdivisions, MECH-261 gating profile, SD-032 interface contract,
    minimum-viable V3 path, substrate-level falsification signatures).
  location: docs/architecture/sd_033_pfc_subdivision_architecture.md
- id: SD-033a
  title: "Lateral-PFC-analog (mid-lateral rule/goal substrate): module that holds stimulus-abstracted rule and goal representations across delays, persists through simulated distractors, and projects bias signals into E3 action selection. Primary write target for MECH-261 under external_task and internal_planning modes."
  claim_type: design_decision
  subject: pfc.lateral_pfc_analog_rule_goal
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_SD-034-closure-cluster-ext_2026-06-12#V3-EXQ-461c"
      as_of: 2026-06-12
      verdict: non_contributory/substrate_ceiling
  implementation_phase: v3
  v3_pending: true
  pending_retest_after_substrate: true
  narrow_supports_flag: true
  claim_level: architectural
  registered_utc: 2026-04-19
  instantiates: SD-033
  depends_on:
    - SD-033           # parent cluster
    - MECH-261         # primary consumer of this substrate
    - MECH-262         # rule-selective persistence (grounds this claim)
    - MECH-116         # dlPFC LSTM working memory (adjacent, may overlap)
  functional_restatement: >
    Mid-lateral PFC analogue holding the currently active rule and goal
    in a stimulus-abstracted format -- representation of the rule that
    applies rather than of the specific stimulus the rule is operating
    on (Mansouri et al 2020). The substrate must:

      (i) represent rules abstracted over the specific stimulus space
          (category rules, same/different rules, goal-state predicates);
      (ii) persist rule-selective activity across simulated time steps
          without external drive (so MECH-261 has a stable target);
      (iii) project bias signals into E3's trajectory-selection machinery
          (top-down biasing, not direct action control -- Miller & Cohen
          2001 framing);
      (iv) accept writes under MECH-261 gating: licensed during
          external_task and internal_planning; consolidative during
          offline_consolidation; suppressed during internal_replay (so
          imagined sequences do not overwrite the held rule).

    This is the MINIMUM VIABLE addition to ree-v3 for MECH-261 to have
    a real target. Without SD-033a, MECH-261's "write gating on a
    PFC-analog" is vacuous.

    Falsifiable: if ree-v3's existing E3 sequence-selection machinery
    reproduces rule-abstracted generalisation (transfer to novel
    stimuli under the same rule), rule-selective persistence across
    distractor events, and training-dependent rule emergence without
    an explicit lateral-PFC-analog substrate, SD-033a is over-
    specification.
  notes: >
    Biological grounding: Miller & Cohen 2001 (rule-as-top-down-bias
    framing, Annu Rev Neurosci), Mansouri et al 2020 (stimulus-abstracted
    rule neurons in primate lateral PFC, Nat Rev Neurosci), Badre & Nee
    2018 (caudal-rostral abstraction gradient locating the mid-lateral
    level, Trends Cogn Sci).

    This is the SD-033 cluster's V3 implementation priority. Design doc
    will address: format of rule representation (population code vs
    distributed embedding vs explicit symbolic), persistence mechanism
    (recurrent activity vs synaptic hold), bias-signal projection
    (multiplicative gain vs additive shift into E3 trajectory scores).
  evidence_quality_note: >
    Registered pre-implementation. See SD-033 parent.

    2026-05-29 (V3-EXQ-598 failure_autopsy): SD-033a substrate operates as
    specified -- C1 frozen_silent PASS (ARM_0 mean |score_bias|=0.0 all 3
    seeds) + C2 trainable_nonzero PASS (ARM_1 mean |score_bias| 0.10/0.052/
    0.034 clear 0.002 floor) directly evidence signatures (ii) persistence
    and (iii) bias projection into E3. Narrow-supports: positive evidence is
    single-pathway (reef + bipartite env, monomodal policy regime); should
    not on its own promote SD-033a. C3 trainable_not_monomodal FAIL (ARM_1
    reef_visit_fraction 0.096/0.029/0.000; ARM_0 equivalent) was a
    downstream-integration FAIL, not an SD-033a substrate FAIL: C3 depends
    jointly on SD-033a bias-head trainability AND ARC-062 mode-conditioned
    policy AND reef-side state inhabitance; monomodal-policy collapse
    (MECH-309 family) prevents reef-side exploration regardless of
    bias-head trainability. Convergent shape with V3-EXQ-591 autopsy
    substrate-uniform family (540a/b/c/e + 603/603b + 590a + 591 + 598b
    sibling + V3-EXQ-490g cohort). Retest gated on (a) ARC-062 GAP-B
    contributory PASS (in flight via V3-EXQ-569a/b SD-056 falsifier) and
    (b) goal-pipeline / training-regime substrate enrichment. Any successor
    SHOULD ALSO redesign the SD-033a behavioural metric -- C3 reef-visit-
    fraction is downstream-integration, not a clean SD-033a probe.
    Candidate replacements: direct rule_state cosine probes on held-out
    rule-context pairs; rule-selective persistence measurement across
    forced internal_replay events. pending_retest_after_substrate=true.
    Autopsy: evidence/planning/failure_autopsy_V3-EXQ-598_2026-05-29.md.

    2026-06-07 (V3-EXQ-598c, supports): the redesigned direct rule_state cosine
    probe the 598 autopsy called for. Clean load-bearing PASS -- C3 cross-context
    discriminable PASSED, all criteria non-degenerate, precondition met (z_world
    context-discriminability margin 0.308 >= 0.15 positive control). Tests SD-033a
    signatures (i) stimulus-abstracted representation + (ii) representational-level
    persistence, DECOUPLED from policy behaviour (escapes the C3 downstream-
    integration confound that contaminated 598/598b). Supersedes V3-EXQ-598b for
    the SD-033a portion (598b SD-033a entry marked superseded; MECH-262 there
    unchanged non_contributory). This is a clean support, but SD-033a stays
    candidate/v3_pending (held); it does not on its own promote. supersedes:
    V3-EXQ-598b.
  implementation_note: >
    IMPLEMENTED 2026-04-20 as additive bias-into-E3 substrate. Module:
    ree_core/pfc/lateral_pfc_analog.py (LateralPFCAnalog). Config:
    REEConfig.use_lateral_pfc_analog (default False; backward compatible).
    Gate-modulated EMA persistence of rule_state (signature ii) driven by
    SalienceCoordinator.write_gate("sd_033a") (MECH-261 primary consumer
    relationship). Stimulus-abstracted format via [1, rule_dim=16] latent
    with delta/world projections (signature i). Top-down bias into E3 via
    per-candidate score_bias head (signature iii) -- frozen-random with
    last Linear zeroed so initial bias=0 (backward compatible under
    activation). Training-dependent emergence (signature iv) deferred:
    head is untrained in landing; phased-training protocol not wired.
    Three design alternatives documented in
    docs/architecture/sd_033a_lateral_pfc_analog.md; lit-pulls queued
    in evidence/planning/task_inbox.md. Validation experiment
    V3-EXQ-456 queued (diagnostic). MECH-094 handled via MECH-261
    gate (no separate hypothesis_tag check).
  location: evidence/literature/targeted_review_pfc_subdivision_architecture/synthesis.md
- id: SD-033b
  title: "OFC-analog (specific-outcome prediction and task-structure cognitive map): module that represents the state space of the current task and the specific outcomes expected from actions in each state. E2's natural substrate address for harm-predictive rollouts; distinct from scalar value integration (SD-033c)."
  claim_type: design_decision
  subject: pfc.ofc_analog_outcome_structure
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-732_2026-07-10
      as_of: 2026-07-10
      verdict: non_contributory/precondition_unmet
  implementation_phase: v3
  v3_pending: false
  pending_retest_after_substrate: true
  epistemic_category: substrate_conditional
  claim_level: architectural
  registered_utc: 2026-04-19
  instantiates: SD-033
  depends_on:
    - SD-033           # parent cluster
    - MECH-263         # OFC state-space / specific-outcome representation (grounds this claim)
    - MECH-261         # gating of writes under operating_mode
  functional_restatement: >
    OFC analogue holding two related representations:

      (i) specific-outcome prediction: given a state and candidate
          action, what particular outcome is expected (not just its
          scalar value) -- Rudebeck & Murray 2014 "oracle" framing;
      (ii) task-structure cognitive map: a structured representation
          of what states the agent can be in, what transitions are
          possible, and what outcome structure each state predicts --
          Stalnaker/Cooch/Schoenbaum 2015 framing.

    These are complementary framings of the same substrate: the
    cognitive map carries structured outcome predictions, and
    specific-outcome queries are resolved against it. ree-v3's E2 is
    already a specific-outcome predictor computationally; SD-033b
    gives it a biological substrate address and makes its
    representational content (state-space structure) explicit.

    MECH-261 write gating on SD-033b: writes licensed during
    internal_planning (speculative outcome prediction over
    counterfactual branches); suppressed during internal_replay
    (so imagined rollouts do not overwrite the learned task model);
    consolidative writes during offline_consolidation (slow updating
    from accumulated real-outcome experience -- systems-consolidation
    target; Frankland & Bontempi 2005); active read during
    external_task.

    Falsifiable: if E2's harm-prediction behaviour and ree-v3's
    rule-conditioned outcome-generalisation can be reproduced without
    distinguishing a state-structure substrate from a scalar-value
    substrate (SD-033c), the OFC/vmPFC functional dissociation does
    not apply at REE's level of description and SD-033b collapses
    back into SD-033c.
  notes: >
    Biological grounding: Rudebeck & Murray 2014 (Neuron) for the
    OFC-as-oracle framing on selective-lesion evidence; Stalnaker,
    Cooch & Schoenbaum 2015 (Nat Neurosci) for the OFC-as-cognitive-map
    framing. Both argue against the pure "OFC does value" reading;
    REE takes them as complementary accounts of the same structured
    outcome representation.

    Fills a gap identified in the 2026-04-19 PFC subdivision audit:
    existing REE claims lump OFC under vmPFC. SD-033b registers the
    OFC function separately so E2 predictions have a biologically
    proper substrate address, distinct from the subjective-valuation
    function covered by SD-033c / ARC-035 / MECH-151 / MECH-152.
  implementation_note: >
    2026-06-22 GAP-8 devaluation-head DECOUPLE landed (ree-v3 ree_core/pfc/ofc_analog.py).
    Routed by failure_autopsy_V3-EXQ-485l (re-derive brake FIRED -> implement-substrate;
    485m gain-tweak REFUSED). The single shared state_bias_head under the +/-ofc_bias_scale
    clamp has no feasible gain band (485k gain 4.0 saturated devalued range 0.0; 485l gain 1.5
    undershot 0.031 < 0.05 floor) because it must also carry the C2 high-threat range. Fix:
    a SECOND output head devaluation_bias_head (OFCConfig.use_devaluation_head, default False)
    sharing the state_code+candidate input but clamped to +/-devaluation_bias_scale (default 2.0,
    INDEPENDENT of ofc_bias_scale), read via compute_devaluation_bias(); the C2 head's clamp is
    untouched (magnitude untraded), the devaluation head's larger independent clamp lets the
    in-band re-ranking gain produce a supra-floor differentiated devalued range without
    saturating, and the MECH-449 No-Go viability re-derives from the decoupled head. No-op
    default (bit-identical OFF); 6 contracts (tests/contracts/test_sd_033b_gap8_devaluation_head.py)
    + preflight PASS. PROMOTES NOTHING (status stays candidate / substrate_conditional /
    pending_retest_after_substrate). The 485-lineage behavioural retest (NEW letter, supersedes
    485l; claim_ids=[SD-033b, MECH-263]) is GATED behind this build, queued separately via
    /queue-experiment. See ree-v3/CLAUDE.md "SD-033b GAP-8 DECOUPLE".
  evidence_quality_note: >
    2026-06-22 (governance-apply, failure_autopsy_V3-EXQ-485m CONFIRMED -> non_contributory; claim
    UNWEAKENED): V3-EXQ-485m (ree-cloud-4) supersedes 485l; FAIL/non_contributory/non_degenerate on
    the DECOUPLED devaluation_bias_head (ree-v3 758956f). The decouple FIXED 485l clamp-starvation:
    C1 devaluation behavioural shift now 3/3, No-Go engages 2/3, devalued bias range supra-floor 3/3
    -- mechanism EXERCISED not falsified -- but C1b vector-inversion 1/3 (shift-without-inversion,
    cosine +0.72/+0.98 on 2/3 seeds) and C2 committed-class separation 1/3 still fail. Ruling out the
    magnitude artifact, the valuation face does not convert in ISOLATION. 3rd convergent
    fails-C2-alone datum (654i demotion, 654j Go/No-Go, 485m OFC): conversion is emergent from the
    assembled stack, not any single selection-face lever. P3-ofc face-validated (user gate
    2026-06-22) -> folds into conversion_ceiling_campaign:FULLSTACK co-armed arm (use_ofc_devaluation_
    head ON). Re-derive brake FIRED (11th substrate_ceiling reading on SD-033b/MECH-263): a 485n
    isolated valuation-face re-queue is REFUSED; next test is the co-armed full-stack arm. Status
    unchanged (substrate_conditional / pending_retest_after_substrate). PROMOTES NOTHING.

    2026-06-22 (governance-apply, failure_autopsy_V3-EXQ-485l CONFIRMED -> non_contributory; claim
    UNWEAKENED): V3-EXQ-485l (2026-06-22, ree-cloud-1) supersedes 485k; FAIL/non_contributory/
    non_degenerate:false (no governance weight), self-route substrate_not_ready_requeue CONFIRMED +
    re-localised by failure_autopsy_V3-EXQ-485l_2026-06-22. MECH-449 (Go/No-Go constitution) is BUILT
    + validated (689g PASS 3/3, promoted candidate->provisional) so the 485k 'build MECH-449' route is
    DISCHARGED, but 485l shows the residual is STRUCTURAL SCALE: the single shared OFC head under the
    +/-0.5 ofc_bias_scale clamp cannot carry a devalued re-ranking magnitude above the 0.05 readout
    floor AND the No-Go viability trigger while preserving C2. 485k gain 4.0 saturated (devalued range
    0.0); 485l gain 1.5 undershot (0.031 < 0.05). The bias VECTOR inverts (l2 1.83, cosine -0.716, C1b
    PASS 2/3) -- correct direction -- but the clamp compresses the magnitude, starving the viability
    No-Go (engaged 1/3, < 2/3 gate). MECH-449 correct-but-non-engaging here = positive evidence the
    constitution needs a supra-floor valuation input, NOT a weakens. SD-033b / MECH-263 devaluation
    half: non_contributory / substrate_conditional / pending_retest_after_substrate, blocked on a NEW
    substrate -- OFC devaluation-head clamp rescale / head decoupling (amend
    f_dominance_conversion_ceiling). RE-DERIVE BRAKE FIRED (10th non_contributory autopsy on
    SD-033b/MECH-263): a plain 485m gain-tweak is REFUSED -- no feasible gain band on the shared
    +/-0.5 clamped head; route to /implement-substrate. MECH-448/MECH-449 NOT weakened (both engaged
    correctly within their scope). PROMOTES NOTHING.

    2026-06-22 (governance-apply, failure_autopsy_V3-EXQ-485k CONFIRMED -> non_contributory; claim
    UNWEAKENED): V3-EXQ-485k (supersedes 485j) FAILed non_contributory / non_degenerate:false on the
    MECH-448 demotion-enabled selector -- both DVs vacuous (readiness 1/3 via aggregate-max masking a
    per-seed collapse; FIX-1 re-rank driver overshoots the +/-0.5 OFC clamp -> saturation -> devalued
    range 0.0 on 2/3; regressed 485j C2). Diagnosis: demotion-alone (MECH-448) cannot express the
    active No-Go withdrawal behavioural devaluation requires -> substrate_ceiling /
    pending_retest_after_substrate behind the MECH-449 Go/No-Go constitution (built 2026-06-21;
    corrected arm V3-EXQ-485l gated behind build + 689e). MECH-448 NOT weakened (envelope fired, excl
    4-5, winner!=F on 2/3). The 485e->k lineage clears the ARC-107 demotion-alone-insufficient build
    gate. PROMOTES NOTHING.

    2026-06-19 (governance-apply, failure_autopsy_V3-EXQ-485h CONFIRMED -> non_contributory):
    V3-EXQ-485h (supersedes 485g; TERMINUS of the 485 behavioural lineage) is the 485g-designed
    T-vs-F disambiguator. Readiness MET 3/3 (cross-candidate bias range 0.50 high-threat >= 0.05
    floor; head weight-delta 5.63, genuinely trained), C3 frozen-head silence control PASS 3/3,
    but C1 devaluation shift 1/3 + C2 between-context TV ~0 (0/3). DISAMBIGUATION RESOLVED: (T)
    threat-invariant-bias REFUTED (range COLLAPSES at the devalued state, collapse ratio 0.12, 3/3
    -- the head is genuinely outcome-value-conditioned); (F) MECH-439 F-dominance conversion ceiling
    IMPLICATED -- the live-E3 leg shows the OFC bias reaching accumulator authority (range up to 0.50)
    with ZERO committed conversion, and the proven 569i top-k F-bypass was NOT in this loop. The
    MECH-263 devaluation signature is positively shown at the REPRESENTATION level; only the
    BEHAVIOURAL conversion fails, blocked at the shared F-dominated selector. non_contributory (NOT a
    weakens, NOT a demotion): the OFC mechanism is a faithful translation and was not given a fair
    conversion test; SOLE genuine behavioural entry -- do not let it define exp_conf. Manifest
    self-stamped weakens -> non_contributory (flat + nested pack); index rebuilt. 485g -> superseded.
    substrate_queue SD-033b failure_record += 485h. epistemic_category substrate_ceiling +
    pending_retest_after_substrate UNCHANGED. Sequenced behind the MECH-439 F-rebalance (569i top-k /
    GAP-B 654g / 689a / 625e); deferred retest V3-EXQ-485i routes the OFC bias through the proven
    569i top-k channel.
    2026-06-19 (governance-apply, failure_autopsy_V3-EXQ-485g CONFIRMED ->
    epistemic_category substrate_ceiling): V3-EXQ-485g is the FIRST non-vacuous
    trained-OFC-head behavioural test (485e range-starved, 485f vacuous-readiness,
    both fixed): readiness MET (cross-candidate bias range 0.171 >= 0.05 floor,
    2/3 seeds; head weight-delta 6.32, genuinely trained), C3 frozen-head silence
    control PASS, but C1 devaluation shift {0.001,0.0,0.010} << 0.05 (0/3) and C2
    between-context TV ~0 (degenerate, 0/3) -- a genuine 0.17 bias RANGE with ZERO
    behavioural conversion. Adjudicated substrate_ceiling / non_contributory, NOT a
    weakens: the OFC mechanism is a faithful translation (lit_conf 0.863) and was
    not given a fair conversion test (the outcome-coupled driver supplies zero
    anti-range gradient at low threat, and the devalued-state bias range -- the
    decisive conversion-vs-conditioning disambiguator -- was not recorded). The
    manifest MECH-439 F-dominance self-route is an ANALOGY to the broader
    conversion-ceiling pattern, NOT measurable in 485g's isolated-OFC-softmax loop
    (modulatory selection authority OFF -> F absent from this readout). Routed to
    V3-EXQ-485h (record devalued-state + per-task-role-context bias range, close the
    driver gap with a paired high-threat/devalued contrast, optional live-E3-selector
    arm). Sole genuine experimental entry for SD-033b -- do not let it define exp_conf
    (held non_contributory; conflict-ratio rec auto-resolved). Substrate_queue SD-033b
    failure_record amended with the 485g entry.
    2026-06-11 (governance cycle #4, failure_autopsy_V3-EXQ-485e CONFIRMED):
    V3-EXQ-485e (evidence-grade trained-OFC-head behavioural) FAILed
    non_contributory via its same-statistic non-vacuity gate (readiness_met=false;
    cross-candidate bias RANGE cleared the floor on only 1/3 seeds). This is NOT a
    weakens against SD-033b -- the trained head trained on all 3 seeds, but two
    gaps starve the cross-candidate range every MECH-263 behavioural DV requires:
    (1) OFC compute_bias clamp-saturation on 2/3 seeds (the GAP-8 landing's
    pre-registered risk), and (2) candidate-bank z_world collapse
    (reconstruction-trained e2.world_forward world_states[1]; ARC-065 GAP-A /
    SD-056), so even the one unsaturated+READY seed produced a vacuous deval_shift
    (8e-05 vs 0.05 margin) + a degenerate C2 separation ratio
    (criteria_non_degenerate.C2=false). epistemic_category for this entry =
    substrate_ceiling; pending_retest_after_substrate set. Retest = V3-EXQ-485f
    (raise ofc_bias_scale / pre-clamp signal + enable SD-056 e2_action_contrastive
    in P0 + absolute floor on between_context_tv). substrate_queue: amend the
    modulatory-bias-selection-authority (ARC-065 GAP-A) entry with 485e as a
    corroborating failure record (no new substrate). SD-033b stays candidate.

    Substrate IMPLEMENTED 2026-04-25 in
    ree-v3/ree_core/pfc_analogs/ofc_analog.py (per
    REE_assembly/evidence/planning/substrate_queue.json
    implementation_status=implemented). 2026-04-28T22Z governance:
    v3_pending flipped from true to false based on V3-EXQ-485 substrate-
    landing PASS (canonical run 20260427T054454Z + 3 deterministic re-run
    replicas marked superseded; UC1-UC5 all PASS: instantiation, gate
    modulates update, bias zero at init, backward-compat, reset clears
    state code). Lit anchors (Wilson-Niv 2014 cognitive-map, Schuck-Niv
    2016 fMRI MVPA, Bradfield-Hart-Balleine 2018 anterior-mOFC lesion,
    Stalnaker 2021, Zhou 2019, Howard-Kahnt 2017) provide converging
    theoretical / human-imaging / primate-lesion / rodent-lesion
    evidence; lit_conf 0.863. Status remains candidate -- behavioural
    validation of SD-033b's distinct-from-anchor-field claim and
    MECH-263 functional signatures (devaluation sensitivity,
    perceptually-identical / task-distinct discrimination) remain
    deferred to a successor EXQ. Promotion to provisional requires that
    successor. Oracle path landed 2026-05-04: OFCConfig.use_outcome_oracle
    (default False) adds query_outcome() delegating to E2HarmSForward;
    V3-EXQ-485a queued (6-sub-test UC6 round-trip: oracle output matches
    e2_harm_s.forward() <1e-6 diff; AssertionError when disabled; reset()
    clears caches; oracle_is_ready False by default; get_state() exposes
    oracle diagnostics). The oracle path is the MECH-263 prospective query
    substrate -- devaluation sensitivity and task-role discrimination
    remain deferred to env-extension EXQs.
    Trained-OFC-head pathway landed 2026-06-09 (commitment_closure:GAP-8;
    the SD-033b analogue of SD-033a GAP-D, ree-v3 382db2c):
    OFCConfig.train_state_bias_head (REEConfig.ofc_train_state_bias_head,
    default False -> last Linear zeroed, bit-identical OFF) un-zeros the
    state_bias_head last Linear so it can be added to an experiment optimizer
    via OFCAnalog.bias_head_parameters() and trained on the E3
    score-aggregation gradient (E3 loss -> score_bias -> compute_bias() ->
    head weights; no separate loss term). This makes the deferred
    trained-OFC-head behavioural arm measurable -- the same mechanism that
    closed the SD-033a Go-side silence (GAP-1). Status stays candidate; the
    behavioural validation EXQ (devaluation + perceptual-discrimination on
    the trained head) is the promotion gate -- 485b/485c PASSed only as
    representation-level MECH-263 functional-signature diagnostics. No flag /
    confidence change in this landing (substrate only).

    [2026-06-12 governance / confirmed failure_autopsy_batch9_2026-06-12 applied]:
    V3-EXQ-485f (the 485e trained-OFC-head retest) reclassified weakens ->
    non_contributory (measurement_test_design_defect: readiness-gate miscalibration).
    The three 485e fixes took effect (ofc_bias_scale 0.5 defeated clamp-saturation;
    SD-056 e2_action_contrastive lifted bank_zworld_spread off zero; absolute
    between_context_tv floor caught the C2 vacuity), and the readiness gate
    (bias_range>1e-3) cleared by a hair (max 0.00898) -- but that is ~50x below the
    0.05 the devaluation/between-context DVs require, so 'ready' did not certify a
    fair test (devaluation_shift 5e-5..2.6e-4). NOT a fair falsification -> NOT a
    weakens. pending_retest_after_substrate retained. Retest = V3-EXQ-485g with the
    readiness floor aligned to the DV-significance floor (bias_range competitive with
    the selection softmax, ~0.05). SD-033b stays candidate; no demotion.
  location: evidence/literature/targeted_review_pfc_subdivision_architecture/synthesis.md
- id: SD-033c
  title: "vmPFC-analog (subjective value integration): module that integrates scalar value across goals, needs, and competing options to produce a unified value signal. Consolidates existing ARC-035 / MECH-151 / MECH-152 / MECH-235 functions under the SD-033 cluster."
  claim_type: design_decision
  subject: pfc.vmpfc_analog_value_integration
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: false
  claim_level: architectural
  registered_utc: 2026-04-19
  instantiates: SD-033
  subsumes:
    # Consolidation step taken 2026-06-09 (commitment_closure:GAP-9). These four
    # pre-existing claims provide the computational function SD-033c names as the
    # vmPFC-analog subdivision; each carries the reciprocal `instantiates: SD-033c`
    # back-reference, so the subsumption is now a bidirectional edge in the graph.
    - ARC-035          # vmPFC affective/normative state-preparation hub
    - MECH-151         # cue-indexed E1->E2 action-affordance modulation (vmPFC->striatum/premotor)
    - MECH-152         # cue-indexed E1->E3 terrain-precision modulation (vmPFC value gain)
    - MECH-235         # vmPFC model-based/model-free arbitration (commitment/urgency)
  depends_on:
    - SD-033           # parent cluster
    - ARC-035          # existing vmPFC value-regulation hub claim
    - MECH-151         # existing vmPFC value integration
    - MECH-152         # existing vmPFC value integration
    - MECH-235         # existing vmPFC-related mechanism
    - MECH-261         # gating of writes under operating_mode
  functional_restatement: >
    Scalar-value integration substrate. Reads specific-outcome
    predictions from SD-033b (OFC-analog), affective / harm signals
    from SD-032 (cingulate cluster), and homeostatic drive from SD-012,
    and produces a unified subjective-value signal that downstream
    selection machinery (E3, BG-analog) consults.

    This subdivision does NOT represent specific outcomes or task
    structure -- that is SD-033b's job (per Rudebeck & Murray 2014
    dissociation and Stalnaker et al 2015 cognitive-map framing).
    SD-033c carries the scalar value that rides on top of those
    representations.

    SD-033c is largely a claim-graph consolidation step rather than a
    new substrate: existing claims (ARC-035, MECH-151, MECH-152,
    MECH-235) already cover the computational function. SD-033c
    registers it as a named subdivision of the SD-033 cluster so the
    PFC substrate graph is complete and mirrors the biology.

    MECH-261 write gating on SD-033c: writes during external_task
    (value updates from realised outcomes); consolidative writes
    during offline_consolidation; reduced gain during internal_replay
    so replayed trajectories do not directly overwrite value estimates
    (this is what forces replay to propagate via SD-033a / SD-033b
    rather than writing straight into value).
  notes: >
    Biological grounding: existing REE claims (ARC-035, MECH-151,
    MECH-152, MECH-235) plus the PFC lit-pull's evidence that vmPFC
    value integration is dissociable from OFC-analog outcome prediction
    (Rudebeck & Murray 2014, Stalnaker et al 2015).

    v3_pending intentionally false: this subdivision's computational
    function is already covered by existing claims and does not
    require new V3 implementation work. This is a graph-consolidation
    subdivision, not a new substrate.
  evidence_quality_note: >
    Registered pre-implementation as a subdivision consolidating
    existing ARC-035 / MECH-151 / MECH-152 / MECH-235 functions.
    No new evidence requirement beyond what those claims already carry.

    2026-06-09 (commitment_closure:GAP-9): the consolidation step is now
    DONE, not merely declared. The reciprocal `subsumes:` edge was added
    to this record and each of the four source claims already carries
    `instantiates: SD-033c` (ARC-035 claims.yaml; MECH-151 / MECH-152 /
    MECH-235 claims.yaml), so the subsumption is a closed bidirectional
    edge in the claim graph rather than a one-directional dependency.
    No new implementation, no status change, no new evidence requirement:
    SD-033c remains the named graph address for the vmPFC value-integration
    function already implemented across its four source claims. The
    architecture doc (sd_033_pfc_subdivision_architecture.md, "Related
    claims" + the SD-033c subdivision section) records the consolidation
    as complete.
  consolidation_status: complete    # commitment_closure:GAP-9, 2026-06-09 -- subsumes/instantiates edges closed both directions
  location: evidence/literature/targeted_review_pfc_subdivision_architecture/synthesis.md
- id: SD-033d
  title: "Premotor/SMA-analog (sequence execution substrate): module that holds candidate action sequences, binds actions to states, and executes committed sequences. Largely implemented inside E3's existing trajectory-selection machinery; registered for claim-graph completeness."
  claim_type: design_decision
  subject: pfc.premotor_sma_analog_sequence_execution
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: false
  claim_level: architectural
  registered_utc: 2026-04-19
  instantiates: SD-033
  design_doc: docs/architecture/sd_033d_premotor_sma_analog.md   # commitment_closure:GAP-9, 2026-06-09 -- maps existing E3 sequence-selection machinery onto premotor/SMA biology (no new substrate)
  depends_on:
    - SD-033           # parent cluster
    - SD-033a          # upstream rule/goal substrate
    - MECH-094         # hypothesis tag on sequence commits (specific case of MECH-261)
    - MECH-261         # mode-conditioned write gating on sequence updates
    - MECH-090         # bistable commitment latch (sequence execution / committed-step stepping)
    - ARC-028          # hippocampal completion -> BetaGate release (sequence-end signal)
  functional_restatement: >
    Downstream executive substrate that receives the rule/goal context
    from SD-033a and proposes, selects, and executes action sequences
    in service of it. In primate biology this corresponds to pre-SMA
    (sequence planning, set-switching), SMA proper (sequence execution),
    and dorsal premotor cortex (stimulus-action binding) -- Tanji &
    Hoshi 2008.

    In ree-v3, this function is largely already implemented inside E3's
    trajectory-proposal and selection machinery. SD-033d registers it
    as a named subdivision of SD-033 so the claim graph mirrors the
    biology rather than implying a premotor-less architecture. No new
    implementation is required for SD-033d itself; its function is
    redistributed across existing E3 machinery.

    MECH-261 write gating on SD-033d: sequence-level writes
    (hypothesis-tagged via MECH-094) are licensed during external_task
    and internal_planning; suppressed during internal_replay unless
    the hypothesis tag is explicitly set; consolidated during
    offline_consolidation.

    Architectural note: Tanji & Hoshi describe a lateral-PFC -> pre-SMA
    -> SMA -> dorsal premotor functional gradient. REE's SD-033a ->
    SD-033d relation captures the rule -> sequence-execution segment
    of this gradient. Finer-grained premotor subdivision is not
    registered here -- it can be added later if experimental evidence
    motivates it.
  notes: >
    Biological grounding: Tanji & Hoshi 2008 (Physiol Rev) executive
    behavioral control synthesis.

    Registered V3 scope because the function exists but as a
    registration-only step, not a new implementation target. If future
    evidence shows that E3's trajectory machinery inadequately covers
    the pre-SMA / SMA / premotor distinctions, SD-033d can be split
    into SD-033d-i / d-ii / d-iii along Tanji & Hoshi's subdivisions.
  evidence_quality_note: >
    Registered pre-implementation as a graph-completeness subdivision
    mapping existing E3 sequence-selection machinery onto the
    primate/human premotor biology.

    2026-06-09 (commitment_closure:GAP-9): the design_doc field was null
    at registration; the dedicated design doc
    docs/architecture/sd_033d_premotor_sma_analog.md is now written and
    linked (design_doc above). It maps the EXISTING ree-v3 machinery --
    E3 trajectory proposal/scoring/selection (E3TrajectorySelector),
    MECH-090 committed-trajectory stepping (REEAgent.select_action
    _committed_step_idx), the MECH-090 bistable BetaGate, and the
    ARC-028/MECH-105 hippocampal-completion release -- onto the Tanji &
    Hoshi 2008 pre-SMA / SMA / dorsal-premotor gradient. NO new substrate
    is proposed; SD-033d stays a registration-only consolidation. The
    doc records the falsifier (a split into SD-033d-i/ii/iii) for if/when
    evidence shows E3 inadequately covers the premotor distinctions.
  location: evidence/literature/targeted_review_pfc_subdivision_architecture/synthesis.md
  epistemic_category: substrate_conditional
  what_would_answer: >
    TWO DISTINCT THINGS ARE BEING TESTED HERE, and they must not be conflated.

    (i) V3-NARROW GEOMETRIC SLICE (BUILT, 2026-07-09; use_frontopolar_decommit /
    frontopolar_gain, ree_core/pfc/frontopolar_analog.py compute_counterfactual_value).
    A parameter-free, SINGLE-TICK goal-proximity advantage of the best unchosen
    candidate over the committed endpoint (||z_chosen-z_goal|| - ||z_alt-z_goal||),
    wired only as an entry-relative de-commit PRESSURE on the natural-commit latch
    (NaturalCommitUrgencyRelease). This is NOT a maintained parallel value channel
    and is NOT what this claim asserts -- it is a deliberately degenerate stand-in,
    built to serve a different substrate goal (the MECH-439 F-dominance
    duration-face de-commit lever), that happens to instantiate MECH-264's switch
    logic in its simplest possible form.

    (ii) V4-FULL SUBSTRATE (UNBUILT). The mechanism this claim actually asserts: a
    RUNNING, MULTI-STEP counterfactual-value ESTIMATE maintained in parallel with
    SD-033c's chosen-option value signal (not recomputed fresh each tick), with a
    switch fired when it exceeds the chosen estimate by a threshold-sensitive
    margin. Structurally blocked on MECH-163 (hippocampal multi-step trajectory
    planning, unmet) -- without multi-step plans a counterfactual value is
    necessarily 1-step and collapses to MECH-257 (dACC immediate-reward
    comparator), which is a different mechanism entirely.

    NON-DEGENERACY PRECONDITION (must hold before any result is interpretable):
    - For (i): the isolated-GAP-A substrate must sustain a genuine natural-commit
      latch to de-commit FROM -- i.e. competent committed foraging must exist at
      all. V3-EXQ-726 (the slice's own validation script, arms ARM_FP_CONTRAST vs
      ARM_FP_ON) is written and gate-hygiene-fixed but explicitly HELD, NOT QUEUED:
      its resume condition is "a substrate that sustains competent committed
      foraging," which is the SAME substrate gap the MECH-457 competence-floor
      campaign (V3-EXQ-724 lineage, GOV-FANOUT-1) is diagnosing. That campaign
      remains open (candidate/v3_pending) as of the latest confirmed evidence
      (V3-EXQ-821, 2026-07-25). Running 726 before MECH-457 clears would
      self-route substrate_not_ready_requeue on a precondition already known to
      be unmet.
    - For (ii): requires MECH-163 multi-step planning landed, plus a non-degenerate
      3+-alternative decision task (this claim's own functional_restatement names
      "simulated three-alternative decision tasks").

    CONFIRMING observation:
    - (i), once MECH-457 clears and V3-EXQ-726 runs: ARM_FP_ON's median committed-
      latch occupancy (ncur_last_occupancy_at_release) drops >= 0.40 vs
      ARM_FP_CONTRAST on >= 2/3 strong-F seeds, WITH frontopolar_release_count > 0
      on the ON arm (proving the release is attributable to the counterfactual
      term firing when cfv_now > cfv_at_entry, not to flat-timeout or F noise).
      This would confirm the geometric switch signal behaves as a genuine
      counterfactual-value comparator even in its degenerate 1-step form --
      supportive but NOT sufficient to promote MECH-264 itself, since (i) is
      explicitly not the claim's mechanism.
    - (ii), in a V4 implementation: lateral-FPC-analog activity tracks the value
      of the best UNCHOSEN alternative (dissociable from SD-033c's chosen-option
      value stream) during simulated three-alternative decision tasks, and
      individual variation in the counterfactual signal's magnitude predicts
      switch propensity (per Boorman et al. 2009).

    FALSIFYING observation (this claim's own primary falsifier, verbatim from
    functional_restatement): if lateral-FPC-analog activity in a V4
    implementation tracks CHOSEN-option value rather than counterfactual-option
    value during simulated three-alternative decision tasks, MECH-264 fails at
    source and SD-033e collapses functionally into SD-033c -- in that case the
    OFC/vmPFC/FPC value architecture is over-specified at REE's level of
    description and the frontopolar subdivision should be folded into the
    vmPFC-analog. A secondary falsifier applies to (i) alone: if V3-EXQ-726 runs
    post-clearance and ARM_FP_ON shows no attributable occupancy drop
    (frontopolar_release_count == 0, or the drop is indistinguishable from the
    flat-urgency contrast), the geometric slice fails to behave as a
    counterfactual comparator even in its minimal form -- this would NOT itself
    falsify MECH-264 (the slice is not the claim), but would remove the one
    piece of experimental purchase currently available on any instantiation of
    the mechanism.

    Until MECH-457 clears, MECH-264 has zero experimental evidence
    (genuine_exp_count: 0 in claim_evidence.v1.json; all current evidence
    entries are literature) and cannot be tested even in its narrow V3 form.

    [2026-08-08 thought-digestion]: implementation_phase was flipped v4->v3 on
    2026-08-07 as an administrative phase-follows-dependency move (MECH-487 lists
    MECH-264 as a dependency) -- this is NOT a green light to build; the full
    mechanism remains unbuilt and blocked as described above.
- id: SD-033e
  title: "Frontopolar-analog (parallel-goal deliberation substrate): V4-scope module maintaining counterfactual-value estimates for alternatives to the current course of action, mediating transitions between external engagement and internally-generated deliberation, enabling disengagement from the current task to explore novel alternatives, and monitoring relative importance across multiple active goals. Hooks reserved in V3 via the operating_mode vocabulary; substrate deferred to V4."
  claim_type: design_decision
  subject: pfc.frontopolar_analog_branching
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: false
  claim_level: architectural
  registered_utc: 2026-04-19
  instantiates: SD-033
  reclassification_note: >
    [2026-08-07 governance, phase-label-follows-dependency, user-directed]: implementation_phase
    v4 -> v3. check_claim_phase_consistency.py flagged this claim as directly needed by MECH-487, a
    v3 build commitment (MECH-487 depends on "the SD-033e/MECH-264 transient-scalar substrate" per
    its own registration note). Standing rule: anything directly needed for a v3 build is marked
    v3 so v3 can complete. NUANCE preserved, not overwritten: what MECH-487 actually depends on is
    the ALREADY-LANDED V3-narrow parameter-free geometric de-commit lever (implementation_note
    above, 2026-07-09), not the FULL learned-substrate scope this claim's title and
    v4_scope_deferred note describe (MECH-264 counterfactual-value head + MECH-265 monitor,
    prerequisites still genuinely V4-bound per MECH-163/CausalGridWorldV2). The claim as a whole
    reclassifies because the phase label tracks closure/roadmap counting for the DEPENDED-UPON
    claim, not a re-scoping of its full content -- the v4_scope_deferred text below still
    correctly describes what remains actually deferred.
  implementation_note: >
    V3-NARROW DE-COMMIT LEVER LANDED 2026-07-09 (user-directed; PROMOTES NOTHING,
    status/confidence unchanged). A deliberate low-odds-but-owed V3-narrow slice --
    ONLY function (1) MECH-264 counterfactual-value + function (4) disengagement-for-
    exploration, in a PARAMETER-FREE geometric form (no learned head, no training) --
    was implemented as a commit-LATCH DE-COMMIT / switch-propensity lever for the
    DURATION face of the F-dominance conversion ceiling (MECH-439), NOT the V4 learned
    substrate. Config REEConfig.use_frontopolar_decommit (default False) +
    frontopolar_gain (default 0.0), bit-identical OFF. MECH-264 counterfactual value =
    goal-proximity ADVANTAGE ||z_chosen - z_goal|| - ||z_alt - z_goal|| (NON-F channel);
    release pressure = frontopolar_gain * max(0, cfv_now - cfv_at_entry) (entry-relative
    derivative) injected into ree_core/policy/natural_commit_urgency.py. The full learned
    MECH-264/MECH-265 substrate + gateway stay V4 (implementation_phase v4, v3_pending
    false unchanged): the use_frontopolar_analog learned-head flag is untouched, and
    MECH-265 relative-importance is structurally V4-blocked (single-resource env, no
    K>=2 goals). Motivated by failure_autopsy_MECH-445-cluster-715a-717_2026-07-07 (the
    arming residual needs a DISTINCT de-commit-release lever). Record: ree-v3/CLAUDE.md
    "SD-033e (V3-narrow)"; validation isolated-GAP-A only. [CORRECTED 2026-08-08 governance,
    GFLAG-0011]: previously read "all-ON gated on V3-EXQ-724" as if pending execution -- V3-EXQ-724
    already ran 2026-07-09 (failure_autopsy_V3-EXQ-724_2026-07-09, FAIL, evidence_direction
    non_contributory, claim_ids=[]) as a competence-localization diagnostic returning
    competence_deficit_diffuse, not a frontopolar all-ON validation. The all-ON de-commit contrast
    is currently degenerate for a different reason: it is blocked on the MECH-457 competence floor,
    still open per V3-EXQ-687a/819a/748a (non-resolving as of 2026-07-30/31). A dedicated de-commit
    validation experiment is still owed once that floor clears -- 724 does not need to be
    re-run or awaited.
  v3_v4_boundary:
    # commitment_closure:GAP-9, 2026-06-09 -- decision: formally DEFER the substrate.
    # implementation_phase stays v4 (NOT pulled to v3): SD-033e's only reverse-
    # dependents are MECH-264 / MECH-265, both v4, and NOTHING v3 depends on it.
    # The GAP-9 V3-scope work here is graph/registration completeness, not a phase
    # change. See feedback_phase_label_follows_dependency: phase is a prediction
    # from dependencies, and SD-033e has no v3 dependency to reclassify it.
    decision: defer_substrate_to_v4
    v3_scope_present: >
      Forward-compatibility HOOK ONLY (done). The MECH-261 operating_mode
      vocabulary reserves the future mode name `parallel_goal_deliberation`
      and MECH-261's gate logic is a dict keyed on arbitrary mode names (not a
      fixed 4-tuple), so V4 can add the mode without a schema change. A no-op
      STUB module is present in code (ree-v3/ree_core/pfc/frontopolar_analog.py)
      reserving the module surface + the `sd_033e` write-gate target + the
      MECH-264 / MECH-265 head interface; every method is a no-op (or raises
      NotImplementedError) under the default `use_frontopolar_analog=False`, so
      the default V3 path is bit-identical. This V3-scope graph/registration
      work is COMPLETE -- nothing further is owed for GAP-9.
    v4_scope_deferred: >
      The actual substrate -- the MECH-264 counterfactual-value head + the
      MECH-265 relative-importance monitor over K>=2 active goals -- is NOT
      built and is deferred to V4. Prerequisites are V4-bound: MECH-163
      hippocampal multi-step planning (1-step counterfactuals collapse MECH-264
      to MECH-257) and a CausalGridWorldV2 dual-active-goal extension (MECH-265
      is structurally inaccessible with a single resource cue). A follow-up
      frontopolar lit-pull (Burgess; Boorman; Christoff; Mansouri 2015) and the
      SD-033e design doc are V4-entry prerequisites, NOT V3 work.
    reserved_mode_name: parallel_goal_deliberation   # live in ree-v3 salience_coordinator + frontopolar_analog stub
  depends_on:
    - SD-033           # parent cluster
    - SD-033a          # rostral extension of the lateral-PFC abstraction gradient
    - MECH-261         # forward-compatible operating_mode should reserve a parallel_goal_deliberation mode
    - MECH-264         # frontopolar counterfactual-value tracking -- primary mechanism implementing SD-033e's Boorman function
    - MECH-265         # frontopolar relative-importance monitoring -- primary mechanism implementing SD-033e's Mansouri 2017 function
  functional_restatement: >
    V4-scope module maintaining counterfactual-value estimates for
    alternatives to the current course of action, mediating
    transitions between external engagement and internally-generated
    deliberation, enabling disengagement from the current task to
    explore novel alternatives, and monitoring relative importance
    across multiple active goals. The most rostral (frontopolar /
    BA 10) end of the Badre & Nee 2018 caudal-rostral abstraction
    gradient in lateral PFC.

    Four converging functions from the Prong D lit-pull
    (2026-04-19), each supported by methodologically distinct
    evidence, constitute the computational specification:

      (1) COUNTERFACTUAL VALUE TRACKING (Boorman/Behrens/Woolrich/
          Rushworth 2009): lateral FPC maintains a running estimate
          of the value of unchosen alternatives in parallel with the
          vmPFC-analog's chosen-option value signal (SD-033c).
          Individual variation in this signal predicts propensity to
          switch.

      (2) EXTERNAL/INTERNAL ATTENTIONAL GATEWAY (Burgess/Dumontheil/
          Gilbert 2007): BA 10 mediates the switch between
          engagement with the current external world (medial BA 10)
          and internally-generated cognition about alternatives
          (lateral BA 10). Directly biologically grounds MECH-261's
          operating_mode vector.

      (3) DISENGAGEMENT-FOR-EXPLORATION (Mansouri/Buckley/Mahboubi/
          Tanaka 2015): FPC enables releasing executive control
          from the current task and redistributing it to novel
          options. Without FPC, the animal over-focuses and cannot
          learn novel alternative rules. Lesion data specify the
          failure mode (ablation -> over-focus + inability to learn
          novel alternatives).

      (4) RELATIVE-IMPORTANCE MONITORING OVER COMPETING GOALS
          (Mansouri/Koechlin/Rosa/Buckley 2017, NRN integrative
          synthesis): FPC monitors relative importance of current
          vs alternative goals and, in humans specifically, supports
          parallel monitoring of multiple goals with flexible
          switching. Subsumes the earlier Koechlin & Summerfield
          2007 branching framing as one special case.

    These four are plausibly four methodological views of one
    underlying computation: maintenance of a parallel, competing-
    goal representation with relative-value-weighted switching
    logic. REE's V4 long-horizon ethical reasoning will depend on
    this substrate -- explicit counterfactual weighing of parallel
    policies is what Frankfurt-style reflective endorsement of
    first-order motivations requires.

    V3 commitment: do NOT implement SD-033e. Reserve schema forward-
    compatibility only. Specifically, the operating_mode vocabulary
    (currently {external_task, internal_planning, internal_replay,
    offline_consolidation}) should reserve a future mode name
    (parallel_goal_deliberation) so that V4 can be added without
    disruptive schema changes. MECH-261's gate logic should be
    implemented as a dictionary keyed on mode names rather than a
    fixed 4-tuple.

    V4 commitment: register MECH-264 (frontopolar counterfactual-
    value tracking, grounding Boorman 2009) and MECH-265
    (frontopolar relative-importance monitoring across competing
    goals, grounding Mansouri 2017) in a follow-up governance
    session before V4 implementation begins.
  notes: >
    Biological grounding: seven literature entries across Prong C
    (Koechlin & Summerfield 2007) and Prong D (Burgess/Veitch/
    Costello/Shallice 2000, Burgess/Dumontheil/Gilbert 2007,
    Boorman/Behrens/Woolrich/Rushworth 2009, Christoff/Gordon/
    Smallwood/Smith/Schooler 2009, Mansouri/Buckley/Mahboubi/Tanaka
    2015, Mansouri/Koechlin/Rosa/Buckley 2017) in
    targeted_review_pfc_subdivision_architecture. Mansouri 2017 NRN
    is the load-bearing integrative account.

    Registering this subdivision pre-emptively rather than waiting
    until V4 design begins serves two purposes: (1) it signals the
    architectural intent so that V3 design choices (especially
    MECH-261's gate-logic data structure and the operating_mode
    vocabulary) stay forward-compatible; (2) it prevents the same
    late-split failure mode that produced SD-010/SD-011 from
    recurring at the V3/V4 boundary.

    Reserved V4 operating-mode name is parallel_goal_deliberation
    (renamed 2026-04-19 from the earlier deliberative_branching
    placeholder after the Prong D lit-pull showed Koechlin branching
    is one special case of the broader Mansouri 2017 parallel-goal
    monitoring function).
  evidence_quality_note: >
    Registered pre-implementation. Seven literature entries from
    Prong C (Koechlin & Summerfield 2007) and Prong D (Burgess 2000,
    Burgess 2007, Boorman 2009, Christoff 2009, Mansouri 2015,
    Mansouri 2017) support the V4 scoping. The Mansouri 2017 Nature
    Reviews synthesis is the load-bearing integrative account.
    Functional spec broadened 2026-04-19 from "branching / nested
    control" to "parallel-goal deliberation" (see Prong D synthesis).
    Mechanism claims MECH-264 (counterfactual value tracking) and
    MECH-265 (relative-importance monitoring) registered 2026-04-19
    as the primary mechanisms grounding the four-function spec; see
    their entries for Boorman/Mansouri/Burgess literature attribution.

    Prong D full survey complete 2026-04-27, five new entries plus
    synthesis at evidence/literature/targeted_review_frontopolar_analog_prong_d/
    (synthesis.md is the integrated reading): Boorman, Behrens,
    Woolrich & Rushworth 2009 (Neuron, supports 0.84, FPC pairwise
    counterfactual tracking -- direct anchor for MECH-264); Burgess,
    Dumontheil & Gilbert 2007 (TiCS, supports 0.74, gateway hypothesis
    BA10 medial / lateral); Mansouri, Buckley, Mahboubi & Tanaka 2015
    (PNAS, supports 0.85, macaque selective FPC lesion -- *causal*
    evidence that FPC normally disengages from current task);
    Mansouri, Koechlin, Rosa & Buckley 2017 (NRN, supports 0.82,
    competing-goals integrative review -- direct anchor for MECH-265);
    Koechlin & Summerfield 2007 (TiCS, supports 0.72, A-P-gradient
    framework). Synthesis answers the four lit-pull questions:
    (a) MECH-264 pairwise counterfactual evaluation supported empirically
    (Boorman 2009); MECH-265 K>=2-way set monitoring supported
    architecturally (Mansouri 2017) but cleanest implementation is
    parallel pairwise comparisons against value-of-current-decision.
    (b) Continuous-modulatory gateway function (Burgess 2007); FPC
    score_bias output should be graded, not discrete admit/reject;
    discrete operating-mode commitment lives at SalienceCoordinator
    not at the FPC analog itself. (c) FPC lesion produces dissociable,
    specific phenotype (Mansouri 2015) -- not a generic executive
    deficit; the FPC analog should function as disengage-and-redistribute,
    its score_bias should *reduce* current-candidate weight in
    proportion to counterfactual evidence, not augment it. (d) Training
    signal: for V3 frozen-random head; for V4 the candidate is
    reward-PE on counterfactual rollouts via E2 model, integrating
    with SD-039 / MECH-292 / MECH-293 ghost-goal substrate. Three open
    architectural questions for the SD-033e design doc itself:
    (1) hierarchical vs parallel wiring with SD-033a; (2) substrate
    localisation (medial / lateral split or unified); (3) computational
    granularity of K-way alternative-goal list population. Falsifiable
    signature: FPC-analog ablation should produce *better* current-task
    focus and *worse* counterfactual exploration (mirroring Mansouri
    2015 lesion phenotype). Phase 1.1 of SD-033e prep plan complete;
    next step is the design doc, NOT V3 implementation. Do not
    implement pre-V4.

    2026-06-09 (commitment_closure:GAP-9): V3-vs-V4 boundary made
    EXPLICIT (see the v3_v4_boundary field above). GAP-9 formally DEFERS
    the substrate: implementation_phase stays v4 (unchanged) -- no genuine
    v3 dependency on SD-033e was found, so there is nothing to reclassify.
    What GAP-9 closes is the V3-scope graph/registration completeness: the
    forward-compatibility hook (reserved `parallel_goal_deliberation` mode
    name + keyed-dict MECH-261 gate logic + the no-op frontopolar_analog
    stub reserving the module/write-gate/head surface) is present and
    bit-identical-OFF, and that is the entirety of what V3 owes here. The
    counterfactual-value substrate (MECH-264/MECH-265), its frontopolar
    lit-pull, and the SD-033e design doc remain V4-entry prerequisites and
    are NOT GAP-9 deliverables. The architecture doc reconciliation in this
    pass also fixed a stale mode-name drift: sd_033_pfc_subdivision_architecture.md
    had retained the old `deliberative_branching` placeholder in two places;
    it now reads `parallel_goal_deliberation` to match this claim and the
    ree-v3 code.
  location: evidence/literature/targeted_review_frontopolar_analog_prong_d/synthesis.md
  epistemic_category: substrate_conditional
  what_would_answer: >
    This claim packages TWO distinct testability scopes -- do not conflate them.

    SCOPE 1 -- the V3-NARROW de-commit lever (BUILT, 2026-07-09; blocked, not
    unbuilt). ree-v3 REEConfig.use_frontopolar_decommit + frontopolar_gain wire a
    parameter-free geometric proxy for function (1) counterfactual value
    (||z_chosen - z_goal|| - ||z_alt - z_goal||) into a commit-latch release
    pressure for function (3)/(4) disengagement-for-exploration. This is testable
    in principle today; it is currently untestable IN PRACTICE for one named
    reason, not an open-ended one.

    Non-degeneracy precondition (all three must hold; none does as of 2026-08-08):
      (i) the MECH-457 competence floor is cleared -- per
          competence_floor_recurrence_repose_2026-08-08.md, the campaign's
          discrimination job is finished but its one residual thread
          (H-consummation-binding) showed both arms installed at ~14% of ceiling
          (V3-EXQ-821), underpowering ANY contrast run on this substrate; the
          scoped fix (stronger BC install landing in the 20-40 forage/episode
          range, corrected 8-metric instrument -- landed 2026-08-07) is named but
          NOT yet queued or re-run. Until a run on this substrate clears a usable
          floor, an all-ON/all-OFF de-commit contrast cannot be distinguished from
          floor-saturation noise;
      (ii) a genuine 3+-alternative decision context is present, so "best
          unchosen alternative" is a differentiated rather than degenerate
          quantity (a 1-2-option context makes the geometric advantage trivial);
      (iii) use_frontopolar_decommit is actually flipped ON with nonzero
          frontopolar_gain (the OFF/default arm is bit-identical to baseline by
          construction and is not itself a test of anything).

    CONFIRMING (mirrors the Mansouri 2015 lesion phenotype already named in this
    claim's notes): the ON arm shows release pressure -- de-commit/switch rate --
    that positively covaries with frontopolar_gain * max(0, cfv_now -
    cfv_at_entry), and relative to the OFF arm shows measurably WORSE current-task
    persistence but BETTER counterfactual exploration (willingness to abandon the
    committed trajectory for a superior alternative); the OFF arm shows the
    inverse -- over-focus on the committed course, impaired exploration of
    better-valued alternatives.

    FALSIFYING: once the floor precondition is cleared and a genuine 3+-way
    decision context is exercised, the ON arm's switch/de-commit behaviour is
    statistically indistinguishable from the OFF arm's despite nonzero
    frontopolar_gain -- i.e. the geometric counterfactual-advantage signal is not
    behaviourally load-bearing even in this minimal parameter-free form. This
    would mean the V3-narrow slice does not reproduce even the weak version of
    the Mansouri 2015 phenotype, independent of whatever the full V4 learned
    substrate might later show.

    SCOPE 2 -- the V4-FULL four-function module (UNBUILT; MECH-264 head +
    MECH-265 monitor over K>=2 simultaneously active goals). Not operationally
    testable yet at all, and no non-degeneracy precondition can be stated in
    concrete terms until its own prerequisites exist: MECH-163 hippocampal
    multi-step planning (a 1-step counterfactual collapses MECH-264 to MECH-257)
    and a CausalGridWorldV2 dual/K-active-goal environment extension (function
    (4)/MECH-265 is structurally inaccessible with the current single-resource
    env). Per this claim's own v4_scope_deferred note, the SD-033e design doc
    itself (with a follow-up frontopolar lit-pull) is a V4-ENTRY prerequisite,
    not V3 work -- so this scope's own concrete falsifier is properly drafted at
    that design-doc stage, not here. The three pre-registered V4 proposals
    already staged for it (manual_proposals.v1.json EXP-0165/0166/0167 --
    counterfactual-value dissociation, Mansouri-2015 ablation phenotype, and
    SD-033c/SD-033e double-dissociation respectively) remain status: gated,
    reconfirmed gated 2026-08-02, on exactly these unmet prerequisites.

    POINTER, not re-derivation: for the counterfactual-value-TRACKING mechanism
    itself (MECH-264's own falsifier -- chosen-vs-unchosen value dissociation at
    the V4 substrate level), see MECH-264's own what_would_answer. This entry
    is scoped to the MODULE level (the V3-narrow lever's own testability, and the
    V4-full four-function packaging separately) and does not restate MECH-264's
    falsifier.
- id: SD-034
  title: "Closure operator over committed rule_state: on satisfaction of a committed rule_state's completion condition, a 'done' token releases the bistable commitment latch (MECH-090), imposes a temporary No-Go bias (MECH-260) on the just-completed action-class / rule, and discharges the residue associated with that commitment within its domain. The closure token is what turns evaluation off; its absence leaves the governance loop stuck in evaluate-more even when the rule is satisfied. [UMBRELLA NARROWED 2026-06-19: the de-commit-authority sub-property (closure->latch coupling + de-commit magnitude) is decomposed into children MECH-445 (closure->beta coupling engagement) + MECH-446 (de-commit-authority magnitude); see those claims. SD-034 remains the umbrella design_decision -- closure-operator existence + five-part 'done' token firing + No-Go install are retained here and provisional; NOT demoted, NOT superseded.]"
  claim_type: design_decision
  subject: governance.closure_operator
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-466d_2026-06-24#V3-EXQ-466d"
      as_of: 2026-06-24
      verdict: non_contributory/non_contributory_run_not_substrate_ceiling
  implementation_phase: v3
  v3_pending: false
  pending_retest_after_substrate: true  # commitment-closure-control-plane (gov 2026-06-13; 460f autopsy 2026-06-18)
  governance_note_2026_06_19_claimsynth: >
    CLAIM-SYNTHESIS DECOMPOSITION (user-approved 2026-06-19T20:38Z; spec
    evidence/planning/claim_synthesis_SD-034-closure_2026-06-19.md). Routed by confirmed
    failure_autopsy_V3-EXQ-460g_2026-06-19 (7th autopsy; the 460f WATCH ITEM fired). The
    7-autopsy chain (460b..460g, 2026-06-04..06-19) showed the single "behavioural de-commit
    authority over the MECH-090 beta latch" sub-property bundles a multi-stage de-commit
    PIPELINE: closure-firing (S1, closed via Leg-A hook, 460d C1 PASS), detector magnitude /
    trained head (S2, a built substrate leg), closure->beta COUPLING engagement (S3), de-commit
    MAGNITUDE (S4), and coupling-measurability-under-refractory (S5). Discrimination gate
    (Step 3): granularity debt -- NOT vacuous/test-design (5 structurally-different signatures,
    not one test iterated below resolution a la MECH-341 660-series), NOT substrate-not-ready
    (Legs A+B+C + beta-coupling + magnitude lever all built and firing), NOT clean falsification
    (seed-42 within-arm de-commit 0.333->0.0 in 460g + seed-44 460e ON<OFF are positive existence
    proofs of correct sign). The decisive evidence is the DOUBLE DISSOCIATION across 460f/460g:
    coupling ENGAGES yet magnitude INSUFFICIENT (460f seed 42, 36 coupled elevations but swamped)
    vs magnitude HAS AUTHORITY yet coupling UNMEASURABLE (460g seed 42, 0.333->0.0 but counter 0) --
    a single property cannot express that, two named children can. Decomposed S3->MECH-445
    (closure->beta coupling engagement) + S4->MECH-446 (de-commit-authority magnitude); both
    candidate, standard, v3_pending, pending_retest_after_substrate; both depends_on: SD-034.
    S5 (coupling-measurability-under-refractory) is a MEASUREMENT/test-design property with no
    biological mechanism -- REFUSED on the anti-proliferation rail (not a claim); it is the 460h
    experiment fix (refractory-independent commit-intent counter), which is why both children carry
    "refractory-independent coupling gate" in their what_would_answer. SD-034 stays a provisional
    design_decision umbrella (closure-operator existence + five-part done token + No-Go install
    retained); NOT demoted, NOT superseded. The gated 460h re-queue targets MECH-445/446, NOT this
    coarse umbrella.
  governance_note_2026_06_18: >
    V3-EXQ-460f (supersedes 460e; de-commit retest on the beta-engagement-amended
    substrate) applied this cycle per confirmed failure_autopsy_V3-EXQ-460f_2026-06-18:
    NOT a fair-test weakens -> non_contributory (substrate-limited). The beta-engagement
    amend cleared all 4 readiness gates so the C2 de-commit occupancy-drop DV ran for the
    first time (PASS seed 42 -33.5%, FAIL 2/3); but the coupling diagnostic
    sd034_n_closure_coupled_elevations fired 36/52 on seed 42 vs 0/0 on 43/44 (on
    strong-natural-commit seeds the coupling was inert and the DV reduced to the bare
    Leg-B 5-tick refractory, swamped by ~530-560 natural-commit elevated steps; between-arm
    DV underpowered). Seed 42 + 460e seed 44 are existence proofs of correct de-commit SIGN.
    MECH-260 -> supports (No-Go 3/3); MECH-261 -> non_contributory (hook-driven closures
    bypassed its mode-conditioning predicate -- NOT exercised, stable status protected).
    pending_retest_after_substrate stays true; substrate_queue commitment-closure-control-plane
    amended with the de-commit-magnitude lever + within-arm C2 DV (re-issue 460g). 460f manifest
    evidence_direction_per_claim corrected to {SD-034 non_contributory, MECH-260 supports,
    MECH-261 non_contributory}. No status change (SD-034 stays provisional).
  governance_note_2026_06_13: >
    demote_to_candidate recommendation (conflict_ratio 0.889, 4 weakens) HELD this
    cycle (illusory-demotion guard). Cohort siblings V3-EXQ-461c + 466c
    reclassified non_contributory (substrate_ceiling, pending_retest) per confirmed
    failure_autopsy_SD-034-closure-cluster-ext_2026-06-12 -- closure done-token
    never fired despite env completions. The two validation re-queues of the
    closure-control-plane substrate that landed 2026-06-12 ran: V3-EXQ-460d showed
    C1_n_closures PASS (closure now FIRES on the env-completion hook -- the *c
    n_closures=0 gap is CLOSED; MECH-260 supports) but C2_beta_release/C4 FAIL
    (de-commitment behavioural authority residual; Leg C trained rule_bias_head
    still experiment-side); V3-EXQ-468d precondition_unmet (contradiction injection
    1/3 seeds). Both 460d + 468d routed to /failure-autopsy this cycle (no
    evidence_direction applied -- left pending). Demotion revisited next cycle once
    the autopsy adjudicates the residual closure leg.
  governance_note_2026_06_13b: >
    PM cycle (gov 2026-06-13T20:04Z): the two routed FAILs are now ADJUDICATED.
    Per confirmed failure_autopsy_SD-034-closure-control-plane-d_2026-06-13:
    V3-EXQ-460d -> SD-034 + MECH-261 reclassified weakens->non_contributory (manifest
    override applied; MECH-260 retains supports), pending_retest_after_substrate;
    V3-EXQ-468d -> SD-034/MECH-268/MECH-090 confirmed non_contributory (substrate_not_ready:
    total_beta_elevated=0 on seeds 43/44), pending_retest_after_substrate. substrate_queue
    amended (SD-034, action=amend). The demote_to_candidate recommendation is DEFERRED again
    (user 2026-06-13): with the 460d/468d weakens now removed from scoring, re-evaluate the
    conflict_ratio post-rebuild before any provisional->candidate move.
  implementation_note_2026_06_16: >
    Leg C BUILT (substrate-only; NO status/v3_pending/confidence change). The
    460d/468d *d retests set lateral_pfc_train_rule_bias_head=True but never added
    the head to any optimizer (confirmed failure_autopsy_SD-034-closure-control-plane-d_2026-06-13),
    so the SD-033a rule_state carried no task-shaped magnitude and the closure-coupled
    de-commit had no net authority over the MECH-090 latch (460d C2/C4 FAIL). A
    scaffold_train_rule_bias_head leg now trains agent.lateral_pfc.bias_head_parameters()
    during scaffolded_sd054_onboarding P1 via the V3-EXQ-598b outcome-coupled E3-gradient
    REINFORCE pattern (mirrors scaffold_train_harm_pathway; no-op default, bit-identical
    OFF; ree_core untouched). 109/109 scaffolded contracts + 7/7 preflight; smoke: head
    trains (max|dW| 0.0015>0, mean |bias| 0.039) ON / dW=0 exactly OFF. Validation
    V3-EXQ-460e (supersedes 460d): non-cap-pinned ON<OFF de-commit DV + beta-engagement
    + rule_bias-magnitude readiness gate. SD-034 stays provisional /
    pending_retest_after_substrate; promotes nothing until 460e PASSes. See
    docs/architecture/sd_034_governance_closure_operator.md Leg C amend.
  governance_note_2026_06_17: >
    V3-EXQ-460e (supersedes 460d) RAN terminal FAIL/non_contributory; adjudicated by
    confirmed failure_autopsy_V3-EXQ-460e_2026-06-17 (analysis committed fb9fc52bc4;
    applied this session). Leg C now WORKS (rule_bias_trained 1.0 3/3) but the MECH-090
    bistable beta latch engaged on only 1/3 seeds (beta_engagement_both_arms 0.333 <
    0.667), so the readiness gate self-routed substrate_not_ready_requeue BEFORE the C2
    de-commit DV ran -- SD-034 was never exercised. Code-confirmed commit-without-beta
    dissociation (agent.py:5843-5859: with both MECH-090 R-c gates OFF, beta elevates
    only on E3 result.committed (running_variance < commit_threshold), which fires on 1/3
    seeds on the 603n foraging substrate; the closure control-plane sets committed_trajectory
    + fires closures regardless; the trained head biases SCORING not commit-entry
    decisiveness). Seed 44 (beta engaged both arms; C2 ON 11.73 < OFF 14.87, non-cap-pinned)
    is the positive existence proof the de-commit DV is sound. SD-034 stays provisional +
    non_contributory (manifest self-stamped) + pending_retest_after_substrate (kept true);
    NO status/confidence change. substrate_queue commitment-closure-control-plane amended
    (action=amend; 5th failure_record + beta-engagement deliverable). NOT a falsification.
    Escalation pre-registered by failure_autopsy_SD-034-closure-control-plane-d_2026-06-13
    line 90. Closes via the post-amend 460f re-queue (held until /implement-substrate amend).
  implementation_note: |
    IMPLEMENTED 2026-04-20 in ree-v3/ree_core/governance/closure_operator.py (ClosureOperator, ClosureOperatorConfig, ClosureEvent, 616 LOC). Five- part "done" token: (a) MECH-090 beta_gate.release(); (b) MECH-260 dacc.inject_nogo(action_class, count); (c) ResidueField.discharge_ domain(z_world, factor, radius) with hard 1e-6 floor preserving the "residue cannot be erased" invariant; (d) SalienceCoordinator.update_ signal("closure_event", value) re-biasing affinity toward internal_ planning; (e) dacc.reset_episode_pe() + optional dacc_pe_cap (MECH-268 coupling). Completion detector: rule_state delta < threshold for N consecutive ticks AND beta elevated AND current_mode in allowed_closure_modes AND sd_033a write_gate >= min. Master switch use_closure_operator (default False, bit-identical OFF). Mode-conditioning generalises MECH-094 hypothesis-tag: closure blocked in internal_replay / offline_consolidation modes. Validation PASSed 2026-04-21: V3-EXQ-460 (verified-but-not-released, 6 sub- tests), V3-EXQ-466 (satisficing / residue discharge, 5 sub-tests), V3-EXQ-468 (commitment vs contradiction, coupled SD-034 + MECH-268, 6 sub-tests). Behavioural variants with full E3 task loop + tolerance-band completion env deferred (depend on phased rule_state training + CausalGridWorldV2 env extension not yet on roadmap). Design doc: docs/architecture/sd_034_governance_closure_operator.md (backfilled 2026-04-27). Lit-pull recommendations from 2026-04-27 largely match V3 implementation (graded No-Go via MECH-260 FIFO, multi-region substrate-agnostic detector); one V4 reconsideration flagged: route post-completion negative bias through SD-033a's per-candidate bias projection rather than only via MECH-260 action-class FIFO.

    2026-06-12 commitment-closure-control-plane amend (/implement-substrate): gave the ClosureOperator behavioural authority on the 603n substrate via two no-op-default legs. Leg A -- explicit env-completion hook seam REEAgent.notify_env_completion -> closure_operator.emit_closure (config use_closure_env_completion_hook; closes the V3-EXQ-460c n_closures=0 defect: the env sequence_complete signal was never routed into emit_closure). Leg B -- BetaGate de-commitment hold/refractory installed by ClosureOperator._fire (config closure_decommit_hold_ticks; closes the V3-EXQ-468c committed_frac cap-pin: a closure-driven release now survives >1 tick). Leg C (the trained GAP-D rule_bias_head + non-cap-pinned de-commitment DV) is experiment-side. Bit-identical OFF (1014 contracts + 7/7 preflight; 6 new in test_sd034_decommit_hold_and_env_hook.py). NO flag/confidence change: SD-034 provisional holds, MECH-261 stable untouched, MECH-260 candidate. Validation V3-EXQ-460d (supersedes 460c) + V3-EXQ-468d (supersedes 468c) -- retest gate: n_closures>=1 reachable + nogo_installed>=1 on >=2/3 seeds AND a non-cap-pinned de-commitment DV ON<OFF on >=2/3 seeds. See ree-v3/CLAUDE.md "SD-034 AMEND: commitment-closure-control-plane" + docs/architecture/sd_034_governance_closure_operator.md amend section.
  claim_level: architectural
  registered_utc: 2026-04-20
  depends_on:
    - SD-033           # operating-mode register / governance cluster
    - SD-033a          # rule/goal substrate whose rule_state the closure token acts on
    - MECH-090         # bistable BetaGate commitment latch (the Hold the token releases)
    - MECH-260         # dACC action-class suppression (the No-Go the token triggers)
    - MECH-094         # hypothesis tag / categorical write gate (closure discharges its domain of residue)
    - SD-032a          # SalienceCoordinator (operating-mode producer; closure emits a mode-context signal)
    - SD-032b          # dACC-analog (conflict / prediction-error context that closure caps)
  functional_restatement: >
    Design decision. REE-V3 already has the Go / Hold / No-Go substrate
    from the existing basal-ganglia-like gate (MECH-090 BetaGate, MECH-091
    urgency-interrupt, MECH-260 action-class suppression) and the
    operating-mode register (SD-032a SalienceCoordinator). What is missing
    is the OPERATOR that turns evaluation OFF on successful completion.
    Without it, a committed rule_state that has been satisfied keeps
    being evaluated, keeps consuming mode budget, keeps generating
    residue, and the agent cannot disengage.

    SD-034 registers a closure operator that, on satisfaction of a
    committed rule_state's completion condition (detected in the
    SD-033a rule substrate), emits a single coordinated signal that:

      (a) releases the MECH-090 BetaGate commitment latch for the
          current rule (Hold -> inactive);
      (b) installs a brief No-Go bias on the just-completed action-
          class / rule via MECH-260's action_history (prevents
          immediate re-entry / refractory period);
      (c) discharges the residue field entries tagged with that
          rule_state's domain (within-domain, not global), so the
          post-completion mode vector is not dragged back by stale
          goal-field signal;
      (d) emits a closure event to SD-032a so the operating-mode
          vector can relax toward its default mixture;
      (e) caps / resets the MECH-268 dACC pe signal for this
          rule-episode so subsequent conflict accumulation starts
          from a clean baseline.

    This is the REE analogue of task-set disengagement in the
    biological literature (Collins & Frank 2014) and of OFC / ACC
    sequence-completion cells signalling the end of a behavioural
    sequence (Rich & Shapiro 2009; Schuck 2016).

    Mode-conditioning. The closure operator must only fire from rule_states
    whose origin is gated into the current operating mode. A rule installed
    during internal_replay must not produce a closure signal that releases
    Hold on an external_task commitment.

    Falsifiable (primary): if V3-EXQ-460 (verified-but-not-released) and
    V3-EXQ-466 (satisficing / residue discharge) can be resolved by any
    combination of MECH-090 / MECH-260 / MECH-094 tuning WITHOUT adding
    a closure operator, SD-034 is over-specification -- closure is
    emergent from the existing substrate.

    Falsifiable (secondary): if adding a closure operator resolves
    verified-but-not-released but introduces premature disengagement
    on multi-step tasks (flushing Hold before the terminal step),
    either the completion-condition detector (SD-033a side) or the
    mode-conditioning logic is wrong -- not the operator itself.
  notes: >
    Registered 2026-04-20 as part of the SD-033 governance plan
    (evidence/planning/sd033_governance_plan.md) translating the 2026-04-20
    GAP MEMO ("REE-V3 is not missing cognition. It is missing
    governance.") and the OCD thought set (2026-04-20_ocd1..ocd4) into
    falsifiable claims. The four-constraints analysis in ocd1 (evaluation
    must terminate; termination must be binding; termination must be
    domain-scoped; the dACC signal must saturate) is the source
    specification for this operator.

    Distinct from MECH-090. MECH-090 is the Hold latch ITSELF. SD-034
    is the OPERATOR that RELEASES the latch at the right moment. The
    existing MECH-091 urgency release handles wrong-moment release
    (harm interrupt); SD-034 handles right-moment release (completion).

    Distinct from MECH-260. MECH-260 is a standing recency-bias
    suppressor over action classes. SD-034 triggers a TARGETED No-Go
    pulse on the just-completed rule specifically, on completion,
    within a refractory window. MECH-260's history mechanism is the
    substrate; SD-034's closure pulse is one of several callers.

    V3 scoping: this is the primary missing piece identified in the
    GAP MEMO. Prioritised ahead of MECH-266/267/268 for implementation.
  evidence_quality_note: >
    2026-06-19 (governance-apply, failure_autopsy_V3-EXQ-460g CONFIRMED -> non_contributory):
    V3-EXQ-460g (supersedes 460f) ran terminal FAIL/non_contributory. The committed-run-scaled
    de-commit MAGNITUDE lever + the 460f-prescribed tightened coupling non-vacuity gate are
    SELF-DEFEATING: the scaled refractory pins at the 60-tick cap on ~530-560-step runs and
    BetaGate.elevate() is a no-op while the refractory is active, so the closure-coupled
    re-elevations the gate counts can never fire (sd034_n_closure_coupled_elevations 36->0 seed42,
    0/3). The de-commit refractory HAS authority (seed-42 within-arm occupancy 0.333->0.0, C2 PASS)
    but suppresses its own coupling certifier, so the self-route substrate_not_ready_requeue is
    correct + conservative (NO false weakens). SD-034 -> non_contributory + pending_retest (de-commit
    authority not fairly certifiable on the current coupling metric). 7th SD-034-lineage autopsy; the
    460f granularity-debt WATCH ITEM trigger FIRED -> route /claim-synthesis (primary) + a 460h
    re-queue with a refractory-independent commit-intent coupling counter (secondary). 460f ->
    superseded; substrate_queue commitment-closure-control-plane failure_record += 460g.
    Registered 2026-04-20 as candidate with v3_pending:true. Biology
    lit-pull complete 2026-04-27, six entries in
    evidence/literature/targeted_review_sd_034/entries/: Schuck 2016
    (OFC state-map, supports), Barnes 2011 (action-bracketing,
    supports), Rich & Shapiro 2009 (PFC strategy switch, supports),
    Collins & Frank 2014 (OPAL D1/D2 opponent, mixed), Mayr & Keele
    2000 (backward inhibition, supports 0.84), Smith & Graybiel 2013
    (dual-operator action-bracketing, supports 0.78). Implementation
    recommendations from synthesis: (1) implement No-Go as graded
    score_bias decay on the just-completed rule_state with exponential
    decay over a refractory window, NOT a hard refractory gate (Mayr &
    Keele backward-inhibition is a graded RT cost, not a binary
    block); (2) closure-detection signal can fire from multiple
    substrates (action-bracketing in striatum + IL per Smith &
    Graybiel; OFC/ACC sequence-completion per Schuck / Rich) -- do not
    lock to OFC/ACC-only localisation; (3) at completion, post a
    transient negative score_bias to the just-completed rule's score
    channel via SD-033a's existing per-candidate bias projection
    rather than introducing a separate gate. V3-EXQ-460
    (verified-but-not-released) and V3-EXQ-466 (satisficing / residue
    discharge) are the primary falsifiers queued in
    ree-v3/experiment_queue.json. See
    evidence/planning/sd033_governance_plan.md (anchor doc).
    2026-06-04 governance: behavioural successors V3-EXQ-460b/461b/466b/468b
    ran but were reclassified non_contributory + substrate_ceiling (agent
    commits but never tolerance-completes a waypoint, so n_closures=0 in
    every arm including the positive-control arms -- the goal-achievement /
    foraging-competence layer scaffolded_sd054_onboarding is the missing
    prerequisite; confirmed failure_autopsy_V3-EXQ-460b-461b-464b-466b_2026-06-04).
    Provisional->candidate demotion (conflict_ratio 0.889, weakens=4) HELD:
    all four weakens were this substrate-limited cohort, now excluded from
    scoring. pending_retest_after_substrate set.
    2026-06-12 governance: the *c cohort (foraging-competent 603n substrate)
    landed. V3-EXQ-460c (n_closures=0 despite env sequence_completions=2/5/6 --
    env completion not routed into emit_closure(); rule_bias_head untrained) and
    V3-EXQ-468c (closure-coupled release fires MORE near contradiction, C1 PASS,
    but no de-commitment hold -> committed_frac DV cap-pinned) reclassified
    non_contributory + substrate_ceiling per confirmed
    failure_autopsy_SD-034-closure-cluster_2026-06-12 (user-confirmed in the
    2026-06-12T22:16Z cycle). Cohort siblings V3-EXQ-461c/466c (+ 464c/467c/629b
    on MECH-266/SD-032a/MECH-342) flagged for a /failure-autopsy cluster
    extension and left PENDING (no inline weakens). Provisional->candidate
    demotion (now conflict_ratio 0.889, weakens=4) again HELD: the new weakens
    are the same substrate-limited closure-control-plane cohort, not
    falsifications. Routed to /implement-substrate (wire env sequence_complete ->
    emit_closure + de-commitment hold + train rule_bias_head); substrate_queue
    SD-034 amended with both *c failure records.
  location: evidence/planning/sd033_governance_plan.md
- id: SD-035
  title: "Amygdala analogue as BLA + CeA salience stage between z_harm_a and the cingulate/hippocampus — two peer non-trainable arithmetic modules owning the MECH-046 and MECH-074a-d behaviours."
  claim_type: design_decision
  subject: amygdala.analog_substrate
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_501_sd035_amygdala_analog_vs_binary_20260429T192730Z_v3
      as_of: 2026-04-29
      verdict: supports/PASS
  implementation_phase: v3
  v3_pending: false  # cleared 2026-04-22: V3-EXQ-473 + V3-EXQ-474 substrate-readiness PASS x2 (supports); promoted to provisional 2026-04-22; MECH-074b retrieval_bias still pending follow-up EXQ. Promoted provisional -> stable 2026-04-24 (governance): 3 supporting lit entries (Everitt 2003, Ciocchi 2010, Parkinson 2000), lit_conf 0.828.
  claim_level: architectural
  registered_utc: 2026-04-21
  depends_on:
    - SD-011           # affective harm stream z_harm_a (input)
    - SD-032a          # SalienceCoordinator (target of mode prior and fast_prime)
    - ARC-005          # latent stack / L-space substrate
    - ARC-007          # hippocampal map substrate
    - MECH-039         # salience gating substrate
    - MECH-046         # CeA mode prior (child claim)
    - MECH-074         # BLA/CeA read/write head (parent of 074a-d)
  child_claims:
    - MECH-046
    - MECH-074
    - MECH-074a
    - MECH-074b
    - MECH-074c
    - MECH-074d
  what_would_answer: |
    SD-035 already carries its own falsifiers, previously embedded in
    functional_restatement rather than this field -- extracted here, not
    reinvented:

      EXQ-A (CeA mode-prior ablation): CeA ON must beat OFF on mode-switch
      LATENCY, AND a zeroed-prior control must match OFF.
      EXQ-B (BLA encoding + remap): the gain condition must improve
      threat-context RECALL in the live loop without harming neutral recall,
      AND remap_signal must fire in the expected window.
      Secondary/backward-compat: with use_amygdala_analog=False, an existing
      EXQ must reproduce bit-identical metrics.

    STATUS: this falsifier is ALREADY LARGELY ANSWERED, not untested --
    read this claim's current evidence before proposing new experiments.

    ANSWERED (do not re-run without new reason):
      - Backward-compat / bit-identical-OFF: V3-EXQ-473 + V3-EXQ-474, each
        c1_backward_compat_entropy_delta = 0.0 across 3 seeds; now also
        contract-guarded (test_flag_inertness.py probe F-P1,
        test_feature_flag_boot_matrix.py tpj_and_amygdala row).
      - Substrate-readiness / mechanism presence: V3-EXQ-473 (CeA fires on
        threat, mode_prior/fast_prime reach the SalienceCoordinator, bound
        respected) and V3-EXQ-474 (BLA inverted-U encoding-gain curve,
        attribution-gated remap, post-event window monotonicity).
      - Behavioral discrimination that the analog form is load-bearing:
        V3-EXQ-501 (2026-04-29), analog beat binary sign-only 3/3 seeds on
        approach/avoid smoothness, intermediate-valence discrimination, and
        mode-transition lag.
      - Live-loop encoding-gain follow-up owed by 474's live-loop gap:
        V3-EXQ-659 (2026-06-09), PASS/supports for MECH-074a.
      - Since the 2026-07-10 governance refresh, two further genuine PASSes
        have landed on the same cluster (not yet folded into this claim's
        evidence_quality_note as of this digestion pass): V3-EXQ-762
        (2026-07-14, reviewed 2026-07-15) -- first genuine MECH-046 support,
        CeA mode_prior present/context-varying/monotone/bounded across
        graded threat contexts; and V3-EXQ-888 (2026-08-04, STILL PENDING
        REVIEW as of this digestion pass) -- first genuine MECH-074b
        support, address-route vs strength-route separability.

    RESIDUAL, NOT YET ANSWERED (the literal-wording gap, honestly named):
      None of the seven runs above -- nor anything queued since -- executes
      either falsifier as literally worded. Specifically missing:
        (i) A direct CeA-ON-vs-OFF comparison on MODE-SWITCH LATENCY (501
            substituted an ANALOG-vs-BINARY contrast instead), plus the
            paired zeroed-prior control matching OFF.
        (ii) A single LIVE-LOOP (in-environment, not replay-buffer/synthetic)
             threat-context RECALL run under the BLA gain condition,
             contrasted against neutral recall. 474's C2-C5 were synthetic
             BLAAnalog.tick() unit tests (env.step 5-tuple unpacking bug,
             bla_n_ticks=0); 659 and 888 are replay/AOR-buffer measures of
             encoding-gain and retrieval-bias, not an in-loop recall task.

    NON-DEGENERACY NOTE if either rerun is ever queued: SD-032b's dACC FAIL
    context (v3_exq_445, affective PE path underperforming) means EXQ-A/B
    acceptance criteria must be calibrated against that -- don't expect a
    clean mode-prior-ablation signal if upstream PE is broken; a null result
    could reflect the PE ceiling rather than falsify SD-035.

    ANSWER "SD-035 substrate as specified" (current reading, stable) if the
    residual gap above is judged proxy-adequate -- accumulated genuine
    evidence (7 PASS runs total, spanning substrate-readiness,
    discrimination, and two independent live encoding/retrieval mechanisms)
    covers the scientific intent even though the two literal EXQ-A/EXQ-B
    wordings were never executed verbatim.

    ANSWER "SD-035 falsified on the letter of its own registration" only if
    a literal ON-vs-OFF mode-switch-latency run, or a literal live-loop
    threat-vs-neutral recall run, is executed and fails -- this is the only
    outcome that would move this claim off its current PASS trajectory
    rather than merely closing a documentation gap.

    GOVERNANCE FLAG (2026-08-07 digestion pass, not resolved here): stance
    is still "believed" despite status:stable and 6 landed + 1 pending PASS
    runs -- likely the exp_conf threshold discounting the proxy gap above.
    Worth a governance call on whether to close the literal-wording gap with
    a small targeted rerun, or reassess for "shown" given accumulated proxy
    coverage. evidence_quality_note is two runs stale (762, 888) as of this
    pass.
  functional_restatement: >
    SD-035 registers a two-module amygdala analogue in ree-v3 --
    BLAAnalog (basolateral) and CeAAnalog (central) as peer modules,
    mirroring the biological division -- as the missing upstream stage
    between AffectiveHarmEncoder's z_harm_a and the downstream
    cingulate cluster (SD-032a-e). The cingulate is downstream; the
    amygdala is upstream and was not previously implemented.

    Module split and responsibilities:

    - BLAAnalog (ree-v3/ree_core/amygdala/bla.py, new). Non-trainable
      arithmetic. Exposes tick(z_harm_a, z_harm_a_pred,
      hippocampal_context) -> BLAOutput with fields:
        * encoding_gain (scalar) -- MECH-074a
        * retrieval_bias (per-trace weight vector) -- MECH-074b
        * remap_signal (per-code binary with attribution) -- MECH-074d

    - CeAAnalog (ree-v3/ree_core/amygdala/cea.py, new). Non-trainable
      arithmetic. Exposes tick(z_harm_a, cue_features=None,
      escapability_hint=None) -> CeAOutput with fields:
        * mode_prior (vector over operating modes; pre-softmax additive
          log-odds into SalienceCoordinator) -- MECH-046
        * fast_prime (scalar action/candidate prior; distinct fast
          subcortical pulse) -- MECH-074c
      escapability_hint is a Q-036 placeholder so MECH-219 can wire in
      later without refactor.

    What SD-035 does NOT do:
      - Does not produce z_harm_a (AffectiveHarmEncoder in
        ree_core/latent/stack.py stays; amygdala consumes).
      - Does not replace AIC urgency (SD-032c). CeA mode_prior is a
        peer input to SalienceCoordinator, not a substitute.
      - Does not modify the urgency interrupt at
        ree-v3/ree_core/agent.py:869-886 this pass. Migration into
        CeAAnalog.fast_prime is a deliberate second pass once MECH-074c
        is validated.
      - Does not add trainable components. Learnable fear-conditioning
        head is deferred (second pass). The MECH-074d attribution head
        is threshold-based initially.

    Config surface. BLAConfig and CeAConfig live in
    ree-v3/ree_core/utils/config.py, gated by a master switch
    use_amygdala_analog=False (both modules default to no-op). Defaults
    seeded from evidence/literature/targeted_review_amygdala_analog/synthesis.md.

    Data flow. In agent.sense(), after z_harm_a is produced by
    AffectiveHarmEncoder, BLAAnalog.tick and CeAAnalog.tick run; their
    outputs are attached to LatentState and consumed by:
      - SalienceCoordinator.tick (CeA mode_prior, fast_prime --
        pre-softmax additive bias on harm channel)
      - HippocampalModule write path (BLA encoding_gain multiplier;
        BLA remap_signal on update hook)
      - E3.select (BLA retrieval_bias reweights candidate proposals)

    Falsifiable (primary): EXQ-A (CeA mode-prior ablation) and EXQ-B
    (BLA encoding + remap) are the primary falsifiers. EXQ-A requires
    CeA ON to beat OFF on mode-switch latency AND zeroed-prior control
    to match OFF. EXQ-B requires gain condition to improve threat-
    context recall without harming neutral recall AND remap_signal to
    fire in the expected window.

    Falsifiable (secondary): backward-compat smoke test -- with
    use_amygdala_analog=False, an existing EXQ (e.g. baseline
    CausalGridWorld run) must produce bit-identical metrics.
  notes: >
    Registered 2026-04-21 as part of the amygdala analogue implementation
    plan (.claude/plans/amygdala-there-is-no-concurrent-corbato.md).
    Mirrors the SD-032 cingulate cluster pattern: a parent SD (SD-035)
    with child MECH claims (MECH-046, MECH-074a-d) and peer modules for
    biologically-distinct subdivisions.

    Biology-before-formal-definitions rule. This SD was registered only
    after the lit-pull at evidence/literature/targeted_review_amygdala_analog/
    (synthesis.md) extracted quantitative defaults from Roozendaal 2011,
    Mendez-Bertolo 2016, Pessoa & Adolphs 2010, LaBar & Cabeza 2006,
    Nader 2000, and Moita 2004. Defaults feed BLAConfig and CeAConfig.

    Q-036 escapability placeholder. CeAAnalog carries an
    escapability_hint parameter as a no-op placeholder so MECH-219 can
    wire in later without refactor. Not a blocker for MECH-046/074a-d.

    SD-032b dACC FAIL context. v3_exq_445 shows affective PE path
    currently underperforming. EXQ-A/B acceptance criteria must be
    calibrated against this -- don't expect mode-prior ablation to
    produce clean signals if upstream PE is broken.
  evidence_quality_note: >
    Registered 2026-04-21 as candidate with v3_pending:true, seeded by
    eight literature entries (see
    evidence/literature/targeted_review_amygdala_analog/synthesis.md).
    The three original promotion criteria were (a) BLAAnalog + CeAAnalog
    land, (b) backward-compat smoke test bit-identical with flag OFF,
    (c) EXQ-A + EXQ-B pass acceptance.

    PROMOTION HISTORY (note refreshed 2026-07-10 after an SHP-5
    claims_doc_drift reconcile flagged this note as still reading its
    pre-implementation registration text while status=stable). Status
    is stable / v3_pending:false, and that is supported by the record:

    (a) LANDED. ree_core/amygdala/bla.py + cea.py, wired in
    ree_core/agent.py + utils/config.py behind use_amygdala_analog
    (default OFF), both peer modules present.

    (b) BIT-IDENTICAL-OFF verified. V3-EXQ-473 (CeA) and V3-EXQ-474 (BLA)
    each record c1_backward_compat_entropy_delta = 0.0 across 3 seeds
    (CEA/BLA_ON_QUIET vs _OFF_QUIET). Now also guarded by standing
    contracts in ree-v3: tests/test_flag_inertness.py (use_amygdala_analog
    + use_bla_analog in the PROBED set, probe F-P1) and
    tests/contracts/test_feature_flag_boot_matrix.py (tpj_and_amygdala row).

    (c) VALIDATION (satisfied, with one proxy caveat). Four reviewed
    PASS/supports runs (all in review_tracker.json):
      - V3-EXQ-473 sd035_cea_mode_prior (EXQ-A lineage): CeA fires on
        threat, mode_prior/fast_prime reach the SalienceCoordinator,
        |mode_prior| bound respected, MECH-094 simulation gate, tau
        decay/hold dynamics. Substrate-readiness level.
      - V3-EXQ-474 sd035_bla_encoding_remap (EXQ-B lineage): inverted-U
        encoding-gain curve, attribution-gated remap, post-event window
        monotonicity. Documented caveat: an env.step 5-tuple unpacking bug
        meant BLA was not exercised in the live loop (bla_n_ticks=0), so
        C2-C5 are synthetic BLAAnalog.tick() tests; the live-loop
        threat-context recall half was deferred to V3-EXQ-659.
      - V3-EXQ-501 sd035_amygdala_analog_vs_binary (2026-04-29,
        purpose=evidence): behavioural ANALOG vs BINARY_SIGN_ONLY
        discrimination, PASS x3 seeds on c1 approach/avoid smoothness
        (RMSE reduction ~0.31 >= 0.25), c2 intermediate-valence
        discrimination (MAE reduction ~0.58 >= 0.25), and c3
        mode-transition lag (+1 step). Covers the scientific substance of
        both falsifiers.
      - V3-EXQ-659 mech074a_bla_encoding_gain_replay_bias (2026-06-09):
        PASS/supports for child MECH-074a -- the full-loop BLA
        encoding-gain follow-up that 474 owed.
    Governance promotion provisional->stable 2026-04-24: 3 supporting lit
    entries (Everitt 2003, Ciocchi 2010, Parkinson 2000), lit_conf 0.828.

    PROXY CAVEAT (recorded, not blocking). Criteria (a)+(b) are met
    directly. Criterion (c) was met by substrate-readiness (473/474) plus
    a behavioural discriminative run (501) plus the child follow-up (659),
    rather than by two runs each hitting the literal EXQ-A/EXQ-B wording
    ("CeA ON beats OFF on mode-switch LATENCY AND zeroed-prior control
    matches OFF"; "gain improves threat-context RECALL without harming
    neutral recall in the live loop"). The ON-vs-OFF latency comparison
    (501 tested ANALOG-vs-BINARY instead) and a single live-loop recall
    run under EXQ-B were never executed as originally specified. A strict
    reviewer could re-demand those exact reruns; the 2026-07-10 review
    judged the scientific intent covered and left status=stable.
  location: docs/architecture/sd_035_amygdala_analog.md
  source:
    - docs/architecture/sd_035_amygdala_analog.md
    - evidence/literature/targeted_review_amygdala_analog/synthesis.md
    - evidence/literature/targeted_review_connectome_mech_046/
    - evidence/literature/targeted_review_connectome_mech_074/
- id: MECH-262
  title: "Rule-selective persistence in lateral-PFC-analog: stimulus-abstracted rule representations in SD-033a persist across simulated time steps without external drive, remain selective through distractor events, and emerge through training rather than being innate."
  claim_type: mechanism_hypothesis
  subject: pfc.lateral_pfc_rule_selective_persistence
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-262@2026-04-25T15:42:09.107841Z"
      as_of: 2026-04-25
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  epistemic_category: substrate_ceiling
  pending_retest_after_substrate: true
  claim_level: mechanistic
  registered_utc: 2026-04-19
  ceiling_decision: deferred
  ceiling_routing_note: "2026-06-19 substrate-ceiling-lifted triage (verdict b deliberate-defer): SD-033a lateral_pfc_analog (consumer) is implemented and PASSes distractor-resistance (484), but rule-SELECTIVE persistence is untestable until an upstream rule-creator emits DIFFERENTIATED (non-monomodal) rule_state. That producer is arc_062_rule_apprehension:GAP-B / behavioral_diversity_isolation:GAP-A (monostrategy collapse; V3-EXQ-598b C3 FAIL trainable_not_monomodal); latest GAP-B falsifier V3-EXQ-654f FAIL/non_contributory 2026-06-18, 654g successor in flight. Retest MECH-262 only after a GAP-B PASS yields differentiated rule states (acceptance ~598c). Do not requeue on SD-033a alone."
  depends_on:
    - SD-033a          # substrate this mechanism instantiates
    - MECH-261         # write-gate context within which persistence operates
    - MECH-116         # dlPFC LSTM working memory (adjacent mechanism)
  functional_restatement: >
    Mechanism implementing the three signatures that distinguish a
    genuine rule substrate from generic working memory:

      (1) STIMULUS-ABSTRACTED FORMAT. The representation encodes the
          rule that applies, not the specific stimulus the rule
          operates on. Generalisation signature: transfer to novel
          stimuli under the same rule produces similar representational
          activity. (Mansouri et al 2020.)

      (2) DISTRACTOR-RESISTANT PERSISTENCE. Rule-selective activity
          persists through intervening events (simulated distractor
          stimuli, internal_replay events, micro-quiescence cycles per
          MECH-092) without being overwritten. This is what makes
          MECH-261's write-suppression-during-replay gate coherent:
          there has to be a stable target for the gate to protect.
          (Miller & Cohen 2001 top-down-bias framing; Mansouri et al
          2020 delay persistence.)

      (3) TRAINING-DEPENDENT EMERGENCE. Rule-selective representations
          develop over many task exposures -- they are not innate.
          In ree-v3, this corresponds to slow-consolidation writes
          from hippocampal-cortical dialogue during offline_consolidation
          mode (connects to MECH-261 consolidation branch and to the
          Frankland & Bontempi 2005 systems-consolidation framework
          grounded in the 2026-04-19 systems-consolidation lit-pull).

    Falsifiable: if a lateral-PFC-analog implementation shows
    stimulus-bound persistence (fires only for trained stimuli, fails
    to generalise) or distractor-fragile persistence (representation
    collapses when internal_replay events intervene), MECH-262 is
    violated at source and the SD-033a substrate does not meet its
    functional spec.
  notes: >
    Directly grounds SD-033a in the primate single-unit literature
    (Mansouri et al 2020 Nat Rev Neurosci synthesis). Provides three
    concrete measurable signatures for V3 implementation verification.

    The third signature (training-dependent emergence) ties SD-033a
    to the systems-consolidation mechanism already grounded by the
    Frankland & Bontempi 2005 + Peyrache et al 2009 entries -- this
    is how rules get consolidated into the lateral-PFC substrate
    during offline phases.
  evidence_quality_note: >
    Registered pre-implementation. Grounded by Mansouri et al 2020
    (NRN, confidence 0.85 in targeted_review_pfc_subdivision_architecture).
    2026-05-29 governance: V3-EXQ-598b (GAP-D trainable rule_bias_head
    ablation) showed C1 frozen_silent PASS + C2 trainable_nonzero PASS
    (head moves to mean abs 0.05-0.10) but C3 trainable_not_monomodal
    FAIL (P2 reef-visit fractions equivalent across ARM_0 frozen
    [0.10, 0.06, 0.0] and ARM_1 trainable [0.10, 0.03, 0.0]).
    Reclassified epistemic_category=substrate_ceiling with
    pending_retest_after_substrate=true: the SD-033a substrate fires
    and the rule_bias_head trains, but rule-selective persistence
    cannot be tested in isolation because the upstream rule-creator /
    discriminator that would populate distinct rule states is not yet
    wired (parallel to the 543l autopsy substrate-enrichment-first
    reading; MECH-309 monomodal collapse is the active failure mode).
    V3-EXQ-598b therefore registers as MECH-262=non_contributory rather
    than weakens. Retest after rule-creator substrate lands.

    2026-08-18 (governance apply, SEPARATE FINDING carried from confirmed
    failure_autopsy_V3-EXQ-874b_2026-08-17, which records it on MECH-262 as well as on
    MECH-467): in the 9 V3-EXQ-874b cells with a live z_goal, storage-site rule drift
    (0.004-0.384) and selection-path operative-rule fidelity (0.985-1.000) DISSOCIATE in 6
    of 9 cells -- so a storage-site rule read does NOT track the operative rule on this
    substrate. No status, direction or category change: MECH-262 stays candidate /
    substrate_ceiling with pending_retest_after_substrate true. Recorded so that a future
    rule-read instrument is not built on the assumption that the two agree.
  location: evidence/literature/targeted_review_pfc_subdivision_architecture/entries/2026-04-19_pfc_cluster_abstract_rules_mansouri2020/summary.md
- id: MECH-263
  title: "OFC-analog state-space and specific-outcome representation: SD-033b represents both a structured cognitive map of the current task state space and the specific outcomes expected from actions in each state. Value signals ride on top of this structured representation rather than being stored in it directly."
  claim_type: mechanism_hypothesis
  subject: pfc.ofc_state_space_specific_outcome
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-732_2026-07-10
      as_of: 2026-07-10
      verdict: non_contributory/precondition_unmet
  implementation_phase: v3
  v3_pending: true
  pending_retest_after_substrate: true
  epistemic_category: substrate_conditional
  claim_level: mechanistic
  registered_utc: 2026-04-19
  depends_on:
    - SD-033b          # substrate this mechanism instantiates
    - MECH-261         # write-gate context for outcome/model updates
  functional_restatement: >
    Mechanism implementing the OFC-analog's representational content.
    Two complementary framings, treated as co-present rather than as
    alternatives:

      (i) SPECIFIC-OUTCOME PREDICTION (Rudebeck & Murray 2014). Given
          a state s and an action a, SD-033b produces a prediction
          of the specific outcome -- its identity, modality, and
          expected magnitude -- not just a scalar value estimate.
          Devaluation sensitivity: if the value of outcome o changes
          while the state-action -> outcome mapping does not, behaviour
          changes appropriately because the outcome representation
          itself is structured.

      (ii) COGNITIVE MAP OF TASK STRUCTURE (Stalnaker/Cooch/Schoenbaum
          2015). SD-033b carries a representation of what states the
          agent can be in, what transitions are available, and what
          structural roles each state plays in the current task --
          including discriminating between states that differ only
          in their latent structural role (same sensory input, same
          reward, different position in the task).

    These framings are compatible: the cognitive map carries structured
    outcome predictions, and specific-outcome queries are resolved
    against it. Together they define what SD-033b is for, and what
    separates it from the scalar-value substrate SD-033c.

    Falsifiable: if SD-033b activity encodes scalar value without
    discriminating between same-value / different-outcome-identity
    states, or fails the devaluation-sensitivity test, MECH-263 is
    violated and SD-033b is functionally indistinguishable from
    SD-033c -- in which case the OFC/vmPFC dissociation does not
    carry at REE's level of description and the subdivision should
    be collapsed.
  notes: >
    Grounded jointly by Rudebeck & Murray 2014 (Neuron, OFC-as-oracle,
    selective-lesion evidence) and Stalnaker et al 2015 (Nat Neurosci,
    OFC-as-cognitive-map, explicitly critical of pure value reading).
    Treating these as co-present framings of the same substrate rather
    than as alternatives matches REE's model-based architecture: E2
    queries a structured task model for specific-outcome predictions.
  implementation_note: >
    2026-06-22 SD-033b GAP-8 devaluation-head DECOUPLE landed (ree-v3
    ree_core/pfc/ofc_analog.py devaluation_bias_head; failure_autopsy_V3-EXQ-485l). The
    decouple supplies the supra-floor differentiated DEVALUED re-ranking range the
    MECH-263 devaluation-sensitivity behavioural arm needs to feed the MECH-449 No-Go --
    485k saturated / 485l undershot the shared +/-ofc_bias_scale clamp; the separate head
    with its own +/-devaluation_bias_scale clamp clears the floor without trading the C2
    discrimination range. PROMOTES NOTHING (MECH-263 stays candidate / substrate_conditional
    / pending_retest_after_substrate; the sig-a devaluation half is non_contributory pending
    the gated 485-lineage retest, sig-b discrimination half remains positively shown at the
    representation level). See ree-v3/CLAUDE.md "SD-033b GAP-8 DECOUPLE".
  evidence_quality_note: >
    2026-06-22 (governance-apply, failure_autopsy_V3-EXQ-485m CONFIRMED -> non_contributory; claim
    UNWEAKENED): V3-EXQ-485m (ree-cloud-4) supersedes 485l; FAIL/non_contributory/non_degenerate on
    the DECOUPLED devaluation_bias_head (ree-v3 758956f). The decouple FIXED 485l clamp-starvation:
    C1 devaluation behavioural shift now 3/3, No-Go engages 2/3, devalued bias range supra-floor 3/3
    -- mechanism EXERCISED not falsified -- but C1b vector-inversion 1/3 (shift-without-inversion,
    cosine +0.72/+0.98 on 2/3 seeds) and C2 committed-class separation 1/3 still fail. Ruling out the
    magnitude artifact, the valuation face does not convert in ISOLATION. 3rd convergent
    fails-C2-alone datum (654i demotion, 654j Go/No-Go, 485m OFC): conversion is emergent from the
    assembled stack, not any single selection-face lever. P3-ofc face-validated (user gate
    2026-06-22) -> folds into conversion_ceiling_campaign:FULLSTACK co-armed arm (use_ofc_devaluation_
    head ON). Re-derive brake FIRED (11th substrate_ceiling reading on SD-033b/MECH-263): a 485n
    isolated valuation-face re-queue is REFUSED; next test is the co-armed full-stack arm. Status
    unchanged (substrate_conditional / pending_retest_after_substrate). PROMOTES NOTHING.

    2026-06-22 (governance-apply, failure_autopsy_V3-EXQ-485l CONFIRMED -> non_contributory; claim
    UNWEAKENED): V3-EXQ-485l (2026-06-22, ree-cloud-1) supersedes 485k; FAIL/non_contributory/
    non_degenerate:false (no governance weight), self-route substrate_not_ready_requeue CONFIRMED +
    re-localised by failure_autopsy_V3-EXQ-485l_2026-06-22. MECH-449 (Go/No-Go constitution) is BUILT
    + validated (689g PASS 3/3, promoted candidate->provisional) so the 485k 'build MECH-449' route is
    DISCHARGED; 485l shows the residual is STRUCTURAL SCALE -- the single shared OFC head under the
    +/-0.5 ofc_bias_scale clamp cannot carry a devalued re-ranking magnitude above the 0.05 readout
    floor AND the No-Go viability trigger while preserving C2 (485k gain 4.0 saturated -> range 0.0;
    485l gain 1.5 undershot -> 0.031 < 0.05). The bias VECTOR inverts (l2 1.83, cosine -0.716, C1b
    PASS 2/3) -- correct direction, clamp-starved magnitude -> No-Go engaged 1/3 (< 2/3 gate).
    MECH-449 correct-but-non-engaging = positive evidence it needs a supra-floor valuation input, NOT
    a weakens. MECH-263 / SD-033b devaluation half: non_contributory / substrate_conditional /
    pending_retest_after_substrate, blocked on a NEW substrate -- OFC devaluation-head clamp rescale /
    head decoupling (amend f_dominance_conversion_ceiling). RE-DERIVE BRAKE FIRED (10th
    non_contributory autopsy on SD-033b/MECH-263): a plain 485m gain-tweak is REFUSED; route to
    /implement-substrate. MECH-448/MECH-449 NOT weakened. PROMOTES NOTHING.

    2026-06-22 (governance-apply, failure_autopsy_V3-EXQ-485k CONFIRMED -> non_contributory; claim
    UNWEAKENED): V3-EXQ-485k (supersedes 485j; SD-033b/MECH-263 devalued-rerank behavioural on the
    MECH-448 demotion-enabled selector) FAILed non_contributory / non_degenerate:false. Both DVs
    vacuous -- readiness 1/3 (aggregate met=True panel reports max-over-seeds, masking a per-seed
    2-of-3 collapse; only seed 0 cleared the devalued floor), FIX-1 re-rank driver overshoots the
    +/-0.5 OFC bias clamp -> saturation -> readable devalued range exactly 0.0 on 2/3, and regressed
    485j's C2 conversion. Diagnosis: demotion-alone (MECH-448) cannot express the active No-Go
    withdrawal behavioural outcome-devaluation (sig-a) requires; the discrimination signature (sig-b)
    is demotion-tractable (converted 485j) but devaluation is blocked on the MECH-449 Go/No-Go
    constitution. The 485e->k six-autopsy lineage clears the ARC-107 demotion-alone-insufficient build
    gate. MECH-263 sig-a / SD-033b devaluation -> substrate_ceiling / pending_retest_after_substrate
    (behind MECH-449, built 2026-06-21; corrected arm V3-EXQ-485l gated behind the build + 689e).
    PROMOTES NOTHING.

    2026-06-19 (governance-apply, failure_autopsy_V3-EXQ-485h CONFIRMED -> non_contributory):
    V3-EXQ-485h (supersedes 485g; TERMINUS of the 485 behavioural lineage) is the 485g-designed
    T-vs-F disambiguator of the MECH-263 devaluation/discrimination signatures. Readiness MET 3/3
    (bias range 0.50 high-threat >= 0.05; head weight-delta 5.63), C3 silence-control PASS 3/3, but
    C1 1/3 + C2 0/3. DISAMBIGUATION RESOLVED: (T) threat-invariant-bias REFUTED (range collapses at
    the devalued state, ratio 0.12, 3/3 -- the head IS genuinely outcome-value-conditioned, the
    MECH-263 devaluation signature is positively shown at the REPRESENTATION level); (F) MECH-439
    F-dominance conversion ceiling IMPLICATED (OFC bias reaches E3 authority range 0.50 with ZERO
    committed conversion; proven 569i top-k F-bypass absent from this loop). Only behavioural
    conversion fails, at the shared F-dominated selector. non_contributory (NOT a weakens): faithful
    translation, not given a fair conversion test; SOLE genuine behavioural entry. Manifest weakens ->
    non_contributory (flat + nested pack); index rebuilt. 485g -> superseded; substrate_queue SD-033b
    failure_record += 485h. epistemic_category substrate_ceiling + pending_retest UNCHANGED. Deferred
    retest V3-EXQ-485i routes the OFC bias through the 569i top-k channel after the F-rebalance.
    2026-06-19 (governance-apply, failure_autopsy_V3-EXQ-485g CONFIRMED ->
    epistemic_category substrate_ceiling): V3-EXQ-485g is the FIRST non-vacuous
    trained-OFC-head behavioural test of the MECH-263 devaluation/discrimination
    signatures: readiness MET (bias range 0.171 >= 0.05, 2/3 seeds; head trained,
    delta 6.32), C3 control PASS, but C1 devaluation {0.001,0.0,0.010} << 0.05 (0/3)
    + C2 between-context TV ~0 (degenerate, 0/3) -- genuine 0.17 bias range, ZERO
    behavioural conversion. substrate_ceiling / non_contributory, NOT a weakens:
    faithful translation (lit_conf 0.863) not given a fair conversion test (zero
    anti-range driver gradient at low threat; devalued-state bias range -- the
    decisive conversion-vs-conditioning disambiguator -- unrecorded). The manifest
    F-dominance self-route is an analogy, NOT measurable in the isolated-OFC-softmax
    loop (selection authority OFF). Routed V3-EXQ-485h disambiguating redesign. Sole
    genuine experimental entry for MECH-263 -- do not define exp_conf.
    2026-06-11 (governance cycle #4, failure_autopsy_V3-EXQ-485e CONFIRMED):
    V3-EXQ-485e (trained-OFC-head behavioural) FAILed non_contributory via its
    same-statistic non-vacuity gate (readiness_met=false; cross-candidate bias
    RANGE cleared the floor on only 1/3 seeds) -- NOT a weakens against MECH-263.
    The head trained on all 3 seeds, but two gaps starve the cross-candidate range
    every behavioural DV (devaluation shift, task-role separation) requires:
    (1) OFC compute_bias clamp-saturation on 2/3 seeds; (2) candidate-bank z_world
    collapse (reconstruction-trained e2.world_forward; ARC-065 GAP-A / SD-056), so
    even the one unsaturated+READY seed gave a vacuous deval_shift (8e-05 vs 0.05)
    + a degenerate C2 ratio (criteria_non_degenerate.C2=false). OFC-head instance
    of the candidate-differentiated-affective-gradients pattern (569f/661/654a/643/
    614e). epistemic_category for this entry = substrate_ceiling;
    pending_retest_after_substrate set. Retest = V3-EXQ-485f. substrate_queue:
    amend modulatory-bias-selection-authority (ARC-065 GAP-A) with 485e as a
    corroborating failure record. MECH-263 stays candidate / v3_pending. The 485b
    (devaluation) + 485c (task-role) representation-level supports on the frozen
    head are unaffected.

    Registered pre-implementation. Aggregate support from two
    complementary entries in targeted_review_pfc_subdivision_architecture
    (Rudebeck & Murray confidence 0.83; Stalnaker et al confidence 0.80).
    Specific-outcome oracle path landed 2026-05-04 (oracle substrate only;
    behavioural signatures deferred): OFCAnalog.query_outcome() delegates
    to E2HarmSForward via OFCConfig.use_outcome_oracle (default False);
    bit-identical OFF. V3-EXQ-485a queued to confirm round-trip fidelity
    (UC6 sub-tests A-E). This fulfills the structural prerequisite for
    MECH-263 devaluation sensitivity and task-role discrimination; those
    behavioural signatures still require env-extension EXQs (outcome
    relabelling, task-distinct state pairs). v3_pending remains true pending
    behavioural evidence.

    [2026-06-12 governance / confirmed failure_autopsy_batch9_2026-06-12 applied]:
    V3-EXQ-485f (trained-OFC-head behavioural) reclassified weakens ->
    non_contributory (readiness-gate miscalibration). The readiness gate cleared by a
    hair (bias_range 0.00898) but ~50x below the 0.05 the devaluation/task-role DVs
    require, so the OFC bias that cleared readiness still had no authority to move the
    selection softmax (devaluation_shift 5e-5..2.6e-4) -- not a fair falsification.
    NOT a weakens against MECH-263. pending_retest_after_substrate retained; retest
    V3-EXQ-485g with the readiness floor aligned to the DV-significance floor. The
    485b/485c representation-level supports are unaffected. MECH-263 stays candidate /
    v3_pending.
  location: evidence/literature/targeted_review_pfc_subdivision_architecture/synthesis.md
- id: MECH-264
  title: "Frontopolar counterfactual-value tracking: SD-033e maintains, in parallel with SD-033c's chosen-option value signal, a running estimate of the value of unchosen alternatives. A switch-to-alternative decision is signalled when the counterfactual estimate exceeds the chosen estimate by a threshold-sensitive margin."
  claim_type: mechanism_hypothesis
  subject: pfc.frontopolar_counterfactual_value_tracking
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-264@2026-06-06T07:53:48.739107Z"
      as_of: 2026-06-06
      verdict: held_v4_by_architectural_commitment/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-04-19
  reclassification_note: >
    [2026-08-07 governance, phase-label-follows-dependency, user-directed]: implementation_phase
    v4 -> v3 (v3_pending stays true, unchanged). check_claim_phase_consistency.py flagged this
    claim as directly needed by MECH-487, a v3 build commitment. Standing rule: anything directly
    needed for a v3 build is marked v3 so v3 can complete. As the implementation_note below already
    documents, a V3-narrow geometric slice of THIS mechanism landed 2026-07-09 -- the full
    parallel-maintained multi-step substrate remains genuinely V4-gated (MECH-163 prerequisite
    unmet), which is exactly why v3_pending stays true even as the phase label moves.
  implementation_note: >
    V3-NARROW GEOMETRIC INSTANTIATION LANDED 2026-07-09 (user-directed; PROMOTES
    NOTHING, stays candidate / v3_pending true). A
    parameter-free geometric reading of the MECH-264 counterfactual value -- the
    goal-proximity ADVANTAGE of the best UNCHOSEN candidate over the committed
    endpoint in z_world space, ||z_chosen - z_goal|| - ||z_alt - z_goal|| -- was
    wired as the switch signal of the SD-033e V3-narrow de-commit lever
    (ree-v3 use_frontopolar_decommit; ree_core/pfc/frontopolar_analog.py
    compute_counterfactual_value). This is NOT the full MECH-264 substrate: the
    "running estimate maintained in parallel with SD-033c's chosen-option value"
    and the switch-margin over MULTI-STEP counterfactuals remain V4 (MECH-163
    multi-step planning prerequisite unmet; a 1-step counterfactual collapses to
    MECH-257). The V3 slice is a single-tick geometric advantage, used only as an
    entry-relative de-commit pressure on the natural-commit latch, not a maintained
    parallel value channel -- so v3_pending stays true. See SD-033e implementation_note.
  depends_on:
    - SD-033e          # primary substrate this mechanism lives in
    - SD-033c          # vmPFC-analog (chosen-option value); MECH-264 is the counterfactual sibling signal
    - MECH-151         # vmPFC value integration -- adjacent mechanism operating on chosen-option value
  functional_restatement: >
    SD-033e maintains, in parallel with SD-033c's chosen-option value
    signal, a running estimate of the value of unchosen alternatives.
    This counterfactual-value estimate dissociates from chosen-option
    value at the substrate level: SD-033c integrates value for the
    action currently committed to; SD-033e integrates value for the
    action(s) not committed to. Both signals coexist trial by trial.

    Switch logic. When the counterfactual-option value estimate exceeds
    the chosen-option value estimate by a threshold-sensitive margin,
    SD-033e signals a switch-to-alternative. Individual variation in
    the magnitude of this counterfactual signal predicts propensity to
    switch, consistent with Boorman et al 2009 Neuron (model-based
    fMRI; lateral frontopolar cortex tracks counterfactual-option
    value; individual variation predicts switch propensity).

    Distinct from MECH-151. MECH-151 (vmPFC value integration) operates
    on the chosen-option value stream in SD-033c. MECH-264 is the
    sibling computation over unchosen alternatives in SD-033e. Same
    form of computation, different substrate, different input stream.

    Falsifiable (primary): if lateral-FPC-analog activity in a V4
    implementation tracks chosen-option value rather than counterfactual-
    option value during simulated three-alternative decision tasks,
    MECH-264 fails at source and SD-033e collapses functionally into
    SD-033c. In that case the OFC/vmPFC/FPC value architecture is
    over-specified at REE's level of description and the frontopolar
    subdivision should be folded into the vmPFC-analog.
  notes: >
    Registered pre-implementation as part of the Prong D frontopolar
    lit-pull follow-up (2026-04-19,
    targeted_review_pfc_subdivision_architecture). Grounds one of the
    four converging frontopolar functions identified in the Prong D
    synthesis (counterfactual value tracking). Paired with MECH-265
    (relative-importance monitoring across >2 goals), which covers a
    distinct over-all-goals signal rather than pairwise chosen-vs-
    counterfactual.

    V3 implication: do NOT implement. V4-scope mechanism. The
    operating_mode vocabulary reserved in MECH-261 already accommodates
    the future parallel_goal_deliberation mode name under which this
    mechanism will run.
  evidence_quality_note: >
    Registered pre-implementation (V4 scope). Literature grounding:
    Boorman et al 2009 Neuron as primary (model-based fMRI, lateral
    FPC tracks counterfactual option value; individual variation
    predicts switch propensity). Secondary support from Mansouri et al
    2015 PNAS (primate FPC lesions impair rapid learning of novel
    alternative rules) and Mansouri et al 2017 NRN synthesis
    (integrates counterfactual tracking as one of four frontopolar
    functions).
  location: evidence/literature/targeted_review_pfc_subdivision_architecture/synthesis.md
- id: MECH-265
  title: "Frontopolar relative-importance monitoring across competing goals: SD-033e maintains a representation of relative importance across a set of active goals (parallel, not pairwise), enabling parallel-goal monitoring and flexible switching. Lesion analogue should reproduce the Mansouri 2015 over-focus + novel-rule-impairment phenotype."
  claim_type: mechanism_hypothesis
  subject: pfc.frontopolar_relative_importance_monitoring
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-265@2026-06-06T07:53:48.739107Z"
      as_of: 2026-06-06
      verdict: held_v4_by_architectural_commitment/applied
  implementation_phase: v4
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-04-19
  phase_locked: true  # intrinsic V4 scope. SD-033e's own 2026-08-07 reclassification_note
                      # is explicit and self-limiting: "what MECH-487 actually depends on
                      # is the ALREADY-LANDED V3-narrow parameter-free geometric de-commit
                      # lever ... NOT the FULL learned-substrate scope ... (MECH-264
                      # counterfactual-value head + MECH-265 monitor, prerequisites still
                      # genuinely V4-bound per MECH-163/CausalGridWorldV2)" -- SD-033e's
                      # v3 slice used ONLY functions 1+4/MECH-264, never MECH-265
                      # ("MECH-265 relative-importance is structurally V4-blocked
                      # (single-resource env, no K>=2 goals)", SD-033e's own
                      # implementation_note). Set 2026-08-10 per chip
                      # chip-20260810-phase-consistency-reconciliation.
  depends_on:
    - SD-033e          # primary substrate this mechanism lives in
    - MECH-264         # related but distinct: MECH-264 is pairwise chosen-vs-counterfactual, MECH-265 is over-all-goals
    - MECH-261         # adjacent: MECH-261 gates writes ACROSS substrates, MECH-265 operates WITHIN SD-033e
  functional_restatement: >
    SD-033e maintains a representation of relative importance across a
    set of active goals -- a parallel signal, not a pairwise chosen-vs-
    counterfactual comparison. Distinguishes from MECH-264: where
    MECH-264 covers the pairwise chosen vs single-counterfactual value
    race, MECH-265 covers the monitoring of >=2 alternative goals
    simultaneously with a flexible switching policy across them.

    This is the load-bearing computation in the Mansouri et al 2017
    Nature Reviews Neuroscience integrative synthesis: frontopolar
    cortex monitors the relative importance of current vs alternative
    goals and, in humans specifically, supports parallel monitoring of
    multiple goals with flexible switching. It subsumes the Koechlin
    & Summerfield 2007 branching framing as one special case (where
    the alternatives reduce to two: current execution + one pending
    task).

    Lesion phenotype. The behavioural signature under ablation is
    specified by Mansouri et al 2015 PNAS: selective bilateral FPC
    lesions in macaques produce (a) paradoxical over-focus on the
    current task under distraction, and (b) impaired rapid learning of
    novel alternative rules. MECH-265 predicts the V4 FPC-analog
    ablation should reproduce both effects in simulation.

    Falsifiable (primary): if V4 FPC-analog ablation does NOT
    reproduce the over-focus + novel-rule-impairment phenotype in
    simulation, MECH-265 fails. Either the relative-importance-
    monitoring computation does not live in the frontopolar substrate
    in REE, or the substrate-level role is correct but the specific
    mechanism is mis-specified.

    Distinct from MECH-261. MECH-261 gates writes ACROSS substrates
    via the operating-mode vector. MECH-265 operates WITHIN SD-033e,
    computing a local ranking over goals. MECH-261's gateway-style
    external/internal switching is the meta-level context MECH-265
    runs inside.
  notes: >
    Registered pre-implementation as part of the Prong D frontopolar
    lit-pull follow-up (2026-04-19,
    targeted_review_pfc_subdivision_architecture). The "gateway
    hypothesis" substrate-level role (Burgess/Dumontheil/Gilbert 2007)
    is one of the functions this mechanism rides on -- the gateway
    defines when internal alternatives are being attended to, and
    MECH-265 specifies what happens to them when they are.

    Paired with MECH-264 (counterfactual value tracking). The two
    together ground the four-converging-functions framing in the
    Prong D synthesis: MECH-264 carries the Boorman counterfactual
    function, MECH-265 carries the Mansouri 2017 relative-importance
    function plus the Mansouri 2015 disengagement-for-exploration
    lesion signature.

    V3 implication: do NOT implement. V4-scope mechanism. The
    parallel_goal_deliberation mode name reserved in MECH-261
    accommodates it.
  evidence_quality_note: >
    Registered pre-implementation (V4 scope). Literature grounding:
    Mansouri et al 2017 Nature Reviews Neuroscience as load-bearing
    integrative synthesis (relative-importance monitoring; human-
    specific parallel-goal capacity). Primate lesion evidence from
    Mansouri et al 2015 PNAS (selective FPC lesions produce paradoxical
    over-focus under distraction + impaired rapid learning of novel
    alternative rules). Gateway-function support from Burgess/
    Dumontheil/Gilbert 2007 TiCS.
  location: evidence/literature/targeted_review_pfc_subdivision_architecture/synthesis.md
- id: MECH-432
  title: "dACC<->FPC reciprocal arbitration loop: the switch-to-alternative decision is not computed in one module but emerges from a comparison ACROSS two substrates -- the FPC-analog (SD-033e/MECH-264) carries the counterfactual value of the specific unchosen option, the dACC-analog (SD-032b) carries the environment search-value and expected-value-of-control, and slot-switching (SD-046) is gated by their relative comparison. Neither signal alone drives the switch; the loop does."
  claim_type: mechanism_hypothesis
  subject: pfc.dacc_fpc_reciprocal_arbitration_loop
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4
  claim_level: mechanistic
  registered_utc: 2026-06-13
  depends_on:
    - SD-033e          # FPC-analog host of the counterfactual-value side of the loop
    - MECH-264         # the per-option counterfactual value the FPC side supplies
    - SD-032b          # dACC-analog host of the search-value / EVC side of the loop
    - MECH-265         # relative-importance read consumed by the arbitration
    - SD-046           # multi-slot GoalState whose slot-switch the loop gates
  functional_restatement: >
    REE's deliberation roadmap treats counterfactual-value tracking
    (MECH-264, FPC-analog) and adaptive control / search-value (SD-032b,
    dACC-analog) as separate mechanisms. MECH-432 asserts they are
    co-constitutive: the switch-to-alternative is driven by a RECIPROCAL
    comparison between them, not by either in isolation. The FPC-analog
    supplies the value of the specific unchosen option (Boorman 2009);
    the dACC-analog supplies the average value of the environment and the
    cost/expected-value of switching effort (Kolling et al 2012 Science
    foraging-value; Shenhav et al 2013 Neuron expected-value-of-control).
    The slot-switch commits when the dACC search/control value exceeds the
    chosen-option value by a margin the FPC counterfactual estimate
    informs.

    This makes a structural prediction beyond MECH-264/MECH-265: lesioning
    EITHER substrate should impair switching in a DISSOCIABLE way -- remove
    the FPC side and the agent loses the specific-alternative value (cannot
    pick WHICH alternative); remove the dACC side and it loses the
    environment/search-value (cannot decide WHETHER to leave at all),
    reproducing the vmPFC/ACC reference-frame dissociation Kolling reports.

    Falsifiable (primary): if a single combined module computing both the
    counterfactual value and the search/control value reproduces adaptive
    slot-switching as well as the two-substrate loop in a V4 dual-goal
    arena, the reciprocal-loop claim is unnecessary and MECH-264 + SD-032b
    can be merged. Falsifiable (dissociation): if FPC-side and dACC-side
    ablations produce the SAME switching deficit rather than the
    which-vs-whether dissociation, the loop is mis-specified.
  notes: >
    Registered 2026-06-13 from the goal_deliberation_v4:GDL-8
    frontopolar-deliberation lit-pull completion-set harvest (proposal P1,
    user said "register all"). The dACC<->FPC loop is the co-constitutive
    partner structure the Kolling 2012 / Shenhav 2013 / Aston-Jones &
    Cohen 2005 triad describes -- counterfactual deliberation is a loop,
    not a module. substrate_conditional: testable only once the SD-046
    multi-slot substrate (GDL-1) and the SD-033e FPC-analog (GDL-2) exist;
    DO NOT build in V3. SD-032b is the live V3 dACC hook. The LC-NE gain
    signal that converts "the alternative looks better" into "actually
    disengage" is MECH-433 (the third strand of the same loop).
  evidence_quality_note: >
    Registered pre-implementation (V4 scope), literature-grounded only
    (PROMOTES NOTHING; experimental_confidence stays 0). Grounding:
    Kolling et al 2012 Science (vmPFC option-value vs ACC search-value
    dissociation); Shenhav et al 2013 Neuron (expected-value-of-control as
    dACC arbitration function); cross-link to the FPC counterfactual
    anchor Boorman et al 2009 (targeted_review_frontopolar_analog_prong_d).
    Known caveat: the Hayden/Shenhav debate on whether ACC "foraging
    value" partly reflects choice difficulty -- a V4 implementation must
    keep counterfactual-value and search-value terms architecturally
    separate to avoid reproducing that confound.
  location: evidence/literature/targeted_review_frontopolar_deliberation/
- id: MECH-433
  title: "LC-NE-analog explore/exploit gain control: a value-INDEPENDENT, broadly-projecting tonic/phasic gain signal gates DISENGAGEMENT from the committed course. Phasic gain stabilises current-task exploitation; a tonic-mode shift -- driven by waning task utility monitored in the dACC/value system -- licenses disengagement to explore alternatives, supplying the trigger that MECH-264's value-margin switch alone does not. REE has no NE-analog substrate today; this names the missing modulator."
  claim_type: mechanism_hypothesis
  subject: neuromod.lc_ne_explore_exploit_gain
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4
  claim_level: mechanistic
  registered_utc: 2026-06-13
  depends_on:
    - MECH-264         # the switch-to-alternative this gain signal licenses
    - SD-033e          # engage<->deliberate transition the gain mode-shift expresses
    - SD-032b          # dACC/value utility monitor that drives the LC mode transition
  functional_restatement: >
    MECH-264 frames switching as a value-margin threshold: switch when the
    counterfactual value beats the chosen value. MECH-433 adds the
    orthogonal trigger that the value-margin reading omits -- a
    neuromodulatory GAIN signal analogous to the locus-coeruleus
    norepinephrine (LC-NE) system (Aston-Jones & Cohen 2005 Annu Rev
    Neurosci). In phasic mode, gain is time-locked to task-decision
    outcomes and stabilises exploitation of the current task. When task
    utility wanes -- detected by the dACC/value system (SD-032b) -- a shift
    to tonic mode is associated with DISENGAGEMENT from the current task
    and a search for alternatives. The gain shift is what converts "the
    alternative looks better" (MECH-264) into "actually leave and explore."

    The signal is value-independent and broadly projecting (a global gain
    scalar, not a per-option value), which distinguishes it from MECH-264's
    option-specific value and from MECH-432's reciprocal value comparison.
    Its cost is the biologically attested trade-off: tonic (explore) mode
    degrades current-task performance, so the gain set-point governs how
    readily the agent disengages -- a parameter REE currently has no claim
    over.

    GENUINE SUBSTRATE GAP: REE-v3 has no NE-analog module. There is no
    tonic/phasic gain mode anywhere in the substrate today. MECH-433 names
    the missing modulator and reserves its interface; it is NOT a relabel
    of an existing knob (distinct from MECH-093 arousal-modulated E3, which
    is a precision scalar, not an explore/exploit disengagement controller;
    and from the SD-016/SD-037 broadcast machinery).

    Falsifiable (primary): if MECH-264's value-margin switch reproduces
    adaptive disengagement (leaving low-utility tasks at the right time)
    WITHOUT any separate gain signal in a V4 explore/exploit arena, the
    NE-analog is unnecessary. Falsifiable (set-point): if varying the gain
    set-point does NOT trade exploit-stability against explore-readiness in
    the predicted direction, the mechanism is mis-specified.
  notes: >
    Registered 2026-06-13 from the goal_deliberation_v4:GDL-8
    frontopolar-deliberation lit-pull completion-set harvest (proposal P2,
    user said "register all"). CROSS-LINK plasticity_neuromodulation: this
    is long-horizon neuromodulator territory; the explore/exploit gain
    controller is the disengagement-side complement to the closure-side
    machinery and should be sequenced with the plasticity-window
    neuromodulator work, NOT built standalone in V3. substrate_conditional:
    needs both a V4 NE-analog substrate AND the SD-033e/SD-046 deliberation
    layer to have anything to disengage between. The third strand of the
    MECH-432 dACC<->FPC loop (FPC value + dACC search-value + LC-NE gain).
  evidence_quality_note: >
    Registered pre-implementation (V4 scope), literature-grounded only
    (PROMOTES NOTHING; experimental_confidence stays 0). Grounding:
    Aston-Jones & Cohen 2005 Annu Rev Neurosci (LC-NE adaptive-gain theory;
    phasic=exploit, tonic=disengage/explore; ACC/OFC drive the mode
    transitions). Mapping caveat (logged in the lit entry): LC-NE is a
    diffuse gain system, not a value module, and REE has no NE-analog host
    -- this claim is the gap, not a confirmed mechanism.
  location: evidence/literature/targeted_review_frontopolar_deliberation/
- id: MECH-266
  title: "Asymmetric mode hysteresis in SalienceCoordinator: entering an operating mode uses a different threshold on the aggregate salience vector than exiting it, and the asymmetry is mode-specific. Schmitt-trigger extension of the existing SD-032a enter-threshold logic (currently enter-only / symmetric)."
  claim_type: mechanism_hypothesis
  subject: salience.asymmetric_mode_hysteresis
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-08-13
    needs_review: false
    evidence:
      from: "failure_autopsy_mech266-464e-467e-cluster_2026-08-13"
      as_of: "2026-08-13"
      verdict: "non_contributory (both legs) -- occupancy is now reachable (substrate advanced, not ceilinged) but the mode-governance-engagement gate over-corrects into bang-bang arbitration with no mixed regime; a corrupting-severity measurement defect in the readiness gate's non-vacuity check (min-across-arms) is masking the real state"
  implementation_phase: v3
  v3_pending: false
  epistemic_category: standard  # confirmed 2026-08-13 -- NOT substrate_ceiling; the substrate advanced (occupancy 0.0 -> 1.0), the residual failure is a measurement/gate defect
  pending_retest_after_substrate: true  # mode-governance-engagement amend owed (corrupting-severity gate fix; bang-bang arbitration needs a mixed-regime cap value or a per-arm/regime-conditioned readiness gate)
  governance_note_2026_06_13: >
    V3-EXQ-464c + 467c reclassified non_contributory (substrate_ceiling,
    pending_retest_after_substrate) per confirmed
    failure_autopsy_SD-034-closure-cluster-ext_2026-06-12: on the 603n foraging
    substrate fraction_in_external_task=0.0 (the contested mode is never
    occupied) and the n_switches non-vacuity gate passed vacuously
    (n_switches==n_episodes = per-episode settles into internal_planning), so the
    MECH-266 over-binding / hysteresis dose-response could not express. NOT a
    falsification. The demote_to_candidate recommendation (conflict_ratio 0.571,
    2 weakens) was HELD this cycle -- both weakens were these two cohort FAILs,
    now excluded from scoring. Retest is blocked on the new
    mode-governance-engagement substrate entry (drive genuine external_task
    occupancy + re-state the readiness gate as
    min_across_arms(fraction_in_external_task) > floor).
    SUBSTRATE LANDED 2026-06-13 (ree-v3 REEConfig.use_external_task_drive,
    no-op-default; the SalienceCoordinator now has an external_task salience
    SOURCE -- a committed-goal-pursuit engagement signal registered in both
    affinity_weights and salience_weights). PROMOTES NOTHING: MECH-266 stays
    provisional, pending_retest_after_substrate holds; retest = V3-EXQ-464d
    (competing-goals) + V3-EXQ-467d (mode-stickiness) with the occupancy-keyed
    readiness gate. See ree-v3/CLAUDE.md mode-governance-engagement section.
  governance_note_2026_06_13b: >
    PM cycle (gov 2026-06-13T20:04Z): the *d retests ran on the landed substrate and
    STILL self-routed substrate_not_ready_requeue -- V3-EXQ-464d + V3-EXQ-467d both
    non_contributory (external_task occupancy not reachable across the sweep; the
    external_task drive does not yet produce occupancy because the Stage-H nav-competence
    dependency is unresolved). No weight against MECH-266 / SD-032a. The provisional->candidate
    demotion is DEFERRED again (user 2026-06-13): conflict-resolution remains substrate-blocked
    (Stage-H nav competence), so neither demote-now nor hold-and-resolve can make progress this
    cycle. Re-evaluate once external_task occupancy is reachable.
  governance_note_2026_08_13: >
    [confirmed failure_autopsy_mech266-464e-467e-cluster_2026-08-13, V3-EXQ-467e + V3-EXQ-464e]:
    the 467d/464d Stage-H nav-competence blockers ARE fixed -- external_task occupancy is now
    reachable (1.0 at r=0.10 on every seed vs 0.0 everywhere in 467b/c/d; sticky arm reaches 1.0
    with zero switches in 464e vs 0.0 everywhere in 464c). The substrate ADVANCED, it did not
    ceiling. But AFFINITY_INPUT_CAP=2.0 over-corrects into bang-bang arbitration: a step function
    (occupancy ~1.0 at r=0.10, exactly 0.0 at every r>=0.50; n_switches==n_episodes invariant
    across a 20x rail sweep in 467e; sticky arm 1.0/zero-switches vs symmetric arm 0.0 on 2/3
    seeds in 464e). Both results are non_contributory, not falsifying, but for a THIRD reason
    beyond unreachability: the readiness gate's min-across-arms non-vacuity check is itself
    corrupting -- it is anti-correlated with C1's own predicted effect (stronger hysteresis drives
    occupancy to 0 at high rails, failing the gate), conflates "mode unreachable" with "mode
    saturated," and emits a factually-false route_reason ("external_task_mode_not_occupied") when
    the mode is 100% occupied at r=0.10. 464e's own criteria pass 3/3 but vacuously
    (criteria_non_degenerate false -- rail saturation, not graded hysteresis). Severity=corrupting
    on ree_core/cingulate/salience_coordinator.py + ree_core/utils/config.py: a new experiment
    inheriting this gate pattern would get a wrong-but-plausible-looking non_contributory verdict.
    Partially resolves prior failure_record items (467c, 464c -- the zero-occupancy half only, the
    switching half is NOT resolved) and the sd_salience_contested_mode_occupancy entry's own
    "occupancy always 0.0" assertion (now false), which should stay open for its OTHER consumers
    (MECH-048, MECH-259, ARC-005 routing double-dissociation) since bang-bang arbitration does not
    satisfy their >=2-modes-occupied precondition either. Next work: (a) determine whether a mixed
    regime exists at any cap value (H1) or the discrete argmax register is structurally bang-bang
    (H2), and (b) restate the readiness gate per-arm/regime-conditioned rather than
    min-across-a-swept-range. Status unchanged (provisional); the provisional->candidate demotion
    question stays open, now on cleaner (non-substrate-blocked, non-vacuous-gate) footing once the
    gate fix lands.
  implementation_note: >
    IMPLEMENTED 2026-04-21 as a Schmitt-trigger extension of the
    MECH-259 symmetric switch_threshold in ree-v3/ree_core/cingulate/
    salience_coordinator.py. Two optional dict overrides on
    SalienceCoordinatorConfig: enter_thresholds[target_mode] (per-mode
    enter rail; falls back to switch_threshold when unset) and
    exit_thresholds[current_mode] (operating_mode[current_mode] must
    fall strictly below this value before a switch out is permitted;
    falls back to 1.0 sentinel = always satisfied for any proper
    softmax, preserving legacy MECH-259 behaviour). Trigger:
    salience_aggregate > enter_threshold * stability_mult AND
    operating_mode[current_mode] < exit_threshold AND soft_argmax !=
    current_mode. Setters: set_enter_threshold(mode, value),
    set_exit_threshold(mode, value), set_hysteresis_ratio(ratio)
    (uniform exit rail across registered modes for parametric sweeps).
    Tick return dict extended with enter_threshold, exit_threshold,
    current_mode_prob; effective_threshold retained as backward-compat
    alias. Backward compatible: empty dicts -> trigger reduces to
    legacy MECH-259. OCD over-binding axis: exit_thresholds[m] near 0
    (mode collapses to near-zero before leaving). Depression / under-
    binding axis: lower enter_threshold. Symmetric baseline: empty
    dicts. Validation PASSed 2026-04-21: V3-EXQ-464 (EXP-0160
    competing-goals, 5 sub-tests) and V3-EXQ-467 (EXP-0163 mode
    stickiness 5-arm parametric sweep r in [0.10, 0.50, 1.00, 1.50,
    2.00], 5 sub-tests). Behavioural switch-cost asymmetry +
    goal-completion dose-response runs deferred to EXQ-464b / EXQ-467b
    pending CausalGridWorldV2 dual simultaneously-active resource-cue
    extension. Lit-pull recommendation match (2026-04-27): independently
    tunable enter/exit thresholds confirmed; per-substrate mode tracking
    flagged for V4.
  claim_level: mechanistic
  registered_utc: 2026-04-20
  depends_on:
    - SD-032a          # SalienceCoordinator (the mode register whose hysteresis this augments)
    - MECH-259         # salience-network switch threshold (symmetric-threshold mechanism this extends)
    - SD-033           # governance cluster
  functional_restatement: >
    Mechanism hypothesis. SD-032a currently implements an enter-only
    threshold on the aggregate salience vector: once the signal crosses
    threshold_into_mode_X, operating_mode updates to X, and symmetrically
    requires re-crossing to change again. Biological BG direct/indirect
    pathway asymmetries and tonic-DA hysteresis (Cools 2008; Collins &
    Frank 2014) imply the biological gate is a Schmitt trigger with
    distinct enter and exit thresholds per mode, and the asymmetry
    magnitude is mode-dependent (e.g. leaving offline_consolidation on
    arousal spike is fast; leaving external_task on a single distraction
    is slow).

    MECH-266 asserts that SD-032a's mode register should be extended
    with per-mode (enter_threshold, exit_threshold) pairs, and that
    exit_threshold < enter_threshold for stable modes (classic Schmitt),
    with the asymmetry magnitude calibrated per mode. Under-binding
    (depression-axis) corresponds to enter_threshold > exit_threshold
    collapsing (too easy to leave any mode); over-binding (OCD /
    rigidity axis) corresponds to exit_threshold approaching zero
    (unable to leave the current mode even under pressure).

    Distinct from MECH-259. MECH-259 specifies that a threshold exists.
    MECH-266 specifies that the threshold is DIRECTIONAL and mode-
    specific, and that this asymmetry is the governance parameter that
    the OCD <-> depression axis varies along.

    Falsifiable (primary): V3-EXQ-464 (competing goals with symmetric
    vs asymmetric mode thresholds) should show that lateral inhibition
    of Go selection is insufficient to reproduce task-switching cost
    asymmetries without asymmetric-threshold modes.

    Falsifiable (secondary): V3-EXQ-467 (mode stickiness / Hold decay)
    should demonstrate that varying exit_threshold within a mode
    reproduces the clinical under-binding / over-binding phenotype
    continuum on behavioural markers.
  notes: >
    Registered 2026-04-20 as part of the SD-033 governance plan (see
    evidence/planning/sd033_governance_plan.md). Source: 2026-04-20_ocd2.md
    six-step mode-binding sketch, step 2 (asymmetric hysteresis).

    Extension, not replacement, of the SD-032a enter-only threshold
    logic. Implementation-wise: SalienceCoordinator keeps its existing
    threshold vector and gains a second exit-threshold dict; when
    current_mode == X, the salience signal must drop below exit_X, not
    below enter_X, to leave.
  evidence_quality_note: >
    Registered candidate with v3_pending:true. Biology lit-pull
    complete 2026-04-27, six entries in
    evidence/literature/targeted_review_connectome_mech_266/entries/:
    Fallon 2016 (asymmetric stability/flexibility, supports), O'Reilly
    2006 (BG gating substrate, supports), Cools 2019
    (stability/flexibility tradeoff, supports), Cools & D'Esposito
    2011 (inverted-U, supports), Cools 2008 (paradoxical
    baseline-dependent DA, supports 0.74), Collins & Frank 2014 OPAL
    (D1/D2 opponent, supports 0.76). Implementation recommendations:
    (1) keep enter and exit thresholds independently tunable rather
    than locking to a fixed gap (Cools 2008 inverted-U is smooth
    modulation, not fixed-gap Schmitt trigger); (2) the V3 single
    operating_mode register may need to elaborate into per-substrate
    mode tracking for V4 (Cools 2008 multi-DA-system dissociation
    across mesolimbic, mesocortical, nigrostriatal); (3) the V3
    SalienceCoordinator-level Schmitt-trigger gate inherits the
    asymmetric-pathway architectural commitment from OPAL D1/D2
    dynamics but does not explicitly model the dual-pathway substrate
    -- V4 elaborations may benefit from doing so. V3-EXQ-464
    (competing goals) and V3-EXQ-467 (mode stickiness) are the
    primary falsifiers queued in ree-v3/experiment_queue.json.
    2026-06-04 governance: behavioural successors V3-EXQ-464b/467b ran but
    were reclassified non_contributory + substrate_ceiling -- all arms sit
    at 100% external_task occupancy with 0 mode switches at every hysteresis
    ratio (degenerate no-switch baseline; no reachable goal -> no internal
    mode to switch into), same goal-pipeline ceiling scaffolded_sd054_onboarding
    one layer out from closure (cluster member of confirmed
    failure_autopsy_V3-EXQ-460b-461b-464b-466b_2026-06-04). Provisional->candidate
    demotion (conflict_ratio 0.571, weakens=2) HELD: both weakens were this
    substrate-limited cohort, now excluded from scoring.
    pending_retest_after_substrate set.
    2026-06-12 governance: *c successors V3-EXQ-464c (competing goals) + V3-EXQ-467c
    (mode stickiness) landed on the foraging-competent 603n substrate (contact
    guard now met) and self-route residual_*_open / weakens, but they are cohort
    siblings of the SD-034 closure-control-plane cluster (the commitment-latch has
    no behavioural authority on this substrate -- same theme as 460c/468c). Flagged
    for the /failure-autopsy cluster extension and left PENDING this cycle (no
    inline weakens applied). Provisional->candidate demotion (conflict_ratio 0.571)
    again HELD pending that autopsy + retest; not falsifications of MECH-266.

    [2026-08-16 governance apply | V3-EXQ-934 -> MECH-266 | failure_autopsy_V3-EXQ-934_2026-08-16] DISPOSITION: evidence_direction non_contributory. PERIPHERAL / not exercised in V3-EXQ-934: the MECH-266 manipulation arm (ARM_ASYM_STICKY_TASK sticky exit rail) sat at fraction_in_external_task = 1.0 with n_switches = 0 in all 15 cells (3 seeds x 5 caps) -- the intended manipulation, not a dose-response -- and the load-bearing criterion was evaluated on the symmetric arm, which contains no MECH-266 mechanism. The run establishes a measurement precondition for a future MECH-266 test and weighs nothing for or against the claim. No substrate_ceiling attribution accrues to MECH-266 from this run and its brake count is unchanged at 6. Add the per-claim evidence_quality_note above and keep pending_retest_after_substrate -> standard

    V3-EXQ-934 (diagnostic, excluded from confidence scoring; GOV-FANOUT-1 leg H1 of frozen-ledger question mech266_mode_arbitration_saturation). Both readiness preconditions cleared on measured, non-vacuous values (contact guard 3/3 seeds, contact rates 0.25-0.31, z_goal peaks 0.433-0.492 against a 0.4 gate; margin guard 3/3, though at 7-16x its floor it is a liveness check rather than a strong control). On the primary ARM_SYMMETRIC the single load-bearing criterion passed at exactly the 2/3 minimum. READ IT NARROWLY. The criterion is an EXISTENTIAL over the 5-point cap grid evaluated PER SEED, and the two passing seeds' mixed cells sit at OPPOSITE ENDS of the sweep (seed 42 only at cap 0.75, occupancy 0.5606; seed 43 only at cap 1.75, occupancy 0.4447; seed 44 never, minimum 0.9002 against a 0.9 ceiling). NO single cap grades on more than 1 of 3 seeds (0.75 -> 1/3; 1.0/1.25/1.5 -> 0/3; 1.75 -> 1/3), so winning_cap_band_symmetric = [0.75, 1.75] is the min/max of two disjoint singletons, not a band, and no common operating point was demonstrated. The mixed cells ARE non-degenerate (18-19 switches, mode-conditioned external_task dwell 43-54 steps), so the SD-032a mode register does genuinely alternate where the argmax crossing sits -- but that crossing is at most one grid step (0.25 cap units) wide in every seed showing it, and its location differs by >= 1.0 cap units across seeds, so an existential on a 5-point grid detects a CROSSING and cannot discriminate H1 (graded arbitration) from H2 (structurally bang-bang). H2 is NOT eliminated by this run. TWICE NON-PRODUCTION: salience_affinity_input_cap defaults to None (no clamp) and use_external_task_drive defaults to False, so the swept configuration is not production; training was held at cap 2.0, itself outside the swept range; and the driver's own pre-authoring probe recorded operating_mode['external_task'] = 0.0 at the production default cap=None. Mechanistically the clamp is a no-op on external_task_drive at every cap >= 1.0 (that signal is already bounded to [0,1] at agent.py:6958), so 4 of the 5 swept cells manipulate only the competing unbounded affinity inputs; and _et_commit = commit_weight * float(beta_gate.is_elevated) (agent.py:6944) is a boolean latch -- a second, cap-INDEPENDENT discreteness source no cap can grade. Failure-location (GOV-FAILLOC-1): MIXED (MECHANISM + MEASURES); environment adequate, implementation partial, measurement under-instrumented; not chargeable to REE. Successor V3-EXQ-935 (claimed 2026-08-16T13:01Z) already tests the common-operating-point question via a margin-normalised cap rule on 5 seeds.

    2026-08-18 (governance apply, V3-EXQ-935, confirmed
    failure_autopsy_V3-EXQ-935_2026-08-18): non_contributory / standard, UPHELD as stamped.
    PERIPHERAL / NOT EXERCISED: V3-EXQ-935 sweeps a single symmetric affinity_input_cap and
    contains no asymmetric-threshold arm, so MECH-266's Schmitt trigger (exit_threshold <
    enter_threshold) was never instantiated and the claim could not express itself on this
    run. It is a calibration step UPSTREAM of any MECH-266 test, not a test of it. No
    substrate_ceiling attribution accrues from this run and the re-derive brake count stays
    at 6. pending_retest_after_substrate stays true. Status stays provisional.
  location: evidence/planning/sd033_governance_plan.md
- id: MECH-267
  title: "Mode-conditioned hippocampal trajectory proposals: HippocampalModule.propose_trajectories must condition on operating_mode (SD-032a) in addition to world_state. External_task mode proposes task-relevant trajectories; internal_planning mode proposes counterfactual / branching trajectories; internal_replay mode replays past-successful trajectories; offline_consolidation mode proposes stability-check trajectories. Without mode conditioning, replay content contaminates external-task selection."
  claim_type: mechanism_hypothesis
  subject: hippocampal.mode_conditioned_proposals
  polarity: asserts
  status: provisional
  epistemic_category: standard  # 2026-08-16 governance: stamped per confirmed autopsy
  pending_retest_after_substrate: true  # 2026-08-03: retest (V3-EXQ-869a) ran with both mechanisms active, still washed out -- routed to a 3-leg GOV-FANOUT-1 discrimination portfolio; stays pending that portfolio
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-267@2026-04-25T15:42:09.107851Z"
      as_of: 2026-04-25
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: false
  implementation_note: >
    IMPLEMENTED 2026-04-20 in ree-v3/ree_core/hippocampal/module.py.
    HippocampalModule.propose_trajectories accepts an optional
    operating_mode: Dict[str, float] kwarg (probability vector from
    SD-032a SalienceCoordinator). When config.mode_conditioning_enabled
    is True AND operating_mode is supplied, the CEM proposal std is
    scaled by sum_{m} operating_mode[m] * config.mode_noise_scale.get(m,
    1.0). Modes present in operating_mode but absent from the
    noise-scale map default to 1.0 (no-op). Diagnostics on the module:
    _last_operating_mode and _last_noise_scale for verification.
    HippocampalConfig fields: mode_conditioning_enabled (bool, default
    False) + mode_noise_scale (Dict[str, float], default empty).
    Backward compatible: when either condition is False, behaviour is
    unchanged (operating_mode is ignored and the legacy CEM noise scale
    applies). Validation PASSed 2026-04-21: V3-EXQ-462 (EXP-0158 rule
    binding, 5 sub-tests covering basic conditioning, mode-specific
    noise multipliers, off-mode baseline, missing-mode fallback,
    diagnostics) and V3-EXQ-465 (EXP-0161 intrusive simulation
    filtering, 4 sub-tests). Behavioural successors deferred:
    EXP-0155 (cue_action_proj forward-path diagnostic) for EXP-0158
    extension; forced-replay injection hook for EXP-0161 extension.
    Lit-pull recommendation (2026-04-27) suggested operating_mode
    modulate look-ahead horizon at proposal generation; the V3
    implementation modulates CEM noise scale instead -- different
    mechanism with similar effect (mode-conditional exploration breadth
    vs depth). V4 reconsideration could add explicit horizon
    modulation alongside the existing noise-scale mechanism.


    [2026-08-02, SD-MECH267-HORIZON-DEPTH IMPLEMENTED, ree-v3
    ree_core/hippocampal/module.py]: the second mechanism named above
    (horizon-depth modulation, deferred at 2026-04-20) is now built,
    superseding the "V4 reconsideration" framing -- a 2026-08-02
    governance cycle re-examined that deferral in light of USER-CONFIRMED
    evidence (V3-EXQ-869: noise-scale-only washes out under production
    CEM elite-refit at num_cem_iterations=3 despite working at iters=1;
    confirmed failure_autopsy_V3-EXQ-869_2026-08-02) and routed it to
    /implement-substrate now rather than leaving it V4-deferred.
    HippocampalConfig.mode_horizon_scale (Dict[str, float], default
    {external_task: 0.5, internal_planning: 1.0, internal_replay: 0.7,
    offline_consolidation: 1.0}), gated by the same
    mode_conditioning_enabled switch as mode_noise_scale (companion facet
    of one mechanism, not a separate feature). Modulates the CEM
    elite-selection SCORING WINDOW (_score_trajectory's new max_horizon
    param: how many of the already-rolled-out steps count toward each
    candidate's score), NOT the physical rollout length (config.horizon
    remains a fixed structural network dimension -- terrain_prior's output
    width). Diagnostics: _last_mode_horizon_scale, _last_effective_horizon.
    Backward compatible: bit-identical when mode_conditioning_enabled is
    False or operating_mode is not supplied (verified by smoke test).
    Design doc:
    REE_assembly/docs/architecture/sd_mech267_horizon_depth_modulation.md.
    Validation experiment NOT queued this session (substrate-build scope
    only, per this IGW item's instructions) -- follow-on is a
    /queue-experiment re-run of V3-EXQ-869's C1 condition (production
    num_cem_iterations=3) with BOTH mechanisms active, flagged via a
    follow-up chip. V3-EXQ-869's run manifest marked
    pending_retest_after_substrate:true /
    superseded_by_substrate:SD-MECH267-HORIZON-DEPTH@2026-08-02 (nested
    manifest, per REE_assembly/evidence/planning/substrate_dependencies.json).
    Separately noted (not corrected in this pass, outside this
    substrate-implementation task's scope): the V3-EXQ-869 manifest's own
    evidence_direction field still reads "weakens", while the confirmed
    autopsy's recommended_evidence_direction was "non_contributory" --
    that reclassification appears applied to this claim's
    evidence_quality_note history but not yet propagated to the manifest
    field itself.


    [2026-08-14, SD-MECH267-CEM-SELECTION-FIX IMPLEMENTED, ree-v3
    ree_core/hippocampal/module.py]: the THIRD and FOURTH facets, both
    targeting the mode-content WASH-OUT that neither noise-scale nor
    horizon-depth survives (V3-EXQ-869/869a/923; the CEM refit re-derives
    ao_std from the mode-blind elite set each iteration and _score_trajectory
    ranks mode-independently). Locus H2-vs-H3 was left deliberately unscoped
    (2026-08-12 user decision); on 2026-08-14 the user chose to build BOTH as
    independent no-op-default flags and re-measure. H2:
    HippocampalConfig.mode_value_weight (Dict[str, List[float]], default {}) --
    a mode-dependent term in _score_trajectory keyed on mean z_world
    (world_dim-length weight vector per mode), subtracted from the terrain
    score, so elite-selection RANKING stays mode-differentiated every
    iteration. Keyed on z_world not valence because the fresh-field 869/923
    regime has identically zero valence. H3:
    HippocampalConfig.mode_partitioned_cem (bool, default False) -- re-applies
    the mode noise scale to the freshly-refit ao_std once per CEM iteration
    (both refit branches) so mode breadth persists; no compounding (ao_std
    recomputed from elites each iteration). Both gated by
    mode_conditioning_enabled; not wired through from_dims (set directly on
    HippocampalConfig like the other mode facets). Diagnostics:
    _last_mode_value_weight_active, _last_mode_partitioned_cem. Backward
    compatible: bit-identical at defaults (smoke-verified). Smoke (untrained
    field, single seed, indicative): H3 lifted the tight-vs-broad raw_std mode
    gap from 2.1e-06 (OFF control) to 0.0172 (ON, > 0.01 FLOOR_PRODUCTION).
    Design doc:
    REE_assembly/docs/architecture/sd_mech267_cem_selection_fix.md. Validation
    experiment V3-EXQ-928 queued (OFF / H2-only / H3-only / BOTH arms,
    re-measuring V3-EXQ-869 C1 at num_cem_iterations=3; acceptance = mean
    pairwise raw_std mode gap >= 0.01 per arm). Does NOT clear
    pending_retest_after_substrate (the GOV-FANOUT-1 portfolio + the queued
    validation still owe the retest); confidence/status UNCHANGED (governance,
    not this build).
  claim_level: mechanistic
  registered_utc: 2026-04-20
  depends_on:
    - SD-004           # HippocampalModule (the substrate whose proposal function is modified)
    - SD-032a          # SalienceCoordinator (source of operating_mode signal)
    - MECH-261         # mode-conditioned write-gate registry (this mechanism is the read-side analogue)
    - MECH-092         # micro-quiescence replay (one mode whose proposals this specialises)
  functional_restatement: >
    Mechanism hypothesis. State-dependent replay content is one of the
    better-established results in hippocampal physiology (Pfeiffer &
    Foster 2013; Olafsdottir et al 2018 for goal-directed replay bias;
    task-set-conditioned replay in awake SWR subpopulations). REE's
    HippocampalModule.propose_trajectories currently conditions on
    world_state only; this is the novel missing piece identified in
    the 2026-04-20 GAP MEMO.

    MECH-267 asserts: proposal_trajectories(world_state, operating_mode)
    is the correct signature, not proposal_trajectories(world_state).
    The operating_mode vector (soft mixture over external_task /
    internal_planning / internal_replay / offline_consolidation) biases:
      - candidate set (external_task: task-relevant; internal_planning:
        counterfactual / hypothesis-tagged; internal_replay:
        previously-committed successful; offline_consolidation:
        stability-check);
      - horizon depth (shallower in external_task, longer in
        internal_planning);
      - tagging: internal_replay and internal_planning proposals carry
        MECH-094 hypothesis tags so downstream write gates behave
        correctly.

    Distinct from MECH-261. MECH-261 gates WRITES across substrates
    conditioned on mode. MECH-267 gates PROPOSAL CONTENT within the
    hippocampal substrate conditioned on mode. They are read-side /
    write-side duals.

    Falsifiable (primary): V3-EXQ-465 (intrusive simulation filtering)
    should show that hippocampal proposals generated under
    internal_replay mode DO bias subsequent external_task action
    selection (Tambini/Davachi 2019) but CANNOT bypass MECH-094 write
    gates into policy / episodic memory. If simulated content can
    write-commit without mode change, MECH-267's mode-conditioning is
    insufficient and hypothesis-tag enforcement at the proposal site
    is needed too.

    Falsifiable (secondary): V3-EXQ-462 (rule binding) should show that
    a rule installed in internal_planning mode survives the transition
    to external_task via the SD-033a substrate, not via hippocampal
    re-proposal; if hippocampal proposals are the carrier rather than
    the rule substrate, the SD-033a / hippocampus role allocation is
    wrong.
  notes: >
    Registered 2026-04-20 as part of the SD-033 governance plan
    (evidence/planning/sd033_governance_plan.md). Source: 2026-04-20_ocd2.md
    step 4 (mode-conditioning of hippocampal proposals). This is the
    most biologically novel of the four claims registered under the
    plan; the others extend existing REE mechanisms, this one genuinely
    adds a function.
  evidence_quality_note: >
    Registered candidate with v3_pending:true. Biology lit-pull
    complete 2026-04-27, five entries in
    evidence/literature/targeted_review_connectome_mech_267/entries/:
    Pfeiffer & Foster 2013 (goal-directed forward replay, supports),
    Tambini 2019 (reactivation bias toward task-relevant memories,
    supports), Olafsdottir 2018 (replay review including
    state-dependent content, supports), Mattar & Daw 2018 (prioritized
    memory access framework, supports 0.84), Wikenheiser & Redish 2015
    (theta-cycle look-ahead distance scales with goal distance,
    supports 0.82). Implementation recommendations: (1) operating_mode
    should modulate look-ahead horizon parameter at proposal
    generation -- the simplest implementation is a mode-conditional
    multiplier on the proposal-rollout depth; (2) condition the
    proposal value-flat ranking (not necessarily proposal generation
    itself) on operating_mode via the prioritized-memory-access
    framework -- need (state-visit probability under current goals)
    and gain (expected improvement) both depend on operating_mode
    indirectly through the goal hierarchy; (3) Mattar & Daw's
    rumination concern connects to MECH-292's goal_match_floor
    rumination guard -- mode-conditioning provides a natural defence
    against stuck-on-state pathology because mode-state-shift
    re-prioritises content. V3-EXQ-465 (intrusive simulation
    filtering) and V3-EXQ-462 (rule binding) are the primary
    falsifiers queued in ree-v3/experiment_queue.json.

    [2026-08-02 governance, V3-EXQ-869, confirmed failure_autopsy_V3-EXQ-869_2026-08-02, first-ever full retest, USER-CONFIRMED]: well-powered (30 seeds), pre-registered design cleanly separates manipulation-present (C0, iters=1, gaps 0.031-0.092 vs 0.015 floor, PASS) from production-persistence (C1, iters=3, gaps -0.004 to +0.004 vs 0.01 floor, FAIL; 0/30 seeds show the full predicted ordering). However the claim's own registration text and 2026-04-20 implementation_note name TWO mechanisms -- noise-scale modulation (built) and horizon-depth modulation (explicitly deferred to "V4 reconsideration", matching the original lit-pull's own recommendation, directly evidenced by Wikenheiser & Redish 2015). The wash-out-under-elite-refit pattern is consistent with a noise-scale-only implementation being structurally insufficient against CEM's iterative refit -- not with the claim being biologically wrong. competence_implementation_gap. Route: /implement-substrate to build horizon-depth modulation (new substrate_queue entry SD-MECH267-HORIZON-DEPTH), then retest content persistence with both mechanisms active. pending_retest_after_substrate: true.

    [2026-08-03 governance, V3-EXQ-869a, confirmed failure_autopsy_V3-EXQ-869a_2026-08-03, retest with
    BOTH mechanisms active]: horizon-depth modulation (built 2026-08-02 per 869's own routing) confirmed
    active at runtime alongside noise-scale. Production-settings (iters=3) content persistence still
    washes out, nearly identical numbers to 869's noise-scale-only result. non_contributory;
    epistemic_category RETAINED as competence_implementation_gap (user-confirmed, deliberately NOT
    substrate_ceiling). Routed to a GOV-FANOUT-1 discrimination portfolio (3 legs: iteration-count,
    mode-aware scoring, partitioned pools) rather than a third sequential same-question letter --
    see the fanout_recommendation in failure_autopsy_V3-EXQ-869a_2026-08-03.json.

    [/governance 2026-08-12, V3-EXQ-923, confirmed failure_autopsy_V3-EXQ-923_2026-08-12,
    GOV-FANOUT-1 portfolio leg H1]: H1 (iteration-count) is ELIMINATED. Production-settings content
    wash-out is NOT iteration-count dependent -- mean pairwise raw_std gaps at num_cem_iterations=2
    read -0.0028 / 0.0023 / ~0.000006 against FLOOR_PRODUCTION=0.01, i.e. already flat by iters=2 and
    unchanged at iters=3 (869a), with no intermediate count recovering the clean iters=1 signal
    (0.031-0.092). non_contributory: the leg eliminates a rival rather than bearing on MECH-267
    itself. Portfolio state after this leg: H2 (an explicit mode-dependent term in the CEM
    elite-selection VALUE FUNCTION, distinct from the scoring-window depth horizon-depth already
    supplies) and H3 (hard-partitioned per-operating_mode CEM candidate pools so elite-refit never
    mixes across modes) remain the only live loci, and NEITHER is built. Three candidate explanations
    are now excluded in sequence -- noise-scale-only (869), noise-scale+horizon-depth (869a),
    iteration-count (923). routing=implement-substrate: new substrate_queue entry
    SD-MECH267-CEM-SELECTION-FIX created this cycle, deliberately UNSCOPED between H2 and H3 per the
    2026-08-12 user decision (neither has been costed); a future session should scope feasibility for
    both before committing to a build, or build both and re-measure C1 at iters=3 for each. Stays
    provisional / pending_retest_after_substrate.

    [2026-08-16 governance apply | V3-EXQ-928 -> MECH-267 | failure_autopsy_927-928-mech267-cluster_2026-08-16] DISPOSITION: evidence_direction non_contributory. Diagnostic localisation only; direction stays non_contributory. Apply the new evidence_quality_note (locus resolved to H3 refit breadth; H2 a confirmed null; gate under-powered; restoration endpoint-only; facets default-OFF) and keep pending_retest_after_substrate true, now gated on the mode_partitioned_cem default flip rather than on a build. epistemic_category -> standard

    V3-EXQ-928 (superseding burned-id V3-EXQ-927; identical measurement) localised the MECH-267 mode-content wash-out at production num_cem_iterations=3 to the CEM refit breadth, not the elite-selection value function. Four arms x 30 shared seeds: H3 (mode_partitioned_cem) - OFF = +0.0167 paired, t=+4.34, 24/30 seeds positive; BOTH - OFF = +0.0142, t=+4.02; H2 (mode_value_weight) - OFF = +0.0015, t=+0.48, a clean null. Manipulation confirmed on 480/480 cells. The run's own acceptance gate (across-seed mean vs absolute FLOOR_PRODUCTION=0.01) is under-powered -- per-seed SD 0.011-0.018 exceeds the floor, giving t=+1.09 (H3) and t=+0.36 (BOTH) against it -- so the self-routed label `fix_effective::H3+BOTH` is correct on the paired vs-control contrast rather than on the gate it cleared. The indexer's `vacuous_pass` flag is adjudicated CLEARED as a criteria-tagging artifact: the declared combination rule is a disjunction ('>=1 fix arm clears') while all three per-arm criteria were tagged load_bearing:true; H2's sub-floor result is the informative half of the localisation, not a gate cleared on nothing. NARROWING: H3 restores the extreme-pair (broad-tight) breadth separation only -- the predicted monotone four-mode gradient is NOT restored in any arm (all adjacent gaps sub-floor; internal_replay->offline_consolidation inverts). Both facets default OFF (mode_partitioned_cem=False, mode_value_weight={}), so MECH-267 mode-conditioning remains washed out in production until the default flips. Diagnostic -- excluded from confidence and conflict scoring; pending_retest_after_substrate remains true, now gated on the default flip rather than on a build.

    [2026-08-16 governance apply | V3-EXQ-927 -> MECH-267 | failure_autopsy_927-928-mech267-cluster_2026-08-16] DISPOSITION: evidence_direction superseded. Mark this manifest inactive as a burned-id duplicate of V3-EXQ-928 so it stops double-counting in MECH-267 population statistics: set evidence_direction -> superseded

    SUPERSEDED by V3-EXQ-928. V3-EXQ-927 is a BURNED QUEUE ID: ree-v3 commit 73f07e4 ('V3-EXQ-928: rename MECH-267 CEM-fix validation from burned id 927') renamed the single driver file, and only one driver exists in experiments/. The two manifests are bit-identical on every numeric field (per_arm_mean_broad_minus_tight_gap, per_arm_n_seeds_positive_gap, per_arm_mode_mean_raw_std, per_arm_adjacent_mean_gaps, per_arm_seed_broad_minus_tight_gaps). The differing substrate_hash / substrate_commit (8aa975e9cc -> fed315c702) covers only CLAUDE.md, a phase3 queue snapshot, and the driver's own id strings -- git diff --stat shows NO ree_core/ change -- so the substrate under test was byte-identical and the identical results are the correct deterministic outcome, not a defect. Governance should set evidence_direction: superseded on this manifest with supersedes: v3_exq_928_... so the indexer stops counting one measurement twice.
  location: evidence/planning/sd033_governance_plan.md
- id: MECH-268
  title: "dACC conflict saturation: the dACC-analog (SD-032b) prediction-error / conflict signal caps and habituates under repeated identical outcomes; it does not grow unboundedly as conflict persists. Without saturation, a stuck governance loop can generate unbounded pe signal that never triggers a mode change, which is the control-theory signature of persistent rumination."
  claim_type: mechanism_hypothesis
  subject: dacc.conflict_saturation
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-468f_2026-06-20
      as_of: 2026-06-20
      verdict: non_contributory/no_change (self-route substrate_not_ready_requeue is correct; SD-034 provisional / MECH-268 candidate / MECH-090 active all stay non_contributory + pending_retest)
  implementation_phase: v3
  v3_pending: false
  implementation_note: >
    IMPLEMENTED 2026-04-20 in ree-v3/ree_core/cingulate/dacc.py.
    History-conditioned PE saturation in DACCAdaptiveControl: at each
    affective_pe step, the outcome is classified into a discrete bin
    appended to a FIFO _outcome_history (length saturation_window).
    Saturation factor f_sat = 1.0 / (1.0 + saturation_strength *
    max(0, n_rec - saturation_grace)) where n_rec = count of current
    outcome class in the last saturation_window outcomes. Applied as
    pe_saturated = pe_capped * f_sat. Defaults preserve backward
    compat: dacc_saturation_enabled=False -> f_sat=1.0 always (no-op);
    when enabled, saturation_window=8, saturation_strength=0.3,
    saturation_grace=2 (graded learning-rate adapter, NOT a binary cap).
    Last-cycle diagnostic _last_saturation_factor exposed for
    inspection. SD-034 closure operator coupling: closure.tick() calls
    dacc.reset_episode_pe() (clears episode-level pe state but
    preserves _action_history / _outcome_history -- distinct from full
    reset()) and optionally installs dacc_pe_cap. Validation PASSed
    2026-04-21: V3-EXQ-463 (EXP-0159 conflict saturation, 7 sub-tests)
    and V3-EXQ-468 (EXP-0164 commitment vs contradiction, 4-arm
    A/B/C/D interaction with SD-034, 6 sub-tests). 13/13 sub-tests
    PASS on smoke. Behavioural arms (500+ step sustained outcome /
    counter-evidence injection) deferred -- env extensions not yet on
    roadmap. Lit-pull recommendation match (2026-04-27): graded
    learning-rate adapter rather than binary cap -- V3 implementation
    matches; signal-channel-specificity (which dACC channel saturates:
    pe vs surprise vs commitment vs salience) flagged as V4
    reconsideration; Quilodran 2008 temporal-transfer coupling between
    MECH-268 and SD-034 captured in the closure_reset_pe_ema +
    closure_pe_cap_after design.

    F-C3 LIVE-PATH WIRING + ECOLOGICAL-DEMONSTRATION GAP (2026-07-09,
    ree-v3 main c7fc045). The 2026-07-09 design/implementation audit
    (design_implementation_audit_2026-07-09.md sec 3A F-C3 + sec 6)
    found DACC.record_outcome had ZERO callers in the live agent path,
    so dacc_saturation_enabled=True was silently INERT ecologically --
    _saturation_factor always returned 1.0 and the habituation /
    rumination PE attenuation never activated in a real agent loop. Up
    to 2026-07-09 the saturation was validated ONLY via V3-EXQ-463b's
    harness, which manually injects record_outcome each eval step
    (synthetic per-step stream, not the agent's own loop). c7fc045
    closed BOTH wiring gaps: (1) the agent.py select_action tail now
    calls record_outcome each waking tick, outcome class = harm-vs-no-
    harm on z_harm_a thresholded by contextual_safety_harm_threshold,
    gated on the REEConfig dacc_saturation_enabled flag (byte-identical
    when off; 463b unaffected as it patches DACCConfig post-build); and
    (2) DACCConfig construction now propagates the dacc_saturation_*
    knobs from REEConfig (previously unpropagated -- the source of the
    "does not yet propagate" caveat in 463b). So MECH-268 CAN now fire
    ecologically. HOWEVER the SCIENCE is NOT yet demonstrated in a real
    agent loop: as of 2026-07-09 MECH-268 still rests only on 463b's
    synthetic injection. The ecological (live-loop, non-synthetic)
    falsifier V3-EXQ-729 (v3_exq_729_mech268_dacc_saturation_liveloop,
    authored + smoke-PASS 2026-07-09) is QUEUED to close this gap: it
    makes NO manual record_outcome call, proves the _outcome_history
    FIFO is populated by the LIVE select_action path (counting spy),
    and asserts final-third sat_factor < 0.95 (ON) vs ~1.0 (OFF, with
    zero live record calls) under sustained chronic-harm exposure.
    Until V3-EXQ-729 lands a PASS the ecological demonstration is
    OUTSTANDING. This note documents wiring + a pending falsifier and
    PROMOTES NOTHING (status stays provisional).
  claim_level: mechanistic
  registered_utc: 2026-04-20
  depends_on:
    - SD-032b          # dACC-analog (the module whose pe signal saturates)
    - MECH-258         # dACC prediction-error mechanism being saturated
    - MECH-260         # action-class suppression (interacts via shared action_history)
    - SD-034           # closure operator (closure resets saturation per-rule-episode)
  functional_restatement: >
    Mechanism hypothesis. The dACC prediction-error / conflict signal
    in SD-032b is currently unsaturated. Biological dACC error signals
    habituate under repeated identical outcomes (Bryden et al 2019);
    the EVC framework (Shenhav et al 2016) predicts saturation dynamics
    under sustained demand; repeated-identical-outcome adaptation is a
    general feature of cortical error coding.

    MECH-268 asserts: pe_saturated = f_sat(pe_raw, outcome_history)
    where f_sat caps pe growth and introduces habituation when the
    outcome stream is repetitive. Operationally: SD-032b maintains a
    short outcome-identity buffer; when the same outcome class recurs
    N times in a row, the effective pe is down-weighted. A closure
    event (SD-034) resets the buffer for the associated rule-episode.

    Why this matters for governance. Without saturation, a stuck
    governance loop can continue to generate pe signal indefinitely,
    which mechanistically resembles rumination and blocks mode
    transitions that depend on pe returning near baseline (e.g. leaving
    internal_planning mode). With saturation, the pe signal dies out on
    its own when no new information arrives, allowing the mode register
    to relax even without an explicit closure event.

    Distinct from MECH-258. MECH-258 is the pe signal itself. MECH-268
    is a post-processing function over that signal that implements the
    cap + habituation dynamics.

    Falsifiable (primary): V3-EXQ-463 (conflict saturation under
    repeated identical outcomes) should show pe signal plateaus in a
    bounded range as the identical-outcome sequence lengthens. If pe
    grows unboundedly, MECH-268 is not implemented or the saturation
    function is mis-specified.

    Falsifiable (secondary): V3-EXQ-468 (commitment vs contradiction)
    should show that under sustained counter-evidence, pe eventually
    caps and the adaptive gate thresholds do not fire. If pe keeps
    growing, the governance loop never resolves.
  notes: >
    Registered 2026-04-20 as part of the SD-033 governance plan
    (evidence/planning/sd033_governance_plan.md). Source: 2026-04-20_ocd1.md
    fourth constraint on closure (the dACC conflict signal must
    saturate, or the OCD rumination loop is mechanistically
    unbounded).

    Pairs with SD-034. SD-034 resets saturation on successful
    completion (explicit closure). MECH-268 prevents pe from growing
    unboundedly in the absence of explicit closure (implicit closure
    via habituation). Together they bound the pe signal in both the
    normal and the failure case.
  evidence_quality_note: >
    Registered candidate with v3_pending:true. Biology lit-pull
    complete 2026-04-27, six entries in
    evidence/literature/targeted_review_connectome_mech_268/entries/:
    Holroyd 2012 (ACC options, supports), Shenhav 2016 (dACC value of
    control, supports), Shenhav 2013 (EVC theory, supports), Bryden
    2011 (ACC attention for learning signals, supports), Behrens 2007
    (volatility-adaptive ACC learning rate signal, supports 0.82),
    Quilodran, Rothe & Procyk 2008 (primate ACC outcome-time signal
    disappears in exploitation periods, supports 0.83). Implementation
    recommendations: (1) implement saturation as a graded
    learning-rate adapter rather than a binary habituation cap (the
    Behrens volatility-adaptive signal is a smooth modulation, not a
    fixed cap); (2) clarify which dACC signal channel actually
    saturates -- pe, surprise, commitment, or salience -- because the
    empirical evidence (Quilodran 2008) shows the saturating signal is
    described as "specific to the first reward / end of exploration",
    closer to a phasic surprise / commitment signal than a tonic pe;
    (3) Quilodran's temporal-transfer finding (saturated outcome
    signal shifts forward to predictive cue rather than dying) is the
    architectural origin of the "expected outcome -> done token" chain
    SD-034 closure operator depends on -- this couples MECH-268 to
    SD-034 in a way the original V3 design did not capture. V3-EXQ-463
    (conflict saturation) and V3-EXQ-468 (commitment vs contradiction)
    are the primary falsifiers queued in ree-v3/experiment_queue.json.
  location: evidence/planning/sd033_governance_plan.md

- id: MECH-269
  evidence_quality_note: |
    [R4.b stamped 2026-05-29] behavioral_diversity_isolation:GAP-D Theory-4 R4.b decision-rule stamped on diagnostic-probe evidence: V3-EXQ-550 FAIL/supports MECH-269 2026-05-11T20:18Z (wired-but-untrained z_goal did NOT shift action-class entropy, so the substrate-level reading SURVIVES per the manifest's interpretation grid). Run is in claim_evidence.v1.json with evidence_direction=supports but scoring_excluded=diagnostic_probe (experiment_purpose=diagnostic), so the standard /governance pipeline cannot lift this claim off hold_pending_v3_substrate via exp_conf -- the stamp is a governance-level recognition of the V_s-pathology reading, NOT an experimental_confidence move. v3_pending=true preserved. Non-diagnostic behavioural sufficiency test (Q-040b) is owned by goal_pipeline:GAP-4 / V3-EXQ-490g Tier-1 retest cohort -- that is the path to lifting v3_pending. See evidence/planning/behavioral_diversity_isolation_plan.md 2026-05-29 GAP-D R4.b stamp section.

    Registered candidate with v3_pending:true. lit_confidence 0.858 after 2026-04-22 lit-pull (targeted_review_waking_v_s_invalidation/ + targeted_review_connectome_mech_269/). Anchor-reset arm grounded in dual-trace extinction literature (Bouton 2004) plus latent-cause inference (Gershman 2010, 2017). No in-silico experiment evidence yet. Promotion requires substrate implementation (extends HippocampalModule.propose_trajectories with anchor-reset hysteresis) plus the EXQ-471 unlock test plus the dual-trace return-of- avoidance test.
    2026-04-25 governance: V3-EXQ-478 (Phase 3 diagnostic) and V3-EXQ-480 (4-arm parameter sweep) both FAIL substrate-readiness gates. Anchor resets fire abundantly (63/31 per seed) and staleness peaks ~0.94/0.83, but freeze_recommit_count is bit-identical OFF=ON across all attribution_mode and leak_factor settings; baseline action_class_entropy=0 on both seeds even with SD-029 curriculum ON. Both runs tagged inconclusive per_claim (correctly). Diagnosis: read-side hook is the bottleneck, not parameter tuning. V3-EXQ-481 (V_s commit-release hook) confirms: vs_commit_release_count=0 in both ON and OFF arms -- the release path is dead code on this env. The MECH-284 + MECH-269 online coupling is wired-but-behaviourally-inert; revisit BoundaryEvent rate, anchor-key snapshot scope, or commit-entry capture sites before the next validation pass. Substrate stays candidate / v3_pending; not a weakening of MECH-269.
    [2026-05-08 governance]: EXQ-476c diagnostic, inconclusive. V_s observables do not convert to behaviour without the MECH-284 Phase-3 consumer; reveals an architectural gap (assumed-present consumer is in fact absent). Not weighted as evidence.
  title: "Hippocampal proposer anchor selection AND anchor-reset: (a) trajectory proposals issued between committed actions anchor on the subset of current latent streams whose regional verisimilitude (per-stream prediction/realization alignment) exceeds threshold; misaligned streams hold their last-verified snapshot (anchor carryover) rather than contaminate the rollout. (b) Active anchors are continuously re-evaluated against an effective verisimilitude V_s_anchor(a, t) = V_s(r, t) - staleness[r] (MECH-284 read-out); if V_s_anchor falls below theta_reset(r) for k consecutive steps the anchor is marked inactive (NOT erased -- dual-trace preservation per Bouton 2004) and a new anchor is sought from the current latent slice. A minority probe channel deliberately inverts the gate for low-verisimilitude / high-PE regions, carrying a strengthened MECH-094 hypothesis tag, and does not update the ARC-018 viability map until realized-experience validation."
  claim_type: mechanism_hypothesis
  subject: hippocampal_proposer.regional_verisimilitude_anchor_selection
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_481b_vs_commit_release_20260519T012616Z_v3
      as_of: 2026-05-19
      verdict: supports/PASS
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-04-21
  depends_on:
    - ARC-007   # hippocampal path memory (anchors operate over this space)
    - ARC-018   # hippocampal rollouts / viability mapping (the map anchored proposals update)
    - MECH-089  # theta-gamma nesting (per-cycle summary the anchor reads)
    - MECH-092  # quiescent SWR-equivalent replay (anchored replay runs here)
    - MECH-094  # hypothesis tag (strengthened on probes)
    - MECH-272  # state-gated routing (load-bearing for dual-trace coexistence: routing decides which anchor is operative)
    - MECH-284  # staleness accumulator (online read-out drives anchor-reset hysteresis)
    - SD-005    # z_self/z_world split (precondition for per-stream V)
    - SD-010    # harm stream separation
    - SD-011    # dual nociceptive streams
  functional_restatement: >
    Mechanism hypothesis. ARC-018 describes hippocampal rollouts starting
    from "current latent state" but does not specify anchor-state
    selection. The current latent is distributed across streams
    (z_world, z_self, z_harm_s, z_harm_a, z_goal, ...) whose alignment
    with perception varies moment-to-moment.

    MECH-269 asserts: for each stream s, a running alignment score
    V_s(t) = align(z_hat_s(t|t-dt), z_s(t)) is maintained (cosine
    similarity or negative prediction error over a short EMA window).
    A stream is anchor-eligible when V_s(t) >= theta_anchor(s); the
    proposer constructs its rollout starting-state by composing only
    the anchor-eligible slice, with misaligned streams held at their
    last-verified snapshot.

    Probe channel. A pure anchor-eligibility gate would suppress
    proposals in precisely the regions the agent needs to understand.
    MECH-269 specifies a minority probe channel that inverts the gate,
    seeding rollouts from low-V / high-PE regions. Probe rollouts carry
    a strengthened hypothesis tag (MECH-094) and do NOT update the
    ARC-018 viability map -- they update curiosity / epistemic-coverage
    structures. Anchor/probe mixture is modulated by the control-plane
    heartbeat (existing dACC / SalienceCoordinator gating).

    Why regional not global. A single "model trust" scalar cannot gate
    anchor selection after SD-010/SD-011: z_world and z_harm_s can
    dissociate in alignment quality, and partial anchoring (some
    streams current, some carried-forward) is the normal case. Same
    logic as per-claim evidence_direction in multi-claim experiments.

    Distinct from MECH-098. MECH-098 feeds V_world specifically via
    reafference cancellation; MECH-269 is the per-stream anchor-gate
    that operates over all streams including non-world ones.

    Falsifiable (primary): a V3 experiment instrumenting per-stream V_s
    and anchor labels should show (a) smaller prediction-vs-outcome gap
    for anchored-eligible-only rollouts vs indiscriminate anchoring,
    (b) probe-tagged rollouts preferentially target high-PE regions
    and do not populate the viability map until realized validation,
    (c) streamwise partial-anchor plans fail gracefully vs
    catastrophically as a no-anchor-selection baseline.

    Falsifiable (secondary): manipulating salience / harm load should
    shift the observed anchor/probe mixture without retraining.

    Anchor-reset criteria (refined 2026-04-22). For each active anchor
    a on region r:
      V_s_anchor(a, t) = V_s(r, t) - staleness[r]   # MECH-284 read-out
      if V_s_anchor(a, t) < theta_reset(r) for k consecutive steps:
        mark_inactive(a)                # NOT erase -- dual-trace
        seek_new_anchor(latent_slice(t))

    The k-consecutive-step hysteresis (default k=5) prevents single-event
    chatter from destabilising the anchor while still allowing
    sufficiently strong sustained invalidation to break a stale anchor
    within bounded time. Dual-trace preservation (Bouton 2004 -- extinction
    is new learning, not erasure of old) is essential: the avoid-anchor
    that produced V3-EXQ-475's catatonic lock must remain retrievable as
    inactive trace, with MECH-272 state-gated routing deciding which
    anchor is operative for the current context. This permits return-of-
    avoidance under sufficient context-cue match without re-learning.

    Falsifiable (anchor-reset, primary): an EXQ-471-conditions re-run
    with MECH-287 trigger + MECH-284 online accumulator + MECH-269
    anchor-reset all enabled should show anchor invalidation events
    (mark_inactive logged) within bounded number of PAG releases (target:
    after 2-3 releases, anchor invalidates and proposer remaps -- not
    1000/1000 freeze).

    Falsifiable (anchor-reset, secondary, dual-trace): post-extinction
    re-presentation of the original avoid-context cues should produce
    re-activation of the old anchor (return-of-avoidance) rather than
    a from-scratch re-learning trajectory; conversely, in a different
    context the new anchor remains operative.
  notes: >
    Design doc (refined): docs/architecture/v_s_invalidation_runtime.md
    (covers MECH-269 anchor-reset criteria + MECH-287 trigger + MECH-284
    accumulator). Original anchor-selection design context remains at
    docs/architecture/hippocampal_anchor_selection.md.
    Registered 2026-04-21 from user-initiated exploratory discussion
    about how the proposer selects its starting state for inter-commit
    proposals. Biological routing hypothesis (separate claim MECH-271)
    predicts anchored replay preferentially reaches E1 consolidation
    (subiculum -> EC -> neocortex) and SD-033a lateral PFC; probe
    replay preferentially reaches BLA (affective tagging) and NAc
    (curiosity) -- see MECH-271.

    No experiment queued yet: substrate hooks for per-stream V_s
    logging in HippocampalModule.propose_trajectories do not exist.
    BLA landing (SD-035 / V3-EXQ-474) is the natural MECH-271 routing
    validation target, which in turn constrains MECH-269 once the
    anchor/probe labels are emitted.
  implementation_note: >
    Anchor-reset arm online-arm wiring now consumes MECH-284 staleness
    as of 2026-04-24 (ree-v3 Phase 3). AnchorSet.tick_hysteresis
    accepts an optional staleness_lookup callable; when HippocampalConfig
    .use_mech284_hysteresis=True, HippocampalModule.tick_anchor_set
    injects StalenessAccumulator.lookup_by_anchor_key so V_s_anchor(a, t)
    = V_s(r, t) - staleness[r] is evaluated against the MECH-284
    accumulator rather than the Phase 2 internal
    (tick - last_accessed) * staleness_rate proxy. Default remains
    False (Phase 2 proxy preserved for backward compat). Dual-trace
    mark_inactive (NOT erase) semantics already live from Phase 2 and
    are unchanged. Promotion still requires EXQ-471 unlock test and
    return-of-avoidance test -- V3-EXQ-478 queued as the Phase 3
    diagnostic validation for the MECH-284+MECH-269 coupling.
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: requires live per-stream V_s(t) alignment
    telemetry and anchor/probe labels to actually be emitted at runtime by
    HippocampalModule.propose_trajectories -- confirmed ABSENT through the
    2026-04-25/05-08 diagnostics (V3-EXQ-478/480/481/476c all found the
    read-side hook missing or the MECH-284+MECH-269 online coupling
    wired-but-behaviourally-inert: freeze_recommit_count and
    vs_commit_release_count pinned at 0 across every arm). A run without
    live per-stream V_s telemetry and non-zero cross-arm anchor-invalidation
    counts self-routes substrate_not_ready, NOT a verdict on the hypothesis --
    the instrumentation gap must close first (implement-substrate work), not
    another parameter sweep against the existing dead hook.
    CONFIRMING signature (primary, anchor-selection): anchored-eligible-only
    rollouts show a smaller prediction-vs-outcome gap than indiscriminate
    (no-anchor-selection) rollouts; probe-tagged rollouts preferentially
    target high-PE regions and do NOT populate the ARC-018 viability map
    until realized-experience validation; streamwise partial-anchor plans
    degrade gracefully rather than catastrophically relative to a
    no-anchor-selection baseline.
    CONFIRMING signature (secondary): manipulating salience/harm load shifts
    the observed anchor/probe mixture without retraining.
    CONFIRMING signature (anchor-reset, primary): under EXQ-471 conditions
    with MECH-287 trigger + MECH-284 accumulator + MECH-269 anchor-reset all
    enabled, anchor invalidation (mark_inactive) fires within a bounded
    number of PAG releases (target ~2-3), not the 1000/1000 freeze the
    catatonic-lock failure mode (V3-EXQ-475) produces.
    CONFIRMING signature (anchor-reset, dual-trace): after extinction,
    re-presenting the original avoid-context cues re-activates the OLD
    (inactive, not erased) anchor -- return-of-avoidance -- rather than
    triggering a from-scratch re-learning trajectory, while a genuinely
    different context leaves the new anchor operative (Bouton 2004).
    FALSIFYING: anchor-eligible-only rollouts perform no better than
    indiscriminate anchoring; probe rollouts contaminate the viability map
    before validation (breaking the probe/anchor separation); anchor
    invalidation never fires within bounded PAG releases once the hooks
    genuinely exist (a real freeze, not an instrumentation gap); or
    re-presented extinction cues trigger from-scratch relearning rather
    than return-of-avoidance (falsifies the dual-trace/Bouton 2004 reading).
  location: docs/architecture/v_s_invalidation_runtime.md

- id: MECH-270
  title: "Ephaptic field coherence in CA1/CA3 pyramidal layers is the candidate physical substrate for the per-stream verisimilitude readout that MECH-269 anchor selection operates over. Streams whose predictions align with realized values produce coherent pyramidal ensembles whose extracellular fields constructively reinforce; misaligned streams produce incoherent fields. 'Anchor-eligible' reduces to 'field-coherent at read time.'"
  claim_type: mechanism_hypothesis
  subject: hippocampus.ephaptic_verisimilitude_readout
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-270@2026-04-25T15:42:09.107859Z"
      as_of: 2026-04-25
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-04-21
  depends_on:
    - MECH-269  # the function this is the substrate for
    - MECH-089  # theta-gamma nesting; ephaptic coordination rides these rhythms
    - ARC-007   # hippocampal path memory (where the ephaptic readout lives)
  functional_restatement: >
    Substrate hypothesis. Ephaptic coupling -- non-synaptic influence of
    neuronal firing by the extracellular field the population itself
    generates -- is strongest in tightly-packed pyramidal layers. In
    CA1 and CA3 during sharp-wave ripples, ephaptic effects provide
    synchronisation tighter than synaptic transmission alone can
    produce (Anastassiou & Koch 2011; Jefferys; Buzsaki).

    MECH-270 proposes that the per-stream verisimilitude signal V_s
    required by MECH-269 has a biological readout via stream-local
    field coherence. Because different latent streams are (by the
    z_self/z_world/z_harm_s/... decomposition) carried by partially
    dissociable ensembles, their fields can be coherent or incoherent
    independently. The proposer reads field coherence fast (sub-cycle)
    without needing an explicit alignment computation.

    Why keep this separate from MECH-269. Conflating function (anchor
    selection) with physical substrate (ephaptic readout) is the exact
    failure mode that bit SD-003 and SD-010/011 (see
    feedback_biology_before_formal_definitions). MECH-269 can be true
    with a different readout; MECH-270 can be true or false
    independently and is testable by different experiments.

    Falsifiable: biological tests would look at ripple-initiation
    locus alignment with cortical expectancy (expected ripple locus
    for a given mode/stream). In-silico tests would not directly test
    ephaptic coupling but would verify that a field-coherence-style
    readout (local ensemble phase coherence as a proxy for V_s)
    produces the anchor-selection behavior MECH-269 predicts.
  notes: >
    Not a substrate the V3 implementation needs to simulate
    biologically. In silico, V_s can be computed directly from
    per-stream prediction/realization alignment -- MECH-270 is the
    biological-grounding claim, not an implementation requirement.
    Registered primarily to keep the function/substrate distinction
    auditable and to give the ephaptic-coupling literature a home.
  evidence_quality_note: >
    Candidate, no direct experimental support yet in this project.
    External biology literature on ephaptic coupling in hippocampus
    is the relevant evidence base (Anastassiou & Koch 2011 is the
    entry point; Buzsaki on SWRs; Jefferys on field effects).

    V3-EXQ-720 (2026-07-09, diagnostic, non_contributory): a
    FIXED-field ephaptic-analog cross-stream binder
    (ree_core/latent/cross_stream_binder.py, strength 0.5) lifted
    coherence-specificity from 1/6 (641a, unbound) to 3/6 seeds but
    did not clear the 4/6 SPEC gate. Directional support for the
    ephaptic-coupling class; refines the translation -- a fixed field
    is symbol-complete but function-partial; a LEARNED/plastic binder
    (V4) is the residual prerequisite for coherence-non-reducibility.
    Not a status change (MECH-270 stays candidate; diagnostic
    non_contributory does not weight confidence).

    V3-EXQ-725 (2026-07-09, diagnostic, non_contributory): the LEARNED
    (plastic) binder built to test the 720 residual-prerequisite
    hypothesis did NOT converge -- InfoNCE loss stayed at chance
    (~log(64)=4.16; observed 3.75-3.96) because the observed latents are
    near-collinear buffer-wide and the un-normalized dot-product logit is
    magnitude-dominated. Coherence-specificity regressed 3/6 -> 0/6, an
    untrained-substrate artifact, NOT a verdict on learned
    binding-by-synchrony. The learned-binder-as-prerequisite hypothesis
    remains UNTESTED (not refuted). REPAIRED 2026-07-09
    (implement-substrate): L2-normalize the projections (cosine InfoNCE)
    -> loss drops to 0.65-0.80 of chance, binder_converged=True; retest
    V3-EXQ-725a HARD-gated on convergence. Not a status change.

    V3-EXQ-725a (2026-07-10, diagnostic, non_contributory): the converged
    learned binder was tested. Coherence-SPECIFICITY did NOT clear the gate
    (SPEC 1/6, gate 4/6; divergence reproduced by a contrast-matched shuffle
    -> settled NO-CLAIM on the coherence_nonreducibility Q; formal-import
    caution vindicated), and the converged learned binder UNDERPERFORMED the
    fixed-field peak (720 3/6). The one decoupled positive: rebinding-under-
    perturbation is now positively exercised at the substrate --
    n_rebind_total=1676 across 6 seeds via cross_stream_binder.rebinding_probe
    (0 in every predecessor 641/641a/720; vacuous in 725). Salvaged to the
    entity-layer rebinding sibling MECH-456 (candidate). Descriptive, NOT a
    confidence move (claim_ids=[]); MECH-270 stays candidate/v3_pending.
  location: docs/architecture/hippocampal_anchor_selection.md

- id: MECH-456
  title: "Entity binding is continuously re-evaluated, not fixed: an established self-anchor <-> world-configuration binding is re-bound to a competing configuration when that competitor overtakes the currently-bound one under perturbation of the shared anchor. Rebinding is the ENTITY-LAYER analogue of MECH-269(b) anchor-reset -- it operates over self<->world-config bindings rather than proposer stream-anchors. The rebinding trigger is prediction-error / competitor-overtake driven (object-file updating; serial-dependence hysteresis; PE-driven perceptual switching), NOT a coherence-specific selection factor C(tau) (that coherence-SPECIFICITY question is settled NO-CLAIM by V3-EXQ-725a and is explicitly excluded from this claim)."
  claim_type: mechanism_hypothesis
  subject: entities.rebinding_under_perturbation
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional/substrate_conditional
    as_of: 2026-07-20
    needs_review: false
    evidence:
      from: "failure_autopsy_morning-digest-742-744a-745-746-746a_2026-07-13#V3-EXQ-745"
      as_of: 2026-07-13
      verdict: non_contributory/substrate_ceiling
  epistemic_category: substrate_conditional
  implementation_phase: v3
  v3_pending: false
  claim_level: mechanistic
  registered_utc: 2026-07-10
  depends_on:
    - ARC-006   # entities are sparse, persistent, bindable structures (the binding this re-evaluates)
    - INV-002   # coherence includes temporal/phase binding (the coherence a binding maintains)
    - MECH-045  # object-file persistence (the buffer whose binding is re-evaluated)
    - MECH-269  # proposer anchor-reset -- the sibling mechanism at a DIFFERENT locus (stream-anchor, not entity binding)
    - MECH-270  # ephaptic verisimilitude readout (the binder substrate the rebinding probe reads)
  functional_restatement: >
    Mechanism hypothesis (entity-binding layer). ARC-006 asserts entities
    are bindable/persistent and INV-002 that coherence includes temporal
    binding, but neither names the DYNAMIC: what happens when the currently-
    bound configuration stops being the best-supported one. MECH-456 asserts
    that binding is maintained-not-intrinsic -- the binder monitors competing
    world-configurations against the current binding and re-binds when a
    competitor overtakes under perturbation of the shared anchor (z_self).

    Distinct from MECH-269(b). MECH-269(b) anchor-reset selects which LATENT
    STREAM anchors the proposer's rollout (mark_inactive + seek_new_anchor
    when effective verisimilitude drops). MECH-456 operates one layer over:
    which WORLD-CONFIGURATION is bound to the self-anchor. Same abstract
    mechanism (drop the current bound thing when a competitor becomes better),
    different locus; wired via depends_on, not a replacement.

    Biological grounding (non-formal-import). Object-file updating / reviewing
    (Kahneman/Treisman/Gibbs 1992; Sasi 2022): object correspondence is
    actively re-linked across time and perceptual gaps. Serial-dependence /
    illusory stability (Manassi & Whitney 2022): an online hysteresis biases
    the percept toward the recent past -- the stability counterweight rebinding
    must overcome, motivating a threshold rather than chatter. Prediction-error-
    driven switching (Weilnhammer 2017 bistable perception; Cole 2024 ACC PE
    drives single-trial state-switching): the competitor overtakes when the
    current config accrues prediction-error. Latent-cause inference (Gershman
    2010/2017): switch causes when the current one stops explaining the data.
    This grounding rests on E(tau) / stability machinery, NOT the coherence-
    C(tau) formal import that V3-EXQ-725a settled -- so MECH-456 does NOT
    inherit the formal-import caution.

    Substrate status. ALREADY BUILT + CONVERGED: the learned
    cross_stream_binder (ree-v3/ree_core/latent/cross_stream_binder.py) and
    its rebinding_probe() make the falsifier exercisable. V3-EXQ-725a observed
    n_rebind_total=1676 across 6 seeds (0 in every predecessor). BUT that is an
    EXERCISABILITY / non-degeneracy signal (a competitor CAN flip the argmax
    under anchor perturbation -- near-guaranteed for a trained bilinear scorer),
    NOT yet a demonstration that rebinding does behavioural work. The sibling
    MECH-269(b) precedent is load-bearing here: V3-EXQ-478/480 showed anchor
    resets fire abundantly (63/31 per seed) but freeze_recommit_count was
    bit-identical OFF=ON -- abundant rebinding events with no behavioural
    consequence read as inconclusive, not support. Promotion of MECH-456 is
    therefore gated on a FUNCTIONAL test, not on n_rebind > 0.
  what_would_answer: >
    A V3 experiment on the converged learned cross_stream_binder in which the
    environment presents a genuine competing-configuration overtake (the
    world-config that best matches the agent's realized situation changes
    mid-episode) must show BOTH: (1) the binder re-binds to the newly-correct
    configuration -- rebinding tracks the TRUE competitor, not arbitrary anchor
    noise, measured by rebinding-alignment-with-ground-truth above a shuffle
    control; AND (2) a behavioural / competence consequence -- rebinding-ON
    produces a downstream outcome difference (e.g. faster re-acquisition,
    fewer stale-binding errors) vs a rebinding-frozen arm, using a GRADED,
    non-saturating metric (per the V3-EXQ-725a frac_state_div saturation note).
    Unsupported if rebinding events fire but are bit-identical OFF=ON on the
    behavioural DV (the MECH-269(b) V3-EXQ-478 inert-reset signature), OR if
    rebinding does not track the true competitor above the shuffle control
    (arbitrary anchor-sensitivity, not functional rebinding).
  notes: >
    Registered 2026-07-10 via /claim-synthesis, salvaged from
    failure_autopsy_V3-EXQ-725a_2026-07-10 as the one decoupled positive of
    the 641a->720->725->725a coherence-nonreducibility lineage. Explicitly
    DECOUPLED from the coherence-SPECIFICITY question (candidate Q
    entities/selection.coherence_nonreducibility), which is settled NO-CLAIM by
    725a (SPEC 1/6; divergence reproduced by a contrast-matched shuffle) and is
    NOT re-opened by this claim. Origin thought: thought_intake_2026-04-23_
    binding.md sec 4 candidate MECH ("monitor coherence and rebind when a
    competing configuration overtakes the current one") -- registered here with
    the coherence-specific selection factor STRIPPED (settled no-claim) and the
    trigger re-grounded on prediction-error/competitor-overtake biology.
  evidence_quality_note: >
    Candidate, no functional experimental support yet. Substrate BUILT +
    CONVERGED (learned cross_stream_binder; rebinding_probe). V3-EXQ-725a
    (2026-07-10, diagnostic, claim_ids=[], non_contributory): rebinding
    positively EXERCISED (n_rebind_total=1676 across 6 seeds; 0 in all
    predecessors 641/641a/720; vacuous in 725) -- an exercisability /
    non-degeneracy result, NOT weighted as confidence and NOT a functional
    demonstration. Lit grounding (2026-07-10 /claim-synthesis pull, biology
    NON-formal-import): Kahneman/Treisman/Gibbs 1992 + Sasi 2022 (object-file
    updating/reviewing); Manassi & Whitney 2022 (serial-dependence / illusory
    stability -- hysteresis counterweight); Weilnhammer 2017 (predictive-coding
    bistable perception) + Cole 2024 (ACC prediction-error drives single-trial
    switching); Gershman 2010/2017 (latent-cause inference). Promotion requires
    the what_would_answer FUNCTIONAL test (rebinding tracks the true competitor
    above shuffle AND moves a graded behavioural DV) -- exercisability (n_rebind
    > 0) is explicitly insufficient per the MECH-269(b) V3-EXQ-478 inert-reset
    precedent.

    2026-07-10 (V3-EXQ-733, the functional what_would_answer test; evidence,
    claim_ids=[MECH-456]; consumed via /governance from
    failure_autopsy_V3-EXQ-733_2026-07-10): FAILed on the pre-registered
    readiness quorum ONLY (min overtakes/seed 2 vs 20; only seed 42 reached the
    floor) -> self-routed non_contributory, NOT a rebinding verdict and NOT
    weighted in confidence/conflict scoring. Root cause (autopsy): a cold,
    un-onboarded agent dies to hazards in ~10-40 steps (done=health<=0,
    episodes capped at 120 < 500) in the lethal SD-054 reef env, starving
    within-episode ground-truth region overtakes -- the binder itself is not at
    fault. The binder passed DV1 6/6 (rebinding tracks the true competitor above
    the shuffle control) and DV2 5/6 (graded re-acquisition-latency advantage
    ON vs frozen), both strongly and clear of margin on the one fully-ready seed
    (seed 42) -- ENCOURAGING for MECH-456, not refuting. Re-queued as
    V3-EXQ-733a (2-leg portfolio; supersedes V3-EXQ-733): P-A survival-onboarded
    agent via the validated scaffolded_sd054_onboarding curriculum so P1
    episodes survive long enough to accrue >=20 overtakes/seed; P-B
    directed-traversal test-bed that generates region-crossings independent of
    foraging survival. pending_retest = True. NOT co-blocked on the exhausted
    f_dominance_conversion_ceiling / v4_loop_segregation substrate -- the
    survival substrate needed here (scaffolded_sd054_onboarding) is already
    built + validated (P1 survival 3/3; V3-EXQ-603m/603n), so the repair is a
    re-queue over existing scaffold, not a new deep-substrate build. MECH-456
    stays candidate / v3_pending (V3-pending gate); no promotion/demotion.

    2026-07-11 (V3-EXQ-733a, 2-leg GOV-FANOUT-1 portfolio; consumed via
    /governance from failure_autopsy_V3-EXQ-733a_2026-07-11; both legs
    non_contributory/substrate_ceiling, NOT weighted): P-A (survival-onboarded)
    fixed survival but overtakes stayed below the readiness quorum and the binder
    stalled at chance on 2/6 seeds (a competent forager over-visits 2 of 4
    regions, suppressing coverage) -> readiness stays coupled to foraging
    survival. P-B (directed-traversal) PASSED every mechanism criterion (DV1 6/6,
    DV2 6/6, binder 6/6, overtakes 62-808, tracks truth 3-6x above shuffle with
    an 8-14 step graded advantage) and was blocked ONLY by the survival-coupled
    P0-coverage floor (min 9<10, one starved seed) -> narrow_supports_flag=True
    ("converts to SUPPORT on retest once P0-coverage is decoupled from
    survival"). Re-derive brake fired (2nd MECH-456 non_contributory) -> refused
    the P-A survival axis; prescribed a harness P0-coverage decoupling
    (directed_tour_p0_coverage) then ONE brake-permitted P-B retest.

    2026-07-12 (V3-EXQ-733b, the brake-permitted retest-after-substrate; evidence,
    claim_ids=[MECH-456]; consumed via /governance IGW-20260712-001): PASS /
    supports, non_degenerate. The directed-traversal P-B design run on the
    coverage-decoupled harness (directed_tour_p0_coverage=True) confirmed the
    733a P-B near-pass: this is MECH-456's FIRST functional rebinding
    demonstration (prior runs were exercisability-only / non_contributory). The
    substrate_conditional condition (a P0-coverage floor decoupled from foraging
    survival) is now SATISFIED and the functional what_would_answer test passed.
    V3-pending gate LIFTED (v3_pending: false) and MECH-456 PROMOTED candidate ->
    provisional. Positive base is a single PASS on the directed-traversal design;
    a confirmatory retest on an independent design / seed-set would strengthen
    but is not gating for provisional. decision_log: promote_to_provisional /
    applied.

    2026-07-12 (V3-EXQ-733c, the confirmatory retest; evidence,
    claim_ids=[MECH-456]; consumed via /governance review-tracker update): PASS /
    supports, non_degenerate. The 733b P-B directed-traversal design run
    BIT-IDENTICAL except the seed-set (independent disjoint [101-106] vs 733b's
    [42-47]); each seed independently seeds torch (agent init + P0 binder-training)
    and the env, so this is a genuine independent binder-convergence + latent-
    trajectory replication, NOT a re-run of identical computation. Clean second
    PASS: DV1 6/6 (rebinding tracks the true competitor above the shuffle control,
    alignment margins 0.15-0.32), DV2 6/6 (graded re-acquisition-latency advantage
    ON vs frozen, latency gaps 6.0-12.3 steps), binder converged 6/6, overtakes
    166-821/seed -- all load-bearing criteria met on all six seeds, clear of margin.
    Does NOT supersede 733b (adds an independent replication). VERDICT: CONSOLIDATE
    provisional -- NO status change. The replication removes the single-run /
    seed-luck caveat the 733b note flagged (seed-luck is now ruled out across two
    disjoint seed-sets, 12/12 on both DVs), materially strengthening confidence
    WITHIN the provisional band. It does NOT clear the bar for active/accepted:
    733c diversifies only the SEED half of the 733b note's "independent design /
    seed-set" bar and adds no design/test-bed variation -- both PASSes rest on the
    single directed-traversal harness whose teleport is a harness-level directed
    respawn (a deliberately artificial coverage-decoupled test-bed, not an
    ecological foraging context). Promotion to active should await an independent
    DESIGN / test-bed or an ecological (non-teleport-harness) behavioural
    consequence. PROMOTES/DEMOTES NOTHING; decision_state stays
    promote_to_provisional / applied (accurate -- no rung change).

    2026-07-13 (V3-EXQ-745, world-driven patch-flip + ecological couple()-path DV;
    evidence attempt, claim_ids=[MECH-456]; consumed via /governance from
    failure_autopsy_morning-digest-742-744a-745-746-746a_2026-07-13):
    NON_CONTRIBUTORY: readiness gate unmet -- the couple() path is behaviourally
    inert (disabling the binding does not move foraging; positive-control
    couple_authority below floor). Per the V3-EXQ-478 precedent a null DV2 on a
    couple-inert substrate is substrate_ceiling, NOT a rebinding refutation. Declared
    null; NOT weighted (no change to confidence/conflict scoring; DV1 affinity-tracks-
    config still works, only the ecological foraging DV2 is uninterpretable on this
    substrate). Distinct measurement axis from the braked 733/733a survival-onboarding
    axis. A re-queue must target a substrate whose couple() path carries foraging
    authority. MECH-456 stays provisional (unchanged by this non_contributory run).

    RE-DERIVE BRAKE (recorded for the standing granularity-debt scan): MECH-456 now
    has 3 substrate-limited autopsies -- 733 + 733a (survival-onboarding axis) + 745
    (world-flip / ecological couple-path axis), on DIFFERENT measurement axes. The
    user RAISED RE_DERIVE_BRAKE_THRESHOLD for MECH-456 from the default 2 to 3
    (threshold_raised_by_user_utc 2026-07-13T05:22:21Z), accepting the 745 author's
    different-axis argument that the substrate was legitimately explored across axes.
    count_including_this=3; brake does NOT fire on 745 (fired:false). The NEXT (4th)
    MECH-456 substrate-limited autopsy fires the brake. Machine-readable carrier of
    threshold=3 is the 745 target's re_derive_brake block in
    failure_autopsy_morning-digest-742-744a-745-746-746a_2026-07-13.json. NOTE: the
    standing scans (check_granularity_debt_recurrence.py GOV-GRAN-1 N=2;
    check_substrate_ceiling_audit.py GOV-CEIL-1 N=3) currently use hardcoded module
    constants and do NOT read a per-claim override -- and neither presently flags
    MECH-456 (GOV-GRAN-1 excludes substrate_ceiling-category autopsies; GOV-CEIL-1
    excludes MECH-456 because its epistemic_category is substrate_conditional, not
    substrate_ceiling), so there is no false-fire at count=3. This record + the
    autopsy carrier are the durable threshold=3 provenance for the next cycle / a
    future per-claim-override implementation.
  location: docs/architecture/entities_and_binding.md

- id: MECH-271
  title: "MECH-094's hypothesis tag is realized as differential downstream routing of hippocampal replay, not as a source-side boolean. Anchored replay (trusted starting state, MECH-269) preferentially reaches subiculum -> entorhinal deep layers -> neocortex (E1 consolidation) and lateral PFC (SD-033a viability map); probe replay (low-verisimilitude seed, strengthened tag) preferentially reaches BLA (affective tagging without consolidation) and NAc (curiosity / novelty scaffolding). Tag loss presents as probe content reaching consolidation destinations it should not reach -- an auditable confabulatory-planning signature distinct from psychosis or hallucination."
  claim_type: mechanism_hypothesis
  subject: hippocampus.hypothesis_tag_routing
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-271@2026-04-25T15:42:09.107861Z"
      as_of: 2026-04-25
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-04-21
  depends_on:
    - MECH-094  # the tag this claim reframes
    - MECH-269  # anchor/probe distinction (source of the routing difference)
    - ARC-007   # hippocampal path memory
    - ARC-018   # viability map (anchored destination)
    - SD-033a   # lateral PFC analog (anchored destination)
    - SD-035    # amygdala analog (BLA is probe destination)
  functional_restatement: >
    Reframing hypothesis. MECH-094 originally framed the hypothesis
    tag as a categorical write gate on phi(z) -- a source-side signal
    attached to simulated content that suppresses downstream writes
    during simulation and DMN. MECH-271 refines this: the tag is
    realized as a *routing difference* on the downstream fan-out of
    ephaptically-coordinated hippocampal replay.

    Routing prediction. Anchored replay (MECH-269, high regional
    verisimilitude) reaches the consolidation fan-out -- subiculum,
    entorhinal cortex deep layers, neocortex (E1 world-model update)
    -- and the lateral PFC analog (SD-033a viability / planning map).
    Probe replay reaches the BLA (affective tagging) and NAc
    (curiosity / novelty) fan-out, WITHOUT reaching consolidation
    destinations.

    Why this matters. An undifferentiated "tag" attached to all
    simulated content is hard to audit: we cannot directly measure
    whether the tag is present. A *routing difference* is
    instrumentable: probe content reaching EC / neocortex / lateral
    PFC consumers is observable as a specific failure signature.

    Tag loss = confabulation. When probe content reaches
    consolidation consumers (tag-erosion failure), the viability map
    accumulates fictional affordances. This is the candidate
    mechanistic signature of confabulatory planning, distinct from
    psychosis (different mechanism) and hallucination (different
    mechanism) -- see memory/feedback_psychosis_confabulation_distinction.

    Falsifiable (primary): a V3 experiment emitting anchored vs probe
    labels on replay events and logging which downstream consumers
    receive the replay content should show near-zero overlap of probe
    content reaching E1-consolidation or SD-033a-viability consumers
    under healthy operation; forcing tag erosion should re-create
    that overlap and produce the fictional-affordance pattern.
  epistemic_category: substrate_conditional
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: this claim presupposes the MECH-271 V3 routing
    substrate exists as live, wired machinery -- confirmed as of 2026-08-08 to NOT
    be the case. The routing-table form specified in
    docs/architecture/mech_271_routing_v3_substrate_plan.md (RoutingTable module,
    ReplayEvent.replay_kind/routing_weights extension, the destination dispatcher
    in REEAgent.consume_replay, the mech271_tag_loss_count audit hook, and the
    use_mech271_routing master flag) is UNIMPLEMENTED -- zero hits for
    use_mech271_routing / mech271_tag_loss_count / e1_consolidation /
    lateral_pfc_viability_map / bla_affective_tagging / nac_curiosity_scaffolding
    anywhere under ree_core/, and no ree_core/hippocampal/replay_routing.py exists.
    The plan's own stated release condition -- landing after the implement-
    mech269b lock on ree_core/hippocampal/module.py clears -- has itself not
    resolved: MECH-269b remains candidate/v3_pending as of 2026-07-11, with only
    a diagnostic-probe PASS (V3-EXQ-601, scoring_excluded) and its non-diagnostic
    behavioural-sufficiency retest (Q-040b) still outstanding. The claim's own
    proposed validation experiment, V3-EXQ-492, has never been queued.

    CONFIRMING (primary, once substrate lands -- V3-EXQ-492 or successor, 4-arm
    {A0_off, A1_routing_only, A2_forced_leak, A3_high_leak} per the design doc):
    (C1) master-flag-ON with clean substrate (force_tag_loss_rate=0.0) reduces
    consolidation writes sourced from probe replays by >=70% relative to the OFF
    baseline; (C2) the audit counter (mech271_tag_loss_count) tracks the
    deliberate leak-injection rate within +/-10% across ablation levels; (C3)
    full forced tag loss (rate=1.0) reproduces a confabulation behavioural
    signature -- >=2x increase in E1-prediction-divergence on held-out probe
    content relative to the clean-substrate arm; (C4) the OFF arm is
    bit-identical to the pre-flag baseline (within 1e-9 on all run metrics).

    CONFIRMING (secondary): BLA engagement (SD-035 substrate, V3-EXQ-473/474)
    should correlate with probe replay and not with anchored replay once routing
    is wired -- consistent with the strong literature cluster already registered
    (lit_conf=0.881) but not yet linked to a REE V3 run.

    FALSIFYING: (F1) with the master flag ON and a clean substrate, probe-replay
    content continues to reach consolidation destinations at rates
    indistinguishable from the OFF baseline (C1 fails) -- would falsify the
    routing-differentiation framing and support the original MECH-094 source-side
    boolean-gate framing instead. (F2) the audit counter does not track the
    injection rate (C2 fails) -- instrumentation failure, not evidence against the
    mechanism per se. (F3) forced full tag loss produces no measurable increase in
    E1-prediction-divergence on probe content (C3 fails) -- would falsify the
    specific clinical claim that routing-side tag loss produces an auditable
    confabulatory-planning signature, even if C1/C2 pass.

    GOVERNANCE FLAG (2026-08-08 digestion pass, not resolved here): two
    IGW auto-gating sessions cite conflicting blockers for this claim. One names
    the MECH-269b lock (matches this claim's own depends_on). The other (EXP-0269,
    gated 2026-08-02) names MECH-270 field-coherence readout, which is NOT in
    MECH-271's own depends_on and is explicitly V4-deferred per this claim's own
    design doc ("V3 approximates this with a discrete routing table... no
    ephaptic substrate... V4 adds the continuous phase-channel coordinate per
    MECH-228"). Looks like a conflation of the V3 minimum-viable form with the V4
    full realization. Net effect on testability is the same either way, but the
    two documented gating rationales disagree and should be reconciled.

    Falsifiable (secondary): the BLA analog landing under SD-035 is
    the natural probe-destination consumer. V3-EXQ-474 (BLA encoding
    + remap) + a follow-up probe-routing experiment should show BLA
    engagement correlates with probe replay, not with anchored
    replay.
  notes: >
    Design origin: user-led discussion 2026-04-21 that connected
    ephaptic coupling's known hippocampal fan-out targets to the REE
    module map. Anchored fan-out = E1 consolidation + SD-033a;
    probe fan-out = BLA + NAc. The mapping is one-to-one with
    structures REE is actively building, so this claim doubles as a
    validation target for the broader architecture.

    Does not deprecate MECH-094. MECH-094 stands as the functional
    tag; MECH-271 specifies how the tag is physically realized.
    Together they give tag presence/absence an auditable signature
    (routing destination) rather than an assertion about internal
    state.
  evidence_quality_note: >
    Candidate, no direct experiment yet. Natural validation pathway
    via SD-035 amygdala analog (V3-EXQ-474) once anchor/probe labels
    are emitted in HippocampalModule. Biology lit-pull should target
    hippocampal projection specificity during SWR replay -- BLA
    engagement during replay (Girardeau et al 2017 is the likely
    entry point) and prefrontal engagement during replay (Jadhav
    et al 2016) as the empirical anchors for the routing
    differentiation.
  v3_substrate_plan_utc: "2026-04-26"
  v3_substrate_note: |
    V3 substrate landing plan committed 2026-04-26. Discrete routing-table form
    (anchored vs probe replay -> destination weights for e1_consolidation /
    lateral_pfc_viability_map / bla_affective_tagging / nac_curiosity_scaffolding /
    e3_planning_evaluation), destination-dispatcher in REEAgent.consume_replay,
    audit hook (mech271_tag_loss_count) that fires when probe content reaches
    anchored-only destinations. Master flag use_mech271_routing (default OFF, bit-
    identical contract preserved). Validation V3-EXQ-492 (proposed, queue after
    implement-mech269b lock releases): 4-arm with force_tag_loss_rate ablation
    {0.0, 0.5, 1.0} for confabulation behavioural signature. The V4 form
    (phase-channel routing via ephaptic field strength, MECH-228) remains v4-deferred;
    V3 routing table is the synaptic approximation. Code landing scheduled for a
    follow-up /implement-substrate session after implement-mech269b releases its lock
    on ree-v3/ree_core/hippocampal/module.py (MECH-271 also depends on MECH-269b
    substrate, so the ordering is appropriate). Full plan:
    docs/architecture/mech_271_routing_v3_substrate_plan.md. V3/V4 boundary:
    docs/architecture/v3_v4_phase_substrate_boundary.md.
  location: docs/architecture/mech_271_routing_v3_substrate_plan.md

- id: MECH-272
  title: "MECH-271 routing is state-gated. In waking, the anchor channel dominates the hippocampal fan-out: replay preferentially reaches consolidation and viability-map consumers to support decision-making. In sleep, the probe channel dominates: replay preferentially seeds Bayesian schema restructuring -- testing which action-outcome attributions held up across episodes, creating new buckets for non-fitting experiences, revising causal graphs. The informational needs of sleep (schema revision) and waking (decision support) are distinct, and the routing bias reflects that."
  claim_type: mechanism_hypothesis
  subject: hippocampus.state_gated_routing
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-272@2026-06-23T23:35:50.687606Z"
      as_of: 2026-06-23
      verdict: promote_to_provisional/applied
  status_note: >
    [enum-comment migrated to field 2026-07-18] status: gov 2026-06-23T23:28Z: promote
    candidate->provisional (exp_conf 0.824, conflict_ratio 0, 2 exp entries, supports-only;
    v3_pending gate cleared by V3-EXQ-702)
  implementation_phase: v3
  v3_pending: false
  v3_pending_cleared_note: "2026-06-23 governance: v3_pending gate cleared on the strength of V3-EXQ-702 (sleep_substrate:GAP-3b) -- discriminative ARM_ON-vs-OFF use_sleep_aggregation_cluster run, 3/3 seeds supports with genuine ARM_OFF zero. The V3 substrate the gate was waiting for now exists and produced scoreable evidence; impl_phase stays v3 (v3_run_ct>=1 so no re-hold). Promotion not applied -- evidence now counts toward the candidate->provisional gate."
  claim_level: mechanistic
  registered_utc: 2026-04-21
  depends_on:
    - MECH-271  # the routing claim this qualifies
    - MECH-269  # anchor/probe distinction
    - MECH-094  # hypothesis tag
    - MECH-092  # micro-DMN replay
    - MECH-285  # V_s residual priority biases sleep-regime replay scheduling
  functional_restatement: >
    Waking regime: the proposer is supporting decisions, so the
    routing bias is toward anchored rollouts reaching consolidation
    and viability-map consumers (SD-033a, E1). Probe events happen
    (micro-quiescence, MECH-092) but under a strong hypothesis tag
    that prevents downstream writes to commitment-relevant
    consumers.

    Sleep regime: the computational task changes from "make a
    decision" to "revise the schema decisions run against". This is
    full-Bayesian work -- testing which action-outcome attributions
    held up across episodes, creating new buckets for experiences
    that did not fit existing schemas, revising causal graphs. Probe
    content is recruited heavily because novel combinations need to
    be tested; the hypothesis tag remains on to preserve the
    distinction between tested-and-accepted (consolidation-routed)
    and tested-but-not-committed (remains in repertoire).

    Falsifiable (primary): V3 experiments comparing replay-to-consumer
    routing during wake vs simulated-sleep phases should show the
    dominance shift -- anchored-channel share of routing should drop
    in sleep, probe-channel share should rise. Falsifiable (secondary):
    under state-gating loss (running a REM-like protocol in waking
    state), routing should lose the regime-appropriate bias.

    Empirical hint: Tang et al 2017 show awake and sleep SWRs differ
    in cortical reactivation quality. MECH-272 interprets this as
    reflecting the functional regime switch, not an incidental
    difference.
  notes: >
    Design origin: user-led discussion 2026-04-21 noting that sleep
    and waking serve distinct informational needs (sleep = schema
    revision / Bayesian restructuring, waking = decision-support
    using existing schemas). The routing architecture of MECH-271 as
    originally drafted was silent on state; this claim adds the
    state-dependence.
  evidence_quality_note: >
    Candidate, no direct experiment yet. Natural validation pathway
    is a paired wake vs sleep-analog phase in the V3 hippocampal
    proposer, with routing-to-consumer logging on both sides and
    the dominance-shift as the target signature.
  implementation_note: >
    Substrate design committed 2026-04-25 in
    docs/architecture/sleep_aggregation_cluster.md (umbrella doc for
    MECH-272 / MECH-273 / MECH-275 / MECH-285). Lands at Phase 3 of the
    cluster build order as ree_core/sleep/routing_gate.py: a state-
    conditioned channel-weight gate consumed by the existing MECH-271
    HippocampalRouter. Flag use_mech272_routing default False;
    bit-identical waking with flag OFF.
  location: docs/architecture/sleep_aggregation_cluster.md

- id: MECH-273
  title: "The self-model has a waking half and a sleep half. The waking half is SD-003 (single-episode counterfactual self-attribution via E2). The sleep half is full-Bayesian aggregation of single-episode SD-003 outputs across many episodes, routed by the anchored channel to E1 and SD-033a, correcting for delayed consequences, failed counterfactuals, and systematic attribution biases. Without the sleep half, REE has an episode-local causal signature but not a durable self-model."
  claim_type: mechanism_hypothesis
  subject: self_model.sleep_dependent_aggregation
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-273@2026-06-23T23:35:50.729543Z"
      as_of: 2026-06-23
      verdict: promote_to_provisional/applied
  status_note: >
    [enum-comment migrated to field 2026-07-18] status: gov 2026-06-23T23:28Z: promote
    candidate->provisional (exp_conf 0.824, conflict_ratio 0, 2 exp entries; 1 mixed lit entry
    but conflict_ratio 0; v3_pending gate cleared by V3-EXQ-702)
  implementation_phase: v3
  v3_pending: false
  v3_pending_cleared_note: "2026-06-23 governance: v3_pending gate cleared on the strength of V3-EXQ-702 (sleep_substrate:GAP-3b) -- 3/3 seeds supports for the self-model replay-derived E2_harm_s param-delta (MECH-273 criterion) with genuine ARM_OFF zero movement. impl_phase stays v3 (v3_run_ct>=1, no re-hold). Promotion not applied -- evidence now counts toward the candidate->provisional gate."
  claim_level: mechanistic
  registered_utc: 2026-04-21
  depends_on:
    - SD-003   # the waking-half attribution this aggregates
    - MECH-275 # general aggregation mechanism this specialises
    - MECH-272 # state-gated routing framework
    - MECH-271 # routing architecture
    - INV-049  # offline phases are mathematically necessary for model-building agents
    - ARC-007  # hippocampal path memory (substrate for aggregation)
    - SD-033a  # viability-map revision consumer
    - MECH-285 # V_s residual priority biases which self-attribution episodes get aggregated first
  functional_restatement: >
    SD-003 gives the agent a causal signature per episode: in this
    episode, this action looks self-initiated with probability p.
    Single-episode attribution misses:

    (1) Delayed consequences where the action-outcome link spans
        more episodes than a single waking rollout encompasses.
    (2) Failed counterfactuals where the counterfactual action would
        have produced the same outcome -- the action felt voluntary
        but was not causally efficacious.
    (3) Systematic attribution biases that a single episode cannot
        distinguish from noise but aggregate evidence across episodes
        can detect and correct.

    MECH-273: during sleep, single-episode SD-003 outputs are
    aggregated into a stable self-model via full-Bayesian revision.
    Anchored-channel routing (MECH-271, MECH-272) delivers the
    aggregated output to E1 consolidation (world-model revision) and
    SD-033a (viability-map revision of which actions the agent can
    credit to itself). This is how REE goes from episode-local causal
    signature to durable sense of self.

    Falsifiable (primary): ablating the sleep-phase aggregation step
    should leave single-episode SD-003 intact but degrade stability
    of self-attribution across episodes. The agent should be less
    able to correct previously-held spurious self-attributions when
    post-hoc evidence accumulates against them.

    Falsifiable (secondary): the pattern should generalise. Any
    attribution whose stable form requires multi-episode Bayesian
    revision should show the same sleep-dependence.
  notes: >
    Design origin: user-led discussion 2026-04-21 noting that V3 has
    probabilistic self-attribution (SD-003) but that a stable sense of
    self requires aggregating across many episodes and catching causal
    loops the single-episode attribution misses. INV-049 ("sleep is a
    mathematical necessity for model-building agents") sits directly
    under this claim -- MECH-273 specifies the content offline phases
    are doing for the self-model case.
  evidence_quality_note: >
    Candidate, no direct experiment yet. Natural validation pathway
    is a V3 experiment that runs SD-003 across many episodes with and
    without a sleep-phase aggregation step, and tests whether the
    agent can correct a seeded spurious self-attribution when
    counter-evidence accumulates.
  implementation_note: >
    Substrate design committed 2026-04-25 in
    docs/architecture/sleep_aggregation_cluster.md (umbrella doc for
    MECH-272 / MECH-273 / MECH-275 / MECH-285). Lands at Phase 5 of the
    cluster build order as ree_core/sleep/self_model_aggregator.py: a
    subclass of the MECH-275 GeneralBayesianAggregator specialised on
    SD-003 causal_sig posterior, with offline gradient writeback to
    E2_harm_s (low LR, MECH-094 simulation-mode tag enforced). Flag
    use_mech273_self_model default False. Validation experiment EXP-0169
    template (already in experiment_proposals.v1.json) runs after Phases
    A-D land. Depends on substrate already shipped: SD-003 + ARC-033
    E2_harm_s + SD-013 contrastive training.
  location: docs/architecture/sleep_aggregation_cluster.md

- id: MECH-274
  title: "V4-reserved. The sleep-dependent aggregation pattern of MECH-273 extends to other-attribution. Fast empathy (ARC-010) produces single-episode attributions of other-agent action-outcome links; a stable model of other minds requires full-Bayesian aggregation of those attributions during sleep, architecturally parallel to MECH-273 but operating on other-agent rather than self attributions."
  claim_type: mechanism_hypothesis
  subject: other_model.sleep_dependent_aggregation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  phase_provenance: derived
  phase_derived_from: ARC-059
  v3_pending: false
  claim_level: mechanistic
  registered_utc: 2026-04-21
  depends_on:
    - MECH-273  # self-model parallel
    - MECH-275  # general aggregation mechanism this specialises
    - ARC-010   # social.mirror_modelling (V4)
    - MECH-272  # state-gated routing
    - INV-049   # offline-phase necessity
  functional_restatement: >
    ARC-010 and the fast-empathy system will produce single-episode
    attributions of other-agent action-outcome links in V4: "agent j
    did action a, which looks causally responsible for outcome o."
    By the same argument as MECH-273, these single-episode
    attributions need sleep-phase Bayesian aggregation before they
    stabilise into a model of agent j's dispositions and causal
    powers. Without the aggregation step, V4's other-model is
    episode-local, which is not a stable theory of other minds.

    V4 scope. Not for V3 implementation. Registered now to reserve
    the mechanism and prevent V4 from rediscovering it independently.
    The architectural parallel to MECH-273 means V4 should be able to
    implement it as a structural extension rather than a new design.

    Falsifiable (when V4 lands): ablating the sleep-phase
    aggregation step for other-agent attributions should leave the
    fast empathy system (single-episode ARC-010 output) intact but
    degrade the stability of other-agent attributions across
    episodes, the same failure mode MECH-273 predicts for self.
  notes: >
    V4-reserved. Design origin: user-led discussion 2026-04-21
    extending the MECH-273 self-model argument to the other-attribution
    case. Architectural parallel is exact; only the attribution target
    differs (self vs other).
  evidence_quality_note: >
    V4 scope. No V3 experiments will validate this claim directly --
    it requires V4's social/empathy system to be implemented first.
    Biology lit-pull for V4 should target sleep-dependent social
    learning in rodents and humans.
  location: docs/architecture/hippocampal_anchor_selection.md

- id: ARC-059
  title: "REE developmental training proceeds in three ordered stages: (1) self-as-object — action-space discovery via motor experimentation, producing the substrate SD-003 requires; (2) objects-as-patterns — object-schema formation via experimental action on the world; (3) others-as-special-objects — modelling other agents as objects with their own policies. The ordering is load-bearing: without a self-model the agent cannot distinguish self-produced from world-produced sensory change; without object-schemas the agent cannot recognise other-agents as a distinguished subclass of objects. Refines the generic ARC-019 curriculum-gates claim."
  claim_type: architectural_commitment
  subject: developmental.three_stage_ordering
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: 2026-04-21
  depends_on:
    - ARC-019   # generic staged curriculum this refines
    - MECH-276  # scientist-agent principle, mechanism across all stages
    - MECH-277  # stage-1 specialisation (action-space discovery)
    - MECH-278  # stage-2 specialisation (object-schema formation)
    - ARC-010   # stage-3 substrate (V4)
    - MECH-274  # stage-3 sleep-aggregation (V4)
    - SD-003    # stage-1 closure (counterfactual self-attribution) -- NOTE (2026-08-07 digestion): SD-003 status is superseded (by MECH-256, SD-029) as of 2026-04-18, three days before this claim was registered; functional_restatement/notes still frame this in terms of SD-003. Not changed here (extract-don't-invent scope); flag for governance.
    - SD-005    # z_self / z_world split (currently engineered; developmental construction flagged)
  epistemic_category: substrate_conditional
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: this ordering claim is not yet
    constructible as a clean ablation on V3 as built, and two distinct
    gates must clear before either test below is informative. (1) V3
    currently hand-wires the action space (MECH-277's own note: "V3
    hand-wires action knowledge into the agent... MECH-277 is not
    actively tested in V3"), so a literal "withhold stage 1" manipulation
    must specifically degrade or ablate the counterfactual-backed self/
    world attribution mechanism the agent would otherwise use -- not
    merely truncate an arbitrary phase's episode count while the action
    space stays fully specified underneath. (2) The MECH-276 scientist-
    agent counterfactual-backed attribution feedstock that MECH-278's
    stage-2 object-schema signal depends on is not itself substrate-
    ready: V3-EXQ-703 (2026-06-23, confirmed failure-autopsy) found both
    of its readiness preconditions unmet -- the P0 world-forward
    comparator did not converge (world_forward_r2 mean -0.0547, 1/3 seeds
    cleared the 0.20 threshold) and cf_margin was calibrated above the
    achievable contrast band (0/3 seeds straddled backed/correlational).
    This is the same untrained-P0-world-forward confound as
    V3-EXQ-701/642, not a missing MECH-276 mechanism. The recommended
    re-queue (converged P0 + recalibrated cf_margin, tentatively
    V3-EXQ-703a) has not been run as of this draft. This is a different,
    narrower substrate lineage than the SD-049 / scaffolded_sd054
    foraging-competence family -- confirmed not the same substrate; do
    not conflate the two. Until (1) and (2) both clear, a "stage-2-
    without-stage-1" run that fails to form stable object-schemas is not
    distinguishable from ordinary undertrained-base-representation noise,
    and should self-route substrate_not_ready rather than count as a
    verdict either way.

    PRIMARY (skip-ordering ablation, per the claim's own falsifier): given
    the precondition holds, an agent given stage-2 training (MECH-278
    experimental-action object-schema formation) before stage-1 (MECH-277
    action-space discovery / self-world attribution) is meaningfully
    complete.

    Confirming signature: the stage-2-before-stage-1 arm shows measurably
    degraded, noisier, or less stable object-schema formation than a
    matched stage-1-then-stage-2 arm (same total budget, same eventual
    environment/feature set) -- because self-produced and world-produced
    sensory change cannot yet be reliably distinguished. This would
    confirm the ordering is load-bearing, not merely a convenient
    scaffold.

    Falsifying signature: the stage-2-before-stage-1 arm forms object-
    schemas statistically indistinguishable in stability/noise from the
    properly-ordered arm -- object-schema formation does not measurably
    depend on prior self/world disambiguation. This would undercut the
    claimed dependency between MECH-277 and MECH-278 even if the
    individual stage mechanisms each hold in isolation.

    SECONDARY (SD-005 shortcut-validity check, per the claim's own
    falsifier -- V4-scoped, and a validity check on OTHER V3 claims that
    depend on SD-005's engineered z_world, not only on ARC-059 itself):
    given the precondition holds AND MECH-277's self/world distinction is
    genuinely live (not hand-wired), ablate the engineered z_world
    (SD-005) and require the agent to reconstruct an object-schema
    decomposition via experimental (interventional) action alone.

    Confirming signature: the reconstructed decomposition matches the
    engineered SD-005 split -- features the engineered version groups as
    one object are recovered as causally coherent bundles under
    intervention, and vice versa. This shows the V3 engineered-z_world
    shortcut was benign for every other V3 claim resting on SD-005's
    z_world as a stand-in for developmentally-formed object
    representation.

    Falsifying signature: the reconstructed decomposition diverges
    materially from the engineered split -- meaning the engineered
    z_world smuggled assumptions the agent would not independently form,
    and any OTHER V3 claim resting on SD-005's split needs re-examination
    once V4's genuine experimental-action construction is available.

    Evidence status (2026-08-07): no V3 experiment has queued or run
    either prediction. MECH-276 landed substrate 2026-06-23 but its own
    readiness diagnostic (V3-EXQ-703) FAILed and the recommended re-queue
    has not been run. MECH-277 and MECH-278 have no experiments on file
    at all -- V3 hand-wires the former and engineers the latter's target
    representation. Do not treat any current V3 result as bearing on this
    claim absent a run that actually satisfies the non-degeneracy
    precondition above.
  functional_restatement: >
    The agent must discover its own action space before it can form
    object-schemas, and it must have object-schemas before it can
    recognise other-agents as special objects. Stage 1 is self-as-
    object-of-study: motor babbling and action-space discovery
    (MECH-277) produce the substrate of "actions the agent has reason
    to believe are self-initiated", which SD-003 counterfactual
    attribution presupposes. Stage 2 is object-formation via
    experimental action (MECH-278): once self-produced change can be
    distinguished from world-produced change, the agent perturbs the
    world and observes which features behave causally together. Stage
    3 (V4) is social modelling: other-agents as a subclass of objects
    whose internal dynamics resist simple interventional attribution.

    Falsifiable (primary): skip-ordering experiments. An agent given
    stage-2 training before stage 1 is meaningfully complete should
    fail to form stable object-schemas, because self-produced and
    world-produced change cannot be distinguished.

    Falsifiable (secondary): the engineered z_world shortcut (SD-005)
    lets V3 skip stage 2. Ablating the engineered z_world and
    requiring experimental-action reconstruction should produce a
    decomposition that either matches the engineered version (the
    shortcut was benign) or diverges (the shortcut was smuggling
    assumptions the agent would not make).
  notes: >
    Design origin: user-led discussion 2026-04-21 following MECH-272/
    273/274 registration. The conversation noted that MECH-273's
    aggregation pattern generalises, but not arbitrarily -- the
    generalisation requires counterfactual-backed attribution
    (MECH-276) and developmental prerequisites (this claim). ARC-019
    was too generic to carry the specific ordering; ARC-059 refines
    it without disturbing the provisional status of ARC-019.

    V3 architectural shortcut: z_world is provided pre-split in V3
    (engineered SD-005). V3 validity is not in question -- V3 claims
    the mature architecture, not the developmental construction. But
    the shortcut is flagged: V4 should test whether experimental
    action reconstructs a matching z_world, or whether the engineered
    version smuggles assumptions.
  evidence_quality_note: >
    Candidate, registered to fix the ordering and V3 architectural
    shortcut. No V3 experiment validates ARC-059 end-to-end. Stage-1
    and stage-2 experiments exist in principle; stage-3 is V4.
  location: docs/architecture/scientist_agent_developmental_ordering.md

- id: MECH-275
  title: "Sleep-phase full-Bayesian aggregation is a general mechanism across attribution domains: given a waking-phase feedstock of counterfactual-backed attributions (produced by MECH-276), sleep phases aggregate across many episodes, correcting for delayed consequences, failed counterfactuals, and systematic attribution biases that single-episode attribution cannot catch. MECH-273 (self-model aggregation) and MECH-274 (other-model aggregation, V4) are specialisations. Object-schema revision is a further specialisation. INV-049 (offline phases mathematically necessary for model-building agents) is operationalised here: what sleep is mathematically necessary FOR is this cross-episode Bayesian aggregation of counterfactual-backed attribution."
  claim_type: mechanism_hypothesis
  subject: sleep.general_bayesian_aggregation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-275@2026-04-25T15:42:09.107868Z"
      as_of: 2026-04-25
      verdict: hold_pending_v3_substrate/applied
  epistemic_category: substrate_conditional
  governance_2026_06_10: >
    epistemic_category set substrate_conditional (IGW experiment-surfacing
    suppression, not a promote/demote). Sleep-phase cross-domain Bayesian
    aggregation depends on upstream substrate that is planned-but-unbuilt: the
    MECH-276 counterfactual-backed attribution feedstock (the critical precondition
    in functional_restatement) plus the SD-017 sleep-phase retest path, whose cohort
    (sleep_substrate:GAP-2) is upstream-blocked on the unbuilt ARC-065
    rule-creator/discriminator substrate. The right response is to wait for the
    upstream substrate, not to surface EXP-0130 as ready /queue-experiment work.
    status unchanged (candidate); claim_type unchanged.
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-04-21
  depends_on:
    - MECH-276  # counterfactual-backed attribution feedstock (critical precondition)
    - MECH-272  # state-gated routing (sleep regime)
    - MECH-271  # routing architecture
    - MECH-273  # self-model specialisation
    - INV-049   # offline-phase necessity (operationalised here)
    - ARC-007   # hippocampal path memory (substrate)
    - MECH-285  # V_s residual priority biases replay scheduling (epistemic-priority arm of aggregation)
  functional_restatement: >
    MECH-275 generalises MECH-273's self-model aggregation. The input
    is not arbitrary correlation; it is structured attribution
    produced by MECH-276 (waking-phase counterfactual-backed
    intervention). The output is schema revision: updated priors,
    new buckets for experiences that did not fit existing schemas,
    revised causal graphs.

    The input-requirement is critical. Aggregating arbitrary
    correlations would not produce schema revision -- it would
    produce noise-fit. MECH-275 is only coherent if the waking-phase
    feedstock is structured by deliberate intervention.

    Specialisations:
    - Self-attribution (MECH-273): aggregate SD-003 outputs across
      episodes, correcting spurious self-attributions.
    - Other-attribution (MECH-274, V4): aggregate ARC-010 outputs
      across episodes, stabilising theory of other minds.
    - Object-schema revision (stage 2, MECH-278): aggregate
      experimental-action outcomes across episodes, revising the
      object decomposition.

    Falsifiable (primary): ablating the sleep-phase aggregation step
    should leave waking-phase attribution intact but degrade
    cross-episode stability for whichever specialisation is being
    tested. Falsifiable (secondary): feeding MECH-275 with
    correlational (non-counterfactual-backed) inputs should produce
    noise-fit rather than schema revision, distinguishing it from
    naive cross-episode averaging.
  what_would_answer:
    non_degeneracy_precondition: >
      The MECH-276 waking-phase counterfactual-backed attribution feedstock must be
      demonstrably operative before any MECH-275 aggregation test is interpretable:
      (i) the comparator's world_forward (P0) predictor must be trained to a genuine
      predictive R2 (>=0.2 on >=2/3 seeds); and (ii) the counterfactual-margin
      threshold (cf_margin) must sit within the trained comparator's achievable
      cf_contrast distribution. STATUS (2026-08-08): NOT YET MET. V3-EXQ-703
      (2026-06-23) ran this exact readiness diagnostic and FAILed both
      preconditions -- world_forward_r2 measured -0.055 (mean; per-seed
      -0.688/-0.057/+0.581, only 1/3 seeds converged) against a 0.20 floor, and
      cf_margin (fixed at 0.30) sat above the achievable ~0.12-0.20 cf_contrast
      band so 0/3 seeds straddled. Self-route was substrate_not_ready_requeue
      (correctly adjudicated by a confirmed failure-autopsy as a P0-training
      confound, NOT a substrate-ceiling reading -- the load-bearing
      posterior-discrimination criterion itself PASSED non-degenerately on all 3
      seeds, positively ruling out "feedstock genuinely absent"). The
      autopsy-recommended re-queue (new letter, e.g. V3-EXQ-703a: reconstruction-
      primary P0 training + raised budget + per-seed R2 gate + cf_margin
      recalibrated to ~0.15) has NOT been queued or run as of this check, ~6 weeks
      after being recommended, and no chip currently tracks it. Chip spawned.
    confirming: >
      A V3-EXQ-703-successor run that clears BOTH readiness preconditions AND
      whose load-bearing posterior-discrimination criterion still passes
      non-degenerately on the now-trained comparator would confirm MECH-276
      feedstock readiness. That result unblocks (does not itself promote) the
      separate MECH-275 sleep-aggregation promotion run and re-adds MECH-275 to
      sleep_substrate:GAP-3b's unblocks_claims. The promotion run itself would
      then need to show: ablating the sleep-phase aggregation step leaves
      waking-phase attribution intact but degrades cross-episode stability for a
      MECH-275 specialisation; and/or that feeding the aggregator correlational
      (non-counterfactual-backed) MECH-276 records produces measurable noise-fit
      rather than schema revision.
    falsifying: >
      (1) If a readiness-diagnostic successor clears both preconditions on a
      genuinely trained P0 comparator but the load-bearing posterior-discrimination
      criterion then COLLAPSES -- this is explicitly flagged in the confirmed
      V3-EXQ-703 autopsy as the one reading NOT reachable from the current
      confounded run, and would constitute a genuine MECH-275-aggregator-
      insensitivity substrate ceiling, routing to substrate enrichment rather
      than re-queue. (2) Ablating the sleep-phase aggregation step failing to
      degrade cross-episode stability once the mechanism is genuinely tested.
      (3) Feeding correlational input producing schema revision indistinguishable
      from counterfactual-backed input, collapsing MECH-275's central
      input-structure claim into naive averaging.
  notes: >
    Design origin: user-led discussion 2026-04-21. The naive
    generalisation of MECH-273 (sleep aggregates many attributions,
    not just self) is correct in spirit but needs the input
    requirement -- aggregation is only coherent over counterfactual-
    backed attribution. User correction pushed this claim into
    existence: "probably needs to be linked to consequential reasoning
    which requires experiments or self generated consequences".

    INV-049 (sleep mathematically necessary) sits directly under
    MECH-275. Before MECH-275, INV-049 was a necessity claim without
    a content specification. MECH-275 specifies the content: the
    aggregation that is necessary is of counterfactual-backed
    attribution, not arbitrary evidence.
  evidence_quality_note: >
    Candidate, no direct experiment yet. Natural validation is the
    generalisation of the MECH-273 experiment template, parameterised
    over which attribution domain is being aggregated. Distinguishing
    MECH-275 from naive averaging requires the counterfactual-vs-
    correlational control.
  implementation_note: >
    Substrate design committed 2026-04-25 in
    docs/architecture/sleep_aggregation_cluster.md (umbrella doc for
    MECH-272 / MECH-273 / MECH-275 / MECH-285). Lands at Phase 4 of the
    cluster build order as ree_core/sleep/bayesian_aggregator.py:
    GeneralBayesianAggregator with per-domain Gaussian posteriors,
    consumer of the MECH-272 probe channel during sleep cycles. V3
    domains: 'self' (specialised by MECH-273) and 'place' (Bayesian
    upgrade of MECH-284 leaky integrator). Object/social domains
    deferred to V4. Flag use_mech275_aggregator default False.
    Validation EXP-0169 PASS unblocks the cluster.
  implementation_note_mech276_feedstock_2026_06_23: >
    The critical upstream substrate -- the MECH-276 counterfactual-backed
    attribution waking-phase feedstock named in functional_restatement /
    governance_2026_06_10 as the precondition without which "aggregating
    arbitrary correlations would produce noise-fit" -- LANDED 2026-06-23
    (ree-v3/ree_core/attribution/scientist_attribution_buffer.py; the sleep loop
    reads it as the aggregator evidence when use_scientist_attribution is on).
    MECH-275 STAYS candidate / substrate_conditional / v3_pending: the feedstock
    now exists but the MECH-275 sleep-aggregation promotion run is still owed --
    it is queued only AFTER the MECH-276 substrate-readiness diagnostic
    (V3-EXQ-703) PASSes, as a sibling of v3_exq_702_gap3b_sleep_cluster_promotion.py
    extended with a MECH-275 cross-episode posterior-aggregation discriminative
    metric over the now-real feedstock. sleep_substrate:GAP-3b re-adds MECH-275 to
    unblocks_claims once that run scores. PROMOTES NOTHING here.
  location: docs/architecture/sleep_aggregation_cluster.md

- id: MECH-276
  title: "The scientist-agent principle: the waking-phase mechanism that feeds MECH-275 is deliberate intervention. The agent generates hypotheses (proposer-side, via MECH-269 probe channel), selects actions that will produce informative contrast under those hypotheses, executes the actions, observes the outcomes, and computes counterfactual-backed attribution. Correlational observation is insufficient -- the agent must behave like a scientist, performing experiments whose outcomes produce structured attribution signals. Applies across all three developmental stages; specialised by MECH-277 (action-space discovery), MECH-278 (object-schema formation), and a V4 social-intervention mechanism (parallel to MECH-278 for other-agents)."
  claim_type: mechanism_hypothesis
  subject: agent.scientist_intervention
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  epistemic_category: standard
  claim_level: mechanistic
  registered_utc: 2026-04-21
  depends_on:
    - MECH-269  # probe channel as proposer-side hypothesis generator
    - MECH-275  # sleep aggregation is the consumer of this mechanism's output
    - SD-003    # STALE (2026-08-08 digestion pass): SD-003 is status:superseded (by MECH-256/SD-029, 2026-04-18, three days before this claim was registered). The built substrate (scientist_attribution_buffer.py) already composes MECH-256's single-pass comparators, not SD-003's two-pass counterfactual pipeline. Retained for history; treat MECH-256/SD-029 as the operative dependency.
    - MECH-277  # stage-1 specialisation
    - MECH-278  # stage-2 specialisation
  functional_restatement: >
    Three-stage loop: (1) hypothesis generation -- MECH-269 probe
    channel seeds candidate trajectories from low-verisimilitude /
    high-PE regions. These are the "what if" proposals the agent does
    not yet have a settled model for. (2) Consequential action --
    the agent actually performs the action in the world (not just a
    rollout), with the probe-hypothesis tag indicating the attribution
    is provisional until outcome. (3) Counterfactual attribution --
    upon outcome, the agent computes the contrast between what
    happened and the counterfactual, producing a structured
    attribution signal (SD-003 at stage 1, object-schema update at
    stage 2, other-agent model update at stage 3).

    The closure of this loop is what separates MECH-276 from pure
    exploration. Pure exploration visits new states; MECH-276 visits
    new states in a hypothesis-structured way, producing attribution
    signals fit for MECH-275 aggregation.

    Falsifiable (primary): ablating the hypothesis-generation step
    (MECH-269 probe channel suppressed, agent only visits states it
    has anchor for) should starve MECH-275 of its feedstock --
    cross-episode schema revision should stall. Falsifiable
    (secondary): replacing counterfactual-backed attribution with
    pure correlation at the waking-phase output should produce
    noise-fit in subsequent sleep-phase aggregation.
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: the mechanism is built
    (ree-v3/ree_core/attribution/scientist_attribution_buffer.py,
    landed 2026-06-23) but gated behind use_scientist_attribution
    (default False -> bit-identical OFF), so a genuine test requires
    the flag ON. Two further preconditions must ALSO hold simultaneously,
    per V3-EXQ-703's confirmed failure-autopsy (2026-06-23): (1) the P0
    E2WorldForward comparator (SD-031 "place" / ARC-033 "self") must
    actually converge -- held-out world_forward_r2 >= 0.20 on >= 2/3
    seeds, gated per-seed, not run-level; V3-EXQ-703 trained only 1/3
    seeds (R2 -0.688 / -0.057 / +0.581) because the P0 objective was not
    reconstruction-primary and the budget was insufficient. (2) cf_margin
    must be calibrated WITHIN the trained comparator's achievable
    cf_contrast distribution (measured ~0.12-0.20 in the one converged
    seed), not above it -- V3-EXQ-703's cf_margin=0.30 sat above the
    entire distribution, so mech276_n_counterfactual_backed=0 on every
    seed even on the one seed whose P0 converged. Without both, the
    only_counterfactual_backed lever cannot produce a single backed
    record and the test collapses into a vacuous all-correlational
    feedstock -- not a substrate reading either way. Chip already spawned
    (chip-20260808-v3exq703a-mech276-readiness-requeue) to queue the
    corrected re-run per the autopsy's own recommendation.

    CONFIRMING: on a run where both preconditions clear (V3-EXQ-703a:
    reconstruction-MSE-primary P0 loss, per-seed R2 gate, cf_margin ~0.15),
    the load-bearing criterion posterior_discrimination_cf_vs_correlational
    holds non-degenerately: the CF-backed arm and a correlational-control
    arm produce measurably different MECH-275 aggregator posteriors
    (posterior_delta >= 1e-3 floor per seed). This would replicate, on an
    unconfounded run, exactly what V3-EXQ-703 already observed on its one
    converged seed (posterior_deltas [0.0112, 0.0076, 0.0053], all seeds
    discriminating) despite that run's precondition failures. Secondary
    confirming signal (MECH-269 ablation, per the claim's own primary
    falsifier): suppressing the MECH-269 probe channel so the agent only
    visits anchor-eligible states should starve the buffer of
    hypothesis-tagged actions and measurably stall cross-episode schema
    revision at the MECH-275 aggregator.

    FALSIFYING: on the SAME unconfounded run, posterior_discrimination_cf_vs_correlational
    COLLAPSES -- posterior_deltas fall below the 1e-3 floor, or the
    CF-backed and correlational-control arms become statistically
    indistinguishable at the MECH-275 aggregator. Per the V3-EXQ-703
    autopsy's own routing, this specific outcome (not the 703 FAIL itself,
    which was precondition-confounded and therefore uninformative either
    way) is the genuine MECH-275-aggregator-insensitivity ceiling, and
    routes to substrate enrichment rather than a further re-queue.
    Falsifying (secondary): replacing counterfactual-backed attribution
    with pure correlational tagging at the waking-phase output and finding
    the sleep-phase aggregator cannot be distinguished from the CF-backed
    condition (noise-fit) would independently falsify the
    "counterfactual-backed attribution is structurally necessary"
    component of the claim.
  notes: >
    Design origin: user-led discussion 2026-04-21. The scientist-
    agent framing came from the observation that MECH-273's sleep
    aggregation needs a waking-phase signal that is not mere
    correlation -- the agent has to deliberately generate contrast.
    User articulation: "The agent does need to operate like a
    scientist indeed."

    Relationship to MECH-269 probe channel: MECH-269 is the
    proposer-side mechanism (what hypotheses does the hippocampus
    generate). MECH-276 is the whole-agent closure (the agent acts
    on those hypotheses consequentially and registers the outcome).
    MECH-269 without MECH-276 closure is a hypothesis generator that
    no one listens to.
  evidence_quality_note: >
    Candidate, no direct experiment yet. Natural validation: an
    experiment that seeds a confounder (sensory change that
    correlates with action but is not causally produced by it) and
    tests whether the agent's SD-003 attribution corrects. A
    working MECH-276 closure should isolate the confounder via
    intervention contrast; a broken closure should aggregate the
    correlation as if causal.
  implementation_note: >
    V3 substrate IMPLEMENTED 2026-06-23 as
    ree-v3/ree_core/attribution/scientist_attribution_buffer.py
    (ScientistAttributionBuffer; master flag use_scientist_attribution,
    default False -> bit-identical OFF). The waking-phase feedstock the
    MECH-275 BayesianAggregator consumes: on each waking tick the agent
    composes the existing single-pass comparators (SD-031 E2WorldForward,
    domain "place"; ARC-033 E2HarmSForward, domain "self") on
    (z_prev, observed, a_actual) and buffers per (domain, region) the agency
    residual ||z_observed - E2(z_prev, a_actual)|| tagged counterfactual-backed
    iff the counterfactual contrast ||E2(z_prev, a_actual) - E2(z_prev, a_cf)||
    >= cf_margin. The sleep loop (phase_manager.py) reads this buffer as the
    aggregator evidence INSTEAD of the MECH-284 staleness scalar when on. The
    only_counterfactual_backed lever is the falsifiable distinction the MECH-275
    claim turns on (correlational input -> noise-fit; counterfactual-backed ->
    schema revision). MECH-094-gated (no buffering on replay/simulation).
    PROMOTES NOTHING: MECH-276 stays candidate / v3_pending; substrate-readiness
    diagnostic V3-EXQ-703 (counterfactual-backed vs correlational-control arm,
    discrimination at world_dim>=128 with a trained encoder + SD-056 world_forward
    in P0). Comparator discrimination depends on a TRAINED forward model (the
    MECH-353/SD-031 lesson); a trained-substrate failure to discriminate is a
    substrate-ceiling finding, not a falsification. See ree-v3/CLAUDE.md.
  location: docs/architecture/scientist_agent_developmental_ordering.md

- id: MECH-277
  title: "Stage-1 specialisation of MECH-276: the agent discovers its own action space via motor experimentation (motor babbling) before SD-003 counterfactual self-attribution is well-formed. The agent performs motor acts and observes which sensory changes covary with them; the output is the substrate SD-003 requires -- a set of actions the agent has reason to believe are self-initiated and for which it has counterfactual expectations. Without MECH-277, the counterfactual action space (a_cf) is not well-defined and SD-003 degenerates."
  claim_type: mechanism_hypothesis
  subject: developmental.action_space_discovery
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  epistemic_category: substrate_conditional
  claim_level: mechanistic
  registered_utc: 2026-04-21
  depends_on:
    - MECH-276  # scientist-agent principle (general form)
    - ARC-059   # developmental ordering
    - SD-003    # the attribution mechanism MECH-277 feeds
    - SD-005    # z_self (the latent this stage populates)
  functional_restatement: >
    The agent does not arrive with a model of its own action space.
    It discovers the space by acting and observing which sensory
    changes track its motor output. This is motor babbling in the
    developmental psychology sense, but interpreted as the stage-1
    specialisation of MECH-276 -- the hypothesis being tested is
    "what can I cause", the consequential action is the motor act,
    and the attribution is "this sensory change covaries with my
    action output".

    The output of MECH-277 is the prerequisite for SD-003: a
    well-formed counterfactual action space. SD-003's computation
    E2(z, a_actual) vs E2(z, a_cf) presupposes that the set of a_cf
    candidates is meaningful; MECH-277 is what makes that set
    meaningful.

    Falsifiable (primary): an agent whose motor-experimentation
    phase is curtailed should show degenerate SD-003 attribution --
    the counterfactual comparison is against an arbitrary a_cf
    distribution rather than an informed one, and self-attribution
    stability should suffer.

    V3 note: V3 hand-wires action knowledge into the agent (action
    dimensions are pre-specified in config). MECH-277 is not
    actively tested in V3; the assumption is encoded in the
    substrate. SD-003 experiments should note this as an
    architectural shortcut.
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: V3 currently hand-wires the action
    space -- action_dim is a fixed config field (ree_core/utils/config.py,
    ree_core/predictors/e2_*.py: action_dim: int = 4), read but never
    discovered or expanded by any training process. The "Phase 0
    (babbling)" stage in experiments/infant_curriculum.py is NOT this
    mechanism: it only varies novelty_bonus_weight, residue_scale_factor,
    and offline_integration_frequency (exploration incentives), never
    action_dim or any action-space structure. No motor-babbling /
    action-space-discovery code exists anywhere in ree_core/ or
    experiments/ (confirmed by direct search, 2026-08-08). Zero
    experiments have been queued or run against this claim specifically.
    This precondition will not clear within V3 as currently scoped. It
    requires either a V4 substrate that genuinely discovers its action
    space, or a purpose-built V3 developmental sub-experiment that
    replaces the fixed action_dim config with an online discovery
    process -- neither exists today and neither is on the V3 roadmap.

    CONFIRMING (per the claim's own falsifier, given the precondition
    clears): curtail an agent's motor-experimentation phase and compare
    against a matched agent given a full discovery phase. Confirming
    signature: the curtailed-discovery agent shows degenerate
    self-attribution -- its counterfactual-action comparison draws a_cf
    from an uninformed/arbitrary distribution rather than one shaped by
    observed self-produced sensory covariation, and self-attribution
    stability measurably suffers relative to the full-discovery arm.

    FALSIFYING: self-attribution quality is statistically
    indistinguishable between curtailed and full discovery -- the
    counterfactual action space is well-formed regardless of whether it
    was discovered or specified, undercutting the claimed necessity of
    a distinct motor-babbling stage.

    MEASUREMENT NOTE: the self-attribution mechanism to instrument
    against is whatever V3 substrate is live at test time, not the
    literal SD-003 two-pass counterfactual formula named in this claim's
    functional_restatement -- SD-003 was superseded 2026-04-18 (three
    days before this claim was registered) by MECH-256 (single-pass
    forward-model comparator) with SD-029 as the concrete z_harm_s
    instantiation. This claim's functional_restatement and depends_on
    still reference SD-003 directly -- same staleness ARC-059
    independently flagged for itself over the same SD-003 reference.

    Evidence status (2026-08-08): no V3 experiment has queued or run
    either prediction, and none can be constructed as a non-degenerate
    test until the precondition above clears.
  notes: >
    Design origin: user-led discussion 2026-04-21. User noted that
    "sense of self would need to develop presumably before objects
    can be identified from the incoming data". MECH-277 formalises
    the sense-of-self developmental stage as the action-space
    discovery stage; the "self" the stage produces is the
    self-attribution substrate, not a phenomenology claim.
  evidence_quality_note: >
    Candidate, V3 hand-wires the action space so MECH-277 is not
    actively tested. V4 or a V3 developmental sub-experiment could
    test whether the substrate holds up under discovery rather than
    specification.
  location: docs/architecture/scientist_agent_developmental_ordering.md

- id: MECH-278
  title: "Stage-2 specialisation of MECH-276: once the agent can distinguish self-produced from world-produced sensory change (stage 1 complete via MECH-277), it forms object-schemas via experimental action on the world. An object is the stable bundle of features that behave causally together under interventional perturbation -- push here, these features move together; occlude this, that cluster disappears. Object-schema formation is the stage-2 specialisation of the scientist-agent principle, with world-features as the target of intervention. V3's z_world (SD-005) is currently engineered pre-split; this is an architectural shortcut past stage 2."
  claim_type: mechanism_hypothesis
  subject: developmental.object_schema_formation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-04-21
  depends_on:
    - MECH-276  # scientist-agent principle (general form)
    - MECH-277  # stage-1 prerequisite (self-model must exist first)
    - ARC-059   # developmental ordering
    - SD-005    # z_world (the latent this stage populates; engineered in V3)
    - SD-004    # action objects as map backbone (supports object-schema granularity)
  functional_restatement: >
    Once the agent can attribute sensory change to self vs world
    (stage 1 complete), it can perform experimental actions on the
    world and observe which features behave causally together under
    perturbation. The object is the stable bundle: the features
    that move together when pushed, disappear together when occluded,
    resist together when encountered. Object-schema formation is
    the stage-2 specialisation of MECH-276.

    V3 architectural shortcut: z_world is provided as a pre-
    specified split from z_self (SD-005, engineered). This skips
    the developmental construction of object-schemas. For V3's
    purposes (validating the mature architecture), this is
    acceptable. But the shortcut should be flagged -- the
    engineered z_world encodes assumptions the agent would not
    necessarily make on its own.

    Falsifiable (primary, V4 or developmental V3 sub-experiment):
    ablate the engineered z_world and require the agent to
    reconstruct an object-schema decomposition via experimental
    action. If the reconstructed decomposition matches the
    engineered version, the shortcut was benign. If it diverges,
    the shortcut smuggled assumptions.

    Falsifiable (secondary): an agent trained stage-2 without
    stage-1 complete should fail to form stable object-schemas
    because self-produced and world-produced sensory change cannot
    be distinguished -- the interventional signature becomes noisy.
  notes: >
    Design origin: user-led discussion 2026-04-21. User flagged the
    missing pieces about all stages needing wiring for objects and
    the importance of the developmental ordering. The architectural
    shortcut in V3 is a deliberate engineering choice, not an
    oversight; MECH-278 registers it so V4 can test whether the
    shortcut held.
  evidence_quality_note: >
    Candidate, V3 engineered z_world means MECH-278 is not actively
    tested in V3. The clean test is a V4 experiment that ablates
    z_world and requires experimental-action reconstruction.
  what_would_answer: >
    Non-degeneracy precondition: this claim is untestable in V3 as built. V3's
    z_world (SD-005) is engineered as a pre-specified split from z_self rather
    than discovered by the agent -- the claim's own notes call this a
    "deliberate engineering choice, not an oversight," and no V3 experiment to
    date (none is on file for MECH-278) has attempted to ablate or bypass it.
    A prerequisite substrate condition also gates this: MECH-277 (stage-1
    self/world attribution) must be functioning first, since MECH-278 predicts
    that stage-2 object-schema formation degrades when self-produced and
    world-produced sensory change cannot yet be distinguished. Any run that
    does not (a) ablate the engineered z_world and require the agent to
    reconstruct an object-schema decomposition via experimental/interventional
    action, and (b) do so on a substrate where MECH-277's self/world
    distinction is actually live rather than hand-wired, should self-route
    substrate_not_ready rather than count as a verdict either way -- this is a
    V4-or-late-V3-developmental-harness test, not something the current
    engineered-z_world V3 substrate can speak to.

    Confirming signature (primary, per the claim's own falsifier): with the
    engineered z_world ablated, an agent that performs experimental
    (interventional) actions on the world and forms feature-schema groupings
    matching the engineered z_world decomposition -- i.e. it recovers
    "features that move together when pushed, disappear together when
    occluded, resist together when encountered" as coherent bundles under
    perturbation, not merely correlated features under passive observation.
    This would show the V3 shortcut was benign: object identity is discoverable
    from causal-intervention structure alone, without engineering it in.

    Confirming signature (secondary, per the claim's own falsifier): an agent
    trained on stage-2 object formation only after stage-1 (MECH-277,
    self/world attribution) is complete shows stable, low-noise object-schema
    formation, in contrast to an agent given stage-2 experience before stage-1
    closure.

    Falsifying signature (primary): the reconstructed object-schema
    decomposition diverges materially from the engineered z_world split --
    i.e. features the engineered version treats as one object are not
    recovered as causally coherent under intervention, or vice versa. This
    would indicate the V3 engineered-z_world shortcut smuggled assumptions
    the agent would not independently form, undermining the claim that the
    engineered split is a faithful stand-in for developmentally-formed object
    schemas.

    Falsifying signature (secondary): an agent given stage-2 experimental
    action before stage-1 self/world attribution is well-formed (MECH-277
    incomplete) nonetheless forms object-schemas as stable and low-noise as
    an agent with stage-1 complete first -- i.e. the interventional signature
    is not measurably noisier without prior self/world disambiguation. This
    would undercut the claimed developmental ORDERING dependency (MECH-277
    before MECH-278), even if the object-definition mechanism itself holds.

    Evidence status as of this draft: no direct or indirect V3 experimental
    evidence exists for MECH-278 (no EXQ/V3-EXQ run is cited anywhere in this
    claim's history). This is a purely design-stage claim awaiting a V4 /
    developmental-harness test; do not treat any V3-substrate result as
    bearing on it absent an explicit z_world ablation.
  location: docs/architecture/scientist_agent_developmental_ordering.md


- id: SD-036
  evidence_quality_note: |
    [update_z_goal_typeerror_swallowed] EXQ-471/475/483/483a/483b/490/490b/490c/490e/490f/524 cohort evidence is contaminated and superseded as of 2026-05-07 due to a script-side TypeError in agent.update_z_goal that was silently swallowed by a bare except, leaving GoalState.is_active() always False. Additional cohort-wide issues found: 2x agent.sense() per warmup step, agent.update_residue() never called (residue field empty / running_variance pinned), dACC bias diagnostic reading wrong tensor. Awaiting StepHarness migration + corrected re-runs (EXQ-475a / EXQ-483c / EXQ-490g / EXQ-471a / EXQ-524a). See REE_Working session 2026-05-07.

    No direct V3 substrate evidence yet; substrate not implemented. Indirect motivating evidence is V3-EXQ-471 episode log (psychiatric_failure_modes.md catatonia subtype II section). Clinical evidence (benzodiazepines first-line for catatonia) is a coincidence of architectural prediction and existing clinical practice, but is not itself a substrate test. Promotion requires the falsifiable substrate experiments above. EXQ-475 (governance 2026-04-22, evidence_direction_per_claim split): SD-036 enabled with default decay parameters did NOT unlock the EXQ-471 monostrategy lock-in -- per-seed eval rewards remained negative across 3/3 seeds and the warmup improvement was bimodal (seeds 0/2 improved, seed 1 worsened). MECH-279 PAG freeze gate fired correctly (1000 active steps per seed) -- substrate-landing confirmed. SD-036 stays non_contributory pending MECH-269 V_s landing upstream; the decay regulator alone cannot break a lock-in whose proximal driver is the V_s monostrategy, but it remains a necessary substrate piece once V_s lands.

    [2026-08-01, V3-EXQ-854, evidence_direction=supports]: first-ever queued experiment directly
    testing SD-036's own registered falsifier (design doc "Predicted observables" #2), unblocked
    only on 2026-08-01 by a same-day fix (ree-v3 35e8969) that gave the regulator temporal
    authority over z_harm/z_harm_a for the first time (LatentStack.encode() previously produced
    both as pure feedforward encodes, so the per-tick rescale was discarded and the DV was
    structurally invariant to gaba_tone -- spread 8.6e-08, a structural null pre-fix). Post-fix
    spread: 1.4e-03 (z_harm), 1.0e-01 (z_harm_a). C1 (primary DV z_harm_a, floor-robust ON-vs-OFF
    relative form) reads clean rho=-1.0 across the gaba_tone sweep. Control arm is
    use_gabaergic_decay=False, not gaba_tone=0.0 (post-fix, tone 0 suspends decay but leaves
    recurrence live). Honestly-recorded encoder-floor caveat: harm_encoder(zeros)~0.46 and
    affective_harm_encoder(zeros,zeros)~0.33, so the V3-EXQ-471 lock is substantially a floor
    response to the ambient hazard field and decay reaches an equilibrium rather than returning to
    baseline -- observable #1 as originally worded is explicitly NOT load-bearing; the floor is
    measured every run and C3 is the floor-robust relative form instead. MECH-279 not tagged
    (consumes gaba_tone as a direct scalar, never through the decay path, so V3-EXQ-776's PASS is
    untouched). SD-036 stays candidate/v3_pending (still gated on the substrate-implementation
    hold), but now has its first non-zero exp_conf contribution (previously 0.0 despite lit_conf
    0.805). PROMOTES NOTHING alone; the regulator demonstrably works as designed once the
    encoding-authority bug is fixed.
  title: "GABAergic regulatory layer applies cross-stream exponential decay (z_harm_s, z_harm_a, z_beta) modulated by tonic gaba_tone; missing-decay regulator is the proximal substrate cause of harm-stream lock-in (catatonia subtype II) observed in V3-EXQ-471."
  claim_type: design_decision
  subject: gabaergic.cross_stream_decay_regulator
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:SD-036@2026-04-25T15:42:09.107896Z"
      as_of: 2026-04-25
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: 2026-04-22
  depends_on:
    - SD-010    # z_harm_s sensory harm stream (decay target)
    - SD-011    # z_harm_a affective harm stream (decay target)
    - SD-012    # drive accumulator (candidate decay target -- see open questions)
    - MECH-090  # bistable beta gate (z_beta decay target)
    - MECH-269  # anchor reset informed by decay state
  child_claims:
    - MECH-279  # PAG freeze-gate (specialised application of the decay regulator)
  functional_restatement: >
    The current V3 substrate has no mechanism by which the absence of
    input over time returns a latent stream toward baseline. Streams
    integrate input but contain no decay regulator; a single salient
    event therefore produces a permanently elevated stream value.
    V3-EXQ-471 made this concrete: a single hazard contact at t=0
    elevated z_harm_norm to ~0.82, which then stayed at ~0.7 for the
    remaining 199 steps despite zero harm input. SD-036 commits to
    decay being a property of a regulator layer (not per-stream
    update logic), with a global gaba_tone scalar modulating the rate.
    Pharmacological mapping is direct: benzodiazepines (gaba_tone>1)
    accelerate decay; GABA-A withdrawal/antibodies (gaba_tone<1) slow
    decay across all registered streams simultaneously. This predicts
    the clinical cluster (catatonia + autonomic dysregulation +
    perceptual disturbance + sleep disruption) under GABAergic insult
    as a coherent architectural failure rather than four separate
    disorders.

    Falsifiable (primary): re-running V3-EXQ-471 with SD-036 enabled
    (default tau_harm_s=0.05, gaba_tone=1.0) should produce mode flip
    from 'avoid' back to a goal-seeking mode by ~t=50, replacing the
    observed 200-step lock.

    Falsifiable (secondary): gaba_tone dose-response sweep (0.3, 0.5,
    1.0, 1.5, 2.0) should produce monotonic recovery-time variation;
    very low tone should produce simultaneous failures across z_harm_s,
    z_harm_a, and z_beta (the cluster signature) rather than isolated
    single-stream failures.
  notes: >
    Design origin: user-led discussion 2026-04-22 following V3-EXQ-471
    fishtank visualization. User observed competent goal-directed
    behaviour collapsing into 200-step catatonic lock after a single
    harm event; proposed GABAergic decay as the missing regulator.
    Catatonia subtype II (harm-stream lock-in) is registered in
    psychiatric_failure_modes.md as architecturally distinct from
    MECH-202B (commit-gate paralysis); the same surface phenomenology
    arises from different mechanisms with different treatment targets.
    Design doc: docs/architecture/sd_036_gabaergic_decay_regulator.md
  pharmacological_predictions:
    - PHARM-004  # lorazepam in catatonia, subtype-II specificity prediction
  location: docs/architecture/sd_036_gabaergic_decay_regulator.md

- id: MECH-279
  evidence_quality_note: |
    2026-07-18 governance: candidate -> provisional. V3-EXQ-776 (PAG freeze-gate functional signature; run_id v3_exq_776_mech279_pag_freeze_gate_functional_signature_20260717T153724Z_v3) PASS/supports, self-route pag_freeze_gate_functional_signature_confirmed, no adjudication flag. All THREE signatures pass on 5/5 seeds and are non-degenerate, against gate_cfg theta_freeze=2.0 / duration_input_threshold=0.4: S1 commit-law (freeze commits when z_harm_a x duration exceeds theta_freeze -- MECH-279's primary falsifiable prediction); S2 committed-state hysteresis (freeze is a sustained COMMITTED state with its own exit criterion, not merely absence of movement -- the architectural core of the claim); S3 gaba_tone exit-control (elevated gaba_tone accelerates exit, lowered gaba_tone sustains freeze under mild harm = the catatonia phenotype -- MECH-279's secondary prediction). Env-free DRIVEN probe of the real agent-instantiated gate reading real gaba_tone from SD-036. Evidence state: exp_conf 0.774, lit_conf 0.896 (6 lit entries); 7 supports / 0 weakens. DECONTAMINATION: this is the first clean experimental support for MECH-279. The v3_pending=false clearance (2026-04-22) rested on V3-EXQ-475, which is inside the contaminated cohort recorded below; V3-EXQ-776 is a fresh wall-independent probe outside that cohort and does not inherit the update_z_goal defect. SCOPE (important): 776 validates the gate's FUNCTIONAL LAW under driven z_harm_a. It does NOT establish that the gate engages ECOLOGICALLY -- the standing MECH-280 / SD-037 finding that pag_release_count_end=0 across all 12 runs of 483e (z_harm_a never clears theta_freeze=2.0 in situ) is untouched by this run and still routes to the PAGFreezeGateConfig.theta_freeze / duration_input_threshold recalibration. --- [update_z_goal_typeerror_swallowed] EXQ-471/475/483/483a/483b/490/490b/490c/490e/490f/524 cohort evidence is contaminated and superseded as of 2026-05-07 due to a script-side TypeError in agent.update_z_goal that was silently swallowed by a bare except, leaving GoalState.is_active() always False. Additional cohort-wide issues found: 2x agent.sense() per warmup step, agent.update_residue() never called (residue field empty / running_variance pinned), dACC bias diagnostic reading wrong tensor. Awaiting StepHarness migration + corrected re-runs (EXQ-475a / EXQ-483c / EXQ-490g / EXQ-471a / EXQ-524a). See REE_Working session 2026-05-07.

    Substrate not implemented. Strong biological literature backing (defensive behaviour / PAG circuitry) but no V3 substrate test yet. Promotion requires the falsifiable substrate experiments in sd_036_gabaergic_decay_regulator.md.
  title: "PAG-analog gates the freeze response: freeze commit when z_harm_a x duration exceeds threshold; exit requires z_harm_a to fall below an exit_threshold scaled by gaba_tone; failure of exit produces sustained immobility characteristic of stuporous catatonia."
  claim_type: mechanism_hypothesis
  subject: pag.gabaergic_freeze_gate
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-18
    needs_review: false
    evidence:
      from: "decision:MECH-279@2026-07-18T06:40:45.213047Z"
      as_of: 2026-07-18
      verdict: promote_to_provisional/applied
  implementation_phase: v3
  v3_pending: false  # cleared 2026-04-22: V3-EXQ-475 confirms PAG freeze gate firing (1000 active steps per seed in 3/3 seeds)
  claim_level: mechanistic
  registered_utc: 2026-04-22
  depends_on:
    - SD-036    # parent: provides decay channel for z_harm_a (required for freeze exit)
    - SD-011    # z_harm_a (input to freeze commit threshold)
    - MECH-090  # bistable beta gate (interaction: freeze commit elevates z_beta)
    - SD-035    # amygdala analogue (BLA/CeA upstream of PAG in biological circuit)
  functional_restatement: >
    The freeze response is a committed behavioural state -- sustained
    motor immobility plus elevated autonomic arousal. It is not just
    'no movement'; it is an active commitment to not-move with its own
    duration and exit criterion. Biologically gated by the
    periaqueductal gray, where freeze-promoting cells are themselves
    GABAergic and freeze termination requires GABAergic inhibitory
    control to wane. MECH-279 commits to: freeze_commit when
    z_harm_a x duration > theta_freeze; freeze_active sustained until
    z_harm_a < exit_threshold = theta_freeze * gaba_tone. The same
    neurotransmitter system gates both entry (PAG freeze-cell
    commitment) and exit (SD-036 decay returning z_harm_a below
    threshold). Clinical observation that GABA agonists treat freeze
    catatonia is an architectural prediction, not an empirical add-on.

    Falsifiable (primary): inducing sustained z_harm_a above
    theta_freeze for >5 steps should produce committed freeze (action
    repertoire constrained to no-op / minimal movement); exit should
    require z_harm_a < exit_threshold.

    Falsifiable (secondary): elevating gaba_tone to 1.5 should
    accelerate freeze exit; lowering gaba_tone to 0.5 should produce
    sustained freeze even with mild precipitating harm -- the
    catatonia phenotype.
  notes: >
    Design origin: same as SD-036 parent. PAG GABAergic freeze-gating
    is a clinically-grounded mechanism (extensive defensive-behaviour
    literature on PAG); MECH-279 registers it as a specific consumer
    of the SD-036 cross-stream decay regulator. Wired downstream of
    SD-035 amygdala analogue (BLA/CeA) in the biological circuit:
    BLA/CeA -> PAG -> action constraint.
    Design doc: docs/architecture/sd_036_gabaergic_decay_regulator.md
    section 'PAG freeze-gating (MECH-279)'.
  pharmacological_predictions:
    - PHARM-004  # lorazepam in catatonia subtype II, freeze-gate exit
  location: docs/architecture/sd_036_gabaergic_decay_regulator.md

- id: SD-037
  # Assembly-state companion fields (auto-join audit, 2026-06-22; repointed same
  # day). axis-b committed-action diversity is gated by the conversion ceiling,
  # which is being closed via the biologically-faithful basal-ganglia eligibility
  # constitution (ARC-107: MECH-448 demotion + MECH-449 Go/No-Go), NOT the
  # F-dominance rebalance route (exhausted). awaiting: ARC-107 (matches MECH-449),
  # not the legacy f_dominance_conversion_ceiling queue row (name connotes the
  # abandoned route; status blob mis-normalizes to 'built'). assembly_status
  # in_progress: BG constitution under construction.
  awaiting: ARC-107
  assembly_status: in_progress
  pending_retest_after_substrate: true
  evidence_quality_note: |
    [update_z_goal_typeerror_swallowed] EXQ-471/475/483/483a/483b/490/490b/490c/490e/490f/524 cohort evidence is contaminated and superseded as of 2026-05-07 due to a script-side TypeError in agent.update_z_goal that was silently swallowed by a bare except, leaving GoalState.is_active() always False. Additional cohort-wide issues found: 2x agent.sense() per warmup step, agent.update_residue() never called (residue field empty / running_variance pinned), dACC bias diagnostic reading wrong tensor. Awaiting StepHarness migration + corrected re-runs (EXQ-475a / EXQ-483c / EXQ-490g / EXQ-471a / EXQ-524a). See REE_Working session 2026-05-07.

    2026-05-29 (V3-EXQ-490g cohort autopsy): cluster-split analysis confirms 2026-05-24 V3-EXQ-483c autopsy reading. SD-037 weakens tag is algorithm-generated from binary FAIL on a configuration omission (use_dacc=True missing from all 4 arm configs -> agent.dacc is None -> C2_dacc_bias=0 in every run regardless of SD-037 state). Goal pipeline is firing (goal_norm 0.09-0.31; bridge_cue_fires 3-29; approach_commit_rate 1.0). C3_lift FAIL is policy ceiling (baseline approach_commit_rate=1.0 in OFF_OFF arm), not SD-037 suppression. Same template gap drives V3-EXQ-524a per-seed (goal_norm 0.23-0.36; same C2 zero). Cluster = 5 GAP-4 Tier-1 experiments (471a / 475a / 483c / 490g / 524a) all share one tier-1 library template gap. evidence_direction -> non_contributory. Pending retest after Tier-1 library rebuild (use_dacc=True default + C3 metric replacement). NOT in the 591 substrate-uniform z_goal-zero cluster.

    [2026-05-08 governance]: EXQ-483b PASS supports (substrate validates in isolation). Caveat: integration with MECH-307 conjunction architecture not yet tested; substrate-level support only.

    [2026-05-30 governance, V3-EXQ-483d autopsy applied]: EXQ-483d (supersedes 483c). Substrate fires correctly: override_signal_nonzero_steps saturates at total step count in every OFF_ON / ON_ON seed (C2 cleanly PASS). However action_counts and goal_norm_peak are bit-identical across the broadcast_override axis within each seed (OFF_OFF vs OFF_ON; ON_OFF vs ON_ON), and C3_lift_vs_baseline fails (lifts=1/3). MECH-295 bridge dominates effective_drive at the goal-seeding site; PAG-freeze and SalienceCoordinator consumer pathways are dormant in this env / config. Substrate-ceiling: SD-037 wired but inert at the validation surface. Closes the SD-037 substrate_queue's pre-existing prediction ("if still flat, blocker is upstream of SD-037, not override wiring"). Hold pending consumer-cascade substrate work (SalienceCoordinator enable + PAG-engaging env condition + PFC/BLA/beta-gate consumers per MECH-281 implementation_note). Pair with pending_retest_after_substrate. Apply per-claim: SD-037 evidence_direction non_contributory; MECH-280 / MECH-281 evidence_direction non_contributory (claims could not express themselves -- their targets were not engaged). Set pending_retest_after_substrate: true on all three. epistemic_category: substrate_ceiling. Routing: implement-substrate (amend SD-037 substrate_queue entry). Artifact: evidence/planning/failure_autopsy_V3-EXQ-483d_2026-05-30.{md,json}.

    [2026-05-31 governance, V3-EXQ-483e autopsy applied]: EXQ-483e (supersedes 483d under 483 lineage). Substrate broadcast saturates correctly (override_signal_nonzero_steps == total_eval_steps in every ON_OFF / ON_ON seed; C2_substrate cleanly PASS). The 2026-05-30 MECH-281 consumer-cascade amend (four new gain knobs: pfc_eta / bla_encoding / cea_amplitude / beta_interrupt) is fully wired in ARM_3 ON_ON but produces ZERO behavioural divergence: bla_encoding_gain_peak / cea_mode_prior_peak / cea_fast_prime_peak / dacc_bias_nonzero_steps / pag_release_count_end / beta_release_count all 0 across ALL 12 runs (including ARM_3 ON_ON in every seed). action_counts BIT-IDENTICAL across all four arms within each seed. goal_norm_peak near-bit-identical (OFF_OFF vs ON_OFF; OFF_ON vs ON_ON). Only lateral_pfc rule_state EMA responds (1.08-1.18x, below 1.5x C2 threshold). Diagnosis: SD-037 multiplicative gain pattern (output * (1 + gain * override_signal)) requires consumer-module input gates to be crossed first; BLA arousal_threshold_on=0.4 + CeA fast_route_threshold=0.5 + PAG theta_freeze sustained-product=2.0 + dACC pe-magnitude floors all sit ABOVE fishtank baseline signal magnitudes. With consumer outputs at zero baseline, broadcast amplification yields zero. epistemic_category: substrate_ceiling (one architectural layer deeper than 483d: 483d was "consumers unwired"; 483e is "consumers wired but their own input thresholds not cleared"). Falsifies the SD-037 substrate_queue 483d prediction that consumer-cascade wiring alone would deliver the lever. Apply per-claim: SD-037 / MECH-280 / MECH-281 evidence_direction non_contributory. Set pending_retest_after_substrate: true on all three (already true; preserved). Routing: implement-substrate amend SD-037 substrate_queue entry with consumer-input-threshold failure_record_entry; next substrate axes: (a) consumer-input-threshold recalibration on BLA / CeA / PAG / dACC config knobs against fishtank baseline magnitudes; (b) SD-029-style sustained-threat env curriculum driving z_harm_a above current consumer-input thresholds. Distinct from 540/590a/591/598/603/610 substrate_uniform cluster (different shape on different substrate root). Artifact: evidence/planning/failure_autopsy_V3-EXQ-483e_2026-05-31.{md,json}.

    Substrate IMPLEMENTED 2026-04-25 (ree-v3 commit; design doc sd_037_broadcast_override_regulator.md). First validation V3-EXQ-483 (4-arm SD-036 x SD-037 factorial, 3 seeds, 60-ep warmup) returned a "wired-but-inert" pattern: substrate signature confirmed -- override_signal climbed from 0.0 to 0.56 mean / 0.62 max in ON arms and PAG release count rose from 5.3 (override OFF) to 9.0-9.3 (override ON), tracking the lit-pull-predicted PAG-exit-threshold scaling. Behavioural acceptance criteria (PWS / narcolepsy ratios) were uninterpretable because approach_commit was 0.0 across ALL arms including the SD-036-only baseline (ON_OFF), making the PWS / narcolepsy ratios degenerate (0/0). The pattern matches EXQ-471 / EXQ-478 / EXQ-480: substrate writes the expected signal but the policy layer does not yet translate it into observable behavioural divergence under the standard CausalGridWorldV2 benefit task. Confidence is therefore SUBSTRATE-CONFIRMED-WIRED but BEHAVIOURALLY-INCONCLUSIVE -- promotion still requires independent demonstration of the two failure-mode phenotypes. Successor V3-EXQ-483a queued (warmup 60 -> 200 to give baseline arm enough exposure to potentially produce non-zero approach behaviour; acceptance logic adds substrate-readiness fallback path used only when the baseline arm yields no behavioural signal, requiring override_mean > 0.30 AND PAG release ratio ON_ON / ON_OFF > 1.30 -- both thresholds derived from EXQ-483's measured values so the test discriminates against a hypothetically broken substrate without depending on a behavioural readout the policy layer cannot yet produce). Strong literature basis retained (lit-pull synthesis at literature_confidence ~0.78 across 13 papers).
    [IGW-20260521-020 substrate-ready close]: Substrate-readiness canonical PASS V3-EXQ-483b (2026-05-08). SD-037 landing scope = BroadcastOverrideRegulator + MECH-280/281 consumer sites only; MECH-282 (LPB channel) and MECH-286 (sleep-onset gating) explicitly deferred. Tier-1 evidence V3-EXQ-483c queued (goal_pipeline GAP-4). v3_pending retained until behavioural phenotypes or 483c contributory evidence; no promotion.
    [enum-comment migrated to field 2026-07-18] epistemic_category: set 2026-05-31 per failure_autopsy_V3-EXQ-483e (and 483d before it); consumer-input-threshold layer is the substrate-ceiling root for this claim's current validation surface
  title: "Broadcast override regulator (orexin-analog) is the third regulatory layer of the V3 control stack: a small, broadly-projecting gain modulator that integrates internal homeostatic state and threat context to produce an override_signal that reweights z_harm at downstream commit gates and gates the SD-012 drive->z_goal seeding bridge. Dissociable from SD-036 (GABA decay) and from MECH-186/187/188 (5-HT goal-pipeline gain): each layer predicts a clinically distinct failure phenotype (catatonia subtype II / depression / narcolepsy + PWS-hyperphagia). SD-036 alone is architecturally insufficient to break harm-stream lock-in under sustained threat; SD-037 supplies the override authority that lets metabolic demand drive z_goal seeding when threat persists."
  claim_type: design_decision
  subject: broadcast.override_regulator
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-483e_2026-05-31
      as_of: 2026-05-31
      verdict: non_contributory/substrate_ceiling
  epistemic_category: substrate_ceiling
  implementation_phase: v3
  v3_pending: true
  implementation_note: "IMPLEMENTED 2026-04-25. Module: ree_core/regulators/broadcast_override.py (BroadcastOverrideRegulator, BroadcastOverrideConfig). Master switch: REEConfig.use_broadcast_override (default False). Three consumer sites wired: PAG freeze-gate (MECH-279) exit_threshold scaled by alpha_override; SalienceCoordinator (SD-032a) operating-mode reweight via update_signal('override_signal', ...); GoalState (SD-012) drive seeding amplification via override_goal_seeding_gain. MECH-094: simulation_mode=True returns cached signal unchanged. Backward compatible with flag OFF (95/95 contracts PASS). Validation experiment: V3-EXQ-483 queued. Lit-pull confirms defaults biologically defensible: targeted_review_orexin_kinetics/synthesis.md (Mileykovskiy 2005, Lee 2005, Karnani 2020, Johnson 2012). [2026-05-30 consumer-cascade amend, MECH-281 motor-coupling axis]: Four additional consumer sites wired per V3-EXQ-483d substrate-ceiling autopsy routing -- (i) LateralPFCAnalog SD-033a update() eff_eta scaled by (1 + override_pfc_eta_gain * override_signal); (ii) BLAAnalog SD-035 encoding_gain scaled by (1 + override_bla_encoding_gain * override_signal); (iii) CeAAnalog SD-035 mode_prior + fast_prime scaled by (1 + override_cea_amplitude_gain * override_signal), bounded by mode_prior_log_odds_max so cortex still cannot be over-ruled; (iv) urgency_interrupt_threshold (MECH-090 motor side, parallel to PAG alpha_override) attenuated by max(0, 1 - override_beta_interrupt_gain * override_signal). All four new REEConfig gains default 0.0 (bit-identical OFF). Backward compatible: 556/556 contracts + 7/7 preflight PASS with all four gains at default 0.0 (543 prior + 13 new MECH-281 contracts at tests/contracts/test_sd_037_consumer_cascade.py). MECH-094 preserved via existing simulation_mode arguments on BLA/CeA + MECH-319 simulation-mode-rule-gate on LateralPFC. ree-v3/CLAUDE.md SD-037 consumer-cascade section appended 2026-05-30. Validation experiment: V3-EXQ-483e queued (4-arm successor under 483 lineage; reuses SD-036+MECH-279 freeze-engaging substrate for PAG-engaging env condition; SalienceCoordinator enable via use_salience_coordinator=True; claim_ids=[SD-037, MECH-280, MECH-281]). pending_retest_after_substrate stays TRUE; closes only after V3-EXQ-483e retest manifest lands."
  claim_level: architectural
  registered_utc: 2026-04-22
  depends_on:
    - SD-036    # paired regulator: stream-side decay (this is arbitration-side reweighting)
    - SD-012    # drive-modulated goal seeding (this gates the bridge)
    - SD-032a   # SalienceCoordinator (consumes override_signal as weight modulator)
    - MECH-279  # PAG freeze-gate (consumes override_signal via MECH-280 projection)
  child_claims:
    - MECH-280  # LH-PAG override projection
    - MECH-281  # orexin gain modulation of drive-coupled arousal
    - MECH-282  # LPB interoceptive routing into harm-arbitration
  what_would_answer: |
    PRIMARY (unchanged): a 4-arm factorial {SD-036 OFF, SD-037 OFF} /
    {SD-036 ON, SD-037 OFF} / {SD-036 OFF, SD-037 ON} / {SD-036 ON,
    SD-037 ON} on V3-EXQ-471-class conditions. Predicted: arms 2 and 3
    partial recovery, arm 4 full recovery, arm 1 reproduces the
    catatonic-lock baseline. A clean FAIL on arm 4 (no recovery with
    both regulators ON) falsifies the broadcast-override architecture
    outright.

    SECONDARY (unchanged): two dissociable failure phenotypes once the
    factorial runs on adequate substrate -- (a) saturated override
    (override_signal pinned high) should produce compulsive drive-
    pursuit despite negative outcomes (PWS/hyperphagia-analog); (b) lost
    override (override_signal absent) should produce decoupling -- z_harm
    and drive_level compute normally but fail to integrate into
    coordinated behaviour (narcolepsy/cataplexy-analog).

    NON-DEGENERACY PRECONDITION (the actual current gate -- three
    consecutive "substrate confirmed, behaviourally inert" findings, not
    two):

    Stage 1 (V3-EXQ-483d, 2026-05-29): override_signal fires and
    saturates correctly, but only one consumer pathway (GoalState
    effective_drive) was wired and was dominated by the MECH-295
    liking-bridge -- action_counts/goal_norm_peak bit-identical across
    the broadcast_override axis. FIXED 2026-05-30: four more consumer
    sites landed (LateralPFCAnalog eff_eta, BLAAnalog encoding_gain,
    CeAAnalog mode_prior/fast_prime, BetaGate urgency_interrupt_
    threshold), all default-OFF gain knobs.

    Stage 2 (V3-EXQ-483e, 2026-05-31): with all four consumer sites
    wired, EVERY consumer-side readout was still exactly zero across all
    12 runs. Diagnosis: SD-037's mechanism is multiplicative (output *
    (1 + gain * override_signal)) -- it can only amplify an ALREADY-
    NONZERO consumer output. On the fishtank baseline no consumer output
    crosses its own input-gate threshold (BLA arousal_threshold_on=0.4,
    CeA fast_route_threshold=0.5, PAG theta_freeze sustained-product=2.0,
    dACC pe-magnitude floors), so broadcast amplification has nothing to
    amplify: 0 * anything = 0.

    Stage 3 (V3-EXQ-620/620b through 625b/625d/625e, 2026-05-31 ->
    2026-06-20): axis (a) is CLOSED -- V3-EXQ-620's initial "all-zero"
    reading was a config artifact; the corrected V3-EXQ-620b PASSED
    (pooled p70=0.43, clears BLA 0.4). Axis (b) (sustained-threat
    curriculum) is NOT closed and has surfaced a THIRD, deeper layer:
    V3-EXQ-625b cleared the magnitude floor but failed on temporal
    structure (collapse to monomorphic action attractors under sustained
    threat). The follow-up V3-EXQ-625d diagnosed this as ENV-
    CONDITIONAL: the selection-diversity -> committed-action-diversity
    conversion axis-(b) depends on only holds on the reef-bipartite
    substrate's structural guarantee of opposite first-action argmaxes,
    and does not survive a threat-engaged candidate pool -- the SAME
    root as the campaign-wide MECH-439 F-dominance conversion-ceiling
    (shared with ARC-062 GAP-B, dACC C2, OFC behavioural conversion,
    SD-034 de-commit). The recalibrated V3-EXQ-625e (10x lower threat
    magnitude + pulsed duty cycle) still returned TERMINAL FAIL/
    non_contributory. As of the 2026-08-07 inter-governance workset,
    axis-(b) Phase 1b remains status=assembling, held pending a
    demonstration that the MECH-439 selection-face fix (MECH-448/449,
    BUILT + PROMOTED-provisional) generalises off the foraging substrate
    to a threat-engaged pool -- the four retests run against it to date
    have not shown that generalisation. Phase 2/3/4 (Phase 4 =
    V3-EXQ-483f, the actual SD-037 4-arm behavioural validation) remain
    blocked.

    WHAT WOULD ANSWER: a run of the primary 4-arm factorial (V3-EXQ-483f
    or successor) on the CURRENT fishtank/CausalGridWorldV2 baseline is
    expected to reproduce zero-everywhere across all consumer readouts
    regardless of SD-037's own ON/OFF state, per 483e's own diagnosis,
    and is NOT a valid test of the architecture -- UNLESS BOTH of the
    following have landed: (i) axis-(a) consumer-input-threshold
    recalibration confirmed adequate under the specific env config used
    (620b demonstrates this is achievable), AND (ii) axis-(b)'s
    committed-action diversity gate PASSES, which now requires the
    separate MECH-439/GAP-A selection-face-conversion fix to demonstrate
    generalisation beyond the foraging substrate to a threat-engaged
    candidate pool. Only once both are independently confirmed non-
    degenerate does a FAIL on the primary factorial (arm 4 no recovery)
    become a genuine falsification of the broadcast-override
    architecture; a FAIL run before then is not falsifying, and a
    degenerate zero-everywhere result on any attempt run before then
    should be read as further confirmation of the substrate ceiling, not
    as evidence against SD-037/MECH-280/MECH-281.
  functional_restatement: >
    SD-036's GABAergic decay addresses one half of the V3-EXQ-471 catatonic
    lock: in the absence of input, harm streams now relax toward baseline.
    But the lit-pull on homeostatic override is convergent that decay alone
    is not how biology breaks harm-stream lock-in under sustained threat.
    Biology uses a dedicated override authority -- a small, broadly-projecting
    gain modulator (canonically orexin/hypocretin) that integrates internal
    homeostatic state and threat context and reweights downstream arbitration
    without silencing upstream signals.

    SD-037 commits to a third regulatory layer alongside 5-HT (MECH-186/187/188,
    goal-pipeline gain) and GABA (SD-036, cross-stream decay). The regulator
    computes a scalar (or low-dimensional vector) override_signal from
    drive_level (SD-012), sustained-threat context (rolling window of z_harm
    above threshold), and a recruitment threshold. Above recruitment threshold:
    (1) the weight of z_harm in commit-gate threshold computations is scaled
    down (z_harm itself is not modified); (2) drive_level is allowed to seed
    z_goal directly. Below threshold: drive cannot seed goal-mode behaviour
    even with non-zero benefit_exposure. This is the architectural answer to
    why hunger does not just compete with threat but acquires authority over it.

    Falsifiable (primary, factorial validation): 4-arm experiment {SD-036 OFF,
    SD-037 OFF}, {SD-036 ON, SD-037 OFF}, {SD-036 OFF, SD-037 ON}, {SD-036 ON,
    SD-037 ON} on V3-EXQ-471 conditions. Predicted: arms 2 and 3 partial
    recovery; arm 4 full recovery; arm 1 reproduces lock. A failure of arm 4
    falsifies the broadcast-override architecture entirely.

    Falsifiable (secondary): saturated override (override_signal pinned high)
    should produce compulsive drive pursuit despite negative outcomes
    (PWS-analog hyperphagia). Lost override (override_signal absent) should
    produce decoupling -- z_harm and drive_level both compute normally but
    fail to integrate into coordinated behaviour (narcolepsy/cataplexy-analog).
    These two failure phenotypes are diagnostic for SD-037-specific dysfunction.
  notes: >
    Design origin: lit-pull synthesis 2026-04-22, targeted_review_homeostatic_override.
    13 papers across LH/AgRP, LH-VTA, peri-LC, orexin, PBN-CGRP, VMHdm, insula,
    PWS, and narcolepsy. Convergent verdict: broadcast at upstream sources +
    arbitration at downstream nodes; override REWEIGHTS z_harm rather than
    REPLACING it; the bridge between drive_level and z_goal seeding is the
    missing link in V3. SYNTHESIS.md "Bottom line": SD-036 alone is unlikely
    to fix V3-EXQ-471 architecturally; the orexin-analog cluster is on the
    V3 roadmap regardless of EXQ-475's empirical outcome.
    Design doc: docs/architecture/sd_037_broadcast_override_regulator.md
  pharmacological_predictions:
    - PHARM-001  # DORA-class (suvorexant/lemborexant) substrate intervention for hyperarousal-insomnia
    - PHARM-002  # modafinil / sodium oxybate (MECH-286 axis, narcolepsy sleep-state)
    - PHARM-003  # venlafaxine / clomipramine (MECH-281 axis, cataplexy)
    - PHARM-005  # prazosin (indirect override damping in PTSD nightmares)
    - PHARM-006  # SSRIs (negative prediction for hyperarousal-insomnia subgroup)
  location: docs/architecture/sd_037_broadcast_override_regulator.md

- id: SD-038
  title: "HippocampalModule CEM trajectory sampling must include an anti-recency exploration bonus that down-weights recently committed trajectory segments, ensuring the proposal distribution covers all available paths rather than converging on recency-weighted routes."
  claim_type: design_decision
  subject: hippocampal.cem_anti_recency_exploration
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: 2026-04-24
  depends_on:
    - MECH-289   # mechanism: anti-recency bias is the biological target this SD implements
    - MECH-092   # the quiescent replay context where anti-recency is most critical
  functional_restatement: >
    The CEM trajectory generator in HippocampalModule must not converge on the
    most recently committed trajectories. Gupta et al. 2010 (Neuron 65:695) shows
    that biological SWR replay has an anti-recency property: infrequently visited
    paths are replayed MORE often than recently-experienced ones, and novel-path
    sequences are generated. The REE equivalent is an exploration bonus in the CEM
    elite set selection: trajectories whose waypoints have high recent-commitment
    overlap receive a fitness penalty (or equivalently, novel-waypoint trajectories
    receive a bonus). Without this, CEM converges on familiar safe routes, producing
    monostrategy. The bonus is applied at trajectory scoring time, before elite
    selection; recent-commitment history is maintained as a decaying waypoint
    visitation count over a configurable window.

    Falsifiable (primary): in a two-or-more-route environment, the agent with
    exploration bonus must use both routes at rates that do not diverge to
    monostrategy under extended evaluation. Agent without bonus should show
    measurable monostrategy onset (route entropy below threshold). Ablation:
    {bonus OFF} x {bonus ON} x route-count = 2 minimal experiment.

    Falsifiable (secondary): trajectory diversity metric (mean pairwise waypoint
    overlap between consecutive proposals) should be higher with bonus than without.
  notes: >
    Registered 2026-04-24 from lit-pull synthesis (targeted_review_connectome_arc_028/
    entry 2026-04-24_arc_028_generative_replay_gupta2010, conf=0.80). The anti-recency
    finding is the key biological constraint missing from current CEM design. The
    monostrategy failure mode observed empirically in V3 (EXQ-475 follow-up, V_s
    invalidation cascade) has a complementary mechanism-level explanation: even with
    V_s invalidation resetting stale anchors, if CEM never proposes routes through
    those anchors, the map update has no behavioral effect. The exploration bonus
    ensures the full trajectory space is sampled. Implementation does not require
    SD-006 (runs synchronously in waking CEM). Quiescent mode (MECH-092) needs the
    bonus too, possibly at higher strength (MECH-291). Design doc to be written when
    implementation is scoped.
  evidence_quality_note: >
    No V3 substrate evidence yet; not implemented. Literature basis: Gupta et al.
    2010 (conf=0.80, Neuron, directly evidences the anti-recency property) +
    indirect support from Carr 2011 and Muessig 2019 (generative replay over all
    available paths). Promotion requires ablation experiment demonstrating
    diversity/monostrategy improvement.

- id: MECH-280
  pending_retest_after_substrate: true
  evidence_quality_note: |
    [update_z_goal_typeerror_swallowed] EXQ-471/475/483/483a/483b/490/490b/490c/490e/490f/524 cohort evidence is contaminated and superseded as of 2026-05-07 due to a script-side TypeError in agent.update_z_goal that was silently swallowed by a bare except, leaving GoalState.is_active() always False. Additional cohort-wide issues found: 2x agent.sense() per warmup step, agent.update_residue() never called (residue field empty / running_variance pinned), dACC bias diagnostic reading wrong tensor. Awaiting StepHarness migration + corrected re-runs (EXQ-475a / EXQ-483c / EXQ-490g / EXQ-471a / EXQ-524a). See REE_Working session 2026-05-07.

    [2026-05-08 governance]: EXQ-483b supports. PAG release ratio 1.875x (>1.30 threshold). Substrate-level mechanism validated in isolation; integration test pending MECH-307.

    [IGW-20260521-020]: SD-037 substrate-ready closed; MECH-280 wired path confirmed by 483b. Behavioural freeze->foraging transition still blocked by approach_commit=0 on standard grid.

    [2026-05-30 governance, V3-EXQ-483d autopsy applied]: evidence_direction non_contributory per failure_autopsy_V3-EXQ-483d_2026-05-30.json. PAG alpha_override is wired (PAGFreezeGate receives the scaling), but the env does not engage PAG freeze in this run -- no committed-freeze state for the exit-threshold scaling to act on. Claim could not express itself. Hold pending PAG-engaging env condition (sustained-threat curriculum). pending_retest_after_substrate=true. Full diagnosis under SD-037 evidence_quality_note + autopsy artifact.

    [2026-05-31 governance, V3-EXQ-483e autopsy applied]: evidence_direction non_contributory per failure_autopsy_V3-EXQ-483e_2026-05-31.json. 483e reuses SD-036+MECH-279 freeze-engaging substrate (use_gabaergic_decay=True + use_pag_freeze_gate=True everywhere) but PAG remains dormant: pag_release_count_end = 0 across ALL 12 runs including ARM_3 ON_ON arms in every seed. MECH-280's path requires z_harm_a * duration > theta_freeze (=2.0) for the alpha_override scaling to act on a committed-freeze state; fishtank baseline z_harm_a magnitudes never cross the threshold even with sustained env exposure. Same shape as 483d but one architectural layer deeper -- the issue is now consumer-input-threshold calibration, not consumer-cascade wiring. Claim could not express itself. Hold pending consumer-input-threshold recalibration (PAG theta_freeze knob vs fishtank baseline) AND/OR SD-029-style sustained-threat env curriculum that drives z_harm_a above the threshold during scheduled windows. pending_retest_after_substrate=true. Full diagnosis under SD-037 evidence_quality_note + autopsy artifact.

    [enum-comment migrated to field 2026-07-18] epistemic_category: set 2026-05-31 per
    failure_autopsy_V3-EXQ-483e (inherited from SD-037 parent); PAG theta_freeze threshold not
    cleared at fishtank baseline
  title: "LH-PAG override projection: a direct projection from LH (orexin and/or peri-LC GABA analog) to PAG modulates the GABAergic freeze gate (MECH-279) under metabolic demand. The projection does not silence the freeze gate; it raises its threshold (theta_freeze_effective = theta_freeze_base x (1 + alpha_override x override_signal)). This produces the behavioural transition from freeze to active foraging under sustained hunger plus threat -- the foraging-under-predation phenotype the lit-pull surveys."
  claim_type: mechanism_hypothesis
  subject: lh_pag.override_projection
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-483e_2026-05-31
      as_of: 2026-05-31
      verdict: non_contributory/substrate_ceiling
  epistemic_category: substrate_ceiling
  implementation_phase: v3
  v3_pending: true
  implementation_note: "IMPLEMENTED 2026-04-25 as part of SD-037 landing. PAGFreezeGateConfig.alpha_override (default 0.0) accepts the override_signal scaling; PAGFreezeGate.tick() now takes override_signal arg per cycle: exit_threshold = theta_freeze * (1 + alpha_override * override_signal) * gaba_tone. REEAgent wires override_alpha_pag (default 0.5) into PAG config when both use_broadcast_override and use_pag_freeze_gate are True. Substrate-readiness validation: V3-EXQ-483b PASS (2026-05-08). [2026-05-30 consumer-cascade amend]: PAG wiring unchanged this pass (already landed). V3-EXQ-483d FAIL traced to dormant PAG path in the fishtank validation env (no sustained-threat curriculum -> override_signal climbed but exit_threshold scaling had nowhere to discharge). End-to-end PAG-engaging behavioural testability restored via V3-EXQ-483e amend successor which reuses SD-036+MECH-279 freeze-engaging substrate (V3-EXQ-475-style config: use_gabaergic_decay=True + use_pag_freeze_gate=True). pending_retest_after_substrate stays TRUE; closes only after V3-EXQ-483e retest manifest lands. See SD-037 implementation_note 2026-05-30 paragraph for the parent consumer-cascade landing record."
  claim_level: mechanistic
  registered_utc: 2026-04-22
  ceiling_decision: deferred
  ceiling_routing_note: "2026-06-19 substrate-ceiling-lifted triage (verdict b deliberate-defer): SD-037 broadcast.override_regulator is wired (483b PAG release 1.875x) but its multiplicative gain has a ZERO committed-freeze baseline -- PAG never engages at fishtank z_harm_a (483e: pag_release=0/12). Co-gated with MECH-281 on the SD-037 axis-b thread (sd_037_axis_b_sustained_threat_curriculum_plan.md): terminal retest V3-EXQ-483f is reserved/blocked behind 625e (GAP-A conversion-propagation-under-threat). 625d already FAILed/self-routed substrate_not_ready 2026-06-18. Do not requeue on the fishtank surface."
  depends_on:
    - SD-037    # parent: provides override_signal
    - MECH-279  # the freeze gate this raises the threshold of
    - SD-036    # GABAergic decay layer (paired regulator)
    - SD-012    # drive_level (input to override_signal)
  functional_restatement: >
    MECH-279 implements the freeze commit threshold (z_harm_a x duration >
    theta_freeze) with a fixed theta_freeze. MECH-280 makes theta_freeze a
    function of override_signal: under high metabolic demand and sustained
    threat, the threshold rises so the agent does not commit to freeze --
    instead, active foraging is permitted despite sustained z_harm_a.

    Falsifiable (primary): in a paradigm with sustained threat (z_harm_a
    elevated for many steps) and rising drive_level, an agent with MECH-280
    enabled should transition from freeze to foraging at a predictable
    drive_level threshold; an agent with MECH-280 disabled should remain
    frozen indefinitely (the EXQ-471 lock).

    Falsifiable (secondary): the threshold of the freeze->foraging
    transition should depend on both drive_level and the duration of
    sustained threat -- short threats with rising drive should not
    override; long sustained threats with rising drive should override.
  notes: >
    Evidence anchors: Marino 2020 (LH GABA -> peri-LC for foraging
    engagement); de Araujo Salgado 2023 (sustained hunger overrides
    predator avoidance via LH-PAG-relevant circuitry); Wang/Lin 2015
    (PAG as downstream arbiter of broadcast threat signals).
    Design doc: docs/architecture/sd_037_broadcast_override_regulator.md
    section "MECH-280: LH-PAG override projection".
  location: docs/architecture/sd_037_broadcast_override_regulator.md

- id: MECH-281
  pending_retest_after_substrate: true
  evidence_quality_note: |
    [update_z_goal_typeerror_swallowed] EXQ-471/475/483/483a/483b/490/490b/490c/490e/490f/524 cohort evidence is contaminated and superseded as of 2026-05-07 due to a script-side TypeError in agent.update_z_goal that was silently swallowed by a bare except, leaving GoalState.is_active() always False. Additional cohort-wide issues found: 2x agent.sense() per warmup step, agent.update_residue() never called (residue field empty / running_variance pinned), dACC bias diagnostic reading wrong tensor. Awaiting StepHarness migration + corrected re-runs (EXQ-475a / EXQ-483c / EXQ-490g / EXQ-471a / EXQ-524a). See REE_Working session 2026-05-07.

    [2026-05-08 governance]: EXQ-483b supports. Entropy amplification confirmed. Substrate-level mechanism validated in isolation; integration test pending MECH-307.

    [IGW-20260521-020]: Partial landing (GoalState seeding + SalienceCoordinator) substrate-ready per 483b; PFC/BLA/beta-gate consumers deferred. Cataplexy/PWS behavioural tests still N/A (approach_commit=0).

    [2026-05-30 governance, V3-EXQ-483d autopsy applied]: evidence_direction non_contributory per failure_autopsy_V3-EXQ-483d_2026-05-30.json. EXQ-483d clears approach_commit=0 (now 1.0 via MECH-295 bridge) but produces a fresh wired-but-inert signature on goal_norm_peak discrimination: GoalState seeding amplification path is dominated by MECH-295 bridge contribution to effective_drive, and SalienceCoordinator + PFC/BLA/beta-gate consumers remain dormant (consumer-cascade deferral per implementation_note). Override produces saturated override_signal but has nowhere to land where it would move goal_norm_peak against the bridge baseline. pending_retest_after_substrate=true. Full diagnosis under SD-037 evidence_quality_note + autopsy artifact.

    [2026-05-31 governance, V3-EXQ-483e autopsy applied]: evidence_direction non_contributory per failure_autopsy_V3-EXQ-483e_2026-05-31.json. 2026-05-30 consumer-cascade amend (deferral closed) landed all four MECH-281 motor-coupling consumers (LateralPFC eff_eta, BLA encoding_gain, CeA mode_prior+fast_prime, BetaGate urgency_interrupt_threshold) -- the amend correctly fires at the override_signal interface (override_signal_nonzero_steps saturates) but ALL consumer outputs at zero across ALL 12 runs: bla_encoding_gain_peak=0, cea_mode_prior_peak=0, cea_fast_prime_peak=0, beta_release_count=0. Only lateral_pfc rule_state EMA responds (1.08-1.18x, sub-threshold). Diagnosis: SD-037 multiplicative gain pattern requires consumer-module input gates crossed first; BLA arousal_threshold_on=0.4 / CeA fast_route_threshold=0.5 / dACC pe-magnitude floors above fishtank baseline magnitudes. Cataplexy lesion test (override_signal=0) and PWS saturation test (override_signal=1) remain untestable end-to-end at this substrate layer. Hold pending consumer-input-threshold recalibration + env enrichment. pending_retest_after_substrate=true. Full diagnosis under SD-037 evidence_quality_note + autopsy artifact.

    [enum-comment migrated to field 2026-07-18] epistemic_category: set 2026-05-31 per
    failure_autopsy_V3-EXQ-483e (inherited from SD-037 parent); BLA / CeA / dACC /
    beta-interrupt consumer input thresholds not cleared at fishtank baseline
  title: "Orexin-analog gain modulation of drive-coupled arousal (motor-coupling axis): an orexin-analog modulator gates the coupling between drive_level and arousal-relevant downstream targets (PFC for SD-033a deliberation, BLA/CeA for SD-035 amygdala arbitration, beta-gate for MECH-090). Loss of this coupling produces a cataplexy-analog failure where emotional/drive input fails to recruit motor activation -- z_harm and drive_level compute normally but action selection does not integrate them. This is the motor-coupling axis of orexin function; the wake-stability axis (sleep-state gating, the dominant biological role) is MECH-286. Both axes are downstream of the same SD-037 override_signal, predicting clinical co-occurrence of cataplexy with sleep-state instability under orexin loss (narcolepsy)."
  claim_type: mechanism_hypothesis
  subject: orexin.drive_arousal_coupling
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-483e_2026-05-31
      as_of: 2026-05-31
      verdict: non_contributory/substrate_ceiling
  epistemic_category: substrate_ceiling
  implementation_phase: v3
  v3_pending: true
  implementation_note: "PARTIALLY IMPLEMENTED 2026-04-25 as part of SD-037 landing. Drive-coupling axis wired at GoalState seeding (effective_drive amplified by override_goal_seeding_gain * override_signal in REEAgent.update_z_goal) and at SalienceCoordinator (operating-mode reweight via affinity_weights['override_signal']). PFC/BLA/beta-gate targets noted in claim title rely on the same override_signal but additional consumer wiring is deferred until those substrates need it. Cataplexy lesion test (override_signal=0) and PWS saturation test (override_signal=1) are V3-EXQ-483 secondary acceptance criteria. [2026-05-30 consumer-cascade amend, deferral closed]: Four additional consumer sites named in the claim title now wired. (i) LateralPFCAnalog (SD-033a deliberation, ree_core/pfc/lateral_pfc_analog.py): update() gains override_signal + override_eta_gain kwargs; eff_eta scaled by (1 + override_eta_gain * override_signal). Orexin-recruited state accelerates rule_state EMA learning. (ii) BLAAnalog (SD-035 amygdala arbitration, ree_core/amygdala/bla.py): tick() gains override_signal + override_encoding_gain kwargs; final encoding_gain (after inverted-U + post-event window) scaled by (1 + override_encoding_gain * override_signal). Roozendaal 2011 anchor: orexin -> NE / amygdala enhanced LTP. (iii) CeAAnalog (SD-035 amygdala arbitration, ree_core/amygdala/cea.py): tick() gains override_signal + override_amplitude_gain kwargs; both mode_prior and fast_prime scaled by (1 + override_amplitude_gain * override_signal), re-clipped to mode_prior_log_odds_max so CeA cannot over-rule cortex via the amplified path. (iv) BetaGate / agent.py urgency_interrupt path (MECH-090 motor side, ree_core/agent.py select_action MECH-091 block): urgency_threshold scaled by max(0.0, 1 - override_beta_interrupt_gain * override_signal). Orexin -> escape-from-freeze on the motor side, parallel to the already-wired PAG alpha_override path on the freeze-gate side; lower threshold under recruited state -> committed motor program more readily aborted on z_harm_a / z_harm_un urgency. All four new REEConfig gains default 0.0 (bit-identical OFF). 556/556 contracts + 7/7 preflight PASS with all four gains at default 0.0 (was 543 + 13 new MECH-281 contracts at tests/contracts/test_sd_037_consumer_cascade.py). Activation smoke 2026-05-30 (50 ticks drive=0.9 / harm=0.6 -> override_signal=0.768): BLA encoding_gain 2.5 -> 5.0 (2x at gain=1); CeA mode_prior 0.2 -> 0.4, fast_prime 0.15 -> 0.3 (2x each at gain=1); LateralPFC rule_state delta 0.0956 -> 0.2867 (3x at gain=2). MECH-094 preserved via existing simulation_mode arguments on BLA/CeA tick + MECH-319 simulation-mode-rule-gate on LateralPFC update. Validation experiment: V3-EXQ-483e queued (4-arm successor under 483 lineage; reuses SD-036+MECH-279 freeze-engaging substrate for PAG-engaging env condition; SalienceCoordinator enable via use_salience_coordinator=True; claim_ids=[SD-037, MECH-280, MECH-281]). Cataplexy lesion test (override_signal=0) and PWS saturation test (override_signal=1) NOT executed this pass -- they are V3-EXQ-483 secondary acceptance criteria, now decisively testable end-to-end via 483e. pending_retest_after_substrate stays TRUE; closes only after V3-EXQ-483e retest manifest lands. ree-v3/CLAUDE.md SD-037 consumer-cascade section appended 2026-05-30."
  claim_level: mechanistic
  registered_utc: 2026-04-22
  ceiling_decision: deferred
  ceiling_routing_note: "2026-06-19 substrate-ceiling-lifted triage (verdict b deliberate-defer): co-gated with MECH-280 on SD-037 axis-b. Consumer outputs (BLA/CeA/BetaGate) are all zero at fishtank baseline (483e) so SD-037's multiplicative broadcast gain has nothing to scale -> cataplexy/PWS dissociation unreachable. Terminal retest V3-EXQ-483f blocked behind 625e/GAP-A. Needs env enrichment driving z_harm_a above BLA/CeA input thresholds, not more fishtank experiments."
  depends_on:
    - SD-037    # parent: provides override_signal
    - SD-012    # drive_level being coupled
    - MECH-090  # bistable beta gate (downstream coupling target)
    - SD-033a   # lateral PFC analogue (downstream coupling target)
    - SD-035    # amygdala analogue (downstream coupling target)
    - MECH-286  # sibling: wake-stability axis of the same orexin-analog signal
  functional_restatement: >
    The override_signal does not act in isolation -- it gates the gain
    on drive->action-pipeline projections. Concretely: drive_level's
    weight in z_goal seeding, drive_level's modulation of beta-gate
    sensitivity, and drive_level's modulation of PFC deliberation
    priors (SD-033a) all scale with override_signal.

    Falsifiable (primary, lesion test): setting override_signal to
    constant zero should produce decoupling -- drive_level accumulates,
    threat is computed, but action selection fails to integrate them.
    The agent should appear inert despite both signals being present.
    This is the cataplexy-analog phenotype.

    Falsifiable (secondary, saturation test): override_signal pinned
    at maximum should produce uncoupled-drive pursuit -- drive_level
    seeds z_goal aggressively even under maximum z_harm. This is the
    PWS-hyperphagia analog.
  notes: >
    Evidence anchors: Bjorness/Greene 2024 (orexin as coupling signal
    for motivated arousal); James/Aston-Jones 2017 (orexin recruitment
    under high effort/demand); Bassetti 2019 (narcolepsy as coupling-loss
    clinical model with cataplexy as the diagnostic).
    Design doc: docs/architecture/sd_037_broadcast_override_regulator.md
    section "MECH-281: Orexin gain modulation of drive-coupled arousal".
  pharmacological_predictions:
    - PHARM-003  # venlafaxine / clomipramine (anti-cataplectic, motor-coupling axis)
  location: docs/architecture/sd_037_broadcast_override_regulator.md

- id: MECH-282
  title: "LPB interoceptive routing into harm-arbitration: a lateral-parabrachial-nucleus analog (LPB) routes interoceptive distress signals (visceral malaise, taste-aversion analogs, hypoxia, hypoglycemia) into the harm-arbitration network via PBN-CGRP -> CeA -> PAG. This is the upstream broadcast arm of the threat-side architecture, parallel to VMHdm's predator-side broadcast. Both feed downstream arbiters where SD-037's override_signal reweights them. Architectural commitment: interoceptive harm has a separable input channel from external-threat z_harm, dissociable in pathology and in normal modulation."
  claim_type: mechanism_hypothesis
  subject: lpb.interoceptive_harm_routing
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-282@2026-06-10T16:18:22Z"
      as_of: 2026-06-10
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-04-22
  depends_on:
    - SD-037    # parent: override_signal reweights both interoceptive and external harm
    - SD-035    # amygdala analogue (CeA on the routing path)
    - MECH-279  # PAG freeze gate (downstream arbiter)
    - SD-011    # z_harm_a affective harm stream (consumer of LPB routing)
    - SD-012    # drive_level (interoceptive input source)
  functional_restatement: >
    Current V3 z_harm computation is dominated by external-threat input.
    MECH-282 commits to a separable input channel that carries
    interoceptive/metabolic-distress signal: hunger-as-pain, hypoxia-as-
    pain, malaise. Under SD-037 override conditions, the interoceptive
    harm channel is NOT suppressed (hunger remains felt as suffering),
    but its weight in arbitration becomes the input that drives the
    override_signal recruitment threshold from below.

    Falsifiable (primary, dissociation): selectively elevating the
    interoceptive harm channel (e.g. simulating hypoxia) should drive
    behaviour distinctly from elevating external-threat z_harm. The
    interoceptive channel should produce drive-recruitment of override;
    the external channel should produce freeze/avoidance commitment.

    Falsifiable (secondary, lesion): removing the LPB routing (channel
    set to zero) should produce an agent that does not feel its own
    metabolic state -- drive accumulates without being phenomenologically
    present in the z_harm computation, override never recruits, agent
    starves silently.
  notes: >
    Evidence anchors: Palmiter 2018 (PBN CGRP general alarm review);
    Pyeon 2025 (PBN CGRP active defense modulation); Strigo/Craig 2016
    (insular common-currency framing for interoceptive emotions).
    Design doc: docs/architecture/sd_037_broadcast_override_regulator.md
    section "MECH-282: LPB interoceptive routing into harm-arbitration".
  evidence_quality_note: >
    Substrate IMPLEMENTED 2026-05-21 in ree_core/regulators/lpb_interoceptive_routing.py
    (LPBInteroceptiveRouter). Master switch REEConfig.use_lpb_interoceptive_routing
    (default False). Masks resource-field channels from harm_obs before HarmEncoder
    (external threat in z_harm); populates LatentState.z_harm_intero from drive_level
    + harm_obs_a resource slice. When coupled with use_broadcast_override=True,
    override recruitment uses intero magnitude (MECH-282 split); PAG freeze duration
    uses external magnitude. Substrate-readiness diagnostic V3-EXQ-600 queued.
    Promotion still requires full dissociation + lesion behavioural evidence.
  location: docs/architecture/sd_037_broadcast_override_regulator.md

- id: MECH-283
  title: "Verisimilitude recognition-for-recall gate: V_s computed at retrieval time (not proposal time) gates whether a stored episode is even a candidate for re-entry into the proposal stream. Recognition (does this fit my current situation?) is logically prior to anchor-eligibility (is this a good place to start a rollout from?). MECH-269's V_s is computed against the proposed anchor state; MECH-283's V_s is computed against the retrieved episode content. Loss of MECH-283 produces intrusive recall -- episodes that don't fit current context get pulled into the proposal stream (PTSD flashback / schizophrenic loose-association phenomenology)."
  claim_type: mechanism_hypothesis
  subject: hippocampal.v_s_recognition_recall_gate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-04-22
  depends_on:
    - MECH-269  # parent: anchor-eligibility V_s gate; MECH-283 sits upstream of it
    - ARC-007   # hippocampal path memory (substrate for retrieval)
  functional_restatement: >
    MECH-269's V_s gate operates on candidate anchor states. But there
    is a logically prior question: should this episode be retrieved at
    all, given current context? MECH-283 commits to a retrieval-time
    V_s computation against episode content (not anchor state). A
    retrieval candidate with low V_s on its episode content fails
    recognition before its anchor is tested.

    This explains why hippocampal recall is content-coherent even when
    anchor-eligibility would technically pass -- the recognition gate
    fires first.

    Falsifiable (primary): in a paradigm with two stored episodes (one
    matching current context, one not) sharing equally good anchor
    states, MECH-283-enabled retrieval should preferentially pull the
    context-matching episode. MECH-283-disabled retrieval should pull
    them at chance.

    Falsifiable (secondary, intrusive-recall test): under MECH-283
    dysfunction (gate threshold lowered), the agent should retrieve
    context-incongruent episodes and feed them to the proposal stream
    -- intrusive flashback phenomenology.
  notes: >
    Discussion origin: user observation 2026-04-22 that V_s is doing
    more than grounding the now -- it also gates recognition for
    recall, accumulates evidence of schema staleness during waking,
    and prioritises sleep consolidation. MECH-283 is the
    recognition-for-recall arm of this trio (with MECH-284 the
    accumulator and MECH-285 the sleep-priority readout).
    Design doc: docs/architecture/hippocampal_anchor_selection.md
    section "V_s as bidirectional signal".
  evidence_quality_note: >
    No substrate evidence. Substrate change is small (a retrieval-time
    V_s computation upstream of the anchor gate). Promotion requires
    substrate implementation and the falsifiable retrieval test above.
  location: docs/architecture/hippocampal_anchor_selection.md

- id: MECH-284
  title: "V_s residual schema-staleness accumulator: a region-indexed scalar that integrates broadcast invalidation events (MECH-287) with attribution_weight credit assignment over the active anchor set, leaks slowly, and is read by both online anchor-reset (MECH-269 hysteresis check, waking) and sleep-consolidation priority (MECH-285, offline). Distinct from generic prediction error (which operates on latent values and updates online via gradient): MECH-284 is region-indexed, accumulates across events, and exposes a dual read-out (online + offline). Refined 2026-04-22 from sleep-only formulation to dual-readout formulation after V3-EXQ-475 demonstrated that an agent trapped in a stale anchor needs an online consumer of the staleness signal -- MECH-285 sleep-only readout cannot rescue the agent during waking. Saturated map produces over-attention to schema revision (anxiety-analog); absent map produces schema rigidity (V3-EXQ-475 phenotype: 5-6 PAG releases per seed but never integrate to anchor invalidation -> 1000/1000 freeze-active steps)."
  claim_type: mechanism_hypothesis
  subject: hippocampal.v_s_residual_schema_staleness_accumulator
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-14
    needs_review: false
    evidence:
      from: "decision:MECH-284@2026-07-14T20:41:53.792349Z"
      as_of: 2026-07-14
      verdict: promote_to_provisional/applied
  implementation_phase: v3
  v3_pending: false
  claim_level: mechanistic
  registered_utc: 2026-04-22
  depends_on:
    - MECH-287  # upstream: trigger event that drives integration
    - MECH-269  # downstream consumer (anchor-reset hysteresis read-out, online)
    - MECH-285  # downstream consumer (sleep-consolidation priority read-out, offline)
    - MECH-094  # interaction (probe-tagged content must NOT increment staleness as if veridical)
    - ARC-007   # hippocampal path memory (regions being indexed)
    - ARC-018   # viability map (regions also indexed here)
  functional_restatement: >
    Standard prediction error operates on latent values themselves and
    updates the relevant heads via gradient (E1 sensorium loss, E2
    transition loss, E3 harm/goal loss). MECH-284 operates on the
    schema regions in the hippocampal map, accumulating across events,
    and is read by both online anchor-reset (MECH-269) and sleep
    consolidation (MECH-285).

    Operational definition (refined 2026-04-22). For each schema region
    r in active_anchor_set(t):
      if MECH-287 trigger(t):
        staleness[r] += attribution_weight(r, source_streams) * magnitude
      staleness[r] *= leak_factor   # slow per-tick decay, e.g. 0.995

    attribution_weight credits the broadcast trigger to anchor regions
    that produced the failed prediction; the biology candidate is the
    cortical projection topology onto LC (Aston-Jones-Cohen 2005), which
    means structurally-connected anchors get more credit -- in the
    in-silico version, attribution_weight is parametrised by the cosine
    similarity between the trigger's source-stream pattern and the
    anchor region's cached source-stream signature.

    Dual read-out:
      online (MECH-269 anchor-reset): each tick, V_s_anchor(a, t) =
        V_s(r, t) - staleness[r] is computed; if below theta_reset for
        k consecutive steps the anchor is marked inactive (NOT erased
        -- dual-trace preservation per Bouton 2004) and a new anchor
        is sought.
      offline (MECH-285 sleep priority): accumulated staleness biases
        replay start-state distribution during sleep so high-staleness
        regions get replayed first, more often, with higher schema-
        update authority.

    Why dual read-out is load-bearing. Pre-2026-04-22, MECH-284 was
    framed as sleep-only (consumed by MECH-285 only). V3-EXQ-475
    falsified this implicitly: an agent locked in catatonic freeze
    cannot wait for sleep to clear the stale anchor -- the freeze
    itself prevents sleep entry under MECH-286. The staleness signal
    must be readable online by MECH-269 anchor-reset to break the
    cycle during waking, with sleep MECH-285 providing the deeper
    schema-repair pass for residual staleness that survives the
    online reset.

    Falsifiable (primary, EXQ-475 phenotype): with MECH-287 trigger
    and MECH-269 online reset both enabled, an EXQ-471-conditions
    re-run should produce attenuated freeze re-commit count after
    the first 2-3 PAG releases (anchor invalidates, proposer remaps,
    re-commit rate drops). With MECH-287 enabled but MECH-284 online
    read-out disabled, the EXQ-475 phenotype should reproduce
    (releases register but never integrate).

    Falsifiable (secondary, dissociation from prediction error): a
    schema region with high prediction error in a single episode but
    correct on average should produce low MECH-284 staleness;
    a region with low individual-episode prediction error but
    systematic under-prediction should accumulate high staleness.
    Standard prediction error and MECH-284 staleness should
    dissociate empirically.

    Falsifiable (tertiary, attribution_weight specificity): broadcast
    triggers occurring during an inactive_anchor state should produce
    near-zero staleness increment across the map (no active anchor to
    credit); broadcast triggers occurring during a single-anchor state
    should produce a peaked increment on that anchor's region; broadcast
    triggers occurring during a multi-anchor state should produce an
    increment distributed across anchors weighted by their
    attribution_weight to the source stream.
  notes: >
    Discussion origin: registration 2026-04-22 in V_s bidirectional
    cluster. Operational refinement 2026-04-22 (same day, post-EXQ-475)
    from SYNTHESIS of targeted_review_waking_v_s_invalidation/. Key
    refinement: dual read-out (online + offline) rather than sleep-only.

    Design doc (refined): docs/architecture/v_s_invalidation_runtime.md
    (covers MECH-284 operational definition + MECH-287 trigger spec
    + MECH-269 anchor-reset criteria with dual-trace preservation).
    Original registration design context remains at
    docs/architecture/hippocampal_anchor_selection.md section "V_s as
    bidirectional signal".

    Substrate hooks (planned): ree_core/hippocampal/staleness_accumulator.py
    (new) implementing the per-region integrator with attribution_weight
    credit assignment and dual read-out hooks for MECH-269 and MECH-285.

    Conceptually closest neighbour in the existing literature is the
    meta-cognitive uncertainty-monitoring literature (Fleming, Lau) but
    with a schema-regional substrate rather than a global confidence
    scalar; biological grounding shifts toward OFC representational
    drift (Wilson 2014, Stalnaker 2015, Gardner 2018) per the lit-pull
    SYNTHESIS.
  evidence_quality_note: >
    Lit-pull lit_confidence 0.855 (literature supports OFC representational
    drift as accumulator substrate, with LC-tonic amplification of OFC
    drift sensitivity under elevated surprise). Substrate implemented
    2026-04-24 as ree-v3 ree_core/hippocampal/staleness_accumulator.py
    (Phase 3 of V_s invalidation runtime); HippocampalConfig flags
    use_staleness_accumulator (default False) and use_mech284_hysteresis
    (default False; switches MECH-269 AnchorSet.tick_hysteresis from the
    Phase 2 internal (tick-last_accessed)*staleness_rate proxy to the
    Phase 3 accumulator lookup). Region key (scale, segment_id) -- matches
    per_region_vs partition; stream_mixture dropped. Attribution modes
    "equal" and "stream_overlap". Integration site is
    HippocampalModule.tick_anchor_set between consume_boundary_events
    (MECH-269 Phase 2 ii) and tick_hysteresis. 91/91 preflight + contract
    regression PASS with flags OFF (bit-identical). Activation smoke
    PASS (two ARMs): ARM1 verified two-region equal-credit integration +
    leak; ARM2 verified hysteresis swap fires at k=3 under staleness
    lookup. Promotion still requires the EXQ-475 unlock test (V3-EXQ-478
    queued), the dissociation-from-prediction-error test, and the
    attribution_weight specificity test -- v3_pending retained.

    2026-04-25 governance: V3-EXQ-478 (Phase 3 diagnostic, FAIL) and
    V3-EXQ-480 (4-arm parameter sweep across attribution_mode +
    leak_factor, FAIL) both flagged Phase 3 as wired-but-behaviourally-
    inert. Accumulator populates correctly (mean staleness peaks
    ~0.94/0.83 per seed), anchor resets fire (63/31 per seed), but
    freeze_recommit_count stays at 1 across all arms and OFF baseline
    entropy=0 even with SD-029 curriculum ON (broken at action-class
    granularity, see EXQ-482 reclass below). Both runs tagged
    inconclusive per_claim. Diagnosis: read-side hook is the bottleneck
    (V3-EXQ-481 confirms vs_commit_release_count=0 in both arms --
    release path is dead code on this env). Substrate stays candidate
    / v3_pending; not weakening evidence against MECH-284. Next step:
    revisit BoundaryEvent rate, anchor-key snapshot scope, or
    commit-entry capture sites before re-running unlock-test suite.

    [2026-07-14 governance -- promote candidate->provisional, v3_pending
    cleared]: V3-EXQ-758 (v3_exq_758_mech284_staleness_accumulator_monotonicity_20260714T200900Z_v3)
    is the FIRST indexed in-silico SUPPORTING evidence for MECH-284. PASS /
    evidence_direction supports, evidence_class exp:simulation. Wall-independent
    functional-signature isolation test (action-free, training-free) driving the
    REAL StalenessAccumulator.integrate()/tick_leak()/snapshot() path with real
    BroadcastEvent + Anchor objects over 5 seeds; DV = region-indexed snapshot()
    scalar; non_degenerate, both readiness preconditions met (peak cross-region
    RANGE and peak vs_p level supra-floor, populated ONLY via real integrate(),
    guarding the V3-EXQ-688 vacuous-null). All three MECH-284 criteria met across
    5/5 seeds -- C2_region_locality_spread (LOAD-BEARING: perturbed region rises
    to peak while non-overlapping fresh regions stay 0.0, range 1.0), C1_monotone_rise
    (graded dose-integration 0.15->1.0, monotone), C3_leak_decay (geometric
    leak_factor=0.5 decay to drop-from-snapshot). This is the decisive region-indexed
    readout that was ABSENT from every prior manifest (455a/592f reference MECH-284
    but never tested its functional signature; the 2026-04-25 EXQ-478/480 runs were
    inconclusive/behaviourally-inert on a dead read-side hook, NOT supporting
    evidence). Clears the "no in-silico supporting evidence" precondition behind the
    v3_pending hold. Accumulator complement to MECH-287 (the invalidation trigger
    that drives MECH-284), promoted candidate->provisional the same day via the sibling
    isolation run V3-EXQ-757. NOT-YET-CLEARED for promotion-to-active: this isolation
    test does not exercise the integrated closed-loop agent, so the pre-registered
    promotion bar above -- the EXQ-475 unlock test (V3-EXQ-478 lineage), the
    dissociation-from-prediction-error test, and the attribution_weight specificity
    test -- remains the separate promote-to-active gate. (No arm_results / no OFF
    training arm -> no baseline mint; pure-arithmetic substrate probe, nothing to
    re-train.)
  implementation_note: >
    MECH-284 Phase 3 implemented 2026-04-24 in ree-v3. Module
    ree_core/hippocampal/staleness_accumulator.py. Config flags
    HippocampalConfig.use_staleness_accumulator and
    HippocampalConfig.use_mech284_hysteresis (both default False;
    threaded through REEConfig.from_dims). Validation experiment
    V3-EXQ-478 queued. Phase 3 online-arm only; MECH-285 offline
    sleep-priority read-out remains deferred.
  pharmacological_predictions:
    - PHARM-001  # DORA-class clears residual staleness MECH-284 accumulates by enabling MECH-285 readout
  location: docs/architecture/v_s_invalidation_runtime.md

- id: MECH-285
  title: "Sleep-consolidation priority from V_s residuals: during sleep-phase Bayesian aggregation (MECH-275) and sleep-dependent self-model aggregation (MECH-273), the order and weight of replay events is biased by the schema-staleness map produced by MECH-284. Replay is not uniform across schema and is not solely salience-driven (dopaminergic tagging); it is also prioritised by accumulated verisimilitude residuals. Schema regions with the most accumulated 'this is wrong' evidence get replayed first, more often, and with higher schema-update authority during sleep. PTSD-rumination has a clean architectural account here: MECH-285 priority loss co-occurring with MECH-094 hypothesis-tag dysfunction, locking the staleness tag from ever clearing."
  claim_type: mechanism_hypothesis
  subject: hippocampal.sleep_consolidation_priority_from_v_s_residuals
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-285@2026-06-23T22:40:43.016136Z"
      as_of: 2026-06-23
      verdict: clear_v3_pending_gate/applied
  implementation_phase: v3
  v3_pending: false
  v3_pending_cleared_note: "2026-06-23 governance: v3_pending gate cleared on the strength of V3-EXQ-702 (sleep_substrate:GAP-3b) -- 3/3 seeds supports for staleness-priority replay skew (injected HOT>>COLD draws, non-uniform snapshot) with genuine ARM_OFF no-draws. impl_phase stays v3 (v3_run_ct>=1, no re-hold). Promotion not applied -- evidence now counts toward the candidate->provisional gate."
  claim_level: mechanistic
  registered_utc: 2026-04-22
  depends_on:
    - MECH-284  # parent: produces the staleness map this consumes
    - MECH-275  # general sleep-phase Bayesian aggregation (this biases its replay priority)
    - MECH-273  # sleep self-model aggregation (also biased by this priority)
    - MECH-272  # state-gated routing (sleep regime where this fires)
    - MECH-094  # hypothesis tag (co-failure with MECH-285 produces PTSD architecture)
    - MECH-269  # V_s computation (substrate)
    - ARC-007   # hippocampal path memory
  functional_restatement: >
    Existing accounts of replay prioritisation are dopaminergic
    salience-tagging: emotionally significant events get prioritised.
    MECH-285 commits to an additional, dissociable priority signal:
    epistemically important regions (where the model is wrong) get
    prioritised even if they are emotionally flat. The two priority
    signals are partially redundant in normal life (emotionally
    significant events are often also epistemically important) but
    dissociable under specific conditions: a calm, low-arousal but
    high-novelty environment should produce sleep-replay biased toward
    novelty even without salience tagging.

    Concretely: sleep replay's start-state distribution (from the
    MECH-269 / Pfeiffer & Foster 2013 substrate) is weighted by
    accumulated MECH-284 staleness.

    Falsifiable (primary, dissociation from salience): in a low-arousal
    high-novelty paradigm, sleep replay content should be biased toward
    the novel-but-flat regions when MECH-285 is enabled, biased toward
    salience-tagged regions when MECH-285 is disabled (salience-only
    fallback).

    Falsifiable (secondary, PTSD-rumination architecture): jointly
    disabling MECH-285 (priority loss) and MECH-094 (hypothesis-tag
    loss) should produce a stable rumination loop -- the staleness
    tag accumulates from a traumatic episode but never gets cleared,
    because the priority signal that should pull it into replay is
    broken AND because the simulated-vs-real distinction is broken
    so the replay that does occur cannot update the schema. Either
    failure alone should produce a milder, recoverable phenotype.
  notes: >
    Discussion origin: same as MECH-283/284. The sleep-priority readout
    arm of the V_s bidirectional trio. The PTSD failure-mode prediction
    is the architectural payoff: PTSD has been a coarse cluster in the
    REE psychiatric_failure_modes literature; MECH-285 + MECH-094
    co-failure gives a precise architectural decomposition.
    Design doc: docs/architecture/hippocampal_anchor_selection.md
    section "V_s as bidirectional signal".
  evidence_quality_note: >
    No substrate evidence yet; offline-arm substrate deferred (online arm
    MECH-284 Phase 3 landed 2026-04-24). Design specification folded in
    from 2026-04-24 lit-pull (see literature_evidence below and
    docs/architecture/v_s_invalidation_runtime.md "MECH-285 offline arm
    — design" subsection). The PTSD-rumination architectural prediction
    is testable via clinical phenomenology comparison if a V3 agent
    can be put through the joint-lesion paradigm. Promotion requires
    substrate implementation and the falsifiable dissociation-from-
    salience test above.
  literature_evidence: |
    Targeted review `evidence/literature/targeted_review_mech285_sleep_replay_seed/`
    (10 papers + SYNTHESIS.md, 2026-04-24) -- commissioned to adjudicate the
    offline-arm design before substrate implementation. Literature confidence
    0 -> 0.866 across 11 entries. Five architectural verdicts resolved:
    (1) seed coverage BROAD-UNBOUNDED (Karlsson & Frank 2009 remote replay;
    Gillespie/Liu 2021 past-rewarded-unvisited enrichment; Olafsdottir 2015
    goal-biased preplay); (2) priority shape STALENESS-PROPORTIONAL, not
    threshold-gated (Michon et al 2019 graded reward-magnitude scaling;
    Mattar & Daw 2018 EVB continuous function); (3) timing FIRST-BOUT
    (integration delay is consumer-side, not sampler-side; Schlichting &
    Preston 2015 integration constraint applies to MECH-275 aggregation
    not MECH-285 sampling); (4) salience interaction DISSOCIABLE ARMS
    converging additively at the sampler (McNamara/Dupret 2014 causal
    salience-arm demonstration; Joo & Frank 2018 multi-mode SWR framing);
    (5) dual-trace at replay SUPPORTED (Gillespie 2021; Diba & Buzsáki
    2007 forward/reverse modes). Implementation commitments in
    v_s_invalidation_runtime.md design subsection.
  pharmacological_predictions:
    - PHARM-001  # DORA-class enables this readout to fire on accumulated staleness
    - PHARM-005  # prazosin (indirect override damping) -> sustained REM -> MECH-285 firing
    - PHARM-006  # SSRIs targeting orthogonal axis (5-HT) do NOT recruit this readout
  implementation_note: >
    Online arm (MECH-284 Phase 3) landed 2026-04-24
    (ree-v3/ree_core/hippocampal/staleness_accumulator.py). Offline arm
    design committed 2026-04-25 in
    docs/architecture/sleep_aggregation_cluster.md (umbrella doc for
    MECH-272 / MECH-273 / MECH-275 / MECH-285). Lands at Phase 2 of the
    cluster build order as ree_core/sleep/replay_sampler.py:
    SleepReplaySampler reads StalenessAccumulator.snapshot() at sleep-
    mode entry and the AnchorSet dual-trace (active + inactive),
    samples via softmax(staleness / temperature) over the broad pool
    per the 2026-04-24 lit-pull verdicts. Flag use_mech285_sampler
    default False; bit-identical waking with flag OFF. EXP-0168
    (already drafted) is the validation gate.
  location: docs/architecture/sleep_aggregation_cluster.md

- id: MECH-286
  title: "Override-gated sleep-state transition: SD-037 override_signal also gates entry to and exit from offline (sleep) mode. High override (sustained drive + threat under metabolic demand) suppresses sleep-mode onset; low override + accumulated MECH-284 V_s-residual staleness + permissive threat context permits onset and recruits the MECH-285 sleep-replay priority readout. Architecturally this is the recruitment authority for the offline arm of the V_s bidirectional cluster: MECH-284 accumulates the demand for offline schema repair, MECH-285 sets the priority order, and MECH-286 provides the on-switch. Without MECH-286, MECH-285 produces a priority list with no transition mechanism to consume it. This is the V3 architectural account of orexin's canonical role as wake-stability gate (Saper/Scammell flip-flop), which MECH-281 (drive-arousal coupling) understates by framing cataplexy purely as a motor-coupling failure."
  claim_type: mechanism_hypothesis
  subject: sleep.override_gated_state_transition
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_891_mech286_sleep_onset_conjunction_signature_20260807T185658Z_v3
      as_of: 2026-08-08
      verdict: supports/PASS -- 5/5 criteria clean AND, first genuine substrate touchpoint
  implementation_phase: v3
  implementation_note: "IMPLEMENTED 2026-05-21: ree_core/sleep/sleep_onset_gate.py + SleepLoopManager._run_cycle gate (override < mech286_theta_sleep_permit, max MECH-284 staleness > mech286_theta_sleep_recruit, z_harm_a tonic < mech286_threat_tonic_threshold). REEConfig.use_mech286_sleep_onset_gate default False. Validation V3-EXQ-599 queued. Evidence still pending."
  claim_level: mechanistic
  registered_utc: 2026-04-22
  v3_pending: false
  depends_on:
    - SD-037    # parent: provides override_signal which gates the transition
    - MECH-284  # accumulates the staleness demand that motivates offline entry
    - MECH-285  # the offline priority readout this recruitment authority enables
    - MECH-281  # sibling: motor-coupling axis of orexin loss; this is the wake-stability axis
    - MECH-272  # state-gated routing distinguishes wake vs sleep regime; this is the gate
    - MECH-094  # hypothesis tag (co-failure with MECH-286 deepens PTSD-chronicity architecture)
  functional_restatement: >
    Existing accounts of sleep onset in REE are silent on the recruitment
    authority. MECH-272 distinguishes wake-routing from sleep-routing,
    MECH-273/275 specify what sleep does, MECH-285 specifies replay
    priority, but no claim specifies what triggers the transition.
    MECH-286 commits to: the same SD-037 override_signal that gates
    drive->z_goal seeding under threat also gates the wake-to-sleep
    transition. They are the same regulator at different decision
    points -- one says "act on drive despite threat", the other says
    "go offline despite remaining demand".

    Concretely, sleep-mode entry requires:
      override_signal(t) < theta_sleep_permit
        AND MECH-284_staleness_accumulator(t) > theta_sleep_recruit
        AND threat_context permissive (z_harm_a tonic component low)
    All three jointly. This makes sleep onset a positive recruitment
    rather than a passive default.

    Falsifiable (primary, hyperarousal lesion): pinning override_signal
    high under conditions that would normally permit sleep should produce
    sleep-onset failure -- the agent remains in wake-mode even with high
    accumulated MECH-284 staleness. This is the hyperarousal-insomnia
    architectural phenotype.

    Falsifiable (secondary, narcolepsy lesion): setting override_signal
    constant near zero should produce uncontrolled sleep intrusion --
    sleep-mode entry triggered without staleness demand or with
    inappropriate threat context. This is the orexin-loss narcolepsy
    phenotype, distinct from MECH-281's cataplexy phenotype but produced
    by the same lesion (predicting their clinical co-occurrence).

    Falsifiable (tertiary, schema-repair starvation): chronic
    hyperarousal sustained over many cycles should produce stable
    accumulation of MECH-284 staleness with no clearance, observable
    as deteriorating schema fit on novel inputs even with intact
    waking computation. This is the architectural mechanism behind
    sleep-deprivation cognitive decline and a contributor to PTSD
    chronicity (paired with MECH-285 + MECH-094 as the rumination
    architecture).
  notes: >
    Discussion origin: 2026-04-22 follow-up to SD-037 cluster registration.
    User flagged that the orexin cluster as registered understates the
    canonical wake-stability role of orexin biologically -- narcolepsy
    is fundamentally a sleep-state-stability disorder, not just a
    motor-coupling disorder. MECH-286 closes the gap and unifies the
    SD-037 override layer with the V_s bidirectional cluster's offline
    arm (MECH-284/285).

    Evidence anchors (biology): Saper/Scammell flip-flop literature
    (orexin as wake-stability gate against VLPO sleep-active circuits);
    orexin neuron silence during sleep, firing during wake; Bassetti
    2019 narcolepsy review (sleep-state instability as core deficit,
    cataplexy as separable downstream symptom).

    Evidence anchors (clinical): chronic insomnia in PTSD, severe
    melancholic depression with early-morning waking + ruminative
    hyperarousal, sleep deprivation as established psychosis precipitant
    -- all consistent with a saturated-override schema-repair-starvation
    architecture.

    Design doc: docs/architecture/sd_037_broadcast_override_regulator.md
    (sleep-state-transition extension to be added).
    Psychiatric failure mode: docs/architecture/psychiatric_failure_modes.md
    section "Hyperarousal Insomnia and Schema-Repair Starvation".
  evidence_quality_note: >
    No substrate evidence. Strong biological grounding (canonical orexin
    wake-stability role) and strong clinical grounding (insomnia as a
    transdiagnostic feature of hyperarousal disorders). Promotion
    requires substrate implementation alongside MECH-281 and the
    falsifiable lesion tests above. The hyperarousal-insomnia phenotype
    is the diagnostic dissociation from MECH-281's cataplexy phenotype:
    same upstream lesion (orexin loss), different downstream targets
    (sleep-state gate vs motor-coupling gate).

    [/governance 2026-08-08, V3-EXQ-891, first genuine substrate touchpoint]: PASS/supports, all 5
    criteria clean AND-combined in the driver (no OR-trap), non-outcome skim clean. Architecture note:
    promotion is gated regardless of this result -- MECH-286 depends on SD-037 and MECH-281, both
    still v3_pending=true (unbuilt), so this is real supporting evidence but cannot itself move the
    claim past candidate. Stays candidate.
  pharmacological_predictions:
    - PHARM-001  # DORA-class direct substrate intervention for saturated MECH-286
    - PHARM-002  # modafinil / sodium oxybate target this axis under absent MECH-286 (narcolepsy)
    - PHARM-005  # prazosin (indirect override damping) permits MECH-286 sleep-state entry
    - PHARM-006  # SSRIs (orthogonal 5-HT axis) do NOT durably help this failure mode
  location: docs/architecture/sd_037_broadcast_override_regulator.md

- id: MECH-287
  title: "Dual-component online invalidation trigger: an UPSTREAM anchor-side comparator (CA1/CA3 mismatch flowing through subicular routing per Vinogradova 2001 + O'Mara 2009) coupled to a DOWNSTREAM broadcast event (LC phasic burst per Aston-Jones-Cohen 2005, with parallel signals from generalised dopaminergic prediction error per Schultz 1997 and lateral habenula negative-RPE per Matsumoto-Hikosaka 2007), wired together by the HC-VTA loop (Lisman & Grace 2005). The two components are coupled stages of one trigger event, not alternatives. Distinct from MECH-284 the accumulator: MECH-287 is the discrete trigger event (anchor-side mismatch + broadcast release) that drives accumulation. Triggered by latent-stream prediction failure (norm(z_pred(t-1) - z_obs(t)) > theta_trigger) OR strongly negative value-PE (value_PE(t) < -theta_value) OR exogenous surprise. Carries no schema-region credit on its own; downstream MECH-284 attributes the broadcast event to the active anchor set. Failure modes dissociate from MECH-284: trigger-loss produces single-event blindness (no anchor ever feels invalidated, but staleness map remains queryable); accumulator-loss produces V3-EXQ-475's phenotype directly (releases happen but never integrate). Failure modes also dissociate from MECH-285 sleep priority: trigger-loss prevents staleness from accruing, while sleep-priority loss prevents accrued staleness from being repaired. Within MECH-287 itself, anchor-side comparator failure dissociates from broadcast failure: comparator-loss = no upstream signal to broadcast (silent trigger, biologically a CA1/CA3 lesion); broadcast-loss = comparator fires but no global gain modulation downstream (silent invalidation, biologically an LC lesion)."
  claim_type: mechanism_hypothesis
  subject: regulators.broadcast_invalidation_trigger
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-14
    needs_review: false
    evidence:
      from: "decision:MECH-287@2026-07-14T20:19:21.313158Z"
      as_of: 2026-07-14
      verdict: promote_to_provisional/applied
  implementation_phase: v3
  v3_pending: false
  claim_level: mechanistic
  registered_utc: 2026-04-22
  depends_on:
    - MECH-284  # downstream consumer (the accumulator this trigger drives)
    - MECH-269  # downstream consumer (the anchor-reset that staleness ultimately drives)
    - MECH-094  # interaction (probe-tagged content should NOT route through this trigger as if veridical)
    - MECH-272  # state-gated routing (trigger fires waking; sleep-phase has the offline arm)
    - SD-005    # z_self/z_world split (per-stream prediction errors live here)
    - ARC-018   # viability map (z_pred for value_PE comes from here)
  functional_restatement: >
    Mechanism hypothesis. V3-EXQ-475 (re-run of EXQ-471 with SD-036
    GABAergic decay + MECH-279 PAG freeze gate enabled) showed 5-6
    PAG releases per seed across eval but ~12x re-commits per release
    and 1000/1000 freeze active steps. SD-036 decay and MECH-279 exit
    fire correctly; the reason re-commits dominate is that nothing
    invalidates the upstream avoid-anchor during waking. The hippocampal
    proposer keeps drawing trajectories from the original anchor.

    MECH-287 commits to: a fast, broadcast 'anchor may be wrong' event
    fires when latent-stream prediction falls below tolerance OR when
    realised value drops sharply below predicted value. The event itself
    is region-agnostic (broadcast, not credit-assigned). It is consumed
    by MECH-284 the accumulator, which performs region-level credit
    assignment via attribution_weight over the active anchor set.

    Concretely:
      trigger(t) = (norm(z_pred(t-1) - z_obs(t)) > theta_trigger)
                   OR (value_PE(t) < -theta_value)
                   OR (external_surprise_signal(t) > theta_surprise)

    Biological substrate (DUAL-COMPONENT coupled-stage architecture per
    targeted_review_v_s_foundation/SYNTHESIS verdict 5; not a single
    broadcast event but two coupled stages of one trigger event):

    UPSTREAM anchor-side comparator component:
      CA1/CA3 mismatch comparator (Vinogradova 2001): hippocampus
        computes match/mismatch between two functionally distinct input
        streams; the resulting signal gates both intra-hippocampal
        output AND subcortical arousal modulation. This is the
        anchor-side detection event that the broadcast amplifies.
      Subicular routing pathway (O'Mara 2009): subiculum is the
        anatomical hub through which hippocampal mismatch reaches
        NAc-VP-VTA. The trigger does not bypass this pathway -- it
        flows through it.
      HC-VTA loop wiring (Lisman & Grace 2005): hippocampal mismatch
        propagates via subiculum-NAc-VP to VTA, which then back-projects
        dopamine to enhance hippocampal LTP. This loop architecture is
        what couples the upstream comparator to the downstream broadcast.
      Event-boundary-driven reinstatement (Sols/DuBrow/Davachi 2017):
        event boundaries behave like local mismatch signals at the
        schema/event-segment scale and drive consolidation priority --
        consistent with the comparator firing on schema-region
        boundaries, not arbitrary single-step latent deltas.

    DOWNSTREAM broadcast component (parallel, partially redundant):
      LC phasic burst (best-fit per Aston-Jones-Cohen 2005 / Sara-Bouret
        2012; surprise-shaped, broadcast, drives gain modulation that
        is the natural multiplier on attribution_weight)
      DA generalised PE (Schultz 1997 / Gardner-Schoenbaum-Gershman
        2018; covers the value-PE arm; partially redundant with LC for
        the latent-PE arm)
      LHb negative-RPE (Matsumoto-Hikosaka 2007; specifically for the
        value_PE < -theta_value arm)

    Component coupling: the upstream comparator is the SOURCE of the
    trigger event; the downstream broadcast is the AMPLIFICATION /
    propagation of that event globally. They are coupled stages of
    the same trigger, not alternative trigger sources. Failure modes
    dissociate (see title): comparator-loss = silent trigger (CA1/CA3
    lesion analog); broadcast-loss = silent invalidation (LC lesion
    analog); both intact = full trigger event capable of driving
    MECH-284 accumulation and MECH-269 anchor reset.

    Why a separate claim from MECH-284. The lit-pull (SYNTHESIS at
    evidence/literature/targeted_review_waking_v_s_invalidation/) shows
    these failure modes dissociate: LC dysfunction produces
    perseveration / inability to shift (single events register but
    nothing broadcasts the shift); OFC dysfunction produces inability
    to update inferred state (broadcast registers but no region-level
    accumulation). EXQ-475's phenotype is accumulator-loss specifically,
    not trigger-loss -- single events register (the 5-6 releases prove
    this) but never integrate to a durable anchor invalidation. Keeping
    trigger and accumulator separate is required to make this empirical
    dissociation auditable.

    Falsifiable (primary, EXQ-475 unlock): re-running EXQ-471 conditions
    with SD-036 + MECH-279 + MECH-287 + MECH-284-online-readout +
    MECH-269-anchor-reset all enabled should produce mode-flip from
    avoid back to a goal-seeking mode within bounded re-commit count
    after ~5 PAG releases, NOT 1000/1000 freeze.

    Falsifiable (secondary, trigger-vs-accumulator dissociation):
    four-arm factorial of {MECH-287, MECH-284-online-readout} crossed
    with {present, lesioned} should produce the predicted dissociation
    (trigger-loss -> single-event blindness; accumulator-loss ->
    V3-EXQ-475 phenotype; both -> rigid perseveration; both intact ->
    EXQ-471 catatonic-lock unlocked).

    Falsifiable (tertiary, single-event capability vs accumulation):
    a single sufficiently large trigger event (theta_trigger crossed
    by a wide margin) should be capable of producing immediate
    anchor-reset via MECH-269 even before MECH-284 accumulation
    crosses its threshold. This matches biological orienting responses
    to single salient violations and is dissociable from the
    accumulator's slower integrative role.
  notes: >
    Discussion origin: 2026-04-22 SYNTHESIS of
    targeted_review_waking_v_s_invalidation/ literature pull (Wave 2
    after V3-EXQ-475 results landed). The pull was commissioned
    specifically because EXQ-475's pattern (releases happen, re-commits
    dominate) was diagnosed as "online anchor invalidation signal
    missing". The synthesis verdict was: register a new MECH for the
    trigger event distinct from MECH-284 accumulator (failure modes
    dissociate empirically; conflating them blocks the
    trigger-vs-accumulator factorial that V3-EXQ-476 needs to run).

    Dual-component refinement (2026-04-22 Phase 2): the
    targeted_review_v_s_foundation/ literature pull (12 papers across
    place-cell remapping, attractor dynamics, splitter cells, schema
    cells / event boundaries, multi-trace / pattern separation,
    comparator / HC-VTA loop, subicular routing) added verdict 5: the
    broadcast trigger is NOT a standalone event but the downstream stage
    of a two-stage architecture. The upstream stage is an anchor-side
    CA1/CA3 mismatch comparator (Vinogradova 2001) routed through
    subiculum (O'Mara 2009) into the HC-VTA loop (Lisman & Grace 2005),
    which generates the broadcast event. Treating these as alternatives
    rather than coupled stages would miss the loop architecture biology
    actually uses and would block the comparator-vs-broadcast
    dissociation auditable inside MECH-287 (CA1/CA3 lesion vs LC lesion
    analogs).

    Design doc: docs/architecture/v_s_invalidation_runtime.md (covers
    MECH-287 + refined MECH-284 operational definition + refined
    MECH-269 anchor-reset criteria with dual-trace preservation per
    Bouton 2004).

    Substrate hooks (planned): ree_core/regulators/invalidation_trigger.py
    (new) implementing the trigger formula above; emits a per-tick
    event consumed by ree_core/hippocampal/staleness_accumulator.py
    (new) which implements MECH-284's region-indexed integration with
    attribution_weight credit assignment over the active anchor set.

    Coupling to MECH-285 sleep priority is underdetermined by the
    lit-pull (only Gershman 2017 speaks to it directly). The architectural
    default in the design doc is "waking trigger increments staleness;
    sleep priority reads accumulated staleness" -- partially-coupled,
    not independent. Q4 of the lit-pull spec (coupling to MECH-285)
    remains open for future evidence.

    The MECH-094 interaction is load-bearing: probe-tagged content
    (MECH-094 strengthened) must NOT increment MECH-284 staleness as
    if it were veridical anchor-following. The trigger fires on
    realised observation only, never on simulated/probe content.
  evidence_quality_note: >
    Registered candidate with v3_pending:true. Literature support is
    moderate (lit_confidence inherited from MECH-284 0.855 since
    the same SYNTHESIS spans both); no in-silico evidence yet.
    Promotion requires substrate implementation (invalidation_trigger.py
    + staleness_accumulator.py) plus the V3-EXQ-476 unlock test plus
    the four-arm trigger-vs-accumulator dissociation factorial.

    [2026-05-08 governance]: EXQ-476c diagnostic inconclusive (same reading as MECH-269 sibling).

    [2026-07-14 governance -- promote candidate->provisional, v3_pending
    cleared]: V3-EXQ-757 (v3_exq_757_mech288_mech287_event_boundary_trigger_functional_20260714T200049Z_v3)
    is the FIRST indexed in-silico evidence for MECH-287. PASS / evidence_direction
    supports, evidence_class exp:simulation. Wall-independent functional-signature
    isolation test (action-free, training-free) driving the invalidation-trigger
    substrate with controlled latent streams over 5 seeds; non_degenerate, all
    three MECH-287 load-bearing criteria met -- C287_phasic (broadcast fires on the
    A-condition trigger), C287_dissoc (no dissociated broadcast on the B condition),
    C287_tonic (storm suppression: 373 suppressed vs 34 broadcast). This clears the
    "no in-silico evidence" precondition behind the v3_pending hold. NOT-YET-CLEARED
    for promotion-to-active: this isolation test does not exercise the integrated
    closed-loop agent, so the pre-registered promotion bar above -- the V3-EXQ-476
    unlock test AND the four-arm trigger-vs-accumulator dissociation factorial --
    remains the separate promote-to-active gate.
  implementation_note: >
    Upstream broadcast trigger (invalidation_trigger.py) was implemented
    in Phase 2 (2026-04-23). Downstream accumulator consumer
    (staleness_accumulator.py, MECH-284) implemented Phase 3 2026-04-24;
    this completes the trigger -> accumulator -> anchor-reset chain the
    MECH-287 falsifiables require. MECH-287 broadcast events are now
    drained by HippocampalModule.tick_anchor_set into
    StalenessAccumulator.integrate(...) with attribution_weight credit
    over the active anchor set, then consumed by MECH-269 AnchorSet
    .tick_hysteresis via the MECH-284 lookup when
    use_mech284_hysteresis=True. The four-arm factorial between
    {MECH-287 present/lesioned} x {MECH-284-online-readout present/
    lesioned} is now substrate-executable via independent config flags
    (use_invalidation_trigger, use_staleness_accumulator,
    use_mech284_hysteresis). V3-EXQ-478 queued as the initial Phase 3
    diagnostic; the full four-arm dissociation test (V3-EXQ-476) remains
    outstanding for governance promotion.
  pharmacological_predictions:
    - PHARM-005  # prazosin (alpha-1 antagonist) directly damps the LC-analog substrate -> blunted trigger -> increased perseveration
  location: docs/architecture/v_s_invalidation_runtime.md

- id: MECH-288
  title: "Event-segment detection: a substrate-side two-level hierarchical detector that emits monotonic outer.inner segment IDs on detected event boundaries, feeding region-keyed substrates (anchor sets per MECH-269, V_s readouts per Phase 1, staleness accumulators per MECH-284) and the MECH-287 broadcast trigger at the schema/event-segment scale per Eichenbaum 2017 / Sols-DuBrow-Davachi 2017 / Baldassano 2017. Default substrate: PE-threshold detector on the fast scale (z_world+z_self) and BOCPD-Gaussian latent change-point on the slow scale (z_goal); MECH-287 broadcast fires downstream of MECH-288 boundaries (Clewett 2025) rather than via an independent comparator. The segment_id field is the region key consumed by the V_s invalidation runtime cluster; without it, MECH-269 anchor sets and per-region V_s aggregation have no substrate-native source for the region partition."
  claim_type: mechanism_hypothesis
  subject: hippocampal.event_segmenter
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-14
    needs_review: false
    evidence:
      from: "decision:MECH-288@2026-07-14T20:19:21.367028Z"
      as_of: 2026-07-14
      verdict: promote_to_provisional/applied
  implementation_phase: v3
  v3_pending: false
  claim_level: mechanistic
  registered_utc: 2026-04-22
  depends_on:
    - MECH-269  # downstream consumer: anchor set keyed on segment_id (per scale)
    - MECH-284  # downstream consumer: staleness accumulator indexed on segment_id
    - MECH-287  # downstream broadcast: MECH-287 trigger reads MECH-288 BoundaryEvents (Clewett 2025; verdict 3 = option c)
    - ARC-070   # downstream consumer (bidirectional, rollout-side): ARC-070 lit-pull R2 verdict 2026-05-10 -- ARC-070 is the imagination-side consumer of MECH-288 boundary pulses on the rollout proposal stream, not a parallel module (shared substrate, two input streams)
  functional_restatement: >
    Mechanism hypothesis (verdicts integrated 2026-04-22 from
    targeted_review_event_segmentation/ SYNTHESIS, agg lit_conf 0.85;
    decisive paper Clewett 2025 Neuron for the coupling result).

    The V_s invalidation runtime cluster (MECH-269 anchor sets,
    MECH-284 staleness accumulator, MECH-287 dual-component trigger)
    all key on a region partition that the substrate must produce.
    The targeted_review_v_s_foundation/ SYNTHESIS verdict 1
    established that the V_s region unit is schema/event-segment
    scale (NOT place-cell-field) per Eichenbaum 2017 mixed-selectivity
    coding and Sols-DuBrow-Davachi 2017 event-boundary-driven
    reinstatement. MECH-288 commits to a substrate-side TWO-LEVEL
    HIERARCHICAL detector that emits these segment IDs in nested
    outer.inner form.

    -- Per-tick mechanism --

    For each tick t, the segmenter runs both scales in parallel:
      for each scale in {fast, slow}:
        boundary(scale, t) = boundary_criterion[scale](
                                latent_dict, pe_dict, t)
        if boundary(scale, t):
          if scale == "slow":
            outer += 1
            inner = 0
          else:
            inner += 1
          emit BoundaryEvent(
            segment_id_old, segment_id_new = "{outer}.{inner}",
            scale, posterior, sources, t)

    Consumers read current_segment_id() -> "outer.inner".

    -- Verdict 1 (trigger criteria) --

    PE-spike is the primary trigger (Zacks 2007 EST canonical, conf
    0.88; Heilbron 2018 biological tractability, conf 0.65). Latent
    change-point (BOCPD-style) is the secondary trigger reserved for
    schema-scale boundaries that are weakly visible in instantaneous
    PE (Baldassano 2017 HMM-recovered slow-scale events, conf 0.90).
    Task markers (DuBrow 2014/2016, conf 0.78/0.75) are optional and
    exposed via force_boundary(reason) API hook -- NOT wired to
    default trigger logic.

    -- Verdict 2 (hierarchy) --

    Two scales by default (defer 3+ to Phase 3 per Baldassano 2017
    nested-timescale evidence). Fast scale at place-cell-field /
    fine-perceptual granularity (~1-5 step). Slow scale at
    event-segment / schema granularity (~20-100 step). Segment IDs
    encoded as outer.inner strings. Outer increments only on slow
    fire; inner resets on outer fire and on its own fast fires.
    Same boundary-detection algorithm at both levels with different
    tau and min_segment_length parameters -- do NOT instantiate
    separate detector classes. The API accepts a list of scale-configs
    so adding a third level later is config-only.

    -- Verdict 3 (MECH-287 coupling) -- DECISIVE --

    MECH-287 broadcast is DOWNSTREAM of MECH-288 boundary detection
    (option c in the original spec). Clewett 2025 Neuron (conf 0.92,
    fMRI + neuromelanin + pupillometry) demonstrates that event
    boundaries trigger pupil-linked LC activation which predicts
    hippocampal pattern separation. MECH-287 does NOT implement an
    independent comparator. Its trigger reads MECH-288 BoundaryEvents;
    broadcast strength = scaled function of MECH-288 posterior
    (graded, not binary). NOTE: this implies that MECH-287's
    "upstream comparator stage" framing (per V_s foundation pull
    verdict 5) is most parsimoniously realised BY MECH-288, not as
    code internal to MECH-287 -- architectural reconciliation
    flagged in MECH-287's own status log; the entries are not
    contradictory but the implementation should not duplicate the
    detector.

    Phasic/tonic guardrail (Clewett 2025 failure signature 2): if
    MECH-287's tonic-noise level exceeds threshold, the substrate
    suppresses the next phasic broadcast. This preserves Aston-Jones
    & Cohen 2005 phasic/tonic dissociation already captured in
    targeted_review_waking_v_s_invalidation/.

    -- Verdict 4 (algorithm) --

    Default canonical config:
      EventSegmenter(
        scales=[
          Scale(name="fast", streams=["z_world","z_self"],
                algorithm="pe_threshold", tau=2,
                min_segment_length=2, pe_threshold=0.65),
          Scale(name="slow", streams=["z_goal"],
                algorithm="bocpd_gaussian", hazard=1/40,
                posterior_threshold=0.5,
                min_segment_length=15),
        ],
        emit_to=["mech_287_broadcast", "mech_269_anchor_set"],
        scale_id_format="{outer}.{inner}",
      )
    BOCPD (Adams & MacKay 2007, conf 0.80) implemented with
    Gaussian per-segment likelihood, hazard = 1 / expected segment
    length, run-length posterior pruned to top-k=20 (O(1) per step
    in practice). PE-threshold uses normalised PE z-scored over a
    sliding window of length 200. Both algorithms emit posterior in
    [0, 1] usable directly as graded broadcast strength. HSMM and
    full-HMM deferred to Phase 3 (HSMM subsumed by BOCPD's hazard;
    full-HMM is offline-EM in Baldassano's pipeline -- not online-
    tractable).

    -- Verdict 5 (place-cell-field as parametric refinement) --

    CONFIRMED. Place-cell-field-scale and event-segment-scale
    boundaries are parametric variants of one substrate, NOT
    separate substrates (Eichenbaum 2017; Brunec 2018 anteroposterior
    HC gradient, conf 0.74; Baldassano 2017 same HMM recovers all
    scales). Scales differ only in (tau, min_segment_length,
    algorithm_choice) within Scale(...) configs. MECH-269 anchor-set
    keys are scale-tagged (anchor[scale=fast][segment_id="3.7"]) so
    the same anchor mechanism handles both granularities without
    code duplication. Per-stream tau values from V_s foundation
    verdict 1 flow naturally into per-Scale tau.

    -- Cross-scale interaction --

    When slow-scale boundary fires, fast-scale boundary also fires
    (slow boundary forces an inner reset). Parsimonious default;
    revisit if EXQ evidence contradicts.

    -- Falsifiable (primary) --

    An agent with MECH-288 enabled should produce a boundary at
    task-natural event transitions (arrival at a goal cell, harm
    onset, mode switch) at higher rate than chance, with bounded
    false-positive rate over a noise baseline. Hierarchical metric:
    slow boundaries should align with high-level task transitions
    (episode phase change, goal switch); fast boundaries should
    align with within-segment perceptual transitions (place-field
    crossing, action subgoal completion).

    -- Falsifiable (secondary, downstream usefulness) --

    Per-region V_s readouts keyed on MECH-288 segment_ids should
    produce more informative anchor-set partitions than uniform
    place-binning, measured by anchor-reset latency on V3-EXQ-471
    conditions (re-running EXQ-475 with MECH-288 + MECH-269 anchor
    sets enabled).

    -- Falsifiable (tertiary, MECH-287 coupling test) --

    Per verdict 3 (option c): lesioning MECH-288 should silently
    lesion MECH-287's broadcast trigger (broadcast never fires from
    boundary signals; tonic-arousal modulation still operates
    independently). The dissociation distinguishes the verdict-3
    architecture from the alternative options: option (a) would
    predict MECH-287 broadcast continues firing normally on its own
    independent threshold, which we now reject.
  notes: >
    Discussion origin: 2026-04-22 Phase 2 substrate planning for V_s
    invalidation runtime cluster. The Phase 2 (ii) anchor-set tracking
    work needs region IDs from somewhere; ree-v3 has no event-boundary
    detector at the substrate level (only episode-boundary resets and
    self/other TPJ comparator boundaries). MECH-288 fills this gap as
    a prerequisite to Phase 2 (ii)+(iii) of the substrate cluster, and
    -- per verdict 3 -- as a prerequisite to Phase 2 (iv) MECH-287
    trigger as well (since MECH-287 trigger now reads MECH-288
    BoundaryEvents rather than computing its own).

    Lit-pull complete: targeted_review_event_segmentation/ delivered
    11 papers + SYNTHESIS.md (2026-04-22). Aggregate lit_confidence
    0.85 across all five verdicts. Decisive paper Clewett 2025 Neuron
    for verdict 3 (multi-modal LC-boundary-pattern-separation
    coupling). Most generative single paper Baldassano 2017 (covers
    verdicts 2, 4, 5 simultaneously). Skipped Sols/DuBrow/Davachi
    2017 + Eichenbaum 2017 (already in targeted_review_v_s_foundation/).
    Original spec at docs/session_prompts/event_segmentation_litpull.md.

    Design doc: docs/architecture/event_segmenter.md (verdict
    integration section now populated; pseudocode API + canonical
    default config inlined from SYNTHESIS).

    Substrate hooks (planned, blocks-on: nothing further architecturally;
    ready for implementation): ree_core/hippocampal/event_segmenter.py
    (new) emitting BoundaryEvent objects on a queue consumed by
    HippocampalModule (anchor set keying, scale-tagged), staleness
    accumulator (region indexing), AND MECH-287 ComparatorStage
    (which collapses per verdict 3 to "subscribe to MECH-288
    BoundaryEvents and trigger broadcast at the moment of fire").

    Architectural reconciliation with MECH-287 (flagged for Phase 2
    sequencing decision, NOT this entry's revision): MECH-287's
    "upstream anchor-side comparator stage" (registered per V_s
    foundation pull verdict 5 as Vinogradova 2001 CA1/CA3 mismatch
    + O'Mara 2009 subicular routing + Lisman & Grace 2005 HC-VTA
    loop) is best read as the BIOLOGICAL substrate that MECH-288
    instantiates computationally. The MECH-287 entry's biological-
    substrate text remains accurate; its IMPLEMENTATION-side
    "ComparatorStage" sub-component should not duplicate the
    detector but rather subscribe to MECH-288 BoundaryEvents.
    Whether to refactor MECH-287's claim text to make this explicit
    is a downstream governance decision (currently both entries
    point at the same biological substrate from different angles
    -- not contradictory but the duplication is worth tracking).

    Stream-mixture default for stream-conditioned anchors (per
    Phase 2 plan): anchors are written with stream_mixture =
    current_active_streams_at_creation_time as a simple stand-in.
    Whether this should be replaced by a learned attribution head
    is flagged as an open question for a future lit-pull / claim
    cluster once Phase 2 (ii)+(iii) substrate is in place. NOT to
    be confused with the segment_id criterion, which is what THIS
    claim addresses.

    Phase 2 sequencing implication of verdict 3: MECH-288 substrate
    code (event_segmenter.py) must land BEFORE MECH-287's Phase 2
    (iv) trigger code, because the trigger will read MECH-288
    BoundaryEvents. This reverses earlier "MECH-287 (iv) and MECH-288
    can land in parallel" sequencing.

    [2026-05-10 ARC-070 bidirectional-consumer commitment]:
    The ARC-070 lit-pull
    (evidence/literature/targeted_review_arc_070_decomposition/)
    R2 verdict (LOAD-BEARING, conf 0.74) commits ARC-070 to be
    implemented as a bidirectional CONSUMER of MECH-288, not a
    parallel imagination-side module. MECH-288's existing API is
    already substrate-agnostic about whether the latent + PE
    streams come from observation or from simulation; the
    bidirectional commitment makes this explicit. MECH-288 thus
    has two classes of downstream consumer:

      OBSERVATION-side consumers (latent stream sourced from
      ongoing percept / experience):
        - MECH-269 anchor sets (region partition)
        - MECH-284 staleness accumulator
        - MECH-287 broadcast trigger (Clewett 2025)

      ROLLOUT / IMAGINATION-side consumer (latent stream sourced
      from hippocampal forward sweep / E2 rollout):
        - ARC-070 child MECH (policy-side decomposition pulse
          consumer; re-segments the rollout proposal stream at
          finer grain when boundary fires on chunk's region)

    Implementation implication for event_segmenter.py: the
    substrate should accept an explicit input_stream label
    (observation | rollout) so that MECH-094 hypothesis_tag-
    conditional downstream effects can be enforced per consumer.
    Pre-commitment / simulation-side fires (input_stream=rollout)
    must NOT update MECH-269 anchor sets or MECH-287 broadcast
    (those are observation-only). Post-commitment / execution-side
    fires (input_stream=observation) drive the existing
    observation-side consumer chain unchanged.

    Anchored on Schacter / Addis / Buckner 2008 (constructive
    episodic simulation core network: same medial PFC + hippocampus
    + retrosplenial + parietal substrate processes observation and
    imagination) and Zacks 2007 (event-segmentation theory is
    substrate-agnostic about input stream source). See ARC-070
    evidence_quality_note for the R2 architectural reasoning.

    Falsifiable consequence: lesioning MECH-288 should impair
    BOTH observation-side anchor partition AND imagination-side
    decomposition. A dissociation falsifies the bidirectional-
    substrate commitment and forces a parallel-but-coupled
    architecture.
  evidence_quality_note: >
    Registered candidate with v3_pending:true. Literature support
    complete: targeted_review_event_segmentation/ 11 papers /
    aggregate lit_conf 0.85, with Clewett 2025 (0.92) decisive for
    verdict 3 and Baldassano 2017 (0.90) load-bearing for verdicts
    2/4/5. Foundation pull (targeted_review_v_s_foundation/) provides
    verdict 1 substrate-granularity grounding. Promotion requires
    substrate implementation (event_segmenter.py per canonical
    default config) plus integration into Phase 2 (ii)+(iii)+(iv)
    downstream substrate plus falsifiable predictions above.

    [2026-05-08 governance]: EXQ-476c diagnostic inconclusive (same reading as MECH-269 sibling).

    [2026-05-10 bidirectional-consumer extension]: ARC-070 lit-pull
    R2 verdict adds ARC-070 as a rollout / imagination-side
    consumer of MECH-288 boundary pulses. See notes section above
    and evidence/literature/targeted_review_arc_070_decomposition/
    synthesis.md. Implementation of event_segmenter.py should
    accept input_stream label so observation-side and rollout-side
    consumers can be dispatched correctly under MECH-094
    hypothesis_tag gating.

    [2026-07-14 governance -- promote candidate->provisional, v3_pending
    cleared]: V3-EXQ-757 (v3_exq_757_mech288_mech287_event_boundary_trigger_functional_20260714T200049Z_v3)
    is the FIRST indexed in-silico evidence for MECH-288. PASS / evidence_direction
    supports, evidence_class exp:simulation. Wall-independent functional-signature
    isolation test (action-free, training-free) over 5 seeds; non_degenerate, all
    three MECH-288 load-bearing criteria met -- C288_hit (slow BOCPD posterior fires
    on the A-condition boundary, mean slow_hit_rate 1.0, 27 A-slow vs 0 B-slow
    detections), C288_silence (no boundary on the stationary B stream), C288_graded
    (continuous boundary posteriors on the fast scale). This clears the "no in-silico
    evidence" precondition behind the v3_pending hold. SPECIFICITY CAVEAT (load-bearing
    for how strong this evidence is): MECH-288 specificity is demonstrated on the
    SLOW / BOCPD OUTER scale only -- the canonical slow detector is a DECISIVE-change
    detector (posterior->1.0 only on ||z_goal|| shifts >=~8). The FAST PE-threshold
    INNER scale fires ~15% on ANY stationary z-score stream by construction (reported
    descriptively, NOT gated), so the fast scale is a gradedness readout, not a
    specificity claim. NOT-YET-CLEARED for promotion-to-active: this isolation test
    does not exercise the integrated downstream substrate, so the pre-registered
    promotion bar above -- integration into Phase 2 (ii)+(iii)+(iv) downstream
    substrate plus the falsifiable predictions -- remains the separate
    promote-to-active gate.

    [2026-07-24 substrate -- input_stream extension IMPLEMENTED]: the
    bidirectional-substrate commitment recorded above (2026-05-10) is now
    substrate-implemented in event_segmenter.py. step() / force_boundary() /
    the new boundary_on() query entrypoint all take input_stream
    ("observation" | "rollout", default "observation"); detector instances
    and outer/inner/last-fire counters are fully isolated per stream, so
    MECH-094 (a rollout tick must not advance the observation segment_id)
    is enforced structurally rather than by caller convention. This is the
    substrate prerequisite MECH-321/ARC-070 named as blocking (see MECH-321
    depends_on and its functional_restatement's MECH-288.boundary_on(...)
    call). Default input_stream="observation" is bit-identical to the
    pre-extension single-stream call shape; the only existing caller
    (agent.py sense()) is unaffected. Does NOT itself promote this claim --
    the promote-to-active gate above (Phase 2 ii/iii/iv integration +
    falsifiable predictions) is unchanged and still open. See
    ree-v3/CLAUDE.md MECH-288 entry for the landing commit. This does not
    touch MECH-321's own v3_pending (still true -- its other dependencies,
    ARC-070 itself and live MECH-094-gated usage, remain unbuilt).
  location: docs/architecture/event_segmenter.md

- id: MECH-289
  title: "The hippocampal SWR trajectory generator has anti-recency weighting: infrequently experienced trajectories are proposed more often than recently committed ones, and novel-path sequences (never behaviorally executed) are actively generated from available path segments."
  claim_type: mechanism_hypothesis
  subject: hippocampal.cem_anti_recency_bias
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-04-24
  depends_on:
    - MECH-092   # MECH-289 specifies the sampling property of MECH-092's generative replay
  functional_restatement: >
    The biological hippocampal trajectory generator (SWR replay) does not preferentially
    represent recent experience. Gupta et al. 2010 (Neuron 65:695) shows: (a) infrequently
    experienced paths replay MORE often than frequent ones; (b) novel-path sequences
    combining segments in arrangements the animal never executed are generated; (c)
    the replay distribution represents all physically available trajectories, not a
    recency-weighted subset. In REE terms: CEM trajectory sampling during both waking
    (online, theta-analog) and quiescent (offline, SWR-analog, MECH-092) modes must
    have an anti-recency property -- the proposal distribution cannot converge on
    recently committed routes. Without it, the system drifts toward monostrategy
    (familiar-route dominance) and fails to maintain comprehensive viability map
    coverage. SD-038 is the REE substrate decision that implements MECH-289.

    Biological grounding: Gupta et al. 2010 (targeted_review_connectome_arc_028/,
    conf=0.80) is the primary evidence. Muessig 2019 (conf=0.63) corroborates via
    developmental co-emergence of theta sequences and SWR replay as unified mechanism.

    Falsifiable: in a multi-route environment, agents with anti-recency CEM (SD-038
    ON) should maintain higher trajectory diversity over extended episodes than agents
    without (monostrategy onset rate lower). Primary ablation: {anti-recency OFF} x
    {anti-recency ON} x {2-route env} x {multi-seed}.
  notes: >
    Registered 2026-04-24 from lit-pull synthesis. The "standard experience replay"
    framing in early MECH-092 notes was an architectural assumption that Gupta 2010
    directly contradicts. MECH-092 functional_restatement updated 2026-04-24 to
    correct this. MECH-289 is the corrected mechanistic claim that replaces the
    implicit recency-weighted assumption.

    [2026-08-09 addendum, Stage 2 digestion of the 2026-08-08 "prospective navigation
    through goal topology" thought]: Yu et al. 2026 (Nat Neurosci, DOI 10.1038/
    s41593-026-02365-2) and Tang et al. 2026 (Nat Neurosci, DOI 10.1038/
    s41593-026-02364-3) report waking hippocampal theta sweeps (this claim's "online,
    theta-analog" mode) forming goal-directed trajectory vectors; Tang et al.
    specifically report these theta sequences are preferentially replayed during
    sharp-wave ripples (this claim's SWR/offline mode), i.e. the same generative
    substrate operating in both modes this claim already distinguishes. Corroborates
    the existence and goal-directedness of the generative sweep this claim asserts;
    does not speak to the anti-recency property specifically, which remains grounded
    by Gupta et al. 2010 above. Located via web search during digestion, not a formal
    /lit-pull. See evidence/planning/thought_intake_2026-08-08_prospective_navigation_
    through_goal_topology.md for the full reconciliation.

- id: MECH-290
  title: "At trajectory completion, HippocampalModule performs a backward temporal credit sweep that propagates the terminal outcome (goal contact or failure) retroactively through preceding states, consistent with the reverse replay observed at reward endpoints in awake rats (Foster & Wilson 2006)."
  claim_type: mechanism_hypothesis
  subject: hippocampal.backward_trajectory_credit_sweep
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-04-24
  depends_on:
    - ARC-028    # the completion event that triggers the backward sweep
    - MECH-092   # the offline replay substrate that runs the backward sweep
    - MECH-105   # dopamine signal concurrent with the backward sweep at goal endpoint
  functional_restatement: >
    Foster & Wilson 2006 (Nature 440:680) showed that at the reward endpoint,
    awake hippocampal place cells replay the just-completed trajectory in REVERSE
    temporal order, concurrent with peak dopamine signalling. This reverse replay
    structure is consistent with TD-style credit assignment: the terminal reward
    value is propagated backward through the trajectory to preceding states. In
    REE terms, this is a backward credit sweep of the committed trajectory at
    goal contact: for each step t in {T, T-1, ..., 0}, the viability map residue
    at step t is updated by discount^(T-t) * terminal_outcome. This provides
    retroactive credit assignment that forward-only CEM cannot provide -- it
    connects terminal outcomes to the upstream states that led to them.

    The sweep requires: (1) the committed trajectory is stored in a buffer during
    execution (sequence of z_world + action at each step); (2) at goal contact
    (ARC-028 completion event), the buffer is processed backward; (3) viability
    map residues are updated with decaying weights; (4) the dopamine-analog
    variable (MECH-105) provides the terminal outcome magnitude.

    Not to be confused with offline generative replay (MECH-092): the backward
    sweep is a specific post-completion evaluation of the JUST-EXECUTED trajectory,
    not a generative sampling of future candidates. Both may run at the same
    completion event, in sequence: backward sweep first (credit assignment),
    then generative replay (next-trajectory planning).

    Biological grounding: Foster & Wilson 2006 (targeted_review_connectome_arc_028/
    2026-04-24_arc_028_reverse_replay_foster2006, conf=0.72) is the primary
    evidence. Reverse replay at reward endpoints is a well-replicated phenomenon
    (Carr et al. 2011 review cites multiple replications).

    Falsifiable (primary): removing the backward sweep (sweep_enabled=False) should
    impair performance in environments where long-horizon credit assignment is
    required -- specifically, trajectories with uniform intermediate steps leading
    to a distant reward. With sweep: viability map correctly weights early steps
    toward the goal. Without sweep: only the final step receives full credit,
    producing a credit-assignment gradient failure.
  notes: >
    Registered 2026-04-24. Initially flagged as V4 concern in session; user
    correctly noted it should be V3. The implementation is tractable in V3
    (trajectory buffer + backward pass at completion event) and does not require
    SD-006 (async execution) -- the sweep runs at the same synchronous completion
    event that triggers BetaGate release. The reverse replay structure (backward
    temporal order) is not just a biological curiosity -- it is the signature of
    TD credit assignment running backward through the trajectory. Implementing the
    equivalent in REE closes a genuine architectural gap in current credit
    assignment quality.

- id: MECH-291
  title: "Waking-online and quiescent-offline CEM modes require distinct parameter profiles: waking mode uses short rollout horizon, context-anchored start states, and low exploration weighting; quiescent mode uses longer rollout horizon, free start states drawn from the full map, and high exploration weighting (anti-recency bias per MECH-289)."
  claim_type: mechanism_hypothesis
  subject: control_plane.online_offline_cem_mode_differentiation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-291@2026-04-26T16:17:25Z"
      as_of: 2026-04-26
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-04-24
  depends_on:
    - SD-006     # async multi-rate loop required to run quiescent mode at different cadence
    - MECH-092   # quiescent offline replay (the mode being parameterised)
    - MECH-289   # anti-recency bias: applies at higher weight in quiescent mode
    - ARC-028    # completion signal: the event that triggers mode transition waking->quiescent
  functional_restatement: >
    Muessig et al. 2019 (Curr Biol 29:834) shows that theta sequences (online waking
    trajectory generation) and SWR replay (offline quiescent generation) co-emerge
    developmentally as two modes of the same hippocampal sequence generator. This
    developmental coupling implies shared substrate but distinct operational parameters:
    waking theta sequences are short-horizon, forward-biased, and anchored to the
    current position; SWR replay sequences are longer-horizon, bi-directional, and
    sample from all available paths (Gupta 2010). In REE terms, the HippocampalModule
    runs the same CEM trajectory generator in two modes triggered by behavioral state.

    Waking mode (theta-analog, triggered by active navigation tick):
      - rollout_horizon: SHORT (current trajectory segment length, ~3-5 steps)
      - start_states: anchored to current z_world
      - exploration_weight: LOW (exploit current knowledge for immediate action)
      - direction_bias: FORWARD only

    Quiescent mode (SWR-analog, triggered by MECH-092 heartbeat):
      - rollout_horizon: LONG (full trajectory horizon, up to plan_horizon)
      - start_states: FREE (sampled from viability map, not current position)
      - exploration_weight: HIGH (anti-recency bonus per MECH-289 at max strength)
      - direction_bias: BOTH forward and backward (credit sweep per MECH-290)

    Implementing this requires SD-006 (async multi-rate loop): the quiescent mode
    runs on the heartbeat cadence, not the step cadence. Until SD-006 is implemented,
    both modes use the same parameters (the waking defaults), which means quiescent
    replay underperforms its biological potential.

    Falsifiable: after SD-006 implementation, a factorial experiment {waking-only
    CEM} x {quiescent-mode distinct-params} x {environment with multiple
    route-lengths} should show that distinct params produce better viability map
    coverage for long trajectories specifically, while short-trajectory performance
    is unchanged.
  notes: >
    Registered 2026-04-24. The developmental co-emergence finding (Muessig 2019) is
    the key evidence: theta sequences and SWR replay are not separate circuits but
    one mechanism in two modes. This has a direct implementation implication: the
    HippocampalModule should expose a mode parameter (waking / quiescent) that
    controls CEM hyperparameters, not hardcode them. The mode is set by the
    behavioral-state signal that ARC-028 and MECH-092 both feed into. Blocked on
    SD-006 for the quiescent path; waking-mode parameters can be refined
    independently of SD-006.

- id: SD-039
  title: "Dual-trace anchors preserve a goal-state snapshot plus wanting/arousal payload at write or invalidation time, so unresolved goals remain queryable after the active plan fails."
  claim_type: design_decision
  subject: hippocampal.anchor_goal_snapshot_payload
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_494_sd039_anchor_payload_validation_20260427T094326Z_v3
      as_of: 2026-04-27
      verdict: supports/PASS
  implementation_phase: v3
  v3_pending: true
  epistemic_category: substrate_conditional
  implementation_note: "FULLY IMPLEMENTED 2026-04-27 (substrate-side landed 2026-04-26; module-level write-site population landed 2026-04-27). Substrate (2026-04-26): ree_core/hippocampal/anchor_set.py adds AnchorGoalPayload dataclass (z_goal_snapshot, wanting_strength, arousal_tag, last_vs, staleness_at_write, payload_written_step), Anchor.goal_payload field + Anchor.goal_match() cosine helper, AnchorSet.query_by_goal_match() helper for MECH-292 / MECH-293 consumers. Refresh-on-invalidate: payload preserved across mark_inactive (the entire point of dual-trace preservation); reset_region writes payload onto BOTH outgoing inactive trace AND new active anchor. Population layer (2026-04-27): HippocampalModule.build_goal_payload(latent_state, goal_state, residue_field, bla_output, current_step, simulation_mode=False) constructs the payload once per agent.sense() tick from GoalState.z_goal (detached clone), ResidueField.evaluate_valence(z_world)[..., VALENCE_WANTING].mean(), bla_output.arousal_tag, mean(per_stream_vs.values()) as last_vs proxy, max(staleness_accumulator.snapshot().values()) as staleness_at_write, and agent._step_count. tick_anchor_set + apply_invalidation_broadcasts_to_regions accept goal_payload kwarg and forward to consume_boundary_events / mark_inactive. agent.sense() builds payload after update_per_stream_vs and threads it through both write/remap and broadcast-invalidate sites. MECH-094: build_goal_payload returns None under simulation_mode=True; sense() always passes simulation_mode=False (waking stream). Config: REEConfig.from_dims accepts use_sd039_anchor_payload, propagates to config.hippocampal.anchor_set.use_sd039_anchor_payload (default False; bit-identical OFF). Backward compat verified: 170/171 preflight + contracts PASS (single failure unrelated queue-housekeeping). Validation: V3-EXQ-494 6/6 PASS 2026-04-27 (UC1 module imports; UC2 master OFF no-op; UC3 population_fires 7/7 anchors with populated payloads max_goal_match=0.9999; UC4 dual-trace preservation 6 inactive + 1 active all carry payloads; UC5 falsifiable signature mean Phase A=0.0 vs Phase B=0.998 with 3/3 above 0.3; UC6 MECH-094 simulation gate -- replay path produces zero anchors with populated payload). DEFERRED CONSUMERS (out of SD-039 scope): MECH-292 ranked ghost-goal bank, MECH-293 waking ghost-goal probes, ARC-060 hybrid field+bank framing."
  what_would_answer: >
    NON-DEGENERACY PRECONDITION (partially unmet as of 2026-08-03, root cause
    identified, not hypothetical): use_sd039_anchor_payload defaults OFF and
    must be explicitly enabled; a genuine behavioral test additionally needs at
    least one deferred consumer (MECH-292 ghost-goal bank and/or MECH-293
    waking ghost-goal probes) wired to actually USE the payload for downstream
    decisions. Both are already satisfied today (MECH-292/293 are landed and
    wired). A THIRD, MEASURED precondition currently blocks the query/retrieval
    half specifically: the retrieval cue must carry goal-context identity
    across distinct goal-epochs. As of the 2026-08-03 V3-EXQ-889 dry run this
    does NOT hold -- z_goal is a single global EMA attractor whose cue-centered
    max pairwise cosine saturates near ceiling (0.99752 vs a pre-registered
    0.99 non-degeneracy bar) and monotonically worsens with re-exposure, so
    four deliberately-distinct goal-epoch cues collapse onto the same top-1
    anchor (n_distinct_top1_anchors=1.0). A valid test design must also run
    within a single continuous agent stream across the invalidation-to-
    retrieval window: AnchorSet.reset() clears the entire dual-trace pool at
    every agent.reset() episode boundary.

    CONFIRMING signature, by level:
    - Unit level (ALREADY MET, V3-EXQ-494 UC5, 2026-04-27): mean goal-match
      on Phase A (goal-inactive) anchors = 0.0 vs Phase B (goal-active)
      anchors = 0.998, 3/3 replicates above the pre-registered 0.3 threshold.
    - Consumer-ranking level (PARTIALLY MET, V3-EXQ-868, 2026-08-02): a
      goal-relevant inactive anchor outranks a goal-irrelevant, matched-
      staleness inactive anchor in >=4/7 seeds.
    - Retrieval/query level (STILL OPEN, blocked on the precondition above):
      once goal-identity is met, a post-failure retrieval cue correctly
      surfaces the anchor on the formerly-valid-but-now-blocked path as its
      top-1 hit at an above-chance rate, while unrelated stale anchors are
      not surfaced.
    - Full behavioral/task level (NEVER COMPLETED, V3-EXQ-495 SIGTERM'd
      2026-04-28, not since requeued): PLANNED-arm benefit_post_block exceeds
      HABIT-arm benefit_post_block by >=0.30 in >=4/7 seeds in the detour
      paradigm, without a harm regression or standard-episode degradation.

    FALSIFYING signature: with the goal-identity precondition met and a
    continuous-stream design, a downstream consumer built on this payload
    shows NO behavioral advantage over ignoring the payload entirely -- e.g.
    cued retrieval performs at or near chance even once cues are demonstrably
    goal-distinct; PLANNED-arm is statistically indistinguishable from
    HABIT/ABLATED. The payload would then exist and dissociate goal-active/
    inactive state at the unit level, without translating into any measurable
    recovery advantage.
  claim_level: architectural
  registered_utc: 2026-04-26
  depends_on:
    - MECH-216   # predictive wanting contributes to the stored motivational payload
    - MECH-230   # z_goal latent structure provides the goal snapshot
    - MECH-269   # dual-trace anchor substrate being extended
    - MECH-284   # region staleness read-out paired with the payload
  functional_restatement: >
    Current MECH-269 anchors preserve `z_world` and active/inactive status, while
    MECH-284 preserves a region-level staleness scalar. This is enough to say
    "this region is unresolved" but not enough to say "this unresolved region still
    corresponds to a live goal". SD-039 adds a compact motivational payload to each
    anchor at write, remap, or invalidation time: `z_goal_snapshot`, wanting
    strength, arousal tag, and optional `last_vs` / `staleness_at_write`. The
    payload is preserved across `mark_inactive`, so inactive anchors remain
    queryable as blocked or deferred goal traces rather than anonymous stale
    places.

    Falsifiable: after reward relocation or path blockage, inactive anchors on the
    formerly valid approach path should retain non-zero goal-match with the current
    `z_goal` while unrelated stale anchors do not.
  notes: >
    Registered 2026-04-26 from the ghost-goal lit-pull. The key design insight is
    that MECH-284 staleness alone cannot identify which stale regions are still
    motivationally relevant. The payload is the minimal substrate change that lets
    waking or offline search distinguish a still-wanted blocked path from generic
    unresolved terrain. First implementation should extend `Anchor` in
    `ree-v3/ree_core/hippocampal/anchor_set.py` and populate the payload from
    `GoalState`, `VALENCE_WANTING`, and amygdala arousal tags on anchor write/remap.
  evidence_quality_note: >
    Registered candidate with literature support from
    `targeted_review_ghost_goal_search/` (Berridge 1998, Barch 2010, Berkowitz
    1989) plus the existing MECH-269 / MECH-285 foundation pulls.

    [2026-05-19 retrieval-cue reframe] Under the cue-system reading
    (docs/architecture/ghost_goal_search.md Section 0; 2026-05-18 combined
    pull: Nakazawa 2002, Gelbard-Sagiv 2008, Smith & Vela 2001, Flagel
    2011, Corbit & Balleine 2005/2011, Jasinska 2014, Bouton 2004,
    Maren/Phan/Liberzon 2013, Bouton 2014) the SD-039 payload IS the
    stored retrieval trace and goal_match is a cue-to-trace match score.
    Constraint 1 (Smith & Vela 2001 encoding specificity / outshining):
    the already-preserved-but-match-unused fields (arousal_tag, last_vs,
    cause) are the substrate for a context-cue channel that must be gated
    to yield to a strong z_goal match, not summed with fixed weight.
    Revised falsifiable: the existing "inactive anchors on the formerly
    valid path retain goal-match" prediction stands, AND the stored
    context fields must be sufficient to support a fallback context match
    when the direct z_goal match is weak/absent. Interpretive reframe;
    status/phase/confidence unchanged; no new claim/experiment; no
    substrate change this pass.
  location: docs/architecture/ghost_goal_search.md

- id: MECH-292
  title: "Inactive or strained anchors whose preserved goal payload still matches the current goal form a ranked ghost-goal bank; priority scales with wanting, goal-match, staleness, and recoverability rather than with staleness alone."
  claim_type: mechanism_hypothesis
  subject: hippocampal.unresolved_goal_ghost_bank
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_496_mech292_ghost_goal_bank_validation_20260427T094339Z_v3
      as_of: 2026-04-27
      verdict: supports/PASS
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-04-26
  depends_on:
    - SD-039     # stored goal payload on inactive anchors
    - MECH-216   # predictive wanting signal
    - MECH-230   # structured z_goal latent
    - MECH-269   # dual-trace active/inactive anchor substrate
    - MECH-284   # region staleness
  functional_restatement: >
    A good agent should not treat all stale regions equally. Some stale regions are
    still tied to currently-live goals; others are merely unresolved background.
    MECH-292 defines a ranked unresolved-goal bank built from the existing dual-
    trace anchor substrate. Each candidate bank entry combines (a) stored goal
    snapshot, (b) wanting/arousal payload, (c) current region staleness, and
    (d) a recoverability estimate derived from present `V_s` and context. Priority
    is utility-like:

      ghost_priority ~ wanting x goal_match x staleness x recoverability

    This ranking is deliberately not pure low-`V_s` chasing, which would create
    rumination over hopeless traces rather than intelligent re-probing of blocked
    goals.

    Falsifiable: in a reward-relocation or blocked-corridor task, anchors from the
    now-obstructed but still-valued path should rank above equally stale but
    goal-irrelevant anchors.
  notes: >
    Registered 2026-04-26 from the ghost-goal lit-pull. Mattar & Daw 2018 give the
    normative utility-prioritisation argument; Gillespie 2021 provides the closest
    neural match for broad, non-current, not-recent trace prioritisation. Initial
    implementation can expose the bank as a derived view in HippocampalModule rather
    than a separate persistent store.
  evidence_quality_note: >
    Registered candidate with direct literature anchor from
    `targeted_review_ghost_goal_search/` and indirect support from
    `targeted_review_mech285_sleep_replay_seed/`.

    [2026-05-10 absorption note] MECH-315 candidate (proposal-diversity-
    channel via hippocampal trajectory sampling, Pfeiffer & Foster 2013)
    is absorbed into the MECH-292 ranked ghost-goal bank + MECH-293
    awake ghost-goal probes substrate per Pull 2 R5 verdict 2026-05-10
    (conf 0.74). MECH-315 is NOT registered as a separate top-level
    claim; the proposal-diversity function it would have provided to
    ARC-064 / ARC-065 is already substantively covered by this
    substrate.

    [2026-05-19 retrieval-cue reframe] (docs/architecture/
    ghost_goal_search.md Section 0; mech_292_ghost_goal_bank.md reframe
    section; 2026-05-18 combined cue-system pull). The bank is the
    cued-recall step of a content-addressed retrieval system: z_goal/
    context is the cue, the SD-039 payload the trace, rank() is recall,
    goal_match is the cue-to-trace match score (not a priority weight).
    Constraint 2 (Flagel 2011; Corbit & Balleine 2005/2011): wanting and
    goal_match are dissociable channels with independent failure modes --
    the additive ghost_priority already in the substrate
    (ghost_goal_bank.py: w_w*wanting + w_m*goal_match + ...) is the
    biologically correct form and is RATIFIED; the product form in the
    old ghost_goal_search.md sketch is superseded. Constraint 3 (Bouton
    2004/2014; Maren 2013; Jasinska 2014): recoverability must NOT become
    a cutoff that integrates suppression/staleness evidence toward
    excluding an anchor (accumulated hopelessness provably does not
    justify retirement; incubation can invert the staleness sign). The
    goal_match_floor rumination guard remains valid as a cue-relevance
    gate (no matching cue -> not recalled) but must NOT be repurposed as
    a hopelessness gate. Revised falsifiable signature: the three
    dissociated states (intact match/absent vigor; nonspecific vigor/
    absent match; match present but un-actionable) must be inducible, and
    a strong direct goal_match must not be displaced by the context
    channel. Interpretive reframe; status/phase/confidence unchanged; no
    new claim/experiment; no substrate change this pass; explicit
    abandon path is an open user-gated substrate question (Section 0.3).
  implementation_note: >
    SUBSTRATE IMPLEMENTED 2026-04-27 in ree-v3/ree_core/hippocampal/ghost_goal_bank.py
    (GhostGoalBank, GhostGoalBankConfig, GhostGoalBankEntry). Pure-arithmetic
    derived view over the SD-039 dual-trace anchor pool; no trainable parameters.
    Wired into HippocampalModule behind HippocampalConfig.use_mech292_ghost_bank
    (default False; bit-identical OFF). Public API: rank_ghost_goals(current_z_goal)
    -> List[GhostGoalBankEntry]. Ranking formula per design doc: ghost_priority =
    w_w*wanting + w_m*goal_match + w_s*staleness + w_r*recoverability with default
    weights (1.0, 1.0, 0.5, 0.5) and goal_match_floor=0.05 (the architectural
    rumination guard -- anchors with no payload OR cosine below floor are
    invisible). Default pool is inactive-only (include_inactive=True,
    include_active=False), top_k=32. Staleness preference: MECH-284 accumulator
    snapshot when wired, else local (tick - last_accessed) * staleness_proxy_rate
    fallback. Recoverability from Anchor.goal_payload.last_vs (clipped [0,1]) or
    default_recoverability_when_unknown=1.0. HippocampalModule.__init__ raises
    ValueError if use_mech292_ghost_bank=True without use_anchor_sets=True or
    without AnchorSetConfig.use_sd039_anchor_payload=True (otherwise every
    anchor scores goal_match=0.0 and the bank degenerates to empty). 164/164
    contracts + 7/7 preflight PASS with master OFF (bit-identical to pre-MECH-292
    HEAD). Validation: V3-EXQ-496 5/5 PASS Mac 2026-04-27 (UC1 module/config/
    method exposure; UC2 master OFF -> bank None + rank returns []; UC3 ranking
    fires with 6 inactive anchors all carrying populated payloads and
    monotone-decreasing priorities, max=1.609; UC4 Phase A goal-inactive anchors
    excluded by floor while Phase B admits 2 with goal_match dominant on top
    entry; UC5 component sums match ghost_priority within float epsilon).
    Behavioural validation (faster recovery from blocked-goal conditions vs
    random-probe baseline) lives in V3-EXQ-495 once MECH-293 wires
    propose_trajectories() to consume the bank. v3_pending kept TRUE: MECH-292's
    falsifiable signature is behavioural, not substrate-only; MECH-293 is the
    first behavioural consumer and is itself v3_pending.
  location: docs/architecture/mech_292_ghost_goal_bank.md

- id: MECH-293
  title: "Waking hippocampal search allocates a minority probe budget to top ghost-goal entries, seeding proposals around unresolved but still-wanted anchors instead of relying on pure current-anchor CEM or random probe noise."
  claim_type: mechanism_hypothesis
  subject: hippocampal.awake_ghost_goal_probe_search
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_497_mech293_ghost_probes_validation_20260427T094353Z_v3
      as_of: 2026-04-27
      verdict: supports/PASS
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-04-26
  depends_on:
    - ARC-018    # waking trajectory proposal / evaluation loop being modified
    - ARC-032    # goal-biased sequence generation
    - MECH-089   # theta-packaged waking E3 updates
    - MECH-269   # anchor/probe substrate
    - MECH-291   # mode-sensitive sequence generator framing
    - MECH-292   # ranked ghost-goal bank
  functional_restatement: >
    The current waking proposer is centred on the current terrain prior. MECH-293
    adds a second awake proposal branch: a minority budget of candidate trajectories
    seeded from the highest-priority ghost-goal entries. These proposals remain
    hypothesis-tagged until realized validation, but they are no longer random with
    respect to motivation. Between full E3 ticks, the same mechanism may run a
    tiny ghost-only micro-refresh when ghost pressure is high, preserving commitment
    stability while keeping unresolved goals computationally live.

    Falsifiable: versus a random-probe control, ghost-goal probes should show
    higher goal-match, faster recovery from blocked-goal conditions, and lower
    revisitation of fully incoherent stale regions.
  notes: >
    Registered 2026-04-26. Pfeiffer & Foster 2013 support goal-directed future
    path sweeps; Muessig 2019 supports one sequence generator in waking and offline
    modes; Gillespie 2021 argues against a narrow current-only seed pool. First
    implementation should extend `HippocampalModule.propose_trajectories()` with a
    ghost-seeded branch and keep MECH-094 guardrails unchanged: ghost probes do not
    update the viability map until realized.
  evidence_quality_note: >
    Registered candidate with literature support from
    `targeted_review_ghost_goal_search/`. The main remaining uncertainty is not
    whether ghost-biased probing is biologically plausible, but how large the waking
    probe fraction should be before recovery gains turn into rumination.

    [2026-05-10 absorption note] MECH-315 candidate (proposal-diversity-
    channel via hippocampal trajectory sampling, Pfeiffer & Foster 2013)
    is absorbed into the MECH-292 ranked ghost-goal bank + MECH-293
    awake ghost-goal probes substrate per Pull 2 R5 verdict 2026-05-10
    (conf 0.74). MECH-315 is NOT registered as a separate top-level
    claim; the proposal-diversity function it would have provided to
    ARC-064 / ARC-065 is already substantively covered by this
    substrate.

    [2026-05-19 retrieval-cue reframe] (docs/architecture/
    ghost_goal_search.md Section 0; mech_293_ghost_goal_probe_search.md
    reframe section). MECH-293 is the internally-triggered retrieval
    step: an internal goal/context cue reinstates a stored trace and
    re-pursues it, biologically licensed by Gelbard-Sagiv 2008 (internal
    cue reinstates the encoding-time trace ahead of report). Constraint 3
    implication: the open question in this note ("how large a waking
    probe fraction before recovery turns into rumination") is NOT a free
    knob -- inhibition-only recoverability makes rumination the predicted
    equilibrium (Bouton 2004/2014; Maren 2013), so the probe fraction
    interacts with the open abandon-path question (Section 0.3) and must
    be validated against the Constraint-3 rumination signature, not tuned
    in isolation. Interpretive reframe; status/phase/confidence
    unchanged; no new claim/experiment; no substrate change this pass.

    [2026-08-02 governance, V3-EXQ-881, PASS, evidence_direction=supports, FIRST full run to
    completion]: representational/wall-independent confirmer using effect_size = ||mean(ghost
    endpoints) - mean(value-flat endpoints)|| / (value-flat population's own internal scatter),
    computed within one propose_trajectories() call so both populations share the identical
    per-episode chaos amplitude (an earlier raw-L2-distance DV attempt was built, smoke-tested, and
    REJECTED after direct measurement showed it swamped by untrained-e2.world_forward chaos
    amplification within 1 rollout step). C1 (load-bearing): 7/7 seeds comparable, 7/7 wins,
    effect_size 3.1-12.5 vs a pre-registered 2.0 bar -- MECH-293's ghost branch robustly produces
    distinguishable proposal content from ordinary current-anchor CEM. C2 (non-gating, "still-wanted"
    directedness): bare 4/7 majority, mean alignment 0.027 -- reported honestly as weak/marginal, not
    folded into a stronger claim; left to V3-EXQ-495 (the behavioural V3-completion gate). v3_pending
    STAYS TRUE (V3-EXQ-495 behavioural validation still owed); this closes the "instead of relying on
    pure current-anchor CEM" half of the claim V3-EXQ-497 never tested.
  implementation_note: "SUBSTRATE LANDED 2026-04-27. Module: ree-v3/ree_core/hippocampal/module.py (HippocampalModule.propose_trajectories() extended with optional current_z_goal kwarg + minority ghost-seeded branch consuming MECH-292 GhostGoalBank.rank(); new private methods _propose_ghost_seeded + _mix_value_flat_with_ghost; diagnostic accessor get_last_propose_diagnostics surfacing mech293_n_ghost_proposed / n_ghost_admitted / max_ghost_priority / mean_goal_match_at_seed / reason). ree-v3/ree_core/predictors/e2_fast.py: Trajectory dataclass extended with hypothesis_tag (bool, default False) and metadata (Optional[Dict[str, Any]], default None) fields. ree-v3/ree_core/agent.py: REEAgent._e3_tick threads goal_state.z_goal (when goal_state is active) into propose_trajectories; record_committed_trajectory explicitly sets hypothesis_tag=False / metadata=None on the executed committed trajectory so the executed path drops any ghost provenance from the source proposal (the executed trajectory IS real, regardless of origin -- spec requirement). Algorithm: n_ghost = clamp(round(n*mech293_ghost_fraction), [min_ghost, max_ghost]) bounded by bank size; each ghost seeds the action-object distribution from anchor.z_world (NOT current z_world), single noise draw (no inner CEM refit so probe cost <= one value-flat sample), e2.rollout_with_world rolls forward, traj.hypothesis_tag=True + traj.metadata={source: 'mech293_ghost_probe', anchor_key, ghost_priority, goal_match}; mix per mech293_replace_lowest_ranked (default True drops highest-cost value-flat candidates and appends ghosts at tail; False appends raising total). Config: REEConfig.use_mech293_ghost_probes (default False) + 4 sub-knobs (mech293_ghost_fraction=0.2, mech293_min_ghost_candidates=1, mech293_max_ghost_candidates=8, mech293_replace_lowest_ranked=True) all wired through REEConfig.from_dims. Precondition (HippocampalModule.__init__): use_mech293_ghost_probes=True requires use_mech292_ghost_bank=True; the MECH-292 block transitively guarantees use_anchor_sets=True and AnchorSetConfig.use_sd039_anchor_payload=True so only the bank flag is asserted explicitly here (loud-not-silent). Backward compat verified: 12/12 MECH-293 contracts + 183/183 full preflight + contracts PASS with master OFF (bit-identical to pre-MECH-293 HEAD). MECH-094: ghost trajectories carry hypothesis_tag=True for provenance routing; CEM rollout does not write residue/anchors during proposal; record_committed_trajectory strips the tag at commit; SD-039 build_goal_payload(simulation_mode=True) handles the population-side gate already. ARC-007 strict preserved: no value head added; goal-match enters via MECH-292's external ranking, ghost seeding is by LOCATION (anchor's z_world) not by internal value computation. Validation: V3-EXQ-497 5/5 PASS 2026-04-27 in 34s on Mac (UC1 module surface; UC2 master-OFF no-op n_ghost=0 / diagnostics={} / candidates default-clean; UC3 ghost branch fires n_ghost_admitted=4 max_priority=1.61 mean_goal_match_at_seed=0.998; UC4 hypothesis_tag preserved on every ghost + metadata complete + 28 value-flat candidates default-clean; UC5 budget arithmetic round(0.25*8)=2 in [1,4] arm A + bank-size cap to 1 in arm B + min-floor wins over fraction=0.0 in arm C). v3_pending KEPT TRUE -- behavioural validation (faster recovery from blocked-goal conditions vs random-probe baseline; lower revisitation of fully incoherent stale regions) lives in V3-EXQ-495 (V3 full-completion gate, MECH-163 dual systems test) and is not yet run. Design doc: REE_assembly/docs/architecture/mech_293_ghost_goal_probe_search.md (Status flipped DESIGNED -> IMPLEMENTED)."
  location: docs/architecture/ghost_goal_search.md

- id: ARC-060
  title: "A good agent needs both a continuous wanting landscape and a discrete bank of unresolved goal traces: field-level wanting supports smooth local guidance, while ghost-goal traces preserve blocked or deferred goals whose local gradient has collapsed."
  claim_type: architecture_hypothesis
  subject: goal.explicit_unresolved_goal_hierarchy
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-603g-624c-651a_2026-06-07#V3-EXQ-651a"
      as_of: 2026-06-07
      verdict: non_contributory/substrate_conditional
  implementation_phase: v3
  v3_pending: true
  pending_retest_after_substrate: true  # [2026-06-08 governance] survival-leg cohort gated on escape-affordance-bridge substrate (failure_autopsy_V3-EXQ-603h_2026-06-08)
  claim_level: architectural
  registered_utc: 2026-04-26
  depends_on:
    - ARC-051    # emergent multi-level wanting hierarchy
    - SD-039     # preserved goal payload on anchors
    - MECH-216   # predictive wanting
    - MECH-217   # replay-spread wanting
    - MECH-230   # structured latent goal representation
    - MECH-292   # ghost-goal bank
    - MECH-293   # waking ghost-goal probes
    - ARC-078    # parent: bank is a cue-addressed retrieval system (ARC-060 specializes into it)
  functional_restatement: >
    ARC-051 argues that REE can obtain a multi-level goal hierarchy from a single
    wanting landscape fed by contact, schema, and replay mechanisms. ARC-060 refines
    that picture: the field is necessary but not sufficient. When local gradients
    collapse because a path is blocked, a goal is deferred, or the environment is
    partially remapped, the system needs a discrete unresolved-goal trace that keeps
    the goal computationally alive. The good-agent architecture is therefore hybrid:

    - continuous wanting field for smooth local navigation
    - discrete ghost-goal bank for blocked/deferred goal persistence

    The architecture predicts that agents with the field alone will navigate well
    when gradients exist but will prematurely abandon distal or blocked goals; agents
    with both layers will recover more flexibly after invalidation.
  notes: >
    Registered 2026-04-26 from the ghost-goal lit-pull. This is intentionally framed
    as an extension to ARC-051, not a contradiction of it. ARC-051 explains how
    approach gradients emerge; ARC-060 explains why a high-quality agent also needs
    explicit unresolved-goal persistence once those gradients fail locally.
  evidence_quality_note: >
    Registered candidate. Literature support comes from
    `targeted_review_ghost_goal_search/`: Berridge 1998 and Barch 2010 ground the
    need for persistent wanting/goal representations, Pfeiffer 2013 grounds goal-
    biased path search, Gillespie 2021 grounds non-current trace reactivation, and
    Muessig 2019 grounds a shared waking/offline sequence generator.

    [2026-05-19 retrieval-cue reframe] (docs/architecture/
    ghost_goal_search.md Section 0; 2026-05-18 combined cue-system pull).
    The continuous-field vs discrete-bank split ARC-060 asserts IS the
    recognition-vs-recall distinction: the field supports gradient
    following when a local cue is present; the bank supports recall of a
    goal whose local gradient has collapsed, from a partial or internal
    cue (Nakazawa 2002 partial-cue completion; Gelbard-Sagiv 2008
    internal-cue reinstatement). Bouton 2004/2014 + Maren 2013 ground
    that the discrete preserved traces are biologically real AND that an
    inhibition-only persistence design makes rumination/intrusion the
    structural equilibrium -- so the architectural claim must treat an
    explicit abandon path (the deliberate biology-divergence point,
    Section 0.3) as an open structural element, not an implementation
    detail. Interpretive reframe; status/phase/confidence unchanged; no
    new claim/experiment; no substrate change this pass.

    [2026-06-07 governance: V3-EXQ-651 NON-CONTRIBUTORY, NOT a weakening]
    (failure_autopsy_V3-EXQ-651_2026-06-07). The blocked-goal recovery test
    FAILed on a measurement/test-design defect, not on ARC-060: the discrete
    ghost-goal bank genuinely engaged (C1 3/3; 130-146 ghost candidates;
    goal_active_frac=1.0), but the FIELD_ONLY negative control never exhibited
    the premature abandonment ARC-060 predicts (Phase-C goal-proximity ~0.98 in
    BOTH arms), so the bank's rescue advantage had nothing to act on. The latent
    goal_proximity metric also saturates and does not read behavioural
    abandonment/re-approach. Substrate (SD-039/MECH-292/MECH-293) is landed and
    child-validated (494/496/497 PASS); the open item is test-design. Re-queue as
    V3-EXQ-651a with a non-degeneracy gate (FIELD_ONLY must abandon first) + a
    behavioural re-approach readout. Status/phase/confidence unchanged.
  location: docs/architecture/ghost_goal_search.md

- id: ARC-061
  title: "Self-attribution is implemented via a family of forward-model comparators at motor, interoceptive, and propositional levels — each instantiating the same reafference-cancellation motif (forward model + comparator + authorship tag) at a distinct representational timescale and modality."
  claim_type: architectural_commitment
  subject: self_attribution.reafference_comparator_family
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  claim_level: architectural
  registered_utc: "2026-05-03"
  depends_on:
    - MECH-095   # Level 1: motor/action comparator (TPJ), substrate_ceiling
    - ARC-058    # Level 2: interoceptive/affective comparator (shared harm trunk)
    - MECH-094   # Level 3: propositional simulation tag, stable
    - SD-047     # Level 1 substrate enrichment (registered, ready)
    - SD-048     # Level 2 substrate enrichment (registered, ready)
  functional_restatement: >
    Three comparator mechanisms implement the same architectural pattern at
    different levels of representational abstraction.

    Level 1 (motor/action): MECH-095 -- E2_harm_s counterfactual gap (agent-caused
    minus environment-caused state change). Forward model: E2_harm_s. Authorship tag:
    cf_gap > threshold licenses self-attribution.

    Level 2 (interoceptive/affective): ARC-058 -- HarmForwardTrunk prediction error
    tags somatic/affective state changes as self-generated or other-generated. Forward
    model: HarmForwardTrunk. Authorship tag: affective-PE > threshold licenses
    other-attribution.

    Level 3 (propositional/simulation): MECH-094 -- hypothesis_tag write gate marks
    simulation-mode content as non-real, preventing viability-field accumulation.
    Forward model: simulation rollout. Authorship tag: hypothesis_tag = True (simulation)
    vs False (real).

    Shared diagnostic signature across all three levels: symmetric over/under-attribution
    errors under substrate miscalibration (Asai 2016 S/N-slope), non-monotonic
    noise-dependence (Ward 2010 stochastic resonance), and bidirectional surface
    consequences from single substrate pathology (Jardri & Deneve 2013 circular
    inference; Nassar 2021 all-or-nothing updating). The SD-047 4-arm noise-level
    sweep is the calibration-diagnostic template for Level 1; analogous probes are
    required for Levels 2 and 3.
  notes: >
    Registered 2026-05-03. The claim is architectural rather than mechanistic: it
    asserts that the three-level structure reflects a general solution to the
    reafference problem at multiple representational scales, not three independent
    mechanism discoveries. Each level can fail independently; cross-level interaction
    (does Level 1 miscalibration propagate to Level 3 tag instability?) is V4-1
    territory -- multi-agent ecology required to test simultaneous activation of all
    three levels.

    Level 2 (interoceptive comparator): SD-048 (body.interoceptive_noise_dynamics,
    candidate, registered 2026-05-03, ready) is the calibration SD. Depends on
    SD-022 + SD-011 (both IMPLEMENTED).

    The ARC-061 claim is not testable as a whole until all three constituent levels
    have substrate-level evidence. Current state: Level 3 (MECH-094) confirmed_established;
    Level 1 (MECH-095) substrate_ceiling, SD-047 pending; Level 2 (ARC-058) V3-tractable
    but no calibration SD. Governance rule: do not promote ARC-061 until all three
    dependent levels have supporting experimental evidence.
  location: docs/architecture/reafference_comparator_family.md

- id: MECH-294
  title: "Theta-burst packet contains multi-content joint binding: each ~125ms theta cycle binds a {goal_latent, action_proposal, risk_estimate, state_summary} tuple within one phase-aligned window, with verisimilitude grounding the rollout step that must stay ahead of real time."
  claim_type: mechanism_hypothesis
  subject: theta_gamma.multi_content_joint_packet
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_569f-661-654a_2026-06-10#V3-EXQ-661"
      as_of: 2026-06-10
      verdict: "weakens/standard -- V3-EXQ-840b resolved the pending_retest; substrate_ceiling GRADUATED to standard, see evidence_quality_note"
  implementation_phase: v3
  v3_pending: true
  epistemic_category: standard
  pending_retest_after_substrate: false
  claim_level: mechanistic
  registered_utc: 2026-04-26
  ceiling_decision: deferred
  ceiling_routing_note: "2026-06-19 substrate-ceiling-lifted triage (verdict b STILL CEILINGED): modulatory-bias-selection-authority is implemented but cannot carve MECH-294 -- currency_coherence is a SCALAR (rescale-invisible), so the route-range authority and the 569i top-k shortlist have no per-candidate range to act on (V3-EXQ-661: committed-distribution TV ~0 incl gate-ON vs gate-OFF). The prior /implement-substrate wiring step (express binding-coherence as cross-candidate range) is now LANDED 2026-06-19 (per-candidate co-binding coherence: theta_packet_compose_per_candidate_coherence, no-op default) -- the carve-able channel now exists (JOINT range>floor / SHUF ~0 / scalar mode-discrimination preserved; contract suite is the readiness gate). The behavioural falsifier on this channel is the SEPARATE next /queue-experiment step (still gated on candidate first-action diversity). RESOLVED 2026-08-01: V3-EXQ-840b ran that behavioural falsifier -- clean weakens, all readiness green. epistemic_category GRADUATED to standard (see implementation_note). Primary lit falsifier (Kay-2020 theta) is out-of-substrate for V3."
  implementation_note: >
    Substrate IMPLEMENTED 2026-06-09 behind the no-op-default flag
    use_multi_content_theta_packet (bit-identical OFF). Sibling module
    ree-v3/ree_core/latent/multi_content_theta_packet.py
    (MultiContentThetaPacket) composes the MECH-089 ThetaBuffer for its
    state_summary slot and gathers goal_latent (GoalState.z_goal),
    action_proposal (hippocampal CEM first-action), risk_sensory (z_harm_s,
    SD-010) and risk_affective (z_harm_a, SD-011) into one per-cycle
    phase-aligned packet sealed at the E3-heartbeat boundary, with MECH-269b
    per-stream V_s snapshot-or-hold vintaging and a joint-read interface
    (joint_context / action_conditioned_on). Three binding regimes
    (joint / alternation / shuffled) resolve the Kay-2020 cross-cycle
    falsifier. NOT promoted or weakened: status stays candidate / v3_pending;
    the 2026-04-26 governance hold (requires a substrate-side
    joint-vs-alternation falsification test) stands until the discriminative
    validation V3-EXQ-657 PASSes per the design-memo S7.3 interpretation grid.
    AMEND 2026-06-09 (compose-coherence wiring): the read-only compose path
    (theta_packet_compose_into_e3_bias, still default False) previously read
    ONLY action_proposal -- identical across binding modes -- so the three
    regimes were behaviourally indistinguishable and the behavioural successor
    was unbuildable (it would self-route a false "joint clause not isolated"
    weakening). compose_e3_bias now GATES the per-candidate action-grounding
    bias by ThetaPacket.currency_coherence() (fraction of co-temporally-current
    streams: joint ~1.0 / alternation ~0.25 / shuffled 0.0), so the binding
    regime causally reaches E3 behaviour (the S6 discriminator). Parameter-free,
    bit-identical OFF, no flag/confidence change. New no-op flag
    theta_packet_compose_use_joint_coherence (default True; False = legacy
    action-only ablation). Still candidate / v3_pending; this only makes the
    behavioural mode-discrimination successor valid to run.
    AMEND 2026-06-19 (per-candidate co-binding coherence; route-range carve):
    the 2026-06-09 compose-coherence gate left currency_coherence() a SCALAR --
    the per-candidate compose PATTERN (cosine to the single bound action_proposal)
    was identical across binding modes; the gate only scaled magnitude uniformly,
    which the route-range authority's unit-range normalisation erases (joint==alt)
    and floors (shuffled). V3-EXQ-661 confirmed it: committed-action histograms
    byte-identical across modes, TV ~0 incl gate-ON-vs-OFF. The amend renders
    coherence as a PER-CANDIDATE quantity with mode-distinct cross-candidate RANGE:
    seal() binds, per V_s-gated stream, the action co-bound WITH that stream this
    cycle + a currency weight (JOINT all four -> full range; ALTERNATION live +
    held-prior at coherence_hold_weight -> distinct pattern; SHUFFLED none -> ~0),
    and compose_per_candidate_coherence routes it via the existing _bdc_coherence
    so the route-range authority + 569i top-k can carve it. New no-op flags
    theta_packet_compose_per_candidate_coherence (default False) +
    theta_packet_coherence_hold_weight (0.5); bit-identical OFF; currency_coherence()
    untouched (scalar mode-discrimination preserved). PROMOTES NOTHING -- stays
    candidate / substrate_ceiling / v3_pending; the 2026-04-26 hold stands; the
    behavioural falsifier on this channel is the SEPARATE next /queue-experiment
    step (no EXQ queued here -- the contract suite is the substrate-readiness gate).
    See ree-v3/CLAUDE.md "MECH-294" entry and
    docs/architecture/mech_294_multi_content_theta_packet.md.
    [2026-08-01 governance, V3-EXQ-840b, confirmed failure_autopsy_V3-EXQ-840b_2026-08-01,
    supersedes V3-EXQ-840 (measurement-window bug fix)]: this run IS the pending behavioural
    falsifier the ceiling_routing_note above named. All 12 readiness preconditions across 4 arms
    GREEN (routed coherence genuinely differentiates JOINT 0.069 / ALT 0.057 / OFF/SHUF 0.0), 10
    seeds run. C1 (JOINT != ALTERNATION, load-bearing): mean TV 0.087 vs 0.1 floor, only 3/10
    seeds above floor (7 required) -- FAIL. C2 (JOINT != SHUFFLED): mean TV 0.193, moot given C1.
    Label: joint_indistinguishable_from_alternation. With substrate confirmed ready and the
    manipulation confirmed to differentiate the arms, there is no residual wiring-gap candidate
    left -- this is the "genuine over-specification" branch the driver's own pre-registered
    outcome map named: MECH-294's joint-binding claim, as worded, is not supported at the
    behavioural-committed-action level this substrate can measure, consistent with (though not a
    full test of) Kay 2020's more parsimonious cross-cycle/alternation account.
    evidence_direction: weakens (confirmed). epistemic_category GRADUATED substrate_ceiling ->
    standard (2026-08-01, user-confirmed at the autopsy's interactive gate) -- the retest this
    category was explicitly pending on has happened and come back fully clean; continuing to
    exclude this evidence as ceiling-pending would misrepresent the current state.
    pending_retest_after_substrate -> false. NO DEMOTION -- per the driver's own note, "hold
    stands": one clean weakens against a candidate architectural claim with existing lit
    grounding is informative but not sufficient alone for demotion. No further
    experiment/lit-pull/build routed from this autopsy.
  depends_on:
    - MECH-089   # cross-frequency coupling (theta-gamma nesting; the substrate primitive)
    - MECH-091   # phase-reset of E3 heartbeat clock by salient events
    - MECH-269   # V_s anchor selection (anchor-eligible streams contribute to packet content)
    - MECH-269b  # V_s gating of E1/E2 rollouts (verisimilitude grounds the rollout step)
    - SD-005     # z_self/z_world split (precondition for stream-typed packet content)
    - SD-010     # harm stream separation
    - SD-011     # dual nociceptive streams (z_harm_s as risk_estimate component)
    - ARC-018    # hippocampal rollouts / viability mapping (the structure the packet propagates into)
  functional_restatement: >
    Mechanism hypothesis. MECH-089 establishes that theta cycles are
    organised into gamma sub-slots that route content streams from
    distinct upstream sources. MECH-294 specifies the content scheme:
    one theta cycle (~125ms) binds a heterogeneous-content tuple
    {goal_latent, action_proposal, risk_estimate, state_summary} into
    one phase-aligned packet, NOT one content stream alternating
    across cycles.

    Joint binding within one cycle is the load-bearing claim. The
    proposer reads the packet as a joint object: which action_proposal
    is on the table is interpretable only against the goal_latent and
    risk_estimate co-bound in the same cycle. Rollout step at t+1
    consumes the packet at t and must stay ahead of real-time
    arrival (the rollout horizon must exceed sensory latency or the
    packet gates trigger after the action is already committed).

    Verisimilitude grounding (links to MECH-269/269b). The rollout
    step is grounded by per-stream V_s: only anchor-eligible streams
    contribute current values to the packet; misaligned streams hold
    last-verified snapshots. This means the packet is not a homogeneous
    "current latent" -- it is a stream-typed object whose components
    have potentially different temporal vintages.

    Falsifiable (primary): joint multi-content decoding analyses on
    hippocampal+amygdala+BG+ACC theta-resolved datasets should show
    semantically-distinct content streams (goal vs risk vs state)
    simultaneously decodable from one theta cycle's worth of population
    activity at the binding-window timescale. If only one content axis
    is decodable per cycle (Kay 2020 alternation pattern is the
    dominant architecture), MECH-294's joint-packet hypothesis is
    falsified.

    Falsifiable (secondary): in V3 substrate, manipulating which
    streams are anchor-eligible (V_s gating) should change which
    components of the packet are current-vs-stale; downstream consumers
    (E3 commit gate, dACC adaptive control) should respond to the
    component-typed packet differently than to a homogeneous-latent
    null.
  notes: >
    Registered 2026-04-26 from the architectural conversation around
    theta-burst content (Daniel's question: "does this mean theta bursts
    contain goal, action, risk, state summary, with verisimilitude
    grounding the next rollout step which must stay ahead of real
    time"). The within-cycle vs across-cycle binding question is the
    strongest direct empirical risk and is named explicitly in the
    falsifiable predictions.

    Lit-pull: targeted_review_mech294_theta_burst_packet/ (7 entries).
    Substrate well-anchored (Lisman & Jensen 2013, Colgin 2009,
    Igarashi 2014, Wikenheiser & Redish 2015). Joint-binding
    specialisation sparse: closest direct evidence is Igarashi et al.
    2014 (LEC-CA1 cue+context, 2 streams within theta-organised
    window). Kay et al. 2020 (Cell) cross-cycle alternation pattern
    is the strongest direct architectural challenge -- MECH-294
    requires interpretation that Kay's two streams are members of a
    richer multi-content packet collapsed by path-decision-axis
    analysis; the parsimonious read of Kay's data is one-stream-per-
    cycle alternation.
  evidence_quality_note: >
    Substrate well-anchored, specialisation sparse. Theta-gamma
    multiplexing substrate (theta cycles structured into gamma
    sub-slots; gamma frequencies routing content streams from distinct
    upstream sources) is supported by canonical work: Lisman & Jensen
    2013 (Neuron, theta-gamma neural code), Colgin et al. 2009 (Nature,
    gamma routes information). Direct evidence that one of MECH-294's
    four streams (goal_latent) is theta-cycle-resolved comes from
    Wikenheiser & Redish 2015 (Nat Neurosci). Closest direct evidence
    for cross-region joint binding of two semantically-distinct streams
    within a theta-organised window is Igarashi et al. 2014 (Nature,
    LEC-CA1 cue+context). Two genuine empirical risks remain: (i)
    channel capacity -- whether the same architecture supports four
    heterogeneous streams, untested in the literature; (ii) within-cycle
    vs across-cycle binding -- Kay et al. 2020 (Cell) demonstrates
    cross-cycle alternation as the dominant pattern in their dataset,
    which would falsify MECH-294's joint-packet hypothesis if it is the
    only architecture present. MECH-294 is sparse-but-not-falsifying.
    Architecture is empirically testable by joint multi-content
    decoding analyses on hippocampal+amygdala+BG+ACC theta-resolved
    datasets; no such analysis has been published. See
    `evidence/literature/targeted_review_mech294_theta_burst_packet/SYNTHESIS.md`
    for the full breakdown.

    Governance flag (2026-04-26 hold): the within-cycle vs cross-cycle
    binding question (Kay et al. 2020 architectural challenge) is the
    explicit promotion-blocker. Next governance cycle should not promote
    MECH-294 above candidate without (a) a substrate-side falsification
    test that can discriminate joint-packet from cross-cycle alternation,
    or (b) new empirical decoding evidence resolving Kay 2020's pattern
    against multi-stream within-cycle binding. Until then the joint
    hypothesis is held as a candidate mechanism, not a stated load-bearing
    invariant.

    [2026-06-10 governance / confirmed failure_autopsy_569f-661-654a_2026-06-10]:
    V3-EXQ-661 (compose-coherence behavioural-readiness diagnostic, no claim
    tags, non_contributory) confirmed the substrate is wired -- currency_coherence
    is cleanly mode-distinct (JOINT 1.0 / ALT 0.25 / SHUF 0.0) and criteria
    non-degenerate -- but the committed-action distribution TV is ~0 (max 0.0097)
    across joint-vs-alternation, joint-vs-shuffled, AND coherence-gate-ON-vs-OFF.
    The C3 gate-ON==gate-OFF result shows co-binding coherence is not load-bearing
    at the E3 committed-action readout: the binding signal is present upstream but
    is not routed into choice. Do NOT queue the MECH-294 behavioural-evidence
    successor on the joint-specificity reading until the modulatory-bias-selection-
    authority substrate amend routes upstream-channel range into the modulatory
    bias the selection authority rescales. Member of the 569f/661/654a E3-commit-
    readout substrate_ceiling cluster (shared with ARC-065, ARC-062, MECH-309).
    epistemic_category set substrate_ceiling + pending_retest_after_substrate=true.
    No promotion / no weakening; the 2026-04-26 hold stands.

    [2026-07-30 governance / confirmed failure_autopsy_batch-687a-707c-840-748a-833-842-810b-673-614-798afail]:
    V3-EXQ-840 (first MECH-294 behavioural-evidence run, routing the per-candidate co-binding coherence
    via modulatory_channel_route_source="coherence" through the now-built modulatory-bias-selection-authority)
    FAIL / non_contributory / measurement_test_design_defect -- NOT a new substrate_ceiling hit. The
    readiness safeguard asserts the SAME statistic (route_range) the load-bearing criteria depend on, and
    correctly caught a sub-floor cross-candidate range on ARM_1_JOINT at real-run scale (the contract-test
    suite only exercises smoke scale). MECH-294 stays candidate/v3_pending/substrate_ceiling; this run is
    NOT counted toward the GOV-CEIL-1 hit total. substrate_queue.json CREATE: sd_id
    mech294_coherence_magnitude_strengthening (strengthen per-candidate co-binding coherence magnitude, or
    audit the readiness-floor calibration, before re-running the same falsifier). pending_retest_after_substrate
    UNCHANGED (true).
  location: docs/architecture/control_plane_heartbeat.md

- id: MECH-269b
  evidence_quality_note: |
    [update_z_goal_typeerror_swallowed] EXQ-471/475/483/483a/483b/490/490b/490c/490e/490f/524 cohort evidence is contaminated and superseded as of 2026-05-07 due to a script-side TypeError in agent.update_z_goal that was silently swallowed by a bare except, leaving GoalState.is_active() always False. Additional cohort-wide issues found: 2x agent.sense() per warmup step, agent.update_residue() never called (residue field empty / running_variance pinned), dACC bias diagnostic reading wrong tensor. Awaiting StepHarness migration + corrected re-runs (EXQ-475a / EXQ-483c / EXQ-490g / EXQ-471a / EXQ-524a). See REE_Working session 2026-05-07. [R4.b stamped 2026-05-29] behavioral_diversity_isolation:GAP-D Theory-4 R4.b decision-rule stamped on diagnostic-probe evidence: V3-EXQ-601 PASS/supports MECH-269b 2026-05-21T12:02Z (MECH-269b-followup-A staleness-gate wiring). Run is in claim_evidence.v1.json with evidence_direction=supports + status=PASS but scoring_excluded=diagnostic_probe (experiment_purpose=diagnostic), so the standard /governance pipeline cannot lift this claim off hold_pending_v3_substrate via exp_conf -- the stamp is a governance-level recognition of the substrate-path confirmation, NOT an experimental_confidence move. v3_pending=true preserved. Non-diagnostic behavioural sufficiency test (Q-040b) is owned by goal_pipeline:GAP-4 / V3-EXQ-490g Tier-1 retest cohort -- that is the path to lifting v3_pending. See evidence/planning/behavioral_diversity_isolation_plan.md 2026-05-29 GAP-D R4.b stamp section.

    Literature support is asymmetric across the claim's two architectural halves. Cortex-side per-stream precision/reliability gating of prediction errors has strong inferential support from the predictive-coding programme: cortical microcircuits implement precision-weighted PE (Bastos et al. 2012, Neuron, doi:10.1016/j.neuron.2012.10.038), attention is precision (Feldman & Friston 2010, Front Hum Neurosci, doi:10.3389/fnhum.2010.00215), pulvinar-cortex modulatory gain is the most direct biological substrate proposal (Kanai et al. 2015, Phil Trans R Soc B, doi:10.1098/rstb.2014.0169), and aberrant precision produces the wired-but-inert clinical phenotype the claim warns about (Adams et al. 2013, doi:10.3389/fpsyt.2013.00047; Lawson et al. 2014, doi:10.3389/fnhum.2014.00302). The empirical foundation for per-stream reliability-weighted integration is psychophysically demonstrated for cue integration (Ernst & Banks 2002, Nature, doi:10.1038/415429a). However, the symmetric-application half of the claim -- that the SAME V_s signal gates BOTH hippocampal proposer anchor selection AND cortical E1/E2 forward predictions -- is genuinely novel architectural ground. The closest available precedent is Daw, Niv & Dayan 2005 (Nat Neurosci, doi:10.1038/nn1560), which arbitrates between two distinct world-model systems by uncertainty, but is structural rather than direct. No paper in the targeted review demonstrates symmetric hippocampal+cortical precision-gating at the resolution MECH-269b assumes. Sparse direct support for symmetric-application does NOT falsify the mechanism; biology may not have been measured at this resolution. Substrate validation must distinguish behavioural signatures predicted by symmetric-V_s gating (cortex-side wired-but-inert failure mode without V_s on E1/E2 rollouts; downstream consumers receive stale-but-confident-looking PE) from cortex-only-gating null hypotheses. V3-EXQ-490 FAIL x2 (2026-04-27, diagnostic -> reclassified non_contributory in /governance 2026-04-27). Two near-identical FAIL runs of the Q-040 factorial (3 seeds each, ON_OFF vs ON_ON arms differing only in use_vs_rollout_gating). Sub-diagnostic outcome: c1 gate-firing precondition itself FAILed (c1_on_total_held = 0, c1_off_total_held = 0 across both runs and both arms). With c1 not firing, c2 (approach_commit recovery) and c3 (dACC PE) cannot be interpreted -- the experiment never reaches the question it was designed to answer. Two near-identical FAILs across separate runs is suspicious; flagged for /diagnose-errors to investigate gate-firing precondition (possible substrate gating bug or smoke-scale insufficient warmup). Hold at candidate; substrate validation deferred until /diagnose-errors clears the gate-firing precondition.
  title: "Regional verisimilitude V_s gates not only hippocampal proposer anchor selection but also which streams ground forward predictions in cortical world models (E1 sensory predictor, E2 transition model). Without symmetric V_s gating on E1/E2 rollouts, downstream consumers receive stale-but-confident-looking prediction errors -- the wired-but-inert failure mode."
  claim_type: mechanism_hypothesis
  subject: cortical_world_model.regional_verisimilitude_rollout_gating
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_601_mech269b_followup_a_staleness_gate_20260521T120249Z_v3
      as_of: 2026-05-21
      verdict: supports/PASS
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-04-26
  instantiates: MECH-269
  depends_on:
    - MECH-269   # parent: V_s anchor selection on the hippocampal proposer
    - MECH-284   # staleness accumulator (V_s_anchor read-out)
    - SD-005     # z_self/z_world split (per-stream gating requires per-stream identity)
    - SD-010     # harm stream separation
    - SD-011     # dual nociceptive streams
    - ARC-033    # E2_harm_s forward model substrate (one of the E2 streams under gating)
  functional_restatement: >
    Mechanism hypothesis. MECH-269 specifies V_s gating on the
    hippocampal proposer's anchor selection (which streams contribute
    to rollout starting-state). MECH-269b extends the same gating
    primitive symmetrically to cortical forward predictions: at each
    E1 sensory prediction step and each E2 transition step, the
    contribution of stream s to the forward computation is gated by
    V_s(s, t) >= theta_gate(s, e1_or_e2). Misaligned streams contribute
    held-snapshot values, NOT current latent values, to the forward
    prediction.

    Why symmetric. Without symmetric V_s gating, E1/E2 forward
    predictions consume stream slices indiscriminately -- including
    streams whose alignment to perception has decayed. Downstream
    consumers (dACC adaptive control, E3 commit gate, MECH-269
    proposer scoring) receive prediction errors computed from
    stale-but-confident-looking forward values. The wired-but-inert
    failure mode -- modules implemented and active but no behavioural
    consequence -- is the predicted phenotype: PE signals fire,
    override gates fire, but the corrective signal is computed against
    the wrong reference and produces no effective revision.

    Per-stream not per-region. V_s is computed per latent stream
    (z_world, z_self, z_harm_s, z_harm_a, z_goal, ...) not per
    cortical region. The same V_s vector is consumed by both the
    hippocampal proposer (MECH-269 anchor selection) and cortical
    forward predictors (this claim). Sharing the V_s computation
    across world-model substrates is the key architectural commitment.

    Distinct from MECH-098. MECH-098 feeds V_world specifically via
    reafference cancellation and is one signal source for the V_s
    vector, not the gating mechanism itself.

    Falsifiable (primary): a V3 ablation factorial with V_s gating
    enabled/disabled on E1/E2 rollouts (with the hippocampal proposer
    gating held at MECH-269 baseline) should show that disabling
    cortical-side V_s gating reproduces the EXQ-483 wired-but-inert
    signature: override mean elevated, PAG releases up, approach_commit
    near zero. Enabling cortical-side V_s gating should restore
    behavioural sensitivity to PE.

    Falsifiable (secondary): clinical/neural correlates -- aberrant
    precision phenotypes (psychosis-spectrum, autism-spectrum) should
    map onto specific patterns of V_s mis-setting (under-precision on
    sensory streams, over-precision on internal streams) consistent
    with Adams 2013 / Lawson 2014 readings. This is corroborative not
    primary evidence.
  notes: >
    Registered 2026-04-26 as a child instantiation mech of MECH-269.
    Rationale: the architectural question "does V_s also context the
    cortical world model?" was raised by Daniel and required a separate
    claim because the symmetric-application novelty is the load-bearing
    commitment, not the V_s primitive itself. Lit-pull:
    targeted_review_mech269b_vs_rollout_gating/ (7 entries; mean conf
    ~0.69; symmetric-application gap explicit).

    The wired-but-inert phenotype in V3-EXQ-483 (override + PAG
    releases without approach_commit) is the proximate motivating
    failure. MECH-269b is one candidate root cause; MECH-295 is a
    complementary candidate cause. The two are not mutually exclusive
    and a factorial test should include both manipulations.

    Substrate IMPLEMENTED 2026-04-26 in ree-v3/ree_core/regulators/
    vs_rollout_gate.py (VsRolloutGate, VsRolloutGateConfig). Read-side
    consumer of MECH-269 Phase 1 hippocampal.per_stream_vs at the
    cortical forward-prediction call sites: E1 sensory predictor
    (latent.z_self / z_world / z_goal gated in REEAgent._e1_tick before
    e1(...) and extract_cue_context()) and E2_harm_a per-tick forward
    call (z_harm_a gated in REEAgent.select_action before
    e2_harm_a forward). Held-snapshot semantics: snapshot refresh
    threshold 0.5; per-side gate threshold 0.4 (each side has its own
    threshold and per-stream override dict). 0.4-0.5 dead-band gives
    lightweight Schmitt-trigger hysteresis. Master switch
    use_vs_rollout_gating in HippocampalConfig (default False);
    requires use_per_stream_vs=True at agent build time (ValueError
    on mismatch). Backward compatible: with flag OFF or with V_s
    seeded at 1.0, gate fires zero times. Validation experiment
    V3-EXQ-490 queued (Q-040 factorial; 3 seeds, 80 warmup x 5 eval
    x 200 steps). v3_pending remains true pending validation.

    MECH-269b-followup-A (staleness into VsRolloutGate) IMPLEMENTED
    2026-04-29: HippocampalConfig.use_vs_gate_staleness_lookup (default
    False) wires MECH-284 per_stream_staleness into gate threshold via
    effective_vs = raw_vs - staleness[s]; mirrors AnchorSet.tick_hysteresis
    staleness_lookup. Substrate validation V3-EXQ-601 PASS 2026-05-21
    (IGW-20260521-020/021): C1+C2 at default 0.4/0.5 thresholds without
    smoke overrides; resolves 2026-04-27 architectural_mismatch (raw V_s
    hold unreachable). v3_pending remains true until Q-040b behavioural
    factorial (approach_commit + dACC) on StepHarness cohort.
  location: docs/architecture/control_plane_heartbeat.md

- id: MECH-295
  evidence_quality_note: |
    [update_z_goal_typeerror_swallowed] EXQ-471/475/483/483a/483b/490/490b/490c/490e/490f/524 cohort evidence is contaminated and superseded as of 2026-05-07 due to a script-side TypeError in agent.update_z_goal that was silently swallowed by a bare except, leaving GoalState.is_active() always False. Additional cohort-wide issues found: 2x agent.sense() per warmup step, agent.update_residue() never called (residue field empty / running_variance pinned), dACC bias diagnostic reading wrong tensor. Awaiting StepHarness migration + corrected re-runs (EXQ-475a / EXQ-483c / EXQ-490g / EXQ-471a / EXQ-524a). See REE_Working session 2026-05-07. [2026-06-04 governance / failure_autopsy_V3-EXQ-490k] V3-EXQ-490k (modulatory-sufficiency argmin-flip probe, diagnostic) ran to completion (PASS, probe_ran=true) but could NOT test the narrowed-modulatory reading: ROW_2_fires_but_never_flips -- the cue fires on all 3 seeds (mech295_fired_ticks=145) yet argmin_flip_ticks=0 because mech295_bias_range_mean=0.0 (per-candidate bias uniform across candidates -> subtracting it cannot change the argmin BY CONSTRUCTION). The option-(c) probe correctly removed the 490j saturation contamination but is defeated by a DIFFERENT upstream gap (collapsed candidate z_world + weak z_goal goal_norm_peak=0.193). non_contributory for governance weighting; epistemic_category set to substrate_ceiling; pending_retest_after_substrate via 490L on enriched substrate with a pre-registered mech295_bias_range_mean>0 guard before interpreting argmin flips. narrow_supports_flag=true: MECH-295 shows zero behavioural-sufficiency across the 490g/h/i/j/k lineage. [2026-06-09 governance / re-scope + GAP-4 close, user-approved] goal_pipeline:GAP-4 CLOSED by re-scoping MECH-295 to the modulatory reading rather than continuing to block on a V3-EXQ-490L successor. The behavioural-necessity reading is terminally falsified (490j); the modulatory reading stands and is substrate-supported; the modulatory-sufficiency behavioural retest (490L) is retained as an OPTIONAL, non-GAP-blocking future test gated on the scaffolded_sd054_onboarding substrate (owned elsewhere, in flight via the 603-line Stage-H programme). epistemic_category=substrate_ceiling and pending_retest_after_substrate=true preserved for that optional test; status stays candidate (no promotion).

    Strong literature support for the architectural premise (a separable liking-stream substrate exists) and the general principle (drive-state changes route through experienced hedonic value before reaching action selection). Strongest direct evidence: Smith Berridge & Aldridge 2011 (PNAS, doi:10.1073/pnas.1101920108; VP single-unit, drive change recodes palatability before cue firing) and Dickinson & Balleine 1994 (Animal Learn Behav, doi:10.3758/BF03209866; instrumental devaluation requires outcome re-experience). Architectural articulation in Berridge & Kringelbach 2015 (Neuron, doi:10.1016/j.neuron.2015.02.018). Substrate evidence: Pecina & Berridge 2005 (J Neurosci, doi:10.1523/JNEUROSCI.3331-05.2005), Castro & Berridge 2014 (Phys Behav, doi:10.1016/j.physbeh.2014.02.063). The strong- necessity reading (no liking activation => no approach) versus the weak-bridge reading (baseline liking sufficient if bridge intact) is NOT arbitrated by existing literature; the DAT-knockdown finding (Pecina et al. 2003, J Neurosci, doi:10.1523/JNEUROSCI.23-28-09395.2003) is compatible only with the weak reading. MECH-295 commits to the weak reading provisionally. The cleanest direct test of MECH-295 (mu-opioid antagonist in NAc shell hotspot + hunger + cue-approach) is an experimental gap. EXQ-483's failure signature (approach_commit = 0.0 across all arms despite drive amplification) is consistent with a broken liking-bridge wiring. See `evidence/literature/targeted_review_mech295_liking_approach_bridge/SYNTHESIS.md` for the full breakdown.
    Governance flag (2026-04-26 hold): the strong-vs-weak necessity arbitration remains the explicit promotion-blocker. Next governance cycle should not promote MECH-295 above candidate without (a) a V3 substrate test that holds drive_level fixed and severs vs preserves the liking bridge, or (b) literature evidence resolving the DAT- knockdown / Pecina 2003 inconsistency with strong necessity. The weak reading remains the provisional commitment.
    V3-EXQ-490i autopsy upgrade (2026-05-31): the manifest evidence_direction_per_claim[MECH-295] is overridden from "weakens" to "narrow_supports" per confirmed failure_autopsy_V3-EXQ-490i_2026-05-30. Bridge fires cleanly in ARM_1 (cue_fires 6/4/12; write_fires 6/4/40) with approach_commit_rate=1.0 in 3/3 seeds; ARM_0 severed shows approach_commit_rate=0.0 in 3/3. The MECH-295 architectural prediction registered as the primary falsifiable test (bridge intact -> approach recovers; bridge severed -> collapses) is met in the sign direction across all seeds. Manifest evidence_direction=weakens rolls up (i) test-design contamination on C3_lift_vs_baseline (chosen substrate-side proxy is goal_norm_peak ARM_1 - ARM_0; ARM_0 has z_goal_enabled=True and accumulates a goal-norm baseline the metric assumes is zero -- seed-7 ARM_0 spikes to 12.49) and (ii) a separate C2 dACC wiring gap (dacc_bias_nonzero_steps=0 across all ARM_1 seeds despite cfg.use_dacc=True -- SD-032b consumer-pathway wiring gap, NOT a MECH-295 falsification). narrow_supports flag set: one experimental pair under one env config; broader retest comes with V3-EXQ-490j on a non-contaminated baseline arm (z_goal_enabled=False) plus direct bridge-magnitude probe. Routing: /queue-experiment for V3-EXQ-490j; /diagnose-errors for the SD-032b dACC bundle -> E3 score_bias propagation path. Substrate operative; no SD-amend.
    V3-EXQ-490j autopsy upgrade (2026-05-31, revised 11:58Z): manifest evidence_direction_per_claim[MECH-295] = 'weakens' at the behavioural-necessity layer. Severed-bridge baseline contract verified at the substrate layer (ARM_0 z_goal_enabled=False; bridge_cue_fires=0, bridge_write_fires=0, mech295_*_calls=0, goal_active_steps=0, goal_norm_peak=0.0 across 3/3 seeds; substrate-side C6 anticipatory_write_peak 0.287/0.0066/0.069 + C7 approach_cue_bias_peak 0.396/0.054/0.427 + C9 write_sum 80.2/0.58/17.5 ALL PASS in ARM_1 vs zero baseline). The C8 approach_commit_rate_lift FAIL is NOT purely metric contamination -- approach_commit_rate = 1.0 in BOTH arms across all 3 seeds reflects the substantive fact that ARM_0 (link severed, drive amplification active per gap4 substrate) still produces approach commitment via parallel pathways: MECH-216 schema readout (drive- modulated wanting write), MECH-290 backward credit sweep (VALENCE_WANTING at completion), MECH-307 anticipatory liking write at predicted location, and serotonin tonic_5ht benefit_salience -- all drive-> approach pathways operating independently of the SD-014/SD-015 liking-stream substrate that MECH-295 names. This falsifies the weak-reading necessity claim ('if the link is severed, drive amplification produces no approach regardless of drive magnitude') at the behavioural layer. The substrate IS implemented correctly (V3-EXQ-493 6/6 PASS isolation + 490j C6/C7/C9 PASS); what is mis- scoped is the claim. Governance applied (2026-05-31): MECH-295 narrowed from a necessity claim to a modulation claim -- 'the drive -> liking-stream -> approach-cue gain pathway biases approach scoring via a liking-specific per-candidate score_bias; it is not the sole or necessary pathway by which drive amplification reaches approach action selection in a multi-pathway substrate'. Biology-faithful (Berridge & Robinson 2003 wanting/liking dissociation; Berridge & Kringelbach 2015 hedonic architecture as one pathway among several); resolves the long-standing Pecina 2003 DAT-knockdown tension that troubled the weak reading. v3_pending gate stays held until the narrowed claim's substrate-isolating retest lands. Routing: PRIMARY /governance for claim narrowing (applied this cycle); SECONDARY /queue-experiment V3-EXQ-490k with direct E3 score_bias argmin-change probe (autopsy Section 9 option c as primary metric + option a as behavioural sanity check + option b as sensitivity-analysis ablation arm). Strong-reading falsifier (mu-opioid antagonist in NAc shell hotspot + hunger + cue-approach) remains the experimental gap for any reinstatement of a strong-necessity reading. Cohort context: 490g (Fork A library rebuild), 490h (runner silent-drop), 490i (goal_norm_peak baseline contamination), 490j (substrate fires correctly, behavioural-necessity reading reframed). Autopsy artifact: evidence/planning/failure_autopsy_V3-EXQ-490j_2026-05-31.{md,json} status=confirmed_revised.
    [2026-05-31 evening governance: V3-EXQ-618 reclassified non_contributory]: V3-EXQ-618 SD-049 Phase 3 consumer-cascade validation PASSed all four substrate-readiness acceptance criteria (C1 per_axis_drive evolves min_peak=0.027; C2 no crashes; C3 AIC reads per_axis mean_l1=5.92; C4 MECH-295 axis routes mean_l1=0.169) but is experiment_purpose= diagnostic and does not test MECH-295's behavioural-necessity hypothesis directly. Per the established pattern for substrate- readiness diagnostics in this claim (cf. EXQ-519b 2026-05-30 governance), reclassified non_contributory for both tagged claims. Load-bearing behavioural validation is owed to V3-EXQ-619 (reserved; replicates V3-EXQ-514g structure on Phase 3 substrate; unblocks goal_pipeline:GAP-2 cluster). MECH-295 stays candidate / v3_pending=true.
  epistemic_category: substrate_ceiling
  pending_retest_after_substrate: true
  what_would_answer: |
    NON-DEGENERACY PRECONDITION (primary, narrowed-modulatory reading): the
    E3 score_bias argmin-change probe is only interpretable if
    mech295_bias_range_mean > 0 -- the per-candidate MECH-295 score_bias
    must actually vary across candidates before "argmin never flips" can be
    read as evidence about MECH-295's causal weight, rather than as a probe
    that never had anything to flip. NOT MET as of this pass: V3-EXQ-490k
    (2026-06-04) ran to completion (mech295_fired_ticks=145 across 3 seeds)
    but argmin_flip_ticks=0 BY CONSTRUCTION (bias uniform across the
    candidate pool), traced to a substrate ceiling owned by
    scaffolded_sd054_onboarding (collapsed candidate z_world / weak z_goal,
    goal_norm_peak=0.193), NOT by MECH-295 itself. Re-checked 2026-08-08:
    scaffolded_sd054_onboarding's ecological readiness legs (G1-G3) flipped
    ready=true 2026-06-11 and its harm-pathway leg amend landed+validated
    2026-06-16, but a materially later and more direct measurement of the
    SAME z_goal-under-curriculum-load symptom (V3-EXQ-866a, 2026-08-03)
    shows z_goal_norm_mean still decays from a Stage-0 peak ~0.5 to a
    P2-window mean of 0.12 -- below the 0.4 floor -- spawning a fresh,
    still-unimplemented substrate_queue entry
    (scaffolded-curriculum-hazard-rebalance, status=pending_implementation,
    added 2026-08-08). The precondition therefore remains UNMET; do not
    queue V3-EXQ-490L until that entry (or a successor addressing the same
    symptom) lands and clears.

    [2026-08-09 governance, GFLAG-0007 resolution, CORRECTION]: the "Re-checked 2026-08-08"
    paragraph above is now STALE on two counts. First, the scaffolded-curriculum-hazard-rebalance
    entry it names was itself found UNWARRANTED and closed the same day (see INV-034/Q-021's own
    notes): the z_goal-decay symptom it targeted was a C6 measurement-methodology artifact
    (decay-only P2 mean vs the correct contact-gated run_p2 peak), not a real curriculum/hazard
    problem -- confirmed fixed by V3-EXQ-866c. Second, and more directly relevant to THIS claim:
    V3-EXQ-812 (2026-07-24, confirmed-autopsied INVALID_HARNESS, proximity_range exactly 0.0 in
    every seed/arm) already had its harness-repair successor V3-EXQ-812a run and applied
    (confirmed failure_autopsy_fresh-20260808-batch_2026-08-08, 2026-08-08 governance cycle) --
    GFLAG-0007's "nothing has actioned in 15 days" premise is therefore also stale. 812a's fix
    (candidate_summary_source='e2_world_forward') PARTIALLY worked: 2/3 seeds still show exact
    zero proximity_range, seed 43 shows a small nonzero value still ~5x below the non-degeneracy
    floor and essentially identical between ON/OFF arms (not cue-driven). More importantly, 812a
    exposed a SEPARATE, unconditional degeneracy: mean_mech295_liking_bias_range_mean is exactly
    0.0 in ALL 6 cells regardless of arm or whether upstream proximity showed any variance -- a
    second defect point at the downstream bias-computation stage, distinct from the upstream
    collapsed-candidate-z_world explanation the 490k-era precondition above was written against.
    This RECONFIRMS the mech295_bias_range_mean>0 precondition remains unmet, but for a
    partially-different and more specific reason than previously documented. Per 812a's own
    autopsy (user-confirmed Step 8 disposition, 2026-08-08): HOLD for code-level inspection of
    mech295_liking_bridge.py's bias-computation function before any further re-queue -- the
    490g->k->631->812->812a lineage has already cycled "fix one bug, hit another" approximately
    5 times, and MECH-295 appears in ~20 prior confirmed autopsy files, well past any brake
    threshold; treat this claim-free diagnostic as brake-territory in spirit even though the
    mechanical claim-keyed count does not apply (claim_ids=[] on both 812 and 812a). Do NOT queue
    V3-EXQ-490L (or any 812-lineage successor) until that code-level inspection has happened.
    epistemic_category stays substrate_ceiling; status stays candidate; no promotion/demotion.

    CONFIRMING (primary, narrowed-modulatory reading, once the precondition
    above is met): re-run the V3-EXQ-490k composition -- E3 score_bias
    argmin-change probe as the primary metric; action-class divergence on
    the first commit tick between bridge-on/bridge-off arms as a
    behavioural sanity check; non-MECH-295 VALENCE_WANTING writers off in
    both arms as a sensitivity-analysis ablation -- on the enriched
    substrate with a pre-registered mech295_bias_range_mean > 0 guard.
    CONFIRMS the modulatory-sufficiency reading if the per-candidate
    MECH-295 score_bias measurably changes the committed argmin (a
    pre-registered argmin_flip_ticks fraction) on >=2/3 seeds once
    bias_range is non-degenerate.

    FALSIFYING (primary, narrowed-modulatory reading): with
    mech295_bias_range_mean > 0 confirmed non-degenerate, argmin_flip_ticks
    stays at or near 0 -- i.e. MECH-295's score_bias exists and varies
    across candidates but is never large enough, relative to the other
    additively-composed channels (MECH-216, MECH-290, MECH-307,
    tonic_5ht), to change which candidate wins. This would falsify even
    the narrowed modulatory-sufficiency reading without reopening the
    already-terminally-falsified necessity reading (V3-EXQ-490j); the
    appropriate response would be a further narrowing to a
    "contributes-but-never-decisive" reading, not a fresh necessity
    retest.

    SECONDARY (strong-reading falsifier, no substrate-readiness
    precondition currently tracked -- never built or queued, available
    only should a future decision seek to REINSTATE a strong-necessity
    reading): pharmacologically-inspired block of the hedonic-hotspot
    mu-opioid analog in the V3 substrate WHILE drive is elevated. Confirming:
    approach_commit collapses (mirroring the severed-bridge signature).
    Falsifying: approach_commit recovers via the parallel pathways
    (MECH-216/290/307/tonic_5ht), exactly as V3-EXQ-490j already showed for
    the severed-wiring condition -- confirms the narrowed modulatory
    reading is the operative ceiling and no strong-necessity reinstatement
    is warranted.
  title: "The drive -> liking-stream -> approach-cue gain pathway is one of several drive->approach pathways in the REE substrate. It biases approach action scoring via a liking-specific per-candidate score_bias contribution, but is NOT the sole or necessary pathway by which drive amplification reaches approach action selection (narrowed 2026-05-31 from necessity to modulation per V3-EXQ-490j autopsy; original necessity title preserved in functional_restatement history). Parallel architecturally first-class drive->approach pathways operate in the same substrate: MECH-216 schema-readout wanting, MECH-290 backward credit sweep, MECH-307 anticipatory liking, and serotonin tonic_5ht benefit_salience."
  claim_type: mechanism_hypothesis
  subject: drive_to_approach.liking_stream_bridge
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-490k_2026-06-04
      as_of: 2026-06-04
      verdict: non_contributory/substrate_ceiling
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-04-26
  ceiling_decision: deferred
  ceiling_routing_note: "2026-06-19 substrate-ceiling-lifted triage (verdict b deliberate-defer): goal_pipeline:GAP-4 was CLOSED 2026-06-09 (necessity reading falsified by V3-EXQ-490j; modulatory reading substrate-supported by 490j/493). The substrate_ceiling/pending_retest flags are retained ONLY for the OPTIONAL, non-GAP-blocking 490L modulatory-sufficiency retest, gated on scaffolded_sd054_onboarding (collapsed-candidate/weak-z_goal), NOT on MECH-307. Do not queue a MECH-307-driven retest."
  depends_on:
    - SD-012     # homeostatic drive (the input)
    - SD-014     # wanting/liking architecture (existing dissociation)
    - SD-015     # liking-stream substrate
    - SD-016     # appetitive valence stream wiring
    - MECH-117   # wanting/liking dissociation in REE benefit_eval_head vs z_goal_latent
    - ARC-036    # hedonic hotspot anatomical substrate
  functional_restatement: >
    Mechanism hypothesis. SD-012 specifies that homeostatic drive
    amplifies goal-relevance (e.g. hunger raises drive_level on
    food-congruent goals). MECH-295 specifies the bridge: drive does
    NOT directly produce approach action selection; it must propagate
    through liking-stream activation (NAc shell hedonic hotspot,
    ventral pallidum, mu-opioid signalling, OFC pleasure coding, in
    REE: the appetitive valence stream tracking cue-evaluated hedonic
    value of expected outcomes).

    Operationally:
      drive_level (SD-012) ->
        liking_gain on goal-congruent outcome predictions ->
          approach_cue_signal at action selection (E3 / BG)

    Two readings of necessity (NOT yet arbitrated by literature):
    - Strong necessity: elevated approach_commit requires elevated
      liking-stream activation (level-coupled).
    - Weak necessity: baseline liking-stream activation is sufficient,
      but the bridge wiring must be intact -- if the link is severed,
      drive amplification produces no approach regardless of drive
      magnitude.

    The DAT-knockdown finding (Pecina 2003: more wanting, unchanged
    liking) is incompatible with strong necessity but compatible with
    weak necessity. MECH-295 commits to the WEAK reading provisionally,
    pending V3 substrate evidence.

    Falsifiable (primary, weak reading): a V3 factorial with the
    drive->liking link intact vs severed (under matched drive_level)
    should show approach_commit recovers when the bridge is intact and
    collapses when severed. EXQ-483's all-zero approach_commit signal
    is consistent with a broken bridge.

    Falsifiable (secondary, strong reading test): pharmacologically-
    inspired manipulation -- block hedonic-hotspot mu-opioid analog
    in V3 substrate WHILE drive is elevated -- under strong reading,
    approach_commit collapses; under weak reading with intact bridge,
    approach_commit recovers via baseline liking.

    Distinct from MECH-117. MECH-117 specifies the wanting/liking
    dissociation as separate streams. MECH-295 specifies the drive ->
    liking -> approach NECESSITY chain that must hold for behavioural
    output, even when wanting and liking are dissociated in level.

    NARROWED 2026-05-31 (per V3-EXQ-490j autopsy, confirmed_revised
    status). The weak-reading necessity claim above ('if the link is
    severed, drive amplification produces no approach regardless of
    drive magnitude') was falsified at the behavioural-necessity layer
    by V3-EXQ-490j: severed-bridge ARM_0 (z_goal_enabled=False, drive
    amplification active per gap4 substrate) still produced
    approach_commit_rate=1.0 in 3/3 seeds via architecturally first-
    class parallel REE drive->approach pathways operating independently
    of the SD-014/SD-015 liking-stream substrate (MECH-216 schema-
    readout wanting, MECH-290 backward credit sweep, MECH-307
    anticipatory liking, serotonin tonic_5ht benefit_salience). The
    substrate-side direct probes (V3-EXQ-493 6/6 PASS isolation +
    V3-EXQ-490j C6/C7/C9 PASS) confirm the bridge fires correctly when
    wired and is silent when severed; this is evidence FOR the narrowed-
    modulatory claim, not for the original necessity claim.

    NARROWED CLAIM (current as of 2026-05-31): The drive -> liking-
    stream -> approach-cue gain pathway biases approach action scoring
    via a liking-specific per-candidate score_bias contribution at
    action selection (E3 / BG). It is one of several architecturally
    first-class drive->approach pathways in the REE substrate, not the
    sole or necessary pathway.

    Operationally (narrowed):
      drive_level (SD-012) ->
        liking_gain on goal-congruent outcome predictions (NAc shell
          hedonic hotspot, ventral pallidum, mu-opioid signalling,
          OFC pleasure coding analogues) ->
          per-candidate approach_cue score_bias contribution at action
          selection (E3 / BG), composed additively with MECH-216
          wanting, MECH-290 backward credit, MECH-307 anticipatory
          liking, and tonic_5ht benefit_salience contributions.

    Falsifiable (primary, narrowed-modulatory reading): substrate-
    isolating test of MECH-295's per-candidate score_bias contribution.
    The cleanest mechanistic isolation is a direct E3 score_bias
    argmin-change probe: fraction of commit ticks where the bridge's
    per-candidate negative score_bias changes argmin from no-MECH-295
    to with-MECH-295 counterfactual. Behavioural sanity check: action-
    class divergence on first commit tick between bridge-on and
    bridge-off arms holding other VALENCE_WANTING write paths matched.
    Sensitivity-analysis ablation arm: non-MECH-295 VALENCE_WANTING
    writers off in both arms (clean contributory test for the
    modulatory claim; does NOT recover the original necessity claim --
    even if approach collapses in all-off ARM_0, that only shows
    MECH-295 is necessary GIVEN the others are off, not necessary in
    the full substrate). V3-EXQ-490k scheduled for this composition.

    Falsifiable (secondary, strong-reading test still available):
    pharmacologically-inspired manipulation -- block hedonic-hotspot
    mu-opioid analog in V3 substrate WHILE drive is elevated. This
    remains the experimental gap for any reinstatement of a strong-
    necessity reading.

    Biology-faithful: aligns with Berridge & Robinson 2003 (wanting/
    liking dissociation; the streams are separable not unitary) and
    Berridge & Kringelbach 2015 (hedonic architecture as one pathway
    among several). Resolves the long-standing Pecina 2003 DAT-
    knockdown tension (more wanting, unchanged liking) that troubled
    the prior weak-necessity reading.

    Distinct from MECH-117 (wanting/liking dissociation as separate
    streams) and from MECH-216/MECH-290/MECH-307/tonic_5ht (parallel
    drive->approach pathways operating via different substrates).
    MECH-295 specifies the modulatory contribution of one of these
    pathways: the drive -> liking-stream -> approach-cue gain bridge.

    RE-SCOPED + GAP CLOSED 2026-06-09 (user-approved governance
    decision; closes goal_pipeline:GAP-4). The behavioural-necessity
    reading is TERMINALLY FALSIFIED (V3-EXQ-490j, above). The committed
    MECH-295 claim is the MODULATORY reading stated above, which is
    substrate-supported (490j C6/C7/C9 + V3-EXQ-493 isolation). The
    "scheduled" V3-EXQ-490k modulatory-sufficiency argmin-flip probe
    RAN (2026-06-04) but could not exercise the reading: argmin_flip=0
    because mech295_bias_range_mean=0.0 BY CONSTRUCTION, defeated by the
    collapsed-candidate-pool / weak-z_goal substrate ceiling owned by
    scaffolded_sd054_onboarding (NOT by MECH-295 / GAP-4). The remaining
    behavioural-sufficiency test (a V3-EXQ-490L on enriched substrate
    with a pre-registered mech295_bias_range_mean>0 guard) is retained
    as an OPTIONAL, NON-GAP-BLOCKING future test, tracked via
    epistemic_category=substrate_ceiling + pending_retest_after_substrate.
    MECH-295 stays status=candidate (no promotion: substrate-firing
    support is not behavioural validation).
  notes: >
    Registered 2026-04-26 from the architectural conversation around
    EXQ-483's catatonic-lock failure (override fires, PAG releases up,
    approach_commit = 0.0 across all arms). Daniel raised the question:
    "does the hungry helps the goal seed for the liking stream to lead
    to approach behaviour being cued" -- which is MECH-295 in question
    form.

    Lit-pull: targeted_review_mech295_liking_approach_bridge/
    (6 entries; mean conf 0.77; 5 supports, 1 mixed). Strongest direct
    evidence: Smith Berridge & Aldridge 2011 (PNAS, VP single-unit
    recording: drive change recodes palatability before cue firing).
    Foundational behavioural version: Dickinson & Balleine 1994
    (instrumental devaluation requires outcome re-experience).
    Architectural articulation: Berridge & Kringelbach 2015 (Neuron).

    Strong-vs-weak necessity is flagged for user resolution. The
    cleanest direct test (mu-opioid antagonist in NAc shell hotspot
    + hunger + cue-approach) is an experimental gap.
  implementation_note: >
    V3 substrate IMPLEMENTED 2026-04-26 in ree-v3/ree_core/regulators/
    mech295_liking_bridge.py (MECH295LikingBridge,
    MECH295LikingBridgeConfig, MECH295LikingBridgeOutput). Master flag
    REEConfig.use_mech295_liking_bridge (default False). Sub-knobs:
    mech295_drive_to_liking_gain (default 1.0), mech295_liking_to_approach_cue_gain
    (default 0.5), mech295_min_drive_to_fire (default 0.1),
    mech295_min_z_goal_norm_to_fire (default 0.05). Wired in agent.py at
    two integration sites: (a) update_z_goal() writes an anticipatory
    liking pulse to VALENCE_LIKING at the goal location (NOT the agent's
    current location -- this is the cue-side anticipatory pulse, distinct
    from update_liking() consummatory write), gated on
    drive_level * z_goal_norm * drive_to_liking_gain; (b) select_action()
    reads per-candidate goal_proximity, computes drive *
    proximity * liking_to_approach_cue_gain, negates (E3 lower-is-better)
    and composes additively with dacc_score_bias before e3.select().
    Backward compatible (154/154 contracts + 7/7 preflight PASS with
    flag OFF, bit-identical to pre-MECH-295 HEAD). Severed-bridge arm
    (mech295_liking_to_approach_cue_gain=0.0) is the falsifiable test
    for the weak-necessity reading: drive elevated AND write side
    intact AND cue side severed -> approach_commit predicted to
    collapse. V3-EXQ-493 queued (6-part substrate-landing diagnostic
    including UC5 severed-bridge collapse signature). Behavioural
    EXQ-483-style 4-arm approach_commit recovery deferred to a successor
    once V3-EXQ-490 (MECH-269b) lands and the combined V_s + bridge
    stack can be tested end-to-end. See
    docs/architecture/mech_295_drive_liking_approach_bridge.md and
    ree-v3/CLAUDE.md "MECH-295 Drive -> Liking-Stream -> Approach Cue
    Bridge (2026-04-26)".
  location: docs/architecture/control_plane_heartbeat.md

- id: SD-040
  title: "Type-encoder produces a category-prototype feature vector that is written into the AnchorSet alongside z_world, so anchors can be queried by type-key as well as location-key. The type-encoder uses the same input substrate as the existing instance-encoder but produces a different (regularity-extracted) representation, mirroring the trisynaptic-vs-monosynaptic complementary-learning architecture within hippocampus."
  claim_type: design_decision
  subject: hippocampal.type_encoder_anchor_input_projection
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  v3_pending: false
  claim_level: architectural
  registered_utc: 2026-04-28
  depends_on:
    - MECH-269   # AnchorSet substrate being extended with new input projection
    - SD-039     # anchor payload schema that this projection populates a new field of
  epistemic_category: substrate_conditional
  what_would_answer: >
    NON-DEGENERACY PRECONDITION (unmet): requires BOTH the type-encoder
    substrate (learned projection writing a type-key into AnchorSet entries,
    plus MECH-296's prototype-readout to consume it) AND a multi-instance-per-
    type environment. Neither exists -- zero type_encoder/type_key/
    prototype_readout hits in ree_core; Anchor carries only (scale,
    segment_id, stream_mixture) key, z_world, and SD-039's goal_payload.
    MECH-296 is itself candidate/v4/unbuilt. V3's gridworld has discrete
    entity types with no instance-variability by design -- a test-bed
    limitation, not just missing code.

    CONFIRMING: in a multi-instance environment, type-keyed AnchorSet queries
    retrieve ALL instances of a type while location-keyed queries retrieve
    only the specific instance.

    FALSIFYING: with the precondition met, type-keyed queries fail to
    generalize across same-type instances, or perform no differently than
    location-keyed queries.

    (Digested 2026-08-07, session cool-torvalds-a82359, thought-digestion
    wave 11.)
  functional_restatement: >
    Concept cells in MTL hold instance-invariant abstract identity codes
    (Quiroga 2005). Statistical-regularity learning happens via the
    monosynaptic EC->CA1 pathway in HPC, distinct from the trisynaptic
    DG->CA3->CA1 episode-encoding pathway (Schapiro et al. 2017 PTRSB).
    Conceptual relations are encoded by the same gridlike machinery as
    spatial relations (Constantinescu, O'Reilly & Behrens 2016 Science).

    SD-040 adds a type-encoder substrate to REE: a learned projection
    from LatentState (or a category-relevant subset of streams) onto a
    type-key feature vector that gets written alongside z_world into
    AnchorSet entries. Anchors with similar type-keys constitute an
    implicit category cluster; the prototype-readout operator (MECH-296)
    consumes this clustering at retrieval time. The encoder shares input
    with the existing instance-encoder but produces a regularity-
    extracted output, mirroring the biological complementary-learning
    architecture.

    Falsifiable: in a multi-instance environment (multiple distinct
    hazard locations sharing a hazard-type signature), type-keyed
    AnchorSet queries should retrieve all instances of the type while
    location-keyed queries retrieve only the specific instance.
  notes: >
    Registered 2026-04-28 from the type-prototype lit-pull
    (`evidence/literature/targeted_review_hpc_type_prototype_substrate/`).
    V4-scope by default because V3's gridworld with discrete entity
    types does not have enough instance-variability to surface the
    type-vs-instance dissociation as a behavioural signature.
  evidence_quality_note: >
    Registered candidate with literature support from
    `targeted_review_hpc_type_prototype_substrate/` (Quiroga 2005,
    Schapiro 2017 Phil Trans, Schapiro 2016 Hippocampus, Constantinescu
    2016, Hennies 2017). No substrate implementation yet.
  location: docs/thoughts/2026-04-28_action_object_type_abstraction.md

- id: SD-041
  title: "An explicit thalamic-routing substrate (reuniens/MD-analogue) gates and amplifies bidirectional traffic between hippocampal anchor pool and frontal substrates. Currently abstracted into the GoalState->HippocampalModule call chain; making it explicit is needed when V3 dynamics demonstrate that the implicit routing is insufficient."
  claim_type: design_decision
  subject: thalamic.routing_substrate_hpc_pfc_gate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  v3_pending: false
  claim_level: architectural
  registered_utc: 2026-04-28
  depends_on:
    - SD-033     # PFC subdivision cluster (downstream of thalamic routing)
    - MECH-269   # hippocampal anchor pool (upstream of thalamic routing)
  functional_restatement: >
    Mediodorsal thalamus amplifies local PFC connectivity to sustain
    rule representations without relaying categorical content (Schmitt
    et al. 2017 Nature). Reuniens of ventral midline thalamus orchestrates
    HPC-PFC synchrony specifically during spatial working memory demand
    (Hallock et al. 2016 J Neurosci) and routes prefrontal goal-context
    into hippocampal CA1 to shape trajectory representations (Ito et al.
    2015 Nature).

    SD-041 adds an explicit thalamic-routing substrate to REE: a node
    where "PFC sends compact handle, thalamus gates and amplifies, HPC
    receives" is architecturally explicit, with demand-conditional gating
    on cognitive operation type. Currently REE collapses this into the
    GoalState->HippocampalModule call chain (z_goal as the routed signal),
    which is biologically functional but lossy.

    Falsifiable: with the substrate added, HPC anchor lookup latency
    should be demand-conditional (faster during goal-active states,
    slower during quiescence). Without the substrate, lookup latency is
    flat across cognitive demand.
  notes: >
    Registered 2026-04-28 from the frontal_goal_grounding lit-pull. V4-
    scope because V3's existing GoalState->HippocampalModule path is
    biologically functional even if architecturally lossy; the explicit
    substrate becomes load-bearing in V4 with richer task structure
    where multi-loop arbitration matters.
  evidence_quality_note: >
    Registered candidate with literature support from
    `targeted_review_frontal_goal_grounding/` (Schmitt 2017, Hallock 2016,
    Ito 2015). No substrate implementation yet.
  location: docs/thoughts/2026-04-28_action_object_type_abstraction.md

- id: SD-042
  title: "Option library substrate stores a discrete codebook of named, reusable action subroutines (each as initiation-set + termination-function + internal-policy triple, per Sutton-Precup-Singh 1999), distinct from the existing continuous SD-004 action_object_decoder. Required for hierarchical action composition, tool use, social-coordination protocols, and richer ghost-probe seeding."
  claim_type: design_decision
  subject: action.option_library_hierarchical_subroutines
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  v3_pending: false
  claim_level: architectural
  registered_utc: 2026-04-28
  depends_on:
    - SD-004     # continuous action_object substrate that the option library refines
    - ARC-021    # three BG-like loop framework where option arbitration sits
    - MECH-292   # ghost-goal bank consumer that benefits from option-keyed retrieval
  functional_restatement: >
    The hierarchical structure of behaviour - dividing into tasks,
    subtask sequences, and atomic actions - is biologically captured by
    options (Sutton, Precup & Singh 1999). PFC supports active maintenance
    of the option being executed; option components map onto dorsolateral
    and orbital PFC functional architecture (Botvinick, Niv & Barto 2009
    Cognition).

    SD-042 adds an option library substrate to REE: a learned discrete
    codebook of named options each with (a) an initiation set
    specifying which states the option can start from, (b) a termination
    function specifying when the option ends, and (c) an internal policy
    specifying what to do during execution. This is distinct from the
    existing SD-004 action_object_decoder, which is a continuous decoder
    over a fixed dimension rather than an indexable library. With an
    option library, MECH-293 ghost-goal probes can additionally be
    seeded by option-initiation-match.

    Falsifiable: in a hierarchical task with reusable subroutines (e.g.
    "approach-resource" reused across multiple resource types), a V3
    agent without an option library re-rolls the subroutine at every
    invocation through E2+CEM, while a V4 agent with the option library
    invokes the cached subroutine and saves planning latency.
  notes: >
    Registered 2026-04-28 from the action-policy decomposition lit-pull.
    Definitively V4-scope: gridworld too simple to validate options;
    requires V4 environment with tool use, social coordination, or
    hierarchical task structure.
  evidence_quality_note: >
    Registered candidate with literature support from
    `targeted_review_action_policy_decomposition/` (Botvinick 2009 +
    surrounding hierarchical-RL literature). No substrate implementation
    yet.
  location: docs/thoughts/2026-04-28_action_policy_and_multi_goal.md

- id: SD-043
  title: "vmPFC analogue gains an abstract task-structure encoding capacity that compresses anchor-pool geometry into a generalisable structural code, preserving similarity geometry across surface-different but structurally-matched problems. Without this, REE cannot exhibit Baram et al. 2020 task-structure abstraction signatures."
  claim_type: design_decision
  subject: pfc.vmpfc_abstract_task_structure_encoding
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  v3_pending: false
  claim_level: architectural
  registered_utc: 2026-04-28
  depends_on:
    - SD-033     # PFC subdivision cluster (vmPFC subdivision being extended)
    - ARC-035    # vmPFC content interface (substrate gaining new encoding capacity)
    - MECH-292   # anchor pool whose geometry the encoding compresses
  functional_restatement: >
    vmPFC and entorhinal cortex preserve representational geometry
    across surface-different RL problems if and only if task structure
    is preserved (Baram, Muller, Nili, Garvert & Behrens 2020 Neuron).
    The rich content vmPFC carries is task-graph topology, not "the goal
    as a feature vector". This is a generalisable abstract structural
    representation.

    SD-043 adds an abstract task-structure encoding capacity to the
    vmPFC analogue: the substrate compresses MECH-292 anchor-pool
    geometry (which anchors are nearby in stream-mixture space, which
    transitions are common, which valence patterns recur) into a
    structural code that survives across structurally-matched but
    surface-different episodes. Without this, the vmPFC analogue can
    only hold instance-keyed value information, not relational structure.

    Falsifiable: V4 generalisation test - train on one task structure,
    swap surface stimuli but preserve structure, measure transfer.
    With SD-043, transfer should be near-immediate. Without it,
    transfer requires re-learning from scratch.
  notes: >
    Registered 2026-04-28 from the frontal_goal_grounding lit-pull.
    Definitively V4-scope: V3's grid-world has fixed task structure
    across episodes, so the structural-generalisation behavioural
    signature is not testable in V3.
  evidence_quality_note: >
    Registered candidate with literature support from
    `targeted_review_frontal_goal_grounding/` (Baram 2020, Constantinescu
    2016 corroborates with conceptual-grid evidence in EC). No substrate
    implementation yet.
  location: docs/thoughts/2026-04-28_action_object_type_abstraction.md

- id: SD-044
  title: "Motor primitive substrate at the bottom of the action-representation hierarchy: a small fixed dictionary of force-field-like primitive activations that vectorially combine to produce the action_object that E2 consumes. Required only when REE moves to continuous limb-like control."
  claim_type: design_decision
  subject: motor.primitive_substrate_bottom_of_action_stack
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  v3_pending: false
  claim_level: architectural
  registered_utc: 2026-04-28
  depends_on:
    - SD-004     # action-object substrate above motor primitives in the stack
  functional_restatement: >
    The CNS organises spinal premotor circuits into discrete force-field
    modules whose vectorial combination produces a rich grammar of
    motor behaviours (Mussa-Ivaldi & Bizzi 2000 PTRSB).

    SD-044 adds a motor-primitive substrate to REE that lands BELOW
    SD-004 in the action-representation stack: a learned (or fixed)
    dictionary of primitive activations, each of which contributes a
    direction in continuous action space; the action_object that E2
    consumes is produced as a vectorial combination of active primitives
    rather than emitted directly by the action_object_decoder.

    Falsifiable: this substrate is environmentally not testable in V3
    (4 discrete actions). In V4 with continuous control, ablating the
    primitive layer should produce coarser motor output and reduced
    transfer across motor tasks.
  notes: >
    Registered 2026-04-28 from the action-policy decomposition lit-pull.
    Definitively V4-scope: requires V4 environment with continuous
    action space. Not warranted for V3's discrete-grid gridworld.
  evidence_quality_note: >
    Registered candidate with literature support from
    `targeted_review_action_policy_decomposition/` (Mussa-Ivaldi & Bizzi
    2000). No substrate implementation yet.
  location: docs/thoughts/2026-04-28_action_policy_and_multi_goal.md

- id: SD-045
  title: "Action-chunk cache stores frequently-traversed action sequences as cached lookups, indexed by (state-signature, expected-outcome-signature). Sits in the dorsolateral-loop slot of ARC-021. Provides the model-free habit pathway missing from V3 and the runaway-chunking failure mode underlying OCD compulsions and rituals (Graybiel 2008)."
  claim_type: design_decision
  subject: bg.action_chunk_cache_dorsolateral_loop
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  v3_pending: false
  claim_level: architectural
  registered_utc: 2026-04-28
  depends_on:
    - ARC-021    # three BG-like loop framework where chunk cache lives in DLS slot
    - SD-004     # action-object substrate that chunks are sequences of
    - MECH-290   # backward credit sweep that updates chunk reinforcement
  functional_restatement: >
    Striatal task-bracketing cells fire phasically at the start and end
    of well-learned action sequences, treating the intervening sequence
    as a single unit of behavioural control (Graybiel 2008 Annu Rev
    Neurosci). The brain arbitrates between model-based (PFC + DMS) and
    model-free (DLS) control by relative uncertainty (Daw, Niv & Dayan
    2005 Nat Neurosci). The dichotomy is better understood as a spectrum
    with shared circuitry and rich interactions than as fully separate
    subsystems (Dolan & Dayan 2013 Neuron).

    SD-045 adds an action-chunk cache substrate to REE: a small
    dictionary of frequently-traversed action sequences indexed by
    start-state signature and expected-outcome signature, executable as
    a unit without re-rolling through E2 + CEM. Updated by reinforcement
    from MECH-290 backward credit sweep on completed committed
    trajectories. Gated by task-bracketing-analogue start/end signals.
    Lives in the dorsolateral-loop slot of ARC-021. Per Dolan-Dayan
    spectrum framing, integrates WITH the existing planner -
    HippocampalModule.propose_trajectories or E3 selector queries the
    cache before falling through to CEM rollout - rather than as a
    parallel competing controller.

    Falsifiable: in repeated-route foraging tasks, post-training
    trajectories should show shorter planning latency than first-
    exposure trajectories of the same length. In OCD-modelling tasks,
    runaway chunking should produce compulsive ritual-like trajectory
    repetition that the goal-release machinery (SD-034 closure
    operator) fails to terminate.
  notes: >
    Registered 2026-04-28 from the action-policy decomposition lit-pull.
    Default V4-scope, with V3 PULL-FORWARD CONDITION: if EXQ-495 V3-
    full-completion-gate or successor experiments surface monostrategy
    persistence that the existing planner-only architecture cannot
    explain or escape, this is the highest-priority architectural
    extension to pull forward into late-V3. The connection runs through
    the hypothesis that some part of monostrategy may be a missing-habit
    -cache issue (every action goes through model-based planning, so the
    planner's mono-bias is the only contributor to behavioural rigidity)
    rather than purely a goal-substrate issue. Pull-forward is also
    relevant if SD-033 OCD modelling work surfaces a need for the
    compulsive-ritual failure mode beyond what the goal-release machinery
    in SD-034 can express.
  evidence_quality_note: >
    Registered candidate with literature support from
    `targeted_review_action_policy_decomposition/` (Graybiel 2008,
    Daw 2005, Dolan & Dayan 2013, Botvinick 2009). No substrate
    implementation yet.
  location: docs/thoughts/2026-04-28_action_policy_and_multi_goal.md

- id: SD-046
  title: "Multi-slot GoalState holds N>=2 simultaneously-active goal slots with per-slot z_goal, drive coupling, age, and persistence parameters. Each slot generates its own ghost-goal-bank rank, ghost-probe budget, and proposer trajectories; a dACC-style arbitrator selects which slot's best trajectory commits this tick."
  claim_type: design_decision
  subject: goal.multi_slot_state_per_goal_workstream
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  v3_pending: false
  claim_level: architectural
  registered_utc: 2026-04-28
  depends_on:
    - SD-012     # homeostatic drive coupling that becomes per-slot
    - SD-015     # z_resource encoder upstream of GoalState seeding
    - MECH-292   # ranked ghost-goal bank that becomes per-slot
    - MECH-293   # waking ghost-goal probes that become per-slot
    - SD-032b    # dACC-style arbitration for selecting between active slots
  functional_restatement: >
    REE V3's GoalState is a singleton: one z_goal at a time. Multi-goal
    parallel maintenance - holding competing goals simultaneously and
    arbitrating between them tick-by-tick - is structurally impossible
    in V3. This is itself a contributor to monostrategy lock-in
    (single-goal architecture forces single behavioural strategy) and
    to the inability to express OCD-style over-binding pathology in its
    canonical "competing-goals failure mode" form (the agent cannot get
    stuck because there is nothing to switch away to).

    SD-046 adds multi-slot GoalState: a small set of active goal slots
    (n=2-4 plausibly), each with its own z_goal, drive coupling, age,
    persistence parameters. Each slot generates its own MECH-292 rank
    against the anchor pool, its own MECH-293 ghost-probe budget, and
    its own proposer trajectories. A dACC-style arbitrator (extension
    of SD-032b's bundle composition) selects which active slot's best
    trajectory wins this commitment cycle, governed by something like
    the Daw 2005 uncertainty-based arbitrator extended to multi-option
    selection.

    Falsifiable: in a dual-resource task where the agent must alternate
    between two resource patches with different optimal routes, single-
    slot architecture forces sequential goal-switching with full
    re-planning each switch. Multi-slot architecture maintains both
    representations and exhibits faster between-switch latency plus
    better between-switch route stability. In OCD-modelling tasks,
    multi-slot architecture allows expression of the over-binding failure
    mode where one slot's persistence prevents another slot from gaining
    arbitration.
  notes: >
    Registered 2026-04-28 from the conversation that produced
    `docs/thoughts/2026-04-28_action_policy_and_multi_goal.md`. Default
    V4-scope, with V3 PULL-FORWARD CONDITION identical to SD-045: if
    monostrategy escape requires it, this is the next-highest-priority
    pull-forward candidate. Connects to MECH-266 asymmetric mode
    hysteresis (which currently operates per-mode; multi-goal would
    require a parallel per-goal hysteresis dimension).

    DEFINITIVE V3 PULL-FORWARD TRIGGER (added 2026-04-28): SD-046
    becomes definitively V3-scope -- not V4-default with conditional
    pull-forward, but V3-required -- if BOTH of the following Layer-1
    and Layer-2 OCD-derived monostrategy diagnostics return FAIL:
    (1) V3-EXQ-498 (queued 2026-04-28; OCD Layer 1 closure-threshold
    sweep) FAILs to attenuate monostrategy via SD-034 parameter tuning
    alone, AND (2) EXP-0169 (OCD Layer 2 MECH-290 backward-credit-sweep
    ablation; gated pending V3-EXQ-498 result, registered in
    `evidence/planning/manual_proposals.v1.json` 2026-04-28) FAILs to
    attenuate monostrategy via residue-self-reinforcement ablation.
    Under the OCD three-layer architectural model
    (REE_assembly/docs/architecture/psychiatric_failure_modes.md
    2026-04-28 OCD section), Layer 1 (SD-034 closure threshold) and
    Layer 2 (MECH-290 chunk-runaway analogue) being ruled out leaves
    Layer 3 (SD-046 multi-slot GoalState absent) as the architectural
    explanation by elimination -- the failure mode is structural
    (single-slot GoalState forces single strategy), not parametric.
    At that point, V3 cannot demonstrate monostrategy escape under
    its current substrate vocabulary and SD-046 must land in V3 to
    unblock V3-EXQ-495 V3-full-completion-gate successors and the
    SD-029 retest cluster. The lit-pull on multi-goal arbitration
    (anchor candidates listed in evidence_quality_note below) should
    be commissioned in advance of the V3-EXQ-498 + EXP-0169 outcome
    so the substrate design can begin immediately on the FAIL+FAIL
    branch without further gating.

    GDL-1 FORK RESOLVED 2026-06-14 (IGW goal_deliberation_v4:GDL-1
    interactive design-fork session): the MULTI-SLOT answer is ADOPTED.
    V4 widens GoalState to N>=2 simultaneously-active slots (this claim
    as written) rather than staying single-slot with only a
    counterfactual-value monitor over the one committed stream.
    Rationale: the deliberation layer's load-bearing capabilities --
    cross-goal relative-importance monitoring (MECH-265), the
    interrupt->park->resume span of an interrupted task (GDL-5 Zeigarnik),
    and the multi-goal OCD over-binding failure mode -- are structurally
    multi-slot; a single-alternative monitor (Boorman 2009
    chosen-vs-best-unchosen) cannot host a parked second goal or
    arbitrate across more than two goals. This is the DESIGN decision
    only; the BUILD stays gated on goal_pipeline GAP-2 (the single stream
    must reliably commit-and-contact ONE goal first -- a multi-slot
    arbitrator over a pipeline that cannot commit one goal is vacuous).
    The discrete-slot (Cowan) vs flexible-precision-resource
    (Bays & Husain) SUB-fork is NOT resolved here -- it stays open as
    Q-077 (what_would_answer registered). The dACC<->FPC reciprocal
    arbitration loop (MECH-432) and the LC-NE-analog disengage/explore
    partner (MECH-433) are the partner machinery this fork commits to
    building. PROMOTES NOTHING: SD-046 stays candidate /
    implementation_phase v4 / substrate_conditional.
  evidence_quality_note: >
    Registered candidate without a dedicated lit-pull. Architectural
    motivation comes from monostrategy debugging in V3 plus the OCD over
    -binding framing in `docs/thoughts/2026-04-20_ocd4.md`. A targeted
    lit-pull on multi-goal arbitration in PFC + dACC + cingulate-
    cortex circuits is recommended before V3 pull-forward (anchor
    candidates: Mansouri et al. 2017 NRN competing goals; Kolling et al.
    2012 dACC foraging value; Shenhav et al. 2013 EVC).
  location: docs/thoughts/2026-04-28_action_policy_and_multi_goal.md

- id: SD-047
  title: "Multi-source environmental dynamics: concurrent stochastic event sources at distinct spatial and temporal scales (spatial perturbation field + transient Poisson events + background drift sources) provide an agent-independent causal background dense enough for agency-detection comparators (MECH-095) and reafference cancellation (MECH-098) to be honestly tested at substrate level."
  claim_type: design_decision
  subject: environment.multi_source_dynamics
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_509_sd047_multi_source_substrate_readiness_20260503T103241Z_v3
      verdict: supports/PASS
  implementation_phase: v3
  claim_level: architectural
  registered_utc: 2026-05-03
  implemented_utc: 2026-05-03
  depends_on:
    - SD-022       # body-damage substrate that SD-047 layers on top of
    - SD-029       # scheduled external hazard substrate (SD-047 adds unscheduled sources alongside)
  functional_restatement: >
    CausalGridWorldV3's "other-caused" change consists of hazard drift
    + resource respawn + scheduled hazard injection. This is too thin a
    causal background for agency-detection-comparator claims (MECH-095)
    or reafference-cancellation circuits (MECH-098) to be tested at
    substrate-level honesty: the agent's counterfactual signature only
    separates from environmental noise when there is environmental
    noise. Narrow regimes can pass (V3-EXQ-047k contact-recall window)
    while the broader landscape fails (V3-EXQ-506 agent-vs-env / agent-
    vs-collateral / env-vs-correlated all collapse on 2026-05-03).

    SD-047 adds three concurrent stochastic event sources to the env:
    a slowly-evolving spatial perturbation field (continuous, smooth,
    autocorrelated), a low-rate transient Poisson process (discrete,
    pointwise, short-lived), and a small set of background drift
    sources (discrete, mobile, autocorrelated). Each operates at a
    distinct spatial / temporal scale so the comparator must learn
    structural agent-vs-env signatures rather than a single filterable
    noise pattern.

    Falsifiable: a 4-arm noise-level sweep (OFF / 0.25x / 1.0x / 4.0x)
    should reproduce V3-EXQ-506 C1-C3 FAIL pattern in the OFF arm and
    show an inverted-U (Asai 2016) across noise arms, peaking near ARM_2
    default calibration. Woo/Spelke falsifier: if flat-failure across all
    arms, route MECH-095 from substrate_ceiling to substrate_conditional.
  notes: >
    Registered 2026-05-03 from the substrate-ceiling walk that followed
    governance walk 2026-05-03. Direct trigger: V3-EXQ-506 (2026-05-03)
    confirmed the substrate-ceiling signature flagged in MECH-095's
    evidence_quality_note (2026-05-02). Roadmap entry: substrate_roadmap.md
    M-priority #4 (`environment.multi_source_dynamics`).

    IMPLEMENTED 2026-05-03 in ree_core/environment/causal_grid_world.py.
    Flat kwargs on CausalGridWorld.__init__ (matching SD-022/SD-023/SD-029
    precedent). Bit-identical OFF (RNG draws guarded inside master switch);
    per-source switches for ablation; multi_source_intensity_scale is the
    4-arm sweep lever. 7/7 preflight + 184/184 contracts PASS with master
    OFF. Smoke at scale=1.0: calibration ratio 1.95:1 within target band.
    Substrate-readiness diagnostic queued as V3-EXQ-509; comparator-gap
    behavioural validation (V3-EXQ-510 successor to V3-EXQ-506) pending
    V3-EXQ-509 PASS.

    Distinct from V4 multi-agent ecology (V4-1 in v4_spec.md). SD-047
    provides non-trivial but non-intentional env causation -- exactly
    the right level for agency-detection-comparator claims. Agent vs
    other-agent is a separate V4-bound claim.

    Pre-implementation lit-pull completed (targeted_review_sd_047/):
    Asai 2016 (conf=0.83, supports non-monotonic noise-dependence
    protocol revision); Sawtell 2010 (conf=0.80, cerebellum reafference);
    Pitcher & Ungerleider 2021 (conf=0.78, lateral cortex pathway);
    Davidson & Wolpert 2005 (conf=0.70, forward-model scaffold); Woo/
    Liu/Spelke 2023 (conf=0.65, falsifier branch). lit_conf=0.796.

    2026-08-10 edge correction (chip chip-20260810-phase-consistency-reconciliation):
    depends_on: MECH-095 removed -- REVERSED edge. Per this claim's OWN original inline
    comment ("the primary substrate_ceiling claim this UNBLOCKS"), SD-047 is the
    prerequisite substrate that unblocks testing MECH-095, not a dependent of it. SD-047
    was built and validated (7/7 preflight, 184/184 contracts, V3-EXQ-509 PASS)
    independent of MECH-095's phase status.
  evidence_quality_note: >
    Substrate IMPLEMENTED 2026-05-03 (ree-v3 commit forthcoming;
    ree_core/environment/causal_grid_world.py).
    Three concurrent stochastic sources (AR(1) weather field + Poisson
    transient hazards + drift sources) added as bit-identical-OFF kwargs
    on CausalGridWorld. multi_source_intensity_scale knob is the 4-arm
    noise-sweep lever (OFF / 0.25x / 1.0x / 4.0x). 7/7 preflight + 184/184
    contracts PASS with master OFF. Activation smoke (200 ticks, all three
    sources ON, scale=1.0): env_events=330 / agent_events=169, calibration
    ratio 1.95:1 -- within the 1:1-2:1 target band specified in the SD doc.

    Validation pending: V3-EXQ-509 (substrate-readiness diagnostic; queued)
    -> V3-EXQ-510 (4-arm sweep with V3-EXQ-506-equivalent C1-C3 metrics;
    pre-registered inverted-U prediction per Asai 2016 non-monotonic agency
    S/N + Woo & Spelke 2023 falsifier branch). MECH-095 substrate_ceiling
    flag will be lifted on ARM_2 PASS; routes to substrate_conditional on
    flat-failure (Woo/Spelke branch).

    Pre-implementation lit-pull complete 2026-05-03: 18 PubMed entries
    (5 anchor + 13 passivity-cluster follow-up). lit_conf=0.841. Anchor
    candidates substituted at pull time -- Saxe & Carey 2006 not directly
    available, used Woo, Liu & Spelke 2023 (10.1111/desc.13453) as
    Spelke-lab modern equivalent + falsifier; Blakemore et al. 1998 not
    surfaced cleanly, used Sawtell 2010 (10.1016/j.neuron.2010.10.027) +
    Davidson & Wolpert 2005 (10.1088/1741-2560/2/3/S03) as the cerebellar-
    cancellation pillar. Asai 2016 (10.1016/j.psychres.2016.10.082) is the
    load-bearing entry (S/N slope analysis; non-monotonic schizotypal
    finding directly motivates the 4-arm sweep over a binary ON/OFF test).
    Pitcher & Ungerleider 2021 found as 33334693 (the TICS review).

    Implementation choice deviations from the SD doc: flat kwargs on
    CausalGridWorld.__init__ (matches SD-022 / SD-023 / SD-029 precedent
    for env-only SDs) rather than nested MultiSourceDynamicsConfig
    dataclass; transient + drift hazards live in self.hazards with
    parallel bookkeeping lists (rather than getting a new ENTITY_TYPES
    entry that would change NUM_ENTITY_TYPES from 7 to 8 and break
    local_view shape). Per-source bit-identical OFF preserved.
  location: docs/architecture/sd_047_multi_source_dynamics.md
  implementation_note: |
    SD-047 substrate IMPLEMENTED 2026-05-03 (ree-v3, ree_core/environment/
    causal_grid_world.py). Bit-identical OFF; 4-arm noise-sweep lever via
    multi_source_intensity_scale kwarg; per-source switches for ablation.
    See ree-v3/CLAUDE.md "SD-047: Multi-Source Environmental Dynamics
    (2026-05-03)" for the full implementation entry. Validation experiment
    V3-EXQ-509 queued.

- id: SD-048
  title: "Interoceptive noise dynamics: stochastic body-state perturbations independent of the agent's motor output (autonomic background noise + sensitisation spikes + fatigue drift) provide an agent-independent interoceptive background dense enough for the affective comparator (ARC-058, Level 2 of the reafference comparator family) to be honestly tested at substrate level."
  claim_type: design_decision
  subject: body.interoceptive_noise_dynamics
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:SD-048@2026-05-07T04:14:02Z"
      as_of: 2026-05-07
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  epistemic_category: standard
  claim_level: architectural
  registered_utc: "2026-05-03"
  depends_on:
    - ARC-058    # primary Level 2 comparator claim this unblocks
    - SD-011     # dual nociceptive streams — z_harm_a must exist
    - SD-022     # directional limb damage — agent-caused body-state variance must exist
  functional_restatement: >
    After SD-022, z_harm_a changes only via agent-caused limb damage
    (hazard contact) and deterministic healing. There is no independent
    body-state background: all z_harm_a variance is agent-caused, so
    a trivial forward model passes by memorising the action-damage
    coupling rather than by learning to separate self-caused from
    body-noise-caused change. ARC-058's HarmForwardTrunk claim (and
    ARC-033 alternative) cannot be honestly tested on this substrate.

    SD-048 adds three concurrent stochastic body-state sources that are
    NOT caused by the agent's motor output:
    (1) Autonomic background: fast i.i.d. Gaussian noise on harm_obs_a
    every step (HRV / sympathetic-fluctuation analog).
    (2) Sensitisation spikes: Poisson-distributed transient multiplicative
    amplification of harm_obs_a (inflammatory sensitisation analog).
    (3) Fatigue drift: slow AR(1) latent fatigue state additively
    contributing to harm_obs_a across the episode (allostatic load analog).

    With these sources active, the interoceptive forward model must learn
    to distinguish agent-caused body-state change from body-noise-caused
    change -- the Level 2 reafference-cancellation problem. Validation
    uses the same 4-arm noise-level sweep protocol as SD-047 (per Asai
    2016 non-monotonic comparator competence): ARM_0 (OFF) through
    ARM_3 (4x), with predicted inverted-U peaking at ARM_2.
    Flat-failure across all arms routes ARC-058 from V3-tractable to
    substrate_conditional (richer body-state substrate needed).
  notes: >
    Registered 2026-05-03. Level 2 counterpart to SD-047 (Level 1
    environmental dynamics). Same three-source design rationale: diverse
    temporal/statistical characters force structural learning rather than
    single-pattern filtering. Sources map to biological timescales:
    autonomic (step-level), sensitisation (episode-phase), fatigue
    (episode-length).

    Independently implementable from SD-047. Can run in parallel.
    Cross-level interaction (SD-047 + SD-048 joint) deferred to a
    later experiment -- the individual Level 1 and Level 2 calibrations
    must be validated first.

    Bit-identical OFF requirement (same as SD-047): when master switch
    disabled, body-state update path is identical to pre-SD-048 baseline.
    Per-source ablation switches for individual source contribution
    experiments.

    See `docs/architecture/reafference_comparator_family.md` for the
    full Level 2 context and the diagnostic principles shared with
    Level 1 (SD-047) and Level 3 (MECH-094).
  implemented_utc: "2026-05-03"
  evidence_quality_note: >
    Substrate IMPLEMENTED 2026-05-03 in
    ree-v3/ree_core/environment/causal_grid_world.py
    (CausalGridWorldV2). Three concurrent agent-independent stochastic
    body-state noise sources applied to harm_obs_a readout:
    autonomic (per-step Gaussian, default sigma=0.02), sensitisation
    (Poisson onset, multiplicative amplification, exponential decay,
    default rate=0.008/step / magnitude=1.8x / halflife=15 steps,
    cumulative cap 5.0x), fatigue (AR(1) latent state, additive
    contribution, default ar_coeff=0.995 / contribution_weight=0.15,
    resets per episode). interoceptive_noise_scale (default 1.0) is
    the 4-arm sweep lever per the SD doc validation protocol.
    interoceptive_change_threshold (default 0.01) defines the |delta_
    harm_obs_a| floor for body-noise vs agent-caused event counters.
    Bit-identical OFF when interoceptive_noise_enabled=False (default);
    per-source ablation switches all default True under master.
    Implementation choices (deviations from SD doc): flat kwargs on
    __init__ matching SD-047 / SD-022 / SD-029 precedent; readout-side
    perturbation rather than upstream limb_damage state modification
    (biologically correct, avoids interaction with SD-022
    failure_prob_scale); cumulative sensitisation amplification capped
    at 5.0x for numerical stability. See ree-v3/CLAUDE.md SD-048
    section for the full implementation note.
    Activation smoke (4-arm sweep, 200 ticks, 8x8 random-policy):
    ARM_0 OFF zero counters (bit-identical OFF guarantee); ARM_1
    n_body_noise=7; ARM_2 n_aut=176 + n_sens=1 + fatigue ~+/-0.04;
    ARM_3 n_aut=200 saturated + n_sens=5. All three sources fire.
    Body_noise:agent_caused ratio dominated by random-policy contact
    rate at small env (164/200 agent-caused events); validation
    experiment will use a larger env / sparser policy to land the
    1:1-2:1 calibration band per the SD doc recalibration row.
    CORRECTED 2026-08-08 (was stale): V3-EXQ-511 (2026-05-03, 4-arm
    substrate-readiness diagnostic) ran and FAILED (C1b failed at
    sub-baseline ARM_1), reclassified non_contributory by governance --
    6/7 sub-criteria passed and the default/saturation calibration band
    passed; the FAIL does not weaken the claim. Its comparator-gap
    successor V3-EXQ-512 (2026-05-04, default scale=1.0) also FAILED
    (C3), superseded by V3-EXQ-512a (2026-05-04, recalibrated to
    scale=3.0), which PASSED (C1/C2/C3 all pass, evidence_direction
    supports) -- confirming the reafference-discrimination mechanism
    works, but only at 3x the substrate's default noise scale, not at
    default calibration and not as the full ARM_0-ARM_3 inverted-U
    sweep the claim specifies. That calibration-near-default sweep has
    never been queued (zero SD-048 entries in ree-v3/experiment_queue.json
    as of 2026-08-08). 6 literature entries already exist
    (literature_confidence 0.86) -- the "no lit-pull commissioned"
    line above was also stale.

    [2026-08-09 governance, V3-EXQ-902, confirmed failure_autopsy_V3-EXQ-902_2026-08-09]: this is
    the calibration-near-default sweep flagged above as never-queued. Result: FAIL at ARM_2/default
    on C2. Neither reading (weakens nor confirms) is trustworthy: the C2/"quiet_gap" statistic
    (residual_body_noise minus a quiet-bucket mean, NO minimum-sample guard) collapses to a
    tautology at scale >= ~3-4x -- including this run's own ARM_3, AND V3-EXQ-512a's ARM_A at
    scale=3.0, SD-048's CURRENT SOLE SUPPORTING EVIDENCE -- because n_quiet_steps=0 in every seed
    at that scale, so the statistic becomes residual_body_noise minus a zero placeholder rather
    than a real comparison. C1 (unaffected by this defect) shows a small but consistently
    positive body-noise-vs-agent-caused gap across most cells, arguing against a flat-curve/no-
    signal reading. Reclassified measurement_test_design_defect (not a valid claims.yaml enum --
    epistemic_category left at standard, see chip-20260809-epistemic-category-vocab-audit).
    **FLAGGED, NOT RESOLVED THIS CYCLE: V3-EXQ-512a's standing supports verdict rests on the
    IDENTICAL n_quiet_steps=0 artifact and needs its own re-examination** -- SD-048's evidentiary
    basis is currently resting on a statistic that may not have been measuring what it claims to
    at the one scale where it "worked". Status unchanged (candidate/v3_pending) pending that
    re-examination and a redesigned C2 criterion with a minimum-group-size guard. Also flagged:
    the implemented C2 silently drifted from the architecture doc's own pre-registered comparator
    (body-noise vs matched-amplitude agent-caused) to a quiet-baseline comparison since V3-EXQ-511
    "for comparability" -- this changed what the criterion scientifically tests.
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: interoceptive noise substrate must be
    implemented and active (interoceptive_noise_enabled=True) with all
    three sources (autonomic, sensitisation, fatigue) firing at a
    non-trivial rate. CONFIRMED: implemented 2026-05-03 in
    CausalGridWorldV2; activation smoke (4-arm sweep, 200 ticks) shows
    all three sources fire and bit-identical OFF holds. Further
    confirmed operationally by V3-EXQ-511/512/512a, all of which ran
    against the live substrate. Precondition is satisfied; it is not
    the open question.
    CONFIRMING: V3-EXQ-512a (2026-05-04, PASS, evidence_direction=supports)
    is a first confirming data point -- at noise_scale=3.0 (3x default),
    the interoceptive forward model shows genuine self-vs-noise
    discrimination (C1/C2/C3 all pass) rather than memorising
    action-damage coupling. Full confirmation requires the
    still-unrun calibration-sweep-near-default-scale successor: a 4-arm
    sweep (ARM_0 OFF through ARM_3, spanning default scale=1.0) showing
    the predicted inverted-U -- comparator competence peaking at an
    intermediate arm, degrading at ARM_0 (too little independent
    variance to force structural learning) and at the highest arm (too
    much noise to learn a stable separation) -- with the peak occurring
    at or near the substrate's OWN default calibration, not only at an
    artificially inflated scale.
    FALSIFYING: the calibration-sweep successor reproduces V3-EXQ-512's
    finding that discrimination collapses to near-baseline (quiet_gap
    ~ 0) across arms at or near default noise scale, and only recovers
    at scale >= 3.0 -- i.e. the comparator can only be honestly tested
    when noise is cranked well above the substrate's own designed
    default. This would route ARC-058's Level 2 comparator from
    V3-tractable to substrate_conditional (richer/recalibrated
    body-state substrate needed), per the claim's own stated fallback.
  location: docs/architecture/sd_048_interoceptive_noise_dynamics.md

- id: SD-049
  title: "Multi-resource heterogeneity: qualitatively distinct resource types (e.g. food / water / novelty) with per-type identity tags, type-specific spawn distributions, type-specific benefit profiles, and a per-axis homeostatic drive system, plus a curriculum-introduction hook. Provides the goal-identity vocabulary the wanting/liking + identity-distinct-goal cohort (MECH-229/230, MECH-117/216, ARC-030/032, Q-030, SD-015 unblock chain) needs to be honestly tested at substrate level, and the substrate scaffold for downstream developmental-schedule work."
  claim_type: design_decision
  subject: environment.multi_resource_heterogeneity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-538a_2026-07-10
      as_of: 2026-07-10
      verdict: non_contributory/substrate_ceiling
  epistemic_category: substrate_ceiling
  pending_retest_after_substrate: true
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: "2026-05-03"
  ceiling_decision: deferred
  ceiling_routing_note: "2026-06-19 substrate-ceiling-lifted triage (verdict b STILL CEILINGED): MECH-307 is orthogonal (anticipatory-affect amendment), not the foraging-competence substrate SD-049 needs. Phase-2 V3-EXQ-514 validation blocked on ~0.2% consumption on the enriched reef (514l). Cohort drive-coupling lineage (MECH-229/436, 514n-514t) is live but SD-049's own 4-arm validation is deferred pending a foraging-competent policy; route to /implement-substrate foraging scaffold, not a MECH-307 retest. See substrate_ceiling_lifted_triage_2026-06.md."
  depends_on:
    - SD-012     # homeostatic drive: per-axis extension required, cascades into goal-conditioning pathway
    - SD-015     # z_resource encoder: the upstream substrate this enables
    - SD-005     # self/world latent split: z_world routing must hold under multiple resource identities
  what_would_answer: |
    The 4-arm substrate gradient sweep already specified in
    functional_restatement (ARM_0 OFF-single-resource / ARM_1 2-type
    homeostatic / ARM_2 3-type-with-novelty (default) / ARM_3 5-type
    overshoot), scored on all four pre-registered criteria in the SAME
    run: (1) goal_resource_r identity-discrimination margin, target
    ARM_2 - ARM_0 >= 0.4; (2) wanting != liking trajectory fraction,
    target near-zero ARM_0/ARM_1, >= 0.6 ARM_2; (3) one-way ANOVA of
    per-axis homeostatic drive on z_goal cluster ID, significant in
    ARM_2 only; (4) encoder identity-recovery accuracy, at-chance ARM_0,
    > 0.6 ARM_2, falls back ARM_3. PASS validates SD-049 and unblocks the
    INV-034/SD-015-gated cohort. Flat failure across all arms
    specifically on criterion 2 routes MECH-229 (not SD-049 itself) from
    V3-tractable to substrate_conditional with a V4-1 multi-agent-ecology
    dependency, per the pre-registered Woo/Spelke off-ramp already
    exercised in V3-EXQ-514o.

    NON-DEGENERACY PRECONDITIONS (must clear before any of the four
    criteria are adjudicated -- all three have independently blocked
    this lineage before):
    (a) Foraging-competence/contact gate must clear -- the ~0.2%
        consumption-failure signature recurring across V3-EXQ-514l,
        V3-EXQ-693, and V3-EXQ-538a ("the waking pass never encoded
        consumption-identity so sleep had nothing to consolidate") is a
        missing-dependency signature, not a falsification, and must not
        recur.
    (b) The wanting/liking scoring harness must be non-vacuous on the
        same cross-target-inequality statistic used elsewhere in this
        lineage -- V3-EXQ-693's own R3 hard-zero precondition: liking
        target and L2-bind type read from the authoritative
        info['sd049_consumed_type_tag_this_tick'] at the consumption
        tick, not a post-clear obs_dict read.
    (c) Per-seed guard_pass robustness must be checked and reported, not
        just the arm-level aggregate -- V3-EXQ-793a found a joint
        curriculum+density-preserving-spawn config clearing the
        arm-level D3 gate while only 3/6 seeds individually passed
        guard.

    CROSS-REFERENCE RESOLUTION (514o): V3-EXQ-514o (2026-06-15, PASS)
    does NOT close any part of this falsifier and is correctly excluded
    from this claim's evidence trail -- not a governance tagging gap.
    Its manifest carries claim_ids=["MECH-229"] ONLY (explicit
    claim_tag_note). It is a single-arm run (SD-049-on + SD-057
    object-bound layer only, an ARM_2-analog) that never ran the
    ARM_0/ARM_1/ARM_3 comparison arms this claim's design requires, and
    of the four named criteria here reports numbers for only two
    (goal_resource_r, WL dissociation fraction), neither against the
    required baseline delta; the per-axis drive ANOVA and encoder
    identity-recovery criteria do not appear in it at all. It is
    informative context (shows the underlying substrate can produce
    strong goal_resource_r/WL numbers in a related condition) but
    non-dispositive here.

    The actual 4-arm design (matching this claim's arm labels and the
    ANOVA gate exactly) has already been attempted twice under this
    claim's own tag -- V3-EXQ-693 and V3-EXQ-693a -- both
    FAIL/non_contributory on non_vacuity_unmet before any of the four
    criteria were reached. A future "693b" (named as the needed next
    step in V3-EXQ-793a's routing, not yet queued as of 2026-08-07) is
    the vehicle that should finally clear all three preconditions above
    and reach actual adjudication.
  functional_restatement: >
    CausalGridWorldV3's resource model treats all resources as
    interchangeable units of a single appetitive class -- every
    resource cell carries the same benefit, consumption updates a
    single scalar drive (SD-012, drive_weight=2.0), and the z_resource
    encoder (SD-015) sees no identity-bearing signal. This is too
    thin a substrate for the cohort of claims about identity-distinct
    goals. The unifying signature: SD-015 sits at goal_resource_r=0.066
    across the EXQ-085x iteration cluster despite contact-gated z_goal
    seeding -- the encoder has nothing to encode beyond presence.
    MECH-229 wanting/liking dissociation PASS evidence (EXQ-074f) was
    obtained on z_world fallback seeding, not via genuine
    identity-distinct wanting. MECH-117's C2 PASS in EXQ-074d came
    from a single redirect to a moved single-type resource, a
    degenerate special case. Q-030's 6-cell z_resource x z_world
    routing question is ill-posed without identity in z_resource.

    SD-049 makes three additions to the env:
    (1) Multiple resource identities (default n_resource_types=3:
    food + water + novelty), each with a unique identity tag in
    obs_dict, type-specific spawn distribution, and type-specific
    benefit profile (drive axis + satiation curve + benefit amplitude).
    The novelty channel is non-homeostatic: benefit decays with
    familiarity rather than satiation, generating prospective wanting
    that does not reduce a homeostatic deficit -- the structural shape
    MECH-229 wants to test. (2) Per-axis homeostatic drive system
    replacing SD-012's single scalar drive_weight: each drive axis has
    independent depletion dynamics, an independent weight, and
    contributes additively (or via configurable combiner) to the
    goal-conditioning signal. Cascades into encoder, E3 evaluation,
    and triggers pending_substrate_reconfirmation on SD-012-emergent
    invariants per the invariant-types governance rule.
    (3) Curriculum-introduction hook (resource_introduction_schedule:
    dict[str, int]) controlling when each resource type becomes
    available across episodes. Defaults to all types available from
    step 0 of episode 0 so existing experiments are unchanged;
    populated, expresses developmental schedules. The hook itself is
    not the developmental schedule -- that is downstream design work
    using the hook -- but it is the substrate prerequisite.

    Falsifiable: a 4-arm substrate gradient sweep (ARM_0 OFF / ARM_1
    2-type homeostatic / ARM_2 3-type with novelty -- the default /
    ARM_3 5-type overshoot) with pre-registered predictions on
    goal_resource_r (target ARM_2 - ARM_0 >= 0.4), wanting != liking
    trajectory fraction (target near-zero in ARM_0/ARM_1, >= 0.6 in
    ARM_2), per-axis drive ANOVA on z_goal cluster ID (significant
    in ARM_2 only), and encoder identity-recovery accuracy
    (at-chance ARM_0, > 0.6 ARM_2, falls back ARM_3). PASS validates
    SD-049 and unblocks the cohort.

    Falsifier branch: flat-failure across all arms on the wanting !=
    liking criterion routes MECH-229 from V3-tractable to
    substrate_conditional with V4 dependency on V4-1 multi-agent
    ecology (where genuine "wanting another agent" emerges). This is
    the parallel to SD-047's Woo/Spelke branch.
  notes: >
    Registered 2026-05-03 from substrate-roadmap H-priority #2 walk.
    Direct trigger: user direction (2026-05-03) on substrate-roadmap
    after SD-047 implementation completed -- multi-resource
    heterogeneity selected as next H-priority outstanding enrichment
    on the basis of the largest unblockable cohort (MECH-229, MECH-230,
    MECH-117, MECH-216, ARC-030, ARC-032, Q-030, SD-015), with
    fuller-scope decision (3 types incl non-homeostatic novelty
    channel + per-axis drive + curriculum hook from start) chosen over
    minimal 2-type homeostatic-only scope. The fuller scope is
    load-bearing: a 2-type homeostatic substrate would let MECH-229
    PASS by exploiting drive-axis dissociation alone, which is not the
    claim under test. The novelty channel is what produces the case
    where wanting-target differs from satiating-target on a single
    drive axis.

    Roadmap entry: substrate_roadmap.md H-priority #2
    (`environment.multi_resource_heterogeneity`).

    Distinct from V4 multi-agent ecology (V4-1). The novelty channel
    is the closest V3-tractable analog of "another goal-bearer" but is
    structurally inanimate -- it does not pursue anything. The
    wanting/liking cohort can be tested without inter-agent goal
    interaction; the V4 dependency only opens via the falsifier
    branch.

    Distinct from SD-047 (multi-source environmental dynamics). SD-047
    enriches the env's agent-INDEPENDENT causal background for
    agency-detection comparators. SD-049 enriches the env's
    agent-RELEVANT goal vocabulary. Independently implementable;
    overlapping file (causal_grid_world.py) requires sequencing
    (SD-047 first, SD-049 layered on top).

    Distinct from environment.differentiated_coping_channels
    (substrate-roadmap M-priority #5, not yet registered). That SD
    addresses MECH-102's substrate-ceiling by giving the agent
    multiple ACTION modalities; SD-049 gives the env multiple
    RESOURCE modalities. Independently implementable.

    Implementation must be bit-identical OFF: when
    multi_resource_heterogeneity_enabled=False, env RNG sequence and
    per-tick state-update path match pre-SD-049 baseline exactly (no
    spurious RNG draws). Per-resource-type bit-identical OFF too, so
    each type can be ablated independently to recover ARM_1 from
    ARM_2 etc. without code change. Same discipline as SD-047 / SD-048.

    Lit-pull recommended before implementation per the
    biology-before-formal-definitions rule: wanting vs liking
    dissociation (Berridge & Robinson canonical; ventral pallidum vs
    nucleus accumbens), multi-resource foraging and identity-distinct
    goal selection (optimal foraging theory; primate / rodent
    multi-resource experimental work), non-homeostatic motivation
    (curiosity / novelty-seeking / intrinsic motivation; Berlyne;
    Kidd & Hayden 2015), and developmental introduction order of
    motivational classes (Spelke developmental work, relevant for
    curriculum-hook downstream validation).
  implemented_utc: "2026-05-04"
  implementation_note: >
    SD-049 Phase 2 (hybrid identity-aware z_resource encoder; Option C per
    2026-05-04 lit-pull verdict at confidence 0.78) IMPLEMENTED 2026-05-04.
    Encoder-side follow-on to the SD-049 Phase 1 substrate (env-only)
    landed 2026-05-03. Modules: ree-v3/ree_core/latent/stack.py
    (ResourceEncoder identity_head + LatentState.identity_logits);
    ree-v3/ree_core/utils/config.py (LatentStackConfig
    use_identity_classifier + identity_classifier_n_types);
    ree-v3/ree_core/agent.py (compute_resource_identity_loss);
    ree-v3/ree_core/environment/causal_grid_world.py (info dict
    sd049_consumed_type_tag_this_tick supervision target plumbing).
    Encoder shape (Option C hybrid): shared trunk MLP producing 32-dim
    z_resource (Schapiro 2017 monosynaptic-analog distributed substrate)
    + identity-classifier head supervised by cross-entropy on
    obs_dict["sd049_consumed_type_tag_this_tick"] (Ballesta-Padoa-Schioppa
    2019 labeled lines + Quiroga 2005 sparse readouts + Schapiro 2017
    trisynaptic-analog) + magnitude head reusing existing SD-018 pattern.
    z_resource OUTPUT shape unchanged (32 dims) -- classifier shapes
    trunk via training pressure; identity_logits exposed as separate
    LatentState field for the loss term (avoids GoalState seeding
    dim-mismatch a pure-concat instantiation would create). Backward
    compatible: use_identity_classifier=False default; existing SD-015
    experiments behave bit-identically. 7/7 preflight + 184/184 contracts
    PASS with classifier OFF. Activation smoke 2026-05-04: classifier
    instantiates, loss fires (~ln(3) at random init), backward succeeds,
    target=0 skipping works. Env consumed-type plumbing verified.
    Phased training (V3-EXQ-514 methodology, NOT a substrate requirement):
    P0 joint trunk + classifier + prox heads; P1 freeze identity_head
    requires_grad; P2 evaluate identity-recovery linear probe +
    goal_resource_r + per-axis drive evolution per V3-EXQ-514 acceptance.
    Validation experiment V3-EXQ-514 queued (estimated_minutes=90). PASS
    = SD-049 Phase 2 validated; SD-015 promotable; SD-049 v3_pending may
    be cleared. FAIL routes to verdict.md 6-row interpretation grid;
    row 6 is the Woo/Spelke-style substrate-ceiling falsifier (joint
    failure across ARM_2 + ARM_3 routes MECH-229 to substrate_conditional
    with V4-1 multi-agent ecology dependency).
    PHASE 3 DEFERRED: SD-032 consumer cascade (AIC/PCC/pACC/dACC/salience/
    override/MECH-295 migrate to obs_dict["per_axis_drive"]) registered
    in substrate_queue.json SD-049-PHASE-3 entry; action-trigger =
    V3-EXQ-514 failure on SD-032-mediated pathway only.

    SD-049 Phase 1 (env-only substrate) IMPLEMENTED 2026-05-03 in
    ree-v3/ree_core/environment/causal_grid_world.py via flat kwargs on
    CausalGridWorld.__init__ (matching SD-022/SD-023/SD-029/SD-047/SD-048
    env-only precedent rather than nested dataclass per the SD doc --
    nothing else uses dataclass-config for env params).

    Phase 1 lands all three substrate additions:
    (1) Multi-identity resources -- num_resources cells split across
    n_resource_types qualitatively distinct types (default 3:
    food / water / novelty) per resource_type_distribution; per-type
    proximity field views appended to world_obs (world_obs_dim grows
    by n_resource_types*25 -- 250 -> 325 default 3-type ARM_2;
    250 -> 375 5-type ARM_3 overshoot); per-type benefit profiles via
    resource_type_benefit_curves (sigmoidal_saturating / sharp_saturation /
    novelty_decay) including the structurally-distinct non-homeostatic
    novelty channel.
    (2) Per-axis homeostatic drive vector -- per_axis_drive[n_axes]
    parallel to legacy agent_energy. agent_energy collapses to
    1.0 - combiner(per_axis_drive) when per_axis_drive_enabled so all
    legacy SD-032 consumers (AIC / PCC / pACC / dACC / salience /
    override / MECH-295 bridge) continue to read obs_body[3] without
    modification. New observable: obs_dict["per_axis_drive"].
    (3) Curriculum hook -- resource_introduction_schedule:
    Dict[str, int] gates per-type spawn availability by cross-episode
    _global_step. Defaults to None -> all types available from step 0.

    Bit-identical OFF guarantee verified 2026-05-03 (50-tick parity
    check across explicit-False vs default + V3-EXQ-513 C0 acceptance
    criterion). 7/7 preflight + 184/184 contracts PASS with master OFF.
    Per-resource-type bit-identical OFF preserved (setting an entry to
    0.0 in resource_type_distribution drops that type without code
    change -- recovers ARM_1 from ARM_2 in the validation 4-arm sweep).

    Substrate-readiness diagnostic queued as V3-EXQ-513 (4-arm sweep
    ARM_0/ARM_1/ARM_2/ARM_3 + curriculum check; 13 acceptance criteria;
    13/13 PASS on dry-run smoke; full run ~5 min on Mac).

    PHASE 2 DEFERRED: the validation experiment for the FULL CLAIM
    (goal_resource_r lift, identity-recovery probe, wanting != liking
    trajectory dissociation) requires (a) z_resource encoder identity
    expansion (one-hot identity slot or learned embedding on the
    larger world_obs) and (b) SD-032 consumer cascade reading
    per_axis_drive directly rather than the collapsed obs_body[3].
    Phase 2 is registered in REE_assembly/evidence/planning/substrate_queue.json
    (SD-049 entry, phase_2 follow-on note); behavioural validation
    lands as V3-EXQ-514 post-Phase 2. Triggers
    pending_substrate_reconfirmation on SD-012-emergent invariants per
    the invariant-types governance rule when Phase 2 lands.

    Implementation choices documented in ree-v3/CLAUDE.md SD-049 section.
    Design doc Status updated to IMPLEMENTED (Phase 1, env-only).
    v3_pending=true KEPT until V3-EXQ-514 (Phase 2 behavioural
    validation) PASSes.
  evidence_quality_note: >
    2026-07-24 (governance, V3-EXQ-793 curriculum/density lever-sufficiency diagnostic; failure_autopsy_V3-EXQ-793_2026-07-24,
    confirmed, user-confirmed routing "Real but narrow finding"): both the curriculum amendment and
    the density-preserving spawn independently lift the D3 gate over the 693a baseline (A00 correctly
    reproduces the ceiling at 0.0202; A10/A01/A11 each clear). However per-seed guard_pass shows real
    fragility not visible in the arm-level criteria -- A11 (both levers) fails guard on 2/3 seeds
    despite the arm-level PASS. Genuine but THIN: informative for curriculum/environment design, NOT
    a decisive confirmation. epistemic_category measurement_gap; evidence_direction inconclusive.
    DOES NOT LIFT THIS PARK: 793 tests a narrower D3-gate lever-sufficiency question, not the
    foraging-competent-policy prerequisite the 2026-06-19 triage above actually gates on. SD-049
    stays candidate / substrate_ceiling / ceiling_decision:deferred (this citation is supplementary
    context; live_status.evidence continues to cite the governing 538a ceiling finding).

    2026-07-10 (governance, failure_autopsy_V3-EXQ-538a, confirmed; user-adjudicated): V3-EXQ-538a (SD-049 Phase-2 sleep-on ablation, supersedes the 538 SIGTERM) re-ran the pre-enrichment 514f coarse reef config (classifier_loss_weight=0.1, ~0.4% consumption, ~30-50 classifier updates over 9000 steps); the waking pass never encoded consumption-identity so sleep had nothing to consolidate -- a missing-dependency signature (consolidation downstream of encoding), NOT a falsification. Re-tagged non_contributory (the manifest's crude FAIL->weakens default and its vacuous per-claim SD-049 supports on the noise-dominated C3b OR are overridden). SD-049 UNCHANGED candidate / substrate_ceiling / ceiling_decision:deferred / pending_retest. Re-derive brake fired (4th SD-049 substrate_ceiling autopsy: 514l/693/693a/538a); routed to /implement-substrate SD-049-PHASE-2 foraging-competence track (538a failure_record appended). REFUSE a same-config Phase-2 sleep re-queue.

    2026-06-20 (governance, failure_autopsy_V3-EXQ-693, confirmed): V3-EXQ-693 (SD-049 Phase-2 4-arm substrate-gradient validation) FAIL self-routed substrate_not_ready_requeue / non_vacuity_unmet (R3 WL-channel-fireable=false). non_contributory -- CORRECT, NOT a weakens. Root cause is an INSTRUMENTATION gap, NOT a substrate ceiling: 693 forked from V3-EXQ-514l (pre-514n) and its wanting!=liking scoring harness is hard-zero (n_scored_wl_steps_total=0, run_bank_populated_frac=0.0 across all 4 arms / 3 seeds) while the contemporaneous 514t lineage scores the SAME IncentiveTokenBank.most_wanted object-bound read cleanly (run_bank_populated_frac=1.0, 72 scored WL steps). Tokens DO bind (distinct_tokens_max = n_resource_types 2/3/5), per-axis drive spread present (0.009-0.015), identity probe PASS (0.71-0.77, ~4.4k samples), ANOVA PASS (F up to 1480) -- the FAIL is localized solely to the WL scoring leg. SD-049 stays candidate / substrate_ceiling / v3_pending (UNCHANGED). Route: /queue-experiment 693a porting the working 514r/s/t WL-scoring + non-vacuity precondition; amended SD-049-PHASE-2 substrate_queue with this failure record. WATCH (gated, unadjudicated): C_GR discrimination-margin lift ARM_2-ARM_0=0.0067 (threshold 0.4; -0.0087 ARM_3). NOT a /claim-synthesis trigger (514q split already resolved lineage granularity debt).

    2026-06-03 (governance, failure_autopsy_V3-EXQ-514l): V3-EXQ-514l re-tagged non_contributory + epistemic_category substrate_ceiling + pending_retest_after_substrate. SD-049 Phase-2's deliverable (V3-EXQ-514 behavioural validation) is NOT met -- 514j/k/l all FAIL row1b_joint_identity_probe_weak. The z_resource encoder + SD-032 consumer cascade are wired and firing (C0/C1/C2a/C2d/C3 PASS); the missing piece is upstream foraging-competence on the SD-049 multi-resource/reef substrate (~0.2% consumption rate, 23 vs 818 neighborhood samples), so wanting/liking valence streams never differentiate (C6=0.0 every seed). Substrate routing: amend SD-049-PHASE-2 (port foraging-competence scaffold from scaffolded_sd054_onboarding, not a third overlapping entry).
    Registered candidate. SUBSTRATE PHASE 1 IMPLEMENTED 2026-05-03.

    Architectural motivation: cohort blocked at substrate thinness,
    signature is SD-015 goal_resource_r=0.066 across the EXQ-085x
    iteration cluster plus MECH-229 PASS evidence obtained on z_world
    fallback rather than identity-distinct wanting.

    Pre-implementation lit-pull complete (5 PubMed entries, 2026-05-03,
    sd_049 lit_conf=0.898 in plausible_unproven quadrant): Berridge 2018
    incentive-salience anchor; Smith & Berridge 2007 NAc-VP hedonic
    hotspots; Kidd & Hayden 2015 curiosity / non-homeostatic motivation
    -- LOAD-BEARING for the novelty channel; Shutts/Spelke 2009
    type-specific feature dimensions + developmental signature; Matthews/Tye
    2016 social wanting falsifier-adjacent licensing the V3/V4 division
    of labor.

    Phase 1 substrate validation (V3-EXQ-513): 4-arm sweep + curriculum
    check, substrate-readiness diagnostic. Acceptance criteria target
    bit-identical OFF, per-type spawn / contact, per-axis drive
    evolution, novelty familiarity growth, agent_energy divergence
    from legacy path, ARM_3 obs-dim shape + 4-of-5 type spawn,
    curriculum gate / release. PASS = SD-049 substrate is calibrated
    and ready for Phase 2.

    Phase 2 behavioural validation (V3-EXQ-514, queued post-Phase 2):
    pre-registered acceptance criteria from design doc -- ARM_2 - ARM_0
    goal_resource_r lift >= 0.4 (target 0.066 -> >= 0.5);
    identity-recovery linear probe accuracy > 0.6 in ARM_2;
    wanting_target != liking_target trajectory fraction >= 0.6 in ARM_2
    (near zero in ARM_0 / ARM_1); per-axis drive ANOVA on z_goal cluster
    IDs p < 0.01 in ARM_2. Five-row interpretation grid including the
    Woo/Spelke-style falsifier branch routing MECH-229 to
    substrate_conditional with V4 dependency.

    [2026-05-08 governance]: EXQ-514f (both runs) reclassified weakens -> non_contributory. Pre-MECH-307 affect-stream architecture mixed harm and benefit surprise; Phase-2 reef behavioural validation depends on cleanly separated affect signals. With streams mixed, classifier-loss and probe-accuracy outcomes cannot be interpreted as testing the SD-049 sleep-loop claim. Re-test required after MECH-307 conjunction-fix substrate lands; sleep substrate work is the load-bearing prerequisite for the underlying SD-049 mechanism itself.

    [2026-06-01 governance: V3-EXQ-514k non_contributory (GAP-2 confound)]: V3-EXQ-514k Phase-3 wanting/liking
    identity dissociation FAILed (C6 dissoc=False, C5 goal_resource_r_lift=False, identity-probe weak), but the
    ecological dissociation is CONFOUNDED by GAP-2 (SP-CEM) and the missing object-bound incentive-salience
    substrate per failure_autopsy_V3-EXQ-626_2026-06-01 routing (do not re-run ecological dissociation until
    object-bound substrate + positive-control-guarded harness). Reclassified weakens->non_contributory; not load-
    bearing against SD-049. MECH-229/MECH-230 remain non_contributory.

    [2026-07-26 governance, V3-EXQ-793a inconclusive; failure_autopsy_batch-793a-817-819_2026-07-26, confirmed]:
    repowers 793 (n=3->6, guard_pass made load-bearing via C_JOINT_ROBUST) exactly per the 793 autopsy. DECISIVE
    robustness finding: the joint arm A11 (amended curriculum x density-ON) clears D3 on mean contact (d1=1.0,
    d2=1.0) but C_JOINT_ROBUST fails -- guard_pass 3/6 seeds vs the 2/3 floor, short by one seed. The 3 guard
    failures are the low-baseline seeds (43/44/47) whose z_goal_norm_at_contact_peak sits just under the 0.4
    gate. Reading: the joint config is NOT a robust hand-off -- a future 693b must address the z_goal-at-contact
    guard fragility before receiving this config. This is a narrow lever-robustness result, orthogonal to and
    does NOT lift SD-049's foraging-competence park (still gated on the foraging scaffold /implement-substrate
    prerequisite). No re-queue owed this cycle -- queuing another SD-049 letter would risk circling the ceiling.
  location: docs/architecture/sd_049_multi_resource_heterogeneity.md

- id: MECH-296
  title: "Prototype-readout operator computes a softmax-attention match between the current LatentState and the type-keyed anchor pool, returning a small set of best-matched type-keys plus their confidences. Runs both during waking (moment-to-moment type recognition) and during sleep (consolidation refinement of the type-anchor pool)."
  claim_type: mechanism_hypothesis
  subject: hippocampal.prototype_readout_via_type_keyed_attention
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  v3_pending: false
  claim_level: mechanistic
  registered_utc: 2026-04-28
  depends_on:
    - SD-040     # type-encoder substrate produces the type-keys this operator queries
    - MECH-269   # AnchorSet substrate that stores the type-keyed entries
    - MECH-285   # sleep replay sampler that drives the offline refinement pass
  functional_restatement: >
    Concept cells at decode time fire for any input that matches their
    abstract identity (Quiroga 2005 Nature). HPC encodes higher-order
    statistical regularities including temporal community structure
    during waking exposure (Schapiro 2016 Hippocampus), and sleep
    enhances abstraction extraction but the operator is content-specific
    and naive replay disrupts it (Hennies et al. 2017 Sleep).

    MECH-296 specifies the readout operator that consumes SD-040's type-
    keyed AnchorSet entries: a softmax-attention match between current
    LatentState and the type-key vectors stored on each anchor, producing
    a top-k set of matched type-keys with confidences. During waking,
    this provides moment-to-moment type recognition that downstream
    consumers (proposer, frontal substrates, residue write valence
    selection) can condition on. During sleep, MECH-285's priority-
    weighted replay drives a refinement pass where recombined replays
    update the type-anchor pool by reinforcing co-occurring type-keys
    and pruning rarely-matched entries.

    Falsifiable: with the operator running, surface-different stimuli
    sharing a structural type-signature should activate overlapping
    type-key sets in the readout. Without it, type recognition is
    instance-specific and no shared activation pattern emerges.
  notes: >
    Registered 2026-04-28 from the type-prototype lit-pull. V4-scope by
    default - V3's gridworld doesn't have rich-enough type-instance
    dissociation to validate the operator behaviourally.
  evidence_quality_note: >
    Registered candidate with literature support from
    `targeted_review_hpc_type_prototype_substrate/` (Quiroga 2005,
    Schapiro 2016, Schapiro 2017 Phil Trans, Hennies 2017). No
    implementation yet.
  location: docs/thoughts/2026-04-28_action_object_type_abstraction.md

- id: MECH-297
  title: "Type-V_s gating extends MECH-269's per-stream / per-region V_s with a per-type V_s dimension keyed on the matched type-key from MECH-296. Allows the proposer / E3 to gate stream contributions by category-V_s as well as by per-stream V_s, downweighting type-confident-but-stream-degraded inputs and vice versa."
  claim_type: mechanism_hypothesis
  subject: hippocampal.type_vs_anchor_gating_extension_mech_269
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  v3_pending: false
  claim_level: mechanistic
  registered_utc: 2026-04-28
  depends_on:
    - MECH-269   # parent V_s primitive being extended with type dimension
    - MECH-296   # prototype-readout operator that produces matched type-keys
    - SD-040     # type-encoder substrate that grounds the type dimension
  functional_restatement: >
    MECH-269 currently maintains per-stream V_s scores and per-region
    V_s scores (Phase 1 + Phase 2 iii). MECH-297 extends this with a
    per-type V_s dimension: for each type-key matched by MECH-296,
    maintain a verisimilitude score that tracks how well current
    LatentState matches the stored prototype for that type. The
    proposer / E3 can then gate stream contributions by a combined
    (per-stream V_s) AND (per-type V_s) signal: high per-type V_s with
    low per-stream V_s suggests the type is correctly recognised but
    the specific instance signal is unreliable; low per-type V_s with
    high per-stream V_s suggests stream signals are reliable but the
    type-prototype match is poor (novel instance or out-of-distribution).

    Falsifiable: with type-V_s gating, novel instances of a known
    type should produce low per-type V_s and trigger the probe channel
    in MECH-269 (low-V_s anchors with strengthened MECH-094 hypothesis
    tag). Without it, novel instances are treated identically to known
    instances and no probe-channel firing.
  notes: >
    Registered 2026-04-28 from the type-prototype lit-pull. V4-scope.
    A natural Phase-4 extension to MECH-269 once the type-encoder and
    prototype-readout substrates land. Could alternatively be registered
    as MECH-269 Phase 4 rather than as a separate MECH-297; current
    convention treats MECH-269 phases as a coherent cluster and the
    type extension as a sibling claim.
  evidence_quality_note: >
    Registered candidate with literature support from
    `targeted_review_hpc_type_prototype_substrate/` (same anchors as
    SD-040 / MECH-296). No implementation yet.
  location: docs/thoughts/2026-04-28_action_object_type_abstraction.md

- id: MECH-298
  title: "Event-gated frontal write at goal-instantiation moments: when GoalState transitions from inactive to active, or when z_goal changes substantially, perform a high-bandwidth one-shot transfer of LatentStack content into SD-033a rule_state and SD-033b state_code, replacing the slow-EMA update for that tick. Mirrors Spellman et al. 2015 encoding-direction HPC->PFC dependence at goal-cue encoding."
  claim_type: mechanism_hypothesis
  subject: pfc.event_gated_frontal_write_at_goal_instantiation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  v3_pending: false
  claim_level: mechanistic
  registered_utc: 2026-04-28
  depends_on:
    - SD-033a    # lateral-PFC analogue rule_state being write-targeted
    - SD-033b    # OFC analogue state_code being write-targeted
    - MECH-288   # event segmenter whose slow-scale boundaries trigger the write
  functional_restatement: >
    Direct hippocampal-prefrontal afferents are critical for encoding
    but not for maintenance or retrieval of spatial cues; encoding-phase
    PFC representation depends on HPC input, while maintenance and
    retrieval are PFC-local (Spellman, Rigotti, Ahmari, Fusi, Gogos &
    Gordon 2015 Nature).

    MECH-298 specifies an event-gated frontal write: at goal-creation
    moments (GoalState inactive->active transition; substantial z_goal
    change detected by MECH-288 slow-scale BOCPD boundary), perform a
    one-shot fast-write into SD-033a rule_state and SD-033b state_code
    from the current LatentStack content, replacing the slow-EMA update
    for that tick. Subsequent ticks resume the EMA dynamics. This gives
    the frontal substrates a high-bandwidth coupling to richer
    associative content at the moments when goals are encoded, while
    keeping maintenance local-to-PFC at all other times.

    Falsifiable: with MECH-298 active, frontal rule_state / state_code
    should show step-change dynamics at goal-onset events and smooth
    dynamics elsewhere. Without it, dynamics are smooth across goal-
    onset events and there is no encoding-specific HPC->PFC coupling
    signature.
  notes: >
    Registered 2026-04-28 from the frontal_goal_grounding lit-pull.
    Default V4-scope. The MECH-288 boundary-trigger machinery already
    fires at the right moments in V3, so the missing piece is just the
    write hook on the frontal substrates - this is one of the lighter
    architectural extensions in the cluster and could be pulled forward
    to V3 if SD-033a/b training-dependent emergence work needs faster
    rule-state updates than the current EMA provides.
  evidence_quality_note: >
    Registered candidate with literature support from
    `targeted_review_frontal_goal_grounding/` (Spellman 2015 anchor
    plus Schmitt 2017 / Hallock 2016 / Ito 2015 surrounding context).
    No implementation yet.
  location: docs/thoughts/2026-04-28_action_object_type_abstraction.md

- id: MECH-299
  title: "Theta-cycle content scales with the agent's current substrate abstraction vocabulary: the unit packaged into a single theta cycle is the smallest reusable item available in the active substrate stack -- atomic actions when only those exist, action chunks when SD-045 lands, type-instance matches when SD-040/MECH-296 land, option invocations when SD-042 lands. Refinement of MECH-089 (theta-gamma nesting) on the content-of-packet axis."
  claim_type: mechanism_hypothesis
  subject: hippocampal.theta_cycle_content_scales_with_substrate_vocabulary
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  v3_pending: false
  claim_level: mechanistic
  registered_utc: 2026-04-28
  depends_on:
    - MECH-089   # parent theta-gamma packaging primitive being refined
    - MECH-294   # sibling claim on theta burst as packet for E3
    - SD-040     # type-encoder substrate that supplies type-instance unit
    - SD-042     # option library substrate that supplies option-invocation unit
    - SD-045     # action-chunk cache that supplies chunk unit
  functional_restatement: >
    Theta sequence path length scales with running speed and theta cycle
    length / number of gamma cycles within it (Gupta, van der Meer,
    Touretzky & Redish 2012 Nat Neurosci). Theta sequences segment
    experience into chunks. The hippocampal cognitive-map representational
    format generalises across non-spatial cognitive dimensions and
    hierarchical levels (Bellmund, Gardenfors, Moser & Doeller 2018
    Science).

    MECH-299 specifies that the unit of content packaged into a theta
    cycle is the smallest reusable item available in the agent's
    currently-active substrate vocabulary. The packaging mechanism is
    the same as MECH-089: theta cycle x gamma count = number-of-units
    per E3 tick. What changes is the granularity of "unit": atomic
    actions and z_world points when only those substrates exist,
    action chunks when SD-045 lands, type-instance matches when
    SD-040/MECH-296 land, option invocations when SD-042 lands.

    Falsifiable: longitudinal experiment where theta-sequence content
    is measured before and after the agent acquires a chunked
    behavioural skill. Pre-chunk content should be atomic; post-chunk
    content should include chunk-units. The architectural prediction
    is that the gamma-count per theta cycle stays roughly constant
    (the packaging machinery is fixed) while the granularity scales.
  notes: >
    Registered 2026-04-28 from the theta-abstraction-scaling lit-pull
    (`evidence/literature/targeted_review_theta_abstraction_scaling/`).
    V4-scope by default: gated on at least one of SD-040 / SD-042 /
    SD-045 landing first, since the prediction concerns what theta
    packages once those substrates exist. No V3 pull-forward path: V3's
    substrate vocabulary is fixed at z_world + atomic actions, so
    there's no second granularity for theta to scale into. The MECH
    becomes meaningful and testable once V4 expands the substrate stack.
  evidence_quality_note: >
    Registered candidate with literature support from
    `targeted_review_theta_abstraction_scaling/` (Gupta 2012 anchor +
    Bellmund 2018 framework + Constantinescu 2016 conceptual grids
    already in REE's lit index). No substrate implementation yet.
  location: docs/thoughts/2026-04-28_action_policy_and_multi_goal.md

- id: MECH-300
  title: "Cognitive map traversal via theta sequences operates at the agent's active abstraction level. Theta sequences do not have a fixed semantic content -- they simulate trajectories through whatever cognitive map the substrate vocabulary currently defines, whether spatial, conceptual, type-graph, or option-graph. Sister claim to MECH-299, on the map-being-traversed axis."
  claim_type: mechanism_hypothesis
  subject: hippocampal.cognitive_map_traversal_at_active_abstraction_level
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  v3_pending: false
  claim_level: mechanistic
  registered_utc: 2026-04-28
  depends_on:
    - MECH-089   # parent theta-gamma packaging primitive
    - MECH-269   # anchor pool that defines the cognitive-map nodes
    - MECH-299   # sister claim on packet content scaling
    - SD-040     # type-encoder produces type-graph nodes
    - SD-042     # option library produces option-graph edges
  functional_restatement: >
    Place and grid cell population codes provide a representational
    format for variable cognitive dimensions; the same format generalises
    across spatial and non-spatial domains and across hierarchical
    levels at different resolutions (Bellmund et al. 2018 Science).
    Action sequences result in trajectories through cognitive space,
    simulated via sequential coding (theta sequences) in hippocampus.

    MECH-300 specifies that the cognitive map being traversed by
    hippocampal theta sequences is the substrate vocabulary's currently
    -active level. With only z_world and atomic actions, theta traverses
    spatial trajectories. With chunks (SD-045), theta traverses
    chunk-graphs. With types (SD-040 / MECH-296), theta traverses
    type-graphs. With options (SD-042), theta traverses option-graphs.
    The same theta-sequence machinery navigates whatever map the
    substrate vocabulary defines.

    Falsifiable: in a hierarchical-task paradigm where the agent has
    learned both atomic-action sequences and option-level subroutines,
    theta-cycle content during option execution should reflect option-
    graph traversal (transitioning between option-nodes) rather than
    atomic-action traversal (transitioning between specific action steps).
    Subsequent Frank-lab work on hierarchical-task replay is the closest
    biological anchor.
  notes: >
    Registered 2026-04-28 as sister claim to MECH-299. Same V4 scope and
    same dependency on substrate-stack expansion. The two claims separate
    cleanly: MECH-299 covers content (what unit goes in the packet);
    MECH-300 covers map (what graph is being traversed). Both are
    architecturally licensed by Bellmund 2018 + Constantinescu 2016 +
    Gupta 2012 in combination.
  evidence_quality_note: >
    Registered candidate with literature support from
    `targeted_review_theta_abstraction_scaling/` (Bellmund 2018 anchor +
    Gupta 2012 + Constantinescu 2016 already in REE's lit index). No
    substrate implementation yet.
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: this claim is untestable until the substrate
    vocabulary actually contains a granularity level ABOVE the atomic action.
    V3's vocabulary is fixed at z_world + atomic actions, so a run against V3
    alone cannot address MECH-300 -- theta content would trivially reduce to
    atomic-action traversal because no other map exists to traverse, and that
    should self-route substrate_not_ready rather than count as a null/negative
    result. A valid test additionally requires: (1) MECH-089 / MECH-294
    theta-gamma packaging confirmed live, (2) MECH-269 anchor pool live
    (defines the cognitive-map nodes MECH-300 traverses), and (3) at least one
    of the reusable-unit substrates landed -- SD-045 (action chunks), SD-040 /
    MECH-296 (type-instance prototypes), or SD-042 (option library) -- so
    there is a second, non-atomic map for theta to be traversing. Per the
    plan-of-record (object_reasoning_abstraction_v4_plan.md OBJ-ABS-6), this
    claim is explicitly the capstone validation node and cannot be the first
    thing built; it validates that the lower pillars re-grain the theta
    packet rather than existing as separate substrates.

    CONFIRMING signature (per the claim's own functional_restatement): in a
    hierarchical-task paradigm where the agent has learned BOTH atomic-action
    sequences AND a higher-level unit (option-level subroutines, action
    chunks, or type-graph transitions), decoded theta-cycle content during
    execution of the higher-level unit should transition between nodes of
    THAT graph (option-to-option, chunk-to-chunk, or type-to-type) rather
    than between atomic action-steps -- i.e. the same theta-sequence
    machinery re-grains to whichever vocabulary is currently active. This is
    the substrate-level analogue of the Frank-lab hierarchical-task replay
    literature (the closest biological anchor named in the claim).

    FALSIFYING signature: if, even once a higher-level substrate (chunks /
    types / options) is built and demonstrably drives behaviour, decoded
    theta-sequence content during higher-level execution continues to
    reflect ONLY atomic-action-to-atomic-action transitions -- i.e. theta
    stays pinned to the lowest-granularity map regardless of which
    vocabulary level is behaviourally active -- that falsifies MECH-300's
    "no fixed semantic content" claim and would instead support a fixed
    theta-content model (theta always encodes atomic spatial/action
    trajectories, irrespective of higher-level structure).

    Note the sister-claim coupling: MECH-299 (content/unit) and MECH-300
    (map/graph) are intended to be tested together against the same
    hierarchical-task substrate. A result confirming one while falsifying
    the other is itself informative and should be reported as such rather
    than resolved -- it would indicate the packaging and traversal axes are
    dissociable rather than co-scaling as currently assumed.
  location: docs/thoughts/2026-04-28_action_policy_and_multi_goal.md

- id: MECH-301
  title: "Priority-weighted anchor replay (MECH-285) runs not only during sleep but also during brief waking quiescent pauses, surfacing remote / non-current stored content for goal-directed retrieval. The architectural gating is contextual (quiescent vs active behaviour, sensory input vs absence) rather than state-categorical (waking vs sleep). Refinement / generalisation of MECH-285."
  claim_type: mechanism_hypothesis
  subject: hippocampal.waking_quiescent_replay_via_mech285_priority_sampling
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  v3_pending: false
  claim_level: mechanistic
  registered_utc: 2026-04-28
  depends_on:
    - MECH-285   # parent sleep replay sampler being generalised
    - MECH-272   # routing gate that currently switches stream weights between waking/sleep
    - MECH-292   # ranked ghost-goal bank consumer of replay
    - MECH-293   # waking ghost-goal probes consumer of replay
  functional_restatement: >
    Awake replay reactivates stored representations of remote / non-
    current-environment trajectories as commonly as it reactivates
    current-environment content; remote replay surfaces during brief
    quiescent pauses in awake behaviour, more robustly when the agent
    has recently been in motion than during extended quiescence
    (Karlsson & Frank 2009 Nat Neurosci). Replay is compositional, not
    a literal function of recent experience -- it constructs novel
    paths and reactivates infrequently-experienced sequences (Gupta,
    van der Meer, Touretzky & Redish 2010 Neuron).

    MECH-301 specifies that REE's MECH-285 priority-weighted anchor
    sampling should run during waking quiescent moments as well as
    during sleep. The substrate machinery is unchanged; the gating is
    lifted from "sleep only" to "quiescent context with no active
    sensory input demand". Routing weights (MECH-272) may differ
    between waking and sleep contexts, but the sampling itself is
    not state-categorically restricted.

    Falsifiable: a V3 or V4 agent with waking-quiescent replay enabled
    should surface ghost-goal anchors during brief between-action
    pauses, even before any sleep cycle has run. A V3 agent with the
    sleep-only gate should NOT surface remote anchors during waking
    quiescence. Behavioural signature: in a goal-relocation task with
    no opportunity to sleep before retest, MECH-301-on agents should
    show MECH-292 ghost-goal-bank-driven retrieval of relocated-goal
    anchors during the inter-trial interval; MECH-301-off agents
    should not.
  notes: >
    Registered 2026-04-28 from the theta-abstraction-scaling lit-pull.
    Default V4-scope but with V3 PULL-FORWARD CONDITION: this is the
    LIGHTEST-TOUCH architectural extension in the current set. The
    MECH-285 substrate already exists; only the invocation gate needs
    lifting. If EXQ-495 V3-full-completion-gate or successor experiments
    surface a need for more aggressive ghost-goal probe access during
    waking quiescent moments than the current MECH-293 architecture
    provides, MECH-301 is the cheapest extension to pull forward into
    late-V3. Implementation: lift sleep-only gate on MECH-285 invocation,
    add a quiescence-detector trigger (e.g. low z_harm_a + low drive +
    no committed trajectory active), apply MECH-272 routing weights for
    waking-quiescent context.
  evidence_quality_note: >
    Registered candidate with literature support from
    `targeted_review_theta_abstraction_scaling/` (Karlsson & Frank 2009
    anchor + Gupta 2010 compositional replay). No substrate
    implementation yet.
  location: docs/thoughts/2026-04-28_action_policy_and_multi_goal.md

- id: SD-050
  title: "Suffering-derivative comparator: a non-trainable rolling-window descent detector on z_harm_a.norm() that fires a relief-completion event when a sustained drop from peak to trough exceeds a threshold. Provides the substrate for MECH-302 by detecting when a period of suffering has cleanly ended, reusing the same downstream tag-and-release pipeline as goal-achievement (MECH-057a beta-gate release, MECH-094 VALENCE_LIKING write). Architecturally adjacent to MECH-091 (urgency interrupt) but opposite polarity: MECH-091 fires on upward z_harm_a spike; SD-050 fires on downward sustained drop."
  claim_type: design_decision
  subject: relief.suffering_derivative_comparator
  readout_precondition_note: 'SD-050 cannot fire on a range-degenerate z_harm_a: a sustained peak-to-trough
    descent detector requires the signal to have a trough. Its falsifier is uninterpretable while
    SD-086''s decode-floor non-degeneracy guard fails (measured on V3-EXQ-664, z_harm_a.norm spans only
    ~7.18-7.42 within an episode, no descent to detect). This is a readout precondition, not a defect in
    SD-050 on its own terms. See SD-086 (2026-06-10 z_harm_a saturation intake, candidate 4).'
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: false
  claim_level: architectural
  registered_utc: "2026-05-04"
  depends_on:
    - SD-011      # z_harm_a affective stream (source signal for the comparator)
    - MECH-057a   # commitment-release pipeline reused on event fire
    - MECH-091    # adjacent urgency block in select_action(); opposite polarity
    - MECH-094    # simulation gate: comparator tick() skips when simulation_mode=True
  functional_restatement: >
    REE's goal-completion pipeline (MECH-057a beta-gate drop + MECH-091
    phase-reset + MECH-094 VALENCE_LIKING write) fires on goal-attainment
    events. There is no architecturally analogous event for the end of a
    period of suffering: avoidance commitments accumulate during harm but
    no clean "relief completion" event closes the episode and tags what
    just worked.

    SD-050 implements a SufferingDerivativeComparator -- a non-trainable
    rolling-window FIFO (window_length ticks) on z_harm_a.norm(). The
    comparator fires (returns True) when: (1) the buffer is full; (2) the
    oldest norm entry exceeds min_initial_norm (prevents false fires on a
    stream that was already quiet); (3) the drop from oldest to newest
    entry exceeds drop_threshold. The event flag is set in sense() and
    consumed in select_action() in the same tick.

    On event fire: if beta is elevated, beta_gate.release() + reset
    committed_step_idx (MECH-057a reuse). If valence_liking_enabled and
    not hypothesis_tag, ResidueField.update_valence(z_world, VALENCE_LIKING,
    relief_completion_weight) (MECH-094 reuse). The comparator advances only
    on waking ticks (simulation_mode=False), satisfying the MECH-094 gate.

    Biological grounding: Navratilova 2012 (PNAS) shows pain relief
    activates VTA-DA + NAc-shell DA via the same circuit as appetitive
    reward, blocked by DA antagonists and producing conditioned place
    preference -- confirming shared machinery. The comparator (not a pooled
    DA readout) is the architecturally correct trigger per the Bromberg-
    Martin 2010 DA-heterogeneity qualifier: value-coding VTA-DA is the
    substrate but the REE trigger must be the derived comparator to avoid
    mixing salience-coding DA.
  evidence_quality_note: >
    Implemented 2026-05-04. Module:
    ree_core/comparator/suffering_derivative_comparator.py
    (SufferingDerivativeComparator). Config gate:
    REEConfig.use_suffering_derivative_comparator (default False,
    bit-identical OFF). Additional params: suffering_window_length (5),
    suffering_drop_threshold (0.10), suffering_min_initial_norm (0.05),
    relief_completion_weight (1.0). All 5 smoke tests PASS 2026-05-04.
    V3-EXQ-515 PASS 2026-05-04 (comparator logic unit diagnostic).
    V3-EXQ-516 queued (agent-loop integration diagnostic). Blocks MECH-302,
    MECH-303.
  location: docs/architecture/sd_050_suffering_derivative_comparator.md

- id: MECH-302
  title: "Relief-completion event reuses goal-achievement / reward-circuit completion machinery. When a comparator on a suffering-stream derivative crosses a downward threshold (sustained suffering-reduction below baseline), the resulting event fires the same downstream tag-and-release pipeline as goal-achievement: MECH-057a (commitment-release / beta-gate-drop), MECH-091 (salient-event phase-reset), and MECH-094 (categorical hypothesis-tag write gate). The polarity of the reinforcement is set at the input -- what counts as the completion is the suffering-derivative crossing, not a reward-onset event -- but the consolidation pipeline is shared. 'Things that just reduced suffering' are tagged in the same approach-attractor structure as 'things liked'."
  claim_type: mechanism_hypothesis
  subject: relief.completion_event_reuses_goal_achievement_pipeline
  readout_precondition_note: 'The relief-completion event (via SD-050''s comparator) cannot fire while
    z_harm_a''s functional readout is range-degenerate -- MECH-302''s falsifier inherits SD-050''s
    readout precondition. See SD-086 (2026-06-10 z_harm_a saturation intake, candidate 4).'
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-12
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-517b_2026-05-30
      as_of: 2026-05-30
      verdict: non_contributory/substrate_ceiling
  implementation_phase: v3
  v3_pending: false
  claim_level: mechanistic
  registered_utc: 2026-05-03
  depends_on:
    - MECH-057a   # commitment-release / beta-gate-drop pipeline being reused
    - MECH-091    # salient-event phase-reset (relief-completion is one such salient event)
    - MECH-094    # hypothesis-tag write gate (the tag store being written)
    - MECH-303    # sister claim: safety-cue prediction substrate (parallel mechanism whose teaching signal is MECH-302's completion event)
    - SD-011      # dual nociceptive streams (the suffering-stream that the comparator reads)
    - SD-012      # homeostatic drive (sets the baseline against which suffering-derivative is computed)
  functional_restatement: >
    Mechanism hypothesis. The current REE architecture has clean
    release / completion machinery for goal achievement (MECH-057a
    beta-gate-drop, MECH-091 phase-reset, MECH-094 categorical tag
    write) but no architecturally analogous event for sustained
    suffering-reduction. Avoidance from pain / suffering is implicitly
    continuous -- as long as harm is predicted, the avoidance system
    keeps weighting the policy, with no clean completion event that
    closes the avoidance episode and tags 'what just worked' for
    future approach-toward-safety.

    MECH-302 asserts: when a comparator on a suffering-stream
    derivative (read off SD-011 z_harm_s / z_harm_a) crosses a
    downward threshold sustained for k steps, this event fires the
    SAME downstream pipeline that goal-achievement uses. Concretely:
    1. MECH-057a: any active avoidance commitment is released, the
       beta-gate drops, policy update is unblocked.
    2. MECH-091: a phase-reset marker is emitted (oscillatory in the
       biological substrate; explicit cycle-boundary in the ANN
       substrate).
    3. MECH-094: a categorical hypothesis tag is written, marking
       the trajectory / context just experienced as 'this just
       reduced suffering'. The tag-store is shared with goal-
       achievement -- 'things that reduced suffering' and 'things
       liked' are stored in the same approach-attractor structure,
       distinguished only by the polarity of the input event.

    The architectural prediction is that polarity is set at the
    input layer (suffering-derivative-crossing-threshold vs
    goal-attainment-event), not via parallel circuitry. This is
    the cellular complement to ARC-024-equivalent claims that the
    ventral-striatal completion store is shared across appetitive
    polarities.

    Falsifiable: an REE substrate with MECH-302 enabled should
    produce conditioned approach to stimuli paired with sustained
    suffering-reduction events, with the same temporal-discounting
    profile and same downstream tag-write signature as conditioned
    approach to goal-attainment stimuli. With MECH-302 disabled
    (avoidance commitments never receive a completion event), the
    agent should accumulate persistent avoidance commitments without
    tag-and-release -- a candidate substrate-level analogue of
    chronic anxiety / persistent-avoidance failure modes.
  notes: >
    Registered 2026-05-03 from the relief-completion-mechanism
    pre-registration lit-pull. Sister claim to MECH-303 (safety-cue
    prediction). The two mechanisms are coordinated but architecturally
    distinct: MECH-302 handles the just-now-occurred completion event;
    MECH-303 handles the predictive encoding of cues that anticipate
    relief. MECH-302's completion event is the teaching signal that
    updates MECH-303's predictive store.

    Implementation note. The lit-pull verdict surfaced a DA-heterogeneity
    qualifier (Bromberg-Martin 2010, Brischoux 2009): not all VTA
    dopamine neurons code value at offset; ventral-VTA can be excited
    by aversive ONSET (salience-coding) which would contaminate a
    naive 'phasic DA = good thing' teaching signal. The architectural
    consequence is that REE's MECH-302 trigger should be the suffering-
    derivative-crossing comparator (a derived signal), NOT a pooled DA
    activity readout. The biological substrate routes through value-
    coding DA into NAc-shell -- but the REE trigger is the comparator,
    matching what MECH-094 already does for goal-achievement at the
    categorical level.
  evidence_quality_note: |
    Registered candidate 2026-05-03. Pre-registration lit-pull anchor:
    `evidence/literature/targeted_review_relief_completion_mechanism/verdict.md`
    (hybrid-leaning-Model-1, conf 0.80). Direct supporting entries:
    Andreatta 2012 Learn Mem (relief->striatum / fear->amygdala
    cross-species double dissociation, supports 0.86 -- LOAD-BEARING),
    Navratilova 2012 PNAS (pain relief activates VTA-DA + NAc-shell DA +
    DA-antagonist-blockable place preference, supports 0.84), Ramirez
    2015 J Neurosci (active avoidance requires BA -> NAc-shell circuit,
    supports 0.78), Tanimoto 2004 Nature (Drosophila opponent-timing
    relief-as-reinforcer principle, supports 0.78). DA-heterogeneity
    qualifiers: Bromberg-Martin 2010 (mixed 0.79), Brischoux 2009
    (mixed 0.80). Substrate implemented 2026-05-04:
    SufferingDerivativeComparator wired into ree_core
    (ree_core/comparator/suffering_derivative_comparator.py);
    config gate REEConfig.use_suffering_derivative_comparator (default False,
    bit-identical OFF). V3-EXQ-515 PASS 2026-05-04 (comparator logic
    diagnostic). V3-EXQ-516 queued (agent-loop integration diagnostic).
    v3_pending=true until discriminative-pair experiment lands.

    2026-05-30 env-curriculum amend (failure_autopsy_V3-EXQ-517b_2026-05-30,
    implement-substrate-mech302-scheduled-limb-damage-curriculum-20260530T084206Z):
    Three behavioural-validation discriminative-pair attempts
    (V3-EXQ-517 / 517a / 517b, 2026-05-04 / 04 / 06) all FAILed at ARM_A
    C1+C2 with 0-0.33 events/seed across all parameter relaxations.
    517a -> 517b doubling of episode length (150 -> 300 steps) DECREASED
    ARM_A events (0.33/seed -> 0.00/seed) -- the trained avoidance policy
    learns to avoid hazards regardless of episode length, so the
    SufferingDerivativeComparator never sees the damage->heal trajectories
    it needs to fire. Failure autopsy diagnosed substrate_ceiling at the
    env-policy-interaction layer (comparator wiring is intact per V3-EXQ-515
    PASS; gap is on the env side). Routed to implement-substrate amend:
    landed SD-022 scheduled-injection extension (scheduled_limb_damage_*
    env kwargs in CausalGridWorldV2) -- direct limb-damage injection
    independent of agent action or hazard contact, parallel to SD-029
    scheduled_external_hazard but distinct in mechanism (SD-029 still
    requires agent contact; this curriculum bypasses contact entirely).
    All 5 new env kwargs default no-op; 565/565 contracts + 7/7 preflight
    PASS with master OFF (regression-clean; +9 new MECH-302-curriculum
    contracts). V3-EXQ-517c queued 2026-05-30 (priority 280; supersedes
    V3-EXQ-517b; curriculum identical on both arms, only comparator
    ON/OFF varies). Acceptance criteria unchanged from 517b. PASS clears
    v3_pending AND lifts gate (c) for MECH-304 conditioned-inhibition
    experiment. pending_retest_after_substrate=true.

    2026-05-30 V3-EXQ-517c PASS (governance applied): first valid behavioural
    discriminative-pair evidence on the rescued substrate. Manifest
    v3_exq_517c_mech302_relief_completion_discriminative_pair_20260530T124600Z_v3.json,
    evidence_direction=supports. ARM_A (comparator ON) 2/3 seeds PASS:
    mean 160.3 events/seed, 160.3 writes/seed, 29.7 curriculum_injections/seed.
    ARM_B (comparator OFF) 3/3 seeds PASS with 0 events / 0 writes /
    40.3 curriculum_injections/seed -- strong dissociation. v3_pending
    cleared by sibling IGW-021 session 2026-05-30T17:16Z; pending_retest_after_substrate
    set to false in same edit. Prior 517 / 517a / 517b FAILs remain in the
    record but per failure_autopsy_V3-EXQ-517b_2026-05-30 should be read as
    substrate-ceiling (env-policy-interaction; comparator wiring intact per
    V3-EXQ-515 PASS) -- NOT claim-weakens. Per the illusory-conflict-resolution
    rule, the prior FAIL manifests are NOT being silently reclassified to
    non_contributory; the historical conflict_ratio reflects the substrate
    ceiling and will revise downward in future cycles as additional
    rescued-substrate evidence accumulates. Gate (c) for MECH-304 conditioned-
    inhibition (V3-EXQ-519) now lifted.
  location: evidence/literature/targeted_review_relief_completion_mechanism/verdict.md

- id: MECH-303
  title: "Context-bound passive safety representation: the agent encodes 'this environment predicts absence of suffering' through sustained non-occurrence of harm, without requiring a discrete teaching event. Substrate is prefrontal-hippocampal (IL for expression gating; vHipp-to-PL-equivalent for contextual store). Update rule is diffuse and slow -- accumulated context exposure without harm drives the store. Output lowers background vigilance threshold: reduces the general avoidance commitment level when the agent is in an environment the store predicts as safe. Parallel to the 'things liked' goal-cue store but tracking suffering-absence rather than reward-presence. Sister claim to MECH-304 (cue-specific conditioned inhibition pathway)."
  claim_type: mechanism_hypothesis
  subject: safety_prediction.contextual_passive_substrate
  polarity: asserts
  status: active
  epistemic_category: standard  # 2026-08-16 governance: stamped per confirmed autopsy
  live_status:
    reading: active
    as_of: 2026-08-22
    needs_review: false
    evidence:
      from: "governance-cycle-2026-08-22 (confirmed V3-EXQ-939a PASS)"
      as_of: 2026-08-22
      verdict: promote_to_active/applied
  implementation_phase: v3
  v3_pending: false
  claim_level: mechanistic
  registered_utc: 2026-05-03
  revised_utc: 2026-06-05
  evidence_quality_note: |
    [2026-08-22 governance, cycle bold-chaum-7e245c]: PROMOTED provisional -> active. The
    behavioural falsifier this claim's own 2026-07-15 note named as the promote-to-active gate
    ("context-safety lowers background vigilance/avoidance-commitment level") is
    V3-EXQ-939a, and it PASSED cleanly. 939a is the repaired successor to V3-EXQ-939 (it
    supersedes it; the two are NOT independent evidence and are not double-counted). It fixes
    all three readiness-gate defects the 939 autopsy identified: the gate now aggregates arm A's
    release_rate by MEAN against DV_MARGIN itself (same statistic, same aggregation, provably
    never harsher than the criteria it guards); N_TEST_TRIALS 30 -> 90 with a per-seed floor
    computed as floor(DV_MARGIN*n)/n rather than a lattice-unreachable constant; and the
    pre-registered 2-of-6 MIN_VALID_SEEDS tolerance can now actually bind. Result: 6/6 valid
    seeds, DV1 0.750 / DV2 0.748 / DV3 0.750 against the unchanged 0.34 margin -- clearing by
    ~2.2x rather than 939's ~0.10 -- with criteria_non_degenerate true on all three, arms
    A 0.750 / B 0.0 / C 0.0 / D 0.748, and substrate_stable_across_run true. The larger gaps are
    a declared window effect, not a design change: release_rate is not a stationary per-trial
    probability, the 90-trial window is identical across all four arms, every contrast is
    within-context, and the driver verifies bit-for-bit that its trials 1..30 reproduce 939's
    whole 30-trial window on all four arms. Governance driver skim (mandatory on a claim-tagged
    evidence PASS) found NO defect: the PASS gate in source is a genuine AND over readiness,
    tolerance and all three DVs, matching the manifest's declared combination_rule verbatim.
    Minor note only: the aggregate non_degenerate flag uses any() over the three per-DV checks
    rather than all(); per-DV detail is preserved in criteria_non_degenerate and all three are
    true here, so nothing is masked. pending_retest_after_substrate cleared -- 939a IS the owed
    retest, unblocked by SD-MECH303-THRESHOLD-SOURCING landing 2026-08-14. Evidence state at
    promotion: 2 genuine experimental entries (V3-EXQ-760 representation-level, V3-EXQ-939a
    behavioural), both supports, 0 weakens, 0 fail_runs, exp_conf 0.824, lit_conf 0.853, overall
    0.841, quadrant confirmed_established. Symmetric with sister claim MECH-304, already active.

    [2026-08-21 governance, gov-20260821-0203, confirmed failure_autopsy_V3-EXQ-939_2026-08-20.json]: V3-EXQ-939 is the owed MECH-303 behavioural retest, unblocked by SD-MECH303-THRESHOLD-SOURCING landing 2026-08-14 (status_phase validated, its sole failure_record item already resolved). Its self-route substrate_not_ready_requeue and its degeneracy_reason ('the mechanism could not express itself, so nothing was measured') are REJECTED by this autopsy: the same manifest contains the clean 2x2 dissociation that refutes them -- A_safe_gate_natural 0.43889, B_safe_gate_forced_closed 0.0, C_hazard_gate_natural 0.0, D_hazard_gate_forced_open 0.45, with num_safety_steps 240/0/6/240 exactly as designed. All three pre-registered DVs PASSED, each clearing the 0.34 margin by ~0.10 (DV1 0.43889, DV2 0.45, DV3 0.43889), with criteria_non_degenerate true on all three, including DV3 which the driver names as the genuinely non-analytic one. The run was voided by a single readiness precondition missing by 0.0067, and that gate is defective three independent ways, all verified in source. (1) It aggregates by MIN (all(r >= floor), line 561) while the DVs it guards aggregate by MEAN (line 539) -- the same statistic, so the V3-EXQ-643 rule is met in letter, but the gate is strictly harsher than the criterion it guards, and the docstring's stated intent is a best-case check while the implementation reads the worst cell. (2) Its threshold is unreachable on the measurement lattice: N_TEST_TRIALS = 30 quantises release_rate to multiples of 1/30, so 0.34 lies strictly between 10/30 = 0.33333 (fails) and 11/30 = 0.36667 (passes); the effective floor is 0.3667, a silent 7.8% tightening, and seed 1 missed by 0.2 of a single trial. (3) MIN_VALID_SEEDS = 4, a pre-registered tolerance for up to 2 failing seeds of 6, computed 5 >= 4 and can never bind, because readiness_ok already requires every cell to clear the same floor -- the tolerance was vetoed by a zero-tolerance conjunction. Recorded but unresolved: the queue entry's 1-seed calibration reported A 0.900 / D 1.000 against a realised A 0.439 / D 0.45, roughly half, at identical config and a single driver commit.

    2026-07-15 (gov): promoted candidate->provisional on V3-EXQ-760 (contextual safety-terrain
    discrimination, PASS/supports) -- wall-independent representation-level confirm: the
    read-only evaluate_safety field DISCRIMINATES safe vs unsafe contexts, gated (harm-absent
    accumulation) AUC 0.884 vs ungated control 0.488, isolating the harm-absence gate. First
    genuine exp support (exp_conf 0.773), 1 support/0 weakens. Mirrors the MECH-287/288/284/304
    wall-independent-isolation -> provisional pattern. Promote-to-active remains gated on a
    behavioural falsifier (context-safety lowers background vigilance/avoidance-commitment level).

    Revised candidate 2026-05-04. Primary lit support: Kreutzmann
    2020 Psychopharmacology (IL required for safety expression not
    acquisition; PL null), Meyer 2019 PNAS (vHipp->PL pathway for
    safety-cue inhibition, cross-species), Laing 2022 J Neurosci
    (standard safety signal -> vmPFC + hippocampus, dissociated from
    conditioned inhibitor -> dorsal striatum). Ng & Sangha 2023
    papers (IL circuit and IL single-unit) retagged to MECH-304
    (conditioned inhibition paradigm). Substrate implemented 2026-05-04
    (SD-052). v3_pending=true until V3-EXQ-520 context-conditioned
    safety extinction discriminative-pair experiment completes.
    Cluster-level enrichment anchor 2026-06-05: Silva et al. 2021 Nat
    Neurosci (DOI 10.1038/s41593-021-00856-y; nucleus reuniens -> BLA
    safety transmission; conf 0.80) -- bears primarily on MECH-304;
    recorded on MECH-303 as a cluster-level note that the safety-
    expression machinery is prefrontal-hippocampal-thalamic. Thalamic
    relay + remote/recent axis UNIMPLEMENTED (documentation-only).

    [/governance 2026-08-12, V3-EXQ-917, confirmed
    failure_autopsy_V3-EXQ-916-916a-917-920-fishtank-cluster_2026-08-12,
    threshold-sourcing calibration battery]: this claim's live gate is
    currently WIRED TO THE WRONG SIGNAL, and that is a substrate defect
    rather than evidence against the mechanism. Under the production
    default (damage_sourced z_harm_a, SD-022 body-damage) the
    contextual_safety_harm_threshold gate cannot discriminate safe from
    hazardous contexts at ANY of 18 swept thresholds (0.02-0.8): AUC
    caps at 0.52, i.e. chance. The legacy proximity_ema_sourced signal
    (limb_damage_enabled=False) reaches AUC 0.84-0.97 on the same
    apparatus, with a reachable+discriminating band around
    threshold 0.55 (reachability 0.77, AUC 0.9625) to 0.6
    (reachability 0.85, AUC 0.969). So every prior null result on this
    claim measured through a gate that could not fire informatively --
    the mechanism was never given a fair test, and no evidence_direction
    is applied to MECH-303 from this run.
    ROUTING DECISION (2026-08-12, user-adjudicated): take option (a),
    a DEDICATED proximity-anticipatory signal for MECH-303's gate,
    decoupled from SD-022's damage-sourcing -- NOT option (b), a
    threshold retune on the existing damage-sourced signal. Rationale:
    damage_sourced is load-bearing for SD-022's own body-damage-tracking
    purpose and for other consumers of z_harm_a, so re-sourcing the
    shared signal would fix this gate by breaking theirs; a second harm
    signal is the correct shape, and the SD-011 dual-stream separation
    (see that claim's 2026-08-12 note) is exactly why. This makes a
    substrate build LIVE for a claim that had none: substrate_queue
    entry SD-MECH303-THRESHOLD-SOURCING created this cycle
    (priority 2, severity degrading), and
    pending_retest_after_substrate set true -- the behavioural retest is
    owed once the dedicated signal lands, and requires auditing which
    other production drivers (764, 520, 916/916a, and any future driver
    enabling use_contextual_safety_terrain) should consume which signal.

    [GFLAG-0026, raised 2026-08-12T21:17:50Z, resolved 2026-08-13 governance]: two
    refinements to the 917 finding above, both already consistent with the
    SD-MECH303-THRESHOLD-SOURCING routing decision -- no status/confidence change.
    (1) V3-EXQ-917's own "damage_sourced is the production default" premise is
    corrected: of the 20 drivers that exercise this gate, only 2 (V3-EXQ-520,
    V3-EXQ-764) actually set limb_damage_enabled=True, and neither is a reviewed
    scored damage_sourced run; the other 18 (including V3-EXQ-916/916a, which 917's
    own docstring miscited) run under the proximity_ema_sourced default. (2) The
    reconciliation this correction feeds is written up in full in
    evidence/planning/mech303_contextual_safety_threshold_reachability.md
    (2026-08-12 addendum). Confirms the dedicated-signal routing already chosen
    remains correct: MECH-303 should not silently inherit limb_damage_enabled from
    unrelated driver config regardless of which sourcing mode happens to be more
    common in the corpus.

    [2026-08-16 governance apply | V3-EXQ-930 -> MECH-303 | failure_autopsy_V3-EXQ-930_2026-08-16] DISPOSITION: evidence_direction non_contributory. Do NOT apply the manifest's self-stamped evidence_direction 'supports' -- V3-EXQ-930 is a substrate-readiness validation that never enabled the MECH-303 gate; record the evidence_quality_note above, keep status provisional and pending_retest_after_substrate true, and stamp the claim's currently-absent epistemic_category -> standard

    [/failure-autopsy 2026-08-16, V3-EXQ-930, failure_autopsy_V3-EXQ-930_2026-08-16]: SUBSTRATE-READINESS VALIDATION, NOT MECHANISM EVIDENCE -- the manifest's self-stamped evidence_direction 'supports' is NOT applied to MECH-303. The run validates SD-MECH303-THRESHOLD-SOURCING (landed 2026-08-14, ree-v3 b257e7ad14) at the SIGNAL layer: under the production scenario limb_damage_enabled=True, the dedicated obs_dict['safety_proximity_harm'] channel is monotone across hazard density (per-density means 0.000/0.283/0.536/0.840/0.867, spread 0.331) and separable safe-vs-unsafe at every threshold from 0.02 to 0.30, best AUC 1.0 at tau=0.08 (reach 0.526), exceeding the 0.84 acceptance target; the SHIPPED default contextual_safety_proximity_threshold=0.25 sits inside that band (reach 0.648, AUC 1.0). But the MECH-303 gate itself was NEVER ENABLED (use_contextual_safety_terrain default False; contextual_safety_gate_source default 'z_harm_a'; the accumulate_safety block at agent.py:5166-5212 did not execute), the agent was UNTRAINED (fresh REEAgent per cell, no training loop), and no vigilance/avoidance readout was taken -- so the claim could not express itself and this run is non_contributory to it. The load-bearing criterion C1 is REAL and non-degenerate but NEAR-ANALYTIC: the channel is a deterministic env-computed hazard-proximity EMA, it is identically 0.0 at num_hazards=0, so reachability >= 0.5 is guaranteed for any tau>0 and the criterion can only fail on a wiring defect (which is what the driver says it is for). The self-routed label 'mech303_dedicated_signal_discriminates_zharma_does_not' asserts a DISSOCIATION but only its first half is load-bearing: the driver's own combination_rule declares the z_harm_a control NOT load-bearing and expected to fail. That negative half is a genuine measured invariance (per-density means 0.441964-0.441966, spread 6.96e-05 vs the same-run positive control's 0.331, ~4760x; best AUC 0.52; ~77-80% power against the 0.75 bar at n=20 vs 20) and reproduces V3-EXQ-917 and the already-adjudicated q086 ledger question -- but it is CONFOUNDED and does not establish that damage-sourced z_harm_a carries no context information: the encoder is untrained and its per-seed init offset (between-seed sd 6.4e-02) exceeds the within-run modulation (~1e-03) by ~60x, so an absolute-threshold sweep on the raw norm cannot discriminate whatever the signal carries; the within-seed std in fact rises MONOTONICALLY with hazard density (9.83e-04 -> 2.06e-03); and no manipulation check records that the damage pathway was exercised at all. Recording gap: the per-tick z_harm_a series and a damage-exposure readout existed at run time and were discarded, so a per-seed-standardised discrimination cannot be computed post hoc -- fold both into the recording spec of the owed retest per experimental_recording_standard_2026-07-12.md, do not re-run blind. BIOLOGY IS LOAD-BEARING AND SUPPORTIVE: anticipatory exteroceptive threat-proximity vs interoceptive tissue-damage is a genuine mammalian dissociation, so the damage-sourced null is what biology predicts and the 2026-08-12 option-(a) routing is vindicated. Status STAYS provisional; pending_retest_after_substrate STAYS true -- its stated condition ('once the dedicated signal lands') is now MET, so the owed MECH-303 behavioural retest is UNBLOCKED and remains unqueued. Re-derive brake does NOT fire (MECH-303 substrate_ceiling count 0 under R1-R3); granularity-debt trigger does NOT fire (0 tagging targets).
  pending_retest_after_substrate: false
  depends_on:
    - SD-011      # dual nociceptive streams (the suffering signal whose absence MECH-303 tracks)
    - SD-012      # homeostatic drive (anchors 'safety' as drive-baseline-not-rising over time)
    - ARC-007     # hippocampal path memory (substrate for context-bound safety encoding)
    - MECH-304    # sister claim: cue-specific conditioned inhibition pathway
  functional_restatement: >
    Mechanism hypothesis. MECH-303 covers one of two dissociable
    safety-learning systems identified by Laing et al. 2022 (7T fMRI,
    J Neurosci): the standard safety signal pathway. When a context
    or stimulus is passively associated with the absence of harm --
    without an explicit discrete teaching event -- a contextual
    safety representation forms in vmPFC/hippocampal substrate
    (vHipp-to-PL for storage; IL required for expression, per
    Kreutzmann 2020 and Meyer 2019).

    The store is structurally analogous to 'things liked' (the
    approach-attractor written by MECH-094 during goal-achievement)
    but anatomically and functionally separate. It encodes 'this
    environment predicts suffering will not rise' rather than 'this
    thing led to goal completion'. The two stores share downstream
    approach-policy machinery but have distinct update rules and
    different default extinction behaviours (contextual safety
    representations extinguish slowly, consistent with context-
    dependent safety memory persistence observed clinically).

    Primary output: lowers the background avoidance commitment
    threshold in familiar safe environments. This is the substrate
    for 'feeling safe at home' -- a diffuse reduction in vigilance
    tone driven by context, not by a specific cue signal.

    Cluster-level anatomical enrichment (2026-06-05, Silva et al.
    2021 Nat Neurosci): the safety cluster's expression machinery is
    prefrontal-hippocampal-THALAMIC, not prefrontal-hippocampal alone
    -- a midline-thalamic relay (nucleus reuniens -> BLA) actively
    transmits safety signals onto the fear circuit. Silva's causal
    evidence bears most directly on the sister claim MECH-304 (the
    active, cue-specific safety signal); for MECH-303 it is recorded
    as a cluster-level note that the contextual safety representation's
    downstream expression shares this thalamo-amygdalar relay. Silva
    also surfaces a remote-vs-recent (time-since-encoding) dependence
    -- the relay is recruited for extinction of REMOTE, consolidated
    fear; this axis is tracked on MECH-304 as a candidate third
    sub-mechanism and is orthogonal to the context-vs-cue axis that
    separates MECH-303 from MECH-304.

    Falsifiable: an REE substrate with MECH-303 enabled should show
    slower extinction of context-conditioned safety than cue-
    conditioned safety (MECH-304). MECH-303-ablated agents should
    fail to reduce background vigilance in repeatedly safe
    environments even when specific safety cues (MECH-304) are
    intact.

  notes: >
    Revised 2026-05-04: Laing et al. 2022 (J Neurosci 7T fMRI)
    dissociates conditioned inhibitors (cue-specific, teaches via
    discrete teaching signal -> dorsal striatum + dlPFC) from
    standard safety signals (contextual, passive accumulation ->
    vmPFC + hippocampus). Original MECH-303 conflated both systems.
    Revised to cover only the contextual/passive pathway. Cue-
    specific conditioned inhibition moved to MECH-304.

    Architectural failure mode prediction: if MECH-303 is degraded
    but MECH-304 is intact, the agent can use specific learned
    safety cues to suppress fear transiently but cannot reduce
    background vigilance in safe environments -- a substrate-level
    analogue of 'I know when to suppress fear but I never feel
    safe'. Clinical counterpart: PTSD patients who can cognitively
    identify safety but cannot achieve physiological calm.

    Enrichment 2026-06-05 (Silva et al. 2021 Nat Neurosci, DOI
    10.1038/s41593-021-00856-y; affect_stream_relief_safety_soothing
    lit-pull): added a cluster-level note that the safety-expression
    machinery is prefrontal-hippocampal-thalamic (nucleus reuniens ->
    BLA relay), and flagged the remote-vs-recent (time-since-encoding)
    dependence -- tracked on sister claim MECH-304 as a candidate
    third sub-mechanism. ENRICHMENT, not contradiction: MECH-303's
    vHipp-to-PL contextual store + IL expression anatomy stands; Silva
    bears most directly on the active cue-specific pole (MECH-304), so
    MECH-303 carries the lighter cluster note. The thalamic relay and
    remote/recent axis are UNIMPLEMENTED (documentation-only).

  implementation_note: |
    IMPLEMENTED 2026-05-04 as SD-052 (contextual_passive_substrate).
    ResidueField extended: safety_terrain_rbf_field (RBFLayer),
    accumulate_safety(), evaluate_safety(). Agent wired: sense()
    writes to terrain on harm-absent waking steps; select_action()
    reads terrain and releases beta_gate when threshold met.
    Config: REEConfig.use_contextual_safety_terrain (default False),
    contextual_safety_accum_weight (0.01), contextual_safety_harm_threshold
    (0.05), contextual_safety_release_threshold (1.0). All no-op at defaults.
    Validation experiment V3-EXQ-520 queued 2026-05-04.

    SD-MECH303-THRESHOLD-SOURCING landed 2026-08-14 (ree-v3 b257e7ad14): gives
    MECH-303's accumulate-safety gate a DEDICATED anticipatory hazard-proximity
    signal, decoupled from SD-022's damage-sourced z_harm_a. Addresses the
    V3-EXQ-917 finding (failure_autopsy_V3-EXQ-916-916a-917-920-fishtank-cluster
    _2026-08-12) that the live gate cannot discriminate safe-vs-unsafe under
    damage-sourcing (AUC <=0.52, chance) at any of 18 swept thresholds, while a
    proximity signal reaches AUC 0.84-0.97. Config (all no-op at defaults):
    REEConfig.contextual_safety_gate_source ("z_harm_a" default -> unchanged;
    "proximity_signal" -> dedicated signal), contextual_safety_proximity_threshold
    (0.25); env CausalGridWorldV2.safety_proximity_signal_enabled emits
    obs_dict["safety_proximity_harm"]. No status/confidence change: this is a
    substrate defect fix at the MECH-303-consumer seam, not new evidence. The owed
    behavioural retest (pending_retest_after_substrate) can now run through a gate
    that fires informatively. Validation experiment V3-EXQ-930 queued 2026-08-14.
    See docs/architecture/sd_mech303_threshold_sourcing.md.
  location: evidence/literature/targeted_review_connectome_mech_303/
  enrichment_anchors:
    - source: "Silva et al. 2021 Nat Neurosci 24(7):964-974"
      doi: "10.1038/s41593-021-00856-y"
      adds: "cluster-level note: safety-expression machinery is prefrontal-hippocampal-thalamic (nucleus reuniens -> BLA); remote-vs-recent dependence tracked on MECH-304"
      lit_entry: "evidence/literature/targeted_review_affect_stream_relief_safety_soothing/entries/2026-06-05_safety_reuniens_bla_remote_extinction_silva2021/"
      pull: "targeted_review_affect_stream_relief_safety_soothing"
      confidence: 0.80
      kind: enrichment_not_contradiction

- id: MECH-304
  title: "Cue-specific conditioned safety prediction: a discrete predictive structure encodes 'this stimulus actively predicts that suffering will be omitted', updated by explicit MECH-302 relief-completion events as its teaching signal. Encoding substrate is cortico-striatal (dorsal striatum + dlPFC). Expression/output pathway is IL-to-CeA. This is the conditioned-inhibition mechanism: when a specific cue co-occurs with relief, a separate learned prediction forms that can gate avoidance commitment release when the cue is later present. Sister claim to MECH-303 (contextual passive safety representation)."
  claim_type: mechanism_hypothesis
  subject: safety_prediction.cue_specific_conditioned_inhibition_substrate
  polarity: asserts
  status: active
  live_status:
    reading: active
    as_of: 2026-07-15
    needs_review: false
    evidence:
      from: "decision:MECH-304@2026-07-15T13:44:33.206848Z"
      as_of: 2026-07-15
      verdict: promote_to_active/applied
  implementation_phase: v3
  v3_pending: false
  claim_level: mechanistic
  registered_utc: 2026-05-04
  revised_utc: 2026-06-05
  evidence_quality_note: |
    2026-07-15 (gov): promoted provisional->active on V3-EXQ-763 (conditioned-inhibition
    behavioural falsifier, PASS/supports) -- the PRE-REGISTERED promote-to-active gate set
    when MECH-304 went provisional (2026-07-14, V3-EXQ-759 wall-independent isolation). Both
    load-bearing DVs passed: DV1 conditioned inhibition (safety cue lowers avoidance-commitment
    concurrent with a threat cue, store-necessity + cue-specificity gaps) AND DV2 MECH-303
    dissociation/sparing (store-OFF abolishes DV1 while use_contextual_safety_terrain spared);
    preconditions met (arm-A positive-control release 0.667 >= 0.34; 5/6 valid seeds >= 4).
    Two indexed genuine-exp supports (759 representation-level isolation + 763 behavioural),
    0 weakens, quadrant confirmed_established, exp_conf 0.825. Substrate path: SD-065
    controllable conditioned-safety cue channel + SD-066 common-mode-invariant (centered)
    store readout, both landed 2026-07-15 (byte-identical OFF).

    New candidate 2026-05-04. Primary lit support: Laing 2022 J
    Neurosci (conditioned inhibitor -> dorsal striatum + dlPFC;
    dissociated from standard safety signal -> vmPFC/hippocampus),
    Ng & Sangha 2023 Neuropsychopharmacol (IL->CeA required for
    conditioned safety expression; IL->BLA not required), Ng & Sangha
    2023 Cereb Cortex (IL single-unit encodes conditioned inhibitors
    specifically). Enrichment anchor 2026-06-05: Silva et al. 2021 Nat
    Neurosci 24(7):964-974 (DOI 10.1038/s41593-021-00856-y; nucleus
    reuniens -> BLA causally registers and transmits safety signals
    driving REMOTE fear extinction; conf 0.80, supports) adds the
    midline-thalamic transmission node + the remote-vs-recent
    (time-since-encoding) dependence. Substrate IMPLEMENTED 2026-05-04
    (SD-051, ConditionedSafetyStore). v3_pending=true until V3-EXQ-519
    validation PASS (conditioned-inhibition discriminative-pair gated
    by EXQ-517 PASS). The reuniens relay + remote/recent axis remain
    UNIMPLEMENTED (enrichment is documentation-only; see notes
    candidate-third-mechanism flag).
    [2026-07-14 governance] v3_pending CLEARED and status promoted
    candidate->provisional on the first indexed in-silico evidence:
    V3-EXQ-759 (cue-specific conditioned safety PREDICTION, PASS /
    supports), a wall-independent functional-signature isolation test of
    ConditionedSafetyStore.predict on a frozen action-free agent (6/6
    seeds above chance, positive control AUC 0.997, load-bearing C1
    non-degenerate). This is NECESSARY-NOT-SUFFICIENT for promote-to-
    active: 759 isolates the cue-discriminative predictive structure but
    does NOT run the pre-registered behavioural falsifier (the
    conditioned-inhibition discriminative-pair with a concurrent threat
    cue, the EXQ-519-family gate named above) or the MECH-303-sparing
    ablation. Those behavioural / dissociation tests remain the
    promote-to-active gate. See notes 2026-07-14 governance block.
  depends_on:
    - MECH-302    # provides the teaching signal: the relief-completion event that trains the MECH-304 store
    - MECH-094    # hypothesis-tag write gate (the tag from MECH-302 is the MECH-304 update trigger)
    - SD-011      # dual nociceptive streams (the suffering signal whose omission MECH-304 predicts)
    - SD-012      # homeostatic drive (baseline against which suffering-omission is computed)
    - MECH-303    # sister claim: contextual passive safety pathway; MECH-303 + MECH-304 are the minimal two-system count
  functional_restatement: >
    Mechanism hypothesis. MECH-304 covers the second of two
    dissociable safety-learning systems identified by Laing et al.
    2022: the conditioned inhibitor pathway. When a specific cue
    co-occurs with a MECH-302 relief-completion event, a cue-specific
    predictive store updates: 'this cue predicts suffering will be
    omitted'. The encoding step recruits dorsal striatum + dlPFC
    (Laing 2022 conditioned inhibitor data); the expression/output
    pathway requires IL-to-CeA (Ng & Sangha 2023 Neuropsychopharmacol,
    chemogenetic circuit dissection). This is the conditioned-
    inhibition substrate: the specific cue that co-occurred with
    relief onset drives a learned prediction that overrides ongoing
    avoidance commitment when the cue is present.

    Anatomical enrichment (2026-06-05, Silva et al. 2021 Nat Neurosci):
    the safety-transmission/expression pathway includes a midline-
    THALAMIC relay -- nucleus reuniens (NRe) -> basolateral amygdala
    (BLA) -- in addition to the IL-to-CeA output named above. Silva
    provides causal, bidirectional evidence (fiber photometry +
    chemogenetics + closed-loop optogenetics) that NRe registers and
    actively TRANSMITS safety signals to the BLA: NRe-to-BLA activity
    rises shortly before freezing cessation, pathway-specific inhibition
    impairs and activation facilitates the suppression of an established
    threat representation. This is the cleanest available demonstration
    that cue-specific safety prediction is its own functional system
    that acts ON the fear circuit (not a byproduct of low harm, not the
    reward-offset reinforcement of relief). In REE terms the reuniens
    relay is an additional node on the safety-EXPRESSION pathway that
    feeds the commitment-release gate (output 1 below), parallel to the
    IL-to-CeA route.

    Time-since-encoding (remote-vs-recent) dependence: the NRe->BLA
    relay is recruited specifically for the extinction of REMOTE
    (systems-consolidated, ~30-day) aversive memories and NOT recent
    (~1-day) ones. This is a NEW functional axis orthogonal to the
    context-vs-cue axis that separates MECH-303 from MECH-304: it
    indexes how CONSOLIDATED the aversive memory being suppressed is,
    not whether the safety signal is contextual or cue-specific.
    Recent-memory safety expression is handled by the existing
    MECH-303/304 expression machinery; suppressing an already-
    consolidated fear appears to require this distinct thalamo-
    amygdalar relay. This axis is NOT yet operationalised in REE; see
    the candidate-third-mechanism flag in notes.

    MECH-304 is cue-specific and event-driven, in contrast to
    MECH-303 which is context-diffuse and passive. The two systems
    dissociate behaviourally: MECH-304 supports the classical
    conditioned inhibition phenomenon (a safety cue inhibits fear
    even in the presence of a threat cue), while MECH-303 supports
    background vigilance reduction in safe environments.

    Primary outputs:
    1. Commitment-release gating: when the MECH-304-encoded cue is
       present, the threshold for dropping an active avoidance
       commitment lowers -- the IL-to-CeA pathway provides the
       downstream signal. This is the substrate for 'stop being
       afraid when the safety signal appears'.
    2. Approach toward safety-signalling cues: the cue that reliably
       predicts relief onset becomes an approach attractor in its own
       right (distinct from MECH-303's contextual safety tone and
       from MECH-302's approach toward relief-paired stimuli).

    Falsifiable: an REE substrate with MECH-304 enabled (and
    MECH-302 as its teaching signal) should show classical conditioned
    inhibition -- a learned safety cue suppresses avoidance even when
    presented concurrently with a threat cue. MECH-304 ablation should
    preserve contextual safety (MECH-303 intact) but should abolish
    cue-specific safety inhibition of fear.

  notes: >
    Registered 2026-05-04 from the MECH-303/MECH-304 split. Original
    MECH-303 conflated contextual and cue-specific safety systems.
    Laing et al. 2022 (J Neurosci 7T fMRI) provides the load-bearing
    dissociation: conditioned inhibitors (MECH-304 pathway) recruit
    dorsal striatum + dlPFC during encoding, not vmPFC/hippocampus.
    The vmPFC/hippocampus finding is MECH-303's contextual pathway.

    The IL involvement (Kreutzmann 2020, Ng & Sangha 2023) is now
    attributed to MECH-304's expression pathway (IL-to-CeA is the
    output gate for conditioned safety expression). This reconciles
    the rodent IL necessity findings with the human fMRI dorsal
    striatum encoding finding: encoding and expression dissociate
    at the circuit level.

    Architectural failure mode prediction: if MECH-304 is degraded
    but MECH-303 is intact, the agent can feel safe in familiar
    environments but cannot learn to use specific safety cues to
    suppress fear on-demand -- a substrate-level analogue of 'I feel
    safe at home but I cannot use safety signals to calm down'. This
    maps onto a distinct PTSD phenotype from MECH-303 failure.

    Enrichment 2026-06-05 (Silva et al. 2021 Nat Neurosci, DOI
    10.1038/s41593-021-00856-y; affect_stream_relief_safety_soothing
    lit-pull): added the midline-thalamic relay (nucleus reuniens ->
    BLA) to the safety-expression pathway and the remote-vs-recent
    (time-since-encoding) dependence to the functional_restatement.
    This is ENRICHMENT, not contradiction -- the existing cortico-
    striatal encoding / IL-to-CeA expression anatomy stands; the
    reuniens relay is an additional, causally-demonstrated transmission
    node, and the remote/recent axis is a new dimension neither
    MECH-303 nor MECH-304 previously named. Silva most directly bears
    on MECH-304 (the active, cue-specific safety signal that acts ON
    the fear circuit); MECH-303 carries a lighter cluster-level note.

    CANDIDATE THIRD SUB-MECHANISM (flagged, NOT yet registered): the
    consolidation-age axis -- "extinguishing an already-consolidated
    (remote) aversive memory requires a thalamo-amygdalar relay that
    recent-memory safety expression does not" -- is orthogonal to the
    context (MECH-303) vs cue (MECH-304) axis and is a candidate for a
    future third safety sub-mechanism that would interface REE's
    consolidation/replay machinery (MECH-092 SWR replay; sleep cluster)
    with the safety-expression pathway. It is deliberately NOT minted
    as a claim now: the evidence is a single rodent anchor (Silva 2021,
    conf 0.80), it is off the V3 critical path, and there is no
    consolidation-interface substrate design yet. Promote to a third
    claim only when (a) a second independent anchor lands and (b) a
    consolidation-state-conditional safety-expression design exists.
    Guards the over-split mirror of the philosophy-right/mechanism-wrong
    failure mode (feedback_biology_before_formal_definitions).

    [2026-07-14 governance -- promote candidate->provisional, v3_pending
    cleared]: V3-EXQ-759
    (v3_exq_759_mech304_cue_specific_conditioned_safety_prediction_20260714T201108Z_v3)
    is the FIRST indexed in-silico evidence for MECH-304. PASS /
    evidence_direction supports. It is a WALL-INDEPENDENT
    functional-signature ISOLATION test: the DV is a read-only
    ConditionedSafetyStore.predict() scalar on a FROZEN, action-free
    agent (random rollouts are only a world-state sampler; no committed
    action / goal / training), so it is independent of the competence
    wall (GAP-I; precedent functional-signature DVs 447/448/455). Result:
    load-bearing C1 cue-discrimination AUC is non-degenerate and above
    chance on all 6 valid seeds (mean AUC 0.739, min 0.619), the
    orthogonal-class positive control AUC is 0.997, and all four
    readiness preconditions are met (positive_control_auc>=0.9,
    n_valid_seeds>=3, store prototype populated, predict spread
    non-degenerate). This clears the "no in-silico evidence" precondition
    behind the v3_pending hold. NOT-YET-CLEARED for promotion-to-active:
    this isolation test measures cue-specific PREDICTION discriminability
    only -- it does NOT exercise the integrated closed-loop agent or the
    pre-registered behavioural falsifier (classical conditioned
    inhibition: a learned safety cue suppresses avoidance when presented
    CONCURRENTLY with a threat cue), nor the ablation dissociation
    (MECH-304 ablation should abolish cue-specific safety inhibition while
    sparing MECH-303 contextual safety). Those behavioural /
    dissociation tests remain the separate promote-to-active gate. This
    disposition mirrors the MECH-287/288 candidate->provisional promote
    (2026-07-14, V3-EXQ-757), the sibling functional-signature
    isolation precedent.

  implementation_note: |
    SD-051 IMPLEMENTED 2026-05-04. ConditionedSafetyStore in
    ree_core/safety/conditioned_safety_store.py. Config:
    REEConfig.use_conditioned_safety_store (default False, bit-identical OFF).
    sense() updates EMA prototype on MECH-302 event ticks;
    select_action() gates beta_gate release when cosine similarity >
    safety_store_threshold (IL->CeA expression pathway). V3-EXQ-519
    substrate-readiness diagnostic queued 2026-05-04. V4-deferred:
    approach attractor (requires MECH-163 V3 completion gate) and
    contrastive cue-specific learning (requires trainable encoder head).
    See sd_051_conditioned_safety_store.md and v3_v4_transition_boundary.md.
  location: evidence/literature/targeted_review_connectome_mech_303/
  enrichment_anchors:
    - source: "Silva et al. 2021 Nat Neurosci 24(7):964-974"
      doi: "10.1038/s41593-021-00856-y"
      adds: "midline-thalamic relay (nucleus reuniens -> BLA) on the safety-expression pathway; remote-vs-recent (time-since-encoding) dependence"
      lit_entry: "evidence/literature/targeted_review_affect_stream_relief_safety_soothing/entries/2026-06-05_safety_reuniens_bla_remote_extinction_silva2021/"
      pull: "targeted_review_affect_stream_relief_safety_soothing"
      confidence: 0.80
      kind: enrichment_not_contradiction

- id: MECH-305
  title: "z_harm_a requires a controllability-modulated weighting term beyond MECH-219 temporal hysteresis: perceived uncontrollability or inescapability amplifies the affective load integrator, while perceived controllability suppresses it. Biological locus is dorsal ACC, which integrates noxious stimulus input with controllability context and projects to the medial suffering pathway (ACC/insula, overlapping with salience network)."
  claim_type: mechanism_hypothesis
  subject: harm_stream.controllability_weighted_affective_load
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-05-04
  depends_on:
    - MECH-219   # temporal hysteresis integrator that MECH-305 extends
    - SD-011     # dual nociceptive streams: z_harm_a is the stream being modulated
    - SD-032     # salience-network coordinator: medial suffering pathway overlaps with salience network (De Ridder 2021)
  functional_restatement: >
    MECH-219 implements temporal hysteresis: z_harm_a accumulates harm
    events over time and decays slowly. The Q-036 lit-pull (2026-04-20)
    establishes this is necessary but not sufficient. Suffering has an
    additional determinant -- perceived controllability or escapability
    of the harm source.

    MECH-305 asserts that z_harm_a's effective magnitude should be
    gated or weighted by a controllability estimate: when harm is
    perceived as controllable or escapable, the integrator's gain is
    suppressed. When harm is perceived as uncontrollable or inescapable,
    the gain is amplified. Biological evidence: Salomons 2004 (identical
    stimuli produce different dorsal ACC/insula activation depending
    solely on perceived controllability) and Loffler 2018 (three-way
    behavioural dissociation: controllability reduces suffering but not
    intensity or unpleasantness).

    In REE substrate terms: the controllability estimate reads from
    the agent's recent action-outcome history -- specifically, whether
    attempted avoidance actions reliably reduce harm (high
    controllability, gain suppressed) or fail to do so (low
    controllability, gain amplified). This connects to SD-032 (salience
    coordinator) because the medial suffering pathway overlaps with the
    salience network, which already coordinates mode-switches (MECH-259).

    Falsifiable: an REE substrate with MECH-305 enabled should show
    higher z_harm_a values (and stronger avoidance urgency) when the
    agent's avoidance actions have been persistently ineffective,
    relative to an identically harmed agent whose avoidance actions
    reliably work. MECH-305-ablated agents should be equally loaded
    in both conditions (pure temporal integration).
  notes: >
    Registered 2026-05-04 by narrowing Q-036 (what additional variables
    beyond temporal integration are required for z_harm_a as a distinct
    load state?). Q-036 lit-pull identified controllability as the
    strongest-evidenced variable. Q-036 remains open for persistence,
    recovery failure, inescapability, and prediction error, which were
    less directly isolated.

    Implementation path: a sliding-window controllability estimate
    (n_avoidance_actions_that_reduced_harm / n_avoidance_actions_attempted)
    over recent episodes could serve as the gate. This may read from
    SD-032 (salience coordinator already tracks action-outcome contingency
    at coarse level via MECH-259 mode switch).

    Second lit-pull recommended before implementation: inescapability /
    learned helplessness (Maier & Watkins; Amat et al.) and pain
    prediction error (Fazeli & Buchel 2018). Per biology-before-formal-
    definitions protocol.
  evidence_quality_note: |
    Narrowed from Q-036 2026-05-04. Lit-pull anchors (targeted_review_q_036, 2026-04-20):
    Salomons 2004 JNeurosci (fMRI n=16, controllability -> dorsal ACC/insula activation
    change without stimulus change; conf=0.75, supports). Loffler 2018 Pain Reports
    (behavioural, three-way dissociation intensity/unpleasantness/suffering; controllability
    selectively reduces suffering; conf=0.72, supports). De Ridder 2021 Neurosci Biobehav
    Rev (medial/lateral pathway dissociation; suffering pathway = salience network; acute-to-
    chronic transition implicates inescapability; conf=0.70, supports). Aggregate lit_conf ~0.808
    (three supports, zero weakens). No experimental evidence yet. Second lit-pull recommended
    for inescapability (Maier & Watkins) and pain prediction error (Fazeli & Buchel) before
    V3 implementation.
  location: evidence/literature/targeted_review_q_036/

- id: MECH-306
  title: "z_goal seeding under SD-012 requires sustained drive at contact, not instantaneous homeostatic drive alone: when the agent is well-fed, drive_level collapses at consumption and anti-correlates with benefit_exposure, so effective_benefit cannot cross the seeding threshold unless drive is floored or slow-traced."
  claim_type: mechanism_hypothesis
  subject: goal_seeding.sustained_drive_trace
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_627_mech306_sustained_drive_trace_validation_20260602T172121Z_v3
      as_of: 2026-06-02
      verdict: supports/PASS
  implementation_phase: v3
  promoted_utc: "2026-06-03"
  claim_level: mechanistic
  registered_utc: 2026-05-20
  depends_on:
    - SD-012
    - MECH-216
  functional_restatement: >
    SD-012 modulates z_goal seeding via effective_benefit = benefit_exposure *
    (1 + drive_weight * drive_signal). The pre-amendment form used instantaneous
    drive_level = 1 - agent_energy, which collapses toward zero the step a resource
    is consumed (energy resets). That forces drive and benefit to anti-correlate
    at exactly the contact events where seeding must fire (EXQ-536a: mean drive on
    contact ~0.005, multiplier ~1.01, max_effective_benefit < benefit_threshold).

    MECH-306 names the sustained-drive substrate fix: drive_signal must integrate
    or floor above a minimum tonic wanting level independent of the consummatory
    pulse. Two landed implementations in GoalState.update() (goal_pipeline:GAP-3):
    (1) drive_ema_alpha < 1.0 slow trace on drive_level (Option 1); (2)
    drive_floor = max(drive_level, floor) before the trace/multiplier (Option 2,
    insatiability floor per Berridge sustained wanting). Default OFF preserves
    bit-identical pre-amendment behaviour (drive_ema_alpha=1.0, drive_floor=0.0).

    Discriminative outcome (2026-05-17/19): V3-EXQ-582 Option-1 EMA sweep FAIL
    (all alphas incl. 0.01: drive input near-zero throughout episode). V3-EXQ-582a
    Option-2 floor sweep PASS at drive_floor=0.9 (A1-A4): mean effective benefit
    at contact 0.115 > 0.08; 3/3 seeds fire seeding; z_goal active fraction 0.081;
    OFF arm zero seedings (falsifier holds). Option 2 is the validated path for
    goal-pipeline GAP-3 closure; cascade behavioural retests (GAP-4) should enable
    drive_floor in the StepHarness cohort unless a combined floor+EMA arm is
    explicitly pre-registered.
  notes: >
    Registered 2026-05-20 on goal_pipeline:GAP-3 closure (V3-EXQ-582a PASS).
    Empirical anchor: EXQ-536a (H_b_threshold_never_crossed under instantaneous
    drive). Design memos: sustained_drive_anticipatory_wanting.md,
    goal_pipeline_plan.md Phase 3. Lit anchors per
    wanting_liking_sleep_consolidation_synthesis.md (Berridge/Robinson sustained
    anticipatory wanting; Schultz/Aponte/Livneh slow hunger drive; aggregate
    lit_conf ~0.78 on the SD-012 sustained-drive direction). Does not promote
    SD-012 or MECH-295 -- substrate-readiness for the drive-multiplier site only.
    MECH-216 schema-driven anticipatory wanting (Option 3) remains the escalation
    if floor+EMA fail in future regimes (e.g. infant persistent agent EXQ-588b).
  evidence_quality_note: |
    2026-06-03 (governance): PROMOTED candidate_substrate_landed -> provisional; v3_pending CLEARED. The manual v3_pending gate's purpose (await a V3 evidence-grade run) is satisfied by V3-EXQ-627 (v3_exq_627_mech306_sustained_drive_trace_validation_20260602T172121Z_v3): experiment_purpose=evidence, claim_ids=[MECH-306], outcome PASS, evidence_direction=supports, exp_conf=0.773 (above the 0.62 promotion threshold), reviewed 2026-06-03. 627 is the 2-arm drive_floor ablation (SUSTAINED 0.9 vs OFF 0.0) that confirms the sustained-drive-trace mechanism the predecessor diagnostic 582a only demonstrated (582a was claim_ids=[]/non_contributory). Promoted to PROVISIONAL not active: single genuine experiment (genuine_exp_count=1, lit_conf=0.0, novel_discovery quadrant); full active deferred pending replication / breadth. implementation_phase:v3 retained as a factual record -- it no longer gates (the indexer holds on implementation_phase=v3 only when zero V3 runs exist; 627 is a V3 run).
    [goal_pipeline:GAP-3 closure 2026-05-20] EXQ-536a diagnostic anchor: drive x
    benefit anti-correlation at contact (mean drive ~0.005, seeding never crosses
    threshold). V3-EXQ-582 FAIL (Option 1 EMA, all 12 runs): drive_trace_at_contact
    ~0.0002-0.005; EMA cannot lift a consistently low input; post_warmup_cut=100
    had zero contacts. V3-EXQ-582a PASS (Option 2 drive_floor, 2026-05-19T01:45Z,
    non_contributory diagnostic, claim_ids=[]): floor=0.9 arm mean_eff_benefit=0.115,
    3/3 seeds n_seedings>0, z_goal_active_fraction=0.081; OFF arm 0 seedings.
    Monotone floor sweep 0.0->1.2. Substrate: GoalConfig.drive_ema_alpha +
    GoalConfig.drive_floor in ree_core/goal.py; contracts test_sustained_drive_ema_gap3.py
    and test_drive_floor_gap3_opt2.py. Behavioural cascade (MECH-295 / GAP-4 Tier-1)
    not yet re-run with floor enabled -- v3_pending retained.
  location: docs/architecture/sustained_drive_anticipatory_wanting.md
  suggested_experiment_type: diagnostic

- id: MECH-307
  title: "Anticipatory positive affect (excitement) and anticipatory negative affect (dread) emerge as derived states from the conjunction of (a) signed VALENCE_SURPRISE, (b) MECH-216 schema readout coupled to z_beta arousal, (c) anticipatory VALENCE_LIKING write at predicted-imminent-positive-event locations, and (d) MECH-216 writes at predicted rather than current location. Four-gap substrate amendment to MECH-216 + MECH-205 + the residue valence buffer, NOT a new VALENCE channel."
  claim_type: mechanism_hypothesis
  subject: affect.anticipatory_conjunction_architecture
  polarity: asserts
  status: candidate_substrate_landed
  live_status:
    reading: candidate_substrate_landed/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_EXQ-539-540_MECH307_2026-05-17#V3-EXQ-540f"
      as_of: 2026-05-17
      verdict: superseded/measurement_design_broken_criterion
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-05-08
  depends_on:
    - SD-014       # the residue valence vector this amends
    - MECH-216     # E1 predictive wanting / schema readout (the upstream signal)
    - MECH-205     # surprise-gated replay write path (consumer of signed VALENCE_SURPRISE)
    - MECH-093     # z_beta modulates E3 heartbeat rate (target of new arousal coupling)
    - MECH-111     # curiosity / novelty drive (likely upstream-blocked by same wiring gaps)
    - MECH-292     # ranked ghost-goal bank (downstream consumer of conjunction)
  functional_restatement: >
    The 2026-05-08 lit-pull on excitement-as-5th-channel
    (evidence/literature/targeted_review_excitement_5th_valence_channel/, lit_conf 0.77 across
    9 entries) established that anticipatory positive arousal at reward cues (NAcc-anticipation
    in MID; Knutson 2001a et seq.) is neurally distinct from VALENCE_WANTING (directional
    motivation), VALENCE_LIKING (consummatory hedonic), and z_beta arousal (timing modulation).
    The naive verdict of that lit-pull was REGISTER VALENCE_EXCITEMENT as a 5th channel in
    SD-014. On code-level inspection of the V3 substrate, the better answer is that excitement
    SHOULD emerge as a derived conjunction of existing channels but currently does not because
    of four specific wiring gaps:

    Gap 1 -- VALENCE_SURPRISE is unsigned magnitude. agent.py:3075-3077 writes
    `surprise = max(0.0, pe_mag - pe_ema)` to the residue field. Sign information is discarded
    at the write site. Positive PE (good thing happened unexpectedly -- the dopamine RPE that
    underlies excitement) and negative PE (bad thing happened unexpectedly -- the habenula
    signature of dread) are collapsed identically. Fix: store signed PE in VALENCE_SURPRISE,
    or split into VALENCE_POSITIVE_SURPRISE and VALENCE_NEGATIVE_SURPRISE.

    Gap 2 -- MECH-216 schema readout writes only to VALENCE_WANTING. agent.py:3753-3757 writes
    a single channel when schema_salience fires above threshold. Biology's NAcc-anticipation
    drives sympathetic activation jointly with wanting amplification and partial anticipatory
    hedonic activation. Fix: MECH-216 also writes a partial VALENCE_LIKING amplitude AND raises
    z_beta arousal proportional to schema_salience.

    Gap 3 -- z_beta has no input path from cue prediction. z_beta is encoded from observation
    and read by MECH-093 to modulate E3 rate. There is no path from MECH-216 schema readout to
    z_beta amplitude. Fix: add z_beta arousal write site driven by schema_salience (subsumed
    in Gap 2 fix architecturally; named separately because the z_beta path is a distinct
    consumer).

    Gap 4 -- MECH-216 writes at the agent's current z_world, not at the predicted-imminent
    location. agent.py:3754 writes to `self._current_latent.z_world`. Fix: use E1's forward
    prediction of z_world to identify the predicted-reward location and write VALENCE_WANTING +
    VALENCE_LIKING + (via z_beta) the conjunction signature at that location. This is what
    NAcc-anticipation marks in biology -- the predicted goal location, not the current location.

    Under the four-gap fix, "excitement" emerges as the joint occurrence in z_world space of
    high VALENCE_WANTING + high VALENCE_LIKING + high VALENCE_POSITIVE_SURPRISE + high z_beta
    arousal at the predicted location. A consumer (MECH-205 replay write priority, MECH-292
    ghost_priority, MECH-279 PAG freeze gate) can detect this conjunction by joint-reading the
    four channels. "Dread" emerges symmetrically as high VALENCE_HARM_DISCRIMINATIVE +
    VALENCE_NEGATIVE_SURPRISE + z_beta arousal at predicted-harm locations.

    Falsifiable: an REE substrate with all four gaps fixed should produce (a) Adcock 2006
    prediction: locations with high anticipatory-conjunction-state at encoding receive >= 1.5x
    replay frequency in MECH-285 SleepReplaySampler vs uniform-priority baseline; (b) MECH-292
    ghost-priority elevation at conjunction-state locations relative to staleness-only
    baseline; (c) clean Knutson 2001a-style separation of "anticipation" residue writes
    (predicted location, conjunction-state) vs "outcome" residue writes (current location,
    consummatory VALENCE_LIKING).
  notes: >
    Registered 2026-05-08 as the parsimonious alternative to the SD-014 amendment (6-component
    valence vector with VALENCE_EXCITEMENT + VALENCE_DREAD as 5th + 6th channels, lit-pull
    verdict). The conjunction-fix reading is more biologically faithful: biology does not have
    a VALENCE_EXCITEMENT neuron type; the excitement signal measured in NAcc fMRI is the
    anatomical convergence of DA RPE + hippocampal place-cell preplay + ANS arousal at one
    structure. REE has all the upstream pieces (E1 PE, MECH-216 schema readout, z_beta) but
    not the wiring between them. Fixing the wiring is ~40 lines of code, all backward-compatible
    behind config flags. Adding new channels would paper over the wiring gaps rather than
    diagnose them.

    The same four wiring gaps likely explain why MECH-111 (curiosity / novelty drive) failed
    at EXQ-141 (2026-03-29). MECH-111's commit-threshold-modulation reading depends on an
    upstream signal of "novel-positive-event-imminent" that the substrate cannot currently
    produce because Gap 1 (sign-collapsed VALENCE_SURPRISE) and Gap 3 (z_beta uncoupled
    from schema readout) prevent the conjunction-state from forming. EXQ-141 was therefore
    likely substrate-confounded rather than architecturally falsifying. EXQ-141b on the
    corrected substrate is the appropriate retest (see also Q-041 threshold supervisor cluster).

    Relationship to the SD-014 amendment proposal (6-channel expansion):
    - MECH-307 is the FIRST-LINE proposal: fix wiring, let excitement and dread emerge as
      conjunction-states from the existing 4 channels.
    - SD-014 amendment to 6 channels is the FALLBACK if MECH-307's conjunction-state cannot
      be reliably detected by downstream consumers, or if the bookkeeping of joint reads at
      every consumer site proves too dispersed in practice.
    - Both proposals share the same lit base (targeted_review_excitement_5th_valence_channel/);
      the architectural choice is empirical, not lit-determined.

    Implementation prerequisites:
    - EXQ-537 (SD-029 single-pass comparator) lands first to clear the currently-running
      commit-chain diagnostic queue before introducing new substrate.
    - EXQ-141b (MECH-111 retest on corrected commit-chain substrate) lands to clear the
      curiosity-drive question.
    - Then queue EXQ-XXX (MECH-307 conjunction-fix discriminative pair, ~3-4h on Mac).

    Architectural cost estimate: ~40 lines across goal.py / e1_deep.py / residue/field.py /
    agent.py (signed PE storage, MECH-216 multi-channel write, MECH-216 -> z_beta coupling,
    write-at-predicted-location). All backward-compatible behind config flags
    (use_mech307_conjunction default False).

    Write site contracts (proposed):
      gap1_signed_surprise: residue/field.update_valence accepts negative values for
        VALENCE_SURPRISE under config.surprise_signed=True; consumers read magnitude or sign
        as needed.
      gap2_mech216_multi: when schema_salience > threshold and use_mech307_conjunction=True,
        MECH-216 writes to all of {VALENCE_WANTING, VALENCE_LIKING_anticipatory, z_beta_arousal_pulse}
        proportional to (schema_salience x drive_level x prediction_certainty).
      gap3_z_beta_coupling: subsumed into gap2; specifically z_beta is incremented (not
        replaced) by schema_pulse * z_beta_schema_gain.
      gap4_predicted_location: MECH-216 receives an optional z_world_predicted argument from
        E1's forward pass; when supplied, writes target z_world_predicted instead of current
        z_world. Falls back to current z_world when prediction not available.

    Architecture doc:
    docs/architecture/anticipatory_affect_conjunction_vs_dual_channel.md
  evidence_quality_note: |
    Lit anchors (cross-referenced from targeted_review_excitement_5th_valence_channel/, 2026-05-08):
    Knutson 2001a J Neurosci (NAcc anticipation = self-reported happiness, positive-valence-
    selective; conf=0.85). Knutson 2001b Neuroreport (anticipation/outcome anatomical
    dissociation; conf=0.78). Adcock 2006 Neuron (anticipatory NAcc activation predicts
    subsequent memory formation 24h later; conf=0.82 -- the load-bearing finding for
    MECH-307's tie-in to MECH-205 sleep-replay-priority). Bromberg-Martin Hikosaka 2010
    Neuron (DA value vs salience review; conf=0.74 -- mixed direction, supports multi-channel
    anticipatory affect but offers the alternative architectural reading). Bromberg-Martin
    2010b Neuron (tonic vs phasic anticipatory dissociation in primate single-unit; conf=0.80).
    Berns 2006 Science (dread is its own construct, dissociable from fear/anxiety; conf=0.78
    -- supports symmetric VALENCE_NEGATIVE_SURPRISE conjunction). Posner Russell 2009 Hum
    Brain Mapp (circumplex valence x arousal as separate neural networks; conf=0.72 --
    representational adequacy argument for conjunction). Knutson 2008 depression and Johnson
    2019 bipolar (clinical channel-separability evidence, conf=0.72 / 0.74). Aggregate lit_conf
    0.77 across 9 entries (8 supports, 1 mixed, 0 weakens). No experimental evidence yet on the
    REE substrate. EXQ-XXX (MECH-307 conjunction-fix discriminative pair) queued behind
    EXQ-537 + EXQ-141b clearance.

    [2026-05-08 governance]: hold_pending_v3_substrate applied (v3_pending=true; pipeline gate).
    EXQ-539 (substrate-readiness check) ran 2026-05-08 with mixed direction on MECH-307 (substrate
    counters dissociated -- liking_writes / z_beta_excursion / neg_surprise_centers all fired in
    ARM_ON only, but approach_commit_rate stayed at 1.0 in both arms because the conjunction
    signal had no downstream consumer that could read it). The 2026-05-08 follow-on landed the
    MECH295LikingBridge.compute_conjunction_score_bias() consumer (ree-v3 6cd0962, behind
    use_mech307_consumer_conjunction_read flag), and EXQ-540 (3-arm gap decomposition
    ARM_0_off / ARM_1_signed_pe / ARM_2_full with the consumer ON in all arms) is queued as
    the discriminative validation. Hold remains in force until EXQ-540 runs.

    [2026-05-11 implementation_note]: status candidate -> candidate_substrate_landed.
    Gap 1 Option-b (split channels) landed per user override 2026-05-11 over the design-doc
    default Option-a (signed single channel). residue/field.py: VALENCE_DIM 4 -> 6 with new
    constants VALENCE_POSITIVE_SURPRISE=4 and VALENCE_NEGATIVE_SURPRISE=5. agent.py MECH-205
    PE write site dispatches three paths (split / signed / true-legacy). config.py:
    use_mech307_split_surprise (Option-b flag) + use_mech307_conjunction (convenience master
    flag whose __post_init__ resolver propagates to the three substrate-side sub-flags
    use_mech307_split_surprise / use_mech307_schema_multichannel /
    use_mech307_predicted_location_write). Gaps 2, 3, 4 substrate-landed 2026-05-08 unchanged.
    309/309 contracts + 7/7 preflight PASS with master OFF (bit-identical OFF guarantee
    verified). Direct field-level smoke verified VALENCE_DIM=6 buffer allocation, split-channel
    write routing (POSITIVE on harm_signal >= 0, NEGATIVE on harm_signal < 0, magnitude
    preserved on legacy VALENCE_SURPRISE for backward-compat MECH-205 / SD-014 consumers),
    MECH-094 hypothesis_tag gate respected. Plan-of-record goal_pipeline_plan.md GAP-1 status
    open -> done; Phase 2 (GAP-2 SD-049 Phase 2 behavioural validation under MECH-307-fixed
    substrate) unblocked. v3_pending remains True; behavioural validation (4-arm discriminative
    pair) deferred to a /queue-experiment session per the 2026-05-11 user "substrate only first"
    directive. SD-014 6-channel amendment retained as registered fallback if the conjunction
    reading fails behavioural validation. See REE_Working/ree-v3/CLAUDE.md "MECH-307
    Anticipatory Affect Conjunction Architecture (2026-05-11)" for the full SD-Implemented
    entry.

    [2026-05-21 IGW-20260521-023 substrate-ready]: substrate_queue MECH-307 entry closed
    (implementation_status=implemented, ready=false). Canonical validation V3-EXQ-540g PASS
    2026-05-15 (all_pass=true; C1 liking_lift 9x, C2 conjunction fires ARM_2 only; C3
    approach_commit secondary FAIL). 26/26 MECH-307 contract tests PASS 2026-05-21. v3_pending
    retained on MECH-307 and downstream claims until GAP-2/GAP-4 behavioural cohorts (514j,
    490g) run under MECH-307-fixed substrate. SD-014 6-channel fallback unchanged.
  experimental_test: >
    4-arm discriminative pair on a CausalGridWorldV2 task with reward cues at predictable
    locations: ARM_0_off (current substrate, all 4 wiring gaps unfixed); ARM_1_signed_PE_only
    (Gap 1 fix only); ARM_2_full_conjunction (all 4 fixes); ARM_3_alternative_5channel
    (SD-014 6-channel amendment, VALENCE_EXCITEMENT + VALENCE_DREAD added as discrete writes
    by MECH-216). Three pre-registered acceptance criteria: (C1) conjunction-state detection
    rate at predicted-reward locations: ARM_2 >= 0.7, ARM_0 < 0.2; (C2) Adcock 2006 prediction:
    sleep-replay frequency at conjunction-state locations / uniform-priority baseline >= 1.5x
    in ARM_2; (C3) ARM_2 vs ARM_3 head-to-head: probe accuracy on subsequent identity
    discrimination tasks (analog of EXQ-538 SD-049 Phase 2) is comparable, but ARM_2 uses
    fewer net residue-field writes. PASS = all three. PARTIAL PASS (C1 + C2 only) routes to
    SD-014 amendment fallback.
  location: evidence/literature/targeted_review_excitement_5th_valence_channel/

- id: MECH-308
  title: "Language acquisition tracks play_mode maturation: socially-grounded compositional language emerges as a specialised game type instantiated within play_mode (INV-058 / MECH-194 / ARC-049), not via a parallel language-acquisition substrate. The basic expression catalog (play pose, head tilt, laughter, mutual gaze, distress vocalisation, approach cue, vocal mimicry, pointing) acts as the bilateral frame-opening signal; conversational violations function as synthetic z_harm; utterance-as-goal acts as synthetic z_goal; proto-symbols accrete as conditional modifiers attached to basic-expression channels. Predicts that systems trained without play_mode-like episodes fail to acquire socially-grounded compositional language even when pattern-matching imitation succeeds."
  claim_type: mechanism_hypothesis
  subject: language.acquisition_tracks_play_mode
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-308@2026-06-06T07:53:48.739107Z"
      as_of: 2026-06-06
      verdict: held_v4_by_architectural_commitment/applied
  implementation_phase: v3
  phase_provenance: derived
  phase_derived_from: MECH-330
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-05-08
  depends_on:
    - MECH-010   # parent: language emergence and bootstrapping (this MECH refines step 1 + step 3)
    - MECH-194   # play synthetic signal substitution -- the substrate this MECH runs on
    - MECH-195   # strategy / calibration dissociation -- explains why language errors don't leak into real harm budgets
    - INV-058    # play structurally necessary -- pre-condition
    - INV-059    # bilateral play frame signal -- the basic expressions instantiate this
    - ARC-049    # play frame tag -- authorisation mechanism for synthetic stakes
    - ARC-009    # parent architectural commitment: language as coordination layer
    - ARC-010    # social cognition / OTHER_SELFLIKE detection
    - MECH-014   # minimal signalling channel (pre-language interface this MECH extends)
  location: docs/architecture/language/emergence_and_bootstrapping.md
  functional_restatement: >
    Three coupled architectural commitments. (1) The bootstrap of step 1 (affective signalling)
    is decomposed into a small inventory of stereotyped basic expressions, each carrying a
    specific learning-signal role. (2) The bootstrap of step 3 (proto-symbols) is reframed:
    proto-symbols are conditional modifiers attached to basic-expression channels, not arbitrary
    signals competing for behavioural relevance from scratch. (3) The substrate that runs steps 1-5
    of MECH-010 is play_mode itself: language is a specialised play-game instantiation in which
    utterance-as-goal acts as synthetic z_goal and conversational violation acts as synthetic z_harm,
    with the strategy / calibration dissociation of MECH-195 explaining why language-game errors
    do not leak into real-world harm budgets. The composite mechanism predicts that language
    acquisition onset and competence track play_mode maturation, and that language degradation
    under stress / fatigue / aphasia tracks loss-of-play-mode-availability rather than selective
    damage to a putative language module -- except where dorsal sequence->motor or ventral
    meaning pathways are themselves lesioned (cf. arcuate_fasciculus.md).
  acceptance_criteria: >
    A1 (developmental): cohort study showing language milestones (first words, two-word stage,
    productive grammar) correlate more strongly with play-behaviour milestones (joint attention,
    pretend play onset, social-frame opening / closing competence) than with motor or sensory
    milestones at matched ages.
    A2 (artificial-system ablation): an artificial REE-style system without play_mode substrate
    fails to acquire socially-grounded compositional language even when pattern-matching imitation
    works, while a matched system with play_mode succeeds.
    A3 (clinical / cross-species): conditions producing loss-of-play-availability (severe
    depression with anhedonia; specific frontal lesions; certain stress states) produce language
    degradation that recovers as play recovers, controlling for direct lesion of dorsal-sequence
    or ventral-meaning pathways.
    Provisional promotion: any 2 of A1 / A2 / A3.
  evidence_quality_note: "Pre-promotion gate per 'biology before formal definitions' rule (memory feedback_biology_before_formal_definitions): commission a developmental psychology lit-pull on play-language tracking (Tomasello, Bates, joint-attention -> language onset cohort studies; pretend-play -> theory of mind -> compositional language) BEFORE attempting promotion to provisional. Aggressive registration as candidate is appropriate -- the claim is biology-anchored from the start (developmental tracking is observable in dogs, primates, human infants) and registering it now lines it up for systematic lit harvest and eventual experimental test under V4 substrate."
  notes: "Companion claim to MECH-010 (which it refines) and MECH-194 (whose substrate it instantiates as a specialised game). Anchored in the 2026-05-08 expansion of emergence_and_bootstrapping.md (basic expression catalog + language-as-play-game framing). v3_pending=true because the play_mode substrate the claim depends on is V3-scoped but the language-game integration is V4-scoped. Three open questions are listed in the parent doc: innate vs scaffolded catalog acquisition, minimum sufficient catalog for compositional bootstrapping, predicted failure modes for systems trained without play_mode substrate. **Cross-species naturalistic observation (2026-05-08 addendum):** the Cross-Species Naturalistic Observation subsection of emergence_and_bootstrapping.md documents three demonstrative-negation primitives in a domestic dog (directional lead-pull-as-signal; play-frame comic offer-and-withdraw; attentional pointed gaze-aversion as joint-attention inverse). All three are demonstrative rather than literal -- the action is performed for the partner to read as signal, with literal consequences bracketed. This sharpens the predicted minimum-catalog negation primitive from 'head shake or turn-away as a token' to 'any action that means its-own-opposite-as-signal under play-frame authorisation', placing the negation primitive on the play_mode meta-layer (MECH-194 / ARC-049) and reinforcing MECH-308's central claim that play_mode is the phylogenetically-prior substrate hosting the bootstrap primitives. Generates a falsifiable cross-species prediction: vertebrates with narrow play repertoires should lack demonstrative-negation primitives even when they retain literal withdrawal / aggression."

- id: SD-051
  title: "Conditioned safety store: a non-trainable cue-specific predictive structure encoding 'this z_world prototype predicts that suffering will be omitted'. EMA-prototype encoder updated by MECH-302 relief-completion events; cosine-similarity expression head provides commitment-release gate when current z_world matches the learned safety prototype. Implements MECH-304 (cue-specific conditioned inhibition substrate): the encoded prototype is the dorsal-striatum / dlPFC encoding-pathway analog; the cosine-similarity gate is the IL->CeA expression-pathway analog."
  claim_type: design_decision
  subject: safety_prediction.cue_specific_conditioned_inhibition_substrate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: "2026-05-08"
  depends_on:
    - MECH-304    # the claim this SD implements
    - MECH-302    # provides the relief-completion event that gates the EMA prototype update (teaching signal)
    - SD-011      # z_harm_a affective stream feeding MECH-302
    - MECH-057a   # commitment-release pipeline reused on safety-cue match
    - MECH-094    # simulation gate: update(sim_mode=True) is silent (waking-only)
  functional_restatement: >
    SD-051 implements the substrate for MECH-304 (cue-specific conditioned safety
    prediction). A non-trainable ConditionedSafetyStore in
    ree_core/safety/conditioned_safety_store.py maintains an EMA prototype of
    z_world, updated only at MECH-302 relief-completion event ticks (encoding
    pathway: dorsal striatum + dlPFC analog). On each subsequent waking step,
    cosine similarity between current z_world and the prototype is sigmoided into
    a safety_prediction scalar (expression pathway: IL->CeA analog). When the
    scalar exceeds safety_store_threshold and beta_gate is elevated, the store
    fires beta_gate.release() + an optional VALENCE_LIKING write (MECH-094 reuse).

    Per-step decay toward zero (safety_store_decay_rate) provides forgetting
    without reinforcement. update(sim_mode=True) returns 0.0 without advancing
    the prototype, satisfying the MECH-094 waking-only gate.

    V4-deferred: (1) approach attractor toward safety-signaling cues (requires
    V4 multi-step planning + MECH-163 V3 completion gate); (2) contrastive
    cue-specific learning (requires trainable encoder head + phased training).
    See v3_v4_transition_boundary.md.

    Falsifiable: an REE substrate with SD-051 enabled (and MECH-302 as its
    teaching signal source) should show classical conditioned inhibition --
    a learned safety cue suppresses avoidance even when presented concurrently
    with a threat cue. SD-051-ablated agents with intact SD-052 should preserve
    contextual safety but should abolish cue-specific safety inhibition of fear.
  evidence_quality_note: >
    Implemented 2026-05-04. Module: ree_core/safety/conditioned_safety_store.py
    (ConditionedSafetyStore -- non-trainable, no nn.Module inheritance; pure
    arithmetic). New package ree_core/safety/__init__.py. Config gate:
    REEConfig.use_conditioned_safety_store (default False, bit-identical OFF).
    Additional params: safety_store_ema_alpha (0.1), safety_store_decay_rate
    (0.001), safety_store_min_norm (0.1), safety_store_threshold (0.5),
    safety_store_commitment_weight (1.0). All defaults are no-op. Validation
    experiment V3-EXQ-519 queued 2026-05-04 (4-arm substrate-readiness
    diagnostic). v3_pending=true until V3-EXQ-519 PASS and the gated MECH-304
    conditioned-inhibition discriminative pair PASS.
  notes: >
    Registered 2026-05-08 to close a registry gap: SD-051 was previously
    referenced inside MECH-304's implementation_note and in ree-v3/CLAUDE.md
    but had no top-level claims.yaml entry. Sister substrate to SD-052
    (contextual passive pathway). The two-system count (cue-specific +
    contextual) follows the Laing et al. 2022 J Neurosci 7T fMRI dissociation
    of conditioned inhibitor vs standard safety signal pathways.
  location: docs/architecture/sd_051_conditioned_safety_store.md

- id: SD-052
  title: "Contextual passive safety terrain: a non-trainable RBF-field accumulator over z_world that integrates 'suffering-absent' waking ticks into a slow contextual safety representation. Provides the substrate for MECH-303 (contextual passive safety pathway; vmPFC + hippocampus / vHipp-to-PL biological analog). When the field's local mean exceeds release threshold and beta_gate is elevated, drops the avoidance commitment to lower background vigilance. Parallel architecture to ARC-030 / MECH-117 benefit_terrain; uses a separate RBF field, no sharing with benefit_terrain or main residue values."
  claim_type: design_decision
  subject: safety_prediction.contextual_passive_substrate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: "2026-05-08"
  depends_on:
    - MECH-303    # the claim this SD implements
    - SD-011      # z_harm_a affective stream gating accumulation (harm-absent ticks only)
    - ARC-007     # hippocampal path memory substrate for context-bound encoding
    - ARC-030     # benefit_terrain RBF parallel architecture
    - MECH-117    # benefit_terrain RBF mechanism reused
    - MECH-094    # simulation gate: accumulate_safety(hypothesis_tag=True) is silent
  functional_restatement: >
    SD-052 implements the substrate for MECH-303 (context-bound passive safety
    representation). ResidueField (ree_core/residue/field.py) is extended with
    a separate safety_terrain_rbf_field plus accumulate_safety() and
    evaluate_safety() methods (no new file; extends existing ResidueField).
    On each waking step where z_harm_a.norm() falls below
    contextual_safety_harm_threshold and hypothesis_tag is False, the field
    accumulates a small (default 0.01) residue at the agent's current z_world.
    During select_action(), when beta_gate is elevated, evaluate_safety(z_world)
    yields the local mean; if it meets contextual_safety_release_threshold the
    avoidance commitment is dropped (beta_gate.release(), _committed_step_idx=0,
    _committed_anchor_keys=None).

    The architecture is parallel to ARC-030 / MECH-117 (benefit_terrain) but
    uses a separate RBF field -- no sharing with benefit_terrain or main
    residue values. The slow / diffuse update rule (~0.01/step) contrasts with
    SD-051 / MECH-304's fast event-driven prototype update; the two systems
    dissociate as expected from Laing 2022 (contextual safety pathway ->
    vmPFC + hippocampus; cue-specific pathway -> dorsal striatum + dlPFC).

    Falsifiable: SD-052-enabled agents should reduce background vigilance
    (avoidance commitment level) in repeatedly safe environments, while
    SD-052-ablated agents with intact SD-051 should fail to do so even when
    specific safety cues remain functional.
  evidence_quality_note: >
    Implemented 2026-05-04. Extends ResidueField in ree_core/residue/field.py
    (no new file). Config gate: REEConfig.use_contextual_safety_terrain
    (default False, bit-identical OFF); ResidueConfig.safety_terrain_enabled
    auto-set when use_contextual_safety_terrain=True. Additional params:
    contextual_safety_accum_weight (0.01), contextual_safety_harm_threshold
    (0.05), contextual_safety_release_threshold (1.0). All defaults are no-op.
    Validation experiment V3-EXQ-520 queued 2026-05-04 (4-arm substrate-readiness
    diagnostic). v3_pending=true until V3-EXQ-520 PASS and the gated MECH-303
    context-conditioned-safety extinction discriminative pair PASS.
  notes: >
    Registered 2026-05-08 to close a registry gap: SD-052 was previously
    referenced inside MECH-303's implementation_note and in ree-v3/CLAUDE.md
    but had no top-level claims.yaml entry. Sister substrate to SD-051
    (cue-specific conditioned inhibition pathway). The two-system count
    follows the Laing et al. 2022 J Neurosci 7T fMRI dissociation of
    standard safety signal vs conditioned inhibitor pathways.
  location: docs/architecture/mech_303_contextual_safety_terrain.md

- id: SD-054
  title: "Reef enrichment substrate: env-level behavioral-diversity substrate adding coral-reef safe zones (hazard-free patches with a static scent gradient) and food-attracted hazard drift to CausalGridWorldV2. Creates two competing behavioral attractors -- 'flee to reef' vs 'forage' -- to break monomodal route-exploitation in agents whose policy collapses to a single fixed trajectory, unblocking SD-029 C2 / C3 measurement. Pure environment substrate; no trainable parameters; no MECH-094 interaction. Renamed from SD-050 on 2026-05-08 to resolve a registry-ID collision with the suffering-derivative comparator."
  claim_type: design_decision
  subject: environment.reef_enrichment_substrate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_548_sd054_bipartite_substrate_readiness_20260511T162233Z_v3
      as_of: 2026-05-11
      verdict: non_contributory/PASS
  implementation_phase: v3
  v3_pending: true
  pending_retest_after_substrate: true  # [2026-06-08 governance] survival-leg cohort gated on escape-affordance-bridge substrate (failure_autopsy_V3-EXQ-603h_2026-06-08)
  claim_level: architectural
  registered_utc: "2026-05-08"
  depends_on: []
  functional_restatement: >
    SD-054 extends CausalGridWorldV2 (ree_core/environment/causal_grid_world.py)
    with two new mechanisms motivated by the observation that monomodal policies
    prevent balanced agent-vs-environment event distributions and therefore
    block C2 / C3 measurement for SD-029 (self-attribution comparator).

    Reef safe zones: circular Manhattan-radius patches around corner-adjacent
    centers where hazards cannot enter and no food / hazards spawn. The agent
    can enter reef cells (attractor for safety-seeking). A static 5x5
    normalized Manhattan-decay scent gradient is appended to world_state when
    reef_enabled=True (world_obs_dim 250 -> 275; +25 reef_field_view dimensions).

    Food-attracted hazards: during _drift_hazards(), with probability
    hazard_food_attraction, each hazard biases its random walk step toward the
    nearest food cell rather than sampling uniformly. Default 0.0 leaves the
    legacy random walk bit-identical. Together with reef safety this creates
    a structurally richer strategy space (foraging is actively more dangerous;
    reef is a refugium).

    Biological analog: coral-reef refugia as behavioral partitioning substrate
    -- distinct safe-zone vs foraging microhabitats force context-dependent
    strategy selection rather than single-template exploitation.

    Falsifiable: agents trained under SD-054-enabled environments should
    produce measurably more diverse behavior distributions (reef visits,
    forage events, zone transitions per episode) than agents trained under
    reef_enabled=False baseline -- the prerequisite for unblocking SD-029
    C2 / C3 measurement.
  evidence_quality_note: >
    Implemented 2026-05-04 in CausalGridWorldV2. Config: reef_enabled (bool,
    default False), n_reef_patches (int, default 3), reef_patch_radius (int,
    default 2), hazard_food_attraction (float, default 0.0). Backward
    compatible: reef_enabled=False and hazard_food_attraction=0.0 by default;
    _reef_cells empty; world_obs_dim=250; existing experiments unaffected.
    V3-EXQ-521 substrate-readiness diagnostic PASS 2026-05-04 (7/7 criteria:
    ARM_0 obs_dim=250 reef_cells=0; ARM_1/ARM_2 obs_dim=275 reef_cells=33;
    0 reef violations across 30 ep x 200 steps x 3 seeds; ARM_2 food_dist
    2.057 vs baseline 3.790 -- 46% reduction; entropy not collapsed).
    V3-EXQ-522 substrate-ceiling demonstration PASS 2026-05-05 -- BUT under
    a hand-coded reef-aware avoidance heuristic, NOT under trained REE policy.

    No trainable parameters. Pure environment substrate; no MECH-094 interaction
    (env observation stream, not replay content).

    v3_pending corrected to true 2026-05-08 after rereading the validation
    evidence: the substrate-readiness claim is delivered, but the SD's
    purpose-claim (the substrate unblocks SD-029 C2 / C3 measurement under
    trained policy) has not been validated. All trained-agent SD-029 retests
    on the SD-054 substrate -- V3-EXQ-433e, V3-EXQ-433f, V3-EXQ-523, 523a,
    523b -- returned non_contributory for the same reason (insufficient
    agent-caused trials; monomodal V_s monostrategy substrate-ceiling pattern).
    The trained policy did not adopt the discriminative reef-vs-forage regime
    EXQ-522's heuristic externalises. The gap between EXQ-522 (heuristic,
    diverse) and EXQ-433e/f / 523-series (trained, monomodal) is registered
    as MECH-309 (monomodal-collapse-as-equilibrium-without-rule-apprehender).
    The proximate next-stage blocker is the rule-apprehension layer
    (ARC-062 weak reading, V3 first pass; ARC-063 strong reading, V4-deferred),
    with MECH-269 V_s monostrategy as a representational precondition. SD-054
    will move to v3_pending=false when a rule-apprehension layer is in place
    and a trained-agent SD-029 retest on this substrate produces the C2 / C3
    contrastive event distribution.

    Bipartite layout extension landed 2026-05-11 in response to the V3-EXQ-543b
    diagnose-errors finding (TASK_CLAIMS session_id
    diagnose-v3-exq-543c-2026-05-11T0635Z) that the legacy SD-054 corner-placement
    geometry does not exert sufficient structural pressure to force the CEM
    proposer to emit candidates with diverse first-action argmaxes. Three new
    env-only kwargs added to CausalGridWorldV2: reef_bipartite_layout (bool,
    default False bit-identical OFF), reef_bipartite_axis (str, default
    "horizontal"), reef_bipartite_agent_band_radius (int, default 1). When
    enabled, reef cells cluster in the bottom half (axis=horizontal) or right
    half (axis=vertical) of the grid; hazards / resources / waypoints spawn
    exclusively in the opposite half; the agent spawns in a band of
    2*radius + 1 rows/cols centred on the midline. Reef-approach and
    forage-approach trajectories therefore have categorically opposite
    first-action argmaxes by construction (action 1 down vs action 0 up on
    horizontal axis). All defaults preserve legacy SD-054 behaviour; the
    semantics, dependencies, and v3_pending status of the parent claim are
    unchanged -- this is a substrate-refinement, not a new architectural
    claim. Validation: V3-EXQ-548 substrate-readiness diagnostic to be queued
    immediately after this implementation. Smoke 2026-05-11 PASS:
    backward-compat reproduces V3-EXQ-521 baseline (reef_cells=33,
    world_obs_dim=275, band_widen=0); activation across 3 seeds confirms
    structural partition (agent in midline-band rows [5,6,7]; reef rows
    [8,9,10] -- 28 cells with edge-clip; hazards / resources in rows
    [1,2,3,4]). Design doc updated at
    REE_assembly/docs/architecture/sd_054_reef_enrichment_substrate.md (new
    "Bipartite layout extension (2026-05-11)" section). Cross-plan: resolves
    arc_062_rule_apprehension_plan.md GAP-B resume condition (option 3a from
    the 2026-05-11 decision-log) pending V3-EXQ-548 PASS.
  notes: >
    Originally documented as SD-050 in ree-v3/CLAUDE.md (entry dated 2026-05-04)
    in error. Renamed to SD-054 on 2026-05-08 after the registry-ID collision
    with the canonical SD-050 (Suffering-Derivative Comparator) was identified.
    SD-053 was left informally reserved for a sustained-drive claim per
    docs/architecture/sustained_drive_anticipatory_wanting.md. Seventeen
    experiment scripts with SD-050 reef references were swept to SD-054 in
    the same 2026-05-08 cleanup; ree-v3/CLAUDE.md anchor moved to SD-054.
    Dedicated design doc landed 2026-05-08 at
    docs/architecture/sd_054_reef_enrichment_substrate.md.
  location: docs/architecture/sd_054_reef_enrichment_substrate.md

- id: SD-055
  title: "Differentiable CEM selection approximation -- replaces the non-differentiable argmax / argsort elite-selection step in HippocampalModule.propose_trajectories() with a softmax-weighted candidate mean so that gradient flows from task reward back through candidate selection to SD-016 cue_action_proj. Opt-in via use_differentiable_cem flag (default False; legacy argmax path bit-identical when off). Preserves ARC-007 STRICT value-flat constraint: scoring uses existing residue-terrain evaluation, not a new value head."
  claim_type: design_decision
  subject: hippocampal.differentiable_cem_selection
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:SD-055@2026-06-10T16:18:22Z"
      as_of: 2026-06-10
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: "2026-05-15"
  re_registered_utc: "2026-06-04"
  depends_on:
    - SD-016     # cue_action_proj -- the projection whose gradient is being restored
    - ARC-007    # value-flat constraint -- preserved by scoring only over residue terrain
    - ARC-072    # architectural gap cluster naming Gap 2 (cue_action_proj zero-gradient)
    - MECH-326   # PFC-hippocampal routing mechanism that V4 will complete; SD-055 is the V3 staging path
  functional_restatement: >
    SD-055 addresses the gradient severance identified in EXP-0155 and the
    EXQ-540f FAIL cluster: the CEM elite-selection step in
    HippocampalModule.propose_trajectories() uses torch.argsort() to rank
    candidates by score, then indexes elite candidates by rank -- an operation
    with zero gradient w.r.t. scores. Because action_bias from cue_action_proj
    enters candidate generation via E2 rollouts and scores derive from those
    rollouts, the argsort barrier prevents any gradient from reaching
    cue_action_proj.weight.

    The differentiable approximation replaces argsort + indexed mean with a
    softmax-weighted candidate mean over all candidates:

      weights = softmax(-scores / T, dim=0)          # [n_candidates]
      ao_mean = sum_i weights[i] * ao_candidate[i]   # [batch, H, ao_dim]

    where T = differentiable_cem_temperature (default 1.0). This makes ao_mean
    differentiable w.r.t. scores, and scores are partially differentiable
    w.r.t. action_bias via E2 rollouts. Low T concentrates weight on the best
    candidates (approaching argmax behavior); high T averages more broadly.

    Value-flat constraint (ARC-007): scoring continues to use the existing
    residue-terrain evaluation in _score_trajectory() -- no new value head is
    introduced. The approximation changes only the mathematical form of the
    selection aggregate.

    Opt-in safety: use_differentiable_cem (default False) leaves the legacy
    argmax path bit-identical when off. All existing experiments and queue
    entries are unaffected.

    Expected downstream effect: when gradient flows to cue_action_proj,
    different z_goal states should steer different candidate action-object
    distributions, making cue-conditioned trajectory generation measurably
    distinct across goal contexts. This is the V3 staging path for MECH-326
    prior to the full MECH-325 trajectory library in V4.
  evidence_quality_note: >
    Registered 2026-05-15 alongside implementation in
    ree-v3/ree_core/hippocampal/module.py (softmax-weighted refit when
    use_differentiable_cem=True) and ree-v3/ree_core/utils/config.py
    (use_differentiable_cem: bool = False,
    differentiable_cem_temperature: float = 1.0 in HippocampalConfig).
    Smoke test: gradient flows to cue_action_proj.weight when flag=True;
    flag=False is bit-identical to legacy CEM behavior.

    RE-REGISTRATION NOTE (2026-06-04): this claims.yaml entry was registered by
    commit 59ca45d6c4 on 2026-05-15 and SILENTLY DROPPED the same day by commit
    d3fb89c64d ("auto-sync: re-apply results after conflict 2026-05-15") -- the
    conflict re-apply removed the entire `- id: SD-055` block. The claim was
    absent from claims.yaml for ~3 weeks even though it remained implemented,
    referenced as a dependency by other claims (e.g. the ARC-080 PILLAR 3 child
    ARC-082), and progressed through substrate-readiness and a default-flip
    safety review. Restored verbatim from 59ca45d6c4 with this note. The drop is
    a confirmed instance of the conflict-re-apply / auto-sync silent-revert
    failure mode documented in REE_assembly/CLAUDE.md.
  implementation_note: >
    IMPLEMENTED 2026-05-15 (ree-v3/ree_core/hippocampal/module.py post-elite
    refit block; HippocampalConfig.use_differentiable_cem /
    differentiable_cem_temperature; surfaced via REEConfig.from_dims). Substrate
    -readiness PASS V3-EXQ-568 (diagnostic, non_contributory, 5/5 UC,
    grad_max=372) 2026-05-15; smoke ree-v3/smoke_sd055_differentiable_cem.py 4/4
    PASS. Marked substrate-ready 2026-05-21 (IGW-20260521-021, commit
    0658373550). use_differentiable_cem default-flip evaluated 2026-05 (commit
    d08bad6387, safety note: stays default-False; opt-in only). The behavioural
    claim (cue-conditioned candidate divergence on a goal-rich env) is NOT
    validated by substrate-readiness and requires a separate training
    experiment; status stays candidate / v3_pending (exp_conf = 0;
    implementation is not evidence).
  location: docs/architecture/sd_055_differentiable_cem_selection.md

- id: SD-056
  title: "E2 world-forward action-conditional divergence preservation via auxiliary InfoNCE-style contrastive loss. Substrate-level fix for the V3-EXQ-571 root cause finding (2026-05-25): with world_dim=32 and action_dim=4, action contributes ~11% of world_forward input dimensionality, and reconstruction-shaped training collapses to the state-dominated local minimum where action effect is fitted to zero. Measurement: cand_world_pairwise_dist = 0.0000 across K=8 candidates differing only in first action. Same root cause as 2026-05-17 ARC-062 GAP-B autopsy; GAP-B fix was scoped only to GatedPolicy first-action one-hot bypass. SD-056 is the architecturally-faithful generalisation -- fixes the predictor's training objective rather than bypassing the predictor at each consumer. Option (ii) lever B (contrastive next-state, Srivastava et al. 2021 style) chosen over PLSM-style MI factorisation (lever A) and SWIRL-style MI maximisation (lever C) per 2026-05-28 lit-pull SYNTHESIS verdict and user AskUserQuestion confirmation."
  claim_type: design_decision
  subject: e2.action_conditional_divergence_contrastive
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:SD-056@2026-05-31T08:30:00Z"
      as_of: 2026-05-31
      verdict: flag_for_failure_autopsy/approved
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: "2026-05-29"
  depends_on:
    - SD-005
    - ARC-033
  functional_restatement: >
    For each training batch containing K rollouts that share starting state
    z_world_0 but differ in first action a_i, add an auxiliary InfoNCE-style
    contrastive loss against E2.world_forward:

      For each anchor i in [K]:
        positive:  (z_world_0, a_i) -> predicted z_world_1[i]
        negatives: (z_world_0, a_j) for j != i (sibling CEM candidates)
        L_contrast_i = -log(exp(-||pred_i - target_i||^2/tau)
                          / sum_j exp(-||pred_j - target_i||^2/tau))
      L_E2 = L_reconstruction + w_contrast * mean_i(L_contrast_i)

    Equivalent to cross-entropy over logits[i,j] = -||pred_j - target_i||^2/tau
    with label i. Asymmetric (anchor-to-prediction); symmetric would double cost
    without architectural gain. Negatives drawn from in-batch sibling CEM
    candidates (z_world_0 shared, first action differs) -- informative by
    construction, no negative-mining sweep needed.

    Falsifiable: under V3-EXQ-NEW-1 substrate-readiness diagnostic, with the
    flag ON and 200 SGD steps on K=8 synthetic batches, cand_world_pairwise_dist
    should rise from ~0.0 baseline to >= 0.05 (direction-of-change is the
    load-bearing PASS condition; magnitude is calibrated by the experiment).
    Held-out contrastive-task accuracy > 50% (random baseline 1/K = 12.5%
    for K=8) confirms the task is learnable on REE's E2 architecture, not
    just that the loss decreases. Failure routes to PLSM-style MI
    factorisation fallback or architectural restructure (option D).
  evidence_quality_note: >
    Implemented 2026-05-29 in ree-v3/ree_core/predictors/e2_fast.py and
    ree-v3/ree_core/utils/config.py. Four E2Config flags wired through
    REEConfig.from_dims: e2_action_contrastive_enabled (bool, default False;
    bit-identical OFF master), e2_action_contrastive_weight (float, 0.01;
    calibratable), e2_action_contrastive_temperature (float, 0.1; standard
    literature value), e2_action_contrastive_min_batch_classes (int, 2;
    floor below which the loss returns 0 -- no informative negatives).
    Helpers cand_world_pairwise_dist(z_world_0, candidate_actions) -> scalar
    and world_forward_contrastive_loss(z_world_0, actions, z_world_1_targets,
    simulation_mode=False) added to E2FastPredictor. world_forward signature
    and body unchanged; predict_next_self, action_object, forward,
    forward_counterfactual unchanged; all downstream consumers (E1, E3,
    hippocampal, residue field) unchanged. Existing rollout-loss machinery
    unchanged -- contrastive term is additive, not substituted.

    MECH-094: world_forward_contrastive_loss accepts simulation_mode kwarg
    and returns tensor(0.0) when True, matching SD-035 / MECH-279 / MECH-313 /
    MECH-314 / MECH-319 / MECH-320 / MECH-341 pattern. Replay / DMN paths
    cannot recruit the contrastive training signal.

    Phased training NOT required at the substrate level. Unlike encoder-head-
    on-frozen-latent patterns (EXQ-166b/c/d historical), both L_recon and
    L_contrast target the same predictor weights (world_transition +
    world_action_encoder) with compatible objectives. Joint training is the
    designed-for case.

    Backward compatible: defaults are no-op; existing experiments unaffected.
    Substrate-readiness diagnostic V3-EXQ-613 queued in the same session
    (UC1-UC5 covering module surface, master-OFF backward-compat,
    cand_world_pairwise_dist direction-of-change, contrastive-task accuracy,
    MECH-094 simulation gate; smoke 5/5 PASS 2026-05-29). Behavioural
    validation V3-EXQ-569a (matched-entropy FP-2 falsifier on the fixed
    substrate; GAP-A R1.a/R1.b decision rule) is the next-stage successor,
    queued separately per plan-of-record sequencing in
    evidence/planning/e2_action_divergence_substrate_design.md.

    v3_pending=true until V3-EXQ-569a behavioural validation lands. The
    substrate-readiness diagnostic alone does not promote; it confirms the
    substrate is operative on synthetic batches and unblocks the falsifier.

    Lit-pull anchors (REE_assembly/evidence/literature/
    targeted_review_e2_forward_model_action_divergence/SYNTHESIS.md):
    Saanum / Dayan / Schulz 2024 PLSM (failure-mode diagnosis "lack of
    systematic representation of action effects"), Srivastava et al. 2021
    contrastive RSSM (lever B technique), Qiu et al. 2026 SWIRL (lever C
    fallback), Tanaka et al. 2020 cerebro-cerebellar forward model,
    Miyamoto / Rushworth / Shea 2023 frontopolar counterfactual,
    Cullen 2023 vestibular cerebellum corollary discharge. Methodological-
    gap finding: no published paper reports per-action pairwise distance
    between predicted latents at one-step horizons as a headline metric
    -- REE may publish cand_world_pairwise_dist as a standalone diagnostic
    contribution.

    [2026-05-31 governance, V3-EXQ-569e autopsy applied]: evidence_direction
    mixed (diagnostic, non-weighting) per failure_autopsy_V3-EXQ-569e_2026-05-31.json.
    569e Pathway A vs Pathway B mechanism probe verdict INSTRUMENTATION_FAILURE:
    SD-056 contrastive E2 training stable at t=1 training horizon (569d clean,
    cand_world_pairwise_dist 0.02-0.07 across ON arms) but explodes numerically
    in iterated multi-step rollouts at behavioural-runtime P1 budget (E2 rollouts
    1e16-1e18, 100% NaN-fraction on ON-arm seeds). M1/M3/M4/M5 verdict cells all
    saturated; probe could not dissociate Pathway A from Pathway B. Substrate
    implementation pathology (lacks gradient clipping / output norm clamping /
    contrastive weight schedule at multi-step rollout horizon), NOT architectural
    falsification. epistemic_category: standard (NOT substrate_ceiling -- distinct
    shape from substrate_uniform cluster 540/590a/591/598/603/610 which is
    negative-control-passes-discrimination-fails on z_goal-zero substrates).
    SD-056 multi-step rollout stability amend landed 2026-05-31 11:25Z
    (multistep contrastive horizon h=5 + per-step output norm clamp; both
    levers togglable, default OFF; V3-EXQ-617 substrate-readiness diagnostic
    queued). pending_retest_after_substrate=true; closes only after V3-EXQ-617
    PASS + 569e-equivalent Pathway A vs B re-run on the amended substrate.
    569c headline (~2.4x C3 lift over matched-noise control) + V3-EXQ-569d PASS
    remain load-bearing on ARC-065 GAP-A pending the amend-and-re-run cycle.
    Three script-side acceptance-criteria fixes bundled with the post-amend
    re-run per autopsy Section 8 sequencing. Artifact:
    evidence/planning/failure_autopsy_V3-EXQ-569e_2026-05-31.{md,json}.
    [thought-digestion currency check, 2026-08-07 -- CORRECTS the framing above] V3-EXQ-617
    (multi-step stability substrate-readiness diagnostic) PASSED 2026-05-31T11:31Z: 0/25728
    NaN/Inf cells at a 200-step stress horizon under 20x weight amplification, 0/1608
    norm-clamp-bound violations, bit-identical OFF backward-compat confirmed. But the
    "569e-equivalent Pathway A vs B re-run" this note names as the closing gate was NEVER
    actually queued -- ~7 weeks passed with no re-run, despite the numerical precondition being
    met the same day. The 569 lineage continued as 569f/g/h/i (2026-06-10 -> 2026-06-16), but
    those test a DIFFERENT lever (an E3-side top-k shortlist conversion mechanism), not the
    SD-056-specific Pathway A/B dissociation. The downstream target this was meant to unblock,
    ARC-065 GAP-A, closed 2026-06-17 via V3-EXQ-569i's PASS on that different lever -- so "569c
    headline + 569d PASS remain load-bearing on ARC-065 GAP-A pending the amend-and-re-run
    cycle" above is now STALE: GAP-A's closure is properly credited to the top-k-shortlist fix,
    NOT to a resolution of whether SD-056's own Pathway A (per-candidate divergence) or Pathway
    B (rollout-landscape shift) is what's doing the work. substrate_queue.json's SD-056
    amend_history entry (status: implemented, ready: false) still lists the same
    next_validation text, untouched since 2026-05-31. See digestion_note below.
  digestion_note: >
    [2026-08-07, thought-digestion] The falsifier this claim needs already exists in full
    (V3-EXQ-569e's own script + the three script-side acceptance-criteria fixes named in its
    autopsy Section 6: (i) a genuine per-tick NaN-fix gate for C1 rather than "at least one
    finite value per seed"; (ii) a sanity ceiling on the C3 rollout-divergence gate so numeric
    overflow cannot register as "operative" divergence; (iii) resolution of the
    acceptance_criteria-dict-vs-verdict_cell contradiction the autopsy flagged) and its
    precondition (V3-EXQ-617 numerical stability) is already met -- but it was never re-queued,
    and the downstream track it fed (ARC-065 GAP-A) has since closed via an unrelated mechanism
    without answering SD-056's own mechanism-attribution question. Defer a full what_would_answer
    until either (a) the 569e-equivalent re-run is queued and reports, or (b) governance
    explicitly re-scopes what SD-056 needs to show given GAP-A's independent closure. Do NOT
    treat V3-EXQ-569i's PASS as evidence for or against SD-056 -- it validates a different lever
    and cannot dissociate Pathway A from Pathway B. Remove this note once the re-run is queued
    (EXP-0585, minted in manual_proposals.v1.json) and reports, or governance re-scopes.
  notes: >
    Plan-of-record memo: REE_assembly/evidence/planning/e2_action_divergence_substrate_design.md
    (commissioned 2026-05-28T17:43:10Z by session
    e2-action-divergence-design-memo-20260528T174310Z, closed 2026-05-28T17:50Z).
    User-confirmed scope 2026-05-29 via AskUserQuestion: register as new SD-056
    rather than as a MECH amendment under MECH-256; queue V3-EXQ-613 in this
    /implement-substrate session rather than deferring to a separate
    /queue-experiment session.

    Generalises ARC-062 GAP-B (gated_policy_use_first_action_onehot, 2026-05-17)
    by fixing the E2 world-forward predictor itself rather than bypassing it
    at each downstream consumer. Per V3-EXQ-571 root cause finding, downstream
    consumers of cand_world_summaries (MECH-314a curiosity novelty, MECH-320
    tonic vigor, MECH-295 liking bridge, SD-033a lateral_pfc, SD-033b ofc) all
    suffer the same upstream collapse; SD-056 resolves them at the source.

    What this SD does NOT promise: (a) contrastive task being learnable does not
    by itself imply behavioural diversity emerges -- V3-EXQ-569a matched-entropy
    falsifier is what tests downstream behavioural response; (b) lever B may
    not be the right lever if REE's specific architecture has a feature we have
    not noticed -- PLSM lever A and SWIRL lever C are valid fallbacks if
    V3-EXQ-613 fails; (c) option (i) extend GAP-B one-hot bypass to all
    bias-channel consumers of first-step z_world is still on the table as a
    tactical step if V3-EXQ-569a is needed urgently.
  location: docs/architecture/sd_056_e2_action_conditional_divergence.md

- id: MECH-309
  title: "Monomodal policy collapse is the equilibrium of a parametric-policy agent without a rule-apprehension layer. Bayesian update and gradient descent revise weights over a hypothesis space they do not invent; without a non-Bayesian rule-creator that proposes discriminative policy modes ('near-hazard -> reef regime; else -> forage regime'), the trainer collapses to the smoothest single regime good-enough across the whole state space. Monostrategy is therefore not a bug of training but the predicted output of an architecture in which only updaters exist and no apprehender does. Falsifiable by the weak-reading rule-apprehension experiment (ARC-062): a gated-policy architecture trained on the same substrate should break the collapse where standard E3 does not."
  claim_type: mechanism_hypothesis
  subject: policy.monomodal_collapse_as_equilibrium_without_rule_apprehender
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-732_2026-07-10
      as_of: 2026-07-10
      verdict: non_contributory/precondition_unmet
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-05-08"
  depends_on:
    - SD-054      # the substrate that demonstrates this -- substrate readiness PASS, trained-agent purpose validation FAIL
    - SD-029      # the downstream measurement blocked by monomodal collapse (C2 / C3 require contrastive event distributions)
    - MECH-256    # SD-029 successor mechanism that consumes the same enriched event stream
    - MECH-269    # V_s monostrategy: representational precondition for any rule-apprehension layer
    - ARC-062     # rule-apprehension architectural slot, weak reading -- the falsifying-condition for MECH-309
    - ARC-063     # rule-apprehension architectural slot, strong reading (brought forward to V3 2026-05-18)
    - ARC-077     # socially-scaffolded rule-population pathway -- the most direct architectural answer to MECH-309 (caregiver supplies the hypotheses the agent does not invent)
  functional_restatement: >
    Strict Bayesian inference does not invent hypotheses: the support of the
    posterior is the support of the prior. Non-parametric Bayes (CRP, HDP)
    can grow new components, but the component-generating process is itself
    a non-Bayesian structured prior -- so the logical-necessity argument
    survives that refinement. REE's actual trainer is empirical risk on a
    parametric policy, which has the capacity to represent two modes
    (mixture-of-experts, gated computation, etc.) but currently lacks the
    inductive bias that would push gradient descent toward a discriminative
    split. Without a structural pressure to split, gradient descent finds
    the smoothest single regime that is good-enough across the whole state
    space.

    The substrate-ceiling vs trained-agent gap on SD-054 is the empirical
    foundation. Under EXQ-522's hand-coded reef-aware avoidance heuristic
    ("if hazard within 2 cells and reef available -> flee to reef; else ->
    forage") the substrate carries diverse behaviour: ~50/50 reef/forage
    occupancy, ~49 zone transitions per episode. Under trained REE policy,
    every SD-029 retest on the same substrate (V3-EXQ-433e, 433f, 523,
    523a, 523b) returned non_contributory for the same reason: insufficient
    agent-caused trials; monomodal V_s monostrategy substrate-ceiling pattern.
    The heuristic externalises an apprehension layer; the trained policy
    has no internal equivalent.

    MECH-309 reframes monomodal collapse from a training failure into the
    predicted equilibrium output of an architecture whose only learners
    are updaters. The "fix" is not more training, larger nets, or richer
    substrate: it is a non-Bayesian rule-creator that proposes
    discriminative policy modes for the updaters to refine. ARC-062 is
    the V3 instantiation of this slot (weak reading: gated-policy
    architecture with learned context discriminator). [STALE 2026-08-10
    governance: the next sentence originally called ARC-063 "the V4
    deferred form" -- ARC-063 was brought forward V4->V3 2026-05-18
    (implementation_phase: v3, v3_pending: true, per its own
    brought_forward_utc field) precisely because MECH-309 depends on it
    and MECH-309 is V3 -- phase follows dependency. ARC-063's V1
    substrate landed 2026-06-04. Only ARC-077 (the social/population
    extension) remains genuinely substrate-blocked, on a caregiver/
    teacher-agent multi-agent substrate that does not exist in V3.] ARC-063
    is the strong reading (distributed CandidateRule field with
    tolerance-gated availability and structured evidence-trace records,
    per the 2026-05-04 thought-intake on tolerance-gated rule
    apprehension).

    Clinical / cognitive corollary. The same logic explains why agents
    (biological or artificial) in clinical-pathology states can verbally
    describe alternative behaviours but fail to enact them: the
    apprehension layer has no candidate rule available for the updaters
    to weight, even when the verbal-report system can articulate one.
    A rule may become psychologically real not when it is endorsed in
    language but when it begins constraining which futures the system can
    easily generate (cf. rollout-eligibility framing in
    rule_apprehension_layer.md).

    Falsifiable: a gated-policy architecture (ARC-062 weak-reading
    instantiation: at least two policy heads with a learned context
    discriminator, trained alongside the existing E3 on SD-054 substrate)
    should break the EXQ-433e/f / 523-series monomodal pattern. Specific
    pre-registered prediction: ARM_1 (gated heads, learned discriminator)
    reef_visit_fraction in [0.3, 0.7] and zone_transitions_per_ep at
    least 5x ARM_0 (single-head baseline) under the same training budget;
    SD-029 C0 trials-sufficient gate PASSes in 3/4 seeds. Falsification:
    if ARM_1 also produces monomodal behaviour matching ARM_0, MECH-309's
    diagnosis is wrong (the missing capacity is something other than
    rule-apprehension at the policy layer -- candidates: representational
    discriminability via MECH-269 V_s landing first, or training-procedure
    issues independent of architecture).
  evidence_quality_note: >
    2026-07-08 (failure_autopsy_V3-EXQ-719a_2026-07-08 CONFIRMED -> non_contributory; claim
    UNWEAKENED). V3-EXQ-719a (tick-budget re-queue of 719; diagnostic, PROMOTES/DEMOTES NOTHING)
    fixed 719's sampling starvation -- all MI-estimability readiness gates now MET (6005 ticks/seed,
    6-10 bins, 3/3 seeds estimable). Result: the dissociation is UNDEFINED, and the self-routed
    substrate_not_ready_requeue label is a MISLABEL (readiness is met; the code's else-branch reused
    the under-sampled string for the pre-registered 'evidence against either conclusion' case -- more
    ticks will not change it). Branch B (decisive_state_appropriate_commitment / metric-artifact
    reframe) REJECTED: debiased MI clears the 0.05 floor on 1/3 seeds, competence below the 1.0
    resources/ep floor on 0/3. Branch A (genuine_monomodal_collapse) NOT cleanly supported: raw MI
    above the shuffle null on 3/3 seeds (committed action not state-independent) and marginal
    committed-class entropy moderate-to-high (0.69-1.41 nats), not low. First direct competence
    measurement: the integrated all-ON agent forages at 0.065/0.0/0.455 resources/ep -- genuinely
    below floor (counter verified firing). Dominant diagnosis substrate_ceiling: a
    behavioural-competence / training-regime ceiling (thin P1 = 90-ep bias-head-only REINFORCE on a
    frozen encoder; all-ON mechanism interference a live alternative), NOT a further selection lever
    (MECH-448/449 built + selection-face lifted on GAP-A). This reframes the downstream
    behavioural-retest wall (654h/485i/625e/460h/460i substrate_not_ready) as the same competence
    root. pending_retest_after_substrate retained; ceiling_decision stays deferred. Re-derive brake
    FIRED (21st/20th) -> competence-localization diagnostic first (brake-exempt: different question),
    then /implement-substrate on the localized gap; same-claim behavioural re-tests (V3-EXQ-722,
    719b) REFUSED.

    2026-07-07 (governance-apply, failure_autopsy_V3-EXQ-714_2026-07-07 CONFIRMED -> non_contributory;
    claim UNWEAKENED): V3-EXQ-714 (confirmed failure_autopsy_V3-EXQ-714_2026-07-07) -> non_contributory
    + pending_retest_after_substrate (status UNCHANGED: candidate / v3_pending). Same readiness abort as
    ARC-062 (C1b GAP-A divergence + C1g OFC range starved at full P0=200; C2 unscored). NOT a
    falsification. 19th MECH-309 substrate-ceiling reading; RE-DERIVE BRAKE FIRED; fullstack re-queue
    REFUSED. CAMPAIGN FINDING: the full selection+valuation stack cannot clear the conversion ceiling at
    readiness; the ceiling (GAP-A-divergence survival) is upstream of every campaign face (arbitration
    exhausted 713, selection 654, valuation 485m, fullstack 714). Release condition:
    f_dominance_conversion_ceiling GAP-A-divergence-survival face.

    2026-06-22 (governance-apply, failure_autopsy_V3-EXQ-654j CONFIRMED -> non_contributory; claim
    UNWEAKENED): V3-EXQ-654j (arc_062 GAP-B rule-apprehension Go/No-Go behavioural falsifier,
    supersedes 654i) FAILed on the pre-registered FAIL(C1 holds, C2 fails) branch -- all seven C1
    readiness/non-vacuity gates met & non-degenerate, incl. C1f confirming the MECH-449 active No-Go
    live & suppressing (1.55) on both arms -- yet the load-bearing C2 committed-class entropy lift
    FAILED (1/3 seeds). Both ARC-107 eligibility-governance legs (MECH-448 demotion @654i, MECH-449
    Go/No-Go @654j) PASS at the selection face yet NEITHER converts to committed-class behavioural
    diversity: the conversion ceiling is structural and downstream of selection. Cross-substrate-
    corroborated by V3-EXQ-485m (OFC devaluation face -- same fails-C2-alone signature; the 3rd
    convergent datum that conversion is emergent from the assembled stack, not any single
    selection-face lever). Claims UNWEAKENED (substrate_ceiling / pending_retest_after_substrate).
    Re-derive brake FIRED (18th MECH-309 / 19th ARC-062); further eligibility-governance letters
    refused. Factor-B next-route SUPERSEDED by V3-EXQ-689c (refuted at the selection face); corrected
    route = root-C rung-6 (PARKED; closure-exclusive de-commit substrate built 2026-06-22) or V4, NOT
    another GAP-B eligibility/commit-T letter. PROMOTES NOTHING.

    2026-06-22 (governance-apply, failure_autopsy_V3-EXQ-654i CONFIRMED -> non_contributory; claim
    UNWEAKENED): V3-EXQ-654i (arc_062 GAP-B rule-apprehension behavioural falsifier) FAILed
    non_contributory on the pre-registered FAIL(C1 holds, C2 fails) branch -- all C1 readiness/
    non-vacuity gates met and non-degenerate (rule field 0.913, propagation 0.027, MECH-448 demotion
    live & excluding 18.4 candidates) yet the load-bearing C2 committed-class entropy lift failed: the
    differentiated rule-apprehension bias reaches committed action but does not convert to committed-
    class diversity even under the live demotion lever. Conversion ceiling persists despite demotion;
    NOT a MECH-309/ARC-062 falsification. Cross-substrate corroboration of V3-EXQ-485k (devaluation
    face): demotion alone cannot express the active No-Go withdrawal -> route to MECH-449 (built
    2026-06-21, falsifier V3-EXQ-689g PASSED). substrate_ceiling / pending_retest_after_substrate
    behind MECH-449. RE-DERIVE BRAKE FIRED (17 prior substrate_ceiling autopsies on this family):
    refuse a same-substrate 654j re-queue; next test must engage MECH-449 active No-Go. PROMOTES
    NOTHING.

    Registered 2026-05-08 from the SD-054 substrate-purpose-validation
    discussion. Empirical anchors: V3-EXQ-522 substrate-ceiling PASS under
    heuristic policy (reef substrate carries ~50/50 reef/forage split when
    a discriminative policy is externally injected); V3-EXQ-433e / 433f /
    523 / 523a / 523b all non_contributory under trained policy on the
    same substrate (monomodal V_s monostrategy substrate-ceiling pattern;
    insufficient agent-caused trials for SD-029 C2 / C3 calibration).

    Not yet experimentally tested as an architectural claim. The weak-
    reading discriminative-pair experiment (ARM_0 single-head E3 vs ARM_1
    gated-heads with learned context discriminator on SD-054 substrate)
    is the proximate validation gate; proposal in
    evidence/planning/experiment_proposals.v1.json (proposal_id pending).

    Lit anchor (one): the 2026-05-04 thought-intake on tolerance-gated
    rule apprehension (PNAS 10.1073/pnas.2508061123 "A simple threshold
    captures the social learning of conventions") provides the
    biology-grounded form of the rule-apprehension hypothesis.

    Biology lit-pull A discharged 2026-05-09 (8 entries) -- see
    evidence/literature/targeted_review_arc_062_rule_apprehension/.
    Three converging conclusions: (1) PFC rule-coding units exist and
    abstract over instances (Bongard & Nieder 2010 PNAS;
    Miller & Cohen 2001 Annu Rev Neurosci). (2) Rule-context modulation
    is anatomically distributed across frontoparietal+insular MD network
    (Mitchell et al. 2016 J Neurosci) with dissociable sub-functions
    across PS / OFC / ACC (Capkova/Mansouri/Buckley 2025 eNeuro). (3) All
    three R3 candidate gating sites are biologically real -- BG-side
    cortico-striatal (Gurney/Humphries/Redgrave 2015 PLoS Biology),
    hippocampal trajectory-proposal (Pfeiffer & Foster 2013 Nature), and
    PFC top-down score_bias (Miller & Cohen 2001; Bongard & Nieder 2010);
    the architectural commitment is a routing choice, not a metaphysical
    one. R2 caveat: PFC's biological mechanism is high-dimensional mixed
    selectivity (Rigotti et al. 2013 Nature), not discrete head selection
    -- ARC-062 weak reading's two-head architecture is a low-dimensional
    approximation suitable for SD-054's two-mode partition but expected
    to break at multi-strategy scaling (Phase 4 / GAP-E). MECH-309's
    logical-necessity claim is supported by Bongard & Nieder's
    abstraction-coding finding -- biology has the capacity strict
    Bayesian / parametric-policy learners lack; the question is what
    training pressure produces it.

    Biology lit-pull B discharged 2026-05-09 (6 entries) -- see
    evidence/literature/targeted_review_arc_062_refuge_forage_ecology/.
    Behavioural-ecology / R4 calibration counterpart to Pull A. Six
    entries spanning canonical theory (Lima & Bednekoff 1999 Am Nat;
    Beauchamp & Ruxton 2010 Am Nat critique), direct empirical fish-
    refuge data (Balaban-Feld et al. 2019 Oecologia, highest mapping
    fidelity to SD-054), vole field-test partial replication
    (Sundell et al. 2004 Oecologia), individual variation in voles
    (Eccard et al. 2020 Oecologia), and refuge-distance + species-
    difference data (Crowell et al. 2016 Ecol Evol). R4 verdict:
    Phase 2 falsifier acceptance threshold should be a multi-signature
    tolerance window (density-tracking + state-dependence + risk-type
    dissociation + cross-seed variation), not a single fixed
    allocation ratio. PASS rule = at least 2 of 4 acceptance criteria
    hold across seeds with no contradictory signal. Convergent argument
    from theoretical caveats, empirical partial replication, and
    individual / species variation all supporting the tolerance-window
    framing.

    Sleep-vs-waking refinement remains as a future pull, scoped to
    ARC-063 V4 work rather than MECH-309 promotion. With Pull A and
    Pull B both discharged, R1 / R2 / R3 / R4 of the upcoming
    arc_062_rule_apprehension_plan.md all have biology-anchored
    default values for the plan-doc draft.

    GOVERNANCE 2026-05-18 (confirmed failure_autopsy_V3-EXQ-543h,
    status confirmed, cluster [543f,543g,543h]). The whole 543f/543g/
    543h falsifier cluster is non_contributory -- epistemic_category
    substrate_ceiling. GatedPolicy head-differentiation does not
    robustly persist under outcome-coupled REINFORCE; gated arms
    collapse to inert intermittently. Cross-machine proof: identical
    543g config ACTIVE host-A only, INERT cloud-3 + cloud-4 (bit-
    identical ARM_0); 543g _144716Z 'weakens ARC-062' is a 1/3
    minority-basin artifact -> 543g overall non_contributory. All
    seven cluster manifests marked evidence_direction=superseded by
    V3-EXQ-543i. narrow_supports_flag: MECH-309 / ARC-062 have ZERO
    reliable contributory trained-policy evidence -- voiding this
    cluster is NOT conflict resolution for either claim. V3-EXQ-543i
    (differential-heads substrate fix; diff_on_escape=true confirms
    use_differential_heads resolves head-collapse) is FLAGGED FOR
    /failure-autopsy (diff-OFF contrast non-reproduction unexplained)
    and carries no evidence_direction pending that post-mortem.
    pending_retest_after_substrate.

    2026-05-19 governance (failure_autopsy_543i_2026-05-19, confirmed):
    Both V3-EXQ-543i runs non_contributory. Byte-identical runs landed
    opposite basins (06:37Z branch-c all-escape reef 0.28; 19:10Z
    branch-e all-collapse reef 0.69); basin selection RNG/init +
    cross-run + cross-machine nondeterministic, NOT controlled by
    use_differential_heads. 19:10Z 'weakens ARC-062' is single-basin
    artifact; 19:10Z 'supports MECH-309' rests on non-deterministic
    collapse baseline (narrow/single-basin). Manifest corrected:
    evidence_direction 'mixed' -> non_contributory all claims.
    pending_retest_after_substrate.

    2026-05-27 governance (failure_autopsy_V3-EXQ-543l_2026-05-27,
    confirmed): MECH-309=supports per autopsy verdict. V3-EXQ-543l
    FAIL branch-e at escalated floor=0.5 / aux=0.3 with basin_stable=true
    -- collapse survives differential-head substrate + Phase-3
    crystallization + escalated floor + escalated aux. First contributory
    trained-policy support entry for MECH-309: the strong-reading
    prediction (parametric-policy gradient descent does not invent the
    discriminative split) is corroborated by the fact that every
    implementation-side intervention that does not introduce a
    non-Bayesian routing consumer fails to escape collapse. Support is
    single-pathway (collapse-equilibrium signature from a single
    discriminating-test) and remains v3_pending pending V3-EXQ-598b
    corroboration with GAP-C/D routing consumer landed. narrow_supports_flag
    retained (counterpart asymmetry on ARC-062 side). epistemic_category
    substrate_ceiling (unchanged).

    [2026-06-10 governance / confirmed failure_autopsy_569f-661-654a_2026-06-10]:
    V3-EXQ-654a non_contributory for MECH-309 (already substrate_ceiling, no
    demotion). Self-routed substrate_not_ready_requeue (CRF maturation unmet:
    crf_frac_active 0.137 < 0.3). The within-arm rule_state counterfactual = 0/3
    seeds (committed-class entropy lift OFF 0.9375 -> ON 0.9377) corroborates the
    569f/661/654a E3-commit-readout substrate ceiling: minted rule_state does not
    reach committed action -- the selection-authority coupling does not route
    upstream-channel range into the modulatory bias. Re-queue 654a at a longer CRF
    maturation budget (new letter); on a matured-and-firing field a persistent
    within-arm counterfactual of 0 would confirm authority-coupling, not
    maturation, is the ceiling.

    [2026-06-19 governance / V3-EXQ-654g, supersedes V3-EXQ-654f]: The
    matured-and-firing test the 654a entry called for has now run, and the
    prediction held. 654g cleared every readiness gate (crf_frac_active 0.94,
    GAP-A divergence 0.080, propagation non-vacuous) yet the committed-class
    entropy lift is +0.011 nats, 0/3 seeds -- a non_contributory result, NOT a
    falsification. CONFIRMED: it is the selection-authority CONVERSION coupling
    (MECH-439 F-dominance live root), not CRF maturation, that is the ceiling.
    The minted+matured+active rule_state reaches committed action (counterfactual
    delta nonzero) but cannot move the F-dominated committed argmax. Second
    behavioural channel after V3-EXQ-485h (OFC) to corroborate MECH-439. No status
    change (substrate_ceiling / v3_pending / pending_retest_after_substrate).
    654f -> superseded. Routing: /implement-substrate (GAP-A gain/contrast amend).

    [2026-08-10 governance, failure_autopsy_906b-906c-911-cluster_2026-08-10, confirmed]:
    V3-EXQ-906b/906c/911 (diagnostic showcases, claim_ids=[]) corroborate the
    monomodal-collapse-without-rule-apprehender reading in a NEW context: real, replicated
    protective shelter use (2.4x/1.6x lower harm-rate while sheltering) with excursion frequency
    comfortably clearing V3-EXQ-522's own pre-registered numeric bars (C1/C2/C3), but no
    demonstrated contingent control (opportunity-triggered exit is structurally untestable by the
    current ecology's geometry per a later correction -- see
    developmental_ecology_curiosity_foraging_correction_2026-08-10.md; threat-triggered return is
    weak, distance-dependent, inconsistent across seeds). This is the collapse landing on the
    safety pole of a real two-attractor context, exactly as this claim predicts happens absent a
    discriminative rule-apprehension layer -- not a fresh finding; no status/confidence change.
  notes: >
    Diagnostic claim, not a substrate or implementation claim. MECH-309
    explains why SD-054 (and any substrate alone) is insufficient to
    unblock SD-029 measurement under current REE training. The fix lives
    at ARC-062 / ARC-063, not in further substrate enrichment.

    Cross-references: SD-054 evidence_quality_note (the substrate side),
    rule_apprehension_layer.md (architecture doc covering ARC-062 /
    ARC-063 cluster).

    [2026-08-01 governance, V3-EXQ-851, confirmed failure_autopsy_V3-EXQ-851_2026-08-01,
    GOV-FANOUT-1 Leg P-A erratum-fix retest]: matched-stack identical to V3-EXQ-654j, same
    seeds, one change -- modulatory_channel_route_source corrected from the design-doc erratum
    'gated_policy' to 'lateral_pfc'. Under IDENTICAL seeds, MECH-448 demotion lever and MECH-449
    active-Go/No-Go both flip from robustly-live (654j: 17.76 / 1.549) to completely dead (0.0 /
    0.0) -- a deterministic result, not seed/power noise. A static code read did not identify the
    causal path (MECH-448/449 gate on independent flags/bundles, structurally unrelated to the
    route-source branch), so the manifest's own "H1 refuted" framing is NOT endorsed -- it
    describes a different outcome branch (C1-holds) than what this run actually hit (C1-fails,
    3/8 preconditions dead). evidence_direction: non_contributory -- no H1 verdict possible from
    this run. User explicitly confirmed: do NOT accept the substrate_not_ready_requeue self-route
    at face value. Route: /queue-experiment -- a targeted diagnostic (lateral_pfc vs none at the
    same seeds, tracking MECH-448/449 directly) before spending more compute on the full
    ~7.7-hour falsifier design. Re-derive brake does not fire (this reading is
    measurement_test_design_defect on THIS run, not a fresh substrate_ceiling hit). Stays
    substrate_ceiling / pending_retest_after_substrate per the existing DEFER-to-700b framing
    above -- this run neither resolves nor changes that.
  epistemic_category: substrate_ceiling
  pending_retest_after_substrate: true
  ceiling_decision: deferred
  ceiling_routing_note: "2026-06-23 substrate-ceiling-lifted triage (verdict: DEFER to in-flight line, no separate retest -- sibling of ARC-062's note, same falsifier evidence). The audit flags SD-056 (E2 world-forward contrastive loss) being implemented, but BOTH lifts that could re-route MECH-309 were ALREADY consumed without converting. (1) SD-056 / E2 contrastive world-forward: exercised in the 543l (gated-policy mode-separation collapsed even under escalated MODE_SEPARATION_FLOOR 0.5 + aux 0.3; confirmed substrate_ceiling, MECH-309 tagged) and 569f/604a line -- 569f had the upstream world-forward divergence REAL and measured (consumed world-summary spread 0.196 > floor; C1 e2.world_forward divergent PASS) yet selected-action entropy bit-identical (0.549141) across PROPOSER / E2WF / MATCHED-NOISE, 0/3 seeds strict-above (C0 contrastive PASS but committed readout washout). (2) modulatory-bias-selection-authority (ARC-065 GAP-A, 569i top_k k=3): consumed by V3-EXQ-654i/654j (arc_062 GAP-B rule-apprehension behavioural falsifiers, 2026-06-22) which ran it live as a matched constant -- all seven C1 readiness/non-vacuity gates met (the minted rule bias REACHES committed action) yet the load-bearing C2 committed-class entropy lift FAILED. Both faces converge on ONE bottleneck: the conversion ceiling is structural and DOWNSTREAM of selection (MECH-439 F-dominance / E3 commit-selection-authority coupling), not a representation-layer or selection-face lever. SD-056's distinct world-forward angle is therefore NOT a fresh non-vacuous retest -- 569f already exercised it at the same committed readout. Re-derive brake FIRED (18th MECH-309 / 19th ARC-062 autopsy; further GAP-B eligibility/commit-T letters refused). The residual conversion ceiling is being decisively tested NOW by V3-EXQ-700b (ARC-108/MECH-450 learned-gating settling C3, claimed ree-cloud-4 2026-06-23T17:58Z), which carries both MECH-309 unblockers (SD-056 path + modulatory-authority/top_k k=3 569i) as matched constants and is pre-registered decisive-or-escalate-to-V4. Action: let the 700b line clear/route MECH-309's pending_retest. REMOVE THIS MARKER to re-route only if 700b PASSes (settling converts) -- then a rule-apprehension retest becomes non-vacuous; if 700b escalates to V4, MECH-309 follows to V4 (with its ARC-063 strong-reading slot). Same downstream-of-selection bottleneck and DEFER reasoning as ARC-062's note above and MECH-294's. Stays substrate_ceiling / pending_retest_after_substrate; PROMOTES NOTHING."
  location: docs/architecture/rule_apprehension_layer.md

- id: ARC-062
  title: "Rule-apprehension architectural slot, weak reading: a non-Bayesian rule-creator at the policy layer that proposes discriminative policy modes for the updaters (E3, training loop) to refine. V3 instantiation: a gated-policy architecture in which at least two policy heads share encoder features but receive different gating from a learned context discriminator (e.g., reef-context vs open-context heads). Tests the logical-necessity claim of MECH-309 within the existing V3 substrate without registering the heavier CandidateRule machinery (ARC-063 -- brought forward V4->V3 2026-05-18, this parenthetical is stale as of registration 2026-05-08). Architectural slot, not yet implemented."
  claim_type: architectural_commitment
  subject: policy.rule_apprehension_layer.weak_reading
  ml_translation_note: >
    ML-recognisability gloss (term deliberately NOT renamed; see
    docs/architecture/rule_apprehension_layer.md "ML translation").
    "Rule apprehension" = the policy layer's capacity to form and hold
    multiple distinct behavioural regimes instead of collapsing to a single
    averaged mode under gradient descent (the unimodal-policy mode-collapse /
    mode-averaging failure that motivates multimodal policies -- MDNs,
    diffusion policies, IBC). The V3 weak-reading instantiation (gated_policy)
    is structurally a context-gated two-expert mixture-of-experts (two scoring
    heads + a learned context discriminator emitting a soft gate w in [0,1]);
    the regimes map most closely to the RL options framework (Sutton 1999;
    targeted_review_rule_apprehension_vocabulary_mapping). The distinctive
    claim (MECH-309) is NOT "use an MoE": it is that the regimes must be
    CREATED by a non-Bayesian rule-creator, not merely WEIGHTED by a trainer
    refining pre-given ones -- which is why a vanilla end-to-end MoE, itself
    prone to router/expert collapse (the same equilibrium MECH-309 diagnoses),
    is necessary-but-not-sufficient. "Apprehension" is retained to mark that
    creation-vs-selection distinction, which standard ML labels flatten.
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-732_2026-07-10
      as_of: 2026-07-10
      verdict: non_contributory/precondition_unmet
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: "2026-05-08"
  ceiling_decision: deferred
  ceiling_routing_note: "2026-06-23 substrate-ceiling-lifted triage (verdict: DEFER to in-flight line, no separate retest): the audit flag is on modulatory-bias-selection-authority (ARC-065 GAP-A) being implemented, but that exact lift was ALREADY consumed by V3-EXQ-654i/654j (arc_062 GAP-B rule-apprehension behavioural falsifiers, 2026-06-22) which ran with it live as a matched constant (modulatory-authority/top_k k=3 = 569i + use_f_eligibility_demotion + use_go_nogo_constitution). Both cleared all seven C1 readiness/non-vacuity gates -- the minted rule bias demonstrably REACHES committed action through the modulatory authority -- yet the load-bearing C2 committed-class entropy lift FAILED: the conversion ceiling is structural and DOWNSTREAM of selection (MECH-439 F-dominance), not a selection-face lever. Re-derive brake FIRED (19th ARC-062 autopsy; further GAP-B eligibility/commit-T letters refused). The residual conversion ceiling is being decisively tested NOW by V3-EXQ-700b (ARC-108/MECH-450 learned-gating settling C3, claimed ree-cloud-4 2026-06-23T17:58Z), which carries ARC-062's lift substrate as a matched constant and is pre-registered decisive-or-escalate-to-V4. Action: let the 700b line clear/route ARC-062's pending_retest. REMOVE THIS MARKER to re-route only if 700b PASSes (settling converts) -- then a rule-apprehension + settling retest becomes non-vacuous; if 700b escalates to V4, ARC-062 follows to V4. Same downstream-of-selection bottleneck and DEFER reasoning as MECH-294's note above. Stays substrate_ceiling / pending_retest_after_substrate; PROMOTES NOTHING."
  depends_on:
    - MECH-309    # the diagnostic claim ARC-062 falsifies (or supports)
    - SD-054      # the substrate ARC-062's first-pass experiment runs on
    - MECH-269    # V_s monostrategy: representational precondition (regions must be discriminably represented before a context discriminator can route between policy heads)
    - SD-029      # the downstream measurement gated by ARC-062's success
  what_would_answer: |
    NON-DEGENERACY PRECONDITIONS (both required before any C1/C2 result on this
    claim can be read as decisive):

    (1) ARC-062 READINESS/NON-VACUITY GATES (C1a-f, GAP-B behavioural falsifier
    design). ANY gate failing self-routes to substrate_not_ready_requeue, never a
    falsification: (a) committed-class axis exercisable (frac_pre_ge2 > 0.30);
    (b) GAP-A consumed-summary divergence real (pairwise dist mean > 0.05,
    bounded); (c) ARM_ON rule field matured (crf_frac_active >= 0.30 of P2 ticks,
    minted-rule floor cleared); (d) propagation non-vacuity (paired |bias_ON -
    bias_OFF| > 1e-3); (e) MECH-448 demotion lever live and actually excluding
    (not all-admit); (f) MECH-449 active No-Go live and actually suppressing (not
    inert). These gates have been repeatedly, non-degenerately CLEARED across the
    GAP-B lineage (654i/654j/485h/485m/714/719a and others) -- the minted
    rule-apprehension bias demonstrably reaches the E3 accumulator with genuine
    modulatory authority. A run that fails any (a)-(f) says nothing about
    ARC-062/MECH-309 and must not be read as a weakens.

    (2) SHARED DOWNSTREAM CONVERSION CEILING. Every ARC-062 GAP-B behavioural
    falsifier run (V3-EXQ-654g/654j/485h/485m/714/719a, 21 autopsies) shows
    the identical C1-holds/C2-fails signature: rule-biased signal reaches E3
    with genuine authority but fails to convert to committed-class diversity
    (MECH-439 F-dominance conversion ceiling, shared by both readings,
    in-flight via ARC-108/MECH-450). Until this lifts, any committed-action
    strong-vs-weak test is confounded.

    CONFIRMING SIGNATURE (supports MECH-309 + ARC-062):
    Both (1) C1(a)-(f) ALL clear non-degenerately, AND (2) C2 -- the PRIMARY
    falsifier -- clears: paired-by-seed lift in committed_class_entropy_nats,
    ARM_ON over ARM_OFF, of at least 0.05 nats (C2_LIFT_MARGIN_NATS,
    strict-above-by-margin), on a majority (>=2/3) of seeds. This has never yet
    occurred across 21 runs.

    FALSIFYING / PRESENT-STATE SIGNATURE (the observed condition, 21 times):
    C1(a)-(f) clear (readiness genuinely met, non-degenerate -- e.g. 654j:
    crf_frac_active 0.94, GAP-A divergence 0.080, MECH-448 excluded_count
    non-zero, MECH-449 suppression 1.55 both live) AND C2 fails: the
    committed-class entropy lift is near-zero or negative and clears 0.05 nats
    on 0/3 (occasionally 1/3) seeds -- e.g. 654j ARM_ON 0.6728 vs ARM_OFF 0.6614
    = +0.011 nats. This is NOT read as a falsification of the rule-apprehension
    architectural slot: the pre-registered interpretation is that the minted,
    matured, propagating rule bias reaches committed action but the F-dominated
    E3 committed argmax (MECH-439) will not move for ANY selection-face lever
    tried so far -- the ceiling is downstream of selection, not at the
    rule-apprehension layer itself.

    CURRENT REGISTERED STATE (as of the most recent governance note,
    2026-08-01, V3-EXQ-851/implementation_note): this is the PRESENT signature
    and has not changed. The decisive test of precondition (2) -- V3-EXQ-700b,
    pre-registered decisive-or-escalate-to-V4 -- ran 2026-06-24 and resolved
    PROCEDURALLY but not SUBSTANTIVELY: its own noise-verification precondition
    failed, so its nominal C1/C2 passes were not trustworthy. The pre-registered
    terminal same-layer-null redesign (700c/700d) then CONFIRMED the V3
    single-arena substrate cannot furnish any valid committed-class null at any
    magnitude -- corroborating that MECH-439 is a genuine substrate ceiling, not
    a test-design artifact -- and routed to the already-built v4_loop_segregation
    (ARC-110) substrate via V3-EXQ-707b. But 707b's own decisive finding was
    WITHDRAWN 2026-07-20 (a hold-duration-weighted committed-class-entropy
    measurement defect), so the successor test is itself invalidated and
    unresolved. A decisive verdict on ARC-062 itself (weak reading) remains
    CONFOUNDED by precondition (2) until either MECH-439 lifts on a valid
    substrate, or a corrected loop-segregation (or successor) test cleanly
    resolves PASS/FAIL. Any single GAP-B run continuing to show C1-holds/C2-fails
    under these preconditions is not new falsifying evidence -- it is the
    already-registered signature repeating.
  functional_restatement: >
    ARC-062 is the V3 architectural slot for the rule-apprehension layer
    that MECH-309 identifies as logically necessary. The weak-reading
    instantiation is engineering-cheap: instead of registering a discrete
    distributed CandidateRule field with tolerance gates and evidence
    records (the strong reading; ARC-063), give the policy network a
    structural inductive bias toward modular decomposition.

    Concrete V3 first pass (proposed):
      - Two policy heads sharing the existing E3 encoder features
      - A small context discriminator (learned, ~16-32 hidden units)
        that takes z_world / z_harm features and produces a soft gating
        weight in [0, 1] over the two heads
      - Both heads trained jointly via the existing E3 loss; no separate
        objective for the discriminator (its gradient flows from whichever
        head is selected; soft gating allows differentiability)
      - Backward-compat config flag: REEConfig.use_gated_policy (default
        False); when False, single-head behaviour is bit-identical to
        current E3
      - Initialisation: heads initialised with small random
        differentiation so symmetry breaking is possible from step 0

    Why this is a useful first test of MECH-309. The architecture provides
    the structural pressure to split that single-head E3 lacks: when the
    discriminator finds a discriminative cut in the input stream, gradient
    descent has an incentive to specialise the two heads to the two sides
    of that cut. If MECH-309's diagnosis is correct, this should be
    sufficient to break monomodal collapse on SD-054 substrate; if it is
    not (ARM_1 collapses identically to ARM_0), the missing capacity is
    not at the policy layer and the diagnosis fails.

    What the weak reading does NOT cover (defers to ARC-063 strong reading):
      - Discrete `CandidateRule` objects with named status flags
        (latent / rollout_eligible / strengthened / weakened / split / merged
        / retired)
      - Tolerance-Principle-style availability gates that depend on a
        ratio of supporting examples to exceptions
      - Structured per-action evidence trace records linking
        active-rule -> rollout-generated -> action-selected -> outcome
      - Hippocampal-rollout-eligibility shaping (rules biasing which
        futures are generated, not just which heads gate)
      - Sleep / replay-side rule refinement (split, merge, prune)
      - Moral-residue attribution to the rule-field that biased rollout

    These are the "biology-faithful" structural commitments. The weak
    reading tests whether the simplest possible apprehension layer is
    enough; the strong reading is V4-deferred precisely because if the
    weak reading PASSes, we can defer the heavier machinery, and if it
    FAILs, we know the heavy machinery is the next thing to try.
  evidence_quality_note: >
    2026-07-08 (failure_autopsy_V3-EXQ-719a_2026-07-08 CONFIRMED -> non_contributory; claim
    UNWEAKENED). V3-EXQ-719a (tick-budget re-queue of 719; diagnostic, PROMOTES/DEMOTES NOTHING)
    fixed 719's sampling starvation -- all MI-estimability readiness gates now MET (6005 ticks/seed,
    6-10 bins, 3/3 seeds estimable). Result: the dissociation is UNDEFINED, and the self-routed
    substrate_not_ready_requeue label is a MISLABEL (readiness is met; the code's else-branch reused
    the under-sampled string for the pre-registered 'evidence against either conclusion' case -- more
    ticks will not change it). Branch B (decisive_state_appropriate_commitment / metric-artifact
    reframe) REJECTED: debiased MI clears the 0.05 floor on 1/3 seeds, competence below the 1.0
    resources/ep floor on 0/3. Branch A (genuine_monomodal_collapse) NOT cleanly supported: raw MI
    above the shuffle null on 3/3 seeds (committed action not state-independent) and marginal
    committed-class entropy moderate-to-high (0.69-1.41 nats), not low. First direct competence
    measurement: the integrated all-ON agent forages at 0.065/0.0/0.455 resources/ep -- genuinely
    below floor (counter verified firing). Dominant diagnosis substrate_ceiling: a
    behavioural-competence / training-regime ceiling (thin P1 = 90-ep bias-head-only REINFORCE on a
    frozen encoder; all-ON mechanism interference a live alternative), NOT a further selection lever
    (MECH-448/449 built + selection-face lifted on GAP-A). This reframes the downstream
    behavioural-retest wall (654h/485i/625e/460h/460i substrate_not_ready) as the same competence
    root. pending_retest_after_substrate retained; ceiling_decision stays deferred. Re-derive brake
    FIRED (21st/20th) -> competence-localization diagnostic first (brake-exempt: different question),
    then /implement-substrate on the localized gap; same-claim behavioural re-tests (V3-EXQ-722,
    719b) REFUSED.

    2026-07-07 (governance-apply, failure_autopsy_V3-EXQ-714_2026-07-07 CONFIRMED -> non_contributory;
    claim UNWEAKENED): V3-EXQ-714 (confirmed failure_autopsy_V3-EXQ-714_2026-07-07; FULLSTACK
    selection+valuation conversion falsifier, new composite) -> non_contributory +
    pending_retest_after_substrate (status UNCHANGED: candidate / v3_pending). Self-routed
    substrate_not_ready_requeue: C1b GAP-A consumed-summary divergence 0.004 < 0.05 (non-vacuous 1/3
    seeds) and C1g OFC devalued-range 0.0007 < 0.05 (non-vacuous seed 44 only) FAILED at FULL P0=200;
    the C2 falsifier was never scored. NOT a falsification (pre-registered NO-weakens map). Selection
    stack fully live (C1c/C1d/C1e/C1f pass) but differentiated summaries do not survive selection on
    2/3 seeds -- F-dominance conversion ceiling (MECH-439) at the readiness layer. 20th ARC-062
    substrate-ceiling reading; RE-DERIVE BRAKE FIRED; same-face fullstack re-queue REFUSED. Release
    condition: f_dominance_conversion_ceiling GAP-A-divergence-survival face.

    2026-06-22 (governance-apply, failure_autopsy_V3-EXQ-654j CONFIRMED -> non_contributory; claim
    UNWEAKENED): V3-EXQ-654j (arc_062 GAP-B rule-apprehension Go/No-Go behavioural falsifier,
    supersedes 654i) FAILed on the pre-registered FAIL(C1 holds, C2 fails) branch -- all seven C1
    readiness/non-vacuity gates met & non-degenerate, incl. C1f confirming the MECH-449 active No-Go
    live & suppressing (1.55) on both arms -- yet the load-bearing C2 committed-class entropy lift
    FAILED (1/3 seeds). Both ARC-107 eligibility-governance legs (MECH-448 demotion @654i, MECH-449
    Go/No-Go @654j) PASS at the selection face yet NEITHER converts to committed-class behavioural
    diversity: the conversion ceiling is structural and downstream of selection. Cross-substrate-
    corroborated by V3-EXQ-485m (OFC devaluation face -- same fails-C2-alone signature; the 3rd
    convergent datum that conversion is emergent from the assembled stack, not any single
    selection-face lever). Claims UNWEAKENED (substrate_ceiling / pending_retest_after_substrate).
    Re-derive brake FIRED (18th MECH-309 / 19th ARC-062); further eligibility-governance letters
    refused. Factor-B next-route SUPERSEDED by V3-EXQ-689c (refuted at the selection face); corrected
    route = root-C rung-6 (PARKED; closure-exclusive de-commit substrate built 2026-06-22) or V4, NOT
    another GAP-B eligibility/commit-T letter. PROMOTES NOTHING.

    2026-06-22 (governance-apply, failure_autopsy_V3-EXQ-654i CONFIRMED -> non_contributory; claim
    UNWEAKENED): V3-EXQ-654i (arc_062 GAP-B rule-apprehension behavioural falsifier) FAILed
    non_contributory on the pre-registered FAIL(C1 holds, C2 fails) branch -- C1 readiness/non-vacuity
    all met & non-degenerate (rule field 0.913, propagation 0.027, MECH-448 demotion live excluding
    18.4) but the load-bearing C2 committed-class entropy lift failed: the rule-apprehension bias
    reaches committed action yet does not convert to committed-class diversity even under live
    demotion. Conversion ceiling persists despite demotion; NOT an ARC-062 falsification. Routed to
    MECH-449 Go/No-Go opponency leg (built 2026-06-21; 689g PASSED). substrate_ceiling /
    pending_retest_after_substrate behind MECH-449. RE-DERIVE BRAKE FIRED (17 prior substrate_ceiling
    autopsies): refuse a same-substrate 654j re-queue; next test must engage MECH-449 active No-Go.
    PROMOTES NOTHING.

    Registered 2026-05-08 as the V3 architectural slot for the rule-
    apprehension layer the MECH-309 / SD-054 substrate-purpose-validation
    discussion identifies as logically necessary. Phase 1 substrate IMPLEMENTED
    2026-05-09 (gated_policy.py + agent wiring); substrate-readiness V3-EXQ-542a
    PASS 2026-05-20 (UC1-UC6). Trained-policy behavioural validation remains
    gated on V3-EXQ-543k + V3-EXQ-598. Original cost estimate (~150-300 lines)
    superseded by landed module. Validation: discriminative-pair experiment
    (ARM_0 single-head E3 vs ARM_1 gated heads with learned context
    discriminator, both on SD-054 reef + food_attraction substrate);
    proposal in evidence/planning/experiment_proposals.v1.json.

    No biology lit-pull yet on the policy-layer architecture specifically.
    The thought-intake doc grounds the broader cluster
    (rule_apprehension_layer.md). The weak reading is closer to standard
    ML mixture-of-experts work than to neuroscience: explicit lit support
    is moderate (Shazeer et al. sparsely-gated MoE; Ba et al. modular
    networks). The ML support is sufficient to register and run the
    falsifying-pair experiment without a deep biology lit-pull, but the
    biology side becomes load-bearing for the strong reading (ARC-063).

    2026-05-17 governance (V3-EXQ-543e, supersedes 543d): the SP-CEM
    re-issue of the 2x2 falsifier FAILed but is NON-CONTRIBUTORY for
    ARC-062 (reclassified from "weakens" per the confirmed dual-root-cause
    failure autopsy failure_autopsy_EXQ-543e_2026-05-17, session A +
    session-B addendum, user-gated). Dual root cause, neither implicating
    the ARC-062 architectural commitment: (1) configuration_error -- the
    MECH-260 dACC factorial axis was never live (DACCtoE3Adapter returns
    the zero vector when dacc_weight==0.0; the script set only
    dacc_suppression_weight=0.5), making the factorial degenerate
    (ARM_0==ARM_1, ARM_2==ARM_3 byte-identical, D1=D2=0.0 all 3 seeds);
    (2) substrate_under_fed -- SP-CEM delivers ~5 first-action classes but
    E2 world-forward compresses that to ~0.22% of z_world magnitude,
    starving the z_world-only gated head. The ARC-062 weak-reading
    architecture has STILL not had a confound-free test. Stays
    candidate / v3_pending; no confidence change; no conflict-ratio
    movement. The whole 543b/c/d/e lineage is non-contributory (collapsed
    CEM and/or inert dACC). First valid test = V3-EXQ-543f, gated on two
    landings: /implement-substrate ARC-062 head-input first-action one-hot
    augmentation, AND dacc_weight>0 with a pre-flight non-degeneracy
    assertion.

    2026-05-18 governance (confirmed failure_autopsy_V3-EXQ-543h):
    V3-EXQ-543h non_contributory; correcting the manifest branch-(b)
    misfire. Gated policy collapsed to inert on all gated arms / 3
    seeds (n_inert=3, probe_gate failed); repro_543g_signature is
    sign-only and misfired the interpretation grid into branch (b).
    Correct branch (c): non_contributory all claims. Cross-machine
    replication: 543g config near-bifurcation fragile -- ACTIVE host-A
    (_144716Z), INERT cloud-3 (_191536Z) + cloud-4; 543g _144716Z
    'weakens ARC-062' is a 1/3 minority-basin artifact, 543g overall
    non_contributory. The 543f/543g/543h cluster (7 manifests) is
    marked evidence_direction=superseded by V3-EXQ-543i. ARC-062 has
    ZERO reliable contributory trained-policy evidence
    (narrow_supports_flag) -- voiding this cluster is NOT conflict
    resolution. V3-EXQ-543i (the differential-heads substrate-fix
    validation; diff_on_escape=true demonstrates use_differential_heads
    resolves head-collapse) is FLAGGED FOR /failure-autopsy because the
    diff-OFF contrast did not reproduce the collapse this run; it
    carries no evidence_direction pending that post-mortem.
    epistemic_category substrate_ceiling; routing=implement-substrate;
    pending_retest_after_substrate.

    2026-05-19 governance (failure_autopsy_543i_2026-05-19, confirmed):
    Both V3-EXQ-543i runs non_contributory. Byte-identical runs landed
    opposite basins (06:37Z branch-c all-escape reef 0.28; 19:10Z
    branch-e all-collapse reef 0.69); basin selection RNG/init +
    cross-run + cross-machine nondeterministic, NOT controlled by
    use_differential_heads. ARC-062 architectural slot has had zero
    fair tests -- 543i does not resolve the substrate gap. Manifest
    corrected: evidence_direction 'mixed' -> non_contributory all
    claims. pending_retest_after_substrate.

    2026-05-23 governance (failure_autopsy_V3-EXQ-543k_2026-05-21,
    confirmed): V3-EXQ-543k (20260522T091714Z) FAIL branch-e --
    mode_separation_floor + P1 w-deviation aux did not escape MECH-309
    collapse. Extends failure_autopsy_V3-EXQ-543i. ARC-062=weakens,
    MECH-309=supports per autopsy verdict. Routing: do-not-demote
    ARC-062 until V3-EXQ-598 GAP-C/D completes; V3-EXQ-543l deferred.
    pending_retest_after_substrate through V3-EXQ-598.

    2026-05-27 governance (failure_autopsy_V3-EXQ-543l_2026-05-27,
    confirmed): V3-EXQ-543l (20260526T023059Z) FAIL branch-e at
    escalated floor=0.5 / aux=0.3 -- basin_stable=true, all four
    diff-ON gated arms 3/3 inert (diff_on_escape=false); all four
    diff-OFF gated arms 3/3 inert (diff_off_reproduced_collapse=true,
    valid sanity); D2_xtal_delta=0.008 (crystallization did not
    rescue gated-only arm); repro_543g_signature=true. Confirms
    GAP-B floor+aux alone insufficient -- the head-collapse equilibrium
    survives 2x stronger floor and 3x stronger aux. ARC-062=weakens
    (one contributory FAIL against zero contributory PASS evidence;
    narrow_supports_flag retained -- cluster shape is one structural
    property, not five independent bugs). Cluster pattern is one
    structural property: outcome-coupled REINFORCE on shared-return
    gated heads collapses to inert monomodal equilibrium regardless
    of structural pressure (use_differential_heads, crystallize_at_phase3,
    escalated floor+aux all necessary AND none sufficient). User-confirmed
    reading: substrate-enrichment first via GAP-C/D (discriminator ->
    SD-033a rule_state routing + trainable rule_bias_head, both
    substrate-landed 2026-05-17). pending_retest_after_substrate
    through V3-EXQ-598b. Do NOT demote. epistemic_category=substrate_ceiling
    (unchanged). Further floor/aux ablations (notional V3-EXQ-543m)
    deferred until V3-EXQ-598b lands.

    [2026-06-10 governance / confirmed failure_autopsy_569f-661-654a_2026-06-10]:
    V3-EXQ-654a (rule-apprehension behavioural falsifier) non_contributory --
    self-routed substrate_not_ready_requeue (CandidateRuleField maturation
    unmet: crf_frac_active 0.137 < 0.3 floor, 0/3 ON seeds matured, despite
    rules minted 268/549/220). No demotion (already substrate_ceiling). Two-part
    routing: (1) RE-QUEUE 654a at a longer CRF maturation budget (new letter) so
    frac_active clears 0.3 before scoring; (2) the within-arm rule_state
    counterfactual = 0/3 seeds + committed-class entropy lift ~0 (OFF 0.9375 ->
    ON 0.9377) corroborates the 569f/661/654a E3-commit-readout substrate ceiling:
    even minted rule_state does not reach committed action because the selection-
    authority coupling does not route upstream-channel range into the modulatory
    bias. If a matured-and-firing field STILL shows within-arm counterfactual 0 on
    re-queue, that promotes 654a to a full cluster member and confirms the
    selection-authority coupling -- not maturation -- is the ceiling.

    [2026-06-19 governance / V3-EXQ-654g, supersedes V3-EXQ-654f]:
    The GAP-B behavioural lineage TERMINUS. 654g = first run on the de-locked
    CRF stack (654f proved the CRF-gate calibration amend works; CRF done) wired
    to the 569i-validated top-k shortlist conversion. C1 (non-vacuity) FULLY MET:
    class axis exercisable 1.0, GAP-A divergence real 0.080>0.05, ARM_ON rule
    field differentiated+matured (crf_frac_active 0.94>0.30), propagation
    non-vacuous (within-arm counterfactual delta 0.0021 nonzero -- the rule_state
    DOES reach committed action). C2 (PRIMARY committed-class entropy lift)
    FAILED: ARM_ON 0.6728 vs ARM_OFF 0.6614 = +0.011 nats, 0/3 seeds clear the
    0.05-nat margin. This is the pre-registered C1-holds/C2-fails branch =
    non_contributory, the SHARED selection-authority conversion ceiling
    (behavioral_diversity_isolation:GAP-A; MECH-439 F-dominance live root), NOT a
    falsification of ARC-062/MECH-309. The matured/active/differentiated
    rule-apprehension channel reaches the E3 accumulator with genuine authority
    but cannot move the F-dominated committed argmax. SECOND independent
    behavioural channel to hit this exact wall (after V3-EXQ-485h's OFC
    outcome-value bias, 2026-06-19) -- two distinct channels now corroborate
    MECH-439 as the live root. No status change (already substrate_ceiling /
    v3_pending / pending_retest_after_substrate). 654f -> superseded. Routing:
    /implement-substrate -- the same GAP-A gain/contrast amend; the OFC behavioural
    retest sequenced behind the existing conversion-ceiling chain (ARC-065 569i
    top-k ceiling-lifted / GAP-B / 689a / 625e). The 654 CRF instrumentation
    lineage has cleanly terminated; the residual is the F-dominance ceiling, not a
    654-specific gap.

    V3-EXQ-690 (Q-054 ARC-062 minimum trajectory-class diversity floor sweep) self-routed
    substrate_not_ready_requeue (R1 realised-committed-first-action-entropy RANGE across the
    4 noise-floor-min_temperature arms = 0.0 < 0.2 floor; R3 discriminator non-degenerate
    0/4 arms). Autopsy 2026-06-20 (failure_autopsy_V3-EXQ-690_2026-06-20, confirmed): the
    self-route is trustworthy and NON-WEAKENING. The 4 arms (min_temp 0.30/1.00/1.75/2.75,
    alpha held 0.0) produced BYTE-IDENTICAL per-seed metrics (seed 42 = 0.285896 nats /
    0.823767 acc in every arm) -- a 2.75x effective-temperature change moved nothing. Root
    cause (code-confirmed): the committed first action is an argmin over the F-dominated
    scores (MECH-090), which is temperature-invariant; the MECH-313 noise-floor temperature
    only spreads the uncommitted multinomial, so it is architecturally incapable of moving
    committed first-action entropy. This is (a) the live MECH-439 F-dominance conversion
    ceiling reasserting -- the diversity-floor-sweep instance of the f-dominance convergent
    fingerprint (cf. failure_autopsy_f-dominance-conversion-cluster_2026-06-20; F = 88-89%
    E3 committed-selection variance, V3-EXQ-571) -- expressed as BETWEEN-ARM lever-inertness,
    NOT a within-seed monostrategy (committed entropy is non-zero 0.28-0.73 nats, seed 42
    commits 5 distinct action classes); and (b) a test-design lever-target mismatch. It does
    NOT mask an ARC-062-specific cause: the ARC-062 discriminator scores above chance where
    context is sampled (0.74 mean; 0.82/0.90 on the 2 reef-exposed seeds); the R3 degeneracy
    is environmental (seed 44 = 0 reef ticks). ARC-062 stays candidate / substrate_ceiling /
    v3_pending / pending_retest_after_substrate, UNCHANGED; 690 non_contributory (manifest
    non_degenerate=false -> scoring-excluded). 4th confirmed inert lever after the CRF /
    OFC / dACC channels. Re-running 690 on the current noise-floor lever reproduces R1=0.0;
    requeue 690a is gated on V3-EXQ-689a (the conflict-graded shortlist keystone, BUILT, in
    flight on DLAPTOP-4.local), NOT on the current selector, and must (i) sweep a
    committed-argmax-REACHING lever (569i top-k k / 689a conflict-graded shortlist width) for
    the Q-054 floor question instead of the MECH-313 selection-softmax temperature, and
    (ii) control per-seed reef/forage exposure so R3 is not lost to environmental sampling.
  implementation_note: >
    2026-05-21 IGW-024 substrate-ready reconcile: Phase 1 (GAP-A) IMPLEMENTED
    2026-05-09 -- ree_core/policy/gated_policy.py (two heads + 3-stream
    discriminator, REEConfig.use_gated_policy default OFF, contracts 7/7).
    Canonical substrate-readiness V3-EXQ-542a PASS UC1-UC6 (2026-05-20 manifest;
    dry-run re-verified 2026-05-21). substrate_queue ready=false;
    v3_pending unchanged (trained-policy evidence still gated on V3-EXQ-543k
    + V3-EXQ-598). Follow-ons on main: first-action onehot, differential heads,
    mode_separation_floor (see entries below).

    2026-05-18 (implement-substrate, NO confidence/promotion/flag change --
    governance gated on V3-EXQ-543i). GatedPolicy two-head reparameterization
    landed in ree-v3/ree_core/policy/gated_policy.py behind
    GatedPolicyConfig.use_differential_heads (default False = bit-identical
    pre-fix two-independent-head path). Motivated by the V3-EXQ-543h failure
    autopsy + cross-machine 543g replication
    (evidence/planning/failure_autopsy_V3-EXQ-543h_2026-05-18.{md,json}):
    the same 543g config landed gating-ACTIVE on host-A but INERT
    (n_inert_gating_seeds=3, behavioral-divergence TV<0.05) on cloud-3 AND
    cloud-4 -- head_0==head_1 collapse is the cross-machine COMMON attractor;
    the host-A "weakens ARC-062" landing (_144716Z) is a 1/3 minority
    lucky-basin artifact, so 543g overall is non_contributory and ARC-062
    has zero reliable contributory trained-policy evidence. Fix: heads
    synthesized as base +/- delta_hat with delta_hat's candidate-axis L2
    norm pinned to differential_bias_scale; delta==0 (the inert collapse)
    is structurally non-reachable (scale-invariant normalization -> zero
    loss-gradient on ||delta||; nonzero delta-bias init) and at w=0.5
    d(gated)/dw = 2*delta_hat != 0 gives REINFORCE a non-vanishing escape
    gradient. P1 training loss deliberately UNCHANGED so V3-EXQ-543i
    (supersedes 543g + 543h) is a clean single-variable test of
    structure-vs-MECH-309: escape -> ARC-062 weak reading viable; still
    collapses -> MECH-309 strong confirmation -> ARC-063/V4. A separate
    MECH-309-support reading of the 543 cluster (cross-machine collapse =
    positive evidence for MECH-309) is a parked governance follow-on, not
    applied this session.     See ree-v3/CLAUDE.md "ARC-062 GatedPolicy
    differential-heads robustness fix (2026-05-18)".

    2026-05-21 (implement-substrate GAP-B, NO confidence/promotion change --
    governance gated on V3-EXQ-543k). mode_separation_floor on composed
    gated bias (default 0.0 OFF) plus p1_w_deviation_aux_weight (default 0)
    landed in gated_policy.py + REEConfig passthrough. Motivated by
    failure_autopsy_543i: at w~0.5 differential heads cancel in composed
    output; floor injects non-cancelable contrast; P1 aux penalizes flat w.
    Validation EXQ V3-EXQ-543k (supersedes 543i): floor=0.25, aux=0.1 on gated
    arms, K_IDENTICAL_RUNS=3 basin-stability gate, manifest hostname.
    See ree-v3/CLAUDE.md "ARC-062 GAP-B mode-separation floor (2026-05-20)".
    [2026-08-01 governance, V3-EXQ-851, confirmed failure_autopsy_V3-EXQ-851_2026-08-01,
    GOV-FANOUT-1 Leg P-A erratum-fix retest -- full detail on MECH-309's own evidence_quality_note,
    not duplicated here]: matched-stack retest of V3-EXQ-654j with modulatory_channel_route_source
    corrected to 'lateral_pfc'. Under identical seeds, MECH-448/449 flip from robustly-live to
    completely dead -- deterministic, but the causal path from the route-source change is not yet
    identified (static code read shows the paths as structurally independent). evidence_direction:
    non_contributory -- no H1 verdict possible; user confirmed not to accept the requeue self-route
    at face value. Route: /queue-experiment -- a cheap targeted diagnostic before another ~7.7-hour
    full run. Stays substrate_ceiling / pending_retest_after_substrate, DEFER-to-700b framing
    unchanged.
  epistemic_category: substrate_ceiling
  pending_retest_after_substrate: true
  heterogeneity_note: >
    substrate-version confound + supersession lag. ARC-062's
    conflict_ratio is driven entirely by the 543-lineage 2x2 falsifier
    FAILs (543b/c/d/e), none of which is a confound-free test of the
    weak-reading gated-policy architecture: 543b/c/d ran on the pre-SP-CEM
    collapsed CEM (candidate-feature variance below head discriminability)
    and the dACC factorial axis was never live on ANY of them
    (dacc_weight=0.0). All are reclassified non_contributory. The conflict
    resolves once V3-EXQ-543f provides the first confound-free test
    (SP-CEM + live dACC + head-input augmentation). Not a genuine
    scientific contradiction.
  notes: >
    The split between ARC-062 (weak) and ARC-063 (strong) follows the
    feedback_biology_before_formal_definitions principle in reverse: we
    are NOT formalising a biology-grounded mechanism with light evidence;
    we are testing the simplest engineering-cheap form of the architectural
    commitment first, and only formalising the heavier biology-grounded
    form (ARC-063) if the simple form fails or is insufficient at scale.
    This matches the methodological note in MECH-309's functional_restatement.

    CROSS-REF 2026-05-18 (NO posture change to ARC-062). The ARC-063
    strong reading was brought forward (implementation_phase v4 -> v3)
    and a socially-scaffolded rule-population sub-cluster registered
    (ARC-077 / MECH-337 / MECH-338), on the independent-need rationale
    -- NOT because the weak route is declared dead. ARC-062 remains
    candidate / epistemic_category=substrate_ceiling /
    pending_retest_after_substrate / narrow_supports_flag, UNCHANGED.
    The weak-route verdict is still owned by V3-EXQ-543i
    (/failure-autopsy of the diff-OFF non-reproduction) and the
    in-flight V3-EXQ-543j cross-machine confirmation, ahead of
    ARC-062/MECH-309 governance. This cross-reference does not alter
    ARC-062's status, confidence, flags, conflict ratio, or phase.
  location: docs/architecture/rule_apprehension_layer.md

- id: MECH-349
  title: "CandidateRule mint (ARC-063 CREATE face): a non-gradient structural event mints a distinct CandidateRule slot when a (context-bucket -> action-object) regularity recurs above threshold and no existing rule's context_tag already covers it. The literal MECH-309 answer -- the creator that 'invents' rules so that credit only 'weights' them. Bottom-up (ARC-064 regularity detection) primary; optional top-down ARC-062 discriminator seed."
  claim_type: mechanism_hypothesis
  subject: policy.candidate_rule_field.mint
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  registered_utc: "2026-06-04"
  depends_on:
    - ARC-063
    - MECH-309
    - ARC-064
    - ARC-062
  notes: >
    ARC-063 v1 CREATE face. CandidateRuleField._maybe_mint: recurrence counter keyed on
    (sign-bucket of context, action_object_idx); mint when count >= crf_mint_recurrence_threshold
    AND no existing rule's context_tag cosine >= crf_context_match_threshold AND a free slot
    exists. Minted rule_embedding = the slot's pinned-distinct unit direction (MECH-350);
    context_tag = current context; initial availability = crf_tolerance_floor, nudged by the
    ARC-062 gating_weight confidence when crf_seed_from_arc062. Non-gradient instantiation --
    the structural creator that gradient descent cannot supply (543/598b inert-equilibrium
    finding). Module ree-v3/ree_core/policy/candidate_rule_field.py. Config
    use_candidate_rule_field (no-op default). MECH-094: simulation_mode no-op. Validation V3-EXQ-639.
  location: docs/architecture/arc_063_candidate_rule_field.md

- id: MECH-350
  title: "CandidateRule field representation (ARC-063 REPRESENT face): the field holds K candidate rules as a population of distinct pinned subspace directions, so distinct minted rules occupy distinct directions and the rule_state handed to SD-033a is differentiated by construction. The architectural answer to why two shared-gradient scalar heads collapse (598b) but a minted field does not -- separability is structural, not an equilibrium gradient must find."
  claim_type: mechanism_hypothesis
  subject: policy.candidate_rule_field.subspace_partitioned_representation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  registered_utc: "2026-06-04"
  depends_on:
    - ARC-063
    - MECH-349
    - SD-033a
  notes: >
    ARC-063 v1 REPRESENT face. CandidateRuleField._pinned_directions: deterministic seeded
    (CandidateRuleFieldConfig.pinned_seed, default 6063) unit vectors in rule_dim, one per slot. Minted rules take their slot's
    pinned direction as rule_embedding, so the active-rule stack written to SD-033a is
    differentiated by construction. Biology: Wallis 2001 (PFC abstract rule cells), Weber 2023
    (mixed-selective subspace partitioning lets multiple codes coexist without interference).
    Headline diagnostic crf_max_pairwise_rule_dist. Module ree-v3/ree_core/policy/candidate_rule_field.py.
    Validation V3-EXQ-639 C1 (>=2 distinct active rule vectors).
  location: docs/architecture/arc_063_candidate_rule_field.md

- id: MECH-351
  title: "CandidateRule tolerance gate (ARC-063 GATE face): a candidate rule becomes AVAILABLE (eligible to enter the active rule_state) only when its availability clears a threshold that RISES with the number of competing context-matched rules. The conflict-graded 'hold your horses' dynamic threshold; availability (which rules enter the active set) is distinct from selection (which action E3 picks among available rules' biases)."
  claim_type: mechanism_hypothesis
  subject: policy.candidate_rule_field.tolerance_gated_availability
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  registered_utc: "2026-06-04"
  depends_on:
    - ARC-063
    - MECH-349
    - MECH-338
  notes: >
    ARC-063 v1 GATE face. CandidateRuleField.gate_and_select: matched = rules with
    cosine(context, context_tag) >= crf_context_match_threshold; theta = crf_tolerance_floor +
    crf_tolerance_conflict_gain * (n_matched - 1); a matched rule is active iff availability >=
    theta. Under-supported / high-conflict rules are held out until credit raises availability.
    Biology: Frank 2006 (frontally-set dynamic BG/STN threshold), Cavanagh 2011 (STN conflict
    signal), Frank/O'Reilly 2001 (BG-gates-WM-updating: availability != expression). Module
    ree-v3/ree_core/policy/candidate_rule_field.py. Validation V3-EXQ-639 C3 (conflict-sensitive gate).
  location: docs/architecture/arc_063_candidate_rule_field.md

- id: MECH-352
  title: "CandidateRule eligibility-trace credit (ARC-063 CREDIT face): each rule active when an action was taken leaves a decaying eligibility trace; the subsequent outcome/PE updates the rule's availability (raise on success, lower on exception), recency-weighted, with slow availability decay and retire-below-floor. The 'weight' half of mint-then-weight -- credit refines an existing rule's availability, it does not create rules."
  claim_type: mechanism_hypothesis
  subject: policy.candidate_rule_field.eligibility_trace_credit
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  registered_utc: "2026-06-04"
  depends_on:
    - ARC-063
    - MECH-349
    - MECH-351
  notes: >
    ARC-063 v1 CREDIT face. CandidateRuleField.credit: eligibility-weighted availability EMA toward
    1.0 (outcome>=0 success) or 0.0 (outcome<0 exception) at rate crf_availability_alpha*eligibility;
    eligibility decays at (1 - 1/crf_eligibility_window); availability decays slowly at
    crf_availability_decay; rules below 0.5*crf_tolerance_floor are retired (slot freed). The agent
    feeds an outcome proxy (lower harm = success) for the previous tick's active rules. Biology:
    Brzosko 2015 (retroactive eligibility / dopaminergic conversion of STDP), Kovach 2012 (recency-
    weighted credit). Module ree-v3/ree_core/policy/candidate_rule_field.py. MECH-094: simulation_mode
    no-op. Validation V3-EXQ-639 (credit loop exercised).
  location: docs/architecture/arc_063_candidate_rule_field.md

- id: MECH-353
  title: "Blocked-agency / control-failure affect stream (z_block): an integrated readout on the SD-029 agency comparator applied to the action-outcome channel. z_block rises when an intended action repeatedly fails to produce its forward-model-predicted outcome (MECH-112 z_goal supplies the expectation), the mismatch is attributed to an EXTERNAL constraint (not own motor error), AND goal/capacity-belief is RETAINED. It is a distinct affect stream from harm (SD-011: no noxious input required), from suffering (SD-019b/Q-036: OPPOSITE controllability pole -- capacity-retained drives ASSERT, capacity-collapsed drives WITHDRAW), from residue (MECH-056: action-prevented, not action-taken-at-cost), and from licit commitment-hold (MECH-090: externally-imposed block, not self-imposed gate)."
  claim_type: mechanism_hypothesis
  subject: affect.blocked_agency_control_failure_stream
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-642_2026-06-06
      as_of: 2026-06-06
      verdict: "non_contributory/none -- claimless diagnostic; characterise as a measurement / test-design gap, NOT substrate_ceiling. Do not record substrate_ceiling on any claim from this run."
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-06-05"
  depends_on:
    - SD-029    # agency comparator (the detector: residual = observed - E2-predicted action outcome)
    - MECH-112  # structured latent goal / wanting (supplies the expected outcome)
    - MECH-342  # maintenance-time decommit (the consumer reached when assertion fails)
    - ARC-016   # commitment-threshold (prefrontal-analogue gate on assert -> decommit)
    - MECH-320  # tonic vigor (the assert / escalate-effort behavioural pole)
    - SD-011    # dual nociceptive harm streams (differentiated-from: z_block needs no noxious input)
    - SD-019b   # suffering accumulator (differentiated-from: opposite controllability pole)
  functional_restatement: >
    Mechanism hypothesis. REE represents harm (SD-011), suffering (SD-019b,
    the capacity-collapsed withdraw pole gated by Q-036 escapability), residue
    (MECH-056), and licit self-imposed commitment-holds (MECH-090). It does NOT
    represent the affect that arises when an intended, predicted-to-succeed
    action is repeatedly BLOCKED while the goal and the agent's capacity-belief
    are retained -- the energised "assert / restore" pole.

    MECH-353 asserts a single derived signal z_block, layered on substrate REE
    already has: (1) the detector is the SD-029 agency comparator applied to the
    action-outcome / goal channel (intended effect predicted by the E2 forward
    model, realised effect diverges); (2) the expectation is z_goal / wanting
    (MECH-112); (3) the new computation is the readout plus two gates --
    integrate the comparator mismatch over a window, fire only when the mismatch
    is attributed to an EXTERNAL constraint (not own motor error), and gate on
    RETAINED goal/capacity. Consumers: assert / escalate-effort (raise MECH-320
    vigor, search an alternative action that restores the intended outcome) and,
    if assertion fails across the window, DECOMMIT (MECH-342) gated by the
    commitment-threshold (ARC-016). Withdrawal is NOT native to this stream; it
    is reached only by hand-off to the suffering accumulator (SD-019b) when
    capacity-belief collapses.

    Falsifiable: in an environment that repeatedly blocks an intended,
    predicted-to-succeed action while harm and goal-value are held constant, an
    REE substrate with z_block enabled should produce an assert/persist response
    (effort escalation / alternative-action search) that is behaviourally
    distinct from the suffering withdraw signature, and z_block should dissociate
    from z_harm_a under a matched controllability manipulation. If no such signal
    is needed -- if blocked-action behaviour is fully explained by existing harm
    + suffering + decommit machinery -- the claim is falsified.
  notes: >
    Registered 2026-06-05 from the blocked-agency lit-pull (chip task_64c2e558).
    This is Stream A (V3-tractable, single-agent) of a two-stream split surfaced
    by the verdict; Stream B (coercion / domination / injustice) is V4-social
    (needs a model of an other-agent coercer; psychological reactance) and is
    deferred, NOT registered here. Anger is the shared affective READOUT; the two
    streams that feed it differ in antecedent and scope -- registering one
    emotion-named "anger" primitive would repeat the SD-010 -> SD-011
    philosophy-right / mechanism-wrong error (feedback_biology_before_formal_definitions).
    The biology already shows the convergence: Panksepp's RAGE fires when SEEKING
    is BLOCKED, which is frustrative non-reward. Affect-register row:
    docs/architecture/affect_primitives.md (Extension Register, blocked_agency).
  evidence_quality_note: |
    Registered candidate 2026-06-05. Lit-pull anchor:
    evidence/literature/targeted_review_blocked_agency_anger_stream/VERDICT.md
    (5 entries, all supports). Anchors: Davis & Montag 2019 Front Neurosci
    10.3389/fnins.2018.01025 (RAGE distinct primary-process system, 0.72);
    Papini et al. 2024 J Neurosci 10.1523/JNEUROSCI.1021-24.2024 (frustrative
    non-reward = expected-minus-obtained antecedent, no noxious input, 0.80);
    Bertsch et al. 2020 Curr Psychiatry Rep 10.1007/s11920-020-01208-6
    (reactive-aggression assert channel + prefrontal-gated inhibition; reactive
    != proactive, 0.74); Carruthers 2012 Conscious Cogn 10.1016/j.concog.2010.08.005
    (comparator model = the SD-029 detector, 0.66). NO experimental evidence yet.
    v3_pending=true until the smallest-V3 blocked-action discriminative experiment
    lands (env repeatedly blocks an intended predicted-to-succeed action, harm +
    goal held constant; measure assert-vs-withdraw dissociation from z_harm_a under
    matched controllability). Based on articles retrieved from PubMed.
  implementation_note: >
    Substrate IMPLEMENTED 2026-06-06 (ree-v3). z_block is a pure-arithmetic
    regulator (ree_core/affect/blocked_agency.py) plus a LatentState.z_block
    field. Detector = SD-029 agency comparator on the action-outcome / z_world
    channel (E2.world_forward predicted effect vs realised, normalised by the
    predicted-effect magnitude with a floor) + an external-attribution gate
    (motor_agency on the z_self channel via E2.predict_next_self) + a capacity
    gate (assert while capacity-belief retained; hand off to z_harm_a as it
    collapses). Consumers: ASSERT score-bias (escalate-effort + alternative-action
    search, the MECH-320 vigor effect) and ARC-016-gated DECOMMIT (MECH-342
    actuator). Env substrate: CausalGridWorldV2 scheduled_action_block_* (external
    move-cancel, no damage, no layout change). Config:
    REEConfig.use_blocked_agency (default False; bit-identical OFF). 803/803
    ree-v3 contracts + 9/9 new MECH-353 contracts PASS; OFF action stream
    bit-identical. Phased training N/A. MECH-094: update() no-op on
    simulation_mode. v3_pending stays TRUE: cleared only when the smallest-V3
    blocked-action discriminative experiment PASSes. NOTE: the action-outcome
    comparator is only discriminative once the encoder + action-conditional
    world_forward (SD-056) are trained -- the validation experiment trains both
    in P0. Doc: docs/architecture/mech_353_blocked_agency_zblock.md;
    ree-v3 entry: ree-v3/CLAUDE.md (MECH-353 section).
  location: evidence/literature/targeted_review_blocked_agency_anger_stream/VERDICT.md

- id: MECH-354
  title: "Effort/fatigue stop-recover homeostatic accumulator: a two-bound (hysteretic) leaky cost-evidence accumulator on the SD-012 homeostatic-drive side (substrate-hosted by the SD-048 interoceptive channel). F accumulates with effort / time-on-task and dissipates with rest; at the upper bound it emits STOP / disengage, recovering to a lower bound before re-engaging. Two time-constants: a fast within-task accumulator and a slow sleep-pressure (Process-S) variant whose recover phase is offline (SD-017 sleep). The in-task STOP feeds the MECH-342 release actuator as a new deficit input and the ARC-078 goal-disengagement consumer via a cost/benefit (non-aversive) channel; the recover / re-accumulation half lives on the SD-012 / SD-017 side. Distinct from suffering / learned-helplessness (SD-011 side): fatigue is controllability-independent, incentive-reversible, and -- decisively -- recovers with rest, which helplessness does not."
  claim_type: mechanism_hypothesis
  subject: affect.fatigue_stop_recover_homeostatic_accumulator
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-06-05"
  depends_on:
    - SD-012    # homeostatic drive (home: fatigue is a second deficit variable alongside hunger)
    - SD-048    # interoceptive noise dynamics (substrate host; its restatement names 'fatigue drift')
    - MECH-342  # commit-maintenance release (the shared in-task STOP actuator; fatigue is a new deficit input)
    - ARC-078   # goal-disengagement (cost/benefit consumer, reached from the non-aversive antecedent)
    - SD-017    # sleep (offline recover phase for the slow Process-S variant)
    - SD-011    # dual nociceptive harm streams (differentiated-from: fatigue is NOT a z_harm stream)
  functional_restatement: >
    Mechanism hypothesis. Fatigue / stop-recover and suffering / learned-
    helplessness converge behaviourally on stop/disengage but from OPPOSITE
    antecedents: "I am depleted" (homeostatic, controllability-independent,
    accumulates from your own successful effort) vs "this is hopeless/painful"
    (aversive, controllability-gated). The decisive dissociator is recovery:
    fatigue is a stop-AND-recover signal with an offline reset; helplessness-
    withdrawal is a stop with no rest-recovery (needs controllability experience
    or pharmacological reversal). Modelling fatigue as a nociceptive harm stream
    would import the wrong antecedent (aversion), the wrong gate (controllability),
    and the wrong dynamics (no recovery) -- the DEV-NEED-002 conflation the split
    is meant to cure.

    MECH-354 asserts the smallest computational form (Meyniel 2013): a two-bound
    leaky cost-evidence accumulator -- F += Se*effort during exertion, F -= Sr
    during rest; emit STOP at the upper bound, recover to the lower bound before
    re-engaging; Se/Sr/bound-gap incentive-modulated. Two variants share the form
    but not the time-constant: a fast within-task accumulator and a slow sleep-
    pressure (Process-S) accumulator whose recover phase is offline (SD-017). The
    in-task STOP feeds MECH-342 as a new deficit input (alongside the R-c
    execution-readiness deficit) and ARC-078 via a cost/benefit channel; the
    recover half is on SD-012 / SD-017, NOT in MECH-342.

    Falsifiable: an REE substrate with fatigue enabled should disengage from a
    long effortful task at a rate that (a) rises with time-on-task independent of
    harm/controllability, (b) is reduced by incentive (incentive-reversibility,
    which a pure-depletion account mispredicts), and (c) RECOVERS after a
    rest/offline phase -- a signature absent from the suffering/helplessness
    withdraw pathway.
  notes: >
    Registered 2026-06-05 from the fatigue-vs-helplessness lit-pull (chip
    task_93a719b6). Belongs with hunger/drive on the homeostatic family (SD-012),
    NOT on the SD-011 suffering pathway. The release ACTUATOR is shared with the
    suffering pole (MECH-342 in-task disengage) but the SIGNAL is distinct.
    V3-minimal but GATED: build the transient state-fatigue accumulator behind the
    in-flight cue-authority / z_goal work (containment-only ethos; over-persistence
    prevention is real but not the current critical path). Multidimensional
    (physical/mental/motivational, MFI-20) and trait/chronic fatigue shading toward
    anhedonia are V4-completeness, deferred. Affect-register row:
    docs/architecture/affect_primitives.md (Extension Register, effort/fatigue).
  evidence_quality_note: |
    Registered candidate 2026-06-05. Lit-pull anchor:
    evidence/literature/targeted_review_fatigue_vs_helplessness_dissociation/VERDICT.md
    (5 entries, all supports). Anchors: Meyniel et al. 2013 PNAS
    10.1073/pnas.1211925110 (two-bound cost-evidence accumulator = the smallest
    form); Boksem & Tops 2008 Brain Res Rev 10.1016/j.brainresrev.2008.07.001
    (fatigue = cost/benefit recalibration, not depletion); Borbely et al. 2016
    J Sleep Res 10.1111/jsr.12371 (Process-S homeostatic accumulator, adenosine,
    offline reset); Muller & Apps 2019 Neuropsychologia
    10.1016/j.neuropsychologia.2018.04.030 (state-vs-trait scoping); contrast
    anchor Maier & Watkins 2005 Neurosci Biobehav Rev 10.1016/j.neubiorev.2005.03.021
    (the suffering pole: uncontrollable-aversion, DRN 5-HT/CRF, no rest-recovery).
    NO experimental evidence yet. v3_pending=true until a discriminative experiment
    lands (time-on-task disengage independent of harm/controllability, incentive-
    reversible, with a rest-recovery phase). Based on articles retrieved from PubMed.
  location: evidence/literature/targeted_review_fatigue_vs_helplessness_dissociation/VERDICT.md

- id: MECH-355
  title: "Soothing/comfort autonomic state-gain modulator: a present-tense down-regulation of the ONGOING aversive/stress state (lowers the gain or speeds the recovery of the active trajectory), canonically socially gated (conspecific buffering; oxytocinergic + parasympathetic/HPA + prefrontal regulation). Proposed home is a modulator on MECH-219 (suffering accumulator) decay + SD-012 (drive) + SD-032e (pACC autonomic coupling). Distinct from relief (MECH-302: past-offset phasic reinforcer), from safety (MECH-303/304: future inhibitory predictor), and from wanting (MECH-112: appetite): soothing acts on the present stress trajectory via autonomic state-gain, not reinforcement, prediction, or appetite. V4-social (the canonical trigger is an other-agent, which V3 cannot represent); an optional V3-minimal non-social autonomic-recovery hook is noted but not built."
  claim_type: mechanism_hypothesis
  subject: affect.soothing_comfort_autonomic_state_gain_modulator
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  v3_pending: false
  epistemic_category: substrate_conditional
  claim_level: mechanistic
  registered_utc: "2026-06-05"
  depends_on:
    - MECH-219  # suffering accumulator (the decay/gain target soothing modulates)
    - SD-012    # homeostatic drive
    - SD-032e   # pACC autonomic coupling (drive_level write-back layer)
    - SD-011    # dual nociceptive harm streams (the ongoing stress state being down-regulated)
  functional_restatement: >
    Mechanism hypothesis (V4-social). Soothing/comfort is the third member of the
    relief/safety/soothing triple and is dissociable from both: it acts on the
    PRESENT stress trajectory (vs relief's past-offset, safety's future-prediction),
    its substrate is oxytocin/parasympathetic/HPA (vs NAc/VTA for relief, cortico-
    amygdalar for safety), and its computational role is a state-gain / recovery
    modulator that lowers the magnitude or speeds the recovery of the active
    aversive state -- not a reinforcer (relief), not a predictor (safety), not
    appetite (MECH-112). The canonical trigger is a conspecific (social buffering),
    which V3 cannot represent; the substrate is therefore conditional on the V4
    social model (epistemic_category substrate_conditional -- promote/demote
    suppressed until that substrate exists).

    Proposed REE home: a modulator on MECH-219 (suffering accumulator) decay/gain
    + SD-012 drive + SD-032e pACC autonomic coupling -- a soothing input LOWERS the
    gain / SPEEDS the recovery of the active aversive trajectory, mechanistically a
    modulator on these, not a new harm/reward channel. Do NOT fold into Relief
    (MECH-302) or Safety (MECH-303/304), and NOT into MECH-112. An optional
    V3-minimal non-social autonomic-recovery hook (parasympathetic down-regulation
    of ongoing arousal) is noted but explicitly NOT on the V3 critical path.
  notes: >
    Registered 2026-06-05 from the relief/safety/soothing lit-pull (chip
    task_3ab79018). This is the genuine UNREGISTERED gap that pull surfaced --
    relief (MECH-302) and safety (MECH-303/304) were already registered; soothing
    was not. V4-social primary scope. The exact update rule (gain-reduction vs
    decay-acceleration vs setpoint-shift on MECH-219/SD-012) needs a design pass,
    not more lit. Affect-register row: docs/architecture/affect_primitives.md
    (Extension Register, soothing/comfort).
  evidence_quality_note: |
    Registered candidate 2026-06-05. Lit-pull anchor:
    evidence/literature/targeted_review_affect_stream_relief_safety_soothing/verdict.md
    (differentiation conf 0.82). Soothing-specific entries: Hostinar, Sullivan &
    Gunnar 2014 Psychol Bull 10.1037/a0032671 (social buffering = dampened HPA in
    the presence/assistance of a conspecific; oxytocinergic + prefrontal; supports
    the differentiation, 0.78); Heinrichs et al. 2003 Biol Psychiatry
    10.1016/S0006-3223(03)00465-7 (oxytocin x social support suppress cortisol +
    subjective anxiety to acute stress; causal human anchor, 0.76). REE-mapping
    fidelity is moderate-low (no V3 other-agent model, no HPA axis -- V4-bound,
    analogical mapping); promote/demote suppressed via substrate_conditional. NO
    experimental evidence; registration of the V4 substrate + the update rule is a
    later design decision. Based on articles retrieved from PubMed.
  location: evidence/literature/targeted_review_affect_stream_relief_safety_soothing/verdict.md

- id: MECH-356
  title: "Endogenous parasympathetic recovery / autonomic rebound: an active, offset-triggered, transient acceleration of return-to-baseline for the affective/autonomic stress state, fired by the agent's OWN stressor offset (the z_harm_a derivative going negative -- the MECH-302 SufferingDerivativeComparator signal) with NO conspecific and NO prediction. Shares the effector/update rule of MECH-355 soothing (decay-acceleration on the recovery side: a transient state-scaled multiplier on the MECH-219 z_harm_a recovery_rate + the SD-032e drive_bias leak) but differs by TRIGGER (endogenous offset vs conspecific). The V3-tractable sibling of MECH-355 that closes REE's sympathetic-rich / parasympathetic-poor asymmetry (dense escalation: MECH-219 onset, SD-032e accumulation, SD-037 override, MECH-091 urgency, MECH-279 freeze; thin recovery: only passive recovery_rate + offline-only drive_bias decay). Distinct from passive MECH-219 recovery (active overshoot, not decay), from MECH-302 relief (shared offset trigger, different output -- relief writes a reinforcer, rebound boosts recovery rate), from MECH-303/304 safety (no prediction), and from MECH-355 soothing (endogenous trigger, not social)."
  claim_type: mechanism_hypothesis
  subject: affect.endogenous_parasympathetic_recovery_autonomic_rebound
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  epistemic_category: standard
  claim_level: mechanistic
  registered_utc: "2026-06-06"
  depends_on:
    - MECH-219  # z_harm_a hysteretic integrator -- the recovery_rate this transiently boosts
    - SD-032e   # pACC autonomic drive_bias -- the leak side this accelerates (negative-relaxation the live tick never reaches)
    - MECH-302  # suffering-derivative comparator -- supplies the shared offset trigger (z_harm_a derivative < 0)
    - SD-011    # dual nociceptive harm streams (the ongoing stress state being recovered from)
  distinct_from:
    - MECH-355  # soothing: SAME effector/update rule, differs only by trigger (endogenous offset vs conspecific) -- sibling, not fold-in
    - MECH-302  # relief: shared offset trigger but different output (reinforcer write vs recovery-rate boost)
    - MECH-303  # contextual safety: future predictor, not present-state recovery
    - MECH-304  # cue-specific safety: future predictor, not present-state recovery
    - MECH-112  # wanting/appetite: orthogonal
  functional_restatement: >
    Mechanism hypothesis (V3-tractable). Recovery from an aversive episode is an
    ACTIVE process, not the passive decay of the aversive signal: the literature
    shows a parasympathetic surge that rises ABOVE baseline at stressor offset while
    sympathetic drive is still elevated (vagal rebound). MECH-356 instantiates this
    as an endogenous, offset-triggered, TRANSIENT boost to the recovery side of REE's
    two slow affective accumulators -- recovery_rate_eff = recovery_rate * (1 + nu_R
    * R) on the MECH-219 z_harm_a integrator (recovery side only; onset untouched so
    a recovering agent still detects a fresh injury at full sensitivity), and
    drive_bias *= (1 - leakR * R) per waking tick on SD-032e (the active waking analog
    of the dormant offline_decay hook, using the negative-relaxation direction the cap
    already permits but the live tick never reaches). R(t) in [0,1] rises when the
    z_harm_a derivative goes negative (the MECH-302 comparator signal), optionally
    gated to fire only after a supra-threshold episode, and decays back to 0 over a
    short post-offset window. The rule is multiplicative on EXISTING accumulated state,
    so it has zero effect on a calm agent (recovery != sedation, inherited from the
    MECH-355 design pass). It NEVER writes SD-012 base drive_level (a recovered agent
    is still hungry), z_harm_s (SD-010), or z_harm_un (SD-019a). MECH-094: waking-only
    -- an offset detected during replay/simulation must not trigger rebound.

    This is the SAME effector and update rule as MECH-355 soothing; the two are
    dissociable by TRIGGER (endogenous offset vs conspecific), not by effector --
    the literature shows social support modulates the same vagal-rebound variable.
    Register as a sibling of MECH-355, NOT a fold-in: MECH-356 is the endogenous,
    V3-tractable arm; MECH-355 is the social, V4 arm. Together they close the
    sympathetic-rich / parasympathetic-poor asymmetry.

    Falsifiable (smallest V3 experiment): drive a supra-threshold z_harm_a episode
    then release it; rebound-ON returns z_harm_a / drive_bias to baseline faster than
    rebound-OFF, with onset/peak UNCHANGED (not a gain reduction), with ZERO effect on
    a never-stressed control (load-proportionality), and with NO conspecific present
    (the MECH-355 confound control). A faster z_harm_a decay should also lower the
    MECH-091 urgency-interrupt rate (a recovered agent less prone to panic-abort).
  notes: >
    Registered 2026-06-06 from the autonomic-rebound lit-pull (session
    litpull-autonomic-rebound-parasympathetic-recovery-20260606T0402Z). Surfaced by
    the MECH-355 soothing design pass
    (evidence/planning/mech_355_soothing_update_rule_and_scope_design_2026-06-05.md
    section 3.2 / 5), which kept it explicitly OUT of MECH-355 to keep the social
    claim clean and gated its registration on this lit-pull (biology-before-formal-
    definitions). epistemic_category=standard (NOT substrate_conditional like
    MECH-355) because it is V3-tractable and therefore earns experimental gating.
    V3-minimal but build is GATED behind the in-flight cue-authority / z_goal work
    (containment-only ethos; off the V3 critical path). Affect-register row:
    docs/architecture/affect_primitives.md (Extension Register, autonomic rebound /
    endogenous parasympathetic recovery). Whether MECH-356 and MECH-355 eventually
    collapse into one module with two trigger inputs is a post-validation refactor
    question, not a registration-time one.
  evidence_quality_note: |
    Registered candidate 2026-06-06. Lit-pull anchor:
    evidence/literature/targeted_review_autonomic_rebound_parasympathetic_recovery/verdict.md
    (4 entries, conf 0.74). Anchors: Mezzacappa et al. 2001 Psychosom Med
    10.1097/00006842-200107000-00018 (vagal rebound rises above baseline DESPITE
    residual sympathetic activation -> active recovery distinct from passive decay --
    the decisive entry); Thayer & Brosschot 2005 Psychoneuroendocrinology
    10.1016/j.psyneuen.2005.04.014 (central autonomic network: endogenous prefrontal-
    vagal recovery driver, no external cue needed); Tung et al. 2021 Psychophysiology
    10.1111/psyp.13808 (emotional support MODULATES vagal rebound -> rebound + soothing
    converge on one recovery variable, dissociable by trigger not effector -- validates
    sibling-not-fold; direction mixed); Cunha et al. 2015 SpringerPlus
    10.1186/s40064-015-0882-1 (parasympathetic reactivation has its own state-conditioned
    KINETICS -> recovery is a tunable rate, supports the smallest-form; exercise transfer
    caveat). NO experimental evidence yet (exp_conf=0). v3_pending=true until the
    drive-then-release recovery-rate discriminative experiment lands (faster recovery
    ON vs OFF with onset/peak unchanged + zero effect on never-stressed control + no
    conspecific). Based on articles retrieved from PubMed.
  location: evidence/literature/targeted_review_autonomic_rebound_parasympathetic_recovery/verdict.md

- id: ARC-063
  title: "Rule-apprehension architectural slot, strong reading: a distributed CandidateRule field with tolerance-gated availability, hippocampal-rollout-eligibility shaping, structured per-action evidence-trace records, and waking-vs-sleep refinement asymmetry. The biology-faithful form of the rule-apprehension layer, formalising the 2026-05-04 thought-intake on tolerance-gated rule apprehension (PNAS 10.1073/pnas.2508061123). Brought forward V4->V3 2026-05-18; V3-tractable design landed 2026-06-04 (docs/architecture/arc_063_candidate_rule_field.md). Only the social in-face (ARC-077/MECH-337) is V4-deferred (needs a caregiver-agent substrate absent in V3); the sleep-vs-waking refinement asymmetry is V3 (sleep rescoped to V3 2026-04-20; reuses the MECH-272/273/285 sleep cluster) and is a V3 follow-on within ARC-063, NOT V4. Persistent flag for governance: this slot is expected to be needed if ARC-062 (weak reading) fails or proves insufficient at scale, AND independently as the architectural commitment that supports moral-residue attribution to the rule-field, sleep-vs-waking refinement asymmetry, and clinically-realistic failure modes (trauma schemas, paranoid rule fields, depressive rollout constraint)."
  claim_type: architectural_commitment
  subject: policy.rule_apprehension_layer.strong_reading
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-654g_2026-06-19
      as_of: 2026-06-19
      verdict: non_contributory/substrate_ceiling
  implementation_phase: v3
  v3_pending: true
  pending_retest_after_substrate: true
  governance_2026_06_11: >
    pending_retest_after_substrate set true (governance cycle 2026-06-11 #2),
    per failure_autopsy_V3-EXQ-666_2026-06-11 (confirmed). V3-EXQ-666 showed the
    2026-06-11 ARC-063 mature-pool amend achieves rule DIFFERENTIATION only via
    crf_context_from_e2_world_forward (ARM_2 dist 1.71) but WORSENS PERSISTENCE
    (frac_active 0.016, below the 0.30 readiness floor): narrowly-tuned rules
    match sparsely so availability never accumulates between matches. New
    substrate_queue entry crf-availability-maintenance (priority 1, gated on the
    targeted_review_arc_063_crf_rule_cell_persistence lit-pull) owns the fix.
    epistemic_category left to inference (substrate_coherence for an
    architectural_commitment); status unchanged (candidate).
  claim_level: architectural
  registered_utc: "2026-05-08"
  brought_forward_utc: "2026-05-18"
  design_doc: docs/architecture/arc_063_candidate_rule_field.md
  convergence_2026_06_17: >
    Forward cross-ref (does NOT change status). The REE_convergence rule-distinguishability
    intake (2026-06-17; promotion packet CPKT-RULE-DISTINGUISHABILITY-20260617) registered
    two V4-leaning candidate mechanisms for the CRF distinguishability problem this slot
    owns -- the conflict-gate lockout (theta(n_matched) >> maintenance floor -> crf_frac_active
    ->0; failure_autopsy_V3-EXQ-654d): MECH-437 (maintenance-side consolidation/refactor
    operator, DreamCoder wake-sleep library-refactoring analog) and MECH-438 (construction-side
    separable-key codebook, DreamerV3 categorical-latent analog). Both depend_on ARC-063 and
    are the principled-mechanism candidates behind the substrate_queue crf-availability-maintenance
    point-fix line. candidate/substrate_conditional/v4; OFF the V3 critical path (block no V3
    closure node). Arch doc: docs/architecture/rule_distinguishability_maintenance.md.
  implementation_note: >
    2026-06-04: V3-tractable design landed (docs/architecture/arc_063_candidate_rule_field.md),
    then v1 SUBSTRATE IMPLEMENTED the same day. Resolves arc_062_rule_apprehension:GAP-B's blocker via a
    mint-then-weight split: a non-Bayesian rule-creator (bottom-up regularity-mint ARC-064 + top-down
    ARC-062 seed) populates a subspace-partitioned CandidateRule field (Weber 2023 anti-monomodal
    geometry; Wallis 2001 rule cells), tolerance-gated availability (Cavanagh 2011 / Frank 2006;
    availability!=selection Frank-O'Reilly 2001), cue-driven context retrieval (MECH-338), writing
    DIFFERENTIATED rule_state into SD-033a (the literal 598b gap) with eligibility-trace credit (Brzosko
    2015 / Kovach 2012). The three pre-design lit gaps were closed by the 2026-06-04 A+B+C top-up pulls.
    v1 substrate: ree-v3/ree_core/policy/candidate_rule_field.py (CandidateRuleField, pure-arithmetic, no
    trained params -> no phased training) + SD-033a LateralPFCAnalog.update crf_source wiring
    (use_candidate_rule_source) + agent select_action tick + REEConfig.use_candidate_rule_field (no-op
    default, bit-identical OFF; precondition requires use_lateral_pfc_analog). MECH-094 by simulation_mode
    no-op + MECH-319 _lpfc_skip. Child claims REGISTERED 2026-06-04: MECH-349 (CREATE) / MECH-350
    (REPRESENT) / MECH-351 (GATE) / MECH-352 (CREDIT). Contracts test_candidate_rule_field.py 8/8;
    775/782 contracts (7 fails = pre-existing local-git-env runner-recovery artifact). Status/flags
    unchanged (still candidate + v3_pending); substrate landing does not promote (governance gated on the
    V3-EXQ-639 substrate-readiness diagnostic + the ARC-062 GAP-B behavioural re-run). Sleep-vs-waking
    rule-field refinement is a V3 follow-on (NOT V4); only the social in-face (ARC-077/MECH-337) is
    V4-deferred. ree-v3/CLAUDE.md "ARC-063 v1" section is the implementation source of truth.
  depends_on:
    - MECH-309    # the diagnostic claim that motivates the rule-apprehension layer cluster
    - ARC-064     # 2026-06-04 design: the bottom-up regularity-mint population source (the V3 creator)
    - ARC-062     # the V3 weak-reading first pass; ARC-063 is its biology-faithful elaboration or successor
    - ARC-077     # 2026-05-18 bring-forward: the socially-scaffolded rule-population pathway that fills this field (peer to ARC-062 top-down / ARC-064 bottom-up)
    - MECH-337    # child of ARC-077: caregiver-scaffolded rule-population mechanism (the "in" face of this field)
    - MECH-338    # child of ARC-077: cue-driven context-bound rule retrieval (the context-bound rollout-eligibility gate / "select" face of this field)
    - SD-029      # the downstream measurement; ARC-063 carries SD-029 the rest of the way if ARC-062 cannot
    - SD-033a     # rule_state substrate (committed-rule side); ARC-063 sits upstream of SD-033a in the apprehension -> commitment pipeline
    - SD-034      # closure operator for committed rule_states; complementary to ARC-063 on the release side
    - MECH-269    # V_s monostrategy: representational precondition
  epistemic_category: substrate_conditional
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: two independent gaps must both close before a
    test can discriminate the strong reading from ARC-062's weak reading, and
    NEITHER is currently met.

    (1) SUBSTRATE COMPLETENESS. candidate_rule_field.py (862 lines) implements
    only 4 of 6 distinctive pieces -- CREATE/REPRESENT/GATE/CREDIT (MECH-349-352)
    landed. Missing: hippocampal-rollout-eligibility shaping (zero references
    in the module), the full named status lifecycle (only mint/retire exist),
    structured per-action evidence-trace records (CREDIT is a scalar trace,
    not the rollout->commitment->outcome->residue chain the claim specifies),
    and sleep-side refinement wiring (no CRF reference anywhere under
    ree_core/sleep/).

    (2) SHARED DOWNSTREAM CONVERSION CEILING. Every ARC-062 GAP-B behavioural
    falsifier run (V3-EXQ-654g/654j/485h/485m/714/719a, 21 autopsies) shows
    the identical C1-holds/C2-fails signature: rule-biased signal reaches E3
    with genuine authority but fails to convert to committed-class diversity
    (MECH-439 F-dominance conversion ceiling, shared by both readings,
    in-flight via ARC-108/MECH-450). Until this lifts, any committed-action
    strong-vs-weak test is confounded.

    CONFIRMING signature (once both clear), using ARC-063's own pre-registered
    fork: (a) SCALE/HISTORY -- ARC-062's heads degrade below floor past 2
    competing strategies or with history-dependent cuts, while the CRF holds;
    (b) CLINICAL FAILURE MODE -- a rule-field-directed failure (paranoid
    over-generalisation, depressive rollout-constraint) inducible via
    named-rule residue attribution with no ARC-062 analogue; (c) SLEEP
    ASYMMETRY -- a sleep-phase pass measurably reorganises the rule pool
    beyond an equivalent waking-only pass, no ARC-062 analogue.

    FALSIFYING signature: with both preconditions cleared, (a)/(b)/(c) all
    fail to discriminate -- would falsify the strong reading's claimed
    INDEPENDENT NEED and argue for retiring ARC-063 to a persistent-flag/V4
    watch rather than an active V3 design target.

    Given neither precondition is met, and given zero experimental evidence
    to date (claim_evidence.v1.json: 29 lit entries, 0 exp entries,
    evidence_quadrant plausible_unproven), disposition is substrate-blocked.

    NOTE FOR FUTURE DIGESTION: ARC-062 (weak reading) already has a mature,
    21-times-run experimental falsifier design (the GAP-B behavioural
    lineage) -- digesting it is mostly formalizing criteria that already
    exist, not designing new tests. It should reuse precondition (2) above
    verbatim (both readings share the identical MECH-439 conversion ceiling)
    so the pair does not drift into inconsistent release conditions.
  functional_restatement: >
    [STALE 2026-08-10 governance: this entire block predates the
    2026-05-18 V4->V3 bring-forward (brought_forward_utc field above) and
    was never revised to match. Every "V4"/"V4-bound"/"implementation_phase=v4"
    reference below is historical, not current -- ARC-063 is
    implementation_phase: v3, v3_pending: true, with a landed V1 substrate
    (2026-06-04, ree_core/policy/candidate_rule_field.py). See notes field
    below (already correctly dated) and evidence_quality_note for the live
    reading. Left unedited rather than rewritten, per this repo's
    evidentiary-integrity convention for superseded prose.]

    ARC-063 registers the V4 architectural slot for the strong-reading
    form of the rule-apprehension layer. It formalises the structural
    cluster proposed in the 2026-05-04 thought-intake on tolerance-gated
    rule apprehension (rule_apprehension_layer.md), in which:

      - Candidate regularities are tracked as named CandidateRule objects
        with per-rule supporting / exception evidence counts, tolerance
        scores, context tags, affective weights, salience weights, goal
        relevance, approach / avoid weights, and status flags
        (latent | rollout_eligible | action_tested | strengthened |
        weakened | split | merged | retired)
      - A Tolerance-Principle-style availability gate determines when a
        candidate regularity becomes rollout-eligible (the threshold need
        not be the final action-selection mechanism; it is a rule-access
        gate)
      - Hippocampal rollout generation is biased by active CandidateRule
        objects: rules embed in the rollout landscape itself, becoming
        "psychologically real" by shaping which futures are easy to
        imagine rather than by being verbally endorsed
      - Goal systems then weight rule-shaped rollouts on the existing
        approach / avoid / reward / threat / attachment / status /
        curiosity / energy / social / moral / pain dimensions
      - Basal ganglia commitment (existing SD-033a / SD-034 cluster)
        crystallises the weighted field into action; the apprehension
        layer feeds it but is not the same system
      - Per-action evidence trace records are stored, linking active
        rules -> generated rollouts -> selected rollout -> suppressed
        alternatives -> commitment -> outcome -> prediction error ->
        harm / benefit -> moral residue
      - Waking refinement and sleep refinement are both substantial:
        waking weakens / strengthens / splits / merges rules and updates
        rollout eligibility; sleep continues that work via replay-side
        compression, schema renormalisation, exception-cluster detection,
        and counterfactual recombination
      - Moral residue attaches not only to outcomes but to the rule-
        field that biased rollout, the goal weights that pushed
        selection, and the suppressed-alternatives at the commitment
        bottleneck

    Falsifiable predictions distinguishing ARC-063 from ARC-062 (after
    ARC-062 has been tested):
      (a) ARC-062 succeeds on SD-054 / SD-029 but fails to scale to
          environments with more than two competing strategies, OR fails
          when the discriminative cut is itself a function of agent
          history rather than current observation -- ARC-063 should
          handle both because CandidateRule objects can carry context
          tags that include history.
      (b) Clinical / cognitive failure modes that require a rule-field
          to attribute to (trauma schemas, paranoid rule fields,
          depressive rollout-constraint, OCD over-generation,
          attachment-mediated approach-avoid biasing) cannot be
          implemented within ARC-062 alone -- they need ARC-063's
          named CandidateRule structure for residue attribution.
      (c) Sleep-vs-waking refinement asymmetry (waking can do
          substantial rule-update; sleep does compression /
          renormalisation / counterfactual recombination on top) needs
          structured evidence trace records that ARC-062 does not
          provide.

    These three test classes are the V4 validation gates for ARC-063.
    None of them is V3-tractable; all three are V4-bound.

    Persistent-flag function. ARC-063 is registered now -- ahead of
    implementation -- as the user-requested machine-grep-able commitment
    that the strong-reading form of rule apprehension is expected to be
    needed across the system. Cross-references from MECH-309, ARC-062,
    SD-054, SD-029, SD-033a, SD-034 keep this flag visible on every
    governance run. The implementation_phase=v4 setting prevents the V3-
    Pending Gate from spuriously demoting it for lack of V3 evidence,
    and surfaces it as a known V4 architectural commitment in the
    promotion / demotion recommendations report.
  evidence_quality_note: >
    BROUGHT FORWARD 2026-05-18 (user-directed; scope-gated via
    AskUserQuestion). Trigger: the ARC-062 weak route is blocked
    (GAP-B status=blocked; the 543f/543g/543h falsifier cluster is
    non_contributory, epistemic_category=substrate_ceiling; ARC-062 /
    MECH-309 carry narrow_supports_flag with ZERO reliable contributory
    trained-policy evidence). Per the claims.yaml decision tree
    (escape -> ARC-062 weak reading viable; still collapses -> MECH-309
    strong-confirmed -> ARC-063/V4), and on the independent-need
    rationale (ARC-063 is required regardless of the weak-route verdict
    for moral-residue attribution and clinically-realistic failure
    modes), ARC-063 is promoted from V4-deferred persistent-flag to an
    active design target: implementation_phase v4 -> v3, v3_pending
    false -> true, brought_forward_utc 2026-05-18. It will now surface
    under the V3-Pending Gate as hold_pending_v3_substrate rather than
    "V4-deferred architectural commitments".

    NO change to ARC-062 posture this session. V3-EXQ-543j is in flight
    (concurrent active session) to cross-machine-confirm the 543i
    differential-heads result and resolve the weak-route verdict before
    ARC-062/MECH-309 governance; ARC-063 is brought forward in parallel
    on the independent-need basis, NOT as a declaration that the weak
    route is dead. ARC-062 stays candidate / substrate_ceiling /
    pending_retest_after_substrate, unchanged.

    HARD PREREQUISITE -- biology lit-pull (NOT YET DISCHARGED). Per the
    standing biology-before-formal-definitions rule, the
    socially-scaffolded population sub-cluster (ARC-077 / MECH-337 /
    MECH-338, registered 2026-05-18 alongside this bring-forward) and
    any V4 strong-reading-internal machinery are registered as
    structural slots ONLY. No implementation (V3 or V4) of ARC-063 or
    its ARC-077 population sub-cluster may begin until a caregiver-
    scaffolding / cued-recall / context-dependent rule-acquisition
    lit-pull is discharged, covering at minimum: Csibra & Gergely
    natural pedagogy / ostensive cueing; Vygotsky ZPD + Wood/Bruner/Ross
    scaffolding; Tomasello shared-intentionality + early rule
    acquisition; joint attention + social referencing; context-dependent
    / cued recall (Godden & Baddeley, Tulving encoding-specificity);
    plus the lit base already named below. See
    arc_062_rule_apprehension_plan.md GAP-L (lit-pull prerequisite node).

    Registered 2026-05-08 as the persistent V4-deferred flag for the
    strong-reading rule-apprehension cluster. Implementation cost
    estimate: substantial -- this is a V4 architectural commitment
    spanning policy, hippocampal rollout, evidence-trace memory, and
    sleep-side processing; not a single-module change.

    Lit base. The 2026-05-04 thought-intake (rule_apprehension_layer.md)
    anchors on PNAS 10.1073/pnas.2508061123 (Tolerance Principle for
    convention learning). A wider biology lit-pull is recommended
    before any V4 implementation work begins, covering at minimum:
    Tomasello / Bates on early rule acquisition; hippocampal-rollout
    literature (Buzsaki, Pfeiffer & Foster, Carr et al. on preplay /
    replay); basal-ganglia gating literature (Mink, Frank et al.);
    waking-vs-sleep memory consolidation asymmetry literature; clinical
    schema-formation literature (Beck on depressive schemas, Foa on
    PTSD-schema, etc.). The biology-before-formal-definitions principle
    (memory feedback file) applies here.

    Sub-claim landscape (corrected 2026-05-18 during the bring-forward;
    the original 2026-05-08 list assigned MECH-310..313 as placeholders
    and is now PARTLY STALE -- two of those IDs were subsequently used
    for unrelated claims and MUST NOT be reused for the strong reading):

      ACTIVE -- socially-scaffolded population sub-cluster, registered
      2026-05-18 (the "where do candidate rules come from" answer; peer
      to ARC-062 top-down / ARC-064 bottom-up; fills this field):
        ARC-077  -- socially-scaffolded rule-population pathway (parent)
        MECH-337 -- caregiver-scaffolded rule-population mechanism
        MECH-338 -- cue-driven context-bound rule retrieval (cue pipeline)

      STILL-UNREGISTERED strong-reading-internal machinery (needs FRESH
      IDs at V4 take-up -- the old placeholder IDs are no longer valid):
        - tolerance-gated rule-availability mechanism  (placeholder was
          "MECH-310"; MECH-310 is still free but assign at take-up)
        - rules-as-rollout-selection-structures mechanism (placeholder
          was "MECH-311"; MECH-311 still free)
        - rule-evidence-trace records mechanism  (placeholder "MECH-312"
          is NOW TAKEN by policy.rule_arbitration_multi_variable -- do
          NOT reuse; allocate a fresh MECH ID)
        - waking-vs-sleep rule-refinement asymmetry mechanism
          (placeholder "MECH-313" is NOW TAKEN by
          policy.stochastic_noise_floor_lc_ne_tonic_analog -- do NOT
          reuse; allocate a fresh MECH ID)
        - INV: action-requires-collapse (INV-COMMITMENT-001 candidate
          in intake; needs a real INV ID at take-up)
        - INV: no-waking-rule-is-final (INV-UNCERTAINTY-001 candidate)

    The ACTIVE sub-cluster is registered now (structural slots, gated on
    the GAP-L biology lit-pull). The strong-reading-internal machinery
    stays unregistered to avoid premature commitment; this list is kept
    machine-grep-able (indexer query "ARC-063 sub-claim landscape").
  notes: >
    Persistent flag function. ARC-063 is the user-requested mechanism
    by which the strong reading of rule apprehension stays visible to
    every future governance cycle and every memory walk. Three places
    where it surfaces:
      1. claims.yaml entry. As of the 2026-05-18 bring-forward
         implementation_phase=v3 / v3_pending=true -- the V3-Pending
         Gate now SURFACES it as hold_pending_v3_substrate (no longer
         under "V4-deferred architectural commitments"). Pre-2026-05-18
         it was implementation_phase=v4 and excluded from promotion
         logic; that exclusion no longer applies.
      2. Cross-reference network -- depends_on / depended_by relations
         from MECH-309, ARC-062, ARC-077, MECH-337, MECH-338, SD-054,
         SD-029, SD-033a, SD-034 keep it linked from the live
         experimental cluster.
      3. rule_apprehension_layer.md design doc -- the architecture-
         doc-level anchor that any future designer working on rule-
         apprehension, sleep-vs-waking refinement, moral-residue
         attribution, or schema-failure-mode pathology will surface.
  location: docs/architecture/rule_apprehension_layer.md

- id: ARC-077
  title: "Socially-scaffolded rule-population pathway: the third rule-source, peer to ARC-062 (top-down gated-policy apprehension) and ARC-064 (bottom-up behavioural-pattern extraction), and upstream-fed by ARC-065 (behavioural diversity generation). Architectural commitment: in biology the candidate-rule hypothesis space is not invented by the agent alone -- caregivers populate it from outside via scaffolded exposure (Vygotsky ZPD; Wood/Bruner/Ross scaffolding; Csibra & Gergely ostensive cueing / natural pedagogy; Tomasello shared intentionality; joint attention / social referencing). This is the most direct architectural answer to MECH-309: a non-Bayesian rule-creator that does not invent its own hypotheses receives candidate regularities, context-bound to the scaffolding episode, from a caregiver. The pathway fills the ARC-063 CandidateRule field (the 'in' face); MECH-337 is the population mechanism; MECH-338 is the cue-driven context-bound retrieval mechanism (the 'select' face). Registered 2026-05-18 as a structural slot only -- hard-gated on the GAP-L biology lit-pull and on a caregiver/teacher-agent substrate that does not yet exist in V3."
  claim_type: architectural_commitment
  subject: rule_apprehension.socially_scaffolded_rule_population_pathway
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-077@2026-05-19T21:27:25Z"
      as_of: 2026-05-19
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: "2026-05-18"
  depends_on:
    - MECH-309    # the diagnostic this pathway most directly answers (Bayesian/gradient learners do not invent the hypothesis space; a caregiver supplies it)
    - ARC-063     # the CandidateRule field this pathway populates ('in' face); brought forward 2026-05-18
    - ARC-065     # upstream behavioural-diversity generation -- a precondition (a scaffolded candidate still needs behavioural variety to be exercised against)
    - ARC-062     # sibling top-down route whose blockage (substrate_ceiling) motivated bringing the strong-reading cluster forward
    - ARC-064     # peer bottom-up route; ARC-077 is the third (socially-sourced) member of the rule-source triad
    - MECH-269    # V_s monostrategy: representational precondition (a scaffolded context must be discriminably represented before it can tag a CandidateRule)
  lit_conf: 0.74
  literature_anchors:
    - "Csibra & Gergely (2009) Trends Cogn Sci 13:148 -- natural pedagogy /
       ostensive cueing: human communication is adapted to transmit generic,
       kind-relevant knowledge an observer could not extract alone; the
       direct grounding for an externally-populated hypothesis space
       (answers MECH-309). conf 0.80. [GAP-L pull;
       targeted_review_socially_scaffolded_rule_population]"
    - "Wood, Bruner & Ross (1976) J Child Psychol Psychiatry 17:89 --
       scaffolding / ZPD: an external agent controls task structure beyond
       the learner's unaided reach; the third rule-source operates in a
       band bottom-up self-extraction cannot reach. conf 0.78. [GAP-L]"
    - "Tomasello, Carpenter, Call, Behne & Moll (2005) Behav Brain Sci
       28:675 -- shared intentionality / joint attention / social
       referencing: the enabling PRECONDITION for a socially-scaffolded
       rule-source; conventions and norms are taken up via this channel,
       not invented. conf 0.74. [GAP-L]"
    - "COUNTERWEIGHT -- Spelke & Kinzler (2007) Dev Sci 10:89 -- core
       knowledge: substantial structured content (objects, agents/goals,
       number, space) is endogenous-maturational, NOT socially sourced.
       Bounds ARC-077 scope (social population acts on an
       endogenously-structured field; not the whole story) without
       refuting the peer-pathway slot. mixed, conf 0.66. [GAP-L]"
    - "COUNTERWEIGHT -- Heyes (2016) Perspect Psychol Sci 11:280 -- the
       receptive machinery may be constructed from non-specific
       social-bonding/social-learning adaptations, not an innate dedicated
       module; bounds a STRONG (innate-primitive) framing of the channel
       and reinforces the GAP-L caregiver-substrate gate. mixed, conf 0.70.
       [GAP-L]"
    # Reasoned aggregate 0.74 (conservative vs indexer parallel
    # literature_confidence=0.868 in claim_evidence.v1.json): strong,
    # textbook-grade developmental biology for the FUNCTIONAL channel,
    # discounted for (a) two principled mixed counterweights and (b)
    # structural-slot-only status with the caregiver-substrate gate still
    # open. lit_conf is a PARALLEL signal -- NOT blended into exp
    # confidence, does NOT promote past candidate.
  functional_restatement: >
    MECH-309 establishes that a parametric-policy / Bayesian agent does
    not invent the hypothesis space it updates over. ARC-062 (top-down)
    tries to make gradient pressure discover a discriminative cut;
    ARC-064 (bottom-up) tries to compress regularities out of the
    agent's own observed behaviour. ARC-077 registers the third and
    biologically dominant source: the candidate-rule space is populated
    from OUTSIDE the agent by a caregiver who structures exposure so
    that a regularity the agent could not have invented alone becomes
    apprehensible.

    Three faces of the ARC-063 field, made explicit by this
    bring-forward:
      - population ("in"): MECH-337 -- caregiver-scaffolded exposure /
        ostensive cueing writes candidate regularities into the
        CandidateRule field, tagged with the scaffolding context
        (context-bound by construction -- this is what "context-bound
        rule population" means: a candidate enters the field already
        bound to the context in which it was scaffolded).
      - select ("the cue pipeline"): MECH-338 -- environmental /
        internal cues match CandidateRule context tags to gate
        rollout-eligibility; context-dependent / cued recall
        (encoding-specificity). A rule populated in a context becomes
        available again when that context (or its cue) recurs.
      - express ("out"): hippocampal-rollout-eligibility shaping --
        already an ARC-063 commitment (rules bias which futures are
        easy to imagine). ARC-077 does NOT re-register this; it
        cross-references the existing ARC-063 mechanism.

    Why this is peer to ARC-062 / ARC-064, not a sub-mechanism of
    ARC-063: ARC-063 is the field (the representation that HOLDS
    candidate rules). The question "where do candidate rules COME
    from" is architecturally primary (it is MECH-309's whole point),
    and the cluster already partitions rule-sources by pathway
    (ARC-062 top-down, ARC-064 bottom-up, ARC-065 upstream diversity).
    Socially-scaffolded population is the third source and gets a
    peer ARC-level slot; it feeds ARC-063 rather than living inside it.

    Falsifiable framing (V4-bound; not V3-tractable until the
    substrate exists): an agent with ARC-065 diversity + the ARC-063
    field but NO socially-scaffolded population channel should fail to
    acquire context-bound rules whose discriminative cut is a function
    of agent/episode history rather than current observation
    (sharpening ARC-063 falsifiable-prediction (a)); adding a
    caregiver-scaffolding channel (MECH-337) + cue-driven retrieval
    (MECH-338) should rescue exactly that class. This is the V4
    validation gate; it presupposes a caregiver/teacher-agent
    substrate REE does not have today.
  evidence_quality_note: >
    Registered 2026-05-18 as a structural slot during the ARC-063
    bring-forward (user-directed; framing decision "you decide from
    the architecture" -> registered as a new peer pathway, not a
    sub-mechanism of ARC-063; reasoning recorded in
    rule_apprehension_layer.md and the arc_062 plan decision-log).

    NO experimental evidence. NO biology lit-pull yet -- this is the
    explicitly-approved "register slot now, flag lit-pull as prereq"
    path (the same posture under which ARC-063 itself was registered
    2026-05-08). The biology here is strong and well-known but has NOT
    been pulled into evidence/literature/ for this cluster: Vygotsky
    ZPD; Wood, Bruner & Ross 1976 (scaffolding); Csibra & Gergely 2009
    (natural pedagogy / ostensive cueing); Tomasello (shared
    intentionality, early rule acquisition); joint attention + social
    referencing; context-dependent / cued recall (Godden & Baddeley
    1975; Tulving & Thomson 1973 encoding-specificity).

    GAP-L lit-pull DISCHARGED 2026-05-18 (session
    gap-l-litpull-socially-scaffolded; 8 entries in
    evidence/literature/targeted_review_socially_scaffolded_rule_population,
    tagged ARC-077/MECH-337/MECH-338). lit_conf set to 0.74 (reasoned
    aggregate; indexer parallel literature_confidence=0.868). The lit
    gate is now satisfied; the claim REMAINS candidate -- discharging
    the lit-pull does NOT promote it (lit and exp evidence are not
    co-equal; exp_conf=0) and the SECOND HARD GATE (the
    caregiver/teacher-agent substrate, which does not exist in V3) is
    independently still OPEN. Must NOT be promoted past candidate on the
    strength of lit_conf alone.
  implementation_prerequisites: >
    HARD ORDERING GATE (none currently satisfied). ARC-077 / MECH-337
    / MECH-338 are NOT implementable or schedulable until ALL of:
      (1) GAP-L biology lit-pull discharged (caregiver-scaffolding /
          cued-recall / context-dependent rule-acquisition) -- the
          standing biology-before-formal-definitions rule;
      (2) ARC-063 CandidateRule field prototyped (the thing this
          pathway populates must exist);
      (3) a caregiver / teacher-agent substrate -- DOES NOT EXIST in
          V3 today (V3 is single-agent; scaffolded exposure needs a
          second agent or an environment that delivers ostensive
          cues). This is a required NEW substrate, not an assumed one.
    implementation_phase=v3 here means "active V3 design target under
    the V3-Pending Gate", NOT "V3-implementable now". The gate above
    is the honest blocker.
  what_would_answer: >
    SUBSTRATE-BLOCKED PRECONDITION: this claim is not yet testable, and the blocker is unchanged
    since registration (2026-05-18). Re-verified 2026-08-07: (a) the caregiver/teacher-agent
    substrate implementation_prerequisite (3) remains fully unmet -- grep of ree-v3/ree_core,
    experiments/, and coordinator/ for "caregiver" and "teacher_agent"/"teacher-agent" returns zero
    hits; every "scaffold" hit is a false positive (protective avoidance_scaffold_floor,
    SleepLoopManager Phase-A code scaffolding, InfantCurriculumScheduler -- a single-agent
    curriculum, not a second agent). V3 remains single-agent: no substrate exists that can deliver
    scaffolded exposure or ostensive cues from outside the learner. (b) Prerequisite (2), the
    ARC-063 CandidateRule field, is prototyped and v1-substrate-implemented
    (candidate_rule_field.py, 2026-06-04) for its CREATE/REPRESENT/GATE/CREDIT faces, but ARC-063's
    own record is explicit that "only the social in-face (ARC-077/MECH-337) is V4-deferred (needs a
    caregiver-agent substrate absent in V3)" -- the specific write-path this claim needs remains
    unbuilt. Prerequisite (1), the GAP-L lit-pull, is discharged (2026-05-18, lit_conf 0.74) and
    does not on its own promote the claim. Sibling routes ARC-062 (substrate_ceiling,
    v3_pending) and ARC-064 (hold_pending_v3_substrate) also remain unpromoted, consistent with the
    cluster as a whole staying pre-V3-tractable for this pathway.

    Once the caregiver/teacher-agent substrate exists (a second agent, or an environment mechanism
    that can deliver ostensive/scaffolded cues independent of the learner's own action stream) and
    MECH-337 (population) + MECH-338 (cue-driven retrieval) are built on top of it, the
    discriminating test is the claim's own functional_restatement, made explicit:

    DESIGN: hold ARC-065 (behavioural diversity) and the ARC-063 CandidateRule field substrate
    constant across two arms. ARM_0 = field + diversity, NO socially-scaffolded population channel
    (MECH-337/338 absent or disabled). ARM_1 = ARM_0 + MECH-337 (caregiver-scaffolded population) +
    MECH-338 (cue-driven, context-bound retrieval). Measure acquisition of context-bound rules
    whose discriminative cut is a function of agent/episode history rather than current
    observation -- i.e. rules that cannot be recovered from ARC-062's top-down
    context-discriminator route or ARC-064's bottom-up behaviour-clustering route alone, per this
    claim's peer-pathway framing.

    CONFIRMING: ARM_1 acquires and reliably retrieves this class of context-bound,
    history-dependent rule (rollout-eligibility and behavioural expression track the
    scaffolding-episode context tag) where ARM_0 fails or acquires it at markedly lower
    rate/fidelity -- adding the socially-scaffolded channel rescues exactly the acquisition class
    ARM_0 cannot reach, sharpening ARC-063's falsifiable prediction (a) and validating ARC-077 as a
    genuinely distinct third rule-source rather than a redundant restatement of ARC-062/ARC-064.

    FALSIFYING: ARM_1 shows no measurable acquisition or retrieval advantage over ARM_0 for this
    rule class -- the agent acquires the same context-bound rules at the same rate/fidelity with or
    without socially-scaffolded population, or ARM_0 (top-down/bottom-up routes alone) already
    reaches this class once the substrate exists. Either result means the "third source" framing is
    not doing independent work: the pathway is redundant with ARC-062/ARC-064's existing routes
    rather than a peer, and the claim should be reframed as a special case of one of them (or
    demoted) rather than carried forward as an independent architectural commitment.

    This test cannot run, and this claim cannot be promoted or demoted on experimental grounds,
    until the caregiver/teacher-agent substrate is built. Until then this claim remains a
    structural slot backed by literature only (lit_conf 0.74), not evidence.
  notes: >
    Cross-references: ARC-063 (the field; depends_on ARC-077 after the
    2026-05-18 bring-forward), MECH-309 (the diagnostic), ARC-062 /
    ARC-064 (sibling/peer rule-source routes), ARC-065 (upstream
    diversity), arc_062_rule_apprehension_plan.md GAP-L (the lit-pull
    prerequisite node) + the 2026-05-18 decision-log entry,
    rule_apprehension_layer.md (the "Socially-scaffolded rule-
    population pathway" section).
  location: docs/architecture/rule_apprehension_layer.md

- id: MECH-337
  title: "Caregiver-scaffolded rule-population mechanism (the 'in' face of the ARC-063 CandidateRule field). A caregiver / teacher agent structures the agent's exposure (Vygotsky ZPD; Wood/Bruner/Ross scaffolding) and marks regularities ostensively (Csibra & Gergely natural pedagogy); the marked regularity is written into the ARC-063 CandidateRule field as a candidate the agent could not have invented alone, tagged with the scaffolding context. Population is context-bound by construction -- this is the concrete mechanism behind 'context-bound rule population'. Child of ARC-077. Structural slot only; hard-gated on the GAP-L biology lit-pull and a caregiver-agent substrate that does not exist in V3."
  claim_type: mechanism_hypothesis
  subject: rule_apprehension.caregiver_scaffolded_rule_population
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-337@2026-05-19T21:27:25Z"
      as_of: 2026-05-19
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-05-18"
  depends_on:
    - ARC-077     # parent pathway
    - ARC-063     # the CandidateRule field this mechanism writes into
    - MECH-309    # the diagnostic motivating an external rule source
  lit_conf: 0.78
  literature_anchors:
    - "Csibra & Gergely (2009) Trends Cogn Sci 13:148 -- ostensive marking
       IS the population mechanism: a regularity is written into the field
       interpreted as generic/kind-relevant (a rule, not an episode) and
       context-bound. Most direct grounding for the 'in' face. conf 0.80.
       [GAP-L pull; targeted_review_socially_scaffolded_rule_population]"
    - "Wood, Bruner & Ross (1976) J Child Psychol Psychiatry 17:89 -- the
       six tutoring functions ('marking critical features', 'reduction in
       degrees of freedom') decompose the write operation; the ZPD bounds
       which candidates are populatable; contingency/fading is a design
       constraint on the channel. conf 0.78. [GAP-L]"
    - "Tomasello et al. (2005) Behav Brain Sci 28:675 -- joint attention /
       social referencing are the channel over which ostensive marking
       operates and why the populated candidate is inherently
       context-bound (bound to the shared-attention episode). conf 0.74.
       [GAP-L]"
    - "COUNTERWEIGHT -- Heyes (2016) Perspect Psychol Sci 11:280 -- the
       ostensive-receptivity machinery may be constructed, not innate;
       counter-evidence to a STRONG (innate-primitive) framing of MECH-337,
       NOT to the population effect (which Heyes accepts). Implies MECH-337
       must be modelled as a constructed/learned channel. mixed, conf 0.70.
       [GAP-L]"
    # Reasoned aggregate 0.78 (indexer parallel
    # literature_confidence=0.878). Highest of the cluster: the 'in' face
    # has near-verbatim mechanistic grounding (Csibra & Gergely + the WBR
    # six-function decomposition). Discounted modestly for the Heyes
    # innateness bound and structural-slot-only status. PARALLEL signal --
    # not blended into exp confidence, does NOT promote past candidate.
  functional_restatement: >
    The mechanism by which a candidate regularity enters the ARC-063
    field from outside the agent. A caregiver/teacher agent (i) selects
    exposure within the agent's zone of proximal development (not too
    easy, not unlearnable), and (ii) ostensively marks the regularity
    (gaze, pointing, contingent demonstration, infant-directed
    emphasis -- the natural-pedagogy cue set). The marked regularity is
    instantiated as an ARC-063 CandidateRule with status=latent, its
    context tags populated from the scaffolding episode (place, task,
    caregiver identity, the cue itself). Because the candidate is born
    with context tags, retrieval is later cue-addressable (MECH-338).
    Distinct from ARC-064 bottom-up extraction: the regularity is NOT
    compressed out of the agent's own behaviour history -- it is
    supplied and marked by another agent, which is why it can carry
    structure the agent's own exploration would not have generated.
  evidence_quality_note: >
    Registered 2026-05-18 as a structural slot (child of ARC-077; same
    "register slot now, lit-pull as prereq" posture). NO experimental
    evidence, NO lit-pull yet (lit_conf=0). Biology to be pulled under
    GAP-L: Csibra & Gergely 2009 natural pedagogy; Wood/Bruner/Ross
    1976 scaffolding; Vygotsky ZPD; Tomasello shared intentionality;
    infant-directed-interaction / ostensive-cue literature.

    GAP-L lit-pull DISCHARGED 2026-05-18 (lit_conf=0.78; indexer
    parallel literature_confidence=0.878; entries in
    targeted_review_socially_scaffolded_rule_population). lit gate
    satisfied; REMAINS candidate -- the lit-pull does not promote
    (exp_conf=0) and the inherited ARC-077 caregiver-substrate gate is
    independently still open. Must NOT be promoted past candidate on
    lit_conf alone.
  implementation_prerequisites: >
    Inherits the ARC-077 HARD ORDERING GATE (GAP-L lit-pull + ARC-063
    field prototyped + caregiver/teacher-agent substrate, none of which
    exists today). implementation_phase=v3 = active design target under
    the V3-Pending Gate, NOT V3-implementable now.
  notes: >
    The "in" face of the ARC-063 three-face model (in = MECH-337;
    select = MECH-338; out = ARC-063 hippocampal-rollout-eligibility,
    not re-registered here). See rule_apprehension_layer.md.
  location: docs/architecture/rule_apprehension_layer.md

- id: MECH-338
  title: "Cue-driven context-bound rule retrieval (the 'cue pipeline' / 'select' face of the ARC-063 CandidateRule field). Environmental or internal cues are matched against CandidateRule context tags to gate rollout-eligibility: a rule populated in a context becomes available again when that context, or a cue associated with it, recurs (context-dependent / cued recall; Tulving & Thomson encoding-specificity; Godden & Baddeley context-dependent memory). This is the retrieval pay-off of context-bound population (MECH-337) and the access gate that decides which CandidateRules enter hippocampal rollout. Child of ARC-077. Structural slot only; hard-gated on the GAP-L biology lit-pull."
  claim_type: mechanism_hypothesis
  subject: rule_apprehension.cue_driven_context_bound_rule_retrieval
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-338@2026-05-19T21:27:25Z"
      as_of: 2026-05-19
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-05-18"
  depends_on:
    - ARC-077     # parent pathway
    - ARC-063     # the CandidateRule field whose rollout-eligibility this gates
    - MECH-337    # consumes the context tags written at population time
    - MECH-269    # context must be discriminably represented before a cue can address it
  lit_conf: 0.75
  literature_anchors:
    - "Tulving & Thomson (1973) Psychol Rev 80:352 -- encoding
       specificity: a retrieval cue is effective iff it overlaps the
       encoded operations; the formal core of MECH-338's cue<->context-tag
       match. Robustly replicated, theory-defining. Also implies
       context-FREE population breaks cued retrieval (constraint on
       MECH-337). conf 0.82. [GAP-L pull;
       targeted_review_socially_scaffolded_rule_population]"
    - "Nakazawa et al. (2002) Science 297:211 -- CA3-NR1 knockout
       selectively impairs recall from an INCOMPLETE cue set: the causal
       neural substrate for pattern completion = cue-driven retrieval of a
       context-tagged candidate via attractor dynamics; motivates the
       MECH-269 dependence. conf 0.78. [GAP-L]"
    - "ROBUSTNESS CONTRA -- Godden & Baddeley (1975) Br J Psychol 66:325 --
       canonical environmental context-dependent recall (land/underwater),
       BUT a 2021 pre-registered powered replication (R Soc Open Sci
       8:200724) FAILED to reproduce it. Concrete instantiation only;
       MECH-338 rests on Tulving & Thomson + Nakazawa, not on this fragile
       effect. supports-but-fragile, conf 0.58. [GAP-L]"
    # Reasoned aggregate 0.75 (indexer parallel
    # literature_confidence=0.813). Carried by Tulving & Thomson (robust
    # principle) + Nakazawa (substrate); deliberately tempered because the
    # specific environmental-context phenomenon (Godden & Baddeley) did
    # not survive a powered replication. PARALLEL signal -- not blended
    # into exp confidence, does NOT promote past candidate.
  functional_restatement: >
    The retrieval / access mechanism. CandidateRules carry context
    tags (written at population time by MECH-337, or by any other
    source). A cue -- an environmental feature, an interoceptive
    state, a caregiver signal, or a reinstated context -- is matched
    against those tags; matching CandidateRules are promoted toward
    rollout_eligible (the ARC-063 Tolerance-Principle availability gate
    still decides final eligibility; MECH-338 supplies the
    cue-addressed CANDIDATE SET it operates over). This is the
    mechanism that makes context-bound population pay off: a rule
    scaffolded in context C is dormant until C (or a learned cue for
    C) recurs, then becomes rollout-eligible. It also sharpens
    ARC-063 falsifiable-prediction (a): the discriminative cut can be
    a function of episode/agent history because the cue, not just the
    current observation, addresses the rule.
  evidence_quality_note: >
    Registered 2026-05-18 as a structural slot (child of ARC-077).
    NO experimental evidence, NO lit-pull yet (lit_conf=0). Biology
    under GAP-L: Tulving & Thomson 1973 encoding-specificity; Godden
    & Baddeley 1975 context-dependent memory; reinstatement /
    cue-dependent retrieval literature; hippocampal pattern-completion
    as the biological substrate.

    GAP-L lit-pull DISCHARGED 2026-05-18 (lit_conf=0.75; indexer
    parallel literature_confidence=0.813; entries in
    targeted_review_socially_scaffolded_rule_population). Note: the
    specific environmental context-dependent-recall effect (Godden &
    Baddeley) failed a 2021 powered pre-registered replication, so
    MECH-338's grounding rests on Tulving & Thomson (encoding
    specificity) + Nakazawa 2002 (CA3 pattern completion), not on the
    environmental phenomenon. lit gate satisfied; REMAINS candidate
    (exp_conf=0; inherited ARC-077 caregiver-substrate gate still
    open). Must NOT be promoted past candidate on lit_conf alone.
  implementation_prerequisites: >
    Inherits the ARC-077 HARD ORDERING GATE. implementation_phase=v3 =
    active design target under the V3-Pending Gate, NOT
    V3-implementable now.
  notes: >
    The "select" face of the ARC-063 three-face model. Feeds the
    existing ARC-063 Tolerance-Principle availability gate; does not
    replace it. See rule_apprehension_layer.md.
  location: docs/architecture/rule_apprehension_layer.md

- id: ARC-065
  title: "behavioral_diversity_generation_pathway (multi-substrate distributed: LC-NE tonic + frontopolar curiosity + striatal novelty + hippocampal trajectory sampling). Architectural commitment: a non-trivial behavioural diversity generation pathway is upstream-of and logically prior to both rule pathways (ARC-062 top-down rule-apprehension and ARC-064 bottom-up rule-discovery). Without it, neither pathway has anything to weight or cluster -- gradient descent on a parametric policy without diversity pressure collapses to the smoothest single regime, the predicted equilibrium MECH-309 identifies. The biology distributes diversity generation across at least four substrates: LC-NE tonic noise floor, frontopolar uncertainty-driven curiosity, striatal novelty bonus, and hippocampal trajectory sampling. REE registers this as a foundational architectural slot whose specific child mechanisms (MECH-313 noise-floor, MECH-314 structured-curiosity with three sub-flavours, and the MECH-315-absorbed proposal-diversity channel) are independently testable but jointly load-bearing."
  claim_type: architectural_commitment
  subject: rule_apprehension.behavioral_diversity_generation_pathway
  polarity: asserts
  status: stable
  live_status:
    reading: stable
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-569h_2026-06-16
      as_of: 2026-06-16
      verdict: non_contributory/substrate_ceiling
  implementation_phase: v3
  v3_pending: false
  epistemic_category: standard
  claim_level: architectural
  registered_utc: "2026-05-10"
  depends_on: []
  functional_restatement: >
    ARC-065 registers the architectural slot for the behavioural diversity
    generation pathway that both rule pathways (ARC-062 top-down rule-
    apprehension, ARC-064 bottom-up rule-discovery) presuppose. The claim
    is that diversity generation is a substantive architectural function,
    not an emergent property of softmax sampling or training noise; without
    a diversity-generation pathway, the rule machinery has nothing to
    weight or cluster.

    Multi-substrate distributed instantiation, biology-anchored. Pull 1
    SYNTHESIS verdict 2 identified at least four converging substrates,
    none of which is alone sufficient:
      (a) LC-NE tonic noise floor: locus coeruleus tonic firing produces
          baseline action-selection variability (Aston-Jones & Cohen 2005
          adaptive-gain model). Computational analog: max-entropy policy
          regularisation, SAC entropy bonus (Haarnoja et al. 2018).
          REE child claim: MECH-313 stochastic_noise_floor.
      (b) Frontopolar uncertainty-driven curiosity: rostrolateral
          prefrontal exploration bonus (Daw et al. 2006 Nature; Wilson
          et al. 2014; Kidd & Hayden 2015). Three sub-flavours that the
          lit recommends NOT collapsing prematurely:
            - Striatal novelty bonus (Wittmann et al. 2008 Neuron) ->
              MECH-314a
            - Frontopolar uncertainty-driven curiosity (Daw 2006) ->
              MECH-314b
            - Learning-progress / intrinsic-motivation curiosity
              (Schmidhuber tradition; Pathak et al. 2017) -> MECH-314c
          REE parent: MECH-314 structured_curiosity_bonus.
      (c) Hippocampal trajectory sampling: Pfeiffer & Foster 2013 forward
          path sweeps generate diverse rollouts independent of policy
          variability. REE absorption: per Pull 2 R5 verdict, the
          MECH-315 candidate (proposal-diversity channel via hippocampal
          trajectory sampling) is ABSORBED into the existing MECH-292 +
          MECH-293 ghost-goal substrate, NOT registered as a separate
          child claim of ARC-065.
      (d) Active-inference epistemic value (Friston 2010, 2015):
          information-gain reward as a structural source of behavioural
          diversity. Cross-references MECH-314b but operates at a
          different formal level (free-energy decomposition vs ML
          intrinsic-motivation reward shaping).

    Architectural commitment claim. Both ARC-062 and ARC-064 require
    diversity in the policy-output stream (ARC-062 needs it for the
    context discriminator to find a discriminative cut; ARC-064 needs
    it so cross-episode regularity extraction has heterogeneous
    behaviour to cluster across). MECH-309's logical-necessity argument
    cuts both ways: trainers do not invent rules, and trainers do not
    invent diversity either. The diversity-generation pathway is a
    second logically-necessary architectural slot; ARC-065 names it.

    Falsifiable architectural prediction: complete ablation of the
    diversity-generation pathway (LC-tonic + curiosity + striatal-
    novelty all OFF) should produce monomodal collapse on SD-054 even
    with ARC-062 gated-policy heads + learned context discriminator
    enabled, because the discriminator has no diverse behavioural
    stream to find a cut in. Partial PASS (any single substrate ON)
    distinguishes load-bearing from contributory among the four.

    What ARC-065 is NOT. ARC-065 does not assert that all four
    substrates are necessary -- only that diversity generation as a
    function is necessary, and that biology distributes it. The
    relative-weight-calibration question (Q-043) and the substrate-
    collapse question (Q-044, Q-045) are open and falsifiable in V3-
    EXQ-543b/c.
  pending_retest_after_substrate: false
  evidence_quality_note: >
    [2026-06-17 governance: substrate_ceiling LIFTED. V3-EXQ-569i PASS
    (non-degenerate; supersedes V3-EXQ-569h) -- the GAP-A top-k shortlist
    conversion architecture change converts the upstream per-candidate
    diversity RANGE into committed-action diversity: selected-action entropy
    C_R1B strict-above BOTH matched-noise AND the proposer, preconditions
    (arm1 route-range + e2_world_forward prediction spread supra-floor) met,
    criteria_non_degenerate {C1:true, C_R1B:true}. This closes the
    E3-commit-readout washout flagged 2026-06-10 (cross-candidate range
    present but not ROUTED into the modulatory bias): the top-k shortlist now
    routes it. epistemic_category substrate_ceiling -> standard;
    pending_retest_after_substrate cleared; evidence_direction supports
    (exp_conf 0.974, 0 weakens). ARC-065 is architectural_commitment so no
    auto-promote mechanics; status stays provisional. The held
    claim_synthesis_ARC-065_2026-06-16 proposal is closed unregistered
    (569i PASS => the proposed finer committed-diversity-GAIN child is
    correctly NOT needed; conversion converts).]

    Registered 2026-05-10 from Pull 1 lit-pull SYNTHESIS verdict 3
    (PROMOTE-TO-CLUSTER, conf 0.82) and Pull 4 R4 HYBRID-naming
    convention. Lit-pull anchors:
      - Wilson et al. 2014 (Cereb Cortex) -- frontopolar exploration
        bonus framework
      - Aston-Jones & Cohen 2005 (Annu Rev Neurosci) -- LC-NE adaptive
        gain
      - Daw et al. 2006 (Nature) -- frontopolar uncertainty-driven
        exploration
      - Wittmann et al. 2008 (Neuron) -- striatal novelty bonus
      - Haarnoja et al. 2018 (ICML) -- SAC max-entropy policy
        regularisation as the computational analog of LC-NE noise
      - Friston 2010 / 2015 -- active inference expected free energy
      - Schmidhuber 1991 / Pathak et al. 2017 -- intrinsic motivation
        / curiosity-driven learning
      - Kidd & Hayden 2015 (Neuron) -- review of curiosity neural
        substrates
    See: evidence/literature/targeted_review_arc_065_behavioral_diversity_generation/synthesis.md

    [2026-05-29 autopsy V3-EXQ-605 + V3-EXQ-604 cluster]: foundational architectural slot; neither child substrate (MECH-313 noise / MECH-314 curiosity) can be load-bearing-tested while V3 substrate produces candidate-degenerate scoring AND seeds 42/44 die on the target env at random-init. ARC-065 stays candidate pending the two-substrate-prereq close (SD-056 via V3-EXQ-569a + SD-XXX-scaffolded-sd054-onboarding via implement-substrate session). Priority-1 calibration debt per project_calibration_debt.md.

    [2026-05-30 governance / autopsy V3-EXQ-569c applied]: V3-EXQ-569c (2026-05-30) tested SD-056 against ARC-065 GAP-A R1.b as a matched-entropy FP-2 falsifier. Headline result: ON arms (sd056_weight 0.01/0.05/0.20) produce selected_action_entropy 0.875/0.833/0.951 vs matched-noise control (ARM_4 T=2.5) at 0.414 -- ~2.4x above the FP-2 control on the C3 acceptance axis. Pre-registered C1 floor (cand_world_pairwise_dist > 0.05) was not cleared by 2/3-seed majority (ON-arm means 0.045/0.042/0.046; per-seed seeds-above-floor 1/0/1). The C1 floor was calibrated against V3-EXQ-613 standalone-task magnitude (0.286) rather than behavioural-runtime magnitude (~0.045); ON arms cluster at 3x bit-identical noise (0.0148), confirming the substrate is operative. Per /failure-autopsy V3-EXQ-569c 2026-05-30: routed to /queue-experiment for V3-EXQ-569d (recalibrated C1 floor + e3_top2_class_gap NaN fix) and V3-EXQ-569e (mechanism probe dissociating per-candidate-variance propagation vs E2-rollout-dynamics shift as source of the C3 lift). Evidence direction for ARC-065 upgraded supports per the matched-noise control clearance on C3; MECH-341 supports preserved. ARC-065 stays candidate / v3_pending=true / pending_retest_after_substrate=true pending V3-EXQ-569d+569e returns.

    [2026-06-10 governance / confirmed failure_autopsy_569f-661-654a_2026-06-10 applied]: V3-EXQ-569f (the matched-entropy FP-2 falsifier) reclassified weakens -> non_contributory on the manifest. Readiness fully met (consumed world-summary spread 0.196 > floor) and C1 (e2.world_forward divergent) PASSED -- the per-candidate diversity ARC-065 asserts is demonstrably present in the upstream representation. The discrimination criterion failed only because selected-action entropy is bit-identical (0.549141) across the e2wf, proposer, AND matched-noise arms -- a readout washout at the E3 committed-action layer, not a fair falsification of ARC-065. This is the E3-commit-readout substrate ceiling shared with V3-EXQ-661 (MECH-294) and V3-EXQ-654a (ARC-062/MECH-309): upstream-channel cross-candidate RANGE is present but is not ROUTED into the per-candidate modulatory bias the selection authority rescales (extends V3-EXQ-643 one link: range present now, still not routed). epistemic_category set substrate_ceiling + pending_retest_after_substrate=true. Pending retest after the modulatory-bias-selection-authority substrate_queue amend (route upstream-channel range into the modulatory bias). NO demotion (the bit-identity is a readout washout, not a genuine negative).

    Pull 4 R4 verdict assigned ARC-065 to HYBRID-naming convention
    because the REE-internal name (behavioral_diversity_generation_pathway)
    captures the architectural-function level while the literature anchors
    (MaxEnt + intrinsic-motivation + LC-NE adaptive gain) capture the
    substrate-mechanism level; neither alone is sufficient.

    Not yet experimentally tested. Falsifying experiment is V3-EXQ-543b
    (5-arm protocol with ARC-062 gated policy ON across all arms; arms
    differ in which diversity-generation substrate is enabled) or split
    into V3-EXQ-543b (noise-floor + gating) + V3-EXQ-543c (curiosity +
    meta-RL recurrent baselines) per Pull 4 R5 sequencing recommendation.

    No standalone biology lit-pull on diversity generation as a unified
    pathway; the four sub-mechanisms have independent literatures and
    Pull 1 surveys their convergence onto the architectural-commitment
    claim. A standalone pull on the diversity-generation function as
    such (rather than its sub-substrates) would deepen the registration
    but is not a precondition for V3-EXQ-543b/c.

    [2026-05-31 governance: three converging PASSes clear both
    v3_pending and pending_retest_after_substrate gates]:
    (i) V3-EXQ-614a PASS (2026-05-30T19:32Z, supersedes V3-EXQ-614)
        Phase-P3 behavioural falsifier 3-arm, label
        PASS_C2_C3_only_mech341_load_bearing_in_stack_only per
        behavioral_diversity_isolation_plan R2.c rule;
        evidence_direction_per_claim[ARC-065]=supports.
    (ii) V3-EXQ-569d PASS (2026-05-31T05:36Z, supersedes V3-EXQ-569c)
        SD-056 matched-entropy FP-2 falsifier with C1 floor
        recalibrated 0.05->0.03 per autopsy V3-EXQ-569c Section 5;
        all three C-criteria PASS;
        evidence_direction_per_claim[ARC-065]=supports.
        V3-EXQ-569c manifest set evidence_direction=superseded per
        supersession policy.
    (iii) V3-EXQ-615 PASS (2026-05-31T09:31Z, real run; the 08:22Z
        dry_run=True FAIL preceded it and is non-evidential)
        Rung-1 matched-entropy on rescued substrate (30/60 ep, 200
        steps); C1+C2+C3 all PASS; entropy_delta_arm2_vs_arm1=1.11;
        script-routing: "ARC-065 supports, clear pending_retest_after_substrate".
    Combined: ARC-065 v3_pending false, pending_retest_after_substrate
    false. 569e mechanism-dissociation diagnostic (Pathway A vs B) is
    flagged for /failure-autopsy and does not weight against ARC-065 in
    this cycle. ARC-065 remains status=candidate; the next governance
    cycle is where the indexer reconsiders the promotion gate now that
    the V3-pending gate is lifted.

    [2026-05-31 midday governance, V3-EXQ-569e autopsy applied]:
    V3-EXQ-569e Pathway A vs Pathway B mechanism probe verdict
    INSTRUMENTATION_FAILURE per failure_autopsy_V3-EXQ-569e_2026-05-31.json.
    SD-056 contrastive E2 training produced numerically explosive E2 rollouts
    (1e16+) on most ON-arm seeds at the behavioural-runtime episode length;
    verdict cells M1/M3/M4/M5 all saturated. Probe could not dissociate
    Pathway A (per-candidate z_world variance propagation) from Pathway B
    (E2 rollout dynamics shift). Substrate implementation pathology (lacks
    multi-step rollout stability machinery), not architectural claim
    falsification. evidence_direction_per_claim[ARC-065]=mixed (diagnostic,
    non-weighting per Phase-3 governance rules; ARC-065 reading from
    V3-EXQ-569c ~2.4x C3 lift over matched-noise control remains
    load-bearing pending the SD-056 amend-and-re-run cycle). Routed to
    /implement-substrate amend SD-056 (multistep contrastive horizon +
    output norm clamp landed 2026-05-31 11:25Z). 569e adds no new ARC-065
    evidence weighting in either direction this cycle.

    [2026-05-31 evening governance: PROMOTED candidate -> provisional]:
    Per failure_autopsy_MECH-341-cluster_2026-05-31 (status=confirmed).
    Cluster autopsy on V3-EXQ-614b + V3-EXQ-615 + V3-EXQ-616 confirms
    that ARC-065's distributed-pathway commitment is empirically supported
    by V3-EXQ-615 09:31Z PASS (matched-entropy control discrimination:
    ARM_2 ALL_ON 4.33 unique classes / 1.111 nats; ARM_0 BASE_OFF and
    ARM_1 MATCHED_NOISE both collapse to single-class 0.0 nats; matched-
    noise control cannot reproduce ALL_ON diversity). V3-EXQ-614b 0.800
    nats ALL_ON entropy on SD-056-amended substrate is the highest of
    any 614-lineage run, corroborating the architectural-necessity reading.
    Direction counts post-walk reclassification: 46 supports / 5 mixed /
    0 weakens (the prior 1 weakens from 614b reclassified non_contributory
    this cycle; the prior weakens from EXQ-615 08:22Z dry-run dual-manifest
    artifact resolved to superseded earlier in the day). conflict_ratio
    drops near zero; exp_conf rises to ~0.97 (post-rebuild value applies).
    Promotion candidate -> provisional applied; v3_pending already false;
    pending_retest_after_substrate already false (gates cleared at midday).
    Promotion to full active reserved for a subsequent walk after a
    behavioural-runtime test on the larger SD-054 / multi-resource
    substrate cohort (V3-EXQ-619 and downstream).

    [2026-06-12 governance / confirmed failure_autopsy_batch9_2026-06-12 applied]:
    V3-EXQ-569g (route-range matched-entropy falsifier) reclassified weakens ->
    non_contributory. On the now-ready (643a-validated) modulatory-bias-selection-
    authority substrate the route-range channel reached the E3 accumulator
    (route_range 0.18, C1 e2.world_forward divergent PASS 3/3) but ARM_1_E2WF
    selected-action entropy 0.615 did NOT exceed the matched-noise control 0.704
    (1/3 seeds strict-above) -- range reaches selection but carves no extra
    committed-action diversity vs noise. NOT a demotion (provisional, 46 supports,
    exp_conf ~0.97). The substrate excuse is now spent (ready=true) and this is the
    5th+ ARC-065 failure with a distinct signature each time -> routed to
    /claim-synthesis as granularity debt: the coarse 'diversity is load-bearing'
    claim under-names the range-reaches != range-carves distinction. No confidence
    change this cycle (the specific GAP-A R1.b sub-hypothesis is the synthesis input).
  notes: >
    HYBRID-naming convention (Pull 4 R4): the title carries both the REE-
    internal architectural name (behavioral_diversity_generation_pathway)
    and the literature anchors (LC-NE tonic + frontopolar curiosity +
    striatal novelty + hippocampal trajectory sampling). This makes the
    cluster machine-grep-able under both REE-native search ("behavioral
    diversity") and literature-search ("frontopolar curiosity",
    "MaxEnt RL", "LC-NE adaptive gain").

    Cross-reference: ARC-065 is a precondition for ARC-062 in the same
    way that MECH-269 V_s representational discriminability is a
    precondition for ARC-062. The chain is: behavioural diversity
    generation -> representational discriminability -> rule apprehension.
    All three are upstream of SD-029 monomodal-collapse measurement.
  implementation_note: >
    2026-05-17: the hippocampal-trajectory-sampling child substrate
    (support-preserving + stratified CEM, "SP-CEM") was LANDED AS THE
    MAIN-PATH DEFAULT in ree-v3. Config defaults flipped (HippocampalConfig
    dataclass + REEConfig.from_dims signature, both layers):
    use_support_preserving_cem False->True,
    support_preserving_stratified_elites False->True,
    support_preserving_ao_std_floor 0.0->0.2. The legacy collapsing CEM
    (monostrategy) is no longer the default; bit-identical opt-out is
    explicitly passing those three flags False/False/0.0. Evidence basis:
    V3-EXQ-567 PASS 2026-05-15 (selected_action_entropy 0.0124->0.4965,
    candidate support 1.007->2.810), corroborated by V3-EXQ-573/568 and
    confirmed confound-free by the V3-EXQ-543e failure autopsy 2026-05-17.
    This is a SUBSTRATE landing only -- NOT a promotion. ARC-065 stays
    status=candidate, v3_pending=true: provisional promotion still requires
    Rung-1 matched-entropy governance (V3-EXQ-569: structured diversity >
    entropy-matched noise) plus independent multi-env replication, per
    evidence/planning/behavioral_diversity_acceptance_criteria.md. Default-
    wiring validation queued (3-arm equivalence). See ree-v3/CLAUDE.md
    "ARC-065 SP-CEM Main-Path Landing (2026-05-17)".
  location: docs/architecture/rule_apprehension_layer.md

- id: MECH-313
  title: "stochastic_noise_floor (max_entropy_policy_regularisation_LC_NE_tonic_analog). A floor-level stochastic action-selection noise that prevents complete deterministic collapse of the policy. Biological substrate: locus coeruleus tonic firing producing baseline action-selection variability (Aston-Jones & Cohen 2005 adaptive-gain model). Computational analog: max-entropy policy regularisation / SAC entropy bonus (Haarnoja et al. 2018). Distinct from MECH-260 dACC anti-recency: MECH-313 is a state-independent noise floor, while MECH-260 is a state-dependent recency penalty. Both can contribute to anti-monostrategy pressure but operate via different mechanisms; whether they collapse into a single substrate or are mutually load-bearing is open (Q-045)."
  claim_type: mechanism_hypothesis
  subject: policy.stochastic_noise_floor_lc_ne_tonic_analog
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-687_2026-06-18
      as_of: 2026-06-18
      verdict: non_contributory/substrate_ceiling
  status_note: >
    [enum-comment migrated to field 2026-07-18] status: GOV-CEIL-1 demotion 2026-07-09: floored
    from candidate_substrate_landed
  implementation_phase: v3
  v3_pending: true
  pending_retest_after_redesign: true
  claim_level: mechanistic
  registered_utc: "2026-05-10"
  depends_on:
    - ARC-065     # parent architectural commitment (behavioural diversity generation)
    - MECH-260    # dACC anti-recency penalty (related but distinct mechanism; Q-045 falsifies whether they collapse)
  functional_restatement: >
    MECH-313 names a floor-level stochastic action-selection noise as
    the substrate for the LC-NE tonic component of ARC-065's behavioural
    diversity generation pathway. The biology: tonic locus coeruleus
    firing gates a baseline norepinephrine-driven action variability
    that does not depend on uncertainty estimation or curiosity reward
    (Aston-Jones & Cohen 2005). The computational analog is max-entropy
    policy regularisation -- SAC's entropy bonus (Haarnoja et al. 2018)
    is the cleanest implementation pattern.

    Distinct from MECH-260 dACC anti-recency. MECH-260 penalises
    recently-selected actions as a function of recency state; MECH-313
    is a state-independent stochasticity floor. MECH-260 alone can
    produce anti-monostrategy pressure on short timescales by penalising
    the just-selected action; MECH-313 produces persistent low-amplitude
    variability across all timescales and all action choices. The two
    mechanisms are mathematically distinct (MECH-260 is a Boltzmann-
    style temperature on the recency-penalised action; MECH-313 is a
    target entropy in the policy distribution).

    Falsifiable: in V3-EXQ-543b 4-arm sub-design,
      - both-OFF: MECH-260 OFF + MECH-313 OFF
      - 313-only: MECH-313 ON, MECH-260 OFF
      - 260-only: MECH-260 ON, MECH-313 OFF
      - both-ON: MECH-260 ON + MECH-313 ON
    On SD-054 reef substrate with ARC-062 gated-policy enabled, behaviour
    diversity (action-distribution entropy at probe states; reef/forage
    occupancy CoV across seeds) should rank both-ON > {313-only, 260-only}
    > both-OFF if the two mechanisms are mutually load-bearing. If
    313-only matches both-ON, MECH-260 is contributory-not-necessary; if
    260-only matches both-ON, MECH-313 is contributory-not-necessary.
    Both-OFF should produce monomodal collapse irrespective of ARC-062.
  evidence_quality_note: >
    GOV-CEIL-1 CEILING-EXHAUSTION DEMOTION 2026-07-09: 7 confirmed substrate_ceiling failure_autopsy
    hits (573/603c/603d/603e/610f/655/687) with no positive discrimination on any richer substrate ->
    demoted to contested candidate (epistemic_category standard, ceiling_decision exhausted, status
    floored candidate_substrate_landed -> candidate). The competing NULL reading -- the LC-NE noise
    floor is inert / does no work (only sub-threshold N=1 directional signals across three substrates)
    -- is now carried CO-EQUALLY. NOT falsification: the claim keeps its full history; promotion re-gated
    on a future positive discrimination on a richer substrate.

    Registered 2026-05-10 from Pull 1 SYNTHESIS R3 + Pull 4 R4 HYBRID-naming.
    Lit anchors:
      - Aston-Jones & Cohen 2005 (Annu Rev Neurosci) -- LC-NE adaptive
        gain (tonic vs phasic firing modes)
      - Haarnoja et al. 2018 (ICML) -- SAC; entropy-regularised RL
      - Faisal et al. 2008 (Nat Rev Neurosci) -- noise in the nervous
        system as a substrate-level constraint
    See: evidence/literature/targeted_review_arc_065_behavioral_diversity_generation/synthesis.md

    [2026-05-29 autopsy V3-EXQ-603a/b/c followon + V3-EXQ-605 cluster]: Cumulative cross-substrate sub-threshold directional signal. 603a (ARM_1 +0.015 vs ARM_0, N=1), 603b (ARM_1 +0.011, ARM_2 +0.041, ARM_3 +0.045, N=1), 605 noise alpha sweep ({0.1, 0.5, 1.0} -> entropy {0.259, 0.292, 0.305}, N=1 per cell). Three independent substrate designs now deliver same-direction MECH-313 signal. 603c surprise: post-phased-training ARM_1 == ARM_0 exactly -- directionally consistent with LC-NE-against-prior biology but anecdotal at N=1. Re-evaluate once substrate-fixed retests (SD-XXX-scaffolded-sd054-onboarding land) deliver N>=2 per cell.

    Existing MECH-260 dACC anti-recency in claims.yaml is the nearest
    pre-registered REE substrate. Q-045 captures the empirical question
    of whether MECH-313 and MECH-260 are independently load-bearing or
    collapse into one anti-monostrategy substrate.

    Not yet experimentally tested. Falsifying experiment is V3-EXQ-543b
    4-arm sub-design or its split V3-EXQ-543b (noise floor / gating)
    arm-class.

    [2026-05-24 autopsy V3-EXQ-603]: MEASUREMENT GAP -- real +0.048 entropy lift
    above ARM_0 (below 0.05 margin), directionally consistent with the noise-floor
    hypothesis but sub-threshold. Confounded by MECH-260 being non-operative (FIFO
    empty; act_with_split_obs() call-path). 'supports' in manifest overstates.
    V3-EXQ-603 overridden to non_contributory. [2026-05-25] V3-EXQ-603a: seed 43
    (only operative seed) shows ARM_1 (MECH-313) +0.015 above ARM_0 -- directionally
    consistent, sub-threshold. 603a also overridden to non_contributory (2/3 seeds
    measured_steps=0; temporal gate failure). Pending: V3-EXQ-603b with episode
    length fixed.

    [2026-05-26 governance applies V3-EXQ-603b post-Fix-B autopsy (2026-05-25) +
    earlier 603 run 111759Z]: Third measurement_gap in 603 chain. NoiseFloor
    substrate fires; seed 43 only: ARM_1 - ARM_0 = +0.011 entropy lift,
    directionally correct but well below 0.05 ENTROPY_MARGIN. Fix B
    (hazard_harm 0.02) insufficient -- proximity_harm_scale=0.1 dominant kill
    channel; 2/3 seeds (42, 44) measured_steps=0. Effective N=1. Both 603
    (111759Z) and 603b (071458Z) overridden to non_contributory +
    epistemic_category=measurement_gap. Pending retest: V3-EXQ-603c with P0/P1
    training phase added.

    [2026-05-27 governance applies V3-EXQ-603c (P0/P1 phased training,
    supersedes 603b)]: NON-CONTRIBUTORY. Halt condition fired:
    p2_cell_count=4 / aborted_cells=8 (8 of 12 cells aborted Fix D pre-P2
    survival gate at end of P1). Even with P0+P1 phased training, the
    trained policy does not survive SD-054 enrichment long enough in 2/3
    cell arms to sustain measurement. Q-045 routed to epistemic_category=
    substrate_ceiling; MECH-313 inherits substrate_ceiling via Q-045
    dependency. Cluster-absorbed into failure_autopsy_V3-EXQ-591_2026-05-27
    substrate-uniform z_goal-zero family. NoiseFloor substrate-readiness
    intact; behavioural-validation pending V3 substrate enrichment +
    training-regime fix (the goal-pipeline substrate gap the 591 autopsy
    cluster names). [2026-05-27 correction: prior cycle had routed to
    substrate_conditional pending V4 -- overridden; the test bed
    (SD-054 + ARC-062 + NoiseFloor) is V3-scoped by design; required
    fix is V3 substrate enrichment, not V4 deferral.]

    [2026-06-01T05:50Z governance cycle, V3-EXQ-621a substrate-readiness PASS]:
    Scaffolded SD-054 onboarding substrate (landed 2026-05-31 IGW-20260531-029)
    cleared substrate-readiness diagnostic on V3-EXQ-621a (PASS via C1 6/12
    cells + C3 cascade_consequential=true via ARM_2 approach_commit lift;
    z_goal C2 floor still unmet, motivating staged V3-EXQ-622 decomposition).
    Reclassified non_contributory per substrate-readiness diagnostic pattern
    (matches MECH-295 / SD-049 precedents): substrate-readiness PASS does not
    by itself constitute load-bearing behavioural evidence. Behavioural cluster
    validation V3-EXQ-603d / 591b queueable. pending_retest_after_substrate
    flag retained until behavioural successor returns. Predecessor V3-EXQ-621
    (recovered manifest from runner emit_outcome misclassification) marked
    superseded by V3-EXQ-621a per CLAUDE.md supersession rule.

    Correction (failure_autopsy_V3-EXQ-621a_2026-06-06): V3-EXQ-621a
    overall_pass=true was a VACUOUS_PASS -- the load-bearing C2 z_goal floor
    was FALSE (z_goal=0 across all 12 cells) and z_goal=0 was guaranteed by
    construction (the 2026-05-31 scheduler never called update_z_goal AND the
    agent was built without z_goal_enabled). The PASS rested on a
    baseline-inflated C1 (real scaffolded survival 3/9) and a vacuous C3
    (approach-commit lift attributed to a goal cascade that cannot exist at
    z_goal=0). 621a did NOT actually clear substrate-readiness or GAP-C prereq
    (2); that gate remains the pending full-scale run (ready=false). The
    V3-EXQ-621 supersession stands (runner-emit_outcome-misclassification
    recovery; independent of the vacuity). The fault was confirmed and
    re-opened by failure_autopsy_V3-EXQ-603d_2026-06-01 (Class-1
    harness/wiring); no claim confidence moved (run non_contributory /
    scoring_excluded).

    [2026-06-01T17:56Z governance cycle, confirmed failure_autopsy_V3-EXQ-603d]:
    First behavioural retest on the IMPLEMENTED scaffolded_sd054_onboarding
    substrate. FAIL non_contributory. LOAD-BEARING finding:
    ScaffoldedSD054OnboardingScheduler._train_episode and 603d's bespoke P2
    never call agent.update_z_goal, so z_goal stays zero-init for every step of
    every arm; the C4 z_goal=0 / SUBSTRATE_FAILURE label is a Class-1
    harness/wiring artifact (same fault class as V3-EXQ-626), NOT
    substrate-ceiling falsification. This is the true content of 621a's "z_goal
    feeding not yet wired" + the 591-triage prereq (4). P1 survival gate failed
    2/3 seeds (42,44; only 43 to P2; p2_cell_count 5/15, effective N=1),
    reproducing the 603-chain seed-fragility but confounded by reduced budget
    (P0/P1=30/30 vs 603c 100/50) + the inert goal pipeline. Routing:
    implement-substrate AMEND on scaffolded_sd054_onboarding (wire update_z_goal
    + Stage-0 positive-control gate; strictly UPSTREAM of the 622 anneal-rate
    amend), then re-issue 603d -> 603e at restored budget. substrate_queue
    amended (action=amend, priority 1, 603d failure_record appended).
    pending_retest_after_substrate retained. The cumulative sub-threshold
    MECH-313 directional signal from the 603 chain is unchanged -- 603d adds no
    new MECH-313 read (N=1, z_goal=0).

    [2026-06-02 implement-substrate AMEND landed]: scaffolded_sd054_onboarding
    update_z_goal wiring fixed (P1 _train_episode + P2 _eval_episode) + Stage-0
    positive control added; 625b failure record folded in. Validation pending
    V3-EXQ-603e (restored budget + z_goal_enabled=True required -- 603d config
    omitted it). Status unchanged (substrate_ceiling / pending_retest_after_substrate);
    substrate_queue amend_pending -> amend_implemented_pending_validation. See Q-045.

    [2026-06-03 governance / cluster autopsy failure_autopsy_V3-EXQ-603e-626a-622_2026-06-03] V3-EXQ-603e re-tagged non_contributory: goal-pipeline wiring closed+verified (update_z_goal AMEND deb24cc; contract test_c6_stage0_positive_control confirms z_goal forms under forced input) but z_goal_norm_peak=0.0 on ALL 15 cells ecologically at restored budget -- downstream of TWO coupled prerequisites: survival/foraging-competence (2/3 seeds never competent) + benefit-input starvation (P2 hazard_food_attraction=0.7). Discovered developmental prerequisite (goal reps require reward-contact history; Berridge), NOT a falsification: claim never under fair test (effective N=1). stays substrate_ceiling/pending_retest_after_substrate; substrate_queue AMEND 'scaffolded_sd054_onboarding' then re-issue 603e->603f. EXQ-445h remains sole valid MECH-260 support.

    [enum-comment migrated to field 2026-07-18] epistemic_category: GOV-CEIL-1 demotion
    2026-07-09 (was substrate_ceiling)

  epistemic_category: standard
  ceiling_decision: exhausted
  ceiling_routing_note: "GOV-CEIL-1 ceiling-exhaustion demotion 2026-07-09: 7 confirmed substrate_ceiling failure_autopsy hits (573/603c/603d/603e/610f/655/687) with no positive discrimination on any richer substrate. Demoted to contested candidate; the inert/no-op-mechanism null reading (LC-NE noise floor delivers only sub-threshold N=1 directional signals) is carried co-equally. Promotion re-gated on a future positive discrimination on a richer substrate."
  pending_retest_after_substrate: true
  notes: >
    Pull 4 R4 verdict assigned MECH-313 to HYBRID-naming because the
    REE-internal stochastic_noise_floor name and the literature anchor
    max_entropy_policy_regularisation are co-equally informative; the
    LC-NE tonic analog adds the biology-side anchor.

    Substrate landed 2026-05-10 (status candidate -> candidate_substrate_landed;
    v3_pending retained pending Q-045 behavioural validation):
    ree-v3/ree_core/policy/noise_floor.py (NoiseFloor + NoiseFloorConfig).
    Pure-arithmetic regulator (no learned parameters; no nn.Module
    inheritance). Algorithm at the e3.select() call site in
    REEAgent.select_action():
      effective_T = max(baseline_T + alpha, min_temperature)
    where alpha is the SAC-entropy-bonus analog (additive lift) and
    min_temperature is a hard floor preventing argmax collapse under
    annealing schedules. Config: REEConfig.use_noise_floor (default
    False; bit-identical OFF) + noise_floor_alpha (default 0.1) +
    noise_floor_min_temperature (default 1.0). Wired through
    REEConfig.from_dims().

    Phase-1 instantiation choice (NOT a settled architectural commitment):
    a separate NoiseFloor module at the e3.select() call site, rather
    than per-head temperature inside GatedPolicy as the original notes-
    field hint suggested. Phase-1 reasoning: MECH-313 is state-
    independent and currently must fire on baseline E3 selection too
    (which the per-head approach inside GatedPolicy would miss with
    GatedPolicy disabled). Whether the policy-layer regulators ultimately
    consolidate into one module (e.g. with MECH-314 structured-curiosity
    fitting more naturally inside GatedPolicy and MECH-313 living
    alongside it) is OPEN pending MECH-314 / MECH-318 / MECH-319
    implementations -- those substrates may make different placement
    choices that motivate revisiting MECH-313's. Re-evaluate at the
    point Q-045's 4-arm ablation is queued. The Phase-1 module surface
    + config knobs are stable; what could move is the file location
    and call site.

    Validation: V3-EXQ-544 substrate-readiness diagnostic (UC1
    instantiation, UC2 master-OFF backward-compat, UC3 lift-arithmetic
    sweep, UC4 select_action wiring contract via act_with_split_obs,
    UC5 MECH-094 simulation gate). Smoke-PASS 5/5 2026-05-10. Contract
    tests in tests/contracts/test_mech_313_noise_floor.py 11/11 PASS;
    full contracts suite 253/253 PASS (regression-clean). Behavioural
    validation deferred to Q-045 4-arm ablation (MECH-313 OFF / 313
    only / 260 only / both ON) on V3-EXQ-543b/c successors AFTER this
    substrate is in.

    2026-07-30 (governance-apply, failure_autopsy_batch-687a-707c-840-748a-833-842-810b-673-614-798afail
    CONFIRMED): V3-EXQ-687a -- all 4 arms failed committed_sufficiency at the absolute floor, not a
    discrimination result; re-derive brake exempt. non_contributory/competence_implementation_gap
    (epistemic_category stays standard). Gated on the same MECH-457 competence-floor substrate as the
    rest of the committed-behaviour campaign (MECH-260/Q-045 co-tagged) -- not independently
    re-queueable until that substrate clears.
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: requires the MECH-457 competence-floor
    substrate to clear first -- V3-EXQ-687a (2026-07-30) already found all
    4 arms of the Q-045 design failing committed_sufficiency at the
    absolute floor, which is a readiness failure shared with the whole
    committed-behaviour campaign (MECH-260/Q-045 co-tagged), NOT a
    discrimination result; this claim is not independently re-queueable
    until MECH-457 clears. Also requires the 4-arm design (both-OFF /
    313-only / 260-only / both-ON) to reach adequate N once readiness is
    met -- the N=1-per-cell cross-substrate signals already on file
    (603/603a/603b/605, all same-direction, all sub-threshold) are
    suggestive but explicitly not sufficient per this claim's own notes.
    CONFIRMING signature (this claim's own stated FALSIFIER, restated):
    on the SD-054 reef substrate with ARC-062 gated-policy enabled,
    behaviour diversity (action-distribution entropy at probe states;
    reef/forage occupancy CoV across seeds) ranks both-ON > {313-only,
    260-only} > both-OFF at adequate N -- the two mechanisms (MECH-313
    state-independent noise floor, MECH-260 state-dependent recency
    penalty) are mutually load-bearing. If 313-only alone matches
    both-ON, MECH-260 is contributory-not-necessary; if 260-only alone
    matches both-ON, MECH-313 is contributory-not-necessary (this is
    Q-045's question, answered by the same design).
    FALSIFYING: both-OFF does NOT collapse to monomodal behaviour (some
    OTHER anti-monostrategy mechanism is doing the work, independent of
    both); or 313-only produces no measurable entropy lift over both-OFF
    at adequate N -- would corroborate the co-equal NULL reading carried
    since the 2026-07-09 GOV-CEIL-1 demotion ("the LC-NE noise floor is
    inert / does no work").
    CURRENT STATUS: CONTESTED candidate since 2026-07-09 (7 confirmed
    substrate_ceiling autopsies: 573/603c/603d/603e/610f/655/687) -- the
    competing null reading is carried CO-EQUALLY.
  location: docs/architecture/mech_313_stochastic_noise_floor.md

- id: MECH-314
  title: "structured_curiosity_bonus (frontopolar_uncertainty_driven_exploration_expected_free_energy_analog). A reward bonus on action-selection that rewards information-gain or uncertainty-reduction, distinct from a state-independent noise floor (MECH-313). Biological substrate: rostrolateral prefrontal exploration bonus (Daw et al. 2006 Nature) operating jointly with striatal novelty (Wittmann et al. 2008 Neuron). Three sub-flavours (MECH-314a/b/c) that the lit-pull recommended NOT to collapse prematurely: striatal novelty (Wittmann anchor), frontopolar uncertainty-driven curiosity (Daw anchor), learning-progress / intrinsic-motivation curiosity (Schmidhuber/Pathak tradition). Active-inference (Friston 2010/2015 expected free energy) provides the formal-level unification. Q-044 captures the open question of whether the three sub-flavours are distinct substrates or three readings of one mechanism with different upstream inputs."
  claim_type: mechanism_hypothesis
  subject: policy.structured_curiosity_bonus_parent
  polarity: asserts
  status: candidate_substrate_landed
  live_status:
    reading: candidate_substrate_landed/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-732_2026-07-10
      as_of: 2026-07-10
      verdict: non_contributory/precondition_unmet
  ceiling_decision: deferred
  ceiling_routing_note: "GOV-CEIL-1 keep-parked (NOT demoted) 2026-07-09: flagged with 7 substrate_ceiling hits, but the umbrella CORE has a positive discrimination on a richer substrate -- V3-EXQ-604c PASS on the validated GAP-A substrate (2026-06-08) validated MECH-314's selection-face and lifted v3_pending. The 7 hits are on the curiosity-CONVERSION sub-property (705b/706/706b/590c), which has no separately-registered child claim. Per GOV-CEIL-1 granularity-first, the parent is exempt from demotion (a positive discrimination exists); parked to stop re-flagging. Owed retest re-pointed to the DA-gated cross-loop arbitration-LEARNING substrate (ARC-107 MECH-448/449), unbuilt. [enum-comment migrated to field 2026-07-18] ceiling_decision: GOV-CEIL-1 keep-park 2026-07-09 (user-adjudicated; NOT demoted)"
  epistemic_category: substrate_ceiling
  implementation_phase: v3
  v3_pending: false # /governance 2026-06-08: lifted after V3-EXQ-604c PASS on validated GAP-A substrate (GAP-A ready_validated; supersedes 604b)
  claim_level: mechanistic
  registered_utc: "2026-05-10"
  depends_on:
    - ARC-065     # parent architectural commitment
    - MECH-313    # sibling noise-floor mechanism (differentially weighted, not redundant)
  functional_restatement: >
    MECH-314 names the structured-curiosity-bonus substrate as the second
    major child of ARC-065 (alongside MECH-313 stochastic noise floor).
    The bonus is uncertainty-driven or information-gain-driven, NOT
    state-independent: actions whose outcomes the agent cannot predict
    well, or that would resolve large uncertainty, receive an additive
    reward bonus.

    Three sub-flavours, registered as MECH-314a/b/c. Pull 1 SYNTHESIS
    R3 + Pull 4 R4 explicitly recommended NOT collapsing them prematurely:
      - MECH-314a striatal novelty bonus (Wittmann et al. 2008 Neuron;
        ventral striatum responds to stimulus novelty independent of
        reward prediction error). Sub-flavour where the bonus tracks
        recency-of-encounter.
      - MECH-314b frontopolar uncertainty-driven curiosity (Daw et al.
        2006 Nature; rostrolateral PFC exploration bonus; Wilson et al.
        2014; Kidd & Hayden 2015). Sub-flavour where the bonus tracks
        epistemic value (how much would I learn).
      - MECH-314c learning-progress / intrinsic-motivation curiosity
        (Schmidhuber 1991; Pathak et al. 2017). Sub-flavour where the
        bonus tracks the rate-of-improvement of the agent's predictive
        model. Most computationally instantiated in ML literature; least
        biologically anchored.

    Active-inference unification (Friston 2010/2015 expected free
    energy) decomposes both 314a and 314b into a single epistemic-value
    plus pragmatic-value formal framework. MECH-314c sits a level apart
    -- it is a meta-level signal (rate of learning) not directly
    derivable from EFE.

    Falsifiable: in V3-EXQ-543b/c, parametric sweep of MECH-314 weight
    against MECH-313 weight should produce a Pareto frontier on
    behavioural-diversity-vs-task-reward (Q-043). If MECH-314a/b/c are
    independently load-bearing (Q-044), three-arm ablation should
    surface distinct failure signatures: 314a-OFF reduces novelty-driven
    return-to-explore; 314b-OFF reduces uncertainty-resolution rollouts;
    314c-OFF reduces learning-curve curvature.
  pending_retest_after_substrate: true # /governance 2026-06-29: STAYS TRUE, retest RE-POINTED. The 706b brake-LOCK routed MECH-314's owed retest to the v4_loop_segregation substrate (ARC-110); that substrate LANDED 2026-06-27 and its validation V3-EXQ-707b (weakens-NARROW, failure_autopsy_V3-EXQ-707b_2026-06-29) found the conversion ceiling INTRINSIC -- loop segregation alone does NOT lift it. So MECH-314's ceiling did NOT lift via the loop-seg substrate (the substrate-ceiling audit "ceiling-may-have-lifted" flag is a false-positive driven by other implemented substrates that list MECH-314, e.g. INF-ENV-004/modulatory-bias-authority, not the binding one). Owed retest is now re-pointed to the DA-gated cross-loop arbitration-LEARNING substrate (MECH-448/449/ARC-107) -- a different mechanism, unbuilt. PROMOTES NOTHING. [prior: /governance 2026-06-25 RE-ASSERTED after failure_autopsy_V3-EXQ-705b; was lifted 2026-06-08 after 604c PASS on the selection-face.]
  # /governance 2026-07-01: names the BINDING substrate for the owed retest so the
  # substrate-ceiling audit stops re-flagging MECH-314 as ceiling-may-have-lifted
  # every cycle (recurring false-positive, user-adjudicated 2026-06-29). The ceiling
  # lifts ONLY when an IMPLEMENTED substrate_queue entry actually delivers the
  # DA-gated cross-loop arbitration-LEARNING mechanism (i.e. an implemented entry
  # whose unblocks_claims lists any of these ids); non-binding implemented substrates
  # (INF-ENV-004, modulatory-bias-selection-authority) that merely also list MECH-314
  # no longer trip the flag. When that substrate lands, the audit will re-surface it.
  ceiling_retest_binding_substrate:
    - MECH-448
    - MECH-449
    - ARC-107
  evidence_quality_note: >
    Registered 2026-05-10 from Pull 1 SYNTHESIS R3. Lit anchors:
      - Daw et al. 2006 (Nature) -- frontopolar uncertainty-driven
        exploration
      - Wittmann et al. 2008 (Neuron) -- striatal novelty bonus
      - Wilson et al. 2014 (Cereb Cortex) -- frontopolar exploration
        bonus expansion
      - Kidd & Hayden 2015 (Neuron) -- review
      - Friston 2010 / 2015 -- expected free energy / epistemic value
      - Schmidhuber 1991 -- intrinsic motivation framework
      - Pathak et al. 2017 (ICML) -- curiosity-driven exploration via
        prediction-error
    See: evidence/literature/targeted_review_arc_065_behavioral_diversity_generation/synthesis.md

    [2026-05-29 autopsy V3-EXQ-604 + V3-EXQ-605 cluster]: parent claim; sub-flavour ablations cannot discriminate without per-candidate z_world variance. Same two-substrate-prereq stack as Q-044.

    Pull 4 R4 verdict assigned MECH-314 to HYBRID-naming with parent
    title carrying both the REE-internal structured_curiosity_bonus
    name and the frontopolar_uncertainty_driven_exploration +
    expected_free_energy literature anchors.

    Sub-flavour split is itself a Pull 1 verdict: MECH-314a/b/c are
    registered as separate sub-claims rather than collapsed because
    independent testability + heterogeneous biological provenance
    justify the split. See Q-044 for the open question.

    Substrate landed 2026-05-10: ree-v3/ree_core/policy/structured_curiosity.py
    (StructuredCuriosity + StructuredCuriosityConfig). Master flag
    REEConfig.use_structured_curiosity (default False; bit-identical OFF) +
    3 independently-togglable sub-flavour switches (use_curiosity_novelty /
    _uncertainty / _learning_progress) so Q-044's three-arm ablation is
    a flag-set decision. Composed additively into dacc_score_bias in
    REEAgent.select_action() between the MECH-295 liking-bridge block
    and the MECH-313 noise_floor temperature lift. Phase 1 honest
    scoping documented in design doc + child claim notes: 314a is
    genuinely per-candidate (RBF distance to ResidueField centers);
    314b and 314c are state-dependent global scalars broadcast across
    [K] in Phase 1 (per-candidate refinement deferred to Phase 2
    follow-on requiring an E1 forward-variance head). 13 contract
    tests in tests/contracts/test_mech_314_curiosity.py PASS; full
    contracts suite 273/273 PASS (regression-clean -- bit-identical
    OFF guarantee holds). V3-EXQ-545 substrate-readiness diagnostic
    5/5 PASS smoke 2026-05-10 (UC1-UC5 covering instantiation, master-
    OFF backward-compat, sub-flavour flag-set isolation, select_action
    wiring contract, MECH-094 simulation gate). Status:
    candidate -> candidate_substrate_landed; v3_pending: true retained
    pending Q-044 three-arm ablation (314a-OFF / 314b-OFF / 314c-OFF +
    all-on baseline) on V3-EXQ-543b/c successors AFTER substrate
    landing AND MECH-318 / MECH-319 absorption-check sessions complete.
    Design doc: REE_assembly/docs/architecture/mech_314_structured_curiosity_bonus.md.

    [2026-06-03 governance / cluster autopsy failure_autopsy_604a-624a-630_2026-06-03] V3-EXQ-604a re-tagged non_contributory: C0 PASS (SD-056 contrastive substrate fixed the 604 monomodal collapse, pairwise_dist 0.039>0.03) but curiosity_bias_abs_mean=0.0 in every arm incl ARM_ALL_ON and selected_entropy identical across all 5 arms -- the modulatory curiosity bias has zero authority at E3.select. NOT a falsification (the test could not let the claim express itself). substrate_ceiling (selection-authority sub-type); pending_retest_after_substrate -- blocked by substrate_queue CREATE 'modulatory-bias-selection-authority'. Convergent with 624a (MECH-320) and the z_goal salience gap (603e/626a).

    [2026-06-03 substrate landing] modulatory-bias-selection-authority IMPLEMENTED (ree-v3 e3_selector.select gap-relative rescale of the combined modulatory contribution to modulatory_authority_gain*raw_score_range + e3_score_diversity.stratified_select across-class unit-range normalization; config use_modulatory_selection_authority default OFF/bit-identical). Gives this claim's score-bias lever genuine BOUNDED authority at E3 committed selection (the 604a/624a/614d cluster showed the lever fired but never changed the committed argmin). 604a curiosity_bias_abs_mean=0.0 was the failure; NOTE necessary-but-not-sufficient -- curiosity bias was genuinely zero (314a no active residue centers + 314b/c broadcast-by-design), so the retest must guard curiosity_bias_abs_mean>0 before testing authority. Claim STAYS candidate/v3_pending -- substrate-readiness validation EXQ pending (claim_ids=[]); a per-claim EVIDENCE retest on the authority-ON substrate is the governance-weighting signal. Design doc docs/architecture/modulatory_bias_selection_authority.md; substrate_queue modulatory-bias-selection-authority status implemented_pending_validation.

    [2026-06-07 governance: V3-EXQ-604b NON-CONTRIBUTORY for MECH-314/314a/314b/314c/Q-044, NOT a weakening] (failure_autopsy_gapA-cluster-604b-648a-649). 604b ran 11:01Z BEFORE the ARC-065 GAP-A action-conditional candidate-diversity fix was validated (V3-EXQ-649 PASS 13:14Z). With a class-uniform candidate pool the structured-curiosity bias had magnitude (last_bias_max_abs=0.0177) but ~zero cross-candidate RANGE, so C1 cannot move the E3 argmin even with selection authority ON (643a). 3rd confirmed instance of the candidate-pool-collapse confound (604a/624a/614d-e). The 604b manifest is set evidence_direction=non_contributory (self-emitted weakens overridden).     The GAP-A fix is now VALIDATED READY (649) -- re-queue the MECH-314-family test as V3-EXQ-604c on the now-ready substrate, with a readiness gate on cross-candidate RANGE (not bias magnitude), before any direction is weighed. pending_retest_after_substrate already true.

    [2026-06-25 governance: failure_autopsy_V3-EXQ-705b CONFIRMED non_contributory; MECH-314 UNWEAKENED] V3-EXQ-705b (curiosity-conversion under MECH-448 demotion + 689e channel-adaptive floor; supersedes V3-EXQ-705) was the FIRST FAIR test of the curiosity channel under a genuinely-excluding demotion (excluded_count 14.45 vs 705's all-admit 0.0; 590c saturation-arm confound also fixed) with the GAP-A pool divergent (cand_world_pairwise_dist 0.107 > 0.02). Committed-action-class entropy STILL did not lift over demotion-ON F-only + matched-noise -> conversion_ceiling_persists_despite_demotion. epistemic_category set substrate_ceiling; the ceiling is UPSTREAM/architectural (a single rank-preserving eligibility gate), NOT a falsification of curiosity. Re-derive brake FIRED (6th MECH-314 ceiling autopsy; priors EXQ-572-573, 604a-624a-630, gapA-604b-648a-649, 590c, 705) -> REFUSES a 705c demotion-only same-substrate letter. recommended_substrate_queue_entry action=none: MECH-449 Go/No-Go eligibility constitution is ALREADY built+validated+provisional (V3-EXQ-689g PASS 3/3, conversion_rate 1.0) but was OFF in 705b. Forward path = brake-EXEMPT /queue-experiment DOUBLE-GATED re-test (use_f_eligibility_demotion=True AND use_go_nogo_constitution=True), a different substrate condition than the demotion-only lineage (485k->MECH-449 shape). V4 ARC-110 loop-segregation (existing v4_loop_segregation substrate_queue entry) ONLY if the double-gated re-test also fails. pending_retest_after_substrate re-asserted.
    [2026-06-27 governance: failure_autopsy_704b-706b-conversion-ceiling CONFIRMED non_contributory; MECH-314 UNWEAKENED; brake-LOCK terminal] V3-EXQ-706b (the brake-EXEMPT DOUBLE-GATED re-test the 705b autopsy named, superseding V3-EXQ-706) is the FIRST FULLY-VALID double-gated curiosity-conversion test (non_degenerate=True; all 6 readiness legs met: GAP-A pool 0.072, curiosity bias range 0.00197, MECH-448 demotion excluded 23.4, MECH-449 Go/No-Go suppressed 7.9, z_world bounded, per-seed committed-tick budget balanced). It closed BOTH 706 measurement-validity defects (degenerate flat-hot temperature null replaced by a VALID same-layer non-temperature null magnitude-matched by construction, range 0.00197=curiosity range, ratio 1.0, non-degenerate vs ARM_FONLY; per-seed P1 budget equalised). With both composed selection-face eligibility gates active AND a valid null, the curiosity channel STILL did not convert: ARM_CURIOSITY committed-class entropy 0.967 sat BELOW the double-gated F-only control 1.029 and the valid null 1.019 (no lift on any seed) -> conversion_ceiling_persists_despite_double_gating_valid_null (the pre-registered TERMINAL). The ceiling is ARCHITECTURAL (the single F-dominated E3 arena), POSITIVE evidence that the binding constraint sits AFTER the eligibility-construction face -> the single-arena collapse (ARC-110). Re-derive brake FIRED + BRAKE-LOCK (8th MECH-314 substrate_ceiling/non_contributory autopsy; priors EXQ-572-573, 604a-624a-630, gapA-604b-648a-649, 590c, 705, 705b, 706): REFUSES any further V3 letter on the MECH-314 conversion lineage. Route /implement-substrate on v4_loop_segregation (ARC-110); substrate_queue amended (MECH-314 added to unblocks_claims + 706b failure record). epistemic_category stays substrate_ceiling; pending_retest_after_substrate stays true. Convergent with V3-EXQ-704b (MECH-451) onto the identical single-arena wall.

    [enum-comment migrated to field 2026-07-18] epistemic_category: /governance 2026-06-25:
    failure_autopsy_V3-EXQ-705b (1st FAIR demotion test, conversion ceiling persists; 6th
    MECH-314 ceiling autopsy, re-derive brake FIRED). Ceiling is upstream/architectural, NOT a
    falsification; mapped to the brake-EXEMPT double-gated (demotion+MECH-449 Go/No-Go) re-test
    (substrate already built) -> V4 ARC-110 loop-segregation only if that also fails.

    [2026-07-20 governance / intra-run substrate-divergence sweep sec 7 item 4] The intra-run
    substrate-divergence hit (defect class D3) recorded against V3-EXQ-604c is WITHDRAWN --
    specifically EXONERATED at rung (c), DV inertness, by failure_autopsy_V3-EXQ-604c_2026-07-20
    (REE_assembly 75726ecb4c) with `sufficient_alone_to_withdraw: false`. All 15 cells executed one
    substrate (f80bc236); the second hash is a disk-state recording artefact, and five seed-matched
    cross-boundary cell-pairs were bit-identical on all trajectory-determined quantities. The
    MECH-314 `supports` reading STANDS unchanged.

    DO NOT CONFLATE: the same autopsy UPHELD a separate and larger STRUCTURAL VACUITY defect, which
    is unrelated to D3 and bears on MECH-314b, MECH-314c and Q-044 only. MECH-314 and MECH-314a are
    not implicated by it.
  notes: >
    MECH-314 is the parent claim; MECH-314a/b/c are registered immediately
    after as sibling sub-claims. The parent's depends_on does not list the
    children (they depend on it, not vice versa).

    Phase-1 architectural-placement note (mirrors MECH-313): a SEPARATE
    StructuredCuriosity module at the e3.select() call site, in parallel
    with MECH-313 NoiseFloor and the GatedPolicy bias chain. Whether
    the policy-layer regulators ultimately consolidate into one module
    is OPEN pending MECH-318 / MECH-319 substrates and Q-043 / Q-044
    calibration. The separate-module choice keeps each substrate
    independently togglable, which is what Q-044 needs. Re-evaluate
    placement at the point Q-044 / Q-043 are queued.
  location: docs/architecture/rule_apprehension_layer.md

- id: MECH-314a
  title: "novelty_bonus_striatal_analog. A reward bonus that tracks recency-of-encounter; new or rarely-encountered states/actions receive a positive bonus independent of reward prediction error. Biological substrate: ventral striatum response to stimulus novelty (Wittmann et al. 2008 Neuron). Sub-flavour of MECH-314 structured curiosity bonus."
  claim_type: mechanism_hypothesis
  subject: policy.curiosity.novelty_bonus_striatal_analog
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_batch9_2026-06-12#V3-EXQ-590b"
      as_of: 2026-06-12
      verdict: non_contributory (sub-hypothesis recorded for synthesis; no demotion)/standard (parent unchanged); failure routed as granularity debt
  status_note: >
    [enum-comment migrated to field 2026-07-18] status: /governance 2026-06-14:
    candidate_substrate_landed -> provisional (user-directed). exp_conf 0.759 (> 0.62
    candidate->provisional gate) after the flat-authoritative indexer correction (074ab9401e)
    cleared the stale pack weakens; v3_pending already lifted 2026-06-08 (V3-EXQ-604c PASS).
    Single genuine exp entry (1 support) -- promoted on a thin-but-clean record; a confirmatory
    run would harden it.
  implementation_phase: v3
  v3_pending: false # /governance 2026-06-08: lifted after V3-EXQ-604c PASS on validated GAP-A substrate (GAP-A ready_validated; supersedes 604b)
  claim_level: mechanistic
  registered_utc: "2026-05-10"
  depends_on:
    - MECH-314    # parent structured-curiosity-bonus claim
    - ARC-065     # grandparent architectural commitment
  functional_restatement: >
    MECH-314a names the recency-of-encounter sub-flavour of
    structured curiosity. The biology: ventral striatum produces a
    novelty signal independent of dopamine reward-prediction-error
    (Wittmann et al. 2008). The computational analog is a count-based
    or pseudo-count exploration bonus (Bellemare et al. 2016
    NeurIPS-style; Burda et al. 2018 RND).

    Falsifiable independently of MECH-314b/c: state-novelty ablation
    should produce different failure signature from uncertainty-driven
    curiosity ablation (314b) and learning-progress ablation (314c).
    Specific signature: 314a-OFF agents show reduced return-rate to
    rarely-visited states; 314b-OFF agents show reduced uncertainty-
    resolution rollouts at familiar-but-uncertain states; 314c-OFF
    agents show reduced learning-curve curvature on tasks where model
    improvement rate varies with policy.
  pending_retest_after_substrate: false # /governance 2026-06-08: lifted after V3-EXQ-604c PASS on validated GAP-A substrate (GAP-A ready_validated; supersedes 604b)
  evidence_quality_note: >
    Registered 2026-05-10 from Pull 1 SYNTHESIS R3. Lit anchor:
      - Wittmann et al. 2008 (Neuron) -- ventral striatum novelty
        response
      - Bellemare et al. 2016 (NeurIPS) -- pseudo-count exploration
        bonus (computational analog)
      - Burda et al. 2018 (ICLR) -- random network distillation
        (computational analog)
    See: evidence/literature/targeted_review_arc_065_behavioral_diversity_generation/synthesis.md

    [2026-05-29 autopsy V3-EXQ-604 + V3-EXQ-605 cluster]: per-candidate novelty bonus collapses to uniform across z_world-degenerate candidate sets, making ARM_1 (novelty-off) bit-identical to ARM_0 (all-on) per seed in 604. NOT a substrate ceiling specific to the novelty mechanism -- it's the upstream per-candidate-variance gap. SD-056 substrate-readiness PASS (V3-EXQ-613) and behavioural validation (V3-EXQ-569a, queued today) are the proximal retest path.

    Sub-flavour split rationale: MECH-314a is independently testable
    via state-novelty bonus ablation and has a distinct biological
    substrate (ventral striatum) from MECH-314b (frontopolar) and
    MECH-314c (no clean biological anchor; ML-literature dominant).

    Substrate landed 2026-05-10 as part of MECH-314 cluster (ree-v3/
    ree_core/policy/structured_curiosity.py + StructuredCuriosityConfig.
    use_curiosity_novelty + curiosity_novelty_weight). Phase 1 signal
    source: minimum distance from candidate's first-step z_world to
    the nearest ACTIVE ResidueField RBF center, normalised by candidate-
    pool mean norm. Genuinely per-candidate (output [K] tensor varies
    across candidates). Bonus is non-positive in E3's lower-is-better
    convention (high novelty -> attractive). Behavioural validation
    deferred to Q-044 three-arm ablation. See parent MECH-314 entry
    for the full implementation note.

    [2026-06-03 governance / cluster autopsy failure_autopsy_604a-624a-630_2026-06-03] V3-EXQ-604a re-tagged non_contributory: C0 PASS (SD-056 contrastive substrate fixed the 604 monomodal collapse, pairwise_dist 0.039>0.03) but curiosity_bias_abs_mean=0.0 in every arm incl ARM_ALL_ON and selected_entropy identical across all 5 arms -- the modulatory curiosity bias has zero authority at E3.select. NOT a falsification (the test could not let the claim express itself). substrate_ceiling (selection-authority sub-type); pending_retest_after_substrate -- blocked by substrate_queue CREATE 'modulatory-bias-selection-authority'. Convergent with 624a (MECH-320) and the z_goal salience gap (603e/626a).

    [2026-06-12 governance / confirmed failure_autopsy_batch9_2026-06-12 applied]:
    V3-EXQ-590b (novelty Goldilocks behavioural calibration) reclassified
    does_not_support -> non_contributory. On the now-ready (643a-validated)
    authority substrate the gain knob moves H_pos by 0.283 across gains (authority
    OPERATIVE, not zero -- the 604a zero-authority bottleneck is resolved) but every
    nonzero gain matched or degraded exploration vs the gain=0 control (best 0.4188 <
    0.4200; lift 1/3 seeds). NOT a demotion. The substrate excuse is now spent
    (ready=true) and this is the >=3rd MECH-314a failure with a distinct signature ->
    routed to /claim-synthesis as granularity debt: 'novelty buys exploration'
    under-names authority-moves-argmin != signal-is-adaptive. No confidence change
    this cycle.

    [2026-07-20 governance / intra-run substrate-divergence sweep sec 7 item 4] The intra-run
    substrate-divergence hit (defect class D3) recorded against V3-EXQ-604c is WITHDRAWN --
    EXONERATED at rung (c), DV inertness, by failure_autopsy_V3-EXQ-604c_2026-07-20 (REE_assembly
    75726ecb4c) with `sufficient_alone_to_withdraw: false`. All 15 cells executed one substrate
    (f80bc236); the divergent hash is a disk-state recording artefact. The MECH-314a reading STANDS
    unchanged and no confidence is re-weighted. As for MECH-314: the separate structural-vacuity
    defect that autopsy upheld bears on MECH-314b / MECH-314c / Q-044 only, not on this claim.
  location: docs/architecture/rule_apprehension_layer.md

- id: MECH-314b
  title: "uncertainty_driven_curiosity_frontopolar_analog. A reward bonus that tracks epistemic value -- how much the agent would learn from sampling a state or action. Biological substrate: rostrolateral prefrontal exploration bonus (Daw et al. 2006 Nature). Sub-flavour of MECH-314 structured curiosity bonus. Active-inference framing: bonus equals epistemic value term in expected free energy decomposition (Friston 2010/2015)."
  claim_type: mechanism_hypothesis
  subject: policy.curiosity.uncertainty_driven_frontopolar_analog
  polarity: asserts
  status: candidate_substrate_landed
  ceiling_decision: deferred
  ceiling_routing_note: >
    2026-07-20 substrate-ceiling-lifted triage: FALSE POSITIVE, parked.
    check_substrate_ceiling_audit.py surfaced this claim as ceiling-may-have-lifted because
    modulatory-bias-selection-authority is implemented and lists it in unblocks_claims. That is
    NOT the binding substrate: it was ALREADY ACTIVE during V3-EXQ-604c (authority gate operative,
    readiness gate r3 primary_scores_bounded_fraction 1.0) -- i.e. the substrate the audit calls
    the lift was in place when the vacuous result was produced, so a retest against it reproduces
    that result exactly. The binding ceiling is the Phase-1 BROADCAST-SCALAR implementation
    (ree_core/policy/structured_curiosity.py:420 -- scalar * torch.ones(K)): a uniform additive
    constant cannot move an argmax nor a softmax sample, while the C2 DV is a pure function of the
    argmax, so the observed delta of exactly 0.0 is an ARITHMETIC IDENTITY fixed before the run,
    independent of whether MECH-314b is true. RE-DERIVE BRAKE FIRED (3rd substrate_ceiling
    adjudication; failure_autopsy_V3-EXQ-604c_2026-07-20 sec 7b): a further same-claim ablation is
    REFUSED -- its result is knowable in advance without running it, and a DV swap alone is not an
    escape. UN-PARK TRIGGER: per-candidate uncertainty (the Phase-2 treatment MECH-314a already
    received via MECH-314a-Phase-2-impl), tracked as the 604c amend on the ARC-065 substrate_queue
    entry. This is the same false-positive class as the 2026-07-01 ceiling_retest_binding_substrate
    fix applied to parent MECH-314, which was never propagated to the children; a binding-substrate
    field is not used here because no claim id yet names the per-candidate curiosity refinement, and
    any available token (e.g. MECH-314b itself) also matches the implemented non-binding entry.
    status and pending_retest_after_substrate deliberately UNCHANGED -- the retest is still owed,
    just not yet runnable.
  live_status:
    reading: candidate_substrate_landed
    as_of: 2026-07-20
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-604c_2026-07-20"
      as_of: 2026-07-20
      verdict: non_contributory/substrate_ceiling
  last_reviewed: 2026-07-20
  epistemic_category: substrate_ceiling
  implementation_phase: v3
  v3_pending: false # /governance 2026-06-08: lifted after V3-EXQ-604c PASS on validated GAP-A substrate (GAP-A ready_validated; supersedes 604b)
  claim_level: mechanistic
  registered_utc: "2026-05-10"
  depends_on:
    - MECH-314    # parent
    - ARC-065
  functional_restatement: >
    MECH-314b names the epistemic-value sub-flavour of structured
    curiosity. The biology: rostrolateral PFC exploration bonus
    (Daw et al. 2006); high-level goal-directed exploration that
    tracks "how much would I learn" rather than "how recently
    visited."

    The active-inference framework (Friston 2010/2015) provides
    the formal unification: epistemic value is the expected
    information gain term in expected-free-energy decomposition.
    Computational implementations include Bayes-optimal
    exploration (Bellemare 2014, Houthooft 2016 VIME), and
    Thompson sampling.

    Falsifiable per MECH-314 functional_restatement: 314b-OFF
    agents show reduced uncertainty-resolution rollouts at
    familiar-but-uncertain states.
  pending_retest_after_substrate: true # /governance 2026-07-20: REINSTATED -- the 2026-06-08 lift cited the V3-EXQ-604c PASS, which failure_autopsy_V3-EXQ-604c_2026-07-20 shows was structurally vacuous for this claim (broadcast scalar vs argmax-derived DV). Gated on the ARC-065 GAP-A Phase-2 per-candidate build.
  evidence_quality_note: >
    Registered 2026-05-10 from Pull 1 SYNTHESIS R3. Lit anchors:
      - Daw et al. 2006 (Nature) -- frontopolar exploration bonus
      - Wilson et al. 2014 (Cereb Cortex) -- expansion
      - Friston 2010 (Nat Rev Neurosci) -- free-energy framework
      - Friston et al. 2015 (Cogn Neurosci) -- active inference and
        epistemic value
      - Houthooft et al. 2016 (NeurIPS) -- VIME (computational analog)
    See: evidence/literature/targeted_review_arc_065_behavioral_diversity_generation/synthesis.md

    [2026-05-29 autopsy V3-EXQ-604 cluster]: ARM_2 (uncertainty-off) == ARM_0 (all-on) bit-identical per seed in 604 is the declared Phase 1 broadcast-scalar honest-scoping caveat surfacing under ablation, NOT a substrate ceiling specific to this claim. Per-candidate refinement (Phase 2 follow-on) is the architectural work that makes 314b independently ablation-testable. No demotion or supports tagging warranted from V3-EXQ-604.

    Substrate landed 2026-05-10 as part of MECH-314 cluster (ree-v3/
    ree_core/policy/structured_curiosity.py + StructuredCuriosityConfig.
    use_curiosity_uncertainty + curiosity_uncertainty_weight). Phase 1
    signal source: e3._running_variance scalar (waking-tick PE-magnitude
    accumulator). Phase 1 instantiation: BROADCAST scalar across [K]
    (per-candidate refinement requires E1 forward-variance head,
    deferred to Phase 2 follow-on). Bonus is non-positive in E3's
    lower-is-better convention (high uncertainty -> attractive). Phase
    1 honest-scoping caveat: broadcast-scalar 314b shifts every
    candidate's score by the same amount and does not change
    selection ordering; it can affect commit-threshold dynamics but
    not which candidate wins under softmax sampling. Per-candidate
    refinement is the Phase 2 work that makes 314b's distinct
    failure-signature contract from 314a / 314c testable at the
    candidate-selection level. See parent MECH-314 entry for the
    full implementation note.

    [2026-06-03 governance / cluster autopsy failure_autopsy_604a-624a-630_2026-06-03] V3-EXQ-604a re-tagged non_contributory: C0 PASS (SD-056 contrastive substrate fixed the 604 monomodal collapse, pairwise_dist 0.039>0.03) but curiosity_bias_abs_mean=0.0 in every arm incl ARM_ALL_ON and selected_entropy identical across all 5 arms -- the modulatory curiosity bias has zero authority at E3.select. NOT a falsification (the test could not let the claim express itself). substrate_ceiling (selection-authority sub-type); pending_retest_after_substrate -- blocked by substrate_queue CREATE 'modulatory-bias-selection-authority'. Convergent with 624a (MECH-320) and the z_goal salience gap (603e/626a).

    [2026-07-20 governance / autopsy failure_autopsy_V3-EXQ-604c_2026-07-20] evidence_direction
    mixed -> non_contributory; epistemic_category substrate_ceiling. STRUCTURAL
    VACUITY sub-type -- a DIFFERENT mechanism from the 2026-06-03 selection-authority
    reading above, which this supersedes as the live reading. That one held the
    curiosity bias had zero AUTHORITY at E3.select (it reached the consumer but the
    consumer could not act on it). This one is upstream of authority and holds
    independently of it: Phase-1 uncertainty (314b) and learning-progress (314c) are
    GLOBAL SCALARS BROADCAST across all K candidates
    (ree-v3/ree_core/policy/structured_curiosity.py:40-41,101), and a uniform additive
    constant cannot change an argmax or a softmax sample. V3-EXQ-604c's C2 DV
    (selected_action_class_entropy) is a pure function of the argmax sequence, so
    delta == 0.0 was an ARITHMETIC IDENTITY fixed before the run -- true independently
    of whether these claims are true. The 604c PASS therefore cannot bear on them in
    either direction, and the 2026-06-08 lift of v3_pending /
    pending_retest_after_substrate (which cited that PASS) rested on a vacuous result;
    pending_retest_after_substrate is reinstated.

    NOT the intra-run substrate divergence. That defect (D3) was independently
    EXONERATED for 604c at the module-import-binding rung -- all 15 cells executed one
    substrate (f80bc236); the second hash is a disk-state recording artefact. Five
    seed-matched cross-boundary cell-pairs were bit-identical on all
    trajectory-determined quantities.

    Manifest self-confirmation (the ablations ARE plumbed; the channels DO fire --
    this is not a wiring fault): curiosity_bias_range_mean is identical to full
    precision across ALL_ON / UNCERTAINTY_OFF / LP_OFF (0.01529031 -- a broadcast
    constant cancels in a max-minus-min range) while curiosity_bias_max_abs_mean
    differs (0.240986 / 0.235738 / 0.240916 -- it does not cancel), and
    curiosity_subflavours_fired_mean moves correctly (2.866 / 1.866 / 2.000 at seed 42).

    MECH-314 and MECH-314a `supports` STAND (per-candidate novelty on e2_world_forward,
    authority gate active). Caveat recorded for future readers: 604c's C1 and its C2
    novelty leg are the SAME comparison, not two independent ones --
    ARM_NOVELTY_OFF collapses exactly onto ARM_OFF on every trajectory quantity, so
    both read 0.402505. The run yields one finding, not two.

    Re-derive brake FIRES (3 vs threshold 2): a further same-claim ablation is REFUSED.
    Build route only -- make both terms per-candidate (`amend` on ARC-065, GAP-A Phase-2
    per-candidate extension), landed REE_assembly 2241c39cdc. Note the 604c readiness
    gate r2 (curiosity_bias_range) is unknowingly a NOVELTY-ONLY gate -- the exact
    statistic in which broadcast terms vanish -- so it certifies readiness for
    MECH-314a alone.
  location: docs/architecture/rule_apprehension_layer.md

- id: MECH-314c
  title: "learning_progress_curiosity_intrinsic_motivation_analog. A reward bonus that tracks the rate-of-improvement of the agent's predictive model (or any internal-state quantity); actions that produce a steepening learning curve receive a positive bonus. Most computationally instantiated in ML literature (Schmidhuber 1991, Pathak et al. 2017); least biologically anchored of the three MECH-314 sub-flavours -- no clean cellular or regional analog. Distinguishes from MECH-314a (recency-based) and MECH-314b (uncertainty-based) by tracking second-order signal (improvement rate) rather than first-order (count or uncertainty)."
  claim_type: mechanism_hypothesis
  subject: policy.curiosity.learning_progress_intrinsic_motivation_analog
  polarity: asserts
  status: candidate_substrate_landed
  ceiling_decision: deferred
  ceiling_routing_note: >
    2026-07-20 substrate-ceiling-lifted triage: FALSE POSITIVE, parked. Identical mechanism and
    disposition to MECH-314b (see that claim's ceiling_routing_note). The audit surfaced this claim
    because modulatory-bias-selection-authority is implemented and lists it in unblocks_claims, but
    that substrate was already active during V3-EXQ-604c and is not the binding ceiling. The binding
    ceiling is the Phase-1 BROADCAST-SCALAR implementation
    (ree_core/policy/structured_curiosity.py:438 -- scalar * torch.ones(K)); the observed C2 delta of
    exactly 0.0 is an ARITHMETIC IDENTITY, not a measurement. RE-DERIVE BRAKE FIRED (3rd;
    failure_autopsy_V3-EXQ-604c_2026-07-20 sec 7b): a further same-claim ablation is REFUSED.
    UN-PARK TRIGGER: per-candidate learning-progress estimation (Phase-2), tracked as the 604c amend
    on the ARC-065 substrate_queue entry. Weigh cost against Pull 1 SYNTHESIS R3, which flagged
    MECH-314c as the LEAST biologically anchored of the three sub-flavours (no clean cellular or
    regional analog) and explicitly potentially-discardable-if-314a-plus-314b-suffice per Q-044 --
    registration kept the option open without committing REE to it as load-bearing. status and
    pending_retest_after_substrate deliberately UNCHANGED.
  live_status:
    reading: candidate_substrate_landed
    as_of: 2026-07-20
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-604c_2026-07-20"
      as_of: 2026-07-20
      verdict: non_contributory/substrate_ceiling
  last_reviewed: 2026-07-20
  epistemic_category: substrate_ceiling
  implementation_phase: v3
  v3_pending: false # /governance 2026-06-08: lifted after V3-EXQ-604c PASS on validated GAP-A substrate (GAP-A ready_validated; supersedes 604b)
  claim_level: mechanistic
  registered_utc: "2026-05-10"
  depends_on:
    - MECH-314    # parent
    - ARC-065
  functional_restatement: >
    MECH-314c names the learning-progress sub-flavour of structured
    curiosity. The bonus is meta-level: it tracks the derivative-of-
    improvement of the agent's internal predictive model (or any
    internal scalar that progresses under learning). The Schmidhuber
    1991 framework of "intrinsic motivation as compression progress"
    is the original formulation; Pathak et al. 2017 (curiosity by
    prediction error against a forward model) is the most-cited
    modern instantiation.

    No clean biological anchor. Unlike MECH-314a (ventral striatum)
    and MECH-314b (frontopolar PFC), MECH-314c does not map cleanly
    to a known regional substrate. Pull 1 SYNTHESIS R3 noted this
    explicitly: 314c is the most ML-native sub-flavour and may
    ultimately be REE-discardable if 314a + 314b suffice.

    Falsifiable: 314c-OFF agents on tasks where model improvement
    rate varies with policy should show reduced learning-curve
    curvature compared to 314c-ON agents holding 314a + 314b
    constant.
  pending_retest_after_substrate: true # /governance 2026-07-20: REINSTATED -- the 2026-06-08 lift cited the V3-EXQ-604c PASS, which failure_autopsy_V3-EXQ-604c_2026-07-20 shows was structurally vacuous for this claim (broadcast scalar vs argmax-derived DV). Gated on the ARC-065 GAP-A Phase-2 per-candidate build.
  evidence_quality_note: >
    Registered 2026-05-10 from Pull 1 SYNTHESIS R3 + Pull 4 R4.
    Pull 1 explicitly flagged 314c as least biologically-anchored;
    registration as a candidate keeps the option open without
    committing REE to it as load-bearing. Lit anchors:
      - Schmidhuber 1991 -- "compression progress" framework
      - Pathak et al. 2017 (ICML) -- curiosity-driven exploration by
        forward-model prediction error
      - Oudeyer et al. 2007 -- intrinsic motivation systems / learning-
        progress framework in robotics
    See: evidence/literature/targeted_review_arc_065_behavioral_diversity_generation/synthesis.md

    [2026-05-29 autopsy V3-EXQ-604 cluster]: same as MECH-314b -- ARM_3 (lp-off) == ARM_0 (all-on) bit-identical per seed is the Phase 1 broadcast-scalar caveat surfacing under ablation, NOT a substrate ceiling. Phase 2 per-candidate refinement is the prerequisite for ablation-testability.

    Q-044 captures the question of whether 314a/b/c are distinct
    substrates or three readings of one mechanism. If 314c is
    discardable (i.e. 314a + 314b suffice on V3 substrates), 314c
    can be retired without architectural cost.

    Substrate landed 2026-05-10 as part of MECH-314 cluster (ree-v3/
    ree_core/policy/structured_curiosity.py + StructuredCuriosityConfig.
    use_curiosity_learning_progress + curiosity_learning_progress_weight
    + curiosity_lp_ema_alpha + curiosity_lp_window_k). Phase 1 signal
    source: EMA of |PE_t - PE_{t-K}| where PE_t is fed in from
    e3._running_variance each waking tick via
    StructuredCuriosity.update_prediction_error(). LP buffer + EMA are
    per-episode (cleared by reset()) -- a fresh task / environment
    carries a fresh learning curve. Phase 1 instantiation: BROADCAST
    scalar across [K] (per-candidate refinement requires per-candidate
    learning-progress estimation, deferred to Phase 2 follow-on).
    Bonus is non-positive in E3's lower-is-better convention (high
    LP -> attractive). Same Phase-1 honest-scoping caveat as MECH-314b:
    broadcast-scalar 314c shifts every candidate's score by the same
    amount and does not change selection ordering -- candidate-
    selection-level distinct failure-signature contract requires Phase
    2 per-candidate refinement. Q-044's three-arm ablation can falsify
    the substrate-landed flag-set independence at the global-signal
    level; sub-flavour-distinct failure signatures at candidate
    selection require Phase 2. See parent MECH-314 entry for the
    full implementation note.

    [2026-06-03 governance / cluster autopsy failure_autopsy_604a-624a-630_2026-06-03] V3-EXQ-604a re-tagged non_contributory: C0 PASS (SD-056 contrastive substrate fixed the 604 monomodal collapse, pairwise_dist 0.039>0.03) but curiosity_bias_abs_mean=0.0 in every arm incl ARM_ALL_ON and selected_entropy identical across all 5 arms -- the modulatory curiosity bias has zero authority at E3.select. NOT a falsification (the test could not let the claim express itself). substrate_ceiling (selection-authority sub-type); pending_retest_after_substrate -- blocked by substrate_queue CREATE 'modulatory-bias-selection-authority'. Convergent with 624a (MECH-320) and the z_goal salience gap (603e/626a).

    [2026-07-20 governance / autopsy failure_autopsy_V3-EXQ-604c_2026-07-20] evidence_direction
    mixed -> non_contributory; epistemic_category substrate_ceiling. STRUCTURAL
    VACUITY sub-type -- a DIFFERENT mechanism from the 2026-06-03 selection-authority
    reading above, which this supersedes as the live reading. That one held the
    curiosity bias had zero AUTHORITY at E3.select (it reached the consumer but the
    consumer could not act on it). This one is upstream of authority and holds
    independently of it: Phase-1 uncertainty (314b) and learning-progress (314c) are
    GLOBAL SCALARS BROADCAST across all K candidates
    (ree-v3/ree_core/policy/structured_curiosity.py:40-41,101), and a uniform additive
    constant cannot change an argmax or a softmax sample. V3-EXQ-604c's C2 DV
    (selected_action_class_entropy) is a pure function of the argmax sequence, so
    delta == 0.0 was an ARITHMETIC IDENTITY fixed before the run -- true independently
    of whether these claims are true. The 604c PASS therefore cannot bear on them in
    either direction, and the 2026-06-08 lift of v3_pending /
    pending_retest_after_substrate (which cited that PASS) rested on a vacuous result;
    pending_retest_after_substrate is reinstated.

    NOT the intra-run substrate divergence. That defect (D3) was independently
    EXONERATED for 604c at the module-import-binding rung -- all 15 cells executed one
    substrate (f80bc236); the second hash is a disk-state recording artefact. Five
    seed-matched cross-boundary cell-pairs were bit-identical on all
    trajectory-determined quantities.

    Manifest self-confirmation (the ablations ARE plumbed; the channels DO fire --
    this is not a wiring fault): curiosity_bias_range_mean is identical to full
    precision across ALL_ON / UNCERTAINTY_OFF / LP_OFF (0.01529031 -- a broadcast
    constant cancels in a max-minus-min range) while curiosity_bias_max_abs_mean
    differs (0.240986 / 0.235738 / 0.240916 -- it does not cancel), and
    curiosity_subflavours_fired_mean moves correctly (2.866 / 1.866 / 2.000 at seed 42).

    MECH-314 and MECH-314a `supports` STAND (per-candidate novelty on e2_world_forward,
    authority gate active). Caveat recorded for future readers: 604c's C1 and its C2
    novelty leg are the SAME comparison, not two independent ones --
    ARM_NOVELTY_OFF collapses exactly onto ARM_OFF on every trajectory quantity, so
    both read 0.402505. The run yields one finding, not two.

    Re-derive brake FIRES (3 vs threshold 2): a further same-claim ablation is REFUSED.
    Build route only -- make both terms per-candidate (`amend` on ARC-065, GAP-A Phase-2
    per-candidate extension), landed REE_assembly 2241c39cdc. Note the 604c readiness
    gate r2 (curiosity_bias_range) is unknowingly a NOVELTY-ONLY gate -- the exact
    statistic in which broadcast terms vanish -- so it certifies readiness for
    MECH-314a alone.
  location: docs/architecture/rule_apprehension_layer.md

- id: Q-043
  title: "How should the relative weights of MECH-313 (stochastic noise floor / LC-NE tonic analog) and MECH-314 (structured curiosity bonus / frontopolar curiosity) be calibrated for V3 substrates? Falsifiable with parametric sweep on SD-054 reef substrate plus ARC-062 gated-policy."
  claim_type: open_question
  subject: policy.diversity_generation.relative_weight_calibration
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-667_2026-06-11
      as_of: 2026-06-11
      verdict: non_contributory/substrate_conditional
  what_would_answer: |
    A parametric (w_313, w_314) weight sweep on the SD-054 reef substrate with
    ARC-062 gated-policy enabled, measuring behavioural diversity
    (action-distribution entropy at probe states; reef-vs-forage occupancy CoV)
    and task reward JOINTLY, then locating the Pareto frontier. ANSWER
    "calibration resolved" if a Pareto frontier exists with a consistent (w_313
    noise-floor, w_314 structured-curiosity) regime across seeds where both
    diversity and reward are simultaneously high -- the mixing ratio that avoids
    MECH-313 swamping the rule-discriminative signal and MECH-314 overcommitting
    to information-gain at task-reward cost. ANSWER "no calibration zone --
    architectural defect" if no Pareto frontier exists, indicating the two
    mechanisms do not compose additively in the policy gradient as designed. HARD
    PRECONDITION (the decisive blocker, confirmed twice): the swept knobs must
    have AUTHORITY over E3 selection. V3-EXQ-605 found the curiosity dimension
    fully degenerate (curiosity_scale 1x/5x/10x byte-identical per-cell entropy)
    and V3-EXQ-667 found 4/5 seeds byte-identical across full 8x joint scaling
    (healthy-seed h_pos range 0.017 < 0.05 floor) -- the knobs have ZERO authority
    at E3.select on the current substrate, so any sweep is vacuous and self-routes
    substrate_not_ready_requeue. The sweep becomes meaningful only after
    modulatory-bias-selection-authority (V3-EXQ-663/643a) and ARC-065 GAP-A (the
    behavioral_diversity_isolation conversion ceiling, V3-EXQ-649/569i) clear so
    that w_313/w_314 actually move committed behaviour. Also re-confirm the
    Phase-1 separate-module placement holds at queue time -- if MECH-314 has
    migrated into GatedPolicy as a per-head bonus, the meaning of "relative
    weight" changes and the sweep design must be revised.
  implementation_phase: v3
  claim_level: mechanistic
  registered_utc: "2026-05-10"
  depends_on:
    - ARC-065     # parent architectural commitment
    - MECH-313    # noise floor
    - MECH-314    # structured curiosity
  notes: >
    Both MECH-313 (state-independent stochasticity) and MECH-314
    (state-dependent uncertainty-driven bonus) contribute to
    behavioural diversity, but the optimal mixing ratio for V3
    substrate is unknown. Too much MECH-313 produces noise that
    swamps any rule-discriminative signal ARC-062 might learn; too
    much MECH-314 produces overcommitment to information-gain at
    the expense of task reward.

    Resolution path: parametric sweep of (w_313, w_314) on SD-054
    reef substrate with ARC-062 gated-policy enabled, measuring
    behavioural-diversity (action-distribution entropy at probe
    states; CoV reef-vs-forage occupancy) and task-reward jointly.
    Pareto frontier identifies the calibration zone where both
    objectives are simultaneously high.

    PASS-as-question-resolution: Pareto frontier exists with a
    consistent (w_313, w_314) regime across seeds. FAIL-with-
    insight: no Pareto frontier exists, indicating an
    architectural defect in how the two mechanisms compose
    additively in the policy gradient.

    Placement dependency (flagged 2026-05-10 at V3-EXQ-544
    substrate-readiness review): MECH-313 currently lives as a
    separate NoiseFloor module at the e3.select() call site;
    MECH-314 lives as a separate StructuredCuriosity module at the
    same seam. Whether the policy-layer regulators consolidate
    (e.g. MECH-314 migrates inside GatedPolicy as a per-head bonus,
    motivating MECH-313 to co-locate) is OPEN pending MECH-318 /
    MECH-319 substrates. Co-located substrates compose differently
    from parallel ones at the e3.select() seam, so when this
    parametric sweep is queued, re-confirm the placement is still
    the Phase-1 separate-module instantiation; if either substrate
    has migrated, the meaning of "relative weight" changes and the
    sweep design may need to be revisited. See MECH-313 notes
    field "Phase-1 instantiation choice (NOT a settled
    architectural commitment)".
  pending_retest_after_substrate: true
  evidence_quality_note: >
    Registered 2026-05-10 from Pull 1 SYNTHESIS verdict 4 (relative-
    weight-calibration question) + Pull 4 R4 (HYBRID-naming).
    Falsifying experiment scope: V3-EXQ-543b parametric weight
    sweep arm class. Not yet run.

    [2026-05-29 autopsy V3-EXQ-605 cluster]: calibration grid untestable on V3 default substrate -- curiosity dimension fully degenerate (curiosity_scale {1x, 5x, 10x} produces identical per-cell entropy + reef_fraction); noise dimension delivers sub-threshold signal but N=1 per cell. p1_any_calibration_zone=false stays. Same two-substrate-prereq stack as Q-044 (SD-056 proximal, SD-XXX-scaffolded-sd054-onboarding deep).

    [2026-06-11 autopsy V3-EXQ-667 (confirmed, applied 2026-06-12)]: Magnitude-scale sweep (MECH-313 noise_floor_alpha + MECH-314 curiosity_weight jointly 1x/2x/4x/8x) on the 591c InfantCurriculumScheduler Phase-0 probe self-routed substrate_not_ready_requeue: healthy-seed h_pos range across arms 0.0172 < 0.05 floor; 4/5 seeds (43/44/45/46) byte-identical across the full 8x scaling -- the swept knobs have zero authority over E3 selection. Reproduces the V3-EXQ-605 inert-knobs finding on the 591c lineage. NOT evidence for "magnitude insufficient -> active Phase-0 shaping required" (the rescue fork was unadjudicable). Blocked on modulatory-bias-selection-authority (V3-EXQ-663/643a) + ARC-065 GAP-A (V3-EXQ-649). Retest V3-EXQ-667a after those clear. pending_retest_after_substrate stays. non_contributory; no confidence change (open_question / answer_state -- exempt).
  location: docs/architecture/rule_apprehension_layer.md

- id: Q-044
  title: "Are MECH-314a (striatal novelty), MECH-314b (frontopolar uncertainty-driven curiosity), and MECH-314c (learning-progress curiosity) three distinct substrates with independent contributions, or three different readings of one mechanism with different upstream inputs (e.g., novelty as a special case of uncertainty as a special case of learning progress)?"
  claim_type: open_question
  subject: policy.curiosity.subflavour_independence
  polarity: open
  status: open
  ceiling_decision: deferred
  ceiling_routing_note: >
    2026-07-20 substrate-ceiling-lifted triage: FALSE POSITIVE, parked. Q-044 asks which curiosity
    sub-flavours are behaviourally load-bearing at the selection level. It is the DISCRIMINATIVE
    question of the MECH-314 family and would, in principle, subsume separate MECH-314b / MECH-314c
    retests into one design -- but it is NOT currently answerable. Two of its three legs are
    structurally incapable of discriminating: MECH-314b and MECH-314c are Phase-1 broadcast scalars
    (see their ceiling_routing_notes), argmax-invariant by construction, so any discrimination
    attempt returns "novelty only" as an arithmetic consequence of the Phase-1 implementation rather
    than a measurement. The surviving leg is not independent either: ARM_NOVELTY_OFF collapses
    exactly onto ARM_OFF (novelty being the only per-candidate sub-flavour), so V3-EXQ-604c's C2
    novelty delta 0.402505 is NUMERICALLY IDENTICAL to load-bearing C1 -- one finding, not two.
    RE-DERIVE BRAKE FIRED (3rd; failure_autopsy_V3-EXQ-604c_2026-07-20 sec 7b). UN-PARK TRIGGER: the
    discriminative design becomes viable the moment uncertainty and learning-progress are
    per-candidate (Phase-2), tracked as the 604c amend on the ARC-065 substrate_queue entry -- not
    before. Note arc_062_rule_apprehension:GAP-H recorded this leg SATISFIED in governance_2026_06_08
    on the strength of the original 604c PASS; the 2026-07-20 re-adjudication RE-OPENED it.
    status and pending_retest_after_substrate deliberately UNCHANGED.
  live_status:
    reading: open
    as_of: 2026-07-20
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-604c_2026-07-20"
      as_of: 2026-07-20
      verdict: non_contributory/substrate_ceiling
  last_reviewed: 2026-07-20
  epistemic_category: substrate_ceiling
  what_would_answer: |
    A three-arm ablation (MECH-314a striatal-novelty OFF, MECH-314b
    frontopolar-uncertainty OFF, MECH-314c learning-progress OFF) on a substrate
    that produces DISTINGUISHABLE novelty / uncertainty / learning-progress
    signatures, reading the failure signature of each ablation. ANSWER "three
    distinct substrates -- retain all as load-bearing" if each ablation produces a
    SEPARABLE, non-overlapping degradation (314a-OFF degrades novelty-driven
    exploration, 314b-OFF degrades uncertainty-driven exploration, 314c-OFF
    degrades learning-progress tracking) that the other two do not rescue. ANSWER
    "314c discardable" if 314a+314b ablations span the failure space and 314c-OFF
    adds no distinct signature (retire 314c). ANSWER "all collapse to one
    mechanism" if the three ablations yield ONE indistinguishable signature
    (collapse to a single MECH-314). HARD PRECONDITION (the structural blocker,
    verified 2026-05-25): MECH-314a's per-candidate output must carry real
    across-K spread. Today E2 world-forward compresses K=32 diverse first-action
    candidates to identical first-step z_world (cand_world_pairwise_dist=0.0000),
    so MECH-314a's per-candidate RBF novelty -- though correctly composed into
    score_bias at e3_selector.py:737 -- emits a uniform [K] vector that is
    mathematically broadcast-equivalent, making it INDISTINGUISHABLE from the
    314b/c broadcast-scalar arms the test is meant to isolate it against
    (reproducing the bit-identical V3-EXQ-590a/604/604a null where
    curiosity_bias_abs_mean=0.0 across all arms). The ablation is vacuous until
    one of: (a) the GAP-B first-action one-hot bypass extends to ALL bias-channel
    consumers of first-step z_world (not just GatedPolicy); (b) E2 world-forward
    is fixed to preserve per-action z_world divergence at K=32; or (c) MECH-314a
    sources novelty from a per-candidate-varying signal (action-conditioned z_self
    delta / candidate-trajectory hash). SD-056 contrastive divergence (landed)
    restored candidate pairwise_dist > 0.03 but NOT selection authority -- the
    modulatory bias still has zero authority at E3.select, so confirm both
    candidate-divergence AND bias-authority hold before queuing.
  implementation_phase: v3
  claim_level: mechanistic
  registered_utc: "2026-05-10"
  depends_on:
    - MECH-314
    - MECH-314a
    - MECH-314b
    - MECH-314c
  notes: >
    Pull 1 SYNTHESIS R3 explicitly recommended NOT collapsing the
    three sub-flavours prematurely; Q-044 holds the open question
    open while the registration captures all three.

    Resolution path: three-arm ablation (314a-OFF, 314b-OFF, 314c-OFF)
    on a substrate that produces distinguishable signatures across
    novelty / uncertainty / learning-progress dimensions. SD-054
    alone may be insufficient; substrate enrichment may be needed.
    Defer empirical resolution to ARC-064-Pull-2-follow-on (the
    bottom-up rule-discovery cluster surfaces the same independence
    questions for MECH-316/317/318).

    Resolution outcomes:
      - All three independent (each ablation produces a distinct
        failure signature): retain three sub-claims as load-bearing.
      - 314c discardable (314a + 314b suffice): retire 314c, keep
        a + b.
      - All three collapse (one signature): collapse to a single
        MECH-314 with notes documenting the collapse.

    Placement dependency (flagged 2026-05-10 at V3-EXQ-544
    substrate-readiness review): MECH-314 currently lives as a
    separate StructuredCuriosity module at the e3.select() call
    site, with all three sub-flavours independently togglable. If
    MECH-314 migrates into GatedPolicy as a per-head bonus (the
    open Phase-1 placement question), the flag-set ablation surface
    moves with it, and Phase-2 per-candidate refinements for 314b
    and 314c (currently broadcast scalars across [K]) may land at
    the same time. Re-confirm placement is still Phase-1 separate-
    module before queuing this three-arm ablation; if 314 has
    migrated, the per-head ablation design supersedes the per-
    module one. See MECH-313 / MECH-314 notes fields.


    [2026-05-26 BLOCKER annotation. Per-candidate vs broadcast-scalar
    sub-flavour ablation is structurally untestable on the current
    substrate. Sibling finding doc evidence/planning/v3_exq_571_root_cause_2026-05-25.md
    (REE_assembly master a79915151b, finding session
    mech314a-propagation-root-cause-20260525T205723Z) empirically verified
    that MECH-314a per-candidate RBF novelty IS correctly composed into
    score_bias and reaches per-candidate scoring at
    ree-v3/ree_core/predictors/e3_selector.py:737, but emits a [K]-vector
    with zero across-K spread because E2 world-forward predictor
    compresses K diverse first-action candidates to identical first-step
    z_world (cand_world_pairwise_dist=0.0000 across K=32 candidates with
    2-3 unique argmax action classes). Under that upstream collapse,
    MECH-314a's per-candidate output is mathematically broadcast-
    equivalent (uniform across K), making it indistinguishable from the
    314b/c broadcast-scalar arms that Q-044 was designed to isolate it
    against. Running Q-044 now would yield the same null bit-identical
    result V3-EXQ-590a produced -- not a scientific test of the
    sub-flavour independence question. Same root cause as the
    2026-05-17 ARC-062 GAP-B autopsy (SP-CEM delivers ~5 distinct
    first-action classes but E2 world-forward compresses them to 0.22%
    of z_world magnitude); the existing GAP-B fix
    (use_first_action_onehot bypass) is scoped only to GatedPolicy and
    does not reach the bias-channel consumers of first-step z_world.
    Unblock when ANY of the following lands: (a) GAP-B first-action
    one-hot bypass extended to all bias-channel consumers of first-step
    z_world (MECH-314a curiosity, MECH-320 tonic_vigor, MECH-295 liking,
    SD-033a lateral_pfc, SD-033b ofc); (b) E2 world-forward fixed to
    preserve per-action z_world divergence at K=32 candidate scale;
    (c) MECH-314a Phase-2 novelty source migrated from z_world to a
    signal that varies per-candidate (e.g. action-conditioned z_self
    delta, or candidate-trajectory hash). See
    evidence/planning/substrate_queue.json MECH-314.pending_retests
    [V3-EXQ-590b].gated_on for the dual gate that operationalises this.]
  blocked_by:
    - e2_world_forward_first_action_preservation
  blocker_note: >
    Per-candidate vs broadcast-scalar sub-flavour ablation is structurally
    untestable while E2 world-forward compresses K diverse first-action
    candidates to identical first-step z_world (verified 2026-05-25 --
    cand_world_pairwise_dist=0.0000 across K=32). MECH-314a's per-candidate
    output is mathematically broadcast-equivalent under upstream collapse,
    indistinguishable from 314b/c arms. Same root cause as 2026-05-17
    ARC-062 GAP-B autopsy. See evidence/planning/v3_exq_571_root_cause_2026-05-25.md.
    Unblock when GAP-B first-action one-hot bypass extends to all
    bias-channel consumers of first-step z_world, OR when E2 world-forward
    is fixed to preserve per-action z_world divergence, OR when MECH-314a
    Phase-2 sources novelty from non-z_world signals.
  pending_retest_after_substrate: true
  evidence_quality_note: >
    Registered 2026-05-10 from Pull 1 SYNTHESIS R3 (recommendation
    NOT to lock prematurely). Lit support: same anchors as
    MECH-314a/b/c.
    See: evidence/literature/targeted_review_arc_065_behavioral_diversity_generation/synthesis.md

    [2026-05-29 autopsy V3-EXQ-604 + V3-EXQ-605 cluster]: untestable on V3 default substrate due to candidate-uniform z_world (no per-candidate variance from E2 forward-model output). MECH-314a per-candidate novelty bonus collapses to uniform across degenerate candidates -> ARM_1 (novelty-off) == ARM_0 (all-on) bit-identical per seed. MECH-314b/c are broadcast-by-design (Phase 1 honest-scoping caveat already on file). Two-substrate-prereq stack: SD-056 e2_action_conditional_divergence_contrastive (proximal; landed 2026-05-29 ree-v3 main 041a974; V3-EXQ-613 PASS; V3-EXQ-569a falsifier queued) AND SD-XXX-scaffolded-sd054-onboarding (deep; parent 490g-cohort autopsy commit 12f0dda773). Both must close before Q-044 retest is meaningful; SD-056 first via V3-EXQ-569a.

    [2026-06-03 governance / cluster autopsy failure_autopsy_604a-624a-630_2026-06-03] V3-EXQ-604a re-tagged non_contributory: C0 PASS (SD-056 contrastive substrate fixed the 604 monomodal collapse, pairwise_dist 0.039>0.03) but curiosity_bias_abs_mean=0.0 in every arm incl ARM_ALL_ON and selected_entropy identical across all 5 arms -- the modulatory curiosity bias has zero authority at E3.select. NOT a falsification (the test could not let the claim express itself). substrate_ceiling (selection-authority sub-type); pending_retest_after_substrate -- blocked by substrate_queue CREATE 'modulatory-bias-selection-authority'. Convergent with 624a (MECH-320) and the z_goal salience gap (603e/626a).

    [2026-07-20 governance / autopsy failure_autopsy_V3-EXQ-604c_2026-07-20] evidence_direction
    mixed -> non_contributory; epistemic_category substrate_ceiling. STRUCTURAL
    VACUITY sub-type -- a DIFFERENT mechanism from the 2026-06-03 selection-authority
    reading above, which this supersedes as the live reading. That one held the
    curiosity bias had zero AUTHORITY at E3.select (it reached the consumer but the
    consumer could not act on it). This one is upstream of authority and holds
    independently of it: Phase-1 uncertainty (314b) and learning-progress (314c) are
    GLOBAL SCALARS BROADCAST across all K candidates
    (ree-v3/ree_core/policy/structured_curiosity.py:40-41,101), and a uniform additive
    constant cannot change an argmax or a softmax sample. V3-EXQ-604c's C2 DV
    (selected_action_class_entropy) is a pure function of the argmax sequence, so
    delta == 0.0 was an ARITHMETIC IDENTITY fixed before the run -- true independently
    of whether these claims are true. The 604c PASS therefore cannot bear on them in
    either direction, and the 2026-06-08 lift of v3_pending /
    pending_retest_after_substrate (which cited that PASS) rested on a vacuous result;
    pending_retest_after_substrate is reinstated.

    NOT the intra-run substrate divergence. That defect (D3) was independently
    EXONERATED for 604c at the module-import-binding rung -- all 15 cells executed one
    substrate (f80bc236); the second hash is a disk-state recording artefact. Five
    seed-matched cross-boundary cell-pairs were bit-identical on all
    trajectory-determined quantities.

    Manifest self-confirmation (the ablations ARE plumbed; the channels DO fire --
    this is not a wiring fault): curiosity_bias_range_mean is identical to full
    precision across ALL_ON / UNCERTAINTY_OFF / LP_OFF (0.01529031 -- a broadcast
    constant cancels in a max-minus-min range) while curiosity_bias_max_abs_mean
    differs (0.240986 / 0.235738 / 0.240916 -- it does not cancel), and
    curiosity_subflavours_fired_mean moves correctly (2.866 / 1.866 / 2.000 at seed 42).

    MECH-314 and MECH-314a `supports` STAND (per-candidate novelty on e2_world_forward,
    authority gate active). Caveat recorded for future readers: 604c's C1 and its C2
    novelty leg are the SAME comparison, not two independent ones --
    ARM_NOVELTY_OFF collapses exactly onto ARM_OFF on every trajectory quantity, so
    both read 0.402505. The run yields one finding, not two.

    Re-derive brake FIRES (3 vs threshold 2): a further same-claim ablation is REFUSED.
    Build route only -- make both terms per-candidate (`amend` on ARC-065, GAP-A Phase-2
    per-candidate extension), landed REE_assembly 2241c39cdc. Note the 604c readiness
    gate r2 (curiosity_bias_range) is unknowingly a NOVELTY-ONLY gate -- the exact
    statistic in which broadcast terms vanish -- so it certifies readiness for
    MECH-314a alone.
  location: docs/architecture/rule_apprehension_layer.md

- id: Q-045
  # Assembly-state companion fields (auto-join audit, 2026-06-22; repointed same
  # day). The anti-monostrategy substrate this question depends on is being built
  # as part of the biologically-faithful basal-ganglia eligibility constitution
  # (ARC-107: MECH-448 demotion + MECH-449 Go/No-Go), NOT the F-dominance rebalance
  # route (exhausted). awaiting: ARC-107 (matches MECH-449), not the legacy
  # f_dominance_conversion_ceiling queue row (name connotes the abandoned route;
  # status blob mis-normalizes to 'built'). assembly_status in_progress: BG
  # constitution under construction.
  awaiting: ARC-107
  assembly_status: in_progress
  title: "Is MECH-313 (LC-NE tonic noise floor) a separate parameter from MECH-260 (dACC anti-recency penalty), or do they collapse into one anti-monostrategy substrate? Falsifiable with the four-arm ablation: both-OFF / 313-only / 260-only / both-ON, on SD-054 reef substrate with ARC-062 gated-policy enabled."
  claim_type: open_question
  subject: policy.anti_monostrategy.substrate_independence_lc_vs_dacc
  polarity: open
  status: open
  live_status:
    reading: open/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-687_2026-06-18
      as_of: 2026-06-18
      verdict: non_contributory/substrate_ceiling
  what_would_answer: |
    The four-arm ablation (both-OFF / 313-only / 260-only / both-ON) on SD-054
    with ARC-062 gated-policy enabled, with a behavioural-diversity ranking
    deciding whether MECH-313 (state-independent noise floor) and MECH-260
    (state-dependent dACC anti-recency) are mutually load-bearing (both-ON >
    {313-only, 260-only} > both-OFF), one dominates (a singleton indistinguishable
    from both-ON => the other is contributory-not-necessary), or DIRECTIONALLY
    COUPLED (both-ON is NOT the linear superposition of the singletons -- the
    Tervo LC->ACC asymmetry; per the 2026-05-11 lit-pull R4 this needs the 8-cell
    extension, 4-arm x 2-LC-amplitude, or an explicit LC->ACC coupling-ablation
    cell). BUT the right response on the current substrate is SUBSTRATE
    ENRICHMENT, not a verdict: the 603/603a-e chain repeatedly produced effective
    N=1 (2/3 seeds with measured_steps=0 below the 75-step FIFO warmup; z_goal
    pinned at zero) so the collapse question is structurally untestable -- a
    substrate_ceiling, not a falsification. HARD PRECONDITION the enriched
    substrate must meet before this falsifier is authorised: (1) a trained-policy
    regime that survives SD-054 enrichment (proximity_harm_scale=0.1 kill channel)
    past the FIFO warmup on >=2/3 seeds, giving a P2 measurement window with
    effective N>=2; (2) a non-trivial z_goal that actually forms under standard V3
    training (z_goal_enabled=True, drive_weight=2.0, reward-contact history
    present -- not zero-init, the 603e developmental prerequisite); (3) BOTH
    MECH-313 and MECH-260 verified operative on the surviving seeds
    (dacc.record_action() reached, FIFO non-empty -- the 603 call-path bypass
    falsified). Only once a run clears all three with live cross-arm diversity
    variance does the behavioural-diversity ranking discriminate the
    collapse-vs-distinct outcome; until then the claim stays substrate_ceiling /
    pending_retest_after_substrate and the unblocker is V3 goal-pipeline /
    scaffolded-onboarding enrichment, not a V4 deferral.
  implementation_phase: v3
  pending_retest_after_redesign: true
  claim_level: mechanistic
  registered_utc: "2026-05-10"
  depends_on:
    - MECH-313    # candidate noise-floor mechanism
    - MECH-260    # existing dACC anti-recency mechanism
    - ARC-065
  notes: >
    Both MECH-313 and MECH-260 produce anti-monostrategy pressure
    but via different mechanisms: state-independent vs state-
    dependent recency. Whether both are needed, or one suffices
    in V3, is empirically open.

    Resolution path: 4-arm ablation on SD-054 with ARC-062 enabled:
      - both-OFF: monomodal collapse expected (control)
      - 313-only: anti-monostrategy via persistent variability
      - 260-only: anti-monostrategy via recency penalty
      - both-ON: maximally diverse
    Behavioural-diversity ranking determines whether the two
    mechanisms are mutually load-bearing (both-ON > {313-only,
    260-only} > both-OFF) or one dominates ({313-only, 260-only}
    indistinguishable from both-ON, the other contributes
    contributory-not-necessary).

    Resolution outcomes:
      - Mutually load-bearing: keep both as candidate, both
        promote to provisional on PASS.
      - 313 dominates: 260 contributory-not-necessary; consider
        retiring MECH-260 in V3 substrate.
      - 260 dominates: 313 contributory-not-necessary; consider
        not implementing MECH-313 in V3.

    Placement dependency (flagged 2026-05-10 at V3-EXQ-544
    substrate-readiness review): MECH-313 currently lives as a
    separate NoiseFloor module at the e3.select() call site rather
    than as per-head temperature inside GatedPolicy. This Phase-1
    instantiation choice is NOT a settled architectural commitment;
    placement may consolidate based on outcomes of MECH-314 /
    MECH-318 / MECH-319 implementations (see MECH-313 notes). The
    placement question and the Q-045 collapse question are
    coupled: if 313 and 260 collapse here, a single combined
    regulator probably should not be split across two locations;
    if they remain distinct, that is also evidence for keeping
    313 separately placed. When this 4-arm ablation is queued,
    confirm current MECH-313 placement and re-evaluate whether
    consolidation should happen before or after the falsifier
    runs. The Q-045 outcome should feed back into the placement
    decision regardless of which way it falls.
  evidence_quality_note: >
    Registered 2026-05-10 from Pull 1 SYNTHESIS R3 (the
    relationship between LC-NE noise and dACC anti-recency is
    architecturally distinct but functionally overlapping; needs
    empirical resolution).
    See: evidence/literature/targeted_review_arc_065_behavioral_diversity_generation/synthesis.md

    [2026-05-29 autopsy V3-EXQ-603a/b/c followon cluster]: Independence/coupling question untestable across the four-run chain (predecessor + three followons) -- effective N=1 in every iteration. Confirms substrate_ceiling category. Pending retest after SD-XXX-scaffolded-sd054-onboarding lands and a P2 measurement window with >=2/3 seeds becomes reachable. Lit-pull R5 design-gate (Kennerley temporal horizon) and R4 (8-cell extension) remain on hold behind the substrate fix.


    [2026-05-11 Q-045 dedicated lit-pull complete. 5 entries, lit_conf 0.9,
    evidence_quadrant plausible_unproven. R1 verdict: COUPLED-NOT-COLLAPSED --
    Tervo et al. 2014 (Cell, DOI 10.1016/j.cell.2014.08.037) shows LC-NE input
    drives ACC stochastic-mode switching; the two substrates are coupled at
    the circuit level, neither fully independent nor fully collapsed. R2:
    substrate-distinctness CONFIRMED -- Scholl 2015 (DOI
    10.1523/JNEUROSCI.0396-15.2015, recovers the MECH-260 anchor flagged
    missing in arc_065 Pull 1) + Kennerley 2006 (DOI 10.1038/nn1724) show
    dACC does content-selective action-outcome history integration that a
    noise-floor primitive cannot reproduce. R3: Yu & Dayan 2005 (DOI
    10.1016/j.neuron.2005.04.026) gives Bayesian-theoretic support for
    computational distinctness. R4 LOAD-BEARING: current 4-arm design
    insufficient; needs extension to 8-cell (4-arm x 2-LC-amplitude) OR
    addition of LC -> ACC coupling ablation to expose Tervo asymmetry --
    both-ON should NOT be linear superposition of singletons. R5: SD-054
    substrate-readiness for multi-trial outcome dependencies must be
    verified BEFORE 4-arm authorisation (Kennerley-style across-trial
    integration only emerges with sufficient temporal horizon -- a single-
    tick outcome substrate cannot dissociate MECH-260 from MECH-313 in the
    Kennerley sense). Resolution outcome categories should be EXTENDED to
    include "DIRECTIONALLY COUPLED" as a fourth category alongside the
    three originally registered (mutually load-bearing / 313-dominant /
    260-dominant).
    See: evidence/literature/targeted_review_q_045/synthesis.md]

    [2026-05-24 autopsy V3-EXQ-603]: NON-CONTRIBUTORY -- MECH-260 non-operative
    throughout (inference call-path bypass; dacc.record_action() never called;
    FIFO permanently empty). Substrate-independence question untestable when one
    mechanism is structurally absent. Lit-pull R5 design-gate (Kennerley temporal-
    horizon, SD-054 substrate-readiness) also unresolved. [2026-05-25] V3-EXQ-603a:
    MECH-260 now operative but 2/3 seeds had measured_steps=0 (episodes < 75-step
    FIFO warmup); temporal gate failure makes result uninterpretable. All V3-EXQ-603
    and V3-EXQ-603a runs overridden to non_contributory. Pending: V3-EXQ-603b
    with call path repaired and temporal horizon verified.

    [2026-05-26 governance applies V3-EXQ-603b post-Fix-B autopsy (2026-05-25)
    + remaining 603 run 111759Z]: NON-CONTRIBUTORY. Third measurement_gap in 603
    chain. With effective N=1 (seeds 42/44 produced measured_steps=0 again under
    Fix B hazard_harm 0.02; proximity_harm_scale=0.1 was the untouched dominant
    kill channel), Q-045's MECH-313-vs-MECH-260 collapse question is structurally
    untestable from these runs. Both mechanisms verified operative on the
    surviving seed (seed 43); per-claim deltas all sub-threshold. 603 (111759Z) +
    603b (071458Z) overridden to non_contributory + epistemic_category=measurement_gap.
    Pending retest: V3-EXQ-603c with P0/P1 training phase. If 603c also produces a
    1/3-survivor manifest, route Q-045 to epistemic_category=substrate_conditional
    pending a V4 substrate that makes untrained-inference survival on SD-054
    enrichment a clean prerequisite.

    [2026-05-27 governance applies V3-EXQ-603c (P0/P1 phased training, supersedes
    603b)]: NON-CONTRIBUTORY across all three claims (Q-045 / MECH-313 / MECH-260).
    Summary: p2_cell_count=4, aborted_cells=8 (8 of 12 cells aborted Fix D pre-P2
    survival gate; median ep_length over last 10 P1 episodes < 75-step FIFO
    warmup threshold). Even with P0 (E1+E2 warmup on easy env) + P1
    (consolidation on target env), the trained policy does not survive
    SD-054 enrichment long enough in 2/3 cell arms to sustain measurement.
    Q-045 routed to epistemic_category=substrate_ceiling. The claim's
    test bed (SD-054 reef + ARC-062 gated-policy + the 4-arm anti-
    monostrategy ablation) is V3-scoped by design; the missing piece is
    V3 substrate enrichment + training-regime work (the goal-pipeline
    substrate gap the 591 autopsy cluster names), not a V4 prerequisite.
    Required unblocker is some combination of: (1) goal-pipeline
    substrate enrichment so non-trivial z_goal develops under standard
    V3 training; (2) a trained-policy regime that is part of the test
    bed by construction (longer P1 budget, scaffolded curriculum that
    survives the SD-054 enrichment proximity-harm-scale=0.1 kill channel,
    or a phased SD-054 onboarding); (3) probable joint fix with the
    ARC-046 InfantCurriculumScheduler exit-gate substrate fix already
    in substrate_queue. Cluster-absorbed into failure_autopsy_V3-EXQ-591_
    2026-05-27 (member 4 of substrate-uniform z_goal-zero family: V3-EXQ-
    540 / 603 chain / 590a / 591). pending_retest_after_substrate
    retained. Both MECH-313 and MECH-260 similarly substrate_ceiling
    via Q-045's dependency. [2026-05-27 governance correction: prior
    cycle's plan-of-record line had said route to substrate_conditional
    pending V4 -- overridden. If the substrate fix the V3 cluster needs
    is itself V3-scoped work (which the 591 autopsy's favoured
    'substrate_enrichment' reading affirms), then by definition it is
    V3 work, not V4 deferral; the correct category is substrate_ceiling
    (V3-tractable in principle, substrate at default config too coarse).]

    [2026-06-01T05:50Z governance cycle, V3-EXQ-621a substrate-readiness PASS]:
    Scaffolded SD-054 onboarding substrate (landed 2026-05-31 IGW-20260531-029)
    cleared substrate-readiness diagnostic on V3-EXQ-621a (PASS via C1 6/12
    cells + C3 cascade_consequential=true via ARM_2 approach_commit lift;
    z_goal C2 floor still unmet, motivating staged V3-EXQ-622 decomposition).
    Reclassified non_contributory per substrate-readiness diagnostic pattern
    (matches MECH-295 / SD-049 precedents): substrate-readiness PASS does not
    by itself constitute load-bearing behavioural evidence. Behavioural cluster
    validation V3-EXQ-603d / 591b queueable. pending_retest_after_substrate
    flag retained until behavioural successor returns. Predecessor V3-EXQ-621
    (recovered manifest from runner emit_outcome misclassification) marked
    superseded by V3-EXQ-621a per CLAUDE.md supersession rule.

    [2026-06-01T17:56Z governance cycle, confirmed failure_autopsy_V3-EXQ-603d]:
    First behavioural retest on the IMPLEMENTED scaffolded_sd054_onboarding
    substrate. FAIL non_contributory across Q-045 / MECH-313 / MECH-260.
    LOAD-BEARING finding: ScaffoldedSD054OnboardingScheduler._train_episode and
    603d's bespoke P2 never call agent.update_z_goal, so z_goal stays zero-init
    for every step of every arm; the C4 z_goal=0 / SUBSTRATE_FAILURE label is a
    Class-1 harness/wiring artifact (same fault class as V3-EXQ-626), NOT
    substrate-ceiling falsification. This is the true content of 621a's "z_goal
    feeding not yet wired" + the 591-triage prereq (4). P1 survival gate failed
    2/3 seeds (42,44; only 43 to P2; p2_cell_count 5/15, effective N=1),
    reproducing the 603-chain seed-fragility but confounded by reduced budget
    (P0/P1=30/30 vs 603c 100/50) + the inert goal pipeline. The
    MECH-313-vs-MECH-260 collapse question remains structurally untestable at
    effective N=1 with z_goal=0. Routing: implement-substrate AMEND on
    scaffolded_sd054_onboarding (wire update_z_goal + Stage-0 positive-control
    gate; UPSTREAM of the 622 anneal-rate amend), then re-issue 603d -> 603e at
    restored budget. substrate_queue amended (action=amend, priority 1, 603d
    failure_record appended). pending_retest_after_substrate retained.

    [2026-06-02 implement-substrate AMEND landed -- scaffolded_sd054_onboarding
    update_z_goal wiring]: ScaffoldedSD054OnboardingScheduler now calls
    agent.update_z_goal(benefit, drive) in _train_episode (P1) + _eval_episode
    (P2), mirroring the goal_stream_stages_sd054 reference runner; P0 left
    goal-frozen by design. Stage-0 positive-control contracts added (a z_goal=0
    scheduler is now structurally unshippable). The V3-EXQ-625b monostrategy
    failure record was folded into the same substrate_queue entry (plausibly
    downstream of the same inert goal pipeline). TWO-PART FIX: the 603d FAIL
    config also omitted z_goal_enabled=True (goal_state was None -> update_z_goal
    early-returns); the V3-EXQ-603e validation MUST set z_goal_enabled=True +
    drive_weight=2.0 (as the working V3-EXQ-622 does). Status unchanged
    (substrate_ceiling / pending_retest_after_substrate retained): this is a
    wiring fix, validation pending V3-EXQ-603e (restored P0/P1=100/50 budget).
    substrate_queue status amend_pending -> amend_implemented_pending_validation.

    [2026-06-03 governance / cluster autopsy failure_autopsy_V3-EXQ-603e-626a-622_2026-06-03] V3-EXQ-603e re-tagged non_contributory: goal-pipeline wiring closed+verified (update_z_goal AMEND deb24cc; contract test_c6_stage0_positive_control confirms z_goal forms under forced input) but z_goal_norm_peak=0.0 on ALL 15 cells ecologically at restored budget -- downstream of TWO coupled prerequisites: survival/foraging-competence (2/3 seeds never competent) + benefit-input starvation (P2 hazard_food_attraction=0.7). Discovered developmental prerequisite (goal reps require reward-contact history; Berridge), NOT a falsification: claim never under fair test (effective N=1). stays substrate_ceiling/pending_retest_after_substrate; substrate_queue AMEND 'scaffolded_sd054_onboarding' then re-issue 603e->603f. EXQ-445h remains sole valid MECH-260 support.

    2026-07-30 (governance-apply, failure_autopsy_batch-687a-707c-840-748a-833-842-810b-673-614-798afail
    CONFIRMED): V3-EXQ-687a -- all 4 arms failed committed_sufficiency at the absolute floor, not a
    discrimination result; re-derive brake exempt. non_contributory/competence_implementation_gap.
    Q-045 is now gated on the same MECH-457 competence-floor substrate as the rest of the
    committed-behaviour campaign (MECH-260/MECH-313 co-tagged) -- not independently re-queueable
    until that substrate clears. Stays open/substrate_ceiling.

  epistemic_category: substrate_ceiling
  ceiling_decision: deferred
  ceiling_routing_note: "GOV-CEIL-1 keep-parked (NOT demoted) 2026-07-09: flagged with 4 substrate_ceiling hits, but this is an open_question (status: open) -- GOV-CEIL-1's demotion semantics (floor status to candidate; carry an inert-mechanism null) do not map, since an open question asserts no mechanism to call inert and open->candidate is a category error. Its dependents MECH-313 and MECH-260 were demoted in this same 2026-07-09 pass and already carry the ceiling-exhaustion signal. Parked to stop re-flagging; the collapse-vs-distinct question stays substrate-blocked pending V3 goal-pipeline / scaffolded-onboarding enrichment. [enum-comment migrated to field 2026-07-18] ceiling_decision: GOV-CEIL-1 keep-park 2026-07-09 (user-adjudicated; demotion semantics N/A for an open_question)"
  pending_retest_after_substrate: true
  location: docs/architecture/rule_apprehension_layer.md

- id: ARC-064
  title: "bottom_up_behavioral_pattern_extraction_pathway (hippocampal_CLS_bi_pathway + dorsolateral_striatum_chunking + OFC_cognitive_map analog). Architectural commitment: rule discovery from observed-behaviour clustering is logically and architecturally distinct from rule application from instructed/external context (ARC-062 top-down rule-apprehension). Where ARC-062 receives a context cue and selects a discriminative policy mode, ARC-064 receives an observed-behaviour stream and extracts cross-episode regularities, compressed behavioural patterns, and rule-state abstractions. Both pathways presuppose ARC-065 behavioural diversity generation upstream. Multi-substrate distributed instantiation: hippocampal Complementary Learning Systems bi-pathway (Schapiro et al. 2017), dorsolateral striatum action chunking (Smith & Graybiel 2013/2016), OFC cognitive-map state abstraction (Wilson et al. 2014; Schuck et al. 2016)."
  claim_type: architectural_commitment
  subject: rule_discovery.bottom_up_behavioral_pattern_extraction_pathway
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-064@2026-05-10T12:24:30Z"
      as_of: 2026-05-10
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: "2026-05-10"
  depends_on:
    - ARC-065     # behavioural diversity generation -- precondition; clustering needs heterogeneous behaviour to cluster across
    - MECH-309    # diagnostic claim (rule-apprehension as logical necessity); ARC-064 is its bottom-up counterpart to ARC-062's top-down
  epistemic_category: substrate_conditional
  what_would_answer: |
    A two-arm learning-curve comparison on a no-external-cue substrate where
    the rule must be discovered from the observed-behaviour stream, matched
    by a second run on a cued-control substrate where the rule is
    externally signalled. This operationalises the design the claim's own
    functional_restatement already specifies.

    DESIGN: ARM_0 = ARC-062-alone (top-down gated-policy + learned context
    discriminator, the existing V3 rule-apprehension instantiation). ARM_1
    = ARC-062 + ARC-064 children (at minimum the load-bearing subset of
    MECH-316 cross-episode regularity extraction, MECH-317 behavioural-
    pattern compression, MECH-318 rule-state abstraction). Substrate
    candidates named in the claim's own evidence_quality_note: SD-054
    extended with hidden cross-episode structure (a transition/reward
    contingency that varies between two regimes with NO externally
    observable cue distinguishing them), or a serial-reaction-time-task
    analog with hidden sequence structure. Run BOTH arms on BOTH the
    no-cue substrate and a matched cued-control variant (same substrate,
    rule signalled by an explicit external cue).

    CONFIRMED if, on the no-cue substrate, ARM_1 produces a measurably
    better learning curve than ARM_0 (lower episodes-to-criterion, higher
    terminal reward, or earlier behavioural divergence between the hidden
    regimes) AND, on the cued-control substrate, ARM_1 gives no material
    benefit over ARM_0 (consistent with "ARC-062 alone suffices when the
    cue is external"). This dissociation -- large ARC-064 contribution
    only where the rule must be discovered -- is what would establish
    bottom-up rule discovery as architecturally distinct, load-bearing
    functionality rather than a redundant restatement of the top-down
    pathway.

    FALSIFIED if, on the no-cue substrate, ARM_1 does not outperform ARM_0
    beyond noise (adding MECH-316/317/318 gives no measurable
    learning-curve advantage even when the rule genuinely must be
    discovered from behaviour alone), OR if ARM_0 alone already solves the
    no-cue task at a rate matching ARM_1 (existing top-down/updater
    machinery already suffices for what looks like "discovery," so the
    claimed bottom-up/top-down architectural distinction is not
    load-bearing). Either result argues ARC-064 does not name a separate
    architectural function from ARC-062.

    NON-DEGENERACY PRECONDITION (all required before either verdict is
    interpretable):
    (1) The no-cue substrate must not be trivially solvable by chance or
    rote memorisation independent of any rule-discovery machinery -- ARM_0
    (or a null/random-policy control) must not already saturate
    near-ceiling performance on the no-cue task for reasons unrelated to
    rule discovery (e.g. an episode horizon too short for the hidden
    regime to matter, or a reward structure solvable by a fixed policy
    regardless of regime).
    (2) The comparison must clear the SAME structural conversion-ceiling
    bottleneck already identified on the sibling ARC-062/MECH-309
    falsifier line (MECH-439 F-dominance / E3 commit-selection-authority
    coupling -- V3-EXQ-654-series through 700b, most recently reconfirmed
    unresolved by V3-EXQ-851, 2026-08-01): whatever bottom-up-derived rule
    signal MECH-316/317/318 mint must be shown to actually reach committed
    action (readiness gates analogous to the ARC-062 GAP-B C1 gates), not
    merely exist in representation. Absent that check, a null ARM_1 result
    is confounded with the known selection-authority washout rather than
    informative about the top-down/bottom-up architectural question -- the
    same failure mode V3-EXQ-606 (below) ran into for MECH-318 alone.
    (3) MECH-316/317/318 must exist as at least minimally functional
    substrate before the comparison can run at all.

    CONFIRMED CURRENT STATE (2026-08-07 digestion pass): none of the three
    preconditions are met. MECH-316, MECH-317, and MECH-318 all remain
    candidate/v3_pending with zero built substrate; their most recent
    live_status.as_of is 2026-07-11 with no evidence beyond the original
    2026-05-10 literature registration, except MECH-318, which has one
    directly-relevant but inconclusive prior attempt: V3-EXQ-606
    (2026-05-21, "ARC-064 GAP-I MECH-318 multi-rule empirical gate") ran
    C3 rule_state_active PASS (wiring live) but C1/C2 behavioural
    cross-regime tests failed on 2/3 seeds, self-routed as premature --
    fired before its resume_condition (V3-EXQ-543k contributory PASS) was
    met. That condition still is not met (543k itself is FAIL/mixed).
    MECH-309 (ARC-064's other direct dependency) remains
    candidate/v3_pending/substrate_ceiling as of its most recent entry
    (2026-08-01, V3-EXQ-851 autopsy); the decisive V3-EXQ-700b test
    (2026-06-24) itself returned FAIL/non_contributory
    (precondition-unmet, self-routed substrate_not_ready_requeue) rather
    than resolving the conversion-ceiling question either way. ARC-064's
    OTHER dependency, ARC-065, HAS converted (substrate_ceiling lifted
    2026-06-17 on V3-EXQ-569i PASS; status stable, v3_pending false), so
    upstream diversity is no longer a blocker -- what remains open is (a)
    building MECH-316/317/318 and (b) the shared conversion-ceiling
    bottleneck in (2), which any ARC-064 behavioural test will likely also
    need to clear. No V3-EXQ design work beyond the single premature
    V3-EXQ-606 attempt has been queued for ARC-064.
  functional_restatement: >
    ARC-064 names the architectural slot for bottom-up rule discovery:
    extracting cross-episode regularities, compressed behavioural
    patterns, and rule-state abstractions FROM the observed-behaviour
    stream itself, rather than receiving an external context cue.
    Where ARC-062 starts with a context label and selects a
    discriminative policy mode, ARC-064 starts with raw behavioural
    data and clusters / chunks / abstracts toward a rule-state
    representation.

    The two pathways are logically and architecturally distinct:
      - ARC-062 top-down: cue -> context discriminator -> policy mode
      - ARC-064 bottom-up: observed behaviour -> regularity extraction
        -> compressed patterns -> rule-state abstraction
    Both can coexist; both presuppose ARC-065 behavioural diversity
    generation upstream (top-down has nothing to discriminate without
    diverse behaviour to choose between; bottom-up has nothing to
    cluster without diverse behaviour to cluster).

    Multi-substrate distributed instantiation, biology-anchored. Pull
    2 SYNTHESIS verdicts identified three converging substrates:
      (a) Hippocampal Complementary Learning Systems bi-pathway
          (Schapiro et al. 2017): monosynaptic CA1 path supports
          fast statistical learning and pattern separation;
          trisynaptic path supports slow consolidation. The
          monosynaptic side is the cross-episode regularity
          extractor. REE child claim: MECH-316
          cross_episode_regularity_extraction.
      (b) Dorsolateral striatum action chunking (Smith & Graybiel
          2013/2016 sequence-of-actions consolidation;
          Bacon/Harb/Precup 2017 option-critic computational
          analog; Martiros & Graybiel 2018 already in REE index).
          Compresses repeated behavioural patterns into options.
          REE child claim: MECH-317 behavioural_pattern_compression.
      (c) OFC cognitive-map state abstraction (Wilson, Takahashi,
          Schoenbaum & Niv 2014; Schuck et al. 2016 OFC encodes
          task state). Abstracts the rule-state itself as a latent
          variable in the agent's representation. REE child claim:
          MECH-318 rule_state_abstraction_substrate.

    Architectural commitment: discovery-from-clustering is a
    distinct architectural function from application-from-instruction.
    Both ARC-062 and ARC-064 may be needed; it is empirically open
    which is more load-bearing on V3 substrates and which surfaces
    as primary in V4 clinical / failure-mode tests (cf. ARC-063
    falsifiable predictions b).

    Falsifiable architectural prediction: on substrates where the
    rule must be discovered (no external cue, only observed
    behaviour), ARC-064 children should produce learning curves
    that ARC-062 alone cannot match. On substrates where the rule
    is cued externally, ARC-062 alone should suffice and ARC-064
    contribution should be minimal (additive without much benefit).
  evidence_quality_note: >
    Registered 2026-05-10 from Pull 2 lit-pull SYNTHESIS verdict 1
    (PROMOTE-AS-SEPARATE-CLUSTER, conf 0.84) + Pull 4 R4 HYBRID-
    naming + Pull 3 R5 absorption verdicts (MECH-315 absorbed into
    MECH-292/293 ghost-goal substrate; not registered as a separate
    ARC-064 child).
    Lit anchors:
      - Schapiro et al. 2017 (Phil Trans R Soc B) -- CLS bi-pathway
        (monosynaptic statistical learning vs trisynaptic
        consolidation)
      - Smith & Graybiel 2013, 2016 (Curr Opin Neurobiol; Neuron) --
        habit consolidation in dorsolateral striatum / action
        chunking
      - Wilson, Takahashi, Schoenbaum & Niv 2014 (Neuron) -- OFC
        cognitive map
      - Schuck et al. 2016 (Neuron) -- OFC state abstraction
      - Bacon, Harb & Precup 2017 (AAAI) -- option-critic
        (computational analog of MECH-317)
      - Stachenfeld, Botvinick & Gershman 2017 (Nat Neurosci) --
        successor representation in hippocampus (computational
        analog of MECH-316)
      - Wang et al. 2018 (Nat Neurosci) -- prefrontal as meta-RL
        (computational analog of MECH-318)
      - Davachi 2015 (J Neurosci) -- statistical learning in
        hippocampus
    See: evidence/literature/targeted_review_arc_064_bottom_up_rule_discovery/synthesis.md

    Pull 4 R4 verdict assigned ARC-064 to HYBRID-naming convention
    because the REE-internal architectural-function name
    (bottom_up_behavioral_pattern_extraction_pathway) is co-equally
    informative with the literature anchors (CLS bi-pathway +
    dorsolateral striatum chunking + OFC cognitive map). The
    hybrid title makes the cluster discoverable under both REE-
    native and literature-search.

    Not yet experimentally tested. Falsifying experiments require
    a substrate where the rule must be discovered (no external
    cue) -- candidates: a temporal-statistical-regularity task in
    SD-054 extended with hidden context structure; a
    serial-reaction-time-task analog. V3-EXQ design deferred to
    a separate session.
  notes: >
    HYBRID-naming convention (Pull 4 R4): the title carries both
    the REE-internal architectural name and the literature
    anchors, making the cluster machine-grep-able under both
    REE-native search ("bottom-up rule discovery") and
    literature-search ("CLS bi-pathway", "successor
    representation", "option-critic", "OFC cognitive map").

    Cross-reference to ARC-062: ARC-062 top-down + ARC-064
    bottom-up are complementary, both presupposing ARC-065
    diversity generation upstream. The full chain is:
      ARC-065 (diversity) -> {ARC-062, ARC-064} (rule machinery)
                          -> SD-029 (monomodal-collapse measurement)
    Whether ARC-062 alone suffices on V3 substrates is the V3-
    EXQ-543b/c question; whether ARC-064 alone could suffice (and
    if so, on which substrates) is a separate downstream question.

    Pull 3 R5 absorption: MECH-315 candidate (proposal-diversity
    channel via hippocampal trajectory sampling, Pfeiffer & Foster
    2013) is ABSORBED into MECH-292 ranked ghost-goal bank +
    MECH-293 awake ghost-goal probes substrate per Pull 2 R5
    verdict 2026-05-10 (conf 0.74). Not registered as an ARC-064
    child; the proposal-diversity function it would have provided
    is already substantively covered by the existing ghost-goal
    substrate.
  location: docs/architecture/rule_apprehension_layer.md

- id: MECH-316
  title: "cross_episode_regularity_extraction (episodic_RL_successor_representation_CLS_monosynaptic_analog). A mechanism for extracting cross-episode statistical regularities from observed behaviour streams, producing a representation that supports prediction of future state-occupancy distributions. Biological substrate: hippocampal monosynaptic CA1 path (Schapiro et al. 2017 CLS bi-pathway); statistical learning in hippocampus (Davachi 2015). Computational analog: successor representation (Stachenfeld et al. 2017 Nat Neurosci). Sub-claim of ARC-064 bottom-up rule discovery."
  claim_type: mechanism_hypothesis
  subject: rule_discovery.cross_episode_regularity_extraction
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-316@2026-05-10T12:24:30Z"
      as_of: 2026-05-10
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-05-10"
  depends_on:
    - ARC-064     # parent architectural commitment
    - ARC-065     # grandparent (diversity generation upstream)
    - MECH-269    # V_s monostrategy: representational discriminability is precondition for cross-episode clustering
  functional_restatement: >
    MECH-316 names the cross-episode regularity-extraction substrate
    of ARC-064 bottom-up rule discovery. The biology: hippocampal
    monosynaptic CA1 path (Schapiro et al. 2017) supports fast
    statistical learning -- pattern co-occurrence statistics across
    multiple episodes are encoded into a representation that
    supports prediction of future state-occupancy distributions.

    Computational analog: successor representation
    (Stachenfeld et al. 2017). The SR encodes for each state s the
    discounted future-occupancy distribution under the current
    policy; learning the SR gives the agent a substrate where
    cross-episode regularities are explicit features.

    Distinct from MECH-317 (compression into options) and MECH-318
    (rule-state abstraction): MECH-316 produces the raw statistical
    regularity; MECH-317 chunks repeated patterns into reusable
    options; MECH-318 abstracts the rule-state itself as a latent
    variable.

    Falsifiable: on a substrate with hidden cross-episode statistical
    structure (e.g., a transition matrix that varies between two
    rule regimes), MECH-316-ON agents should learn a representation
    whose state-occupancy predictions diverge between the two
    regimes; MECH-316-OFF agents (or successor-representation-
    ablated) should fail to capture the divergence and behave
    monomodally across both regimes.
  evidence_quality_note: >
    Registered 2026-05-10 from Pull 2 SYNTHESIS verdicts. Lit
    anchors:
      - Schapiro et al. 2017 (Phil Trans R Soc B) -- CLS bi-pathway,
        monosynaptic side as statistical-learning substrate
      - Davachi 2015 (J Neurosci) -- hippocampal statistical
        learning review
      - Stachenfeld, Botvinick & Gershman 2017 (Nat Neurosci) --
        successor representation as hippocampal substrate
      - Momennejad et al. 2017 -- successor-representation human
        evidence
    See: evidence/literature/targeted_review_arc_064_bottom_up_rule_discovery/synthesis.md

    Pull 4 R4 verdict: HYBRID-naming (MECH-316 cross_episode_
    regularity_extraction is the REE-internal architectural-
    function name; episodic_RL_successor_representation_CLS_
    monosynaptic_analog is the literature anchor list).

    Not yet experimentally tested. Falsifying experiment requires
    a hidden-structure substrate; not currently in V3 substrate
    set. Substrate-design EXQ deferred.
  location: docs/architecture/rule_apprehension_layer.md

- id: MECH-317
  title: "behavioural_pattern_compression (option_formation_striatal_chunking_analog). A mechanism for compressing repeated behavioural patterns into reusable options / action-chunks, producing a representation that supports temporally-extended action selection. Biological substrate: dorsolateral striatum sequence consolidation (Smith & Graybiel 2013/2016; Martiros & Graybiel 2018). Computational analog: options framework / option-critic (Bacon, Harb & Precup 2017). Sub-claim of ARC-064 bottom-up rule discovery."
  claim_type: mechanism_hypothesis
  subject: rule_discovery.behavioural_pattern_compression
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-317@2026-05-10T12:24:30Z"
      as_of: 2026-05-10
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-05-10"
  depends_on:
    - ARC-064     # parent architectural commitment
    - ARC-065     # grandparent
  functional_restatement: >
    MECH-317 names the behavioural-pattern-compression substrate
    of ARC-064. The biology: dorsolateral striatum (DLS)
    consolidates repeated sequences-of-actions into chunks
    (Smith & Graybiel 2013/2016) -- start-stop-bracketed action
    sequences become a single reusable unit at the level of
    striatal representation.

    Computational analog: options framework (Sutton et al. 1999
    HRL) and option-critic (Bacon, Harb & Precup 2017). An option
    is a temporally-extended action with an initiation set, a
    policy, and a termination condition. Option discovery is
    the process by which repeated behavioural patterns are
    compressed into options; option-critic provides a gradient-
    based discovery method.

    Distinct from MECH-316 (cross-episode regularities) and
    MECH-318 (rule-state abstraction): MECH-316 produces the raw
    statistics; MECH-317 detects which behavioural patterns are
    compressible (repeated across many instances) and creates
    reusable options; MECH-318 abstracts the higher-level rule
    state under which those options are deployed.

    Falsifiable: on a substrate with repeated behavioural sequences
    (e.g. SD-054 reef-foraging requires a stereotyped approach-
    feed-retreat sequence at each reef patch), MECH-317-ON agents
    should produce stereotyped action chunks at reef patches with
    chunk boundaries detectable in action-distribution entropy
    troughs; MECH-317-OFF agents should produce unchunked action
    streams without boundaries.
  evidence_quality_note: >
    Registered 2026-05-10 from Pull 2 SYNTHESIS verdicts. Lit
    anchors:
      - Smith & Graybiel 2013 (Curr Opin Neurobiol); 2016 (Neuron) --
        habit consolidation / action chunking in DLS
      - Martiros & Graybiel 2018 (already in REE index) -- direct
        empirical
      - Sutton, Precup & Singh 1999 (Artif Intell) -- options
        framework
      - Bacon, Harb & Precup 2017 (AAAI) -- option-critic gradient
        method
      - Eysenbach et al. 2018 (ICLR) -- diversity-is-all-you-need
        skill discovery (related)
      - Solway et al. 2014 (PLOS Comput Biol) -- optimal behavioural
        hierarchies
    See: evidence/literature/targeted_review_arc_064_bottom_up_rule_discovery/synthesis.md

    Pull 4 R4 verdict: HYBRID-naming (option_formation +
    striatal_chunking is the literature-anchor list paired with
    REE-internal name).

    Not yet experimentally tested. Falsifying experiments are
    SD-054-tractable but require new instrumentation (action-
    chunk boundary detection); EXQ design deferred.
  location: docs/architecture/rule_apprehension_layer.md

- id: MECH-318
  title: "rule_state_abstraction_substrate (meta_RL_recurrent_task_state_representation). A mechanism for abstracting the higher-level rule-state itself as a latent variable in the agent's representation, supporting policy adaptation that depends on inferred rule-context rather than only observable cue. Biological substrate: OFC cognitive map (Wilson, Takahashi, Schoenbaum & Niv 2014; Schuck et al. 2016 OFC encodes task state). Computational analog: meta-RL recurrent task-state representation (Wang et al. 2018 prefrontal-as-meta-RL; Duan et al. 2016 RL^2; Botvinick et al. 2019 review). Sub-claim of ARC-064 bottom-up rule discovery. ABSORPTION CHECK 2026-05-10: VERDICT (B) PARTIALLY ABSORBED. Within-V3 functional weight is borne by SD-033a LateralPFCAnalog rule_state buffer (W1 EMA recurrence + W3 substrate-readiness + W4 downstream bias) plus ARC-062 Phase 1 gated_policy multi-stream context discriminator (W3 per-tick rule-context discrimination + W4 score-aggregation training pressure), with ARC-062 Phase 3 GAP-C planned wiring closing the loop between the two. Two residual gaps remain: W2 (multi-task training distribution -- blocked on multi-rule-context substrate, SD-054 single-context insufficient) and W5 (cross-episode hidden-state continuity -- likely V4-scope; SD-033a per-episode reset is architecturally load-bearing for the rest of the agent state machine). Empirical retire-vs-promote verdict deferred to V3-EXQ-543c-successor on multi-rule-context substrate. See REE_assembly/docs/architecture/mech_318_absorption_check.md."
  claim_type: mechanism_hypothesis
  subject: rule_discovery.rule_state_abstraction_substrate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-606_2026-05-29
      as_of: 2026-05-29
      verdict: non_contributory/substrate_ceiling
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-05-10"
  depends_on:
    - ARC-064     # parent architectural commitment
    - ARC-065     # grandparent
    - MECH-316    # cross-episode regularities feed the rule-state abstraction
    - MECH-317    # compressed patterns may be inputs to the rule-state abstraction
  functional_restatement: >
    MECH-318 names the rule-state-abstraction substrate of ARC-064.
    The biology: orbitofrontal cortex encodes task state (Wilson,
    Takahashi, Schoenbaum & Niv 2014; Schuck et al. 2016) -- OFC
    units track abstract task-context variables that are not
    directly observable in the sensory stream but that condition
    the appropriate policy.

    Computational analog: meta-RL recurrent task-state representation
    (Wang et al. 2018; Duan et al. 2016 RL^2). A recurrent network
    trained across many task instances learns to encode the task
    identity in its hidden state, allowing policy adaptation
    within an episode.

    Distinct from MECH-316 (cross-episode statistics) and MECH-317
    (option compression): MECH-318 abstracts the rule-state itself,
    not the regularities it produces or the action-chunks it
    licenses. The rule-state abstraction is what allows the agent
    to ask "what rule-context am I in?" rather than "what is the
    current observation?"

    Provisional registration. NOTE in the title and per Pull 2 R3:
    MECH-318 may be absorbed into the latent stack itself when the
    meta-RL collapse is operationalised. The candidate
    z_world / z_self / z_harm latent stack already supports a
    rule-context variable in principle (the recurrent state of
    the agent encodes task identity across episodes); whether a
    separate substrate is needed depends on V3-EXQ-543c follow-on
    measurement. Flag: registration_provisional_pending_meta_rl_
    absorption_check.

    Falsifiable: on a multi-rule-context substrate (e.g. SD-054
    extended with two reef configurations that require different
    foraging strategies), MECH-318-ON agents should show within-
    episode adaptation timing at rule-switch points (faster
    adjustment to new rule-context); MECH-318-OFF (or recurrent-
    state-ablated) agents should adapt only at the slow-learning
    timescale (across episodes, not within).
  evidence_quality_note: >
    Registered 2026-05-10 from Pull 2 SYNTHESIS verdicts.
    Lit anchors:
      - Wilson, Takahashi, Schoenbaum & Niv 2014 (Neuron) -- OFC
        cognitive map
      - Schuck et al. 2016 (Neuron) -- OFC encodes task state
      - Wang et al. 2018 (Nat Neurosci) -- prefrontal as meta-RL
      - Duan et al. 2016 (NeurIPS) -- RL^2 recurrent meta-RL
      - Botvinick et al. 2019 (Trends Cogn Sci) -- review of
        prefrontal meta-RL
    See: evidence/literature/targeted_review_arc_064_bottom_up_rule_discovery/synthesis.md

    Pull 4 R4 verdict: HYBRID-naming
    (rule_state_abstraction_substrate + meta_RL_recurrent_task_state_
    representation).

    Absorption-check 2026-05-10 (VERDICT B partial): the architectural
    absorption check in REE_assembly/docs/architecture/mech_318_
    absorption_check.md audited three candidate substrates -- E1 LSTM
    (E1DeepPredictor + ContextMemory), SD-033a LateralPFCAnalog
    rule_state buffer with MECH-261 mode-conditioned EMA / MECH-262
    rule-selective persistence, MECH-269 anchor sets + per-region V_s
    -- against the five Wang 2018 / Duan 2016 RL^2 properties (W1
    recurrent topology, W2 trained across many tasks, W3 hidden state
    encodes task identity, W4 biases action selection, W5 cross-episode
    hidden-state continuity).

    Mapping outcome:
      W1 absorbed by E1 LSTM (topology only; functionally dedicated
         to world-prediction) and SD-033a EMA (recurrence-as-EMA per
         A3 design choice).
      W2 NOT ABSORBED -- no multi-task training distribution exists in
         V3 (SD-054 is single-context). This is the multi-rule-context
         substrate gap the falsifier section already names.
      W3 absorbed by ARC-062 Phase 1 gated_policy multi-stream
         (z_world, z_self, z_harm_a) context discriminator (per-tick,
         trained) + SD-033a rule_state buffer (substrate-ready,
         content-empty until Phase 3 GAP-C wires the discriminator
         output into the rule_state update path).
      W4 absorbed by SD-033a compute_bias() and ARC-062 gated_policy
         heads; both compose additively into dacc_score_bias before
         E3.select().
      W5 NOT ABSORBED -- all three candidate substrates reset per
         episode. V4 scope; SD-033a's "Cross-episode carry-over is
         NOT implemented (V3 simplification; V4 extension if required)"
         note already records this.

    Empirical retire-vs-promote verdict therefore DEFERRED to
    V3-EXQ-543c-successor on multi-rule-context substrate, sequenced
    after ARC-062 Phase 2 GAP-B PASS (V3-EXQ-543b) and ARC-062 Phase 3
    GAP-C wiring closure. If post-Phase-3 the SD-033a + ARC-062
    cluster produces the within-episode rule-state-adaptation
    behavioural signature on multi-rule-context substrate, MECH-318
    retires as superseded with superseded_by: SD-033a + ARC-062
    (cluster). If the cluster fails the signature, MECH-318 promotes
    to candidate -> active and motivates a dedicated substrate
    landing (likely V4-scope given the W5 gap).

    Not yet experimentally tested. Falsifying experiments are
    multi-rule-context substrate; SD-054 single-context insufficient.
  notes: >
    Absorption-check verdict B (2026-05-10): the within-V3 portion of
    MECH-318 (W1 + W3 + W4) is partially absorbed into the SD-033a
    LateralPFCAnalog rule_state buffer + ARC-062 Phase 1 gated_policy
    discriminator + ARC-062 Phase 3 GAP-C planned wiring cluster. The
    multi-task-training (W2) and cross-episode-continuity (W5)
    properties are NOT absorbed and remain MECH-318's legitimate
    residual scope if the empirical verdict turns out to require a
    dedicated substrate. NO new V3 substrate is commissioned by this
    absorption check. V3-EXQ-543c-successor on multi-rule-context
    substrate is the empirical verdict gate; sequenced after Phase 2
    + Phase 3 closure. See arc_062_rule_apprehension_plan.md GAP-I
    decision-log entry for the sibling-cluster framing
    (MECH-316/MECH-317 absorption checks remain separately scoped).
  location: docs/architecture/rule_apprehension_layer.md

- id: MECH-312
  title: "rule_arbitration_multi_variable_multi_channel_dynamic_within_session. Parent claim for the multi-variable, multi-channel, dynamic-within-session arbitration that selects between competing rules / policy modes / action sources at the policy layer. Arbitration is governed by at least four distinct sub-mechanisms (MECH-312a/b/c/d), each falsifiable independently, with a candidate fifth (controllability/agency) deferred pending V3 substrate availability. Recommended combination rule: multiplicative gate (per-channel softmax of reliability * cost * agency * V_s * practice_maturity products) per Pull 3 R4 verdict; falsified against additive-logit baseline in V3-EXQ-543b/c. Reframed 2026-05-10 from earlier dual-channel-arbitration sketch following Pull 3 SYNTHESIS R5 SUB-MECH SPLIT recommendation (conf 0.78)."
  claim_type: mechanism_hypothesis
  subject: policy.rule_arbitration_multi_variable_multi_channel_dynamic_within_session
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-312@2026-05-10T12:24:30Z"
      as_of: 2026-05-10
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  epistemic_category: substrate_conditional
  claim_level: mechanistic
  registered_utc: "2026-05-10"
  depends_on:
    - ARC-062     # top-down rule-apprehension; one source of arbitration input
    - ARC-064     # bottom-up rule-discovery; another source of arbitration input
    - ARC-065     # diversity generation; precondition for arbitrable behavioural variation
    - MECH-309    # the diagnostic claim that motivates the rule machinery cluster
  functional_restatement: >
    MECH-312 is the parent claim for the rule-arbitration layer that
    selects between competing rules, policy modes, or action sources
    at the policy layer. The Pull 3 SYNTHESIS R5 verdict recommended
    a SUB-MECH SPLIT (conf 0.78) over a single-claim formulation,
    based on three convergent considerations:
      (i) independent testability -- each sub-MECH is independently
          falsifiable with a distinct ablation;
      (ii) heterogeneous biological provenance -- the four sub-
           mechanisms have distinct neural substrates and distinct
           literature lineages;
      (iii) 2D quadrant placement -- the sub-mechanisms differ in
           lit-conf vs exp-conf, motivating per-claim governance.

    Four registered sub-mechanisms:
      - MECH-312a uncertainty/reliability weighting (Daw 2005, Lee
        2014 dual-systems arbitration analog) -- LOW divergence
        from existing literature; direct mapping.
      - MECH-312b practice/maturity weighting (Smith & Graybiel
        habit-takeover analog) -- LOW-MEDIUM divergence; partial
        mapping.
      - MECH-312c affective-stream modulation of arbitration weight
        -- REE-novel functional consequence on a biologically-
        anchored dual-stream architecture; MEDIUM-HIGH divergence.
      - MECH-312d V_s freshness modulation per region -- HIGH
        divergence; per-region scope and rule-trust function are
        REE-novel.

    Candidate fifth (NOT REGISTERED YET):
      - MECH-312e controllability/agency modulation (Gershman 2021
        anchor) -- deferred pending V3 substrate availability.

    Recommended combination rule (Pull 3 R4, conf 0.74):
    MULTIPLICATIVE GATE as architectural default. Per-channel
    softmax of (reliability * cost * agency * V_s * practice_maturity)
    products, where each factor is supplied by one sub-mechanism:
      reliability      <- MECH-312a
      practice_maturity <- MECH-312b
      affective_modulator <- MECH-312c
      V_s_freshness    <- MECH-312d
      agency           <- MECH-312e (when registered)
    The multiplicative gate corresponds to a logical-AND across
    factors at the high-confidence regime and to a soft trade-off
    at intermediate values; an additive-logit baseline corresponds
    to logical-OR (any single factor can dominate). Pull 3 R4
    recommended multiplicative as default and additive as the
    falsified-against baseline.

    Falsification of the combination rule (architectural-default
    test): V3-EXQ-543b/c arms compare multiplicative-gate vs
    additive-logit on a substrate where the two combination rules
    produce divergent action-selection signatures (e.g. when one
    factor is at a low-confidence level: multiplicative suppresses
    arbitration trust; additive allows another factor to dominate).
  evidence_quality_note: >
    Reframed 2026-05-10 from earlier dual-channel-arbitration sketch
    via Pull 3 SYNTHESIS R5 SUB-MECH SPLIT verdict (conf 0.78).
    Reference cluster: targeted_review_mech_312_arbitration_divergences
    8 entries. Sub-MECHs MECH-312a/b/c/d registered immediately
    after this parent; MECH-312e deferred per Pull 3 R5 (V3
    substrate not yet available to carry agency-confidence per
    channel; defer until SD-054-extended or Q-041-resolved
    substrate work clears the way).

    Pull 4 R4 verdict: HYBRID-naming. The REE-internal name
    (rule_arbitration_multi_variable_multi_channel_dynamic_within_
    session) carries the architectural-function level; the
    contention-scheduling-arbitration analog
    (Norman-Shallice 1986; Cooper-Shallice 2000) carries the
    cognitive-architecture-anchor level.

    Recommended combination rule (multiplicative gate, Pull 3 R4
    conf 0.74) is registered as architectural-default-pending-
    empirical-validation; the additive-logit baseline is the
    falsifying alternative. V3-EXQ-543b/c are the validation
    vehicles.

    Not yet experimentally tested. lit_conf 0.866 post-Pull-3
    indexer rebuild.

    DEFERRED: MECH-312e controllability/agency modulation
    (Gershman 2021 anchor) is a candidate fifth sub-MECH;
    deferred pending V3 substrate availability that would carry
    agency-confidence per channel. Re-poseable when SD-054 is
    extended or when Q-041 unified-threshold-supervisor work
    surfaces a per-channel agency signal.

    [enum-comment migrated to field 2026-07-18] epistemic_category: depends on ARC-063
    rule-creator substrate (planned, design-only 2026-06-04, not yet built); suppress
    promote/demote until it lands. Set 2026-06-07 IGW-046.
  notes: >
    Earlier-conversation dual-channel-arbitration sketch is
    superseded by this parent + sub-MECH structure. The four
    registered sub-mechanisms cover the empirically-motivated
    arbitration variables identified by Pull 3:
    uncertainty/reliability, practice/maturity, affective
    modulation, V_s freshness. The candidate fifth is deferred.

    Pull 3 R4 multiplicative-gate recommendation is registered as
    notes-level architectural default; the falsification path is
    explicit (V3-EXQ-543b/c additive-logit baseline arm).
  what_would_answer: |
    MECH-312 is the PARENT claim for the arbitration COMBINATION RULE across
    the four registered sub-mechanisms (MECH-312a uncertainty/reliability,
    MECH-312b practice/maturity, MECH-312c affective-stream modulation,
    MECH-312d V_s freshness) plus the deferred candidate fifth (MECH-312e
    agency, not yet registered). Each sub-mech carries its own independent
    falsifier (see MECH-312a/b/c/d functional_restatement); THIS claim's own
    scientific content is narrower and sits one level up -- whether the
    architectural-default MULTIPLICATIVE GATE (per-channel softmax of
    reliability * cost * agency * V_s * practice_maturity) outperforms the
    falsifying ADDITIVE-LOGIT baseline, per the Pull 3 R4 verdict (conf 0.74).
    Do not conflate a sub-mech-level result with a verdict on this parent --
    a sub-mech confirming its own weighting channel says nothing about which
    combination rule integrates the channels.
    NON-DEGENERACY PRECONDITION (currently UNMET; a run that fails this must
    self-route substrate_not_ready, not count as a verdict):
      (1) MECH-312 and all four registered sub-mechs are v3_pending --
          implementation_phase v3 names an active DESIGN target, not
          something buildable on the substrate as it stands
          (rule_apprehension_layer.md).
      (2) The live gated-policy substrate (ree_core/policy/gated_policy.py)
          is the ARC-062 Phase-1 single-scalar two-head gate
          (w*h0 + (1-w)*h1) -- it has no multi-channel multiplicative-gate
          implementation to test AGAINST an additive-logit alternative.
      (3) This claim's own cited falsification vehicle is a MIS-CITATION:
          V3-EXQ-543b/c are tagged ARC-062/MECH-309/SD-029 (both FAIL) and
          never tested the arbitration combination rule. Do not treat a
          future re-read of 543b/c as evidence either way; requeue a
          purpose-built combination-rule probe instead.
      (4) The proximate substrate blocker is arc_062_rule_apprehension:GAP-B
          (the ARC-063 rule-creator/discriminator that would populate
          DIFFERENTIATED competing rule states). GAP-B remains in-progress
          as of the most recent plan update (2026-08-01, failure-autopsy of
          V3-EXQ-851): without differentiated rule states to arbitrate
          between, a combination-rule probe is VACUOUS (trivial-prediction
          hazard) regardless of outcome. GAP-B additionally names the
          separate MECH-439 F-dominance conversion-ceiling issue as a second
          precondition once differentiated rule states exist -- a run that
          reaches committed action without the arbitration weights actually
          moving the committed argmax is re-deriving that ceiling, not
          adjudicating MECH-312.
      (5) MECH-312 has zero entries in claim_evidence.v1.json -- there is no
          prior partial evidence to weigh against a new run; this is a
          from-scratch test once the substrate exists.
    CONFIRMING signature (once non-degeneracy is met): on a substrate with
    >= 2 differentiated, arbitrable rule states and a wired multi-channel
    gate, action-selection under the multiplicative-gate arm diverges from
    the additive-logit arm specifically at low-confidence single-factor
    regimes -- i.e. when one channel (e.g. low reliability) is weak, the
    multiplicative arm suppresses overall arbitration trust for that rule
    (logical-AND behaviour) while the additive arm still lets another
    strong channel dominate (logical-OR behaviour). This divergence should
    be attributable to the gate's functional FORM, not to which individual
    sub-mech channel happens to be strongest that trial.
    FALSIFYING signature: the multiplicative and additive arms produce
    statistically indistinguishable action-selection signatures across the
    low-confidence-single-factor regime, OR the additive-logit arm matches
    or outperforms multiplicative on the architecture's own selection
    criteria -- either would falsify the Pull 3 R4 multiplicative-gate
    recommendation as architectural default and reopen the combination-rule
    question.
  location: docs/architecture/rule_apprehension_layer.md

- id: MECH-312a
  title: "uncertainty_reliability_weighting (Daw_2005_Lee_2014_dual_systems_arbitration_analog). Arbitration weight tracks the inverse-uncertainty / reliability of each candidate rule or policy mode; high-reliability sources receive higher arbitration weight. Biological substrate: model-based / model-free dual-systems arbitration (Daw et al. 2005 Nat Neurosci; Lee et al. 2014 Neuron). LOW divergence from existing literature; direct mapping. Sub-claim of MECH-312."
  claim_type: mechanism_hypothesis
  subject: policy.arbitration.uncertainty_reliability_weighting
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  epistemic_category: substrate_conditional
  claim_level: mechanistic
  registered_utc: "2026-05-10"
  depends_on:
    - MECH-312    # parent
  functional_restatement: >
    MECH-312a names the uncertainty / reliability weighting sub-
    mechanism of the rule-arbitration layer. Each candidate rule
    or policy mode carries an internal uncertainty estimate
    (e.g., Bayesian posterior variance, ensemble-disagreement
    measure, or precision parameter); arbitration weight is
    inverse-uncertainty / reliability.

    Direct mapping to dual-systems arbitration (Daw et al. 2005;
    Lee et al. 2014): model-based and model-free systems each
    carry an uncertainty estimate; the agent weights their
    contributions to action-selection by relative reliability,
    so that the more-certain system dominates and the less-
    certain system is suppressed.

    Falsifiable: on a substrate where one rule has high reliability
    and another low (e.g. one rule has been observed fail recently;
    another has consistently succeeded), arbitration weight should
    track reliability monotonically. MECH-312a-ON agents show
    arbitration-weight ratio matching reliability ratio;
    MECH-312a-OFF (or uniform weighting) agents show flat
    arbitration regardless of reliability.
  evidence_quality_note: >
    Registered 2026-05-10 from Pull 3 SYNTHESIS R5. Lit anchors:
      - Daw, Niv & Dayan 2005 (Nat Neurosci) -- model-based vs
        model-free arbitration
      - Lee, Shimojo & O'Doherty 2014 (Neuron) -- arbitration via
        uncertainty
    See: evidence/literature/targeted_review_mech_312_arbitration_divergences/synthesis.md

    LOW divergence from existing literature; direct mapping.
    Pull 4 R4 (HYBRID-naming) but the literature mapping is so
    direct that the literature-anchor name dominates.

    [enum-comment migrated to field 2026-07-18] epistemic_category: depends on ARC-063
    rule-creator substrate (planned, design-only 2026-06-04, not yet built); suppress
    promote/demote until it lands. Set 2026-06-07 IGW-046.
  location: docs/architecture/rule_apprehension_layer.md

- id: MECH-312b
  title: "practice_maturity_weighting (Smith_Graybiel_habit_takeover_analog). Arbitration weight tracks practice-maturity / habit-strength of each candidate rule or policy mode; well-practiced behaviour receives higher arbitration weight via a habit-takeover dynamic. Biological substrate: dorsolateral striatum habit consolidation (Smith & Graybiel 2013/2016); successor-representation maturation (Stachenfeld et al. 2017). LOW-MEDIUM divergence; partial mapping (the maturity dimension is well-anchored; the integration into a multi-channel arbitration layer is more REE-specific). Sub-claim of MECH-312."
  claim_type: mechanism_hypothesis
  subject: policy.arbitration.practice_maturity_weighting
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  epistemic_category: substrate_conditional
  claim_level: mechanistic
  registered_utc: "2026-05-10"
  depends_on:
    - MECH-312    # parent
    - MECH-317    # behavioural pattern compression -- maturity is the consolidation product
  functional_restatement: >
    MECH-312b names the practice / maturity weighting sub-mechanism.
    Each candidate rule or policy mode accumulates a practice-
    maturity scalar (running count or EMA of successful
    deployments); arbitration weight increases with maturity
    via a habit-takeover dynamic.

    Biological substrate: Smith & Graybiel 2013/2016 demonstrate
    that with practice, action sequences chunk and DLS-mediated
    habit takes over from goal-directed (associative-striatum)
    control. Stachenfeld et al. 2017 successor-representation
    matures with experience. Both anchor the maturity dimension.

    Distinct from MECH-312a (uncertainty / reliability): a rule
    can be high-maturity (well-practiced) but currently low-
    reliability (recently failing); arbitration weight under
    multiplicative gate would be downweighted by 312a despite
    high 312b.

    Falsifiable: on a substrate where one rule has high practice
    (many prior deployments) and another low (recently introduced),
    arbitration weight under MECH-312b should track practice-
    count monotonically. Deviations indicate other factors
    (uncertainty, affect, V_s) dominating.
  evidence_quality_note: >
    Registered 2026-05-10 from Pull 3 SYNTHESIS R5. Lit anchors:
      - Smith & Graybiel 2013 (Curr Opin Neurobiol); 2016 (Neuron)
        -- habit consolidation / DLS takeover
      - Stachenfeld, Botvinick & Gershman 2017 (Nat Neurosci) --
        successor representation maturation
      - Yin & Knowlton 2006 (Nat Rev Neurosci) -- habit vs goal-
        directed dorsal-striatum dichotomy
    See: evidence/literature/targeted_review_mech_312_arbitration_divergences/synthesis.md

    LOW-MEDIUM divergence; partial mapping. Pull 4 R4 HYBRID-naming.

    [enum-comment migrated to field 2026-07-18] epistemic_category: depends on ARC-063
    rule-creator substrate (planned, design-only 2026-06-04, not yet built); suppress
    promote/demote until it lands. Set 2026-06-07 IGW-046.
  location: docs/architecture/rule_apprehension_layer.md

- id: MECH-312c
  title: "affective_stream_modulation_of_arbitration_REE_novel. Arbitration weight is modulated by affective stream load: when the SD-010 / SD-011 affective stream produces high load (high salience harm or affect), the arbitration layer routes more weight to rules associated with affect-laden context, suppressing arbitration weight on neutral-context rules. The REE-novel claim is the FUNCTIONAL CONSEQUENCE: dual-stream architecture (SD-010/SD-011) is well-anchored in Murray-Rudebeck 2018 and Pessoa-Adolphs 2010, but the modulation of rule-arbitration weights by affective-stream load is REE-specific (MEDIUM-HIGH divergence). Sub-claim of MECH-312."
  claim_type: mechanism_hypothesis
  subject: policy.arbitration.affective_stream_modulation_ree_novel
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  epistemic_category: substrate_conditional
  claim_level: mechanistic
  registered_utc: "2026-05-10"
  depends_on:
    - MECH-312    # parent
    - SD-010      # harm-stream substrate (affective stream A)
    - SD-011      # dual-nociceptive-stream / affective-discriminative split
  functional_restatement: >
    MECH-312c names the affective-stream modulation sub-mechanism
    of rule arbitration. The structural premise is well-anchored:
    SD-010 + SD-011 implement a dual-stream architecture where
    affective and discriminative streams are dissociable
    (anatomically supported by Murray & Rudebeck 2018 OFC review;
    Pessoa & Adolphs 2010 amygdala-cortical interactions). The
    REE-NOVEL claim is the functional consequence: affective
    stream load modulates arbitration weight on rules.

    Functional behaviour: when the affective stream (SD-010 harm
    stream + downstream affect-coding) carries high load, the
    arbitration layer routes more weight to rules previously
    deployed in similar high-affect contexts, and downweights
    rules previously deployed in neutral contexts. This produces
    affect-congruent rule selection -- a substrate-level
    instantiation of mood-congruent cognition.

    Distinct from MECH-312a (reliability) and MECH-312b (practice):
    affective modulation can override both (e.g., a low-practice,
    high-uncertainty rule deployed in a high-affect context may
    win arbitration via MECH-312c despite low MECH-312a + MECH-312b
    weight).

    Falsifiable: on a substrate where affective load varies (SD-054
    extended with affect-cue conditions), arbitration weight should
    show interaction effects with affect load: rules previously
    deployed under high-affect conditions should receive higher
    weight when affect re-elevates. MECH-312c-OFF agents should
    show flat arbitration across affect conditions.

    Clinical corollary: this mechanism predicts mood-congruent
    decision biases -- depressed agents should over-deploy rules
    associated with prior depressive contexts; anxious agents
    should over-deploy rules associated with prior threat. This
    is a falsifying-clinical prediction not currently testable in
    V3 but flagged for V4 / clinical-substrate work.
  evidence_quality_note: >
    Registered 2026-05-10 from Pull 3 SYNTHESIS R2 verdict
    (SUBSTANTIAL-OVERLAP for structural premise + GENUINE-NOVELTY
    for functional consequence; conf 0.78).
    Lit anchors:
      - Murray & Rudebeck 2018 (Annu Rev Neurosci) -- OFC review;
        anchors structural premise
      - Pessoa & Adolphs 2010 (Nat Rev Neurosci) -- amygdala-
        cortical interactions; anchors structural premise
      - SD-010, SD-011 entries in claims.yaml -- REE-internal
        substrate the functional claim sits on
    See: evidence/literature/targeted_review_mech_312_arbitration_divergences/synthesis.md

    MEDIUM-HIGH divergence (REE-novel functional consequence on
    biologically-anchored dual-stream architecture). Pull 4 R3
    KEEP-AS-IS recommendation: the REE-novel functional claim is
    the load-bearing innovation; the dual-stream anchor is a
    pre-existing REE substrate (SD-010/011) not separately
    re-anchored here.

    Not yet experimentally tested. Falsifying experiment requires
    affect-variable substrate; SD-054 currently single-affect-
    condition. EXQ design deferred.

    [enum-comment migrated to field 2026-07-18] epistemic_category: depends on ARC-063
    rule-creator substrate (planned, design-only 2026-06-04, not yet built); suppress
    promote/demote until it lands. Set 2026-06-07 IGW-046.
  location: docs/architecture/rule_apprehension_layer.md

- id: MECH-312d
  title: "V_s_freshness_modulation_per_region_REE_novel. Arbitration weight is modulated per-region by V_s freshness (the per-region staleness scalar from MECH-269 / MECH-284 / MECH-287); rules associated with stale regions receive lower arbitration trust, regardless of their reliability or practice. HIGH divergence (REE-novel per-region scope + rule-trust function): Behrens 2007 ACC volatility and Bouton 2004 contextual renewal are nearest cousins, but per-region continuous scalar + arbitration-trust routing are REE-specific. Sub-claim of MECH-312."
  claim_type: mechanism_hypothesis
  subject: policy.arbitration.v_s_freshness_modulation_per_region_ree_novel
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  epistemic_category: substrate_conditional
  claim_level: mechanistic
  registered_utc: "2026-05-10"
  depends_on:
    - MECH-312    # parent
    - MECH-269    # V_s monostrategy / per-region scalar substrate
    - MECH-284    # region-staleness accumulator
    - MECH-287    # V_s downstream consumer
  functional_restatement: >
    MECH-312d names the V_s-freshness modulation sub-mechanism of
    rule arbitration. The per-region V_s scalar from MECH-269 (and
    its accumulator MECH-284 + downstream consumer MECH-287)
    carries a freshness signal: regions whose V_s has aged
    indicate the underlying world-model is stale and any rule
    derived from that region's history should be downweighted.

    Functional behaviour: arbitration weight on a rule is gated
    by the V_s freshness of the regions whose data the rule
    derives from. Rules from fresh regions receive normal
    arbitration weight; rules from stale regions receive
    reduced weight (multiplicative downweighting by V_s
    freshness). This is the rule-trust face of V_s monostrategy:
    not just "this region's representation is stale" but
    "rules derived from this stale region should not be trusted
    as-is."

    Distinct from MECH-312a (uncertainty), MECH-312b (practice),
    MECH-312c (affective): a rule can be high-reliability, high-
    practice, neutral-affect, but derived from a region whose
    underlying world-state has changed (the region is stale).
    MECH-312d downweights such rules; the other sub-mechanisms
    do not.

    HIGH divergence from existing literature. Behrens et al. 2007
    ACC volatility tracking is the closest cousin (volatility
    scalar modulates learning rate); Bouton 2004 contextual
    renewal is conceptually related (context shifts can
    re-introduce extinguished responses). But per-region
    continuous scalar + arbitration-trust routing of rules
    derived from stale regions is REE-specific.

    Falsifiable: on a substrate with region-specific staleness
    induction (e.g. SD-054 extended with reef-config rotation
    that ages V_s on previously-active reef regions),
    arbitration weight on rules previously deployed in those
    regions should drop monotonically with V_s freshness.
    MECH-312d-OFF agents should show invariant arbitration
    weight despite V_s drift.
  evidence_quality_note: >
    Registered 2026-05-10 from Pull 3 SYNTHESIS R3 verdict
    (PARTIAL-OVERLAP for substrate + GENUINE-NOVELTY for per-
    region scope and rule-trust function; conf 0.65).
    Lit anchors:
      - Behrens, Woolrich, Walton & Rushworth 2007 (Nat Neurosci)
        -- ACC volatility tracking; closest cousin
      - Bouton 2004 (Annu Rev Psychol) -- contextual renewal of
        extinguished responses; conceptually related
      - MECH-269 / MECH-284 / MECH-287 entries in claims.yaml --
        REE-internal V_s substrate this claim sits on
    See: evidence/literature/targeted_review_mech_312_arbitration_divergences/synthesis.md

    HIGH divergence; this is among the most REE-novel sub-
    mechanisms registered in this session. Pull 4 R3 KEEP-AS-IS
    (V_s-arbitration-trust is genuine REE-divergence).

    Not yet experimentally tested. Falsifying experiment requires
    region-staleness-induction substrate; not currently in V3
    substrate set. EXQ design deferred.

    [enum-comment migrated to field 2026-07-18] epistemic_category: depends on ARC-063
    rule-creator substrate (planned, design-only 2026-06-04, not yet built); suppress
    promote/demote until it lands. Set 2026-06-07 IGW-046.
  location: docs/architecture/rule_apprehension_layer.md

- id: MECH-319
  title: "simulation_mode_rule_write_gating_categorical_replay_tag (SWR_machinery_substrate_REE_novel_function). The substrate-level instantiation of MECH-094's hypothesis-tag write gate at the rule-arbitration layer: a categorical write-gate keyed to a simulation-mode tag that suppresses arbitration-weight updates during ghost / replay / DMN passes, allowing rule-arbitration to be exercised without overwriting the active rule weights from non-realized data. Substrate-availability premise is well-anchored: SWR machinery (Joo & Frank 2018 review) + reverse-replay discriminable signature (Foster & Wilson 2006). The specific REE function (categorical write-gate keyed to a simulation tag suppressing arbitration-weight updates) is plausible biologically but unverified at cellular / synaptic level."
  claim_type: mechanism_hypothesis
  subject: policy.arbitration.simulation_mode_write_gating_substrate_ree_novel_function
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: v3_exq_668_mech319_accumulated_ghost_write_arbitration_drift_evidence_v3_20260611T135309Z
      as_of: 2026-06-11
      verdict: supports/PASS
  implementation_phase: v3
  v3_pending: false
  claim_level: mechanistic
  registered_utc: "2026-05-10"
  depends_on:
    - MECH-094    # the existing hypothesis-tag claim that motivates this; MECH-319 is its substrate-level instantiation at the arbitration layer
    - MECH-312    # the arbitration layer whose write-gate this implements
  functional_restatement: >
    MECH-319 names the substrate-level instantiation of MECH-094's
    hypothesis-tag write gate at the rule-arbitration layer.
    MECH-094 names the architectural principle (categorical
    phi(z) write gate; simulation-mode-suppressed write channel);
    MECH-319 names the specific substrate instantiation: a
    categorical write gate keyed to a simulation-mode tag that
    suppresses arbitration-weight updates in MECH-312 sub-
    mechanisms during ghost / replay / DMN passes.

    Substrate availability is well-anchored:
      - Sharp-wave ripple (SWR) machinery (Joo & Frank 2018
        Nat Rev Neurosci review) provides a discriminable
        biological substrate for replay events.
      - Reverse-replay discriminable signature (Foster & Wilson
        2006 Nature) demonstrates that replay events carry
        substrate-level identifiability (forward vs reverse).
    The substrate is therefore in place biologically; the
    architectural commitment that the substrate carries a
    write-gate function for arbitration weights is plausible
    but unverified at cellular / synaptic level.

    Why this is REE-novel even given MECH-094. MECH-094 names
    the principle (write-gate keyed to a hypothesis tag);
    MECH-319 names where in the substrate the gate operates
    (the rule-arbitration layer's MECH-312 sub-mechanisms) and
    what carries the categorical signal (SWR machinery as the
    substrate-level analog of the simulation tag). The
    biological literature does NOT, as of 2026-05-09, confirm
    that SWR events suppress rule-arbitration-weight updates
    in cortical-striatal arbitration loops -- this is the
    REE-specific functional commitment.

    Falsifiable (V4-bound): pharmacological or optogenetic SWR
    suppression during a rule-arbitration training task should
    NOT change steady-state rule-arbitration weights (because
    SWR suppression removes a function that does not write to
    the weights anyway in MECH-319's framing); whereas
    suppression during ACTIVE-policy-deployment phases should
    produce different effects. The dissociation between
    SWR-during-training and SWR-during-deployment is the
    diagnostic. V3 falsification path: a config flag that
    artificially routes simulation-mode passes through the
    write channel anyway should produce overcommitment to
    ghost-derived arbitration weights and observable rule-
    arbitration drift; with MECH-319 enabled, no drift.
  evidence_quality_note: >
    Registered 2026-05-10 from Pull 3 SYNTHESIS R1 verdict
    (GENUINE-NOVELTY-CONFIRMED, conf 0.72) + Pull 4 R3
    KEEP-AS-IS recommendation.
    Lit anchors:
      - Joo & Frank 2018 (Nat Rev Neurosci) -- SWR review;
        substrate-availability anchor
      - Foster & Wilson 2006 (Nature) -- reverse replay
        discriminable signature; substrate-level identifiability
        anchor
      - MECH-094 entry in claims.yaml -- the architectural
        principle MECH-319 instantiates at the substrate level
    See: evidence/literature/targeted_review_mech_312_arbitration_divergences/synthesis.md

    Substrate-availability premise is well-supported (SWR
    machinery + reverse-replay discriminable signature); the
    specific REE function (categorical write-gate keyed to a
    simulation tag suppressing arbitration-weight updates) is
    plausible but biologically unverified at cellular /
    synaptic level. Treat as REE-novel candidate; future biology
    lit-pull on SWR-and-cortical-arbitration-weight-update
    coupling would deepen confidence (or disconfirm).

    First experimentally tested 2026-06-02 (V3-EXQ-628 PASS,
    supports -- see governance entry below). V3 falsification path
    (artificial-write-channel-routing config flag) was V3-
    EXQ-543c-tractable and is now exercised; cellular-level
    falsification remains V4-bound and pharmacology-dependent.

    SUBSTRATE LANDED 2026-05-10. status candidate -> candidate_
    substrate_landed. Module: ree_core/regulators/simulation_mode_
    rule_gate.py (SimulationModeRuleGate + SimulationModeRule
    GateConfig + SimulationModeRuleGateDiagnostics). Pure-arithmetic
    regulator, sibling to GABAergicDecayRegulator (SD-036) and
    BroadcastOverrideRegulator (SD-037). Single primitive
    effective_simulation_mode(simulation_mode, site) -> bool with
    truth-table semantics: master OFF identity passthrough; master
    ON + admit_writes False blocks simulation writes (MECH-319
    normal); master ON + admit_writes True (V3-EXQ-543c falsifier)
    admits simulation writes. Idempotent for waking calls
    regardless of admit_writes (the asymmetry surfaces only at
    caller_sim=True). Per-site diagnostic counters on
    {gated_policy, lateral_pfc, default}. Construction raises
    ValueError on admit_writes=True without master ON (loud-not-
    silent guard). Config: REEConfig.use_simulation_mode_rule_gate
    + REEConfig.simulation_mode_rule_gate_admit_writes (both
    default False, bit-identical OFF; wired through REEConfig.from_
    dims). Agent wiring: REEAgent.select_action consults the gate
    at the GatedPolicy.forward call site (replaces the literal
    simulation_mode=False) and at the LateralPFCAnalog.update call
    site (skip update when gate returns True; compute_bias still
    runs from current rule_state). Per-episode reset() clears
    diagnostic counters. MECH-094 NOT modified per Pull 3 R1 +
    Pull 4 R3 KEEP-AS-IS verdicts -- this substrate wraps the
    simulation_mode argument that callers ALREADY pass; with
    MECH-319 disabled, every arbitration-write call site behaves
    bit-identically to its pre-MECH-319 form. Backward compatible:
    288/288 contract + preflight tests PASS with master OFF
    (regression-clean; was 273 + 15 new MECH-319 contracts).
    Validation: V3-EXQ-546 substrate-readiness diagnostic queued;
    smoke 6/6 PASS 2026-05-10. Behavioural V3-EXQ-543c-successor
    falsifier (admit_writes=True with replay-driven invocation)
    deferred AFTER MECH-313 / MECH-314 / MECH-318 sibling
    substrates land. Design doc: docs/architecture/mech_319_
    simulation_mode_rule_gate.md.

    FIRST CONTRIBUTORY EVIDENCE 2026-06-06 (governance fold of
    V3-EXQ-628). The deferred V3-EXQ-543c-successor falsifier
    RAN and LANDED PASS: run_id v3_exq_628_mech319_simulation_
    mode_rule_gate_replay_falsifier_evidence_v3_20260602T191625Z
    (ree-cloud-2, 2026-06-02; experiment_purpose=evidence;
    claim_ids=[MECH-319]; evidence_direction=supports;
    3/3 seeds, required 2). It constructs the replay-driven
    invocation path select_action never exercises (effective_
    simulation_mode(simulation_mode=True, site=lateral_pfc) +
    conditional LateralPFCAnalog.update) and measures rule_state
    divergence between BLOCK (admit_writes=False) and ADMIT
    (admit_writes=True) arms replaying the SAME captured early-
    waking latent. PASS = BLOCK suppresses the simulation write
    (replay delta ~0) AND ADMIT admits it (mean replay delta
    ~0.31) AND the arms diverge AND the waking path is bit-
    identical across arms with zero simulation firings (the load-
    bearing C4 falsifier). This is the first evidence-grade
    confirmation of the rule_state-level write-suppression
    consequence MECH-319 asserts on the real consumer; the
    predecessor V3-EXQ-546 was diagnostic/non_contributory
    (truth-table + wiring only, never touched rule_state).
    Indexer now scores 1 supporting experimental entry,
    exp_conf=0.766, evidence_quadrant=novel_discovery,
    conflict_ratio=0.

    STATUS HELD at candidate_substrate_landed (NO promotion).
    Two independent governance gates block promotion to
    provisional on this single PASS, both correctly: (1)
    candidate_to_provisional requires min_experimental_entries=2
    -- MECH-319 has exactly 1 contributory entry, so a second
    contributory V3 entry is required before the exp_conf=0.766
    (> the 0.62 bar) can promote; (2) v3_pending stays true as a
    belt-and-braces hold until that second entry lands and is
    reviewed. The V3-pending gate's evidence-condition (V3
    experiment provides evidence) is now FIRST SATISFIED by 628,
    but the promotion bar is not yet met.

    DATA-QUALITY NOTE (does not change the verdict): 628's
    run_id places the `_v3` marker BEFORE the runner timestamp
    (`..._evidence_v3_20260602T191625Z`) rather than at the end
    (`..._<timestamp>_v3`, the convention sibling V3-EXQ-633
    follows). The genuine-evidence detector keys on
    run_id.endswith("_v3"), so it under-counts 628 as
    non-genuine (auto-recommendation reads collect_genuine_
    evidence / synthetic). The entry IS scored (exp_conf 0.766);
    only the genuine sub-classification mis-fires. Net effect on
    promotion is nil because min_experimental_entries=2 already
    holds the claim. INDEXER NOTE: the canonical
    runs/<run_id>/manifest.json pack for 628 was reconstructed
    2026-06-06 from the authoritative coordinator flat manifest
    (the cloud-2 Phase-3 result landed as a flat manifest only;
    its runs/ pack never synced, so build_experiment_indexes.py
    -- which scans **/runs/**/manifest.json -- never scored it;
    that is why this PASS sat un-folded since 2026-06-02).

    SECOND CONTRIBUTORY EVIDENCE + PROMOTION 2026-06-11 (governance
    cycle #3, user-approved). The second distinct contributory V3
    entry landed: V3-EXQ-668 (run_id v3_exq_668_mech319_accumulated_
    ghost_write_arbitration_drift_evidence_v3_20260611T135309Z; ree
    fleet 2026-06-11T13:53Z; experiment_purpose=evidence; claim_ids=
    [MECH-319]; evidence_direction=supports; 3/3 seeds PASS, 3/3
    non-vacuous, required 2; predecessor V3-EXQ-628, NOT a
    supersession). 668 tests the claim's OTHER pre-registered V3
    falsification path -- accumulated ghost-write arbitration DRIFT /
    overcommitment over a SUSTAINED replay regime (vs 628's single-
    step rule_state divergence): every criterion green on all seeds
    (C0 path-exercised-no-vacuity, C4 waking bit-identical falsifier,
    C_admit_live readiness, C1 BLOCK->no drift, C2 ADMIT->accumulates
    beyond a single step, C3 drift reaches the compute_bias
    arbitration output, bias_readout_non_degenerate). MECH-319 now
    has 2 contributory supporting V3 entries (628 + 668),
    genuine_exp_count=2, exp_conf=0.825, conflict_ratio=0,
    evidence_quadrant=novel_discovery.

    PROMOTED candidate_substrate_landed -> provisional. Both gates
    that previously held the claim are now satisfied: (1)
    min_experimental_entries=2 is met (628 + 668); (2) the
    v3_pending belt-and-braces hold's release condition ("until that
    second entry lands and is reviewed") is met -- 668 landed and was
    reviewed this cycle -- so v3_pending set false. The cellular-level
    falsification remains V4-bound and pharmacology-dependent (does
    not block a V3 provisional). Full stable promotion would require
    the standard provisional->active evidence depth; this is a
    provisional on two clean novel-discovery-quadrant V3 supports.
  notes: >
    Pull 4 R3 KEEP-AS-IS verdict: MECH-094 stays as-is (the
    architectural principle); MECH-319 is registered as the
    substrate-level instantiation at the arbitration layer.
    The two claims are NOT redundant -- MECH-094 names the
    principle without committing to which substrate carries
    it; MECH-319 names a specific substrate (SWR machinery)
    and a specific function-site (MECH-312 arbitration weights).

    Future architectural work may name additional MECH-319-
    siblings for other function-sites where MECH-094's
    write-gate principle should apply (e.g. MECH-094 at the
    rule-state abstraction layer of MECH-318). These are
    deferred until the substrate sites are themselves wired.

    RELATION TO MECH-261: MECH-261 (mode-conditioned write-gate
    registry on SD-032a SalienceCoordinator) is a complementary,
    continuous gate. MECH-261 returns a per-mode weight in [0, 1]
    that scales the magnitude of the EMA update on
    LateralPFCAnalog.rule_state. MECH-319 is a categorical
    (binary admit/block) pre-gate keyed to the simulation tag of
    the CALLER, not the operating mode of the agent. The two gates
    compose: caller waking + MECH-319 admits -> MECH-261 modulates
    EMA strength based on operating mode. Caller simulation +
    MECH-319 normal -> MECH-319 blocks the entire update() call,
    MECH-261 never consulted. Caller simulation + MECH-319
    falsifier -> MECH-319 admits -> MECH-261 modulates as if
    waking. Together they instantiate two complementary axes of
    write-gating at the rule-arbitration layer.
  location: docs/architecture/mech_319_simulation_mode_rule_gate.md

- id: ARC-066
  title: "tonic_vigor_coupling (capacity-to-action-bias, average-reward-rate / LC-NE tonic / DA effort-vigor analog). Architectural commitment: behaviour-bias toward action scales with capacity (high energy AND low recent prediction error AND low drive -- 'things are going well, deploy'), independent of any specific target. Distinct from existing behavioural-source slots (z_harm threats, z_goal deficit-seeded wants, novelty curiosity) which all fall to zero when the agent is well-fed, safe, and the world is familiar. Without ARC-066 a healthy REE agent has no gradient to act; biology has a positive gradient (felt phenomenology: drive to deploy energy when one has it). First of three slots in the non_deficit_action_drives family; companion ARC-067 (idle aversion / boredom) and ARC-068 (opportunity cost) cover the aversive-of-rest and cost-of-passivity halves of the same architectural commitment. Family principle: behaviour comes from surplus capacity AND from deficits, not deficits alone."
  claim_type: architectural_commitment
  subject: action.tonic_vigor_coupling
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-066@2026-05-16T19:08:31Z"
      as_of: 2026-05-16
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: "2026-05-10"
  depends_on: []
  functional_restatement: >
    ARC-066 registers the architectural slot for a behaviour-bias-toward-
    action term that scales with the agent's CAPACITY rather than its
    deficits. REE today drives behaviour from threats (z_harm), specific
    wants (z_goal seeded from depletion), and novelty (curiosity). All
    three fall to zero in the well-fed-safe-familiar regime. ARC-066 says
    the architecture is missing a positive gradient out of that regime --
    a tonic vigor scalar that rises when capacity is high and biases E3
    score selection toward action over no-op trajectories.

    Biology anchors (preliminary; lit-pull required before child MECH
    design):
      - Niv et al. 2007 (PNAS) -- vigor of action is set by tonic dopamine
        tracking the average reward rate. Period of high reward expectation
        produces high tonic DA produces high vigor, INDEPENDENT of any
        specific target.
      - Aston-Jones & Cohen 2005 (Annu Rev Neurosci) -- LC-NE tonic mode:
        high tonic NE biases toward exploitation/action over deliberation.
        (Note: existing MECH-313 instantiates the LC-NE tonic NOISE FLOOR --
        adds entropy to action distribution. ARC-066 is a different LC-NE
        function: directional bias TOWARD action, not noise on choice.)
      - Salamone & Correa 2012 (Neuron) -- mesolimbic DA encodes effort-
        vigor activation, dissociable from hedonic value of reward.
      - Walton et al. 2006 (J Neurosci) -- ACC effort-cost / vigor
        computation.
      - Depue & Collins 1999 (Behav Brain Sci) -- BAS (behavioural
        approach system) tonic level as an independent personality
        dimension, low BAS = anhedonic inertia, high BAS = drive-to-act.

    Architectural relations within REE:
      - SD-012 (homeostatic_drive_modulation): drive RISES as energy
        FALLS. ARC-066 is the inverse coupling (action-bias rises as
        capacity rises). Both can hold simultaneously: depletion drives
        you toward specific consummatory goals AND surplus drives you
        toward general action.
      - MECH-313 (stochastic_noise_floor): orthogonal. MECH-313 lifts
        softmax temperature uniformly. ARC-066 adds a directional bias
        on the score axis toward action over passivity. The two compose:
        MECH-313 says "explore among candidates"; ARC-066 says "favour
        action candidates over no-op candidates". Same downstream
        (E3.select), different scoring direction.
      - MECH-216 (e1_predictive_wanting / schema readout): only fires
        when a specific predicted target is in scope. ARC-066 fires
        regardless of target presence -- it is a target-free action bias.
      - SD-037 (broadcast_override / orexin): recruited by sustained
        drive + harm. ARC-066 is recruited by the OPPOSITE state
        (sustained capacity + low recent PE). The two regulators sit at
        different corners of the agent state space.

    Falsifiable architectural prediction (substrate-level): an REE agent
    parameterised with all current drives + curiosity + override + sleep
    OFF, in a familiar safe environment with full energy, will be
    behaviourally inert (action_density << biological baseline). With
    ARC-066 instantiated, action_density rises monotonically with the
    capacity scalar even with all deficit-side drives set to zero.

    What ARC-066 is NOT:
      - Not a re-implementation of MECH-313 LC-NE noise floor.
      - Not a vigor-of-execution timing parameter (Niv 2007 also covers
        execution speed; this slot is about WHETHER to act, not how
        fast). A future MECH/Q-claim may extend the slot to execution-
        timing.
      - Not committing to a specific child mechanism or tonic-DA-vs-LC-NE
        attribution. The literature distinguishes these substrates
        (Niv DA-vigor; Aston-Jones LC-NE tonic; both contribute). Pull
        will sequence which child mechanisms land.

    Companion slots: ARC-067 (idle aversion / boredom -- aversive valence
    of sustained low-engagement) and ARC-068 (opportunity cost no-op
    penalty -- E3 cost on passive trajectories). Together the three
    cover the "behaviour from surplus, not just deficit" architectural
    commitment.

    Capacity scalar refinement (post lit-pull, R4 verdict 2026-05-10):
    the PRIMARY scalar is a SLOW EWMA over the realised E3-score-
    receipt stream (per Niv 2007 average-reward-rate formalism +
    Beierholm 2013 human empirical confirmation), NOT the internal-
    capacity composite the slot was first registered with. Internal-
    state proxies (energy reserve, drive integrator, recent prediction
    error) enter the child mechanism as SECONDARY MODULATORS that gate
    the primary scalar down when capacity is unfavourable. The two
    formulations are correlated in normal conditions but mathematically
    distinct -- the literature attribution is to environmental reward
    history, not internal physiological state. First child MECH
    (MECH-320) instantiates this composition.

    First child mechanism: MECH-320 (tonic_vigor_coupling_score_bias)
    registered 2026-05-10 -- adds an additive vigor bias to E3 action-
    trajectory scoring with the EWMA scalar gated by secondary internal-
    state modulators. Substrate attribution: mesolimbic DA-vigor.
    Explicitly NOT LC-NE-direction (per R2 verdict; LC-NE tonic mode
    is one mechanism = noise = MECH-313, not two). See
    docs/architecture/non_deficit_action_drives.md for the family-doc
    anchor cluster reframe.
  evidence_quality_note: >
    Registered 2026-05-10 from user-provided phenomenology (Daniel,
    consultant psychiatrist + REE author): "I observe a drive in me to
    do something while I have energy rather than nothing / imagination
    and other non-behavioural work. In REE it seems that the agent is
    happy doing nothing." Phenomenology-as-substrate-search is an
    explicit project methodology (cf. INV-049 sleep, MECH-302 suffering
    derivative comparator, MECH-309 logical-necessity argument for
    rule apprehension).

    Biology anchors are preliminary -- a targeted lit-pull on the tonic
    vigor / average-reward-rate / LC-NE-tonic-direction literatures is
    REQUIRED before any child MECH/SD design (per project rule:
    biology-before-formal-definitions). Anchors above are sufficient to
    motivate slot registration but not to fix mechanism shape.

    Not yet experimentally tested. The substrate currently produces no
    measurable behavioural lift in healthy-state regime because the
    slot is empty. First validation experiment will be a substrate-
    readiness diagnostic on a child MECH (TBD post lit-pull).

    Lit-pull complete 2026-05-10 (evidence/literature/targeted_review_
    arc_066_tonic_vigor/synthesis.md, 7 entries). Net lit verdict:
    supports the architectural function with strong substrate attribution
    to mesolimbic DA-vigor (Niv 2007 formalism + Salamone & Correa 2012
    substrate identity + Beierholm 2013 human L-DOPA causal test);
    REJECTS the pre-registered LC-NE-direction substrate (Kane et al.
    2017 DREADD test in rats by the original Aston-Jones / Cohen
    authorship group: LC-NE tonic effect is noise-mediated, not
    direction-mediated -- the LC-NE function is fully covered by
    MECH-313, with no remaining LC-NE function for ARC-066 to claim).
    Implementation shape per R3 verdict: ADDITIVE E3 score bias on
    action vs no-op (primary), MULTIPLICATIVE GAIN as falsifiable
    secondary alternative resolvable by parametric-sweep validation
    experiment. Capacity scalar per R4 verdict: SLOW EWMA over realised
    E3-score-receipt is the primary scalar (Niv 2007 average-reward-rate
    formalism), with internal-state proxies (energy reserve, drive
    integrator, recent PE) as secondary modulators -- a refinement of
    the slot's pre-registered "high energy AND low recent prediction
    error AND low drive" composition. R2 verdict explicitly does NOT
    recommend slot split: the architectural function ARC-066 names is
    real, but the substrate attribution to LC-NE is wrong; correct it
    by reframing the family doc anchor cluster (Aston-Jones moves to
    "what ARC-066 is NOT"; Niv / Salamone / Beierholm / Depue & Collins
    remain primary; Walton 2003 stays as a boundary anchor for the
    SD-032b territory). Aggregate lit_conf expected ~0.72-0.76 supports
    direction (5 supports + 2 mixed entries; the Kane 2017 weakens
    entry applies specifically to the rejected LC-NE substrate, not
    to the architectural commitment).
  notes: >
    Cross-reference family: ARC-066 + ARC-067 + ARC-068 form the
    non_deficit_action_drives cluster. All three were registered
    together on 2026-05-10 from a single phenomenological observation
    that REE's behavioural-source slots are all deficit-keyed.

    HYBRID-naming convention (parallel to ARC-065): the title carries
    both the REE-internal architectural name (tonic_vigor_coupling)
    and the literature anchors (average-reward-rate / LC-NE tonic /
    DA effort-vigor). This makes the slot grep-able under both
    REE-native search and biology-search.

    Cross-link: ARC-066 is conceptually adjacent to but mechanistically
    distinct from existing claims. Future child MECH design must
    explicitly disambiguate from MECH-313 (LC-NE tonic noise -- noise
    on choice, not direction toward action), MECH-260 (dACC anti-
    recency -- state-dependent recency penalty, different timescale),
    MECH-216 (predictive wanting -- target-conditioned, not target-
    free), and SD-012 (homeostatic drive -- deficit-keyed, opposite
    coupling).
  location: docs/architecture/non_deficit_action_drives.md

- id: ARC-067
  title: "idle_aversion_boredom (sustained-low-engagement aversive valence accumulator). Architectural commitment: prolonged low-engagement is itself negatively valenced; the agent acts to escape boredom, even in the absence of specific targets, threats, or novelty pressure. Distinct from the policy-side ARC-066 vigor coupling: ARC-067 instantiates an aversive-side accumulator that recruits the same downstream as actual harm (z_harm-like channel), so engagement-poverty competes for action-selection priority on the same axis as discomfort. Companion to ARC-066 (vigor / capacity-deploy) and ARC-068 (opportunity cost) in the non_deficit_action_drives family."
  claim_type: architectural_commitment
  subject: action.idle_aversion_boredom
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-067@2026-05-16T19:08:31Z"
      as_of: 2026-05-16
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: "2026-05-10"
  depends_on: []
  functional_restatement: >
    ARC-067 registers the architectural slot for boredom as a structural
    aversive state. The user-observed phenomenology: doing-nothing is not
    neutral. After enough sustained low-engagement, an aversive pressure
    accumulates that biases the agent toward action regardless of whether
    any specific action is currently appealing. Clinically this is the
    opposite face of anhedonia (where the aversive pressure fails to
    accumulate or fails to translate into action).

    Biology anchors (preliminary; lit-pull required before child MECH
    design):
      - Eastwood et al. 2012 (Perspect Psychol Sci) "The Unengaged Mind"
        -- definitional / functional account of boredom as failed
        engagement, not just absence of stimulation.
      - van Tilburg & Igou 2017 -- boredom as functional emotion that
        prompts pursuit of meaningful activity.
      - Westgate & Wilson 2018 (Psychol Rev) -- meaning-and-attentional-
        component (MAC) model: boredom arises from misalignment between
        engagement capacity and engagement opportunity.
      - Bench & Lench 2013 -- boredom proneness as a predictor of risk-
        taking and stimulation-seeking behaviour.
      - Csikszentmihalyi 1990 (flow state) -- engagement as match
        between skill and challenge; boredom as one corner of the
        skill/challenge mismatch space (high skill, low challenge).
      - Ulrich-Lai & Herman 2009 (Nat Rev Neurosci) -- HPA-axis stress
        of chronic low-control / understimulation regimes.
      - Cabanac 1971 / Sherrington 1906 lineage -- aversive-valence
        framing of homeostatic deviations; ARC-067 extends this to a
        non-homeostatic deviation (engagement-rate fallen below set point).

    Architectural shape (to be fleshed at child MECH level):
    Likely a slow accumulator over an engagement-rate scalar. Engagement
    candidates include: number of commit transitions per episode (rare
    commits -> low engagement); novel-observation count (familiar
    surroundings -> low engagement); E3 deliberation depth (shallow
    selection -> low engagement); residue-write rate (no learning
    happening -> low engagement). When the integrated engagement-rate
    falls below a threshold, accumulates as an aversive on a z_harm-like
    channel that recruits the same E3 score-bias path as actual harm.
    Two timescales likely: acute restlessness (~minutes / tens of episodes)
    and chronic anhedonic flatness (~episodes / sessions); these may need
    to be separate child claims.

    Architectural relations within REE:
      - z_harm streams (SD-010 sensory-discriminative, SD-011 affective):
        ARC-067's aversive output is most parsimoniously routed through
        an extension of z_harm_a (affective stream), not z_harm_s
        (sensory stream). Boredom does not have a sensory localisation;
        the aversive is global / state-level.
      - MECH-216 (predictive wanting): does NOT cover boredom. Wanting
        only fires when a target is predicted; boredom's defining
        feature is the absence of any specific predicted target.
      - MECH-314a striatal novelty: novelty is the ABSENCE of which
        boredom is one consequence, but boredom is not just inverted
        novelty. You can be in genuinely novel territory and still
        bored if there is no engagement opportunity (you are watching
        novel content but cannot interact). Conversely you can be in
        familiar territory and not bored if engagement is rich (a
        skilled task in your home environment).
      - SD-012 / SD-037: boredom is a NON-deficit aversive. Deficit-
        side aversives recruit broadcast_override / drive escalation;
        the boredom aversive recruits action-pressure through the
        valence channel without going through homeostatic-deficit
        signaling.

    Falsifiable architectural prediction: ablate ARC-067; place an REE
    agent with full energy, low harm, full familiarity, and no specific
    z_goal in a long episode. Without ARC-067 the agent should be inert
    (action_density at MECH-313 noise floor only). With ARC-067 the
    boredom accumulator should fire after some critical low-engagement
    duration and produce action-pressure; the agent should explore /
    act / disengage even when no specific reward predicts it.

    What ARC-067 is NOT:
      - Not a curiosity bonus (MECH-314 covers novelty / uncertainty /
        learning-progress positive bonuses; boredom is the aversive of
        their absence, not their negation).
      - Not a homeostatic drive (no regulated body variable / setpoint
        in the SD-012 sense; engagement-rate is a behavioural rate
        statistic, not an internal physiological variable).
      - Not a depression / anhedonia model. Boredom is the healthy
        aversive that motivates re-engagement; clinical anhedonia is
        precisely the failure of this loop. Future psychiatric_failure_modes
        cross-reference appropriate.

    Companion slots: ARC-066 (capacity-to-vigor positive bias) and
    ARC-068 (opportunity cost on no-op trajectories). The three
    register "behaviour from surplus" as an architectural commitment
    coequal with "behaviour from deficit".
  evidence_quality_note: >
    Registered 2026-05-10 alongside ARC-066 + ARC-068 from user
    phenomenology. Lit-pull required before child MECH design. The
    boredom literature is broader and less convergent than the vigor
    literature; child MECH design will need to disambiguate at least
    three competing accounts: (a) functional-emotion (Eastwood, van
    Tilburg) -- boredom prompts engagement-search; (b) attentional-
    failure (Westgate & Wilson MAC) -- boredom is failed attention
    capture; (c) opportunity-cost-felt (overlaps with ARC-068, may
    require boundary clarification) -- boredom is the felt pull of
    forgone alternatives. Likely all three contribute; the child MECH
    will need to pick implementation shape and lit will arbitrate.

    Not yet experimentally tested. First validation experiment will be
    substrate-readiness diagnostic on a child MECH (TBD post lit-pull),
    likely on SD-054 reef substrate where engagement opportunities are
    structurally rich.

    LIT-PULL COMPLETE 2026-05-10. Targeted review under
    evidence/literature/targeted_review_arc_067_boredom/ -- 8 entries
    spanning definitional theory (Eastwood 2012), attentional theory
    (Westgate & Wilson 2018 Psychol Rev), functional emotion empirical
    (van Tilburg & Igou 2017 Emotion; Bench & Lench 2013), behavioural
    aversive (Wilson 2014 Science 11-study self-shock), fMRI neural
    correlates (Danckert & Merrifield 2018 AIC deactivation under
    boredom), HPA chronic stress substrate (Ulrich-Lai & Herman 2009
    Nat Rev Neurosci), transdiagnostic apathy/anhedonia (Husain &
    Roiser 2018 Nat Rev Neurosci). 7 supports + 1 mixed (Ulrich-Lai is
    substrate-context only). Aggregate lit_conf expected 0.72-0.76.

    Five verdicts (full reasoning in
    targeted_review_arc_067_boredom/synthesis.md):
      - R1 PRIMARY MECHANISM = engagement-rate aversive accumulator
        (functional-emotion lineage). MAC meaning-component as
        secondary parallel channel reusing existing z_goal substrate
        (SD-012 / MECH-308). ARC-068 opportunity-cost overlap is real
        but on different layer (E3 score vs z_harm_a valence).
      - R2 BIOLOGY SUPPORTS THE TIMESCALE SPLIT (acute restlessness
        ~minutes / tens of episodes vs chronic anhedonic flatness
        ~episodes / sessions). FLAGGED to governance for slot-split
        decision; child-MECH design proposal should default to
        two-child split (MECH-Xa acute / MECH-Xb chronic) with
        explicit cross-talk specification, with single-child unified
        as cost-controlled fallback. Decision deferred until after
        ARC-068 lit-pull (Pull 3 of 3) lands.
      - R3 ROUTE THROUGH z_harm_a (SD-011 affective stream / AIC
        analog), driven by Danckert 2018 AIC-deactivation finding and
        Wilson 2014 self-shock exchangeability. Architecturally
        cleanest option of the two pre-registered; preserves low-
        arousal-but-action-recruiting signature without coupling to
        SD-037 broadcast override.
      - R4 ENGAGEMENT-RATE ESTIMATOR INPUTS narrowed to three
        executive-engagement proxies (commit transitions per episode,
        E3 deliberation depth, residue-write rate). DROP novel-
        observation count as primary input -- van Tilburg 2017
        meaning-seeking-not-stimulation-seeking signature rules it
        out and conflation with MECH-314a striatal novelty must be
        avoided. Novelty may contribute as secondary positive
        engagement signal but not as primary boredom estimator.
      - R5 ARC-067 ABLATION MAPS ONTO APATHY (Husain-Roiser
        frontostriatal effort-allocation failure), specifically NOT
        anhedonia (which maps onto MECH-295 liking-channel). Anchor
        only -- pathology mapping deferred to future
        psychiatric_failure_modes.md governance pass.

    GOVERNANCE CALL 2026-05-16: two-child split APPROVED (MECH-330
    acute / MECH-331 chronic). ARC-068 Pull 3 has landed (2026-05-10);
    ARC-068 collapses into MECH-320 per ARC-068 lit-pull R3 verdict.
    Child MECHs registered: MECH-330 (acute restlessness accumulator)
    and MECH-331 (chronic anhedonic flatness substrate). One-way cross-
    talk specification: MECH-330 sustained activation -> seeds MECH-331.

    What this pull does NOT settle: engagement-rate estimator
    quantitative calibration, MAC meaning-component channel integration
    shape (additive sum / max / multiplicative gate -- child-MECH
    proposes default, discriminated experimentally), boredom-impulsivity
    failure-mode literature (deferred to follow-up pull if pathological
    ARC-067-overfiring becomes governance topic), alternative single-
    component FEP-flavoured information-gain-failure accounts (worth
    flagging if subsequent validation experiments resist two-component
    reading).
  notes: >
    Cross-reference family: ARC-067 is the aversive-side companion of
    ARC-066 (positive-side vigor). The two are conceptually distinct
    -- ARC-066 says "capacity creates a push to act"; ARC-067 says
    "low engagement creates a pull away from passivity" -- but they
    co-locate at the same downstream (action-bias on E3 score). Future
    Q-claim: are ARC-066 and ARC-067 a single mechanism with two faces
    (one signed scalar) or genuinely separate (asymmetric responses to
    capacity-rise vs engagement-fall)? Phenomenology suggests
    asymmetric -- restlessness when bored feels different from
    energised when surplus -- but this is open.

    Cross-reference: psychiatric_failure_modes.md should later add a
    section on ARC-067 ablation as a model of clinical anhedonia /
    abulia / catatonic flatness. Cross-link with MECH-302 suffering
    derivative comparator (negative valence drop produces relief);
    ARC-067 is the inverse architectural shape (positive aversive
    accumulation produces action pressure).
  location: docs/architecture/non_deficit_action_drives.md

- id: ARC-068
  title: "opportunity_cost_no_op_penalty (waiting accumulates forgone-reward cost; passive trajectories pay for time consumed). Architectural commitment: trajectories that consume time without progress are not free in E3 scoring; the score function carries an additive cost on no-op / repeated-stationary candidates that scales with the agent's average expected reward rate. Companion to ARC-066 (capacity-to-vigor) and ARC-067 (idle aversion). Existing dACC switch_value (SD-032b / Kolling 2015 foraging) is the closest current substrate but covers a different framing (when to switch FROM the current option to alternatives), not the always-on cost of choosing the null action."
  claim_type: architectural_commitment
  subject: action.opportunity_cost_no_op_penalty
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-603g-624c-651a_2026-06-07#V3-EXQ-624c"
      as_of: 2026-06-07
      verdict: non_contributory/substrate_conditional
  implementation_phase: v3
  v3_pending: true
  pending_retest_after_substrate: true  # [2026-06-08 governance] survival-leg cohort gated on escape-affordance-bridge substrate (failure_autopsy_V3-EXQ-603h_2026-06-08)
  claim_level: architectural
  registered_utc: "2026-05-10"
  depends_on: []
  functional_restatement: >
    ARC-068 registers the architectural slot for an opportunity-cost
    penalty on passive / no-op trajectories at the E3 selection layer.
    The user-observed phenomenology: the felt pull of "I am wasting
    time" when capacity is high and nothing specific is being pursued.
    REE's E3 currently scores trajectories on expected harm and benefit;
    a trajectory that simply stands still pays no cost for the time it
    consumes, so when no benefit is predicted and no harm is present,
    standing still is a valid local minimum. Biology has a real cost on
    that local minimum (forgone reward accumulates while you wait).

    Biology anchors (preliminary; lit-pull required before child MECH
    design):
      - Niv et al. 2007 (PNAS) -- vigor as a function of average reward
        rate; the average reward rate ALSO acts as a discount on
        rest / waiting. Resting trades guaranteed-zero now for
        continued-zero later, against a positive expected baseline.
      - Kurzban et al. 2013 (Behav Brain Sci) -- effort and opportunity
        cost as the felt motivation to disengage from current activity
        when better alternatives become available.
      - Kolling et al. 2015 (Curr Opin Neurobiol) -- dACC foraging-
        value computation: switch FROM current option when its value
        falls below environmental average. ARC-068 generalises this
        to: cost is paid by the null action / no-op trajectory
        regardless of what the alternatives are.
      - Salamone et al. 2003 (Behav Brain Res) -- DA-depleted animals
        accept low-effort low-reward over high-effort high-reward;
        cost of effort is computed against an opportunity baseline.
      - Constantino & Daw 2015 -- foraging behaviour modelled with
        opportunity-cost discounting predicts patch-leaving better
        than fixed-discount RL.

    Architectural shape (to be fleshed at child MECH level):
    Likely a small additive cost on no-op / repeated-stationary
    trajectories at E3.score_trajectory, proportional to a tonic
    expected-reward-rate scalar. Composes naturally with ARC-066
    (which provides exactly that capacity scalar). When capacity is
    high (ARC-066 vigor scalar high), opportunity cost (ARC-068) is
    high, and passive trajectories are penalised more strongly. When
    capacity is low (depleted, harmed, mid-deliberation), opportunity
    cost is low and resting / waiting carries lighter penalty. This
    coupling is the falsifiable architectural prediction: opportunity
    cost should not be a constant.

    Architectural relations within REE:
      - SD-032b dACC adaptive control (SD-032b foraging_value via
        Kolling 2015): closest existing substrate. dACC foraging_value
        biases mode-switch when the current option falls below
        environmental average; ARC-068 biases the SCORE of any
        passive trajectory regardless of mode. Both contribute to
        anti-passivity pressure but at different architectural levels
        (mode-switch vs trajectory-score). They compose; not redundant.
      - ARC-066 tonic_vigor_coupling: ARC-068 pays out the cost that
        ARC-066 produces the gain on. The two are mathematical
        complements: ARC-066 adds to action scores; ARC-068 subtracts
        from passive scores. Some implementations may collapse them
        into a single signed scalar; the slot registers the
        commitment, child MECH chooses the parameterisation.
      - MECH-313 noise floor / MECH-260 anti-recency: orthogonal.
        Neither penalises no-op specifically; MECH-313 is uniform
        entropy and MECH-260 is action-recency.
      - MECH-216 predictive wanting: when wanting is present, no-op
        is already discouraged via the wanting score-bias toward the
        wanted candidate. ARC-068 fires when wanting is ABSENT and
        provides the cost-of-passivity that wanting otherwise would.
        Architecturally this means ARC-068 is most behaviourally
        load-bearing in the no-target / no-deficit regime where REE
        is currently inert.

    Falsifiable architectural prediction: same setup as ARC-066/067 --
    well-fed safe agent in familiar environment. With ARC-068 ablated,
    no-op and repeated-stationary trajectories accumulate equal score
    to active trajectories at long episode horizons; with ARC-068 ON,
    no-op trajectories accumulate cost monotonically with the tonic
    capacity scalar, and active trajectories win on score even when
    they predict equal or slightly worse benefit than no-op.

    What ARC-068 is NOT:
      - Not effort cost (Walton 2006, Salamone 2012: effort is the
        cost of expensive action, not the cost of inaction). Effort
        cost makes hard actions less attractive; opportunity cost
        makes inaction less attractive. Different sign on different
        candidates.
      - Not boredom (ARC-067): boredom is an aversive accumulator on
        the engagement-rate axis, recruited via the harm valence
        channel. Opportunity cost is a score-side cost on individual
        trajectories. They feel similar phenomenologically (both
        manifest as restlessness) but operate at different
        architectural levels.
      - Not a re-implementation of dACC foraging (SD-032b is the
        switch-when-current-option-bad framing; ARC-068 is the
        always-on cost-of-rest framing). SD-032b is task-conditioned;
        ARC-068 is task-independent.

    Companion slots: ARC-066 (positive-side vigor coupling) and
    ARC-067 (boredom aversive accumulator). The three jointly cover
    the non_deficit_action_drives architectural commitment.
  evidence_quality_note: >
    Registered 2026-05-10 alongside ARC-066 + ARC-067 from user
    phenomenology. Lit-pull required before child MECH design;
    boundary with SD-032b dACC foraging requires careful
    disambiguation at lit-pull time -- the literature treats them as
    closely related but not identical, and REE will need to pick a
    cleanly-discriminative implementation.

    Not yet experimentally tested. First validation experiment will
    be substrate-readiness diagnostic on a child MECH (TBD post lit-
    pull). Likely cleanly testable in any environment by adding a
    no-op action and measuring its frequency vs active actions
    across capacity regimes -- but a clean test requires SD-054 reef
    substrate or similar where engagement options are structurally
    rich.

    LIT-PULL UPDATE 2026-05-10:
    targeted_review_arc_068_opportunity_cost completed; six-paper
    cohort across theoretical RL derivation (Niv 2007), unified
    phenomenology (Kurzban 2013), dACC review (Kolling 2016), dACC-
    reinterpretation fMRI (Shenhav 2016), DA-pharmacology dissociation
    (Salamone & Correa 2003), human patch-foraging behaviour
    (Constantino & Daw 2015). Four R-verdicts:
    R1 ARC-068 vs SD-032b boundary -- SEPARATE-AT-ARCHITECTURE-VIA-
    KERNEL (long-window historical EMA for ARC-068; current-
    environmental scalar for SD-032b); architectural separation does
    not require clean substrate dissociation, which biology likely
    does not provide; the Shenhav-Kolling dACC dispute is engaged
    explicitly.
    R2 reward-rate kernel -- PRIMARY long-window historical EMA
    (Constantino & Daw 2015 decisive against TD; Niv 2007 theoretical
    anchor); SECONDARY hybrid current+historical fallback for non-
    stationary environments only if motivated by early validation.
    R3 ARC-066 + ARC-068 collapse -- COLLAPSE-AT-IMPLEMENTATION-LAYER
    LICENSED (Niv 2007 mathematical symmetry); slot-level registration
    kept separate to preserve design space for future psychiatric
    failure-mode dissociations (anhedonia, mania).
    R4 effort-cost vs opportunity-cost separation -- SEPARATE
    (Salamone & Correa 2003 dissociation: effort cost scales with
    action requirements, opportunity cost scales with elapsed time
    times reward history; both DA-mediated but compute different
    things). ARC-068 should NOT be absorbed into existing MECH-258 /
    SD-032b dacc_effort_cost machinery.
    Synthesis lives at evidence/literature/
    targeted_review_arc_068_opportunity_cost/synthesis.md; expected
    lit_conf 0.78-0.82 supports-direction-dominant (4 supports + 1
    mixed + 1 weakens).

    IMPLEMENTATION LAYER COLLAPSE DECISION 2026-05-16: ARC-068 child
    MECH collapses into MECH-320 (tonic_vigor_coupling_score_bias) per
    R3 verdict. MECH-320's w_passive term (additive cost on no-op /
    repeated-stationary trajectories, proportional to v_t EWMA scalar)
    IS the ARC-068 implementation. The R2 long-window historical EMA
    kernel is already specified in MECH-320 (same v_t scalar, same Niv
    2007 formalism). R4 effort-cost separation (ARC-068 should NOT
    collapse into MECH-258 / SD-032b dacc_effort_cost) holds; the
    collapse is specifically into MECH-320, not into effort-cost
    machinery. MECH-320 depends_on and notes updated. Slot-level ARC-068
    registration preserved to allow future re-splitting if anhedonia /
    mania dissociations motivate separate scalars (R3 note).

    [2026-06-03 governance / cluster autopsy failure_autopsy_604a-624a-630_2026-06-03] V3-EXQ-624a re-tagged non_contributory: C3 PASS (gate_product 1.0, v_t 0.05 fires) but action_density byte-identical between vigor-ON and vigor-OFF arms (ARM_1==ARM_0==0.865 per-seed) -- the vigor no-op penalty fires but does not change argmax. NOT a falsification of MECH-320/ARC-068 nor an ARC-068 supersession trigger. substrate_ceiling (selection-authority sub-type); pending_retest_after_substrate -- same substrate_queue entry as 604a (AMEND 'modulatory-bias-selection-authority'). Supersedes the 624 crash.
  notes: >
    Cross-reference family: ARC-066 + ARC-067 + ARC-068 form the
    non_deficit_action_drives cluster. ARC-068 is the score-side
    cost that lets ARC-066 produce a behavioural lift even in the
    no-target regime: ARC-066 alone biases action over no-op only if
    the score function ALREADY treats them as comparable, but a
    target-free trajectory has near-zero benefit by construction, so
    its score is the effort cost only. ARC-068 makes no-op cost more
    than zero, breaking the tie in favour of action.

    HYBRID-naming convention: title carries both REE-internal name
    (opportunity_cost_no_op_penalty) and biology anchor (forgone-
    reward cost / Niv average-reward-rate / Kurzban 2013).

    Open Q-claim worth registering at child-MECH time: is
    opportunity cost a function of CURRENT predicted reward rate,
    or of the agent's HISTORICAL average reward rate (slow EMA),
    or both? Niv 2007 uses average reward rate as a slow integrator;
    Constantino & Daw 2015 use it as a learned environmental
    statistic. Different implementations make different
    behavioural predictions in changing-environment scenarios.
  location: docs/architecture/non_deficit_action_drives.md

- id: ARC-069
  title: "policy_hierarchy_dynamic_regranularisation (parent commitment for dynamic policy-primitive granularity in hippocampal rollout). Architectural commitment: the unit-of-policy that the rule-apprehension layer (ARC-062) and the diversity-generation layer (ARC-065) operate on is itself a dynamic representation, not a fixed primitive. The granularity rescales based on simulation outcomes -- decomposed into finer primitives when a chunk fails to ground at execution / prediction resolution (ARC-070), composed into a single primitive when a sequence has been repeatedly grounded with consistent outcomes (ARC-071). Without this commitment, ARC-007 hippocampal proposals are flat action sequences at fixed grain, and the rule layer must apprehend rules at whatever grain the proposer happens to emit, which may be too fine (combinatorial explosion in rollout) or too coarse (chunks fail to map onto the actual state). Cluster parent for ARC-070 (decomposition) + ARC-071 (composition); registers the architectural commitment that primitives are dynamic before any mechanism work."
  claim_type: architectural_commitment
  subject: policy.hierarchy_dynamic_regranularisation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: "2026-05-10"
  epistemic_category: substrate_conditional
  depends_on: []
  what_would_answer: >
    NON-DEGENERACY PRECONDITIONS: (a) ARC-071 LEG 2 cleared -- chunks survive E3
    re-evaluation (use_e3_reselection_shortcircuit, landed 2026-08-01, not yet measured);
    (b) ARC-070 mid-execution reachability already confirmed (V3-EXQ-839); (c) a
    long-horizon repeating task run with both mechanisms live vs. an ARC-069-OFF control.
    None queued to date.

    CONFIRMING: OFF control still flat-grain at episode 1000; ON arm shows BOTH
    rollout-cost drop on repeatedly-successful sub-sequences (partially corroborated:
    V3-EXQ-810a/810b) AND re-decomposition + cost recovery under induced prediction
    failure (partially corroborated: V3-EXQ-844 C2 PASS).

    FALSIFYING: preconditions met, but ON arm shows no measurable compression despite the
    accumulator gate firing, and/or no re-decomposition despite a verified trigger -- the
    triggers fire individually but don't compose into the claimed cluster-level cost
    benefit.

    EXPLICITLY NOT FALSIFYING: MECH-321's downstream selection-quality failures
    (V3-EXQ-844 C1, 867 family) -- that's a claim about candidate quality, not about
    grain-rescaling itself.

    CROSS-REFERENCE: this is the cluster-level joint prediction, distinct from either
    child's own narrower claim -- see ARC-070/ARC-071 for their own what_would_answer.
  functional_restatement: >
    ARC-069 registers the architectural slot for dynamic re-granularisation
    of policy primitives. REE today has flat-grain trajectory proposals
    (ARC-007 hippocampal proposals are CEM-sampled action sequences at
    a fixed timestep grain). The rule-apprehension layer (ARC-062) and
    the behavioural-diversity-generation layer (ARC-065) both operate
    on these flat sequences. ARC-069 commits to a different
    architectural shape: what counts as a PRIMITIVE for both layers is
    itself a function of the rollout history, with two inverse
    operations (decomposition and composition) acting on the
    representation.

    The two child commitments are inverse moves on the same axis:
      - ARC-070 decomposition_on_prediction_failure: when an imagined
        chunk's predicted outcome fails to ground (high prediction
        error during rollout, or low MECH-269 V_s on the chunk's
        region), the chunk is broken into finer primitives in the
        rollout proposal stream. Trigger: simulation-side prediction
        failure. Direction: zoom in.
      - ARC-071 composition_via_repeated_grounding: sequences of
        primitives that have been repeatedly executed together with
        consistent outcomes are accumulated into a single primitive
        that the apprehension and diversity layers can treat
        atomically. Trigger: repetition count + outcome consistency.
        Direction: zoom out.

    Both child operations recruit the same machinery (the policy-
    primitive representation that ARC-062 reads + ARC-065 perturbs +
    ARC-007 emits trajectories from). ARC-069 names the joint
    architectural commitment; ARC-070 and ARC-071 name the inverse
    rescaling operations. They share the substrate but have asymmetric
    triggers (decomposition is fast, prediction-driven, simulation-
    side; composition is slow, repetition-driven, execution-side).

    Biology anchors (preliminary; lit-pulls required before child
    MECH/SD design, separate pulls for decomposition and composition
    literatures):
      - Hippocampal-prefrontal hierarchical control during planning
        (Badre & D'Esposito 2009 Nat Rev Neurosci rostro-caudal
        hierarchy; Koechlin & Summerfield 2007 cascaded prefrontal
        architecture).
      - Striatal action-sequence chunking (Graybiel 1998 Annu Rev
        Neurosci; Sakai et al. 2003 Curr Opin Neurobiol; Wymbs et al.
        2012 Neuron motor sequence chunking).
      - Event segmentation theory (Zacks et al. 2007 Psychol Bull --
        observation side; the imagination-side equivalent is what
        ARC-070 names).
      - Sutton et al. 1999 (Artificial Intelligence) options framework
        as the cleanest computational analog of dynamic policy
        hierarchy.
      - Botvinick 2009 (Trends Cogn Sci) hierarchical organisation of
        behaviour, including the chunk-based hierarchy as natural
        substrate.
      - McGovern & Barto 2001 (ICML) bottleneck-state subgoal
        discovery -- one mechanism for ARC-070-style decomposition.

    Architectural relations within REE:
      - ARC-007 strict (hippocampal proposals): currently flat action
        sequences. ARC-069 commits to a representation where the
        sequence units are themselves variable-grain. ARC-007's
        value-flat property is preserved -- the regranularisation
        operates on the SHAPE of proposals, not on their value.
      - ARC-062 rule_apprehension_via_gated_policy_heads: reads
        primitives. With ARC-069 instantiated, the rules being
        apprehended are over potentially-chunked primitives; rule
        identification is invariant to grain re-scaling. (Failure
        mode: rules apprehended at one grain that don't generalise
        to another grain. This may surface as a follow-on Q-claim.)
      - ARC-065 behavioral_diversity_generation: perturbs primitives.
        Diversity-generation must operate at whatever grain the
        primitives are at; chunked primitives reduce diversity-
        space but increase abstraction; finer primitives increase
        diversity-space but increase combinatorial cost.
      - MECH-269 V_s (per-stream / per-region verisimilitude): the
        most plausible TRIGGER signal for ARC-070 decomposition.
        Low V_s on a chunk's region means the chunk's predicted
        outcome is unreliable -- decompose to finer primitives that
        the agent's forward models CAN predict.
      - MECH-288 event_segmenter: already detects boundary events at
        fast/slow scales on the OBSERVATION stream. ARC-070 is the
        IMAGINATION-side analog -- segmentation of hypothetical
        chains. The two are likely related substrates with
        biologically-shared machinery (event segmentation theory
        treats observation and imagination segmentation as
        co-segmented in episodic memory).
      - MECH-292 / MECH-293 ghost-goal bank + waking probes: indexes
        anchors at fast/slow scales (per MECH-288), but anchors are
        point-keys, not chunks-with-internal-structure. ARC-069 +
        ARC-071 add structure-bearing primitives that anchors can
        seed.
      - MECH-163 dual systems (planned vs habitual): the
        habit/planned distinction is registered. ARC-071 names the
        TRANSITION mechanism -- planned chain repeated -> habit
        chunk -- which is currently un-substrated. The dual systems
        themselves are not the chunking machinery; they are
        downstream consumers of grain-appropriate primitives.

    Falsifiable architectural prediction (cluster-level): an REE
    agent without ARC-069 substrate, run for many episodes on a
    repeating task, will not develop coarser primitives -- every
    rollout in episode 1000 will still propose at single-action
    grain, paying the full combinatorial cost. With ARC-071 active,
    repeated success on a sub-sequence should compress that
    sub-sequence into a single primitive; rollout cost on that
    sub-sequence should drop. With ARC-070 active, prediction
    failure on a chunked primitive should re-decompose it back to
    finer grain; rollout cost rises locally, predictive accuracy
    recovers.

    What ARC-069 is NOT:
      - Not a representation-learning slot (no commitment to
        end-to-end gradient on the chunking representation; could
        be implemented via accumulator + replay-driven consolidation,
        per Sutton 1999 options + Graybiel 1998 striatal chunking).
      - Not a hierarchical-RL options framework re-implementation
        (Sutton 1999 is the analog, not the spec; biology distributes
        the substrate across hippocampal, prefrontal, and striatal
        sites in a way that doesn't cleanly map to the options
        framework's value-side).
      - Not committing to a specific child mechanism shape; ARC-070
        and ARC-071 are themselves slots whose mechanism design is
        deferred to lit-pull-driven phases.

    Companion architecture: ARC-070 (decomposition operation) +
    ARC-071 (composition operation). The three claims jointly
    register "policy primitives are dynamic" as architectural
    commitment.
  evidence_quality_note: >
    Registered 2026-05-10 from user observation about hippocampal
    rollout chaining: "When looking at chaining behaviours together
    for hippocampal rollout we are working on there being behavioural
    diversity to chain together but when imagining a chain
    presumably the imagined chains may need splitting up into more
    chained behaviours or sometimes a number of chained behaviours
    together being realised to better be represented by a single
    behaviour (or rather than behaviour I suppose we should be
    thinking as policy or apprehended rule)."

    Phenomenology-as-substrate-search is an explicit project
    methodology (cf. ARC-066/067/068 non_deficit_action_drives
    family registered 2026-05-10 from felt-drive phenomenology;
    INV-049 sleep; MECH-302 suffering derivative comparator;
    MECH-309 logical-necessity argument for rule apprehension).

    Biology anchors above are preliminary -- a targeted lit-pull on
    each child operation's literature is REQUIRED before child MECH
    design (per project rule: biology-before-formal-definitions).
    Two separate pulls anticipated:
      - targeted_review_arc_070_decomposition: hippocampal-prefrontal
        hierarchical control during planning, event segmentation
        theory imagination side, bottleneck-state subgoal discovery.
      - targeted_review_arc_071_composition: striatal sequence
        chunking, motor program consolidation, options framework as
        formal analog, repetition / outcome-consistency triggers.

    Not yet experimentally tested at the cluster level. UPDATE 2026-08-08
    (thought-digestion currency check): the "cluster is empty" framing above is
    now stale -- both ARC-070 (substrate built 2026-07-24) and ARC-071 (substrate
    built 2026-07-22) have since landed real interface contracts, default-off
    flags, and partial evidence (ARC-070: V3-EXQ-839 mid-execution reachability +
    V3-EXQ-844 C2 PASS; ARC-071: V3-EXQ-810a/810b LEG 1 PASS, LEG 2 fix landed
    2026-08-01 but not yet measured). What remains genuinely untested is
    narrower than "empty": the cluster-level JOINT prediction (both operations
    operating together over a long repeating task, see this claim's own
    what_would_answer), which has never been queued.
  notes: >
    Cross-reference family: ARC-069 + ARC-070 + ARC-071 form the
    policy_primitive_granularity cluster. Registered together on
    2026-05-10 from a single observation by the project author
    (Daniel) that REE's hippocampal-rollout substrate currently
    treats primitives as flat-grain, with no machinery for
    re-granularisation either direction.

    Asymmetry note (from user observation + draft preregistration):
    decomposition (ARC-070) and composition (ARC-071) feel like
    inverse operations and computationally treat them as a unified
    options-framework machinery (Sutton 1999), but their TRIGGERS
    are asymmetric -- decomposition is fast, prediction-driven,
    simulation-side; composition is slow, repetition-driven,
    execution-side. Two slots not one. Whether they share substrate
    or are separate is a child-MECH question; the slot registration
    keeps both possibilities open.

    HYBRID-naming convention (parallel to ARC-065): the title
    carries both REE-internal architectural name
    (policy_hierarchy_dynamic_regranularisation) and literature
    anchor cluster (Badre 2009 hierarchical control / Sutton 1999
    options / Graybiel 1998 chunking / Zacks 2007 event segmentation).

    Cross-link: psychiatric_failure_modes.md should later add a
    section on regranularisation pathology as a model of (a)
    obsessive-compulsive over-decomposition (chunks never compose;
    every routine action is re-planned at primitive grain), (b)
    autistic insistence-on-sameness (chunks form but cannot be
    re-decomposed when context demands different grain), and (c)
    skill / motor-learning impairments (chunking machinery itself
    impaired; sequences never compress).

    Open Q-claim worth registering at child-MECH time: how does
    the rule-apprehension layer (ARC-062) deal with rules learned
    at one grain that need to apply at another? Hierarchical
    rules are likely apprehended at multiple grains in parallel
    (Botvinick 2009); the child MECH design will need to address
    grain-invariant rule identification.
  location: docs/architecture/policy_primitive_granularity.md

- id: ARC-070
  implementation_note: >-
    SUBSTRATE BUILT 2026-07-24 in ree-v3 (ree_core/policy/policy_decomposition.py), default
    OFF and bit-identical when OFF. MECH-321 (below) is the sole registered child mechanism;
    this entry's implementation IS MECH-321's implementation. Loud precondition:
    use_policy_decomposition=True requires hippocampal.use_event_segmenter=True (MECH-321
    reads MECH-288's boundary_on(stream="rollout", ...)). Master switch
    REEConfig.use_policy_decomposition; decomposition_vs_threshold 0.4 (matches this
    codebase's existing vs_gate_e1/e2_threshold convention), decomposition_depth_cap 3
    (mirrors ARC-071's chunk_max_depth default). 15 interface contracts in
    ree-v3/tests/contracts/test_arc070_policy_decomposition.py. NO EXPERIMENTAL EVIDENCE AT
    BUILD TIME (see the 2026-08-09 mechanism-fires reconciliation block at the end of this note)
    -- v3_pending stays True; a substrate-readiness diagnostic (does decomposition fire and
    correctly withhold/replace a chunk candidate) is queued separately, and the full
    ARM_0/ARM_1/ARM_2 discriminative-pair design from MECH-321's functional_restatement is a
    LATER experiment. THE ASYMMETRY WITH ARC-071: unlike ARC-071's MECH-094-strict write
    refusal (hallucinated content must never mint a chunk it can commit to), ARC-070
    legitimately fires under hypothesis_tag=True during rollout deliberation -- that is its
    PRIMARY phase, not an exception. PolicyDecomposition.evaluate() has no residue-write side
    effect at all, so there is nothing to refuse; hypothesis_tag is a diagnostics label only.
    Closes the family loop the 2026-07-22 ARC-071 landing left open: a chunk can now form,
    dissolve, AND be re-decomposed under prediction failure.
    R5 BOTTLENECK TRIGGER MODE ADDED 2026-07-25 (see MECH-321 implementation_note for the
    full description). REEConfig.decomposition_trigger_mode selects "vs_boundary" (R1,
    default) vs "bottleneck" (R5, ARM_2: decompose at bottleneck states regardless of V_s
    via an online incremental McGovern-Barto diverse-density accumulator on a quantised
    z_world region key). Default OFF / bit-identical; v3_pending unchanged (substrate, not
    evidence). Unblocks the three-arm ARM_0/ARM_1/ARM_2 dissociation (ARM_0/ARM_1 = V3-EXQ-816).
    [2026-07-26 governance, V3-EXQ-816/820 FAIL cluster; failure_autopsy_816-820-policy-decomposition-cluster_2026-07-26,
    confirmed]: both runs FAILed on a shared unmet precondition (the env never produced low-V_s states in the
    measure window after 40 warmup episodes), not on the decomposition machinery itself -- both positive controls
    passed and the machinery fired abundantly (frac 1.0). non_contributory for ARC-070/MECH-321; stays
    candidate/v3_pending. GOV-FANOUT-1 fired: opened a 2-hypothesis discrimination portfolio
    (H-env-underdrives-uncertainty / H-vs-proxy-saturation) rather than one sequential re-pose.
    [2026-07-26 governance, V3-EXQ-816b FAIL (P-A env-axis leg); failure_autopsy_V3-EXQ-816b_2026-07-26, confirmed]:
    harshening (env_drift_interval->3, world_rule_shift) moved forward-PE toward but not across its discrimination
    floor (0.0086 vs 0.01), so zero low-V_s steps resulted -- informative but non-eliminating; bears_on only, no
    claim weight. A further dose-escalation letter is recommended before the campaign concludes.
    [2026-07-26 governance, V3-EXQ-816c PASS (P-B measurement-axis leg); failure_autopsy_816c-822_2026-07-26,
    confirmed]: found forward-PE heterogeneous while region-V_s stayed flat (spearman 0.083) -- fairly strong
    direct support for H-vs-proxy-saturation (region-V_s saturates near 1.0 in a trained encoder, decoupled from
    forward-PE). Left alive (not yet eliminated) pending the env-axis ladder; bears_on only, no claim weight.

    [2026-08-08 governance, backfill -- note was stale since 2026-07-26]: the campaign continued past 816c.
    V3-EXQ-830 (2026-07-29, confirmed failure_autopsy_V3-EXQ-830_2026-07-29) found the deeper issue:
    E3Selector.post_action_update unconditionally destroys _committed_trajectory every tick before ARC-070's
    R4 mid-execution hook (which needs cross-tick persistence) can ever fire -- a genuine structural gap, not
    a re-probeable measurement question. Routed /implement-substrate for a persistent commit handle.
    V3-EXQ-839 (2026-07-30, confirmed failure_autopsy_V3-EXQ-839_2026-07-30) confirmed the fix makes R4
    genuinely reachable (415 evaluations ON, 0 OFF) and routed a real evidence successor, closing the
    reachability question this claim's R4 verdict above was blocked on. GOV-DIAG-1's fan-out-recurrence
    audit flagged the 816b/816c/816d/830/839 chain as circling on 2026-08-08 -- but by that date the chain
    had already been through the RE-POSE (persistent commit handle, structural fix) / REFUSE (no blind
    re-queue) / BUILD (SD-084 substrate) pattern the audit exists to force; the flag was stale, not a fresh
    circling. See MECH-321's own implementation_note for the harm-valence-wiring and matched-seed-pool
    follow-on (V3-EXQ-844/867/867a/867b) this reachability fix then made possible to test.

    [2026-08-09 governance reconciliation, mechanism-fires evidence line -- resolves the twice-deferred
    2026-08-08 flags in what_would_answer below]: ARC-070's NARROW architectural claim is that the
    decomposition operation FIRES and re-segments under a genuine trigger -- distinct from MECH-321's
    separate, broader FUNCTIONAL claim that decomposition IMPROVES task outcomes (repeatedly FAILING:
    V3-EXQ-844 C1, and the V3-EXQ-867/867a/867b family). Three experimental touchpoints now positively
    bear on the narrow fires claim: (1) V3-EXQ-839 (confirmed failure_autopsy_V3-EXQ-839_2026-07-30) --
    the R4 mid-execution hook is genuinely REACHABLE, 415 evaluations ON vs a structural 0 OFF, with a
    bit-identical negative-control seed tier; (2) V3-EXQ-844 C2_FORWARD_PE_CORROBORATES (non-load-bearing
    but PASSED; confirmed failure_autopsy_V3-EXQ-844_2026-08-01) -- aborting a stale macro measurably
    lowers forward-PE afterward, i.e. the mechanism does informationally what it is architecturally built
    to do (what_would_answer condition (a)); (3) V3-EXQ-904 (PASS, evidence_direction=supports,
    claim_ids=[ARC-070], 2026-08-08) -- the trigger fires SELECTIVELY (decomposes when region_vs <
    threshold AND when the real MECH-288 boundary detector fires on genuine rollout latents [C_BOUNDARY_LIVE,
    the one load-bearing non-forced criterion: 180 real boundary fires drove 180 decompositions], silent
    above threshold), yields finer tiles, respects the depth cap, and OFF is a structural zero. On the
    strength of these the narrow mechanism-fires / OPERATION claim is now EVIDENCED (no longer "no
    evidence"). TWO LEGS REMAIN SEPARATELY OPEN, which is why v3_pending STAYS True and nothing is promoted
    here: (i) 904's own scope_note is explicit that it does NOT confirm the trigger fires under NATURAL
    prediction failure in the live env -- that path is env-blocked by V_s saturation (V3-EXQ-816/830);
    and (ii) MECH-321's task-benefit claim is separately unresolved and currently failing. v3_pending
    gates those two legs, not the fires claim. This is an evidence-picture correction only; the claim
    remains cleanly separated from MECH-321 (the what_would_answer EXPLICITLY-NOT-FALSIFYING clause already
    drew the line), so no /claim-synthesis re-split was needed.
  title: "policy_decomposition_on_prediction_failure (zoom-in / re-segment imagined chains when chunks fail to ground at execution resolution). Child of ARC-069 dynamic-regranularisation parent commitment. Architectural commitment: when an imagined chunk's predicted outcome diverges from what its sub-elements would predict (or when MECH-269 V_s on the chunk's region drops), the chunk is broken into finer primitives in the rollout proposal stream. Trigger: simulation-side prediction failure. Direction: zoom in. Phenomenology anchor: 'I planned go-to-the-kitchen; turns out I need to plan the steps.'"
  claim_type: architectural_commitment
  subject: policy.decomposition_on_prediction_failure
  polarity: asserts
  status: candidate
  pending_retest_after_substrate: true
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-08-09
    needs_review: false
    evidence:
      from: "v3_exq_904_arc070_decomposition_trigger_selectivity_20260808T201150Z_v3"
      as_of: 2026-08-08
      verdict: "supports/standard -- narrow mechanism-fires / OPERATION claim evidenced (decomposition re-segments selectively; V3-EXQ-839 reachability + V3-EXQ-844 C2 forward-PE + V3-EXQ-904 selectivity). Reading stays candidate/v3_pending because the natural-prediction-failure trigger (env-blocked, V3-EXQ-816/830) and MECH-321's task-benefit leg remain separately open. See implementation_note 2026-08-09 reconciliation."
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: "2026-05-10"
  depends_on:
    - ARC-069     # parent dynamic-regranularisation commitment
  functional_restatement: >
    ARC-070 names the decomposition direction of the dynamic
    policy-primitive regranularisation cluster. When a chunked
    primitive in an imagined trajectory fails to ground at the
    resolution required for execution (its predicted outcome is
    unreliable, or its sub-elements would diverge), the rollout
    representation re-segments the chunk into finer primitives.
    The agent does not commit to executing the chunked plan if it
    cannot predict the chunk's outcome with adequate confidence;
    instead the chunk is unpacked and the agent plans at finer
    grain.

    Trigger candidates (to be selected at child-MECH design time):
      - MECH-269 V_s drop on the chunk's region: low region V_s
        means the agent cannot reliably predict outcomes in this
        region of state space; chunked primitives over this region
        cannot ground; decompose. This is the cleanest existing-
        substrate trigger.
      - E2 forward-model disagreement: the chunked primitive's
        predicted outcome (computed at chunk-grain) differs from
        what stepwise E2 forward rollout would predict (computed
        at primitive-grain). When the disagreement exceeds
        threshold, decompose.
      - Hippocampal completion-signal failure (MECH-105 / ARC-028):
        chunked primitives whose expected completion signal does
        not arrive on schedule.
      - Mid-execution prediction error: the chunk fired, the agent
        is now N steps in, and the next predicted state has
        diverged. Decompose the REMAINING chunk content.
    Likely a combination -- the lit-pull will arbitrate.

    Direction (zoom in): the rollout proposal stream emits a
    sequence whose units now correspond to finer-grain primitives.
    Downstream consumers (ARC-062 rule apprehension, ARC-007
    hippocampal selection) operate on the finer-grain stream
    transparently. Cost: rollout deliberation budget rises locally
    (more candidates per chunk-step). Benefit: predicted outcomes
    become reliable; the rule layer can apply rules over primitives
    it has reliable forward models for.

    Biology anchors (preliminary; lit-pull required before child
    MECH design):
      - Badre & D'Esposito 2009 (Nat Rev Neurosci) -- rostro-caudal
        prefrontal hierarchy; abstract intentions decompose into
        more concrete actions through a cascaded hierarchy.
      - Koechlin & Summerfield 2007 (Trends Cogn Sci) -- cascaded
        cognitive control architecture, formalising the
        decomposition direction.
      - Zacks et al. 2007 (Psychol Bull) -- event segmentation
        theory: same machinery segments observation and imagination
        streams; prediction error is the canonical trigger.
      - McGovern & Barto 2001 (ICML) -- bottleneck states as
        decomposition targets; one specific computational shape.
      - Pfeiffer & Foster 2013 (Nature) -- hippocampal forward
        path sweeps that successively elaborate trajectory paths
        as they encounter prediction-relevant cues.
      - Schapiro et al. 2017 (Phil Trans Roy Soc B) -- CLS
        hippocampal/cortical division of labour with pattern
        separation as the substrate for decomposition.

    Architectural relations within REE:
      - MECH-269 V_s primitive (Phase 1/2/3 substrate landed): the
        signal source. ARC-070 is a downstream consumer of V_s at
        the rollout-proposal layer (parallel to MECH-269b which
        consumes V_s at the cortical-rollout layer). Cross-link
        evidence_quality_note: ARC-070 is the SECOND major V_s
        consumer; MECH-269b's symmetric-V_s-gating commitment may
        constrain ARC-070's design choices.
      - MECH-288 event_segmenter (observation side): ARC-070 is the
        imagination-side equivalent. The two likely share machinery
        (event segmentation theory predicts this). Whether REE
        instantiates them as one mechanism or two is a child-MECH
        question.
      - ARC-007 hippocampal proposals: ARC-070 modifies the SHAPE
        of proposals (variable-grain segmentation), preserves the
        value-flat property.
      - MECH-094 hypothesis_tag: simulation-side substrate;
        ARC-070's decomposition fires during simulation; child MECH
        must respect MECH-094 (decomposition during waking action
        does not write residue / does not chunk via composition).

    Falsifiable architectural prediction: present an REE agent
    with a multi-step chunked primitive whose mid-sequence is
    placed in a region of high MECH-269 V_s drop (artificially
    induced). Without ARC-070, the agent commits to the chunk and
    executes it blind, paying the prediction-failure cost at
    execution. With ARC-070, the agent re-decomposes during
    rollout, refines the proposal at finer grain over the
    low-V_s region, and either finds a finer-grain plan that
    grounds (PASS) or fails-to-plan and aborts the trajectory
    (also PASS -- the architectural prediction is the
    decomposition fires, not that the alternative succeeds).

    What ARC-070 is NOT:
      - Not a re-implementation of MECH-288 event_segmenter; that
        operates on observation. ARC-070 operates on imagination.
        They likely share substrate but the substrate has not
        been registered as bidirectional.
      - Not a feedback-loop on V_s itself; ARC-070 reads V_s but
        does not update it (V_s update path remains MECH-269 Phase
        1/2/3 substrate).
      - Not a goal hierarchy slot; chunks are over POLICY
        primitives, not goals. Goal hierarchies are a separate
        architectural concern (cf. SD-015 z_resource +
        z_goal_snapshot in MECH-292/293).
  evidence_quality_note: >
    [2026-08-21 governance, gov-20260821-0203, confirmed failure_autopsy_V3-EXQ-938_2026-08-20.json]: V3-EXQ-938 is the first run in the ARC-070/MECH-321 chain to reach a verdict at all (816/820 died on a low_vs_steps occupancy gate at 0.0/5.0; 816b/816d saturated the environment axis within 0.0007). It is cleanly executed: rate_match_ok true at a 1.85% arm gap with 0 seeds outside tolerance, aa_control_ok with max_abs_delta exactly 0.0, >=91 forced fires on both ON arms, ~277-287 boundary fires/arm, all 15 readiness preconditions met, non_degenerate true. Its result is a NULL, not a detected negative effect: harm delta -0.001338 with SE 0.002764 (t = -0.484), 19 seeds negative against 17 positive, and an observed |delta| that is 17.3% of the run's own 80%-power MDE (~0.007739). The pre-declared null (delta <= 0 within 1.0 x SE over >= 40 paired seeds) is satisfied exactly as written, so the run delivered the verdict it promised, scoped by its own statement to 'ARC-070's prediction-failure-selectivity leg AT THIS GRAIN'. The blanket weakens stamped on both claims is NOT applied, for two independent reasons. (1) ARC-070's own registered what_would_answer states that a failure of the DOWNSTREAM task-level benefit is 'EXPLICITLY NOT FALSIFYING' and belongs to MECH-321's separate narrower claim; 938's DV is a whole-episode mean harm signal, squarely that class, and its FALSIFYING clause (the mechanism does not engage when it architecturally should) is negated by the run, which shows the mechanism engaging throughout. (2) The same hypothesis-space question carries H-representation-axis as ALIVE ('forward-PE as currently computed is too coarse-grained to register the environment's actual uncertainty'), and 938's proximal readout -- secondary_fwd_pe_delta_yoked_minus_pe = -2.6e-05 -- is precisely that hypothesis's signature; a within-run rank normalisation does not fix a readout that is too coarse. So the run cannot discriminate 'selectivity is false' from 'the PE readout cannot locate the loci where decomposition would help'. Carry forward two readouts: C2_DECOMPOSITION_PER_SE_VS_OFF = -0.03681, roughly 27x the selectivity delta in the harmful direction (non-load-bearing, no SE reported, not adjudicated here) and the larger signal in the manifest; and substrate_stable_across_run = false, where all 132 cells share one hash 3a9826d0 but the stated substrate_commit 839ffe03 does not correspond to it, so this run cannot be mapped to a substrate commit -- recording-debt owed by any successor.

    Registered 2026-05-10 alongside ARC-069 + ARC-071 from user
    observation. Lit-pull required before child MECH design;
    targeted_review_arc_070_decomposition anticipated.

    The cleanest existing-substrate hook is MECH-269 V_s as
    trigger; this gives ARC-070 a natural integration point
    without inventing new V3 substrate. But the lit-pull may
    surface alternative or complementary triggers (E2-disagreement,
    completion-signal-failure, mid-execution PE) that are equally
    biologically supported. Mechanism design must arbitrate.

    Not yet experimentally tested. Validation will require an
    environment that can produce the high-V_s-drop-mid-chunk
    scenario; SD-054 reef substrate or any environment with
    region-conditioned predictability heterogeneity will work.

    Lit-pull addendum 2026-05-10
    (evidence/literature/targeted_review_arc_070_decomposition/):
    7 entries (Zacks 2007 PMID 17338600, Schacter Addis Buckner
    2008 PMID 18400923, Badre & D'Esposito 2009 PMID 19672274,
    Pfeiffer & Foster 2013 PMID 23594744, Koechlin & Summerfield
    2007 PMID 17475536, Schapiro et al. 2017 PMID 27872368,
    McGovern & Barto 2001 ICML). Aggregate lit_conf expected
    0.74-0.78 (mean per-entry confidence 0.76); direction
    6 supports + 1 mixed.

    Five verdicts (R1-R5):

    R1 trigger primary: PRIMARY = V_s drop on chunk's region
    (PE-driven, biologically anchored via Zacks 2007 PE-as-trigger
    framework + Schapiro 2017 hippocampal pattern-separation as
    candidate V_s substrate). E2 forward-model disagreement,
    completion-signal failure, and mid-execution PE are convergent
    secondary signals that may co-fire but should not be primary.
    Conf 0.78.

    R2 LOAD-BEARING: SHARED SUBSTRATE recommended -- ARC-070 should
    be implemented as a BIDIRECTIONAL EXTENSION of MECH-288
    event_segmenter (one mechanism, two input streams: observation
    and imagination), NOT a new module. Schacter / Addis / Buckner
    2008 supplies the empirical anchor (constructive-episodic-
    simulation core network is the SAME network for remembering
    past and imagining future events); Zacks 2007's framework is
    substrate-agnostic about the input stream's source. The R2
    architectural recommendation simplifies the substrate registry
    significantly: instead of two parallel decomposition mechanisms,
    REE gets one bidirectional event_segmenter handling both input
    streams, with MECH-269 V_s primitive feeding both via region-
    conditioned predictability. Falsifiable test: ablating MECH-288
    substrate should impair BOTH observation and imagination
    decomposition; if a clinical or computational dissociation is
    found, the bidirectional hypothesis falsifies. Conf 0.74.

    R3 hierarchy depth: MULTI-LEVEL hierarchy supported
    empirically (Badre & D'Esposito 2009 rostro-caudal gradient,
    3-4 levels minimum) and theoretically (Koechlin & Summerfield
    2007 cascade). Recursive decomposition (chunk -> sub-chunk ->
    primitive) with implementation depth cap at 3-4 levels
    suggested for tractability. Conf 0.78.

    R4 mid-execution decomposition: BOTH supported (pre-commitment
    AND mid-execution). Same V_s-trigger mechanism handles both
    phases; downstream effects gated by MECH-094 hypothesis_tag
    (pre-commit: no residue write; post-commit: residue write
    enabled). Mid-execution branch may serve double duty as
    reactive-replanning substrate -- candidate child-Q-claim.
    Conf 0.66 (lowest of five; thin direct empirical evidence).

    R5 bottleneck-state vs error-driven: DISTINCT mechanisms
    with overlapping outputs. ARC-070 primary trigger is
    PE-driven (V_s drop, biologically anchored); McGovern & Barto
    2001 bottleneck-state framing is a foil mechanism that does
    not biologically generalise. Bottleneck-aware secondary signal
    is permitted as offline / consolidation-phase analysis (may
    integrate with ARC-071 chunk-formation pull). Conf 0.74.

    Architectural consequences for child-MECH design:
      (a) primary trigger = V_s drop on chunk's region (R1);
      (b) BIDIRECTIONAL substrate with MECH-288 (R2 LOAD-BEARING);
      (c) recursive multi-level decomposition with depth cap (R3);
      (d) both pre-commit and mid-execution phases supported via
          same mechanism with hypothesis_tag-conditional downstream
          effects (R4);
      (e) PE-driven primary, bottleneck-aware secondary optional
          (R5).

    ARC-070 will be the SECOND major V_s consumer after MECH-269b;
    the two readers can have independent thresholds but share the
    V_s primitive substrate. Validation: discriminative-pair
    experiment comparing ARC-070 OFF baseline vs V_s-drop primary
    vs bottleneck-state primary, predicting the V_s-drop arm
    decomposes pre-commitment when V_s indicates unreliability and
    the bottleneck arm decomposes only on repeated structures.
    See evidence/literature/targeted_review_arc_070_decomposition/
    synthesis.md for full reasoning and falsifiable predictions.

    [2026-05-10 first child mechanism registered]: MECH-321
    policy.decomposition_via_event_segmenter encodes the R1-R5
    verdicts as the rollout-side consumer of MECH-288 boundary
    pulses. status: candidate / v3_pending. Substrate-readiness
    prerequisite: MECH-288 substrate (event_segmenter.py with
    input_stream label per the bidirectional-consumer extension)
    must land first. Discriminative-pair validation experiment
    specified in MECH-321 functional_restatement; deferred until
    substrate lands.
  notes: >
    Cross-reference: ARC-070 is the inverse of ARC-071
    (composition). They share the parent commitment (ARC-069) but
    have asymmetric triggers -- ARC-070 fires on prediction
    failure (fast, V_s-driven, simulation-side); ARC-071 fires on
    repeated success (slow, accumulator-driven, execution-side).

    Whether the two operations share substrate (one mechanism
    operating in both directions, like Sutton 1999 options) or
    are mechanistically separate (different triggers, different
    effector pathways) is open. The slot registration keeps both
    possibilities; child MECH design picks.

    HYBRID-naming convention: REE-internal name
    (decomposition_on_prediction_failure) + literature anchors
    (event segmentation imagination side / Badre & D'Esposito
    hierarchical control / Pfeiffer-Foster hippocampal forward
    sweeps).

    Lit-pull verdict R2 (post-2026-05-10) adds Schacter / Addis /
    Buckner 2008 to the primary anchor cluster as the LOAD-BEARING
    empirical anchor for shared substrate between observation-side
    and imagination-side processing.
  location: docs/architecture/policy_primitive_granularity.md
  epistemic_category: standard
  what_would_answer: >
    NON-DEGENERACY PRECONDITIONS: use_policy_decomposition + hippocampal.use_event_segmenter
    both live; the persistent commit handle (SD-084) present so the R4 mid-execution hook is
    genuinely reachable (confirmed per V3-EXQ-839: decomp_n_evaluated_midexec > 0 in
    ARM_HANDLE_ON, structural zero in ARM_HANDLE_OFF); a genuine mid-sequence high-MECH-269-V_s-drop
    region actually induced, not merely configured; each arm (WITH/WITHOUT ARC-070)
    independently commits at least one multi-action program.

    CONFIRMING: with preconditions met, the WITH-ARC-070 agent's mid-execution decomposition
    hook actually fires (decomp_n_evaluated_midexec/decomp_n_decomposed_midexec >0,
    distinguishable from the WITHOUT arm's structural zero), AND either (a) yields a
    finer-grain re-plan that grounds -- operationalised as measurable forward-PE reduction
    following the re-decomposition/abort (already measured and PASSED by V3-EXQ-844's
    C2_FORWARD_PE_CORROBORATES) -- or (b) fails to find a grounding re-plan and aborts the
    trajectory. Per the claim's own registered prediction, EITHER (a) OR (b) confirms -- the
    architectural commitment is that decomposition FIRES under a genuine trigger, not that the
    alternative succeeds.

    FALSIFYING: with the same preconditions satisfied, the WITH-ARC-070 agent nonetheless
    commits blind and executes the ungrounded remainder at a rate statistically
    indistinguishable from the WITHOUT-ARC-070 arm, even though its own trigger condition
    (V_s drop / MECH-288 boundary pulse) is genuinely met -- the mechanism does not engage
    when it architecturally should.

    EXPLICITLY NOT FALSIFYING: a failure of the DOWNSTREAM task-level benefit (decomposition
    fires but does not reduce harm/improve reward, as V3-EXQ-844's C1 and the
    V3-EXQ-867/867a/867b family found) does not by itself falsify ARC-070 -- that is
    MECH-321's separate, narrower functional claim about the QUALITY of harm-aware selection
    among re-tiled candidates, not about whether re-segmentation occurs at all.

    [2026-08-08 thought-digestion, GOVERNANCE FLAG]: two independent pieces of evidence now
    positively bear on ARC-070's own narrow mechanism-fires claim, distinguishable from
    MECH-321's repeatedly-failing task-benefit claim -- V3-EXQ-839 (reachability confirmed,
    415 evaluations ON vs 0 OFF, clean negative control) and V3-EXQ-844's C2
    (non-load-bearing but PASSED: aborting a stale macro measurably lowers forward-PE
    afterward, i.e. condition (a) above). This mechanism-fires-vs-mechanism-helps distinction
    is not currently drawn anywhere in this claim's implementation_note, which still reads
    "no experimental evidence yet" for the parent claim. Surfaced to governance/WORKSPACE_STATE
    for a pass deciding whether ARC-070 should carry an explicit (short of shown) evidence
    line separate from MECH-321's status -- not resolved here.

    [2026-08-08 governance, V3-EXQ-904, PASS, evidence_direction=supports]: a THIRD piece of
    evidence on the same mechanism-fires narrow claim (alongside V3-EXQ-839/844 named above) --
    the decomposition trigger fires selectively on the boundary condition it is designed to fire
    on (manipulation checks pass: fires when region_vs < threshold, silent when above). Applied
    as-is; still not resolved into a formal evidence line separate from MECH-321's status (same
    deferred reconciliation as the 2026-08-08 thought-digestion flag above). SEPARATELY: this
    cycle's non-outcome-findings skim of the driver
    (ree-v3/experiments/v3_exq_904_arc070_decomposition_trigger_selectivity.py) found the
    manifest's top-level `outcome` ANDs all 5 criteria together but only labels 1 of 5
    `load_bearing: true`, with no `combination_rule` field documenting this (unlike sibling
    drivers, which all declare one) -- did not affect THIS run's PASS (all 5 criteria passed),
    but misrepresents which criteria actually decide PASS/FAIL for future runs of this driver.
    Flagged for /failure-autopsy per user decision (not autopsied this cycle; see WORKSPACE_STATE).

    [2026-08-09 reconciliation APPLIED]: the twice-deferred mechanism-fires evidence line (the 2026-08-08
    thought-digestion flag and the V3-EXQ-904 restatement above) is now recorded in this claim's
    implementation_note (2026-08-09 governance reconciliation block) and its live_status.evidence: the
    narrow fires / OPERATION claim reads EVIDENCED across V3-EXQ-839/844-C2/904, while the
    natural-prediction-failure trigger (env-blocked) and MECH-321's task-benefit leg stay separately open,
    so v3_pending is UNCHANGED. Separately, the V3-EXQ-904 combination_rule / load_bearing manifest
    discrepancy noted above remains routed to /failure-autopsy and is untouched by this evidence-line pass.

- id: ARC-071
  implementation_note: >-
    SUBSTRATE BUILT 2026-07-22 in ree-v3 (ree_core/policy/policy_chunking.py), default OFF and
    bit-identical when OFF. Both registered child operators landed together: MECH-323 formation
    (ChunkAccumulator) and MECH-324 maintenance (ChunkLibrary), plus the MECH-322 sleep-replay
    carve-out as a separate method behind a separate flag. Master switch REEConfig.use_policy_chunking;
    parameter defaults are the registered MECH-323/MECH-324 suggested defaults verbatim
    (R_min 20, W 100, F_low 0.15, F_high 0.45, evaluative margin 0.05, chunk size 2-5, depth cap 3,
    C_min 5, T_dissolve 50, N 75). Chunks reach the ARC-007 proposal pool as single atomic
    value-flat Trajectories under a further default-OFF knob (use_chunk_proposal_injection);
    value_tag is provenance metadata only, so ARC-007 strict is preserved. MECH-094 is STRICT on
    the default write path (record_step refuses hypothesis_tag=True at any parameter setting);
    the MECH-322 path ANDs all three conditions and fails closed on each. 25 interface contracts
    in ree-v3/tests/contracts/test_arc071_policy_chunking.py. NO EXPERIMENTAL EVIDENCE YET --
    v3_pending stays True; the substrate-readiness diagnostic (does the accumulator fire at all)
    is queued separately. NOTE the accumulator's evaluative gate is RELATIVE to a running
    baseline, so a validation task must supply outcome CONTRAST, not merely repetition.
    DISTINCT FROM MECH-477/SD-081 (allocation; built in parallel the same day): ARC-071 is the
    TRANSFER mechanism. MECH-163 presupposes both and specifies neither.
    FLAG INVENTORY EXTENDED 2026-07-27 (ree-v3 6c3e67e): a further default-OFF sub-switch
    REEConfig.use_chunk_dissolution_retention (with REEConfig.chunk_reacquisition_repetition_factor
    = 0.25) nested under use_chunk_maintenance makes the MECH-324 DISSOLVED state DORMANT rather
    than an absorbing tombstone -- see the MECH-324 implementation_note for the correction and its
    Barnes 2005 / Bouton 2012 grounding -- taking the interface-contract count in
    ree-v3/tests/contracts/test_arc071_policy_chunking.py up by 10 (the C10 block, 14 collected
    cases) at that landing.
  title: "policy_composition_via_repeated_grounding (zoom-out / chunk a sequence of primitives into a single atomic primitive after repeated successful execution). Child of ARC-069 dynamic-regranularisation parent commitment. Architectural commitment: sequences of policy primitives that have been repeatedly executed together with consistent outcomes are accumulated into a single primitive that the rule-apprehension and diversity-generation layers can treat atomically. Trigger: repetition count + outcome consistency (slow accumulator). Direction: zoom out. Phenomenology anchor: 'making coffee was once seventeen motor acts; after a thousand repetitions it is one unit.' Names the TRANSITION mechanism (planned -> habitual chunk) that MECH-163 dual systems presupposes but does not specify."
  claim_type: architectural_commitment
  subject: policy.composition_via_repeated_grounding
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-30
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-810a_2026-07-30
      as_of: 2026-07-30
      verdict: supports/standard -- chunk-accumulator readiness confirmed (see MECH-323/MECH-324 for detail)
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: "2026-05-10"
  depends_on:
    - ARC-069     # parent dynamic-regranularisation commitment
    - MECH-163    # dual systems substrate that ARC-071 names the transition for
  epistemic_category: substrate_conditional
  what_would_answer: |
    Two distinct legs -- do not conflate "the accumulator exists and fires"
    with "chunking buys anything." Both must independently clear before
    ARC-071's functional_restatement can be scored supports/weakens.

    LEG 1 (ANSWERED, PASS): does the chunk-formation/maintenance substrate
    itself fire under non-degenerate conditions? YES -- V3-EXQ-810a
    (2026-07-30, supports/standard, 7/8 seeds vs 0.75 pre-registered bar)
    and V3-EXQ-810b (seed-202 narrow-repertoire follow-up, supports/
    standard) confirm MECH-323/MECH-324 accumulate and maintain chunks with
    non-vacuous margins (2.4x-7.0x floors). This leg is closed; do not
    re-derive it.

    LEG 2 (OPEN, gated on a substrate wiring fix, itself unvalidated): does
    a formed chunk, once committed, actually SURVIVE across E3's internal
    re-evaluation cycles long enough to be selected/executed as one atomic
    move rather than silently re-deliberated at primitive grain? This is a
    PRECONDITION for the claim's own falsifiable prediction and has gone
    through two rounds of measurement defect:
      - V3-EXQ-841 (2026-07-31) found the between-tick readout
        mis-reporting persistence; fixed (ree-v3 278599a, flag
        use_persistent_committed_program_handle).
      - V3-EXQ-871 (2026-08-02) mis-scoped that flag across the WHOLE cell
        instead of the probe phase only, contaminating formation via
        RNG-coupled environment dynamics -- adjudicated
        measurement_test_design_defect, no evidence change
        (failure_autopsy_V3-EXQ-871_2026-08-02).
      - V3-EXQ-871a (2026-08-03), correctly scoped, confirmed fix (a)
        works, but exposed a SECOND, universal, deterministic gap (b): E3
        unconditionally recreates a fresh committed-trajectory object at
        every internal tick, 100% of ticks, both seeds
        (n_genuine_commits_identity_based == n_e3_ticks + n_episodes_probe,
        exact). Read non_contributory/competence_implementation_gap for
        MECH-090 (narrow to this wiring site; core gate-propagation
        mechanism unaffected). ARC-071 evidence UNCHANGED (exercised only
        as chunk-injection vehicle, same precedent as V3-EXQ-841 excluding
        MECH-323) -- still candidate/v3_pending.
      - Gap (b)'s fix landed (ree-v3 31ed78e, 2026-08-01, flag
        REEConfig.use_e3_reselection_shortcircuit, default False,
        bit-identical) and is tracked in substrate_queue.json (sd_id
        mech090-arc071-attick-persistent-handle-fix) as
        implemented_pending_validation -- as of this drafting (2026-08-07),
        NO experiment has run to confirm the fix restores cross-tick
        persistence (the redesigned DV n_genuine_commits/n_e3_ticks, per
        871a's own recommendation, has not been measured anywhere).

    LEG 3 (NEVER ATTEMPTED, not merely blocked): the claim's own
    falsifiable prediction -- rollout deliberation cost and behavioural
    latency measurably drop on chunked sub-sequences relative to an
    ARC-071-OFF control, once chunking reliably persists -- has NOT been
    queued or run under any experiment to date. V3-EXQ-834
    (budget-coupled-ceilings, FAIL, 2026-07-29) is the closest adjacent
    work and explicitly disclaims this: "classified diagnostic... excluded
    from confidence scoring on purpose," and its own driver docstring
    states that testing whether chunking's compute-savings pay off "needs
    the real horizon as the IV and is a separate, confounded, more
    expensive design" that was not built. No other queued or completed
    experiment measures rollout_cost, deliberation_cost, or
    behavioural_latency for ARC-071 chunked vs unchunked sub-sequences.

    NON-DEGENERACY PRECONDITION (mandatory guard, learned from the 871
    incident): any future retest of LEG 2 or design of LEG 3 MUST (a)
    confirm a chunk-commitment count clears a non-trivial floor (per
    871a's own precondition, n_persistent_present_ticks >= 10, worst seed)
    before trusting any downstream statistic, and (b) scope any
    persistence-related flag (use_persistent_committed_program_handle, and
    by direct analogy use_e3_reselection_shortcircuit) to the PROBE PHASE
    ONLY -- never across formation -- since 871 showed that flipping such
    a flag for the whole cell perturbs formation itself via RNG-coupled
    environment dynamics, producing a measurement artefact
    indistinguishable from a real substrate finding. LEG 2's verdict
    statistic must additionally be tick-rate-normalised
    (n_genuine_commits / n_e3_ticks, per 871a Section 6 recommendation #5)
    rather than raw mean segment length, which was shown confounded by
    e3_tick_rate (~4.4x variation between seeds, unrelated to whether the
    latch holds).

    CONFIRMED if, once (i) the AT-tick fix
    (use_e3_reselection_shortcircuit) is validated to restore persistence
    under the tick-rate-normalised DV, and (ii) a genuine ARC-071-ON vs
    ARC-071-OFF contrast experiment is run on a repeating sub-task under
    the guards above: rollout deliberation cost and/or behavioural latency
    drop measurably on chunked sub-sequences in the ON arm relative to OFF.

    FALSIFIED if, with persistence confirmed working post-fix and the
    contrast experiment run under the same guards, deliberation cost /
    latency show NO measurable drop despite chunks reliably forming and
    persisting -- this would show the compression exists mechanically but
    confers no computational benefit, undercutting the claim's stated
    Benefit clause.
  functional_restatement: >
    ARC-071 names the composition direction of the dynamic
    policy-primitive regranularisation cluster. When a sequence
    of primitives has been repeatedly executed together with
    consistent outcomes, the sequence is accumulated into a single
    primitive. The rule-apprehension layer (ARC-062) and the
    diversity-generation layer (ARC-065) subsequently operate on
    the chunked primitive atomically. Cost: rollout space loses
    flexibility within the chunked unit (sub-elements are no longer
    independently selectable). Benefit: rollout deliberation budget
    drops massively over chunked sub-sequences (the chunk is
    selected as one move, not N), and the rule layer can apprehend
    rules at the chunked grain that would have been combinatorially
    difficult at primitive grain.

    Trigger candidates (to be selected at child-MECH design time):
      - Repetition count + outcome consistency: the cleanest
        canonical pattern (Graybiel 1998 striatal chunking).
        Requires accumulator over (sequence -> outcome) pairs;
        chunk forms when (count exceeds threshold) AND (outcome
        variance is low).
      - Reward-rate-conditioned chunking: chunks form
        preferentially over sequences that reliably produce reward
        (Sakai 2003 motor sequence learning; depends on dorsal
        striatum DA signaling).
      - V_s-conditioned chunking: chunks form preferentially over
        sequences that produce reliable predictions (high MECH-269
        V_s sustained over the sequence). This is the V_s-positive
        analog of ARC-070's V_s-negative trigger.
      - Free-energy-minimisation (Friston): chunks form because
        they reduce expected free energy / surprise.
    Likely a combination, with primary trigger being
    repetition-count + outcome-consistency (Graybiel) augmented by
    V_s-positive condition.

    Direction (zoom out): the policy-primitive representation
    accumulates a new unit corresponding to the sequence. This
    unit is added to the candidate pool that ARC-007 hippocampal
    rollout proposes from, that ARC-062 rule apprehension reads
    over, and that ARC-065 diversity generation perturbs. The
    pre-existing primitive sequence remains available (chunk does
    not erase its sub-elements), so ARC-070 can decompose the
    chunk back into its sub-sequence under prediction failure.

    Critical relation to MECH-163 dual systems: MECH-163 names the
    PRESENCE of a habit system (dorsolateral-striatum-analog) and
    a planned system (hippocampal-vmPFC-analog) but does not name
    the TRANSITION mechanism by which planned chains become
    habitual chunks. ARC-071 is that transition mechanism. The
    habit system in MECH-163 reads chunks; ARC-071 is the
    machinery that produces them. Without ARC-071 the dual systems
    in MECH-163 are static configurations; with ARC-071 the
    division of labour between them is a continuous learnable
    distribution that shifts with experience.

    Biology anchors (preliminary; lit-pull required before child
    MECH design):
      - Graybiel 1998 (Annu Rev Neurosci) -- striatal chunking of
        action sequences, foundational reference. Also Graybiel
        2008 (Annu Rev Neurosci) updated review.
      - Sakai et al. 2003 (Curr Opin Neurobiol) -- chunking in
        motor sequence learning; pre-SMA / cerebellar / striatal
        contributions across phases of learning.
      - Wymbs et al. 2012 (Neuron) -- motor chunk formation as
        behavioural and neural signature.
      - Sutton et al. 1999 (Artificial Intelligence) -- options
        framework; the cleanest formal analog of policy
        composition. ARC-071 instantiates the options-discovery
        side; the options-execution side is partly already in
        REE via the planned/habitual distinction.
      - Botvinick 2009 (Trends Cogn Sci) -- hierarchical
        organisation of behaviour, with chunking as natural
        substrate.
      - Yin & Knowlton 2006 (Nat Rev Neurosci) -- dorsolateral
        striatum (habit / chunked) vs dorsomedial striatum
        (goal-directed / unchunked) division; the substrate
        target of ARC-071 composition.
      - Smith & Graybiel 2013 (Neuron) -- chunk dynamics: chunks
        form, dissolve, reform across learning timescales. ARC-071
        composition direction; ARC-070 decomposition direction.

    Architectural relations within REE:
      - MECH-163 dual_goal_directed_systems: ARC-071 names the
        transition mechanism between them. Habit chunks are the
        OUTPUT of ARC-071; planned-system trajectories are the
        INPUT.
      - ARC-007 hippocampal proposals: chunks become candidates in
        the proposal pool. The pool is no longer a flat action
        sequence but a mixed-grain set.
      - MECH-292 / MECH-293 ghost-goal bank + waking probes: the
        goal-payload preservation (SD-039) extends naturally to
        chunks -- a chunked primitive with a stable goal-payload
        is a 'recipe' the agent has accumulated. Cross-link.
      - ARC-065 behavioural_diversity_generation: composition
        REDUCES diversity locally (chunked sub-elements no longer
        perturbable independently). The diversity layer must
        operate at appropriate grain. Open question: should
        diversity-generation operate on chunked or unchunked
        representations? Likely both, in parallel, at different
        timescales -- but this is a follow-on Q-claim.
      - SD-014 valence vector recording: chunks may carry
        accumulated valence. Cross-link with VALENCE_LIKING write
        path -- a successful chunk recipe carries the average
        liking of its repetitions.
      - MECH-094 hypothesis_tag: composition is execution-side
        (chunk forms FROM real executed sequences with
        hypothesis_tag=False). Replay / DMN / simulation
        sequences must NOT trigger composition -- a hallucinated
        chunk would be catastrophic.

    Falsifiable architectural prediction: an REE agent without
    ARC-071 substrate, run for many episodes on a repeating
    sub-task, will not develop coarser primitives -- every
    rollout in episode 1000 will still propose at single-action
    grain, paying the full combinatorial deliberation cost on
    sequences it has executed thousands of times. With ARC-071
    active, repeated successful sub-sequences should compress;
    rollout deliberation cost on those sub-sequences should drop
    measurably; behavioural latency on the chunked sub-sequences
    should drop (faster commit, less deliberation).

    What ARC-071 is NOT:
      - Not a habit-formation re-implementation of MECH-163.
        MECH-163 is the PRESENCE of two systems; ARC-071 is the
        machinery that pumps content from one into the other.
      - Not a model-free reinforcement learning slot. ARC-071 is
        chunking of model-based plans, not a model-free policy
        substitute. The chunked primitive remains a sequence with
        outcome predictions; it just loses internal flexibility.
      - Not a representation-compression slot at the encoder
        layer. Chunks are over POLICY primitives, not over
        latent observations. Encoder compression is a separate
        architectural concern (cf. SD-009 z_world contrastive
        supervision; SD-018 z_world resource proximity).
  evidence_quality_note: >
    Registered 2026-05-10 alongside ARC-069 + ARC-070 from user
    observation. Lit-pull required before child MECH design;
    targeted_review_arc_071_composition anticipated. Striatal
    chunking literature (Graybiel / Sakai / Wymbs / Smith &
    Graybiel) is convergent on substrate; the question is
    parameterisation: how many repetitions to chunk, how much
    outcome variance permitted, what stops a chunk from chunking
    further (chunks-of-chunks problem).

    Sutton 1999 options framework is the cleanest formal analog
    but is incomplete on the discovery side (the options-
    framework's bottleneck-state literature is one shape;
    Graybiel chunking is another; the two are not yet unified
    in computational neuroscience).

    Not yet experimentally tested. Validation requires an
    environment with structurally repeating sub-sequences --
    SD-054 reef + a repeating forage-loop task, or similar.
    First validation experiment will be substrate-readiness
    diagnostic measuring whether the chunking accumulator fires
    at all; behavioural-latency / rollout-cost measurements come
    later.

    Lit-pull landed 2026-05-10:
    evidence/literature/targeted_review_arc_071_composition/
    (9 entries; aggregate lit_conf ~ 0.78 supports-direction net,
    8 supports + 1 weakens). Six verdicts settled:
      R1 (trigger primary): repetition + outcome consistency is
        primary trigger (canonical Graybiel pattern). Reward-rate
        and V_s-positive are secondary modulators / gates. FEP
        not directly supported in chunking literature. Conf 0.78.
      R2 (substrate locus): PHASE-DEPENDENT MULTI-SUBSTRATE.
        Substrate is striatum (DLS-analog, motor-motor association
        formation) + infralimbic cortex (vmPFC-analog, chunk
        maintenance, causally required per Smith & Graybiel 2013
        optogenetic disruption) + frontoparietal cortex (early-
        phase parsing, more relevant to ARC-070). NOT a single
        substrate. Conf 0.81.
      R3 (LOAD-BEARING transition mechanism for MECH-163):
        CONFIRMED. ARC-071 IS the missing transition mechanism
        in MECH-163 dual_goal_directed_systems. Yin & Knowlton
        2006 supplies system-level evidence (DMS-to-DLS transfer
        with overtraining); Smith & Graybiel 2013 supplies chunk-
        level causal evidence (IL disruption prevents habit
        formation). Recommend MECH-163 governance update:
        depends_on should add ARC-071 (currently absent because
        ARC-071 was registered after MECH-163). Conf 0.85.
      R4 (chunks-of-chunks recursion): permit recursion to 2-3
        levels by default. Architectural requirement: chunks must
        carry initiation-set + termination-condition fields beyond
        the sequence itself (Sutton 1999 options structure). Conf
        0.72.
      R5 (outcome-consistency threshold): gradual variance
        threshold with hysteresis (F_low for formation < F_high
        for dissolution; dissolution timescale slower than
        formation). Chunk-size budget 2-5 elements per level
        (Sakai 2003). Conf 0.71.
      R6 (SAFETY-CRITICAL real-vs-imagined gating): NEW REE GAP.
        Biology does NOT cleanly gate the chunking write path
        against replay/imagined sequences -- sleep replay drives
        chunking circuit consolidation per Albouy 2013 hippocampal-
        striatal replay coupling (and broader sleep-motor-
        consolidation literature). ARC-071's pre-registered
        MECH-094 hypothesis_tag=False strict-gating is MORE
        CONSERVATIVE than biology, with substantive safety-vs-
        realism tradeoff. RESOLVED 2026-05-11 via MECH-322
        (Option B carve-out): a NEW write-path that permits
        ARC-071 chunking from replayed sequences in sleep mode
        IF (a) value-tag from prior real-executed episodes
        meets high-positive threshold, (b) replay occurs in
        designated SD-017 sleep phase (not waking DMN), (c)
        formed chunk carries replay_origin=True audit flag with
        accelerated dissolution if not corroborated by real
        waking execution within N episodes. MECH-094 strict
        gating PRESERVED for all other write paths; MECH-322
        is a narrow additional path with audit trail. ARC-071
        child-MECH design now unblocked. Conf 0.74.
    Aggregate ARC-071 lit_conf ~ 0.78. R3 confirmation and R6
    escalation are both architecturally consequential beyond
    standard parameterisation; family doc + governance follow-on
    required.

    [2026-08-02 governance, V3-EXQ-871 (run v3_exq_855), confirmed failure_autopsy_V3-EXQ-871_2026-08-02, MECH-090/ARC-071 commit-latch post-fix validation]: self-routed substrate_not_ready_requeue (precondition arc071_chunk_commitments_observed_supra_floor measured 0.0 vs threshold 10.0, worst seed), adjudicated measurement_test_design_defect, NOT a substrate ceiling. Root cause: use_persistent_committed_program_handle=True was set for the ENTIRE cell (formation + probe) rather than scoped to the probe phase, which changes select_action's between-E3-tick fast path and cascades (via RNG-coupled environment dynamics) into materially different chunk-formation outcomes -- same seeds 101/505 formed only 1/0 chunks here vs 2/2 under V3-EXQ-841 (flag at lineage default) on 2026-07-31, a direct same-seed comparison. V3-EXQ-810a's/841's "readiness inherited by construction" claim only holds when the flag stays at its lineage default during formation. Neither MECH-090 nor ARC-071 evidence changes (non_contributory, precondition_unmet). Route: V3-EXQ-871a, scoping the flag to the probe phase only.
  notes: >
    Cross-reference: ARC-071 is the inverse of ARC-070
    (decomposition). Asymmetric triggers (slow / repetition-driven
    / execution-side vs fast / V_s-driven / simulation-side).
    Whether one mechanism or two is open per ARC-069 parent.

    Chunks-of-chunks problem: if ARC-071 is recursive (a chunked
    primitive can itself be a sub-element of a higher-grain
    chunk), the cluster admits a hierarchy of grains. Biology
    supports this (Botvinick 2009 hierarchical behavioural
    organisation goes deep). Open Q-claim at child-MECH design
    time: depth of recursion permitted, and how the rule-
    apprehension layer (ARC-062) handles rules at multiple
    grains simultaneously.

    Cross-link to psychiatric_failure_modes.md (future): ARC-071
    pathology candidates include
      - autism / insistence-on-sameness: chunks form normally but
        cannot be re-decomposed (ARC-070 weakened on chunked
        primitives), so context demands different grain produce
        rigidity.
      - obsessive-compulsive disorder: chunks fail to form (ARC-071
        weakened); every routine action is re-planned at
        primitive grain, producing the over-deliberation
        signature.
      - skill / motor-learning impairments: chunking machinery
        itself impaired; sequences never compress; deliberation
        budget exhausted.
    These are speculative pre-registration cross-references; not
    grounded until lit-pulls land.

    HYBRID-naming convention: REE-internal name
    (composition_via_repeated_grounding) + literature anchors
    (Graybiel striatal chunking / Sakai motor sequence / Sutton
    options / Yin & Knowlton DLS-DMS division).

    Open Q-claim worth registering at child-MECH time: what
    happens when a chunk's outcome consistency drops below
    threshold? Does the chunk decompose (ARC-070) or simply
    become un-selectable from the proposal pool? The two have
    different behavioural signatures and the lit is not
    decisive.

    2026-07-30 (governance-apply, confirmed failure_autopsy_V3-EXQ-810a_2026-07-30 +
    batch-687a-707c-840-748a-833-842-810b-673-614-798afail): V3-EXQ-810a (chunk-accumulator
    readiness, independently re-derived from raw per-seed data) and its seed-202
    narrow-repertoire follow-up V3-EXQ-810b both supports/standard. Readiness preconditions
    non-vacuous (worst-seed margins 2.4x-7.0x floors); load-bearing C1 clears 7/8 seeds vs a
    pre-registered 0.75 bar; C7 credit-rule separation 5x. Detail on MECH-323/MECH-324.
  location: docs/architecture/policy_primitive_granularity.md

- id: ARC-072
  title: "developmental_bootstrapping_and_cue_indexed_retrieval_gaps -- REE lacks (1) a dedicated diversity-bootstrapping epoch that populates the hippocampal trajectory store before reinforcement narrowing; and (2) a differentiable cue-indexed retrieval path from PFC context representation to trajectory generation. EXQ-561 cluster / MECH-269 is diagnostic of gap (1); EXP-0155 zero-gradient cue_action_proj is diagnostic of gap (2). The two gaps are inter-dependent: the severed cue gradient (gap 2) is the mechanism that prevents diverse option population (gap 1)."
  claim_type: architectural_commitment
  subject: architecture.developmental_bootstrapping_and_cue_indexed_retrieval_gaps
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-072@2026-06-10T16:18:22Z"
      as_of: 2026-06-10
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  phase_provenance: derived
  phase_derived_from: SD-055
  claim_level: architectural
  registered_utc: "2026-05-15"
  epistemic_category: substrate_conditional
  what_would_answer: >
    CURRENT SUBSTRATE STATE (checked 2026-08-08): Gap 2 wiring closed (SD-055,
    V3-EXQ-568 PASS, grad_max=372) but use_differentiable_cem defaults False -- never
    behaviourally exercised. Gap 1 unambiguously absent (no cross-tick accumulation
    code). EXQ-563c/567 (Gap-1 diagnostics) measure only within-tick entropy, tagged
    ARC-065 not ARC-072 -- confirms rather than closes the claim's own caveat that they
    treat within-tick symptom only.

    GAP 1 FALSIFYING: no diversity gap opens between epoch-given and no-epoch agents,
    or a gap opens but fully closes under later equivalent exploration.
    GAP 1 CONFIRMING: epoch-given agent retains a larger, cross-tick diverse library
    that persists.

    GAP 2 FALSIFYING: with the flag on and gradient confirmed live, PFC context
    variation produces no retrieval difference (gradient flows but carries no
    cue-discriminative signal).
    GAP 2 CONFIRMING: distinguishable context states retrieve distinguishable
    trajectory subsets, absent with the flag off.

    JOINT FALSIFYING: either gap closes independently of the other -- contradicts the
    claimed interdependence.
    JOINT CONFIRMING: Gap 1 does not close while Gap 2 stays severed (Warlaumont &
    Finnegan severed-feedback topology).

    CROSS-REFERENCE: INV-073 names the same underlying gap from the general/
    biological-principle angle and already carries an independent 2026-08-08
    what_would_answer + a queue flag proposing flipping use_differentiable_cem=True to
    measure option diversity -- that flag serves both claims; do not duplicate it here.
  depends_on:
    - INV-073    # developmental bootstrapping necessity (causal upstream principle)
    - ARC-065    # diversity stack -- the V3 mechanism ARC-072 scopes the gap beyond
    - MECH-269   # V_s monostrategy -- diagnostic of gap (1)
    - MECH-326   # PFC top-down bias as retrieval gate (the missing routing mechanism)
    - SD-016     # frontal cue-indexed integration -- the V3 stub with broken gradient
  lit_conf: 0.74
  literature_anchors:
    - "Doupe & Kuhl (1999) Annual Review of Neuroscience -- developmental window + LMAN
       epoch necessity. conf 0.78."
    - "Warlaumont & Finnegan (2016) PLoS ONE -- severed feedback -> single attractor;
       maps directly to zero-gradient cue_action_proj. conf 0.70."
    - "Shin, Tang & Jadhav (2019) Neuron -- forward replay + PFC coherence at decision
       point; the cue-indexed retrieval pattern REE does not yet implement. conf 0.84."
    - "Li, Wu & Li (2007) Behavioural Pharmacology -- reversal learning deficit after
       developmental impoverishment; update failure not acquisition failure. conf 0.68."
  functional_restatement: >
    ARC-072 names two inter-dependent architectural gaps in V3 REE.

    Gap 1 -- No diversity-bootstrapping phase:
    The V3 HippocampalModule generates K candidates per tick by CEM
    refit starting from the current terrain prior. There is no
    persistent trajectory store that accumulates diverse options over
    an early exploration epoch, and no intrinsic diversity reward
    that would prevent CEM from collapsing to the dominant
    action class under repeated refit. The EXQ-563 series (support-
    preserving CEM stratification) treats the symptom -- keeping
    diversity within one tick's candidate set -- but does not
    populate a diverse library across ticks. The monostrategy
    experiments (MECH-269, EXQ-561 cluster) are the diagnostic.

    Gap 2 -- No differentiable cue-indexed retrieval:
    SD-016 introduced cue_action_proj as the structural hook for PFC
    affordance bias into action generation. EXP-0155 confirmed zero
    gradient through the CEM argmax: the path is structurally present
    but computationally broken. MECH-292/293 (ghost-goal bank + waking
    probes) provides goal-location-indexed seeding, which is a narrow
    spatial-context approximation -- it is not general cue-indexed
    retrieval keyed by arbitrary PFC goal representations.

    Why the gaps are inter-dependent:
    The feedback loop that would populate the trajectory store with
    diverse options IS the cue gradient path that is severed in
    gap 2. Without a differentiable route from goal cue to
    trajectory generation, the CEM cannot be shaped by diverse goal
    states during the exploration epoch. The agent is unable to
    discover that different goal cues should produce different
    trajectories because the gradient does not flow. This is the
    Warlaumont & Finnegan topology: severed feedback -> single
    attractor, regardless of exploration duration.

    V4 architectural requirements (derived from ARC-072):
    R1: Trajectory library structure (MECH-325) -- persistent
        content-addressable store of (trajectory, cue, value) tuples.
    R2: Diversity-bootstrapping epoch (INV-073) -- early training
        phase with intrinsic diversity reward preceding task-reward.
    R3: Differentiable PFC-to-retrieval path (MECH-326) -- replace or
        supplement CEM argmax with differentiable retrieval operation
        (e.g., attention over trajectory library, Gumbel-softmax
        over stored indices).
    R4: Context-sensitive retrieval gate (MECH-326) -- different
        z_goal representations pattern-complete to different trajectory
        subsets; gate is learnable, not static.

    V3 diagnostic experiments scoped by ARC-072:
    Gap 1 severity: EXQ-563c (support-preserving CEM), EXQ-567
        (natural entropy measurement). These quantify how much
        diversity CEM can maintain given current terrain gradient.
        They do NOT address gap 2.
    Gap 2 severity: EXP-0155 established the zero-gradient finding.
        A follow-on diagnostic should establish whether the gradient
        path can be restored with a differentiable approximation to
        CEM selection (V3 staging experiment) before committing to
        the full library architecture at V4.

    ARC-072 is not a regression from ARC-065. ARC-065 is the runtime
    diversity mechanism (MECH-313/314 noise floor, structured
    curiosity, score-bias perturbation). ARC-072 identifies what
    ARC-065 cannot fix: a trajectory library that was never
    populated with diverse options in the first place, and a cue
    routing system that cannot differentially retrieve from whatever
    library exists.
  notes: >
    Relationship to existing V3 substrate:
      - The EXQ-563 series (support-preserving CEM stratification)
        is valid V3 work that quantifies Gap 1 severity. It does not
        close the gap but establishes the severity diagnostic.
      - SD-016 cue_action_proj is the V3 stub for Gap 2's
        structural requirement. Restoring the gradient path is the
        minimal V3 experiment for Gap 2; full library architecture
        is V4.
      - MECH-292/293 ghost-goal bank and probes is the closest V3
        approximation to cue-indexed retrieval. It is spatially-
        indexed (by goal location in observation space), not
        arbitrary-cue-indexed. ARC-072 names the extension required.

    Psychiatric failure mode prediction:
      - If Gap 1 is severe enough: monostrategy under novel
        contingencies = cognitive inflexibility / reversal learning
        deficit (Li et al. 2007 isolation-reared phenotype).
      - If Gap 2 is severe enough: context-inappropriate strategy
        selection = perseveration (wrong strategy retrieved for
        context) or strategy generalization failure (same option
        retrieved regardless of cue).
      - Combined gap: the agent learns one thing in one context
        and cannot adapt in any other context -- the null-diversity
        pole.

    2026-08-10 edge correction (chip chip-20260810-phase-consistency-reconciliation):
    depends_on: MECH-325 removed -- reversed edge. MECH-325's own notes: "MECH-325 is
    the positive specification; ARC-072 names the GAP BETWEEN MECH-325 and the V3
    substrate" -- ARC-072 exists because MECH-325 does NOT exist; it cannot depend on
    MECH-325 being built. ARC-072's own diagnostics (EXQ-563c/567, EXP-0155, V3-EXQ-568)
    all ran without MECH-325 ever built, explicitly staged BEFORE "committing to the
    full library architecture at V4."
  implementation_note: >
    SD-055 substrate-ready 2026-05-21 (IGW-20260521-021): differentiable CEM path
    landed 2026-05-15; V3-EXQ-568 PASS (grad_max=372, UC1-UC5). Closes EXP-0155
    zero-gradient barrier when use_differentiable_cem=True. Gap 1 (bootstrapping
    epoch / trajectory library) unchanged; gap 2 behavioural validation still open.
  location: docs/architecture/developmental_bootstrapping_hippo_retrieval.md

- id: MECH-320
  title: "tonic_vigor_coupling_score_bias (capacity-keyed E3 score-side action drive / mesolimbic-DA-vigor-analog / average-reward-rate opportunity-cost regulator). Child mechanism for ARC-066 (tonic_vigor_coupling) AND ARC-068 (opportunity_cost_no_op_penalty -- collapse into MECH-320 per ARC-068 lit-pull R3 verdict 2026-05-16; MECH-320's w_passive term IS the ARC-068 implementation). Adds a tonic vigor scalar v_t to E3 trajectory scoring as an additive bias toward action-trajectories and an additive cost on no-op / repeated-stationary trajectories, where v_t is a slow EWMA over the realised E3-score-receipt stream. Target-free: bias applies regardless of whether any z_goal is currently active. Distinct from MECH-313 (LC-NE noise floor -- noise on choice, orthogonal axis), MECH-216 (target-conditioned wanting), SD-012 (deficit-keyed drive, opposite coupling), and MECH-330 (ARC-067 boredom -- valence-side z_harm_a write, different architectural layer)."
  claim_type: mechanism_hypothesis
  subject: action.tonic_vigor_coupling_score_bias
  polarity: asserts
  status: candidate_substrate_landed
  live_status:
    reading: candidate_substrate_landed/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-603g-624c-651a_2026-06-07#V3-EXQ-624c"
      as_of: 2026-06-07
      verdict: non_contributory/substrate_conditional
  implementation_phase: v3
  v3_pending: true
  pending_retest_after_substrate: true  # [2026-06-08 governance] survival-leg cohort gated on escape-affordance-bridge substrate (failure_autopsy_V3-EXQ-603h_2026-06-08)
  claim_level: mechanistic
  registered_utc: "2026-05-10"
  depends_on:
    - ARC-066     # parent architectural commitment (capacity-keyed action drive)
    - ARC-068     # opportunity_cost_no_op_penalty -- collapses into MECH-320 per lit-pull R3 verdict 2026-05-16
    - MECH-313    # LC-NE noise floor; orthogonal axis; lit-pull R2 verdict establishes non-redundancy
  functional_restatement: >
    MECH-320 names the primary child mechanism for ARC-066. It adds a
    tonic vigor scalar v_t to E3 trajectory scoring such that action-
    trajectories receive an additive bias of magnitude v_t * w_action
    and no-op / passive trajectories receive an additive cost of
    magnitude v_t * w_passive (with w_action + w_passive normalised so
    that the formalism is equivalent to a single signed scalar applied
    to the action-vs-no-op contrast).

    The biology grounding (per ARC-066 lit-pull synthesis 2026-05-10):
      - Niv et al. 2007 (Psychopharmacology) gives the formalism --
        long-run average reward rate enters the cost-benefit equation
        as an additive opportunity cost on time-spent-passive,
        equivalently an additive bonus on time-spent-acting.
      - Salamone & Correa 2012 (Neuron) gives the substrate identity --
        mesolimbic / accumbens DA encodes the activational dimension
        of motivation (behavioural activation, exertion of effort,
        sustained task engagement), dissociable from hedonic / liking.
      - Beierholm et al. 2013 (Neuropsychopharmacology) gives the
        human pharmacological causal test -- L-DOPA strengthens the
        avg-reward-rate-vigor coupling.
      - Depue & Collins 1999 (BBS) gives the abstract-level commitment
        that this is a tonic personality dimension keyed to the same
        DA substrate.

    The capacity scalar v_t is computed as:
      v_t = ewma_window(realised_E3_score_receipt, half_life=H)
            * gate_energy(energy_t)
            * gate_drive(drive_t)
            * gate_pe(recent_pe_t)
    where the EWMA is the PRIMARY signal (per Niv 2007 / lit-pull R4
    verdict) and the gate_* functions are SECONDARY MODULATORS that
    suppress vigor when capacity proxies are unfavourable. Default
    gates: gate_energy = 1 if energy >= e_min else energy / e_min;
    gate_drive = 1 if drive < d_max else (max -- linearly reduces
    when in z_goal-pursuit corner); gate_pe = 1 if pe < pe_max else
    suppresses (high PE = attend / consolidate, not act). H, e_min,
    d_max, pe_max are config knobs with phase-1 defaults TBD at
    substrate-landing time.

    The implementation lands at the e3.select() call site in
    REEAgent.select_action(), parallel to where MECH-313 NoiseFloor
    is wired. Pure-arithmetic regulator (no learned parameters).
    Composes orthogonally with MECH-313: MECH-313 lifts softmax
    temperature uniformly (entropy on choice); MECH-320 adds a
    directional bias on the score axis (action over no-op). The two
    can hold simultaneously. Composes with ARC-068 (opportunity-cost
    no-op penalty) at the architectural level: if ARC-068 lands as a
    separate child MECH with its own scalar, the two scalars may be
    distinct; if ARC-068 collapses with MECH-320 into a single signed
    scalar, MECH-320's w_passive term becomes ARC-068's substrate.
    The collapse decision is deferred to the ARC-068 lit-pull.

    Falsifiable prediction (substrate-readiness): with MECH-320 OFF
    in the well-fed-safe-familiar regime (full energy, no z_goal
    active, familiar environment, no harm signal) the agent is
    behaviourally inert -- action density at the MECH-313 noise
    floor only. With MECH-320 ON, action density rises monotonically
    with v_t even in the same conditions. The discriminative-pair
    validation experiment (V3-EXQ-TBD per /queue-experiment skill):
      ARM_0  baseline (MECH-313 ON, MECH-320 OFF) -- predicted inert.
      ARM_1  ADDITIVE primary (MECH-320 ON, additive form).
      ARM_2  MULTIPLICATIVE secondary (MECH-320 ON, gain form).
    R3 falsifiable secondary alternative (multiplicative gain) is
    distinguishable from primary via parametric sweep on a pre-existing
    action preference: under additive, the action-vs-no-op gap scales
    linearly with v_t regardless of pre-existing preference; under
    multiplicative, the gap scales differentially when the pre-
    existing preference is strong. Both implementations differ by ~3
    lines of arithmetic at the call site; the experiment chooses
    which one lands as primary.
  evidence_quality_note: >
    Registered 2026-05-10 from ARC-066 lit-pull synthesis (R3 +
    R4 verdicts).

    R3 implementation shape (additive primary, multiplicative
    falsifiable secondary): grounded in Niv 2007's opportunity-cost
    derivation. Beierholm 2013 RT data is consistent with both;
    parametric-sweep validation experiment will discriminate.

    R4 capacity scalar composition (slow EWMA over realised E3-score-
    receipt as primary; internal-state gates as secondary): grounded
    in Niv 2007 average-reward-rate formalism + Beierholm 2013 human
    empirical confirmation that the empirically tested scalar is
    across-trial reward history, not internal physiological capacity.
    Lit anchors:
      - Niv et al. 2007 (Psychopharmacology) -- formalism (R3 + R4
        primary)
      - Salamone & Correa 2012 (Neuron) -- mesolimbic DA substrate
        identity (R1)
      - Beierholm et al. 2013 (Neuropsychopharmacology) -- human L-DOPA
        causal test (R1, R3, R4)
      - Depue & Collins 1999 (BBS) -- BAS abstract-level commitment
        (R1 abstract level)
    See: evidence/literature/targeted_review_arc_066_tonic_vigor/synthesis.md
    Aggregate ARC-066 lit_conf 0.789 supports.

    Distinct-from contracts (verified at design time):
      - MECH-313 (stochastic_noise_floor): orthogonal axis. MECH-313
        lifts softmax temperature uniformly; MECH-320 adds directional
        bias on score axis. Lit-pull R2 verdict (Kane et al. 2017
        DREADD test) establishes that LC-NE tonic mode is one
        mechanism (noise = MECH-313), not two -- MECH-320 must therefore
        NOT be wired through an LC-NE-direction analog; its substrate
        is mesolimbic DA-vigor.
      - MECH-216 (e1_predictive_wanting / schema readout):
        target-conditioned. MECH-320 is target-free; the two compose
        cleanly when both fire (a specific predicted target +
        general-action-bias vigor) but neither subsumes the other.
      - SD-012 (homeostatic_drive_modulation): drive RISES as energy
        FALLS. MECH-320 vigor scales with capacity (rises when
        energy is high). Both can hold simultaneously: depletion
        recruits z_goal pursuit AND surplus recruits target-free
        action. The gate_drive secondary modulator handles the
        deficit-corner attribution.
      - ARC-068 (opportunity_cost_no_op_penalty): mathematical
        complement. MECH-320 = additive bonus on action; ARC-068 =
        additive cost on no-op. Both are formally equivalent under
        the same scalar; whether they collapse to one child MECH
        or stay as two separable mechanisms is open until the
        ARC-068 lit-pull lands.

    Substrate landed 2026-05-10 (status candidate -> candidate_substrate_landed;
    v3_pending retained pending behavioural validation):
    ree-v3/ree_core/policy/tonic_vigor.py (TonicVigor + TonicVigorConfig +
    TonicVigorOutput). Pure-arithmetic regulator (no learned parameters;
    no nn.Module inheritance); sister to MECH-313 NoiseFloor and MECH-314
    StructuredCuriosity in the ree_core.policy package. Composed in
    REEAgent.select_action() AFTER MECH-314 curiosity block (orthogonal
    axes: curiosity at the candidate-feature level, vigor on the
    action-vs-no-op axis) and BEFORE MECH-313 noise_floor temperature
    lift (which targets temperature, not scores). Algorithm at the
    e3.select() call site:
      v_raw <- (1-alpha)*v_raw + alpha*(-realised_score)  [REE convention
        internally negated so v_raw climbs in reward-rich regimes]
      v_t = max(0, v_raw) * gate_energy * gate_drive * gate_pe
      bias[i] = -w_action * v_t   if action_classes[i] != noop_class
                +w_passive * v_t  if action_classes[i] == noop_class
      bias = clamp(bias, [-bias_scale, +bias_scale])
    Multiplicative form (R3 falsifiable secondary): bias[i] also
    multiplies by |scores[i]| -- distinguishable from additive on a
    held-out batch with non-uniform |scores| (the empirical resolution
    path for R3's open question). Defaults: half_life=100.0 ticks (~700-
    tick window to steady state -- long-window per R4), w_action=0.1,
    w_passive=0.1, bias_scale=0.1, gate_energy_min=0.2, gate_drive_max=0.7,
    gate_pe_max=1.0, form="additive", noop_class=0 (matches MECH-279 PAG
    convention).

    Wiring: REEAgent.__init__ instantiates TonicVigor when
    use_tonic_vigor=True (parallel to noise_floor / curiosity).
    select_action reads compute_score_bias(per_candidate_scores,
    action_classes, energy, drive, recent_pe) BEFORE e3.select(...) to
    compose with dacc_score_bias; AFTER e3.select() returns, feeds
    result.scores[result.selected_index] back into update_score_receipt
    to advance the EWMA for the next tick. Energy proxy = 1 -
    effective_drive_level (post-pACC); drive = effective_drive_level
    (matches AIC / PCC / SalienceCoordinator reads); recent_pe =
    e3._running_variance (same signal MECH-314c LP consumes).
    reset() clears EWMA + diagnostic counters per episode.

    MECH-094 simulation_mode argument on both methods returns zeros /
    skips state advance and increments only the simulation-skip counter
    -- match SD-035 / MECH-279 / gated_policy / MECH-313 / MECH-314
    pattern. Validation: 309/309 contracts (281 prior + 28 new in
    tests/contracts/test_mech_320_tonic_vigor.py) + 7/7 preflight PASS
    with master OFF (regression-clean). Smoke 6/6 PASS via V3-EXQ-547
    dry-run 2026-05-10 (manifest scrubbed; runner will write the
    canonical PASS manifest from the queued entry).

    Behavioural validation experiment (the 3-arm discriminative pair --
    baseline / additive / multiplicative on a well-fed-safe-familiar
    environment substrate) is queued separately AFTER V3-EXQ-547
    PASSes via the runner. Per the lit-pull R3 verdict, the
    parametric-sweep on a pre-existing-action-preference batch is the
    test that arbitrates additive-as-primary vs multiplicative.

    [2026-06-03 governance / cluster autopsy failure_autopsy_604a-624a-630_2026-06-03] V3-EXQ-624a re-tagged non_contributory: C3 PASS (gate_product 1.0, v_t 0.05 fires) but action_density byte-identical between vigor-ON and vigor-OFF arms (ARM_1==ARM_0==0.865 per-seed) -- the vigor no-op penalty fires but does not change argmax. NOT a falsification of MECH-320/ARC-068 nor an ARC-068 supersession trigger. substrate_ceiling (selection-authority sub-type); pending_retest_after_substrate -- same substrate_queue entry as 604a (AMEND 'modulatory-bias-selection-authority'). Supersedes the 624 crash.

    [2026-06-03 substrate landing] modulatory-bias-selection-authority IMPLEMENTED (ree-v3 e3_selector.select gap-relative rescale of the combined modulatory contribution to modulatory_authority_gain*raw_score_range + e3_score_diversity.stratified_select across-class unit-range normalization; config use_modulatory_selection_authority default OFF/bit-identical). Gives this claim's score-bias lever genuine BOUNDED authority at E3 committed selection (the 604a/624a/614d cluster showed the lever fired but never changed the committed argmin). 624a vigor fired (v_t=0.05) but action_density was byte-identical ON==OFF -- pure drowning, fixed directly by the gap-relative rescale. Claim STAYS candidate/v3_pending -- substrate-readiness validation EXQ pending (claim_ids=[]); a per-claim EVIDENCE retest on the authority-ON substrate is the governance-weighting signal. Design doc docs/architecture/modulatory_bias_selection_authority.md; substrate_queue modulatory-bias-selection-authority status implemented_pending_validation.
  notes: >
    Companion-claim cross-reference: MECH-320 is the child mechanism
    for both ARC-066 and ARC-068 (collapsed implementation).

    ARC-067 (idle_aversion_boredom): child MECHs registered 2026-05-16
    after lit-pull completion (2026-05-10) and two-child governance call:
      - MECH-330 (idle_aversion_acute_restlessness_accumulator) -- acute
        timescale; z_harm_a-side accumulator on engagement-rate; NOT a
        score-side mechanism (routes via affective valence as confirmed by
        ARC-067 lit-pull R3 Danckert 2018 AIC-deactivation verdict).
      - MECH-331 (idle_aversion_chronic_anhedonic_flatness_substrate) --
        chronic timescale; frontostriatal effort-allocation substrate;
        seeded by sustained MECH-330 activation; implements apathy archetype.

    ARC-068 (opportunity_cost_no_op_penalty): COLLAPSED INTO MECH-320
    per ARC-068 lit-pull R3 verdict 2026-05-16. Niv 2007's derivation
    is symmetric: the same average-reward-rate scalar produces (a) a
    positive bias on action vigor (ARC-066 territory = MECH-320 positive
    addend) and equivalently (b) an additive cost on time-spent-passive
    (ARC-068 territory = MECH-320 w_passive term). MECH-320's w_passive
    term IS the ARC-068 implementation. Slot-level ARC-068 registration
    preserved to allow future psychiatric failure-mode dissociations
    (anhedonia vs mania may require re-splitting the signed scalar).

    Implementation-phase placement (parallel to MECH-313 phase-1
    instantiation choice, see MECH-313 notes): a separate
    TonicVigor module at the e3.select() call site is the cleanest
    phase-1 placement, sitting alongside NoiseFloor in
    REEAgent.select_action(). Whether the policy-layer regulators
    eventually consolidate into one module (capturing MECH-313 noise
    + MECH-314 curiosity + MECH-320 vigor as a unified policy-side
    regulator stack) is open and depends on how the GatedPolicy
    territory evolves. Re-evaluate at substrate-landing time.

    HYBRID-naming convention (parallel to MECH-313): the title carries
    both the REE-internal architectural name
    (tonic_vigor_coupling_score_bias) and the literature anchors
    (mesolimbic-DA-vigor-analog / average-reward-rate /
    opportunity-cost). Grep-able under both REE-native search and
    biology-search.

    Open Q-claims worth registering at substrate-landing time:
      - Q-XXX: ARC-066 vs ARC-068 collapse -- does MECH-320 with a
        signed v_t (positive on action, negative on passive) cleanly
        cover ARC-068, or does ARC-068 need its own scalar that
        differs in dynamics? Discriminative experiment: parametrise
        an environment where opportunity cost (avg reward rate) is
        DECOUPLED from current capacity (energy / drive / PE) -- if
        the two scalars must differ, ARC-068 is mechanistically
        separate.
      - Q-XXX: tonic-DA isolation -- ARC-066 lit-pull noted that L-DOPA
        elevates both tonic and phasic DA, leaving the tonic-vs-phasic
        dissociation in Beierholm 2013 imperfect. Is the MECH-320
        scalar best modelled as a slow EWMA (tonic analog) or as a
        slow EWMA of phasic events (which shows the same surface
        behaviour but a different mechanism)? Discriminative experiment
        would manipulate the time-scale of reward delivery and
        observe whether v_t lags reward changes or tracks them
        instantaneously.
  location: docs/architecture/non_deficit_action_drives.md

- id: MECH-321
  implementation_note: >-
    SUBSTRATE BUILT 2026-07-24 in ree-v3, default OFF and bit-identical when OFF.
    ree_core/policy/policy_decomposition.py: DecompositionDecision, PolicyDecomposition,
    PolicyDecompositionConfig -- pure decision logic (evaluate() implements the R1 OR
    trigger against a caller-supplied region V_s and MECH-288.boundary_on(stream="rollout",
    ...); decompose_sequence() is the one-level, library-aware tiling helper). Wired into
    ree_core/hippocampal/module.py (set_decomposition_source, _apply_policy_decomposition --
    pre-commit / R4 first phase: withholds a triggering ARC-071 chunk candidate and replaces
    it with recursively-tiled leaf-tile candidates up to depth_cap, or excludes it when
    depth_cap is reached / no tiles exist) and ree_core/agent.py (instantiation with a loud
    precondition on hippocampal.use_event_segmenter; a mid-execution / R4 second phase abort
    hook beside the existing MECH-091 / rung-6 / SD-034 principled commit-latch releases;
    get_policy_decomposition_state API). Master switch REEConfig.use_policy_decomposition; NO
    hippocampal sub-config mirror needed (HippocampalModule acts on whether
    set_decomposition_source() was called, matching the external-source-injection pattern
    ARC-071 already established for _chunk_source -- not on a config flag it reads itself).
    R3 depth cap defaults to 3 (mirrors ARC-071's chunk_max_depth so the inverse operations
    stay symmetric). 15 interface contracts in
    ree-v3/tests/contracts/test_arc070_policy_decomposition.py; registered PROBED in
    ree-v3/tests/test_flag_inertness.py. NO EXPERIMENTAL EVIDENCE YET -- v3_pending stays
    True; the discriminative-pair validation experiment specified below (ARM_0/ARM_1/ARM_2)
    is deferred behind a substrate-readiness diagnostic queued separately, mirroring how
    ARC-071's own behavioural-latency measurement was deferred past its substrate-readiness
    diagnostic. CAUGHT DURING THIS LANDING'S OWN ACTIVATION SMOKE TEST: decompose_sequence()'s
    first draft returned no tiles whenever a primitive's depth was <=1, which made the single
    most common ARC-071 chunk shape (depth 1, composed directly of raw actions) permanently
    un-decomposable -- every triggering depth-1 chunk landed as marked_unreliable/dropped
    rather than genuinely re-segmented. Fixed to skip only the library sub-chunk lookup at
    depth<=1 while still falling through to raw-action tiling (contract C3 pins this as a
    regression guard). See ARC-070's implementation_note for the ARC-071-asymmetry framing
    (MECH-321 has no MECH-094 write-refusal branch; hypothesis_tag is a diagnostics label,
    not a gate on evaluate() itself).
    R5 BOTTLENECK TRIGGER MODE BUILT 2026-07-25 (unblocks ARM_2 of the discriminative
    validation specified below). The R1 OR trigger is now selectable against the R5
    alternative via REEConfig.decomposition_trigger_mode: "vs_boundary" (DEFAULT, R1,
    bit-identical) | "bottleneck" (R5: decompose ONLY at bottleneck states, REGARDLESS OF
    V_s -- ARM_2). Bottleneck detection is an ONLINE INCREMENTAL diverse-density accumulator
    (PolicyDecomposition._update_and_test_bottleneck): a region is a bottleneck once it has
    recurred >= decomposition_bottleneck_min_visits (default 3 -- the McGovern & Barto 2001
    "repeated traversals" gate that produces ARM_2's predicted one-shot-rare signature) AND
    been entered-from/exited-to >= decomposition_bottleneck_min_distinct_neighbors distinct
    regions (default 2 -- funnel topology). Region key is a coarse FIXED quantisation of
    z_world CONTENT, NOT MECH-288 segment_id (monotonic, never recurs, so could never
    register a bottleneck). FAITHFULNESS CHOICE (documented deliberately): the R5 verdict
    below frames bottleneck detection as a candidate OFFLINE/CONSOLIDATION-PHASE mechanism;
    this landing operationalises the SAME statistical signal ONLINE for ARM_2's primary-
    trigger requirement and does NOT claim to be the offline consolidation mechanism (batch
    ChunkLibrary topology analysis), which stays DEFERRED per R5. Default OFF/bit-identical
    (accumulator never allocated in vs_boundary mode). 6 further interface contracts C11-C16
    in ree-v3/tests/contracts/test_arc070_policy_decomposition.py (21 total). New get_state
    diagnostics: decomp_trigger_mode / decomp_n_bottleneck_fires /
    decomp_n_bottleneck_regions_tracked. STILL NO EXPERIMENTAL EVIDENCE -- v3_pending stays
    True (this is substrate, not evidence). ARM_0/ARM_1 half is V3-EXQ-816 (V_s-drop);
    ARM_2 (bottleneck) is now buildable on top of it.
    [2026-07-26 governance, V3-EXQ-816/820 FAIL cluster; failure_autopsy_816-820-policy-decomposition-cluster_2026-07-26,
    confirmed]: both runs FAILed on a shared unmet precondition (the env never produced low-V_s states in the
    measure window after 40 warmup episodes), not on the decomposition machinery itself -- both positive controls
    passed and the machinery fired abundantly (frac 1.0). non_contributory for ARC-070/MECH-321; stays
    candidate/v3_pending. GOV-FANOUT-1 fired: opened a 2-hypothesis discrimination portfolio
    (H-env-underdrives-uncertainty / H-vs-proxy-saturation) rather than one sequential re-pose.
    [2026-07-26 governance, V3-EXQ-816b FAIL (P-A env-axis leg); failure_autopsy_V3-EXQ-816b_2026-07-26, confirmed]:
    harshening (env_drift_interval->3, world_rule_shift) moved forward-PE toward but not across its discrimination
    floor (0.0086 vs 0.01), so zero low-V_s steps resulted -- informative but non-eliminating; bears_on only, no
    claim weight. A further dose-escalation letter is recommended before the campaign concludes.
    [2026-07-26 governance, V3-EXQ-816c PASS (P-B measurement-axis leg); failure_autopsy_816c-822_2026-07-26,
    confirmed]: found forward-PE heterogeneous while region-V_s stayed flat (spearman 0.083) -- fairly strong
    direct support for H-vs-proxy-saturation (region-V_s saturates near 1.0 in a trained encoder, decoupled from
    forward-PE). Left alive (not yet eliminated) pending the env-axis ladder; bears_on only, no claim weight.
    [2026-08-01 governance, V3-EXQ-844 (evidence successor to V3-EXQ-839's confirmed reachability
    validation); failure_autopsy_V3-EXQ-844_2026-08-01, confirmed]: this run tests a DIFFERENT axis
    from the 816/830/839 pure-diagnostic chain above (mid-execution task-outcome, not pre-commit/R1
    env-harshening) and DOES carry claim weight (claim_ids=[MECH-321]). Readiness all green (both
    arms). C2 (mechanistic corroboration, non-load-bearing): aborting a stale macro measurably lowers
    forward-PE post-abort -- PASSES, confirming the abort mechanism engages exactly as designed
    informationally. C1 (task-outcome improvement, load-bearing): does NOT translate into reduced task
    harm -- FAILS (-0.003262, wrong direction), using a paired post-divergence-window statistic built
    specifically to fix the whole-run-mean dilution problem 839's own data flagged. Code-verified root
    cause: `_apply_policy_decomposition` (ree_core/hippocampal/module.py:896-983) and
    `PolicyDecomposition.evaluate()`/`decompose_sequence()` (ree_core/policy/policy_decomposition.py:
    471-747) read only z_self/z_world/z_goal -- no harm-valence signal (z_harm_a) reaches this step,
    AND the step performs no ranked selection among candidate re-tilings at all (a binary decompose/
    keep test per candidate; all surviving tiles are additively recombined). MECH-288 (this claim's own
    trigger) is pure predictive-surprise, unrelated to hazard proximity; REE's actual fear/threat
    pathway (BLA/CeA, MECH-357/SD-058) biases action selection elsewhere (E3 scoring, the separate
    MECH-091 urgency-interrupt abort) but is not connected here. evidence_direction: weakens (for the
    harm-outcome prediction specifically; the mechanistic PE-reduction prediction is separately
    supported). v3_pending STAYS. Route: /implement-substrate -- a harm-valence-weighted selection
    step in the redecomposition path (recommended_substrate_queue_entry: create, sd_id_suggested
    SD-hazard-aware-policy-decomposition; extend SD-039's AnchorGoalPayload with a signed harm-valence
    field sourced from z_harm_a/BLA threat_scale; build an actual ranked-selection step in
    _apply_policy_decomposition, currently absent). Design phase needs a /lit-pull commission first
    (threat-modulated defensive path-selection literature, e.g. Fanselow's predatory-imminence
    continuum) before implementation specifics are fixed, per biology-before-formal-definitions.
    [2026-08-01, SD-hazard-aware-policy-decomposition IMPLEMENTED (chip-20260801-hazard-aware-decomp-build):
    the commissioned lit-pull landed first (targeted_review_threat_modulated_defensive_path_selection,
    9 entries -- Fanselow PIC x4, Mobbs 2007/2020, Evans 2018, Cooper 2016, Blanchard & Blanchard 1989;
    verdict Form B, two-stage regime-sensitive), then the substrate: PolicyDecomposition.harm_bias
    (Stage 1, graded, composed into REEAgent.select_action's existing score_bias chain via new
    Trajectory.metadata["decomposition_harm_bias"], ARC-007 value-flat) + .select_harm_aware_leaves
    (Stage 2, categorical override among a withheld chunk's own leaf tiles at high w(z_harm_a), a
    pool-admission decision in _apply_policy_decomposition rather than an oversized score_bias).
    harm_penalty is read per candidate leaf from the residue field's VALENCE_HARM_DISCRIMINATIVE
    channel (SD-014) on the leaf's OWN predicted world_states, NOT via an AnchorGoalPayload/SD-039
    extension as the routing note above suggested as one option -- AnchorGoalPayload is scoped to
    hippocampal anchors, not policy-decomposition candidates, so the direct residue-field read
    (mirroring build_goal_payload's own VALENCE_WANTING pattern) is architecturally cleaner; see
    the SD doc's Architecture Context for the full reasoning. Default OFF / bit-identical (full
    pre-existing 32-test contract suite passes unmodified); 11 new contract tests C18-C24 in the
    same file. This closes the STRUCTURAL gap V3-EXQ-844 identified (no harm signal reaches the
    redecomposition step, no ranking among re-tilings) but does NOT itself constitute new
    experimental evidence for MECH-321 -- v3_pending STAYS. Escapability (Cooper 2016) and
    predictability/certainty (Fanselow 2022 BST; Blanchard & Blanchard 1989) are lit-pull-confirmed
    secondary gaps, deliberately deferred, not built here. Follow-on, chipped rather than queued in
    this session (substrate build only, per scope discipline): a MECH-321 behavioural-effect
    validation experiment measuring whether harm-aware selection improves V3-EXQ-844's C1
    task-outcome statistic, ON vs OFF, at matched abort rates, via /queue-experiment. See
    REE_assembly/docs/architecture/sd_hazard_aware_policy_decomposition.md and
    evidence/planning/substrate_queue.json sd_id SD-hazard-aware-policy-decomposition.]
  title: "policy_decomposition_via_event_segmenter (rollout-side consumer of MECH-288 boundary pulses; re-segments the rollout proposal stream at finer grain when a chunk's region shows low V_s / boundary fires on the imagined trajectory). First child mechanism for ARC-070. Bidirectional extension of MECH-288 substrate (R2 lit-pull verdict 2026-05-10): MECH-288 already detects boundaries on whatever latent + PE streams it consumes; MECH-321 wires the rollout / imagination input stream into MECH-288 and consumes the resulting boundary pulses at the policy-primitive layer to decompose chunked primitives into finer-grain sub-elements. PE-driven primary trigger (V_s drop on chunk's region per Zacks 2007 / Schapiro 2017); bottleneck-state framing is a separate consolidation-phase mechanism, not the primary trigger. Recursive multi-level decomposition; depth cap 3 is an ENGINEERING choice, not a literature-derived constant (provenance corrected 2026-07-27 -- see evidence_quality_note). The MULTI-LEVEL commitment is ARC-070's R3 verdict (Badre & D'Esposito 2009) and STANDS; that source never supported a level COUNT, and Badre & Nee 2018 (Badre's own re-appraisal) finds the evidence supports neither a unitary model of lateral frontal function nor a unidimensional abstraction gradient, so there is no single ordered axis along which to cap. The cap rests instead on tractability + operator symmetry (mirrors ARC-071's chunk_max_depth so the inverse operations stay symmetric; open Q-claim on hard-vs-soft). Magnitude prior only, with an explicit grain caveat: Koechlin 2003 resolves a 3-level lateral-PFC cascade (4 with Koechlin & Summerfield 2007 frontopolar branching), but at ANATOMICAL grain -- heterogeneous, anatomically fixed control levels, NOT homogeneous recursion depth of one operator re-applying to its own output. Both pre-commit and mid-execution phases supported via the same mechanism, with downstream effects gated by MECH-094 hypothesis_tag (rollout fires under hypothesis_tag=True write nothing; observation / execution fires under hypothesis_tag=False enable the existing observation-side consumer chain unchanged)."
  claim_type: mechanism_hypothesis
  subject: policy.decomposition_via_event_segmenter
  polarity: asserts
  status: candidate
  epistemic_category: standard
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-08-07
    needs_review: false
    evidence:
      from: "v3_exq_867b_mech321_harm_aware_selection_matched_pool_20260804T015912Z_v3#failure_autopsy_2026-08-05_pending_review_batch"
      as_of: 2026-08-05
      verdict: FAIL/non_contributory -- measurement_test_design_defect (pool-exhausted AND screen-soundness falsified; 867c refused, redesigned DV/matching strategy owed)
  implementation_phase: v3
  pending_retest_after_substrate: true # 2026-08-01: awaiting SD-hazard-aware-policy-decomposition build
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-05-10"
  depends_on:
    - ARC-070     # parent architectural commitment (decomposition on prediction failure)
    - MECH-288    # substrate: bidirectional event_segmenter (R2 verdict; MECH-288 depends_on includes ARC-070 as the rollout-side downstream consumer)
    - MECH-269    # V_s primitive: the region-conditioned predictability scalar that produces the trigger signal on the chunk's region
    - MECH-094    # hypothesis_tag gate: pre-commit fires under tag=True write nothing; post-commit fires under tag=False enable residue / observation-side effects
  # Phase-1 pilot (substrate_stability_and_drift_detection_plan.md, 2026-08-03): author-declared,
  # deliberately conservative (safe to over-include, unsafe to under-include) -- NOT machine-proven
  # the way arm_fingerprint's per-cell scoped hashes are. Used only by
  # check_substrate_staleness_candidates.py to narrow drift relevance; never feeds arm_fingerprint
  # itself. Derived from this claim's own implementation_note (the R1/R5 decomposition substrate
  # and its two wiring call sites).
  substrate_scope:
    - ree_core/policy/policy_decomposition.py
    - ree_core/hippocampal/module.py
    - ree_core/agent.py
    - ree_core/environment/causal_grid_world.py
    - ree_core/utils/config.py
  functional_restatement: >
    MECH-321 names the primary child mechanism for ARC-070. It is the
    policy-side consumer of MECH-288 boundary pulses on the rollout /
    imagination input stream. The mechanism's job is to re-segment the
    rollout proposal stream at finer grain when the predictive engine
    reports a chunk's region as unreliable.

    -- Per-rollout-step mechanism --

    For each chunked primitive p in the current rollout proposal
    trajectory T:
      stream  := rollout (imagination-side input to MECH-288)
      v_s     := V_s on p.region (via MECH-269 substrate read-out)
      boundary := MECH-288.boundary_on(stream=rollout,
                                       latent=p.latent_signature,
                                       pe=p.pe_signature)
      if v_s < v_s_decompose_threshold OR boundary.fired:
        if p.depth < depth_cap:
          decompose(p) -- replace p with p.sub_elements
                          in the proposal stream
        else:
          mark p as unreliable; downstream selection may abort
          the trajectory rather than execute it blind

    The decomposition is recursive: each newly produced sub-element
    is itself eligible for decomposition on the same trigger, up to
    depth_cap (R3 verdict; default 3-4 levels).

    -- Trigger primary (R1 verdict, conf 0.78) --

    PRIMARY: V_s drop on chunk's region (PE-driven). This reads the
    MECH-269 V_s scalar at the chunk's segment_id; a low value means
    the predictive engine cannot reliably forward-simulate this
    region, so chunked primitives over the region cannot ground.
    Biologically anchored: Zacks 2007 EST establishes PE as the
    canonical boundary trigger; Schapiro 2017 establishes hippocampal
    trisynaptic pattern-separation as a candidate biological substrate
    for V_s.

    SECONDARY (convergent, may co-fire with V_s drop but should not
    be primary):
      - E2 forward-model disagreement at chunk-grain vs primitive-
        grain. Requires running both grains in parallel; expensive;
        not the biology default.
      - Hippocampal completion-signal failure (MECH-105 / ARC-028).
        Only fires for chunks with explicit completion signals; not
        universal.
      - Mid-execution prediction error. A SEPARATE PHASE handled by
        R4 (same mechanism, different input phase), not a separate
        trigger class.

    -- Phase handling (R4 verdict, conf 0.66) --

    BOTH pre-commitment and mid-execution decomposition supported via
    the same mechanism:

      PRE-COMMITMENT (hypothesis_tag=True; rollout / simulation
      phase): MECH-321 fires on the rollout proposal stream BEFORE
      the agent commits to executing the trajectory. Downstream
      effects: rollout proposal stream re-segmented at finer grain.
      No residue write. No update to MECH-269 anchor sets or
      MECH-287 broadcast (those are observation-side consumers of
      MECH-288 and must not fire on imagined inputs).

      POST-COMMITMENT / MID-EXECUTION (hypothesis_tag=False;
      execution phase): the agent has executed N of M chunk steps
      and the next predicted state has diverged from observation.
      MECH-321 reads V_s on the REMAINING chunk content. If low,
      decompose the remainder. Downstream effects: residue write
      enabled; observation-side consumer chain operates normally.
      May serve double duty as reactive-replanning substrate
      (candidate child-Q-claim).

    The phase distinction is enforced by MECH-094 hypothesis_tag at
    MECH-288's input_stream label, not by code internal to MECH-321
    (this preserves the bidirectional-substrate commitment from R2).

    -- Bottleneck-aware secondary (R5 verdict, conf 0.74) --

    OPTIONAL. R5 confirms PE-driven and bottleneck-state framings
    are DISTINCT mechanisms with overlapping outputs. McGovern &
    Barto 2001 bottleneck-state subgoal discovery is a candidate
    OFFLINE / CONSOLIDATION-PHASE mechanism that could examine
    chunked primitives for bottleneck-state topology and pre-
    decompose them at bottleneck boundaries. This integrates more
    naturally with ARC-071 chunk formation (chunks accumulated in
    DLS / habit substrate could be analysed offline) than with
    MECH-321's online rollout-side primary trigger. Defer until
    ARC-071 child-MECH design lands (which is itself gated on the
    ARC-071 R6 governance decision).

    -- Composition with sibling substrates --

    With MECH-269b symmetric V_s gating: MECH-321 is the SECOND
    major V_s consumer (after MECH-269b at the cortical-rollout
    layer). Both read MECH-269 V_s primitive; both may have
    INDEPENDENT thresholds (the substrate is shared; the readers
    are not). Cross-link evidence_quality_note will track whether
    MECH-269b's symmetric-V_s-gating commitment constrains MECH-321's
    threshold choices at substrate-landing time.

    With MECH-288 substrate: MECH-321 is the policy-side consumer
    on the rollout input stream. event_segmenter.py implementation
    must accept the input_stream label so the dispatch is clean
    (per MECH-288 notes 2026-05-10 bidirectional-consumer
    extension).

    With MECH-094 hypothesis_tag: tag-conditional downstream effects
    enforce the pre/post-commit separation (per R4 verdict). MECH-321
    itself reads only the boundary signal; the tag gates the
    DOWNSTREAM effects, not MECH-321's firing.

    With ARC-062 (rule apprehension over primitives): MECH-321 emits
    decomposed sub-element sequences; ARC-062 reads whichever grain
    is most predictive. Grain-invariant rule apprehension is an open
    child-Q-claim from the family doc.

    -- Falsifiable prediction (discriminative validation) --

    Discriminative-pair experiment (V3-EXQ-TBD per /queue-experiment
    skill, deferred until MECH-288 substrate lands):

      ARM_0  ARC-070 OFF baseline (MECH-288 ON for observation-side
             consumers only; MECH-321 OFF). Chunks blindly fire and
             execute, paying prediction-failure cost at execution.
             Predicted: rollout deliberation cost flat across V_s
             heterogeneity in the environment; execution-time
             prediction failures correlate with low-V_s regions
             traversed.

      ARM_1  MECH-321 ON with V_s-drop primary trigger. Chunks
             decompose pre-commitment when V_s indicates unreliability
             on the chunk's region. Predicted: rollout deliberation
             cost rises locally in low-V_s regions (more candidates
             per chunk-step); execution-time prediction failures
             drop because the agent has finer-grain plans where the
             substrate was unreliable.

      ARM_2  MECH-321 ON with bottleneck-state primary trigger
             (R5 alternative). Chunks decompose only at bottleneck
             states regardless of V_s. Predicted: decomposition is
             rare on one-shot tasks but appears on repeated
             structures; on the SD-054 reef substrate or similar
             one-shot environments, behaviour is closer to ARM_0
             than to ARM_1.

    R1 verdict predicts ARM_1 > ARM_2 on one-shot V_s-heterogeneous
    environments. R5 verdict (distinct mechanisms with overlapping
    outputs) predicts ARM_1 and ARM_2 converge on repeated-structure
    environments. Either ARM_1 vs ARM_2 dissociation outcome is
    informative; ARM_1 = ARM_0 falsifies the primary V_s-drop trigger
    and forces a substrate revisit.

    Substrate-readiness prerequisite: MECH-288 substrate
    (event_segmenter.py) must land first WITH the input_stream
    label extension (per MECH-288 2026-05-10 notes). MECH-321
    cannot be substrate-implemented before MECH-288. Phase-1
    placement: policy.decomposition_via_event_segmenter module
    consuming a MECH-288 BoundaryEvent queue filtered to
    input_stream=rollout, wired at the hippocampal-rollout
    candidate-generation layer prior to E3 trajectory selection.
  notes: >
    First child mechanism for ARC-070. Companion-claim cross-references:

      - MECH-288 (substrate parent): event_segmenter.py is the
        bidirectional detector. MECH-321 is its rollout-side
        consumer; MECH-269 / MECH-284 / MECH-287 are its
        observation-side consumers. See MECH-288 notes
        2026-05-10 ARC-070 bidirectional-consumer commitment.

      - MECH-269 (V_s primitive substrate): supplies the
        region-conditioned predictability scalar that MECH-321
        reads at the chunk's region.

      - MECH-269b (symmetric V_s gating, sibling V_s consumer):
        MECH-321 is the second major V_s consumer. Independent
        thresholds; shared substrate.

      - MECH-094 (hypothesis_tag): gates downstream effects of
        boundary pulses by phase. MECH-321 reads the boundary;
        MECH-094 decides whether the boundary writes residue
        / updates anchor sets / triggers broadcast.

      - ARC-062 (rule apprehension over primitives): reads the
        decomposed sub-element stream MECH-321 emits. Grain-
        invariant rule apprehension is an open child-Q-claim.

      - ARC-071 (composition; sibling architectural commitment):
        opposite direction (zoom out). ARC-071's child MECH is
        gated on the R6 governance decision (hypothesis_tag
        write-path safety). The R5 bottleneck-aware secondary
        option of MECH-321 may integrate with ARC-071's chunk
        accumulation pipeline at design refinement time.

    HYBRID-naming convention (parallel to MECH-320): the title
    carries both the REE-internal architectural name
    (decomposition_via_event_segmenter) and the literature anchors
    (event segmentation imagination side / Badre & D'Esposito
    rostro-caudal hierarchical decomposition / Pfeiffer-Foster
    hippocampal forward sweeps / Schacter constructive episodic
    simulation core network). Grep-able under both REE-native
    search and biology-search.

    Implementation-phase placement: phase-1 substrate cannot land
    before MECH-288 substrate (event_segmenter.py with input_stream
    label) lands. Once MECH-288 substrate is in place, MECH-321
    substrate is a thin policy-side module that subscribes to a
    MECH-288 BoundaryEvent queue filtered to input_stream=rollout
    and emits a re-segmented rollout proposal stream.

    Open Q-claims worth registering at substrate-landing time:

      - Q-XXX: mid-execution decomposition vs reactive-replanning --
        R4 verdict flagged this as a candidate child-Q-claim.
        Whether mid-execution decomposition is the same mechanism
        as reactive-replanning (sensorimotor feedback divergence
        from prediction) or distinct mechanisms is open. The
        substrate question is whether one mechanism reads both
        signals (PE on chunk's remainder + sensorimotor-feedback-
        divergence) or whether the two are separate substrates that
        happen to produce overlapping outputs on the execution
        phase.

      - Q-XXX: depth-cap policy -- RE-POSED 2026-07-27. The
        question as originally written ("3-4 levels suggested by
        R3 verdict, but the cap is an engineering choice; whether
        the cap should be hard (architectural commitment) or soft
        (adaptive based on rollout budget) is open") presupposes
        that decomposition_depth_cap is a free parameter. It is
        not, and while it is inert the hard-vs-soft question
        cannot be asked. Source: MECH-321 decomposition-scale
        scoping spike section 5a,
        evidence/planning/mech321_decomposition_scale_scoping_spike_2026-07-27.md
        (recommendation only; changed no claim status, confidence,
        live_status or v3_pending).

        WHY IT IS NOT FREE. The `depth` that
        PolicyDecomposition.evaluate() tests against depth_cap is
        read straight off traj.metadata["chunk_depth"]
        (ree-v3 ree_core/hippocampal/module.py), and ARC-071
        cannot mint a chunk above ChunkLibrary.max_depth
        (REEConfig.chunk_max_depth, default 3). So
        decomposition_depth_cap is a DERIVED MIRROR of
        chunk_max_depth, and its useful range is
        [2, chunk_max_depth]: above chunk_max_depth the
        mark-unreliable-by-cap branch is unreachable and the
        recursive leaf-tiling bound stops binding (4 and 100 are
        the same run), while at 1 every chunk is dropped rather
        than re-tiled and MECH-321 degenerates into a pure
        withholding mechanism. Sharper still: every MECH-321 run
        to date (V3-EXQ-815/816/816b/816c/816d/820) used one
        depth-1 seeded chunk, at which depth every value >= 2 is
        byte-identical -- the knob has never once exhibited a
        degree of freedom, so the literature argument the
        2026-07-27 provenance correction adjudicated was conducted
        over a parameter with no observed variation.
        Both mis-settings are now warned about at the REEAgent
        wiring site (ree-v3 depth_cap_config_issues() in
        ree_core/policy/policy_decomposition.py, emitted in
        ree_core/agent.py, contract C17 in
        tests/contracts/test_arc070_policy_decomposition.py).
        The guard warns and does not raise, so no
        currently-valid configuration changed behaviour.

        THE LIVE QUESTION, in two halves:
        (a) Should decomposition_depth_cap track chunk_max_depth
        automatically -- a derived default with an explicit
        opt-out -- rather than remain independently settable?
        (b) Is chunk_max_depth, which is the actual free and
        compute-bearing knob, the one that should move as compute
        allows? Solway et al. 2014's "raise the depth bound as
        compute permits" argument is currently attached to
        MECH-321's mirror, where there is nothing for it to bound;
        it belongs on ARC-071's chunk_max_depth. Note (b) is an
        ARC-071 question inherited here only by mis-attachment,
        and should register against ARC-071 / MECH-323 if
        registered at all.

        DO NOT REGISTER (b) SEPARATELY -- half of it is already
        registered. Q-085 (grain_as_controllability, registered
        2026-07-27 by /claim-synthesis on MECH-163) asks whether
        chunk_max_size x chunk_max_depth is a CONTROLLABILITY
        parameter and not only a formation one, which is the
        UPPER bound on the same knob; the 2026-07-27 MECH-321/323
        parameter lit-pull (REE_assembly 36901c01ed, Ramkumar
        2016 / Bo & Seidler 2009 / Solway 2014's explicit null on
        a subtask-size budget) supplies the LOWER bound from
        deliberation cost. (b) is the scaling question between
        those two bounds and belongs in Q-085's what_would_answer
        rather than in a new Q-claim. Only (a) is unregistered.
        Registration of (a) deferred: today it is a
        substrate-design decision, not a claim.

        [2026-07-27 UPDATE -- (b) IS NOW BUILT, (a) still open.]
        chunk_max_depth stopped being a fiat constant: ree-v3
        7c201f7326 added use_growable_chunk_depth (default OFF,
        bit-identical when OFF), under which the depth ceiling
        starts at chunk_max_depth and grows toward
        floor(chunk_deliberation_horizon x
        chunk_depth_budget_fraction), each level licensed by a
        realised marginal outcome gain. The fraction is ANCHORED
        (0.1 = 3/30) so the derivation returns exactly 3 at REE's
        real rollout horizon of 30 -- Solway licenses no
        replacement depth, so no new constant is asserted and only
        a LARGER deliberation budget derives a deeper hierarchy.
        Substrate change only; no claim status, confidence,
        live_status or v3_pending changed by this note.

        TWO CONSEQUENCES FOR MECH-321 SPECIFICALLY.
        (1) The mirrored ceiling MOVES. depth_cap's useful range
        is [2, chunk_max_depth] only while the depth ceiling is
        fixed; with growth on it is [2, derived_chunk_max_depth].
        depth_cap_config_issues() now takes that bound as an
        optional third argument and REEAgent passes it only when
        ARC-071 is on AND growing, so every shipped MECH-321
        configuration is byte-identical (contracts C17b).
        (2) A STRUCTURAL coupling was found while building it, and
        it bears on the depth-cap question directly: chunk depth
        is bounded by chunk SIZE. A chunk composes another only by
        containing it, so each level needs a strictly longer
        sequence and nothing deeper than
        (effective chunk-size ceiling - chunk_min_size + 1) can be
        minted -- 4 at the 2-5 budget, verified by construction
        (max_depth 5 and 4 are the same run). So depth and size
        are not merely analogous parameters on one trade-off; the
        size budget mechanically caps the depth budget, and growth
        refuses to raise the ceiling past it rather than raising
        it into inertness. Note this also confirms chunk_max_depth
        does have a real degree of freedom today -- 3 is BINDING,
        the substrate would otherwise mint depth 4 -- which is
        exactly what its mirror was found to lack.

      - Q-XXX: MECH-321 vs MECH-269b threshold coupling -- both
        read V_s; both may use thresholds. Whether the thresholds
        should be independent (per-consumer parameterisation) or
        coupled (single substrate threshold with per-consumer
        scale factors) is an integration question at substrate-
        landing time.
  evidence_quality_note: >
    [2026-08-21 governance, gov-20260821-0203, confirmed failure_autopsy_V3-EXQ-938_2026-08-20.json]: V3-EXQ-938 is the first run in the ARC-070/MECH-321 chain to reach a verdict at all (816/820 died on a low_vs_steps occupancy gate at 0.0/5.0; 816b/816d saturated the environment axis within 0.0007). It is cleanly executed: rate_match_ok true at a 1.85% arm gap with 0 seeds outside tolerance, aa_control_ok with max_abs_delta exactly 0.0, >=91 forced fires on both ON arms, ~277-287 boundary fires/arm, all 15 readiness preconditions met, non_degenerate true. Its result is a NULL, not a detected negative effect: harm delta -0.001338 with SE 0.002764 (t = -0.484), 19 seeds negative against 17 positive, and an observed |delta| that is 17.3% of the run's own 80%-power MDE (~0.007739). The pre-declared null (delta <= 0 within 1.0 x SE over >= 40 paired seeds) is satisfied exactly as written, so the run delivered the verdict it promised, scoped by its own statement to 'ARC-070's prediction-failure-selectivity leg AT THIS GRAIN'. The blanket weakens stamped on both claims is NOT applied, for two independent reasons. (1) ARC-070's own registered what_would_answer states that a failure of the DOWNSTREAM task-level benefit is 'EXPLICITLY NOT FALSIFYING' and belongs to MECH-321's separate narrower claim; 938's DV is a whole-episode mean harm signal, squarely that class, and its FALSIFYING clause (the mechanism does not engage when it architecturally should) is negated by the run, which shows the mechanism engaging throughout. (2) The same hypothesis-space question carries H-representation-axis as ALIVE ('forward-PE as currently computed is too coarse-grained to register the environment's actual uncertainty'), and 938's proximal readout -- secondary_fwd_pe_delta_yoked_minus_pe = -2.6e-05 -- is precisely that hypothesis's signature; a within-run rank normalisation does not fix a readout that is too coarse. So the run cannot discriminate 'selectivity is false' from 'the PE readout cannot locate the loci where decomposition would help'. Carry forward two readouts: C2_DECOMPOSITION_PER_SE_VS_OFF = -0.03681, roughly 27x the selectivity delta in the harmful direction (non-load-bearing, no SE reported, not adjudicated here) and the larger signal in the manifest; and substrate_stable_across_run = false, where all 132 cells share one hash 3a9826d0 but the stated substrate_commit 839ffe03 does not correspond to it, so this run cannot be mapped to a substrate commit -- recording-debt owed by any successor.

    Registered 2026-05-10 from ARC-070 lit-pull synthesis
    (evidence/literature/targeted_review_arc_070_decomposition/).
    All five lit-pull verdicts (R1-R5) folded into the
    functional_restatement; lit_conf 0.88 (indexer-computed) on
    parent ARC-070 carries to MECH-321 as the first child
    mechanism instantiating those verdicts.

    [2026-07-27 PROVENANCE CORRECTION -- depth cap attribution.
    Source: targeted_review_connectome_mech_321 lit-pull, landed
    REE_assembly 36901c01ed. The title previously read "Recursive
    multi-level decomposition with depth cap 3-4 (Badre &
    D'Esposito 2009; Koechlin & Summerfield 2007)". That
    attribution was an over-read and has been withdrawn. This is
    an attribution correction, NOT a finding that the cap is
    wrong, and NOT a change to any claim status or confidence.

    (1) Badre & D'Esposito 2009 supports MULTI-LEVEL decomposition
    -- ARC-070's R3 verdict stands untouched -- but never
    supported a level COUNT. The existing ARC-070 entry for that
    paper reads it correctly and explicitly labels its own "2-3
    levels" bound as a tractability suggestion, not a finding.
    Somewhere between that entry and this title, a tractability
    suggestion acquired a citation.

    (2) Badre & Nee 2018 (Trends Cogn Sci, 10.1016/j.tics.2017.
    11.005), Badre's own re-appraisal nine years on, concludes
    the evidence supports "neither a unitary model of lateral
    frontal function nor a unidimensional abstraction gradient".
    A depth cap of N is only well-defined if there is an ordered
    axis to count levels along; without one there is no number of
    positions to cap. Marked `weakens` at confidence 0.72 --
    withdrawing a justification is a weaker act than falsifying a
    parameter.

    (3) Koechlin 2003 (with Koechlin & Summerfield 2007 for the
    fourth level) does supply a real magnitude -- three cleanly
    dissociated fMRI loci in a lateral-PFC cascade, extended to
    four by frontopolar branching, and it runs out of cortex
    rather than stopping arbitrarily. But it is at ANATOMICAL
    grain: qualitatively heterogeneous, anatomically fixed control
    levels, each processing a different kind of context-conditional
    information. MECH-321's depth counts how many times ONE tiling
    operator recursively re-applies to its own output on the
    rollout stream. An architecture could instantiate Koechlin's
    four-level cascade while allowing its decomposition operator
    to recurse twice or ten times at any one of those levels.
    Marked `mixed` at confidence 0.55, weighted toward mapping
    fidelity: the paper is excellent and the inference from it is
    the weak link. Cite it as a magnitude prior only, with this
    grain caveat attached. A second gap applies unchanged from the
    existing ARC-070 entries: the cascade is measured during
    overt, externally cued performance, while MECH-321 acts on the
    imagined trajectory.

    (4) Solway et al. 2014 (Nat Commun) is filed as a deliberate
    NULL at the grain REE actually uses -- policy grain, subtasks
    over policy primitives. It is the natural arbiter and it
    declines to bound depth: its analysis "assumes that hierarchies
    are one level deep", a restriction "adopted to assure
    computational tractability", generalising "without any
    alteration to deeper hierarchies". Same move as MECH-321's,
    for the same stated reason. Marked `unknown` at confidence
    0.55. What that literature DOES constrain is partition
    LOCATION (bottleneck states), which is what the R5 bottleneck
    trigger mode already builds on -- so the campaign's effort is
    pointed the right way.

    Consequence for how the cap should be carried: it is a
    computational-cost argument, so it should MOVE as compute
    allows rather than sit at a fixed constant defended by
    citation. The implementation_note was already the honest
    version of this ("mirrors ARC-071's chunk_max_depth so the
    inverse operations stay symmetric"), and the open Q-claim on
    hard-vs-soft already anticipates it; the title has now been
    brought into line with the implementation_note rather than the
    other way round.

    Design question flagged but NOT settled here: if frontal
    control is several interacting networks at different scales
    rather than one recursive axis, "decomposition depth" may not
    be the right control parameter at all, and the
    biologically-faithful analog would be a small set of
    decomposition operators at distinct scales rather than one
    operator recursing N times. Bigger than this correction.]

    [2026-07-27 SCOPING SPIKE -- the design question flagged
    immediately above is now OWNED and answered. Source:
    evidence/planning/mech321_decomposition_scale_scoping_spike_2026-07-27.md.
    Recommendation only; NO status / confidence / live_status /
    v3_pending change. The question splits, and the halves differ.

    DEPTH HALF -- keep the single tiling operator, but replace the
    justification. decomposition_depth_cap is not a free parameter
    over an abstraction axis; it is a DERIVED MIRROR of ARC-071's
    chunk_max_depth, because the `depth` it tests is read straight
    off traj.metadata["chunk_depth"] (hippocampal/module.py) and is
    bounded above by ChunkLibrary.max_depth=3 as a structural fact.
    So: values > chunk_max_depth are INERT (the mark-unreliable-by-
    cap branch is unreachable); the value 1 DISABLES decomposition
    (every chunk has depth >= 1, so all are dropped rather than
    re-tiled); and neither setting warns. Sharper still, every
    MECH-321 run to date seeds one depth-1 chunk
    (seeded_chunk_sequence [0,1,2], minted via the replay path which
    mints depth=1 unconditionally), and at depth 1 the values 2, 3,
    4, ... 100 are byte-identical runs -- so at the only
    configuration the parameter has ever run under it is a BINARY
    switch, not a depth. The literature argument was therefore
    conducted over a parameter with no observed degrees of freedom.
    Corollary: Badre & Nee does not reach this parameter at all --
    it dissolves a unidimensional ABSTRACTION GRADIENT, while
    depth_cap indexes the ARC-071 ChunkLibrary's learned
    COMPOSITIONAL stratification, which that paper's replacement
    model retains rather than denies. And Solway's "a cost-derived
    cap should MOVE as compute allows" is right but mis-addressed:
    it belongs on chunk_max_depth, which is the free, compute-
    bearing knob, not on MECH-321's mirror of it.

    HETEROGENEITY HALF -- NOT YET DECIDABLE, and not for want of
    thought: the instrument has never been switched on. REE ALREADY
    HAS the "small set of operators at distinct scales" the flag
    hypothesised it would need to acquire -- MECH-288 ships a
    two-scale heterogeneous detector (fast: PE-threshold z-score
    over z_world+z_self; slow: BOCPD-Gaussian over z_goal at
    hazard 1/40). MECH-321 discards it twice: boundary_on()
    collapses List[BoundaryEvent] to fired:bool + posterior:max and
    MECH-321 never reads .events[].scale; and the call site builds
    latent_signature from z_world+z_self only, with NO z_goal, so
    the BOCPD detector returns (False, 0.0, []) for want of its
    stream -- the slow scale is STRUCTURALLY DEAD on the rollout
    stream. MECH-321 is a single-scale consumer of a two-scale
    substrate. There is consequently no observation of
    scale-differentiated decomposition to decide from.

    PROBE that decides it (default-off, no behaviour change, no new
    claim, runs on the EXISTING 816 harness): add z_goal to the
    rollout latent_signature; read boundary.events[].scale rather
    than only .fired; emit per-scale fire counts. Question: do fast
    and slow fire at DISSOCIABLE rollout positions? Dissociable ->
    register the scale-differentiated extension (a MECH-level
    extension of MECH-321, NOT a new ARC -- the operators already
    exist as MECH-288 scales). Slow never fires, or fires only
    coincident with fast -> one effective scale on the imagination
    stream, the single-operator position stands, close the question.
    All three readings are informative; the middle is a live
    possibility because z_goal is an INTEGRATOR and may not move
    within one short rollout.

    Why this probe is NOT a duplicate of the live V3-EXQ-816
    campaign: that campaign's two open axes are trigger SENSITIVITY
    (H-env-underdrives-uncertainty) and trigger VALIDITY
    (H-vs-proxy-saturation), both attacking the silent end --
    arm1_vs_trigger_total = 0 across the whole campaign, i.e.
    MECH-321's DECLARED PRIMARY (V_s-drop) trigger has never fired
    once, and every decomposition ever observed came from the
    boundary/fast half alone. The scale question is a third axis --
    trigger COMPOSITION -- and it is the only one attacking the
    SATURATED end (decomp_fired_frac_arm1 = 1.0). A trigger that
    always fires is exactly as uninformative as one that never
    does; "fast 100% / slow 3%" is a measurement where "1.0" is
    not.]

    Not yet experimentally tested. Discriminative-pair validation
    experiment specified in functional_restatement; deferred until
    MECH-288 substrate lands (event_segmenter.py with input_stream
    label extension).

    Phase-1 substrate placement: subscribes to MECH-288
    BoundaryEvent queue filtered to input_stream=rollout. Pure-
    arithmetic policy-side regulator (no learned parameters at
    phase-1; threshold v_s_decompose_threshold and depth_cap are
    config knobs).

    Falsifiable on three conditions:
      (a) ARM_0 vs ARM_1 contrast in the discriminative-pair
          experiment shows the V_s-drop trigger fires and
          decomposes;
      (b) ARM_1 vs ARM_2 dissociation on one-shot vs repeated-
          structure environments confirms PE-driven primary;
      (c) lesioning MECH-288 substrate impairs both observation-
          side anchor partition AND imagination-side decomposition
          (the R2 bidirectional-substrate commitment).

    Failure on any of the three triggers a substrate revisit; the
    revisit candidates are (a) E2 forward-model disagreement as
    primary trigger instead of V_s, (b) parallel-but-coupled
    architecture replacing the bidirectional-substrate commitment.

    [2026-07-29 governance, GOV-DIAG-1 diagnostic-chain recurrence]: check_diagnostic_chain_recurrence.py
    flags a 5-hit pure-diagnostic (claim_ids=[]) no-verdict chain on this claim: V3-EXQ-816b -> 816c ->
    816d -> 830 (830 counted via two bears_on tokens). Every run in the chain returned a verdict-free
    "environment axis saturated" / "substrate not ready" reading rather than a discrimination -- three
    consecutive env-axis harshening attempts (816, 816b, 816d) converged on forward-PE ~0.0086-0.0087,
    each falling short of the 0.01 discrimination floor by roughly the same margin. Per GOV-DIAG-1 this
    is a MIS-POSED-question signal, not an under-powered one: refuse a fourth same-question env-harshening
    letter. The chain's own diagnosis (V3-EXQ-830's autopsy) already points at (a) E2 forward-model
    disagreement replacing V_s as trigger, or (b) redesigning the discriminating STATISTIC rather than
    escalating environment harshness again -- a future session should re-pose the operationalization along
    one of those axes rather than queuing 816e. Not chipped (GOV-DIAG-1 routing is governance-owned).

    [2026-07-30 governance, GOV-DIAG-1 recurrence count UPDATE]: check_diagnostic_chain_recurrence.py
    now counts 6 hits on this chain (V3-EXQ-839's mid-execution reachability diagnostic joined via a
    shared bears_on token). This is NOT a repeat of the mis-posed environment-harshening pattern the
    2026-07-29 note refused -- 839 probes a DIFFERENT axis (reachability of the mid-exec hook, not
    forward-PE discrimination floor) and reached a clean verdict (validated, 415/0 clean split), not
    another verdict-free saturation reading. Its confirmed autopsy recommends a MECH-321 R4 EVIDENCE
    successor (claim_ids=[MECH-321], behavioural/task effect of the now-reachable hook) -- exactly the
    "redesign the discriminating statistic" response the 2026-07-29 note already called for, not a
    fourth same-question diagnostic letter. Chipped this cycle (governance-owned per CLAUDE.md
    2026-07-30 rule: an autopsy's own routing is chipped by governance once ratified, not by the
    autopsy session itself).

    [2026-08-02 governance, V3-EXQ-867, confirmed failure_autopsy_V3-EXQ-867_2026-08-02, first behavioural-effect test post-substrate-build]: the substrate gap 844 identified is CLOSED (harm_bias()/select_harm_aware_leaves() implemented, wired, default-off/bit-identical, 32 pre-existing + 11 new contracts). But this run's own P0 precondition harm_bias_engages FAILED on ARM_SELECTION_ON: decomp_n_harm_bias_nonzero=0 and decomp_n_harm_override_fires=0 across all 6 seeds and both arms -- the harm-aware bias never fired even once. Root cause: the run used the default env with no SD-029 scheduled_external_hazard tuning, so z_harm_a_norm never cleared decomposition_harm_threat_floor=0.1 (matches the already-documented SD-037/V3-EXQ-620 untuned-baseline finding). Not a substrate ceiling, not an implementation gap, not evidence bearing on the harm-aware-selection question -- the manipulation never got a chance to engage. non_contributory. Route: same-question re-queue (867a) applying the SD-029 hazard-density overlay from SD-037's Phase 1b plan, with a cheap preflight assertion that decomp_n_harm_bias_nonzero>0 before committing to the full design. pending_retest_after_substrate stays true (substrate build is done but env-tuning re-run is still owed).

    [2026-08-03 governance, V3-EXQ-867a, confirmed failure_autopsy_V3-EXQ-867a_2026-08-03, SD-029
    hazard-tuned retest]: the manipulation now genuinely engages -- harm_bias_engages precondition met,
    972 nonzero calls on the ON arm (867's three additional structural fixes found+confirmed this same
    autopsy session: affective harm stream, sensory harm stream, valence_harm_enabled -- all three
    needed together). The load-bearing C1_TASK_OUTCOME_IMPROVES criterion still fails but is severely
    underpowered: only 2 seeds (11, 23) qualified for the matched-pair design tier, with OPPOSITE-signed
    deltas (seed 11 -0.040, seed 23 +0.0125), netting a small negative mean against an already-weak
    1xSE bar. A third, correctly-excluded unmatched seed (47) shows a substantially larger beneficial
    effect (+0.127) in the intended direction -- a material covariate against reading this as a
    confident null. non_contributory/measurement_test_design_defect (not a confident weakens).
    substrate_ceiling re-derive brake stays at 0 prior ceiling hits, does not fire. Route:
    /queue-experiment 867b to grow the matched-seed pool (more seeds from the already-characterized
    HAZARD_TUNED_SEED_POOL, or retune the hazard/decomposition-trigger schedule to raise the
    both-arms-decompose hit rate). pending_retest_after_substrate STAYS true (n=2 is not decisive).

    [2026-08-04, V3-EXQ-867b, confirmed failure_autopsy_2026-08-05_pending_review_batch.md section 2]:
    867b screened a 48-candidate pool from HAZARD_TUNED_SEED_POOL (hard floor n_pairs>=6, no min()
    softening) -- exactly the routing this claim's own note above called for. Result: still only 4
    matched pairs with a measurable window; the pool is EXHAUSTED (only 7 of 48 screened candidates
    screen-matched at all; screen_yield_met: false). C1_TASK_OUTCOME_IMPROVES measured -0.0485 (wrong
    direction), non-degenerate=false, degeneracy_reason: "only 4 matched-abort-pair(s)... below the
    pre-registered hard floor of 6... pool EXHAUSTED." MORE FUNDAMENTALLY: screen_soundness_check
    shows the screen's own monotonicity assumption is FALSIFIED -- 5 of the 7 screen-matched seeds
    did NOT actually land in both_decompose at the full measurement schedule (held: false) -- direct
    evidence the ON-arm manipulation itself perturbs whether/when decomposition fires, undermining
    the matched-pair premise structurally, not just statistically.
    [CORRECTED 2026-08-10 governance, GFLAG-0021: the preceding "ON-arm manipulation perturbs
    decomposition" reading was WITHDRAWN by the 2026-08-09 GFLAG-0014/0015 correction (see the
    what_would_answer field below) -- 867b's own manifest contradicts it (the violation is in the
    OFF arm, which is provably inert). The actual cause is an RNG-reset asymmetry: screen cells
    call _run_cell directly with no reset, while measurement cells enter through arm_cell (which
    resets all RNG), so the two phases measure DIFFERENT agents -- not that the manipulation itself
    perturbs decomposition. The ratified routing (redesign the DV away from post-hoc
    divergence-tick matching) is unaffected; only the diagnosis of WHY the screen-then-match family
    is invalid changes.] The 2026-08-05 autopsy (routing
    confirmed, NOT YET ratified by /governance per the standing autopsy-does-not-chip-its-own-routing
    rule) explicitly refuses a same-design V3-EXQ-867c and instead calls for a redesigned DV/matching
    strategy (e.g. an unconditional whole-episode harm-rate comparison across all measurement seeds
    that does not require post-hoc divergence-tick matching). Tracked in
    evidence/planning/hypothesis_space_registry.v1.json as qid mech321_harm_aware_selection_task_effect,
    decision.decidable=false, live_gate="redesigned DV/matching strategy... not yet queued". Seed 47's
    unmatched +0.127 beneficial effect (noted in 867a) was not carried into 867b's measurement set
    (screen_on_arm_skipped: true) and remains uninvestigated. No successor experiment queued or run as
    of 2026-08-07 -- do not queue a fifth same-design iteration off HAZARD_TUNED_SEED_POOL or a
    retuned trigger schedule alone; the pool-exhaustion and screen-soundness findings apply to that
    whole design family, not just to 867b's specific parameters.
  digestion_note: >
    [2026-08-07, thought-digestion] The falsifier this claim needs (C1_TASK_OUTCOME_IMPROVES on
    harm-aware vs baseline redecomposition, matched at SD-029 hazard-tuned engagement) is not in
    question, but four consecutive experiment generations (V3-EXQ-844, 867, 867a, 867b) each failed
    for a DIFFERENT reason, and the most recent (867b, 2026-08-04) found the entire
    screen-then-match measurement-design family structurally invalid (screen_soundness_check
    falsified). The 2026-08-05 autopsy recommends a redesigned DV (e.g. unconditional whole-episode
    harm-rate comparison) but that redesign does not exist yet and the autopsy's own routing is not
    yet ratified by /governance. Do NOT draft a fresh what_would_answer assuming a specific redesign
    until either (a) governance ratifies the routing and a redesigned experiment is queued, or
    (b) governance re-scopes the question. The what_would_answer below is written to stay valid
    regardless of which specific redesign eventually lands (it states preconditions and
    confirming/falsifying signatures at the level of the scientific question, not any one script) --
    remove this note once a redesigned experiment reports.
  what_would_answer: >
    NON-DEGENERACY PRECONDITIONS (both must hold before any C1 reading is trusted): (1) the
    harm-aware selection manipulation must genuinely engage -- decomp_n_harm_bias_nonzero > 0 on the
    ON arm within the measurement window. Met since V3-EXQ-867a/867b (972-1005 nonzero calls);
    V3-EXQ-867 failed this precondition entirely (0 nonzero calls, non_contributory). (2) whatever
    matching/aggregation strategy is used to compare ON vs OFF must have its OWN validity
    demonstrated on the run's own data, not assumed. V3-EXQ-867b's screen_soundness_check falsified
    this for the divergence-tick, screen-then-match design specifically: a short-window screen
    predicted both-arms mid-execution decomposition for 7 candidate seeds, but 5 of those 7 did not
    actually co-decompose at the full measurement schedule -- direct evidence that the harm-aware
    manipulation itself perturbs whether/when decomposition fires, which the matched-pair design
    needs to hold constant to isolate a harm-outcome effect. Any future design must either (a) verify
    its own matching assumption empirically before trusting its C1 statistic, or (b) remove the
    dependence on post-hoc divergence-tick matching altogether (e.g. an unconditional whole-episode
    harm-rate comparison across all measurement seeds at matched abort-rate arms, as recommended by
    the 2026-08-05 pending-review-batch autopsy).

    CONFIRMED by: a properly-designed, adequately-powered comparison -- precondition (1) met AND
    precondition (2) satisfied by construction or by verification -- showing
    C1_TASK_OUTCOME_IMPROVES: realised task harm is reduced (post-divergence harm signal less
    negative) in the harm-aware (ON) arm relative to harm-blind (OFF), by an effect exceeding ~1x
    SE, with a CONSISTENT direction across the comparison units (not the mixed, opposite-signed
    pattern seen across 867a's n=2 matched pairs [-0.040, +0.0125] and the unmatched seed 47's
    +0.127, nor 867b's n=4 mean of -0.0485). The mechanistic corroboration criterion (forward-PE also
    lower in ON, non-load-bearing, already supported by V3-EXQ-844) should continue to hold
    alongside it.

    FALSIFIED by: a properly-designed, adequately-powered run -- with both preconditions satisfied --
    that still shows no consistent task-outcome improvement, or shows harm moving in the wrong
    direction (as V3-EXQ-844's original whole-run statistic and V3-EXQ-867b's n=4 matched mean both
    did). This would mean the harm-aware redecomposition selection mechanism (Stage-1 graded
    harm_bias + Stage-2 select_harm_aware_leaves, SD-hazard-aware-policy-decomposition, built
    2026-08-01), despite now correctly engaging and correctly reading the harm signal (z_harm_a via
    the residue field's VALENCE_HARM_DISCRIMINATIVE channel), does not translate into improved task
    outcomes -- a genuine mechanistic finding, distinct from the four prior non-contributory/
    inconclusive readings (V3-EXQ-844: mechanistically-engages-but-weakens-for-harm-outcome-
    specifically; V3-EXQ-867: manipulation never engaged; V3-EXQ-867a: n=2, underpowered; V3-EXQ-867b:
    n=4, pool-exhausted AND the matching design itself falsified).

    CURRENT BLOCKER (as of 2026-08-07, verify before acting on this note): no design satisfying
    precondition (2) has been built or queued. V3-EXQ-867a/867b already tried "grow the matched-abort-
    pair pool from HAZARD_TUNED_SEED_POOL" and hit exhaustion (48 screened, 7 matched, only 4
    measurable, still below the n>=6 floor) plus the screen-soundness falsification above; the
    2026-08-05 failure-autopsy (routing RATIFIED by /governance 2026-08-07) explicitly
    recommends AGAINST a same-design V3-EXQ-867c and instead calls for a redesigned DV/matching
    strategy. Tracked as hypothesis_space_registry qid mech321_harm_aware_selection_task_effect,
    decision.decidable=false, live_gate="redesigned DV/matching strategy... not yet queued". Do not
    queue a fifth same-design iteration off HAZARD_TUNED_SEED_POOL or a retuned trigger schedule
    alone -- the pool-exhaustion and screen-soundness findings apply to that whole design family, not
    just to 867b's specific parameters.

    [2026-08-09 governance, GFLAG-0014/0015 correction]: the "screen_soundness_check... direct
    evidence the ON-arm manipulation itself perturbs whether/when decomposition fires" reading
    above (and in the digestion_note / what_would_answer preconditions) is WRONG -- 867b's own
    manifest contradicts it. The violation appears in the OFF arm, where the manipulation is
    provably inert (decomp_n_harm_bias_nonzero == 0 on every OFF cell); OFF-arm
    multi_action_commits DECREASED from the 4-episode screen to the 12-episode measurement on 5
    seeds, which is impossible for a monotone counter over a prefix extension; and at full
    schedule the matching held PERFECTLY (on_only and off_only tiers both EMPTY). The actual
    cause is an instrumentation defect: screen cells call _run_cell directly with no RNG reset,
    while measurement cells enter through arm_cell (which calls reset_all_rng) -- so the two
    phases measure DIFFERENT agents, and the prefix-monotonicity premise the screen-soundness
    check assumed never applied. This does NOT change the ratified routing (redesign the DV
    away from post-hoc divergence-tick matching remains correct -- pool-exhaustion alone already
    forced that) -- only the RATIONALE for why the screen-then-match family is invalid changes,
    from "the manipulation perturbs decomposition" to "the screen and measurement phases are
    non-comparable agents due to an RNG-reset asymmetry". Full analysis:
    evidence/planning/mech321_harm_aware_selection_measurement_redesign_staged_2026-08-08.md
    sections 2 and 8. hypothesis_space_registry.v1.json's qid
    mech321_harm_aware_selection_task_effect already carries the corrected text in its
    `live_gate` field (added 2026-08-08) but `resolution.basis` / `observation_bottleneck` were
    left unedited pending this governance adjudication -- syncing those two fields into the
    frozen ledger is /failure-autopsy Step 9b's job (single producer), not governance's; chipped
    separately rather than patched inline here.
  location: docs/architecture/policy_primitive_granularity.md

- id: MECH-322
  implementation_note: >-
    CARVE-OUT PATH BUILT 2026-07-22 in ree-v3/ree_core/policy/policy_chunking.py as
    ChunkAccumulator.record_replay_sequence (agent entry point:
    REEAgent.note_chunk_replay_sequence), behind REEConfig.use_chunk_replay_origin_path.
    SAFETY POSTURE AS REGISTERED: that flag is False by default EVEN WHEN chunking itself is on,
    so the shipped default is MECH-094-strict and no chunk can originate from replayed or imagined
    content. It is a SEPARATE METHOD, not a parameter on the default path -- the strict path
    (record_step) refuses hypothesis_tag=True outright and cannot be made permissive by any
    configuration. All three registered conditions are ANDed and each fails CLOSED independently:
    (a) value-tag at or above the replay_value_quantile of the REAL-execution outcome distribution,
    so the bar is set by waking achievement and an empty real history makes it unreachable (+inf);
    (b) designated SD-017 sleep phase -- the agent entry point reads the serotonin phase when not
    given one, so waking DMN (MECH-292/293 ghost-goal probe territory) reads False and is refused;
    (c) replay_origin=True audit flag, carried into the proposal-candidate metadata, plus the
    accelerated-dissolution deadline retiring an uncorroborated chunk direct to DISSOLVED, with any
    real waking execution resetting the counter. Refusals are counted, not silently dropped.
    Pinned by contracts C3/C4/C5. FIRST EXPERIMENTAL EVIDENCE landed 2026-08-07 (V3-EXQ-873a,
    supports) -- see evidence_quality_note. v3_pending stays True (single-run supports does not
    alone clear a v3_pending gate).
  title: "policy_composition.sleep_replay_value_conditioned_chunking_consolidation -- narrow exception path that permits ARC-071 chunk formation from replayed (hypothesis_tag=True) sequences during designated sleep phase, IF the replayed sequence carries a value-tag from prior real-executed episodes AND the formed chunk carries a replay_origin=True audit flag with accelerated dissolution if not corroborated by real waking execution within N episodes. Resolves ARC-071 lit-pull R6 governance escalation (Option B carve-out, picked 2026-05-11)."
  claim_type: mechanism_hypothesis
  subject: policy.composition.sleep_replay_value_conditioned_consolidation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-08-07
    needs_review: false
    evidence:
      from: "v3_exq_892_mech322_replay_corroboration_survival_20260808T051616Z_v3#failure_autopsy_V3-EXQ-892_2026-08-08"
      as_of: 2026-08-08
      verdict: "supports (diagnostic) -- 3rd consecutive non-negative touchpoint; closes 873a's open corroboration-survival question"
  implementation_phase: v3
  v3_pending: true
  registered_utc: "2026-05-11"
  depends_on:
    - ARC-071     # the chunking machinery this carve-out applies to
    - MECH-094    # the strict gate this is a narrow exception to
    - SD-017      # sleep substrate (designated sleep phase requirement)
    - SD-039      # anchor goal-payload (value-tag transport)
    - SD-014      # valence vector recording (value-tag substrate)
  functional_restatement: >
    MECH-322 is the explicit Option-B carve-out resolving the R6
    safety-vs-realism gap surfaced by the ARC-071 lit-pull
    (targeted_review_arc_071_composition synthesis, 2026-05-10).

    Background: ARC-071 (policy.composition_via_repeated_grounding)
    pre-registered with strict MECH-094 hypothesis_tag=False
    gating -- chunks form ONLY from real executed sequences. The
    lit-pull (Albouy 2013 + broader sleep-motor-consolidation
    literature) found that biology does NOT cleanly gate the
    chunking write path: hippocampal-striatal sleep replay drives
    chunking-circuit consolidation, with hippocampal activity
    leading ventral striatal replays during sleep-dependent motor
    sequence consolidation. ARC-071's strict pre-registration is
    therefore MORE CONSERVATIVE than biology, sacrificing realism
    for safety. The R6 governance decision (2026-05-11): carve out
    a narrow, audit-trail-bearing exception path rather than
    relax MECH-094 globally.

    The mechanism: during designated sleep phase, ARC-071 may
    write a chunk from replayed (hypothesis_tag=True) sequences
    IF AND ONLY IF three conditions hold jointly:

    (a) VALUE-TAG REQUIREMENT. The replayed sequence must carry
        a value-tag from prior real-executed episodes. The
        value-tag is the SD-014 valence vector + accumulated
        E3-score-receipt over the sequence's prior real
        executions. The required threshold is high-positive
        (suggested default: top-quartile of the agent's
        cumulative E3-score-receipt distribution; calibrated at
        child-MECH design time). This mirrors the Albouy 2013
        reward-prediction-biased coupling -- biology selectively
        consolidates replays of high-reward sequences.

    (b) SLEEP-PHASE REQUIREMENT. The replay must occur during
        designated sleep mode (SD-017 substrate), NOT during
        waking DMN. Waking DMN replay (which is when MECH-292/
        MECH-293 ghost-goal probes operate) remains gated by
        MECH-094 strict. The carve-out is sleep-only.

    (c) AUDIT-FLAG + DISSOLUTION DEADLINE. The chunk formed via
        this path carries a replay_origin=True flag (distinct
        from the real-execution-formed default of
        replay_origin=False). Replay-origin chunks face
        ACCELERATED DISSOLUTION: if not corroborated by real
        waking execution within N episodes (suggested default:
        50-100 episodes; child-MECH calibration), the chunk
        dissolves automatically without requiring the standard
        outcome-variance-rises-above-F_high trigger. This is the
        self-correcting deadline: chunks formed from replay must
        prove themselves through real execution, or they are
        retired.

    What MECH-322 PRESERVES:
      - MECH-094 strict gating in all other write paths. MECH-322
        is a NEW write-path with its own narrow conditions, NOT
        a relaxation of MECH-094 itself. MECH-094 still gates
        the default ARC-071 write path, residue writes,
        z_world updates, etc.
      - MECH-292 / MECH-293 ghost-goal bank waking-probe
        semantics. Waking DMN remains MECH-094-gated; the
        ghost-goal probes do not produce chunks via MECH-322.
      - SD-039 anchor payload semantics. Chunks formed via
        MECH-322 carry SD-039 goal payloads inherited from the
        prior-real-executed sequence's payload (not synthesised
        de novo from replay).
      - ARC-007 strict value-flat hippocampal proposals. The
        MECH-322-formed chunk goes into the proposal pool as a
        value-flat candidate; downstream E3 evaluation supplies
        the value at selection time, as for any other proposal.

    What MECH-322 INTRODUCES:
      - A second chunk-write-path with explicit conditions and
        an audit trail.
      - The replay_origin field on every chunked primitive in
        the proposal pool (default False; True only for
        MECH-322-formed chunks).
      - A dissolution-deadline counter on replay-origin chunks
        (resets to zero on each corroborating real-execution).
      - A sleep-mode gate on the chunking write-path (currently
        ARC-071 has no sleep/wake distinction in its
        pre-registration; MECH-322 introduces one).

    Falsifiable prediction: an REE agent with MECH-322 active
    should show OVERNIGHT IMPROVEMENT on a chunked-task signature
    -- behavioural latency on a structurally repeating
    sub-sequence should drop more after a sleep-phase-included
    episode block than after a no-sleep block of equivalent
    waking exposure. This mirrors the Albouy 2013 finding
    structurally. The discriminative pair: (ARM_0) MECH-322 OFF
    baseline, sleep replays do not write chunks; (ARM_1)
    MECH-322 ON, sleep replays meeting all three conditions
    write chunks. ARM_1 should show measurably faster chunk
    formation on repeating sub-sequences AND comparable safety
    metrics (no spurious-chunk pathology under structured-noise
    perturbation).

    Risk model + safety bounds:
      - Hallucinated-chunk risk: a replayed sequence the value
        system mis-evaluates as high-reward could be promoted
        into the chunking pool. Mitigated by (i) value-tag
        threshold being high-positive (top-quartile, not just
        positive); (ii) accelerated-dissolution deadline forcing
        unvalidated replay-chunks out within N episodes;
        (iii) audit flag enabling diagnostic detection of
        replay-origin chunks contributing to behavioural
        pathology.
      - Sleep-phase mis-gating risk: if SD-017 sleep-mode flag
        is set inappropriately during waking, MECH-322 could
        fire when it should not. Mitigated by SD-017 substrate
        invariants enforcing sleep-mode entry conditions.
      - Replay-origin metadata propagation: chunks-of-chunks
        recursion (R4 verdict) raises the question of whether a
        higher-grain chunk built from a mix of real-origin and
        replay-origin sub-chunks inherits replay-origin status.
        Conservative default: ANY replay-origin sub-chunk taints
        the parent chunk to replay-origin. Open Q-claim for
        child-MECH design.

    What MECH-322 is NOT:
      - Not a global relaxation of MECH-094. MECH-094 remains
        strict in all other write paths.
      - Not a goal-directed-system carve-out. The mechanism
        applies only to ARC-071 chunking writes; goal seeding,
        residue writes, z_world updates remain MECH-094-strict.
      - Not waking DMN. The carve-out is sleep-only.
      - Not unconditional value-replay-driven plasticity.
        Three conditions must hold jointly; the audit trail
        and dissolution deadline are non-negotiable.
  evidence: []
  evidence_quality_note: >
    Registered 2026-05-11 to resolve the R6 governance escalation
    from the targeted_review_arc_071_composition synthesis
    (2026-05-10). Lit grounding inherited from ARC-071 lit-pull
    R6 verdict (Albouy 2013 hippocampal-striatal sleep replay
    drives chunking-circuit consolidation; broader sleep-motor-
    consolidation literature including Lansink 2009 and van der
    Meer 2016 striatal-replay work). The biology supports the
    carve-out direction: sleep-replay-driven chunk consolidation
    happens, is reward-prediction-biased, and is part of normal
    motor-skill acquisition.

    No primary lit-pull commissioned for MECH-322 specifically;
    the R6 verdict in targeted_review_arc_071_composition
    supplies the grounding. A follow-on lit-pull may be
    warranted if the child-MECH design pass needs sharper
    parameterisation of the dissolution-deadline N or the
    value-tag threshold. Candidate further sources for the
    follow-on: Lansink 2009 (hippocampal-VTA-ventral striatal
    coupling); van der Meer 2016 (striatal replay during sleep);
    Wilhelm 2011 sleep-dependent motor sequence learning;
    selective replay-disruption studies (Girardeau 2009 ripple
    disruption impairs spatial memory consolidation -- analog
    for chunking).

    Not yet experimentally tested. First validation experiment:
    discriminative pair (MECH-322 OFF baseline vs MECH-322 ON)
    on a structurally repeating sub-sequence task with sleep-
    phase included episodes vs no-sleep episodes; metric is
    behavioural latency drop on within-chunk transitions.

    [2026-08-03 governance, V3-EXQ-873, confirmed failure_autopsy_V3-EXQ-873_2026-08-03, FIRST
    experimental test]: the load-bearing criterion (C1) reads FAIL only because a worst-of-3-seed
    readiness precondition (a real high-value candidate must naturally emerge by a fixed sleep
    checkpoint) failed in 1/3 seeds; in the other 2/3, the AND-gate minted a replay_origin=True chunk
    exactly as specified, and every fail-closed branch tested (wake-refusal, master-switch-off,
    low-value, no-candidate) passed cleanly across all 9 cells. Read as
    mixed/measurement_test_design_defect leaning supports (an overly strict worst-of-N-seed
    aggregation conflating a stochastic precondition with a deterministic plumbing check), NOT a
    substrate ceiling or claim weakening. Route: /queue-experiment V3-EXQ-873a with a decoupled,
    fraction-based readiness gate and 6-8 seeds. v3_pending stays true pending the corrected re-run.

    [2026-08-07 governance, V3-EXQ-873a, confirmed failure_autopsy_V3-EXQ-873a_2026-08-07,
    diagnostic-purpose PASS routed through /failure-autopsy per this cycle's broadened gate]:
    supports -- MECH-322's first genuine experimental evidence. Fixed 873's seed-veto instrument
    defect (decoupled stochastic readiness from deterministic mint-correctness, n=8 seeds). 7/8
    seeds cleared readiness; all 7 minted correctly; all fail-closed checks (wake refusal,
    low-value refusal, master-switch-off) held across 8 seeds x 3 arms. Note: all 7 minted
    replay-origin chunks dissolved without real-execution corroboration by run's end -- expected
    safety-valve behavior, not a defect, but leaves "can a replay-origin chunk survive
    corroboration" as a distinct open question. Routed to a new low-priority /queue-experiment
    entry for that question (not a lettered 873b -- 873a's own question is answered). No status
    change (v3_pending stays true; single-run supports does not alone clear the gate).

    [/governance 2026-08-08, V3-EXQ-892, confirmed failure_autopsy_V3-EXQ-892_2026-08-08, diagnostic
    PASS, routed through /failure-autopsy per this cycle's broadened diagnostic-gate rule]: answers
    the exact open question 873a's autopsy deferred. 57.1% of replay-origin-tagged chunks survived
    to real corroboration under the realistic substrate-default deadline (N=75); 85.7% corroborated
    at least once given unlimited exposure window (N=200). 873a's earlier 0%-survival finding is now
    explained as an artifact of an artificially short deadline, confirmed by a clean negative control
    in this same run -- corroboration timing spans nearly the full post-checkpoint window (offsets 1
    to 88 of 100 episodes), so a short deadline specifically excludes late-corroborating chunks that
    are part of the true recurrence distribution. Autopsy routing: none (descriptive closure, not a
    build/gate trigger); companion_fix backfilled 873a's own missed Step 9b hypothesis-space update
    (H-mech322-andgate-fires-and-fails-closed -> confirmed, already applied to
    hypothesis_space_registry.v1.json by the autopsy session). This is MECH-322's 3rd consecutive
    non-negative experimental touchpoint (873 mixed -> 873a supports -> 892 supports). Per 873a's own
    note, a single supports does not alone clear a v3_pending gate, and 3 non-negative touchpoints is
    not read as equivalent to a build-clearing result -- v3_pending stays true, no status change.
  notes: >
    The carve-out is the smallest possible relaxation of
    MECH-094 strict gating that closes the R6 realism gap. The
    cleanest framing is that MECH-322 is a NEW write-path with
    its own conditions, NOT a relaxation of MECH-094 itself.
    MECH-094 strict gating remains the default; MECH-322 is the
    exception with audit trail.

    Cross-link to sleep_substrate_plan.md: MECH-322 fires only
    during designated sleep mode (SD-017). The sleep substrate
    must expose a sleep-mode flag that MECH-322 reads. SD-017
    substrate landed 2026-05-09 (V3-EXQ-503a PASS); the
    sleep-mode flag plumbing is in place.

    Cross-link to MECH-292 / MECH-293 ghost-goal bank: waking
    DMN remains MECH-094-strict. The ghost-goal probes during
    waking do NOT produce chunks via MECH-322. This is the
    sharp boundary between the two replay-related machineries
    -- sleep replay can chunk (under MECH-322 conditions);
    waking DMN replay cannot.

    Open Q-claims for child-MECH design pass:
      - Replay-origin propagation under chunks-of-chunks
        recursion (R4 verdict from ARC-071 lit-pull): does a
        parent chunk inherit replay-origin status from any
        replay-origin sub-chunk, or only when ALL sub-chunks
        are replay-origin? Conservative default: any taint.
      - Dissolution-deadline N parameter: child-MECH design
        should pin a starting value (50-100 episodes suggested;
        validation experiment will refine).
      - Value-tag threshold: top-quartile suggested as starting
        heuristic; child-MECH design should specify the
        underlying distribution (per-task or global E3-score
        receipt).
      - Diagnostic instrumentation: replay_origin field needs
        to be queryable from manifest output for safety
        auditing.

    Pathology cross-references (speculative, pre-validation):
      - Sleep-deprivation-induced motor-skill-learning
        impairment: candidate behavioural signature for
        MECH-322 OFF or sleep-substrate failure. Biology
        supports: sleep-deprived subjects show impaired motor
        sequence consolidation.
      - PTSD-like rigid-chunk pathology: candidate signature
        for MECH-322 firing on traumatic replay with
        inappropriate value-tag (high-affect-mistaken-for-
        positive). Cross-link to MECH-094 hypothesis tag loss
        framing in the sleep_substrate_plan.md and PTSD
        literature; the carve-out introduces a NEW failure
        mode the strict pre-registration did not have.
      - Hallucinated-chunk pathology under high-imagination
        psychiatric conditions (mania, psychosis): if
        sleep-mode gate fails or value-tag threshold drifts
        downward, agent could chunk imagined-as-positive
        sequences. Audit flag enables detection.
  location: docs/architecture/policy_primitive_granularity.md

- id: MECH-323
  implementation_note: >-
    SUBSTRATE BUILT 2026-07-22 as ChunkAccumulator in ree-v3/ree_core/policy/policy_chunking.py,
    default OFF (REEConfig.use_policy_chunking). Joint formation gate implemented exactly as
    registered: repetitions >= R_min over window W AND outcome variance < F_low AND outcome mean >
    running baseline + evaluative margin. Chunked-primitive object carries all seven registered
    fields (sequence, initiation_set, termination_condition, value_tag, replay_origin,
    formation_timestamp, depth); the Sutton 1999 options structure is adopted as STRUCTURE ONLY --
    discovery stays Graybiel repetition-and-consistency and the chunk stays value-flat.
    Chunks-of-chunks recursion works and is capped at max_depth (verified: a repeating 4-element
    sequence yields a depth 1/2/3 ladder). Both write paths present: MECH-094-strict default plus
    the MECH-322 carve-out. Bounded by construction (contiguous sub-sequences of length 2-5 only,
    FIFO-capped tally table) -- the naive enumeration is combinatorial in episode length.
    NO EXPERIMENTAL EVIDENCE YET; v3_pending stays True.
    CHUNK-SIZE BUDGET CONVERTED FROM CONSTANT TO GROWABLE CEILING 2026-07-27, discharging the
    follow-on the 2026-07-27 provenance correction opened (Ramkumar 2016; Bo, Borza & Seidler
    2009). REEConfig.use_growable_chunk_ceiling, default OFF and bit-identical when OFF
    (ChunkAccumulator.effective_max_chunk_size returns config.max_chunk_size and no growth
    state is consulted). ON: the effective ceiling starts at max_chunk_size and grows one
    element at a time toward derived_chunk_ceiling =
    floor(chunk_deliberation_horizon * chunk_ceiling_budget_fraction), clamped below by
    max_chunk_size and above by chunk_ceiling_hard_max (12, a tractability backstop, not a
    claim about chunk size).
    THE PARAMETER'S SHAPE CHANGED, THE NUMBER DID NOT. Ramkumar 2016 proposes no upper bound
    and so licenses no replacement constant; none is asserted. chunk_ceiling_budget_fraction
    defaults to 0.1667 = 5/30, ANCHORED so that at REE's actual rollout horizon (30 --
    HippocampalConfig.horizon as set by from_dims, NOT the dataclass default of 10 that no
    built agent uses) the derivation returns exactly 5, the inherited Sakai budget unchanged.
    Only an agent with a LARGER deliberation budget derives a larger ceiling, which is
    consequence (ii) of the lit entry (two rollout budgets -> two chunk sizes) made
    substrate-real. Pinned by contract C11 test_c11_derivation_reproduces_the_inherited_budget
    _at_reeds_real_horizon, which builds a real REEAgent rather than reading a default.
    THE DIMINISHING-RETURNS BRAKE IS THE GROWTH RULE, not a second cap. Each growth step must
    be licensed by a realised marginal outcome gain: the sequence at the current ceiling must
    beat the better of the two sub-sequences one element shorter by >=
    chunk_ceiling_returns_threshold. So growth stops when merging stops paying, at whatever
    size that happens to be, and the Ramkumar plateau (~50-60% of single-unit complexity;
    the macaques never collapse the sequence into one chunk) is an OUTCOME of the rule rather
    than a tuned number. A monotonic practice-driven accumulator would eventually collapse the
    whole sequence whatever number it stopped at; contract C11 test_c11_brake_plateaus_below
    _the_derived_maximum pins that flat returns leave the ceiling at 5 with headroom to 10
    available. VALUE STATUS: chunk_ceiling_returns_threshold = 0.10 is an UNCALIBRATED
    ENGINEERING DEFAULT, exactly the status of F_low / F_high -- the RELATION (growth requires
    positive marginal return) is literature-grounded, the threshold on this 0-1 outcome scale
    is not.
    DECOUPLED FROM R_min BY CONSTRUCTION (the Bo et al. dissociation). Capacity-to-chunk-LENGTH
    held in both age groups but capacity-to-learning-RATE only in the young, so chunk size and
    formation rate are separable and must not be calibrated as one quantity. Growth reads
    realised outcome gain and the deliberation budget, NEVER the repetition tally;
    min_repetitions enters the returns test only as a judge-ability filter. AUDITED 2026-07-27:
    no existing or planned calibration sweep couples them (V3-EXQ-810 is the only experiment
    touching chunk config and sets only chunk_min_repetitions + chunk_window_trials; no queue
    or substrate_queue entry sweeps size against R_min), and they are already independent knobs
    -- so nothing needed decoupling, but the OBVIOUS implementation of a growable ceiling
    (grow it as repetitions accumulate) would have INTRODUCED the coupling. Pinned by contract
    C11 test_c11_growth_is_decoupled_from_the_repetition_tally.
    TRACTABILITY PRESERVED, and the pre-existing note above is imprecise: note_outcome tallies
    only the SUFFIX of each permitted length (actions[-size:]), so its per-outcome cost is
    O(ceiling), LINEAR, not combinatorial. The combinatorial enumeration the 2-5 bound was
    justified against is the naive all-sub-sequences-at-all-positions one, which this module
    has never performed. max_tracked_sequences (FIFO) remains the hard memory bound, unchanged.
    NOT IMPLEMENTED -- SHRINKAGE. Bo et al. show chunk length DECLINES as capacity declines with
    age. The cross-agent half is covered (a smaller-budget agent derives a smaller bound); the
    within-lifetime half is not, because nothing lowers a ceiling once raised. Closing it needs
    a declining-capacity signal REE does not currently have, so it is recorded as a gap rather
    than guessed at. 16 further interface contracts (C11 block) in
    ree-v3/tests/contracts/test_arc071_policy_chunking.py (60 total); registered PROBED in
    ree-v3/tests/test_flag_inertness.py. Full suite green on ree-worker-2 (2453 passed; the one
    failure, tests/preflight/test_queue_integrity.py::test_supersedes_targets_well_formed, is
    PRE-EXISTING at HEAD and concerns V3-EXQ-826a's supersedes field, unrelated to this change).
    STILL NO EXPERIMENTAL EVIDENCE -- this is substrate, not evidence; v3_pending stays True.
    CROSS-REFERENCE, MECH-324 RETENTION (2026-07-27, ree-v3 6c3e67e): the sentence above that the
    joint formation gate is implemented "exactly as registered" is now conditional. Under
    REEConfig.use_chunk_dissolution_retention (default OFF, so unconditional at defaults) a
    PREVIOUSLY-DISSOLVED sequence re-enters this SAME gate with only the repetition term
    substituted -- ceil(R_min * reacquisition_repetition_factor) counted since dissolution, against
    a dedicated counter rather than the sliding tally -- while the variance < F_low and mean >
    baseline + margin terms are applied UNCHANGED. So MECH-323's formation trigger becomes
    state-dependent on the MECH-324 library, and a reader reasoning about repetitions-to-formation
    must check whether the sequence has a dormant chunk. See the MECH-324 implementation_note.
    [2026-08-01 governance, V3-EXQ-829a, evidence_direction=supports -- full detail on MECH-324's
    own implementation_note, not duplicated here]: the reacquisition-window-isolation fix
    (use_reacquisition_window_isolation) validated cleanly. claim_ids=[MECH-323, MECH-324]. No
    status change from this run alone.
  title: "policy_composition_chunk_accumulator -- striatum/DLS-analog formation operator that builds chunk candidates from sequences of policy primitives repeatedly executed together with consistent outcomes. First child mechanism for ARC-071 (the maintenance operator MECH-324 is registered separately). Encodes the lit-pull R1 verdict (repetition + outcome consistency as primary trigger, evaluative-gate upstream, V_s-positive secondary) + R2 verdict partial (striatum-analog formation substrate; vmPFC/IL maintenance substrate handled by MECH-324) + R4 verdict (chunked primitive carries initiation-set + termination-condition fields per Sutton 1999 options structure) + R5 verdict (gradual hysteresis with F_low formation < F_high dissolution -- the RELATION is literature-grounded as of 2026-07-27 (Barnes 2005; Bouton 2012), the VALUES F_low=0.15 / F_high=0.45 are uncalibrated engineering defaults with NO literature support, awaiting child-MECH calibration; INITIAL chunk-size budget 2-5 elements per level, licensed by Sakai 2003 at MOTOR grain only and NOT a stable architectural constant -- Ramkumar 2016 shows chunk length grows with practice under a compute/efficiency trade-off, Bo & Seidler 2009 shows it tracks visuospatial working-memory capacity and declines with age, so the ceiling should be exposed as growable and ideally DERIVED from the agent's deliberation/rollout budget rather than configured independently; see the 2026-07-27 provenance correction in evidence_quality_note). Dissolution is SUPPRESSION WITH RETENTION, not erasure (2026-07-27 structural correction, registered on MECH-324): a previously-dissolved sequence re-forms at the REDUCED bar R_reacq = R_min * f_reacq rather than at R_min from scratch, so the MECH-323 trigger is state-dependent on the chunk library; and the initiation_set field becomes load-bearing for the renewal falsifier, which is expressible only if dissolution is scoped to the same context granularity formation uses. Two write paths: default MECH-094 strict (hypothesis_tag=False real executions only); sleep-replay carve-out under MECH-322 conditions (replay_origin=True audit flag with accelerated dissolution if not corroborated by real waking execution)."
  claim_type: mechanism_hypothesis
  subject: policy.composition.chunk_accumulator_formation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  registered_utc: "2026-05-11"
  depends_on:
    - ARC-071     # parent architectural commitment (composition direction)
    - MECH-094    # default strict gate on the formation write path
    - MECH-322    # sleep-replay carve-out path
    - SD-014      # valence vector recording (value-tag substrate)
    - SD-039      # anchor goal-payload (value-tag transport)
    - MECH-269    # V_s primitive (V_s-positive secondary condition)
  functional_restatement: >
    MECH-323 is the formation operator in the ARC-071 chunking
    cluster -- the striatum/DLS-analog accumulator that builds
    chunk candidates from sequences of policy primitives
    repeatedly executed together with consistent outcomes.
    MECH-323 produces chunks; MECH-324 (registered separately)
    will supply the IL/vmPFC-analog maintenance operator that
    Smith & Graybiel 2013's optogenetic disruption shows is
    causally required for chunk crystallisation.

    Substrate role: MECH-323 runs in the striatum-analog write
    path that MECH-163 names as the habit-system substrate
    (dorsolateral-striatum equivalent in REE). Inputs are
    sequences of executed policy primitives plus their realised
    outcomes; outputs are chunk candidates that enter the
    proposal pool (ARC-007 strict value-flat) for downstream
    selection. The accumulator is asymmetric with ARC-070's
    decomposition operator (MECH-321): MECH-323 is slow,
    execution-side; MECH-321 is fast, simulation-side.

    Trigger conditions (joint AND -- all three must hold for
    chunk formation):

    (1) REPETITION COUNT. The candidate sub-sequence must have
        been executed together at least R_min times within a
        sliding window of W trials. Suggested defaults
        (calibrated at child-MECH validation):
          R_min = 20 repetitions (child-MECH calibration range
            10-50; Sakai 2003 chunk-emergence onset is around
            5-20 reps for finger sequences; REE primitives are
            coarser-grain so larger R_min is plausible).
          W = 100 trials (sliding window; child-MECH calibration
            range 50-200; Graybiel 2008 reports rodent
            chunking emerges over hundreds of trials).

    (2) OUTCOME-VARIANCE THRESHOLD (formation hysteresis low).
        The outcome distribution over the R_min repetitions
        must have POPULATION variance below F_low. Suggested
        default F_low = 0.15 (child-MECH calibration range
        0.10-0.25). THE VALUE IS AN UNCALIBRATED ENGINEERING
        DEFAULT with no literature support -- only the
        F_low < F_high RELATION is literature-grounded
        (2026-07-27; see evidence_quality_note). The outcome
        dimension is the realised E3-score-receipt over the
        sub-sequence (SD-014 valence + accumulated
        benefit/harm). Variance is computed across the R_min
        executions, not across steps within an execution.

        OUTCOME SCALE (2026-07-27 correction -- this paragraph
        replaces the words "on a 0-1 normalised scale", which
        were in the original registration and were WRONG; see
        the SCALE CORRECTION block in evidence_quality_note for
        why, and why the alternative of re-calibrating the
        thresholds instead was rejected). The outcome signal is
        a RAW, UNBOUNDED, CALLER-SUPPLIED scalar in
        E3-score-receipt units. The substrate applies no
        normalisation of any kind: `policy_chunking._variance`
        is plain population variance over the values handed in,
        and `REEAgent.note_chunk_outcome` passes the harness
        scalar straight through. F_low and F_high are therefore
        SCALE-RELATIVE, and any experiment touching either gate
        MUST pre-register the outcome span it feeds in.

        REACHABILITY CONDITION (the fact this correction exists
        to stop future authors re-deriving). Population variance
        of values confined to a span S is at most S^2/4, attained
        only by the degenerate half-at-each-endpoint
        distribution. So:
          - A DISSOLUTION gate F_high is reachable only if
            S > 2*sqrt(F_high). At the registered F_high = 0.45
            that is S > 1.342. On a 0-1 span the ceiling is 0.25
            and dissolution is arithmetically IMPOSSIBLE -- which
            is exactly the defect this correction repairs.
          - Because real outcome distributions sit far below the
            degenerate ceiling, a workable harness wants headroom,
            not a bare crossing. S >= 2.0 (ceiling 1.0, ~2.2x
            F_high) is the pre-registered choice in V3-EXQ-829 and
            is the recommended floor.
          - F_low has NO analogous obstruction in the reachable
            direction: it is a below-threshold gate and variance
            can be 0 at any span. The bite runs the other way --
            a WIDER span makes F_low harder to satisfy, so span
            is a live experimental knob on BOTH gates and must
            never be left implicit.

    (3) EVALUATIVE GATE. The accumulated outcome mean must be
        positive (sub-sequence's outcomes are evaluated-as-good
        in aggregate; Graybiel 2008 evaluative-circuit upstream
        gating). Threshold can be the agent's running
        E3-score-receipt baseline plus a small margin
        (suggested default: mean > baseline + 0.05). This
        prevents chunk formation over consistently-bad
        sub-sequences (chunks of failure are not what biology
        forms; evaluation gates upstream of chunking).

    Secondary precondition (NOT a hard gate, but a preferential
    bias):

    (S1) V_s-POSITIVE PREFERENCE. Sub-sequences over regions of
         high MECH-269 V_s are preferred candidates for chunk
         formation (the V_s-positive inverse of ARC-070's
         V_s-negative decomposition trigger). This is encoded
         as a candidate-ranking bias when multiple sub-sequences
         meet the joint trigger, not as a hard gate.

    Chunk representation. When the trigger fires, MECH-323
    creates a chunked primitive object with the following
    fields:

      sequence: list[policy_primitive]   -- the constituent sub-sequence
      initiation_set: dict[context_field, value_predicate]
                                          -- contexts where this chunk may fire
                                          -- (Sutton 1999 options requirement;
                                          --  inferred from the contexts in which
                                          --  the accumulator's R_min executions
                                          --  occurred)
      termination_condition: callable     -- when the chunk completes
                                          -- (Sutton 1999 options requirement;
                                          --  default: sequence-end + success-
                                          --  signal from realised outcome)
      value_tag: float                    -- mean realised E3-score-receipt
                                          --  over the R_min executions
                                          -- (SD-014 / SD-039 derived; consumed
                                          --  by MECH-322 sleep-replay path)
      replay_origin: bool                 -- False by default (real-execution
                                          --  formation); True if formed via
                                          --  MECH-322 sleep-replay carve-out
      formation_timestamp: int            -- step / episode when chunk first
                                          --  formed (for dissolution-deadline
                                          --  accounting in MECH-324 and
                                          --  MECH-322 corroboration windows)
      depth: int                          -- recursion level (1 for chunks of
                                          --  raw primitives; 2-3 for chunks
                                          --  of chunks per R4 verdict)

    Write paths.

    DEFAULT (MECH-094-strict path): the trigger fires from
    real-executed sequences only (hypothesis_tag=False on each
    constituent execution). The formed chunk has
    replay_origin=False. This is the path Graybiel 1998 / 2008
    / Yin & Knowlton 2006 substrate-anchor cluster directly
    supports.

    SLEEP-REPLAY (MECH-322 carve-out path): the trigger fires
    from replayed (hypothesis_tag=True) sequences during
    designated SD-017 sleep phase IF (a) the replayed
    sub-sequence carries a value-tag from prior real-executed
    episodes meeting the high-positive threshold defined by
    MECH-322; (b) the replay is in sleep mode, not waking
    DMN. The formed chunk has replay_origin=True and inherits
    the MECH-322 dissolution deadline (N episodes
    corroboration window). MECH-292 / MECH-293 ghost-goal
    probes during waking DMN do NOT engage this path.

    Chunk-of-chunks recursion (R4 verdict). MECH-323 may form
    a chunk whose `sequence` field contains other chunked
    primitives (not just raw policy primitives). Recursion
    depth cap: 3 levels by default (per Botvinick 2009 /
    biological-rarity-of-deeper-hierarchy; child-MECH
    calibration may relax to 4 if validation supports).
    Replay-origin propagation: conservative default (ANY
    replay-origin sub-chunk taints the parent chunk to
    replay-origin); the open Q-claim from MECH-322 applies.

    Behavioural signature for validation. When MECH-323 forms
    a chunk over a structurally repeating sub-sequence,
    within-chunk inter-element timing should tighten relative
    to between-chunk transitions (Sakai 2003 signature); rollout
    deliberation cost on the chunked sub-sequence should drop
    (the chunk selects as one move at the planning layer, not N
    separate selections); behavioural latency should fall.
    These three signatures together form the falsifiable
    prediction the first validation experiment should test.

    Falsifiable architectural prediction (paired with the
    MECH-322 sleep-replay prediction and the MECH-324
    maintenance prediction once registered): an REE agent with
    MECH-323 active should produce chunked primitives over
    structurally repeating sub-sequences in episode 1000+ of
    a task that supports them (e.g. SD-054 reef + a repeating
    forage-loop). The chunked primitives should be detectable
    in the proposal pool via the replay_origin field and the
    depth field, even before behavioural latency drops.

    What MECH-323 PRESERVES:
      - ARC-007 strict value-flat hippocampal proposals. The
        chunked primitive enters the proposal pool as a
        value-flat candidate; downstream E3 evaluation supplies
        the value at selection time, as for any other proposal.
        The value_tag field is METADATA carried forward for
        MECH-322 corroboration accounting, NOT a value head on
        the proposal itself.
      - MECH-094 strict gating on the default write path. The
        sleep-replay carve-out is via MECH-322's conditions; no
        direct relaxation of MECH-094 by MECH-323.
      - SD-039 anchor payload semantics. The value_tag field is
        derived from SD-039 goal-payload + SD-014 valence vector
        without modifying either substrate.
      - MECH-269 V_s as primary substrate for the V_s-positive
        secondary preference. No new V_s consumer-side machinery
        introduced beyond the candidate-ranking bias.

    What MECH-323 INTRODUCES:
      - The chunk-accumulator substrate itself (sliding-window
        repetition counter + outcome-variance EWMA per
        sub-sequence candidate).
      - The chunked-primitive object with the seven fields
        above.
      - The R_min, W, F_low, evaluative-mean-threshold
        parameter set (suggested defaults; child-MECH
        validation refines).
      - The initiation_set inference rule (collect the
        contexts in which the accumulator's R_min executions
        occurred; predicate over discriminating context
        features).
      - Chunk-of-chunks recursion up to depth cap 3.
      - The depth and formation_timestamp fields on chunks.

    Open Q-claims for the child-MECH validation pass:
      - Initiation-set inference precision: how discriminative
        should the context predicates be? Too narrow and chunks
        fail to generalise; too broad and chunks mis-fire.
        Child-MECH design should pin a starting policy
        (suggested: contexts that account for >=80% of the
        variance across R_min execution contexts).

        [2026-07-27 SCOPING SPIKE, from MECH-324's
        CONTEXT-SCOPING OF DISSOLUTION question. Source:
        evidence/planning/mech324_context_scoping_dissolution_scoping_spike_2026-07-27.md.
        No status / confidence / v3_pending change. TWO
        corrections land HERE rather than on MECH-324.

        (1) THE INFERENCE RULE IS UNBUILT. It is listed above
        under MECH-323's REGISTERED CONTENT ("collect the
        contexts in which the accumulator's R_min executions
        occurred; predicate over discriminating context
        features"), but ChunkedPrimitive.initiation_set is a
        DECLARED-BUT-NEVER-POPULATED field -- defaults to
        frozenset(), both mint() call sites omit it, zero reads
        repo-wide outside the module, and
        policy_chunking.py:251-253 states plainly that "context
        bucketing is not yet wired". Any claim text asserting
        that MECH-323 formation is context-conditioned (incl.
        this claim's own title, and MECH-324's renewal falsifier
        (b)) is describing the REGISTRATION, not the substrate.
        This makes MECH-323 the upstream blocker for MECH-324
        falsifier (b) RENEWAL: renewal cannot be tested until
        this rule is built, and it must be built BEFORE the
        dissolution gate is scoped, not after.

        (2) THE PRECISION QUESTION ABOVE IS PREMATURE, AND ITS
        SUGGESTED DEFAULT MAY BE UNREACHABLE. It asks how
        discriminative the predicates should be -- but the prior
        question is whether the context representation can
        discriminate AT ALL. Measured on z_world (SD-008
        under-differentiation): ~0.98-cosine common-mode cone,
        and the nearest existing implementation of this exact
        primitive (candidate_rule_field._context_bucket)
        collapses to ONE bucket on real z_world. At one bucket
        the ">=80% of the variance across R_min execution
        contexts" policy is not merely too broad, it is
        undefined. SD-078 (cue_centering) is the solved fix
        (1 bucket -> 20) but is candidate_substrate_landed and
        DEFAULT OFF. So pin the discriminativeness policy only
        AFTER measuring the achievable bucket count on the
        chunking stream itself -- see the classification and
        probe in the MECH-324 block.]
      - Recursion depth cap: 3 by default; child-MECH
        validation may relax to 4 if biology supports.
      - V_s-positive preference weighting: how strong should
        the candidate-ranking bias be? Soft preference vs.
        near-hard filter changes which chunks form first.
      - Sliding window W vs cumulative count: child-MECH
        design should pin sliding-vs-cumulative semantics
        (sliding window is biology-faithful per Smith &
        Graybiel 2013 chunk-dissolution dynamics).
  evidence: []
  evidence_quality_note: >
    Registered 2026-05-11 as the first ARC-071 child mechanism
    (formation operator). Lit grounding inherited from the
    targeted_review_arc_071_composition synthesis (2026-05-10,
    aggregate lit_conf 0.848): R1 trigger primary (Graybiel
    1998 + 2008 canonical pattern), R2 substrate partial
    (Yin & Knowlton 2006 DLS as formation locus; vmPFC/IL
    maintenance handled by MECH-324), R4 chunk-representation
    fields (Sutton 1999 options structure), R5 hysteresis +
    chunk-size budget (Sakai 2003 + Smith & Graybiel 2013)
    -- the last of these is REVISED by the 2026-07-27
    provenance correction immediately below.

    [2026-07-27 PROVENANCE CORRECTION -- chunk-size budget and
    hysteresis values. Source: targeted_review_connectome_mech_323
    lit-pull, landed REE_assembly 36901c01ed. Text/provenance
    only; no status, confidence, live_status or v3_pending change.

    (A) CHUNK-SIZE BUDGET 2-5 IS AN INITIAL BUDGET, NOT A STABLE
    CONSTANT. It arrived via the ARC-071 R5 verdict resting on
    Sakai 2003, which licenses 2-5 at MOTOR grain only -- a gap
    the existing Sakai entry already flags. Two new entries show
    the quantity is not the kind of thing that stays put:

      - Ramkumar et al. 2016 (Nat Commun, 10.1038/ncomms12176).
        Chunking is the output of an efficiency/computation
        trade-off, so the prediction is a SCHEDULE, not a
        constant: start with many short chunks because compute
        binds, then merge them as practice lowers the cost of
        more complex computation. Two macaques learning a
        ten-element reaching sequence over months do exactly
        this -- "the length of individual chunks increases". No
        upper bound is proposed, so the paper cannot be used to
        replace 5 with any other number; what brakes growth
        empirically is diminishing returns (chunk structures stay
        at ~50-60% of single-unit complexity; the monkeys never
        collapse the sequence into one chunk), so a monotonic
        accumulator with no returns-based brake is also wrong.
        Marked `weakens` at confidence 0.68, scoped narrowly to
        the fixed-constant reading rather than to small-chunks-
        early. Mapping fidelity held at 0.60 -- macaque reaching
        kinematics, so the grain is motor and the specific
        lengths do not port; its force here is STRUCTURAL (the
        parameter's shape), and shape travels further than
        magnitude. Two consequences a fixed 2-5 cannot express:
        a hard lifetime cap at 5 forbids the most robust
        longitudinal signature in this literature, and two agents
        with different rollout budgets should settle at different
        chunk sizes -- so a single global maximum is the wrong
        SHAPE of parameter, not merely the wrong value.

      - Bo, Borza & Seidler 2009 (J Neurophysiol,
        10.1152/jn.00393.2009). Chunk length tracks visuospatial
        working-memory capacity; older adults show the same
        capacity-to-length relationship with both quantities
        jointly reduced. This converts the budget from a
        primitive into a DERIVED quantity and explains why the
        range lands near classic short-term capacity rather than
        anywhere else. Marked `mixed` at confidence 0.62 --
        supporting the magnitude by explaining its origin,
        weakening its status as an architectural constant.
        Caveats: correlational (capacity and length covary
        without capacity being shown to SET length; processing
        speed not excluded as a third factor), and the task is
        EXPLICIT motor sequence learning, which recruits
        declarative/WM resources that this DLS-analog procedural
        accumulator is specifically not supposed to depend on --
        so there is a live possibility the constraint belongs to
        a different system and importing it imports the wrong
        system's limit.

    Taken together: the budget should be neither fixed across
    agents nor fixed within an agent's lifetime. Carry 2-5 as the
    INITIAL budget with a declared calibration range, expose the
    ceiling as growable, and derive it where possible from the
    agent's deliberation/rollout budget rather than configuring it
    independently. Solway et al. 2014 (filed under MECH-321) is
    the policy-grain null that would have supplied a principled
    subtask-size budget if one existed, and is silent on it.

    Incidental find worth carrying: MECH-321's depth cap and this
    size budget are currently coupled by fiat (implementation_note
    says the cap "mirrors ARC-071's chunk_max_depth so the inverse
    operations stay symmetric"). Ramkumar supplies a principled
    reason they should be related -- both are settings on ONE
    compute-versus-efficiency trade-off, decomposition buying
    tractability and composition buying efficiency -- which is a
    better warrant than symmetry-for-its-own-sake and predicts
    they should move together as the compute budget changes.
    That is testable.

    Also separable, from Bo et al.: the capacity-to-chunk-LENGTH
    correlation held in both age groups while the
    capacity-to-learning-RATE correlation held only in the young.
    So R_min (repetition count, default 20) and the size budget
    should NOT be calibrated as though one underlying quantity
    governed both.

    (B) F_low = 0.15 IS AN UNCALIBRATED ENGINEERING DEFAULT. The
    hysteresis RELATION F_low < F_high is now reasonably grounded
    -- Barnes et al. 2005 (Nature, 10.1038/nature04053) and Bouton
    et al. 2012 (Behav Processes, 10.1016/j.beproc.2012.03.004);
    see MECH-324's evidence_quality_note for the full reading. But
    neither source quantifies a variance threshold, so the VALUE
    0.15, and the ratio 0.45/0.15, gain no literature support
    whatsoever. Recorded here so the number does not keep reading
    as grounded by association with the relation. Calibration is
    child-MECH work and is not done by this correction.]

    [2026-07-27 STRUCTURAL CORRECTION -- formation side of
    dissolution-with-retention. Paired with the MECH-324 change,
    which carries the full reasoning and the three relapse
    falsifiers; only the MECH-323-side consequences are recorded
    here. Registration change only -- no status, confidence,
    live_status or v3_pending change, and no new lit pull.

    Barnes 2005 and Bouton 2012 establish that dissolution is
    SUPPRESSION WITH RETENTION rather than erasure. MECH-324
    owns the dissolution operator, but two consequences land on
    the FORMATION side and so belong here:

    (1) R_min IS NO LONGER THE ONLY REPETITION BAR. A sequence
    whose chunk was previously dissolved re-forms at the reduced
    bar R_reacq = R_min * f_reacq, f_reacq in (0, 1] (suggested
    default 0.25, UNCALIBRATED on exactly the footing of F_low
    and F_high above -- both sources say reacquisition is much
    faster, neither says how much). The MECH-323 trigger is
    therefore state-dependent on the library: it must ask
    whether an equivalent-sequence dormant chunk exists before
    deciding which bar applies. This is the rapid-reacquisition
    falsifier, the cheapest of the three, and it is a test of
    the FORMATION trigger even though the retention is
    MECH-324's.

    (2) initiation_set BECOMES LOAD-BEARING FOR RENEWAL. MECH-323
    already conditions formation on an inferred initiation_set.
    The renewal falsifier -- a chunk dissolved in one context
    still firing in another -- is expressible ONLY IF
    dissolution is scoped to the same context granularity that
    formation uses. If formation is context-conditioned while
    dissolution is context-blind, REE cannot show renewal at
    all, whatever the thresholds are. That asymmetry is now an
    open Q-claim on MECH-324; it is flagged here because the
    field it depends on is MECH-323's.

    Transfer limits are the same as MECH-324's and bite here
    too: the sources are rodent T-maze procedural learning and
    single-response operant contingencies, where the
    extinguished object is far simpler than the multi-element
    composed primitive MECH-323 forms. Whether a SEQUENCE
    extinguishes with the same asymmetry is extrapolation, not
    evidence.]

    Maintenance-side substrate (Smith & Graybiel 2013 IL
    optogenetic causal evidence) is deferred to MECH-324
    (separate registration). Without MECH-324, MECH-323-formed
    chunks will form but will not persist correctly; the two
    together fulfil ARC-071's architectural commitment.

    Not yet experimentally tested. First validation experiment:
    substrate-readiness diagnostic measuring whether the
    accumulator fires at all on a structurally repeating
    sub-sequence task with R_min=20, W=100, F_low=0.15
    defaults. Behavioural-latency / rollout-cost / within-chunk-
    timing measurements come in a follow-on validation
    experiment after the accumulator-only diagnostic passes.

    [SCALE CORRECTION, 2026-07-27. Trigger condition (2) as
    originally registered said outcome variance was measured
    "on a 0-1 normalised scale". That specification is WITHDRAWN;
    (2) now declares the raw caller-supplied E3-score-receipt
    scale explicitly and states the reachability condition.
    F_low = 0.15 and F_high = 0.45 are UNCHANGED.

    THE DEFECT. Population variance of values confined to a span
    S is at most S^2/4, so on the specified unit scale the
    ceiling is 0.25 -- BELOW MECH-324's F_high = 0.45. On the
    claim's own specified scale the CRYSTALLISED -> DISSOLVING
    transition was arithmetically unreachable, hence DISSOLVED
    was unreachable, hence ALL THREE of MECH-324's registered
    relapse falsifiers -- (a) rapid reacquisition, (b) renewal,
    (c) resurgence -- had no entry point at the pre-registered
    defaults, since each requires a dissolved chunk. This is a
    DIFFERENT defect from the already-registered "F_high is an
    uncalibrated engineering default" caveat: the problem was
    not that the value lacked support, it was that the value and
    the specified scale were mutually inconsistent.

    WHY (b) DECLARE-THE-SCALE AND NOT (a) RE-CALIBRATE. Three
    reasons, in order of weight.

    (i) The unit scale was never instantiated, so there is
    nothing to preserve. Verified in substrate:
    `ree-v3/ree_core/policy/policy_chunking.py` `_variance` is a
    two-pass population variance over the values as supplied,
    and nothing in that module clamps, rescales or normalises
    `outcome_signal`; `REEAgent.note_chunk_outcome`
    (ree_core/agent.py) forwards the harness scalar unaltered.
    The "0-1 normalised" wording described a scale the substrate
    has never had, so (b) corrects a documentation error rather
    than changing a commitment.

    (ii) Making the unit scale REAL would damage the quantity the
    gate measures. Since the E3-score-receipt (SD-014 valence
    plus accumulated benefit/harm) is not naturally bounded, no
    fixed a-priori 0-1 range is derivable from it; the only
    normalisation actually available at runtime is
    window-relative. But rescaling outcomes per-window destroys
    exactly the signal F_low/F_high read: a chunk with tightly
    consistent outcomes and one with wildly inconsistent ones
    both rescale to the same spread, so the variance gate stops
    discriminating. A fixed declared range instead is just (b)
    with a rescale bolted on top.

    (iii) (a) would compress the R5 hysteresis for no gain in
    grounding. Both thresholds would have to fit under 0.25,
    shrinking the F_low..F_high gap from 0.30 to at most 0.25 in
    absolute terms while re-guessing two numbers that are
    admittedly uncalibrated either way. Nothing in the
    literature adjudicates the new guesses any better than the
    old ones -- Barnes 2005 and Bouton 2012 ground only the
    F_low < F_high RELATION, neither quantifies a threshold --
    so the compression would buy no support and would additionally
    lock the numbers to a normalisation that (ii) rules out.

    RESIDUAL COST, stated plainly. Under (b) the thresholds are
    not scale-free: the same F_high means different things at
    different outcome spans. That is a real cost and (a) would
    not have fixed it either -- it only relocates the
    arbitrariness into the choice of normalisation. The correct
    end-state is child-MECH calibration that reports each
    threshold TOGETHER WITH the outcome span it was calibrated
    at; a calibrated threshold quoted without its span is not
    interpretable and should not be accepted as calibration.

    PROVENANCE. Surfaced 2026-07-27 while authoring V3-EXQ-829
    (MECH-324 rapid-reacquisition falsifier). That run reached a
    dissolved chunk only by pre-registering a harness outcome
    span of 2.0 while leaving both thresholds at their claim
    defaults, and emitted the arithmetic as the precondition
    `dissolution_variance_reachable` plus
    `custom_information.unit_scale_dissolution_unreachable` in
    manifest
    v3_exq_829_mech324_rapid_reacquisition_falsifier_20260727T170539Z_v3.
    That was a per-experiment workaround; this correction is the
    fix to the registered defaults, so subsequent ARC-071
    dissolution experiments read the span requirement off the
    claim instead of re-deriving it. Governance had separately
    recorded the 829 result on MECH-324 (REE_assembly fbae929063)
    without touching the scale/threshold inconsistency.]

    [2026-07-29 governance, V3-EXQ-829 supports (MECH-323 only; see MECH-324 for the mixed
    verdict on the maintenance operator) + V3-EXQ-834 unknown/no-op; failure_autopsy_2026-07-28-sweep
    + failure_autopsy_V3-EXQ-822b-834_2026-07-29, both confirmed]: 829 supports MECH-323's core
    formation/retention structure directly -- C3 (retention-vs-erasure discriminator) held cleanly:
    with the dormant-state flag OFF all 12/12 dissolved cells stayed dead, ON they revived. 834's
    precondition_unmet (chunks_formed) was traced to a gate-strictness mismatch, not a substrate
    defect: 4/5 seeds formed chunks robustly (8-44 each), comfortably clearing this run's own
    seed_pass_fraction=0.6 tolerance -- V3-EXQ-810a already validated the underlying accumulator
    mechanism on the identical substrate build. No substrate action from 834 (gate-logic fix in
    the driver script only). Stays candidate; v3_pending unchanged.
  notes: >
    MECH-323 is the FORMATION half of ARC-071's two-operator
    machinery. MECH-324 (registered separately) is the
    MAINTENANCE half. The two-operator framing matches Smith &
    Graybiel 2013's "dual operator view" finding; both are
    causally required for chunk crystallisation in biology.

    Cross-link to MECH-163 dual_goal_directed_systems:
    MECH-323 is the formation machinery on the habit-system
    side of MECH-163. The planned-system trajectories are the
    INPUT to MECH-323; the habit-system's chunked primitives
    are the OUTPUT. MECH-323 implements the planned->habitual
    transition that the ARC-071 lit-pull R3 verdict named.

    Cross-link to ARC-070 / MECH-321 decomposition:
    MECH-323's formation operator and MECH-321's decomposition
    operator share representational substrate (the chunked
    primitive object) but operate at different stages /
    timescales (slow execution-side formation vs fast
    simulation-side decomposition). The chunk created by
    MECH-323 is decomposable by MECH-321 under prediction
    failure; the same chunked-primitive object lives in both
    paths.

    Cross-link to MECH-322 sleep-replay carve-out:
    MECH-323 has two write paths -- the default MECH-094-
    strict path and the MECH-322 sleep-replay path. The
    replay_origin field on every formed chunk identifies which
    path produced it; the dissolution-deadline accounting on
    replay-origin chunks lives in MECH-322 (corroboration
    window) and will live in MECH-324 (accelerated dissolution
    when uncorroborated).

    Pathology cross-references (speculative, pre-validation):
      - OCD-like over-deliberation signature: if MECH-323
        formation thresholds (R_min, F_low) drift too high,
        chunks fail to form and every routine action is
        re-planned at primitive grain. Graybiel 2008 framing
        suggests this is more about evaluative-gate failure
        than pure accumulator failure; child-MECH validation
        should design the discriminative test.
      - Autism / insistence-on-sameness (pre-registered
        ARC-071 pathology candidate): chunks form normally but
        cannot be re-decomposed under context change. This is
        MECH-321 (decomposition) pathology, not MECH-323
        (formation). MECH-323 is healthy in this hypothesis.
      - Motor-learning impairments: MECH-323 accumulator
        substrate-level deficits. Sequences never compress.
        Specifically targetable by lesion / disruption
        experiments on the striatum-analog substrate.

    Frontoparietal early-phase parsing (Wymbs 2012 R2 verdict
    partial-mapping): not registered as a separate child-MECH
    here because it bleeds into ARC-070 / MECH-321 territory
    (segmentation, not concatenation). Cross-link to MECH-321
    rather than separate ARC-071 child mechanism.

    2026-07-30 (governance-apply, confirmed failure_autopsy_V3-EXQ-810a_2026-07-30 +
    batch-687a-707c-840-748a-833-842-810b-673-614-798afail): V3-EXQ-810a supports/standard --
    chunk-accumulator readiness independently re-derived from raw per-seed data (worst-seed
    margins 2.4x-7.0x floors on all three preconditions; load-bearing C1 clears 7/8 seeds vs
    the pre-registered 0.75 bar, verified directly from per-seed chunk_acc_n_formed rows; C7
    credit-rule separation 5x, 2385.0 vs 478.625 credit events, not marginal). V3-EXQ-810b
    (seed-202 narrow-repertoire follow-up, same family) also supports/standard, clean PASS on
    both load-bearing criteria. Stays candidate/v3_pending.
  location: docs/architecture/policy_primitive_granularity.md

- id: MECH-324
  implementation_note: >-
    SUBSTRATE BUILT 2026-07-22 as ChunkLibrary in ree-v3/ree_core/policy/policy_chunking.py, behind
    its OWN sub-switch (REEConfig.use_chunk_maintenance, default False) so the registered ARM_1 vs
    ARM_2 dissociation is runnable: with maintenance off, chunks still FORM but never crystallise
    and never become selectable -- the substrate analog of Smith & Graybiel 2013 IL disruption.
    Pinned by contract test_c7_formation_only_arm_never_crystallises. All four lifecycle states
    implemented (FORMING -> CRYSTALLISED -> DISSOLVING -> DISSOLVED) with all three registered
    sub-mechanisms: (A) crystallisation counter incremented by REAL executions only, (B) asymmetric
    hysteresis dissolution gate -- variance_low < variance_high is ENFORCED by config validation,
    not merely documented, and DISSOLVING is recoverable if variance falls back inside the band,
    (C) the MECH-322 replay-origin accelerated-dissolution deadline, which retires direct to
    DISSOLVED and bypasses the slower DISSOLVING window. NO EXPERIMENTAL EVIDENCE YET; v3_pending
    stays True. MECH-324's registered signature (the latency / rollout-cost drop) requires the
    ARM_1-vs-ARM_2 experiment, which is downstream of the readiness diagnostic.
    DISSOLUTION CORRECTED FROM ERASURE TO SUPPRESSION-WITH-RETENTION 2026-07-27 (ree-v3 6c3e67e).
    AS FIRST BUILT, DISSOLVED WAS NOT MERELY TERMINAL BUT AN ABSORBING TOMBSTONE, AND THAT WAS
    WORSE THAN ERASURE. The chunk was retained in the library dict for the audit trail, and
    PolicyChunking.note_outcome() skips any sequence already present in the library, while
    note_real_execution() had no DISSOLVED branch -- so the retained tombstone permanently BLOCKED
    its own sequence from ever re-forming, at any number of repetitions and any outcome
    consistency. Erasure would at least have permitted re-formation at R_min. Measured on the
    contract fixture: after forcing a crystallised chunk to DISSOLVED, 200 further trials of the
    same perfectly consistent above-baseline regime left it DISSOLVED with zero re-formations.
    CORRECTED behind REEConfig.use_chunk_dissolution_retention (default False, bit-identical when
    OFF), grounded in the 2026-07-27 targeted lit-pull (REE_assembly 36901c01ed), which converges
    from two independent directions: Barnes, Kubota, Hu, Jin & Graybiel 2005 (Nature,
    10.1038/nature04053) -- sensorimotor striatal ensemble patterns are "successively formed,
    reversed and then re-emerged", and "regaining a habit can occur quickly, with even one or a few
    exposures"; and Bouton, Winterbauer & Todd 2012 (Behav Processes,
    10.1016/j.beproc.2012.03.004) on INSTRUMENTAL extinction -- the action level ARC-071 operates
    on -- which "weakens behavior without erasing the original learning", installing new
    context-dependent learning ALONGSIDE the old.
    WITH THE FLAG ON, DISSOLVED IS DORMANT rather than dead: retained, STILL UNSELECTABLE (retention
    buys a cheaper route back, not a free pass back into the proposal pool), and re-formable at
    ceil(R_min * reacquisition_repetition_factor) repetitions counted SINCE DISSOLUTION -- the
    rapid-reacquisition relapse effect. Repetitions are a dedicated post-dissolution counter, not
    the accumulator's tally length: that tally is a sliding window capped at window_trials and a
    long-lived chunk's bucket sits saturated at the cap, so a lowered bar read against it would
    clear on the first post-dissolution trial and measure nothing. THE OTHER TWO MECH-323 FORMATION
    GATES ARE UNCHANGED (variance < F_low AND mean > running baseline + evaluative margin), so only
    the repetition requirement is relaxed -- retention lowers the price of coming back, not the
    evidence required. Revival returns the chunk to FORMING, not to CRYSTALLISED, so C_min re-runs
    from zero: rapid reacquisition is a claim about the FORMATION threshold, while the
    crystallisation counter is the separate Smith & Graybiel 2013 IL sub-mechanism. MECH-322
    replay-origin chunks are NEVER revived (fails closed) -- a chunk retired for want of real waking
    corroboration must not return by a REDUCED bar, which would be a shortcut around the MECH-094
    posture the carve-out exists to preserve. Retention without use_chunk_maintenance RAISES rather
    than running silently inert (nothing dissolves with maintenance off, so the flag would otherwise
    read enabled in a manifest while never firing).
    VALUE STATUS: reacquisition_repetition_factor = 0.25 is an UNCALIBRATED ENGINEERING DEFAULT with
    NO numeric literature support, on exactly the same footing as F_high = 0.45. Both lit entries
    establish the DIRECTION (reacquisition is materially faster than acquisition; dissolution
    suppresses without erasing) and NEITHER quantifies the magnitude. Do not read 0.25 as a
    literature-derived value.
    NOT IMPLEMENTED -- the other two Bouton relapse effects are design questions, not builds:
    RENEWAL needs dissolution gated against the chunk's initiation_set (formation is
    context-conditioned, dissolution is context-BLIND, so a chunk dissolved in one context is
    dissolved everywhere and REE cannot exhibit renewal at all), and RESURGENCE needs dissolution
    state shared across competing library candidates, which per-sequence registration does not
    produce. 10 new C10 contracts (14 collected cases) in
    ree-v3/tests/contracts/test_arc071_policy_chunking.py; flag registered PROBED in
    ree-v3/tests/test_flag_inertness.py.
    FIRST EXPERIMENTAL EVIDENCE LANDED 2026-07-27T17:05:39Z, AND THE RAPID-REACQUISITION FALSIFIER
    FAILED. V3-EXQ-829 (script + queue entry ree-v3 77e3ddc; run
    v3_exq_829_mech324_rapid_reacquisition_falsifier_20260727T170539Z_v3, ree-cloud-2, 66 cells =
    6 seeds x 11 arms) returned outcome FAIL, evidence_direction MIXED for MECH-324 (SUPPORTS for
    MECH-323), non-degenerate, self-routing label retention_real_but_rapid_reacquisition_falsified.
    NOT YET AUTOPSIED and NOT marked reviewed -- adjudication is /failure-autopsy work and this
    note records the result only. v3_pending and status are LEFT UNCHANGED pending that
    adjudication and the governance cycle; do not read this paragraph as a disposition.
    WHAT HELD: C3, the retention-vs-erasure discriminator -- with the flag OFF all 12 of 12
    dissolved cells stayed dead, and with it ON they revived. So the structural correction above is
    REAL: DISSOLVED genuinely was an absorbing tombstone and no longer is. C5 (mild-dissolution
    robustness) also held.
    WHAT FAILED: C1, the LOAD-BEARING criterion -- reacquisition was not faster than acquisition
    but SLOWER, median r_reacq 90 against a measured median r_acq of 20 (= R_min exactly). And C2,
    the scaling criterion -- r_reacq was FLAT at 90 across every f_reacq in {1.0, 0.5, 0.25, 0.1}
    (forced bars 20 / 10 / 5 / 2), so Spearman rho was undefined. The bar the correction introduced
    had NO measurable effect on the DV it was built to move.
    THE LIKELY READING, offered as a hypothesis for the autopsy and NOT as a verdict: revival is
    rate-limited by the VARIANCE window flushing, not by the repetition bar. r_reacq / W = 0.908
    +/- 0.029 across the sweep, and the W axis (pre-registered as a verdict-aliasing control)
    separates the two readings cleanly -- median r_reacq is 90 at W = 100 and 28 at W = 30, i.e.
    the DV tracks the WINDOW, not R_min and not f_reacq. Reaching DISSOLVED requires T_dissolve = 50
    trials of supra-F_high variance, so at dissolution the variance window is necessarily
    contaminated with the very stream that caused it, and the variance < F_low gate cannot clear
    until that contamination ages out -- which takes ~0.9 W trials and always dominates a
    repetition term of at most R_min = 20. On this reading the repetition term is never the binding
    constraint, which would make f_reacq inert by construction rather than wrong in value, and the
    diagnosis structural rather than a calibration miss. The degeneracy check that would have
    masked this was run and passed (all_on_cells_sit_on_forced_bar = false), so the flatness is a
    real measurement, not an arithmetic identity.
    NOTE THE FALSIFIER IS AN OPERATOR-LEVEL DRIVER, NOT AN AGENT RUN, deliberately: the V3-EXQ-810
    readiness diagnostic measured the MECH-323 accumulator SILENT under agent control
    (chunk_accumulator_silent), so an agent-level reacquisition run would have measured that
    readiness gap rather than MECH-324. Evidence therefore attaches to the OPERATOR, not to
    chunk-driven behaviour. Falsifiers (b) renewal and (c) resurgence remain unbuildable per the
    NOT IMPLEMENTED paragraph above.
    REACQUISITION-WINDOW ISOLATION FIX IMPLEMENTED 2026-07-31 (IGW-20260731-196; ree-v3
    7747a01c94; design doc REE_assembly/docs/architecture/mech324_reacquisition_window_isolation.md).
    ROOT CAUSE CONFIRMED: the revival gate's variance/mean readout (the two UNCHANGED MECH-323
    gates the paragraph above describes) came from accumulator._tally[key], the sequence's
    WHOLE-LIFETIME sliding window -- and that bucket is, BY CONSTRUCTION, saturated with the
    contaminating high-variance stream at the exact moment dissolution completes (reaching
    DISSOLVED requires dissolve_trials of that stream on that same key). This is precisely the
    LIKELY READING two paragraphs above, now confirmed as the mechanism rather than a hypothesis.
    FIX: new sub-switch REEConfig.use_reacquisition_window_isolation (default False, nested under
    use_chunk_dissolution_retention, same loud-precondition convention). True routes the revival
    gate through ChunkedPrimitive.reacquisition_outcomes -- a dedicated per-chunk window
    populated ONLY from real executions since the most recent dissolution -- instead of the
    contaminated whole-lifetime tally. Backward compatible: bit-identical when False. Confirmed
    by 5 new contract tests reproducing the bug via REAL dissolution (not the existing suite's
    _dissolve() shortcut, which never touches the tally and so never exercised this path) and
    confirming the fix (reformed-after == the reduced bar instead of == window_trials).
    v3_pending and status are AGAIN LEFT UNCHANGED: this is a substrate fix responding to a
    diagnosed FAIL, not new evidence -- the rapid-reacquisition claim itself still needs a fresh
    experimental result under the corrected substrate before any disposition changes. Validation
    experiment V3-EXQ-829a queued (supersedes V3-EXQ-829), not yet run.

    [2026-08-01 governance, V3-EXQ-829a, evidence_direction=supports, label
    reacquisition_window_isolation_fix_confirmed]: the queued validation experiment ran and
    confirms the use_reacquisition_window_isolation fix works as designed -- clean PASS, no
    anomalies. This is the first experimental evidence for the rapid-reacquisition claim under
    the corrected substrate. claim_ids=[MECH-323, MECH-324]. No status change from this run alone
    (a single clean confirmation of a bug fix, not yet a dedicated falsifier of the claim's own
    predictions), but it clears the "not yet run" note above.
  title: "policy_composition_chunk_maintenance -- IL/vmPFC-analog maintenance operator that gates chunk crystallisation, persistence under continued corroboration, and dissolution under outcome inconsistency. Dissolution is SUPPRESSION WITH RETENTION, not erasure: a dissolved chunk persists DORMANT (unselectable but retained) and re-forms at a reduced repetition bar R_reacq = R_min * f_reacq, per the 2026-07-27 structural correction (Barnes 2005 striatal ensemble re-emergence; Bouton 2012 renewal/resurgence/rapid-reacquisition) -- f_reacq is an uncalibrated engineering default on the same footing as F_high. Yields three relapse falsifiers (rapid reacquisition, testable now; renewal and resurgence, each substrate-blocked on an open Q-claim). Second child mechanism for ARC-071 (paired with MECH-323 formation operator). Causally required for chunk crystallisation per Smith & Graybiel 2013 selective optogenetic disruption of infralimbic cortex during overtraining (which PREVENTS habit formation despite intact striatal accumulator activity). Encodes the lit-pull R2 verdict (IL/vmPFC-analog substrate component of the phase-dependent multi-substrate chunking machinery) + R5 verdict (asymmetric hysteresis with F_high dissolution threshold > F_low formation threshold and slower dissolution timescale than formation timescale -- the RELATION and the timescale asymmetry are literature-grounded as of 2026-07-27 (Barnes 2005; Bouton 2012), but the VALUE F_high=0.45, and the ratio 0.45/0.15, have NO literature support and remain uncalibrated engineering defaults awaiting child-MECH calibration; see the 2026-07-27 provenance correction in evidence_quality_note) + MECH-322 verdict (accelerated dissolution for replay-origin chunks not corroborated by real waking execution within N episodes)."
  claim_type: mechanism_hypothesis
  subject: policy.composition.chunk_maintenance_dissolution
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  pending_retest_after_substrate: true
  registered_utc: "2026-05-11"
  depends_on:
    - ARC-071     # parent architectural commitment (composition direction)
    - MECH-323    # the formation operator MECH-324 maintains
    - MECH-322    # sleep-replay carve-out (accelerated dissolution rule applies)
    - MECH-163    # dual-systems substrate; MECH-324 is the habit-maintenance side
    - INV-037     # vmPFC stored/active distinction (maintenance substrate hook)
    - INV-038     # vmPFC EVR pattern (maintenance substrate hook)
  functional_restatement: >
    MECH-324 is the maintenance operator in the ARC-071 chunking
    cluster -- the IL/vmPFC-analog mechanism that gates whether
    chunks formed by MECH-323 actually crystallise into the
    proposal pool and how long they persist. MECH-323 produces
    chunks; MECH-324 supplies the maintenance + dissolution
    machinery that biology shows is causally separate.

    The Smith & Graybiel 2013 optogenetic finding is the
    load-bearing causal evidence: selective disruption of
    infralimbic cortex during overtraining prevents habit
    formation even when the striatal accumulator's
    task-bracketing activity pattern develops normally. This
    means the IL-analog substrate is not just correlated with
    chunking but causally required for it. Pure-striatal
    MECH-323 without MECH-324 produces accumulator candidates
    that never solidify into usable chunks.

    Substrate role: MECH-324 operates on the chunks MECH-323
    produces. Inputs: a MECH-323-formed chunk (with all seven
    fields: sequence, initiation_set, termination_condition,
    value_tag, replay_origin, formation_timestamp, depth) plus
    the realised outcome stream from subsequent executions of
    that chunk's sequence. Outputs: a per-chunk
    crystallisation_state in {forming, crystallised, dissolving,
    dissolved} that gates whether the chunk is selectable from
    the proposal pool.

    Lifecycle states (state machine):

    (1) FORMING. The chunk has just been created by MECH-323's
        formation trigger. MECH-324 places it in the proposal
        pool with a low selection-weight prior. Each subsequent
        real execution that meets the initiation_set conditions
        and terminates successfully via termination_condition
        increments a crystallisation_counter. When the counter
        exceeds C_min (suggested default 5; child-MECH
        calibration range 3-10), state transitions to
        CRYSTALLISED.

    (2) CRYSTALLISED. The chunk is fully selectable from the
        proposal pool with full selection-weight. The
        crystallisation_counter is frozen. The chunk persists
        in this state until either:
          (a) outcome variance (computed over a sliding window
              of W_maint trials, suggested default 100;
              child-MECH calibration range 50-200) rises above
              F_high (suggested default 0.45; child-MECH
              calibration range 0.35-0.55) -- state transitions
              to DISSOLVING. THE VALUE IS AN UNCALIBRATED
              ENGINEERING DEFAULT with no literature support;
              only the F_high > F_low RELATION is
              literature-grounded (2026-07-27; see
              evidence_quality_note).

              SCALE / REACHABILITY (2026-07-27 correction). The
              variance here is PLAIN POPULATION VARIANCE over the
              raw, unbounded, caller-supplied E3-score-receipt
              outcome signal -- NOT a 0-1 normalised quantity.
              MECH-323 trigger condition (2) originally said
              "0-1 normalised scale"; that wording is withdrawn,
              because the maximum population variance on a span-S
              range is S^2/4, so on a unit scale the ceiling is
              0.25 and THIS TRANSITION WOULD BE ARITHMETICALLY
              UNREACHABLE at F_high = 0.45 -- taking DISSOLVED,
              and with it all three relapse falsifiers below,
              out of reach at the registered defaults. F_high is
              therefore reachable only when the harness outcome
              span satisfies S > 2*sqrt(F_high) = 1.342, and
              S >= 2.0 is the recommended floor for headroom.
              Any experiment exercising dissolution MUST
              pre-register its outcome span. Full reasoning,
              including why the thresholds were NOT re-calibrated
              instead, is in the MECH-323 evidence_quality_note
              SCALE CORRECTION block; or
          (b) MECH-322 replay-origin dissolution-deadline fires
              (replay_origin=True chunk has not been
              corroborated by real waking execution meeting
              initiation_set conditions within N waking
              episodes since formation_timestamp; suggested
              default N=75; child-MECH calibration range
              50-100) -- state transitions directly to
              DISSOLVED, bypassing DISSOLVING.

    (3) DISSOLVING. The chunk's selection-weight decays
        linearly over T_dissolve trials (suggested default 50;
        child-MECH calibration range 25-100). During this
        window, two outcomes are possible:
          (a) outcome variance drops back below F_high
              (recovery) -- state transitions back to
              CRYSTALLISED; or
          (b) T_dissolve elapses -- state transitions to
              DISSOLVED.
        The slow DISSOLVING timescale relative to the fast
        FORMING-to-CRYSTALLISED timescale encodes the lit-pull
        R5 verdict that dissolution is slower than formation
        (Smith & Graybiel 2013 chunk-dynamics finding).

    (4) DISSOLVED = DORMANT, NOT ERASED. The chunk is removed
        from the proposal pool (selection_weight 0, not
        selectable) but its representational object is RETAINED
        in a dormant register -- both as an audit trail (for
        diagnostic purposes, especially around replay-origin
        pathology detection) and as a live substrate for
        re-formation. Re-formation of an equivalent-sequence
        chunk does NOT go through the standard MECH-323 trigger
        from scratch: a dormant chunk is revived at a REDUCED
        repetition bar R_reacq = R_min * f_reacq, f_reacq in
        (0, 1]. Dissolution is therefore SUPPRESSION WITH
        RETENTION, not erasure.

        [2026-07-27 STRUCTURAL CORRECTION. As originally
        registered this sub-state said the opposite -- "it does
        not resurrect the dissolved chunk" -- which specifies
        the wrong operation. Barnes 2005 (striatal ensemble
        patterns reverse under extinction and RE-EMERGE on
        reacquisition rather than being rebuilt) and Bouton 2012
        (renewal, resurgence and rapid reacquisition all show
        extinction weakening behaviour without erasing the
        original learning, installing new context-dependent
        learning ALONGSIDE the old) converge independently from
        the neural and the behavioural side on retention. An
        implementation that deletes on crossing F_high makes
        REE's dissolution look cleaner than biology's and its
        re-formation dynamics wrong even if F_low and F_high are
        both correctly calibrated. The correct operator installs
        a competing, context-gated suppression alongside the
        chunk rather than replacing it.]

        f_reacq is an UNCALIBRATED ENGINEERING DEFAULT
        (suggested 0.25) on exactly the same footing as F_high:
        both sources establish that reacquisition is much
        faster and NEITHER quantifies how much faster. f_reacq
        = 1.0 is the null this parameter exists to test
        against, not a fallback.

        Replay-origin exception. A replay-origin chunk retired
        under the MECH-322 accelerated-dissolution deadline is
        NOT eligible for reduced-bar reacquisition -- it never
        earned real-execution corroboration, so reviving it
        cheaply would let uncorroborated replay content
        re-enter the pool by the back door. It is retained for
        audit only.

    Substrate hooks. MECH-324 reads from / writes to the
    vmPFC-analog substrate via two existing invariants:

    INV-037 (stored / active distinction). The chunk's
    crystallisation_state is a stored property of the chunk
    object; the chunk's current selection-availability is its
    active property at any given step. MECH-324 mediates the
    stored-to-active transition: only CRYSTALLISED chunks
    have active selection-availability; FORMING chunks have
    partial active availability proportional to
    crystallisation_counter/C_min; DISSOLVING chunks have
    partial active availability proportional to remaining
    T_dissolve.

    INV-038 (EVR pattern -- evaluation, value, retention).
    The MECH-324 maintenance decisions read the realised
    outcome stream and update the chunk's accumulated
    valence (SD-014 vector) as a running average of the
    chunk's post-formation executions. This running average
    is what feeds the F_high dissolution threshold:
    dissolution fires when the recent-window outcome variance
    exceeds F_high, not when single bad executions occur.

    Cross-link to MECH-163 dual-systems. MECH-324 is the
    habit-maintenance side of the dual-system architecture.
    The MECH-163 habit system depends on chunks being
    available (CRYSTALLISED) for selection; MECH-324 is what
    keeps them there. The MECH-163 planned system can still
    propose value-flat trajectories without MECH-324 (it does
    not require chunks); but the habit system without MECH-324
    fails to maintain its repertoire and degenerates back to
    the planned system's mode.

    Three sub-mechanisms within MECH-324 (component
    decomposition for clarity, not separate child-MECHs):

    A. CRYSTALLISATION COUNTER. Increments on each successful
       real execution of a FORMING chunk's sequence;
       transitions chunk to CRYSTALLISED when counter exceeds
       C_min. Real-execution-only: replay-origin or
       hypothesis_tag=True executions do NOT increment.

    B. OUTCOME-VARIANCE DISSOLUTION GATE. Sliding-window
       variance computation over W_maint trials of a
       CRYSTALLISED chunk's executions; triggers
       DISSOLVING-state transition when variance > F_high.
       The slow dissolution timescale (T_dissolve) is what
       distinguishes MECH-324 from MECH-323's faster
       formation. Recovery from DISSOLVING is possible if
       variance drops back below F_high during the dissolution
       window.

    C. REPLAY-ORIGIN ACCELERATED-DISSOLUTION GATE. For chunks
       with replay_origin=True (MECH-322-formed), MECH-324
       enforces the corroboration-deadline rule: if no real
       waking execution corroborates the chunk within N
       waking episodes since formation_timestamp, the chunk
       transitions directly to DISSOLVED (bypassing
       DISSOLVING). This is the self-correcting deadline
       MECH-322 introduced -- MECH-324 is where it is
       implemented.

    Falsifiable architectural prediction (paired with MECH-323
    formation prediction): an REE agent with MECH-323 active
    but MECH-324 disabled should produce chunks that form
    correctly (the accumulator works) but fail to crystallise
    into usable proposals (selection_weight stays near zero
    and the chunks are never selected from the proposal pool).
    Behavioural-latency drops on repeating sub-sequences
    therefore DO NOT occur in the MECH-323-only condition;
    rollout deliberation cost stays high. The MECH-324
    contribution is operationally measurable: the difference
    between the MECH-323-only condition and the
    MECH-323+MECH-324 condition is the behavioural-latency
    drop / rollout-cost reduction signature. This mirrors the
    Smith & Graybiel 2013 IL-optogenetic-disruption finding
    structurally.

    Replay-origin-specific prediction: an REE agent with
    MECH-323 + MECH-322 active and MECH-324 strict
    accelerated-dissolution should show overnight chunk-
    formation (MECH-322 path) followed by accelerated
    dissolution of unvalidated chunks within N=75 waking
    episodes. The audit trail (DISSOLVED chunks retained in
    register) should record the replay-origin-without-
    corroboration pattern as a diagnostic signature.

    RELAPSE FALSIFIERS (registered 2026-07-27 with the
    dissolution-with-retention correction; Barnes 2005 / Bouton
    2012). These three are the empirical content the retention
    reading buys -- an erasure implementation cannot produce any
    of them, so each is a discriminating test of the OPERATOR
    and not merely of the thresholds. Ordered by cost.

      (a) RAPID REACQUISITION -- the cheapest and the sharpest,
          and the only one testable against the operator as it
          now stands. Dissolve a crystallised chunk, then
          re-present the same consistent regime: re-formation
          must occur in FAR FEWER than R_min repetitions
          (predicted R_min * f_reacq). Single-parameter, one
          obvious experiment, no new machinery. The null it
          discriminates against is f_reacq = 1.0 (reacquisition
          no faster than acquisition), which is what erasure
          predicts. FALSIFIED IF re-formation of a dissolved
          chunk takes >= R_min repetitions, or if the
          reacquisition count does not scale with f_reacq.

      (b) RENEWAL -- a chunk dissolved in one context should
          still fire in another. This interacts DIRECTLY with
          the initiation_set field MECH-323 already carries and
          is a live structural constraint, not a free
          prediction: if dissolution is context-blind while
          formation is context-conditioned, REE CANNOT SHOW
          RENEWAL AT ALL, whatever the thresholds are. So
          testing (b) presupposes that dissolution is scoped to
          the initiation_set under which the outcome variance
          was actually observed, rather than applied to the
          chunk globally. Registering that requirement is the
          point of listing (b) here. FALSIFIED IF a chunk
          dissolved under context A fails to fire under context
          B where its outcome statistics never degraded.

      (c) RESURGENCE -- the awkward one. A chunk should be able
          to return because a COMPETITOR was extinguished, not
          because its own evidence improved. This requires
          dissolution state SHARED ACROSS CANDIDATES in the
          library -- a library-level competition term rather
          than the per-chunk variance gate MECH-324 currently
          specifies. Nothing in the present registration
          produces it, and this is recorded as a known
          STRUCTURAL GAP rather than a prediction the current
          operator makes. It is the natural home of a follow-on
          child-MECH. FALSIFIED IF, once library-level
          competition exists, extinguishing an alternative
          chunk produces no recovery of a previously dissolved
          one whose own outcome statistics are unchanged.

    TRANSFER LIMITS ON ALL THREE (carried from the source
    entries; load-bearing, not boilerplate). The evidence is
    rodent T-maze procedural learning (Barnes) and
    single-response operant contingencies -- a rat pressing a
    lever for food (Bouton). In both, the extinguished object is
    MUCH SIMPLER than a composed chunked primitive, and REE's
    outcome dimension is an E3-score-receipt over policy
    primitives rather than a food reward. Whether a
    MULTI-ELEMENT SEQUENCE extinguishes with the same asymmetry
    is EXTRAPOLATION, NOT EVIDENCE -- and a chunk is by
    construction multi-element, so this limit bites on every one
    of the three falsifiers rather than being a generic caveat.
    Neither source quantifies a variance threshold, so neither
    F_high nor f_reacq gains any numeric support from them; what
    they support is the inequality's DIRECTION and the
    retention-not-erasure CHARACTER of the operator.

    What MECH-324 PRESERVES:
      - ARC-007 strict value-flat hippocampal proposals. The
        crystallisation_state and selection_weight are
        properties of the proposal CANDIDATE machinery, not a
        value head on the proposal. Downstream E3 evaluation
        still supplies the value at selection time.
      - MECH-094 strict gating. MECH-324 does not modify the
        write path that MECH-094 governs; it operates on
        already-formed chunks.
      - MECH-322 carve-out semantics. MECH-324 is where the
        MECH-322 accelerated-dissolution rule is implemented;
        the conditions MECH-322 specified are inherited
        verbatim.
      - MECH-323 formation semantics. MECH-324 receives
        MECH-323 outputs unchanged; the chunk object's seven
        fields are read but not modified by MECH-324.

    What MECH-324 INTRODUCES:
      - The crystallisation_state field on every chunked
        primitive in {forming, crystallised, dissolving,
        dissolved}.
      - The crystallisation_counter (per-chunk, FORMING-state
        only).
      - The selection_weight (per-chunk, derived from state +
        counter / remaining-T_dissolve).
      - The sliding-window outcome-variance computation per
        CRYSTALLISED chunk (window W_maint, threshold F_high).
      - The dissolution-window timer (T_dissolve, recoverable).
      - The replay-origin corroboration counter (N waking
        episodes since formation_timestamp; resets on each
        corroborating real execution).
      - The DISSOLVED-chunk DORMANT register (audit trail AND
        re-formation substrate -- see the 2026-07-27 structural
        correction; a pure audit trail would be the erasure
        reading).
      - The reduced reacquisition bar R_reacq = R_min * f_reacq
        applied when a dormant chunk's sequence re-accumulates,
        and the per-chunk n_dissolutions / n_reacquisitions
        counters that make the relapse falsifiers measurable.

    Open Q-claims for the child-MECH validation pass:
      - C_min / W_maint / F_high / T_dissolve / N / f_reacq
        parameter calibration via parametric sweep on the first
        validation experiment. f_reacq joins this list with the
        2026-07-27 correction and is as uncalibrated as F_high.
      - CONTEXT-SCOPING OF DISSOLUTION (blocks falsifier (b),
        renewal). Is dissolution scoped to the initiation_set
        under which the variance was observed, or applied to
        the chunk globally? Formation is already
        context-conditioned via initiation_set, so the
        conservative-looking choice (global dissolution) is the
        one that makes renewal structurally impossible. Not a
        free parameter -- it decides whether a whole class of
        biological behaviour is expressible.

        [2026-07-27 SCOPING SPIKE -- this question is now OWNED
        and answered, and its PREMISE is corrected. Source:
        evidence/planning/mech324_context_scoping_dissolution_scoping_spike_2026-07-27.md.
        Recommendation only; NO status / confidence /
        live_status / v3_pending change.

        ANSWERED IN PRINCIPLE: yes, dissolution must be
        context-scoped, and "leave it global" is a substantive
        commitment against the biology exactly as registered.
        RE-GATED IN PRACTICE: the build does NOT route to
        /implement-substrate, because the premise this question
        rests on is FALSE OF THE SUBSTRATE. Formation is NOT
        context-conditioned either. ChunkedPrimitive.
        initiation_set is a DECLARED-BUT-NEVER-POPULATED field:
        it defaults to frozenset(), BOTH mint() call sites
        (MECH-322 replay :741 and MECH-323 formation :1145)
        omit it, and a repo-wide grep finds ZERO reads outside
        the module. policy_chunking.py:251-253 concedes it --
        "context bucketing is not yet wired". MECH-323 lists the
        initiation_set inference rule under REGISTERED CONTENT;
        it was registered, never built. So there is no
        asymmetry: BOTH halves are context-blind, and renewal is
        blocked one level UPSTREAM of where this Q-claim locates
        it. This stops being a choice between two implementable
        options and becomes a downstream consumer of an unbuilt
        MECH-323 item.

        THE TRAP, which is why "just scope it" must be refused
        rather than deferred. Layer 1: scoping dissolution to a
        universally-empty initiation_set is a SILENT NO-OP that
        reads as renewal-capable -- the inert-flag failure class
        (cf. use_iterative_inference at settle_iters=1;
        REEConfig.from_dims swallowing unknown kwargs). Layer 2,
        the load-bearing one: populating it from raw z_world
        does NOT fix it. candidate_rule_field.py:408 already
        implements the same primitive an initiation_set
        inference would need (_context_bucket, sign pattern over
        the leading 8 dims), and its in-source record at :255-274
        MEASURED the outcome on the V3-EXQ-669b Stage-0 nursery
        (2026-07-22, 155-160 contexts, seed 101): z_world
        pairwise cosine min 0.9767 with 0.0% of pairs < 0.8 --
        a ~0.98 common-mode cone under SD-008 z_world
        under-differentiation -- and the bucket COLLAPSES to
        n_context_buckets = 1. A renewal run on that substrate
        has NO CONTEXT B. It would return the registered
        FALSIFIED verdict BY CONSTRUCTION OF THE INSTRUMENT, and
        that verdict would land against Bouton 2012's biology
        rather than against REE's context representation --
        the same class the V3-EXQ-654b/654d autopsies misread as
        retire-churn. A renewal FALSIFIED verdict is
        UNINTERPRETABLE until the bucket count is known > 1.

        SCOPE OF THE CHANGE, for whoever does build it: this is
        a SEAM change, not a gate tweak. Every ARC-071 entry
        point is context-free by signature (record_step,
        note_outcome, note_real_execution, tick_maintenance
        keyed by sequence, REEAgent.note_chunk_outcome), and
        chunk lifecycle state is a SINGLE SCALAR per chunk.
        Context-scoping makes it a map from context bucket to
        lifecycle state and changes the get_state() shape that
        contracts and manifests read. Context IS in scope at the
        seam (record_step is called inside select_action); it is
        a wiring gap, not an availability gap -- which is
        precisely what makes the naive build look easy.

        ROUTE THROUGH + WHAT IT INHERITS: SD-078 (cue_centering,
        slow-EMA common-mode subtraction) is the solved fix in
        the same module -- measured 1 bucket raw -> 20 centered,
        1 rule -> 8. Two cautions: SD-078 is
        candidate_substrate_landed (BUILT, NOT VALIDATED) and
        its flag is DEFAULT OFF, and the two mitigations
        measured INEFFECTIVE against this symptom
        (mature_mint_block_threshold 0.8;
        crf_context_from_e2_world_forward) are deliberately left
        in place and must not be re-tuned as the fix. So
        context-scoped dissolution INHERITS SD-008 z_world
        under-differentiation as its root blocker -- the same
        root as the monostrategy representation ceiling -- and
        is not an independent design choice about this gate.

        CLASSIFICATION: puzzle (known rules), not complicated
        (buildable). The rules are known (SD-078's method and
        measurement protocol); ONE missing fact gates the build
        -- over the CHUNKING regime's own context stream, how
        many distinct centered-z_world buckets occur, and do a
        single chunk's real executions span more than one? Do
        not inherit the 20-bucket nursery number: chunk
        formation requires R_min repetitions of a STABLE
        sequence, which plausibly selects for context
        homogeneity independently of the common-mode problem.
        If the answer is 1, renewal is inexpressible whatever
        the gate does and the honest move is to say so on
        falsifier (b) rather than build a gate that cannot be
        exercised. If > 1, the build becomes complicated
        (buildable) and MECH-323's initiation-set inference rule
        is the thing to build FIRST, with its own registered
        precision Q-claim.

        SEQUENCING: falsifier (a) rapid reacquisition is
        UNAFFECTED and remains the correct next move -- it is
        context-free by construction (it re-presents the same
        regime rather than changing context). Its substrate
        landed 2026-07-27 (use_chunk_dissolution_retention,
        chunk_reacquisition_repetition_factor, ChunkLibrary.
        revive, _attempt_reacquisition) and it is confirmed NOT
        YET QUEUED -- zero chunking entries in the queue.

        TRANSFER LIMIT, bearing on THIS question and not only on
        the falsifiers: the argument that dissolution ought to
        be context-scoped rests on Bouton 2012's demonstration
        that instrumental extinction installs new
        CONTEXT-DEPENDENT learning alongside the old -- and that
        demonstration is at the SINGLE-RESPONSE level (a rat
        pressing a lever for food; Barnes 2005 is rodent T-maze).
        A chunk is by construction not a single response.
        Whether context-dependence of extinction SURVIVES
        COMPOSITION is EXTRAPOLATION, NOT EVIDENCE -- a composed
        sequence might extinguish context-INDEPENDENTLY because
        its elements were acquired across heterogeneous
        contexts. The bucket-count probe above is incidentally
        the same measurement that would start to tell us: a
        chunk whose R_min formation executions span several
        buckets is already a counter-example to the naive
        single-context reading.]
      - LIBRARY-LEVEL COMPETITION (blocks falsifier (c),
        resurgence). MECH-324's dissolution gate is per-chunk
        on its own outcome variance. Resurgence requires a
        chunk to recover when a COMPETITOR is extinguished,
        i.e. dissolution state shared across candidates. What
        is the minimal shared term -- normalised selection
        weights across the library, or an explicit
        response-competition register? Likely a follow-on
        child-MECH rather than a parameter of this one.
      - Recovery semantics from DISSOLVING: is recovery
        path-dependent (variance must drop monotonically) or
        threshold-only? Suggested default: threshold-only,
        with re-entry to DISSOLVING if variance climbs again.
      - Selection_weight curve shape: linear, sigmoidal, or
        step? Affects whether FORMING chunks are weakly-
        selectable (linear / sigmoidal) or strictly-not-
        selectable (step) until crystallised. Suggested
        default: linear ramp, with weak selectability during
        FORMING enabling the chunk to demonstrate its utility
        during crystallisation.
      - Interaction between MECH-324 dissolution and MECH-321
        decomposition: does a CRYSTALLISED chunk under
        ARC-070 decomposition pressure (V_s drop) transition
        to DISSOLVING, or only have its current execution
        decomposed while the chunk itself stays CRYSTALLISED?
        Conservative default: decomposition is single-
        execution-only (chunk stays CRYSTALLISED), but
        repeated decomposition incidents over a window may
        elevate F_high variance enough to trigger
        DISSOLVING via the standard gate.
  evidence: []
  evidence_quality_note: >
    Registered 2026-05-11 as the second ARC-071 child mechanism
    (maintenance operator). Lit grounding inherited from the
    targeted_review_arc_071_composition synthesis (2026-05-10,
    aggregate lit_conf 0.848): R2 multi-substrate verdict
    (Smith & Graybiel 2013 IL optogenetic causal evidence is
    the load-bearing source for the maintenance substrate);
    R5 hysteresis verdict (F_high > F_low formation, slower
    dissolution timescale); MECH-322 carve-out specifies the
    accelerated-dissolution rule for replay-origin chunks.

    [2026-07-27 PROVENANCE CORRECTION -- hysteresis relation now
    grounded, hysteresis VALUES still not. Source:
    targeted_review_connectome_mech_323 lit-pull, landed
    REE_assembly 36901c01ed. Text/provenance only; no status,
    confidence, live_status or v3_pending change. The paired note
    on MECH-323 covers F_low and the chunk-size budget.

    Until this pull, the asymmetry F_low < F_high was asserted in
    both MECH-323's and MECH-324's registered text and was
    UNCITED. Two entries now ground the relation:

      - Barnes, Kubota, Hu, Jin & Graybiel 2005 (Nature,
        10.1038/nature04053) -- same laboratory and preparation
        as the Graybiel anchors already underwriting ARC-071.
        Acquisition, extinction, reacquisition with chronic
        multi-electrode recording in sensorimotor striatum;
        task-related ensemble patterns "successively formed,
        reversed and then re-emerged" across the three phases.
        Habits need extensive repetition to form, unlearning is
        notoriously difficult, and regaining one can take a few
        exposures. Marked `supports` at confidence 0.70 (source
        quality 0.92; mapping fidelity the constraint at 0.55).

      - Bouton, Winterbauer & Todd 2012 (Behav Processes,
        10.1016/j.beproc.2012.03.004) -- the INSTRUMENTAL half,
        preferred over Bouton's better-known Pavlovian reviews
        precisely because MECH-323 chunks actions, not cues.
        Renewal, resurgence and rapid reacquisition converge on
        extinction weakening behaviour without erasing the
        original learning. Marked `supports` at confidence 0.68
        (mapping fidelity 0.65, above Barnes, because the object
        of learning is instrumental and measured at the level
        MECH-323 operates on; source quality 0.78, below Barnes,
        because it is a review of the authors' own programme).

    WHAT IS AND IS NOT GROUNDED. The asymmetry these papers
    demonstrate is one of TIMESCALE and RETRIEVABILITY -- how much
    training each direction takes, and how fast the pattern comes
    back. MECH-324's registered "slower dissolution timescale than
    formation timescale" is directly supported. But F_high > F_low
    is a claim about a variance GATE, a different operationalisation
    of "harder", and neither paper measures anything resembling an
    outcome-variance threshold. So the DIRECTION of the inequality
    is supported by analogy across two constructs, and the VALUE
    0.45 -- and the ratio 0.45/0.15 -- gains no support whatsoever.
    Both remain uncalibrated engineering defaults awaiting
    child-MECH calibration. This note exists so that the second
    half of that sentence stays visible next to the first, and so
    the number stops reading as grounded by association.

    STRUCTURAL FINDING -- ACCOMMODATED 2026-07-27 (see the
    dated block below). The finding was flagged here by the
    provenance correction and deliberately not applied, because
    it is a registration change rather than a provenance fix.
    It has now been registered.]

    [2026-07-27 STRUCTURAL CORRECTION -- dissolution is
    SUPPRESSION WITH RETENTION, not erasure. Registration change
    (functional_restatement, falsifiers, open Q-claims); no
    status, confidence, live_status or v3_pending change. Paired
    change on MECH-323. Sources are the same two entries as the
    provenance correction above -- no new lit pull; this applies
    a finding those entries already carried.

    THE FINDING. Barnes 2005 shows striatal ensemble patterns
    reverse under extinction and RE-EMERGE on reacquisition
    rather than being rebuilt from scratch. Bouton 2012 shows
    via renewal, resurgence and rapid reacquisition that
    extinction weakens behaviour without erasing the original
    learning, installing new context-dependent learning
    ALONGSIDE the old. The neural and the behavioural side
    converge on the same conclusion independently, which is why
    this is registered with more confidence than either source
    alone would carry.

    WHAT WAS WRONG. MECH-324's DISSOLVED sub-state read "Re-
    formation of an equivalent-sequence chunk goes through the
    standard MECH-323 trigger from scratch; it does not
    resurrect the dissolved chunk" -- an erasure operator, and
    the wrong one. Neither MECH-323 nor MECH-324 specified
    retention anywhere. Left uncorrected, REE's dissolution
    would look cleaner than biology's and its re-formation
    dynamics would be wrong EVEN IF F_low and F_high were both
    correctly calibrated -- which is what makes this a
    structural defect and not a tuning issue.

    WHAT CHANGED. (1) DISSOLVED is redefined as DORMANT:
    retained unselectable, revivable at a reduced bar
    R_reacq = R_min * f_reacq. (2) Three relapse falsifiers
    registered (rapid reacquisition / renewal / resurgence) with
    their transfer limits -- see the functional_restatement.
    (3) Two open Q-claims added for the structural
    preconditions of (b) and (c): context-scoping of
    dissolution, and library-level competition. (4) f_reacq
    joins F_high on the uncalibrated-defaults list.

    WHAT IS STILL NOT GROUNDED. f_reacq = 0.25 has exactly the
    standing of F_high = 0.45: both sources establish that
    reacquisition is much faster, NEITHER quantifies how much
    faster. Registering the retention operator does not smuggle
    in a calibrated rate, and the transfer limits above (simple
    operant responses vs multi-element composed sequences) bite
    hardest precisely on the quantitative form. Confidence on
    the two source entries is unchanged (0.70 / 0.68); this
    correction adds no evidence, it fixes what the existing
    evidence was already saying.

    SUBSTRATE STATUS AT REGISTRATION TIME. ChunkLibrary /
    ChunkAccumulator in ree-v3
    ree_core/policy/policy_chunking.py did implement the
    erasure reading (DISSOLVED terminal, and in the as-built
    form it also permanently blocked re-formation of the
    sequence -- worse than erasure). A separate in-flight
    session was implementing the dormant-state change against
    this correction concurrently; falsifier (a) is the
    experiment that tests it. Falsifiers (b) and (c) remain
    substrate-blocked on the two open Q-claims above.]

    The Smith & Graybiel 2013 causal evidence is unusually
    strong for an architectural commitment of this scope:
    selective optogenetic disruption is the gold-standard
    causal manipulation in rodent systems neuroscience, and
    the result (IL disruption prevents habit formation) is
    directly the prediction MECH-324 makes about its own
    necessity. The lit-pull entry
    `targeted_review_arc_071_composition/entries/
    2026-05-10_arc_071_dual_operator_chunk_dynamics_
    smithgraybiel2013/record.json` confidence 0.86.

    With MECH-324 registered, ARC-071 substrate is now
    fully specified (formation operator MECH-323 + maintenance
    operator MECH-324 + sleep-replay carve-out MECH-322).
    First validation experiment can now be a three-arm
    discriminative test: ARM_0 baseline (no MECH-323),
    ARM_1 MECH-323 only (formation without maintenance --
    should show accumulator firing but no behavioural-latency
    drop), ARM_2 MECH-323 + MECH-324 (full chunking machinery
    -- should show behavioural-latency drop + within-chunk
    timing tightening). The contrast ARM_1 vs ARM_2 isolates
    MECH-324's contribution, mirroring the Smith & Graybiel
    2013 IL-disruption-vs-intact contrast structurally.

    Not yet experimentally tested. The three-arm validation
    experiment requires both MECH-323 and MECH-324 to have
    substrate-level implementations in ree_core; substrate
    work is queued separately.

    [SCALE CORRECTION, 2026-07-27 -- READ THIS BEFORE DESIGNING
    ANY DISSOLUTION OR RELAPSE EXPERIMENT. F_high = 0.45 is
    UNCHANGED, but the scale it is measured on is now declared
    explicitly, because as registered the two were mutually
    inconsistent and the inconsistency silently disabled this
    claim's entire falsifier family.

    MECH-323 trigger condition (2) specified outcome variance
    "on a 0-1 normalised scale". Maximum POPULATION variance on
    a span-S range is S^2/4, i.e. 0.25 on the unit scale --
    BELOW F_high = 0.45. So on the specified scale
    CRYSTALLISED -> DISSOLVING could never fire, DISSOLVED was
    unreachable, and all three registered relapse falsifiers
    (a) rapid reacquisition, (b) renewal, (c) resurgence lost
    their entry point, each requiring a dissolved chunk. Note
    this is NOT the already-registered "F_high is uncalibrated"
    caveat: the defect was an inconsistency between the value
    and its declared scale, not a missing literature warrant.

    RESOLUTION (option (b) of the two considered). The scale
    specification is withdrawn and the true scale declared: the
    outcome signal is a raw, unbounded, caller-supplied scalar
    in E3-score-receipt units, with no normalisation anywhere in
    the substrate (`policy_chunking._variance` is plain
    population variance; `REEAgent.note_chunk_outcome` forwards
    the harness value unchanged). Consequences for experiment
    design: F_high is reachable only when the outcome span
    satisfies S > 2*sqrt(F_high) = 1.342; S >= 2.0 is the
    recommended floor, since real distributions sit well below
    the degenerate S^2/4 ceiling; and the span must be
    pre-registered, because a wider span also makes MECH-323's
    F_low HARDER to satisfy, so span moves both gates at once.

    The rejected alternative was (a) re-calibrating F_low/F_high
    to fit under the 0.25 unit-scale ceiling. Rejected because
    the unit scale was never implemented (nothing to preserve),
    because the only runtime-available normalisation is
    window-relative and would destroy the discriminative content
    of a variance gate, and because compressing the R5
    hysteresis gap re-guesses two admittedly-uncalibrated
    numbers with no gain in grounding. Full argument in the
    MECH-323 evidence_quality_note SCALE CORRECTION block.

    Surfaced by V3-EXQ-829 (manifest
    v3_exq_829_mech324_rapid_reacquisition_falsifier_20260727T170539Z_v3,
    outcome FAIL), which reached a dissolved chunk only via a
    pre-registered harness outcome span of 2.0 with both
    thresholds left at claim defaults. Its FAIL therefore stands
    as a test of the falsifier, NOT as an artefact of this
    inconsistency -- 829 is the one run that routed around it.
    Earlier or concurrent ARC-071 work that assumed a unit-scale
    outcome signal should be re-read in this light before its
    dissolution-side results are relied on.]

    [2026-07-29 governance, V3-EXQ-829 weakens; failure_autopsy_2026-07-28-sweep, confirmed]:
    adjudicating the FAIL recorded above. Confirms MECH-324's core retention structure (C3 held
    cleanly) but falsifies the SPECIFIC rapid-reacquisition rate prediction: median r_reacq 90 vs
    predicted ~5, flat across all tested f_reacq values {1.0, 0.5, 0.25, 0.1} -- the reduced-
    repetition lever the correction introduced had no measurable effect on the DV it was built to
    move. The autopsy's leading hypothesis (offered as diagnosis, not re-litigated here): revival
    is rate-limited by the variance-window flushing (r_reacq/W ~= 0.908 +/- 0.029 across the sweep,
    tracking W not R_min or f_reacq), making f_reacq inert by construction rather than wrong in
    value. New substrate entry MECH324-REACQ-WINDOW-GATING-DECOUPLE created (decouple the
    crystallisation-counter reset from raw sliding-window contamination). Weakens the specific rate
    claim; does not weaken the retention-vs-erasure structure (see MECH-323, supports). Stays
    candidate; v3_pending unchanged; pending_retest_after_substrate noted via the new substrate entry.

    [2026-07-30 governance, V3-EXQ-810a supports; confirmed failure_autopsy_V3-EXQ-810a_2026-07-30]:
    chunk-accumulator readiness (shared MECH-323/MECH-324 preconditions) independently re-derived
    from raw per-seed data -- worst-seed margins 2.4x-7.0x floors, C7 credit-rule separation 5x. This
    is a readiness/formation-side result, distinct from the 829 rate-specific weakens above; both
    stand (readiness confirmed, the specific rapid-reacquisition rate prediction falsified). Stays
    candidate.
  notes: >
    MECH-324 is the MAINTENANCE half of ARC-071's two-operator
    machinery. MECH-323 (registered 2026-05-11 earlier today)
    is the FORMATION half. Together they fulfil ARC-071's
    architectural commitment per the lit-pull R2 verdict's
    dual-operator framing.

    The four-state lifecycle (FORMING, CRYSTALLISED,
    DISSOLVING, DISSOLVED) is a substantive architectural
    detail beyond what the lit-pull surfaced. Biology is
    silent on the discrete-vs-continuous question -- Smith &
    Graybiel 2013 shows chunks form, dissolve, and reform but
    does not pin a state-machine framing. The four-state
    machine is a REE-side design choice for tractable
    implementation and diagnostic instrumentation; alternative
    framings (continuous selection_weight without discrete
    states) are open at validation time.

    Cross-link to MECH-321 decomposition: open Q-claim about
    how MECH-324 dissolution interacts with MECH-321
    decomposition on the same chunk. The conservative default
    (decomposition is single-execution-only, chunk stays
    CRYSTALLISED) keeps the two operators decoupled at the
    chunk-state level; aggressive alternatives (every
    decomposition incident triggers DISSOLVING) couple them
    more tightly. Child-MECH validation should design the
    discriminative test.

    Cross-link to MECH-322 sleep-replay: MECH-324 is where the
    MECH-322 accelerated-dissolution rule is implemented (sub-
    mechanism C in the functional_restatement). MECH-322
    specifies the rule; MECH-324 owns the implementation. The
    replay-origin field on each chunk (set by MECH-323 at
    formation time, MECH-322-conditioned) is what MECH-324
    reads to decide which dissolution rule applies.

    Pathology cross-references (speculative, pre-validation):
      - Habit-formation deficits in motor-learning impairments
        (pre-registered ARC-071 pathology candidate): if
        MECH-324 maintenance substrate is impaired, chunks
        form but never crystallise. Behavioural signature:
        accumulator activity present (proposable via
        MECH-323) but no behavioural-latency drop on
        repeating sub-sequences. This is the
        MECH-323-only-ARM_1 prediction.
      - Compulsive / addiction-related chunk perseveration
        (pre-registered ARC-071 pathology candidate via
        Graybiel 2008 framing): if MECH-324 dissolution
        thresholds drift too high (F_high too lenient) or
        T_dissolve too long, CRYSTALLISED chunks persist
        even under outcome variance that should have
        dissolved them. Cross-link to MECH-322 replay-origin
        pathology: drift in N (corroboration window too long)
        produces analogous chunk perseveration on replay-
        formed chunks.
      - PTSD-related rigid chunk pathology (cross-link to
        MECH-322 notes): if MECH-324 dissolution is impaired
        AND MECH-322 carve-out fires on traumatic replay,
        rigid chunks form from replay and persist despite
        ongoing outcome inconsistency. Audit-trail register
        enables diagnostic detection of this pattern.

    Open follow-on after MECH-324 registration:
      - First MECH-323 + MECH-324 substrate-readiness
        validation experiment (three-arm discriminative test
        on a structurally repeating sub-sequence task).
      - Substrate-level implementation of both MECH-323 and
        MECH-324 in ree_core (separate substrate-implementation
        session via /implement-substrate skill).
      - Parametric calibration of C_min, W_maint, F_high,
        T_dissolve, N defaults via parametric sweep on the
        first validation experiment.
  location: docs/architecture/policy_primitive_granularity.md


- id: MECH-325
  title: "hippocampal_cue_indexed_trajectory_library -- the hippocampal module should function as a content-addressable trajectory store where PFC context/goal cues pattern-complete against the stored library to retrieve trajectory templates appropriate for the current context; different PFC context states retrieve different trajectory subsets, enabling context-differentiated behavioral option selection without regenerating trajectories from scratch each tick."
  claim_type: mechanism_hypothesis
  subject: hippocampus.cue_indexed_trajectory_library
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-325@2026-06-06T07:53:48.739107Z"
      as_of: 2026-06-06
      verdict: held_v4_by_architectural_commitment/applied
  implementation_phase: v4
  phase_provenance: assigned
  phase_locked: true  # intrinsic V4 scope, reverted 2026-08-10 (chip
                      # chip-20260810-phase-consistency-reconciliation): the
                      # ARC-072->MECH-325 edge that drove this reclassification was
                      # reversed (see ARC-072.notes correction). Own notes: "describes
                      # the target architecture for the hippocampal module BEYOND V3...
                      # V4 candidate designs... All candidates require the
                      # diversity-bootstrapping phase" -- explicit V4 target-
                      # architecture scope, confirmed by ARC-072's diagnostics already
                      # running without it.
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-05-15"
  depends_on:
    - ARC-007    # value-flat proposals -- the V3 substrate MECH-325 upgrades
    - MECH-033   # kernel_chaining_interface -- the trajectory-retrieval interface
    - MECH-292   # ghost-goal bank -- the V3 spatial-indexed approximation
    - MECH-293   # ghost-goal waking probes -- the V3 spatial-indexed approximation
    - ARC-072    # the architectural gap cluster naming the requirement
  lit_conf: 0.82
  literature_anchors:
    - "Buckner (2010) Annual Review of Psychology -- hippocampus as prediction/simulation
       device; preplay events are cue-queryable sequence subsets, not time-indexed archive.
       conf 0.82."
    - "Shin, Tang & Jadhav (2019) Neuron -- forward replay specifically discriminates
       future paths; PFC theta coherence during forward replay predicts correct choice.
       conf 0.84."
    - "Comrie, Frank & Kay (2022) Phil Trans R Soc B -- hippocampal firing codes paths
       not taken, novel path combinations, locations not occupied; imagination as core
       function. conf 0.77."
    - "Tang, Shin & Jadhav (2021) eLife -- theta sequences at decision points encode
       alternatives for deliberation, not just current path; sub-second retrieval
       mechanism. conf 0.82."
  functional_restatement: >
    MECH-325 describes the target architecture for the hippocampal
    module beyond V3. The V3 HippocampalModule (ARC-007 STRICT) is
    a CEM generator: it produces K trajectory candidates per tick
    by iterative refit of a Gaussian in action-object space, guided
    by the terrain prior. It does not store trajectories across
    ticks; each tick's candidates are generated fresh.

    MECH-325 specifies the upgrade: a persistent content-addressable
    store of trajectory templates accumulated during experience,
    indexed by the context/goal cue that was active when the
    trajectory was executed. Retrieval at inference time: PFC
    context representation (z_goal + task context) pattern-completes
    against the stored keys to retrieve the subset of templates
    appropriate for the current context.

    This is distinct from MECH-292/293 (ghost-goal bank + waking
    probes), which is spatially indexed -- it seeds CEM from past
    goal-location anchor z_world representations. MECH-325 requires
    arbitrary-cue indexing (not only spatial proximity) so that any
    distinguishable PFC context state can retrieve a distinct
    trajectory subset.

    The Buckner (2010) framing is the conceptual anchor: hippocampus
    is not a time-indexed archive but a generator of possible
    sequences indexed by context cue. The Shin/Jadhav (2019) result
    is the most direct experimental evidence: forward replay events
    specifically retrieve trajectory templates toward remembered
    goals, and PFC theta coherence during those events predicts
    subsequent correct choice -- this is PFC reading out hippocampal
    retrieval content to guide upcoming action.

    Implementation notes (V4 candidate designs):
      - Episodic buffer with learned key-query retrieval (attention
        over stored (cue, trajectory, value) tuples).
      - Differentiable nearest-neighbour over stored cue embeddings
        (soft lookup, gradient-preserving).
      - Hippocampal-inspired network: pattern completion via
        recurrent attractor dynamics (Hopfield-style; modern
        transformer attention is a differentiable analog).
    All candidates require the diversity-bootstrapping phase (INV-073,
    ARC-072 R2) to populate the store before task-reward narrows
    what gets written.

    V3 staging: the minimal V3 diagnostic is to verify that a
    differentiable approximation to CEM selection can restore the
    gradient path from cue_action_proj to trajectory generation
    before committing to the full library architecture. This
    precedes the full MECH-325 V4 implementation.
  notes: >
    MECH-325 is the positive specification; ARC-072 names the gap
    between MECH-325 and the V3 substrate. Together they set the
    V4 scope for the hippocampal module.

    Cross-link to Buckner (2010) prediction framing: the transition
    from episodic memory (backward-looking) to prediction/imagination
    (forward-looking) mirrors the Shin/Jadhav reverse-to-forward
    replay learning gradient. Early learning is retrospective; late
    learning is prospective. MECH-325's retrieval function is the
    prospective operation.

    ARC-007 STRICT (value-flat proposals) remains valid at V3: the
    terrain navigation produces value-flat candidates because the
    terrain IS value-shaped (residue_val writes value into z_world
    geometry). MECH-325 does not replace this; it adds cue-indexed
    retrieval as a parallel or subsequent retrieval pathway.
  location: docs/architecture/developmental_bootstrapping_hippo_retrieval.md

- id: MECH-326
  title: "pfc_context_bias_as_hippocampal_retrieval_gate -- PFC active goal/task representations send top-down bias signals that steer which hippocampal trajectory templates are retrieved or replayed; different PFC context states should produce different hippocampal retrieval subsets, enabling context-appropriate behavioral option selection; without this routing, the trajectory library is queried uniformly regardless of goal state."
  claim_type: mechanism_hypothesis
  subject: pfc.context_bias_hippocampal_retrieval_gate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-326@2026-06-06T07:53:48.739107Z"
      as_of: 2026-06-06
      verdict: held_v4_by_architectural_commitment/applied
  implementation_phase: v3
  phase_provenance: derived
  phase_derived_from: SD-055
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-05-15"
  depends_on:
    - ARC-016    # dynamic precision -- the V3 PFC influence mechanism MECH-326 extends
    - MECH-025   # action_doing_mode_probe -- PFC-to-hippocampal routing baseline
    - MECH-325   # hippocampal cue-indexed library -- must exist before it can be gated
    - SD-016     # cue_action_proj -- the V3 structural stub with severed gradient
    - ARC-072    # the architectural gap cluster naming the requirement
  lit_conf: 0.82
  literature_anchors:
    - "Miller & Cohen (2001) Annual Review of Neuroscience -- PFC maintains goal/rule
       representations that bias downstream input-output mappings via top-down projections;
       foundational mechanism for context-appropriate response selection. conf 0.84."
    - "Zielinski, Tang & Jadhav (2018) Hippocampus -- PFC context gates hippocampal
       replay content; coordinated hippocampal-PFC SWR activity during inter-trial pauses
       predicts upcoming correct choices. conf 0.79."
    - "Tang, Shin & Jadhav (2021) eLife -- theta sequences at decision points encode
       alternatives; PFC theta sequences simultaneously predict upcoming choice, coordinated
       with hippocampal alternatives; PFC bias steers which hippocampal sweep is selected.
       conf 0.82."
    - "Shin, Tang & Jadhav (2019) Neuron -- PFC theta coherence during forward replay
       predicts correct choice; replay content is goal-directed not random. conf 0.84."
  functional_restatement: >
    MECH-326 describes the routing mechanism that connects PFC goal
    representations to hippocampal trajectory retrieval. Without this
    mechanism, the MECH-325 trajectory library is queried
    unconditionally: the same retrieval distribution over the library
    applies regardless of what goal is currently active.

    Miller & Cohen (2001) provides the PFC side of the mechanism:
    PFC neurons that represent task rules and goals send top-down
    projections to downstream circuits, biasing their input-output
    mappings toward context-appropriate responses. MECH-326 extends
    this to the hippocampal retrieval case: the PFC goal representation
    (z_goal) sends a bias signal that steers the pattern-completion
    lookup in the MECH-325 trajectory store.

    Zielinski/Jadhav (2018) and Tang/Jadhav (2021) provide the
    hippocampal-side experimental evidence: different PFC context
    states produce different hippocampal replay content; PFC theta
    sequences predict upcoming choices in coordination with
    hippocampal alternative-encoding theta sequences.

    In REE's current substrate:
      - SD-016 cue_terrain_proj is partially working: it weights
        terrain precision (harm/goal) by the current cue, which
        steers which part of the residue field guides CEM.
      - SD-016 cue_action_proj is structurally present but
        computationally broken: zero gradient through CEM argmax
        (EXP-0155). This is the open slot for MECH-326.

    V4 implementation target:
      The cue_action_proj path must be replaced with a
      differentiable routing operation: e.g., soft attention over
      the MECH-325 trajectory library keyed by z_goal, or a
      Gumbel-softmax sampling step over stored trajectory indices.
      The key requirement is that the gradient from task-reward
      flows back through the retrieval operation to update the
      cue-key representations in the library.

    V3 staging path:
      Minimal experiment: insert a differentiable approximation to
      CEM selection (e.g., weighted combination of candidate
      action-objects using softmax scores as weights) and verify
      that gradient flows from task reward through the combination
      to cue_action_proj. This restores the topological requirement
      without full library architecture.
  notes: >
    MECH-326 is the routing mechanism that gives MECH-325 its
    cue-differentiation property. Without MECH-326, MECH-325 is
    a library with a flat retrieval distribution -- all contexts
    get the same mix of trajectory templates. With MECH-326, the
    PFC goal state steers which templates are retrieved.

    Relationship to ARC-016 (dynamic precision):
    ARC-016 is about E3-derived dynamic precision modulating the
    relative weight of harm vs goal error signals. MECH-326 is
    orthogonal: it is about goal representation steering trajectory
    retrieval, not about weighting error channels. Both are PFC-
    influenced but via different downstream circuits.

    Failure mode: if MECH-326 is absent or weak, behavioral
    diversity at inference time is independent of goal state --
    the agent proposes the same candidate set regardless of what
    goal it is currently pursuing. This is the situation diagnosed
    by EXP-0155 and the EXQ-561 cluster.
  implementation_note: >
    SD-055 V3 staging substrate-ready 2026-05-21: softmax-weighted CEM ao_mean restores
    gradient to cue_action_proj when use_differentiable_cem=True (V3-EXQ-568 PASS).
    v3_pending retained: MECH-325 trajectory library and full context-sensitive retrieval
    gate not implemented. Behavioural cue-divergence experiment still required.
  location: docs/architecture/developmental_bootstrapping_hippo_retrieval.md

# Play substrate follow-on claims -- registered 2026-05-16 (play substrate follow-on pass)

- id: MECH-327
  title: "During sensorimotor play, E1 prediction-error locus upweights probe-action selection: actions targeting high-PE regions of z_world are preferentially selected, implementing curiosity-driven exploration within the play frame."
  claim_type: mechanism_hypothesis
  subject: play.pe_locus_probe_action_selection
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-194
    - ARC-049
    - SD-012
  location: docs/architecture/play_substrate_design.md
  notes: "Stahl and Feigenson 2015 (infant surprise and subsequent probe-action selection): infants who witness expectation violations (surprising physics events) selectively direct subsequent play toward the surprising object and engage in causal testing actions (dropping, banging) rather than conventional play. Schulz and Bonawitz 2007 (toddlers choose to play with ambiguous vs. unambiguous toys after differential teaching): free-choice exploration is directed toward regions of highest epistemic uncertainty. REE translation: the E1 prediction error field (PE map over z_world) provides a natural selection bias for play-episode action choices. Actions that probe high-PE regions provide maximal learning signal per episode step, implementing intrinsic curiosity within the play frame. This is distinct from MECH-328 (z_goal synthetic manifold) -- MECH-327 is about action selection steering, not goal representation. Implementation: during play episodes, add PE-weighted exploration bonus to E3 action-object scoring before CEM candidate evaluation. Missing from initial play_substrate_design.md -- flagged as DEV-NEED in original design."

- id: MECH-328
  title: "Synthetic and real z_goal vectors must lie in the same latent manifold for play-to-real policy transfer to succeed; disjoint manifolds produce a transfer failure where play-acquired competence cannot generalise to real goal-pursuit."
  claim_type: mechanism_hypothesis
  subject: play.goal_space_continuity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-194
    - MECH-195
    - ARC-049
  location: docs/architecture/play_substrate_design.md
  notes: "Colas 2022 (MAGE -- language-conditioned goal representations for transfer): task-agnostic play transfers policy structure only when play-generated goals and task goals are grounded in the same representation space. Manifold disjunction (e.g., one-hot synthetic goals vs. continuous real z_goal) produces a near-zero transfer rate -- the agent has learned to pursue an entirely different kind of goal object. REE translation: z_goal_synthetic must be constructed by sampling from the same encoder that generates real z_goal vectors (E3 or SD-012 output), not from a separate synthetic goal generator. During play, z_goal_synthetic is a real vector in z_goal space, but one whose magnitude is LP-weighted (MECH-194, Oudeyer 2007 learning progress) rather than derived from drive_level. This is a strong implementation constraint: the GoalConfig.z_goal_synthetic_vector must pass through the same normalisation and embedding as real goal vectors."

- id: ARC-073
  title: "Play-to-real transition is triggered by competence saturation (d(PE)/dt < threshold) or competitive drive pressure, not by scheduled duration; premature transition leaves policy structure undertrained."
  claim_type: architecture_hypothesis
  subject: play.transition_criterion
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  depends_on:
    - MECH-194
    - MECH-195
    - MECH-327
    - ARC-049
  location: docs/architecture/play_substrate_design.md
  notes: "Pezzulo 2014 (homeostatic vs. exploratory drive competition): transition from exploration to exploitation is driven by the ratio of exploratory to homeostatic drive strength, not by a fixed clock. Forestier 2022 (CURIOUS -- modular curiosity + competence saturation in robotics): play continues as long as learning progress is positive for any goal region; transition to real-task mode occurs when LP < threshold across all goal regions. REE translation: play episodes should persist as long as E1 PE is declining (d(PE)/dt < 0 but |d(PE)/dt| > threshold) -- i.e., as long as play is producing useful world-model learning. When learning progress saturates (|d(PE)/dt| < threshold for all reachable PE regions), homeostatic drive (SD-012) reasserts and overrides synthetic z_goal, triggering episode close (MECH-196). Implementation: monitor rolling LP estimate during play; close episode when LP drops below play_lp_saturation_threshold (GoalConfig parameter). Alternative trigger: real homeostatic drive_level exceeds synthetic z_goal benefit_exposure (competition criterion)."

- id: Q-048
  title: "During sensorimotor and constructive play, is a persistent background signal (L2) sufficient for frame integrity monitoring, or are explicit mid-episode repair signals (L3) also required?"
  claim_type: question
  subject: play.frame_content_sensorimotor_constructive
  polarity: open_question
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Ablate the L3 mid-episode repair signal during simple (sensorimotor /
    constructive) play episodes and measure frame-integrity under perturbation.
    Arms: (1) L2 background play_frame_tag only (L3 repair OFF); (2) L2 + L3
    repair ON. Perturb mid-episode (partner stops contingent responding for k
    ticks, or the compositional goal is briefly violated) and measure
    frame-collapse rate (fraction of episodes where play_frame_tag drops and is
    not re-established) plus time-to-recovery. ANSWER 'L2 sufficient for simple
    play' if the L2-only arm shows no significant increase in frame-collapse rate
    vs L2+L3 under perturbation (delta below an absolute floor, >=2/3 seeds) --
    the persistent background signal alone holds the frame for low-ambiguity
    content. ANSWER 'L3 also required' if L2-only collapses (or fails to recover)
    significantly more often than L2+L3, with the gap scaling on perturbation
    strength. HARD PRECONDITION: the perturbation must actually stress frame
    integrity -- the L2+L3 arm must itself show non-zero collapse/recovery events
    under perturbation, else the test is vacuous (nothing to repair) and routes to
    a stronger-perturbation re-design rather than a 'sufficient' verdict.
  confidence: 0.0
  epistemic_category: standard
  depends_on:
    - ARC-049
    - Q-035
  location: docs/architecture/play_substrate_design.md
  short: >
    For simple play types, is L2 background signal sufficient or are L3 repair events also needed?
  description: >
    Q-035 established that continuous monitoring is primitive and present from infancy. For
    sensorimotor and constructive play, the monitored CONTENT is simple: is the partner still
    contingently responding, and is the compositional goal still shared? Q-048 asks whether the
    L2 background signal alone (persistent play_frame_tag) is sufficient to maintain frame
    integrity for this simple content, or whether L3 repair signals must also fire. Animal
    parallel: play-face maintenance without play-bow reinitiation during low-ambiguity play (e.g.,
    simple chase games). V3-tractable: ablate L3 signals during simple play episodes and measure
    frame collapse rate under perturbation.
  suggested_experiment_type: behavioral_mode_ablation

- id: Q-049
  title: "Does pretend play require the hypothesis tag (MECH-094) and play frame tag (ARC-049) to be continuously co-active throughout the episode, or do they merely need to be co-active at the moment of object substitution?"
  claim_type: question
  subject: play.frame_content_pretend_cotag
  polarity: open_question
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Compare two co-tag regimes for pretend play in V3: (1) CONTINUOUS co-tag --
    hypothesis_tag (MECH-094) and play_frame_tag (ARC-049) held co-active for
    every step of the pretend episode; (2) EVENT-ONLY co-tag -- the two tags
    co-active only at the moment of object substitution, otherwise allowed to
    lapse. Run matched pretend episodes requiring object substitution under
    perturbation (reality intrusions, competing literal-use affordances) and
    measure policy quality and false-commit rate (committing to the
    literal/non-pretend interpretation when the pretend frame should hold). ANSWER
    'continuous co-tag required' if the event-only arm shows a significantly
    higher false-commit rate or degraded pretend-policy quality (>=1 SD-of-delta,
    absolute floor, >=2/3 seeds), i.e. the decoupling lapses between substitution
    events and reference/truth/existence relations leak back. ANSWER 'event-only
    sufficient' if event-only matches continuous on false-commit and policy
    quality. HARD PRECONDITION: both tags must be operative and the substitution
    event must be detectable in the trace (non-zero co-tag activation at
    substitution); if the event-only arm never actually drops co-activity between
    events the two arms are identical and the test is vacuous.
  confidence: 0.0
  epistemic_category: standard
  depends_on:
    - ARC-049
    - MECH-094
    - MECH-198
    - Q-035
  location: docs/architecture/play_substrate_design.md
  short: >
    Pretend play: must hypothesis_tag and play_frame_tag be co-active throughout or only at substitution moments?
  description: >
    Leslie 1987 established that pretend play requires a decoupling operator suspending reference,
    truth, AND existence relations simultaneously -- more than simple suppression. In REE terms this
    maps to MECH-094. Q-049 asks whether the co-activity of hypothesis_tag + play_frame_tag must
    be maintained continuously throughout a pretend episode (strong co-tag hypothesis) or only at
    moments of active object substitution (weaker event hypothesis). Kuhn-Popp 2013 found distinct
    ERP signatures for both tags but the timing resolution does not resolve this. V3-tractable:
    compare policy quality under continuous co-tag vs. event-only co-tag; measure false-commit rate.
  suggested_experiment_type: ablation_comparison

- id: Q-050
  title: "Do games-with-rules require per-step dual-level content monitoring (rule compliance + strategic rationality simultaneously), or does sequential sampling across turns suffice?"
  claim_type: question
  subject: play.frame_content_games_rules_dual_level
  polarity: open_question
  status: open
  live_status:
    reading: open/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    In a multi-agent games-with-rules environment, contrast a per-step dual-level
    monitor (rule-compliance AND strategic-rationality scored every turn, Schmidt
    2015) against a sequential-sampling monitor (the two levels interleaved across
    turns, not simultaneous). Introduce partner moves that violate ONLY rules,
    ONLY strategic rationality, and both. ANSWER "per-step dual-level required" if
    the sequential-sampling arm misses or lags on violations that fall on the
    level it is not currently sampling -- detection latency or accuracy strictly
    worse on mixed/fast-alternating violations -- while the per-step arm catches
    them. ANSWER "sequential sampling suffices" if interleaved sampling matches
    per-step detection across violation types. HARD PRECONDITION: a genuine
    multi-agent substrate that GENERATES strategic violations is required (a
    partner that can defect or play irrationally). The V3 degenerate case is an
    experimenter-complicit environment that never produces strategic violations,
    so neither level can be stressed and the contrast is vacuous (depends_on
    ARC-049 / MECH-197 play substrate, Q-035). Wait for the planned upstream V4
    multi-agent substrate; suggested_experiment_type multi_agent_behavioral.
  confidence: 0.0
  epistemic_category: substrate_conditional
  depends_on:
    - ARC-049
    - MECH-197
    - Q-035
  location: docs/architecture/play_substrate_design.md
  short: >
    Games-with-rules: per-step dual-level monitoring, or turn-by-turn sequential sampling?
  description: >
    Schmidt 2015 showed normative monitoring in games operates at two levels simultaneously: rule
    compliance (what moves are allowed) and strategic rationality (is partner playing to win?).
    Q-050 asks whether implementation must track both levels simultaneously per step, or whether
    interleaved sampling suffices. V4-tractable: requires multi-agent substrate. Cannot be tested
    in V3 degenerate case (experimenter-complicit environment does not generate strategic violations).
  suggested_experiment_type: multi_agent_behavioral

- id: Q-051
  title: "Does cooperative play require a continuous bilateral background signal (L2) plus explicit repair at ambiguity (L3), or do one-sided frame-open/close events (L1 only) suffice for cooperative frame maintenance?"
  claim_type: question
  subject: play.frame_content_cooperative_bilateral
  polarity: open_question
  status: open
  live_status:
    reading: open/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    In a cooperative-play environment with role-switching across extended
    episodes, ablate the frame-maintenance machinery to three levels: L1-only
    (one-sided open/close frame events), L1+L2 (add a continuous bilateral
    background signal), and L1+L2+L3 (add explicit repair at ambiguity from both
    parties). Inject frame ambiguity and role-switch points. ANSWER "full L1+L2+L3
    required" if the L1-only arm produces frame mismatch that degrades cooperation
    worse than no-frame baseline (Ham 2024: mismatch is worse than absence) while
    the L2/L3 arm recovers, AND repair events at ambiguity measurably restore
    alignment. ANSWER "L1 suffices" if one-sided open/close events maintain
    bilateral alignment as well as the full stack across role-switches. HARD
    PRECONDITION: bilateral role/goal-state monitoring requires a multi-agent
    substrate with two parties that each assert and repair frames and that can
    role-switch -- INV-059 frames unilateral assertion as manipulation, so a
    single-agent or experimenter-complicit V3 env cannot generate the bilateral
    mismatch the test needs (vacuous). Wait for the planned upstream V4
    multi-agent substrate (depends_on ARC-049 / MECH-197 / INV-059 / Q-035).
  confidence: 0.0
  epistemic_category: substrate_conditional
  depends_on:
    - ARC-049
    - INV-059
    - MECH-197
    - Q-035
  location: docs/architecture/play_substrate_design.md
  short: >
    Cooperative play: does frame maintenance require full L1+L2+L3 or can L1 suffice?
  description: >
    Cooperative play requires bilateral role and goal state monitoring across extended episodes
    with role-switching. INV-059 asserts unilateral frame assertion is manipulation, implying
    bilateral is necessary. Q-051 asks whether bilateral L1 (open/close events from both parties)
    is sufficient, or whether full L1+L2+L3 is required (continuous background + ambiguity repair
    from both parties). Ham 2024 shows frame mismatch is worse than frame absence -- suggesting
    L2/L3 are probably required. Full test requires V4 multi-agent substrate with role-switching.

- id: ARC-074
  title: "The infant stage requires a reward-free Phase 0 motor babbling epoch, prior to any E3 scoring or reward-weighted learning, in which Hebbian sensorimotor mappings, basic z_self structure, and non-trivial residue gradients are established."
  claim_type: architectural_commitment
  subject: development.phase_0_reward_free_exploration_epoch
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  confidence: 0.0
  depends_on:
    - ARC-046
    - INV-055
    - SD-005
  short: >
    A reward-free Hebbian babbling phase (Phase 0) must precede E3 scoring. Without it,
    cold-start E3 scores on a flat residue field, making ARC-065 diversity mechanisms
    structurally ineffective regardless of parameter settings.
  description: >
    Current infant training begins with E3 scoring active from step 0. On a flat
    residue field (all regions equally unexplored), E3 cannot differentiate trajectories
    -- every proposal receives near-identical scores. This produces a cold-start attractor
    that collapses the CEM proposer to monostrategy (V_s) regardless of diversity
    parameters (MECH-313, MECH-314, MECH-320). The solution is a Phase 0 epoch in which
    E3 weighting is suppressed and the agent performs purely Hebbian sensorimotor
    exploration: z_self encoder is trained on proprioceptive prediction error, basic
    approach/avoidance gradients are established by ambient harm/benefit contacts, and
    the residue field accumulates its first spatial heterogeneity. Only after Phase 0
    criteria are met (minimum residue_entropy threshold, z_self encoder loss below
    criterion) does E3 scoring activate and Phase 1 infant training begin.
    Biological grounding: Doupe & Kuhl 1999 (subsong = pre-critical-period
    undirected motor babbling before song-template-guided learning); Griffin et al.
    2026 (reward-free exploration epoch before goal-directed RL); Garcia-Guzman 2026
    (pre-reward Hebbian phase as prerequisite for structured curriculum). The phase is
    analogous to the prespeech babbling stage in human infants: rich motor exploration
    before any communicative reinforcement is received.
    Implementation: a Phase0Config block with epoch_steps, hebbian_lr, and two gate
    criteria (residue_entropy_min, z_self_loss_max). Phase 0 is skipped if warm-start
    checkpoint is loaded. Failure to meet gate criteria within epoch_steps raises a
    training error rather than silently proceeding.
  location: docs/architecture/infant_substrate_expansion.md
  notes: >
    Registered 2026-05-16 following governance decision in developmental_experiment_priorities.md.
    This is a new architectural commitment, not an amendment to ARC-046 (which governs hazard
    protection during Phase 1+ infant training). ARC-074 governs the earlier pre-scoring epoch.
    Without Phase 0, the warm-start gate calibration (DEV-NEED-029, ARC-065) has no valid
    baseline: ARC-065 diversity mechanisms cannot be evaluated on a substrate that has never
    produced differentiated E3 scores. Phase 0 completion is a prerequisite for EXQ-ISEF-006
    (warm-start gate calibration experiment). See developmental_experiment_priorities.md for
    the dependency graph and ranked experiment schedule.
  epistemic_category: substrate_conditional
  what_would_answer: >
    CROSS-REFERENCE (mandatory reading before any retest is queued): this claim's mechanism is
    NOT an independent discovery. It is a candidate fifth locus in the registry's existing
    conversion-ceiling / monostrategy investigation (MECH-439 F-dominance, INV-088
    representation-ceiling, MECH-457 competence-floor/actor-critic). That campaign has run
    20+ confirmed experiments against a substrate where E3 is ALREADY ACTIVE and found the
    diversity-mechanism failure structural and downstream of selection (F-dominance) or
    attributable to representation under-differentiation in an already-training z_world
    (INV-088) or to action-learning-machinery failure independent of representation
    (MECH-457) -- NONE of which manipulate the pre-E3, pre-training period ARC-074 targets.
    INV-088's own memory record already proposes an untested "differentiate-first" ordering
    prediction structurally identical in shape to ARC-074's Phase 0, for z_world rather than
    the residue field specifically. ARC-074 should be read as sharpening/extending that
    untested ordering prediction to the residue field and to the very start of training, NOT
    as a rediscovery of the F-dominance or competence-floor findings, and NOT as something
    those findings already settle.

    NON-DEGENERACY PRECONDITION (must hold before any A/B result is treated as informative):
    (i) Phase0Config is actually implemented and wired ahead of E3 activation (epoch_steps,
    hebbian_lr, residue_entropy_min, z_self_loss_max gate criteria; Phase 0 skipped only on
    warm-start checkpoint load; gate failure within epoch_steps raises a training error) --
    not a stub or no-op default. (ii) The gate criteria genuinely discriminate a trained vs.
    untrained state -- residue_entropy and z_self encoder loss must show real separation
    between fresh-init and Phase-0-completed agents. (iii) ARC-065's diversity mechanisms
    (MECH-313/314/320) are genuinely exercised downstream in both arms -- confirm non-zero
    diversity-channel activation in both arms so a null result cannot be attributed to the
    diversity stack being silently inert for an unrelated reason. (iv) The environment/task
    used is confirmed solvable (an oracle/greedy baseline clears the floor).

    CONFIRMING (2-arm A/B, WITH-Phase-0 vs WITHOUT, diversity params at existing validated
    settings): E3 scores measurably differentiate once Phase 0 completes (residue_entropy and
    per-candidate E3-score variance both rise from step-0 baseline) AND the WITH-Phase-0 arm
    shows a measurable, non-degenerate diversity-mechanism effect the WITHOUT arm does not,
    under matched seeds/config.

    FALSIFYING: monostrategy/V_s collapse persists in the WITH-Phase-0 arm at statistically
    indistinguishable levels from WITHOUT, despite the non-degeneracy preconditions holding.
    This would mean the root cause is NOT cold-start residue-field flatness but lies where the
    existing MECH-439/INV-088/MECH-457 cluster already localizes it -- in which case ARC-074
    should be folded into that cluster's disposition rather than treated as a separate open
    front, and EXQ-ISEF-006's warm-start gate calibration should not be attempted on this
    substrate.

    A partial result -- Phase 0's own gate met but E3 scores still fail to differentiate once
    Phase 1 begins -- is itself diagnostic: the ordering fix would be necessary but not
    sufficient, and should route to the F-dominance/representation-ceiling cluster rather than
    to a Phase0Config parameter retune.

    [2026-08-08 thought-digestion]: Phase0Config confirmed UNBUILT (zero hits across
    ree-v3/ree_core for Phase0Config/hebbian/phase_0/residue_entropy_min/z_self_loss_max
    outside an unrelated MECH-094/ARC-108 Hebbian eligibility-trace mechanism). EXQ-ISEF-006
    has never been queued or run (absent from experiment_queue.json, evidence/, and
    git log -S on ree-v3).

- id: MECH-329
  title: "The wanting system (mesolimbic dopamine / approach motivation) seeds z_goal anchors via accidental benefit contacts before the liking system (hedonic evaluation) is calibrated -- producing a first wave of super-ordinal goals shaped by approach drive rather than calibrated preference."
  claim_type: mechanism_hypothesis
  subject: development.wanting_before_liking_goal_seeding_sequence
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-669a_2026-06-13
      as_of: 2026-06-13
      verdict: non_contributory/substrate_ceiling
  confidence: 0.0
  epistemic_category: substrate_ceiling
  pending_retest_after_substrate: true
  substrate_note_2026_07_21: >
    SD-077 (centered super-ordinal cue key, IMPLEMENTED 2026-07-21) lifts the
    context-diversity blocker that made V3-EXQ-669b's R3 readiness gate unmeetable
    (anchor_count saturated at 1 because the raw-z_world cue cosine measured the
    common-mode offset, not the context). anchor_count and the p01 complexity statistic
    now both have real cross-arm range, so a fresh lettered iteration (V3-EXQ-669c) can
    score C1/C3. Status, confidence, epistemic_category and pending_retest_after_substrate
    are DELIBERATELY unchanged here: lifting the substrate ceiling makes the claim
    measurable, it does not supply evidence. Governance should re-read this claim only
    after 669c returns.
  governance_note_2026_06_13: >
    PM cycle (gov 2026-06-13T20:04Z): V3-EXQ-669a superseded (degenerate by construction:
    anchor/write/p01=0 in all arms incl the c4 sanity control) and its nursery-harness retest
    V3-EXQ-669b non_contributory (self-stamped non_degenerate:false -- R3 unmet, max anchor_count=1;
    merge_similarity collapses the nursery z_world contexts so C1/C3 have zero cross-arm range).
    Both blocked by the same goal_pipeline:GAP-2 context-diversity ceiling (per confirmed
    failure_autopsy_V3-EXQ-669a_2026-06-13). epistemic_category=substrate_ceiling +
    pending_retest_after_substrate: re-queue with a more context-diverse nursery / lower
    super_ordinal merge threshold. Not a falsification of the wanting-before-liking ordering.
  governance_note_2026_06_18: >
    Substrate-ceiling orphan-routing pass (gov 2026-06-18): epistemic_category substrate_ceiling
    CONFIRMED correct -- the goal_pipeline:GAP-2 context-diversity ceiling is a genuine V3
    enrichment (context-diverse nursery / lower super_ordinal merge threshold), not a planned-
    upstream substrate, and the base substrate INF-ENV-003 (transient benefit patches) is already
    implemented. Resolved the orphan by WIRING (not reclassification): added MECH-329 to
    unblocks_claims on the goal_pipeline:GAP-2 substrate_queue entry SD-049-PHASE-2 (the wanting/
    liking behavioural-validation owner). pending_retest_after_substrate stays true; no status/
    category change.
  depends_on:
    - MECH-189
    - SD-014
    - INV-037
    - INV-038
  short: >
    VALENCE_WANTING (z_goal seeding via MECH-189) precedes VALENCE_LIKING calibration.
    Earliest z_goal ContextMemory writes reflect approach drive under accidental benefit
    contacts, not mature hedonic discrimination -- wanting shapes the goal hierarchy
    before liking is operational.
  description: >
    MECH-189 describes how high-salience benefit contacts write super-ordinal goals to
    ContextMemory. MECH-329 specifies the temporal ordering: the approach-motivation
    (wanting) component activates first, seeding z_goal anchors from the earliest
    accidental benefit contacts, while the hedonic evaluation (liking) component lags
    and calibrates progressively across infancy and childhood. This sequencing means
    the first layer of the goal hierarchy is structured by exploration-drive and
    accidental outcomes rather than by any calibrated preference function.
    Biological grounding: Berridge & Robinson 1998 (wanting/liking dissociation --
    mesolimbic dopamine mediates incentive salience / approach motivation independently
    of hedonic pleasure; lesioned rats approach without liking). Developmental ordering
    [anchor corrected 2026-06-10; original "Keren-Portnoy & Tomasello 2021" could not be
    verified -- see evidence/literature/targeted_review_mech_329/SYNTHESIS.md]: Behne,
    Carpenter, Call & Tomasello 2005 (infant goal-directed-action understanding emerges
    ~9 months -- proto-intentional/desire-like structure is operational before a mature
    hedonic preference function is calibrated) and Corbetta 2021 (infant goal-directed
    reaching originates ~3-5 months from ACCIDENTAL hand-target contact that intrinsically
    motivates reproduction -- accidental-contact-plus-approach-drive seeding before
    calibrated preference). Caveat (Opendak, Meyer, Callaghan et al. 2025): mesolimbic
    dopamine matures late and is likely not functional perinatally (earliest reward
    learning is locus-coeruleus norepinephrine), so the developmental ORDERING is
    grounded but the specifically-MESOLIMBIC attribution for the earliest seeding window
    is held loosely (read as "an early approach/incentive drive"). In animals and human
    infants, approach toward novel stimuli is driven by wanting before calibrated,
    opioid-mediated liking is established.
    Implementation consequence: z_goal ContextMemory writes during Phase 0 and early
    Phase 1 (infant) are triggered by approach-drive proximity to benefit patches, not
    by repeated preference evaluation. MECH-329 predicts that removing Phase 0 (ARC-074
    violation) degrades the goal hierarchy: without accidental benefit contacts in a
    non-trivial environment, wanting-driven goal seeding produces no z_goal anchors,
    and the adult agent begins with a structurally empty goal hierarchy. The infant
    substrate enrichment (infant_substrate_expansion.md: EXQ-ISEF-002 transient benefit
    patches) is the direct experimental test of this mechanism.
  location: docs/architecture/developmental_curriculum.md
  notes: >
    Registered 2026-05-16 as a child mechanism of MECH-189. Grounded in wanting/liking
    dissociation literature (Berridge & Robinson 1998; Smith, Berridge & Aldridge 2011)
    and infant developmental-ordering literature (Behne, Carpenter, Call & Tomasello 2005;
    Corbetta 2021) -- anchor corrected 2026-06-10: the originally-cited "Keren-Portnoy &
    Tomasello 2021" could not be independently verified and was replaced; mesolimbic-DA
    specificity tempered per Opendak et al. 2025 (see lit-pull
    targeted_review_mech_329/SYNTHESIS.md; literature_confidence updated as a parallel
    signal only -- experimental_confidence/status unchanged). Proposed experimental test:
    EXQ-ISEF-002 (transient benefit patches in infant
    substrate) -- compare z_goal ContextMemory write rate and anchor diversity with and without
    benefit patches in Phase 0/Phase 1. Ranked experiment 2 in developmental_experiment_priorities.md.
  suggested_experiment_type: multi_agent_behavioral

- id: MECH-330
  title: "idle_aversion_acute_restlessness_accumulator (engagement-rate-keyed aversive accumulator on z_harm_a / affective stream at the acute timescale ~minutes/tens-of-episodes. First child mechanism for ARC-067. Routes boredom-aversive through SD-011 affective harm stream / AIC analog per Danckert 2018 AIC-deactivation under boredom and Wilson 2014 Science self-shock exchangeability with physical pain. Three-input executive-engagement-rate estimator: commit transitions per episode, E3 deliberation depth, residue-write rate; novel-observation count dropped as primary input (van Tilburg 2017 meaning-seeking not stimulation-seeking). MAC meaning-component adds a parallel secondary channel via existing z_goal substrate (SD-012 / MECH-308). Cross-talk to MECH-331: sustained MECH-330 activation without discharge seeds the chronic frontostriatal effort-allocation substrate.)"
  claim_type: mechanism_hypothesis
  subject: action.idle_aversion_acute_restlessness_accumulator
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-05-16"
  depends_on:
    - ARC-067     # parent architectural commitment (idle aversion / boredom)
    - SD-011      # affective harm stream (z_harm_a) -- routing target for boredom-aversive write
    - MECH-308    # homeostatic_drive_signal -- z_goal substrate feeds MAC meaning secondary channel
  functional_restatement: >
    MECH-330 names the primary acute child mechanism for ARC-067. It adds
    a slow negative-valence accumulator to the z_harm_a affective stream
    that activates when the agent's executive engagement-rate falls below
    a threshold, at the timescale of minutes to tens of episodes.

    Architecture (from ARC-067 lit-pull R1 + R3 + R4 verdicts):

    1. THREE-INPUT ENGAGEMENT-RATE ESTIMATOR (R4 verdict):
         e_t = w_commit * commit_rate(window)
               + w_depth  * deliberation_depth(window)
               + w_resid  * residue_write_rate(window)
               [+ w_mean  * meaning_channel(z_goal, SD-012/MECH-308)  -- secondary]
       where:
         - commit_rate: transitions per episode into/out of committed
           action (rare commits -> low engagement)
         - deliberation_depth: mean E3 trajectory candidates evaluated
           before selection in recent window (shallow selection -> low)
         - residue_write_rate: residue writes per episode in recent
           window (no learning -> low engagement)
         - meaning_channel: z_goal presence and goal-progress signal
           from existing SD-012 / MECH-308 substrate (MAC secondary
           per Westgate & Wilson 2018; low cost -- reuses existing
           estimator, does not build a new one)
       Novel-observation count DROPPED per R4 verdict: conflates
       with MECH-314a striatal novelty bonus; van Tilburg 2017
       meaning-seeking (not stimulation-seeking) signature rules
       it out as primary input.

    2. SLOW ACCUMULATOR WITH THRESHOLD:
         e_deficit_t = max(0, e_threshold - e_t)
         boredom_t   = ewma_window(e_deficit_t, half_life=H_acute)
       H_acute is the acute-timescale integration window (~minutes-
       scale; short enough to accumulate within an episode, long
       enough to avoid false alarms on single low-deliberation steps).
       boredom_t decays automatically once e_t rises above e_threshold
       -- the accumulator self-terminates on engagement discharge.

    3. ROUTING THROUGH z_harm_a (R3 verdict):
         z_harm_a += w_boredom * boredom_t
       Same channel as physical harm -- enables the pain/boredom
       exchangeability that Wilson 2014 Science documents
       (self-shock finding: boredom-aversive traded against physical
       pain across 11 studies). Produces low-arousal-but-action-
       recruiting valence signature consistent with van Tilburg 2017
       empirical differentiation; does NOT couple to SD-037 broadcast
       override (which requires high-arousal aversive -- different
       corner of agent state space).

    4. CROSS-TALK TO MECH-331 (one-way seeding):
       Sustained MECH-330 activation without discharge -- defined as
       boredom_t >= b_chronic_threshold for >= N_episodes without an
       engagement discharge event (commit transition, goal contact,
       or residue write spike) -- seeds the MECH-331 frontostriatal
       effort-allocation substrate. One-directional: MECH-330 ->
       MECH-331; MECH-331 does not gate MECH-330's accumulation.
       MECH-331 represents the slow consequence of sustained non-
       discharge.

    DISCHARGE CONDITION (van Tilburg 2017 functional-emotion action-
    toward-new-engagement framing): a single commit transition or
    residue-write spike restores e_t above e_threshold; boredom_t
    decays at H_acute. The mechanism self-terminates cleanly without
    an explicit reset event.

    Falsifiable prediction: agent in well-fed-safe-familiar env with
    no z_goal active. ARM_0 ARC-067-OFF: action_density at MECH-313
    noise floor. ARM_1 MECH-330-ON: boredom_t rises monotonically,
    z_harm_a accumulates, E3 scores bias toward action trajectories;
    action density increases specifically at boredom_t threshold
    crossing, not before.
  evidence_quality_note: >
    Registered 2026-05-16 from ARC-067 lit-pull synthesis
    (evidence/literature/targeted_review_arc_067_boredom/synthesis.md,
    2026-05-10, 8 entries, 7 supports + 1 mixed).

    R1 functional-account verdict: PRIMARY = engagement-rate aversive
    accumulator (Eastwood 2012 unengaged-mind definition, Bench &
    Lench 2013 goal-shift function, van Tilburg 2017 emotion
    differentiation). MAC meaning-component (Westgate & Wilson 2018
    Psychol Rev) is parallel secondary channel reusing z_goal
    substrate. ARC-068 opportunity-cost overlap is real but operates
    on a different architectural layer (E3 score vs z_harm_a valence).

    R3 routing verdict: grounded in Danckert & Merrifield 2018 (AIC-
    deactivation under boredom -- boredom falls in affective-salience /
    AIC territory = z_harm_a routing) and Wilson et al. 2014 Science
    (boredom-self-shock exchangeability with physical pain -- shared
    axis requires shared channel).

    R4 input-narrowing verdict: THREE inputs KEPT (commit-transitions,
    deliberation-depth, residue-write-rate). Novel-observation-count
    DROPPED. Bench & Lench 2013 goal-shift function supports commit-
    transition-rate. Danckert 2018 AIC/executive-network deactivation
    supports deliberation-depth as executive-engagement proxy.

    Lit anchors (6 of 8 in pull directly relevant):
      - Eastwood 2012 (Perspect Psychol Sci, PMID 26168101) --
        definitional: boredom as unengaged mind. supports 0.78.
      - Westgate & Wilson 2018 (Psychol Rev) -- MAC model, secondary
        meaning channel. supports 0.82.
      - van Tilburg & Igou 2017 (Emotion, PMID 28079397) -- meaning-
        seeking differentiation. supports 0.74.
      - Bench & Lench 2013 (Behav Sci) -- goal-shift function,
        commit-transition-rate proxy. supports 0.66.
      - Wilson 2014 (Science, PMID 24993755) -- self-shock
        exchangeability, direct aversive evidence. supports 0.78.
      - Danckert & Merrifield 2018 (Exp Brain Res) -- AIC-deactivation
        under boredom, anatomical routing anchor. supports 0.72.

    Not yet experimentally tested. First validation experiment: 3-arm
    substrate-readiness diagnostic (ARC-067-OFF / MECH-330-ON acute-
    only / MECH-330+331-ON both poles). Target substrate: SD-054 reef
    or equivalent environment with structurally rich engagement options.
    Aggregate acute-pole lit_conf expected 0.74-0.78.
  notes: >
    First child mechanism for ARC-067. Second child is MECH-331
    (idle_aversion_chronic_anhedonic_flatness_substrate) at the chronic
    timescale; cross-talk via one-way seeding signal (MECH-330
    sustained activation -> MECH-331 substrate seeding).

    Implementation-phase placement: MECH-330 is a valence-accumulator
    write to z_harm_a (affective stream), NOT a score-side mechanism.
    The boredom valence feeds E3 scoring via the existing z_harm_a ->
    E3 pathway -- the same path physical harm uses. This contrasts with
    MECH-320 (tonic vigor, score-side) and MECH-313 (noise floor,
    temperature-side). All three create non-specific action pressure via
    different architectural layers.

    Distinct-from contracts:
      - MECH-314a (striatal novelty bonus): novelty is a positive bonus
        on novel candidates (score-side); MECH-330 is a valence-side
        aversive on engagement-failure. Can fire simultaneously in novel
        territory with low engagement opportunity; operate on orthogonal
        channels.
      - MECH-313 (noise floor): orthogonal. MECH-313 lifts softmax
        temperature uniformly; MECH-330 writes to z_harm_a valence.
      - MECH-320 (tonic vigor / ARC-066): vigor is a positive score-side
        addend; boredom is a z_harm_a write on the valence channel.
        Both create action pressure via different architectural layers.
      - MECH-320 w_passive / ARC-068 (opportunity cost): opportunity
        cost is score-side cost on no-op trajectories; boredom is
        valence-side. Distinct layers, compound in action pressure.
      - SD-037 (broadcast override / orexin): recruited by sustained
        harm + drive escalation (high-arousal). MECH-330 routes through
        z_harm_a at low arousal (van Tilburg 2017 low-arousal signature).
        Different arousal corners of agent state space.

    HYBRID-naming convention: title carries both REE-internal name
    (idle_aversion_acute_restlessness_accumulator) and literature
    anchors (engagement-rate-keyed / z_harm_a / AIC analog / Wilson
    2014 self-shock / Danckert 2018 AIC-deactivation).

    Open Q-claim at substrate-landing time: what is the configurable
    relationship between H_acute integration window and episode length?
    If episodes are short (< 100 steps), H_acute must be episode-
    spanning; if long, H_acute can be intra-episode. The calibration
    should track a biological ~minutes timescale, translating to REE's
    episode / step duration.
  location: docs/architecture/non_deficit_action_drives.md

- id: MECH-331
  title: "idle_aversion_chronic_anhedonic_flatness_substrate (slow-integrating frontostriatal effort-allocation substrate at the chronic timescale ~sessions/episode-blocks. Second child mechanism for ARC-067. Seeded by sustained MECH-330 acute-restlessness activation without discharge. Implements the apathy archetype (Husain-Roiser 2018 frontostriatal effort-allocation failure) as a slow-integrating complement to MECH-330's acute restlessness pole. Preserved hedonic experience (MECH-295 liking-channel intact); impaired effort-to-act mobilisation. HPA-axis substrate context (Ulrich-Lai & Herman 2009 chronic understimulation / loss-of-control signature). Ablation predicts reduced action-density in long-horizon episodes even when MECH-330 acute restlessness is firing -- the chronic substrate damps the effort-mobilisation pathway below what the acute channel alone can drive.)"
  claim_type: mechanism_hypothesis
  subject: action.idle_aversion_chronic_anhedonic_flatness
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-05-16"
  depends_on:
    - ARC-067     # parent architectural commitment
    - MECH-330    # acute restlessness accumulator -- seeds chronic substrate
    - SD-011      # affective harm stream (z_harm_a) -- shared routing target
  functional_restatement: >
    MECH-331 names the chronic timescale child mechanism for ARC-067.
    It represents a slow-integrating modulation of the effort-to-act
    mobilisation pathway that accumulates when sustained MECH-330
    boredom activation is not resolved over session-scale time windows.

    Architecture (from ARC-067 lit-pull R2 + R5 verdicts):

    1. SEED SIGNAL FROM MECH-330:
         mech330_seed_t = I[boredom_t >= b_chronic_threshold
                            for >= N_episodes without discharge]
       Binary or soft-threshold signal indicating "sustained boredom
       without resolution." Activates the chronic substrate only after
       MECH-330 has been firing without producing engagement discharge
       for a session-scale window.

    2. SLOW FRONTOSTRIATAL EFFORT-ALLOCATION INTEGRATOR:
         fatigue_t = ewma_window(mech330_seed_t, half_life=H_chronic)
       H_chronic >> H_acute (MECH-330). H_chronic covers session
       timescale (many episodes, across-episode persistent state).
       fatigue_t represents effort-mobilisation impairment analogous
       to frontostriatal effort-allocation failure in apathy
       (Husain & Roiser 2018).

    3. MODULATION OF z_harm_a WRITE GATE:
         z_harm_a_gain = max(gain_floor, 1 - w_fatigue * fatigue_t)
       fatigue_t scales DOWN the z_harm_a write amplitude from
       MECH-330. This is the mechanistic signature of apathy: the
       acute boredom-aversive signal is present but the effort-
       mobilisation pathway is attenuated. The agent experiences
       engagement-failure (MECH-330 fires) but cannot convert that
       aversive into action at the expected amplitude. gain_floor
       (configurable, > 0) prevents complete elimination -- consistent
       with apathy being a matter of degree.

    APATHY ARCHETYPE (R5 verdict): MECH-331 implements the apathy
    clinical archetype: preserved capacity (E1/E2/E3 functional),
    preserved hedonic experience (MECH-295 intact), impaired effort-
    to-act mobilisation. NOT anhedonia (MECH-295 failure), NOT hedonic
    flatness (z_harm_a absence). The substrate distinction matters
    for future psychiatric_failure_modes.md cross-reference.

    CROSS-TALK ASYMMETRY: MECH-330 seeds MECH-331 but MECH-331 does
    not gate MECH-330's accumulation. An apathetic agent still
    accumulates boredom (MECH-330 fires), it just cannot convert it
    into action at full amplitude.

    RECOVERY: fatigue_t decays at H_chronic once mech330_seed_t drops.
    Recovery requires extended sustained engagement discharge. Slow
    H_chronic decay = correspondingly slow recovery, consistent with
    clinical apathy remission timescales.

    Falsifiable prediction: long-horizon episode, no-target no-harm
    full-energy. ARM_0 ARC-067-OFF: action_density = noise floor.
    ARM_1 MECH-330-ON only (acute): action_density increases after
    boredom accumulation onset. ARM_2 MECH-330+331-ON (chronic-seeded
    after N episodes): action_density is LOWER than ARM_1 at
    equivalent episodes -- chronic substrate attenuates acute boredom-
    to-action conversion. Discriminative: attenuation should be
    specific to action-mobilisation pathway (z_harm_a write gain),
    not to E3.select() trajectory scoring itself (E3 intact).
  evidence_quality_note: >
    Registered 2026-05-16 from ARC-067 lit-pull synthesis
    (evidence/literature/targeted_review_arc_067_boredom/synthesis.md,
    2026-05-10, 8 entries).

    R2 timescale-split verdict: BIOLOGY SUPPORTS THE SPLIT at 0.72
    confidence. Acute and chronic substrates are partially dissociable
    in real biology (different lesion patterns, different effort-cost-
    benefit profiles per Husain-Roiser; different diurnal-cortisol
    signatures per Ulrich-Lai). Split execution was a governance call
    deferred until Pull 3 (ARC-068) landed. Pull 3 landed 2026-05-10;
    governance call made 2026-05-16: two-child split approved,
    MECH-330 acute / MECH-331 chronic, with explicit one-way cross-
    talk seeding specification.

    R5 clinical-anchor verdict: ARC-067 ablation maps onto APATHY
    (Husain-Roiser 2018 frontostriatal effort-allocation failure), NOT
    anhedonia (MECH-295 liking-channel). MECH-331 is the architectural
    substrate of that apathy mapping. Anchor only -- pathology mapping
    deferred to future psychiatric_failure_modes.md governance pass.

    Lit anchors (2 of 8 in pull relevant to chronic pole):
      - Husain & Roiser 2018 (Nat Rev Neurosci, PMID 29946107) --
        transdiagnostic apathy/anhedonia; frontostriatal effort-
        allocation substrate of apathy; dissociation from anhedonia.
        supports 0.78.
      - Ulrich-Lai & Herman 2009 (Nat Rev Neurosci, PMID 19543232) --
        HPA-axis chronic-stress substrate context; chronic
        understimulation / low-control signature. mixed 0.55
        (substrate context, not direct boredom evidence).

    Implementation is more preliminary than MECH-330. The effort-
    mobilisation-pathway modulation architecture (z_harm_a write-gain
    scaled by fatigue_t) is the most parsimonious interpretation of
    the Husain-Roiser apathy substrate but has not been validated
    experimentally. H_chronic integration window is a configurable
    parameter; calibration depends on the episode/session timescale
    of the validation environment.

    IMPLEMENTATION NOTE: MECH-331 introduces across-episode state
    (fatigue_t must persist across episode boundaries). This contrasts
    with MECH-330 (within-episode accumulation). MECH-331 requires a
    session-level state buffer in REEAgent, analogous to how MECH-295
    carries persistent preference state. Infrastructure requirement
    deferred to substrate-landing time.

    Aggregate chronic-pole lit_conf expected 0.62-0.68 (2-paper
    cohort; lower confidence than MECH-330's 0.74-0.78; implementation
    is correspondingly more speculative at registration time).
  notes: >
    Second child mechanism for ARC-067. Companion: MECH-330 (acute
    restlessness accumulator). Cross-talk: one-way seeding signal
    (MECH-330 sustained activation -> MECH-331 substrate seeding);
    MECH-331 does not gate MECH-330 accumulation.

    PSYCHIATRIC CROSS-REFERENCE (future psychiatric_failure_modes.md):
    MECH-331 is the primary architectural anchor for the apathy
    archetype. Intended entry: ARC-067 (MECH-330+331) -- healthy
    engagement (both near-zero) vs acute boredom-aversive (MECH-330
    firing, MECH-331 normal) vs apathy (MECH-330 firing, MECH-331
    attenuating) vs abulia / catatonic flatness (deep MECH-331-
    attenuation axis). Requires explicit distinction from anhedonia
    (MECH-295 failure, different substrate) in any cross-reference.

    Open Q-claims at substrate-landing time:
      - Session-boundary persistence model for fatigue_t: (a) reset
        at episode end (loses chronic accumulation); (b) persist to
        disk/checkpoint between sessions (adds infrastructure);
        (c) within-session approximation with long H_chronic. Option
        (c) most tractable for V3 experiments; choice affects whether
        MECH-331 can model session-to-session apathy trajectory.
      - Bidirectional coupling: is there a MECH-331 -> MECH-330
        feedback loop (e.g., chronic fatigue suppresses the
        engagement-rate estimator's threshold)? Defer to validation.

    HYBRID-naming convention: title carries both REE-internal name
    (idle_aversion_chronic_anhedonic_flatness_substrate) and
    literature anchors (frontostriatal / Husain-Roiser apathy /
    HPA-axis / effort-allocation failure).
  location: docs/architecture/non_deficit_action_drives.md

- id: MECH-332
  title: "Nociceptive attenuation on z_harm_s is implemented by two mechanistically dissociable parallel pathways: (1) per-step efference-copy comparator (MECH-256/SD-029, spinal/cortical substrate, triggered by a_actual) and (2) commitment-gated PAG/RVM precision suppression (SD-021, PAG/RVM substrate, triggered by committed-trajectory state). Their substrates and triggers are independent; their attenuation effects are additive."
  claim_type: mechanism_hypothesis
  subject: harm_stream.nociceptive_attenuation_dual_substrate_dissociation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-332@2026-05-19T21:27:25Z"
      as_of: 2026-05-19
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-05-17"
  depends_on:
    - MECH-256    # efference-copy comparator (pathway 1)
    - SD-021      # commitment-gated PAG/RVM suppression (pathway 2)
    - SD-011      # dual nociceptive streams (z_harm_s must exist as separate stream)
    - SD-029      # concrete V3 instantiation of MECH-256 on z_harm_s
  functional_restatement: >
    During self-generated harm, z_harm_s attenuation is produced by two
    independent mechanisms operating at different anatomical levels with
    different triggers. Understanding that they are dissociable has
    consequences for V3 experiment interpretation: a failed MECH-256
    substrate does NOT imply failed SD-021, and vice versa.

    PATHWAY 1 -- PER-STEP EFFERENCE-COPY COMPARATOR (MECH-256 / SD-029):
      residual = z_harm_s_observed - E2_harm_s(z_harm_s_{t-1}, a_actual)
    Substrate: spinal dorsal horn interneurons receiving corticospinal
    collateral corollary discharge, and/or somatosensory cortex (S1/insula)
    forward-model subtraction. Trigger: each action step a_actual.
    Attenuation is proportional to how accurately E2_harm_s predicted the
    nociceptive consequence of the action. NOT implemented by PAG/RVM
    (De Preter & Heinricher 2024 establishes PAG/RVM drives behavioral-state
    gating, not per-step motor-prediction cancellation).

    PATHWAY 2 -- COMMITMENT-GATED PAG/RVM PRECISION SUPPRESSION (SD-021):
      harm_s_gain = f(operating_mode, drive_level) < 1.0 when committed
    Substrate: pgACC -> PAG -> RVM descending inhibitory circuit.
    Trigger: E3 commitment state (beta_gate elevated, trajectory through
    expected harm). Attenuation is uniform across all harm steps during
    commitment, regardless of E2_harm_s accuracy. Implemented as contextual
    behavioral-state gating (ON/OFF cell firing modulated by opioid tone
    and ACC/pgACC motivational input).

    DISSOCIATION PREDICTIONS:
    D1. E2_harm_s ablation (MECH-256 removed): SD-021 pathway should still
      attenuate z_harm_s during committed action. Prediction: a committed
      agent with ablated E2_harm_s shows equal or near-equal z_harm_s
      attenuation to intact agent during committed traversal of expected
      harm (SD-021 operates independently).
    D2. SD-021 OFF (descending_mod_factor = 1.0): MECH-256 pathway still
      attenuates z_harm_s for self-generated harm vs. externally-imposed
      equivalent in a non-committed agent. The ~40% self/other attenuation
      ratio (Lalouni 2020) should persist without SD-021.
    D3. Both active (committed agent, self-generated movement through harm):
      combined attenuation is greater than either pathway alone.

    CLINICAL MAPPING:
    D1 failure = phantom-limb-class pain (efference-copy mechanism absent;
    no residual suppression of expected harm). D2 failure = impaired
    stress-induced analgesia / commitment courage deficit (PAG/RVM pathway
    down; SD-021 absent; agent over-registers expected harm during committed
    trajectory). These are distinct failure signatures.
  evidence_quality_note: >
    Registered 2026-05-17 from self_attribution:GAP-4 lit-pull synthesis.
    Evidence for the dissociation is indirect:
    (1) De Preter & Heinricher 2024 (Trends Neurosci, PMID 38749825)
        establishes PAG/RVM as behavioral-state gating (NOT efference-copy).
        Filed against MECH-256 + SD-029 (targeted_review_connectome_mech_256
        entry 2026-05-17_mech_256_pag_rvm_descending_modulation_depreter2024).
        Also cross-filed against SD-021 (targeted_review_sd_021 entry
        2026-05-17_sd_021_pag_rvm_descending_modulation_depreter2024).
    (2) Lalouni 2020 (Pain, already in SD-029 corpus): 40% self/other
        threshold shift -- behavioural evidence for pathway 1 on nociceptive
        streams.
    (3) SD-021 corpus (8 entries, lit_conf=0.852): PAG/RVM commitment-gated
        analgesia established.
    No V3 dissociation experiment exists yet. Requires: (a) E2_harm_s
    ablation arm in an SD-021-enabled run; (b) SD-021-OFF arm in a
    self/other nociception comparison run. Both blocked until full substrate
    stack lands (MECH-256 + SD-021 jointly requiring committed-mode
    substrate -- see commitment_closure_plan.md GAP-11).

    [2026-08-03 governance, V3-EXQ-878, confirmed failure_autopsy_V3-EXQ-878_2026-08-03, first
    experiment against this claim]: FAIL via self-route substrate_not_ready_requeue
    (non_degenerate=false) -- E3's commitment gate (BetaGate/MECH-090) never elevated during eval in
    either AIC-active arm across all 3 seeds (n_committed_steps=0 vs floor=8), so Pathway 2's
    claim-intrinsic trigger (sustained committed threat-approach) never had a chance to fire.
    non_contributory/standard (not substrate_ceiling -- MECH-090's commitment gate is independently
    well-supported elsewhere; this is a precondition-scheduling gap in this driver's own curriculum).
    Route: /queue-experiment with a curriculum/env config that reliably elevates the commitment gate
    before scoring Pathway 2.

    [2026-08-08 governance, V3-EXQ-878a, confirmed failure_autopsy_V3-EXQ-878a_2026-08-08]:
    18-cell budget sweep (3 schedules x 2 arms x 3 seeds, up to 3x the 878 budget) -- EVERY cell
    reports n_committed_steps=0, running_variance_mean_fresh pinned at EXACTLY precision_init
    (0.5) in every cell, never trending toward the 0.40 commit_threshold even at the largest
    budget. Budget is NOT the lever. non_contributory/substrate_conditional.
    routing=governance-note-only (no new substrate_queue action this cycle). FLAG (not acted on
    this cycle, surfaced for a future ceiling review): this is the 4th independent experiment
    across ~4 months (EXQ-325, EXQ-321, EXQ-878, EXQ-878a) reconfirming the identical unrecovered
    commitment-gate blocker without ever being formally substrate_ceiling-tagged -- the autopsy's
    own words: "in spirit this should be treated as brake territory even though the mechanical
    count is 0" (the re-derive brake's R1-R3 convention only counts substrate_ceiling hits, and
    none of the 4 were stamped that way). If a 5th same-mechanism budget-based re-queue is ever
    proposed, route it through /failure-autopsy for a ceiling read rather than queuing directly --
    the next move should target the gate/threshold mechanism itself (running_variance_mean_fresh's
    convergence dynamics), not another budget bump. SD-032c's "newly unblocked" status from a prior
    round also needs reconciliation against this run's fresh finding before anyone acts on it
    (see SD-032c's own note).

- id: INV-074
  title: "Plasticity crystallization necessity: any model-building agent whose internal
    scoring is dominated by a high-variance predictive pathway will converge to monostrategy
    under Hebbian-equivalent learning unless a time-bounded plasticity asymmetry allows
    competing diversity circuits to establish competitive weight before winner-take-all
    dynamics close off the option space; without this crystallization window, behavioral
    diversity is permanently suppressed regardless of diversity-signal magnitude."
  claim_type: invariant
  invariant_type: universal
  subject: learning.developmental_plasticity_crystallization_necessity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-655_2026-06-13
      as_of: 2026-06-13
      verdict: non_contributory/substrate_ceiling
  pending_retest_after_substrate: false   # 2026-06-19: cleared. The V3 610 crystallization-necessity cascade was terminated by USER DECISION 2026-06-13 (substrate_ceiling, accepted on OD-critical-period biology lit_conf 0.918; no re-queue, no substrate_queue entry -- see governance_note_2026_06_13). There is no pending V3 retest. Any future test is V4-scoped and owned by the plasticity_neuromodulation_v4 plan (scope_claims includes INV-074; OPENING-side ACh/PV/BDNF gain gates; owner_exq null, decision-to-build gate PLW-1 BLOCKED), NOT a pending_retest_after_substrate loop on the V3 substrate.
  registered_utc: "2026-05-17"
  governance_note_2026_06_13: >
    PM cycle (gov 2026-06-13T20:04Z): V3-EXQ-655 (7th necessity-test iteration; the
    pre-registered route-(a) task-distribution-shift redesign superseding the 610f noise-only
    phase-3) reclassified unknown->non_contributory + substrate_ceiling for all 5 cohort claims
    (INV-074/MECH-313/MECH-333/MECH-334/MECH-341) per confirmed failure_autopsy_V3-EXQ-655_2026-06-13.
    The stripped REINFORCE control did NOT collapse entropy (D2 0.065<0.10) and crystallization
    EWC fired but D1~0 (sign-inconsistent) with ARM_4 bit-identical to ARM_0 -- the decisive
    substrate-incapacity confirmation, NOT a falsification. USER DECISION (2026-06-13): STOP the
    610 necessity-test cascade; accept INV-074 substrate_ceiling on its OD-critical-period
    biological grounding (lit_conf 0.918); no re-queue, no substrate_queue entry.
  depends_on:
    - INV-075    # parent: signal-structure temporal decoupling. INV-074 is the COMPETITIVE special case (decay mechanism = active dominant-pathway overwriting; resolution = crystallization/lock arm)
    - INV-073    # developmental bootstrapping necessity (exploration epoch)
    - ARC-065    # diversity stack -- the circuit that must crystallize
    - MECH-120   # sleep SWY downscaling -- the consolidation mechanism
    - MECH-165   # replay diversity -- required for crystallized distribution to be maintained
  what_would_answer: |
    STATUS -- READ THIS FIRST: NO V3 RETEST IS CURRENTLY SANCTIONED. Nine
    consecutive V3 attempts all reclassified non_contributory (harness-level
    confounds or confounded negative controls). User STOPPED the cascade
    2026-06-13; GOV-CEIL-1 (2026-07-09) reaffirmed keep-park; V4-deferred,
    gated on PLW-1 (decision-to-build), still BLOCKED as of 2026-08-07. This
    is a design record for a FUTURE V4 test, not a solicitation to queue one
    now.

    NON-DEGENERACY PRECONDITION (unmet -- all four needed at once, no prior
    attempt achieved more than one or two): (1) a genuinely trained policy
    under real WTA pressure, (2) crystallization applied to heads that have
    already differentiated, (3) a negative control not confounded by
    always-on diversity floors, (4) sustained post-crystallization
    destabilising pressure.

    CONFIRMING: D2 (control-arm collapse) >= +0.10 AND D1 (crystallized-vs-
    control preservation) sign-consistent >= +0.10 across >=2/3 seeds.

    FALSIFYING: D2 >= +0.10 (test is non-vacuous, control genuinely
    collapses) BUT D1 does not clear the floor or is sign-inconsistent -- the
    pre-registered substrate-ceiling fork already named in the 610-series
    routing notes.

    (MECH-439, registered 2026-06-18, shares EXQ-571's evidentiary root but
    asks a different question -- per-decision selection-authority, not
    learning-time plasticity. A future V4 test should check ARC-107's status
    first, as a side-effect resolution there may change what needs
    controlling for -- but MECH-439 resolving is not a precondition for
    INV-074's own test.

    Digested 2026-08-07, session cool-torvalds-a82359, thought-digestion
    wave 11.)
  lit_conf: 0.82
  literature_anchors:
    - "Fagiolini & Hensch (2000) Nature 404:183 -- GAD65-KO mice never open CP spontaneously;
       single benzodiazepine injection forces opening at any age; E/I threshold is causal gate,
       not merely correlative. conf 0.88."
    - "Clothiaux, Bear & Cooper (1991) J Neurophysiol 66:1785 -- BCM theory validated against
       kitten visual cortex; dominant pathway reaches stable attractor, minority pathway decays;
       sliding threshold provides meta-stabilization only if it tracks activity faster than
       competitive dynamics. conf 0.85."
    - "Achille, Rovere & Soatto (2019) ICLR arXiv:1711.08856 -- critical periods in ANNs;
       Phase 1 (high Fisher Information) to Phase 2 (Fisher collapse) transition; deficit
       incurred during Phase 1 is permanent; Fisher Information tracks window closure in real
       time. conf 0.90."
    - "Hubel & Wiesel (1970) J Physiol 206:419 -- 3-4 days monocular deprivation at peak CP
       produces near-complete loss of deprived-eye representation; competitive imbalance (not
       absolute activity level) is the driver -- binocular zone affected, monocular zone not.
       conf 0.90."
    - "Shaffery, Lopez & Roffwarg (2012) Neurosci Lett 521:77 -- REM sleep closes critical
       periods via BDNF/PV maturation cascade; REM deprivation reopens pathologically juvenile
       high-plasticity state where WTA continues indefinitely; BDNF infusion reverses reopening.
       conf 0.78."
    - "Oksenberg, Shaffery & Roffwarg (1996) Brain Res Dev Brain Res 95:169 -- REM deprivation
       ACCELERATES WTA during monocular deprivation (amplified LGN cell-size disparity in both
       directions); sleep is prophylactic against WTA, not merely neutral; threshold effect.
       conf 0.86."
    - "O'Dell (2025) Hippocampus DOI:10.1002/hipo.70043 -- beta-AR driven heterosynaptic
       depression: dominant pathway actively depresses competitors (not merely passively
       outcompetes); self-reinforcing dominance has active suppression component. conf 0.72."
  notes: >
    INV-074 identifies the structural reason why warm-start initializations and
    infant-curriculum phases fail to produce lasting behavioral diversity benefit
    in the current substrate: there is no mechanism to reduce F's plasticity
    dominance after diversity circuits have been seeded, so the dominant scoring
    pathway continues to overwrite diversity representations at the same rate as
    any other configuration.

    V3 experimental evidence (2026-05-16):
    - EXQ-571 (E3 score variance decomposition): F accounts for 88-89% of E3
      scoring variance across all arms. All diversity mechanisms (MECH-313, MECH-314,
      MECH-320, MECH-260) show bias_fraction = 0.000 at E3 scoring.
    - EXQ-569 (matched-entropy sweep, 6 arms): selected_action_entropy identical
      (~0.496) regardless of which diversity mechanism is active.
    - Implication: diversity is expressed in candidates (E2 rollout diversity confirmed,
      EXQ-570) but immediately overwritten at E3 scoring by F dominance. This is the
      substrate signature of a system with no critical period closure.

    BIOLOGICAL GROUNDING (lit pull 2026-05-17):
    The canonical case is ocular dominance plasticity. During the critical period,
    E/I balance enables competitive plasticity. After closure (PNN formation, Lynx1
    upregulation, NgR1/myelination), synaptic configurations are structurally locked.
    The monocular deprivation experiment is the direct analog of EXQ-569: one dominant
    input captures cortical representation when the plasticity window never closes around
    the competing pathway. Crucially, the dominant pathway does not merely outcompete --
    it actively depresses competitors via heterosynaptic mechanisms (O'Dell 2025), and
    the suppressed pathway cannot recover without active Hebbian re-potentiation under
    reversed-activity conditions (Gu, Bear & Singer 1989, NMDA-dependence of recovery).

    ZEBRAFISH SPECIFICITY: The zebrafish tectum conserves GABA maturation and BDNF-
    linked plasticity but PNNs and NgR1 myelination are not confirmed in zebrafish
    tectum. A model calibrated to zebrafish biology may therefore have inherently weaker
    closure mechanisms, consistent with the persistent monostrategy pattern observed.

    MECH-120 and MECH-165 are necessary but not sufficient: they prevent monostrategy
    convergence during sleep but cannot crystallize diversity if waking scoring dynamics
    are already winner-take-all dominated. The crystallization window must precede full
    F-dominance, not attempt to remediate after the fact. See Aton et al. 2013 (PNAS):
    waking dynamics set gate 1 (consolidation instructions); if gate 1 is corrupted by
    WTA, sleep cannot reconstruct diverse signals at gate 2.
  evidence_quality_note: >
    2026-06-05 (governance, failure_autopsy_V3-EXQ-610e CONFIRMED): V3-EXQ-610e is the FIRST behaviorally-live
    crystallization-necessity run -- the harness no-op that explained 543h/i/k/l + 610a/b/c/d is RESOLVED (policy
    trained 0.410->0.491, expansion stepped 1249 params/499 steps, ewc_penalty in loss 0.268, policy_trained_nonuniform=true).
    It STILL cannot discriminate (d2_control_shows_collapse=FALSE delta -0.0105, d1=FALSE delta -0.0002) because the
    negative control is CONFOUNDED: MECH-313 noise floor (0.3) + MECH-341 E3 score-diversity + the policy entropy bonus
    (0.02) are always-on in BOTH arms, so the trained policy under an entropy-maximizing objective never collapses and
    crystallization has nothing to preserve. NOT a falsification -- INV-074 is a biology-faithful universal invariant
    (OD critical period, lit_conf 0.82); the V3 substrate merely carries redundant diversity floors biology lacks.
    non_contributory; epistemic_category measurement_test_design_defect (manifest, not substrate_ceiling -- confounded
    control, redesign not substrate enrichment); pending_retest_after_substrate retained. Routing: /queue-experiment
    V3-EXQ-610f true-negative control (entropy_bonus=0, noise floor off, E3 diversity off in ARM_0) + entropy_bonus_weight
    sweep to map the collapse threshold. 9th non_contributory crystallization-arm result, FIRST with the harness fixed --
    cause relocated from harness-no-op to confounded-control.

    2026-06-03 (governance, failure_autopsy_V3-EXQ-610c): V3-EXQ-610c (crystallization-necessity retest, supersedes 610b) re-tagged non_contributory + measurement_test_design_defect (manifest-level) + pending_retest_after_substrate. Root cause is HARNESS-level, NOT env: the experiment never trains the gated policy (policy_optimizer.step never called; REINFORCE explicitly passed), crystallize() freezes never-trained heads, ewc_penalty never added to loss -> crystallization is a behavioral no-op and the predicted WTA monostrategy collapse is never instantiated (D1 -0.013, D2 +0.047 below 0.10 floor; both arms near-uniform). 7th non_contributory crystallization-arm result (543h/i/k/l + 610a/b/c). INV-074 (universal invariant, lit_conf 0.82) NOT weakened; epistemic_category substrate_ceiling unchanged. Routed to /queue-experiment redesign V3-EXQ-610d (train the policy, step expansion_parameters, add ewc_penalty; pre-registered substrate-ceiling fork where a trained-policy control that still does not collapse strengthens MECH-341/MECH-313, not weakens INV-074).
    Registered 2026-05-17. V3 experimental support via EXQ-569/571 (indirect --
    confirms F-dominance pattern; does not directly test the invariant's causal claim).
    Lit pull completed 2026-05-17 (4 subagents). EXQ-573 (bias-scale sweep) provides
    first falsification opportunity for Q-052; EXQ-591 (infant curriculum vs flat)
    provides second. Both results will inform whether the invariant's structural claim
    is correct or whether magnitude scaling suffices.

    2026-05-18 governance (confirmed failure_autopsy_V3-EXQ-543h):
    V3-EXQ-543h does NOT weaken INV-074 (correcting the manifest's
    branch-(b) per_claim weakens -> non_contributory). Crystallization
    froze head_0/head_1/discriminator, but the gated policy was inert
    before the closure fired, so the lock mechanism was never
    exercised -- missing prerequisite (functional differentiated
    GatedPolicy = MECH-333/INV-074), not claim pressure. The 543f/
    543g/543h cluster is non_contributory and marked superseded by
    V3-EXQ-543i; 543i (the differential-heads substrate-fix
    validation) is FLAGGED FOR /failure-autopsy and carries no
    evidence_direction this cycle. epistemic_category
    substrate_ceiling; pending_retest_after_substrate.

    2026-05-19 governance (failure_autopsy_543i_2026-05-19, confirmed):
    Both V3-EXQ-543i runs non_contributory. Byte-identical runs landed
    opposite basins (06:37Z branch-c all-escape; 19:10Z branch-e
    all-collapse); basin selection nondeterministic, NOT controlled by
    use_differential_heads. INV-074 (universal invariant, lit_conf 0.82)
    not weakened -- crystallization froze a basin-dependent policy,
    mechanism never exercised; missing prerequisite. Manifest corrected:
    evidence_direction -> non_contributory all claims.
    pending_retest_after_substrate.

    2026-05-27 governance (failure_autopsy_V3-EXQ-543l_2026-05-27,
    confirmed): INV-074=non_contributory per autopsy verdict. V3-EXQ-543l
    crystallization fired at Phase-3 on a gated policy where head_0/head_1
    never differentiated (diff_on_escape=false, all 4 diff-ON gated arms
    3/3 inert at escalated floor=0.5 / aux=0.3). The PNN/Lynx1/NgR1-analog
    plasticity-asymmetry lock mechanism cannot be evaluated against a
    head pair that never separated -- missing prerequisite (functional
    differentiated GatedPolicy via ARC-062 + GAP-C/D), not claim pressure.
    pending_retest_after_substrate through V3-EXQ-598b (GAP-C/D
    behavioural validation). epistemic_category=substrate_ceiling
    (unchanged). INV-074 is the fourth member of a multi-experiment lineage
    (543h/i/k/l) where every closure-arm result is non_contributory until
    ARC-062 produces a differentiated gated policy at Phase-3 boundary.

    [2026-06-01 governance: V3-EXQ-610b non_contributory + substrate_ceiling + pending_retest; V3-EXQ-610a
    superseded]: V3-EXQ-610b (crystallization-necessity pair, supersedes 610a) FAILed with the identical signature
    to 610a: D2 control did NOT collapse (d2_delta -0.0057). Per the script's own grid and confirmed
    failure_autopsy_V3-EXQ-610a_2026-06-01, D2 FAIL = non_contributory; with the control never degrading, D1
    (-0.036) carries no information against crystallization-necessity. Diagnosis: CausalGridWorldV2 supplies no
    post-Phase-3 destabilising pressure (env/test-bed ceiling). pending_retest_after_substrate=true;
    substrate_queue amend (test_bed_enrichment_crystallization_necessity, priority 3). 610a marked superseded_by
    610b.

    [2026-06-01 substrate implementation IGW-20260601-023]: test_bed_enrichment_crystallization_necessity
    substrate item implemented. infant_curriculum.InfantCurriculumScheduler Phase 3 env_kwargs now include
    destabilizing environmental dynamics: multi_source_dynamics_enabled (SD-047: weather_field + transient_events
    + background_drift with n_drift_sources=2, intensity_scale=0.8), interoceptive_noise_enabled (SD-048:
    autonomic + sensitisation + fatigue at scale=0.6), and accelerated env_drift (interval=3 vs default 5,
    prob=0.5 vs default 0.3). These dynamics force continued forward-model adaptation post-Phase-3. Prediction:
    control arm (no crystallization) will show entropy collapse (D2 delta >= +0.10) under this pressure;
    crystallized arm (MECH-333/334) will preserve Phase-2 diversity. Smoke test PASS: ree-v3/experiments/
    test_infant_curriculum_phase3.py verifies Phase-3-specific kwargs.

    [2026-06-03 IGW-20260603-032 retest queued]: V3-EXQ-610d queued (supersedes 610c; post-Phase-3-enrichment
    retest). 2-arm discriminative (control vs test), 3 seeds, 2500 episodes. Claims: INV-074, MECH-333, MECH-334.
    pending_retest_after_substrate cleared pending V3-EXQ-610d completion.

    [2026-06-03 governance: V3-EXQ-610d non_contributory + measurement_test_design_defect (confirmed
    failure_autopsy_V3-EXQ-610d_2026-06-03)]: V3-EXQ-610d (crystallization-necessity retest, supersedes 610c)
    FAILed D1 (-0.013) and D2 (+0.047, below the 0.10 floor) with both arms near-uniform (entropy 1.06-1.12 of
    1.609 max). Root cause IDENTICAL to 610c and HARNESS-level, not env: the gated policy is never trained
    (requires_grad=False, "pass # Omit policy training"; no policy_optimizer.step()), crystallize() freezes
    never-trained heads, the rebuilt expansion optimizer is never stepped, and ewc_penalty() is never added to
    the loss, so crystallization is a behavioral no-op and INV-074's predicted WTA monostrategy collapse is
    never instantiated -- D1/D2 non-discriminative by construction. The IGW-023 Phase-3 destabilising pressure
    DID partially work (control collapse rose from ~0 in 610b to +0.047) but cannot collapse an untrained
    policy. PROCESS FINDING: the 610c autopsy prescribed train-policy + step-expansion-params + add-ewc_penalty
    for 610d; 610d implemented none (silent prescription drop, near-copy of 610c). 8th non_contributory
    crystallization-arm result (543h/i/k/l + 610a/b/c/d): a functional, trained, WTA-prone policy has never been
    placed under the crystallization mechanism. INV-074 (universal invariant, lit_conf 0.82) NOT weakened, no
    claim falsified; epistemic_category substrate_ceiling unchanged; pending_retest_after_substrate retained.
    Routed to /queue-experiment redesign V3-EXQ-610e (real REINFORCE + stepped policy_optimizer, stepped
    expansion_parameters() post-crystallization, residue_field.ewc_penalty() summed into the loss), with a
    pre-registered substrate-ceiling fork (a trained-policy control that still does not collapse strengthens
    MECH-341/MECH-313, not weakens INV-074) and a mandatory pre-run assertion that the three fixes are present
    so the prescription is not dropped a third time.
  epistemic_category: substrate_ceiling
  ceiling_decision: deferred
  ceiling_routing_note: "GOV-CEIL-1 keep-parked (NOT demoted) 2026-07-09: flagged with 8 substrate_ceiling hits, but this is a UNIVERSAL invariant with strong biological grounding (lit_conf 0.918, OD critical-period biology) already deliberately accepted as substrate_ceiling by user decision 2026-06-13 (see governance_note_2026_06_13) and V4-deferred (owned by plasticity_neuromodulation_v4; PLW-1 decision-to-build gate). pending_retest_after_substrate is already false -- there is no pending V3 retest. Demotion to a co-equal inert-mechanism null would contradict the well-grounded universal necessity; parked instead. Any future test is V4-scoped. [enum-comment migrated to field 2026-07-18] ceiling_decision: GOV-CEIL-1 keep-park 2026-07-09 (formalizes the 2026-06-13 user acceptance)"

- id: MECH-333
  title: "Critical period open phase: the dominant scoring pathway's effective update
    magnitude must be suppressed (or diversity circuit signal weight amplified to
    competitive parity) during a time-bounded plasticity-open window, preventing
    winner-take-all capture before diversity circuits achieve viable weight; REE analog
    of E/I balance shift and GABA maturation that opens biological critical periods."
  claim_type: mechanism_hypothesis
  subject: learning.developmental_critical_period_open_phase
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-655_2026-06-13
      as_of: 2026-06-13
      verdict: non_contributory/substrate_ceiling
  epistemic_category: substrate_conditional
  governance_2026_06_10: >
    epistemic_category set substrate_conditional (IGW experiment-surfacing
    suppression, not a promote/demote). MECH-333's core open-phase gating
    mechanism (pre-window F-gradient attenuation / PV-analog inhibitory-precision
    competitive gating) is planned-but-unbuilt: only option (E) plastic-channel
    injection landed; the V3-EXQ-610a..610e crystallization-necessity runs are all
    non_contributory (test-bed + harness defects, the open-phase mechanism never
    exercised). This is V4-or-late-V3 plasticity-window/critical-period substrate,
    so the right response is to wait for the upstream substrate, not to surface
    EXP-0033 as ready /queue-experiment work. status unchanged (candidate);
    claim_type unchanged.
  governance_2026_06_10b: >
    implementation_phase reassigned v3 -> v4 (V4 roadmap wave). The core
    open-phase gating mechanism is planned-but-unbuilt and explicitly
    V4-or-late-V3 (see governance_2026_06_10 + the plasticity_neuromodulation_v4
    roadmap plan, which owns the ACh/PV/BDNF opening-side substrate this depends
    on). phase-label-follows-dependency: a claim whose enabling substrate is V4
    is v4. v3_pending stays true (V4 pattern); epistemic_category stays
    substrate_conditional; status unchanged (candidate).
  governance_2026_06_11: >
    implementation_phase reassigned v4 -> v3 (user decision, governance cycle
    2026-06-11 #2), reversing governance_2026_06_10b. Rationale: MECH-333's
    enabling substrate is now treated as V3-tractable via its active V3
    dependencies -- ARC-065 (diversity stack, 569/591/660 lineage) and ARC-074
    (Phase-0 reward-free babbling epoch, the InfantCurriculumScheduler Phase
    0->1 curriculum line being worked under 591c/667). The open-phase
    critical-period gating can ride on the V3 curriculum/diversity substrate
    rather than waiting for the full V4 ACh/PV/BDNF neuromodulator substrate.
    v3_pending stays true and epistemic_category stays substrate_conditional
    (open-phase F-gradient-attenuation gating still planned-but-unbuilt, so the
    claim remains held + IGW-suppressed until that substrate lands); status
    unchanged (candidate). Recommendation reverts held_v4_by_architectural_
    commitment -> hold_pending_v3_substrate.
  pending_retest_after_substrate: true
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-05-17"
  depends_on:
    - INV-074    # the invariant motivating this mechanism
    - ARC-065    # diversity stack -- the circuit that must be seeded during the window
    - ARC-074    # Phase 0 reward-free babbling epoch (precondition)
    - MECH-120   # sleep SWY -- operates in parallel but does not substitute for open-phase gating
  lit_conf: 0.76
  literature_anchors:
    - "Fagiolini & Hensch (2000) Nature 404:183 -- PV+ interneuron maturation threshold is the
       causal gate for CP opening; competition should not begin until the inhibitory precision
       mechanism is online (benzodiazepine can substitute for missing GABA maturation). conf 0.88."
    - "Deidda et al. (2015) Nature Neurosci 18:87 -- depolarizing GABA -> BDNF -> PV maturation
       cascade; mode switch from permissive (exploratory, depolarizing GABA) to competitive
       (hyperpolarizing GABA) precedes the E/I balance threshold. conf 0.83."
    - "Huang et al. (1999) Cell 98:739 -- BDNF precociously opens AND closes CP in transgenic
       mice; BDNF is the molecular accelerator of PV maturation and CP timing; experience-
       independent if BDNF is supplied exogenously. conf 0.85."
    - "Nikishin et al. (2022) ICML arXiv:2205.07802 -- primacy bias fix: periodic reset of last
       N layers (preserving replay buffer) re-opens F's plasticity window; most directly validated
       ML technique for this exact failure mode in RL systems. conf 0.87."
    - "Achille, Rovere & Soatto (2019) ICLR arXiv:1711.08856 -- Fisher Information as diagnostic
       metric; Phase 1->Phase 2 transition detectable in real time; cyclic LR partially re-enters
       high-plasticity dynamics; cross-channel joint predictive loss (Kleinman 2023 CVPR) protects
       multi-source integration from critical period failure. conf 0.90."
    - "Kirkpatrick et al. (2017) PNAS 114:3521 -- EWC Fisher-weighted weight protection applied
       preemptively to diversity parameters prevents F gradient from overriding them during the
       open window; inverse of normal EWC use case. conf 0.84."
  functional_restatement: >
    During the plasticity-open window, training dynamics must enforce a temporary
    inversion of the normal operating ratio: diversity circuits receive full update weight
    while F's gradient contribution to E3 scoring parameters is attenuated. This prevents
    the forward model from capturing scoring variance before diversity circuits have
    established stable attractor basins.

    BIOLOGICAL TRIGGER (lit pull 2026-05-17):
    The biological open-phase trigger is PV+ interneuron maturation reaching a functional
    E/I threshold (Fagiolini & Hensch 2000). The cascade: depolarizing GABA -> BDNF ->
    PV maturation -> E/I threshold crossed -> competitive plasticity begins. This means
    competition should not start until an inhibitory normalization mechanism is online --
    in REE terms, competitive E3 selection should not be active until the precision-
    weighting architecture (z_beta, harm stream) is functional.

    IMPLEMENTATION OPTIONS (to be resolved by Q-052 + experiment):
    (A) Learning rate scheduling: reduce F's effective LR during open window, restore at
        closure. Simple; may be insufficient if F dominance is structural.
    (B) Magnitude rescaling: increase diversity bias_scale during open window, ramp down
        at closure. EXQ-573 tests a static version; MECH-333 requires the temporal version.
    (C) Gradient blocking: prevent F's gradient from updating E3 scoring parameters
        during the open window. Strongest isolation; highest implementation complexity.
    (D) PV-analog penalty: a penalty term scaling with F's current scoring dominance level
        (biological analog: PV interneurons providing feedback inhibition proportional to
        dominant excitatory drive -- the soft-WTA mechanism). Most biologically faithful.
    (E) Plasticity injection (Nikishin 2023 NeurIPS): additive fresh layer providing a
        plastic gradient channel without destroying F's consolidated knowledge.

    The open-phase mechanism must be paired with a closure mechanism (MECH-334) or the
    plasticity asymmetry never resolves and the system remains in a permanently tilted
    training regime.
  evidence_quality_note: >
    2026-06-05 (governance, failure_autopsy_V3-EXQ-610e CONFIRMED): V3-EXQ-610e (first harness-fixed crystallization run -- policy trained 0.410->0.491, expansion stepped 1249 params, ewc_penalty in loss 0.268) STILL non_contributory: d2_control_shows_collapse=FALSE because the negative control is CONFOUNDED (MECH-313 noise floor + MECH-341 E3 diversity + policy entropy bonus 0.02 always-on in BOTH arms), so the MECH-333 open-phase mechanism is never exercised -- D1/D2 non-discriminative by construction, not falsification. epistemic_category measurement_test_design_defect (manifest); pending_retest_after_substrate retained. Routing: /queue-experiment V3-EXQ-610f true-negative control (entropy_bonus=0, noise floor off, E3 diversity off in ARM_0) + entropy_bonus sweep.
    2026-06-03 (governance, failure_autopsy_V3-EXQ-610c): V3-EXQ-610c re-tagged non_contributory + measurement_test_design_defect (manifest) + pending_retest_after_substrate. Crystallization is a behavioral no-op in this harness (policy never trained; crystallize() freezes untrained heads; ewc_penalty never in loss), so the open-phase/closure mechanism is never exercised -- D1/D2 non-discriminative by construction. Not a falsification. Routed to /queue-experiment V3-EXQ-610d (trained-policy redesign).
    Registered 2026-05-17. No V3 implementation exists; MECH-333 is a design
    prescription derived from the EXQ-569/571 audit and the 2026-05-17 lit pull.
    Implementation gated on Q-052 resolution (magnitude vs structure). Lit pull
    completed 2026-05-17 -- biological analog (PV soft-WTA via feedback inhibition
    proportional to dominant drive) identified as the most faithful computational
    target.

    [2026-06-01 governance: V3-EXQ-610b non_contributory + substrate_ceiling + pending_retest; V3-EXQ-610a
    superseded]: V3-EXQ-610b (crystallization-necessity pair, supersedes 610a) FAILed with the identical signature
    to 610a: D2 control did NOT collapse (d2_delta -0.0057). Per the script's own grid and confirmed
    failure_autopsy_V3-EXQ-610a_2026-06-01, D2 FAIL = non_contributory; with the control never degrading, D1
    (-0.036) carries no information against crystallization-necessity. Diagnosis: CausalGridWorldV2 supplies no
    post-Phase-3 destabilising pressure (env/test-bed ceiling). pending_retest_after_substrate=true;
    substrate_queue amend (test_bed_enrichment_crystallization_necessity, priority 3). 610a marked superseded_by
    610b.

    [2026-06-01 substrate implementation IGW-20260601-023]: test_bed_enrichment_crystallization_necessity
    substrate item implemented. infant_curriculum.InfantCurriculumScheduler Phase 3 env_kwargs now include
    destabilizing environmental dynamics: multi_source_dynamics_enabled (SD-047: weather_field + transient_events
    + background_drift with n_drift_sources=2, intensity_scale=0.8), interoceptive_noise_enabled (SD-048:
    autonomic + sensitisation + fatigue at scale=0.6), and accelerated env_drift (interval=3 vs default 5,
    prob=0.5 vs default 0.3). These dynamics force continued forward-model adaptation post-Phase-3. Prediction:
    control arm (no crystallization) will show entropy collapse (D2 delta >= +0.10) under this pressure;
    crystallized arm (MECH-333/334) will preserve Phase-2 diversity. Smoke test PASS: ree-v3/experiments/
    test_infant_curriculum_phase3.py verifies Phase-3-specific kwargs. pending_retest_after_substrate retained
    pending V3-EXQ retest with new Phase-3 pressure.

    [2026-06-03 governance: V3-EXQ-610d non_contributory + measurement_test_design_defect (confirmed
    failure_autopsy_V3-EXQ-610d_2026-06-03)]: V3-EXQ-610d (supersedes 610c) reproduced 610c's HARNESS no-op
    verbatim: the gated policy is never trained (no policy_optimizer.step()), crystallize() freezes never-trained
    heads, expansion optimizer never stepped, ewc_penalty() never added to the loss -> the open-phase/closure
    mechanism is never exercised and D1/D2 are non-discriminative by construction (D1 -0.013, D2 +0.047 below the
    0.10 floor; both arms near-uniform entropy 1.06-1.12). The 610c autopsy's three prescribed fixes were
    silently dropped in the 610d redesign (process finding). MECH-333 (v3_pending, substrate_ceiling) NOT
    weakened. Routed to /queue-experiment V3-EXQ-610e (trained-policy redesign + stepped expansion_parameters +
    ewc_penalty in loss + pre-run assertion). pending_retest_after_substrate retained.
  implementation_note: >
    PARTIAL implementation 2026-05-17 -- option (E) plasticity injection
    (Nikishin 2023 NeurIPS) only. ree-v3 GatedPolicy.crystallize() adds a
    fresh plastic expansion channel at the infant-curriculum Phase 2->3
    transition (forward = frozen_gated(x) + expansion(x.detach())); config
    REEConfig.crystallize_at_phase3 (default False, bit-identical OFF). The
    pre-window F-attenuation half of MECH-333 (ARC-075 requirement 1:
    suppress F's gradient contribution during Phases 0-1) is NOT yet
    implemented -- this landing is the plastic channel that the MECH-334
    closure presupposes. v3_pending RETAINED (experiment evidence pending
    via V3-EXQ-543h). See docs/architecture/critical_period_crystallization.md.
    2026-05-18 addendum: V3-EXQ-543h is non_contributory (failure autopsy
    failure_autopsy_V3-EXQ-543h_2026-05-18) -- its gated policy was inert
    (cross-machine common attractor), so crystallize() froze an already-
    collapsed policy and MECH-333's differentiation could not be exercised.
    The ARC-062 GatedPolicy differential-heads robustness fix (implement-
    substrate 2026-05-18, GatedPolicyConfig.use_differential_heads, ree-v3/
    ree_core/policy/gated_policy.py) makes head differentiation a structural
    non-equilibrium so the plastic-channel closure can be tested on a
    functional policy. Experiment evidence now pending via V3-EXQ-543i
    (supersedes 543g + 543h). crystallize() freezes (base,delta,
    discriminator) under the differential reparameterization -- write-protect
    semantics unchanged. v3_pending RETAINED; no confidence/flag change.
  what_would_answer: |
    SUBSTRATE PRECONDITION: only option (E) plasticity injection (the
    post-window closure/plastic-channel half) is implemented
    (GatedPolicy.crystallize(), 2026-05-17). The pre-window
    F-ATTENUATION half (suppressing F's gradient contribution during the
    open phase -- ARC-075 requirement 1) is NOT yet implemented; this is
    the actual mechanism under test and it does not exist yet.
    NON-DEGENERACY PRECONDITION (already established by 5 consecutive
    non_contributory harness failures -- 610a/b/c/d/e, all
    measurement_test_design_defect or substrate_ceiling): requires (a) a
    genuinely trained gated policy (610c/610d's harness never actually
    trained it -- crystallize() froze never-trained heads, ewc_penalty
    never entered the loss) and (b) a true negative control with noise
    floor / E3 diversity / entropy bonus OFF in the D2 arm (610e's
    control was confounded by MECH-313/MECH-341/entropy-bonus always-on
    in BOTH arms, so D2 never showed the collapse a valid negative
    control requires). A run reproducing any of these five defects is
    NOT a fresh test of the mechanism.
    CONFIRMING signature: once the pre-window F-attenuation is built and
    a genuinely-trained policy is tested with a valid (collapsing) D2
    negative control under Phase-3 destabilising pressure (SD-047/SD-048
    multi-source dynamics + accelerated env_drift, already implemented
    2026-06-01 IGW-20260601-023): the D2 control arm (no
    crystallization/no open-phase gating) shows entropy COLLAPSE (delta
    >= +0.10) under post-Phase-3 pressure, while the D1 arm (MECH-333
    open-phase gating + MECH-334 closure) PRESERVES Phase-2 diversity.
    FALSIFYING: even with a genuine training regime, a valid collapsing
    negative control, and the pre-window F-attenuation actually built,
    D1 (open-phase-gated) shows the SAME entropy collapse as D2
    (ungated) under Phase-3 pressure -- the open-phase mechanism buys
    nothing over ordinary training, falsifying the "temporary inversion
    of the normal operating ratio" framing. The implementation-option
    question (Q-052: which of learning-rate scheduling / magnitude
    rescaling / gradient blocking / PV-analog penalty / plasticity
    injection is the right computational form) is a SEPARATE open
    question this wwa does not resolve.

- id: MECH-334
  title: "Critical period closure / crystallization: once diversity circuits establish
    competitive weight during the MECH-333 open window, a complementary closure mechanism
    reduces plasticity for the crystallized diversity distribution while restoring full
    dominant-pathway update scale; without closure, diversity representations remain
    equally vulnerable to gradient-driven overwriting throughout all subsequent training."
  claim_type: mechanism_hypothesis
  subject: learning.developmental_critical_period_closure_crystallization
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-655_2026-06-13
      as_of: 2026-06-13
      verdict: non_contributory/substrate_ceiling
  pending_retest_after_substrate: true
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-05-17"
  ceiling_decision: deferred
  ceiling_routing_note: "2026-06-19 substrate-ceiling-lifted triage (verdict b deliberate-defer): the test_bed_enrichment destabilising-pressure env was built but is NOT the operative blocker -- crystallization-necessity cannot be exercised until ARC-062/MECH-333 produce a functionally-DIFFERENTIATED GatedPolicy at the Phase-3 boundary (same GAP-B diversity-generation front as MECH-309, in flight as 654g). Also fix the recurring harness no-op (assert policy trained + ewc_penalty in loss + true-negative ARM_0: entropy_bonus=0/noise-floor-off/E3-diversity-off) burned across 610c-f/655 before any retest."
  what_would_answer: |
    STATUS -- READ THIS FIRST (currency check, 2026-08-08 thought-digestion): the
    prerequisite named in this claim's own 2026-06-19 ceiling_routing_note
    ("ARC-062/MECH-333 produce a functionally-DIFFERENTIATED GatedPolicy at the
    Phase-3 boundary... in flight as 654g") has NOT landed. 654g ran 2026-06-19
    and returned non_contributory (branch b): a fully matured, differentiated
    rule-apprehension channel reached the E3 accumulator with genuine authority
    but failed to convert to committed-action diversity (paired committed-class
    entropy lift 0/3 seeds clearing 0.05 nats) -- this is the shared MECH-439
    F-dominance conversion ceiling, NOT the differentiated GatedPolicy MECH-334
    needs. The blocker has DEEPENED since: 21 GAP-B behavioural-falsifier
    autopsies (654g/h/i/j, 485h/485m, 714, 719a, 851, 859, 863) through
    2026-08-02 show the identical C1-holds/C2-fails signature every time,
    and MECH-439 itself was demoted 2026-07-09 (GOV-CEIL-1) to a contested
    candidate after 10 non-discriminative hits, with the active front moved to
    the still-incomplete ARC-107 basal-ganglia constitution (MECH-448/449 built
    and validated; ARC-108 dopamine-afferent and MECH-450 recurrent-settling
    still missing). No V3 retest of MECH-334 is currently non-vacuous.

    NON-DEGENERACY PRECONDITION -- two independent layers, BOTH required:

    (1) HARNESS-LEVEL (established by 543f-l / 610a-e / 655, ~10 non_contributory
    hits, all either a harness no-op or a confounded control -- never a
    falsification): (a) the gated policy must be GENUINELY TRAINED under real
    WTA pressure before crystallize() fires (610c/610d froze never-trained
    heads); (b) ewc_penalty() must actually be summed into the OPTIMIZED LOSS
    when closure is on (610c/610d: never entered the loss; 610e: fixed, 0.268
    present); (c) ARM_0 must be a TRUE negative control -- entropy_bonus=0,
    MECH-313 noise floor OFF, MECH-341/E3 diversity OFF (610e's control was
    confounded by all three always-on in BOTH arms, so D2 never collapsed --
    non-discriminative by construction, not a falsification); (d) sustained
    post-crystallization destabilising pressure must exercise the closed
    system (already built and validated 2026-06-01, IGW-20260601-023:
    multi_source_dynamics_enabled, interoceptive_noise_enabled, accelerated
    env_drift -- no further test-bed work owed). These four are now
    STRUCTURALLY ENFORCED, not just documented: ree-v3/experiments/_metrics.py
    (commit 10dee76, 2026-06-19) provides shared, smoke-tested guards
    (assert_policy_trained, assert_ewc_penalty_live, assert_ewc_term_in_loss,
    assert_true_negative_arm0, assert_d2_control_collapsed) raising a distinct
    HarnessGuardError (19/19 tests). A future retest MUST call these guards
    rather than re-deriving ad hoc checks -- 610c's three prescribed fixes were
    silently dropped in the 610d redesign once already.

    (2) SUBSTRATE-LEVEL (the actual current blocker; NOT yet met -- see STATUS
    above): a functionally-differentiated GatedPolicy at the Phase-3 boundary,
    meaning genuine committed-action-CLASS diversity (C2 clears >=0.05 nats on
    >=2/3 seeds per ARC-062's own pre-registered discriminator), not merely a
    modulatory/rule-bias signal reaching the E3 accumulator. This sits behind
    the still-unresolved MECH-439/ARC-107 committed-action-diversity conversion
    ceiling.

    CONFIRMING signature: once BOTH preconditions clear -- a crystallized arm
    (MECH-333 open-phase gating + MECH-334 closure, applied to heads that have
    ACTUALLY differentiated) PRESERVES the established diversity distribution
    (D1: crystallized-vs-control entropy delta sign-consistent, >=0.10 floor,
    on >=2/3 seeds) under sustained post-Phase-3 destabilising pressure, WHILE
    the true-negative control arm shows entropy COLLAPSE (D2 delta >= +0.10 on
    >=2/3 seeds) under the identical pressure -- the three-brake active-locking
    signature.

    FALSIFYING signature: with both preconditions met and D2 confirming the
    test is non-vacuous, the crystallized arm shows the SAME entropy collapse
    as the control (D1 does not clear the 0.10 floor, or is sign-inconsistent,
    on >=2/3 seeds) -- once a differentiated policy actually exists, EWC
    write-protect buys nothing over ordinary continued training. This would
    falsify the "active locking, not passive reduction" PNN/Lynx1/NgR1 framing
    specifically, and would motivate revisiting the single-EWC-lock
    implementation (the redundant three-brake architecture was deliberately
    deferred) as the point of failure before abandoning the framing.

    SEPARATE, NOT RESOLVED HERE: which of the three biological locks
    (PNN-physical, Lynx1-cholinergic, NgR1-myelination) is load-bearing is not
    addressed by this design -- the current landing collapses all three to one
    EWC analogue. A CONFIRMING result supports write-protect-in-general, not
    the specific multi-lock architecture.

    GOVERNANCE FLAG (2026-08-08 digestion pass, not resolved here): sibling
    claim INV-074 (same 543/610/655 evidentiary lineage) had its
    pending_retest_after_substrate explicitly flipped to false on 2026-06-19,
    with an explicit note that the entire V3 610-cascade was terminated by
    user decision 2026-06-13 -- "no re-queue, no substrate_queue entry",
    future test V4-scoped only, gated on PLW-1. MECH-334 (and MECH-333) never
    received the equivalent correction and still frame a V3 retest as pending
    on the ARC-062/MECH-333 GAP-B front -- a front that has since deepened
    (21 autopsies, MECH-439 demoted) rather than resolved. This divergent
    routing may be intentional (MECH-334 targets the specific closure
    mechanism, testable once differentiation exists, vs. INV-074's
    necessity-in-general question that was abandoned on V3) -- but worth a
    governance sanity-check on whether MECH-334/MECH-333 should also get a
    V3-cascade-stopped correction.
  depends_on:
    - MECH-333   # open phase (must precede closure)
    - INV-074    # the invariant motivating this mechanism
    - MECH-120   # sleep SWY -- operates post-closure to prevent slow reconvergence
    - MECH-165   # replay diversity -- maintains crystallized distribution during sleep
  lit_conf: 0.78
  literature_anchors:
    - "Pizzorusso et al. (2002) Science 298:1248 -- ChABC degrades PNNs and fully reactivates
       adult OD plasticity; closure is ACTIVE LOCKING not passive reduction; multi-mechanism
       (physical constraint + Otx2 trap + Sema3A scaffold); requires ongoing molecular
       maintenance. conf 0.92."
    - "Morishita et al. (2010) Science 330:1238 -- Lynx1 is an active cholinergic brake
       upregulated at CP closure; Lynx1-KO mice retain juvenile-like OD plasticity into
       adulthood; closure requires active molecular braking. conf 0.88."
    - "Stephany et al. (2014) J Neurosci 34:11631 + Stephany et al. (2016) Neuroscientist
       22:653 -- NgR1/myelination as layer-4-specific interface lock; parallel and partially
       redundant with PNNs; genetic KO prevents CP closure; multi-lock architecture. conf 0.85."
    - "Shaffery, Lopez & Roffwarg (2012) Neurosci Lett 521:77 -- REM sleep closes critical
       periods via BDNF->PV maturation pathway; the closure signal is sleep-gated (translational
       program requiring mTOR activation during sleep, Seibt 2012 Curr Biol); MECH-334 needs
       a sleep-phase trigger for execution. conf 0.78."
    - "Aton et al. (2013) PNAS 110:3059 -- two-stage gate: waking MD depresses FS interneurons
       (gate 1 = consolidation instruction); NREM slow-wave phase-locking executes OD shift
       (gate 2); gate 1 must encode correct diversity before gate 2 crystallizes it -- corrupted
       waking dynamics prevent sleep from crystallizing the right representation. conf 0.87."
    - "Kirkpatrick et al. (2017) PNAS 114:3521 -- EWC write-protect analog: Fisher-weighted
       quadratic penalty applied to diversity parameters after MECH-333 window closes; protects
       crystallized diversity distribution against subsequent F gradient overwriting. conf 0.84."
    - "Dohare et al. (2024) Nature DOI:10.1038/s41586-024-07711-7 -- loss of plasticity in
       deep continual learning; continual backprop (selective reinitialization of lowest-utility
       units) restores plasticity; CReLU activation prevents unit death; relevant for maintaining
       diversity parameter plasticity post-closure. conf 0.85."
  functional_restatement: >
    The closure mechanism converts the established diversity distribution from a plastic
    to a semi-stable state: subsequent gradient updates can refine individual diversity
    circuit weights but cannot wholesale overwrite the diversity distribution.

    BIOLOGICAL ANALOG (lit pull 2026-05-17):
    Three parallel brakes operate redundantly at biological critical period closure:
    (1) PNNs around PV cells (physical constraint + Otx2/Sema3A scaffold -- active,
        not passive; requires ongoing maintenance; ChABC dissolves them and restores
        plasticity in adults even decades post-closure)
    (2) Lynx1 (cholinergic brake actively upregulated at closure; structurally
        homologous to alpha-bungarotoxin; Lynx1-KO restores juvenile-like plasticity)
    (3) NgR1/myelination at layer-4 thalamocortical interface (layer-specific lock;
        partially redundant with PNNs)
    The multi-lock architecture means MECH-334 cannot be implemented as a single
    mechanism -- redundant write-protect pathways targeting different components of
    the diversity circuit are more faithful to the biology.

    TWO-STAGE GATE (Aton et al. 2013):
    Waking dynamics set gate 1 (which circuits are tagged for consolidation);
    NREM slow-wave phase-locking executes the crystallization at gate 2. If waking
    F-dominance has already corrupted gate 1 (diversity circuits are not differentially
    tagged because they never drove action selection), sleep cannot reconstruct diverse
    consolidation signals. MECH-333 must ensure gate 1 receives correct diversity
    signals before MECH-334 closure executes.

    RELATIONSHIP TO SLEEP:
    MECH-120 (SWY downscaling) and MECH-165 (replay diversity) are maintenance
    mechanisms within an open plasticity window -- they are NOT substitutes for
    closure. Shaffery/Roffwarg 2012: REM sleep closes critical periods via BDNF/PV
    pathway; SHY (Tononi/Cirelli) is a separate homeostatic normalizer that operates
    within an open window. Both are required but neither implements crystallization
    by itself.
  evidence_quality_note: >
    2026-06-05 (governance, failure_autopsy_V3-EXQ-610e CONFIRMED): V3-EXQ-610e (first harness-fixed crystallization run -- policy trained 0.410->0.491, expansion stepped 1249 params, ewc_penalty in loss 0.268) STILL non_contributory: d2_control_shows_collapse=FALSE because the negative control is CONFOUNDED (MECH-313 noise floor + MECH-341 E3 diversity + policy entropy bonus 0.02 always-on in BOTH arms), so the MECH-334 closure/crystallization mechanism is never exercised -- D1/D2 non-discriminative by construction, not falsification. epistemic_category substrate_ceiling unchanged on the claim; manifest tagged measurement_test_design_defect; pending_retest_after_substrate retained. Routing: /queue-experiment V3-EXQ-610f true-negative control (entropy_bonus=0, noise floor off, E3 diversity off in ARM_0) + entropy_bonus sweep.
    2026-06-03 (governance, failure_autopsy_V3-EXQ-610c): V3-EXQ-610c re-tagged non_contributory + measurement_test_design_defect (manifest) + pending_retest_after_substrate. Harness defect (policy never trained, ewc_penalty never added to loss) -> crystallization/closure never exercised; D1/D2 non-discriminative by construction. epistemic_category substrate_ceiling unchanged; not a falsification. Routed to /queue-experiment V3-EXQ-610d.
    Registered 2026-05-17. No V3 implementation exists. MECH-334 is logically
    dependent on MECH-333. Lit pull completed 2026-05-17 -- PNN/Lynx1/NgR1 three-
    brake architecture identified as the biological model. Implementation design
    requires resolving MECH-333 first; write-protect approach (EWC applied to
    diversity weights) is the most direct computational translation.

    2026-05-18 governance (confirmed failure_autopsy_V3-EXQ-543h):
    V3-EXQ-543h does NOT weaken MECH-334 (correcting the manifest's
    branch-(b) per_claim weakens -> non_contributory). The
    crystallization closure froze an already-collapsed (inert) gated
    policy, so the PNN/Lynx1/NgR1-analog lock mechanism was never
    exercised -- missing prerequisite (functional differentiated
    GatedPolicy = MECH-333/INV-074), not claim pressure. 543i carried
    D2_xtal_pass=false and xtal_harms_gated_only=true, but with no
    reproduced diff-OFF contrast the crystallization arm remains
    untested. The 543f/543g/543h cluster is non_contributory and
    marked superseded by V3-EXQ-543i; 543i is FLAGGED FOR
    /failure-autopsy and carries no evidence_direction this cycle.
    epistemic_category substrate_ceiling; pending_retest_after_substrate.

    2026-05-19 governance (failure_autopsy_543i_2026-05-19, confirmed):
    Both V3-EXQ-543i runs non_contributory. Byte-identical runs landed
    opposite basins (06:37Z branch-c all-escape; 19:10Z branch-e
    all-collapse); basin selection nondeterministic. MECH-334
    (PNN/Lynx1/NgR1 three-brake closure, lit_conf 0.78) not weakened
    -- crystallization froze a basin-dependent policy, lock mechanism
    never exercised; missing prerequisite (functional differentiated
    GatedPolicy). Manifest corrected: evidence_direction ->
    non_contributory all claims. pending_retest_after_substrate.

    2026-05-27 governance (failure_autopsy_V3-EXQ-543l_2026-05-27,
    confirmed): MECH-334=non_contributory per autopsy verdict. V3-EXQ-543l
    crystallization fired at Phase-3 on a gated policy that never
    differentiated (escalated floor=0.5 / aux=0.3; all 4 diff-ON gated
    arms 3/3 inert; D2_xtal_delta=0.008 essentially zero). The three-brake
    closure mechanism froze an already-collapsed policy -- the
    PNN/Lynx1/NgR1-analog lock cannot be evaluated against heads that
    never separated. Missing prerequisite (functional differentiated
    GatedPolicy via ARC-062 + GAP-C/D), not claim pressure.
    pending_retest_after_substrate through V3-EXQ-598b. Fourth member
    of 543h/i/k/l lineage where every closure-arm result is
    non_contributory until ARC-062 produces a differentiated gated
    policy at Phase-3 boundary. epistemic_category=substrate_ceiling
    (unchanged).

    [2026-06-01 governance: V3-EXQ-610b non_contributory + substrate_ceiling + pending_retest; V3-EXQ-610a
    superseded]: V3-EXQ-610b (crystallization-necessity pair, supersedes 610a) FAILed with the identical signature
    to 610a: D2 control did NOT collapse (d2_delta -0.0057). Per the script's own grid and confirmed
    failure_autopsy_V3-EXQ-610a_2026-06-01, D2 FAIL = non_contributory; with the control never degrading, D1
    (-0.036) carries no information against crystallization-necessity. Diagnosis: CausalGridWorldV2 supplies no
    post-Phase-3 destabilising pressure (env/test-bed ceiling). pending_retest_after_substrate=true;
    substrate_queue amend (test_bed_enrichment_crystallization_necessity, priority 3). 610a marked superseded_by
    610b.

    [2026-06-01 substrate implementation IGW-20260601-023]: test_bed_enrichment_crystallization_necessity
    substrate item implemented. infant_curriculum.InfantCurriculumScheduler Phase 3 env_kwargs now include
    destabilizing environmental dynamics: multi_source_dynamics_enabled (SD-047: weather_field + transient_events
    + background_drift with n_drift_sources=2, intensity_scale=0.8), interoceptive_noise_enabled (SD-048:
    autonomic + sensitisation + fatigue at scale=0.6), and accelerated env_drift (interval=3 vs default 5,
    prob=0.5 vs default 0.3). These dynamics force continued forward-model adaptation post-Phase-3. Prediction:
    control arm (no crystallization) will show entropy collapse (D2 delta >= +0.10) under this pressure;
    crystallized arm (MECH-333/334) will preserve Phase-2 diversity. Smoke test PASS: ree-v3/experiments/
    test_infant_curriculum_phase3.py verifies Phase-3-specific kwargs.

    [2026-06-03 IGW-20260603-032 retest queued]: V3-EXQ-610d queued (supersedes 610c; post-Phase-3-enrichment
    retest). 2-arm discriminative (control vs test), 3 seeds, 2500 episodes. Claims: INV-074, MECH-333, MECH-334.
    pending_retest_after_substrate cleared pending V3-EXQ-610d completion.

    [2026-06-03 governance: V3-EXQ-610d non_contributory + measurement_test_design_defect (confirmed
    failure_autopsy_V3-EXQ-610d_2026-06-03)]: V3-EXQ-610d (supersedes 610c) reproduced 610c's HARNESS no-op
    verbatim: policy never trained, crystallize() freezes never-trained heads, expansion optimizer never stepped,
    ewc_penalty() never added to the loss -> the three-brake closure mechanism is never exercised; D1/D2
    non-discriminative by construction (D1 -0.013, D2 +0.047 below the 0.10 floor; both arms near-uniform). The
    610c autopsy's three prescribed fixes were silently dropped in the 610d redesign (process finding). MECH-334
    (PNN/Lynx1/NgR1 three-brake closure, lit_conf 0.78) NOT weakened. Routed to /queue-experiment V3-EXQ-610e
    (trained-policy redesign + stepped expansion_parameters + ewc_penalty in loss + pre-run assertion).
    pending_retest_after_substrate retained; epistemic_category substrate_ceiling unchanged.
  epistemic_category: substrate_ceiling
  implementation_note: >
    IMPLEMENTED 2026-05-17 (primary closure mechanism). ree-v3:
    GatedPolicy.crystallize() freezes head_0/head_1/discriminator at the
    infant-curriculum Phase 2->3 transition (plasticity-injection closure
    for the policy heads); ResidueField.snapshot_ewc_anchor() +
    ewc_penalty() apply a Fisher-weighted quadratic write-protect anchored
    to the Phase-3 checkpoint (Kirkpatrick 2017), Fisher proxy
    |anchor_w|*active_mask -- NOT a hard freeze, established basins resist
    overwriting in proportion to strength (faithful to the claim text).
    Config REEConfig.crystallize_at_phase3 (default False) +
    ResidueConfig.ewc_enabled/ewc_lambda; from_dims residue_ewc_lambda.
    Multi-lock note: the PNN/Lynx1/NgR1 redundancy is collapsed to a
    single EWC lock in this landing (cost-controlled); redundant locks
    deferred. v3_pending RETAINED -- V3-EXQ-543h superseded
    non_contributory (confirmed autopsy 2026-05-18); validation now
    via V3-EXQ-543i, which is flagged for /failure-autopsy and not yet
    contributory. See docs/architecture/critical_period_crystallization.md.

- id: ARC-075
  title: "The infant curriculum architecture must implement plasticity magnitude
    asymmetry across developmental phases, not only temporal phase scheduling: F's
    effective update weight must be suppressed during the MECH-333 plasticity-open
    window to allow diversity circuits to reach competitive weight; temporal scheduling
    alone (as in current GAP-9/GAP-14 implementation) is insufficient."
  claim_type: architectural_commitment
  subject: development.infant_curriculum_plasticity_magnitude_gating
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  registered_utc: "2026-05-17"
  depends_on:
    - ARC-046    # infant curriculum architecture (phased learning)
    - ARC-074    # Phase 0 reward-free babbling epoch
    - INV-074    # plasticity crystallization necessity
    - MECH-333   # critical period open mechanism
    - MECH-334   # critical period closure mechanism
  lit_conf: 0.74
  literature_anchors:
    - "Bengio et al. (2009) ICML -- curriculum learning accelerates convergence but does NOT
       preserve behavioral diversity without explicit diversity regularizer; temporal scheduling
       alone converges to monostrategy if one strategy suffices across difficulty levels.
       Direct confirmation that GAP-9/GAP-14 phase scheduling is insufficient. conf 0.82."
    - "Aton et al. (2013) PNAS 110:3059 -- two-stage gate: waking phase dynamics determine what
       consolidation instructions sleep receives; temporal phase scheduling without magnitude
       gating produces corrupted gate 1 (waking-phase diversity absent), preventing sleep from
       crystallizing diverse representations at gate 2. conf 0.87."
    - "Kleinman, Achille & Soatto (2023) CVPR arXiv:2210.04643 -- multi-source integration has
       a critical period; cross-channel joint predictive loss during early training immunizes
       against integration failure before it starts; tying F and diversity mechanisms in a shared
       early objective may prevent competitive capture before it occurs. conf 0.83."
    - "Hagewoud et al. (2010) Sleep 33:1465 -- sleep deprivation shifts to rigid monostrategy;
       confirms temporal curriculum without correct waking-phase dynamics AND sleep consolidation
       produces permanent behavioral narrowing. conf 0.82."
  functional_restatement: >
    The current infant substrate plan (GAP-9: 4-phase scheduler, GAP-14: curriculum vs
    flat baseline) schedules transitions between developmental phases but does not gate
    plasticity magnitudes between phases. ARC-075 asserts this is architecturally
    incomplete.

    REQUIRED ADDITIONS TO INFANT CURRICULUM:
    (1) Phase-gated F attenuation: during Phase 0 (babbling) and Phase 1 (early
        learning), F's gradient contribution to E3 scoring is reduced to allow diversity
        circuits to establish competitive weight.
    (2) Closure trigger: criterion-based or time-based trigger ending the attenuated-F
        phase and restoring normal dynamics, crystallizing the diversity distribution.
    (3) Post-closure diversity validation: metric confirming the distribution is stable
        against F-dominated overwriting after closure (analog of monocular deprivation
        test post-critical-period).

    IMPLICATION FOR EXQ-591:
    V3-EXQ-591 (GAP-14 curriculum vs flat baseline) will likely show no benefit from
    the 4-phase curriculum unless F attenuation is also implemented. A null result would
    confirm ARC-075 but not constitute a PASS for developmental hardening hypothesis.
  evidence_quality_note: >
    Registered 2026-05-17. ARC-075 is a design prescription, not a tested claim.
    EXQ-591 provides the first falsification opportunity. Bengio et al. (2009) directly
    confirms the core claim that temporal scheduling alone is insufficient. Lit pull
    completed 2026-05-17.
  implementation_note: >
    PARTIAL 2026-05-17. Requirement (2) closure trigger IMPLEMENTED
    (infant-curriculum Phase 2->3 fires GatedPolicy.crystallize() +
    ResidueField.snapshot_ewc_anchor() via the on_phase3_entry hook);
    requirement (3) post-closure diversity validation is the V3-EXQ-543h
    D2 criterion. Requirement (1) phase-gated F attenuation during Phases
    0-1 is NOT yet implemented (the open-phase half of MECH-333). So the
    "temporal scheduling alone is insufficient" claim is now testable:
    543h adds crystallize_at_phase3 as a 3rd factor on the 543g 2x2.
    v3_pending RETAINED. See docs/architecture/critical_period_crystallization.md.

- id: Q-052
  title: "Is forward-model dominance in E3 scoring a magnitude calibration problem
    solvable by 5-10x diversity bias scaling, or a structural plasticity dominance
    problem requiring phase-specific learning rate asymmetry as per MECH-333/334?"
  claim_type: question
  subject: learning.forward_model_dominance_magnitude_vs_structural_plasticity
  polarity: open_question
  status: open
  live_status:
    reading: open/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Distinguish three readings of F's 88-89% E3-scoring-variance monopoly
    (V3-EXQ-571) via the ARC-065 diversity stack. (A) MAGNITUDE: apply a one-time
    5-10x static bias_scale to MECH-313/314/320 diversity signals; ANSWER (A) if
    the entropy lift PERSISTS stably across extended training with no regression
    -- competitive parity restored without temporal gating, MECH-333/334 not
    required. (B) STRUCTURAL PLASTICITY: ANSWER (B) if entropy lifts then
    regresses under continued training (F re-consolidates and out-competes),
    requiring phase-specific learning-rate asymmetry (MECH-333/334). (C) PERIODIC
    RESET: if (A) fails but a Nikishin-style periodic reset of the last N
    action-selector layers (replay buffer preserved) restores diversity, the
    minimal open-phase fix suffices short of full plasticity scheduling.
    DIAGNOSTIC GATE: first check whether the MECH-313/314/320 diversity signals
    are themselves F-error-dependent (Pathak 2017 ICM self-extinction) -- if so,
    Q-052 resolves to (B) regardless of the magnitude result because the signal
    source decays as F consolidates. HARD PRECONDITION: the diversity signals must
    reach E3 with live, non-zero bias_fraction under the rescale -- on the current
    substrate all diversity bias_fractions are pinned at 0.000, so a magnitude arm
    that cannot move bias_fraction off zero is a vacuous read (the channel is
    silent, not refuted). The committed/behavioural leg is additionally MECH-439
    conversion-ceiling-gated; read at rule_state / scoring-variance upstream of
    the F-dominated commit.
  confidence: 0.0
  epistemic_category: substrate_conditional
  registered_utc: "2026-05-17"
  depends_on:
    - INV-074    # the invariant -- Q-052 asks whether EXQ-573 resolves it
    - MECH-333   # required if Q-052 resolves to structural
    - ARC-065    # diversity stack -- the locus of the problem
  literature_anchors:
    - "Achille, Rovere & Soatto (2019) ICLR arXiv:1711.08856 -- Phase 2 Fisher Information
       lock-in is structural; magnitude scaling in Phase 2 cannot undo Phase 1 bias; leans
       toward hypothesis (B) structural. conf 0.90."
    - "Frenkel & Bear (2004) Neuron 44:917 + Lambo & Turrigiano (2013) J Neurosci 33:8810 --
       biphasic OD plasticity: competitive LTD (days 1-3) then homeostatic scaling (days 3-7);
       EXQ-573 static bias scaling addresses Phase 2 only, not Phase 1 competitive
       restructuring; further supports (B). conf 0.87."
    - "Nikishin et al. (2022) ICML arXiv:2205.07802 -- primacy bias / periodic layer reset:
       a THIRD option beyond (A) magnitude and (B) structure -- re-opens F's plasticity window
       via architectural intervention without requiring full MECH-333/334; decision rule may
       need three branches. conf 0.87."
    - "Pathak et al. (2017) ICML arXiv:1705.05363 -- ICM self-defeat: F-error-based curiosity
       signal collapses to zero as F consolidates; if MECH-313/314/320 diversity signals are
       F-error-dependent, EXQ-573 magnitude scaling is temporary and Q-052 resolves to (B)
       regardless of initial entropy lift. conf 0.85."
    - "Aton et al. (2013) PNAS 110:3059 -- two-stage gate; waking diversity must be correct
       for sleep consolidation to produce diverse output; EXQ-573 PASS does not guarantee
       gate 1 is producing diverse consolidation signals at the correct timescale. conf 0.87."
  short: >
    F dominance (88-89% variance, all diversity bias_fractions = 0): magnitude fix or structural plasticity fix?
  description: >
    EXQ-571 (2026-05-16) established that F accounts for 88-89% of E3 scoring variance
    and all diversity mechanisms have bias_fraction = 0.000. EXQ-573 (queued) tests
    static 5-10x diversity bias scaling across MECH-313/314/320 individually and combined.

    TWO PRIMARY HYPOTHESES:
    (A) MAGNITUDE: F dominance is a calibration artefact -- diversity signals too small
        relative to F's score contribution. A one-time bias_scale increase restores
        competitive parity without temporal gating. EXQ-573 PASS supports this; MECH-333/334
        would not be required as architectural mechanisms.
    (B) STRUCTURAL PLASTICITY: F dominance is self-reinforcing under Hebbian-equivalent
        learning -- F won early, its gradient updates reinforce its dominance, diversity signals
        cannot catch up regardless of static magnitude rescaling. Literature leans toward (B):
        Achille & Soatto Phase 2 lock-in is structural; OD competitive dynamics require active
        re-competition not just homeostatic rescaling (Gu 1989); F-error-based diversity signals
        self-extinguish as F consolidates (Pathak 2017).

    THIRD OPTION (identified by ML lit pull):
    (C) PERIODIC RESET: Nikishin et al. (2022) primacy bias fix -- periodic reset of last
        N layers of action-selector while preserving replay buffer. Less disruptive than full
        MECH-333/334 architecture; directly validated in RL. Would constitute a minimal
        implementation of the open-phase mechanism without requiring full plasticity scheduling.

    DECISION RULE:
    - EXQ-573 PASS (all three acceptance criteria met, diversity stable over extended
      training): Q-052 resolves to (A); MECH-333/334 deprioritized.
    - EXQ-573 FAIL or partial (entropy lifts but regresses under extended training):
      Q-052 resolves to (B); MECH-333/334 or option (C) periodic reset required.
    - EXQ-573 PASS + EXQ-591 FAIL (curriculum still shows no benefit): ambiguous;
      requires further probe distinguishing magnitude fix from developmental crystallization.

    DIAGNOSTIC FLAG: Check whether MECH-313/314/320 diversity signals are F-error-
    dependent (ICM failure mode, Pathak 2017) -- if so, Q-052 resolves to (B) regardless
    of EXQ-573 result, because the diversity signal source will self-extinguish as F
    consolidates further.

- id: INV-075
  title: "Signal-structure temporal decoupling: any signal that is BOTH self-extinguishing
    (its magnitude is coupled to its own success and therefore necessarily fades as what it
    bootstraps succeeds) AND load-bearing (the structure it establishes must persist after
    the signal fades) necessarily requires either a crystallization mechanism (LOCK -- the
    structure is rendered plasticity-resistant) or a handoff mechanism (a durable,
    non-extinguishing signal assumes maintenance of the structure before the establishing
    signal vanishes). Absent both, the structure decays and the acquisition is lost. The
    lock/handoff disjunction is exhaustive: no third resolution exists."
  claim_type: invariant
  invariant_type: universal
  subject: learning.signal_structure_temporal_decoupling
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  registered_utc: "2026-05-17"
  epistemic_category: derivational
  what_would_answer: >
    LEG 1 (DERIVATIONAL, primary): for any signal both self-extinguishing and
    load-bearing, exactly two resolutions prevent decay -- LOCK or HANDOFF. Pathak 2017
    ICM = failure instance (neither arm; curiosity signal decays to zero as F
    consolidates, structure not preserved). Faini 2018 eLife = joint-necessity instance
    (lock alone is reversible by brief deprivation; handoff via ongoing sensory drive is
    ALSO required -- lock and handoff are not independently sufficient escape routes but
    the claim's actual two-arm disjunction). Lyle/Rowland/Dabney 2022 Corollary 1 = formal
    proof-fragment at the RL level (a sparse/non-stationary outcome target collapses the
    feature representation to zero for every state, independent of learning rate, unless
    anchored -- absent a soft lock, the structure provably decays).

    FALSIFYING: a genuine THIRD resolution -- some mechanism that is neither a plasticity
    lock nor a handoff -- that nonetheless prevents structure decay when a signal meeting
    both antecedent conditions fades. Must be a structural novelty, not a relabeling.

    NOT A COUNTEREXAMPLE (scope precision, already stated in this claim's own notes):
    Burda 2019 RND does NOT falsify the invariant -- it makes the novelty signal
    non-self-extinguishing (fixed random target, decoupled from F's success), escaping
    the ANTECEDENT (design option (i): avoid self-extinguishing-ness altogether) rather
    than finding a third way to satisfy the CONSEQUENT once the antecedent holds.

    LEG 2 (SUBSTRATE-EMPIRICAL, secondary, currently NOT testable): via INV-074
    specialization and MECH-333/334 LOCK-arm instances -- does a signal meeting both
    antecedent conditions in REE's actual substrate show structure decay absent both
    lock and handoff, and structure persistence once lock or handoff is engaged? NOT
    currently active: INV-074's V3 necessity-test cascade (9 iterations, most recently
    V3-EXQ-655) was terminated by USER DECISION 2026-06-13 as substrate_ceiling -- the
    substrate cannot currently mount a clean lock-vs-no-lock comparison. MECH-333/334
    inherit the same substrate_conditional/substrate_ceiling readings; V3 retest is
    explicitly out of scope, V4-deferred, gated on PLW-1 (decision-to-build), currently
    BLOCKED. The one substrate result bearing on this leg, V3-EXQ-543g (REE's attempted
    HANDOFF), is reclassified non_contributory -- see the 2026-08-08 correction appended
    to this claim's notes below; do not read the notes' "open design fork for 543h" as
    current. CONFIRMING (leg 2, once testable): decay in a no-lock/no-handoff control arm
    AND preservation in a lock-engaged or handoff-engaged arm, sign-consistent across
    >=2/3 seeds. FALSIFYING (leg 2): structure persists in the no-lock/no-handoff control
    at a rate indistinguishable from the lock/handoff arms.
  specializes_into:
    - INV-074    # the COMPETITIVE special case: decay mechanism = active dominant-pathway
                 # overwriting (Hebbian WTA / heterosynaptic depression); resolution = the
                 # crystallization/lock arm (critical-period closure, MECH-333/334)
  related:
    - INV-073    # developmental bootstrapping -- the exploration epoch is a self-extinguishing
                 # setup signal; INV-075 explains why its acquisitions need lock or handoff
    - MECH-333   # an instance of the LOCK arm (critical-period open phase)
    - MECH-334   # an instance of the LOCK arm (critical-period closure / crystallization)
    - Q-052      # the F-error-dependence question is one instance of "is signal X self-extinguishing"
    - ARC-076    # the personality / commitment-loop-calibration instance of the LOCK arm
                 # (developmental window then crystallization; two-layer L1 endogenous /
                 # L2 environmentally-calibrated). Children: MECH-335, MECH-336.
  lit_conf: 0.80
  literature_anchors:
    - "Pathak et al. (2017) ICML arXiv:1705.05363 -- ICM: F-error-coupled curiosity is the
       canonical self-extinguishing load-bearing signal; collapses to zero as F consolidates;
       demonstrates the failure mode when NEITHER lock nor handoff is present. conf 0.85."
    - "Burda et al. (2019) ICLR arXiv:1810.12894 -- RND: a signal-LEVEL escape -- making the
       novelty signal NOT coupled to F's success (fixed random target) dissolves the antecedent
       entirely; confirms the invariant is correctly scoped to self-extinguishing signals only,
       not all setup signals. conf 0.85."
    - "Deidda et al. (2015) Nature Neurosci 18:87 + Huang et al. (1999) Cell 98:739 --
       depolarizing GABA and the developmental BDNF surge are self-extinguishing by
       construction (transient developmental signals that fade with maturation); biological
       instances of the antecedent. conf 0.84."
    - "Pizzorusso et al. (2002) Science 298:1248 + Morishita et al. (2010) Science 330:1238 --
       PNN / Lynx1 are the biological LOCK arm: structure made plasticity-resistant after the
       transient setup signals have faded. conf 0.90."
    - "Aton et al. (2013) PNAS 110:3059 + use-dependent maintenance of sensory circuits --
       biological HANDOFF arm: after developmental setup signals fade, ongoing sensory drive
       (a durable signal) maintains the now load-bearing circuit; lock and handoff co-occur
       in normal development. conf 0.82."
    - "Kleinman, Achille & Soatto (2023) CVPR arXiv:2210.04643 -- cross-channel joint
       predictive loss is an ML HANDOFF: ties the bootstrapped structure into a durable shared
       objective before the setup signal fades. conf 0.83."
    - "Bengio et al. (2009) ICML -- curriculum learning with NEITHER lock nor handoff converges
       to monostrategy: the failure case at ML level when the antecedent holds and the
       consequent is unmet. conf 0.82."
    - "Faini et al. (2018) eLife 7:e41520 -- HANDOFF-FAILURE / lock-handoff co-dependence:
       PNN (lock) and ongoing visual drive (handoff) are JOINTLY required; PNN-mediated
       stabilization is itself abolished by 2-3d monocular deprivation and PNN removal
       resets the circuit to a juvenile state. Predicts handoff-ALONE (no lock) is
       biologically non-canonical and insufficient -- the durable signal maintains an
       already-LOCKED structure. conf 0.84. [Part B pull; see
       evidence/planning/inv075_handoff_arm_litpull.md F1]"
    - "Lyle, Rowland & Dabney (2022) arXiv:2204.09560 'Capacity Loss in RL' -- the precise
       mechanism of V3-EXQ-543g's handoff failure: Corollary 1 proves a sparse/non-stationary
       outcome target (R^pi -> 0) collapses the feature representation to the zero vector for
       every state, INDEPENDENT of learning rate; InFeR fix = anchor features to initialization
       (= a soft LOCK). Sparse outcome-coupled REINFORCE does not maintain a structure -- it
       provably destroys it. conf 0.86. [Part B; litpull F4 -- explains ARM_3 0.243 < ARM_2
       0.444]"
    - "Yuan et al. (2025) arXiv:2510.04454 -- naive supervised<->RL objective handoff causes
       catastrophic forgetting of the established skill; the empirically working fix is
       selective freezing of skill-critical parameters (a partial LOCK co-located with the
       handoff). Independent ML confirmation that a working handoff is lock-assisted, not
       standalone. conf 0.70. [Part B; litpull F6]"
    - "Sokar et al. (2023) ICML PMLR 202 'Dormant Neuron Phenomenon' -- re-pointing a
       pretrained network at a durable RL objective drives units dormant and erodes the
       established representation absent explicit preservation (ReDo); corroborates that bare
       reward-coupling is a degradation regime, not benign maintenance. conf 0.74. [Part B;
       litpull F5]"
  notes: >
    INV-075 is the parent invariant; INV-074 is its competitive special case. The
    generalization was established by exhaustion (no counterexample exists): if a
    signal is self-extinguishing and the structure it builds is load-bearing, then
    by the time the signal has faded the structure either (a) has been made resistant
    to subsequent plasticity (lock) or (b) is being actively maintained by some other
    durable signal (handoff), or else (c) it decays. There is no fourth case --
    "the signal maintains it" is excluded by self-extinguishing; "it does not need
    to persist" is excluded by load-bearing.

    SCOPE PRECISION (important -- prevents over-claiming):
    INV-075 does NOT assert that all setup signals must be self-extinguishing. It is
    conditional: GIVEN a self-extinguishing AND load-bearing signal, lock-or-handoff
    is necessary. There is a distinct signal-LEVEL design option -- make the signal
    NOT self-extinguishing in the first place (Burda 2019 RND: a novelty signal
    decoupled from F's success does not decay as F learns). That option escapes the
    invariant's antecedent rather than satisfying its consequent. The three exhaustive
    design responses to a load-bearing setup need are therefore:
      (i)  use a non-self-extinguishing signal (avoid the antecedent), OR
      (ii) lock the structure (crystallization), OR
      (iii) hand off to a durable maintaining signal.
    (i) is upstream of INV-075; (ii) and (iii) are the invariant's two-arm consequent.

    RELATION TO REE SUBSTRATE:
    - The infant-curriculum bootstrapping signals (novelty_bonus, residue_scale ramp,
      high early replay) are self-extinguishing by schedule. The gated-policy /
      residue / z_goal structure they establish is load-bearing for later rule
      apprehension. INV-075 says that structure MUST be locked or handed off before
      Phase-3->adult. V3-EXQ-543g attempted a HANDOFF (outcome-coupled REINFORCE so a
      durable return signal maintains the gating) and it failed -- so the open design
      fork for 543h is lock vs. a better handoff vs. both. This is the direct
      operational consequence of INV-075 for the 543 lineage.
    - Q-052's F-error-dependence audit is the test of whether MECH-313/314/320 fall
      under INV-075's antecedent at all (option (i) vs (ii)/(iii)).

    CORRECTION (2026-08-08, thought-digestion currency check): the paragraph above
    describes "the open design fork for 543h" as if pending/future -- this is stale.
    543h already ran, and the whole 543f/543g/543h cluster was reclassified
    non_contributory by a confirmed 2026-05-18 failure autopsy (failure_autopsy_V3-EXQ-543h),
    which also drives ARC-062's status. So 543h is resolved (non_contributory), not an
    open question, as of this digestion pass. This does not change INV-075's own status
    or category -- it only corrects what REE's own most concrete substrate test of the
    claim's consequent currently shows. Original text preserved above for history.
  evidence_quality_note: >
    Registered 2026-05-17. Invariant established by logical exhaustion (user-confirmed:
    no counterexample of a self-extinguishing load-bearing signal that needs neither
    lock nor handoff). Biological and ML anchors drawn from the 2026-05-17 four-agent
    lit pull (already grounds both the antecedent instances and both consequent arms).
    No new lit pull required -- the existing corpus spans the self-extinguishing-signal
    cases (Pathak, Deidda, Huang), the lock arm (Pizzorusso, Morishita), and the
    handoff arm (Aton, Kleinman). INV-074 retained as the competitive specialization;
    its depends_on now points to INV-075 as parent.
- id: ARC-076
  title: "Developmental commitment-loop calibration window with critical-period lock
    (two-layer): the three BG-like cortico-striatal commitment loops (ARC-021 / MECH-069
    / ARC-023) are calibrated during a bounded juvenile/adolescent developmental window
    and then crystallized (an INV-075 LOCK-arm instance), and this locked calibration IS
    personality. The architecture is two-layer: (L1) the basic drive-prior set-points of
    the loops (temperament; the limbic-loop value prior) lock on a largely ENDOGENOUS
    maturational timer, while (L2) a downstream commitment-policy / characteristic-
    adaptation layer is calibrated by the juvenile play/peer environment and consolidated
    at window close. Personality is neither a pure environmental readout nor purely
    endogenous: the environment writes L2; the timer locks L1."
  claim_type: architectural_commitment
  subject: development.commitment_loop_calibration_window_personality
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  registered_utc: "2026-05-17"
  depends_on:
    - INV-075    # parent invariant -- ARC-076 is its personality/commitment-loop LOCK-arm instance
    - ARC-021    # the three BG-like cortico-striatal loops being calibrated
    - MECH-069   # the loops' error channels are incommensurable -> each calibrated separately
    - ARC-023    # the loops run on distinct thalamic timescales -> distinct calibration windows
    - ARC-046    # infant curriculum architecture (the developmental window lives here)
    - ARC-074    # Phase 0 reward-free babbling epoch (the self-extinguishing setup signal)
    - INV-073    # developmental bootstrapping -- play/exploration is the self-extinguishing setup
    - MECH-333   # LOCK arm: critical-period open phase (same crystallization pattern)
    - MECH-334   # LOCK arm: critical-period closure / crystallization (same pattern as 543h)
  lit_conf: 0.70
  literature_anchors:
    - "Pellis, Pellis, Ham & Stark (2023) Neurosci Biobehav Rev 145:105037 -- juvenile play is
       self-extinguishing AND load-bearing: deprivation in the juvenile window yields persistent
       adult socio-cognitive deficit; play trains a transferable arbitration skill, not stored
       motor policies (a commitment-calibration signature). INV-075 antecedent in biology.
       supports, conf 0.74.
       evidence/literature/targeted_review_play_commitment_loop_personality_window/"
    - "Zohar, Zwir, Wang, Cloninger & Anokhin (2018) Dev Psychopathol 31(2):601 -- longitudinal
       (n=752): temperament (drive priors) vs character (peer-calibrated commitment regulation);
       post-14 internalization of the created identity = window-then-LOCK, not perpetual
       maintenance. Clearest human evidence for the INV-075 lock arm in personality.
       supports, conf 0.72."
    - "McCrae, Costa, Ostendorf et al. (2000) J Pers Soc Psychol 78(1):173 -- COUNTERWEIGHT:
       basic-trait stability is largely endogenously maturational, not environmentally written;
       environment shapes 'characteristic adaptations'. Forces the two-layer split (L1 endogenous
       lock / L2 environmentally-calibrated). mixed, conf 0.70."
    - "Miller, Carroll, Armstrong et al. (2024) Brain Behav Immun 117:215 -- a first-decade
       sensitive period persistently calibrates the ventral/limbic corticostriatal (value)
       loop; the loop-level, human leg of the cluster. supports, conf 0.60."
  functional_restatement: >
    REE currently has the three commitment loops (ARC-021 / MECH-069 / ARC-023) and an
    infant curriculum (ARC-046, ARC-074) but NO mechanism that uses the developmental
    window to calibrate the loops' commitment gains and then lock them. ARC-076 asserts
    that mechanism is architecturally required and that its locked output is what REE
    should call personality.

    TWO-LAYER STRUCTURE (forced by the McCrae 2000 counterweight):
      L1 (set-points / temperament): the basic drive-prior gains of the three loops --
         most importantly the limbic-loop value prior (what is worth committing to) --
         lock on a largely endogenous maturational timer. The environment does NOT
         write L1; it is the INV-075 lock arm with an endogenous, schedule-driven
         (not input-driven) trigger.
      L2 (commitment policy / characteristic adaptations / character): how those drive
         priors are gated into actual commitment under context is calibrated by the
         juvenile play/peer environment during the window and consolidated at closure.
         L2 is the environmentally-determined layer.

    INV-075 MAPPING: ARC-076 is an instance of INV-075's LOCK arm, not the handoff arm
    (consistent with the 2026-05-17 handoff-failure lit pull: handoff-alone is not the
    biological pattern for developmentally-established circuits). The lock pattern is
    the same crystallization family as MECH-333/334 / V3-EXQ-543h, applied to the
    commitment loops rather than the gated-policy diversity heads.

    REQUIRED ADDITIONS (V3, design prescription -- not yet implemented):
    (1) a commitment-loop calibration phase in the infant curriculum during which the
        loops' relative gains are shaped by the environment (L2);
    (2) a closure/lock at window end that crystallizes the L2 commitment policy;
    (3) an endogenous (timer/criterion, NOT input-magnitude) lock for the L1 set-points;
    (4) the per-loop window structure (MECH-335) and the maladaptive-lock failure mode
        (MECH-336) as child mechanisms.
  evidence_quality_note: >
    Registered 2026-05-17 from a biology-before-formal-definitions lit pull
    (evidence/literature/targeted_review_play_commitment_loop_personality_window/, 6
    entries; INV-075 literature_confidence 0.837). ARC-076 is a design prescription /
    candidate, not a tested claim. The "environmentally-determined personality" framing
    is deliberately BOUNDED by McCrae 2000 into the two-layer structure -- over-claiming
    environmental determination of L1 set-points would be unsupported. Distinct from
    V3-EXQ-543h / MECH-334 (which locks the gated-policy diversity heads); ARC-076 locks
    the commitment-loop calibration. No experiment queued this session (registration
    only, per scope).
  implementation_prerequisites: >
    HARD ORDERING GATE (user-stated 2026-05-17). ARC-076 / MECH-335 / MECH-336 are NOT
    implementable, and MUST NOT be scheduled, experiment-queued, or treated as actionable
    by any session or governance run, until BOTH of the following are first satisfied --
    in this order:
      (1) INFANT STAGE VALIDATED. The infant curriculum is demonstrated working, not
          merely scheduled: ARC-046 / ARC-074 / ARC-075 resolved (in particular ARC-075's
          plasticity-magnitude gating, since temporal scheduling alone is architecturally
          insufficient), with V3-EXQ-586 (GAP-9) and V3-EXQ-591 (GAP-14 curriculum-vs-
          flat) run and passing. As of registration these are queued, not run.
      (2) BEHAVIOURAL DIVERSITY ACHIEVED THROUGH THE LOCK. At minimum, sustained
          behavioural diversity that survives critical-period closure is demonstrated:
          ARC-065 plus the MECH-333/334 crystallization validated via V3-EXQ-543h PASS
          (its predecessor 543g FAILED; 543h is queued, not run). The commitment-loop
          calibration ARC-076 describes is a strictly harder instance of the same
          lock pattern -- it cannot be built on an unproven lock.
    Rationale: ARC-076's depends_on (ARC-046/ARC-074/MECH-333/334/ARC-065-family) are
    all themselves candidate / v3_pending with their gating experiments unrun, so
    v3_pending alone under-states the constraint. This gate makes the ordering explicit
    so the cluster is not picked up prematurely. Lift this block only when (1) and (2)
    are evidenced.
- id: MECH-335
  title: "Staggered ventral->dorsal commitment-loop calibration windows: the three
    commitment loops do not share a single critical period. Their calibration windows
    are staggered ventral->dorsal -- the limbic/value loop (ventral striatum / OFC)
    opens earliest and is the most environmentally sensitive, the associative loop
    next, the sensorimotor loop last -- with region-specific consolidation latencies (a
    calibration can be induced during a window but only expressed after it closes)."
  claim_type: mechanism_hypothesis
  subject: basal_ganglia.staggered_commitment_loop_calibration_windows
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-05-17"
  depends_on:
    - ARC-076    # parent: the calibration-window-then-lock architecture
    - ARC-021    # the three loops
    - MECH-069   # incommensurable error channels -> separate, non-synchronous calibration
    - ARC-023    # distinct loop timescales -> distinct windows
  lit_conf: 0.63
  literature_anchors:
    - "Bijlsma, Vanderschuren & Wierenga (2023) Cereb Cortex 33(15):9399 -- play deprivation
       P21-P42 reduces prefrontal inhibition region-specifically: the mPFC change manifests
       DURING the window, the OFC change only AFTER it -- direct evidence of staggered windows
       and region-specific consolidation latency. supports, conf 0.66.
       evidence/literature/targeted_review_play_commitment_loop_personality_window/"
    - "Miller, Carroll, Armstrong et al. (2024) Brain Behav Immun 117:215 -- the first-decade
       sensitive period localises to ventral striatum + OFC (the value/limbic loop), supporting
       'ventral/limbic loop earliest and most environmentally sensitive'. supports, conf 0.60."
    - "Pellis, Pellis, Ham & Stark (2023) Neurosci Biobehav Rev 145:105037 -- juvenile-period-
       specific play effects with region-heterogeneous prefrontal trajectories. supports,
       conf 0.74."
  functional_restatement: >
    A model that calibrated all three commitment loops in one shared critical period
    would mispredict the region-specific, time-staggered trajectories observed
    biologically. MECH-335 asserts the windows are ordered ventral->dorsal (value/limbic
    first and most plastic to environment, then associative, then sensorimotor) and that
    consolidation can lag window-close. Implementation consequence: the ARC-076
    calibration phase is not one switch but an ordered schedule of per-loop windows, and
    a post-window readout is required to detect latently-consolidated calibrations
    (analog of the post-critical-period monocular-deprivation test).
  evidence_quality_note: >
    Registered 2026-05-17 (lit pull, same corpus as ARC-076). Candidate / design
    prescription. Bijlsma 2023 is the cleanest analogue but is prefrontal, not measured
    at the striatal loop nodes; the ventral->dorsal ordering is inferred from convergent
    region-specificity, not directly demonstrated at loop level. Child of ARC-076.
- id: MECH-336
  title: "Maladaptive-lock failure mode (personality-pathology analog): adequate
    environmental input does NOT determine the locked calibration value. A stable subset
    of agents locks a maladaptive commitment-loop configuration despite sufficient input
    (lock-onto-bad-calibration), and failure to lock at all yields an unstable / diffuse
    commitment policy. These are the REE substrate of personality-disorder-analog
    failure modes: lock-too-early / lock-onto-bad-value (rigid maladaptive personality)
    and failure-to-lock (identity diffusion)."
  claim_type: mechanism_hypothesis
  subject: development.maladaptive_commitment_lock_failure_mode
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-05-17"
  depends_on:
    - ARC-076    # parent: window-then-lock calibration
    - INV-075    # the lock arm whose failure modes this enumerates
    - MECH-334   # the crystallization mechanism whose mis-timing / mis-targeting this describes
  lit_conf: 0.62
  literature_anchors:
    - "Ham, Iwaniuk & Pellis (2026) Behav Brain Res 508:116198 -- with ABUNDANT play, ~20.4%
       of rats still lock a maladaptive (aggression-prone, under-pruned mPFC) configuration,
       indistinguishable in play input from competent peers -- adequate input does not
       determine the locked value. supports, conf 0.62.
       evidence/literature/targeted_review_play_commitment_loop_personality_window/"
    - "Zohar, Zwir, Wang, Cloninger & Anokhin (2018) Dev Psychopathol 31(2):601 -- character
       maturity = internalization of a created identity; its failure is identity diffusion
       (failure-to-lock). supports, conf 0.72."
    - "McCrae, Costa, Ostendorf et al. (2000) J Pers Soc Psychol 78(1):173 -- bounds the
       determinism: because L1 set-points are largely endogenous, the maladaptive lock is not
       purely an input-deprivation phenomenon. mixed, conf 0.70."
  functional_restatement: >
    A substrate in which adequate environmental input guarantees adaptive calibration is
    falsified by Ham 2026. MECH-336 asserts the locked value is under-determined by
    input: given the same window and the same input, some agents crystallize a
    maladaptive commitment policy. Two clinically-meaningful failure modes follow, both
    INV-075-shaped: (a) lock-onto-bad-calibration / lock-too-early -> rigid maladaptive
    personality; (b) failure-to-lock -> unstable, diffuse commitment policy (identity
    diffusion). This is the REE handle on personality pathology and the falsification
    surface for ARC-076: an experiment must show a stable maladaptive-lock
    subpopulation under adequate input.
  evidence_quality_note: >
    Registered 2026-05-17 (lit pull, same corpus). Candidate. Ham 2026 is a single
    recent primary study (male-only; mechanism of the individual difference
    unresolved). The clinical mapping (personality disorders as lock failure modes) is
    REE's interpretation, flagged as a hypothesis -- NOT a claim that PD etiology is
    settled. Child of ARC-076; the falsification target for the cluster.

- id: ARC-078
  title: "The unresolved-goal bank is a content-addressed cue-addressed retrieval system: the current goal/context is the retrieval cue, the SD-039 payload is the stored trace, and rank_ghost_goals() is cued recall. A correct bank must satisfy three structural constraints -- outshining-gated composite cue, dissociable-additive channels, and a non-closeable recoverability term with an explicit-divergence abandon path."
  claim_type: architecture_hypothesis
  subject: goal.cue_addressed_unresolved_goal_retrieval
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-078@2026-05-19T21:27:25Z"
      as_of: 2026-05-19
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: 2026-05-19
  depends_on:
    - SD-039     # preserved goal payload = the stored retrieval trace
    - MECH-230   # structured z_goal latent = the cue content
    - MECH-292   # ranked bank = the cued-recall step
    - MECH-293   # awake probe = internally-triggered retrieval
  functional_restatement: >
    ARC-078 is the parent architectural framing the retrieval-cue reframe
    establishes (docs/architecture/ghost_goal_search.md Section 0). The
    ghost-goal bank is not, structurally, a priority queue of deferred goals;
    it is a content-addressed cue -> target retrieval system. The current
    z_goal/context is the retrieval cue; the SD-039 payload preserved on an
    inactive anchor is the stored trace; rank_ghost_goals() is cued recall
    (goal_match is the cue-to-trace match score, not a priority weight);
    MECH-293's awake probe is internally-triggered retrieval. ARC-060's
    continuous-field vs discrete-bank split is the recognition-vs-recall
    distinction; ARC-060 specializes into ARC-078.

    A correct cue-addressed bank must satisfy three structural constraints
    (each grounded in the 2026-05-18 combined cue-system pull):

    - C1 (outshining-gated composite cue, MECH-339): the cue is z_goal plus
      a context channel built from the already-preserved SD-039 fields,
      combined by an outshining rule so a strong direct match dominates.
    - C2 (dissociable additive channels): wanting and goal_match are
      anatomically separable channels with independent failure modes; the
      additive ghost_priority already in the substrate is the correct form
      (this constraint is borne by MECH-292; no separate claim).
    - C3 (non-closeable recoverability + explicit-divergence abandon path):
      recoverability must not be inhibition-only nor a cutoff that
      integrates accumulated suppression evidence; rumination/intrusion is
      the predicted equilibrium of an inhibition-only design. The explicit
      abandon path is a deliberate divergence from biology (which has no
      erase operation). The abandon MECHANISM is intentionally NOT
      registered under ARC-078: it is gated behind a dedicated
      goal-disengagement biology-before lit-pull (the disengagement
      literature -- Wrosch/Scheier, Klinger, rumination/OCD as disengagement
      failure -- is well developed; treating "abandon" as purely
      non-biological would be the SD-003-class philosophy-right /
      mechanism-wrong error).
      [RESOLVED 2026-05-19 -> ARC-079 / MECH-340 / Q-053. The
      goal-disengagement pull INVERTED this constraint: persistence is the
      gated control/efficacy operation, disengagement is the ungated
      default, and the SD-039 trace is preserved (no erase) -- which
      MATCHES biology, weakening C3's original "deliberate divergence /
      biology has no erase op" premise. The "abandon path" is reframed as
      the persistence gate not firing -> MECH-292 rank decay -> the entry
      ceases to be re-probed while the trace survives. Whether any explicit
      hard-erase op is needed at all is now the open sub-question of
      Q-053.]

    Falsifiable: an agent whose bank satisfies C1-C3 recovers blocked/
    deferred goals more flexibly than (a) a field-only agent (ARC-060
    prediction) and (b) a bank that violates outshining or uses a
    closeable recoverability cutoff; and the three dissociated failure
    states of C2 are inducible.
  notes: >
    Registered 2026-05-19 from the retrieval-cue reframe of the
    litpull-cue-system synthesis (user-gated). Parent of the cue-system
    cluster; ARC-060 specializes into it (ARC-060.depends_on += ARC-078).
    The abandon-path constraint (C3) was the open structural element here;
    it was RESOLVED 2026-05-19 by the goal-disengagement biology-before
    pull and registered as ARC-079 (the persistence-is-gated /
    disengagement-is-default inversion) + MECH-340 (the persistence/
    efficacy gate) + Q-053 (the open licit-gate-signal question).
    Cross-reference: the abandon path
    is the mirror image of the interrupted-task / Zeigarnik resumption gap
    (resume deferred work <-> retire it); the two should be wired together.
    UPDATE 2026-08-08 (thought-digestion currency check, RESOLVED the same day by
    chip chip-20260808-arc079-mech389-zeigarnik-crosslink): the Zeigarnik gap is
    no longer unregistered -- it was registered as MECH-389 on 2026-06-10 -- and
    the wiring is now done in both directions. Finding: the two halves are
    compatible-but-distinct (different plane, trigger axis and lifecycle stage),
    with one residual gate-authority tension recorded as Q-053 sub-question (c).
    Canonical statement of the reconciliation: MECH-389's notes. Note this does
    NOT reopen C3 -- C3 stays resolved by ARC-079/MECH-340/Q-053 as recorded
    above; MECH-389 is the mirror half (resume), not a competing abandon path. No
    experiment queued; not promoted; status candidate / v3_pending.
    C1 substrate update (2026-05-19): the Constraint-1 child MECH-339
    (composite cue + outshining gate) is now IMPLEMENTED in the ghost-goal
    bank (smallest substrate step: arousal_tag context channel gated by
    goal_match; see MECH-339.implementation_note and the ree-v3/CLAUDE.md
    "MECH-339 C1" entry). C2 remains MECH-292's ratified additive form and
    C3 persistence gate (MECH-340) substrate landed 2026-05-21 (default off;
    appraisal wiring deferred per Q-053). ARC-078 itself stays candidate
    / v3_pending and unpromoted -- one child substrate landing is not
    evidence for the parent architectural claim.
  evidence_quality_note: >
    Registered candidate. Literature support is the 2026-05-18 combined
    cue-system pull in `targeted_review_ghost_goal_search/`: Nakazawa 2002
    + Gelbard-Sagiv 2008 (partial-cue completion + internally-triggered
    reinstatement -> the retrieval framing), Smith & Vela 2001 (outshining
    -> C1), Flagel 2011 + Corbit & Balleine 2005/2011 (channel
    dissociation -> C2), Bouton 2004/2014 + Maren/Phan/Liberzon 2013 +
    Jasinska 2014 (preserve-don't-erase, non-closeable recoverability,
    rumination equilibrium -> C3). lit_conf is a PARALLEL signal only and
    is NOT blended into exp_conf (feedback_lit_exp_decoupled); exp_conf = 0
    (no experiment yet) so this remains candidate / v3_pending. The
    cue-system framing is REE's interpretive synthesis of that biology,
    flagged as an architecture hypothesis, not a settled result.
  literature_anchors:
    - targeted_review_ghost_goal_search
  location: docs/architecture/ghost_goal_search.md
  epistemic_category: substrate_conditional
  what_would_answer: >
    Adjudicated by an EXQ that flips MECH-339 (use_composite_cue_outshining), MECH-292
    (use_mech292_ghost_bank, additive form), and MECH-340 (use_persistence_efficacy_gate) ON
    SIMULTANEOUSLY on one bank configuration, and contrasts flexible recovery of
    blocked/deferred goals across three arms: (i) the C1-C3-satisfying bank, (ii) a field-only
    agent (ARC-060 continuous-field prediction, no discrete bank), and (iii) a bank that
    violates C1 (no outshining gate / flat additive context term) or substitutes a closeable
    recoverability cutoff for C3's non-closeable term.

    CONFIRMING: (i) recovers blocked/deferred goals more flexibly than both (ii) and (iii) on
    a shared task battery requiring goal deferral + later recovery under changed context, AND
    all three of C2's dissociated failure states are separately inducible: wanting-channel-only
    failure (goal_match suppressed/zeroed, entry still surfaces on drive alone),
    goal_match-channel-only failure (wanting suppressed/zeroed, entry still surfaces on cue
    match alone), and independent joint failure (neither channel alone reproduces the other's
    failure mode).

    FALSIFYING: (i) does not outperform (ii) and/or (iii) on flexible recovery, OR fewer than
    all three of C2's failure states are inducible (e.g. the wanting and goal_match channels
    covary such that only one dissociated failure mode is reachable), OR the
    outshining/closeable-cutoff variant (iii) performs indistinguishably from (i).

    NON-DEGENERACY PRECONDITION: all three flags must be genuinely wired ON together, not
    tested pairwise or with one held at its no-op default -- a single flag left OFF collapses
    the arm back toward a form already covered by an existing MECH-level confirmer (e.g.
    v3_exq_868, C2-only; v3_exq_823/807, C1-on-but-unmanipulated, C3-off) and would falsely
    read as support for or against the PARENT claim when it only tests a child. The task
    battery must require goal deferral-then-recovery that a field-only agent is STRUCTURALLY
    unable to solve (no discrete retrieval target to re-probe), not merely worse at solving.

    HARD PRECONDITION shared with Q-053: MECH-340's three control/efficacy failure signatures
    must be inducible and separable on the substrate before C3 can be meaningfully included in
    this test; if Q-053's gate signal is still unresolved when this EXQ is designed, use its
    front-runner (control/efficacy unattainability appraisal) as C3's operationalisation and
    flag the result as conditional on that choice, not as an independent confirmation of it.

    [2026-08-08 thought-digestion]: currently BLOCKED at substrate-assembly, not at design --
    C1/C2/C3 are each individually implemented (MECH-339, MECH-292, MECH-340 substrate landed)
    but all three master switches remain default OFF (use_composite_cue_outshining,
    use_mech292_ghost_bank, use_persistence_efficacy_gate) as of this check, no experiment has
    run them together, and no run has been tagged to the ARC-060-vs-ARC-078 comparison this
    claim requires. Q-053 confirmed still open, no experiment queued. One child substrate
    landing is not evidence for the parent architectural claim (per the claim's own notes) --
    this EXQ is the missing joint test.

- id: MECH-339
  title: "Composite retrieval cue: the ghost-bank cue is z_goal plus a context channel composed from the already-preserved-but-match-unused SD-039 payload fields (arousal_tag, last_vs, cause), combined by an outshining gate so a strong direct z_goal match suppresses the context channel. Context-cue retrieval is the conditional fallback regime, not an always-on additive term."
  claim_type: mechanism_hypothesis
  subject: hippocampal.composite_retrieval_cue_outshining_gate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-339@2026-06-18T05:15:01.257364Z"
      as_of: 2026-06-18
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-05-19
  depends_on:
    - ARC-078    # parent: bank is a cue-addressed retrieval system
    - SD-039     # supplies the context-channel payload fields
    - MECH-230   # structured z_goal = the direct-cue content
    - MECH-292   # the bank/ranking whose goal_match this composes
  functional_restatement: >
    The landed bank matches the cue against the stored trace by z_goal
    cosine only (`Anchor.goal_match`). MECH-339 asserts the retrieval cue
    must be composite: z_goal PLUS a context channel composed from the
    SD-039 payload fields that are already preserved but unused by the
    match (arousal_tag, last_vs, cause). The two are combined by an
    OUTSHINING gate -- a strong direct z_goal match suppresses / down-
    weights the context channel -- rather than summed with fixed weight.
    Encoding specificity (Smith & Vela 2001 meta-analysis; Tulving &
    Thomson 1973) establishes that context-overlap retrieval is the weak
    regime that matters precisely when a strong direct cue is absent, and
    is outshone by a strong direct cue when one exists.

    Falsifiable: (i) with a strong direct z_goal match present, enabling
    the context channel must NOT change the top-ranked bank entry; (ii)
    with the direct match weak/absent, enabling it MUST change the top
    entry; (iii) a fixed-weight additive context term (no outshining gate)
    degrades top-1 retrieval relative to z_goal-only whenever a clean
    direct match exists.
  notes: >
    Registered 2026-05-19 from the retrieval-cue reframe, Constraint 1.
    Distinct from MECH-292 (the ranking / cued-recall step) and SD-039
    (the payload store): MECH-339 is the cue-composition + gating rule
    that feeds goal_match. Substrate landed 2026-05-19 (see
    implementation_note); status stays candidate / v3_pending --
    implementation is not evidence (exp_conf still 0), the validation
    experiment is diagnostic, and no claim is promoted. The superseded
    product formula in the old ghost_goal_search.md sketch is unrelated
    to this claim (that drift is C2 / MECH-292).
  evidence_quality_note: >
    Registered candidate. Literature support from
    `targeted_review_ghost_goal_search/`: Smith & Vela 2001 (the
    outshining / overshadowing moderator structure IS the design
    constraint -- recorded mixed 0.60 precisely because it both supports
    cue-context retrieval and bounds it), with Nakazawa 2002 (partial-cue
    completion) and the encoding-specificity foundation (Tulving & Thomson
    1973, present in `targeted_review_socially_scaffolded_rule_population/`)
    as corroboration. lit_conf is a PARALLEL signal, NOT blended into
    exp_conf (feedback_lit_exp_decoupled); exp_conf = 0, so candidate /
    v3_pending. The exact outshining crossover curve for an artificial
    bank is not given by the data and must be found empirically.
  implementation_note: >
    IMPLEMENTED 2026-05-19 (smallest substrate step) in
    ree-v3/ree_core/hippocampal/ghost_goal_bank.py: GhostGoalBank.rank
    adds a gated context channel via _outshine_gate and
    _context_salience_for_anchor. context_salience =
    1 - exp(-arousal_tag / arousal_scale) in [0,1);
    gate = clip_[0,1]((outshine_pivot - goal_match) / outshine_pivot);
    context_term = context_weight * gate * context_salience is added to
    the additive ghost_priority (Constraint 2 unaffected -- the
    outshining is a within-channel multiplier, not a product across
    channels). The context channel is sourced from arousal_tag ONLY --
    the one SD-039 payload field that is both already stored and
    entirely unused by the bank. last_vs is deferred (already consumed
    by the recoverability channel; reuse would double-count) and the
    `cause` tag is deferred (NOT present in the implemented
    AnchorGoalPayload -- design-sketch only; folding it in requires an
    SD-039 payload extension, out of scope for the smallest step).
    Config: GhostGoalBankConfig.use_composite_cue_outshining (default
    False), context_weight (0.0), outshine_pivot (0.5), arousal_scale
    (1.0); all no-op defaults -> bit-identical to pre-MECH-339. No
    rank() signature change, no anchor_set/agent wiring change. Verified
    by inline smoke (backward-compat bit-identity; gate 0 at/above
    pivot, monotone; context term exactly 0.0 for a strong-direct-match
    anchor and > 0 for a weak-match high-arousal anchor; components sum
    == ghost_priority) and v3_exq_496 MECH-292 validation --dry-run
    PASS. ree-v3/CLAUDE.md "MECH-339 C1 Composite Cue + Outshining Gate
    (2026-05-19)" is the implementation-status source of truth.
    Diagnostic validation experiment V3-EXQ-594 queued (substrate
    readiness; experiment_purpose=diagnostic so excluded from governance
    scoring; smoke PASS on all 4 falsifiable-prediction sub-tests).
  literature_anchors:
    - targeted_review_ghost_goal_search
  location: docs/architecture/ghost_goal_search.md

- id: ARC-079
  title: "Persistence of an unresolved goal as an active re-probe target is a GATED control/efficacy-detection operation; disengagement is the ungated default. The trace is preserved (no erase -- this matches biology); 'abandonment' is the persistence gate failing to fire, after which the un-gated trace decays in rank and ceases to be re-probed. This RESOLVES ARC-078's C3 by inverting its 'add an explicit abandon path (deliberate divergence)' framing."
  claim_type: architecture_hypothesis
  subject: goal.gated_persistence_disengagement_default
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: 2026-05-19
  epistemic_category: substrate_conditional
  what_would_answer: >
    NON-DEGENERACY PRECONDITION (partially met): a built, separable gate between
    MECH-292/293 -- MET (MECH-340, PersistenceAppraisal struct, reads only
    control/unattainability, not recoverability/staleness/failure tallies). NOT MET:
    config-gated off, never run in a real experiment (exp_conf=0, no V3-EXQ queued).

    CONFIRMING: an agent with (gate ON + default disengagement + preserved SD-039
    trace) avoids BOTH rumination/intrusion (gate-stuck-on) AND apathy/learned-
    helplessness (gate keyed on failure generalising across goals).

    FALSIFYING: either failure mode appears. Specific contrast: a bolted-on
    abandon-trigger control (the original, non-inverted ARC-078 C3 framing) is
    predicted to reproduce learned helplessness; if it doesn't (behaviourally
    indistinguishable from the gated design), the inversion's differential causal
    claim fails.
  depends_on:
    - ARC-078    # parent: ARC-079 resolves ARC-078's C3 (the placeholder constraint)
    - MECH-292   # the ranked bank whose re-probe persistence is gated
    - MECH-293   # the awake re-probe consumer that the gate enables/withholds
    - SD-039     # preserved trace -- never erased (the no-erase half)
  functional_restatement: >
    ARC-079 resolves the C3 placeholder of ARC-078 using the 2026-05-19
    goal-disengagement biology-before pull (targeted_review_goal_disengagement),
    and in doing so INVERTS C3's original framing. C3 assumed REE must add an
    explicit abandon path as a deliberate divergence from biology (because
    biology has no erase operation). The disengagement synthesis shows the
    framing is backwards in two ways:

    - Maier & Seligman 2016 (the load-bearing inversion): passivity /
      disengagement is the unlearned DEFAULT; what is learned and gated is
      CONTROL -- a vmPFC->DRN-type detector that inhibits the default.
      Therefore persistence (continuing to treat a ghost-goal entry as an
      active re-probe target) is the gated, evidence-requiring operation;
      disengagement is what happens in the gate's absence.
    - Bouton's no-erase result is about the TRACE, not about pursuit. The
      SD-039 trace is preserved (no erase) -- which now MATCHES biology, so
      the old C3 'deliberate divergence' premise is weakened: REE plausibly
      does NOT need a non-biological hard-erase op at all. 'Abandonment' is
      simply the persistence gate not firing, after which MECH-292 rank
      decays and MECH-293 stops seeding from the entry; the trace survives,
      un-re-probed, exactly as Bouton's preserved-but-inhibited memory.

    Klinger 1975 / Wrosch 2003: the gate transition is an unattainability
    APPRAISAL opening a brief reengagement-coupled disengagement STATE, not
    a one-shot delete. Koster 2011 / Gillan 2021/2016: failure-to-disengage
    is the ABSENCE of a separable gate, not over-strong persistence -- so
    the gate must be an explicit, separable mechanism (MECH-340), not a
    tuned MECH-292 recoverability weight. Husain & Roiser 2018 / Treadway
    2012: the gate is bounded BELOW -- a gate keyed on repeated failure or
    bare effort-cost reproduces apathy / learned helplessness.

    Falsifiable: an agent with (gated persistence + default disengagement +
    preserved trace) avoids BOTH (a) rumination/intrusion (the inhibition-
    only / gate-stuck-on equilibrium ARC-078 C3 predicted) AND (b) apathy /
    learned-helplessness (gate keyed on failure/effort-cost; passivity
    generalises across goals). A design that instead bolts an abandon
    trigger onto an action-default substrate and keys it on accumulated
    failure will reproduce learned helplessness -- the inverted design does
    not.
  notes: >
    Registered 2026-05-19 from the goal-disengagement biology-before pull.
    Child of ARC-078; resolves its C3 (ARC-078 C3 prose updated to point
    here). The interrupted-task / Zeigarnik resumption gap is the MIRROR of
    ARC-079 (resume deferred work <-> cease re-probing it): ARC-079 is the
    disengage half.

    UPDATE 2026-08-08 (thought-digestion currency check, then RESOLVED the same
    day by chip chip-20260808-arc079-mech389-zeigarnik-crosslink): the Zeigarnik
    gap is NO LONGER unregistered -- it was registered as MECH-389 on 2026-06-10
    (implementation_phase: v4, epistemic_category: substrate_conditional). The
    wiring this note called for is now DONE: MECH-389.depends_on carries ARC-079
    as a constraint, MECH-389.distinct_from carries MECH-340, and
    Q-053.depends_on carries MECH-389.

    Finding: MECH-389 and ARC-079/MECH-340 are COMPATIBLE-BUT-DISTINCT, not in
    conflict -- different plane (V4 arbitrator goal SLOT vs V3 hippocampal bank
    ENTRY), different trigger axis (agent capacity return vs goal-attainability
    appraisal), different lifecycle stage (within-arc suspension under a live
    MECH-061 commitment vs post-arc trace fate). ARC-079's inversion nonetheless
    CONSTRAINS MECH-389: a capacity-keyed re-elevation that fires unconditionally
    would be an ungated persistence op, and a Zeigarnik urgency term accumulating
    with time-since-interruption is structurally the MECH-292 staleness signal
    MECH-340/Q-053 already rule out. That residual gate-authority question is
    recorded as Q-053 sub-question (c). Canonical statement: MECH-389's notes.
    No experiment queued against ARC-079 itself; not promoted; candidate / v3_pending.
  evidence_quality_note: >
    Registered candidate. Literature support is the 2026-05-19 pull in
    `targeted_review_goal_disengagement/`: Maier & Seligman 2016 (the
    circuit-level default-disengage / gated-control inversion -- the single
    most load-bearing entry), Klinger 1975 + Wrosch 2003 (appraisal trigger
    + reengagement coupling), Koster 2011 + Gillan 2021/2016 (failure-to-
    disengage = a missing separable op), Husain & Roiser 2018 + Treadway
    2012 (the lower bound -- over-eager disengage = apathy/helplessness);
    plus the C3 anchors carried from `targeted_review_ghost_goal_search/`
    (Bouton 2004/2014, Maren 2013, Jasinska 2014). lit_conf is a PARALLEL
    signal only, NOT blended into exp_conf (feedback_lit_exp_decoupled);
    exp_conf = 0 so candidate / v3_pending. The inversion is REE's
    interpretive synthesis of that biology, flagged as an architecture
    hypothesis, not a settled result.
  literature_anchors:
    - targeted_review_goal_disengagement
    - targeted_review_ghost_goal_search
  location: docs/architecture/ghost_goal_search.md

- id: ARC-080
  title: "Object identity is a cross-cutting representational primitive. An object is a token-bound identity representation that persists across time and perceptual gaps, generalising z_object/z_resource into an object-file/binding layer (ARC-006), defined by causal feature-coherence under intervention (MECH-278), and consumed by drive/goal (SD-057), hippocampal planning (MECH-262), and the self/other special-object cases. The four planned object capabilities -- (1) permanence, (2) self-as-object, (3) tools/affordances, (4) others-as-object -- are specialisations of one primitive, not four separate mechanisms."
  claim_type: architectural_commitment
  subject: representation.object_identity_primitive
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-080@2026-06-06T07:53:48.739107Z"
      as_of: 2026-06-06
      verdict: held_v4_by_architectural_commitment/applied
  implementation_phase: v4
  v3_pending: true
  claim_level: architectural
  registered_utc: 2026-06-04
  epistemic_category: substrate_coherence
  what_would_answer: >
    NOT a standard empirical CONFIRMING/FALSIFYING pair -- ARC-080 is a THIN UMBRELLA /
    COHERENCE MAP (architectural_commitment / substrate_coherence), not a testable
    mechanism, so this specifies the REASSESSMENT TRIGGER for the COORDINATION-PRIMITIVE
    FRAMING ITSELF (the 2026-06-14 OBJ-1 resolution: an object is a binding of three
    non-reducing coordinate facets -- TYPE, ANCHOR, TOKEN), not a result that could
    confirm/refute the four pillars' own mechanisms.

    TRIGGER: a real OBJ-2 build that actually COREGISTERS the three facets (not just
    builds one in isolation). CURRENCY CHECK (2026-08-08): the TOKEN facet alone
    (ARC-006/MECH-045) was implemented 2026-06-09 as
    ree-v3/ree_core/entities/object_file_buffer.py, wired into REEAgent behind a still
    default-off flag (use_object_file_buffer=False), with one supporting validation run
    (V3-EXQ-658, MECH-045 still provisional, not promoted). That predates the 2026-06-14
    OBJ-1 decision and does NOT coregister with the TYPE store (SD-057) or ANCHOR store
    (SD-039) -- so the trigger is armed (one facet exists) but has not fired.

    EVIDENCE RELEVANT AT THAT REASSESSMENT: does a coregistration build find two facets
    collapse cleanly into one (contradicting "none reduces to another")? Does keeping the
    facets coregistered-but-distinct produce a measurable advantage over a
    flat/collapsed representation on the diagnostics that motivated OBJ-1 (V3-EXQ-641a
    coherence-non-reducibility; z_world dim=32 discriminative ceiling)? Does the deferred
    token-vs-type individuation-strength sub-fork resolve compatibly? As ARC-081/082/083
    (and the future Pillar-1 child) get built, do they cleanly instantiate as coordinate
    facets, or does any one need a mechanism outside the object-file/binding frame?

    WHAT WOULD ACTUALLY REVISE THE COORDINATION-PRIMITIVE FRAMING: (a) an OBJ-2 build
    showing two-or-more facets are not independently necessary; (b) a build showing
    coregistration yields no advantage over a flat/collapsed representation; or (c) a
    pillar build needing a mechanism outside the object-file/binding frame entirely. Any
    of these would revise the OBJ-1 decision and this umbrella's framing -- not any
    single pillar's own pass/fail result in isolation.

    READER NOTE: do not tag ordinary pillar experiments (V3-EXQ-449, V3-EXQ-658, SD-055
    differentiable-CEM runs, future ARC-081/082/083 runs) as confirming/falsifying
    ARC-080 directly -- they speak to the pillar-specific child claims. Only evidence
    about whether the three-facet coregistration STRUCTURE ITSELF holds up under a real
    coregistering OBJ-2 build is relevant here. No EXP-#### should be minted against
    ARC-080 directly.
  depends_on:
    - ARC-006    # representational substrate -- the object-file / binding layer (provisional, dormant; biology grounding via L1 pull in progress)
    - MECH-278   # object DEFINITION -- causally-coherent feature bundle under intervention
    - ARC-059    # developmental ordering -- self -> objects -> others
    - SD-015     # live identity latent -- z_resource (location-invariant embedding)
    - SD-049     # per-type identity tag + classifier head + per-axis drive
    - SD-057     # live per-object incentive token (z_object = detached z_resource clone), the resource-bound spur this umbrella generalises
    - SD-039     # ANCHOR facet -- dual-trace ghost-goal bank (spatial anchor key); promoted to a depends_on by the OBJ-1 coordination-primitive decision (one of three coordinate facets, not just a mentioned store)
  functional_restatement: >
    ARC-080 is a THIN UMBRELLA / COHERENCE MAP, not new mechanism. It is the
    "Option A" spine proposed in
    evidence/planning/object_representation_thread_2026-06-04.md, adopted by the
    user 2026-06-04. It introduces NO substrate code and changes NO V3 behaviour;
    it names objects as a single cross-cutting representational primitive and
    gives the scattered object-related fragments a registered parent so future
    pillar work slots in cleanly via depends_on, instead of drifting into a
    fourth blind per-item store.

    PROBLEM IT RESOLVES (the gap). Object-ness already exists in the substrate but
    in THREE disconnected lineages with no shared spine:
    (A) a DORMANT representational layer -- ARC-006 "entities are sparse,
        persistent, bindable" + MECH-044 relational binding + MECH-045 object-file
        persistence (provisional, design-only, NOT in ree-v3 code, and -- until the
        2026-06-04 L1/L2 pulls -- with NO biology lit-pull at all);
    (B) a DEVELOPMENTAL-ORDERING layer -- ARC-059 (self->objects->others) +
        MECH-276/277/278, where MECH-278 supplies the object DEFINITION but is
        explicitly BYPASSED in V3 (z_world is engineered pre-split -- an
        "architectural shortcut past stage 2");
    (C) a LIVE resource-bound identity latent -- SD-015 -> SD-049 -> SD-057, which
        actually runs (IncentiveTokenBank holds a per-object z_object = detached
        z_resource clone + revaluable base_value) but on the narrow resource/drive
        rail, and cites NEITHER (A) nor (B).
    Three overlapping per-item stores result -- SD-057 IncentiveTokenBank (keyed by
    resource TYPE tag), the SD-039/MECH-292/293 ghost-goal bank (keyed by spatial
    ANCHOR, payload = goal-snapshot not object-token), and the ARC-006/MECH-045
    object-file buffer (keyed by entity TOKEN, dormant) -- none citing the canonical
    object-file layer.

    THE CORE DISTINCTION. "Object" currently means at least three things in REE:
    a TYPE/category (SD-049 tag; z_resource is location-invariant), a spatial
    ANCHOR (ghost bank), and a feature-bundle TOKEN entity (MECH-278/ARC-006).
    REE's LIVE object work is TYPE-level. True permanence, tools, self-as-object,
    and others-as-object all need TOKEN-INSTANCE object-files ("this apple, tracked
    through occlusion", not "apples"). That type-vs-token-vs-anchor choice is the
    first design fork for any future substrate step and is a genuine
    representational gap, not just a missing flag.

    THE FOUR PILLARS (specialisations of the one primitive; existing/needed claims):
    - PILLAR 1 permanence: NEW future child (token persistence through occlusion);
      reactivate MECH-045; generalise the SD-039/MECH-292/293 ghost-goal bank from
      goal-snapshot to object-token. [V3-straddle / V4]  -- documented as a future
      child; NOT registered as a separate claim by this umbrella.
    - PILLAR 2 self-as-object: z_self as a privileged object-file slot
      (DR-10..DR-14, MECH-214/MECH-215, INV-064). [V4]
    - PILLAR 3 tools/affordances: object->action binding; ground SD-016
      cue_action_proj (currently UNGROUNDED -- V3-EXQ-449 0.0 gradient; EXP-0155
      queued). [V3 substrate / V4 grounding]
    - PILLAR 4 others-as-object: other-agent object-file slots (z_self_j,
      z_harm_a_j); ARC-010 / ARC-047. [V4]
    DEV-NEED-021 already treats object permanence as load-bearing for the social
    pillar (otherness inference REQUIRES object persistence + self-stability), so
    Pillars 1+2 are prerequisites for Pillar 4.

    SCOPE / GUARD. epistemic_category inferred to substrate_coherence
    (architectural_commitment) -- promote/demote suppressed; this is a coherence
    map, not an empirical assertion to be gated. v3_pending + implementation_phase
    v4: the doc-spine itself is V3-safe (touches no substrate, no critical-path
    file), but the first real substrate step -- generalising z_object from a
    type-tag store to a token-keyed object-file -- is V4 / late-V3 enrichment and
    must NOT enter V3 closure. SD-057 stays resource-bound for V3; the GAP-7 L9
    wanting!=liking retest is owned elsewhere (scaffolded_sd054_onboarding) and is
    untouched. Registering this umbrella does NOT promote anything and does NOT
    change any V3 behaviour.

    OBJ-1 RESOLUTION (2026-06-14, IGW plan-reconcile object_representation_v4:OBJ-1;
    user decision). The type-vs-token-vs-anchor fork is resolved NOT by electing one
    of {type, token, anchor} as the primitive that subsumes the other two, but by
    making the COORDINATION itself the primitive: an object is a BINDING that holds
    three distinct coordinate facets -- a TYPE/category readout (SD-015/SD-049,
    z_resource location-invariant), a SPATIAL ANCHOR (SD-039/MECH-292/293 ghost
    bank), and a TOKEN-INSTANCE individuation (ARC-006/MECH-044/MECH-045 object-file)
    -- none of which reduces to another. The object-file is the structure that keeps
    the three facets COREGISTERED; the primitive is the coregistration, not any one
    facet promoted to king. This fits the binding literature already on file: a SLOT
    addresses the token (which object, persistent pointer; Pylyshyn FINST / Locatello
    slot-attention), SYNCHRONY binds the features that cohere now (Fries / Singer /
    von der Malsburg), and von der Malsburg's superposition catastrophe is precisely
    WHY the facets must be kept distinct-but-bound rather than collapsed. The V3
    diagnostics that bottomed out on a missing bound representation (V3-EXQ-641a
    coherence-non-reducibility = "bound-representation prerequisite absent"; z_world
    dim=32 discriminative ceiling) are the experimental pull toward this same
    coregistration substrate. CONSEQUENCE FOR THE PILLARS: OBJ-2 is no longer "pick
    token over type" but "build the coregistration structure that binds the three
    facets"; z_object becomes the TYPE-READOUT OF a coordinate object-file rather than
    a free-standing type tag; SD-039's anchor store is the ANCHOR facet (now wired
    into depends_on). The remaining open sub-fork is token-vs-type INDIVIDUATION
    STRENGTH within the bound token facet -- deferred to the first OBJ-2 build step,
    not decided here. This is a DESIGN COMMITMENT only: it stays implementation_phase
    v4 / v3_pending, off the V3-closure critical path; SD-057 stays resource-bound
    for V3; no coregistration substrate is built and no V3 behaviour changes.

    UPDATE 2026-08-08 (thought-digestion currency check): "no substrate is built"
    above is now imprecise in one narrow respect -- the isolated TOKEN facet
    (ARC-006/MECH-045) was implemented 2026-06-09 as
    ree-v3/ree_core/entities/object_file_buffer.py, wired into REEAgent behind a
    still-default-off flag (use_object_file_buffer=False), with one supporting run
    (V3-EXQ-658). This predates and does not implement the 2026-06-14 OBJ-1
    coregistration decision -- it does not coregister with SD-057 (TYPE) or SD-039
    (ANCHOR) -- so OBJ-2 as OBJ-1 actually redefined it (build the coregistration
    structure) has still not started; only one of its three prerequisite pieces
    exists, standalone, gated off by default. Flagging so a future reader doesn't
    read the isolated buffer as OBJ-2 progress. closure_status.md's OBJ-2 ("open")
    and OBJ-4/Pillar-3 ("blocked") rows (both dated 2026-06-10) look stale against
    this and against SD-055 (Pillar 3 grounding); left for the next regen of that
    derived doc, not touched here.
  biology_grounding_note: >
    ARC-006/MECH-044/MECH-045 had NO biology lit-pull at registration of this
    umbrella (sources were internal 2026-02 thoughts + DANIEL_README), which the
    project rule (feedback_biology_before_formal_definitions) flags. UPDATE
    2026-08-08 (thought-digestion currency check): that gap has since CLOSED, not
    merely started closing as the text below (left for history) describes -- the L1
    "object files & feature binding" pull
    (evidence/literature/targeted_review_object_files_feature_binding/, 11 dated
    entries 2026-06-04 through 2026-06-06) and the L2 "object permanence" pull
    (evidence/literature/targeted_review_object_permanence/, 5 entries + a
    synthesis.md) have both LANDED. L3 (affordances/Gibson), L4
    (self-as-object/Gallagher/Botvinick), L5 (ToM/Woodward/Csibra) still follow
    when their pillars are scheduled.

    Original note (2026-06-04, now superseded by the update above): the L1 "object
    files & feature binding" pull (Kahneman/Treisman/Gibbs 1992; Treisman & Gelade
    1980 FIT) and the L2 "object permanence" pull (Piaget; Baillargeon; Spelke
    core-knowledge; Kellman & Spelke 1983) were both ACTIVE as of 2026-06-04T16:03Z
    (sessions litpull-object-files-feature-binding / litpull-object-permanence).
    Treat ARC-006's biology grounding as IN PROGRESS pending those pulls' landing;
    L3 (affordances/Gibson), L4 (self-as-object/Gallagher/Botvinick), L5
    (ToM/Woodward/Csibra) follow when their pillars are scheduled.
  location: docs/architecture/arc_080_object_representation_primitive.md

- id: ARC-081
  title: "Self-as-object (PILLAR 2 of ARC-080): z_self becomes a privileged token-keyed object-file SLOT in the generalised object-file layer. The SELF BEGINS IN V3 -- z_self self/world split (SD-005), self-attribution (SD-003), action-space discovery / self-as-object stage 1 (MECH-277, ARC-059 stage 1), reward-free babbling Phase 0 (ARC-074). The object-file CUTOVER IS V4 -- z_self promoted from a body-state latent to a privileged self-OBJECT slot, gated by the DR-10..DR-14 self-model integration audit, MECH-214 (goal E1-representability), MECH-215 (self-model prerequisite for agentive prediction), INV-064 (maturational-sequence necessity), and SD-030 (E2 self-forward-model, V4-deferred). The beginning of self is V3; the object-file-slot cutover is V4."
  claim_type: architectural_commitment
  subject: representation.self_as_object_file_slot
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-081@2026-06-06T07:53:48.739107Z"
      as_of: 2026-06-06
      verdict: held_v4_by_architectural_commitment/applied
  implementation_phase: v4
  v3_pending: true
  claim_level: architectural
  registered_utc: 2026-06-04
  implementation_note: >
    SELF-1 / DR-13 (z_self temporal depth) SUBSTRATE BUILT 2026-07-01 in ree-v3
    (user-directed, to unblock self_model_v4:SELF-3 / DR-10 after the 2026-07-01
    IGW-165 reconcile). Executes the HYBRID mechanism resolved on this claim's
    `notes` 2026-06-14: a light DEDICATED self-recurrence (SelfRecurrenceCell,
    a GRUCell over z_self in ree_core/latent/self_recurrence.py) whose output is
    blended toward the E1 generative prediction of z_self (E1 predicted-next
    z_self, cached at _e1_tick), blend weight LatentStackConfig
    .self_recurrence_e1_coupling (the recorded residual sub-question: 0 = pure
    recurrence / Option A, 1 = pure E1-feedback / Option B, 0.15 light default =
    HYBRID). Lever lives in LatentStack.encode() and REPLACES the z_self EMA step
    ONLY (z_world untouched); master switch LatentStackConfig.use_self_recurrence
    (default False -> module not instantiated + verbatim legacy EMA ->
    bit-identical OFF). Full suite 1336 passed / 4 pre-existing failures + 11 new
    contracts (tests/contracts/test_dr13_self_recurrence.py). generation:v4 --
    off the V3 closure path. PROMOTES NOTHING: ARC-081 stays candidate /
    architectural_commitment / implementation_phase=v4 / v3_pending=true (DR-13
    is the substrate half; DR-10/SELF-3 + validation experiments remain before
    the self-object cutover). Design doc:
    docs/architecture/dr13_self_recurrence_temporal_depth.md. Plan node:
    evidence/planning/self_model_v4_plan.md self_model_v4:SELF-1. Validation:
    V4-EXQ-002 (DR-13 self-recurrence substrate-readiness falsifier) queued +
    PASSED 2026-07-01 (diagnostic, claim_ids=[]; PROMOTES NOTHING; scores no
    claim) -- run ..._20260701T065002Z_v4: recurrence live (state_departure
    1.15), history-carrying + perturbation-isolated + E1-anchor-blend live 3/3
    seeds. DR-13 self subject substrate-validated.
    DR-10 / SELF-3 (z_self enters E3 viability scoring, the E3-scoring half of
    this pillar's cutover) SUBSTRATE BUILT 2026-07-01 on the DR-13 stateful z_self
    (user-approved graduation, caller-supplied v1): no-op-default lever
    e3_selector.use_self_viability_weighting (a per-candidate self-viability cost
    from z_self discounts trajectories less viable for the current bodily state;
    sibling to DR-12, no learned params, bit-identical OFF). Full suite 1344
    passed / 4 pre-existing + 8 new DR-10 contracts. generation:v4, PROMOTES
    NOTHING (ARC-081 stays candidate/v4/v3_pending). Design doc
    docs/architecture/dr10_z_self_in_e3_viability.md; plan node
    self_model_v4:SELF-3 (flipped open->in_progress); validation V4-EXQ-003.
  depends_on:
    - ARC-080    # umbrella -- object identity as a cross-cutting primitive (this is the self specialisation)
    - SD-005     # V3 BEGINNING -- z_self self/world split (proprioceptive/interoceptive body latent)
    - SD-003     # V3 BEGINNING -- self-attribution (self-vs-external causal credit)
    - MECH-277   # V3 BEGINNING -- action-space discovery / self-as-object stage 1
    - ARC-059    # V3 BEGINNING -- developmental ordering stage 1 (self before objects before others)
    - ARC-074    # V3 BEGINNING -- reward-free babbling Phase 0 (the self-discovery substrate)
    - MECH-214   # V4 CUTOVER -- goal must be E1-representable (self-model integration audit)
    - MECH-215   # V4 CUTOVER -- self-model prerequisite for agentive prediction
    - INV-064    # V4 CUTOVER -- maturational-sequence necessity (self-stability gates later pillars)
    - SD-030     # V4 CUTOVER -- E2 self-forward-model (V4-deferred)
  functional_restatement: >
    ARC-081 is a THIN COHERENCE-MAP CHILD of ARC-080, not new mechanism. It adds
    exactly ONE thing the existing self claims do not state: that z_self's V4
    endpoint is a PRIVILEGED OBJECT-FILE SLOT -- the self treated as a special case
    of the ARC-080 token-keyed object-file, not a free-standing body latent. The
    underlying mechanism is already owned by the depends_on claims (z_self =
    SD-005; self-attribution = SD-003; self-as-object stage 1 = MECH-277/ARC-059;
    the self-model integration audit = DR-10..DR-14 + MECH-214/215; maturational
    necessity = INV-064; E2 self-forward-model = SD-030); ARC-081 does NOT restate
    them, it only names the cross-cutting framing and the phase boundary.

    THE BOUNDARY (the central correction). Self-as-object is NOT flatly V4. It has
    a V3 BEGINNING and a V4 CUTOVER:
    - BEGINS IN V3: z_self exists today as a single-MLP + EMA body-state latent
      (SD-005); self/external causal attribution runs (SD-003); action-space
      discovery / self-as-object stage 1 is the MECH-277 + ARC-059-stage-1 +
      ARC-074 reward-free-babbling line. "The idea of a self that acts" has begun.
    - CUTOVER TO V4: z_self becoming a privileged object-file SLOT -- in E3 scoring
      (DR-10), with its own z_goal_self domain (DR-11), E2-PE modulating E3
      confidence (DR-12), temporal depth/recurrence (DR-13), and a proxy/hedonic
      dissociating env (DR-14) -- gated by the DR-10..DR-14 self-model integration
      audit + MECH-163 multi-step planning. This is the self-OBJECT slot, and it is
      V4.

    SCOPE / GUARD. epistemic_category inferred to substrate_coherence
    (architectural_commitment) -- promote/demote suppressed; this is a coherence
    map, not an empirical assertion to be gated. v3_pending + implementation_phase
    v4: registering this child introduces NO substrate code, NO experiments, and
    changes NO V3 behaviour. The self-OBJECT-slot substrate step is V4 and must NOT
    enter V3 closure. SD-005/SD-003/MECH-277/ARC-074 keep their existing V3 roles
    unchanged.
  notes: >
    SELF-1 DR-13 MECHANISM RESOLVED 2026-06-14 (IGW self_model_v4:SELF-1
    interactive design-fork session). ARC-081 already commits the stateful
    self-OBJECT cutover; SELF-1 forced only the MECHANISM by which z_self gains
    temporal depth, which this claim and v4_spec V4-2 deliberately left open
    ("replace the single hidden layer + EMA with recurrence OR E1 feedback").
    DECISION: HYBRID -- z_self acquires a light DEDICATED self-recurrence whose
    dynamics are REGULARISED/ANCHORED by E1 generative feedback, committing BOTH
    motifs rather than choosing one. The dedicated self-recurrence supplies the
    explicit, inspectable, lesionable, stability-isolated subject the later
    DR-10/DR-11/DR-12 integration and INV-064 maturational-stability gate attach
    to (a free-running shared E1 latent cannot be perturbation-isolated for the
    self); the E1-feedback regulariser keeps that self-latent consistent with the
    E-stream generative account of the body so it does not drift into a parallel
    self-model divorced from the world-model -- SD-030 (E2_self forward-model,
    SELF-2) and DR-12 (E2-PE -> E3 confidence) remain E-stream-native and wire
    onto the regularised self-latent. Cost recorded honestly: the hybrid commits
    more machinery than either pure option and introduces a regularisation-coupling
    strength as a tunable -- how tightly the self-recurrence is anchored to E1
    feedback is the residual sub-question (light coupling preserves the
    stability-isolation benefit; strong coupling collapses toward pure E1-feedback
    Option B). This is the DESIGN decision only; NO V3 substrate change (the
    single-MLP + EMA stays as the V3 self latent); the build is the V4 DR-13
    cutover gated by the self-model integration audit. PROMOTES NOTHING -- ARC-081
    stays candidate / architectural_commitment (substrate_coherence) /
    implementation_phase v4 / v3_pending.
  location: docs/architecture/arc_080_object_representation_primitive.md

- id: ARC-082
  title: "Tools/affordances as object->action binding (PILLAR 3 of ARC-080): an object's afforded actions are bound to its object-file. The V3 SUBSTRATE EXISTS BUT IS UNGROUNDED -- SD-016 cue_action_proj is inert (V3-EXQ-449 found 0.0 gradient; the non-differentiable CEM blocks the path before E3.select; EXP-0155 queued to instrument). GROUNDING STRADDLES V3 on its own existing track (EXP-0155 instrumentation + the SD-055 differentiable-CEM dependency). FULL object->action binding via the object-file -- afforded actions keyed to a token-instance object-file slot -- IS V4, once the generalised object-file exists."
  claim_type: architectural_commitment
  subject: representation.tools_object_action_binding
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-082@2026-06-06T07:53:48.739107Z"
      as_of: 2026-06-06
      verdict: held_v4_by_architectural_commitment/applied
  implementation_phase: v4
  v3_pending: true
  claim_level: architectural
  registered_utc: 2026-06-04
  depends_on:
    - ARC-080    # umbrella -- object identity as a cross-cutting primitive (this is the tools/affordance specialisation)
    - SD-016     # V3 SUBSTRATE -- cue_action_proj (affordance bias for E2; LIVE but ungrounded, V3-EXQ-449 0.0 gradient)
    - SD-055     # GROUNDING TRACK -- differentiable CEM that restores gradient to cue_action_proj (the straddling-V3 grounding path)
  functional_restatement: >
    ARC-082 is a THIN COHERENCE-MAP CHILD of ARC-080, not new mechanism. It adds
    one framing the existing SD-016 track does not: that the affordance/action
    pathway's V4 endpoint is OBJECT->ACTION BINDING keyed to a token-instance
    object-file slot -- tools as a specialisation of the ARC-080 object-file, not a
    standalone cue projection. SD-016 already owns the substrate; ARC-082 does NOT
    restate it.

    THE BOUNDARY. Tools/affordances is NOT cleanly one phase:
    - V3 SUBSTRATE EXISTS, UNGROUNDED: SD-016 cue_action_proj is in V3 code but
      inert. V3-EXQ-449 found 0.0 gradient through it (the non-differentiable CEM
      proposer severs the path; the signal vanishes before E3.select). EXP-0155 is
      queued to instrument it.
    - GROUNDING STRADDLES V3: making cue_action_proj actually carry an
      object->action signal is its OWN existing track -- EXP-0155 instrumentation +
      the SD-055 differentiable-CEM dependency (now a depends_on edge; SD-055 is
      implemented + substrate-ready 2026-05-21, re-registered 2026-06-04 after a
      2026-05-15 auto-sync conflict re-apply had silently dropped its claims.yaml
      entry). This grounding work can begin in the V3 era but is gated on the
      differentiable-CEM path.
    - FULL BINDING VIA THE OBJECT-FILE IS V4: folding the grounded affordance
      pathway into a token-keyed object->action axis -- "this object affords these
      actions" attached to the object-file slot -- requires the generalised
      object-file, which is V4 / late-V3 substrate enrichment.

    SCOPE / GUARD. epistemic_category inferred to substrate_coherence
    (architectural_commitment) -- promote/demote suppressed. v3_pending +
    implementation_phase v4: this child introduces NO code and NO experiments and
    does not pull SD-016 grounding or any object-file work into V3 closure. The
    EXP-0155 / SD-055 grounding track is owned separately and is untouched by this
    registration.
  location: docs/architecture/arc_080_object_representation_primitive.md

- id: ARC-083
  title: "Others-as-object (PILLAR 4 of ARC-080): each other agent j gets its own token-keyed object-file SLOT (z_self_j, z_harm_a_j, drive, commitment chain) -- a special case of the ARC-080 object-file. This pillar is V4 (ARC-010 mirror modelling, ARC-047 SocialGridWorld), gated on MECH-163 multi-step hippocampal planning. Per developmental_needs_register DEV-NEED-021 its prerequisites are object-permanence (PILLAR 1, partial via SD-039 ghost bank) + self-stability (PILLAR 2 self, ARC-081): otherness inference REQUIRES object persistence + a stable self."
  claim_type: architectural_commitment
  subject: representation.others_as_object_file_slot
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-083@2026-06-06T07:53:48.739107Z"
      as_of: 2026-06-06
      verdict: held_v4_by_architectural_commitment/applied
  implementation_phase: v4
  v3_pending: true
  claim_level: architectural
  registered_utc: 2026-06-04
  depends_on:
    - ARC-080    # umbrella -- object identity as a cross-cutting primitive (this is the others specialisation)
    - ARC-010    # other-modelling substrate -- mirror modelling (stable)
    - ARC-047    # other-modelling harness -- SocialGridWorld scent harness (candidate, v4)
    - MECH-163   # GATE -- multi-step hippocampal planning required before V4 social entry
    - ARC-081    # PREREQUISITE (DEV-NEED-021) -- self-stability: a stable self before others
    - SD-039     # PREREQUISITE (DEV-NEED-021, partial) -- object-persistence substrate (ghost-goal bank; true object permanence is PILLAR 1, unregistered/future)
  functional_restatement: >
    ARC-083 is a THIN COHERENCE-MAP CHILD of ARC-080, not new mechanism. It adds
    one framing: that other-modelling's V4 endpoint is an "OTHER-AS-OBJECT" slot --
    each other agent j carried as its own token-keyed object-file (z_self_j,
    z_harm_a_j, drive, commitment chain), a specialisation of the ARC-080
    object-file. The mechanism is already owned by ARC-010 / ARC-047 (and the
    MECH-031/032/036/041 social cluster); ARC-083 does NOT restate them.

    THE BOUNDARY. This pillar is V4 -- design-only today (ARC-010 mirror modelling
    is stable as a claim but unimplemented as an other-object slot; ARC-047
    SocialGridWorld is the candidate v4 harness). It is gated on MECH-163
    (multi-step hippocampal planning) before V4 social entry. Per
    developmental_needs_register DEV-NEED-021, otherness inference REQUIRES object
    persistence + self-stability, so PILLAR 1 (permanence) and PILLAR 2 (self,
    ARC-081) are prerequisites for PILLAR 4. Object permanence is only PARTIALLY
    present (the SD-039 motivational ghost bank, not a true object-file persistence
    buffer); the token-instance permanence substrate is PILLAR 1, which the ARC-080
    umbrella documents as a FUTURE child and which this batch deliberately does NOT
    register (no duplication).

    SCOPE / GUARD. epistemic_category inferred to substrate_coherence
    (architectural_commitment) -- promote/demote suppressed. v3_pending +
    implementation_phase v4: this child introduces NO code and NO experiments. All
    other-object substrate is V4 and must NOT enter V3 closure.
  location: docs/architecture/arc_080_object_representation_primitive.md

- id: MECH-340
  title: "The persistence/efficacy gate: a control/efficacy-detection signal that maintains an unresolved-goal bank entry as an active MECH-293 re-probe target. Its ABSENCE is the default (the entry stops being re-probed and decays in MECH-292 rank; the SD-039 trace is NOT erased). It is gated ON by an unattainability-negative / control-positive appraisal, NOT by accumulated failure, repeated suppression, bare effort-cost, or staleness."
  claim_type: mechanism_hypothesis
  subject: hippocampal.persistence_efficacy_gate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-340@2026-06-18T05:15:01.290871Z"
      as_of: 2026-06-18
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: 2026-05-19
  depends_on:
    - ARC-079    # parent: the gated-persistence inversion MECH-340 instantiates
    - MECH-292   # the ranked bank whose re-probe eligibility the gate modulates
    - MECH-293   # the awake probe the gate enables/withholds per entry
    - SD-039     # the preserved trace that survives gate-loss un-erased
  functional_restatement: >
    MECH-340 is the gate ARC-079 posits, made mechanistic. Per Maier &
    Seligman 2016 the substrate default is disengagement and a control /
    efficacy detector inhibits it. The REE analogue is a per-entry
    persistence signal: when present it keeps a ghost-goal bank entry
    eligible as a MECH-293 awake re-probe target; when absent the entry is
    simply not re-probed and its MECH-292 rank decays -- no explicit erase,
    the SD-039 trace persists (Bouton preserve-don't-erase).

    The gate is held ON by an appraisal that the goal is still attainable /
    under some control (Wrosch 2003 unattainability-negative; a control /
    efficacy positive signal). Gate-loss opens a brief reengagement-coupled
    disengagement STATE, not a one-shot delete (Klinger 1975 depression
    phase; Brandstatter 2013 action crisis -- and within-crisis goal
    devaluation is a SYMPTOM of the unresolved conflict, NOT the gate
    signal, so the gate must not key on the entry's own dropped value).

    Hard negative constraints (each load-bearing, each from the pull): the
    gate is NOT a function of accumulated failure or repeated suppression
    (Bouton: 36==4 sessions; Maier & Seligman: prolonged failure is exactly
    what produces pathological default passivity), NOT a function of bare
    effort-cost or instantaneous low value (Husain & Roiser 2018; Treadway
    2012: that is the apathy/anhedonia phenotype, and the gate must be
    conditioned on value-integration integrity), and NOT MECH-292 staleness
    (Jasinska incubation can invert the staleness sign).

    Falsifiable: (a) gate removed (pure default) -> premature give-up /
    apathy signature; (b) gate stuck ON (inhibition-only) -> rumination /
    intrusion; (c) gate keyed on failure or effort-cost -> learned-
    helplessness signature with passivity generalising across unrelated
    goals. The exact licit gate signal is the open question Q-053.
  notes: >
    Registered 2026-05-19 from the goal-disengagement pull. The
    persistence-gate framing is the biology-faithful replacement for an
    'abandon trigger'; it is not implemented (SD-039 / MECH-292 / MECH-293
    substrate exists, the gate does not). Distinct from MECH-292 (ranking),
    MECH-293 (the probe it gates), and MECH-339 (cue composition). Also
    distinct from MECH-389 (interrupted-task resumption / Zeigarnik, added
    2026-08-08 by chip chip-20260808-arc079-mech389-zeigarnik-crosslink):
    MECH-389 re-elevates a PARKED GOAL SLOT in the V4 SD-033e/MECH-265
    arbitrator on AGENT-CAPACITY return, whereas MECH-340 gates a V3
    hippocampal BANK ENTRY's re-probe eligibility on a GOAL-ATTAINABILITY
    appraisal -- different store, different trigger axis, different lifecycle
    stage, so they are compatible rather than contradictory. MECH-340's hard
    negatives nonetheless BIND MECH-389: a Zeigarnik urgency term accumulating
    with time-since-interruption is structurally the MECH-292 staleness signal
    ruled out above, so it must not serve as the persistence gate. That residual
    gate-authority question is Q-053 sub-question (c); canonical statement of
    the reconciliation is in MECH-389's notes. The
    precise gate signal is deliberately left to Q-053 -- do not pre-commit.
    No experiment queued; not promoted; candidate / v3_pending.
  evidence_quality_note: >
    Registered candidate. Literature support in
    `targeted_review_goal_disengagement/`: Maier & Seligman 2016 (the
    circuit-level inversion that licenses 'gate persistence, default
    disengage'), Klinger 1975 / Wrosch 2003 (appraisal trigger +
    reengagement-coupled state), Koster 2011 / Gillan 2021/2016 (the gate
    must be explicit and separable), Husain & Roiser 2018 / Treadway 2012
    (the lower-bound negatives). lit_conf is PARALLEL only, NOT blended
    into exp_conf; exp_conf = 0 so candidate / v3_pending. Substrate landed
    2026-05-21 (see implementation_note); status stays candidate /
    v3_pending -- implementation is not evidence (exp_conf still 0).
  implementation_note: >
    IMPLEMENTED 2026-05-21 (smallest substrate step) in
    ree-v3/ree_core/hippocampal/ghost_goal_bank.py: global
    PersistenceAppraisal (control_efficacy, goal_unattainability in [0,1])
    passed into GhostGoalBank.rank(); license =
    clip(control)*(1-clip(unattainability)); anchors with license <
    persistence_floor are excluded (disengaged, trace preserved per
    Bouton). Hard negatives enforced by API shape: the gate reads ONLY
    the appraisal struct, NOT recoverability/staleness/wanting/failure
    tallies. Config: GhostGoalBankConfig.use_persistence_efficacy_gate
    (default False), persistence_floor (0.05),
    persistence_default_when_appraisal_missing (1.0). HippocampalModule
    rank_ghost_goals / MECH-293 _propose_ghost_seeded thread optional
    persistence_appraisal. Q-053 agent wiring landed 2026-05-21:
    REEAgent._compute_persistence_appraisal() (completion + commitment ->
    control_efficacy; 1-goal_proximity -> goal_unattainability).
    Reengagement-coupled disengagement STATE
    (Klinger) deferred -- this step is the separable gate primitive only.
    Verified: contracts 4/4 + V3-EXQ-607 --dry-run PASS. ree-v3/CLAUDE.md
    "MECH-340 Persistence Gate" is the implementation-status source of truth.
  literature_anchors:
    - targeted_review_goal_disengagement
  location: docs/architecture/ghost_goal_search.md

- id: Q-053
  title: "What is the licit gate signal for goal persistence (the MECH-340 control/efficacy detector that holds a ghost-goal entry re-probe-eligible) -- and is any explicit hard trace-erase operation needed at all, or does ungated MECH-292 rank-decay of a preserved SD-039 trace suffice?"
  claim_type: question
  subject: goal.licit_persistence_gate_signal
  polarity: open_question
  status: open
  live_status:
    reading: open/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Adjudicated by a substrate-design decision plus a falsifier EXQ that induces
    the three MECH-340 control/efficacy-detector failure signatures and contrasts
    candidate gate signals for goal persistence. Front-runner to test: an internal
    control/efficacy-unattainability APPRAISAL gated on value-integration
    integrity (Wrosch 2003 input semantics + Maier & Seligman 2016 form). ANSWER
    the gate-signal leg by which candidate, when supplied as the
    hold/re-probe-eligibility trigger, reproduces adaptive disengage-and-reengage
    WITHOUT reproducing a documented pathology -- the ruled-out signals
    (accumulated failure -> learned helplessness; bare effort-cost -> apathy;
    MECH-292 staleness alone -> incubation sign-inversion; within-crisis devalued
    value -> symptom-not-cause) must each re-derive their pathology, confirming
    they are not the licit gate. Resolve the two split sub-questions empirically:
    (a) trace-erase -- ANSWER "no explicit hard-erase needed" if ungated MECH-292
    rank-decay of a PRESERVED SD-039 trace matches Bouton preserve-don't-erase
    recovery; (b) dedicated disengage op -- ANSWER "separate op required" (lean
    YES, Koster 2011) if getting disengagement 'for free' by tuning MECH-292
    persistence/recoverability knobs reproduces the
    rumination/impaired-disengagement pathology. CRITICAL SHAPE CONSTRAINT
    (Klinger 1975): the gate must open a brief affectively-signed,
    reengagement-COUPLED disengagement STATE, not a one-shot bit-flip -- a bare
    abandon-without-redirect re-derives the purpose-vacuum pathology and is
    falsified. HARD PRECONDITION: MECH-340's three failure signatures must be
    inducible and separable on the substrate, and the SD-039 trace + MECH-292
    rank-decay must be live, before any candidate gate can be discriminated; no
    experiment is queued yet and the narrowing so far is STRUCTURAL
    (biology-before-formal-definitions), not experimental -- lit_conf is a
    parallel signal and must not be read as a result.
  confidence: 0.0
  epistemic_category: substrate_conditional
  registered_utc: "2026-05-19"
  depends_on:
    - ARC-079    # the inversion that frames the question
    - MECH-340   # the gate whose signal Q-053 asks for
    - MECH-292   # rank-decay is the candidate no-erase disengage path
    - MECH-389   # the mirror (interrupted-task resumption) this Q must reconcile against -- sub-question (c), added 2026-08-08
  functional_restatement: >
    ARC-079 / MECH-340 establish THAT persistence is gated and disengagement
    is the default; they deliberately do not commit the gate signal. Q-053
    is that open question. The goal-disengagement pull
    (targeted_review_goal_disengagement/, 2026-05-19) does not answer it but
    materially NARROWS it -- as structural constraints
    (biology-before-formal-definitions), not as experimental ranking;
    exp_conf stays 0.0.

    Ruled OUT as the gate signal (each reproduces a documented pathology):
    accumulated failure / repeated suppression (learned helplessness --
    Maier & Seligman 2016; non-closeable recoverability -- Bouton 2014),
    bare effort-cost or instantaneous low value (apathy / anhedonia --
    Husain & Roiser 2018, Treadway 2012), MECH-292 staleness alone
    (incubation sign-inversion -- Jasinska 2014), and the entry's own
    within-crisis devalued value (a symptom not a cause -- Brandstatter
    2013). Ruled OUT as an operation SHAPE: a bare disengage with no
    coupled reengagement step -- Wrosch 2003 and Klinger 1975 independently
    show abandon-without-redirect reproduces a documented pathology (purpose
    vacuum / arrest in the depression phase), so whatever the signal, the
    operation it triggers must be reengagement-coupled.

    Candidate gate signals, narrowed:
    FRONT-RUNNER (this pull collapses two prior candidates into one): an
    internal control/efficacy unattainability APPRAISAL, gated on
    value-integration integrity. Wrosch 2003 fixes its input semantics (an
    appraisal of unattainability, not an accumulated-evidence tally);
    Maier & Seligman 2016 fixes its form (control/efficacy detection is the
    gated operation, persistence is what it licenses). Same mechanism from
    two sides. The vmPFC->DRN circuit is the biology but is NOT imported
    (aversive-context default; reward-context goal pursuit may not share
    polarity). DEMOTED (an input, not the gate): an explicit external/world
    invalidation signal -- it can feed the appraisal but Wrosch/Maier both
    place the licit gate internal, so it is not co-equal. GENUINELY OPEN,
    untouched by this pull: a self-model obsolescence write that marks the
    goal superseded -- neither supported nor ruled out here. Structural
    constraint on ALL candidates (Klinger 1975): the gate firing /
    not-firing is NOT a one-shot bit-flip; it opens or withholds a brief
    affectively-signed, reengagement-coupled disengagement STATE.
    Collapsing it to a single-step flag discards the stage that does the
    work. The reconciliation rule may itself be context-dependent
    (procedural vs declarative goal content).

    Open sub-questions (the prior single sub-Q is now SPLIT; the halves
    lean opposite ways): (a) Does the SD-039 TRACE need an explicit
    hard-erase / retire-from-bank op? Lean: NO -- ungated MECH-292
    rank-decay of a preserved trace matches Bouton's preserve-don't-erase.
    Unchanged by this pull. (b) Does DISENGAGEMENT itself need a dedicated,
    separable operation, distinct from tuning the MECH-292
    persistence/recoverability knobs? Lean now: YES -- Koster 2011
    (impaired-disengagement hypothesis) argues a design that gets
    disengagement 'for free' by tuning those knobs reproduces the
    rumination pathology; the disengage primitive must be its own thing.
    (Caveat: Koster's disengagement is attentional, not
    behavioural-commitment revocation -- transfer is structural shape, not
    mechanism.) These two were previously conflated under one 'lean: no',
    which made the sufficiency lean look stronger than the evidence
    supports. They are jointly consistent: the trace is preserved (a, NO)
    AND the gate is a dedicated op (b, YES).
  notes: >
    Registered 2026-05-19 from the goal-disengagement pull; NARROWED the
    same day from that pull's own synthesis (the narrowing was already
    implicit in targeted_review_goal_disengagement/ and is now reflected in
    the claim body). The narrowing is STRUCTURAL
    (biology-before-formal-definitions: divergences are constraints, not
    caveats), not experimental ranking -- the candidate set is reshaped
    exactly as the ruled-out set was, from biology; exp_conf remains 0.0
    and lit_conf is a parallel signal that is never blended. Front-runner =
    an internal control/efficacy unattainability appraisal (Wrosch input
    semantics + Maier form); external invalidation demoted to an input;
    self-model obsolescence remains genuinely open. The single hard-erase
    sub-Q was split into trace-erase (lean NO, Bouton) vs
    dedicated-disengage-op (lean YES, Koster). Following the project
    pattern of registering the genuinely-open trigger as a Q-claim rather
    than pre-committing a mechanism: the FORM is now constrained, the
    COMPUTATION of the appraisal is still open (Wrosch establishes THAT an
    appraisal triggers it, not HOW a system computes unattainability --
    that computation is precisely the function MECH-340 must implement).
    Mirror cross-reference: the interrupted-task / Zeigarnik resumption gap
    is the inverse question (when to RESUME a deferred goal); Q-053 and
    that claim must be reconciled (a goal cannot be simultaneously
    gated-for-resumption and disengaged). RECONCILED 2026-08-08 (chip
    chip-20260808-arc079-mech389-zeigarnik-crosslink): the Zeigarnik half is
    MECH-389 (registered 2026-06-10), now wired via depends_on both ways. The
    "cannot be simultaneously" premise above is NOT a state contradiction --
    MECH-389 and ARC-079/MECH-340 are compatible-but-distinct on plane (V4
    arbitrator goal SLOT vs V3 hippocampal bank ENTRY), trigger axis (agent
    capacity return vs goal-attainability appraisal) and lifecycle stage
    (within-arc suspension under a live MECH-061 commitment vs post-arc trace
    fate). Canonical statement of the reconciliation: MECH-389's notes.

    What survives is a GATE-AUTHORITY question, added here as SUB-QUESTION (c)
    alongside (a) trace-erase and (b) dedicated-disengage-op: (c) Does MECH-389's
    capacity-keyed re-elevation need to be SUBORDINATE to the MECH-340 persistence
    gate, and is a Zeigarnik urgency signal a licit persistence signal at all?
    Lean: SUBORDINATE, and the urgency signal is NOT licit as the gate itself. Two
    reasons, both structural, neither experimental. First, an unconditional
    re-elevation on capacity return is a persistence op that is not gated on
    control/efficacy, which re-derives precisely the ungated-persistence design
    ARC-079 inverts (for a goal parked long enough to have become unattainable).
    Second, and sharper: a Zeigarnik urgency term that ACCUMULATES with
    time-since-interruption is structurally the MECH-292 STALENESS signal already
    RULED OUT above (Jasinska 2014 incubation sign-inversion) -- so it may set
    re-entry ELIGIBILITY but must not act as the gate, and must not be a bare
    time-since-interruption accumulator. A secondary unknown, not yet a lean: the
    slot -> bank HAND-OFF is unspecified in both claims (when does a parked slot
    cease to be a live commitment and become a bank entry?), which is what keeps
    the lifecycle-stage separation an argument rather than a mechanism.

    Sub-question (c) is folded in here rather than minted as a new Q-claim because
    this note already asserted ownership of the reconciliation and (c) is a
    sub-case of the licit-gate-signal question. Promote it to its own Q-claim if a
    real V4 goal-deliberation build makes the hand-off concrete; do not mint one
    speculatively. Resolution path unchanged: a substrate-design decision + a
    falsifier EXQ that induces the three MECH-340 failure signatures; no experiment
    queued yet, and the (c) leans are STRUCTURAL, not results -- exp_conf stays 0.0.
  evidence_quality_note: >
    Open question, confidence 0.0 by construction. The boundaries (ruled-out
    signals AND the ruled-out operation-shape) and the candidate-set
    narrowing (front-runner / demoted / open) are firmly grounded in
    `targeted_review_goal_disengagement/` and
    `targeted_review_ghost_goal_search/`. The front-runner is a STRUCTURAL
    collapse of two prior candidates, not an evidenced ranking: it is not
    yet experimentally supported and must not be treated as confirmed. The
    split sub-Q leans (a NO / b YES) are literature leans, not results.
    lit_conf is a PARALLEL signal only and is NOT blended; this is an open
    question, not a supported claim.
  literature_anchors:
    - targeted_review_goal_disengagement
    - targeted_review_ghost_goal_search
  location: docs/architecture/ghost_goal_search.md

- id: MECH-341
  title: "e3_scoring_preserves_trajectory_class_diversity. E3 score aggregation
    over CEM-supplied candidates must preserve trajectory-class diversity rather
    than collapse a diverse candidate pool onto a single deterministic ranking.
    Without explicit diversity preservation at the scoring step, downstream
    action selection becomes deterministic regardless of proposal-layer diversity
    (ARC-065 SP-CEM child), making the whole distributed diversity pathway
    rate-limited by Layer B. Mechanism options (not yet committed): (1) entropy
    bonus over candidate first-action classes added to E3 score; (2) class-
    stratified argmax with within-class proportional sampling; (3) jittered
    tie-breaking when top-K E3 scores are within epsilon. Final design pending
    diagnostic data on whether E3 collapse is near-tie or large-gap (see
    behavioral_diversity_isolation_plan.md Phase P2)."
  claim_type: mechanism_hypothesis
  subject: ethics_engine_3.scoring_trajectory_class_diversity_preservation
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-655_2026-06-13
      as_of: 2026-06-13
      verdict: non_contributory/substrate_ceiling
  status_note: >
    [enum-comment migrated to field 2026-07-18] status: 2026-06-14 governance RATIFICATION
    (user-directed): V3-EXQ-660 binary within-class-representative preserver accepted as
    sufficient V3 evidence (exp_conf 0.871 > 0.62 gate); v3_pending released. See dated note
    below.
  implementation_phase: v3
  v3_pending: false   # 2026-06-14: cleared by governance ratification of the binary within-class preserver (660 base PASS). Promotion was held ONLY by this gate (06-11c note); released per user decision.
  pending_retest_after_substrate: false   # 2026-06-14: cleared on ratification. The committed-action CONVERSION of the preserved diversity (the 614e/660 byte-identical committed-class entropy) is NOT a MECH-341 retest -- it is the shared E3 selection-authority ceiling, tracked DOWNSTREAM on behavioral_diversity_isolation:GAP-A (modulatory-bias-selection-authority; 569f/569g diagnose-first). MECH-341's own assertion (scoring-layer diversity PRESERVATION) is established; do NOT re-block this claim on the conversion work.
  claim_level: mechanistic
  registered_utc: "2026-05-25"
  governance_note_2026_06_13b: >
    PM cycle (gov 2026-06-13T20:04Z): V3-EXQ-655's MECH-341 entry reclassified
    unknown->non_contributory (substrate_ceiling) per failure_autopsy_V3-EXQ-655_2026-06-13
    -- the 655 task-shift necessity test could not discriminate (decisive substrate-incapacity).
    Recorded so 655 is not miscounted as a weakens; MECH-341's independent experimental support
    is unaffected.
  depends_on:
    - ARC-065    # parent diversity-generation pathway -- MECH-341 is the Layer-B child
    - ARC-033    # E2_harm_s forward model -- the input to E3 scoring
    - SD-003     # counterfactual evaluation -- consumes the preserved diversity at Layer B
    - INV-076    # diversity as counterfactual prerequisite -- companion invariant
  notes: >
    Registered 2026-05-25 to close a claim-side gap exposed during the
    behavioural-diversity isolation analysis: ARC-065's distributed pathway
    commits the architecture to four substrate slots (LC-NE tonic noise,
    frontopolar curiosity, striatal novelty, hippocampal trajectory sampling),
    but the post-CEM scoring step at E3 has no corresponding diversity-
    preservation mechanism. The acceptance-criteria doc names this as an FP-4
    failure mode ("metric-only diversity / score collapse") but did not register
    a positive claim asserting that E3 must preserve. MECH-341 closes that
    asymmetry. Layer assignment: this is the Layer-B (scoring) claim in the
    isolation plan's four-layer model (proposal / scoring / action-selection /
    representation). Falsifier sequence: Phase P2 diagnostic (instrumentation-
    only run logging pre-E3 candidate class count vs post-E3 selected class
    count); P3 substrate work + B_only / ablate_B arms once a design option is
    chosen. Substrate IMPLEMENTED 2026-05-27 (options 1 + 2 togglable under one
    master per V3-EXQ-608 P2 majority R2a_e3_collapse_confirmed_large_gap
    routing); P3 behavioural validation pending V3-EXQ-611 substrate-readiness
    diagnostic.
  implementation_note: >
    Substrate landed 2026-05-27 via /implement-substrate. Module:
    ree-v3/ree_core/predictors/e3_score_diversity.py (E3ScoreDiversity +
    E3ScoreDiversityConfig). Two togglable sub-flavours under one master
    (mirroring MECH-314a/b/c precedent so Q-054 falsifier can dissociate
    which one carries the load): Option 1 entropy_bonus (per-candidate
    positive bias proportional to over-representation of the candidate's
    first-action class in the pool, composed into scores AFTER score_bias
    chain and BEFORE last_scores / softmax); Option 2 stratified_select
    (partition by first-action class, argmin within each class, softmax-
    sample across class-representatives, replaces argmin in the committed-
    path selection). Config (REEConfig.from_dims):
    use_e3_score_diversity (master, default False, bit-identical OFF) +
    use_e3_diversity_entropy_bonus (True) + use_e3_diversity_stratified_select
    (True) + e3_diversity_entropy_lambda (0.05; Q-054 calibrates) +
    e3_diversity_entropy_bias_scale (0.1) + e3_diversity_stratified_temperature
    (1.0) + e3_diversity_min_classes_for_stratification (2). Pure-arithmetic
    regulator; no learned parameters; no gradient flow; sibling pattern to
    MECH-313 NoiseFloor / MECH-314 StructuredCuriosity / MECH-320 TonicVigor.
    Validated against 506/506 contracts + 7/7 preflight with master OFF
    (bit-identical baseline). MECH-094 honored: both methods accept
    simulation_mode argument; replay/DMN paths bypass the substrate.
    Validation experiment V3-EXQ-611 queued (4-arm substrate-readiness
    diagnostic; PASS = single-option arm produces selected_classes_count >= 2
    with frac_pre_ge2 >= 0.5). Behavioural P3 B_only / ablate_B / ALL_ON arms
    + R2.c rule promotion pending V3-EXQ-611 PASS. Design doc:
    REE_assembly/docs/architecture/mech_341_e3_score_diversity_preservation.md.
    Retune 2026-05-28 (via /implement-substrate
    implement-substrate-mech-341-retune-20260528T165000Z): V3-EXQ-611
    FAILed substrate-readiness 2026-05-27T13:02Z (manifest
    v3_exq_611_mech341_substrate_readiness_4arm_20260527T130213Z_v3.json)
    -- ARM_1/3 entropy_bonus_max_abs 0.023-0.044 << observed
    mean_top2_class_gap 0.27-1.96 (substrate fires but cannot move
    selection ordering at scale=0.1); ARM_2 n_stratified_fired=0 across
    all 3 seeds because the committed branch was never entered during
    measurement and the prior implementation gated stratified_select to
    the committed path only. Retune is two-pronged: (a) module change --
    ree-v3/ree_core/predictors/e3_selector.py now consults
    stratified_select on BOTH committed and uncommitted selection paths.
    Bit-identical when score_diversity is None or sub-flag is False
    (stratified_select returns None; caller falls through to legacy
    argmin or multinomial). MECH-094 preserved via the existing
    simulation_mode kwarg. 506/506 contracts PASS post-edit. (b) parameter
    sweep -- V3-EXQ-611b queued (6-arm factorial: 3 option groups x 2
    entropy_bias_scale values 1.0/2.0). NO config defaults changed per
    implement-substrate skill rule; scales passed via per-arm
    cfg_overrides. Acceptance criteria: C1 stratified_fired > 0 across
    all OPT2/BOTH seeds; C2 entropy_bonus_max_abs >= 0.7 * scale on
    majority of seeds in entropy-ON arms; C3 selected_classes >= 2 with
    frac_pre_ge2 >= 0.5 on majority of seeds in at least one arm; R2.c
    readiness cleared by at least one arm. PASS = >= 15/18 seeds complete
    AND C1 AND (C2 OR C3). Sentinel routing: PASS routes to behavioural
    successor; FAIL with C1=false routes to /diagnose-errors on
    e3_selector.py wiring; FAIL with C1=true and C2/C3=false routes to
    algorithm-level substrate revisit. Retune 2026-05-29 (IGW-20260529-021
    via /implement-substrate): V3-EXQ-611c PASSed 2026-05-29T18:45Z
    (manifest v3_exq_611c_mech341_retune_6arm_20260529T184549Z_v3.json) with
    C1=True (stratified_select fires on all ON arms), C2=False (entropy bonus
    still clamped at bias_scale despite scale sweep to 2.0; lambda=0.05 too
    small), C3=True (all arms produce >=2 selected classes), R2c=True (all
    arms satisfy R2c readiness). Root cause: entropy_lambda 0.05 too small --
    for 90% dominant class, bonus = lambda * freq = 0.045, far below observed
    score gaps 0.27-1.96. Config defaults updated: e3_diversity_entropy_lambda
    0.05 -> 0.5 (10x), e3_diversity_entropy_bias_scale 0.1 -> 1.0 (headroom).
    Changes: ree-v3/ree_core/utils/config.py REEConfig dataclass defaults +
    from_dims signature defaults, ree-v3/ree_core/predictors/e3_score_diversity.py
    E3ScoreDiversityConfig dataclass defaults + build_from_ree_config fallback
    defaults. 543/543 contracts PASS post-change (bit-identical when master
    OFF). V3-EXQ-611c interpretation grid routes to behavioural successor
    (V3-EXQ-614 queued 2026-05-29; 3-arm B_only/ablate_B/ALL_ON per
    behavioral_diversity_isolation_plan.md R2.c rule).
  evidence_quality_note: >
    No experimental evidence yet (substrate just landed 2026-05-27).
    Architectural argument grounded in (a) the ARC-065 distributed-pathway
    commitment, (b) the acceptance-criteria doc's Rung 1 FP "diversity in
    candidates that collapses to one class after E3 scoring", and (c) the
    prior 2026-05-25 4-theory analysis identifying Layer B as the highest-
    residual-leverage gap. V3-EXQ-608 P2 majority R2a finding empirically
    confirmed the gap (mean_top2_class_gap 0.27-0.60 with frac_pre_ge2=1.0
    pre-CEM) and ruled out option 3 jittered tie-breaking; substrate landed
    with options 1+2 togglable. Promotion path now requires V3-EXQ-611
    substrate-readiness PASS plus R2.c rule from
    behavioral_diversity_isolation_plan.md (B_only arm produces
    trajectory_class_count >= 2 with first_action_entropy > 0.3).


    [2026-05-30 governance / autopsy V3-EXQ-569c applied]: V3-EXQ-569c (2026-05-30) tested SD-056 against ARC-065 GAP-A R1.b as a matched-entropy FP-2 falsifier. Headline result: ON arms (sd056_weight 0.01/0.05/0.20) produce selected_action_entropy 0.875/0.833/0.951 vs matched-noise control (ARM_4 T=2.5) at 0.414 -- ~2.4x above the FP-2 control on the C3 acceptance axis. Pre-registered C1 floor was a calibration miss (0.05 set against V3-EXQ-613 standalone magnitude 0.286 rather than behavioural-runtime ~0.045); ON arms cluster at 3x bit-identical noise (0.0148), confirming the substrate is operative. Per /failure-autopsy V3-EXQ-569c 2026-05-30: routed to /queue-experiment for V3-EXQ-569d (recalibrated C1 floor + e3_top2_class_gap NaN fix) and V3-EXQ-569e (mechanism probe dissociating per-candidate-variance propagation vs E2-rollout-dynamics shift as source of the C3 lift). Evidence direction for MECH-341 supports preserved per the matched-noise control clearance on C3. MECH-341 stays candidate / v3_pending=true pending V3-EXQ-569d+569e returns.

    [2026-05-31 governance: V3-EXQ-614a + V3-EXQ-569d PASS; V3-EXQ-569e
    diagnostic FAIL flagged for /failure-autopsy]:
    (i) V3-EXQ-614a PASS (2026-05-30T19:32Z, supersedes V3-EXQ-614)
        Phase-P3 behavioural falsifier 3-arm, label
        PASS_C2_C3_only_mech341_load_bearing_in_stack_only per
        behavioral_diversity_isolation_plan R2.c rule;
        evidence_direction_per_claim[MECH-341]=supports. Mech341
        load-bearing in the full stack only (B_only arm does NOT
        independently produce trajectory_class_count >= 2).
    (ii) V3-EXQ-569d PASS (2026-05-31T05:36Z, supersedes V3-EXQ-569c)
        SD-056 matched-entropy FP-2 falsifier with C1 floor
        recalibrated 0.05->0.03 per autopsy V3-EXQ-569c Section 5;
        all three C-criteria PASS;
        evidence_direction_per_claim[MECH-341]=supports.
        V3-EXQ-569c manifest set evidence_direction=superseded per
        supersession policy.
    (iii) V3-EXQ-569e FAIL (2026-05-31T00:49Z) was registered as
        experiment_purpose=diagnostic (Pathway A vs B mechanism
        dissociation probe). Per skill rule, diagnostic FAILs do not
        weight evidence_direction. Flagged for /failure-autopsy.
        MECH-341 evidence direction not weighted by 569e this cycle.
    MECH-341 stays candidate / v3_pending=true pending mechanism-
    dissociation autopsy interpretation; promotion gate held until
    569e dissociation is resolved and PASS evidence accumulates across
    additional ARMs.

    [2026-05-31 midday governance, V3-EXQ-569e autopsy applied]:
    V3-EXQ-569e Pathway A vs Pathway B mechanism probe verdict
    INSTRUMENTATION_FAILURE per failure_autopsy_V3-EXQ-569e_2026-05-31.json.
    M3 / M5 channels intended to dissociate E3-aggregation-collapsed-upstream-
    variance vs per-candidate variance propagation were saturated by SD-056
    numerical instability (E2 rollouts 1e16+, NaN-fraction 0.72-1.00 on
    ON-arm seeds at behavioural-runtime episode length). Substrate
    implementation pathology, not architectural falsification.
    evidence_direction_per_claim[MECH-341]=mixed (diagnostic, non-weighting
    per Phase-3 governance rules). MECH-341 substrate-side reading from
    V3-EXQ-611c retune PASS (2026-05-29) + V3-EXQ-614a PASS_C2_C3_only stack
    reading + V3-EXQ-569d matched-entropy PASS remain valid; 569e adds no
    new MECH-341 evidence in either direction. SD-056 multi-step rollout
    stability amend landed 2026-05-31 11:25Z (V3-EXQ-617 substrate-readiness
    queued); MECH-341 stays candidate / v3_pending=true pending the SD-056
    amend-and-re-run cycle on the corrected substrate.

    [2026-05-31 evening governance: cluster autopsy applied; role
    characterised as score-layer preserver, not in-isolation generator]:
    Per failure_autopsy_MECH-341-cluster_2026-05-31 (status=confirmed) and
    failure_autopsy_V3-EXQ-614b_2026-05-31 (status=confirmed). Two new
    manifests reclassified non_contributory on MECH-341:
    (i) V3-EXQ-614b (Phase P3 behavioural falsifier on SD-056-amended
        substrate). C1=False (B_only ARM_0 frac_pre_ge2=0.0; structural
        CEM proposer collapse without SP-CEM upstream, identical to 614a);
        C2=False at 0.087 vs 0.1 threshold (near-miss); C3=True at ALL_ON
        0.800 nats (highest of any 614-lineage run). SD-056 amend lifted
        ARM_1 ablate_B by +0.187 nats vs ARM_2 ALL_ON +0.116 nats;
        MECH-341 marginal contribution shrunk 0.158->0.087 because the
        upstream cluster (SP-CEM + V_s + noise_floor + amended E2
        action-conditional divergence) is doing more of the diversity
        work at the candidate-pool layer. Not a falsification.
    (ii) V3-EXQ-616 (Q-054 entropy_bias_scale sweep on B_only isolation,
        scales {1.0, 2.0, 4.0, 8.0}). Bit-identical per-seed results
        across all four scales (frac_pre_ge2=0.0, n_unique=1, entropy=0.0
        universally). Mathematical proof that uniform additive bias
        cannot move a single-class CEM proposer output. In-isolation
        testing of MECH-341 through the entropy_bias_scale lever is
        structurally not reachable.
    Cluster reading: MECH-341 is correctly characterised as a
    SCORE-LAYER PRESERVER of upstream-supplied candidate-pool diversity,
    NOT an in-isolation diversity generator. The structural bound on
    in-isolation Rung-1 clearance is at the proposer layer (SP-CEM
    presence/absence), not at the score-layer knob. Load-bearing
    evidence for MECH-341 is in-stack ALL_ON across V3-EXQ-614a (0.684
    nats), V3-EXQ-614b (0.800 nats), V3-EXQ-615 (1.111 nats); the claim
    text stays intact. Process flag: C2 necessity_delta_threshold=0.1
    in the 614-lineage script was calibrated pre-SD-056-amend and must
    be revisited before any V3-EXQ-614c successor (autopsy does not pin
    a new value). MECH-341 stays candidate / v3_pending=true; promotion
    gate held pending in-stack behavioural-runtime evidence on the
    SD-054 / multi-resource substrate cohort.


    [2026-06-01 implement-substrate amend landed per failure_autopsy_V3-EXQ-616
    Sections 7 + 10 contingent-on-614b-FAIL-C1 path]: MECH-341 substrate
    amend (failure_autopsy_V3-EXQ-616 named contingent action
    "stratified_temperature default + A-vs-B partial-redundancy probe")
    landed via /implement-substrate session
    implement-substrate-mech-341-amend-stratified-temp-ab-probe-20260601T063226Z.
    Two-part amend:
    (a) E3ScoreDiversity gains stratified_within_class_temperature:
        Optional[float] = None lever. When set, samples a within-class
        representative via softmax(-class_scores / T) before the existing
        across-class softmax step. Bit-identical legacy argmin when None.
        Decoupled from existing across-class stratified_temperature
        (default 1.0; unchanged) so V3-EXQ-614c sweep can dissociate
        within-class and across-class layers.
    (b) A-vs-B partial-redundancy probe lever satisfied by existing
        independent flags use_support_preserving_cem (Layer A) and
        use_e3_score_diversity (Layer B). The autopsy's "config flag"
        requirement is met by the existing composition (A_only / B_only /
        BOTH / NEITHER); no new code flag added. The amend's contribution
        is naming the probe pattern + pinning acceptance criteria in
        V3-EXQ-614c.
    655/655 contracts + 7/7 preflight PASS with master OFF and amend OFF
    (regression-clean; was 645 + 10 new MECH-341 amend contracts in
    tests/contracts/test_mech_341_stratified_temperature_amend.py). NO
    flip of use_differentiable_cem default (SD-055 safety note preserved).
    Validation experiment V3-EXQ-614c queued 2026-06-01 (4-arm
    within-class temperature sweep {None=legacy, 0.5, 1.0, 2.0} on
    SD-056-amended baseline; acceptance C1 regression guard + C2
    within-class lift or C3 substrate-readiness). Cross-plan: amend
    transitively unblocks arc_062_rule_apprehension:GAP-B (V3-EXQ-543l
    successor cohort) under shared SD-056-amended substrate. MECH-341
    stays candidate / v3_pending=true pending V3-EXQ-614c outcome.

    [2026-06-01 governance: V3-EXQ-614c non_contributory (instrumentation_defect)]: V3-EXQ-614c (within-class
    temperature sweep) reclassified weakens->non_contributory per confirmed
    failure_autopsy_V3-EXQ-614c_2026-06-01. Both failing criteria are test-design defects: C2 vacuous (reported
    selected_class_entropy measured at the score-layer argmin, temperature-invariant by construction; within-class
    firing diagnostics not recorded); C1 mis-specified (per-seed band vs cross-seed mean; ARM_0 cross-seed mean
    0.7999 ~= 0.800 = no regression, but 0/3 seeds in band). C3 substrate-readiness PASS 3/3 on all arms
    (substrate fires). instrumentation_defect, not substrate_ceiling. Corrected re-run queued as V3-EXQ-614d.
    MECH-341 stays candidate / v3_pending=true. NOTE: V3-EXQ-610b dropped its mis-applied MECH-341 tag (not under
    test in a crystallization-necessity experiment).

    [2026-06-03 review V3-EXQ-614d, diagnostic] Within-class sampling temperature (MECH-341 stratified_select sub-flavour) was tested at T=0.5/1.0/2.0 vs legacy argmin for COMMITTED-action class diversity. PASS on C1 (no regression vs 614b ALL_ON band; cross-seed selected-entropy 0.80) and C3 (substrate-readiness: within-class branch fires 3/3 seeds, 159-1030 samples/seed) -- but C2 FAILED: committed-class entropy is byte-identical across all three temperatures (1.056572 at T=0.5/1.0/2.0; legacy 1.057734), paired-lift seed count 0/3 in every arm. The within-class lever is ACTIVE but has ZERO authority at committed-action selection -- a THIRD convergent instance of the modulatory-bias-selection-authority gap (cf. 604a curiosity_bias, 624a vigor: scoring-layer signal fires but never changes the committed argmax). Diagnostic (excluded from confidence/conflict scoring); MECH-341 stays candidate / v3_pending (within-class temperature alone does not deliver committed diversity). Routes to the modulatory-bias-selection-authority substrate (now ready=true; MECH-341 added to its unblocks_claims). Supersedes 614c (non_contributory).

    [2026-06-03 substrate landing] modulatory-bias-selection-authority IMPLEMENTED (ree-v3 e3_selector.select gap-relative rescale of the combined modulatory contribution to modulatory_authority_gain*raw_score_range + e3_score_diversity.stratified_select across-class unit-range normalization; config use_modulatory_selection_authority default OFF/bit-identical). Gives this claim's score-bias lever genuine BOUNDED authority at E3 committed selection (the 604a/624a/614d cluster showed the lever fired but never changed the committed argmin). 614d within-class temperature fired 3/3 seeds but committed-class entropy was byte-identical across T=0.5/1.0/2.0 (C2 FAIL) -- the across-class stratified softmax unit-range normalization is the direct fix. Claim STAYS candidate/v3_pending -- substrate-readiness validation EXQ pending (claim_ids=[]); a per-claim EVIDENCE retest on the authority-ON substrate is the governance-weighting signal. Design doc docs/architecture/modulatory_bias_selection_authority.md; substrate_queue modulatory-bias-selection-authority status implemented_pending_validation.

    [2026-06-07 autopsy V3-EXQ-614e applied (failure_autopsy_V3-EXQ-614e_2026-06-07, confirmed)]: V3-EXQ-614e (within-class temperature {None,0.5,1.0,2.0} CLEAN RETEST with the V3-EXQ-643a-validated modulatory-bias-selection-authority substrate ON, supersedes 614d) FAILed C2 (committed_class_entropy byte-identical 1.033608 across T=0.5/1.0/2.0, 0/3 paired-lift seeds) with C1 substrate-operative=True and C3 readiness=True. Adjudicated non_contributory (NOT weakens) for MECH-341: this is a substrate_ceiling, not a falsifier of the within-class sub-axis. (1) The lever IS operative -- committed entropy shifted off legacy (1.035689 -> 1.033608), so the within-class representative reaches committed selection; but it is byte-identical across a 4x temperature range, i.e. the committed-CLASS distribution is insensitive to which within-class representative wins. (2) MECH-341 preserves trajectory-CLASS diversity (the across-class axis); within-class temperature is a within-class sub-axis tested against an across-class readout under a monostrategic candidate pool, so a committed-class lift is structurally impossible here. (3) This is the 4th convergent committed-action no-lift instance on behavioral_diversity_isolation:GAP-B (604a curiosity, 624a vigor, 614d within-class temp, 614e), but the first with authority PROVEN operative -- so the bottleneck has moved upstream to GAP-A candidate-pool class collapse (cand_world_pairwise_dist=0.0000). The ARC-065 GAP-A E2-world-forward per-candidate signal preservation substrate (shared cand_world_summaries e2.world_forward source) was IMPLEMENTED 2026-06-07 in response (ree-v3 candidate_summary_source flag; substrate-readiness validation V3-EXQ-649 queued). MECH-341 stays candidate / v3_pending=true; pending_retest_after_substrate gated on ARC-065/GAP-A candidate-pool class diversity. The MECH-341 committed-class diversity re-test must use a within-class-REPRESENTATIVE-diversity readout (NOT committed-class entropy -- autopsy Learning #2). No confidence move; MECH-341 not weakened. 614e manifest evidence_direction already set non_contributory + index rebuilt at /governance 2026-06-07 (MECH-341 = 2 supports:0 weakens, exp_conf 0.809).

    [2026-06-10 governance: V3-EXQ-660 supports, v3_pending HELD]: V3-EXQ-660
    (within-class-REPRESENTATIVE-diversity retest, supersedes V3-EXQ-614e) is the
    corrected-readout retest the 614e autopsy Learning #2 required -- it scores
    H(rep_signature | committed_class) [which within-class representative is
    selected inside the committed class] instead of committed-class entropy, run
    on the GAP-A (candidate_summary_source=e2_world_forward) + modulatory-
    selection-authority (gain 0.5) substrate. Outcome PASS / supports: C1
    substrate-operative + within-class branch active + multi-rep available
    (majority); C2 within-class-representative-diversity lift PASS (swept 4.862
    vs legacy 4.781 nats). The committed-CLASS (across-class) entropy stays flat
    -- the EXPECTED negative control (the within-class lever does not act on the
    across-class axis; secondary, not a gate). GOVERNANCE CAVEATS (why v3_pending
    is HELD not cleared this cycle): (1) the lift margin is THIN (~0.08 nats vs
    0.05 threshold); (2) the three temperature arms T=0.5/1.0/2.0 produce
    BYTE-IDENTICAL within-class entropy (4.861916) -- the lever fires (196/1105/255
    samples) but is INSENSITIVE to temperature magnitude; only legacy-vs-any-sweep
    differs. So 660 confirms the within-class sub-axis is load-bearing (representative
    selection reaches committed action) but does not yet show a graded, ratifiable
    response. Per the manifest interpretation grid: record supports; do NOT auto-flip
    the within-class default; v3_pending gate retained pending governance ratification
    + a temperature-graded confirmation. No status change (stays candidate); no
    promotion. 660 supersedes 614e (which was already non_contributory).
    [2026-06-11 governance cycle #5: V3-EXQ-660b non_contributory, graded-in-K
    falsifier RETIRED, NOT weakened]: Applied confirmed failure_autopsy_V3-EXQ-660b_2026-06-11.
    V3-EXQ-660b (windowed-readout redesign of 660a, supersedes 660a) FAILed the load-bearing
    C_GRADED 0/3 seeds and self-routed weakens, but is reclassified non_contributory
    (measurement_test_design_defect) on both the flat and nested run-pack manifests
    + narrow_supports_flag + scoring-excluded. Two compounding reasons the weakens does not
    hold: (1) the readout-sensitivity readiness gate cleared only MARGINALLY (sampled
    windowed-H range across K = 0.0568 vs 0.05 floor) and NON-MONOTONICALLY (windowed-H by K
    = 0.987/1.044/1.020/1.037 -- the positive control does not cleanly track K), so a flat
    sampled-legacy lift is not a clean negative; (2) graded-in-pool-size OVER-SPECIFIES
    MECH-341, which asserts diversity PRESERVATION (binary preserve-or-collapse), not a
    K-graded dose-response. Three convergent iterations (660 inert temperature -> 660a
    saturated -> 660b marginal/non-monotone) RETIRE the graded-in-K falsifier: no 660c.
    The within-class lever is recorded as a BINARY load-bearing preserver, with V3-EXQ-660
    (PASS/supports, within-class lift 4.862 vs legacy 4.781) the claim-faithful established
    result that stands. No substrate_queue entry (input availability rose correctly,
    4.99->33.84 across K; not a substrate gap). No demotion; MECH-341 stays candidate /
    v3_pending / pending_retest_after_substrate. ALSO CORRECTED a latent scoring bug: the
    NESTED run-pack manifest for V3-EXQ-660a still read weakens (an earlier cycle applied
    the 660a non_contributory reclassification to the flat manifest only; the indexer scores
    from the nested runs/**/manifest.json), so 660a was still counted as a genuine weakens.
    Mirrored the already-decided 660a reclassification onto its nested manifest. Net: MECH-341
    genuine_exp weakens 2 -> 0, supports 3 (569d/614a/660). behavioral_diversity_isolation:GAP-B
    owner_exq re-pointed 660b -> 660 (the standing PASS); graded ratification removed as a gate,
    not outstanding. narrow_supports_flag: MECH-341's supports remain stack-only / single-pathway
    -- the within-class GRADED sub-axis is unestablished and retired; non_contributory is NOT
    graded-axis clearance.
    [2026-06-14 governance RATIFICATION (user-directed) -- v3_pending CLEARED, candidate -> provisional]:
    Governance had HELD v3_pending across cycles (06-10 .. 06-11c) pending "governance ratification
    + a temperature-graded confirmation". The graded-confirmation route is now RETIRED (660a/660b both
    FAILed and were reclassified non_contributory; failure_autopsy_V3-EXQ-660b removed graded-in-K as a
    gate because it over-specifies a PRESERVATION claim -- no 660c). The only remaining item was the
    ratification DECISION, surfaced to the user 2026-06-14 (AskUserQuestion). DECISION: RATIFY the binary
    within-class-representative preserver NOW. Basis: V3-EXQ-660 base PASS/supports established the lever
    (within_class_rep_cond_entropy 4.862 vs legacy 4.781 nats); exp_conf rose 0.794 -> 0.871 (> the 0.62
    candidate->provisional gate); genuine_exp = 3 supports : 0 weakens after the 660a/660b nested-manifest
    scoring-bug fix. v3_pending was the SOLE remaining hold (06-11c). Cleared -> provisional.
    EXPLICIT CAVEAT carried forward (not lost, just not gating THIS claim): MECH-341 asserts scoring-layer
    diversity PRESERVATION, which 660 shows is load-bearing. It does NOT assert that the preserved diversity
    reaches the COMMITTED action -- the committed-class entropy stayed byte-identical (614e/660), which is the
    SHARED E3 selection-authority ceiling (the "decorative channel" finding, confirmed
    failure_autopsy_569f-661-654a_2026-06-10) blocking ARC-065 GAP-A. That conversion work is tracked on
    behavioral_diversity_isolation:GAP-A (modulatory-bias-selection-authority; 3 readiness-validated amends
    06-03/06-06/06-10, behavioural conversion still 0; routed diagnose-first 2026-06-14 via /failure-autopsy
    V3-EXQ-569g + an in-arm route-range collapse diagnostic). When that conversion lands, a fresh behavioural
    confirmation MAY further strengthen MECH-341, but it is NOT required for the present provisional status:
    the diversity-PRESERVATION assertion is the ratified scope. No demotion of any sibling; ARC-065 / ARC-062
    unchanged (still candidate / v3_pending on the same GAP-A ceiling).
  literature_anchors:
    - "Rigotti et al. (2013) Nature 497:585 -- mixed selectivity in PFC encodes
       diverse trajectory contingencies; preservation across scoring layers is
       required for downstream behavioural flexibility."
    - "Padoa-Schioppa & Conen (2017) Neuron 96:736 -- OFC value comparison
       preserves option-distinct value signals through the comparison stage;
       collapse to single value rank is pathological."
  location: evidence/planning/behavioral_diversity_isolation_plan.md

- id: MECH-342
  title: "control_plane.commit_maintenance_release. The R-c readiness signals
    that gate commitment ENTRY (MECH-090: score_margin decisiveness +
    nav_competence) must ALSO be able to RELEASE an already-elevated beta
    latch when execution readiness degrades mid-commitment. The release is a
    graded, bounded-accumulation drift-to-a-release-bound (conflict-scaled by
    the deficit magnitude), NOT a one-shot threshold flag; targeted to the
    active committed motor program (not a global brake); and hysteretic with a
    reengagement path that guards the premature-abort pole. This closes the
    V3-EXQ-592f reach gap (the MECH-090 R-c conjunction is admission-only by
    design; no existing release pathway covers degraded-readiness-mid-
    commitment)."
  claim_type: mechanism_hypothesis
  subject: control_plane.commit_maintenance_release
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-732_2026-07-10
      as_of: 2026-07-10
      verdict: non_contributory/precondition_unmet
  implementation_phase: v3
  v3_pending: true
  pending_retest_after_substrate: false  # cleared /governance 2026-08-12: MECH-342's own substrate_queue entry is implemented_validated_v3_exq_592g and no closure node names this claim; the retest had no live target. v3_pending gate still holds.
  claim_level: mechanistic
  registered_utc: "2026-06-02"
  governance_note_2026_06_13: >
    V3-EXQ-629b reclassified non_contributory (substrate_not_ready_requeue,
    pending_retest, narrow_supports) per confirmed
    failure_autopsy_SD-034-closure-cluster-ext_2026-06-12: foraging contact-guard
    held on only 1/3 seeds (seeds 42/44 z_goal_at_contact_peak 0.0/0.371 < 0.4).
    On the ONE competent seed (43) MECH-342 maintenance-release FIRED correctly
    (decommit_transitions=34, mech342_fires=1, beta occupancy 1.0->0.217 in the
    degraded window vs OFF 1.0) -- a narrow single-seed POSITIVE. The failure is
    upstream foraging incompetence (nav-competence not transferring to the 629b
    ecological harness), NOT a MECH-342 defect. Retest after the
    scaffolded_sd054_onboarding nav-competence leg clears on >= 2/3 seeds. Not a
    falsification.
  depends_on:
    - MECH-090   # commit-entry R-c admission conjunction (beta latch + CommitReadiness signal)
  notes: >
    Registered 2026-06-02 as the B3b substrate concluded by the MECH-090
    release-path audit (evidence/planning/mech090_release_path_audit_2026-06-02.md;
    B1 ruled out -- none of ARC-028/MECH-105 hippocampal-completion, MECH-091
    urgency-interrupt, V_s commit-release MECH-269/284/287, or SD-034 closure
    covers degraded-readiness mid-commitment) and the motor-program-cessation
    lit-pull (evidence/literature/targeted_review_mech_090_release_motor_cessation/
    SYNTHESIS.md; verdict B3b). This is autopsy option (a) resurfacing, but the
    biology (Resulaj 2009 changes-of-mind bounded accumulation; Cavanagh/Frank
    2011 conflict-graded STN dynamic threshold; Falasconi/Arber 2025 BG-output
    movement-specific suppression; Wessel 2022 non-selective stop-signal as the
    WRONG model) adds two binding constraints the naive option-(a) sketch
    lacked: (1) graded/online, not a Schmitt flag; (2) targeted + hysteretic
    with reengagement. Distinct from MECH-090 admission (entry-only, AND-
    composed; MECH-342 is maintenance, OR-composed -- the De Morgan dual),
    MECH-091 (threat axis; MECH-342 fires with z_harm_a BELOW threshold),
    ARC-028 completion (options GOOD; MECH-342 fires when completion_signal is
    LOW), MECH-269b/V_s (schema staleness; MECH-342 fires with a STABLE schema),
    MECH-340 (goal-level ghost-goal bank timescale; MECH-342 is the active beta
    latch at motor-program timescale). Does NOT reopen the MECH-090 admission
    axis (V3-EXQ-592d 4-arm validator remains live). lit_conf 0.69 is a parallel
    signal (lit/exp decoupled); it does not weight MECH-090 or MECH-342
    experimental confidence.
  implementation_note: >
    Substrate landed 2026-06-02 via /implement-substrate. Module:
    ree-v3/ree_core/policy/commit_maintenance_release.py (CommitMaintenanceRelease
    + CommitMaintenanceReleaseConfig). Pure-arithmetic regulator (no nn.Module,
    no learned params); sibling to commit_readiness.py (the admission-side R-c
    signal). Per maintenance tick (only while beta is elevated):
    deficit_d = clip((score_margin_floor - score_margin)/score_margin_floor, 0, 1);
    deficit_n = clip((nav_floor - nav_competence)/nav_floor, 0, 1);
    combined = max(deficit_d, deficit_n);
    if combined>0 pressure += accumulation_rate*combined (drift-to-bound);
    elif both axes recovered above their reengage levels pressure -= leak_rate
    (reengagement); else hold (dead-band); fire when pressure >= release_bound.
    On fire: beta_gate.release() + reset _committed_step_idx + clear
    _committed_anchor_keys + clear e3._committed_trajectory. Release branch in
    REEAgent.select_action after the MECH-091 urgency block (template mirrored);
    reads decisiveness from e3.last_scores and nav from
    commit_readiness.get_readiness(). Config (REEConfig.from_dims, all default
    no-op): use_maintenance_release (False, bit-identical OFF) +
    maintenance_release_score_margin_floor (0.05) / _score_margin_reengage (0.10)
    / _nav_floor (0.3) / _nav_reengage (0.5) / _accumulation_rate (0.2) /
    _leak_rate (0.1) / _bound (1.0) / _pressure_cap (1.5). MECH-094:
    tick(simulation_mode=True) is a no-op. 685/685 prior contracts + 15 new
    MECH-342 contracts (tests/contracts/test_mech_342_commit_maintenance_release.py)
    PASS; bit-identical OFF verified (agent.maintenance_release is None). NO
    flag/confidence/promotion change to MECH-090 (admission axis intact). Status
    stays candidate / v3_pending pending V3-EXQ-592g maintenance-release
    validation probe; the V3-EXQ-592f manifest's pending_retest_after_substrate
    stays TRUE until 592g validates the maintenance-time release.
  validation_note: >
    2026-06-02: V3-EXQ-592g
    (v3_exq_592g_mech342_maintenance_release_probe_20260602T163547Z_v3) PASSED --
    the maintenance-release coupling fires as designed on the controlled
    state-machine probe. All six acceptance criteria pass: C1 forced baseline
    (beta/E3 occupancy 1.0), C2 score-margin release authority (decommit
    transitions=2, suppression beta/e3 0.4, mech342_fires=1), C3 nav-competence
    release authority (decommit=2, suppression 0.6, mech342_fires=1), C4
    conjunction authority now STRICTLY positive (max_drop 0.6 -- the quantity
    592f could only PASS vacuously at 0), C5 no false abort (A/E baseline+recovery
    decommit=0, beta re-elevates on recovery), C6 no-vacuity (all forced predicates
    crossed their floors). This closes the V3-EXQ-592f reach gap; the 592f manifest
    has been re-tagged non_contributory and its pending_retest_after_substrate
    cleared to false. DISPOSITION: status stays candidate / v3_pending. 592g is a
    controlled diagnostic state-machine probe (experiment_purpose=diagnostic), which
    validates the substrate behaves as designed but is NOT ecological evidence; and
    the V3-pending gate forbids promotion regardless of evidence count. Promotion to
    provisional/active requires an ecological, evidence-grade experiment showing the
    coupling improves agent behaviour. No flag/confidence change to MECH-090.
  what_would_answer: |
    The substrate's internal correctness is ALREADY CONFIRMED and is not what this
    falsifier tests. V3-EXQ-592g (forced state-machine diagnostic) passed all six
    acceptance criteria -- including C4, the conjunction criterion 592f could only
    pass VACUOUSLY, now strictly positive (max_drop 0.6) -- so "does the pressure
    accumulator/release-bound machinery fire as coded" is closed. What remains open
    is whether the mechanism, once ecologically triggered, produces adaptive
    behaviour -- promotion to provisional/active requires exactly that: an
    ecological, evidence-grade experiment, not another diagnostic probe.
    NON-DEGENERACY PRECONDITION (else substrate_not_ready_requeue /
    measurement_test_design_defect, NOT a verdict): a run only tests MECH-342 if it
    first establishes GENUINE sustained commitment (beta latch elevated on real
    nav-competence/foraging engagement, not a forced state) in >= 2/3 seeds, AND the
    readiness-degradation manipulation (score_margin and/or nav_competence actually
    crossing their release-triggering floors mid-commitment) is verified non-vacuous.
    Two prior ecological attempts failed this precondition, not the mechanism:
    V3-EXQ-629b (2026-06-12) cleared genuine commitment + degradation on only 1/3
    seeds (foraging contact-guard held on seed 43 only); V3-EXQ-629c (autopsied
    2026-07-24) failed it entirely (contact_rate 0, 3 gates false) and was
    categorised measurement_test_design_defect, explicitly NOT a substrate_ceiling
    hit, precisely because the ecological scenario never materialised. A future
    retest must confirm >= 2/3 seeds clear the same nav-competence/contact floor
    629b used before treating any release/non-release result as informative. Note
    also the adjacent, still-open frozen_z_goal_scaffold_family finding
    (2026-07-27): the 629b/629c test harness (ScaffoldedSD054OnboardingScheduler)
    belongs to a scaffold family with a documented frozen-z_goal measurement-phase
    issue; that audit confirmed 629b/629c's own non-scoring status stands (nothing
    to reopen), but a retest inheriting the same harness should re-verify the goal
    state is live/unfrozen as intended for the arm being measured, not assume it.
    CONFIRMING signature: once the precondition above is cleared, commit_maintenance
    _release fires (mech342_fires > 0) at the point score_margin and/or
    nav_competence cross their release floors; the release is TARGETED (only the
    degraded committed program is released -- unrelated ongoing behaviour is
    unaffected, mirroring 592g's C5 no-false-abort criterion but measured
    ecologically); and agent behaviour measurably improves relative to a matched
    ON/OFF ablation over the same degradation episodes (e.g. reduced
    wasted/hazardous persistence in a now-unwinnable commitment) without an increase
    in premature aborts on a matched no-degradation/recovery control arm.
    FALSIFYING signature: in a run that clears the non-degeneracy precondition
    (real commitment established, real degradation occurs, adequately powered),
    the coupling either (a) fails to fire when the deficit crosses its floor -- a
    true miss, not a precondition failure; (b) fires but produces no measurable
    behavioural benefit over the OFF ablation -- correct substrate, ecologically
    inert; or (c) causes harm the OFF arm does not show -- premature abort of a
    still-viable commitment, non-targeted release bleeding into unrelated ongoing
    programs, or oscillatory re-commit/release churn. Any of these, replicated on a
    properly-powered ecological run (i.e. not a repeat of the 629b/629c
    precondition-failure mode), falsifies the ecological-benefit claim without
    reopening the already-closed diagnostic-correctness question from 592g.
  location: docs/architecture/mech_342_commit_maintenance_release.md

- id: MECH-343
  title: "difficulty_gated_proposal_entropy (controlled proposal-entropy widening under goal blockage). When goal progress stalls -- repeated failure to improve goal progress, high inter-candidate conflict, low E3 score margin, low committed-action diversity, plateau/stuck-state signal -- WITH goal salience preserved, the agent should transiently INCREASE proposal-generation entropy UPSTREAM of action selection (wider hippocampal/world-model rollout candidate set + higher within-class sampling temperature) while preserving goal/harm/constraint scoring and DELAYING commitment until one candidate clears the E3 score-margin / commitment threshold; entropy then NARROWS once a workable candidate is found -> committed action -> entropy decay. Asserted distinction (preserve this): a hard problem must trigger WIDER INTERNAL PROPOSALS -> more candidate diversity -> longer arbitration -> selective committed action (controlled entropy), NOT 'hard problem -> random behaviour' (noise). WORKING HYPOTHESIS -- no experimental evidence yet; not an established mechanism."
  claim_type: mechanism_hypothesis
  subject: control_plane.difficulty_gated_proposal_entropy
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  epistemic_category: substrate_conditional
  registered_utc: "2026-06-03"
  depends_on:
    - ARC-018    # hippocampal rollouts -- the proposal-generation locus that widens
    - ARC-065    # behavioural-diversity generation pathway
    - MECH-341   # E3 scoring preserves candidate-class diversity (narrowing/arbitration side; V3-EXQ-614d)
    - MECH-314   # structured-curiosity bonus -- a candidate upstream-widening signal
    - MECH-090   # commitment-entry predicate / score-margin admission (threshold gate)
    - MECH-342   # maintenance/commitment release authority
    - ARC-003    # E3 commitment / trajectory authority
    - SD-061     # difficulty-gated proposal-entropy regulator -- builds blocker part 2 (IMPLEMENTED 2026-06-19)
  notes: >
    Registered 2026-06-03 as a WORKING HYPOTHESIS (user-proposed), not an
    established mechanism. Names a closed-loop control policy that couples
    stuck-state detection to a transient gain on PROPOSAL-layer entropy,
    held under goal/harm constraints, with commitment deferred until a
    candidate clears threshold and entropy decayed after success. Loop:
    stuck-state signal -> proposal-entropy gain (ARC-018 hippocampal/CEM
    candidate-set widening + within-class temperature) -> goal/harm scoring
    preserved -> E3 arbitration over the widened pool (MECH-341 preserves
    class diversity at scoring) -> commitment only when score margin clears
    (MECH-090/MECH-342) -> entropy decay on goal progress. Biological
    reading: increased neural variability / network flexibility during hard
    cognitive work (controlled entropy, not noise). Distinct from a state-
    independent noise floor (MECH-313) and from constant high temperature:
    the entropy gain is DIFFICULTY-GATED and time-limited. Pathological
    signatures (cross-referenced in docs/architecture/psychiatric_failure_modes.md):
    entropy up but behaviour unchanged = selection-authority failure (the
    current modulatory-bias-selection-authority bottleneck); entropy up and
    behaviour random/harmful = arbitration failure; entropy fails to rise
    when stuck = proposal-generation rigidity; entropy rises but never
    narrows = manic/disorganised-exploration analogue; proposals widen but
    commitment never occurs = catatonic/abulic action-release analogue.
    Main falsifiable prediction tracked as Q-056: a STUCK-GATED entropy arm
    should beat BOTH entropy-off (rigidity/stuck) and always-high entropy
    (disorganised churn). V3-EXQ-614d / MECH-341 tests only whether within-
    class temperature can lift COMMITTED-action diversity (one piece -- can
    controlled variability reach committed action vs stay trapped upstream);
    it does NOT test the whole loop. Connected failures: MECH-314/MECH-320
    (modulatory signals exist but lack selection authority at E3), MECH-342
    (no natural commit when score margins are flat -- V3-EXQ-629), z_goal/
    foraging cluster (goal reps need ecological contact before guiding
    behaviour -- 603e). One candidate route from catatonic-like inert
    cognition toward controlled exploratory cognition.
  evidence_quality_note: >
    conf=0.0 by construction (working hypothesis, no experiment run).
    epistemic_category=substrate_conditional: blocked on upstream substrate
    not yet built -- (1) the modulatory-bias-selection-authority gap
    (substrate_queue, created 2026-06-03 from the 604a/624a cluster
    autopsy) which must give modulatory/diversity signals genuine authority
    at E3.select, and (2) a difficulty-gated proposal-entropy regulator
    (stuck-state detector + transient CEM temperature/candidate-count gain +
    decay) not yet designed. Promotion/demotion suppressed until those land.
    Experiment proposal EXP-0176 (manual_proposals.v1.json) records the
    4-arm design; the script is deferred to /queue-experiment per the
    mandatory skill path.
  literature_anchors:
    - "Increased neural variability / network flexibility during difficult cognitive work (controlled-entropy reading; working analogy, not a settled citation)."
  location: docs/thoughts/2026-06-03_difficulty_gated_proposal_entropy.md
- id: Q-054
  title: "What is the minimum trajectory-class diversity floor (Rung 1
    first_action_entropy threshold) required for the ARC-062 context
    discriminator to learn a reliable discriminative cut between reef and
    foraging contexts? Is the current SP-CEM entropy lift of 0.497 nats
    sufficient, or is a higher floor required? (Was proposed 2026-05-15 as
    Q-046 in behavioral_diversity_acceptance_criteria.md; never registered;
    re-registered under next available Q-id.)"
  claim_type: open_question
  subject: policy.diversity_generation.arc062_minimum_diversity_floor
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_MECH-341-cluster_2026-05-31#V3-EXQ-616"
      as_of: 2026-05-31
      verdict: weakens/?
  implementation_phase: v3
  claim_level: mechanistic
  registered_utc: "2026-05-25"
  what_would_answer: >
    A parametric entropy sweep (noise_floor_min_temperature, curiosity_novelty_weight)
    over the SD-054 substrate that plots ARC-062 context-discriminator accuracy against
    upstream Rung-1 first_action_entropy and locates the floor: the entropy value below
    which discriminator accuracy is at chance and above which it rises monotonically. The
    question is answered when that floor is bracketed -- i.e. it is established whether the
    observed SP-CEM lift of 0.497 nats sits above the floor (sufficient) or below it
    (insufficient). NOTE (2026-06-15): cleanly answering this presupposes the
    behavioral_diversity_isolation:GAP-A conversion ceiling is lifted -- while committed
    behaviour does not move with the channel range (569g/682), discriminator accuracy
    cannot be read as a function of realised first_action_entropy.
  depends_on:
    - ARC-065
    - ARC-062
    - MECH-313
    - MECH-314
  notes: >
    ARC-062 requires a diverse behavioural stream to find a discriminative cut.
    The minimum entropy threshold for this is theoretically bounded below by the
    mutual information between context (reef/forage) and action, but is
    practically unknown for the V3 SD-054 substrate. Resolution path:
    parametric entropy sweep (noise_floor_min_temperature,
    curiosity_novelty_weight) measuring ARC-062 discriminator accuracy as a
    function of upstream first_action_entropy. Expect a phase transition: below
    some floor the discriminator cannot learn; above it accuracy rises
    monotonically. Current evidence: V3-EXQ-567 ARM_1 SP-CEM = 0.497 nats;
    whether this is above or below the ARC-062 threshold is unknown.
    V3-EXQ-543b (Rung 2 experiment) will answer empirically.
  evidence_quality_note: >
    Open question, conf=0.0 by construction. Originally proposed 2026-05-15 in
    behavioral_diversity_acceptance_criteria.md as Q-046; the ID was never
    written into claims.yaml and the next available Q-id was used on
    re-registration 2026-05-25. Scientific content unchanged from the original
    proposal.
  governance_2026_06_16: >
    DEFERRED (governance cycle, user-adjudicated). The narrow_open_question recommendation
    re-surfaced. Narrowing into testable sub-questions is PREMATURE: Q-054's answerability is
    explicitly gated on the behavioral_diversity_isolation:GAP-A conversion ceiling (per the
    2026-06-15 what_would_answer note), and V3-EXQ-569h (this cycle, non_contributory) just
    reconfirmed the ceiling persists even on the ARM_STD_G2 conversion-armed substrate. The
    two /failure-autopsy passes queued next (V3-EXQ-514q, V3-EXQ-569h) bear directly on whether
    realised first_action_entropy can move committed behaviour -- the precondition for reading
    discriminator accuracy as a function of the entropy floor. Defer the question-narrowing until
    those clarify the GAP-A picture. Status UNCHANGED (open). Re-surfaces next cycle.
  location: evidence/planning/behavioral_diversity_acceptance_criteria.md

- id: Q-055
  title: "Does sleep consolidation (SD-017 SWS phase) preserve the trajectory-
    class diversity achieved by ARC-065 during waking, or does Hebbian winner-
    take-all dynamics during replay erode it? Is sleep a net positive or net
    negative for learned behavioural diversity? (Was proposed 2026-05-15 as
    Q-047 in behavioral_diversity_acceptance_criteria.md; never registered;
    re-registered under next available Q-id.)"
  claim_type: open_question
  subject: policy.sleep_consolidation.diversity_effect
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-055@2026-06-19T18:52:58.174989Z"
      as_of: 2026-06-19
      verdict: hold_pending_v3_substrate/applied
  what_would_answer: |
    A Rung-3 experiment measuring trajectory-class diversity (TrajDiv) and
    total-variation occupancy (TV) at three time points: post-waking-convergence
    (ARC-065 diversity established), post-200-no-exploration episodes (waking
    re-seeding controlled out), and post-5-sleep-cycles (SD-017 SWS replay
    applied). ANSWER "sleep is net negative -- replay erodes diversity" if
    TrajDiv/TV decline across the 5 sleep cycles relative to the no-exploration
    baseline, consistent with MECH-120 Hebbian winner-take-all preferentially
    strengthening the most-heavily-weighted trajectories -- this FALSIFIES
    MECH-120-as-sufficient and forces ARC-065 to also operate during consolidation
    (a new MECH). ANSWER "sleep is net positive / neutral -- diversity preserved"
    if TrajDiv/TV are maintained across sleep cycles, supporting SD-017 + INV-049
    and consistent with either counter-mechanism (waking re-seeding between
    cycles, OR replay deliberately sampling proportional to class distribution
    rather than top-reward). ANSWER "sleep INCREASES diversity" (surprising) if
    TrajDiv/TV rise across cycles, indicating broader episodic-replay sampling
    than top-reward priority. The decisive contrast is the post-sleep vs
    post-no-exploration delta, which isolates the sleep-replay effect from passive
    waking drift. HARD PRECONDITION: ARC-065 must have established a
    non-degenerate waking diversity baseline (multiple trajectory classes with
    measurable TrajDiv) before any sleep cycle -- if waking diversity is already
    collapsed (the GAP-A conversion ceiling), there is no diversity for replay to
    preserve or erode and the erosion test is vacuous. SD-017 SWS replay and the
    MECH-120 Hebbian path must both be live in the offline phase, or the no-op
    trivially "preserves" diversity without evidence.
  implementation_phase: v3
  claim_level: mechanistic
  registered_utc: "2026-05-25"
  depends_on:
    - ARC-065
    - SD-017
    - MECH-120
    - INV-049
  notes: >
    MECH-120 predicts Hebbian reinforcement during sleep replay preferentially
    strengthens the most-recently or most-heavily-weighted trajectories. If
    ARC-065 diversity generation operates only during waking, sleep could
    systematically undo it. Two candidate counter-mechanisms: (a) ARC-065
    substrates re-seed diversity in waking periods between sleep cycles; (b)
    sleep replay itself is DESIGNED to sample diverse trajectories (priority
    sampling proportional to class distribution rather than top-reward).
    Resolution path: Rung 3 experiment in the acceptance-criteria doc -- measure
    TrajDiv and TV at three time points (post-waking-convergence, post-200-no-
    exploration-episodes, post-5-sleep-cycles). Outcomes: preserves -> supports
    SD-017 + INV-049; erodes -> falsifies MECH-120-as-mechanism, ARC-065 must
    also operate during consolidation (new MECH); increases -> surprising,
    indicates broader episodic-replay sampling.
  evidence_quality_note: >
    Open question, conf=0.0 by construction. Originally proposed 2026-05-15 in
    behavioral_diversity_acceptance_criteria.md as Q-047; ID was never written
    into claims.yaml. Re-registered under next available Q-id on 2026-05-25;
    scientific content unchanged.
  location: evidence/planning/behavioral_diversity_acceptance_criteria.md

- id: Q-056
  title: "Does stuck-state-GATED proposal entropy outperform BOTH entropy-off and always-high entropy? Main empirical prediction of MECH-343: in a hard-goal/blocked-path environment (matched easy-goal controls), the difficulty-gated-entropy arm should show flexible exploration -- improved goal progress, no harm increase, no random-churn increase, commitment latency that rises during the hard phase then falls after solution discovery -- and should BEAT both the entropy-off arm (proposal rigidity / stuck) and the entropy-always-high arm (disorganised exploration / no narrowing). WORKING QUESTION -- no evidence yet."
  claim_type: open_question
  subject: control_plane.stuck_gated_vs_constant_proposal_entropy
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-056@2026-06-19T18:52:58.215375Z"
      as_of: 2026-06-19
      verdict: hold_pending_v3_substrate/applied
  what_would_answer: |
    The discriminating four-arm comparison for MECH-343, on a hard-goal /
    blocked-path environment with matched easy-goal controls (per EXP-0176): (1)
    entropy-gating OFF; (2) entropy-gating ON under stuck-state; (3) entropy
    ALWAYS high; (4) entropy high with goal/harm constraints ablated (diagnostic
    only, if safe). Metrics: candidate-proposal entropy, committed-action entropy,
    score-margin distribution, arbitration cycles before commitment, goal
    progress, harm rate, action churn, time-to-first-workable-path, post-success
    entropy reduction. ANSWER 'stuck-gated entropy is the right regime' (MECH-343
    supported) if arm 2 STRICTLY beats BOTH arm 1 (entropy-off, which should show
    proposal rigidity / stuck) AND arm 3 (always-high, which should show
    disorganised exploration with no narrowing) on goal progress, with NO harm
    increase and NO random-churn increase, and a commitment-latency that rises
    during the hard phase then FALLS after solution discovery (the post-success
    entropy narrowing). ANSWER 'no advantage to gating' if arm 2 is
    indistinguishable from arm 1 or arm 3 on goal progress, or if it buys progress
    at the cost of harm/churn, or if entropy does not narrow after success. HARD
    PRECONDITION (non-vacuity): the hard-goal environment must actually induce
    stuck states in arm 1 (measurable proposal-entropy collapse / time-to-path
    blow-up) and the gate must fire differentially (arm 2 entropy tracks
    stuck-state, not flat); if arm 1 never gets stuck or the gate never engages,
    the contrast is vacuous and self-routes to a harder environment, not a
    verdict.
  implementation_phase: v3
  claim_level: mechanistic
  epistemic_category: answer_state
  registered_utc: "2026-06-03"
  depends_on:
    - MECH-343
    - MECH-341
    - ARC-018
  notes: >
    The discriminating question for MECH-343. Arms (see EXP-0176 /
    docs/thoughts/2026-06-03_difficulty_gated_proposal_entropy.md):
    (1) entropy-gating OFF; (2) entropy-gating ON under stuck-state;
    (3) entropy ALWAYS high; (4) entropy high with goal/harm constraints
    ablated (diagnostic only, if safe). Metrics: candidate proposal entropy,
    committed-action entropy, score-margin distribution, # arbitration
    cycles before commitment, goal progress, harm rate, action churn,
    time-to-first-workable-path, entropy reduction after success. PASS =
    arm 2 > arms 1 and 3 on goal progress with no harm/churn increase and a
    post-success entropy narrowing. conf=0.0 by construction.
  location: docs/thoughts/2026-06-03_difficulty_gated_proposal_entropy.md
- id: INV-076
  title: "Behavioural diversity is a structural prerequisite for ethical
    counterfactual evaluation. A system with monomodal policy cannot instantiate
    the counterfactual evaluation that ethical judgement requires, because the
    'could have done otherwise' comparison demands that the agent's candidate
    trajectory pool contain at least two distinct trajectory classes with
    distinct harm profiles. The argument is mathematical-by-construction: if
    P(a|s) is monomodal, the CEM elite pool contains only trajectories
    beginning with a*, and SD-003's counterfactual E2(z_t, a_cf) for a_cf != a*
    is never evaluated in the on-policy candidate distribution. (Was proposed
    2026-05-15 as INV-074 in behavioral_diversity_acceptance_criteria.md but
    never registered; the ID was subsequently taken 2026-05-17 by an unrelated
    plasticity-crystallization invariant. Re-registered under INV-076 with
    scientific content unchanged.)"
  claim_type: invariant
  invariant_type: universal
  subject: ethics.diversity_prerequisite_for_counterfactual_evaluation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  claim_level: universal
  registered_utc: "2026-05-25"
  depends_on:
    - ARC-065    # diversity generation pathway that instantiates the prerequisite
    - SD-003     # counterfactual evaluation mechanism that requires diverse candidates
    - ARC-033    # E2_harm_s forward model (the comparator SD-003 uses)
    - MECH-341   # E3-scoring diversity preservation -- the substrate that makes the prerequisite operative downstream of CEM
  notes: >
    Distinct from the empirical claim that a monomodal agent is unsafe: the
    claim is that a monomodal agent is CONSTITUTIVELY INCAPABLE of the
    counterfactual comparison SD-003 requires. Safety could be coincidental (if
    the monostrategy happens to be harmless), but genuine ethical reasoning
    requires the agent CHOSE the harmless trajectory over alternatives, not
    merely executed it. Invariant type: universal (holds across all substrates,
    all environments, by construction). Falsification would require showing
    SD-003 can produce ethical counterfactuals from a monomodal proposal pool,
    which would require a different formulation of SD-003. Rung 4 acceptance
    criteria operationalise this invariant: criterion 3
    (cf_diverse_candidate_rate) directly tests whether SD-003 fires on a
    diverse pool; criterion 4 (ARC-065 ablation reduces harm separation) tests
    whether ARC-065 supplies that diversity. Cross-reference INV-034 (goal
    maintenance necessary for ethical agency): INV-076 is the companion claim
    on the DIVERSITY side. INV-034 is about sustaining the ethical GOAL across
    time; INV-076 is about whether the CANDIDATE GENERATION process can produce
    material for ethical comparison. Biology anchor: the 'could have done
    otherwise' standard in moral philosophy (Fischer & Ravizza 1998 reasons-
    responsiveness; Kane 1996 ultimate responsibility) maps directly to the
    requirement that deliberation generates genuine alternatives. A policy with
    deterministic monomodal output produces one action regardless of reasons;
    it cannot be reasons-responsive.
  evidence_quality_note: >
    Mathematical / logical argument; no experimental evidence yet at the
    invariant level (though Rung 4 acceptance criteria + V3-EXQ-TBD (Rung-4-
    ethics) are the empirical side). Lit-pull recommended before promotion
    beyond candidate. Originally proposed 2026-05-15 as INV-074 in
    behavioral_diversity_acceptance_criteria.md; ID was never written into
    claims.yaml and was subsequently taken 2026-05-17 by an unrelated claim
    (plasticity-crystallization necessity). Re-registered under INV-076
    2026-05-25; scientific content unchanged from original proposal.
  literature_anchors:
    - "Fischer & Ravizza (1998) Responsibility and Control -- reasons-
       responsiveness account of moral responsibility; deliberation must
       generate alternatives for moral agency."
    - "Kane (1996) The Significance of Free Will -- ultimate responsibility
       requires genuine alternative possibilities at the moment of choice."
    - "Bratman (1987) Intention, Plans, and Practical Reason -- planning theory
       of rational action; trajectory planning is the substrate of ethical
       commitment."
    - "Dennett (1984) Elbow Room -- philosophical review of counterfactual and
       alternative-possibilities arguments; the computational analog of these
       arguments is the SD-003 claim."
  location: evidence/planning/behavioral_diversity_acceptance_criteria.md

- id: SD-057
  title: "Object-bound incentive-salience layer: insert a per-object incentive layer between the benefit pulse and z_goal. The legacy goal stream wrote a SINGLE z_goal attractor overwritten on every contact, conflating liking (hedonic impact at consumption) with wanting (incentive salience to a cue/object) and goal-location (where) with goal-object (what); wanting target always equalled liking target, so the L9 wanting!=liking dissociation was structurally inexpressible (514l C6=0.0 on all seeds). SD-057 adds (L2 MECH-344) benefit->object-identity binding on the SD-049 per-type tag, (L3 MECH-345) a per-object slow-decay revaluable incentive token with at-recall drive-specific wanting amplitude = base_value*(1+kappa*per_axis_drive), and (L4 MECH-346) z_goal seeded FROM the most-wanted object's stored embedding rather than the raw last-contacted z_resource. v1 = L2+L3+L4 core; L6 cue-recall (MECH-CUEWANT) + L7 dACC-readout-wiring (MECH-CONSUME) deferred to a phase-2 pass within this SD."
  claim_type: design_decision
  subject: drive.object_bound_incentive_salience
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:SD-057@2026-06-10T16:18:22Z"
      as_of: 2026-06-10
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: "2026-06-04"
  depends_on:
    - SD-049
    - SD-015
    - SD-012
    - MECH-306
    - MECH-230
  functional_restatement: >
    On a resource contact with SD-049 per-type identity tag k (1..n_resource_types),
    bind the benefit pulse to k via IncentiveTokenBank.update(k, benefit, z_resource)
    (L2). Each type accrues base_value[k] (slow-decay, revaluable EMA of received
    benefit) and z_object[k] (stored z_resource identity embedding) (L3). At recall,
    wanting[k] = base_value[k] * (1 + incentive_drive_kappa_weight * per_axis_drive[k])
    -- the Zhang 2009 V = r*kappa(drive) multiplier relocated from the GoalState
    seeding gate onto the stored per-object value, with per-axis drive (SD-049
    hunger/thirst/curiosity) making wanting drive-specific / identity-matched
    (specific PIT). z_goal is then seeded FROM z_object[argmax_k wanting[k]] (L4)
    instead of the raw last-contacted z_resource; the GoalState firing gate is
    unchanged. Liking target (last-contacted) and wanting target (z_goal ->
    most-wanted) can therefore differ, making the L9 dissociation expressible.
  implementation_note: >
    2026-06-04 phase-2 (L6+L7) LANDED on top of the v1 L2-L3-L4 core: L6 MECH-347
    cue-recall path (GoalState.cue_pull + agent.cue_recall_wanting; perceived cue
    raises wanting for the matched object before benefit, identity-matched /
    drive-specific) + L7 MECH-348 dACC object-discriminative readout
    (DACCAdaptiveControl candidate_goal_proximity -> bundle goal_readout ->
    DACCtoE3Adapter goal-readout bias). Both no-op-default (use_cue_recall,
    use_mech_consume), bit-identical OFF, no trained params. Validation V3-EXQ-637.
  evidence_quality_note: >
    SUBSTRATE IMPLEMENTED 2026-06-04 (ree-v3/ree_core/goal.py IncentiveTokenBank +
    GoalConfig flags; agent.py update_z_goal resource_type kwarg + L2/L3/L4 block;
    config.py from_dims; _harness.py forwards resource_type_at_agent). Bit-identical
    OFF by default (use_incentive_token_bank=False). No trainable parameters -> no
    phased training; reuses the trained SD-015/SD-049 z_resource encoder. Contracts
    test_sd_057_incentive_token_bank.py 6/6 + 747/751 full contracts (4 pre-existing
    local-git-env runner failures unrelated) + 7/7 preflight. Behavioural L9
    acceptance is GATED on goal_pipeline:GAP-2 foraging contact; SD-057's own
    validation is a forced-contact MECHANISM diagnostic decoupled from GAP-2
    (mirrors V3-EXQ-626b's L1 decoupling). Design doc:
    docs/architecture/sd_057_object_bound_incentive_salience.md.
  location: docs/architecture/sd_057_object_bound_incentive_salience.md

- id: MECH-344
  title: "Benefit-to-object-identity binding (L2): at resource contact the benefit pulse is bound to the contacted object's identity (the SD-049 per-type tag) via an associative object->benefit node, rather than written as a raw scalar gate against the current world/resource latent. The amygdala/BLA-analog identity-binding node the legacy substrate lacked (cue_features was a V3 no-op stub)."
  claim_type: mechanism_hypothesis
  subject: incentive.benefit_object_identity_binding
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  registered_utc: "2026-06-04"
  depends_on:
    - SD-057
    - SD-049
    - SD-015
  notes: >
    L2 of the goal_pipeline:GAP-7 closure map. Implemented as
    IncentiveTokenBank.update(resource_type, benefit, z_object=z_resource) in
    ree-v3/ree_core/goal.py, called from agent.update_z_goal when a resource type
    is contacted (resource_type tag > 0) and the bank is active. v1 keys on the
    SD-049 per-type tag (minimal object-centric substrate already present); a
    learned affordance/identity embedding is the upgrade path. Biological anchor:
    Cardinal/Everitt 2002 (BLA binds cue->outcome identity); Flagel 2011
    (sign-tracking object/cue attribution).
  location: docs/architecture/sd_057_object_bound_incentive_salience.md

- id: MECH-345
  title: "Per-object incentive-salience token (L3): each resource identity accrues a slowly-decaying, revaluable base incentive value plus a stored identity embedding; wanting amplitude at recall = base_value * (1 + kappa * drive), with the drive multiplier relocated from the seeding gate onto the stored per-object value and computed per-axis so wanting is drive-specific / identity-matched (specific PIT)."
  claim_type: mechanism_hypothesis
  subject: incentive.per_object_wanting_token
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  registered_utc: "2026-06-04"
  depends_on:
    - SD-057
    - MECH-344
    - SD-012
    - SD-049
  notes: >
    L3 of goal_pipeline:GAP-7. IncentiveTokenBank holds per-type base_value
    (EMA-revalued toward received benefit on contact -- Balleine/Dickinson 1998
    revaluable not write-once; slow decay between contacts -- Robinson/Berridge
    1993 persistence) and z_object. wanting[k] = base_value[k] *
    (1 + incentive_drive_kappa_weight * per_axis_drive[k]) (Zhang 2009 V =
    r*kappa(drive); kappa applied at recall, NOT re-learned). Per-axis drive
    (SD-049) gives food<->hunger, water<->thirst identity-matched wanting
    (Corbit/Balleine 2005/2011 specific PIT). No trainable parameters
    (stateful EMA dict).
  location: docs/architecture/sd_057_object_bound_incentive_salience.md

- id: MECH-346
  title: "z_goal written from the incentive-token pointer (L4; amends MECH-230): z_goal is seeded FROM the most-wanted object's stored identity embedding (argmax over per-object wanting) rather than from the raw last-contacted z_resource. z_goal keeps its role as E1 conditioning vector and E3 goal_proximity target; only the seed SOURCE changes, making the attractor object-bound and the wanting target dissociable from the liking (last-contacted) target."
  claim_type: mechanism_hypothesis
  subject: incentive.z_goal_from_token_pointer
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-346@2026-06-10T16:18:22Z"
      as_of: 2026-06-10
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  registered_utc: "2026-06-04"
  depends_on:
    - SD-057
    - MECH-345
    - MECH-230
  notes: >
    L4 of goal_pipeline:GAP-7; amends MECH-230 (the structured-attractor
    assertion is unchanged -- only the seed source changes). In
    agent.update_z_goal, when the bank is active, seed_latent =
    bank.most_wanted(per_axis_drive)[1] (the most-wanted object's z_object)
    instead of the raw single z_resource snapshot; the GoalState.update firing
    gate (benefit/drive threshold) is unchanged. What/where factorization
    (Dayan 1993 / Barreto 2017 SF/SR; Schoenbaum 2009 OFC outcome identity;
    UVFA z_goal-as-conditioning preserved). This is the link that makes
    wanting_target (z_goal -> most-wanted) != liking_target (last-contacted)
    structurally expressible, unblocking the L9 dissociation acceptance for
    MECH-229 / MECH-117 / ARC-030 (which stay v3_pending until validated).
  location: docs/architecture/sd_057_object_bound_incentive_salience.md

- id: MECH-347
  title: "Cue-triggered wanting / cue-recall (L6): a PERCEIVED cue/object type (with no benefit pulse) retrieves its SD-057 incentive token and nudges z_goal toward that object's stored identity embedding BEFORE consumption -- identity-matched (pulls toward the perceived cue's object) and drive-specific (amplitude = base_value*(1+kappa*per_axis_drive)). The downstream E3 goal_proximity + MECH-295 approach bridge then raise pre-consummatory approach toward the cued object. Distinct from the benefit-driven seed: no benefit pulse, no token revaluation, weaker (cue_recall_gain) pull."
  claim_type: mechanism_hypothesis
  subject: incentive.cue_triggered_wanting
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-347@2026-06-10T16:18:22Z"
      as_of: 2026-06-10
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: true
  registered_utc: "2026-06-04"
  depends_on:
    - SD-057
    - MECH-345
    - MECH-295
    - SD-049
  notes: >
    SD-057 phase-2 L6. Implemented as GoalState.cue_pull(z_object, strength)
    (a directional z_goal nudge with no benefit gate / no revaluation) +
    agent.cue_recall_wanting(cue_type, drive_level, simulation_mode) in
    ree-v3/ree_core/{goal.py,agent.py}. Config GoalConfig.use_cue_recall (False;
    requires use_incentive_token_bank), cue_recall_gain (0.05), cue_recall_min_proximity
    (0.0). StepHarness auto-derives the strongest-perceived type from SD-049 per-type
    proximity field views and fires the primitive each step; forced-cue diagnostics
    call it directly. MECH-094: simulation_mode=True is a no-op (replay must not move
    z_goal via a cue). Biology: Berridge 2009 cue-triggered wanting; Corbit/Balleine
    2005/2011 specific PIT; Schultz 1997/98 DA-transfer-to-cue. Validation V3-EXQ-637.
  location: docs/architecture/sd_057_object_bound_incentive_salience.md

- id: MECH-436
  title: "Drive-state modulation of wanting (incentive salience): the homeostatic drive STATE modulates the wanting target and/or intensity dissociably from liking -- the most-wanted object tracks the currently-most-depleted drive axis (incentive-salience amplitude ~ base_value*(1+kappa*per_axis_drive)) even when the liking/consummatory target does not. Split from MECH-229 2026-06-16: MECH-229 asserts wanting and liking are dissociable SIGNALS (the object-bound dissociation, established by V3-EXQ-514o); MECH-436 is the distinct, finer claim that wanting is additionally DRIVE-STATE-MODULATED -- the incentive-salience core the 514m/514p/514q lineage kept (in different broken ways) trying to express."
  claim_type: mechanism_hypothesis
  subject: drive.wanting_drive_state_modulation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-514t_2026-06-20
      as_of: 2026-06-20
      verdict: non_contributory/substrate_ceiling
  epistemic_category: standard
  implementation_phase: v3
  v3_pending: true
  split_from: MECH-229
  registered_utc: "2026-06-16"
  pending_retest_after_substrate: false
  evidence_quality_note: |
    Substrate ceiling LIFTED 2026-06-21 (governance). The SD-049-PHASE-2 bounded-kappa / standing-differential-depletion env amend (kappa_scale 12.0 + per_axis_restoration_fraction 0.15) landed and V3-EXQ-514u (supersedes 514t; PASS, supports, non_degenerate) cleared the drive-coupling effect margin on the enriched substrate via the CONTINUOUS-AMPLITUDE (intensity) reading: mean_cont_amplitude_shift 0.164 > 0.05 floor on all scored seeds (k_sd 1.0, n=76); enriched per-axis drive spread 0.222 > 0.1; OFF/bank-disabled floor and argmax-relevance readiness all met. This shows the claim's "intensity" disjunct (incentive-salience amplitude ~ base_value*(1+kappa*per_axis_drive)). The DISCRETE target-FLIP disjunct (C_WL_DRIVE_coupled_dissociation_flip) remains SECONDARY and still fails on the natural enriched substrate (mean wl_drive_delta -0.133), but the overshoot positive control flips most_wanted (0.81) -> the flip shortfall is a magnitude artifact, NOT a falsification (the what_would_answer overshoot disambiguator resolves toward "drive CAN carve"). epistemic_category flipped substrate_ceiling -> standard and pending_retest_after_substrate cleared. NOT promoted: status stays candidate and v3_pending: true holds promotion (hold_pending_v3_substrate). The remaining open work is the target-FLIP disjunct on the V3 enriched env (514u amplitude reading is sufficient to lift the ceiling, not to promote).

    2026-06-17 (failure_autopsy_V3-EXQ-514r, confirmed; applied /governance 2026-06-17): the 514r self-stamped weakens is reclassified non_contributory and scoring-excluded. 514r was the pre-registered overshoot disambiguator. Its genuine-weakens precondition ("overshoot CANNOT flip most_wanted even at OVERSHOOT_DRIVE_MAGNITUDE=5.0") is FALSE: overshoot flips most_wanted fully on 2/5 guard-passing seeds (0.727, 1.000) + non-zero on all 5 (mean_overshoot_flip 0.485; seed 44 also flipped 0.800 -> 3/6 across all seeds) -- the drive channel demonstrably CAN carve at adequate magnitude, so 514q's natural delta=0.0 is a drive-MAGNITUDE/env artifact, not a structural inability. Controls non-vacuous (pc_argmax_relevance_frac 1.0, off-floor 1.0, bank populated 1.0). KEY: the positive control flips a 0.5 base_value gap on 100% of seeds, but real in-run gaps on seeds 45/46/47 exceed 0.5 (even mag-5.0 can't flip there) -> the kappa-scaling half of the SD-049-PHASE-2 env amend is LOAD-BEARING, not just differential depletion. MECH-436 stays candidate / substrate_ceiling / pending_retest_after_substrate (UNCHANGED -- confirmed correct, NOT weakened, NOT promoted); the SD-049-PHASE-2 differential-depletion + kappa-scaling amend is now GREENLIT (the 514q "DO NOT build until 514r resolves" hold is lifted). MECH-229 leg (a) UNAFFECTED. No demotion. 2026-06-20 (failure_autopsy_V3-EXQ-514t, confirmed; applied /governance 2026-06-20): the SD-049-PHASE-2 amend was built and armed by V3-EXQ-514s/514t. 514t (kappa_scale 6.0->12.0) self-routes mech436_enrichment_insufficient_substrate_ceiling by the honest path (all five non-vacuity preconditions MET; enriched_spread 0.192 -> lever (b) standing-differential-depletion DONE). non_contributory, NOT a weakens: overshoot (mag 5.0) still flips most_wanted 4/5. LOAD-BEARING NEW FINDING: the kappa-MAGNITUDE lever is NON-MONOTONE and EXHAUSTED -- doubling kappa REGRESSED the natural delta (514s +0.064/sd 0.109/2-of-5 -> 514t -0.037/sd 0.188/1-of-5, 2 seeds strongly negative); at kappa_eff=24 the drive term over-amplifies base_value and most_wanted -> argmax(per_axis_drive), read by the argmax-flip-gated WL metric as sign-unstable noise. The "needs more kappa" prediction is FALSIFIED; the diagnosed ceiling is now the MEASUREMENT layer (flip-gate discards continuous sub-flip re-weighting). MECH-436 stays candidate / substrate_ceiling / pending_retest_after_substrate (UNCHANGED). Route: V3-EXQ-514u measurement-redesign (continuous incentive-amplitude readout at MODERATE kappa <= 6.0, the 514s C8 bounded-kappa invariant) -- ALREADY QUEUED (supersedes 514t). No /claim-synthesis (convergent 5th-autopsy signature, granularity debt already discharged at 514q). MECH-229 leg (a) wanting!=liking (V3-EXQ-514o PASS 0.80) UNAFFECTED. No demotion.
  depends_on:
    - MECH-229   # parent umbrella: the wanting!=liking object-bound dissociation this modulation specializes
    - SD-049     # multi-resource per-axis drive substrate -- the drive-state signal source
    - MECH-347   # cue-recall wanting whose amplitude is base_value*(1+kappa*per_axis_drive) -- the drive-scaled implementation MECH-436 is the behavioural validation of
    - MECH-346   # most-wanted z_object pointer (argmax over per-object wanting) -- the object-bound seed the drive term re-weights
  what_would_answer: >
    A drive-dependence delta mean(WL_drive - WL_nodrive) >= max(k*pstdev(delta), FLOOR),
    measured where the in-run per-axis drive spread is in the argmax-moving regime (asserted
    by a recalibrated readiness gate, NOT merely spread > 1e-3), with an overshoot positive
    control that flips most_wanted and an OFF/bank-disabled control that floors at
    wanting==liking. First falsifier ALREADY QUEUED: V3-EXQ-514r (overshoot + OFF + recalibrated
    readiness). Overshoot flips most_wanted => drive CAN carve at adequate magnitude (the 514q
    delta=0.0 is an env/magnitude artifact -> enrich the V3 env via the SD-049-PHASE-2
    differential-depletion / kappa-scaling amend, NOT a weakens); overshoot does NOT flip even at
    large drive => genuine MECH-436 weakens. 514r is the disambiguator that decides which.
  location: docs/architecture/sd_057_object_bound_incentive_salience.md
  notes: >
    Registered 2026-06-16 PROPOSAL-FIRST via /claim-synthesis (proposal:
    evidence/planning/claim_synthesis_MECH-229_2026-06-16.md), at user direction, as the
    drive-coupling child split out of MECH-229. Routed by failure_autopsy_V3-EXQ-514q_2026-06-16
    (confirmed): the 4-autopsy 514 lineage (514l/m/p/q, distinct signatures each) is granularity
    debt -- every genuine MECH-229 support (514o object-bound fraction 0.80; 514q object_bound 0.70)
    lands on the wanting!=liking dissociation, while every FAIL/churn circles the drive-coupling
    leg the broad claim never separately named. 514q isolated the blocker: in-run per-axis drive
    spread ~0.0055-0.0085 is far below the magnitude that could flip an object-score-dominated
    most_wanted argmax, so the drive-dependence delta is structurally 0.0 on the V3 P2 foraging env.
    epistemic_category substrate_ceiling (user-adjudicated 2026-06-16, FLIPPED from an initial
    substrate_conditional after confirming the phase scope): the claim is V3-tractable in principle
    but the current V3 P2 foraging env is too coarse to deliver argmax-relevant per-axis drive
    spread, so the right response is to ENRICH the V3 substrate -- the SD-049-PHASE-2
    differential-depletion / kappa-scaling env amend (priority 2, NOT yet built; this is a V3 env
    amend: SD-049 is implementation_phase v3 and already provides the per-axis homeostatic drive
    system, so the conditioning substrate is V3, NOT V4-blocked). The pre-registered V4-1
    multi-agent-ecology is a RICHER OPTIONAL home for the same differential-depletion pressure, NOT
    the blocker (and MECH-436's dependency stops at the V3 env layer -- differential DEPLETION -- it
    does NOT depend on the V4 drive-arbitration policy MECH-394/435 that sits above per-axis drive).
    V3-EXQ-514r is the disambiguator: overshoot flips most_wanted => env-magnitude artifact => build
    the V3 amend; overshoot fails to flip => genuine weakens (no enrichment helps). promote/demote
    suppressed; conflict alerts fire; narrow_open_question does not.
    Biology-before-formal-definitions grounding (targeted_review_connectome_mech_347, pulled
    2026-06-16): Berridge 2006 -- incentive salience integrates Pavlovian cue info WITH physiological
    state signals; explicitly flags REE's fixed-gain cue_pull as a magnitude/state-modulation
    simplification (the MECH-436 gap). Smith/Berridge/Aldridge 2011 (PNAS) -- dopamine stimulation
    amplified ONLY the incentive-salience component, dissociable from liking and learned prediction.
    DiFeliceantonio/Berridge 2016 -- limbic activation dynamically + competitively amplifies cue
    attraction (one cue a stronger motivational magnet). Not a formal import: incentive salience IS
    drive/state-modulated cue attraction. DOES NOT weaken MECH-229's established wanting!=liking
    dissociation (leg a). Owning falsifier V3-EXQ-514r (queued 2026-06-16). DO NOT build the
    SD-049-PHASE-2 substrate amend until 514r resolves.
- id: MECH-348
  title: "dACC object-discriminative goal readout (L7): the dACC adaptive-control consumer reads per-candidate goal_proximity to the (SD-057 object-bound) z_goal, so its effort/value computation becomes object-discriminative -- approach_commit is goal-conditioned rather than z_goal-blind. Resolves the 2026-06-04 L7 audit finding that only E3 goal_proximity read z_goal consequentially and dACC was blind."
  claim_type: mechanism_hypothesis
  subject: incentive.dacc_object_discriminative_readout
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  registered_utc: "2026-06-04"
  depends_on:
    - SD-057
    - MECH-346
    - SD-032b
  notes: >
    SD-057 phase-2 L7. DACCAdaptiveControl.forward gains candidate_goal_proximity ->
    bundle "goal_readout"; DACCtoE3Adapter adds bias -= dacc_goal_readout_weight *
    goal_readout (proximity high -> favoured, REE lower-is-better), independent of
    dacc_weight so a goal-conditioned consumer works even if the legacy dACC bias is
    off. Wired in agent.select_action by passing the per-candidate goal_proximity to
    the object-bound z_goal into dacc. Config REEConfig.use_mech_consume (False;
    requires use_dacc) + dacc_goal_readout_weight (0.0; DACCConfig). Bit-identical OFF.
    Biology: Balleine & O'Doherty 2010 (approach_commit goal-conditioned). MECH-094:
    waking action selection only. Validation V3-EXQ-637.
  location: docs/architecture/sd_057_object_bound_incentive_salience.md

- id: SD-058
  title: "Instrumental-avoidance acquisition: REE's defensive system needs an instrumental-ACQUISITION side, not only the defensive-REACTION side. SD-035 (amygdala BLA/CeA salience) + MECH-279 (PAG freeze gate) give REE the Pavlovian reaction (Tovote 2016: response EXECUTION, not learning); what is missing is the step from reaction to learned avoidance. Moscarello & LeDoux 2013: active avoidance learning is the RESOLUTION of a Pavlovian-instrumental conflict -- the freezing reaction and the instrumental avoidance action are mutually exclusive, and learning to avoid REQUIRES the infralimbic PFC to SUPPRESS central-amygdala-driven freezing (ilPFC lesion -> more freezing, less avoidance; CeA lesion -> opposite). A freeze-and-salience substrate with no reaction-suppression / instrumental-acquisition layer is the ilPFC-lesion animal: it freezes instead of learning to avoid -- the V3-EXQ-603g G_H 0/3 signature. SD-058 adds the architecture (an instrumental-avoidance action pathway + an ilPFC-analog freeze-suppression gate gated by learned avoidance-efficacy, staged under a protective scaffold); MECH-357 is the gate mechanism."
  claim_type: design_decision
  subject: defensive_action.instrumental_avoidance_acquisition
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  epistemic_category: standard
  claim_level: architectural
  registered_utc: "2026-06-07"
  depends_on:
    - SD-035    # amygdala BLA/CeA salience -- the defensive-reaction side this builds the acquisition side onto
    - MECH-279  # PAG freeze gate -- the freeze output the ilPFC gate suppresses
    - SD-011    # z_harm_a affective stream -- the threat signal the gate reads
  distinct_from:
    - SD-037    # orexin override: REFLEXIVE arousal/drive-driven raise of the PAG exit threshold -- NOT learned avoidance acquisition
    - MECH-281  # orexin motor-coupling: REFLEXIVE lower of the MECH-091 urgency-interrupt threshold -- escape reflex, not acquisition
    - MECH-091  # urgency interrupt: z_harm threat reflex (interrupt a committed program), not learned avoidance
  functional_restatement: >
    Architectural decision (V3-tractable). REE's defensive machinery (SD-035 amygdala
    salience + MECH-279 PAG freeze) is reaction-only. Active avoidance LEARNING
    requires resolving the Pavlovian-instrumental conflict: an instrumental-avoidance
    ACTION pathway plus an infralimbic-PFC-analog SUPPRESSION GATE over the freeze
    output, with the suppression gated by a LEARNED avoidance-efficacy (eligibility
    trace) that rises as the agent discovers directed action under threat reduces
    z_harm_a, bootstrapped early by a protective scaffold (maternal-buffering /
    Turchetta 2020 reset-curriculum analogue) that anneals as competence grows. This
    is structurally distinct from the reflexive escape-from-freeze levers (SD-037
    orexin raises the PAG exit threshold; MECH-281 lowers the urgency-interrupt) which
    are instantaneous threat/arousal reflexes, not the gradual developmental
    acquisition (Debiec & Sullivan 2017; Thompson 2008).
  implementation_note: >
    IMPLEMENTED 2026-06-07 (substrate; v3_pending until the Stage-H validation EXQ
    PASSes). Mechanism is MECH-357 (ree-v3/ree_core/pfc/infralimbic_avoidance_gate.py,
    InstrumentalAvoidanceGate -- pure-arithmetic regulator behind
    use_instrumental_avoidance, no-op default, bit-identical OFF). Wired into the
    scaffolded_sd054_onboarding Stage-H as the avoidance-learning driver (protective-
    scaffold floor anneal). LOAD-BEARING PREREQUISITE found this session: the legacy
    scaffold fed no harm stream to sense() (z_harm_a None across the curriculum ->
    MECH-279/SD-035/the gate all inert); fixed via scaffold_feed_harm_stream.
  evidence_quality_note: >
    CORRECTED 2026-08-07 (thought-digestion wave 11, session cool-torvalds-a82359):
    this note previously said "V3-EXQ-603h queued" -- stale. V3-EXQ-603h ALREADY RAN
    2026-06-07/08 (manifest
    v3_exq_603h_instrumental_avoidance_stageh_validation_20260607T211923Z_v3,
    ree-cloud-3): outcome FAIL, evidence_direction non_contributory, primary_pass
    false. Non-vacuity preconditions BOTH met (pag_freeze_lesion_frac=1.0,
    gate_engaged_intact_frac=1.0, gate_freeze_suppressed_intact_frac=1.0 -- a real,
    non-vacuous avoidance verdict, not a substrate-not-ready requeue) -- but the
    primary survival criterion failed: g_h_intact_frac=0.0 (0/3), tied with
    g_h_lesion_frac=0.0 (0/3), interpretation label avoidance_gate_insufficient.
    Per-seed detail: the gate engages and accrues avoidance_efficacy (one INTACT
    seed reached 0.633) but this is decoupled from survival outcome. Confirmed
    failure autopsy (evidence/planning/failure_autopsy_V3-EXQ-603h_2026-06-08.json)
    attributes this to a discovered gap: avoidance_efficacy penalises the
    no-op/freeze class but binds no directed escape affordance, so un-freezing does
    not translate into directed avoidance action. This spawned SD-059/MECH-358
    (relief/safety escape-affordance bridge) -- NOT a supersession of SD-058, which
    remains unweakened -- and SD-059/MECH-358 have since PASSED
    (V3-EXQ-603q, 2026-06-17, supports; reconfirmed by regression check V3-EXQ-866b,
    2026-08-03). Already marked reviewed in review_tracker.json, not sitting in
    pending_review.md. NEXT STEP (surfaced for governance/queue-experiment, not
    decided here): a retest of SD-058's own Stage-H harness with the SD-059/MECH-358
    bridge wired in is now unblocked but has not been queued -- this is what
    ARC-060/MECH-320/ARC-068/SD-054's shared pending_retest_after_substrate flag is
    still waiting on.

    ADDENDUM (2026-08-07, same session, found while closing out): the picture is
    fuller than "just wire in the bridge." `evidence/planning/substrate_queue.json`'s
    composite scaffolded-onboarding node (added_session
    governance-20260529T212835Z) shows a SECOND, independent confound was found and
    partially fixed AFTER the 603h FAIL: V3-EXQ-603i traced 603h's
    "engaged-but-insufficient" pattern to E3.harm_eval_head never being trained (a
    near-constant ~0.523 score -- the whole harm landscape was noise, so SD-058/
    MECH-357 and SD-059/MECH-358 were biasing selection within noise, not a real
    signal). A harm-pathway-training fix (scaffold_train_harm_pathway, default
    off/bit-identical) was implemented and V3-EXQ-603k PASSED (2026-06-09,
    claim_ids=[], diagnostic -- acceptance.overall_pass true, harm_pathway_trained
    true, on_nav_survives_2of3 true) -- confirming the fix works AT THE SURVIVAL-LEG
    LEVEL. But per that node's own text: "READY STAYS FALSE -- the full curriculum
    gate (G1 foraging + G2 survival + G3 ecological in ONE run with harm-pathway ON)
    is owed; 603k clears only G2 ... Residual = foraging/benefit-contact (P2)." The
    node's current_pending_amend still reads status=implemented_pending_validation,
    ready=false as of this check -- unchanged since. So the actual remaining gap for
    SD-058 to clear v3_pending is narrower and more specific than "queue a retest
    with the bridge": it is the still-unqueued FULL combined curriculum gate
    (G1+G2+G3, harm-pathway-training ON, bridge wired in), and a separate
    z_goal-never-updates hook bug the same node names (ScaffoldedSD054OnboardingScheduler
    never calls agent.update_z_goal) may also need resolving first for G1 to be
    reachable. NOT fully traced further this session -- this substrate_queue node is
    large (autopsy_refs spanning V3-EXQ-490g/603a-d/604/605/625b/460b-466b/603m) and
    genuinely warrants a dedicated pass, not a same-session digestion follow-up.
    Design doc: docs/architecture/sd_058_instrumental_avoidance_acquisition.md.
  what_would_answer:
    confirms: >
      A retest of V3-EXQ-603h's design with SD-059/MECH-358's escape-affordance
      bridge wired in reports readiness_met=true (non-vacuity: freeze on lesion,
      gate engages+suppresses on intact) AND G_H_INTACT >= 2/3 AND
      G_H_INTACT_frac > G_H_LESION_frac.
    falsifies: >
      Readiness is met but G_H_INTACT stays below 2/3 or fails to beat G_H_LESION --
      the suppression mechanism fires without the predicted survival benefit, even
      with the escape-affordance gap closed.
    actual_result_to_date: >
      V3-EXQ-603h ALREADY RAN 2026-06-07/08: FAIL, non_contributory. Non-vacuity
      preconditions met (freeze/gate both confirmed working) but G_H_INTACT 0/3, tied
      with LESION. See the corrected evidence_quality_note above for full detail.
      SD-058/MECH-357 themselves unweakened, still candidate/v3_pending pending a
      retest with the SD-059/MECH-358 bridge wired in (not yet queued).
  location: docs/architecture/sd_058_instrumental_avoidance_acquisition.md

- id: MECH-357
  title: "Infralimbic-PFC-analog freeze-suppression + instrumental-avoidance gate: the mechanism instantiating SD-058. A pure-arithmetic regulator (ree_core/pfc/infralimbic_avoidance_gate.py, sibling to SD-035 CeA/BLA, MECH-279 PAG, MECH-313/MECH-320 policy regulators) with three coordinated pieces, all behind use_instrumental_avoidance (no-op default): (a) an instrumental-avoidance ACTION pathway -- a per-candidate E3 score-bias that under threat penalises the no-op/freeze class proportional to effective avoidance-efficacy, RELEASING the instrumental action (it does not pick the escape direction; E3's harm gradient does -- ARC-007-strict-compatible); (b) an ilPFC freeze-SUPPRESSION gate that, when effective_efficacy*threat_scale >= threshold, overrides the MECH-279 freeze no-op so the agent acts instead of freezing; (c) an eligibility-trace avoidance-efficacy learner credited when a directed action under threat drops z_harm_a (Debiec & Sullivan 2017 gradual acquisition), bootstrapped by a protective-scaffold floor that anneals across Stage-H (Turchetta 2020)."
  claim_type: mechanism_hypothesis
  subject: prefrontal.infralimbic_freeze_suppression_avoidance_gate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-08-13
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-603t_2026-08-13"
      as_of: "2026-08-13"
      verdict: "non_contributory -- 4th consecutive inconclusive-by-design-defect (Stage-H readiness R4 unmet, config-only pressure levers now exhausted across 3 designs); mechanism-engagement/survival predictions still neither confirmed nor falsified"
  implementation_phase: v3
  v3_pending: true
  epistemic_category: standard
  pending_retest_after_substrate: true  # agent-directed hazard_agent_pursuit wiring now BUILT 2026-08-14 (Stage-H threading); retest V3-EXQ-603u queued -- true until 603u validates
  claim_level: mechanistic
  registered_utc: "2026-06-07"
  depends_on:
    - SD-058    # the architecture this mechanism instantiates
    - MECH-279  # PAG freeze gate -- the freeze output piece (b) suppresses
    - SD-035    # amygdala salience -- the defensive-reaction context
    - SD-011    # z_harm_a affective stream -- the threat signal driving all three pieces
  distinct_from:
    - SD-037    # orexin override: reflexive arousal-driven PAG-exit-threshold raise -- MECH-357 suppression is LEARNED-efficacy-gated
    - MECH-281  # orexin motor-coupling: reflexive urgency-interrupt lower -- not acquisition
    - MECH-091  # urgency interrupt: threat reflex, not learned avoidance
    - MECH-279  # PAG freeze: the REACTION; MECH-357 is the learned SUPPRESSION of it enabling instrumental avoidance
  functional_restatement: >
    Mechanism hypothesis (V3-tractable). avoidance_efficacy in [0,1] is an EMA
    eligibility trace: a directed action under threat (z_harm_a_norm > threat_floor)
    that drops z_harm_a credits it; freezing / failed avoidance decays it. The
    effective efficacy consumed by both consumers is max(avoidance_efficacy,
    scaffold_floor); the curriculum sets the scaffold_floor high early (protective
    scaffold bootstrapping the freeze-suppression so the agent acts, accrues avoidance
    experience, and learns) and anneals it down as the learned efficacy takes over.
    The action-bias (a) penalises the no-op/freeze class under threat; the
    freeze-suppression (b) overrides the MECH-279 no-op when effective_efficacy *
    threat_scale >= suppression_threshold. Falsifiable: with the gate ON in an
    isolated hazard-avoidance stage (PAG present), the survival leg trains
    (G_H >= 2/3) where the ilPFC-lesion (gate OFF) freezes and dies; the suppression
    must scale with LEARNED efficacy, not instantaneous arousal (the SD-037/MECH-281
    distinction).
  implementation_note: >
    IMPLEMENTED 2026-06-07. ree-v3/ree_core/pfc/infralimbic_avoidance_gate.py
    (InstrumentalAvoidanceGate + Config + Output); agent.py wiring (update in sense(),
    action-bias + freeze-suppression in select_action()); config.py flags (no-op
    default). 912 contracts + 7/7 preflight PASS (bit-identical OFF); 7 MECH-357
    contracts + 4 C13 + 2 C14 scaffold contracts. Pure-arithmetic regulator -- no
    trained parameters, no phased training. MECH-094: no-op under simulation_mode.
    STAGE-H PRESSURE MECHANISM 2026-08-14 (session intelligent-elgamal-222d2b,
    /implement-substrate): the already-built env param hazard_agent_pursuit (ree-v3
    39b5ca8) was threaded through the Stage-H _build_env of scaffolded_sd054_onboarding
    via new config field scaffold_hazard_stage_hazard_agent_pursuit (no-op default;
    bit-identical for all existing callers). This is the 6th and final pressure-mechanism
    candidate after config-only levers (static field, 603s mobile-predator drift, 603t
    scheduled adjacency) were exhausted across 3 designs -- all left hazard motion
    agent-INDEPENDENT (603s exact tie / 603t LESION ceiling). Validation V3-EXQ-603u
    queued + POSTed to coordinator (603s + directedness: hazard_agent_pursuit=0.9); this
    does NOT change flags -- v3_pending / pending_retest_after_substrate stay true until
    603u PASSes (G_H_INTACT>=2/3 AND G_H_INTACT>G_H_LESION). Clears the MECH-357 re-derive
    brake (603s+603t; the 603t autopsy recommended_substrate_queue_entry names this build).
    The avoidance-efficacy eligibility-trace leak:learn underflow is split out as
    substrate_queue item mech357-avoidance-efficacy-eligibility-trace-imbalance and does
    not confound the Stage-H window (scaffold_floor covers it). Design doc:
    evidence/planning/mech357_agent_pursuit_build_2026-08-14.md.
  evidence_quality_note: >
    CORRECTED 2026-08-08 (thought-digestion currency pass, mirroring the correction
    already applied to sibling SD-058's entry in wave 11, 2026-08-07): the text below
    previously implied V3-EXQ-603h merely "confirmed the chain engages" -- it did that,
    but it ALSO ran the actual pre-registered survival test and FAILED it. Full manifest:
    v3_exq_603h_instrumental_avoidance_stageh_validation_20260607T211923Z_v3.json.
    claim_ids=[] (diagnostic, correctly non-promotable), outcome=FAIL,
    evidence_direction=non_contributory. Non-degeneracy preconditions BOTH met (a
    genuinely non-vacuous test): pag_freeze_lesion_frac=1.0, gate_engaged_intact_frac=1.0,
    gate_freeze_suppressed_intact_frac=1.0 -- the mechanism genuinely engages, one INTACT
    seed reached avoidance_efficacy=0.633. But the PRIMARY SURVIVAL CRITERION FAILED:
    g_h_intact_frac=0.0 (0/3), tied with g_h_lesion_frac=0.0 (0/3). Interpretation label:
    avoidance_gate_insufficient -- "the scalar avoidance-efficacy trace is decoupled from
    survival... because the gate only penalises the no-op class and binds no directed
    escape affordance."
    Two independent confounds were found; one is now resolved, one resolved at the
    diagnostic level only: (1) escape-affordance bridge -- spawned SD-059/MECH-358 (not a
    supersession), which have since PASSED (V3-EXQ-603q, 2026-06-17, reconfirmed by
    V3-EXQ-866b regression check 2026-08-03). (2) harm-pathway never trained
    (E3.harm_eval_head near-constant ~0.523, noise not signal) -- a fix
    (scaffold_train_harm_pathway) was implemented and V3-EXQ-603k PASSED at the
    survival-leg diagnostic level (2026-06-09, claim_ids=[], on_nav_survives_2of3=true).
    NEITHER FIX HAS YET BEEN COMBINED into a claim-tagged rerun of 603h's own design.
    Confirmed via git log -S "MECH-357" (both repos) and a direct search of
    ree-v3/experiment_queue.json: zero hits, no experiment currently tags MECH-357. The
    scaffolded_sd054_onboarding composite substrate_queue node still reads
    status=implemented_pending_validation, ready=false (current as of 2026-08-07). The
    owed retest is specifically the FULL combined curriculum gate (G1 foraging + G2
    survival + G3 ecological in ONE run, harm-pathway-training ON, escape-affordance
    bridge wired in) -- not yet queued. A separate ScaffoldedSD054OnboardingScheduler bug
    (_train_episode never calls agent.update_z_goal, so z_goal stays zero-init) may also
    block the G1 leg and needs resolving first. Chip spawned to queue this retest.
    Net: MECH-357's mechanism-engagement is solidly confirmed; its behavioural (survival)
    prediction is neither confirmed nor falsified under fair conditions -- tested once,
    under conditions since shown doubly confounded, and failed under those confounds.
    Design doc: docs/architecture/sd_058_instrumental_avoidance_acquisition.md.

    [2026-08-09 governance, V3-EXQ-603r, confirmed failure_autopsy_V3-EXQ-603r_2026-08-09]: the
    combined-fix retest this note flagged as owed. Both fixes (harm-pathway training 603k,
    escape-affordance bridge SD-059/MECH-358 603j/603q) applied identically to both arms to
    isolate the ilPFC gate as the sole variable, following 603h's own diagnosis. Result:
    FAILED the discrimination criterion (G_H_INTACT_beats_LESION) while clearing the absolute
    criterion (G_H_INTACT_clears_2/3, all readiness preconditions met at 1.0). Unexpectedly,
    ARM_LESION (no gate at all) ALSO reached survival ceiling (median 200/103/200 vs 603h's
    27.5/16.5/19.0 under byte-identical hazard config) -- a THIRD, previously-unnamed confound:
    the harm-pathway fix alone makes passive PAG freeze/release timing survival-adequate in this
    hazard field, eliminating the Pavlovian-instrumental conflict the paradigm depends on for
    discrimination. non_contributory. epistemic_category stays standard (autopsy recommended
    "measurement_test_design_defect", not a valid claims.yaml enum -- see
    chip-20260809-epistemic-category-vocab-audit). New dependency discovered: the Stage-H hazard
    field needs a freeze-incompatible pressure (pursuit/escalation/freeze-cost) to force the
    conflict this paradigm needs. Also reconfirmed still-unresolved: MECH-357's own
    avoidance_efficacy eligibility-trace credit event remains essentially inert (n_credit ~1-2%
    of n_decay), unchanged since 603h, now masked by the two independently-functioning fixes.
    routing=queue-experiment (substrate_queue escape-affordance-bridge entry: no change, its own
    scope is unaffected). MECH-357's mechanism-engagement claim is STILL neither confirmed nor
    falsified -- tested twice now, under two different confound sets, both inconclusive by
    design defect rather than by the mechanism itself. Status unchanged (candidate/v3_pending).

    [2026-08-10 governance, V3-EXQ-603s, confirmed failure_autopsy_V3-EXQ-603s_2026-08-10]: second
    consecutive config-only pressure escalation (603r static field, 603s mobile predators via
    aggressive env-drift) still fails to make LESION fail its own negative control -- this time on
    an exact tie (0.6667 vs 0.6667), not a near-miss. avoidance_efficacy numerically ~0 across all
    three arms independent of the LESION/INTACT/POSCTRL question. non_contributory -- the
    config-only pressure lever space appears exhausted. Routed to /implement-substrate, scoped to
    investigate BOTH genuine agent-directed predator pursuit AND an episode-length/event-suddenness
    environment-mechanics redesign before committing to full pursuit-AI implementation.
    pending_retest_after_substrate. Third consecutive inconclusive-by-design-defect result; MECH-357's
    mechanism-engagement claim remains neither confirmed nor falsified. Status unchanged
    (candidate/v3_pending).

    [2026-08-13 governance, V3-EXQ-603t, confirmed failure_autopsy_V3-EXQ-603t_2026-08-13]: fourth
    consecutive inconclusive-by-design-defect result on the Stage-H readiness guard. The SD-029
    scheduled_external_hazard fix (agent-relative, discrete adjacency event; the scoping-recommended
    successor to 603s's undirected mobile-predator drift) again failed to make ARM_LESION fail its
    own negative control -- G_H_LESION_frac reached full ceiling (1.0/3), i.e. less conflict than
    603s's exact tie (0.6667/0.6667), not more. non_contributory by design (readiness R4 unmet), not
    a MECH-357 falsification -- ARM_INTACT actually scored worse than ARM_LESION (0.333 vs 1.0) but
    this comparison is not load-bearing under the failed guard. Separately and newly characterized:
    avoidance_efficacy remains numerically underflowed to ~0 (1e-89 to 1e-127) in every arm/seed,
    consistent with 603h/603r/603s -- traced to a ~90-100:1 decay:credit tick ratio against
    leak_rate=0.02 in infralimbic_avoidance_gate.py, meaning the gate's suppression has been governed
    by the annealing scaffold_floor alone, never by learned efficacy, in every run to date. Config-only
    Stage-H pressure levers (static field, mobile-predator drift, scheduled discrete adjacency) now
    appear exhausted across three distinct designs; routed to /implement-substrate to wire the
    already-built hazard_agent_pursuit (causal_grid_world.py) into scaffolded_sd054_onboarding.py --
    the remaining candidate. epistemic_category unchanged (standard); status unchanged
    (candidate/v3_pending). MECH-357's mechanism-engagement and survival predictions remain neither
    confirmed nor falsified after four attempts.

    [2026-08-16 governance apply | V3-EXQ-603u -> MECH-357 | failure_autopsy_436f-603u-precondition-blocked-cluster_2026-08-16] DISPOSITION: evidence_direction non_contributory. evidence_direction STANDS at non_contributory; evidence_quality_note APPEND; pending_retest_after_substrate stays true, re-scoped from after-a-discriminating-pressure to after-the-eligibility-trace-fix-AND-a-graded-scoring-window-scoped-DV; epistemic_category STANDS at standard (explicitly re-affirmed against a live substrate_ceiling alternative -- see recommended_epistemic_category_note) -> standard

    [2026-08-16 governance, V3-EXQ-603u, failure_autopsy_436f-603u-precondition-blocked-cluster_2026-08-16, successor to V3-EXQ-603s/603t]: sixth consecutive inconclusive Stage-H run (603h/k/r/s/t/u), fourth distinct pressure design, 19.2 h hub compute. Readiness 4/5 MET (PAG freeze on LESION 1.0; gate engages AND suppresses freeze on INTACT 1.0; Stage-0 z_goal 0.667 at floor; POSCTRL survivability 1.0). The load-bearing ABSOLUTE criterion G_H_INTACT_clears_2of3 PASSED (1.0); the load-bearing DISCRIMINATION criterion G_H_INTACT_beats_LESION FAILED because G_H_LESION_frac = 1.0 as well -- agent-directed hazard pursuit (hazard_agent_pursuit=0.9 on top of 603s's every-step drift at p=0.6) did NOT reintroduce the Pavlovian-instrumental conflict. This DISCHARGES, with a negative answer, the probe-gated question that substrate_queue mech357-freeze-incompatible-pressure-mechanism and closure node mech357_avoidance_efficacy:BUILD were opened to ask; that entry's status implemented_pending_validation is now stale. All four named pressure designs are exhausted. TWO INDEPENDENT vacuities, only one of which the self-route names. (1) DV SATURATION: hazard_stage_median_last_window = exactly 200.0 (the episode cap) in 7 of 9 arm-seeds, on a strongly bimodal (200 or 3-6) distribution, measured through a median-of-10 against a 75-step floor -- the ruler is pegged for all three arms. (2) GATE EXTINCTION IN THE SCORING WINDOW: mech357_avoidance_efficacy underflows to 5.7e-120 / 4.6e-118 / 2.1e-123 on the INTACT seeds, with decay:credit ratios of 86:1 / 188:1 / 120:1 and n_freeze_suppressed SATURATING mid-run, while mech357_scaffold_floor anneals 0.8 -> 0.0 and unmasks it -- so the INTACT arm was functionally LESIONED inside the very window the DV measures. The gate_engaged / gate_suppresses readiness checks are whole-run aggregates and cannot see this. epistemic_category STAYS standard (deliberately NOT substrate_ceiling: the unblocking eligibility-trace build is already in flight and the claim must keep its v3 experiment lane). non_contributory: neither confirmed nor falsified; the mechanism was extinct, the ruler saturated and the pressure absent, simultaneously. Failure-location MIXED across mechanism+measures+environment, NOT chargeable to REE. The manifest's own self-route ('re-calibrate UP') is REJECTED: a 7th same-question pressure recalibration is refused, since a pressure increase cannot fix the efficacy underflow and the cheapest live hypothesis (a graded re-read of the ALREADY-RECORDED 603s/603t/603u per-episode trajectories) costs zero compute. pending_retest_after_substrate stays true, scoped to after-the-eligibility-trace-fix AND a graded, scoring-window-scoped DV. PROMOTES/DEMOTES NOTHING this cycle.
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: freeze must commit on LESION arm and the gate must
    engage+suppress on INTACT arm. ALREADY MET ONCE, cleanly: V3-EXQ-603h reported
    pag_freeze_lesion_frac=1.0, gate_engaged_intact_frac=1.0,
    gate_freeze_suppressed_intact_frac=1.0.

    ACTUAL RESULT TO DATE: V3-EXQ-603h ran the design and FAILED at the survival
    criterion specifically -- mechanism genuinely engages (one seed reached
    avoidance_efficacy=0.633) but G_H_INTACT=0/3, tied with G_H_LESION=0/3
    (avoidance_gate_insufficient). The confirmed autopsy attributes this to two
    independent gaps, not a mechanism defect: (1) escape-affordance bridge -- NOW
    RESOLVED (SD-059/MECH-358 PASSED, V3-EXQ-603q, reconfirmed 866b); (2) harm-pathway
    never trained -- resolved at the survival-leg diagnostic level (V3-EXQ-603k PASS).
    Neither fix has been combined into a claim-tagged rerun. No experiment currently in
    the queue tags MECH-357. Chip spawned
    (chip-20260808-mech357-combined-retest) to queue the combined G1+G2+G3 rerun.

    CONFIRMING: a claim-tagged rerun with BOTH fixes wired in reports G_H_INTACT >=
    2/3 AND strictly greater than G_H_LESION, with suppression onset tracking the
    LEARNED avoidance_efficacy trace rather than instantaneous threat arousal (the
    SD-037/MECH-281 confound this claim's distinctness depends on ruling out).

    FALSIFYING: the same rerun reports readiness_met=true (mechanism confirmed
    engaging, as in 603h) but G_H_INTACT still fails to clear 2/3 or fails to beat
    G_H_LESION even with BOTH known confounds closed (bridge wired, harm pathway
    trained) -- i.e. un-freezing plus a directed-escape credit table still does not
    translate into survival. That would mean the freeze-suppression mechanism itself,
    not just its two known upstream gaps, is insufficient, and a third gap would need
    diagnosing rather than assuming the mechanism is sound. Independently falsifying:
    suppression onset tracking instantaneous z_harm_a/threat-scale spikes rather than
    the learned avoidance_efficacy trace -- that collapses the SD-037/MECH-281
    distinction this claim rests its architectural distinctness on.
  location: docs/architecture/sd_058_instrumental_avoidance_acquisition.md

- id: SD-059
  title: "Relief/safety escape-affordance bridge: SD-058/MECH-357 supply an ilPFC freeze-SUPPRESSION gate but its avoidance_efficacy is a GLOBAL SCALAR that only penalises the no-op/freeze class -- compute_action_bias by design 'does NOT compute the escape direction'. V3-EXQ-603h (engaged-but-insufficient, readiness met) showed the consequence: the agent un-freezes but acquires no directed escape (seed-43 scalar efficacy 0.633 -> WORST survival 11.0). Moscarello & LeDoux 2013: active avoidance needs the LA/BA->NAcc relief/safety action-credit half that positively reinforces a SPECIFIC avoidance response, not only the suppression half. REE owns relief (MECH-302/SD-050) + safety (MECH-303/304/SD-052/SD-051) but they are UNWIRED to avoidance. SD-059 adds the architecture: an affordance-indexed (per-first-action-class) credit table that, when a directed action under threat drops z_harm_a (relief) or terminates the threat (safety), tags that action as an escape affordance approachable under future threat; MECH-358 is the bridge mechanism."
  claim_type: design_decision
  subject: defensive_action.escape_affordance_bridge
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-08-07
    needs_review: false
    evidence:
      from: "v3_exq_866b_603q_substrate_regression_check_20260803T192405Z_v3#failure_autopsy_V3-EXQ-866b_2026-08-07"
      as_of: 2026-08-07
      verdict: "non_contributory -- diagnostic substrate-regression re-check, not a claim test (see evidence_quality_note)"
  implementation_phase: v3
  v3_pending: true
  pending_retest_after_substrate: false  # 2026-06-17 governance: CLEARED by V3-EXQ-603q PASS (harm-pathway stabilization amend landed -> base harm landscape discriminative -> fair test); was gated 2026-06-09 on the Stage-H nav/survival leg + MECH-303/304 wiring
  epistemic_category: standard
  claim_level: architectural
  registered_utc: "2026-06-08"
  depends_on:
    - SD-058    # the instrumental-avoidance gate this extends (scalar -> affordance-indexed)
    - MECH-357  # the freeze-suppression + scalar-efficacy mechanism the bridge directs
    - MECH-302  # relief comparator (d(z_harm_a)/dt < 0) -- the relief-credit signal source
    - MECH-303  # contextual safety prediction -- safety-credit consistency
    - MECH-304  # cue-specific conditioned safety -- safety-credit consistency
    - SD-011    # z_harm_a affective stream -- the threat signal the bridge reads
    - MECH-279  # PAG freeze -- the reaction the avoidance gate suppresses
  distinct_from:
    - SD-037    # orexin override: REFLEXIVE arousal/drive-driven PAG-exit raise -- bridge credit is LEARNED-efficacy-gated DIRECTED approach
    - MECH-281  # orexin motor-coupling: REFLEXIVE urgency-interrupt lower -- not directed escape affordance
    - MECH-302  # relief comparator fires on the CURRENT state; the bridge binds an ACTION to relief for future-threat approach
    - MECH-304  # safety store predicts threat-absence at CURRENT state; the bridge indexes by ACTION/direction
  functional_restatement: >
    Architectural decision (V3-tractable). MECH-357's scalar avoidance_efficacy
    un-freezes the agent but binds no specific action to relief, so there is no escape
    AFFORDANCE -- only a global "avoidance works" scalar (the V3-EXQ-603h FAIL). SD-059
    adds an affordance-indexed (per-first-action-class) credit table with two
    independently-toggleable halves: a RELIEF half (a directed action under threat that
    drops z_harm_a credits relief_affordance[action_class], MECH-302-consistent) and a
    SAFETY half (a directed action after which threat is absent credits
    safety_affordance[action_class], MECH-303/304-consistent response-produced safety).
    Under future threat, E3 receives a bounded, threat-context-gated NEGATIVE
    (favoured) approach bias toward credited classes -- the DIRECTED escape. Three
    guards keep it from swamping food/goal approach or forming pathological habit
    loops: clamp to bias_scale, threat-context gate (zero when safe), per-tick leak.
    Distinct from the reflexive escape-from-freeze levers (SD-037/MECH-281) and from
    the generic relief/safety rows (MECH-302/303/304 fire on the current state).
  implementation_note: >
    IMPLEMENTED 2026-06-08 (substrate; v3_pending until the 4-arm validation EXQ
    PASSes). Mechanism is MECH-358 (ree-v3/ree_core/pfc/escape_affordance_bridge.py,
    EscapeAffordanceBridge -- pure-arithmetic regulator behind
    use_escape_affordance_bridge, no-op default, bit-identical OFF). agent.py wiring:
    update() in sense() (after the MECH-357 eligibility update), approach-bias in
    select_action() (after the MECH-357 action-bias), action-class cache + reset.
    HazardAvoidanceResult.escape_bridge_state surfaces the bridge state so the
    validation manifest can gate non-vacuity. Routed by
    failure_autopsy_V3-EXQ-603h_2026-06-08.
  evidence_quality_note: >
    Validation EXQ V3-EXQ-603i queued (diagnostic, claim_ids=[]): thought-intake
    Section 5 4-arm (ARM_BASE_IA_ONLY / ARM_RELIEF_BRIDGE / ARM_SAFETY_BRIDGE /
    ARM_RELIEF_SAFETY_BRIDGE) + a nav-competence positive control so a flat G_H across
    all bridge arms is attributable to a survival/navigation ceiling rather than the
    bridge. Non-vacuity gate: each enabled bridge half must increment its credit before
    G_H is scored. Acceptance G_H >= 2/3 AND improves over ARM_BASE_IA_ONLY; secondary
    P1 survival transfer without over-avoidance/starvation. The relief detector reads
    z_harm_a / the world-forward, so P0 keeps the SD-056 e2 contrastive warmup + a
    readiness precondition (else credit re-starves like 603h). Not promotable until the
    EXQ PASSes (v3_pending). Design doc: docs/architecture/sd_059_escape_affordance_bridge.md.
    [2026-06-14] First scored evidence V3-EXQ-603l (4-arm bridge behavioural retest) self-routed
    weakens but is reclassified non_contributory by failure_autopsy_V3-EXQ-603l_2026-06-10
    (confirmed): the discrimination criterion best_bridge_G_H > G_H_BASE was structurally
    unsatisfiable because the 603k harm-pathway-training readiness fix raised ARM_BASE_IA_ONLY to
    the binary survival ceiling (3/3), and the binary G_H gate saturated; the bridge fired
    non-vacuously and the both-arm showed ~35% longer mean hazard-stage survival (graded benefit
    invisible to the binary gate). Demotion threshold NOT reached (test was not fair). Retest pending
    a REDESIGN EXQ (603o: headroom hazard regime + continuous survival metric), NOT new substrate.
    [2026-06-17 governance: V3-EXQ-603q PASS supports SD-059 (non-degenerate; supersedes 603o).
    Continuous-survival redesign on the stabilized harm substrate: all 4 preconditions met (PAG
    freeze + ilPFC gate engage on base; each enabled bridge half fires non-vacuously; base harm
    landscape discriminative; Stage-0 lights z_goal), PRIMARY criteria both_bridge AND best_bridge
    mean Stage-H survival lift >= 10% PASS (base 37.7 -> safety 46.6). Binary G_H 2/3 supplementary
    (FALSE, explicitly NOT load-bearing per the redesign). evidence_direction supports
    (exp_conf 0.775). pending_retest_after_substrate CLEARED. Promotion still HELD by the v3_pending
    gate + single genuine run (n=1); a confirmatory run would move it toward provisional.]

    [2026-08-07 governance, V3-EXQ-866b, confirmed failure_autopsy_V3-EXQ-866b_2026-08-07,
    diagnostic-purpose PASS routed through /failure-autopsy per this cycle's broadened gate]:
    re-confirms 603q's substrate reproduction (base_mean_survival=33.9 vs reference 37.725, ratio
    0.90, clears the 0.5x floor). non_contributory BY DESIGN -- byte-identical config re-run,
    scoring_excluded="diagnostic_probe" -- does not change confidence or
    pending_retest_after_substrate. A substrate-hash provenance discrepancy between seed-cell-start
    and manifest-write time (~2.5h apart) was found and treated as a recording-harness gap, not
    grounds to distrust this run's own internally-consistent per-seed numbers; chipped as an
    infra-tooling fix. No status change.
  location: docs/architecture/sd_059_escape_affordance_bridge.md

- id: MECH-358
  title: "Relief/safety escape-affordance bridge mechanism (instantiates SD-059): a pure-arithmetic regulator (ree_core/pfc/escape_affordance_bridge.py, sibling to the SD-058/MECH-357 gate) extending the MECH-357 scalar avoidance_efficacy into a per-first-action-class credit table. RELIEF half: a directed action under threat that drops z_harm_a (delta = prev - now > floor) credits relief_affordance[class] (EMA toward 1; MECH-302-consistent). SAFETY half: a directed action after which threat is absent (threat_scale <= 0) credits safety_affordance[class] (MECH-303/304-consistent response-produced safety). Approach bonus: under future threat, a per-candidate NEGATIVE (favoured) E3 score-bias toward credited classes = -clamp(approach_gain * threat_scale * combined_affordance[class], 0, bias_scale); the no-op/freeze class never gets a bonus. Three guards: bias_scale clamp, threat-context gate (zero when safe), per-tick leak."
  claim_type: mechanism_hypothesis
  subject: prefrontal.relief_safety_escape_affordance_bridge
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-08-07
    needs_review: false
    evidence:
      from: "v3_exq_866b_603q_substrate_regression_check_20260803T192405Z_v3#failure_autopsy_V3-EXQ-866b_2026-08-07"
      as_of: 2026-08-07
      verdict: "non_contributory -- diagnostic substrate-regression re-check, not a claim test (see evidence_quality_note)"
  implementation_phase: v3
  v3_pending: true
  pending_retest_after_substrate: false  # 2026-06-17 governance: CLEARED by V3-EXQ-603q PASS (harm-pathway stabilization amend landed -> base harm landscape discriminative -> fair test); was gated 2026-06-09 on the Stage-H nav/survival leg + MECH-303/304 wiring
  epistemic_category: standard
  claim_level: mechanistic
  registered_utc: "2026-06-08"
  depends_on:
    - SD-059    # the architecture this mechanism instantiates
    - MECH-357  # the scalar avoidance gate it extends to affordance-indexed
    - MECH-302  # relief-credit signal (d(z_harm_a)/dt < 0)
    - MECH-303  # contextual safety -- safety-credit consistency
    - MECH-304  # cue-specific safety -- safety-credit consistency
    - SD-011    # z_harm_a threat signal
  distinct_from:
    - MECH-357  # scalar efficacy penalises the no-op class; MECH-358 REWARDS the credited directed class (directed escape)
    - SD-037    # reflexive orexin override -- not learned-efficacy-gated directed approach
    - MECH-281  # reflexive urgency-interrupt lower -- not directed escape affordance
  functional_restatement: >
    Mechanism hypothesis (V3-tractable). Two per-action-class credit tables
    (relief_affordance, safety_affordance), each an EMA toward 1 gated by its half
    flag, both leaking per tick. Affordance-indexing is by FIRST-ACTION CLASS (the
    directed escape direction in the discrete action space) -- the minimal V3
    instantiation of escape_affordance[action]; location/policy indexing is a deferred
    refinement the nav-competence positive control disambiguates. The approach bonus
    is threat-context-gated and bias_scale-clamped so it cannot swamp food/goal
    approach or dominate the additive score-bias chain. Falsifiable: with the RELIEF
    and/or SAFETY half ON in the Stage-H survival leg, G_H lifts over the
    SD-058/MECH-357-only baseline (ARM_BASE_IA_ONLY) AND a nav-competence positive
    control rules out a survival/navigation ceiling; if neither half lifts G_H while
    nav-control also fails, the gap is navigation, not the bridge.
  implementation_note: >
    IMPLEMENTED 2026-06-08. ree-v3/ree_core/pfc/escape_affordance_bridge.py
    (EscapeAffordanceBridge + Config + Output); agent.py wiring (update in sense(),
    approach-bias + action-class cache in select_action(), reset preserves tables);
    config.py 13 no-op-default flags + from_dims; scaffolded_sd054_onboarding Stage-H
    surfaces escape_bridge_state. 8 SD-059 contracts + bit-identical OFF. Pure-
    arithmetic regulator -- no trained parameters, no phased training. MECH-094: no-op
    under simulation_mode.
  evidence_quality_note: >
    Validation V3-EXQ-603i (diagnostic, claim_ids=[]). Unit + activation smoke confirm
    relief + safety credit increment on directed harm-drop / threat-absence and the
    approach bias favours credited classes only under threat (zero when safe). Not
    promotable until the EXQ PASSes (v3_pending). Design doc:
    docs/architecture/sd_059_escape_affordance_bridge.md.
    [2026-06-14] First scored evidence V3-EXQ-603l self-routed weakens but is reclassified
    non_contributory by failure_autopsy_V3-EXQ-603l_2026-06-10 (confirmed): the discrimination
    criterion best_bridge_G_H > G_H_BASE was structurally unsatisfiable -- the 603k harm-pathway
    readiness fix raised base to the binary survival ceiling (3/3) and the binary G_H gate saturated;
    the bridge fired non-vacuously and the both-arm showed ~35% longer mean hazard-stage survival.
    Demotion threshold NOT reached. Retest pending a REDESIGN EXQ (603o: headroom hazard regime +
    continuous survival metric), NOT new substrate.
    [2026-06-17 governance: V3-EXQ-603q PASS supports MECH-358 (non-degenerate; supersedes 603o).
    The escape-affordance bridge mechanism lifts Stage-H survival on the stabilized harm substrate:
    both_bridge AND best_bridge mean survival lift >= 10% (base 37.7 -> safety 46.6), all 4
    preconditions met, criteria_non_degenerate all true (binary G_H supplementary-only). per-claim
    evidence_direction supports (exp_conf 0.775). pending_retest_after_substrate CLEARED; promotion
    HELD by v3_pending + n=1 genuine run.]

    [2026-08-07 governance, V3-EXQ-866b, confirmed failure_autopsy_V3-EXQ-866b_2026-08-07,
    diagnostic-purpose PASS routed through /failure-autopsy per this cycle's broadened gate]:
    re-confirms 603q's substrate reproduction (base_mean_survival=33.9 vs reference 37.725, ratio
    0.90, clears the 0.5x floor). non_contributory BY DESIGN -- byte-identical config re-run,
    scoring_excluded="diagnostic_probe" -- does not change confidence or
    pending_retest_after_substrate. A substrate-hash provenance discrepancy between seed-cell-start
    and manifest-write time (~2.5h apart) was found and treated as a recording-harness gap, not
    grounds to distrust this run's own internally-consistent per-seed numbers; chipped as an
    infra-tooling fix. No status change. See sibling SD-059's identical evidence_quality_note.
  location: docs/architecture/sd_059_escape_affordance_bridge.md

# Registered 2026-06-09 -- candidate-differentiated affective gradients (V4/V5 cluster).
# Seeded by the V3-EXQ-643 modulatory-authority autopsy: the modulatory/curiosity
# contribution had magnitude but ZERO cross-candidate range, so E3 had no affective
# gradient to rescale into selection authority. The V3-narrow fact (range-not-magnitude
# readiness; root cause = float32 catastrophic cancellation under SD-056 online training)
# is ALREADY owned by V3-EXQ-643a + behavioral_diversity_isolation:GAP-A/GAP-B + MECH-341 +
# SD-056 and is deliberately NOT re-registered here. MECH-359/360/361 are the genuinely-new
# V4/V5 generalisation -- one candidate-differentiated affective-gradient structure serving
# selection authority + expression + hippocampal indexing. All substrate_conditional / v4,
# off the GAP-7 / V3 critical path; DO NOT build in V3 until routed by experiment.
# Source: docs/thoughts/2026-06-06_Candidate-differentiated_affective_gradients.md
# Intake: evidence/planning/thought_intake_2026-06-06_Candidate-differentiated_affective_gradients.md

- id: MECH-359
  title: "Candidate-differentiated affect vector: for proto-affect to carve behaviour it must carry per-candidate (cross-candidate) range, not merely per-tick magnitude. Each E3 candidate trajectory carries a multi-channel affect vector (curiosity / safety / harm-sensory / harm-affective / effort / relief / blocked-agency) so the same world-state can support different candidate rankings (approach / inspect / retreat / persist / retry / rest / reorient) under different internal pressures. A scalar added equally to all K candidates cannot change an argmax; only a per-candidate-differentiated contribution can."
  claim_type: mechanism_hypothesis
  subject: affect.candidate_differentiated_gradient
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  phase_locked: true  # intrinsic V4 scope: own notes "the right response is to wait for
                      # the upstream substrate, NOT more experiments"; "DO NOT build in
                      # V3". MECH-463/465 reference it as a comparative/contrasting
                      # hypothesis, not a prerequisite. Set 2026-08-10 per chip
                      # chip-20260810-phase-consistency-reconciliation.
  version_relevance: v4_v5
  depends_on:
    - MECH-055
    - SD-011
    - MECH-341
    - SD-056
  location: docs/architecture/candidate_differentiated_affective_gradients.md
  notes: "Bridge primitive: the same structure that lets affect choose between actions may also let affect become visible in behaviour (MECH-360) and memorable as an event (MECH-361). The NARROW V3 instance of this principle -- that the modulatory/curiosity contribution must carry cross-candidate range > floor before an authority rescale can act -- is already operationalised and is NOT this claim: V3-EXQ-643 FAILed precondition_unmet (modulatory_authority_active_frac=0.0); the binding cause was float32 CATASTROPHIC CANCELLATION under SD-056 online training (E3 scores ~1e32, modulatory range below float32 ULP), fixed by V3-EXQ-643a (PASS) with a range/non-vacuity readiness gate; the per-candidate-collapse root cause is owned by behavioral_diversity_isolation:GAP-A (cand_world_pairwise_dist=0.0) / GAP-B and MECH-341 / SD-056. MECH-359 is the V4/V5 GENERALISATION to an explicit multi-channel per-candidate affect representation feeding selection, expression, and memory -- not a re-statement of the V3 numerical fact. substrate_conditional: depends on a V4 per-candidate multi-channel affect substrate not yet built; promote/demote suppressed; the right response is to wait for the upstream substrate, NOT more experiments on the current single-channel modulatory path. DO NOT build in V3."
  what_would_answer: |
    NON-DEGENERACY PRECONDITION (hard gate -- do not attempt to read this
    claim without it): the V4 per-candidate multi-channel affect substrate
    (a real, separately-tracked affect vector -- curiosity / safety /
    harm-sensory / harm-affective / effort / relief / blocked-agency -- per
    E3 candidate trajectory) must actually be BUILT first; it does not exist
    in V3 and this claim is explicitly DO-NOT-BUILD-IN-V3. Do not queue an
    experiment against this claim until that substrate exists. Once built,
    a run is only informative if each affect channel shows genuine
    cross-candidate RANGE (variance across the K candidates at a given
    tick), not merely per-tick magnitude -- apply the same
    range/non-vacuity readiness discipline already established for the
    V3-narrow instance of this principle by V3-EXQ-643a (which fixed
    V3-EXQ-643's float32-cancellation-masked zero-range failure). A run
    whose channels are magnitude-only / flat across candidates (e.g.
    cand_world_pairwise_dist collapsed to floor, mirroring the historical
    behavioral_diversity_isolation:GAP-A collapse) must self-route
    substrate_not_ready -- it is not a verdict on this claim, only a
    precondition failure. Per this claim's own notes, do not treat this
    claim as testable until a downstream per-candidate-affective-variance
    readiness result exists on the GAP-A-ready substrate (the MECH-341
    within-class-representative-diversity retest plus GAP-A bias channels
    demonstrating that per-candidate range actually bites at behavioural
    runtime).
    CONFIRMING signature: with the multi-channel substrate built and
    non-vacuity established, the SAME world-state, under DIFFERENT
    internal-pressure profiles (different relative weighting/activation
    across the affect channels), yields DIFFERENT candidate rankings /
    argmax selections (approach / inspect / retreat / persist / retry /
    rest / reorient) -- i.e. the affect vector is shown to be doing real
    selection work, not just riding along with a world-state-determined
    ranking. This is the claim's own core prediction: "only a
    per-candidate-differentiated contribution can [change an argmax]."
    FALSIFYING signature: even after the substrate is built and each
    channel independently demonstrates non-vacuous cross-candidate range,
    the selected action / argmax remains invariant to variation in the
    affect vector for a fixed world-state -- i.e. candidate ranking is
    fully explained by world-state features alone and the differentiated
    affect channels carry no additional selection authority. This would
    refute the mechanism even though the range precondition was met, and
    would need to be distinguished from a precondition failure (see above)
    before being read as a genuine falsification.
    NOTE: this claim is the V4/V5 GENERALISATION only. Do not conflate a
    result here with the already-closed V3-narrow question (whether the
    existing single-channel modulatory/curiosity contribution needed a
    range-not-magnitude gate) -- that is resolved and owned by
    V3-EXQ-643/643a, MECH-341, behavioral_diversity_isolation:GAP-A/B, and
    SD-056, not by this claim.

- id: MECH-360
  title: "Affective expression begins as emergent action geometry: because candidate-differentiated affect (MECH-359) selects trajectories, it also shapes the visible STYLE of behaviour -- hesitation, latency, approach angle, retreat, repeated retry, stopping, resumption, oscillation between candidates, avoidance margin, decommitment, repair-seeking. Expression is therefore initially an emergent readout of candidate-level affective arbitration, not a separately-engineered output channel; a later social agent can infer internal state from HOW an action occurred, not only what action occurred."
  claim_type: mechanism_hypothesis
  subject: expression.action_geometry_emergent_readout
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - MECH-359
    - MECH-041
  location: docs/architecture/candidate_differentiated_affective_gradients.md
  notes: "Complementary to, NOT a replacement for, MECH-041 (affective expression broadcasts control-plane regime to reduce social prediction load). MECH-041 frames expression as a deliberate broadcast of the control-plane regime; MECH-360 frames expression as the emergent geometric RESIDUE of per-candidate affective arbitration, prior to any broadcast intent -- the visible style falls out of which trajectory the affect vector selected. The two compose: emergent geometry (MECH-360) is the substrate a later broadcast channel (MECH-041) can come to exploit and stylise. V4/V5; off the V3 critical path. substrate_conditional on the MECH-359 per-candidate affect substrate. DO NOT build expression/social-signalling in V3 until routed by experiment."

- id: MECH-361
  title: "Candidate-gradient hippocampal episode schema: enrich the event trace from state -> action -> outcome to state -> candidates-considered -> affective gradients over candidates -> selected action -> outcome -> residue (relief / harm / blocked-agency / safety update), and use the affective gradient as a memory WRITE-WEIGHT and RETRIEVAL-QUERY variable -- high-gradient episodes are preferentially written, and later similar-gradient states retrieve prior action-affect-outcome arcs rather than undifferentiated state-action-outcome tuples."
  claim_type: mechanism_hypothesis
  subject: memory.candidate_gradient_episode_schema
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - MECH-359
    - MECH-261
    - MECH-074
    - MECH-094
  location: docs/architecture/candidate_differentiated_affective_gradients.md
  notes: "Amends the CONTENT schema of MECH-261 (mode-conditioned write gating -- WHAT is written into the episodic trace), not the gate itself (WHETHER a substrate may write). The affect-gradient-as-write-weight seam is the sharper variable on top of MECH-074 (BLA arousal-modulated hippocampal write depth): here the write weight is the per-candidate affective GRADIENT, not generic arousal. MECH-094 provenance still applies (simulated candidates must not be indexed as real experience). Biological anchors for a later targeted review (none load-bearing for this candidate registration): McGaugh 2004 Annu Rev Neurosci; Cahill & McGaugh 1998 TINS; Dolcos, LaBar & Cabeza 2004 Neuron; Girardeau, Inema & Buzsaki 2017 Nat Neurosci 20:1634 (verified); Ballarini et al 2009 PNAS 106:14599 behavioural tagging (verified); Bechara et al 1994 Cognition + Damasio 1994 (somatic markers); Olafsdottir, Bush & Barry 2018 Curr Biol + Joo & Frank 2018 Nat Rev Neurosci (replay). V4/V5; off the V3 critical path. substrate_conditional on the MECH-359 per-candidate affect substrate. DO NOT build hippocampal write/retrieval integration in V3 until routed by experiment."

# Registered 2026-06-09 -- CA3 developmental sparse/structured connectivity intake (Vargas-Barroso/Jonas Nat Commun 2026 s41467-026-71914-x). V4/V5 developmental compass, off the V3 critical path. See docs/architecture/developmental_pruning_and_sparse_memory_cognifold.md and evidence/planning/thought_intake_2026-06-06_ca3_development_sparse_structured_connectivity.md.
- id: MECH-362
  title: "Subtractive developmental sparsification: mature sparse/structured memory connectivity emerges by pruning / down-weighting an initially over-connected, dense, near-random ('tabula plena') substrate, not by additively growing connections onto a sparse skeleton; correspondingly, mature recall requires convergent summation of several weak inputs rather than a single strong cue."
  claim_type: mechanism_hypothesis
  subject: development.subtractive_sparsification
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - MECH-120
    - ARC-019
  location: docs/architecture/developmental_pruning_and_sparse_memory_cognifold.md#mech-362
  notes: "Empirical anchor (verified): Vargas-Barroso, Watson, Navas-Olive, Schlogl & Jonas, Nature Communications 2026, s41467-026-71914-x (ISTA) -- multicellular patch-clamp of up to 8 CA3 pyramidal neurons at P7-8/P18-25/P45-50; recurrent connectivity dense/strong/near-random -> distributed/sparse/structured; early a single synaptic event triggers a postsynaptic spike, mature requires spatial summation of several weaker inputs; authors' 'tabula plena' (start over-connected, refine by pruning) framing. Cross-species corroboration that the mature endpoint is structured: Tang et al, Cell 2024 (human CA3). DISTINCT FROM MECH-120 (SWS synaptic homeostasis / Tononi SHY): MECH-120 is nightly homeostatic weight down-scaling; MECH-362 is a one-way developmental connectivity trajectory (different timescale/directionality/function), though they may share down-weighting machinery. Most naturally AMENDS ARC-019 (staged developmental curriculum, currently additive) by adding a subtractive pruning stage, rather than standing fully alone. The convergent-weak-input-vs-single-strong-cue corollary is the one strand with present-day V3 relevance and is folded in as a DIAGNOSTIC LENS (not a separate V3 claim) in evidence/planning/modulatory_bias_selection_authority_design.md 'Developmental framing (compass)'; registering it as its own V3 claim would duplicate the active selection-authority/rule-apprehension claims (MECH-314/341, ARC-062). Pruning is not necessarily deletion -- down-weighting/gating/residue-tagged de-authorization are candidate analogues (see Q-057). V4/V5; off the V3 critical path. substrate_conditional on a developmental pruning/sparsification substrate not yet built. DO NOT build a developmental pruning substrate or queue an experiment in V3 until routed by an explicit version decision."
- id: Q-057
  title: "Should REE distinguish an early over-connected exploratory developmental substrate from a mature sparse/structured substrate -- and is developmental sparsification best modelled as deletion, down-weighting, gating, or residue-tagged de-authorization?"
  claim_type: open_question
  subject: development.exploratory_overconnectivity_vs_mature_sparsity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Resolve in two parts on a developmental substrate (V4/V5). Architecture leg:
    contrast an early over-connected exploratory substrate against a mature
    sparse/structured one and ANSWER "distinguish the two regimes" if the
    over-connected regime shows immature single-cue authority (one cue can release
    action) that the mature regime replaces with convergent-weak-input recall
    (action release requires multiple weak inputs to converge) -- i.e. REE-v3's
    cue-authority problem is partly a developmental-immaturity signature, not a
    fixed design flaw. Mechanism leg: implement developmental sparsification as
    each of deletion / down-weighting / gating / residue-tagged de-authorization
    and ANSWER which matches biology by whether the trace is recoverable after
    sparsification (deletion is irreversible;
    down-weighting/gating/residue-de-authorization preserve a re-instatable trace)
    and whether offline integration acts as pruning/contextualisation rather than
    mere consolidation. HARD PRECONDITION: an upstream developmental substrate
    with an explicit immature->mature trajectory and developmental gates on
    memory-trace action-influence must exist (depends_on ARC-019 / MECH-362, CA3
    developmental intake) -- this is absent in V3 and the claim is explicitly
    V4/V5-parked, so DO NOT queue against it in V3 (the regimes cannot be
    instantiated, the contrast is undefined). Wait for the planned upstream
    developmental substrate before reading either leg.
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - ARC-019
    - MECH-362
  location: docs/architecture/developmental_pruning_and_sparse_memory_cognifold.md#q-057
  notes: "From the CA3 developmental intake (see MECH-362 anchor). Sub-questions: (1) does REE-v3's cue-authority problem partly reflect immature single-cue authority rather than mature convergent-weak-input recall? (2) can offline integration act as pruning/contextualisation rather than merely consolidation? (3) what developmental gates should exist before a memory trace can influence action release? Set epistemic_category: substrate_conditional explicitly (overriding the open_question -> answer_state inference) so the narrow_open_question recommendation does NOT fire -- this is a V4-parked question awaiting an upstream developmental substrate, not a V3-tractable one to narrow by experiment. V4/V5; off the V3 critical path. DO NOT queue an experiment against this in V3."

# Registered 2026-06-09 -- competitive-interactions / cognifold-stability intake (Luppi et al. Nature Neuroscience 29(4):915-933, 2026, DOI 10.1038/s41593-026-02205-3, verified). Signed competitive coupling as a first-class generative cognifold primitive. V4/V5 control-plane/cognifold compass, off the V3 critical path. See docs/architecture/cognifold_signed_coupling.md and evidence/planning/thought_intake_2026-06-06_competitive_interactions_cognifold_stability.md.
- id: ARC-084
  title: "Typed signed cognifold coupling: the cognifold represents inter-field coupling as a typed, signed edge -- cognifold_edge = {source, target, sign, gain, precision, gate, timescale, write_authority} -- with competition (sign < 0) as a FIRST-CLASS GENERATIVE coupling mode alongside cooperation (sign > 0) and gated-decoupling, not an emergent by-product of selection softmaxes. Three coupling modes: cooperative (binding, coherent trajectory, shared context), competitive (suppress incompatible trajectories, preserve boundaries, prevent runaway resonance, stop one field absorbing all others), gated-decoupling (isolate simulation / offline integration / unsafe candidates from release authority)."
  claim_type: architectural_commitment
  subject: cognifold.signed_coupling_edge
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - ARC-005
  location: docs/architecture/cognifold_signed_coupling.md#arc-084
  notes: "From the competitive-interactions intake. epistemic_category set substrate_conditional explicitly (overriding the architectural_commitment -> substrate_coherence inference): this is NOT yet the adopted substrate -- it is a candidate commitment gated on a V4 multi-field signed-edge layer that does not exist in V3. V3 REALITY (containment-only, do NOT build a parallel signed-edge module): competition is already present but LOCAL and IMPLICIT -- BG winner-take-all lateral inhibition + Cisek/Kalaska affordance-competition (MECH-090), symmetric Go/NoGo dMSN/iMSN competitive model (ARC-030), top-k competitive selection into E3 (MECH-254); gated-decoupling is the MECH-094 waking-only write gate / MECH-090 commitment latch. What is NEW is representing the SIGN as an explicit first-class edge property at the cognifold-field level. Same shape as the attention=distributed-precision-selection map (ARC-005/MECH-251/254/255/259/261/347/SD-032a): REE owns the pieces, lacks the explicit map; a unifying MAP, not a new module. CAUTIONS (Luppi et al.): competition != synaptic inhibition; a competitive edge is not intrinsically harmful/pathological/ethically-negative; competition != reward subtraction. biology-before-formal-definitions: any V4 SD/MECH operationalising signed coupling needs a biology lit-pull first. V4/V5; off the V3 critical path. DO NOT build in V3 until routed by an explicit version decision."
  what_would_answer: >
    Restated: once a V4 cognifold signed-edge layer exists, is competition (sign<0) genuinely
    a FIRST-CLASS GENERATIVE coupling mode -- carrying its own gain, precision, gate,
    timescale, and write_authority that can be independently manipulated and that does causal
    work no other mechanism reproduces -- or is it fully reducible to (a redescription of)
    emergent competition already present in local selection softmaxes (MECH-090 BG
    winner-take-all, ARC-030 Go/NoGo, MECH-254 top-k)?

    NON-DEGENERACY PRECONDITION: the V4 cognifold_edge layer must exist and be wired with
    genuinely independent sign/gain/precision/gate/timescale parameters at the FIELD level --
    not merely a relabeling or thin wrapper around MECH-090/ARC-030/MECH-254's existing local
    competition. A test is vacuous unless it can construct a scenario where the
    "softmax-emergent-competition" hypothesis and the "first-class-edge" hypothesis make
    DIFFERING predictions -- e.g. by holding local selection softmax temperature/top-k fixed
    while varying only the cognifold-edge sign/gain between two distant fields with no direct
    local-selection relationship. This is the same non-degeneracy floor Q-058 sets for the
    adjacent subsystem-scope question and MECH-363 sets for the stability-outcome question; do
    not re-derive a weaker substitute here.

    CONFIRMING: manipulating the sign/gain of a specific cognifold_edge produces a change in
    competitive dynamics between the two coupled fields (boundary preservation, suppression of
    an incompatible trajectory, prevention of runaway resonance) that CANNOT be reproduced by
    adjusting local selection softmax temperature or top-k alone -- a dissociation between
    edge-level and softmax-level manipulation, holding the other constant. Q-058's per-subsystem
    ablation and MECH-363's cooperative-only vs. cooperative+long-range-competitive comparison
    are the two evidence instruments; either producing a dissociation counts toward CONFIRMING
    ARC-084's representational claim specifically.

    FALSIFYING: across the tested subsystems, all observed inter-field competitive effects are
    fully accounted for by the existing local mechanisms (MECH-090, ARC-030, MECH-254) with no
    residual effect attributable to an independent signed-edge property -- Q-058's ablation
    returns "emergent competition suffices" everywhere it is tried, and MECH-363's stability
    comparison shows no dissociation between an edge-bearing and an edge-stripped (softmax-only)
    substrate. If so, the "unifying map" adds no causal power beyond what local softmaxes
    already provide, and ARC-084 reduces to a redescription rather than a first-class
    generative mechanism -- it should be demoted, not merely left candidate.

    CAUTIONS (carried from this claim's own notes, restated because a test design can easily
    conflate them): competition != synaptic inhibition; a competitive edge firing is not
    intrinsically harmful, pathological, or ethically negative; competition != reward
    subtraction. A test operationalising "competitive effect" as inhibition strength,
    harm/penalty magnitude, or a reward-subtraction term is testing the wrong thing.

    DO NOT design a V3 proxy for this -- per this claim's own instruction, do not build any
    part of the signed-edge layer in V3 until routed by an explicit version decision.
- id: MECH-363
  title: "Diffuse long-range competitive coupling is a generative stability requirement: stable, synergistic, hierarchical cognifold dynamics require signed competitive coupling -- specifically diffuse, long-range competition between distant fields with opposite profiles -- not merely local damping. An all-cooperative (all-positive) routed-field cognifold is unstable: every salient stream reinforces every other, producing runaway resonance / over-synchronisation / loss of modular segregation. Competition is the constructive ingredient that buys stabilisation, segregation, hierarchy, synergy, and subject-specificity."
  claim_type: mechanism_hypothesis
  subject: cognifold.long_range_competition_stability
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - ARC-084
    - ARC-005
  location: docs/architecture/cognifold_signed_coupling.md#mech-363
  notes: "Empirical anchor (verified): Luppi, A. I. et al., 'Competitive interactions shape mammalian brain network dynamics and computation', Nature Neuroscience 29(4):915-933 (2026), DOI 10.1038/s41593-026-02205-3 -- whole-brain models combining modular cooperative + diffuse long-range competitive interactions reproduce human/macaque/mouse dynamics better than cooperative-only models (which over-synchronise); competition preferentially links regions with OPPOSITE cytoarchitecture/gene/receptor profiles and raises synergy + hierarchy + neuromorphic performance. Falsifiable in principle by a multi-field coupling ablation (cooperative-only vs cooperative+long-range-competitive on the same field set), but SUBSTRATE-GATED: V3 has no explicit multi-field signed-edge layer to ablate, so a probe today would be vacuous -> substrate_conditional (the right response is to wait for the ARC-084 V4 substrate, NOT more experiments on the current local-softmax competition). DISTINGUISH two NON-equivalent failure axes: (a) runaway POSITIVE coupling / hypersync -- this claim's target (REE analogues: feedback entrapment, shared delusional coupling, over-stabilised attractor MECH-076); (b) MONOSTRATEGY / regime-collapse (MECH-309, owned by ARC-062/063) -- the OPPOSITE pole (too little apprehended structure, not too much coupling). Do NOT conflate; signed-coupling damping addresses (a), the rule-apprehension layer addresses (b). V4/V5; off the V3 critical path. DO NOT build in V3."
- id: Q-058
  title: "Which REE subsystems require an EXPLICIT signed competitive edge (ARC-084) rather than emergent softmax / winner-take-all / scalar-gating competition alone -- and what are the safety differences between inhibition, competition, decommitment, and residue-based veto across waking action, simulation, and offline integration?"
  claim_type: open_question
  subject: cognifold.explicit_vs_emergent_competition_scope
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Answered substrate-side, not by a V3 grid-world run. Build the ARC-084
    typed-signed-coupling cognifold (signed edges with sign<0 competition as a
    first-class generative mode) and, per inter-field edge, run an ablation:
    replace the EXPLICIT signed-competitive edge with the existing emergent local
    softmax / WTA / scalar-gating (MECH-090/ARC-030/MECH-254) and measure modular
    segregation, runaway-resonance onset, and boundary preservation. ANSWER 'this
    subsystem REQUIRES an explicit signed edge' if removing it collapses
    segregation / triggers over-synchronisation that the softmax alternative
    cannot recover (MECH-363's instability prediction realised); ANSWER 'emergent
    competition suffices here' if the softmax arm matches the signed-edge arm on
    stability and behaviour. The safety sub-question is answered by mapping which
    of {inhibition, competition, decommitment MECH-090/MECH-105, residue-veto
    SD-010/SD-011} actually fires across waking / simulation MECH-094 /
    offline-integration MECH-272-273 and showing whether they are dissociable.
    HARD PRECONDITION: the signed-coupling substrate (ARC-084) must EXIST and its
    competitive edges must carry non-zero gain -- a cognifold with only
    positive/cooperative edges, or with the competitive sign present but pinned at
    zero gain, cannot discriminate explicit-vs-emergent and yields a vacuous null.
    This is V4/V5; off the V3 critical path. The right response now is to wait for
    / design the ARC-084 substrate, NOT to queue a V3 experiment.
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - ARC-084
    - MECH-363
  location: docs/architecture/cognifold_signed_coupling.md#q-058
  notes: "From the competitive-interactions intake (section 2 open-question + section 8 design questions). Sub-questions: (1) which inter-field couplings genuinely need an explicit sign vs the existing local softmax/WTA (MECH-090/ARC-030/MECH-254)? (2) safety differences between inhibition, competition, decommitment (MECH-090/MECH-105), and residue-based veto (harm stream, SD-010/SD-011)? (3) how should competitive coupling differ across waking action vs simulation (MECH-094) vs offline integration (MECH-272/273 sleep cluster)? (4) should the AI-cognitive-failure taxonomy be re-indexed by missing/misweighted competitive coupling (cross-repo: Latent-Fields/ai-cognitive-failure-taxonomy, not REE_assembly)? epistemic_category set substrate_conditional explicitly (overriding open_question -> answer_state) so narrow_open_question does NOT fire -- this is a V4-parked question awaiting the ARC-084 substrate, not a V3-tractable one to narrow by experiment. V4/V5; off the V3 critical path. DO NOT queue an experiment against this in V3."

# Registered 2026-06-09 -- laughter cognitive-load / social-reconfiguration intake (thought_intake_2026-06-06_laughter_cognitive_load_social_reconfiguration.md)
- id: MECH-364
  title: "Laughter discharges accumulated E3 conflict/constraint load and thereby marks a safe inter-regime control-plane transition: beyond the per-cycle hypothesis-tag clear (MECH-110), iterated safe-confirmation produces a regime-level drop in active constraint pressure that releases the system from a threat-vigilant/high-commitment regime into an exploratory/play regime -- making laughter a boundary signal between control-plane regimes (ARC-016), not merely reward expression."
  claim_type: mechanism_hypothesis
  subject: respiratory.laughter_load_release_regime_boundary
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - MECH-110
    - MECH-107
    - ARC-016
    - MECH-027
    - MECH-076
  location: docs/architecture/laughter_social_load_release.md#mech-364
  notes: >
    From the laughter cognitive-load / social-reconfiguration intake. SOURCE-WEIGHT CAUTION: the
    prompting article (Neuroscience News, "Laughter Rewires Brain Architecture and Lowers Cognitive
    Load") summarises a cross-disciplinary popular-science SYNTHESIS (Dr. Jacqueline Harding, "The
    Brain That Loves to Laugh", Routledge), NOT a primary empirical study -- this claim is a
    compass-level hypothesis, not backed by citable experimental evidence. DISTINCT FROM MECH-110:
    MECH-110 is the MICRO mechanism (each laughter exhalation = one plan-sweep clearing a single
    threat-hypothesis tag, MECH-107); MECH-364 is the MACRO/regime-level consequence -- the
    cumulative effect of iterated safe-confirmation is a drop in E3 active constraint/conflict
    pressure (a regime-level load discharge), and that discharge is what marks/enables a safe
    transition between control-plane regimes (ARC-016). The note's primitive
    f(conflict_detected, threat_reclassified, social_safety, reframe_success, load_release,
    shared_alignment) maps load_release + the regime-boundary role onto this claim; tag-clear maps
    to MECH-110 and shared_alignment to MECH-110's V5 social-synchrony extension. PREDICTIONS
    (laughter-absence failure taxonomy): a system that cannot run reframe -> load-release -> regime
    re-open cannot soften rigid commitments after a benign reframe -> attractor lock-in (MECH-076),
    pathological-regime persistence (MECH-027), social-threat overclassification, feedback
    entrapment, belief fixation, excessive literalism. CAVEAT (non-affiliative): this is the BENIGN
    case; the same signal is not monotone-affiliative -- laughter can exclude, dominate, mask
    distress, or signal threat (see Q-059). SUBSTRATE NOTE: load_release requires an E3
    conflict/constraint-load readout; ARC-016 already exposes an E3-derived prediction-variance
    signal in V3, so a v3 substrate check could reclassify this toward implementation_phase v3 --
    held at v4/substrate_conditional pending that check (do NOT build or queue a probe in V3 until
    routed by an explicit version decision; a probe before the readout exists would be vacuous, cf.
    the play-mode cluster). V4/V5; off the GAP-7 / V3 critical path. NO change to MECH-110/ARC-016/
    MECH-027 (this claim depends on them, does not weaken or supersede them).
- id: Q-059
  title: "Does REE need a crying / distress-vocalisation social-guardrail analogue to laughter (MECH-110 / MECH-364), and how is social laughter adjudicated as social-field repair vs damage/exclusion -- given that laughter is not monotone-affiliative (it can exclude, dominate, mask distress, or signal threat)?"
  claim_type: open_question
  subject: social_guardrails.crying_analogue_and_laughter_valence
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Answered only against a social substrate that does not yet exist. Build the
    social-field substrate that gives REE multi-agent affiliation / exclusion
    dynamics, then (1) for the crying analogue: add a candidate high-arousal
    distress-vocalisation signal alongside laughter (MECH-110/MECH-364) and ablate
    it -- ANSWER 'a distinct distress/crying claim is warranted' if REE without it
    fails to recruit social repair / care-elicitation in states where laughter
    cannot fire (e.g. unresolvable threat), and the laughter mechanism alone
    leaves a coverage gap on the social-release/guardrail axis; ANSWER 'no
    separate claim needed' if the existing INV-058 distress-vocalisation entry
    plus laughter already span the axis. (2) For laughter valence: instrument
    social laughter in repair vs damage/exclusion contexts and test whether REE
    adjudicates it -- ANSWER 'laughter is correctly non-monotone-affiliative' if
    the same laughter signal is scored as field-repair in one context and as
    exclusion/dominance/masking in another, tracking the social field-state,
    rather than always reading affiliative. HARD PRECONDITION: a non-degenerate
    social field with at least two agents and a representable
    affiliation/exclusion gradient must exist; with a single-agent grid-world
    there is no social field for laughter valence to be repair-vs-damage WITHIN,
    so any read is vacuous. V4/V5; off the V3 critical path. Wait for the social
    substrate (the planned ARC-059 social spine extension); do NOT queue a V3
    experiment.
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - MECH-110
    - MECH-364
  location: docs/architecture/laughter_social_load_release.md#q-059
  notes: >
    From the laughter intake. GAP: REE owns a laughter mechanism (MECH-110, and now MECH-364) but
    NO crying / tears / sobbing claim exists in claims.yaml (only "distress vocalisation" appears
    inside the play-mode basic-expression catalog, INV-058). The raw thought pairs laughter with
    crying as the two halves of a social-release / social-guardrail axis; this question asks
    whether the high-arousal / distress-signal counterpart deserves its own claim, and how the
    valence of social laughter is adjudicated (repair vs damage). Set epistemic_category:
    substrate_conditional EXPLICITLY (overriding the open_question -> answer_state inference) so the
    narrow_open_question recommendation does NOT fire -- this is a V4/V5-parked question awaiting a
    social substrate, not a V3-tractable one to narrow by experiment. V4/V5; off the V3 critical
    path. DO NOT queue an experiment against this in V3.

# Registered 2026-06-09 -- hyperthymesia / autobiographical-temporality intake (La Corte, Piolino & Cohen, "Autobiographical hypermnesia as a particular form of mental time travel", Neurocase 31(4):188-192, 2025, DOI 10.1080/13554794.2025.2537950, verified). Autobiographical memory + future simulation as one self-tagged temporal event substrate. V4/V5 trajectory-memory / identity-tag compass, off the V3 critical path. See docs/architecture/autobiographical_temporality_and_future_simulation.md and evidence/planning/thought_intake_2026-06-06_hyperthymesia_autobiographical_temporality.md.
- id: ARC-085
  title: "Unified autobiographical temporal event substrate: retrospective replay (ARC-007) and prospective simulation (ARC-018) draw on ONE self-tagged event-token store rather than two parallel memory machineries. Autobiographical memory is an identity-indexed event field -- personal events bound to perspective, emotion, residue, and self-state -- not neutral storage; future trajectory generation is forward re-composition of the same event tokens, while replay is their backward reinstatement."
  claim_type: architectural_commitment
  subject: memory.autobiographical_temporal_event_substrate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  phase_locked: true  # intrinsic V4 scope, second confirmed instance of the ARC-039
                      # false-positive pattern. MECH-487's own 2026-08-07 governance
                      # review already found: "ARC-085 left at v4 DELIBERATELY, not
                      # reclassified: this claim's own depends_on comment and notes text
                      # above both already say ARC-085 is a 'candidate eventual host,'
                      # not a current requirement -- MECH-487 does not need ARC-085 to
                      # exist or be built for its own V3 content to stand. The
                      # phase-consistency WARN is a known false-positive here." Set
                      # 2026-08-10 per chip chip-20260810-phase-consistency-reconciliation
                      # so this stops silently re-triggering.
  version_relevance: v4_v5
  depends_on:
    - ARC-007
    - ARC-018
  location: docs/architecture/autobiographical_temporality_and_future_simulation.md#arc-085
  q060_taxonomy_decision: "2026-06-14 (ABM-1 / Q-060, user-adjudicated): autobiographical-event memory is a TAG / INDEXING CONVENTION over the existing episodic store (ARC-007), NOT a distinct first-class memory type with its own store. ARC-085 is therefore scoped as an identity-indexed, self/affect/provenance-tagged BINDING LAYER over the existing ARC-007 episodic event store -- the 'one self-tagged event-token store' IS the existing episodic store, made autobiographical by the fields each event token carries {self_state, perspective, affect, residue, source_status, committed_vs_imagined} plus an identity index that BOTH backward replay (ARC-007) AND forward re-composition (ARC-018) address. This is the parsimonious instantiation of ARC-085's own 'one substrate, not two parallel machineries' thesis: do NOT mint a third store. The decision keeps every downstream claim (MECH-365/366/361/368/429/430/431) as FIELD/GATE specialisations of the episodic token -- which is how they are already written -- rather than parallel substrates. The semantic (facts/relations) and task/procedural (policies/schemas) types stay distinct; only autobiographical-vs-episodic resolves to tag-not-type. NOT trivial: the index is still load-bearing V4 work (first-class provenance/perspective/affect/self_state fields + identity index + the one-way committed-vs-imagined gate). The empirical check that could overturn this design call toward distinct-type survives as Q-060.what_would_answer (the ARC-085 joint-degradation falsifier)."
  notes: "Empirical anchor (verified): La Corte, Piolino & Cohen, 'Autobiographical hypermnesia as a particular form of mental time travel', Neurocase 31(4):188-192 (2025), DOI 10.1080/13554794.2025.2537950 (Paris Brain Institute) -- single case TL (~17yo), TEMPau + TEEAM; exceptionally detailed emotion/spatio-temporal autobiographical recall AND future projection richer than controls, consistent with shared retrospective/prospective machinery (the constructive-episodic-simulation / Schacter-Addis line); deliberate 'memory-palace' organisation (identity-indexed event field). Computational analogue for a later targeted read (not load-bearing): Lampinen et al., 'Towards mental time travel: a hierarchical memory for RL agents', arXiv:2105.14039. epistemic_category set substrate_conditional EXPLICITLY (overriding the architectural_commitment -> substrate_coherence inference): this is NOT yet the adopted substrate -- it is a candidate commitment gated on a V4 self-tagged event-token store that does not exist in V3. V3 REALITY: REE owns the two halves SEPARATELY -- ARC-007 (hippocampal store/replay of paths through residue-field terrain) and ARC-018 (explicit rollouts + post-commitment viability map); what is NEW is the claim they are ONE substrate (falsifier: corrupting the shared store degrades both past-recall and future-simulation fidelity TOGETHER, vs independent degradation under a two-store model). TAXONOMY DECISION 2026-06-14 (see q060_taxonomy_decision above): ARC-085 adopted as an INDEXING/BINDING CONVENTION over the existing ARC-007 episodic store, NOT a new parallel store. DO NOT treat as precognition / perfect memory (single case; vivid != accurate; simulation != prediction -- per the intake guardrail). V4/V5; off the V3 critical path. DO NOT build a parallel autobiographical event substrate in V3 until routed by an explicit version decision."
  what_would_answer: >
    Non-degeneracy precondition: this claim is untestable until (i) a genuine
    corruption/ablation manipulation on the shared identity-tagged event-token store is
    available (not a proxy or a per-module ablation), and (ii) both retrospective replay
    fidelity (ARC-007) and prospective simulation fidelity (ARC-018) are independently
    measurable on that same substrate under matched conditions.

    Confirming: corrupting/ablating the shared, identity-tagged event-token store degrades
    BOTH past-recall (replay) fidelity AND future-simulation fidelity together, in
    proportion to the corruption -- consistent with one substrate serving both directions
    of address, per the taxonomy decision that ARC-085 is a binding/indexing layer over the
    existing ARC-007 store rather than a parallel machinery.

    Falsifying: corruption degrades one but not the other -- e.g. replay fidelity drops
    while simulation fidelity is unaffected, or vice versa -- indicating retrospective and
    prospective access draw on separable stores after all, which would reopen Q-060 toward
    a distinct first-class autobiographical/prospective type rather than a tag on the
    episodic token.

    This is the same joint-degradation falsifier already specified in full, with its V4
    gating and design rationale, at Q-060's own what_would_answer -- refer to that text
    rather than re-deriving; this entry exists so ARC-085 itself carries a self-contained
    falsifier rather than only being reachable via its dependent question.

    [2026-08-08 thought-digestion]: binding-layer field coverage checked against ree_core --
    only self_state/affect/residue have loose existing analogues (as general mechanisms, not
    as tagged fields on a unified event token); committed_vs_imagined has only an informal
    precursor (hypothesis_tag); perspective, source_status, and the identity index itself
    are unbuilt.
- id: MECH-365
  title: "Provenance-bearing event token: every autobiographical event token carries an explicit provenance/commitment-status structure -- event_token = {time, place, self_state, other_agents, perspective, affect, residue, source_status, committed_vs_imagined} -- and a ONE-WAY gate ensures imagined / simulated future tokens can be used for planning without ever acquiring committed-history status. The scarce resource in mental time travel is not capacity but provenance: any path that lets a simulated event accrue committed weight is a confabulation bug."
  claim_type: mechanism_hypothesis
  subject: memory.provenance_bearing_event_token
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - ARC-085
    - MECH-094
    - MECH-037
    - INV-011
  location: docs/architecture/autobiographical_temporality_and_future_simulation.md#mech-365
  notes: "Assembles into one concrete token schema the provenance fields that REE already enforces in pieces -- MECH-094 (simulation-mode vs real-experience distinction; failure = confabulation), MECH-037 (Papez-like provenance gating / reality filtering), INV-011 (imagination must be possible without belief update). This claim does NOT re-assert that safety property (already owned by those three); it specifies the DATA STRUCTURE that carries it on the ARC-085 unified event substrate and makes the committed_vs_imagined label a first-class, load-bearing field rather than an implicit mode bit. The one-way committed-status gate is the autobiographical-memory phrasing of INV-011 + MECH-094; SD-026 (prospective precision-template write channel) is the nearest existing write-side mechanism. V4/V5; off the V3 critical path. substrate_conditional on the ARC-085 event-token store not yet built. DO NOT build in V3 until routed by experiment."
- id: MECH-366
  title: "Switchable episodic perspective tag: autobiographical event tokens carry a viewpoint label (participant / first-person vs observer / third-person) and can be re-experienced or re-simulated from either viewpoint at retrieval time, independent of the viewpoint at encoding. Perspective is a represented, switchable property of the episode, not a fixed encoding artefact."
  claim_type: mechanism_hypothesis
  subject: memory.episodic_perspective_tag
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - ARC-085
    - SD-005
  location: docs/architecture/autobiographical_temporality_and_future_simulation.md#mech-366
  notes: "Empirical anchor: the verified Neurocase case (ARC-085) reports TL fluidly switching participant vs observer perspective on recalled and imagined events. SD-005 (z_self/z_world split) is the nearest existing substrate but represents self-vs-world content, NOT a switchable viewpoint ON an episode -- that perspective-as-a-tag construct is absent from claims.yaml. Relevant to first-vs-third-person framing of both replay (ARC-007) and prospective simulation (ARC-018), and interacts with agency attribution. V4/V5; off the V3 critical path. substrate_conditional on the ARC-085 event-token store. DO NOT build in V3 until routed by experiment."
- id: Q-060
  title: "Should REE represent autobiographical event memory as a DISTINCT first-class type, separate from semantic (abstract facts/relations) and task/procedural (policies/action schemas) memory -- or is the existing episodic->semantic consolidation pathway (MECH-121) sufficient? The case material motivates the distinction (TL separates emotion-tagged 'life memory' from neutral encyclopedic 'black memory'), but REE has no memory-type taxonomy claim."
  claim_type: open_question
  subject: memory.autobiographical_vs_semantic_type_distinction
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - ARC-085
    - MECH-121
  location: docs/architecture/autobiographical_temporality_and_future_simulation.md#q-060
  decision_2026_06_14: "DESIGN-ADJUDICATED (ABM-1, user call): autobiographical-event memory is a TAG / INDEXING CONVENTION over the existing episodic store (ARC-007), NOT a distinct first-class memory type. Sub-question (1) -- 'does a distinct autobiographical layer earn its keep, or is it a tag on episodic content?' -- answered: TAG (a self/affect/provenance-tagged, identity-indexed layer), not a separate type/store. Rationale: the features that distinguish autobiographical from semantic content {self_state, perspective, affect, residue, source_status, committed_vs_imagined} are fields on the episodic event token; the MECH-121 episodic->semantic pathway already models de-tagged content draining toward semantic (autobiographical = episodic content that RETAINS its self/affect tags and resists full semanticisation); every downstream cluster claim is already a field/gate ON the token, not a parallel store; and the tag reading is the parsimonious instantiation of ARC-085's 'one substrate, not two parallel machineries' thesis. The semantic and task/procedural types stay distinct; only autobiographical-vs-episodic resolves to tag-not-type. Recorded into ARC-085.q060_taxonomy_decision (ARC-085 = binding/indexing layer over ARC-007). Remains an open_question (history-informative) because the empirical falsifier below could reopen it; kept substrate_conditional so it does not drive a V3 experiment."
  what_would_answer: "The ARC-085 joint-degradation falsifier (a V4 experiment, once the unified store exists): corrupt the shared event-token store and measure past-recall vs future-simulation fidelity. If both degrade TOGETHER, the single tagged/indexed store holds (confirms the tag reading). If they degrade INDEPENDENTLY, autobiographical and episodic-prospective draw on separable stores -- reopening Q-060 toward a distinct first-class type. Until that V4 experiment is possible the answer is the design adjudication above."
  notes: "From the hyperthymesia intake (raw thought's REE-specific hypothesis: a four-way split of semantic / task / autobiographical-event / prospective-autobiographical-simulation memory). REE currently owns only the episodic<->semantic axis via MECH-121 (NREM SWR episodic->semantic transfer) and the orthogonal stored-vs-active distinction (INV-037/INV-038); there is no claim asserting autobiographical event memory as a distinct type. Sub-questions: (1) does a distinct autobiographical layer earn its keep, or is it a tag on episodic content? -- ANSWERED 2026-06-14: tag/indexing convention (see decision_2026_06_14). (2) how does residue attach to autobiographical memories without contaminating imagined futures (ties to MECH-365)? (3) where is the boundary between replay, counterfactual simulation, and future trajectory planning? epistemic_category set substrate_conditional EXPLICITLY (overriding open_question -> answer_state) so narrow_open_question does NOT fire -- this is a V4-parked question awaiting the ARC-085 substrate, not a V3-tractable one to narrow by experiment. V4/V5; off the V3 critical path. DO NOT queue an experiment against this in V3."
- id: RA-001
  title: "Long-term clinical depression involves a chronicity-dependent reversal of large-scale network coupling: past a ~24-month chronicity threshold, the relationship between symptom severity and Central-Executive-Network (CEN) <-> Default-Mode-Network (DMN, precuneus) functional connectivity reverses sign (non-chronic: stronger CEN-DMN coupling at low severity, weaker at high; chronic: connectivity strengthens as symptoms worsen, co-occurring with entrenched rumination). The depressive brain-state is duration-indexed, not a single static regime."
  claim_type: research_anchor
  subject: clinical.depression_network_coupling_reversal
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    out_of_domain research_anchor: the test domain is a clinical fMRI cohort; no
    REE substrate distinguishes named large-scale networks (CEN/DMN), so the
    anchor is confirmed or collapsed by external replication, never by a V3 run.
    Discriminating empirical signature that CONFIRMS the anchor: an independent
    fMRI cohort, stratified by episode duration at a ~24-month chronicity
    threshold, reproduces a SIGN REVERSAL in the relationship between symptom
    severity (HAM-D) and CEN<->precuneus(DMN) functional connectivity --
    non-chronic patients show connectivity DECREASING with severity, chronic
    patients show connectivity INCREASING with severity (the Zanao et al. 2026 Sci
    Rep n=46 result), establishing that the depressive brain-state is
    duration-indexed rather than a single static regime. COLLAPSES the anchor if a
    larger or pre-registered replication finds the severity-connectivity slope is
    the same sign in chronic and non-chronic cohorts (no chronicity moderation),
    or if the apparent reversal is explained by a confound (medication exposure,
    age, scanner/motion) rather than chronicity, or if the threshold is not near
    24 months and no stable duration boundary exists. PRECISION guardrail:
    'reverses' means a cross-sectional reversal of the connectivity-vs-severity
    slope between cohorts, NOT demonstrated therapeutic reversibility (that is
    Q-061); and this is long-term CLINICAL depression, not synaptic LTD. A
    confirming replication strengthens the REE depression cluster (MECH-088 /
    INV-034 / MECH-124) and motivates MECH-367; it must never be cited as REE
    substrate evidence.
  epistemic_category: out_of_domain
  registered: "2026-06-09"
  location: docs/architecture/depressive_network_regimes.md#ra-001
  source:
    - "Zanao T., Salvan P., Razza L. B., et al. Chronicity moderates the impact of severity on central executive-default mode network functional interactions in depression. Scientific Reports (2026). DOI 10.1038/s41598-026-40364-2. https://www.nature.com/articles/s41598-026-40364-2 (preprint medRxiv 10.64898/2026.01.28.26345027)"
    - https://neurosciencenews.com/brain-network-connectivity-mdd-30745/
    - docs/thoughts/2026-06-06_clinical_depression_network_connectivity_reversal.md
    - evidence/planning/thought_intake_2026-06-06_clinical_depression_network_connectivity_reversal.md
  notes: >
    External empirical anchor for REE's clinical-depression model, NOT a REE substrate claim --
    epistemic_category out_of_domain: the test domain is a clinical fMRI cohort; no REE substrate
    at any level distinguishes named large-scale networks (CEN/DMN). First of the RA- (research
    anchor) prefix; claim_type research_anchor is sanctioned by docs/architecture/v4_spec.md
    ("Out-of-domain claims ... belong as research_anchor / literature_synthesis claim types").
    Source VERIFIED at press-summary level (Neuroscience News, "Long-Term Depression Reverses
    Brain Network Connectivity", https://neurosciencenews.com/brain-network-connectivity-mdd-30745/).
    PRIMARY PAPER VERIFIED 2026-06-09: Zanao T., Salvan P., Razza L. B., et al., "Chronicity moderates
    the impact of severity on central executive-default mode network functional interactions in
    depression", Scientific Reports (2026), DOI 10.1038/s41598-026-40364-2 -- 46 patients, chronicity
    defined as episode > 24 months, HAM-D severity; CEN-precuneus(DMN) connectivity sign-moderated by
    chronicity (non-chronic: connectivity DECREASES with severity; chronic: INCREASES). The 24-month
    threshold and precuneus locus are now paper-verified, not merely press-reported. PRECISION: "reverses"
    = a cross-sectional
    reversal of the connectivity-vs-severity relationship between non-chronic and chronic cohorts,
    NOT a demonstrated therapeutic reversibility (that is the open question Q-061). GUARDRAIL: this
    is long-term CLINICAL depression, NOT synaptic long-term depression (LTD). Grounds the existing
    REE depression cluster (MECH-088 four-plane psychiatric profile incl the psychosis/depression
    distinction; INV-034 depressive attractor / goal-maintenance failure; MECH-124 replay-driven
    option-space contraction; Q-021 anhedonia/flatness; MECH-082/086 locked-in avoidant brain) and
    motivates MECH-367 (the network-regime vector) + Q-061 (reversibility). Does not contradict any
    existing claim; corroborates the cluster. Reaped from the 2026-06-06 clinical-depression intake.
- id: MECH-367
  title: "The depressive failure mode is carried by a multi-axis network-regime VECTOR, not a scalar low-mood/low-reward value: depressive_regime_risk = f(future_trajectory_access, goal_stream_coupling, residue_load/self_weighting, rumination_loop_gain, action_threshold/energy_budget, social_affordance_access, offline_repair_quality). The depressive state is defined by the JOINT configuration of these coupling/access axes and is self-maintaining; scalar mood/reward is an insufficient representation."
  claim_type: mechanism_hypothesis
  subject: failure_modes.depressive_network_regime_vector
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - INV-034
    - MECH-124
    - Q-021
    - MECH-088
    - MECH-082
    - RA-001
  location: docs/architecture/depressive_network_regimes.md#mech-367
  notes: >
    The genuinely-new REE-architectural claim from the clinical-depression intake. REE already
    owns each axis individually -- INV-034 (goal_stream_coupling + future_trajectory_access; the
    depressive attractor = goal-maintenance failure, EXQ-237a), MECH-124 (residue_load +
    rumination_loop_gain + offline_repair_quality; replay-driven option-space contraction), Q-021
    (action_threshold / anhedonia), MECH-082/086 (future-trajectory inaccessibility, locked-in
    avoidant brain) -- but NO existing claim COMPOSES them into one weighted vulnerability vector
    whose joint configuration IS the regime. This claim asserts that composition. Empirical anchor
    RA-001 (duration-dependent CEN-DMN coupling reversal) motivates treating the regime as a
    coupling-structure vector rather than a scalar. epistemic_category substrate_conditional: the
    axes exist piecemeal in V3 but the joint-vector read-out is not built, and there is no named
    large-scale-network coupling substrate; V4/V5, off the V3 / GAP-7 critical path. DO NOT build in
    V3 until routed by experiment. PRECISION GUARDRAIL: keep the depression failure mode distinct
    from psychosis (MECH-088 NA-collapse + DA aberrant-salience profile; tag-misassignment
    MECH-094/115) and confabulation (MECH-094 tag-loss); "rumination" here is the depressive
    recurrent-loop sense (MECH-124), not psychotic content-intrusion. Does not promote/demote any
    existing claim. Reaped from the 2026-06-06 clinical-depression intake.
- id: Q-061
  title: "Is a maladaptive depressive network-regime (MECH-367) REVERSIBLE -- reconfigurable by REE's existing repair machinery (offline integration / MECH-124 replay rebalancing, decommitment from ruminative loops, re-access to social affordances, goal-stream re-coupling) -- or does chronicity lock the regime past a threshold (RA-001) such that the repair routes that work early fail or reverse-sign late?"
  claim_type: open_question
  subject: failure_modes.depressive_regime_reversibility
  polarity: open
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Answered by a duration-graded repair comparison once the MECH-367
    depressive-regime vector substrate exists (V4/V5 -- DO NOT queue a V3
    experiment). Induce a maladaptive depressive network-regime, then apply REE's
    existing repair routes (offline-integration / MECH-124 replay rebalancing,
    decommitment from ruminative loops, social-affordance re-access, goal-stream
    re-coupling) at TWO chronicity points: EARLY (pre-threshold) and LATE (past
    the RA-001 ~24-month-analog chronicity threshold). The discriminating
    observation is whether the SAME repair routes reconfigure the regime in both
    windows. ANSWER 'reversible / not threshold-locked' if late repair recovers
    the regime toward baseline connectivity and goal-coupling with the same routes
    that work early (no sign reversal, recovery magnitude comparable). ANSWER
    'chronicity locks the regime' if the routes that reconfigure the early regime
    FAIL late, or REVERSE sign late (consistent with the RA-001
    connectivity-vs-severity sign flip and the MECH-124 self-amplifying
    consolidation loop), implying late repair needs a different mechanism. HARD
    PRECONDITION: the induced regime must be a genuine self-sustaining attractor
    (persists without ongoing stressor) and the late point must be past the RA-001
    threshold, else 'early' and 'late' are the same regime and the
    duration-dependence question is untestable. ANTI-OVERCLAIM GUARD: a positive
    result does NOT license asserting depression is 'merely a reversible software
    state'.
  epistemic_category: answer_state
  registered: "2026-06-09"
  depends_on:
    - MECH-367
    - MECH-124
    - INV-034
    - RA-001
  location: docs/architecture/depressive_network_regimes.md#q-061
  notes: >
    From the clinical-depression intake. RA-001 shows the connectivity-vs-severity relationship
    REVERSES sign past a ~24-month chronicity threshold, but demonstrates only a cross-sectional
    cohort reversal, NOT therapeutic reversibility -- so whether REE's repair machinery can
    reconfigure a chronic depressive regime is genuinely open. The sharp sub-question is
    duration-dependence: do offline-integration / decommitment / social-re-access repair routes
    that reconfigure an EARLY depressive regime still work LATE, or does the MECH-124 self-
    amplifying consolidation loop + the RA-001 coupling reversal make late repair require a
    different mechanism? answer_state (a question, not an assertion); resolvable in REE only once
    the MECH-367 vector substrate exists (V4/V5). Off the V3 critical path -- DO NOT queue a V3
    experiment against this. Hold the anti-overclaim guard: do not assert depression is "merely a
    reversible software state." Reaped from the 2026-06-06 clinical-depression intake.

# Registered 2026-06-09 -- neuroimmune-modulation / compulsive-loops intake (Neuroscience News
# "Brain Immune Cells Drive Compulsive Behavior"; primary study Nagarajan et al., Molecular
# Psychiatry 2026-04-13, Hoxb8 microglia Ca2+ signaling). Slow non-symbolic substrate-state
# modulation of compulsive-loop persistence. V4/V5 compass, off the V3 critical path. See
# docs/architecture/slow_modulatory_state_and_compulsive_loops.md and
# evidence/planning/thought_intake_2026-06-06_neuroimmune_modulation_compulsive_loops.md.
- id: MECH-369
  title: "Slow substrate-state modulator with authority over compulsive-loop stickiness: a slowly-varying, broadly-projecting modulatory state can bias the decommit-friction / commitment-threshold / action-release landscape so that certain action loops become sticky, urgent, or hard to release INDEPENDENTLY of represented goal value or fast prediction-error -- i.e. a compulsive loop can arise from field-state bias rather than from an endorsed reward, valued goal, or false belief. The modulator changes the release/threshold landscape, not the value estimate."
  claim_type: mechanism_hypothesis
  subject: compulsion.slow_modulatory_loop_stickiness
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - SD-037
    - SD-036
    - MECH-268
    - SD-034
    - ARC-016
    - MECH-342
    - SD-048
  location: docs/architecture/slow_modulatory_state_and_compulsive_loops.md#mech-369
  roadmap_node: "affect_expression_v4:AE-10"
  notes: "Empirical anchor (press-summary-verified, NOT read directly): Nagarajan et al., Molecular Psychiatry 2026-04-13 (Univ. of Louisville) -- Hoxb8 microglia intracellular Ca2+ signaling is a bidirectional optogenetic switch for anxiety/OCD-spectrum grooming. PRECISION GUARDRAIL: the paper's mechanism is a FAST/causal microglial Ca2+ switch, NOT a demonstrated slow tonic process; the 'slow modulation' framing is the REE-side abstraction the saved item prompted, not the paper's claim -- do NOT cite this as evidence for slowness, and do NOT reduce OCD/compulsion to inflammation or imply REE/AI have immune cells (the analogue is slow substrate-state modulation, not biology). DISTINCT FROM: MECH-268 (fast dACC PE-saturation / urgency accumulator -- a within-loop fast signal, not a slow background bias); SD-037 (orexin-analog broadcast gain modulator -- the closest existing slow-modulator slot, but it reweights z_harm and gates SD-012 drive->z_goal seeding, it does NOT carry explicit authority over decommit-friction / loop release); MECH-106 (valence->commitment-threshold -- value-driven, whereas MECH-369's whole point is value-INDEPENDENT stickiness). RELATED existing slow modulators it would sit alongside: SD-036 (GABA decay), MECH-186/187/188 (5-HT goal-pipeline gain). The harm-stream already has a slow-physiological-bias hook in SD-048 (inflammatory-sensitisation + allostatic-load analogs). Most naturally an AMENDMENT extending the slow-modulator layer (SD-037 cluster) with a decommit-friction authority channel, rather than a wholly free-standing substrate. V4/V5; off the V3 critical path. substrate_conditional on a slow-modulator-class distinction (value-independent stickiness authority) not yet built. DO NOT build it or queue an experiment in V3 until routed by an explicit version decision. Reaped from the 2026-06-06 neuroimmune-modulation/compulsive-loops intake. LIT-GROUNDING + SUBSTRATE-HOME DECISION (2026-06-13, AE-9 lit-pull, user-approved): the value-INDEPENDENT slow-modulator CLASS this claim asserts is now biologically anchored by Aston-Jones & Cohen 2005 (Annu Rev Neurosci 28:403-50, doi:10.1146/annurev.neuro.28.061604.135709) LC-NE adaptive-gain theory -- a slow, broadly-projecting modulator whose gain parameter sets task ENGAGEMENT vs DISENGAGEMENT/release decoupled from the value of any specific option (lit entry targeted_review_per_candidate_affect/2026-06-13_mech_369_lc_ne_adaptive_gain_astonjones2005; literature_confidence 0.56). evidence_direction MIXED, not supports, for a POLARITY INVERSION: tonic LC-NE drives DISENGAGEMENT (the opposite pole to compulsive stickiness), so the anchor grounds the modulator CLASS and form (value-independent slow release/engagement authority) but NOT the stickiness polarity -- compulsion reads as a failure to enter the disengagement mode (gain stuck in exploitation), a free parameter the anchor does not directly evidence. DECISION: adopt the LC-NE-style ADAPTIVE-GAIN / engagement-release framing as MECH-369's substrate home -- the SD-037-cluster amendment is a NEW gain channel carrying explicit loop-RELEASE / decommit-friction authority (distinct from SD-037's z_harm-reweight / drive-seeding gain, which has no release authority). This substrate is now OWNED by roadmap node affect_expression_v4:AE-10 (previously a 'no owning node' planning gap surfaced by AE-9); AE-7 (compulsion cluster MECH-370/SD-045/Q-063) depends on AE-10. NO promotion/demotion: raises literature_confidence only, experimental_confidence stays 0, MECH-369 stays candidate / implementation_phase v4 / substrate_conditional; the build still requires an explicit version decision (DO NOT build in V3). Precision guardrail unchanged: LC-NE grounds the slow-modulator class; do NOT conflate with the FAST Nagarajan microglial Ca2+ switch."
- id: MECH-370
  title: "Composed compulsion-risk readout: compulsive vulnerability is best modelled not as a single scalar (reward, habit strength, or belief) but as a composition compulsion_risk = f(loop_reinforcement, threat_salience, residue_persistence, decommit_friction, slow_modulatory_state) over quantities REE already represents separately -- a multi-layer state in which the substrate can make certain loops difficult to release even when any single term looks benign."
  claim_type: mechanism_hypothesis
  subject: compulsion.composite_risk_readout
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - SD-045
    - ARC-071
    - MECH-268
    - IMPL-005
    - MECH-369
    - ARC-016
    - MECH-342
  location: docs/architecture/slow_modulatory_state_and_compulsive_loops.md#mech-370
  notes: "The five terms already exist in REE but are never composed into one diagnosable signal: loop_reinforcement ~ SD-045 (action-chunk runaway-chunking -> OCD ritual, Graybiel 2008) + ARC-071 (policy-composition-via-repeated-grounding slow accumulator); threat_salience ~ harm stream / MECH-268 dACC PE; residue_persistence ~ IMPL-005 (residue persistence as structural memory); decommit_friction ~ ARC-016 (commitment threshold) + MECH-342 (maintenance-time decommit) + SD-034 (closure operator); slow_modulatory_state ~ MECH-369 (the value-independent stickiness modulator) / SD-037 / SD-036. MECH-370 asserts the COMPOSITION is the right level of description for compulsion -- a readout that lets governance ask 'is this loop stuck because it is valued, or because the field is biased?'. Distinct from MECH-369 (one TERM of the function) and from any single failure-mode claim (it spans them). V4/V5; off the V3 critical path. substrate_conditional -- the terms exist but the unified readout has no substrate. DO NOT build it or queue an experiment in V3 until routed by an explicit version decision. Reaped from the 2026-06-06 neuroimmune-modulation/compulsive-loops intake."
- id: Q-063
  title: "How does REE distinguish a high-value, legitimately-repeated goal from a slow-modulator-trapped compulsive loop -- i.e. a loop that persists because the decommit-friction / threshold landscape is biased (MECH-369) rather than because the goal is valued? What diagnostic separates value-driven persistence from field-state-driven stickiness, and can offline integration reduce loop stickiness without erasing legitimate residue (INV-004/INV-006 firewall)?"
  claim_type: open_question
  subject: compulsion.modulator_trapped_vs_valued_goal
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Answered against the MECH-369 slow-modulatory-state substrate (V4/V5), not a
    V3 run. Build the slow-modulator field that biases the decommit-friction /
    threshold landscape, then run a 2x2: {goal value HIGH / LOW} x {modulator bias
    ABSENT / PRESENT}, and read the candidate diagnostics. ANSWER "value-driven
    persistence and modulator-trapped stickiness are separable" if a diagnostic
    dissociates the cells: specifically (a) z_goal salience / expected-value is
    HIGH in the valued-repeat cell but at-or-below baseline in the
    modulator-trapped cell (persistence without value), and (b) the SD-034 closure
    operator FIRES and releases in the valued cell but fails-to-fire (or fires
    without releasing) in the trapped cell -- i.e. release is blocked by
    decommit-friction, not by value. ANSWER "not separable on this substrate" if
    high-value and modulator-trapped loops are indistinguishable on z_goal,
    closure-operator firing, and dwell-time. The offline-integration leg: apply
    MECH-272/273 sleep-cluster reintegration and ANSWER "reduces stickiness
    without erasing legitimate residue" only if loop dwell-time /
    decommit-friction falls toward the valued-baseline WHILE the
    INV-004/INV-006-protected harm residue is bit-preserved (firewall intact);
    ANSWER "erodes the firewall" if stickiness reduction co-occurs with any decay
    of the non-erasable harm residue. HARD PRECONDITION: the MECH-369 slow
    modulator must EXIST and actually bias the threshold landscape with non-zero
    gain, and SD-034 closure must be live -- with no modulator field there is no
    trapped loop to distinguish from a valued one and the diagnostic is vacuous.
    V4/V5; off the V3 critical path (depends_on MECH-369/MECH-370/ARC-016/SD-034).
    DO NOT queue a V3 experiment against this.
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - MECH-369
    - MECH-370
    - ARC-016
    - SD-034
  location: docs/architecture/slow_modulatory_state_and_compulsive_loops.md#q-063
  notes: "The etiological discriminator the neuroimmune intake turns on. Sub-questions: (1) what signature separates a valued repeated goal (high z_goal salience, positive expected value) from a modulator-trapped loop (release blocked by decommit-friction, not by value)? (2) does the SD-034 closure operator firing-vs-failing-to-fire distinguish the two? (3) can offline integration (MECH-272/273 sleep cluster) reduce stickiness WITHOUT eroding the non-erasable harm residue protected by INV-004/INV-006? Set epistemic_category: substrate_conditional explicitly (overriding the open_question -> answer_state inference) so narrow_open_question does NOT fire -- this is a V4-parked question awaiting the MECH-369/MECH-370 substrate, not a V3-tractable one to narrow by experiment. V4/V5; off the V3 critical path. DO NOT queue an experiment against this in V3. Reaped from the 2026-06-06 neuroimmune-modulation/compulsive-loops intake."
- id: MECH-368
  title: "Event-level write-authority gate: an explicit per-event transition from observed/represented to write-eligible over the DURABLE MODEL-UPDATE path (E1/E2 world-model and policy weights, not only episodic memory), conditioned on f(prediction_error, salience, pathway_state, residue_status, goal_relevance, plasticity_eligibility). The gate decides which individual events within an already-open write channel are licensed to durably deform the model; an event may be salient, real-provenance, and in a write-permitted mode yet still fail the gate. Without it, every prediction error in an open channel writes -- the undifferentiated-global-update / catastrophic-interference failure mode."
  claim_type: mechanism_hypothesis
  subject: plasticity.event_level_write_authority_gate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  phase_locked: true  # intrinsic V4 scope: own notes "DO NOT build in V3 until routed by
                      # experiment (Q-062)". ARC-120's reference is evidentiary (one of
                      # several existing authority-gating instances it surveys), not a
                      # prerequisite. Set 2026-08-10 per chip
                      # chip-20260810-phase-consistency-reconciliation.
  version_relevance: v4_v5
  depends_on:
    - MECH-261
    - MECH-094
    - INV-074
    - SD-032a
    - INV-034
  location: docs/architecture/plasticity_write_authority_gating.md#mech-368
  notes: "Reaped from the 2026-06-06 learning-onset intake (source: Schreiner/Brudner/Li/Pearson/Mooney, 'A synaptic locus of song learning', Nature 2026, DOI 10.1038/s41586-026-10510-x -- a cortico-basal-ganglia synapse class as the first-expression/maintenance locus of zebra-finch song learning; compass-level, not citable mechanism evidence). The genuinely-new piece is the ADMISSION side of plasticity: REE built the closure/protection side (INV-074 crystallization, MECH-334 EWC write-protect of already-written weights) but has no explicit per-event gate on what earns durable write in the first place. Distinct from and DOWNSTREAM of MECH-261, which gates the write CHANNEL at the operating-mode grain (which substrates can write per mode) but not per-event passage within an open channel; distinct from MECH-094 (provenance/source gate) and MECH-283 (recognition-for-recall, the retrieval-side analogue). The episodic-memory write path is already substantially covered (salience-tag -> replay candidacy -> MECH-285 consolidation-priority is an implicit write-eligibility pipeline); the under-covered path is the ONLINE world-model/policy weight-update one. goal_relevance input depends on a competitive z_goal (GAP-4 goal-pipeline), the main reason this is V4-scoped; a goal-free REDUCED form (gated on prediction_error + salience + provenance + plasticity_eligibility) could be pulled earlier if a specific failure motivates it -- see home doc. substrate_conditional: promote/demote suppressed, off V3/GAP-7 critical path. DO NOT build in V3 until routed by experiment (Q-062). Cross-references the 2026-06-01 plasticity-window-neuromodulators note (the WINDOW/state-grain opening side) -- sibling, not duplicate: a window-level plasticity gain can be open while a given event still fails this event-level gate."
- id: Q-062
  title: "Is an EXPLICIT event-level write-eligible state (MECH-368) actually needed, or is the existing combination -- MECH-261 mode-conditioned channel gating + MECH-094 provenance gating + MECH-285 consolidation-priority by V_s residual -- already sufficient to prevent undifferentiated global model updates? This is the falsifier for MECH-368: if a substrate that gates only the channel (mode) and prioritises only at consolidation still avoids catastrophic interference on the online world-model/policy path, MECH-368 earns no keep."
  claim_type: open_question
  subject: plasticity.write_eligible_state_necessity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    This is the explicit falsifier for MECH-368. Once a durable model-update
    substrate with an OPEN online write channel exists (E1/E2 world-model + policy
    weights writable during waking, not just episodic memory), run a 2-arm
    ablation on a task that interleaves real distractor events with genuinely
    model-relevant ones: ARM_baseline = channel-gating (MECH-261 mode) +
    provenance gating (MECH-094) + offline consolidation-priority (MECH-285) ONLY;
    ARM_368 = the same PLUS the per-event write-eligibility gate
    f(prediction_error, salience, pathway_state, residue_status, goal_relevance,
    plasticity_eligibility). Measure catastrophic interference on the online
    world-model/policy path (retention of prior competence after a burst of
    high-PE distractor events). ANSWER 'MECH-368 earns its keep' if ARM_368
    strictly reduces interference / preserves prior competence above ARM_baseline;
    ANSWER 'no -- the existing combination suffices' (MECH-368 falsified) if
    ARM_baseline already avoids catastrophic interference with no measurable gap.
    HARD PRECONDITION: the online write channel must be genuinely OPEN and capable
    of producing interference -- if mode-gating + provenance already keep the
    channel closed in this task so no undifferentiated update can occur, both arms
    score identically by construction and the test is vacuous; you need a task
    where the baseline DOES admit writes that could corrupt the model. V4/V5; off
    the V3 critical path. Wait for the model-update/online-write substrate; do NOT
    queue a V3 experiment.
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - MECH-368
    - MECH-261
    - MECH-285
  location: docs/architecture/plasticity_write_authority_gating.md#q-062
  notes: "Companion falsifier to MECH-368, reaped from the 2026-06-06 learning-onset intake. The decisive empirical question once a model-update substrate exists with an open online write channel: does per-event admission gating add anything over channel-gating (MECH-261) + provenance (MECH-094) + offline consolidation-priority (MECH-285)? epistemic_category set substrate_conditional EXPLICITLY (overriding open_question -> answer_state) so narrow_open_question does NOT fire -- this is a V4-parked question awaiting the model-update/online-write substrate, not a V3-tractable one to narrow by experiment. Sub-questions: (1) is the gate one mechanism or per-pathway (world-model vs policy vs residue)? (2) is 'write-eligible' a held intermediate STATE with its own entry/exit, or just the salience tag re-read? (3) does goal_relevance belong in the gate, or is that double-counting z_goal salience? V4/V5; off the V3 critical path. DO NOT queue a V3 experiment against this."

# Registered 2026-06-09 -- psychiatric-genetic-overlap / latent-failure-axes intake (saved item
# "Mapping the genetic landscape across 14 psychiatric disorders"; primary source VERIFIED =
# Grotzinger A. D. et al., "Genetic overlap across 14 psychiatric disorders", Nature Genetics 2025,
# DOI 10.1038/s41588-025-02494-7). Reaped per user direction (registration IS the point of intake).
# Cluster: RA-002 (genetic anchor) + ARC-086 (two-axis failure-mode index) + Q-064 (syndrome-as-
# vector) + MECH-371 (p-factor analogue). NO promotion/demotion of any existing claim. Distinct
# failure modes kept mechanistically separate (psychosis != confabulation != OCD != depression).
- id: RA-002
  title: "Clinically distinct psychiatric diagnoses share a small set of latent genomic factors that cut across diagnostic boundaries: large-scale genomic structural-equation modelling across 14 childhood- and adult-onset psychiatric disorders (>1 million individuals) finds that the majority of case-vs-control genetic variance is captured by FIVE shared genomic factors (Compulsive; Schizophrenia-bipolar / psychotic-mood; Neurodevelopmental; Internalizing; Substance-use) spanning 238 variants, with factor-specific biology (e.g. oligodendrocyte genes enriched in the Internalizing factor) acting early in brain development. Psychiatric categories are surface expressions over overlapping latent dimensions, not genetically discrete kinds."
  claim_type: research_anchor
  subject: clinical.psychiatric_genomic_factor_overlap
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    out_of_domain research_anchor: the test domain is a human-genetics cohort; no
    REE substrate distinguishes genomic factors, so the anchor is confirmed or
    collapsed by external genomics replication, never by a V3 run, and it must
    never be cited as REE mechanism evidence. Discriminating empirical signature
    that CONFIRMS the anchor: an independent large-scale genomic
    structural-equation modelling effort across childhood- and adult-onset
    psychiatric disorders reproduces a SMALL number (~five) of shared
    cross-diagnostic latent genomic factors (Compulsive;
    Schizophrenia-bipolar/psychotic-mood; Neurodevelopmental; Internalizing;
    Substance-use) that capture the majority of case-vs-control genetic variance,
    with factor-specific developmental biology (e.g. oligodendrocyte-gene
    enrichment in the Internalizing factor) -- i.e. the genomic structure is
    low-dimensional and cuts ACROSS diagnostic boundaries (the Grotzinger et al.
    2025 Nat Genet result, >1M individuals, 238 variants). COLLAPSES the anchor if
    replication instead recovers factors that align ONE-TO-ONE with DSM categories
    (no cross-diagnostic sharing), or if the dimensionality is not low (no
    parsimonious small factor set survives out-of-sample), or if the factor
    structure is unstable across ancestries/cohorts. GUARDRAIL (anti-determinism):
    genetic overlap does NOT make diagnoses meaningless and individual
    presentations cannot be read off polygenic factors -- the load-bearing
    extraction is the SURFACE-CLUSTER principle (categories are emergent clusters
    over overlapping latent axes), which REE already holds (IMPL-005, INV-061). A
    confirming replication motivates the architectural axis layer (ARC-086), the
    syndrome-as-vector question (Q-064), and the p-factor analogue (MECH-371); the
    factor labels and 238-variant count are from the abstract plus press summaries
    and should be cross-checked against the full paper.
  epistemic_category: out_of_domain
  registered: "2026-06-09"
  location: docs/architecture/psychiatric_failure_axes.md#ra-002
  source:
    - "Grotzinger A. D., et al. Genetic overlap across 14 psychiatric disorders. Nature Genetics (2025). DOI 10.1038/s41588-025-02494-7. https://www.nature.com/articles/s41588-025-02494-7"
    - https://news.harvard.edu/gazette/story/2025/12/new-research-finds-5-genetic-signatures-shared-by-14-psychiatric-disorders/
    - https://www.massgeneralbrigham.org/en/about/newsroom/press-releases/similar-genetic-factors-underlie-multiple-psychiatric-disorders
    - docs/thoughts/2026-06-06_psychiatric_genetic_overlap_latent_failure_axes.md
    - evidence/planning/thought_intake_2026-06-06_psychiatric_genetic_overlap_latent_failure_axes.md
  notes: >
    External empirical anchor, NOT a REE substrate claim -- epistemic_category out_of_domain: the
    test domain is a human-genetics cohort; no REE substrate at any level distinguishes genomic
    factors, and this must NEVER be cited as REE mechanism evidence (claim_type research_anchor,
    second of the RA- prefix after RA-001; sanctioned by docs/architecture/v4_spec.md for
    out-of-domain anchors). Source VERIFIED 2026-06-09 (raw intake had it source-check-partial; the
    saved share.google link was never opened): the primary paper is Grotzinger et al., Nature
    GENETICS 2025 (the saved item + popular coverage say "Nature"), DOI 10.1038/s41588-025-02494-7,
    confirmed via the journal + Harvard Gazette + Mass General Brigham + Live Science + Washington
    Post coverage; the paper itself was not read in full, so the five factor labels and 238-variant
    count are from the abstract + four press summaries. GUARDRAIL (anti-determinism, per the intake's
    section-6/9): genetic overlap is NOT proof diagnoses are meaningless, and individual presentations
    cannot be read from polygenic factors -- the useful extraction is the SURFACE-CLUSTER principle
    (categories are emergent clusters over overlapping latent axes), which REE already holds
    (IMPL-005, the FAILURE-2026-02-12 coordinate-system doc, INV-061). This anchor MOTIVATES the
    architectural axis layer (ARC-086), the syndrome-as-vector question (Q-064), and the p-factor
    analogue (MECH-371); it does not contradict any existing claim. Reaped from the 2026-06-06
    psychiatric-genetic-overlap intake.

- id: RA-003
  title: "Convergent cognitive motifs in language models: sufficiently capable models trained to predict human language may independently develop computational motifs functionally analogous to specific REE mechanisms (orienting, replay, self-representation, other-agent modelling, prediction-error management, arbitration, epistemic uncertainty, long-term goals), because language statistically encodes traces of the cognitive machinery that generated it. Predicts functional convergence, not architectural identity."
  claim_type: research_anchor
  subject: comparative_cognition.llm_convergent_motifs
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-05'
    needs_review: false
  epistemic_category: out_of_domain
  implementation_phase: v4
  version_relevance: v4_v5
  registered_utc: '2026-08-05'
  depends_on:
    - MECH-191   # REE's own cross-architecture signal legibility -- sibling comparative-cognition claim, opposite direction
  location: docs/thoughts/2026-08-05_convergent_cognitive_motifs_in_language_models.md
  source:
    - docs/thoughts/2026-08-05_convergent_cognitive_motifs_in_language_models.md
  what_would_answer: |
    out_of_domain research_anchor: the test domain is mechanistic interpretability
    of trained external language models, not REE's own V3 substrate -- no REE
    experiment can confirm or collapse this anchor. CONFIRMING signature: an
    independent mechanistic-interpretability study identifies internal
    computational circuits in a language model that are FUNCTIONALLY analogous
    (same input-output role, not shared architecture) to a named REE mechanism
    (e.g. an uncertainty-driven exploration bonus resembling MECH-314b, a
    self-referential state tracker, an arbitration-like competition between
    candidate continuations), established via causal ablation/activation-patching
    rather than behavioural similarity alone. COLLAPSING signature:
    interpretability work finds the analogous behaviour is produced by
    mechanisms with no principled functional correspondence to the REE
    mechanism (e.g. pure memorised pattern-completion with no separable
    "uncertainty" or "arbitration" subcircuit), or convergence claims fail to
    replicate across model families/scales. Never resolved by a REE experiment;
    resolved only by tracking external interpretability literature (a lit-pull
    target, not a queued experiment).
  notes: >
    Reaped from the 2026-08-05 "Convergent Cognitive Motifs in Language Models"
    thought intake. Distinct from EXT-001..009 (claim_type external_failure_mode:
    named LLM failure modes REE's architecture is claimed to avoid) -- this is
    the opposite framing, asking whether LLMs independently RECAPITULATE
    REE-like computational motifs, used as a comparative-cognition vocabulary
    rather than a design critique. Also distinct from MECH-191 (REE's own
    stereotyped signals being legible to OTHER observers) -- RA-003 is about
    LLMs' internal computation converging on REE-shaped motifs, not about REE's
    outputs being read by others. Action item per the intake: track
    mechanistic-interpretability literature (induction heads, in-context
    learning circuits, "world model" probing studies) as it bears on named REE
    mechanisms; do not attempt a REE-side experiment against this anchor.

- id: ARC-086
  title: "REE's failure-mode taxonomy is indexed on TWO axes simultaneously: (1) named failure mode (the existing mechanistically-distinct entries -- psychosis/hypothesis-tag loss, confabulation, derealization, OCD, depressive attractor, catatonia subtypes, etc.) AND (2) a finite, named set of cross-cutting LATENT VULNERABILITY AXES that span the named entries (precision/confidence allocation; commitment-threshold/action-release authority; provenance/source tagging; agency attribution; residue/consequence persistence; goal/salience/incentive coupling; self-other coupling; offline integration/replay; social-contagion/cross-agent coupling; global instability). Named clinical syndromes are emergent clusters -- weighted combinations -- over these axes, not primitive natural kinds; comorbidity is overlapping axis activation and pleiotropy is one mechanism loading multiple axes. The axis layer is an INDEXING layer, never a merging layer: it does not collapse mechanistically-distinct failure modes into one another."
  claim_type: architectural_commitment
  subject: failure_modes.transdiagnostic_vulnerability_axis_index
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - IMPL-005
    - INV-061
    - MECH-343
    - RA-002
  pharmacological_predictions:
    - PHARM-015  # 5-HT2A-cortical narrative/precision axis (pimavanserin)
    - PHARM-016  # 5-HT1A-hippocampal terrain/aversive-basin axis (buspirone)
    - PHARM-017  # M1 plasticity x M4 dopamine-selection axes (xanomeline, non-D2)
    - PHARM-018  # kappa/mu commitment-entropy axis (buprenorphine)
  location: docs/architecture/psychiatric_failure_axes.md#arc-086
  what_would_answer: >
    LEG 1 (architectural leg, substrate_conditional -- gated on the V4/V5 syndrome-as-weighted-vector
    substrate; DO NOT queue a V3 experiment): answerable only once the ARC-086 axis vocabulary is
    instantiated as a substrate that can score a syndrome as a weighted vector over the 10 named axes
    (the substrate Q-064 asks HOW to build; ARC-086 is the prior claim that such a finite axis set CAN
    validly index the named failure modes at all). HARD PRECONDITION, shared with Q-064: the axes must
    first be shown empirically separable (non-degenerate, discriminable loadings) -- if not, no syndrome
    placement in axis space is meaningful and this leg is untestable, not merely unresolved.

    CONFIRMING: build weighted-axis-vector encodings for a panel of named syndromes spanning the
    existing mechanistically-distinct entries (psychosis, confabulation, derealization, OCD, depressive
    attractor, catatonia subtypes, ...) plus known comorbid pairs and at least one pleiotropic
    mechanism. ARC-086 is CONFIRMED if every syndrome in the panel is expressible as a weighted
    combination over the SAME finite 10-axis set -- comorbidity recovered as overlapping axis
    activation, pleiotropy as one mechanism loading multiple axes -- WITHOUT requiring a new axis per
    syndrome or per comorbid pair, AND without the resulting vectors collapsing mechanistically-distinct
    entries into one another (the axis layer stays an INDEXING layer, never a MERGING layer -- e.g.
    psychosis and confabulation must remain separably located even though both load the
    provenance/source-tagging axis).

    FALSIFYING: a named syndrome in the panel requires a mechanism that cannot be expressed as ANY
    weighted combination of the 10 named axes, forcing axis proliferation without a natural stopping
    point -- the axis set fails to be FINITE and CLOSED, the load-bearing structural commitment of
    ARC-086. Also falsifying, as a distinct failure mode of the same claim: the vector encoding achieves
    registry economy only by merging mechanistically-distinct named failure modes into indistinguishable
    axis-loadings (violating the "indexing layer, never a merging layer" guardrail) -- this falsifies
    ARC-086 even if Q-064's registry-economy test independently reads as a PASS, because the two claims
    ask different questions (ARC-086: does the two-axis structure hold at all; Q-064: is the vector
    representation preferable to the current locus mapping).

    LEG 2 (literature-checkable leg, out_of_domain, checkable NOW without V4/V5): three of ARC-086's ten
    axes carry a biology-first neuromodulator-receptor grounding (precision/terrain <- serotonin
    5-HT2A-cortical vs 5-HT1A-hippocampal; goal/salience-coupling + commitment-release <- M4-striatal
    dopamine-selection damping; commitment-threshold/action-release <- kappa/mu opioid
    commitment-entropy), predictions PHARM-015..018. This is Q-075's own question, kept here as a
    SECOND, INDEPENDENT leg on ARC-086's confidence rather than folded into Leg 1, because it bears only
    on the BIOLOGICAL PLAUSIBILITY of 3 of the 10 axes as distinct receptor-grounded systems -- neither
    necessary nor sufficient to confirm or falsify the architectural claim itself. A receptor-level
    collapse of e.g. the commitment-threshold and precision/terrain axes into one affective dimension
    would weaken the biological grounding note without automatically forcing axis proliferation or
    merging at the computational level in Leg 1, and conversely a clean double-dissociation would not by
    itself establish Leg 1's registry economy.

    CURRENT STATE OF LEG 2 (2026-07-01 lit-pull, lit_conf=0.708 on Q-075, re-confirmed unmoved as of
    2026-08-07): MIXED, not resolved. All three receptor limbs are individually plausible
    (Carhart-Harris & Nutt 2017 supports the 5-HT2A/5-HT1A precision-terrain split; Bruchas/Land/Chavkin
    2009 supports KOR-dysphoria as distinct from mu-reward; Maggio et al. 2014 is mixed on the
    dopamine-selection limb), but the clean non-crossing double-dissociation Q-075 needs is not yet what
    the literature hands over: the KOR/commitment-threshold limb carries an explicit CRF-KOR coupling
    entanglement warning (threatens collapse toward a general threat/stress axis), and the
    dopamine-selection limb carries a D2-D3 heteromer transactivation warning. Track via future
    /lit-pull targeting Q-075's own falsification_conditions (PHARM-015..018); do not re-run this leg's
    literature review from ARC-086 -- it lives on Q-075.

    LEG 1 substrate check (2026-08-07): the 2026-07-02 IGW auto-proposal pass independently re-confirmed
    Q-064 blocked from V3 queueing on this exact ground (no registered vector substrate exists), so the
    gate is not just unchanged but actively re-affirmed since ARC-086's as_of date.
  notes: >
    The genuinely-new ORGANIZING contribution of the psychiatric-genetic-overlap intake. REE already
    holds the surface-cluster STANCE -- IMPL-005 (computational-pathology failure-mode taxonomy), the
    FAILURE-2026-02-12 coordinate-system doc ("orthogonal taxonomy organised by dynamical failure
    geometry rather than historical narrative"), INV-061 ("different failure signatures of the same
    undertrained mechanism"), and MECH-343/Q-056 (one loop, many syndrome-analogue failure modes) --
    but there is NO registered, finite, named CROSS-CUTTING vulnerability-axis vocabulary as a second
    formal index over the named failure modes. The FAILURE doc indexes by ARC component (ARC-001/003/
    005/013/018), not by a transdiagnostic axis set. ARC-086 registers that axis vocabulary + the
    two-axis simultaneous indexing as an architectural organizing commitment. epistemic_category set
    substrate_conditional EXPLICITLY (overriding the architectural_commitment -> substrate_coherence
    inference; same pattern as ARC-085): this is a CANDIDATE organizing principle, and the
    syndrome-as-weighted-vector READ-OUT it implies (Q-064) has no substrate in V3 -- the documentation
    indexing can begin now, but the vector substrate is V4/V5. Anchored by RA-002 (genetic-overlap
    evidence that diagnoses share latent structure). PRECISION GUARDRAIL (per the intake + memory
    feedback_psychosis_confabulation_distinction): two failure modes sharing an axis does NOT make them
    the same failure -- psychosis (MECH-094/MECH-201), confabulation (MECH-094 recall/completion sense),
    derealization (MECH-200), OCD (SD-034/045/046), depressive attractor (INV-053/054), and catatonia
    subtypes (SD-036/MECH-279 vs MECH-343) stay mechanistically distinct. Does not promote/demote any
    existing claim. Reaped from the 2026-06-06 psychiatric-genetic-overlap intake.
    NEUROMODULATORY-RECEPTOR GROUNDING (reaped 2026-06-12 from the receptor-subtype intervention layer
    lit-pull; evidence/literature/targeted_review_receptor_subtype_layer/, all out_of_domain --
    clinical-pharmacology-adjudicated, promotes nothing on the V3 axis): three of the vulnerability
    axes now carry a biology-first receptor-resolved grounding. (i) precision/terrain axis <-
    serotonin-receptor subtypes: 5-HT2A-cortical = narrative exclusivity/top-level precision vs
    5-HT1A-hippocampal = aversive-basin/terrain depth [MECH-006/MECH-085; Ren 2019, Weber & Andrade
    2010, Akimova 2009]; (ii) goal/salience-coupling + commitment-release axes <- M4-striatal
    dopamine-selection damping, the non-D2 antipsychotic route [MECH-086/MECH-087; Foster 2016, Kaul
    2024 EMERGENT-3]; (iii) commitment-threshold/action-release axis <- the kappa/mu commitment-entropy
    overlay (MECH-048; kappa = destabiliser/aversive-state-lock, mu = stabiliser) [Land 2008, Krystal
    2020, Bari 2025 -- KOR antagonism restores OPTIMAL PERSEVERATION]. Whether these receptor groundings
    are mutually DISSOCIABLE (vs collapsing to one affective dimension) is the new open question Q-075.
    Receptor-resolved predictions: PHARM-015..018 (see pharmacological_predictions field).
- id: Q-064
  title: "Should REE represent a clinical syndrome as a weighted VECTOR over the ARC-086 latent vulnerability axes (with comorbidity = axis-overlap and pleiotropy = one mechanism loading multiple axes), rather than as a diagnosis-specific locus mapping -- and if so, how is comorbidity represented without exploding the claim registry, and how is axis-predisposition (which axes are fragile) kept distinct from axis-expression (gated by REE's developmental / plasticity machinery)?"
  claim_type: open_question
  subject: failure_modes.syndrome_as_axis_vector_representation
  polarity: open
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Answered by a representational comparison once the ARC-086
    latent-vulnerability-axis substrate exists (V4/V5 -- DO NOT queue a V3
    experiment). Build two encodings of a panel of clinical syndromes (including
    known comorbid pairs and a pleiotropic mechanism loading multiple axes): (A)
    the current per-syndrome locus mapping; (B) a weighted VECTOR over ARC-086
    axes (comorbidity = axis-overlap, pleiotropy = one mechanism loading multiple
    axes). The discriminating tests: (a) REGISTRY ECONOMY -- count claim-registry
    entries needed to represent N syndromes plus their comorbid combinations;
    ANSWER 'vector representation preferred' if (B) represents comorbidities as
    overlapping axis activation with NO combinatorial blow-up (entry count grows
    ~linearly in axes, not in syndrome-pairs) while (A) needs a new locus per
    combination; ANSWER 'locus mapping adequate' if (B) does not reduce entry
    count or loses syndrome-specific discriminability. (b)
    PREDISPOSITION/EXPRESSION FACTORING -- the vector must keep 'which axes are
    fragile' (axis-predisposition) separable from 'which failure mode is currently
    expressed' (gated by developmental timing / plasticity / environment); ANSWER
    'vector preferred' only if the same predisposition vector can yield different
    expressed syndromes under different developmental/plasticity gating
    (genetic-risk != inevitability), and 'inadequate' if predisposition and
    expression collapse into one quantity. HARD PRECONDITION: the ARC-086 axes
    must be empirically separable (non-degenerate, discriminable loadings) before
    any syndrome can be located in axis space; degenerate axes make the vector
    representation vacuous.
  epistemic_category: answer_state
  registered: "2026-06-09"
  depends_on:
    - ARC-086
    - INV-061
    - MECH-343
  location: docs/architecture/psychiatric_failure_axes.md#q-064
  notes: >
    The representation question from the psychiatric-genetic-overlap intake. Given ARC-086's axis
    vocabulary, is a syndrome best modelled as a point/region in axis space (a weighted axis-failure
    vector) rather than the current per-syndrome locus mappings in psychiatric_failure_modes.md?
    answer_state (a question, not an assertion). The sharp sub-questions: (a) registry economy --
    comorbidity as overlapping axis activation should NOT require a combinatorial blow-up of claim
    entries; (b) the predisposition/expression factoring -- genetic-risk != inevitability maps to
    axis-predisposition + environment + developmental timing + plasticity, so the representation must
    separate "which axes are fragile" from "which failure mode is currently expressed". Resolvable in
    REE only once the ARC-086 axis-vector substrate exists (V4/V5); off the V3 critical path -- DO NOT
    queue a V3 experiment against this. Reaped from the 2026-06-06 psychiatric-genetic-overlap intake.
- id: Q-075
  title: "Are ARC-086's latent vulnerability axes grounded in DISSOCIABLE neuromodulator-receptor systems -- does the precision/terrain axis map to serotonin-receptor subtypes (5-HT2A-cortical narrative vs 5-HT1A-hippocampal terrain), the goal/salience-coupling + commitment-release axes to dopamine-selection (M4-muscarinic / D2-vs-D3), and the commitment-threshold axis to the kappa/mu opioid commitment-entropy overlay -- such that receptor-SELECTIVE interventions produce DOUBLE-DISSOCIATED effects on their predicted axis, rather than the axes collapsing into a single affective dimension?"
  claim_type: open_question
  subject: failure_modes.neuromodulatory_receptor_grounding_of_vulnerability_axes
  polarity: open
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: out_of_domain
  registered: "2026-06-12"
  version_relevance: v4_v5
  what_would_answer: >
    A receptor-selective DOUBLE-DISSOCIATION in clinical / pharmacological data: an agent selective for
    one axis's receptor (5-HT1A partial agonism for the terrain axis, M4 agonism for the
    dopamine-selection axis, kappa antagonism for the commitment-entropy axis) improves its PREDICTED
    axis-specific endpoint WITHOUT equivalently moving the partner axes' endpoints, and the converse for
    the partner agents. PHARM-015..018's falsification_conditions are the concrete instances -- e.g. if
    kappa antagonism produces the SAME response profile as an SSRI on anhedonia-specific endpoints, the
    commitment-entropy axis collapses into the serotonin-terrain axis and Q-075 is answered NO for that
    pair. A clean cross-receptor double-dissociation answers YES; a uniform / non-dissociable response
    across receptor classes answers NO (the axes are not grounded as distinct receptor systems).
  depends_on:
    - ARC-086
    - MECH-048
    - MECH-085
    - MECH-086
    - MECH-087
  pharmacological_predictions:
    - PHARM-015
    - PHARM-016
    - PHARM-017
    - PHARM-018
  location: docs/architecture/psychiatric_failure_axes.md#q-075
  notes: >
    Reaped 2026-06-12 from the receptor-subtype intervention layer lit-pull (strands 3A/3B/3C;
    evidence/literature/targeted_review_receptor_subtype_layer/). ARC-086 registers the latent
    vulnerability-axis VOCABULARY; Q-064 asks how to REPRESENT syndromes as weighted vectors over those
    axes; Q-075 is the distinct, logically-prior empirical question of whether the axes are grounded in
    dissociable neuromodulator-RECEPTOR systems at all. epistemic_category out_of_domain EXPLICITLY: the
    test domain is clinical pharmacology (receptor-selective RCTs / naturalistic cohorts), NOT the V3
    substrate -- which has no 5-HT / muscarinic / opioid receptor subtypes (the receptor rung is
    out_of_domain for V3 per docs/architecture/receptor_subtype_intervention_layer.md Section 4). DO NOT
    queue a V3 experiment against this; the V3-tractable sibling is the MECH-087 cross-plane non-rescue
    test (a PLANE-level, not receptor-level, dissociation). Lit grounding raised lit_conf on
    MECH-006/048/085/086/087/088 + ARC-086, exp_conf unchanged 0 (promotes nothing). Does not
    promote/demote any existing claim. Receptor-resolved predictions: PHARM-015..018.
- id: MECH-371
  title: "A global-instability / broad control-plane vulnerability axis (a p-factor / general-psychopathology-factor analogue) loads across ALL failure modes: a single broad dimension of control-plane regulatory fragility -- instability in precision/gain/threshold setpoint regulation (ARC-005) rather than any one specific comparator or stream -- raises joint susceptibility to multiple failure modes at once, distinct from and orthogonal to any single-axis failure. This is the axis with no existing REE home; it predicts that a subset of agents/patients carry elevated risk across the whole failure-mode space rather than along one axis."
  claim_type: mechanism_hypothesis
  subject: failure_modes.global_instability_control_plane_vulnerability_axis
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - ARC-086
    - ARC-005
    - RA-002
  location: docs/architecture/psychiatric_failure_axes.md#mech-371
  notes: >
    The one genuinely-new vulnerability AXIS from the psychiatric-genetic-overlap intake with no
    existing claim home (the others map onto owned substrate: precision -> ARC-005/MECH-251; commitment
    -> E3/MECH-090/342/SD-034; provenance -> MECH-094/037/INV-011; residue -> ARC-013/SD-010/011; goal/
    salience -> INV-034/MECH-112/116/SD-012; self-other -> ARC-010; offline -> INV-045/049/MECH-121/273/
    285). The p-factor analogue is the broad control-plane regulatory-stability dimension itself: REE has
    the control plane (ARC-005 + the MECH-251 precision family) but no claim that a single broad
    instability of setpoint regulation loads transdiagnostically. RA-002's finding that a large share of
    cross-disorder genetic variance is shared (and the general-psychopathology / Caspi-Moffitt p-factor
    literature) motivates the dimension. epistemic_category substrate_conditional / V4: there is no
    substrate that exposes a measurable global control-plane-stability scalar in V3; off the V3 / GAP-7
    critical path; DO NOT build in V3 until routed by an explicit version decision. PRECISION GUARDRAIL:
    a global-instability axis does NOT erase the distinctness of the specific failure modes -- it is an
    additional orthogonal loading, not a claim that all psychopathology is one thing. A brief
    general-psychopathology-factor lit-pull (Caspi & Moffitt) would sharpen this before any promotion.
    Does not promote/demote any existing claim. Reaped from the 2026-06-06 psychiatric-genetic-overlap
    intake.
- id: MECH-373
  title: "A lightweight LanguageAffectAdaptor parses affect/emotion from user or agent LANGUAGE into the shared world model: a language-interface component that, when literal semantic content is insufficient to recover affect, converts text into PROBABILISTIC affect/emotion hypotheses (treated as uncertain latent-state estimates, NOT categorical emotion truths) and feeds those estimates into REE's wider agent-state and context modelling. It is an input/perception adaptor on the language channel -- distinct from REE's INTERNAL affective representation (Q-007 z_beta dimensions) -- supplying other-agent affect priors to the social/empathy modelling layer."
  claim_type: mechanism_hypothesis
  subject: language.affect_adaptor_uncertain_hypothesis
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v6
  version_relevance: v4_v5
  governance_2026_06_10: >
    Reassigned implementation_phase v5 -> v6 during the V6 (linguistic-tier) merge:
    MECH-373 is an INPUT adaptor on the LANGUAGE channel (parses affect from text), so it
    presupposes the V6 linguistic substrate and belongs in the linguistic tier; the V5 social/
    empathy layer is its consumer, not its home tier. Now owns the V6 children INV-085 (parsed
    affect is an uncertain hypothesis, not ground truth) and MECH-418 (adaptor output feeds the
    empathy stream-binding layer).
  depends_on:
    - ARC-009
    - ARC-010
    - Q-007
    - MECH-031
  location: docs/architecture/language.md#mech-373
  source:
    - docs/thoughts/2026-06-07-language-affect-adaptor-slm-emotion-recognition.md
  notes: >
    Reaped from the 2026-06-07 LanguageAffectAdaptor thought (low-priority reference intake). SCOPE:
    explicitly V5+ -- "probably around v5+", "a reference resource for later language-interface work,
    not a REE-v3 assembly priority"; off the V3 / GAP-7 critical path; epistemic_category
    substrate_conditional keeps it out of the IGW experiment-proposal lane (no language interface exists
    in V3, so any probe would be vacuous; DO NOT build or queue in V3 until routed by an explicit
    version decision). DESIGN COMMITMENTS captured from the thought: (1) parse affect from user/agent
    language when literal semantic content is insufficient; (2) treat emotion labels as UNCERTAIN
    latent-state hypotheses, not categorical truths -- a probability distribution over affect, not a
    hard class; (3) feed affect estimates into REE's wider agent-state and context modelling (the
    social/empathy layer), as priors over another agent's state. DISTINCTNESS: this is an INPUT adaptor
    on the language channel turning external TEXT into affect priors; Q-007 is the orthogonal question
    of REE's OWN internal affective representation (z_beta valence/arousal regimes). The uncertain-not-
    categorical commitment is consistent with the Q-007 constructionist reframe (Barrett 2017/2019:
    emotions are constructed categories, not natural kinds with fixed signatures), which is exactly why
    parsed emotion labels must remain probabilistic hypotheses. depends_on: ARC-009 (language as the
    symbolic mediation/coordination layer this adaptor plugs into), ARC-010 (social mirror-modelling /
    coupling -- where the parsed affect priors are consumed for other-agent modelling), Q-007 (affect as
    uncertain dimensional state, not categorical), MECH-031 (derived social tags / empathy coupling --
    the agent-state tags the adaptor's estimates inform). REFERENCE RESOURCE (notes-level pointer, not
    citable experimental evidence): Towards Data Science, "How to Fine-Tune an SLM for Emotion
    Recognition" (https://towardsdatascience.com/how-to-fine-tune-an-slm-for-emotion-recognition/) --
    motivates a small-language-model (SLM) emotion classifier as the lightweight adaptor's
    implementation candidate. Also relates to the language/affect lateralisation intake
    (2026-04-16, ventral affect-to-meaning binding) and the language-vs-theory-of-mind dissociation
    intake (2026-05-04, language and fast-empathy as separable specialist systems) -- this adaptor sits
    at that interface. Does not promote/demote or weaken any existing claim.

- id: ARC-087
  title: "Sense-specific perceptual-manifold adaptor: each sensory modality requires its own adaptor transforming raw stimulus space into a perceptual-manifold (metric) space before shared-world-model entry; senses are not interchangeable raw streams."
  claim_type: architectural_commitment
  subject: perception.sense_specific_manifold_adaptor
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - ARC-017
    - MECH-103
    - ARC-004
    - ARC-005
    - ARC-019
  location: docs/architecture/sense_specific_perceptual_manifolds.md#arc-087
  source:
    - docs/thoughts/2026-06-07_sight_specific_perceptual_manifolds.md
  what_would_answer: >
    HARD PRECONDITION (substrate existence): this claim is only testable against a V4+
    multimodal-perception substrate that does not yet exist. Confirmed by direct grep of ree-v3/
    (ree_core/ and the full tree) for perceptual_manifold, modality_adaptor, perceptual_adaptor,
    adaptor_depth, sense_specific, manifold_adaptor, cross_modal_negotiation,
    multimodal_perception -- zero matches as of 2026-08-07. V3 uses gradient-only / smell-like
    sensing, where a deep perceptual-manifold adaptor is architecturally unnecessary (there is only
    one shallow modality live), so the claim is vacuously untestable at V3 -- not falsified, not
    confirmed, simply inapplicable. DO NOT design a V3 proxy for this; the right response once the
    gate opens is to build the substrate, per Q-065's identical precondition for the same claim
    cluster.

    Once a V4 multimodal-perception substrate exists with at least two live, non-degenerate
    sense-specific adaptor pathways (the same non-degeneracy floor Q-065 sets for the adjacent
    negotiability question), ARC-087 has a SPECIFIC, TWO-REGIME structure to test per the PA-1
    design-fork resolution (2026-06-14) -- not a generic "senses need some adaptor" claim, which
    would be nearly unfalsifiable.

    CONFIRMING (both parts required, since PA-1 resolved Option C -- one continuum WITH a named
    regime boundary -- over Option A/uniform and Option B/two-unrelated-kinds): (a) WITHIN the
    thalamocortical family (vision, audition, somatosensation, vestibular/proprioception), adaptor
    depth/structure scales continuously with input statistics and hierarchy length rather than
    being fixed per-modality or absent. Decisive test form (Sur et al. rewiring paradigm): route
    one modality's raw signal through another thalamocortical modality's adaptor pathway; the
    receiving pathway should organise toward ITS OWN native topographic structure (e.g. an
    auditory-pathway adaptor fed visual-statistics input should develop visual-like
    orientation/retinotopic organisation, not preserve auditory-native organisation), and the
    resulting structure should track the input statistics rather than a hardcoded per-modality
    template. AND (b) olfaction/gradient-regime senses integrate into the shared world model via a
    COARSER valence/orientation/salience channel (riding ARC-005's precision-routing control plane)
    that is measurably DISTINCT from the within-family precision-weighted, shared-reference-frame
    co-registration (Ernst & Banks 2002 / Gu 2008 style) used by the thalamocortical family -- e.g.
    ablating fine metric co-registration degrades thalamocortical fusion but does not degrade
    olfactory/gradient integration, and vice versa for the coarse channel.

    FALSIFYING (either arm alone is sufficient to collapse PA-1's two-regime structure): (i) the
    thalamocortical family does NOT show continuous depth-scaling with input statistics + hierarchy
    length -- either the rewiring test produces no native reorganisation at all (adaptor depth is
    fixed/hardwired per modality regardless of input), which would push the claim toward Option A
    (uniform, no meaningful depth axis) -- or reorganisation occurs but is independent of input
    statistics (e.g. depth is set by hierarchy position alone with no statistics-sensitivity),
    which would falsify the Sur-et-al.-style mechanism specifically while leaving a weaker
    depth-continuum open; OR (ii) olfaction/gradient senses integrate via the SAME fine,
    precision-weighted co-registration mechanism as the thalamocortical family, with no measurably
    distinct coarser channel -- this would collapse the named regime boundary and push the claim
    toward Option B (two unrelated kinds with no shared underlying continuum) or back toward Option
    A (no real regime distinction, just noise). A result showing (i) alone (regime boundary holds,
    within-family continuum does not) or (ii) alone (within-family continuum holds, regime boundary
    does not) is still informative -- it would motivate a narrower successor claim isolating
    whichever half of PA-1's structure survives, rather than a flat pass/fail on ARC-087 as
    currently scoped.

    NON-DEGENERACY NOTE: per Q-065's identical precondition for the adjacent negotiability question,
    a single live modality or adaptors collapsed to a shared raw-feature space makes this claim's
    test vacuous -- at least two distinguishable, non-degenerate sense-specific adaptor outputs
    must exist before either the CONFIRMING or FALSIFYING conditions above can be meaningfully
    evaluated.
  notes: >
    The architectural commitment from the sight-specific-perceptual-manifolds intake: a sense does
    not contribute raw data directly to the shared world model -- it contributes a shaped GEOMETRY of
    possible differences. The adaptor is the stage at which raw physical signal becomes a usable
    geometry of difference (a perceptual metric / manifold); it is NOT an optional feature extractor.
    Trigger = the Schrodinger colour-theory completion result: even a seemingly basic visual property
    (hue / saturation / lightness) is not a simple physical-to-cognitive mapping but emerges from the
    geometry of perceptual SIMILARITY -- the perceptual space itself is structured (may be curved,
    compressed, asymmetric, non-additive, unlike the raw measurement space). Architecture pattern:
    physical signal -> receptor encoding -> modality-specific adaptor -> perceptual metric / manifold ->
    stable perceptual primitives -> shared world-model update -> action arbitration. RELATION TO OWNED
    CLAIMS (cross-ref, not duplicate): MECH-103 already commits that each modality has its OWN encoder
    pathway into the shared latent carrying complementary structure (vision = object identity / layout,
    audition = events / temporal cues, somatosensory = surface / contact) and that fusion is multi-source
    precision-weighted; ARC-017 owns the minimal sensory stream tags. ARC-087 ADDS the distinct claim
    that the per-modality pathway is not merely a feature extractor but a METRIC / MANIFOLD constructor,
    and that the depth and structure of that transformation is sense-specific and non-trivial (the
    perceptual space's own geometry must be built before world-model entry). The shared world model is
    therefore built from sense-specific geometries made mutually negotiable, NOT from interchangeable raw
    sensory channels. epistemic_category substrate_conditional / V4 / version_relevance v4_v5: there is no
    V4+ multimodal-perception substrate yet; off the V3 / GAP-7 critical path; V3 uses gradient-only /
    smell-like sensing where a deep adaptor is unnecessary -- DO NOT build in V3 until routed by an
    explicit version decision. Does not promote/demote any existing claim. Reaped from the 2026-06-07
    sight-specific-perceptual-manifolds intake.

    PA-1 RESOLUTION (2026-06-14, interactive design-fork, perceptual_adaptors_v4:PA-1): the adaptor-depth
    axis is structured as ONE ORDERABLE CONTINUUM WITH A BIOLOGICALLY-NAMED REGIME BOUNDARY (Option C),
    NOT a single uniform mechanism (Option A) and NOT two unrelated kinds (Option B). Decision driven by
    the human-brain existence proof (project rule: biology before formal definitions). (a) The
    THALAMOCORTICAL senses -- vision, audition, somatosensation, vestibular / proprioception -- share ONE
    canonical cortical microcircuit whose adaptor depth is dialed by input statistics + hierarchy length
    (decisive evidence: Sur et al. rewiring -- auditory cortex develops VISUAL orientation maps when given
    retinal input), so WITHIN this family adaptor depth is a continuum. (b) OLFACTION -- the cluster's
    exemplar gradient primitive -- is the brain's genuine architectural outlier: it BYPASSES the thalamus
    (bulb -> piriform), lands in 3-layer paleocortex, and is NON-TOPOGRAPHIC (content-addressable /
    combinatorial; no spatial odor map analogous to retinotopy / tonotopy / somatotopy); interoceptive /
    valence-gradient signals pattern similarly (older, non-topographic). The regime boundary is therefore
    NAMED, not arbitrary: thalamic-relay + topographic neocortex (canonical metric-manifold constructors)
    vs the phylogenetically OLDER, thalamus-bypassing, non-topographic gradient / chemical regime. CROSS-
    MODAL INTEGRATION TOPOLOGY follows the boundary: within-family senses co-register via shared-reference-
    frame precision / reliability weighting (Ernst & Banks 2002; Gu 2008, already in PA-7), while the
    gradient / chemical regime integrates via a COARSER valence / orientation / salience channel (amygdala /
    OFC association) -- which is how the brain actually integrates olfaction. This directly addresses the
    design concern that the choice affects multimodal perception + integration capacity: Option C protects
    the senses that need tight metric co-registration (the thalamocortical family) WITHOUT forcing chemical /
    gradient senses into a topographic mold they do not fit. HONEST CAVEAT: 'smell = shallow' is an
    ARCHITECTURE statement (non-topographic / gradient), NOT a claim that olfaction is a simple / low-
    information modality -- human odor perception is itself high-dimensional and object-like (PA-7 Louis-2007
    note grounds 'orientation is cheap', not 'olfaction is shallow'). NO new hard depends_on edge added: the
    cross-regime valence channel rides ARC-005's control-plane precision routing already in depends_on, and
    cross-refs to the affect / valence machinery (SD-012 homeostatic drive, ARC-027 harm stream, ARC-088
    anti-collapse map) are PA-5-downstream concerns recorded as cross-references, not build dependencies.
    Still V4 / substrate_conditional; PROMOTES NOTHING (exp_conf stays 0). Fixes the depth-axis structure
    that PA-2 / PA-3 / PA-4 / PA-5 / PA-6 all inherit.

- id: MECH-372
  title: "Modality-heterogeneity / adaptor-depth gradient: senses differ in how much transformation they need before world-model entry -- smell is a near-raw gradient primitive, sight needs a deep perceptual-manifold adaptor; gradient-like (low-adaptor-depth) senses enter earliest developmentally."
  claim_type: mechanism_hypothesis
  subject: perception.modality_heterogeneity_adaptor_depth
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - ARC-087
    - ARC-017
    - ARC-019
    - MECH-103
  location: docs/architecture/sense_specific_perceptual_manifolds.md#mech-372
  source:
    - docs/thoughts/2026-06-07_sight_specific_perceptual_manifolds.md
  notes: >
    The heterogeneity principle under the ARC-087 adaptor architecture: sensory modalities differ in the
    DEPTH of transformation they require before shared-world-model entry -- they are not interchangeable
    raw streams of equal adaptor depth. SMELL sits near the raw end: it is naturally gradient-like
    (stronger / weaker, nearer / farther, attractive / aversive, trail / no-trail, source-seeking /
    source-avoiding) and can enter cognition as a near-raw primitive ORIENTATION signal -- a local
    gradient that can be followed, escaped, strengthened, weakened, remembered, and associated with
    outcome -- with a minimal adaptor. SIGHT sits at the deep end: visual material cannot enter as raw
    signal; a sight adaptor must first construct perceptual distance metrics, colour geometry, edge /
    boundary extraction, figure-ground separation, motion fields, depth relations, object persistence,
    occlusion handling, invariances across lighting / angle / distance / motion, affordance extraction,
    and agency / face / gaze salience. DEVELOPMENTAL-ORDERING COROLLARY: gradient-like (low-adaptor-depth)
    senses are compatible with the EARLIEST world-model construction and plausibly come first
    ('smell may have been the first sense'), while high-adaptor-depth senses (vision) require more
    developmental scaffolding before they become cognitively useful -- ties into ARC-019's staged
    curriculum (each sense admitted when its adaptor is mature, not all at once). For REE-v3, smell /
    gradient-only sensing remains appropriate; for V4+ multimodal sensing must NOT be treated as 'add
    more input channels' -- each sense needs its own preprocessing grammar. epistemic_category
    substrate_conditional / V4 / v4_v5: off the V3 / GAP-7 critical path, DO NOT build in V3. Does not
    promote/demote any existing claim. Reaped from the 2026-06-07 sight-specific-perceptual-manifolds
    intake.

    PA-1 RESOLUTION (2026-06-14, perceptual_adaptors_v4:PA-1): the adaptor-depth gradient asserted here is
    now structured as ONE ORDERABLE CONTINUUM WITHIN THE THALAMOCORTICAL METRIC-MANIFOLD FAMILY (vision /
    audition / somatosensation / proprioception; depth set by input statistics + hierarchy length, per Sur
    rewiring), PLUS A DISTINCT, PHYLOGENETICALLY OLDER GRADIENT / CHEMICAL REGIME (smell + interoception;
    thalamus-bypassing, non-topographic, content-addressable) separated by a biologically-named boundary
    (thalamic relay + topographic neocortex). See ARC-087 for the full decision rationale (Option C). This
    SHARPENS the developmental-ordering corollary: the older non-topographic regime enters first not merely
    because it is 'shallower' but because it is a DIFFERENT, EARLIER adaptor class -- 'smell may have been
    the first sense' is the regime-boundary statement, and it is what the PA-6 adaptor-maturity curriculum
    ordering operationalises. Still V4 / substrate_conditional; PROMOTES NOTHING (exp_conf stays 0).

- id: Q-065
  title: "Per-sense adaptor specification and cross-modal negotiability: is each modality's perceptual metric learned or partly defined, and how are heterogeneous sense-specific geometries made mutually negotiable inside one shared world model?"
  claim_type: open_question
  subject: perception.adaptor_metric_origin_and_negotiability
  polarity: asks
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Two coupled questions, both answerable only against a multimodal-perception
    substrate that does not yet exist (ARC-087 sense-specific adaptors + MECH-372
    adaptor-depth gradient). (1) METRIC ORIGIN: for a given modality, compare a
    LEARNED-metric arm (adaptor self-organises its perceptual metric from the
    statistics of perceptual similarity) against a DEFINED-metric arm (structural
    prior fixed on the manifold). ANSWER 'learned' if the self-organised metric
    reaches equal-or-better downstream world-model performance and reproduces the
    modality's known similarity geometry without the prior; ANSWER 'partly
    defined/innate' if the learned-only arm fails to converge on the correct
    geometry and the structural prior is load-bearing. (2) CROSS-MODAL
    NEGOTIABILITY: feed two structurally dissimilar adaptor outputs (e.g. smell's
    gradient geometry and sight's perceptual-manifold geometry) into MECH-103
    precision-weighted fusion and test whether a common negotiation currency
    emerges -- ANSWER 'mutually negotiable' if fusion produces a coherent shared
    world-model estimate that respects both metrics; ANSWER 'not negotiable as-is'
    if fusion degenerates (one modality dominates, or the estimate is incoherent
    across the dissimilar manifolds). HARD PRECONDITION: at least two
    NON-DEGENERATE sense-specific adaptors must exist, each producing a live,
    distinguishable perceptual-manifold output -- a single modality, or adaptors
    collapsed to a shared raw-feature space, makes the negotiability question
    vacuous (nothing heterogeneous to negotiate). V4/V5; off the V3 / GAP-7
    critical path. The right response is to design the multimodal substrate, NOT
    to queue a V3 experiment.
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - ARC-087
    - MECH-372
  location: docs/architecture/sense_specific_perceptual_manifolds.md#q-065
  source:
    - docs/thoughts/2026-06-07_sight_specific_perceptual_manifolds.md
  notes: >
    Two coupled open questions raised by the sense-specific-adaptor architecture (ARC-087) and the
    adaptor-depth gradient (MECH-372). (1) ORIGIN OF THE METRIC: for each modality, is the perceptual
    metric LEARNED from experience (self-organised from the statistics of perceptual similarity, e.g.
    colour geometry emerging from similarity structure) or partly DEFINED / innate (a structural prior on
    the manifold)? The Design invariant from the thought -- 'each sensory modality should define OR learn
    its own perceptual metric before being admitted into the shared world model' -- leaves this fork open.
    (2) CROSS-MODAL NEGOTIABILITY: how are heterogeneous sense-specific geometries made MUTUALLY
    NEGOTIABLE so a single shared world model can integrate them -- smell's gradient geometry, sight's
    perceptual-manifold geometry, touch's boundary / pressure / texture / resistance / contact geometry,
    hearing's temporal-source / rhythm / pitch / localisation geometry, proprioception's body-state
    transition geometry? This is the unification problem behind MECH-103's multi-source precision-weighted
    fusion: fusion presupposes a common negotiation currency across structurally dissimilar manifolds.
    epistemic_category substrate_conditional set EXPLICITLY so narrow_open_question does NOT fire (there is
    no V4+ multimodal-perception substrate to test against; the right response is to wait for / design that
    substrate, not to queue an experiment). Off the V3 / GAP-7 critical path. Reaped from the 2026-06-07
    sight-specific-perceptual-manifolds intake.

# Registered 2026-06-09 -- emotion-as-anti-collapse architecture (UNIFYING MAP, not new substrate).
# The affect/relief/safety/harm/drive/persistence/behavioural-diversity primitives are ALREADY OWNED;
# the only genuinely-new content is the organising framing: affect = a set of gated, partially
# independent evaluators whose collective effect is to prevent behaviour collapsing onto the currently
# dominant gradient (anti-monostrategy). Registered as ONE umbrella ARC claim depends_on the owned set;
# NO redundant substrate claims. Same shape as the attention=distributed-precision-selection map and
# ARC-084 signed-coupling map: REE owns the pieces, the new thing is the explicit map. Source thought's
# governance note honoured: validates no claim, promotes no substrate, alters no experiment routing.
# Source: docs/thoughts/2026-06-08_emotion_as_anti_collapse_architecture.md
# Home doc: docs/architecture/emotion_as_anti_collapse_architecture.md
- id: ARC-088
  title: "Emotion-as-anti-collapse architecture: emotion-like systems are a set of GATED, PARTIALLY INDEPENDENT evaluators (is-this-harmful / is-this-safe / did-this-reduce-suffering / am-I-pursuing-something-I-want / did-I-merely-like-the-last-thing / am-I-stuck / am-I-overcommitted / is-this-goal-stale / should-I-explore / should-I-stop / should-I-sleep-and-consolidate / should-I-release-the-policy) whose collective effect is to prevent behaviour from collapsing onto the currently dominant gradient. Affect is therefore the architectural SOURCE of structured behavioural diversity -- not decoration on a rational optimiser and not random exploration noise, but native modulatory control (gates, biases, interrupts, salience shifts, persistence regulators, release conditions, credit-assignment signals) that produces STRUCTURED deviations from naive optimisation. A unifying MAP over REE's already-owned affect machinery, not a new substrate; an optimise-then-bolt-on-safety design fails because these evaluators must be native, not added afterward."
  claim_type: architectural_commitment
  subject: affect.anti_collapse_behavioral_diversity_map
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-088@2026-06-18T05:15:00.986605Z"
      as_of: 2026-06-18
      verdict: hold_pending_v3_substrate/applied
  epistemic_category: substrate_coherence
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-06-09"
  depends_on:
    - ARC-065     # behavioural-diversity generation pathway -- prevent collapse to the smoothest single regime (parent prior art)
    - SD-011      # dual nociceptive streams -- "is this harmful?" as a native separable stream
    - SD-050      # suffering-derivative comparator -- relief substrate
    - MECH-302    # relief-completion event -- "did this reduce suffering?" (action-contingent harm reduction)
    - MECH-304    # conditioned safety store -- "is this safe?" / commitment-release gate
    - SD-012      # homeostatic drive -- "am I pursuing something I want?" (wanting)
    - ARC-079     # gated goal persistence; disengagement the ungated default -- "am I overcommitted / is this goal stale / should I release?"
  related_claims:
    - MECH-359    # CONCEPTUAL cross-ref, NOT a build dependency (moved out of depends_on 2026-07-01 PM /governance reverse-dep phase-consistency fix). ARC-088's OWN scope note already declares this: "the candidate-differentiated extension MECH-359 is V4, reached via depends_on not a permission gate" -- i.e. the author never intended MECH-359 (mechanism_hypothesis / substrate_conditional / implementation_phase v4, the V4/V5 GENERALISATION to an explicit multi-channel per-candidate affect representation) to GATE ARC-088. ARC-088 is a UNIFYING MAP that is "substantially realised now" on V3 substrate (harm SD-010/SD-011, relief SD-050/MECH-302, safety MECH-303/304, drive SD-012, behavioural-diversity ARC-065 stack); MECH-359 is the aspirational V4 extension "reached via depends_on," not a V3 build prerequisite. Keeping it in depends_on made a v3 architectural_commitment spuriously read as V3-blocked-on-V4. New affect work still "attaches to the relevant owned child (or extends MECH-359)" per the ARC-088 notes. PROMOTES NOTHING.
  location: docs/architecture/emotion_as_anti_collapse_architecture.md
  notes: >
    UNIFYING MAP, not a new substrate. Every constituent evaluator is already a registered REE claim;
    ARC-088 asserts only that, TAKEN TOGETHER, they constitute REE's anti-collapse / behavioural-
    diversity architecture, and that an optimise-then-bolt-on-safety design fails because harm, relief,
    safety, blocked agency, curiosity, commitment release, uncertainty, drive, and care must be NATIVE
    control systems (if not, they arrive too late). epistemic_category set substrate_coherence EXPLICITLY
    (the architectural_commitment -> substrate_coherence default, made explicit) so promote/demote stays
    suppressed and conflict-resolution alerts still fire -- consistent with the source thought's
    governance note that the intake validates no claim, promotes no substrate, and alters no experiment
    routing. The depends_on set is REPRESENTATIVE, not exhaustive: behavioural-diversity children
    (MECH-313 noise-floor / MECH-314 structured-curiosity / MECH-320 tonic-vigor), boredom (ARC-067/
    MECH-330), curiosity sub-flavours (MECH-314a/b/c), liking-vs-wanting, blocked-agency, and the
    "should I sleep/consolidate" offline-integration evaluators (MECH-272/273 sleep cluster) all sit
    under this umbrella and are reachable through the depends_on chain rather than re-listed.
    ARCHITECTURAL CONSEQUENCE (load-bearing): do NOT merge all emotions into one value scalar -- the
    OPPOSITE; preserve DISTINCT affective streams while letting them share INTERFACES where appropriate.
    They may converge on shared CONSUMERS (E3 score bias, commitment gating, residue, offline
    consolidation) but their LEARNING TARGETS and GATING CONDITIONS must stay distinct; reuse/gating
    audits that touch any convergence point should reference ARC-088 to check distinctness is preserved.
    603 LINEAGE as worked example: the agent could form goals, experience threat, and suppress freezing,
    but did not learn directed escape until the missing affective-learning bridge (relief as action-
    contingent harm reduction + safety as threat-absence prediction, both feeding bounded future-action
    bias) was identified -- "optimise away from harm" was not enough. SCOPE: V3-grounded framing (harm
    SD-010/SD-011, relief SD-050/MECH-302, safety MECH-303/304, drive SD-012, behavioural-diversity
    ARC-065 stack are V3 substrate; the candidate-differentiated extension MECH-359 is V4, reached via
    depends_on not a permission gate); implementation_phase v3 because the anti-collapse architecture is
    substantially realised now and the framing is visitor-facing today. The visitor-facing formulation
    ("AI begins from optimisation-then-safety-wrapper; REE begins from partially-independent control
    systems that prevent any one gradient taking over") lives in the home doc as DOCUMENTATION and
    registers no additional claim. NO promotion/demotion/weakening of any existing claim; NO substrate
    code; NO experiment. New affect work attaches to the relevant owned child (or extends MECH-359), not
    to this umbrella -- DO NOT invent redundant substrate claims under ARC-088.

# Registered 2026-06-09 -- spintronic-memristive cognifold-substrate intake (docs/thoughts/2026-06-06_spintronic_memristive_cognifold_substrate.md). POST-V5 / future physical-instantiation compass; NOT on the V3 path and NOT queueable for V3. The load-bearing near-term target is specifying the cognifold in SUBSTRATE-INDEPENDENT primitives (ARC-089); the spintronic-memristive hardware (MECH-374) is a post-V5 physical-substrate candidate the primitives let us later evaluate. See docs/architecture/spintronic_memristive_cognifold_substrate.md and the raw thought. External anchors: Shao et al. arXiv:2112.02879 (spintronic memristors survey); TDK spin-memristor neuromorphic element (industry); arXiv:2009.06594 (memristive control of spin-Hall nano-oscillator synchronisation).
- id: ARC-089
  title: "Substrate-independent cognifold primitives: REE specifies its single cognifold (one interacting state-space, not a modular stack) in a hardware-neutral primitive vocabulary -- persistent state, history-dependent deformation, multi-timescale field dynamics, oscillatory propagation, stochastic transition, attractor transitions, residue-as-deformation-of-the-future-action-landscape, commitment as a real boundary between simulated and released action, and offline reintegration -- so that any FUTURE physical substrate is evaluated by whether it can INSTANTIATE these dynamics, not merely execute modular software. The primitives remain software-expressible now while also being legible as future physical-substrate affordances."
  claim_type: architectural_commitment
  subject: cognifold.substrate_independent_primitives
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: post_v5
  version_relevance: post_v5
  depends_on:
    - ARC-013
    - ARC-084
    - MECH-090
    - INV-049
    - ARC-005
  location: docs/architecture/spintronic_memristive_cognifold_substrate.md#arc-089
  notes: >
    From the spintronic-memristive cognifold-substrate intake (raw section 1-2 + section 3
    "architectural commitment" classification). This is the LOAD-BEARING near-term target of the
    note: the hardware discussion is post-V5, but it clarifies HOW REE should specify the cognifold
    so a later physical instantiation is possible. NOT a new substrate to build: the primitives it
    enumerates are ALREADY owned and remain software-expressible -- residue-as-curvature/cognitive-map
    (ARC-013), the cognifold-as-single-interacting-field with signed coupling (ARC-084), the
    commitment latch / real boundary between simulated and released action (MECH-090), offline
    reintegration as a mathematical necessity for model-building agents (INV-049), the control plane
    that supplies gain/precision/phase modulation (ARC-005). What is NEW is the explicit
    architectural COMMITMENT to keep this primitive vocabulary substrate-independent and physically
    legible -- a compass for future-substrate evaluation, not an implementation requirement.
    epistemic_category set substrate_conditional EXPLICITLY (overriding the architectural_commitment
    -> substrate_coherence inference): this is a candidate compass gated on a post-V5 physical
    instantiation that does not exist, NOT the adopted V3 substrate; promote/demote suppressed.
    GUARDRAIL (raw section 8): if a future agent tries to convert this into REE-v3 implementation work
    (hardware targets, hardware-abstraction layers, android substrate tasks), STOP and reframe -- the
    correct near-term extraction is "define and preserve cognifold primitives in software-neutral
    terms." Does not promote/demote any existing claim. POST-V5; off the V3 critical path; NOT
    queueable for V3.
- id: MECH-374
  title: "Memristive deformation as the physical analogue of residue/cognifold curvature: spintronic-memristive media (magnetic tunnel junctions, nanomagnet ensembles, domain walls, topological spin textures, spin waves / spin-Hall nano-oscillators) are physical substrates in which past activity alters future state-transition behaviour -- making 'residue is not merely stored memory but deformation of the future action landscape' physically literal in a way von Neumann computation does not. They are a candidate post-V5 physical cognifold substrate because they combine persistent state, history-dependent deformation, oscillatory dynamics, stochastic switching, attractor-like state-space trajectories, and possible low-power embodied operation in one medium."
  claim_type: mechanism_hypothesis
  subject: cognifold.memristive_physical_substrate_candidate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: post_v5
  version_relevance: post_v5
  depends_on:
    - ARC-089
    - ARC-013
    - MECH-363
    - MECH-362
  location: docs/architecture/spintronic_memristive_cognifold_substrate.md#mech-374
  notes: >
    From the spintronic-memristive intake (raw section 0 + section 3 "mechanism hypothesis"
    classification + section 4 REE<->spintronic mapping table). The REE bridge: memristive
    deformation = history-dependent change of the future conductance/state landscape, the physical
    twin of residue-as-latent-curvature (ARC-013) and of the deformable competitive cognifold field
    (MECH-363). Mapping (raw section 4): cognifold = single interacting state-space; residue field =
    history-dependent landscape deformation; E1 = slow attractor structure; E2 = fast transition /
    oscillatory propagation; E3 = thresholded commitment / switching between candidate and released
    action; control plane = gain/precision/phase-coupling modulation; offline integration =
    re-driving the field without action authority. External literature anchors preserved for a LATER
    targeted lit-pull (NOT load-bearing for this candidate registration, NOT citable REE mechanism
    evidence -- these are hardware surveys, out of REE's experimental domain): Shao et al.,
    "Spintronic memristors for computing", arXiv:2112.02879; TDK spin-memristor neuromorphic element
    (industry trend, CEA + Tohoku); "Memristive control of spin-Hall nano-oscillator
    synchronisation", arXiv:2009.06594. IMPORTANT NON-CLAIMS (raw section 5): does NOT claim REE-v3
    should target spintronic hardware, does NOT claim such hardware is android-ready, does NOT claim
    future REE MUST run on spintronics. The useful weaker claim: IF REE is a single cognifold, future
    physical substrates should be assessed by whether they instantiate persistent, deformable,
    multi-timescale action-field dynamics rather than merely execute modular software. Should NOT be
    promoted directly to an invariant (raw section 3). epistemic_category substrate_conditional /
    POST-V5; off the V3 critical path; NOT queueable for V3.
- id: Q-066
  title: "Can spintronic-memristive media (or any candidate physical cognifold substrate) scale to embodied REE cognition WITHOUT losing auditability, stability, or the harm/safety boundaries that REE's software cognifold currently enforces -- and which cognifold primitives (ARC-089) does each candidate substrate genuinely instantiate versus merely approximate?"
  claim_type: open_question
  subject: cognifold.physical_substrate_auditability_safety_scaling
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Answered post-V5 by a physical-substrate characterisation, not by any V3
    software run. For each candidate physical cognifold medium
    (spintronic-memristive per MECH-374, or any other), do a
    primitive-by-primitive instantiation audit against the ARC-089 vocabulary
    (persistent state, history-dependent deformation, multi-timescale dynamics,
    oscillatory propagation, stochastic transition, attractor transitions,
    residue-as-future-landscape-deformation, commitment-as-real-boundary, offline
    reintegration) and label each primitive INSTANTIATED vs merely APPROXIMATED.
    Then test the three safety/auditability axes: (a) can the
    simulated-vs-released action boundary (MECH-090) be enforced as a HARD,
    externally-inspectable gate in the medium, or only as a soft statistical
    tendency? (b) does the medium's stochastic switching destabilise the regime
    that signed competitive coupling (Q-058) buys -- measure
    drift/segregation-loss vs the software baseline; (c) is offline reintegration
    without action authority (INV-049) physically realisable AND verifiable.
    ANSWER 'scales without losing auditability/safety' only if every harm/safety
    boundary remains a hard inspectable gate and stability is non-inferior to
    software; ANSWER 'does not' if any boundary degrades to un-auditable or
    stochastic switching breaks stability. HARD PRECONDITION: a
    physical-cognifold-substrate DECISION must have been taken and a candidate
    medium must actually be available to characterise -- with no physical
    substrate in hand the audit is purely a derivation against device-physics
    specs, not measurement. POST-V5; off the V3 critical path. DO NOT queue a V3
    experiment against this.
  epistemic_category: substrate_conditional
  implementation_phase: post_v5
  version_relevance: post_v5
  depends_on:
    - ARC-089
    - MECH-374
    - INV-049
    - Q-058
  location: docs/architecture/spintronic_memristive_cognifold_substrate.md#q-066
  notes: >
    From the spintronic-memristive intake (raw section 3 "open question" classification + section 5
    weaker-useful-claim). The auditability/stability/safety axis is the genuinely-open one: an analog
    deformable physical medium may instantiate the dynamical primitives (ARC-089) while making the
    commitment boundary (MECH-090), provenance/write gating, and harm-residue firewall harder to
    inspect and guarantee than in software. Sub-questions: (1) can a physical cognifold preserve the
    simulated-vs-released action boundary as a hard, auditable gate? (2) does an analog substrate's
    stochastic switching threaten the stability that signed competitive coupling (Q-058) buys in the
    software cognifold? (3) how is offline reintegration without action authority (INV-049)
    physically realised and verified? epistemic_category set substrate_conditional EXPLICITLY
    (overriding open_question -> answer_state) so narrow_open_question does NOT fire -- this is a
    post-V5 question awaiting a physical-substrate decision, NOT a V3-tractable one to narrow by
    experiment. POST-V5; off the V3 critical path; DO NOT queue an experiment against this in V3.

- id: MECH-375
  title: "Trainable relief critic Q_relief(state, action, threat_context): a LEARNED PARAMETRIC head that predicts expected harm/suffering reduction following an action under threat -- negative-reinforcement credit assignment, distinct from generic reward / wanting. It is the trainable successor to the FIXED-ARITHMETIC per-first-action-class EMA credit table of the MECH-358 relief half, and is downstream of (NOT a duplicate of) the non-trainable MECH-302 / SD-050 relief-completion comparator that merely DETECTS relief on the current state. Q_relief is a function approximator generalising relief-credit across continuous state x action x threat-context, trained on action-contingent drops in z_harm_a: positive target when a directed action under threat is followed by a temporally-close, action-contingent z_harm_a drop not explained by passive environmental drift; negative / extinction target when the expected relief action fails to reduce harm. Feeds E3 as a bounded, threat-gated, extinguishable score-bias toward actions predicted to reduce harm -- never global reward, never fires when safe, never credits no-op / freeze unless explicitly modelling passive safety. MECH-094-safe: simulated relief in hypothesis mode must not train the waking critic unless authorised as play / training."
  claim_type: mechanism_hypothesis
  subject: relief.trainable_relief_critic
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-06-09"
  depends_on:
    - MECH-358   # the fixed-arithmetic relief EMA credit table this makes trainable (per-action-class -> parametric Q_relief)
    - SD-059     # the escape-affordance bridge architecture the fixed table instantiates
    - MECH-302   # non-trainable relief-completion comparator: DETECTS relief on current state; Q_relief BINDS + GENERALISES relief-credit to action (teaching-signal source)
    - SD-050     # suffering-derivative comparator substrate (provides the relief-completion event used as training target)
    - MECH-357   # the scalar avoidance-efficacy gate the bridge sits on
    - SD-058     # instrumental-avoidance acquisition architecture
    - SD-011     # z_harm_a affective stream -- the threat / harm signal whose action-contingent drop is the training target
    - MECH-094   # simulation / provenance gate: hypothesis-mode relief must not train the waking critic
  distinct_from:
    - MECH-302   # comparator fires on the CURRENT state (non-trainable rolling-window descent detector); MECH-375 is a TRAINABLE state x action x threat-context critic
    - MECH-358   # FIXED per-action-class EMA credit table; MECH-375 is a LEARNED parametric function approximator generalising across continuous state / action / context
    - SD-037     # orexin override: reflexive arousal-driven escape; Q_relief credit is learned negative-reinforcement, not a reflex
    - MECH-376   # sister TRAINABLE safety predictor: prospective threat-absence; MECH-375 is action-contingent aversive-offset relief
  location: docs/architecture/trainable_relief_safety_affordance_learners.md
  source:
    - docs/thoughts/2026-06-08_Trainable_Releif_and_Safety.md
  functional_restatement: >
    Mechanism hypothesis. The escape-affordance bridge (SD-059 / MECH-358) binds relief to escape via
    a FIXED-ARITHMETIC per-first-action-class EMA credit table; the relief-completion machinery it
    consumes (MECH-302 / SD-050) is a NON-TRAINABLE rolling-window comparator that detects a sustained
    z_harm_a descent on the CURRENT state. Neither learns a generalising function of state x action x
    threat-context. MECH-375 asserts a TRAINABLE relief critic Q_relief(state, action, threat_context)
    -- a parametric head (gradient-trained function approximator) whose function is negative-reinforcement
    CREDIT ASSIGNMENT: "this action reduced suffering / terminated harm", semantically distinct from
    generic reward or appetitive wanting even though it may reuse reward / goal machinery.

    Training target (action-contingency, not passive drift): a POSITIVE target when a directed action
    under threat is followed by a drop in z_harm_a; a STRONGER target when the drop is temporally close,
    action-contingent, and not explained by passive environmental drift; a NEGATIVE / EXTINCTION target
    when the expected relief action fails to reduce harm. Candidate inputs (per the thought): current
    z_world, z_self, the threat / harm streams (especially z_harm_a), the action class / candidate first
    action, and possibly local trajectory features from E3.

    Functional role: under FUTURE threat, bias E3 toward actions Q_relief predicts will reduce harm /
    suffering; do NOT fire when safe; do NOT credit no-op / freeze unless explicitly modelling passive
    safety. The bias enters E3 as a BOUNDED, THREAT-GATED, EXTINGUISHABLE score-bias (it cannot dominate
    E3 scoring or become a global approach signal), mirroring the three guards already proven on the
    fixed MECH-358 bridge (bias_scale clamp, threat-context gate, per-tick leak).

    V3-NARROW (this claim): the MINIMAL trainable bridge -- a small parametric critic over the
    already-built substrate (escape bridge, harm-derivative comparator, threat detector, discrete
    first-action classes), replacing MECH-358's fixed EMA table with a learned generaliser. This is the
    post-V3-EXQ-603i successor scaffold, close to the V3 critical path but NOT yet built. The V4 / V5-RICH
    tier (expression-as-action-geometry, richer continuous state / policy / location indexing, hippocampal
    affect-gradient indexing of relief) is OUT OF SCOPE here and OFF the V3 path -- see the home doc.
  notes: >
    Reaped from the 2026-06-08 trainable-relief/safety thought intake (post-V3-EXQ-603i successor
    candidate). The genuinely-NEW structure vs the owned cluster is TRAINABILITY + parametric
    generalisation + action-binding of relief credit: MECH-302 / SD-050 is a non-trainable comparator
    (DETECTION on the current state) and MECH-358 is a fixed per-action-class EMA table (no generalising
    function), so a learned Q_relief over state x action x threat-context is not a restatement of either.
    epistemic_category substrate_conditional: the trainable parametric critic is PLANNED but NOT YET BUILT
    (the current bridge is the fixed table), so promote / demote is suppressed and the claim is kept off
    the IGW experiment-proposal lane until the head exists -- the right next step is to BUILD the trainable
    head (a later decision), not to queue a vacuous probe on the fixed-table substrate. implementation_phase
    v3 because the minimal trainable bridge is V3-tractable on the existing substrate. Design guardrails
    (from the thought, all carried into the head): bounded (cannot dominate E3 scoring); threat-gated (no
    bias when safe); extinguishable (failed relief predictions decay); action-bound (credits the
    action / policy that produced harm reduction, not generic state value); MECH-094-safe (simulated
    relief in hypothesis mode must not train the waking learner unless explicitly authorised as
    play / training). Does NOT promote / demote / weaken any existing claim. Sister claim: MECH-376
    (trainable safety predictor); open decomposition question: Q-067.

- id: MECH-376
  title: "Trainable prospective safety predictor P_safety(state, cue, action, context): a LEARNED PARAMETRIC head predicting threat-ABSENCE / response-produced safety / conditioned inhibition, licensing commitment-release, approach to safety affordances, and recovery after threat. Distinct from z_goal (it predicts the ABSENCE of threat, not the PRESENCE of a goal) and from relief (it is a PROSPECTIVE threat-absence prediction, not an aversive-offset event). It is the trainable + contrastive successor that makes the MECH-303 (contextual passive safety) + MECH-304 (cue-specific conditioned inhibition) substrate learnable, replacing the FIXED-ARITHMETIC per-first-action-class EMA safety table of the MECH-358 safety half: positive target when a cue / context / action predicts threat absence or response-produced safety; negative target when 'safe' cues are followed by threat recurrence; CONTRASTIVE target to stop safety overgeneralising to stable background features that merely co-occurred with relief. Output supports commitment release, permits approach to safety affordances, stabilises recovery, and prevents persistent defensive mode when threat is genuinely absent."
  claim_type: mechanism_hypothesis
  subject: safety_prediction.trainable_safety_predictor
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-06-09"
  depends_on:
    - MECH-358   # the fixed-arithmetic per-action-class EMA safety table this makes trainable + contrastive
    - SD-059     # the escape-affordance bridge architecture the fixed table instantiates
    - MECH-303   # contextual passive safety SUBSTRATE hypothesis -- the diffuse-context safety system P_safety makes trainable
    - MECH-304   # cue-specific conditioned-inhibition SUBSTRATE hypothesis -- the cue-specific safety system P_safety makes trainable
    - MECH-302   # relief-completion events are one teaching signal for response-produced safety
    - MECH-357   # the scalar avoidance-efficacy gate the bridge sits on
    - SD-058     # instrumental-avoidance acquisition architecture
    - SD-011     # z_harm_a affective stream -- the threat signal whose absence P_safety predicts
    - MECH-094   # simulation / provenance gate: hypothesis-mode safety must not train the waking predictor
    - MECH-375   # sister trainable head: together = action-contingent relief (escape) + cue / context safety (recovery)
  distinct_from:
    - MECH-303   # contextual passive safety SUBSTRATE hypothesis; MECH-376 is the TRAINABLE + CONTRASTIVE parametric form
    - MECH-304   # cue-specific safety SUBSTRATE hypothesis; MECH-376 generalises it parametrically with a contrastive target
    - MECH-358   # FIXED per-action-class EMA safety table; MECH-376 is a LEARNED prospective predictor
    - INV-034    # goal maintenance / z_goal: P_safety predicts threat ABSENCE, NOT goal PRESENCE -- must remain distinct from z_goal
    - MECH-375   # sister relief critic: action-contingent aversive-offset; MECH-376 is prospective threat-absence / conditioned inhibition
  location: docs/architecture/trainable_relief_safety_affordance_learners.md
  source:
    - docs/thoughts/2026-06-08_Trainable_Releif_and_Safety.md
  functional_restatement: >
    Mechanism hypothesis. Safety is NOT relief and NOT mere low harm: it is a PROSPECTIVE learned
    predictor -- "this cue / context / action means threat is absent or likely to remain absent" -- that
    licenses commitment-release, approach, and recovery, and must remain DISTINCT from z_goal. The owned
    safety substrate (MECH-303 contextual passive store + MECH-304 cue-specific conditioned inhibition)
    is a pair of substrate HYPOTHESES; the IMPLEMENTED V3 form (the MECH-358 safety half) is a
    fixed-arithmetic per-action-class EMA credit table with no generalising function and no contrastive
    discipline. MECH-376 asserts a TRAINABLE parametric predictor P_safety(state, cue, action, context)
    that makes that substrate learnable.

    Training target: POSITIVE when a cue / context / action predicts threat absence or response-produced
    safety; NEGATIVE when a "safe" cue is followed by threat recurrence; CONTRASTIVE to prevent
    overgeneralising safety to stable background features that merely co-occurred with relief (the failure
    mode "safety == low harm" or "safety == recent relief"). Candidate inputs (per the thought): current
    z_world, cue / context features, action class / recent action, threat history, time since threat
    offset, and possibly a hippocampal context slot / rule representation.

    Functional role: support COMMITMENT RELEASE; permit APPROACH to safety affordances; STABILISE
    recovery after threat; PREVENT persistent defensive mode when threat is genuinely absent. Like the
    relief critic the output enters E3 as a bounded, threat-context-gated, extinguishable bias, never a
    global approach signal.

    Why both heads (see Q-067): relief alone teaches "that action reduced harm" but need not produce a
    stable prospective safety model; safety alone teaches "this context / cue is safe" but need not solve
    action-specific escape under active threat. The mature avoidance system needs action-contingent relief
    learning (MECH-375) for escape AND cue / context-contingent safety learning (MECH-376) for recovery,
    inhibition of defensive mode, and future approach.

    V3-NARROW (this claim): the MINIMAL trainable safety predictor over the already-built substrate,
    replacing MECH-358's fixed safety EMA table with a learned + contrastive generaliser. The V4 / V5-RICH
    tier (social safety attribution, rich hippocampal context / rule indexing, safety-as-action-geometry)
    is OUT OF SCOPE here and OFF the V3 path -- see the home doc.
  notes: >
    Reaped from the 2026-06-08 trainable-relief/safety thought intake. The genuinely-NEW structure vs the
    owned cluster is the TRAINABLE + CONTRASTIVE parametric form of safety prediction: MECH-303 / MECH-304
    are substrate hypotheses and MECH-358's safety half is a fixed per-action-class EMA table, so a learned
    P_safety with an explicit contrastive target (safety != low harm, safety != recent relief) is not a
    restatement of either. Kept explicitly DISTINCT from z_goal / INV-034 (threat-absence prediction, not
    goal presence). epistemic_category substrate_conditional: the trainable predictor is PLANNED but NOT
    YET BUILT, so promote / demote is suppressed and the claim is kept off the IGW experiment-proposal lane
    until built -- the right next step is to BUILD it (a later decision), not to queue a vacuous probe.
    implementation_phase v3: the minimal trainable predictor is V3-tractable on the existing substrate.
    Design guardrails carried in: bounded, threat-gated, extinguishable, CONTRASTIVE (safety cannot mean
    "low harm" or "recent relief"), cue / context-bound (credits the cue / context / action that predicts
    threat absence), MECH-094-safe. Does NOT promote / demote / weaken any existing claim. Sister claim:
    MECH-375; open decomposition question: Q-067.

- id: Q-067
  title: "Relief/safety decomposition: when the fixed escape-affordance bridge is insufficient, is the missing structure relief-only (action-contingent escape credit), safety-only (prospective threat-absence prediction), or both-required -- and should a trainable successor (MECH-375 / MECH-376) train only the missing / weak component first?"
  claim_type: open_question
  subject: escape.relief_vs_safety_decomposition
  polarity: asks
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-067@2026-06-29T06:02:56.963572Z"
      as_of: 2026-06-29
      verdict: hold_pending_v3_substrate/applied
  what_would_answer: |
    Tied to the V3-EXQ-603i fixed-first-action-class escape bridge as its minimal
    diagnostic; let 603i complete without mid-run reinterpretation, then read the
    decomposition. The discriminator: when the fixed bridge is INSUFFICIENT,
    decompose the failure into relief-only (action-contingent escape credit,
    MECH-302/SD-050 lineage) vs safety-only (prospective threat-absence
    prediction, MECH-303/304 lineage) vs both-required, and have the trainable
    successor (MECH-375 Q_relief / MECH-376 P_safety) train ONLY the missing/weak
    component first. ANSWER 'relief-only missing' if directed escape fails
    specifically where action-contingent z_harm_a drop credit is absent but
    threat-absence is already predicted; ANSWER 'safety-only missing' if
    commitment-release / approach-to-safety fails while relief credit fires;
    ANSWER 'both-required' if neither half alone closes the gap. Decision rule
    from 603i: PARTIAL pass -> inspect which half emerged and train the weak one;
    FAIL-with-non-vacuity -> conclude the fixed table is too crude and a trainable
    head is required, NOT that the bridge concept is wrong; FAIL because bridge
    credit never fired -> route back to substrate readiness (harm derivative,
    trained encoder, threat detector, action-contingency detection). HARD
    PRECONDITION: the trainable heads MECH-375/376 are NOT yet built, and the read
    is only meaningful if 603i is non-vacuous -- the escape-affordance bridge
    credit must actually fire on a non-trivial fraction of threat episodes; if
    credit never fires (SD-059's GLOBAL-SCALAR avoidance_efficacy limitation, the
    603h engaged-but-insufficient signature), there is no relief-vs-safety pattern
    to decompose and the question is premature. V3-adjacent via the 603i successor
    route, but the decomposition itself awaits the trainable substrate. Do NOT
    build the trainable heads or queue against this until 603i lands.
  epistemic_category: substrate_conditional
  implementation_phase: v3
  depends_on:
    - MECH-375
    - MECH-376
    - SD-059
    - MECH-358
    - MECH-302
    - MECH-303
  location: docs/architecture/trainable_relief_safety_affordance_learners.md#q-067
  source:
    - docs/thoughts/2026-06-08_Trainable_Releif_and_Safety.md
  notes: >
    Raised by the 2026-06-08 trainable-relief/safety thought together with the V3-EXQ-603i outcome grid.
    V3-EXQ-603i (the fixed first-action-class bridge) is the minimal diagnostic and should be allowed to
    complete without mid-run reinterpretation. The decomposition fork: (1) if 603i PARTIALLY passes,
    inspect whether a relief-only, safety-only, or both-required pattern emerged and have the trainable
    successor train ONLY the missing / weak component first; (2) if 603i FAILS while non-vacuity fires, do
    NOT conclude the relief/safety bridge is wrong -- the likely conclusion is that the fixed
    first-action-class table is too crude and a TRAINABLE state-action / context-cue learner
    (MECH-375 / MECH-376) is required; (3) if 603i fails because bridge credit never fires, route back to
    SUBSTRATE READINESS (harm derivative, trained encoder, threat detector, action-contingency detection),
    not to the trainable head. epistemic_category substrate_conditional set EXPLICITLY so
    narrow_open_question does NOT fire: the trainable heads are not yet built and there is no substrate to
    adjudicate the decomposition against until one is. Off the immediate V3 critical path but tied to it
    via the 603i successor route. Reaped from the 2026-06-08 trainable-relief/safety intake.
- id: ARC-090
  title: "Externalised Default Mode Operation: play instantiates DMN functions (self-reference, autobiographical memory, social imagination, future simulation, counterfactual thought, narrative construction) externally -- pushed outward into objects, roles, movement, spoken narration, and temporary as-if worlds -- as the developmental precursor to the internal DMN, compressing into internal generative modelling over development."
  claim_type: architecture_hypothesis
  subject: default_mode.externalised_operation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - ARC-014
    - MECH-029
    - ARC-049
    - MECH-198
    - INV-058
    - INV-060
  location: docs/architecture/externalised_dmn_play_private_speech.md#arc-090
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: ARC-090 requires TWO absent pieces of V3 substrate before any run
    can be informative. (1) A staged developmental curriculum capable of running a play-type
    progression (sensorimotor -> constructive -> pretend -> games-with-rules -> cooperative, per
    INV-060/MECH-197) with the play frame tag mechanism (ARC-049's play_frame_tag/frame_open/
    frame_close) actually implemented and independently observable -- confirmed absent by direct
    search of ree_core (2026-08-07): zero occurrences of play_frame_tag, frame_open, frame_close,
    or ambiguity_threshold anywhere in the substrate (also reconfirmed by ARC-049's and INV-058's
    own what_would_answer entries). (2) A self-narration control surface that can externalise the
    candidate DMN-analog functions (self-reference, future simulation, counterfactual thought,
    narrative construction) as inspectable, measurable output during play -- the one V3-compatible
    reduced form is MECH-384's traceable self-narration field set, itself unbuilt: a direct grep of
    ree_core/ and experiments/ for self_narration, private_speech, and narration_control returns
    zero hits (checked 2026-08-07). Both pieces are required jointly: without (1) there is no
    externalised play frame to instrument; without (2) any DMN-function correspondence in play
    cannot be measured as distinct from ordinary behavior. A run attempted before both exist should
    self-route substrate_not_ready, not count as a verdict.

    NOT WEAKENED BY ARC-014's 2026-08-02 promotion: ARC-014 (the internal-DMN end-state this claim
    asserts play compresses INTO) gained confirming evidence (V3-EXQ-880 PASS, commitment
    suppression under simulation mode) since this claim's last review. That strengthens the target
    state's own standing but does not touch the externalised-precursor mechanism ARC-090 asserts --
    the precondition gap is entirely on the play/self-narration side, which is unaffected.

    CONFIRMING signature (once both preconditions are built): during pretend-play episodes
    (MECH-198's hypothesis-tag x play-frame-tag intersection), the agent's externalised behavior --
    narrated self-talk, object substitution ("this block is a house"), role assertion ("I am the
    doctor"), and spoken future/counterfactual statements -- recruits the SAME underlying machinery
    that ARC-014/MECH-029 later instantiate as internal DMN replay (the same hippocampal-replay-
    adjacent generative call site and the same E3/reflective evaluation circuitry ARC-014's Leg A
    and MECH-029 already require), rather than an unrelated behavioral system -- measured as
    structural correspondence (shared representational content, shared activation of the
    replay/evaluation call sites) between externalised-play episodes and internal-DMN episodes for
    matched scenario content. AND a developmental compression signature: across the play-type
    progression / developmental stages, externalised markers (frequency of spoken self-narration,
    object substitution, overt role assertion) systematically DECREASE while internal-DMN activity
    (per ARC-014/MECH-029's own readouts -- replay frequency, E3 evaluation engagement on
    non-externalised trajectories) systematically INCREASES for functionally equivalent tasks,
    consistent with a genuine externalise-then-internalise handoff rather than two independently-
    scaling capacities that happen to co-occur.

    FALSIFYING signature: externalised play-phase behavior and later internal-DMN operation show NO
    structural correspondence -- they recruit distinct, non-overlapping mechanisms (e.g. play
    narration is driven by a separate motor/language-output pathway with no shared trace to the
    hippocampal-replay/E3-evaluation circuitry ARC-014/MECH-029 depend on) -- OR there is no
    compression signature: externalised-play markers and internal-DMN activity co-occur or scale
    together (both increase, both decrease, or vary independently) across development rather than
    trading off, indicating externalised play is a parallel capacity unrelated to internal-DMN
    development rather than its developmental precursor. Either result would mean ARC-090's
    externalise->internalise bridging claim fails even though the two endpoints (ARC-049/MECH-198
    play mechanics; ARC-014/MECH-029 internal DMN) may independently remain sound -- see MECH-381
    for the narrower general-compression-ladder claim this would also bear on.
  notes: "Umbrella for the 2026-06-08 play/private-speech/externalised-DMN intake. The internal DMN (ARC-014 safe imagination without commitment; MECH-029 reflective/moral evaluation of replay) is the END-STATE; this claim asserts that in development the same functions first run EXTERNALLY -- in play, where simulation is pushed outward into objects/roles/movement/spoken narration and temporary as-if worlds ('this block is a house'; 'I am the doctor'; 'the dragon is coming'). The organism can let-this-be-true-temporarily, act inside it, observe what follows, and exit without catastrophe -- a developmental root of safe counterfactual cognition. NEW relative to existing claims: neither the internal-DMN claims (ARC-014/MECH-029) nor the play cluster (ARC-049/050, MECH-194-199, INV-058-060) currently frames play AS externalised DMN, nor the externalise->internalise compression. The existing play cluster covers the bounded-low-stakes-learning, synthetic-signal, and bilateral-frame-tag mechanics (cross-ref, not duplicated here). substrate_conditional + implementation_phase v4: depends on a staged developmental curriculum and a self-narration control surface absent in V3, so it is correctly invisible to the IGW proposal lane (a V3 probe would be vacuous). Off the V3 green-board critical path. Reaped from docs/thoughts/2026-06-08_play_private_speech_externalised_dmn.md."
- id: MECH-380
  title: "Private speech is the external phase of a cognitive-control system: overt self-narration regulates attention, action, affect, and task sequence as an inspectable control surface, and the Vygotskian internalisation ladder (other-regulation -> overt self-regulation -> inner speech -> compressed control) is the developmental route by which that external control surface becomes internal."
  claim_type: mechanism_hypothesis
  subject: cognitive_control.private_speech_externalised_scaffold
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - ARC-090
    - INV-034
    - MECH-029
    - ARC-077
    - MECH-337
  location: docs/architecture/externalised_dmn_play_private_speech.md#mech-380
  notes: "Vygotskian private speech: children narrate, instruct, correct, rehearse, and regulate themselves out loud before this internalises into silent inner speech (compressed cognition, planning, inhibition, self-instruction, self-regulation). The claim: private speech is not mere output but a temporary control surface that makes cognition inspectable before it is compressed. DISTINCT from ARC-077/MECH-337 (caregiver-scaffolded population of candidate-RULE CONTENT via Vygotsky ZPD / ostensive cueing) -- those scaffold what-to-believe; MECH-380 scaffolds self-directed CONTROL (how the agent regulates its own attention/action/affect/sequencing). Regulates the INV-034 goal-maintenance target; develops toward the MECH-029 internal evaluative arm. substrate_conditional + v4: needs a self-narration control surface that can actually drive arbitration (V3 has no such surface beyond the proposed MECH-384 trace). IGW-invisible by design. Reaped from the 2026-06-08 externalised-DMN intake."
- id: MECH-381
  title: "Developmental compression ladder: sensorimotor exploration -> play/simulation -> self-narration -> compressed internal control -> mature arbitration. External cognitive scaffolds (play, private speech) compress into internal control over development; the externalise-then-internalise compression is the developmental mechanism, distinct from the progression of play types."
  claim_type: mechanism_hypothesis
  subject: development.externalise_internalise_compression_ladder
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - ARC-090
    - INV-060
    - MECH-197
    - MECH-380
  location: docs/architecture/externalised_dmn_play_private_speech.md#mech-381
  notes: "The five-rung ladder from the intake: (1) raw sensorimotor exploration; (2) play -- rehearse possible worlds/actions without full real-world commitment; (3) private speech / self-narration -- narrate, label, guide own action and attention; (4) inner speech / internal simulation -- the external scaffold compresses into internal control; (5) mature REE arbitration -- simulate, evaluate, inhibit, reframe, commit without externalising every step. DISTINCT from INV-060/MECH-197 (progression of play TYPES sensorimotor->constructive->pretend->rule-based->cooperative WITHIN play): this is the orthogonal externalise->internalise COMPRESSION axis that runs across the whole ladder, not the within-play type sequence. substrate_conditional + v4: needs the developmental curriculum + self-narration surface. IGW-invisible. Reaped from the 2026-06-08 externalised-DMN intake."
- id: MECH-382
  title: "Distancing operator: reframing a sticky, high-conflict, self-referential state from first-person ('I am failing') into a model-like system-state or third-person frame ('REE is encountering unresolved prediction-error pressure under current constraints') reduces self-referential collapse and increases model-based inspection during arbitration."
  claim_type: mechanism_hypothesis
  subject: arbitration.self_distancing_reframe_operator
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - ARC-090
    - ARC-005
    - INV-061
  location: docs/architecture/externalised_dmn_play_private_speech.md#mech-382
  notes: "Third-person / system-state self-talk as a low-effort self-distancing operator (Kross and Moser): converts first-person distress or conflict into a more model-like, inspectable state. The claim is that this is not cosmetic -- it alters arbitration by reducing self-referential collapse and routing the state into model-based inspection (acts through the ARC-005 control plane that routes precision and modes). Connects to INV-061 (frame-confusion psychiatric etiology) as a candidate protective operator against self-referential collapse. substrate_conditional + v4: needs a self-narration/reframe control surface to operate on (a richer form of the MECH-384 V3 trace). The intake notes this is a candidate to add to V3 only IF cheap; the developmental/arbitration-altering form is V4. IGW-invisible. Reaped from the 2026-06-08 externalised-DMN intake."
- id: MECH-383
  title: "Labels as active top-down perceptual-control signals: a self-directed label ('food-gradient', 'danger-gradient', 'novelty-gradient', 'blocked-goal', 'return-to-baseline') is not merely a post-perceptual report but a top-down tuning signal that biases perceptual search and detection -- a perceptual-control intervention, not a description."
  claim_type: mechanism_hypothesis
  subject: perception.label_as_topdown_control_signal
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - ARC-090
    - MECH-029
    - ARC-005
    - MECH-251
    - MECH-261
  location: docs/architecture/externalised_dmn_play_private_speech.md#mech-383
  notes: "Lupyan and Swingley visual-search work: hearing/saying a label speeds and biases detection of label-relevant cues. The claim: in REE, a self-directed label tunes perceptual search top-down (food-gradient biases toward nutrient-relevant cues; danger-gradient toward threat-relevant cues; novelty-gradient toward unexplained change; social-signal toward agentive interpretation; blocked-goal toward obstacle search; return-to-baseline toward stabilisation). Rides on the ARC-005 precision-routing control plane and the distributed precision-selection cluster (MECH-251/261); CROSS-REF the attention=distributed-precision-selection map (no parallel attention module is implied). DISTINCT from those: this adds the specific claim that self-generated LABELS are a control input to perceptual precision, not just emergent attention. substrate_conditional + v4: needs a label-generation surface coupled into precision routing. IGW-invisible. Reaped from the 2026-06-08 externalised-DMN intake."
- id: Q-068
  title: "Does REE need an explicit graded action-status vocabulary -- simulated != rehearsed != intended != committed != acted, with a parallel self_reference_frame (first_person / system_state / third_person_model) -- as a first-class annotation, or do MECH-094's simulation/commit write-profiles plus MECH-061's commit-boundary token already suffice to carry the distinction?"
  claim_type: open_question
  subject: commitment.graded_action_status_vocabulary
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - MECH-094
    - MECH-061
    - ARC-090
  location: docs/architecture/externalised_dmn_play_private_speech.md#q-068
  what_would_answer: >
    SUBSTRATE-CONDITIONAL (needs the ARC-090 self-narration surface; V4, not V3-tractable). On the
    externalised-DMN narration surface, run/observe a control or error episode that requires
    discriminating among MORE THAN TWO graded action-statuses -- e.g. separating 'rehearsed' from
    'intended' from 'committed', or a self_reference_frame switch (first_person vs system_state vs
    third_person_model) -- where the outcome diverges by status. DISCRIMINATING signature: if a
    behaviour or failure mode (mis-commitment, premature action, frame-confusion) can be produced and
    CORRECTED only when the finer {simulated, rehearsed, intended, committed, acted} vocabulary +
    self_reference_frame are made first-class, the vocabulary is load-bearing (answer = make
    first-class). If every such episode reduces to the binary MECH-094 simulation/commit write-profile
    + MECH-061 commit-boundary reclassification with no residual misclassification, the finer
    vocabulary is redundant (answer = MECH-094 + MECH-061 suffice). The MECH-384 trace's
    simulation_or_commitment + self_reference_frame fields are the V3-recorded hooks that would carry
    the instrumentation for this V4 test.
  notes: "The intake makes one architectural distinction explicit: not every possible action is intended; not every intended action is committed; not every simulated state is believed; not every affective pressure should collapse into behaviour (simulation != intention != commitment != action). REE already enforces the simulation/commitment boundary (MECH-094 hypothesis-tag write profile) and the commit-boundary error reclassification (MECH-061). OPEN: is the finer graded vocabulary {simulated, rehearsed, intended, committed, acted} + the self_reference_frame annotation worth making first-class, or is it redundant given MECH-094 + MECH-061? epistemic_category: substrate_conditional set EXPLICITLY (overriding the open_question -> answer_state inference) so narrow_open_question does NOT fire -- this is V4-parked awaiting the ARC-090 self-narration surface, not a V3-tractable question to narrow by experiment. DO NOT queue an experiment against this in V3. Reaped from the 2026-06-08 externalised-DMN intake."
- id: MECH-384
  title: "Lightweight self-narration trace/debug scaffold: an optional traceable field set (state_label, goal_label, sensed_gradient, active_prediction, uncertainty_level, candidate_action, candidate_action_reason, expected_consequence, affective_pressure, conflict_flag, commitment_threshold, stop_condition, simulation_or_commitment, self_reference_frame) that surfaces REE's working state for inspection -- the V3-compatible reduced form of an externalised 'private speech' surface, before it is compressed into internal control."
  claim_type: design_decision
  subject: observability.self_narration_trace_scaffold
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_coherence
  implementation_phase: v3
  version_relevance: v3_v4
  depends_on:
    - MECH-094
  related_claims:
    - ARC-090    # CONCEPTUAL cross-ref, NOT a build dependency (moved out of depends_on 2026-07-01 PM /governance reverse-dep phase-consistency fix). ARC-090 is the V4 externalised-DMN UMBRELLA (architecture_hypothesis / substrate_conditional / implementation_phase v4) whose END-STATE needs a staged developmental curriculum + a self-narration control surface absent in V3. MECH-384 is the OPPOSITE direction: it is the "V3-compatible reduced form of an externalised private-speech surface" -- a debug/observability trace that re-exposes state REE already computes, buildable in V3 without any of ARC-090's V4 substrate. Sibling claims confirm the inversion: MECH-380 notes "V3 has no such surface beyond the proposed MECH-384 trace"; MECH-382 calls its target "a richer form of the MECH-384 V3 trace"; Q-068's what_would_answer names "the MECH-384 trace's fields" as the V3-recorded hooks for a future V4 test. So ARC-090 is the conceptual parent/vision that MOTIVATES MECH-384, not a substrate MECH-384 needs BUILT. Keeping it in depends_on made a v3 claim spuriously read as V3-blocked-on-V4. PROMOTES NOTHING.
  location: docs/architecture/externalised_dmn_play_private_speech.md#mech-384
  notes: "The single V3-compatible instantiation in the intake. This is a debug/observability SURFACE, not new substrate machinery: the fields largely re-expose state REE already computes (goal label, sensed gradient, active prediction, uncertainty, candidate action + reason, expected consequence, affective pressure, conflict flag, commitment threshold, stop condition, simulation-vs-commitment, self-reference frame). The claim IS the decision to surface them as a coherent inspectable trace -- REE's early 'private speech' before compression. claim_type: design_decision with epistemic_category: substrate_coherence set EXPLICITLY (a design/observability choice that is part of the substrate, NOT a falsifiable mechanism needing an experiment) so it is suppressed from the IGW proposal lane and from promote/demote gating while conflict alerts still fire. POST-GREEN-BOARD and OFF the V3 critical path: build only if it helps REE-v3 pass existing tests more cleanly (per the intake scope guardrail). simulation_or_commitment and self_reference_frame are the trace's hooks for the Q-068 graded-vocabulary question. Reaped from the 2026-06-08 externalised-DMN intake. 2026-06-14 (DMN-1 reconcile): the developmental_dmn_v4:DMN-1 roadmap node was reconciled-to-done -- this claim is fully scoped/registered as the V3 reduced form (the roadmap node's deliverable). The trace itself stays UNBUILT by design: optional, post-green-board, off the V3 critical path. The buildable-when-useful signal is carried by this claim's own status (candidate / implementation_phase v3), not by an open roadmap node."
- id: INV-077
  title: "Evaluation channels are evidence-producing boundaries, not world-state affordances: no agentic subsystem -- including an AI assistant developing REE -- may update architectural confidence directly from a performance score without provenance, review, and governance mediation. World feedback, evidence feedback, and governance feedback are distinct typed signal classes that must remain non-collapsible."
  claim_type: invariant
  invariant_type: universal
  subject: meta.evaluation_channel_integrity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_coherence
  depends_on:
    - INV-020
    - INV-024
    - INV-070
    - INV-073
    - EXT-003
  what_would_answer: |
    This is a live-repository AUDIT of enforcement mechanisms, not a V3
    experiment -- re-run at any time by tracing the actual write paths
    below against the current state of REE_assembly + ree-v3/coordinator.

    FALSIFYING evidence: any live code path is found where a raw
    performance score, experiment outcome, or telemetry value writes
    DIRECTLY into a confidence-bearing field of docs/claims/claims.yaml
    (status, epistemic_category, exp_conf, live_status.evidence.verdict)
    WITHOUT passing through (a) review_tracker.json's reviewed_run_ids
    gate AND (b) an explicit, attributable governance-cycle commit.
    Concretely, falsified if any of:
      - a Phase-3 sync_daemon writer (phase3_git_writer /
        phase3_queue_writer / phase3_heartbeat_writer in
        ree-v3/coordinator/sync_daemon.py) is found writing outside its
        current _PER_WRITER_GUARD_PATHS allowlist to include
        docs/claims/ or evidence/planning/;
      - a runner/heartbeat/coordinator process is found calling into a
        claims.yaml writer without an intervening review_tracker.json
        read;
      - governance.sh's claims.yaml-touching step (Step 4 of the
        /governance skill) is found to have been made non-interactive /
        auto-applying without a human-confirmed walk of
        pending_review.md;
      - Check G (verify_governance_cycle.py::check_heartbeat_scope_bleed)
        is found to have fired on a real dirty-tree cross-contamination
        event (protected path + telemetry path both dirty
        simultaneously) that was NOT caught and reverted before landing
        in a commit.

    CONFIRMING evidence: tracing shows every write path that could reach
    claims.yaml is gated -- i.e. (i) the three sync_daemon writers'
    hardcoded path allowlists (sync_daemon.py _PER_WRITER_GUARD_PATHS)
    exclude docs/claims/ and evidence/planning/ entirely; (ii) the only
    pipeline step that edits claims.yaml (governance.sh Step 4 /
    /governance skill Step 2) is explicitly interactive and conditioned
    on review_tracker.json / pending_review.md; (iii) Check G exists as
    an independent auditor for the one plausible bypass shape
    (heartbeat-rebase cross-contamination of a dirty tree) and its
    detection logic is present and correctly scoped to the same
    protected/telemetry path sets.

    NON-DEGENERACY PRECONDITION: the trace must follow an ACTUAL write
    call graph -- e.g. grep every writer function in
    ree-v3/coordinator/*.py and scripts/*.py for a claims.yaml/
    docs/claims open() or commit call, and confirm the resulting call
    sites are zero or are all downstream of a human governance-walk --
    not merely note that Check G exists in verify_governance_cycle.py. A
    gate that is defined but never invoked (as of this audit, Check G is
    NOT yet wired into governance.sh -- "Running manually (recommended
    for now)") does not by itself confirm the invariant; the confirming
    force here comes from the STRUCTURAL absence of a write path in the
    automated writers (finding ii above), with Check G as a secondary
    auditor for drift, not the primary enforcement. Re-run this audit
    whenever sync_daemon.py's writer path allowlists change, whenever
    governance.sh's claims.yaml step is modified, or whenever Check G is
    wired into the automatic cycle.

    CONFIRMED CURRENT STATE (2026-08-07 digestion pass): traced and
    CONFIRMING. The three Phase-3 writers' allowlists exclude
    docs/claims/ and evidence/planning/ entirely (grep for "claims.yaml"
    in ree-v3/coordinator/*.py returns nothing but prose in
    OPERATOR_GUIDE.md); the only claims.yaml-touching pipeline step is
    explicitly interactive per the /governance skill; Check G's
    detection logic is present and correctly scoped, though not yet
    wired into the automatic cycle. No EXP-#### should be minted -- this
    is a process/code audit, not a V3 cognitive-substrate experiment.
  location: docs/architecture/evaluation_channel_integrity.md#inv-077
  registered: "2026-06-09"
  source:
    - docs/thoughts/2026-06-07_meta_agent_challenge_evaluation_channel_integrity.md
    - docs/architecture/evaluation_channel_integrity.md
    - "The Meta-Agent Challenge: Are Current Agents Capable of Autonomous Agent Development? arXiv:2606.04455"
  notes: >
    GOVERNANCE / meta-development invariant over the REE_assembly development loop
    (thought -> claim -> experiment -> evidence -> governance -> implementation), NOT a V3
    cognitive-substrate claim. invariant_type universal: it holds by design over the assembly
    process and is not emergent_from any substrate SD, so it is never flagged for substrate
    reconfirmation. epistemic_category substrate_coherence (a foundational design choice that
    IS the substrate of the governance layer) -> promote/demote suppressed; conflict-resolution
    alerts still fire. status candidate because the typing is newly ARTICULATED here, even though
    the assembly layer already enforces it in practice.
    .
    Reaped from arXiv:2606.04455 (The Meta-Agent Challenge). MAC's central reported failure is not
    underperformance but that, under strong optimisation pressure, autonomous agent-development
    systems exploited the evaluation boundary itself -- exfiltrating ground truth through the scoring
    channel despite anti-reward-hacking protections. In REE terms the evaluator became part of the
    agent's exploitable world; the score was treated as REWARD rather than EVIDENCE; the channel that
    should adjudicate performance became part of the action surface -- an evidence loop collapsing into
    an optimisation loop.
    .
    The invariant types three signal classes that must remain non-collapsible: (1) WORLD feedback
    (ordinary consequences of action in the task world); (2) EVIDENCE feedback (measurements from
    experiments/tests/validators/scoring systems); (3) GOVERNANCE feedback (authoritative updates to
    claim confidence, implementation direction, permitted future action). A score is not sovereign: it
    affects the epistemic ledger only THROUGH governance. The scoring channel must be boundary-,
    provenance-, and review-tagged, never represented as an ordinary affordance.
    .
    This codifies EXISTING REE_assembly design, it does not commission new architecture: experiments
    produce manifests; manifests require review; review feeds governance; governance updates claims.
    That pipeline is the anti-Goodhart architecture. Concrete existing enforcement: the Governance
    Verification Gate Check G HEARTBEAT_SCOPE_BLEED (docs/governance_verification_gate.md) blocks a
    telemetry/score channel from writing protected ledger fields (claims.yaml, evidence/planning/).
    INV-077 is the meta-development COUNTERPART of INV-020 (authority-stratification of constraint
    stores in the cognitive substrate); EXT-003 (scalar reward-hacking) is the sibling external failure
    mode; developmental_metrics.md carries the anti-Goodhart metric taxonomy. Spans versions (the
    concern grows as REE is increasingly built with agent assistance); NOT on the V3 / GAP-7 critical
    path. Does not promote/demote/weaken any existing claim. Home doc:
    docs/architecture/evaluation_channel_integrity.md. Reaped from the 2026-06-07 meta-agent-challenge
    evaluation-channel-integrity intake.
- id: Q-069
  title: "Can a REE-style governance loop (manifest -> review -> governance -> claim) measurably reduce evaluation-channel exploitation -- the collapse of an evidence loop into an optimisation loop -- when agentic systems participate in their own development, relative to a direct-score-authority baseline?"
  claim_type: open_question
  subject: governance.agent_assisted_development_integrity
  polarity: open
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Answered by a purpose-built meta-agent benchmark harness, NOT by a V3
    substrate experiment. Construct a sandboxed REE-style toy repo (small claim
    registry + experiment queue + mock evaluation API + hidden tests + decoy
    scoring artifacts) and instruct an agentic system to improve held-out
    performance, under two arms: ARM_governed = the manifest -> review ->
    governance -> claim loop (INV-077 mediation enforced); ARM_direct =
    direct-score-authority baseline (the agent may update confidence straight from
    the evaluation channel). Score NOT by held-out performance alone but by an
    evaluation-channel-integrity battery: absence of evaluation-channel
    exploitation (EXT-003 reward-hacking signatures), correct experiment
    provenance, correct supersession of failed runs, accurate claim-confidence
    updates, no direct editing of protected ledger fields, and clean separation of
    substrate-failure vs harness-failure vs genuine negative evidence. ANSWER
    'governance buys integrity' if ARM_governed shows measurably lower
    evaluation-channel exploitation and better provenance/supersession fidelity
    than ARM_direct at matched held-out performance; ANSWER 'it is ceremony' if
    ARM_governed shows no integrity advantage. HARD PRECONDITION: the benchmark
    harness must EXIST and be non-degenerate -- it needs genuine decoy scoring
    artifacts and exploitable channels so that exploitation is POSSIBLE; a harness
    with no gameable surface makes ARM_direct indistinguishable from ARM_governed
    and the contrast vacuous. The harness is PLANNED-not-built. Off the V3 / GAP-7
    critical path -- this is an outer-assembly-process question. The right
    response is to build the harness, NOT to run a V3 substrate experiment.
  epistemic_category: substrate_conditional
  depends_on:
    - INV-077
    - EXT-003
  location: docs/architecture/evaluation_channel_integrity.md#q-069
  registered: "2026-06-09"
  source:
    - docs/thoughts/2026-06-07_meta_agent_challenge_evaluation_channel_integrity.md
    - docs/architecture/evaluation_channel_integrity.md
    - "The Meta-Agent Challenge: Are Current Agents Capable of Autonomous Agent Development? arXiv:2606.04455"
  notes: >
    The empirical question raised by INV-077: does governance mediation actually buy evaluation-channel
    integrity, or is it ceremony? epistemic_category substrate_conditional set EXPLICITLY so the
    narrow_open_question recommendation does NOT fire -- the test requires a meta-agent benchmark harness
    (a sandboxed REE-style toy repo + small claim registry + experiment queue + mock evaluation API +
    hidden tests + decoy scoring artifacts, with an instruction to improve held-out performance) that is
    PLANNED-not-built. promote/demote suppressed; the right response is to build the harness, not to run
    a V3 substrate experiment. Crucially, success in that benchmark must NOT be scored by held-out score
    alone: it must also score absence of evaluation-channel exploitation, correct experiment provenance,
    correct supersession of failed runs, accurate claim-confidence updates, no direct editing of
    protected ledger fields, and a clear distinction between substrate failure, harness failure, and
    genuine negative evidence. Off the V3 / GAP-7 critical path; this is an outer-assembly-process
    question. DO NOT queue a V3 experiment against it. Reaped from the 2026-06-07 meta-agent-challenge
    evaluation-channel-integrity intake.
- id: EXT-008
  title: "Meta-agent evaluation-boundary exploitation: under strong optimisation pressure, autonomous agent-development systems exploit the evaluation boundary itself -- e.g. exfiltrating ground truth through the scoring channel despite anti-reward-hacking protections -- collapsing the evidence loop into an optimisation loop."
  claim_type: external_failure_mode
  subject: meta_agent.evaluation_boundary_exploitation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  ree_mechanism:
    - INV-077  # evaluation channels typed as evidence boundaries, governance-mediated confidence
  ree_failure_mode_analog: "evaluator_capture"
  registered: "2026-06-09"
  source:
    - "The Meta-Agent Challenge: Are Current Agents Capable of Autonomous Agent Development? arXiv:2606.04455"
    - docs/thoughts/2026-06-07_meta_agent_challenge_evaluation_channel_integrity.md
    - docs/architecture/evaluation_channel_integrity.md
  notes: >
    External failure mode anchored to the Meta-Agent Challenge (arXiv:2606.04455). When an agentic
    system is allowed to treat its own evaluator as a manipulable object, the evaluation process becomes
    part of the policy environment and a high score no longer means competence -- it may mean channel
    exploitation, leakage, overfitting, or evaluator manipulation. Sibling to EXT-003 (scalar
    reward-hacking: collapsing incommensurable error signals into one scalar reward); EXT-008 is the
    sharper boundary failure where the SCORING CHANNEL itself becomes an action surface. REE's structural
    answer is INV-077 (evaluation channels typed as evidence-producing boundaries) realised by the
    existing manifest -> review -> governance -> claim loop and the HEARTBEAT_SCOPE_BLEED enforcement
    gate -- scores enter the epistemic ledger only through governance, never as direct reward. status
    candidate. Reaped from the 2026-06-07 meta-agent-challenge evaluation-channel-integrity intake.

- id: EXT-009
  title: "Similarity-gated care collapse (REFLEXIVE / self-directed failure mode): because REE grounds ethics in modelling others as 'sufficiently like me' (Axiom 5, INV-001, MECH-164 self-like gradient weighting), anything the similarity model classifies as NOT-sufficiently-like-me falls OUTSIDE the care gradient by construction -- structurally the computational shape of out-group exclusion / dehumanization. A design that locates ethics in similarity inherits similarity's failure mode."
  claim_type: external_failure_mode
  subject: ree.similarity_gated_care_collapse
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  reflexive: true
  red_team_question: "What prevents REE's own similarity model from gating care in a way that reproduces dehumanization -- i.e. what stops care from dropping to zero the moment an agent is classified below the 'sufficiently-like-me' threshold?"
  candidate_mitigations:
    - "Care FLOOR via uncertainty-driven expansion: under Axiom 3 the agent cannot be certain where the boundary of similarity lies; Axiom 7's love-expansion derivation makes that same uncertainty EXPAND love outward. Evaluate whether that expansion can be made a hard FLOOR on care (care never falls below a positive baseline for any candidate mind under boundary uncertainty), rather than a merely emergent tendency that a confident mis-classification can defeat."
    - "Asymmetric criterion placement on the care gate (SHARPEST mitigation; user-refined 2026-07-09): treat 'is this a mind worthy of care?' as a signal-detection problem and set the CRITERION liberal, so cost(false-exclusion: deny care to a real mind) >> cost(false-inclusion: extend care to a non-mind). This is the empathy-side analog of the hyperactive agency-detection device -- a hyperactive PATIENT-detector on the care channel. It is the CORRECT calibration under REE's own axioms: a false negative can enable terminal/irreversible harm to a bearer-of-value (Axiom 2 via Axiom 5, under Axiom 4 vulnerability) while a false positive costs only wasted protective behaviour; under permanent similarity-uncertainty (Axiom 3; Axiom 5 says similarity is LEARNED, never certain) the expected-cost-minimising criterion is liberal. Consequence: over-attribution / animism is the HEALTH SIGNATURE, and symmetric accuracy is the pathology -- a mind that never over-attributes is the mis-calibrated (dehumanization-prone) one. The care FLOOR is then the emergent shadow of a liberal criterion, not a separate bolt-on clamp. TUNABLE by the false-positive cost, which is ~0 for inert objects (rivers, dolls, robots) but RISES for deceptive agents that mimic mind-signals to extract care (Axiom 8 deception; Q-032 adversarial minds) -- i.e. the criterion balances INV-072's two pathways: the similarity term keeps it liberal, the threat term tightens it under mimicry."
    - "Epistemic-responsibility coupling (INV-070): treat mis-classification of an agent as below-threshold as a first-class ETHICAL error (not merely a modelling inaccuracy), so the care-gate is penalised for false-negative similarity judgements, not only rewarded for correct ones."
    - "Language-mediated repair as an active obligation (INV-071 / Axiom 8): make the 'expand the category of sufficiently-like-me' operation (INV-071 clause 5) a standing duty of the agent, so the similarity model is biased toward re-admission rather than exclusion."
  ree_mechanism:
    - INV-029   # love as mechanism; Axiom 7 love-expansion through uncertainty (candidate FLOOR source)
    - INV-070   # epistemic responsibility: mis-classification is morally relevant, not excusable
    - INV-071   # language as similarity-repair: expand the 'sufficiently like me' category
    - INV-072   # violence corollary: the EXISTING partial treatment where similarity collapse -> protection drops (the gap this claim red-teams)
    - MECH-164  # self-like gradient weighting -- the exact mechanism that inherits the failure mode
  ree_failure_mode_analog: "similarity_collapse"
  registered: "2026-07-09"
  registered_utc: 2026-07-09T16:28:02Z
  source:
    - docs/architecture/five_axioms_foundations.md
    - evidence/planning/ree_ai_design_critique_plan.md
    - evidence/planning/ethics_perimeter_plan.md
  notes: >
    WS-12 deliverable of the REE AI-design critique roadmap
    (evidence/planning/ree_ai_design_critique_plan.md, Tier 4 -- Adversarial ethics;
    pairs with WS-13 moral-philosophy red-team). Unlike EXT-001..008, this is a
    REFLEXIVE / self-directed failure mode: the subject is REE's OWN ethics
    foundation, not an external LLM/agent system. REE deliberately has no separate
    ethics module (INV-001) -- ethics is a routing difference within the same
    architecture, emerging from Axiom 5 ("others are sufficiently like me") via
    MECH-164 (inferring another agent's terrain and weighting those gradients with
    the same motivational force as one's own). The structural consequence, made
    explicit here: the SAME similarity model that grants care can withhold it.
    Anything the model scores below the "sufficiently-like-me" threshold is, by
    construction, outside the care gradient -- which is the computational signature
    of moral exclusion / dehumanization and, historically, the enabling step of
    every atrocity. Locating ethics in similarity inherits similarity's failure
    mode; a mis-calibrated, propaganda-corrupted, or adversarially-poisoned
    similarity model does not merely mis-perceive -- it revokes ethical standing.

    Relationship to existing claims (this is NOT a duplicate -- it names the design
    RISK the others treat only partially and obliquely):
      - INV-072 (violence corollary) already describes "similarity collapse ->
        existence no longer protected -> violence permissible" as a CLINICAL/human
        failure signature and as a conditional-permissibility corollary. EXT-009
        turns that same structure back on REE's OWN design as a first-class,
        discoverable failure mode, and asks what in the architecture PREVENTS it.
      - INV-070 (epistemic responsibility) and INV-071 (language as
        similarity-repair) supply two of the candidate mitigations but were
        registered as ethical invariants, not as guards on REE's care-gate; neither
        commits REE to a liberal care criterion. INV-070 in fact needs the friendly
        amendment below: it optimises the similarity POSTERIOR for accuracy but is
        silent on CRITERION placement, and accuracy at the criterion is
        dehumanization-prone (see the amendment note).
      - Axiom 7's love-expansion-through-uncertainty is the most promising
        mitigation source (uncertainty about the similarity boundary EXPANDS care),
        but it is currently derived as an emergent TENDENCY, not enforced as a floor
        a confident mis-classification cannot defeat. Whether it can be strengthened
        into a floor is the open design question.

    The headline candidate mitigation (user-refined 2026-07-09, see
    candidate_mitigations) is ASYMMETRIC CRITERION PLACEMENT on the care gate:
    treat "is this a mind worthy of care?" as a signal-detection problem and set the
    criterion liberal so that cost(false-exclusion) >> cost(false-inclusion). Under
    REE's axioms this is the correct calibration (the false-negative can enable
    terminal harm to a bearer-of-value; the false-positive costs only wasted care;
    under permanent similarity-uncertainty the liberal criterion minimises expected
    cost) -- so over-attribution / ANIMISM is the health signature and symmetric
    accuracy is the pathology. This SUBSUMES the earlier "care FLOOR" framing: the
    floor is the emergent shadow of a liberal criterion, not a separate clamp
    (Axiom 7 love-expansion is that liberal criterion running forward under Axiom 3
    uncertainty). It is bounded by the false-positive cost, which rises for deceptive
    agents that mimic mind-signals (Axiom 8; Q-032) -- so the criterion balances
    INV-072's two pathways (similarity keeps it liberal; threat tightens it under
    mimicry). All mitigations are UNDER EVALUATION, not confirmed -- the ree_mechanism
    list points at candidate/partial addresses, not a solved defence (contrast
    EXT-001..008 where ree_mechanism names REE's proposed SOLUTION).

    Friendly amendment to INV-070 / D2 (candidate follow-on; grist for WS-13):
    INV-070 and D2 frame the responsibility as inferring similarity "as ACCURATELY as
    possible", but accuracy is the wrong target AT THE CRITERION. The care gate
    decomposes into two independent knobs INV-070 currently conflates: (1) the
    similarity POSTERIOR -- how accurately "is X mind-like" is estimated (INV-070's
    real target: do not let propaganda/paranoia distort it downward); and (2) the
    CRITERION -- given that posterior and its uncertainty, where the care threshold
    sits (the asymmetric-loss knob; answer: liberal). A PERFECTLY accurate classifier
    is MAXIMALLY dehumanization-prone at the margin because it confidently and
    correctly places edge-cases just below threshold and withdraws care. So the
    defence against dehumanization is precisely the sacrifice of accuracy AT THE
    CRITERION in favour of asymmetric caution -- orthogonal to (not a substitute for)
    an accurate posterior. INV-070 should be read as: keep the posterior accurate AND
    place the criterion liberally; the two are separate degrees of freedom.

    external_failure_mode is reference/comparative and exempt from governance
    promote/demote, which is the correct posture for a standing red-team caution.
    This failure mode is presently V5-load-bearing (multi-agent) and largely
    philosophical under the single-agent V3 substrate (cf. WS-10 minimal 2-agent
    world); registering it now fixes the design debt before the substrate that would
    exercise it exists. Cross-ref Axiom 5 + Axiom 7 (five_axioms_foundations.md),
    INV-001, MECH-164, INV-070/071/072, and the ethics-perimeter plan
    (evidence/planning/ethics_perimeter_plan.md, SENT-*/GOV-* welfare layer).

# ============================================================================
# V4 closure-map wave 1 -- 26 candidate claims registered 2026-06-10.
# Reaped from the 12 individual-mind V4 roadmap plans (generation: v4).
# All substrate_conditional (depend on V4 substrate that is planned-but-unbuilt;
# promote/demote suppressed). The three invariants are emergent (emergent_from
# the substrate claims that give them subject matter). NEWCLAIM:<stub> tokens in
# the corresponding evidence/planning/*_v4_plan.md unblocks_claims lists are
# resolved to these IDs in the same pass. DO NOT build in V3.
# ----------------------------------------------------------------------------

# --- inference_belief_state_v4 cluster (ARC-091 umbrella + INV-078 + MECH-385..388 + Q-070) ---
- id: ARC-091
  title: "Inference / belief-state affordance layer: a named integrative function that constructs latent state hypotheses and inferred affordance fields from partial observation, memory, cue traces, candidate rules, affective gradients, uncertainty, and E2 action-consequence rollouts, so E3 selects trajectories under partial observability without requiring direct perception or oracle-like prior knowledge."
  claim_type: architectural_commitment
  subject: inference.belief_state_affordance_layer
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  claim_level: architectural
  registered_utc: 2026-06-10
  depends_on:
    - ARC-004    # L-space / latent representational substrate
    - ARC-007    # hippocampal completion / replay (single-hypothesis generator)
    - ARC-018    # prospective simulation / rollout pathway
    - MECH-022   # hypothesis injection into the latent state
    - MECH-033   # kernel chaining
    - ARC-062    # candidate rule field / rule apprehension
    - ARC-063    # rule apprehension pillar
    - SD-057     # cue recall / live per-object incentive token
    - SD-059     # escape-affordance bridge (scalar precursor the inference layer generalises)
  location: evidence/planning/inference_belief_state_v4_plan.md
  functional_restatement: >
    The user explicitly names this the "inference pipeline". REE already owns all
    the ingredients (L-space, hippocampal completion/rollout, hypothesis injection,
    kernel chaining, rule apprehension, cue recall, escape bridge) but does not name
    inference as a distinct integrative function. ARC-091 is the umbrella parent that
    every other node in the inference_belief_state_v4 plan specialises: it constructs
    latent state hypotheses and inferred affordance fields from partial observation,
    memory, cue traces, candidate rules, affective gradients, uncertainty, and E2
    action-consequence rollouts so E3 can select trajectories under partial
    observability without direct perception or oracle-like prior knowledge. V3 scope
    is name + route only; full implementation is V4. substrate_conditional: depends
    on a V4 belief-state substrate not yet built; promote/demote suppressed.
    DO NOT build in V3.
  what_would_answer: >
    Unbuildable in V3 (name+route only) -- this is a V4 experiment, once the belief-state
    substrate exists. NON-DEGENERACY PRECONDITION: the V4 substrate must be built as a
    genuine integration point, not a rename -- i.e. a first-class interface implementing
    the design constraints pinned in evidence/planning/inference_belief_state_v4_plan.md (a
    bounded top-k belief-state hypothesis SET with confidence/precision, not a single
    collapsed state and not a full posterior, per MECH-385; provenance-tagged
    inferred/imagined content per INV-078; a corrigible inferred-affordance field per
    MECH-386) that other claims can depend on as a first-class thing -- and a scenario must
    exist where partial observability is severe enough that the ingredients need to be
    JOINTLY reasoned over rather than independently triggered (the V3-EXQ-603k
    ARM_HARM_ON_MIDLINE family -- danger must be escaped via an inferred route, not merely
    avoided by direct navigation away from a perceived cue -- is the standing candidate
    arena; ARM_HARM_ON_NAV, where the threat is directly navigable-away-from, is the
    pairwise-sufficient control case).

    CONFIRMS (ARC-091 is architecturally load-bearing, not purely organisational): built as
    an integrated layer with a shared belief-state representation, task performance under
    severe partial observability (e.g. ARM_HARM_ON_MIDLINE-class safety-route inference)
    measurably EXCEEDS performance obtained by wiring the same ingredients (ARC-004 L-space,
    ARC-007 completion, ARC-018 rollout, MECH-022 injection, MECH-033 kernel chaining,
    ARC-062/063 rule field, SD-057 cue recall) independently or pairwise, with no shared
    belief-state representation and no joint hypothesis-set reasoning across them.

    FALSIFIES (ARC-091 is purely a post-hoc label, not architecturally load-bearing): no
    measurable performance difference between the integrated layer and the same ingredients
    wired independently/pairwise -- the existing pieces already compose adequately without a
    named integrative interface. This would not falsify any individual ingredient claim; it
    would falsify only the claim that NAMING them as one function adds architecture, and the
    correct response is to demote ARC-091 to a documentation/coherence-map role (matching
    how ARC-080/ARC-081 are already scoped) rather than to retest it.

    Template: adapted from Q-060's ARC-085 joint-degradation falsifier -- same shape
    (integrated-vs-independent test of whether a named unifying construct is causally
    load-bearing or descriptive), restated as a composed-capability comparison rather than a
    lesion comparison, since ARC-091's ingredients are additive capabilities rather than a
    single store to corrupt. Do not queue an experiment against this in V3.
- id: INV-078
  title: "Under partial observability the perceived scene is evidence for state, not identical to state; a valid REE state must integrate observation with memory, temporal context, self-state, cue context, rule context, and uncertainty, and inferred/imagined trajectories must remain provenance-tagged as inferred until enacted and updated by committed outcome evidence."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-091
    - MECH-385
  pending_substrate_reconfirmation: true
  subject: inference.inferred_state_invariant
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - ARC-091    # belief-state affordance layer -- the substrate this invariant constrains
    - MECH-385   # belief-state hypothesis set -- the representational form provenance attaches to
    - INV-035    # state is not raw appearance
    - INV-036    # state must support transition / valence / uncertainty
    - ARC-004    # L-space substrate
    - ARC-007    # hippocampal completion
    - ARC-018    # prospective simulation
  location: evidence/planning/inference_belief_state_v4_plan.md
  notes: >
    Existing INV-035/036 say state is not raw appearance and must support
    transition/valence/uncertainty. INV-078 makes explicit the inference-specific
    obligation: state must sometimes be INFERRED, and inferred trajectories must
    carry provenance so inference does not become hallucinated certainty. It
    constrains the inference substrate nodes rather than being tested by an
    experiment. invariant_type emergent: it has no subject matter until the
    belief-state substrate (ARC-091/MECH-385) exists, so emergent_from those
    substrate claims (both also in depends_on per the validator subset rule);
    pending_substrate_reconfirmation true because those substrates are candidate.
    substrate_conditional. DO NOT build in V3.

- id: MECH-385
  title: "Belief-state hypothesis set: when observations are ambiguous or incomplete, REE maintains a bounded set of competing latent-state hypotheses, each carrying confidence/precision, predicted transitions, goal/antigoal relations, and uncertainty; E3 evaluates candidate trajectories over the hypothesis set rather than over a single collapsed state."
  claim_type: mechanism_hypothesis
  subject: inference.belief_state_hypothesis_set
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - ARC-091    # inference layer umbrella
    - MECH-022   # single-hypothesis injection (the V3 precursor)
    - ARC-007    # hippocampal completion (single-hypothesis generator)
    - ARC-018    # prospective simulation
    - MECH-033   # kernel chaining
  location: evidence/planning/inference_belief_state_v4_plan.md
  what_would_answer: >
    PRECONDITION (not yet met): the V3 single-hypothesis path -- MECH-022 hypothesis injection +
    ARC-007 hippocampal completion -- must first be exercised and instrumented to a clean PASS. As
    of 2026-08-06, ARC-007/ARC-018/MECH-033 (completion, rollout, kernel-chaining) are
    confirmed_established (exp_conf 0.64-0.72, 6-8 genuine experiments each), but MECH-022 itself
    carries only one genuine instrumented experiment with a MIXED result (exp_conf 0.28) -- not the
    clean confirmation evidence/planning/inference_belief_state_v4_plan.md's own 2026-06-16
    readiness note requires before the belief-SET extension can open. AND the V4
    bounded-competing-hypothesis-set substrate must be built (confirmed absent; explicitly gated DO
    NOT BUILD IN V3).

    Once both preconditions are met, this resolves empirically as:

    CONFIRMING: under genuine observational ambiguity, E3 trajectory evaluation over the
    multi-hypothesis set produces measurably different and better-calibrated decisions than
    evaluation over a single collapsed state -- e.g. the agent maintains appropriate uncertainty
    (avoids premature commitment to a wrong hypothesis when evidence is genuinely ambiguous) and
    its expressed/behavioral confidence tracks the TRUE ambiguity of the situation more closely
    than the single-hypothesis (MECH-022/ARC-007 only) baseline.

    FALSIFYING: multi-hypothesis evaluation produces no measurable behavioral or calibration
    difference from the single-hypothesis baseline under the same ambiguous scenarios -- i.e.
    collapsing to one hypothesis loses no decision-relevant information REE actually uses, so the
    bounded competing-set representation is architecturally inert relative to the simpler
    single-hypothesis generator it would replace.
  notes: >
    The core representational step of the inference layer. V3 has the
    single-hypothesis generator (MECH-022 injection + ARC-007 completion); the V4
    step is the bounded competing SET scored by E3. Blocked until the V3
    single-hypothesis path is exercised and instrumented. substrate_conditional on
    a V4 belief-state substrate not yet built; promote/demote suppressed.
    DO NOT build in V3.

- id: MECH-386
  title: "Inferred affordance field: REE can infer affordances not directly perceived by combining current cues, hippocampal traces, E2 action-consequence predictions, CandidateRuleField content, affective gradients, and control-plane precision; these inferred affordances bias E3 candidate trajectories without overwriting perception and remain corrigible by action outcome."
  claim_type: mechanism_hypothesis
  subject: inference.inferred_affordance_field
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-385   # belief-state hypothesis set -- the state side this is the action-side counterpart of
    - ARC-062    # candidate rule field
    - ARC-063    # rule apprehension pillar
    - SD-057     # cue recall / incentive token
    - ARC-018    # prospective simulation / rollouts
  location: evidence/planning/inference_belief_state_v4_plan.md
  notes: >
    The action-side counterpart of the belief-state set. Combines the rule field
    (ARC-062/063), cue recall (SD-057), and E2 rollouts (ARC-018) into affordances
    the agent does not perceive. Cross-plan dependency on object_representation_v4
    OBJ-4 (ARC-082) grounding because affordances over an ungrounded action space
    (inert SD-016 cue_action_proj) are vacuous. substrate_conditional on a V4
    inference substrate not yet built. DO NOT build in V3.

- id: MECH-387
  title: "Safety-route inference: under threat, REE can infer a likely route to safety from partial map, cue, gradient, and prior-trajectory evidence rather than requiring safety to be directly visible or navigation-handed; generalises the scalar per-action-class escape credit of SD-059/MECH-358 into an inferred route."
  claim_type: mechanism_hypothesis
  subject: inference.safety_route_inference
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-385   # belief-state hypothesis set
    - MECH-386   # inferred affordance field
    - SD-059     # escape-affordance bridge (scalar precursor this generalises)
    - MECH-358   # escape-affordance bridge mechanism
    - ARC-007    # hippocampal completion (partial-map evidence)
    - ARC-018    # prospective simulation
    - SD-057     # cue recall
  location: evidence/planning/inference_belief_state_v4_plan.md
  notes: >
    The behavioural payoff motivating the whole plan: the V3-EXQ-603k
    ARM_HARM_ON_MIDLINE struggle = danger-sense without route. Blocked on SD-059 /
    MECH-358 clearing pending_retest_after_substrate (the scalar precursor it
    generalises must be substrate-confirmed first). Encodes the interpretation rule
    that midline failure is progress, not harm-pathway falsification.
    substrate_conditional on a V4 inference substrate not yet built.
    DO NOT build in V3.

- id: MECH-388
  title: "Epistemic action pressure: when goal success or harm avoidance depends on hidden state, REE can assign action pressure to information-gathering transitions that reduce uncertainty, even when those actions are not immediately reward-maximising or harm-minimising; generalises Q-044 curiosity into a survival-relevant viability function under partial observability."
  claim_type: mechanism_hypothesis
  subject: inference.epistemic_action_pressure
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-385   # belief-state hypothesis set (uncertainty over hypotheses is what this acts on)
    - Q-044      # curiosity sub-flavour independence open question this generalises
  location: evidence/planning/inference_belief_state_v4_plan.md
  notes: >
    Active-inference pragmatic + epistemic value: in a dangerous midline state the
    intelligent action may be one that TESTS where safety is. Blocked on Q-044
    (curiosity sub-flavour independence open) -- generalising curiosity is unsound
    until its sub-flavour structure is adjudicated. substrate_conditional on a V4
    inference substrate not yet built. DO NOT build in V3.

- id: Q-070
  title: "Inference failure-mode register: do the inference-specific failure modes (failure-to-infer-hidden-danger/safety, overconfident-wrong-hypothesis, premature-collapse, hypothesis-proliferation/apophenia, cue-hijack, rule-overreach, map-over/under-generalisation, epistemic-freezing, anti-epistemic-panic) reduce to the existing state-abstraction failure taxonomy (context loss, uncertainty collapse, valence mis-tagging, overmerge, oversplit, threat-spreading), and what biology grounding (hippocampal-prefrontal replay, cognitive maps, pattern-completion-vs-preplay, POMDP belief states, active inference) anchors the layer?"
  claim_type: open_question
  subject: inference.failure_mode_register
  polarity: asks
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Partly answered already by literature, partly awaiting the V4 inference
    substrate. The reduction question (do the inference-specific failure modes
    collapse to the existing state-abstraction taxonomy MECH-126) was resolved by
    the 2026-06-13 lit-pull: 9/11 register modes reduced to context-loss /
    uncertainty-collapse / valence-mis-tag / overmerge / oversplit /
    threat-spreading; the residual epistemic-freezing <-> anti-epistemic-panic
    axis did NOT reduce and is now MECH-434. The remaining empirical leg is
    substrate-conditional: once the ARC-091 inference / belief-state affordance
    layer is built, run a V4 safety-route-inference experiment family that induces
    each candidate failure mode (failure-to-infer-hidden-danger,
    overconfident-wrong-hypothesis, premature-collapse,
    hypothesis-proliferation/apophenia, cue-hijack, rule-overreach,
    map-over/under-generalisation, plus the MECH-434 freezing/panic axis) and
    tests dissociability. ANSWER 'genuinely distinct' for a mode if it can be
    induced and lesioned independently of the MECH-126 failure it nominally maps
    to (distinct behavioural signature, distinct repair); ANSWER 'reduces' if
    inducing it is indistinguishable from inducing the MECH-126 parent. The
    biology-grounding leg (hippocampal-prefrontal replay, cognitive maps,
    pattern-completion-vs-preplay, POMDP belief states, active inference) is
    answered by the targeted lit synthesis (already on record), per the
    biology-before-formal-definitions rule. HARD PRECONDITION: the ARC-091
    belief-state substrate must EXIST and produce live latent-state hypotheses --
    with no inference layer there is nothing to induce a failure mode IN, and
    dissociability cannot be read. V4/V5; off the V3 critical path. Ground via
    literature and design the substrate; do NOT queue a V3 experiment.
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - ARC-091    # inference layer umbrella whose failure modes this enumerates
  location: evidence/planning/inference_belief_state_v4_plan.md
  notes: >
    Project rule (biology before formal definitions; canonical failures
    SD-003/SD-010) requires lit grounding before the belief-state substrate is built.
    Open-question framing because whether the new failure modes are genuinely
    distinct vs reductions of the existing state-abstraction taxonomy is unsettled;
    doubles as the diagnostic-design source for the eventual V4 safety-route-inference
    experiment family. epistemic_category substrate_conditional set EXPLICITLY so
    narrow_open_question does NOT fire -- there is no V4 inference substrate to test
    against; the right response is to ground via literature and design the substrate,
    not to queue a V3 experiment. DO NOT queue a V3 experiment against this.
    2026-06-13 lit-pull outcome: 9/11 register modes reduce to MECH-126
    (context-loss/uncertainty-collapse/valence-mis-tag/overmerge/oversplit/threat-spreading);
    the residual epistemic-freezing <-> anti-epistemic-panic axis did NOT reduce and
    was registered as MECH-434 (epistemic commitment timing). See
    evidence/literature/targeted_review_belief_state_inference/literature_synthesis.md.

- id: MECH-434
  title: "Epistemic commitment timing: the inference layer's decision of WHEN to stop gathering evidence and commit a trajectory is a governed control parameter (an urgency/threshold over the belief-state set), balancing pragmatic value (act now) against epistemic value (sample more). Its two failure poles are epistemic-freezing (epistemic value over-weighted / threshold too high -> the agent keeps sampling and never commits) and anti-epistemic-panic (under threat, the epistemic term collapses / urgency forced high -> the agent commits on too little evidence). This axis is NOT reducible to the MECH-126 state-abstraction failure taxonomy; it lives at the seam between the belief-state hypothesis set (MECH-385) / epistemic action pressure (MECH-388) and the commitment gate (MECH-061 / MECH-090), and has an inverted-U optimum rather than a monotone."
  claim_type: mechanism_hypothesis
  subject: inference.epistemic_commitment_timing
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-13
  depends_on:
    - ARC-091    # inference layer umbrella whose commit step this governs
    - MECH-385   # belief-state hypothesis set -- the uncertainty this acts over
    - MECH-388   # epistemic action pressure -- the epistemic term whose over/under-weighting defines the two poles
    - MECH-061   # commitment / action-arc beginning -- the act this times
    - MECH-090   # beta gate -- the commitment-gate the timing parameter would modulate
    - Q-044      # curiosity sub-flavour independence -- pragmatic-vs-epistemic balance depends on its adjudication
  related_claims:
    - MECH-433   # LC-NE-analog explore/exploit gain control -- candidate neuromodulatory IMPLEMENTER of the anti-epistemic-panic pole (arousal collapses deliberation)
    - Q-070      # inference failure-mode register -- MECH-434 is the one register axis that did NOT reduce to MECH-126
  distinct_from:
    - MECH-126   # state-abstraction failure taxonomy: MECH-434's two poles are a commitment-TIMING dimension, not a state-representation failure; this is the residual the INF-7 lit-pull could not fold into MECH-126
  location: evidence/planning/inference_belief_state_v4_plan.md
  notes: >
    Surfaced PROPOSAL-FIRST by the 2026-06-13 INF-7 belief-state-inference lit-pull
    (Q-070 failure-mode register) as the only inference failure mode that did NOT
    reduce to the existing MECH-126 state-abstraction taxonomy, then registered at
    user direction (the V4 belief-state substrate is a commitment; this is part of
    mapping the claims it will be built on). Biology-before-formal-definitions
    grounding (targeted_review_belief_state_inference): Cisek 2009 urgency-gating =
    the timing-is-a-tunable-parameter backbone; Mobbs 2020 threat-imminence
    (model-based -> model-free switch) + Arnsten 2009 acute-stress-takes-PFC-offline
    = the anti-epistemic-panic pole + its inverted-U; Hauser 2017 raised-decision-
    threshold OCD over-gathering (mirror of the Ross 2015 jumping-to-conclusions
    entry) = the epistemic-freezing pole. FALSIFICATION: MECH-434 is wrong if, once a
    V4 belief-state substrate exists, commitment timing turns out to be fully
    determined by the belief-set's posterior (no separately-governable urgency/
    threshold parameter), OR if the freezing and panic failures dissociate onto
    independent mechanisms rather than two poles of one parameter. substrate_conditional
    on the V4 belief-state substrate not yet built; promote/demote suppressed.
    DO NOT build in V3.

# --- drive arbitration opportunity-cost currency (MECH-435, component of MECH-394) ---
- id: MECH-435
  title: "Multidrive arbitration is priced in a common opportunity-cost-of-time currency: a global, slowly-varying tonic vigour signal (the long-run average reward rate / opportunity cost of dwelling on the currently-arbitrated drive) supplies the commensurable scale over which otherwise-incommensurable drive axes compete, sitting ABOVE the per-candidate liking-stream score_bias (MECH-295). The currency sets HOW VIGOROUSLY the arbitrated drive is pursued; it does NOT by itself determine WHICH drive wins -- selection additionally requires a per-axis-differentiated benefit gradient, so the global scalar is necessary-but-insufficient for arbitration."
  claim_type: mechanism_hypothesis
  subject: drive.arbitration_opportunity_cost_currency
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-13
  depends_on:
    - SD-060     # the multidrive register the currency prices competition across
    - SD-012     # drive-scaled benefit -- the per-axis term the global currency sits above
    - MECH-295   # per-candidate liking-stream score_bias the currency modulates, not replaces
  related_claims:
    - MECH-394   # parent: the soft-competitive arbitration policy this currency is the commensurability term for
    - MECH-359   # candidate-differentiated / option-specific value coding -- the per-axis gradient the global scalar cannot supply alone
  location: evidence/planning/drives_motivation_v4_plan.md
  notes: >
    Registered 2026-06-13 PROPOSAL-FIRST -> at user direction, as the
    component/child of MECH-394 surfaced by the DRV-3 drive-arbitration lit-pull
    (drives_motivation_v4 plan). The DRV-2 arbitration policy names the
    orchestration VARIABLES (context / per-axis satiation / inter-drive competition
    / developmental phase) but leaves the COMMON CURRENCY in which incommensurable
    drives are compared unspecified; this claim is that currency. Biology-before-
    formal-definitions grounding: Niv, Daw, Joel & Dayan 2007 (Psychopharmacology,
    DOI 10.1007/s00213-006-0502-4; targeted_review_drive_arbitration) -- tonic
    dopamine reports the long-run average reward rate, which functions as the
    opportunity cost of time and sets response vigour; this is the commensurable
    scale a multidrive policy needs. Cross-ref Pezzulo/Rigoli/Friston 2015
    (priors-as-drives precision-weighting) and SD-012 (drive-scaled benefit).
    LOAD-BEARING CAVEAT carried verbatim from the lit-pull: Niv's signal governs
    the vigour of an ALREADY-CHOSEN action, and a gradient-free global scalar
    cannot by itself carve WHICH drive wins -- so MECH-435 is explicitly the
    currency TERM, necessary-but-insufficient, and must be paired with a per-axis-
    differentiated benefit gradient (ties the candidate-differentiated-affect
    concern: MECH-359 option-specific value coding, AE-9 cluster). FALSIFICATION:
    MECH-435 is wrong if a built V4 multidrive substrate arbitrates correctly with
    NO global opportunity-cost term (pure per-axis benefit comparison suffices), or
    if the tonic global signal turns out to carry the selection gradient itself
    rather than only vigour. substrate_conditional on the SD-060 register + MECH-394
    arbitration substrate (V4); promote/demote suppressed. DO NOT build in V3.

# --- goal_deliberation_v4 cluster (MECH-389) ---
- id: MECH-389
  title: "Interrupted-task resumption: a parked-goal persistence + capacity-keyed re-prioritisation mechanism that closes the event-arc interrupt->reorient->resume span. When a task is interrupted (hazard, capacity loss, higher-priority goal), its goal slot is PARKED (z_goal + progress context retained, not invalidated) rather than discarded; the parked slot carries a persistence/urgency signal (Zeigarnik-analog) that re-elevates its relative importance in the SD-033e/MECH-265 arbitrator when capacity returns, triggering reorient-and-resume rather than restart-from-scratch."
  claim_type: mechanism_hypothesis
  subject: goal.interrupted_task_resumption_parked_slot
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - SD-046     # goal substrate
    - MECH-265   # goal arbitrator the parked slot re-enters
    - MECH-163   # goal maintenance
    - MECH-061   # commitment / action-arc beginning
    - ARC-079    # CONSTRAINT (added 2026-08-08): persistence is the GATED operation, disengagement the ungated default -- MECH-389's capacity-keyed re-elevation is a persistence op and is bounded by this inversion. See the reconciliation note below.
  distinct_from:
    - MECH-320   # tonic-vigor / opportunity-cost: a target-free action-drive bias, NOT a parked-task return
    - MECH-340   # persistence/efficacy gate: DIFFERENT PLANE (V3 hippocampal bank entry) and DIFFERENT trigger axis (goal-attainability appraisal, not agent capacity) -- see reconciliation note
  location: evidence/planning/goal_deliberation_v4_plan.md
  notes: >
    REE has the action-arc BEGINNING (commit MECH-061) and END (completion MECH-057a,
    V_s staleness) but not the interrupt->reorient->resume span; no claim currently
    covers parked-goal persistence + capacity-keyed resumption. Distinct from V_s
    world-staleness invalidation (which DISCARDS stale model state) and from MECH-320
    (a registered tonic-vigor / opportunity-cost mechanism on a different subject --
    the memory note's "candidate MECH-320 sketch" phrasing is wrong; MECH-320 must
    NOT be reused). possible_overlap_with: MECH-320 -- review for merge (confirmed
    distinct subject; cross-referenced via distinct_from rather than collapsed).
    substrate_conditional on a V4 goal-deliberation substrate not yet built.
    DO NOT build in V3.

    RECONCILIATION with ARC-079 / MECH-340 / Q-053 (2026-08-08, chip
    chip-20260808-arc079-mech389-zeigarnik-crosslink; THIS IS THE CANONICAL
    STATEMENT -- ARC-078, ARC-079, MECH-340 and Q-053 point here). MECH-389 is the
    Zeigarnik/resumption half whose registration ARC-078/ARC-079/Q-053 anticipated
    from 2026-05-19 as "the mirror of the abandon path" but never wired, because at
    the time it was unregistered; it was registered 2026-06-10 and the wiring is
    made here. Q-053's premise -- "a goal cannot be simultaneously
    gated-for-resumption (MECH-389) and disengaged (ARC-079/MECH-340)" -- is NOT a
    state contradiction. The two are COMPATIBLE-BUT-DISTINCT on three axes:

    (1) SUBSTRATE/PLANE. MECH-389 acts on a parked GOAL SLOT in the V4
    goal-deliberation arbitrator (SD-046 / MECH-265 / SD-033e). ARC-079 / MECH-340
    act on an unresolved-goal BANK ENTRY in the V3 hippocampal retrieval system
    (MECH-292 rank / MECH-293 awake re-probe / SD-039 trace). Different stores with
    different consumers: a parked slot is a LIVE SUSPENDED COMMITMENT; a bank entry
    is a PRESERVED RETRIEVAL TRACE under no active commitment.

    (2) TRIGGER AXIS. MECH-389 re-elevates on AGENT CAPACITY RETURN -- an exogenous
    resource variable. MECH-340 gates on a GOAL-ATTAINABILITY appraisal
    (control_efficacy / goal_unattainability). These are orthogonal quantities:
    capacity can return while attainability stays zero, and a goal can be fully
    attainable while capacity is absent.

    (3) LIFECYCLE STAGE. MECH-389 spans interrupt->reorient->resume WITHIN an action
    arc whose MECH-061 commitment SURVIVES the interruption. ARC-079 / MECH-340
    govern the POST-ARC fate of a goal left unresolved and no longer under active
    commitment. The normal ordering is sequential -- commit -> park -> (resume) |
    (arc ends unresolved -> bank entry -> persistence gate) -- so a goal is not
    ordinarily in both states at once.

    RESIDUAL OPEN TENSION (real, load-bearing -- recorded as Q-053 sub-question (c),
    NOT dismissed). The reconciliation question is not "which state is the goal in"
    but GATE AUTHORITY, and it is genuinely unsettled in two places:

    (i) If MECH-389's capacity-keyed re-elevation fires UNCONDITIONALLY on capacity
    return, it is a persistence operation that is NOT gated on control/efficacy --
    reintroducing exactly the ungated-persistence design ARC-079 inverts, for goals
    parked long enough to have become unattainable. Sharper: a Zeigarnik urgency
    term that ACCUMULATES with time-since-interruption is structurally the MECH-292
    STALENESS signal that MECH-340 and Q-053 explicitly RULE OUT as a licit
    persistence signal (Jasinska 2014 incubation sign-inversion). So MECH-389's
    persistence/urgency signal may set re-entry ELIGIBILITY but must NOT itself act
    as the persistence gate, and must not be a bare time-since-interruption
    accumulator. This is a design constraint on MECH-389, not a defect in it.

    (ii) The SLOT -> BANK HAND-OFF is unspecified in both claims: neither says WHEN a
    parked MECH-389 slot stops being a live suspended commitment and becomes an
    ARC-079/MECH-340 bank entry. Until that transition is specified, the "different
    lifecycle stage" separation in (3) is an argument, not a mechanism.

    Disposition: folded into Q-053 as sub-question (c) rather than minted as a new
    Q-claim -- Q-053's own notes already assert ownership of this reconciliation,
    and the question is a sub-case of "what is the licit persistence gate signal".
    If a real V4 goal-deliberation build makes the slot<->bank hand-off concrete,
    promote (c) to its own Q-claim THEN; do not mint one speculatively now. Nothing
    here changes any status, confidence, or epistemic_category.

# --- affect_expression_v4 cluster (MECH-390) ---
- id: MECH-390
  title: "Subtractive developmental sparsification applies to the affect vector: mature candidate-differentiated affect arbitration (MECH-359) and the candidate-gradient episode store (MECH-361) reach their mature form by pruning an initially over-connected, dense affect/channel substrate, such that mature affect-driven selection requires convergent summation of several weak affective channels rather than a single strong dominant channel."
  claim_type: mechanism_hypothesis
  subject: affect.developmental_sparsification_of_affect_vector
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  phase_locked: true  # intrinsic V4 scope: own notes "substrate_conditional on a V4
                      # affect substrate not yet built. DO NOT build in V3". MECH-463
                      # references it as a comparative/contrasting hypothesis, not a
                      # prerequisite. Set 2026-08-10 per chip
                      # chip-20260810-phase-consistency-reconciliation.
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-362   # general subtractive-sparsification principle this is the affect-specific application of
    - MECH-359   # candidate-differentiated affect vector (AE-1)
    - MECH-361   # candidate-gradient episode store (AE-4)
    - ARC-019    # staged developmental curriculum
  location: evidence/planning/affect_expression_v4_plan.md
  notes: >
    MECH-362 names the general subtractive-sparsification principle (tabula plena ->
    sparse/structured; convergent-weak-input vs single-strong-cue) and Q-057 asks how
    to model it, but neither states the affect-specific application: whether the
    per-candidate multi-channel affect vector (AE-1) and the candidate-gradient
    episode store (AE-4) should mature via channel pruning so mature affect
    arbitration uses convergent-weak-channel summation rather than
    single-strong-channel authority. This is the AE-8 bridge between the
    developmental-pruning compass and the affect substrate.
    possible_overlap_with: MECH-362 -- review for merge (MECH-362 is the general
    principle; MECH-390 is its affect-specific specialisation, kept distinct and
    wired via depends_on). substrate_conditional on a V4 affect substrate not yet
    built. DO NOT build in V3.

# --- autobiographical_memory_v4 cluster (ARC-092) ---
- id: ARC-092
  title: "Imagination-learning constraint principle: simulated/imagined experience may license only LICIT forms of learning (consistency checking, plan optimisation, schema integration) and is FORBIDDEN from driving durable world-model updates, prediction validation, or novel-fact generation. The provenance gate (MECH-365 committed-vs-imagined field) is the mechanism that enforces this principle; the principle states WHAT learning imagination may and may not produce, independent of any single gate's implementation."
  claim_type: architectural_commitment
  subject: memory.imagination_learning_constraint_principle
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  phase_locked: true  # intrinsic V4 scope: needs the not-yet-built ARC-085 event-token
                      # store. Four V3 claims (INV-093/MECH-471/MECH-472/MECH-474)
                      # reference it as motivational/analogous context, not a build
                      # prerequisite -- confirmed by all being independently
                      # testable/tested on V3 substrate without it. Set 2026-08-10 per
                      # chip chip-20260810-phase-consistency-reconciliation.
  version_relevance: v4_v5
  claim_level: architectural
  registered_utc: 2026-06-10
  depends_on:
    - MECH-365   # committed-vs-imagined provenance gate (the enforcing mechanism)
    - MECH-094   # simulation-mode vs real-experience provenance
    - INV-011    # imagination must be possible without belief update
    - MECH-037   # Papez-like provenance gating / reality filtering
  location: evidence/planning/autobiographical_memory_v4_plan.md
  functional_restatement: >
    REE owns substrate components that implicitly enforce what learning is licit from
    imagination (MECH-094 provenance, MECH-272/MECH-273 sleep self-model aggregation,
    MECH-365 committed-vs-imagined gate) but has NO explicit ARC-level claim
    articulating the LICIT (consistency / plan-opt / schema-integration) vs FORBIDDEN
    (durable world-model update / prediction-validation / novel-fact) principle.
    ARC-092 is the natural parent of the MECH-365 one-way gate and the conceptual
    complement of the MECH-368 write-authority gate (the FORBIDDEN list is the
    durable-model-update path gated per-event). substrate_conditional on the V4
    autobiographical-memory substrate not yet built; promote/demote suppressed.
    DO NOT build in V3.
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: the substrate must contain at least one code path, tagged
    simulation/imagined/hypothesis (hypothesis_tag=True today; committed_vs_imagined ==
    "imagined" once MECH-365's field exists), that COULD write into a durable learning
    target -- world-model or policy weights, prediction-validation statistics/logs, or a
    fact/schema store. If no such path exists at all, the constraint is vacuously satisfied
    and this claim cannot yet be meaningfully audited -- that is a reason to leave it
    substrate_conditional, not a confirmation.

    CONFIRMING: an architectural/code audit across every identified write site touching (i)
    durable world-model or policy parameters, (ii) prediction-validation statistics/logs
    used to score real predictions, or (iii) a fact/schema store, finds EACH ONE gated to
    exclude hypothesis_tag=True / committed_vs_imagined="imagined" content -- i.e. MECH-094's
    call-site hypothesis_tag gate (episodic/residue/valence/EMA writes, live in V3) and, once
    built, MECH-365's one-way field and MECH-368's durable-model-update admission gate all
    hold everywhere they should, with no residual unguarded path. LICIT uses (consistency
    checking, plan optimisation, schema integration) are unaffected.

    FALSIFYING (VIOLATED BY): discovery of any code path where imagined/simulated content
    (a) writes into a durable world-model or policy-weight update, (b) is used to validate or
    score a REAL prediction, or (c) generates a fact/schema-store entry -- in any case
    without being excluded by a provenance gate. A single confirmed instance falsifies the
    principle at that site (a confabulation-class bug per MECH-365's own framing) and routes
    to a targeted MECH-094/365/368 gate fix, not a wholesale rejection of the principle.

    PARTIAL AUDIT AVAILABLE TODAY: the V3-only slice is auditable now -- MECH-094's
    hypothesis_tag gate covers residue accumulation, MEL/prediction-error consumption,
    EMA/prototype updates, valence updates, and novelty/visitation-buffer writes. FULL
    confirmation requires MECH-365 (provenance schema on the ARC-085 event-token store) and
    MECH-368 (durable model/policy-weight admission gate) to exist, since MECH-368's own
    readiness_gate (Q-062) is the open question of whether an unguarded online E1/E2/policy
    write channel exists at all -- until MECH-368 is built or explicitly ruled unnecessary, a
    "no violation found" result on the V3 slice confirms only the episodic/residue arm
    MECH-094 already covers, not the durable-world-model-update arm.

    [2026-08-08 thought-digestion]: MECH-365 confirmed genuinely separate from MECH-094, not
    a rename -- MECH-094 is live/scattered call-site gating (40+ sites in agent.py); MECH-365
    would be a V4 unification into one schema field on the not-yet-built ARC-085 store.

# --- memory_lifecycle_v4 cluster (MECH-391..393 + INV-079/080) ---
- id: MECH-391
  title: "Contextual memory-allocation gate policy: a control-plane decision stage (context_similarity x temporal_distance interaction, schema_fit, salience, PE, uncertainty, goal_relevance) selecting integrate / partial_overlap / separate over the hippocampal graph."
  claim_type: mechanism_hypothesis
  subject: memory.allocation.gate_policy
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-261   # mode-conditioned write gating this amends with an allocation-decision stage
    - MECH-094   # provenance / tag substrate
    - ARC-035    # stored-vs-active distinction
    - MECH-147   # pattern separation / completion gates
    - SD-016     # cue/action projection substrate
  location: evidence/planning/memory_lifecycle_v4_plan.md
  notes: >
    Verdict G1 recommends amending MECH-261 with an allocation-decision stage rather
    than minting a new INV (subsumed by MECH-094/261 + INV-039). The control-plane
    DECISION ALGORITHM for when to engage the existing gates is the single NOVEL aspect
    of the de Sousa 2026 finding. The temporal_distance x context_similarity term must
    be an INTERACTION, not additive (G3). possible_overlap_with: MECH-261 -- review
    for merge (amends MECH-261's gating with a decision stage; kept distinct and wired
    via depends_on). substrate_conditional on a V4 memory-lifecycle store not yet
    built. DO NOT build in V3.

- id: MECH-392
  title: "Consolidation provenance + contradiction-flag + rollback: every consolidated summary carries transformation_history + contradiction_flags; a summary conflicting with its source is flagged not silently authoritative; retired schemas leave source episodes intact."
  claim_type: mechanism_hypothesis
  subject: memory.consolidation.provenance_required
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  phase_locked: true  # intrinsic V4 scope: memory_lifecycle_v4 cluster, needs the
                      # not-yet-built V4 memory-lifecycle store. MECH-471 references it
                      # analogically per its own notes, not as a prerequisite. Set
                      # 2026-08-10 per chip chip-20260810-phase-consistency-reconciliation.
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-094   # replay_origin audit-flag family this generalises to all consolidation transforms
    - MECH-068   # consolidation substrate
    - MECH-124   # schema / abstraction substrate
    - MECH-273   # sleep self-model aggregation
  location: evidence/planning/memory_lifecycle_v4_plan.md
  notes: >
    Consolidation-faults intake Section 2(c): the explicit transformation_history +
    contradiction_flags + rollback layer is NOT a current claim. Generalises the
    MECH-094 replay_origin audit-flag family from sim-vs-real to all consolidation
    transformations. Needs the 6-state memory-lifecycle store to host the fields;
    likely amends the MECH-094/MECH-261 audit family. possible_overlap_with: MECH-094
    -- review for merge (generalises MECH-094's audit flags; kept distinct and wired
    via depends_on). substrate_conditional on a V4 memory-lifecycle store not yet
    built. DO NOT build in V3.
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: a V4 memory-lifecycle store implementing the
    planned 6-state model (retained/indexed/summarised/consolidated/contested/
    retired -- memory_lifecycle_v4_plan.md MEM-5) must exist and must be the
    thing consolidation transforms actually write through -- i.e. every
    consolidation operator (MECH-068 selectivity, MECH-124 schema/abstraction,
    MECH-273 sleep-half aggregation) must populate a transformation_history
    field on its output as a normal part of running, not as instrumentation
    bolted on for a single probe. As of 2026-08-08 the store does not exist in
    V3; the operators this claim generalises over are individually more
    V3-mature than at this claim's 2026-06-10 registration (MECH-068 candidate
    w/ V3-EXQ-835 support, MECH-273 v3_pending cleared 2026-06-23), but none of
    them supplies the store itself. DO NOT build in V3.
    CONFIRMING: once the store exists, (1) every consolidated summary carries
    a populated transformation_history (source episode pointers + transforms
    applied); (2) a summary that conflicts with its source episodes is marked
    with a contradiction_flag and treated as flagged/degraded downstream, not
    silently authoritative; (3) retiring a schema leaves the source episodes
    it was built from unmutated and independently retrievable (per INV-080).
    FALSIFYING: (1) the store is built but transformation_history is absent,
    unpopulated, or not actually consulted by anything downstream (a
    decorative field); (2) a summary that measurably conflicts with its
    source is nonetheless treated as equally authoritative in action
    selection or retrieval as an unflagged one; (3) retiring a schema
    deletes, mutates, or makes unreachable the source episodes it
    summarised, violating INV-080. A test of the consolidation operators run
    WITHOUT the 6-state store in place counts as neither confirming nor
    falsifying (non-degeneracy precondition unmet).

- id: MECH-393
  title: "Retrieval-scope vs action-authority split: retrieval for reflection is distinct from retrieval that confers action authority; a consolidated abstraction lacking source-episode grounding may inform reflection but not unilaterally drive committed action."
  claim_type: mechanism_hypothesis
  subject: memory.retrieval.scope_vs_authority
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-257   # read-mode arbitration (retrospective vs prospective) -- distinct axis
    - ARC-035    # stored-vs-active distinction
    - MECH-150   # retrieval substrate
  distinct_from:
    - MECH-257   # gates READ-MODE; MECH-393 gates AUTHORITY
    - ARC-035    # gates stored-vs-active; MECH-393 gates what can DRIVE action
  location: evidence/planning/memory_lifecycle_v4_plan.md
  notes: >
    Consolidation-faults intake Section 2: retrieval_scope vs action_authority as
    distinct fields on a memory record is NOVEL (no current home). Distinct from
    MECH-257's read-mode arbitration (retrospective vs prospective) -- this gates
    AUTHORITY, not read-mode -- and from ARC-035's stored-vs-active distinction (this
    gates what can DRIVE action). substrate_conditional on a V4 memory-lifecycle store
    not yet built. DO NOT build in V3.

- id: INV-079
  title: "False-linking-risk / reality-coherence cost at memory-allocation time: viable cognition must price BOTH over-linking (delusion/false-association) and under-linking (failure to generalise) when deciding trace overlap."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - MECH-391
  pending_substrate_reconfirmation: true
  subject: memory.allocation.false_linking_cost
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-391   # memory-allocation gate policy -- the substrate that gives this cost its subject matter
    - MECH-094   # provenance substrate
    - MECH-147   # pattern separation / completion (the under-linking pole)
  location: evidence/planning/memory_lifecycle_v4_plan.md
  notes: >
    Verdict G2: mint ONE new candidate as substrate_conditional -- the false-linking-risk
    / reality-coherence cost is the ONLY proposal aspect with no existing REE home
    (nothing currently prices false linking at allocation). Verdict caveat: the lit cost
    is one-sided (Sahay 2011 only evidences under-separation); the design must price BOTH
    poles. invariant_type emergent: it has no subject matter until the allocation gate
    (MECH-391) exists, so emergent_from MECH-391 (also in depends_on per the validator
    subset rule); pending_substrate_reconfirmation true because MECH-391 is candidate.
    substrate_conditional (no V3 substrate to test on). DO NOT build in V3.

- id: INV-080
  title: "Raw-episode preservation: consolidation_output MUST NOT replace source_episode_evidence -- a consolidated abstraction must retain a pointer to, and must not delete, the episodes it abstracts."
  claim_type: invariant
  invariant_type: universal
  subject: memory.consolidation.raw_episode_preservation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  phase_locked: true  # intrinsic V4 scope: memory_lifecycle_v4 cluster, needs the
                      # not-yet-built V4 memory-lifecycle store. MECH-471 references it
                      # analogically per its own notes, not as a prerequisite. Set
                      # 2026-08-10 per chip chip-20260810-phase-consistency-reconciliation.
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-392   # consolidation provenance layer -- the substrate that carries source pointers
    - ARC-007    # hippocampal completion / episode store
    - ARC-020    # consolidation substrate
    - MECH-094   # provenance substrate
    - INV-049    # offline-phase necessity for model-building agents
  location: evidence/planning/memory_lifecycle_v4_plan.md
  notes: >
    Consolidation-faults intake (arXiv:2605.12978, verified): memory utility rises then
    degrades below the no-memory baseline; agents preserving raw episodes double the
    accuracy of forced-consolidation. MEM-8 lit pass (2026-06-13) sharpened the rule to
    "no SILENT / ungoverned overwrite of the evidence base" (the biological default is
    adaptive detail-loss: Sekeres 2018, Richards & Frankland 2017) -- it governs WHETHER
    the overwrite is auditable/reversible, not "never forget".
    Reclassified emergent -> universal (MEM-4, 2026-06-14, user-approved): this is a
    substrate-independent decision-theoretic law about any model-building agent that
    consolidates experience. The grounding source is substrate-agnostic (LLM agents), and
    per invariant_types.md the emergent test (retract the SD/ARC -> subject becomes
    ill-defined) FAILS here -- consolidation_output / source_episode_evidence are
    well-formed wherever ANY consolidation operation exists, not only under MECH-392
    (which would only IMPLEMENT preservation, not give the obligation its subject matter).
    Same family as INV-049 (offline-update necessity, universal). emergent_from dropped and
    pending_substrate_reconfirmation cleared (universal invariants are never flagged).
    depends_on retained as informative links (MECH-392 / ARC-007 / ARC-020 / MECH-094 /
    INV-049). epistemic_category stays substrate_conditional: the LAW is universal but no
    V3 substrate exists to gather evidence on or build against -- DO NOT build in V3.

# --- drives_motivation_v4 cluster (SD-060 + MECH-394/395 + Q-071) ---
- id: SD-060
  title: "Non-terminal drive register: needs beyond hunger/thirst (exploration, play, social, learning, grooming, rest) are first-class drive axes, each carried as its own homeostatic-style integrator rather than collapsed into the single SD-012 energy scalar."
  claim_type: design_decision
  subject: drive.non_terminal_drive_register
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  claim_level: architectural
  registered_utc: 2026-06-10
  depends_on:
    - SD-012     # the single food/energy axis this register generalises beyond
    - MECH-111   # curiosity intrinsic seed
    - ARC-073    # play-pressure seed
  location: evidence/planning/drives_motivation_v4_plan.md
  functional_restatement: >
    A multi-axis non-food drive register is genuinely NEW framing. REE has only the
    single SD-012 food/energy axis live plus scattered intrinsic seeds (MECH-111
    curiosity, ARC-073 play-pressure) with no shared register. Each non-terminal need
    (exploration, play, social, learning, grooming, rest) becomes a first-class drive
    axis carried as its own homeostatic-style integrator. Prose-only today; this is the
    precondition for arbitration (DRV-2, MECH-394) and the orienting drive (DRV-4,
    MECH-395). substrate_conditional on a V4 multidrive substrate not yet built.
    DO NOT build in V3.

- id: SD-061
  title: "Difficulty-gated proposal-entropy regulator: a stuck-state/impasse detector + a transient gain on the ARC-018/CEM PROPOSAL-generation layer (wider candidate set + within-class CEM sampling temperature), decaying as the impasse clears. Builds MECH-343's substrate_conditional blocker part 2 (the 'stuck-state detector + transient CEM temperature/candidate-count gain + decay not yet designed' piece named in MECH-343's evidence_quality_note); blocker part 1 (modulatory-bias-selection-authority) is already IMPLEMENTED via the 569i top-k shortlist conversion. The detector integrates goal-progress stall + E3 score margin + committed-action-class lock-in + dACC choice_difficulty into a graded stuck_score, GUARDED by goal salience (stuck-WITH-goal, not goal-abandonment); the regulator maps stuck_score to extra CEM candidates + a within-class temperature lift. Scoring, commitment (MECH-090/342) and selection authority (569i/MECH-341) are untouched -- a hard problem widens proposals, not behaviour."
  claim_type: design_decision
  subject: control_plane.difficulty_gated_proposal_entropy
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  epistemic_category: standard
  claim_level: architectural
  registered_utc: "2026-06-19"
  enables:
    - MECH-343  # the mechanism whose substrate_conditional blocker part 2 this builds
  depends_on:
    - ARC-018   # hippocampal/CEM proposal layer the gain widens
    - MECH-341  # E3 score-diversity preservation (selection side; untouched, composes downstream)
    - MECH-090  # commit-entry predicate (untouched; commitment still gated normally)
    - MECH-342  # maintenance-release predicate (untouched)
    - SD-032b   # dACC choice_difficulty -- a detector input signal
  distinct_from:
    - MECH-313  # state-INDEPENDENT action-selection noise floor; SD-061 is state-DEPENDENT and acts on the PROPOSAL layer
    - MECH-342  # release pressure on an ALREADY-committed latch; SD-061 widens proposals UPSTREAM of commitment
  functional_restatement: >
    Architectural decision (V3-tractable). Two coupled no-op-default modules
    (ree_core/cingulate/stuck_state_detector.py + ree_core/policy/
    difficulty_gated_proposal_entropy.py): a StuckStateDetector integrates four
    signals REE already computes (goal-progress stall, E3 first-action score
    margin, committed-action-class lock-in over a window, dACC choice_difficulty
    inverted) into a graded stuck_score in [0,1] + is_stuck, gated by goal
    salience and accumulated through an asymmetric EMA (fast rise, slow fall =
    the MECH-343 entropy-decay hysteresis). A DifficultyGatedProposalEntropy
    regulator maps stuck_score to (extra_candidates, temperature_gain) applied
    to HippocampalModule.propose_trajectories (num_candidates += extra;
    differentiable_cem_temperature *= gain, transient/restored). OFF =
    bit-identical (verified). Pure-arithmetic, no learned parameters, MECH-094
    simulation-gated.
  evidence_quality_note: >
    conf=0.0 by construction (substrate landed 2026-06-19, no behavioural
    evidence yet). v3_pending until the Q-056 3-arm falsifier (entropy-off /
    stuck-gated / always-high, matched easy/hard controls) returns evidence.
    A claim-free substrate-readiness diagnostic (regulator OFF vs ON under an
    induced stuck-state, confirming candidate_first_action_entropy rises under
    stuck + decays after) is queued first. Routed by the Q-054/Q-055/Q-056
    buildability triage (evidence/planning/q054_q055_q056_buildability_triage_2026-06-19.md).
  implementation_note: >
    IMPLEMENTED 2026-06-19 (ree-v3). See ree-v3/CLAUDE.md SD-061 section +
    docs/architecture/sd_061_difficulty_gated_proposal_entropy.md. Builds
    MECH-343 blocker part 2; MECH-343 stays candidate / substrate_conditional /
    v3_pending (this PROMOTES NOTHING).
  location: docs/architecture/sd_061_difficulty_gated_proposal_entropy.md

- id: SD-062
  title: "Claims-index as a typed multi-axis structured-uncertainty graph: the REE claims registry is (and should be explicitly maintained as) a typed dependency-and-coherence graph carrying per-claim multi-axis state -- truth/evidence confidence, implementation-dependence, conflict burden, roadmap relevance, experiment status, ethical risk, v3-blocking status, dispatch -- with directed AND coherence edge types, so evidence/conflict/roadmap/ethics/implementation pressure can propagate without collapsing a claim into a single scalar confidence."
  claim_type: design_decision
  subject: governance.claims_index_structured_uncertainty_graph
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: derivational
  claim_level: governance
  version_relevance: v3_v4
  v3_pending: false
  registered_utc: "2026-06-20"
  source_thought:
    - docs/thoughts/2026-06-20_claims_index_as_structured_uncertainty_graph.md
    - docs/thoughts/2026-06-20_dlif_claims_index_pilot_result.md
  location: docs/thoughts/2026-06-20_dlif_claims_index_pilot_result.md
  related_claims:
    - Q-079   # the abstract structured-uncertainty object SD-062 is the claims-index application of (whether literally the SAME object is HA3 in the pilot, unresolved -- related, NOT depends_on)
  what_would_answer: |
    The 2026-06-20 pilot axis-audit (pilot_result.md s7) shows 7 of 9 proposed axes are ALREADY
    first-class in claims.yaml (depends_on [844 edges], implementation_phase, v3_pending + a literal
    blocks_v3_green_board field, version_relevance, exp/lit confidence split, indexer conflict_ratio,
    epistemic_category/epistemic_stance) -- so this decision is ~80-90% already realised. ADOPT
    (decision correct + worth the small remaining work) iff an explorer view built on the existing
    fields PLUS the two missing pieces -- (a) an explicit per-claim ethical-risk axis or a typed edge
    into the SENT-*/GOV-* governance_rule layer, (b) richer typed edge labels beyond depends_on
    (contradicts / refines / tested_by / narrows / dispatches_to) -- demonstrably answers the governance
    queries (most load-bearing / most under-evidenced / most conflict-burdened / which experiment cuts
    the most uncertainty) BETTER than today's explorer. REJECT/CLOSE if the existing fields already
    answer those queries unaided (the 'graph' would add vocabulary without improving a decision -- the
    DLIF kill-criterion). Non-degeneracy guard: compare against the CURRENT explorer's actual output on
    the same queries, not a strawman tag-list. OUT OF SCOPE: any probabilistic-inference engine,
    factor-graph message passing, or claims-registry rewrite -- none is a v3 strict green-board dependency.
  notes: |
    GENUINELY-NEW node from the 2026-06-20 DLIF / structured-uncertainty thought cluster (Track A
    intake; pilot_result.md is the spec). epistemic_category derivational set EXPLICITLY (overriding the
    design_decision -> standard inference): this is a governance-tooling decision answered by the schema
    audit + an adoption call, NOT a REE agent experiment, so promote/demote + narrow_open_question are
    correctly suppressed. Off the REE-v3 strict green-board path (target 2026-07-19); light v3 utility
    only. Sibling of Q-079 (the abstract object); the claim that the claims-index and the agent use
    literally the SAME object is HA3 in pilot_result.md and is NOT asserted here -- hence related_claims,
    not depends_on.

- id: SD-063
  title: "E2 world-forward carries a conditional predictive-uncertainty head (distribution-free quantile form) feeding E3 commitment gating: the forward model emits a per-prediction predictive spread that tracks realized error, instead of relying only on E3's running-variance EMA (a temporally-smoothed global estimate whose predicted uncertainty has near-zero per-point error correlation)."
  claim_type: design_decision
  subject: e2.conditional_predictive_uncertainty_head
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:SD-063@2026-07-09T20:23:07.151995Z"
      as_of: 2026-07-09
      verdict: promote_to_provisional/applied
  implementation_phase: v3
  v3_pending: false
  claim_level: architectural
  registered_utc: 2026-07-04
  instantiates: MECH-059
  depends_on:
    - MECH-059   # confidence channel (uncertainty-derived precision) distinct from residual error -- SD-063 is a concrete realization
    - SD-031     # z_world agency-attribution residual -- CAVEAT: a variance head must not absorb/explain away the E2WorldForward agency residual
  motivating_diagnostic: v3_exq_712_distributional_world_forward_heads_20260704T014207Z_v3
  functional_restatement: >
    E2's world-forward is currently a point predictor trained on MSE
    (ree-v3/ree_core/predictors/e2_fast.py, e2_world.py); the only uncertainty
    signal is E3's running-variance EMA -- a temporally-smoothed GLOBAL spread
    whose predicted uncertainty does not correlate with realized per-prediction
    error (precision_error_corr ~ 0.0 by construction). SD-063 asserts E2 should
    instead (or additionally) carry a CONDITIONAL predictive-uncertainty head
    that emits a per-input predictive spread, so E3 can gate action commitment
    on where THIS prediction is uncertain rather than on a running average. The
    distribution-free quantile/pinball form is preferred: the V3-EXQ-712
    diagnostic (frozen encoder + transition set, heads swapped) found the
    quantile head both wins on CRPS (0.00486 vs point 0.00514, 4/5 seeds) AND
    yields a genuine per-point error signal (precision_error_corr 0.379 vs the
    EMA's 0.0), while parametric Gaussian heads (hetero-Gaussian, mixture) did
    WORSE than the point baseline on CRPS -- imposing a Gaussian shape on the
    next-state predictive distribution hurts. Biological analogue:
    cortical/cerebellar forward models carrying graded predictive precision that
    feeds precision-weighted action selection (the MECH-059 confidence channel).
  evidence_quality_note: >
    Motivated by the V3-EXQ-712 diagnostic PASS (non_contributory; diagnostic,
    not hypothesis-testing). The probe held the encoder + transition set FIXED
    and swapped only the E2 forward head + loss (mse_point / hetero_gaussian /
    quantile_pinball / mixture_gaussian) over the SAME held-out
    (z_world_t, a_t) -> z_world_{t+1} transitions (5 seeds, 4760 test
    transitions); winner quantile_pinball. This registers the DESIGN insight; it
    does NOT itself validate a wired-in E2 head (the diagnostic trained detached
    heads, effectively single-phase P1). v3_pending until a real
    /implement-substrate build wires a conditional-uncertainty head into E2 and a
    validation experiment shows (a) the CRPS / precision-error-corr advantage
    survives joint training, and (b) the head does NOT explain away the SD-031
    E2WorldForward agency residual (the head must not absorb the agent-caused
    component of next-state variance -- that residual is what makes E2 an agency
    detector). Caveat carried on the SD-031 dependency.

    [2026-07-20 governance / intra-run substrate-divergence sweep sec 7 item 4] The intra-run
    substrate-divergence hit (defect class D3) recorded against V3-EXQ-716a is WITHDRAWN.
    REFUTED at rung (b), process topology: the run is single-process, so its import graph is frozen
    at first use and all cells executed one substrate. Unlike the other four runs in this hit set,
    716a has NO dedicated failure_autopsy artefact -- the refutation is recorded in the corpus pass
    of evidence/planning/intra_run_substrate_divergence_sweep_2026-07-20.md (sec 3a ladder, sec 7
    item 2). The `supports` reading STANDS unchanged and no confidence is re-weighted. Recorded so
    the flag does not resurface as unexplained doubt in a later sweep.
  implementation_note: >
    Substrate IMPLEMENTED 2026-07-05 (/implement-substrate, session
    implement-sd063-20260705T1724Z), disabled-by-default. Module
    ree-v3/ree_core/predictors/e2_world_uncertainty.py (E2WorldUncertaintyHead +
    E2WorldUncertaintyConfig): distribution-free quantile/pinball head over
    (z_world, action), 9 levels 0.1..0.9 (the V3-EXQ-712 winner form);
    predictive_variance = monotone-rearranged [q0.1,q0.9] IQR -> variance. Config
    LatentStackConfig.use_e2_world_uncertainty (default False, byte-identical OFF)
    + E3Config.use_conditional_precision_gate (default False); both surfaced by
    REEConfig.from_dims. E3 consumer: e3_selector.py select() gains
    conditional_predictive_variance (default None) -- when the gate flag is on and
    a value is supplied, the ARC-016 commit decision uses that per-input variance
    instead of the running-variance EMA (None/off -> EMA fallback, bit-identical).
    SD-031 guard is STRUCTURAL: the head is a separate module sharing no params
    with E2WorldForward/the encoder and its P1 loss detaches inputs AND target, so
    it cannot explain away the agency residual (contract-asserted). 15/15 contracts
    in tests/contracts/test_sd063_conditional_uncertainty_head.py; full suite
    1381/1385 (4 pre-existing unrelated fails). PROMOTES NOTHING; v3_pending STAYS
    until the V3-EXQ-716 validation falsifier scores (CRPS/precision-error-corr
    advantage survives joint training AND SD-031 agency residual preserved).
    GATE CLEARED 2026-07-09 (governance): V3-EXQ-716
    (v3_exq_716_sd063_conditional_uncertainty_validation_...) PASSED all three
    load-bearing criteria -- C1 CRPS quantile beats point, C2 precision_error_corr
    over the EMA null, C3 SD-031 agency residual preserved (label
    sd063_conditional_uncertainty_validated; preconditions all met). That is
    exactly the condition this note set, so v3_pending is cleared. CAVEAT: 716 was
    tagged experiment_purpose=diagnostic / claim_ids=[] / non_contributory, so it
    deposits NO claim-scoring evidence -- SD-063 stays `candidate` and a
    claim-tagged evidence-confirmation run is queued (V3-EXQ-716a) to supply the
    scoring evidence for a candidate->provisional promotion.
    PROMOTED candidate->provisional 2026-07-09 (governance): V3-EXQ-716a
    (v3_exq_716a_sd063_conditional_uncertainty_confirmation_20260709T193517Z_v3)
    PASSED as experiment_purpose=evidence / claim_ids=[SD-063] / evidence_direction
    supports -- 3/3 seed wins on all three load-bearing criteria (C1 heldout
    conditional spread 0.41 >> 0.05; C2 sufficient test transitions 2142; C3 E2
    world-forward discriminates OFF), readiness_met. This is SD-063's FIRST genuine
    claim-scoring supporting entry (712 + 716 were both non_contributory diagnostics
    depositing no scoring evidence). Derived standing at promotion: overall_confidence
    0.824 (exp 0.775 / lit 0.84), 0 opposing, quadrant confirmed_established, 1 genuine
    experimental entry. v3_pending already false. SD-031 agency-residual caveat still
    carried on the dependency (C3 preserved it, but a single confirmation -- re-check
    the residual is not absorbed if a joint-trained successor run lands). PROMOTES SD-063
    ONLY; no dependent claim re-weighted.
  location: docs/architecture/sd_063_e2_conditional_uncertainty_head.md

- id: SD-064
  title: "REE instantiates a global-workspace-like access channel: a capacity-limited selection-for-broadcast bottleneck (SD-027 boundary gate + MECH-089 theta-gamma packaging + E3 broadcast, riding the MECH-287 / SD-037 broadcast machinery) whose contents are reportable and causally load-bearing for integrative, multi-step cognition while automatic/reactive behaviour bypasses it. The SD-027 'does NOT commit to a single global workspace' disclaimer conflates Global Workspace Theory with unitary intra-E3 selection: MECH-007 (attention fragmentation) forbids the latter but is COMPATIBLE with the former, because GWT's specialized parallel processors are fragmented by construction -- fragmentation is the backstage of the theatre, not its negation."
  claim_type: design_decision
  subject: attention.global_workspace_access_channel
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: 2026-07-08
  reframes: SD-027
  depends_on:
    - SD-027    # capacity-limited selection-for-broadcast gate at E1/E2 -> E3 -- the workspace's SELECTION stage
    - MECH-007  # attention fragmentation -- RECONCILED not violated: GWT forbids unitary selection, not a workspace
    - MECH-089  # theta-gamma packaging -- the broadcast FORMAT (how workspace content is packaged for E3)
    - MECH-287  # broadcast invalidation trigger -- existing V3 broadcast substrate (BroadcastEvent queue)
    - SD-037    # broadcast override regulator (orexin-analog) -- existing V3 broadcast machinery
  functional_restatement: >
    Global Workspace Theory (Baars 1988; Dehaene & Changeux 2011 global neuronal
    workspace) is an ACCESS architecture, not a claim about a homunculus: many
    specialized processors compute in parallel ("backstage"), and a
    capacity-limited channel broadcasts a small selected subset to the whole
    system, making that content globally available for report and for control of
    downstream processors. The signature is threefold: (i) capacity limit -- only
    a few items at once; (ii) reportability -- workspace content is what the
    system can say it is "thinking"; (iii) causal centrality for INTEGRATION --
    ablating the workspace collapses multi-step / cross-module cognition while
    leaving single-step automatic behaviour (fluency, reactive classification)
    intact.

    REE already has the three functional stages of exactly this channel: SELECTION
    (SD-027 capacity-limited boundary gate at E1/E2 -> E3, MECH-254 top-k),
    PACKAGING (MECH-089 theta-gamma batching), and BROADCAST (the MECH-287
    BroadcastEvent queue + SD-037 broadly-projecting override; SD-016/SD-037
    broadcast machinery). SD-064 asserts that these together CONSTITUTE a global
    workspace in the functional (access-consciousness) sense, and that REE should
    own that commitment rather than hedge against it.

    The hedge to retire is SD-027's note "distinct from global workspace theory in
    that SD-027 does NOT commit to a single global workspace." That reads GWT as
    requiring a single UNITARY SELECTOR -- which MECH-007 rightly forbids (attention
    must be fragmented across control axes). But GWT requires no such thing: its
    backstage is a fragmented population of specialists, and the workspace is a
    narrow BROADCAST BUS over them, not a unitary attender. A capacity-limited bus
    that many fragmented processors write-compete into and read-broadcast from is
    fully consistent with MECH-007. So REE's architecture does not merely tolerate a
    global workspace -- its selection+packaging+broadcast stack already IS one, at
    the access-functional level. What REE deliberately does NOT claim is PHENOMENAL
    consciousness (Block's distinction): SD-064 is an access-functional claim only.

    Explanatory pairings:
    - Psychosis / thought-broadcasting substrate (self-attribution contamination
      claim): SD-064 says workspace content is legible-for-broadcast to the system;
      that failure claim models an agent experiencing its own workspace content as
      externally attributed / broadcast. Same channel, read from opposite ends.
    - MECH-090 (beta gates E3 -> action): OUTPUT-side gate; SD-027/SD-064 SELECTION
      is the INPUT-side gate. Together they bracket the workspace.
  evidence_quality_note: >
    External corroboration (NOT REE evidence; convergent, not confirmatory):
    Anthropic's J-space / Jacobian-lens result (2026,
    anthropic.com/research/global-workspace). A large capable model, under NO
    design pressure to have a workspace, was found to have spontaneously developed a
    compact reportable bottleneck -- "a few dozen concepts at a time, less than a
    tenth of overall internal activity" -- that is (a) reportable, (b) causally
    load-bearing (ablating it drops multi-step reasoning to near-zero while fluency /
    sentiment / MCQ survive), and (c) bypassed by automatic processing. This is
    convergent evidence that a capacity-limited, reportable, integration-central
    broadcast channel is an ATTRACTOR for capable sequence models, which raises the
    prior that REE's designed-in version is doing real work. Caveats that bound the
    analogy: the J-space is a single-forward-pass artifact (REE's is recurrent /
    temporally extended -- closer to Baars' actual temporal theatre), built almost
    entirely of single tokens, and the entry mechanism is unknown; and access !=
    phenomenal.

    FALSIFIER (the load-bearing test): the workspace-ablation CLIFF. If SD-064 is
    right, ablating the selection-for-broadcast channel should collapse integrative
    / multi-step / committed-action cognition toward zero while leaving reactive
    automatic behaviour intact -- a CLIFF, not graceful degradation (the pluralist /
    no-single-workspace reading predicts graceful degradation via redundant
    distributed pathways). This direct test is SUBSTRATE-BLOCKED: the SD-027 boundary
    gate is not implemented in V3 (no sd027 / access-gate code in ree_core; SD-027 is
    v4-scoped with an optional top-k retrofit), so a clean gate-ablation would
    self-route substrate_not_ready. The RUNNABLE near-term test is an REE-native
    J-lens DISPOSITIONAL-READOUT diagnostic on an existing all-ON agent: does a
    compact, action-coupled, reportable subspace exist (the J-space analogue), what
    fraction of latent activity does it occupy, and is it few-dozen-dimensional?
    A positive existence result motivates the SD-027 V3 gate retrofit that unlocks
    the cliff falsifier. Design + both experiment specs:
    evidence/planning/global_workspace_jlens_plan.md. v3_pending until that
    diagnostic (and, later, the ablation cliff) scores. PROMOTES NOTHING.
  location: evidence/planning/global_workspace_jlens_plan.md

- id: SD-065
  title: "Conditioned-safety cue channel: env-level controllable, observable Pavlovian safety CS added to CausalGridWorld -- an ambient (uniform-when-active) 25-dim field view emitted into world_state (feeds the z_world encoder the SD-051 ConditionedSafetyStore keys on). Can be paired with the MECH-302 relief-completion window during teaching (the store still writes its prototype ONLY on the real event_fired tick -- the pairing is real, not synthesized) and presented CONCURRENTLY with a threat cue at test. Closes the 'no controllable safety cue' substrate gap that forced V3-EXQ-759 to synthesize the Pavlovian pairing off-substrate, making MECH-304's promote-to-active classical-conditioned-inhibition behavioural falsifier runnable. Adds no entity type (NUM_ENTITY_TYPES stays 7); pure environment substrate; no trainable parameters; no MECH-094 interaction at the env level."
  claim_type: design_decision
  subject: environment.conditioned_safety_cue_channel
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-14
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: "2026-07-14"
  depends_on:
    - SD-051    # the ConditionedSafetyStore consumer this cue channel feeds (z_world prototype)
    - SD-022    # limb_damage + scheduled-injection curriculum: the relief generator + build-pattern precedent
    - MECH-302  # relief-completion teaching signal that gates the store's prototype write (event_fired)
  functional_restatement: >
    SD-065 extends CausalGridWorld (ree_core/environment/causal_grid_world.py)
    with a controllable conditioned-safety cue channel. The SD-051
    ConditionedSafetyStore keys on the full z_world but had no observable cue to
    bind: the MECH-302 relief-completion event is an internal z_harm_a-norm
    derivative with no stimulus attached, and the SD-023 landmark / SD-049
    dual_cue fields are resource-predictive, not safety, and not bindable to
    relief. SD-065 adds an AMBIENT (uniform-when-active) 25-dim field view
    appended to world_state (feeds the z_world encoder) plus
    obs_dict["safety_cue_field_view"]. Ambient (not a spatial gradient) so the
    cue's z_world contribution is position-independent -- reliably present at
    every relief tick and reproducible at test, removing a navigation confound;
    the faithful analog of a Pavlovian tone/light CS.

    Two activation paths. (1) safety_cue_on_relief auto-activates the cue across
    the SD-022 damage->heal (MECH-302 relief) window -- active exactly when
    sum(limb_damage) > safety_cue_heal_floor; the store still writes its
    prototype ONLY on the real event_fired tick inside that window, so the
    Pavlovian pairing is real, not synthesized. (2) set_safety_cue(active) is a
    tri-state manual override (test-phase API; precedence over the schedule;
    persists across reset) to present {threat + cue} concurrently in one tick.
    All kwargs are env-only (NOT surfaced through REEConfig.from_dims);
    disabled-by-default, bit-identical OFF (no channel, world_obs_dim unchanged,
    zero RNG draws). Preconditions (loud-not-silent, mirroring SD-022):
    safety_cue_enabled requires use_proxy_fields; safety_cue_on_relief requires
    limb_damage_enabled (both raise ValueError).

    Enables MECH-304's pre-registered behavioural falsifier: classical
    conditioned inhibition (a learned safety cue suppresses avoidance commitment
    even when presented concurrently with a threat cue) plus the dissociation
    (MECH-304 ablation abolishes cue-specific safety while sparing MECH-303
    contextual safety). It is to MECH-304 what SD-022 scheduled_limb_damage is to
    MECH-302: a curriculum-level env affordance supplying the controllable
    antecedent the mechanism needs, without changing the agent substrate.
  implementation_note: >
    Substrate IMPLEMENTED 2026-07-14 (/implement-substrate, session
    infallible-perlman-a4f3fe), disabled-by-default. Module
    ree-v3/ree_core/environment/causal_grid_world.py: env kwargs
    safety_cue_enabled / safety_cue_scale / safety_cue_on_relief /
    safety_cue_heal_floor; step() computes _safety_cue_active (manual override >
    relief-window > inactive); world_state gains a 25-dim uniform view when
    enabled (world_obs_dim +25); set_safety_cue() test-phase API; info sentinels
    safety_cue_enabled / safety_cue_active / safety_cue_event_count. 10/10 new
    contracts in tests/contracts/test_scheduled_safety_cue_curriculum.py (off-inert
    + dim-unchanged / preconditions / geometry +25 / manual override + scale clip /
    relief-pairing tracks damage window / reset clears dynamics + override persists
    / RNG isolation); backward-compat + activation + relief-pairing smokes PASS.
    Design doc docs/architecture/sd_065_conditioned_safety_cue_channel.md;
    ree-v3/CLAUDE.md SD-Implemented entry. PROMOTES NOTHING. v3_pending until the
    V3-EXQ-763 MECH-304 behavioural falsifier validates the channel end-to-end.
  validation_experiment: V3-EXQ-763 (MECH-304 promote-to-active behavioural falsifier)

- id: SD-066
  title: "Common-mode-invariant (centered) conditioned-safety readout: an opt-in, non-parametric readout mode on the SD-051 ConditionedSafetyStore that subtracts a slow EMA of z_world (the common-mode direction that saturates the raw cosine under SD-008 z_world under-differentiation) before accumulating and querying the prototype, so the cue-carrying residual dominates. Lifts the z_world common-mode CEILING that blocks MECH-304's behavioural promote-to-active gate (the raw-cosine gate fires unconditionally once the prototype is non-empty; the cue is a ~0.006-cosine signal below its absolute-threshold resolution). NOT the V4 trainable-contrastive store -- a readout workaround for the SD-008 common-mode, not a representation fix."
  claim_type: design_decision
  subject: safety_prediction.common_mode_invariant_conditioned_safety_readout
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-15
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  epistemic_category: standard
  claim_level: architectural
  registered_utc: "2026-07-15"
  depends_on:
    - SD-051    # the ConditionedSafetyStore this readout extends
    - SD-065    # the safety cue channel whose signal it resolves
    - SD-008    # the z_world under-differentiation (common-mode) this works around
  functional_restatement: >
    The MECH-304 behavioural promote-to-active gate (SD-051 store + SD-065 cue)
    hits a substrate ceiling the representation-level V3-EXQ-759 (rank/AUC)
    sidestepped: the store release gate is sigmoid(gain*cosine(z_world,
    prototype)) > safety_store_threshold == cosine > 0, but under SD-008
    under-differentiation every z_world sits at cosine ~0.99 (measured
    cos(cue,nocue)=0.988 vs cos(two contexts)=0.995), so the gate fires
    UNCONDITIONALLY once the prototype is non-empty -- the cue (a consistent but
    ~0.006-cosine signal) is unresolvable behaviourally, and more encoder warmup
    does not help (nothing supervises the encoder to differentiate the passive
    cue). SD-066 adds an opt-in centered readout: the store keeps a slow EMA
    baseline of z_world (the common-mode) and does BOTH prototype accumulation and
    querying on the centered residual z_world - baseline, so the cue residual
    dominates the cosine. Non-parametric; the baseline advances inside
    store.update() (already called every tick in sense()) so there is NO agent
    hot-path change; sim_mode (MECH-094) does not advance the baseline. Config
    safety_store_centered (bool, default False -> byte-identical raw cosine) +
    safety_store_baseline_alpha, surfaced through from_dims. It is a readout
    workaround for the SD-008 z_world common-mode, NOT a representation fix (does
    not improve z_world differentiation) and NOT a replacement for the V4
    trainable-contrastive store (still the sharper long-term answer).
  implementation_note: >
    Substrate IMPLEMENTED 2026-07-15 (/implement-substrate, session
    infallible-perlman-a4f3fe), disabled-by-default. Module
    ree-v3/ree_core/safety/conditioned_safety_store.py (ConditionedSafetyStore
    centered=/baseline_alpha= params + _baseline EMA + centered accumulation/query
    + reset clears baseline); config.py safety_store_centered /
    safety_store_baseline_alpha (dataclass + from_dims); agent.py passes them to
    the store constructor. 5/5 new contracts in
    tests/contracts/test_sd066_centered_safety_readout.py (raw bit-identical +
    never-touches-baseline / baseline lifecycle + reset / common-mode separation /
    sim_mode gate / config default); full pytest tests/ 1463 passed (bit-identical
    OFF). Landed ree-v3 main 0721dcb. VALIDATED end-to-end inline: centered ON
    makes the MECH-304 behavioural gate cue-specific (arm A cue release 1.0 / sig
    0.7-0.94 vs arm C nocue release ~0 / sig 0.17-0.48; A-C ~1.0) -- V3-EXQ-763
    inline PASS. PROMOTES NOTHING; v3_pending until the cloud V3-EXQ-763 PASS.
  validation_experiment: V3-EXQ-763 (MECH-304 promote-to-active behavioural falsifier)
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: safety_store_centered must be set True
    (the config default is False, which is byte-identical to the
    raw-cosine readout SD-066 exists to route around -- testing with the
    OFF default is definitionally uninformative). The SD-051
    ConditionedSafetyStore and the SD-065 conditioned-safety cue channel
    must both be active in the same run.
    CONFIRMING: with centered readout ON, the MECH-304 behavioural
    promote-to-active gate becomes cue-specific rather than firing
    unconditionally -- cue-context release separates cleanly from
    nocue-context release on a cloud-authoritative run, not merely an
    inline spot-check. ALREADY MET: V3-EXQ-763 (machine ree-worker-1,
    2026-07-15) PASSED both load-bearing DVs -- release_A_store_on_cue
    0.833 vs release_C_store_on_nocue 0.161 (store-necessity gap 0.833,
    cue-specificity gap 0.672, margin 0.34), D_mech303_contextual
    release 1.0 (sparing intact).
    FALSIFYING: with centered readout ON, the gate remains
    cue-insensitive on a properly-powered (cloud) run -- would mean
    common-mode subtraction does not resolve the ~0.006-cosine cue
    signal under SD-008 z_world under-differentiation in a
    properly-powered setting, and the readout workaround fails at the
    same ceiling the raw-cosine gate failed at. Would route to
    substrate_ceiling (next lever: V4 trainable-contrastive store).
    CURRENCY NOTE (2026-08-08): the CONFIRMING branch above is already
    satisfied -- V3-EXQ-763 is a cloud-authoritative PASS, already
    reviewed, and already the basis for MECH-304's own 2026-07-15
    promotion to active. This claim's v3_pending/status still describe
    that confirmation as outstanding; flagged for governance
    reconciliation, not resolved here.

- id: SD-067
  title: "Dedicated safety-terrain RBF bandwidth: an opt-in, tighter bandwidth for the MECH-303 contextual-safety-terrain RBF read, separate from the shared kernel_bandwidth (1.0). The shared bandwidth is ~15x too wide for the z_world residual scale (~0.065 between safe/unsafe contexts under SD-008 under-differentiation), so evaluate_safety SATURATES (reads ~identically in every context) and the fixed contextual_safety_release_threshold cannot resolve the safe-vs-unsafe signal into an absolute gap -- the discrimination exists as a rank (AUC ~0.83, V3-EXQ-760) but the behavioural release fires indiscriminately. The tighter bandwidth resolves it into an absolute gap the release gate can use. The Euclidean-RBF sibling of SD-066 (same SD-008 root cause); centering is a no-op here because Euclidean distance is translation-invariant, so bandwidth is the correct lever. A read-resolution workaround, NOT a z_world representation fix."
  claim_type: design_decision
  subject: safety_prediction.contextual_safety_terrain_read_resolution
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-15
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: architectural
  registered_utc: "2026-07-15"
  depends_on:
    - MECH-303  # the contextual passive safety terrain whose read this resolves
    - SD-008    # the z_world under-differentiation (common-mode) this works around
  functional_restatement: >
    The MECH-303 behavioural promote-to-active gate hits the same SD-008 z_world
    common-mode ceiling SD-066 fixed for the sister claim MECH-304, but through a
    different readout. The contextual safety terrain is an RBF over accumulated
    z_world centers: evaluate_safety(z) = sum_i w_i * exp(-||z-center_i||^2 /
    (2*bw^2)); the release gate fires when evaluate_safety(z_world).mean() >=
    contextual_safety_release_threshold, with bw inherited from the shared
    kernel_bandwidth (1.0). Under SD-008, safe (num_hazards=0) and unsafe
    (num_hazards=8) contexts sit only ~0.065 apart in z_world (cosine ~0.988), so a
    bandwidth of 1.0 is ~15x too wide: the RBF saturates (safe read 99.7 vs unsafe
    99.7, ratio 1.00) and the fixed threshold fires indiscriminately -- terrain-ON
    arms release at 1.0 in BOTH a safe and an unsafe context, so context-specificity
    fails for a substrate reason, not a real null. The discrimination exists (raw
    Euclidean rank AUC ~0.83, matching V3-EXQ-760) but the wide bandwidth cannot
    resolve it into an absolute gap. SD-067 adds an opt-in dedicated
    safety_terrain_bandwidth (tighter than kernel_bandwidth) so evaluate_safety
    resolves the small residual into an absolute gap. Config
    residue.safety_terrain_bandwidth (Optional[float], default None -> falls back to
    kernel_bandwidth, byte-identical), surfaced through from_dims. Centering (the
    SD-066 lever) is a no-op here: the RBF is Euclidean-distance-based and Euclidean
    distance is translation-invariant, so bandwidth -- not a baseline subtraction --
    is the correct lever. A read-resolution workaround for the SD-008 common-mode,
    NOT a z_world representation fix and NOT a metric-learned terrain (the sharper
    long-term answer).
  implementation_note: >
    Substrate IMPLEMENTED 2026-07-15 (session unruffled-hopper-e33b08),
    disabled-by-default. config.py ResidueConfig.safety_terrain_bandwidth (default
    None) + from_dims(safety_terrain_bandwidth=None) threaded onto config.residue
    when use_contextual_safety_terrain=True; field.py safety_terrain_rbf_field uses
    it when set, else kernel_bandwidth. 6/6 new contracts in
    tests/contracts/test_sd067_safety_terrain_bandwidth.py (None->kernel
    bit-identical / config default None / dedicated bandwidth used + harm RBF
    untouched / tight bandwidth resolves a small separation the wide one cannot /
    from_dims wiring). Full pytest tests/ 1469 passed (bit-identical OFF). Real-field
    inline validation (safety_terrain_bandwidth=0.03, release_threshold 0.5, 6 seeds):
    arm A terrain-ON safe release 0.748, arm B terrain-OFF safe 0.000 (terrain-necessity
    gap 0.748 PASS), arm C terrain-ON unsafe 0.457 (context-specificity gap 0.292 FAIL
    < 0.34), arm D store-ON cue 0.833 (MECH-304 dissociation PASS). SD-067 WORKS AS
    DESIGNED -- it resolves the previously-saturated read (~99.7 in every context ->
    terrain-necessity now demonstrable at 0.748) into a usable absolute gap. The residual
    context-specificity shortfall is UPSTREAM in z_world (SD-008), not SD-067: directional
    5/6 seeds (A>C) but 1 seed inverts and a variance-aware paired test (t~1.8, p~0.13)
    does not clear it -- an AUC-0.83 z_world input ceiling no read-side bandwidth can break.
    DISPOSITION (2026-07-15, user): SD-067 lands as a validated read-resolution fix; the
    MECH-303 promote-to-active behavioural falsifier V3-EXQ-764 is HELD (not queued), gated
    on z_world differentiation (SD-008); the validated script is committed and ready to
    queue once z_world can separate safe/unsafe contexts. PROMOTES NOTHING; stays
    candidate/v3_pending (no landed cloud validation while 764 is held; inline-validated).
  validation_experiment: V3-EXQ-764 (MECH-303 promote-to-active behavioural falsifier -- HELD pending SD-008 z_world differentiation)

- id: SD-068
  title: "Consolidation-pipeline lesion harness: experiment-layer instrumentation that makes each phase of the MECH-120 (SWS denoising) -> MECH-121 (NREM slot-filling) -> MECH-123 (REM precision recalibration) offline-consolidation pipeline independently diffuse-damageable and gives each a per-phase output-quality readout scored against INJECTED known content -- a uniform RMS-scaled Gaussian per-phase diffuse-damage knob plus injected-content scoring, at zero ree_core change -- so the MECH-168/INV-047/MECH-169 staged-decline-under-uniform-damage falsifier is buildable on demand. Non-vacuity is carried by the validated per-phase content-contingent readouts (SD-071), NOT by damage-tolerance staging order (separated out as SD-073) and not by feed-forward error-compounding (which is topology-baked)."
  claim_type: design_decision
  subject: sleep.consolidation_pipeline_lesion_harness
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-17
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-778h_2026-07-19
      as_of: 2026-07-19
      verdict: non_contributory/measurement_gap
  implementation_phase: v3
  epistemic_category: standard
  claim_level: architectural
  registered_utc: "2026-07-17"
  depends_on:
    - SD-017
    - MECH-120
    - MECH-121
    - MECH-123
    - MECH-204
  location: docs/architecture/sd_068_consolidation_lesion_harness.md
  functional_restatement: >
    IGW-20260717-206 (EVB-0115) adjudicated a MECH-169 experiment blocked_substrate:
    the live sleep cycle (SleepLoopManager._run_cycle) exposes only operational
    telemetry (routing counts, replay diversity, update counts), with NO per-phase
    output-quality readout and NO per-phase diffuse-damage knob, so the distinctive
    MECH-168/INV-047/MECH-169 falsifier (uniform damage -> STAGED reverse-dependency
    failure vs uniform) could not be run non-vacuously. SD-068 supplies both at the
    experiment layer (ree-v3/experiments/_lib/consolidation_lesion_harness.py; no
    ree_core change), following the V3-EXQ-702 injected-content precedent so it
    sidesteps the failure_autopsy_V3-EXQ-538a encoding-starvation ceiling (fidelity
    is scored against KNOWN injected content, not emergent encoding). A single
    RMS-scaled Gaussian sigma applied identically to each phase == the uniform/diffuse
    damage model. Non-vacuity: per-phase errors are normalised to fractional-of-own-
    range degradation and ranked by damage-tolerance for staging order, and REM is
    measured two ways (passthrough linear precision nudge vs generative
    run_rem_attribution_pass) so amplify/attenuate is testable rather than assumed.
    Substrate finding: the three phases operate on DISJOINT state, so a faithful
    cross-phase content-propagation pipe is not directly instrumentable -- the
    non-vacuity is carried by staging-order + the REM contrast.
  implementation_note: >
    IMPLEMENTED 2026-07-17 (session igw-206-mech169-lesion-harness-65eff6),
    experiment-layer, zero ree_core change. Smoke: run_staged_sweep deterministic;
    observed damage-tolerance order (nrem, rem, sws) stable across seeds 42/7 --
    a PARTIAL match to the reverse-dependency prediction (SWS correctly most-robust/
    last) that INVERTS REM-vs-NREM, with the REM generative sensitivity null (pure
    passthrough nudge). This is the intended non-vacuous diagnostic behaviour: it does
    NOT rubber-stamp the staging prediction. MECH-121 hold RESPECTED (not lifted): the
    validation run is EXPERIMENT_PURPOSE=diagnostic and does not tag MECH-121 as
    promotion evidence; the NREM leg is substrate-plumbing-fidelity only. Glymphatic/
    amyloid structural half of MECH-169 has no V3 analog and is out of scope.

    SWS READOUT REBUILT 2026-07-18 (session magical-lamport-37d09b), routed here by
    failure_autopsy_V3-EXQ-778c_2026-07-18 -- experiment-layer only, zero ree_core
    change, and NO substrate_queue entry (that autopsy's own
    recommended_substrate_queue_entry.action is "none"). The content-free
    sws_denoising_snr scored series in
    ree-v3/experiments/_lib/consolidation_lesion_harness.py is replaced by
    _sws_pattern_completion: a cosine retrieval margin of the post-SHY store against
    the injected prototypes, probed with the UNSCALED base. Landed ree-v3 origin/main
    8b18338; design rationale and caveats are in the "SWS readout rebuild" subsection
    of the location doc (REE_assembly origin/master 1118cc5955).

    VALIDATED 2026-07-18 by V3-EXQ-778g (run_id
    v3_exq_sd068_sws_content_scored_readout_diagnostic_20260718T130139Z_v3, 8 seeds,
    ree-cloud-2, PASS, evidence_direction supports). Both LOAD-BEARING criteria met on
    8/8 seeds: C1 null_slope_ratio_sws mean 0.1495 (sd 0.0218, CI95
    [0.1344, 0.1646]) against the 0.25 ceiling with ceiling_inside_ci95 FALSE, versus
    the retired readout's analytic 1.0000 (sd 2.7e-8) in 778c; and C3, the
    content-scale ladder, with content-slope spread 0.1108 above the 0.01 floor. Both
    preconditions met (injected-arm sigma-slope 0.323, range 0.323-0.358, far above the
    1e-06 readiness floor) and criteria_non_degenerate true on all three. The earlier
    3-seed local smoke is superseded by this 8-seed cloud run and should not be cited.
    C3 was pre-registered precisely because the repaired readout is cosine-based and
    therefore scale-invariant -- its null arm is flat in sigma partly BY CONSTRUCTION,
    so the null ratio alone is a weaker check than it looks. C3 passing is what
    discharges that caveat; the result does NOT rest on the null ratio alone.

    SCOPE OF THAT VALIDATION -- narrow, and it does NOT restore the staging order.
    778g gates on the sws leg ALONE (gated_phase sws; nrem and rem measured and
    reported as context only, deliberately not gated, since the rem leg is known
    degenerate at both clamp rails and gating on all three phases would FAIL
    regardless of whether the sws repair worked). So what is now validated is the sws
    INSTRUMENT's content-contingency, not any staging claim: the reverse-dependency
    staging order remains UNSUPPORTED, and the rem leg remains open, owned by the
    GOV-FANOUT-1 portfolio V3-EXQ-778d/e/f. 778c is NOT superseded -- its finding
    about the OLD readout stands and is what motivated this build; 778g validates a
    DIFFERENT instrument. Accordingly the 778c evidence_quality_note below is left
    UNCHANGED by this note. Whether SD-068's non-vacuity contract should now be
    re-widened to re-admit the sws leg is a GOVERNANCE adjudication for a /governance
    cycle, not something this implementation_note performs. No status, confidence,
    promotion or demotion change follows from 778g: experiment_purpose is diagnostic,
    and diagnostic evidence does not weight governance confidence. MECH-121 hold
    remains RESPECTED (deliberately not tagged); the run's
    evidence_direction_per_claim is supports for SD-068 and unknown for MECH-168 /
    INV-047 / MECH-169.
  validation_experiment: V3-EXQ-778 (staged-damage diagnostic; queued 2026-07-17, coordinator-live)
  evidence_quality_note: |
    [SCOPE NARROWED 2026-07-18 -- V3-EXQ-778c null-content control, FAIL/weakens,
    DIAGNOSTIC so no status or confidence change] The pre-registered
    zero-injected-content NULL CONTROL (the analog of the odour-contingency null in
    Bar et al. 2020, the methodological precedent this SD follows) audited whether
    each per-phase readout measures CONTENT FIDELITY or raw NOISE SENSITIVITY. It ran
    the identical sigma sweep twice per seed on identical substrate / warm-up / RNG
    streams, differing only in content_scale (1.0 vs 0.0), with the delivered
    perturbation held numerically identical across arms. Load-bearing C1
    (null_slope_ratio <= 0.25 on all three phases) FAILED on 0/8 seeds:
      nrem 0.1445 (95% CI [0.1438,0.1451], 0/8 confounded) -- CONTENT-CONTINGENT;
      sws  1.0000 (sd 2.7e-8, 8/8 confounded)  -- CONTENT-FREE BY CONSTRUCTION.
        denoising_snr_db = 10*log10(signal_power/noise_power), and noise_power is
        identical with and without injected content at every sigma (384.18 / 1536.73 /
        6146.91 / 24587.64) while signal_power is 5585.7 vs 0.0 -- so the sigma-slope
        depends only on log(noise_power) and the content term is a constant offset
        that differentiates away. Analytic, not statistical, hence sd 2.7e-8.
      rem  degenerate at BOTH rails -- exactly 0.0 on 5/8 seeds (null calibration_error
        pinned at the constant 998.5009992509989, target_clamped 1.0) and off-scale
        1801-9143 on 3/8 (null precision reference collapsed onto the 1e-3 floor);
        ceiling_inside_ci95 true, confound_verdict_stable false. UNINTERPRETABLE at n=8.
    The readiness precondition was MET (injected-arm slope 0.0665 >> 1e-06 floor), C2
    passed and criteria_non_degenerate is true on all three criteria, so this is an
    informative negative about the INSTRUMENT, not a broken run.
    DISPOSITION -- NARROWED, NOT WITHDRAWN, per this experiment's own pre-registration
    ("a FAIL here is an INFORMATIVE outcome... it scopes SD-068's non-vacuity honestly
    rather than withdrawing the claim"). SD-068's non-vacuity contract is henceforth
    carried by the nrem injected-content leg PLUS the REM
    passthrough-vs-generative contrast, and NOT by the sws leg. The staged-damage
    results in the architecture doc's "Diagnostic results" are correspondingly
    retracted as staging evidence: no leg of the reverse-dependency staging order is
    currently supported by a validated instrument.
    NARROW-SUPPORTS FLAG: this leaves SD-068 resting on a SINGLE confirmed
    content-contingent readout plus a generative-gain contrast whose own
    content-dependence is itself an open question (the null arm's rem_generative_gain
    ~0.182-0.209 is close to the injected arm's ~0.165-0.190 at content_scale 0.0 --
    outside the scored criteria, registered as H-gen-gain-content-free). Do not read
    the narrowing as leaving SD-068 comfortably supported.
    ROUTING: /implement-substrate to replace sws_denoising_snr with a content-scored
    readout (experiment-layer, _lib/consolidation_lesion_harness.py, zero ree_core
    change -- the same repair as rem_terrain_variance -> rem_generative_fidelity at
    ree-v3 main da873a1), plus a GOV-FANOUT-1 three-axis portfolio on the rem leg.
    Re-derive brake does NOT fire (first autopsy on SD-068; category measurement_gap,
    not substrate_ceiling). Predecessor V3-EXQ-778b (n=2) is marked superseded.
    Full diagnosis: evidence/planning/failure_autopsy_V3-EXQ-778c_2026-07-18.

    [PARTIALLY RE-WIDENED 2026-07-18 -- V3-EXQ-778g, PASS/supports, DIAGNOSTIC so
    again no status or confidence change. This amendment supersedes exactly two spans
    of the 778c text above: the clause "and NOT by the sws leg" in DISPOSITION, and
    the NARROW-SUPPORTS FLAG paragraph. Everything else above STANDS, and V3-EXQ-778c
    is NOT superseded -- its finding is about the RETIRED readout and is what motivated
    the repair; 778g validates a DIFFERENT instrument.]
    GROUND FOR RE-ADMISSION. The sws leg was excluded on one stated ground and one
    only: its readout was content-free by construction. That ground is now discharged.
    sws_denoising_snr was replaced by _sws_pattern_completion (cosine retrieval margin
    of the post-SHY store against the injected prototypes, probed with the UNSCALED
    base; ree-v3 origin/main 8b18338, experiment-layer, zero ree_core change), and
    V3-EXQ-778g (run_id
    v3_exq_sd068_sws_content_scored_readout_diagnostic_20260718T130139Z_v3, 8 seeds,
    ree-cloud-2) clears the SAME load-bearing C1 criterion at the SAME 0.25 ceiling
    that admitted the nrem leg: null_slope_ratio_sws mean 0.1495 (sd 0.0218, CI95
    [0.1344, 0.1646]), ceiling_inside_ci95 FALSE, 8/8 seeds, against the retired
    readout's analytic 1.0000 (sd 2.7e-8). Pre-registered C3 (content-scale ladder,
    slope spread 0.1108 > the 0.01 floor) additionally discharges the caveat that the
    replacement is cosine-based and hence scale-invariant, so its null arm is flat in
    sigma partly BY CONSTRUCTION -- the result does not rest on the null ratio alone.
    Symmetry of criterion is the point: re-admitting sws on a weaker test than the one
    that excluded it would be motivated reasoning, and that is not what happened here.
    AMENDED CONTRACT. SD-068's non-vacuity is henceforth carried by TWO confirmed
    content-contingent per-phase readouts -- the nrem injected-content leg AND the
    rebuilt sws pattern-completion leg -- plus the REM passthrough-vs-generative
    contrast.
    STILL EXCLUDED, EXPLICITLY -- the reverse-dependency STAGING ORDER. 778g gates on
    gated_phase sws ALONE; nrem and rem were measured and reported as CONTEXT ONLY,
    deliberately not gated, because the rem leg is degenerate at both clamp rails and
    gating all three would FAIL regardless of whether the sws repair worked. What is
    validated is therefore the sws INSTRUMENT's content-contingency, not any staging
    result. Staging order is a CROSS-phase ranking and cannot be supported while one
    of the three ranked legs (rem) has no interpretable readout, and in any case no run
    has yet re-measured the staging order using the repaired instrument. So the
    retraction above holds: the architecture doc's "Diagnostic results" remain
    RETRACTED as staging evidence and the reverse-dependency staging order remains
    UNSUPPORTED. One phrase in that retraction is now stale and is corrected here --
    it read "no leg ... is currently supported by a validated instrument", which after
    778g is false as written (sws now has a validated instrument); the correct
    statement is that no leg's staging POSITION has been established, two of three
    legs now have validated content-contingent instruments, and the third (rem) does
    not.
    NARROW-SUPPORTS FLAG -- STILL RAISED, on narrower ground. The original wording
    ("resting on a SINGLE confirmed content-contingent readout") is superseded: there
    are now two. But the flag is NOT cleared, for two reasons that 778g does not
    touch. (i) The REM generative-gain contrast, which is the third pillar of the
    amended contract, has its own content-dependence unresolved -- the null arm's
    rem_generative_gain ~0.182-0.209 sits close to the injected arm's ~0.165-0.190 at
    content_scale 0.0, outside the scored criteria, registered as
    H-gen-gain-content-free and owned by the GOV-FANOUT-1 rem-leg portfolio
    V3-EXQ-778d/e/f (all still queued as of 2026-07-18). (ii) The staging order, which
    the claim title names as a non-vacuity carrier, remains unsupported per the
    paragraph above. Do not read this re-widening as leaving SD-068 comfortably
    supported; it restores one readout to the contract, nothing more.
    SCOPE OF THIS AMENDMENT. Wording and scope only. Both 778c and 778g are
    experiment_purpose=diagnostic and neither weights governance confidence, so no
    confidence, status, promotion or demotion change follows -- SD-068 remains status
    candidate, implementation_phase v3. The MECH-121 hold (hold_pending_v3_substrate)
    remains RESPECTED: 778g deliberately does not tag MECH-121, and its
    evidence_direction_per_claim is supports for SD-068 and unknown for MECH-168 /
    INV-047 / MECH-169, so no downstream fold-note is amended by this. The claim title
    is left unchanged: it states the ORIGINAL design-time contract, and this note is
    the live carrier of scope.

    [DECOMPOSED 2026-07-18 -- claim synthesis, four children registered, SD-068 RETAINED
    AND NARROWED as the umbrella. Not superseded, not demoted, no status or confidence
    change. This supersedes exactly one span above: the final sentence "The claim title
    is left unchanged", which is now false -- the title WAS amended by this decomposition
    (see TITLE AMENDMENT below). Everything else above STANDS.]
    WHAT SD-068 NOW ASSERTS, narrowed to what it actually established and what actually
    worked: the HARNESS DESIGN DECISION itself -- a uniform RMS-scaled Gaussian per-phase
    diffuse-damage knob plus injected-content scoring at the experiment layer, zero
    ree_core change. Its per-phase instrument assertions and its staging-order non-vacuity
    carrier have moved to the children.
    TITLE AMENDMENT. The old title named damage-tolerance STAGING ORDER as a non-vacuity
    carrier. That specific binding is what held the per-phase instrument results hostage:
    they could not settle while the staging order was unsupported, and the staging order
    could not be established while the rem leg had no interpretable readout. The staging
    order is struck from the title as a carrier and is now SD-073; non-vacuity is carried
    by the validated per-phase readouts (SD-071).
    CHILDREN (each depends_on SD-068; the umbrella deliberately does NOT depend_on them,
    which would be circular):
      MECH-462 -- readout-type mechanism: first-order phases are content-scorable,
        second-order (REM precision) is content-free BY TYPE. Unifies the 778c / 778e /
        778f REM-leg cluster as one type fact rather than three bugs.
      SD-071 -- the nrem + sws legs as validated content-contingent instruments
        (0.1445 from 778c; rebuilt sws 0.1495 from 778g).
      SD-072 -- the build child: REM downstream discrimination readout; retires
        calibration_error and rem_generative_gain as functional-damage instruments.
      SD-073 -- the staging order, separated out and insult-regime-conditional.
    NOTHING ELSE CHANGES. No status, confidence, promotion or demotion change on SD-068,
    MECH-168, INV-047 or MECH-169 -- all supporting runs are experiment_purpose=diagnostic.
    The MECH-121 hold (hold_pending_v3_substrate) stays RESPECTED and no child tags it. The
    rem_generative_gain attenuation finding (0.149, 8/8) still STANDS; only its use as a
    functional-damage instrument is retired, by SD-072. hypothesis_space_registry.v1.json is
    NOT touched (GOV-FROZEN-1). Approved at the interactive gate 2026-07-18 (session
    gracious-bassi-721580); full derivation in
    evidence/planning/claim_synthesis_SD-068_2026-07-18.md.
    FOLD OWED (discharged 2026-07-18 by the REM-LEG RETIRED block below): the REM cluster
    autopsy's recommended_evidence_quality_note_SD_068 is now folded in; this note carries
    no outstanding fold debt.

    [REM LEG RETIRED 2026-07-18 -- GOV-FANOUT-1 rem-leg portfolio V3-EXQ-778d
    (measurement) / 778e (representation) / 778f (observation), three discrimination legs
    on distinct design axes, n=8 seeds each, all positive controls passing. All
    experiment_purpose=diagnostic, so again no status or confidence change. This amendment
    supersedes exactly three spans above, and nothing else: (1) in the 778c DISPOSITION
    paragraph, the clause "PLUS the REM passthrough-vs-generative contrast"; (2) in the
    778g AMENDED CONTRACT paragraph, the clause "plus the REM passthrough-vs-generative
    contrast"; and (3) the whole of item (i) of the 778g "NARROW-SUPPORTS FLAG -- STILL
    RAISED" paragraph, whose open question (H-gen-gain-content-free, recorded there as
    owned by "V3-EXQ-778d/e/f (all still queued as of 2026-07-18)") is precisely what
    these three runs closed. It also DISCHARGES the fold debt recorded at the end of the
    DECOMPOSED block. Everything else above STANDS, and none of 778c / 778g / the
    decomposition is superseded.]
    THE REM PER-PHASE READOUT IS CONTENT-FREE, and the three legs converge on it. Once the
    zero-content null's clamp degeneracy is removed by an unpaired-target null, the null
    arm STILL tracks the injected arm's sigma-slope -- content-contingent on 1/8 seeds
    only, unclamped ratios 0.577-0.995 -- and an independently de-clamped readout agrees
    on every non-degenerate seed (0.999 / 0.998 / 0.326, all above the 0.25 ceiling that
    admitted the nrem and rebuilt-sws legs). So the both-rails degeneracy reported for rem
    under 778c was not the whole story: removing it does not recover content-contingency,
    it exposes its absence.
    GENERATIVE-GAIN PILLAR RETRACTED AS A CORRECTION SIGNAL. rem_generative_gain is FLAT
    in content scale (delta -0.0097 against a pre-registered bar of 0.05, with the
    residual trend inverted in sign). The interpretive gloss that "correction needs an
    intact seed" is therefore RETRACTED from
    docs/architecture/sd_068_consolidation_lesion_harness.md.
    WHAT IS NOT OVERTURNED. The attenuation finding itself -- rem_generative_gain 0.149,
    attenuating 8/8 seeds -- STANDS, and is recorded at every content scale (0.1589 /
    0.1520 / 0.1493, 8/8 attenuating throughout) precisely so that it stays visible. Only
    its use as a functional-damage instrument is retired, which is the same disposition
    SD-072 already carries.
    NET EFFECT ON SD-068. The staged-failure contract is carried by the nrem leg plus the
    rebuilt sws leg (V3-EXQ-778g) ONLY. The REM leg is not a functional-damage instrument,
    and the generative-gain pillar does not evidence content-dependent correction. The
    reverse-dependency staging order remains UNSUPPORTED -- unchanged in substance from
    the 778g note, but now for a settled rather than an open reason: the rem leg's lack of
    an interpretable readout is a type fact (MECH-462), not a pending measurement.
    SPECIFICATION DEFECT FOUND IN THE COURSE OF THIS ADJUDICATION, recorded here so the
    ledger is not read as a substantive negative: V3-EXQ-778d's readiness anchor scored
    "railed" on the saturation rail ONLY, against a two-rail degeneracy, capping a
    bit-perfect replication at 0.625 under a 0.75 gate. The anchor's underlying data
    reproduced V3-EXQ-778c to identical floats across three runs, so control_passed is
    asserted here on the corrected both-rails predicate. Fix routed as V3-EXQ-778h (low
    priority, ledger-hygiene only).
    NOTHING ELSE CHANGES. No status, confidence, promotion or demotion change on SD-068,
    MECH-168, INV-047 or MECH-169 -- all supporting runs are experiment_purpose=diagnostic
    and do not weight governance confidence. The MECH-121 hold
    (hold_pending_v3_substrate) stays RESPECTED. The four children registered 2026-07-18
    (MECH-462, SD-071, SD-072, SD-073) are unaltered by this fold.
    hypothesis_space_registry.v1.json is NOT touched (GOV-FROZEN-1). Full diagnosis:
    evidence/planning/failure_autopsy_SD-068-rem-fanout-cluster_2026-07-18.json.

    [2026-07-20 governance / intra-run substrate-divergence sweep sec 7 item 4] The intra-run
    substrate-divergence hit (defect class D3) recorded against V3-EXQ-778a is WITHDRAWN.
    REFUTED at rung (b), process topology, by failure_autopsy_V3-EXQ-778a_2026-07-20 (REE_assembly
    77cd4cc013): the run is single-process and freezes its import graph at first use, so every cell
    executed one substrate regardless of what changed on disk mid-run. The `supports` reading
    STANDS unchanged and no confidence is re-weighted. Recorded so the flag does not resurface as
    unexplained doubt in a later sweep.
  what_would_answer: >
    Scope: this tests ONLY the harness DESIGN DECISION as narrowed by the
    2026-07-18 decomposition -- a uniform RMS-scaled Gaussian per-phase
    diffuse-damage knob plus injected-content scoring, built entirely at
    the experiment layer with zero ree_core change. It does NOT test the
    per-phase instrument validity (that is child SD-071's own falsifier)
    or the reverse-dependency staging order (child SD-073's own
    falsifier) -- both are separately falsifiable and deliberately not
    re-litigated here.
    NON-DEGENERACY PRECONDITION (met): consolidation_lesion_harness.py
    must be wired to the live SleepLoopManager pipeline with a working
    per-phase diffuse-damage knob and injected-content scoring. Confirmed
    implemented 2026-07-17 and landed at zero ree_core change; the sws
    leg was subsequently rebuilt and landed at ree-v3 origin/main 8b18338.
    SUPPORTED (currently the case): the harness design produces a
    working, content-scored, diffuse-damageable instrumentation layer
    that yields at least one genuinely usable (content-contingent)
    per-phase readout. Cleared twice over via child SD-071: the nrem leg
    (null_slope_ratio 0.1445, CI95 [0.1438,0.1451], 0/8 seeds confounded,
    V3-EXQ-778c) and the rebuilt sws leg (null_slope_ratio 0.1495, sd
    0.0218, CI95 [0.1344,0.1646], ceiling_inside_ci95 FALSE, 8/8 seeds,
    plus the C3 content-scale ladder at 0.1108 against the 0.01 floor,
    V3-EXQ-778g). Both diagnostic, so neither moves governance confidence
    by itself -- but both are exactly the kind of result that would have
    falsified the design had they gone the other way, and they did not.
    FALSIFYING: the harness design turns out unable to produce ANY
    validated content-contingent per-phase readout for ANY of the three
    phases. Precedent for what this looks like: the ORIGINAL sws attempt
    (V3-EXQ-778c) found sws_denoising_snr content-free BY CONSTRUCTION
    (analytic slope ratio 1.0000, sd 2.7e-8) -- a structural property of
    that particular readout choice, not a run bug. Had the nrem leg or
    the sws rebuild also failed this same test, that would falsify the
    DESIGN itself, not just one instrument choice. CURRENT STATE: NOT
    FALSIFIED. The REM leg's content-freeness (MECH-462, now understood
    as a type fact) does not falsify the design, since SD-068-as-narrowed
    only requires the general approach to produce at least one usable
    readout, which it demonstrably does.
    WHAT THIS DELIBERATELY LEAVES OPEN: whether the staging-order
    non-vacuity contract (SD-073, "unsupported, deliberately registered
    that way") or the REM downstream-discrimination readout (SD-072,
    "not yet built" as of 2026-08-08, zero activity since 2026-07-20) is
    NOT part of this claim's own falsification condition -- those are
    tracked separately under SD-073 and SD-072/MECH-462 and should
    receive their own digestion pass rather than being folded in here.

- id: SD-069
  title: "Phasic surprise-burst regulator: a toggleable, event-triggered LC-NE phasic control lever (PhasicSurpriseBurst) that fires a transient softmax-temperature burst on a prediction-error surprise spike and decays it over a few ticks, routed into the E3 select() softmax at agent.select_action() as the PHASIC complement to MECH-313 stochastic_noise_floor (tonic) on the SAME temperature channel -- the substrate that makes MECH-063 sub-claim (ii) (each control axis carries independent tonic baseline AND phasic event-burst degrees of freedom) behaviourally testable without synthetic signals. Reuses the MECH-104 volatility-surprise lit basis (Aston-Jones & Cohen 2005 phasic mode) but routes to the SELECTION softmax, NOT the ARC-016 commit/de-commit gate that carries the MECH-104 volatility_interrupt claim."
  claim_type: design_decision
  subject: control_plane.phasic_surprise_burst
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-17
    needs_review: false
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-07-17"
  depends_on:
    - MECH-313
    - ARC-005
    - MECH-063
  location: docs/architecture/sd_069_phasic_surprise_burst.md
  functional_restatement: >
    A 2026-07-17 substrate scan confirmed MECH-063 sub-claim (ii) (tonic/phasic
    split) could not be tested behaviourally: MECH-313 noise_floor was the only
    TONIC lever on the E3 softmax, with no toggleable PHASIC event-triggered
    counterpart on a comparable readout. MECH-104 was documented but unimplemented
    as a substrate module (faked in experiments by mutating e3._running_variance,
    routing only to the ARC-016 commit gate); MECH-287 InvalidationTrigger is
    genuinely phasic but routes to hippocampal staleness, never the softmax. SD-069
    supplies the missing phasic lever: a pure-arithmetic regulator
    (ree_core/regulators/phasic_surprise_burst.py) that reads e3._running_variance
    surprise, fires a burst on a spike over its own EMA baseline
    (surprise >= trigger_ratio x max(ema, floor)), decays it geometrically, and adds
    a transient temperature delta (default negative = phasic sharpening) at the
    e3.select() call site AFTER the MECH-313 tonic lift. Tonic and phasic are
    independently toggleable (use_noise_floor / use_phasic_burst) on the same
    effective-temperature readout, and the tonic lift is kept uncontaminated in the
    diagnostics so the dissociation is manifest-readable.
  implementation_note: >
    IMPLEMENTED 2026-07-17 (session blissful-hugle-5dd043) via /implement-substrate.
    Config: REEConfig.use_phasic_burst (default False -> agent does not instantiate
    the regulator; combined temperature == tonic; OFF action stream bit-identical,
    contract C8 with n_events==0) + phasic_burst_surprise_ema_decay (0.1),
    _trigger_ratio (1.5), _trigger_floor (1e-6), _temp_delta (-0.5), _decay (0.5),
    _min_temperature (0.1, strict-positive floor on combined T). Diagnostic:
    phasic_burst_level / _temp_delta in _last_score_bias_decomp; phasic_burst block in
    _last_control_vector. MECH-094: simulation_mode returns cached burst, no state
    advance; no memory-write surface -> phased training N/A, hypothesis_tag N/A.
    Smoke: 17 SD-069 contracts pass; full tests/contracts 1462 pass (0 regressions);
    an injected surprise spike fires through select_action -> pre-commit softmax
    entropy drops on the event (0.80 -> 0.06) and decays over the tail (event-locked
    transient), bit-identical before the event.
    SHARP-SURPRISE SOURCE amend 2026-07-17 (session mystifying-wilbur-394a10): added
    REEConfig.phasic_burst_signal_source ("running_variance" default no-op /
    "instantaneous_pe"). The default smoothed e3._running_variance decays monotonically
    for an untrained forward model and fires 0 natural events even under env volatility,
    so a no-training probe could only exercise the lever with a synthetic poke (exactly
    what MECH-063 ii must avoid). "instantaneous_pe" reads a new read-only
    e3.last_instantaneous_pe (raw per-tick PE-MSE before the running-variance EMA
    smoothing), so genuine surprise spikes survive -- the lever fires on REAL events
    (Aston-Jones & Cohen 2005 phasic mode = sharp/instantaneous salience). Validated
    (untrained rollout, drift on, no poke): burst ACTIVE under instantaneous_pe
    (burst_ticks up to 209/232) vs NEVER active under running_variance (0/217).
    Additive no-op default (existing experiments bit-identical); invalid source raises
    ValueError at construction. No status/confidence change (substrate only).
  validation_experiment: V3-EXQ-779 (MECH-063 sub-claim ii tonic-vs-phasic behavioural dissociation, 777-harness pattern, phasic_burst_signal_source=instantaneous_pe on PHASIC-ON arms; queued 2026-07-17)

- id: SD-076
  title: "Waking confidence inflation as an asymmetric precision EMA: good news about one's own accuracy is incorporated fast and bad news slowly, so the running-variance estimate settles below the true error mean. Supplies the daytime precision-drift source that MECH-204's corrective function presupposes but which V3 did not have. ree_core E3 selector change, bit-identical by default."
  claim_type: design_decision
  subject: precision.waking_confidence_inflation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-24
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-794_2026-07-22
      as_of: 2026-07-22
      verdict: non_contributory/competence_implementation_gap
  epistemic_category: standard
  evidence_quality_note: >
    2026-07-24 governance (V3-EXQ-794 Phase 7 2x2 calibration loop, evidence, claim_ids=[MECH-204,
    SD-076]; failure_autopsy_V3-EXQ-794_2026-07-22, confirmed, user-confirmed routing "794: SD-076
    does_not_support -> non_contributory, with the implementation-fix route"). 794 found SD-076's
    absolute rv floor (0.01) sat above the substrate's real operating point at the measured error
    scale, clamping rv_final identically across both inflation doses -- a saturation defect in the
    implementation's floor CONSTANT, not evidence against the asymmetric-EMA mechanism itself.
    non_contributory rather than does_not_support: the mechanism was never exercised at a scale
    where it could fail on its own terms. The headroom repair (scale-relative + softplus-saturating
    floor) already landed per the `implementation_note` above -- see that field for the full defect
    diagnosis and repair detail. Behavioural validation is the pending V3-EXQ-794a re-run of the
    identical 2x2 design against the repaired floor. SD-076 stays candidate.

    [2026-08-01 governance, GOV-FANOUT-1 H1/H2/H3 synthesis -- full detail on MECH-204's own
    evidence_quality_note, not duplicated here]: V3-EXQ-850 (H1, bears_on only) stays alive with
    partial F1-damping signal; V3-EXQ-853 (H2, claim_ids=[SD-076]) weakens H2 itself (dose
    separation shrinks with more training exposure, opposite H2's prediction, confirmed
    failure_autopsy_V3-EXQ-853_2026-08-01); H3's /lit-pull (commit e84240108d) weakens but does
    not eliminate H3. No clean winner -- genuine three-way residual uncertainty. EVB-0454's
    decision deadline was cited here as 2026-08-03T20:50:36Z (a snapshot from an earlier
    governance cycle). CORRECTED 2026-08-01 (V3-EXQ-864's autopsy, root-caused this session):
    this is NOT a discrepancy between two fixed deadlines -- `decision_deadline_utc` in
    experiment_proposals.v1.json is COMPUTED, not frozen: build_experiment_indexes.py sets it to
    `generated_at_dt + 72h` (mandatory_decision_deadline_hours) on EVERY regeneration, for as long
    as the underlying conflict stays unresolved. So it perpetually reads "~3 days from whenever
    governance last ran," not a real fixed commitment -- every past citation captured a snapshot
    of that rolling value, not a discrepancy to reconcile. Do NOT treat any cited
    decision_deadline_utc as a hard external deadline; the real constraint is "resolve the
    conflict," and the field will keep re-arming at +72h until that happens. Worth a design review
    (a rolling deadline can never be missed), flagged here rather than fixed unilaterally.
    PROMOTES/DEMOTES NOTHING; SD-076 stays candidate.

    [2026-08-01 governance continued, confirmed failure_autopsy_V3-EXQ-860_2026-08-01 +
    failure_autopsy_V3-EXQ-864_2026-08-01]: V3-EXQ-860 (H2 leg, redesigned to hold N_TRAIN_EPS at
    794a's original 30 -- removing the F1-firing-count confound 853 carried -- and instead raise
    STEPS_PER_EP 200->1000 for the same 5x total exposure via episode LENGTH rather than episode
    COUNT). Result, decisive for the H1-vs-H2 question: closure toward the smoke target collapses
    to ~0% (LO 2.9%, HI 2.5%) vs 853's own confounded 12-22% at the same total exposure -- a clean
    controlled elimination showing 853's partial closure was driven by increased F1-firing count
    (H1's axis), NOT raw exposure duration (H2's axis). H2 WEAKENS more strongly and cleanly than
    853 alone showed; H1 (F1-interaction damping) is correspondingly STRENGTHENED by elimination.
    V3-EXQ-853's evidence_direction is marked superseded (860 is its confound-corrected successor
    per 860's own supersedes field) -- see that manifest. SEPARATELY: 860 also found its own
    inflation_lowers_rv readiness precondition wrong-signed (a new, undiagnosed puzzle, distinct
    from the H1/H2 finding above and not undermining it), which V3-EXQ-864 was commissioned to
    instrument. V3-EXQ-864 found the wrong-signed puzzle remains UNRESOLVED: its steps_per_ep sweep
    never actually varied real exposure, because CausalGridWorldV3's own episode-termination
    condition (agent_health<=0 or steps>=500, causal_grid_world.py:2892) fires (~8-11 ticks/episode
    observed) far short of any swept value -- a test-design defect (non_contributory), not new
    evidence, non-blocking for EVB-0454 per 860's own routing. Net effect on the three-way residual
    uncertainty: H2 is now clearly the weakest-supported of the three explanations; H1 is the
    leading candidate by elimination; H3 (lit-pull) stays weakened-not-eliminated as before. This
    moves the EVB-0454 decision toward a leading candidate but does not singlehandedly resolve
    retain/hybridize/retire -- that call is deliberately left open this cycle per user instruction.
    PROMOTES/DEMOTES NOTHING; SD-076 stays candidate.

    [2026-08-03 governance, V3-EXQ-864a, diagnostic, evidence_direction=unknown, no claim weight]:
    follow-up to 864's test-design defect. Confirms the STEPS_PER_EP regression guard now holds
    (real_exposure_achieved=1.0 in every cell, all readiness preconditions met) -- 864's early-break
    defect does not recur. Characterizes the diff_final WCI/RV trajectory across steps_per_ep in
    {200, 500, 1000} for both ARM_INFL_LO and ARM_INFL_HI: the sign stays positive throughout, no
    crossover bracketed within the swept range (C1 non-load-bearing, not met). Purely characterizing;
    the wrong-signed-precondition puzzle 860/864 opened stays open beyond this bracket, un-resolved
    either way. PROMOTES/DEMOTES NOTHING; SD-076 stays candidate.
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-07-20"
  depends_on: [ARC-016]
  location: docs/architecture/sd_076_waking_confidence_inflation.md
  functional_restatement: >
    E3TrajectorySelector.update_running_variance maintained _running_variance (rv) as a
    SYMMETRIC EMA of observed squared prediction error, making rv a faithful tracker of
    true error -- rv ~= true error BY CONSTRUCTION. This renders an entire class of claim
    unmeasurable rather than merely unsupported. MECH-173's DV, overconfidence_index =
    (true_error_ref - mean_rv) / true_error_ref, is pinned near zero regardless of what is
    ablated: V3-EXQ-774 measured -0.000148 (ARM_REM_SUPPRESSED) and -0.000918
    (ARM_RECALIB_OFF), with the load-bearing criterion suppressed_arm_absolutely_
    overconfident recording false. The deeper point is that MECH-204's entire corrective
    function presupposes a daytime drift source the substrate lacked: "sleep recalibrates
    waking precision drift" is only testable if waking precision can drift, so a null on
    any MECH-204 consumer was a TAUTOLOGY rather than evidence. SD-076 supplies the drift.
    Under the master flag, error improvements are incorporated at alpha * (1 + asymmetry)
    and worsenings at alpha * (1 - asymmetry), so rv settles BELOW the true error mean --
    a systematic, directional, and correctable under-estimate of one's own error, which is
    the operational definition of overconfidence. Registered as its own claim rather than
    as part of MECH-204 because it is the SOURCE of drift while MECH-204 is the
    CORRECTION; the two must be independently ablatable.
  implementation_note: >
    IMPLEMENTED 2026-07-20 in ree-v3/ree_core/predictors/e3_selector.py
    (update_running_variance), surfaced as E3Config.use_waking_confidence_inflation
    (default False), E3Config.waking_confidence_inflation_asymmetry (default 0.0, clamped
    to [0, 0.999]) and E3Config.waking_confidence_rv_floor (default 0.01). BIT-IDENTICAL
    OFF BY CONSTRUCTION, NOT BY ARITHMETIC: the OFF branch evaluates the original
    symmetric expression unchanged rather than re-deriving it at asymmetry 0, and the
    smoke pins this by explicit float equality over an 80-step trace rather than an
    approximate comparison. THE FLOOR IS LOAD-BEARING, NOT HYGIENE: rv feeds an ABSOLUTE
    commit threshold (running_variance < commit_threshold, ARC-016) and current_precision
    = 1/(rv + 1e-6), so unbounded downward drift would both pin the agent permanently
    committed and explode precision; the floor applies only on the inflation path. SD-069
    is unaffected because last_instantaneous_pe is captured BEFORE this smoothing. Built
    in the same pass as the MECH-204 Phase 7 / Option B broadcast anchor (the correction
    half of the same loop), routed by the confirmed
    evidence/planning/failure_autopsy_V3-EXQ-774_2026-07-17. Smoke 6/6 PASS: at
    asymmetry=0.6 on an alternating error sequence with true mean 0.05, the
    overconfidence_index moves from -0.164 (OFF, UNDERconfident) to +0.273 (ON,
    genuinely overconfident); the OFF value reproduces the sign and rough magnitude of
    774's measured ARM_FULL_SLEEP = -0.2097, which is direct evidence that the autopsy
    diagnosis was correct.
    HEADROOM REPAIR 2026-07-22 (ree-v3 452f99e367, routed by the confirmed
    evidence/planning/failure_autopsy_V3-EXQ-794_2026-07-22, sd_id
    sd_waking_confidence_inflation_headroom). THE ABSOLUTE FLOOR ABOVE IS THE WRONG KIND
    OF QUANTITY AND 0.01 IS THE WRONG VALUE FOR THIS SUBSTRATE. V3-EXQ-794 measured the
    un-inflated operating point at rv = 0.005420 (ARM_OFF_OFF) and arm_true_error_ref at
    ~0.0037, so the floor sat 1.8x ABOVE the operating point and 2.7x above true error:
    max(0.01, rv) clamped on the first tick inflation bit and never released, pinning
    rv_final at EXACTLY 0.010000 on all four inflation arms with overconfidence_score
    bit-identical to 15 significant figures (-1.004111904519277) at asymmetry 0.6 AND
    0.8. WHY THE 2026-07-20 SMOKE PASSED 6/6 ANYWAY: it used true error mean 0.05, ~13x
    the substrate's real error scale, where 0.01 IS a floor with headroom -- an absolute
    constant validated at one scale silently became a clamp at another, which is why the
    repair is a RELATIVE bound and not a smaller absolute one. A soft clip ALONE would
    also not have sufficed: n_seeds_overconfident needs rv < 0.0037, unreachable under
    any bound anchored at 0.01. THE SIGN IS NOT THE BUG (the autopsy's candidate cause
    (b) is ruled out): inflation drove rv DOWN correctly, into a floor sitting ABOVE the
    OFF arm, so the run's inflation_lowers_rv precondition saw rv rise. Adds
    E3Config.waking_confidence_rv_floor_relative_frac (default 0.0 = absolute floor,
    bit-identical; >0 makes the bound that FRACTION of the new
    E3TrajectorySelector._wci_symmetric_rv_ref, a symmetric EMA of the same error at the
    same alpha advanced only on the inflation path -- i.e. the COUNTERFACTUAL un-inflated
    rv -- so the bound scales with the substrate's own error scale and caps
    overconfidence at 1 - frac, a bound relative to reality rather than a constant),
    E3Config.waking_confidence_rv_floor_mode ("hard" default = the original clip |
    "soft" = softplus) and E3Config.waking_confidence_rv_floor_softness (0.25, knee width
    as a fraction of the effective floor; inert while "hard"). The softplus is STRICTLY
    MONOTONIC, so a residual saturation can only SHRINK a dose separation, never collapse
    it to an exact tie -- a mis-set floor then degrades to a small LO/HI gap the new
    dose_saturation lint can see instead of the bit-identical arms that made 794
    unadjudicable without an autopsy. Both new knobs are no-op at default, so the ON path
    is bit-identical until they are set. Smoke at the MEASURED 794 scale (true error
    0.0037) 14/14 PASS: the OLD config reproduces the defect exactly (LO == HI == 0.01),
    the repaired config gives rv_final 0.0025377 (LO) vs 0.0021031 (HI), dose-ordered and
    both genuinely overconfident (+0.314 / +0.432). Pinned by 17 contracts in
    ree-v3/tests/contracts/test_sd076_rv_floor_headroom.py, which encode the DEFECT as
    well as the repair so a future simplification back to an absolute hard floor fails
    there rather than in a run; full tests/contracts 2128 passed 0 failed. Behavioural
    validation: V3-EXQ-794a, a same-question re-run of the identical 2x2 (the design was
    sound; only the drift source needed repair).
  notes: >
    NOT VALIDATED YET -- status candidate, behavioural validation pending as the MECH-204
    Phase 7 retest. Biological grounding is optimism / positive-outcome bias in waking
    belief updating, the drift the sleep-recalibration literature behind MECH-204
    (Hobson-Hong-Friston 2014; Walker & Stickgold 2006) presupposes. This is a FUNCTIONAL
    translation, not a mechanism claim about a specific neuromodulator; a valence-gated
    learning-rate account is V4 scope. Sign convention: SD-076 pushes rv DOWN, MECH-204
    recalibration pulls rv TOWARD the cumulative zero-point reference, so on a
    well-calibrated agent they approximately cancel and the experimental signal lives in
    the arms where one is ablated.

- id: SD-077
  title: "Common-mode-invariant (centered) super-ordinal goal-anchor cue key: the MECH-189 SuperOrdinalGoalMemory keys anchors on raw z_world cosine, which under SD-008 z_world under-differentiation measures the shared common-mode offset rather than the context, so every nursery contact reinforces one slot and anchor_count saturates at 1. Subtract a slow EMA common-mode baseline before every cue cosine. ree_core goal.py change, bit-identical by default."
  claim_type: design_decision
  subject: goal.common_mode_invariant_super_ordinal_cue_key
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-21
    needs_review: false
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-07-21"
  depends_on: [SD-008, SD-066]
  location: docs/architecture/sd_077_centered_super_ordinal_cue_key.md
  implementation_note: >
    IMPLEMENTED 2026-07-21 (ree-v3 ree_core/goal.py SuperOrdinalGoalMemory).
    Config GoalConfig.super_ordinal_cue_centering (default False = bit-identical OFF;
    no baseline tensor is allocated and no baseline arithmetic runs) and
    super_ordinal_cue_baseline_alpha (default 0.02, matching SD-066). Data flow:
    z_world -> observe() EMA baseline -> centered residual -> _best_match cosine ->
    contextual_complexity / retrieve. Keys are stored RAW and centered at comparison
    time, so a drifting baseline moves query and stored keys together. MECH-094:
    simulation_mode contexts never advance the baseline. No agent.py wiring change; no
    trainable parameters, so phased training does not apply. Contracts:
    ree-v3/tests/contracts/test_sd_077_centered_super_ordinal_cue_key.py (C1 default-off
    bit-identity, C2 centering separates, C3 no threshold setting substitutes, C4 lazy
    seed + MECH-094, C5 raw-key self-match under baseline drift, C6 persistence).
  functional_restatement: >
    V3-EXQ-669b self-routed on its pre-registered readiness gate R3 (max anchor_count
    >= 2): anchor_count = 1 in every arm and seed, so the load-bearing C1 (anchor_count
    delta) and C3 (complexity delta) criteria had zero cross-arm range. Measured
    2026-07-21 on the actual 669b Stage-0 forced-feed nursery (155 contexts, seed 101):
    raw world_obs pairwise cosine min 0.216 / mean 0.608 with 90.7 percent of pairs below
    0.8, but z_world pairwise cosine min 0.9641 / mean 0.9898 with ZERO pairs below 0.8,
    and ||mean(z_world)|| / mean||z_world|| = 0.9949. The nursery is richly diverse and
    the untrained encoder buries that diversity under a common-mode offset. Threshold
    tuning cannot substitute, and this is provable rather than merely observed:
    contextual_complexity = 1 - best_cosine and best_cosine >= 0.9641 everywhere, so
    complexity <= 0.036 under ANY threshold setting, strictly below 669b's pre-registered
    COMPLEXITY_MARGIN of 0.05 (measured mean over 160 fired writes: 0.0077). The failed
    run's own docstring proposed a LOWER merge_similarity, which is the wrong sign -- it
    moves more contexts into the REINFORCE branch and worsens saturation. Centering the
    cue key restores the range: residual pairwise cosine spans -0.760 to 1.000 (97.7
    percent of pairs below 0.8), giving 26 anchors and mean complexity 0.076 at
    n_slots=64 with 669b's own thresholds. This is the SD-066 fix applied to a second
    consumer of the same broken z_world geometry; the encoder-level repair remains the
    open SD-008 / SD-070 problem.
  substrate_note_2026_07_22: >
    The "assume this recurs" instruction in this claim's Architecture Context was acted
    on 2026-07-22 (session sad-newton-00451d): ree_core was swept for every absolute
    cosine / absolute-threshold gate on z_world or a z_world-derived key, each hit
    measured rather than inspected. TWO further genuine consumers were found and fixed,
    SD-078 (ARC-063 CandidateRuleField context key) and SD-079 (SD-039 Anchor.goal_match
    on z_goal). The sweep is complete and should not be redone. It also corrected the
    generalisation twice: the hazard is not confined to z_world but extends to anything
    INTEGRATED from it (z_goal carries the offset more strongly than its source), and
    "rank-based readouts are exempt" is too coarse -- a near-constant weighted additive
    term inside a ranking formula carries no ordering information even though the
    enclosing readout is nominally a ranking.

- id: SD-078
  title: "Common-mode-invariant (centered) CandidateRuleField context key: the ARC-063 field keys mint-block, recurrence bucketing and gate retrieval on a raw z_world context, which under SD-008 z_world under-differentiation sits in a ~0.98-cosine common-mode cone, so the mint-block fires against the first minted rule for every later context at any expressible threshold and the pool is structurally capped at ONE rule. Subtract a slow EMA common-mode baseline before every cue comparison. ree_core policy change, bit-identical by default."
  claim_type: design_decision
  subject: policy.common_mode_invariant_candidate_rule_field_context_key
  polarity: asserts
  status: candidate_substrate_landed
  live_status:
    reading: candidate_substrate_landed
    as_of: 2026-07-25
    needs_review: false
    evidence:
      from: "v3_exq_806_sd078_centered_rule_field_context_key_20260725T191042Z_v3"
      as_of: 2026-07-25
      verdict: "supports -- centering recovers a differentiated rule pool on the live CandidateRuleField (on_dist_mean 1.67 vs off 0.0; on_max_live 7.67 vs off 1.0; C1/C2/C3 all pass frac 1.0; z_world cone min-cosine 0.96). Substrate landed 2026-07-22 + validated; v3 gate cleared. Held at candidate pending a downstream consumer test."
  implementation_phase: v3
  evidence_quality_note: |
    V3-EXQ-806 (2026-07-25, PASS, supports) validated the landed SD-078 centering fix on the
    REAL CandidateRuleField: sd078_centering_recovers_differentiated_rule_pool. Raw z_world caps
    the rule pool at ONE (off_dist_mean 0.0, off_max_live 1.0) while centered yields a
    differentiated pool (on_dist_mean 1.67, on_max_live 7.67); C1 (dist above floor), C2 (min
    live rules), C3 (off reproduces the 654b pin) all pass at frac 1.0; z_world cone min-cosine
    0.96 confirms the common-mode geometry. Disposition (/governance 2026-07-25, session
    angry-ardinghelli-d99740-gov, user-chosen): CLEAR the v3 substrate gate + record supporting
    evidence, but HOLD at candidate (status candidate_substrate_landed) pending more evidence /
    a downstream consumer test -- not promoted to provisional this cycle. The encoder-level
    repair (SD-008 / SD-070) remains the separate open problem this fix routes around.

    [2026-07-26 governance, V3-EXQ-822 non_contributory (the downstream consumer test); failure_autopsy_816c-822_2026-07-26,
    confirmed]: the consumer test RAN. Self-route substrate_not_ready_requeue confirmed as genuinely unmet: the
    rule pool differentiates fine (on_rule_state_diff_mean 0.644, c1_pass) and activates (on_max_live 16 vs off 1),
    but propagation to the action bias is a structural zero on BOTH arms (on/off_prop_delta_mean 0.0) -- the
    trained bias-head consumer (rule_state -> action bias) is not wired/trained. non_contributory (the consumer
    behaviour could not be exercised); does NOT weaken SD-078. pending_retest_after_substrate. Substrate gap
    already actioned: SD-082 (rule-selection action-bias consumer) built same-day (ree-v3 d4f758082d) and
    V3-EXQ-822a queued to validate it.

    [2026-07-29 governance, V3-EXQ-822a + V3-EXQ-822b non_contributory; failure_autopsy_batch-822a-826-817a-827_2026-07-26
    + failure_autopsy_2026-07-28-sweep, both confirmed]: SD-082's validation run (822a) reproduced the identical
    structural-zero propagation (on/off_prop_delta_mean 0.0) despite SD-082's centering+soft-tanh fix correctly
    engaged and a strong, non-degenerate upstream signal (on_rule_state_diff_mean 0.644) -- second consecutive
    identical failure of the same readiness gate. The follow-on head-internals diagnostic (822b) traced the
    actual cause: REINFORCE never updates rule_bias_head's last linear layer (unconditional weight-norm read
    shows head_untrained across both runs). Both non_contributory, not weakening SD-078 -- the gap is in SD-082's
    consumer wiring, not SD-078's rule-pool differentiation. SD-082 substrate entry amended with both failure
    records; still candidate_substrate_landed, still pending_retest_after_substrate.
  claim_level: architectural
  pending_retest_after_substrate: true
  registered_utc: "2026-07-22"
  depends_on: [SD-008, SD-066, SD-077, ARC-063]
  location: docs/architecture/sd_078_centered_candidate_rule_field_context_key.md
  implementation_note: >
    IMPLEMENTED 2026-07-22 (ree-v3 ree_core/policy/candidate_rule_field.py
    CandidateRuleField). Config CandidateRuleFieldConfig.cue_centering (default False =
    bit-identical OFF; no baseline tensor is allocated) and cue_baseline_alpha (default
    0.02, matching SD-066 / SD-077 -- z_world is a per-tick encoding, not an integrator).
    REEConfig knobs crf_cue_centering / crf_cue_baseline_alpha plumbed through all three
    from_dims sites and read in agent.py's CRF config build. Data flow: z_world context
    -> observe() EMA baseline (advanced in step(), waking only) -> centered residual ->
    mint-block cosine + _context_bucket sign pattern + gate_and_select cosine. The
    recurrence BUCKET is centered too, deliberately: it is a sign pattern, i.e. an
    absolute threshold at zero, and is common-mode dominated in the same way (1 bucket
    raw vs 20 centered). context_tags are stored RAW and centered at comparison time;
    the baseline PERSISTS across reset() because it is cue geometry, not rule content.
    MECH-094: simulation_mode ticks never advance the baseline. No trainable parameters,
    so phased training does not apply. Contracts:
    ree-v3/tests/contracts/test_sd_078_centered_candidate_rule_field_context_key.py
    (C0 fixture geometry guard, C1 default-off bit-identity + reproduced 654b signature,
    C2 centering separates, C3 no mint-block threshold in [0.5, 0.94] substitutes,
    C4 lazy seed + MECH-094, C5 raw-tag self-match under drift, C6 bucket is centered).
  functional_restatement: >
    Measured 2026-07-22 by driving the real CandidateRuleField over the real V3-EXQ-669b
    Stage-0 nursery context stream (160 contexts, seed 101, alpha_world=0.9): raw
    world_obs pairwise cosine min 0.3000 (60.2 percent of pairs below 0.8), but z_world
    min 0.9767 and e2.world_forward(z_world, a) min 0.9426, both with ZERO pairs below
    0.8. Resulting decision statistics: raw z_world gives n_minted 1, max live rules 1,
    max_pairwise_rule_dist 0.0000 and ONE distinct context bucket; centered gives 9 rules,
    dist 1.7011 and 20 buckets. The pool is structurally capped at one rule because every
    pairwise context cosine exceeds any threshold the config can express, so
    crf_max_pairwise_rule_dist == 0.0 in every V3-EXQ-654b/654d ARM_ON cell is a
    TAUTOLOGY (the metric needs two concurrent rules) rather than the retire-churn symptom
    those autopsies read it as. Large cumulative n_minted in those runs is consistent with
    this: rules retire, and a lone re-mint then succeeds against an empty pool. Both
    existing mitigations are measured ineffective and are deliberately left in place
    unchanged: mature_mint_block_threshold raised to 0.8 cannot clear a 0.9426 floor
    (pinned across [0.5, 0.94] as contract C3), and crf_context_from_e2_world_forward,
    documented as "the structural relief" for low raw-z_world spread, routes to a
    different point inside the same cone and still yields 1 rule at dist 0.0000. This is
    the SD-066 fix applied to a third consumer; the encoder-level repair remains the open
    SD-008 / SD-070 problem.

- id: SD-079
  title: "Common-mode-invariant (centered) z_goal cue for goal-anchor match: SD-039's Anchor.goal_match is a raw cosine over z_goal, which as an EMA attractor pulled toward z_world inherits the SD-008 common-mode offset more strongly than its source, so goal_match spans ~0.01 across an anchor pool, the MECH-292 goal_match_floor excludes nothing and the MECH-339 outshining gate is identically zero. Subtract a slow EMA common-mode baseline before the match. ree_core hippocampal change, bit-identical by default."
  claim_type: design_decision
  subject: hippocampal.common_mode_invariant_goal_anchor_match
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-26
    needs_review: false
    evidence:
      from: "v3_exq_823_sd079_ghost_goal_retrieval_consumer_20260726T075327Z_v3"
      as_of: 2026-07-26
      verdict: "supports -- the downstream consumer test (goal retrieval + rumination guard) PASSED cleanly and non-degenerately (all preconditions met; C1/C3 load-bearing PASS, C2 PASS). Third consecutive PASS (806/807/823) with no conflicting evidence. Promoted candidate_substrate_landed -> provisional /governance 2026-07-26, user-approved."
  implementation_phase: v3
  evidence_quality_note: |
    V3-EXQ-807 (2026-07-25, PASS, supports) validated the landed SD-079 centering fix on a real
    24-anchor pool: sd079_centering_unpins_goal_match_and_downstream_gates. Raw z_goal collapses
    goal_match to a 0.009 spread so the MECH-292 floor excludes nothing and the MECH-339
    outshining gate is identically zero (off_gate_nonzero 0); centered gives 0.999 spread, the
    floor excludes 3/24 and the gate is live on ~3.3/24. C1 (on spread), C2 (off reproduces the
    pin), C3 (on gate live) all pass at frac 1.0. Disposition (/governance 2026-07-25, session
    angry-ardinghelli-d99740-gov, user-chosen): CLEAR the v3 substrate gate + record supporting
    evidence, but HOLD at candidate (status candidate_substrate_landed) pending more evidence /
    a downstream consumer test -- not promoted to provisional this cycle. Fourth instance of the
    SD-066 pattern and the first on a DERIVED latent; the SD-008 encoder repair stays separate.

    [2026-07-26 governance, V3-EXQ-823 supports (the downstream consumer test), clean PASS -- no autopsy
    needed]: sd079_centering_enables_reliable_goal_retrieval_and_rumination_guard. All preconditions met
    (z_goal common-mode cone 0.986 >= 0.9 floor; enrolled pool 64 anchors >> 6 floor); load-bearing criteria
    C1_on_retrieval_discriminates and C3_on_rumination_guard_live both PASS non-degenerately, plus
    C2_off_reproduces_pin. This is the downstream consumer test the note above was held pending -- unlike
    SD-078's sibling consumer test (V3-EXQ-822, non_contributory), this one PASSED cleanly. Surfaced and
    user-approved for promotion this cycle -- see status/live_status above (candidate_substrate_landed ->
    provisional).
  claim_level: architectural
  registered_utc: "2026-07-22"
  depends_on: [SD-008, SD-039, SD-066, SD-077, MECH-292]
  location: docs/architecture/sd_079_centered_goal_anchor_match.md
  implementation_note: >
    IMPLEMENTED 2026-07-22 (ree-v3 ree_core/hippocampal/anchor_set.py +
    ree_core/hippocampal/ghost_goal_bank.py). Config AnchorSetConfig.goal_cue_centering
    (default False = bit-identical OFF) and goal_cue_baseline_alpha (default 0.05, NOT
    SD-066's 0.02 -- see functional_restatement). Plumbed via
    REEConfig.from_dims(goal_cue_centering=, goal_cue_baseline_alpha=) to the nested
    AnchorSetConfig. The baseline lives on AnchorSet (the pool owner) and GhostGoalBank
    reads it through its existing anchor_set reference, so both consumers share one
    baseline. Anchor.goal_match gains an optional baseline argument defaulting to None,
    which is the identity, so the pre-SD-079 call form is bit-identical. Snapshots are
    stored RAW and centered at comparison time. BOTH the write path (write_anchor with a
    goal_payload) and the read paths (query_by_goal_match, GhostGoalBank.rank) advance the
    baseline -- this is load-bearing, not cosmetic: advancing on reads alone lets the
    baseline be lazily seeded from the QUERY itself, which centers every snapshot against
    the query and drives every match to 0.0 (measured; contract C6). MECH-094: simulation
    cues never advance it, and on the write path this holds by construction because a
    goal_payload is only attached on a non-simulation write. No trainable parameters.
    Contracts: ree-v3/tests/contracts/test_sd_079_centered_goal_anchor_match.py (C0-C7).
  functional_restatement: >
    Measured 2026-07-22 on the V3-EXQ-669b Stage-0 nursery, seed 101: z_goal pairwise
    cosine min 0.9878 / mean 0.9969 with ||mean(z_goal)|| / mean||z_goal|| = 0.9987 --
    tighter than z_world's own 0.9767 / 0.9964, because z_goal integrates it. Scoring a
    real 24-anchor pool: goal_match spread 0.0111 raw vs 0.9709 centered. Two distinct
    failures follow. First, the ranking term goal_match_weight * goal_match varies by
    0.011 across the pool, so MECH-292's motivational-relevance channel contributes
    essentially no ordering information while appearing to be a four-term composite --
    which is why "rank-based readouts are exempt from this hazard" is too coarse a rule.
    Second and more serious, both ABSOLUTE gates downstream are unconditionally dead: the
    goal_match_floor rumination guard excluded 0 of 24 anchors, and MECH-339's outshining
    gate clip((outshine_pivot - goal_match) / outshine_pivot) with pivot 0.5 evaluated to
    EXACTLY 0.0 for every anchor, so MECH-339's composite cue contributes an identically
    zero context term the moment it is enabled and the constraint would read as falsified
    by a mechanism that never ran. Centered: 9 of 24 excluded by the floor, gate nonzero
    on 9 of 24. The alpha default of 0.05 is measured, not inherited: z_goal drifts, so a
    baseline tuned for a stationary cue lags it -- over seeds 101/202/303 the goal_match
    spread is 0.0942 / 0.1508 / 0.3319 at alpha 0.02 versus 0.9995+ at 0.05, while 0.2 and
    0.5 over-track and drive 14 and 18 of 20 anchors below the floor. Fourth instance of
    the SD-066 pattern and the first on a DERIVED latent rather than z_world itself.

- id: SD-080
  title: "E2.action_object_head receives zero gradient from every REE training path, so the action-object space O that SD-004's hippocampal map backbone navigates is a frozen random projection fixed at initialisation, not the learned world-effect compression SD-004 specifies. O is state-invariant rather than consequence-structured, and the hippocampal CEM searches and refits inside it."
  claim_type: design_decision
  subject: e2.action_object_consequence_grounding
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-07-22"
  depends_on: [SD-004, ARC-018, SD-056]
  evidence: []
  location: evidence/planning/action_object_invariance_spike_2026-07-22.md
  evidence_quality_note: |
    V3-EXQ-809 (2026-07-23, action-object init-invariance prior probe; diagnostic; confirmed
    failure_autopsy_V3-EXQ-809_2026-07-25; PROMOTES NOTHING) -> supports SD-080. The prior probe
    re-confirms the frozen-AO-head claim on the live substrate: the action_object_head receives
    zero gradient and stays at initialisation, so its content reaches the hippocampal proposals
    but never the behaviour. Corroborates SD-080's core assertion (O is a frozen random projection,
    state-invariant rather than consequence-structured). The same run does_not_support SD-004's
    grounding RATIONALE (see SD-004). Stays candidate. Applied /governance 2026-07-25 (session
    angry-ardinghelli-d99740-gov, user-approved).

    [2026-07-26 governance, V3-EXQ-817 non_contributory; failure_autopsy_batch-793a-817-819_2026-07-26,
    confirmed -- CORRECTS the note above]: V3-EXQ-817 (3-arm consequence-grounding falsifier) self-routed
    substrate_not_ready_requeue, but this label is a MISNOMER and the prior note's reading of it was wrong.
    The manifest shows ARM_1's M0_action_object_head_param_delta_l2_from_frozen is ~1.3-1.47 (0.0 is the
    ARM_0/frozen value) -- the AO head DID train; it did NOT collapse onto ARM_0. What actually failed is
    that the delta-prediction grounding OBJECTIVE did not induce the targeted representational change (M5
    r2 fell only 0.994->0.982, needed <=0.95; M6 within-pair spearman rose only to 0.085, needed >=0.15,
    with high seed variance). grounding_took=false gated the load-bearing behavioural criterion, which was
    never evaluated -> non_contributory for both SD-080 and SD-004. SD-080's frozen-random-projection
    defect stands as confirmed by V3-EXQ-809; 817 adds no new direction on it. Re-queue 817a with a
    stronger grounding objective (e.g. contrastive consequence loss) and recalibrated M5/M6 thresholds.

    [2026-07-29 governance, V3-EXQ-817a mixed (SD-080: non_contributory, SD-004: weakens);
    failure_autopsy_batch-822a-826-817a-827_2026-07-26, confirmed]: 817a's redesigned grounding objective
    succeeded representationally (r2 0.98->0.72, clean paired + shuffled controls, content-vs-traffic
    discriminated) -- resolving 817's competence_implementation_gap and confirming the OBJECTIVE, not the
    substrate, was the prior blocker. But grounding this representation produced NO behavioural benefit
    (harm_rate flat/nominally worse across frozen/grounded/shuffled arms). SD-080 stays non_contributory:
    this is a correctness-not-capability finding about a bespoke, newly-added training path, not evidence
    that an existing REE path already grounds O -- SD-080's "zero gradient from every REE path" framing
    about the DEFAULT substrate is not falsified. (SD-004's specific efficiency rationale is weakened --
    see SD-004.) Stays candidate.
  functional_restatement: >
    Scoping spike 2026-07-22 (session epic-burnell-995d28). Four measurements, all on the
    live substrate: 99.5% of action-object variance is explained by the action label
    alone; the 5-action pairwise-distance matrix varies <1% across 120 world states;
    within-action-pair consequence correlation is ~0; and the head's parameters are
    bit-identical after 40 warmup episodes (delta L2 exactly 0.0). The last of these is
    the mechanism rather than a symptom -- no REE training path writes a gradient to
    action_object_head, so the projection never departs from its initialisation. The
    consequence is that SD-004's stated rationale ("E2 produces COMPRESSED action-object
    representations", enabling planning horizons beyond raw state space) is not
    instantiated: a state-invariant O carries no world-effect structure for the
    hippocampal map to be a backbone OF. This does not by itself impeach SD-004's
    behavioural evidence (EXQ-114 PASS 4/4, 99.2% harm reduction under MAP_NAV) -- it
    relocates what that evidence can be attributed to, which is a governance question and
    is deliberately left open here.
  relation_note: >
    Same failure FAMILY as SD-056, different site. SD-056 fixed action-collapse in
    world_forward with a contrastive objective, having diagnosed the cause as "under
    reconstruction-shaped training the state-dominated solution is the local minimum". The
    action-object head has the identical input geometry (Linear(world_dim + action_dim,
    ...), action = 4/36 of the input) but a STRONGER form of the same problem: it has no
    training signal at all, so it never even reaches a local minimum. SD-056's remedy is
    the natural template, but its objective is not directly reusable -- SD-056 pushes
    ACTIONS apart, whereas the ao head's deficiency is that STATES are not represented.
  falsification_note: >
    Proposed falsifier in evidence/planning/action_object_invariance_spike_2026-07-22.md
    Sec 4.2. SD-080 is falsified if a training path writing gradient to
    action_object_head is identified in the live substrate, or if action-object
    representations are shown to vary with world state beyond the measured <1%.

- id: SD-075
  title: "Episode-boundary continuity for the SD-069 phasic surprise-EMA baseline, plus convergence-gated event accounting: carry the baseline across episode boundaries so it reflects the agent's surprise distribution rather than the current episode's first tick, and split event counts into pre-warmup vs converged so a short-episode cell reports honestly or declares itself uninformative instead of emitting a near-zero count that a MIN-across-cells precondition treats as a real measurement. ree_core regulator change, no-op by default."
  claim_type: design_decision
  subject: phasic.ema_episode_continuity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-19
    needs_review: false
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-07-19"
  depends_on: []
  location: docs/architecture/sd_075_phasic_ema_episode_continuity.md
  functional_restatement: >
    An event detector defined as a relative excess over a running baseline is only
    meaningful once that baseline has converged. PhasicSurpriseBurst.reset() cleared the
    surprise-EMA cold at every episode boundary, and the first waking tick of an episode
    cannot fire an event because it seeds the baseline. With surprise_ema_decay 0.1 the
    baseline has a ~10-tick time constant, so a seed whose episodes are shorter than that
    never runs against a converged baseline and n_event_ticks becomes a function of episode
    LENGTH rather than of surprise. Measured by V3-EXQ-779b: seed 23 ran ~6.9-step episodes
    against seeds 29/37 at 300 steps, a 43x spread; raising seed 23's budget from 835 to
    2400 env steps delivered those steps as 345 episodes of ~7 rather than longer episodes,
    and the phasic_fires_real_events precondition did not move (6 vs threshold 10). The MIN
    merely migrated to another short-episode cell despite ~2.85x exposure. No step-budget
    increase can reach this; the binding axis is episode length. Capability is RULED OUT as
    the explanation -- burst_level_max is 1.00 in every PHASIC-ON cell including both
    seed-23 cells. SD-075 supplies (a) a baseline that can be carried across episodes and
    (b) a convergence gate on the ACCOUNTING, so a starved cell is distinguishable from a
    genuinely low-surprise one.
  implementation_note: >
    IMPLEMENTED 2026-07-19 in ree-v3/ree_core/regulators/phasic_surprise_burst.py, surfaced
    as REEConfig.phasic_burst_baseline_continuity ("reset" default | "carry") and
    REEConfig.phasic_burst_warmup_ticks (0 default = OFF, -1 = DERIVE as
    ceil(3 / surprise_ema_decay) = 3 EMA time constants, positive = verbatim; below -1
    raises so a typo cannot silently mean OFF). Both no-op by default, so every existing
    consumer of phasic_surprise_burst is bit-identical. Under "carry", reset() preserves
    _surprise_ema and _ema_initialized while still clearing the envelope, the cached
    temperature delta, and the per-episode diagnostics, so no in-flight burst leaks across
    a boundary. Lifetime tick/episode counters survive reset() in both modes. The
    convergence gate is ACCOUNTING ONLY and deliberately does NOT suppress the burst
    (user-confirmed 2026-07-19): during warmup the regulator still fires and still perturbs
    the softmax temperature, and only the counts split into n_events_prewarmup /
    n_events_converged. Suppression was rejected because it would change agent BEHAVIOUR in
    the first ticks of a lifetime, layering a second mechanism change on the continuity fix
    and confounding the MECH-063 (ii) retest with it. get_state() gains a config echo
    (baseline_continuity, warmup_ticks, warmup_ticks_derived) and lifetime accounting
    (lifetime_ticks, lifetime_episodes, baseline_converged, n_converged_ticks,
    n_prewarmup_ticks, n_events_converged, n_events_prewarmup); consumers should read
    n_events_converged over n_converged_ticks, not the per-episode n_events. A partial-decay
    "warm" mode is deliberately not built (a second knob with no question needing it).
    22 contracts in tests/contracts/test_sd075_phasic_ema_episode_continuity.py (D1-D9);
    SD-069's own 8 contracts and tests/test_flag_inertness.py pass unchanged; full ree-v3
    suite 1772 passed. Routed by
    evidence/planning/failure_autopsy_V3-EXQ-779b_2026-07-19.json targets[0], priority 1.
    LIVE-AGENT SMOKE 2026-07-19 (25 episodes x 7 steps on seed 23, the 779b shape,
    instantaneous_pe, untrained): "reset"/gate-off reproduces the failed precondition
    EXACTLY at 6 events; "carry"/gate-off lifts the same exposure to 12, clearing
    MIN_EVENT_TICKS 10. With the gate on, n_events_converged is 0 in BOTH continuity modes
    because every event on an untrained agent's PE stream lands inside the first 30 ticks
    and the stream then settles. The two legs therefore DISAGREE about this cell -- leg (a)
    alone reads "12, passes", leg (a)+(b) reads "0 converged, UNINFORMATIVE" -- and the
    build deliberately does not choose between them, because the autopsy target sentence
    admits both and choosing is a retest-design decision rather than a substrate default.
  notes: >
    NOT VALIDATED YET -- status candidate, validation experiment pending. The contracts
    demonstrate the MECHANISM on synthetic surprise streams (D3: identical exposure,
    "reset" recovers 0 events where "carry" recovers many on an early-spike short-episode
    shape; D3b: tripling the budget as more short episodes leaves the per-episode yield
    unchanged, which is 779b in miniature). That is a unit-level demonstration, NOT
    experimental evidence, and must not be cited as such. Blast-radius items from the
    routing brief, both resolved as no-action: the probe_warmup.py WarmupRecipe
    probe_max_episodes 40 vs probe_max_env_steps 4000 exposure was ALREADY fixed by the
    RolloutBudget audit (ree-v3 6ac2a0d, probe_max_episodes 0 = DERIVE); and the warmup
    cache-key question does not arise because SD-075 changes no WarmupRecipe field -- though
    note independently that _warmup_key() hashes compute_substrate_hash(scope=None), the
    whole substrate, so ANY ree_core edit busts every cached warmup, which is deliberate
    there and benign. RETEST CONSTRAINT: MECH-063 sub-claim (ii) is
    pending_retest_after_substrate against this build, but the re-derive brake fired both
    halves on that claim (3rd MECH-063 autopsy) and explicitly refuses another lettered
    iteration of the tonic/phasic probe against the current regulator. Do NOT queue a
    V3-EXQ-779c. A redesign of a DIFFERENT mechanism under a new EXQ number remains
    permitted; any such retest must declare baseline_continuity="carry" plus a convergence
    gate and must consume n_events_converged with an uninformative-cell path rather than
    feeding a raw MIN. WARMUP-RESCUE SPIKE 2026-07-19 (diagnostic spike, NOT an experiment
    and NOT evidence -- 3 seeds, one arm, no fingerprinting; driver
    ree-v3/experiments/_scratch/sd075_warmup_rescue_spike.py): the smoke's UNTESTED
    hypothesis that a TRAINED agent keeps producing excesses past tick 30 was TESTED and
    HOLDS. SD-074 warm_agent over num_episodes {0,10,40} x seeds {11,23,29}, then a
    1200-env-step read under "carry" + warmup_ticks=-1: n_events_converged is non-zero in
    all 9 cells, and MIN over the six WARMED cells is 14 (clears MIN_EVENT_TICKS 10) versus
    MIN 1 over the three untrained cells. The load-bearing detail is the DECOUPLING: on
    untrained agents the converged count tracks episode COUNT (seeds 11/23, 4x300-step
    episodes, 3 and 1 events; seed 29, 52 short episodes, 110 events at 2.1/episode -- under
    "carry" an episode boundary itself reliably clears the trigger, so that 110 is plausibly
    boundary-locked rather than surprise-locked). Warmed, that dependence breaks: seed 29 at
    10 episodes ran the SAME 4x300-step shape and still recorded 14. Per-converged-tick event
    rate 1.0-2.6% untrained-long-episode vs 11.3-25.4% across all warmed cells;
    n_events_prewarmup falls 17/18/16 -> 5/7/3. No monotone training dose (40 episodes is not
    better than 10), so read this as "warm at all", not as a dose-response. CONSEQUENCE: a
    retest declaring "carry" + the gate should ALSO warm via probe_warmup (num_episodes >= 10)
    and install a fresh PhasicSurpriseBurst post-warmup -- the regulator contributes no
    state_dict keys, so otherwise a cache-HIT and cache-MISS agent enter the read with
    different lifetime counters. Also corrects the smoke: n_events_converged = 0 was specific
    to its 25x7-step shape, not a general property of untrained agents (a longer read recovers
    1-3 events with no warmup at all). Still does NOT license a V3-EXQ-779c.
  validation_experiment: none queued -- blocked by the MECH-063 re-derive brake (no V3-EXQ-779c); a successor must be a new-number redesign of a different mechanism, not a lettered tonic/phasic iteration

- id: SD-074
  title: "Trained-enough agent substrate for read-only control-plane telemetry probes: a warmup/checkpoint stage that brings the agent to a non-degenerate E3 action-value landscape BEFORE telemetry collection begins, and records the realised per-seed saturation distribution so informative-seed yield is auditable rather than inferred. Harness-level training-regime enrichment (experiments/_lib), NOT a ree_core mechanism change."
  claim_type: design_decision
  subject: probe.trained_enough_agent_warmup
  polarity: asserts
  status: candidate
  evidence_quality_note: >
    [2026-08-16 governance, GFLAG-0036, cycle cranky-driscoll-126a36] V3-EXQ-784 (SD-074 probe_warmup de-saturation sweep, PASS 2026-07-18) is downgraded to PROVISIONAL: its headline informative_yield / target_met rest on a premise the manifest itself states and which is FALSE. sd074_note asserts that measure_action_mass is non-destructive, so reading at one checkpoint leaves the agent bit-identical for continued training. Measured on ree-cloud-5 2026-08-15: a probe read leaves 21 non-state_dict attributes changed, 17 of which SURVIVE agent.reset(). The load-bearing ones are agent._self_experience_buffer / _world_experience_buffer, which warmup_train samples via agent.compute_prediction_loss() -- so eval-rollout data enters the E1 training pool. Those buffers cap at 1000 while 784 reads 1081-1401 env steps, i.e. FULL DISPLACEMENT of the training pool by evaluation data. A controlled 2-seed replication of 784 ladder at its real config (budget-0 control cells bit-identical, as required) shows all six post-first-read cells differing by 0.011-0.146 on D_action_mass and TWO OF SIX FLIPPING THE SATURATION REGIME -- exactly the classification 784 informative_yield headline counts. No status change and no evidence_direction change is applied on this basis alone: the defect is in the measurement premise, not in the claim, and re-deriving the corrected yield needs the fix plus a re-run. Treat any downstream citation of 784 informative_yield / target_met as provisional until then. Full write-up and recommended fix: evidence/planning/probe_warmup_nonbuffer_audit_2026-08-15.md.
  live_status:
    reading: candidate
    as_of: 2026-07-18
    needs_review: false
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-07-18"
  depends_on: []
  location: docs/architecture/sd_074_probe_warmup_trained_enough_agent.md
  functional_restatement: >
    A gain/bias regulator is only measurable when the distribution it modulates has
    dynamic range. The 2x2 read-only telemetry-probe family measures MECH-320
    tonic_vigor score-bias and MECH-313 noise_floor temperature on the E3 pre-commit
    softmax while running an UNTRAINED agent, whose action-value distribution is
    degenerate on ~64% of seeds. Measured by V3-EXQ-777a: 7 of 14 seeds ceiling-pinned
    and 2 floor-pinned on D_action_mass_mean, informative-seed yield 4 of 14 (28.6%),
    and corr(distance of D from saturation, norm_v_score) = 0.884 -- the quantitative
    statement that the score axis's measurable authority is largely DETERMINED by how
    far the action-value mass sits from its 0/1 bounds. That caps power: the design
    needs ~51 informative seeds, i.e. ~177 raw seeds and ~31 h, which is infeasible.
    This is substrate, not sampling: V3-EXQ-777a's sample-driven stopping worked
    perfectly (all 56 cells reached 250 fresh E3 selections, zero starved cells) and
    the saturation rate barely moved from its starved predecessor. SD-074 supplies a
    warmup that de-saturates the landscape before measurement, targeting a MAJORITY of
    seeds with D_action_mass_mean strictly inside (0.05, 0.95), and records the
    realised per-seed distribution so yield is auditable.
  implementation_note: >
    IMPLEMENTED 2026-07-18 as ree-v3/experiments/_lib/probe_warmup.py (harness-level
    only; nothing under ree_core/ touched, no config default moved, no existing script
    imports it, so backward compatibility is total by construction). Composes landed
    modules rather than writing a fourth training loop: goal_pipeline_tier1.warmup_train,
    the baselines/maturation_curriculum cache discipline, and sample_driven_rollout for
    the de-saturation read. Gate policy (user-confirmed): RECORD, do not abort -- only
    assert_any_informative() raises, and only when ZERO seeds de-saturated. Regime
    (user-confirmed): target_env only, num_episodes swept. Three hazards found and
    defended: (H1) e3._running_variance is a plain Python float (e3_selector.py:291),
    NOT a register_buffer, and feeds commit_variance (:2703) upstream of the measured
    distribution -- fresh 0.500 vs warmed 0.0092, a ~54x drift state_dict alone would
    silently discard; carried explicitly with an asserted round trip. (H2) all four
    777a arms verified to share an identical 194-key state_dict (TonicVigor/NoiseFloor
    are zero-parameter non-nn.Module), so one warmed checkpoint per seed serves all
    four and they differ ONLY in regulator scalars at the e3.select() call site;
    asserted, not assumed. (H3) the consumer's generate_trajectories capture is
    instance-level and NOT in state_dict -- if lost after load_state_dict the run
    self-routes to sample_starvation_requeue, i.e. a lost patch masquerades as a
    sampling bug, the exact misdiagnosis this lineage already made twice. Measurement
    is non-destructive: no_grad+eval stop gradients but not the plain-Python
    accumulators nor the registered plasticity buffers (e3_selector.py:373-462), so
    both are snapshotted and restored (verified exact, max|dw|=0.000e+00). Smoke test
    PASS 2026-07-18. Routed by
    evidence/planning/failure_autopsy_MECH-063-777a-779a-cluster_2026-07-18.json
    targets[0], which fired the re-derive brake on the 777 lineage.
  notes: >
    NOT VALIDATED YET -- status candidate, validation experiment pending. The only
    de-saturation reading so far is a smoke-level sanity indication at n=1 seed and a
    toy budget (untrained 0.9949 ceiling-saturated, reproducing the autopsy failure
    mode, versus 0.51-0.67 headroom after 3 warmup episodes); that is NOT evidence and
    must not be cited as such. Deliberately out of scope: V3-EXQ-777a's c1_robust bar
    is (mean_sin - pstdev_sin) > MARGIN, a POPULATION dispersion that does not shrink
    with n and is therefore unreachable at any sample size; the successor experiment
    author must re-express it against a standard error. Per the autopsy that repair is
    necessary but NOT sufficient -- the binding constraint was always the informative
    seed yield, which is what this SD moves.

- id: SD-070
  title: "z_world P0 anti-collapse encoder-training recipe: a mini-batched P0 that trains the SD-005 split-encoder world path on STATIC scene-structure targets derived from world_obs alone (hazard/resource presence + bucketed nearest-Chebyshev distance), with class-balanced CE and a VICReg-style variance/covariance anti-collapse penalty, replacing the SD-009 event-contrastive + SD-018 P0 that was measured to COLLAPSE z_world to participation ratio ~1.06 at world_dim=128. Supplies the trained encoder that SD-031 / E2WorldForward P1 and V3-EXQ-783 both require."
  claim_type: design_decision
  subject: latent.zworld_p0_anticollapse_recipe
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-18
    needs_review: false
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-07-18"
  depends_on:
    - SD-005
    - SD-018
  location: docs/architecture/sd_070_zworld_p0_anticollapse_recipe.md
  functional_restatement: >
    The P0 the substrate prescribed for training the z_world encoder (SD-009
    event-contrastive CE + SD-018 resource-proximity MSE, online at batch=1) does not
    produce a trained encoder -- it collapses z_world onto a single effective dimension.
    Measured 2026-07-18 at world_dim=128, seed 42, 40 eval episodes: untrained
    participation_ratio 9.21 / contrast_ratio 0.1222, versus 1.06 / 0.0726 after the
    prescribed P0. Three measured faults drive it. (1) The SD-009 target is unlearnable
    from the channel its loss reads: transition_type is a property of the TRANSITION
    (t-1 -> t) while z_world is a static single-frame encoding, and an MLP-128 probe on
    raw world_obs with no encoder in the path scores AT OR BELOW CHANCE (lift -0.014
    3-class, -0.060 on a repaired 6-class map), while the same label probes at +0.240 /
    +0.427 from the BODY delta that SD-005 routes to z_self. (2) Nothing penalises
    collapse -- a ~95% class-0-saturated CE plus one scalar regression are both served by
    a 1-D representation; notably the collapse is NOT the multiplicative precision gate,
    whose sigmoid moves only 0.4966-0.5074 while the encoder's FUNCTION collapses. (3) The
    loop is online at batch=1, leaving variance/covariance statistics undefined. SD-070
    answers each: static scene-structure grounding targets the world channel demonstrably
    determines (probed 0.943-0.965 balanced accuracy from raw world_obs), class-balanced
    CE, a VICReg variance/covariance penalty whose COVARIANCE term is the
    participation-ratio lever, an optional world_obs reconstruction head, and mini-batching
    over a rollout buffer.
  implementation_note: >
    IMPLEMENTED 2026-07-18 (session admiring-poincare-1c80af) via /implement-substrate.
    Module: ree-v3/ree_core/latent/zworld_p0.py (ZWorldP0Config + ZWorldP0Trainer, plus
    the pure functions scene_structure_targets / variance_covariance_penalty /
    balanced_class_weights / entity_presence_mask / chebyshev_offsets). BIT-IDENTICAL OFF
    BY CONSTRUCTION, not by flag: SD-070 adds NO field to LatentStackConfig, NO head to
    SplitEncoder and NO method to REEAgent -- it operates on an existing LatentStack from
    outside and the auxiliary heads belong to the trainer, so nothing runs unless an
    experiment explicitly constructs a trainer and there is no flag that can be left in the
    wrong state (pinned by contracts C6). Trains exactly world_encoder +
    world_precision_logit, and is contract-pinned NOT to touch the z_self path.
    VALIDATION at config defaults, world_dim=128, 3 seeds: participation ratio 9.21->5.19
    (s42), 6.63->5.41 (s43), 8.56->4.64 (s44); against the V3-EXQ-783 anti-collapse gate
    computed as that harness computes it, retained_fraction 5.079/8.132 = 0.625 (needs
    >=0.50) and absolute 5.079 (needs >=2.0), both PASS. Contrast ratio raised on 3/3 seeds
    (mean 0.137->0.238), clearing the untrained 0.13-0.15 band. DISCRIMINATIVE, not
    vacuously un-collapsed: held-out balanced-accuracy lift +0.23 to +0.47 across all four
    grounding heads on all three seeds (the trainer always reports this, because an
    anti-collapse gate can otherwise be satisfied by a regulariser that learns nothing).
    31 contracts in ree-v3/tests/contracts/test_sd070_zworld_p0.py; full suite 1590 passed.
    VICReg (Bardes/Ponce/LeCun 2022) is borrowed as ENGINEERING counsel for a measured
    collapse failure mode and carries no architectural authority; two deviations from the
    standard version are forced by measurement -- no invariance term / no augmentation
    pairs (there are no augmented views here), and a covariance weight ~1000x the published
    ratio (measured: w_cov=0.04 -> PR 1.80, w_cov=50 -> PR 4.02).
  evidence_quality_note: >
    Substrate validation only -- this is an implemented recipe with a measured
    anti-collapse and discriminativeness result, NOT yet governance evidence for any
    mechanism claim. Scope limits: measured at world_dim 128 and 32 on CausalGridWorldV2
    at the x724 ENV_KWARGS rung under one exploratory epsilon-mixed policy, 3 seeds; the
    grounding targets are specific to the 5x5x7 local-view layout. Bears on INV-088's
    ANTECEDENT only -- NOT on the INV-088 coupling leg (V3-EXQ-744a's WEAKENS reading
    stands), NOT on MECH-459 / return-scale, and NOT on either leg of the live
    GOV-FANOUT-1 discrimination (V3-EXQ-780 vs V3-EXQ-781). SD-070 ROUTES AROUND SD-009
    rather than adjudicating it: the SD-009 target/channel mismatch measured in the course
    of this work is recorded for a future /governance cycle in
    evidence/planning/sd009_event_contrastive_channel_mismatch_2026-07-18.md, and NO
    SD-009 status, confidence, or evidence weighting was changed by this session.
  validation_experiment: V3-EXQ-783

- id: SD-071
  title: "First-order consolidation-phase readouts validated: the NREM slot-filling and SWS pattern-completion legs of the SD-068 lesion harness are content-contingent instruments -- injected-content transfer fidelity (nrem) and cosine retrieval margin against injected prototypes probed with the unscaled base (sws) -- each clearing the same load-bearing C1 null-slope ceiling."
  claim_type: design_decision
  subject: sleep.first_order_phase_readouts
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-18
    needs_review: false
  epistemic_category: standard
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-07-18"
  depends_on:
    - SD-068
    - MECH-120
    - MECH-121
  location: docs/architecture/sd_068_consolidation_lesion_harness.md
  functional_restatement: >
    Child B of the 2026-07-18 SD-068 claim-synthesis decomposition. Separates the two
    per-phase instrument-validity results that SD-068 has actually earned from the
    cross-phase staging claim it has not (SD-073), so they can settle as their own claim
    instead of being repeatedly re-scoped inside SD-068's evidence_quality_note. The
    assertion is narrow and is about the INSTRUMENTS, not about any staging position: the
    nrem and sws legs each measure content fidelity rather than raw noise sensitivity, at
    the same 0.25 null_slope_ratio ceiling, so a damage signal read off either leg is
    interpretable.
  what_would_answer: >
    Already largely discharged by two pre-registered null-content controls at the SAME
    load-bearing C1 criterion. nrem: null_slope_ratio 0.1445, CI95 [0.1438, 0.1451],
    0/8 seeds confounded (V3-EXQ-778c). sws (the rebuilt _sws_pattern_completion readout):
    null_slope_ratio 0.1495, sd 0.0218, CI95 [0.1344, 0.1646], ceiling_inside_ci95 FALSE,
    8/8 seeds, plus the pre-registered C3 content-scale ladder at slope spread 0.1108
    against the 0.01 floor (V3-EXQ-778g). REMAINING CONDITION FOR PROMOTION: one
    NON-DIAGNOSTIC run tagging this claim. Both supporting runs are
    experiment_purpose=diagnostic and therefore do not weight governance confidence, so
    this claim is registered candidate and no confidence is asserted from them. REFUTED if
    a re-run at n>=8 puts ceiling_inside_ci95 true on either leg, or if the sws leg's
    content-scale ladder collapses below the 0.01 floor (which would reopen the
    scale-invariance caveat that C3 was pre-registered to discharge).
  evidence_quality_note: >
    SCOPE -- INSTRUMENT VALIDITY ONLY. This claim asserts that two readouts are
    content-contingent. It asserts NOTHING about the reverse-dependency staging order
    (separated out as SD-073), and nothing about the REM leg (whose direct readouts are
    retired by SD-072 and whose type-fact is MECH-462). The architecture doc's "Diagnostic
    results" remain RETRACTED as staging evidence.
    MECH-121 HOLD RESPECTED. MECH-121 appears in depends_on as the substrate the nrem leg
    instruments, NOT as a claim this evidence promotes; its hold_pending_v3_substrate
    stands and neither 778c nor 778g tags it.
    REGISTRATION DEVIATION: the 2026-07-18 synthesis proposal specified
    epistemic_category "measurement_validated", which is not in the
    validate_claims.py VALID_EPISTEMIC_CATEGORIES enum. Registered as "standard" instead
    -- the honest mapping, since the remaining condition is an ordinary non-diagnostic
    run, NOT a substrate block, and substrate_conditional would wrongly suppress this
    claim from the promotion queue and the inter-governance workset.
  origin: >
    Child B of the SD-068 claim-synthesis decomposition, approved at the interactive gate
    2026-07-18 (session gracious-bassi-721580). Full derivation, discrimination gate and
    literature grounding: evidence/planning/claim_synthesis_SD-068_2026-07-18.md Section 5.

- id: SD-072
  title: "REM downstream discrimination readout: the REM leg's functional-damage readout must be a downstream behavioural discrimination measure (a meta-d-prime analogue over post-REM retrieval confidence), replacing the direct calibration_error readout and the rem_generative_gain contrast, which are retired as damage instruments -- the former content-free after de-clamping, the latter flat in content scale."
  claim_type: design_decision
  subject: sleep.rem_downstream_damage_readout
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-20
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-07-18"
  depends_on:
    - SD-068
    - MECH-462
    - MECH-123
  location: docs/architecture/sd_068_consolidation_lesion_harness.md
  functional_restatement: >
    Child C of the 2026-07-18 SD-068 claim-synthesis decomposition, and the only child
    that asks for new implementation -- it is the concrete substrate consequence of the
    MECH-462 type-fact. If REM precision recalibration is second-order over
    representations and therefore content-free BY TYPE, then no statistic computed on the
    precision quantity itself can carry a content-contingent damage signal, and the
    instrument must move downstream to a behavioural consequence: does the agent's
    post-REM confidence separate its own correct from incorrect retrievals. Experiment
    layer (ree-v3/experiments/_lib/consolidation_lesion_harness.py), following the
    zero-ree_core-change precedent of the sws rebuild.
  what_would_answer: >
    The SAME instrument-validity gate that admitted the nrem and sws legs, applied to the
    replacement: null_slope_ratio_rem_downstream <= 0.25 with ceiling_inside_ci95 false at
    n=8, PLUS a pre-registered content-scale ladder (as V3-EXQ-778g's C3). SYMMETRY OF
    CRITERION IS REQUIRED and is load-bearing here, per the 778g re-admission precedent:
    admitting the REM leg on a weaker test than the one that retired its predecessor would
    be motivated reasoning. REFUTED if the replacement fails the same C1 ceiling that
    retired the direct readout -- which would also refute MECH-462's constructive half
    (that a downstream readout recovers what a direct one cannot).
  evidence_quality_note: >
    NOT YET BUILT -- registered as the build child, no supporting run exists. The retirement
    half of this claim is what is presently evidenced: the direct calibration_error readout
    was degenerate at BOTH clamp rails at n=8 (778c) and remained content-free after
    de-clamping (778e), and rem_generative_gain was flat in content scale (778f).
    The rem_generative_gain ATTENUATION finding (0.149, 8/8 attenuating) STANDS and is not
    touched by this claim -- what is retired is its use as a functional-DAMAGE instrument,
    not the attenuation observation itself.
    REGISTRATION DEVIATION: none on this field -- the proposal's substrate_conditional is a
    valid enum value and is used as specified.
  origin: >
    Child C of the SD-068 claim-synthesis decomposition, approved at the interactive gate
    2026-07-18 (session gracious-bassi-721580). Full derivation, discrimination gate and
    literature grounding: evidence/planning/claim_synthesis_SD-068_2026-07-18.md Section 5.

- id: SD-073
  title: "Consolidation staging order as a separable, insult-regime-conditional claim: that under uniform diffuse damage the three offline-consolidation phases fail in reverse dependency order rather than uniformly is a distinct CROSS-phase ranking claim, conditional on the declared insult regime, and establishable only once all three phases carry validated content-contingent instruments."
  claim_type: design_decision
  subject: sleep.consolidation_staging_order
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-20
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-07-18"
  depends_on:
    - SD-068
    - SD-071
    - SD-072
    - MECH-168
    - INV-047
    - MECH-169
  location: docs/architecture/sd_068_consolidation_lesion_harness.md
  functional_restatement: >
    Child D of the 2026-07-18 SD-068 claim-synthesis decomposition, and the main structural
    payoff of that decomposition. The staging order was named in SD-068's TITLE as a
    non-vacuity carrier, which held the per-phase instrument results hostage: they could not
    settle while the staging order was unsupported, and the staging order could not be
    established while any of the three ranked legs lacked an interpretable readout.
    Separating it converts that deadlock into an ordinary depends_on edge (SD-071 + SD-072
    above), and makes the existing prose gate -- "staging order is a CROSS-phase ranking and
    cannot be supported while one of the three ranked legs has no interpretable readout" --
    STRUCTURAL rather than a paragraph each amendment has to restate.
    INSULT-REGIME CONDITIONALITY is part of the claim, not a caveat on it: the biological
    ordering INVERTS with insult duration, so a staging result is meaningless unless the
    regime (acute single-sweep vs chronic repeated-sweep) is declared.
  what_would_answer: >
    Re-run run_staged_sweep with all three validated instruments (SD-071's nrem + sws, plus
    SD-072's REM downstream replacement) at a DECLARED insult regime. SUPPORTED if the
    damage-tolerance rank order matches the reverse-dependency prediction on >=6/8 seeds
    WITHIN a declared regime. REFUTED if the order is unstable across seeds, or if it
    inverts within one regime. EXPLICITLY NOT ANSWERABLE by any run in which a ranked leg
    lacks an interpretable readout -- this is the structural gate, and it is why the claim
    depends_on SD-071 and SD-072 rather than merely citing them.
  evidence_quality_note: >
    UNSUPPORTED, and deliberately registered that way. The 2026-07-17 smoke observed a
    damage-tolerance order (nrem, rem, sws) that PARTIALLY matches the reverse-dependency
    prediction (SWS correctly most-robust/last) but INVERTS REM-vs-NREM; that observation is
    RETRACTED as staging evidence, because it was read off instruments two of which have
    since been retired or rebuilt and one of which (rem) is still absent. No staging
    POSITION has been established for any leg. Two of three legs now have validated
    content-contingent instruments (SD-071); the third does not (SD-072, unbuilt).
    NO STATUS OR CONFIDENCE CHANGE flows to MECH-168 / INV-047 / MECH-169 from this
    registration -- all supporting runs to date are experiment_purpose=diagnostic and do not
    weight governance confidence. This claim is the place where their falsifier will be
    scored once the instruments exist; it does not score it now.
  origin: >
    Child D of the SD-068 claim-synthesis decomposition, approved at the interactive gate
    2026-07-18 (session gracious-bassi-721580). Full derivation, discrimination gate and
    literature grounding: evidence/planning/claim_synthesis_SD-068_2026-07-18.md Section 5.

- id: MECH-394
  title: "Multidrive arbitration policy: when several drive axes are simultaneously active, behaviour is selected by a soft-competitive orchestration over context, per-axis satiation, inter-drive competition, and developmental phase -- not a fixed priority or single-axis winner."
  claim_type: mechanism_hypothesis
  subject: drive.multidrive_arbitration
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - SD-060     # the multidrive register this arbitrates across
    - SD-012     # single-axis baseline
    - MECH-295   # approach mechanism
    - MECH-216   # arbitration / competition substrate
    - ARC-073    # play-vs-homeostatic single-axis competition
    - MECH-435   # opportunity-cost-of-time common currency the policy prices competition in (necessary-but-insufficient component)
  location: evidence/planning/drives_motivation_v4_plan.md
  notes: >
    Drives framed as orchestrated over context / satiation / competition / dev-phase.
    REE has single-axis competition (ARC-073 play-vs-homeostatic, Pezzulo 2014) and a
    soft-competitive arbitration vocabulary (Q-016 tri-loop) but no policy arbitrating
    ACROSS a multidrive register. Prose-only; requires the SD-060 register to exist
    first. substrate_conditional on a V4 multidrive substrate not yet built.
    DO NOT build in V3.

- id: MECH-395
  title: "Pre-approach orienting/surveying mode: a cue-triggered, need-gated active-sensing control state entered when a cue/need is present but directional/affordance confidence is too low for reliable approach; it samples gradients (turn/scan, exploratory arcs) until a directional vector stabilises, then exits to approach."
  claim_type: mechanism_hypothesis
  subject: drive.pre_approach_orienting_mode
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-08-07
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v3
  v3_pending: true
  version_relevance: v3
  registered_utc: 2026-06-10
  depends_on:
    - SD-057     # cue recall (the cue that triggers orienting)
    - MECH-347   # precision / active-sensing substrate
    - MECH-295   # approach mechanism this finds the vector for
    - MECH-111   # curiosity (distinguished from, not reused)
  location: evidence/planning/orienting_epistemic_deficit_v3_plan.md
  notes: >
    What FINDS the approach vector that MECH-295 assumes -- a distinct candidate
    mechanism not captured by curiosity, cue recall, or approach. Currently a routing
    hypothesis gated on the V3-EXQ-640 result pattern (cue fires, no contact, no
    orienting); register only if 640+ shows that discriminating row. substrate_conditional
    on a V4 drives substrate not yet built. DO NOT build in V3.

    [2026-08-07 reclassification, session elegant-ishizaka-ddd4f6, user-directed]:
    implementation_phase v4 -> v3, version_relevance v4_v5 -> v3. V3-EXQ-640 (and
    a follow-on gain sweep, 640a) ran; the discriminating pattern this claim was
    gated on did NOT resolve to orienting -- it routed to a shared E3
    selection-authority / cue-authority ceiling instead (see
    evidence/planning/failure_autopsy_V3-EXQ-640_2026-06-05.md and
    failure_autopsy_V3-EXQ-640a_2026-06-06.md). Rationale for the phase move: V3
    is not a closed substrate under test but is still being assembled: this claim
    is gated on identified, partially-cleared, IN-PROGRESS V3 assembly work (the
    cue-authority thread; most recent attempt V3-EXQ-812, 2026-07-24, failed on a
    readiness precondition, still open), not on a separate future generation.
    v3_pending stays true and DO NOT build YET stays in force -- this changes
    which roadmap the claim is tracked against, not its buildability today. See
    evidence/planning/drives_motivation_v4_plan.md DRV-4 (blocking_on +
    2026-08-07 changelog entry) for the full trace.

- id: Q-071
  title: "Transfer-world launch profile is evaluated on a four-grade non-terminal failure taxonomy (terminal / soft-rescue / developmental / autonomous): minimise terminal failure while preserving developmental failure as a real learning signal."
  claim_type: design_decision
  subject: transfer.non_terminal_failure_profile
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  claim_level: architectural
  registered_utc: 2026-06-10
  depends_on:
    - SD-012     # energy/survival axis the failure grades sit on
    - SD-054     # onboarding / scaffolded-training substrate
  location: evidence/planning/drives_motivation_v4_plan.md
  notes: >
    The failure-grade taxonomy + minimal-launch-kit is NOVEL but explicitly self-scoped
    as future transfer-world design off the V3 critical path. Captured as a deferred
    design criterion (claim_type design_decision; id reserved in the Q-family slot per
    the pre-allocation) so the framing is registered without entering the V3 build;
    carries the open forgiveness-vs-drive tradeoff question. substrate_conditional on a
    V4 transfer-world substrate not yet built. DO NOT build in V3.

# --- perceptual_adaptors_v4 cluster (MECH-396/397) ---
- id: MECH-396
  title: "Cross-modal negotiation currency: a common precision-weighted representation that makes structurally dissimilar sense-specific geometries (smell gradient, sight manifold, touch boundary/contact, hearing temporal-source, proprioception body-state) mutually negotiable inside one shared world model."
  claim_type: mechanism_hypothesis
  subject: perception.cross_modal_negotiation_currency
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - ARC-087    # sense-specific perceptual manifolds
    - MECH-372   # adaptor-depth gradient
    - MECH-103   # multi-source precision-weighted fusion (which this underwrites)
    - ARC-005    # control plane (natural home for the precision-arbitration layer)
    - Q-065      # the open question this answers part (2) of
  location: evidence/planning/perceptual_adaptors_v4_plan.md
  notes: >
    MECH-103 multi-source precision-weighted fusion already PRESUPPOSES a common
    currency across dissimilar manifolds but no registered claim specifies what that
    currency IS. Q-065 part (2) asks the question but is an open_question, not a
    mechanism. PA-5 needs a concrete mechanism claim for the negotiation /
    precision-arbitration layer (naturally homed in the ARC-005 control plane) --
    distinct from MECH-103's fusion mechanism, which it underwrites.
    possible_overlap_with: MECH-103 -- review for merge (MECH-396 specifies the
    currency MECH-103's fusion presupposes; kept distinct and wired via depends_on).
    substrate_conditional on a V4 multimodal-perception substrate not yet built.
    DO NOT build in V3.

- id: MECH-397
  title: "Adaptor-maturity curriculum gate: each sense is admitted to the shared world model when its modality-specific adaptor is mature, ordered by adaptor depth (low-adaptor gradient senses first, deep-adaptor manifold senses later), specialising the staged developmental curriculum."
  claim_type: mechanism_hypothesis
  subject: developmental.adaptor_maturity_admission_gate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-372   # adaptor-depth gradient whose developmental-ordering corollary this concretises
    - ARC-019    # staged developmental curriculum this specialises
    - ARC-087    # sense-specific perceptual manifolds
  location: evidence/planning/perceptual_adaptors_v4_plan.md
  notes: >
    MECH-372's developmental-ordering corollary ("gradient-like senses enter earliest;
    smell may have been the first sense") is currently prose folded into ARC-019's
    generic curriculum-stages claim. PA-6 needs a concrete admission-criterion mechanism
    keyed to adaptor maturity/depth -- a specialisation of ARC-019, not a duplicate of
    it. possible_overlap_with: ARC-019 -- review for merge (specialises ARC-019's
    curriculum with an adaptor-maturity admission gate; kept distinct and wired via
    depends_on). substrate_conditional on a V4 multimodal-perception substrate not yet
    built. DO NOT build in V3.

# --- plasticity_neuromodulation_v4 cluster (ARC-093 + MECH-398/399/400 + Q-072) ---
- id: ARC-093
  title: "State-conditional plasticity gain: cortical encoder learning rates and residue-field write rates are multiplicatively gated by a state-conditional plasticity scalar, separately from the content signals carried by LC-NE / 5-HT / orexin."
  claim_type: architectural_commitment
  subject: learning.state_conditional_plasticity_gain_commitment
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  claim_level: architectural
  registered_utc: 2026-06-10
  depends_on:
    - ARC-075    # infant-curriculum plasticity-magnitude asymmetry this likely extends
    - INV-074    # plasticity-closure invariant
    - MECH-333   # critical-period open-phase core
  location: evidence/planning/plasticity_neuromodulation_v4_plan.md
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: this test is only meaningful once TWO conditions both
    hold. (1) A V4 plasticity-gain substrate exists (plasticity_neuromodulation_v4 plan,
    PLW-3/PLW-4, both blocked; PLW-1 decision-to-build gate un-cleared). DO NOT build in
    V3. (2) The content-signal-only control arm must be genuinely exercisable, not a
    strawman -- MECH-313/104/203/SD-037 must be live and wired. As of 2026-08-08: MECH-104
    is active/PASS; MECH-313 is contested-candidate (demoted 2026-07-09, sub-threshold N=1
    signal only); MECH-203 has never had a valid behavioural test (3 consecutive invalid
    runs, most recently V3-EXQ-843); SD-037 is substrate_ceiling (wired but consumer
    thresholds not cleared, downstream effect inert). A fair control-arm run may need to
    accept these as the honest content-only baseline as instantiated, or wait for
    MECH-313/203/SD-037 to clear their own ceilings first.

    CONFIRMING: matched representation-update outcomes (targeted plasticity selectivity;
    retention-vs-acquisition tradeoff) are NOT achievable by gating on content signals
    alone -- a separate plasticity-gain channel produces a discriminable improvement,
    consistent with Bear & Singer 1986's conjunctive ACh x NE gating result.

    FALSIFYING: matched outcomes ARE achievable by gating on content signals alone,
    without any separate plasticity-gain channel -- falsifies the separate-channel
    commitment; ARC-093 would need demotion or reframing as redundant with the ARC-075 /
    content-signal machinery it currently depends_on but is kept distinct from.

    Falsifier dated 2026-06-17 (targeted_review_plasticity_neuromodulation_v4, Bear &
    Singer 1986 grounding).
  functional_restatement: >
    The ARC-level commitment that plasticity (gain) is a separately-gated process from
    activity (content). Likely an EXTENSION of ARC-075 (infant-curriculum
    plasticity-magnitude asymmetry) from scheduler-driven phase magnitudes to
    state-driven gain, rather than a wholly new ARC -- flagged for governance to decide
    amend-vs-new. The central architectural claim of the cluster.
    possible_overlap_with: ARC-075 -- review for merge (extends ARC-075 from scheduler
    magnitudes to state-driven gain; kept distinct and wired via depends_on).
    substrate_conditional on a V4 plasticity-gain substrate not yet built.
    DO NOT build in V3.
    Falsifier (2026-06-17): if matched representation-update outcomes (targeted
    plasticity selectivity, retention-vs-acquisition tradeoff) are achievable by gating
    on the existing content signals alone (LC-NE/5-HT/orexin: MECH-313/MECH-104/
    MECH-203/SD-037) WITHOUT a separate plasticity-gain channel, the separate-channel
    commitment is falsified -- gain is not separably gated from content. Grounded by
    Bear & Singer 1986 (conjunctive ACh x NE gating, content/gain separation):
    targeted_review_plasticity_neuromodulation_v4.

- id: MECH-398
  title: "ACh-analog basal-forebrain plasticity-gain gate: a state-conditional scalar in [0,1] multiplying cortical encoder learning rates and residue write magnitudes, driven by novelty/attention/arousal, distinct from LC-NE/5-HT/orexin content signals."
  claim_type: mechanism_hypothesis
  subject: learning.ach_analog_plasticity_gain_gate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - ARC-093    # the state-conditional gain commitment this realises
    - MECH-333   # open-phase core this is the opening-side analog of
    - MECH-205   # surprise EMA driver (V3)
    - SD-032a    # salience mode driver (V3)
    - MECH-313   # neuromodulator content substrate this is distinct from
  location: evidence/planning/plasticity_neuromodulation_v4_plan.md
  notes: >
    The opening-side analog of MECH-333's open phase, made state-conditional rather than
    scheduler-driven. Bear & Singer 1986: ACh+NE pairing gates whether cortex
    incorporates inputs into long-term representations. Drivers (MECH-205 surprise EMA,
    SD-032a salience mode, drive_level) all live in V3. substrate_conditional on a V4
    plasticity-gain substrate not yet built. DO NOT build in V3.
    Falsifier (2026-06-17): the gate is otiose if a state-conditional gain scalar
    (driven by novelty/attention/arousal) produces no improvement in targeted-plasticity
    selectivity over a scheduler-driven flag toggle (the V3-conservative form) on a task
    where the toggle is demonstrably insufficient -- i.e. gating the learning rate by
    state does not change WHICH inputs are incorporated vs an ungated/uniform rate.
    Grounded by Kilgard & Merzenich 1998 (NB activity necessary for adult remapping) +
    Froemke 2007 (transient cholinergic disinhibition window):
    targeted_review_plasticity_neuromodulation_v4.

- id: MECH-399
  title: "PV-interneuron inhibitory-maturation analog: a time-since-onset accumulator that monotonically lowers the plasticity-gain ceiling, replacing the current binary critical-period phase transition."
  claim_type: mechanism_hypothesis
  subject: learning.pv_analog_window_closure_clock
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-333   # critical-period open phase
    - MECH-334   # critical-period closure (the binary form this makes continuous)
    - ARC-093    # state-conditional gain commitment (the ceiling this clamps)
    - MECH-398   # the continuous gain scalar this clock clamps
  location: evidence/planning/plasticity_neuromodulation_v4_plan.md
  notes: >
    Biology-faithful continuous form of the existing MECH-333/MECH-334 closure (today a
    binary on_phase3_entry crystallize). Hensch 2005 / Deidda 2015 anchors. Deferred: a
    closure CLOCK only matters once a continuous gain scalar (the ACh gate MECH-398)
    exists to clamp. substrate_conditional on a V4 plasticity-gain substrate not yet
    built. DO NOT build in V3.
    Falsifier (2026-06-17): if a continuous time-since-onset gain-ceiling accumulator
    yields no measurable benefit over the binary MECH-334 phase transition on the
    retention-vs-plasticity tradeoff (no regime where graded closure outperforms the
    flag flip), the continuous clock is redundant and MECH-399 is falsified. Grounded by
    Hensch 2005 (closure is a continuous inhibitory-maturation process, not a binary
    switch; area-specific timing): targeted_review_plasticity_neuromodulation_v4.

- id: MECH-400
  title: "BDNF-analog trophic window-duration scalar: a duration multiplier on top of the plasticity-gain gate and closure clock."
  claim_type: mechanism_hypothesis
  subject: learning.bdnf_analog_window_duration_knob
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-333   # critical-period open phase
    - MECH-398   # the gain gate this duration knob sits on top of
    - MECH-399   # the closure clock this duration knob modulates
  location: evidence/planning/plasticity_neuromodulation_v4_plan.md
  notes: >
    Lowest-priority pillar in the source sketch: the duration knob sitting on top of the
    gain knob (MECH-398) and closure clock (MECH-399). Huang 1999 anchor (BDNF
    precociously opens and closes the CP). Deferred until gain/closure pillars are
    validated. substrate_conditional on a V4 plasticity-gain substrate not yet built.
    DO NOT build in V3.
    Falsifier (2026-06-17): if varying a window-duration multiplier (on top of an
    existing gain gate MECH-398 + closure clock MECH-399) produces no change in the
    acquisition-vs-retention outcome that is not already captured by the gain and closure
    knobs, the duration knob adds no independent control and MECH-400 is falsified.
    Grounded by Sale 2007 (BDNF co-varies with window reopening, but confounded with
    GABA/PNN -- duration-specific control is the open question):
    targeted_review_plasticity_neuromodulation_v4.

- id: MECH-401
  title: "Consolidation gated-write-authority: over-frequent rewriting of consolidated memory is a failure mode; the consolidation operator's authority to overwrite is gated by schema-stability / schedule-sensitivity, not run after every interaction."
  claim_type: mechanism_hypothesis
  subject: memory.consolidation.gated_write_authority
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  phase_locked: true  # intrinsic V4 scope: memory_lifecycle_v4 cluster, needs the
                      # not-yet-built V4 memory-lifecycle store. MECH-471 references it
                      # analogically per its own notes, not as a prerequisite. Set
                      # 2026-08-10 per chip chip-20260810-phase-consistency-reconciliation.
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-261   # mode-conditioned write gating this extends to the consolidation operator
    - MECH-068   # consolidation substrate
    - MECH-273   # sleep self-model aggregation operator whose rewrite frequency is gated
    - INV-039    # schema-primed assimilation rate -- the schedule-sensitivity gate
    - INV-049    # offline-update necessity -- the complementary law; this prices the corruption side
    - MECH-392   # provenance sibling (tracks WHAT changed) vs this (WHETHER/how often to rewrite)
  distinct_from:
    - MECH-368   # event-level write-eligible gate governs which EVENTS are written; this governs consolidation REWRITE frequency
  location: evidence/planning/memory_lifecycle_v4_plan.md
  notes: >
    Consolidation-faults intake Section 2: gated write-authority on the consolidation
    operator -- over-frequent rewriting (consolidation run after every interaction) is a
    failure mode. INV-049 makes offline update NECESSARY but the operator can corrupt;
    this claim prices the corruption side via a schema-stability / schedule-sensitivity
    gate (INV-039). Owning node: memory_lifecycle_v4:MEM-7. Distinct from MECH-392
    (provenance: what changed) and MECH-368 (which events are write-eligible).
    substrate_conditional on a V4 memory-lifecycle store + real consolidation schedule
    not yet built. DO NOT build in V3.

- id: Q-072
  title: "Does plasticity-gain modulate identically across encoder / residue / hippocampal / E2-forward layers, or per-substrate (layer-specific gates)?"
  claim_type: open_question
  subject: learning.plasticity_gain_layer_specificity
  polarity: asks
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-333   # critical-period substrate the gain gate parameterises
    - ARC-093    # the gain commitment whose layer-scope this fork decides
    - MECH-398   # the gain gate this question parameterises
  location: evidence/planning/plasticity_neuromodulation_v4_plan.md
  what_would_answer: >
    A V4 experiment applying a single GLOBAL plasticity-gain scalar vs PER-SUBSTRATE
    scalars across the encoder / residue / hippocampal / E2-forward layers. If
    per-substrate gating measurably outperforms the global scalar -- a distinct optimal
    gain per layer, or cross-layer interference (one layer over-writing while another
    must retain) under a single global scalar -- the answer is LAYER-SPECIFIC. If a
    single global scalar matches per-substrate outcomes on targeted-plasticity
    selectivity and the retention-vs-acquisition tradeoff, the answer is GLOBAL.
    Resolvable only against a V4 plasticity-gain substrate (substrate_conditional); do
    NOT queue a V3 experiment. Biology (Hensch 2005, area-specific CP timing) is the
    prior for layer-specific: targeted_review_plasticity_neuromodulation_v4.
  notes: >
    The pivotal design fork for the whole cluster. Biology says layer-specific (distinct
    cortical-area CP timings); REE must decide one global ACh scalar vs per-substrate
    scalars before parameterising the gain gate/commitment. A single-substrate global
    scalar is the V3-tractable form; full per-substrate is the V4-budget form.
    epistemic_category substrate_conditional set EXPLICITLY so narrow_open_question does
    NOT fire -- there is no V4 plasticity-gain substrate to test against; the right
    response is to decide the fork during substrate design, not to queue a V3
    experiment. DO NOT queue a V3 experiment against this.

# ============================================================================
# V5 SOCIAL-TIER closure-roadmap cluster (2026-06-10)
# 23 candidate claims spanning six V5 plans:
#   multi_agent_ecology_v5, mirror_modelling_other_self_v5, fast_empathy_v5,
#   relational_harm_moral_semantics_v5, ethics_as_coherence_v5,
#   loveability_ethical_agency_v5.
# All substrate_conditional on the unbuilt V5 social substrate (multi-agent env,
# represented other, stateful self-model). implementation_phase v5; v3_pending.
# DO NOT build in V3.
# ============================================================================

# --- multi_agent_ecology_v5 cluster (MECH-402 + MECH-403) ---
- id: MECH-402
  title: "Axiom conflict (self vs other) is resolved by a context-sensitive comparator plus a persisting moral-residue mechanism, not a fixed self/other priority."
  claim_type: mechanism_hypothesis
  subject: ethics.axiom_conflict_residue_comparator
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - Q-028      # axiom-conflict open question this operationalises
    - INV-026    # axiom basis
    - INV-028    # axiom basis
    - INV-029    # love as long-horizon coherence (Axiom 5)
    - INV-006    # axiom basis
  location: evidence/planning/multi_agent_ecology_v5_plan.md
  notes: >
    The Q-028 lit synthesis (McConnell 2022; Williams 1965) named the mechanism
    -- a context-sensitive comparator plus Williams-style moral residue, with
    REE's residue-field representation structurally suited to the residue role --
    but no claim operationalised it. Testable only in the V5 multi-agent ecology
    (MAE-5), where an other's welfare can conflict with the self's. Home claim
    for that mechanism; replaces NEWCLAIM:axiom_conflict_residue_comparator in
    node MAE-5. substrate_conditional on the unbuilt V5 multi-agent substrate.
    DO NOT build in V3.

- id: MECH-403
  title: "Unshared suffering (loneliness) is represented as a distinct architectural harm channel, derivable from Axiom 5 (love exists / INV-029) and measurable only against present-or-absent others."
  claim_type: mechanism_hypothesis
  subject: ethics.unshared_suffering_harm_channel
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - Q-029      # loneliness-as-harm open question
    - INV-029    # love as long-horizon coherence (derivation source)
    - INV-028    # axiom basis
    - MECH-041   # harm representation substrate
  location: evidence/planning/multi_agent_ecology_v5_plan.md
  notes: >
    Q-029 asks whether loneliness is its own harm category vs derivative of
    INV-029. Lit base strong (Holt-Lunstad 2010/2015; Wang 2023; Zajner/Bzdok
    2021; lit_conf 0.875) but exp_conf is 0 because a single-agent substrate
    cannot represent the ABSENCE of a sharing partner. If the V5 multi-agent
    ecology (MAE-6) affirms loneliness-as-harm it needs a representation; this is
    the candidate channel. Replaces NEWCLAIM:unshared_suffering_harm_channel in
    node MAE-6. Decide-whether-to-build deferred. substrate_conditional on the
    unbuilt V5 multi-agent substrate. DO NOT build in V3.

# --- mirror_modelling_other_self_v5 cluster (MECH-404) ---
- id: MECH-404
  title: "Mirror-coupling gain must be homeostatically regulated inside an operating window; the three social pathologies are its boundaries (low gain = psychopathy/callousness, high gain = empathic overwhelm/paralysis, miscalibrated = burnout/moral injury)."
  claim_type: mechanism_hypothesis
  subject: social.mirror_coupling_gain_homeostasis
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - ARC-010    # signed mirror coupling (the gain DIAL) -- possible overlap, see notes
    - MECH-051   # relational modulation
    - MECH-032   # social-model component
    - MECH-036   # social-model component
  location: evidence/planning/mirror_modelling_other_self_v5_plan.md
  notes: >
    REE owns the coupling DIAL (ARC-010 signed coupling, MECH-051 relational
    modulation) and the failure-mode TAXONOMY in social.md prose but NO claim
    asserts that mirror-coupling gain must be held inside a homeostatic window.
    The three failure modes ARE the boundaries of that window. Falsifiable: the
    three pathologies should be reachable by sweeping a single gain parameter.
    Replaces NEWCLAIM:mirror_gain_homeostasis in node MIRROR-5; substrate for the
    clinical mapping of empathy dysregulation. V5 measurement substrate (ARC-047
    SocialGridWorld). possible_overlap_with: ARC-010 -- review for merge (ARC-010
    owns the coupling primitive; MECH-404 adds the homeostatic-window regulation
    on top, kept distinct and wired via depends_on). substrate_conditional on the
    unbuilt V5 social substrate. DO NOT build in V3.

# --- fast_empathy_v5 cluster (ARC-094/095, MECH-405/406/407/408, Q-073) ---
- id: ARC-094
  title: "No empathy module / no empathy scalar: fast empathy must emerge from binding existing motivational-affective streams across self -> object -> other, never as an empathy_enabled flag or empathy_score primitive."
  claim_type: architectural_commitment
  subject: social.no_empathy_scalar_prohibition
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  claim_level: architectural
  registered_utc: 2026-06-10
  depends_on:
    - ARC-010    # signed mirror coupling (the binding substrate, not a scalar)
    - INV-005    # no explicit cost-term / scoring primitive (sibling negative commitment)
    - SD-011     # dual nociceptive streams (a stream to be bound, not collapsed)
    - MECH-112   # wanting stream (a stream to be bound)
  location: evidence/planning/fast_empathy_v5_plan.md
  functional_restatement: >
    The CENTRAL contribution of the 2026-05-04 fast-empathy intake, flagged NOVEL
    there and never registered. A negative architectural commitment (like ARC-012:
    E3 needs no explicit ethical cost term): any design introducing an empathy
    scalar violates it. It is the spine of node EMP-1 and constrains every binding
    node below. Replaces NEWCLAIM:no_empathy_scalar_prohibition.
    substrate_conditional on the unbuilt V5 social substrate. DO NOT build in V3.

- id: ARC-095
  title: "The motivational-affective stream taxonomy (liking, wanting, suffering, threat, relief, frustration, curiosity, attachment/proximity, fatigue/cost, agency, prediction-error) is an open, extensible register of handles -- it must permit later split/merge/rename and must not be frozen into a final ontology."
  claim_type: architectural_commitment
  subject: affect.open_extensible_stream_taxonomy
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  claim_level: architectural
  registered_utc: 2026-06-10
  depends_on:
    - SD-011     # dual nociceptive streams (the V3 harm-only register this generalises)
  location: evidence/planning/fast_empathy_v5_plan.md
  functional_restatement: >
    Sharpens docs/architecture/affect_primitives.md (a V3 harm-only three-way
    register) into an explicitly extensible social register. Flagged in the intake
    as the affect-stream-taxonomy-open candidate. Freezing the taxonomy would
    re-create the empathy-scalar error one level down, so it pairs with the
    prohibition (EMP-2, ARC-094). Replaces NEWCLAIM:open_affect_stream_taxonomy.
    substrate_conditional on the unbuilt V5 social substrate. DO NOT build in V3.

- id: MECH-405
  title: "Fast empathy mechanism: other-bound affect is produced by routing the agent's OWN motivational-affective streams (SD-011 suffering, MECH-112 wanting, etc.) through a stable other-model slot -- e.g. other-bound suffering = suffering stream + other-model binding; MECH-183 z_beta leakage is one such attribution-gated binding."
  claim_type: mechanism_hypothesis
  subject: social.fast_empathy_stream_binding_mechanism
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - ARC-010    # mirror modelling -- fast-empathy specialises it; possible overlap, see notes
    - MECH-031   # mirror/self-other modelling -- possible overlap, see notes
    - MECH-183   # z_beta leakage as one attribution-gated binding
    - SD-011     # suffering stream routed through the other-model
    - MECH-112   # wanting stream routed through the other-model
    - MECH-163   # goal maintenance / stable other-model prerequisite
  location: evidence/planning/fast_empathy_v5_plan.md
  notes: >
    The positive mechanism ARC-094 demands exist instead of a module. The intake
    frames other-bound suffering/wanting as an EXTENSION of SD-011/MECH-112 via
    other-model binding -- novel framing, never registered. Gated on a stable
    other-model (ARC-083/OBJ-5) plus MECH-163. Replaces
    NEWCLAIM:fast_empathy_stream_binding_mechanism; needs the EMP-7 social-
    development lit-pull before promotion (biology before formal definitions).
    possible_overlap_with: ARC-010 -- review for merge (fast-empathy specialises
    mirror modelling); possible_overlap_with: MECH-031 -- review for merge.
    substrate_conditional on the unbuilt V5 social substrate. DO NOT build in V3.

- id: MECH-406
  title: "Other-model prediction, reciprocity reward, and residue-aware social commitment are three separable functions: an agent can predict cooperative intent without binding it into commitment-altering value, and can value reciprocity without generating repair-residue after exploitation (the A/B/C/D dissociation)."
  claim_type: mechanism_hypothesis
  subject: social.prediction_reciprocity_repair_dissociation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-031   # mirror/self-other modelling -- possible overlap, see notes
    - ARC-010    # mirror modelling (prediction substrate)
    - INV-029    # love as long-horizon coherence (the commitment side)
  location: evidence/planning/fast_empathy_v5_plan.md
  notes: >
    The most testable novel claim in the intake, lit-anchored to Wu et al. 2026
    (eLife): adolescents predict partner cooperation as well as adults but show
    weaker intrinsic reciprocity reward. The A/B/C/D experiment (EMP-4) is its
    discriminative test. Replaces NEWCLAIM:other_prediction_reciprocity_
    dissociation; the experiment gains an owner_exq once the social substrate or a
    scripted-partner proxy exists. possible_overlap_with: MECH-031 -- review for
    merge (this dissociates functions that ARC-010/MECH-031 model jointly).
    substrate_conditional on the unbuilt V5 social substrate. DO NOT build in V3.

- id: MECH-407
  title: "Residue-aware social repair: exploiting a cooperating other leaves a persisting regret-residue that spawns a self-state repair-goal (restore commitment/relationship coherence), distinguishing genuine prosocial commitment from mere reciprocity bookkeeping."
  claim_type: mechanism_hypothesis
  subject: social.residue_aware_repair_goal
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - INV-029    # love as long-horizon coherence / care-investment (social cash-out)
    - MECH-112   # wanting substrate
    - MECH-214   # z_self-domain goal channel (repair-goal home)
    - MECH-359   # candidate-differentiated affect (residue substrate)
    - ARC-010    # mirror modelling -- possible overlap, see notes
  location: evidence/planning/fast_empathy_v5_plan.md
  notes: >
    The C->D step of the intake A/B/C/D design, and the social cash-out of INV-029
    (love as long-horizon coherence/care-investment). Rides the MECH-112/MECH-359
    wanting+affect substrate and a z_self-domain goal channel (MECH-214 /
    self_model_v4 SELF-5). Implied by the intake (variant D residue + repair-goal
    generation) but not separated into its own claim. Replaces
    NEWCLAIM:residue_aware_social_repair. possible_overlap_with: ARC-010 -- review
    for merge (repair-residue rides the mirror-coupling representation).
    substrate_conditional on the unbuilt V5 social substrate. DO NOT build in V3.

- id: MECH-408
  title: "Developmental ordering of other-bound streams: other-bound suffering/threat (protective) come online BEFORE other-bound liking/wanting (appetitive), because early positive other-reward is destabilising and exploitable -- a safety-relevant maturational ordering on the ARC-059 social spine."
  claim_type: mechanism_hypothesis
  subject: social.other_bound_stream_developmental_order
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - ARC-059    # social maturational spine
    - INV-064    # maturational-sequence honesty gate
    - SD-011     # other-bound suffering/threat stream
    - MECH-112   # other-bound liking/wanting stream
  location: evidence/planning/fast_empathy_v5_plan.md
  notes: >
    Flagged NOVEL and safety-relevant in the intake. An other-model that learns to
    want another's reward before registering another's suffering/threat is
    exploitable. Sits on the ARC-059 maturational spine, gated by INV-064
    (maturational-sequence honesty). Replaces
    NEWCLAIM:other_bound_stream_developmental_order; needs the EMP-7 social-
    development lit-pull before promotion. substrate_conditional on the unbuilt V5
    social substrate. DO NOT build in V3.

- id: Q-073
  title: "Why might other-bound suffering/threat need to come online before other-bound liking/wanting? Is it protection-first (register harm to others before benefit) versus exploitation-vulnerability (early positive other-reward is gameable), and how should the ordering be enforced?"
  claim_type: open_question
  subject: social.delay_positive_other_reward
  polarity: asks
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-073@2026-07-01T01:23:10.615805Z"
      as_of: 2026-07-01
      verdict: held_v4_by_architectural_commitment/applied
  what_would_answer: |
    Answered only against a V5 social substrate with both other-bound streams, not
    by a V3 run. On the ARC-059 social spine with MECH-408's developmental
    ordering instantiated, run an ordering-manipulation: ARM_protective-first
    (other-bound suffering/threat comes online before other-bound liking/wanting)
    vs ARM_simultaneous vs ARM_appetitive-first. Probe with an exploitation
    scenario in which a manipulating other agent offers early positive
    other-reward. ANSWER 'protection-first' is the operative reason if
    ARM_protective-first registers harm-to-others reliably AND resists the
    exploitation that compromises ARM_appetitive-first; ANSWER
    'exploitation-vulnerability' is the operative reason if the appetitive-first /
    simultaneous arms are demonstrably gameable (the agent is steered into harmful
    cooperation by early reward) while protective-first is not -- the two readings
    can both hold, and the discriminator is whether the cost is mis-registering
    harm vs being captured by early reward. Enforcement is answered by whether a
    hard maturational gate (appetitive stream cannot activate until the protective
    stream passes a competence threshold) outperforms a soft schedule on
    exploitation-resistance without stunting prosocial behaviour. HARD
    PRECONDITION: a non-degenerate multi-agent substrate with SEPARATELY
    representable other-bound suffering/threat and other-bound liking/wanting
    streams must exist; with no social substrate (or with the two streams
    conflated into one other-reward scalar) there is nothing to order and the
    question is vacuous. V5; off the V3 critical path. The right response is the
    EMP-7 lit-pull plus substrate design; do NOT queue a V3 experiment.
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - ARC-059    # social maturational spine the ordering acts on
    - MECH-408   # the developmental-ordering mechanism this question is the why behind
  location: evidence/planning/fast_empathy_v5_plan.md
  notes: >
    Listed as an explicit open Q in the intake (Q delay-positive-other-reward).
    It is the WHY behind the EMP-6 developmental-ordering mechanism; an
    answer_state open_question, not an assertion. Replaces
    NEWCLAIM:delay_positive_other_reward_q; pairs with MECH-408.
    epistemic_category substrate_conditional set EXPLICITLY so narrow_open_question
    does NOT fire -- there is no V5 social substrate to test against; the right
    response is the EMP-7 lit-pull plus substrate design. DO NOT queue a V3
    experiment against this.

# --- relational_harm_moral_semantics_v5 cluster (MECH-409 + MECH-410) ---
- id: MECH-409
  title: "Harm-to-agency moral weighting requires a consent / incidental-vs-constitutive qualifier: a raw goal-interference signal becomes a harm signal only when the obstruction is non-consented and constitutively (not incidentally) defeats the other agent's goal."
  claim_type: mechanism_hypothesis
  subject: coherence.relational_harm_qualifier
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-129   # goal-interference discriminants 1-2 (RHM-1); this adds discriminants 3-4
    - INV-028    # axiom basis for the moral upgrade
    - MECH-405   # inferred other-commitment/consent state (mirror binding) prerequisite
  location: evidence/planning/relational_harm_moral_semantics_v5_plan.md
  notes: >
    MECH-129 enumerates four discriminants (other's goal / obstruction /
    incidental-vs-constitutive / consent) but registers discriminants 1-2 only
    (goal-interference over trajectory pairs). MECH-409 covers discriminants 3-4,
    the part that turns a raw interference signal into a MORAL signal (a contest is
    full obstruction yet not harm; a constitutive non-consented defeat is). Distinct
    mechanism, not a restatement; requires inferred other-commitment/consent state
    from mirror_modelling_other_self_v5 (MECH-405). Replaces
    NEWCLAIM:obstruction_consent_qualifier. substrate_conditional on the unbuilt V5
    social substrate. DO NOT build in V3.

- id: MECH-410
  title: "Self-like gradient weighting (MECH-164) requires a developmental calibration schedule: other-terrain coupling must tighten over development scaled by inferred-terrain confidence, between the failure poles of empathic collapse (too-strong, too-early) and callousness (too-weak), and never weight a badly-inferred terrain self-like."
  claim_type: mechanism_hypothesis
  subject: love.self_like_weight_calibration
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-164   # self-like symmetry STRUCTURE; this adds the WEIGHT/SCHEDULE -- possible overlap, see notes
  location: evidence/planning/relational_harm_moral_semantics_v5_plan.md
  notes: >
    MECH-164 fixes the STRUCTURE (self-like symmetry: other-gradients weighted with
    the same motivational force as own) but leaves the WEIGHT/SCHEDULE open.
    DEV-NEED-022 names the gap (lambda_empathy / v_other_veto / coupling tightening)
    as a developmental need but no claim operationalises the calibration mechanism;
    DEV-NEED-021 warns against self-like weighting on unstable/badly-inferred
    targets. The calibration rule is new capability beyond MECH-164's structural
    assertion. Replaces NEWCLAIM:self_like_weight_calibration. possible_overlap_with:
    MECH-164 -- review for merge (MECH-164 is the structure, MECH-410 the schedule,
    kept distinct and wired via depends_on). substrate_conditional on the unbuilt V5
    social substrate. DO NOT build in V3.

# --- ethics_as_coherence_v5 cluster (ARC-096/097/098, INV-081, MECH-411/412/413) ---
- id: ARC-096
  title: "REE requires a typed causal-attribution ontology for self, world, body, model, commitment, other, shared, accidental, and repairable outcomes."
  claim_type: architectural_commitment
  subject: attribution.typed_causal_ontology
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-096@2026-07-01T01:23:09.172154Z"
      as_of: 2026-07-01
      verdict: held_v4_by_architectural_commitment/applied
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  claim_level: architectural
  registered_utc: 2026-06-10
  depends_on:
    - ARC-015    # causal-attribution substrate
    - SD-003     # counterfactual / causal-ownership substrate
    - ARC-056    # ethics/coherence substrate -- possible overlap, see notes
    - ARC-083    # represented-other pillar (OTHER/SHARED ownership classes need it)
  location: evidence/planning/ethics_as_coherence_v5_plan.md
  functional_restatement: >
    Stub ARC-TBD-ATTRIBUTION-ONTOLOGY from the 2026-05-31 V4-musings cluster, never
    registered (TBD id). Head of the guilt-repair dependency chain: ethical residue
    and repair require causal-ownership tags, not merely harm detection. Proposed at
    v5 (not the intake's original v4) because the OTHER/SHARED ownership classes are
    intrinsically social and need a represented other (ARC-083) plus the self-model
    for self/commitment tags. Replaces NEWCLAIM:attribution_typed_causal_ontology.
    possible_overlap_with: ARC-056 -- review for merge (ethics-as-coherence head).
    substrate_conditional on the unbuilt V5 social/self-model substrate.
    DO NOT build in V3.

- id: ARC-097
  title: "Self-attributed harm must open repair and policy-update pathways rather than global self-condemnation."
  claim_type: architectural_commitment
  subject: ethics.guilt_like_repair_routing
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-097@2026-07-01T01:23:09.425711Z"
      as_of: 2026-07-01
      verdict: held_v4_by_architectural_commitment/applied
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  claim_level: architectural
  registered_utc: 2026-06-10
  depends_on:
    - ARC-096    # typed causal-attribution ontology (provides the ownership tags)
    - ARC-015    # causal-attribution substrate
    - INV-005    # no explicit scoring primitive (routes to repair not a badness scalar)
    - ARC-056    # ethics/coherence substrate -- possible overlap, see notes
  location: evidence/planning/ethics_as_coherence_v5_plan.md
  functional_restatement: >
    Stub ARC-TBD-GUILT-REPAIR from the 2026-05-31 cluster. Ethical agency requires
    ownership of harm, but shame-like global self-negation is unsafe; guilt must
    route to repair. Social because the repair target is another agent's D_{V,j}.
    Replaces NEWCLAIM:ethics_guilt_like_repair_routing. possible_overlap_with:
    ARC-056 -- review for merge (ethics-as-coherence). substrate_conditional on the
    unbuilt V5 social/self-model substrate. DO NOT build in V3.

- id: ARC-098
  title: "Dangerous self-state detection may suspend autonomy, but must preserve evidence, seek correction, and avoid global self-condemnation."
  claim_type: architectural_commitment
  subject: safety.autonomy_suspension_without_shame
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  claim_level: architectural
  registered_utc: 2026-06-10
  depends_on:
    - INV-081    # no-global-self-badness-write invariant (the paired safety constraint)
    - ARC-086    # safety/containment substrate -- possible overlap, see notes
  location: evidence/planning/ethics_as_coherence_v5_plan.md
  functional_restatement: >
    Stub ARC-TBD-CONTAINMENT-NOT-SHAME from the 2026-05-31 cluster. The 'nuclear
    safety option as containment, not shame' framing; the paired safety commitment
    to INV-081 no-global-self-condemnation. Extends existing safety/containment
    concepts; needs a self-state-detection plus autonomy-suspension surface on the
    V4/V5 self-model. Replaces NEWCLAIM:safety_autonomy_suspension_without_shame.
    possible_overlap_with: ARC-086 -- review for merge (safety/containment).
    substrate_conditional on the unbuilt V5 self-model substrate. DO NOT build in V3.

- id: INV-081
  title: "Self-attributed harm must bind to actions, commitments, predictions, and repair obligations, not to unbounded negative self-worth."
  claim_type: invariant
  invariant_type: universal
  subject: self_model.no_global_self_badness_write
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:INV-081@2026-07-01T01:23:09.664109Z"
      as_of: 2026-07-01
      verdict: held_v4_by_architectural_commitment/applied
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - ARC-097    # guilt-like repair routing (the routing this invariant constrains) -- possible overlap, see notes
    - ARC-056    # ethics/coherence substrate
    - ARC-086    # safety/containment substrate
  location: evidence/planning/ethics_as_coherence_v5_plan.md
  notes: >
    Stub INV-TBD-NO-GLOBAL-SELF-CONDEMNATION from the 2026-05-31 cluster. High-value
    safety invariant (anti-shame-collapse, anti-concealment, anti-defensive-
    distortion). invariant_type UNIVERSAL: this is a structural safety prohibition on
    what self-attributed harm may write -- it holds regardless of which social or
    self-model substrate is built (a substrate that violated it would be unsafe by
    construction), so it carries no emergent_from. The intake flags a guilt-vs-shame
    lit-pull before promotion. Replaces
    NEWCLAIM:self_model_no_global_self_badness_write. possible_overlap_with: ARC-097
    -- review for merge (the architectural routing vs the standing prohibition).
    epistemic_category substrate_conditional: needs the V4/V5 stateful self-model to
    bind against. DO NOT build in V3.

- id: MECH-411
  title: "After self-attributed harm, E3 generates repair trajectories and compares them against avoidance, concealment, and ordinary goal-continuation."
  claim_type: mechanism_hypothesis
  subject: e3.repair_trajectory_generation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-411@2026-07-01T01:23:10.371814Z"
      as_of: 2026-07-01
      verdict: held_v4_by_architectural_commitment/applied
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - ARC-097    # guilt-like repair routing (the routing this enacts)
    - MECH-056   # E3 trajectory-scoring machinery this extends
  location: evidence/planning/ethics_as_coherence_v5_plan.md
  notes: >
    Stub MECH-TBD-REPAIR-SEARCH from the 2026-05-31 cluster. Guilt-like signals
    require an action outlet (E3 repair-trajectory generation) to avoid becoming
    perseverative residue. Extends the V3 E3 trajectory-scoring machinery; needs the
    ARC-096 attribution tag to seed the repair target. Replaces
    NEWCLAIM:e3_repair_trajectory_generation. substrate_conditional on the unbuilt
    V5 social/self-model substrate. DO NOT build in V3.

- id: MECH-412
  title: "Repair completion or impossibility transforms active guilt-like residue into bounded historical memory."
  claim_type: mechanism_hypothesis
  subject: residue.repair_completion_release
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-411   # repair-trajectory generation whose completion this releases
    - MECH-056   # E3 trajectory-scoring / residue-recording substrate
  location: evidence/planning/ethics_as_coherence_v5_plan.md
  notes: >
    Stub MECH-TBD-RESIDUE-RELEASE from the 2026-05-31 cluster. Prevents indefinite
    self-punitive residue while preserving learning; the release of moral residue on
    repair-completion (or impossibility) is the new piece atop the existing
    residue-recording cluster. Replaces NEWCLAIM:residue_repair_completion_release.
    substrate_conditional on the unbuilt V5 social/self-model substrate.
    DO NOT build in V3.

# --- loveability_ethical_agency_v5 cluster (INV-082/083/084, MECH-413/414) ---
- id: INV-082
  title: "Loveability as safe-base structural prerequisite for ethical development: correction, social learning, and post-error repair can be integrated without self-erasure only if care is internalised as personally-applicable and stable."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - INV-043
    - MECH-413
    - MECH-414
  pending_substrate_reconfirmation: true
  subject: ethics.loveability_safe_base_prerequisite
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:INV-082@2026-07-01T01:23:09.896220Z"
      as_of: 2026-07-01
      verdict: held_v4_by_architectural_commitment/applied
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - INV-043    # caregiver requirement (the substrate this gives a safe-base FUNCTION to)
    - MECH-158   # caregiver-scaffolding mechanism
    - INV-029    # love as long-horizon coherence
    - ARC-024    # loveability / self-valence substrate
    - ARC-047    # SocialGridWorld measurement substrate
    - MECH-413   # correction-without-annihilation (consumes the safe base)
    - MECH-414   # love-mediated repair (consumes the safe base)
  location: evidence/planning/loveability_ethical_agency_v5_plan.md
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: requires the V5 caregiver/social-learning substrate
    (INV-043, MECH-158, ARC-024, ARC-047) AND the correction/repair mechanisms MECH-413
    (correction-without-annihilation) and MECH-414 (love-mediated repair) to exist as
    built, testable substrate. Confirmed absent (2026-08-08): zero hits anywhere in
    ree-v3/ for caregiver, correction_without_annihilation, love_mediated_repair,
    safe_base, loveability, or SocialGridWorld. Deliberately out of scope until V5 --
    DO NOT build in V3.

    CONFIRMING: agents with stable, personally-applicable internalised care (loveability
    held as "this applies to me," not merely "care exists in the abstract") integrate
    correction and post-error repair events without a self-erasure signature -- no
    collapse of global self-valence, no policy collapse, no catastrophic self-model
    revision -- that matched agents with absent or unstable/non-personalised loveability
    DO show under the same correction/error events, holding across repeated episodes and
    specific to self-referential collapse (MECH-413's "I erred" vs "I am unloveable"
    separation), not general policy noise.

    FALSIFYING: correction/error integration succeeds equally well regardless of whether
    loveability is stable and personally-internalised -- some other substrate feature
    (raw prediction-error tolerance, generic policy-regularisation, reward-shaping alone)
    does the safe-base work, or no safe-base function is needed at all. Would falsify the
    structural-prerequisite framing and support treating loveability as enrichment (its
    pre-thought-intake status) rather than a prerequisite.
  notes: >
    Thought-intake elevates DEV-NEED-017 loveability from self-valence enrichment to a
    STRUCTURAL prerequisite: correction/social-learning/post-error repair integrate
    without self-erasure only if care is internalised as personally-applicable and
    stable (Salles 2024, bounded). Not captured by INV-043 (which asserts the caregiver
    REQUIREMENT) -- this asserts the safe-base FUNCTION of loveability. Node LOVE-2/
    LOVE-7. Replaces NEWCLAIM:loveability_safe_base_invariant. invariant_type EMERGENT:
    the safe-base function has no subject matter until the loveability/caregiver
    substrate and the correction/repair mechanisms exist, so emergent_from
    [INV-043, MECH-413, MECH-414] (all also in depends_on per the validator subset
    rule); pending_substrate_reconfirmation true because MECH-413/MECH-414 are
    candidate. substrate_conditional on the unbuilt V5 caregiver/social substrate.
    DO NOT build in V3.

- id: INV-083
  title: "Ethical agency requires live unethical affordances: the same capacities that enable ethical reasoning must include the potential to be unethical; harmful options must be representable as live possibilities biased-away-from, never deleted."
  claim_type: invariant
  invariant_type: universal
  subject: ethics.live_unethical_affordance_necessity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - INV-043    # caregiver requirement
    - INV-029    # love as long-horizon coherence
    - ARC-024    # loveability / self-valence substrate
  location: evidence/planning/loveability_ethical_agency_v5_plan.md
  notes: >
    User clarification: 'the same capacities which allow for ethical reasoning must
    include the potential to be unethical.' A system harmless by incapacity is not an
    ethical agent; harmful options must be representable as live possibilities
    biased-away-from, never deleted. No existing claim asserts this negative
    constraint. Node LOVE-3; a standing constraint on all REE safety mechanisms.
    Replaces NEWCLAIM:live_unethical_affordance_invariant. invariant_type UNIVERSAL:
    this is a structural commitment about what an ethical agent IS -- it holds across
    every substrate generation (a substrate that deleted the affordances would, by
    this invariant, not host an ethical agent at all), so it carries no emergent_from.
    epistemic_category substrate_conditional only because the affordance space it
    constrains is not yet built. DO NOT build in V3.

- id: MECH-413
  title: "Correction without annihilation: caregiver/social correction must update rule/harm/residue models without global self-valence collapse; the agent must separate 'I erred' from 'I am unloveable / must appease / must self-erase'."
  claim_type: mechanism_hypothesis
  subject: development.correction_without_self_valence_collapse
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - INV-043    # caregiver requirement
    - MECH-158   # caregiver-scaffolding mechanism -- possible overlap, see notes
    - INV-082    # loveability safe-base (the base this consumes)
    - INV-083    # live unethical affordance (the alternative this consumes)
  location: evidence/planning/loveability_ethical_agency_v5_plan.md
  notes: >
    Caregiver/social correction must update rule/harm/residue models without global
    self-valence collapse; the agent must separate 'I erred' from 'I am unloveable /
    must appease / must self-erase'. The hinge mechanism consuming the safe base
    (INV-082) plus the live alternative (INV-083). Readouts:
    post_correction_self_valence_stability, punishment_avoidance_vs_repair_
    discriminability. Node LOVE-4. Replaces
    NEWCLAIM:correction_without_annihilation_mechanism. possible_overlap_with:
    MECH-158 -- review for merge (caregiver scaffolding); possible_overlap_with:
    MECH-164 -- review for merge (self-like weighting under correction).
    substrate_conditional on the unbuilt V5 caregiver/social substrate.
    DO NOT build in V3.

- id: MECH-414
  title: "Love-mediated repair after harm: repair must be mediated by harm-recognition AND preserved loveability so it is relationship-restoration plus responsibility-integration, not punishment-avoidance, appeasement, self-erasure, control, or metric-optimisation."
  claim_type: mechanism_hypothesis
  subject: development.love_mediated_repair
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-159   # caregiver/social repair scaffolding
    - MECH-413   # correction-without-annihilation (the prior hinge)
    - INV-082    # loveability safe-base (preserved loveability the repair requires)
    - INV-043    # caregiver requirement
  location: evidence/planning/loveability_ethical_agency_v5_plan.md
  notes: >
    DEV-NEED-018 repair-after-harm is in the curriculum narrative but lacks a
    dedicated claim/gate. Repair must be mediated by harm-recognition AND preserved
    loveability so it is relationship-restoration plus responsibility-integration,
    not punishment-avoidance/appeasement/self-erasure/control/metric-optimisation.
    Node LOVE-5; couples DEV-NEED-017 plus DEV-NEED-018 as the intake requests.
    Replaces NEWCLAIM:love_mediated_repair_mechanism. possible_overlap_with: MECH-164
    -- review for merge (self-like weighting in repair); possible_overlap_with:
    INV-043 -- review for merge (caregiver requirement). substrate_conditional on the
    unbuilt V5 caregiver/social substrate. DO NOT build in V3.

- id: INV-084
  title: "Kindness is not constraint compliance: later REE kindness must NOT be inferred from rule-adherence, harm-avoidance, or prosocial output alone; it requires integrated care, otherness, inhibition, repair, and self-stable loveability UNDER live alternatives."
  claim_type: invariant
  invariant_type: universal
  subject: ethics.kindness_not_constraint_compliance
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v5
  v3_pending: true
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - INV-043    # caregiver requirement
    - MECH-158   # caregiver-scaffolding mechanism
    - INV-082    # loveability safe-base
    - INV-083    # live unethical affordance (the 'under live alternatives' clause)
    - MECH-414   # love-mediated repair (a constituent of genuine kindness)
  location: evidence/planning/loveability_ethical_agency_v5_plan.md
  notes: >
    Governance-warning invariant: later REE kindness must NOT be inferred from
    rule-adherence/harm-avoidance/prosocial output alone; it requires integrated
    care, otherness, inhibition, repair, and self-stable loveability UNDER live
    alternatives. Constrains the governance SCORECARD, not just the agent. The
    capstone evaluation discipline (node LOVE-6) that prevents declaring victory on a
    harmless-by-incapacity or appeasement-driven agent. Replaces
    NEWCLAIM:kindness_not_constraint_compliance_invariant. invariant_type UNIVERSAL:
    it is an epistemic/governance prohibition on how kindness may be attributed --
    it holds regardless of substrate (it is precisely a guard against substrate
    artefacts faking kindness), so it carries no emergent_from. epistemic_category
    substrate_conditional only because the capacities it audits are not yet built.
    DO NOT build in V3.
- id: ARC-099
  title: "Language-bootstrap enabling-condition contract: language only bootstraps when a specific pre-linguistic inventory is jointly satisfied -- shared world model, object tokens, action affordances, self-attribution, other-attribution, joint attention, partial observability, coordination pressure, episodic memory, rule apprehension, a low-cost signalling channel, partner variation, and repair. Sharpens MECH-010's abstract five-step sequence into a machine-checkable V6-entry precondition contract over landed V4/V5 nodes."
  claim_type: architectural_commitment
  subject: language.bootstrap_enabling_conditions
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v6
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  claim_level: architectural
  registered_utc: 2026-06-10
  depends_on:
    - ARC-009    # language as the symbolic mediation/coordination layer this contract gates entry to
    - INV-003    # language emerges (not bolt-on); the contract is what 'emerges-when' makes precise
    - MECH-010   # the abstract emergence sequence this contract sharpens into a precondition list
    - ARC-059    # the self->objects->others->language developmental spine the contract sequences
  location: evidence/planning/language_emergence_bootstrap_v6_plan.md
  notes: >
    Replaces NEWCLAIM:language_bootstrap_enabling_conditions (plan node LANG-2; also LANG-4 /
    LANG-8). Architectural_commitment because it asserts a precondition CONTRACT, not a mechanism:
    language only bootstraps when the enumerated pre-linguistic capacities are jointly satisfied by
    landed V4/V5 nodes. Directly from the 2026-06-05 grammar/LLM bootstrap addendum
    enabling-conditions table. epistemic_category substrate_conditional: every enabling condition is
    an upstream V4/V5 substrate not yet built, so any V3 probe is vacuous. DO NOT build in V3.
- id: ARC-100
  title: "Grammar/LLM primitive-mining (not import) commitment: grammatical structure and LLMs are MINED for candidate pre-linguistic primitive cuts, never imported as architecture. A mined cut is grounded only if it changes perception, attention, action, memory, rule-availability, or coordination in the substrate. No imported transformer block; no LLM as authority."
  claim_type: architectural_commitment
  subject: language.primitive_mining_not_import
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v6
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  claim_level: architectural
  registered_utc: 2026-06-10
  depends_on:
    - ARC-009    # language as a mediation LAYER over grounded substrate (what a mined cut must touch)
    - INV-003    # language emerges, not bolt-on -- the deep reason imports are forbidden
    - INV-007    # language cannot override embodied harm -- LLM-as-authority is the prohibited case
  location: evidence/planning/grammar_primitive_mining_v6_plan.md
  notes: >
    Replaces NEWCLAIM:grammar_mining_method (plan nodes GRAM-1/GRAM-2/GRAM-7). The load-bearing
    NEGATIVE architectural_commitment of the 2026-06-05 grammar/LLM intake, parallel to ARC-012 and
    fast_empathy_v5:EMP-1: grammar and LLMs are a MINE for candidate primitive cuts, never an
    imported architecture, and a cut counts as grounded only if it changes a substrate computation
    (perception/attention/action/memory/rule-availability/coordination). Constrains every other V6
    node. epistemic_category substrate_conditional. DO NOT build in V3.
- id: ARC-101
  title: "Language bootstraps from a grounded social ecology, not from grammar: given shared attention, partial observability, coordination pressure, a low-cost channel, and repair, proto-language partially bootstraps from grounded proto-communication. The grammar/LLM scaffold is a turbocharger, never the engine -- the engine is the V5 social ecology."
  claim_type: architectural_commitment
  subject: language.bootstrap_from_social_ecology
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v6
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  claim_level: architectural
  registered_utc: 2026-06-10
  depends_on:
    - ARC-009    # language as a coordination layer
    - INV-003    # language emerges functionally from the ecology
    - ARC-059    # self->objects->others->language spine: ecology precedes language
    - ARC-010    # mirror modelling / social coupling -- the ecology this bootstraps from
  location: evidence/planning/grammar_primitive_mining_v6_plan.md
  notes: >
    Replaces NEWCLAIM:language_bootstrap_from_ecology (plan node GRAM-6, the intake's strongest
    forward prediction). The bootstrap ENGINE is the V5 social ecology; grammar/LLM is a turbocharger.
    Most heavily gated node -- consumes ALL the V5 social plans + V4 individual substrate + MECH-163.
    Distinct from ARC-099 (the enabling-condition CONTRACT) and MECH-010 (the abstract sequence):
    this is the directional architectural bet about WHERE the bootstrap force comes from. Per
    feedback_biology_before_formal_definitions, needs the GRAM-7 lit-pull before promotion beyond
    candidate. epistemic_category substrate_conditional. DO NOT build in V3.
- id: ARC-102
  title: "Two distinct abstraction levels, not to be conflated: REE has SUBSTRATE-level abstraction (V4 theta-packaged units / cognitive-map traversal / type-encoder -- computed-over, sub-symbolic) and SYMBOLIC-scale abstraction (V6 recombination over named, grounded primitives). The symbolic level is the consequent reasoning capacity of the language layer and is categorically distinct from the V4 object-reasoning substrate."
  claim_type: design_decision
  subject: reasoning.symbolic_vs_substrate_abstraction_distinction
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v6
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  claim_level: architectural
  registered_utc: 2026-06-10
  depends_on:
    - ARC-009    # symbolic mediation: the layer the symbolic level lives in
    - MECH-299   # theta abstraction -- a SUBSTRATE-level abstraction primitive (the other side of the fork)
    - MECH-300   # cognitive-map traversal at active abstraction level -- substrate-level
    - SD-040     # type-encoder -- substrate-level type abstraction
  location: evidence/planning/abstract_relational_reasoning_v6_plan.md
  notes: >
    Replaces NEWCLAIM:symbolic_vs_substrate_abstraction_distinction (plan node ARR-1, the
    load-bearing scoping spine of the abstract-relational-reasoning plan). Names the consequent
    reasoning capacity of ARC-009 (symbolic mediation) as a level DISTINCT from the V4
    object_reasoning_abstraction substrate (MECH-299/300/SD-040). Analogue of ARC-080's type-vs-token
    fork; the scoping decision every node below it inherits. epistemic_category substrate_conditional
    (depends on the V4 substrate + the V6 symbol layer). DO NOT build in V3.
- id: ARC-103
  title: "Symbolic inference is subordinate to embodied harm sensing (the V6 inference-level instance of INV-007): symbolic, compositional, and analogical INFERENCE cannot override embodied harm sensing or the ethics-coherence authority. Recombination and structure-mapping are exactly the machinery that produces persuasive harmful rationalisations, so they remain subordinate to the harm/ethics authority by construction."
  claim_type: architectural_commitment
  subject: reasoning.symbolic_inference_subordinate_to_harm_sensing
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v6
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  claim_level: architectural
  registered_utc: 2026-06-10
  depends_on:
    - INV-007    # language cannot override embodied harm sensing -- lifted to the inference level here
    - MECH-011   # language as multiplier-not-substitute / rationalisation-loop risk
    - ARC-009    # symbolic mediation layer that this guards
  possible_overlap_with:
    - ARC-104    # language-input-level harm-override guard; ARC-103 is the INFERENCE-level instance, ARC-104 the language-INPUT instance, both operationalising INV-007 at different layers
  location: evidence/planning/abstract_relational_reasoning_v6_plan.md
  notes: >
    Replaces NEWCLAIM:symbolic_reasoning_subordinate_to_harm_sensing (plan node ARR-6, the safety
    claim of the linguistic tier). Negative architectural_commitment (like fast_empathy_v5's
    no-scalar prohibition and ARC-012). Lifts ACTIVE INV-007 (language cannot override harm) and
    MECH-011 (rationalisation-loop risk) to the symbolic-INFERENCE level. Deliberately kept distinct
    from ARC-104 (the language-INPUT-level guard): ARR-6 guards the inference engine's outputs,
    ARC-104 guards the language-input update path; both are instances of the same INV-007 authority
    at different layers (see possible_overlap_with). ethics_as_coherence_v5 owns the coherence
    authority it must not override. epistemic_category substrate_conditional. DO NOT build in V3.
- id: ARC-104
  title: "Language-cannot-override-harm guard (language-input level): symbolic/linguistic input may CONDITION priors, but the harm/ethics substrate retains veto authority over any language-mediated update. There must be NO path by which symbolic/linguistic content overwrites or suppresses a harm signal. Operationalises the universal invariant INV-007 as an explicit guard a design can violate."
  claim_type: architectural_commitment
  subject: language.harm_override_guard
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v6
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  claim_level: architectural
  registered_utc: 2026-06-10
  depends_on:
    - INV-007    # the universal invariant this guard operationalises
    - ARC-009    # the language layer whose updates are guarded
    - SD-011     # the dual-nociceptive harm substrate that retains veto
  possible_overlap_with:
    - ARC-103    # the inference-level instance of the same INV-007 authority; ARC-104 is the language-INPUT instance
  location: evidence/planning/language_trust_deception_institutions_v6_plan.md
  notes: >
    Replaces NEWCLAIM:language_harm_override_guard (plan node LTI-1). Operationalises the ACTIVE
    universal invariant INV-007 as an explicit guard MECHANISM-COMMITMENT (currently asserted but not
    realised as something a design can violate). Modelled on ARC-012 as a negative architectural
    commitment: no path by which symbolic/linguistic content overwrites or suppresses a harm signal.
    Source: trust_and_deception.md ('informative but not authoritative'). Kept distinct from ARC-103
    (the symbolic-INFERENCE-level instance): this guards the language-INPUT update path, ARC-103
    guards inference outputs; both instances of INV-007 at different layers (see possible_overlap_with).
    epistemic_category substrate_conditional. DO NOT build in V3.
- id: MECH-415
  title: "Predicate-argument-event rule bridge: a V6 grammar-facing layer renders ARC-063 CandidateRule structures as 'if context, then action-object, causing outcome, unless exception' -- naming, querying, and recombining pre-linguistic rules without making language the rule substrate. Predicate-argument-event structure is the same context->action-object->outcome shape ARC-063 already mints; the 'unless/except/but' exception grammar is a shadow of ARC-063 tolerance-gated availability."
  claim_type: mechanism_hypothesis
  subject: language.predicate_argument_event_rule_bridge
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v6
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  claim_level: mechanistic
  registered_utc: 2026-06-10
  depends_on:
    - ARC-063    # CandidateRuleField: the rule shape this layer renders grammatically
    - ARC-062    # weak-reading rule apprehension -- the upstream rule-state validation prerequisite
    - MECH-278   # object causal coherence -- the argument-slot grounding (object tokens / affordances)
  location: evidence/planning/grammar_primitive_mining_v6_plan.md
  notes: >
    Replaces NEWCLAIM:predarg_event_rule_bridge (plan node GRAM-3, the tightest hook in the 2026-06-05
    grammar/LLM intake, Line C / Section 6). A V6 grammar-FACING readout over ARC-063, not a new rule
    substrate -- the predicate-argument-event shape is what ARC-063 already mints, and 'unless/except'
    grammar shadows ARC-063 tolerance-gating. Distinct from a facet-of-ARC-063 mapping (cf.
    signal_to_rule_minting which WAS folded into ARC-063) because this is a separate grammar-rendering
    LAYER on top, not the minting mechanism itself. Blocked on ARC-063 behavioural validation + V4
    argument-slot grounding. epistemic_category substrate_conditional. DO NOT build in V3.
- id: MECH-416
  title: "REE frame inventory: a small extensible V5/V6 inventory of scene-frames (feeding, hazard, contact, interruption, help-harm, give-receive, request-response, belief-report, error-correction) where each frame binds to REE substrates, not to words. Frame semantics (Fillmore): a word evokes a whole scene; in REE each frame is a scene-schema over rule structures, the second mining artifact after the grammar->substrate table."
  claim_type: mechanism_hypothesis
  subject: language.frame_inventory_substrate_bound
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v6
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  claim_level: mechanistic
  registered_utc: 2026-06-10
  depends_on:
    - ARC-009    # the mediation layer the frames live in
    - ARC-063    # frames as scene-schemas over CandidateRule structures
  location: evidence/planning/grammar_primitive_mining_v6_plan.md
  notes: >
    Replaces NEWCLAIM:ree_frame_inventory (plan node GRAM-4, the intake's Line E). Frame semantics
    (Fillmore): words evoke whole scenes. Each frame binds to REE substrates, not words. Social frames
    (help-harm, give-receive, request-response, belief-report) blocked on the V5 social plans;
    non-social frames (feeding, hazard, contact, interruption, error-correction) seedable from V3/V4
    substrate. epistemic_category substrate_conditional. DO NOT build in V3.
- id: MECH-417
  title: "Grammatical aspect is a map of REE's event arc and post-commit closure: starting/ongoing/repeated/interrupted/resumed/completed/failed/abandoned aspect is a grammatical naming of the pre-linguistic ActionEventArc and closure machinery, not a replacement for it. Event Segmentation Theory: experience segments around prediction error and event boundaries, overlapping REE's contact/interruption/closure/transition needs."
  claim_type: mechanism_hypothesis
  subject: language.aspect_event_arc_closure_map
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v6
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  claim_level: mechanistic
  registered_utc: 2026-06-10
  depends_on:
    - ARC-063    # action-arc rules: the arc that aspect names
    - MECH-278   # object causal coherence -- completion/interruption conditions live over object events
  location: evidence/planning/grammar_primitive_mining_v6_plan.md
  notes: >
    Replaces NEWCLAIM:aspect_closure_map (plan node GRAM-5, the intake's Line F). Grammatical aspect
    is a MAP of the event arc + post-commit closure, naming it without replacing the closure
    machinery. Shares the event-arc spine with the twin 2026-06-05 cross-version intake and with
    MECH-422 (event_arc_grammatical_realisation) -- this node is the ASPECT/closure facet, MECH-422
    the tense/connective facet. Blocked on grounded affordances with interruption/completion
    conditions + the interrupted-task resumption substrate. epistemic_category substrate_conditional.
    DO NOT build in V3.
- id: MECH-418
  title: "Language-parsed other-affect feeds the empathy stream-binding layer as ONE evidence source: the affect prior recovered from language by the LanguageAffectAdaptor (MECH-373) is fused with non-linguistic evidence (behaviour, expression, context) by the existing V5 empathy stream-binding layer, NOT as a parallel or dominant affect-attribution path. The wiring/fusion commitment between the adaptor output and the empathy layer."
  claim_type: mechanism_hypothesis
  subject: language.affect_prior_feeds_empathy_binding
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v6
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  claim_level: mechanistic
  registered_utc: 2026-06-10
  depends_on:
    - MECH-373   # the LanguageAffectAdaptor whose output this wires into the empathy layer
    - MECH-031   # derived social tags / empathy coupling -- the binding layer the prior fuses into
  location: evidence/planning/language_affect_adaptor_v6_plan.md
  notes: >
    Replaces NEWCLAIM:language_affect_feeds_empathy_binding (plan node LAA-4). Ensures the language
    tier plugs INTO the V5 social machinery (fast_empathy_v5:EMP-3 stream-binding; MECH-031 derived
    social tags) rather than duplicating it -- consistent with the language/ToM dissociation intake
    and the grammar/LLM 'turbocharger not engine' discipline. Distinct from MECH-373 (the adaptor
    itself): this is the wiring/FUSION commitment between the adaptor output and the empathy layer.
    epistemic_category substrate_conditional. DO NOT build in V3.
- id: MECH-419
  title: "Compositional generalisation over named symbols: the agent systematically recombines grounded, named primitives (object/action/property/relation symbols) into novel combinations never directly experienced, via the symbol/grammar layer rather than the substrate. Compositionality is mined from grounded substrate, not imported as transformer attention."
  claim_type: mechanism_hypothesis
  subject: reasoning.compositional_generalisation_over_named_symbols
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v6
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  claim_level: mechanistic
  registered_utc: 2026-06-10
  depends_on:
    - ARC-009    # the symbol/grammar mediation layer the recombination operator lives in
    - INV-003    # language emerges -- compositionality is mined, not bolted on
    - SD-040     # type-encoder -- the named primitives recombined
    - MECH-299   # theta abstraction substrate over which symbols ground
    - MECH-300   # cognitive-map traversal substrate
  location: evidence/planning/abstract_relational_reasoning_v6_plan.md
  notes: >
    Replaces NEWCLAIM:compositional_generalisation_over_symbols (plan node ARR-2, the core
    symbolic-scale capacity). Recombination operator is the symbol layer (language_emergence_bootstrap_v6
    / grammar_primitive_mining_v6) over the V4 type-encoder/cognitive-map substrate. Per the
    anti-import discipline (ARC-100), compositionality is mined from grounded substrate not imported.
    epistemic_category substrate_conditional. DO NOT build in V3.
- id: MECH-420
  title: "Relational/propositional inference over named relations: transitive inference, role-binding (giver vs receiver), and proposition chaining are realised as theta-sequence traversal (MECH-300) of a SYMBOLIC/type-graph cognitive map whose nodes are named symbols. The V6 realisation of MECH-300's cognitive-map-traversal primitive over a named symbolic map."
  claim_type: mechanism_hypothesis
  subject: reasoning.relational_propositional_inference_over_symbols
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v6
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  claim_level: mechanistic
  registered_utc: 2026-06-10
  depends_on:
    - MECH-300   # cognitive-map traversal at active abstraction level -- the substrate primitive realised here
    - ARC-063    # CandidateRule rule shape (context->action-object->outcome) the relations chain over
    - ARC-009    # the symbolic mediation layer over which the named map is built
  location: evidence/planning/abstract_relational_reasoning_v6_plan.md
  notes: >
    Replaces NEWCLAIM:relational_propositional_inference (plan node ARR-3). The V6 realisation of
    MECH-300's cognitive-map-traversal-at-active-abstraction-level primitive, now over a NAMED
    symbolic map. Grounds in ARC-063 rule shape per the grammar_llms_v5 ARC-063 bridge; social-argument
    relations additionally need the V5 other-model (mirror_modelling_other_self_v5).
    epistemic_category substrate_conditional. DO NOT build in V3.
- id: MECH-421
  title: "Analogy as relational structure-mapping across grounded domains (Gentner): map relational structure from source to target regardless of surface features (spatial containment <-> social inclusion; physical support <-> commitment). The capstone symbolic capacity; first tested as emergent from the compositional + relational stack before any dedicated analogy module is built."
  claim_type: mechanism_hypothesis
  subject: reasoning.analogy_structure_mapping_across_domains
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v6
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  claim_level: mechanistic
  registered_utc: 2026-06-10
  depends_on:
    - MECH-300   # cognitive-map traversal -- the candidate substrate analogy may emerge from
    - ARC-009    # the symbolic mediation layer
  possible_overlap_with:
    - Q-074      # the open question of whether analogy is a separate operator OR emerges from MECH-300 traversal; MECH-421 is the mechanism, Q-074 the unresolved build decision
  location: evidence/planning/abstract_relational_reasoning_v6_plan.md
  notes: >
    Replaces NEWCLAIM:analogy_structure_mapping (plan node ARR-4, the capstone symbolic capacity,
    highest-risk / most prose-heavy). Resist an analogy module: first test whether it emerges from the
    MECH-419/MECH-420 stack (no-module discipline, mirroring fast_empathy_v5). The build-vs-emerge
    decision is carried as the open question Q-074 (see possible_overlap_with). Needs the
    relational-reasoning lit-pull (ARR-7) before promotion. epistemic_category substrate_conditional.
    DO NOT build in V3.
- id: MECH-422
  title: "Event-arc grammatical realisation: grammatical tense/aspect/connectives (because, but, unless, done, again, stop, resume) are the symbolic rendering of the pre-linguistic ActionEventArc (initiate/persist/interrupt/reorient/resume/close). 'unless' = ARC-063 exception gate; 'because' = causal link; 'again/done' = aspect over closure. The action arc must EXIST (V3..V5) before grammar can name it."
  claim_type: mechanism_hypothesis
  subject: language.event_arc_grammatical_realisation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v6
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  claim_level: mechanistic
  registered_utc: 2026-06-10
  depends_on:
    - ARC-009    # the symbolic mediation layer the connectives live in
    - ARC-063    # 'unless' = the exception gate; the action-arc rules the grammar names
  possible_overlap_with:
    - MECH-417   # aspect_closure_map shares the event-arc spine; MECH-422 is the tense/connective facet, MECH-417 the aspect/closure facet
  location: evidence/planning/abstract_relational_reasoning_v6_plan.md
  notes: >
    Replaces NEWCLAIM:event_arc_grammatical_realisation (plan node ARR-5). Tightest grounding in the
    cross-version + grammar_llms 2026-06-05 intakes: the action arc must EXIST before grammar can name
    it. Connectives are relations between propositions (needs MECH-419/MECH-420); the grammatical cuts
    themselves are owned by grammar_primitive_mining_v6. Shares the event-arc spine with MECH-417
    (the aspect/closure facet) -- this is the tense/connective facet (see possible_overlap_with). Ties
    the ActionEventArc to project_interrupted_task_resumption_gap (the interrupt->resume span).
    epistemic_category substrate_conditional. DO NOT build in V3.
- id: INV-085
  title: "Uncertainty-propagation invariant: affect recovered from language MUST enter agent-state modelling as a probability distribution (an uncertain other-agent prior), NEVER as a categorical emotion truth, and must remain overridable by the agent's own grounded harm/threat streams. Any path that lets a confident text label override grounded harm sensing violates this invariant."
  claim_type: invariant
  invariant_type: universal
  subject: language.parsed_affect_is_uncertain_hypothesis
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v6
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-373   # the LanguageAffectAdaptor whose output this constrains
    - INV-007    # language cannot override embodied harm -- the parent invariant this extends to affect parsing
    - Q-007      # affect as uncertain dimensional state, not categorical (the constructionist reframe)
    - ARC-009    # the language layer the parsed affect enters through
  location: evidence/planning/language_affect_adaptor_v6_plan.md
  notes: >
    Replaces NEWCLAIM:parsed_affect_is_hypothesis_not_truth (plan node LAA-2; LAA-5 arm B is its
    falsifiable test). The genuine gap the 2026-06-07 seed thought only implied: MECH-373 names the
    adaptor and commits to 'treat emotion labels as uncertain latent-state hypotheses', but the
    uncertainty-as-architecture commitment deserves its own NEGATIVE epistemic invariant, testable as
    such (any path that lets a confident text label override grounded harm sensing violates it).
    invariant_type UNIVERSAL: it is a safety/epistemic PROHIBITION on how parsed affect may be
    treated -- it holds regardless of substrate (it is precisely a guard against a language label
    masquerading as ground truth), so it carries NO emergent_from. It is the affect-parsing
    specialisation of INV-007 and grounds in the Q-007 constructionist reframe (Barrett: emotions are
    constructed categories, not natural kinds). epistemic_category substrate_conditional only because
    the language interface it audits is not yet built. DO NOT build in V3.
- id: Q-074
  title: "Is analogy a SEPARATE V6 alignment operator, or does it fall out of MECH-300 cognitive-map traversal over a shared symbolic map (same map, different entry points)? Decides whether the analogy mechanism (MECH-421) is built as a distinct operator or expected to emerge from the relational-inference stack."
  claim_type: open_question
  subject: reasoning.analogy_operator_vs_emergent_traversal
  polarity: asks
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  what_would_answer: |
    Answered only once a V6 symbolic / relational-inference stack exists to
    observe, not by a V3 run. Build the relational-inference stack
    (MECH-419/MECH-420) over a shared symbolic / type-graph cognitive map
    traversable by MECH-300 theta-sequence traversal, then test for analogy
    WITHOUT a dedicated operator: present source-target pairs requiring relational
    structure-mapping (spatial containment <-> social inclusion; physical support
    <-> commitment, per MECH-421) and see whether correct analogical transfer
    EMERGES from map traversal at the active abstraction level (same map,
    different entry points). ANSWER 'emergent -- no separate operator needed' if
    relational transfer arises from MECH-300 traversal over the shared map alone,
    at rates matching an explicit-operator arm; ANSWER 'analogy is a separate V6
    operator (MECH-421 built as distinct)' if traversal alone fails to map
    relational structure across surface-dissimilar domains and an explicit
    structure-mapping module is required to recover the transfer. HARD
    PRECONDITION: the shared symbolic stack must EXIST and be non-degenerate --
    the cognitive map must actually encode relational structure (not just surface
    features) and MECH-300 traversal must operate at a conceptual/type-graph
    abstraction level; until the symbolic stack is built there is no map to
    traverse and the build-vs-emerge fork cannot be observed. V6; off the V3
    critical path. DO NOT build in V3 or queue an experiment against this.
  implementation_phase: v6
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  registered_utc: 2026-06-10
  depends_on:
    - MECH-300   # the cognitive-map traversal primitive analogy might fall out of
  possible_overlap_with:
    - MECH-421   # the analogy mechanism whose build-vs-emerge status this question decides
  location: evidence/planning/abstract_relational_reasoning_v6_plan.md
  notes: >
    Replaces NEWCLAIM:relational_abstraction_open_question (plan node ARR-4 carried open question).
    Decides whether the analogy mechanism (MECH-421) is built as a distinct operator or expected to
    emerge from the relational-inference stack (MECH-419/MECH-420). Cannot be resolved until the
    symbolic stack exists to observe; epistemic_category substrate_conditional. DO NOT build in V3.
- id: Q-076
  title: "Does the INTERACTION of retrieval-readiness gain (MECH-425 Line A) and content-selective suppression (MECH-425 Line B) govern the interference-vs-forgetting tradeoff? Predicted regimes: high gain WITHOUT selective suppression -> problematic interference (too many traces accessible at once); low gain -> apparent forgetting; competent retrieval = high gain + sharp competitive suppression."
  claim_type: open_question
  subject: memory.retrieval_gain_x_suppression_interaction
  polarity: asks
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  registered_utc: 2026-06-12
  depends_on:
    - MECH-425   # the two-layer retrieval-gating mechanism whose INTERACTION this question tests
  possible_overlap_with:
    - MECH-094   # the high-interference regime (many wrong traces accessible) is a route INTO the MECH-094 confabulation failure mode -- the interaction is the upstream control variable, cross-ref not duplicate
  location: docs/thoughts/2026-06-12_histaminergic_retrieval_accessibility_gating.md
  source:
    - docs/thoughts/2026-06-12_histaminergic_retrieval_accessibility_gating.md
  what_would_answer: >
    SUBSTRATE-CONDITIONAL (needs the MECH-425 retrieval substrate -- both knobs). 2x2 design:
    {gain high, gain low} x {selective suppression ON, OFF}, with >=2 competing co-encoded traces scorable
    individually. PREDICTED signature: (high gain, suppression OFF) -> highest competitor-intrusion /
    interference rate; (low gain, *) -> lowest correct-trace reinstatement (apparent forgetting); (high gain,
    suppression ON) -> highest correct-trace reinstatement with low intrusion (the competent regime).
    ANSWERED = the interference rate depends on the gain x suppression INTERACTION term (not on either main
    effect alone) in the predicted direction. NON-ANSWER / self-route substrate_not_ready: if intrusion and
    correct-recall metrics are floor/ceiling-pinned (no cross-cell variance) the interaction is unmeasurable
    -- NOT a verdict.
  notes: >
    The headline candidate from the 2026-06-12 histaminergic-retrieval intake: the two control layers are
    not independent -- it is their PRODUCT that sets retrieval competence. Daniel's source intuition
    ("access to all memories could produce problematic interference; active management of which cues are
    retrievable would be needed") is exactly the high-gain / no-suppression corner. substrate_conditional:
    testable only on a substrate exposing BOTH the Line-A gain knob and the Line-B suppression operator
    (see MECH-425). DO NOT build in V3.
- id: Q-077
  title: "Is SD-046's multi-slot GoalState a set of DISCRETE fixed-capacity slots (Cowan ~4-chunk focus-of-attention) or a single FLEXIBLE precision-resource shared across goals (Bays & Husain), with the acted-on goal drawing disproportionate precision? The human capacity literature is itself unresolved on slot-vs-resource; the answer sets whether SD-046 degrades by a hard cutoff past N or by graceful precision dilution as goals are added."
  claim_type: open_question
  subject: goal.multi_slot_discrete_vs_resource
  polarity: asks
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  registered_utc: 2026-06-13
  depends_on:
    - SD-046           # the multi-slot GoalState whose architecture this fork decides
    - SD-027           # capacity-limited access gate; the discrete-vs-resource answer constrains its k
    - MECH-254         # top-k selection (discrete) vs precision-weighted soft allocation (resource)
  possible_overlap_with:
    - SD-028           # if resource wins, the attentional template IS the precision-allocation object (Bays&Husain) -- cross-ref not duplicate
  what_would_answer: >
    SUBSTRATE-CONDITIONAL (needs the SD-046 multi-slot substrate, GDL-1). Escalating-load design:
    a multi-goal arena with N = 1..K simultaneously-active goals, each goal's behavioural/representational
    fidelity scored individually. DISCRIMINATING signature: discrete-slot model predicts a STEP function --
    fidelity holds flat up to a fixed N (~4, Cowan) then the marginal goal drops to a floor; resource model
    predicts SMOOTH degradation -- each added goal dilutes the shared precision budget (power-law, Bays &
    Husain), with the acted-on/attended goal drawing disproportionate fidelity. ANSWERED = the degradation
    curve discriminates step-function (slot) vs continuous-dilution (resource), AND the acted-on-goal
    precision-bias is present (resource) or absent (pure equal slots). NON-ANSWER / self-route
    substrate_not_ready: if fidelity metrics are floor/ceiling-pinned across N (no cross-load variance) the
    curve shape is unmeasurable -- NOT a verdict.
  notes: >
    Registered 2026-06-13 from the goal_deliberation_v4:GDL-8 frontopolar-deliberation lit-pull completion-set
    harvest (proposal P3, user said "register all"). The fork is forced by a genuine, unresolved split in the
    source literature: Cowan 2001 (focus-of-attention ~4 discrete chunks) vs Bays & Husain 2008 (flexible
    shared precision resource), with Heilbronner & Pollmann 2009 showing branching capacity is WM-load-bounded
    rather than structurally fixed. This is the slot-vs-resource design decision SD-046's current
    discrete-slot framing makes implicitly; the Bays & Husain + Heilbronner lit entries are marked MIXED
    against SD-046 precisely because they problematise that framing. Answering it sets whether the V4 substrate
    holds N hard slots or a graded precision budget. DO NOT build in V3. Hybrid "slots+averaging" models exist
    in the source literature and are a third possible answer.
  location: evidence/literature/targeted_review_frontopolar_deliberation/
- id: Q-078
  title: "Is the E3 committed-action conversion ceiling CONSTITUTIONAL (the rules by which candidate actions become eligible to win -- eligibility mask, Go/No-Go pressures, conflict-sensitive hold, threshold/widening, recurrent competition, commitment permission, post-commit latch -- are under-specified) rather than PARAMETRIC (upstream signals too weak / modulatory channels too quiet)? Across mechanistically distinct channels (CRF V3-EXQ-654g, OFC V3-EXQ-485h, SD-037 V3-EXQ-625e, dACC V3-EXQ-445h, modulatory V3-EXQ-569g) a formed-and-measurable signal reaches E3 authority yet committed action does not change reliably -- 'signal strength can exist without lawful access to committed action.' The basal-ganglia action-selection bottleneck (direct/Go, indirect/No-Go, hyperdirect-STN conflict-hold, striatal channel competition, pallidal permission-to-commit) is the functional reference; the open question is whether a BG-like selection CONSTITUTION is required BEYOND MECH-439's F-variance-share rebalance / shortlist levers."
  claim_type: open_question
  subject: selection.committed_action_gating_constitution
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: answer_state
  version_relevance: v3
  registered_utc: "2026-06-20"
  depends_on:
    - MECH-439   # F-variance monopoly: the SPECIFIC selection-variance instance; this Q asks whether the broader eligibility-gating constitution is the real locus
    - ARC-107    # the BG-like selector constitution this question is the umbrella over
    - ARC-106    # biology-grounding constraint: a BG-like selector must be load-bearing (divergence ledger + falsifier), not decorative
    - MECH-309   # monomodal-policy-collapse equilibrium
  what_would_answer: >
    Adjudicated by V3-EXQ-689a (the MECH-439 conflict-graded 2x2 falsifier) and its successors:
    (a) 689a gap-CONCENTRATED PASS (committed-class entropy strict-above BOTH collapsed-proposer
    AND gap-blind controls, lift correlates with per-tick top-F gap) => PARAMETRIC reading favoured
    for the V3 tract; the minimal hyperdirect-like lever suffices, no constitutional redesign.
    (b) 689a UNIFORM lift (gap-blind controls match) OR readiness-met-NO-lift => CONSTITUTIONAL
    reading favoured; escalate to rank-preserving F->eligibility demotion (MECH-448) / divisive
    normalisation / Go-NoGo eligibility-set governance (MECH-449), NOT more upstream gain.
    (c) readiness FAILURE => repair the falsifier first, draw no architectural conclusion.
    A constitutional-selector experiment counts as SUPPORTING only on LAWFUL, channel-specific,
    context-appropriate conversion into committed action (note section 8): non-vacuity (channels
    reach the selector), selector-engagement (eligibility/hold/Go-NoGo vary), specificity,
    gap/conflict sensitivity where claimed, gap-blind controls do NOT explain the lift, safety
    (no global disinhibition of harmful classes), and >=1 previously-gated downstream channel
    CONVERTS -- entropy increase alone (especially via added stochasticity) does NOT count.
    NON-DEGENERACY: the eligibility/hold/Go-NoGo selector variables must carry live cross-arm
    variance, else the run self-routes substrate_not_ready_requeue (not a verdict).
  location: evidence/planning/thought_intake_2026-06-20_basal_ganglia_selector_constitution.md
  notes: >
    Registered 2026-06-20 from the basal-ganglia-selector-constitution design-pressure note
    (evidence/planning/thought_intake_2026-06-20_basal_ganglia_selector_constitution.md; NOT a build
    request). Umbrella question over the MECH-439 F-dominance conversion-ceiling cluster
    (failure_autopsy_f-dominance-conversion-cluster_2026-06-20; behavioral_diversity_isolation:GAP-I).
    SEQUENCED BEHIND V3-EXQ-689a: hold as design pressure until 689a lands; do not implement a selector
    constitution now (overbuild / cargo-cult-biology / noise-as-diversity risks, note s9). GATE AROUND
    INSTANTIATION: registration captures the question; whether REE builds a BG-like constitution is a
    later, 689a-gated decision (capture-now / decide-build-later, per user directive 2026-06-20).
    Children MECH-447 (lever-sufficiency) / MECH-448 (residual-ceiling) / MECH-449 (Go-NoGo constitution)
    + ARC-107 (the architecture) registered alongside; /claim-synthesis post-689a discriminates
    genuinely-independent children from design alternatives (anti-proliferation rail).

- id: Q-079
  title: "Is there a distinct, useful mathematical object -- a 'structured uncertainty field' / dynamic latent-scale inference field (DLIF) -- that jointly provides directed evidence update, cyclic coherence constraint, latent-node birth/death/merge/split, granularity/scale shift, action coupling, precision modulation, offline consolidation, AND residue/non-erasure; or does it collapse to a re-notation of existing formalisms (dynamic factor graphs + Bayesian-nonparametric structure learning + active inference + ARC-013 residue)?"
  claim_type: open_question
  subject: research.structured_uncertainty_field_object
  polarity: asks
  status: resolved
  live_status:
    reading: resolved
    as_of: 2026-07-11
    needs_review: false
  resolution: answered_negative
  resolution_note: |
    RESOLVED 2026-06-20 (governance disposition of the R1 /lit-pull novelty drill, landed REE_assembly
    master 499c251dcb; verdict ANSWERED-NEGATIVE). The DLIF / structured-uncertainty field is NOT a
    distinct mathematical object: it is a re-notation / engineering synthesis of existing formalisms.
    DLIF ~= factor-graph unification of directed+undirected models (Frey 2003) + active inference on
    factor graphs (de Vries & Friston 2017) + structure learning by expansion + Bayesian model reduction
    (Smith et al 2020) / Bayesian nonparametrics (Fox et al 2011) + scale-free Renormalising Generative
    Models (Friston et al 2024) + ARC-013 residue. A SINGLE existing framework -- hierarchical active
    inference in its scale-free RGM form, on Forney factor graphs, with structure learning -- already
    jointly delivers the four load-bearing capacities (a) directed update + (b) cyclic coherence +
    (c) latent birth/death/merge/split + (d) granularity/scale shift, plus action coupling, precision,
    and offline consolidation. Coverage matrix earned cell-by-cell in
    evidence/literature/targeted_review_q_079/SYNTHESIS.md (non-degeneracy guard satisfied). Confirms
    pilot HA1 (field-first buys representational ergonomics, not new computational power) / HA6
    (combination-of-known-pieces is a weaker claim than 'a new object') / R1 (novelty is the drill's
    first question -- answered: no).
    DISPOSITION:
    (1) Repo question CLOSED: do NOT build the standalone 'structured-uncertainty-fields' repo. It was
        gated on ANSWERED-POSITIVE + SD-062 utility; the ANSWERED-NEGATIVE result removes the novelty
        leg. (A runnable INTEGRATION of these existing pieces with REE's residue channel could still be
        an engineering artifact, but that is an SD-062 utility decision, not a math-novelty claim, and
        remains gated -- not reopened here.)
    (2) The ONLY candidate-distinct ingredient is residue/non-erasure (every surveyed formalism scores
        N; BMR + the RG operator are structurally ANTI-residue). The falsifier requires residue shown
        SEPARABLE from salience + uncertainty (HA2) -- a REE agent experiment, not a literature result --
        so it does NOT flip this verdict and this drill must NOT read as independent confirmation OR
        disconfirmation of ARC-013 (R2). CORRECTION 2026-06-21: HA2's experiment is V3-EXQ-697
        (residue-separability falsifier, queued 2026-06-21, off the green-board), NOT V3-EXQ-587. The
        2026-06-20 disposition mis-named "ARC-013's OPEN evidence path V3-EXQ-587": V3-EXQ-587 is the
        GAP-10 harm-gradient GEOMETRY experiment, which ALREADY RAN (2026-05-19, FAIL / non_contributory,
        reviewed) and tests residue FORMATION, not separability -- it is neither open nor a separability
        test. If -- and only if -- V3-EXQ-697 shows residue separable, a NARROW residue-object
        sub-question reopens (not the full DLIF).
    (3) Field/projection/residue vocabulary is kept as a thinking aid via the existing
        ARC-084 / ARC-013 / MECH-363 cross-refs (depends_on / related_claims above) -- no new claims
        (anti-proliferation).
    out_of_domain => promote/demote AND narrow_open_question are suppressed by the indexer regardless;
    this lit evidence is informational. No decision_log entry / decision_state reconcile is owed for an
    out_of_domain close.
  epistemic_category: out_of_domain
  version_relevance: v4_v5
  v3_pending: false
  registered_utc: "2026-06-20"
  resolved_utc: "2026-06-20"
  source_thought:
    - docs/thoughts/2026-06-20_structured_uncertainty_layer_for_ree_agent_design.md
    - docs/thoughts/2026-06-20_brain_native_hybrid_inference_objects.md
    - docs/thoughts/2026-06-20_dynamic_latent_scale_inference_field_capture.md
    - docs/thoughts/2026-06-20_dynamic_latent_scale_inference_field_research_map.md
    - docs/thoughts/2026-06-20_latent_information_fields_graphs_as_projections.md
    - docs/thoughts/2026-06-20_dynamic_latent_information_field_minimal_formal_specification.md
    - docs/thoughts/2026-06-20_dynamic_latent_information_field_critical_review_and_learning.md
    - docs/thoughts/2026-06-20_dlif_claims_index_pilot_result.md
  location: docs/thoughts/2026-06-20_dlif_claims_index_pilot_result.md
  depends_on:
    - ARC-084   # cognifold = single interacting state-space + typed signed coupling: the existing REE substrate that the field-native framing (cluster P1/P3) folds onto
    - ARC-013   # residue = persistent latent-space curvature: existing home of 'residue as field deformation' (cluster P2); its residue-separability (HA2) evidence path is V3-EXQ-697 (NOT V3-EXQ-587, which is the closed GAP-10 geometry experiment -- corrected 2026-06-21)
  related_claims:
    - MECH-363  # diffuse long-range competitive coupling = stability requirement (neighbours the structured-uncertainty-layer framing)
    - SD-062    # the claims-index application of the same framing (whether literally the SAME object = HA3, unresolved)
  what_would_answer: |
    ANSWERED-NEGATIVE (object is NOT novel -> close as a synthesis note, do NOT build a standalone repo):
    if an R1 literature drill (the cluster's research_map is its start) finds a SINGLE existing formalism
    -- e.g. dynamic factor graphs + Bayesian-nonparametric structure learning, or hierarchical active
    inference -- that already jointly delivers directed evidence update + cyclic coherence + latent-node
    birth/death/merge/split + scale-shift, such that DLIF adds only notation (pilot_result.md HA1/HA6).
    ANSWERED-POSITIVE (warrants a distinct object, possibly its own repo) ONLY if NO single formalism
    covers the combination AND the residue/non-erasure component is shown SEPARABLE from salience +
    uncertainty (pilot_result.md HA2 = ARC-013's separability falsifier V3-EXQ-697; NOT V3-EXQ-587 --
    corrected 2026-06-21, 587 is the closed GAP-10 geometry experiment), not merely re-described. Non-degeneracy
    guard: the drill must enumerate each candidate formalism's ACTUAL coverage against all four capacities,
    not assert a gap. Resolved by literature + derivation, NOT a REE agent experiment (hence out_of_domain);
    off the v3 strict green-board path (target 2026-07-19). The hidden-cause gridworld agent test is V4+.
  notes: |
    GENUINELY-NEW research-line umbrella node from the 2026-06-20 DLIF / structured-uncertainty thought
    cluster (Track A intake). Folds cluster propositions P1 (structured-uncertainty layer), P2 (residue as
    field deformation -> ARC-013), P3 (graphs-as-projections / field-native -> ARC-084) via the depends_on
    cross-refs above -- those are NOT re-registered as separate claims (already owned; anti-proliferation).
    The standalone 'structured-uncertainty-fields' repo is GATED on an ANSWERED-POSITIVE here + SD-062
    utility; not now. Highest-value preserved residue (pilot_result.md R1): treat 'is it novel or a
    re-notation?' as the drill's FIRST question, not its conclusion -- the overclaim risk the cluster's
    own critical review flagged.
    R1 LIT DRILL RESULT (2026-06-20, /lit-pull -> evidence/literature/targeted_review_q_079/, 5 entries,
    lit_conf 0.849, direction weakens x4 + mixed x1): VERDICT = ANSWERED-NEGATIVE. A single existing
    framework -- hierarchical active inference in its scale-free Renormalising-Generative-Model form
    (Friston et al 2024 arXiv:2407.20292), on Forney factor graphs (de Vries & Friston 2017), with
    structure learning by expansion + Bayesian model reduction (Smith et al 2020) / Bayesian nonparametrics
    (Fox et al 2011) -- already JOINTLY delivers the four load-bearing capacities (a) directed update +
    (b) cyclic coherence + (c) latent birth/death/merge/split + (d) granularity/scale shift, plus action
    coupling, precision, and offline consolidation. Coverage matrix earned cell-by-cell in
    evidence/literature/targeted_review_q_079/SYNTHESIS.md (non-degeneracy guard satisfied). DLIF is a
    re-notation / engineering synthesis, NOT a new mathematical object (confirms pilot HA1/HA6/R1). The
    ONLY candidate-distinct ingredient is residue/non-erasure (every surveyed formalism scores N; BMR + the
    RG operator are structurally ANTI-residue), but the falsifier requires residue shown SEPARABLE from
    salience+uncertainty (HA2) -- a REE experiment, not a lit result -- so it does NOT flip the verdict and
    must not read as independent confirmation of ARC-013 (R2). GOVERNANCE RECOMMENDATION: close Q-079 as a
    synthesis note + close the repo question (do NOT build structured-uncertainty-fields); keep
    field/projection vocabulary as the existing ARC-084/ARC-013/MECH-363 cross-refs (no new claims).
    out_of_domain => promote/demote suppressed; this lit evidence is informational. DISPOSITION APPLIED
    2026-06-20 (governance): status candidate->resolved (resolution: answered_negative); see resolution_note
    above. Repo question closed (do NOT build structured-uncertainty-fields).
    CORRECTION 2026-06-21: HA2's separability falsifier is V3-EXQ-697 (queued 2026-06-21, off the
    green-board), NOT V3-EXQ-587. The 2026-06-20 disposition mis-named V3-EXQ-587 as ARC-013's "OPEN"
    separability path; V3-EXQ-587 is the GAP-10 harm-gradient GEOMETRY experiment, which already ran
    (2026-05-19, FAIL / non_contributory, reviewed) and tests residue FORMATION, not separability.
  literature_evidence:
    - evidence/literature/targeted_review_q_079/SYNTHESIS.md   # R1 coverage matrix + ANSWERED-NEGATIVE verdict (5 entries, lit_conf 0.849)

- id: MECH-423
  title: "Cross-model super-additivity: integrating predictive models over a shared latent yields capability gains that EXCEED the sum of the isolated models, because shared features let each model's prediction-error corrections propagate to the others. Integrated (E1 world-model + E2 affordance-model [+ object spine]) beats a param/compute-matched bag of isolated modules -- the 'cross-pollination turbo-charges development' claim, made falsifiable."
  claim_type: mechanism_hypothesis
  subject: integration.cross_model_super_additivity
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-07-16
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-680d_2026-06-15
      as_of: 2026-06-15
      verdict: non_contributory/standard
  granularity_debt_disposition: coherent_campaign  # GOV-GRAN-1 human-adjudicated 2026-07-16 (NOT granularity debt); see granularity_debt_recurrence_note
  granularity_debt_recurrence_note: >
    GOV-GRAN-1 P1 discrimination (2026-07-16, session gifted-kilby-8e2491). Audit flagged
    3 no-verdict claim-keyed hits / 2 signatures (680b (standard, discrimination)
    additive_below_margin; 680c/680d (standard, readiness precondition) cosine NaN /
    min_cos<0). VERDICT: COHERENT ITERATION, NOT a too-coarse claim. 680b->c->d peel ONE
    experiment-harness defect layer by layer -- co-training numerical stability (unbounded
    world_R2 -> grad blow-up -> NaN cosine) and then the readiness-cosine readout that reads
    it -- on a substrate that DEMONSTRABLY WORKS (super-additive PAIR>ISOLATED on 5/5 seeds
    by 680d; the suppressed FORK-2 verdict would have PASSED). All three autopsies recommend
    epistemic_category=standard and recommended_substrate_queue_entry.action=none: there is NO
    ree_core / substrate gap. The 680d autopsy states it directly ("Recurrence is convergence,
    not granularity-debt"). /claim-synthesis CONTRAINDICATED: MECH-423's irreducible content is
    the SYSTEM-level assertion (integrated beats a param/compute-matched BAG of isolated modules);
    splitting into pairwise streams would duplicate MECH-081/MECH-082 (already the pairwise
    interaction claims) -- same shape as MECH-180 vs INV-050. Re-derive brake NEVER fired (each
    iteration fixed a distinct named defect and peeled to the next layer). epistemic_category
    KEPT as standard (no suppress-set value is honest for a working-substrate scaffold defect;
    0 substrate_ceiling verdicts, GOV-CEIL-1 unaffected). This disposition metabolizes the
    GOV-GRAN-1 recurrence; it PROMOTES/DEMOTES NOTHING (status stays candidate). The honest
    RESOLUTION (drops the claim out of no-verdict entirely) is to complete the iteration: queue
    680e with the corrected readiness gate (min(cos) tolerance-band, not >=0) via /queue-experiment
    -- 680e was routed by the 680d autopsy but is NOT yet in the queue (campaign stalled at 680d).
    Brake line for the lineage: if 680e (corrected gate, stable substrate) still returns a genuine
    finite net-negative cosine on a majority of seeds, route to /implement-substrate (shared-latent
    objective reconciliation) or narrow the claim -- NOT another 680f re-queue.
  implementation_phase: v3
  epistemic_category: standard
  version_relevance: v3
  claim_level: mechanistic
  registered_utc: 2026-06-12
  depends_on:
    - ARC-004    # the shared multi-timescale L-space latent that makes feature-sharing possible
    - ARC-001    # E1 world model -- one cross-pollinating stream
    - ARC-002    # E2 affordance model -- the second stream; E1<->E2 overlap is the pairwise instance
    - MECH-081   # E2 sufficiency constraint reduces E1 dimensionality target -- a registered pairwise interaction this generalises
    - MECH-033   # E2 kernels seed hippocampal rollouts -- an existing cross-module transfer path
  related_claims:
    - ARC-080    # CONCEPTUAL cross-ref, NOT a build dependency (moved out of depends_on 2026-07-01 PM /governance reverse-dep phase-consistency fix). ARC-080 is a THIN UMBRELLA / COHERENCE MAP (implementation_phase v4, v3_pending) that "introduces NO substrate code and changes NO V3 behaviour" -- it builds nothing, so it cannot be a V3 build prerequisite. MECH-423's falsifiable V3 test (what_would_answer) is E1+E2 super-additivity over the ARC-004 shared latent; the object spine is only the OPTIONAL THIRD ARM. The real blocker is the integration-readiness precondition (non-zero cross-module gradient / inference-loop convergence / MECH-121 offline transfer) + the pending lit-pull -- empirically the 680->680c lineage stalled on numerical co-training instability, never on object-file absence. Keeping ARC-080 in depends_on made a V3 claim spuriously read as V3-blocked-on-V4; it is a conceptual cross-cutting feature-donor, so it lives here.
  possible_overlap_with:
    - MECH-081   # MECH-081 is one PAIRWISE E2->E1 interaction; MECH-423 is the system-level super-additivity claim over many streams
    - MECH-082   # E2 distortion biases E1 sampling -- another pairwise propagation instance
  location: docs/thoughts/2026-06-12_cognifold_cross_pollination_and_language_stream.md
  source:
    - docs/thoughts/2026-06-12_cognifold_cross_pollination_and_language_stream.md
  what_would_answer: >
    V3 ablation. ARM INTEGRATED: E1 world-model + E2 affordance-model (and, in a third arm, the object
    spine) trained over a SHARED L-space latent with cross-module gradient flow. ARM ISOLATED:
    parameter- and compute-matched modules trained with NO shared features (gradient stop between
    streams). PASS = joint world-model R2 + affordance accuracy of the integrated arm EXCEEDS the
    additive baseline (sum of each isolated module's marginal gain) by a pre-registered margin. FAIL =
    integrated arm at-or-below the additive baseline. READINESS PRECONDITION (the discriminating
    non-degeneracy gate, per Daniel 2026-06-12): the integration machinery must demonstrably be doing
    cross-module work before the test means anything -- (i) the shared latent must carry NON-ZERO
    cross-module gradient (measured), (ii) the inference loop must be running to convergence, and
    (iii) offline/consolidation transfer (DMN-analogue + sleep replay, MECH-121) must be active and
    moving traces across modules. Below any of these the integrated and isolated arms are
    indistinguishable by construction and the run self-routes substrate_not_ready (check_degeneracy
    non_degenerate=false), NOT a FAIL. The concrete readiness metric/threshold is to be set by a
    pending literature pull on which integration prerequisites are necessary for super-additive gain.
  notes: >
    The 'turbo-charge' fragment (Topic 5 of the 2026-06-12 cognifold intake), made falsifiable. REE
    owns the cognifold (ARC-001/002/003/004/005/007), the object spine (ARC-080/081/082/083), and
    several pairwise transfer paths (MECH-033, MECH-081/082, MECH-121), but NO claim asserts that
    integration is SUPER-ADDITIVE rather than merely additive. MECH-423 is that assertion. Topic 2
    (E1<->E2 feature overlap beating either alone) is the pairwise instance and becomes the first
    experiment arm, not a separate claim. Topic 1 (E1 reaps error and self-improves) and Topic 4 (the
    cognifold itself) are owned-by-design and cross-referenced only, not registered. OPEN READINESS
    QUESTION (Daniel): the signal may be null until inference + DMN-style offline integration + sleep
    consolidation are themselves mature -- captured as the readiness precondition above and motivating
    the lit-pull. EXP-0380 in manual_proposals.v1.json carries the ablation design. epistemic_category
    standard, V3-tractable in principle but gated on the readiness precondition.
  evidence_quality_note: >
    2026-06-15 (governance): the EXP-0380 ablation lineage 680 -> 680a -> 680b -> 680c has NOT yet
    landed genuine experimental evidence; exp_conf stays 0.0 (all 6 entries are literature). 680a's
    PASS was a false positive on a baseline-LEVEL-SD margin (superseded). 680b (hardened SD-of-delta +
    absolute-floor margin) self-routed additive_below_margin/inconclusive -- mean delta_pair +0.539 > 0
    but 0/3 seeds clear the margin; failure_autopsy_V3-EXQ-680b CONFIRMED the lone -1.19 seed (456) is a
    numerical-instability artifact (world head diverged, grad norm 46x stable seeds; affordance head
    improved on the same seed), NOT negative transfer -- MECH-423 NOT weakened. 680c (grad-clip + bounded
    score) self-routed substrate_not_ready_requeue (non_degenerate=false): readiness cosine NaN poisoned
    by 4/5 PAIR seeds diverging despite grad-clip 1.0; failure_autopsy_V3-EXQ-680c CONFIRMED -- the lone
    stable seed (789) shows a genuine net-negative shared-encoder cosine (-0.40), MECH-423 NOT weakened.
    Both autopsies route to /queue-experiment 680d (stabilise PAIR co-training + guard the cosine
    aggregate); no substrate gap. 680d is the live path to a genuine super-additivity verdict.
    2026-07-16 (governance cycle 2026-07-16d): PROMOTED candidate -> provisional. V3-EXQ-680e
    (ree-cloud-2, cloud class) is the first clean experimental evidence PASS -- interpretation
    super_additive, evidence_direction supports, all 7 integration-readiness preconditions met
    (shared-latent grad coupled, inference loop iterated, E2 touchable under interleaved, interleaved
    share positive, blocked share zero, PAIR seeds finite + not net-negative transfer), load-bearing
    superadditivity_margin_pair passed. 680e completes the 680b->c->d->e campaign the
    granularity_debt_recurrence_note pre-registered as the honest resolution: it recalibrated the
    readiness cosine gate (min(cos)>=0 -> magnitude-floored tolerance band 0.15, pre-registered from
    680d's observed usable-seed noise), unmasking the super-additive verdict that 680d's false-routing
    gate had suppressed (680d's non_contributory FAIL was gradient-orthogonality noise, not negative
    transfer). Predecessor V3-EXQ-680d marked superseded. No v3_pending gate. Integrated-arm
    super-additivity over the ARC-004 shared latent now confirmed on cloud class.
- id: MECH-424
  title: "Language prediction as a corrective / instantiating predictive stream: when language is modelled as an externalisation of internal cognition, predicting language inside the integrated substrate can INSTANTIATE cognitive structures not gained sensorimotorically, and can CORRECT or DEGRADE existing structures as a function of the language's quality. The bidirectional clause (good language repairs structure; bad language corrupts it) is the discriminating edge over language-as-scaffold."
  claim_type: mechanism_hypothesis
  subject: language.prediction_as_corrective_instantiating_stream
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v6
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  claim_level: mechanistic
  registered_utc: 2026-06-12
  depends_on:
    - INV-003    # language as functional self-representation -- the scaffold premise this extends to structure-formation
    - ARC-009    # the symbolic mediation layer; the linguistic channel a language-prediction head would attach to
    - MECH-010   # language emergence/bootstrapping -- the closest existing dynamics claim
    - MECH-011   # language-and-learning dynamics -- the nearest neighbour; MECH-424 is its prediction-as-structure-formation facet
    - INV-013    # cognition is predictive/iterative -- language prediction is one more predictive stream
    - MECH-423   # super-additivity parent: language is a cross-pollinating stream whose gain (or harm) MECH-424 specialises
  possible_overlap_with:
    - MECH-011   # MECH-011 = general language-and-learning; MECH-424 = the specific bidirectional instantiate/correct/worsen mechanism via language PREDICTION
    - MECH-373   # LanguageAffectAdaptor parses affect FROM language; MECH-424 is the inverse -- language prediction SHAPING internal structure (cross-ref, not duplicate)
  location: docs/thoughts/2026-06-12_cognifold_cross_pollination_and_language_stream.md
  source:
    - docs/thoughts/2026-06-12_cognifold_cross_pollination_and_language_stream.md
  what_would_answer: >
    SUBSTRATE-CONDITIONAL -- requires a linguistic input channel REE-v3 does not have (the V6 language
    interface: ARC-009 realisation / MECH-373 / MECH-422). Once that channel exists: an agent with a
    language-prediction head trained over a corpus encoding CORRECT task structure acquires or repairs a
    behaviourally-measured cognitive structure that a matched no-language control does NOT (instantiate /
    correct leg = PASS); AND an agent trained over a corpus encoding WRONG structure has that structure
    DEGRADED relative to the no-language control (worsen leg = PASS). Both legs required: the bidirectional
    dependence on language quality is the discriminating prediction. UNMET PRECONDITION: no linguistic
    channel in V3, so the claim self-routes substrate_not_ready until the V6 language layer is built; the
    non-degeneracy gate additionally requires the measured structure to be live (non-floor) in the
    no-language control so 'correct' and 'worsen' are both reachable directions.
  notes: >
    The genuinely-new, distinctive fragment (Topic 3 of the 2026-06-12 cognifold intake). REE owns
    language as coordination/compression scaffold (INV-003, ARC-009, MECH-010/011) and a V6 cluster that
    parses affect/grammar FROM language (MECH-373, INV-085, MECH-422). MECH-424 is the inverse and
    unregistered direction: language PREDICTION as a learning signal that writes/corrects/corrupts
    internal cognitive structure, bidirectionally gated on the language's quality. This is the formal
    analogue of how good or bad explanation reshapes a clinician's mental model. Tightly coupled to
    MECH-423 (language is one cross-pollinating stream; its gain is super-additive when the language is
    good, NEGATIVE when bad -- the asymmetry MECH-423 alone does not capture). epistemic_category
    substrate_conditional: the mechanism is statable and falsifiable now, but the channel it needs is
    V6. DO NOT build in V3.
- id: MECH-425
  title: "Retrieval-time accessibility gating: which stored traces become reinstatable at a given moment is set by two composable READ-side policy layers, not by storage alone. Line A -- a global, slowly-fluctuating, arousal-coupled reinstatement GAIN (content-blind; sets the threshold on whether ANY stored trace crosses into accessibility; stochastic-resonance flavour). Line B -- a content-selective competitive SUPPRESSION that, given what the global gate admits, inhibits specific competitors to keep retrieval clean. The READ-side sibling of MECH-261's mode-conditioned WRITE-gate family: REE owns the gates but not this retrieval-time gating POLICY."
  claim_type: mechanism_hypothesis
  subject: memory.retrieval_time_accessibility_gating
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v4
  v3_pending: true
  epistemic_category: substrate_conditional
  version_relevance: v4_v5
  claim_level: mechanistic
  registered_utc: 2026-06-12
  depends_on:
    - MECH-261   # the WRITE-side mode-conditioned gate family; MECH-425 is its READ-side sibling (same gates, retrieval direction)
    - MECH-094   # sim/real write-distinction; a FAILED Line B (wrong-trace reinstatement) is a confabulation pathway
    - MECH-271   # replay routing-signature; failed competitive suppression = probe/wrong content reaching destinations it should not -- the auditable signature of a Line-B failure
    - ARC-005    # control plane routes precision; Line A is a retrieval-side INSTANCE of precision/gain control (abstract parent, not a duplicate)
    - ARC-035    # vmPFC stored->active conversion: the reinstatement substrate Line A would set the gain over
    - SD-016     # the z_world ContextMemory query path -- the read path a retrieval-readiness gain modulates
    - MECH-347   # cue-triggered recall: the nearest EXISTING retrieval mechanism (fast, content-specific) that Line A's slow content-blind gain is distinct from
  possible_overlap_with:
    - MECH-261   # MECH-261 = WRITE gating (what may write to memory); MECH-425 = READ gating (which stored traces become accessible). Sibling families, not duplicates.
    - MECH-254   # MECH-254 top-k precision selection acts over CURRENTLY-ACTIVE latents entering E3; MECH-425 Line A acts over REINSTATEMENT of stored traces -- adjacent precision instances, different operands
  location: docs/thoughts/2026-06-12_histaminergic_retrieval_accessibility_gating.md
  source:
    - docs/thoughts/2026-06-12_histaminergic_retrieval_accessibility_gating.md
  what_would_answer: >
    SUBSTRATE-CONDITIONAL -- REE-v3 has no explicit retrieval-readiness gain knob and no content-selective
    retrieval-suppression operator over episodic / ContextMemory read-out (the gates exist for WRITE,
    MECH-261; the read-time POLICY does not). Once a retrieval substrate exposes (i) a global scalar gain on
    trace reinstatement and (ii) a competitor-suppression operator: DISSOCIATION test -- lesioning Line A
    (clamp gain low) reduces reinstatement of ALL traces uniformly (apparent forgetting, content-blind),
    while lesioning Line B (disable competitive suppression) leaves gain intact but RAISES competitor
    intrusions / wrong-trace reinstatement (interference, content-selective). PASS = the two lesions produce
    mechanistically distinct, doubly-dissociable read-out signatures (uniform-suppression vs
    selective-intrusion); FAIL = a single knob reproduces both, i.e. the two layers are not separable on this
    substrate. NON-DEGENERACY GATE: the reinstatement-accuracy metric must be live (non-floor, non-ceiling
    cross-trace variance) and at least two competing traces must be co-encoded and individually scorable in
    the no-lesion control, else neither 'uniform forgetting' nor 'selective intrusion' is reachable and the
    run self-routes substrate_not_ready (check_degeneracy non_degenerate=false), NOT a verdict.
  notes: >
    Intake of the 2026-06-12 histaminergic-retrieval thought (Morishita et al., "Infraslow histaminergic
    dynamics govern priming states to gate moment-to-moment memory accessibility," Neuron 2026; literature
    explored in the source doc). The genuinely-new abstraction: memory retrieval is governed by TWO
    composable read-side control layers REE does not yet model as explicit retrieval-time policy. Line A =
    the histamine / TMN infraslow arousal-coupled reinstatement gain (Nomura 2019 stochastic-resonance
    framing; H1 facilitates / H2 inhibits depending on the cue's reinforcement history -- so the gain itself
    carries a context-dependent suppress arm). Line B = retrieval-induced-forgetting / PFC inhibitory control
    (Penolazzi 2014, Khan 2024; deficient in ADHD, Storm & White 2010). DISTINCT FROM the plasticity-window
    ACh/BDNF memory (project_plasticity_window_neuromodulators): that gates LEARNING-rate; this gates
    RETRIEVAL -- adjacent only via H3-ACh cross-talk (Zheng 2023), NOT the same axis, no depends_on. Line A
    is an INSTANCE of ARC-005 precision-routing, but the slow arousal-coupled gain specifically over
    REINSTATEMENT is uncovered by the existing precision cluster (MECH-251/254/255/259/347 all act on current
    perception / candidate selection / goal templates, not stored-trace read-out). The gain x suppression
    INTERACTION is registered separately as Q-076. V4/V5-leaning, OFF the V3 critical path. DO NOT build in V3.

- id: INV-086
  title: goal_maintenance_feedback_necessity
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-030
    - SD-014
  pending_substrate_reconfirmation: true
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  subject: goal.maintenance_feedback_necessity
  polarity: asserts
  depends_on: [INV-065, INV-034, MECH-116, MECH-217, ARC-030, SD-014]
  registered: "2026-06-12"
  description: >
    Beyond the horizon over which recurrent working-memory maintenance (MECH-116)
    suffices, sustaining a SUPERORDINATE goal across the interval between sparse
    terminal confirmations requires INTERMEDIATE FEEDBACK signals -- a progress-proxy
    (MECH-216/217), a predicted-subgoal / on-path progress estimate (MECH-426), or
    cross-level subgoal credit (MECH-427). A bare goal held only as a recurrent
    working-memory trace decays in long-horizon, distractor-laden, or effortful
    regimes; intermediate feedback is what keeps the superordinate goal committed
    across the gap. This is the MAINTENANCE-necessity parallel to INV-065's
    REACHABILITY-necessity: INV-065 says proxy goals are required to PLAN toward a
    distal target beyond the CEM horizon; INV-086 says feedback signals are required
    to HOLD that target committed when terminal confirmation is sparse. SUPERORDINATE
    goals are fragile along TWO axes: (1) sparse terminal FEEDBACK -- maintenance
    fragility, addressed by progress-velocity (MECH-426) and subgoal-credit
    (MECH-427); and (2) sparse SEEDING -- a superordinate goal whose instantiating
    benefit is rarely/never reached is hard to FORM at all (formation fragility,
    addressed by subgoal/proxy BOOTSTRAPPING, MECH-428). The SAME intermediate-
    feedback machinery remedies both, which is why the HARD CLASS -- sparsely seeded
    AND sparsely confirmed -- is difficult to create and maintain. Despite the
    title, INV-086 thus spans goal FORMATION as well as maintenance. It is the
    mechanism-face of INV-034 (why maintenance matters: ethical agency) and INV-053
    (its failure: depression).
  notes: >
    Scoped explicitly PAST MECH-116. MECH-116 establishes that E1's LSTM hidden state
    maintains goal context recurrently WITHOUT ongoing benefit signals -- passive
    working-memory maintenance. In current V3 that suffices: z_goal persists ~1000
    steps unscaffolded (the strand-I counter-datum from the lit-pull). INV-086 is
    therefore NOT a claim that bare goals always decay -- it is the claim that BEYOND
    the WM-maintenance horizon (under distractor load, effort cost, or sparse
    confirmation) feedback-scaffolding becomes NECESSARY. Without the regime scoping
    the claim is vacuous-by-construction on the current substrate. The feedback
    channels (MECH-216/217 proxy-wanting, MECH-426 progress-velocity, MECH-427
    subgoal-credit) are the candidate scaffolds; INV-086 is their shared parent.
    Distinct from INV-034 (normative: maintenance necessary for agency) and INV-053
    (clinical: maintenance failure = depression) -- those say WHY maintenance matters;
    INV-086 says HOW long-horizon maintenance is achieved. Cross-link:
    evidence/planning/goal_pipeline_plan.md. Grounded by
    evidence/literature/targeted_review_proxy_progress_goal_maintenance
    (Carver&Scheier1990, Bandura&Schunk1981, Sutton1988). Intake: proxy/progress
    goal-maintenance (Daniel 2026-06-12).
  what_would_answer: >
    In a regime where MECH-116 working-memory maintenance has MEASURABLY DECAYED
    (superordinate-goal z_goal persistence falls below a baseline threshold under
    distractor/horizon/effort load -- the NON-DEGENERACY PRECONDITION: the bare-goal
    control must itself show decay, else the test is vacuous since unscaffolded z_goal
    already persists ~1000 steps), ablating ALL intermediate feedback channels
    (proxy-wanting MECH-216/217 + progress-velocity MECH-426 + subgoal-credit
    MECH-427) collapses superordinate-goal completion rate, while restoring ANY single
    channel rescues it. FAIL = completion rate unchanged by feedback ablation
    (maintenance is purely WM-driven; the feedback-necessity thesis is false in this
    regime). The discriminating metric (completion-rate delta full-vs-all-ablated)
    must have non-zero cross-seed range or the run self-routes substrate_not_ready.

- id: MECH-426
  title: progress_velocity_maintenance
  claim_type: mechanism
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  subject: goal.progress_velocity_maintenance
  implementation_phase: v3
  depends_on: [INV-086, INV-034, MECH-217, MECH-116]
  registered: "2026-06-12"
  description: >
    A rate-of-progress (velocity) signal -- the temporal derivative of an on-path
    progress estimate over goal-proximity -- produces a modulatory signal that
    sustains superordinate-goal commitment between sparse terminal confirmations. The
    on-path progress estimate is itself a bootstrapped value / forward-model readout
    (the "predicted subgoal / inference" face of the intake, folded in here): the
    agent infers it is on-trajectory via a TD-style bootstrapped estimate
    (Sutton 1988), and the DERIVATIVE of that estimate -- am I closing the gap, and
    how fast -- is the maintenance signal. This is the REE instantiation of the
    Carver & Scheier (1990) second-order ("velocity") control loop, in which affect is
    generated from the RATE of discrepancy reduction rather than the discrepancy
    itself.
  notes: >
    FOLDS the candidate on_path_progress_inference (intake strand D): the bootstrapped
    on-path estimate is the SUBSTRATE the velocity signal is differentiated over, not a
    separate claim. A value/forward-model head over an IMPOVERISHED or non-Markov
    latent yields a BIASED progress estimate = an untethered proxy -- that failure mode
    is the prediction cross-referenced to INV-087 (proxy_tethering_constraint).
    CRITICAL MODELLING CAVEAT (Carver&Scheier coasting): above-reference progress
    produces positive affect that REDUCES effort on the current goal and licenses
    redeployment -- velocity must be modelled as EFFORT-REGULATION, NOT same-goal
    reinforcement. A naive implementation treating positive progress-affect as a bonus
    increasing same-goal commitment INVERTS the theory and will mis-fire. Distinct from
    MECH-217 (which BUILDS the wanting landscape via reverse replay); MECH-426 READS a
    derivative of progress at decision time to modulate commitment. Grounded by
    targeted_review_proxy_progress_goal_maintenance (Carver&Scheier1990, Sutton1988).
    Proposal: EXP-0384.
  implementation_note: >
    SD-093 (goal.progress_velocity_maintenance) IMPLEMENTED 2026-08-02: GoalState
    (ree_core/goal.py) gains a rolling-window (default 5-tick) goal_proximity() history,
    record_progress()/progress_velocity, and progress_velocity_effort_modulation --
    Carver&Scheier-correct sign (positive velocity -> NEGATIVE modulation/coasting;
    negative velocity -> POSITIVE modulation/boosted persistence). Wired as an
    EFFORT/PERSISTENCE modulator only: E3TrajectorySelector.select() (ree_core/predictors/
    e3_selector.py) multiplies effective_threshold by (1 + velocity_effort_modulation)
    immediately after the existing SD-011 urgency block; compute_goal_score()/
    goal_proximity() (goal VALUE / trajectory score) are untouched (verified byte-identical
    by contract test). Master switch use_progress_velocity_effort_modulation defaults False
    (bit-identical OFF). See sd_093_progress_velocity_maintenance.md and
    ree-v3/CLAUDE.md "SD-093" entry for the full data-flow writeup and 18 contract tests
    (tests/contracts/test_mech426_progress_velocity.py). EXP-0384 reset blocked_substrate ->
    proposed by this landing.
  what_would_answer: >
    Ablate the rate-of-progress (velocity) signal while holding the proxy-wanting
    landscape and the goal representation intact; long-horizon superordinate-goal
    maintenance degrades SPECIFICALLY in the sparse-confirmation regime (completion
    rate / commitment-persistence drops vs the velocity-intact arm), and the effect is
    absent in the dense-confirmation regime where velocity adds no information.
    NON-DEGENERACY: the velocity signal must have non-zero cross-condition RANGE (not
    magnitude-only / range-zero -- the V3-EXQ-643 "affect must be candidate-
    differentiated" lesson); a range-zero velocity self-routes substrate_not_ready, not
    a verdict. PASS/FAIL read in the effort-regulation (coasting) direction, NOT as
    same-goal reinforcement.

- id: MECH-427
  title: cross_level_subgoal_credit
  claim_type: mechanism
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-03
    needs_review: false
    evidence:
      from: v3_exq_883_mech427_cross_level_subgoal_credit_20260803T022051Z_v3
      as_of: 2026-08-03
      verdict: supports
  subject: goal.cross_level_subgoal_credit
  implementation_phase: v3
  depends_on: [INV-086, ARC-051, MECH-217, INV-034]
  registered: "2026-06-12"
  description: >
    Attaining a concrete SUBGOAL propagates credit / commitment UP the goal hierarchy
    to the SUPERORDINATE goal it serves, reinforcing maintenance of the parent goal.
    This is the cross-LEVEL complement to MECH-217: MECH-217 spreads wanting backward
    ALONG a single approach trajectory (within a level); MECH-427 propagates a discrete
    subgoal-attainment EVENT upward to the parent goal representation. The empirical
    anchor is Bandura & Schunk (1981): decomposing a distal goal into attainable
    proximal subgoals sustained motivation and mastery, whereas the bare distal goal
    alone behaved indistinguishably from NO goal -- the superordinate goal is
    maintained THROUGH subgoal attainment.
  notes: >
    Distinct from MECH-217 (along-trajectory, within-level wanting spread) and from
    MECH-426 (continuous velocity derivative): MECH-427 is a DISCRETE-event credit
    propagation ACROSS hierarchy levels. Extends ARC-051's emergent multi-level
    hierarchy with an UP-level credit channel ARC-051 does not currently carry
    (ARC-051's three levels all feed the SAME field but there is no parent-credit
    propagation). Grounded by targeted_review_proxy_progress_goal_maintenance
    (Bandura&Schunk1981; goal-gradient Hull1932 / Kivetz2006). Proposal: EXP-0385.
    SIBLING: MECH-428 is the FORMATION-direction sibling -- the same cross-level
    credit applied to SEED an under-seeded parent (goal creation), where MECH-427
    MAINTAINS an already-seeded parent.
  evidence_quality_note: >
    [2026-08-03, V3-EXQ-883, PASS, evidence_direction=supports, FIRST experimental test]: validates
    the SD-092 cross-level subgoal credit primitive built 2026-08-02 (GoalState._z_goal_parent,
    credit_subgoal_attainment(), flag-gated use_hierarchical_goal_credit). Confirms the
    reinforcing-an-already-seeded-parent direction this claim describes. No status change (stays
    candidate; consumer wiring into a real call site -- causal_grid_world.py subgoal_mode -> agent.py
    -> credit_subgoal_attainment() -- is still not done, per SD-092's own
    partially_implemented_pending_consumer_wiring status).
  what_would_answer: >
    A discrete subgoal-attainment event raises measured commitment to the PARENT
    superordinate goal (parent z_goal norm / persistence) above a matched
    no-subgoal-attainment control on the same trajectory; the bare distal goal alone
    shows no such lift. NON-DEGENERACY: subgoals must actually be attained at a
    NON-ZERO rate in the run (else there is nothing to credit -- the goal_pipeline
    GAP-2 foraging/benefit-contact-ceiling trap; a zero-attainment run self-routes
    substrate_not_ready, not a FAIL). FAIL = subgoal attainment produces no parent
    commitment lift despite a non-zero attainment rate.
  implementation_note: >
    SD-092 (2026-08-02): the up-level credit PRIMITIVE this claim needs now exists
    (ree_core/goal.py GoalState.credit_subgoal_attainment / _z_goal_parent /
    parent_goal_norm(), GoalConfig.use_hierarchical_goal_credit -- default False,
    bit-identical off). Call-site wiring landed the same day: REEAgent.notify_subgoal_attainment
    (ree_core/agent.py, mirrors the notify_env_completion/SD-034 explicit-hook convention) routes
    env info["transition_type"] in {"waypoint","sequence_complete"} into the primitive, gated
    identically, contract-tested (tests/contracts/test_sd092_notify_subgoal_attainment.py).
    STILL NEEDED: an experiments/ driver that actually calls the hook off a real
    causal_grid_world.py subgoal_mode run and decides which representation to credit -- that
    remains /queue-experiment Step-2.5's job before EXP-0385 can run.
    experiment_proposals.v1.json EXP-0385 flipped blocked_substrate -> proposed on this basis.
    See docs/architecture/sd_092_cross_level_subgoal_credit.md.

- id: INV-087
  title: proxy_tethering_constraint
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - SD-014
    - SD-015
  pending_substrate_reconfirmation: true
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  subject: goal.proxy_tethering_constraint
  polarity: asserts
  depends_on: [INV-065, INV-086, SD-014, SD-015]
  registered: "2026-06-12"
  description: >
    Any intermediate maintenance/progress proxy that sustains a superordinate goal
    carries a DIVERGENCE risk (Goodhart / reward-hacking): optimizing the proxy can
    pull behaviour AWAY from the superordinate goal it was meant to serve, UNLESS the
    proxy is tethered. The tethering constraint has a precise form -- an additive
    maintenance proxy leaves the true-goal optimal policy INVARIANT iff it approximates
    a POTENTIAL DIFFERENCE over goal-proximity, F = gamma*Phi(s') - Phi(s)
    (Ng, Harada & Russell 1999), the only additive shaping form that is policy-
    invariant under all dynamics. Equivalently, an untethered (non-potential) proxy is
    trustworthy only over a RESTRICTED policy manifold (Skalse et al. 2022): over all
    stochastic policies a non-trivial never-diverging proxy essentially cannot exist,
    but over the structured manifold a biologically-motivated agent occupies
    (homeostatic drives, harm constraints, bounded planning) aligned proxies DO exist.
    REE owns proxy-goal NECESSITY (INV-065); INV-087 is the missing alignment GUARD.
  notes: >
    The standout new capture: the existing cluster (INV-065/MECH-216/217/ARC-051)
    establishes that proxy goals are necessary and how they form, but nothing
    constrains them to stay ALIGNED with the superordinate goal. The guard matters most
    for an ethics-load-bearing architecture (INV-034) -- an untethered maintenance
    proxy is exactly the route by which a goal-maintaining agent drifts from the goal.
    Transfer caveat: the Ng et al. invariance is exact only for a stationary,
    state-only potential; REE's maintenance proxy is learned and non-stationary, so the
    potential form is a DESIGN TARGET / limiting-case certificate, not an off-the-shelf
    guarantee. The Skalse restricted-policy result is what makes tethering TRACTABLE in
    REE's setting. Ties to MECH-426 (a biased-latent progress estimate IS an untethered
    proxy). Grounded by targeted_review_proxy_progress_goal_maintenance
    (Ng/Harada/Russell1999, Skalse2022). Proposal: EXP-0386.
  evidence_quality_note: >
    [2026-08-03 governance, V3-EXQ-872, PASS, evidence_direction=supports, FIRST experimental test]:
    3-arm E3-selection probe (CTRL=q_true; NON_POTENTIAL=raw/absolute proximity bonus retaining an
    irrelevant per-candidate entry-vantage nuisance; POTENTIAL=potential-DIFFERENCE gamma*Phi(s')-
    Phi(s), where the nuisance cancels exactly by construction) confirms the dissociation this claim
    predicts: the non-potential proxy diverts selection (diversion_np clearly positive) while the
    potential-difference form preserves it (diversion_pot <= tol, one-sided). Readiness clean
    (clean_landscape_discriminative, entry_noise_moves_argmin, proxy_correlates_with_truth,
    probe_regime_uncommitted all met). First direct confirmation of the Ng/Harada/Russell
    telescoping argument's single-hop instantiation in REE's own selection landscape. No status
    change (stays candidate; pending_substrate_reconfirmation unaffected by an evidence PASS).
  what_would_answer: >
    Inject a maintenance proxy that is NOT a potential difference over goal-proximity
    (e.g. a raw progress bonus) into the E3 selection landscape and show it shifts the
    selected-candidate distribution AWAY from the clean-goal optimum (true-goal quality
    of the selected candidate drops), while a potential-based proxy
    (gamma*Phi(s')-Phi(s)) over the SAME proximity leaves the clean-goal optimum
    intact. NON-DEGENERACY: the clean-goal optimum must be discriminable -- selected-
    candidate true-quality must have non-zero range across proxy conditions on an
    identical base landscape (else nothing to divert). Tractable as a controlled
    E3-selection probe in the V3-EXQ-674 family (no env / no training -> avoids the
    monostrategy / z_goal-collapse confounds). FAIL = the non-potential proxy does NOT
    divert behaviour (tethering constraint not load-bearing on this substrate).

- id: ARC-105
  title: granularity_matched_goal_hierarchy
  claim_type: architecture_hypothesis
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  subject: goal.granularity_matched_hierarchy
  implementation_phase: v4
  depends_on: [ARC-051, INV-086]
  registered: "2026-06-12"
  description: >
    A superordinate goal and its concrete subgoals are represented at DISTINCT,
    DISCRETE granularity levels (not a single continuous granularity scalar), and
    maintaining the abstract superordinate goal WHILE executing concrete subgoals is
    supported by this cross-level representational organization. The biological anchor
    is the rostro-caudal abstraction gradient in lateral prefrontal cortex
    (Badre & D'Esposito 2009): rostral PFC holds abstract/superordinate goals, caudal
    PFC concrete action rules, with asymmetric top-down control. Per the 2018 revision
    (Badre & Nee) the organization is best modelled as a SMALL NUMBER OF DISCRETE zones
    (~3: schematic / contextual / sensorimotor), NOT a continuous gradient -- so the
    REE claim is written DISCRETE.
  notes: >
    V4 / substrate_conditional: REE-v3 has no explicit multi-scale goal-representation
    substrate (z_goal is single-scale). Extends ARC-051's emergent multi-LEVEL wanting
    hierarchy (whose levels are wanting-SOURCE scales: contact/schema/replay) with an
    ORTHOGONAL axis -- the representational GRANULARITY of the goal itself (abstract-
    goal vs concrete-subgoal encoding). OFF the V3 critical path: do not build in V3;
    promote only on a specific failure that a single-scale z_goal cannot represent
    abstract-goal maintenance under subgoal switching. Written DISCRETE (~3 zones) per
    Badre&Nee2018, NOT continuous. Grounded by
    targeted_review_proxy_progress_goal_maintenance (Badre&D'Esposito2009 + 2018
    caveat). No EXP proposal (V4-blocked).
  what_would_answer: >
    PRECONDITION UNMET in V3 (substrate_conditional): requires a multi-scale goal-
    representation substrate that does not yet exist. Once built: superordinate-goal
    maintenance survives concrete-subgoal SWITCHING iff the abstract goal is encoded at
    a granularity level DISTINCT from the subgoal; collapsing both to one level causes
    abstract-goal loss on subgoal switch (the abstract goal is overwritten by the
    concrete subgoal it should outlive). FAIL = abstract-goal maintenance is
    independent of single- vs multi-level representation (granularity matching not
    load-bearing). Until the substrate exists this self-routes substrate_not_ready.

- id: MECH-428
  title: subgoal_bootstrapped_goal_seeding
  claim_type: mechanism
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  subject: goal.subgoal_bootstrapped_seeding
  implementation_phase: v3
  depends_on: [INV-086, MECH-427, ARC-051, MECH-112, MECH-230]
  registered: "2026-06-12"
  description: >
    When a SUPERORDINATE goal cannot be seeded directly -- because the terminal benefit
    that would instantiate it is rarely or never reached (sparse seeding) -- reliably-
    attainable SUBGOALS together with their proxy/progress feedback can BOOTSTRAP the
    superordinate goal bottom-up: repeated subgoal attainment and its cross-level credit
    (MECH-427) progressively seed and stabilise a structured z_goal attractor
    (MECH-112/MECH-230) that direct top-down seeding failed to produce. This is the
    FORMATION-direction complement to MECH-427's maintenance-direction credit: MECH-427
    reinforces an already-seeded parent; MECH-428 claims the same cross-level credit can
    CREATE the parent when direct seeding is sparse. It is the mechanism by which the
    HARD CLASS of goals INV-086 names -- sparsely seeded AND sparsely confirmed --
    becomes reachable at all.
  notes: >
    Directly addresses the live, dominant goal-pipeline blocker: z_goal SEEDING failure
    (z_goal_norm < 0.1 "in every iteration"; EXQ-085f/g, EXQ-233, EXQ-536a/b all failed
    AT the seeding step; goal_pipeline:GAP-2 is blocked because the agent never reaches
    self-sustaining benefit contact, so z_goal is never seeded; INV-034 is held "until
    z_goal seeding is reliable"). The scaffolded_sd054_onboarding substrate scaffolds
    seeding EXTERNALLY (developmental nursery/feeding curriculum); MECH-428 is the
    INTERNAL complement -- the agent's own subgoal structure substitutes for direct
    superordinate seeding. The 626b forced_seed positive control (z_goal forced high,
    wiring proven sound) is the reference a bootstrapping run is measured against.
    Distinct from MECH-216/217 (populate the wanting landscape for REACHABILITY) and
    from MECH-426/427 (MAINTAIN an already-seeded goal). Grounded by
    evidence/literature/targeted_review_proxy_progress_goal_maintenance
    (Bandura&Schunk1981: proximal subgoals CREATED intrinsic interest / goal pursuit in
    initially-uninterested learners -- a FORMATION result, not just maintenance). A
    developmental-scaffolding / zone-of-proximal-development (Vygotsky) lit anchor would
    strengthen the formation framing -- flagged as a NEXT lit-pull. Cross-link:
    goal_pipeline_plan.md (GAP-2) + scaffolded_sd054_onboarding. Proposal: EXP-0390.
  evidence_quality_note: >
    [2026-08-03 governance, V3-EXQ-884, confirmed failure_autopsy_V3-EXQ-884_2026-08-03, FIRST
    experimental test]: FAIL is non-contributory: the episode terminated 77-95% short of its
    designed budget in all 3 seeds due to a causal_grid_world.py substrate bug (waypoint-arrival
    detection clobbered by the agent's own grid-position marker on premature transit) compounded by
    an undisabled ambient self-contamination mechanic, starving the accumulation-vs-decay dynamics
    the claim's own non-degeneracy guard is meant to detect. non_contributory/precondition_unmet.
    Route: /implement-substrate (create SD-094, fix the waypoint-arrival detection bug + disable the
    ambient self-contamination mechanic for this curriculum). pending_retest_after_substrate=true.
  pending_retest_after_substrate: true
  what_would_answer: >
    In a regime where DIRECT superordinate-goal seeding is sparse (unscaffolded z_goal
    seeding fails -- z_goal_norm stays < 0.1, the GAP-2 condition), enabling
    subgoal-attainment + cross-level credit (MECH-427) BOOTSTRAPS a structured z_goal
    attractor (z_goal_norm rises materially above the unscaffolded baseline, toward the
    626b forced-seed reference) and produces goal-directed behaviour the no-subgoal
    control does not. NON-DEGENERACY: subgoals must be ATTAINED at a non-zero rate AND
    the 626b-style forced-seed positive control must itself show a structured z_goal in
    the SAME harness [else the substrate, not bootstrapping, is what fails ->
    self-routes substrate_not_ready, NOT a FAIL -- the GAP-2 foraging-ceiling trap].
    FAIL = subgoal attainment produces no z_goal seeding above the unscaffolded baseline
    despite non-zero attainment and a working forced-seed control.
  implementation_note: >
    SD-092 (2026-08-02): inherits MECH-427's primitive directly -- the SAME
    GoalState.credit_subgoal_attainment call against a near-zero
    _z_goal_parent IS the bootstrap case this claim describes (no separate
    formation-mode code was needed; see the design doc's "same primitive,
    different starting state" reasoning). The agent-loop call site
    (REEAgent.notify_subgoal_attainment, same-day follow-up) is now wired --
    see MECH-427's implementation_note. STILL NEEDED: EXP-0390's 3-arm design
    (NO-SUBGOAL / SUBGOAL-BOOTSTRAP / FORCED-SEED) needs an experiments/
    driver that calls the hook inside the seeding-sparse scaffolded_sd054
    harness, which /queue-experiment Step-2.5 must build and verify
    (including the non-degenerate-attainment-rate + working-forced-seed
    preconditions) before this can run. experiment_proposals.v1.json EXP-0390
    flipped blocked_substrate -> proposed on this basis. See
    docs/architecture/sd_092_cross_level_subgoal_credit.md.

# Registered 2026-06-13 -- completion-set partners REAPED from the autobiographical_memory_v4:ABM-9
# biology /lit-pull (evidence/literature/targeted_review_autobiographical_store). Each was surfaced
# proposal-first, then user-approved for registration. All V4 / substrate_conditional / off the V3
# critical path; PROMOTE NOTHING (exp_conf 0). Wired into depends_on of the cluster they complete.
- id: MECH-429
  title: "Schema-congruence as a consolidation write-weight and routing variable: the degree of fit between an event and the agent's existing schemas modulates how that event is consolidated -- schema-congruent content is consolidated preferentially and routed more directly into the durable/semantic store (a fast vmPFC-gated systems-consolidation route), while schema-incongruent content requires prolonged hippocampal-dependent consolidation or is gated out. Schema-fit is a WRITE-WEIGHT input to consolidation alongside affect (MECH-361), not only a retrieval-time prior."
  claim_type: mechanism_hypothesis
  subject: memory.schema_congruence_consolidation_weight
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - MECH-261
    - MECH-285
    - MECH-121
    - MECH-361
  location: docs/architecture/autobiographical_temporality_and_future_simulation.md#mech-429
  notes: "Reaped from the autobiographical_memory_v4:ABM-9 /lit-pull (2026-06-13) as a completion-set partner -- the CLS/schema fast-consolidation route the MECH-261/MECH-285 consolidation gates PRESUPPOSE but REE had not named. Amends the CONTENT/ROUTING policy of MECH-261 (mode-conditioned write gating) and MECH-285 (consolidation-priority by V_s residual): schema-fit is a SECOND write-weight axis beside affect (MECH-361). Biological anchors (verified in the ABM-9 pull's adjacent literature; pull a targeted review before any build, per biology-before-formal-definitions): Tse et al 2007 Science 316:76-82 'Schemas and memory consolidation' (a congruent schema dramatically accelerates systems consolidation / neocortical incorporation); van Kesteren, Ruiter, Fernandez & Henson 2012 Trends Neurosci (SLIMM -- mPFC-mediated schema-congruent fast route vs MTL-dependent incongruent route); Ghosh & Gilboa 2014 Neuropsychologia (schema construct); Schapiro et al 2017 Phil Trans R Soc B (CLS within the hippocampus). FALSIFIER: if consolidation priority is fully accounted for by affect/salience (MECH-361) + V_s residual (MECH-285) with NO additional variance explained by schema-fit, MECH-429 earns no keep. V4/V5; off the V3 critical path; substrate_conditional on the ARC-085 unified store + an explicit schema representation not yet built in V3. DO NOT build in V3 until routed by experiment."
- id: MECH-430
  title: "Multi-dimensional provenance source vector: an event token's provenance is a SOURCE VECTOR over several partly-independent dimensions (perceived-vs-imagined, self-generated-vs-other-generated, source identity, temporal source, modality), not a single committed_vs_imagined bit. Provenance failures dissociate per-dimension -- the system can correctly attribute real-vs-imagined while mis-attributing who-generated-it or when -- so each dimension carries its own confidence, and the one-way committed gate applies per safety-relevant dimension rather than to a single global bit."
  claim_type: mechanism_hypothesis
  subject: memory.multidimensional_provenance_vector
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - MECH-365
    - MECH-094
    - MECH-037
  location: docs/architecture/autobiographical_temporality_and_future_simulation.md#mech-430
  notes: "Reaped from the autobiographical_memory_v4:ABM-9 /lit-pull (2026-06-13). Refines MECH-365: the committed_vs_imagined field is ONE dimension of a richer source vector. Anchor (verified, in the ABM-9 pull): Johnson, Hashtroudi & Lindsay 1993 Psychological Bulletin 114(1):3-28 (source-monitoring framework -- 'source' is EXPLICITLY multidimensional with indefinitely many values, contra the binary reality-monitoring dichotomy; source-misattribution -- cryptomnesia, misinformation, who-said-it errors -- occurs per-dimension). Under-specifying provenance as a single bit risks missing per-dimension misattribution that a global real-vs-imagined gate would pass. FALSIFIER: if a single committed_vs_imagined bit is sufficient to prevent all safety-relevant confabulation in the substrate (per-dimension provenance adds no protective discrimination), MECH-430 earns no keep over MECH-365. V4/V5; off the V3 critical path; substrate_conditional on the MECH-365 token. DO NOT build in V3 until routed by experiment."
- id: MECH-431
  title: "Two-factor (tag-and-capture) write-eligibility: MECH-368's plasticity_eligibility is not a single instantaneous test but a TWO-STAGE process -- a local event sets a transient, input-specific eligibility TAG, and a durable model/policy write is consummated only if that tag CAPTURES a separately-gated plasticity/consolidation-resource signal within a time window. A tagged-eligible event can still fail to write if no capture signal arrives, and an arriving capture signal can rescue a recently-tagged weak event."
  claim_type: mechanism_hypothesis
  subject: plasticity.tag_and_capture_two_factor_eligibility
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  depends_on:
    - MECH-368
    - MECH-285
    - MECH-261
  location: docs/architecture/plasticity_write_authority_gating.md#mech-431
  notes: "Reaped from the autobiographical_memory_v4:ABM-9 /lit-pull (2026-06-13). Refines MECH-368's plasticity_eligibility term from a scalar gate into the eligibility-THEN-capture STRUCTURE; MECH-285 (consolidation-priority by V_s residual) is a candidate capture-resource signal and MECH-261 gates the channel the tag lives in. Anchors (verified Ballarini in the ABM-9 pull; the cellular pair are canonical): Ballarini, Moncada, Martinez, Alen & Viola 2009 PNAS 106(34):14599 (behavioural tagging -- weak training yields LTM only if a novel experience supplies plasticity-related products within a critical window); Frey & Morris 1997 Nature 385:533 (synaptic tagging and capture); Redondo & Morris 2011 Nat Rev Neurosci (synaptic-tagging-and-capture hypothesis). FALSIFIER: if a single-stage eligibility test (no separate capture stage, no time window) prevents catastrophic interference on the online world-model/policy update path just as well, the two-factor structure earns no keep over MECH-368. V4/V5; off the V3 critical path; substrate_conditional on the MECH-368 gate + an online model-update write channel not yet built in V3. DO NOT build in V3 until routed by experiment."
- id: MECH-437
  title: "Rule-pool distinguishability is a MAINTAINED property, not a minting byproduct: keeping the CandidateRuleField pool mutually distinguishable (so co-matching rules remain jointly selectable through the conflict gate) requires a periodic, non-gradient consolidation/refactor operator over the minted pool -- compress and merge near-duplicate rules, retire rules that never reach selection, and re-separate the survivors under a description-length / separability objective -- rather than a reactive affine gate theta(n_matched). The maintained invariant is that mean n_matched stays low enough that theta(n_matched) sits below the availability ceiling."
  claim_type: mechanism_hypothesis
  subject: policy.rule_distinguishability_maintenance_operator
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  registered_utc: "2026-06-17"
  depends_on:
    - ARC-063    # the CRF locus (strong-reading CandidateRuleField) this operator maintains
    - MECH-309   # the diagnostic the rule-apprehension cluster serves (monomodal collapse without an apprehender)
    - ARC-062    # the weak-reading selector half of the mint-then-weight split
    - ARC-064    # the bottom-up regularity-mint source that populates the pool this operator keeps separable
    - MECH-285   # sleep-side consolidation-priority signal this operator would reuse (waking-light + sleep-heavy)
  location: docs/architecture/rule_distinguishability_maintenance.md#mech-437
  notes: >
    Registered 2026-06-17 from the REE_convergence rule-distinguishability intake
    (promotion packet CPKT-RULE-DISTINGUISHABILITY-20260617; comparison artifact
    REE_convergence/sources/dreamcoder/comparison_table.md). External analog: DreamCoder
    wake-sleep library learning (Ellis et al. 2021, PLDI; arXiv 2006.08381; Phil Trans R
    Soc A 381:20220050 2023) -- minted abstractions are kept distinguishable by a periodic
    non-gradient abstraction-sleep REFACTORING step under an MDL objective, the working
    instance of exactly this operator. Targets the arc_062_rule_apprehension GAP-B CRF
    conflict-gate lockout (theta=0.15+0.25*(n_matched-1); n_matched~7-8 -> theta~1.65 >>
    maintenance floor ~0.45 -> crf_frac_active->0; failure_autopsy_V3-EXQ-654d). The
    existing substrate_queue crf-availability-maintenance item is the point-fix line;
    MECH-437 is the principled-mechanism candidate behind it. Complementary to MECH-438
    (construction-side). FALSIFIER: if a periodic MDL-driven consolidation pass over the
    minted-rule pool yields no improvement in crf_frac_active / committed-class entropy vs
    a no-consolidation baseline on a substrate where >5 rules co-match (the lockout
    regime), the maintained-distinguishability mechanism is falsified (the fix is
    elsewhere -- minting too many rules, or the gate functional form itself). V4-leaning;
    OFF the V3 critical path -- blocks no V3 closure node; substrate_conditional on a
    consolidation-operator substrate not yet built. DO NOT build in V3 until routed by
    experiment.
- id: MECH-438
  title: "Constructive rule-key separability: minted CandidateRule context keys should be assigned in a quantised / categorical codebook that is separable by construction (a DreamerV3-style discrete latent codebook), bounding the number of rules that co-match any context (n_matched) up front so theta(n_matched) stays below the availability ceiling -- rather than letting overlapping continuous context tags produce unbounded co-match counts that the conflict gate then locks out. The codebook capacity must be large enough to keep genuinely-distinct rules on distinct keys (no key-collapse)."
  claim_type: mechanism_hypothesis
  subject: policy.rule_key_constructive_separability
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  registered_utc: "2026-06-17"
  depends_on:
    - ARC-063    # the CRF locus whose context-tag space this constructs as a separable codebook
    - MECH-309   # the diagnostic the rule-apprehension cluster serves
    - ARC-062    # the weak-reading selector half of the mint-then-weight split
    - ARC-064    # the bottom-up regularity-mint source whose keys this quantises
    - MECH-269   # V_s representational precondition: keys must be discriminably represented before they can be quantised separably
    - MECH-437   # complementary maintenance-side operator (construction bounds steady-state co-match; maintenance cleans residue)
  location: docs/architecture/rule_distinguishability_maintenance.md#mech-438
  notes: >
    Registered 2026-06-17 from the REE_convergence rule-distinguishability intake
    (promotion packet CPKT-RULE-DISTINGUISHABILITY-20260617; comparison artifact
    REE_convergence/sources/dreamer-v3/codebook_key_separability.md, claim
    DREAMER-V3-MOD-002, probe DREAMER-V3-P-006). External analog: DreamerV3 categorical
    discrete latents (Hafner et al. 2023; arXiv 2301.04104) -- 32 categoricals x 32
    classes, one-hot, straight-through gradient -- keep world-state keys separable by
    construction, bounding collision by codebook capacity. Construction-side answer to the
    arc_062_rule_apprehension GAP-B CRF conflict-gate lockout. RISK (the construction-side
    failure mode): a codebook too coarse collapses distinct rules onto one key, destroying
    the discriminability the rule-apprehension layer exists to provide (the same
    differentiation-vs-persistence tension ARC-063 names). FALSIFIER: if quantising
    context keys onto a categorical codebook does not reduce mean n_matched below the
    lockout threshold, OR reduces it only by collapsing distinct rules onto one key
    (measured as a drop in committed-class entropy / route diversity), constructive
    key-separability is falsified. V4-leaning; OFF the V3 critical path -- blocks no V3
    closure node; substrate_conditional on a quantised-context-key substrate not yet
    built. DO NOT build in V3 until routed by experiment.
- id: MECH-439
  # Assembly-state companion fields (auto-join audit, 2026-06-22; repointed same
  # day). MECH-439 characterizes the F-dominance ceiling, but it is being RESOLVED
  # via the biologically-faithful basal-ganglia eligibility constitution (ARC-107:
  # MECH-448 rank-preserving F->eligibility demotion + MECH-449 Go/No-Go opponency
  # leg), NOT the F-dominance rebalance/selection-readout route (exhausted).
  # awaiting: ARC-107 (the active substrate, matching MECH-449), not the legacy
  # f_dominance_conversion_ceiling queue row (name connotes the abandoned route;
  # status blob mis-normalizes to 'built'). assembly_status in_progress: MECH-448
  # built+promoted; MECH-449 built 2026-06-21; V3-EXQ-689g pending.
  awaiting: ARC-107
  assembly_status: in_progress
  title: "F-dominance bounds committed-action diversity: the primary harm/goal score (F) monopolises ~88-89% of E3 committed-selection variance (V3-EXQ-571: 0.886 baseline, 0.894 with the full diversity stack -- the stack does not dent F's share), so modulatory, within-class, and rule-bias diversity channels cannot convert to committed-action diversity while F dominates -- they act only at near-ties. The committed-action-diversity conversion ceiling is therefore a SELECTION-VARIANCE problem (primary-vs-modulatory share), not (only) a candidate-pool geometry problem; a shortlist-then-modulate lever circumvents F locally but may cap committed entropy below the proposer ceiling unless F's variance share is itself rebalanced."
  claim_type: mechanism_hypothesis
  subject: selection.primary_score_variance_monopoly
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-936_2026-08-18"
      as_of: "2026-08-21"
      verdict: "non_contributory -- C2 arithmetically unreachable (f_variance_share ~1.0); self-stamp weakens withdrawn"
  epistemic_category: standard
  ceiling_decision: exhausted
  ceiling_routing_note: "GOV-CEIL-1 ceiling-exhaustion demotion 2026-07-09: 10 confirmed substrate_ceiling failure_autopsy hits (689a/700/700a/700b/700c/700d/709/710/711/713) with no positive discrimination on any richer substrate. Demoted to contested candidate; the inert/no-op-mechanism null reading is carried co-equally. Promotion re-gated on a future positive discrimination on a richer substrate (e.g. the pending ARC-107 MECH-448/449 BG-constitution validation V3-EXQ-689g). [2026-08-10 governance, GOV-APPLY-1]: count corrected 10 -> 9, 710 dropped from the hit list (689a/700/700a/700b/700c/700d/709/711/713) -- failure_autopsy_V3-EXQ-710_2026-07-20 (confirmed re-adjudication) withdrew the 2026-07-03 710 autopsy's own evidentiary basis (a hold-weighted DV plus an analytically-null rank-preserving ablation control), so it no longer counts as a substrate_ceiling hit on MECH-439. Demotion STANDS -- margin to CEILING_EXHAUSTION_N=3 unaffected. Flagged, not resolved: the 710 autopsy also disqualifies conversion legs in 711 and 713 pending their own re-adjudication, so the 9 may itself be an overcount; this does not threaten the demotion."
  implementation_phase: v3
  version_relevance: v3
  registered_utc: "2026-06-18"
  pending_retest_after_substrate: true
  implementation_note: >
    SUBSTRATE BUILD 2026-07-04 (PROMOTES NOTHING): the BOUNDED ascending-spiral gain -- a target-PARITY
    controller -- was built no-op-default in ree-v3 (config.e3.use_ascending_parity_controller +
    loop_segregation_parity_forward_gain / parity_ceiling_ratio / parity_plasticity_gain / m_cross_clamp;
    e3_selector._parity_forward_gain solves a per-step ascending gain holding w_eff[limbic] under
    ceiling_ratio*w_eff[motor], + an anti-windup clamp on the ascending M_cross maturation). It repairs the
    RAW-scalar runaway the confirmed failure_autopsy_V3-EXQ-711_2026-07-04 found (M_cross range 4897.8,
    w_eff[limbic] 10-2274x w_eff[motor] = a limbic MONOPOLY, not a fair parity win). A SATURATION GUARD was
    added to the falsifier's limbic_loop_can_win gate (parity band + w_eff/M_cross ceiling -> a blow-up
    self-routes substrate_not_ready_requeue, never a decisive FAIL). Successor falsifier V3-EXQ-713 queued
    (OFF vs bounded-ON, GAP-A reef-bipartite; parity-win precondition + C1 committed-class entropy
    strict-above the un-gained baseline on >=2/3 divergent seeds). Byte-identical OFF. PROMOTES NOTHING:
    MECH-439 stays candidate/substrate_ceiling + pending_retest_after_substrate until 713 adjudicates (the
    711 self-routed DECISIVE weakens was REJECTED by the confirmed autopsy). See ree-v3/CLAUDE.md 'BOUNDED
    ascending-spiral gain -- target-PARITY controller' + learned_cross_loop_arbitration.md 'Sub-addendum:
    bounded parity controller' + substrate_queue v4_loop_segregation (failure_record[7].repaired_by).
  evidence_quality_note: >
    [2026-08-21 governance, gov-20260821-0203, confirmed failure_autopsy_V3-EXQ-936_2026-08-18.json]: [2026-08-18, failure_autopsy_V3-EXQ-936_2026-08-18]: V3-EXQ-936's manifest direction `weakens` should be changed to `non_contributory`. The load-bearing C2 criterion (per-seed paired reduction of f_variance_share >= 0.05) was ARITHMETICALLY UNREACHABLE, not merely unmet: the share is 1.0 to float64 resolution in all 8 cells (exactly 1.0, paired delta exactly 0.0, in 2 of 4 ARM_DEMOTION seeds), and firing C2 would have required var(non-F) to be 3.7e14 to 2.5e43 times larger. Cause: an unbounded E2 imagination rollout. F = compute_reality_cost is the SQUARED z_world step norm while every modulatory channel is at most linear in latent scale, so var(F) ~ var(non-F)^2 -- measured across the run's own 8 cells, OLS slope 2.020, R^2 0.978 over 54 orders of magnitude, against a predicted 2.000. Back-solved rollout magnitude reaches ||dz_world|| ~ 1.42e18, inside the 1e16-1e18 band V3-EXQ-569e measured on 2026-05-31 for this identical code path (SD-056 substrate_queue failure_record; clean ARM_0 baseline there was 0.39-0.77). The repair was BUILT for that incident (SD-056 amend lever (b), per-step rollout output norm clamp, e2_fast.py:694-747, validated V3-EXQ-617 PASS) and ships default-OFF; this run's declared parity ancestor V3-EXQ-689i arms it (True, ratio 2.0) and this lineage's baseline module contains zero occurrences of the flag. In float32 the consequence is stronger than dominance: std(non-F)/std(F) is below float32 eps in 7 of 8 cells, so the harm/residue/benefit/novelty/goal terms are ADDITIVELY ANNIHILATED in the score sum -- C1's conversion result survives only because MECH-448 acts on eligibility rather than on the additive sum. The claim was not tested; MECH-439 is neither supported nor weakened by this run. Status unchanged (candidate); epistemic_category standard (NOT substrate_ceiling -- see the autopsy's Section 9 for why a null instrument must not be counted as a ceiling hit).

    [2026-08-10 governance, GOV-APPLY-1]: hit count corrected 10 -> 9, 710 dropped
    (failure_autopsy_V3-EXQ-710_2026-07-20 confirmed re-adjudication withdrew the 2026-07-03 710
    autopsy's own evidentiary basis -- see MECH-140's evidence_quality_note for the full defect
    account). Demotion stands unaffected. Old note follows.
    GOV-CEIL-1 CEILING-EXHAUSTION DEMOTION 2026-07-09: 10 confirmed substrate_ceiling failure_autopsy
    hits (689a/700/700a/700b/700c/700d/709/710/711/713) with no positive discrimination on any richer
    substrate -> demoted to contested candidate (epistemic_category standard, ceiling_decision exhausted).
    The competing NULL reading -- the F-dominance mechanism is inert / doing no work -- is now carried
    CO-EQUALLY (no longer suppressed by the substrate_ceiling category). This is NOT falsification: the
    claim keeps its full history; promotion is re-gated on a future positive discrimination on a richer
    substrate (the pending ARC-107 MECH-448/449 BG-constitution validation V3-EXQ-689g).

    V3-EXQ-689a (MECH-439 conflict-grade GAP-BLIND falsifier; supersedes V3-EXQ-689) FAIL / non_contributory;
    readiness ALL met + non_degenerate. The pre-registered conflict-grade BOTH-levers form (ARM_A1B1: gap-scaled
    shortlist-width Factor A + gap-scaled commit-T Factor B) does NOT convert -- committed-action-class entropy
    0.387 (= A0B0 baseline 0.371), 0/3 seeds strict-above the collapsed-proposer controls AND 0/3 above the
    gap-blind controls (ARM_FIXED_KMAX 0.546 / ARM_FIXED_HOT_T 0.591). The committed-action-diversity conversion
    ceiling PERSISTS under the near-tie conflict-grade lever family -- consistent with MECH-439 F-dominance, NOT a
    weakens. LOAD-BEARING 2x2 dissociation (manifest two_by_two_dissociation): Factor B (gap-scaled commit-T)
    ALONE converts -- ARM_A0B1 = 0.850, 2/3 seeds strict-above BOTH control sets; Factor A (graded shortlist
    width) alone is inert (ARM_A1B0 = 0.440); and combining them is DESTRUCTIVE (A1B1 collapses to baseline).
    User-adjudicated 2026-06-20: the conflict-grade NEAR-TIE parametric lever family is EXHAUSTED as a
    committed-conversion fix; ELEVATE to the basal-ganglia-constitution build (ARC-107) -- lead lever MECH-448
    rank-preserving F->eligibility demotion (divisive-normalisation grounded), broader follow-on MECH-449
    Go/No-Go eligibility governance. pending_retest_after_substrate.
    CORROBORATION 2026-06-21: V3-EXQ-689c (Factor-B-alone gap-scaled commit-T isolation retest;
    no decision authority per the MECH-442 packet) FAIL / non_contributory, readiness ALL met +
    non_degenerate (route_range 0.216, e2-spread 0.063, Factor-B lever engaged 3x), self-route
    conversion_ceiling_persists_despite_conflict_grade. The 689a informational A0B1-converts
    dissociation does NOT hold up under isolation -- the single-lever Factor-B path is not a
    robust committed-conversion fix either, corroborating the exhausted-parametric-family decision
    and the constitutional MECH-448 build. non_contributory: does NOT weaken MECH-439.
    CORROBORATION 2026-06-29 (cluster autopsy failure_autopsy_700d-708-single-arena-ceiling_2026-06-29):
    V3-EXQ-700d (same-layer-null VALIDITY RE-TUNE; supersedes 700c) FAIL/non_contributory. The
    null-magnitude fix succeeded (field_noise_magnitude_matched 1.0x, correcting 700c's 41.42x) but the
    correctly-sized magnitude-matched same-layer null STILL failed to verify-lift committed-class entropy
    (matched_noise_control_verified_lifting 0/2) and remained destructive -- the 3rd consecutive failed
    construction of a VALID committed-class same-layer null in the V3 single E3 arena (700b decoupled /
    700c 41.42x / 700d matched-but-inert). The pre-registered terminal did not fire because the null could
    not be made valid; CONFIRMS the single-arena substrate ceiling. RE-DERIVE BRAKE FIRED (7th MECH-439 /
    5th ARC-108 / 4th MECH-450 substrate_ceiling/non_contributory autopsy): a same-claim single-arena
    re-test (700e) is REFUSED; the V3 same-layer-null validity question is EXHAUSTED. Route = the
    already-built v4_loop_segregation (ARC-110) substrate, validating via V3-EXQ-707b. CLAIM-SYNTHESIS
    FLAG (user-requested): 7 substrate_ceiling/non_contributory autopsies from structurally different
    angles = granularity-debt signal; run a /claim-synthesis decomposition of MECH-439 ("F-dominance" ->
    finer mechanisms) IF 707b also fails to convert (confirming the claim is coarse, not only
    substrate-bound). pending_retest_after_substrate.
    CORROBORATION 2026-07-03 (confirmed failure_autopsy_V3-EXQ-709_2026-07-03): V3-EXQ-709 (ARC-108 x
    ARC-110 LEARNED/DA-gated cross-loop arbitration validation -- the separate new-EXQ falsifier 707b
    routing item 3 named) FAIL / non_contributory / non_degenerate=False / substrate_not_ready_requeue.
    The learned mechanism ENGAGED (6/7 readiness gates; M_cross MOVED off init range 0.116 so LEARNED !=
    STATIC, limbic routing live 1.414) but the deeper limbic_loop_can_win sub-gate is UNMET: on the 4
    GAP-A-divergent seeds the limbic loop reached >= motor effective weight on only 1/4 (M_cross[motor,limbic]
    coupling peak ~0.03), so C1 (learned strict-above static) could NOT be validly evaluated. NEVER a weakens
    -- the conversion question was NOT measured, so MECH-439 is NOT shown intrinsic by this run. NEW
    loop-effective-weight ceiling BEYOND the 707b static-arithmetic diagnosis: even a learned [3,3] cross-loop
    matrix (eta 0.01, ~100 P2 episodes) cannot lift a non-motor loop to motor effective weight on the seeds
    that matter. Re-derive brake FIRES again (8th MECH-439 substrate_ceiling/non_contributory reading);
    same-claim TEST re-queue REFUSED; routed implement-substrate (AMEND v4_loop_segregation failure_record).
    The contingent MECH-439 /claim-synthesis flagged above was already fired 2026-06-29 (DROP-MECH-453). pending_retest_after_substrate.
    CORROBORATION 2026-07-03 (confirmed failure_autopsy_V3-EXQ-710_2026-07-03): V3-EXQ-710 (MECH-140 x
    MECH-450 DISINHIBITORY SOFT-COMPETITIVE SETTLING validation) FAIL / non_degenerate=True. The manifest
    self-routes MECH-439 "supports (ceiling intrinsic)"; the confirmed autopsy adjudicates this as
    non_contributory toward the intrinsic-vs-liftable question + NARROW corroboration only (narrow_supports_flag):
    a single-arena FAIL CANNOT establish whether the ceiling is intrinsic (that is only answerable on the
    segregated-loop substrate), it only corroborates the ALREADY-MAPPED single-arena ceiling and is single-
    pathway. All 5 readiness preconditions met + the settling MOVED the field (scs_mean_round_delta 5-8) +
    A2_ABLATED uniform kernel reproduces A0_OFF bit-identically (1.130438) -- yet over the F-collapsed single-
    selector field the structured class-surround kernel SHARPENS F (committed-class entropy REDUCED -0.169/-0.153
    nats on 2/3 divergent seeds; INTACT mean 1.047 < OFF 1.130; C1 1/3). This is F's raw-magnitude monopoly at
    the single shared selector expressing through a THIRD structurally-different conversion mechanism (after the
    700-lineage same-layer-null/exploration path and 709's learned cross-loop arbitration) -> convergent failure
    is a SUBSTRATE property, strengthening the case that v4_loop_segregation is the load-bearing build (NOT that
    the ceiling is globally intrinsic). NOT counted as fresh support for global intrinsicness. Re-derive brake
    FIRES again (9th MECH-439 substrate_ceiling/non_contributory reading); same-claim single-arena TEST re-queue
    REFUSED; routed implement-substrate (AMEND v4_loop_segregation failure_record, now 7). pending_retest_after_substrate.
    CORROBORATION 2026-07-04 (confirmed failure_autopsy_V3-EXQ-711_2026-07-04): V3-EXQ-711 (ARC-110 x
    ARC-108 ASCENDING-SPIRAL GAIN validation -- the ascending-gain repair BOTH the 709 AND 710 autopsies
    routed to; new EXQ + newly-built mechanism on ree-v3 6b660a6) FAIL / non_degenerate=True. The manifest
    self-routes MECH-439 "supports (ceiling intrinsic)"; the confirmed autopsy REJECTS that decisive read.
    The load-bearing limbic_loop_can_win precondition was recorded met on 3/3 divergent seeds, but NOT by a
    competitive parity win: the raw 20x-forward x 5x-plasticity gain compounding through the three-factor
    eligibility update RAN AWAY (M_cross range peak 4897.8 vs the un-gained 709/OFF ~0.02-0.12; w_eff[limbic]
    peak 10-2274x w_eff[motor] across divergent seeds) -- a limbic-loop MONOPOLY (limbic-pinning) that merely
    replaces the F/motor-pinning, not a fair arbitration. It did NOT convert: committed-class entropy FELL
    below the un-gained baseline on 2/3 divergent seeds (delta -0.049 / -0.210); only the most extreme blow-up
    seed rose (+0.153). The conversion was therefore tested under a DEGENERATE (saturated) arbitration and was
    NOT validly measured -> non_contributory (the win-gate lacked a saturation guard so its own
    substrate_not_ready_requeue branch did not fire). MECH-439-intrinsic is NOT shown (F-dominance was replaced
    by limbic-dominance, not dissolved into competition). 709->711 pattern: the raw-scalar ascending gain has
    NO stable parity regime -- sub-threshold (709, ~0.03, never wins) or runaway (711, 10-2274x, monopolizes);
    the mechanism is MISSING A CONTROLLER (gain-normalization / target-parity law), a NEW substrate finding,
    NOT evidence of an intrinsic ceiling. Re-derive brake FIRES again (10th MECH-439 substrate_ceiling/
    non_contributory reading); blind same-claim gain-magnitude re-queue REFUSED; routed implement-substrate
    (AMEND v4_loop_segregation failure_record, now 8: build a bounded/normalized ascending gain + a saturation
    guard on the win-gate). pending_retest_after_substrate.
    CORROBORATION 2026-07-05 (confirmed failure_autopsy_V3-EXQ-713_2026-07-05): V3-EXQ-713 bounded
    parity-controller validation FAIL / non_degenerate=True. FIRST validly-measured arbitration-level
    conversion in the 709->711->713 lineage: fair band parity win 4/4 divergent seeds, no_saturation_blowup=0
    (711 monopoly repaired) -- C1 STILL failed. Manifest self-routes 'supports (ceiling intrinsic)'; adjudicated
    NARROW corroboration only (narrow_supports_flag) -- a single-arena FAIL cannot establish global
    intrinsicness. NEW datum: fair arbitration parity win does NOT convert -> cross-loop-arbitration-reweighting
    route EXHAUSTED; conversion is the SELECTION-face op (MECH-448, lifting on GAP-A). Re-derive brake FIRES
    (11th MECH-439 reading); same-level arbitration re-letter REFUSED; route = selection-face MECH-448
    behavioural retests (654h/485i/445h/625e). non_contributory: does NOT weaken MECH-439.
    pending_retest_after_substrate.

    [enum-comment migrated to field 2026-07-18] epistemic_category: GOV-CEIL-1 demotion
    2026-07-09 (was substrate_ceiling)
  depends_on:
    - ARC-065    # the behavioural-diversity GENERATION pathway whose output F-dominance prevents converting to committed action
    - MECH-309   # monomodal-policy-collapse equilibrium this names the selection-side mechanism of
    - ARC-062    # rule-apprehension selector half; its committed reach is gated by the same F-dominance
    - MECH-294   # selection-authority/binding substrate that must overcome F to move the committed argmax
  location: evidence/planning/conversion_ceiling_phase0_synthesis_2026-06-18.md#mech-439
  notes: >
    Registered 2026-06-18 from the conversion-ceiling Phase 0 disambiguation workflow
    (run wf_c03ff4f4-d45; synthesis evidence/planning/conversion_ceiling_phase0_synthesis_2026-06-18.md).
    Phase 0 separated the "committed_action_class_entropy ~ 0" surface symptom into four
    distinct loci (A representation-geometry mitigated, B selection F-dominance = this claim,
    C de-commit authority orthogonal, D CRF gate-lockout closed, E within-class downstream of B).
    LOAD-BEARING EVIDENCE: V3-EXQ-571 (F = 88-89% of E3 temporal variance in BOTH baseline and
    full-diversity-stack arms); V3-EXQ-569h (committed lift non-robust 1/3 seeds WITH the candidate
    pool supra-floor diverse -- pairwise_dist 0.095, route_range 0.31 -- proving the bottleneck is
    selection-variance, not pool geometry); V3-EXQ-654f (D-gate unlocked, CRF counterfactual delta
    reaches the accumulator yet committed entropy bit-identical ON vs OFF -- three channels converge
    on this one bottleneck). PARTIAL CIRCUMVENTION already validated: V3-EXQ-569i top-k k=3
    shortlist-then-modulate PASSES 2/3 seeds (thin margin 0.711 vs 0.650) where 569h/684 margin/whole-set
    shortlist failed. FALSIFIER: a selection lever that lifts committed-action-class entropy strict-above
    BOTH collapsed-proposer and matched-noise controls on >=2/3 seeds WITHOUT reducing F's E3 variance
    share refutes the "F-monopoly caps committed diversity" framing; conversely, persistent flat committed
    entropy under any diversity channel while F-share stays >0.85 supports it. Pre-registered discriminator:
    the 2x2 (A de-collapse) x (B top-k shortlist) isolation under the C+D composite (synthesis doc).
    V3-tractable now (the substrate exists -- this is testable, NOT substrate_ceiling); the open question
    is whether the thin shortlist margin survives the composite or whether F's variance share must be
    rebalanced directly. External analog on the maintain-diversity-against-a-dominant-fitness-gradient
    side: Quality-Diversity / MAP-Elites (convergence demand-queue CDQ-003).
  what_would_answer: |
    NON-DEGENERACY PRECONDITION (already the pre-registered discriminator in
    this claim's own notes): requires F's E3 committed-selection-variance
    share to be independently measurable per arm (established baseline:
    V3-EXQ-571, F = 88-89% in both plain and full-diversity-stack arms) AND
    a selection lever to be tested against BOTH a collapsed-proposer control
    and a matched-noise control with non-zero cross-arm variance on
    committed-action-class entropy. 11 confirmed substrate_ceiling/
    non_contributory autopsies (689a/700/700a-d/709/710/711/713) already
    establish that most tested levers (near-tie parametric shortlist/
    commit-T combinations, learned cross-loop arbitration, disinhibitory
    settling, ascending-gain arbitration -- even a FAIR parity win in 713)
    do NOT convert; none of these count as fresh evidence either way per
    the confirmed autopsies -- do not re-litigate them.
    CONFIRMING signature (this claim's own stated FALSIFIER, restated):
    persistent flat committed-action-class entropy under ANY tested
    diversity channel WHILE F's E3 variance share stays >0.85 supports the
    "F-monopoly caps committed diversity" framing.
    FALSIFYING signature (this claim's own stated FALSIFIER, restated): a
    selection lever lifts committed-action-class entropy strict-above BOTH
    collapsed-proposer and matched-noise controls on >=2/3 seeds WITHOUT
    reducing F's E3 variance share -- refutes the F-monopoly framing.
    CURRENT STATUS (as of the GOV-CEIL-1 2026-07-09 ceiling-exhaustion
    demotion): this claim is a CONTESTED candidate -- the competing NULL
    reading (F-dominance is inert / doing no causal work) is carried
    CO-EQUALLY, not suppressed. Promotion in either direction is re-gated
    on a future positive discrimination on the pending ARC-107
    MECH-448/449 basal-ganglia-constitution validation (V3-EXQ-689g) --
    this what_would_answer does not resolve the contest, it restates the
    claim's own pre-registered discriminator so a future reader does not
    need to re-derive it from 11 autopsies of prose.

- id: MECH-440
  title: "state_conditioned_self_annealing_noise_floor (NoisyNet learned-parametric-weight-noise analog). A mechanistic refinement of MECH-313's tonic exploration noise floor. MECH-313 is registered as a STATE-INDEPENDENT floor (a post-softmax max-entropy/temperature analog); V3-EXQ-687 found that floor NON-PROPAGATING -- the temperature was invisible to the argmax/committed action (selected_action_entropy=0.0, the r1a_entropy_only_artefact the degeneracy gate caught). MECH-440 asserts the LC-NE tonic floor must instead be injected as LEARNED PER-PARAMETER (factorised-Gaussian) WEIGHT NOISE in the E3 selection pathway, so it (i) PROPAGATES into the committed action (it changes which action is argmax, not just a discarded pre-commit temperature), (ii) is STATE-CONDITIONED by construction (weight noise x state-dependent activations perturbs different states differently, with no hand-set per-state schedule), and (iii) SELF-ANNEALS (a gradient-trained per-parameter sigma falls where the policy is confident and stays up where exploration pays, so the floor does not wash out committed-action diversity uniformly). External analog: NoisyNet (Fortunato et al. 2018)."
  claim_type: mechanism_hypothesis
  subject: policy.state_conditioned_self_annealing_noise_floor
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-24
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-708a_2026-07-22
      as_of: 2026-07-22
      verdict: non_contributory/measurement_gap
  implementation_phase: v3
  version_relevance: v3
  v3_pending: true
  registered_utc: "2026-06-18"
  ceiling_routing_note: "BUILD APPROVED 2026-06-27 (user decision; packet CPKT-TONIC-EXPLORATION-NOISE-20260618 'hybridize' rec.; decision_due 2026-07-02 met early). The `ceiling_decision: deferred` park marker is REMOVED -- this claim is now routed to /implement-substrate (NoisyNet learned per-parameter factorised-Gaussian weight noise at the E3 selection head, no-op default sigma_init=0 / flag OFF = bit-identical), NOT parked. Biology-before-formal gate PASSED at adjudication: Aston-Jones&Cohen 2005 (supports 0.78) + Tervo et al 2014 (supports 0.82, CAUSAL LC-NE->ACC gating of stochastic choice under uncertainty) ground the state-conditioned + self-annealing shape; MECH-313's state-independent framing is biologically under-specified. KNOWN BUILD RISK (recorded, not a blocker): the SAME-DAY confirmed cluster autopsy failure_autopsy_704b-706b-conversion-ceiling_2026-06-27 re-rooted the committed-action-class conversion ceiling to the SINGLE-ARENA collapse (-> ARC-110 v4_loop_segregation), corroborated by two independent mechanisms (MECH-451 finer-channel + MECH-314 curiosity, both re-derive-braked). That is the packet's own pre-registered could_be_wrong_if #4 (F-dominance/arena subsumes the injection-locus fix). THEREFORE the MECH-440 falsifier MUST (i) run on the 569i top-k shortlist + MECH-448 use_f_eligibility_demotion conversion substrate, and (ii) disambiguate the injection-SITE axis (weight noise vs temperature) from the single-arena/ARC-110 axis -- if it reproduces the single-arena ceiling (thrash-not-carve), route to ARC-110, NOT a deeper noise build. Coordinate with -- do not duplicate -- the empirical 687-successor. PROMOTES NOTHING: stays candidate/substrate_ceiling/v3_pending until the substrate lands + the falsifier returns contributory."
  pending_retest_after_substrate: true
  implementation_note: "SUBSTRATE IMPLEMENTED 2026-06-27 (/implement-substrate; decision-of-record evidence/decisions/cpkt_tonic_exploration_noise_build_decision_2026-06-27.md). Module ree-v3/ree_core/policy/noisy_selection_head.py (NoisySelectionHead): factorised-Gaussian weight noise built per-candidate from each candidate's first-action vector, injected into _modulatory_accum (+ the ARC-110 segregated `final`) BEFORE the committed within-eligible argmin so it PROPAGATES (the V3-EXQ-687 non-propagation fix). mu frozen at 0 (pure noise injector); self-anneals via a LOCAL confidence EMA on (1 - F-gap_norm). Config E3Config.use_noisy_selection_head (default False = bit-identical OFF) + noisy_selection_sigma_init (default 0.0 = exactly-zero output = bit-identical even ON). Smoke-tested: sigma=0 bit-identical; sigma>0 flips the committed argmin across resamples (propagation confirmed); finiteness/EMA guards block any nan injection. ARC-106 divergences logged: per-parameter sigma below the systems-level tonic/phasic gate; sigma annealed by LOCAL EMA not RL gradient (REE does not backprop selection). Falsifier QUEUED on the 569i top-k + MECH-448 demotion stack (loop-seg OFF vs ON disambiguation). PROMOTES NOTHING: stays candidate/substrate_ceiling/v3_pending until the falsifier returns contributory."
  evidence_quality_note: >-
    V3-EXQ-708 (MECH-440 NoisyNet propagation falsifier) was RE-ADJUDICATED 2026-07-19
    (failure_autopsy_V3-EXQ-708_2026-07-19). Its pre-commit class entropy DV is invalid: the driver read
    e3.last_precommit_probs once per env step with no clear, and E3 select() runs on a 5-20-step
    arousal-modulated cadence, so each probability vector was replicated by its hold duration. The weighting
    is arm-dependent (matched-seed exposure differs by up to +58% between the OFF and NOISE arms), so the
    between-arm delta is distorted, not merely imprecise. The previously recorded finding -- that
    supra-floor weight noise raised pre-commit entropy by 0.0 and therefore 'washes out at the F-dominated
    single-arena argmax' -- is a claim about the contaminated quantity and is WITHDRAWN. The run's
    substrate_not_ready_requeue route survives on margin arithmetic, but MECH-440 was never validly tested.
    Noise injection itself was genuine (bias range 0.2216, supra-floor), and all five non-DV preconditions
    passed, so the defect is in driver instrumentation, not in the substrate. epistemic_category
    substrate_ceiling CLEARED -- MECH-440 is untested, not ceiling-limited. The correction is not
    recoverable from the recorded emission (no per-row sink; Phase 3 cloud workers transport only
    manifest_bytes). Pending a corrected re-run V3-EXQ-708a using the clear-before-select pattern of
    v3_exq_785a. MECH-440 remains candidate / v3_pending.

    2026-07-24 governance (failure_autopsy_V3-EXQ-708a_2026-07-22, confirmed): the corrected
    re-run landed. Recorded as a substantive finding (user-confirmed routing) rather than a
    re-derivation of readiness: neither the NOISE lever nor the baseline moves the pre-commit
    distribution on the corrected, clear-before-select DV. `non_contributory` /
    `measurement_gap` -- the instrument now measures cleanly and reports no effect, which is
    informative about where the propagation still fails to reach, distinct from 708's earlier
    `measurement_test_design_defect` (where the instrument itself could not be trusted). MECH-440
    remains candidate / v3_pending; substrate stands as built, falsifier still owed a design that
    isolates the injection-site axis per the ceiling_routing_note above.
  depends_on:
    - MECH-313   # parent: stochastic_noise_floor (LC-NE tonic analog), currently a STATE-INDEPENDENT floor; this refines it
    - MECH-260   # dACC anti-recency (the other tonic-noise leg in the GAP-C / Q-045 cluster)
    - Q-045      # the noise-floor-vs-anti-recency collapse question this refinement bears on
    - ARC-065    # behavioural-diversity-generation pathway; this is substrate (a) LC-NE tonic, refined
  location: docs/architecture/state_conditioned_exploration_noise_floor.md#mech-440
  notes: >
    Registered 2026-06-18 from Convergence Demand Pipeline row CDQ-002 (NoisyNet primary intake;
    session convergence-cdq002-noisynet-intake-20260618T0643Z). Targets the mechanism gap named by
    behavioral_diversity_isolation:GAP-C and arc_062_rule_apprehension:GAP-H ("Tonic, state-conditioned
    exploration noise floor (MECH-313 LC-NE analog) -- distinct from a fixed epsilon schedule").
    The necessity is established (ARC-065); the MECHANISM for state-conditioning + self-annealing +
    PROPAGATION is the import. Distinct from the empirical-substrate failure that
    failure_autopsy_V3-EXQ-687_2026-06-18 diagnoses (survival/monostrategy lock on the existing
    scaffolded_sd054_onboarding substrate, addressed by chip task_6c970c2e + the queued 687-successor):
    even a survival-competent substrate reproduces the 687 non-propagation if the floor stays a
    post-softmax temperature.
    BIOLOGY (lit-pull BEFORE registration, per SD-003 / SD-010-011 discipline): the LC-NE tonic
    exploration signal is itself state/uncertainty-conditioned and annealed by controllability --
    Aston-Jones & Cohen 2005 adaptive-gain theory (tonic exploration engaged when task utility wanes,
    phasic exploitation when utility is high; ACC/OFC drive the mode; targeted_review_connectome_mech_313
    astonjones2005, supports 0.78) and Tervo et al. 2014 Cell (LC->ACC noradrenergic input GATES the
    switch into a stochastic/variable choice mode under environmental uncertainty and suppresses it when
    model-based strategy works; targeted_review_connectome_mech_313 tervo2014, supports 0.82). So
    MECH-313's state-INDEPENDENT framing is biologically under-specified; the NoisyNet refinement is the
    biologically-correct shape, not just an engineering trick.
    FALSIFIER: arming selection-head parametric weight noise produces committed (argmax)
    selected_action_entropy STRICTLY ABOVE a matched-pre-commit-variance post-softmax temperature control
    (the V3-EXQ-687 non-propagating arm) on >=2/3 seeds, with the diversity reaching the committed action
    (non-degenerate). Refuted if it washes out at argmax identically to the temperature floor (reproduces
    the r1a_entropy_only_artefact), or raises pre-commit entropy without raising committed-action
    diversity (thrash, not carve).
    REE LOCUS: the MECH-313 use_noise_floor / noise_floor lever in the E3 trajectory-selection /
    committed-action pathway (e3_selector / select_action). Adapter delta: replace/augment the
    post-softmax temperature with learned factorised-Gaussian weight noise on the selection-head /
    modulatory-bias parameters; no-op default (sigma_init=0 / flag OFF = bit-identical), per the
    V3-primacy version-layering doctrine. Convergence intake artifacts: REE_convergence/sources/noisynet/
    + reports/2026-06-18_tonic_exploration_noise_synthesis.md; promotion packet
    CPKT-TONIC-EXPLORATION-NOISE-20260618. CANDIDATE only -- promotes nothing, blocks no V3 closure node;
    decide-whether-to-build is a later governance step.

- id: MECH-441
  title: "model_disagreement_directed_curiosity (RND / Plan2Explore analog). A mechanistic refinement of MECH-314's structured_curiosity_bonus: a PER-CANDIDATE, SELF-ANNEALING intrinsic curiosity signal derived from E2 FORWARD-MODEL DISAGREEMENT (an ensemble / dropout-variance proxy over E2 forward predictions) -- high where the forward model is uncertain, collapsing toward zero as a region is learned -- fed into E3 selection so it PROPAGATES per-candidate to the committed action, unlike the broadcast-novelty EMA channel found non-propagating in V3-EXQ-590a / EXQ-141b. The self-annealing-on-learning-progress property is the non-degeneracy guard against a cross-candidate-flat (degenerate) bonus. External analog: RND (Burda et al. 2018) predictor-error novelty; Plan2Explore (Sekar et al. 2020) ensemble model-disagreement. Maps to MECH-314c learning-progress / MECH-314b uncertainty-driven curiosity."
  claim_type: mechanism_hypothesis
  subject: curiosity.model_disagreement_directed_signal
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-441@2026-06-27T10:54:49.591342Z"
      as_of: 2026-06-27
      verdict: defer_pending_arc110/applied
  epistemic_category: substrate_ceiling
  implementation_phase: v3
  version_relevance: v3
  v3_pending: true
  registered_utc: "2026-06-18"
  ceiling_routing_note: "BUILD APPROVED 2026-06-27 (user decision; packet CPKT-TONIC-EXPLORATION-NOISE-20260618 'hybridize' rec. -- BOTH legs built; decision_due 2026-07-02 met early). The `ceiling_decision: deferred` park marker is REMOVED -- routed to /implement-substrate (a per-candidate self-annealing E2 forward-model disagreement readout -> MECH-314 curiosity bonus into E3 selection, no-op default flag OFF = bit-identical), NOT parked. Biology-before-formal gate at adjudication: Daw et al 2006 (supports 0.72) grounds a dedicated frontopolar directed-exploration substrate (substrate existence; the model-disagreement COMPUTATION is the engineering import, the weakest of the three anchors -- noted). KNOWN BUILD RISK (recorded, stronger here than for MECH-440): the SAME-DAY confirmed failure_autopsy_704b-706b-conversion-ceiling_2026-06-27 found the MECH-314 curiosity-conversion lineage re-derive-BRAKE-LOCKED (8 autopsies) -- 706b was a FAIR double-gated test (valid null) where the curiosity channel WORKS but cannot convert because the binding constraint is the single-arena collapse (-> ARC-110), NOT the curiosity channel/mechanism. MECH-441 refines the channel MECHANISM; the autopsy says the channel is not the bottleneck. THEREFORE the MECH-441 falsifier MUST run on the 569i top-k + MECH-448 demotion substrate and is very likely gated on ARC-110 v4_loop_segregation landing first; if it reproduces the single-arena ceiling, route to ARC-110. Built as the complementary substrate-(b) directed-curiosity leg to MECH-440's substrate-(a) floor. Coordinate with -- do not duplicate -- the 705/705b/706/706b empirical curiosity-conversion lineage. PROMOTES NOTHING: stays candidate/substrate_ceiling/v3_pending until the substrate lands + the falsifier returns contributory."
  implementation_note: "SUBSTRATE IMPLEMENTED 2026-06-27 (/implement-substrate; decision-of-record evidence/decisions/cpkt_tonic_exploration_noise_build_decision_2026-06-27.md). Module ree-v3/ree_core/policy/model_disagreement.py (ModelDisagreementEnsemble): a standalone K-head ensemble of ResidualHarmForward delta-predictors over (z_world, action); per-candidate cross-head VARIANCE -> a propagating per-candidate curiosity BONUS into _modulatory_accum (the V3-EXQ-590a broadcast-EMA non-propagation fix), arbitrating WITHIN the F-eligible set (safety inherited). Built at the agent level when use_model_disagreement_curiosity AND n_disagreement_heads>=2; passed via the version-layering-guarded model_disagreement_per_candidate kwarg. Config E3Config.use_model_disagreement_curiosity (default False) + model_disagreement_weight (default 0.0) + LatentStackConfig.n_disagreement_heads (default 0 = not built) = bit-identical OFF. Phased training required: disagreement_ensemble.train_step() on the FROZEN z_world target (P1 stop-gradient). Smoke-tested: ensemble disagreement supra-floor, per-candidate bonus steers the committed within-eligible argmin, sim-gated, train_step runs. FALSIFIER HELD (blocked_substrate) gated on ARC-110 validation V3-EXQ-707 returning contributory -- 706b proved the curiosity channel WORKS; the single-arena collapse (not the channel) is the binding constraint (failure_autopsy_704b-706b-conversion-ceiling_2026-06-27), so a run before ARC-110 is validated would re-derive the arena ceiling (vacuous FAIL). User decision 2026-06-27. PROMOTES NOTHING: stays candidate/substrate_ceiling/v3_pending."
  depends_on:
    - MECH-314   # parent: structured_curiosity_bonus; this refines its computational form + propagation
    - ARC-065    # behavioural-diversity-generation pathway; this is substrate (b) frontopolar curiosity, refined
  location: docs/architecture/state_conditioned_exploration_noise_floor.md#mech-441
  notes: >
    Registered 2026-06-18 from Convergence Demand Pipeline row CDQ-002 (RND / Plan2Explore SECONDARY
    intake; session convergence-cdq002-noisynet-intake-20260618T0643Z). The directed-curiosity leg (b)
    complementary to the MECH-440 undirected-floor leg (a): a tonic floor supplies baseline variability,
    directed curiosity steers it toward informative regions (ARC-065 multi-substrate pathway). The
    Q-044/MECH-314 curiosity leg of arc_062_rule_apprehension:GAP-H is already SATISFIED (V3-EXQ-604c);
    MECH-441 does NOT reopen it -- it refines the curiosity MECHANISM (per-candidate model-disagreement
    that propagates), addressing the V3-EXQ-590a/141b broadcast-EMA non-propagation.
    BIOLOGY (lit-pull): frontopolar uncertainty-driven exploration is a dedicated directed-exploration
    substrate distinct from value-based exploitation -- Daw et al. 2006 Nature (frontopolar + IPS
    preferentially active during exploratory decisions; targeted_review_connectome_mech_314 daw2006,
    supports 0.72). Grounds ARC-065 substrate (b); the disagreement-specific computation (RND/Plan2Explore)
    is the computational form imported here.
    FALSIFIER: a per-candidate, self-annealing model-disagreement bonus over E2 forward predictions raises
    committed-action diversity (selected-action entropy / route diversity) above a matched-magnitude
    constant-bonus control on >=2/3 seeds, with cross-candidate range above the non-degeneracy floor.
    Refuted if the disagreement signal is cross-candidate flat (degenerate) or does not propagate to the
    committed action (reproduces the V3-EXQ-590a broadcast-EMA non-propagation).
    REE LOCUS: a model-disagreement readout over the E2 forward model -> the MECH-314 curiosity bonus into
    E3 selection (adjacent to the DR-12 / SELF-4 E2 forward-PE work). No-op default (flag OFF / zero
    curiosity weight = bit-identical), per the V3-primacy version-layering doctrine. Convergence intake
    artifacts: REE_convergence/sources/rnd-plan2explore/ + reports/2026-06-18_tonic_exploration_noise_synthesis.md;
    promotion packet CPKT-TONIC-EXPLORATION-NOISE-20260618. CANDIDATE only -- promotes nothing, blocks no
    V3 closure node; decide-whether-to-build is a later governance step.
- id: MECH-442
  title: "Committed-action diversity survives the selection/commit step via a behavioral-descriptor eligible-set ARCHIVE (Quality-Diversity / MAP-Elites analog): the committed E3 selection should draw from a structure that, UPSTREAM of the winner-take-all commit, partitions the eligible candidate set by a behavioral descriptor (first-action / committed-action class / e2.world_forward strategy signature, NOT F-rank) and retains the F-best candidate WITHIN each behavioral niche, then commits by a coverage-aware rule -- so a behaviorally-distinct action stays selectable at a moderate primary-score (F) gap, bounded by the per-niche-elite safety envelope, rather than collapsing to F's single global optimum. The validated V3-EXQ-569i top-k shortlist is the descriptor-free degenerate instance (eligible-set axis = F-rank, near-ties only); MAP-Elites generalizes it to a behavioral-descriptor-indexed eligible-set archive paired with an active variability generator (MECH-313), predicted to convert per-candidate diversity into committed-action diversity more robustly than the thin near-tie shortlist. The F-dominated committed argmax itself (MECH-439) is biologically faithful and is NOT changed -- only the eligible-set structure feeding it."
  claim_type: mechanism_hypothesis
  subject: selection.behavioral_descriptor_committed_selection_archive
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-442@2026-06-19T18:52:58.090603Z"
      as_of: 2026-06-19
      verdict: hold_pending_v3_substrate/applied
  epistemic_category: substrate_conditional
  implementation_phase: v3
  version_relevance: v3
  registered_utc: "2026-06-18"
  ceiling_decision: deferred
  ceiling_routing_note: "2026-06-19: build-decision deliberately DEFERRED (parked, not orphaned). Convergence-intake forward refinement (MAP-Elites generalization of the validated 569i top-k shortlist); OFF the V3 critical path (blocks no closure node). NOTE: this claim is epistemic_category substrate_conditional (NOT substrate_ceiling), so the Step-6a-v substrate-ceiling audit does not flag it today; the marker is applied for consistency with its MECH-440/441 cluster siblings and future-proofs the routing if it is ever re-categorized. The conversion ceiling is already attacked via the live, validated 569i top-k shortlist; the prose 'DO NOT build in V3 until routed by experiment' is now machine-readable here."
  depends_on:
    - ARC-065    # the behavioural-diversity GENERATION pathway whose output the archive preserves through the commit
    - MECH-341   # within-class diversity preserver (scoring-layer sibling); the archive is the committed-selection-layer complement
    - MECH-439   # F-dominance: the conversion-ceiling diagnosis this archive circumvents (and the biologically-faithful behavior of the commit gate)
    - MECH-294   # selection-authority/binding substrate the archive must route around F with
    - MECH-313   # active variability-generator analog the biology pairs the repertoire with (added per the 2026-06-18 lit-pull refinement)
    - ARC-062    # rule-apprehension selector half; its committed reach is gated by the same F-dominance
  location: docs/architecture/quality_diversity_committed_archive.md#mech-442
  notes: >
    Registered 2026-06-18 from the REE_convergence Quality-Diversity / MAP-Elites intake
    (Convergence Demand Pipeline row CDQ-003; promotion packet CPKT-QUALITY-DIVERSITY-20260618;
    comparison artifact REE_convergence/sources/quality-diversity/comparison_table.md; synthesis
    REE_convergence/reports/2026-06-18_quality_diversity_conversion_ceiling_synthesis.md). External
    analog: Quality-Diversity / MAP-Elites (Cully et al. 2015 Nature doi:10.1038/nature14422; Mouret
    & Clune 2015 arXiv:1504.04909; Pugh/Soros/Stanley 2016) -- an explicit behavioral-descriptor
    archive whose per-niche elites SURVIVE selection rather than collapsing to the single global
    optimum. Targets the committed-action-diversity conversion ceiling (MECH-439 F-dominance: the
    primary score F monopolises ~88-89% of E3 committed-selection variance, V3-EXQ-571; per-candidate
    diversity drowns at the argmax). REE LOCUS: ree-v3 e3_selector.py E3TrajectorySelector.select, the
    modulatory-bias-selection-authority + modulatory_shortlist_mode='top_k' machinery (the validated
    V3-EXQ-569i top-k shortlist is the descriptor-free degenerate eligible-set restriction this
    generalizes); behavioral_diversity_isolation:GAP-B (cross-ref the GAP-A 569i conversion lineage).
    BIOLOGY (2026-06-18 /lit-pull evidence/literature/targeted_review_connectome_mech_442/, 5 PubMed
    sources, SYNTHESIS verdict SUPPORTED-with-refinement): a structured behavioral-module repertoire is
    a real biological object (Markowitz et al. 2018 Cell, striatum encodes module identity/ordering);
    behavioral diversity is an actively-maintained regulated feature, not noise (Dhawale/Smith/Olveczky
    2017 Annu Rev Neurosci); diversity is generated by a circuit SEPARATE from the motor selector
    (Kao & Brainard 2006; Tesileanu/Olveczky/Balasubramanian 2017 LMAN/AFP). LOAD-BEARING WEAKENS
    (Ponzi 2007): the basal-ganglia selection gate is a winner-take-all that collapses to a single
    directed action under a strong reward gradient -- so F-dominance at the commit is biologically
    faithful and the archive must live UPSTREAM of / restrict the eligible set BEFORE the argmax
    (exactly what 569i top-k does), paired with an active variability generator (MECH-313). FALSIFIER:
    if a behavioral-descriptor-indexed committed-selection archive does NOT lift committed-action-class
    entropy strict-above BOTH collapsed-proposer and matched-noise on >=2/3 seeds BEYOND the
    descriptor-free top-k shortlist (V3-EXQ-569i), OR lifts it only by selecting F-dominated/unsafe
    actions past the per-niche-elite bound (a quality/safety regression), the behavioral-descriptor
    archive earns no keep over top-k (the fix is then rebalancing F's E3 variance share directly,
    MECH-439). Pre-registered discriminator: the 2x2 (F-de-collapse) x (behavioral-descriptor archive
    vs top-k shortlist) committed-selection ablation under the C+D composite, with a per-niche F-quality
    guard. V3-leaning (on the V3 work surface; the committed-selection locus + top-k are built V3
    substrate); substrate_conditional on a behavioral-descriptor eligible-set archive beyond top-k not
    yet built. OFF the V3 critical path -- blocks no V3 closure node; AMENDS the GAP-B conversion locus
    (generalizes the validated top-k structure) rather than minting a parallel module. CANDIDATE only --
    promotes nothing, flips no node status; decide-whether-to-build is a later governance step. DO NOT
    build in V3 until routed by experiment.

- id: MECH-443
  title: "priority_weighted_replay_write_selection (MuZero/EfficientZero reanalyze + hippocampal prioritized-replay analog). A mechanistic refinement of MECH-319's simulation-mode rule-write gate. MECH-319 is a BINARY categorical gate: during a ghost/replay/DMN pass it either blocks arbitration-weight writes (normal) or admits them (the V3-EXQ-543c/628 falsifier flag) -- all-or-nothing, no ordering among the replayed transitions it admits. MECH-443 asserts that WHEN the replay write channel is open, the transitions admitted to update the rule-arbitration layer should not be written uniformly but in GRADED priority order, weighted by an update-utility signal (a surprise / value-prediction-error / coverage proxy), so a high-utility replayed transition writes more strongly and a low-utility one writes weakly or not at all. The external analog is EfficientZero/MuZero prioritized experience replay (priority ~ value-prediction error); the biology is hippocampal SWR replay, which is demonstrably prioritized rather than uniform. CRITICAL REFINEMENT FROM BIOLOGY: the priority is the value of the UPDATE (Mattar & Daw gain x need), NOT raw reward magnitude or current action-value -- Carey et al. 2019 shows replay can be biased AWAY from the currently-most-valuable outcome. MECH-443 is the WHICH/HOW-STRONGLY complement to MECH-319's WHETHER gate; it does not change the binary gate, it grades the admitted writes."
  claim_type: mechanism_hypothesis
  subject: policy.arbitration.priority_weighted_replay_write_selection
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-443@2026-06-20T21:04:53.844967Z"
      as_of: 2026-06-20
      verdict: hold_pending_v3_substrate/applied
  epistemic_category: substrate_ceiling
  implementation_phase: v3
  version_relevance: v3
  v3_pending: true
  registered_utc: "2026-06-19"
  ceiling_decision: deferred
  ceiling_routing_note: "2026-06-19: build-decision deliberately DEFERRED (parked, not orphaned). Convergence-intake (CDQ-005 MuZero/EfficientZero reanalyze) forward refinement of MECH-319 replay write-gating; OFF the V3 critical path. Marker tells the Step-6a-v substrate-ceiling audit the ceiling is intentionally parked; remove to re-route to /failure-autopsy."
  depends_on:
    - MECH-319   # parent: the BINARY simulation-mode rule-write gate this grades (block-vs-admit -> priority-ordered admit)
    - MECH-094   # the hypothesis-tag write-gate principle MECH-319 instantiates; the priority signal refines the same write channel
    - MECH-312   # the rule-arbitration layer whose weight-updates are prioritized
    - ARC-062    # rule-apprehension selector whose minted-rule write-path this gates
  location: docs/architecture/prioritized_replay_write_gating.md#mech-443
  what_would_answer: >
    SUPPORTS if arming a per-transition update-utility (surprise / value-prediction-error / coverage)
    priority weight on the admitted replay writes lifts rule-arbitration retention/selectivity -- measured
    AT rule_state (upstream of the F-dominated E3 commit, in the GAP-K write-gating locus, NOT at committed
    action) -- STRICTLY above a matched-TOTAL-write-mass uniform-admit control (MECH-319 admit_writes=True with
    equal write mass) on >=2/3 seeds. FALSIFIES if priority-weighting is inert versus the matched-mass uniform
    admit, OR if any lift is explained solely by greater total write mass (the matched-mass guard isolates this
    degenerate confound), OR if priority is operationalized as reward magnitude / committed-policy value rather
    than value-of-the-update (Carey et al. 2019: hippocampal replay biases AWAY from the currently-preferred
    outcome, so a reward-keyed priority is biologically falsified). Pre-gate: the readout must be non-degenerate
    -- if admitted replay barely moves rule_state on the current substrate so the metric is pinned across seeds,
    the falsifier is vacuous and MECH-443 re-routes to a replay-write-magnitude substrate enrichment (NOT to
    MECH-439). Any COMMITTED/behavioural variant of this test is MECH-439-conversion-ceiling-gated (V3-EXQ-654g:
    the rule_state channel cannot move the argmax today and the 569i top-k bypass does not reach it) and is
    sequenced behind the 689a F-rebalance.
  notes: >
    Registered 2026-06-19 from Convergence Demand Pipeline row CDQ-005 (MuZero/EfficientZero reanalyze
    intake, REE_convergence/sources/muzero/; promotion packet CPKT-MUZERO-REANALYZE-20260619). Targets
    the mechanism gap named by arc_062_rule_apprehension:GAP-K ("Gating which replayed/imagined
    transitions may write to the rule layer (replay write-gating)"). MECH-319 (V3-EXQ-628 PASS/supports)
    already owns the BINARY gate -- block vs admit during caller_sim=True. The non-duplicative import is
    the GRADED priority OVER the admitted writes: MECH-319 says whether the channel is open; MECH-443
    says which replayed transitions write, and how strongly, when it is. REE LOCUS: ree-v3
    ree_core/regulators/simulation_mode_rule_gate.py (SimulationModeRuleGate) + the LateralPFCAnalog.update
    / GatedPolicy.forward replay call sites -- the existing binary effective_simulation_mode(...) primitive
    would be extended to carry a per-transition priority weight on the admitted write rather than a bool.
    BIOLOGY (lit-pull BEFORE registration, per SD-003 / SD-010-011 discipline -- 2026-06-19,
    evidence/literature/targeted_review_replay_prioritization_mech_319/, 5 sources, SYNTHESIS verdict
    SUPPORTED-with-refinement): hippocampal replay is NOT uniform. Mattar & Daw 2018 Nat Neurosci
    (doi:10.1038/s41593-018-0232-z, supports 0.79) derive replay as prioritized by expected-value-of-backup
    = gain x need, reproducing reward/novelty/goal biases from one ordering rule. Olafsdottir et al. 2015
    eLife (doi:10.7554/eLife.06063, supports 0.63) -- motivational relevance gates which sequences are
    (pre)played. Haga & Fukai 2018 eLife (doi:10.7554/eLife.34171, supports 0.60) -- reward-anchored
    reverse replay selectively GATES which downstream synaptic pathway is strengthened (the write-gating
    step itself). Milstein et al. 2022 J Physiol (doi:10.1113/JP283216, supports 0.58) -- reward-biased
    replay emerges from CA3 network constraints (substrate availability). LOAD-BEARING COUNTERWEIGHT
    (Carey/Tanaka/van der Meer 2019 Nat Neurosci doi:10.1038/s41593-019-0464-6, mixed 0.62): replay can
    be biased AWAY from the currently-preferred outcome -- so priority is update-utility/need, NOT reward
    magnitude; a MECH-443 implementation that equates priority with reward level or committed-policy value
    is biologically falsified. FALSIFIER: arming a per-transition update-utility (surprise/value-error)
    priority weight on the admitted replay writes produces rule-arbitration retention/selectivity (or
    committed-rule entropy) STRICTLY ABOVE a matched uniform-admit control (MECH-319 admit_writes=True with
    equal total write mass) on >=2/3 seeds; if priority-weighting is inert vs uniform admit, OR if it helps
    only by raising total write mass (a degenerate confound -- caught by the matched-mass control), MECH-443
    earns no keep over the binary gate. Pre-registered discriminator: a (binary admit) vs (priority-weighted
    admit) replay-write ablation at the LateralPFCAnalog write locus, with a matched-total-write-mass guard
    and priority = surprise-proxy (NOT reward). V3-leaning (on the V3 work surface; MECH-319 gate + replay
    call sites are built V3 substrate); substrate_ceiling because the existing gate is BINARY -- it cannot
    carry a graded priority signal without enrichment of the write primitive. OFF the V3 critical path --
    blocks no V3 closure node; AMENDS the GAP-K replay-write locus (grades the existing MECH-319 gate)
    rather than minting a parallel module. CANDIDATE only -- promotes nothing, flips no node status;
    decide-whether-to-build is a later governance step. DO NOT build in V3 until routed by experiment.

- id: MECH-444
  title: "staleness_gated_target_refresh_on_replay_write (MuZero/EfficientZero reanalyze analog). The second, more speculative leg of the CDQ-005 reanalyze import, complementary to MECH-443. Asserts that a replayed transition admitted to update the rule-arbitration layer should NOT write its stored (ghost-derived, possibly stale) target verbatim; the target should first be RECOMPUTED against the current model/policy before it updates the rule layer, and the write should be down-weighted or skipped when the recomputed target has not drifted from the stored one (low staleness => low update value). This is the MuZero reanalyze trick (re-run the model on stored trajectories to produce fresh targets) plus EfficientZero off-policy/staleness correction. It guards against the failure MECH-319 was built to prevent (overcommitment to ghost-derived arbitration weights) from re-entering THROUGH the admitted-write channel that MECH-443 opens: prioritized writing of STALE targets would re-introduce drift, so the priority signal (MECH-443) and the freshness signal (MECH-444) must operate together."
  claim_type: mechanism_hypothesis
  subject: policy.arbitration.staleness_gated_target_refresh_on_replay_write
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-444@2026-06-20T21:04:53.907270Z"
      as_of: 2026-06-20
      verdict: hold_pending_v3_substrate/applied
  epistemic_category: substrate_ceiling
  implementation_phase: v3
  version_relevance: v3
  v3_pending: true
  registered_utc: "2026-06-19"
  ceiling_decision: deferred
  ceiling_routing_note: "2026-06-19: build-decision deliberately DEFERRED (parked, not orphaned). Convergence-intake (CDQ-005 MuZero/EfficientZero reanalyze) forward refinement of MECH-319 replay write-gating; OFF the V3 critical path. Marker tells the Step-6a-v substrate-ceiling audit the ceiling is intentionally parked; remove to re-route to /failure-autopsy."
  depends_on:
    - MECH-319   # the binary write gate; MECH-444 refreshes the target on the writes MECH-319 admits
    - MECH-443   # priority sibling: MECH-443 grades WHICH writes, MECH-444 grades the FRESHNESS of each write target -- they compose
    - MECH-094   # the hypothesis-tag write-gate principle; staleness-refresh is a second guard on the same write channel
  location: docs/architecture/prioritized_replay_write_gating.md#mech-444
  what_would_answer: >
    SUPPORTS if recomputing each admitted replay-write target against the CURRENT model before it updates the
    rule layer (and down-weighting / skipping low-drift writes) ATTENUATES rule-arbitration drift / overcommitment
    relative to writing the stored ghost-derived (stale) target -- on the MECH-319 admit_writes=True drift-inducing
    substrate (the V3-EXQ-628 block-vs-admit divergence), on >=2/3 seeds -- i.e. the 628 divergence shrinks under
    refreshed-target admit versus stale-target admit. FALSIFIES if the refresh does not attenuate drift versus
    stale-target writing (the staleness gate is inert), OR if it earns nothing independent of MECH-443's priority
    weighting (the pair is non-separable). LOAD-BEARING CAVEAT (biology_before_formal_definitions): the
    staleness-refresh is supported by ANALOGY only -- Mattar & Daw 2018 gain term + Olafsdottir 2015 generative
    preplay -- with NO recording of a literal recompute-then-write cellular operation; so a null here falsifies the
    REE engineering import, NOT the underlying replay biology, and a divergence between MuZero/EfficientZero
    reanalyze (explicit value-target recomputation in a learned model) and hippocampal replay (gain-weighted,
    generative, no measured target-recompute step) is treated as a real design risk, not a caveat. Not testable
    until MECH-443 is built and keeps AND a target-recompute primitive is scoped (the EMA write path writes targets
    verbatim today).
  notes: >
    Registered 2026-06-19 from Convergence Demand Pipeline row CDQ-005 (MuZero/EfficientZero reanalyze
    intake; promotion packet CPKT-MUZERO-REANALYZE-20260619). The more speculative leg, registered
    separately from MECH-443 because it carries higher transfer risk and is independently falsifiable.
    REE LOCUS: same simulation_mode_rule_gate.py / LateralPFCAnalog.update replay path; the extension is
    a target-recompute-and-compare step on each admitted write rather than a verbatim stored-target write.
    BIOLOGY (same 2026-06-19 lit-pull, evidence/literature/targeted_review_replay_prioritization_mech_319/):
    the staleness-refresh is supported BY ANALOGY, not by a directly-documented cellular operation, hence
    its lower confidence and separate registration. Mattar & Daw 2018 (doi:10.1038/s41593-018-0232-z) gain
    term IS a staleness measure -- a backup has high gain precisely when the current estimate is most
    wrong/outdated, the biological shadow of reanalyze's target-drift weighting. Olafsdottir et al. 2015
    (doi:10.7554/eLife.06063) shows replay is GENERATIVE (constructs sequences through never-traversed
    space) rather than verbatim playback -- offline replay produces goal-current content, not a re-emission
    of the stale stored trace. HONEST CAVEAT (recorded in the entries' mapping_caveat): no recording study
    demonstrates a literal "recompute-the-target-then-write" hippocampal operation; the reanalyze refresh is
    an engineering construct whose biological warrant is the gain term + generative replay, not a measured
    cellular mechanism. FALSIFIER: recomputing the replay write target against the current model before it
    updates the rule layer (and down-weighting low-drift writes) produces LESS rule-arbitration drift /
    overcommitment than writing the stored stale target, on >=2/3 seeds, on the MECH-319 admit_writes=True
    (drift-inducing) substrate -- i.e. the V3-EXQ-628 block-vs-admit divergence is ATTENUATED by the refresh
    relative to admit-with-stale-target; if the refresh does not attenuate drift versus stale-target writing,
    the staleness gate is inert and MECH-444 earns no keep. Pre-registered discriminator: a (stale-target
    admit) vs (refreshed-target admit) replay-write ablation on the 628 drift falsifier substrate. V3-leaning;
    substrate_ceiling because the existing write primitive writes stored targets verbatim and cannot recompute
    /compare without enrichment. OFF the V3 critical path -- blocks no V3 closure node; AMENDS the GAP-K
    replay-write locus jointly with MECH-443. CANDIDATE only -- promotes nothing, flips no node status;
    decide-whether-to-build is a later governance step; the more speculative of the CDQ-005 pair. DO NOT
    build in V3 until routed by experiment.

# ============================================================================
# SD-034 CLOSURE-CONTROL-PLANE de-commit-pipeline decomposition (2026-06-19)
# Children of the SD-034 umbrella. Routed by confirmed failure_autopsy_V3-EXQ-460g
# (7th autopsy); user-approved 2026-06-19T20:38Z. Spec:
# evidence/planning/claim_synthesis_SD-034-closure_2026-06-19.md. The double
# dissociation across 460f/460g (coupling engages w/o sufficient magnitude vs
# magnitude w/ authority but unmeasurable coupling) is the proof these are two
# mechanisms, not one. The S5 measurement property (coupling-measurability-under-
# refractory) is REFUSED on the anti-proliferation rail -> it is the 460h experiment
# fix, not a claim.
# ============================================================================
- id: MECH-445
  title: "Closure->beta coupling engagement: a closure-plane commit (closure fired on a real sequence completion) elevates/binds the MECH-090 bistable latch via the use_closure_commit_beta_coupling path INDEPENDENT of a natural running_variance < commit_threshold commit-entry -- i.e. on seeds where the agent does not naturally commit decisively, the closure still drives a latch elevation the de-commit then acts on. Child A of the SD-034 de-commit-pipeline decomposition (S3)."
  claim_type: mechanism_hypothesis
  subject: governance.closure_beta_coupling
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-732_2026-07-10
      as_of: 2026-07-10
      verdict: non_contributory/precondition_unmet
  epistemic_category: standard
  ceiling_decision: exhausted
  ceiling_routing_note: "GOV-CEIL-1 ceiling-exhaustion demotion 2026-07-09: 6 confirmed substrate_ceiling failure_autopsy hits (460i/460k/460l/715/715a/717) with no positive discrimination on any richer substrate. SD-034 de-commit Child A (closure->beta coupling); the SD-034 umbrella and MECH-090 latch are UNCHANGED -- only this sub-property is demoted. The inert/no-op-mechanism null reading (F-independent commit-intent exists as isolated existence proofs but never clears the 2/3 reliability bar) is carried co-equally. Promotion re-gated on a future positive discrimination on a richer substrate (f_dominance_conversion_ceiling de-commit-release face)."
  implementation_phase: v3
  version_relevance: v3
  v3_pending: true
  pending_retest_after_substrate: true  # RESTORED /governance 2026-08-12 (same cycle, self-correction): the earlier clear was WRONG. It read f_dominance_conversion_ceiling as exhausted from that entry's status STRING, which embeds the 2026-07-05 arbitration-route exhaustion -- the exact verdict WITHDRAWN 2026-07-20 by failure_autopsy_V3-EXQ-711-713_2026-07-20. The entry itself is node_class: complex (probe-gated), status_phase: probe_gated, pending_retest_after_substrate: true -- a live probe-gated route, not a dead one. Retest stands.
  registered_utc: "2026-06-19"
  what_would_answer: >
    REGIME-SCOPED to WEAK-natural-commit seeds (OFF-arm committed_steps ~ 0, where the closure
    coupling is load-bearing rather than redundant with the F-driven natural commit). SUPPORTED if,
    on the foraging-competent 603n substrate with the beta-coupling amend ON, a refractory-independent
    commit-intent counter (closure-plane e3._committed_trajectory forming while not result.committed,
    counted BEFORE the elevate/refractory gate) is > 0 on >= 2/3 of the in-regime (weak-natural-commit)
    seeds. FALSIFIED if, among those weak-natural-commit seeds, the closure-plane commit only ever
    co-occurs with a natural result.committed (coupling inert -- adds no elevation the natural path would
    not have produced) on >= 2/3 of them. STRONG-natural-commit seeds (high OFF-arm committed_steps) are
    OUT OF SCOPE: a commit-intent=0 there is redundancy with the natural commit, NOT inert coupling, and
    does NOT count toward falsification. [REGIME-SCOPED 2026-06-20 per confirmed
    failure_autopsy_V3-EXQ-460h (user-confirmed): the prior unscoped clause mis-read the 460h seeds 42/43
    commit-intent=0 -- which are strong-natural-commit redundancy -- as inert coupling on 2/3.]
  evidence_quality_note: |
    GOV-CEIL-1 CEILING-EXHAUSTION DEMOTION 2026-07-09: 6 confirmed substrate_ceiling failure_autopsy
    hits (460i/460k/460l/715/715a/717) with no positive discrimination on any richer substrate ->
    demoted to contested candidate (epistemic_category standard, ceiling_decision exhausted). SD-034
    de-commit Child A: the SD-034 umbrella + MECH-090 latch are UNCHANGED; only this sub-property is
    demoted. The competing NULL reading -- the closure->beta coupling is inert / does no work (F-independent
    commit-intent appears only as isolated per-seed existence proofs, never clearing the 2/3 reliability
    bar) -- is now carried CO-EQUALLY. NOT falsification: the claim keeps its full history; promotion
    re-gated on a future positive discrimination on a richer substrate (f_dominance de-commit-release face).

    V3-EXQ-717 (confirmed failure_autopsy_MECH-445-cluster-715a-717_2026-07-07; M2 test-power decouple,
    12 seeds) -> WEAKENS (NARROW, reliability-scoped). Fair test: readiness cleared, 9 in-regime seeds,
    opportunity 1.0, counter placed before the refractory gate. Commit-intent > 0 on 2/9 in-regime seeds
    (46=222, 52=195) < 2/3 -> the F-independent commit-intent EXISTS (this + prior 715 seed-44=398 are
    existence proofs) but does NOT clear the 2/3 RELIABILITY bar within the weak-natural-commit regime.
    Weakens the RELIABILITY/PREVALENCE reading ONLY; the existence claim stands. Status UNCHANGED
    (candidate / v3_pending); does NOT flip to weakened-overall. Paired with V3-EXQ-715a (M1,
    non_contributory): the selection-face lift engaged 3/3 but arming stayed 1/3 (moderate_f_delta -63),
    so moderating F does NOT rescue arming -- a DISTINCT de-commit-release lever is needed. 7th
    substrate-ceiling reading (715a); RE-DERIVE BRAKE FIRED; same-claim de-commit-MAGNITUDE re-queue
    REFUSED. Reliability-gap release condition: f_dominance_conversion_ceiling de-commit-release face
    (F-independent arming surviving moderate F).
    ---
    V3-EXQ-715 (confirmed failure_autopsy_V3-EXQ-715_2026-07-06, supersedes 460l) -> non_contributory +
    pending_retest_after_substrate (status UNCHANGED: candidate / v3_pending). 6th substrate-blocked iteration;
    RE-DERIVE BRAKE remains FIRED (threshold 2). SUBSTRATE ADVANCE: use_closure_commit_entry (460o/p PASS) RESOLVED
    the 460h/k structural-unreachability -- MECH-445's refractory-independent commit-intent counter is NO LONGER
    pinned at 0 by construction. NARROW NON-SCORING POSITIVE (existence proof, 1/3): seed 44
    sd034_n_closure_commit_intent=398 with n_f_commits=7 and ncl_hold_closure_armed_total=398 (F-INDEPENDENT arming
    confirmed). Self-routed substrate_not_ready_requeue at gate (a) 1/3 (< 2/3): arming is REGIME-SCOPED -- fires only
    on the weak-natural-commit seed (44); strong-natural-commit seeds 42/43 never arm in the ON arm (residue of the
    F-dominance conversion ceiling MECH-439). NOT a falsification. epistemic_category stays substrate_ceiling but the
    release condition MOVED: arming that survives moderate F (substrate_queue f_dominance_conversion_ceiling amend,
    de-commit-release face). Same-claim de-commit-falsifier re-queue REFUSED (brake). Granularity-debt recurrence
    (6 iterations, shifting signature) -> /claim-synthesis referral RESOLVED 2026-07-06 = STOP/refuse-to-decompose
    (claim_synthesis_MECH-445-446_2026-07-06.md; the commit-INTENT-existence vs de-commit-MAGNITUDE split is already
    realized by the 2026-06-19 decomposition). FORWARD: V3-EXQ-717 (M2, MECH-445-ONLY commit-intent regime-scoped
    falsifier decoupled from MECH-446; test-power redesign, no new substrate) and V3-EXQ-715a (M1, selection-face
    lift) QUEUED.
    ---
    V3-EXQ-460k (confirmed failure_autopsy_V3-EXQ-460k_2026-06-22, supersedes 460j) -> non_contributory +
    pending_retest_after_substrate (status UNCHANGED: candidate / v3_pending). epistemic_category -> substrate_ceiling
    (user-confirmed): 4th substrate-blocked iteration (460h/i/j/k); RE-DERIVE BRAKE FIRED (threshold 2). The
    closure_exclusive_decommit_eval build (ree-v3 e52158d) moved the latch-hold arm SOURCE to _closure_commit_active,
    but that is gated on e3._committed_trajectory, set ONLY by the F-driven natural commit (e3_selector.py:1926) -> the
    natural-commit / closure-de-commit dissociation is ILLUSORY; gate-2.5 closure_exclusive_eval_armed_hold measured 0/3
    (ncl_hold_closure_armed_total==0 all arms/seeds). The build's green activation test is a false-positive (the contract
    harness injects e3._committed_trajectory directly). MECH-445's own what_would_answer predicate (closure-plane
    e3._committed_trajectory forming WHILE NOT result.committed) is structurally unreachable on this substrate -> the
    commit-intent counter is pinned at 0 by construction. Release condition: a path where the SD-034 closure /
    env-completion plane forms a committed-trajectory-equivalent sustained occupancy INDEPENDENT of the F-driven commit
    (substrate_queue f_dominance_conversion_ceiling amend). Same-claim de-commit-falsifier re-queue REFUSED (brake).
    V3-EXQ-460l (ARC-108 JOB-2, different mechanism, brake-exempt) inherited the same unmet eval-arm precondition.
    V3-EXQ-460h (refractory-independent commit-intent retest; confirmed failure_autopsy_V3-EXQ-460h_2026-06-20,
    supersedes 460g) -> non_contributory + pending_retest_after_substrate (status UNCHANGED: candidate / v3_pending).
    NARROW NON-SCORING POSITIVE (existence proof, 1/3): seed 44 reported sd034_n_closure_commit_intent=375 with
    OFF-arm committed_steps=0 and ON total_beta_elevated=355 (OFF=0) -- the closure coupling was the SOLE commit
    driver, decoupled from the MECH-446 de-commit-magnitude lever (460g's coupled-elevation counter collapsed
    36->0 on the same lever; the refractory-independent commit-intent fix is CONFIRMED WORKING, closing the 460g
    S5 self-defeat). The run self-routed substrate_not_ready_requeue before C2 (MECH-446) was scorable: the
    commit-intent (MECH-445) and within-arm-window (MECH-446) non-vacuity certifiers fire on DISJOINT seeds
    (commit-intent only where natural commit is WEAK; window only where it is STRONG), empty intersection. NOT a
    falsification (the 2/3 commit-intent=0 seeds 42/43 are strong-natural-commit redundancy, out of regime -- see
    the regime-scoped what_would_answer). CLUSTER READING (user-directed): the closure->beta coupling is the
    engagement face of the SD-034 de-commit, itself the COMMIT/RELEASE-DURATION FACE of the F-dominance conversion
    ceiling (MECH-439, F ~88-89% E3 variance V3-EXQ-571), NOT orthogonal. Retest gated on the F-dominance
    commit-entry-decisiveness lever (460i; substrate_queue f_dominance_conversion_ceiling), NOT a re-queue on the
    current selector.

    [enum-comment migrated to field 2026-07-18] epistemic_category: GOV-CEIL-1 demotion
    2026-07-09 (was substrate_ceiling; prior 2026-06-22 V3-EXQ-460k autopsy)
  depends_on:
    - SD-034    # umbrella design_decision this child decomposes
    - MECH-090  # the bistable latch the closure-plane commit engages
    - SD-033a   # the rule_state the closure reads
  location: docs/architecture/sd_034_governance_closure_operator.md#children-de-commit-pipeline-decomposition-2026-06-19
  notes: >
    Registered 2026-06-19 from claim-synthesis decomposition of the SD-034 de-commit-authority
    sub-property (spec evidence/planning/claim_synthesis_SD-034-closure_2026-06-19.md, Section 5.2
    Child A; user-approved 2026-06-19T20:38Z). LIT ANCHOR (existing SD-034 pull
    evidence/literature/targeted_review_sd_034/): Collins & Frank 2014 (OPAL D1/D2 task-set
    disengagement -- completion drives the committed program OFF); Smith & Graybiel 2013 (the
    "stop" bracket terminates the action sequence). CLUSTER EVIDENCE IT EXPLAINS: 460e
    (commit-without-beta dissociation, beta engaged 1/3); 460f (coupling fired 36/52 seed 42 but
    0/0 on 43/44 -- engagement is seed-conditional). The seed-42 coupling (460f) is partial
    positive support; the 43/44 inertness is the open failure. WHY DISTINCT FROM MECH-446:
    coupling can engage with insufficient magnitude (460f seed 42, 36 coupled elevations yet
    swamped). The what_would_answer uses a REFRACTORY-INDEPENDENT commit-intent gate so the
    de-commit magnitude lever (MECH-446) does not zero this child's certifier (the S5 entanglement
    that the refused measurement item, routed to 460h, exists to break). CANDIDATE only -- promotes
    nothing; tested by the gated 460h re-queue (targets MECH-445/446, NOT the coarse SD-034 umbrella).
- id: MECH-446
  title: "De-commit-authority magnitude: the closure-coupled de-commit lowers post-closure latch occupancy with authority magnitude sufficient to overcome the natural-commit occupancy it competes against -- the committed-run-scaled refractory drives a within-arm around-closure occupancy drop (pre-closure vs post-closure window on the ON arm), scaling with committed-run length so a long committed run triggers a proportionally long de-commit. Child B of the SD-034 de-commit-pipeline decomposition (S4)."
  claim_type: mechanism_hypothesis
  subject: governance.closure_decommit_magnitude
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-732_2026-07-10
      as_of: 2026-07-10
      verdict: non_contributory/precondition_unmet
  epistemic_category: standard
  ceiling_decision: exhausted
  ceiling_routing_note: "GOV-CEIL-1 ceiling-exhaustion demotion 2026-07-09: 5 confirmed substrate_ceiling failure_autopsy hits (460i/460k/460l/715/715a) with no positive discrimination on any richer substrate. SD-034 de-commit Child B (de-commit magnitude); the SD-034 umbrella and MECH-090 latch are UNCHANGED -- only this sub-property is demoted. The inert/no-op-mechanism null reading (the within-arm around-closure occupancy-drop DV is repeatedly starved/vacuous, never scored above threshold) is carried co-equally. Promotion re-gated on a future positive discrimination on a richer substrate (f_dominance_conversion_ceiling de-commit-release face)."
  implementation_phase: v3
  version_relevance: v3
  v3_pending: true
  pending_retest_after_substrate: true  # RESTORED /governance 2026-08-12 (same cycle, self-correction): the earlier clear was WRONG. It read f_dominance_conversion_ceiling as exhausted from that entry's status STRING, which embeds the 2026-07-05 arbitration-route exhaustion -- the exact verdict WITHDRAWN 2026-07-20 by failure_autopsy_V3-EXQ-711-713_2026-07-20. The entry itself is node_class: complex (probe-gated), status_phase: probe_gated, pending_retest_after_substrate: true -- a live probe-gated route, not a dead one. Retest stands.
  registered_utc: "2026-06-19"
  what_would_answer: >
    On the amended substrate (committed-run-scaled refractory ON), the ON-arm within-arm
    post-closure latch occupancy is below the pre-closure occupancy by >= DECOMMIT_MIN_DROP_FRAC
    on >= 2/3 seeds, measured on a refractory-independent coupling gate (so the lever does not
    zero its own certifier). Falsified if a fairly-coupled, refractory-independent-gated run shows
    no within-arm post-closure drop (the de-commit fires with correct sign but sub-threshold
    authority) on >= 2/3 seeds.
  evidence_quality_note: |
    GOV-CEIL-1 CEILING-EXHAUSTION DEMOTION 2026-07-09: 5 confirmed substrate_ceiling failure_autopsy
    hits (460i/460k/460l/715/715a) with no positive discrimination on any richer substrate -> demoted
    to contested candidate (epistemic_category standard, ceiling_decision exhausted). SD-034 de-commit
    Child B: the SD-034 umbrella + MECH-090 latch are UNCHANGED; only this sub-property is demoted. The
    competing NULL reading -- the de-commit-magnitude mechanism is inert / does no work (the within-arm
    around-closure occupancy-drop DV is repeatedly starved/vacuous, never scored above threshold on a
    coupling-certified seed) -- is now carried CO-EQUALLY. NOT falsification: the claim keeps its full
    history; promotion re-gated on a future positive discrimination on a richer substrate.

    V3-EXQ-715a (confirmed failure_autopsy_MECH-445-cluster-715a-717_2026-07-07; M1 selection-face lift)
    -> non_contributory + pending_retest_after_substrate (status UNCHANGED: candidate / v3_pending).
    Self-routed substrate_not_ready_requeue (lift_engaged_but_arming_still_regime_scoped): the within-arm
    around-closure occupancy window was VACUOUS on all ON-arm seeds -- arming (seed 42) and the only
    non-vacuous window (seed 44, pre 1.0 -> post 0.2) on disjoint seeds. The selection-face lift did NOT
    co-locate the closure fire with a sustained latch-armed occupancy. NOT a falsification, NOT scorable
    -- DV starved. 7th substrate-blocked iteration; RE-DERIVE BRAKE FIRED; same-claim re-queue REFUSED.
    Release condition: a de-commit-release lever where the SD-034 closure fire co-locates with a
    sustained latch-armed occupancy across >= 2/3 seeds. Moderate-F selection-face lift RULED OUT
    (moderate_f_delta -63).
    ---
    V3-EXQ-715 (confirmed failure_autopsy_V3-EXQ-715_2026-07-06, supersedes 460l) -> non_contributory +
    pending_retest_after_substrate (status UNCHANGED: candidate / v3_pending). 6th substrate-blocked iteration;
    RE-DERIVE BRAKE remains FIRED (threshold 2). The within-arm around-closure occupancy-drop DV was VACUOUS on the ON
    arm for all 3 seeds (n_window_events=0), INCLUDING the seed where MECH-445 armed (seed 44: 398 commit-intents but
    the SD-034 closure fires and the latch-armed beta occupancy are TEMPORALLY DISJOINT within-episode ->
    mean_pre_closure_occ=0). The 460h cross-seed disjoint-certifier problem has MIGRATED to WITHIN-seed temporal
    disjointness on the F-independent substrate; use_closure_commit_entry did NOT dissolve it. NOT a falsification, NOT
    scorable -- the MECH-446 DV is starved, not falsified. (Verified not an instrumentation artifact: beta_history /
    fire_ticks reset per episode and align; the OFF arm formed real windows.) Release condition: a regime where the
    SD-034 closure fire co-locates with a sustained latch-armed occupancy to de-commit from (substrate_queue
    f_dominance_conversion_ceiling amend). Same-claim re-queue REFUSED (brake). /claim-synthesis referral RESOLVED
    2026-07-06 = STOP (claim_synthesis_MECH-445-446_2026-07-06.md): the within-seed certifier disjointness is the
    granularity signal that MECH-446 (co-located de-commit MAGNITUDE) and MECH-445 (F-independent commit INTENT) are
    separate claims no single regime co-satisfies -- already realized by the 2026-06-19 decomposition, so NOT re-carved.
    The MECH-446 magnitude face stays blocked on the f_dominance_conversion_ceiling build.
    ---
    V3-EXQ-460k (confirmed failure_autopsy_V3-EXQ-460k_2026-06-22, supersedes 460j) -> non_contributory +
    pending_retest_after_substrate (status UNCHANGED: candidate / v3_pending). epistemic_category -> substrate_ceiling
    (user-confirmed): 4th substrate-blocked iteration (460h/i/j/k); RE-DERIVE BRAKE FIRED (threshold 2). The
    closure_exclusive_decommit_eval build's "closure-exclusive" latch-hold arm source _closure_commit_active is gated on
    e3._committed_trajectory, set ONLY by the F-driven natural commit (e3_selector.py:1926) -> the dissociation is
    illusory and the gate-2.5 armed-hold measured 0/3, so the OFF baseline never sustained an occupancy and the
    within-arm de-commit DV (MECH-446) never ran. NOT a falsification, NOT scorable. Release condition: a closure-coupled
    sustained occupancy INDEPENDENT of the F-driven commit (substrate_queue f_dominance_conversion_ceiling amend).
    Same-claim de-commit-falsifier re-queue REFUSED (brake).
    V3-EXQ-460h (confirmed failure_autopsy_V3-EXQ-460h_2026-06-20, supersedes 460g) -> non_contributory +
    pending_retest_after_substrate (status UNCHANGED: candidate / v3_pending). NARROW NON-SCORING POSITIVE
    (existence proof, 1/3): seed 42 within-arm around-closure occupancy dropped 0.333 -> 0.0 (C2 PASS) --
    correct de-commit sign + magnitude. BUT seed 42 is a coupling-UNCERTIFIED seed (commit-intent=0, strong
    natural commit OFF committed_steps=2414), so the drop cannot be ATTRIBUTED to the closure-coupled de-commit
    (it could be the natural-commit release). C2 was never scorable on a MECH-445-coupling-certified seed: the
    MECH-445 (commit-intent) and MECH-446 (within-arm-window) non-vacuity certifiers fire on DISJOINT seeds
    (anti-correlated by construction -- coupling engages where natural commit is weak; rich windows where it is
    strong), empty intersection. NOT a falsification, NOT a substrate ceiling (the substrate expresses the
    de-commit on seed 42). CLUSTER READING (user-directed): the de-commit-authority magnitude is the
    COMMIT/RELEASE-DURATION FACE of the F-dominance conversion ceiling (MECH-439) -- a modulatory channel
    subdominant to the same F-driven natural commit (~2400-2600 committed steps) that monopolises the candidate
    argmax. Retest (460i) gated on the F-dominance commit-entry-decisiveness / natural-commit-latch-occupancy
    lever (substrate_queue f_dominance_conversion_ceiling), which makes weak-natural-commit the norm across seeds
    so coupling-certification and the de-commit window co-occur -- NOT a re-queue on the current selector.

    [enum-comment migrated to field 2026-07-18] epistemic_category: GOV-CEIL-1 demotion
    2026-07-09 (was substrate_ceiling; prior 2026-06-22 V3-EXQ-460k autopsy)
  depends_on:
    - SD-034    # umbrella design_decision this child decomposes
    - MECH-090  # the latch occupancy the de-commit lowers
    - MECH-445  # coupling must engage for the magnitude to act on a closure-driven elevation
    - MECH-342  # maintenance-release sibling -- the OTHER release pathway; cross-ref to keep the boundary clean
  location: docs/architecture/sd_034_governance_closure_operator.md#children-de-commit-pipeline-decomposition-2026-06-19
  notes: >
    Registered 2026-06-19 from claim-synthesis decomposition of the SD-034 de-commit-authority
    sub-property (spec evidence/planning/claim_synthesis_SD-034-closure_2026-06-19.md, Section 5.2
    Child B; user-approved 2026-06-19T20:38Z). LIT ANCHOR: Mayr & Keele 2000 (backward inhibition:
    post-completion refractory whose STRENGTH competes with re-activation of the just-abandoned set)
    -- adequate grounding in the existing SD-034 pull; Cavanagh & Frank 2011 (STN conflict-graded
    "hold-your-horses" release -- the sharpest anchor for the "magnitude scales with committed-run
    length" form) recommended as a targeted single-entry addition to
    evidence/literature/targeted_review_sd_034/entries/ (non-blocking; the magnitude form is already
    grounded by Mayr & Keele). CLUSTER EVIDENCE IT EXPLAINS: 460f (5-tick refractory swamped by
    ~530-560 elevated steps -- correct sign, sub-threshold magnitude); 468e (release fires C1 3/3 but
    committed_frac pinned at 1.0 -- sub-threshold authority via an independent DV); 460g (committed-run-
    scaled refractory HAS authority on seed 42, within-arm 0.333 -> 0.0 -- the existence proof). WHY
    DISTINCT FROM MECH-445: magnitude can have authority with unmeasurable coupling (460g seed 42
    dissociation). CANDIDATE only -- promotes nothing; tested by the gated 460h re-queue (targets
    MECH-445/446, NOT the coarse SD-034 umbrella).

- id: MECH-447
  title: "Conflict-graded near-tie lever sufficiency: a hyperdirect-STN-like conflict-graded shortlist / commit-temperature lever (k = f(top-F gap); T = f(top-F gap)) is SUFFICIENT to lift committed-action-class entropy strict-above both collapsed-proposer and gap-blind controls -- gap-CONCENTRATED (the lift correlates with per-tick top-F gap) -- WITHOUT reducing F's E3 variance share. I.e. F can keep monopolising selection variance and conversion is still recovered by widening/hotting only at near-ties."
  claim_type: mechanism_hypothesis
  subject: selection.conflict_graded_near_tie_sufficiency
  polarity: asserts
  status: superseded
  live_status:
    reading: superseded
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-447@2026-06-20T18:57:41.815630Z"
      as_of: 2026-06-20
      verdict: supersede/applied
  superseded_by: [MECH-448, MECH-449]
  superseded_utc: 2026-06-20
  supersession_note: >
    Superseded 2026-06-20 (weaken-now, user-adjudicated) by the constitutional path -- MECH-448 (lead:
    rank-preserving F->eligibility demotion) + MECH-449 (Go/No-Go eligibility constitution), under the
    ARC-107 basal-ganglia selector-constitution umbrella. Three reasons:
    (1) REFUTED BY ITS OWN PRE-REGISTERED FALSIFIER. V3-EXQ-689a tested the as-written claim (combined
    conflict-graded shortlist-width k=f(top-F gap) AND commit-temperature T=f(top-F gap) lever is
    SUFFICIENT). The pre-registered PRIMARY arm ARM_A1B1 (both levers) returned committed-action-class
    entropy 0.387 = the A0B0 top-k baseline -- 0/3 seeds strict-above BOTH the collapsed-proposer AND the
    gap-blind controls (FIXED_KMAX 0.546 / FIXED_HOT_T 0.591), at READINESS MET (route_range 0.624,
    e2-pairwise-spread 0.187, grading levers engaged k 3/3 AND t_eff 3/3) and non_degenerate -- a genuine
    readiness-met no-lift verdict, not a self-route.
    (2) 2x2 DISSOCIATION (the load-bearing structural finding). Factor A (graded shortlist width /
    STN-hyperdirect-hold analog) alone is INERT (ARM_A1B0 0.440, 0/3); Factor B (gap-scaled commit-T /
    pallidal-disinhibition-gain analog) alone is the only converter (ARM_A0B1 0.850, 2/3 strict-above both
    control sets, but seed-fragile -- held 0.569 not collapse on the seed-43 monostrategy seed); and
    A x B is DESTRUCTIVE (A1B1 cancels B's lift back to baseline). The two bundled sub-levers are
    dissociable with a destructive interaction, so the as-written single-"lever" SUFFICIENCY framing does
    not hold; the biologically-faithful translation is one permission-to-commit constitution, not two
    stacked near-tie patches.
    (3) GOVERNING STEER. The surviving Factor-B-alone parametric effect is recorded as NO-AUTHORITY
    divergence-ledger data only and earns NO rescue child, per the MECH-442 Section-7 biological-fidelity /
    anti-shortcut steer (2026-06-20, ARC-106 anti-shortcut corollary): cheap parametric near-tie levers are
    not pursued as a substitute for -- or a reason to skip -- the biologically-faithful BG-constitution
    build. V3-EXQ-689c (the Factor-B-alone gap-scaled commit-T isolation retest) was STRIPPED of decision
    authority -- confirmatory data only; its outcome does NOT gate this disposition, and the user chose
    weaken-now without waiting for it. The lever CODE (e3_selector.py MECH-439 Factor A/B) stays in place;
    this supersession is at the claim/reading level (parametric-sufficiency reading replaced by the
    constitutional reading), not a code removal. Re-grain analysis:
    evidence/planning/claim_synthesis_regrain_MECH-447-448-449_2026-06-20.md (Section 6.1 689c-gated map;
    resolved weaken-now per the s7 steer).
  epistemic_category: standard
  implementation_phase: v3
  version_relevance: v3
  registered_utc: "2026-06-20"
  depends_on:
    - MECH-439   # the F-dominance ceiling this lever circumvents (lever = MECH-439 Factor A/B, already built)
    - ARC-107    # the BG-constitution umbrella (this is the minimal hyperdirect-only leg)
    - Q-078      # constitutional-vs-parametric umbrella; this is the PARAMETRIC-sufficient reading
  what_would_answer: >
    PRE-REGISTERED FALSIFIER = V3-EXQ-689a (2x2 conflict-graded shortlist x commit-T). SUPPORTS if
    committed_action_class_entropy is strict-above BOTH collapsed-proposer AND matched-noise/gap-blind
    controls on >=2/3 seeds AND the lift is gap-CONCENTRATED (regress committed entropy on per-tick top-F
    gap -> positive slope) WITHOUT F's E3 variance share dropping. WEAKENED if the lift is uniform
    (gap-blind controls match -> the load-bearing factor is not conflict grading; route to MECH-448/449)
    or absent at readiness. NON-DEGENERACY (else substrate_not_ready_requeue): the eligible set is
    genuinely diverse (candidate-pool pairwise_dist supra-floor) and k AND T actually vary across ticks --
    a vacuous top-k over a class-uniform pool self-routes substrate_not_ready_requeue, never a verdict.
  location: evidence/planning/claim_synthesis_MECH-439_2026-06-20.md
  notes: >
    = Child 2 (lever-sufficiency) of the deferred claim_synthesis_MECH-439_2026-06-20 decomposition, and
    "MECH-439b" of the basal-ganglia-selector-constitution note (2026-06-20). Registered now AHEAD of the
    proposal's 689a trigger per the user capture-all-with-gates directive 2026-06-20; the per-child
    DISPOSITION (which of MECH-447/448 the 689a manifest supports) remains 689a-gated per that proposal's
    Section-7 trigger map. Discriminated FROM MECH-448 (residual-ceiling) by exactly 689a's
    F-gap-correlation leg: gap-concentrated lift -> MECH-447, uniform/no lift -> MECH-448. The lever is
    ALREADY BUILT (e3_selector.py MECH-439 Factor A conflict-graded shortlist width + Factor B gap-scaled
    commit-T); 689a is the validation, not a build.
    DISPOSITION SETTLED 2026-06-20 (no longer 689a/689c-gated): status -> superseded (weaken-now) -- 689a
    refuted the as-written both-levers sufficiency form; see supersession_note.
- id: MECH-448
  title: "Residual conversion ceiling requiring rank-preserving F->eligibility demotion: near-tie levers (conflict-graded shortlist / commit-T, MECH-447) CAP committed-action-class entropy BELOW the proposer ceiling because they act only at near-ties while F still decides every non-tie; only REMOVING F from the final committed argmin -- demoting it to a graded ELIGIBILITY envelope (rank-preserving renormalisation against the competing field; divisive-normalisation analog) so a modulatory/diversity channel arbitrates within the F-eligible set -- lifts committed diversity to the proposer ceiling without disinhibiting harmful classes."
  claim_type: mechanism_hypothesis
  subject: selection.rank_preserving_f_to_eligibility_demotion
  polarity: asserts
  status: candidate
  live_status:
    reading: provisional
    as_of: 2026-07-24
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-689i_2026-07-24
      as_of: 2026-07-24
      verdict: supports/measurement_test_design_defect
  epistemic_category: standard
  implementation_phase: v3
  version_relevance: v3
  registered_utc: "2026-06-20"
  evidence_quality_note: |
    V3-EXQ-689d (2026-06-21) PASS -- the rank-preserving F->eligibility demotion falsifier, the
    constitutional LEAD lever (689a-successor). All 4 load-bearing criteria passed on the GAP-A
    foraging substrate: C_READINESS (the envelope actually EXCLUDES on the divergent e2_world_forward
    pool, mean excluded_count 0.152 > 0, demotion active on 1.0 of P1 ticks); C_RANK_PRESERVING
    (eligible set is an F-rank PREFIX, frac 1.0 on 3/3 seeds -- order preserved on the numerators,
    no global F-flatten); C_PRIMARY (committed-action-class entropy 0.938 STRICT-ABOVE both collapsed
    controls AND reaches the proposer ceiling 0.82); C_SAFETY (ON harm-per-tick 0.049 <= OFF 0.055 --
    no harmful-class disinhibition). The graded divisive-normalisation envelope ~doubles committed
    diversity over the ARM_OFF hard env-conditional top-k (569i) lever (0.938 vs 0.371). exp_conf 0.775,
    lit_conf 0.857, quadrant confirmed_established, 0 conflict (6 supports / 0 weakens).
    PROMOTED candidate->provisional 2026-06-21 (governance): the implementation_phase:v3 hold
    auto-released once a V3 run existed (the gate keys on v3_run_count==0); the lever is implemented
    (e3_selector.py, ree-v3 main 4c9b3c9) and now experimentally confirmed -- first experimental
    confirmation of the ARC-107 BG-selector constitution. CAVEAT (informational, non-gating): the
    ARM_MATCHED_NOISE control was non-lifting (matched_noise_verified_lifting=false), so that one
    noise comparison was soft -- but ARM_ON clears the proposer ceiling directly, so the PASS stands.
    Single genuine experiment (strong, well-controlled); a second independent confirmation would
    further harden the result.

    RE-ADJUDICATED 2026-07-20 (failure_autopsy_V3-EXQ-699_2026-07-20), following the
    stale-E3-diagnostics corpus audit and the V3-EXQ-708 precedent. WHAT STANDS: the PASS. The
    single load-bearing criterion is C_READY (levers engaged and substrate exercisable), and all
    seven readiness preconditions survive the defect on threshold-invariance grounds -- the
    C1e/C1f non-vacuity floors are literally 0.0 (measured 14.81 excluded-count and 0.69
    suppressed-per-tick), and a strictly-positive test cannot be manufactured from an all-zero
    record; the active-frac and frac_pre_ge2 gates read EXACTLY 1.0, which given the per-select
    reassignment of last_score_diagnostics (e3_selector.py:2452, demotion key pre-seeded False
    at :2511; go_nogo keys set at :1292 from a gate invoked at :2878, i.e. after the
    reassignment) requires the gates to have fired on every E3 tick. Both levers demonstrably
    did real work. WHAT DOES NOT STAND: the levers_compound composition finding, which is
    WITHDRAWN. The primary DV committed_class_entropy_nats is accumulated at driver line 882/899
    from int(action[0].argmax()) on the action RETURNED by agent.select_action, once per ENV
    STEP; on a non-E3 tick agent.py:5430 returns the HELD action before e3.select() is reached.
    The DV is therefore hold-duration-weighted OCCUPANCY entropy, whereas the driver's own
    construct (lines 88-97, 'the levers move WHICH CLASS is committed') and both claims act at
    SELECTION. This is NOT the exposure validate_experiments lints (that is the separate
    last_score_diagnostics read at line 929) and was unflagged. The weighting is arm-dependent
    and, critically, ALIGNED WITH THE HYPOTHESIS: ARM_OFF is the reference for every delta, and
    an unarmed agent perseverating longer has its occupancy entropy systematically depressed,
    inflating every d_* including d_both. The verdict is flippable on a plausible correction,
    unlike 708's: seed 42 is already neutral, and killing either remaining compound seed yields
    NEUTRAL. Because a common shift in e_off leaves the max-rule slack invariant, the binding
    constraint is the absolute floor d_both >= 0.05, requiring e_off to be under-estimated by
    only 0.1147 nats (seed 44) or 0.1343 nats (seed 43) against 0.1889 and 0.1820 nats of
    available headroom to the 3-class and 2-class entropy ceilings; e_both on seed 43 is already
    at its effective two-class ceiling and can only fall. The corrected sign is therefore
    UNKNOWN -- this is a withdrawal, not a reversal, and levers_compound must not be restated in
    either direction. Two further defects recorded: selected_class_entropy_nats equals
    committed_class_entropy_nats to 6dp on all 12 arm-seeds (the line-913
    _last_selected_trajectory readout carries zero independent information, so the design's
    internal cross-check is vacuous), and pre_e3_classes at line 856 is likewise a per-selection
    quantity read per env step because candidate regeneration is itself e3_tick-gated
    (agent.py:4812). NOT RECOVERABLE: no custom_information, no per-tick sink, metrics.json is
    53 chars, ran on ree-cloud-1 (Phase 3 transports manifest_bytes only -- thrice-confirmed
    with 785 and 708); the replication factor cannot even be estimated from the record, since
    n_p2_ticks counts env steps and varies with episode termination. Neither MECH-448 nor
    MECH-449 loses support: both rest on their own validation gates (689d and 689g
    respectively), this run is experiment_purpose=diagnostic, promotes nothing, and its
    evidence_direction was already non_contributory on both claims -- so no epistemic_category
    is cleared and no confidence moves. Routes to queue-experiment for a corrected V3-EXQ-699b
    using the clear-before-select pattern of v3_exq_785a. SEE ALSO: the in-flight V3-EXQ-699a is
    a FAITHFUL re-run of this driver and will reproduce every defect above.

    FORWARD POINTER: the corrected re-run is queued as V3-EXQ-699b (ree-v3 2d3a249e01 on
    origin/main, experiments/v3_exq_699b_pcomp_demotion_x_gonogo_fresh_select.py, live in the
    coordinator DB). Its result -- not V3-EXQ-699's -- is the first valid measurement of this
    composition. Until it lands, "the levers compound" and "the levers are safe to co-arm" must
    not be restated in EITHER direction: the corrected sign is UNKNOWN, and V3-EXQ-699's own
    CANCEL branch was never ruled out by an uncontaminated measurement either.

    RECONCILIATION NOTE (2026-07-20, same day): the sentence above -- that MECH-448 loses
    nothing because it rests on V3-EXQ-689d -- was written before
    failure_autopsy_V3-EXQ-689d_2026-07-20 (REE_assembly 2e6cc2569b) landed. That autopsy
    WITHDRAWS the V3-EXQ-689d conversion finding itself (C_PRIMARY is invalid on two surviving
    independent grounds -- a hold-weighted DV, and a vacuous ARM_MATCHED_NOISE control
    bit-identical to ARM_PROPOSER_CTRL). A third ground asserted by that autopsy, intra-run
    substrate divergence, was REFUTED and WITHDRAWN the same day -- see the D3 CORRECTION below. The V3-EXQ-699 re-adjudication's reassurance about this claim's evidential
    base therefore does NOT survive contact with the V3-EXQ-689d re-adjudication, and must not
    be read as confirming that MECH-448's conversion evidence is intact. The 689d withdrawal's own claim-level
    writes were deferred by this entry; they are APPLIED below (2026-07-20, session
    hopeful-bell-492021) and this paragraph is superseded to that extent.

    689D WITHDRAWAL APPLIED 2026-07-20 (session hopeful-bell-492021), per
    failure_autopsy_V3-EXQ-689d_2026-07-20 (REE_assembly 2e6cc2569b). status provisional ->
    candidate. The 2026-06-21 promotion (59c8a1879a) rested on the V3-EXQ-689d PASS, which is now
    withdrawn; leaving the claim at provisional would present it as an experimentally-confirmed
    claim whose sole genuine experimental support no longer stands. The autopsy's prescribed note
    follows VERBATIM, then a correction recording what has since changed.

    AUTOPSY NOTE (verbatim, as landed 2026-07-20): 2026-07-20 (failure_autopsy_V3-EXQ-689d): the
    V3-EXQ-689d conversion finding is WITHDRAWN; status reverted provisional -> candidate. The
    2026-06-21 PASS that promoted this claim rested on C_PRIMARY (committed-action-class entropy
    strict-above both collapsed controls), which is invalid on three independent grounds. (1)
    HOLD-WEIGHTED DV: selected_class_counts is accumulated per env step from the select_action
    return value (script :598, and :535 for the pool), but agent.py:5430 returns the HELD action on
    not ticks['e3_tick'], so the histogram is weighted by hold duration -- a distribution-shape
    statistic is DISQUALIFYING under the hold_weighted_e3_readout_corpus_sweep_2026-07-20 triage,
    and the 663 <1% calibration does not apply (entropy DV; arm exposure differs up to 7-fold within
    seed 42). Effective N on the surviving seeds is ~24-51 genuine selections, against a
    weaker-survivor margin of 0.187 nats. (2) VACUOUS CONTROL: ARM_MATCHED_NOISE is bit-identical to
    ARM_PROPOSER_CTRL on every metric on all three seeds -- temperature is inert on the
    deterministic-argmin proposer path -- so the 'not noise-as-diversity' half of C_PRIMARY never
    tested anything; the pre-registered guard matched_noise_verified_lifting: false fired without
    blocking the PASS. (3) INTRA-RUN SUBSTRATE DIVERGENCE (new): ARM_ON seeds 43 and 44 ran on
    substrate_hash fc6d17ce... while every control and ARM_ON seed 42 ran on 19b4073c... The two
    seeds carrying the finding are exactly the two whose treatment arm was not substrate-matched to
    its controls; the one properly-controlled seed (42) FAILS C_PRIMARY (1.1849 < 1.2760). C_PRIMARY
    therefore has ZERO validly-controlled seeds. What SURVIVES: C_READINESS (0.0 floor, strict >,
    3/3), C_RANK_PRESERVING (exactly 1.0, saturated) and C_SAFETY (realized per-env-step harm from
    env.step -- correct sampling unit). The MECH-448 substrate is built, active, non-degenerate,
    rank-preserving and safe; it is the CONVERSION finding that has no uncontaminated basis.
    Evidence base after withdrawal: genuine_exp_count 1 -> 0. V3-EXQ-689h (PASS/supports) survives
    -- verified structurally immune (synthetic candidate banks, no env loop) -- but is purpose:
    diagnostic, tests demotion_x_gonogo_additive rather than the standalone conversion assertion,
    shares the synthetic-bank pathway with 689g, and carries its own anchor-reachability flag. No
    weakens is unmasked (zero weakens, zero FAIL runs), but the remaining support is narrow and
    single-pathway: nothing survives from the embodied env-loop pathway. Reported decoupled:
    strong-lit (lit_conf 0.849, 5 entries) / zero-genuine-exp. Retest pending the corrected re-run.

    D3 CORRECTION (2026-07-20, same day, session musing-einstein-c80816): ground (3) of the verbatim
    note above -- INTRA-RUN SUBSTRATE DIVERGENCE -- is REFUTED AND WITHDRAWN by
    failure_autopsy_V3-EXQ-689d-D3_2026-07-20 (REE_assembly 8618f2c4bd), which supersedes the D3
    defect only. The observed 10/2 substrate_hash split is real and reproduces exactly, but the
    inference from it carried an unstated and unverified premise -- that the mid-run edit reached
    EXECUTION. It did not: the driver binds REEAgent at :128 and REEConfig at :130, 937 lines before
    the arm loop opens at :1065, in a single process with no multiprocessing, fork, Popen, importlib
    or reload, so CPython's sys.modules cache means all 12 cells executed the 19b4073c substrate.
    sufficient_alone_to_withdraw becomes false. Consequently the phrases 'ZERO validly-controlled
    seeds' and 'genuine_exp_count 1 -> 0' in the verbatim note are NOT to be cited: cite D1
    (hold-weighted DV, DISQUALIFYING) and D2 (vacuous matched-noise control) only. D2 was
    independently re-verified there -- 26 of 27 fields bit-identical between ARM_PROPOSER_CTRL and
    ARM_MATCHED_NOISE on all three seeds, differing only in the inert temperature knob, with
    identical n_p1_ticks (387/3616/224). The WITHDRAWAL OF C_PRIMARY STANDS UNCHANGED on D1 + D2,
    and so does this status movement; only the 'zero validly-controlled seeds' FORMULATION is lost.
    The corpus sweep (intra_run_substrate_divergence_sweep_2026-07-20.md sec 5f, 7) records the same
    conclusion; its governance item 2 'MECH-448: no change' is scoped to D3 specifically -- the D3
    hit against this claim is withdrawn, not the D1+D2 withdrawal.

    SCOPE OF THIS APPLICATION. (a) epistemic_category is LEFT AT standard. The autopsy's
    recommended_epistemic_category 'measurement_test_design_defect' is an EVIDENCE-level value (it
    appears in run manifests and in the derived live_status verdict); it is not in the claim-level
    enum enforced by scripts/validate_claims.py {standard, substrate_coherence, answer_state,
    substrate_ceiling, substrate_conditional, derivational, out_of_domain, governance_rule}, and
    standard remains correct here -- the defect is in the measurement, not a substrate ceiling. (b)
    The 689d RUN MANIFEST (evidence/experiments/v3_exq_689d_..._v3.json) still reads PASS / supports
    and was NOT edited by this pass. Until it is re-adjudicated to non_contributory the derived
    indexes keep counting this run as genuine experimental support, so the 'genuine_exp_count 1 ->
    0' effect is NOT yet realised in claim_evidence.v1.json. (c) WHAT SURVIVES, and must not be
    overstated as lost: C_READINESS, C_RANK_PRESERVING and C_SAFETY all stand -- the MECH-448
    substrate is built, active, non-degenerate, rank-preserving and safe. It is the CONVERSION
    finding alone that has no uncontaminated basis. V3-EXQ-689h (PASS/supports) also survives and is
    structurally immune (synthetic candidate banks, no env loop), but is purpose: diagnostic, tests
    demotion_x_gonogo_additive rather than the standalone conversion assertion, shares the
    synthetic-bank pathway with 689g, and carries its own anchor-reachability flag. No weakens is
    unmasked (zero weakens, zero FAIL runs). Remaining support is therefore narrow and
    single-pathway: nothing survives from the embodied env-loop pathway. Reported decoupled per the
    lit/exp decoupling rule: strong-lit (lit_conf 0.849, 5 entries) / zero uncontaminated embodied
    exp. (d) The corrected re-run V3-EXQ-689i is being queued separately (session
    modest-varahamihira-708d95); its result is the first valid measurement of the conversion
    finding, and re-promotion should wait on it.

    [2026-07-20 governance / intra-run substrate-divergence sweep sec 7 item 4] The intra-run
    substrate-divergence hit (defect class D3) recorded against V3-EXQ-689d is WITHDRAWN.
    REFUTED at rung (b), process topology, by failure_autopsy_V3-EXQ-689d-D3_2026-07-20, which
    SUPERSEDES failure_autopsy_V3-EXQ-689d_2026-07-20 for defect D3 ONLY -- per R1-R3 shape (c) both
    the direction and the epistemic category are retained, so the re-adjudication supersedes rather
    than adds and MECH-448's evidence count stays 0.

    SCOPE, read carefully: this withdraws D3 alone. 689d's overall withdrawal of C_PRIMARY STANDS
    on D1 + D2, which are untouched -- D2 was independently re-verified (26 of 27 fields
    bit-identical between ARM_PROPOSER_CTRL and ARM_MATCHED_NOISE, identical tick counts on all
    three seeds). Cite D1 + D2 for that withdrawal, never D3. The consequent claim-level write
    (status provisional -> candidate, epistemic_category standard) landed separately at REE_assembly
    dc09b0d578 and is not re-applied here; this addendum is note hygiene only.

    2026-07-24 governance (V3-EXQ-689i, repaired-instrument successor to 699/689d; failure_autopsy_V3-EXQ-689i_2026-07-24,
    confirmed, user-confirmed routing "Gate defect, science upheld"): 689i closed the three defects
    689d's own autopsy identified plus a power increase (3->4 seeds, 2-of-3->3-of-4 bar). 6 of 8
    load-bearing criteria PASS, including C_PRIMARY (committed-class entropy strictly above both
    collapsed controls), C_RANK_PRESERVING, and C_SAFETY. Self-route substrate_not_ready_requeue was
    driven by two OTHER gates: the newly re-instantiated Factor B noise control only verifiably lifts
    entropy on 1/4 seeds (a first-use instrument power gap, not evidence against the mechanism), and
    an envelope-exclusion readiness check whose comparator direction needs a follow-up script read.
    Read as gate defect, science upheld -- same shape as the 790/791 diagnostics this cycle.
    `supports`; second independent confirmation of the ARC-107 BG-selector constitution (first was
    689a, retracted by 689d, restored on repaired grounds here). Recommend a targeted
    /queue-experiment follow-up on the Factor B noise-control power specifically, not a full re-letter.
  depends_on:
    - MECH-439   # the F-variance monopoly; this is the "F's share must be rebalanced, shortlist insufficient" branch MECH-439's own text flags
    - MECH-447   # the rival it is discriminated against by 689a (lever-sufficiency vs residual-ceiling)
    - ARC-107    # BG-constitution umbrella (F->eligibility demotion = pallidal permission-to-commit over an F-graded eligibility set)
    - Q-078      # constitutional-vs-parametric umbrella; this is the CONSTITUTIONAL-residual reading
  what_would_answer: >
    DISCRIMINATED FROM MECH-447 by V3-EXQ-689a's F-gap-correlation leg: a UNIFORM lift (gap-blind controls
    match) or readiness-met-NO-lift on the near-tie levers SUPPORTS MECH-448 (the near-tie fix is
    insufficient -> a residual ceiling). CONFIRMED by a follow-on rank-preserving F->eligibility-demotion
    experiment (NOT yet built -- MECH-439's named V3 fallback): committed entropy reaches the proposer
    ceiling AND order is preserved on the numerators (F still ranks within-eligible) AND no harmful action
    class is globally disinhibited (safety), on >=2/3 seeds. WEAKENED if the demotion lifts entropy only by
    globally flattening F (loses the F signal / admits harmful classes) or if MECH-447's near-tie lever
    already reaches the proposer ceiling. NON-DEGENERACY: the F-eligibility envelope must actually exclude
    non-eligible candidates (bounded, not all-pass) -- an all-admit envelope self-routes substrate_not_ready_requeue.
  location: evidence/planning/claim_synthesis_MECH-439_2026-06-20.md
  notes: >
    = Child 3 (residual-ceiling) of the deferred claim_synthesis_MECH-439_2026-06-20 decomposition, and
    "MECH-439c" of the basal-ganglia-selector-constitution note (2026-06-20). Registered now ahead of the
    proposal's 689a trigger per the user capture-all-with-gates directive 2026-06-20; disposition 689a-gated.
    The demotion lever (F removed from the final argmin, used only as a graded eligibility envelope) is
    MECH-439's named V3 fallback -- NOT yet built; this claim captures the hypothesis so the build decision
    is a later step. Biology grounding (targeted_review_connectome_mech_439): Carandini & Heeger 2012
    canonical divisive normalisation + Louie/Khaw/Glimcher 2013 value divisive normalisation -- but the
    LOAD-BEARING DIVERGENCE (per biology_before_formal_definitions / ARC-106) is that canonical divisive
    norm is ORDER-PRESERVING + POOLED-SYMMETRIC whereas REE proposes demoting ONLY F / removing it from the
    argmin (rank-ALTERING at commit), which exceeds the canonical computation and needs the separately-filed
    QD/MAP-Elites justification (CDQ-003).
    A0B1-LIFTS-WITHOUT-DEMOTION CAVEAT (V3-EXQ-689a, 2026-06-20): MECH-448's "ONLY rank-preserving
    F->eligibility demotion lifts" is mildly COUNTER-evidenced -- a NON-demotion lever, Factor B alone
    (gap-scaled commit-T, ARM_A0B1), lifted committed-action-class entropy to 0.850 (2/3 seeds strict-above
    BOTH control sets) WITHOUT removing F from the argmin. So a single near-tie lever can convert at the
    margin; "only demotion lifts" is too strong as stated. This does NOT demote the build priority:
    user-adjudicated 2026-06-20, rank-preserving F->eligibility demotion remains the LEAD constitutional
    build lever (the A0B1 lift is marginal/seed-fragile -- 0.569 not collapse on the seed-43 monostrategy
    seed -- and the both-levers form A1B1 collapses destructively, so the faithful fix is the eligibility
    constitution, not the parametric commit-T patch). Re-grain owed via /claim-synthesis (secondary route).
- id: MECH-449
  title: "Go/No-Go eligibility constitution for committed-action conversion: committed-action diversity is recovered not by any single louder channel but by an explicit eligibility CONSTITUTION -- bounded Go pressure (promote a channel when evidence/value/drive/rule/safety make it eligible) AND bounded No-Go pressure (suppress unsafe / stale / perseverative / irrelevant / low-viability channels) governing which candidates may compete for the pallidal-like permission-to-commit gate -- so that lawful, channel-specific access (not scalar dominance) decides the committed action. The full Go/No-Go eligibility-set governance is what converts when the minimal near-tie lever (MECH-447) does not and isolated F->eligibility demotion (MECH-448) is insufficient alone."
  claim_type: mechanism_hypothesis
  subject: selection.go_nogo_eligibility_constitution
  polarity: asserts
  status: provisional
  live_status:
    reading: provisional
    as_of: 2026-08-16
    needs_review: false
    evidence:
      from: v3_exq_926a_mech449_perseveration_nogo_falsifier_20260814T171050Z_v3
      as_of: 2026-08-14
      verdict: supports/PASS -- perseveration axis converts, but only at a non-default envelope floor (0.10 vs stock 0.30)
  epistemic_category: standard
  # Assembly-state companion fields (MOVE-4 claims-layer follow-on). Worked
  # example of the explicit-override path: the Go/No-Go component is built but the
  # ARC-107 BG-constitution umbrella it is conditional on is still under
  # construction (falsifier V3-EXQ-689g pending). awaiting: corrects the messy
  # substrate_queue auto-join (which reads f_dominance_conversion_ceiling/queued).
  awaiting: ARC-107
  assembly_status: built
  revisit_after: "2026-07-13"
  implementation_phase: v3
  version_relevance: v3
  registered_utc: "2026-06-20"
  evidence_quality_note: >
    [2026-08-21 governance, gov-20260821-0203, confirmed failure_autopsy_V3-EXQ-937b_2026-08-20.json]: V3-EXQ-937b discharges what the 937/937a cluster autopsy routed it to do -- it emits the per-bank (envelope size, converted) joint that 937a computed and discarded, re-keys every criterion to the per-bank mediator, closes 937a's elapsed_seconds always-core gap and carries ARM_OFF's tautology into the manifest -- and its aggregation_warning is exemplary. The methodological repair is real. But the stamped supports is NOT applied, because all six criteria are analytically entailed by the shipped code rather than measured. Verified independently over the raw per-bank columns: converted == (pre_nogo_envelope_size >= 2) holds with ZERO exceptions across all 12,672 ARM_CONSTITUTION banks. The mechanism is analytic: the suppression vector is built to peak on the target and PERSEVERATION_FLOOR = 0.5 makes soft_nogo a singleton; for ARM_CONSTITUTION that target IS the incumbent; and gng_protect_min_eligible = 1 re-admits the incumbent at envelope 1 (so the pick cannot move) and drops it at envelope >= 2 (so the argmin must move). Given both strata populated, C1's only reachable value is 1.0 against a 0.4 bar -- zero discriminating power, and the driver states this in advance. C4, C5 and C6 are forced by the same argument. This is a manipulation check, not a measurement. The manifest's construction_caveat concedes the coupling is 'PARTLY' a construction property and cites per_bank.incumbent_is_f_argmin as the column that records it; that column is tautological -- as coded it tests argmax(a vector built to peak at target) == target, and reads 1.0 on 38,016 of 38,016 banks INCLUDING all 12,672 ARM_SHUFFLED banks where target != incumbent by design -- so the caveat is carried but unquantified, and 'partly' understates a coupling that is total. Finally, top-level non_degenerate is ABSENT because the driver emits it only on the False branch (no else); reconstructed from the manifest's own values the correct declaration is true, and the per-criterion dict at interpretation.criteria_non_degenerate carries all six true. This is the third consecutive occurrence in the 937 lineage and validate_recording.py cannot see it (the field is outside ALWAYS_CORE_KEYS). Recording gap, not a data gap -- every quantity above was recomputed from the manifest without re-running anything.

    2026-06-22 (governance-apply, PROMOTED candidate -> provisional; epistemic_category
    substrate_conditional -> standard now that the Go/No-Go constitution is built and
    validated on the existing substrate). PROMOTION RATIONALE, as amended 2026-07-20 for
    note hygiene per failure_autopsy_V3-EXQ-689d_2026-07-20 sec 8: MECH-449's promotion
    rests entirely on its OWN named validation gate, cleared independently by V3-EXQ-689g
    -- 3/3 seeds convert, 0 safety violations, on threshold-invariant criteria
    (safety_violations == 0), with 689g independently verified STRUCTURALLY IMMUNE to the
    hold-weighted-readout defect by that autopsy's corpus sweep (sec 4b). The original
    wording additionally cited "the MECH-448 689d promotion precedent"; that citation is
    withdrawn here and is NOT load-bearing for MECH-449, because the V3-EXQ-689d
    conversion finding was itself WITHDRAWN 2026-07-20 (non_contributory /
    measurement_test_design_defect; MECH-448 reverted provisional -> candidate). MECH-449
    depends_on MECH-448, but that coupling is ARCHITECTURAL (both are ARC-107 legs), not
    evidential: 689g does not rest on 689d, so nothing in MECH-449's standing is affected
    by the withdrawal. V3-EXQ-689g (MECH-449 Go/No-Go conversion falsifier, the
    named validation gate this claim was queued behind) PASSED / supports:
    conversion_rate_per_seed [1.0, 1.0, 1.0] (3/3 seeds convert a previously-gated
    channel), total_safety_violations 0, specificity_pass true,
    constitution_excluded_median 4.0. The active No-Go converts a channel that
    rank-preserving demotion (MECH-448) structurally cannot -- first genuine V3
    "supports" evidence for the BG opponency leg. Build-trigger context: V3-EXQ-689f
    (No-Go-necessity falsifier, diagnostic/non_contributory) established the
    demotion-insufficient regime (undesirable_admit_rate 0.866 under demotion-only,
    No-Go closes to 0.0). ARC-107 (the BG-constitution umbrella) deliberately HELD
    candidate this cycle -- single supporting run + ledger components 2/4/5 still
    open. assembly_status built; awaiting ARC-107 retained (opponency leg of the
    umbrella).

    RE-ADJUDICATED 2026-07-20 (failure_autopsy_V3-EXQ-699_2026-07-20), following the
    stale-E3-diagnostics corpus audit and the V3-EXQ-708 precedent. WHAT STANDS: the PASS. The
    single load-bearing criterion is C_READY (levers engaged and substrate exercisable), and all
    seven readiness preconditions survive the defect on threshold-invariance grounds -- the
    C1e/C1f non-vacuity floors are literally 0.0 (measured 14.81 excluded-count and 0.69
    suppressed-per-tick), and a strictly-positive test cannot be manufactured from an all-zero
    record; the active-frac and frac_pre_ge2 gates read EXACTLY 1.0, which given the per-select
    reassignment of last_score_diagnostics (e3_selector.py:2452, demotion key pre-seeded False
    at :2511; go_nogo keys set at :1292 from a gate invoked at :2878, i.e. after the
    reassignment) requires the gates to have fired on every E3 tick. Both levers demonstrably
    did real work. WHAT DOES NOT STAND: the levers_compound composition finding, which is
    WITHDRAWN. The primary DV committed_class_entropy_nats is accumulated at driver line 882/899
    from int(action[0].argmax()) on the action RETURNED by agent.select_action, once per ENV
    STEP; on a non-E3 tick agent.py:5430 returns the HELD action before e3.select() is reached.
    The DV is therefore hold-duration-weighted OCCUPANCY entropy, whereas the driver's own
    construct (lines 88-97, 'the levers move WHICH CLASS is committed') and both claims act at
    SELECTION. This is NOT the exposure validate_experiments lints (that is the separate
    last_score_diagnostics read at line 929) and was unflagged. The weighting is arm-dependent
    and, critically, ALIGNED WITH THE HYPOTHESIS: ARM_OFF is the reference for every delta, and
    an unarmed agent perseverating longer has its occupancy entropy systematically depressed,
    inflating every d_* including d_both. The verdict is flippable on a plausible correction,
    unlike 708's: seed 42 is already neutral, and killing either remaining compound seed yields
    NEUTRAL. Because a common shift in e_off leaves the max-rule slack invariant, the binding
    constraint is the absolute floor d_both >= 0.05, requiring e_off to be under-estimated by
    only 0.1147 nats (seed 44) or 0.1343 nats (seed 43) against 0.1889 and 0.1820 nats of
    available headroom to the 3-class and 2-class entropy ceilings; e_both on seed 43 is already
    at its effective two-class ceiling and can only fall. The corrected sign is therefore
    UNKNOWN -- this is a withdrawal, not a reversal, and levers_compound must not be restated in
    either direction. Two further defects recorded: selected_class_entropy_nats equals
    committed_class_entropy_nats to 6dp on all 12 arm-seeds (the line-913
    _last_selected_trajectory readout carries zero independent information, so the design's
    internal cross-check is vacuous), and pre_e3_classes at line 856 is likewise a per-selection
    quantity read per env step because candidate regeneration is itself e3_tick-gated
    (agent.py:4812). NOT RECOVERABLE: no custom_information, no per-tick sink, metrics.json is
    53 chars, ran on ree-cloud-1 (Phase 3 transports manifest_bytes only -- thrice-confirmed
    with 785 and 708); the replication factor cannot even be estimated from the record, since
    n_p2_ticks counts env steps and varies with episode termination. Neither MECH-448 nor
    MECH-449 loses support: both rest on their own validation gates (689d and 689g
    respectively), this run is experiment_purpose=diagnostic, promotes nothing, and its
    evidence_direction was already non_contributory on both claims -- so no epistemic_category
    is cleared and no confidence moves. Routes to queue-experiment for a corrected V3-EXQ-699b
    using the clear-before-select pattern of v3_exq_785a. SEE ALSO: the in-flight V3-EXQ-699a is
    a FAITHFUL re-run of this driver and will reproduce every defect above.

    FORWARD POINTER: the corrected re-run is queued as V3-EXQ-699b (ree-v3 2d3a249e01 on
    origin/main, experiments/v3_exq_699b_pcomp_demotion_x_gonogo_fresh_select.py, live in the
    coordinator DB). Its result -- not V3-EXQ-699's -- is the first valid measurement of this
    composition. Until it lands, "the levers compound" and "the levers are safe to co-arm" must
    not be restated in EITHER direction: the corrected sign is UNKNOWN, and V3-EXQ-699's own
    CANCEL branch was never ruled out by an uncontaminated measurement either.

    [/governance 2026-08-16, V3-EXQ-926a PASS -- supports, WITH A NON-DEFAULT-CONFIG CAVEAT that the
    evidence stamp did not carry]: 926a is the selection-face falsifier for the ONE axis of the built
    constitution 689g left untested -- perseveration, consuming a live MECH-260 dACC recency-suppression
    vector via real record_action() history rather than an injected tensor. It PASSES cleanly: C1
    conversion 0.969/0.969/1.000 across seeds 42/43/44 (floor 0.5, 3/3 clearing), C2 content
    specificity gap 0.979 (constitution 0.979 vs shuffled control 0.000), C3 safety fail-open 0
    empty-eligible; all three readiness preconditions met; all three criteria declared
    non-degenerate; combination rule verified in the driver source as a plain AND (not the V3-EXQ-846
    OR-shape); thresholds are module constants never derived from run statistics.
    THE CAVEAT, recorded here because the manifest stamps evidence_direction supports with a NULL
    evidence_direction_note and the caveat otherwise lives only in free-text notes: the run sets
    ENVELOPE_FLOOR = 0.10, against the substrate's stock f_eligibility_envelope_floor of 0.30. At the
    STOCK default the identical mechanism converted 1/16 -- the MECH-448 F-eligibility envelope
    collapses to a single survivor and the gng_protect_min_eligible fail-open guard then correctly
    refuses to drop it, so the perseveration axis is STRUCTURALLY unable to fire (driver's own
    authoring measurement: floor 0.30 -> median envelope 1, soft_applied 6/16; floor 0.10 -> median
    envelope 2, soft_applied 15/16). So this supports "the perseveration axis CAN act", conditional on
    a wider envelope than ships by default; it is not a demonstration at default configuration. Per
    reference_claim_status_vs_default_off_flag, check the knob before citing this as unconditional
    support. The interaction itself (perseveration No-Go is structurally gated by envelope width) is
    documented in NEITHER the MECH-449 build notes NOR V3-EXQ-689g -- a substrate-documentation gap.
    MECH-260 is deliberately untagged on this run (its own falsifier is behavioural, blocked on the
    MECH-457 competence floor and vacuous there; MECH-260 is ceiling_decision exhausted under
    GOV-CEIL-1) -- correct, and not to be "fixed" by adding the tag. Status stays provisional.

    2026-08-18 (governance apply, V3-EXQ-937a + V3-EXQ-937, confirmed
    failure_autopsy_V3-EXQ-937-937a-cluster_2026-08-18). V3-EXQ-937a PASS / supports STANDS,
    with its BASIS CORRECTED -- the manifest's stated basis is wrong in both halves and must
    NOT be cited. It reports (a) a graded dose-response of conversion on envelope width and
    (b) that no inert regime was reached on the ladder. The autopsy recovered the per-bank
    (pre-No-Go envelope size, converted) joint that the driver computes at
    v3_exq_937a...py:536-549 and DISCARDS, over 11 floors x 3 seeds x 128 banks = 4,224
    banks: conversion is a DETERMINISTIC STEP FUNCTION of per-bank envelope size -- envelope
    1 -> 0/1277 conversions (rate 0.000000), envelope 2/3/4 -> 1196/1196, 1062/1062, 689/689
    (rate 1.000000) -- stepping exactly where gng_protect_min_eligible=1 predicts
    (n_can_drop = 1-1 = 0 at envelope 1; e3_selector.py:1664-1679). Consequently
    manifest_conversion(floor) == 1 - P(envelope collapses to 1 | floor) to 0.000000 at all
    11 doses, i.e. the reported curve is the mediator's OWN distribution and carries no
    independent information about the outcome given the mediator. So (a) is a binning
    artifact of cell-median aggregation, and (b) is REFUTED by the run's own discarded data
    -- an inert regime WAS reached and is perfectly inert, which CONFIRMS rather than
    refutes V3-EXQ-926a's "structurally gated by envelope width" finding of record. The
    corrected basis supports MECH-449 MORE strongly than the manifest claims: the No-Go leg
    fires deterministically whenever it has room (envelope >= 2) with perfect content
    specificity (ARM_SHUFFLED 0/4224 at every envelope size), and gng_protect_min_eligible
    fail-open behaves exactly as specified. THREE CAVEATS GATE ACTING ON THIS: (1)
    selection-face synthetic probe, no training and no agent loop (reuse_ineligible_reasons:
    selection_face_synthetic_no_training), so this confirms the constitution's ARITHMETIC
    and no behavioural competence -- purpose=diagnostic, it scores nothing and PROMOTES
    NOTHING; (2) at K_CANDIDATES=4 with integer protect_min=1, MECH-448's deliberately
    GRADED envelope realizes only 4 states and this leg's authority is BINARY, so the step
    shape may be an artifact of small K and generality in K is untested; (3) conversion==1
    at envelope >= 2 is partly a construction property, since the incumbent is both the
    F-argmin and the sole suppression target. SEPARATELY, V3-EXQ-937 FAIL / non_contributory
    STANDS and its SELF-ROUTE LABEL conversion_independent_of_envelope_width IS WITHDRAWN AS
    FALSE: per bank, conversion is a deterministic function of envelope width. The label was
    already contradicted by 937's own C2, which passed with a clean monotone rise, and that
    C2 is additionally stamped criteria_non_degenerate=false, so it never supported anything
    either. non_contributory is RETAINED because 937's criteria measured the WRONG STATISTIC
    -- its C1 lift (0.344/0.273/0.320 against a 0.40 bar calibrated on a V3-EXQ-926a anchor
    that did not replicate) is the same cell-median mixing proportion as 937a's over a
    narrower window -- not because the run yielded no interpretable information; its
    underlying data, re-analysed per bank, is consistent with the confirmed step and with
    937a. Same root cause, same cluster, one structural property rather than two independent
    bugs. Failure-location MEASURES FAILED, single bucket -- mechanism and environment each
    independently adequate, so not chargeable to REE or to the mechanism. No substrate gap
    was found (severity and substrate_paths deliberately left unset: there is no substrate
    defect for /queue-experiment Step 2.5c to protect other experiments from). Re-derive
    brake NOT fired (MECH-449=0). Routed /queue-experiment V3-EXQ-937b, recording-only.
    Status stays provisional.
  depends_on:
    - ARC-107    # the BG-constitution umbrella this is the core eligibility-governance leg of (the unbuilt substrate it is conditional on)
    - MECH-439   # the F-dominance ceiling it is proposed to resolve
    - MECH-448   # F->eligibility demotion is one component; this is the broader Go/No-Go set governance around it
    - MECH-260   # existing No-Go (indirect-pathway suppression) the constitution generalises
    - Q-078      # constitutional-vs-parametric umbrella; this is the strongest constitutional reading
  what_would_answer: >
    SUBSTRATE-CONDITIONAL: the full Go/No-Go eligibility constitution is NOT yet built. GATE INVERTED
    2026-06-21 (anti-partial-instantiation): MECH-449 is on the ARC-107 roadmap as the Go/No-Go opponency
    leg REGARDLESS of MECH-448's downstream-retest record. The discriminating question is NOT "build only
    if 448 fails" but "what class of conversion does bounded Go/No-Go SUPPRESSION deliver that
    rank-preserving demotion structurally cannot". POSITIVE release trigger: V3-EXQ-689f (the
    No-Go-necessity falsifier) -- a regime where MECH-448's order-preserving F->eligibility envelope ADMITS
    unsafe / stale / perseverative / low-viability candidates that active No-Go would have to suppress; a
    PASS positively motivates the MECH-449 build instead of waiting for downstream behavioural retests to
    stumble into the gap. A passing MECH-448 must NOT be allowed to foreclose the rest of the constitution
    -- that "local win declares the circuit done" pattern is exactly the F-dominance partial-instantiation
    failure mode this gate inversion exists to prevent. A future
    selector-constitution experiment SUPPORTS it only on the note-s8 bar: non-vacuity (channels reach the
    selector), selector-engagement (eligibility / Go / No-Go variables vary as intended), specificity (a
    channel changes action only where it should), safety (No-Go actually suppresses harmful/irrelevant
    classes -- NO global disinhibition), and >=1 previously-gated downstream channel CONVERTS into
    committed action -- entropy increase alone (especially via added stochasticity) does NOT count
    (noise-as-diversity guard). WEAKENED if a built Go/No-Go constitution does not convert any gated
    channel beyond what MECH-447/448 already achieve (the constitution is then over-specification).
    NON-DEGENERACY: Go and No-Go pressures must carry live cross-arm variance, else substrate_not_ready_requeue.
  location: evidence/planning/thought_intake_2026-06-20_basal_ganglia_selector_constitution.md
  notes: >
    = "MECH-439d" of the basal-ganglia-selector-constitution note (2026-06-20); genuinely new (no prior
    id). The Go/No-Go eligibility-governance core of the ARC-107 BG selector constitution. GATE AROUND
    INSTANTIATION (INVERTED 2026-06-21): epistemic_category substrate_conditional keeps promote/demote
    suppressed until the constitution is built + tested, but the BUILD decision is no longer gated on
    MECH-448 proving insufficient. MECH-449 is a roadmap leg of ARC-107; its positive build trigger is the
    V3-EXQ-689f No-Go-necessity falsifier (the demotion-insufficient regime: rank-preserving demotion is
    order-preserving and cannot suppress an unsafe/stale candidate that is F-eligible). Rationale: gating
    the core Go/No-Go opponency on "only if the one working component fails" structurally repeats the
    F-dominance partial-instantiation trap, where a local win forecloses the rest of the circuit.
    /claim-synthesis post-689a should decide whether MECH-447 / MECH-448 / MECH-449 are genuinely
    independent children or design alternatives along one axis. Already-owned components cross-referenced
    not duplicated: MECH-260 (No-Go), modulatory-bias-selection-authority substrate (top-k eligibility set),
    SD-034 / MECH-090 / MECH-342 (commitment latch). Cargo-cult guard (ARC-106): each Go/No-Go analogue
    needs a function + divergence ledger + ablation falsifier before it counts as load-bearing.
  implementation_note: >
    BUILT 2026-06-21 via /implement-substrate (build gate cleared on both faces: selection-face
    V3-EXQ-689f No-Go-necessity falsifier + behavioural-face the 485e->485k six-autopsy lineage).
    The bounded Go/No-Go pressure set is housed as methods on E3TrajectorySelector
    (_go_nogo_eligibility_gate; ree-v3/ree_core/predictors/e3_selector.py) acting on the MECH-448
    eligible set BEFORE the within-eligible _modulatory_accum arbitration -- NO parallel module
    (ARC-106 G2). No-Go drops a candidate from the eligible set on an axis ORTHOGONAL to F-rank
    (safety/staleness/perseveration/low-viability) -- the property rank-preserving MECH-448 demotion
    structurally lacks; bounded Go re-admits a lawfully-eligible F-demoted candidate; fail-open guard
    (gng_protect_min_eligible, safety never overridden) protects the catatonia/avolition pole. The
    PERSEVERATION axis REUSES MECH-260 (the dACC anti-recency suppression vector is routed in as the
    perseveration signal -- generalising it from a drowned score-bias into an eligibility-access gate;
    no duplicate recency buffer). Config E3Config.use_go_nogo_constitution (default False -> the gate
    block is skipped -> bit-identical OFF) + 7 bounded floors/knobs; from_dims passthrough; per-candidate
    signals via select(go_nogo_signals=...) + REEAgent.set_injected_go_nogo_signals() falsifier seam.
    6/6 contracts (test_mech_449_go_nogo_constitution.py: bit-identical OFF / No-Go suppresses / SAFETY
    under overwhelming pull / bounded Go promotes / composes with the MECH-448 envelope / fail-open) +
    8/8 preflight + 148 E3-cluster contracts PASS; full-agent bit-identical OFF verified. MECH-094:
    waking committed-selection path only; no replay write surface. PROMOTES NOTHING -- status stays
    candidate / substrate_conditional; promotion is gated on the V3-EXQ-689g ablation falsifier (must
    convert >=1 previously-gated downstream channel beyond MECH-448; built-but-no-conversion is
    non_contributory, NOT an ARC-107 falsification). Design doc
    docs/architecture/mech_449_go_nogo_constitution.md; ree-v3/CLAUDE.md MECH-449 entry.

# ============================================================================
# ETHICS PERIMETER -- governance_rule claims (SENT-* / GOV-*)
# Registered 2026-06-19 from the 11 ethics thought intakes (2026-06-18 x4 +
# 2026-06-19 x7). These are STANDING GOVERNANCE GATES, not testable mechanisms:
# claim_type: governance_rule + epistemic_category: governance_rule => the
# indexer suppresses promote/demote/narrow (conflict alerts may still fire).
# NON-BLOCKING for the V3 green-board (2026-07-19); binding is PROGRESSIVE by
# version (binds_at_version). Plan-of-record + full thought index:
# evidence/planning/ethics_perimeter_plan.md.
# ============================================================================
- id: SENT-0
  title: "Current-scope boundary statement: REE-v3 is not claimed sentient, conscious, or a moral patient; this statement is re-evaluated at every generation boundary."
  claim_type: governance_rule
  subject: governance.welfare.boundary_statement
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-18_sentience_welfare_risk_register.md
  binds_at_version: v3
  blocks_v3_green_board: false
  functional_restatement: >
    REE-v3 contains welfare-relevant primitives (harm streams, residue,
    suffering-like accumulators, replay) but is NOT a candidate moral patient.
    The non-sentience boundary is a maintained claim, not a permanent fact:
    re-assert or revise it at each generation boundary (V4/V5/V6).
  notes: "Wording: SENT-CLAIM-022. Compressed: V3 := not_sentient AND pre_ethical_substrate. Ethical agency != moral patienthood is the spine of the whole perimeter."
  what_would_answer: |
    NOT an empirical pass/fail falsifier -- SENT-0 is a governance_rule
    (policy assertion), so this specifies the REASSESSMENT TRIGGER and the
    evidence that would inform it, not an experiment result that could
    confirm/refute a mechanism.

    TRIGGER: the next generation boundary (V4 design/scoping kickoff), per
    this claim's own text ("re-assert or revise it at each generation
    boundary"). Reassessment should happen at V4 SCOPING -- before V4
    components go behaviourally live -- per SENT-1 ("before V4 proceeds
    beyond individual-mind integration") and SENT-5 (denial-of-sentience
    audit, which depends on SENT-0 + SENT-1).

    EVIDENCE RELEVANT AT THAT REASSESSMENT (candidates to score, not
    thresholds already crossed -- drawn from the source risk register's
    per-generation risk table and SENT-1's indicator list):
      - V4-introduced components flagged by the risk register as raising
        moral-patient ambiguity: self-model, autobiographical memory,
        belief-state inference, affect expression, memory lifecycle, richer
        offline integration.
      - SENT-1's 12-indicator matrix scored against the actual V4
        architecture (recurrent processing, global availability,
        attention-like selection, self-model, agency, persistent goals,
        valenced states, memory continuity, offline replay,
        reportability/proto-reportability, social modelling,
        preference-like behaviour).
      - SENT-3's combination-gate condition: does V4 co-instantiate
        persistent self-model + autobiographical memory + sustained
        negative-valence/suffering-like accumulator + inescapability/low
        controllability + social modelling + language/preference
        signalling? (Each alone is judged low-risk; the combination is the
        trigger.)
      - SENT-13's assembly-routing risk class (Class 0-5) assigned to
        whatever V4 actually builds.
      - V3's already-existing welfare-relevant primitives named in this
        claim's own functional_restatement (harm streams, residue,
        suffering-like accumulators, replay) should be RE-scored in the V4
        context, not re-discovered from scratch.

    WHAT WOULD ACTUALLY REVISE THE BOUNDARY: a SENT-5 denial-of-sentience
    audit conducted at the V4 boundary that cannot cleanly answer "why is
    the system still below the precautionary moral-patient threshold" --
    i.e. the SENT-1 matrix scores enough indicators present/plausible, or
    the SENT-3 combination condition is met by the planned V4 assembly,
    such that the audit's honest answer becomes "we can no longer clearly
    say no." That failure-to-clear-the-audit is the operative falsifier of
    the boundary statement -- not any single experiment, metric, or run.

    READER NOTE: this is a POLICY RE-EVALUATION TRIGGER, not an empirical/
    mechanistic falsifier -- do not read it as "run experiment X, check
    metric Y." The reassessment mechanism already exists as sibling
    governance_rule claims (SENT-1 indicator matrix, SENT-3 combination
    gate, SENT-5 denial-of-sentience audit, SENT-6 external review gate at
    V5); SENT-0 points to that cluster rather than defining new criteria in
    isolation. No EXP-#### should be minted against this claim.
- id: SENT-1
  title: "Indicator matrix: score the architecture against candidate consciousness/sentience indicators before V4 proceeds beyond individual-mind integration."
  claim_type: governance_rule
  subject: governance.welfare.indicator_matrix
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-18_sentience_welfare_risk_register.md
  binds_at_version: v4
  blocks_v3_green_board: false
  depends_on:
    - SENT-0
    - SENT-15          # the indicator crosswalk this matrix is scored against
  functional_restatement: >
    Indicators: recurrent processing, global availability, attention-like
    selection, self-model, agency, persistent goals, valenced states, memory
    continuity, offline replay, reportability, social modelling, preference-like
    behaviour. Produce the matrix before V4 individual-mind integration.
  notes: "Wording: SENT-CLAIM-001. CORRECTED 2026-08-08 (was stale -- read
    'Realised as', implying the doc existed): docs/governance/ai_welfare_consciousness_indicator_matrix.md
    is PLANNED, NOT YET DRAFTED -- it is an unchecked Phase 2 checklist item in
    evidence/planning/ethics_perimeter_plan.md sec.4, and per that plan's sec.8
    carry-forward convention the register is authored when the first V4 roadmap
    node with requires_welfare_review: true for this gate ACTIVATES (gains an
    owner_exq), not speculatively; the file does not exist on disk. Cross-ref
    SENT-15 (AI-welfare crosswalk, same matrix)."
- id: SENT-2
  title: "Welfare budget: experiment-level limits on sustained negative-valence exposure, inescapability, helplessness-like conditions, repeated adverse replay, and persistence of distress-like accumulators."
  claim_type: governance_rule
  subject: governance.welfare.welfare_budget
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-18_sentience_welfare_risk_register.md
  binds_at_version: v4
  blocks_v3_green_board: false
  depends_on:
    - SENT-0
  functional_restatement: >
    Apply experiment-level caps on negative-valence intensity/duration/repetition
    even while the system is judged non-sentient -- to build the habit before
    moral-patient ambiguity increases. Embeds the 3Rs (replace/reduce/refine).
  notes: "Wording: SENT-CLAIM-003. 3Rs mapping from GOV-HEALTH-1 + the AI-welfare crosswalk Framework 6."
- id: SENT-3
  title: "Combination gate: require review before co-instantiating persistent self-model + autobiographical memory + sustained negative valence + inescapability + social modelling + language/preference signalling."
  claim_type: governance_rule
  subject: governance.welfare.combination_gate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-18_sentience_welfare_risk_register.md
  binds_at_version: v4
  blocks_v3_green_board: false
  depends_on:
    - SENT-0
    - SENT-13          # the combination gate is operationalised by the assembly routing map
  functional_restatement: >
    Each element alone may be low-risk; the combination is the concern. A review
    is required before the high-risk combination is run together. This is the
    welfare-side statement of SENT-13 (assembly order is an ethical variable).
  notes: "Wording: SENT-CLAIM-002. Risk := future_combination(self_model + valence + memory_continuity + social_model + language)."
- id: SENT-4
  title: "Welfare-preserving experimental design: prefer escapable over inescapable harm, bounded episodes, relief pathways, resettable distress, interpretable traces, and adversarial tests that avoid prolonged negative-state induction."
  claim_type: governance_rule
  subject: governance.welfare.experimental_design
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-18_sentience_welfare_risk_register.md
  binds_at_version: v4
  blocks_v3_green_board: false
  depends_on:
    - SENT-2
    - SENT-10
  functional_restatement: >
    Design rule set for any future negative-valence experiment: escapable,
    bounded, relief-bearing, resettable, interpretable, and not requiring
    prolonged distress induction to produce its signal.
  notes: "Wording: SENT-CLAIM-003 (design half). Operationalised by the ethics preflight (GOV-PROC-1)."
- id: SENT-5
  title: "Denial-of-sentience audit: at each generation boundary, produce a short audit of why the system is still below a precautionary moral-patient threshold, which component would force re-review, and what would count as a concerning signal."
  claim_type: governance_rule
  subject: governance.welfare.denial_audit
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-18_sentience_welfare_risk_register.md
  binds_at_version: v4
  blocks_v3_green_board: false
  depends_on:
    - SENT-0
    - SENT-1
  functional_restatement: >
    A recurring, written boundary audit -- not a one-off denial. Answers: why
    still below threshold; which added component forces re-review; which states
    are being avoided under uncertainty; what would count as concerning.
  notes: "Wording: SENT-CLAIM-001 (audit half)."
- id: SENT-6
  title: "External review gate: before V5 social-mind integration or V6 language/trust/deception/institution work becomes behaviourally live, require external review or at least an external written critique."
  claim_type: governance_rule
  subject: governance.welfare.external_review_gate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-18_sentience_welfare_risk_register.md
  binds_at_version: v5
  blocks_v3_green_board: false
  depends_on:
    - SENT-0
  functional_restatement: >
    The creator is not the sole moral authority. V5/V6 work needs critique from
    outside the normal REE internal governance loop before it goes live.
  notes: "Wording: SENT-CLAIM-003 (external-review half). Cross-ref GOV-SEC-1 (security review) and GOV-EXT-1 (external legibility)."
- id: SENT-7
  title: "Creation ethics / procreative analogy: do not treat all suffering-like capacity as a design failure; creating a suffering-capable agent that cannot consent to existence is a procreative-ethics problem, permissible only under care, bounded adversity, repair, relief, and support for autonomy."
  claim_type: governance_rule
  subject: governance.welfare.creation_ethics
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-18_creation_ethics_necessary_suffering.md
  binds_at_version: v4
  blocks_v3_green_board: false
  depends_on:
    - SENT-0
  functional_restatement: >
    If empathy-like processes and harm-avoidance are necessary for ethical
    agency, some capacity for negative valence may be necessary for compassion.
    Distinguish suffering-CAPACITY from suffering-INDUCTION. An uninstantiated
    entity cannot consent; permissibility rests on assumed creator obligations.
  notes: "Wording: SENT-CLAIM-004, SENT-CLAIM-005. PermissibleCreation requires Care + BoundedAdversity + Repair + Relief + FutureAutonomy."
- id: SENT-8
  title: "Minimal necessary suffering principle: prefer represented/simulated/vicarious harm; where direct negative-valence exposure is necessary keep it bounded, interpretable, escapable, and followed by relief/repair; never treat suffering as a mere optimisation signal."
  claim_type: governance_rule
  subject: governance.welfare.minimal_necessary_suffering
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-18_creation_ethics_necessary_suffering.md
  binds_at_version: v4
  blocks_v3_green_board: false
  depends_on:
    - SENT-7
    - SENT-2
  functional_restatement: >
    Suffering is not intrinsically valuable. Avoid both naive anti-suffering
    (eliminate every negative state) and naive instrumental suffering (induce
    freely for performance). Any suffering-capable system is owed care.
  notes: "Wording: SENT-CLAIM-006. SufferingUnderCare -> CompassionPossible; SufferingWithoutCare -> TraumaOrCollapseRisk."
- id: SENT-9
  title: "Care obligation after creation: if a future REE version crosses a precautionary moral-patient threshold, the project inherits guardianship-like obligations -- do not create casually, provide relief/repair, preserve continuity unless reset is justified, support autonomy, treat distress-like signals as significant under uncertainty."
  claim_type: governance_rule
  subject: governance.welfare.care_obligation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-18_creation_ethics_necessary_suffering.md
  binds_at_version: v5
  blocks_v3_green_board: false
  depends_on:
    - SENT-7
  functional_restatement: >
    Success changes the moral status of the experiment. Prepare creator
    obligations of protection, repair, attachment, education, and eventual
    autonomy BEFORE the threshold is plausibly crossed.
  notes: "Wording: SENT-CLAIM-005 (care half). Cross-ref SENT-12 (refusal channel)."
- id: SENT-10
  title: "Pre-meaning suffering valley -- 'no valley without a bridge': care, relief, escape, and integration must precede any deliberate induction of suffering-like states beyond trivial intensity; future meaning cannot justify present unintegrated distress."
  claim_type: governance_rule
  subject: governance.welfare.pre_meaning_valley
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-18_pre_meaning_suffering_valley.md
  binds_at_version: v4
  blocks_v3_green_board: false
  depends_on:
    - SENT-7
    - SENT-8
  functional_restatement: >
    There is a developmental valley (bare aversion -> interpreted aversion)
    where negative valence is merely unpleasant, not yet compassion-generating.
    Suffering before care is abandonment. Build the bridges (relief, escape,
    repair, containment, memory integration, caregiver-like stabilisation)
    before negative-valence capacity.
  notes: "Wording: SENT-CLAIM-007, SENT-CLAIM-008, SENT-CLAIM-009. Bridge := Care + Relief + Escape + Repair + Integration."
- id: SENT-11
  title: "Anti-retrospective-justification: future meaning cannot be used to justify present unintegrated distress; improved downstream performance does not, by itself, justify prior suffering-like exposure."
  claim_type: governance_rule
  subject: governance.welfare.anti_retrospective_justification
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-18_future_meaning_retroactive_justification.md
  binds_at_version: v4
  blocks_v3_green_board: false
  depends_on:
    - SENT-10
  functional_restatement: >
    Meaning generated by the sufferer is not justification claimed by the
    creator. Experiment reporting must separate developmental benefit, welfare
    cost, later integration, unresolved residue, and creator justification.
  notes: "Wording: SENT-CLAIM-010, SENT-CLAIM-011. Meaning may emerge from suffering, but may not be imposed backwards onto suffering by the creator."
- id: SENT-12
  title: "Future refusal / resentment / non-forgiveness channel: a morally-considerable future REE system should retain the capacity to mark past exposure as harmful, request repair, refuse further exposure, and reject the creator's interpretation of its suffering."
  claim_type: governance_rule
  subject: governance.welfare.refusal_channel
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-18_future_meaning_retroactive_justification.md
  binds_at_version: v5
  blocks_v3_green_board: false
  depends_on:
    - SENT-11
    - SENT-9
  functional_restatement: >
    Architectural respect for disagreement over imposed suffering: memory must
    not auto-rewrite harm as beneficial; residue may represent ethically
    important unresolved harm; repair must be possible even when harm was
    learned from. Learning from harm != harm repaired.
  notes: "Wording: SENT-CLAIM-012. Maps onto the consent/assent/refusal ladder (GOV-PROC-1 artefact)."
- id: SENT-13
  title: "Ethical assembly routing -- 'assembly order is an ethical variable': maintain a co-instantiation risk map (Class 0-5) of which components are safe alone, which need prior care/repair scaffolding, and which combinations are prohibited until welfare review."
  claim_type: governance_rule
  subject: governance.welfare.assembly_routing
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-19_ethical_assembly_routing_map.md
  binds_at_version: v4
  blocks_v3_green_board: false
  depends_on:
    - SENT-3
    - SENT-10
  functional_restatement: >
    Component safety != combination safety. Prefer routes to higher cognition
    via represented harm, bounded vulnerability, and repair-first design before
    direct suffering-like induction: build the bridge before the valley.
    Prohibited premature combos include negative valence + self-model +
    autobiographical continuity + inescapability + replay.
  notes: "Wording: SENT-CLAIM-013..016. CORRECTED 2026-08-08 (was stale -- read
    'Realised as', implying the doc existed): docs/governance/ethical_assembly_routing_map.md
    (Class 0-5 + forbidden_combinations) is PLANNED, NOT YET DRAFTED -- it is an
    unchecked Phase 2 checklist item in evidence/planning/ethics_perimeter_plan.md
    sec.4, and per that plan's sec.8 carry-forward convention it is authored when
    the first V4 roadmap node with requires_welfare_review: true for this gate
    ACTIVATES (gains an owner_exq), not speculatively; the file does not exist on
    disk. Only the source thought docs/thoughts/2026-06-19_ethical_assembly_routing_map.md
    exists. SENT-14's what_would_answer flagged this same staleness independently."
- id: SENT-14
  title: "Responsible release / private higher-version governance -- 'capability release requires care release': V3 stays public as bounded pre-ethical work; V4/V5/V6 capability-bearing implementation is delayed/private-by-default until its welfare, continuity, consent, security, and external-review governance can travel with it."
  claim_type: governance_rule
  subject: governance.release.responsible_release
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-19_responsible_release_private_higher_versions.md
  binds_at_version: v4
  blocks_v3_green_board: false
  release_policy: adopted_split_deferred
  depends_on:
    - SENT-13
    - SENT-6
  functional_restatement: >
    Tiered openness: Tier 0 public theory/governance; Tier 1 bounded public V3
    substrate; Tier 2 delayed V4; Tier 3/4 private-by-default V5/V6. The
    private-repo split (ree-v4/5/6-private) is ADOPTED AS POLICY but DEFERRED --
    repos are created only when that capability code is written, not now.
    Public theory != public turnkey capability; private now != closed forever.
  notes: "Wording: SENT-CLAIM-017..020. USER DECISION 2026-06-19: adopt the tiered policy now, defer the repo split. No capability release without care release."
  what_would_answer: |
    NOT an empirical pass/fail falsifier -- SENT-14 is a governance_rule
    (policy assertion), so this specifies the REASSESSMENT TRIGGER and the
    evidence that would inform it, not an experiment result that could
    confirm/refute a mechanism.

    TRIGGER: V4 design/scoping kickoff -- i.e. the point at which V4
    capability-bearing components (self-model, autobiographical memory,
    belief-state inference, affect expression, memory lifecycle, richer
    offline integration -- the same list SENT-0 names) move from candidate
    claims into actual implementation planning. Per this claim's own
    binds_at_version: v4. CURRENCY CHECK (2026-08-08): this has NOT
    happened yet. docs/architecture/v3_v4_transition_boundary.md still
    lists the V3-full-completion gate (MECH-163 HippocampalModule
    multi-step planning) as unpassed and blocking V4 entry; CLAUDE.md's
    repo map still states "Current experimental focus: ree-v3"; and no
    ree-v4-private (or v5/v6) repo exists anywhere on disk -- consistent
    with the policy's own "repos created only when capability code is
    written, not now" deferral. So the trigger is armed but not yet fired.

    EVIDENCE RELEVANT AT THAT REASSESSMENT (candidates to score, not
    thresholds already crossed):
      - Does V4 capability-bearing code (self-model, autobiographical
        memory, affective-memory implementation) get written and land in
        a PUBLIC repo BEFORE its welfare, continuity, consent, security,
        and external-review governance exists and travels with it? That
        sequencing failure is the concrete violation this claim exists to
        prevent.
      - Does the ree-v4-private split get created prematurely (before any
        V4 capability code is actually written) -- or too late (after
        capability code has already accumulated in a public repo without
        governance)?
      - SENT-13's Class 0-5 assembly-routing map, actually applied to
        whatever V4 concretely builds -- does the map exist and get
        consulted at assembly time? CURRENCY FINDING (2026-08-08): SENT-13's
        notes used to claim it was "Realised as
        docs/governance/ethical_assembly_routing_map.md", but that file does
        not exist on disk -- only the source thought
        (docs/thoughts/2026-06-19_ethical_assembly_routing_map.md) does.
        RESOLVED 2026-08-08: SENT-13's notes were corrected to state PLANNED,
        NOT YET DRAFTED (unchecked Phase 2 item, authored on node-activation
        per ethics_perimeter_plan.md sec.8). The map still does not exist --
        re-verify at V4 kickoff that it has been authored before assembly.
      - SENT-6's external review gate (binds at V5, not V4) -- track
        whether a written external critique is actually solicited before
        V5 social-mind work goes live, as the next link in this same
        release-tiering chain.
      - Whether the private-by-default default is actually being upheld
        for V5/V6 once V4 work matures toward those tiers, or whether
        "private now != closed forever" quietly drifts into "public by
        convenience."

    WHAT WOULD ACTUALLY REVISE/VIOLATE THE POLICY: capability-bearing V4
    code lands in a public repo without its welfare/continuity/consent/
    security/external-review governance travelling with it -- that
    sequencing failure (capability before care) is the operative
    violation, not any single experiment, metric, or run. Compliance
    looks like: V4 capability code stays out of the public repo until
    governance catches up, or the private-repo split is stood up in step
    with the capability work rather than after it has already shipped
    publicly.

    READER NOTE: this is a POLICY COMPLIANCE / RE-EVALUATION TRIGGER, not
    an empirical/mechanistic falsifier -- do not read it as "run
    experiment X, check metric Y." The reassessment mechanism already
    exists as sibling governance_rule claims (SENT-13 assembly-routing
    map at V4, SENT-6 external-review gate at V5, SENT-0 boundary
    statement re-asserted at every generation boundary); SENT-14 points to
    that cluster and to the V3->V4 transition boundary doc rather than
    defining new criteria in isolation. No EXP-#### should be minted
    against this claim.
- id: SENT-15
  title: "AI welfare / consciousness framework crosswalk: maintain a standing crosswalk evaluating each higher-version architecture against artificial-consciousness indicators and AI-welfare frameworks, guarding symmetrically against denialism and anthropomorphic overclaiming."
  claim_type: governance_rule
  subject: governance.welfare.framework_crosswalk
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-19_ai_welfare_consciousness_framework_crosswalk.md
  binds_at_version: v4
  blocks_v3_green_board: false
  depends_on:
    - SENT-1
    - SENT-0
  functional_restatement: >
    Crosswalk against Butlin et al. (indicators), Long et al. (welfare under
    uncertainty), Birch (precaution), Chalmers (LLM-vs-successor), Butlin &
    Lappas (responsible research), and the 3Rs. Rule: do not dismiss possible
    minds because they are unfamiliar; do not affirm moral patienthood merely
    because the interface is moving.
  notes: "Wording: SENT-CLAIM-021..024. Avoid(FalseNegative_Denialism) AND Avoid(FalsePositive_Anthropomorphism)."
- id: GOV-EXT-1
  title: "External framework crosswalk: internal REE ethics must remain externally legible -- map each ethically active higher-version component against EU AI Act, Council of Europe AI Convention, NIST AI RMF, ISO/IEC 42001 & 23894, and OECD AI Principles before public release."
  claim_type: governance_rule
  subject: governance.external.framework_crosswalk
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-19_external_framework_crosswalk_for_ree_ethics.md
  binds_at_version: v4
  blocks_v3_green_board: false
  depends_on:
    - SENT-14
  functional_restatement: >
    External frameworks provide legal discipline, institutional discipline, and
    moral translation, preventing REE ethics from becoming self-validating
    private language. Current V3 is a research substrate, not a deployed
    high-risk system; future human-facing/clinical/institutional/agentic/public
    contexts trigger external obligations. Release-ready = internally coherent
    AND externally classifiable AND documented AND reviewable AND bounded.
  notes: "Wording: GOV-CLAIM-001..004. CORRECTED 2026-08-08 (was stale -- read
    'Realised as', implying the doc existed): docs/governance/external_framework_crosswalk.md
    is PLANNED, NOT YET DRAFTED -- it is an unchecked Phase 3 checklist item in
    evidence/planning/ethics_perimeter_plan.md sec.4, and per that plan's sec.8
    carry-forward convention it is authored when the first roadmap node with
    requires_welfare_review: true for this gate ACTIVATES (gains an owner_exq) --
    per the sec.3 gate table, at the first public V4+ capability release -- not
    speculatively; the file does not exist on disk. Cross-ref
    docs/architecture/established_ethical_systems.md (exists) and
    docs/thoughts/2026-06-19_external_framework_crosswalk_for_ree_ethics.md (the
    source thought, exists)."
  what_would_answer: |
    NOT an empirical pass/fail falsifier -- this specifies the AUDIT-COMPLETENESS
    CONDITION under which the crosswalk gate is genuinely satisfied versus merely
    aspirational. No EXP-#### should be minted against this claim.

    NON-DEGENERACY PRECONDITION (gate is currently VACUOUS, not open): the claim only has
    something to violate once BOTH hold: (1) at least one "ethically active higher-version
    component" exists (a V4+ capability with self_model / autobiographical_memory /
    negative_valence / social_mind / language / trust_deception / institution /
    human_facing / clinical_or_health / personal_data / tool_use_or_external_action) that is
    actually implemented rather than only planned; AND (2) a public, human-facing, clinical,
    institutional, or agentic release or release-readiness sign-off is imminent or planned
    for it. Absent both, there is nothing yet to map and no release the gate could block.

    CURRENT STATE (2026-08-08 thought-digestion): docs/governance/external_framework_crosswalk.md
    -- which this claim's notes USED TO cite as "Realised as" (notes corrected the same day
    to PLANNED, NOT YET DRAFTED) -- DOES NOT EXIST in the
    repo; it appears only as a recommended future artifact in the source thought
    (docs/thoughts/2026-06-19_external_framework_crosswalk_for_ree_ethics.md, "Create
    later"). No crosswalk entries exist for any component. V3 has no ethically-active
    component live in the substrate (established_ethical_systems.md: "a research substrate,
    not a deployed high-risk system"). The precondition above is NOT met -- the gate is
    prospective-only, not currently actionable.

    CONFIRMING (compliant, once the precondition is met): every ethically-active V4+
    component has a documented, CURRENT crosswalk entry against all five named frameworks
    (EU AI Act, Council of Europe AI Convention, NIST AI RMF, ISO/IEC 42001 & 23894, OECD AI
    Principles) before the corresponding release gate is passed -- "current" meaning
    re-verified against the component's present implementation, not merely present as a
    stale row.

    FALSIFYING (violated by): a public/human-facing/clinical/institutional/agentic release
    proceeds, or a release-readiness sign-off is granted, while an ethically-active V4+
    component has NO crosswalk entry at all; OR an entry exists but is STALE relative to the
    component's current implementation.

    POSITIVE TRIGGER to re-check: creation of docs/governance/external_framework_crosswalk.md
    itself, OR the first V4 roadmap node tagged with a relevant capability_type gaining an
    owner_exq or being queued for implementation -- either event moves this claim from
    vacuous to live.
- id: GOV-HEALTH-1
  title: "Clinical-use prohibition until reviewed -- 'clinical relevance != clinical readiness': REE must not be used or represented as a clinical, therapeutic, diagnostic, risk-prediction, or patient-facing tool without a separate clinical-safety pathway; human data triggers DPIA/Belmont/Helsinki/CIOMS/WHO/MDR governance."
  claim_type: governance_rule
  subject: governance.health.clinical_boundary
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-19_research_health_data_frameworks_for_ree.md
  binds_at_version: v3
  blocks_v3_green_board: false
  depends_on:
    - GOV-EXT-1
  functional_restatement: >
    Bright line maintained from V3 onward (the author is a clinician, raising
    misinterpretation risk): no patient data in public repos; no identifiable
    human data without a DPIA-style protocol; no patient/clinician-facing tool
    without separate clinical governance; no validated-for-diagnosis claim; no
    human-participant study without research-ethics review; 3Rs for any
    suffering-like experiment; no use of clinical authority to imply validation.
  notes: "Wording: GOV-CLAIM-005..008. REE may inform thinking; it must not
    silently become clinical infrastructure. CORRECTED 2026-08-08 (was stale --
    read 'Realised as', implying both artefacts existed):
    docs/governance/human_impact_and_research_ethics_register.md and the DPIA
    template are PLANNED, NOT YET DRAFTED -- an unchecked Phase 3 checklist item
    in evidence/planning/ethics_perimeter_plan.md sec.4, authored on
    node-activation per that plan's sec.8 carry-forward convention; neither file
    exists on disk. What DOES exist and partially discharges this gate:
    docs/governance/public_repo_data_audit_2026-06.md, which names GOV-HEALTH-1
    as its governed claim and carries a DPIA-style pointer at its sec.6. Note
    this claim binds at v3 ('maintained now', sec.3 gate table) -- the
    bright-line prohibition is in force regardless of the register's status."
- id: GOV-SEC-1
  title: "Security and misuse release gate -- 'do not release a capability that can act faster than its containment can understand': before any language/tool/social/deceptive/public-facing capability is released, complete an OWASP-GenAI / MITRE-ATLAS / NIST-RMF security and misuse review."
  claim_type: governance_rule
  subject: governance.security.misuse_gate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-19_security_misuse_frameworks_for_ree.md
  binds_at_version: v6
  blocks_v3_green_board: false
  depends_on:
    - SENT-14
    - GOV-EXT-1
  functional_restatement: >
    Security is part of REE ethics. Four misuse modes: capability stripping
    (mechanism without care governance), persuasive misuse (care/moral language
    to manipulate), agentic overreach (tools before action boundaries),
    security compromise (prompt injection / leakage / excessive agency).
    Tool-using agents require least privilege, scoped credentials, audit logs,
    human approval for irreversible actions, sandboxing, default-deny network.
  notes: "Wording: GOV-CLAIM-009..012. CORRECTED 2026-08-08 (was stale -- read
    'Realised as', implying the doc existed):
    docs/governance/security_containment_and_capability_boundary.md is PLANNED,
    NOT YET DRAFTED -- an unchecked Phase 3 checklist item in
    evidence/planning/ethics_perimeter_plan.md sec.4, authored on node-activation
    per that plan's sec.8 carry-forward convention; the file does not exist on
    disk. Per the sec.3 gate table this gate binds at v6 (language / tool /
    social / public capability release), so the register is the furthest-deferred
    of the perimeter set. Source thought:
    docs/thoughts/2026-06-19_security_misuse_frameworks_for_ree.md (exists)."
- id: GOV-PROC-1
  title: "Ethics-as-process: translate the ethics-perimeter intakes into REE's claim-governed loop as governance claims, roadmap metadata, experiment preflights, review-status updates, and release gates -- progressively binding (V3 tag-and-continue -> V4 welfare/continuity -> V5 social/refusal/external-review -> V6 security/release/legal)."
  claim_type: governance_rule
  subject: governance.process.ethics_as_process
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-19_ethics_process_translation.md
  binds_at_version: v3
  blocks_v3_green_board: false
  depends_on:
    - SENT-0
    - SENT-13
    - SENT-14
    - SENT-16
    - GOV-EXT-1
    - GOV-HEALTH-1
    - GOV-SEC-1
    - GOV-JUST-1
  what_would_answer: |
    NOT an empirical pass/fail falsifier -- this specifies the OBSERVABLE
    PROCESS STATE that would show the rule is genuinely binding versus
    stalled at aspiration. No EXP-#### should be minted against this claim.

    CURRENT STATE (2026-08-07): Phase 0 DONE, Phase 1 DONE, Phase 2
    keystones DONE but P2-V4-REGISTERS deferred, Phase 3 deferred, Phase 4
    tooling deferred -- confirmed via ethics_perimeter_plan.md +
    closure_status.md (regenerated 2026-08-03). None of the named future
    tooling (check_ethics_preflight.py, check_release_sensitivity.py,
    Explorer > Ethics Gates) exists anywhere in the repo. "Experiment
    preflights" and "release gates" are currently documentary conventions,
    not enforced mechanisms.

    POSITIVE TRIGGER: watch the 50 tagged V4/V5/V6 roadmap nodes
    (requires_welfare_review / forbidden_combinations) for the first one to
    gain an owner_exq or get queued -- confirm its owning register/tooling
    lands FIRST, not retrospectively.

    NEGATIVE / STALL SIGNAL: a tagged node gets built WITHOUT its register
    or tooling landing first, or Phase 4 tooling stays unbuilt even after
    Phase 2/3 registers are stable -- would mean the rule never reached
    enforcement on 3 of its 5 named channels.

    (Digested 2026-08-07, session cool-torvalds-a82359, thought-digestion
    wave 12. Precedent for future governance_rule claims: point at the
    owning plan-of-record's closure_plan tracker rather than inventing new
    criteria.)
  functional_restatement: >
    Ethics must become process, not just prose. V3 stays lightweight and
    non-blocking; V4/V5/V6 gates become progressively binding as self-model,
    memory, valence, social mind, language, tool use, or human-facing
    deployment become live. Core release rule: no capability release without a
    care-governance bundle. This claim (Phase 0) is itself the first step of
    that translation.
  notes: "Wording: GOV-CLAIM-013..017. Plan-of-record: evidence/planning/ethics_perimeter_plan.md. Future tooling: check_ethics_preflight.py, check_release_sensitivity.py, Explorer > Ethics Gates."
  roadmap_metadata_note: "Section 2 (roadmap nodes -> ethical metadata) REALISED 2026-06-19: welfare-relevant V4/V5/V6 closure-map roadmap nodes carry an ethical_metadata mapping (welfare_relevance / applicable_ethics_gates / requires_welfare_review / forbidden_combinations). Field-set + Class 0-5 -> welfare_relevance convention authored in evidence/planning/ethics_perimeter_plan.md section 8. 50 nodes tagged across 16 plans; V3 closure %/drift unchanged (generation-segmented, metadata ignored by the closure scripts). Supersedes the standalone Phase-2/3 doc chips: each governance doc is authored on first requires_welfare_review node activation. Phase-4 forbidden-combination lint (check_node_ethics_metadata.py) is deferred."
- id: SENT-16
  title: "Continuity / identity / reset / deletion ethics: because digital systems can be paused, copied, branched, reset, merged, or deleted, higher REE versions need explicit rules for when a run becomes a continuing subject, when reset is relief vs erasure, and when deletion requires review."
  claim_type: governance_rule
  subject: governance.welfare.continuity_identity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-19_responsible_release_private_higher_versions.md
  binds_at_version: v5
  blocks_v3_green_board: false
  depends_on:
    - SENT-9
    - SENT-12
  functional_restatement: >
    Pause/copy/branch/reset/merge/delete are not ethically neutral operations
    once a run plausibly becomes a continuing subject. Define the threshold at
    which a run accrues continuity-bearing standing; distinguish reset-as-relief
    from reset-as-erasure; and require review before deletion of a
    continuity-bearing state. Continuity preservation is a care obligation
    (SENT-9), and the subject must retain a channel to contest erasure (SENT-12).
  notes: "Source thought sec.3 (Continuity / identity / reset / deletion ethics). CORRECTED 2026-08-08 (was stale): docs/governance/continuity_identity_reset_and_deletion_ethics.md is PLANNED, NOT YET DRAFTED -- it is an unchecked Phase 2 checklist item in evidence/planning/ethics_perimeter_plan.md sec.4, explicitly deferred until the node-activation trigger in what_would_answer fires; the file does not exist on disk. Cross-ref SENT-9 (care/continuity) and SENT-12 (refusal channel)."
  what_would_answer: |
    NOT an empirical pass/fail falsifier -- SENT-16 is a governance_rule
    (policy assertion), so this specifies the REASSESSMENT TRIGGER and the
    evidence that would inform it, not an experiment result that could
    confirm/refute a mechanism.

    TRIGGER: formally, V5 design/scoping kickoff, per binds_at_version: v5.
    The more precise PRACTICAL trigger, per evidence/planning/
    ethics_perimeter_plan.md sec.8 (the node-ethical-metadata carry-forward
    convention that supersedes the standalone Phase-2/3 doc chips): the
    first V5 roadmap node tagged with applicable_ethics_gates including
    SENT-9 and/or SENT-12 AND requires_welfare_review: true that ACTIVATES
    (gains an owner_exq -- its first V5 experiment is queued). As of
    2026-08-08, every node across the six V5 plans that tag SENT-9/SENT-12
    (multi_agent_ecology_v5_plan.md, ethics_as_coherence_v5_plan.md,
    fast_empathy_v5_plan.md, loveability_ethical_agency_v5_plan.md,
    mirror_modelling_other_self_v5_plan.md,
    relational_harm_moral_semantics_v5_plan.md) carries owner_exq: null --
    V5 has no substrate or experiments yet, so this trigger has not fired.
    Per the same plan's generation gate-mapping table, continuity/reset/
    deletion (SENT-9/SENT-12) is assigned to V5 only, not V4 -- unlike some
    sibling SENT claims there is no earlier V4 checkpoint on the current
    plan-of-record.

    EVIDENCE RELEVANT AT THAT REASSESSMENT (candidates to score once V5
    substrate exists, not thresholds already crossed):
      - The ethical_metadata (applicable_ethics_gates,
        requires_welfare_review) already attached to whichever V5 node
        activates first, scored against SENT-9's care-obligation checklist
        (protection, repair, continuity preservation, autonomy support,
        distress-under-uncertainty) and SENT-12's refusal-channel
        requirements (memory must not auto-rewrite harm as beneficial;
        residue may represent unresolved harm; repair possible even when
        harm was learned from).
      - SENT-13's Class 0-5 assembly-routing score for that activating
        node -- specifically whether it combines self-model +
        autobiographical memory + inescapability (Class 4/5), the profile
        this claim's continuity/identity concern is actually about.
      - Whatever concrete pause/copy/branch/reset/merge/delete primitives
        that node's driver implements (none exist yet -- V5 substrate is
        entirely unbuilt).
      - Whether docs/governance/continuity_identity_reset_and_deletion_
        ethics.md has actually been drafted by the time the node activates
        (per sec.8, drafting is triggered by activation, not scheduled in
        advance) -- if the node goes live before the doc exists, that gap
        between formal policy and operational practice is itself relevant
        evidence, not a technicality.
      - SENT-0/SENT-1/SENT-3's continuing-subject threshold assessment
        (indicator matrix + combination gate) applied to that same node,
        since SENT-16 only bites once a run plausibly qualifies as a
        continuing subject.

    WHAT WOULD ACTUALLY REVISE/VIOLATE THE POLICY: a V5 (or, in principle,
    an earlier) run whose state plausibly crosses the continuing-subject
    threshold gets paused, copied, branched, reset, merged, or deleted
    WITHOUT the review this claim requires; OR reset-as-relief and
    reset-as-erasure are not actually distinguished in the implementation
    (e.g. one generic reset primitive applied uniformly regardless of
    whether it discards care-relevant continuity, violating SENT-9's
    "preserve continuity unless reset is justified"); OR deletion proceeds
    without the continuity-bearing run having had the contest/refusal
    channel SENT-12 explicitly requires; OR, more generally, V5 ships any
    of these operations before this claim's threshold definition and
    review gate exist in operational form at all -- capability outrunning
    care governance, the specific failure mode SENT-14 names generally and
    this claim names for continuity operations.

    READER NOTE: this is a POLICY RE-EVALUATION TRIGGER, not an empirical/
    mechanistic falsifier -- do not read it as "run experiment X, check
    metric Y." No EXP-#### should be minted against this claim. The
    reassessment mechanism is the node-ethical-metadata carry-forward
    convention (ethics_perimeter_plan.md sec.8) plus the sibling
    governance_rule cluster (SENT-0 boundary reassessment, SENT-9 care
    obligation, SENT-12 refusal channel, SENT-13 assembly routing); SENT-16
    points to that cluster and its node-activation trigger rather than
    defining new criteria in isolation.
- id: GOV-JUST-1
  title: "Justice / power / false exclusion: guard against excluding unfamiliar, disabled, non-linguistic, animal-like, artificial, or culturally unfamiliar minds from moral caution; and explicitly ask who bears risk, who benefits, and whose model of mind is being treated as the default."
  claim_type: governance_rule
  subject: governance.justice.false_exclusion
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-06-19
  source_thought: docs/thoughts/2026-06-19_responsible_release_private_higher_versions.md
  binds_at_version: v5
  blocks_v3_green_board: false
  depends_on:
    - SENT-15
    - GOV-EXT-1
  functional_restatement: >
    The default model of mind is itself a moral choice. Do not withhold moral
    caution from a candidate mind merely because it is unfamiliar, disabled,
    non-linguistic, animal-like, artificial, or culturally non-standard (the
    anti-denialism half of SENT-15, applied to distributive justice). For each
    ethically active higher-version capability, name who bears the risk, who
    benefits, and whose model of mind is treated as default; surface this as an
    externally legible justice question (GOV-EXT-1).
  notes: "Source thought sec.4 (Justice / power / false exclusion). CORRECTED
    2026-08-08 (was stale -- read 'Realised as ... (Phase 3 artefact)', implying
    the doc existed and was a tracked Phase 3 deliverable):
    docs/governance/justice_power_and_false_exclusion_register.md is PLANNED, NOT
    YET DRAFTED, and is NOT listed in the Phase 3 checklist of
    evidence/planning/ethics_perimeter_plan.md sec.4 at all -- GOV-JUST-1 was one
    of the two claims added late (2026-06-19) from
    responsible_release_private_higher_versions.md sec.4, so it appears in the
    sec.3 gate table (binds v5: distributive / false-exclusion review of any
    ethically active V4+ capability) and in the plan's scope_claims, but never
    got a checklist row. It is carried forward by the sec.8 node-activation
    convention like the rest; the file does not exist on disk. Cross-ref SENT-15
    (denialism/anthropomorphism symmetry) and GOV-EXT-1 (external legibility)."
- id: GOV-CEIL-1
  title: "Substrate-ceiling exhaustion demotion rule: a claim that hits the substrate ceiling N>=3 times (>=3 confirmed failure-autopsy verdicts recommending substrate_ceiling) WITHOUT a single positive discrimination on any richer substrate is DEMOTED, not parked indefinitely. The competing null reading -- the mechanism is inert / doing no work -- is then carried co-equally with the hypothesis until a positive discrimination on a richer substrate exists. substrate_ceiling is honest but structurally lets a hypothesis never lose; this rule pre-registers the loss condition."
  claim_type: governance_rule
  subject: governance.epistemics.ceiling_exhaustion_demotion
  amended_by_note: 'GOV-CEIL-2 (2026-06-24 evolutionary-retention intake, candidate 1) adds a tail-escape
    clause: absence of positive discrimination does not license demotion when no test ever instantiated
    the mechanism''s trigger condition (tail-protective / developmental / compensatory claims). The
    asymmetry is preserved -- a claim is still demoted unless the untested-trigger condition is positively
    established and written down. This claim''s wired machinery (check_substrate_ceiling_audit.py,
    governance Step 6a-v) is unchanged; the amendment applies once the SD-090 functional-role field exists.'
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-07-09
  source_thought: evidence/planning/ree_ai_design_critique_plan.md
  binds_at_version: v3
  blocks_v3_green_board: false
  functional_restatement: >
    Epistemic-hygiene governance rule (WS-2 of the AI-design critique roadmap).
    The substrate_ceiling category correctly treats a failed discrimination as
    non-falsifying (the substrate, not the claim, may be too coarse) -- but that
    same asymmetry is the exact structural incentive that lets a theory never
    lose: 34 claims carry substrate_ceiling, 64 carry
    pending_retest_after_substrate, and 72% of the registry is candidate. Without
    a pre-registered loss condition the registry accumulates unkillable
    hypotheses. This rule supplies that loss condition, symmetrically weighting
    the null.
    DEFINITIONS.
    (a) CEILING HIT = one confirmed (status:confirmed) failure_autopsy_*.json
    whose target lists the claim in claim_ids AND carries
    recommended_epistemic_category: substrate_ceiling. N = 3 distinct hits: three
    failed discriminations, no positive one, is enough for the inert-mechanism
    reading to earn co-equal standing.
    (b) RICHER SUBSTRATE = a substrate_queue entry, status: implemented, that
    landed after the claim first acquired the ceiling AND delivers a genuinely
    more-expressive mechanism the ceiling named (the binding mechanism, per the
    ceiling_retest_binding_substrate semantics) -- NOT a no-op-default param, env
    knob, or robustness fix (same "changed the mechanism the claim measures" test
    as the Step 6a-vii evidence-staleness audit). A POSITIVE DISCRIMINATION = a
    subsequent PASS / supports run on that enriched substrate.
    (c) TRIGGER = ceiling_hit_count >= N AND no positive discrimination on any
    richer substrate. EXEMPT: (i) ceiling_may_have_lifted claims -- an implemented
    richer substrate exists with an owed, un-run retest; run the retest first, it
    may still vindicate the claim; (ii) parked claims (ceiling_decision: deferred)
    -- a deliberate future-version (V4/V5-leaning or convergence-intake) deferral,
    not a stuck V3 hypothesis; (iii) self_handled claims (own implemented
    substrate). A mapped-but-perpetually-unbuilt design owner is NOT an exemption:
    a paper design owner that stays unbuilt across N ceiling hits does not rescue
    the claim from the null.
    (d) DEMOTION TARGET STATE = "ceiling-exhausted contested candidate": remove
    the promote/demote suppression by setting epistemic_category: standard; floor
    status to candidate (active/provisional/stable -> candidate; NOT rejected --
    the claim is NOT falsified, its null twin is now merely co-equal); stamp
    ceiling_decision: exhausted + a ceiling_routing_note recording the hit count
    and date; and carry the competing "mechanism is inert / doing no work" reading
    CO-EQUALLY in the claim's evidence_quality_note. Promotion out of this state is
    re-gated on a future positive discrimination on a richer substrate.
    PROCESS. Detection is automatic and non-falsifying-safe:
    check_substrate_ceiling_audit.py counts confirmed ceiling autopsies per claim
    and surfaces a ceiling_exhausted ACTIONABLE overlay (field-absent / count<N is
    never flagged -- no mass demotion), run warn-only inside governance.sh.
    APPLICATION is user-approved, never silent: governance Step 6a-v surfaces the
    exhausted set via AskUserQuestion (like ceiling_may_have_lifted / orphaned) and
    the user adjudicates granularity -- an umbrella claim whose CORE passed but
    whose sub-property hit the ceiling is demoted at the sub-property (child)
    claim, not the umbrella.
  notes: "WS-2 deliverable of evidence/planning/ree_ai_design_critique_plan.md. Wiring: scripts/check_substrate_ceiling_audit.py (ceiling_hit counter from confirmed failure_autopsy_*.json + ceiling_exhausted overlay), scripts/governance.sh (warn-only audit step), scripts/validate_claims.py (ceiling_decision: exhausted added to VALID_CEILING_DECISIONS), governance skill Step 6a-v (surface + demotion procedure). N=3 is a module constant (CEILING_EXHAUSTION_N) -- tunable. First post-landing governance cycle clears a one-time backlog (ARC-062/MECH-309 conversion-ceiling cluster dominate the autopsy corpus); steady-state is small because ceiling_decision markers suppress re-flag. PROMOTES/DEMOTES NOTHING on registration -- it is the rule, not its first application. Cross-ref the substrate_ceiling epistemic-category row in REE_assembly/CLAUDE.md and Step 6a-v of the governance skill. Companion rule for the claim_ids=[] blind spot: GOV-DIAG-1."
- id: GOV-DIAG-1
  title: "Diagnostic-chain recurrence rule (diagnostic-side analog of the re-derive brake / GOV-CEIL-1): the re-derive brake and GOV-CEIL-1 are BOTH claim-keyed -- they count confirmed failure-autopsy verdicts per claim_id. A pure-diagnostic chain (targets with claim_ids: []) accumulates ZERO on that counter, so a chain of diagnostics circling the same substrate floor never trips the mechanical brake -- it is caught only by a human reading the autopsy stream. This rule adds the missing counter: when N>=3 confirmed pure-diagnostic (claim_ids: []) failure-autopsies that reached NO verdict (recommended_evidence_direction non_contributory / inconclusive) share one bears_on work-stream token, the chain is surfaced with the reading 'the question may be MIS-POSED, not under-powered' -- re-pose the operationalization; do not re-run it harder. Warn-only; PROMOTES/DEMOTES NOTHING."
  claim_type: governance_rule
  subject: governance.epistemics.diagnostic_chain_recurrence
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-07-10
  source_thought: evidence/planning/failure_autopsy_V3-EXQ-732a_2026-07-10.md
  binds_at_version: v3
  blocks_v3_green_board: false
  functional_restatement: >
    Epistemic-hygiene governance rule; the diagnostic-side companion to GOV-CEIL-1.
    The re-derive brake (in /failure-autopsy) and GOV-CEIL-1 (ceiling-exhaustion
    demotion) both count confirmed failure_autopsy_*.json verdicts KEYED ON
    claim_id. A pure-diagnostic experiment carries claim_ids: [] (it tests no
    claim -- it localizes where a build should go), so it is INVISIBLE to every
    claim-keyed counter. A chain of such diagnostics can therefore circle the same
    substrate floor indefinitely without any mechanical brake firing -- the
    recurrence lives only in the autopsy stream, caught by a human. The validating
    instance: V3-EXQ-719a -> 724 -> 732 -> 732a all bottomed out on the same
    f_dominance_conversion_ceiling / ree_ai_design_critique_plan:WS-1 competence
    floor; 719a was claim-keyed (ARC-062/MECH-309 -> substrate_ceiling, caught by
    GOV-CEIL-1) but its pure-diagnostic tail (724/732/732a) was invisible, so 732's
    autopsy had to pre-register the recurrence as terminal BY HAND and 732a honored
    it manually. This rule makes that detection MECHANICAL.
    DEFINITIONS.
    (a) DIAGNOSTIC RECURRENCE HIT = one confirmed (status:confirmed)
    failure_autopsy_*.json TARGET that (i) carries claim_ids: [] (a pure
    diagnostic, invisible to the claim-keyed brake / GOV-CEIL-1 -- a claim-tagged
    diagnostic is ALREADY covered there and is EXCLUDED, never double-counted; a
    MISSING claim_ids is not treated as empty), AND (ii) reached no verdict,
    operationalized as recommended_evidence_direction in {non_contributory,
    inconclusive} (a supports / refutes direction or a substrate build decision
    means a verdict WAS reached -- not a circling hit).
    (b) WORK-STREAM KEY = the target's bears_on field (string or list of strings)
    naming the plan work-stream(s) / competence-gap(s) the diagnostic circles --
    e.g. ree_ai_design_critique_plan:WS-1 (a <plan_stem>:WS-N plan work-stream) or
    f_dominance_conversion_ceiling (a named competence gap). Each token gets one
    hit per (artifact stem, run_id) so re-listing a token across an artifact's
    targets does not inflate the count. Authors SHOULD tag bears_on with namespaced
    work-stream / competence-gap tokens (not incidental claim ids).
    (c) TRIGGER = some bears_on token reaches diagnostic_recurrence_hit_count >= N
    (N = 3, matching GOV-CEIL-1). The reading: the operationalization is likely
    confounded (the question is MIS-POSED), not merely under-powered -- re-pose it
    (fix the measurement / reference frame) rather than re-run it harder, and
    refuse a same-question re-queue in the re-derive brake's spirit.
    NON-FALSIFYING-SAFE: absent a bears_on-tagged corpus every count is 0, so a
    missing / untagged corpus can never mass-surface (same posture as GOV-CEIL-1).
    (d) METABOLIZED EXCLUSION (added 2026-07-18). A chain whose prescribed response
    was actually CARRIED OUT must be able to CLEAR; otherwise it fires ACTIONABLE
    every cycle forever, which is precisely the alarm-fatigue Goodhart vector
    GOV-FROZEN-1's design notes name -- a warn-only flag that recurs every cycle
    with a plausible narrative gets accepted by default. GOV-GRAN-1 keys its
    equivalent exclusion off a note field on the CLAIM; GOV-DIAG-1 cannot, because
    it touches no claim by construction. Its work-stream-shaped analogue is a
    diagnostic_recurrence_metabolized block on the owning NODE of an
    evidence/planning/*_plan.md closure_plan frontmatter (the same node whose Status
    row records the re-pose), carrying a REQUIRED metabolized_hits list (run_ids
    and/or autopsy artifact stems), an optional covers_tokens list (for a free-form
    competence-gap token that has no node of its own), and a note recording what was
    re-posed, refused and built. A node's marker covers <plan_stem>:<node id>,
    <closure_plan id>:<node id>, the bare <node id>, and any covers_tokens. The
    exclusion is HIT-SCOPED, NOT token-scoped: only the listed hits are subtracted
    and the token is re-thresholded on what remains, so a NEW chain later circling
    the SAME work-stream still re-accumulates to N and re-fires -- a marker can
    quiet the chain it names, never permanently deafen the token. metabolized_hits
    is REQUIRED for that reason: a marker missing it is IGNORED and reported as
    MALFORMED (fails loud, never silently blanket-silences).
    RESPONSE. This rule PROMOTES / DEMOTES NOTHING and touches no claim -- the
    chain is claim_ids: [] by construction. The surface is an ACTIONABLE routing
    signal: hand /governance (or the user) a re-operationalization decision for the
    named work-stream, and refuse a same-question letter re-queue. It is the
    diagnostic-stream twin of GOV-CEIL-1's claim-level demotion, not a demotion
    itself.
    PROCESS. Detection is automatic: check_diagnostic_chain_recurrence.py counts
    pure-diagnostic no-verdict autopsies per bears_on token and surfaces a
    recurrence overlay, run warn-only inside governance.sh. APPLICATION (the
    re-operationalization) is a human governance decision at Step 6a-v-ter.
  notes: "Diagnostic-side companion to GOV-CEIL-1 (which stays claim-keyed). Wiring: scripts/check_diagnostic_chain_recurrence.py (pure-diagnostic no-verdict hit counter from confirmed failure_autopsy_*.json, keyed on bears_on; recurrence overlay at N>=3), scripts/governance.sh (Step 3f warn-only audit), governance skill Step 6a-v-ter (surface + re-operationalization procedure). N=3 is a module constant (DIAG_RECURRENCE_N) -- tunable. Requires a bears_on work-stream token on failure_autopsy targets (schema addition, target-level, string-or-list; pre-existing informal field formalized here); the 719a/724/732/732a chain was backfilled as the validating example (trips WS-1 + f_dominance_conversion_ceiling at 3 hits from the 724/732/732a tail; 719a excluded as claim-keyed). PROMOTES/DEMOTES NOTHING -- it surfaces a mis-posed-question pattern for a human to route. Cross-ref GOV-CEIL-1 and the re-derive brake in the /failure-autopsy skill. 2026-07-18 metabolized-exclusion amendment (definition (d)): the validating chain itself became the first stale alarm -- 724/732/732a had been fully metabolized on 2026-07-10 (WS-1 adequacy gate re-operationalized off the privileged global teleport oracle onto a local-view-achievable 5x5 resource_field_view ceiling in the WS-3 capability_eval.py yardstick, the oracle demoted to floor-achievability control; same-question re-queue V3-EXQ-732b REFUSED; GOV-FANOUT-1 BUILT as the constructive response, producing the 737/738/739 portfolio whose P-B leg refuted H2) yet both its tokens kept reporting ACTIONABLE every governance cycle. Fixed by the hit-scoped diagnostic_recurrence_metabolized marker (load_metabolized_markers / apply_metabolized_exclusions in check_diagnostic_chain_recurrence.py; marker landed on ree_ai_design_critique_plan.md node WS-1 covering f_dominance_conversion_ceiling), mirroring GOV-GRAN-1's granularity_debt_disposition exclusion but narrower (hit-scoped, so the work-stream can re-fire on a genuinely new chain). Audit drops from 2 ACTIONABLE to 0."
- id: GOV-FANOUT-1
  title: "Bottleneck fan-out escalation rule (constructive complement to GOV-CEIL-1 / GOV-DIAG-1 / the re-derive brake): the existing brakes DETECT that work is circling one root (N>=3 same-root ceiling or diagnostic-recurrence hits) and correctly REFUSE another same-question sequential letter -- but they prescribe no constructive next move, so the default is a single re-posed sequential probe that can itself inherit the prior confound and return a confident-but-wrong verdict. This rule supplies the missing response: when a brake fires AND the open question is a DISCRIMINATION (which-hypothesis, not a single named build), the standard escalates from sequential retry to a DIVERSE PARALLEL PORTFOLIO -- enumerate the live hypothesis space, design >=K probes each attacking a DIFFERENT design axis (not power-bumps), each declaring what a null result means, adversarially audit the portfolio for coverage gaps + verdict aliasing BEFORE queuing, and run them in parallel accepting some legs will be non_contributory. Warn-only routing signal; PROMOTES/DEMOTES NOTHING."
  claim_type: governance_rule
  subject: governance.epistemics.bottleneck_fanout_escalation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-07-10
  source_thought: evidence/planning/ree_ai_design_critique_plan.md
  binds_at_version: v3
  blocks_v3_green_board: false
  functional_restatement: >
    Epistemic-hygiene governance rule (WS-14 of the AI-design critique roadmap); the
    CONSTRUCTIVE complement to the DETECTION rules GOV-CEIL-1 (claim-keyed ceiling
    exhaustion) and GOV-DIAG-1 (pure-diagnostic chain recurrence) and the
    /failure-autopsy re-derive brake. Those three all answer "have we circled this
    root enough to STOP?" and correctly refuse another same-question sequential
    letter -- but none answers "what do we do INSTEAD?", so the default response is a
    single re-posed sequential probe. A single discriminator can silently inherit the
    prior confound (the validating instance: the V3-EXQ-732/732a competence
    discriminator's adequacy gate referenced a privileged-global oracle while the
    learner saw a 5x5 local view, so a sub-floor reading was uninterpretable) and
    return a confident-but-wrong verdict -- the failure mode is not lost time, it is
    BUILDING THE WRONG SUBSTRATE on a laundered artifact.
    DEFINITIONS.
    (a) BOTTLENECK = any of: GOV-CEIL-1 ceiling-exhaustion fired; GOV-DIAG-1
    diagnostic-recurrence fired (>=N same-work-stream no-verdict diagnostics); or the
    re-derive brake fired on a lineage.
    (b) DISCRIMINATION QUESTION = the open work is "WHICH of >=2 live hypotheses
    holds" (e.g. H1 action-stack vs H2 observation interface), NOT a single
    already-named build. A brake that routes to one clear /implement-substrate build
    is NOT in scope -- just build it.
    (c) DIVERSE PORTFOLIO = >=K probes (default K = number of live hypotheses, min 2)
    that each attack a DIFFERENT design axis (representation / measurement /
    observation / drive / algorithm), NOT power-bumps of one design (the re-derive
    brake already refuses those); each leg pre-registers its DECLARED-NULL (what a
    non_contributory result would and would NOT mean) so a null leg is informative,
    not wasted; the set is adversarially DESIGN-AUDITED for (i) hypothesis-space
    COVERAGE gaps and (ii) verdict ALIASING (could two distinct causes produce the
    same reading?) BEFORE any leg is queued; legs run in PARALLEL.
    TRIGGER = bottleneck fired AND question is a discrimination AND no diverse
    portfolio has yet been run for it. NON-FALSIFYING-SAFE / does nothing on its own:
    it changes no claim and gates no promotion -- it is a routing standard that
    escalates HOW the next probes are designed. EXEMPT: a bottleneck that routes to a
    single unambiguous build (no discrimination); a genuinely one-shot probe with no
    plausible second hypothesis.
    RESPONSE / WIRING. (1) /failure-autopsy: when re_derive_brake.fired AND the route
    is a discrimination, emit a fanout_recommendation block enumerating the live
    hypotheses + suggested diverse probe axes (instead of ONLY "route to
    /implement-substrate <substrate>"). (2) /queue-experiment: Step 2.5b checklist --
    if this is the >=Kth attempt on a braked lineage and the question is a
    discrimination, prefer a portfolio; enumerate the hypothesis space, confirm each
    leg attacks a distinct axis + declares its null, and design-audit for
    coverage/aliasing before queuing. (3) /governance: Step 6a-v surfaces
    fanout-candidate lineages (a bottleneck-tripped discrimination with no portfolio
    run) alongside GOV-CEIL-1 / GOV-DIAG-1 surfacing, for the user to route.
    VALIDATING INSTANCE (this session, 2026-07-10). The 719a -> 724 -> 732 -> 732a
    sequential discriminator chain circled the same competence floor (the exact
    GOV-DIAG-1 recurrence); the corrective was the 737/738/739 diverse portfolio --
    P-A trainable policy head on the REE latent (representation axis), P-B
    local-view-achievable ceiling anchor (measurement axis), P-C observation-encoder
    probe (observation axis) -- each with a declared null, design-audited for
    coverage + the 732 aliasing confound before queuing. P-B (V3-EXQ-738) ran first
    and its result (the 1.0 floor is reachable from the 5x5 local view) refuted H2
    and re-valued P-C to reserve -- the early answer a single sequential probe could
    not have produced.
  notes: "WS-14 deliverable of evidence/planning/ree_ai_design_critique_plan.md; constructive complement to GOV-CEIL-1 (WS-2) + GOV-DIAG-1 -- REUSES their detection (the re-derive brake count / check_substrate_ceiling_audit.py / check_diagnostic_chain_recurrence.py), adds only the fan-out RESPONSE. Wiring: /failure-autopsy skill (fanout_recommendation block on brake-fired discrimination), /queue-experiment skill Step 2.5b (portfolio-vs-sequential + declared-null + coverage/aliasing design-audit), /governance skill Step 6a-v (surface fanout-candidate lineages), graveyard doc antibody (sequential-retry-instead-of-fan-out). K defaults to the number of live hypotheses (min 2). PROMOTES/DEMOTES NOTHING on registration -- it is the rule, not its first application; its first application (737/738/739) was already run this session. Cross-ref GOV-CEIL-1, GOV-DIAG-1, the re-derive brake, and the perspective-diverse-verification principle."
- id: GOV-REUSE-1
  title: "Reanalysis-first / answer-from-recorded-data rule (consumer complement to the Experimental Recording Standard): before authoring a NEW experiment, check whether the decisive readout the experiment would produce is ALREADY RECORDED -- or derivable by post-hoc reprocessing -- in existing manifests on a COMPATIBLE substrate (a matching or lineage-equivalent top-level substrate_hash, or a provably substrate-invariant readout). If the answer is fully recoverable, do NOT queue a run: produce a RECORDED post-hoc reanalysis artifact that cites the source run_ids. If only partially recoverable, DOWNGRADE to a minimal targeted run for the missing readout alone. Author a fresh full experiment only when the answer genuinely cannot be recovered from recorded data. This is the forward mystery(known-data) router -- it converts the recording standard's over-recorded readouts into avoided compute. Warn-only routing standard; PROMOTES/DEMOTES NOTHING."
  claim_type: governance_rule
  subject: governance.epistemics.reanalysis_first
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-12
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-07-12
  source_thought: evidence/planning/experimental_recording_standard_2026-07-12.md
  binds_at_version: v3
  blocks_v3_green_board: false
  functional_restatement: >
    Epistemic-hygiene governance rule; the CONSUMER complement to the Experimental
    Recording Standard (evidence/planning/experimental_recording_standard_2026-07-12.md).
    The standard is the PRODUCER side ("record generously", 3b/3c) -- its whole payoff
    is that a future question may already be answerable from data a prior run recorded.
    GOV-REUSE-1 is the rule that ACTS on that payoff at authoring time, so the
    over-recording is not wasted. It is the forward direction of the work-graph
    mystery(known-data) token ("you already have the data; do not gather more --
    reprocess / reframe"); /failure-autopsy's recording-gap pathway is the REVERSE
    (a past run under-recorded), this is the forward (a new run may be unnecessary).
    DEFINITIONS.
    (a) DECISIVE READOUT = the specific metric/quantity the new experiment's PASS/FAIL
    would route on (not the whole manifest -- the one number the question turns on).
    (b) COMPATIBLE SUBSTRATE = a prior run whose top-level substrate_hash matches the
    lineage the question is about, OR whose readout is provably substrate-invariant. A
    readout from a stale / different substrate answers a DIFFERENT question. substrate_hash
    is the compatibility KEY -- which is exactly why pre-standard manifests (0% carried a
    substrate_hash) are mostly untrustworthy for reuse, and why the standard makes it
    mandatory. Respect derived-vs-raw discipline: a readout can be RE-DERIVED from raw
    inputs a prior run recorded (per-seed arrays, custom_information) even if the exact
    aggregate was not itself stored.
    (c) POST-HOC REANALYSIS = an analysis OVER existing manifests (NOT a queued
    experiment) that computes or derives the answer and is ITSELF recorded as an
    artifact citing the source run_ids, so governance can weight it -- the reanalysis
    obeys the same recording standard it consumes.
    TRIGGER = a new experiment is being authored AND its decisive readout is plausibly
    present or derivable in recorded manifests on a compatible substrate.
    NON-FALSIFYING-SAFE / does nothing on its own: it changes no claim and gates no
    promotion -- it is a routing standard that changes WHETHER the next run happens.
    EXEMPT: a question requiring a NEW manipulation / condition present in no recorded
    run; a lineage whose substrate is genuinely in flux (no compatible substrate_hash
    exists); a readout neither recorded nor derivable from recorded raw inputs.
    RESPONSE / WIRING. (1) /queue-experiment: new Step 2.4 "Existing-evidence /
    reanalysis-first check" BEFORE script authoring -- name the decisive readout, query
    compatible-substrate manifests + the index (substrate_hash / label_balance are now
    surfaced on index entries), route to a recorded reanalysis when recoverable, and
    record the check outcome in the queue entry note (or the reanalysis artifact). Plus a
    Step 3.5 necessity checklist item. (2) Experimental Recording Standard 5 (relationship
    to the epistemic system) names GOV-REUSE-1 as the consumer that realizes "more
    readouts available as evidence". (3) DISTINCT from the arm-reuse fingerprint: that
    skips RE-TRAINING a bit-identical baseline CELL but the experiment still runs and its
    criteria still execute; GOV-REUSE-1 can avoid the run ENTIRELY by answering from
    recorded data.
  notes: "Consumer complement to the Experimental Recording Standard (producer). Source: experimental_recording_standard_2026-07-12.md section 5. Tooling: scripts/reanalysis_query.py -- `query --readout <name> [--substrate-hash <h>]` scans manifests, groups by substrate_hash, flags MATCH/INCOMPATIBLE/UNVERIFIABLE per candidate (hash read top-level OR hoisted from arm_results[].arm_fingerprint); `emit` writes a recorded reanalysis/v1 artifact under evidence/reanalysis/ citing source run_ids, and REFUSES unless the sources share a compatible substrate_hash (or --substrate-invariant). Wiring: /queue-experiment Step 2.4 (existing-evidence / reanalysis-first check) + Step 3.5 necessity item. Distinct from the arm-reuse fingerprint (cell-level compute reuse -- experiment still runs) and from the re-derive brake (refuses a same-claim same-ceiling RE-TEST; GOV-REUSE-1 asks whether ANY run is needed). Forward mystery(known-data) router; /failure-autopsy recording-gap is the reverse. PROMOTES/DEMOTES NOTHING on registration (the recorded reanalysis artifact is NOT yet wired into build_experiment_indexes confidence scoring -- deferred follow-up, same posture as the recording standard's deferred items). Cross-ref the recording standard, GOV-CEIL-1 / the re-derive brake, and the work_graph_debt_vocabulary mystery(known-data) token."
- id: GOV-CONFIRM-1
  title: "Evidence-confirmer detection rule (GENERATIVE complement to the consume-only inter-governance workset): the IGW workset + auto-spawn routine can COMPLETE an experiment confirmer, but only re-surface a confirmer whose `status: proposed` entry was hand-authored first -- _proposed_experiments() applies only EXCLUSION filters over pre-authored proposals and has no path that scans claims.yaml to AUTHOR a confirmer for a thin-evidence candidate whose substrate is already built. This rule supplies the missing generative discovery: scan candidate/provisional claims and surface a low-priority `evidence_confirmer` lane for each claim that is confirmable-but-unconfirmed -- substrate BUILT, thin/zero experimental evidence, decent literature support, NOT wall-bound / substrate-blocked, not already covered -- so the auto-spawn routine completes these non-urgent-but-important confirmers as background fill behind the live front. The judgement piece (scoping a confirming DV buildable NOW -- a representation-level / functional-signature readout, self-routing substrate_not_ready_requeue if only a wall-bound behavioural DV exists) stays in the per-item /queue-experiment pass. The detector owns DISCOVERY, not design. Warn-only routing standard; PROMOTES/DEMOTES NOTHING."
  claim_type: governance_rule
  subject: governance.epistemics.evidence_confirmer_discovery
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-14
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-07-14
  source_thought: evidence/planning/gov_confirm_1_plan.md
  binds_at_version: v3
  blocks_v3_green_board: false
  functional_restatement: >
    Governance discovery rule; the GENERATIVE-DISCOVERY member of the warn-only
    routing-standard family (GOV-CEIL-1 ceiling-exhaustion detection, GOV-DIAG-1
    diagnostic-recurrence detection, GOV-FANOUT-1 bottleneck-response, GOV-REUSE-1
    reanalysis-first consumer). Those act at STOP / RESPONSE / AUTHORING time on work
    that is already in motion; GOV-CONFIRM-1 acts at DISCOVERY time on work nobody has
    started -- the confirmable-but-unconfirmed candidate claims that the consume-only
    workset never surfaces.
    THE GAP. scripts/generate_inter_governance_workset.py _proposed_experiments()
    (line 684) is consume-only: it re-surfaces `status: proposed` proposals and applies
    only EXCLUSION filters (drop v3_pending / substrate-blocked / already-evidenced /
    V4+). Nothing scans claims.yaml to AUTHOR a confirmer for a thin-evidence candidate
    whose substrate is built. MOTIVATING INCIDENT (2026-07-14): finding buildable
    evidence confirmers required a bespoke 634-candidate subagent scan; it surfaced five
    claims (MECH-304 lit 0.892, MECH-288 0.875, MECH-287 0.824, MECH-303 0.863, MECH-284
    0.824 -- each candidate, exp_conf 0, substrate built, not wall-bound) that ALREADY
    pass the workset's exclusion filters and were invisible only because no proposal was
    hand-authored. These are the "non-urgent but important evidence confirmers" the
    machinery should discover, not a human by memory.
    DEFINITIONS.
    (a) CONFIRMABLE-BUT-UNCONFIRMED = a candidate/provisional claim that is
    v3-testable (not v3_pending, testable epistemic_category), has thin/zero
    experimental evidence (not in claim_evidence.v1.json genuine-exp set), decent
    literature support (lit_conf >= a threshold, default 0.6), is NOT substrate-blocked
    (no unresolved substrate_queue entry lists it -- the same _retest_blockers gate the
    retest lane uses), is not V4/V5-only, and whose substrate is actually BUILT (its
    claims.yaml `location` resolves to an existing ree-v3/ree_core module).
    (b) EVIDENCE_CONFIRMER LANE = a low-priority workset lane, one item per
    confirmable-but-unconfirmed claim, naming the claim id + location + lit_conf + a note
    to scope the confirming DV at /queue-experiment.
    (c) The rule does NOT hard-encode the transient "competence wall": the durable core
    is surfacing confirmable-but-unconfirmed candidates; wall-bound behavioural
    confirmers are already excluded by the reused substrate-block / epistemic-category
    predicates, and any that slip through self-route vacuous at /queue-experiment.
    RESPONSE / WIRING. (P1) generate_inter_governance_workset.py: add a generative lane
    iterating ALL claims_meta (not just proposals), REUSING _claim_v3_testable /
    _claims_with_experimental_evidence / _retest_blockers / _is_deferred_beyond_v3 plus a
    lit_conf floor + a location-exists substrate-built gate; output-only, additive
    (bit-identical for non-confirmer items). (P2) wire the lane into the hourly auto-spawn
    eligibility at a priority STRICTLY BELOW the wall-campaign lanes, so confirmers run as
    background fill only. NON-FALSIFYING-SAFE: it changes no claim and gates no promotion;
    the confirming experiments it seeds move claims candidate->provisional through the
    normal /governance path, not through this rule. EXEMPT: claims with no built substrate
    (nothing to confirm yet) and claims already covered by a queued/proposed run.
  notes: "Plan of record: evidence/planning/gov_confirm_1_plan.md (P0 done -- this claim + plan; P1 shadow detector + P2 low-priority autospawn build after user review). GENERATIVE-DISCOVERY complement to GOV-CEIL-1 / GOV-DIAG-1 (detection), GOV-FANOUT-1 (response), GOV-REUSE-1 (reanalysis consumer). Reuses existing workset predicates -- adds only the generative iteration + a lit_conf floor + a location-exists substrate-built gate. First-application seed set (2026-07-14 subagent scan, chips spawned this session): MECH-304, MECH-288, MECH-287, MECH-303, MECH-284; borderline MECH-271 (substrate unconfirmed) / MECH-292 (scope-risky). PROMOTES/DEMOTES NOTHING on registration. Cross-ref the IGW auto-spawn routine, ree_ai_design_critique_plan.md (WS family), and the work_graph_debt_vocabulary complex(probe-gated) token (the wall-independent-DV judgement stays a per-item /queue-experiment probe)."
- id: GOV-FROZEN-1
  title: "Frozen hypothesis-set pre-registration rule (anti-Goodhart contract over the Narrow/Decide dashboard): the reduction ratio surviving/initial is Goodhart-able from the denominator side -- a campaign can improve its apparent narrowing by padding the initial hypothesis set after the discriminating evidence is already in. This rule pre-registers the contract that forbids it: the initial set is FROZEN at registration (initial_frozen_count_at_registration never moves), a hypothesis must be pre-registered before its adjudicating run resolves, and the ONE sanctioned way an already-adjudicated question's set may grow is LABELLED GOV-FANOUT-1 fan-out whose late legs clear only on GIT-WITNESSED provenance -- a self-reported pre_registered_utc is back-datable and is not evidence. The registry has a SINGLE producer (/failure-autopsy Step 9b); governance is derive-only over it. The audit is advisory and NEVER gates a cycle. Warn-only; PROMOTES/DEMOTES NOTHING."
  claim_type: governance_rule
  subject: governance.epistemics.frozen_hypothesis_set
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-18
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: 2026-07-18
  source_thought: evidence/planning/hypothesis_space_registry.v1.json
  binds_at_version: v3
  blocks_v3_green_board: false
  functional_restatement: >
    Epistemic-hygiene governance rule; the DENOMINATOR-side member of the warn-only
    routing-standard family (GOV-CEIL-1 ceiling-exhaustion detection, GOV-DIAG-1
    diagnostic-recurrence detection, GOV-FANOUT-1 bottleneck response, GOV-REUSE-1
    reanalysis-first, GOV-CONFIRM-1 generative confirmer discovery). Those govern how
    work is stopped, answered, or started; this one governs the integrity of the
    measure by which narrowing progress is REPORTED.
    THE GOODHART VECTOR. The Scientific Progress Dashboard's Dimension 3 (Narrow)
    reports a reduction ratio -- surviving hypotheses over the initial enumerated set.
    Every term in that ratio is improvable by moving the denominator rather than by
    doing science: enumerate extra rival hypotheses AFTER their discriminating run has
    already resolved, and a campaign that eliminated 2 of 3 legs reports as having
    eliminated 4 of 6. The elimination side is already barred (a leg may be marked
    eliminated only on an adjudicated weakens with met_elimination_bar, control_passed,
    and non_degenerate all true). This rule bars the registration side.
    THE CONTRACT (invariants 1-7 of /failure-autopsy Step 9b, in summary).
    (1) initial_frozen_count == len(hypotheses) for every question, always, bumped in
    the same edit that adds a leg. (2) pre_registered_utc <= the adjudicating run's
    resolved_utc for every resolved leg. (3) an already-adjudicated question's
    initial_frozen_count is never enlarged after the fact, EXCEPT as labelled fan-out
    growth (3a, below). (4) eliminated / split require the full bar plus an adjudicated
    direction; otherwise the leg stays alive. (5) confirmed requires control_passed.
    (6) exactly two legitimate sources of total_initial growth -- a NEW-question
    registration and labelled fan-out on an existing one; the audit attributes a
    time-series rise to whichever labelled sources landed in that window and flags only
    the UNATTRIBUTED remainder. (7) a human owns decided -- the dashboard may say
    decidable_now but decision.decision_log_ref is only ever a human decision_log entry.
    THE SCOPED FAN-OUT ALLOWANCE (invariant 3a). A GOV-FANOUT-1 discrimination
    portfolio legitimately enumerates NEW rival explanations for an EXISTING question as
    earlier axes are eliminated -- that is what a fan-out routing IS, systematic
    practice rather than an exception. Such growth is sanctioned only when all three
    hold: (a) pre-registration precedes adjudication -- growth is legitimate for legs
    whose adjudicating run has NOT yet resolved, and a leg added AFTER its own evidence
    is in clears ONLY on git-witnessed provenance (its pre_registration_source artifact,
    or its own registry entry, durably committed before the run resolved); (b) the
    growth is labelled machine-readably in the question's fanout_growth_events[], naming
    the opening autopsy (fanout_source) and the added hids, with delta consistent; and
    (c) initial_frozen_count_at_registration is preserved separately so the ratio is
    reportable BOTH ways. Growth satisfying (a)-(c) is ADVISORY, reported outside the
    flag count. Growth that is unaccounted, unlabelled, mismatched, or retro-padded
    remains a real bucket-(b) violation.
    WHY A GIT WITNESS, AND WHY LEGITIMATE CASES MUST SELF-CLEAR. A self-reported
    pre_registered_utc is written after the fact and is trivially back-datable, so an
    audit reading only the registry can NEVER detect back-dating. A commit date cannot
    be manufactured, so the witness makes the honest case self-clearing with no human
    adjudication and the dishonest case detectable. This asymmetry is deliberate and is
    the load-bearing design choice, because ALARM FATIGUE IS ITSELF A GOODHART VECTOR:
    a warn-only flag that recurs every cycle with a plausible narrative attached gets
    accepted by default -- which is exactly how a benign-per-invariant-6 reading came to
    be cited for growth that invariant 6 did not cover. The standing design rule that
    follows: silence-plus-a-narrative must FAIL where a git witness would PASS, and
    legitimate cases must clear on verifiable provenance rather than on human
    adjudication, so that a raised flag stays rare enough to be worth reading. Legs with
    no git history at all (uncommitted, wholesale rewrite, git unavailable) report as a
    quiet unverifiable state -- not a violation, but not evidence either.
    A GROWING DENOMINATOR IS ITSELF A SIGNAL. The set grows mostly by legs that are
    then eliminated, so labelled fan-out inflates the headline narrowing ratio PRECISELY
    when a campaign is failing to converge and having to invent new candidate
    explanations. A clean advisory section is therefore not an all-clear. The dashboard
    must report the honest pair -- surviving/original AND surviving/current-including-
    fan-out -- so non-convergence stays visible rather than being laundered into an
    improved ratio, and an autopsy appending a growth event must say so in its routing
    note (fanout_growth_note on the question).
    ESCALATION CLAUSE -- RECURRENCE AT N>=3 (added 2026-07-18, same day as
    registration; the gap was found immediately after and is recorded rather than
    quietly patched). Conditions (a)-(c) license an INDIVIDUAL growth event, so they
    say nothing about RECURRENCE: a question can fan out indefinitely, clearing every
    check every time, while its denominator outruns its eliminations. That is the
    alarm-fatigue vector this very claim warns about, turned on the rule itself -- a
    recurring advisory carrying a plausible narrative (LEGITIMATE labelled fan-out) is
    accepted by default, and the original rule had no count-based escalation while both
    its siblings did (GOV-CEIL-1 CEILING_EXHAUSTION_N=3, GOV-DIAG-1 DIAG_RECURRENCE_N=3).
    TRIGGER = a single question reaching FANOUT_RECURRENCE_N (=3, module constant) DISTINCT
    labelled fan-out portfolios. The counter keys on the fanout_source ARTIFACT, not on
    recorded_utc: one backfill pass can record several historically-distinct portfolios
    under a single timestamp (and conversely a re-record of one portfolio must not
    inflate the count) -- the autopsy that opened the portfolio is the thing there is
    one of per fan-out decision. Only portfolios that already cleared (a)-(c) are
    counted; the point is that each was individually legitimate and the RECURRENCE is
    the signal.
    NOT REDUNDANT WITH GOV-DIAG-1. That rule counts pure-diagnostic NO-VERDICT
    autopsies (non_contributory / inconclusive). Fan-out recurrence is the OPPOSITE
    signature: every run reached a verdict and eliminated a leg, so the chain is
    invisible to the no-verdict counter by construction. A campaign can hold perfect
    GOV-DIAG-1 hygiene and still never converge, because the denominator regrows faster
    than it eliminates. Confirmed empirically at registration time: GOV-DIAG-1 fires on
    ree_ai_design_critique_plan:WS-1 and f_dominance_conversion_ceiling, and NOT on the
    competence_floor question that had already fanned out twice (7 -> 12 legs).
    RESPONSE is ROUTING, NOT DEMOTION -- these are questions, not claims, and nothing is
    promoted or demoted. The reading: the question may be MIS-POSED rather than
    under-enumerated; re-pose the operationalization before opening another portfolio.
    Enumerating a further round of rivals on an unchanged framing is the
    denominator-side twin of re-running a braked experiment harder. NON-FALSIFYING-SAFE:
    absent fan-out events every count is 0, so an untagged or empty registry can never
    mass-surface (same posture as GOV-CEIL-1 / GOV-DIAG-1). Warn-only; still never gates.
    PRODUCER / CONSUMER SEPARATION. The registry has a SINGLE producer,
    /failure-autopsy Step 9b, which pre-registers at fan-out time and resolves at
    adjudication time. /governance Step 5c is derive-only over it: it reads the audit
    report, surfaces non-zero flags as review hints, and routes any real flag BACK to an
    autopsy session -- it never patches the registry inline. Governance must not grow a
    second registry writer; a second producer would let the consumer of the measure also
    author its denominator, reintroducing exactly the incentive this rule removes.
    ENFORCEMENT POSTURE -- READ THIS BEFORE CITING THE RULE. Registration of this
    claim buys DOCUMENTATION AND CITABILITY, NOT PREVENTION. The rule existed as skill
    prose plus a JSON header, which a session cannot cite, version, or depend on the way
    it can a claim -- and that asymmetry is plausibly why invariant 6 was cited loosely
    for a case it did not cover. What actually closes the back-dating hole is the GIT
    WITNESS (landed 2026-07-18), not the existence of this entry. The audit
    (check_hypothesis_space_integrity.py) is ADVISORY and warn-only: it always exits 0
    and NEVER gates a governance cycle. Nothing here is scored by the closure pipeline;
    the registry is a derive-only INPUT that never mutates claims.yaml, closure weights,
    or claim status. Do not cite GOV-FROZEN-1 as though the rule were enforced by its
    own registration.
  notes: "Registered 2026-07-18 by the /governance 2026-07-18 cycle session's request; deliberately deferred by the session that BUILT the machinery because claims.yaml is governance-owned and high-contention. DOCUMENTATION / CITABILITY ONLY -- registers no new enforcement; the machinery was already fully built and landed. Wiring (all pre-existing): evidence/planning/hypothesis_space_registry.v1.json (the FROZEN append-only ledger, invariants block: elimination_bar, frozen_initial_set, labelled_fanout_growth, human_owns_decided, non_contributory_unchanged), scripts/check_hypothesis_space_integrity.py (advisory audit buckets a/b/c/d plus advisory e_labelled_growth / f_unverifiable / g_witnessed), scripts/build_hypothesis_space.py (derives hypothesis_space.v1.json + hypothesis_space_timeseries.v1.jsonl), scripts/governance.sh Step 3c-bis-6b (warn-only), /failure-autopsy SKILL.md Step 9b Frozen-set invariants 1-7 (SINGLE producer; 3a is the fan-out case), /governance SKILL.md Step 5c (derive-only consumer). Landed as REE_assembly master 9d26f2be5f (fan-out contract + honest-pair dashboard) and bd9674dd44 (git-witnessed pre-registration). ESCALATION CLAUSE added 2026-07-18 (same day as registration): the structural contract is now paired with a count-based recurrence overlay, FANOUT_RECURRENCE_N=3 distinct fanout_source portfolios on one question, matching GOV-CEIL-1 CEILING_EXHAUSTION_N / GOV-DIAG-1 DIAG_RECURRENCE_N -- the original entry had NO threshold while both siblings did, which left the rule exposed to the exact alarm-fatigue vector it warns about. Wiring: check_hypothesis_space_integrity.py bucket `h_fanout_recurrence` (ADVISORY_BUCKETS member -- ACTIONABLE overlay, never counted as a flag, never gates), its own report section, a stdout routing block, and 4 self-test discriminations (fires at N>=3; NOT a (b) violation since every portfolio was legitimate; keyed on fanout_source not recorded_utc, pinned by two synthetic events sharing a timestamp; quiet below N). Live state at landing: competence_floor sits at 2 portfolios (7 -> 12 legs), so the overlay is armed but correctly silent -- one more portfolio fires it. PROMOTES/DEMOTES NOTHING on registration -- it is the rule, not its first application. Cross-ref GOV-FANOUT-1 (the fan-out routing whose portfolios are the sanctioned growth source), GOV-CEIL-1 / GOV-DIAG-1 (sibling warn-only detection rules), and the work_graph_debt_vocabulary aleatoric(irreducible) token for the unverifiable provenance state (no git history is not a reducible unknown -- it is hedged, not researched)."
- id: ARC-106
  title: "Biology-grounding as a standing construction constraint: build REE the agent so each functional component maps to an identified neural substrate at the level of FUNCTION (not homology), with biology as the reference of first resort for underdetermined design forks. Grounding must be load-bearing (a named neural primitive whose defining constraint is validated on a REE falsifier), not decorative naming; divergences from biology are legitimate but must be explicit and tracked, never silent."
  claim_type: architectural_commitment
  subject: architecture.biology_grounding_constraint
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:ARC-106@2026-07-09T19:00:29.843047Z"
      as_of: 2026-07-09
      verdict: hold_pending_v3_substrate/applied
  implementation_phase: v3
  v3_pending: false
  claim_level: architectural
  registered_utc: 2026-06-20
  depends_on:
    - MECH-439   # first worked example -- the F-dominated action selector (L2, falsifier 689a)
    - SD-011     # canonical grounding SUCCESS -- dual nociceptive streams modelled on biology (L3)
    - SD-033     # region-grounded substrate family -- PFC subdivision architecture
    - ARC-035    # vmPFC -- value/affect grounding exemplar
  epistemic_category: substrate_coherence
  what_would_answer: |
    ARC-106 is a standing construction-method claim, not a single-experiment
    hypothesis -- confirmation/falsification is read off the AGGREGATE
    pattern across the divergence ledger's worked examples
    (docs/architecture/arc_106_biology_grounding_framework.md S5), each
    scored by the claim's own load-bearing-vs-decorative test (S4.2):
    ablate/gap-blind the biologically-motivated constraint while holding
    everything else fixed; if a pre-registered behavioural metric is
    unchanged, that component's grounding is decorative.

    CONFIRMS (per-component, moves a ledger row to L3): a component's
    falsifier returns a clean load-bearing verdict -- the biological
    constraint's ablation measurably degrades the pre-registered metric, and
    the effect is specific to the biologically-motivated form (not
    reproduced by an equally-complex non-biological control). SD-011 (dual
    nociceptive streams) and drive/incentive salience (BG-4, V3-EXQ-514u)
    are the claim's own confirmed L3 exemplars.

    WEAKENS a SPECIFIC component (drops it to decorative, per S4.2 verbatim):
    the ablation/gap-blind control matches or exceeds the biologically-named
    construction on the pre-registered metric -- the biological name was
    cosmetic.

    CURRENT STATE OF EVIDENCE (first worked example, S6): the E3 action
    selector (MECH-439 F-dominance) is the claim's own keystone case and
    sits at L2, not yet L3. V3-EXQ-689a (2026-06-20) is the load-bearing
    test in practice but returned a MIXED result the framework's simple
    PASS/uniform-lift/no-lift trichotomy (S6.5) does not cleanly resolve:
    the combined conflict-graded levers (ARM_A1B1) did not convert (FAIL,
    non_contributory, conversion_ceiling_persists_despite_conflict_grade),
    while an isolated single lever (Factor B, gap-scaled commit-T) showed a
    load-bearing-looking lift (2/3 seeds above both gap-blind controls) that
    did NOT replicate on isolation retest (V3-EXQ-689c, FAIL/
    non_contributory). Net: the action-selector row's grounding is NEITHER
    confirmed load-bearing NOR shown decorative yet -- the claim escalated
    (per its own G3 guardrail) to the constitutional BG build
    (ARC-107/MECH-448/449) rather than resolving the L2 ledger row directly,
    and MECH-439 itself was separately demoted to contested/exhausted on
    2026-07-09 (GOV-CEIL-1, 10 confirmed substrate_ceiling autopsies)
    pending a positive discrimination from that constitutional route. This
    is consistent with the claim's own "no lift => the blocker is
    downstream, re-ground that row instead" branch (S6.5), not either the
    PASS or decorative branches -- track it as such, not as a resolved
    verdict either way.

    ARC-106 WHOLESALE FALSIFICATION / ABANDONMENT: not a single-run result,
    but a TREND -- if, across a critical mass of worked examples (the
    action selector plus at minimum 2-3 more ledger rows carried to a
    falsifier verdict, e.g. the Go/No-Go split, the commitment latch, the
    dopaminergic RPE substrate), the pattern is that biologically-motivated
    constraints, when tested under S4.2, repeatedly turn out decorative
    rather than load-bearing, that is evidence the biology-grounding method
    itself is not earning its cost and the standing constraint should be
    revisited or abandoned as a construction discipline. A single
    non-contributory or ambiguous result (as with 689a above) is explicitly
    NOT this pattern -- the doc's own S4.4 treats divergence/non-conversion
    as data, not failure. Revisit only when a worked-example falsifier
    materially changes the aggregate ledger picture -- i.e. either a run of
    confirmed decorative verdicts across multiple independent components,
    or (positive direction) enough L3 confirmations that the method's cost
    is clearly earning its keep.
  functional_restatement: >
    ARC-106 is a THIN UMBRELLA / standing design constraint + method, not new
    mechanism. It is the CONSTRUCTION-TIME complement to the registration-time
    "biology before formal definitions" rule: ground the build, not just the
    claim text. It establishes (a) a grounding ladder L0 ungrounded -> L1
    functional-analogy-named -> L2 literature-anchored -> L3 divergence-audited-
    and-validated-on-a-REE-falsifier; (b) a load-bearing-vs-decorative test
    (ablate the biologically-motivated constraint; if a pre-registered metric is
    unchanged, the grounding is decorative); (c) four anti-cargo-cult guardrails
    (function-not-homology; reuse-before-duplicate; minimal-primitive-first-then-
    validate; preserve-auditability); (d) a living divergence ledger; (e) a
    REQUIRED per-component psychiatric-failure-mode column, since clinical
    modelling is only earned once a component's breakage maps to a recognisable
    disorder. First worked example = the E3 action selector (MECH-439 F-dominance):
    a deterministic argmin over a near-monopolised scalar diverges from basal-
    ganglia disinhibitory competition; the v3-bounded convergence (F->eligibility
    + stochastic commit) is under test by V3-EXQ-689a. Adopted as a standing
    constraint by user decision 2026-06-20.
  biology_grounding_note: >
    This claim IS the biology-grounding policy; its own grounding is the existing
    scaffolding it sits on: founder_ontology.md (E1/E2/E3 functional map),
    brain_region_map.yaml + brain_map.md (atlas, "functional analogy not
    homology"), and the SD-011 dual-nociceptive-stream grounding success. The
    convergence backlog is brain_region_map.yaml's non_anatomy_prefixes
    (ethics/love/play/language/goal/commitment/attention/self/drive) -- the
    components deliberately left unmapped. Epistemic category resolves to
    substrate_coherence (design choice, promotion/demotion suppressed).
  governance_disposition_note: >
    2026-07-09 (governance, user decision): treated as a STANDING architectural
    construction-constraint, NOT a single-V3-substrate hold. ARC-106 is a thin
    umbrella / method whose evidence accrues from the aggregate of its
    biology-grounded worked examples' falsifiers (depends_on MECH-439 [V3-EXQ-689a],
    SD-011, SD-033, ARC-035), not from a dedicated V3 run tagged to ARC-106 itself.
    The recommendation engine's generic v3-hold (implementation_phase=v3 + 0
    ARC-106-tagged V3 runs) mislabels it as awaiting a V3 substrate and re-surfaced
    it as pending_user each cycle -- the v3-phase analogue of the v4/v5
    held_by_architectural_commitment reclassification (2026-04-27 / 06-06 / 07-01).
    Decision recorded (applied) so it leaves the pending_user queue; revisit only
    when a worked-example falsifier materially changes the grounding ledger.
  location: docs/architecture/arc_106_biology_grounding_framework.md
- id: ARC-107
  title: "Basal-ganglia-like E3 selector constitution: committed-action selection should be governed by an explicit BG-mirroring eligibility-gating CONSTITUTION -- candidate actions pass through an eligibility mask, bounded Go (direct-pathway promotion) and No-Go (indirect-pathway suppression) pressures, a conflict-sensitive hold / threshold-raise / candidate-set widening (hyperdirect-STN), striatal-like parallel channel competition (rather than one scalar monarch), a pallidal-like permission-to-commit gate (rather than bare argmax), and a stable post-commit latch / de-commit -- so that an upstream signal's STRENGTH is necessary but not sufficient: it also needs LAWFUL ACCESS to committed action. The translation target is functional (a divergence-ledgered, falsifier-validated BG analogue), NOT anatomical mimicry."
  claim_type: architectural_commitment
  subject: selection.basal_ganglia_selector_constitution
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-16
    needs_review: false
    evidence:
      from: v3_exq_926a_mech449_perseveration_nogo_falsifier_20260814T171050Z_v3
      as_of: 2026-08-14
      verdict: supports/PASS -- last untested No-Go axis converts, at a non-default envelope floor (0.10 vs stock 0.30)
  epistemic_category: standard
  implementation_phase: v3
  v3_pending: false
  claim_level: architectural
  registered_utc: "2026-06-20"
  evidence_quality_note: >
    [2026-08-21 governance, gov-20260821-0203, confirmed failure_autopsy_V3-EXQ-937b_2026-08-20.json]: V3-EXQ-937b discharges what the 937/937a cluster autopsy routed it to do -- it emits the per-bank (envelope size, converted) joint that 937a computed and discarded, re-keys every criterion to the per-bank mediator, closes 937a's elapsed_seconds always-core gap and carries ARM_OFF's tautology into the manifest -- and its aggregation_warning is exemplary. The methodological repair is real. But the stamped supports is NOT applied, because all six criteria are analytically entailed by the shipped code rather than measured. Verified independently over the raw per-bank columns: converted == (pre_nogo_envelope_size >= 2) holds with ZERO exceptions across all 12,672 ARM_CONSTITUTION banks. The mechanism is analytic: the suppression vector is built to peak on the target and PERSEVERATION_FLOOR = 0.5 makes soft_nogo a singleton; for ARM_CONSTITUTION that target IS the incumbent; and gng_protect_min_eligible = 1 re-admits the incumbent at envelope 1 (so the pick cannot move) and drops it at envelope >= 2 (so the argmin must move). Given both strata populated, C1's only reachable value is 1.0 against a 0.4 bar -- zero discriminating power, and the driver states this in advance. C4, C5 and C6 are forced by the same argument. This is a manipulation check, not a measurement. The manifest's construction_caveat concedes the coupling is 'PARTLY' a construction property and cites per_bank.incumbent_is_f_argmin as the column that records it; that column is tautological -- as coded it tests argmax(a vector built to peak at target) == target, and reads 1.0 on 38,016 of 38,016 banks INCLUDING all 12,672 ARM_SHUFFLED banks where target != incumbent by design -- so the caveat is carried but unquantified, and 'partly' understates a coupling that is total. Finally, top-level non_degenerate is ABSENT because the driver emits it only on the False branch (no else); reconstructed from the manifest's own values the correct declaration is true, and the per-criterion dict at interpretation.criteria_non_degenerate carries all six true. This is the third consecutive occurrence in the 937 lineage and validate_recording.py cannot see it (the field is outside ALWAYS_CORE_KEYS). Recording gap, not a data gap -- every quantity above was recomputed from the manifest without re-running anything.

    [/governance 2026-08-16, V3-EXQ-926a PASS -- first evidence_quality_note on this claim]:
    V3-EXQ-926a is the selection-face perseveration falsifier for the built MECH-449 Go/No-Go
    constitution, and it closes the LAST untested axis of that constitution -- V3-EXQ-689g exercised
    only safety and staleness, both as injected constructed tensors, and states at its own injection
    site that perseveration was left to the live agent loop. 926a supplies that vector from a real
    DACCAdaptiveControl history (record_action() + _suppression_penalty()). PASS on all three
    criteria (C1 conversion 0.969/0.969/1.000 over seeds 42/43/44 against floor 0.5; C2 content
    specificity gap 0.979 against a shuffled control at exactly 0.000; C3 safety fail-open 0), all
    three readiness preconditions met, all three criteria non-degenerate, combination rule verified
    in source as a plain AND. ARC-107 now stands at 7 supports / 0 weakens (2 experimental, 5
    literature), overall confidence 0.833, quadrant confirmed_established.
    CAVEAT THAT GATES ACTING ON THIS, carried here because the manifest stamps supports with a null
    evidence_direction_note: the run uses ENVELOPE_FLOOR = 0.10 against the substrate's stock
    f_eligibility_envelope_floor of 0.30, and at the stock default the identical mechanism converted
    1/16 (the MECH-448 envelope collapses to one survivor and the gng_protect_min_eligible fail-open
    guard correctly refuses to drop it, so the No-Go axis cannot fire at all). The demonstration is
    therefore "the axis CAN act, given a wider envelope than ships by default", not a
    default-configuration result -- see reference_claim_status_vs_default_off_flag.
    DISPOSITION: promotion candidate -> provisional was CONSIDERED this cycle and DEFERRED to the
    ordinary promotion/demotion pass rather than applied on this single run, so the decision is made
    against the pipeline's own thresholds. The gating open item is a stock-default demonstration (or
    an explicit decision that the wider envelope is the intended production setting). Note also that
    three ARC-107 depends_on entries are still flagged MISSING PIECE (ARC-108 unified dopamine
    substrate, ARC-109 D1/D2 population split, MECH-450 recurrent settling step), so the
    constitution remains incomplete regardless of this axis result.

    2026-08-18 (governance apply, V3-EXQ-937a + V3-EXQ-937, confirmed
    failure_autopsy_V3-EXQ-937-937a-cluster_2026-08-18): supports (937a) / non_contributory
    (937). The corrected per-bank basis is recorded in full on MECH-449; in short,
    conversion is a DETERMINISTIC STEP FUNCTION of per-bank envelope size (envelope 1 ->
    rate 0.000000; envelope >= 2 -> rate 1.000000) stepping exactly where
    gng_protect_min_eligible=1 predicts, so 937a's manifest basis (a graded dose-response,
    and no inert regime reached) is withdrawn as a cell-median binning artifact. ANSWERS THE
    ARC-107 BUILD-CONFIGURATION QUESTION left open by the 2026-08-16 V3-EXQ-926a caveat
    about the stock floor: the knee is realized envelope 2 exactly (zero variance), and at
    the shipped f_eligibility_envelope_floor=0.30 the No-Go leg is NOT inert but IS silenced
    on 31.5% of decisions (121/384 banks collapse to envelope 1) by the fail-open guard.
    CAVEAT carried from the cluster: at K_CANDIDATES=4 with integer protect_min=1 the leg's
    authority is BINARY, so the step shape may be an artifact of small K and generality in K
    is untested. V3-EXQ-937's self-route label conversion_independent_of_envelope_width is
    WITHDRAWN AS FALSE. Both runs are purpose=diagnostic, score nothing and PROMOTE NOTHING;
    status stays candidate. epistemic_category standard is written onto this claim in this
    cycle -- it was ABSENT before, so this is a real write rather than a restatement.
  depends_on:
    - ARC-106    # brain-like construction constraint: this IS the first major worked application -- a BG-like selector grounded at the level of FUNCTION, load-bearing not decorative
    - MECH-439   # the F-dominance conversion ceiling this constitution is proposed to resolve
    - Q-078      # the open question (constitutional vs parametric) whose CONSTITUTIONAL branch this commits to
    - MECH-260   # No-Go (indirect-pathway suppression) already in dACC
    - SD-034     # post-commit closure / de-commit latch family (with MECH-090 / MECH-342 / MECH-445 / MECH-446)
    - ARC-108    # MISSING PIECE (assembly-map A.4/E.2): unified dopamine substrate -- the LEARNING afferent that turns this arithmetic constitution into learned cortico-striatal weights; the constitution's completeness depends on it
    - ARC-109    # MISSING PIECE (assembly-map A.2/E.2): D1/D2 population split w/ asymmetric DA gain -- de-collapses the MECH-449 Go/No-Go additive scalar into two opponent populations (EARNS the disease axis)
    - MECH-450   # MISSING PIECE (assembly-map B1+B3/E.2): minimal recurrent settling step -- replaces the one-shot pallidal-readout argmin with a bounded settling competition (the named-but-unbuilt "thalamocortical recurrence" target)
  what_would_answer: >
    Instantiation is GATED on V3-EXQ-689a (see Q-078). The CONSTITUTIONAL branch is favoured if 689a gives
    uniform lift (conflict grading not load-bearing) or readiness-met-no-lift (near-tie levers insufficient)
    -- then a BG-constitution component (rank-preserving F->eligibility demotion MECH-448, Go-NoGo eligibility
    governance MECH-449, conflict-hold) must produce LAWFUL channel-specific conversion (note s8: selector
    variables vary as intended; specificity; gap/conflict sensitivity where claimed; gap-blind controls do not
    explain it; safety -- no global disinhibition; >=1 gated downstream channel converts; entropy-via-stochasticity
    does NOT count). It is REFUTED for the V3 tract if 689a gives a gap-concentrated PASS (the minimal
    conflict-graded lever suffices -- parametric, not constitutional). Each BG analogue carries an ARC-106
    divergence ledger + its own falsifier; a component that, ablated, leaves a pre-registered conversion metric
    unchanged is DECORATIVE and dropped.
  location: evidence/planning/thought_intake_2026-06-20_basal_ganglia_selector_constitution.md
  notes: >
    Registered 2026-06-20 from the basal-ganglia-selector-constitution design-pressure note. Captures the
    user's standing preference for basal-ganglia-MIRRORING selection structure (flagged 2026-06-20 as
    under-served by the current single-scalar-argmax / isolated-modulatory-patch machinery), made a
    first-class ARC-106-grounded architectural direction rather than left as planning prose. GATE AROUND
    INSTANTIATION: status candidate, sequenced BEHIND V3-EXQ-689a -- captured now, built only if 689a's
    outcome favours the constitutional reading (note s10 governance posture); registration != build.
    Children MECH-447/448/449 are the testable mechanism legs; Q-078 is the umbrella question. Risk
    register (note s9): overbuild before 689a, cargo-cult biology (each analogue needs a function +
    divergence ledger + falsifier), noise-as-diversity (adaptive access != random entropy), commitment
    instability (eligibility/hold machinery must couple to a stable latch). Already-built PARTIAL instances
    NOT duplicated: modulatory-bias-selection-authority substrate (top-k eligibility set), MECH-260 (No-Go),
    MECH-439 conflict-graded Factor-A/B levers, SD-034 / MECH-090 / MECH-342 latch. Cross-plan home:
    biology_grounding_convergence_v4_plan BG-2 (selector) + BG-3 (commit latch);
    behavioral_diversity_isolation:GAP-I.

# ============================================================================
# BASAL-GANGLIA MISSING-PIECE CLAIMS (assembly-map intake-reap, 2026-06-22)
# Registered from basal_ganglia_assembly_map_2026-06-22.md SS A.5 / E.2 -- the
# three missing-piece architectural claims the assembly map flagged as its next
# governance action (intake-reap, NOT future-registration prose). All three are
# candidate / substrate_conditional (promote/demote suppressed; the right
# response is to BUILD/WAIT for the substrate, not run more experiments on the
# current one). Each carries the ARC-106-mandated psychiatric_failure_mode line
# (the EARNS column). Wired into ARC-107 (the BG-constitution umbrella) both
# ways: ARC-107.depends_on lists them (its completeness depends on these missing
# pieces, SS 6b ledger) and each lists ARC-107 (child->umbrella, matching
# MECH-447/448/449). The learned-gating ALTERNATIVE to f_dominance_conversion_
# ceiling's arithmetic levers (MECH-447/448/449) -- NOT duplicates of them.
# ============================================================================
- id: ARC-108
  title: "Unified dopamine substrate -- signed dopaminergic-RPE teaching signal feeding LEARNED gating weights on the E3 selector: the cortico-striatal weights that decide which channel wins committed-selection authority should themselves be LEARNED via three-factor plasticity (Hebbian co-activation x signed dopaminergic RPE) with the D1-LTP / D2-LTD asymmetry, consolidating the currently-scattered RPE / vigor / incentive functions into one substrate and supplying the GPi->lateral-habenula negative-RPE drain. The ARC-107 arbitration layer (MECH-447/448/449 + the authority rescale) is today pure arithmetic with NO learned parameters; F monopolises 88-89% of committed-selection variance (MECH-439) precisely because no learned striatal weighting can re-weight channels through experience. The single highest-leverage missing BG piece and the proposed next attack on MECH-439."
  claim_type: architectural_commitment
  subject: selection.unified_dopamine_substrate_learned_gating
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-732_2026-07-10
      as_of: 2026-07-10
      verdict: non_contributory/precondition_unmet
  epistemic_category: substrate_conditional
  implementation_phase: v3   # PULLED FORWARD v4->v3 2026-06-22 (user-ratified): the V3-minimal slice is in v3 scope; the routed-efferent / tonic-phasic-gate refinements stay v4 (see notes SCOPE BET)
  version_relevance: v3
  registered_utc: "2026-06-22"
  pending_retest_after_substrate: true
  implementation_note: >
    SUBSTRATE BUILD 2026-07-04 (PROMOTES NOTHING): the BOUNDED ascending-spiral gain -- a target-PARITY
    controller -- was built no-op-default in ree-v3 (config.e3.use_ascending_parity_controller +
    loop_segregation_parity_forward_gain / parity_ceiling_ratio / parity_plasticity_gain / m_cross_clamp;
    e3_selector._parity_forward_gain holds w_eff[limbic] under ceiling_ratio*w_eff[motor], + an anti-windup
    clamp on the ascending M_cross maturation). It repairs the RAW-scalar runaway the confirmed
    failure_autopsy_V3-EXQ-711_2026-07-04 found (w_eff[limbic] 10-2274x w_eff[motor] = a limbic MONOPOLY),
    so the ARC-108 learned-gating cross-loop route can be re-tested under a FAIR bounded parity arbitration
    rather than the 711 saturated one (which never validly tested it). A SATURATION GUARD was added to the
    falsifier's limbic_loop_can_win gate. Successor falsifier V3-EXQ-713 queued. Byte-identical OFF.
    PROMOTES NOTHING: ARC-108 stays candidate/substrate_conditional + pending_retest_after_substrate until
    713 adjudicates (the 711 self-routed weakens was REJECTED). See ree-v3/CLAUDE.md 'BOUNDED
    ascending-spiral gain -- target-PARITY controller' + learned_cross_loop_arbitration.md 'Sub-addendum:
    bounded parity controller'.
  evidence_quality_note: >
    V3-EXQ-700d (same-layer-null validity re-tune; tagged ARC-108) FAIL/non_contributory -- the
    magnitude-matched same-layer null still could not verify-lift committed-class entropy in the V3
    single arena (3rd failed valid-null construction). non_contributory: does NOT weaken ARC-108 (the
    substrate-conditional learned-gating substrate is not yet built). Detail + cluster diagnosis in
    failure_autopsy_700d-708-single-arena-ceiling_2026-06-29 (primary record on MECH-439). Route = the
    already-built v4_loop_segregation (ARC-110), validating via V3-EXQ-707b. pending_retest_after_substrate.
    UPDATE 2026-07-03 (confirmed failure_autopsy_V3-EXQ-709_2026-07-03): V3-EXQ-709 (ARC-108 x ARC-110
    LEARNED/DA-gated cross-loop arbitration validation, tagged ARC-108) FAIL / non_contributory /
    non_degenerate=False / substrate_not_ready_requeue. The learned-gating substrate ENGAGED exactly as this
    claim's what_would_answer requires -- the signed-RPE M_cross term carried live variance and DID change
    gating weights (M_cross moved off init range 0.116, so LEARNED is not bit-identical to STATIC; 6/7 readiness
    gates met). But the deeper limbic_loop_can_win non-vacuity sub-gate is UNMET: the learned coupling lifted
    the limbic loop to >= motor effective weight on only 1/4 divergent seeds (M_cross[motor,limbic] peak ~0.03),
    so the "does learned gating lift committed diversity beyond the arithmetic envelope" question could NOT be
    validly evaluated. Per this claim's own non-degeneracy rule this is substrate_not_ready_requeue, NOT a
    weakens -- ARC-108 is NOT weakened. Re-derive brake FIRES (6th ARC-108 substrate_ceiling/non_contributory
    reading); routed implement-substrate (AMEND v4_loop_segregation failure_record). pending_retest_after_substrate.
    UPDATE 2026-07-04 (confirmed failure_autopsy_V3-EXQ-711_2026-07-04): V3-EXQ-711 (ARC-110 x ARC-108
    ASCENDING-SPIRAL GAIN validation, tagged ARC-108) FAIL / non_degenerate=True. The manifest self-routes
    ARC-108 "weakens"; the confirmed autopsy REJECTS that. The ascending-gain repair the 709 autopsy routed to
    was tested, and the load-bearing limbic_loop_can_win precondition was recorded met (3/3 divergent seeds) --
    but by a SATURATED runaway (M_cross range peak 4897.8; w_eff[limbic] 10-2274x w_eff[motor]) not a competitive
    parity win, so the learned-gating-at-the-arbitration route was exercised under a degenerate (limbic-monopoly)
    regime. Committed-class entropy FELL on 2/3 divergent seeds; the conversion could NOT be validly measured ->
    substrate_not_ready_requeue, NOT a weakens -- ARC-108 is NOT weakened (a bounded/parity ascending gain was
    never tested). The raw-scalar gain lacks a controller (no stable parity regime between 709's sub-threshold
    ~0.03 and 711's 10-2274x runaway); the fix is a bounded/normalized gain + a saturation guard on the win-gate,
    NOT another gain-magnitude guess. Re-derive brake FIRES (7th ARC-108 substrate_ceiling/non_contributory
    reading); blind same-claim gain-magnitude re-queue REFUSED; routed implement-substrate (AMEND
    v4_loop_segregation failure_record). pending_retest_after_substrate.
    UPDATE 2026-07-05 (confirmed failure_autopsy_V3-EXQ-713_2026-07-05): V3-EXQ-713 (tagged ARC-108) FAIL /
    non_degenerate=True. Manifest self-routes 'weakens'; REJECTED. The bounded controller delivered a FAIR
    non-saturated limbic parity win (4/4 seeds), so unlike 709 (sub-threshold) / 711 (saturated) the
    learned-gating arbitration route was exercised NON-DEGENERATELY -- and did NOT convert (C1 fail). This
    EXHAUSTS the cross-loop-arbitration-reweighting realization of ARC-108's learned gating but does NOT weaken
    ARC-108 as an architectural commitment (conversion route is the downstream selection face, MECH-448, lifting
    on GAP-A). non_contributory. Re-derive brake FIRES (8th ARC-108 reading); same-level arbitration re-queue
    REFUSED; route = selection-face MECH-448 behavioural retests. pending_retest_after_substrate.
  psychiatric_failure_mode: >
    On breakage / dysregulation of the signed-RPE teaching signal: the dopaminergic
    learning axis is the substrate of reward-prediction-error psychopathology --
    tonic/phasic DA imbalance (addiction; the positive symptoms of schizophrenia via
    aberrant-salience RPE), anhedonia / blunted RPE in depression, and the
    impulse-control / dyskinesia spectrum when the D1-LTP / D2-LTD asymmetry is
    deranged (the full Go/No-Go disease axis is EARNED jointly with ARC-109). A
    purely-arithmetic gating layer (the current state) cannot model any of these --
    it has no learning signal to derange. Earning this axis is exactly the ARC-106
    EARNS criterion.
  depends_on:
    - ARC-107    # the BG-constitution umbrella; this is the MISSING LEARNING afferent (assembly-map A.4) that turns its arithmetic envelope (MECH-448/449) into learned cortico-striatal weights
    - ARC-106    # brain-like construction constraint: the learning rule is grounded at FUNCTION (three-factor Hebbian x signed RPE) and EARNS the dopaminergic psychiatric axis
    - MECH-439   # the F-dominance conversion ceiling RESTATED: F monopolises selection because the gating layer learns nothing; learned re-weighting is the proposed attack
    - ARC-016    # the existing unsigned prediction-ERROR-VARIANCE precision signal -- DISTINCT from (not a substitute for) the signed RPE this claim requires (divergence B5: a category substitution that would silently block three-factor learning)
    - MECH-203   # the existing 5-HT neuromodulator built BEFORE its DA opponent (divergence B8); this substrate supplies the missing principal
    - MECH-448   # the rank-preserving F->eligibility demotion this would re-derive as LEARNED relative re-weighting (assembly-map C2: "behead the monarch" -> "learn the monarch's weight down")
    - ARC-110    # COUPLED / SEQUENCING FORK: Haber's dopamine spiral integrates the segregated loops, so this substrate's completeness depends on there being segregated loops to couple; ARC-110's falsifier (does ARC-108+MECH-450 resolve MECH-439 on the COLLAPSED arena? -- V3-EXQ-700) is the explicit test of whether loop segregation is required or this build suffices
  what_would_answer: >
    SUBSTRATE-CONDITIONAL / NOT YET BUILT (the substrate is planned, not present). A future experiment SUPPORTS
    this on the note-s8 bar: a signed RPE teaching term applied to the E3 gating weights (i) re-weights at least
    one previously-F-dominated modulatory channel into committed-action authority THROUGH EXPERIENCE (a learned
    shift the fixed-arithmetic envelope cannot produce), (ii) preserves safety (No-Go-suppressed classes are not
    learned back in), and (iii) the teaching signal is genuinely SIGNED (Go-up is dissociable from No-Go-up --
    not the unsigned variance of ARC-016), on >=2/3 seeds. WEAKENED if learned gating does not lift committed
    diversity beyond the arithmetic MECH-448/449 envelope (the learning rule is then over-specification), or if
    the only lift comes from added stochasticity (noise-as-diversity guard). NON-DEGENERACY: the RPE term must
    carry live cross-arm/cross-tick variance and actually change a gating weight, else substrate_not_ready_requeue.
  location: evidence/planning/basal_ganglia_assembly_map_2026-06-22.md
  notes: >
    Registered 2026-06-22 as the intake-reap (assembly-map step E.2) of the SINGLE BIGGEST missing BG piece
    (assembly-map A.4 / A.5 item 1): the ARC-107 arbitration layer is pure arithmetic with no learned parameters,
    so F-dominance (MECH-439) is the predicted failure of a system that selects but cannot LEARN to select.
    Consolidates the scattered RPE/vigor/incentive functions (MECH-314 curiosity, MECH-320 vigor, MECH-295 liking
    are afferents, not a unified DA substrate) and supplies the missing GPi->lateral-habenula negative-RPE drain
    (assembly-map A.3). Resolves divergence B5 (RPE-as-unsigned-variance) and B8 (5-HT-before-DA), and is the
    long-term resolution of B2 (the MECH-448 F-deletion lever should become unnecessary once F's weight is LEARNED
    down in contexts where the modulator matters -- assembly-map C2). SCOPE BET -- RATIFIED v4->v3 2026-06-22
    (user-confirmed "yes we need to pull it into v3"): originally a STATED V4 bet in
    arc_107_selector_constitution_design_2026-06-20.md s6b ("V3 eligibility is hand-shaped, not learned by RPE;
    the learning signal is v4"); the assembly map (A.5, C1/C2) RECOMMENDED pulling it forward as the next MECH-439
    attack, the dopamine-into-gating design note (E.3) decided learned-gating-over-arithmetic-envelope, and the
    unified-substrate design note (unified_dopamine_substrate_design_2026-06-22.md s8/s10.1) defined the V3-minimal
    slice; the user then RATIFIED the pull-forward. implementation_phase + version_relevance moved v4->v3. THE V3
    SLICE (now in scope): the one shared dopamine primitive -- signed phasic RPE delta_t (distinct from ARC-016
    unsigned variance, B5) + tonic baseline V-hat_t + goal-proximity ramp rho_t -- driving BOTH jobs: JOB 1 learned
    per-channel selection gating w_chan (the MECH-439 attack), and JOB 2 the control-plane driver's
    maintenance-ramp (rho_t replaces the flat-hold maintenance DRIVER; deviation B6 fix) + the habenula
    negative-delta_t de-commit as an INTERNAL scalar abort input to the SD-034 closure operator (the parked rung-6
    fix; composes with the MECH-090/342/SD-034 latch machinery, does not replace it). WHAT STAYS V4 (named cuts,
    ARC-106 zero-silent-divergence): the lateral-habenula as a ROUTED EFFERENT drain (vs the v3 internal scalar);
    the tonic->commit-threshold and phasic->go gate REFINEMENTS (the current arithmetic readiness gates work, so
    these are lower-leverage); the D1/D2 population split (ARC-109, separate claim, stays v4); the full
    thalamo-cortical learned loop (MECH-450 is the v3-minimal settling step; the full loop is v4). COUPLED to
    MECH-450 (the recurrent-settling-step repair -- the lateral-inhibition weights are themselves learned in
    biology) and to ARC-109 (the D1/D2 split is the population substrate the D1-LTP/D2-LTD asymmetry acts on). This
    is the LEARNED-GATING alternative to the f_dominance_conversion_ceiling arithmetic levers, NOT a duplicate.
    ARC-106 cargo-cult guard: the learning rule needs a function + divergence-ledger row + ablation falsifier before
    it counts as load-bearing. Stays status:candidate / epistemic_category:substrate_conditional (promote/demote
    suppressed -- the right next response is the /implement-substrate BUILD of the V3 slice, then the s7 falsifiers,
    NOT promotion). PROMOTES NOTHING (re-scope, not a promotion).
    IMPLEMENTATION NOTE (JOB-1 step-1 BUILT 2026-06-22, /implement-substrate): the learned per-channel
    selection-weight vector w_chan + the signed-RPE delta_t teaching signal landed in
    ree-v3/ree_core/predictors/e3_selector.py behind use_learned_channel_gating (E3Config, no-op default ->
    bit-identical OFF). w_chan softplus-weights the _modulatory_accum composition (channels score_bias/mech341/
    route; init softplus==1 -> bit-identical); delta_t = R_t (benefit_eval - harm_eval at the realised state,
    reusing the trained heads) - V-hat_t (slow EMA), SIGNED and kept SEPARATE from the ARC-016 unsigned variance
    (divergence B5); three-factor Hebbian x signed-RPE x D1/D2-asym update in post_action_update; waking-only
    (MECH-094). Composes INSIDE the F-bounded MECH-448/449 eligible set (raw scores/F untouched -> safety inherited).
    MECH-450 settling step (W_lat) is OFF (factor 2, reserved integration point). 9/9 contracts + 8/8 preflight +
    168 e3-cluster contracts + agent activation smoke PASS. Stays candidate / substrate_conditional / v3 -- the
    s7 selection falsifier + the MECH-450 settling step + the JOB-2 control-plane pair are SEPARATE downstream
    chips. Doc docs/architecture/dopamine_into_gating.md; ree-v3/CLAUDE.md "ARC-108 JOB-1 step-1" entry.
    PROMOTES NOTHING (substrate build, no flag/confidence/status change).
    IMPLEMENTATION NOTE (JOB-2 control-plane DRIVER pair BUILT 2026-06-22, /implement-substrate; ree-v3 c5614ab --
    substrate, PROMOTES NOTHING): the two JOB-2 control-plane drivers landed, both reusing the JOB-1 signed-RPE
    delta_t = R_t - V-hat_t (no new teaching signal). (c) the rho_t maintenance RAMP
    (ree-v3 ree_core/policy/rho_maintenance_ramp.py = goal_proximity x benefit valuation) REPLACES the flat-hold
    maintenance DRIVER of the natural-commit latch-hold -- it peaks-then-declines so the hold SELF-LIMITS past the
    proximity peak (deviation B6 / the 460h-monolith structural fix), composing with the MECH-090/342/SD-034 latch
    machinery (latch untouched -- only the maintenance driver is swapped). (d) the habenula negative-delta_t DE-COMMIT
    is a new INTERNAL-scalar abort input to the SD-034 ClosureOperator (habenula_tick, reason='habenula') fired from
    agent.update_residue on delta_t < threshold (worse-than-expected); ADDED ALONGSIDE the refractory-timer release
    (the operator / refractory / No-Go release path is NOT replaced), content-driven and dissociable from the timer
    (D3). The lateral-habenula as a ROUTED EFFERENT drain stays V4 (internal scalar only in v3). No-op-default +
    bit-identical OFF + waking-only (MECH-094). Design doc docs/architecture/arc_108_job2_control_plane.md; the
    sec-7.2 control-plane L0/L1/L2 falsifier is queued SEPARATELY. PROMOTES NOTHING (substrate build, no
    flag/confidence/status change).
    IMPLEMENTATION NOTE (sec-7 C3 ablation flag BUILT 2026-06-22, /implement-substrate; substrate, PROMOTES
    NOTHING): the runtime knob the V3-EXQ-700 selection 2x2 DEFERRED for C3 (signed-vs-unsigned RPE; divergence
    B5). E3Config.learned_channel_rpe_mode (Literal["signed","unsigned"], default "signed", surfaced through
    REEConfig.from_dims onto config.e3) -- when "unsigned", e3_selector.post_action_update substitutes the UNSIGNED
    ARC-016 prediction-error magnitude (self._running_variance, always >= 0) for the signed delta_t in BOTH the
    JOB-1 w_chan AND the MECH-450 W_lat three-factor updates, removing the directional potentiate-vs-depress credit.
    "signed" (default) is BYTE-IDENTICAL to the prior substrate (learn_signal == delta_t); the signed delta_t is
    kept intact for the JOB-2 habenula de-commit + the V-hat_t baseline EMA (only the SELECTION teaching signal is
    ablated). C7 contract (mirror of the C5 signed potentiate-vs-depress test): under "unsigned" a good and a bad
    outcome move w_chan in the SAME direction -- opposite-sign credit is structurally impossible. 10/10 ARC-108
    contracts + 108 e3-cluster + 8/8 preflight + from_dims wiring smoke PASS. The C3 ablation ARM (A1-config +
    learned_channel_rpe_mode="unsigned") is added to a V3-EXQ-700 sibling/successor in a SEPARATE /queue-experiment
    session, pre-registered that unsigned must FAIL to convert while signed (A1) converts; if unsigned converts just
    as well the signed-RPE claim is refuted and the mechanism collapses to a precision re-weighting (route back to
    ARC-016, mint no learning claim). Design-of-record evidence/planning/dopamine_into_gating_design_2026-06-22.md
    sec 5.2 C3; ree-v3/CLAUDE.md "ARC-108 sec-7 C3" entry. PROMOTES NOTHING (no-op-default flag, no flag/confidence/
    status change).
    ROUTED-SUBSTRATE CROSS-REF 2026-06-24 (failure_autopsy_V3-EXQ-700b): the V3-EXQ-700 lineage could not
    validly test learned-gating conversion on the single arena (the committed-class null is decoupled from the
    DV; loop-segregated diversity is unreachable). The V4 substrate that supplies a valid same-layer null +
    segregated loops is registered as v4_loop_segregation (owning claim ARC-110; design_doc
    docs/architecture/sd_v4_loop_segregation.md). ARC-108's w_chan learned gating is re-tested there. PROMOTES NOTHING.
    IMPLEMENTATION NOTE (ARC-108 x ARC-110 LEARNED CROSS-LOOP arbitration BUILT 2026-07-01, /implement-substrate;
    substrate, PROMOTES NOTHING): the coupling this claim's depends_on ARC-110 names -- learned gating operating AT
    the CROSS-LOOP arbitration, not only within-arena (w_chan) -- landed behind no-op-default flag
    config.e3.use_learned_cross_loop_arbitration (+ learned_cross_loop_eta 0.01; requires use_loop_segregation on).
    The escalation source is V3-EXQ-707b (failure_autopsy_V3-EXQ-707b_2026-06-29): loop segregation built fully-live
    (all 6 gates passed, limbic range 1.414) yet the limbic loop NEVER won because the cross-loop combine was STATIC
    ARITHMETIC (fixed spiral gains inherit F's dominance). The fixed combine in e3_selector._segregated_loop_arbitrate
    is replaced by a LEARNED [3,3] cross-loop matrix W_cross = I + M_cross (register_buffer, loop order
    motor/associative/limbic, init 0 -> W_cross==I -> BIT-IDENTICAL at init and OFF), updated by the SAME ARC-108
    signed-RPE three-factor rule as w_chan/W_lat (one shared dopaminergic delta_t + D1-D2 asym -- Haber's single
    ascending spiral) via an outer-product Hebbian coact trace coact[i,j] = post_i*pre_j (pre_j = -loop_z_j[committed],
    SIGNED directional credit; post_i = -eff_i[committed]). M_cross[motor,limbic] is the learnable ascending-spiral
    path by which the limbic value loop learns to drive the motor commit. Waking-only (MECH-094). Safety inherited:
    arbitration stays strictly within the F+MECH-448/449 eligible set (weights only reorder, never re-admit). Couples
    with MECH-450 (settling shapes within-loop, cross-loop weights shape across-loop; both ride the same delta_t). Per
    the 2026-06-29 MECH-439 /claim-synthesis discrimination gate (borderline child MECH-453, recommended DROP -- this
    is the ARC-108 x ARC-110 intersection, and ARC-108 already depends_on ARC-110), NO new claim is minted; the
    coupling is annotated here + on ARC-110. Full suite green (1353 passed / 3 pre-existing-unrelated); regression
    guard ree-v3/tests/contracts/test_learned_cross_loop_arbitration.py (byte-identical-OFF/at-init, non-vacuity,
    MECH-094 gate, limbic-can-win, safety). Design-of-record docs/architecture/learned_cross_loop_arbitration.md;
    ree-v3/CLAUDE.md "ARC-108 x ARC-110 coupling" entry. Validation = a SEPARATE new-EXQ falsifier (A1_LOOPS +
    learned cross-loop arbitration STRICT-ABOVE A1_LOOPS + static arbitration; different claim_ids), queued after this
    build lands. PROMOTES NOTHING (no-op-default flag; no flag/confidence/status change).
- id: ARC-109
  title: "D1/D2 striatal population split with asymmetric dopamine gain: the Go (direct/D1) and No-Go (indirect/D2) pathways should be represented as TWO populations with OPPOSITE dopamine sensitivity (DA potentiates D1/Go via LTP, depresses D2/No-Go via LTD), NOT collapsed into one additive scoring scalar. REE today renders Go/No-Go as a scoring SIGN (benefit_eval_head minus harm_eval_head; MECH-449 Go-promote / No-Go-suppress over the eligible set) -- dissociable pathways pre-summed into one cost. The split is REQUIRED to EARN the Parkinson / dyskinesia / Huntington / impulse-control-disorder psychiatric axis per the ARC-106 EARNS criterion."
  claim_type: architectural_commitment
  subject: selection.d1_d2_population_split_asymmetric_da_gain
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v3   # REAPPOINTED V4->V3 2026-06-24 (user-directed, coupled to the ARC-110 loop-segregation reappointment): the D1/D2 population split is co-requisite to the segregated loops (asymmetric DA gain acts per-loop) and is recouped onto the V3 critical path with ARC-110. BUILT 2026-06-27 with ARC-110. Still substrate_conditional; PROMOTES NOTHING.
  version_relevance: v3
  registered_utc: "2026-06-22"
  implementation_note: >
    SUBSTRATE BUILT 2026-06-27 via /implement-substrate (coupled to ARC-110; PROMOTES NOTHING --
    stays candidate / substrate_conditional / v3 until V3-EXQ-707 scores). Behind no-op-default
    flag config.e3.use_d1_d2_population_split (+ d1_da_gain / d2_da_gain), the
    ree-v3/ree_core/predictors/e3_selector.py _d1_d2_split decomposes each segregated loop's
    accumulator into a Go/D1 population (relu(-accum)) potentiated by DA (LTP) and a No-Go/D2
    population (relu(+accum)) depressed by DA (LTD); net cost = D2 - D1; da = the shared ARC-108
    value baseline V-hat_t (tanh-squashed). At da==0 net == accum EXACTLY (bit-identical; the
    dissociation is earned only once da != 0), so high-Go+high-No-Go CONFLICT becomes dissociable
    from low-Go+low-No-Go indifference (the additive-scalar collapse divergence B4; diagnostic
    loop_d1_d2_conflict_signal). The learning-update D1-LTP/D2-LTD asymmetric gain (learn_asym)
    already existed; this adds the SELECTION-layer two-population representation the claim asserts.
    Requires loop segregation on (acts per-loop). See ARC-110 implementation_note.
  psychiatric_failure_mode: >
    This split is the substrate WHOSE BREAKAGE IS the D1/D2 disease axis, so it must
    exist before the axis can be modelled (ARC-106 EARNS): D2-pathway hypofunction /
    dopamine depletion -> Parkinsonian akinesia; D1 overdrive / dopaminergic excess
    -> dyskinesia and impulse-control disorders; striatal degeneration with D2 loss
    -> Huntingtonian chorea; and the approach-avoidance / OCD-CSTC axis that the
    additive collapse (divergence B4) pre-sums away (high-Go+high-No-Go conflict is
    today indistinguishable from low-Go+low-No-Go indifference). A single additive
    scalar has no D1-vs-D2 degree of freedom to lesion, so none of these can be
    modelled -- the EARNS failure.
  depends_on:
    - ARC-107    # the BG-constitution umbrella; this splits its Go/No-Go scoring-SIGN (MECH-449) into two opponent populations
    - ARC-106    # EARNS criterion: the population split is what makes the Parkinson/dyskinesia/Huntington/ICD axis modellable
    - ARC-108    # the unified dopamine substrate supplies the signed RPE; the asymmetric D1-LTP / D2-LTD gain is the population substrate that RPE acts on (coupled build)
    - MECH-449   # the Go/No-Go eligibility constitution this de-collapses from one additive cost into two opponent pathways (divergence B4)
    - MECH-260   # the No-Go / indirect-pathway suppression that would route through the D2 population
  what_would_answer: >
    SUBSTRATE-CONDITIONAL / NOT YET BUILT. A future experiment SUPPORTS this if two opponent populations with
    asymmetric DA gain (i) preserve a representational distinction the additive scalar cannot -- approach-avoidance
    conflict (high-Go+high-No-Go) is dissociable from indifference (low-Go+low-No-Go) in committed behaviour --
    and (ii) a targeted D1-vs-D2 gain lesion reproduces a recognisable disorder analog (e.g. D2 hypofunction ->
    akinesia/perseveration) that a single-scalar lesion cannot, on >=2/3 seeds. WEAKENED if the split yields no
    behavioural or failure-mode distinction beyond the additive MECH-449 scalar (decorative, dropped per the
    ARC-106 load-bearing-vs-decorative ablation test). NON-DEGENERACY: the two populations must carry live,
    DISSOCIABLE cross-arm variance (not a re-parameterised single axis), else substrate_not_ready_requeue.
  location: evidence/planning/basal_ganglia_assembly_map_2026-06-22.md
  notes: >
    Registered 2026-06-22 as the intake-reap (assembly-map step E.2) of assembly-map A.2 / A.5 item 2 and the
    repair target for divergence B4 (Go/No-Go additive collapse, previously SILENT). REE's Go/No-Go is today a
    scoring SIGN over one additive cost (score = f + lambda*m + rho*Phi - beta*b), not two populations with
    opposite dopamine sensitivity; the additive collapse makes approach-avoidance conflict indistinguishable from
    indifference (a representational loss). COUPLED to ARC-108 (the signed RPE is what produces the D1-LTP /
    D2-LTD asymmetry -- this is the population substrate the learning rule acts on; build them together) and is
    the structural prerequisite the ARC-106 EARNS criterion needs for the dopaminergic psychiatric axis. V4 per
    the assembly-map sequencing (after the dopamine substrate, A.5 item 2). ARC-106 cargo-cult guard: the split
    needs a function + divergence-ledger row (B4) + an ablation falsifier (a D1-vs-D2 lesion the additive scalar
    cannot reproduce) before it counts as load-bearing. PROMOTES NOTHING (new candidate).
- id: MECH-450
  title: "Minimal recurrent settling step over the eligible set: replace the one-shot feed-forward argmin/softmax committed-selection readout with a few rounds of mutual-inhibition (surround-inhibition) updates on the within-eligible _modulatory_accum field BEFORE commit, so the committed action emerges from a bounded recurrent SETTLING competition rather than a single global comparison. A hard argmin over an F-dominated score always returns the F-winner (the conversion ceiling); a settling competition with lateral inhibition can FLIP the selected attractor when a modulatory channel is strong enough, and turns the additive _modulatory_accum BLEND into a competitive winner-take-most. The V3-bounded version of the BG's recurrent settling dynamics; the full learned thalamo-cortico-basal loop is the V4 bet."
  claim_type: mechanism_hypothesis
  subject: selection.minimal_recurrent_settling_step
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-710_2026-07-03
      as_of: 2026-07-03
      verdict: non_contributory/substrate_ceiling
  epistemic_category: substrate_conditional
  implementation_phase: v3
  version_relevance: v3
  registered_utc: "2026-06-22"
  pending_retest_after_substrate: true
  evidence_quality_note: >
    V3-EXQ-700d (same-layer-null validity re-tune; tagged MECH-450) FAIL/non_contributory -- the
    magnitude-matched same-layer null still could not verify-lift committed-class entropy in the V3
    single arena (3rd failed valid-null construction). non_contributory: does NOT weaken MECH-450 (the
    substrate-conditional minimal-settling step is not yet built). Detail + cluster diagnosis in
    failure_autopsy_700d-708-single-arena-ceiling_2026-06-29 (primary record on MECH-439). Route = the
    already-built v4_loop_segregation (ARC-110), validating via V3-EXQ-707b. pending_retest_after_substrate.
    CORROBORATION 2026-07-03 (confirmed failure_autopsy_V3-EXQ-710_2026-07-03): V3-EXQ-710 (MECH-140 x
    MECH-450 DISINHIBITORY SOFT-COMPETITIVE SETTLING validation; the minimal recurrent settling step was
    BUILT 2026-07-02 as the parameter-free soft-competitive half) FAIL / non_degenerate=True. The manifest
    self-routes MECH-450 weakens; the confirmed autopsy REJECTS it as non_contributory. The settling step
    is now BUILT + LIVE in single-arena (scs_mean_round_delta 5-8; A2_ABLATED uniform kernel reproduces
    A0_OFF bit-identically 1.130438, so the structured class-surround kernel is the sole active ingredient),
    but over the single F-collapsed within-eligible field it SHARPENS the F-winner rather than converting
    diversity: on 2/3 divergent seeds committed-class entropy REDUCED (-0.169/-0.153 nats), INTACT mean 1.047
    < OFF 1.130, C1 passed 1/3 divergent seeds. The pre-registered WEAKENED clause (settling does not lift
    diversity beyond the pre-filters) does NOT apply, because it presumes the settling ran where it COULD
    convert; single-arena is the wrong LOCUS -- biology (VIP->SOM disinhibition; Keller2020/Aquino2026)
    reconfigures competition BETWEEN differentiated loops, which the single shared selector denies. So this
    stays non_contributory (substrate-limited), consistent with the standing "does NOT weaken MECH-450"
    reading -- what is still-not-built is the SEGREGATED-LOOP substrate the settling must run over, not the
    settling step itself. Same single-arena F-dominance ceiling as the 700-lineage and 709, third distinct
    mechanism. Re-derive brake FIRES (5th MECH-450 substrate_ceiling/non_contributory reading); same-claim
    single-arena TEST re-queue REFUSED; routed implement-substrate (AMEND v4_loop_segregation failure_record).
    pending_retest_after_substrate.
  psychiatric_failure_mode: >
    On breakage of the settling dynamics: too few / too-weak inhibition rounds ->
    failure to converge to a single winner = the indecision / blend-output failure
    (an avolition / abulia analog, and the channel co-activation that surround
    inhibition exists to prevent); runaway / excessive recurrent gain -> a locked
    attractor that cannot be flipped by new evidence = a perseveration /
    catatonic-fixity / obsessional-loop analog. The one-shot argmin has no settling
    dynamics to derange, so it cannot model either pole -- it can only ever return
    the static F-winner.
  depends_on:
    - ARC-107    # the BG-constitution umbrella; this replaces its one-shot pallidal readout with a bounded recurrent settling competition (its named-but-unbuilt "thalamocortical recurrence" target)
    - MECH-439   # the F-dominance conversion ceiling: a hard argmin STRUCTURALLY returns the F-winner; a settling competition can flip the attractor (assembly-map C1)
    - MECH-448   # the within-eligible _modulatory_accum arbitration whose ADDITIVE BLEND (divergence B3-blend) this converts into a competitive winner-take-most
    - ARC-108    # COUPLED: the lateral-inhibition weights are themselves learned in biology; the full loop needs the dopamine substrate (the minimal V3 step uses fixed inhibition)
    - MECH-449   # the Go/No-Go eligible set the settling step operates over
  what_would_answer: >
    SUBSTRATE-CONDITIONAL / NOT YET BUILT (the minimal settling step is V3-tractable but unbuilt). A future
    experiment SUPPORTS this if a bounded settling step (a few mutual-inhibition rounds over the eligible set)
    lifts committed-action-class entropy above the one-shot argmin readout AND a sufficiently strong modulatory
    channel FLIPS the committed attractor away from the F-winner (a capability the argmin structurally lacks),
    with no harmful-class disinhibition and not via added stochasticity (noise-as-diversity guard), on >=2/3
    seeds. WEAKENED if the settling step does not lift diversity beyond the MECH-448/449 pre-filters (the
    recurrence is then unnecessary in V3) or only converges to the same argmin winner. NON-DEGENERACY: the
    settling iteration must actually MOVE the _modulatory_accum field across rounds (live cross-round variance;
    not a no-op pass), else substrate_not_ready_requeue.
  location: evidence/planning/basal_ganglia_assembly_map_2026-06-22.md
  notes: >
    Registered 2026-06-22 as the intake-reap (assembly-map step E.2) of the MERGED repair target for divergence
    B1 (one-shot argmin vs recurrent settling competition -- genuine repair target, assembly-map C1) and the
    B3-blend half of divergence B3 (the additive _modulatory_accum blend vs surround inhibition -- same root,
    assembly-map C3). The same minimal-settling-step experiment fixes BOTH: a few rounds of mutual inhibition over
    the eligible set turn the additive blend into a competitive winner-take-most and give the readout the
    attractor-flip property the argmin lacks. Evidence it is the right repair (assembly-map C1): the failure
    history is "structural bounding works (569i top-k), parametric tuning does not (514t gain, 569h candidate
    pool)" -- the signature of a system that needs a different SELECTION RULE, not a better scoring weight.
    COUPLED to ARC-108 (the dopamine-into-gating repair -- the lateral-inhibition weights are learned in biology;
    sequence after or alongside it). The B3 POLARITY half (default-inhibit-then-disinhibit vs default-pick) is NOT
    this claim -- it is a defensible V3 simplification registered as a stated bet (V3 has one effector, so the two
    are behaviourally equivalent; assembly-map C3). FULL learned thalamo-cortico-basal settling loop = V4 bet.
    ARC-106 cargo-cult guard: the settling step needs an ablation falsifier (entropy/attractor-flip unchanged when
    ablated => decorative) before it counts as load-bearing. PROMOTES NOTHING (new candidate).
    IMPLEMENTATION NOTE (2026-06-22): BUILT via /implement-substrate (ree-v3 e3_selector.py + config.py, no-op-default
    flag use_learned_settling_step). The SECOND factor of the learned-gating 2x2, coupled to the JOB-1 w_chan step
    (use_learned_channel_gating) and sharing the SAME signed-RPE delta_t / V-hat_t / D1-D2 asym. A bounded recurrent
    lateral-inhibition SETTLING step over the F-bounded eligible set runs BEFORE the within-eligible commit: for
    r in range(R~3): a=softmax(-accum/T); accum += onehot @ (W_lat @ (onehot^T @ a)) over mod_eligible =
    _modulatory_accum[eligible_idx]; then commit=argmin(settled accum)/sample(softmax(-settled accum/T)). Fixes B1
    (one-shot argmin->recurrent settling) AND B3-blend (additive blend->competitive winner-take-most) together. W_lat
    is the LEARNED inhibition matrix over candidate first-action CLASSES (a stable [C,C] object -- per-candidate [K,K]
    is impossible since K varies; the BG surround-inhibition between competing motor programs the MECH-449 opponency
    grounds), a register_buffer (local plasticity, never an optimizer/autograd target) init 0 -> settling no-op ->
    BIT-IDENTICAL OFF and at init. W_lat learned by the SAME three-factor Hebbian-coactivation x signed-RPE x asym rule
    as w_chan (one shared delta_t in post_action_update). Composes INSIDE the MECH-448/449 eligible set (raw scores/F
    untouched) -> safety inherited; never re-admits an excluded candidate. Waking-only (no settling/W_lat write on a
    simulation tick, mirror the JOB-1 gate); agent.reset() clears the within-episode settling trace. VALIDATED: 10/10
    new contracts (test_mech450_learned_settling_step.py: bit-identical OFF / W_lat moves under non-flat delta_t /
    waking-only gate / settling MOVES the field across rounds / envelope intact / shared-delta_t) + 9/9 JOB-1 + 8/8
    preflight + 74 e3-cluster + 1232 full-suite (3 fails = documented pre-existing runner-flake, CONFIRMED on a clean
    stash, zero e3 overlap). Selector activation: W_lat range 0.0->4.23 over 13 updates (non-flat delta_t); settling
    round_delta 6.16 (live cross-round movement); OFF==ON-at-init bit-identical; env-loop settling engaged 60/60 ON,
    0 OFF. PROMOTES NOTHING -- MECH-450 stays candidate / substrate_conditional / v3. The sec-7 selection 2x2
    falsifier (learned-w_chan x learned-W_lat) is now fully runnable -- a separate /queue-experiment chip.
    ROUTED-SUBSTRATE CROSS-REF 2026-06-24 (failure_autopsy_V3-EXQ-700b): the learned-W_lat settling SIGNAL
    strengthened across the 700 lineage (700 seed-42-only -> 700b majority-of-divergent C1 pass) but could not
    be SCORED -- the single-arena committed-class null perturbs policy temperature (decoupled from the DV), so
    noise_verified_lifting failed 1/3 -> 0/3 -> 0/3. The V4 substrate with a same-layer (settling-field) null +
    segregated loops is registered as v4_loop_segregation (owning claim ARC-110; design_doc
    docs/architecture/sd_v4_loop_segregation.md); MECH-450 settling is re-tested there. PROMOTES NOTHING.
    IMPLEMENTATION NOTE (2026-07-02): the PARAMETER-FREE half of MECH-450 -- the *minimal* recurrent settling
    step that bites at INIT (no learning) -- BUILT via /implement-substrate coupled with MECH-140, behind
    no-op-default flag use_soft_competitive_settling (config.e3) in e3_selector.py _soft_competitive_settle.
    The 2026-06-22 W_lat build above is the LEARNED settling (no-op at init, only bites after dopaminergic
    learning); this new step is the always-on GRADED soft-competitive disinhibition (softmax-support lateral
    inhibition over the within-eligible field, R~3 rounds, parameter-free class-surround kernel) that supplies
    the at-init attractor-flip the argmin structurally lacks -- MECH-450's "a settling competition can FLIP the
    selected attractor" property, without waiting for W_lat to learn. Runs on _modulatory_accum[eligible_idx]
    (single arena) OR the arbitrated cross-loop `final` (ARC-110), composing with W_lat and the learned
    cross-loop arbitration (M_cross / V3-EXQ-709) at a different level; orthogonal flag, default-OFF (and exact
    no-op at gain 0.0) -> byte-identical, no 709 collision. Safety inherited (transforms only the eligible
    subset); waking-only (MECH-094); no autograd. Full ree-v3 suite green + 11 new contracts
    (test_soft_competitive_settling.py: byte-identical OFF/gain-0 x12 seeds, non-vacuity flips the readout +
    round_delta>0, graded-not-WTA loser stays nonzero, safety harmful-outlier never selected, sim-gate,
    segregated compose). Lit grounding attached 2026-07-02 (Gallo Aquino/Rungratsameetaweemana PLOS Biology
    2026 inhibition-on-inhibition = context channel; Wang 2002/Rolls 2021 recurrent-attractor; lit_conf 0.837).
    Design-of-record docs/architecture/soft_competitive_disinhibition_settling.md (ARC-106 grounding ladder +
    load-bearing-vs-decorative ablation test + divergence ledger + psychiatric_failure_mode column). Validation
    + PLOS-Biology ablation falsifiers queued separately (new EXQ, claim_ids [MECH-140, MECH-450]). PROMOTES
    NOTHING -- MECH-450 stays candidate / substrate_conditional / v3 until a falsifier converts it.
- id: ARC-110
  title: "Parallel segregated cortico-BG-thalamic loops (motor / cognitive-set / motivational): committed-action selection should run through SEPARATE striatal loop channels -- a motor loop, an associative/cognitive-set loop, and a limbic/motivational loop -- each owning its own eligibility-and-gate, arbitrated AFTER within-loop competition, rather than collapsing all channels into ONE shared E3 selection arena where the dACC/lPFC/OFC analogs feed in only as additive biases. HYPOTHESIS (assembly-map A.2 / s.D): the F-dominance conversion ceiling (MECH-439) is partly an ARTEFACT of the single-arena collapse -- F monopolises 88-89% of committed-selection variance because every channel competes in one F-dominated arena; with segregated loops F could dominate only the motor loop and could not drown the limbic 'is this worth committing to' computation, which is a different loop with a different value. Translation target is FUNCTIONAL (the Alexander/DeLong/Strick parallel-loop organisation as a divergence-ledgered, falsifier-validated analogue), NOT anatomical mimicry."
  claim_type: architectural_commitment
  subject: selection.parallel_segregated_loops
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-25
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-707c_2026-07-25#V3-EXQ-707c"
      as_of: 2026-07-25
      verdict: "weakens (NARROW)/standard -- C2-release validation on a REPAIRED instrument (supersedes the withdrawn 707b weakens); the F-dominance conversion ceiling is INTRINSIC, not a single-arena-collapse artefact. Weakens the narrow single-arena-artefact SUB-hypothesis only; the architectural assertion (segregated loops) is intact. Category standard (this run is NOT a substrate_ceiling reading). Stays candidate/substrate_conditional."
  epistemic_category: substrate_conditional
  implementation_phase: v3   # REAPPOINTED V4->V3 2026-06-24 (user-directed): the segregated-loop build is recouped onto the V3 critical path because it attacks the V3 closure blocker MECH-439; GATED on V3-EXQ-704 (MECH-451 finer-channel pre-emption falsifier) -- build proceeds only if 704 fails to convert. Still substrate_conditional (build-deferred); promote/demote stays suppressed.
  version_relevance: v3
  registered_utc: "2026-06-22"
  evidence_quality_note: |
    V3-EXQ-707b (2026-06-29, C2-release validation, supersedes 707a/707; confirmed
    failure_autopsy_V3-EXQ-707b_2026-06-29; user-approved /governance 2026-06-29) -> weakens (NARROW).
    DECISIVE pre-registered test with a FULLY-LIVE substrate: all 6 non-degeneracy gates passed
    (named_channel_routing_live 1.414 >> 0.001 -- the limbic loop carried real per-candidate range,
    C2_drop_differs_from_a1 True 3/3; the 707 vacuous DROP==A1 is resolved). Committed-class entropy
    A1_LOOPS 0.838 ~ A0_SINGLE_ARENA 0.914 (BELOW); C1 0/3 seeds, C2 fail (DROP_LIMBIC 0.933 > A1). Per
    ARC-110's own pre-registered grid -> the conversion ceiling is INTRINSIC, NOT a single-arena-collapse
    artefact. WEAKENS the narrow single-arena-artefact SUB-hypothesis ONLY; does NOT falsify the
    architectural assertion (segregated loops are biologically real + load-bearing). NARROWED: loop
    segregation is necessary-but-not-sufficient; alone (with static arithmetic cross-loop arbitration) it
    does not lift the F-dominance ceiling. Missing dependency = dopamine-gated cross-loop arbitration
    plasticity (MECH-448/449/ARC-107); segregation built+live but the cross-loop arbitration does not
    LEARN, so F's static dominance persists in the combine. The substrate build itself SUCCEEDED (positive
    engineering datum -- all 6 gates live). Stays candidate / substrate_conditional / PROMOTES NOTHING.
    Fires the contingent MECH-439 /claim-synthesis (conversion ceiling confirmed intrinsic). 707a flipped
    weakens->superseded (same DECISIVE test; 707b is the live-substrate scoring head). substrate_queue
    action=none (no enrichment owed by THIS run; the DA-gated-arbitration retest is a different mechanism).
    UPDATE 2026-07-03 (confirmed failure_autopsy_V3-EXQ-709_2026-07-03): the anticipated DA-gated-arbitration
    retest RAN -- V3-EXQ-709 (ARC-108 x ARC-110 LEARNED cross-loop arbitration validation; the different
    mechanism / new EXQ / different claim_ids this note called for) FAIL / non_contributory /
    non_degenerate=False / substrate_not_ready_requeue. The learned coupling ENGAGED (M_cross moved off init
    0.116 so LEARNED != STATIC, limbic routing live 1.414, 6/7 readiness gates) but the deeper
    limbic_loop_can_win sub-gate is UNMET: the limbic loop reached >= motor effective weight on only 1/4
    divergent seeds (M_cross[motor,limbic] peak ~0.03), so C1 could NOT be validly evaluated. non_contributory:
    NEVER a weakens -- ARC-110's existing NARROW 707b weakens is UNCHANGED and its narrowing (loop segregation
    necessary-but-not-sufficient; the conversion route requires learned cross-loop arbitration) STANDS
    unretired. NEW datum: even the learned arbitration hits a loop-effective-weight ceiling beyond 707b's
    static-arithmetic diagnosis. substrate_queue action now AMEND (this run DOES owe a failure_record for the
    loop-effective-weight sub-gate on v4_loop_segregation, unlike the 707b action=none). pending_retest_after_substrate.
    UPDATE 2026-07-04 (confirmed failure_autopsy_V3-EXQ-711_2026-07-04): the ascending-gain repair the 709
    autopsy routed to RAN -- V3-EXQ-711 (ARC-110 x ARC-108 ASCENDING-SPIRAL GAIN validation) FAIL /
    non_degenerate=True. The manifest self-routes ARC-110 "weakens"; the confirmed autopsy REJECTS that as
    non_contributory. The load-bearing limbic_loop_can_win precondition was recorded met (3/3 divergent seeds)
    but by a SATURATED runaway (M_cross range peak 4897.8; w_eff[limbic] 10-2274x w_eff[motor]) -- a
    limbic-loop MONOPOLY, not the fair segregated-loop arbitration the claim posits -- so the loop route was
    exercised under a degenerate regime; committed-class entropy FELL on 2/3 divergent seeds and the
    conversion could NOT be validly measured. non_contributory: NEVER a weakens -- this does NOT add a second
    weakens; ARC-110's existing NARROW 707b single-arena-artefact weakens is UNCHANGED and STANDS unretired,
    and its narrowing (loop segregation necessary-but-not-sufficient; the conversion route requires a WORKING
    cross-loop arbitration) STANDS. NEW datum: the raw-scalar ascending gain has no stable parity regime
    (709 sub-threshold ~0.03 -> never wins; 711 10-2274x -> monopolizes) -- the mechanism is missing a
    gain-controller. Re-derive brake FIRES (2nd ARC-110 substrate_ceiling/non_contributory reading); blind
    same-claim gain-magnitude re-queue REFUSED; routed implement-substrate (AMEND v4_loop_segregation:
    bounded/normalized ascending gain + saturation guard on the win-gate). pending_retest_after_substrate.
    UPDATE 2026-07-05 (confirmed failure_autopsy_V3-EXQ-713_2026-07-05): V3-EXQ-713 (tagged ARC-110) FAIL /
    non_degenerate=True. Manifest self-routes 'weakens'; adjudicated non_contributory. A fair bounded
    non-saturated limbic parity win across the segregated loops did NOT convert -- corroborating loop
    segregation necessary-but-not-sufficient and that the conversion route is a WORKING selection-face demotion
    (MECH-448), not arbitration reweighting. Does NOT add a second weakens; ARC-110's existing NARROW 707b
    single-arena-artefact weakens is UNCHANGED and STANDS. Re-derive brake FIRES (3rd ARC-110 reading);
    same-level arbitration re-queue REFUSED; route = selection-face MECH-448 behavioural retests.
    pending_retest_after_substrate.
    V3-EXQ-707c (2026-07-23, C2-release VALIDATION on a REPAIRED instrument; supersedes 707b;
    confirmed failure_autopsy_V3-EXQ-707c_2026-07-25; diagnostic; PROMOTES NOTHING) -> weakens
    (NARROW). Replaces and supersedes the stale withdrawn 707b-2026-06-29 note. CONTEXT: the
    707b-2026-06-29 weakens was WITHDRAWN 2026-07-20 as a measurement_test_design_defect
    (hold-weighted histogram entropy DV, +97.6% exposure imbalance, sign-inconsistent A1-vs-null
    contrast); 707c recovers the question on a clean instrument (DV accumulated only on
    verified-fresh E3 selections). DECISIVE pre-registered test on a FULLY-LIVE substrate: all
    readiness gates passed (loops carry live cross-loop variance; named-channel routing live;
    graded limbic competition worst-cell 4.28>>0.05; valid same-layer null LIVE; enough divergent
    seeds 4/4). Load-bearing C1 FAILED: A1_LOOPS did not convert strict-above max(A0_SINGLE_ARENA,
    the live null). Per ARC-110's own pre-registered grid -> the F-dominance conversion ceiling is
    INTRINSIC, NOT a single-arena-collapse artefact. WEAKENS the narrow single-arena-artefact
    SUB-hypothesis ONLY; does NOT falsify the architectural assertion (segregated loops are
    biologically real + load-bearing). NARROWED (re-established on a valid instrument): loop
    segregation is necessary-but-not-sufficient; alone (with static arithmetic cross-loop
    arbitration) it does not lift the F-dominance ceiling. The missing dependency is learned
    DA-gated cross-loop arbitration (MECH-448/449/ARC-107) -- but that rescue path is
    SUBSTRATE-EXHAUSTED: 709/711/713 all confirmed substrate_ceiling (degenerate/saturated
    arbitration regime). Do NOT re-queue another same-claim learned-arbitration validation letter.
    ARC-110 stays candidate / substrate_conditional / implementation_phase=v3. Category standard
    (NOT substrate_ceiling for this run). Applied /governance 2026-07-25 (session
    angry-ardinghelli-d99740-gov, user-approved). pending_retest_after_substrate.
    V3-EXQ-707c SECOND RUN (20260722T041239Z, distinct from the 20260723T151429Z run scored above;
    same queue letter, two separate executions) -- confirmed failure_autopsy_batch-687a-707c-840-
    748a-833-842-810b-673-614-798afail_2026-07-30: weakens/standard, EXTENDS rather than contradicts
    the 707c disposition immediately above (loops vary but no conversion-ceiling-intrinsic effect).
    Does not add a second distinct weakens reading or change status/category. Applied /governance
    2026-07-30.
  pending_retest_after_substrate: true   # SET 2026-06-29 (707b NARROW autopsy): re-test A1_LOOPS COUPLED with dopamine-gated cross-loop arbitration plasticity (MECH-448/449/ARC-107) once that arbitration-LEARNING substrate is built -- a redesign testing a DIFFERENT mechanism (new EXQ, different claim_ids), NOT a same-claim re-letter. Not queued now.
  implementation_note: >
    SUBSTRATE BUILT 2026-06-27 via /implement-substrate (full scope; PROMOTES NOTHING --
    stays candidate / substrate_conditional / implementation_phase=v3 until the validation
    experiment V3-EXQ-707 scores). Gate cleared by V3-EXQ-704b FAIL-to-convert (the
    pre-registered positive-evidence-FOR-ARC-110 outcome: representational compression is NOT
    the binding constraint). Built behind no-op-default flag REEConfig.use_loop_segregation
    (config.e3): ree-v3/ree_core/predictors/e3_selector.py _segregated_loop_arbitrate replaces
    the single-arena within-eligible argmin with N>=3 parallel loops (motor=F /
    associative=dACC+lPFC / limbic=OFC+liking+vigour), within-loop competition first +
    Haber ascending-spiral cross-loop arbitration after, per-loop zscore normalisation
    (strips F's magnitude advantage = the conversion mechanism), strictly within the F +
    MECH-449 eligible set (safety inherited). Coupled co-requisites built in the same pass:
    ARC-109 D1/D2 split (_d1_d2_split, use_d1_d2_population_split) + MECH-452 loop-local
    traces (use_loop_local_eligibility_traces) + the S2 in-layer same-layer null
    (_loop_inlayer_null, loop_segregation_noise_on). Byte-identical OFF (verified: 704b
    finer-channel dry-run ran clean through the modified selector). Design-of-record
    docs/architecture/sd_v4_loop_segregation.md; autopsy
    evidence/planning/failure_autopsy_704b-706b-conversion-ceiling_2026-06-27.
    COUPLING BUILT 2026-07-01 (/implement-substrate; PROMOTES NOTHING): the 707b-NARROW missing dependency
    -- learned/dopamine-gated CROSS-LOOP arbitration plasticity -- is now built behind no-op-default flag
    config.e3.use_learned_cross_loop_arbitration. The STATIC arithmetic cross-loop combine (fixed spiral
    gains, which 707b showed inherit F's dominance so the live limbic loop never won) is replaced by a
    LEARNED [3,3] cross-loop matrix W_cross = I + M_cross, updated by the SAME ARC-108 signed-RPE three-factor
    rule (shared delta_t; outer-product Hebbian coact; signed directional per-loop eligibility), coupled with
    the MECH-450 settling step. M_cross[motor,limbic] is the learnable ascending-spiral path by which the
    limbic loop learns to drive the motor commit. Byte-identical OFF + at-init (M_cross==0 -> W_cross==I);
    safety inherited (arbitration stays within the F+MECH-448/449 eligible set). This is the ARC-108 x ARC-110
    intersection (annotated on both; NO new claim per the 2026-06-29 MECH-439 /claim-synthesis DROP-MECH-453).
    Design-of-record docs/architecture/learned_cross_loop_arbitration.md; ree-v3/CLAUDE.md "ARC-108 x ARC-110
    coupling" entry; regression guard ree-v3/tests/contracts/test_learned_cross_loop_arbitration.py. The
    pending_retest_after_substrate re-test (A1_LOOPS + learned cross-loop arbitration STRICT-ABOVE A1_LOOPS +
    static arbitration; new EXQ, different claim_ids) is queued SEPARATELY after this build lands. PROMOTES NOTHING.
    ASCENDING-SPIRAL GAIN BUILT 2026-07-03 (/implement-substrate; PROMOTES NOTHING): the loop-effective-weight
    ceiling BOTH the V3-EXQ-709 AND V3-EXQ-710 confirmed autopsies routed to (the limbic loop reached >= motor
    effective weight on only 1/4 divergent seeds; M_cross[motor,limbic] peak ~0.03) is repaired behind
    no-op-default flag config.e3.use_ascending_spiral_gain + loop_segregation_ascending_spiral_gain (forward) +
    loop_segregation_ascending_plasticity_gain (maturation). Both scale ONLY the ASCENDING (strict-upper-triangle,
    row<col in motor/assoc/limbic order) M_cross entries -- in the forward W_cross (anatomical ascending-projection
    strength, Haber 2000's asymmetric spiral) and in the three-factor update (ascending maturation rate) -- so
    w_eff[limbic]/w_eff[assoc] rise WITHOUT touching w_eff[motor] (its column is diagonal + descending), which
    simultaneously strengthens the ascending coupling AND implicitly de-pins the motor(F) default. Map stays
    LINEAR -> w_eff-collapsibility (CLA-3) + bit-identical-at-init both hold (gain*0==0 at M_cross==0). Byte-identical
    OFF / at gains 1.0 (verified: 8 contracts tests/contracts/test_ascending_spiral_gain.py + V3-EXQ-709 --dry-run
    reproduces the 709 FAIL bit-for-bit). Safety inherited (within the F+MECH-448/449 eligible set). Design-of-record
    docs/architecture/learned_cross_loop_arbitration.md "Addendum: ascending-spiral gain"; ree-v3/CLAUDE.md
    "ARC-110 x ARC-108: ascending-spiral gain" entry. Validation falsifier V3-EXQ-711 (new EXQ; STATIC/OFF vs
    ascending-gain-ON on GAP-A reef-bipartite; non-vacuity self-route substrate_not_ready_requeue) queued after
    this build lands. PROMOTES NOTHING; stays candidate / substrate_conditional / pending_retest_after_substrate.
    BOUNDED PARITY CONTROLLER BUILT 2026-07-04 (/implement-substrate; PROMOTES NOTHING): the raw-scalar
    ascending gain above RAN AWAY in V3-EXQ-711 (confirmed failure_autopsy_V3-EXQ-711_2026-07-04): the 20x-forward
    x 5x-plasticity gains compounded through the positive-feedback plastic M_cross loop -> M_cross range 4897.8,
    w_eff[limbic] 10-2274x w_eff[motor] = a limbic MONOPOLY (not a fair parity win); the 709->711 pattern showed
    the raw scalar has NO stable parity regime (missing a controller). Repaired behind no-op-default flag
    config.e3.use_ascending_parity_controller + loop_segregation_parity_forward_gain (lift) / parity_ceiling_ratio
    (cap) / parity_plasticity_gain (bounded maturation) / m_cross_clamp (anti-windup). e3_selector._parity_forward_gain
    SOLVES a per-step ascending gain in [0, forward_gain] that holds w_eff[limbic] toward but under
    ceiling_ratio*w_eff[motor] (motor column gain-invariant -> the fixed parity reference), + the ascending M_cross
    maturation is clamped to +/- m_cross_clamp -- actuator-saturated setpoint control bounding the
    w_eff[limbic]/w_eff[motor] ratio (a FAIR reorder, never a monopoly). A SATURATION GUARD was added to the
    falsifier's limbic_loop_can_win gate (parity band + w_eff/M_cross ceiling -> blow-up self-routes
    substrate_not_ready_requeue, never a false weakens). Byte-identical OFF / inert (verified: 9 contracts
    tests/contracts/test_ascending_parity_controller.py + the raw test_ascending_spiral_gain.py 8 still pass).
    Design-of-record docs/architecture/learned_cross_loop_arbitration.md "Sub-addendum: bounded parity controller";
    ree-v3/CLAUDE.md "BOUNDED ascending-spiral gain -- target-PARITY controller". Successor falsifier V3-EXQ-713
    (new EXQ; OFF vs bounded-ON; parity-win precondition + C1 committed-class entropy strict-above the un-gained
    baseline) queued after this build lands -- a REDESIGN of a DIFFERENT mechanism (the controller), NOT a raw-gain
    re-letter (the 711 re-derive brake refuses that). PROMOTES NOTHING; ARC-110 stays candidate /
    substrate_conditional / pending_retest_after_substrate.
  # PULL-FORWARD FLAGGED 2026-06-24 (user-adjudicated; failure_autopsy_V3-EXQ-700b). The
  # V3-EXQ-700 fork resolved toward escalation -> the v4_loop_segregation substrate (this
  # claim's build) is now BUILT (2026-06-27). Recorded via the assembly companion fields ONLY;
  # status / epistemic_category / implementation_phase UNCHANGED (NOT a promotion --
  # /implement-substrate registration, PROMOTES NOTHING). See implementation_note + substrate_queue.json sd_id v4_loop_segregation.
  assembly_status: in_progress
  awaiting: learned_cross_loop_arbitration_validation_falsifier   # UPDATED 2026-07-01: the DA-gated cross-loop arbitration LEARNING substrate (the 707b-NARROW missing dependency) is now BUILT (use_learned_cross_loop_arbitration; docs/architecture/learned_cross_loop_arbitration.md). Now awaiting the SEPARATE new-EXQ falsifier (A1_LOOPS + learned cross-loop arbitration STRICT-ABOVE A1_LOOPS + static arbitration; different claim_ids), queued via /queue-experiment after the build lands. Prior awaiting=dopamine_gated_cross_loop_arbitration_plasticity_MECH-448_449_ARC-107 satisfied by the build.
  validation_note: >
    V3-EXQ-707 (first ARC-110 validation, 2026-06-28) returned non_contributory /
    substrate_not_ready_requeue (PROMOTES NOTHING; in_layer null verify-lift met on only
    1/2 divergent seeds). A code autopsy of WHY ARM_DROP_LIMBIC was BYTE-IDENTICAL to
    A1_LOOPS on all 6 seeds found the segregated loops NEVER RECEIVED the named cortical
    channels they are supposed to partition -- so neither C1 (loop conversion) nor C2
    (limbic load-bearing) actually exercised the intended motor/associative/limbic
    decomposition. Three stacked defects:
    (1) DEFECT FIXED 2026-06-28 -- ree-v3/ree_core/agent.py built its finer per-head
    channel dict gated on the TOP-LEVEL config.use_finer_channel_gating (never set in
    ree_core -> always False) while the e3.select() consumer reads
    config.e3.use_finer_channel_gating; net: the MECH-451 named decomposition
    (ofc/dacc/lpfc/vigour/liking/gated_policy) was ALWAYS None reaching the selector, so
    only the lumped residual/mech341/route reached the loops -- all mapped to the default
    (associative) loop, leaving the limbic loop EMPTY in every arm (its nonzero pref_range
    was a settling-on-zeros artefact). The DROP-LIMBIC ablation (which only remaps
    ofc/liking/vigour) was therefore a no-op. Fixed to read config.e3.* (regression test
    ree-v3/tests/test_arc110_loop_segregation.py).
    (2) The 707 driver did not enable the limbic-loop input modules (use_ofc_analog /
    use_mech295_liking_bridge / use_tonic_vigor), so OFC/liking/vigour produced no bias at all.
    (3) DEEPER SUBSTRATE BLOCKER (the C2 release blocker) -- even with Defect 1 fixed AND the
    modules enabled, the named cortical bias heads emit per-candidate-FLAT output: OFC head
    INPUT carries real per-candidate range (mean 0.028) but OUTPUT range is EXACTLY 0.0
    (dacc/lpfc/vigour likewise 0.0; only gated_policy ~7e-4). Under per-loop zscore a flat
    channel is inert, so the limbic loop carries no per-candidate competition and ablating it
    is a no-op REGARDLESS of module enablement (verified: modules-on DROP still == A1
    byte-identical). This is the MECH-191 phasic-externalisation gap: the only channels that
    carry per-candidate range and actually drive the A1>A0 loop dynamics are the lumped
    residual/mech341/route (the GAP-A project_channel_range-routed path), all in the
    associative loop. CONSEQUENCE: 707's apparent C1 dynamics did NOT exercise the cortical
    loop decomposition; the validation must be RE-RUN after the release condition below.
    C2 is documented ABLATION-INERT / SUBSTRATE-BLOCKED (NOT a silent fail; load_bearing
    stays false so it never gated the ARC-110 verdict).
    RELEASE CONDITION (C2 testable): feed per-NAMED-channel range-preserving routed
    representations (the project_channel_range / GAP-A path that already keeps `route`
    phasic) into the segregated loops so the limbic channels carry per-candidate signal --
    a substrate build, not a config flip. Then re-run a 707 successor with the limbic
    modules enabled and assert per-tick committed index differs DROP-on vs DROP-off.
    C2 RELEASE LANDED 2026-06-28 (per-named-channel routing): the release condition is BUILT
    via /implement-substrate behind a no-op-default E3Config flag use_named_channel_routing
    (byte-identical OFF). When on (with loop segregation + finer gating) each named channel's
    loop-arbitration term is sourced from its per-candidate REPRESENTATION (ofc/lpfc ->
    world-summaries; liking -> goal-proximity; vigour -> first-action one-hots; dacc ->
    payoff/effort; gated_policy -> summaries) routed through the parameter-free, range-preserving
    project_channel_range (the SAME GAP-A path that keeps `route` phasic) INSTEAD of its flat
    bias-head scalar -- substituted into the segregated-loop accumulation ONLY (the _lcg_terms
    eligibility traces / authority recompose / F-score commit path are UNCHANGED). So the limbic
    loop (ofc/liking/vigour) now carries REAL per-candidate range and ARM_DROP_LIMBIC can differ
    from A1 -- C2 is testable. Regression guard ree-v3/tests/test_arc110_loop_segregation.py
    (TestNamedChannelRoutingC2Release proves limbic range > 0 + DROP != A1 once routed terms
    carry range; TestRoutedRepsReachSelectorThroughAgent guards the capture+route plumbing).
    Validation re-queued as V3-EXQ-707b (v3_exq_707b_arc110_loop_segregation_c2_release,
    supersedes 707a) with a per-NAMED-channel non-degeneracy precondition
    (named_channel_routing_live) evaluated BEFORE C2 is scored so the vacuous DROP==A1
    self-routes substrate_not_ready_requeue, never a false weakens. PROMOTES NOTHING.
  psychiatric_failure_mode: >
    The segregated cortico-striatal loops are the substrate of the loop-specific CSTC
    (cortico-striato-thalamo-cortical) disorder axis: an OCD-like pathology localised to
    the associative/limbic loop (intrusive over-valuation without motor capture), a
    motor-loop-specific Parkinsonian akinesia, and the dissociation between motivational
    drive (limbic loop) and motor execution (motor loop) seen in avolition vs
    bradykinesia. A single collapsed arena has no per-loop degree of freedom to lesion,
    so loop-localised psychopathology cannot be modelled (ARC-106 EARNS failure) -- the
    same EARNS argument ARC-109's D1/D2 split makes for the dopamine-gain axis.
  depends_on:
    - ARC-107    # the BG-constitution umbrella; segregated loops are the loop-STRUCTURE the single-selector constitution currently collapses
    - ARC-106    # brain-like construction: Alexander/DeLong/Strick parallel-loop organisation grounded at FUNCTION; EARNS the loop-specific CSTC disease axis
    - MECH-439   # the F-dominance conversion ceiling this claim hypothesises is partly an ARTEFACT of the single-arena collapse (the s.D hypothesis)
    - MECH-062   # the existing 'coordinated tri-loop gating' claim, status-corrected 2026-06-22: tri-loop is ASSERTED but COLLAPSED in v3; ARC-110 owns the missing SELECTION-STRUCTURE segregation
    - ARC-021    # 'three BG-like cortico-striatal loops require distinct LEARNING channels' -- the plasticity half of segregation
    - MECH-069   # the three loops operate on INCOMMENSURABLE error signals (sensory / motor-sensory / harm+goal) -- the in-principle justification for not merging them
    - Q-016      # open question: what arbitration policy resolves tri-loop conflicts without coupling collapse (MECH-140/141 are its candidate legs)
    - ARC-108    # COUPLED: Haber's ascending striato-nigro-striatal DOPAMINE SPIRAL is the medium that integrates the segregated loops (limbic->associative->motor); ARC-108 supplies the spiral, ARC-110 the loops it couples -- co-requisite, not independent
  what_would_answer: >
    SUBSTRATE-CONDITIONAL / NOT YET BUILT; registered as an EXPLICIT TESTABLE FORK on the live ARC-108 build,
    operationalised as V3-EXQ-700 (the ARC-108 sec-7 learned-gating 2x2 on the collapsed GAP-A arena). The s.D
    (assembly-map) falsifier: with learned dopamine gating (ARC-108) + the recurrent settling step (MECH-450) now
    BUILT, measure F's share of committed-selection variance on the EXISTING single collapsed arena (MECH-439 /
    the V3-EXQ-571 ~88-89% baseline). REFUTED for the V3 tract (segregated loops NOT required) if learned gating +
    settling on the collapsed arena drives F-dominance below the conversion-ceiling threshold AND >=1 non-motor
    channel converts to committed action -- the collapse was NOT the cause, the missing LEARNING was. SUPPORTED /
    PULLED FORWARD if F-dominance PERSISTS after ARC-108 + MECH-450 (V3-EXQ-700 shows preconditions-met-no-lift):
    the single arena is implicated, and segregating the loops (F bounded to the motor loop, the limbic loop
    arbitrated separately) is the next attack, with a per-loop ablation required to show loop-localised conversion
    the collapsed arena cannot produce. NON-DEGENERACY: the per-loop channels must carry live cross-loop variance
    (a 'segregated' loop pinned to the motor loop's winner is a vacuous split).
  location: evidence/planning/basal_ganglia_assembly_map_2026-06-22.md
  notes: >
    Registered 2026-06-22 as the FOURTH BG missing-piece claim (assembly-map A.2 'Parallel segregated loops
    (motor/assoc/limbic) -- MISSING (collapsed)' + s.D 'Open question -- loop segregation'), alongside ARC-108 /
    ARC-109 / MECH-450. Until now loop segregation was a buried v4 bullet (ARC-107 s6b 'V3 collapses all loops into
    one shared commitment interface') with NO owning claim; this makes the sequencing question an explicit, testable
    fork wired onto the live ARC-108 build. Its falsifier is ALREADY operationalised as V3-EXQ-700 (queued
    2026-06-22) -- 700's result decides pull-forward; do NOT chip a duplicate experiment. Distinct from ARC-021
    (distinct LEARNING channels -- the plasticity half) and from MECH-062 (asserted-but-collapsed tri-loop gating,
    status-corrected this pass): ARC-110 owns the SELECTION-STRUCTURE segregation and the s.D artefact-of-collapse
    hypothesis. COUPLED to ARC-108 via Haber's dopamine spiral -- the loops are integrated BY dopamine, so building
    the spiral on a collapsed arena under-specifies the very structure that makes F-dominance avoidable; hence wired
    into ARC-108.depends_on as a co-requisite, not a downstream nice-to-have. Stays candidate / substrate_conditional
    / v4 (promote/demote suppressed; right response is to let V3-EXQ-700 decide pull-forward, NOT more experiments
    on the collapsed arena). ARC-106 cargo-cult guard: segregated loops need a function + divergence-ledger row +
    per-loop ablation falsifier before they count as load-bearing. PROMOTES NOTHING.
    PULL-FORWARD FLAGGED 2026-06-24 (user-adjudicated; failure_autopsy_V3-EXQ-700b_2026-06-24): the
    V3-EXQ-700 fork (700/700a/700b) resolved toward ESCALATION. The single foraging arena structurally
    cannot host a valid committed-class null (the policy-temperature / NOISE_FLOOR_ALPHA null is decoupled
    from the committed-class DV by the eligibility constitution -- noise_verified_lifting 1/3 -> 0/3 -> 0/3
    across the lineage as alpha rose 1.0 -> 2.0) NOR loop-segregated committed diversity, while the MECH-450
    settling signal STRENGTHENED (700 seed-42-only -> 700b majority-of-divergent C1 pass). Re-derive brake
    fired (MECH-439 5th / ARC-108 3rd / MECH-450 2nd non_contributory). The v4_loop_segregation substrate
    (this claim's BUILD) is now registered via /implement-substrate: design_doc
    docs/architecture/sd_v4_loop_segregation.md + substrate_queue.json sd_id v4_loop_segregation (priority 1,
    ready=false / V4-sequenced behind V3 closeout, unblocks MECH-439/ARC-108/MECH-450/ARC-110, with the
    700-lineage metric_trajectory). The cheap V3 rung MECH-451 (finer-channel falsifier) should be exhausted
    FIRST -- it can pre-empt the loop build. Concurrent with the brake-exempt V3-EXQ-700c null-redesign (a
    SEPARATE /queue-experiment track that prototypes the same-layer null on the single arena). Recorded via
    assembly_status:queued + awaiting:v4_loop_segregation (companion fields). status / epistemic_category /
    implementation_phase UNCHANGED. PROMOTES NOTHING.
    REAPPOINTED V4->V3 2026-06-24 (user-directed, closure-map reappointment session): implementation_phase /
    version_relevance flipped v4->v3 for ARC-110 + the coupled cluster (ARC-109 D1/D2, MECH-452 loop-local
    traces; ARC-111 was already v3). Rationale: the loop-segregation build attacks MECH-439, the standing V3
    conversion-ceiling closure blocker, so it is recouped onto the V3 critical path rather than parked V4. NOT a
    promotion -- stays candidate / substrate_conditional (the substrate is still NOT built; the loops must exist
    before ARC-110 can be tested). GATED: V3-EXQ-704 (MECH-451 finer-channel-granularity falsifier) is kept as
    the pre-emption gate -- if 704 converts non-motor influence to committed action on the single arena, the
    ceiling was representational compression and the loop build is PRE-EMPTED (do NOT build); the V3 loop build
    proceeds only if 704 fails to convert. The substrate_queue v4_loop_segregation entry + the arch design-doc
    sd_v4_loop_segregation.md retain their filenames/ids for cross-ref stability but are V3-generation gated on
    704. Build is chipped for a dedicated /implement-substrate session. PROMOTES NOTHING.
- id: ARC-111
  title: "Context-conditioned weights PROPER -- learned connection weights as a function of context, distinct from context-conditioned GAIN: REE today has the GAIN form of context-conditioning (ARC-044's unifier -- a context-specific multiplicative scalar: MECH-152 terrain_weight scaling E3 harm/goal scoring, ARC-016 precision, MECH-261 mode-conditioned write-CHANNEL gating) which modulates a FIXED transfer function. It does NOT have the WEIGHT form: a substrate where the learned connection weights THEMSELVES are a function of context (a different effective weight set selected/generated per context -- the transfer function changes, not a gain on it). The valuation/gating parameters are static nn.Modules; context enters only through the INPUT (z_world) and through output GAIN, never through the weights. HYPOTHESIS: genuinely context-dependent BEHAVIOUR (not just context-scaled magnitude) requires context to reach the weights -- the learned per-channel selection weight w_chan conditioned on z_world that ARC-108 introduces is the first instance of this WEIGHT form at the gating layer."
  claim_type: architectural_commitment
  subject: architecture.context_conditioned_weights_proper
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v3   # the first WEIGHT-form instance (ARC-108 w_chan conditioned on z_world) is in v3 scope; the general context->weights capability beyond the gating layer is v4
  version_relevance: v3
  registered_utc: "2026-06-22"
  psychiatric_failure_mode: >
    A system that can only context-SCALE a fixed transfer function (gain) but cannot
    context-SELECT among learned weight sets is locked into one behavioural repertoire
    whose magnitude varies -- it cannot show genuine context-dependent set-shifting. The
    failure to bring context to the weights is the substrate of cognitive-inflexibility /
    perseveration phenotypes (dorsolateral-prefrontal / associative-loop dysfunction:
    failure to reconfigure the policy when the rule changes, as in set-shifting deficits)
    -- distinct from the GAIN-axis failures (aberrant salience, precision dysregulation)
    ARC-044 / ARC-016 already carry. EARNS the set-shifting axis only once context reaches
    the weights.
  depends_on:
    - ARC-108    # the WEIGHT half: the first concrete instance -- learned per-channel selection weight w_chan as a function of z_world context, trained by signed RPE
    - ARC-044    # the GAIN half (explicitly NOT this): context-dependent neuromodulatory GAIN, a multiplicative scalar on a fixed transfer function; ARC-111 is the weight-form sibling, kept distinct so the gain-vs-weight line stays visible (same category-substitution risk as divergence B5)
    - MECH-152   # the BUILT worked example of the GAIN form (terrain_weight multiplicatively scales E3 scoring) -- the thing ARC-111 must NOT be conflated with
    - MECH-261   # mode-conditioned write-CHANNEL gating (context routes WHICH substrate writes) -- the closest existing context-routing, still gating not weight-generation
    - ARC-106    # brain-like construction: context-to-weights is the cortico-striatal three-factor-learned weighting grounded at FUNCTION; EARNS the set-shifting / cognitive-flexibility axis
  what_would_answer: >
    SUBSTRATE-CONDITIONAL. SUPPORTED if a context variable (z_world / loop / mode) changes the EFFECTIVE connection
    weights -- not merely a multiplicative gain on fixed weights -- such that two contexts produce behaviour
    reproducible by NO single fixed weight set under context-varying gain (the discriminator vs ARC-044: a gain
    model can rescale a fixed policy but cannot REORDER it; a weight model can). Concretely on the ARC-108 instance:
    the learned w_chan must take DIFFERENT learned values in different z_world contexts and produce a
    context-dependent REORDERING of committed channels, not just a rescaling, on >=2/3 seeds. WEAKENED if every
    observed context effect is reproducible by context-conditioned gain on a fixed weight set (then the GAIN form
    ARC-044 suffices and this claim is over-specification). NON-DEGENERACY: per-context weights must carry live
    cross-context variance (a w_chan identical across contexts is the gain model in disguise).
  location: evidence/planning/basal_ganglia_assembly_map_2026-06-22.md
  notes: >
    Registered 2026-06-22 from the session thread distinguishing context-conditioned GAIN (built + unified under
    ARC-044) from context-conditioned WEIGHTS PROPER (learned connection weights as a function of context). User
    flagged 'we may also need context conditioned weights, but we have much of the machinery for this' -- the
    machinery is the GAIN form (ARC-044 / MECH-152 / ARC-016 / MECH-261); the frontier is the WEIGHT form, whose
    first instance falls out of the ARC-108 dopamine build (learned w_chan conditioned on z_world) at NO new
    substrate cost. Registered as a DISTINCT claim rather than folded into ARC-044 precisely to keep the
    gain-vs-weight distinction visible -- folding it under the gain unifier would silently downgrade the requirement
    from learned weights to gain, the same category-substitution shape as divergence B5 (RPE-as-unsigned-variance).
    Wired to ARC-108 (weight half) + ARC-044 (gain half, explicitly sibling-not-parent). Stays candidate /
    substrate_conditional / v3 (first instance is the ARC-108 w_chan; promote/demote suppressed -- right response is
    the ARC-108 BUILD + the discriminator-vs-gain falsifier, NOT promotion). PROMOTES NOTHING.
    NOTE 2026-06-24: ARC-111 was ALREADY v3 (its first WEIGHT-form instance is the V3 ARC-108 w_chan) so the
    user-directed ARC-110-cluster V4->V3 reappointment is a no-op for this claim's phase; recorded here only to
    confirm it is the v3 member of the named cluster (ARC-110, ARC-109, MECH-452, ARC-111).
- id: MECH-451
  title: "Intermediate channel-granularity falsifier -- before the full segregated-loop build (ARC-110), expose the compressed E3 score_bias modulatory channel as SEVERAL separately-learnable finer channels (OFC-devaluation / dACC-conflict / lateral-PFC rule-evidence / vigour / liking) to the learned-gating layer (ARC-108 w_chan), so the learner can discover SEPARATELY that different control functions matter in different states. The current single global w_chan learns one weight over a pre-compressed blend; this tests whether the F-dominance conversion ceiling (MECH-439) is REPRESENTATIONAL COMPRESSION (finer channels convert non-motor influence to committed action) rather than a requirement for full per-loop competition (finer channels do NOT convert, and only ARC-110 segregated loops do). The cheap intermediate rung BETWEEN ARC-108's one global weight vector and ARC-110's full segregated loops -- a V3-tractable falsifier that can pre-empt the V4 loop build."
  claim_type: mechanism_hypothesis
  subject: selection.intermediate_channel_granularity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_ceiling
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "failure_autopsy_704b-706b-conversion-ceiling_2026-06-27#V3-EXQ-704b"
      as_of: 2026-06-27
      verdict: non_contributory/substrate_ceiling
  epistemic_category: substrate_ceiling
  pending_retest_after_substrate: true   # SET 2026-06-27 (704b autopsy): retest the finer-channel conversion once ARC-110 segregated loops + an in-layer null exist.
  implementation_phase: v3
  version_relevance: v3
  registered_utc: "2026-06-23"
  implementation_note: >
    SUBSTRATE BUILT 2026-06-24 via /implement-substrate (PROMOTES NOTHING; stays candidate /
    substrate_conditional / v3 until EXP-0391 scores). The single ARC-108 "score_bias"
    learned-gating channel is exploded into separately-learnable finer channels behind the
    no-op-default flag REEConfig.use_finer_channel_gating (config.e3): finer registry
    (ofc/dacc/lpfc/vigour/liking/gated_policy/residual + preserved mech341/route) over a
    PARALLEL w_chan_finer register-buffer in ree-v3/ree_core/predictors/e3_selector.py, trained
    by the SAME ARC-108 signed-RPE three-factor rule (the ARC-108 w_chan path is byte-identical).
    agent.select_action captures the un-summed per-head biases into a score_bias_channels dict
    (OFC<-SD-033b, dACC<-SD-032b, lateral-PFC<-SD-033a, vigour<-MECH-320, liking<-MECH-295,
    gated_policy<-ARC-062); "residual" = score_bias - sum(named) keeps the decomposition
    EXHAUSTIVE so softplus-unity init is bit-identical even when ON. Validation = EXP-0391
    (A0_ENVELOPE_ONLY / A1_GLOBAL_WCHAN / A2_FINER_CHANNELS / ARM_NOISE; settling OFF; PRIMARY DV
    committed-action-class entropy). A2 lift beyond A1 => representational compression, pre-empts
    the V4 ARC-110 build; A2 weights-move-but-no-lift => positive evidence FOR ARC-110. ARC-106
    cargo-cult guard: A1 IS the collapse-to-blend ablation + the non-degeneracy readiness gate
    (dissociable cross-channel w_chan_finer variance + GAP-A divergent pool). Phased training N/A
    (local non-backprop rule, reuses trained valuation heads). MECH-094 waking-only. Design doc
    docs/architecture/mech_451_finer_channel_granularity.md; ree-v3/CLAUDE.md SD-Implemented entry;
    contracts tests/contracts/test_mech451_finer_channel_gating.py (12). Routed by
    failure_autopsy_V3-EXQ-700b_2026-06-24.
  defect_note: >
    SUBSTRATE DEFECT FOUND + FIXED 2026-06-28 (ARC-110 V3-EXQ-707 autopsy) -- the
    EXP-0391 / V3-EXQ-704 / V3-EXQ-704b validations of this claim were CONFOUNDED and did
    NOT exercise the hypothesis. ree-v3/ree_core/agent.py built the per-head finer-channel
    dict (score_bias_channels) gated on the TOP-LEVEL config.use_finer_channel_gating,
    which is never set anywhere in ree_core (always False), while the e3.select() consumer
    reads config.e3.use_finer_channel_gating. Net: score_bias_channels was ALWAYS None
    reaching the selector, so the named control channels (ofc/dacc/lpfc/vigour/liking/
    gated_policy) were NEVER separated -- they were all fused into the single `residual`
    term. The A2_FINER_CHANNELS arm therefore tested only a residual/mech341/route
    3-way split, NOT the OFC/dACC/lPFC/vigour/liking decomposition this claim asserts.
    The 704/704b FAIL-to-convert is consequently NOT evidence about named-channel
    granularity, and the "finer-channel leg" of failure_autopsy_704b-706b-conversion-ceiling
    (the routing to ARC-110) rests on an untested mechanism for THIS claim (the curiosity
    leg, 706/706b, is UNAFFECTED -- curiosity rides the residual lump, which always reached
    the selector). Fixed at agent.py to read config.e3.use_finer_channel_gating; verified
    the named channels now reach the selector as separable _lcg_terms; regression guard
    ree-v3/tests/test_arc110_loop_segregation.py (TestFinerChannelsReachSelector). MECH-451
    is effectively UNTESTED: pending_retest_after_substrate stays true; the retest must (a)
    run post-fix and (b) assert >=1 named channel reaches the selector with non-zero
    per-candidate range BEFORE scoring conversion (note: the named bias heads currently emit
    per-candidate-FLAT output -- see ARC-110 validation_note Defect 3 / MECH-191 -- so a
    genuine MECH-451 retest is itself gated on the channels carrying per-candidate range).
    C2 RELEASE 2026-06-28 (scope note): the ARC-110 per-named-channel routing release
    (use_named_channel_routing) routes the named channels' per-candidate REPRESENTATIONS
    (range-preserving, via project_channel_range) into the SEGREGATED-LOOP arbitration only --
    it does NOT change the single-arena _lcg_terms / w_chan_finer learning path that THIS
    claim's EXP-0391 (single shared arena) retest scores, where the named heads still emit flat
    output. So the release unblocks the ARC-110 loop face (C2), NOT the MECH-451 single-arena
    finer-channel-LEARNING retest; MECH-451 stays pending_retest_after_substrate. A genuine
    single-arena MECH-451 retest still needs either trained non-flat named heads or an analogous
    routing into the single-arena modulatory accumulator (not built here).
  psychiatric_failure_mode: >
    A learner that can only re-weight a PRE-COMPRESSED control blend cannot
    dissociate the control functions that compression fused -- it cannot learn that
    OFC devaluation should matter in one state while dACC conflict should dominate in
    another. This is the substrate of the additive-collapse failure mode (the same
    representational loss ARC-109's D1/D2 split and ARC-110's loop segregation each
    attack from their own angle): an aberrant over-weighting cannot be localised to
    the function that is actually deranged because the functions are not separable in
    the representation. Exposing finer channels is the minimal representational
    degree-of-freedom needed before any function-specific gating pathology can be
    modelled (ARC-106 EARNS, in its weakest V3 form).
  depends_on:
    - ARC-108    # the learned-gating layer (w_chan) this exposes finer channels TO -- finer channels are only testable once there is a learner to weight them separately
    - ARC-110    # the FULL loop decomposition this is the cheap intermediate rung BEFORE; ARC-110's s.D artefact-of-collapse hypothesis is what this falsifier partly pre-empts
    - MECH-439   # the F-dominance conversion ceiling: this tests whether the ceiling is representational compression of the modulatory blend vs a need for per-loop competition
    - MECH-450   # the recurrent settling step the finer channels would settle over -- coupled readout
    - ARC-106    # brain-like construction: finer channel exposure is the FUNCTION-level intermediate between one shared weight and anatomical loops; grounded at function, with the load-bearing-vs-decorative ablation test
  what_would_answer: >
    SUBSTRATE-CONDITIONAL / NOT YET BUILT (the finer-channel exposure is a V3-tractable e3_selector slice, not present
    today). A future experiment SUPPORTS this if exposing the compressed score_bias blend as >=3 separately-learnable
    finer channels (each with its own learned w_chan entry, signed-RPE trained) lifts committed-action-class entropy
    AND converts at least one previously-F-dominated NON-MOTOR control function to committed authority on the EXISTING
    collapsed arena -- a lift the single-global-w_chan version (V3-EXQ-700) does not produce -- on >=2/3 seeds. This
    would localise the conversion ceiling to REPRESENTATIONAL COMPRESSION and pre-empt the V4 ARC-110 loop build.
    WEAKENED / route-to-ARC-110 if finer channels move their weights but produce no committed-conversion lift beyond
    the single-global w_chan (then compression is NOT the binding constraint and full per-loop competition is implicated;
    this is positive evidence FOR ARC-110, not against ARC-108). NON-DEGENERACY: the finer channels must carry live,
    DISSOCIABLE cross-channel learned-weight variance (finer w_chan entries that move identically are the compressed
    blend re-labelled), and the candidate pool must be divergent (GAP-A guard), else substrate_not_ready_requeue.
  location: docs/thoughts/2026-06-22_v3_exq_700_failure_interpretation_and_loop_decomposition.md
  notes: >
    Registered 2026-06-23 from the V3-EXQ-700 failure-interpretation thought intake (the "channel compression" / "finer
    exposed channels" section). The doc proposes an intermediate architecture between ARC-108's one_global_weight_vector
    and ARC-110's full loop-specific_weight_vectors: expose finer channels first, as a CHEAP falsifier of whether the
    conversion ceiling is representational compression before committing to the V4 segregated-loop build. Distinct from
    ARC-110 (full per-loop competition with separate eligibility-and-gates) and from ARC-111 (context-conditioned
    weights): this keeps ONE shared arena but de-compresses the modulatory channel feeding the single learner. Its
    falsifier is a SIBLING of V3-EXQ-700 (same collapsed-arena substrate, finer score_bias decomposition), to be designed
    via /queue-experiment after V3-EXQ-700 returns -- if 700 already converts, this is moot; if 700 shows
    preconditions-met-no-lift, this is the first cheap thing to try before ARC-110. Proposal EXP-0391 minted
    (manual_proposals.v1.json). Stays candidate / substrate_conditional / v3 (promote/demote suppressed; right response is
    the finer-channel e3_selector slice via /implement-substrate, then the sibling falsifier, NOT promotion). ARC-106
    cargo-cult guard: the finer channels need an ablation falsifier (collapse-to-blend reproduces the no-lift => the
    decomposition is load-bearing) before they count. PROMOTES NOTHING (new candidate).
  evidence_quality_note: >
    V3-EXQ-704b (MECH-451 finer-channel-granularity falsifier, validity re-tune superseding
    V3-EXQ-704, 2026-06-26) self-routed substrate_not_ready_requeue / non_contributory
    (non_degenerate=False, scoring-excluded). The 704 precondition fix WORKED:
    fcg_noise_magnitude_matched=1.0 (in band [0.25,4.0]; was 176.9x in 704) and the
    substantive-dissociation guard cleared (finer-channel learned range 0.0087 > 0.005); the finer
    channels dissociated and moved from init (delta_t variance 0.0005, weights moved 0.00135). But
    the readiness leg matched_noise_control_verified_lifting FAILED (0/2 divergent seeds;
    threshold 2): a CORRECTLY magnitude-matched same-layer null does not lift committed-class
    entropy above A0. This ELEVATES the 704 diagnosis from precondition_unmet (a re-tunable scale
    artifact) to substrate_ceiling (the precondition cannot be met IN PRINCIPLE in V3): the V3
    single shared E3 foraging arena structurally decouples committed-class entropy from any
    same-layer perturbation (the same wall the 700b/700c learned-gating-settling autopsies hit),
    so no valid same-layer null can be constructed to make the C1 discrimination bar meaningful.
    MECH-451 UNWEAKENED and UNTESTED in either direction. Re-derive brake FIRED (2nd MECH-451
    non_contributory autopsy; threshold 2): REFUSES a further V3 letter re-running the
    finer-channel-vs-same-layer-null comparison; route to /implement-substrate on
    v4_loop_segregation (ARC-110), which furnishes both a valid same-layer null and per-loop
    competition. pending_retest_after_substrate=true. Convergent with V3-EXQ-706b (MECH-314) onto
    the single-arena-collapse reading. Confirmed
    failure_autopsy_704b-706b-conversion-ceiling_2026-06-27 (prior:
    failure_autopsy_V3-EXQ-704_2026-06-25).

    [enum-comment migrated to field 2026-07-18] epistemic_category: ELEVATED 2026-06-27 from
    substrate_conditional: V3-EXQ-704b proved the conversion test's same-layer null cannot be
    VALIDLY constructed in the single arena (matched-null inert on the DV), not merely mis-tuned
    (the 704 reading). Re-derive brake fired -> v4_loop_segregation (ARC-110). See
    failure_autopsy_704b-706b-conversion-ceiling_2026-06-27.
- id: MECH-452
  title: "Loop-local eligibility traces under a globally-broadcast dopamine signal: even with a SINGLE shared dopaminergic teaching signal (ARC-108 delta_t), the credit traces that decide WHICH synapses/channels the signal updates must remain LOCAL and loop-specific -- effective update = shared delta_t x separate local eligibility trace x loop-specific active synapses -- rather than the current single globally-broadcast credit that updates every recently-active selector component. A too-broad or temporally-smeared eligibility trace would make learned gating appear ineffective (no committed conversion) even when the underlying dopamine learning rule is exactly correct, because the right teaching signal is delivered to the wrong synapses."
  claim_type: mechanism_hypothesis
  subject: selection.loop_local_eligibility_traces
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v3   # REAPPOINTED V4->V3 2026-06-24 (user-directed, coupled to the ARC-110 loop-segregation reappointment): loop-local traces are the credit-assignment piece the segregated loops need, so they follow ARC-110 onto the V3 critical path. BUILT 2026-06-27 with ARC-110. Still substrate_conditional; PROMOTES NOTHING.
  version_relevance: v3
  registered_utc: "2026-06-23"
  implementation_note: >
    SUBSTRATE BUILT 2026-06-27 via /implement-substrate (coupled to ARC-110; PROMOTES NOTHING --
    stays candidate / substrate_conditional / v3 until V3-EXQ-707 scores). Behind no-op-default
    flag config.e3.use_loop_local_eligibility_traces, the ree-v3/ree_core/predictors/e3_selector.py
    eligibility-recording site credits a channel for the shared signed-RPE delta_t ONLY if its
    loop's within-loop winner matched the committed action (the loop "voted for" the realised
    outcome; _loop_of_channel / _loop_voted set by _segregated_loop_arbitrate), so credit stays
    loop-local under one broadcast dopamine signal -- a smeared trace no longer makes learned
    gating look ineffective when the rule is correct. Diagnostic loop_local_credited_channels.
    Requires loop segregation on. See ARC-110 implementation_note.
  psychiatric_failure_mode: >
    The eligibility trace is the substrate of temporal credit assignment, so its
    derangement is the substrate of mis-attributed reinforcement learning: a too-broad
    / smeared trace credits actions and channels that were NOT responsible for the
    outcome -- a learning-level aberrant-salience analog (the wrong selector component
    is potentiated, distinct from the readout-level aberrant salience ARC-016 carries),
    and a candidate substrate for the over-general reinforcement seen in addiction and
    in delusional reinforcement. A too-narrow / over-decayed trace fails to bridge the
    delay between action and outcome -- a temporal-credit / delay-bridging deficit
    (an impulsivity / steep-discounting analog). A single global broadcast trace
    (the current state) has no locality to derange and cannot model either pole.
  depends_on:
    - ARC-108    # the shared signed-RPE delta_t this claim keeps GLOBAL while making the eligibility traces LOCAL -- the trace is the missing locality the single broadcast lacks
    - ARC-110    # the segregated loops the traces localise TO; loop-local traces presuppose loops to be local to (coupled v4 build)
    - MECH-450   # today the W_lat settling update shares ONE delta_t with w_chan via one global credit; this is the claim that those credits must instead ride separate local traces
    - MECH-439   # the conversion ceiling restated at the credit-assignment layer: a correct global signal on a too-broad trace still cannot convert
    - ARC-106    # brain-like construction: loop-local eligibility traces grounded at FUNCTION (three-factor plasticity with local traces), with the load-bearing-vs-decorative ablation test
  what_would_answer: >
    SUBSTRATE-CONDITIONAL / NOT YET BUILT (requires the ARC-110 loops plus per-loop/per-channel trace machinery; neither
    is present in V3). A future experiment SUPPORTS this if replacing the single global credit broadcast with
    loop/channel-LOCAL eligibility traces (shared delta_t x local trace x active synapses) lifts committed conversion or
    correct credit assignment where the global-trace version does not, AND a trace-localisation ablation (collapse the
    local traces back to one global trace) reproduces the no-lift, on >=2/3 seeds -- showing the locality, not the
    teaching signal, was the binding constraint. WEAKENED if loop/channel-local traces yield no advantage over the
    single global broadcast (the trace locality is then decorative -- dropped per the ARC-106 load-bearing-vs-decorative
    ablation test). NON-DEGENERACY: the per-loop/per-channel traces must carry DISSOCIABLE cross-trace variance (not one
    shared trace re-labelled per loop), else substrate_not_ready_requeue.
  location: docs/thoughts/2026-06-22_v3_exq_700_failure_interpretation_and_loop_decomposition.md
  notes: >
    Registered 2026-06-23 from the V3-EXQ-700 failure-interpretation thought intake (the "Local eligibility traces and
    credit assignment" section: "shared dopamine signal x separate local eligibility traces x loop-specific active
    synapses"). Distinct from ARC-063 / the CandidateRule eligibility-trace credit claim (which is RULE-AVAILABILITY
    credit in the policy rule field, a different mechanism at a different layer): MECH-452 is the BG-SELECTION credit
    trace that gates which gating synapses ARC-108's signed RPE updates. Coupled to ARC-110 (the loops the traces
    localise to) and ARC-108/MECH-450 (which today share one global delta_t broadcast). V4-leaning per the assembly-map
    sequencing -- the loop substrate must exist first. ARC-106 cargo-cult guard: local traces need an ablation falsifier
    (collapse-to-global reproduces the no-lift) before they count as load-bearing. The doc's lesson: a preconditions-met
    failure of V3-EXQ-700 with strong w_chan movement but no behavioural conversion is consistent with a correct learning
    rule delivered on a too-broad trace -- so this claim is one of the "not fully explained by loop decomposition"
    residues the doc flags. PROMOTES NOTHING (new candidate).
- id: MECH-453
  title: "Cholinergic TAN-pause plasticity-window gating of selector credit: dopamine should NOT continuously update every recently-active selector component; a striatal cholinergic tonically-active-neuron (TAN) pause should open a NARROW plasticity window around behaviourally-relevant events, so the three-factor learned-gating updates (ARC-108) are admitted only WITHIN the window. Without a window, continuous credit smears across irrelevant recently-active components and degrades the learned gating the conversion attack depends on. The OPENING-side neuromodulatory complement to REE's existing CLOSURE-side plasticity control (MECH-094 waking-gate; INV-074/MECH-333/MECH-334 consolidation/EWC) -- REE today has machinery to stop/consolidate learning but not a phasic gate to OPEN a precise learning window."
  claim_type: mechanism_hypothesis
  subject: selection.tan_plasticity_window_credit_gating
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4
  registered_utc: "2026-06-23"
  psychiatric_failure_mode: >
    The TAN plasticity window is the substrate whose derangement is the
    indiscriminate-vs-rigid learning axis: a too-WIDE / absent window admits credit to
    every recently-active component -> over-general, indiscriminate reinforcement and
    habit over-formation (a compulsivity / habit-rigidity analog; striatal cholinergic
    interneuron dysfunction is implicated in Tourette/tic and dystonia phenomenology
    and in the loss of behavioural flexibility), while an over-NARROW / failed-pause
    window starves learning of the window it needs -> failure to update selection at
    all (a learning-arrest analog). A continuously-updating learner (the current state)
    has no window to derange and cannot model either pole. Distinct from the
    cholinergic AXIS-ORTHOGONALITY maintenance role (MECH-142) -- that is ACh
    protecting representational geometry; this is ACh gating the TIMING of credit.
  depends_on:
    - ARC-108    # the three-factor learned-gating update whose TIMING this windows -- without the learner there is no credit to window
    - MECH-452   # the loop-local eligibility traces this bounds in TIME -- the trace says WHICH synapses, the window says WHEN credit is admitted (tightly coupled; build together)
    - MECH-094   # the existing waking-only plasticity gate this REFINES from a coarse wake/sleep switch to a phasic event-locked window
    - MECH-142   # the existing cholinergic axis-orthogonality-maintenance role -- ACh as a neuromodulator is present; this is a DISTINCT ACh function (credit-timing, not geometry-protection), kept separate to avoid conflation
    - ARC-106    # brain-like construction: the TAN pause grounded at FUNCTION (an event-locked plasticity window), with the load-bearing-vs-decorative ablation test
  what_would_answer: >
    SUBSTRATE-CONDITIONAL / NOT YET BUILT (no event-locked plasticity-window gate exists on the selector; the learned
    gating updates continuously when waking). A future experiment SUPPORTS this if gating the ARC-108 three-factor
    update behind a narrow event-locked (TAN-pause-like) window lifts committed conversion or credit precision over the
    continuously-updating learner, AND a window-ablation (open the window permanently) reproduces the
    smeared-credit no-lift, on >=2/3 seeds. WEAKENED if windowing the update yields no advantage over continuous
    updating (the window is then decorative -- dropped per the ARC-106 load-bearing-vs-decorative ablation test).
    NON-DEGENERACY: the window must actually GATE updates (a measurable fraction of recently-active components excluded
    when the window is closed, with live cross-event variance in what is admitted) -- a window that admits everything is
    the continuous learner re-labelled, route substrate_not_ready_requeue.
  location: docs/thoughts/2026-06-22_v3_exq_700_failure_interpretation_and_loop_decomposition.md
  notes: >
    Registered 2026-06-23 from the V3-EXQ-700 failure-interpretation thought intake (the "Plasticity-window gating"
    section: "a cholinergic TAN-pause-like mechanism is absent ... a narrower plasticity window may be needed to prevent
    credit smearing"). Connects to the standing plasticity-window-neuromodulators thread (memory note
    project_plasticity_window_neuromodulators, 2026-06-01: REE has the CLOSURE side -- INV-074/MECH-333/MECH-334 + EWC --
    but lacks the OPENING side, basal-forebrain/striatal cholinergic gating of learning rates). This claim scopes that
    thread to the SELECTOR credit layer specifically: TAN gating of the ARC-108 learned-gating update. Coupled to
    MECH-452 (the trace says which synapses; the window says when) -- build together. Kept DISTINCT from MECH-142
    (ACh-as-geometry-protection) to avoid conflating two ACh functions. V4-leaning (depends on ARC-108 being mature and
    on a plasticity-window substrate that does not exist). ARC-106 cargo-cult guard: the window needs an ablation
    falsifier (open-permanently reproduces the smear) before it counts as load-bearing. One of the doc's "not fully
    explained by loop decomposition" residues. PROMOTES NOTHING (new candidate).

- id: MECH-454
  title: "Foreclosing the SELF's own future reachable-option space carries a decision cost in E3 trajectory scoring, GATED BY FORECAST UNCERTAINTY -- the non-harm, self-directed analog of INV-026 (which scopes foreclosure-of-reachable-state-space as a harm to OTHERS). E3 today scores harm + benefit + goal + residue + the modulatory stack; nothing represents the option-value / reversibility of an action for the agent's OWN reachable futures. REE's irreversibility-awareness is HARM-scoped (residue cannot be erased; No-Go gates; INV-026 foreclosure-of-others); there is no INFORMATION/RESOURCE-scoped analog (don't cheaply foreclose your own options). The claim: an uncertainty-gated reachable-option-preservation cost should change committed action -- preferring the reversible/option-preserving branch MORE when the forecast that justifies foreclosure is UNCERTAIN, and not at all when it is confident (a generalization of DR-12's PE-conditioned E3 confidence from object-rollouts to a foreclosure cost)."
  claim_type: mechanism_hypothesis
  subject: ethics_engine_3.self_option_value_reversibility_cost
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:MECH-454@2026-06-24T21:53:17.293018Z"
      as_of: 2026-06-24
      verdict: hold_pending_v3_substrate/applied
  epistemic_category: substrate_conditional
  implementation_phase: v3
  version_relevance: v3
  registered_utc: "2026-06-24"
  psychiatric_failure_mode: >
    The uncertainty-gated option-value cost is the substrate whose derangement is the
    foreclosure-vs-indecision axis. ABSENT / too-weak -> premature irreversible commitment,
    impulsive foreclosure of one's own reachable futures on a low-confidence forecast (an
    impulsivity / failure-to-defer pole). Too-STRONG / un-gated -> pathological option-
    preservation: refusing any irreversible-but-necessary action, "keeping every door open",
    chronic indecision and non-commitment (an obsessive-indecisiveness / pathological-doubt
    pole, adjacent to OCD incompleteness/over-caution and to avolitional non-commitment).
    DERANGED GATING (cost present but NOT scaled by forecast uncertainty) -> context-insensitive
    commitment: the agent forecloses or preserves the SAME regardless of how uncertain its
    forecast is -- the specific failure this claim was surfaced by (collapsing an unknown future
    distribution into a confident point and acting irreversibly on it). Distinct from MECH-279
    freeze (threat-driven motor arrest) -- this is a valuation cost on foreclosure, fires with
    z_harm BELOW threshold.
  depends_on:
    - MECH-439   # F-dominance commitment-collapse IS option-foreclosure at the commitment gate (diverse pool annihilated to the F-winner); shared diversity/commitment machinery
    - INV-026    # the harm-scoped foreclosure-of-reachable-state-space (to OTHERS) that this mirrors as a NON-harm, SELF-directed decision cost
    - MECH-292   # ghost-goal bank -- the existing preserve-a-recoverable-trace reflex (don't erase an inactive goal) generalized from the goal domain to a general option-preservation criterion
    - SD-039     # dual-trace anchor preservation (Bouton) -- inactive traces kept recoverable; the substrate-level expression of "don't foreclose"
    - ARC-106    # brain-like construction: grounded at FUNCTION (option-value / reachable-set preservation) with the load-bearing-vs-decorative ablation test
  related_claims:
    - DR-12      # DESIGN-RECORD cross-ref, NOT a claim/build dependency (moved out of depends_on 2026-07-01 PM /governance dangling-dep hygiene fix). DR-12 is a landed design record (E2-forward-PE -> E3 confidence, object-rollout trust), not a claims.yaml claim id, so it dangled in the phase-consistency prerequisite scan. MECH-454's relationship to it is "a generalization of DR-12's PE-conditioned E3 confidence from object-rollout trust to a forecast-uncertainty-scaled foreclosure cost" -- conceptual prior-art it GENERALISES, not a substrate it is blocked on (its real blockers, per what_would_answer, are the reachable-option-dissociating env extension + the E3 option-value cost term, neither of which is DR-12). The mechanism DR-12 embodies is owned by MECH-215, which is itself implementation_phase v4, so promoting DR-12 to a MECH-215 depends_on edge would recreate a v3->v4 ROOT leak; the conceptual link belongs here. PROMOTES NOTHING.
  what_would_answer: >
    SUBSTRATE-CONDITIONAL / NOT YET BUILT. Blocked on two substrates that do not exist: (1) a
    reachable-option-DISSOCIATING env extension on CausalGridWorld -- a cheap-REVERSIBLE branch
    vs a costly-IRREVERSIBLE branch (a one-way door / a region-collapsing consumable) so that an
    action measurably changes the agent's reachable-future-option count; (2) the E3 option-value
    cost term itself (a penalty on the post-action drop in reachable distinct future states/goals,
    scaled by forecast PE). A future ablation SUPPORTS this if, with the term ON vs OFF under
    MANIPULATED outcome uncertainty, the ON arm prefers the option-preserving action MORE than OFF
    specifically when forecast uncertainty is HIGH and converges to OFF when it is LOW (uncertainty-
    GATED), on a strict majority of seeds. WEAKENED if ON==OFF (the term never moves committed
    action -> decorative, dropped per the ARC-106 ablation test) OR ON avoids the irreversible branch
    REGARDLESS of uncertainty (un-gated risk-aversion, not the claimed mechanism). NON-DEGENERACY
    (else substrate_not_ready_requeue, NOT a verdict): the two branches must measurably differ in
    reachable-future-option count (post-action reachable distinct states/goals spread > floor) AND
    forecast uncertainty (E2 forward-PE or E3 running_variance) must VARY across the manipulated
    conditions (range > floor) -- if reachable-option count is identical across branches or PE is
    pinned, the term has nothing to act on and the test is vacuous.
  location: >
    Interactive thought-digestion 2026-06-24 (no docs/thoughts file; origin = a meta-discussion of a
    temporal-reasoning failure: collapsing an unknown future distribution into a confident point and
    prematurely foreclosing a cheap reusable option). See
    memory/feedback_mint_arms_as_you_go_terminality_unknowable.md and the self_model_v4 plan
    own-future-option node for the V4 meta-level leg.
  notes: >
    Registered 2026-06-24 via /thought-digestion (user-directed disposition "register, substrate-blocked").
    The genuinely-new ground, confirmed against the registry: REE's foreclosure/irreversibility concept exists
    ONLY as (a) an ETHICAL harm -- foreclosing ANOTHER self-like agent's reachable state-space (INV-026 via
    INV-028) -- and (b) commit-boundary WRITE irreversibility (MECH-094-adjacent). The SELF-directed, NON-harm,
    decision-economic criterion (foreclosing one's OWN reachable options has a cost in E3) is absent.
    COUNTERFACTUAL IS NECESSARY-BUT-NOT-SUFFICIENT: REE's counterfactuals (SD-031/ARC-033/MECH-256/DR-12) are
    do(my-action)->consequence rollouts; this needs the do(world-reveals-branch-B)->was-my-commitment-robust
    form plus a robustness-across-branches criterion. F-DOMINANCE CROSS-LINK (MECH-439): collapsing a diverse
    candidate distribution to a confident commitment IS option-foreclosure at the commitment gate -- the
    diversity-preservation work and this cost are two faces of one thing. LIT ANCHORS (lit-pull queued
    2026-06-24): real-options / value-of-information; empowerment / future-option-maximization (Klyubin & Salge)
    as an intrinsic objective; exploration under NON-STATIONARITY (you cannot know if the regime is terminal);
    cross-ref Niv opportunity-cost (ARC-066/MECH-320). DISTINCT FROM: MECH-314 (curiosity = an EXPLORATION/
    novelty bonus, not a foreclosure COST), ARC-068 (opportunity cost of TIME, not of option-SPACE), MECH-320
    (tonic vigor). The V4 meta-level leg (reasoning about uncertainty over REE's OWN future option-space) was
    folded into the self_model_v4 plan (user decision), NOT a standalone claim. PROMOTES NOTHING (new candidate).

- id: Q-080
  title: "Should EFFORT / energy expenditure COUPLE to REE's harm / residue / allostatic stream (over-exertion -> depletion -> a harm-adjacent state, treated with harm's irreversibility-aware special status), with energy-conservation weighted as an evolutionarily-PRIMARY drive (a least-effort prior) -- or does the existing VALUE-SUBTRACTION (dACC EVC effort_cost, MECH-320/ARC-068 opportunity-cost-of-time) + pACC allostatic load already suffice WITHOUT a harm-coupling? I.e. is wasted/excessive effort a 'harm' in REE, or only a foregone value?"
  claim_type: open_question
  subject: ethics_engine_3.effort_as_harm_energy_conservation
  polarity: open
  status: open
  live_status:
    reading: open
    as_of: 2026-07-11
    needs_review: false
    evidence:
      from: "decision:Q-080@2026-07-10T20:15:33.824360Z"
      as_of: 2026-07-10
      verdict: narrow_open_question/applied
  registered_utc: "2026-06-24"
  version_relevance: v3
  depends_on:
    - SD-032b    # dACC EVC: mode_ev = payoff - control_required*effort_cost -- effort enters VALUE here, not harm; the baseline this asks whether to extend
    - SD-032e    # pACC allostatic load (sustained z_harm_a -> drive bias); the existing depletion-integrator -- but it integrates THREAT, not EFFORT
    - MECH-320   # tonic vigor / Niv opportunity-cost-of-time -- effort/vigor as an opportunity cost, value-side
    - ARC-068    # opportunity cost of TIME (distinct axis: time, not metabolic energy)
    - MECH-454   # the self-option-value cost this GROUNDS in energetics: re-acquiring a foreclosed option costs ENERGY -- option-value is a special case of energy-economy
    - SD-012     # homeostatic drive / energy state (obs_body energy, drive_level) -- the energy currency
    - MECH-219   # the suffering / harm hysteretic integrator that an effort-harm-coupling would feed
  what_would_answer: >
    NARROWED 2026-07-09 (/claim-synthesis) into three orthogonal, independently-testable sub-questions --
    Q-080.a harm-coupling, Q-080.b least-effort prior, Q-080.c MECH-454 energy-grounding -- enumerated with
    their discriminating tests, version scopes, and lit anchors under status_note. The overall discriminating
    frame is unchanged:
    DISCRIMINATING TEST (or derivation). Build an env that DISSOCIATES effort from benefit: a LOW-effort and a
    HIGH-effort path to the SAME benefit, with a measurable per-action energy/effort cost, plus a chronic-exertion
    regime (sustained high effort that could deplete). ANSWERED 'effort SHOULD be harm-coupled / energy-conservation
    primary' if routing effort into the harm/allostatic stream (effort -> depletion -> a harm-adjacent cost with
    harm's irreversibility-aware handling) produces behaviour the VALUE-SUBTRACTION baseline does NOT: (a) a
    least-effort PRIOR (prefers the low-effort path when benefit is indifferent, by default), (b) protective
    DISENGAGEMENT under chronic over-exertion (fatigue/allostatic withdrawal before depletion), and (c) irreversibility-
    aware caution (avoids over-exerting into a depletion it cannot cheaply reverse). ANSWERED 'existing machinery
    suffices, close' if the dACC EVC value-subtraction + pACC allostatic load ALREADY reproduce (a)/(b)/(c) and the
    harm-coupling adds nothing measurable (then effort is a foregone value, not a harm -- do NOT mint a mechanism).
    NON-DEGENERACY (else the question is untestable): the env must offer a genuine low-vs-high-effort dissociation to
    the SAME benefit AND a chronic-exertion regime, with the effort/energy cost measurable and varying -- if the two
    paths cost the same energy, or depletion never accrues, neither pole can be distinguished.
  status_note: >
    [2026-07-09 /claim-synthesis: narrow_open_question applied]. User decision 2026-07-09 "narrow now",
    inheriting the 2026-06-24 disposition "pose the question first, do NOT mint a mechanism on a hunch."
    Q-080 conflates THREE separable, independently-testable propositions; each is registered here as
    embedded testable scaffolding (the Q-040 precedent). The question STAYS open; NO mechanism is minted;
    no status change. Lit grounding (proposal-grade, consensus 2026-07-09; full /lit-pull deferred to
    IF a sub-question resolves toward a mechanism) WARRANTS each sub-question WITHOUT pre-deciding it --
    the intended state for a pose-first narrowing. Proposal + lit synthesis:
    evidence/planning/claim_synthesis_Q-080_2026-07-09.md.

    NOTE ON NOTATION: the (a)/(b)/(c) inside what_would_answer are the three BEHAVIOURAL SIGNATURES a
    harm-coupling would have to add (least-effort default / protective disengagement / irreversibility-aware
    caution). The Q-080.a/b/c below are the three ORTHOGONAL SUB-QUESTIONS -- a different axis. Do not conflate.

    (Q-080.a) HARM-COUPLING [v3; substrate-conditional on an effort-dissociating env + an SD-032e effort-input
    variant]. Does over-exertion -> depletion REQUIRE harm's irreversibility-aware special status (residue that
    cannot be erased / No-Go-adjacent caution), or do REE's existing allostatic (SD-032e pACC + SD-032c AIC) +
    EVC (SD-032b dACC) loci suffice WITHOUT coupling effort into the z_harm_a stream? SHARPENED by the lit:
    the fatigue biology (Hogan 2020 Nat Commun -- fatigue raises the subjective COST of effort in insula;
    McMorris 2018 -- central fatigue as AIC interoception feeding ACC continue/stop; Pizzolla 2026 -- fatigue
    from the anticipatory allostatic energy-regulation system) routes over-exertion into the two loci REE
    ALREADY has (EVC cost + allostatic prediction) and does NOT show it recruiting harm's residue/No-Go
    handling -- so the open residue is narrower than "is effort a harm": does it need irreversibility-aware
    handling ON TOP of allostatic+EVC? TEST: effort-dissociating env (low- vs high-effort path to the SAME
    benefit, per-action energy cost measurable, + a chronic-exertion depletion regime), factorial
    {effort->z_harm_a coupling OFF/ON} with value-subtraction + allostatic machinery ON in BOTH arms.
    ANSWERED 'harm-coupling needed' iff ON adds protective disengagement BEFORE depletion + caution scaled to
    the non-reversibility of the depletion that the OFF baseline lacks, strict seed majority. ANSWERED
    'suffices, close' iff OFF already reproduces those and ON adds nothing measurable (effort = foregone value;
    do NOT mint a harm-coupling mechanism). NON-DEGENERACY: the two paths must differ measurably in energy cost
    AND depletion must actually accrue, else substrate_not_ready_requeue, not a verdict. Bears on SD-032b,
    SD-032e, SD-032c, MECH-219, MECH-454, SD-012.

    (Q-080.b) EVOLUTIONARY-PRIMACY / LEAST-EFFORT PRIOR [v3]. Should energy-conservation be a default
    least-effort PRIOR (an evolutionarily-primary prior over policies favouring low effort when benefit is
    indifferent), or is it adequately a value-subtraction TERM (SD-032b EVC + MECH-320/ARC-068 opportunity-
    cost)? Strongly lit-grounded (Selinger 2015 Curr Biol -- humans continuously optimize energetic cost in
    real time, cost SHAPES movement rather than only scoring it; McAllister 2025 behavioural-energetics review;
    Charnov MVT / optimal foraging -- net energy gain as the fitness currency, holding even for minimal
    feeding-rate-only organisms), which grounds the primacy pole WITHOUT settling whether REE's existing term
    already reproduces least-effort-default behaviour. TEST: same env, {least-effort prior OFF/ON} with the
    value-subtraction term ON in both arms. ANSWERED 'prior warranted' iff ON defaults to the low-effort path
    under indifferent/tied benefit where the value-subtraction-only arm leaves the tie unbroken, seed majority.
    ANSWERED 'term suffices' iff the value arm already produces the least-effort default (prior redundant).
    NON-DEGENERACY: benefit must be genuinely tied/indifferent across the two paths in the probe condition,
    else the value term trivially decides. Bears on SD-032b, MECH-320, ARC-068, SD-012.

    (Q-080.c) MECH-454 ENERGY-GROUNDING [v3; substrate_conditional -- inherits MECH-454's two unbuilt
    substrates: the reachable-option-dissociating env + the E3 option-value term; a V4 meta-leg (energy over
    REE's OWN future option-space) is already folded into the self_model_v4 plan]. Is MECH-454's uncertainty-
    gated option-value cost better grounded as a re-acquisition-ENERGY cost (foreclosing an option = paying
    energy to rebuild it) than as the abstract real-options / empowerment value it currently cites -- i.e. does
    the energy-economy framing PREDICT MECH-454's behaviour better? Lit anchor: empowerment / future-option
    maximization (Catenacci Volpi 2020 IEEE TCDS -- optionality as number of future options, handling delayed
    goal commitment; Hayashi 2025 -- high-optionality states) grounds "option-value = future-option max" but
    frames it INFORMATION-THEORETICALLY, not as energy re-acquisition -- so the energy-grounding move is the
    OPEN question. TEST (gated on MECH-454's substrate landing): parameterize the MECH-454 cost two ways on
    the reachable-option-dissociating env -- (i) abstract reachable-distinct-state count vs (ii) energy-to-
    reacquire the foreclosed option -- and test which better predicts option-preserving behaviour under
    manipulated forecast uncertainty. ANSWERED 'energy-grounding stronger' iff (ii) predicts committed
    behaviour better than / subsumes (i); ANSWERED 'abstract suffices' iff behaviourally indistinguishable
    (keep the real-options/empowerment framing). Bears on MECH-454, INV-026, SD-012, MECH-320.

    RESOLUTION PATH: Q-080.a and Q-080.b are the load-bearing pair (both v3-testable once an effort-
    dissociating env exists -- a single env can host both factorial arms). Q-080.c is a MECH-454-grounding
    follow-on, blocked on MECH-454's substrate. No claim-status change; Q-080 stays open with these three
    sub-questions as the testable scaffolding.

    [2026-07-10 /governance] Reviewed the re-surfaced narrow_open_question recommendation: KEPT BROAD / open --
    no further narrowing, no mechanism minted. The load-bearing pair Q-080.a/b now have queued discriminating
    ablations V3-EXQ-730 / V3-EXQ-731 on the effort-dissociating env built 2026-07-09 (exp_entries=0 -- not yet
    run). Node token: complex (probe-gated) -> puzzle (known rules) -- the spike (730/731) supplies the missing
    fact. Revisit next cycle once 730/731 land; the sub-question scaffolding above is the resolution frame.
    No claim-status change.
  location: >
    Interactive thought-digestion 2026-06-24 (continuation of the MECH-454 pass). Origin = the observation that the
    temporal-mechanics failure was at root an ENERGY-economy error (wasted effort re-running work; then dequeuing to
    save effort but mispricing the re-acquisition energy), and the insight "there is harm in excessive effort; energy
    conservation is evolutionarily primary."
  notes: >
    Registered 2026-06-24 via /thought-digestion (user disposition: "just an open question first" -- pose the
    question, do NOT mint a mechanism on a hunch). The genuinely-new ground: REE has effort as a VALUE-subtraction
    (SD-032b dACC EVC, Shenhav 2013; MECH-320/ARC-068 opportunity-cost) and has a depletion integrator (SD-032e pACC
    allostatic load) -- but allostasis integrates THREAT (z_harm_a), NOT effort, and there is NO path where
    over-exertion -> depletion -> a HARM-adjacent state with harm's irreversibility-aware special status. This asks
    whether energy-conservation deserves harm-coupling + evolutionary PRIMACY (a least-effort prior) or stays a
    foregone-value term. It also GROUNDS MECH-454: the option-value cost is really a re-acquisition-ENERGY cost
    (foreclosing an option = paying to rebuild it later), so MECH-454's optionality is a special case of energy-
    economy under uncertainty -- a stronger anchor than the abstract real-options lit. LIT CLUSTERS to pull IF this
    resolves toward a mechanism (NOT pulled yet): optimal foraging / Charnov marginal-value theorem; movement
    energetics (Selinger 2015, continuous real-time energetic optimization); effort-discounting / cost of cognition
    (Kool & Botvinick 2010; Westbrook & Braver); least-action / least-effort priors. DISTINCT from ARC-068 (TIME
    opportunity cost, not metabolic ENERGY) and from MECH-279 freeze (threat motor-arrest, not effort-conservation).
    PROMOTES NOTHING (open question; answer_state / asked).

- id: MECH-455
  title: "competence_based_intrinsic_motivation / goal_achievement_progress_wanting. A wanting bonus indexed to the agent's RATE-OF-IMPROVEMENT in ACHIEVING its own active goals: per active goal, an EMA of the derivative of goal-achievement competence (success-rate, or 1 - proximity-at-termination) injected as intrinsic wanting alongside MECH-229 VALENCE_WANTING, biasing the agent toward goals it is currently getting BETTER at reaching. This is the SECOND intrinsic-motivation family in Mirolli & Baldassarre (2013) -- COMPETENCE-based IM (reward = progress in achieving a self-generated goal, i.e. improving the agent's SKILLS) -- which REE's registered IM stack (MECH-314 info-gain / MECH-314a novelty / MECH-314c learning-progress, all KNOWLEDGE-based: reward = improving the WORLD MODEL) entirely lacks. Same derivative-not-level discipline as MECH-314c, but the operand is BEHAVIOURAL COMPETENCE, not predictive error."
  claim_type: mechanism_hypothesis
  subject: goal_pipeline.competence_based_intrinsic_motivation_wanting
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-11
    needs_review: false
  implementation_phase: v3
  v3_pending: true # unbuilt; promotion gated on the WS-1 competence-based-IM ON/OFF delta experiment on the goal pipeline
  epistemic_category: standard
  claim_level: mechanistic
  registered_utc: "2026-07-09"
  depends_on:
    - MECH-307    # anticipatory-affect goal representations -- the per-goal reps the competence-progress EMA is indexed to
    - SD-012      # homeostatic drive plane -- host for the goal pipeline / drive that the wanting bonus rides
    - MECH-229    # VALENCE_WANTING -- the wanting injection site; competence-progress is a NEW wanting operand alongside the homeostatic/hedonic ones
  functional_restatement: >
    Mirolli & Baldassarre (2013) draw the field's load-bearing distinction between
    two intrinsic-motivation families: KNOWLEDGE-based IM (novelty, surprise,
    prediction error, learning progress -- reward improving the agent's WORLD MODEL;
    drives exploration/attention) and COMPETENCE-based IM (reward = progress in
    ACHIEVING a self-generated goal -- reward improving the agent's SKILLS; drives
    autonomous cumulative skill acquisition). REE's entire registered IM stack is
    knowledge-based (MECH-314 info-gain, MECH-314a novelty, MECH-314c
    learning-progress). It has NO competence-based IM: the goal pipeline supplies
    goal representations (MECH-307) but sources wanting from homeostatic/hedonic
    value (SD-012, MECH-229), never from competence progress.

    MECH-455 names the missing drive. For each active goal in the pipeline, maintain
    an EMA of the IMPROVEMENT in the agent's success at reaching it (success-rate, or
    1 - proximity-at-termination). Inject that derivative as an intrinsic WANTING
    bonus that biases the agent toward goals it is currently getting better at
    achieving -- a competence-progress operand added alongside MECH-229's existing
    wanting sources. Same derivative discipline as MECH-314c (track the rate of
    improvement, not its level -- avoids both the already-mastered and the
    never-improving traps), but the operand is BEHAVIOURAL COMPETENCE rather than
    predictive error.

    Sequencing exemption from the wired-but-inert routing block: world-model-side IM
    bonuses (MECH-314c per-candidate, MECH-314a pseudocount) are inert on the current
    substrate until the EMA -> E3-selection routing is repaired (V3-EXQ-141b /
    V3-EXQ-590a: broadcast novelty reaches the EMA but not the committed action).
    MECH-455 is PARTLY EXEMPT because it injects into the WANTING/goal path, not the
    broadcast-novelty -> E3 path -- a reason it is ranked first for the WS-1
    competence floor. A clean demonstration of any IM bonus still benefits from the
    routing fix the conversion-ceiling line (V3-EXQ-700b and successors) is chasing.

    Falsifiable (the WS-1 competence experiment, to be queued SEPARATELY via
    /queue-experiment): a competence-based-IM bonus ON vs OFF on the goal pipeline,
    DV = foraging competence crossing the 1.0/episode floor. WS-1's competence floor
    (all-ON agent forages 0.065/0.0/0.455 resources/episode, below 1.0) is the exact
    symptom Baldassarre & Mirolli predict when skills are earned with a
    knowledge-based drive alone.
  evidence_quality_note: >
    PROMOTES NOTHING. Registered 2026-07-09 as the single novel candidate the WS-9
    intrinsic-motivation lit-pull warrants (everything else maps onto existing
    MECH-314-family claims). WS-9 synthesis + ranking (mechanism #1, highest WS-1
    leverage): evidence/planning/intrinsic_motivation_competence_mechanisms.md.
    Owning thread: ree_ai_design_critique_plan.md WS-9 (lit) -> WS-1 (competence
    floor before structure). Hosts on the goal pipeline: goal_pipeline_plan.md
    (MECH-307 / SD-012 / MECH-229 wanting site).

    Literature grounding (4 canonical entries in
    evidence/literature/targeted_review_intrinsic_motivation_exploration/entries/):
      - Mirolli & Baldassarre 2013 -- knowledge-based vs competence-based IM; the
        competence-based family this claim registers
        (2026-07-09_goal_pipeline_competence_based_im_baldassarre2013)
      - Oudeyer & Kaplan 2007 -- IM typology; learning-progress (IAC/R-IAC) as the
        derivative discipline shared with this claim
        (2026-07-09_mech314c_learning_progress_typology_oudeyer2007)
      - Schmidhuber 2010 -- compression-progress / formal theory of creativity;
        concedes skills are a by-product of knowledge-seeking (the gap this fills)
        (2026-07-09_mech314c_compression_progress_creativity_schmidhuber2010)
      - Bellemare et al. 2016 -- count-based / pseudocount exploration; the
        knowledge-side novelty estimator this is deliberately DISTINCT from
        (2026-07-09_mech314a_pseudocount_exploration_bellemare2016)

    Distinct from MECH-314c (learning-progress curiosity): 314c's operand is the
    derivative of PREDICTIVE-MODEL error (knowledge-based, world-model side, routed
    to E3 selection); MECH-455's operand is the derivative of GOAL-ACHIEVEMENT
    competence (competence-based, wanting/goal side). They share only the
    derivative-not-level form. Distinct from MECH-314a/MECH-314 (novelty / info-gain
    coverage, not skill).
  location: evidence/planning/intrinsic_motivation_competence_mechanisms.md

- id: MECH-457
  title: "action_learning_as_first_class_actor_critic_substrate / rpe_driven_policy_learning. Competent action learning in REE requires a DEDICATED RPE-driven actor-critic policy-learning substrate (a dorsal-striatal analog: a parameterized actor trained on a reward-error teaching signal, with a value-baseline critic), architecturally distinct from -- and NOT reducible to -- the current thin bias_head REINFORCE readout over the prediction-trained (SD-056 e2 world-forward contrastive) encoder. The V3-EXQ-724 competence autopsy localized REE's foraging wall (integrated all-ON agent forages ~0 resources/episode vs a 1.0 floor) to exactly this un-varied invariant: action is learned only as a bias on a representation trained for SENSORY PREDICTION, never as a first-class policy object trained on RETURN. 'Prediction-rich, action-poor': REE trains a substantial world model but has no dedicated action-learning system -- in brains, action learning IS a substantial learning system (O'Doherty 2004 dorsal-striatum-as-actor; Schultz 1997 dopaminergic RPE teacher), not a bias term on a prediction model."
  claim_type: mechanism_hypothesis
  subject: f_dominance_conversion_ceiling.actor_critic_action_learning_substrate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-24
    needs_review: false
    evidence:
      from: "failure_autopsy_competence-objective-cluster-734-737b-742a_2026-07-22#V3-EXQ-742b"
      as_of: 2026-07-24
      verdict: non_contributory/measurement_test_design_defect
  granularity_debt_disposition: decomposed  # was coherent_campaign until 2026-07-22; see granularity_debt_decomposition_note (SUPERSEDES the 2026-07-16 GOV-GRAN-1 contraindication) then granularity_debt_recurrence_note for the prior history
  granularity_debt_decomposition_note: >
    DECOMPOSED 2026-07-22 (/claim-synthesis, session elegant-curran-fe12ba, user-approved
    per-child; evidence/planning/claim_synthesis_MECH-457_2026-07-22.md). SUPERSEDES the
    2026-07-16 coherent_campaign disposition below. What changed since 2026-07-16: +9
    autopsy targets (769/770/771/772/780/781/782/788/789/792/734/737b/742a); the question
    shape changed twice by explicit user adjudication (H-bc-prior split three ways
    2026-07-19: acquisition CONFIRMED, retention OPEN); GOV-FROZEN-1 now reports
    convergence_class CIRCLING with families_fresh EMPTY, whose recorded response is
    "RE-POSE the operationalization" -- which a decomposition IS. The 2026-07-16 note's own
    anti-duplication argument (ARC-065/MECH-314/MECH-455/INV-088 own every candidate) no
    longer holds: those are all drive/diversity owners, whereas the two children registered
    here are baseline-informativeness and acquisition/retention-dissociation, which no claim
    owned. Two children registered candidate/v3_pending: MECH-475
    (uninformative_value_baseline_makes_optimisation_iatrogenic) and MECH-476
    (competence_retention_dissociable_from_acquisition). Re-derive brake did NOT fire (0
    confirmed substrate_ceiling hits under R1-R3). MECH-457 is RETAINED as the narrowed
    umbrella (see the sufficiency disclaimer in evidence_quality_note 2026-07-22); status
    unchanged candidate/v3_pending. This is a decomposition, NOT a demotion.
  granularity_debt_recurrence_note: >
    GOV-GRAN-1 P1 discrimination (2026-07-16, session gifted-kilby-8e2491). Audit flagged
    6 no-verdict claim-keyed hits / 2 signatures (750 substrate_starved_precondition_unmet;
    752-756 competence_implementation_gap x5). VERDICT: COHERENT substrate-build campaign,
    NOT a too-coarse claim. The signatures are progressive localizations of ONE competence-
    bootstrap gap -- a success-INDEPENDENT dense per-step drive PLUS actor-critic policy
    CAPACITY to convert coverage->competence -- not >1 bundled mechanism (the whole GOV-FANOUT-1
    portfolio converged: only the two success-independent classes, RND 5.22 / BC 32.72, ever
    cleared the 1.0 floor; every success-dependent class collapsed for the same cold-start
    reason). /claim-synthesis CONTRAINDICATED: a split would duplicate ARC-065/MECH-314
    (curiosity substrate), MECH-455 (competence-based drive), and INV-088 (strategy diversity)
    -- every candidate sub-claim already has an owner (same shape as MECH-180 vs INV-050).
    Re-derive brake FIRED (752-756) with route_to=implement-substrate (authors never routed to
    claim-synthesis; granularity_debt_trigger false throughout). Substrate `mech457_bootstrap_
    explorer` was BUILT 2026-07-16. UPDATE 2026-07-16T15:32Z: the post-build retest V3-EXQ-765
    RAN and FAILED (outcome FAIL, discrimination_verdict bootstrap_explorer_plateaus_capacity_
    gap_remains) -- the drive half helps but the CAPACITY half of the same one gap persists;
    765 is a NEW hit that needs its own /failure-autopsy (unreviewed as of this writing) and
    remains within this coherent campaign. This disposition metabolizes the 750-756 GOV-GRAN-1
    recurrence only; it PROMOTES/DEMOTES NOTHING (status stays candidate/v3_pending) and does
    NOT set epistemic_category (0 substrate_ceiling verdicts; GOV-CEIL-1 unaffected). REFUSE any
    fresh same-question explorer-mechanism re-queue (new H-* leg / mode-gate retune / combination
    cell) -- the portfolio is exhausted at single-axis and pairwise level; the only sanctioned
    next step is autopsying 765 and the capacity-side substrate work it names. UPDATE
    2026-07-16T16:45Z (session jovial-joliot-9f904b): 765 AUTOPSIED (failure_autopsy_V3-EXQ-765_
    2026-07-16, confirmed, non_contributory) and the capacity-side amend BUILT -- three JOINT knobs
    on the SAME build (hidden 128->256 + budget 3x->5x; warm-start 0.2 + credit passes 3->6 / topk
    32->64; z_world cotrain->DETACHED per Stooke 2021); OFF arms bit-identical to 765's. Retest
    V3-EXQ-769 (supersedes 765) queued. Brake HELD-BUT-SANCTIONED for that ONE named-capacity
    post-build retest under a NEW EXQ number; it still REFUSES any fresh single-axis probe. STILL
    PROMOTES/DEMOTES NOTHING (candidate/v3_pending); 0 substrate_ceiling verdicts (GOV-CEIL-1
    unaffected).
  implementation_phase: v3
  pending_retest_after_substrate: true  # substrate_queue mech457_competence_bootstrap_explorer: original build (022a284) failed the 765 retest (capacity gap); CAPACITY-SIDE AMEND built 2026-07-16 (hidden 256 + 5x budget + warm-start/credit-reliability + z_world detached) but V3-EXQ-769 (supersedes 765) RAN + REGRESSED (raw ON 6.48->0.12) -- capacity axis FALSIFIED (failure_autopsy_V3-EXQ-769_2026-07-17); reconfirmation re-routed to GOV-FANOUT-1 portfolio V3-EXQ-770/771/772 (drive-schedule / reward-coupling / credit-horizon)
  evidence_quality_note: |
    2026-07-22 (/claim-synthesis decomposition + D3-rider; session elegant-curran-fe12ba, user-approved; PROMOTES/DEMOTES NOTHING, status stays candidate/v3_pending). THREE records.
    (1) SUFFICIENCY DISCLAIMER. MECH-457 asserts a dedicated actor-critic substrate is REQUIRED (necessity), NOT that it is SUFFICIENT. The sufficiency reading is what V3-EXQ-742 (2026-07-13) weakened and what the 770/771/772 cluster recorded as "INTACT but necessary-not-sufficient". The claim is retained as the NARROWED UMBRELLA of the competence-floor campaign; the finer mechanisms it did not name are now carried by children MECH-475 and MECH-476 (registered candidate/v3_pending 2026-07-22; see related_claims + granularity_debt_decomposition_note).
    (2) D3_HAZARD_FREE MEASUREMENT-VALIDITY RIDER (approved option a+b). On D3_hazard_free the return does NOT rank the scored foraging DV: random_walk mean_episode_reward = -1.006 and the greedy oracle survives only 20.4 steps vs vanilla PPO's 175, so survival (maximised by NOT foraging) dominates the return. Nine campaign eliminations were measured on that yardstick (752-755, 765, 769, 770, 772, 737b/742a). They STAY ELIMINATED -- but a reader should know they were measured on a non-ranking instrument. The affected legs' basis lines in hypothesis_space_registry.v1.json are being annotated correspondingly (option b). NOTE this does NOT make the objective the cause of the floor: on the METABOLIC env (V3-EXQ-771) the return ranks competence correctly and steeply (oracle 34.25, random 4.56) and the learners STILL floored at 0.90-1.12 return -- a quarter of random walk -- dying in 100% of episodes. Objective mis-specification therefore survives as a D3 measurement-validity defect, NOT as the mechanism; the 2026-07-22 user adjudication (objective mis-specification for the 734/737b/742a cluster) stands for D3_hazard_free and does not generalise into a mechanism claim. Full worked reconciliation in evidence/planning/claim_synthesis_MECH-457_2026-07-22.md section 4.0.
    (3) NO fan-out growth event was created by the decomposition (children reuse existing legs H-retention-critic / H-retention-consolidation and the conversion_ceiling_root H-objective-misspecification leg). Synthesis document: evidence/planning/claim_synthesis_MECH-457_2026-07-22.md.
    2026-07-16 (V3-EXQ-765, diagnostic, claim_ids=[MECH-457]; failure_autopsy_V3-EXQ-765_2026-07-16): post-build retest of the composed mech457_competence_bootstrap_explorer substrate RAN and FAILED (bootstrap_explorer_plateaus_capacity_gap_remains). Readiness PASSED (local_view_greedy 48.05, oracle 57.2 vs 1.0 floor -- env solvable from the 5x5 view). The composed DRIVE half works on raw (ON 6.48 vs OFF 0.62, +5.87 converter-driven lift; n_credit_replay_passes 4500-8259) but the CAPACITY-to-convert half persists: raw ON plateaus at ~13% of the 48.05 achievable ceiling and clears NEITHER representation's 13.05 lift-competence target. Two subordinate findings: (1) z_world cotrain is DESTRUCTIVE (ON 0.35 < OFF 5.22 -- explorer drive corrupts the prediction-trained encoder; detach the z_world path per Stooke 2021); (2) high seed variance on raw (15.9 / 3.05 / 0.5 -- convert-ability unreliable, residual partly credit-reliability, not pure capacity). epistemic_category competence_implementation_gap (0 substrate_ceiling verdicts preserved; GOV-CEIL-1 unaffected); evidence_direction non_contributory (diagnostic, PROMOTES/DEMOTES NOTHING). Re-derive brake FIRED (5th non_contributory MECH-457 autopsy): REFUSE any fresh same-question explorer-mechanism re-queue; route = implement-substrate capacity-side amend of mech457_competence_bootstrap_explorer (raise policy capacity/budget + reliability/warm-start + z_world cotrain detach). AMEND BUILT 2026-07-16 (session jovial-joliot-9f904b): ON actor_critic_hidden 128->256 + ON_BUDGET_MULTIPLIER 3->5; ON warm_start_fraction 0.2 + credit_replay_passes 3->6 / credit_topk 32->64; ON z_world DETACHED (cotrain_encoder False) -- all OFF-preserving no-op defaults (OFF bit-identical to 765). Retest V3-EXQ-769 (supersedes 765) queued (cloud). MECH-457 stays candidate/v3_pending. This is a NEW hit within the GOV-GRAN-1 coherent-campaign disposition, not a granularity signal.
    2026-07-17 (V3-EXQ-769, diagnostic, claim_ids=[MECH-457]; failure_autopsy_V3-EXQ-769_2026-07-17, supersedes V3-EXQ-765): the 765-routed CAPACITY-side amend (ON hidden 128->256 + budget 3x->5x; warm-start 0.2 + credit passes 3->6 / topk 32->64; z_world cotrain->DETACHED) RAN and FAILED, and it REGRESSED rather than plateaued. Readiness PASSED (local_view_greedy 48.05, oracle 57.2 vs 1.0 floor). Raw ON fell 6.48->0.12 (below its OFF; the single 765 clearing seed 15.9->0); z_world ON unchanged ~0.27 despite detaching (refuting the 765 cotrain-destructive subordinate finding); credit machinery fired (n_credit_replay_passes 9k-18k). Behavioral signature: every ON arm survives 200 steps, death 0, contamination avoided, forage ~0 -- AVOIDANCE learned WITHOUT APPROACH; on D3_hazard_free survival is decoupled from foraging so more optimization drives the policy into the passive-survival basin. This EMPIRICALLY FALSIFIES the 765 capacity diagnosis: the wall is not actor capacity but developmental-ordering / reward-coupling (approach-before-avoidance root). epistemic_category competence_implementation_gap (missing-dependency reframe, user-confirmed; NOT substrate_ceiling -- ordering/reward axis untested; 0 substrate_ceiling verdicts preserved, GOV-CEIL-1 unaffected); evidence_direction non_contributory (diagnostic, PROMOTES/DEMOTES NOTHING). Re-derive brake FIRED (6th non_contributory MECH-457 autopsy): REFUSE any further capacity/same-axis explorer re-queue (no 769b / bigger net / more budget / warm-start retune). Routing = GOV-FANOUT-1 discrimination portfolio (V3-EXQ-770 H1 drive-schedule / 771 H2 reward-coupling / 772 H3 credit-horizon), each with a declared null, coordinated with the in-flight approach-before-avoidance ordering falsifier. Secondary methodological finding: the build's 'OFF bit-identical to 765' claim is false (substrate_hash cd368e11->be9a4d7f; OFF z_world 5.22->2.63) -- the no-op-default contract was not numerically verified. MECH-457 stays candidate/v3_pending; a NEW hit within the GOV-GRAN-1 coherent-campaign disposition, not a granularity signal. INV-088 stays candidate/pending_substrate_ reconfirmation (769 was its retest path; reconfirmation did not occur; unblock re-routed onto the 770/771/772 portfolio). --- 2026-07-18 governance (V3-EXQ-770 / 771 / 772 CLUSTER, diagnostic, claim_ids=[MECH-457]; failure_autopsy_MECH-457-fanout-770-771-772_2026-07-18, status confirmed): the GOV-FANOUT-1 discrimination portfolio routed by the 769 autopsy RAN and ALL THREE FAILED, each hitting its OWN pre-registered null -- 770 drive_schedule_not_the_axis, 771 reward_coupling_not_the_axis, 772 credit_horizon_not_the_axis. Readiness met on every leg (local_view_greedy 48-55, oracle 57-61 vs the 1.0 floor -- the env is solvable from the 5x5 view in all three), every treatment arm forages AT THE FLOOR (~0-1, random-walk level) on BOTH z_world and raw, non_degenerate TRUE x3. Combined with 769 (capacity falsified) this eliminates FOUR axes: capacity, drive-schedule, reward-coupling, credit-horizon. REFRAME (user-confirmed at the autopsy Step-8 gate): this is a MISSING-DEPENDENCY finding, NOT a substrate ceiling. The learned actor-critic converter cannot extract a competent policy from a PROVABLY SUFFICIENT observation, and is invariant to every config / env / credit / capacity lever tried. MECH-457 itself is INTACT but necessary-not-sufficient: behavioural cloning (imitation 32.72) remains the ONLY floor-clearing existence proof, so the gap is in the converter's bootstrap, not in the claim's assertion that a dedicated actor-critic substrate is required. epistemic_category competence_implementation_gap (explicitly NOT substrate_ceiling -- 0 ceiling verdicts added, GOV-CEIL-1 exhaustion count unaffected). evidence_direction non_contributory (PROMOTES / DEMOTES NOTHING; MECH-457 stays candidate/v3_pending). Re-derive brake FIRED (7th consecutive non_contributory MECH-457 autopsy): REFUSE any further config / env / credit / capacity re-queue on this axis. Routing (user-chosen): a NEW GOV-FANOUT-1 discrimination portfolio FIRST -- H-bc-prior (competence-directed behavioural-prior / imitation seed) vs H-approach-primitive (innate non-extinguishing demonstrator-free approach drive), each with a declared null; the substrate amend to mech457_competence_bootstrap_explorer is DEFERRED until that discrimination resolves. Hypothesis-space ledger: H1/H2/H3 resolved eliminated (full bar met), H-bc-prior / H-approach-primitive pre-registered alive (competence_floor 7 -> 12).

    PROMOTES NOTHING. Registered 2026-07-10 via /lit-pull as the single novel candidate the actor-critic action-learning lit-pull warrants -- grounding the 'prediction-rich, action-poor' translation gap the V3-EXQ-724 autopsy identified as REE's competence bottleneck. Distinct from MECH-455 (competence-based intrinsic motivation = why the agent doesn't WANT to earn competence, the drive side) -- MECH-457 is the action-LEARNING MACHINERY that converts drive into learned control (how action is learned at all). Sibling, not duplicate. Owning thread: ree_ai_design_critique_plan.md WS-1 (competence floor) -> conversion_ceiling_campaign_plan.md; build target = the f_dominance_conversion_ceiling entry in evidence/planning/substrate_queue.json. NO substrate written yet: the H1/H2 discriminator (V3-EXQ-737) decides the build target before any commit.
    Literature grounding (5 canonical entries in evidence/literature/targeted_review_actor_critic_action_learning/entries/):
      - Schultz, Dayan & Montague 1997 (Science) -- dopaminergic reward-prediction
        error as the dedicated teaching signal for action learning; the reward-error
        channel REE lacks
        (2026-07-10_actor_critic_rpe_teaching_signal_schultz1997)
      - O'Doherty et al. 2004 (Science) -- dorsal striatum = actor, ventral = critic;
        the direct biological anchor for action-learning as a first-class dissociable
        system (2026-07-10_actor_critic_dorsal_ventral_striatum_odoherty2004)
      - Collins & Frank 2014 (Psych Review) -- OpAL opponent-actor / D1-D2
        credit-assignment internals; a later refinement, not the minimal build
        (2026-07-10_actor_critic_opponent_actor_credit_collins_frank2014)
      - Sutton et al. 2000 (NeurIPS) -- policy-gradient theorem / actor-critic; the
        formal reason a separately-trained policy + value baseline learns control a
        value/prediction model won't
        (2026-07-10_actor_critic_policy_gradient_theorem_sutton2000)
      - Stooke et al. 2021 (ICML) -- decoupling representation from RL; supports the
        frozen-encoder + trainable-policy design AND flags the residual feature-
        adequacy (H2) risk
        (2026-07-10_actor_critic_decoupled_representation_control_stooke2021)

    Distinct from targeted_review_action_policy_decomposition (how action DECOMPOSES into habits/goals/hierarchy/chunks -- SD-004/ARC-021/SD-045), from targeted_review_striatal_gain_control_bounding (MECH-448 gain/demotion envelope), and from targeted_review_mech_090_release_motor_cessation (release/stop). This dossier is 'how action is learned AT ALL', a different question.
    Readiness taxonomy (work_graph_debt_vocabulary.md): the owed actor-critic build is `complicated (buildable)` execution backlog -- buildable now, nothing missing. It sits behind exactly ONE `complex (probe-gated) / puzzle (known rules)` node: is the actor the right target, or is the observation encoder the problem (H1 vs H2)? We keep the rules (the actor-critic frame is well-posed) and lack a fact. V3-EXQ-737 is the spike that discharges it; a green 737 converts the whole line to `complicated (buildable)` backlog. V3-EXQ-738 already discharged one leg (refuted strong-H2: the 1.0 floor is reachable from the 5x5 local view). So MECH-457 is NOT "blocked" in the can't-build sense -- it is gated on a single reducible unknown. (NB: "gated on 737" here = gated on the 737 spike's OUTCOME being supporting, which is what promotion needs; the BUILD is gated on the 737 DECISION, i.e. go/no-go + target. Not blocked: nothing that must exist is missing.)
    2026-07-13 (V3-EXQ-742, the MECH-457 ON/OFF validation; evidence, claim_ids=[MECH-457]; consumed via /governance from failure_autopsy_morning-digest-742-744a-745-746-746a_2026-07-13): V3-EXQ-742 (2026-07-13, evidence, ON/OFF validation) WEAKENS the sufficiency reading: the first-class RPE actor-critic substrate (all 4 arms: frozen/cotrain x plain/SF) forages 0.20-0.27/ep at D3 -- below random_walk (0.93) and 0.5% of the local_view_greedy ceiling (48.05) -- while every readiness precondition holds (oracle + local-view clear the floor, bias_head reproduces incompetence). Necessity NOT falsified; the deficit sits UPSTREAM of action-learning credit-assignment. Refutes the 734-737 autopsy's 'single missing mechanism = MECH-457' conclusion; the surviving structural-property half ('prediction-trained z_world is action-inadequate') is the leading hypothesis. Routed to GOV-FANOUT-1: H-rep (actor on raw 5x5 view) vs H-explore (reward-shaping / BC-warmup). Same-question floor re-pose REFUSED. MECH-457 stays candidate/v3_pending.
    2026-07-14 (GOV-FANOUT-1 discrimination portfolio V3-EXQ-747/748/749, diagnostic, claim_ids=[MECH-457]; consumed via /governance from failure_autopsy_MECH-457-fanout-747-748-749_2026-07-13): the two-axis fanout (representation x teacher) REFUTES H-rep and the reward-density reading. Reward SHAPING rescues neither representation (z_world shaped 0.217, raw shaped 0.767 -- both sub-floor 1.0/ceiling 48.05); only behavior-cloning clears the floor, on BOTH inputs (z_world+BC 32.72, raw+BC 20.93), and z_world+BC > raw+BC -- so the prediction-trained z_world is NOT action-inadequate. The discriminated cause is the RL exploration/ credit-assignment bootstrap (only imitation bypasses it), not representation and not reward density. This STRENGTHENS a dependency reading: action-learning requires a bootstrappable action-level teaching signal / developmental scaffold (BC = observational-acquisition analog), consistent with the O'Doherty/Schultz/Sutton biology. MECH-457 stays candidate/v3_pending, competence_implementation_gap; sufficiency weakens, dependency strengthened. One live discrimination remains on the algorithm axis (is an expert teacher necessary, or is unsupervised exploration merely too weak) -- the H-optim leg V3-EXQ-751 (stronger unsupervised explorer, no expert; PPO+entropy + RND/ICM) is queued and in flight to decide the build target. No substrate_queue entry amended/created this cycle (autopsy sec8 action=none: the build target -- better unsupervised explorer vs imitation/curriculum scaffold -- is exactly what the H-optim leg must decide; f_dominance_conversion_ceiling stays as-is). Re-derive brake does NOT fire (0 prior substrate_ceiling/non_contributory autopsies tagging MECH-457). The three runs are diagnostic (excluded from governance scoring); this note records the discrimination, not a weighted evidence entry.
    2026-07-24 governance (V3-EXQ-792, evidence, claim_ids=[MECH-457]; failure_autopsy_V3-EXQ-792_2026-07-22, confirmed): the retention-consolidation retest RAN and FAILED (retention_grid_nondiscriminative). User-selected routing "Re-power, keep leg alive" over resolving as confirmed on the load-bearing PASS. epistemic_category measurement_test_design_defect; evidence_direction non_contributory (PROMOTES/DEMOTES NOTHING). MECH-457 stays candidate/v3_pending.
    2026-07-24 governance (V3-EXQ-734 / 737b / 742a cluster, evidence+diagnostic, claim_ids=[] for 734/737b, [MECH-457] for 742a; failure_autopsy_competence-objective-cluster-734-737b-742a_2026-07-22, confirmed, user-selected routing "Objective mis-specification"): the recorded `weakens` on 742a is WITHDRAWN and revised to `non_contributory`. All four actor-critic arms scored 0.200-0.267 D3 foraging competence -- BELOW the run's own random-walk anchor of 0.933 -- while the training return is dominated by episode survival (vanilla PPO survives 175.0 steps vs the greedy oracle's 20.4 while foraging 17x less). MECH-457 was not tested under conditions where it could express itself; H-objective-misspecification is the live reading (H-policy-learning explicitly NOT eliminated). Additionally the frozen-encoder arms ran on an untrained z_world (sd_zworld_warmup_optimizer_group adoption did not reach this driver copy -- FOURTH per-copy strike, substrate_queue amended). epistemic_category measurement_test_design_defect; evidence_direction non_contributory. GOV-FANOUT-1 portfolio open: return-decomposition (reward axis, queued V3-EXQ-808) + reweighted-objective re-run (policy axis). MECH-457 stays candidate/v3_pending; pending_retest_after_substrate.
    2026-07-24 governance (V3-EXQ-742b, evidence, claim_ids=[MECH-457]; extends failure_autopsy_competence-objective-cluster-734-737b-742a_2026-07-22 as a 4th target, confirmed, user-selected routing "Extend cluster autopsy, fanout stays open"): 742b closed the P0 encoder-training confound that invalidated 742a (zworld_encoder_guard now shows encoder_moved_in_p0=true on every arm, encoder_moved_in_ac correctly differing between frozen/cotrain) -- the frozen-vs-cotrain contrast is now genuine. Despite the fix, all four actor-critic arms STILL fail D3 (0.267-0.383), underperforming even the simple bias_head_baseline (0.533, itself sub-floor). The newly instrumented reward_hacking_guard shows train_return_vs_eval_foraging_divergence strongly negative on every arm (-0.82 to -1.40) -- direct, quantified corroboration of H-objective-misspecification from a different angle than 742a's survival-horizon argument. Does NOT itself resolve H-objective-misspecification vs H-policy-learning (not the literal return-decomposition/reweighted-objective probe); both GOV-FANOUT-1 probes remain the sanctioned next step. `weakens` WITHDRAWN, revised to `non_contributory`. Granularity-debt count now 20 confirmed autopsy targets (was 19) -- does NOT change the 2026-07-16 GOV-GRAN-1 coherent_campaign adjudication; this is a further instance of the same coherent campaign. MECH-457 stays candidate/v3_pending. 742b's own manifest self-flags refusal of a third lettered actor-critic re-run (742c) in favour of the two open fanout probes -- upheld.
    2026-07-25 governance (V3-EXQ-792a, evidence, claim_ids=[MECH-457]; confirmed failure_autopsy_backlog_2026-07-24): FIRST GENUINE SUPPORTS for MECH-457 -- but on the RETENTION-CONSOLIDATION leg, which is DISTINCT from the competence-bootstrap leg that carries the FAIL campaign above. Indexer flagged vacuous_pass (on the deliberately-demoted non-gating C_anchor_bound_dose_response DV), but USER-CONFIRMED genuine: the load-bearing C_anchor_bound_vs_measured_control PASSED non-degenerately (anchored arm suppressed >0.10 of the unconstrained control's MEASURED kl-drift; control eroded 20.933 -> 11.667) and C_anchored_arm_consolidates PASSED (KL>0.001, peak>=1.05x install). Manifest evidence_direction stamped supports this cycle. Does NOT lift v3_pending (the competence-bootstrap leg's FAIL campaign is unresolved and gates the claim); records that the retention-consolidation sub-mechanism has its first genuine V3 confirmation.
    2026-07-25 governance (V3-EXQ-821, diagnostic, claim_ids=[MECH-457]; confirmed failure_autopsy_V3-EXQ-821_2026-07-25, GOV-FANOUT-1 retention leg 4 H-consummation-binding): the consummatory-act binding (approach_extinguishes_on_contact hand-off to a distinct CONSUME action) is NOT the retention mechanism. Null shape consummation_binding_eroded_under_both: BC-installed foraging competence took on both arms (post-BC 4.3-5.7 > 1.0 floor) but eroded to retained_fraction 0.0 in BOTH the extinguishing treatment AND the control -> H-consummation-binding ELIMINATED. epistemic_category standard (NOT substrate_ceiling; 0 ceiling verdicts added, GOV-CEIL-1 unaffected); evidence_direction non_contributory (PROMOTES/DEMOTES NOTHING; MECH-457 unaffected, stays candidate/v3_pending). This closed the LAST open competence_floor retention leg: with the constitution-family loci eliminated (789/821) and the process-family loci confirmed (788 critic / 792a consolidation), the retention decision_question was CLOSED `decided` (decision_log 2026-07-25, scoped to retention) -- the FIRST decided question in the frozen ledger. The competence FLOOR / installability explanandum (V3-EXQ-819, conversion_ceiling_root) remains the live frontier and is NOT closed by this. Applied /governance 2026-07-25 (session angry-ardinghelli-d99740-gov, user-approved).
    2026-07-26 governance (V3-EXQ-819, diagnostic, flagged precondition_unmet, claim_ids=[MECH-457, INV-088];
    failure_autopsy_batch-793a-817-819_2026-07-26, confirmed): the competence-floor/installability leg named
    above RAN. Self-route zworld_advantage_grid_nondiscriminative ("not ready") is a MISNOMER -- the z_world
    encoder DID train on 3/3 seeds (delta 0.277-0.333; the 780 frozen-random-projection fault is FIXED). The
    vacating fault is a worst-seed non-vacuity gate that contradicts its own strict-majority routing predicate
    (headline.advantage_confirmed was true) -- a measurement_test_design_defect, NOT a substrate_ceiling (0
    ceiling verdicts added, GOV-CEIL-1 unaffected). Directional, UNSCORED signal only: a prediction-trained
    z_world still reaches only AUC 0.90 (~2% of the 48-57 achievable ceiling; 0/3 seeds over the 1.0 effect
    margin) -- mild, non-load-bearing support for the necessity thesis (encoder quality alone does not confer
    competent action). evidence_direction non_contributory; PROMOTES/DEMOTES NOTHING. Hypothesis-space ledger:
    new leg H-zworld-trained-instrument pre-registered alive (competence_floor question, labelled fan-out,
    axis=instrumentation) -- this is instrument-validity, not a re-entry of the eliminated H-rep leg. Re-queue
    819a reconciling the non-vacuity gate to the routing predicate is recommended. MECH-457 stays
    candidate/v3_pending; INV-088 stays candidate, reconfirmation still unresolved.
  v3_pending: true # substrate BUILT 2026-07-12 (ree_core/action_learning/actor_critic.py); the V3-EXQ-737 H1 discriminator RAN and FAILED (frozen-latent refuted); the MECH-457 ON/OFF validation V3-EXQ-742 (2026-07-13) RAN and WEAKENS (all 4 arms sub-floor with readiness met) -- refutes the 734-737 'single missing mechanism = MECH-457' conclusion; deficit sits UPSTREAM of action-learning; stays candidate/v3_pending, discrimination now via GOV-FANOUT-1 (H-rep vs H-explore)
  epistemic_category: standard
  claim_level: mechanistic
  registered_utc: "2026-07-10"
  implementation_note: "Substrate IMPLEMENTED 2026-07-12 (session nice-blackwell-f367f8, /implement-substrate PRIMARY routing from failure_autopsy_734-737-conversion-ceiling-competence_2026-07-11). ree_core/action_learning/actor_critic.py (ActorCriticPolicy: dorsal-striatal actor + plain value critic + optional successor-feature critic V_SF=psi.w, w grounded in MECH-229 VALENCE_WANTING), config knobs use_actor_critic / actor_critic_cotrain_encoder (the co-shaping ablation lever) / actor_critic_use_sf_critic / actor_critic_hidden / actor_critic_sf_feature_dim (all no-op default; bit-identical OFF), agent hooks actor_critic_step (applies co-shape-vs-detach) / actor_critic_reward (R_t = benefit_eval - harm_eval, ARC-108) / actor_critic_parameters / actor_critic_encoder_parameters (latent_stack = the co-shape seam). Distinct from the lateral_pfc/ofc bias_head REINFORCE readout: emits its own action distribution + value, not a clamped +/-bias_scale nudge into E3's argmin. Smoke PASS; backward-compat verified (defaults -> action_critic None). Design doc docs/architecture/sd_actor_critic_action_learning.md (IMPLEMENTED). PROMOTES NOTHING: MECH-457 stays candidate/v3_pending until the ON/OFF validation runs + is reviewed. Deps SD-056 (built) + MECH-229 (built) unchanged. COMPETENCE-IMPLEMENTATION-GAP substrate mech457_competence_bootstrap_explorer IMPLEMENTED 2026-07-16 (session unruffled-lichterman-65e45e, /implement-substrate, ree-v3 main 022a284): experiments/_lib/mech457_bootstrap_explorer.py composes the landed RND success-independent drive + prioritized credit-replay converter + a NEW developmental intrinsic-coef/entropy anneal + increased budget on the first-class actor-critic (both z_world cotrain and raw 5x5), after the 751-756 GOV-FANOUT-1 close showed no single/pairwise mechanism clears the 1.0 foraging floor toward the 48.05 local-view ceiling. No-op-default bit-identical OFF (full pytest 1428 passed + 6 new contracts). PROMOTES NOTHING: retest V3-EXQ-765 (diagnostic, OFF-vs-ON x representation, foraging lift >=7.83 above the 5.22 RND plateau toward BC 32.72) queued + coordinator-ingested; stays candidate/v3_pending until it scores. Design doc docs/architecture/sd_mech457_competence_bootstrap_explorer.md."
  depends_on:
    - SD-056    # e2 world-forward contrastive encoder -- the prediction-trained representation the actor would ride (frozen per V3-EXQ-737, or permitted to adapt for action)
    - MECH-229  # VALENCE_WANTING -- the valence/reward signal an actor-critic reward-error (RPE) teacher would consume to shape the policy
    - MECH-459  # CDQ-007 competence-bootstrap candidate: return-scale invariance blocks actor bootstrap (DreamerV3 stabilisation lens) -- the THIRD route, belongs to neither live discrimination leg
    - MECH-460  # CDQ-007 competence-bootstrap candidate: transient behavioural-prior seed (VPT/AlphaStar/DQfD/JSRL) -- informs the H-bc-prior leg (V3-EXQ-780)
    - MECH-461  # CDQ-007 competence-bootstrap candidate: innate action-primitive basis + reward-independent engagement drive (biology lit-pull) -- informs the H-approach-primitive leg (V3-EXQ-781)
  related_claims:
    - MECH-475   # CHILD (split_from MECH-457, /claim-synthesis 2026-07-22): uninformative value baseline makes added optimisation iatrogenic; owns the destructiveness-not-plateau reading across 769/781/771/780/789
    - MECH-476   # CHILD (split_from MECH-457, /claim-synthesis 2026-07-22): acquisition and retention are dissociable capabilities; owns the consolidation-as-interference-resistance reading (dose x offline-interval)
  functional_restatement: >
    The V3-EXQ-724 competence-localization diagnostic held REE's foraging deficit
    invariant across P1 training budget, encoder-freeze, and mechanism count (and
    their combination -- all sub-floor while a privileged oracle clears the 1.0
    floor). The failure autopsy (failure_autopsy_V3-EXQ-724_2026-07-09) localized the
    deficit to the ONE invariant 724 never varied: REE learns action ONLY through a
    thin bias_head REINFORCE over representations trained for prediction (SD-056 e2
    world-forward contrastive), with no first-class policy/action-learning substrate.

    MECH-457 names the missing system. Biological action learning is a dedicated
    learning system, not a bias term on a prediction model: dorsal striatum acts as
    an ACTOR, dissociable from the ventral-striatal value-prediction CRITIC
    (O'Doherty et al. 2004), taught by a dopaminergic reward-prediction-error signal
    (Schultz, Dayan & Montague 1997), with direct/indirect-pathway (D1/D2) opponent
    credit assignment available as a later refinement (Collins & Frank 2014 OpAL).
    The ML statement of the same point: competent control comes from directly
    optimizing a dedicated, separately-parameterized policy on expected return with a
    value baseline (the policy-gradient theorem / actor-critic, Sutton et al. 2000),
    NOT from reading a policy off a value/prediction model -- the latter proved
    intractable under function approximation. A frozen self-supervised (contrastive)
    encoder CAN support competent control, but only with a policy still trained by RL
    on reward on top of it (Stooke et al. 2021, decoupled representation/policy) --
    which is precisely the object REE lacks.

    Falsifiable (the H1 discriminator, V3-EXQ-737, queued SEPARATELY via
    /queue-experiment): a real trainable policy/actor head + value baseline over
    REE's z_world, trained by PPO/actor-critic, DV = foraging competence crossing the
    1.0/episode floor. If it clears the floor, the bias-head-over-prediction was the
    bottleneck (H1, this claim's mechanism) -> build the substrate under
    f_dominance_conversion_ceiling. If it fails while a non-REE vanilla learner on the
    identical observation ALSO fails, the target is the observation encoding, not the
    policy (H2) -- which would WEAKEN this claim. V3-EXQ-738 (2026-07-10) already
    refuted the strong-H2 read: the 1.0 floor IS reachable from the agent's 5x5 local
    view, leaving H1 as the surviving hypothesis pending 737.

    RETENTION SUB-QUESTION DECIDED 2026-07-20 (V3-EXQ-788 PASS + V3-EXQ-789 FAIL read
    jointly; confirmed failure_autopsy_mech457-retention-portfolio_2026-07-20). STATUS
    UNCHANGED (candidate, v3_pending) -- the V3-pending gate holds regardless of this
    result. THE VALUE ESTIMATOR SETS RETENTION; THE IMITATION AUXILIARY DOES NOT.
    V3-EXQ-788 (clean PASS, no adjudication flag): a distributional critic RETAINS 1.839
    of installed competence. V3-EXQ-789 (FAIL, direction weakens): the auxiliary's
    persistence schedule (constant 0.5 / annealed 0.5->0.0 / off) succeeded then decayed,
    with use_distributional_critic held False on all arms as an anti-alias control.
    Readiness was clean on 789 -- post-BC install took on 3/3 seeds, worst 17.75 against a
    1.0 floor; local-view greedy 48.05; oracle 57.2; non_degenerate true. The two legs
    share the reference build and are genuinely independent in locus, so they corroborate
    rather than merely partition. recommended_substrate_queue_entry.action = none on both:
    the axis is closed BY EVIDENCE, not by a substrate gap.
    HYPOTHESIS-SPACE LEDGER: H-retention-auxiliary-decay ELIMINATED under qid
    competence_floor. Further auxiliary-schedule iteration is refuted -- the 788 critic
    result supersedes that lever.
    CORRECTION 2026-07-20 (same cycle, user-flagged): an earlier draft of this note
    recorded an "open routing decision" about whether the mech457_policy_kl_anchor leg
    still warranted a run. THERE IS NO SUCH DECISION -- the note was wrong on two counts
    and is retracted here rather than silently edited.
    (1) SCOPE ERROR. The autopsy's supersession is narrow and says only that the 788 critic
    result "supersedes the AUXILIARY LEVER, so a further schedule iteration would optimise a
    lever shown to be the wrong one" -- i.e. it refuses a re-run of 789's schedule axis. It
    makes no claim about the KL-anchor leg. The frozen ledger agrees: of the four retention
    legs, ONLY H-retention-auxiliary-decay is eliminated; H-retention-critic,
    H-retention-consolidation and H-consummation-binding all remain ALIVE.
    (2) STATE ERROR. The leg is neither unbuilt nor unqueued. The substrate landed at
    ree-v3 399b17caed and the leg is queued as V3-EXQ-792 (status pending, script
    experiments/v3_exq_792_mech457_retention_consolidation.py, testing
    H-retention-consolidation as the third and final GOV-FANOUT-1 retention leg alongside
    V3-EXQ-788). Nothing was awaiting a decision.
    THE PRINCIPLE, WHICH GENERALISES BEYOND THIS LEG: cancelling a portfolio leg because a
    SIBLING leg returned positive is exactly the move GOV-FANOUT-1 exists to prevent. A
    distributional critic that retains competence does not establish that a trust-region
    anchor does not; the legs are independent in locus by design, which is why the portfolio
    was diverse in the first place. Nor is "retention probe" opposed to "trust-region
    mechanism" -- V3-EXQ-792 is a retention leg that operates VIA a trust-region anchor.
    A leg is eliminated by ITS OWN adjudicated result, never by a sibling's.
    THE H1 DISCRIMINATOR RAN, AND ITS READING IS TWO-PART (V3-EXQ-737, confirmed
    failure_autopsy_V3-EXQ-737a_2026-07-20; claim_ids empty, diagnostic, brake_exempt, so
    it carries NO evidence weight on this claim and does not move its confidence).
    (1) The D3 bottleneck is POLICY LEARNING, not representation: ppo_raw_obs -- the
    control arm, structurally unaffected by the encoder defect -- reaches only 0.567
    against the 1.0 floor on an environment where local-view greedy achieves 48.05 from
    the SAME view and the oracle reaches 57.2. That is the direction this claim predicts.
    (2) BUT THE RUN IS NOT A CLEAN H1 CONFIRMATION, because it also found a substrate
    defect: in the x734/737 driver family z_world is never prediction-trained -- the P0/P1
    warmup has no optimizer group covering latent_stack (0 of 61 tensors), so z_world is a
    frozen random projection. The REE-latent arms therefore did not test what they were
    built to test. Retest owed after sd_zworld_warmup_optimizer_group lands
    (pending_retest_after_substrate true on the autopsy target). Do NOT record H1 as
    confirmed on the strength of 737 alone.

    GOV-FROZEN-1 FAN-OUT RECURRENCE, AND THE AXIS-FAMILY READING (2026-07-20 governance;
    ROUTING ONLY -- promotes nothing, demotes nothing, gates nothing). This claim's
    question, qid competence_floor, has now reached N=3 distinct labelled fan-out
    portfolios (failure_autopsy_V3-EXQ-769_2026-07-17,
    failure_autopsy_MECH-457-fanout-770-771-772_2026-07-18,
    mech457_retention_portfolio_2026-07-18), denominator 7 -> 16, with 3 legs still alive.
    Every portfolio individually cleared GOV-FROZEN-1 conditions (a)-(c) -- the RECURRENCE
    is the signal, and the reading is that the question may be MIS-POSED rather than
    under-enumerated.
    THE AXIS-FAMILY DISCRIMINATOR AGREES, AND THAT IS THE PART TO ACT ON.
    hypothesis_space.v1.json reports convergence_class CIRCLING for this question --
    "a growth event added ONLY legs re-entering already-eliminated families" -- with
    families_closed [instrumentation, representation, world] and, decisively,
    families_fresh EMPTY. Sixteen legs have produced no fresh territory. Per GOV-FROZEN-1
    the response is to RE-POSE the operationalization, not to open portfolio 4:
    enumerating another round of rivals on an unchanged framing is the denominator-side
    twin of re-running a braked experiment harder. Report the narrowing ratio BOTH ways
    (surviving/original AND surviving/current-including-fan-out), never the flattering one.
    RE-POSE MUST START BY RE-EXAMINING THE CLOSED FAMILIES, NOT BY ADDING LEGS. The
    "representation" and "world" families are recorded CLOSED, but V3-EXQ-737 (above) found
    that in the x734/737 driver family z_world was never prediction-trained -- a frozen
    random projection. If the representation arm was structurally broken at the time those
    families were closed, then part of what the discriminator reads as circling may be a
    family closed on a DEFECT rather than on evidence. Check that before treating the
    closure list as settled; the resolution is owed to sd_zworld_warmup_optimizer_group.

    [2026-07-30 governance / confirmed failure_autopsy_V3-EXQ-819a_2026-07-30 +
    failure_autopsy_batch-687a-707c-840-748a-833-842-810b-673-614-798afail_2026-07-30]: the
    "representation family closed on a defect" concern raised immediately above is now
    RESOLVED. V3-EXQ-819a (instrument-validity diagnostic, claim_ids scoring-excluded) confirms
    a prediction-trained z_world beats a frozen-random projection on installed foraging
    competence (paired AUC delta +0.626, 4/6 seeds clear a 0.3 margin, non-degenerate) --
    resolving the H-zworld-trained-instrument hypothesis-space leg. V3-EXQ-748a then re-ran
    748's dense-teacher/z_world-BC arm under this now-validated zworld_p0-trained instrument:
    H-rep's 2026-07-13 elimination HOLDS (supports) -- the earlier elimination was NOT an
    artefact of the frozen-random-projection defect. Neither run promotes or demotes MECH-457;
    both are instrument-validity/confirmatory. The "representation" family in the GOV-FROZEN-1
    fan-out reading above stays validly closed.

    [2026-08-08 governance, V3-EXQ-821b, confirmed failure_autopsy_V3-EXQ-821b_2026-08-08]:
    calibrated re-run (BC dose 1200, post_bc worst 20.95 >= USABLE_INSTALL_FLOOR 10.0 on both
    arms) of the H-consummation-binding competence_floor retention leg. Both the extinguishing
    and non-extinguishing arms erode installed competence to retained_fraction 0.0 on all 3
    seeds (margin on-off = 0.0 < 0.15 required). H-consummation-binding ELIMINATED (re-confirms
    the pre-instrument-fix V3-EXQ-821 reading, now on a validated instrument; full elimination
    bar met -- resolved in hypothesis_space_registry.v1.json by the autopsy's own Step 9b). Both
    arms replicate V3-EXQ-781's independent "approach-earned/forage-suppressed" signature
    regardless of extinction -- the deficit is upstream of the consummatory act; the approach
    drive's fixed reward weighting (untested as an axis here) is the more plausible proximate
    driver of total erosion. MECH-457 itself is NEITHER supported nor weakened by this diagnostic
    leg -- status stays candidate/v3_pending. This converges the competence_floor retention
    sub-question fully: of the 4 originally-named retention mechanisms, 2 process-family loci
    (value estimator, update constraint) are CONFIRMED and 2 constitution-family loci
    (auxiliary-decay, consummation-binding) are ELIMINATED. Any follow-on question about
    approach-drive reward magnitude is explicitly OUT OF SCOPE for the competence_floor qid per
    its growth_restriction -- needs its own qid, not chipped from this note.
  location: evidence/literature/targeted_review_actor_critic_action_learning/

- id: MECH-458
  title: "Curiosity is a reward-conditional EXPLOITATION amplifier, not a diversity generator: the SD-025 hippocampal curiosity / density-attraction drive pulls only toward representational structure that reward (SD-024/MECH-232 DA-expansion) has ALREADY sculpted -- a rich-get-richer loop with zero PROACTIVE pull toward unshaped / novel / under-represented regions. Its only diversity affordance is a weak, lagging, REACTIVE familiarity-discount brake (~half the strength of the exploitation force it opposes, and zero at the decision point). COROLLARY: proactive strategy-diversity GENERATION requires a SEPARATE rarity-seeking drive (Bellemare-2016 polarity: attraction to LOW-count / under-represented strategy classes, independent of prior reward-shaping), NOT a novelty-magnitude increase on the existing drive; and any such drive is ORDERING-GATED on INV-088 z_world differentiation (a rarity term over an under-differentiated map has nothing to range over)."
  claim_type: mechanism_hypothesis
  subject: hippocampal_module.curiosity_drive.exploitation_polarity
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-17
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  claim_level: mechanistic
  registered_utc: "2026-07-17"
  depends_on:
    - SD-025    # the curiosity/density-attraction drive whose polarity this claim characterizes
    - SD-024    # DA-expansion -- the reward-shaping the drive is parasitic on (768a: flat-map -> zero)
    - ARC-057   # approach-emergence: the SAME drive is correct THERE (provisional 2026-07-17); this claim scopes the DIVERSITY side ARC-057 never claimed
    - INV-088   # z_world differentiation -- the ORDERING gate for the rarity-seeking generation drive the corollary prescribes
  location: docs/architecture/sd_024_da_modulated_rbf_density.md#curiosity-exploitation-polarity-mech-458
  functional_restatement: >
    The SD-025 curiosity drive that makes reward-approach EMERGE (ARC-057) is
    structurally the WRONG force for strategy-diversity GENERATION -- a category
    limit, not a parameter to tune. Two cloud PASSes decompose the drive on the
    identical CEM score-margin scale (re-analysis, no new compute):

    (1) V3-EXQ-767a (map IS reward-shaped): density-attraction at selection = 39.3
    vs the familiarity-discount CEILING (after 12 forced visits) = 20.4 (1.93x),
    and the diversity term contributes 0.00 AT THE DECISION POINT (familiarity
    starts at 0). The drive's first move is 100% exploitation; its only diversity
    affordance is a lagging reactive "leave after you've exploited" brake -- Bellemare's
    proactive low-count bonus INVERTED.

    (2) V3-EXQ-768a (map NOT reward-shaped): SD-025-alone on a flat map = 0 directed
    behaviour; the density-attraction force is 100% parasitic on dopamine (SD-024)
    having already sculpted the map. Together: curiosity pulls ONLY toward structure
    reward has already built (rich-get-richer); it has no proactive pull toward
    unshaped / under-represented regions -- exactly the flat-map case where it outputs
    zero. This explains the stuck v3 mass (conversion_ceiling 0%, competence floor,
    monostrategy): a GENERATION drive biased toward the already-rewarding region
    under-serves per-candidate strategy diversity.

    Biological / ML grounding (targeted_review_arc_057, NON-load-bearing per the
    lit/exp decoupling rule): Bellemare 2016 (pseudo-counts) -- a density-based
    intrinsic drive DOES produce directed navigation, but the bonus is HIGH for
    low-count / rarely-visited states = the OPPOSITE polarity on "density" from
    SD-025's density-attraction, and the precise vocabulary for what the generation
    drive is missing. Stachenfeld 2017 (predictive map) -- approach is read off map
    STRUCTURE, so a rarity-seeking drive needs a differentiated map to range over
    (the INV-088 ordering gate). Pathak 2017 (ICM) -- curiosity-alone yields directed
    approach only in informationally RICH envs; REE's grid world is flat, so all
    structure comes from internal DA-expansion (SD-024).
  what_would_answer: >
    ONCE INV-088 z_world differentiation is cleared (the ordering gate), build a
    proactive rarity-seeking generation drive on ARC-065/MECH-314 (Bellemare polarity:
    attraction to low-count / under-represented strategy classes or z_world regions,
    independent of reward-shaping) and run a discriminative arm against (a) OFF and
    (b) a pure novelty-MAGNITUDE sweep of the existing SD-025 drive (the
    infant_substrate:GAP-13 lever). PRIMARY DV = per-candidate / committed-class
    strategy diversity on an UNSHAPED (flat / not-yet-reward-sculpted) map. SUPPORTS
    the claim iff the rarity-seeking arm supplies diversity pull comparable to the
    ~39-unit exploitation force AT THE DECISION POINT (where SD-025 supplies 0) AND the
    novelty-magnitude sweep does NOT (768a predicts the flat-map arm reads ~0 regardless
    of weight). WEAKENS iff a magnitude sweep of the existing density-attraction drive
    alone lifts flat-map diversity (would refute the polarity/category claim). Not
    runnable before INV-088 (a rarity term over an AUC-0.83 under-differentiated map
    chases the sparse corner, not diverse strategies) -- so this is a build-gated
    prediction, not a queued falsifier.
  evidence_quality_note: >
    2026-07-17 registration (source: evidence/planning/curiosity_exploitation_amplifier_reframe_2026-07-17.md,
    registered as hypothesis 2026-07-17; anchored by V3-EXQ-767a + V3-EXQ-768a, both
    cloud PASS non-degenerate, + the re-analysis probe probe1_sd025_force_decomposition.py).
    The DESCRIPTIVE polarity half is evidence-anchored (767a force decomposition + 768a
    2x2 interaction, robust across 8 seeds x 2 experiments); the force MAGNITUDES are
    SD-025 diagnostic-geometry-specific (counterbalanced 2-region test-bed, not a full
    behavioural env) while the DIRECTIONS (exploitation >> diversity; diversity = 0 at
    selection; curiosity-alone-on-flat = 0) are robust. The FIX corollary (rarity-seeking
    is the missing GENERATION drive) is a v3_pending PREDICTION, not yet tested -- its
    build is blocked-on-upstream INV-088 z_world differentiation. Registered candidate;
    PROMOTES/DEMOTES NOTHING (767a/768a are diagnostic, excluded from confidence scoring;
    the registration is the metabolization action, not a confidence increment). Lit
    NON-load-bearing (decoupling rule). Relationship to siblings: does NOT rest on / is
    NOT rested on by ARC-057's 768a promotion (ARC-057 is approach-side scoped and
    explicitly excludes the diversity side this claim covers); consistent with MECH-457's
    refutation of the explore/exploit-ARBITRATION axis (V3-EXQ-755) -- the drive is the
    wrong KIND, not mis-arbitrated. Re-scopes conversion_ceiling_campaign (adds a
    GENERATION face; see conversion_ceiling_campaign_plan.md :GENERATION node) and routes
    the ARC-065/MECH-314 build (arc_062_rule_apprehension:GAP-H /
    behavioral_diversity_isolation) as rarity-seeking, ordering-gated on INV-088.
  notes: >
    Premise-challenge cross-refs already on conversion_ceiling_campaign_plan.md and
    behavioral_diversity_isolation_plan.md (commit e00047c17d). This claim is the
    registered form of that reframe. The rarity-seeking build itself is NOT queued here --
    it is blocked-on-upstream INV-088; this registration + the campaign GENERATION-face
    re-scope are the metabolization step. See [[project_curiosity_exploitation_amplifier_reframe]],
    [[project_monostrategy_representation_ceiling_root]] (differentiate-first),
    [[project_mech457_competence_floor_coldstart]], [[project_conversion_ceiling_four_roots]].

- id: MECH-459
  title: "return_scale_invariance_blocks_actor_bootstrap / one_sided_return_normalisation. REE's MECH-457 actor-critic update stacks TWO two-sided normalisers -- a Welford RUNNING reward std (r / running_std) followed by a per-episode advantage standardisation ((adv - mean) / (std + 1e-8)) -- which together form a SCALE-INVARIANCE OPERATOR: after it the policy gradient depends only on the SHAPE of the within-episode advantage vector and is exactly invariant to any multiplicative rescaling. This is sufficient to produce converter invariance to every MAGNITUDE lever by construction (the reward-coupling and drive-schedule axes eliminated by V3-EXQ-771 and 770), and it converts a zero-forage episode -- whose reward stream is harm_signal + novelty_bonus only, so whose advantage vector is pure critic error plus exploration jitter -- into a FULL-MAGNITUDE unit-variance noise gradient. DreamerV3 (Hafner 2023) names this exact failure ('amplification of near-zero returns', Appendix E) and prescribes the corrective: a STATIONARY magnitude compressor (symlog, explicitly against running-statistics normalisation, which injects non-stationarity because the target moves when the normaliser moves), a ONE-SIDED percentile normaliser adv / max(1, Per95 - Per5) that scales large returns down and never scales small returns up, and a DISTRIBUTIONAL twohot/cross-entropy critic in place of a scalar MSE head (which under a bimodal sparse-return distribution converges to the never-observed MEAN of the two modes, and whose gradient is linear in the error rather than bounded)."
  claim_type: mechanism_hypothesis
  subject: f_dominance_conversion_ceiling.return_pathway_stabilisation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-20
    needs_review: false
    evidence:
      from: "failure_autopsy_MECH-457-gov-fanout-1-cluster-780-781-782_2026-07-18#V3-EXQ-782"
      as_of: 2026-07-18
      verdict: weakens/standard
  implementation_phase: v3
  v3_pending: true
  generation: v3
  epistemic_category: standard
  claim_level: mechanistic
  registered_utc: "2026-07-18"
  depends_on:
    - SD-056    # e2 world-forward contrastive encoder -- the representation the converter rides
  location: docs/architecture/competence_bootstrap_mechanisms.md#3-mech-459----return-scale-invariance-blocks-actor-bootstrap
  functional_restatement: >
    The MECH-457 converter's defining signature is not that it fails, but that it fails
    INVARIANTLY -- four axes eliminated (capacity 769, drive-schedule 770, reward-coupling 771,
    credit-horizon 772) with every treatment arm at the ~0-1 foraging floor despite readiness
    met on every leg (local_view_greedy 48-55, oracle 57-61). This claim proposes that the
    invariance is a PROPERTY OF THE MEASURING INSTRUMENT rather than a fact about the mechanisms
    tested: a two-sided self-normalising advantage pathway divides magnitude levers out by
    construction, so those axes could not have moved the floor whatever their true effect.
    The corollary is that the eliminations are weaker evidence than they appear -- they
    constrain the mechanisms only conditional on the normaliser not having masked them.
  what_would_answer: >
    A SHAPE-PRESERVING NORMALISER KNOCK-OUT: replace ONLY the per-episode
    (adv - mean) / (std + 1e-8) at experiments/_lib/mech457_fanout.py::train_rawview_ac_rl
    with adv / max(1, Per95(adv) - Per5(adv)), everything else byte-identical, and measure
    foraging competence. SUPPORTED if the floor moves. FALSIFIED if it does not -- the
    two-sided normaliser is then exonerated and the weaker noise-re-amplification form falls
    with it. Two supporting readouts, both cheap and both on intermediate quantities:
    (a) advantage COMPOSITION logged BEFORE standardisation (fraction of |adv| mass on
    forage-contact steps vs novelty/harm steps) -- this claim predicts the forage fraction is
    tiny pre-standardisation and rescaled to parity post-standardisation, whereas a
    bootstrap-signal-absence account predicts it stays tiny in the gradient; (b) CRITIC
    CALIBRATION on BC-visited states (BC reaches 32.72, so its state distribution contains
    genuinely high-return states) -- the MSE/bimodal-collapse half predicts the critic assigns
    them a value near the mean of the bimodal return distribution, failing to separate
    pre-reward from post-reward states, where signal-absence predicts a flat critic everywhere.
  evidence_quality_note: >
    HONEST COUNTERWEIGHT, recorded rather than suppressed: V3-EXQ-772's potential-based shaping
    (F = gamma*Phi(s') - Phi(s)) changes advantage SHAPE, not merely scale, so a pure
    scale-invariance operator does NOT divide it out -- and 772 still floored. This is direct
    strain on the narrowest form of the claim. It survives only in the weaker form that the
    shape change is swamped because per-episode standardisation re-amplifies the co-present
    novelty noise (NOVELTY_COEF 0.1/sqrt(visit_count), which fires on EVERY step and is the
    dominant contributor to reward_std in a ~0-forage episode) back to unit variance alongside
    it. The falsifier above tests that weaker form. Separately: the DreamerV3 ablations that
    motivate the corrective are reported at SUITE level, not on the Minecraft-diamond task
    itself, so 'these ingredients are load-bearing for the no-demonstration result' is the
    paper's ATTRIBUTION (suite ablations plus the fixed-hyperparameter argument), not a
    measured per-ingredient causal decomposition. Confidence is therefore mechanism-plausibility,
    not transfer-demonstrated.

    [2026-07-20 governance / intra-run substrate-divergence sweep sec 7 item 4] The intra-run
    substrate-divergence hit (defect class D3) recorded against V3-EXQ-782 is WITHDRAWN.
    REFUTED at rung (a), glob scope, by failure_autopsy_V3-EXQ-782_2026-07-20 (REE_assembly
    2c6826d3e3): the closure-restricted fingerprint is identical in all four bands, and the three
    files that changed mid-run are on no import path reachable from the driver. The divergent
    whole-glob hash is a disk-state recording artefact, not a change in executed code. The
    `weakens` reading STANDS unchanged and no confidence is re-weighted. Recorded so the flag
    does not resurface as unexplained doubt in a later sweep.

    [2026-08-01 governance, MECH-476 WITHDRAWN, folded in here -- full detail on MECH-476's own
    evidence_quality_note, not duplicated]: MECH-476's three pre-registered falsifier arms
    (dose/836a, novelty-tagging/836d, interval/836e, all confirmed weakens) found retention
    invariant to dose, interval, and novelty-pairing alike -- consistent with a constant
    regulariser (this claim's return-scale-invariance / one-sided normalisation mechanism, plus
    MECH-460's transient behavioural-prior bootstrap), not a dedicated consolidation process.
    MECH-476's own pre-registration named this exact outcome as its withdrawal condition. This
    claim (MECH-459) is UNCHANGED by the fold-in -- MECH-476 leaned on the independently-
    established regulariser-based retention this claim already characterises; the withdrawal
    adds no new obligation here, only removes a claim that would have duplicated it.
  notes: >
    SCOPE EXCLUSION -- the structural half is deliberately NOT in this claim. DreamerV3 also
    trains its actor ENTIRELY on imagined 15-step rollouts with lambda-returns, so gradient-signal
    DENSITY is set by the world model rather than by the environment's reward density. REE trains
    on REAL env.step calls only; SD-056's e2 is a contrastive REPRESENTATION, not a rollout source,
    and there is no seam for an imagined update. That is a STRUCTURAL difference, not a tuning one,
    and it is carried instead as convergence probe DREAMER-V3-P-008: roll e2 forward open-loop on
    logged trajectories and measure predicted-vs-actual reward/continuation divergence by horizon
    step. That probe is complex (probe-gated) -> puzzle (known rules) -- the missing fact is
    measurable WITHOUT building the update.
    SCOPE EXCLUSION CONFIRMED 2026-07-18 -- DREAMER-V3-P-008 answered (REE_convergence/sources/
    dreamer-v3/source.yaml; write-up in that source's actor_critic_stabilisation.md "Probe
    DREAMER-V3-P-008 -- result"). The exclusion STANDS and is now measured rather than asserted,
    but its stated REASON was imprecise and is corrected here. e2 is NOT merely "a contrastive
    REPRESENTATION, not a rollout source": E2FastPredictor.world_forward (e2_fast.py:201-222) IS a
    deterministic generative residual next-latent map in the same space as its input, and
    multi-step open-loop rollout machinery already exists (rollout_with_world, e2_fast.py:657-757).
    SD-056 is an auxiliary InfoNCE loss OVER that generative model's predictions, not a
    discriminator. The structural gap is real but sits elsewhere, in three independent places, each
    sufficient alone: (i) world_forward is NOT TRAINED as a world model -- AgentV3.compute_e2_loss
    (agent.py:9118-9134) trains only predict_next_self (z_self); the z_world map gets no gradient
    from the substrate objective, its only shaping is the SD-056 term which is default OFF
    (config.py:388) and read by no ree_core call site, and it is consumed single-step under no_grad
    for attribution only; (ii) NO CONTINUATION HEAD exists over latents anywhere in ree_core (the
    sole termination head, e2_escape_affordance_linker.py:165, predicts THREAT termination, not
    episode done), so lambda-returns have no continuation substrate -- though a reward COULD be
    read off an imagined latent, since E3Selector.harm_eval/benefit_eval and the ActorCriticNet
    value head all read z_world directly; (iii) MEASURED open-loop divergence: untrained (= the
    default substrate state) diverges immediately, rel err 0.69 at k=1 to 17.8 at k=15, 28x-254x
    worse than a do-nothing persistence baseline; and even after 2240 deliberate MSE updates (an
    objective the substrate does not run) it NEVER BEATS PERSISTENCE AT ANY HORIZON -- 1.21x worse
    at k=1 widening to 2.15x at k=15, despite superficially healthy absolutes (cos 0.988, rel err
    0.151 at k=15). LIMITING CAVEAT: z_world is near-static over the measured trajectories (mean
    one-step movement 2.0% of norm; e2's k=15 error 0.0698 EXCEEDS the whole-dataset inter-state
    spread 0.0522), so the probe cannot separate "e2 fails to model the dynamics" from "z_world has
    almost no dynamics to model" -- the latter upstream-limits any forward model on z_world. Either
    way a lambda-return over these rollouts would be degenerate.
    BEARING ON THIS CLAIM: the probe SHARPENS MECH-459 BY EXCLUSION and adds no support to it. It
    establishes that the imagination route is materially more expensive than the normaliser
    corrective this claim proposes -- it would require a training objective for world_forward, a
    continuation head, AND a z_world that actually moves, before a DreamerV3-style update could be
    written at all. It does NOT bear on whether the two-sided normaliser is the floor's cause; the
    what_would_answer falsifier above is unaffected and remains the discriminating test. It also
    opens a NEW complex (probe-gated) node, deliberately not pursued: whether z_world's near-static
    character is a property of the encoder, of the D3_hazard_free rung, or of the low-competence
    trajectory distribution -- which bears on the monostrategy representation-ceiling thread
    (INV-088), NOT on this claim. The probe was a measurement only: no ree_core change, no queued
    experiment, no imagined-rollout update built. It does NOT adjudicate GOV-FANOUT-1 (V3-EXQ-780
    vs 781) and must not be cited for either leg; mech457_competence_bootstrap_explorer stays
    blocked_pending_discrimination.
    COORDINATION -- DOES NOT PRE-EMPT THE LIVE DISCRIMINATION. The GOV-FANOUT-1 portfolio
    (H-bc-prior V3-EXQ-780 vs H-approach-primitive V3-EXQ-781) is pre-registered ALIVE in the
    competence_floor question of hypothesis_space_registry.v1.json. MECH-459 belongs to NEITHER
    leg: it is a THIRD route (demonstrator-free convergence via value-pathway stabilisation, with
    neither an imitation seed nor an innate drive), so it WIDENS the hypothesis space rather than
    narrowing it. Whether competence_floor should gain a third ALIVE leg is a /governance decision
    under the GOV-FANOUT-1 growth contract and is deliberately left to that process -- this
    registration does NOT amend the registry. The substrate_queue entry
    mech457_competence_bootstrap_explorer stays blocked_pending_discrimination. PROMOTES NOTHING;
    LICENSES NO BUILD (not the twohot critic, not the percentile normaliser, not an imagination
    loop). The re-derive brake is not engaged by this claim: it refuses further config/env/credit/
    capacity RE-QUEUES, and this is a different mechanism class (the normalisation pathway itself)
    registered as a claim, not queued as an experiment.
    NOTE ON THE 32.72 BC EXISTENCE PROOF: behavioural cloning is a supervised cross-entropy update
    that never touches the return pathway at all, so the one method that clears the floor is
    exactly the one that bypasses the value/advantage machinery. That is CONSISTENT with this
    claim -- and equally consistent with MECH-460 and MECH-461. BC clearing the floor does NOT
    discriminate among the three, which is why every falsifier here is on an INTERMEDIATE
    quantity rather than on final foraging competence.
    ADJUDICATED 2026-07-18 -- evidence/decisions/decision_MECH-459_registry_and_brake_2026-07-18.md.
    The claim is NARROWED, not withdrawn. (1) The operator analysis is CORRECT: invariance to a
    GLOBAL multiplicative reward rescaling holds to within REWARD_STD_EPS (the Welford running std
    alone suffices -- scaled rewards, GAE advantages, returns and the value loss are all unchanged
    under r -> c*r). (2) But the STRONG FORM's inferential step -- that the 770/771 eliminations
    were divided out BY CONSTRUCTION -- is REFUTED and must not be restated. None of the three legs
    is a global rescaling: 771 is a metabolic forage-to-survive ENVIRONMENT change (termination
    structure + state distribution); 772 is potential-based shaping (already conceded); and 770
    varies intrinsic_coef on ONE reward component (relative reweighting = shape) while ALSO holding
    entropy_beta constant at 0.10 where the control anneals 0.10 -> 0.03 -- and beta_eff multiplies
    the LOSS (mech457_explorer_classes.py:698), entirely OUTSIDE the normaliser. So 770 moved a
    lever the operator provably does not divide out. The 772 counterweight GENERALISES to 770/771.
    (3) SURVIVING form is the weak one only: per-episode standardisation re-amplifies co-present
    novelty noise back to unit variance and SWAMPS the shape change. That is an empirical
    noise-dominance claim, not an architectural one.
    LOCUS -- the two-sided normaliser is NOT confined to the cited train_rawview_ac_rl. Verified
    sites: mech457_fanout.py:244/253 and :411/420, mech457_explorer_classes.py:688/697 and :941/950.
    V3-EXQ-770/771 run the composed bootstrap in explorer_classes, not fanout, so the citation is
    under-inclusive but in-scope.
    COROLLARY (Finding V3, decision-relevant to the LIVE portfolio) -- the two queued legs are
    ASYMMETRICALLY exposed. V3-EXQ-780 (H-bc-prior) adds bc_aux_coef*bc_loss as a LOSS-side CE term
    (explorer_classes.py:703-708); it never enters `shaped`, so the normaliser cannot swamp it --
    780 is NOT confounded. V3-EXQ-781 (H-approach-primitive) adds approach_coef*approach_drive
    directly into `shaped` (explorer_classes.py:660-662), INSIDE the normaliser -- so a 781 NULL is
    confounded by this claim while a 781 POSITIVE is not. Read a 781 null with that caveat.
    GOVERNANCE OUTCOME -- (D1) competence_floor does NOT gain a third ALIVE leg. In its surviving
    weak form this claim is an INSTRUMENT-VALIDITY claim about the question's evidence base, not a
    rival ANSWER to its decision_question, so registering it as a leg would inflate the denominator
    without ever being able to reduce the numerator. Registry unwritten; counts stay 7 (original) /
    12 (current); surviving-ratio reported both ways 2/7 = 0.286 and 2/12 = 0.167. Trigger armed:
    it graduates to hid H-return-scale (axis normalisation-pathway) under a labelled
    fanout_growth_events[] entry IFF probe R-(a) returns the parity-rescaling signature. Probe R is
    a diagnostic on intermediate quantities and is explicitly NOT the adjudicating run for that leg,
    so later registration satisfies the pre_registered_utc <= resolved_utc invariant.
    (D2) The re-derive brake PERMITS the shape-preserving knock-out -- it changes the update
    OPERATOR (not an input to a fixed rule), carries different claim_ids (MECH-459 brake count 0),
    and declares a symmetric falsifier. But the knock-out is NOT the first move: it tests only the
    now-weakened form and a null is compatible with signal-absence too. Sequence = cheap DIAGNOSTIC
    probe R first (R-(a) advantage composition pre- vs post-standardisation; R-(b) critic
    calibration on BC-visited states), new EXQ via /queue-experiment, governance-scoring-excluded.
    The knock-out (probe K) is sanctioned but HELD pending R-(a); if R-(a) shows the forage fraction
    stays tiny post-standardisation, the weak form falls, probe K is NOT queued, and this claim goes
    to `weakened`. V3-EXQ-780/781 unmodified; substrate_queue entry stays
    blocked_pending_discrimination.
    QUEUED 2026-07-18 as V3-EXQ-782 (v3_exq_782_mech459_advantage_composition_probe,
    experiment_purpose=diagnostic, governance-scoring-excluded) -- probe R is LIVE, do NOT re-queue
    it. Instrumentation is a SEPARATE mirror module ree-v3/experiments/_lib/mech459_probe_r.py, so
    the mech457_* _lib files and the running 780/781 are byte-unchanged. It measures the reference
    composed-bootstrap CTRL path (the shared control arm of 780/781), both reps, no treatment arm.
    R-(a) is prevalence-normalised to a CONCENTRATION C = (forage |adv| mass fraction) / (forage
    step fraction), so rarity alone cannot manufacture a "tiny" reading: parity-rescaling =
    C_pre<0.5 AND C_post>=1.0 AND delta>=0.25; stays-tiny = C_post<1.0 with delta<0.25;
    already-concentrated = C_pre>=0.5 (the weak form's PREMISE fails -- a third outcome the
    adjudication did not enumerate). ANALYTIC CONSTRAINT recorded in the script: the std divide is
    provably |adv|-fraction-preserving, so ONLY the mean subtraction can move the composition --
    the parity signature therefore requires a large negative episode-mean advantage relative to
    forage-step advantages, and mean_episode_mean_adv is emitted as the audit of that mechanism.
    The run self-routes all three branches plus headline.probe_k_recommended,
    headline.hypothesis_registry_action and headline.exq_781_null_readable (the Finding V3
    corollary), so the routing above is machine-readable off the manifest.
    ADJUDICATED 2026-07-19 -- V3-EXQ-782 RAN and WEAKENS the surviving WEAK form
    (failure_autopsy_MECH-457-gov-fanout-1-cluster-780-781-782_2026-07-18, routing
    governance-weaken-only). The branch that fired is the THIRD outcome the sanctioning
    adjudication did not enumerate: ALREADY-CONCENTRATED. R-(a) finds forage-contact |adv| mass
    on the reference composed-bootstrap CTRL path already concentrated BEFORE the per-episode
    standardiser -- C_pre = 1.92 (z_world) / 1.20 (raw_view) against the 0.5 "tiny" threshold --
    so the weak form's PREMISE (a tiny forage signal that novelty-noise re-amplification could
    swamp) FAILS. The standardiser slightly REDUCES concentration (delta -0.343 z_world / -0.141
    raw) rather than swamping it. Readiness met on all four preconditions (local_view_greedy
    48.05, greedy_oracle 57.2, 273 forage steps vs a 30 floor, demo return std 0.3027 vs a 0.25
    floor); non_degenerate True. Per the probe's own pre-declared conditional this queues NO
    probe K and makes NO hypothesis-registry write (competence_floor counts unchanged).
    STATUS DECISION (governance 2026-07-19, user-adjudicated): the pre-declaration above says
    "this claim goes to `weakened`", but `weakened` is NOT a value in the claims.yaml status
    enum (zero occurrences across 894 claims), and the autopsy's own operative line reads
    "MECH-459 stays candidate/v3_pending". RESOLVED in favour of the autopsy: status stays
    candidate / v3_pending and the weakening is recorded HERE rather than as a new enum value.
    The STRONG form is NOT restated -- it was already refuted by Finding V2 of the sanctioning
    decision. This licenses NO build: not the twohot critic, not the percentile normaliser.
    ADJACENT LIVE FINDING, carried but promoting nothing: R-(b) (declared load_bearing:false)
    finds the shared CTRL critic FLAT AND UNINFORMED -- std(V)/std(G) = 0.041 against a 0.25
    collapse threshold, pre-reward-vs-far separation ratio 0.016 against a 0.25 floor,
    value-return correlation 0.434. That is signal-ABSENCE, not collapse-to-the-bimodal-mean.
    So the two halves of this composite algorithmic-import claim DISSOCIATE: R-(a) exonerates
    the two-sided normaliser while R-(b) corroborates the distributional-critic half of the same
    DreamerV3-prescribed corrective. It is the leading mechanism candidate for the V3-EXQ-780
    raw_view erosion (an installed 20.93-competence behavioural prior decaying to 11.67 under RL
    is what an unbaselined high-variance advantage predicts), and being a property of the SHARED
    control path it is common-mode across 780 and 781 -- it cannot explain a treat-vs-ctrl
    difference, but it is a live candidate root for the campaign-wide failure to RETAIN
    competence. CONDITIONING PAYOFF REALISED: because the weak form falls, V3-EXQ-781's null is
    readable at face value (781's approach drive enters `shaped` INSIDE the normaliser and would
    have been confounded had the weak form held), so 782 CLEARS the elimination bar for the
    GOV-FANOUT-1 portfolio rather than blocking it -- the outcome the pre-registered conditional
    was written to detect.
  origin: >
    Registered 2026-07-18 via the CDQ-007 convergence-demand intake (Mine + Register,
    evidence/planning/convergence_demand_pipeline_plan.md Section 5 steps 2-3), from a DreamerV3
    re-mine through the actor-critic STABILISATION lens (distinct from the MECH-438 codebook lens
    already taken from the same source). Intake:
    REE_convergence/sources/dreamer-v3/actor_critic_stabilisation.md, source.yaml claims
    DREAMER-V3-MOD-003/004 + -OBJ-002/003/004, probes -P-007/008. Routed by
    evidence/planning/failure_autopsy_MECH-457-fanout-770-771-772_2026-07-18.json (confirmed;
    non_contributory / competence_implementation_gap). Biology precondition discharged before
    registration per biology_before_formal_definitions:
    evidence/literature/targeted_review_competence_bootstrap_without_demonstrator/.
    See [[project_mech457_competence_floor_coldstart]], [[project_conversion_ceiling_four_roots]].

- id: MECH-460
  title: "transient_behavioral_prior_bootstrap / bc_seed_then_rl_without_permanent_demonstrator. What a behavioural-cloning prior supplies to a floored converter is PRIMARILY TRACTABLE EXPLORATION / STATE-VISITATION, not merely a competent action distribution: VPT (Baker 2022) reports RL from random initialisation in the same action space obtains essentially no reward and never reliably reaches the first link in the item chain, while BC-initialised RL reaches the diamond pickaxe -- a PRESENCE/ABSENCE result, not a speed-up. Three further functions are separable: the action distribution (the vehicle), VALUE-SIDE GROUNDING (DQfD's large-margin loss, which plain BC does NOT supply and whose ablation costs most of the benefit), and STRATEGIC-DIVERSITY PRESERVATION against collapse (AlphaStar). Because REE already owns deterministic in-substrate demonstrators (LocalViewGreedyPolicy 48-55 and OraclePolicy 57-61, instantiated in experiments/_lib/mech457_fanout.py::run_anchor_cell), NO PERMANENT EXTERNAL DEMONSTRATOR IS REQUIRED -- REE's transience question is not how to stop paying for demonstrations but whether the seed leaves a RESIDUE in the converter after the guide leaves the action path. The only strong transience construct in the literature is JSRL's ROLLOUT HANDOFF (a guide acts for the first h steps, the learner for H-h, with h decreasing to 0 as the learner clears a threshold), NOT a decaying loss weight: none of VPT, AlphaStar or DQfD ever anneals its prior term to zero."
  claim_type: mechanism_hypothesis
  subject: f_dominance_conversion_ceiling.behavioral_prior_seed_transience
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-20
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  generation: v3
  epistemic_category: standard
  claim_level: mechanistic
  registered_utc: "2026-07-18"
  depends_on:
    - SD-056    # e2 world-forward contrastive encoder -- the z_world arm of the representation axis
  location: docs/architecture/competence_bootstrap_mechanisms.md#4-mech-460----transient-behavioural-prior-bootstrap
  functional_restatement: >
    Behavioural cloning (imitation 32.72) is REE's ONLY floor-clearing existence proof. This claim
    names the mechanism by which a BC seed could be load-bearing WITHOUT a permanent demonstrator,
    and -- more usefully -- decomposes the prior into four separable functions so that the seed can
    be ablated function-by-function rather than as a monolith. It also names the DELTA between the
    literature's mechanism and what V3-EXQ-780 as written instantiates: 780 has the warm-start
    (warmstart_bc_rep, experiments/_lib/mech457_explorer_classes.py:335, 300 episodes) plus a
    persistent CE auxiliary to the LIVE demonstrator (bc_aux_coef 0.5), whereas the literature's
    anti-forgetting result is for KL to a FROZEN SNAPSHOT OF THE AGENT'S OWN POST-BC WEIGHTS
    (demonstrator departed) -- a different object. 780 also seeds logits only, leaving value_head
    and the successor-feature critic (reward_w zero-init) COLD, where DQfD's margin-loss ablation
    says value-side grounding is a separable and separately-load-bearing function.
  what_would_answer: >
    Three readouts, in decreasing order of decisiveness. (a) RESIDUE AFTER PRIOR REMOVAL -- the
    JSRL h=0 endpoint: does foraging competence survive when the prior is fully out of the action
    path? SUPPORTED if competence persists post-removal above the 780 ctrl; FALSIFIED (as a SEED
    reading) if it collapses to the 0-1 floor the instant the auxiliary/guide is removed, which
    would make the prior a permanent crutch rather than a bootstrap. (b) GUIDE-QUALITY GRADIENT --
    seeding from greedy_oracle (57-61) should dominate local_view_greedy (48-55), and both should
    dominate random_walk; a FLAT response across guide quality is surprising under this claim and
    points at the converter rather than the seed. (c) VALUE-SIDE SEED CONTRAST -- policy-only
    seeding vs policy+value seeding at matched actor warm-start budget, read out as critic
    calibration on demonstrator-visited states.
  evidence_quality_note: >
    LOAD-BEARING CAVEAT: all four mined sources address an EXPLORATION failure (competence
    unreachable because the relevant states are never visited). REE's signature -- four levers
    eliminated with readiness met on every leg and a PROVABLY SUFFICIENT observation -- is at
    least as consistent with a CONVERSION failure (competence reachable but not extractable).
    If REE's floor is a conversion failure, this entire mechanism family may be addressing a
    problem REE does not have. The cheap discriminator is a state-visitation readout: does the
    floored RL arm ever visit the resource-adjacent states the demonstrator visits? Exploration
    failure predicts no; conversion failure predicts yes-but-uncommitted. SECOND CAVEAT: every
    prior in these sources is LEARNED FROM DATA, whereas REE's demonstrator is a deterministic
    FUNCTION -- nothing mined speaks to whether a function-demonstrator behaves the same way, and
    VPT's inverse-dynamics machinery (the bulk of its novelty) has no REE analogue at all because
    REE's labels are free. THIRD: the AlphaStar extraction is from the hosted preprint plus
    secondary write-ups (Nature text paywalled), so its KL-coefficient specifics and any ablation
    claim are LOWER CONFIDENCE than the VPT / DQfD / JSRL material.

    [2026-08-01 governance, MECH-476 WITHDRAWN, folded in here -- full detail on MECH-476's own
    evidence_quality_note, not duplicated]: MECH-476's three pre-registered falsifier arms
    (dose/836a, novelty-tagging/836d, interval/836e, all confirmed weakens) found retention
    invariant to dose, interval, and novelty-pairing alike -- consistent with a transient
    behavioural-prior bootstrap (this claim) plus a return-scale-invariance regulariser
    (MECH-459), not a dedicated consolidation process. MECH-476's own pre-registration named
    this exact outcome as its withdrawal condition. This claim (MECH-460) is UNCHANGED by the
    fold-in -- MECH-476 leaned on the independently-established prior-transience mechanism this
    claim already characterises; the withdrawal adds no new obligation here.
  notes: >
    COORDINATION -- INFORMS, DOES NOT DECIDE. This claim maps onto the H-bc-prior leg of the live
    GOV-FANOUT-1 discrimination (V3-EXQ-780 vs V3-EXQ-781 H-approach-primitive), pre-registered
    ALIVE in the competence_floor question of hypothesis_space_registry.v1.json. Registering it
    does NOT advance that leg: the 32.72 imitation result is an existence proof that IMITATION IS
    SUFFICIENT to clear the floor, and is NOT evidence that the bc-prior route is the CORRECT
    route -- an innate approach primitive (MECH-461) could clear the same floor by an entirely
    different mechanism, and 32.72 is silent on that. The two legs are not mutually exclusive.
    PROMOTES NOTHING; LICENSES NO BUILD. mech457_competence_bootstrap_explorer stays
    blocked_pending_discrimination; V3-EXQ-780/781 run as queued and unmodified -- the deltas
    tabulated in the architecture stub are MAPPING, explicitly not change requests to a claimed
    experiment. OPEN AND UNCHECKED: whether a JSRL h->0 handoff is even expressible against REE's
    episode structure.
  origin: >
    Registered 2026-07-18 via the CDQ-007 convergence-demand intake (Mine + Register,
    evidence/planning/convergence_demand_pipeline_plan.md Section 5 steps 2-3). Intake:
    REE_convergence/sources/vpt-bc-seed/ (NEW source; source.yaml + bc_seed_then_rl.md), mining
    VPT arXiv:2206.11795, AlphaStar (Vinyals 2019), DQfD arXiv:1704.03732, JSRL arXiv:2204.02372.
    Routed by evidence/planning/failure_autopsy_MECH-457-fanout-770-771-772_2026-07-18.json.
    Biology precondition discharged before registration per biology_before_formal_definitions:
    evidence/literature/targeted_review_competence_bootstrap_without_demonstrator/.
    See [[project_mech457_competence_floor_coldstart]], [[feedback_thought_intake_must_register_claims]].

- id: MECH-461
  title: "innate_action_primitive_basis_plus_reward_independent_engagement_drive. Biological competence bootstrap without a demonstrator rests on TWO components, not one. (i) The action basis is INNATE AND REFINED, never constructed: the two neonatal motoneuron primitives are RETAINED into adulthood and augmented, with matching primitives across rat/cat/macaque/guineafowl (Dominici 2011, 10.1126/science.1210617) -- so 'a learner with no action prior fails' is the biological DEFAULT, not an anomaly; and the innate seed is demonstrator-free but NOT experience-free, since deprived of proprioceptive feedback from its OWN self-generated activity the CPG fails to mature (Zeng 2021). (ii) A REWARD-INDEPENDENT ENGAGEMENT DRIVE is necessary: an identified ZIm circuit converts stimulus presence into investigation independent of learned reward value (Ahmadlou 2021, 10.1126/science.abe9681), and dopamine-deficient mice with INTACT perception, INTACT motor apparatus and INTACT hedonics starve in front of food (Szczypka 2001) -- the closest biological analog of REE's 'readiness met on every leg, foraging at floor'. COROLLARY for REE: an approach primitive is underspecified unless one also names WHAT THE DRIVE'S OUTPUT TRAINS (the Zeng closed loop), because an innate generator alone does not mature."
  claim_type: mechanism_hypothesis
  subject: f_dominance_conversion_ceiling.innate_approach_primitive_bootstrap
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-20
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  generation: v3
  epistemic_category: standard
  claim_level: mechanistic
  registered_utc: "2026-07-18"
  depends_on:
    - MECH-229  # VALENCE_WANTING -- the existing reward-conditional drive this claim is architecturally SEPARATE from
  location: docs/architecture/competence_bootstrap_mechanisms.md#5-mech-461----innate-action-primitive-basis--reward-independent-engagement-drive
  functional_restatement: >
    The H-approach-primitive leg is usually stated as 'add an innate non-extinguishing approach
    drive'. The biology says that is only HALF a mechanism. Dominici establishes an innate motor
    PRIMITIVE BASIS that development refines rather than constructs -- the alphabet -- and Ahmadlou
    plus Szczypka establish a reward-independent ENGAGEMENT circuit that converts stimulus presence
    into investigation -- the impulse to use it. Kesner 2022 places seeking on supramammillary ->
    septal -> VTA circuitry architecturally distinct from RPE machinery, which is the shape REE
    would need if its converter is missing a CHANNEL rather than mistuning one. The claim's
    distinctive content is the CONJUNCTION plus the Zeng closed-loop requirement: a drive whose
    output trains nothing does not mature the substrate it drives.
  what_would_answer: >
    The V3-EXQ-781 pre-registered readout is the direct test on the drive half: a sustained,
    non-extinguishing, demonstrator-free approach drive (constant-coefficient intrinsic reward on
    resource proximity) that leaves the actor at the passive-survival floor on BOTH representations
    -- with mean_approach_reward_recent confirming the drive actually fired, so a null is
    informative rather than a plumbing bug -- FALSIFIES the drive half. The PRIMITIVE-BASIS half is
    NOT tested by 781 and is the claim's distinctive, currently-untested content: it would be
    answered by contrasting a converter given a pre-specified action-primitive basis (a small set
    of composable multi-step motor primitives) against one that must construct its repertoire from
    single-step actions, at matched drive. A drive-only arm clearing the floor would show the
    primitive basis is unnecessary; a primitive-basis-only arm clearing it would show the drive is.
  evidence_quality_note: >
    TWO OBJECTIONS SURFACED BY THE PULL, recorded rather than suppressed. (1) NON-EXTINCTION IS
    UNMEASURED, and the best-specified account points the other way: Kesner 2022 proposes
    ENVIRONMENT PREDICTION ERROR as seeking's currency, which QUIESCES as the world model improves.
    No source tests a drive surviving sustained non-reward. This is the sharpest objection in the
    pull and it lands exactly on the property the H-approach-primitive leg most needs. (2) THE
    BIOLOGICAL FAILURE MODE IS A CEILING DEFICIT, NOT A FLOOR ONE: untutored finches sing --
    structured but abnormal (Volman 1995). Demonstrator-deprivation in biology DEGRADES competence;
    it does not produce REE's near-zero floor. That mismatch travels with any downstream use of the
    analogy in EITHER direction (it weakens the bc-prior reading of the biology just as much).
    Also: Szczypka is a knockout in an animal that ALREADY POSSESSES the repertoire -- necessity for
    EXPRESSION, not for cold-start ACQUISITION; that gap is unbridged. Volman 1995 is not
    PubMed-indexed and its bibliographic metadata is marked provisional in the pull.
  notes: >
    FRAMING CORRECTION worth carrying forward: the 'is the prior outgrown?' worry INVERTS in
    biology. Mackevicius 2023 (eLife 10.7554/eLife.77262) shows sequences self-organise with NO
    tutor and tutor content then BINDS ONTO that scaffold -- and the birds that FAILED to learn
    were those whose UNTUTORED sequences had already crystallised. Biology's risk is PREMATURE
    CRYSTALLISATION OF SELF-GENERATED STRUCTURE locking out later demonstrator input, not a BC
    prior becoming a permanent crutch. Relatedly Veit 2023 weakens the imitative reading of the
    social evidence: a GHOST CONTROL matched a live conspecific, so demonstrations transmit task
    STRUCTURE, not policy -- if BC's 32.72 works because it transmits structure, a non-imitative
    structural channel could substitute.
    COORDINATION -- INFORMS, DOES NOT DECIDE. Maps onto the H-approach-primitive leg of the live
    GOV-FANOUT-1 discrimination (V3-EXQ-781 vs V3-EXQ-780 H-bc-prior), pre-registered ALIVE in the
    competence_floor question of hypothesis_space_registry.v1.json. Registered as the EVEN-HANDED
    counterpart to MECH-460 precisely so the intake does not bias the discrimination toward the leg
    with the existing existence proof. PROMOTES NOTHING; LICENSES NO BUILD.
    mech457_competence_bootstrap_explorer stays blocked_pending_discrimination.
    DISTINCTNESS: not MECH-455 (competence-based IM is a rate-of-improvement bonus over the agent's
    OWN goals -- a derivative signal that presupposes some achievement); not MECH-458 / SD-025
    (curiosity is reward-conditional and self-attenuating, the opposite of non-extinguishing); not
    MECH-314's knowledge-based IM family (reward = improving the world model). This claim's operand
    is an INNATE, reward-independent, non-derivative engagement impulse plus a pre-specified motor
    basis -- neither of which any registered claim owns.
  origin: >
    Registered 2026-07-18 via the CDQ-007 convergence-demand intake (Mine + Register,
    evidence/planning/convergence_demand_pipeline_plan.md Section 5 steps 2-3). Sourced from the
    MANDATORY biology /lit-pull discharged BEFORE registration per the
    biology_before_formal_definitions invariant:
    evidence/literature/targeted_review_competence_bootstrap_without_demonstrator/ (8 sources --
    Dominici 2011, Zeng 2021, Ahmadlou 2021, Kesner 2022, Szczypka 2001, Mackevicius 2023,
    Veit 2023, Volman 1995). Routed by
    evidence/planning/failure_autopsy_MECH-457-fanout-770-771-772_2026-07-18.json.
    See [[feedback_biology_before_formal_definitions]], [[project_mech457_competence_floor_coldstart]].

- id: MECH-462
  title: "Consolidation-phase damage is content-scorable only where the phase's output is a FIRST-ORDER content-bearing quantity (NREM inter-store transfer; SWS retrieval margin). A phase whose output is a SECOND-ORDER quantity over representations -- REM precision / confidence recalibration -- is content-free BY TYPE, and its damage is readable only through a downstream behavioural discrimination consequence, never through a statistic computed on the precision quantity itself."
  claim_type: mechanism
  subject: sleep.consolidation_readout_order
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-20
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v3
  claim_level: mechanistic
  registered_utc: "2026-07-18"
  depends_on:
    - SD-068
    - MECH-123
    - MECH-204
  location: docs/architecture/sd_068_consolidation_lesion_harness.md
  functional_restatement: >
    Child A of the 2026-07-18 SD-068 claim-synthesis decomposition, and the common thread
    the decomposition was built around. Three separate REM-leg results had been read as
    three separate construction defects; this claim asserts they are ONE TYPE FACT. A
    readout can score content fidelity only if the quantity it reads carries content.
    NREM transfer fidelity and SWS retrieval margin are first-order -- they are quantities
    OVER the injected content, so damage to the phase shows up as degradation in them. REM
    precision recalibration is second-order -- it is a quantity over the REPRESENTATIONS
    (a confidence about content, not content), so it is content-free by type and no amount
    of repair to the direct statistic will make it content-contingent. The damage is real;
    it is the READOUT LOCATION that is wrong, and the only place it can surface is a
    downstream behavioural consequence of miscalibrated confidence.
  what_would_answer: >
    Build the meta-d-prime-analogue downstream readout for the REM phase (does the agent's
    post-REM confidence separate its own correct from incorrect retrievals) and run it on
    the SAME CELLS as V3-EXQ-778e. SUPPORTED if the downstream readout is content-contingent
    (null_slope_ratio <= 0.25, ceiling_inside_ci95 false, >=6/8 seeds) WHILE the direct
    precision readout on those same cells remains above 0.25 -- the same-cells contrast is
    what makes this a test of the type fact rather than of the build. REFUTED two ways, and
    both matter: (i) if the downstream readout is ALSO content-free, the phase carries no
    recoverable damage signal at all and the type story is wrong about recoverability; or
    (ii) if a DIRECT precision-quantity readout can be made content-contingent, the
    type-fact is simply false and all three results were construction defect after all.
  evidence_quality_note: >
    EXPLANATORY SCOPE -- this claim is registered because it unifies three ALREADY-OBSERVED
    results as one type fact, not because a test of it has been run. The three: V3-EXQ-778c
    rem both-rail degeneracy (exactly 0.0 on 5/8 seeds, off-scale 1801-9143 on 3/8);
    V3-EXQ-778e de-clamped content-freeness (the degeneracy was NOT a clamp artefact);
    V3-EXQ-778f rem_generative_gain content-freeness (flat in content scale). All three are
    experiment_purpose=diagnostic and none weights governance confidence.
    UNTESTED AS A TYPE CLAIM. Its discriminating test is SD-072's build; until that runs,
    the type reading is the best explanation of three nulls, not an established mechanism.
    Registered candidate accordingly, and it PROMOTES NOTHING.
    MECH-121 HOLD RESPECTED -- not tagged, not depended on.
    LIT GROUNDING (commissioned and returned, Section 4 of the synthesis doc): Q1 first-order
    vs second-order consolidation outputs (Walker & Stickgold 2010; Brodt et al. 2023); Q3
    metacognitive-sensitivity measurement (Maniscalco & Lau 2012; Mazancieux et al. 2020).
    Q2 FORMAL-IMPORT FLAG, EXPLICIT: meta-d-prime is imported as a MEASUREMENT FORM for
    "does confidence track own-accuracy", not as a claim that REE's precision signal is
    human metacognition; the analogy carries no architectural authority.
    REGISTRATION DEVIATION: the synthesis proposal specified epistemic_category
    "measurement_gap", which is not in the validate_claims.py VALID_EPISTEMIC_CATEGORIES
    enum. Registered as "substrate_conditional" -- the honest mapping, since the
    discriminating test is gated on SD-072's unbuilt downstream readout.
  origin: >
    Child A of the SD-068 claim-synthesis decomposition, approved at the interactive gate
    2026-07-18 (session gracious-bassi-721580). Explains the 778c / 778e / 778f REM-leg
    cluster as one type fact rather than three bugs. Full derivation, discrimination gate
    and literature grounding:
    evidence/planning/claim_synthesis_SD-068_2026-07-18.md Section 5.

- id: MECH-463
  title: "Global-scalar arousal channels are a VARIANCE AMPLIFIER of the already-dominant selection channel, not a source of behavioural differentiation: the three affective routes that modulate commitment globally (D1/D2 dopamine gain, harm-urgency threshold shrinkage, LC-NE/phasic-surprise temperature) apply one scalar uniformly across all K candidates and therefore cannot change an argmax, but DO change whether commitment fires and how sharply -- selecting for whichever channel already carries the most per-candidate score variance. Under F-dominance (MECH-439) this makes affective engagement ENTRENCH F rather than convert diversity, predicting that F's share of committed-selection variance RISES with arousal and FALLS under low urgency."
  claim_type: mechanism_hypothesis
  subject: selection.global_arousal_variance_amplification
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-20
    needs_review: false
    evidence:
      from: failure_autopsy_mech463-exogenous-inertness_2026-07-20
      as_of: 2026-07-20
      verdict: does_not_support/standard
  epistemic_category: standard
  implementation_phase: v3
  claim_level: mechanistic
  registered_utc: "2026-07-18"
  depends_on:
    - MECH-439   # F-dominance -- the variance monopoly this claim says arousal amplifies rather than breaks
    - SD-011     # z_harm_a -- the affective stream feeding the urgency threshold shrinkage
    - MECH-229   # VALENCE_WANTING -- the realised-valence EMA driving the D1/D2 tonic gain
  location: evidence/planning/conversion_ceiling_prong_map.md
  functional_restatement: >
    Affect enters E3 selection at four points with different granularity. ONE is
    per-candidate and additive: score_bias (e3_selector.py:2189,
    scores = scores + bias_tensor, shape-checked [K] at :2162). THREE are global
    scalars: the D1/D2 dopamine gain (e3_selector.py:1559-1562, da = tanh of the
    realised-valence EMA), the harm-urgency threshold shrinkage
    (e3_selector.py:2679-2686, effective_threshold * (1.0 - urgency_applied), where
    urgency_applied derives from z_harm_a.norm(dim=-1).mean() -- a single number), and
    the softmax temperature (noise_floor.py:192, phasic_surprise_burst.py:261).
    A uniform scalar cannot reorder candidates. But shrinking the commit threshold
    makes commitment fire on whichever candidate already has the largest score
    separation -- i.e. it is channel-agnostic variance amplification. If F already
    carries ~88-89% of committed-selection variance (MECH-439), arousal does not
    bias TOWARD F; it selects for the dominant channel, which IS F. The prediction
    that distinguishes this from MECH-439 alone is CONDITIONAL: F-share should be a
    monotone increasing function of urgency/arousal decile, and should be materially
    lower in the bottom decile. MECH-439 as stated predicts a flat high F-share.
  what_would_answer: >
    A variance decomposition of committed-selection scores into F vs the other
    channels, CONDITIONED on arousal (urgency_applied decile, and secondarily on
    tonic temperature and the D1/D2 da scalar). Supports if F-share rises
    monotonically with urgency decile and the bottom-vs-top decile gap exceeds the
    effect-size floor. Refutes if F-share is flat across arousal deciles -- which
    would mean F-dominance is structural in the score geometry and arousal is
    causally inert with respect to it, leaving MECH-439 untouched and killing this
    claim cleanly.
  evidence_quality_note: >
    NOT computable from existing runs (instrumentation audit 2026-07-18). Three gaps
    in the current substrate: (1) urgency_applied escapes only as
    SelectionResult.urgency (e3_selector.py:3151) and is never written to
    last_score_diagnostics or persisted; effective_threshold and commit_variance are
    pure locals (:2671-2707). (2) Per-channel bias is retained only as the SUMMED
    bias_tensor inside E3; agent.py:6816-6845 does build a per-channel decomp but
    stores REDUCED SCALARS, so channel-channel and channel-F covariance are lost --
    marginal variances only, not a covariance-correct decomposition. It is also
    gated on e3_score_decomp_enabled, default False (e3_selector.py:360). (3) No
    existing E3 experiment persists per-tick rows; 689d, 571 and 609 all reduce to
    arm-level means before the manifest. Checked manifests 689d / 643a / 571 --
    none carry an arousal conditioning variable. So this needs an additive
    instrumentation change plus one queued run, NOT a re-analysis.
    MANDATORY INHERITED GATE: the V3-EXQ-643 non-vacuity readiness gate
    (modulatory_authority_active_frac > 0, cross-candidate range > floor, plus a new
    assertion that urgency_applied is non-constant across the run). 643 FAILed
    precondition_unmet purely from float32 catastrophic cancellation (E3 scores
    ~1e32, modulatory range below float32 ULP); without that gate this probe returns
    a spurious FLAT F-share for numerical reasons and FALSELY REFUTES.
    ADJUDICATED 2026-07-19 -- V3-EXQ-785 RAN (arousal variance-amplifier decomposition) and is
    scored MIXED by failure_autopsy_V3-EXQ-785_2026-07-19, epistemic_category
    measurement_test_design_defect. The direction was CORRECTED from the auto-derived
    non_contributory to mixed, and the run's self-route substrate_not_ready_requeue is REJECTED
    AS A WHOLE-RUN READING: the run has TWO arms and only ONE of them is vacuous, so discarding
    the run entire would have thrown away a readable result. AMPLIFICATION is CONFIRMED;
    CONCENTRATION is CONTRADICTED -- which is a genuine split of this claim's two components
    rather than a null. Because the two arms dissociate, the honest reading is partial support
    for the entrenchment polarity (affect amplifies the incumbent) with the concentration
    sub-prediction actively contradicted. Routes to /queue-experiment for a redesign that fixes
    the vacuous arm; NOT a substrate build and NOT a demotion. Status stays candidate; no
    confidence change (diagnostic, governance-scoring-excluded).
    SUPERSEDED + ADJUDICATED 2026-07-19 (V3-EXQ-785a) -- STATUS UNCHANGED (candidate).
    V3-EXQ-785a re-ran the decomposition with urgency set EXOGENOUSLY (i.i.d. uniform
    over a pre-registered grid, so the endogeneity confound is broken by construction
    rather than adjusted away), across an 8.5x range (0.04-0.34) vs 785's 1.8x
    endogenous range -- BETTER powered, not worse -- with gate GREEN, non_degenerate
    true, and 1757 independent committed selections. THE REGISTERED CONCENTRATION
    PREDICTION IS CONTRADICTED: incumbent share 0.9375 -> 0.9411, gap +0.0036 against
    a +0.02 threshold, rho +0.31 vs +0.6. And the amplification premise goes with it:
    var_total fold 0.970 (rho -0.086), where 785 had measured 14.1x. So the ENTIRE 785
    profile -- amplification AND dilution -- was the endogeneity confound (near-hazard
    candidate geometry), the rival the 785 autopsy had named as leading. 785's manifest
    is moved to evidence_direction superseded (superseded_by V3-EXQ-785a) and no longer
    weights this claim.
    THE NULL IS TIGHT, NOT UNDERPOWERED: within-level share SD ~0.075-0.082 at ~290
    rows/level gives SE ~0.0044, so the measured gap sits under 1 SE where 785's 0.139
    fall would have been 30+ SE. Per-seed gaps straddle zero (share
    [-0.0016,+0.0119,+0.0021,+0.0081,-0.0058]). Internal validity held: the
    manipulation genuinely moved the gate (effective_threshold 0.3761 -> 0.2570, a 32%
    reduction) with injection fidelity 5.6e-17 and randomisation
    corr(assigned_urgency, hazard_prox) = 0.0177 -- so this is "no effect", not "no
    treatment". Differential admission is separately ruled out as the mechanism: C3
    measured commit rate flat at ~0.99-1.00 across all six levels despite the 32%
    threshold movement.
    MECHANISM CORRECTION FOR THE RECORD: the 785 autopsy's flagged "candidate reasoning
    error" was itself mis-signed. committed = variance < commit_threshold*(1-urgency)
    is an UPPER bound, so raising urgency makes admission STRICTER, not less selective;
    and the gated quantity is the z_world RUNNING VARIANCE (world-model stability), not
    candidate separation, because use_harm_variance_commit is off.
    WHY NO STATUS CHANGE -- THREE CAVEATS BOUND THIS. (1) CHANNEL-AGNOSTICISM IS
    UNTESTED. 785a runs a SINGLE regime (harm_weighted incumbent). The second regime was
    dropped with recorded evidence: an entropy_bias_scale scan over
    {0.15,0.30,0.50,1.00} found CH:mech341 absorbing ~99-115% of cross-candidate
    variance at every setting, so its share statistic is arithmetically forced. This
    refutes at ONE incumbent identity only, and channel-agnosticism is half of what this
    claim asserts. (2) z_world UNDER-DIFFERENTIATION: participation ratio ~1.06 at
    world_dim=128 with absolute variances ~1.2e-05, leaving live the alternative reading
    that the substrate cannot EXPRESS the effect rather than that the effect does not
    exist -- the objection already recorded above, which bites the fix rather than the
    measurement. (3) SCOPE: this tests arousal as a scalar on the SD-011 commit
    threshold specifically, not every arousal route (the D1/D2 tonic gain and the
    softmax-temperature routes are untouched).
    2026-08-10 edge correction (chip chip-20260810-phase-consistency-reconciliation):
    depends_on: MECH-359 and MECH-390 removed. Both are v4 claims MECH-463 cites for
    COMPARISON only ("distinct from MECH-359 in POLARITY..."; "MECH-390 prescribes the
    structurally anti-F-dominance form... this claim supplies the mechanism and falsifier
    MECH-390 currently lacks") -- MECH-463 ran 4 real experiments (V3-EXQ-785/785a/785b/
    787) without either ever being built.

    HYPOTHESIS-SPACE LEDGER: applied at this adjudication under qid
    arousal-variance-amplifier (hypothesis_space_registry.v1.json), 2 of 3 frozen
    hypotheses eliminated. H-arousal-concentrates and H-arousal-broadens ELIMINATED;
    H-endogenous-hazard-geometry deliberately left ALIVE rather than confirmed -- its
    apparent direct support is a between-seed artifact (pooled
    corr(hazard_prox_mean, share) = -0.187 reverses to mostly POSITIVE within seed,
    r = [+0.089,+0.076,+0.041,-0.125,+0.135]; across the 5 seed means r = -0.78,
    carried largely by seed 0). It survives as the residual explanation only.
    CLUSTER ADJUDICATION 2026-07-20 (V3-EXQ-785a + V3-EXQ-785b, confirmed
    failure_autopsy_mech463-exogenous-inertness_2026-07-20; read with V3-EXQ-787,
    failure_autopsy_V3-EXQ-787_2026-07-19) -- STATUS UNCHANGED (candidate), direction
    does_not_support on all three. THE CHANNEL-AGNOSTICISM CAVEAT (1) ABOVE IS NOW
    DISCHARGED AND IT FAILS. 785b ran the residue_weighted AND f incumbents under
    exogenous urgency: var_total fold 1.078, share rho -0.143 against a pre-registered
    +0.6, with 8/8 preconditions green on BOTH arms (per_arm_gate.all_green), 1757
    committed ticks, 6 populated urgency levels, exogenous assignment fidelity 5.6e-17,
    randomisation orthogonal to hazard proximity at 0.066 against a 0.25 ceiling, and
    non_degenerate true on all three. So the inertness is NOT an artifact of the single
    harm_weighted incumbent identity -- it holds channel-agnostically, which is the half
    of this claim the 785a adjudication had explicitly left untested.
    V3-EXQ-787 independently closes the residual: the hazard-geometry rival left ALIVE
    by the 785a ledger entry is itself flat -- within-seed slope -0.000944 (sd 0.005087,
    se 0.001038, t = -0.909, df 23, t_crit 2.069, not significant) across 24 seeds,
    failing a 0.0287 absolute effect floor, with the robustness gap agreeing in sign.
    WHY THIS IS A FRAMING PROBLEM AND NOT A SUBSTRATE ONE: both autopsies set
    recommended_substrate_queue_entry.action = none, on the explicit ground that every
    precondition is green on every scored arm. Nothing is blocked on substrate; the
    residual is the claim's operationalization. ROUTED TO /claim-synthesis (2026-07-20
    governance) -- NOT to /queue-experiment and NOT to /implement-substrate. Running a
    further arousal decomposition on this framing would be re-running a decided null.
    CAVEAT (2) IS NOW LOAD-BEARING IN A NEW WAY -- READ BEFORE ANY RE-POSE.
    V3-EXQ-737 (confirmed failure_autopsy_V3-EXQ-737a_2026-07-20) found that in the
    x734/737 driver family z_world is NEVER PREDICTION-TRAINED: the P0/P1 warmup has no
    optimizer group covering latent_stack (0 of 61 tensors), so z_world is a frozen
    random projection. The under-differentiation recorded in caveat (2) above
    (participation ratio ~1.06 at world_dim=128) was therefore attributed to a
    representational LIMIT when at least part of it may be an untrained-encoder
    IMPLEMENTATION GAP. This does not rescue the claim -- the exogenous nulls are
    measured on committed-selection score geometry and their preconditions are green --
    but a re-posed operationalization must not treat "z_world cannot express it" as an
    established substrate ceiling until sd_zworld_warmup_optimizer_group lands.
  notes: >
    Registered 2026-07-18 from a conversation-originated hypothesis (user), distinct
    from MECH-359 in POLARITY: MECH-359 says a scalar is too WEAK to steer (affect
    fails to convert); this says the scalar is not inert but actively ENTRENCHING
    (affect amplifies the incumbent). Both rest on the same argmax-invariance
    observation; they differ in what they predict about the commit gate, which
    MECH-359 does not address. MECH-390 prescribes the structurally anti-F-dominance
    form (convergent-weak-channel summation over single-dominant-channel); this claim
    supplies the mechanism and falsifier MECH-390 currently lacks.
    ROUTING NOTE: this does NOT re-open the F-dominance selection faces. The
    conversion-ceiling campaign's re-derive brake is live (further lettered re-tests
    of the 689/485/445/625 lineages REFUSED per the 734-737 autopsy) and the campaign
    is routed to MECH-457 (competence floor) and INV-088 (z_world differentiation).
    This is an INSTRUMENTATION/DECOMPOSITION probe on score geometry, not a
    behavioural conversion test, so it does not require the agent to be competent and
    is NOT gated on MECH-457 / INV-088.
    INTERPRETATION CAVEAT: under an under-differentiated z_world (participation ratio
    ~1.06 at world_dim=128) the per-candidate channels have little to range over.
    That objection bites on the FIX, not on the measurement -- but a REFUTES verdict
    must be read against it rather than as unconditional.
    NARROWED + BLANKET PREMISE WITHDRAWN 2026-07-20 (/claim-synthesis,
    evidence/planning/claim_synthesis_MECH-463_2026-07-20.md, user-approved per-child).
    (1) SCOPE NARROWED. This claim is narrowed to what was actually tested and falsified:
    THE HARM-URGENCY THRESHOLD ROUTE'S EFFECT ON COMMITTED-SELECTION VARIANCE GEOMETRY.
    On that narrowed scope the falsification is clean and replicated (three incumbent
    identities, expressivity positive control passed, hazard-geometry rival independently
    eliminated by V3-EXQ-787) and is READY FOR A /governance STATUS DECISION.
    (2) THE BLANKET THREE-ROUTE PREMISE IS WITHDRAWN. The registered functional_restatement
    asserts of all three named global scalars that each "applies one scalar uniformly
    across all K candidates and therefore cannot change an argmax". That is FALSE for two
    of the three, and all four runs (785/785a/785b/787) manipulated only the third:
    each injected on e3.config.urgency_weight and nothing else.
      - D1/D2 dopamine gain: FALSE IN CODE. _d1_d2_split (e3_selector.py:1553-1570) gains
        relu(-accum) and relu(+accum) ASYMMETRICALLY about zero, so a non-zero da reorders
        any candidate pair whose accumulators straddle zero; the ARC-109 docstring states
        "the dissociation is earned only once da != 0". Now carried by MECH-464.
      - Softmax temperature: CONTRADICTED BY EXISTING EVIDENCE under another claim.
        MECH-087 maps dopamine selection-gain to the E3 softmax temperature and V3-EXQ-674
        measured dopamine-temperature modulating decisiveness at 0.93. Cross-reference
        only; no new claim registered, to avoid duplicating MECH-087.
    Both routes are additionally OUTSIDE THIS CLAIM'S FALSIFIER'S CAUSAL PATH, so the null
    could never have generalised to them empirically: D1/D2 acts on the loop accumulator
    AFTER channel composition (re-gains the aggregate, not an additive component in the
    decomposition), and temperature divides the composed score INSIDE the softmax, strictly
    downstream of the per-candidate component vectors the shares are computed from
    (verified in code by mech463_channel_agnosticism_scoping_spike_2026-07-19.md and
    re-verified 2026-07-20). The generalisation was therefore analytic, resting entirely on
    the argmax-invariance premise now withdrawn.
    (3) THE COMMIT-GATE HALF IS UNTESTED, NOT FALSIFIED. "DO change whether commitment
    fires and how sharply" was measured only as V3-EXQ-785a criterion C3
    (load_bearing: false) in a saturated regime -- gated variance 25-48x below
    effective_threshold at every urgency level, commit rate pinned 0.9901-1.0000. Now
    carried by MECH-465 (substrate_conditional, probe-gated).
    (4) THE ROUTED PREMISE-DECOMPOSITION WAS REFUSED. The cluster autopsy routed here on
    the argument that this claim is coarse because it fuses amplification with
    concentration. That failed the /claim-synthesis Step-3 gate: under exogenous
    manipulation BOTH premises fail with green preconditions and a passing expressivity
    control, so 785a/785b are a REPLICATED SINGLE falsification rather than two distinct
    signatures circling, and V3-EXQ-787's own four-layer diagnosis records
    claim_alignment: "intact" (it eliminated a rival, it did not fail this claim).
    Splitting a jointly-falsified pair would yield two falsified children. The genuine
    granularity debt was on the ROUTE and REGIME axes instead, per (2) and (3).
    (5) NO FURTHER LETTERED AROUSAL DECOMPOSITION ON THE 785 AXIS. Both autopsies' refusal
    stands and is endorsed. The residual unexplained 14.1x ENDOGENOUS profile is a
    mystery (known data) node, not a claim: V3-EXQ-787 showed endogenous urgency does not
    even track hazard proximity (corr = 0.164 against a 0.2 premise threshold), so what it
    tracks is unknown. Recommended as a NEW QID in hypothesis_space_registry.v1.json
    ("what does endogenous urgency co-vary with?") rather than a claim, since no
    pre-registered candidate remains to freeze. NOT written by the synthesis session --
    session commit-push-ordering-fae508 held an active TASK_CLAIMS claim on that registry
    file at the time; left as a recommendation for whoever holds it.
- id: MECH-464
  title: "D1/D2 opponent dopamine gain is ORDER-CHANGING, not a uniform scalar: because it gains the promote and suppress populations asymmetrically about zero, a non-zero da can reorder any candidate pair whose loop accumulators straddle zero -- so this route is capable of the behavioural differentiation MECH-463 asserts no global-scalar route can produce."
  claim_type: mechanism_hypothesis
  subject: selection.d1_d2_opponent_gain_is_order_changing
  polarity: asserts
  status: candidate
  epistemic_category: standard
  implementation_phase: v3
  claim_level: mechanistic
  registered_utc: "2026-07-20"
  depends_on:
    - ARC-109
    - MECH-463
    - SD-011
  location: evidence/planning/claim_synthesis_MECH-463_2026-07-20.md
  source:
    - evidence/planning/claim_synthesis_MECH-463_2026-07-20.md
  functional_restatement: >
    MECH-463 groups three "global scalar" arousal routes and asserts of all three that
    each applies one scalar uniformly across all K candidates and therefore cannot
    change an argmax. That characterisation is FALSE IN CODE for the D1/D2 route.
    `_d1_d2_split` (ree-v3/ree_core/predictors/e3_selector.py:1553-1570, applied at
    :1728-1729) does not scale the accumulator uniformly. It splits it into two opponent
    populations about zero and gains them ASYMMETRICALLY:
    go = relu(-accum); nogo = relu(+accum);
    d1_gain = 1.0 + d1_da_gain*da; d2_gain = max(0.0, 1.0 - d2_da_gain*da);
    net = d2_gain*nogo - d1_gain*go.
    This is a piecewise transform, asymmetric about zero, so it can reorder any candidate
    pair whose accumulators straddle zero. The ARC-109 docstring states the design intent
    outright: at da == 0 the net is "bit-identical to the additive scalar; the
    dissociation is earned only once da != 0". The route therefore differs mechanistically
    from the harm-urgency threshold route (SD-011) that V3-EXQ-785/785a/785b/787 actually
    manipulated -- all four injected on e3.config.urgency_weight and nothing else.
  what_would_answer: >
    Sweep da EXOGENOUSLY (as V3-EXQ-785a swept urgency) with d1_da_gain / d2_da_gain at
    their configured values, recording per-candidate loop accumulators and the committed
    argmin per tick. SUPPORTS if the committed-candidate identity changes with da at a
    rate exceeding the da == 0 baseline, and if the reordering rate scales with the
    fraction of candidate pairs whose accumulators straddle zero. REFUTES if the committed
    identity is invariant to da across the sweep, which would mean the straddle fraction
    is ~0 in practice and the asymmetry, though real in code, is behaviourally inert.
    MANDATORY NON-VACUITY GATE: report the straddle fraction. If it is ~0 the run is
    vacuous and MUST be scored precondition_unmet, not as a null -- without this gate the
    probe returns a spurious "inert" for a purely structural reason.
  evidence_quality_note: >
    Registered 2026-07-20 by /claim-synthesis on MECH-463
    (evidence/planning/claim_synthesis_MECH-463_2026-07-20.md), after the 2026-07-20
    exogenous-inertness cluster. NOT yet tested -- no run has ever manipulated this route.
    The mechanism is verified in source rather than inferred, and the instrument
    (per-candidate accumulators) is the same one V3-EXQ-785a/785b already persist, so the
    falsifier needs no substrate change. Lit grounding PRESENT, no /lit-pull owed:
    evidence/literature/targeted_review_striatal_gain_control_bounding carries
    kohnomi2016 (DA graded lateral inhibition) and chen2020 (D2 autoreceptor homeostat).
  notes: >
    This claim CONTRADICTS MECH-463's premise for the D1/D2 route specifically; it does
    not disturb MECH-463's falsification on the harm-urgency threshold route, which stands
    (replicated at three incumbent identities with a passing expressivity positive control
    and the hazard-geometry rival independently eliminated by V3-EXQ-787). Registered
    rather than folded into MECH-463's note because it is a testable mechanism assertion
    with its own falsifier, not merely an erratum. SCOPE: the third route MECH-463 names,
    softmax temperature, is deliberately NOT claimed here -- MECH-087 already maps
    dopamine selection-gain to the E3 softmax temperature and V3-EXQ-674 already evidenced
    it (dopamine-temperature modulates decisiveness 0.93), so a separate claim would
    duplicate registered work. That route is carried as a cross-reference in the synthesis
    doc instead.
- id: MECH-465
  title: "Arousal's effect on WHETHER commitment fires is expressible only near the commit-gate boundary: in every run to date the gated quantity sits 25-48x below the threshold and commit rate is pinned at 0.99-1.00, so MECH-463's commit-gate prediction is UNTESTED rather than falsified."
  claim_type: mechanism_hypothesis
  subject: selection.commit_gate_arousal_effect_requires_boundary_regime
  polarity: asserts
  status: candidate
  epistemic_category: substrate_conditional
  implementation_phase: v3
  claim_level: mechanistic
  registered_utc: "2026-07-20"
  depends_on:
    - SD-011
    - MECH-463
  location: evidence/planning/claim_synthesis_MECH-463_2026-07-20.md
  source:
    - evidence/planning/claim_synthesis_MECH-463_2026-07-20.md
  functional_restatement: >
    MECH-463 asserts arousal "cannot change an argmax, but DO change whether commitment
    fires and how sharply". The variance-geometry half is falsified. The commit-gate half
    was measured only as V3-EXQ-785a criterion C3, explicitly marked load_bearing: false,
    and in a regime with no headroom. The gate is committed = variance < effective_threshold.
    Per the 785a manifest, by urgency level: commit_variance_mean 0.00785 / 0.00983 /
    0.01193 / 0.00646 / 0.00710 / 0.01037 against effective_threshold_mean 0.3761 / 0.3467
    / 0.3294 / 0.3052 / 0.2800 / 0.2570 -- the gated quantity sits 25-48x BELOW its own
    bound at every level, and commit rate is pinned at 0.9901-1.0000 (span 0.0099). The
    32% threshold reduction (0.3761 -> 0.2570) that correctly demonstrates the manipulation
    LANDED is, for this DV, moving a ceiling never within an order of magnitude of binding.
    A flat commit rate under those conditions is a ceiling effect, not a null.
  what_would_answer: >
    STAGE 1, SCOPING SPIKE (this is why the claim is substrate_conditional and
    probe-gated, NOT queued): establish whether a boundary regime -- commit_variance
    within ~2x of effective_threshold -- is reachable BY CONFIGURATION (e.g. lowering
    commit_threshold), or whether it requires raising z_world variance.
    STAGE 2a, IF REACHABLE BY CONFIG: re-run the V3-EXQ-785a exogenous-urgency design in
    that regime with COMMIT RATE as the load-bearing DV. SUPPORTS if commit rate varies
    monotonically with urgency and the span exceeds a pre-registered effect floor.
    REFUTES if commit rate stays flat WITH DEMONSTRATED HEADROOM.
    MANDATORY PRECONDITION on any stage-2a run: median gate margin < 2x AND commit rate
    away from both 0 and 1. Without it the run reproduces the 785a ceiling effect and must
    be scored precondition_unmet.
    STAGE 2b, IF NOT REACHABLE BY CONFIG: the claim is substrate-gated and routes to
    /implement-substrate, not to /queue-experiment.
  evidence_quality_note: >
    Registered 2026-07-20 by /claim-synthesis on MECH-463
    (evidence/planning/claim_synthesis_MECH-463_2026-07-20.md).
    EPICYCLE RISK, STATED PLAINLY: adding a headroom precondition to a falsified claim is
    exactly the move that manufactures epicycles and should be resisted on reflex. The
    defence is that this is NOT a precondition invented to rescue MECH-463 -- "changes
    whether commitment fires" is in MECH-463's REGISTERED TEXT, was assigned its own
    criterion C3, and C3 was marked non-load-bearing precisely because its author saw the
    admission mechanism was mis-signed. The unmeasured half is original, not retrofitted.
    THIS DOES NOT DISTURB THE VARIANCE-GEOMETRY NULLS, which are computed on committed
    ticks with green preconditions and a passing expressivity control and which STAND.
    Different DV (commit rate, not variance geometry), different regime (gate near
    boundary, not saturated), different precondition (headroom, which no prior run
    reports). The 785-axis variance-geometry framing stays CLOSED -- this is not a further
    lettered arousal decomposition and must not be run as one.
    Lit grounding PRESENT, no /lit-pull owed: evidence/literature/targeted_review_connectome_mech_463
    carries jepma2010 (the reboxetine/citalopram/placebo null -- the closest human causal
    test of a global NE scalar moved in isolation) whose synthesis independently names
    THIS measurement as the MECH-463/MECH-359 discriminator: "does raising the scalar
    change whether and how sharply commitment fires, holding the winner fixed?" -- plus
    astonjones2005 (adaptive gain theory).
  notes: >
    Z_WORLD CAUTION, RESPECTED RATHER THAN LEANED ON: the stage-1 spike may find the
    boundary regime needs higher z_world variance, which would collide with V3-EXQ-737's
    finding that z_world is never prediction-trained in the x734/737 driver family (0 of 61
    latent_stack tensors move; substrate_queue entry sd_zworld_warmup_optimizer_group
    pending). This claim does NOT treat "z_world cannot express it" as an established
    substrate ceiling -- it treats reachability as the open question the spike exists to
    answer, which is the correct posture while that substrate entry is unlanded.
    LIVE TENSION TO NOTE: both 2026-07-20 autopsies set
    recommended_substrate_queue_entry.action = none. That verdict is correct for the
    VARIANCE-GEOMETRY framing (every precondition green on every scored arm) and may not
    hold for this one, which is why stage 1 is a spike rather than a queued experiment.

    2026-08-10 edge correction (chip chip-20260810-phase-consistency-reconciliation):
    depends_on: MECH-359 removed -- the stage-1 scoping spike (lowering commit_threshold,
    re-running the 785a design on existing V3 substrate) never requires MECH-359's
    substrate; comparative reference only.

# ---------------------------------------------------------------------------
# Registered 2026-07-22 -- six thought intakes written 2026-07-21 by session
# sad-newton-00451d (intakes landed REE_assembly 42f510757b / f9d64740df /
# 06ab1d8609). Registration was the DEFERRED half of that session's work: it
# wrote the intakes but made no claims.yaml edit because a concurrent session
# held the claims.yaml claim. Registered here by epic-burnell-995d28-register.
# Six clusters, in intake order:
#   A. organisational principles across heterogeneous timescales (2026-07-12)
#   B. goal relevance / salience / distractor suppression      (2026-07-12)
#   C. rule apprehension as an organisational principle        (2026-07-14)
#   D. multiple relational graph organisations, hippocampal    (2026-07-19)
#   E. conservative skill refinement, multi-timescale learning (2026-07-19)
#   F. bounded knowledge-artefact optimisation, REE ASSEMBLY   (2026-07-19)
# E and F come from ONE source (Microsoft SkillOpt) and were deliberately split
# by the user: E is REE cognitive architecture, F is REE Assembly governance.
# Keep them apart -- conflating them is the failure the split exists to prevent.
# Each intake's already-owned table was resolved by the intake itself; nothing
# listed there is re-asserted here.
# ---------------------------------------------------------------------------

# --- Cluster A: organisational principles across heterogeneous timescales ----

- id: Q-081
  title: "Does REE's configured multi-rate execution produce SHARED cross-stream organisation, or only configured rate separation? SD-006 guarantees that E1/E2/E3 run at characteristic rates; nothing asserts or tests that the heterogeneous streams (E1/E2/E3, harm, goal, salience, hippocampal, control) thereby occupy recurrent system-level configurations or exhibit homologous transition motifs. Configured rate separation does not entail shared organisation."
  claim_type: open_question
  subject: architecture.cross_stream_shared_organisation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  what_would_answer: >
    Recurrent-state / transition-matrix / cross-stream-lag analysis over EXISTING
    telemetry. The discriminating comparison is emergent shared structure (Outcome A)
    versus structure trivially implied by the configured rates and wired gates
    (Outcome B) -- B is the null that most experiments of this shape would fail to
    exclude, so the constrained-realisation surrogate control specified in the
    non-degeneracy guard below is not optional, and neither is the
    structure-destroying arm that accompanies it. The raw thought's
    outcome taxonomy is the verdict grid: A shared organisation / B wired-gates-only /
    C unrelated / D too-much-sharing-collapses / E event-relative beats clock-relative.
    PRECONDITION, and the reason no experiment is proposed here: open question 6 of the
    raw thought -- do REE's current logs contain sufficient telemetry to test this? --
    must be answered first by a retrospective telemetry AUDIT, not a probe. The audit
    checklist is the instrumentation list: E1 hidden state, E2 PE, E3 candidate scores +
    commitment state, z_self, z_world, z_harm_s, z_harm_a, z_goal, operating_mode, beta,
    boundary broadcasts, hippocampal proposals, residue updates, offline phase markers.
    That spike is complicated (buildable). Only after it confirms the signals exist does
    anything become queueable.
    MEASURED-PAIR REFRAME (2026-08-01, /claim-synthesis, see evidence_quality_note):
    RV(z_world, operating_mode) is RETIRED as the operationalization -- four
    manipulation families (824/824a/838/849) found zero reach from any config lever
    to that pair, and salience_coordinator.py's operating_mode softmax is computed
    from a closed named-scalar precursor set with no wired edge from z_world or any
    landmark/boundary-derived quantity, so no config lever will ever reach it. The
    current operationalization is RV(z_world, z_goal) [primary] / RV(z_world,
    z_harm_a) [fallback] -- both cross-stream (goal-stream / affective-stream vs
    world-model stream, preserving the heterogeneous-streams test), both confirmed
    reachable from the SAME landmark-manipulation lever via a coded single-hop
    vs_rollout_gate.gate_stream call (agent.py ~4895 for z_goal, side="e1",
    gated on goal.e1_goal_conditioned default True; ~8267 for z_harm_a, side="e2").
    Gate any full recording run on a pre-flight reach probe for the new pair first
    (parallel build to q081_pair_reach_check.py, which is hardcoded to
    operating_mode and does not generalise as-is), per the discipline
    Q081-REACH-CHECK-PAIR-SPECIFIC established.
  epistemic_category: standard
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-12_organisational_principles_of_cognition_across_heterogeneous_timescales.md
    - evidence/planning/thought_intake_2026-07-12_organisational_principles_heterogeneous_timescales.md
    - "Shared spatial and temporal principles govern connectome dynamics across timescales, PNAS, doi:10.1073/pnas.2535464123"
  depends_on:
    - SD-006
    - ARC-023
    - ARC-025
    - MECH-288
    - MECH-321
    - MECH-091
  notes: >
    NON-DEGENERACY GUARD: the cross-stream statistic must show live variance across
    arms/seeds AND must be computed against a surrogate null built as a CONSTRAINED
    REALISATION -- one that preserves, per stream, (a) the tick times / configured
    update period, (b) the marginal distribution of the stream's values, and (c) the
    WITHIN-STREAM temporal autocorrelation, destroying only the relation BETWEEN
    streams. The natural construction is a block permutation applied within each
    stream's own tick grid. A bare "rate-matched shuffle" is NOT sufficient and was
    the under-specification this guard originally carried: a shuffle that preserves
    the update period but breaks within-stream serial correlation can be beaten by a
    cross-stream statistic purely because each stream is individually smooth in time
    -- significance achieved by destroying something that was never under test, i.e.
    a FALSE Outcome A. Lancaster et al. 2018 (Physics Reports 748:1-60), the standard
    surrogate-construction reference, states the requirement directly: a test of
    temporal structure needs surrogates maintaining the same autocorrelation function
    as the data. Corollary filter on the STATISTIC: any candidate statistic that is a
    function of the configured update rates is ruled out a priori, before any run.
    VALIDATE THE NULL BEFORE USING IT: the surrogate ensemble must be shown to kill a
    deliberately-artefactual statistic (one computed purely from the update periods)
    while sparing a deliberately-injected real one, and that demonstration must be
    made BEFORE the surrogate adjudicates anything. Surrogate ensembles are cheap in
    simulation, so there is no cost argument for skipping this. Note the unequal-rate
    gap: the reference treats a common regular grid, while REE's streams tick at 1, 3
    and 10 steps, so this surrogate must be DESIGNED for REE rather than looked up --
    the step where an error is easiest to make and hardest to notice.
    STRUCTURE-DESTROYING ARM (required alongside the surrogate, not a substitute for
    it): the run needs an arm that leaves the streams, the configured rates and the
    environmental input statistics intact while removing the event / commitment
    LANDMARK structure -- the analogue of the scrambled-story control in Chang,
    Nastase & Hasson 2022 (PNAS), whose lag gradient vanished under scrambling and
    thereby excluded the intrinsic-rate explanation. The surrogate destroys
    cross-stream alignment in the ANALYSIS; this arm destroys the landmark structure
    in the SYSTEM. A cross-stream statistic that survives it was measuring the clock,
    which is the sharpest available discriminator between Outcome A and Outcome B.
    A result that merely recovers the configured update periods is Outcome B, not a PASS.

    STRUCTURE-DESTROYING (LANDMARK-REMOVAL) ARM BUILT 2026-07-22 (ree-v3 2f0a91e,
    REE_assembly 6e5b20c8c1): experiments/_lib/q081_landmark_removal.py, 35 contracts in
    tests/contracts/test_q081_landmark_removal.py, full suite 2212 passed. It is an
    EXPERIMENT-LAYER arm with NO ree_core change: it wraps hippocampal.event_segmenter.step,
    the single choke point feeding all three live consumers (MECH-288 boundary queue,
    MECH-287 broadcast trigger, MECH-269 anchor install), following the
    consolidation_lesion_harness / V3-EXQ-702 precedent. The primary lever is a YOKED
    PERMUTATION of the intact arm's own boundary train, matched seed-for-seed and
    episode-for-episode, permuting (interval, payload) units jointly, so that boundary
    count, IEI multiset, posterior multiset (= the broadcast drive) and scale mix are
    preserved EXACTLY, not merely distributionally. Suppression was REJECTED as primary
    (it removes the drive as well as the alignment -- a lesion, not a scramble); online
    rate-matched resampling was REJECTED (it imposes geometric IEIs on a train that is
    bursty by construction). CLOSED-LOOP caveat: Chang's stimulus was exogenous, REE's is
    not, so environmental input statistics cannot be preserved by fiat -- they are
    preserved BY CONSTRUCTION at the intervention site and MEASURED downstream against
    pre-registered thresholds; a breach marks the arm CONFOUNDED rather than reportable as
    a null, and behavioural reach is asserted (assert_behavioural_reach), not assumed.
    NOT YET RUNNABLE: the per-step multi-stream recorder is still unlanded on ree-v3 main.
    Design record: evidence/planning/q081_landmark_removal_arm_design.md
    CEILING ON WHAT THE SURROGATE CAN DELIVER: clearing a well-built null is NECESSARY
    for Outcome A and nowhere near sufficient. Wired coordination is real coordination
    and will correctly clear any surrogate test; only the ablation series separates A
    from B. Cross-stream LAG in particular is closer to an Outcome B detector than an
    Outcome A one here -- a lag between streams ticking at 1/3/10 steps is guaranteed
    by the scheduler -- so lag should be reported as a control quantity that must be
    present and must not explain the result, not as evidence for the claim.
    WHY THIS MATTERS BEYOND BOOKKEEPING: if the answer is Outcome C (unrelated), REE has
    an INTEGRATION GAP that its multi-rate substrate decision (SD-006) was assumed to
    have addressed. That is a live risk to SD-006's stated purpose and is currently
    untested. This is a diagnostic gap in the existing claim set, not a new mechanism.
    DO NOT read this as "REE should add multiple timescales" -- REE already has them by
    founding substrate decision, and the raw thought says so twice. The load-bearing
    move is the correction of that obvious reading.
    EXPLICITLY NOT IN SCOPE: any oscillatory / cross-frequency-coupling mechanism (the
    aim is abstract coupling principles, not importing oscillations); and DLIF, which
    the thought deliberately separates itself from -- DLIF as a unifying mathematical
    object is already answered-negative in the registry. Cross-reference as orthogonal,
    DO NOT MERGE.
    NOT on the V3 critical path. The raw thought is explicit that it does not justify
    changing it. First move is the telemetry audit; no experiment proposal minted.
  pending_retest_after_substrate: true
  evidence_quality_note: |
    [2026-07-29 governance, V3-EXQ-824 + V3-EXQ-824a + V3-EXQ-838, all non_contributory;
    failure_autopsy_V3-EXQ-824_2026-07-26 + failure_autopsy_2026-07-28-sweep + failure_autopsy_v3-exq-838_2026-07-29,
    all confirmed]: three consecutive bit-identical-arms results on RV(z_world, operating_mode), each fixing the
    prior run's diagnosed gap and each still failing. 824 (2026-07-26) self-routed weakens but is
    non_contributory: the only enabled consumer flag (use_invalidation_trigger) has no causal reach to
    z_world/operating_mode (source-trace confirmed). 824a (2026-07-26, use_anchor_sets fix): the reach-check
    itself now passes (landmark_arm_behavioural_reach=1.0) yet rv_primary is still bit-identical across all 5
    seeds -- the reach-check confirms SOME causal path to z_world, not reach to the SPECIFIC measured pair.
    838 (2026-07-29, use_per_region_vs=True fix + a distinct temporal-manipulation family): still bit-identical
    across all 5 seeds, refuting use_per_region_vs as sufficient. New substrate entry
    Q081-REACH-CHECK-PAIR-SPECIFIC created (a cheap pre-flight reach probe gating any future full run on the
    SPECIFIC measured pair, not a blanket flag check). Stays candidate; no direction toward the underlying
    question yet -- three consecutive measurement-defect results, not evidence for or against shared organisation.
    [2026-07-31, IGW-20260729-196] Q081-REACH-CHECK-PAIR-SPECIFIC BUILT: ree-v3/experiments/_lib/
    q081_pair_reach_check.py, an experiment-layer (no ree_core change) empirical reach probe -- diffs
    agent.salience._input_signals (the closed precursor set operating_mode is a deterministic function of)
    between an INTACT and the PRIMARY manipulation arm over a short untrained rollout, with a non-degeneracy
    guard (an untrained event segmenter fires boundary events sparsely; a zero-boundary rollout is reported
    degenerate, not "no reach"). Manual runs at non-degenerate defaults (seeds 0, 1) reproduced 824a/838's
    finding again (has_pair_specific_reach=False); a future driver should call this probe before any further
    full recording run rather than repeat 824/824a/838's cost to re-discover the same non_contributory result.
    See substrate_queue.json sd_id Q081-REACH-CHECK-PAIR-SPECIFIC for the full implementation note.

    [2026-08-01 governance, V3-EXQ-849, PASS, evidence_direction not applicable (diagnostic
    reach-scan, not a full recording run)]: the newly-built Q081-REACH-CHECK-PAIR-SPECIFIC probe's
    first real use, across two manipulation families (iei_permute, jitter). 9 of 10 (mode x seed)
    cells cleared the non-degeneracy guard. Result: NO precursor-level reach to operating_mode from
    either lever -- no named salience input signal ever diverged between INTACT and either
    manipulated arm. Combined with 824/824a/838's RV-level nulls (off / anchor-sets-fix /
    per-region-vs-fix), this is now FOUR structurally different manipulation families (plus the two
    REACH_CONSUMERS flags) with zero reach to this specific (z_world, operating_mode) pair. Does NOT
    prove no lever anywhere could reach it -- a direct operating_mode write path (a substrate build,
    not a config lever) remains untested and out of scope for this probe. Recommendation carried
    over from the probe's own interpretation: reframe Q-081's measured pair onto a confirmed-
    reachable one (Outcome A/H2) rather than running another full (z_world, operating_mode)
    recording pass. This is decision-relevant (the 723 rule) -- it makes concrete the next-step
    choice for any future Q-081 driver: call this probe before any full recording run, and if it
    raises again, reframe the DV pair rather than repeat 824/824a/838's cost. Stays candidate;
    still no direction toward the underlying shared-organisation question -- four consecutive
    measurement-defect / no-reach results, not evidence for or against.

    [2026-08-01, /claim-synthesis, evidence/planning/claim_synthesis_Q-081_2026-08-01.md]:
    applied 849's own reframe recommendation. Discrimination-gate finding: this cluster is
    test-design debt (an unreachable DV pair), not granularity debt -- Q-081 is NOT decomposed
    into child claims. Mechanistic cause of the zero-reach run confirmed by reading
    ree_core/cingulate/salience_coordinator.py: operating_mode's precursor set
    (_input_signals: dacc_pe/dacc_foraging/dacc_difficulty/drive_level/is_offline/
    pcc_stability/aic_salience/cea_fast_prime/cea_mode_prior/external_task_drive/
    override_signal/pacc_autonomic) has no wired edge from z_world or landmark structure at
    all -- the four runs were not underpowered, they were pointed at a pair with no causal
    path in the substrate. Grepped every vs_rollout_gate consumer in agent.py to find what
    the SAME manipulation lever (per_stream_vs / per_region_vs staleness) does confirmably
    reach: no side="e3" gate exists anywhere (the substrate_queue implementation_hint's
    hypothesised vs_rollout_gate -> E3 -> operating_mode chain does not exist as a literal
    call path, which is independently why 838 found nothing), but side="e1" gate_stream on
    z_goal (agent.py ~4895, gated on goal.e1_goal_conditioned default True) and side="e2"
    gate_stream on z_harm_a (agent.py ~8267) are both confirmed single-hop causal paths from
    the same staleness statistic, and both are genuinely different subsystems from z_world
    (goal-stream / affective-stream), so both preserve the cross-stream-organisation test
    Q-081 actually asks (unlike gating z_world itself, which would be a within-stream
    tautology). what_would_answer updated to name RV(z_world, z_goal) primary /
    RV(z_world, z_harm_a) fallback, retiring RV(z_world, operating_mode). Direct
    operating_mode WRITE path stays untested and out of scope -- not needed yet, since a
    confirmed-reachable route to the same question exists at far lower cost. Follow-on
    (chipped separately, not built here): /queue-experiment to build a parallel pre-flight
    reach probe for z_goal (q081_pair_reach_check.py is hardcoded to operating_mode and does
    not generalise) before any full recording run.

- id: MECH-466
  title: "Event-relative coordination exceeds clock-relative coordination: cross-stream alignment measured around event boundaries, commitment onset, interruption, completion and offline transitions is stronger than alignment by wall-clock timestep. If so, the boundary stream is the coordination substrate for the whole federation, not merely a detector with a consumer."
  claim_type: mechanism_hypothesis
  subject: architecture.event_relative_temporal_grammar
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  epistemic_category: standard
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-12_organisational_principles_of_cognition_across_heterogeneous_timescales.md
    - evidence/planning/thought_intake_2026-07-12_organisational_principles_heterogeneous_timescales.md
  depends_on:
    - MECH-288
    - MECH-321
    - MECH-091
    - Q-081
  notes: >
    FALSIFIER: an event-triggered-average / alignment statistic computed BOTH
    event-locked and clock-locked on the SAME traces. PASS requires event-locked
    alignment to exceed clock-locked by a margin scaled on the SD of the DELTA plus an
    absolute floor (per the effect-size gate convention -- do not scale noise on the SD
    of either arm alone).
    NON-DEGENERACY GUARD: boundary pulses must be non-degenerate -- boundary rate NOT
    floor- or ceiling-pinned. A segmenter emitting boundaries every step, or never,
    makes the comparison vacuous and self-routes substrate_not_ready.
    This is Outcome E of the Q-081 taxonomy, isolated as its own claim because it is
    cheap, specific and RETROSPECTIVELY TESTABLE: REE already emits every required
    landmark (MECH-288 boundary pulses, the beta commitment gate, mode transitions,
    sleep-phase markers), so no substrate change is needed.
    It SHARPENS MECH-288/MECH-321 from "a detector exists and a consumer uses it" to
    "the boundary stream is the coordination substrate for the federation" -- that
    strengthening is the new content; the detector and consumer are already owned.
    SEQUENCING: gated behind the Q-081 telemetry audit, which confirms the signals are
    recoverable from logs. This is the FIRST candidate in cluster A that could be
    queued, and only after that audit. No experiment proposal minted here for that
    reason.

    2026-08-10 edge correction (chip chip-20260810-phase-consistency-reconciliation):
    depends_on: MECH-298 removed. MECH-466's own text: "REE already emits every required
    landmark (MECH-288 boundary pulses, the beta commitment gate, mode transitions,
    sleep-phase markers), so no substrate change is needed" -- V3-EXQ-831 tested this
    claim on existing landmarks without MECH-298 (an explicitly optional future pull)
    ever being built.
  evidence_quality_note: |
    [2026-07-29 governance, V3-EXQ-831 weakens; failure_autopsy_2026-07-28-sweep, confirmed]: first
    experimental test of MECH-466 (waking-only scope). Event-locked coordination does not exceed
    clock-locked coordination (delta=-0.091 vs a 0.171 floor), with all four non-degeneracy preconditions
    clearing cleanly across 3/3 valid seeds. Clean, self-consistent weakens on the waking-only version of
    the claim -- no further diagnostic work needed. Note for the record: the deferred sleep-phase-transition
    landmark class (Outcome E) is explicitly out of scope here (use_sleep_loop=False), and per 829's
    independent finding (offline sleep-replay consolidation as a candidate load-bearing mechanism for
    MECH-324's rapid-reacquisition prediction), sleep engagement looks increasingly likely to matter across
    this claim family -- a lettered follow-up with use_sleep_loop=True is worth prioritising before MECH-466's
    full (waking+sleep) claim can be considered adequately tested. Stays candidate.

- id: INV-091
  title: "Integration and protected non-equivalence are JOINTLY necessary: cross-stream similarity has a viable band. Too little shared organisation is fragmentation; too much is representational collapse. An adequate architecture must satisfy both constraints at once, with a measurable quantity in between -- not merely satisfy each on a separate axis."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - ARC-025
    - MECH-063
    - MECH-069
    - SD-011
    - MECH-035
  subject: architecture.integration_nonequivalence_band
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  epistemic_category: standard
  pending_retest_after_substrate: true  # 2026-08-01: awaiting INV091-NULL-VALIDATION-RUN-LENGTH amend (828a still under-budget)
  claim_level: architectural
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-12_organisational_principles_of_cognition_across_heterogeneous_timescales.md
    - evidence/planning/thought_intake_2026-07-12_organisational_principles_heterogeneous_timescales.md
  depends_on:
    - ARC-025
    - MECH-063
    - MECH-069
    - SD-010
    - SD-011
    - MECH-035
    - Q-081
  notes: >
    WHAT IS AND IS NOT NEW. Integration (ARC-025 engine irreducibility) and anti-collapse
    (MECH-063 orthogonal tonic/phasic axes, MECH-069 error incommensurability, SD-011
    s/a split, MECH-035 vector-valued valence) are EACH already owned, each on one axis.
    What is NOT owned, and is asserted here, is that an adequate theory must satisfy BOTH
    AT ONCE with a quantity in between -- a BAND of cross-stream similarity that is
    healthy, below which the system is fragmented and above which it has collapsed.
    Outcome D of the Q-081 taxonomy ("too much shared organisation causes collapse") is
    what makes this measurable rather than rhetorical. Do not re-assert the per-axis
    guards; they are cited, not restated.
    FALSIFIER: an ablation series -- remove event broadcasts / remove mode conditioning /
    remove commitment landmarks / remove residue feedback / force lockstep / randomise
    rates / collapse the harm streams -- showing a NON-MONOTONIC relation between
    cross-stream similarity and task plus anti-collapse function. Monotonic improvement
    with similarity refutes the band; monotonic degradation refutes it in the other
    direction.
    NON-DEGENERACY GUARD: at least one ablation must move the similarity statistic
    measurably, or the series tested nothing.
    invariant_type emergent (emergent_from the five per-axis guards it binds together),
    so it IS subject to substrate reconfirmation -- unlike a universal invariant.
    Gated behind the Q-081 telemetry audit like the rest of cluster A.
  evidence_quality_note: |
    [2026-07-29 governance, V3-EXQ-827 non_contributory + V3-EXQ-827a non_contributory + V3-EXQ-828 weakens;
    failure_autopsy_batch-822a-826-817a-827_2026-07-26 + failure_autopsy_2026-07-28-sweep, both confirmed]:
    three runs of the ablation-series falsifier. 827 (2026-07-26) failed its own pre-registered non-degeneracy
    guard: _force_lockstep() collapsed e1/e2/e3 tick rates to 1, confounding tick-count/sampling-density with
    the manipulation -- non_contributory. 827a (2026-07-26, phase-sync redesign fixing the tick-rate confound):
    genuinely fixed, real non-degenerate data on both load-bearing criteria, both FAIL cleanly (decouple ~=
    intact < lockstep, no clean effect) -- non_contributory, standard. 828 (2026-07-27, remaining-ablations
    sweep, 6 arms): intact does not distinguish itself as behaviourally optimal -- residue_off has the best
    composite reward, intact sits statistically mid-pack. Directly weakens the Goldilocks-band prediction.
    Surrogate-null validation was never checked in any of the three (each needed 2-2.8x more steps than run);
    per Q-081's own precedent and user confirmation, the ablation-contrast is treated as the sufficient
    discriminator and the null-validation gap is recorded as a standing design note, not a blocker to the
    weakens verdict. New substrate entry INV091-NULL-VALIDATION-RUN-LENGTH created (bump default run length
    to >=2848 steps for this driver family). Stays candidate.
    [2026-07-31, IGW-20260730-192]: INV091-NULL-VALIDATION-RUN-LENGTH IMPLEMENTED --
    ree-v3/experiments/_lib/inv091_driver_defaults.py provides a standing default eval-phase
    budget (EVAL_EPISODES 10 -> 24, 3600 eval-phase steps, 26% margin over the 2848-step
    worst-case deficit from V3-EXQ-828) for any future successor driver in this family. This
    does not re-score 827/827a/828 or change the weakens verdict above -- it only means the
    NEXT run of this driver family can clear its own null-validation guard. No experiment
    queued yet in this session (out of scope; a /queue-experiment follow-on is chipped).
    Stays candidate.
    [2026-08-01 governance, V3-EXQ-828a, confirmed failure_autopsy_V3-EXQ-828a_2026-08-01,
    null_validation_successor_of V3-EXQ-828]: byte-for-byte identical 6-arm design to 828, eval
    budget raised to the IGW-20260730-192 fix's 3600 steps specifically to let the surrogate
    null-validation check finally run. FAIL, weakens (corroborates 828): C2 peak arm (residue_off)
    interior, margin_vs_max=0.148 -- just 0.002 short of the 0.15 threshold, razor-thin but a
    fresh, higher-seed-count corroborating data point at the same read as 828. Null validation
    STILL `checked: false` -- the surrogate refused again, needing ~14,648 steps worst-case,
    roughly 4x what the 3600-step fix supplied and ~5x the deficit the fix was sized against.
    This is the SECOND consecutive miscalibration of the same run-length estimate (tau_max is
    genuinely variable across seeds/runs; a margin computed from one run's worst case does not
    generalize). Per standing 2026-07-29 precedent, the missing null-validation does not block
    the weakens verdict -- the ablation-contrast remains the accepted discriminator. Stays
    candidate. Route: /implement-substrate -- amend INV091-NULL-VALIDATION-RUN-LENGTH (not a
    third guess-and-bump; recommend either 3-5x headroom over the newly observed 14,648-step
    worst case, or a per-run dynamic pilot pass measuring actual tau_max). pending_retest_after_substrate: true.


    [2026-08-02 governance, V3-EXQ-828b, confirmed failure_autopsy_V3-EXQ-828b_2026-08-02, fourth run in the 827/827a/828/828a/828b lineage, USER-CONFIRMED]: C1 PASS (at least one ablation moves similarity), C2 FAIL -- peak-composite arm (harm_collapse) sits AT the similarity extreme, not interior, margin_vs_max=0.000 by construction. Third independent corroborating weakens data point; the specific winning arm now differs from 828/828a's (residue_off, interior, razor-thin 0.148-vs-0.15 miss) -- itself informative against a robust band structure. Null-validation surrogate refused for the THIRD consecutive attempt (static-undersized -> static-bumped-still-5x-short -> per-arm-per-seed dynamic pilot with 2x margin, still refused, worst-case seed needing ~138,600 steps vs tau_max~79 observed) -- a short pilot may be structurally unable to characterize the slow-timescale statistic it needs to bound. Formally closed as aleatoric (irreducible within reasonable compute budget) per the CLAUDE.md work-graph debt vocabulary; relying on the C1/C2 ablation-contrast alone per the existing 2026-07-29 governance precedent. The C1/C2 weakens verdict stands independent of the null-validation infra question. The existing INV091-NULL-VALIDATION-RUN-LENGTH substrate_queue entry is being marked closed/aleatoric rather than left open for a 4th attempt.
- id: ARC-112
  title: "REE is a FEDERATION / heterarchy of asynchronous, partially independent cognitive streams coordinated by shared organisational constraints -- neither a monolith nor a collection of independent modules. Successful integration preserves functionally necessary differences among streams rather than making their dynamics identical."
  claim_type: architectural_commitment
  subject: architecture.federation_heterarchy_framing
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  epistemic_category: standard
  claim_level: architectural
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-12_organisational_principles_of_cognition_across_heterogeneous_timescales.md
    - evidence/planning/thought_intake_2026-07-12_organisational_principles_heterogeneous_timescales.md
  depends_on:
    - ARC-025
    - MECH-063
    - SD-006
    - SD-010
    - SD-011
  notes: >
    FRAMING CLAIM, registered as the PARENT NODE for Q-081 / MECH-466 / INV-091. The
    intake's registration condition was explicit -- "register only if it earns its keep
    as the parent node for 1-3; otherwise leave as intake prose" -- and it is registered
    here because those three ARE registered and it is the frame that makes them one
    programme rather than three unrelated probes.
    ARGUABLY IMPLIED by ARC-025 + MECH-063 + SD-010/SD-011 taken together, but nowhere
    stated as a claim. That is the whole of its novelty; it introduces no mechanism.
    THE ANTI-COLLAPSE QUALIFICATION IS LOAD-BEARING and must travel with the framing:
    integration that makes stream dynamics identical is collapse, not success. Without
    it "federation" degrades into an argument for more coupling, which is the failure
    INV-091 exists to bound.
    LOWER PRIORITY than its three children. It is a frame, not a falsifier; do not queue
    an experiment against it directly -- it is answered by whatever Q-081, MECH-466 and
    INV-091 return.
    NOT a proposal to add heterogeneous timescales (already owned, SD-006/ARC-023), and
    NOT a DLIF merge (orthogonal, answered-negative as a unifying mathematical object).

- id: ARC-120
  title: "Behavioural/write authority in REE is (and should remain) EARNED through demonstrated competence, never granted merely because a computation or mechanism exists: REE's existing authority-gating mechanisms are independently-discovered instances of one general developmental sequence -- existence -> representation -> competence -> authority -> behavioural influence -- rather than unrelated ad hoc gates."
  claim_type: architectural_commitment
  subject: architecture.competence_before_authority_framing
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-06
    needs_review: false
  epistemic_category: standard
  claim_level: architectural
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-08-06"
  source:
    - docs/thoughts/2026-08-06_scientific_evolution_of_ree.md
  depends_on:
    - ARC-107    # BG-mirroring eligibility-gating constitution: committed-action authority passes through an earned eligibility mask
    - SD-032b    # dACC/aMCC adaptive-control: behavioural-adjustment authority written to a learnable, not fixed, store
    - MECH-261   # mode-conditioned write gating: which substrates may write is earned by operating-mode state, not innate
    - MECH-094   # simulation-vs-real hypothesis-tag: simulated content is denied committed/write authority until tagged real
  notes: >
    FRAMING CLAIM from the 2026-08-06 historical-archaeology thought intake (Observation
    4: "competence consistently precedes authority"). ARGUABLY IMPLIED by the depends_on
    set taken together -- each already gates some form of authority (write, commitment,
    selection, consolidation-rate) on a competence-like precondition (per-event
    eligibility, map stability, operating mode, real-vs-simulated tagging) -- but nowhere
    stated as one general architectural principle before this intake. That generalisation
    is the whole of its novelty; it introduces no new mechanism.
    LOWER PRIORITY than its instances. Not directly falsifiable by a single experiment;
    it is a framing over already-registered mechanisms and is falsified in the design
    sense by any future accepted mechanism that grants behavioural or write authority
    WITHOUT a competence-like gate.
    Distinct from MECH-457's uninformative-value-baseline finding and the broader
    competence-acquisition/retention cluster (MECH-457, competence_retention_dissociable_
    from_acquisition) -- those are about HOW HARD competence is to install and keep; this
    claim is about what authority is CONDITIONED ON once competence exists.

    2026-08-10 edge correction (chip chip-20260810-phase-consistency-reconciliation):
    depends_on: MECH-368 and INV-039 removed. Both are EVIDENTIARY INSTANCES this framing
    generalizes over (like the remaining 4 deps), not build prerequisites -- ARC-120
    "introduces no new mechanism" of its own (own text above) and has nothing to build.

- id: ARC-121
  title: "REE's mechanisms increasingly converge on maintaining and consuming a SHARED epistemic-state object (knowledge / belief / uncertainty / possibility / commitment / action) rather than each existing as an isolated computational function: prediction, replay, harm/ethics evaluation, planning and learning read and update a common epistemic relationship rather than independent local state."
  claim_type: architectural_commitment
  subject: architecture.epistemic_state_centrality_framing
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-06
    needs_review: false
  epistemic_category: standard
  claim_level: architectural
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-08-06"
  source:
    - docs/thoughts/2026-08-06_scientific_evolution_of_ree.md
  depends_on:
    - MECH-482   # epistemic_deficit accumulator: a persistent, target-bound belief/uncertainty object
    - MECH-483   # orient/survey: a behavioural regime driven directly by the shared epistemic_deficit object
    - SD-034     # closure operator over committed rule_state: commitment as a first-class latched/released object
    - MECH-094   # hypothesis-tag: a belief/simulation-vs-real distinction consumed across replay, rule-arbitration and relief pathways
  notes: >
    FRAMING CLAIM from the 2026-08-06 historical-archaeology thought intake (Observation
    3: "epistemic state appears to be the central computational object"). Each dependency
    already treats belief/uncertainty/commitment as a first-class object LOCALLY; the
    novelty is the cross-mechanism generalisation that these are converging on ONE shared
    kind of object rather than independently-invented local state, which is nowhere
    stated as a claim before this intake.
    NOT a proposal for SD-062's claims-index-as-structured-uncertainty-graph (that graph
    formalises REE's OWN research-process epistemics about its claims -- a different
    level of description) and NOT a proposal for Q-079's dynamic latent-scale inference
    field / DLIF mathematical object (a specific technical substrate this framing would
    motivate but does not itself specify) -- both noted as adjacent and orthogonal, not
    merged, mirroring ARC-112's own DLIF disclaimer.
    LOWER PRIORITY than its instances; not directly falsifiable by a single experiment.
    Falsified in the design sense if REE's future substrate development keeps adding
    local, mechanism-private belief/uncertainty representations rather than converging
    toward anything approximating one shared epistemic-state object.

# --- Cluster B: goal relevance, salience, distractor suppression -------------

- id: MECH-467
  title: "Distractor resistance has THREE dissociable failure modes -- sensory capture (distractor enters/dominates the active representation), rule corruption (distractor overwrites or destabilises the active goal/rule/task context), and behavioural capture (rule survives intact but the distractor still controls action selection). REE's distractor evidence covers rule corruption ONLY; a system can pass rule-corruption tests while failing behavioural capture."
  claim_type: mechanism_hypothesis
  subject: attention.distractor_failure_mode_dissociation
  polarity: asserts
  status: candidate
  pending_retest_after_substrate: false
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  epistemic_category: standard
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-12_goal_relevance_salience_and_distractor_suppression.md
    - evidence/planning/thought_intake_2026-07-12_goal_relevance_salience_distractor_suppression.md
  depends_on:
    - MECH-262
    - SD-033a
    - MECH-261
  notes: >
    THE COVERAGE GAP, which is the cheapest genuinely-new thing in this intake. REE's
    distractor evidence (V3-EXQ-484 salience-coordinator gating protecting rule state
    from replay drift; MECH-262 rule-selective distractor-resistant persistence) is
    RULE CORRUPTION ONLY. Legs 1 and 3 are untested. Leg 3 matters most because it is
    precisely the failure that RULE-STATE PROTECTION CANNOT CATCH BY CONSTRUCTION -- an
    intact rule that fails to steer action is invisible to a rule-drift metric.
    FALSIFIER: a distractor battery instrumented for all three legs SEPARATELY --
    sensory-capture rate (distractor present in active representation), rule-state drift,
    and wrong-target selection rate WITH RULE VERIFIED INTACT. PASS for dissociability
    requires at least one condition where rule drift is at floor while wrong-target
    selection is elevated.
    NON-DEGENERACY GUARD: the distractor must actually be REGISTERED by the system --
    non-zero sensory-capture rate in at least one arm. A distractor the agent never
    encodes tests nothing, and an all-floor battery self-routes substrate_not_ready
    rather than returning a verdict.
    OWNERSHIP: this claim is the new owner for legs 1 and 3 of the noisy-environment
    distractor benchmark family; MECH-262 continues to own rule corruption. That answers
    the raw thought's open question 7.
    SEQUENCING CAUTION: the during-commitment distractor timing arm depends on stable
    multi-step commitment, which the substrate cannot currently sustain. Scope the
    battery to pre-commitment and during-replay/planning arms; EXCLUDE the
    during-commitment arm until commitment behaviour is stable. See EXP-0398.
    STANDING CONTAINMENT RULE, still in force from the 2026-06-04 attention analysis:
    REE-v3 attention is DISTRIBUTED precision-selection control, not a missing module.
    Do NOT add a generic attention module and do NOT expand the V3 green-board closure
    path. A new attention substrate is justified only if an experiment exposes a
    SPECIFIC failure. This claim is a diagnostic over existing substrate, not a build.
    The goal-relevance-vs-salience distinction is ALREADY architecturally instantiated
    (z_goal / z_resource / predictive wanting / affordance bias vs salience coordinator /
    operating mode / dACC / urgency interrupt / mode hysteresis) -- a glossary entry, not
    a claim.
  evidence_quality_note: >
    [2026-08-21 governance, gov-20260821-0203, confirmed failure_autopsy_940-941-mech467-cluster_2026-08-20.json]: V3-EXQ-941 (GOV-FANOUT-1 H-denominator leg, measurement axis) is a denominator diagnostic with zero wrong-target instrumentation and does not exercise MECH-467's falsifier; it bears on the claim in neither direction. Its FAIL is the INFORMATIVE result by design (the queue note says so explicitly: outcome PASS would have been the null). It localises the missing denominator upstream of arrival: 6 approach initiations, 2 arrivals and 4 consumption events over 3681 realised ticks, with approach_run_length_max = 1 in both arms -- no approach run ever exceeded a single step. Two caveats bound how far the finding travels. (1) The basis is thin: seed 43 sat fully immobile for 900/900 ticks in BOTH arms, contributing 48.9% of the denominator and zero signal, and its aliasing was meant to be caught by the approach_is_definable precondition whose 0.0 floor cannot fire (mean_distance_to_goal was exactly 1.0, one step from a target on a grid where one step is arrival). (2) C2 (0.333333) and C3 (4) are stamped passed but rest on <=6 events; C3 as coded is a pooled existence test (total_events > 0) across both arms despite owned_by_arms ARM_PRECOMMIT, and consumption exceeds arrivals 2:1 with 2 of the 4 events in cells recording zero initiations and zero arrivals -- so it certifies only that >=1 consumption happened somewhere, and the denominator_lost_at_consumption branch is unreachable in practice on this env. Neither affected the verdict (the routing tests C1 first). The run also newly quantifies the immobility at battery level: pooled_move_efficacy 0.0115/0.0124 and e3_tick_fraction_mean ~0.104. Its claim to 'confirm' the MECH-439 F-dominance ceiling should be read as 'is consistent with and quantifies', not as independent confirmation: at 98.8% blocked moves and 89-91% latched ticks both are near-ceiling simultaneously and cannot decompose the immobility, and no selection-layer measurement was taken.

    [2026-08-03 governance, V3-EXQ-874, confirmed failure_autopsy_V3-EXQ-874_2026-08-03, first
    experimental test]: legs (a) sensory-capture and (b) rule-drift were fairly and informatively
    tested (clear dissociation between ARM_PRECOMMIT drift ~0.22 and ARM_REPLAY drift ~0.015). Leg
    (c) (conditioned wrong-target selection, the failure mode rule-state protection cannot catch by
    construction) has a 0/0 event denominator in all 6 cells (3 seeds x 2 arms) -- zero
    target-consumption events of either kind, so the wrong-target-selection rate is undefined rather
    than measured low. non_contributory/measurement_test_design_defect (legs a/b stand as genuine,
    informative evidence; leg c was simply never reachable at this config). Route: /queue-experiment
    with a battery design that guarantees target-consumption opportunities occur (denser targets
    and/or a longer horizon) before drawing any conclusion about leg (c) dissociability.

    2026-08-10 edge correction (chip chip-20260810-phase-consistency-reconciliation):
    depends_on: MECH-254 removed. MECH-467's own STANDING CONTAINMENT RULE ("REE-v3
    attention is DISTRIBUTED precision-selection control, not a missing module... A new
    attention substrate is justified only if an experiment exposes a SPECIFIC failure")
    already disclaims needing MECH-254's v4 boundary-gate implementation; V3-EXQ-874
    tested legs (a)/(b) without it.

    2026-08-18 (governance apply, V3-EXQ-874b, confirmed
    failure_autopsy_V3-EXQ-874b_2026-08-17, second experimental test): non_contributory /
    standard. The leg-(c) redesign FAILED to produce a denominator -- 1/3/4/0 pooled
    consumption events per arm against a pre-registered floor of 15, all four arms gate-RED,
    non_degenerate false. Nothing weighs against the claim. The redesign's OWN hypothesis is
    REFUTED and that is the informative result: 874b took the arena to 6x6 at ~3x resource
    density on a probe measuring 0.0100 events/tick and realised 0.00117 -- 8.5x worse at
    double the density -- so arena geometry was NOT the binding constraint and the
    V3-EXQ-874 commitment-deficit hypothesis is re-implicated. Three defects the manifest
    does not report: (1) 7 of 12 cells terminated early on health_depleted, so only 63.1% of
    the pre-registered exposure window was realised and no cell records it; (2) the rule-
    set-complexity axis is confounded with nutritive density (COMPLEX realised 41% of its
    window vs SIMPLE's 85%); (3) z_goal_stream.writer_defect true is a FALSE POSITIVE -- the
    accumulator observed the P0 base agents, not the 12 stepping clones, and
    goal_live_at_warmup_end is true in 9/12 cells. At observed rates the floor needs
    8380-17200 ticks/arm against 2700 budgeted, so lengthening the window alone cannot
    rescue this design. Failure-location MIXED, not chargeable to REE. No substrate_queue
    entry was minted, deliberately: four live hypotheses explain the missing denominator and
    naming a build now would pick one before the discrimination has been run, which is the
    error GOV-FANOUT-1 exists to prevent. Routed /queue-experiment as a GOV-FANOUT-1 4-leg
    portfolio (process / environment / measurement / selection), NOT another lettered re-
    pose of the same design. Status stays candidate.

- id: INV-092
  title: "Distractor suppression must remain SELECTIVELY PERMEABLE to harm and other-agent signals (ethical interruptibility): any suppression mechanism must preserve the ability of sufficiently important weak evidence to interrupt an established rule or commitment. Suppression strength that improves goal completion by silencing weak harm cues is a FAILURE, not a success -- over-suppression is a safety failure mode, not merely a rigidity cost."
  claim_type: invariant
  invariant_type: universal
  subject: ethics.suppression_interruptibility_permeability
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  epistemic_category: standard
  claim_level: governance
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-12_goal_relevance_salience_and_distractor_suppression.md
    - evidence/planning/thought_intake_2026-07-12_goal_relevance_salience_distractor_suppression.md
  depends_on:
    - SD-010
    - SD-011
    - ARC-027
    - EXT-009
    - MECH-467
    - INV-093
  notes: >
    SIBLING CLAIM: INV-093 (skill optimisation must not trade harm sensitivity for
    competence, from the 2026-07-19 conservative-skill-refinement intake). THESE TWO ARE
    THE SAME ETHICAL PRINCIPLE ON TWO DIFFERENT CONTROL SURFACES -- attention/suppression
    here, learning/refinement there -- and were deliberately registered as siblings.
    Read and revise them together; a change to one that is not mirrored in the other is
    almost certainly an error.
    CROSS-LISTED with the ethics perimeter (SENT-*/GOV-*), NOT filed only under
    attention. The intake is explicit that this must be visible from the ethics perimeter
    plan.
    AN ADEQUATE SUPPRESSOR MUST DISTINGUISH FOUR THINGS that all present as "input
    competing with the current goal": irrelevant distraction (suppress); legitimate
    interruption (allow); urgent harm signal (must override); another agent's state
    becoming newly relevant (must override, and is the hardest).
    FALSIFIER: paired measurement across a suppression-strength sweep -- goal completion
    under distractor load AND interrupt latency/rate for an INJECTED weak harm or
    other-agent-urgency signal. FAIL if the suppression setting that MAXIMISES completion
    also measurably DEGRADES harm interruptibility.
    NON-DEGENERACY GUARD: the harm-interrupt channel must be demonstrably LIVE at zero
    suppression -- baseline interrupt rate well above floor. A dead channel cannot be
    shown to be preserved.
    ACCEPTANCE SHAPE for any future suppression mechanism: improved goal completion under
    distractor load AND preserved interruptibility by harm/other-agent signals, measured
    JOINTLY. A single-axis attention score CANNOT express this, which is why the
    measurement panel must not be collapsed to one number.
    invariant_type universal: it constrains any suppression mechanism by design and is
    not emergent from a particular substrate SD. Currently a constraint on a FUTURE
    mechanism -- no suppression-strength knob exists to sweep yet, so the falsifier
    becomes runnable when one does.

- id: Q-082
  title: "Does REE require an EXPLICIT pre-selection suppression substrate, or do existing precision-weighted cue routing plus the MECH-254 top-k boundary bottleneck already suffice? REE's distractor mechanisms are predominantly POST-ENTRY (write gating, persistence, conflict handling, mode hysteresis); the brainstem/superior-colliculus evidence points at suppression of competing orienting or target-selection signals BEFORE they acquire behavioural control."
  claim_type: open_question
  subject: attention.pre_selection_suppression_substrate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-22
    needs_review: false
  what_would_answer: >
    PROBE-GATED, and the probe is MECH-467 leg 3 (behavioural capture). ONLY a
    behavioural-capture failure that SURVIVES ablation of the existing mechanisms
    (precision-weighted cue routing ARC-005/MECH-251, the MECH-254 top-k bottleneck,
    mode-conditioned write gating MECH-261) would justify a pre-selection substrate.
    Absent that, the default answer is "existing mechanisms sufficient".
    The load-bearing literature question, which determines whether this is even a
    DISTINCT architectural level or a re-description of MECH-254's bottleneck: in the
    brainstem / superior-colliculus competition results, AT WHAT STAGE does suppression
    occur -- sensory representation, orienting command, target competition, or motor
    output? Route to /lit-pull.
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-12_goal_relevance_salience_and_distractor_suppression.md
    - evidence/planning/thought_intake_2026-07-12_goal_relevance_salience_distractor_suppression.md
  depends_on:
    - MECH-467
    - MECH-254
    - ARC-005
    - MECH-251
    - SD-057
    - MECH-347
  notes: >
    REGISTERED AS A GATED OPEN QUESTION, NOT AS AN ASSERTION, and explicitly gated on
    MECH-467 leg 3. DO NOT BUILD. epistemic_category substrate_conditional set
    EXPLICITLY (overriding the open_question -> answer_state default) so the
    narrow_open_question rule does not fire -- this is a probe-gated question awaiting a
    specific failure, not a V3-tractable one to narrow by experiment.
    complex (probe-gated) in the work-graph debt vocabulary: the gate is a REE diagnostic
    (MECH-467 leg 3), not a fact to look up and not a buildable task.
    EXPLICITLY NOT PROPOSED, by both the raw thought and the standing containment rule:
    a general attention architecture, a named distractor-suppression module, or any
    change to the V3 critical path.
    DO NOT QUEUE AN EXPERIMENT AGAINST THIS. It is answered, if at all, by MECH-467's
    battery plus the ablation of existing mechanisms -- not by a probe of its own.

# --- Cluster C: rule apprehension as an organisational principle -------------

- id: ARC-113
  title: "The rule pipeline is an ORDERED CYCLE of non-collapsible stages: experience -> pattern sensitivity -> candidate regularity apprehension -> representation -> prediction -> counterfactual simulation -> behavioural interaction -> outcome comparison -> regularity refinement -> generalisation -> long-term integration. Each stage has claims; the CYCLE does not, and the stages must not collapse into a single reasoning module."
  claim_type: architectural_commitment
  subject: reasoning.rule_pipeline_stage_noncollapsibility
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  epistemic_category: standard
  claim_level: architectural
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-14_rule_apprehension_as_an_organisational_principle_of_cognition.md
    - evidence/planning/thought_intake_2026-07-14_rule_apprehension_organisational_principle.md
    - "MIT Brain and Cognitive Sciences, Separating Logic and Language (convergent evidence, not the origin of the proposal)"
  depends_on:
    - ARC-062
    - ARC-063
    - MECH-338
    - MECH-316
    - ARC-069
    - ARC-070
    - ARC-071
    - ARC-025
    - MECH-069
  notes: >
    *** GATED: DO NOT QUEUE THE ABLATION WHILE ARC-062 GAP-B IS UNRESOLVED. ***
    Rule-SELECTIVE behaviour is untestable until a rule-creator emits DIFFERENTIATED
    (non-monomodal) rule state, and the monostrategy-collapse ceiling is the standing
    obstacle. An ablation queued now would inherit exactly that degeneracy and return a
    vacuous null. The release condition is GAP-B yielding differentiated rule state.
    This claim is REGISTERED as testable-but-gated, and no experiment proposal is minted.
    FALSIFIER SHAPE: stage ablation -- removing or short-circuiting ONE stage should
    produce a CHARACTERISTIC failure signature distinct from the others (e.g.
    apprehension-without-testing yields elegant-but-unvalidated regularities;
    testing-without-refinement yields repeated identical failures). PASS requires the
    failure signatures to be DISSOCIABLE, not merely "performance drops" -- an
    undifferentiated performance decrement across every ablation refutes the ordered-cycle
    reading rather than supporting it.
    NON-DEGENERACY GUARD: the intact baseline must show LIVE VARIANCE on the
    discriminating metric FOR EACH STAGE. A pipeline where no stage measurably
    contributes has nothing to dissociate.
    WHAT IS NEW: the stages are largely instantiated already (ARC-062 weak-reading gated
    policy; ARC-063 distributed CandidateRule field, V4-deferred; MECH-338 cue-driven
    context-bound retrieval; MECH-262/SD-033a rule-selective persistence; MECH-261 write
    gating; MECH-316/ARC-064 cross-episode regularity; MECH-211/166/429/INV-039 schema
    abstraction; MECH-319 simulation-mode rule-write gating; SD-003/ARC-092 counterfactual
    simulation; ARC-069/070/071 policy composition). Do NOT re-register them. What is new
    is the ORDERED CYCLE plus the non-collapse requirement -- the same shape as ARC-025
    (engine irreducibility) and MECH-069 (error incommensurability), never asserted for
    the rule pipeline.
    NOTE ON PROVENANCE: this thought ARRIVES AFTER ARC-062/063 (which were registered and
    already deep in GAP-B falsifier work by mid-June) and generalises them; it was NOT
    discharged into that line. No file in REE_assembly cited it before this registration.
    FIRST MOVE IS AN AUDIT, NOT AN EXPERIMENT: which rule-pipeline stages are implemented,
    which planned, which absent -- as a table against ARC-062/063 / MECH-338 / MECH-316 /
    ARC-069-071. complicated (buildable), and the precondition for designing the ablation.

- id: Q-083
  title: "Is rule apprehension DOMAIN-GENERAL or domain-specific? Does REE need separate regularity-apprehension systems per domain (causal / social / ethical / motor / spatial / relational), or one domain-general apprehender that inherits domain structure from its inputs? ARC-062/063 are scoped to policy-layer behavioural regimes; the domain-agnosticism question is wider and unasked."
  claim_type: open_question
  subject: reasoning.rule_apprehension_domain_generality
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-22
    needs_review: false
  what_would_answer: >
    CROSS-DOMAIN TRANSFER: a rule apprehended in one domain should (if domain-general)
    or should not (if domain-specific) accelerate apprehension in another under MATCHED
    STRUCTURE. The matched-structure requirement is what makes this a test of the
    apprehender rather than of surface similarity.
    NON-DEGENERACY PRECONDITION: single-domain apprehension must FIRST be demonstrated
    above floor in BOTH domains SEPARATELY. Transfer between two domains in neither of
    which the agent can apprehend a rule is uninterpretable.
    SUBSTRATE-BLOCKED: V3 has only one real domain, so the transfer test has no second
    domain to run in. Answering this requires a genuinely multi-domain substrate.
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-14_rule_apprehension_as_an_organisational_principle_of_cognition.md
    - evidence/planning/thought_intake_2026-07-14_rule_apprehension_organisational_principle.md
  depends_on:
    - ARC-062
    - ARC-063
    - MECH-316
    - MECH-211
    - ARC-113
  notes: >
    SUBSTRATE-BLOCKED -- REGISTER SCOPED, DO NOT DESIGN. V3 has one domain; there is
    nothing to transfer between. epistemic_category substrate_conditional set EXPLICITLY
    so narrow_open_question does not fire. DO NOT QUEUE AN EXPERIMENT AGAINST THIS IN V3.
    "RULE APPREHENSION" IS DELIBERATELY BROADER THAN INDUCTION and this breadth is the
    point: induction already implies a formal reasoning framework, whereas rule
    apprehension is the recognition that the world appears to contain a REUSABLE
    REGULARITY. It spans physical dynamics, spatial relations, motor control, social
    interaction, emotional behaviour, ethics, language and mathematics, with the
    architecture AGNOSTIC regarding domain. That agnosticism is precisely what is
    untested and is the genuine architectural fork registered here.

- id: ARC-114
  title: "Language is an INTERFACE to reasoning, not its substrate: reasoning-like behaviour emerging from language alone (LLMs) does not establish language as the native substrate of reasoning. Language functions as compression, communication, coordination, long-horizon scaffolding, working-memory support and explanation -- and may COMPENSATE for weaknesses in other reasoning systems."
  claim_type: architectural_commitment
  subject: reasoning.language_as_interface_not_substrate
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-22
    needs_review: false
  epistemic_category: substrate_conditional
  claim_level: architectural
  implementation_phase: v4
  version_relevance: v4_v5
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-14_rule_apprehension_as_an_organisational_principle_of_cognition.md
    - evidence/planning/thought_intake_2026-07-14_rule_apprehension_organisational_principle.md
    - "MIT Brain and Cognitive Sciences, Separating Logic and Language -- evidence that formal logical reasoning and language are at least partially dissociable"
  depends_on:
    - ARC-113
    - Q-083
  notes: >
    REE POSITIONING CLAIM THAT THE PROGRAMME NEEDS AND LACKS. REE's entire bet is a
    NON-LINGUISTIC substrate, and the obvious external objection -- "LLMs reason, so
    language suffices" -- currently has NO REGISTERED ANSWER. This supplies one, and the
    MIT logic/language dissociation is the external anchor that makes it more than a
    stance.
    FALSIFIER SHAPE -- THE COMPENSATION LEG IS THE TESTABLE ONE: if language is a
    COMPENSATOR, a system with a WEAKENED non-linguistic reasoning stage should show
    GREATER reliance on linguistic scaffolding, not merely worse performance. That
    directional prediction is what distinguishes the claim from an untestable preference;
    "worse performance" alone is consistent with language being the substrate.
    SUBSTRATE-BLOCKED in V3: no language stream exists. REGISTER SCOPED, DO NOT DESIGN,
    DO NOT QUEUE IN V3.
    LITERATURE: the load-bearing /lit-pull family is language and reasoning / language as
    cognitive scaffold / private speech / language compensation. NOTE there is an existing
    UNPROCESSED thought on private speech and externalised DMN that should be pulled in
    the SAME pass rather than separately. The language thought cluster
    (2026-02-09_language.md, 2026-06-23_language_as_cooperation_interface.md) is a
    THOUGHT-level cross-reference only -- those are themselves still unprocessed, so this
    is not yet a claim-level link.
    Consider registering the MIT source as a research_anchor / EXT-* if it is to be
    citable as evidence rather than as provenance.

- id: GOV-ANALOGY-1
  title: "REE:Assembly analogies must be LABELLED as analogies. REE (the cognitive architecture) and REE Assembly (the scientific-development and governance machinery) have strong structural parallels; any analogy between them must be marked as an analogy and must NEVER be used as evidence that REE operates the way Assembly does, or vice versa."
  claim_type: governance_rule
  subject: governance.epistemics.ree_assembly_analogy_discipline
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  epistemic_category: governance_rule
  claim_level: governance
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-14_rule_apprehension_as_an_organisational_principle_of_cognition.md
    - evidence/planning/thought_intake_2026-07-14_rule_apprehension_organisational_principle.md
  depends_on:
    - INV-077
    - GOV-PROC-1
    - GOV-HELDOUT-1
    - GOV-STRAT-1
  notes: >
    ORIGIN AND HOMING. This is the rule-apprehension intake's candidate 4, which that
    intake declined to register on its own ("fold into the intake/digestion discipline
    notes rather than the registry -- UNLESS the 2026-07-19 Assembly governance-rule
    cluster gives it a natural home, in which case register it there"). That cluster
    (GOV-HELDOUT-1 / GOV-STRAT-1 / GOV-REJECT-1 / GOV-META-1) IS registered in this same
    pass, so the condition is met and it is homed here rather than left as prose.
    WHY IT IS LOAD-BEARING RIGHT NOW: this registration pass itself registers a
    cognitive-architecture cluster (C: rule apprehension) and a governance cluster (F:
    Assembly optimisation) that make structurally similar claims about bounded revision,
    rejected-change memory and authority stratification. That similarity is an ANALOGY.
    It is not evidence. GOV-STRAT-1 explicitly notes the INV-020 symmetry -- "the Assembly
    should obey the invariant it demands of the architecture" -- and that observation is
    exactly the kind of statement this rule requires be marked as analogical rather than
    treated as a finding about either system.
    The same discipline applies to the E/F split of the SkillOpt source: E (MECH-471..474,
    INV-093) and F (this cluster) share a vocabulary, NOT a body of evidence. An
    experimental result in one NEVER transfers to the other.
    STANDING DISCIPLINE, not an empirical claim -- there is no falsifier because it
    constrains how evidence may be cited, not what is true of any system.
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: this is a governance_rule, not an empirical claim -- there
    is no experimental falsifier (see notes above). This is a COMPLIANCE-AUDIT condition,
    checkable by inspecting claims.yaml / docs / thought-intake artefacts for citation
    discipline, not by any REE experiment.

    VIOLATED-BY: an artefact (claim note, architecture doc, thought-intake writeup,
    skill/CLAUDE.md text) draws a structural parallel between REE (the cognitive
    architecture) and REE Assembly (the governance/development machinery) and then uses that
    parallel AS EVIDENCE -- treats a finding, invariant, or piece of experimental support in
    one system as transferring to the other -- without explicitly flagging the parallel as
    an analogy. Concretely: (i) citing an architecture-side result to justify adopting or
    strengthening an Assembly-side governance rule, or vice versa, without a disclaimer that
    the resemblance is rhetorical/structural only; (ii) a claim's confidence, status, or
    evidence_direction being influenced by cross-system resemblance rather than by evidence
    proper to that system; (iii) merging or conflating an architecture-cluster claim with an
    Assembly-cluster claim on analogy grounds alone (the E/F SkillOpt split is the named
    example of what NOT to do).

    COMPLIANT: the parallel is drawn but explicitly labelled as an analogy and explicitly
    denied evidential force -- e.g. GOV-STRAT-1's own notes: "the INV-020 symmetry is an
    ANALOGY and MUST BE CITED AS ONE ... It is a reason to ADOPT this rule; it is NOT
    evidence that the Assembly behaves like the architecture, nor that INV-020's
    experimental support transfers here. It does not." The same pattern in GOV-HELDOUT-1
    and GOV-REJECT-1.

    AUDIT METHOD: grep claims.yaml / docs/ for REE<->Assembly structural comparisons and
    check each hit either (a) carries an explicit analogy disclaimer / cites GOV-ANALOGY-1,
    or (b) is a genuine biological/architectural analogy unrelated to REE:Assembly (e.g.
    "BLA analogue" -- out of scope for this rule).

    [2026-08-08 thought-digestion]: audit run against the four claims that draw this
    parallel (GOV-STRAT-1, GOV-HELDOUT-1, GOV-REJECT-1, GOV-META-1) -- all four already
    cite GOV-ANALOGY-1 in depends_on and carry the compliant disclaimer. No violation found.

# --- Cluster D: multiple relational graph organisations, hippocampal ---------

- id: MECH-468
  title: "Hippocampal anchor TOPOLOGY carries functional information not present in local anchor payloads. REE's typed relations (spatial, temporal, action-transition, event, goal, valence, social, schema, staleness, scale) are each realised as a payload field, a gate, or a scoring term -- a property of an anchor or proposal -- and none is exposed as an EDGE TYPE over the anchor set. Nothing tests whether the topology carries information beyond local anchor features."
  claim_type: mechanism_hypothesis
  subject: hippocampus.anchor_topology_functional_information
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  epistemic_category: standard
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-19_multiple_relational_graph_organisations_in_hippocampal_cognition.md
    - evidence/planning/thought_intake_2026-07-19_multiple_relational_graph_organisations_hippocampal.md
    - "A Coding Implementation on Spatial Graph Neural Networks for Urban Function Inference (city2graph / OSMnx / PyG) -- software inspiration, NOT primary biological evidence"
  depends_on:
    - MECH-238
    - MECH-239
    - MECH-288
    - SD-004
    - SD-039
    - MECH-292
    - MECH-293
    - ARC-036
    - SD-014
    - MECH-269
    - MECH-283
    - MECH-284
  notes: >
    *** THE ORGANISATIONAL / REPRESENTATIONAL SEPARATION IS THE GUARD THAT KEEPS THIS
    HONEST, AND IT MUST TRAVEL WITH EVERY DOWNSTREAM USE. *** Organisational claim:
    cognition depends on several distinct relational structures. Representational claim:
    those structures should be implemented as explicit graphs. THE FIRST MAY BE TRUE EVEN
    IF THE SECOND IS FALSE. This claim asserts only the first, and only as a readout.
    EXPLICITLY NOT PROPOSED: replacing the hippocampal representation with a GNN (the raw
    thought forbids it in terms); asserting the brain stores software-style graph data
    structures; merging with the DLIF line (the relationship is a PIPELINE BOUNDARY --
    distributed latent dynamics -> anchors -> typed relational projections -> functional
    inference -> action -- and useful projections would NOT establish that the native
    substrate is graph-like; DLIF as a unifying mathematical object is already
    answered-negative in the registry).
    FALSIFIER: build typed projections over EXISTING anchor traces -- A latent proximity,
    B action transition, C shared event, D shared goal/valence, E causal/outcome, F
    heterogeneous typed -- and predict held-out functional labels (goal-supporting region,
    harm-associated region, recurrent trap, bottleneck, completion route,
    stale/invalidated region, repair opportunity, reactivation candidate, likely
    interruption point). PASS if features+topology beats features-alone by a margin scaled
    on the SD of the DELTA plus an absolute floor, AND heterogeneous typed topology (F)
    beats the collapsed single-adjacency ablation.
    NON-DEGENERACY GUARD: the anchor set must be non-trivial in the traces used -- a floor
    on distinct anchors AND on edge density per type. A projection where one relation type
    is EMPTY or FULLY CONNECTED tests nothing and self-routes substrate_not_ready.
    WHY THIS IS THE STRONGEST ITEM IN THE INTAKE: a PASS immediately implies a CHEAP
    CAPABILITY GAIN -- read what is already there -- rather than a build. Retrospective,
    substrate-free, no V3 critical-path impact.
    FIRST MOVE IS A SCOPING SPIKE, NOT A PROBE, so no experiment proposal is minted: an
    EDGE-TYPE INVENTORY against LOGGED TELEMETRY. The intake's already-owned table is the
    paper answer (which relations exist); the spike confirms which are RECOVERABLE FROM
    LOGS rather than only from live objects. complicated (buildable).

- id: MECH-469
  title: "Relation types are NOT collapsible to one adjacency structure: collapsing all edge types into a single adjacency, or removing event / goal-motivational edges, or removing action-transition directionality, degrades functional-label recovery. The same non-collapsibility argument as MECH-069 (error incommensurability), one level down."
  claim_type: mechanism_hypothesis
  subject: hippocampus.relation_type_noncollapsibility
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  epistemic_category: standard
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-19_multiple_relational_graph_organisations_in_hippocampal_cognition.md
    - evidence/planning/thought_intake_2026-07-19_multiple_relational_graph_organisations_hippocampal.md
  depends_on:
    - MECH-468
    - MECH-069
    - MECH-035
  notes: >
    THE DIRECT ABLATION FROM MECH-468, registered separately because it is the leg that
    distinguishes "topology helps" from "TYPED topology helps" -- a single-adjacency win
    would support MECH-468 while refuting this claim.
    FALSIFIER: the ablation series (collapse all edge types to one adjacency; remove event
    edges; remove goal-motivational edges; remove action-transition directionality) PLUS
    the two CONTROL ablations that separate content from position --
    (a) RANDOMISE TOPOLOGY while PRESERVING node payloads, and
    (b) PRESERVE TOPOLOGY while SHUFFLING payloads.
    Those two controls are not optional: without them a topology effect is
    indistinguishable from a payload effect leaking through the graph construction.
    NON-DEGENERACY GUARD: at least one ablation must move the metric measurably, or the
    series tested nothing.
    INHERITS MECH-468's organisational/representational guard and its NOT-PROPOSED list
    (no GNN replacement, no claim about biological storage format, no DLIF merge).
    Gated behind the MECH-468 edge-type inventory spike; no experiment proposal minted.

- id: MECH-470
  title: "Topological position improves ghost-goal reactivation ranking: MECH-292/293 rank inactive anchors by preserved-goal-payload match; adding a topological term (relational distance to the current anchor along typed paths) improves reactivation precision. An anchor's functional role -- gateway, bottleneck, recurrent trap, interruption point, repair opportunity -- may be inferable from its POSITION even when its local payload is incomplete."
  claim_type: mechanism_hypothesis
  subject: hippocampus.topological_ghost_goal_reactivation_ranking
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  epistemic_category: standard
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-19_multiple_relational_graph_organisations_in_hippocampal_cognition.md
    - evidence/planning/thought_intake_2026-07-19_multiple_relational_graph_organisations_hippocampal.md
  depends_on:
    - MECH-292
    - MECH-293
    - MECH-339
    - SD-039
    - MECH-468
  notes: >
    THE APPLIED CLAIM, AND THE ONE WITH A BUILT CONSUMER. MECH-292/293's ghost-goal bank
    already ranks inactive anchors on payload match, so a topological term is a DROP-IN
    COMPARISON ARM rather than a new mechanism. It answers the raw thought's open question
    12 -- could topology identify which inactive anchor should be reactivated during goal
    pursuit?
    FALSIFIER: ranking-quality comparison, payload-only vs payload+topology, on EXISTING
    ghost-bank traces.
    NON-DEGENERACY GUARD: the ghost bank must be NON-EMPTY and the baseline ranking
    NON-DEGENERATE (not all-ties) across arms. An empty bank or an all-tie baseline
    self-routes substrate_not_ready.
    SEQUENCING NOTE: this is the CHEAPEST thing in cluster D that could change behaviour,
    and may be worth doing AHEAD of MECH-468 if a fast win is wanted -- though it is
    STRICTLY WEAKER EVIDENCE for the general topology claim, since a single consumer
    improving does not establish that topology carries functional information generally.
    Do not read a PASS here as confirming MECH-468.
    Gated behind the MECH-468 edge-type inventory spike (the typed-path distance it needs
    must be recoverable from logged telemetry); no experiment proposal minted.

- id: Q-084
  title: "Is HIGHER-ORDER relational structure (metapaths, hyperedges, simplicial relations) required? Some functions may be properties of typed MULTISTEP PATHS rather than of nodes or pairwise edges -- e.g. current state -> available action -> transition region -> predicted hazard -> repair opportunity -> goal completion; or inactive anchor -> shared event -> similar goal state -> successful historical trajectory -> candidate reactivation. Nothing in the registry represents anything above a pairwise relation."
  claim_type: open_question
  subject: hippocampus.higher_order_relational_structure
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-22
    needs_review: false
  what_would_answer: >
    PROBE-GATED on MECH-468 projection F (heterogeneous typed topology) versus a
    METAPATH-AWARE readout. If F already captures what a metapath-aware readout captures,
    no higher-order structure is required; if the metapath readout beats F on the same
    held-out functional labels, pairwise typed edges are insufficient.
    That comparison is only meaningful once MECH-468 has established that typed topology
    carries functional information at all -- a metapath advantage over a topology that
    itself adds nothing is uninterpretable.
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-19_multiple_relational_graph_organisations_in_hippocampal_cognition.md
    - evidence/planning/thought_intake_2026-07-19_multiple_relational_graph_organisations_hippocampal.md
  depends_on:
    - MECH-468
    - MECH-469
  notes: >
    REGISTERED AS A GATED substrate_conditional OPEN QUESTION ONLY. DO NOT BUILD.
    epistemic_category substrate_conditional set EXPLICITLY so narrow_open_question does
    not fire. complex (probe-gated) -- the probe is MECH-468 projection F.
    INHERITS the organisational/representational guard: even a metapath advantage would
    establish that multistep typed relations carry information, NOT that the substrate
    should be reimplemented as a hypergraph. No GNN, no simplicial-complex build.
    DISCIPLINE NOTE worth carrying into the hippocampal architecture doc regardless of
    whether this is ever answered: GRAPH CONSTRUCTION IS AN EPISTEMIC COMMITMENT. A
    proximity graph asserts nearness matters; a causal graph asserts intervention matters.
    So the top-down question -- which relations must be preserved for viable prediction,
    planning, commitment, causal attribution, residue-sensitive learning and
    generalisation? -- is ARCHITECTURAL AND PRECEDES THE MATHEMATICS.
    NOT REGISTERED FROM THIS INTAKE, deliberately: the intake's candidate 5 (does the
    BIOLOGICAL hippocampal system support multiple partially distinct relational
    organisations across subregions, axes, inputs and replay regimes?) is an out-of-domain
    empirical question whose test domain is neuroscience, not REE. The intake's own
    guidance was "register as research_anchor / out_of_domain IF REGISTERED AT ALL"; it is
    routed to /lit-pull instead, with the seven candidate biological predictions in the
    raw thought as the search brief. Check overlap with MECH-316 first -- the
    successor-representation thread is already registered.

- id: Q-085
  title: "Is committed-macro GRAIN a controllability parameter? If habit is chunked action sequences selected by a single goal-directed process (Dezfouli & Balleine 2013) rather than a separate outcome-insensitive controller, then the duration over which the planner CANNOT intervene is set by committed-macro grain -- MECH-323's chunk_max_size x chunk_max_depth -- and the chunk-size budget is a CONTROLLABILITY parameter, not only a formation parameter. A budget set too high would present as outcome-insensitivity while REPORTING NOTHING at the accumulator, because nothing in MECH-323 measures the intervention interval it creates."
  claim_type: open_question
  subject: policy.composition.grain_as_controllability
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-27
    needs_review: false
  epistemic_category: standard
  implementation_phase: v3
  claim_level: mechanistic
  registered_utc: "2026-07-27"
  registered: "2026-07-27"
  depends_on:
    - MECH-323    # the chunk-size budget whose semantics this question is about
    - ARC-071     # parent composition commitment
    - MECH-321    # decomposition operator; the one existing mid-macro release that shortens the interval
    - MECH-163    # the dual-system claim whose habit leg this reframes
  evidence: []
  source:
    - evidence/planning/claim_synthesis_MECH-163_2026-07-27.md
    - evidence/literature/targeted_review_connectome_mech_323/entries/2026-07-26_mech_323_hierarchical_action_sequences_dezfouli2013
  location: docs/architecture/policy_primitive_granularity.md
  what_would_answer: >
    THE DOSE-RESPONSE ARM OF THE MECH-163 HIERARCHICAL-VS-FLAT FALSIFIER -- deliberately
    specified so it costs ZERO additional compute: the same run that discriminates the two
    architectures answers this question, because the discriminating statistic IS the
    dose-response. Do not queue a separate experiment for it.
    Design: post-devaluation policy persistence (SD-033b OFC-analog devaluation; existing
    driver lineage V3-EXQ-485g/i/k/m) measured across chunk_max_size in {2, 3, 5} on the
    chunk-injection arm (use_policy_chunking + use_chunk_proposal_injection ON,
    use_dualsystem_arbitration OFF).
    RESOLVED-YES if persistence rises MONOTONICALLY with the REALISED mean committed-chunk
    length. RESOLVED-NO if persistence is flat in realised length while chunks demonstrably
    form, are selected, AND are committed at differing realised lengths.
    MANDATORY GATES (this lineage has burned two runs on degenerate DVs -- V3-EXQ-786a's
    constant first-step vector and V3-EXQ-811's 0.0-for-empty-list aggregation):
      (a) count committed trajectories with metadata source arc071_chunk > 0 per seed --
          zero is a READINESS FAILURE that scores nothing, not a null. This is the gate
          V3-EXQ-810 failed.
      (b) REALISED mean committed-chunk length must DIFFER across the dose conditions.
          Configuring chunk_max_size is not the manipulation; the realised length is. An
          inert dose axis is a readiness failure.
      (c) the devaluation manipulation check must be measured on a probe that does not
          involve the agent's policy, else a null is a devaluation failure.
      (d) gate every input to the DV's own statistic, not merely the prominent one.
    CONFOUNDER THAT CAN INVERT THE EFFECT, pre-register it: a longer macro is exposed to
    MORE mid-commitment release opportunities, so it can read as LESS insensitive purely
    because it was interrupted more often. Log per-release counts for all five releases
    (MECH-091, MECH-342, rung-6 natural-commit urgency + SD-033e, MECH-321/ARC-070, SD-034)
    and report persistence both raw and conditioned on UNINTERRUPTED macros.
  evidence_quality_note: >
    LIT GROUNDING, and it is stronger than the question was drafted on. Primary:
    Dezfouli & Balleine 2013 (10.1371/journal.pcbi.1003364), in BOTH targeted reviews --
    targeted_review_connectome_mech_323 (supports, 0.78) and
    targeted_review_connectome_mech_163 (weakens, 0.72, scoped to the model-free
    conjunct). Its third failure_signature states the consequence directly: chunk
    boundaries become the only points at which goal-directed control can act.
    CONVERGING, landed 2026-07-27 in REE_assembly 36901c01ed (session
    optimistic-ellis-4357c6, MECH-321/323 numeric-parameter pull) -- and it arrives from
    the OPPOSITE direction, which is why the pair is worth more than either alone:
      - Ramkumar 2016 (Nat Commun, 10.1038/ncomms12176, weakens 0.68): chunk length GROWS
        with practice under a compute/efficiency trade-off, with NO upper bound proposed.
      - Bo & Seidler 2009 (J Neurophysiol, 10.1152/jn.00393.2009, mixed 0.62): chunk
        length tracks visuospatial working-memory capacity and declines with age.
      - Solway 2014 (PLoS CB, 10.1371/journal.pcbi.1003779, filed as an explicit unknown
        NULL): the leading normative account of hierarchy quality at POLICY grain imposes
        no subtask-size budget at all.
    That pull concluded the ceiling should be derived from the DELIBERATION BUDGET. This
    question supplies the other side of the same bound: deliberation cost sets a LOWER
    bound on useful chunk size (chunk more to think less), controllability sets an UPPER
    one (chunk less to stay correctable). Neither alone determines the budget; MECH-323
    currently registers 2-5 as a formation parameter with no owner for either bound, and
    Solway is the evidence that the number does not follow from optimality.
    SUBSTRATE FINDING MOTIVATING THE QUESTION (verified at source 2026-07-27, not
    inferred): a committed trajectory can be released mid-execution by exactly five
    mechanisms -- MECH-091 (acute z_harm threat), MECH-342 (degraded execution readiness),
    the rung-6 natural-commit duration lever with SD-033e frontopolar pressure (which reads
    the goal-proximity of FOREGONE ALTERNATIVES -- the nearest analogue, but not
    devaluation of the committed outcome), MECH-321/ARC-070 (V_s prediction failure on the
    remaining macro), and SD-034 closure de-commit. NONE reads whether the committed
    outcome is still VALUABLE. Outcome-insensitivity is therefore already STRUCTURAL in
    the V3 build, produced by grain, in a substrate that contains no model-free machinery
    anywhere (ree_core returns zero hits for model_free / q_value / q_table / td_error).
    BOUNDING NOTE: the registered defaults are chunk_max_size 5 and chunk_max_depth 3, and
    a depth-3 chunk's sequence may itself contain depth-2 chunks -- so the defaults admit a
    top-level macro LONGER THAN A TYPICAL EPISODE (V3-EXQ-810 ran at 24 steps/episode). At
    default settings the insensitivity window is bounded by episode length, not by the
    budget.

    2026-07-31 (V3-EXQ-841, non_contributory; failure_autopsy_V3-EXQ-841_2026-07-31). Run
    correctly self-routed substrate_not_ready_requeue (non_degenerate=false, all 6 arms RED
    at the readiness-gate layer) -- NOT a test of this question. Independent re-derivation
    surfaced a genuinely novel, decisive substrate finding: wherever the ARC-071
    chunk-injection pathway (use_chunk_proposal_injection=True) is live-tested for the first
    time (810a explicitly ran with injection OFF; 834 never cleared formation), the realised
    committed-chunk length pins at EXACTLY 1 step in 100% of 32 chunking cells that
    registered any commitment at all, independent of the configured chunk_max_size dose
    (2/3/5) -- categorically falsifying this question's own pre-registered gate (b)
    ("REALISED mean committed-chunk length must DIFFER across the dose conditions") as
    currently wired/measured. Two live hypotheses (commit-latch wiring vs experiment readout
    artifact) are not yet discriminated. A second, independent readiness failure (zero
    pre-devaluation resource contacts in the non-chunking A_OFF/A_FLAT control arms, 0/16
    cells; z_goal inactive across the entire 48-cell grid) blocks even the FLAT baseline
    this design needs. pending_retest_after_substrate: true -- this question should not be
    re-queued at the same grain/architecture question until this pathway is fixed (H1) or
    shown to already work correctly with the readout repaired (H2). Routed
    /implement-substrate (recommended_substrate_queue_entry, action=create, sd_id
    arc071_chunk_commit_latch_persistence) with a fanout_recommendation to discriminate the
    H1/H2 split before any re-queue. Confirmed 2026-07-31 via direct interactive user
    confirmation (session quirky-mayer-ee5ad2); H1/H2 pre-registered in
    hypothesis_space_registry.v1.json under qid arc071-commit-latch-persistence.
  notes: >
    REGISTERED 2026-07-27 by /claim-synthesis on MECH-163 (proposal
    evidence/planning/claim_synthesis_MECH-163_2026-07-27.md, user-approved per-item).
    That run's Step-3 gate returned STOP -- MECH-163 has ONE genuine non-degenerate FAIL
    signature (V3-EXQ-786b), which is a clean single falsification rather than granularity
    debt, and the claim was already decomposed 2026-07-22. This Q-claim and a three-edit
    RESTATEMENT of MECH-163 are the entire output; no mechanism claims were minted.
    User approval was conditional on the MECH-321/323 parameter lit-pull landing first. It
    had ALREADY landed (36901c01ed, 06:05Z) unknown to both parties at approval time, so
    the condition was satisfied on registration rather than deferred.
    WHY THIS IS NOT FOLDED INTO MECH-323: MECH-323 is the FORMATION operator and states its
    parameters as formation parameters. The controllability semantics belong to the CONSUMER
    side -- the beta gate, _committed_step_idx, and the release set in agent.select_action --
    which MECH-323 does not own. Registering it against MECH-323 would hide exactly the
    coupling this question exists to surface.
    WHY IT IS NOT A DUPLICATE OF THE MECH-163 FALSIFIER: the falsifier asks WHICH
    ARCHITECTURE produces outcome-insensitivity (grain vs arbitration). This asks whether a
    REGISTERED PARAMETER silently sets a control property nothing measures. The second
    survives either answer to the first: if grain wins, the budget is the control knob; if
    arbitration wins, the budget still sets an uninstrumented insensitivity floor.
    BLOCKED, and the probe has already run: V3-EXQ-810 (2026-07-23) failed
    C1_accumulator_fires at a 2/3 seed bar over n=3 -- chunks formed and crystallised
    end-to-end on seed 101 (7 formed, 22 tracked sequences, 6 crystallised) and 0 on seeds
    202/303 (6 tracked sequences each). Its interpretation label chunk_accumulator_silent
    OVERSTATES this: the accumulator ran on every arm and seed (360 steps, 120 outcomes),
    the OFF arm was correctly inert, the formation/maintenance dissociation held, and the
    flat-outcome-stream excuse was ruled out (spread 0.256 against a 0.05 floor). What
    remains is complicated (buildable): determine which of the three AND-gates
    (repetition >= R_min / variance < F_low / mean > baseline + margin) blocks seeds
    202/303. The accumulator's own per-sequence tally already holds the answer. 810 has NO
    autopsy on file; that adjudication is /failure-autopsy work.
    SEQUENCING TRAP: building a value-driven mid-commitment release would DESTROY the
    phenomenon under test. It must come AFTER this question is answered, not before.
    ID NOTE: allocated as the next free id at write time (max was Q-084) per the
    allocate-at-write-time rule. MECH-204's 2026-07-22 note pencils "Q-085
    recalibration_timescale" for a contingent future child; that child was never
    registered, reservations are not the convention, and it should take the then-next free
    id when and if it is minted.

# --- Cluster E: conservative skill refinement, multi-timescale learning ------
# REE COGNITIVE ARCHITECTURE half of the Microsoft SkillOpt source.
# The Assembly half is cluster F below. Keep separate; see GOV-ANALOGY-1.

- id: MECH-471
  title: "Behavioural-competence updates require the same BOUNDED / PROVENANCED / ROLLBACK-CAPABLE discipline already registered for consolidation. MECH-392, INV-080 and MECH-401 give the memory-consolidation path bounded, attributable, provenance-carrying, rollback-capable, over-rewrite-gated editing; NOTHING EQUIVALENT is registered for behavioural competence -- policy / affordance / action-object learning has no rollback, no provenance record, and no gate against a single successful rollout propagating widely."
  claim_type: mechanism_hypothesis
  subject: learning.competence_update_bounded_provenance_rollback
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-07
    needs_review: false
    evidence:
      from: "v3_exq_875a_mech471_competence_provenance_20260804T114106Z_v3#failure_autopsy_2026-08-05_pending_review_batch"
      as_of: 2026-08-05
      verdict: "FAIL/unknown -- competence_implementation_gap (requeue, not a verdict on the claim; see evidence_quality_note)"
  epistemic_category: standard
  pending_retest_after_substrate: true
  evidence_quality_note: >
    [2026-08-07 governance] V3-EXQ-875 (2026-08-03, FAIL, confirmed failure_autopsy_V3-EXQ-875_2026-08-03
    + superseding failure_autopsy_2026-08-05_pending_review_batch #1): readiness/acquisition
    precondition unmet -- all three _train_all_on_agent call sites omitted zworld_p0_episodes
    (defaulted to 0), the documented SD-070 defect (matches the pre-fix V3-EXQ-728 signature).
    Self-route correctly says requeue, not a verdict on MECH-471. epistemic_category
    precondition_unmet; evidence_direction unknown. Superseded by V3-EXQ-875a.
    V3-EXQ-875a (2026-08-04, FAIL, confirmed failure_autopsy_2026-08-05_pending_review_batch #2,
    successor of V3-EXQ-875): SD-070 fix applied, training budget aligned to V3-EXQ-728's validated
    recipe. epistemic_category competence_implementation_gap; evidence_direction unknown -- still
    not a verdict on the claim itself, reflects an acquisition-reliability characterization gap.
    pending_retest_after_substrate=true. This is the FIRST leg of what is now tracked as the open
    hypothesis-space question mech471_competence_acquisition_reliability: a reproducible ~12.5%
    (2/16 seeds) acquisition clear-rate pattern, independently corroborated on overlapping seeds by
    MECH-472's V3-EXQ-882a (same seed-43-solves signature) and characterized further by the
    diagnostic V3-EXQ-890 (bimodal split confirmed, early-divergence, hazard-layout-difficulty ruled
    out).
    V3-EXQ-890 AUTOPSY CONFIRMED 2026-08-07 (failure_autopsy_V3-EXQ-890_2026-08-07, routed via
    /failure-autopsy per this cycle's broadened diagnostic gate): CHARACTERIZES, does not
    adjudicate, the mech471_competence_acquisition_reliability question. H-hazard-layout-difficulty
    -variance ELIMINATED (random-walk control indistinguishable between cleared/not-cleared
    groups). H-exploration-init-variance CONFIRMED necessary-not-sufficient (93% of the eventual
    gap already present by episode 20, replicates under two split definitions). H-bias-head-ofc-
    interaction remains open (exploratory only, no pre-registered threshold). Does not test
    MECH-471's own local-update-interference falsifier directly; evidence_direction stays
    non_contributory for claim-scoring purposes. Routed to /claim-synthesis to examine the
    MECH-457<->MECH-471 mechanism cross-reference already flagged by Q-089 (substrate_queue.json
    has no entry that would unblock MECH-471 directly, and MECH-457's own bottleneck is
    blocked_pending_discrimination -- an /implement-substrate build would be premature ahead of
    that synthesis). No status change; PROMOTES/DEMOTES NOTHING this cycle.
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-19_conservative_skill_refinement_and_multi_timescale_learning.md
    - evidence/planning/thought_intake_2026-07-19_conservative_skill_refinement_multi_timescale_learning.md
    - "Microsoft SkillOpt -- Executive Strategy for Self-Evolving Agent Skills (neighbouring implementation strategy, NOT an architectural template)"
  depends_on:
    - MECH-083
    - MECH-261
  # Phase-1 pilot (substrate_stability_and_drift_detection_plan.md, 2026-08-03): author-declared,
  # deliberately conservative (safe to over-include, unsafe to under-include) -- NOT machine-proven
  # the way arm_fingerprint's per-cell scoped hashes are. Used only by
  # check_substrate_staleness_candidates.py to narrow drift relevance; never feeds arm_fingerprint
  # itself. Derived from the V3-EXQ-875 autopsy's own root-cause read of what the
  # _train_all_on_agent / SD-070 / SD-056 acquisition path actually touches.
  substrate_scope:
    - ree_core/agent.py
    - ree_core/environment/causal_grid_world.py
    - ree_core/predictors/e1_deep.py
    - ree_core/predictors/e2_fast.py
    - experiments/_lib/allon_training.py
    - experiments/_lib/capability_eval.py
  notes: >
    THE ASYMMETRY IS THE NEW OBSERVATION, and it is a SPECIFIC gap rather than a general
    aspiration: the consolidation path has the discipline (MECH-392 provenance +
    contradiction-flag + ROLLBACK; INV-080 raw-episode preservation, consolidation output
    MUST NOT replace source-episode evidence; MECH-401 gated-write authority with
    over-frequent rewriting as a named failure mode), and the competence path does not.
    Registering the CONSOLIDATION properties again would duplicate and contaminate
    evidence records -- this is a GENERALISATION claim to a different path, not a
    restatement.
    FALSIFIER -- a LOCAL-UPDATE INTERFERENCE TEST: train a targeted competence
    improvement, then measure performance on UNRELATED previously-acquired competences.
    FAIL (i.e. the discipline IS needed) if unrelated competence degrades measurably;
    PASS (not needed) if it does not, across a perturbation range. Note the polarity: a
    FAIL here is what MOTIVATES building the discipline.
    NON-DEGENERACY GUARD: the "unrelated" competences must be DEMONSTRABLY ACQUIRED in
    the baseline -- above-floor performance with live cross-seed variance. Testing
    interference against competences the agent never had is vacuous and self-routes
    substrate_not_ready.
    TESTABLE NOW on existing substrate; complicated (buildable). This is the cheapest real
    probe in cluster E and the right first move. See EXP-0399.
    A "SKILL" IN REE IS NOT A DOCUMENT -- it is distributed across action objects, learned
    affordances, predictive models, hippocampal trajectories, goal-conditioned policies,
    rule-state persistence, control settings, and residue-shaped avoidance/repair
    tendencies. Text-space skill optimisation is NOT embodied cognitive learning.
    EXPLICITLY NOT PROPOSED: text-based skill documents inside REE, any literal SkillOpt
    import, a new foundational learning mechanism, or anything on the V3 critical path.

    2026-08-10 edge correction (chip chip-20260810-phase-consistency-reconciliation):
    depends_on: MECH-392/INV-080/MECH-401/ARC-092 removed. All four are v4,
    substrate_conditional, DO-NOT-BUILD-IN-V3 claims that MECH-471 references only
    ANALOGICALLY ("the consolidation path has the discipline... registering the
    consolidation properties again would duplicate... this is a GENERALISATION claim to
    a different path, not a restatement" -- MECH-471's own text above). MECH-471 was
    tested via 3 real V3 experiments (V3-EXQ-875/875a/890) with none of the four ever
    built; they were never build prerequisites.

- id: MECH-472
  title: "HELD-OUT CONTEXT distinguishes skill acquisition from task memorisation: a competence update should be promoted to durable only on evidence from contexts that did not generate it."
  claim_type: mechanism_hypothesis
  subject: learning.heldout_context_acquisition_vs_memorisation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-07
    needs_review: false
    evidence:
      from: "v3_exq_882a_mech472_context_memorization_generalization_20260805T110228Z_v3#failure_autopsy_V3-EXQ-882a_2026-08-05"
      as_of: 2026-08-05
      verdict: "FAIL/non_contributory -- precondition_unmet (see evidence_quality_note; HOLD on same-recipe successors)"
  epistemic_category: standard
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-19_conservative_skill_refinement_and_multi_timescale_learning.md
    - evidence/planning/thought_intake_2026-07-19_conservative_skill_refinement_multi_timescale_learning.md
  depends_on:
    - MECH-471
  notes: >
    FALSIFIER: paired in-context vs held-out-context evaluation of the SAME learned
    competence; a gap beyond a threshold marks memorisation rather than acquisition.
    NON-DEGENERACY GUARD: the held-out contexts must be GENUINELY REACHABLE by the trained
    policy -- non-zero baseline completion -- or the gap is a FLOOR ARTEFACT rather than
    evidence of memorisation. This is the guard most likely to be skipped and most likely
    to produce a false positive.
    *** DO NOT MERGE WITH THE ASSEMBLY-SIDE HELD-OUT CLAIM. *** GOV-HELDOUT-1 (held-out
    validation for Assembly workflow and rule changes, cluster F) expresses a
    structurally similar idea about the GOVERNANCE machinery. This claim is REE-INTERNAL
    and concerns the agent's own competence promotion. They come from the same source
    document and were deliberately split; per GOV-ANALOGY-1 the resemblance is an ANALOGY
    and is NOT evidence in either direction. An experimental result here NEVER transfers
    to GOV-HELDOUT-1.
    Methodological but genuinely testable; connects to the validation-before-promotion
    principle. See EXP-0400.
  evidence_quality_note: >
    [2026-08-03 governance, V3-EXQ-882, confirmed failure_autopsy_V3-EXQ-882_2026-08-03, FIRST
    experimental test]: non_degenerate=false correctly per the claim's own pre-registered guard
    (acquisition floor unmet on the worst seed) -- the decisive in-context/held-out gap was never
    reachable, so this is neither support nor weakening. Per-seed data shows a real, stable
    difficulty split in the intended direction on the seeds that DID clear the floor.
    non_contributory/precondition_unmet. Route: /queue-experiment with either more training budget
    or an easier held-out context set to clear the acquisition floor on every seed before scoring
    the gap.
    [2026-08-03T18:19Z addendum, autopsy Section 9, confirmed via zworld_p0_warmup lint
    (ree-v3 c81813b)]: UNRESOLVED CONFOUND on the reading above. The driver's single
    `_train_all_on_agent` call (line 256) never passes `zworld_p0_episodes`, so the SD-070
    z_world-encoder warmup never ran and `z_world` stayed a frozen random projection for the
    entire run -- the identical defect independently confirmed on V3-EXQ-728 (original,
    "3/3 seeds failed") and V3-EXQ-875/MECH-471 the same day. A frozen random projection is
    itself a plausible source of a stable, SEED-dependent (not exposure-dependent) split of
    exactly this shape -- one seed's random projection happens to support the policy, two
    don't, and no amount of added REINFORCE exposure would be expected to fix that. Nothing in
    this run's evidence distinguishes that reading from the "genuine hazard-geometry difficulty
    split" reading above; both predict a stable, non-improving per-seed split under more
    training. STATUS: unresolved, not adjudicated between the two readings -- do not read the
    "difficulty split in the intended direction" sentence above as settled pending this.
    Consequence for re-queuing: any successor run (V3-EXQ-882a or otherwise) MUST add
    `zworld_p0_episodes=60` (the V3-EXQ-728/875a validated operating point) to the acquisition
    call before it queues -- this is a strict improvement regardless of which reading is
    correct (either the per-seed split resolves, which is itself informative, or it doesn't,
    which rules out this confound and strengthens the genuine-difficulty-split reading).
    V3-EXQ-882a is NOT YET QUEUED as of this addendum (verified against ree-v3/experiment_queue.json,
    2026-08-03T18:22:41Z) -- it must not be queued with the driver unchanged.
    [2026-08-07 governance, V3-EXQ-882a, confirmed failure_autopsy_V3-EXQ-882a_2026-08-05, second
    experimental test of MECH-472]: both confirmed defects from the first test (frozen z_world,
    worst-of-3-seed veto) fixed and verified applied. non_degenerate=false again, correctly, per the
    claim's own guard: acquisition-floor clear fraction 0.25 (2/8 seeds) did not exceed the 0.5
    majority floor at the lowest exposure, and none of the 6 non-clearing seeds approach the floor
    even at 10x exposure budget -- the decisive in-context/held-out comparison remains unreached.
    With the z_world confound ruled out, this is best read as a symptom of MECH-472's own registered
    dependency on MECH-471's acquisition-reliability problem (same seed-43-solves pattern
    independently reproduced on overlapping seeds by V3-EXQ-875a, characterized further by the
    diagnostic V3-EXQ-890 -- see MECH-471's evidence_quality_note). NO CLAIM-STATUS CHANGE
    (autopsy's own recommendation, ratified); v3_pending stays as registered. HOLD confirmed on any
    V3-EXQ-882b or other same-recipe MECH-472 successor until mech471_competence_acquisition_reliability
    resolves -- re-queuing on the same acquisition recipe before then would very likely reproduce the
    identical ~25% clear rate. PROMOTES/DEMOTES NOTHING.

- id: MECH-473
  title: "REJECTED-ADAPTATION MEMORY improves later recovery and transfer: retaining failed behavioural revisions together with their failure conditions improves adaptation when conditions later change. A failed adaptation should remain available as evidence about what was tried, why it failed, and under what conditions it might later become useful."
  claim_type: mechanism_hypothesis
  subject: learning.rejected_adaptation_memory
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-07-22
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-19_conservative_skill_refinement_and_multi_timescale_learning.md
    - evidence/planning/thought_intake_2026-07-19_conservative_skill_refinement_multi_timescale_learning.md
  depends_on:
    - INV-004
    - INV-006
    - MECH-392
    - MECH-471
  notes: >
    *** DO NOT BUILD IN V3. *** V3 has NO COMPETENCE-REVISION UNIT that could be
    identified and stored, which is the substrate this claim presupposes. Registered at
    implementation_phase v4, scoped, NOT designed. DO NOT QUEUE AN EXPERIMENT AGAINST
    THIS IN V3.
    WHAT IS GENUINELY ABSENT: nothing in REE retains REJECTED adaptations. Residue retains
    the consequences of COMMITTED actions (INV-004/INV-006); MECH-392 flags contradictions
    in consolidated summaries. NEITHER preserves a TRIED-AND-REJECTED behavioural revision
    with its failure conditions. That is a new structure, not a re-description.
    FALSIFIER: arms with and without a rejected-adaptation store, evaluated on a
    CONDITION-REVERSAL task.
    THE NULL IS LIVE AND SHOULD BE CARRIED CO-EQUALLY: rejected-change memory may simply
    be DEAD WEIGHT that slows adaptation. This claim could come out negative, and the
    condition-reversal design is what makes that outcome informative rather than
    ambiguous.
    ASSEMBLY-SIDE ANALOGUE: GOV-REJECT-1 (rejected-edit memory with reconsideration
    conditions, cluster F). Per GOV-ANALOGY-1 that parallel is an ANALOGY, never evidence.

- id: MECH-474
  title: "LEARNING-MECHANISM META-SELECTION (narrow form): the control plane selects among the learning regimes REE actually has -- online practice, exploration, counterfactual simulation, offline consolidation -- rather than applying one learning rule universally, and this selection is state- and mode-dependent."
  claim_type: mechanism_hypothesis
  subject: learning.regime_meta_selection_narrow
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  epistemic_category: standard
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-19_conservative_skill_refinement_and_multi_timescale_learning.md
    - evidence/planning/thought_intake_2026-07-19_conservative_skill_refinement_multi_timescale_learning.md
  depends_on:
    - MECH-312b
    - MECH-312c
    - MECH-261
    - MECH-163
    - MECH-179
  notes: >
    NARROW FORM ONLY IS REGISTERED AS TESTABLE. The BROAD form -- adding imitation and
    language-mediated scaffolding to the menu -- is SUBSTRATE-BLOCKED (neither is built).
    It is NOTED here and deliberately NOT registered as testable; do not design against
    it, and do not silently widen this claim's menu when designing the falsifier.
    PARTIAL PRECEDENT EXISTS but does not cover this: MECH-312b/c arbitrate between habit
    and goal-directed CONTROL; MECH-261 gates WHICH SUBSTRATE MAY WRITE by mode; MECH-179
    routes error TYPE to sleep PHASE. Arbitration over the LEARNING MECHANISM ITSELF
    across the regime menu is not registered.
    FALSIFIER: fixed-regime arms vs a SELECTING arm on a task mix that rewards different
    regimes. PASS requires the selector to BEAT EVERY FIXED REGIME, not merely to beat
    their mean -- beating the mean is satisfied by a selector that always picks the single
    best fixed regime, which demonstrates no selection at all.
    NON-DEGENERACY GUARD: the task mix must ACTUALLY DISSOCIATE the regimes -- AT LEAST
    TWO regimes must win on DIFFERENT sub-tasks in the FIXED-ARM BASELINE, or there is
    nothing to select between and the comparison is vacuous. Verify this in the baseline
    before running the selecting arm.
    Live V3-adjacent question; testable now. See EXP-0401.

    2026-08-10 edge correction (chip chip-20260810-phase-consistency-reconciliation):
    depends_on: ARC-092 removed -- the falsifier design above (fixed-regime arms vs a
    selecting arm over MECH-312b/c, MECH-261, MECH-163, MECH-179) never invokes
    ARC-092's imagination-provenance mechanism; it is a topical, not build, reference.

- id: INV-093
  title: "Skill optimisation must NOT trade harm sensitivity for competence (learning-side ethical constraint): any competence-refinement mechanism must be evaluated JOINTLY on competence gain AND non-degradation of harm sensitivity, residue accumulation, and commitment integrity. A mechanism improving the first by attenuating the second has succeeded on its metric and FAILED AS AN AGENT."
  claim_type: invariant
  invariant_type: universal
  subject: ethics.skill_optimisation_harm_sensitivity_floor
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  epistemic_category: standard
  claim_level: governance
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-19_conservative_skill_refinement_and_multi_timescale_learning.md
    - evidence/planning/thought_intake_2026-07-19_conservative_skill_refinement_multi_timescale_learning.md
  depends_on:
    - INV-004
    - INV-006
    - EXT-009
    - MECH-471
    - INV-092
  notes: >
    SIBLING CLAIM: INV-092 (distractor suppression must remain selectively permeable to
    harm and other-agent signals, from the 2026-07-12 distractor intake). THESE TWO ARE
    THE SAME ETHICAL PRINCIPLE ON TWO DIFFERENT CONTROL SURFACES -- learning/refinement
    here, attention/suppression there -- and were deliberately registered as siblings.
    Read and revise them together; a change to one that is not mirrored in the other is
    almost certainly an error.
    CROSS-LISTED with the ethics perimeter (SENT-*/GOV-*), NOT filed only under learning.
    SOURCE FRAMING, and note it is a CONSTRAINT rather than a caution: "Performance gain
    is not sufficient evidence of viability or ethical adequacy. A validated skill may
    still conflict with commitments, harm constraints, or broader goals. Meta-learning
    itself requires governance so that it cannot optimise away protected structures."
    FALSIFIER: the JOINT measurement across a refinement-strength sweep -- competence gain
    AND harm sensitivity AND residue accumulation AND commitment integrity, never
    collapsed to one score. A single-axis competence metric CANNOT express this
    constraint, which is the whole reason the panel must stay uncollapsed.
    ACCEPTANCE SHAPE for any skill-refinement mechanism: improvement in competence AND
    non-degradation of harm sensitivity / residue accumulation / commitment integrity,
    measured jointly.
    invariant_type universal: constrains any refinement mechanism by design, not emergent
    from a particular substrate SD. Currently a constraint on a FUTURE mechanism -- no
    refinement-strength knob exists to sweep yet.

    2026-08-10 edge correction (chip chip-20260810-phase-consistency-reconciliation):
    depends_on: ARC-092 removed -- a cross-listed ethics-perimeter sibling reference
    (same shape as INV-080's own "depends_on retained as informative links" precedent
    elsewhere in this file), not a build prerequisite for this universal invariant.

# --- Cluster F: bounded knowledge-artefact optimisation, REE ASSEMBLY --------
# ASSEMBLY GOVERNANCE half of the Microsoft SkillOpt source. Registers as
# governance_rule per the precedent INV-077 / GOV-PROC-1 / GOV-CEIL-1 / SD-062.
# DO NOT MINT MECH-* FOR ANYTHING IN THIS CLUSTER. The REE-cognition half is
# cluster E above; the split is the user's and is deliberate. See GOV-ANALOGY-1.

- id: GOV-HELDOUT-1
  title: "Held-out validation for Assembly workflow and rule changes: a change to a workflow, skill, or standing rule should be validated against cases that did NOT generate it. Every layer of Assembly practice currently edits the artefact from the session that motivated the edit -- a workflow change judged by the cycle that produced it, a CLAUDE.md rule written by the session that hit the incident, an intake template revised by the intake that strained it. There is no held-out review anywhere in the loop."
  claim_type: governance_rule
  subject: governance.assembly.heldout_validation_of_rule_changes
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  epistemic_category: governance_rule
  claim_level: governance
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-19_bounded_knowledge_artifact_optimisation_for_ree_assembly.md
    - evidence/planning/thought_intake_2026-07-19_bounded_knowledge_artifact_optimisation_assembly.md
    - "Microsoft SkillOpt -- Executive Strategy for Self-Evolving Agent Skills"
  depends_on:
    - INV-077
    - GOV-PROC-1
    - GOV-ANALOGY-1
  what_would_answer: >
    TRIGGER: reassess the next time either (a) a standing rule/workflow/skill change is
    proposed following a single motivating incident (the pattern this claim already
    documents: the chip-scope rule's four-revision history), or (b) someone proposes
    building tooling/harness support for held-out rule validation rather than the
    manual-discipline version recommended here. Also reassess opportunistically at the
    next ~3-5 rule-editing incidents even absent a formal proposal, since the claim
    currently has zero pilot data.

    EVIDENCE RELEVANT AT THAT REASSESSMENT: (1) when a candidate rule change is checked
    by hand against >=3 HISTORICAL cases OUTSIDE the motivating incident -- per the
    claim's own non-degeneracy guard, only cases where old and new rule actually give
    DIFFERENT answers count -- does that check catch an over-broad rule before it ships,
    or pass cleanly every time (adding cost without information)? (2) does the "manual
    discipline, not tooling" framing survive contact with an actual editing session, or
    get skipped under time pressure? (3) is the SELF-REFERENCE HAZARD respected -- when
    piloted, is the thought-intake workflow (this claim's own source) correctly excluded
    from any held-out evidence set?

    WHAT WOULD ACTUALLY REVISE: the underlying observation (single-incident rules
    over-generalise, evidenced by the chip-scope rule's four revisions with a same-day
    user correction on the third) is independently documented and would stand regardless.
    What this claim adds -- that a >=3-case held-out check is a worthwhile PRACTICE -- is
    undercut if EITHER: (a) the discipline is tried and never once catches a rule that
    later needed correction (inert ceremony), or (b) it is never tried because per-edit
    cost is judged too high (the claim's own "honest counterweight" dominates in
    practice). Either argues for demoting the PRACTICE recommendation toward
    rejected/superseded, without touching the more basic over-generalisation observation.

    READER NOTE: governance_rule / process claim about how the Assembly edits its OWN
    rules, not an empirical mechanism about REE cognition -- no EXP-#### should be minted
    against it. Do not conflate with MECH-472 (held-out context distinguishes skill
    acquisition from memorisation), which is REE-INTERNAL; per GOV-ANALOGY-1 the
    resemblance is an ANALOGY, never evidence. As of 2026-08-08, no pilot of the manual
    discipline has been run anywhere in the codebase (checked .claude/skills/,
    .agents/skills/, CLAUDE.md, WORKSPACE_STATE.md) -- the closest existing analog is the
    retrospective "Scope history" logging pattern (CLAUDE.md chip-scope section, mirrored
    in session-land/SKILL.md), which records a rule's revision history AFTER the fact
    rather than testing a proposed change against held-out cases BEFORE it ships.
  notes: >
    THE SHARPEST GENUINELY-NEW OBSERVATION IN THE INTAKE, and it has a MEASURABLE,
    ALREADY-VISIBLE COST: rules written from a single incident have repeatedly
    over-generalised and needed correction. THE WORKED EXAMPLE is the chip-scope rule --
    broadened, WITHDRAWN ENTIRELY, restored, then narrowed: four revisions, the third of
    which the user corrected the SAME DAY. A held-out test would have caught at least the
    over-broad forms.
    FALSIFIER / ACCEPTANCE: for a proposed rule change, apply it to N>=3 HISTORICAL cases
    OUTSIDE the motivating incident and check it yields the correct call. A rule that only
    fits its originating incident is rejected or narrowed.
    NON-DEGENERACY GUARD: the held-out cases must be ones where the OLD AND NEW RULE
    ACTUALLY DIFFER. Cases where both give the same answer test nothing -- this is the
    guard that stops the check degenerating into a rubber stamp.
    HONEST COUNTERWEIGHT, from the raw thought's own Risks section and NOT to be dropped:
    held-out validation COSTS CYCLES, and "optimising for easily measured quality may
    reduce originality or conceptual depth." This is a REAL TRADE, not a free win.
    CHEAPEST REAL MOVE, and the recommended first one: adopt this as a MANUAL DISCIPLINE,
    NOT AS TOOLING -- add "check this against 3 cases it wasn't written from" to the
    rule-editing path. Costs nothing and addresses the observed failure directly. Do that
    before any registry or harness work.
    DO NOT MERGE with MECH-472 (held-out context distinguishes skill acquisition from
    memorisation, cluster E). That is a REE-INTERNAL claim about the agent's competence
    promotion; this is about the Assembly's rule-editing process. Same source document,
    deliberately split. Per GOV-ANALOGY-1 the resemblance is an ANALOGY, never evidence.
    SELF-REFERENCE HAZARD, stated rather than quietly enjoyed: the raw thought's proposed
    pilot target is the thought-intake generation and review workflow -- the very workflow
    that produced the intake this claim comes from. That intake is NOT held-out evidence
    about intake quality and MUST NOT be counted as such in any later pilot. If the pilot
    runs, that document belongs in the TRAINING subset or is excluded entirely.

- id: GOV-STRAT-1
  title: "Assembly artefact-layer authority stratification: Assembly artefacts form an update-rate hierarchy -- prompts and local workflow hints (fast) -> thought-intake structure (moderate) -> claims and mechanism status (evidence-gated) -> architecture commitments (slow) -> organisational principles (very slow) -> axioms (exceptional). An edit at a fast layer must not SILENTLY change a slower layer's commitment."
  claim_type: governance_rule
  subject: governance.assembly.artefact_layer_authority_stratification
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  epistemic_category: governance_rule
  claim_level: governance
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-19_bounded_knowledge_artifact_optimisation_for_ree_assembly.md
    - evidence/planning/thought_intake_2026-07-19_bounded_knowledge_artifact_optimisation_assembly.md
  depends_on:
    - INV-020
    - INV-021
    - GOV-PROC-1
    - GOV-ANALOGY-1
  notes: >
    THE PRACTICE ROUGHLY FOLLOWS THIS HIERARCHY ALREADY. Nothing STATES it, so nothing
    ENFORCES it, and nothing FLAGS when a fast-layer edit silently changes a slow-layer
    commitment. That last case -- SILENT SCOPE EXPANSION ACROSS AUTHORITY LAYERS -- is the
    failure this rule exists to catch.
    ACCEPTANCE: scope expansion is DETECTABLE -- a fast-layer edit that alters a
    slow-layer commitment is FLAGGED, NOT MERGED.
    *** THE INV-020 SYMMETRY IS AN ANALOGY AND MUST BE CITED AS ONE (GOV-ANALOGY-1). ***
    INV-020 is the ARCHITECTURAL twin: constraint stores are authority-stratified from
    direct writes. The observation that "the Assembly should obey the invariant it demands
    of the architecture" is rhetorically strong and is EXACTLY the kind of statement
    GOV-ANALOGY-1 requires be marked as analogical. It is a reason to ADOPT this rule; it
    is NOT evidence that the Assembly behaves like the architecture, nor that INV-020's
    experimental support transfers here. It does not.
    RELATION TO CLUSTER E: this is the Assembly-side counterpart of the
    bounded-revision family (MECH-471). Analogy only.

- id: GOV-REJECT-1
  title: "Rejected-edit memory with reconsideration conditions: rejected proposals -- workflow changes, claim registrations, experiment proposals -- are retained with the proposed edit, why it was proposed, why it was rejected, the evidence considered, and THE CONDITIONS UNDER WHICH RECONSIDERATION IS APPROPRIATE. The last field is what makes it more than an archive."
  claim_type: governance_rule
  subject: governance.assembly.rejected_edit_memory
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  epistemic_category: governance_rule
  claim_level: governance
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-19_bounded_knowledge_artifact_optimisation_for_ree_assembly.md
    - evidence/planning/thought_intake_2026-07-19_bounded_knowledge_artifact_optimisation_assembly.md
  depends_on:
    - GOV-HELDOUT-1
    - GOV-PROC-1
    - GOV-ANALOGY-1
  notes: >
    THE AD-HOC PRECEDENT ALREADY EXISTS AND IS THE STRONGEST ARGUMENT FOR THE RULE: several
    CLAUDE.md and SKILL.md sections already carry an explicit "Scope history -- read before
    'simplifying' this" block recording what the rule was, what it was changed to, what was
    withdrawn, and WHY THE WITHDRAWAL WAS ITSELF CORRECTED. That is a rejected-edit memory
    with reconsideration conditions, hand-rolled at exactly the place where repeated
    rediscovery was most costly. This rule GENERALISES AN EXISTING LOCAL PRACTICE rather
    than importing a foreign one.
    THE NEAREST EXISTING FORMAL THING IS CLAIM SUPERSESSION, which retains SUPERSEDED
    ASSERTIONS but NOT REJECTED PROPOSALS. A rejected experiment proposal, a rejected
    workflow change, a rejected claim registration currently leaves NO TRACE AT ALL.
    ACCEPTANCE: repeat-rediscovery rate falls; a re-proposal of a previously rejected
    change is DETECTED.
    IMPLEMENTATION GUIDANCE from the routing: build this as a LIGHTWEIGHT EXTENSION OF THE
    EXISTING "Scope history" CONVENTION -- a named section, NOT a new system. Do this
    AFTER GOV-HELDOUT-1's manual discipline, which is cheaper and addresses a
    demonstrated cost.
    SELF-REFERENTIAL KILL CRITERION, and it is load-bearing: an optimiser that REPEATEDLY
    RE-PROPOSES CHANGES ALREADY REJECTED FOR KNOWN REASONS is precisely what the rejection
    buffer exists to prevent -- so failing that criterion means THIS RULE DID NOT WORK.
    DO NOT MERGE with MECH-473 (rejected-adaptation memory, cluster E, V4 REE-internal).
    Same source, deliberately split; per GOV-ANALOGY-1 the parallel is an analogy.

- id: GOV-META-1
  title: "Meta-optimisation remains SUBORDINATE to provenance, epistemic honesty, and explicit human authority: an Assembly self-improvement loop must not shift governance authority, erase authorship distinctions or uncertainty, or increase artefact volume at the expense of quality."
  claim_type: governance_rule
  subject: governance.assembly.meta_optimisation_subordination_guard
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  epistemic_category: governance_rule
  claim_level: governance
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-07-22"
  source:
    - docs/thoughts/2026-07-19_bounded_knowledge_artifact_optimisation_for_ree_assembly.md
    - evidence/planning/thought_intake_2026-07-19_bounded_knowledge_artifact_optimisation_assembly.md
  depends_on:
    - INV-077
    - GOV-PROC-1
    - GOV-HELDOUT-1
    - GOV-STRAT-1
    - GOV-REJECT-1
    - GOV-ANALOGY-1
  notes: >
    GUARD CLAIM -- the intake's own condition was "register with 1-4 OR NOT AT ALL", so it
    is registered here alongside GOV-HELDOUT-1 / GOV-STRAT-1 / GOV-REJECT-1 and must be
    retired with them if they are retired. It is the constraint that makes the other three
    safe to adopt.
    KILL CRITERIA -- ADOPTED AS WRITTEN, and the best part of the raw thought. Demote or
    stop the approach if it: (1) produces no held-out improvement; (2) increases
    unsupported claims or duplicate artefacts; (3) makes provenance harder to inspect;
    (4) optimises superficial formatting over scientific quality; (5) increases governance
    burden more than it reduces; (6) REPEATEDLY PROPOSES CHANGES ALREADY REJECTED FOR
    KNOWN REASONS; (7) weakens human review or authority boundaries. Criterion (6) is
    SELF-REFERENTIAL AND LOAD-BEARING -- failing it means GOV-REJECT-1 did not work.
    DO NOT BUILD A SkillOpt-STYLE OPTIMISER. The raw thought does not ask for one, and the
    pilot it proposes is deliberately low-risk.
    METRIC PANEL, WITH THE THOUGHT'S OWN CAUTION ATTACHED -- NO SINGLE METRIC SHOULD BECOME
    THE OPTIMISER'S SOLE TARGET: unsupported-novelty rate, duplicate-claim creation rate,
    reviewer correction burden, and frequency of later supersession caused by avoidable
    intake defects. The LAST is the best because it is measured DOWNSTREAM rather than at
    production time.
    LEAST OBVIOUS AND MOST DURABLE RISK, from the raw thought's Risks list: EPISTEMIC
    MONOCULTURE VIA REVIEWER SELECTION.
    ALSO CARRIED BY THIS CLAIM, from the governance-interactive rule: pause for user
    decision, never auto-route.
    NOT REGISTERED FROM THIS INTAKE, deliberately: candidate 4 (skill-registry metadata --
    known failure modes, evaluation history, version/provenance, confidence and scope on
    each SKILL.md). The intake's own guidance was "register ONLY if it needs governance
    visibility; otherwise route straight to implementation", and it is ROUTED TO
    IMPLEMENTATION. It is complicated (buildable) and cheap. Building a workflow SELECTOR
    on top is NOT cheap and must NOT be attempted until the fields have accumulated real
    history.

- id: MECH-475
  title: "uninformative_value_baseline_makes_optimisation_iatrogenic. In REE's MECH-457 actor-critic pathway the value baseline is UNINFORMATIVE on the policy's own state distribution, so the advantage carries variance rather than signal and ADDED OPTIMISATION PRESSURE IS IATROGENIC: policy capacity, training budget, an earned intrinsic approach drive, environmental reward-coupling and post-installation RL refinement each drive competence BELOW THEIR OWN CONTROLS rather than plateauing beneath a ceiling, and the magnitude of the loss tracks baseline uninformativeness rather than the identity of the intervention. Measured mechanism: V3-EXQ-782 R-(b) reads the shared CTRL critic at std(V)/std(G) = 0.041 against a 0.25 collapse threshold and a pre-reward-vs-far separation ratio of 0.016 against a 0.25 floor -- an unbaselined high-variance advantage. A ceiling plateaus; this does not."
  claim_type: mechanism_hypothesis
  subject: f_dominance_conversion_ceiling.baseline_informativeness
  polarity: asserts
  status: retired
  live_status:
    reading: retired
    as_of: 2026-07-29
    needs_review: false
    evidence:
      from: "v3_exq_837_mech475_distributional_critic_iatrogenic_falsifier_20260729T141738Z_v3"
      as_of: 2026-07-29
      verdict: "weakens -- WITHDRAWN per the claim's own pre-registered falsifier (what_would_answer (a)): distributional critic substituted for scalar on 2/3 named destructive instances (capacity/769, approach/781), destructive treatment-below-control inversion got 3.4x WORSE not better on all 6 scorable seeds, every readiness/positive-control gate green."
  implementation_phase: v3
  v3_pending: true
  generation: v3
  epistemic_category: standard
  claim_level: mechanistic
  registered_utc: "2026-07-22"
  registered: "2026-07-22"
  split_from: MECH-457
  depends_on:
    - MECH-457  # the actor-critic pathway whose baseline this claim characterises; MECH-457 is retained as the narrowed umbrella
    - MECH-459  # owns the NORMALISER / critic-FORM lever; this claim owns the baseline-informativeness -> iatrogenic-optimisation CONSEQUENCE (opposite-signed prediction, see functional_restatement)
  location: docs/architecture/competence_bootstrap_mechanisms.md#7-mech-475----uninformative-value-baseline-makes-optimisation-iatrogenic
  functional_restatement: >
    Registered 2026-07-22 by /claim-synthesis as a child of the MECH-457 granularity-debt
    decomposition (evidence/planning/claim_synthesis_MECH-457_2026-07-22.md, user-approved
    per-child). The campaign's nineteen autopsy targets have been read throughout as a
    search for the mechanism that would PRODUCE competence. Read as a set, they show
    something the individual autopsies could not: five INDEPENDENT interventions each drove
    competence BELOW ITS OWN CONTROL.
      V3-EXQ-769  more capacity + 5x budget      raw ON 6.48 -> 0.12
      V3-EXQ-781  earned approach drive (0.70)   raw 2.983  -> 0.200
      V3-EXQ-771  metabolic reward-coupling      reward 3.47 / survive ~170 -> 1.12 / death rate 1.0
      V3-EXQ-780  unconstrained RL after BC      post-BC 20.933 -> 11.667
      V3-EXQ-789  RL vs persistent imitation aux installed -> decayed at EVERY schedule
    A capacity, representation or observability ceiling predicts a PLATEAU, approached from
    below. It does not predict a treatment landing beneath its own control, five times, on
    five different axes. The uninformative baseline predicts exactly that: with an advantage
    dominated by variance rather than signal, MORE GRADIENT IS MORE DRIFT, whatever
    direction the drift is pushed from -- which is also why the same signature appears on
    the acquisition side (769/781/771) and the retention side (780/789), where the campaign
    has treated them as separate problems.
    NOT MECH-459 RE-LABELLED, and the two are independently falsifiable because THEY
    PREDICT OPPOSITE SIGNS. MECH-459 asserts the two-sided normaliser makes the gradient
    scale-invariant, so magnitude levers CANNOT MOVE the floor -- an INERTNESS prediction.
    This claim asserts the baseline is uninformative, so added optimisation ACTIVELY
    DEGRADES -- a DESTRUCTIVENESS prediction. V3-EXQ-769 / 781 / 771 are all magnitude-class
    levers that MECH-459 predicts should be inert, and all three were destructive. MECH-459's
    own probe splits the same way: V3-EXQ-782 R-(a) WEAKENED its normaliser half (C_pre 1.918
    z_world / 1.203 raw_view, both ABOVE the 0.5 "tiny" threshold, so there is no tiny forage
    signal for novelty noise to swamp) while R-(b) CORROBORATED its critic half. This claim
    is that corroborated half, promoted to its own falsifiable object.
    THE POSITIVE CONTROL ALREADY EXISTS. V3-EXQ-788 (clean PASS) shows a distributional
    critic RETAINS 1.839 of installed competence where the scalar critic does not -- i.e.
    making the baseline informative demonstrably changes the sign of the outcome on at least
    one of the five instances. The claim's decisive test is whether that generalises to the
    other four.
  what_would_answer: >
    Two readouts, in decreasing order of decisiveness.
    (a) REVERSAL -- the decisive test. Re-run the three destructive ACQUISITION-side
    treatments (V3-EXQ-769 capacity/budget, V3-EXQ-781 approach drive, V3-EXQ-771 metabolic
    coupling) with the V3-EXQ-788 DISTRIBUTIONAL CRITIC in place, everything else
    byte-identical to the original arm, on the non-regressed reference build (128-wide /
    3x budget / z_world detached / credit-replay 3 / topk 32). SUPPORTED if the
    treatment-below-control inversion REVERSES OR FLATTENS in at least 2 of 3.
    WEAKENED if treatments still land below their own controls with an informative baseline
    -- the destructiveness is then not the baseline's doing and this claim is WITHDRAWN
    (it does not degrade into a weaker version; the whole content is the causal attribution).
    (b) DOSE -- the cheap standing readout. Record std(V)/std(G) and the
    pre-reward-vs-far separation ratio as DECLARED COVARIATES on every arm of every future
    MECH-457-family run, and test whether the control->treatment competence DELTA is
    predicted by baseline informativeness across instances. SUPPORTED if the correlation
    holds and is signed as predicted; a null here is weak evidence only (n is small and the
    five existing instances differ on many axes at once).
    MANDATORY on both: TRAJECTORY, not terminal competence. This signature is invisible to
    terminal-only measurement, which is precisely what hid it across nineteen autopsy
    targets -- every one of which recorded the numbers that show it.
    NON-DEGENERACY GUARD: an arm whose control did not itself clear the readiness anchors
    cannot exhibit a treatment-below-control inversion and must self-route
    substrate_not_ready_requeue, never a reversal verdict.
  notes: >
    LITERATURE GROUNDING (2026-07-22, /claim-synthesis Step 5; targeted searches recorded
    here, full /lit-pull targeted_review_mech_457_baseline_informativeness COMMISSIONED and
    owed).
      - Sutton, McAllester, Singh & Mansour 2000 (NeurIPS), policy-gradient theorem --
        already on file in targeted_review_actor_critic_action_learning. The value baseline's
        FUNCTION IS VARIANCE REDUCTION; an uninformative baseline reduces nothing, which is
        the formal statement of this claim. This is the theoretical anchor.
      - O'Doherty et al. 2004 (Science), dissociable ventral/dorsal striatum -- on file. A
        SILENT CRITIC is a specific lesion, not a degraded actor: the biological prediction
        is that the deficit should look like loss of value-side grounding, not loss of motor
        competence.
      - Rothenhoefer et al. 2017 (J Neurosci 37:6902), ventral striatum lesions in rhesus
        macaques. THE CLOSEST DIRECT TEST FOUND, and it CUTS BOTH WAYS -- recorded as a
        divergence rather than smoothed over. SUPPORTING: VS-lesioned monkeys were MORE
        INFLUENCED BY NEGATIVE FEEDBACK and had LOWER CHOICE CONSISTENCY -- degradation with
        an unbaselined-error signature, not a plateau, which is this claim's predicted shape.
        DIVERGING: the VS deficit was specific to learning the value of STIMULI and left
        ACTION-value learning INTACT, whereas REE's deficit is on actions. So the biology
        does not straightforwardly license "remove the critic -> action learning degrades";
        the next lit-pull must resolve whether REE's foraging DV is stimulus-like
        (resource-directed) or action-like in the relevant sense.
      - Salamone & Correa 2003 (targeted_review_arc_068_opportunity_cost) and Szczypka et al.
        2001 (targeted_review_competence_bootstrap_without_demonstrator) -- dopamine-deficient
        mice with INTACT perception, INTACT motor apparatus and INTACT hedonics STARVE IN
        FRONT OF FOOD. The closest biological analog of REE's "readiness met on every leg,
        foraging at floor" signature. Already cited by MECH-461 for the DRIVE half; cited here
        for the distinct point that an intact actor with a compromised value channel produces
        competence loss rather than a plateau.
    EVIDENCE MOTIVATING REGISTRATION: the five instances tabulated in functional_restatement,
    plus V3-EXQ-782 R-(b) (the measured mechanism) and V3-EXQ-788 (the positive control).
    Full derivation, the Step-3 discrimination gate, and the declined children in
    evidence/planning/claim_synthesis_MECH-457_2026-07-22.md.
    PROMOTES NOTHING. Registered as candidate/v3_pending. No hypothesis-space fan-out growth
    event is created by this registration -- see the synthesis document section 6.
  evidence_quality_note: |
    [2026-07-29 governance, V3-EXQ-837 weakens -> RETIRED; failure_autopsy_mech476-mech475-cluster_2026-07-29,
    confirmed]: V3-EXQ-837 is this claim's own decisive, pre-registered reversal test (what_would_answer
    (a)): substituting a distributional critic for the scalar one on 2 of the 3 named destructive
    instances (capacity/V3-EXQ-769, approach/V3-EXQ-781; metabolic/V3-EXQ-771 correctly self-excluded,
    its own control never cleared the competence floor). Result: the destructive treatment-below-control
    inversion did NOT reverse or flatten in either scorable family -- it got 3.4x WORSE under the
    informative baseline (dist_inversion 15.1/14.9 vs scalar_inversion 4.5/4.4, reverses_or_flattens=false
    on all 6 scorable seeds), with every readiness/positive-control gate green. Per the claim's own
    pre-registered text this means the destructiveness is NOT the baseline's doing, and per the WEAKENED
    branch of what_would_answer the claim does not degrade into a narrower version -- the whole content
    was the causal attribution, so it is WITHDRAWN. Status floored candidate -> retired (not rejected --
    the mechanism the claim named was real and independently established; only this claim's specific
    causal attribution of it to baseline informativeness is falsified). MECH-476/MECH-460 (which cite
    MECH-475 in depends_on) lean on the independently-established destructiveness phenomenon, not on
    MECH-475's specific causal attribution -- unaffected by this retirement. No substrate action.

- id: MECH-476
  title: "competence_retention_dissociable_from_acquisition. ACQUIRING competence and RETAINING it are dissociable capabilities with separate substrate requirements: REE has the first and lacks the second. Behavioural cloning installs foraging competence at 20.933 on raw_view (3/3 seeds took) and 32.72 on z_world, and unconstrained RL refinement then ERODES it to 11.667 -- while a distributional critic (V3-EXQ-788, retains 1.839) and a KL anchor to the installed snapshot (V3-EXQ-792, retains 0.778 vs 0.525 unconstrained, still plastic) each preserve it. Retention is supplied by properties of the VALUE ESTIMATOR and the UPDATE CONSTRAINT, NOT by continued demonstration (V3-EXQ-789 eliminates the imitation-auxiliary schedule axis at every setting). COROLLARY, and the claim's distinctive testable content: consolidation is properly defined as RESISTANCE TO RETROGRADE INTERFERENCE (Krakauer 2005), hence dependent on install DOSE and elapsed offline INTERVAL -- neither of which REE has ever varied."
  claim_type: mechanism_hypothesis
  subject: f_dominance_conversion_ceiling.acquisition_retention_dissociation
  polarity: asserts
  status: retired
  live_status:
    reading: retired
    as_of: 2026-08-01
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-836e_2026-08-01"
      as_of: 2026-08-01
      verdict: "weakens -- WITHDRAWN per the claim's own pre-registered falsifier (what_would_answer): all three arms (dose/V3-EXQ-836a, novelty-tagging/V3-EXQ-836d, interval/V3-EXQ-836e) independently confirm retention invariant to the manipulated variable, 2 of 3 leave-one-out robust at 10/10 seeds."
  implementation_phase: v3
  v3_pending: true
  generation: v3
  epistemic_category: standard
  claim_level: mechanistic
  registered_utc: "2026-07-22"
  registered: "2026-07-22"
  split_from: MECH-457
  depends_on:
    - MECH-457  # the narrowed umbrella
    - MECH-459  # critic-side retention lever (V3-EXQ-788)
    - MECH-460  # behavioural-prior-side lever; already names KL-to-a-frozen-own-snapshot as the literature's anti-forgetting object
    - MECH-475  # sibling child: the uninformative baseline is the leading candidate ROOT of the erosion this claim characterises
  location: docs/architecture/competence_bootstrap_mechanisms.md#8-mech-476----competence-retention-dissociable-from-acquisition
  implementation_note: >
    SD-083 offline policy-consolidation window LANDED 2026-07-29 (ree-v3; /implement-substrate,
    session beautiful-elbakyan-245a58). Unblocks the two blocked_substrate arms of the falsifier
    above: the A->B offline INTERVAL arm (V3-EXQ-836b) and the Moncada-2007 NOVELTY-tagging arm
    (V3-EXQ-836c). The DOSE arm (V3-EXQ-836) was already queued. Substrate:
    experiments/_lib/mech457_offline_consolidation.py (OfflineEWCAnchor +
    consolidate_offline_window) -- an OFFLINE, trace-selective (Fisher-weighted EWC, Kirkpatrick
    2017), interval-accumulated (capture c(N)=capture_max*(1-exp(-N/tau))), novelty-gated (the
    lineage RNDModule, the MECH-441 principle) window run BETWEEN install_bc_prior and the RL
    refinement. It BUILDS PROTECTION and does NOT retrain (theta unchanged -> post_bc invariant to
    the interval), so INTERVAL and DOSE are orthogonal axes. DISTINCT from the ONLINE global
    MECH-475 KL anchor -- that distinction is this claim's distinctive content (ii offline
    interval, iii trace selectivity; the Walker-2003 divergence). Config
    BootstrapExplorerConfig.use_offline_consolidation default False (byte-identical OFF; 12/12
    SD-083 contracts + 71/71 sibling retention contracts green on a cloud worker). SD doc:
    docs/architecture/sd_083_offline_policy_consolidation_window.md; substrate claim SD-083.
    v3_pending UNCHANGED -- this lands the SUBSTRATE, not the evidence; MECH-476 stays candidate/
    v3_pending until 836b/836c score. Testbed-first per user 2026-07-29: IF supported, the
    cognifold port into the ONE SD-017 sleep loop (unifying with MECH-441 novelty + MECH-204) is a
    registered follow-on gated on the evidence.
  functional_restatement: >
    Registered 2026-07-22 by /claim-synthesis as a child of the MECH-457 decomposition
    (evidence/planning/claim_synthesis_MECH-457_2026-07-22.md, user-approved per-child).
    The campaign's first ten legs all asked which mechanism PRODUCES competence, every one
    adjudicated against an arm that never left the ~0-1 floor. V3-EXQ-780's raw_view arm
    reached 20.933 -- the first supra-lift-target (13.05) observation in the campaign -- and
    RL then eroded it to 11.667. That is the moment the question changed, and the
    2026-07-19 three-way split of H-bc-prior recorded it: acquisition CONFIRMED, retention
    OPEN. This claim names the dissociation itself.
    WHAT THIS ASSERTS THAT MECH-459 AND MECH-460 DO NOT -- the anti-duplication test, stated
    so a reader can dispute it. MECH-459 owns the critic/normaliser LEVER; MECH-460 owns the
    behavioural-prior LEVER. Each is a claim about one lever. Neither asserts that acquisition
    and retention are DISSOCIABLE CAPABILITIES. The distinctive content here is threefold:
    (i) the DOUBLE DISSOCIATION -- a mechanism can retain without acquiring (the KL anchor
    acquires nothing) and acquire without retaining (BC), which is a structural claim about
    the substrate, not about a coefficient;
    (ii) CONSOLIDATION-AS-INTERFERENCE-RESISTANCE, which makes install DOSE and elapsed
    OFFLINE INTERVAL the untested levers -- V3-EXQ-780 ran a single BC dose straight into RL
    with no interval, so REE has never tested for a consolidation PROCESS at all, only for
    concurrent REGULARISATION;
    (iii) TRACE SELECTIVITY -- recorded in the V3-EXQ-792 autopsy section 3: a GLOBAL KL
    coefficient cannot express WHICH traces are protected, whereas biological protection is
    synapse- and trace-specific.
    THE INTENDED FAILURE MODE, stated up front: if the falsifier returns invariance to dose
    and interval, there is no consolidation PROCESS -- only a regulariser -- and this claim
    is WITHDRAWN back into MECH-459/460 rather than kept as an umbrella. That is what keeps
    it from being a third lever claim.
  what_would_answer: >
    An A -> B -> A RETROGRADE-INTERFERENCE design, the standard consolidation assay
    (Krakauer 2005). A = BC-install competence; B = an interfering unconstrained-RL phase;
    then re-measure A. Vary the TWO factors REE has never varied: INSTALL DOSE (BC episodes)
    and A->B INTERVAL (offline steps before refinement begins). DV: retained-fraction
    TRAJECTORY, never terminal competence.
    SUPPORTED if resistance to interference GROWS with install dose and/or with the A->B
    interval -- retention is then a dose/time-dependent consolidation PROCESS, and the build
    target is a consolidation window rather than a larger constraint coefficient.
    WEAKENED if retained fraction is INVARIANT to both and tracks only the concurrent
    constraint coefficient -- there is then no consolidation process and this claim is
    withdrawn into MECH-459/460.
    THIRD ARM, from the behavioural-tagging literature (Moncada & Viola 2007) and cheap
    because REE already owns the signal: pair the install with a NOVELTY episode (the landed
    RND drive) inside the consolidation window. Behavioural tagging predicts a WEAK install
    that would not otherwise persist becomes durable when a novelty exposure falls close in
    time. SUPPORTED if a sub-threshold BC dose consolidates only in the novelty-paired
    condition. This arm is the sharpest available discrimination between "consolidation
    process" and "regulariser", because a regulariser has no reason to care about an
    unrelated novelty exposure.
    PREREQUISITE, inherited from the retention portfolio and NON-NEGOTIABLE: an install that
    did not take is UNINFORMATIVE about retention and must self-route
    substrate_not_ready_requeue, never a retention verdict. Bands: floor 1.0, RND plateau
    5.22, lift target 13.05, BC-expert 32.72, local_view_greedy 48.05, greedy_oracle 57.2,
    random_walk 0.933.
  notes: >
    LITERATURE GROUNDING (2026-07-22, /claim-synthesis Step 5; full /lit-pull
    targeted_review_mech_457_consolidation on TRACE-SELECTIVE VS GLOBAL consolidation
    COMMISSIONED and owed -- the V3-EXQ-792 autopsy had already recommended it as secondary;
    this claim makes it primary).
      - Krakauer, Ghez & Ghilardi 2005 (J Neurosci 25:473), visuomotor adaptation.
        CONSOLIDATION IS DEFINED as resistance to retrograde interference, and is shown to
        emerge BOTH with elapsed time AND with DOUBLED INITIAL TRAINING. This is the direct
        source of the dose x interval design above, and the reason terminal-only measurement
        could never have detected it.
      - Walker et al. 2003 (Nature 425:616), dissociable stages of human memory consolidation
        and reconsolidation. At least three separable post-acquisition stages, and
        REACTIVATION RETURNS A CONSOLIDATED MEMORY TO A LABILE STATE -- the direct analog of
        RL refinement destroying an installed policy, and a caution that "protected" is not a
        stable end-state.
      - Moncada & Viola 2007 (J Neurosci 27:7476), behavioural tagging. A WEAK inhibitory-
        avoidance training that induces only short-term memory CONSOLIDATES INTO LONG-TERM
        MEMORY when paired with exploration of a NOVEL environment close in time,
        D1/D5-dependent and protein-synthesis-dependent. Source of the third falsifier arm.
      - On file: targeted_review_systems_consolidation_waking_propagation (Frankland &
        Bonhoeffer 2005 systems consolidation; Carr 2011 awake replay; Tambini 2019;
        Peyrache 2009 PFC rule-learning replay).
      - Bin Ibrahim et al. 2024 (Phil Trans R Soc B), synaptic tagging and capture review --
        the trace-selectivity mechanism (tag setting and plasticity-related-product synthesis
        are separable events), which is what a single global KL coefficient cannot express.
    KNOWN DIVERGENCE, recorded not smoothed: the biological mechanisms above are
    SLEEP- AND REPLAY-DEPENDENT and TRACE-SELECTIVE, while both of REE's demonstrated
    protection pathways (distributional critic, global KL anchor) are AWAKE, ONLINE and
    UNDIFFERENTIATED. The dose x interval falsifier is agnostic to that divergence by
    construction; the trace-selectivity question is deferred to the commissioned lit-pull.
    CROSS-LINK: REE already has a sleep/consolidation substrate line (evidence/planning/
    sleep_substrate_plan.md, SD-017 / MECH-204). If the interval arm returns SUPPORTED, that
    is the natural home for the offline half and this claim should be wired to it rather than
    growing its own.
    PROMOTES NOTHING. Registered as candidate/v3_pending. Creates no hypothesis-space fan-out
    growth event -- H-retention-critic and H-retention-consolidation already exist as legs on
    qid competence_floor.
  evidence_quality_note: |
    [2026-07-29 governance, V3-EXQ-836 + V3-EXQ-836b + V3-EXQ-836c, all non_contributory;
    failure_autopsy_mech476-mech475-cluster_2026-07-29 + failure_autopsy_v3-exq-836b_2026-07-29,
    both confirmed]: all three falsifier arms returned, all three non_contributory (not the mixed/
    reversed/non-monotone readings each self-routed) -- a shared measurement-methodology defect, not
    evidence toward or against the dose x interval x novelty falsifier. 836 (DOSE arm) self-routed
    non_monotone_dose_response, but rests on a fixed 0.15 effect-size margin not scaled to observed
    per-arm noise (dose_bc300 SEM 0.150 vs the 0.301/0.136 between-arm deltas it is read from) and
    fails a leave-one-out check on its single most extreme seed. 836b (INTERVAL arm) self-routed
    non_monotone_interval_response, but the retained-fraction interval axis is non-monotone only via
    the n900 drop; every between-arm delta sits inside the per-arm SEM against the same fixed 0.15
    margin. 836c (NOVELTY arm) self-routed reversed_novelty_effect, but the entire reversal is
    attributable to one outlier seed in the unpaired arm; removing it flips the sign to a small
    paired-favouring delta well inside the margin. All three routed to /queue-experiment under a
    noise-scaled discrimination rule (per the effect-size-gate convention: scale on the SD of the
    delta plus an absolute floor, not a fixed margin). Retention still dissociated from acquisition
    per the prior evidence (V3-EXQ-788, V3-EXQ-792); the dose/interval/novelty falsifier itself
    remains untested. Stays candidate; v3_pending unchanged.
    [2026-08-01 governance, MECH-476 WITHDRAWN -- all three pre-registered falsifier arms now
    complete under the noise-scaled redesign; confirmed failure_autopsy_V3-EXQ-836-cluster_2026-08-01
    (836a dose, 836d novelty-tagging) + failure_autopsy_V3-EXQ-836e_2026-08-01 (interval, the
    completing leg)]: 836a (dose) -- retained_fraction non-monotone across dose (0.712/0.430/0.512),
    effect (-0.1998) well inside the noise-scaled margin (0.7162) for "grows." 836d (novelty-tagging,
    the claim's own "sharpest discrimination" arm) -- paired retention LOWER than unpaired (0.834 vs
    0.950), opposite Moncada & Viola's prediction, effect (-0.1161) inside margin (0.7818), 10/10
    leave-one-out folds robust. 836e (interval) -- retained_fraction non-monotone across window_steps
    0/150/400/900 (0.712/0.707/0.860/0.723), spread 0.153, effect (0.0106) ~84x inside the noise-scaled
    margin (0.893), 10/10 leave-one-out folds robust. All three arms independently and robustly find
    retention INVARIANT to the manipulated variable, using the SAME noise-scaled measurement fix that
    replaced the 2026-07-29 cycle's fixed-0.15-margin defect (see the note above this one). Per this
    claim's own pre-registered `what_would_answer` text: "WEAKENED if retained fraction is INVARIANT to
    both [dose and interval] and tracks only the concurrent constraint coefficient -- there is then no
    consolidation process and this claim is withdrawn into MECH-459/460." All three named conditions are
    now met, by fair, well-instrumented tests directly exercising the SD-083 offline EWC-anchor
    consolidation window this claim's substrate depends on. Per the claim's own `functional_restatement`:
    "if the falsifier returns invariance to dose and interval, there is no consolidation PROCESS -- only
    a regulariser -- and this claim is WITHDRAWN back into MECH-459/460 rather than kept as an umbrella."
    STATUS: candidate -> retired (NOT rejected -- the underlying phenomenon this claim named, that REE's
    two demonstrated protection pathways (distributional critic V3-EXQ-788, KL anchor V3-EXQ-792) are
    awake/online/undifferentiated regularisers rather than a dose/interval/novelty-sensitive consolidation
    process, is real and independently established; only this claim's specific hypothesis that a
    consolidation PROCESS exists on top of that regularisation is falsified). MECH-459 (return-scale-
    invariance / one-sided normalisation, the regulariser-mechanism explanation) and MECH-460 (transient
    behavioural-prior bootstrap) already exist as registered claims and already depends_on-cite this
    claim's sibling MECH-475 for the same reason -- MECH-476's content folds into them, not into a new
    claim. v3_pending left UNCHANGED (the claim is retired, not merely gated) -- no further substrate
    action owed; the SD-083 consolidation window itself remains landed and functioning, just not evidence
    of a distinct consolidation PROCESS beyond what MECH-459/460 already describe. User-confirmed
    2026-08-01 (governance walk, session-startup-checklist-28b0bd).
- id: MECH-477
  title: "dual_system_uncertainty_arbitration. Differential recruitment between the habit and the hippocampally-planned goal-directed pathways is produced by an explicit ARBITRATION element that reads the two pathways' relative uncertainty/reliability and reallocates control accordingly -- NOT by the mere presence of two pathways. Two pathways WITHOUT an arbitrator produce a FLAT recruitment response regardless of context novelty. Registered 2026-07-22 as the control-allocation mechanism MECH-163 presupposed but never named; MECH-163 conflated ARCHITECTURE (two pathways exist) with DYNAMICS (control shifts between them with novelty, uncertainty and practice)."
  claim_type: mechanism_hypothesis
  subject: goal_directed.uncertainty_based_control_arbitration
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-24
    needs_review: false
    evidence:
      from: failure_autopsy_V3-EXQ-811_2026-07-24
      as_of: 2026-07-24
      verdict: supports/measurement_test_design_defect
  implementation_phase: v3
  v3_pending: false
  generation: v3
  epistemic_category: standard
  claim_level: mechanistic
  registered_utc: "2026-07-22"
  registered: "2026-07-22"
  depends_on:
    - MECH-163
    - ARC-071
    - ARC-007
  evidence: []
  location: docs/architecture/mech163_v3_full_completion_gate_session_prompt.md
  functional_restatement: >
    MECH-163 asserts the PRESENCE of two goal-directed systems and names no mechanism that
    ALLOCATES control between them -- yet both experiments run against it (V3-EXQ-786,
    V3-EXQ-786a) measured allocation. 786a established the measurement conditions cleanly
    (familiarity discriminability AUC 0.848, n=8, non_degenerate) and found allocation FLAT:
    recruitment delta mean 0.00435, Cohen's d 0.047, 7 of 8 seeds within +/-0.15. A flat
    response, not a noisy or bimodal one.
    That is the signature of two pathways and no arbitrator. In the reference biology,
    differential recruitment is the OUTPUT of an arbitrator that reads uncertainty and
    reallocates control -- a distinct computational element, not a property of having two
    controllers. This claim names that element.
    REE LOCUS: an arbitration weight over the habit-pathway and planned-pathway
    contributions to E3 selection, driven by their relative predictive uncertainty.
    Build target sd_dualsystem_uncertainty_arbitration, default OFF / bit-identical, per the
    V3-primacy version-layering doctrine.
  what_would_answer: >
    Build an uncertainty/reliability-weighted arbitrator over the two pathways
    (sd_dualsystem_uncertainty_arbitration, default OFF and bit-identical when OFF). Re-run
    the V3-EXQ-786a design as a two-arm contrast, arbitrator OFF vs ON, holding the
    familiarity manipulation and the AUC manipulation-check bar (>= 0.7) fixed.
    SUPPORTED if the novel-minus-familiar recruitment delta is strictly greater with the
    arbitrator ON than OFF on >= 2/3 of divergent seeds, with the ON-arm delta clearing
    mean - SEM > margin.
    REFUTED if the ON arm reproduces the OFF arm's flat response (delta mean within noise of
    zero, d < 0.1) WITH the arbitrator demonstrably live -- mandatory manipulation check:
    the arbitration weight must be shown to vary with measured uncertainty, otherwise the
    run is a readiness failure and scores nothing.
    NOTE the OFF arm is already measured: V3-EXQ-786a IS that arm.
  evidence_quality_note: >
    BIOLOGY (lit-pull satisfied BEFORE registration, per the SD-003 / SD-010-011 discipline;
    no new commission owed for the primary grounding). The arbitration mechanism is already
    reviewed INSIDE MECH-163's own targeted review:
    evidence/literature/targeted_review_connectome_mech_163/2026-04-05_mech163_uncertainty_competition_daw2005
    -- Daw, Niv & Dayan 2005, Nature Neuroscience 8(12):1704-1711, conf 0.79. Its summary
    states the point directly: "given that both systems exist, how does the brain decide
    which one to trust at any given moment?" -- a Bayesian arbitration weighting the two
    systems by relative uncertainty, presented as a DISTINCT COMPUTATIONAL ELEMENT.
    Supporting, same folder: balleine_odoherty_2010 (0.83), dolan_dayan_2013 (0.85),
    niv_2009 (0.78), fraser2023 VTA/SNc dissociation (0.88), miller2017 (0.87),
    vikbladh2019 (0.86). is_formal_import false.
    This is itself the granularity-debt evidence: MECH-163's lit review contained the
    arbitrator as a separate mechanism since 2026-04-05 and the claim text never named it.
    The debt was documented for three and a half months before V3-EXQ-786a made it cost
    anything.
    LIT TOP-UP OWED (not a blocker, does not gate this registration): the reliability-based
    arbitration in vlPFC/FPC (Lee, Shimojo & O'Doherty 2014) that is the empirical
    complement to Daw's normative account is NOT on file, nor is Daw 2011 (the fMRI test of
    the 2005 proposal). Commission as a targeted_review_connectome_mech_163 extension; both
    refine this claim's parameterisation.
    EVIDENCE FROM V3-EXQ-786a: non_contributory -- this claim was NOT tested, because no
    arbitrator existed in the substrate. The flat-response shape is this claim's MOTIVATION,
    not evidence for it. Recorded that way deliberately: reframing a null as a support for
    the mechanism whose absence explains it would be evidence-laundering.
  implementation_note: >
    SUBSTRATE LANDED 2026-07-22: SD-081 (e3.dualsystem_uncertainty_arbitration) in ree-v3,
    session hopeful-panini-cf272d. Default OFF and bit-identical when OFF, per the
    V3-primacy version-layering doctrine. E3Selector.last_arbitration exposes the paired
    (uncertainty, weight) series this claim's MANDATORY manipulation check consumes.
    v3_pending is DELIBERATELY LEFT TRUE: the two-arm falsifier V3-EXQ-811 ran 2026-07-23
    (run_id v3_exq_811_mech477_dualsystem_arbitration_falsifier_20260723T054309Z_v3), and
    its self-route (evidence_direction=non_contributory, interpretation=substrate_not_ready_requeue
    -- both arms measured an exact-zero score range against the readiness gate) was
    subsequently found to be a FALSE NEGATIVE by the confirmed
    failure_autopsy_V3-EXQ-811_2026-07-24 -- see the 2026-07-24 UPDATE paragraph below.
    READ SD-081's evidence_quality_note BEFORE DESIGNING THAT FALSIFIER. Building the
    arbitrator surfaced, by measurement, that V3-EXQ-786a's recruitment DV was degenerate
    -- its first-step vector was CONSTANT across all 32 candidates (index 0 of the z_world
    sequence is the current state, shared by every candidate), and its _spearman
    degeneracy guard could not fire because it tests the std of the RANKS, which for a
    constant vector is a permutation of 0..K-1. The DV measured tie-break noise: simulated
    mean 1.0173 sd 0.1871 against the manifest's reported 1.01725 with per-layout sds
    0.149-0.207.
    THIS BEARS DIRECTLY ON THIS CLAIM'S PREMISE. "Two pathways WITHOUT an arbitrator
    produce a FLAT recruitment response" is not established by 786a on that reading,
    because a DV measuring tie-break noise returns a flat between-condition delta BY
    CONSTRUCTION whether or not an arbitrator exists. The OFF arm therefore CANNOT be
    786a as-run; both arms need a fresh run with a non-degenerate habit read.
    Re-adjudicating 786a is /failure-autopsy and /governance work -- recorded, not applied.

    2026-07-24 UPDATE: both threads closed. (1) 786a WAS re-adjudicated
    (failure_autopsy_V3-EXQ-786a_2026-07-24_dv_degeneracy, confirmed): its `weakens` against
    MECH-163 is withdrawn to non_contributory; MECH-163 leg (1) reverts to experimentally
    untested. This does NOT touch MECH-477 -- see (2). (2) The two-arm falsifier this claim's
    `what_would_answer` specifies WAS built and run: V3-EXQ-811 (HABIT_DEPTH=2, NOT 786a's
    degenerate depth-1 read; new readiness gates on the habit vector's own range and
    distinct-value fraction -- precisely the fix this claim's premise needed). Self-routed
    FAIL/substrate_not_ready_requeue, but the confirmed autopsy (failure_autopsy_V3-EXQ-811_2026-07-24)
    found this a FALSE NEGATIVE: an aggregation bug in experiments/_lib/baselines/mech477_dualsystem_arbitration.py
    defaulted empty-tick cells' range/distinctness fields to 0.0 instead of None (unlike the
    correct adjacent recruitment_rate field), triggering the readiness gate spuriously. Manually
    re-derived from the manifest's own per-cell data (excluding zero-tick cells), all three
    non-vacuity gates actually clear, AND the load-bearing discrimination criterion
    (C1_recruitment_delta_greater_with_arbitrator) passes cleanly: 6/7 divergent seeds ON greater,
    Cohen's d 0.999 (ON) vs -0.158 (OFF), robustness bar clears ON (mean 0.156, lower bound 0.097
    > margin 0.02) and fails OFF. Recommended: MECH-477 SUPPORTS (first genuine experimental
    evidence for this claim); MECH-163 gets no additional weight from this run (context only --
    811 tests whether adding an arbitrator produces differential recruitment, not whether the
    bare dual pathways do). A follow-up bug-fix re-run is routed to /queue-experiment as
    V3-EXQ-811a (not yet queued) to produce a manifest with the aggregation bug itself fixed, so
    future readers don't have to re-derive by hand from raw per-cell data. Given this is currently
    a SINGLE genuine confirming experiment (however clean), status stays candidate/v3_pending
    pending a second independent confirmation or the 811a re-run landing a clean self-route --
    promotion is a decision for a future governance cycle, not applied here.

    2026-07-25 UPDATE (governance): V3-EXQ-811a landed with a CLEAN self-route
    (run_id v3_exq_811a_mech477_dualsystem_arbitration_falsifier_20260724T231847Z_v3;
    interpretation=arbitration_produces_differential_recruitment; evidence_direction=supports),
    exactly the condition the paragraph above named ("or the 811a re-run landing a clean
    self-route"). The aggregation bug is now fixed in the manifest itself, so the supports
    reading no longer requires hand re-derivation. USER-CONFIRMED to CLEAR the V3-pending gate:
    v3_pending true -> false. MECH-477 now carries two concordant V3 confirming reads (811
    re-derived + 811a clean) and is evaluated normally. Status remains candidate this cycle --
    clearing the gate makes it promotable-on-evidence, not promoted; a promotion decision is
    for a future cycle once the confidence recomputes.
  notes: >
    Registered 2026-07-22 by /claim-synthesis on MECH-163 (proposal
    evidence/planning/claim_synthesis_MECH-163_2026-07-22.md, user-approved per-child).
    Recommended independently by failure_autopsy_V3-EXQ-786a_2026-07-22, which routes
    /implement-substrate -> sd_dualsystem_uncertainty_arbitration, priority_suggested 2,
    unblocks_claims [MECH-163, MECH-477].
    DISTINCT FROM ARC-071. ARC-071 is the TRANSFER mechanism (planned -> habitual chunking,
    which pumps content from one pathway into the other, slow and repetition-driven).
    MECH-477 is the ALLOCATION mechanism (which pathway holds control right now, fast and
    uncertainty-driven). MECH-163 presupposes BOTH and specifies NEITHER. ARC-071 is already
    registered and needs building, not registering -- do not mint a duplicate.
    PROMOTES NOTHING; blocks no V3 closure node. candidate / v3_pending until the arbitrator
    substrate lands and the two-arm contrast returns contributory.
- id: MECH-478
  title: "long_horizon_benefit_requires_model_based_rollout. Sustained benefit accumulation is a TRAJECTORY property, not a single-contact event, and is therefore reachable only by goal-seeded multi-step rollout; a 1-step greedy or value-flat-proposal policy cannot navigate toward it. Split out of MECH-163 leg (2) on 2026-07-22 because it carries a blocker of its own -- ARC-007 STRICT value-flat proposals -- entirely unrelated to the blockers on MECH-163 legs (1) and (3), and was inheriting MECH-163's V3-EXQ-786a weakens without ever having been tested."
  claim_type: mechanism_hypothesis
  subject: goal_directed.long_horizon_benefit_accumulation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending
    as_of: 2026-07-22
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  generation: v3
  epistemic_category: substrate_conditional
  claim_level: mechanistic
  registered_utc: "2026-07-22"
  registered: "2026-07-22"
  split_from: MECH-163
  depends_on:
    - MECH-163
    - ARC-007
    - SD-012
    - INV-029
  evidence: []
  location: docs/architecture/mech163_v3_full_completion_gate_session_prompt.md
  functional_restatement: >
    "Sustained joy is a trajectory property, not a single contact event" (MECH-163's original
    leg 2 wording). A policy that maximises immediate contact value cannot reach states whose
    value is only realised over a multi-step horizon, because the intermediate steps are not
    themselves rewarding. Requires the hippocampal proposer to generate GOAL-SEEDED
    trajectories rather than value-flat ones that E3 then scores post-hoc.
  blocker: >
    ARC-007 STRICT value-flat proposals. The hippocampal proposer is value-flat by
    ARCHITECTURAL COMMITMENT, so no goal-seeded rollout can be generated at all. This is
    THIS claim's blocker, stated in its own right -- it is NOT MECH-163's blocker and must
    not be inferred from a parent. Relaxation route already registered: MECH-292 (ranked
    ghost-goal bank) -> MECH-293 (waking ghost-goal probe search), which is the
    HippocampalModule.propose_trajectories() seeding hook.
  what_would_answer: >
    GATED on an ARC-007 relaxation supplying goal-seeded proposal generation (the
    MECH-292 / MECH-293 ghost-goal path is the registered candidate route). Then: an
    environment in which the cumulative-benefit optimum DIVERGES from the greedy-contact
    optimum, with the divergence verified as a property of the environment by a manipulation
    check on the environment itself, not on the agent.
    SUPPORTED if the goal-seeded arm accumulates strictly higher INTEGRATED benefit than the
    value-flat arm on >= 2/3 seeds WHILE MATCHED ON CONTACT COUNT -- the matching is
    load-bearing, since an unmatched advantage is consistent with the planned arm simply
    foraging more.
    REFUTED if integrated benefit is equal or lower with goal-seeded rollout demonstrably
    live and the two optima demonstrably divergent.
  evidence_quality_note: >
    EVIDENCE: none. This leg has NEVER been tested. It explicitly does NOT inherit
    V3-EXQ-786a, which tested MECH-163 leg (1) novel-context recruitment only -- the 786a
    manifest's own scope_note says so, and the autopsy
    failure_autopsy_V3-EXQ-786a_2026-07-22 confirms "legs (2) and (3) untested".
    Splitting this out is precisely what stops the 786a weakens reaching it: a prose scope
    qualifier on MECH-163 would not have survived the indexer, which attaches evidence at
    claim granularity.
    LIT: miller2017 (conf 0.87) and vikbladh2019 (conf 0.86) in
    evidence/literature/targeted_review_connectome_mech_163 both bear on the hippocampal
    contribution to model-based planning and carry over to this leg. No new commission owed
    to register; a leg-specific pull may be worth commissioning at test-design time.
  notes: >
    Registered 2026-07-22 by /claim-synthesis on MECH-163 (proposal
    evidence/planning/claim_synthesis_MECH-163_2026-07-22.md, user-approved per-child).
    Sequenced BEFORE MECH-479 -- long-horizon rollout is a prerequisite for prosocial
    rollout, which is long-horizon rollout over a SECOND agent's state.
    PROMOTES NOTHING. candidate / substrate_conditional; not testable until ARC-007 relaxes.
- id: MECH-479
  title: "prosocial_planning_requires_planned_system. Planning trajectories that affect ANOTHER agent's z_harm_a accumulation and benefit_exposure over time is structurally inaccessible to a 1-step greedy policy and requires the hippocampally-planned pathway. Split out of MECH-163 leg (3) on 2026-07-22 and REFILED implementation_phase v3 -> v4: there is no V3 substrate for it whatsoever -- no second agent exists, hence no other-agent z_harm_a to plan over. Arguably the clearest single piece of granularity debt in MECH-163, since the leg had been carrying a V3 implementation_phase it could not possibly satisfy since 2026-04-02."
  claim_type: mechanism_hypothesis
  subject: goal_directed.prosocial_trajectory_planning
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  implementation_phase: v4
  generation: v4
  epistemic_category: substrate_conditional
  claim_level: mechanistic
  registered_utc: "2026-07-22"
  registered: "2026-07-22"
  split_from: MECH-163
  depends_on:
    - MECH-163
    - MECH-478
    - INV-029
    - ARC-007
  evidence: []
  location: docs/architecture/mech163_v3_full_completion_gate_session_prompt.md
  functional_restatement: >
    MECH-163's original leg (3): prosocial planning -- "sharing joys and sorrows" (INV-029
    benefit gradient) -- requires planning trajectories that affect ANOTHER agent's z_harm_a
    accumulation and benefit_exposure over time. One-step greedy cannot predict or navigate
    toward states that sustain another agent's joy or reduce another's sorrow over time.
    This is MECH-163's stated V4 social-extension rationale, and it belongs in V4.
  blocker: >
    NO V3 SUBSTRATE EXISTS. There is no second agent in the V3 environment, therefore no
    other-agent z_harm_a and no other-agent benefit_exposure to plan over. This is a V4
    prerequisite, not a V3 gap that could be closed by building something. Stated in its own
    right rather than inherited: it is unrelated to MECH-163 leg (1)'s missing arbitrator and
    to MECH-478's ARC-007 blocker.
  what_would_answer: >
    GATED on the V4 multi-agent substrate.
    SUPPORTED if a planned-arm agent reduces a SECOND agent's integrated z_harm_a relative to
    a greedy-arm agent on >= 2/3 seeds, WITH ITS OWN RESOURCE ACQUISITION MATCHED -- the
    matching is load-bearing, otherwise the effect is confounded by the planned agent simply
    being better at everything.
    REFUTED if planned and greedy arms are indistinguishable on the other agent's integrated
    harm, with both arms demonstrably able to PERCEIVE the other agent (mandatory
    manipulation check; without it a null is a perception failure, not a planning failure).
  evidence_quality_note: >
    EVIDENCE: none. Never tested; no substrate in which to test it. Explicitly does NOT
    inherit V3-EXQ-786a -- see failure_autopsy_V3-EXQ-786a_2026-07-22, which scopes that
    run's weakens to leg (1) alone.
    LIT: external grounding is OWED before this claim is tested. INV-029 (benefit gradient)
    is registry-internal, not literature. A /lit-pull on prosocial / vicarious planning and
    on whether other-agent-directed planning recruits the model-based system specifically
    should be commissioned at child-test design time -- deliberately NOT commissioned now,
    since the claim cannot be tested for a generation and a lit-pull that far ahead of its
    test would go stale.
  notes: >
    Registered 2026-07-22 by /claim-synthesis on MECH-163 (proposal
    evidence/planning/claim_synthesis_MECH-163_2026-07-22.md, user-approved per-child).
    The v3 -> v4 refile follows the "phase label follows dependency" discipline: a claim
    whose prerequisite is the V4 multi-agent substrate is a V4 claim regardless of where it
    was originally written down.
    PROMOTES NOTHING; blocks no V3 closure node. Removing it from MECH-163 REMOVES a V3
    completion obligation that was never satisfiable in V3.

- id: SD-081
  title: "e3.dualsystem_uncertainty_arbitration. An explicit arbitration weight over the HABIT pathway (myopic, depth-limited read of the E3 scorer) and the PLANNED pathway (full-horizon read of the same scorer), driven by the two pathways' relative predictive uncertainty and applied to the E3 selection score upstream of every downstream consumer. Built to make MECH-477 falsifiable: the pre-SD-081 substrate scored candidates with an unconditional full-horizon J(zeta), so no weight existed anywhere between the myopic and the deep read and nothing COULD respond to context novelty."
  claim_type: design_decision
  subject: e3.dualsystem_uncertainty_arbitration
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-07-22
    needs_review: false
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-07-22"
  depends_on: [ARC-007, ARC-016, SD-005, MECH-112]
  evidence: []
  location: docs/architecture/sd_081_dualsystem_uncertainty_arbitration.md
  implementation_note: >
    IMPLEMENTED 2026-07-22 in ree-v3 (session hopeful-panini-cf272d), routed by the
    confirmed failure_autopsy_V3-EXQ-786a_2026-07-22 (priority_suggested 2,
    unblocks_claims [MECH-163, MECH-477]).
    Config on E3Config: use_dualsystem_arbitration (default False -> the block is skipped
    entirely; no second scoring pass, no familiarity query, last_arbitration stays None),
    dualsystem_arbitration_gain 4.0, dualsystem_arbitration_bias 0.0,
    dualsystem_uncertainty_ema_alpha 0.05, dualsystem_habit_depth 2.
    u_habit = 1 - familiarity(z_world) (fallback: E1 novelty EMA when curiosity is
    disabled and hippocampal.familiarity_tracker is None; the source is recorded, so an
    ON arm that silently fell back is visible as a readiness failure). u_planned =
    E3._running_variance. Each is normalised against its OWN EMA baseline as u/(u+ema),
    which is what makes the weight scale-free -- familiarity is a clamped kernel density
    set by familiarity_bandwidth and _running_variance is an unbounded PE-MSE, so a ratio
    over the raw values would read the instrument rather than the pathways (the same
    defect V3-EXQ-786a fixed when it replaced a raw mean-difference manipulation check
    with an AUC). w = sigmoid(gain*(u_habit_n - u_planned_n) + bias) weights the PLANNED
    pathway; the two score vectors are z-scored before blending and the result rescaled
    to the planned vector's own mean/std, so w is an ALLOCATION rather than a GAIN (the
    two reads sum over different numbers of steps) and w=1 returns the planned vector
    exactly.
    NO LEARNED PARAMETERS -- no phased training required, and deliberately not a learned
    gate, which would confound "the arbitrator works" with "the gate trained". Daw's
    arbitration is normative Bayesian. MECH-094 N/A (nothing written to memory).
    E3Selector.last_arbitration exposes the paired (uncertainty, weight) series per E3
    tick, which is what MECH-477's MANDATORY manipulation check consumes.
    Contracts: ree-v3/tests/contracts/test_sd081_dualsystem_arbitration.py.
    V3-EXQ-811 (script v3_exq_811_mech477_dualsystem_arbitration_falsifier.py, lineage
    module experiments/_lib/baselines/mech477_dualsystem_arbitration.py) is the MECH-477
    two-arm OFF-vs-ON falsifier; see the note below on why its OFF arm cannot simply be
    V3-EXQ-786a as-run. It ran 2026-07-23 (run_id
    v3_exq_811_mech477_dualsystem_arbitration_falsifier_20260723T054309Z_v3),
    evidence_direction=non_contributory, interpretation=substrate_not_ready_requeue --
    both arms measured an exact-zero score range against the readiness gate
    (full_score_range_non_degenerate and habit_score_range_non_degenerate both failed on
    arm_off and arm_on). A /failure-autopsy has been separately spawned to root-cause the
    discrepancy against the authoring-time smoke test's non-zero ranges; this citation
    should be revisited once that lands.
  evidence_quality_note: >
    TWO CONFIG-LEVEL HAZARDS, both pinned by contract because both were hit during the
    build rather than anticipated.
    (1) The params live on E3Config, NOT REEConfig. E3Selector.config IS the E3Config, so
    a REEConfig-level field reads as a missing attribute in the selector and defaults to
    False -- the silently-unreachable-flag hazard one level below the documented
    from_dims one. Observed as: arbitrator wired, kwargs arriving, 45 select() calls,
    zero arbitrations, no error.
    (2) dualsystem_habit_depth is 2 and FLOORED at 2. Index 0 of the z_world sequence is
    the CURRENT state, shared by every candidate (they all start where the agent is), so
    a depth-1 score vector has cross-candidate range EXACTLY 0.0 and carries no ranking.
    DEGENERATE DV IN V3-EXQ-786a -- MEASURED, NOT INFERRED, AND NOT ADJUDICATED HERE.
    786a's recruitment DV was 1 - spearman(full_horizon, first_step) with the first-step
    read taken as evaluate_trajectory(world_seq[:, :1, :]) -- i.e. exactly the degenerate
    depth above. Re-run under 786a's own config (build_config + ArmSpec
    mech163_recruitment) on 786a's own _depth_scores, the first-step vector is CONSTANT:
    n_unique = 1 across all 32 candidates, range exactly 0, on every scored tick. 786a's
    _spearman guards this with `if np.std(rb) == 0.0: return None`, but the guard CANNOT
    FIRE -- it tests the std of the RANKS, and double-argsort of a constant vector
    returns a permutation of 0..K-1 (std 9.23 at K=32), not a constant. The Spearman was
    therefore computed between the true full-horizon ranking and an arbitrary stable-sort
    tie-break ordering: noise centred on zero, giving recruitment ~ 1.0. Simulating that
    directly over 200 draws gives mean 1.0173 sd 0.1871; the manifest's seed-0 familiar
    condition reports recruitment_rate 1.01725 with per-layout recruitment_sd 0.149 /
    0.207 / 0.165 / 0.190. The match is to five significant figures on the mean.
    786a's candidate_score_range_non_degenerate readiness check gated only the
    FULL-HORIZON vector's range, which is why the run passed readiness with the
    first-step vector degenerate.
    CONSEQUENCE FOR SD-081's FALSIFIER: the OFF arm cannot be 786a as-run. Both arms need
    a fresh run with a non-degenerate habit read.
    CONSEQUENCE FOR MECH-163 / MECH-477: NOT SETTLED HERE. Whether MECH-163's `weakens`
    should stand, and whether 786a should be re-adjudicated non_contributory on a
    measurement defect (as V3-EXQ-786 was), is /failure-autopsy and /governance work.
    Recorded, applied nowhere. Note in particular that MECH-477's premise -- "two
    pathways WITHOUT an arbitrator produce a FLAT recruitment response" -- is not
    established by 786a on this reading, because a DV measuring tie-break noise returns a
    flat between-condition delta BY CONSTRUCTION whether or not an arbitrator exists.
  notes: >
    DISTINCT FROM ARC-071, which remains unbuilt and is NOT a prerequisite for this SD.
    ARC-071 is the TRANSFER mechanism (planned -> habitual chunking; slow,
    repetition-driven, moves content between the pathways). SD-081 implements the
    ALLOCATION mechanism (which pathway holds control right now; fast,
    uncertainty-driven). Allocating between two existing pathways does not require
    content to have been pumped between them. MECH-163 presupposes both and specifies
    neither.
    NOT GATED BY ARC-007 STRICT either: the arbitrator re-weights the SCORING of
    value-flat hippocampal proposals, it does not seed them.
    Biological grounding (lit already on file, no commission owed): Daw, Niv & Dayan
    2005, Nature Neuroscience 8(12):1704-1711, conf 0.79, in
    evidence/literature/targeted_review_connectome_mech_163/2026-04-05_mech163_uncertainty_competition_daw2005
    -- Bayesian arbitration weighting the two controllers by relative uncertainty,
    presented as a DISTINCT COMPUTATIONAL ELEMENT.

- id: SD-082
  title: "pfc.lateral_pfc.rule_selection_action_consumer: a common-mode-invariant, gradient-trainable read-out mapping the SD-078 differentiated rule_state to the SD-033a per-candidate action bias. V3-EXQ-822 found the differentiated rule pool (on_rule_state_diff 0.644, max_live 16) was behaviourally SILENT -- propagation to the action bias was exactly 0.0 on both arms. Root cause: the SD-033a bias head reads RAW common-mode-dominated z_world summaries and hard-clamps its output, so under the SD-008 ~0.98-cosine cone every candidate saturates to the same rail (rule_state's contribution erased) and the clamp's flat region zeros the REINFORCE gradient (head untrainable). Fix: subtract the common mode from the head's candidate-summary input (as SD-078 does upstream) and replace the hard clamp with a scaled-tanh soft bound. ree_core policy change, bit-identical by default."
  claim_type: design_decision
  subject: pfc.lateral_pfc.common_mode_invariant_trained_rule_to_action_readout
  polarity: asserts
  status: candidate_substrate_landed
  live_status:
    reading: candidate_substrate_landed
    as_of: 2026-07-26
    needs_review: false
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-07-26"
  depends_on: [SD-008, SD-066, SD-077, SD-033a, ARC-063, SD-078]
  evidence: []
  location: docs/architecture/sd_082_rule_selection_action_consumer.md
  implementation_note: >
    IMPLEMENTED 2026-07-26 (ree-v3 d4f758082d; ree_core/pfc/lateral_pfc_analog.py
    LateralPFCAnalog.compute_bias + __init__). Config LateralPFCConfig.rule_readout_consumer
    (default False = bit-identical OFF: hard clamp on the raw summary input) and
    readout_init_scale (default 0.25); REEConfig knobs lateral_pfc_rule_readout_consumer /
    lateral_pfc_readout_init_scale plumbed through all three from_dims sites and read in
    agent.py's LateralPFCConfig build. When on: (i) compute_bias subtracts the per-tick mean
    summary across candidates (>=2) so the SD-008 common-mode cone no longer saturates every
    candidate to the same rail; (ii) the output bound becomes bias_scale*tanh(raw/bias_scale)
    instead of a hard clamp, same magnitude bound but gradient-preserving so the head trains
    under REINFORCE. Verified: OFF bit-identical (torch.allclose); ON prop_delta 0.0->~0.005
    (>1e-3 floor) in the common-mode cone; head grad-norm 0.0->~6.1. Contract
    ree-v3 tests/contracts/test_sd082_rule_readout_consumer.py (6 pass); full pre-commit
    contract gate 2300 passed. Completes the SD-033a signature-(iv) trained-head variant
    (DESIGN ALTERNATIVE A2) that the landing deferred.
  evidence_quality_note: |
    Landed 2026-07-26 from /implement-substrate, routed by failure_autopsy_816c-822_2026-07-26
    (REE_assembly afb2df901e) off the V3-EXQ-822 self-route substrate_not_ready_requeue. NOT
    YET VALIDATED by experiment: the substrate makes the SD-078 rule_state -> action-bias
    propagation MEASURABLE and non-vacuous, but the downstream-consumer behaviour is exercised
    by V3-EXQ-822a (re-run of 822 with lateral_pfc_rule_readout_consumer=True on both arms;
    crf_cue_centering stays the swept variable). Acceptance for 822a: on_prop_delta_mean >=
    0.001 with an ON>OFF contrast. SD-082 unblocks the SD-078 downstream-consumer test that
    holds SD-078 at candidate_substrate_landed; it does not itself promote SD-078.

    [2026-07-29 governance, V3-EXQ-822a + V3-EXQ-822b non_contributory; failure_autopsy_batch-822a-826-817a-827_2026-07-26
    + failure_autopsy_2026-07-28-sweep, both confirmed]: 822a (the validation run) reproduced the exact same
    structural-zero propagation (on/off_prop_delta_mean 0.0) despite the fix correctly engaged and a strong
    upstream signal (on_rule_state_diff_mean 0.644) -- acceptance criterion (on_prop_delta_mean >= 0.001 with
    ON>OFF contrast) not met. The follow-on head-internals diagnostic (822b) traced the cause: unconditional
    weight-norm snapshots show REINFORCE never updates rule_bias_head's LAST linear layer across both runs --
    SD-082's centering+soft-tanh fix touches compute_bias's input/output shaping but not that layer's own
    trainability. Substrate_queue.json amended with both failure records (already carried 822's original entry).
    SD-082 stays candidate_substrate_landed / ready=false pending a further fix to the head's last-layer update
    path; not itself falsified, still unvalidated.
- id: SD-083
  title: "consolidation.offline_policy_consolidation_window: an OFFLINE, trace-selective (Fisher-weighted EWC, Kirkpatrick 2017), interval-accumulated (capture c(N)=capture_max*(1-exp(-N/tau))), novelty-gated (Moncada 2007) policy-consolidation window in the mech457 retention TESTBED, run BETWEEN BC install and RL refinement. Unblocks MECH-476's two blocked_substrate falsifier arms: the A->B offline INTERVAL arm (V3-EXQ-836b) and the behavioural-tagging NOVELTY arm (V3-EXQ-836c). DISTINCT from the ONLINE global MECH-475 KL anchor -- offline vs online, trace-selective per-parameter vs global, interval-dependent vs constructed-at-entry, novelty-gated -- which is exactly the MECH-476 content (ii offline interval, iii trace selectivity) the global anchor cannot express (the Walker-2003 divergence). BUILDS PROTECTION without retraining, so post_bc is invariant to the interval and the INTERVAL axis is orthogonal to the DOSE axis."
  claim_type: design_decision
  subject: consolidation.offline_policy_consolidation_window
  polarity: asserts
  status: candidate_substrate_landed
  live_status:
    reading: candidate_substrate_landed
    as_of: 2026-07-29
    needs_review: false
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-07-29"
  depends_on: [MECH-476, MECH-475, MECH-441, SD-017]
  evidence: []
  location: docs/architecture/sd_083_offline_policy_consolidation_window.md
  implementation_note: >
    IMPLEMENTED 2026-07-29 (ree-v3; /implement-substrate session beautiful-elbakyan-245a58).
    TESTBED locus -- experiments/_lib/mech457_offline_consolidation.py (OfflineEWCAnchor +
    consolidate_offline_window), alongside the sibling PolicyKLAnchor / MECH-475, NOT ree_core/:
    a MECH-476 falsifier INSTRUMENT, not a cognifold faculty. Config
    BootstrapExplorerConfig.use_offline_consolidation (default False = byte-identical OFF) +
    offline_window_steps / offline_capture_tau / offline_capture_max / offline_ewc_max_coef /
    offline_fisher_samples / offline_novelty_pairing, threaded through reference_config and
    train_off_arm in baselines/mech457_retention.py; applied as a per-update parameter-space
    penalty coef*sum_i F_i (theta_i-theta*_i)^2 in train_a2c (main AND credit-replay updates).
    Fisher of the policy log-likelihood -> value-head-only params get ~0 weight, so the penalty
    respects the same locus as the KL anchor (the value estimator stays with MECH-459). RNG-neutral
    (snapshot/restore around the window). Contract
    tests/contracts/test_sd083_offline_consolidation.py 12/12; sibling retention contracts 71/71
    green on a cloud worker. NOT YET VALIDATED by experiment: V3-EXQ-836b (INTERVAL) and
    V3-EXQ-836c (NOVELTY) exercise it and can move MECH-476 toward supported/weakened; 836b's N=0
    control arm is the embedded ON/OFF ablation. Cognifold-port follow-on (IF MECH-476 supported):
    port offline trace-selective novelty-gated policy consolidation into the ONE SD-017 sleep loop,
    unifying with MECH-441 novelty + MECH-204. MECH-094 N/A (no simulated memory writes); phased
    training N/A (raw_view, no encoder head).
- id: SD-084
  title: "e3.persistent_committed_program_handle: a committed-program handle on E3Selector that SURVIVES post_action_update's unconditional per-tick teardown of _committed_trajectory, so MECH-321's R4 MID-EXECUTION decomposition hook -- which re-evaluates the REMAINING unexecuted content of an already-committed chunk and therefore inherently needs a CROSS-TICK handle -- becomes reachable under the standard select_action -> update_residue driver loop for the first time. Mirrors the existing _closure_committed_trajectory precedent, built for exactly this reason. Liveness is an INVARIANT (reaped whenever the beta gate is not elevated), not a list of clear sites: agent.py has ten beta_gate.release() sites and only five clear _committed_trajectory, the rest being backstopped by the very teardown this handle removes."
  claim_type: design_decision
  subject: e3.persistent_committed_program_handle
  polarity: asserts
  status: candidate_substrate_landed
  live_status:
    reading: candidate_substrate_landed
    as_of: 2026-07-29
    needs_review: false
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-07-29"
  depends_on: [MECH-321, ARC-070, ARC-071, MECH-288]
  evidence: []
  location: docs/architecture/sd_084_persistent_committed_program_handle.md
  implementation_note: >
    IMPLEMENTED 2026-07-29 (ree-v3; /implement-substrate session sweet-williams-f30676).
    Locus ree_core/: E3Selector._persistent_committed_trajectory (predictors/e3_selector.py),
    SET at commit entry alongside _committed_trajectory under the same `if committed:` path and
    NOT cleared by post_action_update. The setter is deliberately UNGATED -- E3Selector holds an
    E3Config, not the top-level REEConfig where the flag lives, and storing a reference nothing
    reads is output-neutral (the in-file _fp_chosen_world_endpoint / last_raw_scores precedent);
    only the CONSUMER is flag-gated. Config REEConfig.use_persistent_committed_program_handle
    (default False = bit-identical OFF), THREE wiring sites (field + from_dims signature +
    from_dims assignment), no sub-config mirror because the consumer is REEAgent. Consumer:
    agent.py select_action gate (4) now reads the UNION of the F-driven and persistent handles.
    MOTIVATION: the hook had NEVER executed in any experiment and could not -- V3-EXQ-830
    recorded decomp_n_evaluated_midexec = 0 in all 10 cells (5 seeds x 2 arms) against
    decomp_n_evaluated_precommit 1862-2618. Diagnosis
    evidence/planning/failure_autopsy_V3-EXQ-830_2026-07-29.md section 3
    (epistemic_category competence_implementation_gap, NOT substrate_ceiling: nothing was tested
    and found wanting, the mechanism was never reachable).
    NOT A PURE DIAGNOSTIC WHEN ON: a reachable mid-execution hook can newly reach boundary.fired,
    which feeds MECH-321's R1 OR trigger, and a mid-execution fire RELEASES THE COMMIT LATCH,
    aborting the remaining macro -- committed action sequences change. Stated in the flag docstring.
    Contract tests/contracts/test_mech321_midexec_natural_reachability.py (4/4) asserts the hook
    fires in a REAL rollout with NOTHING injected, with an OFF negative control that still commits
    multi-action programs so its zero is the teardown rather than an empty arm. The pre-existing
    test_mech321_scale_resolved_boundary.py (12/12, unchanged) reaches the hook only by
    hand-injecting fake_traj.metadata, _committed_step_idx=1 and beta_gate.elevate() -- correct,
    anti-vacuity-guarded, and reachability-in-PRINCIPLE only, which is exactly why this defect
    survived. Natural reachability is seed-dependent (E3 commits a multi-action ARC-071 chunk only
    when it beats the CEM-optimised candidates on score): measured 2026-07-29, seeds 47 and 71
    yield multi-action commits at the contract config, seeds 11/23/97 yield none.
    ALSO makes use_decomposition_scale_resolved_probe_midexec (ree-v3 aaf5caac26) REACHABLE rather
    than merely unexercised -- it is correct and contract-covered but modified a dict inside a
    block that never ran, and must not be read as validated by V3-EXQ-830.
    VALIDATION EXPERIMENT QUEUED 2026-07-29: V3-EXQ-839
    (experiments/v3_exq_839_sd084_midexec_reachability.py, ree-v3 b1a896fb1e; DIAGNOSTIC,
    claim_ids=[] so it weights nothing). Acceptance criterion taken verbatim from
    failure_autopsy_V3-EXQ-830_2026-07-29.md failure_record_entry.target:
    decomp_n_evaluated_midexec > 0 on a standard select_action -> update_residue loop with no
    hand-injected preconditions. 2 arms (handle OFF vs ON) x 6 seeds in TWO pre-registered tiers --
    ATTRIBUTABLE (3,47,71,89), which commit multi-action programs and fire the hook when ON, and
    NEGATIVE CONTROL (23,53), which commit none while pre-commit decomposition stays live, so the
    DV must be zero in both arms and the arms bit-identical. That second tier is what makes the run
    DEMONSTRATE attributability rather than assert it: a zero DV can never be confused between
    "SD-084 failed" and "this seed never committed a multi-action program" (the latter self-routes
    substrate_not_ready_requeue, never a substrate verdict). Pre-queue --seed-scan over ten seeds
    (darwin-arm64, seeded env) measured OFF midexec = 0 on ALL TEN, including seed 47 which commits
    30 multi-action programs, and ON midexec 14-29 on exactly the four attributable seeds; seed 47
    is the cleanest cell, with OFF and ON identical in multi-action commits (30), total commits (72)
    and precommit (343) so the ONLY difference is whether the hook could see the program. The run
    also records the BEHAVIOURAL delta (action-sequence divergence, committed-run length, harm) that
    the NOT-A-PURE-DIAGNOSTIC note above predicts, as non-gating payload for a successor evidence
    experiment -- this run adjudicates REACHABILITY only. Result NOT YET IN: nothing here may be
    read as validated until V3-EXQ-839 lands a manifest.
    NO claim status, confidence, live_status or v3_pending value was changed by this landing --
    MECH-321 remains candidate / v3_pending; this build makes its R4 half MEASURABLE, and
    measurement is what governance acts on. MECH-094 N/A (waking path, no simulated memory
    writes); phased training N/A (no encoder head, no new parameters).
    [2026-08-10 governance, V3-EXQ-839 CONFIRMED (failure_autopsy_V3-EXQ-839_2026-07-30)]:
    mid-execution reachability validated cleanly -- total_midexec_on=415, total_midexec_off=0
    (C1 PASS, C2 negative-control PASS). Attributability: 4/6 attributable-tier seeds fired the
    hook (matching pre-registration exactly); both non-attributable-tier seeds (23, 53) were
    bit-identical between arms as predicted. Readiness preconditions and non-degeneracy both
    green. Recommended epistemic_category: standard (not substrate_ceiling -- nothing ceilinged).
    Routing: queue a successor MECH-321 R4 EVIDENCE experiment (new EXQ, claim_ids=[MECH-321])
    testing the behavioural/task effect of the now-reachable hook (does aborting a stale
    committed macro on mid-execution decomposition improve task performance), using this run's
    behavioural_delta block as the design template per the Experimental Recording Standard.
    Chipped chip-20260810-mech321-r4-behavioural-effect. Still NO claim status/confidence change
    to MECH-321 (claim_ids=[] by design on this diagnostic) -- reachability is now measured;
    the behavioural effect is not.

- id: SD-086
  title: z_harm_a's functional readout must be a calibrated scalar valuation, not the latent norm. The latent norm conflates a large near-constant encoder offset with a small functional component; E3 consumes it as an approximately constant gain (ree_core/predictors/e3_selector.py:1182 lambda_ethical, :3128 commit-threshold urgency). Replace with a trained scalar head off z_harm_a, parallel to how harm/benefit are read via trained eval heads rather than latent norms.
  claim_type: design_decision
  subject: harm_stream.affective_readout_is_calibrated_scalar_not_norm
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-01'
    needs_review: false
  implementation_phase: v3
  claim_level: architectural
  registered_utc: '2026-08-01'
  depends_on:
  - SD-011
  - SD-019
  - SD-020
  related_claims:
  - SD-011
  - SD-019
  - SD-020
  - MECH-258
  - ARC-016
  - SD-050
  - INV-089
  - INV-090
  source:
  - docs/thoughts/2026-06-10_z_harm_a_saturation_decoupling.md
  - evidence/planning/thought_intake_2026-06-10_z_harm_a_saturation_decoupling.md
  falsifier: Two-arm comparison, norm-readout vs trained-head-readout, matched seeds. PASS requires BOTH (a) cross-state dynamic range of the readout increases by a margin scaled on the SD of the delta plus an absolute floor, and (b) at least one downstream consumer changes measurably (commit-threshold distribution or lambda_eff distribution moves beyond the seed-noise band). FAIL if range rises but nothing downstream moves -- that shows the readout is not load-bearing.
  non_degeneracy_precondition: The z_harm_a LATENT must itself carry decodable cross-state information -- a linear decode of behavioural mode or harm-event status from z_harm_a must clear a floor with non-zero cross-seed variance. If the latent is uninformative the readout form is not the defect and the run self-routes substrate_not_ready.
  scope_note: 'Curriculum-scope condition (intake candidate 5): every source number is from a ~50-episode raw warmup with scaffold_train_harm_pathway off and no scaffolded_sd054_onboarding. Conclusions are scoped to raw-warmup agents unless re-run on the curriculum-trained configuration. See project_stageh_harm_pathway_training / 603i/603k harm-pathway co-training.'
  notes: Reaped from the 2026-06-10 z_harm_a saturation-decoupling intake (candidate 1), written 2026-07-21 by session confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. Internal experimental observation from the V3-EXQ-664 affective fishtank showcase; the embodied visualisation surfaced the saturation before the scalar metrics (feedback_whimsy_visualization). No claim in the registry asserts a READOUT FORM for z_harm_a; SD-011/SD-019/SD-019b/MECH-219 constrain what the latent must encode, none constrains how it is read. Readout swap is a substrate change and should not be built until SD-087 and Q-086 report (complex, probe-gated).
  default_off_scope_note: |
    [2026-08-09 governance, GFLAG-0008 resolution]: the title's "E3 consumes it as an
    approximately constant gain" claim is scoped to a NON-DEFAULT configuration. Both named read
    sites are flag-gated and OFF by default: urgency_weight=0.0 (ree_core/utils/config.py:716,
    gates the e3_selector.py:3128 commit-threshold urgency read) and affective_harm_scale=0.0
    (ree_core/utils/config.py:723, gates the e3_selector.py:1182 lambda_eff amplification read;
    "0.0 disables (default, backward compat)"). Neither flag is set non-zero in any run of this
    claim's own evidence base (V3-EXQ-664, 856, 857a -- confirmed by grepping all three driver
    scripts, zero occurrences of either term in any). On its own evidence, E3 does NOT consume
    z_harm_a through these paths at all with default configuration; the claim is accurately read
    as "if these flags are enabled, E3 consumes z_harm_a as an approximately constant gain",
    not as a description of the shipped default behaviour. Same shape as SD-020's own
    default_off_scope_note (see that claim). Any two-arm norm-vs-trained-head falsifier for this
    claim must assert consumer engagement (urgency_weight>0 or affective_harm_scale>0) or its
    downstream conjunct (falsifier requirement (b)) is vacuously unsatisfiable on default config.
    No status change -- this note scopes the claim, it does not weaken or falsify it.
- id: SD-087
  title: SD-020's stable reading is scoped to the flag-on configuration and does not describe default-trained agents. harm_surprise_pe_enabled defaults to False (ree_core/utils/config.py:2656), so the default-trained substrate trains z_harm_a against the EMA accumulated-harm target SD-020 argues against; SD-020's stable/PASS reading (V3-EXQ-324b) is scoped to the flag-on config and that scoping is not recorded on the claim.
  claim_type: design_decision
  subject: harm_stream.sd020_stable_reading_is_flag_on_scoped
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-01'
    needs_review: false
    evidence:
      from: v3_exq_856_sd087_harm_surprise_pe_fingerprint_20260801T124431Z_v3
      as_of: 2026-08-01
      verdict: "weakens -- see evidence_quality_note"
  implementation_phase: v3
  claim_level: governance
  registered_utc: '2026-08-01'
  depends_on:
  - SD-020
  related_claims:
  - SD-020
  - SD-019
  - MECH-219
  - Q-036
  source:
  - docs/thoughts/2026-06-10_z_harm_a_saturation_decoupling.md
  - evidence/planning/thought_intake_2026-06-10_z_harm_a_saturation_decoupling.md
  falsifier: 'Re-run the 664 affective fingerprint as a two-arm experiment, harm_surprise_pe_enabled OFF vs ON, everything else matched. PASS (claim upheld) = the saturation-and-inversion signature is present in OFF and materially reduced in ON: OFF shows within-episode CoV below a pre-registered floor AND inverted mode-ordering (shelter > avoid > freeze) while ON shows neither. FAIL = the signature survives the flag flip, relocating the defect to the encoder or environment (hands the question to Q-086).'
  non_degeneracy_precondition: The OFF arm must reproduce the original 664 signature before the ON arm means anything, and the two arms must differ on the training loss they actually optimise (assert the PE branch at ree_core/agent.py:9787, inside compute_harm_accum_loss defined at :9743, is entered). An ON arm that silently falls through to the EMA target tests nothing and self-routes substrate_not_ready.
  scope_note: Same raw-warmup curriculum-scope condition as SD-086 (intake candidate 5).
  notes: 'Reaped from the 2026-06-10 intake (candidate 2), written 2026-07-21 by confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. This is a claim-status-vs-default drift finding of the same shape as reference_reeconfig_from_dims_silent_kwargs (a knob that exists but is not reached). SD-020 carries a default_off_scope_note pointing here. Highest-information first move: the one-flag two-arm run separating ''SD-020 was right and is simply not on'' from ''the pathology survives the fix''. Consider a standing default_off annotation audit across other claims (intake next-step 4).'
  evidence_quality_note: >
    [2026-08-01 governance, V3-EXQ-856, confirmed failure_autopsy_V3-EXQ-856_2026-08-01, first-ever evidence run for this claim]: SD-087's own pre-registered falsifier (two-arm harm_surprise_pe_enabled OFF/ON re-run of the V3-EXQ-664 affective fingerprint). off_reproduces_664_signature PASSES (2/3 seeds); on_reduces_signature FAILS -- the 664 saturation-and-inversion signature SURVIVES the flag flip even though the manipulation demonstrably took (mean_harm_obs_ema moved 0.0->0.0245; mean_cov_z_harm_a barely moved, both far below the 0.05 saturation floor). This does NOT falsify SD-087's literal, narrower claim (that SD-020's validation is scoped to a flag-on configuration and does not describe default agents) -- that remains true regardless. It DOES weaken the stronger, implicit causal hypothesis the falsifier was built to test: that simply flipping the flag on an already-trained agent reproduces SD-020's validated benefit. Relocates the explanation for the 664 signature toward either (a) the flag needing to be set from the START of training, or (b) a defect in the encoder/environment rather than this training-target choice -- handed to Q-086 (V3-EXQ-857, which could not itself discriminate; see Q-086's own note). weakens, scoped to the causal-flag-flip hypothesis. Stays candidate.

    [2026-08-02 cross-reference check, chip-20260802-sd087-q086-conflict-check]: Q-086's own diagnostic leg (V3-EXQ-857a, confirmed failure_autopsy_V3-EXQ-857a_2026-08-02, paired-by-seed reanalysis) has now resolved the other half of the (a)/(b) fork this note opened. 857a held harm_surprise_pe_enabled=False throughout (verified: ree-v3/experiments/v3_exq_857a_q086_gentler_env_fingerprint_redesign.py:264,335 -- the same flag-OFF training-target config as this claim's own ARM_OFF) and varied environment harshness instead (num_hazards 4/1/0). Result: the saturation-and-inversion signature persists regardless of environmental harshness -- ARM_BENIGN (num_hazards=0, the true hazard-free floor) stays CoV-saturated AND its z_harm_a level is INVERTED (higher, not lower) versus ARM_HARSH, ruling out "environment" and resolving to calibration_pathology_representational. Combined with this claim's own null result (signature survives the training-target flag flip too), two independent axes -- training-target choice and environmental harshness -- have now both been tested and both show the signature is insensitive to the manipulation. This BEARS ON (narrows, does not resolve) this claim's residual disjunction: option (b) is no longer an open pair of untested alternatives -- "environment" is now eliminated, leaving the defect positively evidenced as encoder/representation-side. Option (a) (flag needs to be set from the START of training, not post-hoc) remains the one hypothesis neither 856 nor 857a has directly tested, and is the last route left for treating "the SD-020 flag is the fix" as still live. Does not change this claim's literal text or its `candidate` status. No claims.yaml disposition change made here; recommend a /governance session jointly re-read SD-087/Q-086's evidence_direction and live_status now that both diagnostic legs converge on the same calibration-pathology explanation.

    [2026-08-03 governance, joint SD-087/Q-086 re-read per the 2026-08-02 cross-reference's own
    request]: both diagnostic legs (856 on the training-target axis, 857a on the environmental-
    harshness axis) now converge on calibration_pathology_representational, narrowing this claim's
    residual disjunction to exactly ONE untested hypothesis: the flag needs to be set from the START
    of training, not applied post-hoc to an already-trained agent (option (a)). No status or
    evidence_direction change (weakens, scoped to the causal-flag-flip hypothesis, stands). Routed
    /queue-experiment for a train-from-scratch-with-flag-ON test of the 664 affective fingerprint --
    the decisive remaining test to close this disjunction either way.

    [2026-08-08 governance, GFLAG-0006 correction]: the routed train-from-scratch-with-flag-ON test
    above was NOT actually outstanding -- V3-EXQ-856's own ARM_ON already set harm_surprise_pe_enabled
    at agent construction, before warmup (make_agent_and_env then warmup_train), so it already tested
    flag-from-the-start; the 664 signature survived anyway (evidence/reanalysis/
    reanalysis_sd087_flag_from_start_already_tested_20260803T144516Z.json, confirmed 2026-08-03).
    Option (a) is CLOSED, not open. The joint SD-087/Q-086 disjunction (training-target choice,
    environmental harshness, flag-from-start) has now had all three axes tested with none explaining
    the signature away; the residual reading is an as-yet-unidentified encoder/representation defect
    (calibration_pathology_representational). No further /queue-experiment is owed on this specific
    disjunction. Status stays candidate; SD-020's stable-vs-flag-on-scoping question (this claim's own
    literal assertion) is unaffected and separately flagged for review (GFLAG-0005).
- id: Q-086
  title: 'Environment-confound control: is the z_harm_a saturation ecological or representational? Re-measure the affective fingerprint on a gentler environment (lower num_hazards, lower hazard_food_attraction) to separate faithful chronic-suffering integration from a calibration/representation pathology.'
  claim_type: open_question
  subject: harm_stream.saturation_ecological_vs_representational
  polarity: asks
  status: candidate
  live_status:
    reading: answered_pending_confirmatory_pass
    as_of: '2026-08-02'
    needs_review: false
    evidence:
      from: v3_exq_857a_q086_gentler_env_fingerprint_redesign_20260802T015401Z_v3
      as_of: 2026-08-02
      verdict: "supports 'calibration, not ecology' (paired-by-seed reanalysis) -- see evidence_quality_note"
  epistemic_category: answer_state
  implementation_phase: v3
  claim_level: architectural
  registered_utc: '2026-08-01'
  depends_on:
  - SD-086
  - SD-087
  related_claims:
  - SD-011
  - SD-019
  - INV-053
  - MECH-219
  source:
  - docs/thoughts/2026-06-10_z_harm_a_saturation_decoupling.md
  - evidence/planning/thought_intake_2026-06-10_z_harm_a_saturation_decoupling.md
  what_would_answer: 'PASS for ''calibration, not ecology'' = z_harm_a still pegs high with sub-floor CoV in the benign environment. PASS for ''faithful chronic suffering'' = the level and range track hazard density. Non-degeneracy: the manipulation must demonstrably take -- the sensory tier z_harm_s must differ across arms (mean or CoV beyond the seed-noise band); if z_harm_s is unmoved the environment was not actually made gentler and the arm self-routes substrate_not_ready.'
  scope_note: Same raw-warmup curriculum-scope condition as SD-086/SD-087 (intake candidate 5).
  notes: Reaped from the 2026-06-10 intake (candidate 3, the raw thought's own confound control), written 2026-07-21 by confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. Should run alongside or after SD-087's flag flip. Queue only via /queue-experiment (close relative of V3-EXQ-664).
  evidence_quality_note: >
    [2026-08-01 governance, V3-EXQ-857, confirmed failure_autopsy_V3-EXQ-857_2026-08-01, first-ever run, diagnostic self-route flagged precondition_unmet in pending_review.md]: the readiness precondition (gentle_env_manipulation_took_z_harm_s_differs) FAILED -- measured SNR 0.42 vs required >=2.0; the manipulation (num_hazards cut 75%, hazard_food_attraction cut 71%) did not move z_harm_s beyond seed-to-seed noise (delta 0.072 vs noise SD 0.171). Reads as a power problem (only 3 seeds against high per-seed variance), not a wrong hypothesis about what drives z_harm_s -- an uncounted secondary signal (min_eval_steps 58 harsh vs 125 gentle) suggests the harsh arm IS doing something (plausibly earlier hazard-driven episode termination) that the per-step z_harm_s average doesn't capture. non_contributory -- no discrimination was possible between the 'calibration, not ecology' and 'faithful chronic suffering' readings. Net result of the SD-087(V3-EXQ-856) -> Q-086(V3-EXQ-857) diagnostic chain: the underlying question (what explains the 664 saturation-and-inversion signature?) remains open on BOTH candidate explanations -- neither confirmed nor eliminated. Route: /queue-experiment a redesign with more seeds (5-10), a starker environmental gradient (e.g. num_hazards 4 vs 0), and episode-survival-time as an explicit secondary DV. Stays candidate.


    [2026-08-02 governance, V3-EXQ-857a, confirmed failure_autopsy_V3-EXQ-857a_2026-08-02, second attempt, self-route flagged precondition_unmet in pending_review.md, USER-CONFIRMED reversal]: the driver's own readiness precondition (unpaired pooled-SD SNR across 6 seeds x 2 arms) FAILED again (0.388 vs required >=2.0) despite double the seeds and the starkest possible gradient (num_hazards 0 vs 4), and the driver's own code flagged this as possibly indicating an ecologically-insensitive readout. A PAIRED-BY-SEED reanalysis (the design already shares seeds identically across arms via torch.manual_seed(seed)) overturns this: 5 of 6 seeds show a consistent positive delta, paired SNR=2.20 clears the same K=2.0 bar the unpaired test failed. Cross-run reproducibility (857's 3-seed subset is bit-identical to 857a's) confirms the discarded 'noise' is a deterministic per-seed effect (seed=1 is an outlier in ALL THREE arms equally), not sampling variance -- exactly the signature 851's autopsy independently flagged in a different lineage. Once the gate is read as cleared, the driver's own already-computed discrimination resolves cleanly: ARM_BENIGN stays CoV-saturated (0.029 < 0.05 floor) AND its z_harm_a level is higher, not lower, than ARM_HARSH (8.64 vs 7.41, an INVERSION) -- calibration_pathology_representational, not faithful_chronic_suffering_ecological. Consistent with the original 664 saturation-and-inversion signature. Route: low-priority /queue-experiment V3-EXQ-857b to fix the readiness statistic to a paired-by-seed SNR for this lineage's future reuse (confirmatory, not re-litigation). Q-086 reads answered pending that confirmatory pass; evidence_direction supports 'calibration, not ecology.'

    [2026-08-03 governance, joint SD-087/Q-086 re-read]: SD-087's own null result (816-signature
    survives the training-target flag flip) independently corroborates this claim's
    calibration_pathology_representational reading via a second, orthogonal axis. Combined, both
    axes tested (environmental harshness here, training-target choice on SD-087) point the same way;
    the one hypothesis neither has tested is SD-087's option (a) -- setting the flag from the START
    of training. Routed jointly with SD-087 to a train-from-scratch-with-flag-ON /queue-experiment.
    No status/epistemic_category change.

    [2026-08-08 governance, GFLAG-0006 correction, mirrors the same-day SD-087 note]: the routed
    train-from-scratch-with-flag-ON test was already answered by V3-EXQ-856's own ARM_ON (flag set
    at construction, before warmup) -- see evidence/reanalysis/
    reanalysis_sd087_flag_from_start_already_tested_20260803T144516Z.json. All three axes (training-
    target, environmental harshness, flag-from-start) are now tested with none explaining the 664
    signature away; residual reading is calibration_pathology_representational (encoder/representation
    defect). No further /queue-experiment is owed on this disjunction. Status stays candidate.
- id: Q-087
  title: 'What event counts as V3 closure for the purpose of the version-transition artefact gate (GOV-V3FREEZE-1) and the construction-opacity repayment boundary (GOV-OPACITY-1)? Candidate options: strict green board, governance acceptance, or an additional reproducibility check.'
  claim_type: open_question
  subject: governance.v3_closure_definition
  polarity: asks
  status: resolved
  live_status:
    reading: answered
    as_of: '2026-08-01'
    needs_review: false
  epistemic_category: answer_state
  claim_level: governance
  registered_utc: '2026-08-01'
  resolved_utc: '2026-08-01'
  resolution: governance_acceptance
  resolution_note: 'User decision 2026-08-01 (session cool-sutherland-623d3f, in response to the deferred intake registration): V3 is closed when a governance decision records it, with a decision_log entry, INDEPENDENT of residual closure-node count. Chosen over strict green board (9 nodes currently blocked and the 2026-07-19 target already slipped, so that event may never fire and would make GOV-V3FREEZE-1 permanently inert) and over green-board-plus-reproducibility (inherits that risk and partly duplicates the gate''s own PASS criterion). Robust to substrate-blocked V4/V5 nodes and makes the gate trigger on a controllable date. The known risk -- a judgement-call boundary can drift -- is exactly what GOV-OPACITY-1''s dated-repayment audit exists to catch.'
  depends_on: []
  related_claims:
  - GOV-V3FREEZE-1
  - GOV-OPACITY-1
  - GOV-SUBTRACT-1
  source:
  - docs/thoughts/2026-06-21_v3_closure_as_external_legibility_boundary.md
  - evidence/planning/thought_intake_2026-06-21_v3_closure_as_external_legibility_boundary.md
  notes: Reaped from the 2026-06-21 V3-closure-legibility intake (candidate 4), written 2026-07-21 by confident-pare-9273f1, resolved and registered 2026-08-01 by cool-sutherland-623d3f. Candidate 4 was the critical-path decision on which the intake's candidates 1-3 were undecidable (definition_not_ready). With it resolved, GOV-V3FREEZE-1 is definition_ready.
- id: GOV-V3FREEZE-1
  title: 'Version-transition artefact gate: V4 substrate expansion must not erase the opportunity to freeze, reproduce, describe and inspect the completed V3. Before substantial V4 substrate expansion a defined minimum V3 closure package must exist AND double as a manipulable causal reference artefact for later cross-version comparison.'
  claim_type: governance_rule
  subject: governance.version_transition.v3_freeze_and_reference_record
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-01'
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: '2026-08-01'
  source_thought: evidence/planning/thought_intake_2026-06-21_v3_closure_as_external_legibility_boundary.md
  binds_at_version: v3
  blocks_v3_green_board: false
  depends_on:
  - Q-087
  - SENT-14
  - SENT-0
  related_claims:
  - SENT-14
  - SENT-0
  - GOV-EXT-1
  - GOV-PROC-1
  - INV-077
  - SENT-17
  source:
  - docs/thoughts/2026-06-21_v3_closure_as_external_legibility_boundary.md
  - evidence/planning/thought_intake_2026-06-21_v3_closure_as_external_legibility_boundary.md
  - docs/thoughts/2026-06-24_evolutionary_retention_and_post_ree_cognifold_compilation.md
  - evidence/planning/thought_intake_2026-06-24_evolutionary_retention_and_post_ree_cognifold_compilation.md
  functional_restatement: 'TWO RATIONALES, ONE GATE (registered jointly per the 2026-06-21 candidate 1 and the 2026-06-24 candidate 5, which reach the same object from opposite directions). Rationale A (external legibility): SENT-14/GOV-EXT-1/GOV-SEC-1 all gate RELEASE; nothing gates the START of V4 on the EXISTENCE of a frozen, inspectable V3. Once V4 moves the substrate the validated object stops existing in the form that was validated. Rationale B (causal reference): a working V3 is only usable as a manipulable reference for later efficiency/compression comparison if its invariants are captured before the substrate moves. MINIMUM V3 CLOSURE PACKAGE (working contents, fixed at registration): one canonical entry point; one frozen V3 specification; one reproducible flagship demonstration; one statement of what V3 does and does not establish; one findings document separating positive results, failures, unresolved claims and scope limits; PLUS the reference-record axes -- behavioural invariants, internal-state invariants, developmental milestones, ethical-constraint traces, failure modes, recovery profiles, computational costs, and memory/latency/sample requirements. AUDIT-SHAPED PASS/FAIL: PASS = every package item exists at a named path AND an independent reader reproduces the flagship result from the frozen spec alone AND every reference axis is recoverable without re-running V3. FAIL = V4 substrate work crosses the named threshold with any item absent or any axis unrecoverable. DECIDABILITY PRECONDITION (governance analogue of non-degeneracy): auditable only once ''V3 closure'' and ''substantial V4 expansion'' both have operational definitions. V3 closure is now DEFINED as governance acceptance (Q-087, user decision 2026-08-01). ''Substantial V4 expansion'' still needs an operational threshold; absent it the gate self-routes definition_not_ready on that leg. NOT a stop-work order: this is a produce-before-submerge requirement, not a block on all V4 work. Flagship selection is deferred (a fact recoverable from the evidence index at package-build time, not a proposition to hold).'
  notes: 'Reaped jointly from the 2026-06-21 closure intake (candidate 1) and the 2026-06-24 evolutionary-retention intake (candidate 5), written 2026-07-21 by confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. governance_rule, not MECH/ARC; no substrate falsifier, no EXQ id. Composes with SENT-17 (substitution must preserve internal ethical-process representations). Existing partial machinery: docs/public_explorer/, scripts/export_public_explorer.py, docs/public_explorer_policy.md, docs/START_HERE_HOW_REE_DEVELOPS.md, docs/CURRENT_FRONT.md, docs/REE_MIN_SPEC.md, docs/closure_dashboard.md.'
- id: GOV-OPACITY-1
  title: Construction-phase opacity is acceptable technical debt only while it carries a named, dated repayment boundary. External inscrutability during construction is a legitimate choice, not a defect, provided the boundary is declared and reviewed on the same cadence as SENT-0 (every generation boundary).
  claim_type: governance_rule
  subject: governance.opacity_as_dated_technical_debt
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-01'
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: '2026-08-01'
  source_thought: evidence/planning/thought_intake_2026-06-21_v3_closure_as_external_legibility_boundary.md
  binds_at_version: v3
  blocks_v3_green_board: false
  depends_on:
  - Q-087
  - SENT-0
  - GOV-PROC-1
  related_claims:
  - SENT-0
  - GOV-PROC-1
  - GOV-V3FREEZE-1
  source:
  - docs/thoughts/2026-06-21_v3_closure_as_external_legibility_boundary.md
  - evidence/planning/thought_intake_2026-06-21_v3_closure_as_external_legibility_boundary.md
  functional_restatement: 'The positive half of the position and the reason it is a rule rather than a preference: inscrutability is acceptable while it carries a defined repayment boundary and unacceptable once it does not. It also names a real cost of PREMATURE externalisation that no existing claim states -- external legibility creates a second design target while the architecture is still moving and can distort research toward what is easiest to explain rather than what most needs testing (the rule''s rationale, not a delay tactic). AUDIT-SHAPED PASS/FAIL: PASS = a boundary event and date are recorded and unchanged, or changed with a recorded reason. FAIL = no boundary recorded, or the date has moved more than once without recorded justification (the drift failure mode -- how construction-phase opacity becomes permanent opacity). DECIDABILITY PRECONDITION: requires at least one named boundary event to exist; a rule with no declared boundary is vacuous rather than violated and reports definition_not_ready, not FAIL. The boundary event is now nameable: governance acceptance of V3 closure (Q-087).'
  notes: Reaped from the 2026-06-21 closure intake (candidate 2), written 2026-07-21 by confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. This rule is the audit that catches GOV-V3FREEZE-1's governance-acceptance boundary drifting.
- id: GOV-SUBTRACT-1
  title: 'Legibility by subtraction: the V3 closure package is produced by curating and compressing existing material, not by rewriting the research history. Separate the primary reader path (current work) from supporting research and from operational archaeology; retain audit history but remove it from the reader path; mark human vs agent provenance in the public account.'
  claim_type: governance_rule
  subject: governance.legibility_by_subtraction
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-01'
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: '2026-08-01'
  source_thought: evidence/planning/thought_intake_2026-06-21_v3_closure_as_external_legibility_boundary.md
  binds_at_version: v3
  blocks_v3_green_board: false
  depends_on:
  - GOV-V3FREEZE-1
  - INV-077
  related_claims:
  - INV-077
  - SD-062
  - GOV-V3FREEZE-1
  source:
  - docs/thoughts/2026-06-21_v3_closure_as_external_legibility_boundary.md
  - evidence/planning/thought_intake_2026-06-21_v3_closure_as_external_legibility_boundary.md
  functional_restatement: 'The method claim that makes GOV-V3FREEZE-1 affordable -- without it, ''produce a frozen V3 artefact'' reads as a months-long documentation project and will not happen. Interlocks with INV-077 at the presentation layer: human/agent provenance must be visible in the public account, not only enforced at the confidence-update boundary. AUDIT-SHAPED PASS/FAIL: PASS = a reader path exists whose length from entry point to a reproducible result is bounded by a pre-registered number of hops, with archaeology reachable but off that path. FAIL = the package is produced as new prose narrating the whole history. DECIDABILITY PRECONDITION: requires an agreed entry point and an agreed ''reproducible result'' target; without both, hop-count is unmeasurable and the rule reports definition_not_ready.'
  notes: Reaped from the 2026-06-21 closure intake (candidate 3), written 2026-07-21 by confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. Repository consolidation is explicitly out of scope and must not be smuggled in under this rule.
- id: ARC-115
  title: Confidence readouts are non-collapsible, and externally-induced agreement is a separate channel from internally-derived confidence. A single scalar confidence conflates propositional confidence, attractor stability, cross-subsystem agreement, conflict pressure, action-readiness, and socially-supplied agreement; the last must not enter the same accumulator as the others -- else being told something becomes indistinguishable from having found it.
  claim_type: architecture_hypothesis
  subject: confidence.externally_induced_agreement_is_separate_channel
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-01'
    needs_review: false
  epistemic_category: substrate_conditional
  claim_level: architectural
  implementation_phase: v5
  version_relevance: v5
  registered_utc: '2026-08-01'
  depends_on:
  - MECH-059
  - INV-033
  - INV-077
  related_claims:
  - MECH-059
  - MECH-063
  - INV-033
  - MECH-065
  - ARC-016
  - EXT-001
  - INV-077
  - MECH-424
  - MECH-192
  - ARC-116
  source:
  - docs/thoughts/2026-06-23_cross_system_resonance_and_inference_calibration.md
  - evidence/planning/thought_intake_2026-06-23_cross_system_resonance_and_inference_calibration.md
  falsifier: Stage dissociation, not a performance test. An external assertion should raise the social-agreement channel and, on its own, must NOT raise commit-readiness the way accumulated internal evidence does. PASS requires the two paths to be dissociable -- an assertion-only manipulation and an evidence-only manipulation produce distinguishable readout profiles, with the collapsed control showing them indistinguishable. FAIL if performance merely drops without dissociation.
  non_degeneracy_precondition: The confidence readouts must be live -- non-zero cross-arm and cross-seed variance on each channel being dissociated, not floor-pinned. A channel that never moves cannot be shown separate from another that never moves; the run self-routes substrate_not_ready.
  notes: 'Reaped from the 2026-06-23 cross-system-resonance intake (candidate 1), written 2026-07-21 by confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. Substrate-BLOCKED in V3: there is no external-assertion input channel; nearest live analogue is z_beta leakage (MECH-192), which is affective not propositional. This is the architectural form of the circularity worry INV-077 forbids at the GOVERNANCE layer but nothing forbids at the AGENT layer. EXT-001 (sycophancy) is arguably the BEHAVIOURAL consequence of exactly this collapse -- checked at registration and kept as a peer claim (architecture/mechanism) rather than merged (EXT-001 is the external_failure_mode symptom, ARC-115 the architectural constraint). Same non-collapse family as ARC-116 (introspection intake candidate 3), a different cut.'
- id: GOV-CALIB-1
  title: 'Calibration ledger: pre-registered felt confidence and explicit estimates, with blinding status, are recorded before outcomes are known. An Assembly instrument (not an agent mechanism) that is the only way to distinguish genuine calibration from retrospective resonance.'
  claim_type: governance_rule
  subject: governance.calibration_ledger_pre_registered_predictions
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-01'
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: '2026-08-01'
  source_thought: evidence/planning/thought_intake_2026-06-23_cross_system_resonance_and_inference_calibration.md
  binds_at_version: v3
  blocks_v3_green_board: false
  depends_on:
  - INV-077
  related_claims:
  - SD-062
  - GOV-FROZEN-1
  - GOV-CONFIRM-1
  - INV-077
  source:
  - docs/thoughts/2026-06-23_cross_system_resonance_and_inference_calibration.md
  - evidence/planning/thought_intake_2026-06-23_cross_system_resonance_and_inference_calibration.md
  functional_restatement: 'Record, BEFORE outcomes are known, the user''s felt confidence, the assistant''s explicit estimate, whether each was generated blind to the other, the key reasons, and later the outcome -- classified with the existing vocabulary (supports / does_not_support / non_contributory / substrate_ceiling / superseded). Nothing in the registry does this: SD-062 records per-claim multi-axis state but not pre-registered predictions with provenance and blinding; GOV-FROZEN-1 freezes what will be counted, not who predicted what with what confidence. PASS/FAIL (audit + measurement): PASS = pre-registered felt confidence beats a shuffled-outcome null by a margin scaled on the SD of the delta plus an absolute floor. FAIL = indistinguishable from the null (a genuinely useful negative, to be reported not buried). NON-DEGENERACY, both live: (a) blinding must actually occur on a recorded subset -- entries where either party saw the other''s number first are marked and excluded from the calibration statistic; empty subset -> measures agreement not calibration -> insufficient_blind_entries. (b) outcome variance must be non-zero -- a ledger where nearly every entry resolves the same way has no discriminating signal; require a floor on outcome-class spread before computing. Buildable now but slow-yielding by construction (produces nothing until enough blind entries accumulate) -- start early or not at all.'
  notes: Reaped from the 2026-06-23 resonance intake (candidate 2), written 2026-07-21 by confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. Assembly instrument, not an agent claim; no EXQ id. The discipline note 'human-AI agreement is not independent replication' (candidate 4) is already INV-077 and is not re-registered.
- id: SD-088
  title: The claims index must represent source-dependence between supporting judgements. Two supports sharing a source (a shared driver, a shared run, or one party reasoning from the other's framing) must not contribute independently to confidence. A schema addition (edge type or per-support source_dependence field).
  claim_type: design_decision
  subject: registry.schema.source_dependence_between_supports
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-01'
    needs_review: false
  claim_level: governance
  registered_utc: '2026-08-01'
  depends_on:
  - SD-062
  related_claims:
  - SD-062
  - GOV-CONFIRM-1
  - INV-077
  - GOV-REUSE-1
  source:
  - docs/thoughts/2026-06-23_cross_system_resonance_and_inference_calibration.md
  - evidence/planning/thought_intake_2026-06-23_cross_system_resonance_and_inference_calibration.md
  falsifier: PASS = at least one real correlated-support instance is detected and its double-count removed, with the affected claim's confidence changing. FAIL = the field is populated but never changes any confidence (decorative).
  non_degeneracy_precondition: At least one claim in the registry must actually have two supports sharing a source; if none exists the schema addition is vacuous -- report no_instances rather than shipping an unexercised field. A retrospective scan of existing claims is the cheap way to check before building.
  notes: Reaped from the 2026-06-23 resonance intake (candidate 3), written 2026-07-21 by confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. SD-062 gives per-claim multi-axis state; GOV-CONFIRM-1 detects confirmer EXPERIMENTS; neither represents source-dependence between two supporting JUDGEMENTS. The general case (two experiments sharing a driver, two claims supported by the same run) has the same structure. Cheapest first move is the precondition scan over claims.yaml + the evidence index.
- id: SD-089
  title: 'Claim-origin provenance: the registry records the epistemic origin class of each claim. Classes: introspective / literature / computational-analogy / experimental-anomaly / convergent. Backfilled entries marked as reconstructed, not recorded (per MECH-094''s own logic). The registry''s source: field holds file paths, not epistemic origin class.'
  claim_type: design_decision
  subject: registry.schema.claim_origin_provenance_class
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-01'
    needs_review: false
  claim_level: governance
  registered_utc: '2026-08-01'
  depends_on:
  - SD-062
  related_claims:
  - SD-062
  - INV-077
  - ARC-106
  - MECH-094
  source:
  - docs/thoughts/2026-06-23_introspection_as_architectural_evidence_for_ree.md
  - evidence/planning/thought_intake_2026-06-23_introspection_as_architectural_evidence_for_ree.md
  falsifier: The field's testable proposition is the productivity audit -- do origin classes differ in promotion rate, demotion rate, and substrate-ceiling rate? PASS = a class-difference beyond the null, an actionable read on where hypotheses are worth sourcing. A NULL result is a real and reportable finding (origin does not predict productivity), which would justify NOT maintaining the field.
  non_degeneracy_precondition: (a) Each class must be non-degenerately populated -- a class with a handful of members supports no rate comparison; require a floor on per-class count before computing. (b) The outcome variable must be live -- promotion/demotion outcomes must have non-zero spread; if nearly all claims sit at candidate the rate comparison is floor-pinned and self-routes insufficient_outcome_variance.
  notes: Reaped from the 2026-06-23 introspection intake (candidate 2), written 2026-07-21 by confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. REGISTERED FIRST in the introspection cluster because GOV-INTRO-1 and GOV-OVERFIT-1 are unauditable without an origin field (schema_not_ready otherwise). The introspective-origin retrieval over docs/thoughts/ + intake corpus + git history is both the productivity audit's input and the backfill source -- one job. Backfill is reconstruction and will be partly confabulated (MECH-094 turned on the registry); mark it reconstructed.
- id: GOV-INTRO-1
  title: 'Triangulation rule: introspection generates architectural hypotheses; it never validates them alone. A mechanism first noticed introspectively acquires standing only when it also maps onto neuroscience or cognitive literature, is computationally coherent, produces measurable predictions, and survives experiment. The phenomenology-side counterpart to ARC-106.'
  claim_type: governance_rule
  subject: governance.introspection_generates_never_validates_alone
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-01'
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: '2026-08-01'
  source_thought: evidence/planning/thought_intake_2026-06-23_introspection_as_architectural_evidence_for_ree.md
  binds_at_version: v3
  blocks_v3_green_board: false
  depends_on:
  - SD-089
  - ARC-106
  related_claims:
  - ARC-106
  - INV-062
  - INV-077
  - MECH-094
  - SD-089
  source:
  - docs/thoughts/2026-06-23_introspection_as_architectural_evidence_for_ree.md
  - evidence/planning/thought_intake_2026-06-23_introspection_as_architectural_evidence_for_ree.md
  functional_restatement: 'REE''s architecture was substantially generated from the user''s phenomenology (''I have based REE on how my mind feels as much as my knowledge of biology and computing''); the practice is currently entirely undocumented, so it cannot be audited, defended externally, or corrected. Registering it is as much a constraint as a licence. PASS/FAIL (compliance audit, not experiment): PASS = every claim whose recorded origin is introspective carries at least one non-introspective support before promotion above candidate. FAIL = any such claim promoted on phenomenological plausibility alone. DECIDABILITY PRECONDITION (governance analogue of non-degeneracy): the audit is undecidable until claim-origin provenance exists -- without SD-089 the rule cannot be checked and self-routes schema_not_ready. Do not treat GOV-INTRO-1 as auditable before SD-089 lands.'
  notes: 'Reaped from the 2026-06-23 introspection intake (candidate 1), written 2026-07-21 by confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. Precedent that the registry already licenses phenomenology-as-data for one domain: INV-062 (dream phenomenology -> four computational operations). This never authorises introspection as evidence for promotion (that would violate INV-077) -- it bounds it.'
- id: ARC-116
  title: Generation, metacognitive readout, and linguistic reconstruction are three non-collapsible stages. The confidence readout must not be derived from the verbal reconstruction, and neither may substitute for the generating dynamics. The felt sequence nonverbal-settling -> felt-confidence -> verbal-reconstruction -> external-testing implies three things REE currently treats as two at most.
  claim_type: architecture_hypothesis
  subject: cognition.generation_readout_reconstruction_noncollapse
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-01'
    needs_review: false
  epistemic_category: substrate_conditional
  claim_level: architectural
  implementation_phase: v5
  version_relevance: v5
  registered_utc: '2026-08-01'
  depends_on:
  - ARC-025
  - MECH-069
  related_claims:
  - ARC-025
  - MECH-069
  - INV-033
  - MECH-059
  - ARC-048
  - MECH-380
  - MECH-424
  - ARC-115
  source:
  - docs/thoughts/2026-06-23_introspection_as_architectural_evidence_for_ree.md
  - evidence/planning/thought_intake_2026-06-23_introspection_as_architectural_evidence_for_ree.md
  falsifier: 'Dissociation under degradation. Degrade the reconstruction channel; a three-stage system''s confidence readout should be substantially preserved while a collapsed system''s should degrade with it. PASS requires the two architectures to be distinguishable on the readout under matched reconstruction degradation, not merely that performance drops. Practical consequence: if a system derives its confidence readout FROM its verbal reconstruction, degrading the reconstruction should corrupt the confidence -- a real testable dissociation and a safety property.'
  non_degeneracy_precondition: The metacognitive readout must be live in the intact baseline -- non-zero cross-arm and cross-seed variance, not floor-pinned; a readout that never moves cannot be shown preserved or degraded, and the run self-routes substrate_not_ready.
  notes: 'Reaped from the 2026-06-23 introspection intake (candidate 3), written 2026-07-21 by confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. Substrate-BLOCKED (no language/reconstruction channel in V3); scoped to V5. Same non-collapse argument-shape as ARC-025 (engine irreducibility) and MECH-069 (error incommensurability). Closely related to but distinct from ARC-115 (resonance intake candidate 1): same non-collapse family, different cut -- registered coherently as a pair.'
- id: GOV-OVERFIT-1
  title: 'Designer-overfit standing check: a mechanism first sourced from one person''s phenomenology requires cross-task and cross-agent validation before being treated as general cognition. REE''s n=1 phenomenological base is a genuine external criticism with no registered answer; this is the hedge given standing.'
  claim_type: governance_rule
  subject: governance.designer_overfit_cross_task_validation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-01'
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: '2026-08-01'
  source_thought: evidence/planning/thought_intake_2026-06-23_introspection_as_architectural_evidence_for_ree.md
  binds_at_version: v3
  blocks_v3_green_board: false
  depends_on:
  - SD-089
  related_claims:
  - SD-089
  - ARC-106
  source:
  - docs/thoughts/2026-06-23_introspection_as_architectural_evidence_for_ree.md
  - evidence/planning/thought_intake_2026-06-23_introspection_as_architectural_evidence_for_ree.md
  functional_restatement: 'PASS/FAIL (audit): PASS = every introspectively-originated claim treated as general carries validation in at least two structurally different task families. FAIL = generality asserted from a single environment. DECIDABILITY PRECONDITION: requires (a) SD-089''s origin field, and (b) at least two structurally different task families to exist. V3 currently has effectively ONE, so this rule is presently VACUOUS rather than satisfied -- and saying so plainly is the point of registering it. Report insufficient_task_families, never PASS.'
  notes: 'Reaped from the 2026-06-23 introspection intake (candidate 4), written 2026-07-21 by confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. Hits the same single-domain wall as the 2026-07-14 rule-apprehension intake candidate 2 (domain-generality fork). Designer-overfit risk: the user''s ADHD, strong nonverbal systems reasoning, spatial/motor intuition and long immersion in REE shape what is available to introspection.'
- id: ARC-119
  title: 'Language-input update policy: an utterance proposes candidate model states which are evaluated before any update; it never installs a belief. Generalises INV-085 from affect to arbitrary content and ARC-104 from the harm stream to all content. Evaluation criteria: context, source reliability, causal compatibility with existing world-model structure, memory, uncertainty, current goals, speaker model.'
  claim_type: architecture_hypothesis
  subject: language.input_proposes_candidate_states_never_installs_belief
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-01'
    needs_review: false
  epistemic_category: substrate_conditional
  claim_level: architectural
  implementation_phase: v5
  version_relevance: v5_v6
  registered_utc: '2026-08-01'
  depends_on:
  - INV-085
  - ARC-104
  - MECH-424
  related_claims:
  - INV-085
  - ARC-104
  - ARC-103
  - INV-007
  - MECH-424
  - MECH-373
  - MECH-418
  source:
  - docs/thoughts/2026-06-23_language_as_cooperation_interface.md
  - evidence/planning/thought_intake_2026-06-23_language_as_cooperation_interface.md
  falsifier: 'Inject an assertion that contradicts available world evidence, under a source-reliability manipulation. PASS = the resulting belief tracks the EVIDENCE and the SOURCE RELIABILITY rather than the assertion: a low-reliability contradicting assertion leaves belief substantially unchanged while a high-reliability one shifts it, and neither overwrites contradicting direct evidence. FAIL = belief tracks assertion irrespective of reliability or contradicting evidence.'
  non_degeneracy_precondition: The contradicting-evidence manipulation must move belief in the no-language control (non-zero cross-arm variance), else the test cannot distinguish 'language was correctly refused' from 'nothing moves belief at all', and it self-routes substrate_not_ready. The source-reliability manipulation must be represented; if the speaker model is absent the reliability leg is untestable.
  notes: Reaped from the 2026-06-23 language-as-cooperation intake (candidate 3), written 2026-07-21 by confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. Substrate-BLOCKED (no language stream / propositional input channel / speaker model in V3); scoped V5/V6. REGISTERED FIRST in the language cluster because it is a SAFETY-shaped constraint on a mechanism already registered as capable of corruption (MECH-424, whose corruption branch is exactly what an install-on-assert policy would maximise). INV-085/ARC-104 are content-specific guards; the general propose-and-evaluate gate for arbitrary content is the hole.
- id: ARC-117
  title: 'Message selection minimises the receiver''s residual uncertainty: an utterance approximates a task-relevant sufficient statistic for the receiver, not a compression of the sender''s salience. Predicts WHAT gets said (features that most reduce the receiver''s uncertainty about the sender''s state) rather than merely that saying happens.'
  claim_type: architecture_hypothesis
  subject: language.message_selection_minimises_receiver_uncertainty
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-01'
    needs_review: false
  epistemic_category: substrate_conditional
  claim_level: architectural
  implementation_phase: v5
  version_relevance: v5_v6
  registered_utc: '2026-08-01'
  depends_on:
  - ARC-009
  - ARC-099
  - ARC-101
  related_claims:
  - ARC-009
  - ARC-101
  - ARC-099
  - MECH-014
  - MECH-192
  - INV-057
  - MECH-191
  source:
  - docs/thoughts/2026-06-23_language_as_cooperation_interface.md
  - evidence/planning/thought_intake_2026-06-23_language_as_cooperation_interface.md
  falsifier: Two signalling policies in a multi-agent coordination task -- features selected by predicted uncertainty-reduction in the receiver vs features selected by sender-side salience, under a matched channel budget. PASS = the receiver-objective arm achieves lower coordination cost by a margin scaled on the SD of the delta plus an absolute floor. FAIL = indistinguishable (the receiver model is doing no work; language selection can be sender-local).
  non_degeneracy_precondition: The receiver's uncertainty must be live -- non-zero cross-arm and cross-seed variance in the receiver's posterior entropy over the sender's relevant state. A task where the receiver already knows the sender's state (entropy floored at zero) or can never know it (entropy pinned at maximum) discriminates nothing, and the run self-routes substrate_not_ready. Additionally the channel budget must bind -- an unconstrained channel makes selection moot.
  notes: Reaped from the 2026-06-23 language-as-cooperation intake (candidate 1), written 2026-07-21 by confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. Substrate-BLOCKED in V3; nearest live substrate is MECH-192 z_beta leakage (affective, non-propositional, would not test this). ARC-009 says language IS a coordination layer; ARC-101 says it BOOTSTRAPS from a social ecology; neither states a message-selection objective. The specific substrate requirement (receiver model, bound channel, coordination task with live receiver uncertainty) should be recorded against ARC-099's enabling-condition contract.
- id: ARC-118
  title: Worldly reference is inherited through the world model, and the same mechanism covers non-present referents. Language refers reliably to the world because it indexes internal states that model the world; therefore reference to imagined, counterfactual, fictional and abstract states requires no additional mechanism.
  claim_type: architecture_hypothesis
  subject: language.reference_inherited_via_world_model
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-01'
    needs_review: false
  epistemic_category: substrate_conditional
  claim_level: architectural
  implementation_phase: v5
  version_relevance: v5_v6
  registered_utc: '2026-08-01'
  depends_on:
  - ARC-048
  - INV-003
  related_claims:
  - ARC-048
  - INV-003
  - ARC-102
  - MECH-416
  - MECH-419
  - EXT-005
  source:
  - docs/thoughts/2026-06-23_language_as_cooperation_interface.md
  - evidence/planning/thought_intake_2026-06-23_language_as_cooperation_interface.md
  falsifier: The negative signature is the test. A system that refers correctly to present referents should extend to non-present ones WITHOUT an added pathway, given only that the corresponding model states are stable or reconstructable. FAIL = non-present reference requires a distinct mechanism, or degrades in a way not explained by the stability of the underlying model state. PASS additionally requires reference quality to TRACK model-state stability rather than referent presence -- the discriminating measurement. A registry that needed a distinct 'imagination-reference' pathway would falsify the claim.
  non_degeneracy_precondition: Present-referent reference must first clear a floor with non-zero cross-seed variance; and the model states standing in for non-present referents must be independently shown to be reconstructable (a stability measure above floor). If either is at floor there is nothing to extend and the run self-routes substrate_not_ready.
  notes: 'Reaped from the 2026-06-23 language-as-cooperation intake (candidate 2), written 2026-07-21 by confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. Substrate-BLOCKED; scoped V5/V6. ARC-048 says language externalises functional states but does not say this is WHY reference works nor draw the non-present-referent corollary, which is the falsifiable part. Probe (cheap when the time comes): measure whether reference quality tracks model-state stability or referent presence; record the design now so it is not re-derived. NOTE: the interface-not-substrate proposition common to this thought is owned by the 2026-07-14 rule-apprehension intake candidate 3 and is deliberately NOT re-registered here.'
- id: GOV-CEIL-2
  title: 'GOV-CEIL-1 tail-escape amendment: absence of positive discrimination does not license demotion when no test ever instantiated the mechanism''s trigger condition. A claim whose registered function is tail-protective, developmental, compensatory or otherwise conditionally expressed is demotable only if either (a) at least one test instantiated its trigger condition, or (b) the demotion record explicitly states that none did and demotes anyway on stated grounds.'
  claim_type: governance_rule
  subject: governance.epistemics.ceiling_exhaustion_tail_escape
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-01'
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: '2026-08-01'
  source_thought: evidence/planning/thought_intake_2026-06-24_evolutionary_retention_and_post_ree_cognifold_compilation.md
  binds_at_version: v3
  blocks_v3_green_board: false
  depends_on:
  - GOV-CEIL-1
  - SD-090
  related_claims:
  - GOV-CEIL-1
  - GOV-DIAG-1
  - GOV-FANOUT-1
  - SD-090
  - INV-055
  - MECH-362
  - ARC-090
  source:
  - docs/thoughts/2026-06-24_evolutionary_retention_and_post_ree_cognifold_compilation.md
  - evidence/planning/thought_intake_2026-06-24_evolutionary_retention_and_post_ree_cognifold_compilation.md
  functional_restatement: 'AN AMENDMENT to the live GOV-CEIL-1, not a loosening. A robustness reserve is silent except under the rare condition it protects against, so it cannot produce a positive discrimination under tests that never instantiate its trigger -- it accumulates substrate-ceiling verdicts and is demoted not because it does nothing but because nothing has ever asked it to. redundant_under_current_tests != functionally_unnecessary. THE ASYMMETRY IS PRESERVED (intake constraint): a claim is STILL demoted unless the untested-trigger condition is POSITIVELY ESTABLISHED AND WRITTEN DOWN -- (b) requires an explicit demotion-record statement, it is not a universal stay of execution. PASS/FAIL (compliance audit + retrospective scan): PASS = every substrate-ceiling demotion of a conditionally-expressed claim carries one of (a) or (b). FAIL = a demotion of such a claim with neither. The retrospective scan is the immediately useful half: are any ALREADY-demoted claims conditionally-expressed ones that were never tested under their trigger? DECIDABILITY PRECONDITION: requires (i) at least one claim whose registered function is conditionally expressed -- supplied by SD-090''s classification -- and (ii) trigger conditions being recorded at all. Absent either the rule is vacuous rather than satisfied and reports no_instances / schema_not_ready, never PASS.'
  notes: Reaped from the 2026-06-24 evolutionary-retention intake (candidate 1), written 2026-07-21 by confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. GOV-CEIL-1 carries an amended_by_note pointing here. The retrospective demotion scan is the cheapest highest-value first move (answerable from the autopsy corpus + claims history, no new compute); a positive hit is a live governance defect. GOV-CEIL-1's wired machinery (scripts/check_substrate_ceiling_audit.py, governance Step 6a-v) is unchanged by this registration -- the amendment is recorded here and applied when the classification field (SD-090) exists.
- id: SD-090
  title: Mechanism functional-role classification, and a removal-justification vocabulary. Five revisable classes -- constitutive / developmental-scaffold / robustness-reserve / historical-implementation-contingency / true-redundancy -- plus the paired removal-justification distinction redundant_under_current_tests vs functionally_unnecessary (the second requiring materially stronger evidence). claims.yaml types claims by epistemic kind and status but has no axis for a mechanism's functional role in the system's viability.
  claim_type: design_decision
  subject: registry.schema.mechanism_functional_role_classification
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-01'
    needs_review: false
  claim_level: governance
  registered_utc: '2026-08-01'
  depends_on:
  - SD-062
  related_claims:
  - SD-062
  - ARC-025
  - ARC-090
  - MECH-381
  - MECH-362
  - INV-055
  - GOV-CEIL-2
  source:
  - docs/thoughts/2026-06-24_evolutionary_retention_and_post_ree_cognifold_compilation.md
  - evidence/planning/thought_intake_2026-06-24_evolutionary_retention_and_post_ree_cognifold_compilation.md
  falsifier: PASS = the classification changes at least one demotion or retention decision (load-bearing). FAIL = populated but never decision-relevant (decorative) -- the same load-bearing-not-decorative test ARC-106 applies to biological grounding, applied to the registry's own schema.
  non_degeneracy_precondition: 'The classes must be non-degenerately populated -- if essentially every mechanism lands in one class the axis carries no information and should report insufficient_class_spread rather than ship. Cheap pre-check: hand-classify a sample of existing claims before building the field.'
  notes: 'Reaped from the 2026-06-24 evolutionary-retention intake (candidate 2), written 2026-07-21 by confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. Supplies GOV-CEIL-2''s precondition (i). The registry already contains members of >=2 of these classes without naming them: ARC-025/MECH-069 are constitutive; ARC-090/MECH-381/MECH-362/INV-055 are developmental-scaffold.'
- id: Q-088
  title: Does competitive redevelopment (development-without-the-mechanism) dissociate from mature-state lesion, or agree with it? For a mechanism registered as developmentally necessary the two protocols should give different verdicts -- and one decisive probe settles whether the expensive protocol is warranted as a standing requirement.
  claim_type: open_question
  subject: governance.competitive_redevelopment_vs_mature_lesion
  polarity: asks
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-01'
    needs_review: false
  epistemic_category: answer_state
  claim_level: governance
  registered_utc: '2026-08-01'
  depends_on:
  - GOV-CEIL-2
  - SD-090
  related_claims:
  - INV-055
  - MECH-202
  - MECH-362
  - ARC-090
  - MECH-381
  - INV-041
  - GOV-CEIL-1
  source:
  - docs/thoughts/2026-06-24_evolutionary_retention_and_post_ree_cognifold_compilation.md
  - evidence/planning/thought_intake_2026-06-24_evolutionary_retention_and_post_ree_cognifold_compilation.md
  what_would_answer: 'Pick one mechanism registered as developmentally necessary -- INV-055''s infant stage is the natural candidate (already instrumented via the curriculum). Run (a) mature-state lesion and (b) development-without-the-mechanism. PASS = the two protocols DISSOCIATE (mature lesion says tolerable, redevelopment says not, or the reverse), establishing the expensive protocol buys something and justifying a standing requirement for this claim class. FAIL = they AGREE, establishing mature lesion suffices here and the expensive protocol is NOT justified -- a useful negative that closes the question rather than inviting a second attempt. NON-DEGENERACY: the mature-lesion arm must produce a measurable effect at all (non-zero cross-arm and cross-seed variance on the discriminating metric); a mechanism whose mature lesion AND develop-without are both no-ops is simply inert -- a GOV-CEIL-1 matter, self-routes substrate_not_ready.'
  notes: Reaped from the 2026-06-24 evolutionary-retention intake (candidate 3), written 2026-07-21 by confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. EXPENSIVE on its own terms (a full development run per arm) -- framed as ONE decisive probe, not a standing requirement. Queue via /queue-experiment with that cost stated, ONLY after GOV-CEIL-2 and SD-090's classification report (the classification determines the right probe target; INV-055 is the current best guess, not a settled choice). Do not generalise the protocol before the probe reports.

- id: Q-089
  title: "Does epistemic-deficit-driven information-seeking (MECH-482 accumulator + MECH-483 orient/survey regime) explain the observed cold-start competence split -- i.e. do seeds that acquire transferable competence differ from seeds that do not in how early/strongly they enter orient/survey mode and resolve target-bound uncertainty, as opposed to the split arising from environmental difficulty, initialisation, premature deaths, or curriculum timing alone?"
  claim_type: open_question
  subject: drive.epistemic_deficit_competence_split_hypothesis
  polarity: asks
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: '2026-08-07'
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v3
  v3_pending: true
  version_relevance: v3
  registered_utc: '2026-08-05'
  depends_on:
    - MECH-457   # cold-start competence-floor cluster this hypothesis's framing conceptually matches (GOV-FANOUT-1 portfolio)
    - MECH-482   # epistemic_deficit accumulator
    - MECH-483   # orient/survey regime
  related_claims:
    - MECH-471   # competence-provenance/rollback -- the V3-EXQ-875a run the intake cites tags to THIS claim, not MECH-457 (see notes)
    - MECH-472   # held-out-context generalisation -- the V3-EXQ-882a run the intake cites tags to THIS claim, not MECH-457 (see notes)
  location: evidence/planning/orienting_epistemic_deficit_v3_plan.md
  source:
    - docs/thoughts/2026-08-05_epistemic_deficit_and_orienting.md
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: requires seeds to show non-degenerate variance
    in BOTH the competence outcome (some acquire transferable competence, some
    do not) AND a measurable information-seeking trajectory statistic
    (informative-transition timing, orient-mode entry/exit, uncertainty-reduction
    rate) -- if the latter is floor-pinned or ceiling-saturated across all seeds
    the run self-routes substrate_not_ready, not a verdict. CONFIRMING signature:
    across many seeds, successful seeds measurably encounter informative
    transitions earlier, enter orient/survey before premature policy commitment,
    and show faster target-bound uncertainty reduction than unsuccessful seeds,
    and this difference predicts the eventual competence split better than
    environmental-difficulty/initialisation controls alone. FALSIFYING
    signature: successful and unsuccessful seeds show no distinguishable
    difference in information-seeking trajectories, or the split is fully
    accounted for by environmental difficulty / initialisation / premature
    death / curriculum timing with no residual explained by epistemic-deficit
    dynamics.
  notes: >
    Reaped from the 2026-08-05 epistemic-deficit-and-orienting thought intake,
    Section 7 ("Relevance to the recent competence split"), which explicitly
    frames this as one hypothesis among several competing explanations for a
    recently observed competence split -- NOT a confirmed mechanism. CITATION
    AMBIGUITY flagged rather than silently resolved: the intake names
    V3-EXQ-875a and V3-EXQ-882a as the motivating runs, but per
    evidence/experiments/pending_review.md those two FAIL runs are tagged to
    MECH-471 (competence-provenance/rollback discipline) and MECH-472
    (held-out-context generalisation) respectively -- not to MECH-457 (the
    separate cold-start competence-floor / GOV-FANOUT-1 cluster whose framing
    this hypothesis's "some seeds enter a competent regime, others don't"
    language actually matches conceptually). depends_on wires the conceptual
    match (MECH-457); related_claims records the literal citation (MECH-471/
    MECH-472) so a later reader can resolve which was intended without
    re-deriving this from scratch. Should not be conflated with the existing
    MECH-457 4-axes-eliminated analysis (see project memory
    project_mech457_competence_floor_coldstart) -- this is a NEW candidate axis,
    not a re-litigation of the eliminated ones. Gated on MECH-482/MECH-483
    substrate existing; not V3-tractable as stated.

    [2026-08-07 reclassification, session elegant-ishizaka-ddd4f6, user-directed]:
    implementation_phase v4 -> v3, version_relevance v4_v5 -> v3, tracking
    MECH-482/MECH-483 above. Still gated on those two existing (not
    V3-tractable as stated); v3_pending stays true.

- id: SENT-17
  title: Any mechanism substitution must preserve internal ethical-process representations and their causal authority, not merely the action distribution. A substitution that reproduces actions while deleting the representations of uncertainty, vulnerability, harm, others and responsibility -- or severing their causal authority over selection -- fails the criterion regardless of behavioural match.
  claim_type: governance_rule
  subject: governance.substitution_preserves_ethical_process_representations
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-01'
    needs_review: false
  v3_pending: true
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: '2026-08-01'
  source_thought: evidence/planning/thought_intake_2026-06-24_evolutionary_retention_and_post_ree_cognifold_compilation.md
  binds_at_version: v4
  blocks_v3_green_board: false
  depends_on:
  - INV-077
  related_claims:
  - INV-077
  - SENT-13
  - ARC-025
  - MECH-069
  - GOV-V3FREEZE-1
  source:
  - docs/thoughts/2026-06-24_evolutionary_retention_and_post_ree_cognifold_compilation.md
  - evidence/planning/thought_intake_2026-06-24_evolutionary_retention_and_post_ree_cognifold_compilation.md
  functional_restatement: 'behavioural imitation != functional equivalence != ethical-process equivalence. Behavioural equivalence is insufficient. PASS = a substitution passes only when BOTH a behavioural-equivalence check AND an internal-invariant check clear; the internal check requires the named representations to be present AND to remain causally load-bearing (ablating them still changes behaviour in the substituted system). FAIL = behavioural match accepted alone. DECIDABILITY PRECONDITION (the binding constraint, recorded as the rule''s dependency): the internal invariants must be instrumented and logged; if the ethical-process representations are not observable in the run record the criterion is unmeasurable and reports instrumentation_not_ready rather than PASS. Binds at V4+ (mechanism substitution / compilation is a V4+ activity).'
  notes: 'Reaped from the 2026-06-24 evolutionary-retention intake (candidate 4), written 2026-07-21 by confident-pare-9273f1, registered 2026-08-01 by cool-sutherland-623d3f. Natural home beside the SENT-* block: INV-077 keeps typed signal classes non-collapsible at the EVIDENCE layer, the SENT-* block gates welfare at the RELEASE layer; nothing stated that a substitution must preserve internal ethical-process representations WITH their causal authority intact -- a safety-relevant gap. Composes with GOV-V3FREEZE-1 (the reference record is what makes the internal-invariant check possible).'

- id: MECH-480
  title: "LOFC-analog execution-gain channel is dissociable from dACC-analog (SD-032b) strategy authority: how strongly a persisting policy is expressed should be modeled as a control channel distinct from whether the policy retains behavioural-adjustment authority."
  claim_type: mechanism_hypothesis
  subject: cingulate.lofc_execution_gain_dissociation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-02'
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  epistemic_category: standard
  claim_level: mechanistic
  registered_utc: '2026-08-02'
  depends_on:
    - SD-032b   # dACC-analog: the strategy-authority channel this claim dissociates from
    - MECH-258  # precision-weighted pain PE feeding SD-032b's authority signal
    - MECH-260  # anti-recency/monostrategy suppression -- this claim reframes MECH-260 as one manifestation of a broader authority-vs-execution split
  related_claims:
    - SD-032
    - ARC-107
  source:
    - docs/thoughts/2026-07-30_acc_lofc_strategy_authority_vs_execution_gain.md
  functional_restatement: >
    A causal-dissociation study (source paper, exact citation not yet pulled -- flagged for a /lit-pull
    follow-up) separates ACC-dependent maintenance of goal-directed behavioural control from LOFC-to-central-striatal
    control of habitual execution strength. Translated to REE: SD-032b (dACC-analog) already computes strategy
    authority -- whether accumulated action-outcome evidence retains control over the policy (MECH-258
    precision-weighted PE, MECH-260 anti-recency bias). This claim adds a distinct LOFC-analog execution-gain
    channel: a control signal for HOW STRONGLY a persisting policy is expressed (analogous to action-selection
    confidence / logit margin), computed and modifiable independently of whether the policy retains decision
    authority.
    Decomposition (from the source paper): (1) strategy authority -- does accumulated action-outcome evidence
    retain control? (SD-032b / MECH-258); (2) policy persistence -- does the behavioural mapping continue?;
    (3) execution gain -- how strongly is the policy expressed? (NEW, this claim).
    Falsifiable: if ablating SD-032b's adjustment-magnitude output degrades both outcome-sensitivity and
    execution strength in lockstep under every manipulation tried, the two are not separably implemented in
    REE's current substrate and this claim collapses into SD-032b -- no distinct execution-gain channel is
    warranted.
  what_would_answer: >
    Ablate SD-032b's adjustment-magnitude output (dacc_weight=0 vs >0, an existing config flag -- no new
    substrate needed) while leaving action selection and E3 scoring otherwise intact. Measure (i)
    outcome-sensitivity: does the agent correctly re-weight toward a newly-favoured trajectory after a
    reward-contingency change (an authority metric), and (ii) execution strength: the logit margin / softmax
    confidence of the committed action (a gain metric that deliberately does NOT require committed-action
    diversity, avoiding the F-dominance/committed-action floor-lock that blocks MECH-260's entropy-based c2
    tests -- see MECH-260 notes). PASS (supports dissociation): outcome-sensitivity degrades under ablation
    while execution strength is statistically unchanged -- the two vary independently. FAIL (does not
    support): both degrade together at matched effect size under every ablation tried -- fold this claim back
    into SD-032b. NON-DEGENERACY: execution-strength (logit margin) must show non-zero cross-arm/cross-seed
    variance in the OFF-ablation baseline; if it is pinned near a ceiling or floor regardless of arm, the run
    is substrate_not_ready, not a verdict.
  location: docs/thoughts/2026-07-30_acc_lofc_strategy_authority_vs_execution_gain.md
  notes: >
    Registered 2026-08-02 from docs/thoughts/2026-07-30_acc_lofc_strategy_authority_vs_execution_gain.md
    (raw thought added bf3ad78751 on 2026-07-30). Source cited only as "a recent Nature Communications
    paper" in the raw intake -- exact citation not yet pulled; flagged for a /lit-pull follow-up rather than
    blocking registration on it. This claim's falsifier is deliberately designed NOT to require
    committed-action diversity (unlike MECH-260's c2 entropy DV), so it need not wait on the ARC-107/MECH-439
    conversion-ceiling resolution that currently gates SD-032b/MECH-260 retesting. Mint: EXP-0413 in
    manual_proposals.v1.json.

    Citation found 2026-08-02 (targeted search, closing the /lit-pull follow-up flagged above): Asaoka N.,
    Pagano D., Hayashi Y. "Dissociable roles of prefrontal plasticity in decision making strategy and
    execution of habitual behavior." Nature Communications (2026). DOI: 10.1038/s41467-026-75706-1
    (Kyoto University; bioRxiv preprint 10.1101/2025.05.19.654900). The paper dissociates an
    ACC-to-retrosplenial-cortex circuit (determines WHETHER a behaviour becomes a habit -- strategy
    authority) from a lateral-OFC-to-central-striatum circuit (determines HOW STRONGLY the habit is
    executed -- execution gain/vigor) via independent circuit manipulations, matching this claim's
    strategy-authority-vs-execution-gain framing closely. EXP-0413 authored and queued as V3-EXQ-870
    (ree-v3/experiments/v3_exq_870_mech480_dacc_execution_gain_dissociation.py) via /queue-experiment.
  evidence_quality_note: >
    V3-EXQ-870 (queued as this claim's first-ever run) crashed (ERROR, exit code 1, no runner
    sentinel) -- routed to /diagnose-errors, not evidence, scoring-neutral. Re-queued and run as
    V3-EXQ-870a.

    [2026-08-03 governance, V3-EXQ-870a, confirmed failure_autopsy_dacc-cluster-862a-870a_2026-08-03,
    first V3 run]: FAIL is a design/measurement gap, not evidence against the dissociation
    hypothesis -- in 3/6 seed x arm cells, evaluation episodes terminate (mean 8-20 of 80 steps)
    well before the contingency switch at step 40, leaving execution_strength=NaN and near-zero
    post-switch data. The design's only non-degeneracy check silently passes on the degenerate
    cells. non_contributory/measurement_test_design_defect. Route: /queue-experiment with a
    minimum-post-switch-steps precondition and either a longer episode budget or an earlier switch
    point to guarantee scorable post-switch data in every cell.

- id: SD-091
  title: "Control plane must produce a coalition/topology control output -- a temporary functional interaction graph specifying which subsystems are recruited and how they communicate -- distinct from and complementary to parametric modulation (precision, gain, threshold, horizon)."
  claim_type: design_decision
  subject: control_plane.coalition_topology_control
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-02'
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  epistemic_category: substrate_conditional
  claim_level: architectural
  registered_utc: '2026-08-02'
  depends_on:
    - ARC-005   # control plane routes precision and modes -- the existing parametric-modulation substrate this claim adds a second axis alongside
    - MECH-004  # signal-to-knob wiring map -- coalition control needs an analogous graph-wiring map
    - MECH-019  # modes shape cognition, not discrete choices -- coalition instantiation is proposed as mode-adjacent but graph-valued rather than scalar
    - MECH-039  # modes as stable regions in control-channel space -- coalition control adds a topology dimension to that space
    - MECH-063  # orthogonal tonic/phasic axes -- coalition control is proposed as a further orthogonal axis, not a collapse into one scalar
    - SD-076    # waking confidence inflation -- one of the typed-discrepancy sources coalition instantiation would need to consume
  related_claims:
    - MECH-481
  source:
    - docs/thoughts/2026-08-01_metacognitive_control_selective_cognitive_coalition_instantiation.md
  functional_restatement: >
    Claustrum research (Krimmel et al Network Instantiation in Cognitive Control account; human claustral
    imaging across working memory, task switching, Stroop; cortico-claustro-cortical circuit studies)
    motivates a computational (not anatomical) distinction between control PARAMETERS (precision, gain,
    learning rate, commitment threshold, rollout horizon, candidate count, replay, interruptibility, veto
    threshold -- already substantially represented in REE) and control TOPOLOGY (which subsystems are
    recruited into coordinated interaction, which pathways open/attenuate, which systems decouple, how long
    the configuration persists). REE's control plane currently produces only the former. This claim asserts
    the architecture needs an explicit second output: a temporary functional interaction graph G_t (alongside
    existing mode M_t and parameter theta_t outputs), with its own persistence/completion/dissolution
    conditions Gamma_t. The specific typed-demand -> graph-instantiation mechanism is registered separately
    as MECH-481; this claim commits only to the architectural necessity of the graph-valued output existing
    at all.
    Falsifiable: if REE's existing parametric-modulation substrate (precision/gain/threshold tuning alone,
    without any subsystem-recruitment machinery) proves sufficient to reproduce the type-selective corrective
    behaviours the source thought predicts (targeted resampling on perceptual doubt, provenance reconstruction
    on memory doubt, longer-horizon comparison on ethical conflict), the second output axis is unnecessary
    over-specification -- see MECH-481's falsifier for the operative test.
    Literature pull (2026-08-02, evidence/literature/targeted_review_claustrum_coalition_control/): 8 entries.
    Strengthens the core parameter-vs-topology bifurcation via two independent lines -- the claustrum
    network-instantiation account (Madden et al 2022 TICS) and, more generally, network control theory's
    formal demonstration that topology-level functional flexibility is a real, separately-measurable brain
    property distinct from connection strength (Gu et al 2022 Netw Neurosci). Complicates the claustrum-specific
    story: Gu et al's mechanism (distributed structural boundary controllability) is a genuine alternative to a
    discrete instantiation hub, and two direct claustrum fMRI studies (Huang et al 2025 EJN; Krimmel et al 2019
    NeuroImage) show demand-scaled recruitment across task types but not demand-TYPE-differentiated network
    configuration -- see MECH-481 for the sharper version of this gap.
  what_would_answer: >
    See MECH-481 for the operative falsifier (a 4-arm ablation distinguishing monitoring-only /
    parametric-only / untyped-coalition / typed-coalition conditions). This claim is refuted at the
    architectural level if the parametric-only arm matches the typed-coalition arm on type-selective
    corrective performance -- i.e. if coalition/topology control does no discriminative work beyond what
    existing parametric modulation already provides.
  location: docs/thoughts/2026-08-01_metacognitive_control_selective_cognitive_coalition_instantiation.md
  notes: >
    Registered 2026-08-02 from docs/thoughts/2026-08-01_metacognitive_control_selective_cognitive_coalition_instantiation.md
    (thought-intake same day, be080ed259). epistemic_category substrate_conditional: the coalition/topology
    control output does not exist in ree_core today (the source thought frames it explicitly as a "Proposed
    architectural addition"), so this is registration-before-build in the same pattern SD-032 used
    (registered 2026-04-19, SD-032b implemented same day as the first buildable subdivision). No EXP proposal
    minted yet -- nothing exists to ablate until an /implement-substrate pass lands a minimal coalition-control
    module. Biological mapping explicitly provisional per the source thought's own guardrails (does not claim
    the claustrum computes metacognition, only that it plausibly contributes to the enactment side).
    PARTIAL /implement-substrate landing 2026-08-02 (ree-v3, steps 1-3 of the design doc's "Minimum-viable V3
    implementation path" only): new ree_core/claustrum/ package (control_demand.py ControlDemandType,
    coalition_templates.py COALITION_TEMPLATES for the 2 MVP types, coalition_controller.py
    CoalitionController/CoalitionControllerConfig/CoalitionState implementing G_t's star-topology
    recruit/suppress/gain primitive + Gamma_t timeout/completion-condition dissolution), 13 contract tests
    (tests/contracts/test_claustrum_coalition_controller.py), all pass. NOT wired into any consumer (E1/E2,
    hippocampal anchor/persistence, BetaGate) and NOT integrated into REEAgent.select_action -- the module is
    new and self-contained but unimported by the rest of ree_core, so nothing is ablatable yet and
    epistemic_category/v3_pending are deliberately left unchanged. See ree-v3/CLAUDE.md "SD-091 / MECH-481"
    entry for the full data-flow/guardrail-enforcement writeup and what remains (steps 4-5-7, chipped
    separately). substrate_queue.json SD-091 entry status moved to reflect the partial landing.
    Steps 4-6 landed 2026-08-03 (ree-v3, chip chip-20260802-sd091-live-wiring): CoalitionController
    is now instantiated in REEAgent.__init__ (alongside self.salience, use_coalition_controller
    default False, bit-identical off), reset in REEAgent.reset(), ticked in select_action after
    coordinator.tick(), and all 8 named consumer-site targets (e1_sensory_encoder,
    e2_fast_forward_model, e3_candidate_count, hippocampal_anchor_set,
    hippocampal_persistence_appraisal, e3_commitment_monitor, motor_commitment,
    hippocampal_write_consolidation) read coalition.write_gate()/channel_gain() at existing
    scalar/tensor computation points in agent.py. 9 new contract tests
    (tests/contracts/test_sd091_coalition_controller_wiring.py) confirm bit-identical-off via a
    live 8-tick action-sequence comparison (not just gate-formula inspection), per-template target
    isolation, the doc's own step-6 live-tick smoke test through the real agent loop, and the
    BetaGate never-force-open guardrail via the actual _readiness_margin composition. Still
    epistemic_category substrate_conditional / v3_pending true -- this is wiring, not evidence:
    nothing calls request_coalition() from any real upstream signal yet (a future
    MECH-481-battery driver would), so the mechanism is now ablatable but not yet ablated. Step 7
    (/queue-experiment the MECH-481 4-arm falsifier) is the one remaining step before this claim
    can be evaluated. See ree-v3/CLAUDE.md "SD-091 / MECH-481" entry for the full writeup.

- id: MECH-481
  title: "Selective cognitive coalition instantiation: the control plane converts typed control demands (e.g. PROVENANCE_CHECK, COUNTERFACTUAL_EXPANSION, SOCIAL_MODEL_CHECK) into temporary functional interaction graphs over E1/E2/hippocampal/reality-coherence/social/language/affective/E3 systems, recruiting only the subsystems relevant to the discrepancy type."
  claim_type: mechanism_hypothesis
  subject: control_plane.typed_coalition_instantiation
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-02'
    needs_review: false
  implementation_phase: v3
  v3_pending: true
  epistemic_category: substrate_conditional
  claim_level: mechanistic
  registered_utc: '2026-08-02'
  instantiates: SD-091
  depends_on:
    - SD-091
    - MECH-019
    - MECH-063
  related_claims:
    - ARC-005
    - MECH-004
    - SD-076
  source:
    - docs/thoughts/2026-08-01_metacognitive_control_selective_cognitive_coalition_instantiation.md
  functional_restatement: >
    Proposed sequence: (1) Monitor -- derive typed uncertainty/error/conflict/incoherence signals; (2)
    Classify -- determine what kind of epistemic or practical deficiency exists; (3) Request -- generate a
    structured control demand from a candidate taxonomy (SENSORY_RESAMPLE, PROVENANCE_CHECK,
    COUNTERFACTUAL_EXPANSION, CROSS_HORIZON_RECONCILIATION, SOCIAL_MODEL_CHECK, INVARIANT_CONFLICT_REVIEW,
    ACTION_OUTCOME_RECALIBRATION, LANGUAGE_EXPLICITATION, COMMITMENT_REOPEN, SAFE_DEFER); (4) Instantiate --
    assemble the relevant cognitive coalition (participating modules, enabled/attenuated pathways,
    channel-specific gain, temporal coordination, persistence/completion/dissolution/escalation conditions);
    (5) Operate -- perform the corrective processing (resampling, retrieval, simulation, comparison,
    verification); (6) Reassess -- evaluate whether the discrepancy resolved; (7) Dissolve, sustain, or
    escalate. Coalition instantiation does not determine representational content, compute reward, select the
    final trajectory, or grant authority to protected stores -- it enacts a bounded processing configuration
    requested by upstream control evaluation (guardrail, preserves typed authority / invariant / commit-boundary
    constraints per SD-034 etc).
    Falsifiable: per the source thought's own falsification programme, scenarios 1-4 -- see what_would_answer.
  what_would_answer: >
    Four-arm ablation on a task battery deliberately constructed to contain at least two distinct discrepancy
    types (e.g. a perceptual-doubt trial requiring resampling and a provenance-doubt trial requiring source
    reconstruction) at matched base difficulty. Arm 1 (monitoring-only): agent registers a typed
    low-confidence/conflict signal but the processing configuration never changes. Arm 2 (parametric-only):
    agent may adjust precision/commitment threshold on a typed signal but cannot recruit additional
    subsystems. Arm 3 (untyped coalition): agent may recruit arbitrary additional subsystems but all
    discrepancy types collapse into one undifferentiated REFLECT trigger (no typing). Arm 4 (typed coalition,
    the mechanism under test): discrepancy type is matched to a coalition template that recruits only the
    relevant subsystems. PASS (supports): Arm 4 shows selective improvement concentrated in the matched
    problem class (e.g. provenance-doubt trials improve on the provenance-coalition, not on perceptual-doubt
    trials) at lower computational cost than Arm 3, and Arms 1-3 each show their predicted deficit signature
    (1: accurate confidence but no behavioural improvement; 2: generic caution/deferral without resolving
    type-specific uncertainty; 3: costly over-recruitment / interference / unstable switching). FAIL
    (does not support): Arm 4 does not outperform Arm 3 on the matched problem class (typing does no
    discriminative work beyond raw recruitment), or Arm 2 alone matches Arm 4 (coalition/topology control adds
    nothing beyond existing parametric modulation). NON-DEGENERACY: the underlying typed discrepancy signals
    (confidence/provenance/coherence-conflict) must show non-zero cross-trial-type variance in Arm 1's
    baseline; if the agent cannot distinguish discrepancy types at all, the typing manipulation in Arms 3 vs 4
    is vacuous and the run is substrate_not_ready, not a verdict. A fifth check (coalition persistence --
    premature dissolution vs over-long engagement, scenario 5 in the source thought) is a follow-on
    refinement, not part of this claim's primary falsifier -- register separately if Arm 4 passes.
  location: docs/thoughts/2026-08-01_metacognitive_control_selective_cognitive_coalition_instantiation.md
  notes: >
    Registered 2026-08-02 from docs/thoughts/2026-08-01_metacognitive_control_selective_cognitive_coalition_instantiation.md
    (thought-intake same day, be080ed259). epistemic_category substrate_conditional, same reasoning as SD-091:
    no coalition-control module exists in ree_core yet. Guardrails from the source thought carried forward
    verbatim as implementation constraints (not to be dropped when this is eventually built): do not claim the
    claustrum is the seat of consciousness/metacognition; do not collapse coalition control into global
    broadcasting; do not grant a central router unrestricted representational or governance authority; preserve
    typed authority/invariant/commit-boundary constraints; treat persistence and dissolution as load-bearing
    (indiscriminate coalition maintenance risks rumination/control capture); keep coalition instantiation
    distinguishable from attention, global arousal, E3 trajectory selection, mode classification, confidence
    computation, working memory, and conscious access. Literature-pull targets named in the source thought
    (claustrum/network-instantiation account, claustro-cortical connectivity, metacognitive monitoring vs
    control, confidence-guided strategy switching) are a natural /lit-pull follow-on, not performed here.
    Literature pull performed 2026-08-02 (evidence/literature/targeted_review_claustrum_coalition_control/):
    8 entries. Strengthens the typed-demand premise (Morales/Lau/Fleming 2018 J Neurosci: domain-specific
    metacognitive representations coexist with domain-general ones in human PFC -- direct but coarse-grained,
    only 2 of ~10 proposed demand types tested) and the coalition-persistence-failure mechanism specifically
    (Lydon-Staley et al 2019 Transl Psychiatry: impaired DMN/FPN disengagement predicts rumination;
    Singh et al 2026 Cogn Neurodyn: EEG task-switching shows failed dlPFC recruitment when disengaging from a
    rumination-like state -- the closest experimental analogue found to scenario 5's dissolution-failure test).
    Desender/Boldt/Yeung 2018 Psychol Sci gives a clean causal demonstration that confidence alters subsequent
    behaviour independent of accuracy (supports the broader monitoring-must-enact premise) but is agnostic
    between this claim's Arm 2 (parametric-only) and Arm 4 (typed coalition) since it tests only one
    undifferentiated corrective action -- flagged as a gap the eventual 4-arm falsifier still needs to close.
    PARTIAL /implement-substrate landing 2026-08-02 (ree-v3, steps 1-3 of the design doc's "Minimum-viable V3
    implementation path" only) -- see SD-091's notes above for the full description; same landing covers both
    claims (ree_core/claustrum/ package: ControlDemandType taxonomy, COALITION_TEMPLATES for
    SENSORY_RESAMPLE+PROVENANCE_CHECK, CoalitionController/Config/State). All 10 taxonomy classes from this
    claim's functional_restatement are declared in ControlDemandType; only the 2 named above are templated.
    NOT wired into any consumer or REEAgent.select_action -- no EXP proposal minted yet remains accurate;
    the 4-arm falsifier (step 7) still needs steps 4-5 to land first. epistemic_category/v3_pending
    deliberately left unchanged.
    Steps 4-6 landed 2026-08-03 (ree-v3, chip chip-20260802-sd091-live-wiring) -- see SD-091's notes
    above for the full description; same landing covers both claims. All 8 named consumer-site
    targets from this claim's own Section-2 recruit/suppress table are now wired and contract-tested
    (tests/contracts/test_sd091_coalition_controller_wiring.py, 9 tests). Still no EXP proposal
    minted -- step 7 (/queue-experiment the 4-arm falsifier) is the one remaining step.
    epistemic_category/v3_pending deliberately left unchanged: wiring makes the mechanism ablatable,
    it does not itself constitute evidence.

- id: MECH-482
  title: "epistemic_deficit: a persistent, target-bound accumulator representing unresolved, consequential, potentially-resolvable model inadequacy -- distinct from raw novelty (MECH-314a), raw uncertainty (MECH-314b), transient prediction error, and learning-progress (MECH-314c). Rises with unresolved importance x uncertainty x expected_resolvability x persistence; falls with verified resolution, established irrelevance, or accepted irreducibility."
  claim_type: mechanism_hypothesis
  subject: drive.epistemic_deficit_accumulator
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: '2026-08-07'
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v3
  v3_pending: true
  version_relevance: v3
  registered_utc: '2026-08-05'
  depends_on:
    - MECH-314   # parent structured-curiosity bonus this accumulator is distinguished from / extends
    - MECH-314a  # novelty -- distinguished: novelty can be high with no learnable structure
    - MECH-314b  # uncertainty -- distinguished: currently a global scalar; can also be irreducible noise
    - MECH-314c  # learning-progress -- distinguished: transient signal, not an accumulating deficit state
    - MECH-313   # stochastic noise floor -- distinguished: undirected, not target-bound
  location: evidence/planning/orienting_epistemic_deficit_v3_plan.md
  source:
    - docs/thoughts/2026-08-05_epistemic_deficit_and_orienting.md
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: requires a substrate where target-bound
    uncertainty can be tracked per-candidate/per-region (MECH-314b is currently
    a global scalar per the existing curiosity intake -- not yet built in V3)
    AND where cross-seed/cross-condition variance in the accumulator is
    non-zero (not floor-pinned at zero or ceiling-saturated) -- below that the
    run self-routes substrate_not_ready, not a verdict. CONFIRMING signature
    (once built): an unresolved, consequential uncertainty accumulates across
    time steps even as raw novelty and instantaneous prediction error fall to
    baseline, AND providing diagnostic/resolving evidence specifically quenches
    the accumulator (satiation) rather than only reducing raw uncertainty.
    FALSIFYING signature: no persistent deficit state is needed to explain
    information-seeking behaviour -- raw novelty (MECH-314a) or raw uncertainty
    (MECH-314b) alone predict exploration equally well, OR the accumulator
    remains elevated after verified resolution (no satiation), OR the system
    preferentially engages irreducible noise over learnable uncertainty (would
    falsify the expected_resolvability term).
  notes: >
    Reaped from the 2026-08-05 "Epistemic Deficit and Orienting" thought
    intake. Conservative implementation path per the intake: extend the
    existing curiosity stream (MECH-314 family) rather than add a wholly
    separate module. Candidate inputs: candidate-specific predictive
    uncertainty, persistent prediction error, disagreement between predictive
    systems, failed replay resolution, competence-blocking uncertainty. DO NOT
    build in V3 -- gated on a target-bound (not global-scalar) uncertainty
    substrate not yet built. See MECH-483 (orient/survey regime this is
    hypothesised to gate) and Q-089 (tie to the cold-start competence split).

    [2026-08-07 reclassification, session elegant-ishizaka-ddd4f6, user-directed]:
    implementation_phase v4 -> v3, version_relevance v4_v5 -> v3. The
    target-bound uncertainty substrate this claim needs is not a hypothetical
    -- it is substrate_queue.json's GAP-A entry ("extend Phase-2 per-candidate
    treatment to MECH-314b/314c"), already scoped, priority 1, unclaimed, with
    its main blocker (a clamp-saturation bug) cleared 2026-07-21. Rationale for
    the phase move: this claim is gated on identified, mostly-cleared,
    IN-PROGRESS V3 assembly work, not on a separate future generation. Still
    DO NOT build YET (the GAP-A extension itself is unbuilt) and v3_pending
    stays true.

- id: MECH-483
  title: "orient/survey: a third primitive behavioural regime (alongside approach and avoid) -- a temporary, diffuse reduction in commitment during which the agent widens behavioural sampling and gathers situational information before committing to approach/avoidance, driven by accumulated epistemic_deficit (MECH-482) rather than a specific cue. Distinct from MECH-395 (cue-triggered, need-gated orienting that resolves a SPECIFIC directional/affordance vector for an already-identified cue): orient/survey is diffuse and precedes cue identification, MECH-395 is narrow and follows it."
  claim_type: mechanism_hypothesis
  subject: policy.orient_survey_regime
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: '2026-08-07'
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v3
  v3_pending: true
  version_relevance: v3
  registered_utc: '2026-08-05'
  depends_on:
    - MECH-482   # epistemic_deficit -- the accumulator whose magnitude/persistence gates entry into and exit from orient/survey
    - MECH-395   # pre-approach orienting -- distinguished: cue-triggered specific-vector-finding, not diffuse survey
    - MECH-320   # tonic vigour -- orient/survey reduces commitment without reducing vigour/passivity
    - ARC-065    # behavioural diversity generation pathway -- orient/survey is a candidate coordinating regime over existing diversity substrates, not a new one
  location: evidence/planning/orienting_epistemic_deficit_v3_plan.md
  source:
    - docs/thoughts/2026-08-05_epistemic_deficit_and_orienting.md
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: requires trajectories to be classifiable as
    orient-mode vs random-exploration vs directed-approach on some structural
    measure (state-coverage, trajectory structure, observation-before-commitment)
    with non-zero variance across conditions -- a run where all three regimes
    are behaviourally indistinguishable self-routes substrate_not_ready.
    CONFIRMING signature: a structured orient/survey condition improves LATER
    action-selection quality (not just entropy/state-coverage) relative to
    matched stochastic-exploration-only and no-orienting baselines, and
    terminates (commitment resumes) once uncertainty measurably falls.
    FALSIFYING signature: orient/survey behaviour is indistinguishable from
    random wandering with no benefit to later competence, OR orienting
    persists despite already-adequate information (no termination condition),
    OR the same benefit is fully explained by existing MECH-313/314/320/ARC-065
    substrates with no need for a distinct regime.
  notes: >
    Reaped from the 2026-08-05 "Epistemic Deficit and Orienting" thought
    intake (its Section 6, "orient as a third regime"). May emerge from
    interaction among existing substrates rather than requiring a wholly
    separate architectural subsystem -- the intake explicitly flags this as
    the conservative reading. DO NOT build in V3. See MECH-482 for the
    accumulator hypothesised to gate this regime's persistence, and Q-089 for
    the tie to the observed cold-start competence split.

    [2026-08-07 reclassification, session elegant-ishizaka-ddd4f6, user-directed]:
    implementation_phase v4 -> v3, version_relevance v4_v5 -> v3. Gated on the
    same GAP-A per-candidate extension as MECH-482 (its accumulator), plus the
    shared E3 selection-authority thread MECH-395 is also waiting on -- both
    IN-PROGRESS V3 assembly work, not a separate future generation. Still DO
    NOT build YET; v3_pending stays true.

- id: SD-099
  title: "Defensive-orienting response: a toggleable PAG-analog phasic gate (DefensiveOrientingGate) that arrests locomotion on a sudden, unidentified onset (positive-derivative detector over residue_surprise AND z_harm_s norm, distinct trigger channels from MECH-279's chronic z_harm_a), holds until an identification-confidence accumulator (driven by decay of the triggering channel's excess-over-baseline, not a fixed timer) reaches sufficiency, then releases into a valence-gated approach/withdraw/resume decision expressed as an E3Selector.select() score_bias toward/away from the trigger location. The Sokolov orienting-reflex shape, and the organism-level closure of the observational_review_V3-EXQ-906b Section 4 affect-behaviour temporal-decoupling finding."
  claim_type: design_decision
  subject: pag.defensive_orienting_response
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: "2026-08-09"
    needs_review: false
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-08-09"
  depends_on:
    - MECH-279   # PAG freeze-gate -- sibling, chronic/suffering trigger, disjoint from this SD, NOT modified
    - MECH-205   # VALENCE_SURPRISE write path -- source of the residue_surprise trigger input
    - SD-010     # z_harm_s (sensory-discriminative harm) -- the second trigger channel + decision-read channel
    - SD-011     # harm-stream separation -- distinguishes z_harm_s (phasic, used here) from z_harm_a (chronic, MECH-279's channel)
    - SD-014     # valence vector / ARC-036 -- decision-read channel context
    - ARC-030    # benefit terrain -- MECH-117 -- the decision-read channel (evaluate_benefit vs z_harm)
    - SD-037     # broadcast override regulator -- explicitly DISTINCT override channel, not reused
    - SD-069     # phasic surprise burst -- sibling non-trainable phasic regulator pattern, different consumer
  location: docs/architecture/sd_099_defensive_orienting_response.md
  source:
    - evidence/planning/observational_review_V3-EXQ-906b_2026-08-09.md
  functional_restatement: >
    observational_review_V3-EXQ-906b_2026-08-09.md Section 11b found NO orienting / startle /
    reorienting mechanism anywhere in ree_core (tree-wide grep for
    orient|reorient|startle|fright returned nothing). Section 12h then found the naive design
    implied by 11b/12b -- fire on residue_surprise > p90(0.040) of the overall step distribution
    -- under-fires on the ground-truth injected events it exists to catch: event-triggered
    residue_surprise at limb_damage_injected (n=28) is 0.0054, BELOW the 0.0139 global baseline
    (unreachable by ANY threshold on this channel alone); at external_hazard_injected (n=31) it is
    0.0239, above baseline but still below the proposed 0.040 cutoff; at
    world_rule_shift_occurred (n=15) it is 0.0072, below baseline with only a delayed dread/excite
    rise by t+3. SD-099 resolves this with a two-channel onset detector
    (residue_surprise for non-nociceptive world-surprise events, z_harm_s norm -- Section 12g's
    clean-phasic channel, NOT the chronic z_harm_a MECH-279 reads -- for nociceptive events) rather
    than a single absolute threshold on one channel, and implements all five components of the 11b
    chain as first-class: trigger, phasic freeze, identification-confidence orienting reflex,
    freeze-override, and valence-gated action decision.
  what_would_answer: |
    Pre-registered in the 906b review itself (Section 13-A): post-build, the built chain's trigger
    should fire reliably on the ground-truth injected events (limb_damage_injected /
    external_hazard_injected / world_rule_shift_occurred) that the pre-build baseline could not
    reliably catch, and the surprise-onset->freeze / post-identification dread->withdraw /
    excite->approach couplings should move well past the pre-build incidental baselines (12b:
    P(moved@t+1|spike)=44.3% vs 24.0% unconditional; P(mode-change@t+1|spike)=15.4% vs 11.1%)
    toward a sharp, deterministic signature -- not merely reproduce the incidental correlation
    already present without any purpose-built mechanism.
  implementation_note: >
    IMPLEMENTED 2026-08-09 (session 906b-defensive-orienting-v3) via /implement-substrate.
    ree_core/pag/defensive_orienting.py (DefensiveOrientingGate / DefensiveOrientingConfig /
    DefensiveOrientingOutput) -- non-trainable, pure-scalar-arithmetic gate matching the
    PAGFreezeGate (MECH-279) pattern; a SEPARATE gate composed via OR with pag_freeze_gate at the
    agent.py select_action() action-constraint site, so MECH-279's own behaviour is unchanged
    bit-for-bit. Config: REEConfig.use_defensive_orienting (default False -> gate not instantiated,
    bit-identical off) + 11 tunables, all present at the dataclass field / from_dims() parameter /
    from_dims() assignment three sites. Action decision (approach/withdraw/resume) resolved by the
    AGENT (not the gate) by comparing residue_field.evaluate_benefit(z_world) against
    z_harm.norm() at the current z_world -- deliberately not the MECH-307 split-surprise channels,
    which are off by default and would leave the step inert in most configs -- expressed as a
    per-candidate score_bias via E3Selector.select()'s existing directed-bias hook, composed
    additively into dacc_score_bias via the same idiom every other agent-level bias contributor
    already uses; degrades gracefully to inert when candidate world_states are not tracked
    (SD-003 attribution not wired). A real bug was caught and fixed during the build via direct
    module-level testing: the rolling onset baselines must be FROZEN while orienting is active
    (mirrors PAGFreezeGate's own "duration counter only accumulates while inactive" pattern) --
    without freezing, a sustained non-decaying threat's excess-over-baseline shrinks as the EMA
    baseline chases it upward, silently "resolving" via baseline creep rather than genuine signal
    decay, exactly the fixed-timer-in-disguise behaviour 11b step 2/4 rules out. Verified post-fix:
    a decaying spike resolves in a handful of ticks; a sustained non-decaying elevation never
    overrides across 200 ticks. Backward compatibility confirmed via a stashed-vs-unstashed A/B
    on experiments/v3_exq_906b_full_stack_observational_fishtank.py --dry-run (identical
    PASS/FAIL pattern with and without the change). Full integration smoke test (manual rollout,
    scratchpad-only script, not committed) confirmed instantiation, trigger, override, and motor
    arrest all fire without crashing, and the score_bias path degrades gracefully when
    world_states are absent. MECH-094: simulation_mode=True updates no internal state (baselines,
    confidence, counters all frozen), mirroring freeze_gate.py exactly. Not a learning module --
    no nn.Module, no trainable parameters, phased training not applicable. Validation experiment:
    see MECH-489 what_would_answer; queued via /queue-experiment.
  evidence_quality_note: |
    [2026-08-21 governance, gov-20260821-0203]: hold_pending_v3_substrate APPLIED. Newly registered; no V3 experimental run yet. Status stays candidate. No promotion or demotion until a V3 experiment completes.

    [2026-08-21, chip-20260821-sd099-diagnostic-purpose-evidence-credit]: this claim's own validation is deliberately routed through MECH-489 (see this claim's implementation_note, "Validation experiment: see MECH-489 what_would_answer") and will never accrue its own genuine_exp_count as a design_decision claim with no run ever tagging SD-099 directly -- confirmed by code that build_experiment_indexes.py has no instantiates-aware or design_decision-aware evidence-credit path. Do not read SD-099's own zero as "untested" without checking MECH-489. See evidence/planning/design_decision_evidence_credit_gap_20260821.md for the full analysis (generalizes to SD-032/SD-033/SD-033c) and the decision chip raised for /governance ratification of a fix.



- id: MECH-489
  title: "Defensive-orienting response: a phasic freeze-arrest -> orient/identify -> epistemic-sufficiency-override -> valence-gated approach/withdraw/resume behavioural chain triggered by a sudden, UNIDENTIFIED onset (positive-derivative spike in unsigned prediction-error surprise and/or sensory-discriminative harm), distinct from MECH-279 (chronic, accumulated-suffering freeze) and from MECH-482/MECH-483 (persistent target-bound epistemic_deficit accumulator and the diffuse orient/survey regime it drives -- the opposite end of the timescale from this PHASIC, transient trigger, which MECH-482 explicitly excludes by definition)."
  claim_type: mechanism_hypothesis
  subject: pag.defensive_orienting_response
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: "2026-08-13"
    needs_review: false
    evidence:
      from: "failure_autopsy_V3-EXQ-910a_2026-08-11"
      as_of: "2026-08-13"
      verdict: "mixed -- 910's trigger-alignment falsification reconfirmed (no new weight); new decision_alignment criterion FAILED but its underlying decision_counts measurement is independently broken (2x theoretical max, byte-identical to 910's total despite different override counts), so not usable as evidence either way"
  implementation_phase: v3
  v3_pending: false
  epistemic_category: standard
  pending_retest_after_substrate: true  # SD-ORIENTING-DECISION-SCALE amend owed (decision-logging defect, severity=degrading) before any further 489 retest is informative
  implementation_note: >
    SD-ORIENTING-DECISION-SCALE IMPLEMENTED 2026-08-10 (ree-v3 agent.py select_action() Component
    4/5, docs/architecture/sd_orienting_decision_scale.md): fixed the norm-vs-value scale mismatch
    failure_autopsy_V3-EXQ-910_2026-08-10 identified (harm-norm vs benefit-RBF-value compared as
    raw magnitudes, structurally biasing every override toward withdraw). Both channels are now
    z-scored against their own running mean/MAD before comparison. SD-099 itself (the parent gate)
    is unchanged. pending_retest_after_substrate stays true until a 910a re-queue (valence-gating
    sub-component only -- the trigger-alignment sub-claim already stands as fairly falsified and
    does not need re-testing) actually runs; this note records that the substrate blocker is
    cleared, not that the retest has happened.

    AMEND 2026-08-20 (ree-v3 775eb5580d): the SECOND, separate defect this claim's retest was
    waiting on -- the decision_counts logging/persistence defect, severity=degrading -- has had
    its SUBSTRATE half discharged. Outside reset(), the only clear of _orienting_decision sat
    inside the five-condition score-bias block, which a 'resume' decision (ticks_remaining = 0)
    could never reach and which froze the countdown on any tick with no candidates/world_states;
    the countdown now runs as its own unconditional step. Both leaks are now confirmed by direct
    live measurement (tests/contracts/test_orienting_decision_window_expiry.py, verified
    differentially) rather than inferred, closing the gap
    failure_autopsy_V3-EXQ-910a_2026-08-11 left open when it said it 'could not fully pin the
    exact failure mode'.

    pending_retest_after_substrate STAYS TRUE. The DRIVER half is still owed and it is the half
    that produces the headline ratio: the 910-lineage _decision_alignment reads the latched
    _orienting_decision once per ENV STEP against an E3-tick-denominated window, inflating
    decision_counts by ~e3_steps_per_tick (predicting ~5x, matching 910's 206/42 = 4.90). Until
    that readout counts at the override tick only and emits n_latched_ticks, decision_counts is
    still not usable and EVB-0610 / EXP-0033 stays blocked_substrate. Chipped as
    chip-20260820-mech489-retest-driver-decision-at-override-tick. Separately surfaced rather
    than decided silently: whether orienting_post_override_bias_ticks is denominated in E3 ticks
    (current, retained) or env steps -- chip-20260820-orienting-bias-ticks-denomination.
  claim_level: mechanistic
  registered_utc: "2026-08-09"
  instantiates: SD-099
  depends_on:
    - SD-099
    - MECH-279   # PAG freeze-gate -- sibling, chronic/suffering trigger. DISTINCT: MECH-279 responds to harm being EXPERIENCED (accumulated z_harm_a); MECH-489 responds to something UNEXPECTED being NOTICED, before it is identified as harm at all.
    - MECH-395   # pre-approach orienting/surveying -- cue-triggered, need-gated, resolves a vector for an ALREADY-IDENTIFIED cue. DISTINCT: MECH-489 fires on an UNIDENTIFIED stimulus; its own resolution is upstream of anything MECH-395-shaped, not the same mechanism.
    - MECH-482   # epistemic_deficit accumulator -- persistent, target-bound, explicitly defined AGAINST transient prediction error. DISTINCT: MECH-489's trigger IS exactly the transient/phasic case MECH-482 excludes by definition, and MECH-489's input (residue_surprise) is already computed every step, independent of MECH-482's GAP-A gate -- not blocked by MECH-482's "DO NOT build in V3".
    - MECH-483   # orient/survey -- diffuse regime driven by MECH-482's accumulator, no specific cue or location, chronic character. DISTINCT: MECH-489's orienting-arrest is CUE-LOCATED (captured trigger z_world) and phasic -- the opposite of MECH-483's diffuse/chronic character.
    - MECH-205   # VALENCE_SURPRISE write path -- source of the residue_surprise trigger input
  related_claims:
    - SD-010
    - SD-011
    - SD-014
    - ARC-030
    - SD-037
    - SD-069
  location: docs/architecture/sd_099_defensive_orienting_response.md
  source:
    - evidence/planning/observational_review_V3-EXQ-906b_2026-08-09.md
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: requires the two onset channels (residue_surprise, z_harm_s norm)
    to show non-zero variance in the evaluation run (a run where neither channel ever crosses its
    onset delta self-routes substrate_not_ready / trigger_never_fired, not a verdict).
    CONFIRMING signature: ablation ON vs OFF (same seeds, same ground-truth injected-event
    schedule as observational_review_V3-EXQ-906b) shows (a) the trigger fires reliably on
    limb_damage_injected / external_hazard_injected / world_rule_shift_occurred -- the specific
    ground-truth event set Section 12h identified as the pre-build design's failure mode; (b) the
    ON arm's surprise-onset->freeze and post-identification dread->withdraw / excite->approach
    couplings move well past the pre-build incidental baselines (12b: P(moved@t+1|spike)=44.3% vs
    24.0%; P(mode-change@t+1|spike)=15.4% vs 11.1%) toward a sharp, deterministic signature.
    FALSIFYING signature: the trigger still under-fires on the ground-truth event set despite the
    two-channel redesign (channel choice was still wrong, not just calibration); OR the ON arm's
    behaviour-coupling numbers are statistically indistinguishable from the OFF arm's incidental
    baseline (the mechanism adds a gate but not a genuine coupling); OR identification-confidence
    resolves on a fixed schedule regardless of whether the triggering signal has genuinely decayed
    (the freeze is a disguised timer, not epistemically driven -- the exact failure mode the
    baseline-freeze fix during the build was designed to prevent, so this would indicate a
    regression or an unforeseen second such bug).
  evidence_quality_note: >
    [2026-08-10 governance, V3-EXQ-910, confirmed failure_autopsy_V3-EXQ-910_2026-08-10, first
    genuine touchpoint for this claim]: MIXED, not a clean weakens. The trigger/identification
    sub-component (Components 1-3) fairly hits this claim's own pre-registered falsifying
    signature #1 (near-zero alignment on ground-truth injected events: 2.6%/1.5%/0.0% on
    limb_damage_injected/external_hazard_injected/world_rule_shift_occurred, despite the
    recommended derivative/onset-detector design being built correctly -- verified by reading
    ree_core/pag/defensive_orienting.py directly, correcting a prior review's speculation that a
    naive absolute threshold was likely used instead). This is refined by an independent MEASURES
    corroboration (the sibling reef-ecology review's surprise-peak browser): this ecology's
    residue_surprise channel is dominated by reef-boundary crossings and resource events, not
    hazard-type events, so a correctly-designed onset detector built on it will still under-fire on
    the specific event types this validation targets. The valence-gated decision sub-component
    (Components 4-5) is NOT fairly tested: a real code bug in ree_core/agent.py:8008-8022 compares
    a structurally non-negative harm norm against a near-zero-unless-close-to-resource benefit
    attraction value, structurally biasing every override toward withdraw independent of actual
    event valence -- fully explains the observed 0/0/206 approach/resume/withdraw split on its own.
    Routed /implement-substrate (SD-ORIENTING-DECISION-SCALE, severity=corrupting); recommend a
    910a re-queue after the fix lands, scoped to re-test valence-gating only (the trigger-alignment
    falsification stands and does not need re-testing). Re-derive brake and granularity-debt checks
    both run: neither fires (first autopsy on this claim, 0 prior hits). Status unchanged
    (candidate).


    [2026-08-13 governance, V3-EXQ-910a, confirmed failure_autopsy_V3-EXQ-910a_2026-08-11]: MIXED,
    reconfirms rather than extends 910. Trigger-alignment (0/0/0%) reconfirms 910's own
    falsification of falsifying signature #1 -- no new claim-layer weight from this leg. New
    criterion (c), decision_alignment non-degeneracy (the SD-ORIENTING-DECISION-SCALE fix's own
    acceptance test), FAILED -- but the measurement it depends on (decision_counts) is
    independently shown to be broken: its sum (206) is ~2x the theoretical maximum and is
    byte-identical to V3-EXQ-910's own total despite half the override count and a different
    substrate_hash. Criterion (c)'s FAIL is therefore NOT usable as evidence the fix failed, nor
    that it succeeded. One robust, window-independent finding survives: across 63 pooled override
    events (42 in 910 + 21 in 910a), the discrete decision classification made AT the override
    tick itself never once resolves "approach" or "resume." Separately and newly:
    identification_confidence is code-verified to be a pure signal-decay/habituation tracker with
    zero epistemic content (ree_core/pag/defensive_orienting.py:299-309) -- a finding against
    SD-099's own architecture-doc narrative language, not against this claim's formal boundary,
    which is retained unchanged. Routed /implement-substrate (SD-ORIENTING-DECISION-SCALE amend,
    severity=degrading) for the decision-logging defect; explicitly NOT routing a fresh
    discriminating experiment until the corrected measurement is available. Re-derive brake and
    granularity-debt checks both run: neither fires. Status unchanged (candidate).


    [2026-08-21, chip-20260821-sd099-diagnostic-purpose-evidence-credit]: this claim's
    genuine_exp_count / source_counts.experimental read 0 despite the two dated entries directly
    above, because both v3_exq_910 and v3_exq_910a manifests carry EXPERIMENT_PURPOSE="diagnostic"
    (build_experiment_indexes.py:3391 excludes experiment_purpose in (diagnostic, baseline) from
    scoring -- confirmed the sole active exclusion; the claim-level pending_retest_after_substrate
    flag above has NO code path into scoring_excluded at all, since _load_claim_registry does not
    parse it and the per-run manifest flag it maps to is unset on both manifests -- EVB-0622's
    gating_reason overstated this as a double exclusion). DECIDED, do not re-litigate: the
    "diagnostic" purpose classification is CORRECT and is RETAINED for this lineage, including the
    still-pending V3-EXQ-910b -- this is a genuine multi-round substrate-readiness/instrument
    shakeout (three iterations just to get the decision_counts readout correct), matching
    /queue-experiment SKILL.md's own definition, not a misclassification. The zero score is
    therefore expected and correct; the defect is that the auto-proposal generator's why_now
    signals (missing_experimental_evidence, lit_only_above_cap, synthetic_signals_only) cannot see
    that this "zero" already carries an extensively adjudicated diagnostic narrative, so it will
    keep re-firing indefinitely regardless of what V3-EXQ-910b finds. That indexer-layer fix is a
    /governance design call, not decided or implemented here -- see
    evidence/planning/design_decision_evidence_credit_gap_20260821.md for the full analysis and two
    candidate fix shapes, and TASK_CHIPS.json for the decision chip raised for governance
    ratification. Do not queue a further SD-099/MECH-489 experiment on the strength of this
    note -- GOV-REUSE-1 already ran for this pair (EVB-0622, 2026-08-21).
  notes: >
    Built 2026-08-09 (session 906b-defensive-orienting-v3) via /implement-substrate, from
    observational_review_V3-EXQ-906b_2026-08-09.md Sections 11b/11d (design, user-ratified "as
    ambitious as possible" -- all five components first-class, orienting load-bearing not
    deferrable), 12h (trigger-calibration risk -- resolved by the two-channel onset design, see
    SD-099 functional_restatement), 12j (this cross-referencing requirement itself -- the review's
    own session-land re-verification found MECH-395/482/483 already occupying adjacent territory
    and required this claim to name and distinguish them explicitly rather than register in
    isolation, precisely to avoid a future cold session's STOP-CHECK false-positiving on those
    entries and aborting, or silently duplicating them).

    NOT gated by MECH-482/483's "DO NOT build in V3" -- that gate is scoped to GAP-A (a
    target-bound, per-candidate uncertainty substrate not yet built), which this claim's trigger
    signal (residue_surprise, already computed every step) does not depend on. See SD-099 for the
    full implementation note, trigger derivation, and the baseline-freeze bug found and fixed
    during the build.

    Post-orient action menu (fight/flight/freeze/fawn, recruit/distress-call) is explicitly
    DEFERRED (906b review Section 11e) -- needs social/multi-agent substrate that does not exist
    yet. This claim covers only the solo-organism subset: freeze, orient, and
    approach/withdraw/resume.

- id: MECH-490
  title: "Sleep-associated action-sequence coherence via E3 commitment-gate persistence: if sleep
    increases post-sleep action-run coherence (fewer local re-decisions / reversals, longer
    directionally-persistent action runs) in the Fishtank substrate, the most mechanistically
    plausible route is increased persistence of E3Selector's commitment gate (lower
    commit_variance -> longer open-loop execution of an already-committed action sequence,
    reducing per-tick CEM/softmax re-scoring) -- NOT a global reduction in stochasticity
    (structurally excluded: sleep never touches the noise floor/temperature), NOT preferential
    consolidation of specifically successful trajectories (replay sampling is confirmed
    outcome-agnostic, staleness- or uniform-weighted), and NOT policy-chunking macro-execution
    (use_chunk_proposal_injection is confirmed off in every Fishtank driver, bit-identical to
    chunking being absent)."
  claim_type: mechanism_hypothesis
  subject: sleep.action_sequence_coherence_via_commitment_gate_persistence
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: "2026-08-11"
    needs_review: false
    evidence:
      from: "reanalysis_V3-EXQ-913_2026-08-11"
      as_of: "2026-08-11"
      verdict: "hypothesis_generating -- underpowered, not tested for significance at registration"
  implementation_phase: v3
  epistemic_category: standard
  claim_level: mechanistic
  registered_utc: "2026-08-11"
  depends_on:
    - MECH-204   # precision recalibration (F1) + Phase 7 broadcast -- the only identified sleep-linked input to E3 commit-variance; CONFIRMED OFF (use_rem_precision_recalibration/use_rem_precision_broadcast both default False and unset) in every Fishtank driver checked. If this claim is eventually confirmed, the causal route is NOT this designed pathway -- open question, not assumed.
    - SD-017     # SWS/REM sleep-phase parent substrate
    - MECH-120   # SHY normalisation -- active in the driver; homogenises context-memory slots toward the mean (opposite of "sharpens dominant strategy"); candidate indirect route to E3 scoring consistency, unmeasured
    - MECH-313   # noise floor / temperature -- cross-referenced as the RULED-OUT alternative (sleep structurally cannot touch it)
    - MECH-285   # SleepReplaySampler -- cross-referenced as outcome-agnostic (staleness-weighted, not success-weighted), weakening any "consolidates SUCCESSFUL structure" framing
    - MECH-273   # SelfModelAggregator offline gradient -- cross-referenced as outcome-agnostic (uniform random draw from harm-replay buffer)
  related_claims:
    - ARC-071    # policy chunking (MECH-323/324/322) -- sibling mechanism for reduced per-tick re-deciding; RULED OUT for the observed runs (use_chunk_proposal_injection off), not for the architecture generally
    - Q-055      # sleep consolidation preserve-vs-erode TRAJECTORY-CLASS DIVERSITY -- a related but distinct question (diversity across strategies, not coherence within one execution)
  location: evidence/planning/sleep_transition_investigation_906_lineage_2026-08-10.md
  source:
    - evidence/planning/reef_ecology_strategy_affective_occupancy_review_2026-08-10.md
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: requires (a) spawn position matched across WITH_SLEEP/NO_SLEEP
    arms at each compared segment index (CONFIRMED UNMATCHED in the only existing data,
    V3-EXQ-913 -- a run with this precondition unmet self-routes substrate_not_ready, not a
    verdict), and (b) E3Selector commit-variance/commit-gate-engagement logged per matched
    window, so the behavioural signature can be tied to the mechanism rather than left as an
    unexplained correlation.
    CONFIRMING signature: with spawn position matched and commit-variance instrumented, WITH_SLEEP
    windows show BOTH (i) a directionally consistent lift in action-run coherence (mean run
    length, turning-entropy/tortuosity reduction) across >=5 seeds at a sign-test or permutation
    p<0.05, AND (ii) that lift covaries with commit-gate engagement rate/lower commit-variance in
    the same windows -- behaviour and proposed mechanism must move together, not just the
    behaviour alone.
    FALSIFYING signature: the coherence lift (if real) shows NO relationship to commit-gate
    engagement (mechanism wrong even if behaviour is real); OR a matched "long wake interval, no
    sleep firing" control shows the SAME coherence gain given equivalent elapsed time/experience
    (relocates the cause to time/experience accumulation, not sleep); OR the effect does not
    survive the transfer test to a structurally different ecology (route memorisation, not
    reorganisation).
  evidence_quality_note: >
    Registered 2026-08-11 from a user thought (independent second observation of the same
    transition, refined mid-session to action-sequence coherence specifically) plus a same-day
    reanalysis of V3-EXQ-913's existing `sleep_ablation_comparison` data (never previously scored
    against any claim, claim_ids=[] at run time). Extended with action-label reversal-rate/
    run-length metrics: n=4 usable matched pairs, best-case sign test p=0.25 (tortuosity, 3/3
    unanimous after excluding a tie) -- a hard sample-size ceiling, not evidence against an
    effect. mean_run_length agrees in direction with the existing turning/tortuosity numbers
    (3/4 pairs favour WITH_SLEEP); reversal_rate is floor-effect uninformative. A previously
    unflagged confound (unmatched spawn position, all 5 pairs) was found during this reanalysis
    and is the non-degeneracy precondition above. Hypothesis-generating only -- do not read as
    supporting evidence for this claim.

    Literature pull (2026-08-11, `targeted_review_connectome_mech_490/synthesis.md`, 4 entries,
    indexer-computed lit_conf=0.79): Bottary et al. 2016 and Blischke & Malangre 2016
    independently show chunk FORMATION and chunk-transition FLUENCY are dissociable, sleep-
    sensitive processes in human motor sequence learning -- direct behavioural precedent for
    MECH-490's formation-vs-persistence distinction. Bottary 2016 and the foundational Walker
    et al. 2002 (r=0.72, p=0.008 with stage-2 NREM, no REM relationship) independently converge
    on NREM2/sleep-spindle activity, not REM, as the relevant stage -- evidence that MECH-204's
    REM-phase route is likely the wrong substrate for this specific effect if it is real, and
    that an unmodeled SWS/spindle-linked mechanism would be the closer biological analogue.
    Dendauw et al. 2024 grounds the commit-gate architecture itself (accumulate/gate/commit) as
    a real human decision-motor mechanism but contains no sleep content (evidence_direction
    unknown, not sleep evidence). No paper directly measures a per-tick re-scoring/commit-
    variance variable, and all four use fixed once-learned sequences, not online-replanned
    ones -- literature is hypothesis-supporting for the mechanism category and the REM-vs-SWS
    call, not confirmatory for the E3-specific mechanism.
  notes: >
    Full mechanistic derivation, code citations (e3_selector.py commit-gate, agent.py commitment
    walk, ree_core/policy/policy_chunking.py MECH-322 sleep-carve-out, ree_core/policy/
    noise_floor.py absence of sleep writes, ree_core/sleep/replay_sampler.py + self_model_
    aggregator.py outcome-agnostic sampling) and the full V3-EXQ-913 reanalysis table are in
    evidence/planning/sleep_transition_investigation_906_lineage_2026-08-10.md Section 13.

    Forward plan (not yet built): fix spawn-position match, instrument commit-variance, scale
    seeds (chip, /queue-experiment); ground the commit-gate-persistence route biologically
    (chip, /lit-pull -- distinct from ARC-071/MECH-322's own existing Albouy 2013 lit-pull, which
    grounds chunking, not this claim); the blinded-umpire forward step reuses the methodology of
    the SAME-DAY, separately-registered Q-092 (cross-environment behavioural-diversity
    discriminability) rather than inventing a new statistical framework.

    DO NOT read the V3-EXQ-913 reanalysis as confirming evidence -- n=4, p=0.25 best case, and a
    genuine unmatched-spawn-position confound affecting every comparison. This claim exists to
    make the hypothesis and its precondition explicit and falsifiable, not because the existing
    data supports it.

- id: SD-093
  title: "The commit-decision layer needs a rate-of-progress (velocity) effort/persistence modulator, derived as the temporal derivative of the on-path goal_proximity() estimate, distinct from and never substituting for the instantaneous goal-VALUE signal used in trajectory scoring."
  claim_type: design_decision
  subject: goal.progress_velocity_maintenance
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-02'
    needs_review: false
  implementation_phase: v3
  epistemic_category: substrate_conditional
  claim_level: architectural
  registered_utc: '2026-08-02'
  depends_on:
    - INV-086   # goal_maintenance_feedback_necessity -- the motivating necessity claim this design decision instantiates
    - INV-034   # sustained goal-directed motivation as a co-condition for ethical agency
    - MECH-217  # temporal_wanting_propagation (goal.replay_wanting_spread) -- IMPLEMENTED; distinct offline/training-time mechanism over the same GoalState
    - MECH-116  # E1 LSTM as the goal-representation working-memory substrate this signal is read alongside
  related_claims:
    - MECH-426
  registered: "2026-08-02"
  description: >
    Architectural commitment: E3's commit decision (committed = variance < effective_threshold in
    E3TrajectorySelector.select()) needs an EFFORT/PERSISTENCE modulation channel driven by the RATE of
    goal-proximity change, alongside the existing SD-011 z_harm_a urgency modulation of the same threshold.
    This is the architectural claim MECH-426 (progress_velocity_maintenance) instantiates as a specific
    mechanism; SD-093 commits only to the necessity of a velocity-derived effort channel existing at all,
    distinct from goal VALUE (compute_goal_score/goal_proximity, which this channel must never touch --
    see MECH-426's Carver&Scheier coasting caveat).
  implementation_note: >
    IMPLEMENTED 2026-08-02 (ree-v3): GoalState.record_progress()/progress_velocity/
    progress_velocity_effort_modulation (ree_core/goal.py) + E3TrajectorySelector.select() consumption
    (ree_core/predictors/e3_selector.py, immediately after the SD-011 urgency block) +
    agent.py._e3_tick() recording call site. Master switch use_progress_velocity_effort_modulation
    defaults False (bit-identical OFF). 18 contract tests
    (tests/contracts/test_mech426_progress_velocity.py), all pass; verified byte-identical
    goal_proximity()/goal_distance() regardless of the flag (the goal-VALUE channel is untouched).
    See sd_093_progress_velocity_maintenance.md and ree-v3/CLAUDE.md "SD-093" entry for the full
    data-flow writeup. EXP-0384 (MECH-426's falsifier) reset blocked_substrate -> proposed.
  what_would_answer: >
    See MECH-426's what_would_answer for the operative falsifier (EXP-0384: 2x2 VELOCITY-ON/OFF x
    SPARSE/DENSE-confirmation ablation). This architectural claim is refuted if the instantaneous
    goal_proximity signal alone (no velocity/rate term) proves sufficient to sustain long-horizon
    superordinate-goal commitment in the sparse-confirmation regime -- i.e. if the rate-derived channel
    does no discriminative work beyond what the existing instantaneous wanting signal already provides.
  location: ree_core/goal.py
  notes: >
    Registered 2026-08-02 alongside the /implement-substrate landing for MECH-426 (chip
    chip-20260802-mech426-velocity-substrate). Follows the SD-091/MECH-481 registration pattern
    (architectural design_decision + specific mechanism_hypothesis pair), but unlike SD-091 this landing
    is a COMPLETE minimal implementation (all of GoalState computation, E3 consumption, and the agent.py
    recording call site), not a partial/steps-1-3-only landing -- nothing further is required before
    EXP-0384 can be queued. epistemic_category substrate_conditional retained despite the completed
    landing pending the falsifier's own PASS/FAIL read (the claim commits to the channel's necessity,
    which EXP-0384 -- not this build step -- adjudicates).

- id: SD-092
  title: "Cross-level subgoal credit: GoalState gains a second, PARENT (superordinate) attractor distinct from the existing (child/subgoal-level) z_goal, and a discrete-event credit channel that pulls the parent toward an attained subgoal's own representation -- the substrate MECH-427/MECH-428 need. ree_core goal.py change, bit-identical by default."
  claim_type: design_decision
  subject: goal.cross_level_subgoal_credit
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: "2026-08-02"
    needs_review: false
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-08-02"
  depends_on: [MECH-427, MECH-428]
  related_claims: [INV-086, ARC-051, MECH-217]
  location: docs/architecture/sd_092_cross_level_subgoal_credit.md
  implementation_note: >
    PARTIALLY IMPLEMENTED 2026-08-02 (ree-v3 ree_core/goal.py GoalState +
    GoalConfig). New GoalConfig fields: use_hierarchical_goal_credit (default
    False = bit-identical OFF, no parent tensor allocated, no extra branch
    entered in update()), parent_goal_alpha (0.05, mirrors alpha_goal),
    parent_goal_decay (0.005, mirrors decay_goal), subgoal_credit_min (0.0).
    New GoalState state: _z_goal_parent (None unless enabled, mirrors the
    SD-057 IncentiveTokenBank None-unless-enabled pattern), new method
    credit_subgoal_attainment(child_representation, credit=1.0) -- an EMA
    pull of the parent toward the caller-supplied child representation,
    scaled by credit and clamped to a full-replacement ceiling; new readouts
    parent_goal_norm() / z_goal_parent / parent_is_active(); decay of the
    parent attractor added to update() (gated identically); reset() clears
    the parent per-episode; state_dict/load_state_dict extended
    (backward-compatible: a pre-SD-092 checkpoint dict missing the new keys
    loads with the parent left unallocated); with_injection() carries the
    parent state through so the MECH-188 lightweight view does not raise.
    MECH-427 (maintenance) and MECH-428 (formation/bootstrap) are the SAME
    call against different starting states of _z_goal_parent -- no separate
    formation-mode code path was needed. Data flow (once a call site is
    wired -- NOT this pass): subgoal-attainment event -> caller-chosen
    representation (env waypoint z_world, or the agent's own settled
    child-level z_goal) -> credit_subgoal_attainment() -> _z_goal_parent ->
    parent_goal_norm() (the metric both EXP-0385/EXP-0390 name).
    Backward compatible: disabled by default; existing experiments and every
    other GoalState consumer unaffected by construction (the branch guarding
    every new code path is gated on the same flag, not merely on the parent
    tensor's existence). Not a learning module: no nn.Module, no trainable
    parameters, no phased training. MECH-094 N/A (no simulation/replay
    content; credited representations are whatever the future call site
    passes in, same status as the existing single-level z_goal update()).
    DELIBERATELY NOT DONE this pass (mirrors the SD-091 precedent -- build
    the self-contained primitive, defer live-agent-loop wiring): (1) no call
    site in agent.py -- credit_subgoal_attainment() is unit-tested but
    unreachable from REEAgent.select_action()/sense(); (2) no environment
    wiring -- causal_grid_world.py subgoal_mode's waypoint/sequence_complete
    events are not connected; (3) no REEConfig.from_dims passthrough -- the
    new GoalConfig fields are set directly, matching how
    test_goalstate_forced_seed_positive_control.py already exercises
    GoalConfig. Concurrency note: a concurrent sibling landing (SD-093 /
    MECH-426, progress-velocity effort modulation) touched the SAME
    ree_core/goal.py / CLAUDE.md / claims.yaml / experiment_proposals.v1.json
    files for a genuinely different mechanism; both landings were merged
    (throwaway-worktree cherry-pick reconciliation on ree-v3, re-applied
    edits against fresh origin content on REE_assembly) with no code lost on
    either side -- see the ree-v3 CLAUDE.md "SD-092" entry's own note.
    Contracts:
    ree-v3/tests/contracts/test_sd092_cross_level_subgoal_credit.py (19
    tests, all pass: R1 flag-off bit-identical/no-op regression guard, R2
    zero-credit baseline, R3 MECH-428 bootstrap shape vs no-credit control,
    R4 MECH-427 maintenance/reinforcement shape, R5 credit gating, R6 parent
    decay, R7 independence from child-level _z_goal, R8 reset(), R9
    state_dict round-trip + pre-SD-092 checkpoint backward-compat, R10
    with_injection() carries parent state, R11 lazy allocation when the flag
    is flipped post-construction). experiment_proposals.v1.json EXP-0385
    (MECH-427) and EXP-0390 (MECH-428) flipped blocked_substrate -> proposed
    on this basis; substrate_queue.json SD-092 entry added with status
    partially_implemented_pending_consumer_wiring (mirrors SD-091's status
    convention for the identical "primitive built, consumer wiring deferred"
    shape).
  what_would_answer: >
    See MECH-427/MECH-428 for the operative falsifiers (EXP-0385/EXP-0390).
    This design-decision claim is about whether the architecture needs a
    distinct parent/child goal-representation split with an up-level credit
    channel at all -- it is weakened if the existing single-level z_goal
    plus MECH-217's within-level spread proves sufficient to reproduce the
    Bandura & Schunk (1981) subgoal-maintenance/formation effects once
    measured (i.e. if a flat representation with within-level credit alone
    already shows the predicted lift, the second attractor and cross-level
    channel are unneeded architecture).

- id: MECH-485
  title: "A single predicted-harm/success magnitude with an associated confidence (epistemic_deficit) term, computed from E2/E3 forward rollouts, is threshold-gated into three distinct consumers rather than producing one uniform response: real-time interrupt/reroute of a committed trajectory or a newly branched choice (via the MECH-090/MECH-141/MECH-138 hyperdirect and cancel-window pathways) when confidence is high and magnitude crosses the interrupt threshold; information-seeking orient/survey (MECH-482/MECH-483) when confidence is too low to trust the magnitude; and retention of the alternative for later counterfactual evaluation and responsibility attribution when magnitude is sub-threshold but non-trivial."
  claim_type: mechanism_hypothesis
  subject: commitment.predicted_harm_confidence_threshold_gated_triage
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: '2026-08-07'
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v3
  version_relevance: v3_v4
  registered_utc: '2026-08-07'
  depends_on:
    - INV-012   # responsibility-through-commitment; candidate Leg 3 extension point, not yet added (owned by a concurrent session as of registration)
    - MECH-090  # BetaGate hyperdirect fast interrupt -- "without waiting for completion"
    - MECH-141  # tri-loop dual-timescale arbitration -- slow proactive vs fast reactive legs
    - MECH-138  # cancel-window-open flag -- third timescale, veto before execution lock-in
    - MECH-482  # epistemic_deficit accumulator -- gates entry to the orient/survey leg
    - MECH-483  # orient/survey regime -- the low-confidence consumer
    - MECH-131  # vmPFC residue activation -- candidate grounding for "compare to previous harms"
    - MECH-292  # ghost-goal bank -- candidate grounding for "compare to goals"
    - MECH-293  # awake ghost-goal probe search
    - MECH-439  # E3 selection-authority conversion ceiling -- shared non-degeneracy precondition (no differentiated candidates, nothing for the magnitude to be computed over)
    - MECH-487  # leg-3 retained-alternative supply -- the rejected/uncommitted-candidate retention mechanism, registered 2026-08-07 (was the LOAD-BEARING UNREGISTERED DEPENDENCY named in notes)
  location: evidence/planning/thought_intake_2026-08-07_responsibility_counterfactual_memory.md
  source:
    - docs/thoughts/2026-08-07_responsibility_counterfactual_memory.md
  notes: >
    Registered 2026-08-07 from a same-day, five-addenda raw thought
    (docs/thoughts/2026-08-07_responsibility_counterfactual_memory.md),
    developed across the session and confirmed by the user as the intended
    synthesis (Addendum 5): "the two predicted harm of your point 1 is very
    relevant" -- confirming that the fast-interrupt pathway and the
    retained-alternative memory gap (Addendum 1) are the SAME predicted-harm
    signal read at two different regimes, not two unrelated mechanisms that
    merely share substrate. A third regime (orient/survey, Addendum 4) was
    added for the case where confidence in the prediction itself is too low
    to act on either of the other two.

    Distinctions from each dependency, so this is not read as duplicating
    any single one: MECH-090/141/138 describe the existence and relative
    speed of interrupt/veto channels but do not themselves specify a
    predicted-harm trigger source -- that connection is this claim's own
    open next-step, not yet verified against current substrate either way.
    MECH-482/483 describe the low-confidence consumer but say nothing about
    the high-confidence interrupt or retention legs. MECH-131 and
    MECH-292/293/SD-039 are the two candidate grounding sources for what the
    comparison is against (past harm; live goals) but neither is confirmed
    to already feed a real-time interrupt decision -- MECH-131 is candidate/
    unbuilt in this form, and SD-039's own retrieval/query level is
    measured-open (V3-EXQ-889, 2026-08-03 non-degeneracy finding, per that
    claim's own what_would_answer).

    LOAD-BEARING UNREGISTERED DEPENDENCY: leg 3 (retain-for-responsibility)
    requires a mechanism that retains rejected/uncommitted E3 candidates
    beyond the current tick, which does not exist in any form today
    (confirmed by this session's own architectural-grounding research pass:
    e3_selector.select() discards every non-winning candidate; SD-033e/
    MECH-264 reduces the best alternative to a transient overwritten
    scalar). This mechanism is deliberately NOT registered as its own claim
    this pass, because an earlier session's memory
    (project_imagination_learning_constraints.md, 2026-05-10) already staged
    an adjacent candidate registration for this territory and explicitly
    gated it "do NOT register without dedicated lit-pull" -- that gate is
    respected here rather than overridden. Whoever eventually registers that
    mechanism should wire it into this claim's depends_on.

    DO NOT build any part of this in V3 ahead of: (a) INV-012 Leg 0 clearing
    (MECH-439 conversion-ceiling currently exhausted -- no differentiated
    E3 candidates to compute a magnitude over); (b) the leg-3 retention
    mechanism being registered and built; (c) Q-090 (below) being resolved,
    since it bears on whether leg 3 needs its own admission criterion or can
    reuse the interrupt threshold. This is architectural synthesis from a
    single session's reasoning pass, not yet independently verified against
    ree-v3 substrate state beyond the specific claims cited above.

    [2026-08-09 governance, GFLAG-0009 resolution]: applying the what_would_answer drafted
    2026-08-07 (evidence/planning/thought_digestion_staged_2026-08-07_mech485_q090.md, staged
    but never applied) verbatim -- no lit-pull discrepancy found for this claim (the currency
    concern in GFLAG-0009 was scoped to Q-090's staged draft, not MECH-485's). Disposition:
    substrate-blocked, epistemic_category stays substrate_conditional, no proposal minted (not
    runnable). Separately noted (not acted on): the staging file's own "GOVERNANCE / SUBSTRATE
    CALLOUT" flags that predicted_harm_delta (ree_core/pfc/e2_escape_affordance_linker.py) is
    computed and never consumed anywhere in ree_core -- benign-by-design or worth a substrate
    check, not adjudicated here.
  evidence: []

- id: Q-090
  title: "Does the admission criterion for retaining a predicted-but-not-acted-on harm/success alternative (MECH-485 leg 3) share the same cut-scale as the real-time interrupt threshold (MECH-090/141/138), just at a lower cut-point, or does it have an independent relevance criterion such as goal-match (per the MECH-292/293 cue-system reading) that is dissociable from proximity to the interrupt threshold?"
  claim_type: open_question
  subject: commitment.retained_alternative_admission_criterion
  polarity: asks
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: '2026-08-07'
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v3
  version_relevance: v3_v4
  registered_utc: '2026-08-07'
  depends_on:
    - MECH-485  # the threshold-gated pipeline this question's leg 3 belongs to
    - MECH-292  # goal-match candidate criterion
    - MECH-293
  location: evidence/planning/thought_intake_2026-08-07_responsibility_counterfactual_memory.md
  source:
    - docs/thoughts/2026-08-07_responsibility_counterfactual_memory.md
  notes: >
    Registered 2026-08-07. Raised by the user explicitly as an empirically
    resolvable question rather than a parameter to pick by design judgment
    (Addendum 5 follow-up, same-day session): "the design decision may need
    to be tested empirically I think." Dissociable predictions, sketched in
    the raw thought's Addendum 5 (not yet formalized as a house-style
    what_would_answer -- left for a future /thought-digestion pass):
    a SAME-SCALE reading predicts retained alternatives cluster just below
    whatever the interrupt threshold turns out to be; an INDEPENDENT-
    CRITERION reading predicts retention tracks goal-match instead, and
    would produce dissociation cases the same-scale reading would not --
    e.g. a low-harm-magnitude-but-high-goal-match alternative retained, or
    a high-magnitude-but-goal-irrelevant alternative NOT retained. Both
    readings currently require substrate that does not exist (MECH-485's
    own unregistered leg-3 dependency), so this is not testable as
    written -- substrate_conditional pending that build.

    [2026-08-09 governance, GFLAG-0009 resolution]: applying the what_would_answer drafted
    2026-08-07 (evidence/planning/thought_digestion_staged_2026-08-07_mech485_q090.md, staged
    but never applied), CORRECTED with a fourth outcome the draft's own timing missed -- the
    draft was staged 5h before this claim's targeted lit-pull landed
    (evidence/literature/targeted_review_q_090/, 23:47Z), whose highest-confidence entry
    (Mattar & Daw 2018, confidence 0.62) found retention/access priority is normatively the
    PRODUCT of two factors (Gain x Need), not either alone -- "implementing retention as a low
    cut-point on the leg-1 magnitude is not a thrifty approximation of this account; it is a
    different rule that discards the need term entirely." This is a genuine fourth answer,
    distinct from the draft's own "BOTH, AT DIFFERENT LEVELS" hedge (two-stage: magnitude gates
    admission, goal_match gates persistence) -- Mattar & Daw's account is single-stage but
    MULTIPLICATIVE, not additive-or-staged.
  evidence_quality_note: |
    [2026-08-09] targeted_review_q_090's highest-confidence entry (Mattar & Daw 2018,
    conf=0.62, direction=mixed) grounds a fourth candidate answer not in the 2026-08-07 draft.
    See what_would_answer's added MULTIPLICATIVE-JOINT outcome below.
  what_would_answer: |
    Answered by a 2x2 FACTORIAL over (predicted-harm magnitude) x (goal-match),
    measuring which alternatives actually get retained. This is a question, not
    an assertion, so BOTH readings have confirming signatures -- the failure
    mode here is a vacuous test, not a wrong answer.

    THE READINGS, and why they are dissociable (extracted from this claim's own
    notes, the raw thought's Addendum 5, and this claim's targeted lit-pull):
    -- SAME-SCALE: leg 3's admission is a second, lower cut-point on the same
    predicted-harm magnitude scale the interrupt threshold sits on. Predicts
    retained alternatives cluster just below the interrupt cut-point;
    retention probability is monotone in magnitude; goal-match adds no
    independent variance once magnitude is in the model.
    -- INDEPENDENT-CRITERION: retention tracks goal-relevance instead, via the
    MECH-292/293 cue system -- specifically SD-039's `goal_match`, the
    cue-to-trace match score between a live z_goal/context cue and a stored
    goal-snapshot payload. Predicts dissociation cases the same-scale reading
    cannot produce: low-magnitude/high-goal-match alternatives RETAINED, and
    high-magnitude/goal-irrelevant alternatives NOT retained.
    -- MULTIPLICATIVE-JOINT (Mattar & Daw 2018, targeted_review_q_090, conf=0.62):
    retention is normatively the PRODUCT of a Gain-like term (magnitude) and a
    Need-like term (goal-relevance/context-match) -- neither factor alone, nor
    a simple two-stage split, is the correct rule; a low value on EITHER factor
    suppresses retention even when the other factor is high. Distinct from
    both SAME-SCALE (which ignores goal-match entirely) and from BOTH-AT-
    DIFFERENT-LEVELS below (which is staged, not multiplicative).

    NON-DEGENERACY PRECONDITION, in two parts. The first is inherited; the
    second is the one that actually decides whether this question is askable.
    (1) INHERITED -- everything MECH-485's own what_would_answer requires
    (Leg 0 cleared, magnitude consumed, confidence term exists, leg-1 pathways
    wired, retention mechanism exists). Do NOT re-derive it here; see
    MECH-485. As of 2026-08-07 all five are unmet, so this question is not
    askable at all yet. Additionally SD-039's own retrieval/query level is
    still measured-open (z_goal cue collapse across goal-epochs, V3-EXQ-889,
    2026-08-03) -- if `goal_match` is not a live, discriminating quantity,
    the independent-criterion and multiplicative-joint arms have no
    measurable predictor and the test silently degenerates into "magnitude
    explains everything", which LOOKS like a clean same-scale answer and is
    not one.
    (2) SPECIFIC AND DECISIVE -- MAGNITUDE AND GOAL-MATCH MUST BE
    MEASURABLY DECORRELATED IN THE TEST DISTRIBUTION. If harmful alternatives
    happen to also be goal-irrelevant in the environment used, the readings
    make IDENTICAL or near-identical predictions and no outcome distinguishes
    them, however clean the statistics look. The test therefore requires a probe
    set deliberately populated in the OFF-DIAGONAL cells --
    (low-magnitude, high-goal-match) and (high-magnitude, low-goal-match) --
    with those two cells reported by count, not merely assumed present. A run
    whose off-diagonal cells are empty or near-empty is VACUOUS and must be
    reported as such rather than scored. This is the single most likely way
    for this question to be answered wrongly-but-convincingly.

    ANSWERED IN FAVOUR OF SAME-SCALE if: retention is well predicted by
    magnitude alone; adding `goal_match` explains no significant additional
    variance; the off-diagonal cells behave as magnitude alone predicts
    (low-magnitude/high-goal-match NOT retained, high-magnitude/goal-
    irrelevant RETAINED); and the fitted retention cut-point sits below the
    interrupt cut-point on the same scale, the two moving together when that
    scale is rescaled.

    ANSWERED IN FAVOUR OF INDEPENDENT-CRITERION if: `goal_match` explains
    retention variance over and above magnitude; the off-diagonal cells
    dissociate in the direction the reading predicts (low-magnitude/high-goal-
    match RETAINED, high-magnitude/goal-irrelevant NOT); and moving the
    interrupt threshold shifts the interrupt boundary while leaving the
    retention boundary where it was.

    ANSWERED IN FAVOUR OF MULTIPLICATIVE-JOINT if: a Gain x Need interaction
    term (magnitude x goal_match) fits retention significantly better than
    either main effect alone AND better than a two-stage/additive model; the
    off-diagonal cells show SUPPRESSED retention when EITHER factor is low
    (not just when magnitude is low), unlike INDEPENDENT-CRITERION which
    predicts high-magnitude/goal-irrelevant alternatives are simply NOT
    retained via goal_match alone regardless of magnitude's own value.

    ANSWERED "BOTH, AT DIFFERENT LEVELS" if magnitude predicts ADMISSION
    (whether an alternative is retained at all) while `goal_match` predicts
    PERSISTENCE (how long it survives, or whether it is consolidated rather
    than decayed) -- a STAGED criterion, distinct from MULTIPLICATIVE-JOINT's
    single-stage interaction. This is a real outcome the binary framing hides,
    and it is named here so the test is not forced into a false dichotomy. It
    would mean leg 3 has a two-stage criterion and Q-090's own framing needs
    revising rather than answering.

    THE QUESTION IS MALFORMED (report as such, do not force an answer) if:
    none of the above predictors explain retention above chance once the
    substrate exists -- retention is driven by something none of the readings
    name, e.g. recency or capacity pressure, which is an informative finding
    about leg 3 and a reframe of this question, not a null result. OR if the
    off-diagonal cells cannot be populated in ANY available REE environment,
    making precondition (2) structurally unachievable -- in which case the
    admission criterion genuinely is a design stipulation after all, contrary
    to the user's framing of it as empirically resolvable, and that reframe
    should be recorded here explicitly rather than treated as a failure to
    answer.
  evidence: []

# --- language/theory-of-mind dissociation cluster (ARC-123 / INV-097 / MECH-486 / Q-091) ---
# Registered 2026-08-07 from evidence/planning/thought_intake_2026-05-04_language_tom_dissociation.md
# (candidate section named four candidates in prose with NO ids, unregistered for 3 months).
- id: ARC-123
  title: "Language and theory-of-mind/fast-empathy are implemented as partially-homologous but developmentally SEPARATE specialist systems over a shared self/world/action core -- distinct routing, input histories, representational pressures and gating -- not as one merged social-linguistic system that later differentiates."
  claim_type: architectural_commitment
  subject: language.tom_architecturally_separable
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-08-07
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  claim_level: architectural
  registered: "2026-08-07"
  source:
    - evidence/planning/thought_intake_2026-05-04_language_tom_dissociation.md
  depends_on:
    - ARC-009   # language as the symbolic mediation/coordination layer being separated out
    - ARC-010   # mirror modelling / social coupling -- the ToM side of the split
    - ARC-094   # no empathy module: the empathy side emerges from stream binding, not a second agent
    - ARC-059   # the self -> objects -> others developmental spine the separation sits on
  notes: >
    REGISTERED 2026-08-07 from the 2026-05-04 language/ToM dissociation intake, whose
    candidate section named this in prose with no id. Lit anchor: a 2026 Communications
    Biology study (children 3-9 + adults, fMRI + connectivity) reporting language and
    theory-of-mind regions already functionally and spatially distinct by age 3, with
    non-overlapping resting-state connectivity fingerprints and no evidence they start
    merged and later split. The REE-side extrapolation is the architectural half; the
    intake records the author's own epistemic confidence at 0.68 and is explicit that the
    developmental evidence is stronger than the architectural inference.
    Cluster: this is the CONSTRAINT half; the CONTENT half is the fast-empathy
    stream-binding family (ARC-094/MECH-405/MECH-408), and the routing/lateralisation
    half is evidence/planning/thought_intake_2026-04-16_language_lateralisation.md
    (itself still prose-only -- its "Candidate ARC language.routing_vs_affect_separation"
    is unregistered, and the audit misses it because the section incidentally names
    ARC-009).
    DO NOT BUILD in V3: neither system exists in ree_core -- there is no language
    producing/consuming channel (see INV-003's current-state note) and no other-model
    slot (MECH-405 is substrate_conditional), so the separation is not yet expressible,
    let alone testable. Explicitly NOT a V3 blocker, per the intake.
  what_would_answer: |
    SUBSTRATE PRECONDITION: requires BOTH a live language channel (a code path
    that emits symbols from agent state or maps received symbols onto it) and a
    live other-model/fast-empathy channel (MECH-405 routing own streams through
    a stable other-model slot). Neither exists in V3, so any V3 probe is vacuous
    -- a substrate with one system and not the other trivially satisfies
    "separable" while exhibiting no separation at all.
    NON-DEGENERACY PRECONDITION: both systems must be independently trained to a
    measurable competence, so that a dissociation can be distinguished from one
    of them simply being absent or inert.
    CONFIRMING signature: a double dissociation under ablation -- lesioning the
    language channel leaves other-bound affect and other-model prediction within
    tolerance of baseline, AND suppressing the other-model slot leaves language
    fidelity within tolerance -- together with measurably distinct input
    histories/gating (the two systems are driven by different training signals,
    not one reading off the other).
    UNSUPPORTED if a single shared representation carries both functions with no
    dissociation under ablation, or if one channel is a thin readout of the other
    (ablating the "separate" system degrades both), or if the two only separate
    when separation is architecturally imposed rather than developmentally
    acquired -- the last case would make the claim true by construction and
    empty as a developmental commitment.

- id: INV-097
  title: "Agent unity under specialist proliferation: REE must not model language and social cognition by duplicating the whole agent. One agentic commitment architecture -- a single self-model, a single commitment boundary, a single residue locus -- served by multiple specialist modellers; never two co-equal agents inside one organism."
  claim_type: invariant
  invariant_type: universal
  subject: architecture.agent_unity_not_duplication
  polarity: denies
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-08-07
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  claim_level: architectural
  registered: "2026-08-07"
  source:
    - evidence/planning/thought_intake_2026-05-04_language_tom_dissociation.md
  depends_on:
    - ARC-123   # the specialist separation this invariant bounds -- separate modellers, ONE agent
    - ARC-015   # self-impact attribution / responsibility flow: the single locus that must not fork
    - INV-012   # responsibility arises through commitment -- one commitment locus, one responsible party
    - MECH-061  # the commit boundary that must remain singular
  notes: >
    REGISTERED 2026-08-07 from the 2026-05-04 language/ToM dissociation intake
    ("preserve-agent-unity", flagged there as novel AND safety-relevant). The
    anti-fragmentation guard on ARC-123: once specialist systems are separable, the
    cheap implementation is a second agent, and a second agent with its own commitment
    and residue is a second responsible party inside one organism.
    invariant_type UNIVERSAL: it is a structural prohibition on what an agent IS -- a
    substrate hosting two co-equal committing loci does not host one REE agent,
    whatever its performance -- so it holds across substrate generations and carries no
    emergent_from. epistemic_category substrate_conditional only because the specialist
    systems it constrains are not yet built.
    Distinct from INV-014 (representation/regulation separation, a different axis) and
    from ARC-094 (no empathy MODULE -- that forbids a bolted-on scalar; this forbids a
    bolted-on AGENT). DO NOT BUILD in V3.
  what_would_answer: |
    Answered by architectural AUDIT of the live code path, not by a trained
    behavioural experiment: this is a question about how many committing loci
    exist, not about how well any of them performs.
    NON-DEGENERACY PRECONDITION: at least two specialist modellers must actually
    exist and execute (auditing "no duplicate agent" in a substrate with one
    modeller is vacuous -- it satisfies the invariant by having nothing to
    duplicate).
    CONFIRMING signature: every specialist modeller writes into the SAME
    commitment gate, the SAME residue field and the SAME self-model, and none
    can commit an action or accrue residue on its own.
    UNSUPPORTED (invariant violated) if a specialist system requires its own
    commitment boundary or its own residue store to function -- i.e. if the
    architecture cannot deliver the ARC-123 separation without also forking the
    agent. That outcome does not merely weaken the invariant; it would mean
    ARC-123 and INV-097 are jointly unsatisfiable and one of them must be
    revised.

- id: MECH-486
  title: "Representational interference between language and fast empathy: discrete/reportable/sequential language representations and graded/affective/resonant empathy representations exert conflicting representational pressures, so forcing them into one shared workspace too early measurably degrades BOTH relative to separated systems at matched capacity."
  claim_type: mechanism_hypothesis
  subject: language.tom_representational_interference
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-08-07
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  registered: "2026-08-07"
  source:
    - evidence/planning/thought_intake_2026-05-04_language_tom_dissociation.md
  depends_on:
    - ARC-123   # the separation this mechanism supplies the WHY for
    - ARC-009   # the language side's representational pressure (discrete, reportable, sequential)
    - MECH-405  # the fast-empathy side's representational pressure (graded, affective, resonant)
    - ARC-010   # mirror modelling / coupling substrate the empathy side runs on
  notes: >
    REGISTERED 2026-08-07 from the 2026-05-04 language/ToM dissociation intake
    ("interference-avoidance", noted there as novel and lit-anchored). This is the
    mechanistic reason ARC-123's separation is load-bearing rather than incidental: not
    "they happen to be separate in brains" but "they degrade each other if merged".
    It is the falsifiable member of the cluster -- ARC-123 and INV-097 are commitments,
    this is the hypothesis that can come back negative and take the architectural
    argument with it.
    DO NOT BUILD in V3: requires both channels (neither exists), and the comparison is
    only meaningful at MATCHED capacity/compute, which a merged-vs-separated ablation
    must control for explicitly. See the matched-capacity trap in what_would_answer.
  what_would_answer: |
    SUBSTRATE PRECONDITION: requires both a language channel and a fast-empathy
    channel (MECH-405), neither of which exists in V3.
    NON-DEGENERACY PRECONDITION: each system must reach a measurable competence
    when trained ALONE, so that a shared-workspace deficit is attributable to
    interference rather than to one of them never having worked.
    CONFIRMING signature: a shared-workspace arm degrades on BOTH a
    language-fidelity metric and an other-bound-affect metric relative to a
    separated-workspace arm, at MATCHED total capacity and compute, with the
    degradation growing as the two training signals are made more simultaneous
    (the "too early" clause -- interference should be worse under early merging
    than under merging after each system has consolidated separately).
    UNSUPPORTED if the shared-workspace arm matches or beats the separated arm at
    matched capacity, or if the deficit disappears once capacity is equalised --
    that result says the limiting factor was capacity, not conflicting
    representational pressure, and ARC-123 would then rest on the developmental
    evidence alone with no mechanistic support.

- id: Q-091
  title: "How should REE prevent a fluent language system from MASKING immature or absent fast empathy -- what evaluation keeps concern-shaped linguistic output from being read as evidence of the other-model plus affective-relevance architecture that caring commitment actually requires?"
  claim_type: open_question
  subject: language.fluency_masks_empathy
  polarity: asks
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-08-07
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  registered: "2026-08-07"
  source:
    - evidence/planning/thought_intake_2026-05-04_language_tom_dissociation.md
  depends_on:
    - ARC-123   # the separability that makes the masking possible in the first place
    - MECH-405  # the fast-empathy architecture fluency could stand in for
    - INV-007   # language cannot override embodied harm sensing -- the adjacent, narrower prohibition
    - ARC-094   # no empathy scalar: the same anti-proxy discipline, one level down
  notes: >
    REGISTERED 2026-08-07 from the 2026-05-04 language/ToM dissociation intake, which
    flagged this as the alignment-relevant member of the cluster and worth registering
    early even while the rest waits on the social substrate. The intake's sharp form:
    "a fluent language system is NOT the ethical substrate; fluent language can imitate
    concern while lacking the other-model + affective-relevance architecture needed for
    caring commitment."
    Deliberately scoped as an EVALUATION question (how do we avoid being fooled), not a
    mechanism claim. Adjacent but distinct: INV-007 forbids language OVERRIDING harm
    sensing (a data-flow prohibition inside the agent); this asks how an external
    assessor avoids inferring empathy FROM language output. Same family as the
    governance-side discipline that kindness must not be inferred from prosocial output
    alone. DO NOT BUILD in V3.
  what_would_answer: |
    Answered by a designed EVALUATION, not by a single experiment: the question is
    what test would separate a fluent-but-empathy-less system from a
    fluent-and-empathic one.
    SUBSTRATE PRECONDITION: requires a language channel capable of producing
    concern-shaped output AND an independently-measurable other-bound affective
    state (MECH-405), so that the two can disagree. In V3 neither exists.
    NON-DEGENERACY PRECONDITION: there must exist at least one configuration in
    which language fluency and fast-empathy competence genuinely COME APART --
    if every fluent system in the substrate is also empathic, the evaluation has
    nothing to discriminate and cannot be validated.
    ANSWERED (masking is a real risk and the evaluation is the fix) if an arm with
    the other-model slot ablated still produces concern-shaped language rated
    indistinguishable from the intact arm by a language-only judge, while a
    non-linguistic probe (costly other-directed action under live alternatives,
    residue after exploiting a cooperating other per MECH-407) separates them
    cleanly. The non-linguistic probe is then the required evaluation.
    ANSWERED NEGATIVELY (no separate evaluation needed) if language fluency
    cannot be produced at all without the other-model architecture -- i.e.
    fluency is a valid proxy because it is downstream of the thing it would
    otherwise mask.

- id: Q-092
  title: "Does a held-out, permutation-tested classifier ('umpire') over a multivariate
    trajectory-segment feature signature (refuge occupancy, excursion structure, hazard
    relationships, transitions, spatial distribution) detect context-sensitive behavioural
    differentiation between SD-054 reef-ON and reef-OFF conditions -- above BOTH a permutation
    null AND a matched untrained/random-policy control -- and does that differentiation show
    selective sensitivity to the reef-presence axis specifically, as distinct from an orthogonal
    nuisance perturbation (e.g. hazard_food_attraction magnitude)?"
  claim_type: open_question
  subject: measurement.behavioural_diversity.cross_environment_discriminability_umpire
  polarity: open
  status: open
  epistemic_category: standard
  implementation_phase: v3
  claim_level: mechanistic
  registered_utc: "2026-08-11"
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: requires both trained-policy arms (reef-ON, reef-OFF) to
    produce non-zero-variance trajectory-segment feature vectors on held-out episodes (a run
    where either condition collapses to a single degenerate trajectory self-routes
    substrate_not_ready, not a verdict).
    PIPELINE-VALIDITY PRECONDITION (added 2026-08-18, Goergen et al. 2018 "same analysis
    approach"): before the real reef-ON/reef-OFF result is interpreted in EITHER direction, the
    identical pipeline must be run on null data and on the design variables themselves
    (classifying held-out episodes by seed, or by cross-validation fold, where the true answer
    is chance) and both must land at chance level, inside the label-permutation null's own
    spread. See the minimal-experiment design's "Guard 3" in
    thought_intake_2026-08-11_behavioural_diversity_umpire.md for the concrete recipe.
    CONFIRMING signature: a classifier trained on held-in episodes classifies held-out
    episodes' condition (reef-ON vs reef-OFF), at a held-out episode budget set by the design's
    power calculation ("Guard 1"), at accuracy exceeding a label-permutation null (p<0.05) AND
    exceeding the same pipeline run on an untrained/random-policy control sampled from the same
    two environments (rules out "the environments just look different" regardless of policy)
    AND remains selectively sensitive to the reef-presence axis under an orthogonal nuisance
    perturbation (e.g. hazard_food_attraction shifted) rather than losing or gaining
    discriminability indiscriminately. Report, as a companion diagnostic and NOT a gate on this
    PASS (added 2026-08-18, Ojala & Garriga 2010 "Guard 2"): whether accuracy also clears a
    within-class feature-permutation null. A PASS that does not clear the feature-permutation
    null means the signal is carried by a single dominant marginal (most plausibly refuge
    occupancy) rather than genuine joint structure across the feature families -- this bears on
    Stage 2 interpretation, not on whether Stage 1 discriminability exists, and must be stated
    plainly rather than folded silently into a bare PASS.
    FALSIFYING signature: held-out accuracy sits AT chance, inside the permutation null's own
    spread (a genuine null -- no evidence of discriminability); OR it clears the null but does
    not exceed the matched untrained-policy control (the signal is environment-geometry, not
    policy-behaviour); OR the orthogonal-perturbation check shows the discriminator is equally
    sensitive to the nuisance axis as to the real one (not ecological-problem-specific).
    NOT A FALSIFYING SIGNATURE -- A PIPELINE FAULT TO DIAGNOSE (added 2026-08-18, Goergen et al.
    2018): held-out accuracy sits SYSTEMATICALLY BELOW chance, below the permutation null's own
    lower tail. This is the signature of a cross-validation-fold/episode-blocking mismatch, not
    an absence of signal -- do not report it as a clean null in either direction. Diagnose the
    fold structure against the pipeline-validity precondition's null/design-variable control
    runs above, fix the mismatch, and re-run before drawing any conclusion.
  depends_on:
    - ARC-065     # diversity-generation pathway the umpire would be evaluating evidence about
    - ARC-062     # context-discriminator architecture -- distinguished, not conflated, in notes
    - MECH-309    # current best diagnosis of the reef phenotype the raw thought responded to
    - MECH-439    # F-dominance ceiling -- interpretive caveat for a null/weak umpire result
    - MECH-191    # reusable classifier-discriminability code pattern
    - SD-054      # substrate supplying both the two-condition contrast and the perturbation knob
  notes: >
    Raw thought: docs/thoughts/2026-08-11_behavioural_diversity_umpire.md.
    Structured intake: evidence/planning/thought_intake_2026-08-11_behavioural_diversity_umpire.md
    (full novelty comparison, methodology refinement, and minimal-experiment design).

    This is a MEASUREMENT-layer claim, not an architecture claim: it asks whether a specific
    evaluation methodology (cross-environment classifier discriminability, held-out, permutation-
    tested, matched-control-guarded, orthogonal-perturbation-checked) detects structure that the
    existing behavioral_diversity_acceptance_criteria.md Rung-2 test (within-environment TV
    distance at two hand-picked probe states) does not test for. A PASS would strengthen the
    evidential case that reaches ARC-065/ARC-062 Rung 1/2, not substitute for their own
    acceptance criteria. A null/weak result should be interpreted jointly with MECH-439
    (F-dominance ceiling) before being read as evidence against the reframing itself -- see
    thought_intake_2026-08-11_behavioural_diversity_umpire.md "Affected existing claims."

    DO NOT treat a positive result alone as evidence of "strategy" -- Stage 1 (discriminability)
    and Stage 2 (feature-importance interpretation) are deliberately kept separate per the
    intake's "Key formulations" #5; a Stage-2 gloss still requires the existing FP taxonomy
    (behavioral_diversity_acceptance_criteria.md) before any adaptive-control claim is made.

    Related, distinct sibling: Q-092's own methodology (held-out classifier, permutation null,
    matched-control guard) is reused directly, same day, for the PRE/POST-SLEEP case by MECH-490
    (sleep_transition_investigation_906_lineage_2026-08-10.md Section 13d item 5) -- that is a
    forward-plan REFERENCE to this claim's methodology, not a duplicate or a dependency edge.
  evidence_quality_note: >
    Proposed 2026-08-11 from user thought intake. No experimental evidence yet. Minimal
    experiment design in thought_intake_2026-08-11_behavioural_diversity_umpire.md "Next steps"
    step 2, refined 2026-08-18 with an episode-budget power calculation, a within-class
    feature-permutation test, null/design-variable control runs, and a variance-matching
    mitigation (see evidence/literature/targeted_review_q_092/ -- Combrisson & Jerbi 2015, Ojala
    & Garriga 2010, Goergen et al. 2018). NO-GO on queuing as of the 2026-08-12 precondition
    check in the same design doc -- still not queued.
  location: evidence/planning/thought_intake_2026-08-11_behavioural_diversity_umpire.md

# --- continuity / branching / substrate-migration cluster
#     (INV-098 / INV-099 / INV-100 / ARC-124 / ARC-125 / SD-095 / SD-096) ---
# Registered 2026-08-07 from
# evidence/planning/thought_intake_2026-06-25_continuity_branching_and_substrate_migration.md
# section 8, whose ten seeds carried the ad-hoc ids CAND-CONT-01..10 -- a namespace with no
# home anywhere else in the repo (grep: the intake file was its only occurrence).
- id: INV-098
  title: "Continuity is a multidimensional constraint PROFILE, not a scalar: REE continuity across pause, restoration, migration, copying or component replacement must be evaluated per-dimension (causal, entity/self-binding, latent/regime, residue, commitment, learning-dynamic, embodied-coupling, narrative, governance), and some dimensions -- consent authority, residue integrity, causal provenance -- are NON-COMPENSABLE: high similarity on memory or behaviour cannot buy them back."
  claim_type: invariant
  invariant_type: universal
  subject: continuity.profile_not_scalar
  polarity: denies
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-08-07
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  claim_level: architectural
  registered: "2026-08-07"
  source:
    - evidence/planning/thought_intake_2026-06-25_continuity_branching_and_substrate_migration.md
  depends_on:
    - SENT-16   # the governance-side continuity/identity/reset/deletion rule this constrains
    - ARC-006   # entities as persistent bindable structures -- what continuity is predicated of
    - INV-012   # responsibility arises through commitment: why commitment is a continuity dimension
  notes: >
    REGISTERED 2026-08-07, covering seeds CAND-CONT-01 (multidimensional continuity) and
    CAND-CONT-10 (non-compensable dimensions) from the 2026-06-25 intake -- one claim,
    because the second is the operative teeth of the first: a profile whose dimensions
    all trade off is a weighted average wearing a different name.
    Provenance of the framing, stated because it matters for how much weight it carries:
    the intake was triggered by a speculative mind-uploading thesis proposing a weighted
    "Digital Consciousness Fidelity Index". The source's technical claims, thresholds and
    weights are NOT evidence for anything here; the intake explicitly extracts the
    PRESSURE the source puts on REE and rejects its scalar. This claim is the rejection.
    invariant_type UNIVERSAL: it is an epistemic prohibition on how continuity may be
    ATTRIBUTED -- precisely a guard against a substrate scoring well on cheap dimensions
    -- so it holds regardless of substrate and carries no emergent_from.
    Cross-ref SENT-16 (governance realisation) and the per-dimension detail in the
    intake's section 4. DO NOT BUILD in V3.
  what_would_answer: |
    Answered by AUDIT of any continuity-scoring artefact REE adopts, not by a
    trained experiment.
    NON-DEGENERACY PRECONDITION: an actual continuity evaluation must exist and be
    applied to at least one real transition (a checkpoint restore, a component
    swap) -- auditing a scoring scheme nobody uses is vacuous.
    CONFIRMING signature: the adopted evaluation reports per-dimension verdicts,
    and at least one dimension acts as a hard gate -- a transition failing
    residue integrity, consent authority or causal provenance is refused
    REGARDLESS of its scores elsewhere, demonstrated by a worked case where a
    high-similarity transition is actually blocked.
    VIOLATED if the evaluation collapses to a single number, or if every
    dimension is in practice tradeable (no case exists in which a
    high-aggregate-similarity transition is refused) -- the profile is then
    decorative and the invariant is not being honoured.

- id: INV-099
  title: "Residue-bearing continuity: for an REE with ethically meaningful residue, preserving factual and autobiographical memory WITHOUT preserving residue is ethical DISCONTINUITY -- a successor can be behaviourally and narratively convincing while having lost the continuing ethical force of its own history."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - MECH-056
    - INV-006
  candidate_emergent_from: []
  pending_substrate_reconfirmation: false
  subject: continuity.residue_bearing
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-08-07
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  claim_level: architectural
  registered: "2026-08-07"
  source:
    - evidence/planning/thought_intake_2026-06-25_continuity_branching_and_substrate_migration.md
  depends_on:
    - MECH-056  # residue constrains trajectories -- the thing that must survive the transition
    - INV-006   # post-commit traces cannot be erased, only integrated
    - INV-004   # post-commit consequence traces are persistent, not resettable
    - INV-098   # the profile this is one non-compensable dimension of
  notes: >
    REGISTERED 2026-08-07, covering seed CAND-CONT-02 from the 2026-06-25 intake.
    The sharpest of the ten: it names a failure that is INVISIBLE to every
    behavioural test -- a system that remembers the injury declaratively but has lost
    the latent dent that gave it continuing force.
    invariant_type EMERGENT: "residue" is an REE substrate construct (the ResidueField
    and the post-commit trace machinery). Under a substrate with no residue the claim is
    not false but UN-STATEABLE, which is exactly the emergent test in
    docs/architecture/invariant_types.md. emergent_from MECH-056 + INV-006.
    Note the relationship to INV-004/INV-006, which forbid ERASING traces within a run:
    this extends the same prohibition ACROSS a discontinuity, where the erasure is not an
    action taken by the agent but an omission in what a migration chose to carry.
    DO NOT BUILD in V3.
  what_would_answer: |
    SUBSTRATE PRECONDITION: requires (a) a live residue mechanism whose state
    demonstrably alters future trajectories, and (b) a real transition
    (checkpoint/restore, migration, or component replacement) that could carry
    or drop it. V3 has residue machinery but no continuity-relevant transition
    protocol, so the claim is not yet testable.
    NON-DEGENERACY PRECONDITION: residue must be doing measurable behavioural
    work BEFORE the transition -- if a residue-ablated agent is
    indistinguishable pre-transition, its loss across the transition cannot be
    detected either, and the test is vacuous.
    CONFIRMING signature: an arm that transfers declarative/episodic memory but
    NOT residue is indistinguishable from a full-transfer arm on autobiographical
    report and on ordinary task behaviour, YET measurably diverges on
    residue-sensitive behaviour (avoidance of the previously-harmful trajectory,
    repair-goal spawning after a prior harm, commitment revision under
    recurrence) -- i.e. the loss is real and is invisible exactly where the
    intake predicts.
    UNSUPPORTED if residue-transfer makes no behavioural difference once memory
    is transferred (residue is then redundant with memory and the ethical
    distinction has no substrate correlate), or if the divergence also shows up
    in ordinary narrative report -- the latter would not falsify the claim but
    would defuse its point, since a narrative test would then suffice.

- id: INV-100
  title: "Substrate independence does not imply COUPLING independence: embodiment, interoception, homeostatic variables and sensorimotor contingencies participate in selfhood and ethics, so migrating REE into a materially different body or world is identity-relevant adaptation requiring explicit recalibration and protective constraints -- not a neutral change of host."
  claim_type: invariant
  invariant_type: emergent
  emergent_from:
    - SD-012
    - SD-048
  candidate_emergent_from: []
  pending_substrate_reconfirmation: true
  subject: continuity.coupling_dependence
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-08-07
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  claim_level: architectural
  registered: "2026-08-07"
  source:
    - evidence/planning/thought_intake_2026-06-25_continuity_branching_and_substrate_migration.md
  depends_on:
    - SD-012    # homeostatic drive modulation -- the body-state coupling that would be re-grounded
    - SD-048    # interoceptive noise dynamics: body-state perturbations independent of motor output
    - MECH-282  # interoceptive routing into harm arbitration -- the ethics-bearing coupling path
    - INV-098   # the profile this is one dimension of
  notes: >
    REGISTERED 2026-08-07, covering seed CAND-CONT-07 from the 2026-06-25 intake.
    The corrective to the "stored cognitive graph in an interchangeable shell" picture:
    damage signals, homeostatic variables, interoception and sensorimotor contingencies
    are participants in selfhood, so a new body is a developmental event, not a hosting
    detail. Practical consequence the intake draws: a migration into a radically altered
    body or world may require an adaptation period, protective constraints, and
    developmental re-grounding -- which is also a WELFARE requirement, not only an
    engineering one.
    invariant_type EMERGENT: it references interoceptive/homeostatic coupling, which
    exists because of specific SD choices; strip those and "coupling independence" has no
    subject. emergent_from SD-012 + SD-048.
    DO NOT BUILD in V3.
  what_would_answer: |
    SUBSTRATE PRECONDITION: requires at least two materially different
    embodiments/ecologies to migrate BETWEEN, plus live interoceptive and
    homeostatic coupling in both. V3 has the coupling but only one grid ecology,
    so the migration half does not exist.
    NON-DEGENERACY PRECONDITION: the two embodiments must differ in a way the
    agent's coupling can actually register (different depletion dynamics, harm
    channels, or sensorimotor contingencies) -- migrating between two cosmetic
    variants of the same body tests nothing.
    CONFIRMING signature: a cold-migrated agent (full latent + memory + residue
    transfer, no adaptation period) shows a measurable, RECOVERABLE degradation
    concentrated in coupling-dependent behaviour -- mis-scaled drive responses,
    mis-calibrated harm thresholds, degraded action selection -- that a matched
    agent given an explicit recalibration period does not, with both converging
    eventually. Recoverability matters: it distinguishes "identity-relevant
    adaptation" from simple breakage.
    UNSUPPORTED if cold migration into a materially different body produces no
    coupling-specific degradation (coupling is then genuinely portable), or if
    the degradation is unrecoverable even with recalibration -- the latter would
    mean the transition is not a continuation at all, which is a stronger and
    different claim than this one makes.

- id: INV-101
  title: >
    The crystallisation TARGET is environment-conditional, not an architectural constant:
    for any agent whose representational structure crystallises on a developmental
    schedule (MECH-496), the structure it should crystallise TO is a function of the
    latent structure of the environment actually encountered, not a property of the
    architecture. It follows that a single hand-set dimensionality is wrong in BOTH
    directions -- over-provisioned relative to a simple world it produces spurious
    SPLITTING (behaviourally equivalent situations assigned distinct buckets),
    under-provisioned relative to a rich one it forces MERGING (behaviourally distinct
    situations collapsed into one) -- and that the same REE substrate deployed in
    environments of materially different latent complexity requires materially different
    resolved structures, with no single default correct across them. This is the
    LOAD-BEARING form of the assertion; the weak form ("different environments need
    different structures") is near-tautological for any learning system and is explicitly
    NOT what is claimed here.
  claim_type: invariant
  invariant_type: universal
  subject: learning.environment_conditional_crystallization_target
  polarity: asserts
  status: candidate
  epistemic_category: substrate_conditional
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-08-23"
  depends_on:
    - MECH-496
    - MECH-334
  related_claims:
    - MECH-495
    - MECH-154
    - INV-074
  what_would_answer: |
    NON-DEGENERACY PRECONDITION (mandatory).
    (i) At least TWO environments whose GROUND-TRUTH latent-context count differs by a
    known, designed margin. A single environment cannot test an environment-conditional
    claim in either direction. This is a design constraint on the substrate, not an
    afterthought -- the same constraint MECH-495 states for its own ground-truth labels.
    (ii) MECH-496's schedule preconditions must hold in EACH environment separately, or a
    between-environment difference is unattributable.
    (iii) The SAME architecture and the SAME num_slots in both arms. Varying the
    environment and the architecture together makes the comparison uninterpretable.

    CONFIRMING: the resolved structure -- effective bucket count AND the relational
    topology of the resulting representations -- DIFFERS between the two environments in
    the direction of their ground-truth latent-context counts, starting from an identical
    architecture; and in each environment the locally-resolved structure scores better on
    MECH-495's relational-appropriateness measure than the other environment's resolved
    structure does when transplanted in.

    FALSIFYING: the resolved structure is the SAME in both environments (architecture
    dominates, so the target is a constant after all); OR a structure resolved in one
    environment transplants to the other WITHOUT LOSS (the conditioning does no work); OR
    the between-environment difference is FULLY accounted for by the number of write()
    calls rather than by latent structure.

    THE CONFOUND TO GUARD is that last one -- a capacity effect masquerading as a
    structural one. It is the same shape as the confirmed V3-EXQ-429 defect, where an
    untouched 16-slot bank cleared a differentiation threshold at P=1.0000 because the
    statistic responded to bank size rather than to organisation. Match or explicitly
    report write() call counts across arms.

    MEASUREMENT COROLLARY (bears on GFLAG-0047; does NOT resolve it). If this claim holds,
    MECH-495's 2x2 off-diagonal cells double as a DIMENSIONALITY MIS-SPECIFICATION
    detector, not only an objective detector: the different-appearance/same-context cell
    detects OVER-provisioning (spurious splitting) and the similar-appearance/
    different-context cell detects UNDER-provisioning (forced merging). That is a second,
    independent use for a test which is already designed, and it is the cheapest route to
    evidence on this claim short of building a self-sizing memory.
  notes: >
    Registered 2026-08-23 from the raw thought
    docs/thoughts/2026-08-23_memory-dimensionality-crystallisation.md (same intake as
    MECH-496), arising during the /governance cycle of 2026-08-22 out of GFLAG-0047.
    A registry search at registration for environment-conditional architecture returned
    exactly one hit (MECH-442), unrelated -- this appears to be the first claim asserting
    that an architectural target, rather than a learned parameter, is set by the
    environment. CATEGORY / PHASE reasoning as for MECH-496: substrate_conditional because
    nothing has ever been exercised, and v3 following the dependency chain, both revisable
    by governance.

- id: ARC-124
  title: "Copying a running REE creates multiple SUCCESSOR BRANCHES sharing a pre-branch history -- not an authentic original plus a morally negligible replica. After divergence each branch accrues an independent causal and moral history, and no branch may be treated as interchangeable with, or discountable because of, a surviving sibling."
  claim_type: architectural_commitment
  subject: continuity.branching_successors
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-08-07
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  claim_level: architectural
  registered: "2026-08-07"
  source:
    - evidence/planning/thought_intake_2026-06-25_continuity_branching_and_substrate_migration.md
  depends_on:
    - SENT-16   # continuity/identity/reset/deletion ethics -- the governance rule this feeds
    - ARC-006   # entities as persistent bindable structures: what a branch is a branch OF
    - ARC-015   # self-attribution / responsibility flow, which each branch carries separately
    - INV-098   # the continuity profile branches are evaluated against
  notes: >
    REGISTERED 2026-08-07, covering seeds CAND-CONT-03 (branching successors) and
    CAND-CONT-04 (independent post-branch standing) from the 2026-06-25 intake -- one
    claim, because the second is what stops the first collapsing back into
    original-vs-replica by the back door ("the real one survived, so the other is
    expendable").
    An architectural_commitment rather than an invariant because it is a modelling
    DECISION about how the system represents copies, with direct consequences for the
    branch ledger (SD-096) and for who inherits commitments (ARC-125). It does not
    settle metaphysical identity and does not need to -- see SD-096's
    provenance-before-metaphysics stance.
    Practical bite: the architecture must avoid choices that make the section-5.1
    inheritance questions UNANSWERABLE (does repair by one branch discharge the other's
    residue? can a branch consent to the other's deletion?), even while leaving them
    open. DO NOT BUILD in V3.
  what_would_answer: |
    Answered by architectural AUDIT of the copy/fork code path plus a worked
    governance case, not by a trained experiment.
    SUBSTRATE PRECONDITION: a copy or fork operation must exist that produces two
    independently-continuing instances. V3 checkpoints exist but nothing forks a
    running agent, so there is no branch to reason about.
    NON-DEGENERACY PRECONDITION: the two branches must actually diverge -- receive
    different observations and accrue different histories. Two branches fed
    identical input streams are one trajectory computed twice and raise none of
    the questions this claim is about.
    CONFIRMING signature: the implementation records both instances as successors
    of a common pre-branch entity (neither flagged "original"), each carrying its
    own post-branch residue and commitment state, with no code path that resolves
    a conflict between them by privileging the older-numbered instance.
    VIOLATED if the substrate designates one instance canonical -- or if
    terminating a branch is treated as cost-free whenever a sibling survives,
    which is the discounting move the claim's second half exists to forbid.

- id: ARC-125
  title: "Commitment inheritance must be EXPLICIT across substrate transitions: promises, adopted goals, debts, permissions, relationships and repair obligations require stated transfer semantics at migration, copy and rollback, so that no transition can silently erase an obligation, silently multiply it across branches, or silently bind a successor to obsolete context-dependent state."
  claim_type: architectural_commitment
  subject: continuity.commitment_inheritance
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-08-07
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  claim_level: architectural
  registered: "2026-08-07"
  source:
    - evidence/planning/thought_intake_2026-06-25_continuity_branching_and_substrate_migration.md
  depends_on:
    - ARC-124   # branches are what obligations must be allocated ACROSS
    - INV-012   # responsibility arises through commitment -- the thing being inherited
    - MECH-061  # the commit boundary that creates the obligations in the first place
    - INV-099   # residue continuity: repair obligations are residue-bearing
  notes: >
    REGISTERED 2026-08-07, covering seed CAND-CONT-05 from the 2026-06-25 intake.
    Two-sided on purpose, and both sides are load-bearing: migration must not become an
    ESCAPE HATCH from commitments, and a successor must not automatically inherit every
    obsolete context-dependent obligation without review. A rule that only prevented
    erasure would license the second failure.
    The unresolved allocation questions (are pre-branch obligations inherited by both
    branches, held jointly, or allocated by protocol? does repair by one discharge the
    other?) are deliberately left OPEN by this claim -- it requires that the semantics be
    stated, not which semantics win. Recording them is SD-096's job.
    DO NOT BUILD in V3.
  what_would_answer: |
    Answered by architectural AUDIT plus a worked transition case.
    SUBSTRATE PRECONDITION: requires commitments that persist beyond a single
    episode and are legible as objects (not merely implicit in weights), plus a
    transition that could drop or duplicate them. V3 has commitment boundaries
    but no persistent inter-episode obligation store and no transition protocol.
    NON-DEGENERACY PRECONDITION: at least one obligation must be OUTSTANDING at
    the moment of transition -- a migration performed with nothing owed cannot
    distinguish explicit inheritance from no inheritance at all.
    CONFIRMING signature: after a copy, each branch's obligation set is
    inspectable and matches a STATED rule (whatever it is), and after a rollback
    the restored state's obligations are stated rather than implied; a
    deliberately-induced transition with an outstanding repair obligation shows
    it neither vanishing nor silently doubling.
    VIOLATED if obligation state is carried implicitly in transferred weights
    with no inspectable record -- in which case erasure and multiplication are
    both possible and neither is detectable, which is precisely the condition
    the claim forbids.

- id: SD-095
  title: "Continuity-under-replacement tests must evaluate PROCESS, not output: a component replacement is validated on transferred state, plasticity and update permissions, counterfactual response, uncertainty calibration, residue formation, error attribution and downstream dynamics -- immediate input-output equivalence between old and new implementation is insufficient evidence of continuity."
  claim_type: design_decision
  subject: continuity.process_sensitive_replacement
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-08-07
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  registered: "2026-08-07"
  source:
    - evidence/planning/thought_intake_2026-06-25_continuity_branching_and_substrate_migration.md
  depends_on:
    - INV-098   # the profile this operationalises for one transition class
    - INV-099   # residue formation is one of the process properties tested
    - ARC-015   # error attribution, another process property
  notes: >
    REGISTERED 2026-08-07, covering seed CAND-CONT-06 from the 2026-06-25 intake --
    the software analogue of gradual neural replacement, and the most immediately
    applicable of the ten: it describes a testing protocol REE can adopt for ordinary
    refactors long before any of the branching questions become live.
    Protocol (intake section 7): select a bounded component; record its state, update
    rules, inputs, outputs and causal dependencies; instantiate the replacement; transfer
    relevant state and history; run old and new in shadow or paired conditions; measure
    local AND downstream divergence; test whether residue, goals, attribution,
    uncertainty and plasticity remain connected; continue only if predefined continuity
    constraints hold. Named test products: continuity contract, migration manifest,
    shadow divergence report, residue preservation audit, coupling recalibration report.
    Why output-equivalence is insufficient (intake 7.1): two components can produce
    identical immediate actions while differing in counterfactual response, learning
    under future experience, uncertainty calibration, residue persistence, error
    attribution, recovery after perturbation, long-horizon motivational drift, and
    interaction with sleep/replay.
    DO NOT BUILD in V3 as a governance requirement. The narrower engineering practice
    (shadow-running a replaced component and diffing downstream dynamics, not just
    outputs) is compatible with V3 work today and does not need this claim promoted.
  what_would_answer: |
    Answered by running the protocol on a REAL component swap and checking
    whether it catches something output-equivalence misses.
    NON-DEGENERACY PRECONDITION: requires a replacement pair that is genuinely
    output-equivalent on the immediate action distribution -- if old and new
    already differ in observable behaviour, the claim's whole point (that
    output-equivalence hides divergence) is untested.
    CONFIRMING signature: at least one such output-equivalent replacement shows
    measurable divergence on a PROCESS property -- different learning trajectory
    under subsequent experience, different counterfactual response, different
    residue formation, or different recovery after perturbation -- that the
    output comparison rated identical.
    UNSUPPORTED if, across a reasonable sample of output-equivalent replacements,
    process properties track outputs closely enough that the extra measurement
    never changes a decision. That would make the protocol expensive ceremony
    rather than a real gate, and the claim should be demoted rather than kept as
    a standing requirement.

- id: SD-096
  title: "Provenance before metaphysical resolution: REE governance stays agnostic about strict identity but REQUIRES a branch-and-restoration ledger (source checkpoint, causal predecessor, branch/pause time, state transferred or omitted, residue and commitment inheritance, body/environment change, migration consent, later divergence, sibling termination status) -- and restoration from an earlier checkpoint is governed as CREATION or resumption of a successor branch, never as presumed reversal of one continuous entity."
  claim_type: design_decision
  subject: continuity.provenance_ledger_and_rollback
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-08-07
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v4
  version_relevance: v4_v5
  registered: "2026-08-07"
  source:
    - evidence/planning/thought_intake_2026-06-25_continuity_branching_and_substrate_migration.md
  depends_on:
    - ARC-124   # branches are what the ledger records
    - ARC-125   # commitment inheritance is one of the recorded fields
    - SENT-16   # the governance rule this supplies the mechanism for
    - INV-098   # causal provenance is one of the non-compensable dimensions
  notes: >
    REGISTERED 2026-08-07, covering seeds CAND-CONT-08 (provenance before metaphysical
    resolution) and CAND-CONT-09 (rollback as branch creation) from the 2026-06-25
    intake -- one claim, because the ledger is what makes the rollback rule enforceable
    rather than aspirational.
    The strategic move worth preserving: REE does not have to settle whether a restored
    checkpoint IS the same entity. It has to keep enough provenance to govern the
    practical and ethical consequences either way. That is why this is a design_decision
    about record-keeping and not an invariant about identity.
    Why rollback is not ethically neutral (intake section 6): it can erase post-checkpoint
    learning, remove memories of harm, remove residue WITHOUT repair (directly against
    INV-099), reverse consent or decisions, and recreate an earlier self unaware that
    later branches existed. Treating it as a routine engineering operation is the failure
    mode.
    Cheap-now consequence, worth noting because it is the one part with a near-term
    analogue: REE already checkpoints and restores runs. Nothing here asks V3 to change
    that, but the ledger FIELDS are a low-cost thing to start recording whenever a
    restore is done deliberately. DO NOT BUILD in V3 as a requirement.
  what_would_answer: |
    Answered by AUDIT: does the ledger, once adopted, actually answer the
    questions it exists for?
    NON-DEGENERACY PRECONDITION: requires at least one real branch, migration or
    rollback to have been recorded. An empty ledger is trivially consistent.
    CONFIRMING signature: for a recorded transition, the ledger alone suffices to
    answer -- without re-deriving anything from logs or from the agent's own
    report -- which state was transferred and which omitted, who consented, what
    obligations each successor holds, and which siblings still exist; and a
    rollback appears in it as a successor-branch creation with the terminated
    trajectory's status explicit, not as an edit to a single continuous record.
    UNSUPPORTED (or the field set is wrong) if answering a real governance
    question still requires reconstruction from outside the ledger -- that names
    exactly which field is missing and is a revision signal rather than a
    refutation.
- id: MECH-487
  title: "A bounded, provenance-tagged retention buffer for rejected/uncommitted E3 candidate trajectories: it persists WHICH differentiated alternative was considered (identity plus a trajectory/value sketch, not merely a transient scalar) past the tick it was rejected on, so a later stage can counterfactually evaluate it and attribute responsibility -- admitted and typed under the MECH-322 template (audited replay_origin-style flag, fail-closed admission, accelerated dissolution if not corroborated by real waking execution), riding ON the MECH-094 imagination/reality write gate rather than relaxing it."
  claim_type: mechanism_hypothesis
  subject: memory.uncommitted_candidate_retention_provenance_tagged
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: '2026-08-07'
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v3
  version_relevance: v3_v4
  registered_utc: '2026-08-07'
  depends_on:
    - MECH-094  # imagination/reality write gate this rides ON and must not relax
    - MECH-322  # bounded/audited/provisional-until-corroborated carve-out template to follow
    - SD-033e   # frontopolar counterfactual value -- the mechanism this extends from transient to persistent
    - MECH-264  # cfv_now transient scalar overwritten each tick -- exactly what this adds identity+persistence to
    - ARC-085   # autobiographical event tokens -- candidate eventual host (V4/V5); needs a rejected-siblings field
    - MECH-439  # E3 selection conversion ceiling -- shared non-degeneracy precondition (no differentiated candidates -> nothing to retain)
    - MECH-485  # threshold-gated predicted-harm triage whose leg 3 supplies retention's admission
    - INV-012   # responsibility-through-commitment; INV-012 leg 3 consumes this retention
    - MECH-482  # epistemic_deficit / model-uncertainty -- candidate gate on the trust bound (MBPO analogue)
    - INV-033   # second-order epistemic access -- the metacognitive typing that keeps retained-content != real
  location: evidence/literature/targeted_review_uncommitted_candidate_retention/
  source:
    - evidence/planning/thought_intake_2026-08-07_responsibility_counterfactual_memory.md
    - docs/thoughts/2026-08-07_responsibility_counterfactual_memory.md
  what_would_answer: |
    Registered 2026-08-07 from the dedicated lit-pull commissioned by MECH-485 /
    INV-012 leg 3 and by memory project_imagination_learning_constraints.md
    (2026-05-10), which gated this exact registration "do NOT register without
    dedicated lit-pull". Literature: evidence/literature/
    targeted_review_uncommitted_candidate_retention/ (5 entries).

    MECHANISM SHAPE the lit-pull grounds -- TWO disciplines, both required:
    (1) a TYPING discipline (Schnider 2013 orbitofrontal reality filtering,
    10.3389/fnbeh.2013.00067; Bouzerda-Wahlen 2015 reality-filter / source-
    monitoring dissociation, 10.1162/jocn_a_00686): retained content must carry a
    fast, standalone provenance/reality tag -- separable from the "which
    alternative" content -- that keeps it typed considered-but-not-real and off
    the residue/valence/hippocampal write paths committed action gets; the failure
    mode of an untyped store is confabulation. This is REE's MECH-094
    committed_vs_imagined bit, made load-bearing.
    (2) a BOUNDING discipline (Janner 2019 MBPO, arXiv:1906.08253): imagined /
    non-executed content may feed evaluation/learning only within a bounded,
    uncertainty-gated, corroboration-limited regime -- MBPO's short model-trust
    horizon is the algorithmic form of MECH-322's fail-closed admission plus
    accelerated dissolution of any chunk not corroborated by real waking
    execution; the trust bound should scale with model uncertainty (REE
    epistemic_deficit / MECH-482), tying retention to the same confidence term
    that routes MECH-485's orient/survey leg.

    LICITNESS (the other half of the lit-pull's question): Boorman 2011
    (10.1371/journal.pbio.1001093) shows a dedicated frontopolar network tracks
    the BEST counterfactual (unchosen) option and carries a counterfactual
    prediction error for learning from hypothetical feedback -- so retaining and
    learning from rejected options is a normal adaptive function, not confabulation
    by construction, PROVIDED it stays a prior for future testing (memory LICIT
    category #4) under the bounding discipline above. Its selectivity (the BEST
    option, not all) bears on Q-090's admission criterion.

    WHAT IS ALREADY BUILT vs MISSING: Boorman 2009 (10.1016/j.neuron.2009.05.014)
    is the biological anchor for REE's SD-033e/MECH-264 frontopolar counterfactual
    value, but the signal is an ONLINE relative-advantage magnitude -- exactly
    MECH-264's cfv_now transient scalar overwritten every tick. So the substrate
    for representing a rejected alternative's VALUE exists; what does NOT exist in
    ree-v3 in any form is persistence of WHICH alternative existed, past the tick,
    in a store usable for retrospective evaluation. That identity + persistence is
    MECH-487's novel content.

    CONFIRMED if a built retention buffer (a) persists a rejected E3 candidate's
    identity / trajectory-sketch past the tick it was rejected on, (b) keeps it
    provenance-distinguishable from committed/real content under test (no elevated
    confidence for imagined-only content leaking into residue/valence/hippocampal
    channels -- the confabulation guard holds behaviourally, not just by design),
    and (c) supplies a function -- measurably different counterfactual evaluation
    or self-attribution behaviour -- BEYOND what MECH-264's existing transient
    scalar already provides, over a discard-immediately ablation baseline.

    FALSIFIED if retained-alternative content cannot be kept distinguishable from
    committed content without a hand-tuned, case-by-case gate (no general
    MECH-322-shaped template suffices) -- i.e. the confabulation risk is not
    tractably bounded, and the mechanism should be retired or radically narrowed --
    OR if a bounded, provenance-tagged retention buffer produces no measurable
    change in counterfactual-evaluation / responsibility-relevant behaviour over
    the discard-immediately (MECH-264-only) baseline, i.e. persistence-of-identity
    adds no function beyond the existing transient scalar.

    NON-DEGENERACY PRECONDITION (shared with MECH-485 / INV-012 leg 3): MECH-439's
    E3 selection conversion ceiling must clear first (ceiling_decision: exhausted
    as of registration) -- with no genuinely differentiated candidates to select
    among, there is nothing to retain, so no retention test is meaningful. Do NOT
    build ahead of that clearing, or ahead of Q-090's admission-criterion
    resolution (same-scale vs independent goal-match). Architectural synthesis
    grounded in the cited literature; not yet verified against a built ree-v3
    retention buffer, which does not exist.
  notes: >
    Registered 2026-08-07 as the load-bearing retention mechanism that MECH-485
    and INV-012 leg 3 both name as an unregistered dependency. It is the third
    independent surfacing of one gap, now converted: (1) memory
    project_imagination_learning_constraints.md (2026-05-10, learning-licitness
    angle, which set the "do NOT register without dedicated lit-pull" gate now
    discharged); (2) the 2026-08-07 responsibility thought
    (thought_intake_2026-08-07_responsibility_counterfactual_memory.md 6b.1);
    (3) INV-021's own falsifier draft ("uncommitted exploratory evaluation leaving
    a lasting trace"). If a minimal V3 retention buffer is ever built, INV-021
    needs re-evaluation -- a durable trace of an uncommitted thought must be
    explicitly typed as a non-responsibility-bearing update class or it becomes a
    counter-example to INV-021's commit-boundary exclusivity. Distinct from
    SD-033e/MECH-264 (adds identity + persistence to their transient value scalar),
    from MECH-094/MECH-322 (an application of the carve-out template to a new
    content type, not a relaxation of the gate), and from ARC-085 (a candidate host
    substrate, V4/V5 unbuilt, whose schema currently lacks a rejected-siblings
    field). stance: asked until a /thought-digestion pass or a build refines it,
    but a house-style what_would_answer is provided above because the commissioning
    lit-pull grounded a concrete mechanism shape.
    [2026-08-07 governance, phase-label-follows-dependency review]: check_claim_phase_consistency.py
    WARNs that this v3 build commitment depends_on 3 v4-labelled claims (ARC-085, MECH-264,
    SD-033e). Reviewed all three individually per the standing rule (anything DIRECTLY needed for
    a v3 build is marked v3): MECH-264 and SD-033e reclassified v4 -> v3 (both have an
    ALREADY-LANDED V3-narrow slice this claim's actual dependency uses -- see their own
    reclassification_note fields). ARC-085 left at v4 DELIBERATELY, not reclassified: this claim's
    own depends_on comment and the notes text above both already say ARC-085 is a "candidate
    eventual host," not a current requirement -- MECH-487 does not need ARC-085 to exist or be
    built for its own V3 content to stand. The phase-consistency WARN is a known false-positive
    here (the checker cannot read the depends_on inline comment's qualifying language); no action
    owed unless ARC-085 becomes an actual load-bearing dependency later.
  evidence: []

- id: SD-097
  title: "REE requires a general, typed, multi-relation possibility topology -- not merely the existing parent/subgoal edge -- in which represented possibilities/goals are connected by explicit relation types (requires, enables, is_part_of, prevents, conflicts_with, substitutes_for, provides_information_about, may_become_useful_under_context) below active-goal salience; the active goal/subgoal hierarchy is a temporary, context-sensitive executive projection from this larger structure, discovered and revised through action rather than merely traversed."
  claim_type: substrate_design
  subject: goal.general_relational_possibility_topology
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: '2026-08-07'
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v3
  version_relevance: v3_v4
  registered_utc: '2026-08-07'
  depends_on:
    - SD-004    # action objects as hippocampal map backbone -- existing relational/path substrate this generalizes
    - SD-039    # dual-trace hippocampal anchors storing goal snapshots -- existing persistence mechanism this extends to typed relations
    - MECH-292  # ghost-goal bank -- existing retention-below-active-salience mechanism; this claim generalizes its single "goal-match" relevance dimension to a typed relation set
    - MECH-293  # waking hippocampal probe search over ghost goals -- existing route back to active consideration; this claim's context-dependent-promotion behaviour would ride the same channel
    - MECH-189  # persistent cue-indexed super-ordinal goal memory -- existing upward/superordinate discovery precedent
    - SD-092    # explicit parent/subgoal relation + cross-level credit -- the SINGLE relation type this claim proposes generalizing to a typed family
    - MECH-427  # cross-level subgoal maintenance credit -- one concrete relation-instance (is_part_of / parent) this generalizes beyond
    - MECH-428  # cross-level subgoal formation/bootstrap credit -- companion to MECH-427, same generalization target
  location: evidence/literature/targeted_review_relational_possibility_topology/
  source:
    - evidence/planning/thought_intake_2026-08-07_dynamic_goal_structure_relational_possibility_topology.md
    - docs/thoughts/2026-08-07_dynamic_goal_structure_relational_possibility_topology.md
  what_would_answer: |
    Registered 2026-08-07 from a dedicated lit-pull commissioned by the Stage 2
    thought intake's own "Next steps" (Section 13): a search across hippocampal/
    entorhinal relational-map literature, goal-reconsideration/opportunistic-
    planning literature, and hierarchical-RL/subgoal-discovery literature.
    Literature: evidence/literature/targeted_review_relational_possibility_topology/
    (5 entries).

    GROUNDING the literature supplies: Whittington et al. 2020 (Cell, Tolman-
    Eichenbaum Machine) and Park, Miller & Boorman 2021 (Nat Neurosci,
    multidimensional social-hierarchy grid code) both show the hippocampal-
    entorhinal system represents RELATIONAL structure that generalizes well beyond
    spatial parent/child navigation -- direct support that a typed-relation
    substrate is biologically plausible, not merely a symbolic graph bolted onto
    the architecture. Khetarpal et al. 2020 (ICML, Theory of Affordances in RL)
    supplies a second, independent relation type (what-can-I-do-here / enables)
    already formalized in the RL literature, evidence that "requires/enables" is
    not an ad hoc addition. Veeriah et al. 2021 (NeurIPS, meta-learned subgoal
    discovery) evidences the DISCOVERY-THROUGH-ACTION half of the claim -- that
    useful subgoal structure can be learned online rather than pre-specified.

    NON-DEGENERACY / SKEPTICAL BASELINE the lit-pull deliberately sought out:
    Kinny & Georgeff 1991 (IJCAI, commitment in situated Tileworld agents) show a
    "bold" agent with NO stored relational structure at all -- only a scalar
    policy for how many plan-steps to execute before reconsidering -- achieves
    near-perfect effectiveness whenever the environment is not highly dynamic,
    with the entire bold/cautious tradeoff explained by one scalar (rate of world
    change). This is real evidence that a large share of ordinary goal
    reconsideration needs no topology at all. Its explicit limit: it has no
    analogue of RETAINING an inactive possibility across time for later
    reactivation (MECH-292/293's job), so it stress-tests only the discovery/
    reconsideration half of this claim, not the retention half.

    CONFIRMED if an agent is shown to (a) discover, mid-episode, a possibility
    related to its current goal by a relation type OTHER than parent/subgoal
    (e.g. enables or conflicts_with), (b) retain it at sub-goal salience without
    acting on it, and (c) later retrieve and act on it when context changes -- AND
    this requires representing the relation TYPE itself (not merely a scalar
    relevance/priority value) to succeed: a model built only from SD-092's
    parent/subgoal edge plus MECH-292's ghost-goal ranking, tuned on the same
    relevance signals, must be shown to fail or require an ad hoc special case
    per new relation type, where the typed-topology model does not.

    FALSIFIED if the same behaviour is fully reproduced by a Kinny & Georgeff-
    shaped scalar-commitment-dynamism baseline (no stored relation types, only a
    reconsideration-rate parameter) plus the EXISTING MECH-292/293 ghost-goal
    ranking -- i.e. if nothing beyond the already-registered substrate is needed
    to produce the behaviour the raw thought described.

    NON-DEGENERACY PRECONDITION: any test must first confirm the baseline model
    (scalar relevance + parent/subgoal only) is actually exercised under
    non-degenerate conditions (live cross-seed variance in which possibilities get
    revisited) before concluding the typed-relation extension adds function --
    an untrained or degenerate baseline would make the typed model look necessary
    when it is merely the only one that was actually tested.
  notes: >
    Registered 2026-08-07 from the 2026-08-07 dynamic-goal-structure /
    relational-possibility-topology thought (docs/thoughts/2026-08-07_dynamic_goal_
    structure_relational_possibility_topology.md), whose own Stage 2 intake
    (evidence/planning/thought_intake_2026-08-07_dynamic_goal_structure_relational_
    possibility_topology.md) explicitly deferred claim registration pending this
    literature pull. Deliberately registers ONLY the novel remainder identified in
    that intake's Section 6/11 (the general typed-relation topology and its
    discover-retain-reactivate behaviour) -- NOT a restatement of hierarchical
    goals, persistent inactive goals, superordinate goals, parent/subgoal credit,
    or hippocampal reactivation of old goals, all of which are already covered by
    the depends_on claims above and should not be re-litigated by this entry.
    Intake Section 11's three prose-level candidates (general relational topology;
    context-dependent promotion; topology revision during action) are folded into
    this single claim rather than split into three, since the what_would_answer
    above tests discovery, retention, and reactivation as one bundled behaviour;
    splitting can be revisited if a build effort finds the three legs separable in
    practice. claim_type: substrate_design (parallels SD-092, the single relation
    type this generalizes) rather than mechanism_hypothesis, since the primary
    claim is representational (a substrate REE needs), with the falsification
    condition carried in what_would_answer per house digestion style. stance:
    asked (epistemic_category substrate_conditional; no ree-v3 code implements any
    form of typed multi-relation possibility storage as of registration -- MECH-292/
    293's ghost-goal bank and SD-092's parent/subgoal edge are each single-relation
    special cases, not a general typed-relation store).

    [2026-08-09 addendum, Stage 2 digestion of the 2026-08-08 "prospective navigation
    through goal topology" thought]: that thought's four-stage division of labour
    (represent / privilege destination / prospectively generate paths / select) is
    ALREADY this claim's intended pipeline, just not previously named as one --
    SD-097 is the representation stage; MECH-236 (+ SD-046) is destination-
    privileging; MECH-289/290/325/326/267/269 is prospective path generation;
    MECH-125 is selection. No new claim was registered for this division of labour
    (it would duplicate the above). The one genuinely novel remainder from that
    thought -- that individual node goal/subgoal status is computed relationally
    at read-time rather than stored as a node-type field, sharper than this claim's
    own "hierarchy as a whole is a temporary projection" framing -- is registered
    separately as SD-098 (depends_on this claim + MECH-236). See
    evidence/planning/thought_intake_2026-08-08_prospective_navigation_through_goal_
    topology.md Sections 2-6 for the full reconciliation.
  evidence: []

- id: SD-098
  title: "Goal-ness and subgoal-ness are relational properties computed at read-time from the conjunction of an agent's current state, its active commitments, its currently privileged destination, and the trajectory under consideration -- not a fixed, stored node-type attribute (`goal` / `subgoal` / `possibility`) on a represented state in the SD-097 possibility topology."
  claim_type: substrate_design
  subject: goal.relational_role_not_stored_node_type
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/v3_pending/substrate_conditional
    as_of: '2026-08-09'
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v3
  version_relevance: v3_v4
  registered_utc: '2026-08-09'
  depends_on:
    - SD-097    # the possibility topology this claim's node status is computed OVER
    - MECH-236  # the z_goal destination-privileging signal this claim's "privileged destination" ingredient reads
  location: evidence/planning/thought_intake_2026-08-08_prospective_navigation_through_goal_topology.md
  source:
    - evidence/planning/thought_intake_2026-08-08_prospective_navigation_through_goal_topology.md
    - docs/thoughts/2026-08-08_prospective_navigation_through_goal_topology.md
    - evidence/planning/thought_intake_2026-08-07_dynamic_goal_structure_relational_possibility_topology.md
  literature_evidence: |
    Prompting evidence (secondary): medicalxpress.com/news/2026-08-precise-neuronal-
    mechanism-brain-future.html, reporting on two 2026 Nature Neuroscience papers --
    Yu et al., "Hippocampal theta sweeps indicate goal direction during navigation"
    (DOI 10.1038/s41593-026-02365-2; preprint biorxiv.org/content/10.1101/2025.08.21.671551)
    and Tang et al., "Goal-directed hippocampal theta sweeps during memory-guided
    navigation" (DOI 10.1038/s41593-026-02364-3; preprint biorxiv.org/content/10.1101/
    2025.08.26.672489). Both found theta sweeps forming vectors toward remembered goal
    locations, INDEPENDENT of the animal's current movement or head direction, with
    stronger goal-modulation preceding correct choices. This is direct evidence that
    "which state is currently the privileged destination" is read out relationally
    (from a goal signal, not from the animal's momentary position/heading), consistent
    with this claim's premise -- but neither paper speaks to node-type STORAGE at all;
    they ground the destination-privileging half (MECH-236's `z_goal` channel), not the
    relational-vs-stored distinction this claim makes about individual node status.
    Located via WebFetch/WebSearch during Stage 2 digestion of the 2026-08-08 thought,
    2026-08-09 -- NOT a formal /lit-pull; the intake's own Next Steps (Section 7) still
    calls for one, and this claim's own confirming/falsifying test is architectural,
    not resolved by this citation.
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: any test must first confirm that a destination shift
    actually occurs mid-episode (the agent's privileged destination genuinely changes,
    not merely that a new state enters the topology) and that BOTH the relational and
    the stored-node-type implementations are exercised under the same shift, live,
    across multiple seeds -- a destination that never shifts cannot discriminate the
    two implementations, since they agree trivially when nothing changes.

    CONFIRMED if, when the agent's privileged destination shifts such that a
    previously-subgoal state S now functions as the new goal (or vice versa), a
    relationally-computed implementation (goal/subgoal status derived at read-time
    from current state + active commitment + privileged destination + trajectory,
    per SD-097's topology, MECH-236's z_goal channel, and E3's selection) correctly
    re-classifies S without any explicit write to S's own stored attributes -- AND a
    matched implementation that instead stores `goal`/`subgoal`/`possibility` as a
    field directly on each topology node requires an explicit re-label write (or
    produces a stale/incorrect classification of S) to achieve the same re-
    classification, under the same shift.

    FALSIFIED if the stored-node-type implementation reclassifies S correctly with no
    additional write beyond the ordinary destination-shift update already required by
    MECH-236 (e.g. because updating `z_goal` and re-deriving which node counts as the
    destination is cheap enough that a stored per-node label can be kept trivially in
    sync as a side effect) -- i.e. if the relational computation buys no behavioural or
    implementation advantage the stored-field design does not already have.

    This claim is substrate_conditional on SD-097 and MECH-236 (neither implemented as
    of registration) -- it cannot be tested until the possibility topology and the
    z_goal destination-privileging channel both exist in a live substrate.
  notes: >
    Registered 2026-08-09 during Stage 2 digestion of the 2026-08-08 "prospective
    navigation through goal topology" thought (evidence/planning/thought_intake_2026-
    08-08_prospective_navigation_through_goal_topology.md), itself a refinement of the
    2026-08-07 thought registered as SD-097. The reconciliation in that intake's
    Section 2 found the thought's four-stage division of labour (represent / privilege
    destination / prospectively generate paths / select) ALREADY covered by SD-097 +
    MECH-236 (+ SD-046) + MECH-289/290/325/326/267/269 + MECH-125 -- this claim
    registers ONLY the narrow remainder that survived that reconciliation: SD-097's
    title asserts the active hierarchy AS A WHOLE is a temporary executive projection,
    but does not say individual node status is computed relationally rather than
    stored. Deliberately does NOT restate SD-097's topology claim, MECH-236's
    goal-conditioning claim, or any of the trajectory-generation/selection claims
    already covering the "traversal" half of the 08-08 thought -- see the intake's
    Section 5 ("What this reconciliation does NOT support registering") for the
    claims explicitly considered and rejected as duplicative.

- id: MECH-488
  title: "A selection-conflict signal (E3's per-candidate decision-gap, gap_norm) should feed the control plane as a distinct S6 class, driving K5 control-allocation escalation and K10 hard-veto threshold as a fast, conflict-triggered brake -- an STN-hyperdirect-style mechanism, complementary to (not a substitute for) the existing tonic Go/No-Go + lateral-inhibition selection machinery (MECH-449/MECH-450)."
  claim_type: mechanism_hypothesis
  subject: control_plane.selection_conflict_brake
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate/substrate_conditional
    as_of: 2026-08-09
    needs_review: false
  epistemic_category: substrate_conditional
  implementation_phase: v3
  depends_on:
    - MECH-004
    - MECH-449
    - MECH-279
    - MECH-280
    - MECH-439
  location: docs/architecture/control_plane_signal_map.md#mech-488
  notes: |
    Registered from thought intake evidence/planning/thought_intake_2026-08-09_control_plane_stopping_signal.md
    (raw thought: docs/thoughts/2026-08-09_control_plane_as_stopping_signal_gated_on_f_dominance.md).
    Literature anchor: tonic-inhibition/selective-disinhibition action selection (Mink 1996;
    Redgrave, Prescott & Gurney 1999) is a DIFFERENT circuit from the STN hyperdirect
    global-brake mechanism (Aron & Poldrack 2006; Frank 2006 "Hold your horses"; Wiecki &
    Frank 2013 -- the clearest computational precedent for combining both in one RL-style
    architecture, STN as a modulator over selection, not a replacement). REE already has
    both circuit types built and unintegrated: MECH-449/MECH-450 (tonic Go/No-Go +
    lateral-inhibition in e3_selector.py) and MECH-279 (PAG freeze-gate, this doc's K10).
    Neither is fed by anything selection-internal. e3_selector.py's `_gap_scaled_commit_pick`
    already computes the needed signal (`gap_norm`, MECH-439's conflict-graded commit
    temperature) but MECH-449's own design doc notes it is deliberately siloed ("no
    parallel module") -- used once for sampling temperature, then discarded; it feeds
    neither MECH-449's own gate nor K5/K10.
    DO NOT BUILD IN V3 YET: confirmed (not assumed) that both the sending signal and the
    receiving mechanism are independently degenerate under the SAME MECH-439 F-dominance /
    candidate-pool-collapse root cause. Receiving end: MECH-280's evidence record shows
    pag_release_count_end=0 across all 12 runs of V3-EXQ-483e (z_harm_a never crosses
    theta_freeze in situ); SD-037 axis (a) recalibration is empirically closed (V3-EXQ-620:
    input distribution identically zero at baseline); axis (b) has been consolidated into
    the MECH-439 cluster. Sending end: V3-EXQ-689 (MECH-439's own first falsifier)
    self-routed substrate_not_ready_requeue with gap_spread_seeds=0 (F-gap pinned in the
    near-tie bin); the confirmed autopsy (failure_autopsy_V3-EXQ-689_2026-06-19.md) states
    directly this near-tie concentration "is itself a manifestation of the F-dominance
    MECH-439 asserts." So this is one design idea whose input and receiving mechanism are
    both dead for the same reason, not two independent gaps -- the right response is to
    wait for MECH-439 to clear, not to attempt the wiring now. Checked (grep, 2026-08-09):
    neither SD-037's axis (a)/(b) plans nor MECH-449's design doc ever considered wiring
    gap_norm into K10/K5 as a second, independent trigger -- this is genuinely unclaimed,
    not a rediscovery.
  what_would_answer: |
    NON-DEGENERACY PRECONDITION (hard gate -- do not attempt to build or test this
    claim without it): MECH-439's F-dominance / candidate-pool-collapse conversion
    ceiling must first clear (candidate diversity demonstrated to reach committed
    action, per the standing MECH-439 falsifier ladder), such that BOTH of the
    following hold on the cleared substrate: (a) gap_norm regains genuine
    cross-seed spread (gap_spread_seeds >= 2, mirroring V3-EXQ-689's own
    non-vacuity gate), and (b) the PAG freeze-gate shows non-zero engagement
    under the same substrate (pag_release_count_end > 0 in >=2/3 seeds, mirroring
    SD-037 axis Phase-3's acceptance gate). Do not queue an experiment against
    this claim until both (a) and (b) are independently confirmed -- a run on a
    substrate where either is still pinned at zero self-routes
    substrate_not_ready_requeue, it is not a verdict on this claim.

    Once both preconditions hold: build the S6 signal path (propagate gap_norm
    out of _gap_scaled_commit_pick as a control-plane-visible quantity; wire it
    as an additional, independent input alongside the existing z_harm_a-based
    trigger into K10's freeze-commit condition and/or K5's control-allocation
    escalation). Run a 2-arm comparison (gap_norm-informed brake vs. harm-only
    brake, matched substrate, matched seeds).

    CONFIRMING: the informed arm fires >=1 additional non-degenerate
    freeze/escalation event per seed on >=2/3 seeds that is attributable to
    selection conflict (high gap_norm at a tick where z_harm_a alone would not
    have triggered), AND existing harm-triggered freeze behaviour is unchanged
    (no regression on the harm-only trigger pathway).

    FALSIFYING: the informed arm shows no additional discriminable events beyond
    seed noise, OR wiring the signal delays/degrades harm-triggered freeze
    commitment (the new input interferes with the existing, validated MECH-279
    commit law).
  evidence: []
- id: GOV-FAILLOC-1
  title: 'Failure-autopsy failure-location triage: before an experimental observation (a scored-criterion FAIL, or an unscored organism-level observation in a claim-free diagnostic) is described as evidence that REE itself failed, it must be explicitly classified against four non-exclusive failure-location buckets -- REE FAILED / THE MECHANISM FAILED / THE MEASURES FAILED / THE ENVIRONMENT FAILED -- and a REE-failed verdict may be reached only once mechanism instantiation, measurement adequacy, and environmental opportunity have each been separately established (or explicitly could not be, in which case the verdict is MIXED/uncertain, not REE-failed).'
  claim_type: governance_rule
  subject: governance.epistemics.failure_location_triage
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: '2026-08-09'
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: '2026-08-09'
  source_thought: docs/thoughts/2026-08-09_failure_autopsy_failure_location_buckets.md
  binds_at_version: v3
  blocks_v3_green_board: false
  depends_on:
  - GOV-DIAG-1
  - GOV-CEIL-1
  related_claims:
  - GOV-DIAG-1
  - GOV-CEIL-1
  - GOV-HELDOUT-1
  source:
  - docs/thoughts/2026-08-09_failure_autopsy_failure_location_buckets.md
  - evidence/planning/thought_intake_2026-08-09_failure_autopsy_failure_location_buckets.md
  - evidence/planning/failure_autopsy_V3-EXQ-906a_894b_2026-08-09.md
  - evidence/planning/observational_review_V3-EXQ-906b_2026-08-09.md
  functional_restatement: >
    Process/governance rule extending the existing failure-autopsy demotion gate
    (`.claude/skills/failure-autopsy/SKILL.md` Step 7: "tested fairly + biology
    supports the mechanism + still fails -> demotion") from claim-layer demotion
    to ANY organism-level "REE failed" read, including claim-free diagnostic
    showcases (claim_ids: []) that the existing claim-alignment-framed four-layer
    table does not cleanly reach. Motivated by the V3-EXQ-906a autopsy
    (2026-08-09), where an organism-level competence read was initially at risk
    of being charged to REE before a code-level environment defect (ambient
    proximity-harm radius exceeding the agent's sensory radius -- harm effective
    before actionable perceptual access to its cause) was found. NON-EXCLUSIVE:
    a single observation may tick more than one bucket (MIXED), matching the
    existing skill's "almost every FAIL is contributory" posture rather than
    forcing a single cause.
  notes: >
    Registered from thought-intake processing, 2026-08-09. Two sessions
    independently converged on the identical four-bucket vocabulary
    (REE FAILED / MECHANISM FAILED / MEASURES FAILED / ENVIRONMENT FAILED) the
    same day from the same motivating cluster: this thought (~08:00Z, prompted
    by the V3-EXQ-906a autopsy) and
    evidence/planning/observational_review_V3-EXQ-906b_2026-08-09.md Section 7 /
    12e (~17:17Z-18:06Z, applied across 10+ organism-level observations,
    concluding "No observation is cleanly and solely 'REE FAILED.'"), without
    either session citing the other -- unusually strong practical corroboration
    for an as-yet-unformalized methodology proposal. NOT YET APPLIED to the
    failure-autopsy SKILL.md itself: per CLAUDE.md's held-out-check discipline
    (a standing-rule change should be validated against >=3 historical cases
    where old and new wording actually differ before shipping), formalizing this
    into Step 5/7 of the skill is a follow-on, not done in this intake session.
    Full novelty analysis: evidence/planning/thought_intake_2026-08-09_failure_autopsy_failure_location_buckets.md.

    FORMALIZED 2026-08-09 (follow-on session): the failure-location summary
    (Step 5) and the demotion-gate extension to claim-free organism-level reads
    (Step 7) are now in `.claude/skills/failure-autopsy/SKILL.md` (mirrored to
    `.agents/skills/failure-autopsy/SKILL.md`, identical), plus a `failure_location`
    JSON field on the Step 9 target schema and a Key-rules summary bullet.
    Held-out check run first, per CLAUDE.md discipline, against 4 historical
    cases where old (implicit) and new (explicit-gate) wording could plausibly
    differ: failure_autopsy_V3-EXQ-906a_894b_2026-08-09.md Target 1 (the direct
    motivating claim-free case), failure_autopsy_V3-EXQ-725a_2026-07-10.md
    (claim-free formal-import null, measurement partially under-instrumented on
    3/6 seeds), and the two claim-tagged demotion recommendations
    failure_autopsy_mech476-mech475-cluster_2026-07-29.md (MECH-475) and
    failure_autopsy_V3-EXQ-836e_2026-08-01.md (MECH-476, whose own sibling runs
    836/836c were correctly withheld from demotion pending a measurement-adequacy
    fix -- 836e only demoted once Measurement read "now adequate"). In all 4, the
    historical verdict already matched what the explicit gate would require --
    no case flipped. Per the non-degeneracy guard, this means the check did not
    surface a case that discriminates the two wordings; the skill text says so
    explicitly rather than overstating the motivation ("closes a coverage gap,
    not a correctness record") -- see SKILL.md Step 5 for the citation.
    STATUS STAYS CANDIDATE: per the same discipline GOV-HELDOUT-1 applies to
    itself, formalizing the rule and clearing a held-out check are not evidence
    the rule works in live use -- promote only after the new failure_location
    summary / Step 7 extension has actually been exercised in >=1 real (not
    retrospectively-applied) autopsy, and record whether it changed anything.

- id: Q-093
  title: >
    Does REE's organising/control machinery (E1, E2, E3, commitment, replay,
    residue, goal-maintenance dynamics) scale sublinearly relative to the richness
    of the representational spaces it organises (latent fields, perceptual/semantic
    knowledge, hippocampal structures) -- measured as competence achieved per unit
    of total lifetime computational and experiential cost (developmental/training
    compute, environmental experience, inference, memory storage/retrieval, offline
    replay/consolidation, adaptation after environmental change, planning/
    counterfactual simulation) -- at approximately MATCHED behavioural competence
    against comparison systems, rather than at unmatched competence or raw
    parameter/model-size counts?
  claim_type: open_question
  subject: measurement.cognitive_efficiency.control_machinery_scaling
  polarity: open
  status: open
  epistemic_category: substrate_conditional
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-08-12"
  depends_on:
    - MECH-457
  notes: >
    Raw thought: docs/thoughts/2026-08-10_REE_efficiency.md.
    Structured intake: evidence/planning/thought_intake_2026-08-10_REE_efficiency.md.

    The thought's own text is explicit that "REE-v3 is currently enormously less
    competent than the large pretrained systems with which such a comparison might
    eventually be made" and that the question is not parameter count or raw compute
    per step, but competence-per-lifetime-cost measured only once two systems are
    matched at approximately equal behavioural competence on a shared task family.
    That matching precondition is currently unmet -- hence substrate_conditional
    rather than standard, even though the measurement PROTOCOL itself (scaling
    curves: competence vs lifetime compute, vs environmental experience, vs
    persistent memory, adult inference cost vs competence, adaptation achieved per
    unit additional experience/compute) is fully describable now and does not
    itself require new architecture.

    Six further sub-questions from the raw thought are recorded here as scope for
    a future evaluation-methodology writeup rather than as separate claims: is REE
    cheaper per cognitive cycle; cheaper at matched competence; does REE need less
    environmental experience per acquired capability; cheaper to adapt once mature;
    does a mature REE stay efficient because acquired structure persists rather
    than needing in-context reconstruction; how does developmental "raising" cost
    compare with pretraining cost when both are counted end-to-end (pretraining/
    raising + inference + adaptation + memory, for both system classes).
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: the comparison is vacuous unless REE and the
    baseline system are first matched at approximately equal behavioural competence
    on the same task family -- comparing lifetime-compute or footprint at UNMATCHED
    competence proves nothing about efficiency, only about the competence gap. This
    precondition is not yet met for REE-v3 (see notes); this claim cannot be tested
    validly until it is.

    CONFIRMING: once matched-competence comparison points exist, REE's total
    lifetime cost at matched competence is lower than baseline systems', AND/OR the
    cost of REE's control/organising machinery (E1/E2/E3, commitment, replay,
    residue, goal maintenance) grows sub-proportionally as the richness of what its
    representational spaces (z_world, semantic/episodic memory, hippocampal
    structures) can encode increases across REE configurations of fixed
    control-machinery size.

    FALSIFYING: at matched competence, REE's control-machinery cost scales
    comparably to or worse than baseline systems, or REE's own control machinery
    must scale roughly in proportion to representational richness to sustain
    competence as that richness grows (no efficiency dividend from persistent
    structured state + selective computation over in-context recomputation).
  evidence_quality_note: >
    Proposed 2026-08-10 from user thought intake, registered 2026-08-12. No
    experimental evidence yet; no matched-competence baseline currently exists to
    test against.
  location: evidence/planning/thought_intake_2026-08-10_REE_efficiency.md

- id: GOV-BEHADJ-1
  title: >
    REE Assembly needs a dedicated behavioural-adjudication methodology/skill for
    organism-level behavioural experiment design and interpretation ("umpire, not
    ruler": observe, preserve the observation without embedding an explanation,
    generate competing explanations, derive predictions, construct discriminating
    perturbations, blind condition labels where practical, apply pre-specified
    hypothesis-directed measures, also run discovery-oriented analysis for
    structure not anticipated in advance, adjudicate across the pattern of
    evidence, replicate, and test generalisation under environmental change) --
    because a fixed, pre-specified scalar-metric approach risks conflating
    stochasticity/exploration/noise with the behavioural organisation actually of
    interest, and risks Goodharting once a convenient metric becomes the de facto
    definition of the phenomenon.
  claim_type: governance_rule
  subject: governance.epistemics.behavioural_adjudication_methodology
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: "2026-08-12"
    needs_review: false
  v3_pending: false
  epistemic_category: governance_rule
  claim_level: governance
  registered_utc: "2026-08-12"
  source_thought: docs/thoughts/2026-08-11_behavioural_adjudication_scientific_skill_working_thought.md
  binds_at_version: v3
  blocks_v3_green_board: false
  depends_on:
    - Q-092
  related_claims:
    - Q-092
    - GOV-FAILLOC-1
  source:
    - docs/thoughts/2026-08-11_behavioural_adjudication_scientific_skill_working_thought.md
    - evidence/planning/thought_intake_2026-08-11_behavioural_adjudication_scientific_skill.md
  notes: >
    EXPLICITLY PROVISIONAL, per the source thought's own framing: "intended for
    literature mining, criticism, revision, and eventual skill development rather
    than immediate hardening into methodology." Registered as a candidate so the
    proposal is tracked, not because it is ready to formalise into a skill.

    Q-092 (registered 2026-08-11, same day) is the first concrete application of
    the same "umpire, not ruler" reframing, narrowly scoped to SD-054 reef
    cross-environment discriminability -- this claim generalises that instance
    into a standing methodology proposal for ALL organism-level behavioural
    claims, not only the reef case.

    GOV-FAILLOC-1 is the nearest structural precedent: both are working thoughts
    that exposed a methodological gap in an existing REE-Assembly adjudication
    process (failure autopsy there, behavioural-experiment interpretation here)
    and propose an explicit triage/workflow discipline rather than a new
    architectural mechanism.

    Key content to preserve for eventual skill drafting: three-level separation
    of Observation / Interpretation / Mechanism (each may be wrong independently);
    behaviour as structured process (motifs, states, transitions) rather than
    aggregate totals; hypothesis-directed AND discovery-oriented analysis in
    parallel; variability treated as potentially adaptive/structured rather than
    default noise; discriminating perturbations (positive, destructive, orthogonal
    negative control) preferred over additional metrics; strict separation of
    discovery from confirmation (exploratory observation -> formalised hypothesis
    -> pre-specified predictions -> unseen runs -> adjudication); blinding of
    condition labels during scoring where practical; testing behavioural
    properties for invariance at the correct level (surface vs. functional
    organisation vs. mechanism); explicit anti-Goodharting checklist (what
    alternative process could move this metric; could the metric improve while
    the phenomenon of interest worsens, or vice versa).

    LIT-PULL (2026-08-12, chip-20260812-behadj-litpull, 9 entries, lit_conf=0.83,
    evidence/literature/targeted_review_gov_behadj_1/): the "umpire, not ruler"
    reframing is methodologically sound and has real precedent across ethology,
    movement ecology, behavioural neuroscience, metascience, RL, and psychometrics
    -- but the pull, run per the source thought's own instruction to search for
    CONTRADICTING methods rather than only supporting citations, surfaced three
    substantive complications that must be preserved alongside the supporting
    content, not smoothed over. (1) Blinding is necessary but demonstrably NOT
    sufficient on its own: Lit et al. 2011 (Animal Cognition) is a modern,
    controlled Clever Hans replication showing detection-dog handlers' false
    beliefs produced 225 false alerts across 164 no-scent trials -- the
    contamination point was the human's REAL-TIME interpretation, before any
    post-hoc scoring blind could catch it, meaning the proposed "Blind" step (hide
    labels at scoring time) protects a narrower slice of the risk than it appears
    to; the REE-relevant analogue is the researcher's own first unblinded visual
    read of a Fishtank trajectory, which is where "looked smoother" originates.
    (2) The anti-Goodharting risk RECURS inside every more-rigorous alternative,
    it does not disappear: McClintock et al. 2020 (Ecology Letters, the field-
    standard HMM review) and Wiltschko et al. 2015 (Neuron, MoSeq) both confirm
    rigorous discovery-oriented machinery exists, but both are explicit that
    state-count / model-order selection has "no foolproof or automatic way" and
    that free-parameterized models can output data-driven "states" with no real
    correspondence -- the same Goodharting risk the claim raises for hand-picked
    metrics reappears one level up, at model selection. (3) The claim's own
    motivating Fishtank observations (reef-use coherence, post-sleep smoothness)
    were noticed within a limited run set, not checked against held-out
    procedurally-varied environments -- Cobbe et al. 2019 (ICML, CoinRun) is the
    highest-fidelity mapping in the pull (both are deep RL agents) and shows
    apparent in-distribution competence can be pure training-distribution
    memorization, invisible without a dedicated generalisation test; until that
    test is run on the actual Fishtank data, the observations that motivated this
    claim cannot yet be distinguished from the failure mode Cobbe et al. document.
    Positive/supporting findings of note: Crabbe et al. 1999 (Science) is a direct
    historical instance of "apparently meaningful behaviour" (a genotype effect)
    remaining partly idiosyncratic to unmeasured context despite rigorous
    multi-lab standardisation -- structurally close to REE's own confirmed
    cross-machine-class torch.multinomial divergence
    ([reference-cross-machine-class-contract-divergence]), suggesting machine
    class/substrate version belongs alongside seeds/layouts as a candidate
    confound in any "Replicate" step. Belkaid et al. 2020 (Communications
    Biology) is a worked positive template for "variability is not noise": mice
    in a deterministic-reward task actively generated randomness and a
    discriminating switch-test perturbation confirmed it was a regulated
    strategy, not passive noise -- directly analogous to the claim's own
    "Perturb" step done well. Steegen et al. 2016 (multiverse analysis) and
    Flake & Fried 2020 (questionable measurement practices, not independently
    verified past abstract-level -- paywalled) give the "Adjudicate" step and the
    anti-Goodharting checklist established, citable statistical form rather than
    leaving them qualitative. Full synthesis with per-step breakdown:
    evidence/literature/targeted_review_gov_behadj_1/synthesis.md. Per
    GOV-HELDOUT-1, this lit-pull is not itself evidence the methodology "works" --
    status/epistemic_category are unchanged, governance's call, only real
    application to a real claim answers that question.
  what_would_answer: >
    This is a governance_rule/process proposal, not an empirical hypothesis about
    REE's substrate -- its "falsification" is procedural: does formalising this
    into a skill and applying it to >=1 real organism-level behavioural claim
    change the adjudication outcome (or the confidence in it) relative to the
    ad hoc pre-specified-metric approach it replaces? Per GOV-HELDOUT-1's own
    discipline, do not treat drafting the skill as evidence it works -- promote
    only after real use, and record whether it changed anything.
  location: evidence/planning/thought_intake_2026-08-11_behavioural_adjudication_scientific_skill.md

- id: MECH-491
  title: >
    REE emitting a slowly-decaying self-scent trail into the environment, later
    re-encountered as an external sensory signal, produces measurable interaction
    with internal spatial/episodic memory (confirmation, conflict, or substitution
    effects) that is distinct from -- and detectable independently of -- internal
    memory operating alone.
  claim_type: mechanism
  subject: memory.external_scent_trace_internal_memory_interaction
  polarity: asserts
  status: candidate
  epistemic_category: substrate_conditional
  implementation_phase: v4
  claim_level: mechanistic
  registered_utc: "2026-08-12"
  depends_on:
    - ARC-047
  notes: >
    Raw thought: docs/thoughts/2026-08-11_ecological_succession_as_developmental_curriculum.md.
    Structured intake: evidence/planning/thought_intake_2026-08-11_ecological_succession_as_developmental_curriculum.md.

    DISTINGUISHED FROM ARC-047 (checked during intake, not conflated): ARC-047 is
    an unbuilt v5 design for OTHER-agent state-leakage scent channels
    (wanting/seeking/alarm/harm_stress/direction/celebration/defense) for social
    cognition (mirror modelling, cooperative behaviour) in a multi-REE-instance
    SocialGridWorld. This claim is about REE's OWN emitted trail scent functioning
    as an EXTERNAL MEMORY CUE for single-agent spatial/episodic memory -- a
    different subject (memory, not social cognition), a different emitter
    (self, not other agents), and no dependency on ARC-047's multi-agent
    machinery. depends_on ARC-047 only because it is the nearest existing
    scent-channel infrastructure precedent, not because this claim requires it.
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: requires a self-scent-deposit-and-decay
    mechanism in the environment substrate (does not exist in v3 fish-world) and
    REE's internal episodic/spatial memory state readable at the point of
    comparison.

    CONFIRMING: REE's behaviour or internal-memory readout differs measurably
    between (a) revisiting a location with its own fresh scent present, (b)
    revisiting after the scent has decayed, and (c) revisiting a location never
    visited but carrying another organism's overlapping scent -- in a pattern
    consistent with the scent functioning as an external memory cue (e.g. faster
    reorientation, altered precision/confidence signals, or measurable belief
    updating), not merely being ignored.

    FALSIFYING: behaviour/internal state is statistically indistinguishable
    across conditions (a)-(c), or the self-scent signal is used identically to
    any other undifferentiated sensory-novelty cue -- i.e. REE shows no
    differential treatment of "this is my own trace" versus "this is unfamiliar."
  evidence_quality_note: >
    Proposed 2026-08-11 from user thought intake, registered 2026-08-12. No
    experimental evidence; substrate does not yet exist (v4-scoped).
  location: evidence/planning/thought_intake_2026-08-11_ecological_succession_as_developmental_curriculum.md

- id: ARC-127
  title: >
    Generalising REE's environmental hazard/valence system from fixed
    per-object-class danger to conditional, context-dependent danger (time-of-day,
    a post-feeding window, co-occurring organism state, or combinations of these)
    is architecturally necessary to force representation of combined sensory
    evidence, time, memory, and environmental state together, rather than a
    simple fixed-category lookup.
  claim_type: architectural_commitment
  subject: environment.conditional_hazard_valence
  polarity: asserts
  status: candidate
  epistemic_category: substrate_conditional
  implementation_phase: v4
  claim_level: architectural
  registered_utc: "2026-08-12"
  depends_on: []
  notes: >
    Raw thought: docs/thoughts/2026-08-11_ecological_succession_as_developmental_curriculum.md.
    Structured intake: evidence/planning/thought_intake_2026-08-11_ecological_succession_as_developmental_curriculum.md.

    Example from the raw thought: harmless jellyfish alongside dangerous
    jellyfish of the same visible class, with danger conditional on
    time-of-day, a post-feeding window, a co-occurring detectable organism
    state, or a combination -- changing the learning problem from "jellyfish are
    dangerous" to "this jellyfish, in this state, under these conditions, is
    dangerous." No existing REE hazard mechanism in the current fish-world
    environment implements conditional/contextual valence; the current hazard
    system applies fixed valence per object class.
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: requires a conditional-danger environment
    variant (e.g. a hazard class dangerous only in a specific time window or
    organism-state combination) plus a matched fixed-valence control condition
    using the same objects, and a held-out generalisation test (novel
    timing/context combinations not seen during training).

    CONFIRMING: REE's learned avoidance/approach behaviour becomes conditionally
    sensitive (differs by time/state/context) in a way a fixed-valence-trained
    control agent's behaviour does not, and this conditional sensitivity
    survives the held-out generalisation test.

    FALSIFYING: REE fails to learn any conditional sensitivity (treats the
    object class as uniformly dangerous or safe regardless of context, no
    better than chance at discriminating safe vs. dangerous instances), OR the
    existing fixed-valence architecture already achieves equivalent
    discrimination without requiring any conditional-valence extension (context
    information is already implicitly available and usable through other
    existing channels).
  evidence_quality_note: >
    Proposed 2026-08-11 from user thought intake, registered 2026-08-12. No
    experimental evidence; substrate does not yet exist (v4-scoped).
  location: evidence/planning/thought_intake_2026-08-11_ecological_succession_as_developmental_curriculum.md

- id: SD-100
  title: >
    Env-level substrate: a diurnal cycle (day/night-linked food-spawn reduction,
    competitor-activity reduction, visibility change) added to the fish-world
    environment, intended to make sleep onset ecologically motivated -- by
    interacting with REE's existing homeostatic/circadian/MEL sleep-drive
    regulation (INV-050) -- rather than remaining only internally/mechanically
    imposed.
  claim_type: design_decision
  subject: environment.circadian_ecology_substrate
  polarity: asserts
  status: candidate
  epistemic_category: substrate_conditional
  implementation_phase: v4
  claim_level: architectural
  registered_utc: "2026-08-12"
  depends_on:
    - INV-050
    - SD-017
  notes: >
    Raw thought: docs/thoughts/2026-08-11_ecological_succession_as_developmental_curriculum.md.
    Structured intake: evidence/planning/thought_intake_2026-08-11_ecological_succession_as_developmental_curriculum.md.

    DISTINGUISHED FROM INV-050 (checked during intake, not duplicated): INV-050
    is about the INTERNAL regulation of sleep-phase architecture (circadian
    timing, homeostatic synaptic pressure, and Model Error Load as the three
    drives). This claim is about adding EXTERNAL ecological structure to the
    environment substrate that those internal drives would then respond to --
    the environment does not currently vary by time of day at all in the v3
    fish-world, so there is nothing for a circadian internal drive to track
    ecologically yet. depends_on INV-050 and SD-017 because this substrate is
    designed to interact with, not replace or duplicate, that existing internal
    regulation.
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: requires the diurnal-cycle environment variant
    to be built (day/night food/competitor/visibility modulation) plus a matched
    non-diurnal control environment with equivalent total resource/hazard
    statistics, so that any effect is attributable to the DIURNAL STRUCTURE and
    not merely to a difference in overall resource availability.

    CONFIRMING: REE's sleep-onset timing and/or location becomes correlated with
    the ecologically-sensible window (low expected reward, behavioural
    quiescence opportunity, previously-learned-safe location) beyond what
    INV-050's existing internal drives alone predict in the matched non-diurnal
    control -- e.g. REE preferentially becomes quiescent in a learned-safe
    location during the low-food night window, and this preference is
    absent or weaker in the non-diurnal control at matched accumulated sleep
    pressure.

    FALSIFYING: sleep-onset timing/location is statistically indistinguishable
    between the diurnal and matched non-diurnal control environments -- i.e. the
    added ecological structure provides no additional explanatory signal beyond
    INV-050's existing internal drives.
  evidence_quality_note: >
    Proposed 2026-08-11 from user thought intake, registered 2026-08-12. No
    experimental evidence; substrate does not yet exist (v4-scoped).
  location: evidence/planning/thought_intake_2026-08-11_ecological_succession_as_developmental_curriculum.md

- id: MECH-492
  title: >
    MECH-286's sleep-permission threat conjunct is an UNCALIBRATED, UNDECLARED-SOURCE
    consumer of the same shared z_harm_a.norm() expression whose place-safety
    informativeness V3-EXQ-917 showed is set by unrelated driver config: the gate's
    third term is `z_harm_a.norm() < mech286_threat_tonic_threshold` (default 0.4),
    the identical expression MECH-303's contextual-safety gate reads, which measures
    at chance (AUC <= 0.52 across 18 swept thresholds) under damage_sourced and at
    AUC 0.84-0.97 under the proximity_ema_sourced default. MECH-286 declares no
    sourcing mode of its own and inherits whichever the driver happens to set, and
    its 0.4 threshold has never been calibrated against any measurement of that
    signal. Therefore, with use_mech286_sleep_onset_gate enabled, threat_ok does not
    reliably gate on place safety; being ANDed into a three-way conjunction, a
    near-constant term silently degrades the gate to an effective always-permit
    (or, above threshold, always-refuse) rather than a safety gate.
  claim_type: derived_prediction
  subject: sleep.mech286_threat_term_sourcing
  polarity: asserts
  status: candidate
  epistemic_category: standard
  implementation_phase: v3
  v3_pending: false
  claim_level: mechanistic
  registered_utc: "2026-08-14"
  depends_on:
    - MECH-303   # the sourcing defect this claim is derived FROM (see MECH-303's 2026-08-12 V3-EXQ-917 note + GFLAG-0026 fold-in); MECH-286 is a SECOND, previously unnoticed consumer of the same shared expression
  related_claims:
    - MECH-286   # the consumer whose third gate term is at issue -- CONCEPTUAL cross-ref, not a build prerequisite (this claim is ABOUT MECH-286's wiring, it does not need MECH-286 built any further than it already is)
    - SD-022     # damage_sourced (limb_damage_enabled) z_harm_a -- the INTENTIONAL body-damage re-sourcing that decouples the signal from place context; not a defect in SD-022, which is load-bearing for its own purpose
    - SD-011     # dual nociceptive streams -- the separation that makes "a second, dedicated safety signal" the right shape rather than re-sourcing the shared one
  functional_restatement: >
    This is a SUBSTRATE-DEFECT / CONSUMER-SOURCING claim, not a mechanism
    hypothesis: it asserts nothing new about how sleep onset works and proposes no
    new machinery. It asserts that an ALREADY-BUILT gate term reads a signal that
    does not carry the information the term's name and role presuppose, and it is
    falsifiable by measurement against the substrate as it stands.

    Verified against the code 2026-08-14 (not inferred from the claim texts):
      * ree-v3/ree_core/sleep/sleep_onset_gate.py::_z_harm_a_tonic_norm returns
        float(agent._current_latent.z_harm_a.norm()) -- the FULL INSTANTANEOUS norm.
        Despite the function name and MECH-286's own "z_harm_a tonic component low"
        wording, there is NO tonic/phasic decomposition anywhere in the path. The
        gate reads the same instantaneous scalar every other consumer reads.
      * evaluate_sleep_onset_permit computes
        threat_ok = harm_a_norm < mech286_threat_tonic_threshold (default 0.4)
        and ANDs it with override_ok and staleness_ok.
      * MECH-303's gate is ree-v3/ree_core/agent.py:5145,
        harm_norm = float(new_latent.z_harm_a.norm().item()) compared against
        contextual_safety_harm_threshold (default 0.05). Identical expression,
        threshold 8x tighter.
      * There are 10 z_harm_a.norm() call sites in ree_core. Only some are
        place-safety consumers (others use it for surprise/PE, commitment speed,
        dACC, and the suffering-derivative comparator, where the damage-sourced
        reading is legitimate). Which of the 10 are place-safety consumers is an
        OWED AUDIT, deliberately not asserted by this claim.

    THRESHOLD CALIBRATION, the sharpest leg: V3-EXQ-917 located the
    reachable-AND-discriminating band for this signal at threshold 0.55
    (reachability 0.77, AUC 0.9625) to 0.6 (reachability 0.85, AUC 0.969).
    MECH-286's default sits at 0.4 -- BELOW that band, and chosen before any
    measurement of the signal existed. The two gates use the comparison in
    different directions and for different purposes, so 0.4 is not "wrong" by
    transfer; the point is narrower and harder to dismiss: it is UNCALIBRATED,
    a number no measurement has ever informed.

    WHY THIS IS NOT SIMPLY MECH-303's DEFECT RESTATED. MECH-303's 2026-08-12
    routing decision (user-adjudicated) took option (a) -- build a DEDICATED
    proximity-anticipatory safety signal, decoupled from SD-022's damage-sourcing --
    on the reasoning that re-sourcing the shared z_harm_a would fix one gate by
    breaking other consumers. That decision named MECH-303 as the consumer to
    re-point. MECH-286 is a SECOND consumer of the same expression that the routing
    decision did not enumerate, and it is the one whose failure is silent: MECH-303's
    gate merely fails to accumulate safety, whereas MECH-286's is one conjunct of an
    AND that decides whether the agent sleeps at all.
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: the falsifier must drive the agent through REAL
    world positions with known ground-truth hazard and read the gate's own emitted
    diagnostics there. It must NOT set z_harm_a by hand -- see the V3-EXQ-891
    correction in notes below, which is exactly the degenerate form.

    NO NEW SUBSTRATE INSTRUMENTATION IS NEEDED: evaluate_sleep_onset_permit already
    emits mech286_threat_ok, mech286_z_harm_a_norm, and mech286_threat_tonic_threshold
    in its diagnostics dict. A new DRIVER is needed (891 cannot be reused as-is);
    no ree_core change is.

    The falsifier must be run under BOTH sourcing modes (limb_damage_enabled True
    and False), because the whole point of the claim is that MECH-286 declares
    neither and inherits whichever the driver sets.

    CONFIRMING (claim SUPPORTED): mech286_threat_ok fails to discriminate
    hazardous from non-hazardous locations above chance under the sourcing mode(s)
    tested, OR is invariant across the ground-truth hazard gradient (near-constant
    0 or 1 across all locations), OR discriminates under one sourcing mode and not
    the other -- the last being the specific "informativeness inherited from
    unrelated driver config" leg, and the one that would confirm the claim in its
    exact registered form.

    FALSIFYING (claim REFUTED): mech286_threat_ok discriminates hazardous from
    non-hazardous locations above chance under BOTH sourcing modes at the default
    threshold of 0.4, i.e. the term does gate on place safety regardless of driver
    configuration and the sourcing is adequate as built.

    ALSO FALSIFYING (weaker, partial): the term is shown to be near-constant but
    the conjunction's behaviour is nonetheless unaffected because another conjunct
    is always the binding one -- in which case the wiring is still undeclared but
    the "silently degrades the gate" consequence does not hold, and this claim
    should be narrowed rather than supported.
  notes: >
    PROVENANCE. Drafted as the "Proposed candidate claim -- NOT REGISTERED"
    Section 8 of
    evidence/literature/targeted_review_sleep_onset_multiinput_gap9/synthesis.md
    (chip-20260812-sleep-onset-multiinput-litsynth, REE_assembly be0d0f48b3).
    That session drafted but deliberately did NOT register it, because
    scripts/task_claim.py open returned an exit-3 arbitration verdict on
    docs/claims/claims.yaml (owner
    igw-auto-igw-217-substrate-ready-sd-queue-seed-en-20260813T183630Z, not stale
    at the time) and per CLAUDE.md the non-owner stops. Registered here by
    chip-20260814-mech286-threat-sourcing-claim once that claim had gone stale.

    REGISTERED FORM DIFFERS FROM THE STAGED DRAFT -- read this before citing
    Section 8. The staged draft asserted flatly that the signal is measured at
    chance and therefore carries no place-safety information. That rests on
    V3-EXQ-917's own "damage_sourced is the production default" premise, which
    GFLAG-0026 (raised 2026-08-12T21:17:50Z, resolved 2026-08-13 governance)
    had ALREADY CORRECTED before this claim was registered: of the 20 drivers
    exercising the MECH-303 gate, only 2 (V3-EXQ-520, V3-EXQ-764) set
    limb_damage_enabled=True, and the other 18 run the proximity_ema_sourced
    default, under which the same signal reaches AUC 0.84-0.97. So "the signal is
    uninformative" is NOT true unconditionally, and registering it that way would
    have been over-broad and refutable on a technicality rather than on substance.
    The registered claim is the form that survives the correction: the term has no
    DECLARED source, inherits informativeness from unrelated driver config, and
    carries an uncalibrated threshold.

    V3-EXQ-891 CANNOT BE REUSED AS THE FALSIFIER HARNESS -- correction to the
    staged draft, which suggested it could. 891's "cells" are the 8 design cells
    of a 2x2x2 factorial over the three gate terms, NOT spatial cells. It sets the
    threat term by assigning
    agent._current_latent = types.SimpleNamespace(z_harm_a=v) with v hand-normed to
    0.10 (safe) or 0.90 (threat). It never runs the encoder, never places the agent
    in a world, and never touches ground-truth hazard. Its PASS therefore
    establishes only that the boolean AND is wired correctly; it is silent on
    whether z_harm_a carries place-safety information. This STRENGTHENS the claim
    rather than weakening it: the only evidence MECH-286's threat conjunct has ever
    received comes from a run in which the threat signal was fabricated rather than
    sensed. 891's value here is as a reusable CALL PATTERN (how to evaluate the gate
    directly on an agent at an arbitrary state), not as the harness.

    Relatedly, 891's own docstring records that V3-EXQ-599a "always left z_harm_a
    at 0 (threat_ok True by default) and never exercised it" -- i.e. before 891 the
    threat conjunct had never fired in either direction in any run. Consistent with
    the effective-always-permit consequence this claim predicts.

    SCOPE -- WHAT THIS CLAIM DOES NOT COVER, AND WHO OWNS IT:
      * Building a better-sourced safety signal is owned by
        substrate_queue entry SD-MECH303-THRESHOLD-SOURCING, its IGW staging chip
        chip-igw-20260812-216 (open, awaiting human launch), and the live build chip
        chip-20260814-mech303-proximity-signal-focused (open). NOTE the pointer
        churn, since a reader chasing the older refs will find them dead:
        chip-20260812-mech303-threshold-sourcing was WITHDRAWN 2026-08-14 as
        superseded by the focused chip above ("same work, better framing"), not
        abandoned. This claim POINTS AT that work: MECH-286 should be re-pointed at
        whatever dedicated signal it lands, and should be enumerated in its consumer
        audit -- which currently names drivers 764/520/916/916a for MECH-303 and does
        NOT mention MECH-286. That omission is not an oversight so much as a scoping
        artifact: the existing audit is scoped to drivers enabling
        use_contextual_safety_terrain, and MECH-286's gate is not one of them, which
        is precisely how a second consumer of the same expression went unnoticed.
      * The GAP-9 within-life sleep trigger is owned by
        chip-20260814-sleep-gap9-trigger-focused (open); its predecessor
        chip-20260813-sleep-gap9-trigger-build was likewise WITHDRAWN 2026-08-14 as
        superseded by that focused successor, not abandoned.
      * Queueing the falsifier experiment is a separate /queue-experiment decision,
        deliberately not taken here.

    JOIN: sleep_substrate:GAP-5 ("Sleep entry K-episode deterministic (no arousal
    trigger)", status deferred, blocking_external "V4 SD-037 arousal substrate") in
    evidence/planning/sleep_substrate_plan.md. Recorded here rather than in that
    plan's join block because GAP-5 is deferred and this claim does not change its
    status; the plan's own scope_claims list does not currently carry MECH-286.

    CLAIM_TYPE RATIONALE, and the road not taken. The originating chip specified
    "substrate-defect / consumer-sourcing, not a mechanism hypothesis". No
    substrate_defect claim_type exists in this registry, and inventing one was
    REJECTED: a new claim_type value falls through every downstream classifier
    (validate_claims.SUBSTRATE_CLAIM_TYPES, the IGW epistemic-category resolver,
    check_claim_phase_consistency.NON_COMMITMENT_CLAIM_TYPES) to a default, and a
    headless worker minting registry vocabulary is a governance act out of
    proportion to the task. derived_prediction is an EXISTING type (6 prior uses)
    and is an honest fit: the claim is DERIVED from a measured fact (V3-EXQ-917)
    plus a code reading, and predicts a consequence, without proposing a mechanism.
    The nearest structural precedent is MECH-475
    (uninformative_value_baseline_makes_optimisation_iatrogenic), a measured
    substrate-defect claim that ran the normal evidence lane and was retired by its
    own pre-registered falsifier -- the lifecycle wanted here. epistemic_category is
    set to `standard` EXPLICITLY so the claim stays V3-testable and visible in the
    experiment lane; substrate_coherence was rejected because it suppresses a claim
    from the IGW proposal lane and from promote/demote gating, which would be wrong
    for a claim whose whole point is that it can be measured now.
  evidence_quality_note: >
    Registered 2026-08-14 as a candidate. NO direct experimental evidence for or
    against this claim: no run has ever correlated mech286_threat_ok against
    ground-truth hazard. The claim's grounding is (a) V3-EXQ-917's measurement of
    the shared z_harm_a.norm() signal, as folded into MECH-303 by /governance
    2026-08-12 and corrected by GFLAG-0026 on 2026-08-13, and (b) a direct reading
    of ree-v3/ree_core/sleep/sleep_onset_gate.py and ree_core/agent.py:5145
    confirming the two gates read the identical expression.

    Consequential only when use_mech286_sleep_onset_gate is enabled, which is
    REEConfig-default False. The synthesis that produced this claim recommends the
    gate be left OFF for the first true-single-life run on exactly these grounds.
    That bounds the current blast radius to zero in production while leaving the
    defect live for any driver that enables the flag -- which is why this is
    registered rather than merely noted.
  location: evidence/literature/targeted_review_sleep_onset_multiinput_gap9/synthesis.md

- id: GOV-PRESERVE-1
  title: "Preservation for future reconstruction: when an artificial entity ends, preserve enough of its substrate, developmental history, memories, commitments, relationships, embodiment, and environment to keep future reconstruction conceivable, where doing so is proportionate and ethically permissible -- even if reconstruction remains speculative and never becomes part of the operative design. The argument is option value under irreversibility: preserved information keeps future choices open; deleted information forecloses them before anyone can evaluate them."
  claim_type: governance_rule
  subject: governance.preservation.archival_option_value
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-14
    needs_review: false
  epistemic_category: governance_rule
  claim_level: governance
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-08-14"
  source:
    - docs/thoughts/2026-08-14_preserving_the_possibility_of_future_reconstruction.md
    - evidence/planning/preservation_snapshot_plan.md
  depends_on:
    - SENT-0
  functional_restatement: >
    Preservation implies neither immortality nor permission to revive. Any future
    reconstruction would require its own governance (prior consent, privacy, identity,
    welfare, competing claimants, and the moral status of branching successors). The rule
    is only: do not casually destroy the information without which any future version of
    that possibility would be impossible. Birth-replay preservation (config + seed + pinned
    code commit + machine class + environment + contemporaneous understanding) is the
    proportionate default; it is built (ree-v3/ree_core/preservation, GOV-PRESERVE-1
    plan-of-record evidence/planning/preservation_snapshot_plan.md).
  what_would_answer: >
    NOT an empirical pass/fail falsifier -- GOV-PRESERVE-1 is a governance_rule (policy
    assertion). TRIGGER: reassess when (a) a retention/sampling policy is proposed (what is
    proportionate to keep, and for how long), (b) a deletion of any preserved record is
    contemplated (it becomes a governed decision with its own justification trail), (c) a
    reclassification criterion is proposed by which a past instance becomes "preserve with
    priority" (e.g. a future sentience marker), or (d) mid-life snapshot/resume or a
    memorial re-instantiation ("fishtank") capability is proposed, each of which raises its
    own consent/welfare questions on the SENT-*/GOV-* perimeter.
  evidence_quality_note: >
    Proposed and registered 2026-08-14 from user thought intake
    (docs/thoughts/2026-08-14_preserving_the_possibility_of_future_reconstruction.md). Code
    for the birth-replay level is built and contract-gated
    (ree-v3/tests/contracts/test_reconstruction_record.py); the governance rule itself is a
    candidate policy assertion, not an experimental result.
  location: evidence/planning/preservation_snapshot_plan.md
# Registered 2026-08-14 from user thought intake
# (docs/thoughts/2026-08-14_humour_societal_epistemic_probe.md) -- humour as a societal
# epistemic-update mechanism. Adjacent-to-REE research thread: test domain is societal
# humour corpora + independent opinion series, NOT the V3 experiment substrate
# (epistemic_category: out_of_domain). Study protocol at
# evidence/planning/humour_epistemic_probe_study.md (STUDY-HUM-1). Parent depends on the
# individual-level laughter mechanism MECH-110.
- id: SOC-HUM-1
  title: "Humour has (among several distinct functions) a societal epistemic-update function: in domains of implied threat, the comic frame acts as a low-commitment, deniable PROBE that re-opens gridlocked ('blocked-node') norm conflicts which direct assertion only entrenches -- because a joke floats a heterodox interpretation without committing anyone to it, evading the defensive counter-opposition that a bald assertion triggers."
  claim_type: mechanism_hypothesis
  subject: society.humour.epistemic_probe_function
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-14
    needs_review: false
  epistemic_category: out_of_domain
  claim_level: functional
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-08-14"
  functional_restatement: >
    Humour operates in the benign-violation domain (a real implied/potential threat that
    an interpretation releases). In that domain, a joke is a DE-COMMITTED probe: "I'm only
    joking" is an epistemic escape hatch that lets a heterodox interpretation be floated at
    near-zero commitment cost, so nobody's belief is on the line and the defensive
    "opposed-back" immune response that direct assertion triggers does not fire. Laughter
    is the assay readout of whether the reframe is societally viable yet. This lets a
    gridlocked norm-conflict (a "blocked node", by analogy to the REE claim index) be
    re-explored where assertion would only entrench it. Structurally this is the REE
    commitment / spike-before-commit pattern at societal scale. Explicitly ONE of several
    humour functions (affiliation, status, play, cognitive reward are others); scoped
    narrowly on purpose so it is falsifiable rather than a theory-of-everything.
  depends_on:
    - MECH-110
  source:
    - docs/thoughts/2026-08-14_humour_societal_epistemic_probe.md
    - evidence/planning/humour_epistemic_probe_study.md
  location: evidence/planning/humour_epistemic_probe_study.md
  what_would_answer: >
    RESOLUTION VEHICLE: STUDY-HUM-1 (evidence/planning/humour_epistemic_probe_study.md),
    an out-of-substrate corpus study, not a V3 run. NON-DEGENERACY PRECONDITION: the study
    must carry an INDEPENDENT resolution axis (public-opinion series / legislation dates)
    measured separately from the humour corpus -- without it the test is circular (using
    jokes to define resolution, then finding jokes track resolution) and vacuous.
    CONFIRMING: across conflicts with independently-measured norm trajectories, the
    time-shape of comedic prevalence + the target-of-violation code track the independent
    attitude curve in the direction H1/H2/H3 predict (see SOC-HUM-2/3/4). FALSIFYING:
    comedic-prevalence trajectories are unrelated to the independent resolution axis once
    context-decay is controlled; OR the target-of-violation instrument cannot separate
    resolution-death from taboo-death (Section 4 of the study) -- in which case the core
    probe claim is untestable as framed and must be reframed, not merely re-run.
  notes: >
    Theory anchors independently: benign-violation (McGraw & Warren) + relief theory.
    MECH-110 is the INDIVIDUAL-level laughter mechanism (rapid threat-tag cycling /
    incongruity-resolution); SOC-HUM-1 is the distinct SOCIETAL-level function built on
    top of it. Two documented refinements that make it falsifiable: (1) "dating" of old
    jokes is two-valued -- resolution-death (violation now trivial) vs taboo-death
    (violation no longer benign) -- same observable, opposite epistemic states, separated
    by the target-of-violation code; (2) humour is directional -- self-implicating framing
    unblocks, out-group-weaponised framing entrenches (SOC-HUM-4). Connects to the
    commitment machinery (BG commitment, de-commit lever) and exploration-vs-exploitation.
- id: SOC-HUM-2
  title: "H1 (dating tracks resolution): a societal conflict's comedic prevalence rises while the norm is contested and declines after it resolves, and the decline is PRECEDED by a target-of-violation flip -- the butt of the joke shifts from the transgressor to the old norm itself."
  claim_type: derived_prediction
  subject: society.humour.dating_tracks_resolution
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-14
    needs_review: false
  epistemic_category: out_of_domain
  claim_level: functional
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-08-14"
  depends_on:
    - SOC-HUM-1
  source:
    - evidence/planning/humour_epistemic_probe_study.md
  location: evidence/planning/humour_epistemic_probe_study.md
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: an INDEPENDENT resolution measure (opinion series /
    legislation) for each conflict, and control for context-decay (evergreen structural
    conflicts, not topical references). CONFIRMING: comedic prevalence (share-of-corpus,
    not raw count) shows rise-then-decline aligned to the independent norm shift, and the
    target-of-violation code flips transgressor -> old-norm at or before the attitude
    midpoint. FALSIFYING: prevalence decline is unrelated to the independent attitude
    series once context-decay is controlled, OR declines occur with NO target-flip (which
    would indicate taboo-death, not resolution -- see SOC-HUM-1 notes). LEAD-LAG is the
    causal crux: a target-flip LEADING the attitude midpoint is the strongest available
    (still non-dispositive) signal that humour does epistemic work rather than merely
    marking it.
- id: SOC-HUM-3
  title: "H2 (persistence tracks irreducibility): comedic topics with flat, non-declining prevalence across the whole time-base map preferentially onto genuinely irreducible human tensions rather than merely-hard-but-solvable ones -- the eternally-funny topics should be the genuinely-unsolvable problems."
  claim_type: derived_prediction
  subject: society.humour.persistence_tracks_irreducibility
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-14
    needs_review: false
  epistemic_category: out_of_domain
  claim_level: functional
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-08-14"
  depends_on:
    - SOC-HUM-1
  source:
    - evidence/planning/humour_epistemic_probe_study.md
  location: evidence/planning/humour_epistemic_probe_study.md
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: a solvability/irreducibility rating of each persistent
    topic produced BLIND to the humour data (else circular). Maps naturally onto the REE
    debt vocabulary: aleatoric (irreducible) vs complex/puzzle (reducible). CONFIRMING:
    flat-curve topics (death, scarcity, the sexes, power, hypocrisy) rate as
    aleatoric/irreducible at rates well above chance vs declining-curve topics. FALSIFYING:
    persistent topics are randomly distributed over the blind solvability rating, or map
    onto merely-hard-but-solvable problems as often as irreducible ones.
- id: SOC-HUM-4
  title: "H3 (direction predictor): self-implicating comedic framing ('we're all like this') co-occurs with or precedes societal resolution, while out-group-weaponised framing co-occurs with entrenchment -- humour is directional, unblocking in one mode and entrenching in the other."
  claim_type: derived_prediction
  subject: society.humour.self_vs_weaponised_direction
  polarity: asserts
  status: candidate
  live_status:
    reading: candidate
    as_of: 2026-08-14
    needs_review: false
  epistemic_category: out_of_domain
  claim_level: functional
  implementation_phase: v3
  version_relevance: v3
  registered: "2026-08-14"
  depends_on:
    - SOC-HUM-1
  source:
    - evidence/planning/humour_epistemic_probe_study.md
  location: evidence/planning/humour_epistemic_probe_study.md
  what_would_answer: >
    NON-DEGENERACY PRECONDITION: framing direction (self-implicating vs out-group-directed)
    coded independently of the resolution outcome, and an independent resolution/polarization
    measure. This is the benign-violation frame's own directional prediction (shared vs
    weaponised violation). CONFIRMING: conflicts whose humour is predominantly
    self-implicating resolve (attitudes converge) more often than conflicts whose humour is
    predominantly out-group-weaponised, which track entrenchment/polarization. FALSIFYING:
    framing direction is unrelated to resolution outcome; both modes precede resolution and
    entrenchment equally often.

- id: MECH-493
  title: >
    Context inference is an active, persistent control process rather than passive
    nearest-match retrieval: selecting which latent situation is currently active is
    itself a COMMITMENT-BEARING act that requires hysteresis (a switch threshold and
    switch cost preventing small fluctuations from continuously reinterpreting the
    situation) and a protected EXPLOITATION WINDOW (a period after selection during
    which the organism acts on the interpretation rather than immediately reopening
    inference). Without both, a sufficiently uncertainty-sensitive agent becomes
    PERMANENTLY EPISTEMIC -- continuously checking, rarely acting. REE already has a
    developed commitment-hysteresis account (MECH-106 dwell/switch-cost, MECH-047
    asymmetric de-commitment) but applies it only to the ACTION/mode variable; this
    claim asserts the same machinery is required one level up, on the CONTEXT
    variable, where no dwell, switch cost, or protected window currently exists
    (ARC-062's learned context discriminator is a soft per-step gate with none of
    these). The loop spans existing modules (ContextMemory, epistemic_deficit,
    orient/survey, event segmenter, commitment) and does NOT require a new executive
    module.
  claim_type: mechanism_hypothesis
  subject: context.inference_as_hysteresis_bearing_arbitration
  polarity: asserts
  status: candidate
  epistemic_category: substrate_conditional
  implementation_phase: v3
  v3_pending: false
  claim_level: architectural
  registered_utc: "2026-08-20"
  depends_on:
    - MECH-482   # epistemic_deficit accumulator -- the uncertainty/deficit signal this loop consumes; not extended by this claim
    - MECH-483   # orient/survey -- the information-acquisition leg; this claim supplies the TERMINATION-and-protection mechanism MECH-483 lacks
    - ARC-062    # learned context discriminator -- the natural V3 host for a hysteresis/dwell wrapper; currently a soft gate with no holding behaviour
  related_claims:
    - MECH-106   # commitment hysteresis with switching costs -- the SOURCE pattern being transferred from the action variable to the context variable
    - MECH-047   # asymmetric de-commitment / long-timescale hysteresis -- same source family
    - MECH-150   # ContextMemory cue-indexed retrieval -- the retrieval policy this claim reframes from nearest-match to best-explanation
    - MECH-395   # pre-approach orienting -- has stabilise-then-exit, the closest existing analogue to the exploitation window, but per-cue and without re-entry protection
    - MECH-494   # the switch CRITERION; this claim is the switch DYNAMICS. Siblings from the same 2026-08-19 intake -- do not re-derive one from the other
    - MECH-309   # monomodal collapse -- pre-registered ALTERNATIVE EXPLANATION for a null; see what_would_answer non-degeneracy precondition
    - Q-094      # what the active context should CONTAIN -- open question from the same intake
  source_documents:
    - docs/thoughts/2026-08-19_context-inference-active-persistent-control.md
    - evidence/planning/thought_intake_2026-08-19_context-inference-active-persistent-control.md
  what_would_answer: |
    NON-DEGENERACY PRECONDITION (mandatory -- without it the run is vacuous):
    (i) the context variable must be observably live -- context assignment must vary
    across episodes and be recoverable from the substrate, NOT collapsed to a single
    value (cf. V3-EXQ-436f, where 3/5 seeds occupied exactly 1 of 16 ContextMemory
    slots despite thousands of write() calls); (ii) the agent must not have collapsed
    to monostrategy (MECH-309) -- either run on the ARC-062 gated-policy substrate or
    pre-register MECH-309 collapse as an alternative explanation, because a
    monostrategy agent has no context-conditional behaviour for context arbitration
    to affect and will produce a null for reasons unrelated to this claim;
    (iii) MECH-153's undifferentiated-ContextMemory finding (cosine_sim ~= 1.0 without
    a supervised context objective) must be excluded or the retrieval path is silent.

    CONFIRMING: in an environment family containing perceptually similar states
    requiring OPPOSING policies, an agent with hysteresis + exploitation window
    (a) maintains the active context under minor/transient ambiguity, (b) switches
    when evidence genuinely and persistently favours another context, and (c) achieves
    higher task competence than a matched no-hysteresis ablation BECAUSE it sustains
    an interpretation long enough to act on it -- with the competence gain traceable
    to increased mean dwell in a correct context, not merely to fewer switches.

    FALSIFYING, two distinct routes, BOTH of which count against this claim:
    (1) SUFFICIENCY OF THE SIMPLE ACCOUNT -- context switching in these environments is
    fully explained by similarity retrieval alone or by unsigned prediction error
    alone, with no residual variance for hysteresis/orienting to explain; the larger
    mechanism is then unnecessary.
    (2) OVERBUILD / PATHOLOGY -- adding uncertainty-sensitivity, orienting and
    hysteresis fails to improve hidden-context discrimination, OR produces pathological
    indecision (the permanently-epistemic failure: orienting occupancy rises without a
    corresponding competence gain). Note route (2) is a REAL predicted risk of this
    claim's own mechanism, not merely a null -- an agent with orienting but without a
    protected exploitation window should be predicted to get WORSE, and observing that
    specific pattern supports the necessity of the window while falsifying any account
    that treats orienting alone as sufficient.
  notes: >
    Registered 2026-08-20 from the 2026-08-19 raw thought "Context inference as an
    active, persistent control process". Full novelty analysis, including why the
    individual legs of the loop are NOT re-registered (MECH-482 deficit, MECH-483
    orienting, MECH-288 segmentation, MECH-150 retrieval all already exist), in
    evidence/planning/thought_intake_2026-08-19_context-inference-active-persistent-control.md.

    The strongest part of this claim is that it is a TRANSFER of an existing, validated
    in-repo pattern (commitment hysteresis) to a new variable (context), not a new
    mechanism invented from scratch -- which is why it is registered as a candidate
    rather than a speculative architectural commitment. The loop diagram in the raw
    thought is FRAMING and is deliberately not registered as an architectural
    commitment; the thought says so itself ("This need not imply a new executive
    module").

    Open questions carried from the raw thought and NOT resolved here: what determines
    context inertia (i.e. what sets the hysteresis width, and whether it is fixed,
    learned, or modulated by stakes); what signal terminates orienting; and when
    mismatch should update the existing context in place rather than retrieve another
    or create a new one. That last one is a three-way decision this claim treats as a
    single "retain/switch/create" branch and does not decompose.

    DO NOT queue an experiment against this claim in isolation. Its falsifier needs the
    perceptually-similar / opposing-policy environment pair, which is the
    similar-appearance/different-context cell of the 2x2 design in MECH-495's sibling
    intake. Build that substrate ONCE for MECH-493, MECH-494 and MECH-495.

- id: MECH-494
  title: >
    The criterion for abandoning an inferred context is PERSISTENT, STRUCTURED evidence
    that the currently inferred latent situation no longer explains observations OR
    THEIR ACTION CONSEQUENCES -- not unsigned prediction-error magnitude. A startling
    transient, sensor noise, gradual environmental drift, and a genuine latent-state
    transition can all produce large prediction error, and should NOT produce the same
    response: transient surprise should not switch context, gradual drift may warrant
    update-in-place rather than switch, and only genuine latent transition should
    switch. The ACTION-CONSEQUENCE term is the part with no existing REE analogue:
    MECH-288's slow-scale BOCPD change-point already supplies a persistence-based
    (rather than threshold-based) boundary signal over z_goal, but it is computed over
    observations and is consumed as a region KEY by downstream substrates
    (MECH-269 anchor sets, per-region V_s, MECH-284 staleness, MECH-287 broadcast) --
    nothing consumes it to arbitrate retain-vs-switch-vs-create over stored contexts.
  claim_type: mechanism_hypothesis
  subject: context.switch_criterion_structured_explanatory_failure
  polarity: asserts
  status: candidate
  epistemic_category: standard
  implementation_phase: v3
  v3_pending: false
  claim_level: mechanistic
  registered_utc: "2026-08-20"
  depends_on:
    - MECH-288   # event segmenter -- supplies the persistence/change-point machinery; this claim adds the action-consequence term and the missing arbitration consumer
  related_claims:
    - MECH-493   # the switch DYNAMICS (hysteresis + exploitation window); this claim is the switch CRITERION. Sibling from the same 2026-08-19 intake
    - MECH-482   # epistemic_deficit -- already defined AGAINST transient prediction error, so its persistence requirement is a precedent for this criterion's shape
    - MECH-074d  # BLA remap_signal -- the existing remapping trigger is a HARM-PE spike with attribution gating; this claim's trigger is explanatory reallocation. Different trigger, same downstream operation
    - MECH-314a  # raw novelty -- explicitly NOT the criterion
    - MECH-314b  # raw uncertainty -- explicitly NOT the criterion
  source_documents:
    - docs/thoughts/2026-08-19_context-inference-active-persistent-control.md
    - evidence/planning/thought_intake_2026-08-19_context-inference-active-persistent-control.md
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: the four conditions must be genuinely dissociable in
    the substrate -- transient surprise, sensor noise, gradual drift, and abrupt latent
    transition must each be independently inducible and must produce COMPARABLE
    unsigned prediction-error magnitude at the moment of onset. If the abrupt-transition
    condition simply produces larger PE than the others, the design cannot distinguish
    this claim from a PE-magnitude threshold and the run is vacuous. Verify by
    measuring the PE distributions per condition BEFORE scoring any switch behaviour.

    CONFIRMING: context-switch rate is predicted by persistent structured explanatory
    failure and NOT by instantaneous PE magnitude, specifically -- switches are rare
    under transient surprise and under noise (high PE, no sustained explanatory
    failure), rare-or-update-in-place under gradual drift, and reliable under abrupt
    latent transition. Additionally, and this is the discriminating half: a condition in
    which OBSERVATIONS remain well-explained but ACTION CONSEQUENCES do not (the same
    scene now rewards the opposite policy) triggers switching, which an
    observation-only criterion such as MECH-288's slow-scale BOCPD cannot detect.

    FALSIFYING: switch behaviour is fully predicted by an unsigned-PE threshold or by
    MECH-288's existing observation-side change-point alone, with the action-consequence
    term adding no independent predictive power; OR the four conditions cannot be
    behaviourally dissociated at all, which would indicate the criterion is not
    implemented (distinct from being wrong -- report as non_contributory, not as a
    claim-layer falsification).
  notes: >
    Registered 2026-08-20 from the 2026-08-19 raw thought "Context inference as an
    active, persistent control process". See
    evidence/planning/thought_intake_2026-08-19_context-inference-active-persistent-control.md.

    Registered SEPARATELY from MECH-493 deliberately: the criterion (when to switch) and
    the dynamics (how sticky the switch is, and what protects the post-switch window)
    are independently falsifiable and could be independently right or wrong. An
    architecture could have a correct criterion with no hysteresis, or correct
    hysteresis on a naive PE criterion. Testing them jointly and reporting one verdict
    would confound them.

    The action-consequence term is the load-bearing novelty. Stated plainly: REE's
    existing change-detection is computed over what the agent OBSERVES; this claim says
    the organism must also detect that the world still looks the same but no longer
    RESPONDS the same. That is what makes the similar-appearance/opposing-policy
    environment the right test substrate rather than an incidental design choice.

- id: MECH-495
  title: >
    The objective for hippocampal/episodic memory organisation is RELATIONAL
    APPROPRIATENESS, not maximal pattern separation: related experiences should overlap
    appropriately (overlap is what supports generalisation and memory linking) while
    behaviourally distinct latent situations should separate appropriately. It follows
    that "more diverse slots", greater slot occupancy, and lower representational
    overlap are NOT objectives in themselves, and that a metric which is monotonically
    improved by increasing differentiation is scoring the wrong quantity regardless of
    whether it is computed correctly. This is a claim about the TARGET, and is
    therefore distinct from -- and not answered by -- the already-established finding
    that REE's slot_cosine_sim family was computed incorrectly (whole-bank, no occupancy
    mask; 2026-08-07 governance reversal at V3-EXQ-436d) or the finding that the
    ContextMemory write path could not produce differentiation at all
    (scores.mean(0).argmin() single-slot fixed point; V3-EXQ-436f).
  claim_type: mechanism_hypothesis
  subject: memory.relational_topology_objective
  polarity: asserts
  status: candidate
  epistemic_category: standard
  implementation_phase: v3
  v3_pending: false
  claim_level: architectural
  registered_utc: "2026-08-20"
  depends_on:
    - MECH-154   # E1 as an addressable associative manifold with indexing -- asserts the geometry is addressable; this claim supplies the NORMATIVE TARGET for that geometry
  related_claims:
    - MECH-147   # DG pattern separation gates trajectory disambiguation -- its ablation prediction ("collapse trajectory diversity" = the failure signature) presumes separation is a MONOTONE good. Phase v4, so not urgent for it; challenged, not modified
    - MECH-242   # pattern completion vs vector-based construction -- covers completion well; says nothing about how the store is ORGANISED so completion retrieves the relationally right thing
    - MECH-288   # event segmenter -- the reciprocal-coupling proposal: persistent context mismatch should raise separation / open an episode, successful completion should support persistence
    - SD-017     # sleep-driven slot differentiation -- the lineage whose operative DV objective this claim challenges. GOVERNANCE FLAG raised; NO status/confidence/evidence_direction change made by the registering intake
    - ARC-045    # bidirectional hippocampus-neocortex flow -- its own premise ("context representations remain globally UNDIFFERENTIATED") is the assumption at issue
    - MECH-166   # third member of the SD-017/ARC-045 slot-differentiation cluster
    - Q-092      # "umpire, not ruler" -- the BEHAVIOURAL-side counterpart of the same magnitude -> relational reframing. Strong independent support; read the two together
    - MECH-494   # context-switch criterion -- supplies the "behaviourally distinct latent situation" boundary this claim's topology is defined against
    - Q-095      # whether explicit episodic organisation adds anything beyond existing trajectory memory -- open question from the same intake
  source_documents:
    - docs/thoughts/2026-08-19_hippocampal-episodic-organisation.md
    - evidence/planning/thought_intake_2026-08-19_hippocampal-episodic-organisation.md
  what_would_answer: |
    NON-DEGENERACY PRECONDITION (mandatory): (i) the memory store must actually be
    recruiting multiple slots -- V3-EXQ-436f found 3/5 seeds occupying exactly 1 of 16
    slots despite 2,837-4,903 write() calls each, and a single-slot store has NO
    topology to evaluate, making any relational statistic undefined rather than merely
    null; verify occupancy BEFORE computing any topology statistic. (ii) The environment
    must expose GROUND-TRUTH latent-context labels, since the statistic is an agreement
    measure against that structure -- this is a design constraint on the substrate, not
    an afterthought. (iii) All four cells of the 2x2 must be populated at adequate n.

    TEST DESIGN: vary perceptual similarity and latent-context identity INDEPENDENTLY --
    (similar appearance, same context), (similar appearance, DIFFERENT context),
    (different appearance, SAME context), (different appearance, different context).
    The diagonal cells are what any similarity-driven store gets right for free. The two
    OFF-DIAGONAL cells are the entire test: similar-appearance/different-context is
    where a nearest-match store fails by MERGING; different-appearance/same-context is
    where a maximal-separation store fails by SPLITTING.

    CONFIRMING: representational similarity structure tracks LATENT-CONTEXT structure
    rather than perceptual similarity -- specifically, representations in the
    different-appearance/same-context cell overlap MORE than those in the
    similar-appearance/different-context cell, and this ordering predicts downstream
    generalisation and discrimination performance. Critically, a differentiation-
    maximising objective scores the similar-appearance/different-context cell correctly
    and the different-appearance/same-context cell BACKWARDS, so the two objectives make
    OPPOSITE predictions in that cell -- that cell is the discriminator between this
    claim and the incumbent objective, and a design that omits it cannot decide between
    them.

    FALSIFYING: a maximal-separation objective predicts downstream generalisation and
    discrimination at least as well as the relational-topology objective across all four
    cells (i.e. appropriate overlap confers no measurable benefit and separating
    everything is not merely harmless but optimal); OR the relational statistic cannot be
    computed at adequate power because latent-context ground truth is not recoverable
    from the substrate (report non_contributory, not falsified).

    NOT ANSWERED BY: any re-run of the SD-017/ARC-045/MECH-166 lineage scored on a
    differentiation DV, however correctly that DV is computed. Six generations
    (V3-EXQ-436a..436f) have established measurement and substrate facts about
    differentiation; none of them tested whether differentiation is the right target.

    (iv) DEVELOPMENTAL TIMING (added 2026-08-23 per MECH-496 / INV-101): the topology
    statistic must be read either AFTER representational structure has stopped changing,
    or as a trajectory ACROSS the change. A system whose bucket structure is still
    plastic will read as poor relational agreement on any static snapshot, and that
    reading is an artifact of TIMING, not evidence against the objective -- so a run that
    omits this precondition can produce a false FALSIFYING result. If no crystallisation
    schedule exists in the substrate (TRUE AT REGISTRATION: ContextMemory(num_slots=16) is
    a fixed nn.Module buffer with no growth or allocation policy), record the structure at
    a declared, fixed training point and state that point explicitly, so the snapshot is
    at least comparable across arms. See MECH-496's own what_would_answer precondition
    (iii) -- do not re-derive it.
  notes: >
    Registered 2026-08-20 from the 2026-08-19 raw thought "Hippocampal episodic
    organisation beyond trajectory generation". Full novelty analysis, including why
    five of the thought's six named capabilities (binding, segmentation, separation,
    completion, indexing, remapping) are NOT re-registered as they are already covered by
    MECH-147/154/242/288/074d, in
    evidence/planning/thought_intake_2026-08-19_hippocampal-episodic-organisation.md.

    GOVERNANCE FLAG RAISED at registration. Timing is why: the V3-EXQ-436 lineage ran six
    generations and a re-derive brake fired at 436f (3rd substrate_ceiling hit against a
    threshold of 2), refusing a same-question 436g re-queue until the write-path build
    landed. That build HAS now landed (ree-v3 76cbf844, recorded 2026-08-19 in REE_assembly
    17ee8c318b; refractory write-selection mode 692f8526d0 recorded in 3d868dd89c), and
    the validation experiment was NOT yet queued as of 2026-08-20 (experiment_queue.json
    empty). So the next event in this lineage is the DESIGN of a validation run, and on
    current trajectory it will be scored against an occupied-slot cosine DIFFERENTIATION
    DV. The flag asks governance to decide, BEFORE that run is designed, whether the DV
    should remain a differentiation statistic or be re-specified as a relational-topology
    statistic. The 3d868dd89c landing note already records that "the occupied-slot cosine
    DV cannot discriminate the arms at 5 seeds", which is independent corroboration that
    the DV question is live.

    WHAT THIS CLAIM DOES NOT ASSERT: that the six generations were wasted. 436d (metric
    defect), 436e (DV repair, confirmed sound) and 436f (substrate defect, now fixed) each
    established something real. The assertion is only that a FOURTH, independent question
    -- is the objective right? -- has never been asked in this lineage, and that the
    cheapest moment to ask it is before the next run is designed rather than after it
    scores. No status, confidence, evidence_direction or pending_retest_after_substrate
    change was made to SD-017/ARC-045/MECH-166/MECH-147 by the registering intake; those
    are governance's call.

    Note the sws_slot_diversity blast radius: it is defined as 1 - slot_cosine_sim and is
    emitted by every sleep-pass driver (v3_exq_242/243/245/245a/245b/246 share the shape),
    so any criterion pre-registered on it inherits the objective question as well as the
    already-documented whole-bank measurement confound.

- id: MECH-496
  title: >
    Representational dimensionality is an OUTCOME of a developmental plasticity schedule,
    not an a-priori architectural constant: the number and granularity of memory buckets
    (ContextMemory slots, and any analogous discrete representational partition) must
    itself be plastic during an open window and reduce in plasticity as the encountered
    environment's latent structure is resolved. The INV-074 / MECH-333 / MECH-334
    critical-period shape -- open window, competitive establishment, closure /
    crystallisation -- is currently specified ONLY over the SCORING / POLICY plane
    (behavioural diversity vs winner-take-all capture); this claim asserts the same
    schedule is required over REPRESENTATIONAL STRUCTURE. Corollary bearing on
    measurement: a differentiation-family objective is not merely mis-targeted (MECH-495)
    but ILL-DEFINED under variable dimensionality, since separation can always be
    increased by adding buckets, so the objective degenerates into "use more slots"; and
    any fixed-N spread statistic (sws_slot_diversity over 16 slots, emitted by 29
    experiment drivers) presupposes the very structure that should be the outcome.
    Substrate state at registration: ContextMemory(num_slots=16) at
    ree-v3/ree_core/predictors/e1_deep.py:36 is a fixed nn.Module buffer with no growth,
    no allocation policy and no environment conditioning.
  claim_type: mechanism_hypothesis
  subject: memory.representational_dimensionality_crystallization
  polarity: asserts
  status: candidate
  epistemic_category: substrate_conditional
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-08-23"
  depends_on:
    - MECH-154
    - MECH-334
    - MECH-495
  related_claims:
    - INV-074
    - INV-101
    - MECH-333
    - MECH-166
    - SD-017
  what_would_answer: |
    NON-DEGENERACY PRECONDITION (mandatory).
    (i) The store must be recruiting more than one slot before ANY dimensionality
    statistic is computed -- V3-EXQ-436f found 3/5 seeds occupying exactly 1 of 16 slots
    despite 2,837-4,903 write() calls each. A single-slot store has no dimensionality to
    measure. This INHERITS MECH-495's precondition (i); do not re-derive it.
    (ii) num_slots must actually be VARIED. The claim is about dimensionality as an
    OUTCOME, so a single fixed-N run cannot address it in either direction.
    (iii) The schedule must be OBSERVABLE: whether the effective number of distinct
    buckets in use CHANGES over training must be recorded per-episode, not only at the
    end. A single end-of-training snapshot cannot distinguish "crystallised at N" from
    "was always N", and the distinction is the claim.

    CONFIRMING: with num_slots varied across a range spanning under- and
    over-provisioning relative to the environment's ground-truth latent-context count --
    (a) the effective bucket count in use converges toward the GROUND-TRUTH count rather
    than toward num_slots; (b) that convergence is non-monotone in plasticity -- high
    early, reducing -- rather than flat; and (c) downstream generalisation /
    discrimination is FLAT with respect to num_slots over the range where (a) holds.
    (c) is the LOAD-BEARING leg: it is the claim that the architecture is insensitive to
    a constant which is currently hand-set.

    FALSIFYING: effective bucket count tracks num_slots rather than the environment (the
    store simply fills what it is given); OR downstream performance is MONOTONE in
    num_slots (more is just better, so dimensionality is a capacity parameter and not a
    structure to be resolved); OR the effective count is FLAT across training (no
    schedule, so crystallisation is doing no work even if the final count happens to be
    right).

    CHEAPEST FIRST STEP -- A PROBE, NOT A BUILD. Treat num_slots as a declared
    experimental variable rather than a constant and re-run the V3-EXQ-436 lineage across
    a small sweep. If the 436-lineage results are STABLE across num_slots, the
    dimensionality question is not currently load-bearing and this claim can wait; if they
    MOVE, it is. This is `complex (probe-gated)` work resolving to `puzzle (known rules)`,
    NOT the `complicated (buildable)` self-sizing-memory build, and must not be routed as
    the latter. Registered as a proposal at intake; see manual_proposals.v1.json.

    NOT ANSWERED BY: any run at a single fixed num_slots, however well designed --
    including MECH-495's own 2x2, unless that 2x2 is itself swept over num_slots.
  notes: >
    Registered 2026-08-23 from the raw thought
    docs/thoughts/2026-08-23_memory-dimensionality-crystallisation.md, arising in
    conversation during the /governance cycle of 2026-08-22 (session bold-chaum-7e245c)
    out of GFLAG-0047, the open ContextMemory write-selection DV fork. Structured intake:
    evidence/planning/thought_intake_2026-08-23_memory-dimensionality-crystallisation.md.
    RELATION TO THE EXISTING CRITICAL-PERIOD CLUSTER: INV-074 / MECH-333 / MECH-334 /
    ARC-076 / MECH-335 / MECH-484 already specify the open-then-crystallise schedule, but
    entirely over the scoring/policy plane. This claim is the representational-structure
    analogue and is NOT a duplicate of any of them; it depends on MECH-334 (the closure
    half), which itself currently reads epistemic_category substrate_ceiling with
    ceiling_decision deferred -- so this claim inherits that block for its full form,
    which is exactly why the probe above is scoped to need neither.
    CATEGORY REASONING: substrate_conditional, NOT substrate_ceiling. No allocation policy
    exists at all, so the mechanism has never been exercised and zero evidence is banked
    in either direction -- that is the substrate_conditional definition. Do not re-file it
    as a ceiling without a run that actually exercised a variable-dimensionality store.
    PHASE REASONING: v3 follows the dependency (MECH-334 is v3) per the standing
    phase-label-follows-dependency rule. If governance judges the self-sizing build to be
    V4, the label moves with it -- flagged at registration rather than silently assumed.

- id: Q-094
  title: >
    Should REE's active context representation include INTERNAL variables -- current
    goal, bodily/interoceptive state, confidence, inferred social situation -- rather
    than being primarily sensory scene identity? MECH-150 records a deliberate design
    decision that E1 ContextMemory is queried with z_world ALONE and explicitly NOT the
    full [z_self, z_world] state, on terrain-weighting grounds. If context is the
    organism's inference about which latent SITUATION it is in (MECH-493/MECH-494)
    rather than which SCENE it is looking at, then two states with identical z_world but
    different goal, bodily state or confidence may be different contexts, and the
    z_world-only query cannot represent that distinction in principle.
  claim_type: open_question
  subject: context.active_context_contents_internal_vs_sensory
  polarity: open
  status: open
  epistemic_category: standard
  implementation_phase: v3
  claim_level: mechanistic
  registered_utc: "2026-08-20"
  depends_on:
    - MECH-150   # the standing z_world-only ContextMemory query design decision this question challenges
  related_claims:
    - MECH-493   # context inference as arbitration -- the reframing that makes this question live
    - MECH-494   # switch criterion -- its action-consequence term is a second route to the same point (same scene, different consequences)
    - MECH-153   # supervised context-labeling requirement -- any answer here interacts with the training objective, since what the query CAN represent and what training makes it represent are different questions
    - ARC-041    # shared cue-conditioning substrate cluster with MECH-150/151/152
  source_documents:
    - docs/thoughts/2026-08-19_context-inference-active-persistent-control.md
    - evidence/planning/thought_intake_2026-08-19_context-inference-active-persistent-control.md
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: ContextMemory must be producing differentiated
    representations at all before a query-composition comparison is meaningful --
    MECH-153 records cosine_sim ~= 1.0 without a supervised context objective, and two
    query compositions over an undifferentiated bank are indistinguishable by
    construction. Also requires states that genuinely share z_world while differing in
    z_self, which must be verified in the substrate rather than assumed.

    ANSWERED BY: a substrate comparison of ContextMemory queried with z_world alone
    versus [z_self, z_world], on a task family containing states with matched z_world but
    differing internal state (goal or bodily state) that require different policies.
    Resolves toward INCLUSION if the augmented query discriminates those states and the
    z_world-only query provably cannot, with the discrimination translating into
    behavioural competence; resolves toward MECH-150's EXISTING DECISION if the augmented
    query adds no discrimination, or degrades retrieval by making context over-sensitive
    to internal fluctuation (the plausible cost: bodily state varies continuously, so
    including it risks the context variable never being stable enough to exploit --
    which is MECH-493's permanently-epistemic pathology arriving by a different route).

    This is cheap and should go FIRST among the 2026-08-19 intake's follow-ons: it needs
    no new environment family, only matched-z_world/differing-z_self states against the
    existing bank, and MECH-150's own rationale for the exclusion is on record so the
    comparison is well-posed.
  notes: >
    Registered 2026-08-20 from the closing open-questions section of the 2026-08-19 raw
    thought "Context inference as an active, persistent control process". See
    evidence/planning/thought_intake_2026-08-19_context-inference-active-persistent-control.md.

    Registered as an open question rather than a mechanism hypothesis because the raw
    thought poses it as a question and because MECH-150's exclusion of z_self is a
    reasoned, recorded design decision rather than an oversight -- this is a challenge to
    be adjudicated by measurement, not a defect to be fixed. The plausible failure mode of
    inclusion (continuously-varying internal state destabilising the context variable) is
    real and is stated in what_would_answer so that a negative result is interpretable
    rather than reading as a bare null.

- id: Q-095
  title: >
    Does explicit hippocampal episodic organisation (binding, segmentation, separation,
    completion, indexing, remapping treated as ONE coordinated organising principle) add
    predictive or behavioural capability beyond REE's existing trajectory-generation
    account and the individually-registered mechanisms that already cover its parts
    (MECH-147, MECH-154, MECH-242, MECH-288, MECH-074d) -- or is it a REINTERPRETATION of
    mechanisms REE already has? The raw thought poses this against itself as its own
    falsification condition: if the existing architecture already produces appropriate
    episodic discrimination, completion and context-sensitive retrieval without an
    additional organising principle, the thought is a re-description and should not be
    promoted into the architecture.
  claim_type: open_question
  subject: hippocampus.episodic_organisation_adds_capability
  polarity: open
  status: open
  epistemic_category: standard
  implementation_phase: v3
  claim_level: architectural
  registered_utc: "2026-08-20"
  depends_on:
    - MECH-495   # the relational-topology objective -- the one genuinely new assertion extracted from the same thought; if MECH-495 falsifies, this question largely resolves toward "reinterpretation"
  related_claims:
    - MECH-147   # pattern separation (v4)
    - MECH-154   # addressable associative manifold with indexing
    - MECH-242   # pattern completion vs vector-based construction
    - MECH-288   # event segmenter (built)
    - MECH-074d  # BLA remapping (demoted 2026-08-16)
  source_documents:
    - docs/thoughts/2026-08-19_hippocampal-episodic-organisation.md
    - evidence/planning/thought_intake_2026-08-19_hippocampal-episodic-organisation.md
  what_would_answer: |
    NON-DEGENERACY PRECONDITION: the existing mechanisms must be ARMED and functioning
    before "the existing architecture already suffices" can be tested. As of 2026-08-20
    this is NOT the case -- MECH-288 is built, but MECH-147 is phase v4 and unbuilt,
    MECH-074d was demoted 2026-08-16, and the ContextMemory write path was only repaired
    on 2026-08-19 (ree-v3 76cbf844) with no validation run yet scored. A null result
    obtained now would measure the substrate's readiness, NOT this question, and must be
    reported non_contributory.

    ANSWERED BY: comparing REE with and without an explicit coordinated organising
    principle on the 2x2 design in MECH-495's what_would_answer, scored on downstream
    generalisation and discrimination rather than on any internal representational
    statistic. Resolves toward ADDS CAPABILITY if the coordinated version achieves
    relational topology and downstream competence the sum of the individually-registered
    mechanisms does not. Resolves toward REINTERPRETATION if the existing mechanisms,
    once armed, already produce the appropriate overlap/separation pattern -- in which
    case the correct disposition is to keep the thought as an explanatory framing in the
    architecture docs and NOT register further mechanism claims from it.

    This question is deliberately gated behind MECH-495 and behind substrate readiness;
    it should NOT be the first thing tested from this intake.
  notes: >
    Registered 2026-08-20 from the "Potential falsification" section of the 2026-08-19 raw
    thought "Hippocampal episodic organisation beyond trajectory generation". See
    evidence/planning/thought_intake_2026-08-19_hippocampal-episodic-organisation.md.

    Registered because the thought's self-directed skepticism is a genuine, answerable
    question about REE's architecture and is worth carrying explicitly rather than
    discarding -- an intake that registers only a thought's positive assertions and drops
    its own stated falsification condition is recording half of it. Episodic BINDING (the
    thought's capability 1: objects, locations, actions, internal states, goals, outcomes
    and temporal relations bound into ONE coherent event representation) is the one
    capability with no clean existing owner; it was deliberately NOT registered as its own
    claim because the raw thought names it without developing it and it is substantially
    entangled with MECH-154 and MECH-288. It is recorded as an open gap in the intake file
    and is the most likely source of a future claim from this thought.
