# Summary

> **PROVENANCE NOTE (2026-08-19, chip `chip-20260818-lit-fuzzy-title-candidates`). Provenance only --
> no `confidence`, `evidence_direction`, `mapping` or `claim_ids_tested` field was touched.**
>
> `source.title` and `source.authors` were fabricated (they were a verbatim copy of the Neuron
> sibling entry's title/author list, not this paper's real byline), and `source.url` pointed at PMID
> 20147401, a hallucinated near-miss that resolves to an unrelated virology paper. `venue`/`year`
> were already correct. Verified via NCBI eutils esummary: the real paper by Andrews-Hanna, Reidler,
> Huang & Buckner (2010) is "Evidence for the Default Network's Role in Spontaneous Cognition," J
> Neurophysiol 104(1):322-335, PMID **20463201**, DOI `10.1152/jn.00830.2009`, PMC2904225. `title`,
> `authors`, `pmid`, `doi`, `pmc`, `volume`/`issue`/`pages` and `url` corrected/added accordingly.
> The prose below (source wording / REE translation / mapping caveat) already described this paper's
> actual content (spontaneous cognition and DMN subsystems), not the fabricated title, so it was left
> unchanged.
>
> This entry was flagged (alongside the sibling Neuron entry above in the same folder) by the
> 2026-08-14 duplicate-entry scan's fuzzy title matcher as a possible same-paper pair with different
> venues. **Resolved as two genuinely different papers** -- confirmed by cross-resolving both PMIDs
> (20463201 here; 20188659 for the Neuron entry) against NCBI: different titles, different
> volumes/pages, and a different (non-overlapping) third co-author (Huang here vs. Sepulcre/Poulin
> there). Both papers legitimately cite MECH-029, so this is **not** a live double-count.

## Source wording
Default-network subsystems are differentially associated with spontaneous thought and internally directed cognition.

## REE translation
Supports MECH-029 by favoring structured reflective replay lanes over a single undifferentiated DMN process.

## Mapping caveat
Subsystem association evidence does not directly encode commitment gating boundaries.
