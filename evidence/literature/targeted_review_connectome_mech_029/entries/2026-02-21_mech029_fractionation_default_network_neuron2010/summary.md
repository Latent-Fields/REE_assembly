# Summary

> **PROVENANCE NOTE (2026-08-19, chip `chip-20260818-lit-fuzzy-title-candidates`). Provenance only --
> no `confidence`, `evidence_direction`, `mapping` or `claim_ids_tested` field was touched.**
>
> `source.url` pointed at PMID 20547162, a hallucinated near-miss that does not identify this paper
> (that PMID resolves to an unrelated record). `source.title`/`authors`/`venue`/`year` were already
> correct. Verified via NCBI eutils esummary: the real PMID for Andrews-Hanna, Reidler, Sepulcre,
> Poulin & Buckner (2010), "Functional-anatomic fractionation of the brain's default network," Neuron
> 65(4):550-562, is **20188659** (DOI `10.1016/j.neuron.2010.02.005`, PMC2848443) -- `pmid`, `doi`,
> `pmc`, `volume`/`issue`/`pages` and `url` corrected/added accordingly.
>
> This entry was flagged (alongside the sibling J Neurophysiol entry below in the same folder) by
> the 2026-08-14 duplicate-entry scan's fuzzy title matcher as a possible same-paper pair with
> different venues. **Resolved as two genuinely different papers** -- confirmed by cross-resolving
> both PMIDs (20188659 here; 20463201 for the J Neurophysiol entry) against NCBI: different titles,
> different volumes/pages, and a different (non-overlapping) third co-author (Sepulcre/Poulin here
> vs. Huang there). Both papers legitimately cite MECH-029, so this is **not** a live double-count.

## Source wording
The default network is functionally and anatomically fractionated into distinct subsystems.

## REE translation
Supports MECH-029 by encouraging decomposed reflective replay pathways with explicit integration constraints.

## Mapping caveat
Subsystem structure does not by itself specify commitment suppression thresholds.
