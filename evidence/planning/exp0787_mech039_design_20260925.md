# EXP-0787 (MECH-039) design, held: the two-part veto readout is built, but its interrupt part cannot move yet

- **Date:** 2026-09-25
- **Session:** bt0925-mech039 (chip `chip-20260925-mech039-two-part-veto-readout`)
- **User decision:** rec-20260925-3b215584, MECH-039 option 1, from `claim_synthesis_MECH-039_20260925.md` as revised by `claim_synthesis_MECH-157-039_redteam_20260925.md`.
- **Instrument:** ree-v3 `REEConfig.use_mech039_veto_readout` / `REEAgent.get_veto_readout()`. Record: `ree-v3/docs/substrate/MECH-039-two-part-veto-readout.md`.
- **Status: NOT QUEUED.** `/queue-experiment` Step 2.5d found the falsifier INERT at the only operating point measured. EXP-0787 is left `blocked_substrate` with named blockers (below).

## 1. What the run would test

- **Veto exception (primary).** An INTERRUPT-part onset (SD-035 CeA fast route, or SD-037 `override_signal` crossing 0.5) should be followed by a fast multi-channel jump and a coordinator mode switch. A CONTROL-part onset (PAG freeze, habenula abort) is the within-channel control and should not be. DV: onset-to-`mode_switch_trigger` latency per part, from the readout, plus the number of channels that move beyond their own SD within k E3 ticks of the onset.
- **MECH-046 shared arm.** The same interrupt latency, CeA-ON against amygdala-OFF, at matched seeds and matched injection times. Record it once and tag it per-claim.
- **Clustering (MECH-039 CONFIRMING).** Condition-labelled channel vectors form separable regions: emergency = the post-injection window, task-engaged = resource pursuit with no hazard in view, default-mode-like = quiescent / no goal. Computed on a **coordinator-OFF arm**, or with the coordinator-derived channels (MECH-261 write gates, rollout conditioning) left out (red-team D6).
- **Emergency as a STATE.** The driver calls `CausalGridWorldV2._inject_external_hazard()` at scheduled steps, so it knows the onset time exactly. A sham schedule at the same steps, with no injection, is the paired control. There is no coordinator enum entry.

## 2. Precondition gate (each part must FIRE under hazard onset)

A failed gate self-routes `non_contributory` / `substrate_not_ready`. It is never read as evidence against MECH-039.

- **G1, interrupt part fires at registered thresholds.** CeA `urgency_fire` count > 0 with `cea_fast_route_threshold` at its default 0.5 (lowering it measures the threshold, not the claim: MECH-046 precondition), OR `override_signal` >= 0.5. The onset must fall within 10 steps after an injection on at least 30% of injection events.
- **G2, control part fires.** PAG freeze onsets > 0 inside post-injection windows (the freeze no-op is the STAY class since ree-v3 `1fc8816`, GFLAG-0508), and habenula aborts > 0.
- **G3, the coordinator can switch.** `n_mode_switches` > 0 in the coordinator-ON arm. Without a switch the latency DV has no terminal event: every onset is censored.
- **G4, non-degeneracy.** Each recorded channel has cross-condition variance > 0. Channels that move in lockstep with hazard level are reported, not scored.
- **MECH-449 part:** NOT armed until V3-EXQ-1090 reports (it is `claimed` / running as of 2026-09-25).

## 3. Measured today (untrained agent, 2 threads, seed 11, 300 steps, injection every 30 steps)

Config: `use_harm_stream`, `use_affective_harm_stream`, amygdala + CeA, `use_broadcast_override`, `use_salience_coordinator`, `use_pag_freeze_gate`, closure operator + lateral PFC + habenula, `alpha_world=0.9`. Driven through `experiments/_harness.StepHarness` so that harm observations reach `sense()`. (A bare `act_with_split_obs` loop never populates `z_harm_a`, so CeA never ticks. Any driver needs this.)

| Gate | Result |
|---|---|
| G1 CeA | **INERT.** `urgency_fire` 0. `low_freq_magnitude` max 0.172, mean 0.133, against threshold 0.5 (about 3x short). This matches the MECH-046 SUBSTRATE note: "no embodied run has reported fire_count > 0". |
| G1 override | 5 onsets, **0 inside post-injection windows**. It is driven by drive plus sustained threat, not by onset. |
| G2 | Met: 29 freeze onsets (13 inside post-injection windows) and 56 habenula aborts. |
| G3 | **INERT.** 0 mode switches in 140 coordinator ticks. This is consistent with the mode-governance lineage being inert at production defaults (`substrate_queue` `mode-governance-engagement`, implemented_pending_validation; `mech019-operating-mode-enabled-consumer`). |

So the control part can move and the interrupt part cannot. The primary contrast (interrupt versus control) and the MECH-046 latency both need a coordinator switch and an interrupt onset, and neither occurs.

## 4. Unblock condition (work-graph debt)

- `complicated (buildable)`: validate or arm `mode-governance-engagement` so that the SalienceCoordinator actually switches in a hazard regime (G3).
- `complex (probe-gated)` / `puzzle (known rules)`: does waking training lift CeA `low_freq_magnitude` over 0.5 under hazard onset? One trained-agent probe answers it. It has not been measured, because the Mac is not the place for a training run. If it does not, CeA's fast route is sub-threshold by construction at this `z_harm_a` scale, and that is an SD-035 calibration question for its owner (not a threshold change made here). **ANSWERED 2026-09-26 (governance-gflags-20260926; GFLAG-0554/0556/0557):** the premise is off-path -- `harm_eval_head` is not on CeA's path (CeA reads mean|z_harm_a| from the never-trained affective_harm_encoder; mode_switch_cea_mechanism_trace_20260925.md sec 2), and neither relative gates nor env de-saturation give z_harm_a onset selectivity (cea_gate_calibration_probe_20260925.md, cea_onset_input_reprobe_20260925.md). The best onset stream is the raw hazard_at_agent scalar, reachable via the default-OFF `safety_proximity_harm` channel at `safety_proximity_ema_alpha=1.0`; the owed work is substrate_queue `cea-onset-input-selector` (probe-gated).
- Once G1 and G3 can move: queue EXP-0787 with the gate in section 2. Tag MECH-039 and MECH-046 per-claim, and use MECH-046's EXQ-A arm as the shared arm.
