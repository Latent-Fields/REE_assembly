# Failure Autopsy Batch — 2026-07-30

**Scope:** all items in `pending_review.md` as of the 2026-07-30T18:26:25Z regeneration (14 items), run inline per user instruction (not chipped — matches the standing rule that `/failure-autopsy` work is reported inline, `pending_review.md` being the self-deriving worklist).

**Dry-run gate:** all 14 run_ids checked via `check_dry_run_citations.py` — 0 dry, 14 clean. No smoke contamination anywhere in this batch.

**Overlap discovery (load-bearing for scope).** Regenerating `pending_review.md` from disk (never trust the committed copy — confirmed necessary: 9→14 items) also surfaced that **another session already autopsied 4 of the original 9 items earlier the same day** (commits landing ~07:30–07:51 local, well before this batch started ~18:25 UTC): `V3-EXQ-819a`, `V3-EXQ-810a`, `V3-EXQ-798a` (the PASS run only), `V3-EXQ-839` — all `status: confirmed`, all committed to `origin/master` (verified clean `git status --porcelain`, real commit `226a40720c` etc. present in `git log`). This batch does **not** re-derive those four; it cites them and covers the genuine remainder, including one item (the 798a FAIL run) that landed *after* that autopsy's target list was fixed and is therefore still open.

## Already-confirmed (cited, not re-autopsied)

| Run | File | Routing | Note |
|---|---|---|---|
| `v3_exq_798a_..._20260729T125858Z_v3` (PASS) | `failure_autopsy_V3-EXQ-798a_2026-07-30.{md,json}` | queue-experiment | C4-readability repair validated; no claim_ids |
| `v3_exq_810a_arc071_chunk_accumulator_readiness_20260728T204535Z_v3` (PASS) | `failure_autopsy_V3-EXQ-810a_2026-07-30.{md,json}` | queue-experiment | chunk_accumulator_fires confirmed |
| `v3_exq_819a_mech457_inv088_..._20260727T005012Z_v3` (PASS) | `failure_autopsy_V3-EXQ-819a_2026-07-30.{md,json}` | governance-registry-resolution | `H-zworld-trained-instrument` → **confirmed** in `hypothesis_space_registry.v1.json` (`competence_floor` question); recommended a secondary confirmatory spike re-running 748's dense-teacher arm under the now-validated instrument — **this is what produced `V3-EXQ-748a`** (see below), via the `chip-20260730-hrep-rederivation` follow-on |
| `v3_exq_839_sd084_midexec_reachability_20260729T220727Z_v3` (PASS) | `failure_autopsy_V3-EXQ-839_2026-07-30.{md,json}` | queue-experiment | midexec_reachable_handle_validated |

---

## New target: `v3_exq_798a_..._20260730T010651Z_v3` (FAIL) — cluster A, second half

**Facts.** Same driver as the already-autopsied PASS, no claim_ids (SD-MEL-PRODUCER environment test-bed validation, not a claim test — script docstring is explicit: "This validates the ENVIRONMENT TEST-BED... not a claim. claim_ids is deliberately EMPTY"). Self-route `substrate_not_ready_requeue`; `non_degenerate: true`. One precondition narrowly unmet: `frozen_pe_responds_to_shock` measured **0.2231** vs threshold **0.25** — every other precondition (world-model convergence, learnability-quartile population, noise-control elevation, cycle-quartile reachability) passed cleanly.

**Diagnosis.** This is the same environment-readiness instrument the sibling PASS run already validated hours earlier the same day. A single narrowly-missed precondition (89% of threshold) on a repeat run of a stochastic shock-response probe reads as measurement variance on a borderline gate, not a new environment defect — the thing the gate exists to catch (unreadable C4, non-elevated noise control) is unambiguously absent here. Self-route is trustworthy: confirm `substrate_not_ready_requeue` as-is.

**Routing:** `queue-experiment` (none required — the environment question is already answered by the PASS sibling; this run adds only a note on gate borderline-ness, worth a wider margin on `frozen_pe_responds_to_shock` if the gate is re-touched, not a re-run). No claim impact (no claim_ids). `epistemic_category: standard`.

---

## Cluster B: `v3_exq_687a_mech313_committed_authority_dissociation_20260730T050850Z_v3` (FAIL)

**Facts.** Claims `Q-045`, `MECH-313`, `MECH-260`. Supersedes `v3_exq_687` (the original 4-arm tonic-noise ablation). Self-route `substrate_not_ready_requeue`, flagged `precondition_unmet` by the indexer. **All four arms** (`ARM_0_both_off`, `ARM_1_mech313_only`, `ARM_2_mech260_only`, `ARM_3_both_on`) failed `committed_sufficiency` at **measured 0.0 vs threshold 50.0** — no arm produced any committed behaviour at all, and two arms additionally failed `dacc_fifo_full` (8.0 vs 8.0, i.e. exactly at the boundary).

**Claim layer.** MECH-260 (dACC anti-recency bias) and MECH-313 (LC-NE tonic noise floor) were both already **demoted via GOV-CEIL-1** on 2026-07-09 — MECH-260 carries 9 confirmed `substrate_ceiling` autopsy hits, MECH-313 carries 8, both well past the re-derive-brake threshold. Q-045 (the open question these two mechanisms were meant to dissociate — separate parameters or one collapsed substrate?) remains `open`.

**Diagnosis (user-confirmed routing: confirm self-route as-is).** The failure shape here is categorically different from the prior 8–9 ceiling hits on these claims: those were *discrimination* failures (negative control passes, discrimination criterion fails — the substrate-ceiling fingerprint). This run is a *universal absolute-floor* failure — zero committed behaviour in **every** arm, including the two-off baseline. That is not evidence about MECH-260/MECH-313's discrimination; it is the same competence-floor signature the MECH-457 campaign has been mapping (an integrated agent that cannot sustain committed behaviour long enough to generate the DV at all). Per the four-layer read: claim_alignment unclear (never let the claim express itself — no arm reached the behaviour the test is built to discriminate); biological reference n/a for this run (the dependency gap is upstream); prerequisites missing (competence floor); measurement adequate in design, inapplicable in practice (nothing to measure).

**Do not count this as a 10th/9th ceiling hit** — it would misattribute a competence-floor failure to a discrimination-ceiling claim already carrying the correct diagnosis. `recommended_epistemic_category: competence_implementation_gap`. Q-045 stays open; recommended `evidence_quality_note`: *"V3-EXQ-687a (2026-07-30): re-derive brake exempt — all 4 arms failed committed_sufficiency at the absolute floor (0.0/50.0), not a discrimination result. Q-045 is now gated on the same MECH-457 competence-floor substrate as the rest of the committed-behaviour campaign, not independently re-queueable until that substrate clears."*

**Routing:** `implement-substrate` — `recommended_substrate_queue_entry.action: none` (the relevant substrate gap is already the MECH-457 competence-floor thread, already in the queue; no new entry needed, just the cross-reference above). Explicitly **not** a same-claim re-queue — nothing would change without the upstream competence-floor fix.

---

## Cluster H: `v3_exq_707c_arc110_loop_segregation_c2_release_repair_20260722T041239Z_v3` (FAIL)

**Facts.** Claims ARC-110. Supersedes `V3-EXQ-707b`. Self-route `valid_null_loops_vary_but_no_conversion_ceiling_intrinsic_weakens_arc110` — **not** flagged untrustworthy by the indexer. C1 (load-bearing, `A1_loops_strict_above_A0_and_in_layer_null`) failed; C2 (non-load-bearing) also failed.

**Diagnosis.** ARC-110 already carries a **decisive, user-approved `weakens/NARROW`** disposition from 707b (2026-06-29, per its `evidence_quality_note`). 707c's own self-route reads as a continuation of exactly that narrowing, not a contradiction — "loops vary, but no *intrinsic* conversion ceiling" is a further refinement of the same weakens direction. Claim-layer: intact-but-narrowed (consistent with the existing note). The 3 confirmed ceiling hits on ARC-110 in the corpus (709/711/713) belong to a **different sub-thread** (learned cross-loop arbitration / gain validation / bounded parity controller) — not the loop-segregation C2-release lineage 707/707a/707b/707c belongs to. No re-derive-brake conflict.

**Routing:** `governance-demotion` is not indicated (already applied via 707b); this run is confirmatory-narrowing evidence. Recommend governance simply extend the existing `evidence_quality_note` with the 707c narrowing, `epistemic_category: standard`.

---

## Cluster I: `v3_exq_840_mech294_theta_packet_binding_committed_action_falsifier_20260730T170540Z_v3` (FAIL)

**Facts.** Claims MECH-294. `experiment_purpose: evidence` (the first evidence-grade behavioural test of the joint-binding claim after two substrate blocks were cleared: the compose-coherence amend 2026-06-09 and the per-candidate co-binding coherence amend 2026-06-19, plus confirmed route-range readiness via 790/791a). Both load-bearing criteria (C1 joint≠alternation, C2 joint≠shuffled) failed; `criteria_non_degenerate` is `false` for both. Self-route `substrate_not_ready_requeue`, **not** flagged untrustworthy. Only one precondition is recorded (`ARM_0_OFF::routed_range_bounded`, trivially met) — the readiness gate that actually decided the self-route (ARM_1_JOINT's routed cross-candidate range vs floor) fires through the `non_degenerate` flag rather than a listed precondition, per the script's own documented design ("the run self-routes substrate_not_ready_requeue with non_degenerate=false, NEVER a substrate verdict").

**Diagnosis.** The script is unusually well-instrumented against exactly the failure mode that sank its predecessor (V3-EXQ-661: a scalar coherence bias was rank-invariant under the route-range authority's rescaling, so a committed-TV null there was uninterpretable). This run's own readiness safeguard — asserting the *same statistic* (route_range) the criteria depend on — caught the same class of problem again: the per-candidate coherence amend produces enough signal to pass its own contract-test suite (`test_mech294_per_candidate_coherence.py`, smoke-verified) but not enough to clear a supra-floor cross-candidate range in a full real run. This is evidence about instrumentation magnitude, not about the joint-binding claim — MECH-294 has had only 1 prior confirmed ceiling hit (a different mechanism, `661_compose_coherence_behavioural_readiness`), so this is not yet a repeat-ceiling pattern.

**Routing (user-confirmed):** `implement-substrate` / `queue-experiment` — recommend strengthening the per-candidate coherence magnitude (or auditing whether the readiness floor itself is miscalibrated relative to what the mechanism can produce) so a real run can clear `route_range` supra-floor, then re-run the **same** falsifier (same question, alphabetic suffix). `recommended_substrate_queue_entry.action: amend` if an existing entry names the coherence-magnitude gap, else `create`. `epistemic_category: measurement_test_design_defect` (readiness-gate-caught, not a substrate verdict). MECH-294 stays candidate/v3_pending.

---

## Cluster K: `v3_exq_748a_mech457_hrep_zworldp0_rederivation_20260730T120515Z_v3` (PASS)

**Facts.** Claims MECH-457. Label `hrep_elimination_confirmed_under_trained_instrument`; load-bearing criterion `C_any_zworld_dense_arm_clears_floor_at_D3` passed.

**Diagnosis.** This is exactly the confirmatory spike the 819a autopsy recommended as a secondary, non-brake, "confirmatory hardening" follow-on (see the already-confirmed table above) — re-running V3-EXQ-748's dense-teacher/z_world-BC arm under the now-validated `zworld_p0`-trained instrument, to directly reconfirm `H-rep`'s 2026-07-13 elimination (which had been read off an older, cotrain=True encoder) against the newer, validated-detached instrument. Result: **H-rep's elimination holds** under the corrected instrument — no reopening. This closes the loop 819a's autopsy explicitly left open.

**Routing:** `governance-registry-resolution` — Step 9b below records this as the confirmatory resolution. No claims.yaml change (MECH-457 stays candidate/v3_pending; H-rep was already `eliminated`, this reconfirms rather than changes the state).

---

## Cluster D, second half: `v3_exq_810b_arc071_mech323_seed202_narrow_repertoire_probe_20260730T175935Z_v3` (PASS)

**Facts.** Claims ARC-071, MECH-323. Both load-bearing criteria (`C_repro`, `C_safety_no_replay_origin`) passed; label `narrow_repertoire_confirmed`.

**Diagnosis.** Same family as the already-confirmed `810a` (chunk-accumulator readiness), run same day as a seed-202-specific narrow-repertoire follow-up probe. Clean PASS, no flags, 0 prior ceiling hits on ARC-071. "Narrow repertoire confirmed" reads as a positive readiness confirmation (the accumulator correctly narrows the candidate repertoire under repeated grounding), not a limitation finding — consistent with 810a's `chunk_accumulator_fires` result.

**Routing:** `queue-experiment` — none required; straightforward close alongside 810a. `epistemic_category: standard`.

---

## Cluster F: `v3_exq_833_stageh_strict_goal_isolation_dv_20260730T060701Z_v3` (PASS)

No claim_ids (StageH diagnostic). Load-bearing criterion `C3_answer_reached` passed; all supporting gates green. Label `goal_term_behaviourally_inert_in_stageh_within_stated_precision` — a clean, informative diagnostic answer (the goal term is inert within the stated measurement precision in Stage-H), not a flagged self-route. **Routing:** straightforward close, no autopsy debt.

## Cluster L: `v3_exq_842_mech217_offline_wanting_spread_readiness_20260730T173047Z_v3` (PASS)

Claims MECH-217. Load-bearing criterion `C1_on_near_exceeds_off_1p5x` passed; all supporting criteria green. Label `mech217_offline_spread_readiness_verified`. Clean, unflagged PASS. **Routing:** straightforward close.

---

## Cluster G: `v3_exq_673_mech171_vicious_cycle_sleep_disruption_20260612T005615Z_v3` (FAIL) — administrative closure

**This is not a fresh autopsy target.** The manifest carries `evidence_direction: non_contributory`, `governance_applied_utc: 2026-07-30T01:44:28Z`, and `recovered_from`: it was recovered 2026-07-29 from an **orphaned runner-prepull-untracked stash on ree-cloud-3** — the exact hazard `CLAUDE.md`'s stash-audit section documents (this run existed at no path on `origin/master`, on no branch, and the coordinator DB held no results row for it at all until recovery). Its disposition is **inherited verbatim from `failure_autopsy_batch9_2026-06-12`**: `does_not_support` — **degenerate** (all 3 arms identical on every metric, `late_pred_loss≡0.0` on every seed; the sleep-compression knob never propagated into the readout; 3 untracked same-EXQ reruns, a since-documented anti-pattern).

**Gap found:** batch9's recommendation — set MECH-171 `epistemic_category: out_of_domain` (parallel to sibling MECH-172, `governance_2026_06_10`: clinical AD multi-year staged progression is not faithfully instantiable in a 300-episode grid-world) — was **never applied to claims.yaml**. 48 days later MECH-171 still carries no `evidence_quality_note` at all.

**Routing:** `governance-demotion` not indicated (candidate stays candidate — `out_of_domain` is a scope note, not a demotion). Recommend governance apply the batch9 text verbatim now: *"V3-EXQ-673 (vicious-cycle sleep disruption, 2026-06-11, recovered from stash 2026-07-29): degenerate (all arms identical, late_pred_loss≡0; 3 untracked reruns under one EXQ). MECH-171 asserts clinical-Alzheimer's staged progression (multi-year vicious cycle, critical intervention window, schema-scaffold collapse) not faithfully instantiable in a 300-episode grid-world. epistemic_category: out_of_domain (parallel to MECH-172)."* Mark reviewed; no new autopsy owed.

## Cluster J: `v3_exq_614_mech341_p3_behavioural_falsifier_3arm_20260529T191318Z_v3` (FAIL) — administrative closure

**Also not a fresh target.** `evidence_direction: superseded` already set (`ADMITTED 2026-07-30 ... SUPERSEDED BY SUBSTRATE, not by a corrected iteration: ree-v3 a45ca7f (2026-05-29T23:41:55Z) raised e3_div[ersity]...` — the E3 diversity mechanism changed roughly an hour after this run, making it structurally stale rather than informative). Confirmed via its own five lettered successors (614a–614e), all already present in `review_tracker.json`'s reviewed set, with 614e itself carrying a confirmed `substrate_ceiling` reading in the corpus. MECH-341's confidence is untouched by 614 either way (superseded evidence, not scored). **Routing:** mark reviewed; no new autopsy owed.

---

## Cross-reference (new, not in the 819a autopsy): INV-088 vs `conversion_ceiling_campaign:GENERATION`

The existing 819a autopsy resolved `H-zworld-trained-instrument` (confirmed, `met_elimination_bar: false` — supports-with-margin, not a full elimination-bar clear) but did not address `conversion_ceiling_campaign_plan.md:GENERATION`'s `upstream_block_reason`, which names INV-088 z_world differentiation as the ordering gate. Per user direction: **read as a partial clear.** The antecedent leg of INV-088 (does a prediction-trained z_world differentiate/confer advantage over a frozen random projection?) now has direct supporting evidence (819a: paired AUC delta +0.626 vs a 0.3 margin, 4/6 seeds). But 819a's own `C_random_projection_provides_measured_baseline` criterion **failed** — the comparator arm itself wasn't cleanly measured — so the antecedent is supported, not fully discharged. `GENERATION` should stay `assembly_status: blocked_on_upstream`, with the block reason narrowed to note the antecedent is now evidenced (not just theorized) and the residual gap is the baseline-measurement criterion, not a live "is z_world differentiated at all" open question.

Recommended `INV-088` `evidence_quality_note` addendum: *"V3-EXQ-819a (2026-07-30 autopsy) + V3-EXQ-748a (H-rep reconfirmation): the antecedent leg (prediction-trained z_world confers competence advantage over frozen random-projection) is now directly evidenced, not merely theorized (paired AUC delta +0.626, 4/6 seeds, control_passed). The coupling leg (is the world/goal evaluator BOUNDED BY this differentiation) remains untested — 819a measured competence advantage, not the evaluator coupling. conversion_ceiling_campaign:GENERATION's ordering gate should be read as PARTIALLY informed, not cleared: the antecedent no longer blocks on 'unknown', but the coupling question INV-088 actually asserts is still open."*

---

## Governance handoff summary

| Target | Routing | Claim action |
|---|---|---|
| 798a FAIL (010651Z) | none — variance on a borderline gate | none |
| 687a | implement-substrate (none — cross-ref existing MECH-457 substrate gap) | Q-045 note only |
| 707c | governance (extend existing note) | ARC-110 note extension |
| 840 | implement-substrate / queue-experiment (coherence magnitude) | none yet — MECH-294 stays candidate |
| 748a | governance-registry-resolution | none — reconfirms H-rep |
| 810b | queue-experiment (none) | none |
| 833 | none | none |
| 842 | none | none |
| 673 | governance (apply batch9 note) | MECH-171 → out_of_domain note |
| 614 | none (already superseded) | none |
| INV-088/GENERATION cross-ref | governance (note addendum) | INV-088 note only |

All four already-confirmed targets (798a-PASS, 810a, 819a, 839) are cited, not re-applied here.
