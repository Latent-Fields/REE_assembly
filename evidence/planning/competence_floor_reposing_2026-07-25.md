# `competence_floor` — re-posing record (GOV-FANOUT-1 recurrence, cycle 2026-07-25)

- **Generated:** 2026-07-25T17:55:21Z
- **Session:** `competence-floor-fan-out-46b459` — "competence_floor re-pose (recurrence cycle 2026-07-25)"
- **Trigger:** the 2026-07-25 `/governance` cycle (`REE_assembly` `989ac1bca0`) re-surfaced the standing overlay flag — *"ACTIONABLE (reported inline, governance/autopsy-owned): competence_floor fan-out re-pose before opening portfolio 4."* The derived `hypothesis_space.v1.json` still reads `convergence_class: circling`, `families_fresh: []`, `families_closed: [instrumentation, representation, world]` — **exactly as the 2026-07-20 record predicted it would, unchanged, every cycle.**
- **Claims:** MECH-457 (candidate / v3_pending), INV-088 — **this record promotes nothing, demotes nothing, gates nothing.** Routing and framing only.
- **Registry writes:** **NONE.** `hypothesis_space_registry.v1.json` has a single producer (`/failure-autopsy` Step 9b); governance is derive-only over it. Every registry delta in §5 is *routed*, not applied.
- **Reads against, does not supersede:** `competence_floor_reposing_2026-07-19.md` and `competence_floor_reposing_2026-07-20.md`. Their conclusions stand. This record records **what materially changed since 07-20**, checks the escalation rule the 07-20 record armed, and re-routes the still-stale registry deltas.

---

## Headline

**The operationalization was already re-posed comprehensively on 2026-07-20. This cycle's job is not to re-pose it again — it is to record that the 07-20 re-pose's own critical-path pivot has now happened, and that its BLOCKING escalation rule is now armed.**

Two things the 07-20 record put on the critical path have both landed:

1. **`sd_zworld_warmup_optimizer_group` is BUILT and VALIDATED** (`ree-v3` `b523b9c70a`; validated `REE_assembly` `25a69fcd4c` 2026-07-22 — guard green, `world_encoder` tensors moved on every seed at `zworld_p0_episodes=60`). The 07-20 record's R3 said the `z_world` install-failure was *"a bug report, not a scientific question"* — a `complex (probe-gated)` → `complicated (buildable)` reclassification owed to this exact build. **That debt is now discharged.** Every `z_world` arm going forward can express a manipulation; the "one-representation campaign reported as a two-representation campaign" pathology (07-20 §Headline) is closed at the substrate.

2. **The retention question (Q1) is answered — the retention frame was productive, not circling.** Three of its four legs resolved, all in the retention frame's favour:
   - `H-retention-critic` — **V3-EXQ-788 PASS** (`retention_critic_retains_competence`; distributional critic retains 1.839 vs 0.525 scalar).
   - `H-retention-consolidation` — **V3-EXQ-792a PASS** (`retention_consolidation_protects_competence`; supersedes 792).
   - `H-retention-auxiliary-decay` — **V3-EXQ-789 FAIL → eliminated** (`retention_auxiliary_succeeded_then_decayed`).
   - `H-consummation-binding` — the only retention leg still open; **substrate-blocked** on `substrate_queue.json:mech457_consummatory_act` (route `/implement-substrate`, not another `/queue-experiment` iteration).

   So an installed competent policy **can** be retained under continued RL — via a distributional value estimator and/or a KL-anchor to the installed policy. That is a discriminated finding, produced by adjudicating from V3-EXQ-780's 20.933 install point on a **trajectory** DV, exactly the instrument the first ten legs lacked.

---

## The escalation check (the 07-20 rule, now applied)

The 2026-07-20 record §8 armed this rule verbatim:

> *"if the retention legs resolve and a portfolio 4 is proposed on an unchanged framing, treat the recurrence flag as **BLOCKING**. That is the pattern it exists to catch."*

Apply it to today's state:

| condition | status 2026-07-25 |
|---|---|
| retention legs resolved? | **YES** — 3 of 4 (788 / 792a / 789); only the substrate-blocked `H-consummation-binding` remains |
| a portfolio 4 proposed on an unchanged framing? | **NO** — `ree-v3/experiment_queue.json` holds a single item, **V3-EXQ-793a**, which is **SD-049** (ARM_2 curriculum/density repower per `failure_autopsy_V3-EXQ-793_2026-07-24`), not a `competence_floor` rival-enumeration leg |

**The blocking trigger is therefore NOT met — but it is now fully armed.** The precondition half (retention resolved) is satisfied; the only thing standing between this flag and BLOCKING status is that no one has yet proposed a fifth portfolio on the same framing. **The next `competence_floor` proposal that enumerates a new candidate mechanism against the unchanged produce/retain framing is BLOCKING on sight.** Record that here so the next cycle does not re-derive it.

---

## The sanctioned forward path — none of it is a portfolio

The 07-20 re-pose already named the moves. What changed is that the gate on the most important one has lifted. In priority order:

1. **R4 is now askable — as ONE leg, not a portfolio.** The 07-20 record's R4 fresh axis —
   > *"Does prediction-training `z_world` confer any advantage over a random projection of the same dimensionality?"*
   — was explicitly gated: *"It becomes askable only after `sd_zworld_warmup_optimizer_group` lands."* **It has landed and validated.** The control condition (a random projection) is the measured baseline that reached 32.72 under BC while running *as* the treatment for sixteen legs; the treatment (a genuinely prediction-trained `z_world`) is now constructible for the first time. This is a single instrument-validity question asked *about* the representation, of a shape the campaign has never taken — **not** a rival mechanism. Route: `/queue-experiment`, carrying the five `measurement_requirement` constraints (§4). This was routed as **D12** on 07-20 and is still unregistered (§5).

2. **The claim-granularity re-pose is already drafted and awaiting user approval.** `claim_synthesis_MECH-457_2026-07-22.md` (session `elegant-curran-fe12ba`, proposal committed `REE_assembly` `bb651543b2` — *registers nothing*) argues the decomposition **is** the re-pose the overlay keeps asking for, and must first clear MECH-457's `granularity_debt_disposition: coherent_campaign` (GOV-GRAN-1, 2026-07-16). It is proposal-first and **needs explicit per-child user approval** before anything touches `claims.yaml`; it is not this session's to land. Surface it, do not action it.

3. **`H-consummation-binding` → `/implement-substrate`** for `mech457_consummatory_act` (substrate-blocked; the last open retention leg). Not a re-queue.

4. **The sibling question advanced independently and is NOT circling.** `conversion_ceiling_root` (also MECH-457-tagged) eliminated `H-policy-learning` via **V3-EXQ-813** (survival-zeroed PPO: raw-obs actor forages 9.033, latent-actor 0.5 under the identical objective) and added **`H-observation-interface`** as the surviving live root alongside a now-VALID `H-substrate-ceiling` (the 734 re-run on the SD-070 encoder-warmup path is guard-green). The representation/observation-interface route is where the live scientific frontier actually sits — and it is being worked, not enumerated.

---

## Why the overlay still reads `circling` (unchanged, and expected)

`convergence_class` partitions on intervention **locus** and is structurally blind to a change of **explanandum** or of **instrument validity** (07-19 and 07-20 both established this; it was routed for documentation in `axis_families._purpose` and is *still not actioned* — §5). The denominator (16) and `families_fresh: []` are real facts about the *count*, but the campaign has, in sequence: swept "tune the learner" (10 legs, eliminated), pivoted to "retain the installed policy" (retention portfolio, now answered), and discharged the `z_world` instrument defect (build landed). A growing denominator on a question whose *kind* of answer keeps narrowing is refinement, not failure — but the overlay cannot see that, so **it will fire again next cycle, unchanged.** Read it against this record and the two prior ones before acting.

---

## §5. Registry deltas — ROUTED, not applied (derive-only; route via `/failure-autopsy` Step 9b)

The registry is materially **stale** relative to resolved evidence. These continue the 07-19/07-20 D-series; several are simply the 07-20 routings that were never applied.

| # | delta | status / basis |
|---|---|---|
| D7 (07-20) | mark `H-retention-critic` **decided** (V3-EXQ-788 PASS) and `H-retention-auxiliary-decay` **eliminated** (V3-EXQ-789) | **auxiliary-decay applied; critic STILL reads `alive` / `adjudicating_runs: []` in the registry — stale.** 788 manifest exists and PASSED. |
| D10 (07-20) | refresh `decision.live_gate` — it still reads *"H-retention-critic + H-retention-auxiliary-decay, queued AS A PAIR ... not yet queued ... queue is empty"*; both have since run and resolved | **still stale** |
| D11 (07-20) | refresh `synthesis` — its "SECOND, UNOWNED explanandum" paragraph reads the `z_world` 0/3 install as an open scientific question; it is a discharged build (`sd_zworld_warmup_optimizer_group` validated) | **still stale** |
| D12 (07-20) | register R4 (prediction-trained vs random-projection `z_world`) as the question's fresh axis **now that the substrate has landed** — **one leg, not a portfolio** | **now unblocked; still unregistered** |
| D13 (new) | record `H-retention-consolidation` as **resolved** by V3-EXQ-792a PASS (`retention_consolidation_protects_competence`); the registry records 792a as an adjudicating run (`d445dea549`) but the leg state still reads `alive` | stale |
| D14 (new) | record on the question that the **07-20 escalation rule is now ARMED** (retention resolved; next same-framing portfolio is BLOCKING) | §"escalation check" |
| — | **Discriminator documentation** (re-stated a THIRD time because it is still not actioned): record in `axis_families._purpose` / the integrity narrative that `convergence_class` partitions on **locus only** and is blind to a change of **explanandum or instrument validity**, so a legitimate re-pose that keeps its loci scores `circling`. Without this, this section is re-litigated every cycle. | not applied |

---

## Outcome, for the next cycle's overlay

**The `competence_floor` recurrence flag for cycle 2026-07-25 is METABOLIZED by re-posing, not by enumeration. N stays 3; no portfolio was opened.**

- The flag **will fire again next cycle, unchanged**; `convergence_class` will keep reading `circling`. Both are expected. Read against this record and the two prior re-poses.
- **The 07-20 BLOCKING escalation is now ARMED**: retention resolved, so the next same-framing portfolio-4 proposal is blocking on sight.
- **The critical-path build has LANDED and validated** — the one move the campaign was actually waiting on. What was a build is now done; R4 (one instrument leg) is the unblocked next question, and the sibling `conversion_ceiling_root` frontier (`H-observation-interface`) is where the live science sits.
- Nothing here is promoted, demoted, or gated. MECH-457 stays `candidate` / `v3_pending`; INV-088 unchanged.
