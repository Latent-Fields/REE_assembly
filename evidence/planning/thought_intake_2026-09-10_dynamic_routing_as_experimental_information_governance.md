# Thought intake -- Dynamic routing as experimental information governance

**Date processed:** 2026-09-15
**Raw thought:** `docs/thoughts/2026-09-10_dynamic_routing_as_experimental_information_governance.md` (436 lines)
**Session:** thought-pipeline-20260915
**Family HUB (full novelty table, full implementation-gap audit, full literature status -- not
duplicated here):**
`evidence/planning/thought_intake_2026-09-10_dynamic_information_governance_propagating_causal_privilege.md`

The raw file names its own parent in its header: `2026-09-10_dynamic_information_governance_propagating_causal_privilege.md`.
It is the assay-generative sibling -- it restates the hub's hypothesis in operational form and adds a
staged programme. **Its overlap with the hub is very high** (the `m_i->j(t) = g_ij * W_ij h_i(t)`
model, the anaesthesia prediction, the motif repertoire, the ephaptic open/closed-loop comparison,
the consciousness bounding and the strong-result chain are all restatements). This intake records
only what it contributes beyond the hub, and folds the rest.

---

## 1. Verbatim core proposal

> The key distinction is now: **representation** -- what information exists in a subsystem;
> **transformation** -- whether another subsystem could in principle decode or use that
> representation; **routing / causal privilege** -- whether that information is presently permitted
> to influence the next state of the organism.

> Two systems can be mutually interpretable yet fail to communicate because their periods of
> effective receptivity do not coincide.

And the architectural summary this file adds, which the hub states only in scattered form:

> CONTENT / What is represented? -- LEGIBILITY / How can one subsystem interpret another? --
> JURISDICTION / Which representation is allowed to influence which subsystem now?

---

## 2. Novelty table (short -- see hub for the full 21-row table)

| Thread unique or sharpest in THIS file | Existing REE coverage | Verdict |
|---|---|---|
| The CONTENT / LEGIBILITY / JURISDICTION triad stated as three required kinds of organisation (s.14), with "jurisdiction itself has dynamics, propagation, repertoire, and history" | ARC-139 owns the first two; MECH-539 owns a different third; MECH-534 owns the mechanism, not the requirement | **NEW -> ARC-145** (hub row 3). This file supplies the cleanest formulation and is cited in the claim's notes |
| Causal access correspondence as a separately maintained replay object, with the FACTORIAL discriminator -- correct representational pairing x shuffled routing pairing, and its converse (s.8) | MECH-540 (subspace maintenance, four static signatures); MECH-548 sleep corollary; the replay-maintenance supplement s.6.3 treats timing as a confound to exclude, not as a maintained object | **Adjacent-but-distinct -> the deferred replay-access-maintenance claim (NOT registered 2026-09-15; proposed to /governance as a MECH-540 signature; see the hub intake section 5).** This file is the SOURCE of the factorial design; the hub's DIG-5 lists arms but does not cross them |
| The four-way static-bridge-vs-dynamically-routed-bridge decomposition: static sufficient / dynamic necessary after bridge / routing without bridge sufficient / neither helps (s.7) | MECH-537's F1-F7 routing table; assays doc s.17 | **Instrument extension -- NOT registered.** Belongs in the work programme as an ML-13 / ML-14 arm set. Its assertive core ("mutual legibility and causal access are separable problems") is ARC-145 |
| Eight adversarial controls, named as a checklist: hidden attention, extra compute, representation repair disguised as routing, off-manifold rescue, behavioural shortcut, estimator artefact, mere arousal scalar, static topology sufficient (s.11) | INV-105 consequence (1); GOV-MATCHAUX-1; MECH-538's high-capacity-bridge warning; the receiver-manifold guard | **Already owned** as evidence standards. Adopted wholesale into MECH-560's and the deferred propagation claim (NOT registered 2026-09-15; see the hub intake section 5)'s notes rather than re-registered |
| Six-stage programme, Stage 0 (no organism change) .. Stage 6 (architectural consideration only after causal evidence and adversarial controls) (s.12) | The "diagnosis without changing the organism" rule from the mutual-legibility programme; assays doc s.21 | **Already owned** as doctrine. Recorded as binding staging on the location doc and in the deferred propagation claim (NOT registered 2026-09-15; see the hub intake section 5)'s notes |
| Everything else (s.1-s.6, s.9, s.10, s.13) | -- | **Redundant with the hub.** The `g_ij` model, circular causality, the high-volume-weakly-governed vs selective-organised contrast, the six lesions, the ephaptic open/closed-loop comparison, the consciousness bounding, and the seven-step strong-result chain are restatements of hub s.2, s.3, s.6, s.8, s.9, s.13 and s.17 |

**Redundancy verdict.** Of 436 lines, roughly 300 restate the hub. The file earns its own intake
because three of its formulations are the ones the claims are written from (s.14's triad, s.8's
factorial, s.7's decomposition grid), but it registers no claim the hub does not also motivate.

---

## 3. Key formulations (verbatim)

- "A static bridge asks whether sender state `x` can be mapped into a receiver-compatible state `y`.
  A dynamic-routing account asks an additional question: At which moments, and under which
  organism-level state, does the sender's information become causally effective at the receiver and
  then propagate onward?"
- "The candidate mechanism should therefore be treated first as a **diagnostic hypothesis about
  effective connectivity**, not as a new semantic module."
- "the useful comparison is not `more information` versus `less information`. It is: high-volume,
  weakly governed transfer vs selective, directionally organised transfer."
- "A positive observational result would justify perturbation. A null result should sharply reduce
  priority for a new routing mechanism."
- "A stored episode may remain intact. The receiving system may remain competent. A static map
  between the two may even remain approximately valid. Yet retrieval can fail if the episode no
  longer recruits the correct route through downstream consumers."
- "Conversely, if shuffled routing has no cost once the representational bridge is correct,
  causal-access correspondence is unnecessary."
- "The strongest null would also be valuable ... That result would tell us not to build this layer."
- "**jurisdiction itself has dynamics, propagation, repertoire, and history**."

---

## 4. Affected existing claims

Cross-referenced via `depends_on` only. **Nothing's status, confidence, evidence, `live_status` or
any other field was touched.** The set is the hub's; the claims this file bears on most directly are
**MECH-540**, **MECH-548**, **MECH-529**, **MECH-272**, **MECH-271**, **MECH-092** (the replay
lineage, via s.8) and **MECH-537**, **MECH-538**, **MECH-539**, **ARC-139** (the bridge lineage, via
s.7).

One distinction is recorded so a later session does not merge them: **s.7's "routing without bridge
sufficient" branch is MECH-537's F-series read backwards.** If appropriately timed gain exposes
already-compatible content without any transformation, the apparent interface failure was a
gating/access failure -- which is the same phenotype MECH-537 names ("encoded but not exposed") with
a temporal rather than a subspace cause. An experiment that cannot separate the two tests neither.

---

## 5. Candidate claims -- REGISTERED this pass

**No claim is registered from this file alone.** It contributes the primary source formulation for
two claims registered in the hub intake and drafted in `proposed_claims.yaml`:

- **ARC-145** (content / legibility / jurisdiction; s.1, s.7, s.14) -- this file is cited as a
  `source_document`.
- **the deferred replay-access-maintenance claim (NOT registered 2026-09-15; proposed to /governance as a MECH-540 signature; see the hub intake section 5)** (causal access correspondence; s.8) -- this file is its `source_thought`, because
  the factorial discriminator originates here.

It also supplies the adversarial-control checklist and the staging that MECH-560 and the deferred propagation claim (NOT registered 2026-09-15; see the hub intake section 5)
adopt in their notes.

---

## 6. Deliberately NOT registered

- **The six lesions** (gain flattening, direction shuffle, phase/timing shuffle, propagation break,
  stereotyped slow-mode lock, top-down removal) -- instruments. Note that *propagation break* is
  nonetheless promoted to the deferred propagation claim (NOT registered 2026-09-15; see the hub intake section 5)'s **primary falsifier**, because there it asserts something
  about REE rather than describing a procedure.
- **The four-way bridge decomposition grid** (s.7) -- instrument; belongs in the work programme.
- **The six-stage programme** (s.12) -- doctrine, recorded on the location doc.
- **The ephaptic relationship** (s.9) -- the computational question is registered as Q-106; the
  biology is out of domain and explicitly not registered ("No result here should be described as
  evidence that REE has an ephaptic mechanism").
- **The consciousness relation** (s.10) -- SENT-0 / SENT-1 own it.

---

## 7. Implementation-gap audit

**See the hub intake's section 7 for the full 13-row audit.** The rows this file's assertions bear
on directly, restated in one line each:

- s.4's candidate directed interfaces (`rawfield25` -> decision-facing state; world representation
  -> E1; -> E2; E1 -> consequence consumer; hippocampal proposal -> E3 selection; replay -> E1;
  control-plane state -> interface gain; decision-facing state -> committed action) are all
  **recordable**, but **no time-resolved directed-influence estimator exists** to score them
  (hub audit row 5, GENUINE GAP).
- s.4's exclusion of trained `z_world` as an admissible sender is **correct and current**
  (V3-EXQ-1010, PASS / `H-F-confirmed`, run `20260909T195348Z`); `rawfield25` primary,
  `ws250_pca32` replication.
- s.7's bridge ladder is **now BUILT** (`ree-v3 experiments/_lib/interface_probe.py`, commit
  `a83f2fb`, on `origin/main`) -- the file's implicit assumption that it is missing is stale
  (hub audit rows 1-2).
- s.8's replay arms need `(sender_episode, receiver_episode)` pairing, which **does not exist in
  `ree_core`** (hub audit row 12, GENUINE GAP) -- and MECH-092's `_do_replay` still has no consumer
  (hub audit row 11). The arms must therefore be experiment-layer over frozen endpoints.
- s.6's lesions need an inter-engine gain overlay; **no `g_ij(t)` exists** (hub audit row 7), and
  the nearest built analogues are `VsRolloutGate` (MECH-269b, default OFF), `BroadcastOverrideRegulator`
  (SD-037 -- which is precisely this file's own "mere arousal scalar" rival) and `RoutingGate`
  (MECH-272, a fixed three-row sleep-phase table, default OFF).
- s.12 Stage 6 ("architectural consideration"): **nothing in `substrate_queue.json` plans any of
  this** (hub audit row 13).

---

## 8. Next steps

1. `/lit-pull` owed on the whole family's anchors -- see hub section 8 item 1. This file cites none
   of its own.
2. **the deferred replay-access-maintenance claim (NOT registered 2026-09-15; proposed to /governance as a MECH-540 signature; see the hub intake section 5) should fan into hippocampal assay B, not become a new experiment family** -- the
   hub's DIG-5 instruction, and this file's s.8 is written as an extension of assay B. Whether the
   replay-maintenance supplement's s.6.3 timing test becomes that assay or stays a control is a
   `/governance` decision.
3. The **four-way decomposition grid (s.7) and the eight adversarial controls (s.11)** should be
   routed to `evidence/planning/mutual_legibility_work_program_20260907.md` as ML-13 / ML-14 arm
   extensions -- the same route the 2026-09-08 receiver-conditioned intake used for its seven-arm
   assay. That is a chip, not an ingestion action.
4. Hardening deferred -- `digestion_drafts.md` carries the draft non-degeneracy / confirming /
   falsifying triads for the deferred replay-access-maintenance claim (NOT registered 2026-09-15; proposed to /governance as a MECH-540 signature; see the hub intake section 5) and for MECH-540, which this file bears on most directly.
