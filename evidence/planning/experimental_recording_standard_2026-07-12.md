# Experimental Recording Standard (V3+)

**Status:** PROPOSED (v0.3) — authoring standard, adopt-forward. Landed 2026-07-12; amended 2026-08-12 (mandatory provenance subset), 2026-09-09 (v0.2: flat scalar readout, §3b), 2026-09-09 (v0.3: re-derivable criteria, §3b).
**Owner doc for:** what every experiment manifest must durably record, regardless of the immediate scientific question.
**Sibling policy:** [`arm_reuse_fingerprint_plan.md`](arm_reuse_fingerprint_plan.md) — the arm-reuse fingerprint is ONE instance of the general principle this doc generalises (see §1).
**Enforced via:** `/queue-experiment` skill Step 3 (authoring) + Step 3.5 (code review). Consumed by `evidence/experiments/scripts/build_experiment_indexes.py`.

---

## 1. Why this exists (the general principle)

REE already runs one form of *prospective epistemic-uncertainty management*: the **arm-reuse fingerprint** mints reusable baseline arms so a later iteration skips re-training a byte-identical baseline (`arm_reuse_fingerprint_plan.md` §7b/§9). That is not a one-off trick — it is one instance of a general rule:

> **Cheaply record now what a future question might need. Storage is far cheaper than recompute, so bias toward GENEROUS over-recording of readouts, from more points, durably.**

The specific gap this standard closes: **experiment manifests record only the readouts relevant to the immediate scientific question.** Later we repeatedly discover that the SAME run could have answered a *different* question had more readouts been durably recorded — and the answer is a re-run instead of a re-read. The arm-reuse fingerprint fixed this for baseline *arms*; this standard extends the same logic to *readouts*.

### The empirical case (why this is real, not hypothetical)

A corpus audit (2026-07-12, 564 flat manifests + 2487 pack manifests, EXQ 028–741) plus the arm-reuse plan's own scan (2026-06-06, 315 manifests) found systemic under-recording of high-reuse readouts:

| Readout | Recorded in | Reuse value |
|---|---|---|
| Raw per-episode returns / trajectories | **0%** | learning curves, variance, early-stopping analysis |
| Substrate commit / code hash | **0%** (flat) / present only in `arm_fp/v1` cells | proving "last week's baseline == this week's" — the reuse prerequisite |
| Latent-representation stats (`eff_rank`, forward-model R2) | **5%**, flat across eras | the substrate thesis is *about* representation quality |
| Exploration / coverage stats | **5%** | any exploration question |
| Full `config` snapshot | **29%** (era-erratic: one era only 2/120) | reproducibility, arm-reuse fingerprinting |
| Explicit seed **list** | **15%** | per-seed re-analysis, reproducibility |
| Timing / wallclock / compute cost | **23% and regressing** (recent era 15/145) | cost accounting, mint prioritisation |
| Per-seed distributions (not just mean±std) | **60%** | recovering individual seed values; 40% still collapse to mean |
| Arm-reuse fingerprint (`arm_fp/v1`) | 28% overall, **77% in recent era** | the counter-trend — the model this standard copies |

### Concrete incidents where recording-debt forced a re-run

- **V3-EXQ-732a** (30x-budget PPO re-run) was avoidable had a **local-view-achievable competence ceiling** been recorded at 724-time. Its absence forced both the confounded 732a re-run AND a *wholly dedicated* run (**V3-EXQ-738**) whose only job was to belatedly record that one denominator (6.05 @D0 → 48.05 @D3). Once recorded, 738's ceiling became the reusable anchor for the entire 734/735/736/737 fan-out — one well-recorded readout retired a confound for four downstream diagnostics. (`failure_autopsy_V3-EXQ-732a_2026-07-10.json`; MEMORY `reference_competence_floor_observability_confound.md`)
- **V3-EXQ-047m** self-stamped `non_degenerate: true` (a false clear) because it recorded only the *probe* partition balance, not the **training-label** class balance (`is_world` ran ~93% positive). The autopsy had to reconstruct the saturation arithmetically. A recorded training-label balance field would have caught the invalidity pre-run and averted the 047m→741 rebuild. (`failure_autopsy_V3-EXQ-047m_2026-07-11.md`)
- **V3-EXQ-643** re-trained, from scratch, a baseline that 604a/569d already computed — because no substrate hash was recorded, the earlier cell could not be *proven* identical and reused. This is the motivating incident for the arm-reuse fingerprint programme itself. (`arm_reuse_fingerprint_plan.md:100-106`)

The recurring shape: **the run that would have answered the later question already happened; it just didn't durably write down the readout.** A false cache-miss on a readout is free; the cost of the omission is a multi-hour re-run.

---

## 2. Design principles (from industry standards)

Distilled from MLflow, Weights & Biases, FAIR, W3C PROV, RO-Crate, Model Cards / Datasheets, and the Allotrope Simple Model (ASM). Attribution in parentheses.

1. **Small mandatory identity+provenance core beneath a large typed optional payload.** Every record carries the same fixed provenance skeleton; the scientific body is family-typed and open-ended. (*ASM: mandatory data-system + device-system documents beneath 65 typed technique schemas; MLflow: auto RunInfo + system tags vs optional params/metrics.*)
2. **Auto-capture provenance; log readouts deliberately.** The "how it was made" (code version, git commit, inputs used, outputs generated, environment, agent) is exactly what the immediate question ignores but reuse depends on — so capture it automatically and always. (*MLflow system tags; W&B auto git+system capture; PROV Entity/Activity/Agent + used/wasGeneratedBy/wasDerivedFrom.*)
3. **Over-record because metadata outlives data and recompute is expensive.** FAIR A2 mandates metadata survive the data itself; reuse (R1) needs more attributes than the originating question used. A `custom_information` catch-all makes over-recording lossless. (*FAIR A2/R1; ASM custom-information-document.*)
4. **Version the record from the inside; evolve additively; validate softly.** A self-declared `schema_version`, additive-only fields, and warn-don't-fail validation keep old records interpretable as the schema grows. Correction is a *new superseding record*, not an edit — matching REE's existing EXQ-letter supersession. (*RO-Crate `conformsTo`; ASM dated schemas + soft validation.*)
5. **Separate raw from derived; make every derived value traceable to its source.** Raw readings and calculated values live in distinct sections; each derived value back-points to the raw ids + the parameters that produced it. (*ASM measurement vs calculated-data with data-source back-pointer; PROV wasDerivedFrom.*)

Sources: MLflow <https://mlflow.org/docs/latest/ml/tracking/>; W&B <https://docs.wandb.ai/guides/track/>, <https://docs.wandb.ai/guides/artifacts/>; FAIR <https://www.go-fair.org/fair-principles/>; PROV <https://www.w3.org/TR/prov-primer/>; RO-Crate <https://www.researchobject.org/ro-crate/>; Model Cards <https://arxiv.org/abs/1810.03993>; Datasheets <https://arxiv.org/abs/1803.09010>; ASM <https://www.allotrope.org/asm> (also the local `bio-research:instrument-data-to-allotrope` skill, read as a worked example).

---

## 3. The standard

### 3a. `recording_schema` version

Every manifest SHOULD carry a top-level string `recording_schema: "rec/v1"`. This is the self-declaring version primitive (principle 4): a future reader/validator keys interpretation off it, and it lets the standard evolve additively without breaking old records. Absent = pre-standard (grandfathered), interpreted best-effort. This does NOT replace `architecture_epoch` (which versions the *substrate*); `recording_schema` versions the *manifest shape*.

### 3b. ALWAYS-record core (every manifest, every experiment_purpose)

Most of these are already conventions; this consolidates them as a mandatory floor. **Bold = currently under-recorded, newly mandatory.**

**Identity & lineage**
- `run_id` (ends `_v3`), `architecture_epoch: "ree_hybrid_guardrails_v1"`, `queue_id`
- `claim_ids` (list actually tested); `experiment_purpose` ∈ {evidence, diagnostic, baseline}
- `supersedes` (if a lettered correction), `recording_schema`
- `timestamp_utc` — ISO-8601 string via `datetime.utcnow().strftime("%Y%m%dT%H%M%SZ")`, never `int(time.time())`

**Provenance (auto-capture — principle 2)**
- **`substrate_hash`** — content hash over the substrate the run executed against (`ree_core/**` + env + `_lib/**`). This is the single highest-value newly-mandatory field: 0% of flat manifests carry it today, and its absence is *the* reason no historical baseline can be safely reused (`arm_reuse_fingerprint_plan.md:128-133`). Reuse it from the arm-fingerprint machinery: `arm_results[i].arm_fingerprint.substrate_hash` already computes exactly this — hoist one copy to the top level. For a single-arm run, compute it via `experiments/_lib/arm_fingerprint.py` helpers.
- **`substrate_commit`** *(added 2026-07-30)* — `{commit, dirty, branch?, dirty_count?, dirty_paths?}`: **which ree-v3 commit** the `substrate_hash` above corresponds to. (`dirty_paths` is capped at 20 with `dirty_count` recording the true total, so a truncated list cannot read as the complete set.) Stamped automatically by `stamp_recording_core`; no driver change needed. **This is the diagnosis half of a detect/diagnose pair, and neither field replaces the other.** `substrate_hash` proves two runs executed different substrate and covers uncommitted edits no SHA can express — but it is opaque, so it cannot say *what* differed. Motivating case: V3-EXQ-614 vs 614a had bit-identical driver bodies, identical seeds and a field-for-field identical `config_summary`, and flipped a verdict FAIL -> PASS purely because `e3_diversity_entropy_lambda` went 0.05 -> 0.5 in ree-v3 `a45ca7f` between the runs; a hash pair says "these differ", a commit pair reduces it to one `git diff` (`recovered_stranded_manifests/RESOLVED_arm2_allon_nonreproducibility_2026-07-30.md`). `dirty` is scoped to exactly the trees `substrate_hash` hashes — an unscoped `git status` would read dirty on nearly every run in these shared multi-session checkouts (one open `experiment_queue.json` edit suffices) and carry no information. It is the one always-core field with an **environmental** dependency: it needs a real git checkout, so it is legitimately absent in the `.git`-less tree `remote_pytest.sh` stages. Absent always beats wrong — the helper fails open rather than recording a SHA resolved against some parent process's repo.
- **`substrate_identity`** *(added 2026-08-07)* — `{source, resolved_at_utc, stamped_at_utc, lag_seconds, drifted_since_resolved, stability_snapshots, hash_on_disk_at_stamp?, commit_source, commit_resolved_at_utc, commit_describes_recorded_hash?}`: **when** the two fields above were captured, not only what they were. Stamped automatically by `stamp_recording_core`; no driver change needed. **A substrate hash is a claim about the past, and until this field existed nothing recorded whether the recording instrument was looking at the past or at the present.** `source: process_snapshot` means the identity was frozen at the first arm cell (or by an explicit `pin_recording_substrate` call) and so *is* the executed substrate; `per_cell_fingerprint_hoist` inherits a cell's; `manifest_write_disk_read` means the stamp was the first look, so the value describes the checkout **as it is at manifest-write time** and coincides with what executed only if nothing moved. Motivating case: **V3-EXQ-866b**, a diagnostic whose entire purpose was certifying that the substrate still reproduced V3-EXQ-603q, recorded a top-level hash (`8e275408`) read **2h26m after** its seed cells, which had themselves pinned `bb755658` — so the one run that most needed to name its own substrate could not (`failure_autopsy_V3-EXQ-866b_2026-08-07.md` §2). Root cause was `manifest_core.compute_single_arm_substrate_hash` calling `compute_substrate_hash` directly, bypassing the 2026-07-20 process-snapshot fix that the per-cell path already used; the two disagreed by construction on any multi-hour run against the shared hub checkout. **Fixed at the value level too** — the single-arm path now resolves through the process snapshot, so every driver that stamps at least one `arm_cell(script_path=Path(__file__))` records the executed hash automatically. Corpus at the time of the fix: 10 of the 249 manifests carrying a `substrate_hash` recorded the write-time read instead of the executed one, with lags up to two days.
  - **A driver that stamps NO arm cell should call `manifest_core.pin_recording_substrate(script_path=Path(__file__))` once at run start** (560 of 915 manifest-writing drivers are in this class). Nothing else can pin the identity for them, and the **commit** half in particular is unrecoverable after the fact: `git rev-parse HEAD` at manifest-write time answers about the tree that *replaced* the executed one. Without the pin such a run still records honestly — `source: manifest_write_disk_read`, and `commit_describes_recorded_hash: false` when the tree moved — but the value is a write-time read.
  - **`stability_snapshots` is how you tell a vacuous `substrate_stable_across_run: true` from an earned one.** A process that froze no identity has nothing to re-check, so its stability verdict is true by default and looks identical on the page to a real one.
- **`machine` / `machine_class`** — where it ran (Mac vs `linux-x86_64-py3.10` cloud); already 56%, make it universal (fingerprint equality is machine-class-bound).
- **`elapsed_seconds`** — wallclock; already 23% and *regressing*. Cheap, and load-bearing for mint-cost accounting and cost-bimodality analysis.

**Outcome & interpretation contract**
- `outcome` ∈ {PASS, FAIL, ERROR} (this exact field name)
- `evidence_direction` (+ `evidence_direction_per_claim` if `len(claim_ids) > 1`)
- `interpretation.preconditions[]` + `interpretation.criteria_non_degenerate{}` (required for diagnostic/baseline — existing gate)
- `non_degenerate` (+ `degeneracy_reason` when false) — existing scoring net

**Machine-readable verdict readout** *(added 2026-09-09)*
- **A FLAT SCALAR READOUT BLOCK**, top-level, under one of the four spellings the runpack converter recognises — **`readout`**, `metrics`, `aggregates`, `summary_metrics` (prefer `readout` for new drivers; the other three are the historical spellings and are read identically). **Pick exactly one.** The converter takes the **first non-empty** of `metrics` -> `aggregates` -> `summary_metrics` -> `readout` and never looks at the rest, so a manifest carrying a nested, non-scalar `metrics` block *and* a good flat `readout` scores from the `metrics` one and lands `values` with no numeric entries — the healthy block is unreachable. Its value must be a **flat dict whose entries are numbers**: the *pre-registered* scalars the verdict actually turns on — each criterion's measured value AND its bar, the decisive extremum, the seed/arm censuses, the degeneracy verdict. Not a nested structure, not prose.

  **This is the one always-core field whose absence disables a *check* rather than a self-description.** `evidence/experiments/scripts/sync_v3_results.py` builds a pack's `metrics.json` `values` by harvesting exactly those four **flat** spellings, and `build_experiment_indexes.py` then reads only the **numeric** entries of `values` (`_is_number`, l.315). A readout recorded only as a dict keyed by arm or by seed — which is what nearly every multi-arm driver emits — matches none of the four, so the pack scores with `values == {}`, and then:
  - **no `fail_if` stop threshold can fire.** The lookup returns `None`, the check is skipped, and `final_status` **falls back to the manifest's own self-declared status** — which is what `claim_evidence.v1.json` records and what drives auto-inferred evidence direction (`_evaluate_runs`, l.2512 / l.2542). The run grades its own homework with nothing able to contradict it.
  - **the duplicate-emission supersession fingerprint is skipped entirely** (l.2252, `if not run.metrics: continue`), so a byte-identical re-emission is never auto-superseded and **both copies score**.
  - **no deltas and no key-metrics columns** reach the index (`_select_key_metrics`, l.2578).

  No converter change can fix this. Flattening nested dicts converter-side would invent metric names that no `fail_if` rule or plan could ever have referenced; only the producer knows which scalars the verdict turns on. It is a **driver-side recording obligation**. **Keep the nested blocks** (`arm_results`, `per_seed_*`, `cell_summary`, `per_arm_gate`) exactly as they are — they are the human-readable record, the flat block is its machine-readable projection, and the two are not redundant.

  **Two encoding rules, both forced by the consumer — get either wrong and the field is present but inert:**
  - **Booleans MUST be emitted as `0` / `1` ints.** `_is_number` excludes `bool` (an int subclass) deliberately, so a raw `True` is *recorded in the manifest and invisible to the indexer*. Measured 2026-09-09: **82** flat manifests carry a raw bool inside their readout block — including V3-EXQ-484's four `C1..C4` criterion verdicts and V3-EXQ-177's three `c*_both_seeds` flags, i.e. in both cases precisely the scalars those runs' verdicts turned on.
  - **Non-finite and `None` values MUST be DROPPED, not emitted.** A `nan` *is* numeric to `_is_number` and pollutes any delta computed from it; an absent key correctly reads as **unmeasured**, which is the honest answer. Measured 2026-09-09: **12** flat manifests emit a `nan` or a `null` inside their readout block (V3-EXQ-165's two `fixed_value_*` entries are `nan`; V3-EXQ-794 / 816 / 820 / 829 emit `null`s).

  Reference implementation: `ree-v3/experiments/v3_exq_1015_mech465_zworld_warmup_budget_dispersion_sweep.py` — its `_flat_scalar()` projection and `readout` block, ~50 lines, all projection of quantities the driver had already computed. No science change, no re-run.

  **Exempt:** a crash / `outcome: ERROR` report and a `dry_run: true` manifest. Neither records a verdict, so neither has scalars to pre-register (22 of the 727 no-readout flat manifests as of 2026-09-09).

  **Coverage at adoption (2026-09-09).** Flat manifests: 206 of 1008 carry a readout block with at least one numeric entry; **727 carry none of the four spellings** and 75 carry one with no numeric entry. Packs: **1094 of 2931 score with no numeric `metrics.values`**, and the rate for packs emitted in 2026-09 is **65%** — so this is the current default, not a legacy backlog (`flat_scalar_readout_recording_gap_20260909.md`). Enforcement is therefore **WARN-only in both validators and never blocking, including under `--strict`** — a gate that fires on two thirds of ordinary work gets disabled, which is worse than no gate. See §4.

**Re-derivable criteria** *(added 2026-09-09)*
- **Every LOAD-BEARING criterion MUST record its MEASURED value and the THRESHOLD it was compared against, as separate machine-readable numeric fields** — not as prose inside a `note` / `detail` / `description`. **With more than one load-bearing criterion, also record `combination_rule`** (which had to hold, and how they combine).

  **A criterion recorded `passed: true` with no measured value and no bar is an ASSERTION, not a record.** Nothing in the artifact says what was measured or what it was compared against, so the verdict cannot be checked by anyone reading the manifest — only by opening the driver source and reconstructing the arithmetic.

  **This is the field whose absence disables a specific, mandatory governance step.** `/governance` Step 2b's driver skim carries a threshold-arithmetic clause — *"given the magnitudes this run actually measured, is the bar attainable in both directions, or does one branch fire BY CONSTRUCTION"* — and that clause simply **cannot be discharged from the artifact** when the criterion records only `passed`. The cost is not hypothetical:
  - **V3-EXQ-936a** set an absolute bar **~7,900x above the maximum attainable effect** and was logged clean for **three consecutive governance cycles**. Its two load-bearing criteria record `name` / `load_bearing` / `passed` / `description`; both bars are interpolated into the `description` *prose*.
  - In the **2026-09-09 cycle alone** the gap forced three separate driver reads, and each found something the manifest could not have shown: **V3-EXQ-900** — the PASS label asserts a FUNCTIONAL half that no criterion tests, while the one functional discriminator recorded is flat to 0.09% (GFLAG-0246); **V3-EXQ-642b** — C1/C2 read a DV clamped at 1.5 in *both* arms, so separation is 0.0 **by construction** (GFLAG-0141); **V3-EXQ-231a** — C2 is an arithmetic consequence of C1 under the driver's own linear map, so it cannot fail when C1 passes (GFLAG-0163; **MECH-106 was demoted on it**).

  **Spell the two halves as a FAMILY, not one fixed pair.** The bar may be `threshold`, `threshold_rho`, `threshold_gap`, `threshold_delta`, `requirement`, `required`, `seeds_required`, `rho_floor`, `bar`, `tol` — any field whose name carries a threshold-family token (`threshold` / `thr` / `bar` / `requirement` / `required` / `floor` / `tol` / `tolerance` / `cutoff` / `minimum` / `target`). The measured half is not name-constrained at all: the corpus legitimately calls it `measured`, `mean`, `measured_rho`, `measured_max`, `measured_auc`, `n_seeds`, `seeds_clearing`, `deciles_scored`. **`measured` + `threshold` is the spelling to PREFER in a new driver**; the rest are historical and are read identically. This is not pedantry about names — the first draft of this rule used a literal spelling list and flagged **26 criteria across 14 manifests that record both halves perfectly well** (see the GOV-HELDOUT-1 record below).

  **Prose does not count, and that is the point rather than a limitation.** V3-EXQ-967 records `"max |energy_A - energy_B| over 2400 matched steps = 0.99 vs tol 1e-09"` in `detail` — both numbers present, neither readable. V3-EXQ-1014 records `measured: 0.25` with its three bars (`>= 0.95`, `>= 0.5`, `>= 3`) only in `description`. A governance skim that has to trust the artifact cannot use either.

  **Both container shapes count.** `criteria` appears as a LIST of dicts (116 flat manifests) and as a DICT keyed by criterion name (104). The dict form has two sub-shapes: values that are dicts (V3-EXQ-1004, which records `required` / `threshold` / `n_seeds` and is compliant), and values that are **bare booleans** — `{"C1_fast_wins": false, "C2_slow_wins": true, ...}` — which is **101 manifests and the worst-recorded shape in the corpus**. A criteria block that carries no `load_bearing` marker anywhere has not narrowed, so **every entry counts as load-bearing**. Without that clause a rule scoped to `load_bearing: true` would exempt the worst shape *by construction*; V3-EXQ-149b is PASS with `evidence_direction: supports` on three bare booleans, one of them FALSE, and no `combination_rule` saying how they combine.

  **Exempt:** a crash / `outcome: ERROR` report and a `dry_run` manifest (also a `_dry_`-prefixed filename — 24 dry manifests in the corpus and not all of them set the field). Neither records a verdict, so neither has criteria to re-derive. A single criterion may opt out with **`threshold_not_applicable: "<reason>"`** — for a genuine **count-based negative existential** ("0 occurrences across 2400 calls"), where a bar is not the right shape and inventing one would be *worse* recording, not better. Driver-wide opt-out: `CRITERIA_THRESHOLD_EXEMPT = "<reason>"`.

  **Coverage at adoption (2026-09-09).** Over the 983 live flat manifests (non-dry, non-ERROR) in `evidence/experiments/`: **216 carry a criteria block with an effective load-bearing set, and 184 of those — 85.2% — record NOT ONE load-bearing criterion carrying both a bar and a measured value. 53 of the 184 are `outcome: PASS`.** Only 32 (14.8%) are fully re-derivable, and **133 of the 216 have more than one load-bearing criterion and no `combination_rule`**. A corpus sweep with the shipped check reports 184 `none_rederivable`, 8 `combination_rule_only` (criteria re-derivable, but several are load-bearing with no stated combination) and **5 `criteria_unrecorded`** — manifests whose `combination_rule` names criteria the artifact never wrote down, the V3-EXQ-900 shape. `partial` is empty: in practice a driver records both halves for all its load-bearing criteria or for none. Authoring side: the lint warns on **461 of 1474 drivers (31.3%)**. Enforcement is therefore **WARN-only in both validators and never blocking, including under `--strict`** — a gate firing on that share of ordinary work gets disabled, which is worse than no gate. See §4.

  **The two rates differ because the two checks catch different halves, and the gap is itself the finding.** A driver can compute both numbers and drop them on the way to the manifest — **V3-EXQ-936a builds a separate verdict-grid dict carrying `measured`/`threshold` while its two actual load-bearing criteria carry only `passed` plus prose** — or build its criteria dynamically so no literal is statically visible at all (V3-EXQ-642b: zero criterion literals in source, manifest flagged). **So the common fix is a PROJECTION, not new computation**: most drivers already have both numbers in hand. Run both checks; neither subsumes the other.

**Reproducibility core (principle 5 prerequisite)**
- **`config`** — the full config snapshot (env params, hyperparameters, schedule). Only 29% today, era-erratic. Without it a run cannot be reproduced OR fingerprinted.
- **`seeds`** — the explicit seed *list* (not a single `seed` int). Only 15% today.

### 3c. GENEROUS optional payload (record by default; bias to over-record — principle 3)

These are the readouts whose omission has repeatedly forced re-runs. **Record them by default unless there is a specific reason not to** — the burden of proof is on *skipping*, exactly as for the arm-mint (`/queue-experiment` "Saving a baseline for reuse"). A false record is free; a false omission is a multi-hour re-run.

Keyed by experiment family:

**All multi-arm / multi-seed experiments**
- `per_seed_*[]` — every seed's full readout, never just mean±std (recover the distribution). 40% still collapse to mean.
- `arm_results[]` — per-arm structured rows, with OFF/baseline internals recorded **as richly as the ON arm** (principle: negative-class symmetry — do not drop OFF-arm latent/coverage internals).
- `arm_fingerprint` per cell (already validator-enforced for multi-arm; `arm_fp/v1`).

**Any experiment that trains or reads a label / classifier**
- **Training-label AND eval-label class balance** — the positive fraction of every binary/categorical label used, for training and eval separately. This is the 047m false-clear fix: the probe-partition guard is not enough; a saturated *training* label invalidates a run silently. Record it as `label_balance: {"<label>": {"train_pos_frac": ..., "eval_pos_frac": ...}}`.

**Any experiment where representation quality could matter (most of them)**
- Latent-representation stats: `zworld_eff_rank`, `e2_forward_r2`, `harm_r2_train/test`, effective rank of any encoder output touched. Only 5% today and NOT trending up despite being central to the substrate thesis. Copy the `v3_exq_740` block.

**Any experiment with exploration / a foraging or navigation env**
- Coverage / visitation-entropy / state-coverage stats (copy `449*/563/569/577`).

**Any diagnostic that gates on an achievable ceiling**
- The **achievable-ceiling denominator computed under the LEARNER's own observability** (not a privileged global oracle). The 732a→738 incident is the canonical cost of omitting this. If a criterion divides by "what's achievable", record the achievable number, its observability regime, and the policy that achieved it.

**Derived-vs-raw discipline (principle 5)**
- Where a manifest reports a calculated value (a ratio, a delta, an aggregate), also record the raw inputs it was computed from, or a back-pointer to them. Do not store only the derived scalar.

**Catch-all (principle 3, makes over-recording safe)**
- `custom_information: {}` — any readout that doesn't map to a standard field goes here verbatim rather than being dropped. Nothing observed should be discarded for lack of a slot.

### 3d. Storage & versioning guidance

- **Additive-only.** New fields are optional additions; never repurpose or remove an existing field's meaning (principle 4). Old manifests stay forward-readable by construction.
- **Correction = supersession, not edit.** A re-run with corrected recording gets a new letter and `supersedes` the prior; the predecessor is marked `evidence_direction: superseded`, not overwritten (existing EXQ-letter policy).
- **Large artifacts by reference.** Checkpoints and bulky arrays: store a content-addressed path/pointer in the manifest, not the blob inline (W&B/MLflow artifact-store pattern). Manifests stay diffable; the git-tracked coordination plane stays lean.
- **Storage is cheap relative to compute.** Run cost is bimodal — most runs are ~23s, a minority run tens of minutes to ~19h (`arm_reuse_fingerprint_plan.md:117-126`). The readouts worth over-recording are concentrated on the expensive runs, where a re-run is exactly what over-recording avoids. Over-record without agonising over per-field storage.

---

## 4. Enforcement surface (where this bites)

The audit found **no single enforcement point today**: only 1 script imports the sanctioned `ree-v3/experiments/pack_writer.py`; 79 scripts define their own `write_manifest`, and 106 write JSON directly with bespoke schemas. And `pack_writer._clean_numeric_metrics` (line ~236) *coerces every metric to a scalar* — it structurally cannot store per-seed lists, `arm_results`, or nested readouts, which is the mechanical reason packs are flat and rich readouts survive only in hand-rolled manifests. So enforcement is **authoring-time discipline via the skill**, backstopped by review, now with a shared mechanical stamper + a relaxed writer + a linter beneath it (below):

1. **`/queue-experiment` Step 3 (authoring)** — a "Record generously" block instructs the author to emit the always-core + the family-keyed optional payload. (Landed 2026-07-12.)
2. **`/queue-experiment` Step 3.5 (code review)** — a checklist group verifies the always-core is present and the family-appropriate optional readouts are recorded before smoke test. (Landed 2026-07-12.)
3. **`failure-autopsy`** — when an autopsy concludes "we couldn't tell because X wasn't recorded", it should cite this standard and route the fix to *recording* X in the re-run, not just re-running. (Reference added; see that skill.)
4. **`view-experiments`** — surfaces which manifests carry the rich readouts vs which are thin (reader-side visibility; optional follow-up).
5. **`experiments/_lib/manifest_core.stamp_recording_core(...)`** — the shared always-core stamper: one no-op-safe call stamps `recording_schema` + `substrate_hash` (multi-arm hoist / single-arm compute via `arm_fingerprint.py`) + `machine`/`machine_class` + `elapsed_seconds` + full `config` + explicit `seeds`. This is the highest-value item (0% of flat manifests carried a substrate hash). (Landed 2026-07-12.)
6. **Flat-scalar-readout WARNs (added 2026-09-09)** — two advisory checks, at the two moments the gap is cheapest to close:
   - **`ree-v3/validate_experiments.py` -> `flat_scalar_readout_lint`** (AST, on the *driver source*) — fires at **authoring** time, which is where `/queue-experiment` Step 3.5 catches it **before compute is spent**. WARN-only in BOTH modes: it never hardens under `--paths`, unlike the arm-fingerprint / degeneracy / manifest-writer gates, because 703 of 1358 manifest-writing drivers would trip it today.
   - **`ree-v3/validate_recording.py` -> `check_flat_scalar_readout`** (on a *written manifest*) — fires on the smoke-test manifest and on a corpus sweep, and distinguishes *absent* from *present-but-no-numeric-entries* (the bool-only shape). **Deliberately advisory even under `--strict`**, unlike that linter's always-core and thin-pack arms: `/queue-experiment` Step 3.5 runs it with `--strict`, and blocking there would gate every new driver on a field 80% of the landed corpus lacks.

7. **Criteria re-derivability WARNs (added 2026-09-09)** — two advisory checks, the same producer/artifact pair as item 6, and for the same reason: neither can see what the other sees.
   - **`ree-v3/validate_experiments.py` -> `criteria_threshold_lint`** (AST, on the *driver source*) — fires at **authoring** time via `/queue-experiment` Step 3.5, before compute is spent. Scans the **load-bearing criterion dict literals** and warns when any of them carries no threshold-family field. Scoping matters: a file-wide token scan was the first cut and is effectively inert (4 of 1474 drivers), because almost every driver says "threshold" or "bar" *somewhere* — V3-EXQ-936a mentions `bar`, `threshold`, `thresholds`, `required` and `target_met` in unrelated code while its two load-bearing criteria record neither half. WARN-only in BOTH modes.
   - **`ree-v3/validate_recording.py` -> `check_criteria_thresholds`** (on a *written manifest*) — fires on the smoke-test manifest and on a corpus sweep. Distinguishes `none_rederivable` / `partial` / `criteria_unrecorded` (a `combination_rule` naming criteria the manifest never wrote down — the V3-EXQ-900 shape) and reports a missing `combination_rule` alongside. **Deliberately advisory even under `--strict`**, like the readout arm and unlike this linter's always-core and thin-pack arms.
   - Contracts: `ree-v3/tests/contracts/test_criteria_threshold_lint.py` (14) and the `CheckCriteriaThresholds` / `CriteriaNeverBlocksUnderStrict` / `CriteriaThresholdTokensAgree` classes in `ree-v3/test_validate_recording.py`. The threshold-family token set is pinned identical on both sides so it cannot drift at one enforcement moment only.


### Deferred-hardening status (landed 2026-07-12)

- ✅ **Relaxed `pack_writer`** to carry structured `per_seed`/`latent`/`config`/`timing` sections VERBATIM beside the scalar `values` block (`_clean_structured_sections`), so the sanctioned writer no longer drops rich readouts. `values` stays scalar (indexer-safe); the change is backward-compatible (a scalar-only caller is byte-identical). **Still open:** making `pack_writer` the *mandatory single writer* across the ~106 direct-writers (a larger migration).
- ✅ **Widened `build_experiment_indexes.py`** so `substrate_hash` + `label_balance` are surfaced/queryable on index entries (`_FLAT_AUTHORITATIVE_FIELDS`, `RunRecord`, `_scan_runs`, the entry + `unlinked_runs` emitters). Verified inert on the current corpus (a full rebuild is byte-identical modulo `generated_at`). **Deliberately still open:** making these fields *load-bearing* to confidence scoring — that changes promotion math and needs user sign-off.
- ✅ **Added `ree-v3/validate_recording.py`** — soft-validate linter: WARN on a manifest missing the always-core, `--strict` to exit non-zero; forward-compatible (unknown `recording_schema` warns, never fails) per §3d.
- ✅ **(2026-08-12) `write_flat_manifest` now HARD-ENFORCES a subset of the always-core, not just soft-WARNs.** `experiments/_lib/manifest_core.MANDATORY_CORE_KEYS` (`recording_schema`, `substrate_hash`, `machine`, `machine_class` — the fields `stamp_recording_core` computes with zero caller effort AND zero environmental dependency) is checked immediately after the manifest is written to disk; if still missing, `write_flat_manifest` raises `ValueError` (escape hatch `REE_ALLOW_INCOMPLETE_PROVENANCE=1`, matching the repo's other explicit-override env vars). Checked AFTER the write so a run's already-spent compute is never lost — a gap here fails the *call*, never the *capture*. Motivated by the audit finding this doc opens with: 0% of flat manifests recorded a substrate hash, and the soft-WARN alone (landed above) left that gap invisible unless someone ran the linter by hand. Two fields are deliberately **not yet** in the hard-enforced subset: `elapsed_seconds`/`config`/`seeds` need a caller kwarg — an AST census of `experiments/v3_exq_*.py` found `config=`/`seeds=` present at 98.4% of `write_flat_manifest` call sites but `elapsed_seconds=`/`started_at=` present at only 37.2% (47.7% of call sites truly missing it), and hard-enforcing those three now would break roughly half of the active experiment fleet's next run; and `substrate_commit` — despite needing no caller kwarg — needs a real git checkout (§3b above already documents it as "legitimately absent in the `.git`-less tree `remote_pytest.sh` stages"), which the first cut of this hardening missed and which broke 10/84 tests in `tests/contracts/test_z_goal_stream_counter.py` on exactly that staged tree before being caught and narrowed out the same day. Investigation + implementation: `chip-20260812-recording-standard-mandatory-provenance`, following on from `REE_assembly/evidence/planning/architecture_epoch_investigation.md` (2026-08-12) sections 2/10, which found this gap.
- ⚠️ **(2026-09-09) The flat scalar readout is always-core but enforced WARN-ONLY, and stays that way for now.** §3b's new "Machine-readable verdict readout" field is checked by `validate_experiments.flat_scalar_readout_lint` (authoring) and `validate_recording.check_flat_scalar_readout` (written manifest), both advisory in every mode — the readout arm of `validate_recording.py` does **not** block even under `--strict`, unlike its always-core and thin-pack arms. Deliberately not hardened: the corpus measurement is 703 of 1358 manifest-writing drivers and 802 of 1008 flat manifests, so a hard gate would fire on the majority of ordinary work, which per CLAUDE.md is how a gate gets disabled. It is also **not** added to `manifest_core.ALWAYS_CORE_KEYS` / `MANDATORY_CORE_KEYS`: those two constants drive `missing_core_fields` (the `--strict`-blocking arm) and `write_flat_manifest`'s hard raise respectively, and the readout is the one always-core field the stamper structurally *cannot* compute — only the driver knows which scalars its verdict turns on. Hardening path, if the corpus ever closes: add it to `ALWAYS_CORE_KEYS` and delete the separate check arm. Measurement + recommendation: `flat_scalar_readout_recording_gap_20260909.md`; investigation `chip-20260909-recording-standard-flat-scalar-readout`.

- ⚠️ **(2026-09-09) Criteria re-derivability is a §3b requirement enforced WARN-ONLY, and stays that way for now.** §3b's new "Re-derivable criteria" field is checked by `validate_experiments.criteria_threshold_lint` (authoring) and `validate_recording.check_criteria_thresholds` (written manifest), both advisory in every mode — the criteria arm of `validate_recording.py` does **not** block even under `--strict`. Deliberately not hardened: 184 of 216 flat manifests carrying load-bearing criteria record none that is re-derivable (85.2%), and 461 of 1474 drivers trip the lint, so a hard gate would fire on a large share of ordinary work — which per CLAUDE.md is how a gate gets disabled. It is also **not** added to `manifest_core.ALWAYS_CORE_KEYS` / `MANDATORY_CORE_KEYS`, for the same structural reason as the flat scalar readout: those constants drive the `--strict`-blocking arm and `write_flat_manifest`'s hard raise, and only the driver knows what its criteria measured and what bar they were held to. Hardening path, if the corpus closes: add the check to the `--strict` exit condition in `validate_recording.main` (the two contract classes `ReadoutNeverBlocksUnderStrict` / `CriteriaNeverBlocksUnderStrict` pin the current behaviour and would need updating together). **Explicitly out of scope and NOT decided here: back-filling the 53 already-PASSING manifests this check flags.** That is a separate and much larger question with real scoring implications — a criterion whose bar turns out to be unattainable may invalidate the run's verdict, not merely its recording — and it belongs to governance, not to a recording-standard amendment. Recommendation only: triage the 53 PASS manifests by claim load-bearingness before deciding, since most will be inert and a handful (the V3-EXQ-936a shape) will not. Investigation: GFLAG-0249, chip `chip-20260909-criteria-measured-threshold-recording-standard`.

---

## 5. Relationship to the epistemic system

This standard is the *input-quality* sibling of the governance epistemic system. Better-recorded readouts mean:
- More runs are reusable (fewer forced re-derivations → the arm-reuse fingerprint gets more cache hits).
- More readouts are available to become *load-bearing* evidence if the indexer's consumed-field set is later widened (§4 deferred).
- The confidence-scoring layer (`build_experiment_indexes.py`) has richer, more auditable inputs.
- **A *later* scientific question can sometimes be answered from data a prior run already recorded — no re-run.** This payoff is realized by the **consumer rule `GOV-REUSE-1`** (registered `governance_rule`, 2026-07-12): before a new experiment is queued, `/queue-experiment` **Step 2.4 (existing-evidence / reanalysis-first check)** asks whether the decisive readout is already recorded on a compatible `substrate_hash` (or derivable post-hoc from recorded raw inputs) and, if so, routes to a **recorded post-hoc reanalysis** citing the source `run_id`s instead of burning compute. Tooling: **`scripts/reanalysis_query.py`** (`query` groups manifests by `substrate_hash` and flags `MATCH`/`INCOMPATIBLE`/`UNVERIFIABLE` for a named readout; `emit` writes the recorded `reanalysis/v1` artifact under `evidence/reanalysis/` and refuses unless the sources share a compatible substrate). (The reanalysis artifact is recorded but not yet wired into `build_experiment_indexes.py` confidence scoring — a deliberate deferred follow-up.) `GOV-REUSE-1` is the forward `mystery (known-data)` router and the reason over-recording here is not waste; `substrate_hash` is the compatibility key that makes a reused readout trustworthy. (`/failure-autopsy`'s recording-gap pathway is the reverse case: a *past* run under-recorded.)

The recording standard *itself* could additionally be registered as a `governance_rule`-type claim (like GOV-DIAG-1 / GOV-FANOUT-1 / the now-registered GOV-REUSE-1) if it proves out — deferred pending adoption evidence. See the sibling analysis [`epistemic_system_formalization_options_2026-07-12.md`](epistemic_system_formalization_options_2026-07-12.md) for how the downstream representation could be formalised.

---

## 6. Changelog

- **2026-07-12** — v0.1 PROPOSED. Authored from a four-thread audit (manifest corpus survey, lineage re-run tracing, industry-standard research, epistemic-system survey). Skill enforcement landed in `/queue-experiment` Steps 3 + 3.5.
- **2026-08-12** — `write_flat_manifest` hard-enforces `MANDATORY_CORE_KEYS` (the auto-computed always-core subset) going forward; `elapsed_seconds`/`config`/`seeds` stay soft-WARN pending corpus-coverage cleanup. See §4 "Deferred-hardening status" above.
- **2026-09-09** — v0.2: §3b gains a **flat scalar readout block** (one of `readout` / `metrics` / `aggregates` / `summary_metrics`) as always-core, with two encoding rules (booleans as 0/1 ints; non-finite and `None` dropped). This is the first always-core field whose absence disables a *check* — with no numeric `metrics.values` no `fail_if` can fire, `final_status` falls back to the manifest's self-declaration, and the duplicate-emission supersession fingerprint is skipped. Enforced WARN-only in both validators (never blocking, including under `--strict`). GOV-HELDOUT-1 check run: **caught an over-broad rule** — see the record below. Measurement: `flat_scalar_readout_recording_gap_20260909.md`.
- **2026-09-09** — v0.3: §3b gains **re-derivable criteria** — every LOAD-BEARING criterion must record its MEASURED value and its THRESHOLD as separate machine-readable numeric fields (not prose), plus `combination_rule` when more than one is load-bearing. This is the field whose absence disables `/governance` Step 2b's mandatory threshold-arithmetic clause: V3-EXQ-936a's bar sat ~7,900x above the maximum attainable effect and was logged clean for three consecutive cycles. Enforced WARN-only in both validators (never blocking, including under `--strict`). GOV-HELDOUT-1 check run: **caught an over-broad rule twice** — see the v0.3 record below. Back-filling historical manifests is explicitly NOT decided here.

### GOV-HELDOUT-1 record for the 2026-09-09 v0.2 amendment (flat scalar readout)

Four cases, none of them the motivating pair (V3-EXQ-1015 / V3-EXQ-1014), each one the old wording and the new wording answer **differently** — a case both versions handle identically tests nothing:

| Case | OLD (§3b silent on readouts) | NEW | Right call? |
|---|---|---|---|
| **V3-EXQ-484** `sd033a_distractor_resistance` (2026-04-27) — `metrics` holds 4 numerics **and** the four `C1..C4` criterion verdicts as raw bools | compliant; the block is present and scores | the four verdicts must be `0`/`1` ints | **yes.** They are exactly the scalars the run's verdict turns on, and `_is_number` excludes `bool`, so they were recorded and invisible. Note the *presence* half agrees with OLD here — only the encoding rule is load-bearing on this case. |
| **V3-EXQ-165** `mech143_hippocampal_value_sensitivity` (2026-03) — `summary_metrics` holds 16 numerics **and** two `nan`s (`fixed_value_post_shuffle_harm_spike`, `fixed_value_adaptation_speed_mean`) | compliant | drop the two keys | **yes.** `_is_number` does *not* exclude non-finite, so a `nan` is numeric to the indexer and pollutes any delta computed from it; absent reads as unmeasured. Again the presence half agrees with OLD. |
| **V3-EXQ-1009** `mech267_elite_channel_ceiling_spike` (2026-09-07, PASS) — rich `arm_results` + `cell_summary`, no flat spelling; pack scores `values={}` | compliant — §3c asks for `arm_results` / `per_seed_*` and it has them | gap: needs a flat scalar projection | **yes.** A PASS recorded with no numeric `metrics.values` is a self-declaration no `fail_if` can contradict. 208 V3 PASS/FAIL manifests are in this exact state. Tests the *presence* half specifically. |
| **V3-EXQ-591g runner-error report** (`v3_v3_exq_591g_runner_error_20260902T201744Z_v3`, `outcome: ERROR`) — no quantitative block anywhere | compliant | **first draft: gap** | **NO — the rule over-fired.** A crash report records no verdict, so it has no scalars to pre-register. **Fixed:** `outcome: ERROR` / `*_runner_error_*` run_ids and `dry_run: true` manifests are now exempt in both checks (22 of the 727 no-readout flat manifests). |

Outcome: **caught an over-broad rule and narrowed it.** The check also confirmed the rule is not scoped to its motivating incident — the three positive cases span three lineages (sd033a, mech143, mech267) and three eras (2026-03, 2026-04, 2026-09).

### GOV-HELDOUT-1 record for the 2026-09-09 v0.3 amendment (re-derivable criteria)

Four cases, **none** of them from the motivating set (V3-EXQ-936a / 900 / 642b / 231a), each one a case where the old wording and the new wording answer **differently** — a case both versions handle identically tests nothing:

| Case | OLD (§3b silent on criterion fields) | NEW | Right call? |
|---|---|---|---|
| **V3-EXQ-1014** `ext002_lineage_e3_latching_repertoire_spike` (2026-09-08, PASS) — one load-bearing criterion recording `measured: 0.25` plus five censuses, with its **three bars** (`>= 0.95`, `>= 0.5`, `>= 3`) only inside the `description` string | compliant; §3b asks nothing of criteria, and the run does record a measured value | gap: the bar is not machine-readable, so `passed` cannot be re-derived | **yes.** Tests the *threshold* half specifically — the measured half is present and correct. |
| **V3-EXQ-967** `mech144_shuffle_inertness_confirmer` (2026-09-01, PASS) — load-bearing C1 records `detail: "max \|energy_A - energy_B\| over 2400 matched steps = 0.99 vs tol 1e-09"` | compliant | gap: both numbers are in prose | **yes.** The information genuinely exists in the artifact and is still unusable to every consumer and to a governance skim. Tests the *prose-does-not-count* clause. |
| **V3-EXQ-149b** `q004_tau_r_largebudget` (2026-08-04, **PASS**, `evidence_direction: supports`) — `criteria` is a bare `{"C1_fast_wins": false, "C2_slow_wins": true, "C3_default_ranked": true}` map with **no `load_bearing` marker anywhere** and no `combination_rule` | compliant | gap on all three, via the effective-load-bearing clause | **yes — and this case CHANGED the rule.** Scoped to `load_bearing: true` alone, as first drafted, the rule would have **exempted this shape by construction** — 101 flat manifests, the worst-recorded in the corpus, and here one supporting a claim on three bare booleans of which one is FALSE. **Fixed:** a criteria block carrying no `load_bearing` marker at all is treated as entirely load-bearing. Confirmed on an older era too (**V3-EXQ-193**, 2026-04-04, FAIL, same shape). |
| **V3-EXQ-1001 / 785 / 785a / 785b / 804 / 805 / 818 / 895 / 940 / 950 / 995 / 997 / 1004 / 1013** — 14 manifests spanning 2026-07 to 2026-09 that record **both** halves under family spellings: `mean`/`requirement`, `measured_rho`/`threshold_rho`, `measured_gap`/`threshold_gap`, `bar`/`measured_auc`, `measured_max`/`threshold`, `n_seeds`/`required`, `mean_rho_rv_vs_commit`/`rho_floor` | compliant | **first draft: gap on all 14** | **NO — the rule over-fired.** The first draft used a literal spelling list (`measured`/`measured_rho`/`measured_value`/`value` x `threshold`/`thr`/`bar`) and flagged **26 criteria across these 14 genuinely-compliant manifests**. Crying wolf on compliant work is how a check gets ignored (CLAUDE.md). **Fixed:** match a threshold-family **token**, and do not name-match the measured half at all — require a threshold-family numeric plus at least one other numeric. Re-verified: all 14 now read clean. |

Outcome: **caught an over-broad rule (twice) and narrowed it.** The check also forced two design corrections on the *implementation* rather than the wording — the AST lint's first cut scanned file-wide for a threshold token and was inert at 4 of 1474 drivers, and its second cut discharged a file when *any* criterion carried a bar, which still missed V3-EXQ-936a (a separate verdict-grid dict carries `measured`/`threshold` while the two load-bearing criteria carry only `passed` plus prose). The rule is not scoped to its motivating incident: the positive cases span four lineages (ext002, mech144, q004, q012) and four eras (2026-04, 2026-08, 2026-09-01, 2026-09-08).

**One measurement in the originating brief did not reproduce and is not carried into this document.** The GFLAG-0249 brief reported 534 manifests declaring a load-bearing criteria list, 486 (91.0%) with zero re-derivable criteria, 180 of them PASS, 40 fully re-derivable, and 489 with no `combination_rule`. Sweeping `evidence/experiments/` no definition reproduces a 534 denominator — the widest construction available (every flat *and* pack manifest carrying either a `criteria` block or a `combination_rule`) yields 233. The figures recorded in §3b above are measured under the predicate stated there and are reproducible from it. **The phenomenon is confirmed and the four named runs are exactly as described; only the magnitude differs** (85.2% rather than 91.0%, and 53 PASS rather than 180).
