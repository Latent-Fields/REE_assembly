# Thought intake -- EXQ-1010 and hippocampal A/B substrate-readiness audit

**Date processed:** 2026-09-15
**Raw thought:** `docs/thoughts/2026-09-09_exq1010_substrate_readiness_audit.md` (191 lines; the document dates itself 2026-09-10)
**Session:** thought-pipeline-20260915
**Family:** FAMILY D (hippocampal translation-maps campaign). The full novelty table for the family is in
`thought_intake_2026-09-09_hippocampal_campaign_adjudication.md` (the hub). This file carries the
family's IMPLEMENTATION-GAP AUDIT in full, because this raw file is the one that asserts the gap list.

**Status of the raw file, honoured:** it declares "This document makes no claim-registry, proposal,
queue, runner, or substrate mutation." Nothing here registers a claim from it. What it produces is a
READINESS LEDGER, and a readiness ledger has a shelf life -- section 7 below re-measures every row
against the live repository on 2026-09-15 and finds that **five of its findings are now discharged and
two are superseded outright**.

---

## 1. Verbatim core proposal

> **do not start the hippocampal assay A or B as a scientific experiment on the present substrate.**
> The exact-task raw-field and PCA-32 representations are adequate experiment-side positive controls,
> but the reusable frozen-endpoint, pairing, drift, and native-consumer instruments required to make
> A/B discriminating do not yet exist. The trained 978-OFF `z_world` is not an adequate
> positive-control sender for this decision task.

Applied throughout via the dossier's ladder:

> `encoded != decodable != natively accessible != bridgeable != pairing-specific != causally used != behaviourally useful`

The bounded adjudication of 1010 is the document's second product:

> **finite-ladder non-recovery on a reproduced OFF regime; strong evidence of a decision-relevant
> accessibility/retention deficit, with sample-size, reader-family, exact-endpoint, and
> process-identity alternatives still open**.
>
> Failed finite decoding is not proof of zero mutual information or irreversible information destruction.

---

## 2. Novelty table (short -- see hub)

Full family table: `thought_intake_2026-09-09_hippocampal_campaign_adjudication.md` section 2.
Rows that originate in THIS file only:

| Thread in this file | Existing REE coverage | Verdict |
|---|---|---|
| The manifest string `H-F-confirmed` is a decision-GRID output, not a measured information-destruction variable; the defensible label is finite-ladder non-recovery | **INV-105** already binds this exactly: "a null measured on a COMPRESSED representation requires a dimensionality-matched RANDOM-PROJECTION floor, not a zero floor, before it can be read as information loss", and the seven-rung ladder forbids collapsing `decodable` into `encoded` | **Already owned** -- cross-ref INV-105. This file is INV-105 correctly APPLIED to one run, which is evidence the invariant is doing work, not a new claim |
| `_best_over_rungs` does not exclude rows carrying `diverged=true` (latent logic hazard, no effect on this run) | none -- a code defect, not a claim | **Not a claim.** Verified live: still true at HEAD (section 7, row I5). Report as an infrastructure defect to be chipped, not registered |
| The "reader-free witness" is really an action-head-free FIELD-RECONSTRUCTION witness (it has a separately fitted same-capacity latent->field decoder plus the fixed oracle rule) | none | **Adjacent-but-distinct naming correction.** Belongs on the 1010 record / the hypothesis-space basis text, not in `claims.yaml`. See section 7, row I6 -- the confirmed H-F basis text still calls it "reader-free" |
| The sample-saturation witness sub-splits by ROWS (`torch.randperm(n_full)`), not by whole episodes, so it inherits none of the primary split's leakage protection, and all three half->full deltas are POSITIVE | none | **Genuinely new METHODOLOGICAL finding, and it is LIVE and UNADOPTED** (section 7, row C3). Not a claim: it is a specific objection to one piece of the H-F basis. Report to `/governance` |
| Substrate-identity warning: 1010 ran `darwin-arm64-py3.13` against 1008's `linux-x86_64-py3.10`, `substrate_stable_across_run: false`, 2 process-snapshot drifts, driver excluded from the substrate hash | [memory] `reference-cross-machine-class-contract-divergence`; `reference-machine-class-omits-torch-version` | **Already owned as a standing hazard.** This file's contribution is its correct APPLICATION -- "these facts do not invalidate the paired within-run capacity comparison; they block the stronger statement that 1010 decoded the exact persisted endpoint" |
| "Banked" in the 1010 driver means banked deterministic RECIPE, not banked data or weights -- 978/1002 persisted neither observations nor encoder weights | none | **Adjacent-but-distinct.** This is the strongest single argument in the file, and it is the root of the "frozen endpoints do not exist as a reusable primitive" gap (section 7, row G2). Candidate to register -- see section 5 |
| The `NOT-adequate-sender` ledger (5 representations x A/B readiness) | `hippocampal_campaign_assay_specifications_20260910.md` section 0.1 reaches the SAME verdict and picks the SAME two sources (`rawfield25` primary, `ws250_pca32` replication) | **Already owned by a planning doc written the next day.** Independent convergence, one day apart |
| "No additional 1010 diagnostic is required to begin instrument qualification"; if governance wants an encoder-objective repair, first refit `mlp512` on nested WHOLE-EPISODE subsets (7/14/21/28, fixed 12-episode test, >=3 orderings/seed) | none | **Genuinely new and NOT taken.** The 2026-09-11 Step 8 gate routed straight to `/implement-substrate` (SD-106). The two alternatives offered and declined were the layer-wise localisation probe and a two-leg fan-out; **this file's nested-episode learning-curve probe was never put to the user.** Section 7, row C3 |

---

## 3. Key formulations (verbatim)

- "`encoded != decodable != natively accessible != bridgeable != pairing-specific != causally used != behaviourally useful`"
- "**Failed finite decoding is not proof of zero mutual information or irreversible information destruction.**"
- "retain the experiment-local `H-F-confirmed` string as provenance, but do not repeat its 'destroyed' paraphrase as established science."
- "The memorisation guard does its stated job: it rules out an under-capacity fit on the observed training labels. It does **not** rule out an under-sampled input-to-label relation."
- "A two-point half/full comparison cannot establish an asymptote."
- "'Banked' therefore means banked deterministic **recipe**, not banked data or weights."
- "Experiment-only adapters must not be relabeled native consumers."
- "representation controls are ready; the experiment is not."
- "Running B now would confound local rehearsal, retrieval strength, source inadequacy, and interface repair."
- "That is instrument work, not a hippocampal mechanism experiment."

---

## 4. Affected existing claims

Cross-reference only. **No status, confidence, evidence record, or field of any existing claim is
touched by this intake.** The raw file itself touches none either (its section 5 ledger ends
"New claims / proposal / queue / runner: **None touched.**").

| Claim | Relation | Note |
|---|---|---|
| **INV-105** | the standard this file APPLIES | Every "do not read the manifest label as science" move in section 2.7 is INV-105 rungs 1-2 enforced. Evidence that the invariant is load-bearing in practice -- worth recording on INV-105's own evidence side at `/governance`, which this intake does not do |
| **MECH-537** | **STALE NOTE, do not fix here** | MECH-537's `notes` say: "This claim supplies the mechanism that would explain the 1002 result *without requiring information loss*." V3-EXQ-1010 has since CONFIRMED H-F (content-discarded-at-encode) on the 978-OFF latent, so that sentence's premise no longer holds for this latent. MECH-537 is not refuted -- routing failure remains a real third category, and `implementation_phase: v3` is still right -- but its motivating example has been overtaken. Currency finding for `/governance` |
| **MECH-457** | `bears_on` on the 1010 autopsy | Carried verbatim from the 1002/1008 autopsies so the GOV-DIAG-1 chain advances. Unchanged |
| **INV-088** | `bears_on` on the 1010 autopsy | Unchanged |
| **ARC-139, MECH-538, MECH-539, MECH-540, MECH-547, MECH-548** | the claims whose instruments the readiness table inventories | The file's own closing line is correct and I confirm it: "The related claims already cover the space; no duplicate is justified" |
| **SD-018** | amended, not by this file | The 2026-09-11 Step 8 gate chose "Amend SD-018 AND flag the new shape"; the new shape became **SD-106** |
| **SD-106** | did not exist when this file was written | Registered since. See section 7 |

---

## 5. Candidate claims -- REGISTERED this pass

**One**, and it is deliberately narrow. Id (assigned 2026-09-15):

| Id | Type | One line |
|---|---|---|
| **INV-109** | invariant (`emergent`) | A reproduced deterministic RECIPE is not a frozen ENDPOINT: a re-created operating regime licenses a within-run paired comparison and nothing about the persisted artifact, so any claim that names a specific prior endpoint requires that endpoint's weights/observations to have been PERSISTED and loaded, not re-derived. |

Justification for registering rather than cross-referencing: INV-105 governs what a *probe* licenses
about a *latent variable*; it says nothing about the identity of the *endpoint the probe was run
against*. The distinction is what separates "1010 decoded a latent produced by the same recipe as
1002/1008's" (true) from "1010 decoded 1002/1008's latent" (not established, and asserted by
inheritance in several downstream texts). It is also the direct root of the assay-readiness gap G2
(frozen endpoints are not a reusable primitive) and of every future A/B run, which are defined over
frozen endpoints by construction. Searched before registering: `grep -n -iE
"frozen endpoint|persisted|banked|re-created|reproduc"` over `claims.yaml` returns nothing that owns
this; `substrate_pin` / `substrate_scope_guard` in `ree-v3/experiments/_lib/` pin the SUBSTRATE COMMIT,
which is a different object (and 1010's own `substrate_stable_across_run: false` is the demonstration
that a commit pin does not deliver endpoint identity).

Everything else this file says is either an application of INV-105, a planning-doc-level readiness
fact, or a code defect. It is not forced into a claim.

---

## 6. Deliberately NOT registered

- **"H-F is only finite-ladder non-recovery."** This is INV-105 applied. Registering it again would
  duplicate a universal invariant with a single-run instance. It belongs as a governance flag against
  the H-F basis text, not as a claim.
- **The 5x2 representation-readiness ledger** (section 3.1 of the raw file). A readiness table, and it
  was superseded one day later by `hippocampal_campaign_assay_specifications_20260910.md` section 0.1,
  which reaches the same verdict with the same two sources. Registering a readiness snapshot as a claim
  would bank a fact with a shelf life.
- **The nested-whole-episode learning-curve probe** (section 4). This is an EXPERIMENT DESIGN. Route
  via `/queue-experiment`, and only after `/governance` decides whether the H-F basis's
  sample-saturation sentence needs the check. Chipping it is the right move; registering it is not.
- **`_best_over_rungs` not filtering diverged rows.** An infrastructure defect in one driver. Chip it.
- **"Experiment-only adapters must not be relabeled native consumers."** A vocabulary discipline that
  INV-105 rung 3 (`natively accessible`) already carries.
- **Any A/B go/no-go verdict.** Superseded -- see section 7.

---

## 7. IMPLEMENTATION-GAP AUDIT (re-measured 2026-09-15 against the live repository)

The raw file's own tables are dated 2026-09-10. Everything below was re-verified today against
`/Users/dgolden/REE_Working/ree-v3` and `/Users/dgolden/REE_Working/REE_assembly`, by reading the files
and the git log, not by trusting the thought.

### 7.1 Headline: the file's central verdict is SUPERSEDED

The file's go/no-go ledger says **Assay A: NO-GO; Assay B: NO-GO; instrument qualification/build first.**
The instrument has since been built.

> `ree-v3` commit **`a83f2fb`** (2026-09-10T21:15:19+0100), *"Add interface_probe P0 instrument +
> tost_equivalence (hippocampal assays A/B prerequisite)"*.

`/Users/dgolden/REE_Working/ree-v3/experiments/_lib/interface_probe.py` (51,912 B) now implements every
symbol the spec's section 4.1 named, and `experiments/_lib/stats.py:202` adds `tost_equivalence`.
Contract tests at `/Users/dgolden/REE_Working/ree-v3/tests/contracts/test_interface_probe.py`.
The commit message records the self-test cost meter: 0.071 s total across 5 cells.

So the correct 2026-09-15 statement is **not** "instrument-blocked". It is:

> **P0 is BUILT. Assays A and B are now SCRIPT-AND-QUEUE-blocked: no experiment script consumes
> `interface_probe`, and no EXQ id is allocated.** Verified: the only file in `ree-v3` importing
> `interface_probe` is its own contract test.

In the work-graph vocabulary this moves both assays from `complicated (buildable)`-gated to
`complex (probe-gated)` with the gate discharged -- running the assay IS the probe, and the thing in
front of it is now ordinary `/queue-experiment` work.

### 7.2 The named instrument components

| # | Component the audit names as missing | Status 2026-09-15 | Evidence |
|---|---|---|---|
| G1 | Adequate source positive control (raw 25-D / full 250-D / PCA-32) | **BUILT** (experiment-layer, as the audit says) | `ree-v3/experiments/v3_exq_1002_zworld_actor_adequacy_oracle_adapter.py` (`_rawfield_features`, `RawFieldAdapterPolicy`, `_split_episodes`); `..._1008_...py` (`_world_state_pca_stats`, `WorldStateAdapterPolicy`). Reuse manifest: `hippocampal_campaign_assay_specifications_20260910.md` section 2.10 |
| G2 | **Frozen source/consumer snapshots with exact artifact IDs** | **PARTIAL -- correcting the raw file, which called this flatly "Missing as a reusable assay primitive"** | Three pieces now exist and the raw file missed one of them. (a) IDENTITY: `interface_probe.hash_tensor_state` at `ree-v3/experiments/_lib/interface_probe.py:119` -- sha256 over a `state_dict`, and its docstring at :126-129 spells out the exact contract the audit asked for ("hash before first eval, re-verify at every boundary, abort on mismatch"); `CaptureRecord` carries `sender_weight_hash` / `receiver_weight_hash`. (b) PERSISTENCE: `ree-v3/experiments/_lib/probe_warmup.py:254` `class AgentSurface`, `:367 capture_agent_surface`, `:395 restore_agent_surface`, with a content-keyed `torch.save`/`torch.load` cache at `:904 _cache_load` / `:922 _cache_store` (keys from `:850 _warmup_key`). (c) FINGERPRINTING: `experiments/_lib/arm_fingerprint.py:836 arm_cell`. What is still missing is the *assay-shaped* composition of these into a sender+consumer PAIR store, and `ree_core` itself has **no** agent-level save/load at all (`grep "def save|def load|def state_dict"` on `ree_core/agent.py` -> zero hits; no `torch.save`/`torch.load` anywhere under `ree_core/`). 1010 still re-creates endpoints from a recipe. **This is INV-109's subject.** |
| G3 | Aligned interface telemetry (sender / actual consumer input / preactivation / target / provenance / gates) | **BUILT** | `interface_probe.capture()` at `ree-v3/experiments/_lib/interface_probe.py:143`, with `CaptureRecord` at :88. Its docstring requires "the signal the live consumer actually reads, not a convenient neighbouring tensor" -- the exact discipline the audit asked for. Closes work-programme **ML-10** |
| G4 | Reusable episode/environment dataset builder with split provenance | **PARTIAL, unchanged** | `_split_episodes` still lives inside the 1002 experiment DRIVER, not in `_lib/`; `interface_probe.capture` records "environment and layout identifiers suitable for block splitting" but does not itself split. The audit's "partial/ad hoc" reading stands. Work-programme **ML-11** |
| G5 | Communication-subspace estimator (RRR / principal angles) | **BUILT, with one sub-gap** | `interface_probe.communication_subspace` :361 (cross-validated reduced-rank regression, `CommunicationSubspaceResult` :324, `_rrr_fit_full_data` :334, `_kfold_indices` :274); `principal_angles` :433 (angles in radians + subspace overlap). Closes **ML-12**. This is MECH-537's registered operationalisation ("cross-validated Reduced Rank Regression from sender activity to the ACTUAL consumer input tensor"), now implemented. **Sub-gap:** no CCA and no *partial* CCA anywhere in the repo (`grep canonical_correlation|\bcca\b` -> zero). The adjudication's section 3.3 leans on partial CCA when reading Gonzalez et al.; REE cannot currently reproduce that analysis |
| G6 | Bridge ladder (identity/native, Procrustes, affine, low-rank, nonlinear, `T(A,B)`) | **BUILT for L0-L5; `T(A,B)` is a GENUINE GAP** | `interface_probe.bridge_ladder` :629 with `BridgeLevel` enum :469 and fitters `_fit_l0` :504, `_fit_l1_procrustes` :509, `_fit_l2_affine` :519 (ridge), `_fit_l3_low_rank_affine` :536, `_BottleneckMLP` :565 + `_fit_nonlinear_bridge` :586, `_ood_zscore` :620, `_pad_or_truncate` :488. Closes **ML-13**; this is MECH-538's `L(A->B)` made measurable. **But the ladder fits `X -> Y` UNCONDITIONALLY** -- there is no receiver-conditioned rung. Assay A's decisive arm (receiver-state-conditioned bridge vs frame-conditioned vs fixed route) and **MECH-547's entire operationalisation** therefore still have no instrument. This is the sharpest single instrument gap in the family |
| G7 | Pairing/content controls (correct / wrong-episode / zero / matched-random / receiver permutation) | **BUILT** | `interface_probe.causal_replacement` :750, with `CausalReplacementCategory` :718 returning the five interpretive categories as an enum plus a defensive `AMBIGUOUS` fallback. Closes **ML-14**. This is INV-105 consequence (1) made mechanical |
| G8 | **Native consumer causal replacement** (ablation + matched replacement at the ACTUAL consumer) | **GENUINE GAP, and open BY CONSTRUCTION** | `causal_replacement` is probe-only: it has no consumer at all, scoring is entirely the caller's `eval_fn` (:766-768), and the module docstring (:24-25) states it "changes NO `ree_core` file and adds NO module to the agent". `ree_core` has no runtime path that ablates a message at a live consumer (`grep message_ablat\|ablate_message\|replace_message\|matched_replacement` over `ree_core/` -> only prose about biological lesion studies). And no *native* REE consumer is wired to any of the three adequate sources -- the audit's point stands verbatim: "Existing adapters are experiment-only readers." Note the tension the raw file does not state: a pure read-only instrument **cannot** close this row, so G8 needs either a `ree_core` change (which the spec's section 4.4 forbids at this stage) or an experiment-layer surgical wrapper |
| G9 | Controlled task-relevant / unused-coordinate latent drift | **PARTIAL -- measurement built, INTERVENTION not** | `interface_probe.per_code_drift` :1012 (`PerCodeDriftResult` :1003) *measures* in-subspace vs orthogonal-complement drift norms, per-dimension normalised -- closes tranche-3 debt 9. It does not *impose* drift. Rotation machinery exists (`_random_orthonormal` :204; the self-test applies a known rotation at :1092-1094) but nothing applies one to a live latent as an intervention, and nothing confines drift to declared-unused coordinates. Closest live analogue is script-local: the LEG-2 invertible 32x32 re-basis in `v3_exq_1008_zworld_adequacy_portfolio_ws250_rebasis.py`. Assay B's manipulated variable (1) is therefore still unwritten |
| G10 | Replay/sleep boundary trigger | **BUILT, scientifically incomplete** -- unchanged | `REEAgent.force_sleep_cycle_at_eval_boundary()` at `ree-v3/ree_core/agent.py:12759` (the raw file's 12666-12718 has shifted +93). Body: `_flush_exploration_episode()` then `sleep_loop.force_cycle(self)`, `None`-safe. The audit's caveat is confirmed by the assay spec's independent check: `ree_core/sleep/replay_sampler.py`'s `SleepReplaySampler` is *by its own docstring* a NO-OP CONSUMER, and `ree_core/sleep/cross_module_consolidation.py`'s `CrossModuleConsolidator` schedules module LOSSES and carries no `(sender_episode, receiver_episode)` pairing. **There is still no paired intersystem replay anywhere in `ree_core`** |
| G11 | Dynamic/recurrent stability instrument (**ML-15/15b**) | **PARTIAL -- MECH-539's half built, MECH-548's half NOT** | `interface_probe.dynamic_compatibility` :953 (`DynamicCompatibilityResult` :945) gives one-step and multi-step MSE/cosine by horizon plus a `compounding` flag -- that is MECH-539's `receiver_transition(T(x_t), a_t)` vs `T(x_{t+1})` criterion verbatim. `manifold_guard` :847 (`ManifoldGuardResult` :835) covers MECH-548's off-manifold half. **But `dynamic_compatibility` measures compounding error across ONE rollout, not repeated re-application of `T`**: no `cumulative_drift` / `repeated_use` / `recurrent_interface` symbol exists anywhere. The assay spec's section 1.8 requires exactly this (`N = 20` consecutive closed-loop consumer steps, decay slope, first-crossing step). MECH-548's cumulative-bridge-drift leg is still uninstrumented |
| G12 | Two-interface stable/change comparator | **STILL A GENUINE GAP** | Nothing supplies two live interfaces each with an adequate native consumer and full controls. MECH-540 requires it and section 7.4 explains why this is the hardest remaining row |
| G13 | Equivalence tests (not in the audit's table; added by the assay spec) | **BUILT** | `stats.tost_equivalence` at `ree-v3/experiments/_lib/stats.py:202` with `TostResult` :183, scipy-free (`_betainc`, `_t_cdf` :159/:172) |
| G14 | Persisted/runtime PCA component in `ree_core` | **STILL ABSENT, and correctly so** | `grep "\bPCA\b|principal_component|pca_lowrank|linalg.svd|eigh"` over `ree_core/` -> every hit is prose in a docstring or comment (`ree_core/latent/zworld_p0.py:100,114,117,133,385,648`; `ree_core/latent/stack.py:951,953`; `ree_core/utils/config.py:245`); zero real SVD/PCA calls. Runtime PCA lives only in `experiments/v3_exq_{589,1008,1010,1017,1023}_*.py` and `experiments/_lib/zworld_p0_warmup.py`. `interface_probe` deliberately bakes in no task semantics (docstring cites the mutual-legibility doc section 15). A design decision, not a gap |
| G15 | (new since the audit) Hippocampal sequence-completion verification + trajectory promotion | **BUILT 2026-09-14, default-OFF** | `ree-v3` `7642cd1` (MECH-057b) adds `verify_sequence_completion` at `ree_core/hippocampal/module.py:3213` and `promote_candidates` at `:3277`, gated by `use_completion_promotion_gate`, default `False` / no-op, 22 new contract tests, no experiment queued. Not part of the A/B instrument, but it is new hippocampal substrate landed inside this family's window and a session reading the raw file's section 3.1 hippocampal row should know it exists |

### 7.3 The 1010 lineage: what actually happened after the audit

| # | The audit's finding (2026-09-10) | Status 2026-09-15 |
|---|---|---|
| C1 | "The current governance registry still records H-F as `alive`, with no evidence runs, and says 1010 is not yet queued (`hypothesis_space.v1.json:5047-5064`). That registry is now stale." | **DISCHARGED 2026-09-11.** `evidence/planning/hypothesis_space_registry.v1.json` now carries `"state": "confirmed"`, `resolving_runs: ["V3-EXQ-1010"]`, `resolved_utc: 2026-09-09T19:53:48Z`, `met_elimination_bar: false`, and a refreshed `synthesis` block ("All five legs resolved"). Applied by `failure_autopsy_V3-EXQ-1010_2026-09-11` at the user gate. **Minor correction to the raw file:** it cites `hypothesis_space.v1.json`, which is the GENERATED file; the hand-authored source is `hypothesis_space_registry.v1.json`. Both now agree |
| C2 | "governance must decide how to apply a **bounded** reading" | **DONE, and the bounding was partially adopted.** `met_elimination_bar: false` is explicitly justified in the registry: "H-F is confirmed but NOT exclusive", H-C's split already attributes part of the gap to geometry. That is the audit's caution taken seriously |
| C3 | Section 2.4: the sample witness does **not** show saturation -- all three half->full deltas are POSITIVE (+0.0200/+0.0136/+0.0356), two exceed the ~0.013 seed spread, and the half set is sampled by ROWS not EPISODES, destroying the grouping that protects the primary split | **NOT ADOPTED, AND THE OPPOSITE IS NOW IN THE CONFIRMED BASIS.** The H-F resolution basis reads: *"not plausibly an under-sampled map (halving the training rows costs only 0.0136-0.0356 against a 0.122 mean gap to the bar)."* That is the same three numbers read as a reassurance rather than as a trend, and the row-vs-episode sub-split objection does not appear anywhere in the basis, the autopsy, or the Step 8 gate. The audit's own remedy -- refit `mlp512` on nested WHOLE-EPISODE subsets -- was **never offered at the gate**; the two declined alternatives were the layer-wise localisation probe and a two-leg fan-out. **This is the single live, unowned methodological debt in the family.** Route to `/governance` |
| C4 | "No additional 1010 diagnostic is required to begin instrument qualification" | **FOLLOWED.** P0 was built the next evening (`a83f2fb`) without a further 1010 run |
| C5 | Implied: an encoder-objective repair is the alternative branch | **TAKEN, AND IT HAS ALREADY RUN AND BEEN AUTOPSIED.** `ree-v3` `616e713` registers **SD-106** (generic bottleneck variance preservation at `observation->z_world`), successor SHAPE to SD-018, both additions DEFAULT-OFF and bit-identical OFF (`ZWorldP0Config.preservation_weight`, `LatentStackConfig.use_world_encoder_skip`). The named root cause is a SCALE defect, not an absence: reconstruction was 0.80% of the trained loss (variance hinge 80.3%, covariance 9.3%, grounding CE 9.5%) because raw MSE on a sparse one-hot observation is ~0.018 against O(1) hinge/CE terms |
| C6 | -- | **V3-EXQ-1023 ran 2026-09-12** (`evidence/experiments/v3_exq_1023_sd106_bottleneck_preservation_validation_20260912T045319Z_v3.json`) and was autopsied 2026-09-14, **CONFIRMED at the `/governance` user gate 2026-09-15T01:14:02Z**. Verdict: SD-106 **weakened (partial)** -- 0/3 seeds clear the 0.85 parity bar (0.7398/0.7191/0.6987) against the PCA-32 anchor's 3/3 (0.8771/0.8578/0.8718), but SD-106-ON beats paired OFF on **every** seed (+0.0722/+0.0442/+0.0366), all 5 readiness preconditions green, C2 non-degenerate. The unresolved fork is training BUDGET (P0a ran 348-396 steps on 2364-2716 buffered observations, below the design doc's own 600-step/4000-observation reference) versus a genuine objective ceiling at weight=200. Routed to `/queue-experiment` with an `amend SD-106` substrate entry and a specified cheap P0a-warmup-only two-metric step-budget probe |
| C7 | `_train_decoder` defines "diverged" only as final CE >= `ln(5)` | **Citation accurate, no line drift.** `def _train_decoder` :786; `uniform_ce = log(max(action_dim,2))` :832; the `"diverged"` assignment :834 |
| C8 | `_best_over_rungs` does not exclude `diverged=true` rows | **Citation accurate (:1178, loop :1184-1193, filters only on track/seed/`v is None`), and the hazard has SHARPENED since the audit wrote it.** SD-106's pre-set acceptance target is *an unchanged re-run of 1010*. If the preservation term changes fit dynamics at the top rung, a diverged row could silently win the max-over-rungs and be read as an SD-106 success. Empirically moot on the 2026-09-09 run (`guards.diverged_rungs = []`); not moot prospectively. **Raise the chip priority accordingly.** Partial mitigation already in the file: the docstring :1181-1183 defends the max, and :1663-1666 assigns the max-over-rungs criterion to the *consumer* rung precisely because the top rung "may legitimately have diverged" |
| C9 | The "reader-free witness" naming | The confirmed H-F basis still says *"A reader-free second witness"*. The audit's correction -- it has a separately fitted same-capacity latent->field decoder (`_train_field_decoder`, `_content_witness`), so it is action-head-free, not reader-free -- **has not been applied**. Minor currency finding |
| C10 | The audit's other 1010 line citations | `include_driver_script_in_hash=False` is **exactly** :1108 (inside the `arm_cell` context opened :1106). The limitations block is :298-**332** (audit said 298-331; the "WHAT THIS RUN CANNOT SAY" header is at :298, SECOND LIMIT :318, THIRD LIMIT :328). `_adjudicate` is :1203-1245, **exact**. The second decision-grid window has **shifted ~-20**: the criteria/interpretation block is :1640-1697, so the audit's "1680-1697" starts mid-dict |

### 7.4 The raw file's OTHER substrate citations (section 3.1), re-checked

All four are accurate as to existence; two have drifted in line number.

| Raw file's citation | Verdict |
|---|---|
| `AnchorSet` stores scale/segment/stream-tagged `z_world` anchors, `ree_core/hippocampal/anchor_set.py:192-290` | **EXACT.** `class AnchorSet` :192; `AnchorKey = Tuple[str, str, Tuple[str, ...]]` :56; `class Anchor` :112 with `z_world` :131-132; `write_anchor(scale, segment_id, stream_mixture, z_world, goal_payload)` :254 |
| Hippocampal forward proposes trajectories; replay starts from recent/valence-selected `z_world`, `ree_core/hippocampal/module.py:1929-1985, 2855-2916, 3954-3970` | **CORRECT IN SUBSTANCE, SHIFTED.** `propose_trajectories` now :1944; `replay()` now :2882-2943 with the default start `theta_buffer_recent[-1]` :2927 and MECH-203 `_select_valence_weighted_start` :2945; `forward()` :4158. The 3954-3970 window now maps to `build_goal_payload` :3901 / `tick_anchor_set` :4018 |
| `ContextMemory` native read/write and z-world cue->action/terrain projections, `ree_core/predictors/e1_deep.py:36-265, 1006-1078` | **EXACT.** `class ContextMemory` :36, `.read()` :221, `.write()` :232; `cue_action_proj` :724, `cue_terrain_proj` :725; `extract_cue_context` :1006, action_bias :1076, `terrain_weight = sigmoid(cue_terrain_proj(...))` :1077 |
| `REEAgent.force_sleep_cycle_at_eval_boundary()`, `ree_core/agent.py:12666-12718` | **SHIFTED +93** -> :12759-12812 |

### 7.5 What is a GENUINE GAP today

Five rows, sharpened from the raw file's ten. The first two are new to this re-measurement -- the raw
file could not see them, because they are gaps *inside* an instrument that did not exist when it wrote.

1. **A receiver-conditioned rung on the bridge ladder (G6).** `bridge_ladder` fits `X -> Y`
   unconditionally. `T(A, B)` -- the transform conditioned on receiver state -- has no fitter.
   This is assay A's decisive arm and **MECH-547's whole operationalisation**; without it assay A can
   run its index/frame/fixed-route arms and cannot run the one that discriminates. `complicated (buildable)`.
2. **Repeated-use / cumulative-bridge-drift measurement (G11).** `dynamic_compatibility` measures
   compounding within one rollout, not `N` re-applications of `T`. The assay spec section 1.8 requires
   the latter and MECH-548 is defined by it. `complicated (buildable)`.
3. **Native consumer causal replacement (G8).** Open **by construction**: a module that "holds no
   agent reference" cannot ablate a message at a live consumer, and no native REE consumer reads any of
   the three adequate sources. Until this moves, every A/B result tops out at INV-105 rung 4
   (`bridgeable`) and can never reach rung 6 (`causally used`). `complex (probe-gated)`.
4. **Controlled drift as an INTERVENTION (G9).** Measurement built, imposition not. Assay B's
   manipulated variable (1) -- known invertible drift of task-relevant coordinates vs drift confined to
   unused ones -- is unwritten. `complicated (buildable)`; the pieces are in 1008's LEG 2.
5. **Paired intersystem replay, and with it the two-interface comparator (G10/G12).** `ree_core`'s two
   sleep-side candidates are a declared no-op consumer and a module-loss scheduler with no pairing, so
   assay B's arms D and E (correct vs marginal-matched-permuted pairing) have no mechanism to express.
   The assay spec is explicit that this is **experiment-layer** work on the
   `consolidation_lesion_harness.py` / V3-EXQ-702 precedent and that `ree_core` must NOT grow a pairing
   mechanism. `complicated (buildable)`, at the experiment layer.

Downgraded from the raw file's gap list because they are now built or partly built: aligned interface
telemetry (G3), communication-subspace estimation (G5), the L0-L5 bridge ladder (G6), pairing/content
controls (G7), per-code drift measurement (G9 half), equivalence tests (G13), frozen-endpoint
identity+persistence primitives (G2, PARTIAL).

Minor open sub-gap, worth one line: **no CCA or partial CCA** exists anywhere in `ree-v3`, so the
partial-CCA analysis the campaign adjudication leans on when reading Gonzalez et al. (E06) has no REE
counterpart.

### 7.5 Live queue state

`ree-v3/experiment_queue.json` holds **one** item (`V3-EXQ-935a`, `claimed`, MECH-266/SD-032a --
unrelated). No hippocampal, interface, bridge, drift, replay-pairing or 1010-successor entry is queued.
No EXQ id is allocated for assay A or B.

---

## 8. Next steps

1. **`/governance`, highest value:** adjudicate finding **C3**. The confirmed H-F basis reads the
   sample witness as reassurance; this audit reads the same three positive deltas as an unresolved
   trend and names a specific, cheap remedy (nested WHOLE-EPISODE subsets, fixed 12-episode test,
   >=3 orderings per seed, `mlp512` only, no new ladder). The decision is not whether H-F is wrong --
   it is whether the basis text should carry the qualification. Note this interacts with the
   already-open SD-106/1023 training-budget question, which is the same shape of doubt one layer down.
2. **`/governance`, cheap:** apply the two naming corrections to the H-F basis -- "reader-free witness"
   -> action-head-free field-reconstruction witness (C9); and the `hypothesis_space.v1.json` vs
   `hypothesis_space_registry.v1.json` citation in any text inheriting this file's section 2.7.
3. **`/governance`, currency:** MECH-537's `notes` premise ("without requiring information loss") has
   been overtaken by the H-F confirmation. Do not demote; re-word.
4. **Chip (infrastructure):** `_best_over_rungs` in
   `/Users/dgolden/REE_Working/ree-v3/experiments/v3_exq_1010_zworld_overcapacity_decoder_sweep.py`
   should exclude rows with `diverged=true`. Latent; no effect on any landed run.
5. **`/queue-experiment` (already owned, do not duplicate):** the SD-106 step-budget probe is specified
   in `failure_autopsy_V3-EXQ-1023_2026-09-14.json`'s `recommended_substrate_queue_entry` and was
   confirmed at the 2026-09-15 governance gate. It has an owner.
6. **Do NOT re-run the A/B readiness audit.** It has been superseded twice in five days. If a session
   needs the current readiness state, read section 7 above plus
   `hippocampal_campaign_assay_specifications_20260910.md` section 4, and re-check `git log --since`
   on `ree-v3/experiments/_lib/`.
7. **Literature:** none owed by this file.
