# Thought intake -- Replay-dependent maintenance of cross-system access (adversarial supplement)

**Date processed:** 2026-09-15
**Raw thought:** `docs/thoughts/2026-09-09_hippocampal_replay_interface_maintenance_supplement.md` (375 lines)
**Session:** thought-pipeline-20260915
**Family:** FAMILY D. Full novelty table in the hub,
`thought_intake_2026-09-09_hippocampal_campaign_adjudication.md` section 2. Short table below.
**Parent:** the campaign adjudication (same date). **This is the file that carries three of the
family's four new claims.**

**Provenance note, material to the intake.** This document is a MERGE of two supplements written
independently on 2026-09-09 by two parallel sessions, neither aware of the other, both landed at the
same path; merged 2026-09-10 (`4a47387a12c`), with the retention rationale recorded separately in
`evidence/planning/hippocampal_replay_supplement_merge_rationale_20260910.md` (`9dce2e1bebf`). The
merge rationale's own observation is worth preserving because it is the one place the
parallel-authorship accident produced evidence rather than duplication:

> **Pass A also has a Gate 0** ... Two sessions independently converged on the same methodological
> commitment. That convergence is itself worth noting.

**Status of the raw file, honoured:** "literature synthesis and assay refinement only. This supplement
does not register, implement, queue, score, or promote a claim." Its section 9 repeats it. That prior
was applied as a prior, not as a veto -- and three threads survived a coverage check that found
**zero** existing owner. The prior is why this intake registers three and not eight.

---

## 1. Verbatim core proposal

> After a sender and/or receiver representation changes enough to break an established cross-system
> mapping, does correctly paired replay restore the receiver's actual use of the sender's still-intact
> information, without improving either system's local memory or competence?

> **Bottom line:** no located biological study completes this causal chain, and none genuinely
> discriminates interface repair from consolidation under preserved local memory.

> The one work that explicitly separates correspondence refresh from rehearsal is computational, and
> **it does not favour repair.**

> The strongest present conclusion is **design-generative, not evidential**: interface repair is a
> live mechanism that deserves a discriminating assay, not a completed biological claim.

---

## 2. Novelty table (short -- see hub for the family table)

| Thread in this file | Existing REE coverage | Verdict |
|---|---|---|
| Replay may maintain or re-establish cross-system access as codes change; the two-interface test | **MECH-540** (sleep as selective interface maintenance; S1-S4 signatures; the two-interface requirement) | **Already owned.** The supplement's independent re-derivation of the two-interface requirement is convergence, not new content |
| The seven-rung ladder's pairing controls (correct / mismatched / zero / moment-matched random) | **INV-105** consequence (1) | **Already owned**, and inherited wholesale by INV-110 link 4 rather than restated |
| Durability: rescue at one step followed by collapse under continued drift is not maintenance | **MECH-548** (recurrent interface stability), **MECH-539** (dynamic compatibility) | **Already owned.** Cross-ref only |
| A sleep-induced change that improves a static probe but destabilises the next waking trajectory is a failure | **MECH-548**'s sleep corollary, already proposed to `/governance` as MECH-540's fifth signature by the 2026-09-08 intake | **Already owned and already in flight.** Do not re-raise |
| Offline work is a family of typed transformations, each with its own readout | **ARC-137** | **Adjacent at a different grain.** ARC-137 partitions by OUTCOME (retention / reorganisation / recalibration / behavioural-access repair); this file's four-way taxonomy partitions by CONFOUND (local consolidation / retrieval triggering / stable routing / interface repair). ARC-137's "behavioural-access repair" holds upstream content fixed at ONE system; interface repair is defined over a RELATION between two. Distinguished explicitly in INV-110's `depends_on` |
| **The six-gate completed-chain standard, within ONE design; Gate 0 interface-lesion proof; the `D > max(E,F,G)` estimand; prospective local-memory equating by equivalence test** | none. `grep -iE "interface lesion\|null subspace\|receiver-potent\|wrong-pair\|mismatched pair\|shuffled pair"` -> 0 | **GENUINELY NEW -> INV-110** |
| **Receiver-local self-healing (label-free Hebbian/homeostatic readout adaptation) as the standing rival** | none. `grep -iE "self-healing\|co-adapt\|local adaptation\|homeostatic readout"` -> **0 on every term**. MECH-120 is nightly synaptic DOWN-SCALING (magnitude, not correspondence); MECH-517/532 are static interface failures; MECH-513 is explicit index REINDEXING, i.e. the labelled operation this does without | **GENUINELY NEW -> MECH-562** |
| **Code-change vs world-change indistinguishability: a self-repairing readout can "correct" a truthful update away** | none. `grep -iE "world change\|latent cause"` -> 2 incidental prose hits in unrelated entries | **GENUINELY NEW -> Q-107.** Distinguished from the 31-claim provenance cluster (which is about content ancestry, not about the coordinate system) and from **GOV-EQUIV-1** (a governance rule binding on SESSIONS, presupposing an outside adjudicator; this asks whether the ORGANISM can do it with none) |
| Stable/coordinated invariant scaffold as a rival that removes the interface problem entirely (Gallego, Keinath, Rule et al., Peters) | none as a claim; **MECH-537**'s orthogonal-complement analysis is the closest instrument | **Adjacent.** Folded into MECH-562's dependency note and into INV-110's Gate 0 (a rotation preserving the receiver-potent subspace is explicitly excluded from counting as "measured change"). Not registered separately -- it is the null case of MECH-562's preconditions |
| Cortical completion and local rehearsal as executable comparators (Cowansage, de Sousa, Clawson) | `pattern completion` has REE owners (MECH-044, ARC-013, and the E1 associative-manifold claim) but none about a receiver completing WITHOUT the sender's message | **Adjacent; assay arm G.** Not registered -- an arm belongs to the assay spec |
| Intervention validity: ~1/4 of detected replay events lack ripples, and ripple timing depends on the decoded replayed location, so the target must be decoded CONTENT not ripple onset | none | **New, biology-side only.** Recorded inside INV-110's notes as a measurement corollary; it constrains how a BIOLOGICAL test is read, and REE has no ripples |
| Kali-Dayan model-assumption audit (perfect persistent storage, fixed cue threshold, exact cleanup, symmetric connectivity) | none | **Not a claim.** It is the reason INV-110 exists -- even the closest formal precursor declines to award unique necessity. Quoted in INV-110's notes |
| The 15 measurement/literature debts (section 8) | -- | **Not claims.** Routed to `/lit-pull`; see section 8 below |

---

## 3. Key formulations (verbatim)

- "Findings distributed across unrelated preparations are useful triangulation, not completion, because the confounds differ across preparations and do not cancel when the rows are stacked."
- "Apparent change is insufficient if it is only unit turnover, registration noise, behavioural change, within-session context remapping, or **a coordinate rotation that preserves the receiver-potent subspace**."
- "**Only the fourth is the target.** The first three can all produce the shared observable -- restored downstream performance despite upstream change -- and none of them requires pair identity to matter."
- "either operation maintains episodic recall in the reported simulations, and combining them produces no clear extra benefit" (on Kali-Dayan).
- "The result is a constructive proof that replay *can* maintain correspondence under its assumptions, not evidence that biological replay does so."
- "'Local' does not mean assumption-free or exposure-free."
- "a changing code and a changing world are not distinguishable from the receiver's vantage point, so a self-repairing readout can 'correct' a truthful update away."
- "**Do not interpret `D > B` alone.** It is compatible with generic replay, additional plasticity opportunity, rehearsal, and homeostatic adaptation."
- "Arms F and G must be run separately rather than as one combined rival, because a single combined comparator cannot say which local mechanism won."
- "Local-memory controls are constraints, not covariates ... conditioning on a treatment-affected memory score is a collider."
- "A decoder that reads sender content out of receiver activity is not use."
- "If the oracle cannot restore use, the lesion is not an interface lesion." / "**`C` fails to restore `U`** -> The lesion is not an interface lesion; the drift model is invalid, not the hypothesis."
- "an unswept timescale silently chooses the winner."
- "The strongest falsifier is not 'replay disruption has no effect.'"
- "a ripple-triggered null bounds ripples, not replay."

---

## 4. Affected existing claims

Cross-reference only. **No status, confidence, evidence record or field of any existing claim is
touched.** One flag PROPOSED to `/governance`, not applied:

1. **MECH-540 evidence-status reinforcement.** MECH-540's notes already say, honestly, that "the
   campaign found NO direct biological evidence that sleep reduces a formal bridge-complexity metric
   between two independently specialised systems." This supplement is the systematic version of that
   sentence -- 28 studies and two models, none closing the chain -- and it adds the reason (the four
   confounded outcomes share one observable). **Proposed:** cite this supplement in MECH-540's
   evidence-status paragraph. A note addition, not a status change, and `/governance` owns it.

Cross-referenced without change: **MECH-540** (the mechanism INV-110 adjudicates), **INV-105**
(inherited controls), **MECH-539** and **MECH-548** (durability), **ARC-137** (distinguished),
**MECH-529** (where legitimate representational change originates -- load-bearing for Q-107),
**ARC-092** (the waking checkpoint, REE's one existing partial answer to Q-107), **ARC-139**
(MECH-562 is the sharpest test of its NECESSITY, not its coherence), **GOV-EQUIV-1** and
**GOV-MATCHAUX-1**.

**Currency findings inherited from the hub** (reported, not fixed): all eight interface claims lack
`what_would_answer`; MECH-537/538's notes are overtaken by the 2026-09-11 H-F confirmation; SD-080's
`unblocks_claims` omits MECH-537.

---

## 5. Candidate claims -- REGISTERED this pass

Three, all in `proposed_claims.yaml`. All `status: candidate`, `epistemic_category:
substrate_conditional`, `registered_utc: 2026-09-15`, location
`docs/architecture/mutual_legibility_communication_subspaces.md` (an EXISTING doc -- no new stub is
needed; they belong beside MECH-540 and INV-105, which they respectively adjudicate and complement).

| Id | Type | Phase | One line |
|---|---|---|---|
| **INV-110** | invariant (`universal`) | v3 | The interface-repair adjudication standard: six links closed within ONE design, a Gate-0 lesion proof before any maintenance arm, the estimand `D > max(E,F,G)` rather than `D > B`, and prospectively equated local memory tested by equivalence. |
| **MECH-562** | mechanism_hypothesis | v4 | Receiver-local self-healing as the standing rival: a consumer can restore use of a drifting sender with no pair identity, no cross-system label and no replay -- so its preconditions (smooth redundant tuning, incremental drift, plasticity faster than drift, sufficient sampling) are its predictions, and its advantage is a function of the drift-rate-to-maintenance-interval ratio. |
| **Q-107** | open_question | v4 | Can a system distinguish a change in its own code from a change in the world that code represents -- and if not, what is a maintenance mechanism entitled to preserve? |

**Why three from a file that disclaims registration.** The disclaimer is about not promoting the
REPAIR HYPOTHESIS on this evidence, and that is honoured -- MECH-540 is untouched and nothing here
raises its confidence. What is registered is the opposite: the bar it must clear (INV-110), the
rival it must beat (MECH-562), and the limit both inherit (Q-107). Registering a rival and a bar
while declining to register the hypothesis is the disclaimer taken seriously, not evaded.

**Why MECH-562 is a `mechanism_hypothesis` and not a methodology note.** Every REE consumer that
learns from a drifting upstream representation is a candidate instance -- E1 and E2 reading `z_world`
through P0/P1, E3 reading E2's action-object space, any downstream reader of `ContextMemory`. If
receiver-local adaptation is doing this work in REE today, some fraction of what reads as
representational improvement in the 978-1010 lineage is readout tracking, and nothing currently
separates them.

---

## 6. Deliberately NOT registered

- **The eight-arm assay B design (section 6.3), Gate 0's four probes, the six implementation points of
  arm F, and the permitted-outcome table.** Instruments and parameters. They belong to
  `evidence/planning/hippocampal_campaign_assay_specifications_20260910.md` section 3, which already
  folds them in. The load-bearing shape -- the estimand and the gate -- is what INV-110 carries.
- **The 28-row evidence matrix.** Not governed intake; routed to `/lit-pull`.
- **Any of the 33 cited sources as REE evidence.** Per `feedback_lit_exp_decoupled`, and per the
  file's own repeated warnings, none raises any claim's confidence. Two are preprints
  (Takigawa 2026, Peters 2026, Okyere 2026) and are labelled as such.
- **The stable-invariant-scaffold rival as a separate claim.** It is the null case of MECH-562's
  preconditions and a Gate 0 stop condition; a separate entry would fragment one question.
- **Cortical completion / local rehearsal (arm G).** An assay arm.
- **The four-way outcome taxonomy as a standalone claim.** It is the JUSTIFICATION for INV-110's six
  links, and is recorded in its notes. As a claim it would assert only that four things are different.
- **A merge proposal between ARC-137 and this file's taxonomy.** They partition different things
  (outcome vs confound) and a merge would lose both. Distinguished instead.

---

## 7. Implementation-gap audit (this file's share)

Family audit in full: `thought_intake_2026-09-09_exq1010_substrate_readiness_audit.md` section 7,
re-measured 2026-09-15. The rows this file's assay B revision specifically presupposes:

| Mechanism this file presupposes | Verified status 2026-09-15 |
|---|---|
| **Arm F: a faithful Rule-O'Leary receiver-local self-repair implementation** (local Hebbian update from presynaptic sender activity and the receiver's own output, homeostatic error against fixed pre-drift output mean/variance, error-gated decay; plus normalisation and fixed-recurrent-model variants) | **GENUINE GAP.** `grep -rn "self_heal\|homeostatic\|hebbian"` over `ree-v3/ree_core/` finds no readout-adaptation rule of this shape, and `interface_probe` is pure and stateless by design so it cannot host one. **This is the single most consequential gap in the family**: without arm F, INV-110's estimand cannot be evaluated and any assay B positive is uninterpretable. `complicated (buildable)` at the experiment layer |
| **Arm E: within-context permuted pairing at matched marginals** | **PARTIAL.** `interface_probe.causal_replacement` (:750) supplies `mismatched`, `same_action_mismatch`, `same_context_mismatch`, `zero`, and a seeded per-dimension `moment_matched_random` (:789-793), returning the interpretive categories as an enum (`CausalReplacementCategory` :718, classification :809-822). What is missing is the intersystem REPLAY that would deliver those pairs -- see the next row |
| **Correctly paired intersystem replay (arm D)** | **GENUINE GAP.** `ree_core/sleep/replay_sampler.py`'s `SleepReplaySampler` is by its own docstring a NO-OP CONSUMER; `ree_core/sleep/cross_module_consolidation.py`'s `CrossModuleConsolidator` schedules module LOSSES with no `(sender_episode, receiver_episode)` pairing. Also open at the substrate-queue layer: `mech092-replay-consumer-missing` (`proposed_REGISTRATION_ONLY_not_a_build_authorisation`) records that "**Replay trajectories are computed and DISCARDED -- `_do_replay` has no consumer anywhere in `ree_core`**", an unowned blocker cited by four claims |
| **Gate 0's known invertible drift with a known analytic inverse** | **PARTIAL.** `interface_probe._random_orthonormal` (:204) and the self-test's known rotation (:1092-1094) are the pieces; `per_code_drift` (:1012) measures the result. Nothing applies a rotation to a live latent as an intervention, and nothing confines drift to declared-unused coordinates |
| **Receiver-dependent `U` = performance(message available) - performance(message ablated or counterfactually replaced), at the ACTUAL consumer** | **GENUINE GAP, open BY CONSTRUCTION.** `causal_replacement` has no consumer -- scoring is entirely the caller's `eval_fn` (:766-768) -- and the module "changes NO `ree_core` file and adds NO module to the agent" (:24-25). No native REE consumer reads any measured-adequate source |
| **Prospective local-memory equating by equivalence test** | **BUILT.** `stats.tost_equivalence` at `ree-v3/experiments/_lib/stats.py:202` (`TostResult` :183), scipy-free. Landed in the same commit as `interface_probe` (`a83f2fb`), for exactly this requirement |
| **Durability: continue drift and continue use after a one-step rescue** | **PARTIAL.** `interface_probe.dynamic_compatibility` (:953) measures compounding across ONE rollout, not repeated re-application of `T`. No `cumulative_drift` / `repeated_use` symbol exists. The assay spec section 1.8 specifies what is needed (N=20 closed-loop steps, decay slope, first-crossing index) |
| **Two interfaces, one drifted and one stable** | **GENUINE GAP.** Unchanged since MECH-540 registered the requirement |
| **`manifold_guard` for the off-manifold half of MECH-548** | **BUILT.** `interface_probe.manifold_guard` :847 (`ManifoldGuardResult` :835) -- Mahalanobis under native receiver covariance, k-NN distance, per-dimension norms, saturation rate, hidden-state trajectory distance at 1 and N steps |

---

## 8. Next steps

1. **`/lit-pull`, this file's principal outward debt.** 33 sources, none governed intake. Highest
   value in the file's own order: (1) the exact biological bridge (identified sender AND receiver
   tracked across one drift interval with preserved local competence, decoded pair identity perturbed,
   receiver-dependent behaviour tested); (2) the closed-loop pair debt (a within-context permutation
   preserving replay marginals and physiological timing while breaking only pairing -- **this does not
   exist as a technique and would have to be developed**); (7) adaptation timescale (the drift rate has
   never been mapped against the replay interval, and "an unswept timescale silently chooses the
   winner"); (10) world-change ambiguity, which is Q-107's literature. Anchor sources for a first
   pull: Kali and Dayan 2004 (DOI 10.1038/nn1202), Rule and O'Leary 2022 (DOI 10.1073/pnas.2106692119),
   Micou and O'Leary 2026 (DOI 10.1371/journal.pcbi.1014297), Rule et al. 2020
   (DOI 10.7554/eLife.51121), Widloski and Foster 2025 (DOI 10.1038/s41467-025-65181-5).
2. **Documentation debt, small and cheap:** eleven of the 33 source footnotes carry "[Title not
   recorded in the source pass.]" (footnotes 20-33 less a few). The file names this itself. A `/lit-pull`
   would discharge it as a side effect.
3. **`/implement-substrate` or `/queue-experiment`, once governance rules:** arm F is the gating build
   (section 7 row 1). Nothing in assay B is interpretable without it. **Do not build it into `ree_core`**
   -- the assay spec's section 4.4 forbids it and the diagnosis-without-changing-the-organism rule
   applies.
4. **`/governance`:** the MECH-540 note addition in section 4; and the standing question, noted in
   INV-110's own notes, of whether INV-110 should instead become a second consequence-set on
   INV-105. The 2026-09-10 GFLAG-0235 ruling on MECH-548's eighth rung is the governing precedent and
   it chose forward-reference over amendment, which argues for standing alone.
5. **Do NOT re-derive the evidence matrix.** Its stopping rule was explicit and met: additional queries
   repeatedly returned the same three disconnected families. Re-running it would reproduce the same
   negative search conclusion at full cost.
