# Claim Synthesis PROPOSAL (2nd pass) -- MECH-439 (F-dominance committed-action conversion ceiling)

- **Generated (UTC):** 2026-06-29T19:35:04Z
- **Skill:** `/claim-synthesis` (proposal-first; nothing registered without per-child user approval)
- **Trigger:** the PRE-REGISTERED CONTINGENT trigger in
  `failure_autopsy_700d-708-single-arena-ceiling_2026-06-29` ("run /claim-synthesis on MECH-439 IF
  V3-EXQ-707b ALSO fails to convert") -- **FIRED** 2026-06-29 when V3-EXQ-707b landed as a DECISIVE,
  fully-live `weakens` (all 6 non-degeneracy gates passed). See
  `failure_autopsy_V3-EXQ-707b_2026-06-29.{md,json}`.
- **Root claim:** `MECH-439` (`candidate`, `mechanism_hypothesis`, `subject:
  selection.primary_score_variance_monopoly`, `epistemic_category: substrate_ceiling`, impl_phase v3).
- **Relationship to the prior pass:** this is the **SECOND** `/claim-synthesis` on MECH-439. The first
  (`claim_synthesis_MECH-439_2026-06-20.md`) decomposed the *conjunctive claim text* into existence /
  lever-sufficiency / residual-ceiling and registered **MECH-447 / MECH-448** (+ the constitution
  MECH-449). That decomposition LANDED. This pass asks the different question the 7-autopsy recurrence
  poses: **does the NEW cluster (689a, 700b/c/d, 704b, 706b, 707b, 708) reveal a finer mechanism the
  registry has NOT yet named?**

---

## VERDICT (Step 3 discrimination gate): **REFUSE new-child decomposition -- the cluster is ALREADY METABOLIZED.**

The honest gate result is the same shape as the skill's first dogfood (MECH-341): a near-empty
decomposition is a **PASS, not a miss**. Every finer mechanism the 7-autopsy recurrence circles is
**already a registered claim with its own falsifier**. There is no UNNAMED chunk of cognition for this
skill to generate. The recurrence is not granularity debt that the registry has failed to name -- it is
the **empirical campaign progressively CONFIRMING the existing decomposition** and establishing the
**sequencing** (segregation necessary-but-not-sufficient -> learned DA-gated arbitration is the next
necessary build). The route is `/implement-substrate` (couple ARC-110 loops to ARC-108 DA-gated
arbitration), **not** a new claim.

One genuinely-licensed STRUCTURAL action is surfaced for the user below (Section 6): apply the
**already-user-confirmed-but-never-applied** 2026-06-20 narrow-and-retain disposition, now that 707b
supplies the decisive evidence the prior pass was waiting on. That generates **zero new claims**.

A single **borderline** candidate child (the cross-loop arbitration-plasticity coupling) is presented in
Section 7 for an explicit user keep/drop decision -- the skill's deliberate "the detector can be wrong"
gate. My recommendation is **drop it** (it is the intersection of two already-registered architectural
commitments), but it is the user's call.

---

## 1. The cluster's failure record (Step 2)

The 7 autopsies named in the trigger, read at the right schema field per artifact type:

| Run | Claim(s) | Outcome / direction | non_degenerate | substrate-ready? | Purest signature |
|---|---|---|---|---|---|
| V3-EXQ-689a | MECH-439 | FAIL / non_contributory | True | yes | conflict-grade BOTH-levers (Factor A+B) ~ baseline; 0/3 strict-above gap-blind controls |
| V3-EXQ-700b | MECH-439/ARC-108/MECH-450 | FAIL / non_contributory | -- | **no** (decoupled null) | learned-gating settling; same-layer null mis-scaled |
| V3-EXQ-700c | MECH-439/ARC-108/MECH-450 | FAIL / non_contributory | -- | **no** (41.42x null) | null magnitude 41x off |
| V3-EXQ-700d | MECH-439/ARC-108/MECH-450 | FAIL / non_contributory / `substrate_not_ready_requeue` | -- | **no** (matched-but-inert null) | magnitude-matched (1.0x) null STILL inert/destructive on committed-class entropy |
| V3-EXQ-704b | MECH-451 | FAIL / non_contributory | **False** | **no** (control unconstructable) | finer channels build+dissociate+learn; matched null cannot verify-lift |
| V3-EXQ-706b | MECH-314 | FAIL / non_contributory | **True** | **yes** | valid double-gated curiosity: ARM_CURIOSITY 0.967 < F-only 1.029 < valid null 1.019; no lift |
| V3-EXQ-707b | ARC-110 | **FAIL / weakens** | **True (all 6 gates)** | **yes (fully live)** | limbic loop carries real range (1.414) but A1_LOOPS 0.838 ~ A0_SINGLE_ARENA 0.914 -- segregation alone does not convert |
| V3-EXQ-708 | MECH-440 | FAIL / non_contributory / `substrate_not_ready_requeue` | -- | **no** (washes at argmax) | supra-floor weight noise (0.22) raises pre-commit class entropy by EXACTLY 0.0 |

**Cluster-autopsy verdict tokens** (read from `cluster_pattern` / `four_layer_diagnosis`, per the skill):
`700d-708` -> `structural_property: single F-dominated arena`; per-target `claim_alignment: intact`
(700d/708 non_contributory, neither weakens) / **`weakened (narrow hypothesis)`** (707b).
`704b-706b` -> `substrate_ceiling`, single-arena collapse, both `claim_alignment: intact`.

---

## 2. Discrimination gate (Step 3) -- run in full

### 2a. The cheap machine-readable first pass (necessary, not sufficient)

MECH-439 carries `epistemic_category: substrate_ceiling` -- the `_EPI_SUPPRESS_PROPOSAL` set. By the
first-pass filter alone this excludes. **But the skill warns this is necessary-NOT-sufficient**, and the
*whole point* of the 707b trigger is to test whether the substrate-ceiling reading still holds once the
enrichment is delivered. So I do NOT stop at the category field -- I run the backstops and the per-FAIL
classification.

### 2b. Backstop signals (all four fire -> already-metabolized)

| Exclude backstop | Fires? | Evidence |
|---|---|---|
| `substrate_ceiling`/`substrate_conditional` category | **YES** | MECH-439 `substrate_ceiling` |
| recent `substrate_queue` amend | **YES** | `v4_loop_segregation` amended repeatedly (700d/708/704b/706b failure records appended; `unblocks_claims` grown) |
| registered children / reverse-dep fan-in | **YES (massive)** | MECH-447, MECH-448, MECH-449, MECH-450, MECH-451, MECH-452, ARC-107, ARC-108, ARC-109, ARC-110, ARC-111 all reference / depend-on MECH-439 |
| in-progress closure-plan GAP owns the cluster | **YES (two)** | `basal_ganglia_assembly_map` (A.4 LEARN = "the conversion ceiling restated") + `conversion_ceiling_campaign` (5-face prong map incl. `P4-learned-gating`) |

This is the **MECH-341 disjunction satisfied in full** -- the opposite of the false-flag risk. MECH-439 is
not a category-only exclude; it is metabolized on every axis the skill checks.

### 2c. Per-FAIL classification

| FAIL | Class | Disposition |
|---|---|---|
| 700b / 700c | test-design (null mis-scaled) | EXCLUDE (fixed by 700d) |
| 700d | **substrate-not-ready** (valid null unconstructable in single arena) | EXCLUDE -> `/implement-substrate` |
| 704b | **substrate-not-ready** + `non_degenerate=False` (control unconstructable) | EXCLUDE -> `/implement-substrate` |
| 708 | **substrate-not-ready** (injection washes at argmax; self-routed `substrate_not_ready_requeue`) | EXCLUDE -> `/implement-substrate` |
| 689a / 706b / 707b | **genuine, non-degenerate, substrate-ready** FAIL signatures | the residue that *could* be granularity debt -> Section 3 |

So 3 of the cluster's runs ARE genuine substrate-ready non-degenerate FAILs (689a near-tie levers; 706b
eligibility-face curiosity; 707b loop-segregation). By the literal count this passes the `>=2 distinct
substrate-ready signatures` bar. **The gate therefore turns on the one remaining question: do 689a / 706b
/ 707b circle a mechanism the registry has NOT named?** Section 3 shows they do not.

---

## 3. Synthesis of the common thread (Step 4) -- and why it is already named

**One sentence:** every genuine member of the cluster shares the signature that a non-F signal which is
*demonstrably present and live UPSTREAM of the commit* (supra-floor route_range, live cross-loop variance
1.414, dissociable finer channels, double-gated curiosity range, supra-floor injected noise) is
**annihilated AT the F-dominated committed-selection step** -- and this annihilation persists *even when
the substrate enrichment is delivered and verified live* (707b: segregation built, all 6 gates passed,
still no lift).

That is a genuinely sharper statement than MECH-439's headline "F monopolises ~88-89% of E3 variance." But
it decomposes into exactly **three loci, and the registry already names all three**:

| Locus the cluster dissociates | Already-registered claim | Falsifier on the books |
|---|---|---|
| (i) **At-argmax annihilation, not upstream-strength deficit** -- the signal exists upstream but is destroyed at the hard committed selection | **MECH-439's own core** (notes: V3-EXQ-569h "supra-floor diverse pool, flat committed output -> bottleneck is selection-variance, not pool geometry") + **MECH-450** (one-shot argmin can only pre-filter, never flip the attractor; additive blend dilutes) | MECH-450 settling-step experiment |
| (ii) **The arbitration is FIXED / non-plastic** -- even per-loop-normalized loops inherit F's static dominance because the cross-loop combine cannot LEARN to down-weight F | **ARC-108** (verbatim: "F monopolises 88-89%... precisely because no learned striatal weighting can re-weight channels through experience") = campaign **P4-learned-gating** | V3-EXQ-700-lineage learned-gating 2x2 |
| (iii) **Single shared arena lets raw F-magnitude swamp non-motor channels** -- per-loop normalization strips the raw-magnitude advantage | **ARC-110** (loop segregation), now NARROWED by 707b to "necessary-but-not-sufficient; requires coupling with learned/DA-gated cross-loop arbitration" | V3-EXQ-707b (just landed; decisive) |

Plus the supporting registered children: **MECH-447** (near-tie lever sufficiency -- the 689a face),
**MECH-448** (F->eligibility demotion), **MECH-449** (Go/No-Go constitution), **MECH-451**
(representational-compression vs per-loop-competition -- the 704b face), **MECH-452** (loop-local credit
traces under broadcast DA), **ARC-109** (D1/D2 split), **ARC-111** (context-conditioned weights).

**There is no fourth, unnamed locus.** The 7-autopsy recurrence is the campaign walking the
already-decomposed `basal_ganglia_assembly_map` (afferents / internal / efferents / **learning**) and the
`conversion_ceiling_campaign` prong map, confirming each face empirically. 707b's specific new content
("segregation alone is insufficient; the arbitration must be LEARNED") is **absorbed by ARC-110's
narrowing + ARC-108's existing thesis**, not a new mechanism.

---

## 4. Lit grounding (Step 5) -- already filed; no new pull owed

The biology for all three loci is filed and load-bearing -- no new `/lit-pull` is required to refuse:

- **(ii) learned arbitration / three-factor plasticity:** the BG-assembly-map A.6 dopaminergic-control
  pull (Niv 2007, Gruber 2006, Howe 2013, Mohebi 2019, Alves da Silva 2018, Matsumoto & Hikosaka 2007,
  Sosa 2021, etc.) -- grounds ARC-108. D1-LTP/D2-LTD asymmetry.
- **(iii) segregated loops:** Alexander/DeLong/Strick (1986) parallel loops; Haber ascending
  striato-nigro-striatal spiral -- grounds ARC-110 (its build's grounding ledger).
- **(i) divisive normalization / argmin-vs-settling:** Carandini-Heeger canonical normalization +
  Louie/Glimcher value normalization (filed `targeted_review_connectome_mech_439` / `..._louie2013`);
  Cisek affordance-competition + Mink focal-Go/surround-No-Go -- grounds MECH-450 (Section 9 of the
  2026-06-20 pass already used these to split the demotion lever into Route-1/Route-2).

The decomposition is biology-grounded; the grounding lives on the already-registered children.

---

## 5. MECH-439's fate (Step 6) -- **umbrella over already-registered children; no new children**

MECH-439's disposition is **narrow-and-retain as the umbrella + existence/bound claim**, exactly the
6.1 disposition the user CONFIRMED on 2026-06-20 -- which was deferred pending 689a and then **never
applied** (MECH-439 is still the broad bundled `candidate` / `substrate_ceiling` claim, no `children:`
field, depends_on does not list 447-452 / ARC-108 / ARC-110). 707b is the decisive evidence that
licenses finishing that job (Section 6).

No new candidate child is proposed as the primary recommendation. The forward "how to break the bound"
mechanisms already exist as MECH-447/448/449/450/451/452 + ARC-107/108/109/110/111.

---

## 6. The one LICENSED structural action (user-gated; ZERO new claims)

**Apply the deferred-and-now-evidenced 2026-06-20 narrow-and-retain.** 707b supplies what the prior pass
was blocked on: the bound is now confirmed **INTRINSIC** (fully-live decisive test, A1_LOOPS 0.838 ~
A0_SINGLE_ARENA 0.914), **not** a single-arena artefact. Proposed edits to the EXISTING MECH-439 entry
(no new ids minted):

1. **Narrow the title/subject** to the existence/bound content: "F monopolises ~88-89% of E3
   committed-selection variance (V3-EXQ-571), and this bound is **INTRINSIC** to the selection step
   (V3-EXQ-707b: confirmed not a single-arena-collapse artefact) -- the *how-to-break-it* forward
   mechanisms are the children." Keep `subject: selection.primary_score_variance_monopoly`.
2. **Add a `children:` field** wiring the umbrella explicitly:
   `[MECH-447, MECH-448, MECH-449, MECH-450, MECH-451, MECH-452]` (mechanism children) and note the
   architectural release route ARC-107/108/109/110/111 in `depends_on`/notes.
3. **Stance:** the existence/bound core is `shown`-adjacent on V3-EXQ-571 **+ V3-EXQ-707b** (intrinsic).
   Whether to flip `candidate -> provisional`/`shown` is a governance call -- and would trigger the
   **derive-only reconcile** (closure-node `behavioral_diversity_isolation:GAP-I` prose + `decision_state`
   by hand, per the 2026-06-20 Section 7 note). **Recommend: leave stance to the live `/governance`
   session** (see concurrency note) rather than flip it here.
4. `epistemic_category`: the umbrella existence-fact is arguably no longer `substrate_ceiling` (the bound
   is shown, not awaiting substrate); the *children* carry the substrate-conditional tags. This is a
   governance re-tag, not a synthesis action -- flag, do not perform.

This is umbrella-wiring + narrowing of an EXISTING claim. It mints nothing and inflates no believed tail.

---

## 7. The borderline candidate child (presented for an explicit keep/DROP -- recommend DROP)

The single mechanism that is *arguably* under-named: **DA-gated cross-loop arbitration plasticity** -- the
COUPLING of ARC-110 (segregated loops) with ARC-108 (learned gating), specifically that learning must
operate **at the cross-loop arbitration**, not only within-arena or within-loop.

| Field | Draft |
|---|---|
| proposed id | MECH-453 (next free at registration time -- current max MECH-452) |
| claim_type | `mechanism_hypothesis` |
| subject | `selection.cross_loop_arbitration_plasticity` |
| one-line | "Loop segregation (ARC-110) and learned within-arena gating (ARC-108) are EACH necessary-but-insufficient for committed-action conversion; the conversion requires DA-gated three-factor plasticity operating AT the CROSS-LOOP arbitration -- the static-arithmetic cross-loop combine inherits F's dominance regardless of per-loop normalization (V3-EXQ-707b: live limbic loop range 1.414 never wins), so the combine weights themselves must be learned." |
| what_would_answer | A1_LOOPS + DA-gated learned cross-loop arbitration STRICT-ABOVE A1_LOOPS + static-arithmetic arbitration on >=2/3 seeds (the 707b-routing-step-3 redesign; new EXQ). Refuted if learned cross-loop arbitration also fails to lift where static did. |
| depends_on | MECH-439 (umbrella), ARC-108, ARC-110, MECH-452 |
| epistemic_category | `substrate_conditional` (the coupled substrate is not yet built) |
| lit | Haber ascending spiral (cross-loop DA integration) + ARC-108's three-factor pull |

**Why I recommend DROP:** this is the **intersection of two already-registered architectural commitments**
(ARC-108 "learned gating decides which channel wins selection authority" ∘ ARC-110 "selection runs through
segregated loops"), and ARC-108 **already `depends_on` ARC-110** (the campaign's explicit segregated-loops
sequencing fork). The coupling requirement is **already written into ARC-110's 707b narrowing** ("requires
coupling with learned/DA-gated cross-loop arbitration"). Registering MECH-453 risks the proliferation the
skill's anti-proliferation rail exists to prevent. The next experiment (707b routing step 3) has a clear
home as the falsifier of **ARC-108-coupled-to-ARC-110**; it does not need a fresh claim to be runnable.

**Why a reasonable person might KEEP it:** it is the sharpest, most-decisively-evidenced (707b) single
sentence in the whole cluster, it has a clean pre-registered falsifier, and giving the imminent
next-experiment an explicit claim home (rather than an implicit ARC-108∩ARC-110 intersection) is cheap
insurance. If the user judges the intersection too implicit to carry the test, register it.

---

## 8. Routing summary (Step 7/8)

- **Primary:** discrimination gate = **REFUSE new-child decomposition** (already metabolized). Route the
  conversion work to `/implement-substrate`: build the **DA-gated cross-loop arbitration** substrate
  (couple ARC-110 loops to ARC-108 learned gating) -- the 707b-routing-step-3 redesign (new EXQ, different
  claim_ids), gated on that arbitration-plasticity build.
- **Licensed structural action (Section 6, user-gated):** apply the deferred 2026-06-20 narrow-and-retain
  + umbrella-wire MECH-439 over its registered children. Zero new claims. **Defer the stance/category flip
  to the live `/governance` session** (concurrency).
- **Borderline (Section 7, user keep/DROP):** MECH-453 cross-loop arbitration plasticity. **Recommend DROP.**
- **Concurrency:** the `/governance cycle 2026-06-29 PM` session holds an active TASK_CLAIMS claim on
  `docs/claims/claims.yaml`. This proposal does **not** edit claims.yaml. Any approved Section-6/7
  registration must be sequenced AFTER that session lands (or handed to it), never racing it.

## 9. Anti-proliferation rails -- audit

- **Discrimination gate is the skill:** PASSED by refusing. The value here is the refusal + the map.
- **Every child testable:** no new children proposed as primary; the one borderline child carries a
  `what_would_answer`; recommend drop regardless.
- **Lit-grounded:** all grounding already filed (Section 4); no new pull owed.
- **Generation not promotion/building:** no stance flip performed; routed the build to
  `/implement-substrate`; deferred governance to `/governance`.
