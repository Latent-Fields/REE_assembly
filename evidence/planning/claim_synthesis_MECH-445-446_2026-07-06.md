# Claim Synthesis -- MECH-445 / MECH-446 (SD-034 de-commit-pipeline children): REFUSAL + forward-moves routing

- **Generated:** 2026-07-06T17:11:10Z
- **Verdict:** **STOP -- refuse to decompose.** Cluster does NOT clear the granularity-debt bar. NOT a demotion; NOT a re-carve. This is the skill's anti-proliferation function firing correctly. The value is the reroute (Section 5), not new claims.
- **Trigger:** SECONDARY route of confirmed `failure_autopsy_V3-EXQ-715_2026-07-06` (user-directed 2026-07-06 "Substrate + claim-synthesis"). Granularity-debt recurrence flag: 6 iterations (460h/i/j/k/l -> 715) circling MECH-445/446 with a shifting failure signature.
- **Claims examined:** MECH-445 (closure->beta coupling / commit-INTENT engagement), MECH-446 (de-commit-authority MAGNITUDE). Both `candidate` / `v3_pending` / `pending_retest_after_substrate` / `epistemic_category: substrate_ceiling`.
- **Predecessor synthesis:** `claim_synthesis_SD-034-closure_2026-06-19.md` (the decomposition that CREATED MECH-445 + MECH-446 from the coarse SD-034 de-commit clause). This document does NOT re-open that split.

---

## 0. One-line verdict

The referral's hypothesis -- "MECH-445 (commit-INTENT existence) and MECH-446 (de-commit MAGNITUDE) may be SEPARATE mechanisms no single regime co-satisfies" -- is **TRUE but already realized**. They were split on 2026-06-19; the "no single regime co-satisfies" property (disjoint, anti-correlated-by-construction certifiers) is already written into both claims' `evidence_quality_note`s and their regime-scoped `what_would_answer`s. The residual is **substrate-not-ready + measurement entanglement**, both already owned by in-flight work. Re-decomposing would manufacture duplicate/untested claims from a substrate ceiling and a measurement bug -- exactly what the discrimination gate exists to prevent.

---

## 1. The cluster's failure record (6 iterations)

| Run | failed_criterion | rec. epistemic_category | routing | brake |
|---|---|---|---|---|
| 460h | discrimination | standard (on the way up) | implement-substrate | -- |
| 460i | negative_control | substrate_ceiling | implement-substrate | -- |
| 460j | precondition | standard | implement-substrate | -- |
| 460k | precondition | substrate_ceiling | implement-substrate | FIRED / refused |
| 460l | negative_control | substrate_ceiling | implement-substrate | FIRED / refused |
| 715  | negative_control | substrate_ceiling | implement-substrate | FIRED / refused |

Every iteration self-routes **substrate-not-ready** and routes to `/implement-substrate`. None is a fair-test weakens; all leave MECH-445/446 `candidate` (PROMOTES NOTHING).

Signature evolution (all traceable to the F-dominance conversion ceiling, MECH-439):
- **460h/k:** MECH-445 commit-intent counter **structurally pinned at 0** (latch source gated on the F-driven `e3._committed_trajectory`).
- **460o/p (`use_closure_commit_entry`, PASS):** RESOLVED the structural-unreachability. MECH-445 commit-intent is now F-independent.
- **715:** narrow F-independent existence proof (seed 44: 398 commit-intents, 7 F-commits) but the failure MIGRATED to (a) **regime-scoped arming** (arms only on the weak-natural-commit seed) and (b) **within-seed temporal disjointness** (SD-034 closure fires do NOT co-locate with the latch-armed occupancy the MECH-446 window reads -> window vacuous on all 3 ON-arm seeds).

## 2. Read the right field (skill Step 2)

715 is a `scope: single` autopsy: `four_layer_diagnosis.claim_alignment = "intact; MECH-445 narrowly strengthened"`; `measurement = "under-instrumented for the ON regime"`; `prerequisites = "missing ... F-independent closure arming that survives MODERATE F"`. The dominant discriminating field (`claim_alignment` intact + `measurement` under-instrumented) points AWAY from granularity debt and TOWARD substrate/measurement.

---

## 3. Discrimination gate (skill Step 3 -- the load-bearing filter)

Classifying the cluster against the four Step-3 classes:

**(a) vacuous-criterion / test-design debt?** PARTLY -- the MECH-446 within-seed temporal disjointness (closure fires _|_ latch occupancy) is a **measurement/instrumentation entanglement**, and it is the DIRECT descendant of the child the 2026-06-19 synthesis already REFUSED ("coupling-measurability-under-refractory", S5). It has no biological mechanism to ground -> not a claim. Route to substrate/DV fix, not decomposition.

**(b) substrate-not-ready?** YES, dominant. All 6 iterations self-route `substrate_not_ready_requeue` / `precondition_unmet`. The binding constraint is the F-dominance conversion ceiling (MECH-439): the F-driven natural commit (~88-89% of E3 variance, V3-EXQ-571) monopolises selection, so the closure latch only arms when F collapses (weak-natural-commit regime), and the sustained occupancy MECH-446 needs to de-commit FROM only forms in the strong-natural-commit regime. -> `/implement-substrate` (the `f_dominance_conversion_ceiling` amend, de-commit-release face).

**(c) clean single-point falsification?** NO. Seed-44 (F-independent commit-intent) and seed-42 (460h within-arm 0.333->0.0) are positive existence proofs of the correct sign. The claims are not wrong.

**(d) granularity debt (>= 2 distinct, genuine, non-degenerate, SUBSTRATE-READY signatures)?** **NO.** After filtering the substrate-not-ready FAILs (all six) and the measurement-entanglement half (b/S5-descendant), **zero substrate-ready granularity-debt signatures remain.**

### Step-1 already-metabolized exclusion -- all four disjuncts fire

- `epistemic_category: substrate_ceiling` on BOTH children (the `_EPI_SUPPRESS_PROPOSAL` cheap first pass).
- Active `substrate_queue` amend: `f_dominance_conversion_ceiling` (priority 1) lists MECH-445/446 in `unblocks_claims`; status carries `decommit_release_duration_face_rung6_460_lineage_readiness_pending`.
- Registered children: MECH-445/446 ARE the registered SD-034 children (06-19); MECH-446 `depends_on` MECH-445.
- In-progress closure-plan GAP: `commitment_closure:GAP-4` is in-progress tracking exactly this (b)-lineage, gated on the same amend ("under active build by 2 concurrent /implement-substrate sessions").

**STOP confirmed.** Report the real cause (substrate-not-ready + measurement entanglement); do not manufacture claims.

---

## 4. The finding the refusal exposes -- the hidden asymmetry

The two children now have **DIVERGED epistemic status that the shared `substrate_ceiling` tag flattens**:

| | MECH-445 (commit-INTENT existence) | MECH-446 (de-commit MAGNITUDE) |
|---|---|---|
| Old blocker | structurally unreachable (pinned at 0) | unmeasurable under refractory |
| 460o/p effect | **RESOLVED** -- F-independent arming demonstrable (seed 44) | not dissolved |
| Current blocker | regime-scoped + **under-powered 3-seed test** | needs closure-*driven* (not F-driven) sustained occupancy = genuine substrate |
| Standalone falsifier | YES (commit-intent counter > 0 on >= 2/3 in-regime seeds; no MECH-446 dependency) | NO -- requires co-located occupancy the substrate does not yet produce |
| Distance to advance | **close** (test-power move) | **far** (substrate build) |

Treating them as one blocked unit is what stalled 6 iterations. The forward program is to **split the paths**: sprint the close one, cheap-probe the blocked one.

Confirmed by the 715 config (`experiments/v3_exq_715_..._falsifier.py`): the only variable was `use_closure_commit_entry`; the **selection-face ceiling-lift already built+validated+promoted (MECH-448 rank-preserving F->eligibility demotion + MECH-449 Go/No-Go) was switched OFF**, and the run used only 3 seeds [42,43,44] with a 2/3 gate -- structurally unable to certify a regime-scoped claim when ~1 of 3 seeds is in-regime.

---

## 5. Forward-moves menu (the hand-off -- this is the payload of the refusal)

Ranked; most need no new claim and no big build.

- **M1 -- Turn on the ceiling-lift already paid for.** Re-run the de-commit falsifier with MECH-448/449 demotion **ON** (verified OFF in 715). Hypothesis: the moderate-F regime it creates makes weak-natural-commit the norm across seeds, so MECH-445 arms on >= 2/3 AND MECH-446 gets a closure-driven occupancy to de-commit from -- potentially unblocking BOTH with zero new build. Frame to the `f_dominance_conversion_ceiling` `/implement-substrate` owner as "does the selection-face lift double as the de-commit-release substrate?" Brake-safe: a NEW substrate configuration, not a same-selector re-derive. Route: `/queue-experiment` (new letter) coordinated with implement-substrate.
- **M2 -- Decouple + sprint MECH-445 alone (cheapest promotable win).** A MECH-445-only test: 9-12 seeds, filter to the weak-natural-commit subset (OFF-arm committed_steps ~ 0), score the refractory-independent commit-intent counter > 0 on >= 2/3 of THOSE in-regime seeds. Converts the narrow 1/3 existence proof toward supported with NO substrate. The 3-seed/2/3 design cannot certify a regime-scoped claim; this fixes test power, not the substrate. Route: `/queue-experiment`.
- **M3 -- Turn MECH-446 vacuity into graded data.** Re-anchor the DV: measure the LAG distribution between SD-034 closure fires and latch-armed occupancy (and/or anchor the pre/post window on the arming tick). "Vacuous 0/3" becomes a number telling implement-substrate exactly how much co-registration the amend must buy. Guard: preserve closure-caused attribution or it degrades to a vacuous pass. Route: `/queue-experiment` DV redesign (guarded).
- **M4 -- Off-piste: prove MECH-446 magnitude in a probe rig.** Test the de-commit magnitude in a controlled environment where a sustained committed occupancy + a co-located closure fire are injected by construction (unit-test-like), decoupled from foraging. A clean capability existence proof that de-risks the substrate build. Label strictly capability-not-ecological (non-scoring toward foraging support). Route: `/queue-experiment` probe.
- **M5 -- Retire the over-specified joint gate.** The "co-occurrence on the SAME seed" criterion is over-specified (the MECH-341 retired-falsifier pattern). Certify 445 in weak-F and 446 in strong-F separately. Removes an artificial coupling; does NOT fully rescue 446 (still needs closure-driven occupancy), so pairs with M1/M3/M4. Route: test-design correction.
- **M6 -- Governance bookkeeping that unlocks the above.** Record MECH-445's partial release (blocker moved from "structurally unmeasurable" to "regime-scoped + under-powered test"); split its epistemic status from 446's. The `evidence_quality_note` text is already drafted in `failure_autopsy_V3-EXQ-715_2026-07-06.json` (`recommended_evidence_quality_note_MECH-445` / `_MECH-446`). Route: `/governance` applies the drafted notes.

**Recommended near-term package:** M6 (bookkeeping) + M2 (445 sprint) + M1 (flip the built lift). None need a big build; all are brake-defensible; together they attack the asymmetry directly. M3/M4 are the fallbacks if M1 does not co-locate.

---

## 6. Routing / hand-off

- **PRIMARY (unchanged, validated by this refusal):** `/implement-substrate` on `f_dominance_conversion_ceiling` (de-commit-release face) per the 715 autopsy. This refusal REINFORCES the autopsy's primary route over its secondary.
- **Governance:** apply the drafted `evidence_quality_note`s for MECH-445 (narrow non-scoring positive + partial release) and MECH-446 (starved-not-falsified) via `/governance`; keep both `candidate` / `pending_retest_after_substrate`. Consider a note distinguishing MECH-445's blocker (test-power/regime) from MECH-446's (substrate) so the shared `substrate_ceiling` tag stops flattening the asymmetry.
- **Experiments:** M1/M2 via `/queue-experiment` (spawned as follow-on chips this session; not queued here).
- **Brake:** re-derive brake stays FIRED for the same-claim de-commit-MAGNITUDE face; M1/M2 are NEW substrate-config / test-power designs, not same-selector re-derives.

## 7. Discipline checklist (self-audit)

- [x] Discrimination gate run; classified substrate-not-ready + measurement entanglement AFTER ruling out granularity debt.
- [x] STOP honoured -- no children proposed, no `claims.yaml` edit, no `claims.json` regen.
- [x] Referral hypothesis checked against the live registry: already realized by the 2026-06-19 decomposition (not fresh debt).
- [x] All four Step-1 already-metabolized exclusion disjuncts verified firing.
- [x] Forward reroute (M1-M6) provided so the refusal is a routing doc, not a dead end.
- [x] Proposal-first respected: user-approved disposition (write refusal record + spawn chips; hold on queuing).
