# Failure autopsy -- V3-EXQ-1000 (SD-e1 ITEM 3 validation: rollout-endpoint contrastive)

**Status:** awaiting_human_confirmation (drafted 2026-09-04 by session `failure-autopsy-v3-exq-1000-20260904`; red-team CONTESTED and accepted, artifact revised; Step 8 gate owed).
**Scope:** single. **Trigger:** `experiment_purpose: diagnostic` PASS, indexer-flagged `vacuous_pass`.
**Run:** `v3_exq_1000_sd_e1_item3_rollout_endpoint_contrastive_validation_20260903T213659Z_v3` (queue V3-EXQ-1000; ree-cloud-2; 904 s; 6 seeds x 4 arms; claim_ids `[]`; unblocks MECH-135 / INV-088).
**Machine-readable twin:** `failure_autopsy_V3-EXQ-1000_2026-09-04.json`.

## 0. One-paragraph verdict

The self-routed label `rollout_endpoint_contrastive_lifts_cr_ratio_h1` is accurate under the driver's own rule. ITEM 3's rollout-endpoint InfoNCE (`ARM_RSD`) is the first arm in the lineage to clear an evaluator bar at majority-seed level: `cr_ratio(h=1)` = 0.771 / 0.995 / 0.846 / 1.66 / 1.41 / 1.81 on seeds 42 / 123 / 7 / 2024 / 17 / 31, over the 0.1 bar on 6/6 seeds by 7.7x-18x and over the depth-0 incumbent by 56x-2714x. The second bar, `e1coe_score_var(h=1) >= 0.002`, is missed on 6/6 seeds by 36x-1700x (1.2e-06 .. 5.5e-05), so the conjunctive C2 is 0/6 and no cell of any arm reaches both at h=1. The indexer's `vacuous_pass` is a manifest-grammar hit (status PASS assigned unconditionally on readiness while C2 is tagged `load_bearing: true`), not scientific vacuity: the discrimination is real, unanimous and control-passed. The h=1 signature, read from the run's own cells (and corrected after red-team): the RSD endpoints have a **shrunk centroid** (norm 0.54-0.81 against 1.41-2.17 for the same-start incumbents and 1.39-1.89 for the real samples) and are **under-dispersed** (spread 0.33-0.62x real, 6/6). The ratio >= 0.77 is spread over a shrunk centroid; dividing the same spread by the real centroid norm gives 0.115 / 0.081 / 0.080 / 0.167 / 0.138 / 0.103, still over 0.1 on 4/6 seeds, so the bar clear survives the correction at majority level. Over-dispersion appears only at depth (h >= 5, 4/6 seeds). At depth the var bar **is** reached: `ARM_RSD` seed 2024 at h=10/20/30 has var 2.4e-03 / 2.7e-03 / 2.9e-03 with cr 2.75 / 3.51 / 3.13 and centroid inside the real band, at 3-3.7x real spread. Whether the bar is reachable at *real* spread has never been measured in the lineage. Routing: `queue-experiment` portfolio on a new question (fidelity anchor / readout demands over-dispersion / goal-orthogonal dispersion) with a substrate-entry amend recording ITEM 3's validation; a lettered re-run on the same bars at h=1 is refused. MECH-135 / INV-088 stay `pending_retest_after_substrate`.

## 1. Facts (Step 2)

**Dry-run gate.** `check_dry_run_citations.py` over the target: 0 dry cited, clean. No dry manifest in scope. `validate_experiments.py --checks dry_run_unreachable_criterion`: no finding for this driver. `dry_run_checked: true`, `excluded_dry_run_ids: []`.

**Recording provenance.** `validate_recording.py`: OK, always-core complete (`rec/v1`, `substrate_hash`, machine/class, `elapsed_seconds`, `config`, `seeds`).

**Design (driver docstring).** Four arms, all ITEM-1 ON: `ARM_OFF` (depth-0 single-step MSE, the routed incumbent), `ARM_ANCHOR` (V3-EXQ-968 stateful + `output_proj_residual`), `ARM_RC_DECAY` (ITEM 2 `rollout_consistency_loss`, H=5, decay 0.5), `ARM_RSD` (ITEM 3 `rollout_sequence_divergence_loss`, K=8 replay windows, H=5, tau 0.1). Uniform trailing-window trigger gives matched E1 grad steps by construction (`n_e1_grad_steps` 2588-2733 per seed, gap 0). Primary DV `cr_ratio(h=1) = CR_rollout/CR_real`, relative lift vs `ARM_OFF` against a fixed bar 3.0 on >= 4/6 seeds; absolute bars `cr_ratio(h=1) >= 0.1` AND `e1coe_score_var(h=1) >= 0.002` route the strongest label independently. Eight readiness preconditions incl. `rsd_objective_engaged` and the red-team-added `rsd_action_sensitive` (row-permuted action control, floor 1.05).

**Readiness (all met, recomputed by the indexer's numeric path).** encoder_trained 0.23 > 0; CR_real(h=1) n=40 every cell (0.18-0.27); missing action calls 0; direct supply 1.0; cr_ratio(h=1) finite > 0 (min 5.4e-04); grad-step gap 0; `rsd_objective_engaged` 1.0 on every seed (`rsd_n_engaged` 2587-2732, 0 degenerate, 1 buffer-starved); `rsd_action_sensitive` 1.382 / 1.316 / 1.417 / 1.519 / 1.430 / 1.332 vs floor 1.05.

**Criteria.** C1 `C1_rsd_relative_lift` (load_bearing, passed): `ARM_RSD` lifts on 6/6 seeds (verdict `lifts`, sign p = 0.0156). C2 `C2_rsd_absolute_bars` (load_bearing, **passed: false**, 0/6). C3 `C3_evaluator_bar_reached_any_arm` (not load-bearing, 0/24 -- the code tests h=1 only, driver:1620-1627, though the statement does not say so). `criteria_non_degenerate` all true. `status: PASS`, `evidence_direction: non_contributory`.

**Per-cell numbers (hybrid readout, h=1 unless stated).**

| arm | seed | cr_ratio h1 | lift vs OFF | spread h1 (x real) | centroid_norm h1 | var h1 | growth h1->h5 | cr h30 hyb / E1-alone |
|---|---|---|---|---|---|---|---|---|
| OFF | 42 / 123 / 7 / 2024 / 17 / 31 | 1.1e-03 / 5.4e-04 / 1.5e-02 / 1.9e-02 / 9.7e-04 / 6.7e-04 | 1 | 2e-04 .. 8e-03 | 1.45-2.10 | 5e-11 .. 1.5e-06 | 2.5-5.5 | 0.002-0.09 / same |
| ANCHOR | same | 2.5e-03 / 1.6e-03 / 3.2e-02 / 0.18 / 3.5e-03 / 3.4e-03 | 2.2 / 2.9 / 2.1 / 9.6 / 3.6 / 5.1 (mixed) | 7e-04 .. 7.6e-02 | 1.57-2.17 | 8e-10 .. 8e-05 | 1.6-12 | 0.025-0.22 / 0.001-0.04 |
| RC_DECAY | same | 3.1e-03 / 3.1e-03 / 1.0e-02 / 6.3e-03 / 8.8e-04 / 2.0e-03 | 2.8 / 5.8 / 0.7 / 0.3 / 0.9 / 3.1 (mixed) | 3e-04 .. 5e-03 | 1.41-2.16 | 6e-10 .. 1e-06 | 0.83-1.5 | 0.001-0.015 / same |
| **RSD** | same | **0.771 / 0.995 / 0.846 / 1.66 / 1.41 / 1.81** | **693 / 1838 / 56 / 88 / 1457 / 2714** | 0.15-0.24 (**0.33-0.62x real**) | **0.544-0.810** | 1.2e-06 .. 5.5e-05 | 0.83-2.68 | 0.022-5.69 / 0.67-5.75 |
| real (Phase 4b) | -- | CR_real 0.18-0.27 | -- | 0.34-0.46 | **1.39-1.89** | never recorded | -- | -- |

Centroid-corrected `ARM_RSD` cr(h=1) (spread / real centroid norm): 0.115 / 0.081 / 0.080 / 0.167 / 0.138 / 0.103 (4/6 >= 0.1). RSD/real spread ratio by horizon: h=1 0/6 above 1; h=2 1/6; h=3 3/6; h>=5 4/6 (seeds 123/2024/17/31; seeds 42 and 7 below real at every h). Trained-horizon (h=5) `ARM_RSD` lifts 22x-2072x (6/6). **Both bars reached at depth:** `ARM_RSD` seed 2024 h=10/20/30 var 2.44e-03 / 2.65e-03 / 2.87e-03, cr 2.75 / 3.51 / 3.13, centroid 1.75 / 1.70 / 1.72 (real 1.63 / 1.64 / 1.60), spread 1.25-1.55 vs real 0.42-0.46; near misses seed 2024 h=5 (1.09e-03) and seed 17 h=10 (1.88e-03). Seed 42's hybrid rollout collapses from h=10 (0.136 -> 0.017 -> 0.022; E1-alone 0.67). One-step per-action probe: `ARM_RSD` mean pairwise 0.22-0.36 vs `ARM_OFF` 7.1e-04 (no external z_world per-action reference exists; E2's 2.8e-02 is a z_self figure and is not used here). RSD training loss 1.71-1.86 (first fifth) -> 1.18-1.39 (last fifth) against uniform log(8) = 2.08; still falling.

**Substrate provenance.** Two per-cell substrate hashes: every `ARM_OFF` cell 5a198a8e8ccf (resolved 21:22:21Z), every ON-arm cell d7fe936d33f0 (21:24:44Z). Both reproduce from origin in throwaway worktrees: 5a198a8e = `origin/main@5f88f7f27c` substrate globs (= the manifest's top-level hash); d7fe936d = the same tree plus the driver script, exactly as `include_driver_script_in_hash=(arm != OFF_ARM)` (driver:1374) prescribes. The only ree_core commit in the run window (2b34521, `offline_integration` cat-dim fix, 20:45Z) is inside the tree both keys hashed and touches a path this driver never calls. `substrate_stable_across_run: false` is a per-cell hash-cardinality trip: the manifest's `substrate_stability_detail` stamps `per_cell_hashes_disagree: true` and `process_snapshot_drift: []` (nothing moved on disk), and `manifest_core.py:1182` sets `stable = report.stable AND NOT cells_disagree`. This is the pattern `failure_autopsy_V3-EXQ-976` learning #6 already recorded; this driver repeated it. Consequence: arm reuse refuses this run's OFF arm (cost only). Not a confound: executed code is import-frozen at process start. The manifest's `hypothesis_space_qid` stamp (`inv088_evaluator_degeneracy_cause`) is a stale lineage-template inheritance; no script joins on it.

## 2. Claim layer (Step 3)

`claim_ids: []` by design; the run validates the substrate entry `SD-e1-rollout-consistency-training`, which unblocks MECH-135 (candidate, standard, pending_retest_after_substrate true, diagnostic_evidence_adjudicated true) and INV-088 (candidate, emergent invariant, same flags). Neither is exercised: the E3-consumer DV (`e1coe_score_var`) has not moved at h=1, so the substrate is not retest-ready. Nothing on either claim changes (`per_claim_recommendation`: STANDS, both). Re-derive brake: this target counts toward no claim under R1-R3 (MECH-135 has 1 prior hit, INV-088 has 2, both from the unrelated MECH-457 competence portfolio); the lineage's diagnostics each routed a different, now-landed substrate step -- the brake's exempt case. A lettered re-run of 1000 on the same two bars at h=1 is nevertheless **refused** (Section 6).

**Granularity-debt trigger: does not fire.** Claim-free target; `granularity_debt_cluster.py` for MECH-135 shows no `weakened` target (1 hit, 108a) and INV-088's `weakened` targets are the MECH-457 competence cluster, a different failure family.

## 3. Biological-reference triage (Step 4)

The closest mechanism is hippocampal-prefrontal constructive simulation of alternative futures: imagined endpoints of candidate action sequences must be discriminable across options and faithful to world structure. The commissioned lit-pull (`targeted_review_e1_forward_model_rollout_consistency`, 2026-08-03) grounds the architecture (Hassabis 2007: constructive simulation is a separable system that fails on its own; Addis & Schacter 2008: recruitment scales with simulated remoteness) and says explicitly that no paper makes contrastive objectives a fix for long-horizon rollout collapse. The rollout-endpoint InfoNCE is a formal import. Its measured signature -- shrunk, under-dispersed endpoints at h=1, over-dispersed at depth on 4/6 seeds, the goal readout reached only at depth on one seed -- is divergence detached from fidelity, the inverse of ITEM 2's collapse. Biology's simulation is generative-and-anchored; a pure discrimination pressure with no surviving accuracy term is not what the reference system does. The divergence is load-bearing for the next build (divergence and accuracy trained jointly), not a reason for a new lit-pull: `lit_status` present for the family, absent specifically for an endpoint contrastive, and the gap is characterised by this run's own data.

## 4. Four-layer diagnosis (Step 5)

| Layer | Status | Notes |
|---|---|---|
| Claim alignment | n/a | claim-free diagnostic; MECH-135 / INV-088 keep `pending_retest_after_substrate` |
| Biological reference | partial | formal import; architecture grounded, no biology for an endpoint contrastive; signature = divergence detached from fidelity |
| Prerequisites | present | ITEM 1 ON everywhere (965); ITEM 3 substrate in the executed tree; 8/8 readiness met |
| Implementation | complete (as designed) | objective engaged 100% of ticks, action-sensitive 1.32-1.52, gradient reaches trunk; no accuracy term survives a confident K-way ordering -- the h=1 centroid shrink and depth over-dispersion are its licensed degeneracy, not a bug. WITHDRAWN after red-team: the first draft's "over-dispersed at h=1" reading and its comparison against E2's 2.8e-02 (a z_self figure) |
| Environment | adequate | healthy real denominator (CR_real 0.18-0.27, n=40 every cell) |
| Measurement | partial | (i) PASS unconditional on readiness + C2 load_bearing -> `vacuous_pass` by construction; label has no branch for one-of-two bars; C3 tests h=1 only while its statement does not say so, and seed 2024 clears both at h>=10; (ii) RECORDING GAP: real-endpoint goal_proximity variance never written (Phase 4b collects the samples); (iii) MEASUREMENT GAP: no endpoint-fidelity readout, no centroid-norm band as a scored quantity; the "real" comparison is 40 one-start rollouts vs 40 different-reset trajectories (lineage convention), so the same-start incumbents are the stronger centroid reference; the var/spread^2 contrast is a fragile ratio of near-zero quantities (3/18 incumbent cells below RSD's max) confounded with the readout slope -- hypothesis basis only |
| Integration | partially coupled | hybrid vs E1-alone sign at h=30 is mixed on the RSD arm (3 lower / 3 higher); seed 42 hybrid collapses from h=10 |
| Scale / capacity | adequate at h=1; dose unconverged | n=6 unanimous; RSD loss still falling; direction of more dose is open (under-dispersed at h=1, over-dispersed at depth on 4/6) |

**Failure-location (GOV-FAILLOC-1): MIXED -- MECHANISM-design + MEASURES; not chargeable to REE.** Mechanism partial (class confirmed for cr_ratio, no surviving accuracy term), measures partial (bar reached only at 3-3.7x real spread; real-endpoint variance unrecorded; C3 blind beyond h=1), environment established, `ree: false`.

**Epistemic category: `standard`** (claim-free; asserts no suppression). The failure mode -- `measurement_test_design_defect` on the label/PASS grammar plus a `recording_gap` on the var bar's reference -- lives in the notes, not the enum.

## 5. Learning extracted (Step 7)

1. A `lifts` label composed from a conjunctive absolute bar hides a cleared bar; separate the bars in the label rule, and read both at every horizon, or a depth cell that clears both (seed 2024 h >= 10) is invisible to every criterion.
2. PASS assigned unconditionally on readiness plus a fail-able `load_bearing: true` criterion is a guaranteed `vacuous_pass` (980's learning, repeated); tag classifying criteria `load_bearing: false` or declare `criteria_aggregation`.
3. A contrastive endpoint objective with squared-L2 logits and no accuracy term that survives a confident ordering is satisfied by shrunk predictions: at h=1 the centroid collapses to 0.3-0.5x the incumbents' norm while spread stays below real; at depth the same objective over-disperses on 4/6 seeds. Train divergence and fidelity jointly.
4. `cr_ratio = spread / centroid_norm` is scale-free and cannot see a shrunk centroid: dividing the same spread by the real centroid norm turns 0.77-1.81 into 0.08-0.17. The bar clear survives that correction on 4/6 seeds, but report the corrected figure beside the raw one and pair the ratio with a centroid-norm band.
5. After six experiments the lineage has never recorded the goal-proximity variance of its own real endpoints; the 0.002 bar is reached in this run only at 3-3.7x real spread, so whether it demands over-dispersion by construction is unknown. Record the denominator-side reference before chasing the bar with objectives.
6. The mint-as-you-go convention (`include_driver_script_in_hash=(arm != OFF_ARM)`) trips `substrate_stable_across_run` on every run that uses it -- 976 recorded this and 1000 repeated it. The fix is in `arm_fingerprint`/`manifest_core` (compare like scopes, or name the cause in the flag), not per driver; until then read `substrate_stability_detail.process_snapshot_drift`, not the bool.
7. A first-draft mechanism reading built on a cross-module reference (E2's z_self divergence as a z_world dispersion reference) and on a ratio of near-zero quantities did not survive a same-manifest recompute. Withdrawn and recorded: the spread-vs-real check is the one to run before writing "over-dispersed".
8. 980's sign-extrapolation ("hybrid reads higher than E1-alone") to a divergence-preserving objective does not hold uniformly; read-across from accuracy arms to contrastive arms is not free.

## 6. Repair pathway and routing (Step 7) -- pending the Step 8 gate

**Node class: `complex (probe-gated) / puzzle (known rules)`.** The frame is well-posed; the missing fact is whether the var bar is reachable at real spread, and whether the residual is the centroid shrink (fidelity), the bar's demand for over-dispersion (readout), or goal-orthogonal dispersion. Three live hypotheses -> **GOV-FANOUT-1 portfolio**, one new EXQ:

- **H-fidelity-anchor** (learning-signal, LEAD): `ARM_RSD_ANCHOR` = InfoNCE + endpoint-accuracy term (ITEM 2 loss or plain endpoint MSE; both landed -- a driver-level composition, no new substrate); readouts at every horizon: centroid-norm band vs real and vs same-start incumbents, spread ratio vs real, endpoint-fidelity MSE for the same (start, sequence) via `copy.deepcopy(env)` branching at eval only, both bars per horizon. Null: centroid stays outside the real band on a majority of seeds at h=1 OR var within 2x of `ARM_RSD`'s.
- **H-readout-saturation** (measurement, cheapest, gates the objective work in its narrowed form): record goal_proximity AND goal_distance variance over the 40 real endpoint samples per horizon, plus each arm's mean goal distance and spread ratio vs real. Null: real-endpoint variance >= 0.002 on a majority of seeds.
- **H-goal-orthogonal-dispersion** (representation): fraction of spread variance along the unit goal axis, real vs each arm. Null: `ARM_RSD`'s fraction within 2x of real.

**Routing: `queue-experiment`** (the portfolio above, new EXQ number). **Substrate hand-off: `amend` `SD-e1-rollout-consistency-training`** -- ITEM 3 validated as *cr bar cleared at h=1 (4/6 after centroid correction), var bar open at h=1, both bars reached at depth on one seed at 3-3.7x real spread*; failure record appended; status string `item3_validated_cr_bar_cleared_var_bar_open_at_h1`; `status_phase` stays `validation_owed`; severity / paths / priority / unblocks unchanged; the 108b, 965 and 976 records stay `open` (their targets conjoin both bars). **Refused:** any lettered iteration of 1000 on the same two bars at h=1 against the same objective.

**Draft `evidence_quality_note`** (for the SD entry's validation record; MECH-135 / INV-088 notes unchanged): see `recommended_evidence_quality_note` in the JSON twin.

## 7. Mechanical checks (Steps 7b, 7c)

**7b** (`autopsy_pre_routing_checks.py`, run twice -- before and after this narrative existed): 0 fires; C1/C2/C3 inapplicable (no `claim_ids`); C5 and C6 applicable on the second pass and quiet. Inapplicable is not clean -- 7c carried the load.

**7c red-team (Fable 5.1, separate agent, reasoning withheld, JSON-first): CONTESTED, contest ACCEPTED.** All headline numbers recomputed and matched. Five findings, all confirmed by the drafter's own cheap confirmers and applied: F1 "over-dispersed at h=1" was false (RSD spread 0.33-0.62x real, 6/6; the >1 ratio is the centroid shrink; the E2 2.8e-02 figure is z_self, not a z_world reference) -- corrected throughout; F2 the both-bars target IS reached at h=10/20/30 on seed 2024 inside the real centroid band (C3 tests h=1 only) -- absolute withdrawn, H-readout-saturation narrowed to "reachable at real spread?"; F3 var/spread^2 downgraded from mechanism to hypothesis basis; F4 the stability flag is a per-cell cardinality trip with `process_snapshot_drift: []`, already 976's learning -- provenance and learning rewritten; F5 the 965 "fully-resolved parent" precedent withdrawn, parent `decidable` left false, stale `hypothesis_space_qid` noted. What survived: the central adjudication, the routing shape, and the ledger confirmation of `H-objective-class-divergence` (the lift is real at the spread level, not only in the ratio). Full report: `red_team` block in the JSON.

## 8. Ledger (Step 9b) -- DRAFTED ONLY

See `hypothesis_space_ledger_pending` in the JSON: Mode B resolves `H-objective-class-divergence` (confirmed) and annotates `H-training-objective` / `H-readout-regime` on `sd_e1_residual_crush_locus` (no `growth_restriction` present at draft time; `decidable` stays false with 3 legs alive); Mode A registers the new question `sd_e1_var_bar_readout_crush` with three pre-registered legs (different-statistic ground; the parent is not fully resolved, so the 965 precedent is not claimed; the lead leg's learning-signal overlap with the parent is stated). Nothing is written to the registry until the gate is held.

## 9. Questions for the Step 8 gate

1. Accept the adjudication: label accurate, `vacuous_pass` = grammar, C2 fail at h=1 = the substantive finding (cr bar cleared 6/6 raw, 4/6 centroid-corrected; var bar untouched at h=1, reached at depth on one seed at 3-3.7x real spread)?
2. Accept routing `queue-experiment` portfolio (fidelity anchor / readout demand / goal-orthogonal) with the SD amend, and the refusal of a lettered re-run?
3. Ledger: confirm `H-objective-class-divergence` and register `sd_e1_var_bar_readout_crush` as a NEW question (not growth on `sd_e1_residual_crush_locus`), leaving the parent `decidable: false`?
4. Should the h=1 centroid shrink be read as a fidelity defect of the objective (the LEAD leg), or is there a reason to weight the readout-demand leg first?
